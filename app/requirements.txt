# macro API (serving tier) — minimal, NOT the heavy engine stack.
# Keep this lean: the API wraps build artifacts + (later) imports pure engine
# functions on demand. Heavy deps (pandas/scipy) are only pulled if/when a route
# needs them.
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30
pydantic>=2.0,<3.0
# The public Company Intelligence reader verifies immutable R2 objects with a
# bounded server-side requests stream.  It is imported by the public ticker
# context router at API startup, so this is a serving-tier dependency rather
# than a worker-only convenience.
requests>=2.31,<3.0
# Reviewed serving-boundary policy (config/site_access.yml). Imported at API
# startup by app/paywall.py, so this is a hard serving-tier dependency.
PyYAML>=6.0,<7.0
# BioCatalyst's serving router re-validates the pointer-bound public projection
# through the shared sector contracts before returning any record. These are
# hard API startup dependencies, not worker-only packages.
jsonschema==4.26.0
referencing>=0.30,<1.0
# Capital Structure uses the same validator for its canonical observed-filing
# projection, so this single pin is a hard serving dependency for both desks.
# Private Research Vault / Filing Forensics object reads. The serving process
# imports boto3 lazily, but it is a hard dependency once a paid R2-backed route
# is requested; pin it here so a fresh API install cannot degrade to 503.
boto3>=1.34,<2.0
# Auth is secretless (verifies tokens via Supabase /auth/v1/user using stdlib
# urllib) — no JWT library needed.
# W8b PR2: ask-the-brain — LLM client for the request-scoped cortex loop.
# Key-optional: the anthropic client is only instantiated when ANTHROPIC_API_KEY
# is present in /etc/macro-api.env.  The endpoints fall back to memo-quote mode
# when the key is absent.
anthropic>=0.28,<1.0
# Optional GeoIP country lookup for the site-access gate.
# The gate imports maxminddb lazily; startup succeeds if the library is absent.
maxminddb>=1.4
# Inline chart rendering (W6c): render_inline_chart tool in brain_gateway calls
# engine.marketing.chart_render which needs pandas + pyarrow to load parquet bars.
# Imported lazily inside _chart_for_chat — startup succeeds if absent (chart
# gracefully unavailable rather than crashing the API process).
pandas>=2.2
pyarrow>=16
# Research Vault download watermark (RV W2): stamp a per-user footer on downloaded
# PDFs. SOFT deps — engine/research_vault/watermark.py imports them lazily and
# degrades to the un-watermarked original bytes if either is absent (a failed
# stamp must never block a paid download).
pypdf>=4.0
reportlab>=4.0
# Error/trace reporting (app/observability.py). SOFT dep by construction: the
# init helper catches ImportError and no-ops, so a fresh box whose pip step has
# not landed yet still serves /api. Pinned >=2.35 because `enable_logs` only
# graduated to a top-level init kwarg there (the helper degrades to the
# portable option set if an older wheel is what actually gets installed).
# Auto-instruments FastAPI/Starlette/logging — no per-route code.
sentry-sdk>=2.35,<3
# Billing spine (MNZ W2): app/billing.py — Stripe Checkout + webhook + portal +
# reconciler. Needs the Entitlements API (GA), so pin >=11. Imported lazily inside
# app/billing.py so the API process still starts if the dep is briefly absent.
stripe>=11,<13
