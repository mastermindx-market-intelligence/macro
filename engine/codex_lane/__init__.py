"""engine/codex_lane — Codex research lane: runner + budget core (CRX-B).

Public surface (frozen cross-lane API):
    from engine.codex_lane.runner import resolve_codex_bin, run_codex, fetch_rate_limits
    from engine.codex_lane.budget import load_cfg, load_state, can_run, note_result, note_rate_limits
"""
