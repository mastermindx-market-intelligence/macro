# W14 r2 forward crops — sector_central_china

Provenance: `capture_sha` == code HEAD `21546c85ed8af3e286abfdd4214a8c99b607efe9`.
Rig: S1 fixture-render + real `body.page-sector-central`; `__skyDeck`; overlay hide
`.rvx-aurora` / `.mx5-aurora` / `.sky-fx` / `#mmb-root` / `#mmb-boot` / `.aurora`;
overlay column populated (`clean` ×6).

Band decision: SIMPLIFIED. The r1 band mixed four idioms (tagged link + 42ch prose
note + lens `?` + four icon pills). The note is demoted into the LENS tip; the
band is tagged links + the `?`. Crops `silinks-dark-en-1440` /
`silinks-light-en-1440` / `silinks-dark-zh-1440` record that one idiom.

| id | why |
|---|---|
| silinks-dark-en-1440 | new `.si-links` band, dark EN |
| silinks-light-en-1440 | same band, light paper treatment |
| details-open-dark-en-1440 | `.si-more` open landing, dark EN |
| mxerror-light-en-1440 | `.mx-error button` light EN |
| silinks-dark-zh-1440 | ZH cell of the simplified band |
| details-open-light-en-1440 | `.si-more` open landing, light paper |
