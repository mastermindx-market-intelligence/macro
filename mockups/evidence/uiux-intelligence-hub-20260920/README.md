# Intelligence Hub: usable mobile Command rows

Before: View all squeezed the explanation into the final narrow grid column. Two current watch conditions rendered Python dictionary representations, including structured machine check fields.

After: On screens up to 620px the identity and unchanged score occupy the first row; the explanation spans the remaining columns below. The existing percentage rail spans both rows. The watch condition renders the source's human text, uses supplied Chinese text when available, preserves legacy strings, escapes markup, and reports unsupported shapes as unavailable. The structured source checks, ranking, scores, prices, number/order of entries and desktop grid remain unchanged.

`verify_interactions.py` opens the real View all control and closes it with Escape. All eight desktop/mobile × English/Chinese × dark/light cells passed. The mobile explanation occupies 84.6% of the row rather than one narrow final column. All 30 entries remain reachable; the two current human watch conditions are preserved. No private renderer invocation, synthetic trading data or membership impersonation is used.

`manifest.json` and `cells/` come from the existing `scripts/capture_page_evidence.py` owner, using its current protected-main code. `interaction-results.json` and `*-view-all.png` capture actual interaction states. Five new regressions first failed on the previous candidate; the owned Intelligence Hub suite then passed 136 tests.

Delivery remains on the existing PR #7361 and the existing VPS updater. No Vercel deployment or retry. New production proof is still owed after exact-head CI and merge; local/browser proof is not production acceptance.
