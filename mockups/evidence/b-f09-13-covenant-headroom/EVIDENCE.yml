schema: mastermind.page_evidence_receipt.v1
packet_slug: b-f09-13-covenant-headroom
captured_at: "2026-09-19T01:31:58Z"
captured_by: HEAL-h3-7127
manifest: manifest.json
manifest_sha: 4993b0c4f74ad2e3c7f55e5591e43b186fc42ea8
# Git commit SHA at capture; HEAD^{tree} = 3bd6070bd9129cd5704972a0ed1ee0e7a576abf4
# for this packet's tree state. The packet is committed as the last evidence
# commit on branch claude/mo-b-f09-13-covenant-headroom.
fixture_render: true
fixture_render_disclosure: >
  The spec R7(a) fixture was rendered by the committed evidence recipe script,
  which invokes the production Jinja2 template and engine; 16 PNG cells were
  captured from that HTML via Playwright/Chromium at dark/light × EN/ZH ×
  1440/390. The page's own setTheme and setLang routines set the presentation
  state, and the recipe fails if the resulting attributes do not match; no
  hidden decorative layers; settled animations; opacity 1. The HTML files in
  rendered-html/ remain the deterministic source for every PNG cell the spec
  names, and a browser that opens them reproduces the 16 cells identically.

# Spec R9 — 16 cells: 8 null + 8 computed (fixture-rendered)
# dark/light x EN/ZH x 1440/390 (desktop + mobile)
# All 16 documented in manifest.json under `cells`.

render_method: fixture_render_via_evidence_recipe_script
template: templates/capital_structure.html.j2
render_recipe_script: mockups/evidence/b-f09-13-covenant-headroom/_render_panel.py
engine: engine/covenant_headroom.py
panel_selector: "#cs-covenant-room"
panel_title_en: "How much room is left under the covenant"
panel_title_zh: "契约限额下还剩多少空间"

states:
  - id: null
    html: rendered-html/null.html
    payload: payloads/null.json
    schema: capital_structure.covenant_headroom.v1
    state_field: refused
    refusal: no_terms_extracted
  - id: computed
    html: rendered-html/computed.html
    payload: payloads/computed.json
    schema: capital_structure.covenant_headroom.v1
    state_field: computed
    fixture: >
      engine/covenant_headroom.compute_headroom over
      tests/fixtures/capital_structure/covenant_credit_agreement_submission.txt +
      synthetic CRSR FY row 2022-12-31 (debt_lt 250M, debt_cur 10M, cash 80M,
      op_income 60M, depreciation 20M, interest_exp 15M).

render_evidence:
  page_theme_routines: true
  presentation_attributes_verified: true
  no_hidden_decorative_layers: true
  settled_animations: true
  opacity: 1

# Spec R7 (changed_paths) — every file the panel touches or its tests cover,
# pinned for the reviewer to diff against the final committed head.
changed_paths:
  - engine/covenant_headroom.py
  - templates/capital_structure.html.j2
  - tests/test_covenant_headroom.py
  - mockups/evidence/b-f09-13-covenant-headroom/_render_panel.py
  - mockups/evidence/b-f09-13-covenant-headroom/rendered-html/null.html
  - mockups/evidence/b-f09-13-covenant-headroom/rendered-html/computed.html
  - mockups/evidence/b-f09-13-covenant-headroom/payloads/null.json
  - mockups/evidence/b-f09-13-covenant-headroom/payloads/computed.json
  - mockups/evidence/b-f09-13-covenant-headroom/manifest.json
  - mockups/evidence/b-f09-13-covenant-headroom/EVIDENCE.yml

# Spec compliance — which BLOCKER/MAJOR each cell exercises.
spec_compliance:
  B1: "maximum_secured_net_leverage_ratio -> definition_differs (test_covenant_headroom + empty/null path)"
  B2: "minimum_fixed_charge_coverage_ratio -> definition_differs"
  B3: "minimum_liquidity / liquidity baskets -> definition_differs"
  B4: "negative_covenant_basket / incurrence baskets -> definition_differs"
  B5: "single-definition law via engine.stock_fundamentals._net_debt"
  B6: "ratio_undefined for EBITDA<=0 / interest_exp<=0 (degenerate denominators)"
  B7: "spec-verbatim ceiling + basis EN/ZH copy (rendered into both states)"
  B8: "panel copy: 'How much room is left under the covenant' / '契约限额下还剩多少空间'"
  B9: "UI never renders literal 'Headroom'; uses 'room left under the covenant' / '契约下剩余空间'"
  B10: "full grid: Limit / Reported / Room + Next step + Filing type + Accession + Filing date"
  B11: "EDGAR link to accession 0001564590-22-038930 in the computed state"
  B12: "spec-verbatim 'Not covered here' footer (visible in null.html)"
  schema: "capital_structure.covenant_headroom.v1"
  parser_version: "capital-structure-covenant-headroom/1.0.0"

# Spec R9 — manifest sha MUST equal the FINAL committed head.
# Re-run `git rev-parse HEAD` after the last code commit and rewrite
# manifest_sha + manifest.json:manifest_sha to the same value, or this packet
# is FIX_REQUIRED (manifest naming a parent or an uncommitted tree).
final_head_capture_command: "git rev-parse HEAD"
final_head_sha: de11481f07974769f0e8fc3fbb1fdad8d722c09c
final_head_tree_sha: aba13bc559317e4bcbec44f5416ccb3bdeb5545e

notes:
  - "16 PNG cells captured via Playwright/Chromium from rendered-html/{null,computed}.html at dark/light × EN/ZH × 1440/390 — 8 null + 8 computed, all unique content-addressed."
  - "Both states render through the committed evidence recipe script and the production template; no test-only fixtures, shims, or hidden decorative layers."
  - "The capture routine loads the rendered HTML beside the committed production assets, calls window.setTheme and window.setLang, verifies their attributes, and captures the panel element."
  - "The computed state injects the spec R7(a) fixture (CRSR FY 2022-12-31) so the panel shows the spec's exact grid: Limit / Reported / Room + Next step + Filing type + Accession + Filing date + EDGAR link."
  - "Spec B1-B6 enforcement is tested in tests/test_covenant_headroom.py; B7-B12 visual compliance is rendered into both null.html and computed.html AND visible in the 16 PNG cells."
