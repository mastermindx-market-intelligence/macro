# Research-entry evidence and limits

The first visible product example is authored HTML, not an empty animation stage. It leads from AAPL financial context to valuation and market context, with three actual dossier anchor destinations. The old illustrated dashboard collage remains behind a keyboard-operable disclosure and is explicitly not current market data. The unsupported institutional-use logo assertion was removed; product navigation branding was not changed.

## Source and time
The example is deliberately historical: dossier snapshot 9 September 2026, from `site/stocks/AAPL.html` at macro@61cb600c966a7762fcfbe9ea77bfcf48ce017e5c. Revenue $416.2B and free cash flow $98.8B are FY2025 figures. The price/free-cash-flow multiple 47.63x is the separate September valuation snapshot. The UI displays both periods, calls this a curated example, and disclaims a live quote, generated AI answer or recommendation. The three current dossier links are not claims that future page values must equal this historical example. Source corrections require changing or withdrawing the example.

## Experiment ownership
Existing Ad Central helpers generated new creative IDs `adc-2206449b1e0e` and `adc-d57dceafb969` in arena `hero-connected-research-20260910`. It is PLANNED / SHADOW with zero spend. Historical creative and assignment identities were not rewritten. No experiment was activated and no conversion lift is claimed.

## Local verification
The new contract first failed all seven tests on the old page. The combined research/mobile/navigation/public-chrome/pricing/Ad Central suite then passed 136 tests. Template/site synchronization checks 98 pairs. Canonical capture covers desktop/mobile x English/Chinese x both preferences. The local-file browser journey actually clicks all three AAPL links, checks visible destination sections and uses Enter to open/close the demo disclosure. `journey.json` is a diagnostic receipt, not a second lifecycle or canonical manifest schema.

## Visual direction and limitations
The public page intentionally retains its light acquisition canvas in both theme preferences, with dark terminal and closing bands. The new example uses the same palette, restrained borders and readable source links; no font files or runtime dependencies were added. The desktop case title was shortened after screenshot inspection to remove an orphan line, and Free reassurance was enlarged to readable proportional text.

These are local captures, not deployment or production acceptance. The static server's billing API 404 is retained in the manifest rather than called a healthy offer service. Authenticated/loading/stale/error states remain explicit gaps. The canonical capture records the pre-commit baseline while exercising working-tree source; source hashes bind the actual tested HTML/CSS. Independent review, current-base CI, release and a real production click-through remain separate gates. Later feature regrouping, entitlement consistency and signup simplification are still owed.

## Final composed source
The research implementation is committed at e09fed8f8992b37b82cbd9a410990e7107a75ed0 and composed with the mobile review repairs at 1f3806e6170e23655e6e0edfd7f857acc9c0aca3. The final paired stylesheet hash is 796dbcb3 across all seven HTML consumers. The source radius literal was replaced by the existing --r-sm token; no new styling authority was introduced.

The composed six-suite regression set passes 148 tests, including the strengthened mobile selector/cache checks. A fresh eight-state canonical capture and six EN/ZH × 320/390/1440 example crops exercise this composed source. Every measured document width equals its requested viewport width. The actual three-link local browser journey and keyboard disclosure pass again, with no page exceptions. `source-snapshot.sha256` and `SOURCE-DIFF.patch` are in-repo for independent review. Earlier 136-test and pre-composition descriptions above are historical receipts, not the final count.

Visual inspection of the final desktop example, the 390px entry viewport and the 320px Chinese example found the content readable without horizontal scrolling. The three-step example stacks on narrow screens and remains fully present without waiting for animation. This first vertical does not claim the rest of the long page has been shortened or that the entire acquisition journey is complete.
