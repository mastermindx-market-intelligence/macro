# Macro Command stack — follow-ups (not blocking the current P5 round)

## E-m3 — light-390 method panel material (INHERITED DEBT)

Reviewer (r17 evidence): the light method panel is a genuine second art
direction rather than a token swap — a cool recessed grey-blue field on white
with hairline row rules, zero glow, and the dark panel's luminance-lift
inverted into a well — but it falls short of the doctrine's light material law
in one respect: there is no elevation or shadow anywhere in the crop and the
panel is a grey fill rather than white material, so depth is carried only by
fill inversion and the eyebrow labels sit at lower contrast than their dark
counterparts.

Adjudication: inherited debt of `macro_suite.css` predating P5. The design lane
has been closed since `901972dd`; P5 CSS is functional overflow repair only.
Confirm with `git diff 901972dd..HEAD -- templates/macro_suite.css` (no
light-theme rule introduced by P5).

## E-n2 — empty column under "What this means for you" at 1440

~140 px empty track under the implication heading at 1440. Report cause
(grid track for an absent card?) on visual review; if not a P5 regression,
treat as inherited suite-shell grid debt. Not a v19 capture/instrument item.
