# Independent mobile review R1

PICKUP_ACK received; review complete. Read-only; no writes, no shell, no network, no subagents.

STATUS: PARTIAL

RESULT:
The CSS mechanism is correct and well-targeted — I could not falsify the repair itself. `1fr` → `minmax(0,1fr)` plus `.sit{min-width:0}` removes exactly the automatic-minimum (`minmax(auto,1fr)`) path by which a `white-space:nowrap` meter row widens its track past the viewport, and `.sit .meter{flex-wrap:wrap}` + `.sit .early{max-width:100%;white-space:normal}` relieves the badge without hiding it (`display:none` is applied only to the decorative `.chat-head .who i` dots, which are pre-existing lines). The footer rule is byte-identical in declarations to the shared family rule. No global overflow suppression, no palette/token/motion change, no third header, no duplicate chrome, every footer destination preserved, paired site copy carries the identical bytes.

But this is **not** a `PASS`. Three material findings, plus test-validity and evidence-quality defects, and a scope deviation on what I was able to read.

- **MAJOR — the motivating 320px defect has no reviewable committed evidence.** The canonical manifest captures exactly two viewports, `desktop [1440,900]` and `mobile [390,844]` (`mockups/evidence/homepage-mobile-reflow-20260909/manifest.json:15-24`). The audited failure was "a 406px-wide document at a 320px viewport" (`NOTES.md:5`) and the added CSS comment itself says "Keep the score and timing visible at **320px**" (`templates/landing.css:1400`). The only 320px artifacts are the two supplemental PNGs on `/Volumes/Mastermind/...` — outside the repo, and unreadable from this sandbox (Read denied on that volume, twice). Uncommitted evidence is not evidence under the spawn-handoff law ("reference images = committed files"). As committed, the PR proves 390 and 1440; it does not prove the width it was opened to fix, and there is no committed baseline capture, so the 406→320 improvement cannot be checked at all.
- **MAJOR — the cache-stamp bump is incomplete across the landing chrome family, so the fix does not reach returning visitors on 4 of 5 pages that use it.** `index.html` was bumped to `landing.css?v=59104b31` (`templates/index.html:90`, `site/index.html:90`), but every other page in the landing chrome family still requests the pre-fix stamp: `templates/about.html:84` and `site/about.html:84` (`landing.css?v=a44e4256`), `site/products/market-terminal.html:84`, `site/products/market-dashboards.html:84`, `site/products/mastermind-ai.html:83`. Those pages carry the identical `.f-cols` footer (`templates/about.html:354`; `tests/test_public_chrome.py:127-133` — hand-authored flagships carry "the landing's BARE `<footer>` … byte-copied from the landing down to the Platform / Research / Resources / Legal columns") and `landing.css` is on the `immutable, max-age=1y` list, the exact hazard `scripts/optimize_assets.py:153-162` was written about (#3617/#3624). Mitigation exists and is real — `optimize()` re-stamps plain-copy pairs and every `site/**/*.html` (`scripts/optimize_assets.py:176-215`) — but this PR is a paired plain-copy asset change that needs no render, so nothing in it schedules that heal. Adjudicate: bump the four stamps in the same PR, or state the render dependency explicitly in the plan's acceptance section.
- **MAJOR (evidence, not code) — the committed capture cannot support the acceptance claim it is offered for.** All eight artifacts are full-page PNGs (390×13591, 1440×10519). At any practical review scale the two changed surfaces — the footer columns and the situation meter — are illegible (the EN light mobile shot renders at 57×2000 here, a 6.84× downscale). No component crop of the footer or the meter exists in either theme or locale. Under the theme-art-direction law the evidence matrix exists so a reviewer can adjudicate hierarchy, material depth and EN/ZH parity; a 35:1 aspect thumbnail cannot carry that. Separately, the receipt reports `elements_wider_than_viewport: 3` at **both** desktop and mobile (`manifest.json:94-105`) with `horizontal_overflow:false` and no identification of which elements — almost certainly the intentional `overflow-x:auto` rails (`.pyr` 1323, `.lanes` 1334, `table.mx`), but the plan's acceptance is worded "no *unintended* horizontal overflow" (`docs/plans/2026-09-09-homepage-mobile-reflow.md:24`) and the receipt gives a reviewer no way to separate intended from unintended.
- **MINOR — the regression tests are looser than the contract they claim to pin.** `_media()` strips all whitespace (`tests/test_landing_mobile_reflow.py:16`), which deletes descendant combinators; the builder handled that for `.meter` (line 35-36 comment) but not for the badge, so `assert re.search(r"\.sit\.early\{...")` (line 38) is satisfied by the correct `.sit .early` **and** by the wrong compound selector `.sit.early`, which would style nothing. The `.sit .meter{flex-wrap:wrap}` assertion (line 36) is searched against the **whole file**, so moving that declaration out of the 680px block — including to an unconditional desktop rule that changes desktop layout — still passes. The brace-depth scan (line 13-15) counts braces inside strings/comments. And every assertion pins CSS *text*, never a measured layout width, so none of these tests can fail if the actual overflow returns by another route. Adequate as a "don't delete these rules" guard; not adequate as the "regression against the affected mobile rules" the plan asks for (`docs/plans/...:18`).
- **NIT — the 320px footer holds by inventory, not by rule.** Two `minmax(0,1fr)` tracks at 320px are ~122px each; the longest unbreakable tokens today ("Institutional" `templates/index.html:885`, "MastermindX" line 895, "Intelligence" line 880) fit, and there is no `overflow-wrap`/`hyphens` guard on `.f-col a` (`templates/landing.css:1310`). A future longer label or a longer ZH-adjacent Latin token re-opens the same page-level overflow the patch just closed.

Theme judgment, stated separately as required:

- **LIGHT (the design that ships):** sound and unchanged in material. The repair is purely geometric — the added declarations carry no color, radius, font or shadow literal, so the light research-workspace treatment (white cards `--panel`, `--hair-2` hairlines, `var(--sh-card)`) is untouched; the dark contrast bands (`.cband` 1290-1296, `footer{background:#04070c}` 1299) keep their existing role. The 900px footer rule is declaration-identical to the shared public-chrome rule (`templates/_public_chrome_css.html.j2:171-172`), so the landing footer now reflows in the same family idiom rather than a locally-invented one — the correct outcome under the navigation/chrome source-of-truth law. Hierarchy at ≤680 is preserved: the gauge and score stay on one rail, the timing badge takes its own right-aligned line via the pre-existing inline `margin-left:auto` (`templates/index.html:1866`) rather than being clipped. I judged this from the CSS and the markup; I could **not** judge rendered typographic hierarchy or column rhythm, because of the thumbnail-only artifacts above.
- **DARK:** there is no dark art direction for this page, and that is a ratified pre-existing decision, not an omission by this patch — `templates/landing.css:36-49` declares "LIGHT-ONLY, AND THE BROWSER MUST BE TOLD (2026-08-26) … no `[data-theme]` rules, no `prefers-color-scheme` block … Do NOT add a dark path here", with `color-scheme: only light` load-bearing against UA auto-dark inversion. The four dark-preference captures are therefore duplicates of the light ones by design (dark/light byte sizes differ by ~400 B at identical heights, `manifest.json:214-243`), which is genuinely useful as *proof the opt-out still holds under a dark preference* — but it is not a second art direction, and the theme half of the evidence matrix adds no independent design information. This is the one case where "dark == light" is lawful; I record it as adjudicated-by-exception, not as dual-theme evidence.

Code/visual acceptance vs production proof, explicitly: the **source change** is accepted on inspection (mechanism correct, paired, family-consistent, non-destructive). **Visual acceptance is incomplete** — 320px is unevidenced in-repo and the committed captures are too coarse to adjudicate the changed components. **No production proof is claimed or present**, correctly disclosed at `NOTES.md:20-22`; the billing 404 (`manifest.json:36-46`) is honestly recorded and proves nothing about live offers.

EVIDENCE:
- `templates/landing.css:1344-1347` / `site/landing.css:1346` — 900px footer grid + `.f-col{min-width:0}`; matches `templates/_public_chrome_css.html.j2:171-172`.
- `templates/landing.css:1400-1404` / `site/landing.css:1401-1404` — 680px `.sits{grid-template-columns:minmax(0,1fr)}`, `.sit{min-width:0}`, `.sit .meter{flex-wrap:wrap}`, `.sit .early{max-width:100%;white-space:normal}`; base rules being corrected: `:904` (`1fr`), `:912` (`.meter` no-wrap flex), `:917` (`.early{flex:none;white-space:nowrap}`), `:918` (`.sc2{white-space:nowrap}`).
- Stale stamps: `templates/about.html:84`, `site/about.html:84`, `site/products/market-terminal.html:84`, `site/products/market-dashboards.html:84`, `site/products/mastermind-ai.html:83` = `landing.css?v=a44e4256`; bumped page = `templates/index.html:90` / `site/index.html:90` = `?v=59104b31`. Re-stamp lane: `scripts/optimize_assets.py:176-215`.
- Evidence receipt: `manifest.json:15-24` (viewports), `:92-105` (`elements_wider_than_viewport: 3` desktop **and** mobile), `:299-302` (resolved SHA = baseline `61cb600c…`, disclosed at `NOTES.md:22`). `EVIDENCE.yml` lists only `templates/landing.css`, which is correct under `scripts/check_ui_visual_evidence.py:313-316` (only `templates/*.css` is material) — no gate finding there.
- Test validity: `tests/test_landing_mobile_reflow.py:13-16, 33-39`.
- Design-system literals: `landing.css` is sanctioned (`scripts/check_design_system.py:67-71`); the added lines carry no literal anyway. No runtime-stylesheet bypass — nothing was authored into page JS.

GAPS:
- Could not read the commissioned diff `/Volumes/Mastermind/.../mobile-review.diff` or the two 320px PNGs — sandbox denies reads on that volume. I therefore reviewed **file state**, never a diff against baseline `61cb600c`; "eight-line geometry patch" is taken on the plan's word for which lines are new.
- The tree I read is **past** the commissioned commit `b9616f24`: `.github/ci/legacy-jobs.yml:9959` already names `tests/test_landing_mobile_reflow.py`, i.e. it contains the post-open CI repair the handoff attributes to PR #7026 head `c045687f` (`agentos/handoffs/MARKET-OS-2026-09-10-homepage-mobile-reflow.md:52-54`). Anything I judged may reflect that later head.
- Builder claims not independently executed (per commission): 4-failed/1-passed before, 56 passed after, 98 pairs. I verified only that both stylesheet copies carry the four rules at identical line numbers and that the 900px rule matches the shared family.
- `?v=59104b31` correctness (that it is the actual content hash of the new `landing.css`) is unverifiable without hashing; no test pins landing asset stamps to content.
- ZH rendering parity at narrow widths judged only structurally (markup and `data-zh` untouched; geometry is language-neutral here) — not from legible imagery.
- No authenticated/loading/stale/empty/error states, and no production/deployed check; both correctly declared, neither supplied.

DEVIATIONS:
- Commission asked me to inspect the tracked diff and the 320px supplemental pictures; both are on `/Volumes/Mastermind` and were denied by the sandbox. I did not attempt any workaround (no shell, no copy, no network), per SCOPE.
- No other deviation: read-only throughout, Read/Grep/Glob only, no successor work, stopping after this return.
