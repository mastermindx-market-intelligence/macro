# A-F05-1 News consequence panel — dual-theme evidence

Captured with `scripts/capture_page_evidence.py` against a locally rendered
`site/news.html` from this branch's `templates/news.html.j2` + real
`data/chronicle/events.jsonl` via `engine.chronicle.impact.glance_consequence_surface`
(no fabricated signals). Route: `news.html#nxConsequence`.

**DARK TREATMENT:** command-center — deep charcoal ground, restrained blue accent
bar on the section head, dark cards with hairline borders.

**LIGHT TREATMENT:** research workspace — cool lavender canvas, white material
card plane, hairline borders, no glow.

Matrix: dark/light × en/zh × desktop(1440)/mobile(390) = 8 rest cells.
