# Calibration origin: exact-source fixture proof

Source pair: R1 `0b2493990358d44dee5b625176c5289eab9a04e4` plus R2 `b5b38cc0b2aa78778cb00e85c25f278b3100fc59`. The R1 source remains on its own PR; no R1 implementation was copied into R2.

32 full commodity-page states, including expanded calibration disclosures, and 8 actual hub-card navigation states are captured across dark/light, EN/ZH, desktop/mobile. The commodity captures use the existing owner with `--full-page --include-calibration`. Hub captures use the actual `_g_vectors` markup, its `_GLOBE_HUB_CSS` / `_HUB_CRITICAL_CSS`, and the same Chromium helpers; no duplicate product renderer was written. The compact mobile hub intentionally hides detailed chips; its actual link reaches the complete asset evidence page.

These are synthetic fixtures, not market observations or production. Global navigation, authentication, globe rendering, live data and provider delivery are omitted. The separate hashed-input numerical comparison is compatibility evidence only.

Every PNG is content-addressed and was hash-checked before publication. 128 commodity card/detail selections and parameter identities plus 8 hub navigation journeys passed; the hub journeys selected all four destination assets. See the companion source proof for tests, field preservation and remaining release gates.
