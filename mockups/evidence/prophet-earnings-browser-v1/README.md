# Prophet earnings browser — component evidence, not a deployed page

Source under test: `a76d390a92e9c7ad86eb88684831fd8f71d3adf8`, PR #7264.

These 32 screenshots are actual Chromium captures of the committed drawer/controller and native Lab HTTP/D5 path, using synthetic episode, earnings and entitlement adapters. The browser has no real account. `/fixture/*` identifies test pages, not production routes. The canonical `scripts/capture_page_evidence.py::run_capture` generated `manifest.json` through its existing `PageDriver` interface; no replacement screenshot schema or acceptance gate was introduced. Real gated-access gaps remain explicit in the manifest.

The four evidence states are covered, corrected, withdrawn guidance and unavailable event evidence. Each has the existing desktop1440/mobile390 × EN/ZH × dark/light matrix. The drawer is actually opened and an exact episode selected before capture. Source references and financial figures are development fixtures, not a live investment observation.

## What the photographs show

Dark treatment uses the incumbent depth, panel and text tokens. Light treatment uses the incumbent white materials, borders and shadows. The test document explicitly applies the existing `--bg` and `--text` tokens to its body; the initial unstyled host did not do so and was rejected as visual evidence. No product CSS was changed to repair that test-host mismatch.

The native renderer supplies the displayed financial wording. The later-correction case retains the original decision-time amount. A withdrawn range is not headlined as current guidance. Source/correction detail remains progressively disclosed.

The initial component functional matrix and the canonical recapture repeat the same 32 states; do not sum them into 64 distinct cases. A separate 24-case Chromium negative campaign covers denial, generation/source failures, malformed responses, close/cancel, empty search, account update and language changes in both themes. Its receipt is held with the native evidence logs, not substituted for these screenshots.

## Deliberate limits

This is not the entire generated U.S. dashboard, a real paid-user session, a live provider read, independent visual approval or a production deployment. The attempted full-page harness extension was platform-blocked and remains incomplete; no full-page result is asserted. `EVIDENCE.yml` can close only the existing mechanical component-evidence requirement. Integration and human/independent design acceptance remain separate.

No market outcomes, model fit, ranking, entry, sizing or trade decision was introduced. The separate forward-expectation species and its 21-session preregistration are unchanged.

Reproduction and exact driver/source receipts: `/Volumes/Mastermind/agent-evidence/prophet-d5-view-20260917/release/frontend-completion/` on the incumbent Studio; `browser_proof.py`, `browser_negative_proof.py`, `capture_native_component_evidence.py`. These are test-only scripts. `manifest.target.driver_sha256` binds the actual capture adapter; `manifest.tool` binds the canonical producer.
