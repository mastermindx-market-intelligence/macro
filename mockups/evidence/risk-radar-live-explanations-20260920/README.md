# Risk Radar causes and live context — exact-source evidence

One existing Risk Radar entry point, not another dashboard container. Original radar
score/word is unchanged; a separate internals hint points at the same derived envelope.
Source-native causes replace generic prose. Older inputs, settled source clocks,
provisional candidates and policy authority remain separate.

## Capture and limits
24 whole-page cells: eight rest, eight modal, eight keyboard-focus states;
1440/390 × EN/ZH × dark/light. Rest uses the canonical page-evidence driver.
Modal/focus uses capture_adapter.py in an isolated local member-access fixture,
with the actual existing button and real keyboard focus/Enter. No credentials or
production-authentication claim. The manifest attributes each cell to its real tool.
Context height at 1440 is 184.625px INSIDE the modal, not in the dashboard grid.

verify_live.py separately loads the actual Jinja component and shipped poller.
Eight cells × four observed states prove healthy → pending → network fallback →
recovery, immutable settled readings/radar score, source dates and no overflow.
The responses and browser clock are synthetic, not natural events or scientific
promotion. live-proof.json binds exact page/JS and all 32 screenshot digests.

## Design review
Dark retains existing luminance surfaces and independent severity inks. Light uses
quiet hairlines and no glow. Both keep short labels, native-shaped ZH, responsive
wrapping and locale-aware trend direction. Source details remain optional.
Reviewed desktop light, mobile Chinese dark, and pending/fallback component states.

## Publication
render-receipt.json records a bounded canonical partial render from the exact
existing settled bundle. All bytes outside the risk section, button hint, scoped
stylesheet and script cache key are retained. Not a full-site rebuild; no collector,
market ledger, score or upstream engine changed.

removal-production-proof.json and removal/ preserve eight REAL public-origin
screenshots proving accepted parent PR 7467. They are distinct from local feature
tests. Only manifest/live-proof/removal-proof named images are publication artifacts.

The same consumer has a native ten-second request deadline, no overlapping request,
and no second polling/retry loop. Pending de-escalation explicitly stays Checking
stress; it cannot announce an all-clear before producer-owned confirmation.

## Checks
102 targeted tests pass. Three discriminating assertions fail on the parent source.
Design, runtime-style and Agent OS checks pass. Global pair sync reports one inherited
theme.css mismatch: both hashes are unchanged from base; only the owned JS pair is
claimed identical. See pair-scope-proof.json; no unrelated theme repair is bundled.
