# Compact risk context — public-inspection repair

Current candidate: R2, following independent review of `fa26d5d1092832f9509b144aac80492317763a1c`.
Final local macro.html SHA256: `b63e24b134deaa97598ce80db0435f24b2e48d65db00e606dcbb16af6bd24bc7`.
This is local canonical-target and browser proof, not deployment. Original August 30 evidence and fa26 review remain historical.

## User outcome and design
The quiet risk-context rail stays inside the regime hero. It now opens a PUBLIC native disclosure, not the member-only Risk Detail dialog. Explanation and evidence remain available anonymously; the separate Risk Detail authorization and controller are untouched. Missing envelope renders no empty disclosure.
DARK TREATMENT: restrained panel2 wash and hairline into the hero; separate directional and hazard inks; no new glow or warning hero.
LIGHT TREATMENT: opaque pale rail against white research material, visible hairline and restrained shadow; dark text, muted dates and non-neon hazard ink.
The same content, source meaning, keyboard behavior and disclosure hierarchy work in both themes. Chinese directional colors follow existing tokens; hazard severity does not invert.

## Current evidence
`browser-receipt.json` and `home-*`: twelve final canonical-page cases, dark/light × EN/ZH × 1440/768/390. Fresh anonymous contexts, no isAnon override. Keyboard opens public explanation; source evidence is readable; keyboard closes with focus retained. Member-only Risk Detail remains gated. No page errors or horizontal document overflow. Light hazard contrast measured 5.25:1.
`detail-{1440,390}-{dark,light}-{en,zh}.png`: actual public explanation, including Chinese. `focus-*` and `hover-*` capture the explicit affordance states.
`live-context-receipt.json`: seven explicit live-feed fixtures run the unchanged production painter, including pending at 390 in both themes/languages. Settled clock remains visible; largest pending rail 130.375px; no document overflow or page errors. Python Playwright logs contain Target-closed teardown diagnostics; these are preserved, not claimed absent.
`settled-state-receipt.json`: sixteen actual-template cases covering absent envelope, positive hazard with missing source, stale trend source and active policy. Each runs both themes/languages at 390; these are fixtures, not production data.
`owner-tests-r2.log` and `.json`: 302 owning/adjacent tests passed with command, workspace and source hashes. Presentation suite has 44 tests. The generated-page smoke accepts deliberate envelope absence and loudly skips an omitted sparse page; deterministic component tests do not read daily artifacts.
`risk-off-mutation.json`: removing the actual RISK_ON condition breaks the actual-wording assertions; no production file was mutated.
`clock-regression-rebuttal.json`: the clock-hiding rule really existed on original6d7; independent finding6 is disproved rather than accepted blindly.

## Provenance and remaining gates
`canonical-target-receipt.json` binds the real builder's 19:48 macro target before externalization; `asset-optimizer-r2.json` binds the standard post-processing; final browser receipts bind final HTML. Unrelated builder outputs were restored in the owning worktree; no manual page splice.
`independent-review-fa26.md` is the first full report, PARTIAL, not approval. Sol consumed it in PR comment5561604621 and terminally closed that read-only reviewer. The source repair remains with Sol on the same PR/branch.
Current protected procedure: Mastermind `f9e46a72d6102b0e94c897590fc58bac89eb4ea6`, compatible same-commit Skillpack1.0.1/bootstrap1. The formal Source Continuity adapter previously refused REMOTE_CENSUS_INCOMPLETE; no checkpoint certification or writer transfer is claimed.
Remaining: exact R2 independent review, concluded current-head CI, explicit Sol release, normal publication and public production-browser proof. No source/review/test result is a production claim.
