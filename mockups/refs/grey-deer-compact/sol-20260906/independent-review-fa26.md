STATUS: PARTIAL

RESULT:
Reviewed artifact: the working tree at `/Users/chriswong/Documents/Cluade/macro-main/.claude/worktrees/sol-macro-briefing-review-20260906`, asserted by the commission to be local HEAD `fa26d5d1092832f9509b144aac80492317763a1c` (base `638851230951990ad1d96efe6431b5d16d21d396`). I could not independently confirm the SHA or tree cleanliness — I hold Read/Grep/Glob only and ran no git, no pytest, no browser. All statements below come from reading source, the committed evidence bundle, and the screenshots.

Not a clean review. **1 blocker, 4 major, 5 minor, 2 nits.** The compaction itself is real and well executed (496px panel → 57px rail; tape top 1045 → 586 at 1440), the live-overlay hook identity is preserved, policy orthogonality is preserved structurally, null vs NONE vs zero is handled correctly in both projections, and — unusually — the light theme carries a genuine art direction rather than a token swap. What fails is (a) who can still read the honesty layer, (b) which caveats survived the move from L1 to L3, (c) one broken empty-state, and (d) two tests that cannot fail.

**BLOCKER 1 — the entire disclosure layer moves behind the registration gate for anonymous readers, and the only browser proof disables that gate.**
The rail is now the sole entry to every Grey Deer read beyond two words, and it calls `mx5OpenDlg('dlg-risk')` (`templates/_risk_envelope_band.html.j2:61`). That function refuses on `page-macro` for anonymous visitors and diverts to the sign-in sheet: `templates/dashboard.html.j2:14840-14846`. The detail projection exists only inside that dialog (`templates/dashboard.html.j2:12441-12443`). Today the same content is public: `production-before.json:5` classifies the capture `PUBLIC_LIVE_BROWSER_NOT_MEMBER_SESSION` and `:11` records the full rendered band text — trend clause, hazard clause, per-source evidence summary, policy state, the clock caveat sentence — read anonymously from `https://www.mastermind-x.com/macro.html`. After this change an anonymous reader gets "Trend Risk-on ▲ · Breakage Fragile" and a sign-in sheet. Consequences: the per-source clocks, the "Where they disagree" list, the coverage caveats and the machine receipt — the artifacts that make the glance honest — become member-only; and `aria-haspopup="dialog"` / `aria-controls="dlg-risk"` (`_risk_envelope_band.html.j2:59-60`) are untrue in that state. The browser receipt cannot detect this because `verify_browser.py:64` stubs `isAnon: () => false` before every click; `README.md:24` discloses the stub as an evidence limitation but never as a change in who can read the disclosure, and `README.md:11`'s claim that "the information architecture … [is] identical" is falsified for the anonymous state. No source, test or screenshot in the bundle exercises the real anonymous path.

**MAJOR 2 — coverage and staleness caveats were demoted from the glance tier to L3-only; a degraded settled read now looks clean.**
Three separate demotions, all in the same direction:
- `_risk_envelope_band.html.j2:53` — `_show_caveat = _stage is none and _caveat_n > 0`. The old band flagged unreadable sources unconditionally beside the hazard clock (`source-review.diff:950-951`, `{% if _unread %}`). So `FRAGILE`/`TRANSMITTING`/`BREAKDOWN` with a source that could not be read now shows no caveat at L1, and none at L2 either (`:186-197` mentions `_caveat_n` only in the `_stage is none` branch, `:188-189`).
- `freshness.per_source` is no longer read anywhere in the file (grep: only `s.coverage` at `:234-235` inside the L3 table survives). The old trend row carried "· older than this session" / "· didn't publish" at the glance tier (`source-review.diff:902-904`). That flag is gone from L1 and L2.
- `data_state` `DEGRADED`/`STALE` now surfaces only in the L3 machine receipt (`:259`).
The removed code carried the doctrine reason in its own comment ("a caveat that changes how the answer reads … sits beside the clock rather than only in the drawer", `source-review.diff:900-901`). Stacked on Blocker 1, a degraded settled read is invisible to an anonymous reader entirely. Note also `engine/risk_envelope.py:496` — `data_state in ("DEGRADED","STALE","UNKNOWN")` yields `MIXED` coherence, i.e. exactly the state that now paints as a clean two-word rail with split hairlines and no words.

**MAJOR 3 — the Risk Detail dialog renders an empty expander whenever the envelope is absent.**
`templates/dashboard.html.j2:12441-12444` places `<details id="gde-context-disclosure"><summary>Why trend and risk can differ</summary>{{ gde.gde_detail(risk_envelope) }}</details>` outside the macro's own guard. `gde_detail` emits nothing when `risk_envelope` is falsy or the schema does not match (`_risk_envelope_band.html.j2:124-125`), so the user gets a control that opens onto nothing. This is not hypothetical: `scripts/build_site.py:6681-6686` sets `_risk_envelope = None` on any compose/write failure and documents that path as the intended honest outcome ("the band renders nothing at all"). The old include satisfied that contract; the new wrapper breaks it. No test covers `risk_envelope=None` at the dashboard level — every presentation test calls the macros directly (`source-review.diff:1224-1230`).

**MAJOR 4 — a unit test is coupled to the generated `site/macro.html` with neither a sparse guard nor a data guard, and it is wired into a CI pack.**
`tests/test_risk_envelope_presentation.py::test_site_macro_matches_the_compact_contract` (`source-review.diff:1436-1454`) reads `ROOT/site/macro.html` and asserts `_count_id(html,"risk-envelope-band") == 1`. Two failure modes: (a) `site/` is omitted from every default fleet session worktree (CLAUDE.md §sparse, `config/sparse_worktree.json`), so the test raises `FileNotFoundError` instead of skipping — the house convention is an existence check plus a loud skip, e.g. `tests/test_stock_dashboard_css.py:237-238` (`pytest.skip("sparse checkout omits site/ (needs_full_checkout)")`); (b) on a night where the envelope composes to `None` (Major 3's documented path) the regenerated `site/macro.html` contains no `#risk-envelope-band` and the assertion turns a benign data outage into a red `synapse read-gate unit tests` job — which this PR itself just widened (`.github/ci/legacy-jobs.yml`, `source-review.diff:9-10`). A pack is one check; that red would pin the fleet for a data reason.

**MAJOR 5 — the test that is supposed to pin hazard wording under risk-off cannot fail.**
`source-review.diff:1475-1476`: `if verdict == "RISK_OFF": assert "while the index holds" not in html`. That string was deleted from the template by this same diff — the new clause is "while the broad trend remains positive" (`_risk_envelope_band.html.j2:195`, zh `:196`). The assertion is therefore vacuous for every input. The behaviour is in fact correct (the clause is gated on `_ms.get('verdict') == 'RISK_ON'`, and `engine/risk_envelope.py:441` makes `RISK_ON` + damaged stage the only `CONTRADICTORY` construction), but the named acceptance gate is unenforced and will not notice a regression.

**MINOR 6** — `test_live_overlay_does_not_hide_the_settled_read_clock` (`source-review.diff:1530-1536`) asserts the absence of a `\.gde-rail:has\(…\.gde-stamp-settled\s*\{\s*display:\s*none` rule that has never existed in any revision of the file. It is a regex against an imaginary defect and pins nothing.

**MINOR 7** — vocabulary drift between the settled rail and the unchanged live painter for the *same* epistemic state. Settled null renders "Unclear" / "不明" (`_risk_envelope_band.html.j2:11-12`, the `gde_hz_en`/`gde_hz_zh` maps); the live chip renders "Live · not enough to say" / "实时 · 暂无法判断" (`templates/risk_envelope_live.js:83-84`), and the two sit in the same rail. The old band used "Not enough to say" / "证据不足", which matched the painter. One state, two plain-word names, side by side.

**MINOR 8** — `live-context-receipt.json` reports `"clock_visible": true` for all three scenarios, but that field is a hard-coded literal in the harness, not an observation: `verify_live_context.py:50` writes `{"scenario": scenario, "clock_visible": True, …}`. The underlying fact *is* asserted (`:44`, `:49`), so the claim holds — but the receipt field itself is not evidence and should not be read as a measurement.

**MINOR 9** — the pending badge is never painted in any evidence. `verify_live_context.py:32` sets `"pending": None` in all three scenarios, so `paint()`'s pending branch (`risk_envelope_live.js:46-57`, longest string "· live read shifting · confirming n/m") is unexercised, while `.gde-pending` carries `white-space: nowrap` (`templates/_risk_envelope_band.css.j2`, `.gde-pending` block) inside the 390px grid area. The widest live string at the narrowest viewport is unproven.

**MINOR 10** — the L2 policy strip prints no state sentence when a policy is active: `_risk_envelope_band.html.j2:205-210` branches only on `not _policy_known` and `_policy_n == 0`, so `policy_count = 3` yields "Policy … 3 active" with no words. The old band's orthogonality caption ("it is not part of the agreement above", `source-review.diff:1044-1046`) is also gone with no replacement at L1 or L2; orthogonality now rests entirely on structure (which does hold — policy sits outside `#gde-rail-reads` past `.gde-rail-rule`, `:95-103`, and outside `.gde-detail-reads`, `:203`).

**NIT 11** — the rail hard-codes `padding: 10px 22px` (`templates/_risk_envelope_band.css.j2:26`) where the old band used `var(--pad)`. `#regime-radar` is `padding: 0` in grid view (`dashboard.html.j2:4402-4415`), so 22px is a hand-matched constant against the hero column's own inset; in `home-1440-dark-en.png` and `home-1440-light-en.png` "RISK CONTEXT" sits ~4-5px left of the "Reflation" chip and the hero body copy above it.

**NIT 12** — the only affordance that the 1352px-wide strip is clickable is an 8×12 chevron at the far right (`_risk_envelope_band.html.j2:114-116`), ~1.2k px from the reads it explains, with no text cue. Correct hover/focus rules exist; nothing in the resting state says "opens".

EVIDENCE:

Design adjudication — DARK, judged as a design (`home-1440-dark-en.png`, `home-390-dark-zh.png`, `detail-1440-dark-en.png`):
Hierarchy is right. The kicker is `--fs-label`/700/`--trk`/`--ink-3`, the two values are `--fs-body`/700 in semantic ink (css `.gde-rail-kicker`, `.gde-k`, `.gde-v`), so the strip stays clearly subordinate to the 75 gauge and the 42-session path; it reads as the hero's footer, not a third headline. Material is `color-mix(--panel2 42%, transparent)` over the glass card plus a `--hair` top rule (css:28-29) — in the shot the band is separated almost entirely by the hairline, which is the intended restrained "instrument calm" and adds no new glow; it is at the floor of legibility as a distinct surface, but it is a deliberate floor, not an accident. Semantic ink is correct under the reserved-hue law: green direction + ▲ for trend, amber health for breakage, achromatic meta; the `▲` keeps direction readable when the zh flip makes both reads red (`home-390-dark-zh.png` shows exactly that — red "风险偏好▲" with the arrow doing the work). Density 57px at 1440 / 44px min target. Detail view in dark: the two `.gde-dblock` rails carry their own inks under `.is-split`, the policy strip is a tile with no rail, and the evidence disclosure is genuinely quiet. Dark passes on its own terms.

Design adjudication — LIGHT, judged separately as a design (`home-1440-light-en.png`, `home-390-light-zh.png`, `home-390-light-en.png`, `detail-1440-light-en.png`, `detail-390-light-en.png`):
This is not a token swap, and I checked before reading the README. The light rail re-specifies its material: an opaque pale fill `color-mix(--panel2 55%, --panel)` instead of dark's translucent wash, a darkened top hairline `color-mix(--ink-1 10%, --hair)`, an `inset 0 1px 0 --ink-1 4%` top highlight, and hover = white panel + `0 4px 14px --ink-1 6%` — shadow where dark uses a brightened wash (`_risk_envelope_band.css.j2`, `[data-theme="light"] … .gde-rail` block). The L2/L3 surfaces are re-specified too (`.gde-disc-body` → `--panel`; `.gde-detail-policy` → `--panel` + `0 1px 0` shadow). That satisfies the TP-0 requirement of a stated, mechanised light art direction. As composed: in `home-1440-light-en.png` the pale strip against the white hero actually separates *better* than the dark equivalent, hairline discipline holds, dates are muted, and the modal in `detail-390-light-en.png` reads as white research material with hairline rules rather than an inverted dark panel. One observation I could not adjudicate read-only: "Fragile" in `--ink-warn` on the light strip is the least legible element of the light composition and it is the hazard word — it is the site-wide token (`theme.css:318`), not this packet's invention, so I raise it as an observation, not a finding; contrast ratios need a measurement I cannot take.

EN/ZH parity: all twelve L1 states present. `browser-receipt.json` records 390px rail heights of 78.19 (EN) vs 60.19 (ZH) — EN wraps to two logical rows, ZH does not; both fit, `overflow:false` in all twelve cases, and the mobile grid (`@media (max-width:700px)` areas `kicker reads reads / meta meta chev`) keeps the warning with Breakage as the CSS comment claims. ZH kickers are de-tracked and de-uppercased throughout (`.gde-rail-kicker`, `.gde-k`, `.gde-v`, `.gde-detail-h`, `.gde-disc-h` zh overrides).

Semantics verified sound (no finding): null vs NONE vs zero — `gde_hz_en/zh` map `NONE→"No breakage"/"未见断裂"` and any unknown/None stage → "Unclear"/"不明" (`:6-12`), with `gde-hz-null → --prov-ink` and `gde-hz-NONE → --ink-ok` (css class map), so a null never paints as calm. Localized-label fallback: `_ms_en/_ms_zh` fall back from `state_label_*` to a verdict-keyed map and only then to "Unavailable"/"不可用" (`:47-49`); `engine/risk_envelope.py:313` guarantees `verdict is None` whenever `usable` is false, so the fallback text and the ink class cannot disagree. Policy counts: `_policy_known = _policy_n is integer and _policy_n >= 0` (`:37-38`) — Jinja 3's `integer` test excludes `bool`, and the parametrised test covers `None/False/True/-1/"0"/1.5`; zero takes no rail space, unknown says "Policy status unavailable". Live-hook identity (`#risk-envelope-band`, `#gde-live-chip`, `#gde-pending-chip`, `#gde-live-receipt`, `data-bundle-id`, `data-settled-session`) is intact and matches every selector the unchanged painter uses (`risk_envelope_live.js:19-24,32-34,78-80,127,151`). Escape/focus return is real, not asserted: `mx5CloseDlg` captures `_dlgTrigger` at open and refocuses on close (`dashboard.html.j2:14865, 14913-14932`), consistent with `focus_after_escape: "risk-envelope-band"` in all twelve `browser-receipt.json` cases. Stocks-mode sibling is not contaminated: although `gde.gde_detail` at `dashboard.html.j2:12443` is not mode-gated, the whole dialog block is excluded from stocks — `site/us_stocks.html` contains zero `mx5-dlg`, zero `dlg-risk`, zero `gde-*`. The Risk Brief and its date survive (`dashboard.html.j2:12434-12436`, and the 64/100 WATCH brief is visible below the disclosure in both detail screenshots). No runtime stylesheet injection: all presentation lives in the governed `_risk_envelope_band.css.j2` partial; the only inline styles are `style="color: var(--gde-ms|--gde-hz)"` (`:69`, `:82`), token references rather than literals, which `scripts/check_design_system.py` (BLOCKING_RULES at :79-80) does not flag.

Logs: `owner-tests-resumed.log` = "289 passed in 15.21s"; `presentation-final.log` = "31 passed in 3.06s". Both are bare `-q` tails with no pytest header, so they name neither the command, the rootdir, nor the sparse-tree banner; I take them as consistent with the commission's claim and not as independent proof of scope. `browser-receipt.json` and `live-context-receipt.json` both bind `html_sha256 926f49e5…37ae`, matching the commission.

GAPS:
- I did not and could not execute anything: no git verification of the SHA/base/cleanliness, no pytest, no browser, no contrast measurement, no network. Read-only tools by commission.
- No ZH capture of the L2/L3 surface exists in the bundle — `detail-*` is EN only (`verify_browser.py:74`, `lang == "en"`). The dialog's longest strings (the two clause paragraphs, the evidence table headers, "证据与来源健康 — 3 个来源，2 处分歧") are unproven in Chinese at 1440 and 390, in either theme. This is the incomplete half of the dual-theme × EN/ZH evidence matrix.
- No hover or `:focus-visible` state is captured in either theme; the light hover shadow sits on the last row of a rounded card and is unverified at the bottom corners.
- No evidence exercises: a degraded/stale *settled* source (the live fixtures vary only the live feed), the pending badge, `policy_count > 0`, `risk_envelope = None`, or the anonymous visitor.
- `browser-receipt.json` and `live-context-receipt.json` are local fixtures over a locally built page; they are correctly self-classified as not production and not an authenticated session.
- I read `README.md` only after adjudicating the screenshots, per the visual standard.

DEVIATIONS:
- Release gates, listed separately from the semantic review and not waived by it: hosted CI has not run on this head; the Source Continuity adapter returned `REMOTE_CENSUS_INCOMPLETE`; no `CHECKPOINT_VERIFIED`; no live publication or post-publish verification. This review confers no Ready, merge, deploy or sourcewriter authority, and I submitted no PR review.
- Scope note, not a violation: the dialog title changes "Risk Radar" → "Risk Detail" (`dashboard.html.j2:12434`) and a new `<details>` is inserted above the Risk Brief. Both are inside the macro-mode dialog and are pinned by the new tests; I flag them only so Sol confirms the rename was intended.
- I stopped after this single report as instructed: no edits, no watcher, no second wave.
