# Live Entry Radar W8 — author-session critique

**Role:** first-pass product + visual critique by the authoring session.
**Not independent.** `independent_of_author: false`. Do not treat this file as a RIG critic receipt.
**Superseded on the ten independent nits** by `reviews/INDEPENDENT_CRITIQUE.md` (judged `b4f557`) and the nits commit `5ef3626d2e59`. Rows below that still describe pre-nit bytes (`--ok`, quiet Probe Set 0, `overflow-x: hidden`, card-level ACCRUING stamps) are historical. Current bytes: `--er-life`; quiet Probe Set stays live; 390 is fit not clip; Priority is one board line plus a card em-dash.

**Commissioning session must** bind critic receipts to the corrected freeze SHA before any `APPROVE_*` verdict.

Pinned Prophet Board at session start: merge `168a9be006914441051cff393927ce465e39138e` /
tree `d540f493a097cb37f3f91e4c7bc81a39b876d069`.

Crops reviewed (24): `mockups/refs/entry_radar/crops/01`–`08`, `10`–`11`, `20`–`21`,
`30`–`31`, `40`–`41`, `50`–`52`, `60`–`61`, `70`, `80`–`81`.

---

## Product (PRC)

| ID | Sev | Finding | Disposition |
|---|---|---|---|
| PRC-W8-001 | major | Several glance why-lines still carry detector keys (`c2f_rebound_atr`, “histogram trough”, “§10 15-session”) against contract §14 / doctrine A1 plain-word glance. C1/C2 board lines were rewritten this session; C3/C5/unavailable still read as lab notes. | **ACCEPT_FOR_W8** — mechanical copy is lawful; W9 must finish the glance pass against live fields. |
| PRC-W8-002 | major | Candidate / featured hues originally bound `--pv-buy`, which flips red under `html[data-lang=zh]`. Candidate is a lifecycle, not a Buy stance. | **SUPERSEDED** — author then used `--ok`; independent VTC-004 moved to `--er-life`. Do not re-bind `--ok`. |
| PRC-W8-003 | minor | Lane “Deep Washout” = C3 confirmed-4H recovery. Contract §14 names the lane; the card still says “confirmed 4H”. Easy to read as a deeper C1. | **RETAIN** — contract lane name; mapping is in DESIGN_NOTES. W9 copy may add a hover receipt. |
| PRC-W8-004 | minor | Disabled C4 chip looks like a dead filter. | **RETAIN** — must stay visible that C4 cannot fire. |
| PRC-W8-005 | minor | Probe Set headline stays 42 on focused states. Independent PRC-001: quiet no longer forces the count to 0. | **ACCEPT_FOR_W8** — fixture Probe Set is a page-level constant; W4 supplies the real count. Quiet stays live. |
| PRC-W8-006 | minor | Drawer clocks print raw `known_at` ISO. Glance freshness is plain-word; the drawer is allowed to be mechanical. | **RETAIN** |

## Visual (VTC)

| ID | Sev | Finding | Disposition |
|---|---|---|---|
| VTC-W8-001 | major | Overlay chips on the 74px spark wrap on 232px cards (lifecycle + expert + C2 variant). Same Prophet overlay-wrap pattern; 390 is 1-col. | **RETAIN** — sister geometry; do not invent a third card width. |
| VTC-W8-002 | minor | Featured aura is quiet on a candidate-heavy board (only C1/C2 fixtures are featured). | **RETAIN** — Best is a filter; featured is not “every candidate”. |
| VTC-W8-003 | minor | Light mobile ZH (`08`) is a verification crop, not a second art direction. Light desktop is the authored light target. | **RETAIN** |
| VTC-W8-004 | minor | C4 disabled chip density matches live lanes, so a skim can try to click it. `aria-disabled` is set; no navigation. | **RETAIN** — better than hiding C4. |

## Strengths (anti-strawman)

- Prophet vs Radar question is the first sentence a reader sees.
- One card per `(ticker, expert)` — FIX.MANY is three episodes, three freshness clocks.
- C4 cannot be a row expert (renderer throws) and cannot be a lane filter.
- Priority is one board-level ACCRUING line; card slot is an em-dash; Opportunity is NOT YET MEASURED; no win-probability number.
- Stale / unavailable / raw / degraded are dashed, unfeatured, no hover lift.
- False-start count is on the card, not tooltip-only.
- REFERENCE banner is unmissable on every state including anon.
- EN/ZH is structural (`.l-en` / `.l-zh`); dark and light are both designed.
- 390px is one column that **fits**. Do not pin it with `overflow-x: hidden`.

## What this critique is not

It is not a quarantined two-pass RIG review. It does not bind `reviews/*.yml`.
It does not approve the reference.
