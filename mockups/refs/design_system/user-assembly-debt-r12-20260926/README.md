# User assembly debt — R12

Purpose: project the existing prepared-answer doctrine into a measurable migration obligation without creating a new design system or approval plane.

Expected failure before source change:
- 3 focused tests failed because the constitution, migration packet, and AGENTS projection did not name user assembly debt.

Verified local source after change:
- focused: 3 passed / 99 deselected
- full tests/test_design_system_foundations.py: 102 passed
- discriminating mutation: renaming ASSEMBLY DEBT to ASSEMBLY LOAD caused the packet test to fail (1 failed / 101 deselected)

Contract:
- user assembly debt = setup, cross-reference, mental math, or configuration required before the useful first read;
- primary-path target = zero, except unavoidable subject identity input;
- the product performs comparison/synthesis before advanced customization;
- full population, evidence, and expert depth remain reachable.

Scope: docs/process law + existing enrolled foundations test only. No production UI, data, Paper, Figma, token, runtime, persistence, or route mutation.
