---
key: MARKET-EXPERIENCE-DIRECTION-2026-09-24
question: >
  What company direction and bounded Meta-CEO responsibility follow the Chairman's
  vision of cumulative historical market experience and anticipatory intelligence?
answer: >
  Adopt the Chairman-directed outcome of continuously learning market intelligence,
  progressively connecting time-correct market and issuer experience, earnings and
  canonical relationships to evaluated forward assessments. Meta-CEO owns the
  cross-domain thesis, integration sequence and acceptance; existing source,
  runtime, data-rights and capital-authority owners retain their boundaries.
rationale: >
  Separate features and large archives do not constitute learned intelligence.
  The useful system connects observations, contextual episodes, mechanisms,
  forecasts, outcomes and corrections, then demonstrates incremental usefulness
  on unseen and prospective evidence. Existing infrastructure must converge rather
  than be duplicated. Strategy publication remains subject to normal source review.
alternatives:
  - option: Continue independent dashboard and research features without a shared learning outcome
    why_not: It leaves experience fragmented and makes progress impossible to judge end to end.
  - option: Train a giant model or install a new graph/database/fabric first
    why_not: It adds cost and duplicate ownership before data quality or incremental value is proven.
  - option: Treat historical analogy or model confidence as trading authority
    why_not: It bypasses independent validation, calibration and existing promotion gates.
evidence:
  - "Chairman current live instruction on 2026-09-24: take leadership as Meta-CEO, establish this direction and determine implementation/infrastructure/compute. Subsequent continue preserves scope."
  - "Mastermind PR #967, exact head 4d8e17abfa245b3519a5950067e1fc87ac9936fa; six-file strategy, guidance, blueprint and offline arithmetic candidate. Draft, not merged."
  - "Mastermind protected Skillpack 1a7d400294b0d37c460b963b8865b40a23173b58; compatible version 1.0.1. Required loaded procedures verified unchanged from ed678f27466392bc53cc03f584f91e4f3adf3aa3."
  - "Targeted calculator and strategic-state tests: host processes 89571 and 95602 completed with exit 0; scenario arithmetic inspected. Not production or forecast-performance proof."
affects:
  - market-memory
  - earnings-intelligence
  - gmi-theme-graph
  - "WS:MARKET-MEMORY-W2C"
  - "WS:EARNINGS-INTELLIGENCE-OS"
  - "WS:EARNINGS-EVENT-INTELLIGENCE-COMPILER"
  - "WS:GMI-THEME-GRAPH"
  - "Mastermind/config/strategic_state.yml"
confidence: high
reversibility: easy
decided_by: ceo-sol
decided_at: 2026-09-24
---

## Decision boundary

The Chairman supplies present direction and bounded Meta-CEO assignment. This record grants no runtime admission, source lease transfer, unlimited spending, provider activation, deployment or trading authority. Existing revenue/product objectives, resource-policy shares and all prohibitions are preserved. No new Agent OS workstream/program parent is minted. The architecture and numerical envelopes in Mastermind #967 remain reviewable proposals, not accepted empirical findings.

The implementation principle is observe broadly, deepen analysis selectively, train only for a defined task, evaluate independently and promote explicitly. Preserve existing Data OS files/Parquet/R2 and derived-index ownership. Market Memory owns experience; Earnings owns event_workspace; GMI owns semantic memberships; current relationship owners own propagation hypotheses; Research Factory orchestrates existing evaluators rather than becoming one.

## Cumulative continuation checkpoint

MISSION_COMPLETE: false. Chairman live Meta-CEO assignment plus repeated continuation remains present intent. Mission: make Mastermind a cumulative, time-correct market-experience intelligence that learns through existing owners and earns forward authority through independent evaluation. No new runtime, production, capital, deployment, trading, provider, credential or unlimited-spend grant.

Protected procedure pin for the current continuation: Mastermind protected master `87777e2e4705abe2cc4175738e0f3bd68134b66a`; `docs/sol_skills/INDEX.md` blob `94d1af402598894372858793a5b1931019c5fa77`; Skillpack 1.0.1 / bootstrap major 1. Required ACTIVE_EXECUTION, RECONCILE_STATE, CLOSEOUT and DELIVERY_WORKFLOW were loaded from that same protected revision before modifications.

### Canonical programme carriers

- Strategy/source carrier: Mastermind #967, current published head `17b496fde020095d566e84f21d9f4af6f1b65bbf`, DRAFT, mission architecture + source-admissibility + SEC-foundation evidence. No merge/acceptance claimed.
- Records carrier: Macro #7936, this decision, current predecessor head `f82148b050a4f2ac0fd1ffa3d02d9eee330a2af4`. This cumulative checkpoint replaces only the prior cumulative body; original decision/frontmatter remains controlling.
- Bounded SEC identity qualification carrier: Macro #8024, branch `claude/market-experience-sec-anchor-migration-20260925`, current remote/local head `d297c1da4d7c1e68e4c3ca0ce8b53d26e627ecae`, title `[DRAFT] Audit EDGAR earnings canonical filing identity`. The original write-capable head was superseded on the SAME carrier by a strict read-only split. Current code has no `--apply` path and cannot write parquet, manifest, coverage JSON or a migration registry. The carrier is clean and exact local==remote. No source-custody replacement or production writer was created.

### Preserved F0 evidence — do not redo

Historical S&P membership source qualification remains: pinned `fja05680/sp500@a2430f2af0c79ddf0748e91de11bdeb1616ab5a7`; 1,262 intervals / 1,209 normalized symbols; 2,720 snapshots; 1996-01-02..2026-08-18; zero malformed rows/inversions/overlaps/normalization collisions; frozen 2019-2023 comparison 20/20 exact under start-inclusive/end-exclusive semantics. At 2026-08-18 inclusive-end logic falsely retains AVB/EQR; existing Mastermind `members_asof` matches the source. Source remains technical qualification only because underlying index-data model-use/redistribution rights are unresolved.

Live Massive store evidence remains: existing R2 `massive_stock_day` owner reported 21,602 ticker stores, 2021-07-06..2026-09-22, 1,361 processed weekdays, zero reported full/recent missing-weekday runs, continuous SPY anchor. Existing entitlement/rights records make this a candidate trainable price substrate after per-security coverage + canonical identity audit; store-level health does not prove every ticker complete. A later deterministic Massive sample-selection command was refused before execution; that exact action was not retried or shifted to another carrier.

Broad Earnings Intelligence remains context/display, not the training corpus: prior health read reported 54,834 rows / 4,472 issuers / 54,527 canonical issuer-period rows / 2,937 QoQ-eligible issuers and explicitly `is_context_only=true`, `display_only=true`. EquityDesk/score inputs are not currently registered as a model-training corpus. Consensus remains unlicensed/ABSENT; transcript model-use rights remain unresolved.

### SEC event foundation — architecture correction

The prior hypothesis “build a historical SEC reconstruction system” was narrowed after current-source inspection. The broad primary-source anchor already exists and the Company Intelligence revision chain already exists. Do not create a second event store, filing identity, revision chain, source clock, corpus registry or publisher.

At current Macro source inspected from protected/default history, `data/edgar/earnings_8k_dates.parquet` is a broad Item-2.02 anchor with 98,975 rows / 1,314 CIKs, 2004-08-24..2026-07-02, every row carrying a parseable SEC acceptance time; 1,143 CIKs span at least eight years and 1,255 have at least twenty rows. The companion manifest has 1,315 entries, all `status=ok`, zero recorded missing older-file shards. The manifest sums 99,758 filings versus 98,975 stored rows; cause of the 783-row delta is UNKNOWN and must not be asserted without canonical-key reconciliation.

The committed parquet is still the legacy shape `ticker,cik,filing_date,acceptance_datetime,items`; current collector contract already expects `ticker,cik,accession,form,filing_date,acceptance_datetime,report_date,items`. Filing identity is `(cik,accession)`; event grouping is `(cik,report_date)`; 8-K/A is a distinct filing revision. The old `(ticker,filing_date)` key can collapse same-day distinct filings.

Existing `scripts/refresh_event_workspaces.py` already preserves SEC `source_available_at` separately from actual `observed_at`, exact filing/event identity, immutable revision chains and predecessor selection. Its source records the August 2026 incident where an incorrectly unbounded discovery published roughly 170 historical homebuilder event generations before timeout, followed by bounded discovery hardening. That incident is evidence to reuse the chain and avoid unbounded backfill, not permission to reproduce it.

### Non-publishing real event proof

A permanently development-visible AAPL event was frozen before release-body inspection: CIK `0000320193`, accession `0000320193-24-000120`, 8-K accepted `2024-10-31T20:30:25Z`, report date 2024-10-31. Exact EX-99.1 file `a8-kex991q4202409282024.htm` was fetched from SEC: 198,505 bytes; SHA-256 `e7710e52e85b34b316a246eb531312e1d983df3c89e87c80ae21e6d5f738bb0f`.

The existing Company Intelligence builder was exercised without publisher effects. Initial external validation failed only because the public validator was incorrectly called on the builder's internal return envelope (blank generation id plus private `_source_sha256` / `_aliases`); the builder itself already validates the publishable payload internally. Re-run against the path-disjoint local code produced a valid internal return for event `evt_cik0000320193_2024q4_results`: SEC availability remained `2024-10-31T20:30:25Z`, reconstruction observation was `2026-09-25T11:17:07Z`, release SHA matched the exact SEC body, revenue 94,930 USD millions carried a replayable byte receipt, transcript was typed absent, consensus/reaction/slides/wire limitations remained visible, authority was `context_only`, and every Prophet rank/size/gate flag remained false. `published=false`. AAPL is development evidence and may never be reused as untouched Market Experience evaluation evidence.

### #8024 implementation delta

#8024 extends the incumbent `collectors/edgar_earnings_8k.py`; it does not create another collector or store. Current head `d297c1da4d7c1e68e4c3ca0ce8b53d26e627ecae` adds a strictly read-only `--audit-identity` path with explicit repeated `--cik` or `--all-legacy`. It builds the canonical-key candidate in memory and emits `edgar_earnings_8k.identity_audit.v1`; receipt fields explicitly say `store_written=false`, `manifest_written=false`, `coverage_written=false`. A target CIK is refused on missing SEC shard, zero Item-2.02 rows, missing accession/form, duplicate `(cik,accession)`, or residual legacy rows after candidate merge. Legacy replacement uses `(cik,filing_date)` rather than `(ticker,filing_date)` so ticker renames cannot preserve duplicate pre-key rows. The readiness field is `candidate_ready_for_migration`, not write authority. A future canonical data migration is a separate admitted effect with its own parquet+manifest+coverage coherence and readback.

Targeted collector + filing-identity suites at current head `d297c1da4d7c1e68e4c3ca0ce8b53d26e627ecae`: **102 passed**, `python -m py_compile` passed and `git diff --check` is clean. Ruff was unavailable on the host (`No module named ruff`), so no Ruff claim. No full suite was run in the sparse worktree, per Macro sparse-worktree law.

Live non-writing rehydration over permanently development-visible AAPL/DHI/PHM/KBH/TOL returned **500/500 keyed candidate rows**, zero canonical-key duplicates, zero report-date gaps, zero residual legacy rows, `safe_to_apply=true`: AAPL 93→94; DHI 104→105; PHM 94→95; KBH 89→90; TOL 114→116. TOL proves the legacy-key loss is real: on 2020-12-07 SEC returns two Item-2.02 filings, accessions `0000794170-20-000060` at 21:49:27Z and `0000794170-20-000061` at 21:52:10Z, while the legacy store retains only the earlier filing. PHM source also exposes an 8-K/A historical revision; the keyed migration keeps it distinct.

### Holds / denied actions / effect state

- A broad all-legacy live SEC dry-run was attempted once and refused by the tool surface **before execution**. EFFECT_NONE; no temporary receipt was created. That exact action was not retried, rephrased, moved to another model/account/carrier or replaced by another broad sweep.
- The earlier follow-up mutation intended to refresh `earnings_8k_dates_coverage.json` alongside a write-capable migration was refused before effect and was not retried or moved to another carrier. That denial was not bypassed: the write path itself was removed. #8024 now qualifies only and cannot mutate the owned data/metadata triplet.
- #8024 remains DRAFT, but the obsolete HOLD-FOR-SOL wording was removed because there is no migration effect left to hold. No merge-on-green, auto-merge, data migration, production publication, paid inference, model training, forecast publication, ranking/sizing/trading authority or capital action.
- Current-head fences on `d297c1da...` are SUCCESS. Current-head `ci` run 36227440522 is still IN_PROGRESS at the latest observation; no green claim. The previous head's only red was unrelated `ci-pack-8` ontology-explorer drift, subsequently falsified as inherited by a newer main workflow_dispatch baseline where `ci-pack-8` and `ci-gate` both succeeded. Independent review remains absent; do not self-approve or merge around that gate.
- EFFECT_UNKNOWN: none.

### Historical identity spine — existing owners, leading source candidate

The identity problem is now decomposed rather than treated as one missing database. Data OS already owns current security/issuer/listing identity: `issuer_master.parquet` carries `cik -> issuer_id`, `security_master.parquet` carries `issuer_cik`, and the canonical current reader explicitly warns that this is CURRENT registrant evidence with no `asof` historical-lineage authority. Company Intelligence already owns the correct PIT semantic contract in `company_identity.v1`: ticker is a `ListingAlias` with half-open `valid_from` / `valid_to`, `IssuerRegistry.resolve_ticker(..., asof=...)` refuses ambiguity, and mappings registered today may not be retro-attributed. Its production registry is only AAPL + DHI/PHM/KBH/TOL, so the contract exists but the broad historical population does not.

`WS:MARKET-OS` already owns the cross-namespace expansion seam. Its proven-live B1A record keeps universe expansion blocked under `NO_GENERAL_NAMESPACE_RENDERER` pending the owner-routed `ListingAlias -> ListingKey` renderer + K1 vocabulary triple; `CIK_LEG_UNOWNED_ACCESS` separately names the Data OS reader-surface repair. Market Experience must consume those owner repairs, not create a second resolver. Adjacent MO-J1 records also prove current navigation-symbol resolution is already owned by the time-aware `VendorAliasTable`; current-symbol navigation and historical identity are distinct jobs.

Data OS already has partial dated rename continuity (`RENAME_EVENTS`, `vendor_aliases` validity windows, exact security supersessions, delisted-symbol receipts) and intentionally fails closed on harder reuse cases such as NYSE `B`. A recent source-readiness census independently records the remaining gap: no general historical issuer/CIK lineage or merger/spin/bankruptcy successor registry is proven. Current curated corrections are not a historical corporate-action spine.

Leading source candidate: Massive point-in-time stock reference. Official Massive documentation observed 2026-09-26 states that `/v3/reference/tickers` accepts a historical `date` and can answer which company held a reused symbol at that date; its 2021 launch example resolves ticker `L` differently in 2006 versus 2021. Current ticker-detail/reference responses carry CIK, Composite FIGI, Share Class FIGI, primary exchange and active state. Massive also exposes experimental `/vX/reference/tickers/{id}/events` specifically for ticker-change timelines, and its current FAQ says delisted tickers remain addressable rather than being removed. This is a strong fit for PIT ticker/security identity and survivorship-safe symbol continuity, but it does NOT by itself prove general acquisition successor/predecessor semantics.

Rights posture is promising but feed-admission remains explicit: `research/licenses/MASSIVE_ENTITLEMENT_RECORD.md` records enterprise rights across historical archives, reference data, corporate actions, retention, redistribution, derived materials and AI/ML; it also says written dataset-specific vendor conditions override the general record. Therefore Massive PIT reference/ticker-events is `candidate_source=true`, not admitted truth, until the existing rights owner confirms no adverse feed-specific designation and the actual production credential proves the required historical endpoint depth. No new identity store, source-rights registry or vendor plane is authorized.

### Current architecture / pilot ruling

Compute is not the critical blocker. A candidate licensed price substrate exists. A broad rights-safe SEC filing-date/availability anchor exists. The missing work is exact source identity, owned metadata coherence, historical universe rights, canonical security→issuer joins, and then frozen evaluation design.

Do not train on recovered S&P membership, EquityDesk, transcripts, consensus or Yahoo under current rights. Do not use current survivors as historical universe. Do not label a present-day SEC backfill as historical Mastermind possession. Any reconstructed event must retain SEC public availability separately from current acquisition/reconstruction time and expose a `public_reconstruction`-equivalent consumer meaning without minting a second lifecycle owner.

The first trainable slice may be frozen only after: (1) Massive per-security coverage + Data OS identity for the chosen actual stored window; (2) canonical-key SEC anchor migration/reconciliation through the incumbent collector owner; (3) exact SEC source-body availability for selected events through Company Intelligence; (4) rights-qualified historical universe definition; (5) cohort/dates/missingness/evaluation split frozen before outcome inspection. Development-visible cases remain permanently excluded from untouched evaluation.

### DO_NOT_REDO

Do not redo broad vision/capacity arithmetic, temporal-contract probe, CPI vintage audit, membership qualification, broad SEC anchor census, five-issuer identity dry-run, AAPL non-publishing workspace proof, or live Massive store-level census unless source/code materially changes. Do not rerun the denied all-legacy sweep or denied coverage-metadata mutation under unchanged conditions. Do not consume sealed E3 Q&A/transcript holdouts. Do not create a duplicate source/event/identity/memory/evaluation/publication/control plane.

### Exact continuation

Primary continuation: (1) finish #8024 exact-head CI and obtain a real independent review for the read-only audit; do not merge on the basis of local tests alone. (2) Qualify the already-entitled Massive point-in-time reference/ticker-events family as the leading historical identity source candidate, including feed-specific vendor designation and an account-level read on the runtime that actually holds Massive credentials. This host currently has neither `MASSIVE_API_KEY` nor `POLYGON_API_KEY`, so live account access is UNPROVEN here and no credential was requested or exposed. (3) Extend existing Data OS / Company Intelligence identity owners only after that source qualification; consume `WS:MARKET-OS`'s existing owner-routed ListingAlias→ListingKey renderer seam rather than minting a Market Experience resolver. (4) Keep general merger/spin/bankruptcy successor lineage as a separate typed gap unless a current owner/source proves it. (5) Continue historical-universe rights determination through the existing rights owner and per-security Massive coverage through existing price/Data OS owners; the previously denied deterministic sample command itself remains frozen. #967 strategy/date-test/review remains separate and cannot bypass its own holds.

FINALIZATION_CLASSIFICATION: CHECKPOINTED_CONTINUATION. Mission remains incomplete. No watcher/background worker was started; no custody transfer or automatic wake exists. Recommended next mode remains Extra High for bounded source repair/tests/integration if it materially changes available action access; mode never overrides a prior denial or permission gate.
