---
key: OUTCOME-LEARNING-POLICY-CALIBRATION-ARCHITECTURE
question: >
  Should Mastermind learn from operating decisions by building a new outcome database,
  generalizing the Portfolio experiment registry, publishing worker/model scorecards, or
  compiling correction-safe studies from existing owners and routing every policy change
  through explicit organizational review?
answer: >
  Establish a distinct future `organizational-learning` semantic program whose first
  component is a deterministic, rebuildable Outcome Compiler over existing owner-native
  evidence. Seal ex-ante expectations before outcomes in the owning Agent OS DEC or
  Executive event, retain lifecycle/evidence truth in Executive OS, GitHub, Agent Dialogue,
  Shared Provider Control and Agent OS, emit versioned derived outcome studies with explicit
  causal grades, preserve only load-bearing learned consequences as Agent OS discoveries,
  and enact any policy delta solely through a separate reviewed Agent OS decision plus
  shadow/canary/rollback evidence. Do not create a mutable outcome store, general company
  experiment registry, worker leaderboard, new Agent OS record type, routing authority or
  self-modifying law.
rationale: >
  The current estate already owns the raw facts needed for learning: Executive OS owns
  Job/Attempt/Worker/Event and placement receipts; GitHub owns implementation, review, CI,
  merge and production evidence; Agent OS owns organizational decisions and discoveries;
  Agent Dialogue/Wake owns continuity evidence; Provider Control owns capacity facts; and
  Model Router owns deterministic suitability policy. Copying those facts into a new mutable
  database would create competing identities, clocks and correction semantics. Historical
  route outcomes are also selected by task difficulty, ambiguity, risk, capability and
  capacity, so raw worker/model success rates cannot identify counterfactual performance.
  A thin compiler can preserve point-in-time evidence, missingness, censoring, overlap,
  uncertainty and causal limits without gaining authority. Prospective bounded canaries can
  later create the support that observational history lacks. Existing Agent OS DSC and DEC
  records already provide the correct durable homes for significant learning and policy
  rulings, avoiding another memory primitive.
alternatives:
  - option: Add a mutable organizational outcome/experiment database to Executive OS or Macro
    why_not: >
      This would duplicate lifecycle, evidence, identity, time and correction truth already
      owned by Executive OS, GitHub, Agent OS and dialogue/provider systems. A convenience
      join store would predictably become a fourth authority and invite stale copied state.
  - option: Generalize `engine/experiments_registry.py` into the company-wide experiment owner
    why_not: >
      The existing registry is a domain-specific Market/Portfolio forward-validation surface
      with market-data maturation and promotion semantics. Organizational policy has different
      interventions, identities, privacy limits and authority; absorbing it would blur owner
      law and create a second operating-policy control plane.
  - option: Rank workers or models by retrospective acceptance, speed or success rate
    why_not: >
      Assignment is intentionally selection-biased, only the chosen route is observed, and
      outcomes reflect case mix, review topology, external gates, downstream rework and luck.
      A scalar ranking would be causally misleading, gameable and organizationally unsafe.
  - option: Add a high-volume Agent OS `LRN` record for every operating episode
    why_not: >
      Agent OS is durable organizational memory, not an event warehouse. Routine evidence
      belongs in canonical owners and rebuildable studies. Existing DSC records preserve
      falsifiable load-bearing findings and DEC records preserve policy changes; production
      evidence, not convenience, would be required before adding another primitive.
  - option: Allow a statistical evaluator to update route policy automatically
    why_not: >
      Statistical evidence never grants organizational authority. Automatic edits would
      create a self-modifying constitution, bypass independent review and make rollback and
      causal uncertainty subordinate to a model-produced recommendation.
evidence:
  - "Protected Mastermind 5a7046c46046a2ecf597c849aaab914b4f7cd5e1 — docs/sol_skills/INDEX.md v1.0.1, COLD_START.md and RECONCILE_STATE.md loaded atomically"
  - "Mastermind 5a7046c — control_plane/executive_runtime.py owns Job/Attempt/Worker/Event receipts; control_plane/model_router.py is deterministic/stateless and stores no lifecycle state"
  - "Mastermind 5a7046c — config/executive_worker_routes.json records route context/reasons but production_armed=false and supplies no logged counterfactual assignment probabilities"
  - "Macro ca31568272d06f2472f65946d2435517611f31dc — Agent OS WS/DEC/DSC/handoff schemas and validators are current durable organizational memory"
  - "Macro 3a6f20dd20589c7975f828b0a4705b98dc34dc95 — WS-EVAL-OS-MEASUREMENT-LAW and DEC-EVAL-OS-RECOVERY-ARCHITECTURE-FREEZE preserve market-claim evaluation and forbid duplicate score/clock/promotion/work-queue authority"
  - "Macro ca315682 — engine/experiments_registry.py and admin/experiments.py are display-only Market/Portfolio experiment machinery, not a general operating-policy registry"
  - "Macro PR #6615 is the active stacked semantic-registration owner; current system-map tests require an exact program census, so organizational-learning registration must be a later reconciled wave"
  - "Baron and Hershey (1988), Outcome bias in decision evaluation, Journal of Personality and Social Psychology, PMID 3367280"
  - "Dudík, Langford and Li (2011), Doubly Robust Policy Evaluation and Learning, arXiv:1103.4601"
  - "Swaminathan and Joachims (2015), Counterfactual Risk Minimization, PMLR 37"
  - "Hernán and Robins, Causal Inference: What If, official online book"
  - "Forsgren et al. (2021), The SPACE of Developer Productivity, ACM Queue"
  - "Manheim and Garrabrant (2018), Categorizing Variants of Goodhart's Law, arXiv:1803.04585"
affects:
  - agentos/**
  - config/mastermind_programs.yml
  - docs/superpowers/specs/2026-08-30-outcome-learning-policy-calibration-design.md
  - docs/superpowers/plans/2026-08-30-outcome-learning-policy-calibration.md
  - docs/superpowers/specs/2026-08-30-outcome-learning-policy-calibration-governance-amendment.md
  - docs/superpowers/specs/2026-09-01-outcome-learning-executive-memory-ccl-reconciliation-amendment.md
  - mastermind/control_plane/**
confidence: high
reversibility: costly
decided_by: ceo-sol
decided_at: 2026-08-30
---

## Operational consequence

The future semantic program key is `organizational-learning`; its durable workstream key is
`WS:OUTCOME-LEARNING-POLICY-CALIBRATION`. Neither is created by this records-only decision because
the current semantic registry has an exact-count contract and PR #6615 already owns the next
stacked program registration. The registration wave must re-read current `main`, current registry
count and current owner collisions immediately before it starts. Attaching this work to
`project-active-build-control`, `shared-ai-provider-control`, Eval OS, Portfolio or Agent OS itself
would be a false parent.

Routine evidence stays in its owner. A material organizational decision may later carry a sealed
machine-readable ex-ante block in its Agent OS DEC. A routine operational exposure may carry the
same semantics in an immutable Executive event payload linked to its Job/Attempt/placement. The
original expectation is never overwritten after the outcome becomes knowable.

The Outcome Compiler may validate contracts, join typed references, compute declared metrics,
missingness, censoring, overlap, estimators, uncertainty and sensitivity, and emit an immutable
`mastermind.outcome_study.v1`. It does not choose workers, mutate source records, create jobs,
change policy or raise its own causal grade. Model prose is downstream explanation only.

## Causal admission law

Every study states the causal question, estimand, treatment, comparison, cohort, source cutoff,
metric definitions, missingness, censoring, overlap, estimator, uncertainty, sensitivity and
known unmeasured confounders. Evidence grades are:

- `DESCRIPTIVE_ONLY`
- `ADJUSTED_ASSOCIATION`
- `PARTIALLY_IDENTIFIED`
- `OPE_SUPPORTED`
- `RANDOMIZED_CANARY`
- `NOT_IDENTIFIED`

Missing propensities, unsupported alternatives, post-outcome feature leakage, ill-defined
interventions, uncontrolled censoring or unresolved source identity force the study to a lower
honest grade. A doubly robust estimator does not manufacture positivity or eliminate unobserved
confounding.

## First consumer ruling

Do not begin with worker/model routing. Start with a compliant handoff-quality intervention whose
assignment can be prospective and whose effects are comparatively low-risk:

- baseline: current law-compliant operator handoff;
- candidate: the same required handoff plus a deterministic preflight validator and concise
  mission checksum naming mission, non-goals, proof target, stop condition and return packet;
- eligible cohort: bounded, reversible, routine/elevated, frozen-scope, capacity-selectable work;
- design: pre-registered blocked randomization where lawful, intention-to-treat analysis, terminal
  and delayed follow-up, explicit guardrails and rollback;
- primary outcome: first substantive return accepted without a handoff-attributable repair;
- guardrails: escaped defects, downstream rework, independent-review severity, effect-unknown,
  authority/duplicate-carrier defects, abandonment and cost/quota class.

No promotion exists until an independent reviewer accepts the study, a load-bearing consequence is
recorded as DSC, a separate DEC authorizes or rejects the policy delta, and the owning runtime proves
the canary/rollback path.

## No-rebuild boundary

Do not create an organizational outcome DB, universal experiment registry, worker leaderboard,
parallel identity/event/time/correction store, new lifecycle, queue, watcher, retry plane, routing
authority, hidden utility score or automatic policy editor. Do not ingest private chain of thought
or publish punitive individual rankings. Do not call a schema, dashboard, merged PR or descriptive
study an institutional learning loop.

## Amendment (2026-09-01) — executive-memory and CCL reconciliation

Repair authority chain: Chairman transfer op `mastermind-agent-evaluation-fable-coo-end-to-end-20260901`
→ Slack ruling `SOL REQUEST_REPAIR / CONTINUE` ts `1788144551` → GitHub review `5061735318` (blockers
A–E) on exact head `29518b3cfad719226db0444ccb4d92a3626b07c9`. Full text:
`docs/superpowers/specs/2026-09-01-outcome-learning-executive-memory-ccl-reconciliation-amendment.md`.

**Ownership ruling.** `organizational-learning` is confirmed the canonical owner of generic
organizational-learning and executive-experience methodology — expectation/outcome semantics,
decision-quality-vs-luck adjudication, assumption/falsifier semantics, memory
admission/application/poisoning/correction/replay law, retrieval-discipline law, and self-model/
memory-efficacy benchmark methodology. CCL-A4 (Chairman Cognition Loop strategic learning) is a
consumer/application of these contracts, not a second memory store, schema authority, retriever,
graph, evaluator, or promotion plane. The CCL program's canonical carrier (currently PR #309/#311/
#292 and their successors) owes the corresponding narrow ownership-acknowledgment correction before
CCL-A3 START.

**Sequencing correction.** A minimal sealed prospective expectation/assumption receipt (the `OL-2a`
contract subset) must exist before the first CCL-A3 effect; CCL-A3 need not wait for randomized OL-5.
With only the minimal receipt, CCL-A3's learning grade ceiling is `DESCRIPTIVE_ONLY`.

**Receipt v2 extension.** `mastermind.decision_expectation_receipt.v1` semantics are preserved
verbatim. A closed `mastermind.decision_expectation_receipt.v2` extension adds `assumptions[]`,
`assumption_resolutions[]` (append-only, never mutating the sealed receipt, with resolution states
`HELD`/`FALSIFIED`/`UNRESOLVED`/`NOT_TESTED`/`CONFOUNDED`, never forced binary), and
`memory_exposure` (pre/final option-set digests and consulted-record influence). No private chain of
thought; `PUBLIC_SAFE` classification required for every V1 durable learning artifact in a public
repository, with restricted/unknown material failing closed rather than entering public Git history.

**Memory law.** Untrusted prose never becomes durable executive memory directly (admission gate);
a valid historical DEC/DSC remains evidence, not current truth or procedure (application gate). Three
frozen inequalities bind every future memory consumer: `REMEMBERED_ACTION != AUTHORIZED_ACTION`,
`REMEMBERED_SUCCESS != CURRENT_PROCEDURE`, `REMEMBERED_TOOL_SEQUENCE != REPLAYABLE_EFFECT`. A
corrected/superseded premise flags downstream lineage `MEMORY_PREMISE_INVALIDATED` without automatic
reversal or policy edit.

**Retrieval law.** Macro Context Index (CXI) remains the only retrieval plane; no memory RAG service,
no graph authority. A future two-pass retrieval law (memory-light first pass, source-attributed second
pass, delta record, anchoring/negative-transfer audit) is specified for later implementation. A later
derived relationship view's edges carry provenance `CANONICAL_DECLARED`, `DETERMINISTIC_DERIVED`, or
`MODEL_INFERRED`; `MODEL_INFERRED` edges have zero authority.

**Benchmark wave.** A later wave `OL-4E` (governance §7, between OL-4D and OL-5A) benchmarks an
empirical, non-psychological self-model across at least memory-light, naive-injection, and
anti-anchored two-pass arms; it never emits a universal CEO/worker/model score.

This amendment changes no `affects` path beyond the records already listed above and confers no new
authority: it remains
`SPEC_ONLY / RECORDS_ONLY`, creates no program, database, record type, canary, or code, and makes no
edit to the Mastermind repository, PR #299, PR #162, or any CCL carrier.