# Executive Capacity Fabric: Go public-reader and request-checked stream

Parent: WS:EXECUTIVE-CAPACITY-FABRIC. Owner: Sol. Full integration remains PARTIAL; streaming/request-check source is BUILT_NOT_PROVEN for production. No new workstream, lifecycle, provider registry, quota store, scheduler or transcript was created.

Procedure: protected Mastermind bffe2ca8506346ea278c8ee469bf1ac30a4de008, Skillpack 1.0.1 / bootstrap 1. Current Chairman continuation authorized the source work. No runtime/policy exception is inferred from this record.

## What became true

The exact existing public reader and CLI at Macro 4681a2c832ecbf489a13e79af69ab42315fe818f ran under isolated Python on the authorized MacBook through Remote Desktop Commander. Source blob identities were checked before execution. The actual --live path fetched provider JSON and HTML, parsed them, and returned exit 0, 37 model rows, acquisition_origin=public_metadata_not_account_entitlement and production_armed=false. This resolves the previous actual-HTML validation gap; no transcribed fixture substituted for the live reads. No API key or provider inference was used. This is one public-read canary, not an installed recurring collector or a complete router.

The new request guard checks current public observations against existing-owner review digests and exclusive deadlines, without granting access or inventing quota. The streaming edge runs that check before credential loading and again before POST. Changed/expired offer assumptions block the next request. An inventory-only unrelated model addition does not invalidate the existing model. Promotion/conditional-retention deadlines are required owner inputs; provider cutoff timezones are not guessed. Automatic quote evaluation and owner publication remain pending.

The single-account streaming source forwards progressively, preserves request/session bytes, observes protocol terminal events and never retries or changes accounts. No partial stream, cancellation, sink failure, missing terminal or uncertain cleanup is called success. It reuses the existing kernel's request/credential boundary and owns no tools or memory. It is not yet an installed localhost proxy or coding-agent harness binding.

## Exact receipts

- Macro #7143 guard implementation: ab710f4a653fcec4cf2505ed7ab49c7faf19b623.
- Mastermind #622 streaming and cross-repo proof: bd46e6c7c728c4fab31aa03ecaaa8a6eb10b599c.
- Full design/evidence checkpoint: Mastermind aecf4d95594ecbc760cdefbece1a623ed667251d, docs/OPENCODE_GO_REQUEST_CHECKED_STREAM_CHECKPOINT_2026-09-14.md.
- New source/test blob identities were matched to the tested local bytes. 20 guard + 39 streaming + 7 composition checks = 66 new passing tests. Combined with the prior suite: 198 passed. Python 3.13.5 / pytest 9.0.2; compile passed.
- Real loopback HTTP proof delivered the first SSE bytes before the server sent its terminal event, preserved the body and session and shut down its fixture server. It used synthetic provider/account/admission data, not Go inference or production TLS.
- Cross-repository parser/guard/stream tests preserve a workspace marker and prior tool-result context. Invalid metadata blocks before key access; expiry during key loading blocks before POST.

## Real boundaries

Sandbox public networking still failed DNS. Native public HTTP reads and the unmodified public collector succeeded. A separate native source-tree inspection was platform-blocked and was not retried via another device, worker or carrier. #622 remains the conflicting old-parent stack; no rebase, force, protected merge, parent conflict resolution or latest-base compatibility claim occurred. The genuine earlier #622 delta from 808101957bc483ac2d9f9491df897166166e8b50 to 097e1bac090be8f254013a2729e0f7ef69c94054 was independently enumerated through GitHub as five added Go files. The new source was added on the same draft carrier only.

#583 is already merged: do not restore its obsolete enrollment/ACL ancestors from #622. #7143 remains stacked on #7103. #594 owns the still-pending time/context-specific quote integration; no parallel pricing engine was created. Current global privacy-digest invalidation is intentionally conservative. A terminal stream receipt is not task acceptance. Synchronous DNS/header/callback blocking still requires the existing outer worker-supervisor bound.

Three registrations remain Chairman-reported, not verified runtime enrollment. No keys, authenticated usage, account mutation, live worker, router activation, paid overflow or consent change occurred. The prior multi-account provider-policy, enrollment/identity and reservation gates remain unresolved. A no-op request-check callback or fabricated review digest is never production admission.

## Exact next action

Sol retains acceptance. After the source-inspection permission boundary is resolved, reconcile existing #622 with landed #583 without restoring superseded credential/ACL code; obtain exact current-base CI and independent review; compose one disabled single-account streaming binding using existing provider-home enrollment and canonical provider/offer/policy/capacity evidence. Disable outer harness/SDK retries consistently. Only after runtime and enrollment gates pass should a real coding-agent task run through streaming, tools and the same workspace to a visible result. That is the first account production acceptance target, not merely a successful HTTP call.

No Fable or other worker was commissioned; no watcher, timer or automation awaits a response. This checkpoint supersedes only the public-reader uncertainty and newly implemented source behavior from the previous offer-discovery handoff. Full production/self-healing activation is not claimed.
