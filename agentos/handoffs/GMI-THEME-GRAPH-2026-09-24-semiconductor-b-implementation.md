---
workstream: "WS:GMI-THEME-GRAPH"
session: claude/ssd-semiconductor-theme-intelligence-b-impl-988406e131fd90b9
model: fable
ended_because: ci_handoff
mission: >
  Implementation carrier and cumulative working checkpoint for
  gmi-semiconductors-fable-ceo-e2e-20260923-chairman-001: ship Semiconductor Theme
  Intelligence B (paid Themes research journey with the TSMC HBM/advanced-packaging
  witness W-A and the onsemi SiC/GaN/specialty witness W-B) through the existing
  GMI/K1/Earnings/FIF/rights/F04/Macro-API owners, orchestrated by one Fable principal
  with bounded fabric children. C1 (SBD-41..48) stays deferred.
state_before: >
  Research/spec/plan/handoff truth existed only on draft/HOLD PR #7780 at
  b68069b2e1296bfc772e4f4e3a0c1cc3845f89eb. No implementation carrier, no PICKUP_ACK,
  no START, no product code. Macro main at packet preparation was dd4d965de7d52f4c68f6dbc984c7a05a70e0d62b.
changed:
  - path: agentos/handoffs/GMI-THEME-GRAPH-2026-09-24-semiconductor-b-implementation.md
    what: "Created the implementation-operation working checkpoint: pickup receipt, procedure pin, current-main/custody reconciliation, Fable custody rulings R1-R4, wave plan and exact next action; updated with wave-1 dispatch, the R4/T05 read-only qualifications, the fabric ship-loop incident and the R4 DECISION_REQUEST; updated again after wave-1 integration (38d950bdabb3), the T02 REJECT review and its fix commit (2deb758859d7), the T05 GLM content-filter failure and the wave-2 dispatch queue; updated again after the T05b reject/fix/re-verify and integration (7d297018db27 + CI d5b0c00d772e), the Sol R4 ruling consumption, the Healthcare R10 disposition, the second T07/T08 REJECT and fix 34b89dd3e8e1, and the T04/T05a lane returns; updated again after T07/T08 and T04 integration, the T05a double REJECT and seat fixes, the T10-FIX REJECT and seat fix, the sec_edgar family, the #7669 and SOL-HC-R12 rulings, the five consumer dispositions and the T09/T10b placements; updated again after the T06 definition_unqualified amendment, the sec_edgar rights-basis qualification (Consumer R11), T10 + T10b integration and CI wiring, the #7669 seam return, the T05a rounds 3-5 (prose parser abandoned for closed house-style grammars grounded on the real EX-99.1 exhibits), the T09 REJECT and its fabric fix lane, the Robotics/Consumer/Mining/Meta-CEO/Finance/Energy/Technology consumption + dispositions, and the B-foundation build-out ruling request to Sol."
verified:
  - claim: "Current protected procedure is compatible and loaded from one pin."
    command: "git rev-parse origin/master in Mastermind; read docs/sol_skills/INDEX.md, ACTIVE_EXECUTION.md, WEB_CEO_DELEGATION.md, WATCHER_ACTION_LOOP.md, RECONCILE_STATE.md, docs/AGENT_DIALOGUE_SESSION_CLOSE_LAW.md, docs/EXECUTIVE_WORKER_ROUTING_CHAIRMAN_ADDENDUM.md at that commit"
    result: "Mastermind a7d2b3049e5cdc523e91e61a6e9d70a1cb911157; Skillpack mastermind.sol_skillpack.v1 1.0.1 / bootstrap 1; same pin as the #7780 packet."
  - claim: "PICKUP_ACK for the operation is durably recorded on the packet carrier."
    command: "gh pr comment 7780 --body-file /tmp/pickup_ack_7780.md"
    result: "https://github.com/mastermindx-market-intelligence/macro/pull/7780#issuecomment-5807221991"
  - claim: "Macro main movement since the packet pin is disjoint from every planned B path."
    command: "git diff --stat dd4d965de7d52f4c68f6dbc984c7a05a70e0d62b 9438880952d3375b00a042381705c2e6c85305e3 -- contracts/theme_graph contracts/evidence_foundation contracts/market_ontology engine/theme_graph engine/market_ontology engine/company_intelligence lib/evidence_foundation.py app/main.py app/earnings.py app/paywall.py templates/basket_detail.html.j2 templates/state_of_themes.html.j2 scripts/check_theme_graph_contracts.py tests/test_theme_graph_contracts.py tests/test_evidence_foundation_contract.py config/theme_sources.yml config/theme_crosswalk.yml"
    result: "Empty diff on planned paths; main moved only research_vault catalog + .github/ci options scope (2 files). Carrier base = 9438880952d3375b00a042381705c2e6c85305e3."
  - claim: "Dependency carriers are unchanged since packet preparation and no STARTed or EFFECT_UNKNOWN operation owns the planned implementation."
    command: "gh pr view 7773 7462 7669 --json headRefOid,state,isDraft; gh pr list --search semiconductor; git branch -r | grep -iE 'semi|robot'; Slack search for the operation key and PICKUP_ACK after 2026-09-21"
    result: "#7773 f10211657c6c open/draft; #7462 31706d7322af open/draft (last commit 2026-09-22 03:04 -0700, store.py hunks = strict-read flags at lines 208-300); #7669 2c28d950aa94 open/draft HOLD-FOR-SOL (template hunks in JS render functions ~402-1502). No robotics or semiconductor implementation branch/PR exists. No Slack thread root exists for this operation."
  - claim: "Bounded custody questions were posted to both held source owners."
    command: "gh pr comment 7462 --body-file /tmp/custody_7462.md; gh pr comment 7669 --body-file /tmp/custody_7669.md"
    result: "#7462 issuecomment-5807268400 (one-line EVIDENCE_COLUMNS append); #7669 issuecomment-5807268641 (generic hidden research mount + two asset includes). Both hunks remain frozen until answered."
  - claim: "Native owner interfaces named by the plan exist on the carrier base."
    command: "git show origin/main:<path> for contracts/theme_graph/evidence.v1.schema.json, engine/theme_graph/store.py, engine/theme_graph/rights.py, lib/evidence_foundation.py, contracts/evidence_foundation/vocabulary.v1.json, engine/company_intelligence/{event_workspace,event_workspace_build,issuer_profiles,identity}.py, contracts/financial_intelligence_packet.schema.json, app/earnings.py, app/paywall.py, engine/earnings_narrative/private_publication.py, engine/research_vault/r2_store.py, templates/basket_detail.html.j2, templates/state_of_themes.html.j2, config/theme_sources.yml, config/theme_crosswalk.yml"
    result: "All present. EVIDENCE_COLUMNS has 12 columns and no curation_assertion; rights registry has 3 families (mastermind_curated direct_display_ok; finviz_themes, ths_concepts unresolved); production_registry enrolls AAPL + DHI/PHM/KBH/TOL only; ticker_cik_ledger has ON=1097864 and no TSM row; vocabulary owner_stores has 13 entries and no curation subtype."
  - claim: "START was emitted separately from PICKUP_ACK and the one implementation carrier exists."
    command: "gh pr comment 7780 --body-file start_7780.md; gh pr create --draft --base main"
    result: "START = https://github.com/mastermindx-market-intelligence/macro/pull/7780#issuecomment-5807391383; carrier = #7870 (draft), first commit 545ece0c7177a56627204e77302cd9c2d4d03f3e on base 9438880952d3."
  - claim: "Wave-1 children returned commits that pass their deterministic gates on a second environment (Studio python3.14) before integration."
    command: "python3 -m pytest tests/test_semiconductor_research_inputs.py -q (fe9238b9aeee); python3 -m pytest tests/test_theme_research_private_binding.py tests/test_theme_research_rights_refresh.py -q plus test_theme_sources_registry/test_market_ontology_exposure_map/test_deploy_update_self_heal/test_theme_graph_contracts (d30457d5de7c); python3 -m pytest tests/test_semiconductor_guidance_history.py tests/test_issuer_profiles_a5a.py -q (53c5bd3702f3)"
    result: "T01 9 passed, leak-guard grep NONE, 26 files; T03 30 passed + 364 adjacent passed, store.py diff 0 lines; T06 32 passed + 38 adjacent passed, fixtures leak-guard NONE. Independent Opus READ_ONLY reviews launched for all three (builder != reviewer; MiniMax output under R20)."
  - claim: "Candidate (i) for the R4 private binding is public by construction."
    command: "gh api repos/mastermindx-market-intelligence/macro --jq '{private,visibility,forks_count}'; curl -sI https://raw.githubusercontent.com/mastermindx-market-intelligence/macro/main/data/theme_graph/evidence.parquet; git ls-files data/theme_graph; grep -n 'git add data/' scripts/ci/daily_engine_commit_outputs.sh"
    result: "private=false visibility=public forks=0; raw parquet HTTP 200; evidence.parquet tracked; daily engine job commits data/ (line 64); lib/config.py data_dir() has no env override and store.py path functions take no root argument, so candidate (iii) is not constructible per-store."
  - claim: "The Earnings owner is form-agnostic and only its intake filter blocks a 6-K; onsemi is silently skipped today by the fiscal cross-check."
    command: "read-only Opus qualification over engine/company_intelligence/*, scripts/refresh_event_workspaces.py:210-233,632-634,663-716,1021-1029, engine/fundamental_forensics/metric_registry.py:50-53, data/edgar/ticker_cik_ledger.json; SEC submissions/exhibits for CIK 0001046179 and 0001097864 fetched read-only"
    result: "build_event_workspace + validate_event_workspace accept a real TSMC 6-K unchanged; _select_newest_results_rows admits only 8-K with Item 2.02 and every TSMC 6-K row has items==''; onsemi 52/53-week period ends (2026-04-03, 2026-07-03) fail the calendar cross-check and the 'quarter ended' regex; ticker ledger has ON=1097864 and no TSM; FIF registry excludes IFRS/TWD/20-F (typed gap for W-A financial packet); guidance_item.v1 has no validator and no currency field; tests/test_issuer_profiles_a5a.py:114 pins len(registry)==5."
  - claim: "Wave 1 (T01 fixtures, T03 rights/admission, T06 guidance history) is integrated on the carrier with review nits applied and CI wiring."
    command: "git cherry-pick -x fe9238b9 53c5bd37 d30457d5; edit .github/ci/legacy-jobs.yml (two new run: steps + one suite appended to the company-intelligence core step); python3 scripts/audit_unrun_tests.py; combined pytest"
    result: "Carrier 38d950bdabb3 pushed to #7870; #7874 closed with a pointer; stray claude/semi-b-* origin branches deleted; unrun-suite audit clean except base-inherited tests/test_render_dead_ref_targets.py; ledger outcomes recorded for T01 (MiniMax-M2.7-highspeed, reviewer-accepted) and T03 (glm-5.3-flash, reviewer-accepted)."
  - claim: "The T02 fix commit closes all three review blockers to an independent reviewer's satisfaction."
    command: "general-purpose Opus READ_ONLY re-verification of 2deb758859d7 (Robotics reference payload proof, recursion probe under setrecursionlimit(200), schema closure walk, gate, audit_unrun_tests, identity-test probes)"
    result: "ACCEPT_WITH_NITS: B1/B2/B3 CLOSED (encode of stamped vs unstamped byte-identical, gmi-curation://robotics_automation/gmirca_18d7852490e571465270aa5024442859, caller dict unmutated); 130 passed, 1 xfailed; fixtures unchanged; store.py/basket_detail untouched; identity test admits only https://example.invalid/. Nits taken in 012db0e6dc59."
  - claim: "T10 part 1 (m1 glm-5.3 lane commit f53c7c3845b3) was independently reviewed and REJECTED on two local law violations; everything else is clean."
    command: "general-purpose Opus READ_ONLY review over the T10 review worktree (sparse-checkout disabled): leak/XSS/authority/bilingual/mount checks, pytest, node --check, regression tests/test_state_of_themes.py"
    result: "REJECT. Blocking: (1) time-mode <option> labels built with .l-en/.l-zh spans render both languages concatenated (repo fix pattern site/assets/js/a7851105.js:763-773: data-en/data-zh + langchange relabel); (2) `limitations` (and authorized_coverage) validated but never rendered (root cause: the commission's UI list omitted it; seat rules the LAW authoritative — a coverage caveat is mandatory). Clean: no token/payload/URL leak, zero innerHTML, authority flags refused unless literal false, no client arithmetic on paid figures, mount hidden with the .theme-research[hidden] author-rule guard, basket_detail untouched. Gate 18 passed; node --check clean. tests/test_state_of_themes.py::test_check_validated_claims is RED AT BASE (43 offenders in unrelated templates; zero in T10 files). Ledger outcome recorded review-rejected/accepted_by none."
  - claim: "T02 (shared curation_assertion v1 contract) is integrated after an independent REJECT review whose three blockers are fixed without re-hashing any fixture."
    command: "git cherry-pick -x d559511f2f2b (=7fcbadb7da43); fix commit 2deb758859d7; COLLECT_LANE=nightly pytest over the seven B suites; scripts/check_theme_graph_contracts.py"
    result: "Review blockers: review.review_due_at and source.native_digest were non-nullable (frozen Robotics reference payload carries null for both); encode_assertion refused unstamped payloads instead of minting. Fix: both fields nullable (anyOf null); encode_assertion is the mint path; curation_revision content-validates before hashing via a shared _validated_content (no recursion); decode_assertion(NaN) -> None; authority tests pin the code rule; frozen Robotics payload pinned verbatim as a test; 28-fixture roster pin; T01 identity-leak test admits only https://example.invalid/ URLs. Gate 194 passed, 1 xfailed; contracts script output unchanged; store.py and basket_detail.html.j2 untouched. Independent Opus READ_ONLY re-verification of 2deb758859d7 in flight."
  - claim: "T05b (Earnings intake generalization) is integrated after an independent REJECT, a seat fix and an independent ACCEPT_WITH_NITS re-verification, with its nit taken."
    command: "general-purpose Opus READ_ONLY review of 6254ec3ab8f9 (REJECT: discriminator refused the real TSMC results 6-K because the SGML parser dropped <DESCRIPTION>; fail-open Apple profile fallback; false reportDate premise; seat-found strictly-before anchor); seat fix 78c84cfaff2e; re-verification ACCEPT_WITH_NITS (B1-B4 CLOSED, nine mutants killed, 169 passed); nit fix 861a0409c5ff; git cherry-pick -x x3 (2177169fb09b e1afae806a97 7d297018db27); CI step edit d5b0c00d772e; scripts/audit_unrun_tests.py; combined gate"
    result: "Results-6-K discriminator reads EX-99.1 description/filename (earnings, quarterly/financial results, results of operations, interim statements, a<q>q<yy>e filename) and never 'press release' alone; 6-K issuers anchor the fiscal quarter on filingDate; profile guard fails closed (RefreshError identity/profile); tests/test_semiconductor_earnings_intake.py wired into the company-intelligence core step; carrier gate 292 passed over nine suites; audit_unrun_tests clean except base-inherited tests/test_render_dead_ref_targets.py."
  - claim: "Sol ruled R4 and the seat consumed the ruling on the same carrier."
    command: "gh api issues/comments/5808854275 (#7780 SOL RULING / CONTINUE, identity SOL-R4-PRIVATE-BINDING-20260924-HEALTHCARE-R11); consumption record posted #7780 issuecomment-5808981293; Healthcare disposition posted #7870 issuecomment-5808981043"
    result: "Architecture choice (ii) ACCEPTED_WITH_QUALIFICATIONS; implementation/production acceptance NOT_GRANTED. T11 plan amended: existing Research Vault via engine.research_vault.r2_store, key theme_graph_private/v1/assertions/<curation_revision>.json registered through the delivery-classification owner, immutable bytes + existing writer/publication, no public-body shortcut (direct_display_ok grants nothing), nonce-object privacy proof by the store owner, positive-only entitlement grace capped at 86,400 s, source rights an independent veto, shared POST /api/themes/v1/research/{query,evidence} behind require_user + enforce_site_full(always=True). Healthcare items not enrolled by the ruling (REPORTED_ECONOMIC_RIGHT / REPORTED_SUPPLY_STATUS, null quantity basis, gross/net, not_disclosed estimate, qualitative precision, signed measurement) returned HELD as a contract extension beyond B."
  - claim: "The T07/T08 first fix (437644197a47) narrowed but did not close the capacity data-deletion defect; fix #2 widens the fingerprint to the whole physical row and refuses competing packets."
    command: "general-purpose Opus READ_ONLY re-verification of 437644197a47 (REJECT: fingerprint = observation+measure_scope only, so 12H vs 16H or model-only rows still collapsed, mislabelled facility_counted_once_multi_owner, and a lone scoped row could be deleted and silence mixed_construction_scope; nit: competing bound packets last-in-list wins); seat fix 34b89dd3e8e1; COLLECT_LANE=nightly gate over seven suites"
    result: "_physical_fingerprint(row) = row minus _CAPACITY_OWNER_FIELDS {assertion_ref, curation_revision, source_business_label, company_node_id, retrospective, stage_source_language} (the JV fixture's two owners differ only in stage phrasing); two DIFFERENT ready bound packets attach neither and add competing_financial_packets; five new tests; 187 passed; schema unchanged. Third independent re-verification in flight."
  - claim: "T04 (K1 reference subtype, glm-5.3-flash, m1) and T05a (witness issuers/profiles, MiniMax-M3, mb) returned single commits that pass their gates on the seat with no push, PR or watcher."
    command: "git bundle pull into refs/lanes/t04 (db857526fad9) and refs/lanes/t05a (ddc0fbad0841); detached review worktrees via worktree_storage.py; pytest"
    result: "T04: 6 files (+884/-8; contracts/evidence_foundation/vocabulary.v1.json +10, engine/theme_graph/curation_assertion.py +259 with reference_for_assertion/_native_clock/_rights_and_missingness, two existing K1 fixtures edited, tests/test_semiconductor_native_identity.py 515 lines) — 213 passed over five suites. T05a: 5 files (+1044/-7; tsm_issuer/on_issuer/tsm_profile/on_profile + extractors, TSM_FIF_GAP typed, event_workspace(+build) generic hooks, tests/test_semiconductor_earnings_witnesses.py 570 lines) — 304 passed over eight suites. Independent Opus READ_ONLY reviews in flight for both; T05a review also maps overlap with the integrated T05b hunks."
  - claim: "T07/T08 (composition) and T04 (K1 reference subtype) are integrated with CI wiring after independent acceptance."
    command: "git cherry-pick -x 51ab864d6f01 437644197a47 34b89dd3e8e1 (T07/T08; third re-verification ACCEPT_WITH_NITS, 18-probe table, stage_source_language judged attribution) and db857526fad9 (T04; ACCEPT_WITH_NITS, custody additive, clocks never fabricated); market-ontology CI step + native-identity suite beside the K1 contract step; two rights-branch tests for the surviving T04 mutant (013639769c65); combined gates"
    result: "Composition gate 216 passed; K1 gate 250 passed; broader owner gate 798 passed + 1 base-inherited failure (f13 accuracy-ledger spec vs CSV). Carried T07/T08 nits: same-selector different physical lines (source_label not in the capacity row), complementary disjoint-key packets labelled competing, retrospective excluded from the fingerprint, authorized_coverage has no reason slot; carried T04 nits: rights.state is the mint-time licensing snapshot (documented; serving path re-checks engine.theme_graph.rights), freshness hardcoded unknown."
  - claim: "Seat fixes landed for the onsemi exhibit type and the SEC rights family."
    command: "6e7375fa13b1 (_select_exhibit_99_1 admits a SOLE bare EX-99 text exhibit on the Item 2.02 8-K path; the T05b pin rewritten to that rule); b256aa6a756a (config/theme_sources.yml sec_edgar: direct_display_ok / keyless_public, review row citing 17 U.S.C. §105, fresh-snapshot test); gates 109 passed / 78 passed + 1 xfailed; check_theme_graph_contracts 0 errors"
    result: "Witness W-B's Q2-2026 8-K (exhibit TYPE EX-99) is no longer dropped by the selector; SEC-sourced evidence resolves to a display-ok family with redistribution not claimed (licensing tuple True/True/False)."
  - claim: "Three external rulings were consumed and five downstream verticals answered on the carrier."
    command: "gh api comments 5808986207 (#7669 custody ruling), 5809358801 (SOL-HC-R12), 5808854275 (R4); dispositions posted #7870 issuecomment-5809602368; checkpoint issuecomment-5809602747"
    result: "#7669: ONE guarded include of templates/_basket_intelligence_mounts.html.j2 after the back link outside #app, vertical partials separately owned (T10 part 2 unfrozen; executor lane T10b dispatched). SOL-HC-R12: B's v1 unchanged; extensions go to a separately gated additive v1.1 under the same owner; unsupported profiles refuse visibly. Materials AM1 HELD->v1.1, AM2 resolver-not-field (rights.family_for_source_ref + additive mapping in T09/T11), AM3 closed discriminator boundary accepted, AM4 confirmed; Finance sec_edgar ACCEPTED/landed; Industrials 1/3 confirmed, 2 accepted as the definition_unqualified amendment; Consumer 1-4 answered; Mining acknowledged."
  - claim: "T06 amendment: absence-on-both optional definitions surface as definition_unqualified:<field> limitations (Industrials W12 / Consumer R10 promise)."
    command: "TZ=UTC COLLECT_LANE=nightly python3 -B -m pytest -p no:cacheprovider -o addopts=\"\" -q tests/test_semiconductor_guidance_history.py tests/test_semiconductor_research_composition.py tests/test_semiconductor_industrial_views.py tests/test_semiconductor_research_temporal.py tests/test_issuer_profiles_a5a.py"
    result: "138 passed; commit c6c67c878b86 pushed."
  - claim: "sec_edgar rights basis qualified per Consumer R11 (SEC dissemination policy, not 17 U.S.C. §105); licensing tuple unchanged."
    command: "pytest tests/test_theme_sources_registry.py tests/test_semiconductor_theme_research_ui.py; node --check site/assets/js/theme-research.js"
    result: "46 passed, node clean; commit 7d456cd37d84."
  - claim: "T10 client integrated after re-verify ACCEPT_WITH_NITS (prior blockers M6/M15 closed) and wired into CI."
    command: "git cherry-pick -x f53c7c3845b3 bc3331a3f786 5169317d1204; edit .github/ci/legacy-jobs.yml unrun-subsector-themes + ci.yml paths; python3 scripts/audit_unrun_tests.py"
    result: "9f209d6b7023 aded6c88f33e 00ffed7d0abb + CI 70fde3c79956 pushed; no semiconductor suite UNRUN."
  - claim: "T10b include seam integrated after independent review ACCEPT_WITH_NITS (+1/-0 on basket_detail at line 308, outside #app; 5/6 mutants killed) and returned to #7669 per the custody ruling."
    command: "git cherry-pick -x ec645bd9f43f; pytest tests/test_basket_intelligence_mounts.py; gh pr comment 7669"
    result: "1b689fa9bc74 + CI 8e478b2feed0 pushed (15 passed); blobs 77e33e33 / 80494fb0 / 245012a2 returned at #7669 issuecomment-5810675425."
  - claim: "T05a closed grammars bind all four REAL EX-99.1 exhibits (local smoke, bodies not committed): TSM Q1 35.90 + (39.0, 40.2, 2026Q2, 31.7); TSM Q2 40.20 + (44.6, 45.8, 2026Q3, 32); onsemi Q1 1513.3 + (1535, 1635, 2026Q2); onsemi Q2 1603.5 + (1650, 1750, 2026Q3)."
    command: "bind_release_document over scratchpad copies of 6-K 0001046179-26-000199/-000451 and 8-K 0001140361-26-018868/-030989 EX-99.1; extract_release_facts / extract_guidance with the matching FiscalPeriod (worktree at 4c76dde6d82b)"
    result: "All four bind; 12-suite gate 425 passed; commit 4c76dde6d82b = refs/lanes/t05a-fix4 (under fifth independent review, NOT yet on the carrier)."
  - claim: "Checkpoint and shared-owner dispositions recorded; B-foundation build-out scope escalated to Sol rather than self-authorized."
    command: "gh pr comment 7870; gh pr comment 7780"
    result: "#7870 issuecomment-5811058195; #7780 issuecomment-5811066300 (options A/B/C)."
  - claim: "T05a closed grammars accepted (review #5 ACCEPT_WITH_NITS, no blockers) and integrated on the carrier as cherry-picks dce087c5e5f5..6740080ab64e (chain ddc0fbad0841→4c76dde6d82b) plus the seat nit fix b8baf4e787b5 (parenthesised onsemi cell → typed absence, sign-blind agreement closed; outlook range fullmatch pinned by two mutation cases; inherited-year comment on the USD template). Witness suite 89→93; all four real EX-99.1 exhibits still bind (TSM Q2 40.20 + 44.6/45.8 2026Q3 fx 32; TSM Q1 35.90 + 39.0/40.2 2026Q2 fx 31.7; ON Q2 1603.5 + 1650/1750 2026Q3; ON Q1 1513.3 + 1535/1635 2026Q2) in a local smoke that never enters the repo."
    command: "python3 -B -m pytest -p no:cacheprovider -o addopts=\"\" -q tests/test_semiconductor_earnings_witnesses.py; local smoke over the four scratchpad exhibit copies with the correct FiscalPeriod"
    result: "93 passed; mutant (dropped $ anchor) → 2 failed; smoke values as listed"
  - claim: "Carrier-level intake integration (f38b73f3ab72): the PRODUCTION tsm/on identities and profiles run through discover_new_homebuilder_revisions on the real 2026 EDGAR submission rows and SGML manifests (public metadata; synthetic bodies). TSMC's results 6-K is admitted on the a<q>q<yy>e filename convention alone (its EX-99.1 is described only 'EX-99.1'); the June monthly-revenue, month-end and consolidated-financial-statements 6-Ks (EX-99.1 a2026q2consolidatedreport-.htm) of the same quarter-end are each refused; onsemi's Item 2.02 8-K is the only May-2026 candidate; the admitted payloads carry the closed-grammar facts and guidance."
    command: "python3 -B -m pytest -p no:cacheprovider -o addopts=\"\" -q tests/test_semiconductor_earnings_intake_integration.py tests/test_semiconductor_earnings_intake.py tests/test_semiconductor_earnings_witnesses.py tests/test_refresh_event_workspaces.py"
    result: "169 passed"
  - claim: "Intake seam found and closed by that test: within the 52/53-week tolerance the event kept the DERIVED calendar quarter end (2026-03-31) instead of onsemi's stated end (2026-04-03), so the profile's date corroboration could never find it in production (fact_revenue would have been a typed absence). scripts/refresh_event_workspaces.py now carries the stated end when drift is non-zero (reachable only for fiscal_calendar 52_53_week; event id unaffected; calendar issuers byte-identical); T05b's tolerance test pins fiscal_period == {2026,1,'2026-04-03'}."
    command: "full company-intelligence CI core step (legacy-jobs.yml:4596 list + the two new suites)"
    result: "2221 passed in 100.16s; audit_unrun_tests.py: no semiconductor suite unrun (pre-existing base reds unchanged)"
  - claim: "CI wiring: tests/test_semiconductor_earnings_witnesses.py and tests/test_semiconductor_earnings_intake_integration.py named in the company-intelligence core run: step (0db3e23bf373); ci.yml paths add both suites plus engine/company_intelligence/issuer_profiles.py and scripts/refresh_event_workspaces.py."
    command: "grep -c in .github/ci/legacy-jobs.yml and .github/workflows/ci.yml"
    result: "present"
  - claim: "Independent READ_ONLY review of f38b73f3ab72: ACCEPT_WITH_NITS, no blockers. Proven: the stated-end hunk is unreachable for DHI/PHM/KBH/TOL/TSM (tolerance 0; brute force over 2024-2027 anchors × ±15d found 0 reachable rows); event id and every alias id are byte-identical between parent and new payloads; no payload validator constrains calendar_end; no ON event has ever been published (DISCOVERY_TICKERS and on_issuer exist only on this branch); the onsemi assertions fail on the parent with detail 'no Quarters Ended table dates a column 2026-03-31'. Nits landed in 2e47e5b6d79d (::notice on the adopt path; exact resolved-accession set assertions; pooling-key hazard comment for a future Jan/Apr/Jul/Oct 52/53-week roster issuer)."
    command: "Opus READ_ONLY agent over the carrier worktree; re-run of the four suites after the nit commit"
    result: "reviewer gate 154 passed; post-nit 169 passed"
  - claim: "LIVE-EDGAR production proof (seat probe, read-only, declared User-Agent, no publish, no repo write): the PRODUCTION discovery for TSM and ON against data.sec.gov + Archives yields evt_cik0001046179_2026q1/2026q2_results (fact_revenue_usd 35.9 / 40.2 usd_billions; guidance 39.0–40.2 2026Q2 fx 31.7 / 44.6–45.8 2026Q3 fx 32) and evt_cik0001097864_2026q1/2026q2_results (fact_revenue 1513.3 / 1603.5 usd_millions with calendar_end 2026-04-03 / 2026-07-03; guidance 1535–1635 2026Q2 / 1650–1750 2026Q3). Under the composer's economics framing (reported→prior outlook/actual/new outlook) assess_management_sequence returns management_sequence_assessment.v1 READY for 2026Q2 for BOTH witnesses, every span byte_replayed under rp_public_primary_v1, authority context_only. Every pre-identity 2025 / early-2026 filing is skipped with a discovery-skip warning; the monthly-revenue, month-end and financial-statement 6-Ks are refused by the discriminator."
    command: "scratchpad proof_live_edgar2.py / proof_live_edgar3.py against live EDGAR (values printed; assessments saved as JSON in the scratchpad only)"
    result: "TSM 2 revisions + READY; ON 2 revisions + READY; 30 skip warnings, 0 exceptions"
  - claim: "Two further production seams found by that run and closed on the carrier: (1) first-ever discovery reaches filings dated before the issuer's attested listing (TSMC Q1-2025 6-K) and build_event_workspace raised 'maps to no issuer at 2025-04-17' — such rows are now skipped on the raw row before any fetch (3d58f49a8ee2; real pre-identity rows of both issuers pinned); (2) onsemi's Q2-2026 exhibit names October 3, 2025 first under a 'Quarters Ended' header (five trailing quarters, oldest first) so first-match dating refused the real filing with drift=270d — for 52/53-week issuers discovery now adopts the unique named date within tolerance, refusing on none or two (e6c3b999b795; unit + carrier-level tests with the real header/rows). Calendar-quarter issuers keep both paths byte-identical (no valid_from; tolerance 0)."
    command: "python3 -B -m pytest -p no:cacheprovider -o addopts=\"\" -q tests/test_semiconductor_earnings_intake_integration.py tests/test_semiconductor_earnings_intake.py tests/test_refresh_event_workspaces.py tests/test_semiconductor_earnings_witnesses.py"
    result: "177 passed; company-intelligence core CI step 2223 passed before the second seam fix (third run in flight)"
  - claim: "Third live-EDGAR seam (definitional, ours): the real Q1→Q2 sequences came back READY but prior_vs_actual REFUSED — TSMC metric_mismatch (fact metric revenue_usd vs guidance revenue), onsemi basis_change (prose basis on the fact vs the guidance's reported_gaap). Both witnesses' facts now state the same closed tokens as their guidance (metric revenue; basis reported_ifrs / reported_gaap; currency USD; _fact_present gains an additive optional currency, homebuilders unchanged) — 0de44e4a1086. Re-run against live EDGAR: BOTH witnesses prior_vs_actual comparable within_range (TSM 40.2 in 39.0–40.2; ON 1603.5 in 1535–1635), refused_deltas [], all_values_visible true, limitations exactly no_external_consensus + definition_unqualified:perimeter + definition_unqualified:definition (measured at 0de44e4a1086; SUPERSEDED for TSMC by 7945d8413df3, which adds fx_assumption_unreconciled because the real prior guidance states an NT$ rate — see the cross-release pins in 58707f971d83), authority all false."
    command: "scratchpad proof_live_edgar3.py (assessments saved as JSON in the scratchpad only); company-intelligence core CI step"
    result: "TSM/ON economics READY + comparable; core step 2231 passed"
  - claim: "Intake review 2 (3d58f49a8ee2 + e6c3b999b795): ACCEPT_WITH_NITS, no blockers. Proven by the reviewer: the pre-identity branch is unreachable for DHI/PHM/KBH/TOL (day-by-day sweep 1993–2030, 0 uncovered days; tripwire plugin over the homebuilder suites, 79 passed) and TSM (tolerance 0); on the parent the real Q1-2025 row fetched manifest + exhibit and then raised — the raise aborted the TICKER's discovery (refresh() catches per ticker), not the whole refresh; the candidate branch is monotone (only converts a refusal into an admission when exactly one named date is within tolerance) and the profile layer is NOT an independent falsifier for an adopted near-miss date (it corroborates the same 'Quarters Ended' construct; the figure stays label-bound, the period string could be off by ≤6d in a narrow shape). Nits landed in 61f94ecd0cc8: filingDate parse guarded (skip, never raise), identity check scoped to the ticker (tickers_at), ambiguity named in the refusal warning, comma-tight dates parse in the scanner, the pre-identity test stubs the real Q1-2025 manifest and fails on the parent with 'TSM maps to no issuer at 2025-04-17'."
    command: "PYTHONPATH=scratchpad/parent_probe pytest -p conftest_parent tests/test_semiconductor_earnings_intake_integration.py -k before_the_attested (parent module injected); suites after nits"
    result: "parent: WorkspaceError raised as cited; head: 182 passed across the four suites"
  - claim: "Alignment review (0de44e4a1086): ACCEPT_WITH_NITS, no blockers, one serving qualification — the FX-assumption residual must be surfaced before the economics panel is served. Landed: guidance_history appends fx_assumption_unreconciled when the prior outlook carries fx_assumption (7945d8413df3; no new refusal reason, frozen role echo unchanged); the facts' reading provenance returns as a non-compared `provenance` key; cross-release sequences from two real filings per witness pinned (58707f971d83: TSM comparable below_range + fx limitation; ON comparable with caption- vs cell-derived units). Reviewer's key finding: NOTHING projects a real event_workspace.v1 payload (facts/period ISO) into the composer's `reported` rows (fiscal_period YYYYQn) — a real workspace reaching the composer today yields economics unavailable/management_sequence_missing silently; the composer's own optional-copy DOES carry basis/currency. → T08b projector packet written (scratchpad packets/T08b_projection.txt), lane pending a host."
    command: "Opus READ_ONLY agent; suites after nits"
    result: "289 passed (reviewer); 323 passed across nine suites after the nits"
  - claim: "T09fix lane (mb glm, rs_20260924T094552Z_89522, 480 s): review ACCEPT_WITH_NITS, no blockers — the owner's EMISSION_OK is read at call time so the tightening test is real; unresolved and unknown families refuse via the owner; unmapped refs (family_for_source_ref → None) pass through WITHOUT consulting the owner — a fail-open for refs no SOURCE_PREFIX_FAMILY prefix matches, including every https://www.sec.gov ref the witness corpus produces (review nit N4; pre-existing in 3dfde5bdd463, tracked as a B item); 14 fixture bundles kept byte-identical sets vs the parent; only divergence is when the owner's rule moves (the fix). Integrated: 3dfde5bdd463 (T09 transport) + a1c6888d5e9f (owner veto) + 7384a891929e (per-family verdict memo; registry-unavailable → 503 with private headers pinned; T09 suites wired into the fastapi serving-boundary job `tier-gate` — which is `gate: data`, i.e. the nightly data-health lane, NOT the PR merge gate (review nit N1); a dedicated `gate: code` job `semiconductor-b-boundary` now carries every B suite on the merge gate). Ledger outcome recorded run-1790243100-semi-b-t09fix-rs_20260924T094552Z_89522 accepted_by reviewer."
    command: "python3 -B -m pytest -p no:cacheprovider -o addopts=\"\" -q tests/test_theme_research_api.py tests/test_theme_research_rights_refresh.py tests/test_theme_research_private_binding.py; python3 -c 'import app.main'"
    result: "65 passed; import OK; audit_unrun_tests: no theme_research suite unrun"
  - claim: "Review 5 (Opus READ_ONLY, 7384a891929e..6328d69cc9db): ACCEPT_WITH_NITS, no blockers — no leak on any RightsRefusal/registry-unavailable path on either route (independent probe, entitled + anonymous, body + headers); memo semantics-preserving across all 14 fixture bundles; fx limitation purely additive; TSM/ON fact tokens byte-identical to guidance on all six comparison fields; cross-release tests commit no exhibit text or figures. Consequential nit N1: EVERY Semiconductor B suite lived only in `gate: data` jobs (tier-gate, neural-web-core, unrun-intl-libraries, unrun-subsector-themes), which ci.yml never packs — the PR merge gate ran none of them. Landed: `gate: code` job `semiconductor-b-boundary` (all 11 B suites; manifest validates 155 gate=code jobs; `scope: exclusive` + curated paths registered in CURATED_EXCLUSIVE because the transport suites' app.main import otherwise smears whole-tree scan roots onto all three packing-ceiling probes; six sibling curated jobs that import app.main gained `engine/market_ontology/semiconductor_theme_research.py` in their declared closures because T09 mounted the theme-research router in app/main.py — the closure audit `test_curated_exclusive_scopes_cover_their_own_import_closure` demanded it), exact limitation-list pins on the cross-release tests (N3), evidence-route 503 test (N7), provenance paths (N12), record corrections (N2/N5/N6). Open, tracked: N4 — `family_for_source_ref` → None fails OPEN for every ref no SOURCE_PREFIX_FAMILY prefix matches (all https://www.sec.gov refs), so the rights gate governs almost nothing the witness corpus produces; N8 — the explicit `except RightsRefusal` is an intent guard, behaviourally identical to the catch-all it precedes."
    command: "python3 scripts/run_ci_pack.py --workflow .github/ci/legacy-jobs.yml --gate code --validate-only; pytest tests/test_theme_research_api.py tests/test_semiconductor_earnings_intake_integration.py"
    result: "Validated 155 legacy jobs (semiconductor-b-boundary in gate=code); 45 passed"
  - claim: "T11a lane (mb glm, rs_20260924T101007Z_50964, c4d75b71e512 on refs/lanes/t11a): Opus READ_ONLY review REJECT — BL-1 MAX_ASSERTION_BYTES=65536 is 4x the Research Vault store's HARD_MAX_STRICT_CONDITIONAL_OBJECT_BYTES (16 KiB), so a 16–64 KiB assertion writes once and every republish RAISES BoundedReadTooLarge instead of classifying identical_noop/conflict_refused; BL-2 the rights gate derives families only from the nullable source.retention_ref and returns an empty set on None → an assertion whose source_uri maps to an emission-refused family (ths_concepts, unresolved) published with zero rights evaluation. Nits: probe accepts falsy non-bool as private; zero-byte object → TypeError; probe prefix unregistered in the delivery-plane census; dual canonical encodings (contract UTF-8 vs adapter ASCII). Passed: store signatures, immutability (only put_bytes_strict_conditional expected_version=None, no delete/retry), hermeticity, classification row + census counts + sha256 receipts, 48/14/41 green, the three census reds pre-exist at the parent. Ledger: run-1790244600-semi-b-t11a-… accepted_by none, review-rejected. Fix packet T11a_fix.txt (ceiling = the store constant; families from source_uri — the transport's field — retention_ref is a storage pointer; strict bool probe; typed corrupt refusal; probe-prefix census row) queued."
    command: "pytest tests/test_theme_graph_private_publication.py tests/test_theme_research_private_binding.py tests/test_semiconductor_assertion_semantics.py tests/test_r2_delivery_plane_classification.py (review worktree at c4d75b71e512)"
    result: "48 + 14 + 41 passed; 3 failed / 18 passed (pre-existing at 6328d69cc9db without the lane)"
  - claim: "T08b lane (mb minimax, rs_20260924T110444Z_17958, c8c1a0fbd733 on refs/lanes/t08b; 2-minute run): gate 53 passed; Opus READ_ONLY review REJECT — B1 `(metric, basis)` dict key raises TypeError on an unhashable basis (contract: never raises); B2 `_coerce_numeric` PARSES strings (\"40.2\"→40.2, \"1_000\"→1000, \"1e400\"→inf) and year/quarter accept strings — a token the producer never typed as a number would become a served figure with a position verdict. PASSED: the real-metadata TSM/ON payloads project to rows keyed exactly as _build_economics needs (cik 0001046179, int fiscal_period, YYYYQn tokens, basis/currency verbatim) and drive the REAL composer to economics.ready / prior_vs_actual comparable; no date-derived period; collision law correct; pure. Observations that shape the next step: nothing calls project_event_workspace yet; the composer's system_replay mode requires lifecycle.recorded_at which the production intake never emits (the reader/binding must supply the publication time); the per-workspace omissions list has no consumer (must be lifted into OwnerBundle.omissions by the binder). Ledger: accepted_by none, review-rejected. Fix packet T08b_fix.txt queued on mb (glm)."
    command: "pytest tests/test_workspace_projection.py tests/test_semiconductor_research_composition.py tests/test_semiconductor_earnings_intake_integration.py (review worktree at c8c1a0fbd733)"
    result: "53 passed (13 + 30 + 10)"
  - claim: "Binding design (Opus READ_ONLY ORCHESTRATION read, 11:45–12:05Z): the composer's economics pane is structurally independent of assertions — with real event workspaces and assertions=() the route serves 200 with economics.status ready, witness_gate positive and a full management triple, while summary/companies/industrial views report typed unavailability (limitations: [] — defect D9, closed by declaring omissions). Owners per OwnerBundle field: event_workspaces = engine/neuralweb/company_intelligence_reader.read_event_workspace (public r2.dev marker→generation→receipt) + the T08b projector; rights_revision = rights.load_registry_snapshot (must be the SAME snapshot the route enforces — D3); revision_tuple/omissions derived in the binder; financial_packets = () by design (derivation_unowned; no FIF midpoint owner); identity_results label key `source_business_label` has NO producer outside the composer (labels only exist once assertions exist → R4); assertions/native_refs/interpretation_blocks → private publication adapter behind R4. No owner scopes TSMC/onsemi to ai_semiconductors or a slice (zero Theme Graph edges at any hop; not in basket/crosswalk; slice_key inert in the composer) → declared two-entry witness roster with limitation slice_scope_unowned as the stopgap. recorded_at := lifecycle.observed_at (production's system-recording clock; generated_at is the SOURCE clock — two-clock law); both witnesses are time_mode latest, so replay clocks are off the critical path. Escalated to Sol as an addendum to 5811066300 (#7780 issuecomment-5813739560): R4; slice ownership; identity vintage (mapping_learned_at has no honest owner → no system_replay for the semiconductor surface until ruled); fail-open on unmapped rights families (T04b closes the EDGAR gap only). Packets written: T08c-1 witness scope (queued mb minimax), T08c-2 public-half binder, T12 CI-safe served proof; T08b-fix amended with recorded_at/cik-None items."
    command: "READ_ONLY agent trace of compose_semiconductor_research with assertions=(); pandas over data/theme_graph/edges.parquet; grep slice_key/source_business_label"
    result: "economics ready / witness_gate positive / prior_vs_actual comparable within_range; TSM/ON → theme:ai_semiconductors: EMPTY at 1 and 2 hops; source_business_label producers: composer + fixtures only"
  - claim: "SOL RULING / CONTINUE (#7780 issuecomment-5813801605, 12:10Z): OPTION A — the incumbent owner retains #7870 and may implement shared hooks 1–4 as additive, separately reviewed integration work preserving writer/admission/effect fences and the two witness obligations; no competing shared writer/client/router; exact ruling research/theme_graph/thematic_research_20260924/SHARED_CONSUMER_DISPOSITION_2026-09-24.md @ fffa29aea244 (blob ec80dd146808). Item 4 qualification: NO wildcard/regex schema acceptance — closed registration binding anchor, slice set, exact schema/version, composer and evidence selector; unknown/mismatched fails; config-driven multi-anchor mounts reuse the existing shell; no fake theme/basket/crosswalk entry for a company-first page; #7669 custody intact. Item 5 (reported-economic-context/profile v1.1) separately gated: return exact proposed contract + native field/ref mapping + positive/negative fixtures; no silent widening of frozen v1, no negative money as positive quantities, no fictitious guidance, no weakening of B's comparison. Addendum dispositions: R4 — keep the qualified private-owner mechanism and proof gates, unbound stays unavailable, no public/fixture/locator fallback, public workspaces alone are not full research acceptance; unmapped rights — the emission boundary FAILS CLOSED when the owner cannot resolve/authorize the source family (prove unknown-family, warm revocation, direct evidence/interpretation behaviour) → T04b amended; slice scope — the two-witness roster is a declared limited proof cohort with slice_scope_unowned filtering data AND evidence, no membership/breadth/rank authority → T08c-1 as designed, T08c-2 gains the scope seam; historical identity — refuse system_replay without supported as-known identity, latest proceeds, source_history is not system belief → T08c-2 refuses system_replay. Return owed to Sol on #7780: exact integrated hooks + reader/user-proof status after integration. Wave1 A/B/C research accepted separately; STOP #7886."
    command: "gh api repos/…/issues/comments/5813801605"
    result: "consumed in the same turn; packets T04b/T08c-2 amended; T04b re-sequenced after T10e (both re-ground fixtures)"
unverified:
  - claim: "12:40Z post-ruling stretch (Fable seat): Robotics registration values (anchor robotics_automation; slices precision_motion|perception; schema robotics_theme_research.v1/2026-09-24.1; composer on #7908) acknowledged for hook 1 — entry lands only when its composer module is on the same tree (#7870 issuecomment-5813976021); Robotics request 7 (JPX TDnet / HKEX / vendor first-party rights families) carried to Sol as a rights-registry admission question, NOT self-authorised (#7780 issuecomment-5813976564); shared system_replay refusal token declared identity_vintage_unsupported at the route/binder boundary. Fabric: mini2 now holds a MiniMax key but has NO internet egress (all HTTPS probes 000) while pool hosts marks it ELIGIBLE — no lane placed there; m1 window closed until 02:00Z; mb 2 slots held by foreign lanes 7930/mo_a3_mor2b_a2_producer since 11:06/11:16Z; chains re-planned: A = T08b-fix(glm)→T10e(minimax), B2 = H1(minimax)→T11a-fix(minimax); mb worktrees semi-b-t08b-fix, -t10e-scope, -t11a-fix, -h1-registry (45 GB free, floor 30). Packets written for hooks 2–4: H2 (registration-driven section partial + mount_context + crosswalk anchor resolver in the registry; absorbs T10d/T10f, both SUPERSEDED), H3 (Theme Tracker mounts every registered anchor admitted by an existing basket row; failure never breaks the page), H4 (client binds each mount to its exact data-schema/labels, one isolated instance per mount, unconfigured/duplicate states, no regex). v1.1 proposal (Sol item 5: additive theme_graph.curation_assertion.v1.1 — two-branch observation union with decimal strings and sign-by-measure-class, REPORTED_ECONOMIC_RIGHT/REPORTED_SUPPLY_STATUS branch objects, native mapping table, P1–P5/N1–N15 fixtures) drafted; H2–H4 packets and the proposal are under independent Opus READ_ONLY review before dispatch/posting."
    result: "No lane launched in this stretch (mb saturated by foreign lanes); no product code changed; nothing merged or deployed. MISSION_COMPLETE: FALSE; SEMICONDUCTOR_B: NOT_BUILT; C1: DEFERRED; V1_1: PROPOSED_NOT_BUILT."
  - claim: "SOL RULING / CONTINUE #7780 issuecomment-5814333887 (12:42Z) consumed at 5814401358: #7870 remains the one shared-foundation integration writer; domain principals own adapters/content/proof; JPX/TDnet, HKEX and issuer/vendor families NOT approved by analogy to sec_edgar (recognition, acquisition/retention, quotation, attributed synthesis, full-body display, dataset redistribution are separate decisions; reachability is not a permission receipt); ONE bounded read-only qualification unit through the existing rights owner path reusing Robotics' retained register (no re-acquisition) → compact source/representation matrix + PROPOSED narrowly scoped registry/prefix change returned for explicit adjudication BEFORE any emission; no invented direct_display_ok, no single first-party family, representation gaps returned verbatim; pending qualification stays refused incl. dependent prose/evidence; example.invalid fixture-only. Technology company-first dispatch → item-5 profile mapping (no dummy theme; exact shared signature owed to Technology after H1). Hourly PARENT_ORCHESTRATOR watch on #7886 noted; B keeps its own child action/wait path. Actions: lane Q1 (rights qualification, glm) minted as a sparse mb worktree with the 71-row R1-corpus source CSV and the S01–S46 register staged as inputs, queued behind the foreign mb lanes; v1.1 proposal r1 REJECTED by independent Opus review (K1 reference binding omitted, v1-shaped semantic layer, no physical_change, P1 self-failing, bare oneOf, rights fail-closed misdescribed as current) → r2 written with §8 profile discriminator; second review running."
    result: "No emission, admission, registry row or prefix change exists for any Robotics family; RIGHTS_QUALIFICATION: QUEUED_NOT_STARTED; V1_1: PROPOSED_NOT_BUILT; nothing merged/deployed."
  - claim: "Independent Opus READ_ONLY review of the hook 2–4 executor packets: H2 ACCEPT_WITH_NITS (folded: section partial defines its own autoescaped t macro because an included partial inherits the parent's |safe macro on the Theme Tracker; ids from anchor_theme_id; pinned crosswalk path; region gate; guarded registry import; whitespace-tolerant comparison); H3 REJECT (theme rows carry theme_id STORY ids, no id key; two UI-test template assertions broken outside its custody; fixture never received the section partial) → rewritten as revision 2 with admission registration_for(row[theme_id]), custody of the two assertions, self-seeded fixture; H4 REJECT (evidence envelope has no request/sections so the proposed validation would disable the drawer; ?v= bump and per-anchor storage key red tests outside its owned set; indentation-pinned test regexes; contradiction over TR_SCHEMA runtime reads) → split into H4a (contract-block validators, node-provable) and H4b (per-mount isolated instances, states, cache-buster). Verified safe: CSS has no #theme-research selector; the registry import closure is stdlib + jsonschema + one contracts read."
    result: "Packets ready in dispatch order H2 → H3 → H4a → H4b after H1 integrates; no lane launched; no product code changed."
  - claim: "13:07Z fabric deadlock: all three mb chain launches failed BEFORE admission with SUPPORT_STALE_ACTIVE_REFUSED → SCP_FAILED rc=75 — the seat kit's executor surface (mm_codex_exec.sh, edited by another session at 12:33Z) differs from mb/m1 copies and pool remote refuses to refresh a host with active lanes; mb is refilled within seconds by a local lane2.py queue (b_f11_10a since 13:07Z, mo_a3_mor2b_a2_producer since 11:16Z, 2 queued), so no remote lane can start from this seat until mb is idle. Read-only diagnosis only; no surface file touched. Dispatcher made retry-tolerant; chains B3 (H1→T11a-fix), A2 (T08b-fix→T10e), C3 (Q1) restarted. H3 r2 ACCEPT_WITH_NITS (folded: patch targets scripts.build_state_of_themes.*, inline seed/copy, synthetic-anchor info line), H4a/H4b r1 REJECT → folded (closed malformed:<path> vocabulary, applyResearchResponseFor sibling, --numstat/digest gates, .l-en/.l-zh spans, ?v= pins at UI test :449/:452, storage-audit facts). v1.1 proposal round 3 ACCEPT_WITH_NITS → nits folded → committed as research/theme_graph/CURATION_ASSERTION_V1_1_PROPOSAL_2026-09-24.md (item-5 return to Sol)."
    result: "No lane launched; no product code changed; V1_1: PROPOSED_NOT_BUILT (proposal final, posted for adjudication); RIGHTS_QUALIFICATION: QUEUED_NOT_STARTED; SEMICONDUCTOR_B: NOT_BUILT."
  - "Pushed 0c7f637cc63c..3e3a7956d014 (12 commits) after review 5 ACCEPT_WITH_NITS with nits landed; checkpoint 5813134102. Shared-owner requests answered on #7870: Mining seam questions (5812295091), Robotics request 6 (5813164362: one aggregator include line for `_robotics_research_mount.html.j2` AFTER T10f lands; `robotics_automation` registered in the queued T10d anchor set). Lanes in flight: T08b-fix (mb glm), T10d (mb glm), T04b (mb glm), T11a-fix (m1 minimax), T10e→T10f→T10c→T07c chain (m1). Open question put to the alignment reviewer: does the facts→reported projection used by the composer carry basis/currency, or would production still refuse on currency_mismatch?"
  - "T10d (build_theme_detail anchor wiring, mb lane queued) and T07c (public composer exports, m1 lane queued behind the worktree mint) have packets and dispatchers but no returns yet."
  - claim: "T05a (witness issuers) and T10-FIX (client) will pass independent re-verification after the seat fixes."
    what_would_verify: "T05a: 532d25212cab (period guard qualifies whole sentences, abbreviation-safe sentence bounds, FX bound to the forward sentence, forward-only horizons, unique date columns; 607 passed; leak probes b3/d2/d3 fail-closed) returning ACCEPT/ACCEPT_WITH_NITS from the third reviewer. T10-FIX: 5169317d1204 (closed bilingual L.errcode map, typedError/readJsonBody/refuseCrossOriginRedirect, same-origin credentials, chip whitelist; 40 passed; node --check clean) returning ACCEPT/ACCEPT_WITH_NITS. Then cherry-picks with CI wiring (witnesses suite into the company-intelligence core step; UI suite into the node-shipping publish-r2-client pack) and the carrier-level TSM discovery integration test (T05b intake + T05a tsm_issuer/tsm_profile end to end)."
  - claim: "T09 (API transport, MiniMax on mb, lane rs_20260924T073351Z_22520) and T10b (basket include seam, MiniMax on m1, lane rs_20260924T073209Z_20140) will return single commits that pass their gates."
    what_would_verify: "Bundle pull, second-environment gate (T09: tests/test_theme_research_api.py both harnesses; T10b: tests/test_basket_intelligence_mounts.py + state_of_themes regression), independent Opus READ_ONLY review, then integration."
  - claim: "T10 part 1 will pass independent review after the bounded fix lane closes the two blockers (bilingual <option> law; limitations/authorized_coverage never rendered) and nits 1/2/4/5/7/8/9."
    what_would_verify: "The T10-FIX executor commit on top of f53c7c3845b3 passing its gate (>=26 tests, node --check) and a second READ_ONLY review returning ACCEPT/ACCEPT_WITH_NITS; then seat cherry-pick of both commits with CI wiring into a node-shipping pack."
  - claim: "The R4-ruled private binding (Research Vault r2_store adapter, theme_graph_private/v1/assertions/<curation_revision>.json) is qualified for live admission."
    what_would_verify: "T11 evidence per the ruling: adapter source revision + delivery-classification registration + reader/publisher bindings; the store owner's nonce-object proof (authenticated positive read + anonymous negative on the same known-existing key + actual bucket/domain public-access configuration); actual deployed cache/grace values and the invalidation path; source-policy snapshot inside the serving path; negative cases; no private bytes/credentials/locators in public evidence."
  - claim: "T04 and T05a will pass independent review; T07/T08 fix #2 closes the capacity deletion defect to the reviewer's satisfaction."
    what_would_verify: "The three in-flight READ_ONLY reviews returning ACCEPT/ACCEPT_WITH_NITS; then cherry-picks with CI wiring (T04 suites beside the evidence-foundation step; T05a witnesses suite in the company-intelligence core step; composition suites in a market-ontology step) and a combined green gate."
  - claim: "Earnings owner can produce real TSMC and onsemi witness sequences end to end."
    what_would_verify: "T05a profiles + T05b intake exercised against real submissions through approved accessors (T11/T12), yielding validated event workspaces with prior outlook / actual / new outlook for both witnesses."
  - claim: "Either witness is production-proven."
    what_would_verify: "T11/T12 real source -> native owner -> rights/entitlement -> API -> existing page proof, both witnesses, plus negative and non-interference proofs."
  - claim: "T05a fix #5 (4c76dde6d82b, closed grammars) is accepted by an independent reviewer."
    why: "Fifth READ_ONLY Opus review running; rounds 3 and 4 were REJECT (prose-parser leaks)."
  - claim: "T09 fix (rights verdict from engine.theme_graph.rights per family; parity tests) is built and accepted."
    why: "GLM lane queued on mini2 (~/lanes/semi-b-t09-fix at 808e294d1c15) behind other orchestrators' lanes; review not started."
  - claim: "T10c (client/mount nits + exact-anchor gate) and T11a (private-publication adapter + classification row) are built."
    why: "GLM lanes queued on m1 (~/lanes/semi-b-t10c-nits) and mb (~/lanes/semi-b-t11a-private) at 8e478b2feed0; hosts at their 2-lane ceilings."
unresolved:
  - "SOURCE_COLLISION (lane-local): engine/theme_graph/store.py one-line EVIDENCE_COLUMNS append is frozen pending #7462 source-writer release or terminal state."
  - "SOURCE_COLLISION (lane-local): templates/basket_detail.html.j2 generic research mount + asset includes frozen pending #7669 release or accepted current template identity."
  - "Private binding for live full-fidelity GMI admission is unproven; synthetic contract/composition/test work continues; live admission lane closed."
  - "mastermind-executive MCP connector is unauthenticated in this session; fabric dispatch uses the installed pool/lease-broker CLI (canonical Subagent Fabric surface on this host), not a new queue."
  - "R4 RULED (#7780 issuecomment-5808854275, SOL-R4-PRIVATE-BINDING-20260924-HEALTHCARE-R11): candidate (ii) ACCEPTED_WITH_QUALIFICATIONS, implementation NOT_GRANTED; consumed at issuecomment-5808981293. T11 may proceed only as adapter/classification/reader qualification; live admission stays held until the ruling's nonce proof, entitlement/grace evidence and source-policy evidence clear. Dependents registered on the same ruling: Robotics live path, Materials T1/T2, Technology ex-Semis (#7891) G3, Healthcare (#7788)."
  - "Shared-contract extension request HELD (Healthcare HC-R10 #7870 issuecomment-5808534349, joined by Materials R10): additive REPORTED_ECONOMIC_RIGHT / REPORTED_SUPPLY_STATUS predicates, null quantity basis, gross/net net_sales, not_disclosed estimate status, qualitative precision, signed/exact negative financial measurement. Disposition at issuecomment-5808981043: beyond B; needs a Sol/Chairman-assigned additive v1.1 with the Robotics and Semiconductor reference payloads pinned byte-identical; no coercion of the R9 fixture on this carrier."
  - "Promised amendment (Industrials W12 item 2 / Consumer R10 item 3, disposition 5809602368): engine/company_intelligence/guidance_history.py compares two ABSENT optional definitions (basis/currency/perimeter/definition) as equal; the amendment surfaces absence-on-both as a definition_unqualified:<field> limitation on the assessment (additive, no refusal, homebuilder behaviour unchanged) with a test. Not yet landed."
  - "SOL-HC-R12 (issuecomment-5809358801) consumed: B's v1 semantics/writer unchanged; Healthcare/Materials/Consumer extension items go to theme_graph.curation_assertion.v1.1 (separately gated, same owner, existing bytes preserved, unknown versions refused); Healthcare needs an explicit typed profile through the same POST routes with visible refusal — T09's closed slice_key enum is that refusal today; the exact profile discriminator for other verticals is deferred to the accepted T09 commit. No competing writer on #7870."
  - "Schema/commission mismatch surfaced by review: `authorized_coverage` may be `unavailable` with no `reason` (the response schema has no reason property there and the commission's shape enumeration omits it while its section list demands one). The honest unavailable status is kept; candidate contract nit for the next schema revision."
  - "Fabric routing law applied 2026-09-24: MiniMax first-pass acceptance for this operation is 0/4 (T01 scope violation, T05a, T05b, T10-FIX all review-rejected) and glm-5.3 0/3 first-pass (T02, T07/T08, T10) — every executor commit needed one or two seat fix cycles plus fresh independent re-verification; GLM is not a lawful re-route for TSMC material (content filter), so the seat is the R21 escalation endpoint for T05a/T05b/T10 fixes. Ledger outcomes recorded per lane (accepted_by reviewer|none, review_rejected, escalated)."
  - "Fabric incident 2026-09-24: executor lanes running under the Claude Code SDK harness (MiniMax mode) obey the Macro repository ship loop (push, PR, merge-on-green label, gh run watch) despite the lane contract; T01 opened #7874 and armed merge-on-green (disarmed: draft, label removed, custody comment 5807689936, stray watcher killed); T06 pushed claude/semi-b-t06-guidance before its lane was terminated (no PR). Every later packet carries a binding LANE LAW preamble forbidding push/PR/labels/watchers; stray origin branches claude/semi-b-t01-inputs and claude/semi-b-t06-guidance are to be deleted after integration."
  - "Executor return blocks were missing or replaced by ship-loop status for T01 and T03 (stdout 1.5 KB / 2 lines); acceptance therefore rests on the commits, second-environment gates and independent review, not on executor self-report."
  - "Candidate DSC (not yet recorded): GMI evidence parquet is an unregistered artifact on the public raw plane (config/r2_delivery_plane_classification.v1.json:166 DEFAULT_DENY:MACRO_GIT_RAW data/**) while config/theme_sources.yml:34 calls data/theme_graph an internal plane; nothing enforces the internal-plane language."
  - "Candidate DSC (not yet recorded): the Macro executor harness (claude -p under MiniMax/GLM keys) inherits the repository CLAUDE.md ship loop; a lane contract alone does not stop push/PR/merge-on-green."
  - "#7669 RULED (issuecomment-5808986207): one generic guarded include templates/_basket_intelligence_mounts.html.j2 after the back link, outside #app; only one such include; vertical partials separately owned; missing partials harmless; #7870 authors the seam as part of its T10 shell hunk and returns the immutable template + aggregator blob; do not edit #7669's branch (head 76cb5371ec77, template blob f6540573c931). T10b executor contract (scratchpad packets/T10b_mounts.txt) dispatched to m1 ~/lanes/semi-b-t10b-mounts on carrier head b256aa6a756a; the partial renders only when the render context supplies theme_research_anchor (wiring per basket from config/theme_crosswalk.yml is later work). #7462 still silent (store.py append frozen). Fresh-read cadence kept (last full read 07:05Z)."
  - "T05 (operator commission, glm-5.3, mb lane rs_20260924T045106Z_54606) FAILED without a commit: after 39 turns / 552 s every call returned GLM `400 [1301] unsafe or sensitive content` (terminal_reason api_error) — a provider content filter on the TSMC/Taiwan material, not quota or admission. Ledger outcome recorded accepted_by=none, escalated. Re-routed as two MiniMax executor contracts: T05a identity+profiles (issuer_profiles/event_workspace/event_workspace_build + tests/test_semiconductor_earnings_witnesses.py; mb worktree ~/lanes/semi-b-t05-witness-mm) and T05b intake (scripts/refresh_event_workspaces.py DISCOVERY_TICKERS, identity-declared results-6-K discriminator, 52/53-week tolerance + tests/test_semiconductor_earnings_intake.py; m1 worktree ~/lanes/semi-b-t05b-intake). Both MiniMax lanes returned commits on TSMC material without a content-filter event (T05b 6254ec3ab8f9 on m1; T05a ddc0fbad0841 on mb)."
  - "Remote lane capacity: both hosts (m1, mb) sit at the 2-lane ceiling most of the time because other orchestrators' lanes (mo_a3_*, cdv1_*, pu_w3_*) hold slots; wave-2 placement runs through a per-host dispatcher that waits for `pool hosts` ELIGIBLE before calling `pool remote` (no ledger noise, no new queue): all wave-2 lanes have returned (T07/T08 51ab864d6f01, T05b 6254ec3ab8f9, T04 db857526fad9, T05a ddc0fbad0841); T10-FIX (MiniMax, m1 ~/lanes/semi-b-t10) was refused once at 06:35Z (slot taken between the eligibility check and admission) and its dispatcher is re-placing; T09 waits for the T07/T08 head."
  - "Native `mastermind-opus-auditor` agents stop at a 12-turn cap before producing a verdict on gate-running reviews (two runs lost); independent reviews now run as general-purpose Opus with ROUTE: AUDIT / MODE: READ_ONLY. Candidate feedback record for the fabric owner."
  - "Shared-contract addition: `published_at_grain_mismatch` (grain/date consistency) is enforced beyond the frozen Robotics rule list; documented in contracts/theme_graph/README.md and notified on #7773 (issuecomment-5808287514); the Robotics owner has not acknowledged it — candidate DEC if Robotics objects."
  - "Cross-vertical dependency: the Basic Materials research operation (gmi-basic-materials-research-20260923-sol-001, carrier #7796) posted R10-SHARED-PROFILE-REQUEST-20260924 on #7773 (issuecomment-5808157680) asking the shared owner for the private object class/callable/namespace/generation rule, rights binding, export boundary and dependency carrier. Seat answered with facts only (#7773 issuecomment-5808307684): shared contract = #7870 module/schema; measurement vocabulary is closed and non-negative (no signed/exact-decimal, no materials bases — an amendment request, not a variant); private profile NOT implemented; single next action = the R4 ruling; rights binding = rights.load_registry_snapshot/assert_current_emission_allowed; custody unchanged. R4 addendum posted on #7780 (issuecomment-5808311511): the ruling now gates Semiconductor T11, the Robotics live path and Materials T1/T2. No Materials scope accepted."
  - "T10-FIX executor contract written (scratchpad packets/T10fix_client.txt; owned files site/assets/js/theme-research.js + tests/test_semiconductor_theme_research_ui.py; runs in m1 worktree ~/lanes/semi-b-t10 on top of f53c7c3845b3). Dispatch is held behind T05a/T05b so the mandatory witness path takes the next slots; CI wiring at integration must land in a node-shipping pack (the Theme Tracker pack at legacy-jobs.yml ~14560 has if:false and no setup-node; the publish-r2-client pack ~10188 ships node)."
  - item: "T09 lane 808e294d1c15 REJECT: local _EMISSION_OK copy failed OPEN when the rights owner tightened; fix lane T09-fix owns app/theme_research.py + tests only."
  - item: "Robotics requests 1-4 (route dispatch by anchor, config-driven mount slices, multi-anchor Theme Tracker mount, per-mount client schema identity) and the Mining/Consumer/Finance/Technology closed-enum / reported-economic-context asks are HELD pending Sol's ruling on #7780 issuecomment-5811066300."
  - item: "B owns the theme_research_anchor wiring hunk in scripts/build_theme_detail.py (T10d) and the additive public export of passes_time_mode / generation_fingerprint / canonical_text (T07c); packets not yet dispatched."
  - item: "Chairman/Astra directive ('Semiconductors builds the base out') reached this session only as peer-seat relays; treated as coordination, not a scope grant."
  - item: "mini2 is a third lane host (py3.14 venv with fastapi; ~/lanes/repos/macro partial+sparse clone shared with other orchestrators' mints — sparse-checkout disable waits on the shared repo lock)."
next_actions:
  - "Consume T08b-fix, T11a-fix, T10e, T10f, T10d, T04b lane returns (gate → Opus READ_ONLY → integrate → ledger). Then T08c-2 (public-half binder: read_event_workspace + witness scope + projector; private half declared absent) → T12 (CI-safe served proof) → seat-local live-EDGAR run (refresh_event_workspaces --out-dir tmp, app.main under TestClient with only the reader's _fetch_bytes stubbed; commit receipts only). Honest end-state before R4: economics witness served on real issuers, industrial half held on R4 — NOT `BUILT` against the W-A/W-B spec."
  - "W-A/W-B visible production result: the live-EDGAR economics triple is proven at the seat; still owed is the served path — accepted T09 transport (rights owner call, fix lane in flight), theme-research response with the economics panel populated from these workspaces, the Theme Tracker / basket mount rendering it — and the R4-bound private publication (T11a) before any real assertion body."
  - "Consume T09-fix, T10c, T11a lane returns: disarm check, bundle pull (stderr discipline), review worktree, gate, Opus READ_ONLY review, integrate, ledger outcome."
  - "Consume T10d and T07c lane returns (disarm check, bundle pull, review worktree, gate, Opus READ_ONLY review, integrate, CI wiring, ledger)."
  - "Consume Sol's build-out ruling on #7780 (A: bounded B2 scope on #7870 / B: separate owner / C: defer) and re-plan Robotics requests 1-4 accordingly."
  - "Consume the two in-flight READ_ONLY re-verifications (T05a 532d25212cab; T10-FIX 5169317d1204). ACCEPT -> cherry-pick -x into #7870 (T05a: ddc0fbad0841, 85e9109de8d0, 532d25212cab; T10: f53c7c3845b3, bc3331a3f786, 5169317d1204), wire the witnesses suite into the company-intelligence core step and the UI suite into the node-shipping publish-r2-client pack (~legacy-jobs.yml:10188) + ci.yml paths, add the carrier-level TSM 6-K discovery integration test (T05b intake with tsm_issuer/tsm_profile), rerun audit_unrun_tests + gates + agentos validate, push, checkpoint, ledger outcomes. REJECT -> seat fix + fresh reviewer."
  - "T09 (mb lane rs_20260924T073351Z_22520) and T10b (m1 lane rs_20260924T073209Z_20140): on return -> disarm check, bundle pull, review worktree, gate, Opus READ_ONLY review, integrate (T09 route tests need fastapi: run on mb or the seat), ledger."
  - "Land the definition_unqualified amendment in guidance_history.py with tests (T06 amendment; homebuilder fixtures unchanged)."
  - "T11 per the R4 ruling as synthetic/LocalStore work: adapter over engine.research_vault.r2_store for theme_graph_private/v1/assertions/<curation_revision>.json (write-then-verify, identical bytes no-op, conflicting bytes refuse), delivery-classification registration entry, reader with bounded strict reads, rights resolver mapping (assertion source route -> registry family; unresolved refuses), negative cases; the nonce-object privacy proof and deployed grace/cache/invalidation evidence are owed by the store/paywall owners on #7780 before any real body."
  - "Executor lanes in flight: T09fix (mb glm), T11a (mb glm), T10d (mb glm), T10c (m1 glm), T07c (m1 minimax); mb holds two foreign lanes, so B lanes queue behind slot waits; mini2 has no GLM/MiniMax/Bailian key and is not a B lane host."
  - "Fresh-read #7780/#7462/#7669/#7773/#7870 before every substantive write (>=15 min cadence); keep store.py frozen; keep HELD extension requests off this carrier; return the immutable template + aggregator blob on #7669 when T10b is accepted."
do_not_redo:
  - "Do not repeat the R01-R09 research, A/B/C selection, written design, plan or 48-requirement mapping."
  - "Do not implement on #7780, #7773, #7462 or #7669; do not transplant their hunks."
  - "Do not create a semiconductor-specific graph, evidence/correction ledger, product master, financial/consensus/expectation store, rights registry, queue, scheduler, watcher, auth or publisher."
  - "Do not execute SBD-41..48 / C1."
  - "Do not fork the Robotics-proposed curation_assertion contract into a semiconductor variant; extend the one shared payload (ruling R1)."
danger_areas:
  - "Macro is public: no full-fidelity paid assertion bodies, credentials or private production captures in this carrier."
  - "A synthetic-fixture green is not native admission; an industrial-only pane does not satisfy either witness."
  - "Current issuer identity is not historical lineage; source-only businesses get no fabricated CIK/security/company node."
  - "Fabric executor first-pass acceptance is low (glm-5.3 0.29, MiniMax-M2.7 0.14 over last 20): every child needs an executable deterministic gate and independent review before integration."
prs: [7780, 7870, 7874, 7773, 7462, 7669, 7788, 7891, 7794, 7796, 7887, 7789, 7804, 7795, 7908, 7881, 7886, 7902]
---

# Semiconductor B — implementation operation working checkpoint

OPERATION: `gmi-semiconductors-fable-ceo-e2e-20260923-chairman-001`
PARENT RESEARCH OPERATION: `gmi-semiconductors-research-20260923-sol-001` (carrier #7780, records only)
RECEIVER: Claude Fable 5.1, Claude Desktop Code session `f6dd4b82-d319-4daf-99a4-ef4fe7dfd9ec`, host `Mac-Studio.local`
PROCEDURE PIN: Mastermind `a7d2b3049e5cdc523e91e61a6e9d70a1cb911157`, Skillpack 1.0.1 / bootstrap 1
CARRIER BASE: Macro `main` `9438880952d3375b00a042381705c2e6c85305e3`
STATE AT THIS COMMIT: `STARTED` (START issuecomment-5807391383); carrier #7870 at 3e3a7956d014 (pushed 2026-09-24 11:25Z; checkpoint issuecomment-5813134102) holds wave 1 (T01/T03/T06), T02, T05b, T07/T08, T04, the T06 definition_unqualified amendment, the sec_edgar family + its qualified basis, T10 (client) and T10b (basket include seam) — each accepted after independent review — with CI wiring for every landed suite; R4, SOL-HC-R12 and the #7669 custody ruling consumed (seam blobs returned on #7669); T05a closed house-style grammars accepted (review #5) and integrated with the seat nit fix (b8baf4e787b5), plus the carrier-level real-metadata intake integration and the 52/53-week stated-end seam fix (f38b73f3ab72, reviewed ACCEPT_WITH_NITS, nits in 2e47e5b6d79d), CI wiring (0db3e23bf373), and three live-EDGAR-found seams closed (3d58f49a8ee2 pre-identity skip; e6c3b999b795 multi-date header rule; 0de44e4a1086 fact/guidance definition tokens; 61f94ecd0cc8 intake review-2 nits; alignment and T09fix reviews ACCEPT_WITH_NITS with nits landed) and the T09 transport integrated (3dfde5bdd463, a1c6888d5e9f, 7384a891929e); T09 lane REJECT (rights veto restated locally) → fabric fix lane re-routed to mb after six mini2 refusals whose real cause was a missing GLM key on that host; T10c/T11a/T10d/T07c lanes queued across m1/mb/m1; B-foundation build-out scope escalated to Sol (#7780 issuecomment-5811066300); `SEMICONDUCTOR_B: NOT_BUILT` (workspace→composer projection (T08b), private binding (T11a), anchor wiring (T10d) and the served production result outstanding; transport integrated; witness profiles + intake integration landed; the W-A/W-B economics triple is PROVEN from live EDGAR through the production discovery at the seat — READY, comparable, within_range for both witnesses — not yet served); `C1: DEFERRED`; `MISSION_COMPLETE: false`; `EFFECT_UNKNOWN: none`.

## Fable custody rulings inside the accepted B architecture

- **R1 — one shared assertion contract, authored here first.** No Robotics implementation carrier exists and no STARTed operation owns `contracts/theme_graph/curation_assertion.v1.schema.json` or `engine/theme_graph/curation_assertion.py`. B authors the shared `theme_graph.curation_assertion.v1` contract exactly as proposed by the Robotics plan (#7773 Task 1: field groups, closed predicate/statement-mode vocabularies, `gmirca_[0-9a-f]{32}` revision, canonical-JSON codec, `validate_assertion`/`curation_revision`/`encode_assertion`/`decode_assertion`/`source_ref_for`) plus the semiconductor plan's closed optional `industrial_context` object. Two shared generalizations are recorded for the Robotics receiver: `source_ref_for` derives its theme segment from `scope.canonical_theme_id` instead of the literal `robotics`, and `subject.company_node_id` is nullable for source-only businesses. The Robotics receiver consumes the accepted revision; it does not re-author the contract.
- **R2 — store.py column is a frozen lane, not a blocker for B.** The one-line `EVIDENCE_COLUMNS` append waits on #7462 (custody question posted). All other T02 work (schema field, module, guard decoding, tests) proceeds; the round-trip test that needs the column is written RED and lands with the column.
- **R3 — template hunk is a frozen lane.** `templates/basket_detail.html.j2` waits on #7669 (custody question posted). The shared client JS/CSS and the `templates/state_of_themes.html.j2` mount (touched by no open PR) proceed.
- **R4 — private binding question is qualified, not improvised.** The existing private publication owner pattern is `engine/earnings_narrative/private_publication.py` over `engine/research_vault/r2_store` (private bucket, LocalStore for tests) read by `app/earnings.py` after `require_user` + `enforce_site_full(always=True)`. T01/T03 qualify whether the incumbent GMI evidence reader/writer can be bound to a non-public runtime root or published through that existing private store owner under current rights. No new store, bucket, env override or public fallback. If neither existing owner path can be proven, the live-admission lane returns to Sol/Chairman as a DECISION_REQUEST while synthetic work continues.

## Custody map (carrier base)

| Path | Status | Owner / gate |
|---|---|---|
| `contracts/theme_graph/curation_assertion.v1.schema.json`, `engine/theme_graph/curation_assertion.py` | new, free | B authors (R1) |
| `contracts/theme_graph/evidence.v1.schema.json` (optional nullable `curation_assertion`) | free | additive; guard tests |
| `engine/theme_graph/store.py` (`EVIDENCE_COLUMNS` append) | **FROZEN** | #7462 |
| `engine/theme_graph/rights.py` (snapshot API) | free (not in #7462 file set) | T03 |
| `scripts/check_theme_graph_contracts.py`, `tests/test_theme_graph_contracts.py` | free (not in #7462 file set) | T02 |
| `contracts/evidence_foundation/vocabulary.v1.json`, `tests/test_evidence_foundation_contract.py` | free | T04 |
| `engine/company_intelligence/*` witness enrollment | free | T05/T06 |
| `engine/market_ontology/semiconductor_theme_research.py`, `contracts/market_ontology/*.schema.json` | new, free | T07/T08 |
| `app/theme_research.py`, `app/main.py` router include | free | T09 |
| `site/assets/js/theme-research.js`, `site/assets/css/theme-research.css`, `templates/state_of_themes.html.j2` | free | T10 |
| `templates/basket_detail.html.j2` | **FROZEN** | #7669 |
| `scripts/build_state_of_themes.py` | not touched by plan | #7664 lane avoided |
| `data/theme_graph/evidence.parquet` (public Git) | no full-fidelity payload ever | R4 |

- 13:5xZ — Sol 5814541566 (12:56Z) consumed: all seat dispatchers killed (capacity evidence → Capacity Fabric owner; no polling); H1 seat-executed directly under ACTIVE_EXECUTION as commit `231c9645dff3` (registry + dispatch + tests + CI closure widening; gate 126 passed, ci_pack 121 passed); mb worktree `semi-b-h1-registry` removed; Opus READ_ONLY review pending.

- 14:3xZ — T08b-fix SEAT_EXECUTED in seat-local worktree `semi-b-t08bfix-seat` at `refs/lanes/t08b`: `446afd24945e` (B1/B2 + N2–N12; gate 128) → Opus READ_ONLY ACCEPT_WITH_NITS (8 nits, no blockers) → nit-fold delta `a32d69726b57` (four-digit year law, composer clock grammar, guarded deepcopy, all-zero CIK, replay end-to-end; gate 155) with a bounded delta re-review in flight. T08c-1 SEAT_EXECUTED in seat-local worktree `semi-b-t08c1-seat` at carrier head: `730503063cb7` (witness roster + owner-chain identity join; live TSM/ON CIKs; 35 tests) — Opus READ_ONLY review in flight; declared deviation: the Data OS CIK reader is `IssuerMaster` built from one parquet read (no module-level `cik_of_issuer` exists). Neither is on the carrier yet: cherry-pick c8c1a0fbd733 + 446afd24945e + a32d69726b57 and 730503063cb7 after their verdicts, then one seat CI-wiring commit (curated job paths + run steps + ci.yml triggers for workspace_projection / witness_scope) with the ci_pack closure test. Reviewer's binder fact for T08c-2: nothing reads a row's `omissions` key today, so the binder MUST lift row omissions into `OwnerBundle.omissions` or dropped facts serve as a clean response.

- 14:5xZ — VERDICTS: H1 `231c9645dff3` ACCEPT_WITH_NITS (9 nits, 0 blockers) → nit-fold delta `4733b7fa84d5` on the carrier (definition_version enforced on the query envelope; `RegisteredContractMismatch`; registry fault → private 503; docstring; cosmetics; gate 128) — bounded delta re-review in flight. T08b-fix `446afd24945e` ACCEPT_WITH_NITS (8 nits, 0 blockers) → delta `a32d69726b57` (gate 155) — delta re-review in flight. T08c-1 `730503063cb7` ACCEPT_WITH_NITS (8 nits, 0 blockers; parquet-read deviation ACCEPTED as honest adaptation) → delta `cd0fbb500d90` (`identity_source_unavailable`, never-raise locate, real bool, split skipif, single read site; gate 37) — delta re-review in flight. Recorded-not-changed nits: H1-N1 bad-slice 400 detail is a fixed string (reduced disclosure); H1-N4 `ResearchQuery.slice_key` Literal in the composer over-states for a second vertical (widen when the composer is next touched); H1-N7 404-vs-400 membership oracle (anchors already public in the mount); T08c-1-N4 sidecar clocks not compared to the live master (build id ≠ learning date). LIVE NEST FINDING (read-only probe 14:4xZ): the production `event_workspaces/` nest (generation 3be5e93e4bc42804dc816973, event_count 5) carries ONLY AAPL/2026Q3, DHI/2026Q3, KBH/2026Q3, PHM/2026Q2, TOL/2026Q3 — NO TSM/ON (the intake roster `DISCOVERY_TICKERS` includes TSM/ON on the carrier only; production runs main) and ONE event per issuer per generation (whole-nest snapshots), so the composer's `prior` (preceding period's guidance) cannot come from the current generation's aliases — the T08c-2 binder must walk the chain (`read_all_event_source_revisions`) or the seat-local proof must build a two-period nest from the real-metadata payloads; production W-A/W-B coverage requires #7870 merge + a nightly run. `SEMICONDUCTOR_B: NOT_BUILT` stands.

- 15:1xZ — DELTA VERDICTS: T08b-fix delta `a32d69726b57` ACCEPT_WITH_NITS (all 8 prior nits CLOSED; datetime parser-only import ruled acceptable; 504k-payload differential fuzz vs parent, zero unexplained divergence) → inert hardening fold `79fc2db488a4`; the four-commit T08b lineage is cherry-picked onto the carrier as `6de56775dc0`/`dd90847d2a3`/`6ca00a58e02`/`d31bc807c76`. H1 delta `4733b7fa84d5` ACCEPT_WITH_NITS (N2/N3/N5/N9 CLOSED, N6 PARTIAL: a non-registration return escaped the envelope, unreachable) → residual fold `31d776705b9` (isinstance guard → private 503, pinned). Carrier gate: registry + api + rights_refresh + private_binding + projection = 245 passed; projection + composition + intake integration + api + registry = 253 passed. mb worktrees `semi-b-h1-registry` and `semi-b-t08b-fix` removed (seat-executed). T08c-1 delta `cd0fbb500d90` re-review in flight; T08c-2 binder seat worktree `semi-b-t08c2-seat` minted at `d31bc807c76` + T08c-1 lineage (`f71d5b51b8a`/`cacdac56697`). CI wiring (curated `semiconductor-b-boundary` paths + run steps + ci.yml triggers for `workspace_projection.py`/`test_workspace_projection.py` and `semiconductor_witness_scope.py`/`test_semiconductor_witness_scope.py`) is owed as ONE seat commit after the T08c-1 cherry-pick, with the ci_pack closure test.

- 16:0xZ — T08c-2 BINDER + T10g CLIENT on the carrier. T08c-2 (`engine/market_ontology/semiconductor_owner_bundle.py` + the leaf `theme_research_binding.py`, registry `load_bundle`, route dispatch, 27+13 tests) seat-executed, Opus READ_ONLY ACCEPT_WITH_NITS (6 nits, 0 blockers) → nit fold (`workspace_unverified.<alias>` for a preceding-read failure on an object the manifest advertises, fail-closed `scope_filter` on any non-empty private tuple, `verify_workspace_cik` comparator, dead-400 note, LAZY loader binding so the registry's import closure stays light for producers, route docstring) → delta re-review **ACCEPT, no blockers**, four low nits folded on the carrier as `cd1b9c4bbcb` (source-parsed drift guard binding the loader's two coverage-absence literals to what the reader can actually raise; same-length tampering mutation so the sha256 branch runs, not the length branch; no interpreter-dependent module counts; no vertical named in the route docstring). Carrier lineage `a2b0f642930` → `bca328e0b94` → `530d8bf81bf` (CI) → `cd1b9c4bbcb`.

- 16:1xZ — **P0 FOUND AND FIXED: the paid client rendered every label as "Unmapped label".** T10g re-grounded the client on the frozen v1 schema after the seat found that under node the production client REFUSED the real composer envelope (`section native_subjects is missing or not an object`): the UI suite's valid case was hand-written and had drifted (status objects for `limitations`/`native_subjects`, a top-level `industrial_views` status, string evidence refs), and the economics management triple was never rendered at all. The valid case is now the composer's own output, jsonschema-validated, and the drifted shapes are asserted rejected. The independent READ_ONLY review of that commit then returned REQUEST_CHANGES on a blocker the seat verified under node before touching anything: the closed bilingual lookup guarded entries with `!Array.isArray(entry)` while EVERY entry is a two-string [EN, ZH] array, so all sixty label keys — status words, view/slice tabs, fact/target chips, time-basis options, error lines, and the five economics role labels — resolved to the typed fallback. The defect predates this branch and stayed green because `pair` and the label maps sat OUTSIDE the node-executed contract block and were only checked by source-substring assertions. Repair (`92ff7243f5e`): correct tuple guard; maps + lookup moved INSIDE the contract block; new pure `labelMapsSelfTest()` positive control that names every key which falls back (reintroducing the old guard turns it red naming all 60); structural test keeping them inside. Same review: the three non-management `expectations` buckets are `const: unavailable` in the frozen contract but any status word was accepted and rendered, and the management assessment's OWN authority block over the paid figures was unvalidated — both now refused. The node batteries also moved their cases from argv to a file (real envelope ~25 KB × 38 cases overran the ~1 MB argument ceiling as an opaque node RangeError). Served W-A payload under node after the fix: envelope valid, five real bilingual rows (Prior outlook / 此前展望 "Revenue 13 – 13.5 US$ bn", Reported actual / 实际报告值 "Revenue 12.34 US$ bn", New outlook / 最新展望, "Comparable · below the prior range", Witness gate "Positive"). 52 UI tests; carrier gate across all B suites 457 passed.

- 16:2xZ — CI: the owner-bundle loader, the binding leaf and their suite are on the curated `gate: code` job (`530d8bf81bf`; `tests/__init__.py` added after the closure audit named it as the one MISS — the suite reaches its helpers through a package import). **The paid client contract suite was `gate: data` only** — the exact gap `semiconductor-b-boundary` exists to close, and the suite that just caught a 60-label rendering failure — so it moved onto the merge gate (`11f110c7feb`) with an explicit `node --version` step, because its `needs_node` skips would report green on a runner without node; `ci-pack` (the `--gate code` runner) installs node 20. The nightly step in `unrun-subsector-themes` stays.


- 17:5xZ — **CARRIER PUSHED `add35d2ff2c`** (was `e2f4d4909156`; 12 commits). Full B gate **500 passed**; curated-exclusive closure audit **zero MISS**; `tests/test_ci_pack.py -k "closure or curated"` **6 passed**. T12 served proof landed (`6019cf26d14`) and is on the merge gate: W-A HBM/TSMC and W-B SiC-GaN/onsemi each render five bilingual economics rows through real app + real reader + mount partial + checked-in client under node; an empty panel fails by construction. Sol return posted on #7780 as issuecomment-5825064933.
- 17:5xZ — T10g delta review REQUEST_CHANGES: its blocker was the curated-scope closure gap the UI-suite wiring opened, already closed by `10dec7a8035`/`e9e09d26c36` (authoritative test 6/6 after). Its six further findings folded as `add35d2ff2c` — the important one: the label positive control was blind to `_bi`, the lookup 8 of 10 economics maps actually use, because `_bi` was a laxer COPY of `pair`; the reviewer reproduced the old `!Array.isArray` bug in `_bi` alone with the self-test still clean. One lookup now, control walks both names, verified red on reintroduction. Also closed: `economics.management.schema` const unvalidated; neither authority block refused an unknown flag; the last bare map read (prototype-truthy, raw slug monolingual) removed and forbidden by a source scan.
- CI traps recorded for the next session: the closure audit raises `ValueError: <job> derives no closure` when a run step names an UNTRACKED file (commit first, then audit); making a JS-contract suite import the composer drags requests/pandas/pyarrow onto every job declaring it (keep a committed envelope fixture pinned by a test in a suite that already has those imports); `tests/fixtures/semiconductor_theme_research/` is roster-pinned to exactly 28 INPUT cases, composed outputs go in `tests/fixtures/theme_research_client/`; node harness cases must go through a FILE, not argv (~1 MB ceiling → opaque `RangeError`).


- 19:0xZ — **ITEM 2, T04b, T12 CORRECTION, HOOKS 2 AND 3 ON THE CARRIER.** Head `9b2132fbb6e`, pushed. Full twenty-suite gate **770 passed, 2 skipped**; curated-exclusive closure audit **{}**; `tests/test_ci_pack.py -k "closure or curated or scope"` 20 passed (6m17s).
  - **Item 2** (`tests/test_semiconductor_publication_primitives.py`, `499ea19528b`) — Opus READ_ONLY ACCEPT_WITH_NITS, no blockers. Its load-bearing claim verified by the reviewer: a well-formed but STALE `previous_manifest_sha256` passes `validate_workspace_manifest`, and a repo sweep finds NO second verifier (the refresh script sets or propagates the link from bytes it just wrote; `app/prophet_lab.py` only consumes the reader's verdict) — so on B's read path the chain link is provenance, not an integrity control. TWO NUMBERS I GAVE SOL WERE WRONG and are corrected on #7780 (issuecomment-5825640745): the object count is **2N + 2**, not four per witness (re-measured: 1 witness = 4 objects / 19 082 B, 2 witnesses = 6 objects / 36 038 B), and 36 038 double-counts the marker, which is a byte-identical twin of the immutable manifest (unique content **34 218**; both manifests grow with the roster, 1 052 B at one witness vs 1 820 B at two). Three properties the suite does NOT cover, by scope: store-level idempotence of a same-directory re-write, disabling the chain-link COMPARISON while leaving the hash call, and any model of a real chain walk (the fixture's transport seam cannot express one) — all covered by the Company Intelligence owner's suites.
  - **T12 correctness fix** (`325a9b5d050`) — the served proof compared rendered text to the SAME served payload, so a wrong-issuer swap and a ×10 scaling both passed. The expectation is now read from the nest INPUT payloads; both mutations verified caught. Retraction posted to Sol.
  - **Hook 2** (`8050a40ebfb` + `23cf3a70ba1`, Opus ACCEPT_WITH_NITS, fold `9b2132fbb6e`) — the mount is the registration's, not the template's. **Design deviation from the packet, measured**: the mount strings live in a NEW leaf `engine/market_ontology/theme_research_mounts.py` (closure 2 files), not on `VerticalRegistration` (closure **55** repo files including the frozen theme-graph store), because four curated jobs declare a page builder and would otherwise re-run on every change to the research stack. Second deviation: a basket claims a mount only on a **PRIMARY** crosswalk claim — the live crosswalk's `ai_semiconductors` lists `ai_infra` among its `basket_ids`, and the crosswalk's own header says `primary_basket_id` is the only mapping sound in the basket→theme direction (the reviewer corroborated with two other consumers). Both calls were put to Sol to overrule.
  - **Hook 3** (`d51fd8f5fed`) — the Theme Tracker board mounts the same way, retiring the inline copy in `state_of_themes.html.j2`. Row-gated: a vertical mounts only where its anchor is already a theme row (`ai_semiconductors` is the first of eighteen in `site/neuralwebdata/theme_state.json`, and rendering the REAL root gives exactly one section with assets once). A registered anchor with no row is logged and mounts nothing; a duplicate row mounts once; any research-layer fault yields no mounts, one warning, and a board that still renders.
  - **Review-found defect, fixed**: an unreadable `config/theme_crosswalk.yml` cached emptiness for the whole process and said nothing — the same failure this repo already paid for one file over. It now warns once in the `::warning title=theme-crosswalk-unreadable::` form and is not cached, so the file coming back recovers in-process.
  - **Review-found missed mutation, closed**: rendering the note as two bare escaped values put BOTH languages on screen with no `l-en`/`l-zh` span, and 139 tests still passed because only the title's spans were asserted.
  - **Still two copies of the slice labels**: the shipped client keeps its own `L.slice` map and `TR_SLICE_KEYS`; `data-slice-labels` is markup hook 4 will read. `test_client_slice_labels_match_the_registration` reconciles them until then, and a second vertical's slice chips still render "Unmapped label" until hook 4 lands.
  - **T04b** (`f2c2893cf07`) — `_filter_bundle_for_rights` fails closed on an unmapped or absent source ref and withholds `interpretation_blocks` whose input revisions are not all served. Opus review in flight.
  - **FINDING — the shipped tracker page has no mount at all today.** `site/state_of_themes.html` was last built 2026-09-23, before T10, and carries zero theme-research markup; no test asserts otherwise (a CI comment that said the client suite does was corrected in `d51fd8f5fed`). The page regenerates on the nightly after merge.
  - **Process**: an independent review was dispatched against the seat's LIVE worktree and watched hook 3 land under it. Reviews are now dispatched against a detached copy at the reviewed commit (`semi-b-h3-review-7984acd39df19a57`).

- 22:0xZ — **HOOKS 4a/4b, THREE REVIEW FOLDS, THE RULED REGISTRY DELTA AND THE GENERATION-COHERENCE GUARD.** Sol rulings 5825621672, 5825697068 and 5825811888 consumed.
  - **H4a/H4b** (`86f699b054d`, `a45788878db`) — the client validates against a mount's SPEC and runs one instance per mount, so two verticals mount on one page with their own bilingual labels. The frozen contract block is byte-identical across H4b (sha256 `b858f982e2…`).
  - **Hook 3 review — REJECT, one blocker, folded (`17b72317b73`).** Both page builders guarded the research import with `except ImportError`. That import first executes `engine/market_ontology/__init__.py` (which imports `exposure_map`), and a malformed registration raises `ValueError` from `MountFacts.__post_init__` at import time — so a research-layer fault killed the ENTIRE Theme Tracker page rather than the mount. **Law for the next session: a guarded import of the research leaf catches `Exception`, never `ImportError`.**
  - **T04b review 1 — REQUEST_CHANGES, folded (`faf5aa4f315`).** Four holes in the attribution the fail-closed rule rests on: `data/baskets/../finviz_themes/private.json` resolved to a PERMITTED family and was served; `source_uri` silently overrode a disagreeing `locator`; `str()` coercion made the integer 12 and the string "12" one revision; a non-mapping row raised `AttributeError` into the route catch-all and returned 503 for the WHOLE request.
  - **THE RETRACTION.** My T04b return and commit told Sol that mapping an EDGAR URI "means naming a rights FAMILY, and rights families are not approved by analogy". FALSE — `config/theme_sources.yml` already carried `sec_edgar` (`direct_display_ok`/`keyless_public`), admitted by `b256aa6a756`, qualified by `7d456cd37d8`, both by this operation, with a review outcome naming these very witnesses. Sol 5825697068 item 5: I raced the ruling, which had already decided it. **Check the register a ruling is about before citing that ruling to refuse work.**
  - **Registry delta APPLIED (`276c6183061`)** — Sol item 5 binds `("https://www.sec.gov/Archives/", "sec_edgar")`; item 6 adds the eleven Robotics publisher families at `rights_class: unresolved`, recognition without permission (the served result is unchanged; the refusal REASON changes). #7870 is the one shared-foundation writer and 5825811888 item 5 says the shared owner applies it once. Every row keeps the R7 warning that `keyless_public` is INFERRED, not an acquisition receipt. Neither prefix table had ANY test before this.
  - **T04b review 2 — REQUEST_CHANGES, two blockers, both mine, folded.** (1) The traversal guard was one encoding away: `%2e%2e` and `..%2f` are traversal to anything that dereferences the URI, and the instant the same branch landed https prefixes, `https://www.sec.gov/Archives/%2e%2e/%2e%2e/vendor/private.json` resolved to `sec_edgar` and was SERVED. A percent-escape is now declined outright, still with no decoding. (2) **`locator` is not a rights ref** — the corpus fills it with `para-3`/`table-1` and the v1.1 proposal names the dependency as `family_for_source_ref(source_uri)`. The original fallback let a paragraph pointer that looks like a repo path attribute a row; my co-equal-veto repair then manufactured contradictions between an external URL and an internal pointer. `source_uri` is now the ONLY field consulted.
  - **Coverage hole the second review found**: the evidence route's SERVE direction was never proven end to end — every witness source is `example.invalid`, fail-closed withheld, so both end-to-end tests had been silently SKIPPING. A new in-memory fixture re-points the sources onto `sec_edgar` (re-stamping the content-hash revision and following the interpretation blocks' input revisions) without touching the pinned 28-case roster.
  - **Generation coherence (Sol item 3, grain from 5825811888 item 4)** — `_serve_witness` withholds the preceding half unless both receipts carry the SAME nonempty `generation_id`: `workspace_generation_split` / `workspace_generation_unqualified`, never a 503 and never a mixed panel. Checked BEFORE `_admit`, so the revision fingerprint never names a period the bundle does not serve. The A→B proof is a real flip: two production-written generations with different publication clocks, the real reader, cache cleared between the halves. Scoped to this publication domain only — no cross-domain generation equality.
  - **Mount custody: the request Sol asked for is EMPTY.** `theme_research_anchor` appears nowhere in `templates/` or `scripts/`; both render paths reach the one section partial, and `templates/basket_detail.html.j2` differs from its pinned base by exactly +1/-0 (the generic aggregator). #7669 needs nothing.
  - **Robotics entry is POST-MERGE ONLY.** Their paste-ready registry/leaf entry imports `robotics_theme_research`, which does not exist on this branch; adding it now breaks the registry.
  - **Not ours, flagged**: three `tests/test_theme_graph_identity_resolution.py` tests fail on this branch because the committed graph holds 2807 company nodes against a 2806 pin.

## Fabric surface and routing

Canonical Subagent Fabric on this host is the installed `pool` CLI (lease broker + `sub.sh`/`remote_sub.sh`, hosts m1/mb remote lanes, Studio seat local only for grok/ocfree). Routing law R23: seat (this Fable) decomposes/adjudicates; operators (glm-5.3, grok-4.6, cursor) own bounded commissions; executors (MiniMax, qwen, glm-flash) implement; independent review by qwen3.8-max/grok/Opus READ_ONLY audit. Every child packet carries mission, exact source refs, owned files, non-goals, deterministic acceptance gate, stop condition and a fixed return format. `WHY NOT FABLE` for every child: bounded, specified by the accepted plan, deterministic pytest gate available. Native Opus children only for READ_ONLY audit or bounded orchestration under the global routing guard.

## Exact next action

The Sol return for all three rulings is **POSTED** (#7780 issuecomment-5852699571), the Communications scope answer is **POSTED** (#7870 issuecomment-5852703250), and the exact-head pack verdict is **RETURNED** (#7780 issuecomment-5853134369). Head is **`27a92dac7678`** — the `main` merge, the curated-closure fix, then the two fix commits below — and the PR is **`mergeable=MERGEABLE`**.

1. **The exact-head pack verdict is RETURNED.** The first hosted run at `1e38d5c955dc` produced **7 failed packs, and SIX of the seven were mine** — not the "two" an earlier return of mine claimed. **One defect caused four of them**: a hard top-level `import jsonschema` in `engine/theme_graph/curation_assertion.py`, a module inside the shared `app.main` import closure, so on a runner without the dependency the whole app import exploded instead of degrading and OTHER teams' suites failed on "route is mounted on the assembled production app" (`test_prophet_lab_api` x7, `test_research_api`, `test_account_prefs` x2, `billing-emails`, `biocatalyst-serving`). **Attribute a pack by reading its traceback, never by the job's name.** Fixed in `52b741c5082b` (deferral via a memoized accessor, the `engine/options_nbbo_cohort.py:82` convention) and `27a92dac7678` (the other two, below). Only `ci-pack-4` was not mine: its own error line names `prelaunch-hardening: step 'collect beacon throttle + growth-event registry + tape per-IP...'` and every pytest run in that pack passed.

   At the new head, `ci-plan` has **not** fanned out yet — the only checks present are the authority set (green) plus `ci-authority/codex/merge-queue-pilot`, whose failure is **not** real: its payload says `allowed: true`, `reason: same_repo_admin_authority_change`, `admin_verified: true`, failing solely on `context_reason: inactive_base_context`. Per Sol 5825697068 item 7, **do not call CI green until the packs land**; return that verdict rather than predicting it.

   Two fixes in `27a92dac7678` are worth carrying as laws. (a) **Anything entering the API's load-time import closure must join the macro-api restart regex** between `# BEGIN MACRO_API_RESTART_TRIGGER` / `# END` in `app/deploy/update.sh`. `app/theme_research.py` importing the registration chain at module level pulled in six `engine/market_ontology/*.py` plus `curation_assertion.py`; without them a merged change deploys to disk and the live API serves the **stale** module until something unrelated restarts it (the 2026-07-26 `engine/research_vault` gap that test exists to catch). Enumerate, never glob — the rest of `market_ontology` is nightly-only. `engine/market_ontology/__init__.py` counts, because it imports `exposure_map` and so is not an inert init. (b) **A `rfind`-anchored template test makes any later loop your bug**: `tests/test_finance_entry_points.py` finds the lanes loop in `state_of_themes.html.j2` via `rfind("{% endfor %}")`, so my inline mount loop moved their anchor (44734 -> 49686) even though their invariant still held (44795 < 44895 < 44946). The fix belongs to the newcomer: put the loop behind an aggregator partial (`templates/_theme_research_mounts.html.j2`, mirroring the basket page), leaving the page's last `endfor` theirs, and prove product-neutrality by diffing the rendered bytes — identical at 106533 bytes, sha256 `7e2356c7de5c...`.

2. **Retention (Sol item 2) — measured, and it splits in two.** *At rest it is ALREADY BUILT*: `write_workspace_generation` writes each generation to its own immutable content-addressed directory `event_workspaces/generations/<generation_id>/`, guards `immutable generation collision`, never deletes a predecessor, and the v2 manifest carries `previous_generation_id` **and** `previous_manifest_sha256` — exactly the "receipt in the NEW generation pointing at the retained prior one" the ruling asks for (A5C frozen spec A). Create no second store. *Reader-resolvable history is CLOSED BY DESIGN*: `read_event_workspace(params)` accepts **only `event_id`** and resolves marker → CURRENT generation, and the sibling reader states the rule — "No source URL, generation id, score threshold, or query expression comes from the model." Making a prior generation resolvable therefore means widening a deliberately closed input surface: an owner + Sol decision, not code on this carrier. **Consequence worth carrying: the `workspace_generation_split` guard is entailed by that reader design** — because the reader always yields the current generation, two halves of one comparison can straddle an ordinary refresh. Enrollment today is `DISCOVERY_TICKERS = HOMEBUILDER_TICKERS + ("TSM", "ON")`.
3. **Q1 rights qualification** — unchanged, BLOCKED: its input register (the Robotics R1 corpus) exists nowhere on this host and must come from #7908.
4. **Post-merge additive commit** — the Robotics registry/leaf entry still waits; `robotics_theme_research` is absent from this tree. `mining_theme_research` arrived WITH the merge and registers no mount (`REGISTRY -> ['ai_semiconductors']`), so nothing is owed for it.
5. **Re-run the closure audit after ANY merge, not only after your own edits.** This merge had to close `gaps: {'finance-intelligence': ('config/theme_crosswalk.yml', 'engine/market_ontology/theme_research_mounts.py')}` — main added a `finance-intelligence` exclusive job declaring `scripts/build_state_of_themes.py`, so shared hook 2's leaf widened into a job that did not exist at the old base. Resolve curated closure lists as a **UNION**: a curated exclusive job must name every file in its import closure, so dropping either side breaks that side's audit.

**Gate discipline for whoever picks this up.** The sparse profile omits `data, mockups, site, verify_shots`. A merge checkout can drop `site/` and produce **16 failures that look exactly like a code regression — including BOTH mandatory W-A/W-B served-witness proofs**; every one was a materialisation artifact and pytest's own footer says so. Run `python3 scripts/worktree_sparse.py add site` before trusting any gate. **Correction to an earlier version of this handoff:** materialising `data/` does NOT cost 65,511 files / 4.41 GiB unless you take all of it — the helper's `add` is top-level only, but **git's own `git sparse-checkout add data/regime data/theme_graph` works** and costs 36 + 8 files / 8.5 MiB. Do that, because it converts two INDETERMINATEs into real answers: `test_check_validated_claims` then passes (its failure is the sparse artifact the tool itself names — "a CHECKOUT fault, NOT a wave of new unearned claims"), and `check_theme_graph_contracts` gives a real verdict. **That verdict is not clean, and it still exits 0**: `::warning theme graph contract breach:: 1168 identity_resolution row(s) violate the state<->ids biconditional`, with a census of 2807 company nodes. So `exit 0` is not a pass with data present either.

**Scope the gate to what your change can BREAK, not to what you own.** The "1339 passed / 0 failed" an earlier version of this handoff recorded was true of the set that was run and false of the code that shipped: it omitted `tests/test_biocatalyst_deploy.py` and `tests/test_finance_entry_points.py` — both other teams', both broken by this lane, both failing locally the whole time. **Also: a new template partial needs adding to every test harness that copies its siblings** — three harnesses copy a fixed allowlist into a synthetic root, and with `ignore missing` a forgotten one renders ZERO mount sections with no error and no assertion naming the file — **and to all three CI declaration sites** (`legacy-jobs.yml` x2, `ci.yml` x1). Production is safe by contrast: the builder uses a `FileSystemLoader` over the real `templates/` dir.

Local evidence at head `27a92dac7678`: **1111 passed · 8 skipped · 1 xfailed** across 18 suites covering every failed pack, then `tests/test_state_of_themes.py` **53 passed · 1 skipped**; closure audit **`gaps: {}`**. Residual: 3 pre-existing `tests/test_biocatalyst_deploy.py` host failures (provisioner no-follow layout, immutable gate/heartbeat, writable/symlinked targets), **baselined identical at HEAD before any edit** — withdraw your changes and re-run before blaming yourself for them.

**Source custody.** An uncommitted foreign edit — the identity re-pin 2,806 → 2,807 carrying a full VMRK adjudication — appeared in the carrier worktree on a path with a normal `H` index flag and an mtime older than any commit made here. It is **not on main** (still 2,806 after merging 606 commits), so it is unlanded work that arrived in the wrong tree. It was preserved byte-identically (sha256 `7f1c4464a2ab…`) to the session scratchpad, the path reverted, and it was **NOT committed here**: the twin-edge repair the edit itself escalates is the theme graph's lane. The three `tests/test_theme_graph_identity_resolution.py` failures remain open and unlanded — and with `data/theme_graph` materialised the contracts checker now MEASURES the lane's size: **1168 breaching identity_resolution rows** and a census of **2807** company nodes, the very number that foreign edit re-pinned to. The breach lives in committed `identity_resolution.parquet` plus the frozen `store.py` (#7462); nothing on this carrier can produce it and the frozen store stays untouched.

Production coverage is still NOT_SERVED: the live `event_workspaces/` nest carries no TSM/ON and one period per issuer per generation, so a served production economics panel needs #7870 merged, a nightly that has seen two consecutive releases per witness, and the retention decision above. State: `MISSION_COMPLETE: FALSE` · `SEMICONDUCTOR_B: NOT_BUILT` · `C1: DEFERRED` · `V1_1: PROPOSED_NOT_BUILT` · `RIGHTS_QUALIFICATION: QUEUED_NOT_STARTED`. Do not place lanes on mini2 until an egress probe passes; do not touch m1 outside its window; never edit a running dispatcher script.
