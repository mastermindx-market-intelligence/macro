---
workstream: WS:CODE-INTELLIGENCE-FABRIC
session: sol/code-intelligence-fabric-agentos-20260830
model: sol
ended_because: complete
mission: >
  Close the records-only Code Intelligence Fabric F0 architecture wave, preserve its exact authority
  and no-rebuild boundaries in Agent OS, and leave C0/Z0 as separately keyed capacity-selectable
  falsifiers rather than treating architecture, upstream probes or green CI as a live capability.
state_before: >
  The Chairman handoff and local F0 evidence existed, and Mastermind PR #276 carried the architecture,
  benchmark, rights, current-source and C0/Z0 plans. Macro Agent OS had no Code Intelligence workstream,
  no explicit semantic parent ruling and no durable continuation capable of surviving this chat.
changed:
  - path: mastermind#276
    what: >
      Protected the records-only F0 source law from final candidate
      e6553b3b640712de4446b0337c291243fde07c61 as accepted squash merge
      620263090fb9f272f763e420ba103b0ff8dc5f31, including two governed read planes,
      canonical verification separation, current Sol action-target boundary, supply-chain rights,
      multi-language/deployment law, benchmark charter and bounded C0/Z0 plans.
  - path: agentos/workstreams/WS-CODE-INTELLIGENCE-FABRIC.md
    what: >
      Created the durable organizational home, exact capability ledger, wave graph, routing state,
      landmines, do-not-redo law and next action under existing macro-context-index.
  - path: agentos/decisions/DEC-CODE-INTELLIGENCE-FABRIC-EXTENDS-CONTEXT-INDEX-WITH-TWO-GOVERNED-READ-PLANES.md
    what: >
      Froze the semantic parent and separated global discovery, exact-worktree semantics and
      canonical Git/GitHub verification without creating a new top-level semantic program.
  - path: agentos/discoveries/DSC-CODE-INTELLIGENCE-ENGINES-ARE-DISPOSABLE-BACKENDS.md
    what: >
      Preserved the falsifiable ruling that Serena/LSP/Zoekt are replaceable engines behind fixed
      Mastermind contracts and never model-facing authority or source truth.
verified:
  - claim: >
      F0's effective Mastermind delta is records only and the accepted carrier contains no CodeIntel
      runtime, capability-profile, service, host, credential, Slack, Linear or Agent OS implementation
      path.
    command: >
      GitHub compare protected Mastermind base a26d0286451d53e78cae96c741ee6e2a51b883ba to
      final PR #276 candidate e6553b3b640712de4446b0337c291243fde07c61 and inspect the
      accepted merge 620263090fb9f272f763e420ba103b0ff8dc5f31.
    result: >
      Exactly eight added research/plan records, 2,041 additions and zero deletions; no
      production/runtime/config path differs from protected master in the F0 carrier.
  - claim: >
      The existing exact Sol action-target resolver remains the authority owner and CodeIntel cannot
      elect a sister Sol or convert worktree identity into action authority.
    command: >
      Read protected control_plane/sol_action_target.py plus the current-source reconciliation in
      Mastermind PR #276.
    result: >
      SessionTargetRegistry plus complete RuntimeBinding evidence owns exact Sol action-target
      resolution; CodeIntel is explicitly evidence-only.
  - claim: >
      macro-context-index is an existing lawful semantic parent for governed cross-repository project
      retrieval, while its current implementation and workstream remain separate from CodeIntel.
    command: >
      Read current Macro config/mastermind_programs.yml entry macro-context-index and
      agentos/workstreams/WS-MACRO-CONTEXT-INDEX.md.
    result: >
      Parent exists and owns authority-classed context retrieval; no current Code Intelligence,
      Serena or Zoekt workstream existed before this carrier.
  - claim: >
      The immediate dedicated semantic-program hypothesis is not the accepted organizational ruling.
    command: >
      Reconcile Macro PRs #6695/#6696 against this decision and current semantic ownership.
    result: >
      Their test-only premise is superseded; both remain non-implementation evidence and must close
      unmerged after this carrier lands.
unverified:
  - claim: >
      A Serena-backed or direct-LSP semantic facade can satisfy exact two-worktree isolation and useful
      Python plus TypeScript/TSX recall within acceptable resource budgets.
    what_would_verify: >
      Complete C0 on a fresh carrier with hostile cross-worktree, repository-config, binary/schema,
      write, restart, null/degraded and multi-language cases; return NO_SAFE_BACKEND if neither passes.
  - claim: >
      Governed Zoekt discovery can meet the 5-minute median / 15-minute ceiling protected-ref freshness
      target with truthful omissions, exclusions and acceptable full/incremental/rebuild cost.
    what_would_verify: >
      Complete Z0 on a fresh carrier across the three real repositories, including stale/corrupt/
      omitted-ref negative controls and colocated-versus-separate topology measurements.
  - claim: >
      CodeIntel improves real worker outcomes enough to justify production rollout.
    what_would_verify: >
      Complete CI4/CI5 real implementation and adversarial PR-review canaries with cognition/tool,
      recall, wrong-owner, stale-source and host-resource evidence against the frozen benchmark.
unresolved:
  - "C0 has no concrete receiver or RuntimeBinding; preferred avenue CTO Sol is a capability preference, not an assignment."
  - "Z0 has no concrete receiver or RuntimeBinding; preferred avenue Terra is a capability preference, not an assignment."
  - "No production profile, service, host, credential, index, semantic backend or Control Room projection is installed or armed."
  - "A future top-level semantic-program split is intentionally held until empirical proof shows the existing parent is insufficient."
next_actions:
  - >
    Land this records-only Macro carrier after exact-head `python3 scripts/agentos.py validate`, hosted
    fences/semantic CI and current-main collision review.
  - >
    Through the canonical capacity/placement owner, bind one concrete eligible CTO Sol receiver to
    fresh child mastermind-codeintel-c0-semantic-falsifier-20260830-sol-001; only then issue pickup,
    watcher setup and separate START on a new C0 GitHub carrier.
  - >
    Independently bind one concrete eligible Terra receiver to fresh child
    mastermind-codeintel-z0-discovery-falsifier-20260830-sol-001; run in parallel only because the
    frozen result paths and implementation surfaces remain disjoint.
  - >
    Sol adversarially reviews each exact returned head. A pass advances only the bounded falsifier;
    it does not arm production or authorize CI1-CI5 automatically.
do_not_redo:
  - "Do not reopen F0 architecture because C0 or Z0 selects a different backend; stable contracts deliberately permit backend replacement and NO_SAFE_BACKEND."
  - "Do not grant direct upstream Serena, generic local stdio MCP, broad GitHub MCP or one super-MCP to workers."
  - "Do not create a second lifecycle, capability registry, workspace registry, action-target owner, code-truth store, index authority, memory plane, retry plane or central semantic router."
  - "Do not interpret PR #276 merge, green CI, local upstream probes or this Agent OS record as installation, arming, production proof or receiver assignment."
  - "Do not emit unbound Slack OPEN_PICKUP/PRECOMMISSION or ask the Chairman to choose a numbered account for ordinary CAPACITY_SELECTABLE placement."
  - "Do not revive Macro PR #6695 or #6696 as implementation carriers after this decision becomes durable."
danger_areas:
  - "Worktree path, Git HEAD and dirty fingerprint are read-scope evidence; they are not Worker, Attempt or Sol authority."
  - "Repository .serena, tsconfig or language-server configuration can supply semantics but may never select commands, plugins, executables or network behavior."
  - "Negative search results must retain coverage/freshness/health/truncation state; plausible empty output is a hard failure."
  - "Terminal TypeScript/TSX is load-bearing; a Python-only semantic backend creates a false-green platform capability."
  - "Control Room may later link evidence, but must not become a code browser, editor, index administrator or another lifecycle surface."
prs: [276, 6695, 6696, 6697]
decisions:
  - DEC:CODE-INTELLIGENCE-FABRIC-EXTENDS-CONTEXT-INDEX-WITH-TWO-GOVERNED-READ-PLANES
discoveries:
  - DSC:CODE-INTELLIGENCE-ENGINES-ARE-DISPOSABLE-BACKENDS
---

## Capability delta

Before F0, there was no durable company architecture for combining company-wide code discovery,
exact candidate-worktree semantics and canonical verification without widening worker authority.
After F0, the source law, benchmark, rights, failure design, multi-language floor and bounded
falsifier sequence are durable. No runtime capability exists yet.

## Routing receipt

```text
C0 PREFERRED_AVENUE: CTO Sol
C0 RECEIVER_BINDING_MODE: CAPACITY_SELECTABLE
C0 PLACEMENT_STATE: WAITING_CAPACITY / needs_placement
Z0 PREFERRED_AVENUE: Terra
Z0 RECEIVER_BINDING_MODE: CAPACITY_SELECTABLE
Z0 PLACEMENT_STATE: WAITING_CAPACITY / needs_placement
WHY NOT FABLE: architecture, authority and falsifier boundaries are frozen; remaining work is bounded empirical engineering/research and does not require scarce principal continuity.
```
