---
workstream: WS:REGIME-BRIEFING-CONTEXT
session: claude/hmm-briefing-context-20260909
model: sol
ended_because: ci_handoff
mission: >-
  Integrate the bounded W1 UTC-normalization refusal and six dependency-scope repairs on the
  existing AI-briefing carrier, preserve honest regime provenance, and leave the exact release
  path recoverable without changing HMM math, regime thresholds, ranking, sizing or trading authority.
state_before: >-
  PR #7024 head 66f9e5d0be33e2f6c1a6d4e749536d2f3e956cc5 was Draft / HOLD-FOR-SOL and
  BUILT_NOT_PROVEN. The incumbent worktree carried an unpublished UTC overflow repair and tests;
  hosted evidence identified six missing exclusive-scope dependency pairs.
changed:
  - path: engine/master_brain.py
    what: Refuse UTC-normalization overflow as explicit unavailable/unknown evidence instead of silently omitting regime probability context.
  - path: tests/test_regime_label_honesty.py
    what: Pin source/model-fit/reference-clock overflow behavior, valid aware-clock equivalence, and the six dependency-scope pairs.
  - path: .github/ci/legacy-jobs.yml
    what: Add engine/axes.py and engine/regime.py to biocatalyst-history, flow-surface, and unrun-government-revenue-grader exclusive scopes only.
  - path: agentos/workstreams/WS-REGIME-BRIEFING-CONTEXT.md
    what: Advance organizational continuation to immutable-head review, current-base checks, Lens acceptance and production proof.
  - path: research/artifacts/regime_briefing_context_20260909/source_integration_20260911j.json
    what: Preserve exact local source hashes, owner-suite proof, static closure proof, release hold and remaining gates.
prs: [7024]
discoveries: [DSC:REGIME-BRIEF-READER-ALREADY-EXISTS]
verified:
  - claim: The original W1 worktree contains the recovered UTC repair, six manifest additions, integration receipt and workstream update with no staged changes.
    command: "git status --short; git diff --cached --name-status; sha256sum engine/master_brain.py tests/test_regime_label_honesty.py .github/ci/legacy-jobs.yml research/artifacts/regime_briefing_context_20260909/source_integration_20260911j.json agentos/workstreams/WS-REGIME-BRIEFING-CONTEXT.md"
    result: "Six paths dirty/untracked including this handoff; staged diff empty; source hashes fdfb30b5..., a32d5ed2..., 97832e2f...; receipt/workstream hashes 4211e1e7... and 33458f2b...."
  - claim: The existing W1 owning pytest command passes on the original worktree with the genuine committed backscan fixture.
    command: "python -m pytest tests/test_admin_brief_gate.py tests/test_ai_desk_panel.py tests/test_altdata_intel.py tests/test_altdata_price_truth.py tests/test_china_intel_hub.py tests/test_master_brain.py tests/test_regime_label_honesty.py tests/test_master_brain_ladder.py tests/test_master_brain_zh_ladder.py tests/test_master_brain_policy.py tests/test_master_brain_producer.py tests/test_master_brain_scorer.py tests/test_risk_brain.py tests/test_risk_radar_review.py tests/test_shock_deescalation.py tests/test_w7_llm_determinism.py -q"
    result: "252 passed; 0 failed; 0 errors; 0 skipped. Recorded in source_integration_20260911j.json."
  - claim: Head-side canonical CI closure and test-registration scans found no findings after the six manifest additions.
    command: "existing curated_exclusive_closure_findings(.github/ci/legacy-jobs.yml) plus gated_unrun_suites()"
    result: "closure={} and suites=[] across 139 code-job definitions; head-only, not current-base or hosted proof."
  - claim: The earlier response-lost record write completed both intended record targets.
    command: "read research/artifacts/regime_briefing_context_20260909/source_integration_20260911j.json and agentos/workstreams/WS-REGIME-BRIEFING-CONTEXT.md"
    result: "Both files exist with the intended W1 source-integration content; do not replay that write."
unverified:
  - claim: The final committed W1 head is compatible with current main and all hosted binding checks.
    what_would_verify: "Commit/push the bounded source and records, then inspect the immutable merge ref and applicable exact-head CI/security checks against current main."
  - claim: The Lens probability-detail interaction works on the real full briefing page after shared Lens PR #6860 integration.
    what_would_verify: "Run the real full-page browser acceptance on the delivered paired assets and required desktop/mobile, EN/ZH, dark/light states after the shared Lens owner lands or equivalent accepted integration is proven."
  - claim: A naturally generated production briefing contains the dated regime evidence through the real provider and customer path.
    what_would_verify: "After accepted normal release, observe a natural production producer run and authenticated browser/machine-consumer read; do not force master_brain.run(persist=False) as a read-only probe."
unresolved:
  - "PR #7024 has not yet published the integrated UTC/scope repair as an immutable new head."
  - "Current-base compatibility and hosted checks remain unproven on the integrated candidate."
  - "Shared Lens PR #6860 remains a separate interaction dependency for touch/mobile disclosure acceptance."
  - "Natural production adoption and authenticated real-browser proof remain absent."
next_actions:
  - "Run python3 scripts/agentos.py validate and git diff --check on the integrated worktree; repair only W1 record/schema findings."
  - "Stage only the intended W1 source/test/manifest, workstream, handoff and source-integration receipt; commit on claude/hmm-briefing-context-20260909 and normal-push to origin."
  - "Pin the new immutable PR #7024 head; run exact-head review plus latest-base integration/hosted checks without merging main into the carrier merely to remove ancestry lag."
  - "After release gates pass, prove the real full-page Lens interaction and a natural production briefing before promoting beyond BUILT_NOT_PROVEN."
do_not_redo:
  - "Do not reapply the six manifest lines; current expected manifest SHA256 is 97832e2ff228994a68daee95a6e67b6d00b65f26210350439c6406a3e20ff3a1."
  - "Do not rewrite the recovered UTC normalization repair; current engine/master_brain.py SHA256 is fdfb30b52b18d81c72f6c56bd383ec1ef84fc3d40744ac545d0a10ee9d6e0537."
  - "Do not rerun the old response-lost record-write request; both record targets were read back successfully."
  - "Do not recreate PR #7032's macro-suite repair; it merged on 2026-09-12 and must enter only through current-base integration evidence."
  - "Do not create a second regime probability route, HMM, forecast ledger, identity plane, or trading authority for this briefing."
danger_areas:
  - "master_brain.run(persist=False) still appends the existing macro thesis ledger; it is not a read-only production probe."
  - "A current-state posterior, diagnostic momentum and a future-state forecast are different objects; UI/model prose must not collapse them."
  - "Legacy regime probabilities lack full historical model/vintage custody; do not fabricate provenance or substitute later reconstruction for missing saved issuance."
  - "The shared Lens touch bug is owned by PR #6860; duplicating theme.js behavior inside W1 would create a second interaction implementation."
---
## §0 State — what is true right now
W1 is **BUILT_NOT_PROVEN** on the existing PR #7024 carrier. The preserved UTC-refusal change,
regression tests and six narrow CI-scope additions are present in the original worktree, with the
integration receipt and Agent OS workstream update. The branch is still on published head
`66f9e5d0be33e2f6c1a6d4e749536d2f3e956cc5`; these changes are not yet committed or pushed.

The user job is to read a regime assessment that says what the system currently estimates, when that
evidence applies, what is uncertain, and what it does **not** forecast. The machine job is to carry the
existing quad-vector evidence through the existing master-brain briefing path without inventing a
second probability route or turning reconstructed momentum into a future-regime claim.

Authority precedence: current Chairman direction and protected Sol procedure; existing regime owner and
W1 direct-reader design; Agent OS for organizational continuity; GitHub for implementation evidence.
W0 PR #7015, shared Lens PR #6860, Eval OS and existing trading/risk owners keep their separate scopes.

## §1 What is LEFT — in order
1. Validate the Agent OS record set and W1 diff (`python3 scripts/agentos.py validate`; `git diff --check`).
2. Inspect the bounded diff, stage only the intended W1 paths, commit on the same branch and normal-push.
3. Pin the new PR #7024 head and perform exact-head semantic review plus latest-main integration checks.
4. Prove the real full-page Lens interaction on the accepted shared interaction implementation.
5. After accepted release, observe one natural production brief through the real producer and authenticated consumer.

## §2 What will bite you
The evidence date belongs to the regime source, not the page-build clock. Missing or invalid provenance
must remain unavailable/unknown; a later reconstruction may support research but cannot stand in for a
past saved issuance. UTC conversion overflow must refuse explicitly rather than silently dropping context.

The deterministic regime evidence note is allowed to describe current support, disagreement, source age
and caveats. The model is not allowed to calculate probabilities, grant signal authority or rewrite the
note. Diagnostic probability momentum is descriptive under the fitted model and is not a future-event forecast.
## §3 What was decided and found
`DSC:REGIME-BRIEF-READER-ALREADY-EXISTS` remains controlling: use the existing direct master-brain
reader instead of adding a world-state/brief-context probability route for this same consumer.

Data/time/null/correction behavior: preserve source `asof`, model-fit cutoff when known, four-state
probabilities and explicit unavailable/degraded reasons. Missing evidence stays missing. Corrections or
later model reconstructions do not rewrite what an earlier saved issuance knew.

Deterministic versus model method: deterministic code validates and projects the regime evidence and
writes the bilingual saved note; the existing model may synthesize surrounding prose but does not own
probability calculation or the evidence note. HMM math and capital policy are unchanged.

Failure states to preserve visibly include malformed probability vectors, impossible dates, timezone
normalization overflow, missing provenance, stale evidence, label/posterior disagreement, and absence of
an eligible saved issuance. None may become a more confident or more actionable state because data is missing.

Acceptance requires the exact immutable candidate to pass its owner tests and integration gates, the user-facing
brief to preserve date/caveat/probability integrity in real browser states, and one natural production run to
emit and serve the result. Green CI alone is not production acceptance.

Stop and return to Sol instead of proceeding if an owned semantic blob changes unexpectedly, current-base
movement touches the decision-source/dependency contract materially, another writer changes this carrier,
Lens integration requires duplicating shared behavior, or any missing/stale evidence would need fabrication.

## §4 Not in scope — do not adopt
Do not introduce a replacement HMM, parallel forecast/event store, second source clock, new reliability grid,
new ranking/sizing/entry/gross rule, autonomous RL allocation, or duplicate Lens implementation. Regime
forecasting, Market Memory conditioning, relationship propagation and portfolio shadow evaluation remain later
separate evidence-gated capabilities. Preserve W0's historical-honesty boundary and existing negative evidence.
