---
workstream: WS:AGENT-OS
session: claude/agent-os-phase-1-adoption
model: fable
ended_because: ci_handoff

mission: >
  Execute Agent OS Phase 1 (Adoption): durable operating sections in the Macro,
  Mastermind, and Terminal agent instruction files; <=10 evidence-anchored decision
  backfills; bring discoveries to >=5; assess the independent-handoff gate honestly
  rather than manufacturing records to close it.

state_before: >
  Phase 0 + Phase 2 merged as Macro #5472 (2026-08-13 14:29Z): schemas, validator,
  status/brief generators, 9 decisions, 3 discoveries, 6 workstreams, 0 handoffs. No
  adoption instructions existed in any repo's CLAUDE/AGENTS files (verified by grep in
  all three), and no later session had begun Phase 1. Mastermind #44 (Phase 1D-A) had
  just added the read-only CEO boot packet consuming `agentos.py brief --json
  --no-remember`.

changed:
  - path: CLAUDE.md
    what: "New §Agent OS — the company knowledge plane (STANDING): task-start reads, when to mint records, hygiene, I1 boundary."
  - path: AGENTS.md
    what: "Required-context item 5 + full §Agent OS knowledge plane section, same content in this file's fuller register."
  - path: agentos/decisions/ (10 new DEC-*.md)
    what: "Backfill of the 10 highest-value standing fleet-law decisions; every date git-derived, every receipt traced to standing prose, enforcement artifacts, or named PRs."
  - path: agentos/discoveries/DSC-SESSION-WORKTREES-ARE-SPARSE.md
    what: "Landmine: session worktrees omit site/, data/, mockups/ by default; truncation + vacuous-guard consequences."
  - path: agentos/discoveries/DSC-PR-CI-ONLY-RUNS-AGAINST-MAIN-BASE.md
    what: "Constraint: only ci.yml/fences.yml carry pull_request triggers, both branches:[main]; stacked PRs get zero CI."
  - path: agentos/workstreams/WS-AGENT-OS.md
    what: "Waves to truth: W0 done (#5472), W2 done (#5472), W1 in_progress carrying the unmet independent-handoff gate; top-level next_action updated."
  - path: agentos/handoffs/AGENT-OS-2026-08-13.md
    what: "This handoff."
  - path: tests/test_agentos_schema.py
    what: "test_valid_handoff_is_counted now asserts baseline+1 handoffs instead of the absolute '1 handoffs' — the committed store legitimately accrues real handoffs, and the absolute pin made the first real handoff (this one) red the suite."

prs: [5556, 5472]

verified:
  - claim: "The full record store (30 records) is schema-valid with zero errors and zero warnings."
    command: "python3 scripts/agentos.py validate"
    result: "agentos: 30 records (6 workstreams, 19 decisions, 5 discoveries, 0 handoffs) — 0 error(s), 0 warning(s); exit 0 (pre-handoff-record count; re-run after this file: 31 records)"
  - claim: "Both agentos test suites pass on the expanded store."
    command: "python3 -m pytest tests/test_agentos_schema.py tests/test_agentos_status.py -q"
    result: "67 passed in 21.02s"
  - claim: "Session worktrees are sparse by default with site/, data/, mockups/ omitted."
    command: "git config core.sparseCheckout && git ls-tree --name-only HEAD | grep -x 'site\\|data\\|mockups' && ls -d site data mockups"
    result: "config true; all three tracked in HEAD; all three absent on disk (DSC:SESSION-WORKTREES-ARE-SPARSE)"
  - claim: "Only ci.yml and fences.yml have pull_request triggers, both filtered to base main."
    command: "grep -rn 'pull_request' .github/workflows/*.yml"
    result: "Trigger blocks only in ci.yml (branches: [main]) and fences.yml (branches: [main]) (DSC:PR-CI-ONLY-RUNS-AGAINST-MAIN-BASE)"
  - claim: "Mastermind adoption section merged; its Executive-contract pinning suite passes with the new section."
    command: "python3 -m pytest tests/test_strategic_state.py -q (in Mastermind); gh pr view 45 --json state"
    result: "42 passed; Mastermind PR #45 MERGED as b38c18dc"
  - claim: "Terminal adoption PR is armed per that repo's own convention."
    command: "gh pr view 415 --json state,labels,autoMergeRequest (in mastermind-terminal)"
    result: "OPEN, label merge-on-green, native auto-merge enabledAt 2026-08-13T18:24:08Z; master's 3 required checks gate it server-side"

unverified:
  - claim: "The adoption sections actually change cold-session behavior (records get written by sessions that did not author Agent OS)."
    what_would_verify: "3 handoffs in agentos/handoffs/ authored by sessions other than the Phase 0 session and this one, from real work — the Phase 1 acceptance gate that remains open."
  - claim: "Macro PR #5556 is green."
    what_would_verify: "Its ci.yml packs concluding; the merge-on-green sweeper merges on conclusion. Armed at handoff per fleet law."

unresolved:
  - "Phase 1 verdict is PARTIAL: the >=3 independent-session handoff gate is 0/3 at this handoff (this record is by a scaffolding-phase session and is not counted). It accrues from real work as adopted instructions take effect; nothing should be manufactured to close it."
  - "Whether Mastermind/Terminal sessions can practically write Macro-side records without friction (cross-repo PR overhead) — if a real session finds it awkward, record that as a DSC and only then consider tooling."

next_actions:
  - "Watch agentos/handoffs/ fill: when >=3 genuine handoffs exist from sessions other than the Phase 0 session and this one, flip WS:AGENT-OS wave W1 to done and declare Phase 1 COMPLETE in the workstream record."
  - "Phase 3 (compile-context over the existing context index) per research/MASTERMIND_AGENT_OS_V1_IMPLEMENTATION_PLAN.md — includes the agenda-integration wave that retires the brief's UNBLOCKED section (DEC:AGENTOS-READINESS-FEEDS-THE-AGENDA)."
  - "Phase 4 (hook auto-capture, report-only) only AFTER W1's manual-adoption evidence exists — the plan sequences it deliberately; do not automate a format manual use has not proven."

do_not_redo:
  - "Do not re-add adoption sections: Macro CLAUDE.md §Agent OS + AGENTS.md §Agent OS knowledge plane; Mastermind AGENTS.md/CLAUDE.md §Agent OS (merged #45); Terminal root AGENTS.md §Agent OS knowledge plane + terminal/AGENTS.md §Agent OS (#415). Grep before writing — duplicating them is drift."
  - "Do not backfill more historical decisions casually: the <=10 cap was spent deliberately on merge discipline, sweeper handoff, model routing, plain-copy pairing, context economy, spawn binding, gauntlet-at-promotion, instrument-vs-market verdicts, coverage pass, and gh quota. New DEC records should come from decisions actually taken by live sessions."
  - "Do not propose a task registry, dispatcher, scheduler, or liveness service in agentos/ — ruled: DEC:AGENTOS-NO-TASK-STORE (C1), DEC:AGENTOS-CLAIMS-ARE-NOT-LIVE-ACTIVITY (C2), invariant I1."
  - "Do not create local Agent OS stores in Mastermind or Terminal — their sections point here on purpose (Charter P7); Mastermind's read path is the one-way ceo_boot_packet bridge (#44)."

danger_areas:
  - "The generated views docs/AGENT_OS_STATE.md and data/governance/agent_os_state.json are nightly-only; committing regenerated copies in a records PR puts independent record edits into conflict on a shared file (DEC:AGENTOS-NIGHTLY-IS-THE-ONLY-REGENERATOR)."
  - "This repo's session worktrees are SPARSE: writes into omitted trees (site/, data/, mockups/) silently truncate committed artifacts, and site-globbing guards pass vacuously (DSC:SESSION-WORKTREES-ARE-SPARSE)."
  - "A stacked PR on a non-main base here gets ZERO CI forever — it can neither hand off nor merge (DSC:PR-CI-ONLY-RUNS-AGAINST-MAIN-BASE)."
  - "cross-repo gh slips: an idle `git push origin --delete <branch>` aimed at the wrong repo closed Terminal #415 mid-flight this session (branch deletion closes an open PR and cancels its armed auto-merge). Recovery: re-push the same head, gh pr reopen, re-label, re-arm — full sequence took ~3 minutes and the head SHA's concluded checks re-associate."
  - "The primary Macro checkout is shared, often detached, and can be missing files your worktree has — grep the ref/worktree, never the shared checkout (observed again this session on .claude/hooks/)."

decisions: [DEC:MERGE-ON-CONCLUDED-CHECKS-ONLY, DEC:DEFAULT-FINISH-HANDS-WAIT-TO-SWEEPER, DEC:OPUS-BUILDS-SONNET-EXPLORES-FABLE-GATED, DEC:PLAIN-COPY-ASSETS-SHIP-PAIRED, DEC:FRONTIER-BURN-IS-CONTEXT-TIMES-TURNS, DEC:SPAWN-CONTEXT-IS-BOUND-NOT-POINTED, DEC:GAUNTLET-GATES-PROMOTION-NOT-BUILD, DEC:INSTRUMENT-VERDICT-IS-NOT-MARKET-VERDICT, DEC:CONCLUSIONS-NEED-A-COVERAGE-PASS, DEC:GITHUB-QUOTA-IS-ONE-SHARED-BUCKET]
discoveries: [DSC:SESSION-WORKTREES-ARE-SPARSE, DSC:PR-CI-ONLY-RUNS-AGAINST-MAIN-BASE]
---

## §0 State — what is true right now

Phase 1 adoption artifacts are complete and shipping: durable Agent OS operating
sections exist in all five required instruction surfaces (Macro CLAUDE.md + AGENTS.md in
PR #5556; Mastermind AGENTS.md + CLAUDE.md merged as #45; Terminal root AGENTS.md +
terminal/AGENTS.md armed as #415), the decision corpus is 19 (9 seeded + 10 backfilled,
the backfill cap spent exactly), the discovery corpus is 5 (3 seeded + 2 new, both
verified against the tree), and validate is green over all of it. Phase 1 is PARTIAL,
not COMPLETE: the >=3 independent-session handoff gate stands at 0/3, deliberately —
records manufactured by a scaffolding session would defeat the gate's purpose.

## §1 What is LEFT — in order

1. Let the independent-handoff evidence accrue from real work; count only handoffs by
   sessions other than the Phase 0 session and this one. At >=3, flip W1 to done.
2. Phase 3: `compile-context` over the existing context index, plus the
   agenda-integration wave (see the workstream record's wave list).
3. Phase 4 (report-only hook capture) strictly after W1's evidence exists.

## §2 What will bite you

The four danger areas in the frontmatter, in one breath: never commit the generated
views; never write into a sparse worktree's omitted trees; never stack a PR on a
non-main base here; and never run a branch-delete without checking which repo's remote
you are pointed at — this session closed its own Terminal PR that way and had to
reopen/re-arm it.

## §3 What was decided and found

Ten backfilled decisions and two discoveries, keys listed in the frontmatter
`decisions:`/`discoveries:` fields. All are backfills/verifications of standing law
rather than new rulings — no live adjudication was taken this session.

## §4 Not in scope — do not adopt

No Phase 4 automation (hooks, reminders, auto-capture), no claim/release tooling, no
task store, no scheduler, no second store in any repo, and no synthetic handoffs to
close the numeric gate. Each is either sequenced later by the plan or permanently
ruled out by the decisions cited in `do_not_redo`.
