---
key: CLAUDE-CONCRETE-HOST-IDENTITY-SEAM-MISSING
claim: >
  Current Operator Continuity archaeology has not proven a canonical concrete host identity seam
  suitable for native Claude realm acceptance. Macro `mastermind.provider_capacity.v1` currently
  projects `host_ref=local-unbound`, and current Mastermind archaeology found no general concrete
  `host_ref` owner in the Executive control plane. OCR-1 must therefore fail closed at the real
  realm census rather than manufacture a host ID from hostname, machine UUID, UID, user or path.
falsifier: >
  Current accepted Capacity Fabric / Executive host preparation lands and proves an opaque reviewed
  concrete `host_ref` that OCR-1 can consume without inventing a second host registry, then the
  exact Worker principal can bind a native Claude preflight receipt to that host.
so_what: >
  The OCR-1 V3 contract/preflight can be implemented now, but a real realm cannot become
  production-accepted merely from Claude login or OS-principal evidence while the host identity
  remains `local-unbound`/unproven. Preserve the H0 -> P0 -> CF2-I owner sequence.
kind: runtime
verified_at: 2026-08-27
verified_by: >
  Read-only current-source archaeology of Macro engine/provider_capacity.py and Mastermind
  Executive/Capacity source code after Mastermind #181 merge.
scope:
  - WS:EXECUTIVE-CAPACITY-FABRIC
  - Operator Continuity OCR-1
confidence: verified
---

## Consequence

OCR-1 V3 should implement and test the explicit refusal vocabulary now. The real host census stops
truthfully at `HOST_IDENTITY_SEAM_UNAVAILABLE` until the canonical owner supplies concrete host
identity. This is a predecessor gate, not permission to create `operator_hosts.json`, a hostname
hash or another device registry.
