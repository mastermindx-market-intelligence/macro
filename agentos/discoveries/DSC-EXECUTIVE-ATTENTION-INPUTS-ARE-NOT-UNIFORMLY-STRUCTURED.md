---
key: EXECUTIVE-ATTENTION-INPUTS-ARE-NOT-UNIFORMLY-STRUCTURED
claim: >
  Current Agent OS/portfolio sources contain rich durable state but do not uniformly publish every
  cognition-allocation input as a structured field. In particular, current workstreams often place
  decision verbs, Sol commissioning language, placement waits, deadlines or blocker context in
  `next_action` prose; the schema supports `needs_ceo` and typed waits, but observed live workstream
  usage is sparse. Therefore an Executive Attention Frontier that semantically scans Markdown would
  manufacture demand/authority/urgency/waits and become an opaque second organizational truth plane.
falsifier: >
  Falsify this by proving current protected canonical source owners already provide complete,
  structured, source-attributed attention admission, authority, ready time, deadline, wait,
  actual-impact, autonomy and service-feasibility facts for every live executive demand family.
  Schema capability alone is insufficient; the fields must be populated in the real portfolio.
  The counterexample must identify an exact protected source/PR against the F0B/F0G baseline in
  Mastermind PR #275 and re-run `python3 scripts/agentos.py validate`; a discovery correction must
  supersede this record rather than silently erase the observed input gap.
so_what: >
  A1 must be strict about demand/authority identity and permissive about optional factors being
  UNKNOWN. Start only from explicit Wake/Executive Inbox/turn-owner/structured decision facts;
  never NLP-extract authoritative factor values from workstream prose. A2 shadow must report input
  coverage/degraded-state rates alongside interruption reduction so a falsely calm frontier cannot
  hide incomplete upstream admission.
kind: architecture
verified_at: 2026-09-02
verified_by: >
  Manual F0B canary against Macro main ede7e065a90b294e9835e98e5326a84e1c14d038 including
  WS-BREATHING-PLATFORM, WS-ACCOUNT-IDENTITY-HARDENING and WS-ADVANCED-DATA-OPTIONS; current Agent OS
  schema/code search for `needs_ceo`, typed wait/review_after; current Wake/turn-owner/Steward owner
  archaeology; Mastermind PR #275 and its protected F0B/F0G real-portfolio source contracts; current
  continuation reconciliation at Macro main 4fe6434d7bc2422f0cf3baceee0566faef72ac93; and
  `python3 scripts/agentos.py validate` as the record-contract verification command.
scope:
  - WS:EXECUTIVE-ATTENTION-ECONOMICS
  - agentos/workstreams/**
  - mastermind:control_plane
  - mastermind:research/MASTERMIND_EXECUTIVE_ATTENTION_ECONOMICS_F0B_PORTFOLIO_CANARY_AND_INPUT_GAPS_2026-08-30.md
confidence: verified
---

## Representative real cases

### Breathing Platform

`owner: ceo-sol` coexists with an explicit placement debt described as `WAITING_CAPACITY /
needs_placement`. The durable accountable owner is not the same thing as the current cognition-turn
owner. EAF must consume current placement/turn facts and never turn that debt into routine Chairman
account scheduling.

### Account Identity Hardening

A blocked user-facing workstream says `Decide the CI gate fix` in `next_action` while its owner is
`terminal-platform`. The verb `Decide` and `blast_radius: user_facing` do not establish executive
authority or current user harm.

### Advanced Data Options

The workstream prose says `Sol commissioning of AD-1T2`, while its old options-source `needs_ceo`
gate is explicitly retired and no replacement structured `needs_ceo` block is present in the
observed YAML. EAF must not resurrect a retired gate by semantic text matching.

## Implementation law

The safe V1 may output many `UNKNOWN` factor values. It may not guess them. Missing optional data
makes items less comparable; missing load-bearing identity/authority/effect truth requires
reconciliation. Real shadow evaluation must separately report source coverage so product calmness
cannot be achieved by silently failing to admit valid attention demand.
