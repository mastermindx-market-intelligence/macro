---
key: OPTIONS-RUNTIME-DEPLOYMENT-AND-OUTCOME-PUBLICATION-BLOCKERS
claim: >
  The September10 native trace locates the missing Options Alpha measurements
  before harvesting: com.mastermind.liveflow is bound to the edd07d7 scheduled
  checkout whose live-flow source predates the accepted measured implementation,
  and all570 observed decision events lack its block. Separately, nightly
  run34421411115 produced1709new episodes but publication was rejected for an
  oversized112.71MB session-outcome file and retried12times; campaign accrual
  timed out. A broader exact source-prefix diagnostic found32matches and2outcome
  prefix mismatches. These are distinct observed blockers, not a completed
  deployment, storage repair, full-history corruption claim or profitable model.
falsifier: >
  Reinspect the exact launch binding, source blob89138ed09ad9e16bc0963dcd02dc592d6ca5b231
  and stage SHA2567fcf4764d0694680cfddc4fdb2975f58dab1f16438aa089d8a3a492ea2810ed9;
  reproduce the selected events in run34421411115/job102735434183; and recompute
  the34 source-prefix commitments at Macro6e1fb2ab35f68bbaf3695ffee5fe5ba268e46bae.
  A contradiction on those identical observations refutes the respective claim.
  Newer deployment/source evidence changes the current action, not this dated
  record. The static hashing-work model is not a measured runtime speedup.
so_what: >
  Do not rerun the initial census or implement another collector. The existing
  deployment owner must qualify adoption of accepted6585 on the actual scheduled
  checkout and prove natural new-session measurements through existing consumers.
  Existing source owners must reconcile the two outcome-prefix disagreements
  before campaign advancement. Remove repeated immutable-byte hashing without
  weakening semantic checks; stop retrying deterministic size rejection and
  review a scalable physical layout under the same logical publication owner.
  Preserve old records, clocks, source identities, score holds and all OA/DNR gates.
kind: architecture
verified_at: 2026-09-10
verified_by: >
  Sol read-only native binding/source/stage inspection and current GitHub/gh
  workflow evidence. Full identities and expected/observed hashes are recorded
  in research/options_estate/OPTIONS_RUNTIME_ROOT_CAUSES_2026-09-10.md.
  Native aggregate receipt2026-09-10T23:05:09.993099Z has SHA256
  6b6496f459b93bf8f389f3fc0b7b55311f3d2991547a0703666c3381ff584d60.
  One-pass current-source prefix checks and independent direct-prefix hashing
  agree on both mismatches; canonical source re-encoding has zero differences.
  Eighteen original read-only diagnostic tests passed in the sandbox.
scope:
  - macro
  - terminal
  - options-intelligence
  - options-alpha
  - "engine/options_signal_campaign.py"
  - "scripts/ci/options_signal_nightly.sh"
  - "data/options_signal_episode/*"
  - "data/options_signal_campaign/*"
  - "research/options_estate/*"
confidence: verified
---

Parent: WS:OPTIONS-ALPHA-INTELLIGENCE-RECOVERY. Same research carrier: Macro #7027.

## Supersession and precise limits

The prior actual-census discovery remains valid for its pinned data. Its initial
unknown-boundary status is narrowed by this observed scheduled-source gap; the
correct next action is controlled source adoption, not another generic trace.
The previously verified8872episode prefix remains valid. It did not certify the
H+60 or session-outcome sources; those are the two new mismatches. Do not rewrite
the earlier truthful narrow result or promote it to whole-checkpoint integrity.

Scheduled checkout: edd07d7324534c81341ebcc4683173714c3b8509. Accepted measured
merge: dbd654edb0fb47449b969b7dcb4fbafc2e0fe3ef in #6585. The after-hours read does
not attest a current running process's imported modules. The matching local
publication receipt is not an independent fresh remote-object fetch. Relevant
tracked source and September10stage bytes remained unchanged at closeout.

The570events have median print-to-durable age336.423454seconds andp95
1679.30462485seconds. This is not quote age, subscriber latency, a causal CPU/fsync
profile or a universal stale threshold. Preserve task-specific entry viability.

Nightly campaign/publisher code matches across observed engine checkout2c9a2d35,
workflow-trigger61cb600c and analytical6e1fb2ab at blobs b61ea1d1 and f06f1eac.
The final integrity gate correctly failed; a separate green DST-paired run had
its engine skipped. Neither fact grants accepted accrual or production proof.

## Unresolved integrity and resource boundary

The current H+60prefix6525 and sessionprefix23771 disagree with their recorded
expected hashes; exact values are in the report. The first historical divergence
is not established. A bounded historical-source read was platform-blocked and
not rerouted. Expected hashes cannot simply be replaced to make a checkpoint pass.

The source's repeated hashing of36808historical references across34distinct
prefixes implies large avoidable byte work in a complete traversal. No full-run
wall-clock speedup is measured. Early cheap prefix refusal and invocation-local
digest reuse must retain every accepted-byte and semantic invariant, including
refusal of these two actual mismatches.

## Verification and closeout

The downloadable helper is a read-only offline diagnostic, not a production
validator or new data plane. Its18tests include a synthetic Git CLI with unchanged
HEAD/tree. Equivalent in-memory prefix logic ran on the actual source bytes;
the packaged CLI did not. A native transfer append was blocked after its first
chunk and was not retried or executed. The partial file is isolated outside the
checkout. No denied request was moved to an alternate carrier.

The own Mini analysis REPL exited0; its last identity check confirmed unchanged
source/stage and the original receipt digest. No deployment, canonical-file
mutation, provider call, workflow rerun/cancel, worker commission, model fit or
trade occurred. Repository-wide AgentOS validation, independent release review,
hosted CI and product acceptance are not claimed. Keep this record carrier draft.

## Exact continuation

Existing deployment/source ownership qualifies controlled6585adoption with rollback,
sole-writer preservation and one natural measured-event-to-consumer proof. In
parallel, the existing campaign/publication owners can investigate the exact
prefix disagreement and prepare disjoint bounded validation/storage repairs.
A size-preflight repair avoids pointless retries but does not itself publish the
backlog. An object/manifest migration must preserve the current logical owner,
ordered historical bytes, prefix commitments and real consumer restore behavior.

After the relevant gates clear, the first visible capability is an honest
measured campaign and conditional plan in the existing Terminal view. AD-1T2,
OA-1C/OA-3/OA-4/OA-5 and current DNR/statistical authority remain independent.
