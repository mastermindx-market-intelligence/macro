---
key: OPTIONS-WORKBENCH-R0-REPLAY-GEOMETRY-FAILURES
claim: >
  Terminal #608 now corrects the reproduced price-band and shared-replay defects
  on its original carrier. The candidate passes the full Vitest suite and twelve
  real-route fixture browser journeys; broader responsive verification, independent
  review and production acceptance remain separate unfinished gates.
falsifier: >
  At Terminal a8192b80fb21dfc3a5e0ebe4894650aeb8ceaf41, npx vitest run
  lib/__tests__/surfaceReplayMounted.test.tsx fails, or the committed Playwright
  cases in Terminal #608 do not exercise the actual SurfaceView, SurfacePane and
  replay consumers under the documented synthetic inputs.
so_what: >
  Resume the existing #608 verification and review boundary, not the old tool
  blocker or another competitor teardown. Preserve the renderer and gamma fixes,
  consume the actual full-responsive process result, and never convert local
  fixture success into production freshness, dealer-position or parity claims.
kind: runtime
verified_at: 2026-09-17
verified_by: >
  mastermind-terminal #608 at a8192b80fb21dfc3a5e0ebe4894650aeb8ceaf41;
  terminal:docs/evidence/options-workbench-r0-20260917/replay-integration/verification.json;
  full Vitest 5546 passed, TypeScript exit 0, twelve actual /options browser cases
  passed in EN/ZH at desktop/tablet/mobile with zero retries.
scope:
  - terminal
  - options-intelligence
  - terminal:terminal/components/surface
  - terminal:terminal/lib/heatSeries.ts
confidence: verified
---

## Mission and authority

Chairman commissioned Quanted-level feature, interaction, reliability and visual
quality in the existing Options Workbench recovery and repeatedly authorized
continuation, including Desktop Commander execution. Parent: Terminal #603.
No new program, pricing kernel, collector, replay store, control plane or runtime
worker is created. Preserve #591/#592, #598/#599 and Macro #6604.

Carrier: `claude/options-workbench-r0-replay-20260917-sol-001`.
Current Terminal head: `a8192b80fb21dfc3a5e0ebe4894650aeb8ceaf41`.
Base: `75c22083249e7a1529be3d6baf819b9ad5ea509f`.
Integration commit: `7799380dfcf88f50eff0824121cc0f8141a86a62`.
Skillpack: `320f586126b7c82c843ef17612f12d40d20a42e0`.
State: BUILT_NOT_PROVEN; draft, not merged/deployed, not accepted full parity.

## Preserved history and supersession

Original reproducer `2699f9e6b20fec922c779b2427156206c973de79` changed no production
source and recorded 86 passing/14 failing cases plus six failing browser journeys.
The earlier `bd578978746fac3ce8974c9a769484f5486d855f` corrected actual price-band
geometry, awaited cache refresh and cursor helpers, but retained five failing
mounted integration tests. Those original red receipts remain intact.

The former provider/view/pane tool-refusal state is now superseded by actual
same-carrier edits, readbacks, commits and tests, not by an assumed permission
setting. Desktop Commander completed the integration in the existing worktree;
no tool/worker failover, hidden source effect, permission or account change is
claimed. Do not resume by redoing the already completed edit.

## Integrated user capability

One existing ReplayProvider now admits the index for its pane group and refreshes
the rolling alias every 60 seconds after the previous read settles. Layout-only
single/quad changes preserve its selected time. Explicit pause at the latest
stored frame remains paused when newer frames appear. Earlier index insertion
preserves the selected timestamp; removal with no surviving preceding observation
shows unavailable instead of silently displaying later information.

Input admission rejects wrong roots, mismatched archive dates, malformed/unordered
or duplicate stamps and inconsistent latest metadata. Obsolete/unmounted reads
cannot replace the current context. Failed index refreshes retain stored frames
with a visible bilingual error; latest-loaded is labeled LATEST STORED, not LIVE.
Current-head revisions refresh the field and candles even at the same HHMM key.
Rendered frames are bound to both selected stamp and accepted session. The old
session cannot wear the new date while a replacement is pending. Archive reads
remain dated, and same-root clicks do not delete the session picker.

Candle fetching is session/revision scoped rather than repeating on each seek.
Ordinary candle growth preserves the user's price framing. Missing Gamma stays
Gamma/unavailable instead of silently switching to premium flow. The renderer's
accepted price-coordinate repair remains unchanged.

An extended real-browser run exposed an empty-time-scale `Value is null` route
crash. The actual library precondition was traced and reproduced by a mounted
consumer test. Framing now waits for real chart time points rather than catching
and hiding an exception or forcing a reload.

## Evidence and limits

Full Vitest: 336 files passed, 5,546 tests passed, zero failed, four pre-existing
todo. TypeScript exits zero. Twelve actual /options browser journeys pass in
EN/ZH at 1440x900, 820x1180 and 390x844, one worker, zero retries and a cold owned
fixture server. They cover growth, pause, layout, failure/recovery, archive
isolation and numeric price-band alignment. Thirty-six additional synthetic
captures and their SHA-256 digests are committed. The mounted consumer family
now contains twenty-one cases. Original geometry discrimination is preserved.

These are local source/fixture results, not observed-dealer, profitability,
market-freshness or production proof. Full as-observed history for every sibling
pane and aggregated candle is not established by this slice. Conditional future
gamma/charm/vanna fields, the expiry clock, adaptive profile resolution and richer
workbench composition remain unfinished parent work.

## Active verification and return

The full responsive suite was actually STARTED via Desktop Commander on device
`3f5ce987-e3eb-40a3-af9f-4b0ae54919cc`, process PID `32987`, in the same Terminal
worktree. Its result is `r0-responsive-full.log` at that worktree root.
Command: `CI=1 TERMINAL_E2E_PORT=33764 node_modules/.bin/playwright test --workers=1 --retries=0 --reporter=list`.
The run was still executing at this checkpoint; no complete-suite pass is claimed.
Read the real process result and compare source hashes before further source edits.
No autonomous reasoning worker, assigned reviewer or automatic production release
is asserted by this process receipt.

Independent review brief is in #608. Preferred avenue Terra; bounded engineering
review does not require scarce Fable principal capacity. Placement remains
WAITING_CAPACITY / needs_placement with no receiver-specific watcher or open-pickup
commission. The interactive tool surface lacks an exposed Executive submit action;
a directory search returned no matching Executive plugin. This does not establish
that the entire Fabric is down and does not authorize a provider-spawn workaround.

## Adjacent accepted work and next action

Macro #7271 at `4bb58c0eb4566a21e3cb3d2b1bdd2a6a2cf962c5` remains the separate
local gamma-sign correction, with 333 selected engine/state/hub/matrix cases
passing. Its independent numerical review, CI and production proof remain distinct.
Do not rebuild it. The source-semantics COMMENT review 5242156602 on Terminal #592
is not a formal approval or production acceptance; preserve its original owner.

Consume the existing responsive run, resolve any attributable failures without
weakening checks, obtain independent exact-head review/current integration proof,
then use the existing release owner and real-session browser acceptance. No merge
or deployment before those gates. Keep the real category/view navigation; the
initial description of sixteen peer top-level tabs was incorrect. Continue from
verified effects, not another audit, replacement branch or duplicate replay system.
