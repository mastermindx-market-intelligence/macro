---
key: NATIVE-HARNESS-CAPABILITY-IS-NOT-COO-ADAPTER-PARITY
claim: >
  At Mastermind bffe2ca8506346ea278c8ee469bf1ac30a4de008, the existing rich Operator Harness
  contract and installed Codex/Grok/Cursor native interfaces expose a path to multi-harness
  COO operation, but narrow capability profiles, sealed worker adapters and unclosed
  result/placement/runtime gates prevent native feature availability from proving COO parity.
falsifier: >
  Read the current protected operator_harness_contract.py, codex_operator_adapter.py,
  executive_agent_capabilities.json and provider adapters, then obtain an owner-issued
  effective-profile/runtime binding and a real managed non-Claude parent -> admitted child ->
  independent review/repair -> idle wake -> exact parent consumption -> useful accepted artifact
  receipt with no Chairman or Fable message relay; this disproves the remaining-parity gap for
  that exact qualified profile, not for every provider or realm.
so_what: >
  Extend the existing OperatorHarnessAdapter and capability/context owners for rich COOs while
  preserving sealed WorkerExecutionAdapter paths; qualify Claude Code, Codex App Server and one
  reusable ACP client with separate provider manifests, and consume the incumbent fabric repairs
  rather than creating another watcher, session registry, queue, transcript store or principal.
kind: architecture
verified_at: 2026-09-14
verified_by: >
  Protected Mastermind bffe2ca8506346ea278c8ee469bf1ac30a4de008 source reads; network-denied
  empty-home initialize-only Studio probes for Codex 0.154.0, Grok 1.0.30 and Cursor agent
  2026.09.10-fd3934a; installed Codex schema generation; current #630 and exact integration
  thread reads; research/evidence in Mastermind #637 at 57ae71e470227c216bd7bb462b5d08560a0029c3.
scope:
  - WS:EXECUTIVE-CAPACITY-FABRIC
confidence: verified
---

## Evidence and scope

Mastermind #637 contains the integration research and transcribed native probe evidence at exact
head `57ae71e470227c216bd7bb462b5d08560a0029c3`:

- `research/ORCHESTRATOR_NATIVE_HARNESS_PARITY_2026-09-14.md`, blob
  `6474d879c91d31b21e7e4c2d0a1883e9b38b090b`;
- `research/ORCHESTRATOR_NATIVE_HARNESS_PROBES_2026-09-14.json`, blob
  `ad8e6d056a790c063fca2db65af2ec292c8d4a17`.

That research PR is Draft/Hold and production-inert. It records observations and proposed adoption,
not an approved runtime generation. Its 48-case adversarial catalogue is entirely NOT_RUN.

Only native `initialize` was sent in the isolated protocol probes. No authentication, session
creation/load, prompt, model turn, provider enrollment or live worker mutation occurred. Native
advertisement is not execution proof. Codex generated 305 schema files / 3,492,670 bytes, but its
empty-home defaults are not the effective settings of a governed operator.

## Load-bearing implementation findings

The current Codex adapter already owns exact current-writer attention delivery and resume.
Native steering, approval, fork and other capabilities cannot be assumed exposed merely because
the installed App Server describes them. Published capability profiles largely remain deliberately
narrow canaries; the helper proof permits one read-only child, depth one, for 60 seconds. Richer
package fixtures exist but do not establish enrolled operating parity.

Claude subscription and ACP worker paths are intentionally sealed/bounded. They should not become
unrestricted by deleting security fences. Rich operation belongs through existing OHF interfaces.
The local pool wrapper is a separate interim launch path: it uses bounded Claude print mode for
GLM and an older Cursor executable than the successfully initialized ACP `agent` binary.

The installed Grok initialization advertises auth method `grok.com`, while the protected preflight
expects `cached_token`. This is a version-contract mismatch to reconcile before authentication,
not a failed account proof. Provider-specific native callback, privacy, helper, resume and cleanup
behavior require explicit qualification.

## Existing owners and unresolved live gates

Preserve incumbent integration operation
`agent-fabric-end-to-end-fable-integration-20260913-sol-001` on exact Slack root
`C0BSBM78V1N/1789324397.992989` and its current action target. #630 remains the heterogeneous
core/step-placement source carrier; the consultation integrity repair and #623/W7 have their own
current writers. #632 role material is not enrolled runtime capability. This discovery originates
no new child, watcher or replacement principal and changes no existing workstream status.

A plan may narrow, not widen, the host-admitted child provider envelope. Actual provider realm,
usage-policy, Capacity and runtime admission remain prerequisites for a live mixed-provider pair.
The research session's Executive read returned HTTP 401. The separate current discovery
`DSC-ASTRA-FABRIC-LIVE-CLIENT-AND-SUBSCRIPTION-GATES` records DCR EFFECT_UNKNOWN and subscription
worker enrollment issues; neither is bypassed or repeated here.

## Exact next action

The incumbent integration owner consumes #637, closes the existing return-consumption dependency
on its original carrier, and executes the admitted managed Codex reference journey without Fable
in the execution path. Rich Claude and ACP source/protocol qualification can be bounded separately.
Promote only the exact model+harness+version+realm+profile actually proven, not the entire vendor.
