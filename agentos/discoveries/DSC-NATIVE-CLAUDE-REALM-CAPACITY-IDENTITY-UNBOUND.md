---
key: NATIVE-CLAUDE-REALM-CAPACITY-IDENTITY-UNBOUND
claim: >
  A native Executive Claude realm such as `claude-pro-01` is not currently proven to be the same
  paid-subscription capacity identity as any Macro `claude_code_oauth_*` capability. Ordinal/name,
  host, reset time, plan type, usage percentage, Slack principal or operator assertion cannot make
  that join canonical. Native realm auth readiness therefore cannot yet authorize automatic
  Capacity Fabric ranking/claim as a five-account Claude pool.
falsifier: >
  OCR-2C proves a provider-supported secret-free same-subscription equality witness with
  rotation/re-enrollment invalidation for a native realm and existing Shared AI Provider Control
  capability; or, after Family-A refusal and separate architecture approval, a versioned Shared AI
  Provider Control contract natively observes those Claude realms and Mastermind consumes it.
so_what: >
  OCR-1 may prove realm/auth readiness, but OCR-5/OCR-8 must not automatically select/count a native
  Claude realm until canonical Provider Control capacity identity exists. Do not map
  `claude-pro-N` to `claude_code_oauth_N` by ordinal and do not widen the current H0 v1 source
  contract in place.
kind: architecture
verified_at: 2026-08-27
verified_by: >
  Mastermind PR #181 / merge b901dee0272a99b8a1d60385848b99b7273e8261; current Macro
  provider_capacity.v1 / key-pool ownership; current Mastermind CF2 source contract.
scope:
  - WS:EXECUTIVE-CAPACITY-FABRIC
  - Operator Continuity
confidence: verified
---

## Evidence and boundary

Macro Shared AI Provider Control already owns Claude OAuth capacity observations. Mastermind's
current accepted H0/CF2 source acquisition is separately frozen and currently enumerates the
existing reviewed capacity identities; it is not a generic native-Claude account mapper.

The safe sequence is:

```text
OCR-1 native realm/auth evidence
-> OCR-2C Family-A identity falsifier
-> rotation-safe join proven OR separately frozen Provider Control version evolution
-> only then automatic native-Claude capacity claim
```

Auth readiness, provider presence and canonical capacity identity remain separate facts.
