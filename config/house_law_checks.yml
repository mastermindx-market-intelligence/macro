schema: house_law_checks.v1
updated: "2026-08-16"

# Canonical registry of every house law and its enforcement status.
# Maintained by scripts/check_house_law_registry.py (meta-guard).
# To add a new guard: (1) create scripts/check_<slug>.py, (2) add an entry here,
# (3) wire the new ci.yml / pages.yml step. The meta-guard blocks merges of
# unregistered check_*.py files. Severity downgrades or entry removals require
# an operator/Fable ruling recorded in the PR.

checks:

  # ── UI / rendering guards ──────────────────────────────────────────────────

  - law_id: ui.crypto_shelf_governor
    summary: >
      The Bitcoin Vector and Crypto Cockpit Tier-1 spines are closed sets.
      Once a surface carries its first data-shelf marker, vector must expose
      exactly S1-S6 plus one data-verdict marker and crypto must expose exactly
      H1-H8. W0 deliberately leaves legacy templates unarmed; W1 and W2 arm
      their respective surface by shipping the complete marker set.
    source_ref:
      - scripts/check_crypto_shelves.py
      - mockups/refs/crypto_cockpit/README.md
    severity: hard
    check_script: scripts/check_crypto_shelves.py
    ci_wiring:
      - workflow: .github/workflows/ci.yml
        job: crypto-shelves
        lane: pr_ci
    selftest: true
    allowlist: null
    ratchet: null
    known_limits:
      - Source-level marker counting cannot prove that CSS or client code leaves every shelf visible.
      - Content nested inside the final details shelf is intentionally outside the Tier-1 count.
    owner_program: crypto-cockpit-masterplan
    notes: >
      The unmarked legacy templates pass with a notice during W0. Partial migration
      is fail-closed: the first marker arms the exact-set and one-verdict checks.

  - law_id: ui.hub_a11y
    summary: >
      The landing hub (and any globe-deck page) must keep its accessibility
      contract: no focusable elements inside aria-hidden subtrees, the <head>
      boot sets the document language, the light-theme pill contrast override
      is present, viewport-fit=cover + safe-area insets ship, and theme.js
      syncs documentElement.lang. Locks in the 2026-07-10 hub a11y fixes
      (PR #2096/#2104 review findings).
    source_ref:
      - scripts/check_hub_a11y.py
      - research/hub_ux_program.md
      - "PR #2195"
    severity: hard
    owner_program: hub-ux-program
    check_script: scripts/check_hub_a11y.py
    ci_wiring:
      - workflow: .github/workflows/ci.yml
        job: hub-a11y
        lane: pr_ci
    selftest: false
    selftest_exempt_reason: >
      Pure-stdlib static checker; functional coverage provided by
      tests/test_check_hub_a11y.py (34 cases incl. violating fixtures).

  - law_id: ui.inline_js_parses
    summary: >
      Every inline <script> block AND quoted on*= handler attribute in committed
      site HTML must be syntactically valid JavaScript (handlers node-checked as
      function bodies, mirroring browser handler compilation). Smart-quote
      contamination (U+2018/U+2019/U+201C/U+201D as JS delimiters — in <script>
      code position or anywhere in a raw on*= value) is a hard error, including
      in templates/*.j2 (Jinja-stripped). A script syntax error blanks the page
      silently (IIFE aborts); a handler error throws on every click.
    source_ref:
      - scripts/check_inline_js.py
      - "PRs #152 #155 #2321 #2409"
    severity: hard
    check_script: scripts/check_inline_js.py
    ci_wiring:
      - workflow: .github/workflows/ci.yml
        job: inline-js
        lane: pr_ci
      - workflow: .github/workflows/render.yml
        job: render
        lane: render
      - workflow: .github/workflows/engine-render.yml
        job: engine-render
        lane: render
    selftest: true
    allowlist: null
    ratchet: null
    known_limits:
      - Scans committed site/; does not cover dynamically generated fragments
        injected by JavaScript at runtime.
      - .j2 templates get the smart-quote scan only — node cannot parse Jinja,
        so plain-JS syntax errors in a .j2 still surface at the next render.
      - Handlers in unquoted attribute syntax (onclick=f()) are not extracted
        (none in the tree; quoted is the house style).
      - Curly quotes INSIDE JS string literals are legal (bilingual display
        copy); only code-position/raw-attribute hits are contamination.
    owner_program: engine-fix-masterplan
    notes: null

  - law_id: ui.site_asset_refs_resolve
    summary: >
      Every template-decided href/src in committed site HTML must resolve to a
      file in the tree. A page that links a missing asset or page is a silent
      404 — the HTML is well-formed and the render succeeds, so the break shows
      up only in production (#3988 and #4042 pruned a stylesheet a sibling page
      still linked; templates/ticker.html.j2 emitted the PLURAL ../baskets/
      directory for pages shipping under site/basket/, dead on 605 pages and 909
      links since the directory was named). Links to stocks/<TICKER>.html warn
      instead of failing: which ticker pages exist is decided nightly by data, so
      hard-gating peer/movers/crypto naming would wedge the render on ordinary
      universe churn.
    source_ref:
      - scripts/check_site_asset_refs.py
      - "PRs #3988 #4042 #4056"
    severity: hard
    check_script: scripts/check_site_asset_refs.py
    ci_wiring:
      - workflow: .github/workflows/render.yml
        job: render
        lane: render
      - workflow: .github/workflows/engine-render.yml
        job: engine-render
        lane: render
      - workflow: .github/workflows/closing-bell.yml
        job: closingbell
        lane: scheduled
      - workflow: .github/workflows/earlyclose.yml
        job: earlyclose
        lane: scheduled
    selftest: true
    allowlist: null
    ratchet: null
    known_limits:
      - Proves file EXISTENCE, not markup validity. Values carrying ${, {{, <, }
        or whitespace are template/JS fragments rather than URLs and are skipped,
        so a genuinely broken href built by JS at runtime is invisible here.
      - Scans committed site/ only; a link a page builds client-side from a JSON
        payload is not covered.
      - /api/* and /ws/* are served by the FastAPI app, not the file tree, so
        they are exempt by prefix rather than checked.
    owner_program: engine-fix-masterplan
    notes: >
      _KNOWN_GAPS is the escape hatch and is currently EMPTY — the six gaps the
      guard shipped with each got their own emitter fix, and
      tests/test_check_site_asset_refs.py ratchets the empty state so a re-pin has
      to be argued in review rather than riding in with a fresh 404.

  - law_id: ui.government_revenue_projection_integrity
    summary: >
      The public Government Revenue surface must be one governed projection of
      committed canonical evidence: canonical and public JSON twins are
      byte-identical, schemas and the content-derived workspace bundle are valid,
      the compact HTML shell is semantically equal to the canonical display
      payload, required workbench markers remain present, and first-paint HTML
      stays inside its raw-byte budget. This prevents a long-running generic
      renderer from rebasing an older full payload over newer procurement truth.
    source_ref:
      - scripts/check_government_revenue_projection.py
      - tests/test_check_government_revenue_projection.py
      - "PR #4223"
    severity: hard
    check_script: scripts/check_government_revenue_projection.py
    ci_wiring:
      - workflow: .github/workflows/render.yml
        job: render
        lane: render
      - workflow: .github/workflows/engine-render.yml
        job: engine-render
        lane: render
    selftest: false
    selftest_exempt_reason: >
      Pure read-only projection checker with focused tamper fixtures covering
      stale twins, malformed matching schemas, oversized or missing shells,
      bundle mismatch, semantic company-shell drift, workflow commit/push
      placement, and metadata-replay invalidation.
    allowlist: null
    ratchet: null
    known_limits:
      - Proves committed serialization and first-paint integrity, not browser
        interactivity after hydration or authorization middleware behavior.
      - Exact issuer attribution and award-event truth are governed separately;
        this fence prevents projection drift but does not create source evidence.
    owner_program: government-revenue-foresight
    notes: >
      Both render lanes repeat the checker after their final heals and immediately
      before each commit or push. The render metadata shortcut is disabled when
      canonical procurement data or any projection-writing helper changed since
      the render parent.

  - law_id: ui.site_js_parses
    summary: >
      Every standalone site/*.js bundle (loaded via <script src=...>) must parse
      without syntax errors. Smart-quote delimiters once blanked markets.html for
      ~2 days (PR #1004 → #1353).
    source_ref:
      - scripts/check_site_js.py
      - "PR #1353"
    severity: hard
    check_script: scripts/check_site_js.py
    ci_wiring:
      - workflow: .github/workflows/ci.yml
        job: inline-js
        lane: pr_ci
    selftest: false
    selftest_exempt_reason: >
      Thin node --check wrapper; covered by the same CI job as ui.inline_js_parses.
    allowlist: null
    ratchet: null
    known_limits:
      - Skips vendor/CDN bundles that are not committed to site/.
    owner_program: engine-fix-masterplan
    notes: null

  - law_id: ui.admin_js_no_undef
    summary: >
      Every bare identifier in admin/static/*.js must resolve to something in
      scope. app.js is a flat top-level browser script, so a `let` declared in
      one function and read from another parses clean, reviews clean, and throws
      a ReferenceError only when the code path runs. PR #3693 shipped exactly
      that ("rejBox is not defined") and the admin Outbox panel rendered BLANK in
      production for ~2 days until #3871 module-scoped it as OBX_REJ. node
      --check cannot see it — an undefined identifier is valid syntax — so this
      runs eslint with a single rule, no-undef.
    source_ref:
      - scripts/check_admin_js.py
      - "PRs #3693 #3871"
    severity: hard
    check_script: scripts/check_admin_js.py
    ci_wiring:
      - workflow: .github/workflows/ci.yml
        job: admin-js-no-undef
        lane: pr_ci
    selftest: false
    selftest_exempt_reason: >
      Thin eslint wrapper with no logic of its own to verify; the detection
      guarantee is covered by tests/test_admin_js_no_undef.py, which plants the
      #3693 defect shape in a temp file and asserts the guard flags it.
    allowlist: null
    ratchet: null
    known_limits:
      - Only the no-undef rule runs. Typos that resolve to a real global
        (e.g. `name`, `status`, `length` on window) are legal references and
        stay invisible.
      - Assumes admin/static/index.html keeps injecting no extra globals; a
        future vendor <script> would need its names added to the eslint env.
      - Needs node + npx on the runner. The pytest lane fails RED rather than
        skipping when the tooling is missing under CI, so an unrunnable guard
        can never read green.
    owner_program: engine-fix-masterplan
    notes: null

  - law_id: ui.nav_gap
    summary: >
      Every page's shared nav menu must maintain a top gap (body top padding).
      A page that zeroes body padding jams the menu against the viewport top.
    source_ref:
      - scripts/check_nav_gap.py
      - "PR #471"
    severity: hard
    check_script: scripts/check_nav_gap.py
    ci_wiring:
      - workflow: .github/workflows/ci.yml
        job: nav-gap
        lane: pr_ci
    selftest: false
    selftest_exempt_reason: >
      Covered by tests/test_check_nav_gap.py.
    allowlist: null
    ratchet: null
    known_limits:
      - Scans committed site/ HTML; does not cover pages generated only at runtime.
    owner_program: engine-fix-masterplan
    notes: null

  - law_id: ui.nav_mega
    summary: >
      Every page that renders the shared nav must carry the Research mega-menu.
      An out-of-loop or hand-static page can freeze the nav without the mega-menu.
    source_ref:
      - scripts/check_nav_mega.py
    severity: hard
    check_script: scripts/check_nav_mega.py
    ci_wiring:
      - workflow: .github/workflows/ci.yml
        job: nav-mega
        lane: pr_ci
    selftest: false
    selftest_exempt_reason: >
      No dedicated selftest; the check is deterministic over committed site HTML.
    allowlist: null
    ratchet: null
    known_limits:
      - Only catches pages already committed to site/; new pages built outside the
        render loop are not caught until they land in site/.
    owner_program: engine-fix-masterplan
    notes: null

  - law_id: ui.title_i18n
    summary: >
      Translated (CJK / t() / zh-interpolated) text must never appear inside HTML
      title= attributes, and t()/td() dual-span markup must never appear inside a
      <title> ELEMENT (RCDATA — markup renders as literal browser-tab text, #2705).
      The dual-span l-en/l-zh mechanism cannot operate in either place. Plain CJK
      inside a <title> element is allowed — the element rule is markup-in-RCDATA,
      not language. Law source: CLAUDE.md ("no translated text in title= attributes").
    source_ref:
      - scripts/check_title_i18n.py
      - CLAUDE.md
    severity: hard
    check_script: scripts/check_title_i18n.py
    ci_wiring:
      - workflow: .github/workflows/ci.yml
        job: title-i18n
        lane: pr_ci
    selftest: true
    allowlist: null
    ratchet: null
    known_limits:
      - Template <title> element rule is line-scoped (dodges committee.html.j2's
        JS-built SVG title strings), so a bare <title> wrapped across lines with the
        t()/td() call on its own line is not caught; {% block title %} bodies ARE
        region-scanned.
      - Rendered-site rule span-checks the page <title> (first title element) only;
        inline-SVG <title> tooltips deeper in the page are not span-checked.
    owner_program: engine-fix-masterplan
    notes: null

  - law_id: ui.zh_filing_term
    summary: >
      申报 must never appear in user-facing Chinese copy. A native reader hears a
      TAX or CUSTOMS declaration (申报纳税, 海关申报) — a self-report filed under
      legal compulsion — where the platform means a market DISCLOSURE: an SEC
      10-K, a 13F, a congressional STOCK Act report is a 披露. Scans templates/
      line by line plus the string literals of engine/ and scripts/ builders, so
      a wrong label is caught at its source rather than a nightly render later.
      Swept by hand twice before the gate existed (PR #4830 Filing Forensics,
      PR #4855 site-wide).
    source_ref:
      - scripts/check_zh_filing_term.py
      - "PR #4855"
      - "PR #4830"
    severity: hard
    check_script: scripts/check_zh_filing_term.py
    ci_wiring:
      - workflow: .github/workflows/ci.yml
        job: zh-filing-term
        lane: pr_ci
    selftest: true
    allowlist: config/zh_filing_term_allowlist.json
    ratchet:
      date: "2026-09-07"
      action: >-
        Drop the three `pending` entries in config/zh_filing_term_allowlist.json
        (templates/index.html behind PR #4765; templates/fundamental_forensics.js
        and .html.j2 behind PR #4830) once those PRs land. The guard prints a
        ::notice the moment a pending file's count falls below its frozen budget,
        so the cleanup announces itself; this clock catches the case where the
        PRs are abandoned and the exemptions quietly become permanent.
    known_limits:
      - Python COMMENTS and DOCSTRINGS are skipped by construction — developer
        documentation is never rendered, and without the skip, writing the law
        down ("use 披露, never 申报") would break the law wherever it is recorded.
      - Rendered site/ is NOT scanned. A hit there always originates in templates/
        or a builder, both of which are; scanning the render output would red main
        on a stale artifact no PR author can fix by hand.
      - data/ is NOT scanned. Exchange announcements collected upstream use 申报
        as an order-entry term of art; the platform does not author that text.
      - The `pending` ratchet is per-FILE, not per-phrase, so while an in-flight
        PR owns a file the guard only caps its count — it cannot tell a swapped
        occurrence from an original one.
    owner_program: engine-fix-masterplan
    notes: >
      Allowlist entries are phrase-scoped AND file-scoped: an occurrence is exempt
      only inside a licensed phrase's span in a licensed file. The two live
      exemptions are the genuine other senses — 申报失业金 (filing for unemployment
      benefits, engine/release_quirks.py) and 买入申报 (SSE/HKEX order-entry term in
      the hk-connect scraper's title filter). Narrowness is pinned by --selftest and
      by tests/test_zh_filing_term.py::test_allowlist_is_file_scoped.

  - law_id: ui.template_site_sync
    summary: >
      Every plain-copy page asset (a non-.j2 file directly under templates/ that
      also ships as site/<name>) must byte-match its template at every commit.
      2026-07-07 incident: the scope=all engine-render run (110 min, checkout
      9208ef4920) pushed commit 2ae709f9f0 whose site/sector_cycles.js was
      byte-identical to the CHECKOUT-TIME template blob — the push step's
      `git pull --rebase -X theirs` resolved the conflict against #1793's
      de-escalation (merged mid-render) in favor of the stale copy, resurrecting
      "validated" wording and reddening validated-claims on main (fixed in #1799).
      Render lanes self-heal via --fix post-rebase; PR CI + the publish gate keep
      one-sided template/site edits from arming the divergence window.
    source_ref:
      - scripts/check_template_site_sync.py
      - "2026-07-07 incident (commits 2ae709f9f0 → bc81006240 / #1799)"
    severity: hard
    check_script: scripts/check_template_site_sync.py
    ci_wiring:
      - workflow: .github/workflows/ci.yml
        job: template-site-sync
        lane: pr_ci
      - workflow: .github/workflows/render.yml
        job: render
        lane: render
      - workflow: .github/workflows/engine-render.yml
        job: engine-render
        lane: render
      - workflow: .github/workflows/daily.yml
        job: engine
        lane: scheduled
    selftest: true
    allowlist: null
    ratchet: null
    known_limits:
      - Direct children of templates/ only; .j2 render inputs and the
        templates/fonts/ binary copytree are out of scope.
      - Pairs whose site copy does not exist yet are skipped (new asset not
        yet shipped).
      - theme.js is compared against its baked form (Supabase config injected
        by lib.site_assets); in stdlib-only contexts (pages.yml publish) this
        degrades to a token-split prefix/suffix compare.
    owner_program: engine-fix-masterplan
    notes: >
      In the three render lanes the wiring is `--fix` inside the push retry
      loop (post-rebase, pre-push) — a self-heal, not a gate: the lane re-copies
      every pair from post-rebase templates/ and folds the delta into a
      follow-up "render-sync" commit.

  - law_id: ui.interfonts_theme_sync
    summary: >
      The Inter @font-face blocks in templates/_interfonts.html.j2 (the
      self-hosted-font partial included in the <head> of the 16 standalone
      templates that do not link theme.css, PR #2119) must be byte-identical,
      in order, to the @font-face lines at the top of templates/theme.css —
      and exactly six of them must exist. The partial is a manual copy whose
      comment says "keep the two in sync"; without this gate an edit to
      theme.css's @font-face rules (a new weight, a renamed font file) drifts
      silently past the partial and the standalone pages lose the self-hosted
      font again — the exact regression #2119 fixed.
    source_ref:
      - scripts/check_interfonts_sync.py
      - templates/_interfonts.html.j2
      - "PR #2119"
    severity: hard
    check_script: scripts/check_interfonts_sync.py
    ci_wiring:
      - workflow: .github/workflows/ci.yml
        job: template-site-sync
        lane: pr_ci
    selftest: true
    allowlist: null
    ratchet: null
    known_limits:
      - Line-prefix match only — @font-face rules must stay one-per-line at
        column 0 in both files; the exact-count assertion (EXPECTED_COUNT=6
        in the script) catches a reformat that breaks this shape in both
        files at once, where list equality would pass vacuously.
      - Does not verify that the standalone templates actually include the
        partial, nor that site/fonts/ ships the referenced .woff2 files.
    owner_program: engine-fix-masterplan
    notes: >
      Wired as extra steps inside the existing template-site-sync pr_ci job
      (ci.yml already triggers on templates/**). Render lanes never write
      templates/, so a hand-edited PR is the only drift vector — pr_ci is the
      complete enforcement point; no render/publish wiring needed.

  - law_id: ui.font_ui_defined
    summary: >
      Every templates/*.html.j2 whose body-level CSS sets the page font via
      var(--font-ui) (font-family: or the font: shorthand in a body{} rule)
      must either link theme.css (a <link rel="stylesheet"> whose href
      attribute points at theme.css — the href VALUE, never the bare string,
      because CSS comments mention theme.css and defeat naive greps) or define
      --font-ui: locally, and should also include _interfonts.html.j2 so the
      self-hosted Inter actually loads (warning if missing). An undefined
      custom property makes the font-family declaration
      invalid-at-computed-value -> unset, so the ENTIRE page renders in the
      browser default serif (Times) — the #2195 sweep broke 6 standalone page
      families this way, fixed in #2647 (local :root definition +
      _interfonts include, commodities.html.j2 precedent). Partials
      (_*.html.j2) referencing var(--font-ui) are allowed (the token resolves
      at host-page level) and emit a warning-level note only.
    source_ref:
      - scripts/check_font_ui_defined.py
      - "PR #2647"
      - "PR #2195"
    severity: hard
    check_script: scripts/check_font_ui_defined.py
    ci_wiring:
      - workflow: .github/workflows/ci.yml
        job: font-ui-defined
        lane: pr_ci
    selftest: false
    selftest_exempt_reason: >
      Pure-stdlib static checker; functional coverage provided by
      tests/test_check_font_ui_defined.py (44 cases incl. the real pre-#2647
      broken shapes, minified/uppercase CSS, Jinja-in-style-block, and the
      naive-grep theme.css comment trap).
    allowlist: null
    ratchet: null
    known_limits:
      - Textual flat CSS scan (selector{block} realignment; Jinja tags are
        pre-stripped so an interpolation inside a style block cannot void the
        scan) — a body font rule contributed to a host page by an included
        partial is attributed to the partial (warning) rather than the host,
        and Jinja-conditional branches are treated as always-active.
      - Presence-based, not cascade-resolved — a theme.css link with
        media="print" or disabled satisfies the check even though the sheet
        never applies on screen, and a --font-ui definition anywhere in the
        page counts regardless of selector reachability.
      - Checks templates/ only, not rendered site/ copies — render lanes
        regenerate pages from these templates, so the template is the sole
        drift vector.
      - Does not verify that _interfonts.html.j2's referenced .woff2 files
        ship in site/fonts/ (ui.interfonts_theme_sync guards the partial's
        @font-face contents).
    owner_program: engine-fix-masterplan
    notes: >
      var(--font-ui, fallback) usages with an explicit fallback are still
      held to the same rule — the page would silently lose Inter even though
      it would not fall all the way back to Times.

  - law_id: ui.ms_board_coherence
    summary: >
      Every rendered Market State board (site/macro.html + the CN/HK/CA
      dashboards) must be single-snapshot coherent — verdict word, 0-100 score,
      meter tick, thesis, override notes and flip line must all be derivable
      from ONE engine/market_state.py snapshot ("Risk Radar forces Risk-off."
      note => score <= 41 and verdict Risk-off; "Capped at Mixed" => score <= 59,
      never Risk-on; verdict word <=> score band; thesis/flip shape <=> verdict).
      2026-07-07 incident: render commit 5bf116a3b3 raced the 07:10
      engine-render (7a1c30ced4); the push step's `git pull --rebase -X theirs`
      resolved conflict regions toward the replayed stale render, stitching
      upstream's score 56/"Mixed" beside the stale "Risk Radar forces Risk-off."
      note — a hybrid ui.template_site_sync cannot catch (it guards plain-copy
      assets, not rendered pages).
    source_ref:
      - scripts/check_ms_board_coherence.py
      - engine/market_state.py
      - "2026-07-07 incident (commit 5bf116a3b3 = hybrid of 7a1c30ced4 + d944f0d713)"
    severity: hard
    check_script: scripts/check_ms_board_coherence.py
    ci_wiring:
      - workflow: .github/workflows/ci.yml
        job: ms-board-coherence
        lane: pr_ci
      - workflow: .github/workflows/render.yml
        job: render
        lane: render
      - workflow: .github/workflows/engine-render.yml
        job: engine-render
        lane: render
      - workflow: .github/workflows/daily.yml
        job: engine
        lane: scheduled
      - workflow: .github/workflows/asia-close.yml
        job: asia
        lane: scheduled
    selftest: true
    allowlist: null
    ratchet: null
    known_limits:
      - Parses the EN (l-en) spans only — EN/ZH share each <p> source line, so a
        line-granular rebase stitch cannot split the pair.
      - Factor-row tones are not checked against the verdict (a legitimately
        mixed board carries mixed tones).
      - Pages without an ms-verdict section are skipped (not every market ships
        the board).
    owner_program: engine-fix-masterplan
    notes: >
      Render lanes wire it twice: a pre-commit gate (restore-from-HEAD + fail
      RED — the commit step is `if: always()`, so a plain red step would not
      stop the push) and a post-rebase `--heal-from origin/main` inside the push
      retry loop, which restores any incoherent page WHOLESALE from post-rebase
      main — a coherent board at most one render old beats a stitched hybrid;
      the next render re-lands freshness.

  - law_id: design.reference_integrity
    summary: >
      No reference laundering: a proposed design reference may not become law
      until the product/taste judgment happened, in the right order,
      independently, and left receipts. No implicit deletion — every production
      capability receives an explicit disposition, a data-motivated REMOVE is a
      mis-filed BLOCKED_DATA, a WORSE verdict on a critical user task and an
      unwarranted "stronger" authority claim are review-blocking until
      adjudicated, and receipts-or-no-canon: only a reference carrying a valid
      approval receipt may be cited as compliant by a registry row or as
      canonical by a migration packet.
    source_ref:
      - scripts/check_reference_integrity.py
      - tests/test_check_reference_integrity.py
      - research/REFERENCE_INTEGRITY_GATE_V1.md
    severity: hard
    check_script: scripts/check_reference_integrity.py
    ci_wiring:
      - workflow: .github/ci/legacy-jobs.yml
        job: reference-integrity
        lane: pr_ci
    selftest: true
    allowlist: null
    ratchet: null
    known_limits:
      - >-
        CI cannot judge taste and does not try. The gate proves the judgment
        HAPPENED (dual independent critics, rationale quarantine, frozen first
        pass, design-authority verdict packet, approval receipt) and left
        auditable receipts — never that the design is good.
      - >-
        The data-motivated-REMOVE rule (blocked-data-as-removal) is a LEXICAL
        heuristic over the rationale text. Deliberately no allowlist: by RIG §1 a
        removal caused by data insufficiency is definitionally a mis-filed
        BLOCKED_DATA, so a match is enforcement, and a rationale that cannot be
        told apart from a data excuse is not yet a documented product decision.
      - >-
        Sparse agent worktrees under-enumerate L7/L9 locally — mockups/design_system
        and data/ are excluded from most session checkouts, so those roots emit a
        ::notice skip instead of a finding. CI checks out the full tree, where both
        rules are armed; a present-but-missing registry file is a hard error.
      - >-
        Status-scaled by design: draft/in_review artifact sets must only be
        internally valid (mid-flight is legal), and a revise/rejected set keeps its
        upheld findings without failing CI — that record IS the artifact. Full
        L1-L6 arms at status approved and under --evaluate.
    owner_program: design-migration-factory
    notes: >
      Founding regression fixture: research/reference_integrity/prophet-board-5514-original/
      — the original Prophet Board mockup (PR #5514) frozen at 668e5954, with the
      production baseline it should have been judged against. The suite asserts the
      checker independently derives the mechanically-derivable defects from that
      fixture's honest ledgers (five silently dropped capabilities, a coverage-motivated
      chart removal, receiptless removals, four WORSE critical tasks, three unwarranted
      authority claims) and that the critic receipts block approval. `--evaluate <id>`
      runs the full rule set on one artifact set (exit 3 = blocked) for use during
      reference review; the default repo mode is the PR gate.

  # ── Git hygiene ────────────────────────────────────────────────────────────

  - law_id: git.no_conflict_markers
    summary: >
      No committed file may contain literal git conflict markers
      (<<<<<<< / ======= / >>>>>>>). A merge once shipped markers inside
      templates/dashboard.html.j2; Jinja treats them as plain text, so they
      rendered visibly. Selftest proves the guard fires before it is trusted.
    source_ref:
      - scripts/check_conflict_markers.py
      - "2026-07-03 incident"
    severity: hard
    check_script: scripts/check_conflict_markers.py
    ci_wiring:
      - workflow: .github/workflows/ci.yml
        job: conflict-markers
        lane: pr_ci
    selftest: true
    allowlist: null
    ratchet: null
    known_limits:
      - Scans templates/, engine/, scripts/, site/; does not cover binary or
        gitignored files.
    owner_program: engine-fix-masterplan
    notes: >
      Selftest flag is spelled --self-test (hyphenated).

  - law_id: ui.start_runtime_single_instance
    summary: >
      site/start.html must load each page runtime (globe-deck.js, sky.js,
      hub-welcome.js, live_config.js, live.js, wh_banner.js) exactly once.
      2026-08-01 second act: an engine-render whose checkout predated the
      #4163 emitter fix re-rendered start.html with the old builder and pushed
      it post-rebase (adad513bdfe) — doubled live pair, banner rail gone, zero
      conflict markers, so git.no_conflict_markers passed and the page shipped
      to main between PR CI runs. The start-writing lanes gate the page
      directly: violating copies are healed wholesale from a known-good ref
      (HEAD pre-commit, origin/main post-rebase) and the lane only goes red if
      the healed copy still violates.
    source_ref:
      - scripts/check_start_runtime.py
      - tests/test_start_publish_integrity.py
      - "2026-08-01 incident (adad513bdfe)"
    severity: hard
    check_script: scripts/check_start_runtime.py
    ci_wiring:
      - workflow: .github/workflows/render.yml
        job: render
        lane: render
      - workflow: .github/workflows/engine-render.yml
        job: engine-render
        lane: render
      - workflow: .github/workflows/daily.yml
        job: engine
        lane: scheduled
    selftest: true
    allowlist: null
    ratchet: null
    known_limits:
      - Counts <script src> basenames only; cannot see a runtime inlined into
        a <script> body or double-executed by client code.
      - The PR-side twin (tests/test_start_publish_integrity.py) gates pull
        requests; this guard exists because render lanes push to main without
        PR CI.
    owner_program: engine-fix-masterplan
    notes: >
      Selftest flag is spelled --self-test (hyphenated). Lanes wire it twice,
      immediately after each git.no_conflict_markers start.html gate: pre-commit
      with --heal-from HEAD, post-rebase with --heal-from origin/main —
      tests/test_start_publish_integrity.py pins both call sites per lane.

  # ── Epistemics guards ──────────────────────────────────────────────────────

  - law_id: epistemics.qledger_metric_validity
    summary: >
      Three readings of the Universal Scoreboard produce impressive numbers that
      do not mean what they appear to mean, and all three are silent. (V1)
      grades.excess is RAW subject-minus-control return, not direction-signed —
      hit carries direction — so a correct bearish call contributes NEGATIVE
      excess and a pooled signed excess_mean over a mixed-direction family
      measures universe drift, not skill. (V2) a direction==0 claim asserts
      salience, not direction, so hit is undefined and a hit rate over such a
      family is a ratio over an empty verdict set; 71% of the live corpus is
      salience. (V3) grading emits every shorter in-scope horizon as it matures,
      so a family declaring horizon_d=63 accrues 5d/21d rows long before its own
      ruler resolves — reading those as its record is DNR:KILL-OFFHORIZON-VERDICTS.
    source_ref:
      - engine/qledger_validity.py
      - scripts/check_qledger_metric_validity.py
      - tests/test_qledger_validity.py
      - research/MASTERMIND_EVALUATION_STANDARDS.md
      - research/MASTERMIND_INTELLIGENCE_CATALOG.md
    severity: manual
    check_script: scripts/check_qledger_metric_validity.py
    ci_wiring: []
    selftest: true
    allowlist: null
    ratchet: null
    known_limits:
      - >
        Audits the STORE, not the emitters. A consumer that recomputes a pooled
        signed excess in its own code is invisible here; T3 of the V1 plan
        retires the known emitter (scripts/grade_qledger.py per-family excess_mean).
      - >
        The default run is advisory (exit 0); only --strict exits non-zero.
        CI runs the guard's --selftest and its pytest suite (legacy job
        qledger-cluster-honest-ci), which proves the auditor works — it does NOT
        run the audit against the real corpus as a gate. ci_wiring is therefore
        [] on purpose: the guard is unit-proven in CI but not yet enforcing.
      - >
        V3 compares the family's declared ruler against horizons present in the
        grade corpus; it cannot distinguish "not yet matured" from "grading
        broken". Both are correctly reported as zero verdicts at the ruler.
      - >
        An absent store exits 0 with a ::notice. Sparse agent worktrees have no
        data/ on disk while the paths are tracked in HEAD.
    owner_program: qualitative-intelligence
    notes: >
      Deliberately unwired and severity=manual on arrival. The findings are
      PRE-EXISTING CONDITIONS of the corpus (18 findings across 11 families on the
      2026-08-12 store), not regressions any PR author caused; wiring it hard on
      day one would red main fleet-wide for a property nobody introduced, which is
      how a guard gets routed around instead of obeyed. T3 of
      research/MASTERMIND_INTELLIGENCE_OS_V1_PLAN.md retires the invalid emitters
      and, in the same PR, wires this into pr_ci with --strict and upgrades severity
      to hard. Precedent for arriving manual-then-upgrading: governance.adjudication_packet
      (an upgrade needs no ruling; a downgrade does).

  - law_id: epistemics.validated_claims
    summary: >
      Every affirmative use of the word "validated" (EN + ZH "已验证") in
      user-facing surfaces must trace to a stored, leak-free, pre-registered
      artifact or an allowlisted justification. Negated/hedged uses are ignored.
      Law source: CLAUDE.md ("The word 'validated' in user-facing text is
      CI-enforced").
    source_ref:
      - scripts/check_validated_claims.py
      - CLAUDE.md
      - "PREREGISTRATION.md §4, D2 §4.3"
    severity: hard
    check_script: scripts/check_validated_claims.py
    ci_wiring:
      - workflow: .github/workflows/cycle-calibration.yml
        job: calibrate
        lane: scheduled
      - workflow: .github/workflows/ci.yml
        job: validated-claims
        lane: pr_ci
    selftest: true
    allowlist: data/regime/validated_claims_allowlist.json
    ratchet: null
    known_limits:
      - Scans templates/, site/*.js, generated *_data.js only; does not scan
        research/*.md (frozen Fable ruling 2026-07-06 — see
        epistemics.research_docs_not_scanned).
      - >-
        Verbatim third-party research text is skipped by construction
        (_THIRD_PARTY_PAGES) — the syndicated-note sinks of the three research-vault
        render targets, site/research/<slug>.html, site/research/index.html, and the
        SSR-baked catalog in site/research_vault.html. Reported speech from a named
        external author is not a platform claim and has no artifact to cite, so the
        alternative was a per-quote allowlist entry (a false "claim of record") every
        time a note used the word in ordinary English. Scoped to the third-party
        SINKS, never the path — platform copy on the same pages (the .rr-gate paywall
        block, the footer, the crawl-hub chrome) is still gated, and templates/ gets
        no exemption at all. Pinned by tests/test_validated_claims_thirdparty.py.
    owner_program: engine-fix-masterplan
    notes: null

  - law_id: epistemics.trial_registration
    summary: >
      Every multiple-testing harness (validate_* / *_phase0 scripts) must register
      its trial budget in the trial ledger. An unregistered harness makes DSR/kill/FDR
      deflation calculations dishonest.
    source_ref:
      - scripts/check_trial_registration.py
      - "engine/trial_ledger.py"
    severity: hard
    check_script: scripts/check_trial_registration.py
    ci_wiring:
      - workflow: .github/workflows/ci.yml
        job: trial-budgets
        lane: pr_ci
    selftest: false
    selftest_exempt_reason: >
      No selftest flag; deterministic scan over script names vs trial ledger.
    allowlist: inline
    ratchet: null
    known_limits:
      - Allowlist (LEGACY_UNREGISTERED) only shrinks; new scripts must register.
      - Scans script names by file pattern; does not parse script contents.
    owner_program: engine-fix-masterplan
    notes: >
      Allowlist is inline in the script (LEGACY_UNREGISTERED constant) and only shrinks.

  - law_id: epistemics.no_literal_ntrials
    summary: >
      No new code may call deflated_sharpe with a caller-asserted literal n_trials
      instead of ledger=; literal trial counts make budget tracking impossible.
    source_ref:
      - tests/test_no_literal_ntrials.py
    severity: hard
    check_script: tests/test_no_literal_ntrials.py
    ci_wiring:
      - workflow: .github/workflows/ci.yml
        job: trial-budgets
        lane: pr_ci
    selftest: false
    selftest_exempt_reason: >
      This entry is a pytest suite, not a standalone script; pytest is the test runner.
    allowlist: inline
    ratchet: null
    known_limits:
      - Allowlist (LEGACY_LITERAL_NTRIALS) only shrinks.
    owner_program: engine-fix-masterplan
    notes: >
      check_script points at the test file because enforcement is via pytest, not a
      standalone guard script. This suite ran in NO workflow until 2026-07-06 — the
      registry wiring pass caught the gap and the file was added to the trial-budgets
      pytest invocation the same day.

  - law_id: epistemics.badge_passport
    summary: >
      Every conviction badge rendered in a desk brief must carry a provenance
      passport. A badge without a passport is an unearned claim.
    source_ref:
      - scripts/check_badge_passport.py
      - "engine/passport.py"
      - "audit #41"
    severity: hard
    check_script: scripts/check_badge_passport.py
    ci_wiring:
      - workflow: .github/workflows/ci.yml
        job: outcome-spine
        lane: pr_ci
    selftest: false
    selftest_exempt_reason: >
      No selftest flag; deterministic scan over site/ HTML.
    allowlist: inline
    ratchet: null
    known_limits:
      - Allowlist (LEGACY_NO_PASSPORT) is fully drained — no new items allowed.
    owner_program: engine-fix-masterplan
    notes: null

  - law_id: epistemics.claim_passport
    summary: >
      Every qual-ladder field that reaches a scored consumer must have a registered
      claim passport entry in config/qual_ladder.yml. Grandfathered debt entries
      (state=DISPLAY) must be cleared once the §3 gate passes.
    source_ref:
      - scripts/check_claim_passport.py
      - config/qual_ladder.yml
    severity: hard
    check_script: scripts/check_claim_passport.py
    ci_wiring:
      - workflow: .github/workflows/ci.yml
        job: house-law-registry
        lane: pr_ci
    selftest: false
    selftest_exempt_reason: >
      No selftest flag; deterministic scan over engine/*.py vs qual_ladder.yml.
      Script exits 0 on the live repo (15 grandfathered DEBT entries, PASS result).
    allowlist: config/qual_ladder.yml
    ratchet: null
    known_limits:
      - 15 grandfathered DEBT entries (altdata.signal_score, policy.dir) are
        DISPLAY-state (gated); must be cleared once §3 gate passes.
      - ADVISORY unregistered fields (31+) are printed but do not fail the check.
    owner_program: qualitative-intelligence-masterplan
    notes: >
      Wired into house-law-registry job (exit 0 confirmed 2026-07-06).

  # ── Contracts guards ───────────────────────────────────────────────────────

  - law_id: contracts.reliability_contract
    summary: >
      A field that has been gated out (reliability state < threshold) must not
      render directionally in any template or site page. Audit #46.
    source_ref:
      - scripts/check_reliability_contract.py
      - "audit #46"
    severity: hard
    check_script: scripts/check_reliability_contract.py
    ci_wiring:
      - workflow: .github/workflows/ci.yml
        job: trial-budgets
        lane: pr_ci
    selftest: false
    selftest_exempt_reason: >
      No selftest flag; deterministic scan over site/ and templates/.
    allowlist: null
    ratchet: null
    known_limits:
      - Literal-path scan; does not catch dynamically computed field references.
    owner_program: engine-fix-masterplan
    notes: null

  - law_id: contracts.artifact_drift
    summary: >
      Each artifact in site/factordata/contracts/artifact_manifest.json must not
      drift from the live artifact's actual top-level JSON fields. Schema drift
      between producer and consumer is invisible at trade time without this gate.
    source_ref:
      - scripts/check_contract_drift.py
      - "site/factordata/contracts/artifact_manifest.json"
      - "NW Rails PR-7 R4"
    severity: hard
    check_script: scripts/check_contract_drift.py
    ci_wiring:
      - workflow: .github/workflows/ci.yml
        job: contract-drift
        lane: pr_ci
    selftest: false
    selftest_exempt_reason: >
      No selftest flag; deterministic comparison of manifest vs live artifacts.
    allowlist: null
    ratchet: null
    known_limits:
      - Wildcard artifacts (per_stock_intel, per_stock_signal) are skipped when
        no live sample is present — normal on CI runners.
    owner_program: nw-rails-lobes
    notes: null

  - law_id: contracts.board_contradictions
    summary: >
      The standout board must not show loud labels that contradict its own quiet
      fields: FRESH on stale/extended cross, urgency=imminent on blocked label,
      green band on Lagging verdict, and a board that is not in its own declared
      sort order — under us_prophet_v1 that means stage buckets in order, score
      non-increasing within a bucket, and no blocked-stage row above a live one;
      on legacy boards it stays the alpha-desc-within-lane check.
    source_ref:
      - scripts/check_board_contradictions.py
      - "engine/desk_scorer.py"
    severity: hard
    check_script: scripts/check_board_contradictions.py
    ci_wiring:
      - workflow: .github/workflows/ci.yml
        job: board-contradictions
        lane: pr_ci
    selftest: false
    selftest_exempt_reason: >
      No selftest flag; deterministic validation of JSON artifact fields.
    allowlist: null
    ratchet: null
    known_limits:
      - PR gate skips when the artifact is not touched by the PR. Fresh renders
        are graded warn-only by the builder's render-lane self-check
        (scripts/build_stock_library.py, "board-contradictions self-check" log
        line, added in PR 2171); the pages.yml twin is workflow_dispatch-only
        and guards manual GitHub Pages mirror re-deploys, not nightly renders.
    owner_program: us-stocks-prebreakout
    notes: null

  - law_id: contracts.signal_gate_pair_coherence
    summary: >
      us_standouts.json and signal_gate.json are written by ONE build_stock_library
      run and carry two copies of the same reading, so every board row with a
      non-empty `signal` blob must equal its gate verdict on every key the gate
      entry carries. Only the gate write is guarded (`if sig_verdict:`), so a
      scope=all re-render can advance the board and leave the gate behind — and
      both files stamp the same `as_of` (the DATA date), so nothing on either
      artifact reveals the skew.
    source_ref:
      - scripts/check_signal_gate_coherence.py
      - "engine/us_board_rank.py::verdict_for"
      - "PR 5490 (2026-08-12: 8 of 69 buy rows skewed; NTES blob ticks 0 vs gate 3)"
    severity: hard
    check_script: scripts/check_signal_gate_coherence.py
    ci_wiring:
      - workflow: .github/workflows/ci.yml
        job: signal-gate-pair-coherence
        lane: pr_ci
    selftest: false
    selftest_exempt_reason: >
      No selftest flag; the seeded-defect round trip lives in
      tests/test_signal_gate_coherence.py, which the same CI job runs on every PR
      (including a mutation guard that a ticks-only predicate fails).
    allowlist: null
    ratchet: null
    known_limits:
      - PR gate skips when neither artifact is touched by the PR. Fresh renders
        are graded warn-only by the builder's own self-check
        (scripts/build_stock_library.py, "::warning title=signal-gate-coherence"),
        which fires at the moment the skew is created.
      - Absent artifacts print n/a and exit 0 — sparse agent checkouts and fresh
        clones carry no site/ tree, and an absent pair is not a contradiction.
      - A differing emit.pair_id alone is not a violation; it only names which
        side is stale once the CONTENT already disagrees.
    owner_program: us-stocks-prebreakout
    notes: null

  # ── Cycles guards ──────────────────────────────────────────────────────────

  - law_id: cycles.cross_page_consistency
    summary: >
      The same instrument must show the same pos_v2/phase_v2 across every page
      that renders it (cycle / country_cycles / markets / sector), within
      tolerance, or carry a declared basis label.
    source_ref:
      - scripts/check_cycle_consistency.py
      - "W3.6 ruling R3-M3"
    severity: hard
    check_script: scripts/check_cycle_consistency.py
    ci_wiring:
      - workflow: .github/workflows/ci.yml
        job: cycle-consistency
        lane: pr_ci
    selftest: false
    selftest_exempt_reason: >
      No selftest flag; deterministic cross-page scan of committed site/ JSON.
    allowlist: null
    ratchet: null
    known_limits:
      - Tolerance is set by the checker; basis labels must be machine-readable.
    owner_program: cycle-intelligence
    notes: null

  - law_id: cycles.ontology_js_sync
    summary: >
      The committed site/cycle_ontology.js must exactly match what
      engine/cycle_ontology.py produces at HEAD. A stale committed JS is
      especially dangerous around the ONTOLOGY_AXIS flag-day axis-flip.
    source_ref:
      - scripts/gen_ontology_js.py
      - engine/cycle_ontology.py
      - "W1.5 A10/A12 ruling"
    severity: hard
    check_script: null
    ci_wiring:
      - workflow: .github/workflows/ci.yml
        job: cycle-ontology-js
        lane: pr_ci
    selftest: false
    allowlist: null
    ratchet: null
    known_limits:
      - Enforced by `python3 -m scripts.gen_ontology_js --check` (diff mode);
        does not validate semantic correctness of the ontology itself.
    owner_program: cycle-intelligence
    notes: >
      check_script is null because enforcement is via gen_ontology_js --check
      (a generator in diff/check mode), not a dedicated check_*.py script.

  # ── Authority guards ───────────────────────────────────────────────────────

  - law_id: authority.cycle_pattern
    summary: >
      No Article-2 money-path module (alert_triage / board_ordering / top_setups)
      may read the cycle-pattern lake (data/cycle_pattern/). The CPI substrate is
      display-only (context tier); lake outputs must never reach scored/board/alert
      pipelines.
    source_ref:
      - scripts/check_cycle_pattern_authority.py
      - "CPI P2"
    severity: hard_for_protected_paths
    check_script: scripts/check_cycle_pattern_authority.py
    ci_wiring:
      - workflow: .github/workflows/ci.yml
        job: cycle-pattern-authority
        lane: pr_ci
      - workflow: .github/workflows/cycle-calibration.yml
        job: calibrate
        lane: scheduled
    selftest: true
    allowlist: inline
    ratchet: null
    known_limits:
      - Literal-path scan only; misses dynamic path construction (e.g.
        f"data/cycle_pattern/{name}") that assembles the forbidden path at runtime.
      - _ALLOWED_READER_PREFIXES list: money-path modules are never exemptable.
    owner_program: cycle-intelligence
    notes: null

  - law_id: authority.research_factory
    summary: >
      No Article-2 money-path module may read data/research_factory/ or import
      engine.research_factory. The factory is display-only (A0-A2 ceiling);
      factory outputs must never reach scored/board/alert pipelines.
    source_ref:
      - scripts/check_research_factory_authority.py
      - "RF-11 Research Factory W1"
    severity: hard_for_protected_paths
    check_script: scripts/check_research_factory_authority.py
    ci_wiring:
      - workflow: .github/workflows/ci.yml
        job: research-factory-authority
        lane: pr_ci
    selftest: true
    allowlist: data/research_factory/authority_allowlist.json
    ratchet:
      date: "2026-09-15"
      action: >
        Bare-WARN debt drained 2026-07-06 (all 23 non-Article-2 readers declared in
        authority_allowlist.json). Remaining: operator review of the 4 cycle_pattern
        cross-lake entries (apply_cycle_pattern_lattice_batch2_outcomes writes factory
        transitions; 3 truth_guard flag-only importers) — ratify or remove their
        allowlist entries, then set this ratchet to null.
    known_limits:
      - Literal-path scan only; misses dynamic path construction.
    owner_program: research-factory
    notes: null

  # ── Neural Web guards ──────────────────────────────────────────────────────

  - law_id: nw.synapse_registry
    summary: >
      The synapse.yml registry must be internally consistent: all paths exist,
      enums are valid, no duplicate artifact paths.
    source_ref:
      - scripts/check_synapse_registry.py
      - config/synapse.yml
    severity: hard
    check_script: scripts/check_synapse_registry.py
    ci_wiring:
      - workflow: .github/workflows/ci.yml
        job: synapse-read-gate
        lane: pr_ci
    selftest: true
    allowlist: null
    ratchet: null
    known_limits:
      - Validates registry internal consistency; does not verify that artifact
        content matches the declared schema.
    owner_program: neural-web
    notes: null

  - law_id: nw.synapse_reads
    summary: >
      Every file that reads a registered synapse artifact must either appear in the
      baseline or be declared in config/synapse.yml. Article-2 money-path violations
      are hard-fail; other undeclared reads are warn-tier.
    source_ref:
      - scripts/check_synapse_reads.py
      - data/neuralweb/read_gate_baseline.json
      - config/synapse.yml
    severity: hard_for_protected_paths
    check_script: scripts/check_synapse_reads.py
    ci_wiring:
      - workflow: .github/workflows/ci.yml
        job: synapse-read-gate
        lane: pr_ci
    selftest: true
    allowlist: data/neuralweb/read_gate_baseline.json
    ratchet: null
    known_limits:
      - Literal-path scan only; misses string-assembled artifact paths.
    owner_program: neural-web
    notes: >
      Allowlist also includes config/synapse.yml consumer declarations.

  - law_id: nw.factor_boundaries
    summary: >
      No Article-2 module may write to synapse artifact paths or reference
      forbidden fields (RUL-NW9/NW11 display-only law). Factor outputs are
      display-only and must never reach scored/alert pipelines.
    source_ref:
      - scripts/check_factor_boundaries.py
      - "RUL-NW9 RUL-NW11"
    severity: hard
    check_script: scripts/check_factor_boundaries.py
    ci_wiring:
      - workflow: .github/workflows/ci.yml
        job: synapse-read-gate
        lane: pr_ci
    selftest: true
    allowlist: inline
    ratchet: null
    known_limits:
      - Literal-path scan; does not catch dynamically constructed paths.
      - Inline _ALLOWED_ACTIONS_ALLOWLIST_PREFIXES exempts allowed_actions
        readers; editing that list silently widens the exemption.
      - Two exact inflation-state producers have AST-proven emit-only spans;
        dynamically constructed key writes are intentionally rejected.
    owner_program: factor-intelligence
    notes: >
      Inflation Intelligence and World State may emit fixed all-false
      allowed_actions mirrors, but remain outside the reader allowlist. Mixed
      key-plus-read lines, subscript/get reads, behavioral branches, adjacent
      paths, and malformed source are mutation-tested to remain hard failures.

  - law_id: nw.dag_conformance
    summary: >
      The per-workflow build-module sequences declared in config/dag.yml must
      exactly match the live .github/workflows/ files. Any undeclared drift
      (lane mismatch not covered by a divergences entry) fails CI.
    source_ref:
      - scripts/check_dag_conformance.py
      - config/dag.yml
      - "Neural Web W5a"
    severity: hard
    check_script: scripts/check_dag_conformance.py
    ci_wiring:
      - workflow: .github/workflows/ci.yml
        job: dag-conformance
        lane: pr_ci
    selftest: true
    allowlist: config/dag.yml
    ratchet: null
    known_limits:
      - Textual (AST-level) lane comparison; does not verify execution ordering
        across parallel bands.
      - Two suspect drift entries exist from W5a adoption and are tracked but
        not yet resolved.
    owner_program: neural-web
    notes: >
      Allowlist is config/dag.yml divergences section.

  - law_id: cs.ingestion_health_gate
    summary: >
      Capital Structure selected filings with zero durable verified evidence
      must fail the isolated capital_structure job. Compiler telemetry.as_of
      is generation time, not source freshness. A proven empty/already-known
      queue is the only permitted non-failing zero-progress result. Collect
      stays green so the market plane is not frozen.
    source_ref:
      - scripts/check_capital_structure_health.py
      - engine/capital_structure/ingestion_health.py
      - "PR #5792"
    severity: hard
    check_script: scripts/check_capital_structure_health.py
    ci_wiring:
      - workflow: .github/workflows/daily.yml
        job: capital_structure
        lane: scheduled
    selftest: false
    selftest_exempt_reason: >
      Gate script over on-disk ingestion_run/health artifacts; behavioral
      coverage is tests/test_capital_structure_ingestion_health.py, including
      an all-storage-failure path that must exit 1.
    allowlist: null
    ratchet: null
    known_limits:
      - The gate reads collector receipts after the CS compilers; it cannot
        repair a dedicated-R2 AccessDenied ACL, only fail closed or fall back
        at collect time via build_source_store(require_writable=True).
    owner_program: capital-structure-intelligence
    notes: >
      Measured 2026-08-08→08-14: dedicated PutObject AccessDenied froze
      source_manifest at 1,258 while both nightly jobs stayed green.

  - law_id: ops.builder_failstreaks
    summary: >
      Nightly observability sentinel (added by #2704): scans the engine job's
      parallel-band artifact dir for per-builder failures and alerts
      Telegram/Discord when the same builder fails on consecutive nights.
      Pure telemetry — always exits 0, never blocks the deploy, writes no
      data/ artifacts and mutates nothing.
    source_ref:
      - scripts/check_builder_failstreaks.py
      - "PR #2704"
    severity: warn
    check_script: scripts/check_builder_failstreaks.py
    ci_wiring:
      - workflow: .github/workflows/daily.yml
        job: engine
        lane: scheduled
    selftest: false
    selftest_exempt_reason: >
      Non-gating alerting sentinel (|| warning in daily.yml); a failure can
      only lose an alert, never block a build or darken an artifact.
    allowlist: null
    ratchet: null
    known_limits:
      - Streak state derives from band artifact logs present on the runner; a
        cancelled engine job (no artifacts) resets streak visibility.
    owner_program: pipeline-reliability
    notes: null

  - law_id: nw.evidence_clock_display_only
    summary: >
      The Global Evidence Clock artifact (data/neuralweb/evidence_clock.json)
      is display-only attention routing: authority must be display_only, every
      row must carry promote/mutate_source_state in forbidden_actions, states
      must stay inside the 9-state vocabulary, clock_ids must be unique, and
      summary counts must match rows. The builder reads every clock source and
      may never mutate one (EC-R1..R8).
    source_ref:
      - scripts/check_evidence_clock.py
      - research/evidence_clock/ADJUDICATION_BY_FABLE.md
    severity: hard
    check_script: scripts/check_evidence_clock.py
    ci_wiring:
      - workflow: .github/workflows/ci.yml
        job: evidence-clock
        lane: pr_ci
    selftest: true
    allowlist: null
    ratchet: null
    known_limits:
      - Validates the committed artifact structurally; the builder's read-only
        runtime invariance is enforced by unit tests and review, not this gate.
    owner_program: neural-web
    notes: null

  - law_id: nw.ruling_graph_conflicts
    summary: >
      Ruling Graph guard (RUL-CL-6/7/8): a new fdr_family token in changed files
      must be declared in the ruling graph's known_fdr_families (H1, hard); a
      privacy-boundary token must never reach site/ or scripts that write to
      site/ (H2 BRIDGE law, hard). Warn tier: re-proposal of a killed/deferred
      idea without citing its ruling_id (W1) and expired come_back_on clocks (W2).
    source_ref:
      - scripts/check_ruling_conflicts.py
      - research/RULING_GRAPH_ADJUDICATION_BY_FABLE.md
      - config/ruling_graph.yml
    severity: hard
    check_script: scripts/check_ruling_conflicts.py
    ci_wiring:
      - workflow: .github/workflows/ci.yml
        job: ruling-graph
        lane: pr_ci
    selftest: true
    allowlist: config/ruling_graph.yml
    ratchet: null
    known_limits:
      - Deterministic substring matching only; W1 re-proposal detection can
        false-positive on benign mentions of a killed idea's match_term.
    owner_program: neural-web
    notes: >
      Registered by the evidence-clock PR as census catch-up — the script
      shipped in #1795 in a race with the #1793 meta-guard census.

  - law_id: nw.entity_thesis_registry
    summary: >
      ETM integrity gate: data/neuralweb/entity_thesis_mechanism_registry.json
      must keep schema/authority discipline (all rows display_only,
      is_context_only true), namespace grammar per registry_type, no invented
      links (every edge carries source.path + source.detail and resolvable
      endpoints), suggestion-only duplicate flags, recomputable counts, and
      referential integrity against species/candidates/trial-ledger sources
      (ETM-C1..C7).
    source_ref:
      - scripts/check_entity_thesis_registry.py
      - config/entity_thesis_mechanism_registry.yml
    severity: hard
    check_script: scripts/check_entity_thesis_registry.py
    ci_wiring:
      - workflow: .github/workflows/ci.yml
        job: neural-web
        lane: pr_ci
    selftest: true
    allowlist: null
    ratchet: null
    known_limits:
      - ETM-C7 referential integrity only checks source files present on disk;
        absent sources are skipped, not failed.
    owner_program: neural-web
    notes: >
      Registered by the evidence-clock PR as census catch-up — the script
      shipped in #1794 in a race with the #1793 meta-guard census.

  # ── Operations guards ──────────────────────────────────────────────────────

  - law_id: ops.hazard_model_freshness
    summary: >
      The hazard model artifact must not be stale beyond a configured threshold.
      Permanent warn (ops telemetry); --strict flag exists for opt-in hard mode.
    source_ref:
      - scripts/check_hazard_model_freshness.py
    severity: warn
    check_script: scripts/check_hazard_model_freshness.py
    ci_wiring:
      - workflow: .github/workflows/cycle-calibration.yml
        job: calibrate
        lane: scheduled
    selftest: false
    selftest_exempt_reason: >
      No selftest flag; ops telemetry check, permanent warn by design (--strict
      exists for opt-in hard mode).
    allowlist: null
    ratchet: null
    known_limits:
      - Warn-only by design; --strict flag must be opted into explicitly.
    owner_program: cycle-intelligence
    notes: >
      Permanent warn — this check is ops telemetry, not a gate.

  - law_id: ops.extraction_drift
    summary: >
      Re-score the extraction anchor set against the current LLM model. Gate:
      field_agree_rate >= 0.85. Requires Anthropic API key; run at model upgrades.
    source_ref:
      - scripts/check_extraction_drift.py
      - research/QUALITATIVE_INTELLIGENCE_UPGRADE_BY_FABLE.md
    severity: manual
    check_script: scripts/check_extraction_drift.py
    ci_wiring: []
    selftest: false
    selftest_exempt_reason: >
      --dry-run flag validates anchor schema and gate math without LLM calls;
      full run requires Anthropic API key (not available in CI).
    allowlist: null
    ratchet: null
    known_limits:
      - Requires ANTHROPIC_API_KEY; cannot run in CI without secrets exposure.
      - --dry-run validates structure only; does not test model agreement.
    owner_program: qualitative-intelligence-masterplan
    notes: >
      Manual run required at each LLM model upgrade. Gate threshold: field_agree_rate >= 0.85.

  - law_id: site.stretch_oracle_disclosure
    summary: >
      The per-ticker drawer renders TWO independent answers to "has this name
      already run" — ladder.alignment.overextended (a 4-leg, oscillator-dominant
      entry-timing brake) and entry_signal.status == "extended" (a relabel of
      ladder.entry.urgency == "caution"). They disagree on roughly 38% of names
      and that divergence is LEGITIMATE (a measurement versus a bucket label), so
      this guard does not force them equal. What it enforces is that the
      divergence stays DISCLOSED: every flagged name must carry the leg that
      actually fired, so no consumer has to guess a cause. HARD invariants:
      overextended=True implies a non-empty overextended_legs; overextended=False
      implies an empty one; overextended_basis equals
      overextension_basis(overextended_legs); and an oscillator-only basis may not
      coexist with a qualifying distance reading (producer and disclosure computed
      from different inputs).
    source_ref:
      - scripts/check_stretch_oracle_contract.py
      - docs/site_semantics/stretch_oracles.md
    severity: manual
    check_script: scripts/check_stretch_oracle_contract.py
    ci_wiring: []
    selftest: false
    selftest_exempt_reason: >
      The script takes a built store directory as its argument and has no
      synthetic self-test mode; its invariants are exercised instead by
      tests/test_stretch_oracle_contract.py, which the engine-render-guards job
      runs on every PR.
    allowlist: null
    ratchet: null
    known_limits:
      - Needs a BUILT stock store to read; it cannot run against a bare checkout,
        which is why it carries no ci_wiring entry of its own.
      - The O1 x O2 confusion counters are annotation-only and never fatal; only
        the four disclosure invariants exit 1.
    owner_program: per-ticker-stock-intelligence
    notes: >
      Enforced at BUILD time rather than as a CI step: scripts/build_stock_library.py
      lazy-imports `check` and runs it over the store it just wrote, so a violation
      fails the producer. Registered here per the census rule after the script
      shipped unregistered in #5574, which reddened the house-law meta-guard on main
      and every PR branched from it.

  - law_id: ops.live_worker
    summary: >
      Pre-flight probe for Cloudflare live-quotes worker deploys. Verifies that
      the worker is reachable and returning expected schema.
    source_ref:
      - scripts/check_live_worker.py
    severity: manual
    check_script: scripts/check_live_worker.py
    ci_wiring: []
    selftest: false
    selftest_exempt_reason: >
      No selftest flag; requires live Cloudflare worker endpoint — not available in CI.
    allowlist: null
    ratchet: null
    known_limits:
      - Requires live Cloudflare endpoint; not runnable in CI.
    owner_program: live-options-flow
    notes: >
      Run manually before each live-quotes worker deploy.

  - law_id: ops.vps_live_freshness
    summary: >
      Once the VPS is the declared live-plane primary, its public status
      envelope must prove that the fast, snapshot, and session-gated bars lanes
      are healthy and within their cadence budgets. Quotes, release detection,
      market-state leaves, baskets, and flow artifacts must also stay within
      their session-aware freshness limits. This is an external dead-man on
      GitHub-hosted compute, so an offline VPS or Mac fleet cannot report itself
      healthy.
    source_ref:
      - scripts/check_vps_live_health.py
      - .github/workflows/vps-live-heartbeat.yml
      - docs/VPS_LIVE_ORCHESTRATION.md
    severity: hard
    check_script: scripts/check_vps_live_health.py
    ci_wiring:
      - workflow: .github/workflows/vps-live-heartbeat.yml
        job: health
        lane: scheduled
    selftest: false
    selftest_exempt_reason: >
      Pure-stdlib evaluator and CLI failure behavior are covered by
      tests/test_vps_live_orchestration.py with healthy, stale/failed, and
      weekend/session-gating fixtures.
    allowlist: null
    ratchet: null
    known_limits:
      - The ten-minute GitHub schedule is best-effort and can itself be delayed.
      - The check proves artifact/lane freshness and last-run success; it does
        not validate the economic correctness of a vendor print.
      - It becomes scheduled only when VPS_LIVE_PRIMARY=true; manual dispatch
        is always available during the pre-cutover soak.
    owner_program: vps-live-orchestration
    notes: >
      The VPS cannot be the only observer of its own liveness. Keep this job on
      ubuntu-latest and keep its status source read-only and secretless.

  - law_id: ops.price_store_freshness
    summary: >
      The regime engine must not silently compute canonical state from a price
      store missing the latest completed NYSE session. The gate compares the
      last SPY daily close against lib/nyse_calendar's expected session — an
      exchange-calendar reference with zero data dependencies, so it still fires
      when the WHOLE nightly collection push dies and every committed store
      agrees on the stale date (2026-07-07 incident: run 28845154707's collect
      push died 5x pre-#1823; the engine persisted a faithful-but-stale
      RISK_OFF the live feed carried all session).
    source_ref:
      - scripts/check_price_store_freshness.py
      - lib/nyse_calendar.py
      - "2026-07-07 incident (commits b1cd43c98f / 38dc031b11; PRs #1823 #1830)"
    severity: warn
    check_script: scripts/check_price_store_freshness.py
    ci_wiring:
      - workflow: .github/workflows/daily.yml
        job: engine
        lane: scheduled
      - workflow: .github/workflows/engine-render.yml
        job: engine-render
        lane: render
    selftest: true
    allowlist: null
    ratchet: null
    known_limits:
      - Unscheduled one-off closures (mourning days, disasters) must be appended
        to lib/nyse_calendar.ONE_OFF_CLOSURES when announced; a missing entry
        costs a false red step + honest stale stamp — and the intraday
        fast-path's refresh_regime_if_stale re-pulls yahoo (keyless, ~16s) and
        logs store-behind-market on every ~30-min tick until the date is
        listed — never a silent wrong fresh.
      - continue-on-error by design — the engine job's if:always() philosophy
        (a scraper failure must still produce a dashboard from cached data);
        the teeth are the --heal yahoo re-pull in the daily lane, the
        freshness.stale stamp engine.market_state.persist writes into the
        canonical snapshot, and the no-regress refusal to clobber a fresher one.
      - engine-render wiring is check-only (that lane is network-free); the
        healing lanes are daily.yml (--heal) and the intraday fast-path's
        refresh_regime_if_stale (calendar-aware market reference).
    owner_program: engine-fix-masterplan
    notes: >
      Third leg of the stale-data defense: #1823 fixed the push (--autostash),
      #1830 detects committed-data freezes after the fact (heartbeat), this
      gate prevents the engine lane from persisting fresh-looking canonical
      state off a calendar-stale store — and self-heals the daily lane.

  - law_id: data.ohlc_basis_coherence
    summary: >
      The breadth OHLC caches must share ONE yfinance adjustment basis:
      data/<ns>/_closes_cache, _high_cache and _low_cache are written from the
      same download, and where all three are present the bar must satisfy
      low <= close <= high. yfinance auto_adjust back-adjusts ONLY the window it
      downloads, so a split or large dividend re-bases the fresh 1mo window while
      the cached history keeps the old basis. Both repair paths
      (BreadthAdapter._merge_refreshed and scripts/heal_breadth_split_seams.py)
      run seam_suspects over the CLOSES matrix ALONE and rewrite the extras only
      as a side effect of a closes-flagged ticker — so a basis split confined to
      high/low is invisible to every scanner in the tree (2026-08-06 incident:
      REZI close/high 1.29-1.44 on 773 of 773 bars, 989 bad bars across 37
      tickers, zero alarms). Close-only consumers are unaffected, which is what
      makes it silent; ATR's TR = max(H-L, |H-C_prev|, |L-C_prev|) is dominated
      by the basis gap and blows up for build_pick_lab, calibrate_bottom_radar
      and residual_momentum_phase0.
    source_ref:
      - scripts/check_ohlc_basis_coherence.py
      - collectors/breadth.py
      - scripts/heal_breadth_split_seams.py
      - "2026-08-06 split-basis incident (memory: high-low-panel-can-split-basis-from-the-closes-panel)"
    severity: warn
    check_script: scripts/check_ohlc_basis_coherence.py
    ci_wiring:
      - workflow: .github/workflows/daily.yml
        job: collect
        lane: scheduled
      - workflow: .github/ci/legacy-jobs.yml
        job: ohlc-basis-coherence
        lane: pr_ci
    selftest: true
    allowlist: null
    ratchet: null
    known_limits:
      - Two tiers by design. Only a SPLIT is fatal — worst excess >= 10% of the
        bar's own range, or >= 20% of a ticker's overlapping bars with at least 5
        of them. Isolated bad bars (yfinance ex-dividend rounding; 17 such bars
        across the three US panels at 2026-08-06, worst 2.5%) annotate as
        ::warning and never gate. A basis drift that is BOTH under 10% and
        confined to fewer than a fifth of a ticker's bars is reported as noise,
        not caught — the alternative is a guard that reds every night on a
        permanent data-quality floor.
      - Coverage is deliberately NOT asserted. The extras stores are
        forward-accruing (collectors/breadth.py "the store grows forward from
        first collection"), so high/low legitimately carry a fraction of the
        closes history — a median 55 bars against 777 at 2026-08-06. A ticker
        whose high/low are entirely absent is therefore invisible here; that is a
        coverage question, owned by the extras store's own accrual, not a basis
        question.
      - The verdict over live data belongs to the daily lane, not the PR pack.
        The nightly collection pushes straight to main, so no PR gate can see the
        commit that introduces a split; the pack lane tests the guard (synthetic
        frames) plus a reader-level pin over the committed caches, and asserts no
        verdict on them (they advance nightly — asserting one asserts about today).
      - continue-on-error in daily.yml by design (a data finding must never
        discard hours of collected bytes). The teeth are the ::error annotation
        and data/quality/ohlc_basis_coherence.json, committed by "commit market
        data" in the same job.
      - Panel discovery is by filesystem shape (any data/<ns>/ holding all three
        caches), so a namespace that never ships an extras store is silently out
        of scope rather than reported missing. Today that is every non-US breadth
        universe plus russell_breadth.
    owner_program: breadth-collector
    notes: >
      The guard closes a hole exactly the shape of the two repair paths: both
      scan closes and heal extras only in that scan's shadow. Two concrete ways a
      split survives them — _merge_refreshed's extras graft is filtered by
      `t in w.columns` against the FRESH window (a ticker healed in closes but
      absent from that batch's High keeps poisoned extras, with no warning because
      the closes half succeeded), and heal_breadth_split_seams.heal reindexes the
      re-pull onto the EXISTING ex.index, so it can pair a healed closes column
      with extras it never touched.

  - law_id: ops.surface_freshness
    summary: >
      First-class display surfaces (allocation, baskets, oracle_state,
      sector_pulse) must not silently serve stale as_of dates. The sentinel
      compares each artifact's as_of stamp against lib/nyse_calendar's expected
      session and prints a ::warning:: annotation per stale surface
      (2026-07-06→09 incident: data/allocation froze for 3 sessions inside an
      exit-0-always sub-build while the allocation page kept rendering the old
      leader; FTR masterplan FT-R8).
    source_ref:
      - scripts/check_surface_freshness.py
      - lib/nyse_calendar.py
      - research/FAST_TURN_TWO_SPEED_TAPE_MASTERPLAN_BY_FABLE.md
      - "PR #2039 (FTR W0)"
    severity: warn
    check_script: scripts/check_surface_freshness.py
    ci_wiring:
      - workflow: .github/workflows/daily.yml
        job: engine
        lane: hook
      - workflow: .github/workflows/engine-render.yml
        job: engine-render
        lane: hook
    selftest: true
    allowlist: null
    ratchet: null
    known_limits:
      - >-
        Invoked from inside scripts/build_baskets.py main() (the build_allocation
        hook pattern — no dedicated workflow step; PAT lacks workflow scope),
        so wiring is lane:hook via the workflows that run build_baskets.
      - >-
        Warn-only by design (exit 0 always) — a stale surface must not block the
        rest of the site build; the teeth are the "::warning::" annotation plus
        the site/allocationdata/freshness.json stale stamp that W3 renders as a
        visible STALE chip.
    owner_program: fast-turn
    notes: >
      FT-R8 freshness contract, first leg: build_allocation snapshot write was
      unbundled from the alerts try/except (candidate structural cause) and the
      wrapper now annotates failures; this sentinel catches whatever class of
      freeze comes next, on every surface it audits.

  - law_id: ops.tushare_plane_freshness
    summary: >
      The token-gated Tushare stores (flow_hist, moneyflow) must not be able to
      go cold without anyone being told. collectors/tushare_client.py returns
      None and never raises for every failure mode it has (no token, endpoint
      error, access denied, credits short, empty response); its callers omit the
      leg rather than write a fabricated zero; and asia-close's collect step is
      explicitly "graceful degradation — never fails on one source". The union
      of those three individually correct decisions is that the entire Tushare
      plane can die with every signal staying green: the workflow succeeds, the
      page rebuilds on schedule, and it prints a three-week-old as-of date in
      exactly the same confident type as a live one (2026-07-24→08-06 incident:
      data/tushare/flow_hist.parquet and moneyflow.parquet both froze at
      2026-07-24 and were still rendered on flow_velocity.html on 2026-08-06 —
      thirteen days — while china_lhb, filled by the SAME adapter list in the
      SAME run, stayed current). The tripwire compares each store's newest trade
      date against the exchange calendar's expected last session — deliberately
      NOT against the store's own newest row or a sibling store, because a
      frozen feed is perfectly self-consistent and reads "fresh" during a total
      outage, while the calendar keeps advancing whether or not the collector
      ever runs again.
    source_ref:
      - scripts/check_tushare_freshness.py
      - collectors/tushare_client.py
      - lib/hk_calendar.py
      - tests/test_tushare_freshness_tripwire.py
      - "PR #4811 (guard shipped)"
      - "2026-07-24→08-06 freeze (flow_hist/moneyflow stale 13 days behind flow_velocity.html)"
    severity: warn
    check_script: scripts/check_tushare_freshness.py
    ci_wiring:
      - workflow: .github/workflows/asia-close.yml
        job: asia
        lane: scheduled
    selftest: true
    allowlist: null
    ratchet: null
    known_limits:
      - >-
        There is no mainland A-share calendar in lib/, so HKEX is used as the
        closest proxy; it shares most holidays but diverges on mainland-only
        closures — Golden Week (Oct 1-7) most notably, where HKEX trades and
        Shanghai does not — and can therefore warn benignly for a few days each
        October. Priced in on purpose, since this emits a warning and never a
        failure: a benign October warning is a much cheaper error than silently
        serving stale flow data for a fortnight.
      - >-
        Audits only the two token-gated stores it names
        (tushare/flow_hist.parquet, tushare/moneyflow.parquet); other stores the
        same adapter list fills (e.g. china_lhb) are not covered by this check.
      - >-
        The threshold is 3 sessions behind — sized to absorb a long weekend plus
        one public holiday and the lane's own once-a-day gate — so a fresh
        freeze stays silent for its first three sessions by design.
    owner_program: engine-fix-masterplan
    notes: >
      Permanently advisory (the script always exits 0), not ratcheted to hard:
      one cold gated source must not red an asia-close collection that landed
      every other store. The teeth are the "::warning title=tushare plane cold"
      annotation, which names the frozen store AND the collector that fills it,
      plus the membership/积分 tier hint for the usual cause. Wired AFTER
      `python -m scripts.collect --group asia` inside the same job so it grades
      this run's stores rather than the previous night's — that ordering is
      pinned by tests/test_tushare_freshness_tripwire.py, as is the line-start
      flushed annotation shape.

  - law_id: ops.membership_snapshot_cadence
    summary: >
      The basket-membership point-in-time planes (同花顺 concept baskets, US
      thematic baskets) must not be able to go cold without anyone being told.
      Both snapshot writers are CONTENT-deduped — a dated side-car is written
      only when membership differs from the newest one stored — which is right
      (membership moves on the vendor's schedule, not the trading calendar) and
      means a writer that is deduping and a writer that has been UNWIRED leave
      the identical trace on disk: nothing. That is not hypothetical. The THS
      PIT store held exactly two snapshots, 2026-06-30 and 2026-07-08, while
      `scripts/build_baskets_china_ths.py --snapshot` ran green ~35 nights in a
      row: the step was healthy, its INPUT was frozen, because the scraper
      (collectors/china_ths_concepts.py) and the seeder
      (scripts/seed_china_ths_baskets.py) appeared in NO workflow at all and the
      2026-07-08 file was a hand-run bundled with a feature commit. The tripwire
      therefore grades three independent axes: cadence (the writer's own
      `_cadence.json` liveness stamp, rewritten on every run INCLUDING the dedup
      skip), source (a THS scrape receipt with complete:true inside the weekly
      budget — the axis that sees a live writer on a frozen input), and
      coherence (the PIT parquet's newest snapshot_date must not lag the newest
      dated side-car, catching a backfill that stopped running).
    source_ref:
      - scripts/check_membership_snapshot_freshness.py
      - scripts/scrape_ths_weekly.py
      - engine/basket_membership_pit.py
      - contracts/baskets_china_ths/scrape_receipt.v1.schema.json
      - tests/test_membership_snapshot_freshness.py
      - "GMI masterplan §7 W1a (research/GLOBAL_MARKET_INTELLIGENCE_MASTERPLAN_BY_FABLE.md)"
      - "2026-06-30→08-11 THS membership freeze (2 snapshots in 6 weeks, ~35 green nights)"
    severity: warn
    check_script: scripts/check_membership_snapshot_freshness.py
    ci_wiring:
      - workflow: .github/workflows/asia-close.yml
        job: asia
        lane: scheduled
      - workflow: .github/workflows/daily.yml
        job: engine
        lane: scheduled
    selftest: true
    allowlist: null
    ratchet: null
    known_limits:
      - >-
        The source axis is audited for the THS suite only. The US suite's
        membership.json is hand-curated, so "the source has not moved this
        month" is its normal state, not a fault — only the writer's cadence and
        the side-car→parquet coherence are graded there.
      - >-
        Budgets are calendar-day, not session-anchored: 4 days for the writer's
        cadence stamp and 10 days for a complete THS scrape (weekly cadence plus
        one missed week of slack). A fresh freeze is therefore silent for its
        first few days by design, and a run of holidays widens that window.
      - >-
        A MISSING store, receipt directory or parquet is INDETERMINATE (one
        ::notice), never a breach — sparse agent checkouts carry no data/ and a
        suite's first run has no stamp by construction. So this guard cannot
        distinguish "never wired" from "not checked out"; it only catches a
        plane that once ran and stopped.
      - >-
        The curated CN suite (baskets_china) is not audited: it shares the THS
        suite's writer and lane, so a cadence breach there would be reported
        twice for one cause.
    owner_program: global-market-intelligence
    notes: >
      Permanently advisory (the script always exits 0), like its ops.tushare
      sibling: one cold membership plane must not red a collection lane that
      landed every other store. The teeth are the
      "::warning title=membership snapshot cadence stalled" annotation, which
      names the suite AND the axis, plus a fail-open alert_triage push. Wired in
      BOTH nightlies because the two suites are advanced by different lanes —
      asia-close (CN_LANE=asia) owns the CN suites, daily.yml (COLLECT_LANE=nightly)
      owns the US one — and in each it is ordered AFTER that lane's snapshot
      writer so it grades this run's stamp rather than the previous night's.

  - law_id: ops.workflow_yaml_parses
    summary: >
      Every file under .github/workflows/ must load as YAML and carry an on:
      trigger block plus a non-empty jobs: mapping. An unparseable workflow
      file makes GitHub stamp a FAILED zero-job run on every push to every
      branch carrying it (2026-07-10→11 incident: metabolism-agenda.yml
      shipped with column-0 python -c continuation lines that terminated the
      run: | block scalar — ~325 failed runs in ~26h from a schedule-only
      workflow; introduced #2135, file repaired #2260). dag-conformance only
      yaml-loads workflows declared as dag.yml lanes, so non-lane workflows
      had no parse guard at all.
    source_ref:
      - scripts/check_workflow_yaml.py
      - "PR #2260 (file repair)"
      - "PR #2135 (regression origin)"
    severity: hard
    check_script: scripts/check_workflow_yaml.py
    ci_wiring:
      - workflow: .github/workflows/ci.yml
        job: workflow-yaml
        lane: pr_ci
    selftest: true
    allowlist: null
    ratchet: null
    known_limits:
      - >-
        PyYAML (YAML 1.1) is not GitHub's exact parser; semantic errors GitHub
        rejects (bad expression syntax, unknown keys) can still pass here. The
        guard covers the observed lethal class: parse failure and missing
        on:/jobs: blocks.
    owner_program: null
    notes: >
      Complements nw.dag_conformance (lane-sequence drift on declared lanes)
      by asserting bare loadability of ALL workflow files, lane or not.

  - law_id: ops.runner_routing_policy
    summary: >
      Public-repository pull-request workflows remain exactly GitHub-hosted during
      Wave B/C, cannot delegate through reusable-workflow call jobs, and cannot
      consume the bounded PC/M1 migration labels. Only registered main-branch
      workflow-dispatch canaries may request those labels, downstream of a hosted
      trust gate. PC render and CI labels remain disjoint and the M1 canary cannot
      regain a broad production label. Every runs-on label in every workflow must
      be declared in the label_registry, and an orphaned label may not be used by
      a scheduled workflow without a dated waiver.
    source_ref:
      - scripts/check_runner_policy.py
      - .github/runner-policy.yml
      - tests/test_runner_policy.py
      - docs/CI_SELFHOSTED_WAVE_BC_RUNBOOK.md
    severity: hard
    check_script: scripts/check_runner_policy.py
    ci_wiring:
      - workflow: .github/ci/legacy-jobs.yml
        job: workflow-yaml
        lane: pr_ci
    selftest: false
    selftest_exempt_reason: >
      The checker has a mutation-focused pytest suite covering fork-route drift,
      computed migration labels, reusable-workflow delegation, trust-gate removal,
      topology overlap, and unregistered custom-label consumers; the checker and
      suite run together in the registered workflow-yaml legacy job.
    allowlist: null
    ratchet: null
    known_limits:
      - >-
        Repository lint is a merge guard, not a runtime admission boundary. The
        live home runners must also reside in the organization runner group that
        is restricted server-side to exact workflow paths pinned to refs/heads/main.
      - >-
        Production self-hosted labels are now in scope for DECLARATION (R11/R12,
        added 2026-08-17): every literal runs-on label in every workflow must be
        a label_registry key, and a workflow with a schedule trigger cannot use
        an orphaned label without a dated waiver. Routing rules R1-R10 still
        govern only the Wave B/C migration labels — this law does not gate which
        production label a job may use, only that the label is declared.
      - >-
        label_registry `status` fields are hand-maintained, refreshed by hand
        from the GitHub runners API, because listing live runners needs an
        admin-scoped token CI does not carry. R11/R12 therefore catch an
        undeclared label or an orphan-scheduled label at PR time, never a host
        that went offline an hour ago — that gap is why `offline` is watch-only
        and not R12-blocking.
    owner_program: ci-recovery-wave-bc
    notes: >
      Registered after the first full PR pack correctly caught the new
      check_runner_policy.py in the house-law census. The runtime counterpart is
      the macro-home-canary organization runner group; static ordering alone is
      not treated as a security boundary. R11/R12 close the class DSC:QUEUED-JOB-
      HOSTAGE-HOLDS-THE-NIGHTLY-CRON-GROUP names: a deregistered host silently
      orphans every label it carried, and a cron job queued on a dead label can
      hold its concurrency group for 24h. (The postmortem that discovery cites,
      research/PROPHET_OUTAGE_2026_08_17_POSTMORTEM.md, ships on a separate
      unmerged PR and is not cited as a source_ref here to avoid a dangling path
      on this branch.)

  - law_id: ops.no_skip_only_suites
    summary: >
      No test may skip in every CI job that names it. A suite can be named by a
      job, be triggerable, and still execute nowhere — because the one job that
      names it does not install what its `pytest.importorskip("X")` / module-probe
      `skipif` gates need. That is the third darkness class, between UNRUN and
      UNTRIGGERABLE, and an importorskip added to clear a red lane creates it by
      construction: main goes green AND weaker in the same commit. It shipped
      twice — #3703, then #3722→#3738 (fixed by #3760, where six tests including
      the two load_ohlcv crypto-source regressions executed in ZERO jobs for the
      day they were on main). The check is static: per test file collect the skip
      gates, per job collect the test files its `run:` steps name and the modules
      its single pip install line provides, and fail when no naming job provides
      a gated module. A first-party gate resolves to that module's own
      third-party import closure (engine.indicators_m2 → numpy/pandas).
    source_ref:
      - scripts/check_skip_only_suites.py
      - "PR #3760 (chart-render/chart-render-data pairing; filed this follow-up)"
      - "PR #3703 (first occurrence)"
    severity: hard
    check_script: scripts/check_skip_only_suites.py
    ci_wiring:
      - workflow: .github/workflows/ci.yml
        job: workflow-yaml
        lane: pr_ci
    selftest: true
    allowlist: null
    ratchet: null
    known_limits:
      - >-
        Gates on things pip cannot install are deliberately out of scope: a data
        artifact (_PANEL.exists()) or a binary (shutil.which("node")) is a
        different darkness class with a different fix.
      - >-
        Suites that NO job names are reported as UNRUN and never fail this gate —
        scripts/audit_unrun_tests.py owns that class, and wiring every unrun suite
        would blow the ci-pack budget.
      - >-
        Scope was `tests/` only until 2026-08-06 (#4693), which made this gate and
        both censuses blind to research/-resident guard suites — the ones most
        likely to be dark, because a packet fenced to files-only writes its suite
        next to the instrument. Discovery is now the whole tree via
        scripts/audit_unrun_tests.discover_suites().
      - >-
        A test_-shaped FILENAME is not a suite: three files under research/ are CLI
        measurement instruments that collect zero tests. Classification reads the
        AST for what pytest would collect rather than trusting the name, so those
        are excluded — reporting them would make the census noise, which is the
        failure mode these guards exist to end.
      - >-
        Transitive dependency closure (PACKAGE_REQUIRES) is a hand-maintained
        table of hard runtime deps. Under-declaring costs a false red;
        over-declaring costs a missed skip-only suite. It is deliberately
        restricted to certainties (pandas→numpy, jinja2→markupsafe, …).
    owner_program: null
    notes: >
      Complements ops.workflow_yaml_parses and the ci-pack packing contract in the
      same job. Its first run surfaced four pre-existing skip-only suites
      (test_marketing_content, test_marketing_live_verify, test_publish_r2,
      test_research_vault), all repaired in the PR that added the guard.

  - law_id: ops.ci_trigger_closure
    summary: >
      A guarded suite's TRIGGER PATHS must reach every module the suite measures.
      The fourth darkness class, and the reverse direction of every census beside
      it: audit_unrun_tests.py asks "can ANY edit start this job" — an any() over
      the closure, satisfied by the test file itself — so a suite stays
      "triggerable" while the ONE module it exists to guard is matched by no
      `on.pull_request.paths` entry. An edit to exactly that module then ships
      without ever running its own guard. The check is static: discover the
      PR-gated workflows (a workflow with no `paths:` covers everything it runs
      unconditionally; one WITH `paths:` governs both its own `run:` steps and any
      job manifest it hands a runner, which is how the packed legacy jobs inherit
      ci.yml's single filter), resolve each named suite's direct first-party reads
      (imports, import_module literals, repo-relative path literals, ROOT/"a"/"b.py"
      joins), and fail when a resolved module is reachable by no gate that runs it.
    source_ref:
      - scripts/check_ci_trigger_closure.py
      - tests/test_ci_trigger_closure.py
      - "PR #4649 (guard shipped)"
      - "PR #3488 (a guard whose own edit cannot start the workflow is half wired)"
      - "PR #4559 (fixture-only heal — same class)"
      - "PR #4733 (false positive, healed the wrong way — see allowlist)"
    severity: hard
    check_script: scripts/check_ci_trigger_closure.py
    ci_wiring:
      - workflow: .github/workflows/ci.yml
        job: workflow-yaml
        lane: pr_ci
    selftest: true
    allowlist: "inline (`# ci-trigger-closure: data` comment at the fixture site)"
    ratchet: null
    known_limits:
      - >-
        A path literal is evidence of a read, not proof of one. A suite whose
        SUBJECT MATTER is path filtering (merge-on-green, ci-trigger, paths-filter)
        carries file NAMES as fixture data, so the heuristic and the domain collide
        by construction. #4733 healed one such finding by adding two `research/**`
        artifacts to ci.yml's paths — encoding a dependency that does not exist and
        arming the full 4-pack run on every edit to two decision-packet files. The
        author-declared `# ci-trigger-closure: data` marker is the sanctioned form;
        it covers the line it sits on when that line carries a path literal, and
        otherwise the statement it opens or the one directly below. It reaches only
        literals and joins — an import is a read no comment can excuse — and every
        suppression is counted in the summary and named by `--report`. Inference was
        prototyped and rejected: "a literal that never reaches an open/read_text/Path
        call is data" drops 787 of 3,824 subjects across 132 suites, including
        PRESS_MUST_RESTART's 15 real ones (reached through @pytest.mark.parametrize).
      - >-
        The gate is DIRECT reads only (depth 1). `--depth N` widens the report to
        transitive reads and is deliberately NOT gated: past depth 1 the
        path-literal heuristic starts resolving placeholder strings out of
        docstrings, and a guard that cries wolf gets ignored — which is the exact
        failure mode this file exists to end.
      - >-
        Reachability is decided under GitHub's `paths` glob semantics (exact,
        fnmatch, `prefix/**` subtree). A path expression GitHub accepts but this
        resolver models differently would be judged on the model, not the runner.
      - >-
        Suites that NO job names are UNRUN, not trigger gaps —
        scripts/audit_unrun_tests.py owns that class.
      - >-
        Scope was tests/ only until 2026-08-06 (#4693) — a suite wired from
        research/ was invisible to this gate even once wired. Suites now come from
        audit_unrun_tests.discover_suites() and run-step tokens resolve against
        that inventory, so a test_-shaped CLI instrument named by a run: step is
        not gated as a suite.
    owner_program: null
    notes: >
      Registered by the 2026-08-06 base heal. The guard shipped wired in #4649
      (workflow-yaml, beside its two sibling censuses) but was never added to this
      registry, so the meta-guard's CENSUS pass hard-failed ci-pack-2 on main and
      on every open PR head — the same shape as the flow_continuity.statement_tape
      entry below, which was registered by a post-hoc census fix for the same
      reason. Belongs to the darkness-census family with ops.no_skip_only_suites
      and ops.workflow_yaml_parses in the same job.

  - law_id: ops.government_revenue_amount_semantics
    summary: >
      Amount classes are never conflated. Obligation, outlay, ceiling, bookings,
      backlog, funded backlog, and GAAP revenue are distinct quantities. The
      classes live once in engine/government_revenue/amount_semantics.py and are
      derived from the collector's canonical number-column declarations. The
      source guard fails when addition, summation, fallback, or publication
      combines incompatible classes or drops the class label from a figure.
    source_ref:
      - scripts/check_government_revenue_amount_semantics.py
      - engine/government_revenue/amount_semantics.py
      - tests/test_government_revenue_amount_semantics.py
      - "PR #4950"
    severity: hard
    check_script: scripts/check_government_revenue_amount_semantics.py
    ci_wiring:
      - workflow: .github/ci/legacy-jobs.yml
        job: unrun-government-revenue
        lane: pr_ci
    selftest: true
    allowlist: null
    ratchet: null
    known_limits:
      - Static source analysis cannot see a conflation assembled at runtime from
        names it cannot resolve, such as getattr or a config-built column list.
      - Coverage derives from the collector's canonical number-column
        declarations, so a ledger field introduced outside those declarations
        carries no class and is not judged.
    owner_program: government-revenue-foresight
    notes: null

  # ── Meta-guard ─────────────────────────────────────────────────────────────

  - law_id: meta.house_law_registry
    summary: >
      The house-law registry (config/house_law_checks.yml) must be internally
      consistent and cover every scripts/check_*.py file in the repo. An
      unregistered check_*.py blocks merges.
    source_ref:
      - scripts/check_house_law_registry.py
      - config/house_law_checks.yml
    severity: hard
    check_script: scripts/check_house_law_registry.py
    ci_wiring:
      - workflow: .github/workflows/ci.yml
        job: house-law-registry
        lane: pr_ci
    selftest: true
    allowlist: null
    ratchet: null
    known_limits:
      - Census only auto-covers scripts/check_*.py — guard-shaped pytest files
        (e.g. tests/test_no_literal_ntrials.py) and harness hooks are voluntary
        entries; adding them is required but not mechanically enforced by census.
      - Wiring check is textual (job steps mention the script basename), not
        execution-order-aware.
    owner_program: engine-fix-masterplan
    notes: null

  - law_id: ci.vintage_pin_fence
    summary: >
      Tests must not equality-pin a live nightly store (govrev latest/candidates,
      options episodes.jsonl, prophet plans, spine predictions.parquet, the
      Release Radar tree under data/release_forecast including cpi_truth,
      data/fred_vintage/release_targets, vector and commodity alert jsonl,
      alerts_log.parquet, tripwire latch state). A hand-typed ``== N`` / ``<= N``
      against a store the nightly advances reddens main and then, through
      merge-on-green, every armed PR. Legal: a watermarked prefix slice, a
      census derived from a committed receipt, a named disclosure set, or the
      #5547 cold-test pattern (bind config.data_dir to empty tmp, or pass
      explicit root=). Shrink-only baseline: fingerprints leave when the pin
      is gone; do not add a row to ship a new pin.
    source_ref:
      - scripts/check_vintage_pin_fence.py
      - tests/test_vintage_pin_fence.py
      - ".github/ci/legacy-jobs.yml"
    severity: hard
    check_script: scripts/check_vintage_pin_fence.py
    ci_wiring:
      - workflow: .github/workflows/ci.yml
        job: vintage-pin-fence
        lane: pr_ci
    selftest: true
    allowlist: null
    ratchet: null
    known_limits:
      - The live scan is a targeted ratchet over tests that read the named
        stores; it is not a general AST linter of every ``== N`` in the tree.
      - A store must be added to LIVE_STORES in the script before a new
        nightly-advanced path is covered.
      - Default-root readers are a closed set (convergence_tier, measured_ic).
        A helper that wraps those calls is a known miss unless the test body
        also names the store or calls the reader directly.
      - Bare config.data_dir() joins and pd.read_parquet loaders taint only
        when the suffix matches a named LIVE_STORES path; an unnamed sibling
        under data/ is invisible until it is added to LIVE_STORES.
    owner_program: pipeline-reliability
    notes: >
      Added with the 2026-08-13 CI traffic-jam fence. The three detonations
      that motivated it (govrev assert 23==8, options 384→1206, prophet
      open-keys 10→11) were healed by #5524; leftover equality pins stay on
      the shrink-only baseline and warn, they never red. Expanded 2026-08-13
      after #5515 missed spine parquet (#5547) and Release Radar
      inflation-truth (unrun-release-forecast) plus sibling alert/tripwire
      stores. Directory prefixes now cover data/release_forecast/** and
      data/fred_vintage/release_targets/**; parquet loaders of a data_dir
      path taint the same way a Path(__file__) chain does. The #5547 cold
      data_dir bind is a legal pattern, not a new check.

  # ── Governance hook ────────────────────────────────────────────────────────

  - law_id: governance.model_routing
    summary: >
      Agent/Task spawns must name an explicit model or use a pinned agent type
      (builder/reviewer). Fable runs only via orchestrator+FABLE-WHY (spawns) or
      a script-level FABLE-WHY line (workflow stages — operator re-enable
      2026-07-18 'fable ultracode'). Workflow scripts whose agent() calls carry
      no model:/agentType: routing are denied.
      Law source: CLAUDE.md §Model routing, enforcement via PreToolUse hook.
    source_ref:
      - .claude/hooks/model_routing_guard.py
      - .claude/settings.json
      - CLAUDE.md
    severity: hard
    check_script: .claude/hooks/model_routing_guard.py
    ci_wiring:
      - workflow: .claude/settings.json
        job: PreToolUse
        lane: hook
    selftest: false
    selftest_exempt_reason: >
      Harness hook, fail-open by design. Not a standalone script; tested by the
      PreToolUse harness, not by pytest or a --selftest flag.
    allowlist: null
    ratchet: null
    known_limits:
      - Fail-open by design (hook errors do not block the tool call).
      - Only fires at tool-use time; does not audit committed workflow YAML files.
    owner_program: engine-fix-masterplan
    notes: null

  - law_id: governance.private_boundary
    summary: >
      data/private/ and data/operator/ must stay gitignored, and committed
      feedback artifacts (data/governance/*.json[l], site/mastermind/*.json)
      may carry only counts — no tickers, fill/position/decision IDs, host
      paths, dollar amounts, or secret env-var patterns (FB-R12/FB-R13).
    source_ref:
      - scripts/check_private_boundary.py
      - research/MASTERMIND_FEEDBACK_CONTRACT_PROGRAM.md
      - "FB-R12 FB-R13"
    severity: hard
    check_script: scripts/check_private_boundary.py
    ci_wiring:
      - workflow: .github/workflows/ci.yml
        job: private-boundary
        lane: pr_ci
    selftest: true
    allowlist: inline
    ratchet: null
    known_limits:
      - site/mastermind/mastermind_snapshot.json is exempt from the private-key,
        dollar-amount, and secret-env scans (FB-R1 paper book publishes tickers
        by design) but not from host-path scans.
    owner_program: mastermind-feedback-contract
    notes: >
      Landed on main concurrently with this registry (2026-07-06); registered
      during the registry PR's merge-with-main — the census pass caught it.

  - law_id: auth.no_client_supplied_identity
    summary: >
      Route handlers must not derive user_id (or equivalent identity) from the
      request body, query string, or path. _pg() uses SUPABASE_SERVICE_ROLE_KEY,
      which bypasses RLS, so token-derived identity is the only thing preventing
      cross-tenant access. Law source: MASTERMIND_SECURITY_AUTH_BILLING_AUDIT.md
      §2.1 (WS-9 / GATE-6).
    source_ref:
      - scripts/check_no_client_supplied_identity.py
      - research/MASTERMIND_SECURITY_AUTH_BILLING_AUDIT.md
      - research/MASTERMIND_RED_TEAM_REMEDIATION_PLAN.md
      - "WS-9 GATE-6 MMX isolation invariant"
    severity: hard
    check_script: scripts/check_no_client_supplied_identity.py
    ci_wiring:
      - workflow: .github/workflows/ci.yml
        job: no-client-supplied-identity
        lane: pr_ci
    selftest: true
    allowlist: null
    ratchet: null
    known_limits:
      - AST over app/** FastAPI/Starlette handlers only. Operator admin tools are a different trust boundary and are not scanned.
      - Dynamic key construction (body.get("user_" + "id"), getattr with a non-literal) is invisible.
      - Pydantic models defined in another module are not joined; only same-file body models are checked for identity fields.
      - A Depends() wrapper that itself reads a client field is a known miss — the parameter then looks injected.
    owner_program: mastermind-security-auth-billing
    notes: >
      Mutation-proven in-script (--selftest plants each surface, then requires
      the live app/ tree to stay clean). Registering the file is not the proof.

  # ── Discipline-only laws (no check_script, no ci_wiring) ──────────────────

  - law_id: nw.article1.no_llm_origination
    summary: >
      LLMs may only de-escalate calibrated keys — never originate signals, scores,
      or escalations. Article 1 of the Neural Web constitution.
    source_ref:
      - engine/neuralweb
      - CLAUDE.md
    severity: discipline
    check_script: null
    ci_wiring: []
    selftest: false
    allowlist: null
    ratchet: null
    known_limits:
      - Enforced by architecture and review; no static scan exists for this constraint.
    owner_program: neural-web
    notes: null

  - law_id: ledgers.nightly_sole_advancer
    summary: >
      The nightly pipeline is the sole advancer of forward ledgers. Intraday lanes
      must discard any data/ writes. Law source: CLAUDE.md.
    source_ref:
      - CLAUDE.md
      - .github/workflows/daily.yml
    severity: discipline
    check_script: null
    ci_wiring: []
    selftest: false
    allowlist: null
    ratchet: null
    known_limits:
      - Enforced by nightly pipeline architecture and code review; no static scan.
    owner_program: engine-fix-masterplan
    notes: null

  - law_id: git.fresh_branch_squash_flow
    summary: >
      Branch off fresh origin/main; finish via commit→push→PR→same-day squash-merge.
      Stash stack is repo-global — never bare git stash/pop. Law source: CLAUDE.md.
    source_ref:
      - CLAUDE.md
    severity: discipline
    check_script: null
    ci_wiring: []
    selftest: false
    allowlist: null
    ratchet: null
    known_limits:
      - Enforced by team convention and code review; no static scan.
    owner_program: engine-fix-masterplan
    notes: null

  - law_id: epistemics.display_only_until_gauntleted
    summary: >
      All signals/scores are display-only until formally gauntleted. Partially
      mechanized by authority.* and nw.factor_boundaries guards (see those
      law_ids).
    source_ref:
      - CLAUDE.md
      - "law_ids: authority.cycle_pattern, authority.research_factory, nw.factor_boundaries"
    severity: discipline
    check_script: null
    ci_wiring: []
    selftest: false
    allowlist: null
    ratchet: null
    known_limits:
      - Partially mechanized by authority.* and nw.factor_boundaries; the rest is
        enforced by review.
    owner_program: engine-fix-masterplan
    notes: >
      Cross-reference: authority.cycle_pattern, authority.research_factory,
      nw.factor_boundaries.

  - law_id: epistemics.preregistered_gates
    summary: >
      All statistical experiments must use pre-registered gates. Partially
      mechanized by epistemics.trial_registration and epistemics.no_literal_ntrials.
    source_ref:
      - CLAUDE.md
      - "law_ids: epistemics.trial_registration, epistemics.no_literal_ntrials"
    severity: discipline
    check_script: null
    ci_wiring: []
    selftest: false
    allowlist: null
    ratchet: null
    known_limits:
      - Partially mechanized; pre-registration of the hypothesis itself is by review.
    owner_program: engine-fix-masterplan
    notes: null

  - law_id: epistemics.nulls_are_printed
    summary: >
      Null results must be printed and surfaced — never hidden. A null finding
      that is not displayed is an epistemics violation.
    source_ref:
      - CLAUDE.md
    severity: discipline
    check_script: null
    ci_wiring: []
    selftest: false
    allowlist: null
    ratchet: null
    known_limits:
      - Enforced by code review and report standards; no static scan.
    owner_program: engine-fix-masterplan
    notes: null

  - law_id: epistemics.research_docs_not_scanned
    summary: >
      research/*.md files are NOT scanned for promotion language or the word
      "validated". "Validated" is legitimate scientific vocabulary in research
      verdicts. The enforcement boundary is user-facing surfaces only
      (templates/, site/). FROZEN RULING: Fable 2026-07-06.
    source_ref:
      - CLAUDE.md
      - scripts/check_validated_claims.py
    severity: discipline
    check_script: null
    ci_wiring: []
    selftest: false
    allowlist: null
    ratchet: null
    known_limits:
      - Intentional carve-out; research verdicts use "validated" as scientific
        vocabulary. Enforced at the scanner level in check_validated_claims.py.
    owner_program: engine-fix-masterplan
    notes: >
      FROZEN RULING (Fable 2026-07-06): research/*.md is NOT scanned for
      promotion language. "Validated" is legitimate scientific vocabulary in
      research verdicts. The enforcement boundary is user-facing surfaces
      (templates/, site/). This rationale must not be reversed without a new
      Fable ruling.

  # ── Known-spurious CI status ───────────────────────────────────────────────

  - law_id: ci.workers_builds_spurious
    summary: >
      The "Workers Builds: macro" red X on PRs is a known external false-red from
      Cloudflare Workers. It must NOT block merges. Law source: CLAUDE.md.
    source_ref:
      - CLAUDE.md
    severity: known_spurious
    check_script: null
    ci_wiring: []
    selftest: false
    allowlist: null
    ratchet: null
    known_limits: []
    owner_program: engine-fix-masterplan
    notes: >
      External Cloudflare Workers build status; not under our control.
      Never block a merge on this red X.

  - law_id: governance.adjudication_packet
    summary: >
      Post-Fable succession bench: adjudication packets must be schema-complete
      per tier and pass hard blockers HB-1..13 — effective-tier law kills
      omission/understatement laundering, tier-2 needs a refute-stance panel
      (>=2) plus operator sign-off, authority/privacy evidence floors (HB-11/12)
      are machine-enforced, and invariant classes (A7 origination, trial-budget
      laundering, null-history deletion) are never approvable (RUL-SUCC-1..12).
    source_ref:
      - research/FABLE_SUCCESSION_OPERATING_SYSTEM.md
      - "PR #1800"
    severity: manual
    check_script: scripts/check_adjudication_packet.py
    ci_wiring: []
    selftest: true
    allowlist: null
    ratchet: null
    known_limits:
      - Not yet wired into pr_ci — RUL-SUCC-12 defers CI wiring to succession
        bench W1 (dag-conformance drift risk on same-day workflow edits). Until
        then run manually or via tests/test_adjudication_packet.py.
      - Validates the queue file only when invoked; opus-driven Research Factory
        gates invoke it as a subprocess via scripts/research_factory_decide.py.
    owner_program: fable-succession-bench
    notes: >
      W1 wires this into pr_ci and upgrades severity to hard (upgrade needs no
      ruling; downgrades do).

  - law_id: ops.nw_health_escalation
    summary: >
      Persistent Neural Web degradation must escalate to a visible ops push:
      >=3 consecutive degraded nightly runs (streak keyed on run date, NOT the
      data-vintage as_of, which freezes when upstream data is stale) fires a
      maintenance-language alert through the W6b push spine (nw_health lane).
      Born from the 5-night silent cortex outage 2026-07-04..09.
    source_ref:
      - scripts/check_nw_health_escalation.py
      - "PR #2012 (M3 organ; NW review sweep 2026-07-09)"
    severity: warn
    check_script: scripts/check_nw_health_escalation.py
    ci_wiring:
      - workflow: .github/workflows/daily.yml
        job: engine
        lane: scheduled
    selftest: false
    selftest_exempt_reason: >
      Nightly ops organ, fail-open by design; functional coverage provided by
      tests/test_nw_health_escalation.py (streak math incl. frozen-as_of
      pathology, adjacency guard, lane single-writer, verb blacklist).
    allowlist: null
    ratchet: null
    known_limits:
      - Detects persistent degradation only (>=3 nights); single-night failures
        remain ::warning-only by design (fail-open law RUL-LIVE3).
      - Reads the engine-phase health core; same-night cortex status arrives
        via cortex_source=previous_run, so cortex-only outages fire on night 4
        at the latest rather than night 3.
    owner_program: neural-web
    notes: null

  - law_id: ops.builder_failstreak_escalation
    summary: >
      A brun() builder failing many CONSECUTIVE nightly runs must escalate to a
      visible ops push instead of hiding behind the band's never-abort design:
      after each builder band, the per-slug rc scratch files advance a
      fail-streak ledger and streak >=2 fires a distinct ::error:: per frozen
      page, a step-summary section and one W6b ops alert. Each write lane
      advances exactly ONE ledger (nightly -> data/ci/builder_failstreaks.json
      in daily.yml; asia -> data/ci/builder_failstreaks_asia.json in
      asia-close.yml, the CN/HK nightly); all other lanes are report-only.
      Born from the 2026-07-16/17 build_cycle two-night silent freeze
      (cycle.html served the 07-13 render while the nightly stayed green).
    source_ref:
      - scripts/check_builder_failstreaks.py
      - "PR #2738 (daily.yml step + ledger); PR #2744 (asia-close port + lane->ledger binding)"
    severity: warn
    check_script: scripts/check_builder_failstreaks.py
    ci_wiring:
      - workflow: .github/workflows/daily.yml
        job: engine
        lane: scheduled
      - workflow: .github/workflows/asia-close.yml
        job: asia
        lane: scheduled
    selftest: false
    selftest_exempt_reason: >
      Nightly ops organ, fail-open by design (always exits 0); functional
      coverage provided by tests/test_builder_failstreaks.py (53 cases: streak
      math, same-day idempotency, gap tolerance, absent-slug decay,
      empty-rc-set guard, lane->ledger binding, multi-art-dir merge, per-lane
      alert type, trading-verb fallback).
    allowlist: null
    ratchet: null
    known_limits:
      - Observability only — never blocks a deploy; a builder red on night 1
        stays ::error-only until the streak threshold (2) is met.
      - Absent-slug decay means a mid-band cancellation that wrote only SOME rc
        files restarts the missing builders' streaks at 1 (accepted; keeping
        absent slugs produces 30-day zombie alerts on renames). Same trade-off
        when one asia band dies but the other still wrote rc files.
      - Alert dedup rides the W6b spine's (source, type_) key; the asia lane
        emits type_=fail_streak_asia so the two nightlies never suppress each
        other inside the dedup window.
    owner_program: engine-fix-masterplan
    notes: null

  - law_id: ops.ledger_heartbeat
    summary: >
      CSP-W6: On trading days where the nightly site republished, each registered
      forward ledger (risk_radar, market_state, mag7_regime, leadership_crack,
      deterioration_cascade) must have advanced its newest asof vs the previous
      run's snapshot. A stall while the site republished means stale ledger content
      backed fresh pages silently. Detection only (CSP-R1 forbids fail-dark):
      stalls append type_=ledger_stall via the W6b push_ops_alert spine and emit
      a ::warning:: annotation; the pipeline job always exits 0 and never gates
      any downstream step. Snapshot persisted in data/ci/ledger_heartbeat_state.json
      (sole writer). Trading-day aware (lib.nyse_calendar); idempotent per
      calendar date. Born from the 2026-07-16 collect-cancel-while-always()
      incident where the site republished on 07-15 stores undetected.
    source_ref:
      - scripts/check_ledger_advance.py
      - "PR #2761 (CSP-W6)"
      - research/CONTAGION_SENSING_PROPAGATION_MASTERPLAN_BY_FABLE.md
    severity: warn
    check_script: scripts/check_ledger_advance.py
    ci_wiring:
      - workflow: .github/workflows/daily.yml
        job: ledger_heartbeat
        lane: scheduled
    selftest: false
    selftest_exempt_reason: >
      Non-gating post-publish detector (exit 0 always, CSP-R1); functional
      coverage provided by tests/test_check_ledger_advance.py (16 cases: advance
      silent, stall+render, stall without render, weekend skip, double-run
      idempotent, missing file skipped, state persistence, multiple stalls,
      corrupt state fail-open, push_ops_alert failure fail-open, stalled_since
      preservation, main() exit-0, render-happened flag, asof/date field fallback).
    allowlist: null
    ratchet: null
    known_limits:
      - Stall detection requires --render-happened flag from the workflow; if the
        flag is not passed (e.g. manual invocation), no stall is escalated even
        on a genuinely frozen ledger.
      - Missing ledger files are silently skipped; a ledger that has never been
        built produces no stall even after republish.
      - The tail-scan reads the last 20 rows; a ledger whose newest row is buried
        deeper (malformed file) may produce a stale asof reading.
    owner_program: csp-program
    notes: null

  # ── Metabolism Phase 0 guards ──────────────────────────────────────────────

  - law_id: metabolism.grader_immutability
    summary: >
      The fitness-producing grader files (grade_thematic, foresight_leadlag,
      theme_placebo, qledger_falsifier, sample_qledger_placebo) are registered
      in config/grader_manifest.yml with SHA-256 hashes.  Any hash drift —
      whether from a loop PR or a human PR — is a hard CI failure.  This is the
      highest-leverage fence in the Metabolism: it prevents the loop from
      rewriting its own fitness scoreboard (R-AUT-7).
    source_ref:
      - scripts/check_grader_manifest.py
      - config/grader_manifest.yml
      - research/AUTONOMIC_LOOP_MASTERPLAN_BY_FABLE.md
    severity: hard
    check_script: scripts/check_grader_manifest.py
    ci_wiring:
      - workflow: .github/workflows/ci.yml
        job: grader-manifest
        lane: pr_ci
    selftest: true
    allowlist: null
    ratchet: null
    known_limits:
      - Covers only the five declared grader files; threshold constants
        embedded in YAML configs are not yet hashed (Phase A hardening).
      - Does not verify the quarantine replay-corpus delta — that is a T2
        operator-tap step run manually when a grader hash legitimately changes.
    owner_program: metabolism-phase0
    notes: >
      Quarantine path: any intentional grader change requires running
      'python3 scripts/check_grader_manifest.py --regen', comparing the
      fitness delta vs the frozen replay corpus, and obtaining an operator
      T2 ratification in the PR body before the manifest update merges.

  - law_id: metabolism.self_mod_fence
    summary: >
      Loop-authored PRs (branch prefix metabolism/* or claude/loop-*, OR a
      'Loop-Authored:' commit trailer) may NEVER touch the IMMUTABLE set:
      .claude/hooks/**, .github/workflows/**, config/grader_manifest.yml,
      config/capability_manifest.yml, config/metabolism_budget.yml,
      engine/neuralweb/capability_broker.py, check_self_mod_fence.py,
      check_grader_manifest.py, settings.json, and the masterplan tier table.
      Fail-closed: unclassifiable input → blocked (R-AUT-5).
    source_ref:
      - scripts/check_self_mod_fence.py
      - research/AUTONOMIC_LOOP_MASTERPLAN_BY_FABLE.md
    severity: hard
    check_script: scripts/check_self_mod_fence.py
    ci_wiring:
      - workflow: .github/workflows/ci.yml
        job: self-mod-fence
        lane: pr_ci
    selftest: true
    allowlist: null
    ratchet: null
    known_limits:
      - Attribution is by branch prefix and commit trailers, not by GitHub
        actor identity — the loop and operator share the same OAuth identity.
        A loop stage that uses a non-reserved branch name and omits the trailer
        would bypass this fence; the branch-protection rule for metabolism/*
        (F4, Phase A) adds a second layer.
      - Does not scan PR body or issue comments for trailer-equivalent text.
    owner_program: metabolism-phase0
    notes: >
      Human/operator PRs (no loop namespace, no Loop-Authored: trailer) pass
      freely even when touching immutable paths — this fence targets the loop
      specifically.  A human PR that intentionally changes a grader must also
      update config/grader_manifest.yml (grader-manifest job catches drift).

  - law_id: metabolism.capability_redline
    summary: >
      Secret VALUES must NEVER land in a tracked git artifact.  The capability
      manifest (config/capability_manifest.yml) and broker
      (engine/neuralweb/capability_broker.py) are scanned for high-entropy
      strings (40-char hex, 20+ base64) and value-capture patterns (secrets.*
      references, os.environ[...] assignments to tracked literals).  Any finding
      is a hard CI failure.  This is the single unforgivable defect in the
      Metabolism (§3 of masterplan / THE REDLINE).
    source_ref:
      - scripts/check_capability_redline.py
      - engine/neuralweb/capability_broker.py
      - config/capability_manifest.yml
      - research/AUTONOMIC_LOOP_MASTERPLAN_BY_FABLE.md
    severity: hard
    check_script: scripts/check_capability_redline.py
    ci_wiring:
      - workflow: .github/workflows/ci.yml
        job: capability-broker
        lane: pr_ci
    selftest: true
    allowlist: null
    ratchet: null
    known_limits:
      - Entropy scan has false-positive risk on long base64-looking strings
        in comments; known-safe names are stripped before the scan.
      - Does not scan the runtime environment (os.environ values at execution
        time) — that is outside the scope of a static git-artifact scan.
      - Does not catch secrets stored in GitHub Actions env-var YAML steps
        (those are scanned by the workflow YAML paths in F2/ci.yml).
    owner_program: metabolism-phase0
    notes: >
      The broker resolves capability IDs to ref NAMES only — callers read
      os.environ[ref_name] independently.  The broker is never in the secret-value
      handling path.  Audit tape records attribution (run_id, actor, sha) but
      no values.

  - law_id: metabolism.blocklist_drift
    summary: >
      One kill-registry, compiled everywhere (R-V4-5).  research/DO_NOT_REBUILD.md
      is the single source of standing kills; scripts/compile_loop_blocklists.py
      deterministically compiles it to config/compiled_kill_registry.yml plus the
      generated block of config/signal_foundry_blocklist.yml.  This check
      recompiles and byte-diffs BOTH committed artifacts, and hard-fails on table
      rows outside DO_NOT_REBUILD.md sections 1-4 (the compiler parses only 1-4;
      a row elsewhere silently never reaches enforcement — MRI-R38 CPI row, found
      live).  Drift heals with `--fix`; prevention is the PostToolUse hook
      .claude/hooks/blocklist_regen_guard.py, which auto-regens on any harness
      Edit/Write to DO_NOT_REBUILD.md in the editing session's own tree.
    source_ref:
      - scripts/check_blocklist_drift.py
      - scripts/compile_loop_blocklists.py
      - config/compiled_kill_registry.yml
      - config/signal_foundry_blocklist.yml
      - .claude/hooks/blocklist_regen_guard.py
      - research/METABOLISM_V4_FIRST_BREATH_BY_FABLE.md
    severity: hard
    check_script: scripts/check_blocklist_drift.py
    ci_wiring:
      - workflow: .github/workflows/ci.yml
        job: capability-broker
        lane: pr_ci
      - workflow: .claude/settings.json
        job: PostToolUse
        lane: hook
    selftest: false
    selftest_exempt_reason: >
      Pure recompile-and-byte-diff against committed artifacts — the discriminating
      failure modes (tampered compiled output, tampered SF generated block,
      misplaced row, --fix heal, hook end-to-end) are covered by
      tests/test_metabolism_v4_w3_mission.py in the metabolism v4 W3 CI suite.
    allowlist: null
    ratchet: null
    known_limits:
      - The compiler's topic→pattern heuristic (3 significant words) provides
        baseline mechanical coverage of killed topics; hand-curated loop
        blocklist entries remain authoritative for phrasings it cannot match.
      - The auto-regen hook fires only on harness Edit/Write tool calls; shell
        appends (cat >>) bypass it — the CI byte-diff remains the backstop, and
        the immune blocklist-drift recipe remains the heal path for main.
      - A misplaced row is detected but not auto-fixable (--fix and the immune
        heal_cmd cannot move a row between sections); the immune lane will
        bounce off it bounded-then-park per R-V7-7.
    owner_program: metabolism-v4
    notes: >
      Established by the v4 sweep finding F1: five propose-test-learn loops held
      divergent blocklists with no cross-read — a construction killed in one loop
      was invisible to the others.

  - law_id: identity.stable_key_vendor_alias_boundary
    summary: >
      Stable universe, page, and ledger keys must not fork when a live listing
      moves to a new vendor symbol. Every current universe key that is absent
      from the latest listing directory must resolve through lib.ticker_aliases
      to one currently listed vendor symbol; alias targets must be unique and
      must not also appear as a second universe member for the same company.
    source_ref:
      - scripts/check_symbol_rename_drift.py
      - lib/ticker_aliases.py
      - tests/test_ticker_alias_boundary.py
      - data/qledger/retired_symbol_disclosures.json
    severity: hard
    check_script: scripts/check_symbol_rename_drift.py
    ci_wiring:
      - workflow: .github/workflows/ci.yml
        job: house-law-registry
        lane: pr_ci
    selftest: false
    selftest_exempt_reason: >
      The direct CI lane runs the live guard and tests/test_ticker_alias_boundary.py;
      that suite pins stable-to-vendor resolution, duplicate-store prevention,
      provider-key collapse, dead-name exclusion, disclosure minima, and the
      committed repository boundary end to end.
    allowlist: null
    ratchet: null
    known_limits:
      - The hard gate evaluates the latest tracked listing directory and current
        universe snapshots; it does not rewrite historical filing or membership
        symbols, which retain the identifiers known at their event time.
      - Stale open point-in-time membership intervals are reported as warnings
        because closing them requires separate dated membership evidence.
      - Class-share spellings use the repository's bounded dot/dash/base-symbol
        tolerance and do not constitute a general company-identity mapper.
    owner_program: stable-symbol-identity
    notes: >
      The only supported seam fetches under the current vendor symbol and stores
      under the permanent membership key. The guard must never mint a second
      price store or reinterpret historical identity.

  - law_id: flow_continuity.statement_tape_schema
    summary: >
      Statement-tape schema guard (FL-E / FC-R9).  Every non-comment row of
      data/statement_tape/registry.jsonl must satisfy the binding schema
      (date, speaker, venue, quote, stance_tag enum, added_by, ISO added_at),
      and PS-R1 is enforced at the key level: any field name implying
      prediction or scoring (signal/forecast/prediction/probability/edge) is a
      hard failure — the tape records what was SAID, never a signal.
    source_ref:
      - scripts/check_statement_tape.py
      - data/statement_tape/registry.jsonl
      - tests/test_statement_tape.py
      - "PR #2297"
    severity: hard
    check_script: scripts/check_statement_tape.py
    ci_wiring:
      - workflow: .github/workflows/ci.yml
        job: engine-render-guards
        lane: pr_ci
    selftest: false
    selftest_exempt_reason: >
      Guard logic (schema, stance enum, PS-R1 key ban, paraphrase warning) is
      covered by tests/test_statement_tape.py in the same engine-render-guards
      pytest whitelist; the dedicated CI step runs the guard against the
      committed registry directly.
    allowlist: null
    ratchet: null
    known_limits:
      - Registry is operator/session-appended only (no automated collector per
        the FL-E scope ruling); the guard validates schema and key-name
        discipline, not the factual accuracy of quotes.
    owner_program: flow-continuity
    notes: >
      Registered by the post-#2297 census fix (rode along in #2311): the guard
      shipped in #2297 wired via pytest only, which the registry census could
      not see; the census red-flagged every later PR.

  - law_id: theme_graph.edge_contract
    summary: >
      The GMI theme graph's structural contract (masterplan §4.1/§4.4, gate G0.2).
      All three stores keep their exact column set and order and validate against
      contracts/theme_graph/*.v1.schema.json; every enum value and every company
      node id is scanned in full; EVERY edge cites at least one evidence_ref that
      resolves to an evidence row with a parseable dated published_at (the general
      form of the semantic-leakage law, with source_class=llm_proposed_ratified
      refused outright without one); identity_epoch >= 2 requires a ratified row in
      config/theme_graph_identity_breaks.yml; and every closed edge in the full
      history still resolves in the latest-belief view, so a closed membership stays
      findable instead of merely absent.
    source_ref:
      - scripts/check_theme_graph_contracts.py
      - contracts/theme_graph/README.md
      - research/GLOBAL_MARKET_INTELLIGENCE_MASTERPLAN_BY_FABLE.md
      - tests/test_theme_graph_contracts.py
    severity: hard
    check_script: scripts/check_theme_graph_contracts.py
    ci_wiring:
      - workflow: .github/ci/legacy-jobs.yml
        job: unrun-intl-libraries
        lane: pr_ci
      - workflow: .github/workflows/daily.yml
        job: engine
        lane: scheduled
    selftest: true
    allowlist: null
    ratchet: null
    known_limits:
      - >-
        The nightly invocation is ADVISORY (rc 0 on a breach, one line-start warning
        annotation) because the graph is display-tier with all six authority booleans
        false, so a contract breach must not take the collect lane down. --strict is
        what CI runs.
      - >-
        A missing store is INDETERMINATE, not a breach — a sparse checkout carries no
        data/ and the state before the first backfill is not a failure.
      - >-
        jsonschema runs on an evenly-spaced sample of <=200 rows per store; enums and
        the company-id grammar are full-scanned, so a bad value outside the sample is
        still caught, but an exotic per-row schema violation outside it could be missed.
      - The guard checks that evidence is DATED and resolvable, never that it is true.
    owner_program: gmi-theme-graph
    notes: >
      Registered with W1b (the wave that created the store). The evidence-resolution and
      llm_proposed_ratified checks are structural rather than empirical: zero
      llm_proposed_ratified edges exist today, and the point of the check is that the
      first one cannot land quietly. Fixture dates in --selftest are constants with no
      relation to the wall clock — a guard whose fixtures age is a scheduled red.

  - law_id: design.system_ratchet
    summary: >
      The design system can only hold if drift is measured before it is banned.
      templates/ carries ~19k hand-rolled decisions the token layer already owns
      — colour literals, font stacks, raw radii, shadow palettes declared as
      custom properties in page-scoped selectors — and each one is a surface that
      will diverge the next time theme.css moves. A gate that reds on all of them
      on day one is a gate somebody disables in week one, so this guard ships
      REPORT-ONLY (R0): it prints per-rule counts plus capped exemplars on every
      PR and always exits 0. The blocking arm is written and tested from the same
      commit rather than deferred — `--mode enforce` exits non-zero on the four
      mechanical rules (colour literal, font-family literal, non-token
      border-radius, literal-valued custom property outside theme.css) — but it
      only ever looks at templates a page-registry row marks
      `design_system.compliant`, or at templates the registry has never seen.
      That scoping IS the ratchet: a migrated surface cannot regress, a brand-new
      surface is born compliant, and the backlog is retired one packet at a time
      instead of in one heroic red-build weekend. Rules 5-8 (new `*-card`
      classes, banned Tier-1 vocabulary, inline `<style>` byte size, emoji
      codepoints) are heuristics or measurements and stay warn-tier for as long
      as this script exists.
    source_ref:
      - scripts/check_design_system.py
      - tests/test_check_design_system.py
      - config/product_experience/page_registry_overrides.yml
      - scripts/build_product_page_registry.py
      - "PR #5459 (design-system constitution + migration factory)"
    severity: warn
    check_script: scripts/check_design_system.py
    ci_wiring:
      - workflow: .github/ci/legacy-jobs.yml
        job: validated-claims
        lane: pr_ci
    selftest: true
    allowlist: null
    ratchet:
      date: "2026-10-01"
      action: >-
        R0 is report-only. The R1 blocking flip is ONE later PR that changes this
        entry's severity to hard AND switches the wired step to `--mode enforce
        --registry` in lockstep — never one without the other, or the registry
        describes teeth the job does not have. R1 blocking flip rides the first
        migration-packet PR; if none has landed by this date, review whether the
        Sol §J.9 chain is still blocked.
    known_limits:
      - >-
        Static text scan, not a rendered-DOM check: it cannot see a colour a
        builder interpolates at render time, and it deliberately blanks Jinja
        expressions and fragment refs (href="#abc", url(#fade)) so a template
        decision is not reported as a stylesheet literal.
      - >-
        Rule 5 (`*-card` classes) is a REGEX HEURISTIC — it cannot tell a new
        component from a rename, so it counts both. Rule 6 ships a deliberately
        small seed vocabulary; the full Tier-1 doctrine lint needs a per-surface
        allowlist (methodology and calibration pages legitimately EXPLAIN the
        terms it bans) and is a later wave.
      - >-
        Rule 7 records inline `<style>` byte size but enforces no growth budget
        yet; R0 exists partly to establish that baseline.
      - >-
        `--mode enforce` with no readable `--registry` treats EVERY template as
        new and therefore blocking. That is fail-closed and it warns when it
        happens, but it is not a mode to wire by accident.
    owner_program: mastermind-design-system
    notes: >
      Wired into the `validated-claims` legacy job on purpose: that job already
      owns templates/** in scripts/run_ci_pack.py's derived scope, so adding this
      step costs the narrow-diff selector NOTHING, where wiring it into a job
      that does not own templates/ would hand that job the whole tree and push
      the pack-selection ceiling. CLOSURE LEGIBILITY is load-bearing in the
      script and its test: both name exactly one scan root, `templates`, and make
      no subprocess call, because the scope analyzer widens an opaque edge to
      every root the module names (incident #5396). Annotations are capped at 10
      TOTAL, summary first, with full detail printed as plain lines below —
      19,057 findings would otherwise bury the Actions summary. `--self-check`
      (alias `--selftest`) proves each rule fires on its own fixture, that a
      clean fixture passes, and that the traversal itself works; a self-check
      derived from what it checks cannot fail, so the fixtures are canned in a
      tempdir rather than read from the live estate.
  - law_id: ui.design_system_forward_ratchet
    summary: >
      design.system_ratchet measures the estate but cannot block it: ~19k
      inherited findings in templates/ mean a whole-estate gate is a gate
      somebody disables in week one. This law is the forward-only half. In
      `--mode enforce-added` the checker parses the PR's own unified diff and
      blocks ONLY on findings whose line number was ADDED by that diff, so a
      newly introduced raw colour, font stack, non-token radius, literal-valued
      custom property, emoji-as-UI, or parallel token root fails closed while
      the identical decision sitting on an unchanged line one row above stays
      green. Pre-existing debt reports and cannot grow. The blocking set is the
      four mechanical rules plus `emoji` and the new `parallel-token-root` rule,
      which fires on a `:root` custom-property family declared anywhere except
      templates/theme.css — theme.css is the sole token authority, and a second
      root is how a page mints a private palette the design system cannot see.
    source_ref:
      - scripts/check_design_system.py
      - tests/test_check_design_system.py
      - research/THEME_PARITY_RATCHET_PRESENTATION_CONVERGENCE_ARCHITECTURE.md
      - docs/superpowers/plans/2026-08-27-tp0-theme-parity-governance-v2.md
    severity: hard
    check_script: scripts/check_design_system.py
    ci_wiring:
      - workflow: .github/ci/legacy-jobs.yml
        job: design-governance
        lane: pr_ci
    selftest: true
    allowlist: null
    ratchet: null
    known_limits:
      - >-
        Changed-line only by construction: moving an existing violation to a new
        line makes it newly blocking, and editing a line adjacent to old debt
        does not. That asymmetry is the intended forward-only bargain, not a bug.
      - >-
        Needs a real merge base. On a shallow checkout `git merge-base` fails and
        the wired step exits 0 with an explicit message rather than guarding
        nothing silently — the fetch-depth 0 in the job is load-bearing.
      - >-
        `parallel-token-root` is naive about nesting: it fires on a `:root` block
        declaring custom properties outside theme.css even when the values are
        pure token derivations. Scoped (non-`:root`) derived properties stay legal.
      - >-
        The `emoji` rule BLOCKS on a narrower set than it REPORTS. Rule 8's
        census regex covers Misc Symbols and Dingbats (U+2600-27BF), where the
        estate already carries ~1,597 typographic glyphs across 145 template
        files — `⚠`×188, `✓`×147, `★`×82, `✕`×59. Blocking on those would red an
        added table row containing a checkmark, so enforce-added blocks only on
        pictographic planes and regional indicators. `✓`/`★`/`⚠` are typography;
        the architecture authorizes blocking new emoji-as-UI, not new text
        symbols. Consequence to know: a pictographic glyph IS still blocking, so
        editing an added line carrying the `🌙` theme-toggle icon in
        templates/theme.js will red — correctly, per the doctrine's ban on
        emoji-as-icon, but it will surprise whoever hits it first.
    owner_program: theme-parity-ratchet
    notes: >
      Shares its check_script with design.system_ratchet on purpose — one script,
      two laws, because `--mode report` (estate census, warn) and `--mode
      enforce-added` (changed lines, hard) are genuinely different contracts and
      collapsing them would hide which one is blocking. CLOSURE LEGIBILITY still
      holds and was re-verified for this change: the module names exactly one scan
      root, `templates`, and makes no subprocess call, so run_ci_pack.py's path
      inference is unchanged. The diff is handed in via `--diff-file` (`-` reads
      stdin); the checker never shells out to git. Runtime JS is deliberately NOT
      in scope here — widening this script to site/ would add a second path
      literal and break that closure — so it gets its own guard,
      ui.runtime_style_injection.
  - law_id: ui.runtime_style_injection
    summary: >
      The design-system checker scans templates/ only, so a builder could obey
      the design system there while authoring the real material system as a
      multi-kilobyte runtime CSS string inside page/composer JavaScript —
      invisible to every design gate. That is the enforcement escape hatch the
      theme-parity program exists to close. This guard counts four injection
      signatures (createElement('style'), style/css .textContent assignment,
      .insertRule(, and inline <style markup) per user-facing .js under
      templates/ and site/, against a frozen per-file baseline. A new injecting
      file absent from the baseline reds; any count above its pinned allowance
      reds. A count BELOW its allowance ALSO reds, demanding the same PR shrink
      the stale budget — that third case is what makes this a ratchet rather
      than a high-water mark. An advisory notice would leave freed headroom
      lying around for a later PR to silently re-inflate into, which is exactly
      the regression the fence exists to prevent.
    source_ref:
      - scripts/check_runtime_style_injection.py
      - tests/test_check_runtime_style_injection.py
      - config/runtime_style_injection_allowlist.json
      - research/THEME_PARITY_RATCHET_PRESENTATION_CONVERGENCE_ARCHITECTURE.md
    severity: hard
    check_script: scripts/check_runtime_style_injection.py
    ci_wiring:
      - workflow: .github/ci/legacy-jobs.yml
        job: design-governance
        lane: pr_ci
    selftest: true
    allowlist: config/runtime_style_injection_allowlist.json
    ratchet:
      date: "2026-12-01"
      action: >-
        The Canada and HK composer budgets (site/canada-stock-v36.js and
        site/hk-stock-v36.js, 2 hits each at baseline) go to ZERO in TP-1/TP-2 as
        their CSS is extracted to templates/stock-dashboard.css. If those entries
        still carry a non-zero allowance by this date, the extraction wave stalled
        — review rather than renewing the budget.
    known_limits:
      - >-
        Textual signature count, not semantic analysis: it cannot tell a
        three-line focus-ring injection from a 40KB material system, and a
        determined builder can evade the exact patterns. It is a ratchet against
        casual growth, not a proof of absence.
      - >-
        Specific uncovered shapes, named rather than implied: constructable
        stylesheets (`new CSSStyleSheet()` / `adoptedStyleSheets` /
        `replaceSync`) are not matched at all, nor is `el.style.cssText=`,
        `setAttribute('style', ...)`, or a `.textContent =` assignment whose
        receiver variable is not literally named `style` or `css`. A future wave
        that adopts constructable stylesheets must extend PATTERNS in the same
        PR or this fence silently stops covering the estate's real mechanism.
      - >-
        A self-raised budget passes: a PR may add a new injecting file and
        regenerate the baseline in the same commit, and the only friction is a
        reviewer reading the JSON diff. That visibility IS the control — the
        allowlist diff is deliberately human-legible — but it is review-tier,
        not machine-tier. `generated_from` is recorded, never validated against
        the PR head.
      - >-
        Vendored minified libraries (plotly-2.32.0.min.js) are counted and frozen
        like anything else. A vendor upgrade changes the count and requires a
        visible baseline update. That is intended — a vendor exemption class would
        be the same bypass in another costume.
      - >-
        theme.js is the sanctioned shared theme owner and the largest single
        budget (site/theme.js 12, templates/theme.js 10). Those are legitimate,
        not debt to drive to zero.
    owner_program: theme-parity-ratchet
    notes: >
      Baseline generated only via `--emit-baseline --generated-from "$(git
      rev-parse HEAD)"`; the script refuses to guess a sha, and no count was ever
      hand-written. Measured at 84d6831: 189 .js scanned, 46 injecting, 91 hits
      (create_style 66, style_markup 14, style_text 11, insert_rule 0). The guard
      REFUSES on a sparse checkout missing site/ rather than reporting a vacuous
      pass over a tree it cannot see — the same failure shape as
      check_template_site_sync.py, and the reason a sparse worktree must opt in
      before regenerating the baseline.
  - law_id: ui.visual_evidence_manifest
    summary: >
      "The light page opens and functions" has been accepted as equivalent to
      "light mode was deliberately designed", which is how a premium dark surface
      shipped with a degraded light plane. This guard makes a material visual
      change owe committed evidence a reviewer can actually look at: a receipt
      (mastermind.page_evidence_receipt.v1, exactly three keys — schema,
      changed_paths, manifest) maps the PR's changed presentation paths to an
      EXISTING mastermind.p0_evidence.v2 manifest, which must contain the eight
      rest cells (desktop/mobile x en/zh x dark/light), each captured, with the
      applied theme and locale matching what was requested and the referenced PNG
      present. CI checks existence and state identity ONLY — a human/Opus reviewer
      owns hierarchy, material depth, semantic colour and taste.
    source_ref:
      - scripts/check_ui_visual_evidence.py
      - tests/test_check_ui_visual_evidence.py
      - scripts/capture_page_evidence.py
      - docs/product_experience/PAGE_EVIDENCE_HARNESS.md
      - research/THEME_PARITY_RATCHET_PRESENTATION_CONVERGENCE_IMPLEMENTATION_AMENDMENT_1.md
    severity: hard
    check_script: scripts/check_ui_visual_evidence.py
    ci_wiring:
      - workflow: .github/ci/legacy-jobs.yml
        job: design-governance
        lane: pr_ci
    selftest: true
    allowlist: null
    ratchet: null
    known_limits:
      - >-
        Material-change detection is deliberately narrow and mechanical (added
        lines in a templates CSS file, an added inline <style in a template, an
        added runtime-style signature in user-facing JS). It will miss a visual
        regression delivered purely through markup reordering, and that is the
        correct trade: this gate fails closed, so a loose detector becomes a
        fleet-wide block on ordinary PRs.
      - >-
        It validates every page in the manifest a receipt references. The
        convention is therefore a migration-scoped manifest; pointing a receipt at
        a large multi-page estate manifest would demand eight rest cells for every
        page in it.
      - >-
        Existence and identity only. A committed screenshot of a genuinely ugly
        light plane passes CI — refusing that verdict to a machine is the point,
        and the design review is where it is caught.
      - >-
        Known false positive, fail-closed by choice: adding a line INSIDE a
        pre-existing multi-line CSS comment is treated as material and will
        demand an evidence matrix. The `/*` opener is unchanged context outside
        the hunk, so from the diff alone the added line is indistinguishable
        from a real declaration, and this gate errs toward demanding evidence
        rather than skipping silently. Adding a whole comment BLOCK is handled
        correctly — the state machine sees the opener and close together. If you
        hit this, add the comment in the same hunk as its `/*`, or attach the
        receipt.
    owner_program: theme-parity-ratchet
    notes: >
      Amendment 1 (A1) is binding here: this must NOT become a second evidence
      plane. The canonical producer stays scripts/capture_page_evidence.py and the
      canonical schema stays mastermind.p0_evidence.v2 — the receipt only MAPS to
      it and is forbidden extra keys, so it cannot grow into a parallel manifest.
      The viewport identity is asserted on `viewport_width` (1440 desktop / 390
      mobile), NOT the PNG's pixel `width`: capture_page_evidence.py emits both,
      and a full-page capture's pixel width need not equal the requested viewport,
      so the plan's original `width` assertion would have checked the wrong
      quantity. All manifest reads are defensive — a failed-capture row carries
      only captured:false plus a reason, so direct indexing would crash the guard
      instead of reporting the gap.
  - law_id: data.append_only_assertions
    summary: >
      A historical assertion over an append-only store must not become false
      merely because a VALID NEW ROW was appended. The rule is MONOTONICITY, not
      equality: `len(rows) == 28` and `ledger_ids <= graded_ids` are fragile;
      `>= floor`, `x in store`, `set(keys) == FIELDS`, a constant-bounded prefix
      slice, a per-row schema contract, and two reads of the same store in one
      run are safe. Covers BOTH shapes the defect takes — an assert over store
      content, and a CI gate whose failing exit code is a function of it.
    source_ref:
      - scripts/check_append_only_assertions.py
      - tests/test_append_only_assertions.py
      - config/synapse.yml
      - research/EVAL_OS_GUARDLAW_T3_HANDOFF_2026-08-12.md
    severity: discipline
    check_script: scripts/check_append_only_assertions.py
    ci_wiring: []
    selftest: true
    allowlist: null
    ratchet: null
    known_limits:
      - >-
        PRODUCER SIBLING (false positive). A store compared to a receipt written by
        the SAME producer run is safe because both move in lockstep, but is reported.
        Observed at tests/test_chronicle.py:2294, where the manifest records rows=5051
        and events.jsonl is 5051 lines. Closing it needs a `producer:` sibling lookup
        in config/synapse.yml.
      - >-
        WITHIN-RUN VIA AN INTERMEDIARY (false positive). When one side reaches the
        store through a call the scanner cannot see into, only the other side carries
        taint. Observed at tests/test_personality_relief_hazard.py:400, where `state`
        comes back from prh.update() over the same ledger being counted.
      - >-
        FROZEN FIRST ROW (false positive). `registration, *events = rows` makes
        `registration` row 0, which an append cannot change; reported anyway at
        tests/test_personality_relief_hazard.py:357. The BYTE form of the same idea
        (`raw[:50_790]`) IS recognised and is not reported.
      - >-
        UNREGISTERED STORE (false negative). A live append-only store missing from
        config/synapse.yml is invisible. data/basket_turn/ledger.jsonl is tracked in
        git and pinned by `assert len(live) == 8` at
        tests/test_basket_membership_stamps.py:259, and this guard reports zero
        findings in that file because synapse.yml carries data/us_basket_turn/ and
        data/china_basket_turn/ but not that path.
      - >-
        ROW-GRAIN MEMBERSHIP (false negative, deliberate). The per-row carve-out
        cannot distinguish a schema contract (`row["kind"] == "event"`, legal) from an
        enumeration of existing ids (`row["id"] in FROZEN_IDS`, fragile) — they are
        lexically identical. The carve-out wins. All four motivating defects are
        whole-store, and without it the guard reports about 5x the findings it can
        justify.
      - >-
        Scope is tests/ and scripts/, JSONL stores only, and the exit-code family is
        scoped to scripts/check_*.py plus registered check_scripts. A path arriving
        via a fixture, another module's constant, or a function parameter is not
        resolved.
    owner_program: mastermind-intelligence-evaluation-os
    notes: >
      DISCIPLINE WITH EMPTY ci_wiring ON PURPOSE (2026-08-13). The guard reports 47
      pre-existing findings on main, none of which a PR author wrote, so wiring it as
      a gate on day one is how a guard gets disabled rather than obeyed. What CI runs
      today is the PROOF THAT THE GUARD WORKS, not a judgement of the tree: --selftest
      (7 known-bad constructions that must be caught, 13 legal ones that must stay
      clean) plus tests/test_append_only_assertions.py, which mutates the detector ten
      ways and the fixtures nine ways and fails if any mutation survives. Both run in
      the always-on house-law-registry job, appended last so they cannot mask the
      meta-guard census ahead of them.
      PROMOTION PLAN. Step 1 (target 2026-09-15): drive the 47 findings down by fixing
      the true positives — including tests/test_market_memory_production_records.py:116,
      which pins data/options_signal_episode/episodes.jsonl at 384 rows while the
      committed store is 1,206 lines, and scripts/check_qledger_metric_validity.py:244,
      whose --strict path is a function of data/qledger/claims.jsonl content and would
      become a fleet-wide scheduled red the day anything arms it. Step 2: close the
      PRODUCER SIBLING false positive with a synapse `producer:` lookup, which retires
      the largest legitimate-pin class. Step 3 (only once the count is zero and stays
      zero for two weeks): promote to `hard` with `--strict` in the house-law-registry
      job and record the promotion here. Do NOT promote on a nonzero count — the
      previous attempt at this law was promoted to hard while itself violating the law
      it shipped, and was structurally blind to that.
  - law_id: ops.nightly_liveness
    summary: >
      The authoritative US nightly (daily.yml / Build B) must produce a run for every
      completed NYSE session, that run must conclude success, and the Prophet store
      must actually advance to that session. All three are separate failures and each
      is observable on its own.
    source_ref:
      - scripts/check_nightly_liveness.py
      - tests/test_workflow_file_size.py
    severity: hard
    check_script: scripts/check_nightly_liveness.py
    ci_wiring:
      - workflow: .github/workflows/nightly-liveness.yml
        job: liveness
        lane: scheduled
      - workflow: .github/ci/legacy-jobs.yml
        job: dag-conformance
        lane: pr_ci
    selftest: true
    allowlist: null
    ratchet: null
    known_limits:
      - >-
        Detection floor is the schedule: 08:00Z and 14:00Z. A bake that dies at 23:00Z
        is reported the next morning, not within the hour. That is deliberate — the
        nightly legitimately runs for hours, so a tighter slot could only report
        INDETERMINATE and would train the operator to ignore the channel.
      - >-
        Check C reads the COMMITTED site/prophet/index.json, so it proves the store
        advanced in git, not that the VPS served it. The 3-min pull and the VPS-side
        freshness_sentinel own that last hop.
      - >-
        Blindness (API error, missing artifact, unparseable stamp) is INDETERMINATE by
        design and exits 0. A guard that is permanently blind therefore reads green;
        test_default_repo_matches_the_git_remote pins the one input most likely to
        cause that silently.
      - >-
        Watches Build B only. Build A (closing-bell.yml) ran green through the entire
        2026-08-11/12 outage while the board it re-rendered still read
        price_through=2026-08-10, so its health proves nothing about the ledger.
    owner_program: prophet-us-eyes-open
    notes: >
      Registered after the 2026-08-11/12 two-session outage, in which the US nightly
      went dark and no existing instrument reported it. heartbeat.yml could not:
      max_age_hours=96 is sized so a Friday bake still looks alive on Monday, and it
      runs on the same macstudio pool as the lane it watches. freshness_sentinel could
      not: PROPHET_MAX_SESSIONS_BEHIND=1 is correct (between the close and the bake,
      one behind IS healthy) and therefore blind for ~24h. Both measure DATA; what
      broke was the SCHEDULE, after #5362 pushed daily.yml past GitHub's silent
      ~512,000-byte processing cap. This guard asks GitHub whether a run was created
      at all, which is the only question that sees a stranded workflow file in hours.
      Fixture dates in --selftest are constants with no relation to the wall clock.

  - law_id: governance.intelligence_registry_integrity
    summary: >
      The Mastermind engine registry is a DERIVED ON-DEMAND VIEW, never hand-authored and
      NEVER COMMITTED. It is a pure function of config/synapse.yml, config/qual_ladder.yml,
      an AST scan of producer source, data/species/registry.json and data/qledger/, built
      in memory by scripts/build_intelligence_registry.py and read by consumers either as
      an import or off `--json` stdout. Only four curated keys may live in
      config/intelligence_registry_overlay.yml. This law fires ONLY on INVARIANTS of the
      derivation and of the hand-edited overlay: a synapse artifact mapping to zero or to
      more than one engine (the partition must be TOTAL and DISJOINT), an authority value
      that names no derivation rule or no artifact that carries it (attribution), an
      overlay key outside the four-key allowlist, an overlay key that is DERIVED, an
      overlay row for a cell the partition does not contain (orphan), a curated
      not_an_engine exclusion of an AUTHORITY-BEARING or evaluated cell, an unknown enum
      value, a missing required field, a null ledger, or an exclusion with an empty reason
      or one that does not record what it deletes. The key allowlist IS the executable form
      of DNR:KILL-PARALLEL-KNOWLEDGE-BASE — without it the overlay silently becomes the
      hand-authored engine list that row forbids.
    source_ref:
      - engine/intelligence_registry.py
      - scripts/build_intelligence_registry.py
      - scripts/check_intelligence_registry.py
      - config/intelligence_registry_overlay.yml
      - research/MASTERMIND_INTELLIGENCE_OS_V1_PLAN.md
    severity: warn
    check_script: scripts/check_intelligence_registry.py
    ci_wiring:
      - workflow: .github/ci/legacy-jobs.yml
        job: intelligence-registry
        lane: pr_ci
    selftest: true
    allowlist: config/intelligence_registry_overlay.yml
    ratchet: null
    known_limits:
      - >-
        THERE IS NO DRIFT GUARD, NO COMMITTED ARTIFACT AND NO `--check` EQUALITY MODE, and
        none may be re-added. Two earlier rounds committed a generated
        data/intelligence_registry.json plus a generated Markdown mirror and pinned them by
        equality against a "stable" input; both were scheduled fleet-wide reds and the pin
        merely RELOCATED between rounds. Round 1 pinned against data/qledger/claims.jsonl
        (append-only); round 2 moved the pin to config/synapse.yml, a MORE volatile input.
        Both rounds cited churn figures taken off a SHALLOW clone (13 and 26 commits in 14
        days) and both UNDERSTATED it. RE-MEASURED ON FULL HISTORY 2026-08-14 (unshallowed,
        11,971 commits reachable): data/qledger/claims.jsonl 70 commits and
        config/synapse.yml 69 commits in the trailing 14 days — 5.4x and 2.7x the shallow
        figures, so the conclusion is strengthened, not softened. The third candidate,
        data/species/registry.json, looked stable at "one commit in its whole history";
        on full history it has 17, every one an adjudication PR (last #4358, 2026-08-03).
        It is genuinely low-churn — but a pin needs stability of CONTENT, and an
        adjudication rewrites exactly the rows a registry derives from. There is no stable
        input on this repo to pin against, so nothing generated is committed and nothing
        is pinned.
      - >-
        SEVERITY IS `warn`, NOT `hard`, EVEN THOUGH THESE ARE PR-CAUSED INVARIANTS. The
        two previous rounds each argued a hard pin was legitimate because "every input
        moves only via a PR", and both were refuted 3/3 by measurement rather than by
        argument. The invariants here really are green by construction, but the same
        sentence was true of the drift pin, so the severity stays advisory until T7 has run
        the gate against the live corpus for a sustained period. Structural violations
        still exit non-zero on the guard's own exit code, so a real violation is visible in
        the job log; `warn` governs how loudly the registry treats it, not whether the
        script reports it.
      - >-
        FAIL CLOSED, NOT SILENT — AND KEYED ON THE PLANE THAT WENT BLIND (ruling
        2026-08-14, amended the same day). The summary line ALWAYS carries an `inputs=`
        clause; when any input could not be read it reads `inputs=INCOMPLETE (...)`, NAMES
        what was missed AND NAMES ITS PLANE, and the COULD NOT LOOK annotation and the
        --json `unreadable_inputs`/`unreadable_by_plane` carry the same facts. What differs
        by plane is only the EXIT CODE. PR-PLANE (config/synapse.yml, the overlay,
        config/qual_ladder.yml, data/species/registry.json, the Article-2 module table,
        producer SOURCE — all config and code, all moved only by a pull request) exits
        non-zero on EVERY run, with or without --strict: a PR author can cause it and a PR
        author can fix it. DATA-PLANE is exactly ONE input, data/qledger/claims.jsonl, the
        only one an automated lane advances (70 commits in the 14 days to 2026-08-14); it
        is fully reported and exits non-zero only under --strict. THE FIRST FORM OF THIS
        RULING GATED ON ANY INCOMPLETE READ, which handed the nightly the power to red
        every PR in flight: one truncated line of 46,696 in the claim store reddened the
        whole intelligence-registry job, for a file no PR author touches — the
        scheduled-fleet-red shape this program has been refuted for three rounds running,
        arriving through the fix for a different one. DEFERRED, WITH AN OWNER: no lane
        passes --strict yet, so corruption of the claim store currently ALERTS everywhere
        and GATES nowhere; wiring a nightly-side --strict run is T7's, in the same wave
        that drains the C-1 backlog and flips this law to `hard`. "Unreadable" includes
        PARTIAL: a store that opens but whose lines do not all parse is named with its
        count (`data/qledger/claims.jsonl (2 unparseable line(s) of 4)`) while the rows
        that DID parse stay in the view — the null law requires the incompleteness be
        REPRESENTED, not that the readable half be discarded. A document that reads but
        does not PARSE is the same state as one that could not be read: synapse -> NOT
        CHECKED + exit 1, overlay and qual_ladder -> treated as absent and NAMED, never a
        bare traceback. Sparse agent worktrees carry no data/ on disk while ~39,900 data
        paths are tracked in HEAD, so every read — including producer SOURCE and the
        qual_ladder_ref probe — goes through the worktree-then-`git show` ladder, and a
        producer readable from neither is announced rather than silently skipped.
      - >-
        `not_an_engine` is the most destructive of the four overlay keys and was once the
        least gated: a 3-character reason removed an authority-bearing engine with every
        law green (measured 2026-08-12: 378 -> 376 engines, gate_size 5 -> 4). It now
        requires ratified_by, a date, a >=40-character reason and a DNR:<KEY>-shaped
        citation if it cites one; exclusions are derived BEFORE being excluded so each
        records its would_be_authority; a curated exclusion of an authority-bearing or
        scored/confirmer/shadow cell is a structural violation; and excluded rows are still
        audited for content findings, so an exclusion cannot deflate the backlog. The
        curated-exclusion count is NOT pinned at zero by a test — a count over
        config/synapse.yml is exactly the class of pin this round deleted.
      - >-
        The engine unit is (producer, owner_program). Measured 2026-08-12, 32 of 378
        engines mix artifact tiers, so the engine-level authority roll-up (a MAX)
        OVERSTATES authority for the low-tier siblings in those cells. A consumer acting
        per-artifact must read artifacts[].artifact_authority, not the roll-up.
      - >-
        The qledger desk literal is extracted by AST from register()/register_batch()/
        make_claim() call sites. A dynamic or indirect desk value does not resolve (1
        producer measured 2026-08-12: engine/china_special_situations.py); those engines
        fall through to ledger waterfall rules 3-5. The builder emits a ::notice naming
        them, so the gap is counted rather than invisible.
      - >-
        Species-to-engine binding is by ledger path, so several species sharing one
        ledger collapse to one engine. Measured 2026-08-12: 17 species bind to
        us_board_ledger, so the conservative least-advanced rule renders that engine
        phase0 even though 6 of the bound species are validated. The full bound set is
        recorded in validation_state_evidence.
      - >-
        `graded_by_design: "yes"` IS PARTLY A WEAK HEURISTIC — see the fuller disclosure in
        epistemics.engine_authority_evidence. Ledger waterfall rules 1 and 4 resolve the
        ledger from a PATH / MODULE-NAME SUBSTRING, so a `yes` they produce is a guess.
        Every engine carries graded_by_design_evidence in {strong, weak_path_heuristic,
        none} and every weak `yes` is enumerated as GRADED_BY_DESIGN_IS_HEURISTIC on every
        run.
    owner_program: qualitative-intelligence
    notes: >
      Split from epistemics.engine_authority_evidence deliberately: one entry would force
      one severity, and the two laws describe different things — this one the derivation's
      own invariants, the sibling the pre-existing corpus backlog those invariants surface.
      Registered at `warn` rather than `hard` per the second known_limit: the argument for
      `hard` here ("green by construction, every input is PR-caused") is verbatim the
      argument that justified the drift pin in rounds 1 and 2, and it was wrong both times,
      so the severity waits on evidence rather than on reasoning. Wired as its OWN legacy
      job `intelligence-registry` (CEO ruling 2026-08-14), not folded into neural-web:
      run_ci_pack.py::_run_job RETURNS on the first non-zero step, so inside a shared job
      there is no safe position — first MASKS the nine sibling suites behind it, last goes
      DARK behind any of them (#4779, an absence of red is not a pass) — and a step-level
      `if: always()` is unavailable because ALLOWED_STEP_KEYS is {name, run, uses, with}.
      188 jobs were measured for consolidation first; zero exact-duplicate signatures and
      no safe merge existed, so the one-off pack rebalance is the cheaper cost.

  - law_id: epistemics.engine_authority_evidence
    summary: >
      An engine holding authority over a human must point at the evidence that earned it.
      Measured 2026-08-12 (Finding C-1): four of the five tier=scored synapse artifacts —
      vector-calibration, hazard-model, vol-regime-gate, vol-regime-basket-overlay-gate —
      carry NO qual_ladder_ref, so things with rank/size/gate authority do not name their
      prereg. Finding C-2: synapse `tier` does not express authority over a human at all —
      site-us-standouts (the Prophet board that orders what a paying user sees) carried
      the same tier=display as a decorative chip. This law reports every ARTIFACT above
      display authority whose qual_ladder_ref is MISSING (AUTHORITY_WITHOUT_EVIDENCE),
      every qual_ladder_ref that resolves to neither a config/qual_ladder.yml key nor an
      existing repo FILE (AUTHORITY_EVIDENCE_UNRESOLVABLE — a DIRECTORY is not a prereg),
      every ref the resolver was given no inputs to probe (AUTHORITY_EVIDENCE_UNCHECKED —
      "could not look", which is reported and is NOT counted as evidence), every
      gate-tripping engine with no curated output_class, every graded_by_design='yes'
      standing on a path-substring heuristic (GRADED_BY_DESIGN_IS_HEURISTIC), a
      graded_by_design value contradicting a resolved ledger, a species whose ledger_binding matches no
      engine (SPECIES_UNBOUND), a registered qledger desk with zero rows in the claim
      corpus, and the scored_path_surfaces completeness flag (an artifact read by an
      Article-2 enforcer module while declaring no surfaces — the measured prophet-index
      case).
    source_ref:
      - engine/intelligence_registry.py
      - scripts/check_intelligence_registry.py
      - research/MASTERMIND_INTELLIGENCE_CATALOG.md
      - research/MASTERMIND_INTELLIGENCE_OS_V1_PLAN.md
    severity: warn
    check_script: scripts/check_intelligence_registry.py
    ci_wiring:
      - workflow: .github/ci/legacy-jobs.yml
        job: intelligence-registry
        lane: pr_ci
    selftest: true
    allowlist: null
    ratchet: null
    known_limits:
      - >-
        Advisory by default for FINDINGS (exit 0 with ::warning annotations); only --strict
        reds on them, and --strict is deliberately NOT passed in CI yet. The findings are
        PRE-EXISTING CONDITIONS of the corpus, not regressions any PR author caused. That
        leniency does NOT extend to PR-PLANE blindness: a run that could not read config or
        code exits non-zero on every run (ruling 2026-08-14). DATA-PLANE blindness — the
        nightly-advanced claim store alone — is reported in full and reds only under
        --strict, because a PR lane must not go red fleet-wide for a store no PR author
        touched. See the FAIL CLOSED known_limit on the sibling law
        governance.intelligence_registry_integrity.
        MEASURED 2026-08-12 on the derived view (378 engines over 642 synapse artifacts, 7
        cells excluded): 181 findings — 21 AUTHORITY_WITHOUT_EVIDENCE, 113
        OUTPUT_CLASS_MISSING, 45 SCORED_PATH_SURFACES_INCOMPLETE, 2 SPECIES_UNBOUND, 0
        unresolvable and 0 unchecked refs — plus 41 GRADED_BY_DESIGN_IS_HEURISTIC.
        Authority splits 355 display / 12 engine_input / 6 user_ranking / 5 gate_size,
        against a synapse `tier` that saw only the 5 scored — which is finding C-2.
        RE-MEASURED 2026-08-14 after the rule-4 same-program restriction (same 378 engines):
        212 findings — 21 AUTHORITY_WITHOUT_EVIDENCE, 109 OUTPUT_CLASS_MISSING, 45
        SCORED_PATH_SURFACES_INCOMPLETE, 2 SPECIES_UNBOUND, 0 unresolvable and 0 unchecked
        — plus 35 GRADED_BY_DESIGN_IS_HEURISTIC; authority is unchanged. These are DATED
        MEASUREMENTS, deliberately not pinned by any test: every one of them moves when a
        sibling PR touches config/synapse.yml, and pinning them is what made rounds 1 and 2
        scheduled fleet-wide reds.
      - >-
        `graded_by_design: "yes"` IS A WEAK HEURISTIC IN TWO OF ITS FOUR RULES, AND SAYS
        SO. It means "the ledger waterfall resolved a store-shaped path", and rule 1
        accepts any artifact path matching /ledger/i ANYWHERE IN THE PATH while rule 4 hops
        to a SAME-PROGRAM consumer whose MODULE NAME matches /grade|ledger/i. A name is not
        proof that graded rows are written, so every `yes` reached that way is a GUESS and
        some of those guesses are wrong on live rows. The derived claim is neither dropped
        nor trusted: every engine carries graded_by_design_evidence — `strong` (rule 2, the
        producer statically imports engine.qledger and a desk literal resolves by AST; or
        rule 3, an artifact DECLARES tier shadow/scored/confirmer, which synapse's own
        meta.tier_vocabulary defines as claim-registered and graded), `weak_path_heuristic`
        (rules 1 and 4), or `none`. Every weak `yes` is ENUMERATED as
        GRADED_BY_DESIGN_IS_HEURISTIC on every run — 35 of the 100 `yes` engines measured
        2026-08-14 — rather than described in prose or kept in a hand-maintained list of
        known-wrong rows, which would rot. T7 MUST NOT treat a weak `yes` as gradeability.
        Mirrored in the engine/intelligence_registry.py module docstring.
      - >-
        THE DISCLOSURE SAID "FILENAME" AND WAS MEASURABLY WRONG UNTIL 2026-08-14, and the
        enum value said so too (`weak_filename_heuristic`). `_LEDGER_PATH_RE` is
        unanchored, so a DIRECTORY component satisfies rule 1: 5 of the 35 live rule-1
        matches carry `ledger` only in a directory — data/qledger/claims.jsonl,
        data/qledger/falsifier_evaluations.jsonl, data/board_ledger/ca_board.parquet and
        the two data/us_board_ledger/retro_grades*.parquet — and ALL FIVE are real grading
        stores whose semantics live in the directory name. Tightening the match to the
        basename was therefore measured and REJECTED (it would drop five true positives —
        the shrink direction, where a detector going blind reads exactly like a detector
        getting stricter); the fix was honesty, so the value is now `weak_path_heuristic`
        and every disclosure string says PATH. Do not re-litigate the narrowing without
        re-running that measurement, which is recorded in _cell_ledger_paths' docstring.
      - >-
        LEDGER WATERFALL RULE 4 IS SAME-PROGRAM ONLY SINCE 2026-08-14; the cross-program
        form is deleted, not deprecated. It used to adopt ANY grader-shaped consumer's
        ledger "EVEN CROSS-PROGRAM". Measured that day on the live corpus: 7 engines
        resolved by rule 4 and SIX of the seven hops crossed a program boundary and were
        wrong or unearned — engine/run.py::engine-fix, the nightly orchestrator, was
        "graded by" hk-canada's data/board_ledger/ca_board.parquet, and
        scripts/build_stock_library.py::us-stocks-prebreakout resolved through
        scripts/grade_us_board.py, a producer owning TWO cells with DIFFERENT ledgers, so
        the per-producer hop index made an ARBITRARY pick. The index is now keyed by
        (producer, owner_program) and the hop may only land inside the resolving engine's
        own program. Live effect measured the same day: rule-4 count 7 -> 1 (the surviving
        hop is engine/experiments_registry.py::qualitative-intelligence ->
        engine/qledger.py::qualitative-intelligence, a program grading its own output), the
        other six fall to rule 5 and publish `ledger='none'` /
        `graded_by_design='no — not yet'`, `yes` 106 -> 100, weak evidence 41 -> 35, and
        the content-finding total 222 -> 212. The engine count is unchanged at 378 — this
        deletes unearned SEMANTICS, never a row.
      - >-
        graded_by_design='yes' also requires a STORE-shaped ledger: a Python module, a
        charter config, and an unexpanded template/glob path cannot hold a graded row, and
        an engine/*_ledger.py producer with no store-shaped artifact falls THROUGH the
        waterfall rather than being its own ledger. Directory paths
        (data/metabolism/agenda/) DO count as stores — refusing them would be the
        shrink-direction failure this tightening exists to avoid, since a detector that
        goes blind reads exactly like a detector getting stricter.
      - >-
        Derived authority UNDER-COUNTS on day one because its input is under-populated:
        only 11 of 642 synapse artifacts carry a non-empty scored_path_surfaces, and
        prophet-index is a confirmed false negative (tier=display, no surfaces, yet
        consumed by scripts/build_site.py). The correct claim is "authority is derived and
        auditable", NEVER "all authority-bearing engines are identified".
      - >-
        The scored_path_surfaces completeness detector is low-precision BY CONSTRUCTION:
        scripts/build_site.py is a consumer on 37 artifacts and scripts/build_stock_library.py
        on 14, so consumer-membership in an Article-2 module is far too wide to carry
        authority. It PROPOSES (45 engines flagged) and never promotes — deriving
        user_ranking from it was rejected on that measurement.
      - >-
        output_class is the only field with a real human cost (113 engines require one as
        measured 2026-08-12). It is deliberately left null rather than guessed:
        output_class selects the metric contract, and a wrong metric contract is worse than
        a null one. There is no auto-proposal mode — curating it is T7's work, through the
        four-key overlay, one rationale per row.
      - >-
        ANNOTATION BUDGET: C-1 findings are annotated per engine (each names its concrete
        heal); every other code is aggregated into one annotation with a count; the
        fail-closed COULD NOT LOOK line is a third channel, budgeted at exactly one. One
        annotation per finding would bury the actionable rows. The budget governs only what
        GitHub RENDERS: since 2026-08-14 EVERY finding, C-1 included, is also printed as a
        plain `  [CODE] engine: detail` line, because an annotation-only deliverable is
        invisible to a terminal run, to a grep of the log body, and to anyone reading the
        raw job log rather than the Actions summary.
      - >-
        Resolvability is NOT adequacy. AUTHORITY_EVIDENCE_UNRESOLVABLE proves only that a
        qual_ladder_ref points at something real — a key in config/qual_ladder.yml or a
        FILE that exists inside this repo. It cannot prove that the document pre-registers
        anything; that judgment belongs to T7's backlog drain. What it does close is the
        set of ways the C-1 backlog was drainable to zero without a single real prereg: an
        arbitrary non-empty string, a DIRECTORY (`git show HEAD:research` exits 0 on a
        tree, so the earlier path_exists probe accepted any folder), and an ABSOLUTE PATH
        (`root / "/etc/passwd"` is `/etc/passwd` under pathlib's absolute-operand rule, so
        the worktree half of the probe answered True for any file on the machine).
        MISSING, UNRESOLVABLE and UNCHECKED are three DISJOINT codes because they need
        three different heals; none of the three is ever silent, and none counts as
        evidence.
    owner_program: qualitative-intelligence
    notes: >
      warn was chosen over the sibling precedent epistemics.qledger_metric_validity's
      manual + ci_wiring:[]. VALID_SEVERITIES permits empty ci_wiring only for
      manual/discipline/known_spurious, so `manual` means the audit never runs against the
      real corpus unless a human remembers. `warn` requires real wiring, so the C-1
      backlog is visible as ::warning annotations on every PR and the count is trackable.
      DATED PROMOTION PLAN: promote to `hard` with --strict in the same PR that drives the
      authority>display evidence_ref backlog to zero, owned by T7 of
      research/MASTERMIND_INTELLIGENCE_OS_V1_PLAN.md. An upgrade needs no ruling; a
      downgrade does. The prescribed C-1 heal is to fill qual_ladder_ref in
      config/synapse.yml — it repairs the canonical source rather than papering over the
      gap in a side file. T1 deliberately did NOT invent those citations: a prereg pointer
      that does not exist is worse than a null one.

  - law_id: cycles.stretch_oracle_disclosure
    summary: >
      The two stretch/extension oracles (O1 ladder.alignment.overextended, the
      oscillator-dominant 4-leg brake; O2 entry_signal.status == "extended", a
      relabel of urgency=="caution") legitimately disagree on ~38% of names, so the
      contract is DISCLOSURE, not agreement: every flagged name must carry the leg
      that actually fired (invariants I1-I4 in the checker; contract doc
      docs/site_semantics/stretch_oracles.md), and the divergence counter is
      re-emitted nightly against the store the render just wrote, so a stale
      divergence number can never again be the newest one anybody has.
    source_ref:
      - scripts/check_stretch_oracle_contract.py
      - docs/site_semantics/stretch_oracles.md
      - tests/test_stretch_oracle_contract.py
      - "PR #5574 (feat(cycles): disclose which leg fires the overextension brake)"
    severity: warn
    check_script: scripts/check_stretch_oracle_contract.py
    ci_wiring:
      - workflow: .github/workflows/daily.yml
        job: engine
        lane: hook
      - workflow: .github/workflows/engine-render.yml
        job: engine-render
        lane: hook
    selftest: false
    selftest_exempt_reason: >
      No selftest flag; the invariant battery (I1-I4 plus the confusion-matrix
      shape) lives in tests/test_stretch_oracle_contract.py against synthetic
      stores, run in pr_ci by the legacy picks-boards pytest sweep, and the live
      gate is the warn-only in-process self-check at the end of
      scripts/build_stock_library.py's stockdata render.
    allowlist: null
    ratchet: null
    known_limits:
      - site/stockdata is nightly-render output and is NOT committed, so no pr_ci
        lane can run the checker against a real store (CLI exits 2 "nothing to
        check" in a plain checkout); PR-tier coverage is the synthetic-store pytest
        suite only.
      - The in-process nightly invocation is display-tier and never fatal (the
        guard must never break the render); violations surface as line-start
        ::warning annotations, never exit codes — lane=hook mirrors the
        surface_freshness precedent for builder-embedded gates.
    owner_program: cycles
    notes: >
      Invoked in-process (lazy import, never a workflow run: body) by
      `scripts/build_stock_library.py` after the stockdata render in the daily and
      engine-render lanes. Operator CLI:
      `python3 scripts/check_stretch_oracle_contract.py site/stockdata`.
      Registered 2026-08-14 by the PR-1b fusion session healing the
      house-law-registry census red that #5574 left (checker shipped without an
      entry; every full-suite ci-pack run since was red on this job).
