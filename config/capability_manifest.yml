# config/capability_manifest.yml — Capability broker manifest (F3, §3 of masterplan)
#
# REDLINE: this file must NEVER contain secret values.
#   - secret_ref fields store NAMES only (the env-var or GitHub Secret name).
#   - The capability broker reads this file and returns ref names; it never
#     reads or returns secret values.
#   - CI (check_capability_redline.py) scans this file for high-entropy strings
#     and any 'secrets.' or 'os.environ[' patterns and hard-fails on any finding.
#
# To add a capability:
#   1. Add a row here (name, kind, ref name, scope — no values).
#   2. The broker resolves by ref name; the runner/caller reads os.environ[ref_name].
#   3. Wire allowed_lanes and allowed_tiers conservatively (principle of least privilege).
#
# Schema: capability_manifest.v1
schema: capability_manifest.v1
generated_at: "2026-07-09"
owner_program: metabolism-phase0

capabilities:

  # ── LLM OAuth (whitehouse-sentinel, future metabolism stages) ──────────────

  - capability_id: claude_code_oauth
    kind: llm_oauth
    secret_ref: CLAUDE_CODE_OAUTH_TOKEN
    scope: >
      Anthropic Claude API access via OAuth token.  Used by whitehouse-sentinel,
      asia-close, cortex, and future metabolism PROPOSE/ADJUDICATE stages.
    storage_locus: gh-secret
    allowed_lanes:
      - whitehouse-sentinel
      - asia-close
      - metabolism-propose     # Phase A — not yet armed
      - metabolism-adjudicate  # Phase A — not yet armed
      - metabolism-agenda
      - metabolism-attention
      - metabolism-dream
      - metabolism-audit
      - metabolism-shadow
      - metabolism-standout
      - cortex
      - whitehouse
      - causal-brainstorm
      - signal-foundry
      - extraction-drift
      - commodity-news
      - risk-brain
      - risk-radar-review
      - influence-extract
      - narrative-brain
      - altdata-brain
      - news-llm
      - ask-brain
      - orchestrator-chat
      - brain-fast       # MNZ-W6a: haiku fallback for fast lane when DeepSeek absent
      - brain-pro        # MNZ-W6a: opus/sonnet pro lane
      # ── AI Daily Brief lanes (operator directive 2026-08-26) ─────────────
      # The AI Daily Brief (engine/master_brain.py) and the AI Desk
      # (engine/ai_desk.py, which briefs through master_brain._call_model) ran
      # DeepSeek-ONLY until 2026-08-26 — one metered rung, so the day its balance
      # ran out the brief stopped rendering. Both now run codex → oauth pool →
      # ANTHROPIC_API_KEY → DeepSeek. This allowlist is FAIL-CLOSED, and a
      # pool-aware lane that resolves nothing silently falls back to the single
      # deprecated legacy token — omitting these names shrinks Claude to one key
      # rather than failing loudly.
      - master-brain
      - ai-desk
      # ── Marketing content lanes (operator directive 2026-07-29) ──────────
      # ChatGPT-first: the marketing writing lanes default to the attached Codex
      # account so Claude subscription tokens stay reserved for website-building
      # sessions. Tier map: Sol writes (copywriter, breaking, copy review,
      # weekend levels ride the copywriter lane), Terra critiques and wires
      # (critic, hot-tape wire, reply voice). Luna is never routed user-facing
      # words. Config: config/marketing.yml copywriter.llm (+ .critic),
      # breaking.llm, reply_desk.voice and config.yml hot_tape.llm.
      - marketing-copywriter     # Sol
      - marketing-breaking       # Sol
      - marketing-copy-review    # Sol
      - marketing-critic         # Terra
      - hot-tape-wire            # Terra
      - reply-voice              # Terra
    allowed_tiers:
      - T0
      - T1
      - T2
    rotation_state: active
    kill_state: active
    notes: >
      Already a GH secret (confirmed by whitehouse-sentinel.yml usage).
      A2 preflight gate (Phase A) pings cheapest tier to verify health before
      any build stage is allowed to proceed.

  # ── VPS deploy key (render pipeline) ─────────────────────────────────────

  - capability_id: vps_deploy_key
    kind: vps_deploy
    secret_ref: VPS_DEPLOY_KEY
    scope: >
      SSH private key for deploying rendered site artifacts to the live VPS.
      Used by render.yml and engine-render.yml deploy steps.
    storage_locus: gh-secret
    allowed_lanes:
      - render
      - engine-render
    allowed_tiers:
      - T0
      - T1
    rotation_state: active
    kill_state: active
    notes: >
      Loop stages may NEVER use this capability (R-AUT-12: no VPS shell for the loop).
      Changes to the live site reach VPS only through the render tail, never a
      loop-direct shell command.

  # ── Multi-key OAuth pool (Metabolism V2-B, R-V2-4) ───────────────────────
  # REDLINE: secret_ref stores the env-var NAME only — never the value.
  # Operator creates CLAUDE_CODE_OAUTH_TOKEN_1/2/3 as GH secrets.
  # The pool (engine/neuralweb/key_pool.py) discovers which subset is present.
  # With only _1 set, the pool serializes (no spread benefit — by design).

  - capability_id: claude_code_oauth_1
    kind: llm_oauth
    secret_ref: CLAUDE_CODE_OAUTH_TOKEN_1
    scope: >
      Primary Metabolism OAuth key for the multi-key pool (V2-B).
      Used by metabolism-dispatch to spread lobe activations across keys.
      Operator places this key first; _2 and _3 added when available.
    storage_locus: gh-secret
    allowed_lanes:
      - metabolism-dispatch
      - metabolism-agenda
      - metabolism-attention
      - metabolism-propose
      - metabolism-adjudicate
      - metabolism-build
      - metabolism-dream
      - metabolism-audit       # V7 (R-V7-2): adversarial Opus code review lane
      - metabolism-shadow
      - metabolism-standout
      - cortex
      - whitehouse
      - causal-brainstorm
      - signal-foundry
      - extraction-drift
      - commodity-news
      - risk-brain
      - risk-radar-review
      - influence-extract
      - narrative-brain
      - altdata-brain
      - news-llm
      - ask-brain
      - orchestrator-chat
      - brain-fast       # MNZ-W6a: haiku fallback for fast lane when DeepSeek absent
      - brain-pro        # MNZ-W6a: opus/sonnet pro lane
      # ── AI Daily Brief lanes (operator directive 2026-08-26) ─────────────
      # The AI Daily Brief (engine/master_brain.py) and the AI Desk
      # (engine/ai_desk.py, which briefs through master_brain._call_model) ran
      # DeepSeek-ONLY until 2026-08-26 — one metered rung, so the day its balance
      # ran out the brief stopped rendering. Both now run codex → oauth pool →
      # ANTHROPIC_API_KEY → DeepSeek. This allowlist is FAIL-CLOSED, and a
      # pool-aware lane that resolves nothing silently falls back to the single
      # deprecated legacy token — omitting these names shrinks Claude to one key
      # rather than failing loudly.
      - master-brain
      - ai-desk
      # ── Marketing content lanes (operator directive 2026-07-29) ──────────
      # ChatGPT-first: the marketing writing lanes default to the attached Codex
      # account so Claude subscription tokens stay reserved for website-building
      # sessions. Tier map: Sol writes (copywriter, breaking, copy review,
      # weekend levels ride the copywriter lane), Terra critiques and wires
      # (critic, hot-tape wire, reply voice). Luna is never routed user-facing
      # words. Config: config/marketing.yml copywriter.llm (+ .critic),
      # breaking.llm, reply_desk.voice and config.yml hot_tape.llm.
      - marketing-copywriter     # Sol
      - marketing-breaking       # Sol
      - marketing-copy-review    # Sol
      - marketing-critic         # Terra
      - hot-tape-wire            # Terra
      - reply-voice              # Terra
    allowed_tiers:
      - T0
      - T1
      - T2
    rotation_state: active
    kill_state: active
    notes: >
      V2-B pool key 1 of 7. Pool works with 1 key present (degrades gracefully).
      key_pool.py tracks session counts + est-tokens in data/metabolism/key_ledger.jsonl
      (belief-state only; quota is unobservable). 429/usage-limit error triggers
      mark_cooling(cool_kind=window|weekly); dispatcher skips cooling keys.

  - capability_id: claude_code_oauth_2
    kind: llm_oauth
    secret_ref: CLAUDE_CODE_OAUTH_TOKEN_2
    scope: >
      Secondary Metabolism OAuth key for the multi-key pool (V2-B).
      Added by operator when a second key is available for load distribution.
    storage_locus: gh-secret
    allowed_lanes:
      - metabolism-dispatch
      - metabolism-agenda
      - metabolism-attention
      - metabolism-propose
      - metabolism-adjudicate
      - metabolism-build
      - metabolism-dream
      - metabolism-audit       # V7 (R-V7-2): adversarial Opus code review lane
      - metabolism-shadow
      - metabolism-standout
      - cortex
      - whitehouse
      - causal-brainstorm
      - signal-foundry
      - extraction-drift
      - commodity-news
      - risk-brain
      - risk-radar-review
      - influence-extract
      - narrative-brain
      - altdata-brain
      - news-llm
      - ask-brain
      - orchestrator-chat
      - brain-fast       # MNZ-W6a: haiku fallback for fast lane when DeepSeek absent
      - brain-pro        # MNZ-W6a: opus/sonnet pro lane
      # ── AI Daily Brief lanes (operator directive 2026-08-26) ─────────────
      # The AI Daily Brief (engine/master_brain.py) and the AI Desk
      # (engine/ai_desk.py, which briefs through master_brain._call_model) ran
      # DeepSeek-ONLY until 2026-08-26 — one metered rung, so the day its balance
      # ran out the brief stopped rendering. Both now run codex → oauth pool →
      # ANTHROPIC_API_KEY → DeepSeek. This allowlist is FAIL-CLOSED, and a
      # pool-aware lane that resolves nothing silently falls back to the single
      # deprecated legacy token — omitting these names shrinks Claude to one key
      # rather than failing loudly.
      - master-brain
      - ai-desk
      # ── Marketing content lanes (operator directive 2026-07-29) ──────────
      # ChatGPT-first: the marketing writing lanes default to the attached Codex
      # account so Claude subscription tokens stay reserved for website-building
      # sessions. Tier map: Sol writes (copywriter, breaking, copy review,
      # weekend levels ride the copywriter lane), Terra critiques and wires
      # (critic, hot-tape wire, reply voice). Luna is never routed user-facing
      # words. Config: config/marketing.yml copywriter.llm (+ .critic),
      # breaking.llm, reply_desk.voice and config.yml hot_tape.llm.
      - marketing-copywriter     # Sol
      - marketing-breaking       # Sol
      - marketing-copy-review    # Sol
      - marketing-critic         # Terra
      - hot-tape-wire            # Terra
      - reply-voice              # Terra
    allowed_tiers:
      - T0
      - T1
      - T2
    rotation_state: active
    kill_state: active
    notes: >
      V2-B pool key 2 of 7. Optional — pool works with only _1 present.
      Same ledger / cooling semantics as claude_code_oauth_1.

  - capability_id: claude_code_oauth_3
    kind: llm_oauth
    secret_ref: CLAUDE_CODE_OAUTH_TOKEN_3
    scope: >
      Tertiary Metabolism OAuth key for the multi-key pool (V2-B).
      Added by operator for maximum spread across a 5h window.
    storage_locus: gh-secret
    allowed_lanes:
      - metabolism-dispatch
      - metabolism-agenda
      - metabolism-attention
      - metabolism-propose
      - metabolism-adjudicate
      - metabolism-build
      - metabolism-dream
      - metabolism-audit       # V7 (R-V7-2): adversarial Opus code review lane
      - metabolism-shadow
      - metabolism-standout
      - cortex
      - whitehouse
      - causal-brainstorm
      - signal-foundry
      - extraction-drift
      - commodity-news
      - risk-brain
      - risk-radar-review
      - influence-extract
      - narrative-brain
      - altdata-brain
      - news-llm
      - ask-brain
      - orchestrator-chat
      - brain-fast       # MNZ-W6a: haiku fallback for fast lane when DeepSeek absent
      - brain-pro        # MNZ-W6a: opus/sonnet pro lane
      # ── AI Daily Brief lanes (operator directive 2026-08-26) ─────────────
      # The AI Daily Brief (engine/master_brain.py) and the AI Desk
      # (engine/ai_desk.py, which briefs through master_brain._call_model) ran
      # DeepSeek-ONLY until 2026-08-26 — one metered rung, so the day its balance
      # ran out the brief stopped rendering. Both now run codex → oauth pool →
      # ANTHROPIC_API_KEY → DeepSeek. This allowlist is FAIL-CLOSED, and a
      # pool-aware lane that resolves nothing silently falls back to the single
      # deprecated legacy token — omitting these names shrinks Claude to one key
      # rather than failing loudly.
      - master-brain
      - ai-desk
      # ── Marketing content lanes (operator directive 2026-07-29) ──────────
      # ChatGPT-first: the marketing writing lanes default to the attached Codex
      # account so Claude subscription tokens stay reserved for website-building
      # sessions. Tier map: Sol writes (copywriter, breaking, copy review,
      # weekend levels ride the copywriter lane), Terra critiques and wires
      # (critic, hot-tape wire, reply voice). Luna is never routed user-facing
      # words. Config: config/marketing.yml copywriter.llm (+ .critic),
      # breaking.llm, reply_desk.voice and config.yml hot_tape.llm.
      - marketing-copywriter     # Sol
      - marketing-breaking       # Sol
      - marketing-copy-review    # Sol
      - marketing-critic         # Terra
      - hot-tape-wire            # Terra
      - reply-voice              # Terra
    allowed_tiers:
      - T0
      - T1
      - T2
    rotation_state: active
    kill_state: active
    notes: >
      V2-B pool key 3 of 7. Optional — pool works with only _1 present.
      Same ledger / cooling semantics as claude_code_oauth_1 and _2.

  - capability_id: claude_code_oauth_4
    kind: llm_oauth
    secret_ref: CLAUDE_CODE_OAUTH_TOKEN_4
    scope: >
      Quaternary Metabolism OAuth key for the multi-key pool (V2-B).
      Added by operator to further spread load across a 5h window.
    storage_locus: gh-secret
    allowed_lanes:
      - metabolism-dispatch
      - metabolism-agenda
      - metabolism-attention
      - metabolism-propose
      - metabolism-adjudicate
      - metabolism-build
      - metabolism-dream
      - metabolism-audit       # V7 (R-V7-2): adversarial Opus code review lane
      - metabolism-shadow
      - metabolism-standout
      - cortex
      - whitehouse
      - causal-brainstorm
      - signal-foundry
      - extraction-drift
      - commodity-news
      - risk-brain
      - risk-radar-review
      - influence-extract
      - narrative-brain
      - altdata-brain
      - news-llm
      - ask-brain
      - orchestrator-chat
      - brain-fast       # MNZ-W6a: haiku fallback for fast lane when DeepSeek absent
      - brain-pro        # MNZ-W6a: opus/sonnet pro lane
      # ── AI Daily Brief lanes (operator directive 2026-08-26) ─────────────
      # The AI Daily Brief (engine/master_brain.py) and the AI Desk
      # (engine/ai_desk.py, which briefs through master_brain._call_model) ran
      # DeepSeek-ONLY until 2026-08-26 — one metered rung, so the day its balance
      # ran out the brief stopped rendering. Both now run codex → oauth pool →
      # ANTHROPIC_API_KEY → DeepSeek. This allowlist is FAIL-CLOSED, and a
      # pool-aware lane that resolves nothing silently falls back to the single
      # deprecated legacy token — omitting these names shrinks Claude to one key
      # rather than failing loudly.
      - master-brain
      - ai-desk
      # ── Marketing content lanes (operator directive 2026-07-29) ──────────
      # ChatGPT-first: the marketing writing lanes default to the attached Codex
      # account so Claude subscription tokens stay reserved for website-building
      # sessions. Tier map: Sol writes (copywriter, breaking, copy review,
      # weekend levels ride the copywriter lane), Terra critiques and wires
      # (critic, hot-tape wire, reply voice). Luna is never routed user-facing
      # words. Config: config/marketing.yml copywriter.llm (+ .critic),
      # breaking.llm, reply_desk.voice and config.yml hot_tape.llm.
      - marketing-copywriter     # Sol
      - marketing-breaking       # Sol
      - marketing-copy-review    # Sol
      - marketing-critic         # Terra
      - hot-tape-wire            # Terra
      - reply-voice              # Terra
    allowed_tiers:
      - T0
      - T1
      - T2
    rotation_state: active
    kill_state: active
    notes: >
      V2-B pool key 4 of 7. Optional — pool works with only _1 present.
      Same ledger / cooling semantics as claude_code_oauth_1.

  - capability_id: claude_code_oauth_5
    kind: llm_oauth
    secret_ref: CLAUDE_CODE_OAUTH_TOKEN_5
    scope: >
      Quinary Metabolism OAuth key for the multi-key pool (V2-B).
      Added by operator to further spread load across a 5h window.
    storage_locus: gh-secret
    allowed_lanes:
      - metabolism-dispatch
      - metabolism-agenda
      - metabolism-attention
      - metabolism-propose
      - metabolism-adjudicate
      - metabolism-build
      - metabolism-dream
      - metabolism-audit       # V7 (R-V7-2): adversarial Opus code review lane
      - metabolism-shadow
      - metabolism-standout
      - cortex
      - whitehouse
      - causal-brainstorm
      - signal-foundry
      - extraction-drift
      - commodity-news
      - risk-brain
      - risk-radar-review
      - influence-extract
      - narrative-brain
      - altdata-brain
      - news-llm
      - ask-brain
      - orchestrator-chat
      - brain-fast       # MNZ-W6a: haiku fallback for fast lane when DeepSeek absent
      - brain-pro        # MNZ-W6a: opus/sonnet pro lane
      # ── AI Daily Brief lanes (operator directive 2026-08-26) ─────────────
      # The AI Daily Brief (engine/master_brain.py) and the AI Desk
      # (engine/ai_desk.py, which briefs through master_brain._call_model) ran
      # DeepSeek-ONLY until 2026-08-26 — one metered rung, so the day its balance
      # ran out the brief stopped rendering. Both now run codex → oauth pool →
      # ANTHROPIC_API_KEY → DeepSeek. This allowlist is FAIL-CLOSED, and a
      # pool-aware lane that resolves nothing silently falls back to the single
      # deprecated legacy token — omitting these names shrinks Claude to one key
      # rather than failing loudly.
      - master-brain
      - ai-desk
      # ── Marketing content lanes (operator directive 2026-07-29) ──────────
      # ChatGPT-first: the marketing writing lanes default to the attached Codex
      # account so Claude subscription tokens stay reserved for website-building
      # sessions. Tier map: Sol writes (copywriter, breaking, copy review,
      # weekend levels ride the copywriter lane), Terra critiques and wires
      # (critic, hot-tape wire, reply voice). Luna is never routed user-facing
      # words. Config: config/marketing.yml copywriter.llm (+ .critic),
      # breaking.llm, reply_desk.voice and config.yml hot_tape.llm.
      - marketing-copywriter     # Sol
      - marketing-breaking       # Sol
      - marketing-copy-review    # Sol
      - marketing-critic         # Terra
      - hot-tape-wire            # Terra
      - reply-voice              # Terra
    allowed_tiers:
      - T0
      - T1
      - T2
    rotation_state: active
    kill_state: active
    notes: >
      V2-B pool key 5 of 7. Optional — pool works with only _1 present.
      Same ledger / cooling semantics as claude_code_oauth_1.

  - capability_id: claude_code_oauth_6
    kind: llm_oauth
    secret_ref: CLAUDE_CODE_OAUTH_TOKEN_6
    scope: >
      Senary Metabolism OAuth key for the multi-key pool (V2-B).
      Added by operator to further spread load across a 5h window.
    storage_locus: gh-secret
    allowed_lanes:
      - metabolism-dispatch
      - metabolism-agenda
      - metabolism-attention
      - metabolism-propose
      - metabolism-adjudicate
      - metabolism-build
      - metabolism-dream
      - metabolism-audit       # V7 (R-V7-2): adversarial Opus code review lane
      - metabolism-shadow
      - metabolism-standout
      - cortex
      - whitehouse
      - causal-brainstorm
      - signal-foundry
      - extraction-drift
      - commodity-news
      - risk-brain
      - risk-radar-review
      - influence-extract
      - narrative-brain
      - altdata-brain
      - news-llm
      - ask-brain
      - orchestrator-chat
      - brain-fast       # MNZ-W6a: haiku fallback for fast lane when DeepSeek absent
      - brain-pro        # MNZ-W6a: opus/sonnet pro lane
      # ── AI Daily Brief lanes (operator directive 2026-08-26) ─────────────
      # The AI Daily Brief (engine/master_brain.py) and the AI Desk
      # (engine/ai_desk.py, which briefs through master_brain._call_model) ran
      # DeepSeek-ONLY until 2026-08-26 — one metered rung, so the day its balance
      # ran out the brief stopped rendering. Both now run codex → oauth pool →
      # ANTHROPIC_API_KEY → DeepSeek. This allowlist is FAIL-CLOSED, and a
      # pool-aware lane that resolves nothing silently falls back to the single
      # deprecated legacy token — omitting these names shrinks Claude to one key
      # rather than failing loudly.
      - master-brain
      - ai-desk
      # ── Marketing content lanes (operator directive 2026-07-29) ──────────
      # ChatGPT-first: the marketing writing lanes default to the attached Codex
      # account so Claude subscription tokens stay reserved for website-building
      # sessions. Tier map: Sol writes (copywriter, breaking, copy review,
      # weekend levels ride the copywriter lane), Terra critiques and wires
      # (critic, hot-tape wire, reply voice). Luna is never routed user-facing
      # words. Config: config/marketing.yml copywriter.llm (+ .critic),
      # breaking.llm, reply_desk.voice and config.yml hot_tape.llm.
      - marketing-copywriter     # Sol
      - marketing-breaking       # Sol
      - marketing-copy-review    # Sol
      - marketing-critic         # Terra
      - hot-tape-wire            # Terra
      - reply-voice              # Terra
    allowed_tiers:
      - T0
      - T1
      - T2
    rotation_state: active
    kill_state: active
    notes: >
      V2-B pool key 6 of 7. Optional — pool works with only _1 present.
      Same ledger / cooling semantics as claude_code_oauth_1.

  - capability_id: claude_code_oauth_7
    kind: llm_oauth
    secret_ref: CLAUDE_CODE_OAUTH_TOKEN_7
    scope: >
      Septenary Metabolism OAuth key for the multi-key pool (V2-B).
      Added by operator to further spread load across a 5h window.
    storage_locus: gh-secret
    allowed_lanes:
      - metabolism-dispatch
      - metabolism-agenda
      - metabolism-attention
      - metabolism-propose
      - metabolism-adjudicate
      - metabolism-build
      - metabolism-dream
      - metabolism-audit       # V7 (R-V7-2): adversarial Opus code review lane
      - metabolism-shadow
      - metabolism-standout
      - cortex
      - whitehouse
      - causal-brainstorm
      - signal-foundry
      - extraction-drift
      - commodity-news
      - risk-brain
      - risk-radar-review
      - influence-extract
      - narrative-brain
      - altdata-brain
      - news-llm
      - ask-brain
      - orchestrator-chat
      - brain-fast       # MNZ-W6a: haiku fallback for fast lane when DeepSeek absent
      - brain-pro        # MNZ-W6a: opus/sonnet pro lane
      # ── AI Daily Brief lanes (operator directive 2026-08-26) ─────────────
      # The AI Daily Brief (engine/master_brain.py) and the AI Desk
      # (engine/ai_desk.py, which briefs through master_brain._call_model) ran
      # DeepSeek-ONLY until 2026-08-26 — one metered rung, so the day its balance
      # ran out the brief stopped rendering. Both now run codex → oauth pool →
      # ANTHROPIC_API_KEY → DeepSeek. This allowlist is FAIL-CLOSED, and a
      # pool-aware lane that resolves nothing silently falls back to the single
      # deprecated legacy token — omitting these names shrinks Claude to one key
      # rather than failing loudly.
      - master-brain
      - ai-desk
      # ── Marketing content lanes (operator directive 2026-07-29) ──────────
      # ChatGPT-first: the marketing writing lanes default to the attached Codex
      # account so Claude subscription tokens stay reserved for website-building
      # sessions. Tier map: Sol writes (copywriter, breaking, copy review,
      # weekend levels ride the copywriter lane), Terra critiques and wires
      # (critic, hot-tape wire, reply voice). Luna is never routed user-facing
      # words. Config: config/marketing.yml copywriter.llm (+ .critic),
      # breaking.llm, reply_desk.voice and config.yml hot_tape.llm.
      - marketing-copywriter     # Sol
      - marketing-breaking       # Sol
      - marketing-copy-review    # Sol
      - marketing-critic         # Terra
      - hot-tape-wire            # Terra
      - reply-voice              # Terra
    allowed_tiers:
      - T0
      - T1
      - T2
    rotation_state: active
    kill_state: active
    notes: >
      V2-B pool key 7 of 7. Optional — pool works with only _1 present.
      Same ledger / cooling semantics as claude_code_oauth_1.

  # ── DeepSeek API key (brain-fast lane primary provider, MNZ-W6a) ─────────

  - capability_id: deepseek_api_key
    kind: llm_api_key
    secret_ref: DEEPSEEK_API_KEY
    scope: >
      DeepSeek API key used by the shared Anthropic-compatible provider waterfall.
      Primary provider for brain-fast and a metered fallback for other configured
      lanes. A DeepSeek V4 Pro request maps to Codex Sol when the Codex fallback is
      reached; V4 Flash maps to Codex Terra.
    storage_locus: gh-secret
    allowed_lanes:
      - brain-fast
      - brain-pro
      - news-llm
      - commodity-news
      - risk-brain
      - risk-radar-review
      - influence-extract
      - narrative-brain
      - altdata-brain
      - whitehouse
      # AI Daily Brief lanes (operator directive 2026-08-26). Both have used this
      # key since they shipped; they were never listed because master_brain called
      # DeepSeek directly instead of going through the shared waterfall. Now that
      # they do, DeepSeek is their LAST rung — the metered floor beneath Codex and
      # Claude, not the default.
      - master-brain
      - ai-desk
    allowed_tiers:
      - T0
      - T1
      - T2
    rotation_state: active
    kill_state: active
    notes: >
      DeepSeek is already a shared waterfall provider. It now has the same
      cross-process usage/cooling ledger identity as the Claude OAuth pool.

  # ── Attached Codex account (shared subscription fallback) ─────────────────

  - capability_id: codex_account
    kind: llm_subscription
    secret_ref: CODEX_ACCESS_TOKEN
    scope: >
      Attached Codex/ChatGPT account exposed through a text-only Codex CLI adapter.
      It is inserted after the Claude OAuth pool and before metered providers, or
      appended as the final fallback in lanes without OAuth. Requested tiers map
      as follows: Claude Opus/Fable and DeepSeek V4 Pro to Sol; Claude Sonnet and
      DeepSeek V4 Flash to Terra; Claude Haiku to Luna.
    storage_locus: self-hosted-runner-auth-cache
    allowed_lanes:
      - metabolism-shadow
      - metabolism-standout
      - cortex
      - whitehouse
      - causal-brainstorm
      - signal-foundry
      - extraction-drift
      - commodity-news
      - risk-brain
      - risk-radar-review
      - influence-extract
      - narrative-brain
      - altdata-brain
      - news-llm
      - ask-brain
      - orchestrator-chat
      - brain-fast
      - brain-pro
      # ── AI Daily Brief lanes (operator directive 2026-08-26) ─────────────
      # Codex is the FIRST rung for both brief lanes (a paid subscription beats a
      # metered key). build_providers does not broker-gate the codex rung today, so
      # these two names are declaratory — they keep the manifest an honest record of
      # which lanes actually consume this account.
      - master-brain
      - ai-desk
      # ── Marketing content lanes (operator directive 2026-07-29) ──────────
      # ChatGPT-first: the marketing writing lanes default to the attached Codex
      # account so Claude subscription tokens stay reserved for website-building
      # sessions. Tier map: Sol writes (copywriter, breaking, copy review,
      # weekend levels ride the copywriter lane), Terra critiques and wires
      # (critic, hot-tape wire, reply voice). Luna is never routed user-facing
      # words. Config: config/marketing.yml copywriter.llm (+ .critic),
      # breaking.llm, reply_desk.voice and config.yml hot_tape.llm.
      - marketing-copywriter     # Sol
      - marketing-breaking       # Sol
      - marketing-copy-review    # Sol
      - marketing-critic         # Terra
      - hot-tape-wire            # Terra
      - reply-voice              # Terra
    allowed_tiers:
      - T0
      - T1
      - T2
    rotation_state: active
    kill_state: active
    notes: >
      Production auto-discovers the local Codex CLI plus attached auth without
      reading or copying credential values. CODEX_ACCESS_TOKEN is the optional
      trusted-automation transport; the current self-hosted runner uses its
      existing ~/.codex/auth.json login. Public hosts without that login skip
      this provider. CODEX_PROVIDER_ENABLED=0 is the emergency kill switch.

  - capability_id: codex_account_2
    kind: llm_subscription
    secret_ref: CODEX_ACCESS_TOKEN_2
    scope: >
      Second isolated Codex/ChatGPT account in the same subscription-provider
      waterfall. It has its own CODEX_HOME auth cache, cooling state, rate-limit
      telemetry, and usage identity; requests fail over between Codex accounts
      before reaching metered providers.
    storage_locus: self-hosted-runner-auth-cache
    allowed_lanes:
      - metabolism-shadow
      - metabolism-standout
      - cortex
      - whitehouse
      - causal-brainstorm
      - signal-foundry
      - extraction-drift
      - commodity-news
      - risk-brain
      - risk-radar-review
      - influence-extract
      - narrative-brain
      - altdata-brain
      - news-llm
      - ask-brain
      - orchestrator-chat
      - brain-fast
      - brain-pro
      # AI Daily Brief lanes (operator directive 2026-08-26) — see codex_account.
      - master-brain
      - ai-desk
      - marketing-copywriter
      - marketing-breaking
      - marketing-copy-review
      - marketing-critic
      - hot-tape-wire
      - reply-voice
    allowed_tiers:
      - T0
      - T1
      - T2
    rotation_state: active
    kill_state: active
    notes: >
      Production selects this account from the second path in
      CODEX_ACCOUNT_HOMES. The standard VPS path is /var/lib/macro-codex-2.
      No credential value enters application memory or a git artifact.

  - capability_id: codex_account_3
    kind: llm_subscription
    secret_ref: CODEX_ACCESS_TOKEN_3
    scope: >
      Third isolated Codex/ChatGPT account in the same subscription-provider
      waterfall. It has its own CODEX_HOME auth cache, cooling state, rate-limit
      telemetry, and usage identity; requests fail over among all Codex accounts
      before reaching metered providers.
    storage_locus: self-hosted-runner-auth-cache
    allowed_lanes:
      - metabolism-shadow
      - metabolism-standout
      - cortex
      - whitehouse
      - causal-brainstorm
      - signal-foundry
      - extraction-drift
      - commodity-news
      - risk-brain
      - risk-radar-review
      - influence-extract
      - narrative-brain
      - altdata-brain
      - news-llm
      - ask-brain
      - orchestrator-chat
      - brain-fast
      - brain-pro
      # AI Daily Brief lanes (operator directive 2026-08-26) — see codex_account.
      - master-brain
      - ai-desk
      - marketing-copywriter
      - marketing-breaking
      - marketing-copy-review
      - marketing-critic
      - hot-tape-wire
      - reply-voice
    allowed_tiers:
      - T0
      - T1
      - T2
    rotation_state: active
    kill_state: active
    notes: >
      Production selects this account from the third path in
      CODEX_ACCOUNT_HOMES. The standard VPS path is /var/lib/macro-codex-3.
      No credential value enters application memory or a git artifact.

  # ── GitHub token (read/write for PR operations) ──────────────────────────

  - capability_id: github_token
    kind: github_pat
    secret_ref: GITHUB_TOKEN
    scope: >
      Standard GitHub Actions GITHUB_TOKEN providing contents read/write and
      pull-request write permissions within the repository workflow context.
    storage_locus: gh-secret
    allowed_lanes:
      - ci
      - render
      - engine-render
      - whitehouse-sentinel
      - metabolism-heartbeat
      - metabolism-propose     # Phase A — contents:read only (per R-AUT-4 F4)
      - metabolism-build       # Phase A — contents+PR write behind required Opus-review
    allowed_tiers:
      - T0
      - T1
      - T2
    rotation_state: active
    kill_state: active
    notes: >
      F4 (Phase 0) per-lane permission blocks: PROPOSE = contents:read only;
      BUILD = contents+PR write behind required Opus-review check status.
      A merge PAT without 'workflows' scope will be provisioned for BUILD
      to prevent the loop from modifying workflow files (R-AUT-5).
