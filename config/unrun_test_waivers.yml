# Suites that are named by no `run:` step ON PURPOSE.
#
# SCHEMA: repo-relative suite path -> reason (one non-empty string). The gate in
# scripts/audit_unrun_tests.py enforces exactly that shape: a non-mapping file, a
# non-string reason, or an EMPTY reason is a hard red, because a waiver without an
# argument is a baseline row wearing a disguise. Every waived-and-unrun row is
# reprinted with its reason on every run — a waiver only stays honest while it is in
# front of whoever reads the census output.
#
# THIS IS NOT A DUMPING GROUND, and it is not the backlog. The 2026-08-09 backlog of
# 969 unrun suites lives in config/unrun_test_baseline.json, which is shrink-only.
# A row here is an EXCEPTION: it needs a measured reason, a named owner, and the
# condition under which it is deleted. The default answer for a new dark suite is to
# wire it into the job that owns its subject (see the signal-contract
# research-resident steps in .github/ci/legacy-jobs.yml for the pattern).

tests/test_entry_radar_w8_rig.py: >-
  W8 Live Entry Radar reference RIG (PR #5737). Listing this file in
  signal-contract edits .github/ci/legacy-jobs.yml, which is a global CI
  invalidator (full 194-job suite) and a ci-authority hit. Measured on
  7a21c455: that listing ran unrun-picks-boards and failed
  tests/test_us_prophet_fusion.py byte-parity (us_prophet_v2_shadow vs
  us_prophet_v2) whose engine bytes matched main. Static pins live in
  mockups/refs/entry_radar/tools/verify.py; live Playwright geometry is
  W9-COND-2. Owner: WS:LIVE-ENTRY-RADAR. Delete this row and add the file
  to the signal-contract entry-radar step when W9 production CI is
  commissioned (or when a non-invalidating pack path exists).

research/prophet_us_audit/test_veto_leg_isolation.py: >-
  test_the_232_bar_warmup_constant_is_the_engine_s_own_measurement fails
  deterministically on main: engine LEG_WARMUP_BARS['m3_s3']=232 but measured minimal
  known-at N=239 on the suite's own fixed fixture (start 2022-01-03, seed 5) — constant
  went stale in the 2D/3D re-anchor window (#4732..#5071); Prophet US program to
  re-pin, then delete this row and add the file to the signal-contract prophet step
  (2026-08-09).

tests/test_ship_loop_hold_wrapper.py: >-
  Intentionally owned by the always-on fast fence rather than a legacy CI pack:
  tests/test_fence_checkout_contract.py dynamically loads this canonical suite and
  executes every HOLD-FOR-SOL terminal-state regression inside fence-pack. Owner is
  WS:CI-MERGE-CONTROL-PLANE / DEC:SOL-HOLD-IS-A-MERGE-BARRIER. Delete this waiver if
  the unrun-test auditor learns transitive fast-fence ownership or the suite is later
  named directly by the canonical fence runner; do not leave it dark.

tests/test_pr_linkage_validator.py: >-
  MAS-28 report-only W1 contract suite. Owner WS:AGENT-OS / MAS-28. W0 forbids
  adding an enforcement or workflow runner for this analyzer. Delete when a
  separately authorized lawful nonblocking runner names this suite, or the suite is deleted.
tests/test_pr_linkage_parser.py: >-
  MAS-28 report-only W1 parser suite. Owner WS:AGENT-OS / MAS-28. W0 forbids
  adding an enforcement or workflow runner for this analyzer. Delete when a
  separately authorized lawful nonblocking runner names this suite, or the suite is deleted.
tests/test_pr_linkage_cli.py: >-
  MAS-28 report-only W1 CLI suite. Owner WS:AGENT-OS / MAS-28. W0 forbids adding
  an enforcement or workflow runner for this analyzer. Delete when a separately
  authorized lawful nonblocking runner names this suite, or the suite is deleted.
tests/test_pr_linkage_mutations.py: >-
  MAS-28 report-only W1 mutation suite. Owner WS:AGENT-OS / MAS-28. W0 forbids
  adding an enforcement or workflow runner for this analyzer. Delete when a
  separately authorized lawful nonblocking runner names this suite, or the suite is deleted.
tests/test_pr_linkage_contract_matrix.py: >-
  MAS-28 report-only W1 contract-matrix suite. Owner WS:AGENT-OS / MAS-28. W0
  forbids adding an enforcement or workflow runner for this analyzer. Delete when a
  separately authorized lawful nonblocking runner names this suite, or the suite is deleted.
tests/test_pr_linkage_header_executable.py: >-
  MAS-28 report-only W1 header-executable suite. Owner WS:AGENT-OS / MAS-28. W0
  forbids adding an enforcement or workflow runner for this analyzer. Delete when a
  separately authorized lawful nonblocking runner names this suite, or the suite is deleted.
tests/test_pr_linkage_epoch_executable.py: >-
  MAS-28 report-only W1 epoch-executable suite. Owner WS:AGENT-OS / MAS-28. W0
  forbids adding an enforcement or workflow runner for this analyzer. Delete when a
  separately authorized lawful nonblocking runner names this suite, or the suite is deleted.
tests/test_pr_linkage_schema_matrix.py: >-
  MAS-28 report-only W1 schema-matrix suite. Owner WS:AGENT-OS / MAS-28. W0 forbids
  adding an enforcement or workflow runner for this analyzer. Delete when a
  separately authorized lawful nonblocking runner names this suite, or the suite is deleted.
tests/test_pr_linkage_rulecase_matrix.py: >-
  MAS-28 report-only W1 rulecase-matrix suite. Owner WS:AGENT-OS / MAS-28. W0
  forbids adding an enforcement or workflow runner for this analyzer. Delete when a
  separately authorized lawful nonblocking runner names this suite, or the suite is deleted.
tests/test_pr_linkage_operational_routes.py: >-
  MAS-28 report-only W1 operational-route suite. Owner WS:AGENT-OS / MAS-28. W0
  forbids adding an enforcement or workflow runner for this analyzer. Delete when a
  separately authorized lawful nonblocking runner names this suite, or the suite is deleted.
tests/test_pr_linkage_purity_boundaries.py: >-
  MAS-28 report-only W1 purity-boundary suite. Owner WS:AGENT-OS / MAS-28. W0
  forbids adding an enforcement or workflow runner for this analyzer. Delete when a
  separately authorized lawful nonblocking runner names this suite, or the suite is deleted.
tests/test_pr_linkage_hostile_regressions.py: >-
  MAS-28 report-only W1 hostile-regression suite. Owner WS:AGENT-OS / MAS-28. W0
  forbids adding an enforcement or workflow runner for this analyzer. Delete when a
  separately authorized lawful nonblocking runner names this suite, or the suite is deleted.

tests/test_f01_credit_wiring_trace.py: >-
  Packet B-A-F01-2 (records-only): guard suite for the F01 credit/commodity
  wiring trace (research/market_intelligence_productization/
  F01_CREDIT_AND_COMMODITY_WIRING_TRACE_2026-09-06.md) and its DSC
  (agentos/discoveries/DSC-F01-CREDIT-STRESS-REACHES-ONE-LABELLED-SURFACE-NO-DEDICATED-PAGE.md). It
  regex-extracts every file:line anchor cited in the trace and asserts the
  file exists and the line is in range, so it fails loudly if the credit
  plane moves out from under the record. No engine/product code is touched
  by this packet, so there is no signal-contract step that owns this
  subject to wire it into without a global CI-authority invalidation
  (.github/ci/legacy-jobs.yml is out of this packet's owned paths). Owner:
  Meta-CEO B, lane F01. Delete this row and wire the file into the
  research-resident F01/credit signal-contract step when one exists.

tests/test_market_ontology_f06_scope.py: >-
  Packet B-F06-2 (records-only): pins the F06 scope-freeze doc (the no-ranker
  ceiling for MO-DELTA-002 and the 8-panel display-only B1B cockpit map for
  MO-PAID-021) against
  research/market_intelligence_productization/MARKET_ONTOLOGY_F06_SCREENER_AND_COCKPIT_SCOPE_2026-09-06.md.
  Not wired into .github/ci/legacy-jobs.yml because that file is a global CI
  invalidator (full 194-job suite, a ci-authority hit) and this suite only reads
  one static research/market_intelligence_productization markdown file — no
  engine or signal-contract path. Owner: Meta-CEO B, lane F06. Adopted by a
  market-ontology-records CI job when one is commissioned (Meta-CEO B,
  B-PLAT-5); delete this row then.
tests/test_market_ontology_half_a_k_chain_docket.py: >-
  Packet B-A-F04-K1 (records-only): pins the Half-A K-chain gate docket + its DEC
  record against MARKET_ONTOLOGY_F00C_GRANULAR_CLOSURE_LEDGER_2026-09-02.csv. Not
  wired into .github/ci/legacy-jobs.yml because that file is a global CI invalidator
  (full 194-job suite, a ci-authority hit) and this suite only reads two static
  research/agentos markdown files plus one CSV — no engine or signal-contract path.
  Owner: Meta-CEO B, lane F04 (+F05 spillover). Adopted by a market-ontology-records
  CI job when one is commissioned (Meta-CEO B, B-PLAT-5); delete this row then.

tests/test_market_ontology_f11_post_vertical_contract.py: >-
  MO-BB4 packet B-F11-3 (PR #6963): records-only contract doc + agentos DEC test for
  F00C ledger rows MO-PAID-031/032/054. Suite is pure text assertions (no network,
  no data/, no site/) and passes locally via
  `python -m pytest tests/test_market_ontology_f11_post_vertical_contract.py -q`
  (16 passed). Owner: WS:MARKET-OS. Exit condition: adopted by the
  market-ontology-records CI job when it lands (Meta-CEO B, B-PLAT-5, in flight);
  delete this row then.

tests/test_market_ontology_f13_accuracy_ledger_spec.py: >-
  Records-only spec-freeze suite for packet B-F13-4 (MO-DELTA-007 personal accuracy
  ledger spec, F13-OPS-LEARNING). Wiring it into a legacy-jobs.yml signal-contract step
  is a global CI invalidator (full 194-job suite) and a ci-authority hit for a suite
  that only reads one markdown file. Owner: Meta-CEO B, lane F13. Adopted by the
  market-ontology-records CI job when it is commissioned (Meta-CEO B, B-PLAT-5);
  delete this row then.

tests/test_market_ontology_f12_commercial_scope.py: >-
  Packet B-F12-6 (records-only): pins the F12 post-tenancy commercial/account scope
  doc's ledger-row answers (MO-PAID-079 commercial_only, MO-PAID-080 account_only),
  the Chairman pricing-defer blockquote, the sibling-boundary bullets, and the
  typed-null / no-fabrication / no-"validated" invariants. Not wired into
  .github/ci/legacy-jobs.yml because that file is a global CI invalidator (full
  194-job suite, a ci-authority hit) and this suite only reads one static research/
  markdown doc plus the repo's own file tree — no engine or signal-contract path.
  Run directly via `python3 -m pytest tests/test_market_ontology_f12_commercial_scope.py -q`
  in CI triage / local verification. Owner: Meta-CEO B, lane F12. Adopted by the
  market-ontology-records CI job when it is commissioned (Meta-CEO B, B-PLAT-5);
  delete this row then.

tests/test_market_ontology_half_b_rights_docket.py: >-
  Records-only regression for packet B-F09-7 (Half-B rights/upstream-gate docket):
  asserts shape of a new research/ doc + a new agentos/decisions/ DEC record + 20
  cell edits in the F00C ledger CSV. No engine/scripts subject owns this file, and
  listing it in .github/ci/legacy-jobs.yml is a global CI invalidator (full-suite,
  ci-authority hit) for a records-only, no-render packet. Run directly via
  `python -m pytest tests/test_market_ontology_half_b_rights_docket.py -q` in CI
  triage / local verification. Owner: Meta-CEO B wave, lane F09. Delete this row
  if a market-ontology-records CI step is ever commissioned.
