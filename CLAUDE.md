# Macro Dashboard — agent instructions

## External SSD storage on configured hosts (operator 2026-09-06)

A host with `~/.config/mastermind/worktree-storage.json` must create new local
agent worktrees under that policy's external root. This storage placement rule
supersedes the internal session-root placement rules below on that host only.
The canonical Git remote, branch/review rules, and existing checkout identities
are unchanged. No missing-drive, full-drive, or wrong-volume condition permits
an internal fallback. The checked-in creation hook delegates to
`scripts/worktree_storage.py`; user-level Claude hooks use its installed copy.
Codex's native Worktree root and every Claude Desktop profile's Worktree location
must point to their client directories under the same external root. Standalone
creation uses that helper's `create` command with JSON `cwd`, `name`, and the
actual `session_id`; do not invent or reuse another session's identity.

Existing active worktrees and the shared Git stores stay in place. New external
worktrees retain sparse defaults. Storage-owned removable-volume locks may be
released by the existing GC only after current volume, process, clean-tree and
recoverability checks; unrelated locks remain protected. An unavailable SSD
blocks cleanup/pruning. This is a physical storage policy, not an additional
scheduler, authority registry or host concurrency rule. See
`research/EXTERNAL_WORKTREE_STORAGE_ROLLOUT_2026_09_06.md` for installation,
verification, activation boundaries and rollback.


Signal-engine + static-site repo (engines in `engine/`, builders in `scripts/`, rendered site in `site/`, programs/masterplans in `research/`). Nightly pipeline runs on a self-hosted Mac Studio via GitHub Actions; render budget is law (~67 min, 4-core-bound) — heavy compute goes off the render path, artifacts to R2.

## Shared workspace + completion (STANDING)

- **The canonical tree is GitHub `origin/main`** (`mastermindx-market-intelligence/macro`) — never a local folder. The **only** local project root is `/Users/chriswong/Documents/Cluade/macro-main` (operator 2026-08-15); **never open `/Users/chriswong/Documents/Cluade/Macro Dashboard` as a workspace** — not as a session cwd, not as an editor root. **At session start: `git fetch origin && git merge --ff-only origin/main`; if the fast-forward FAILS, STOP and tell the operator** — do not rebase, reset, or force past it. Work from a fresh remote-default worktree on a `claude/<task>` branch; never use `~/.codex/worktrees`, `/private/tmp`, a `codex/` branch, or the occupied primary checkout for project work.
- **A merged PR does not update any folder until that folder fast-forwards.** Merging is a GitHub-side event; every local checkout, worktree, runner workspace, and VPS clone keeps its old bytes until it pulls. Verify state against `origin/main`, never against the folder you are standing in — treating a local checkout as truth is how sessions diagnose already-fixed bugs, re-fix merged heals, and clobber main with stale bytes.
- **`macro-main` is a LINKED WORKTREE, not a second clone (verified 2026-08-15).** `macro-main/.git` is a gitfile pointing at `Macro Dashboard/.git/worktrees/macro-main`, so `Macro Dashboard/.git` still owns every object, ref, config, remote, reflog, and the entire worktree registry — that is why `git worktree list` run from `macro-main` prints `Macro Dashboard` FIRST (it is the main working tree). "Never open it as a workspace" is therefore a WORKSPACE rule, NOT "that folder is obsolete, clean it up": never `rm -rf`, move, rename, or iCloud-relocate it, and never run repo-wide destructive git ops from inside it — deleting it destroys `macro-main` and every sibling worktree at once. The clone is also blobless (`remote.origin.partialclonefilter=blob:none`, promisor), so history ops needing file contents fetch over the network and fail offline.
- Macro Dashboard and `/Users/chriswong/Documents/Cluade/charting-app` are one connected product. Check cross-repo authentication, subscriptions, data contracts, APIs, and deployment effects when relevant.
- **DONE** for ordinary substantive verified work is the whole chain **commit → push → PR → CI → same-day squash-merge → live verification** — never abandoned partway, never handed back to the operator to finish. **ONE SESSION OWNS ALL OF IT.** There is no earlier "worker done": the rule that let a session terminate on an armed PR was REMOVED by the project owner on 2026-08-12 ("ur the owner of this project so u keep it until its finished, no need for handoff") after it reported an unfinished job as complete — a session declared itself done while its PR sat `merge-blocked` on a red check, and the owner had to reopen it by hand. Stopping at a local commit, at an open PR, or at an armed-but-unmerged PR is abandoned work, not delivered work. **The one non-merge terminal state is a ratified `HOLD-FOR-SOL` that satisfies `DEC:SOL-HOLD-IS-A-MERGE-BARRIER`: exact head pushed and clean, binding checks concluded green, PR DRAFT, no `merge-on-green`, native auto-merge null, and hold text naming Sol authority + Sol release condition. That state is `PARKED`: terminal for the current ship/merge attempt, NOT SHIPPED, and not a retryable `SHIP LOOP BLOCKED`; report it once and do not poll, arm, mark ready, merge, or re-enter the ship loop until Sol releases the hold. **That terminality is scoped to the ship attempt, never to the worker (`DEC:HOLD-PARKS-SHIP-NOT-DIALOGUE`):** the reciprocal worker↔Sol child dialogue remains nonterminal, the same child, carrier, branch and PR resume on a same-carrier Sol `CONTINUE`/`REQUEST_REPAIR`, and only an explicit same-carrier Sol STOP (or `ACCEPTED / STOP`, `CLOSED / STOP`) closes the child. Before yielding at PARKED you must already have posted one exact-carrier `RESULT / HOLD-FOR-SOL` and established a truthful continuation path — `WATCH_ARMED` only for a real verified registration, otherwise `WATCH_UNAVAILABLE` naming the surface checked and the exact failure. "Waiting for Sol" is not a continuation path, and going silent on the holding authority is not a lawful stop.** `merge-on-green` stays available as a **backstop that may PERFORM the merge** for ordinary work — arming it saves you the merge command — but arming it is NOT a reason to stop: stay until the PR is merged and the change is verified live. Hold only on an explicit operator/Sol request, a genuine non-spurious failing check, or a real deployment blocker.
- **Merge on CONCLUDED checks, never mid-flight (operator 2026-07-28).** "Not red" is not "green": a pending check is not a pass, and an `--admin` merge while the PR's packs are still running used to cancel the PR's own proof run (`pull_request: closed` superseding the live concurrency group — PR #3867 merged at +3 min, packs died `cancelled`, head unproven forever; measured fallout: 100 ci.yml runs/8h with 6 successes, red main, every session pinned). ci.yml now fences merged-close events into their own group so the proof run survives a fast merge, but the discipline stands: wait for the packs to conclude (green, or red that is only the known-spurious "Workers Builds: macro") before squash-merging. Do NOT arm `gh pr merge --auto --squash` as the wait — main carries no branch protection, so GitHub auto-merge has no required checks to gate on and merges IMMEDIATELY (verified PR #3889, 2026-07-28: merged ~1 min after arming with packs pending). That "no protection" is the NORMAL state, not a law of nature: a repository ruleset can appear at any time and reject even the nightly's bot pushes with GH013 — `ci-recovery-bootstrap-freeze-2026-08-15` did exactly that 08-15→08-17 (org-admin-only bypass, no DEC record), froze every Prophet board for three days while the engine kept building green, and only the push died; on mysterious push failures check `gh api repos/{owner}/{repo}/rulesets` FIRST, and any deliberate freeze owes a DEC record + expiry plan (research/PROPHET_OUTAGE_2026_08_17_POSTMORTEM.md). **ARM `merge-on-green`, THEN STAY:** after opening an ordinary PR run `gh pr edit <n> --add-label merge-on-green`. **Current topology (2026-08-21): `.github/workflows/merge-on-green.yml` still runs on `[self-hosted, macOS, ARM64, merge-control]` on the M2; it is NOT yet GitHub-hosted.** W1-A's hosted read-only canary is merged, but production authority stays on M2 until the required post-merge canary receipts accept W1-B. Never describe or reason about the production arbiter as hosted before that cutover lands. The sweeper squash-merges an armed PR once every check has CONCLUDED clean — the spurious "Workers Builds: macro" X excluded — while a genuine red or a merge conflict gets the `merge-blocked` label plus ONE explanatory comment instead of a merge, and the label stays armed so a later rerun that greens the head merges on the next sweep. That is a BACKSTOP, not an exit: the sweeper cannot fix a red, cannot resolve a conflict, and cannot verify the change live, and you will not learn those are needed if you have already stopped. For **ordinary** work the answer to "am I done" is "is it merged". A fully ratified Sol hold is the explicit exception above: answer `PARKED`, not `SHIPPED`, and stop retrying the forbidden merge. Merging by hand on concluded-green stays fully valid and is often faster than waiting a sweep. **Disarming is never silent (PR #5291, 2026-08-11):** the sweeper never removes `merge-on-green` — only sessions do. Removing a sibling's arm to take a PR manual requires, in the same act, a visible marker (`merge-blocked` or a PR comment naming your session and intent) and ownership of that PR to merged-or-handed-back. A bare `gh pr edit --remove-label merge-on-green` with no marker left PR #5291 silently unmergeable twice in one evening (codex transcript receipts, 02:13:34Z/02:21:36Z). After any accidental fast merge, the surviving PR proof run is the merge's evidence — watch it to conclusion. **A recorded HOLD is a merge BARRIER (Sol 2026-08-19, #5974/#5953):** a hold stated in a PR's body or comments (HOLD-FOR-SOL / "do not merge") binds EVERY merge path — the sweeper, blanket-arming sessions, and manual merges alike — regardless of label state; enforce it as state (no arming label, `autoMergeRequest` null, DRAFT, hold comment naming authority + release condition), and never arm a PR you did not open without grepping its title+body+comments for a hold. Once that hold protocol is complete, exact head is pushed/clean, and binding checks are green, classify the delivery state `PARKED / HOLD-FOR-SOL`: terminal for the current ship/merge attempt — not for the worker↔Sol child dialogue, which stays resumable — not deployed/live evidence, not SHIPPED, and not a blocker to poll. Conditional merge authority granted for one PR NEVER transfers to any other PR (`DEC:SOL-HOLD-IS-A-MERGE-BARRIER`). `--admin` remains only for the spurious Workers X, docs-only PRs that trigger no pack checks, and genuine wedges — never to outrun CI.
- **A `merge-blocked` backlog means main is UNPROVEN, not that the PRs are bad (#5037, 2026-08-08).** The sweeper's base-inherited-red refresh — the mechanism that drains an armed backlog once main is healed — can only tell an inherited red from a real one against a RECENT proof of main. `ci.yml` has no `push` trigger, so main is proven ONLY by a `workflow_dispatch`, while the nightly/wire lanes push ~24 `[skip ci]` commits per 2 hours — so any commit-window heuristic ages out in ~100 minutes. Measured 2026-08-08: main's newest proof sat **117 commits / 12 HOURS** back, the refresh resolved zero pack names, and 61 PRs sat armed with 60 of them red on `ci-pack-2`/`ci-pack-3` that main's own last run had proven green. Three properties now hold and must not be regressed: the proof is resolved from the newest completed `ci.yml`/`fences.yml` **RUN on main** (velocity-independent, and cheaper than the walk it replaced); a refresh additionally requires that proof to **POSTDATE** the PR's failing checks (correctness — a green that predates your red does not excuse it — and the loop guard, because `update-branch`'s 422-on-current-head does NOT prevent loops when main moves every few minutes); and the sweeper **dispatches its own main baseline** when its proof is too stale to answer the reds it just saw. **Diagnose a fresh backlog from the sweep log first:** a summary reading `0 main commit(s) classified`, or a proof set carrying no `ci-pack-*`, means the refresh path is closed no matter how green main is. Operator lever unchanged: `gh workflow run ci.yml --ref main`. **But NEVER dispatch over a live baseline (livelock, measured 2026-08-09).** Main-ref ci.yml dispatches share ONE concurrency group with `cancel-in-progress: true`, so every re-dispatch KILLS the in-flight proof: the 11:00Z baseline died 44 min in — likely minutes from concluding — to a sibling session's re-dispatch, whose own run died 4 min later to the next session's. With several pinned sessions each firing the lever, no proof ever concludes, the sweeper keeps reading `0 main commit(s) classified`, and the entire fleet stays pinned — the escape hatch IS the lock. (fences.yml's main-push runs were dying the same way under main's ~1/min push cadence, closing the other proof source; structural fix = event-conditional cancel-in-progress, in flight 2026-08-09.) Preflight before the lever: `gh run list --workflow ci.yml --branch main --json databaseId,status,createdAt --jq '[.[]|select(.status!="completed")]'` — anything `queued`/`in_progress` → WATCH it (`gh run watch <id> --interval 60`) instead of dispatching. Dispatch only over a clear field, or over a run stuck `queued` >40 min with the pool otherwise moving (orphaned-run escape — your dispatch is then the mercy kill, not a murder).
- Account-local Claude memory is not automatically shared with Codex or other accounts. Durable operating rules belong in both repository `CLAUDE.md` and `AGENTS.md`; update both when the standard changes.
- Claude enforces the completion contract with the tracked SessionStart guard plus a Stop adapter. SessionStart still runs `.claude/hooks/ship_loop_guard.py`; Stop routes through `scripts/ship_loop_hold_wrapper.py`, which delegates every ordinary state back to the same canonical guard and recognizes only a fully lawful Sol hold as terminal `SHIP LOOP PARKED`. **A lawful hold whose checks are not yet green gets `HOLD-FOR-SOL WAITING` (or `HOLD-FOR-SOL CHECKS RED`), on `claude/*` as well as `sol/*` (2026-08-28).** That interception used to be `sol/*`-only, so an ordinary held PR fell through to the `unmerged` message and was told to squash-merge and deploy the one PR `DEC:SOL-HOLD-IS-A-MERGE-BARRIER` forbids merging — measured at 121 consecutive such blocks on PR #6608. It corrects the ADVICE only: pending and red still BLOCK, `parked` is still the sole terminal exit and still demands every binding check concluded green, and waiting on CI still does not qualify for the escape ladder — so answer that block with a one-line hold note, not a fresh poll and not a `SHIP LOOP BLOCKED` report. Ordinary `unmerged` remains satisfied by an actually-MERGED PR and by nothing else — an armed `merge-on-green` PR blocks like any other unmerged one, and the block NAMES the reds the sweeper would refuse so you are told what to fix rather than merely that you may not leave. A red that is genuinely your head's files `ci_failed_unmerged`, an INTERNAL code on purpose (10 consecutive / 15 total, NOT the external 2/3): the state this rule exists to prevent — alive session, armed PR, red the sweeper will never merge, head still pushable — must not also be the cheapest state in the guard to leave. The dirty snapshot judges only THIS checkout's work: untracked entries under another fleet's worktree roots (`.claude/worktrees/`, `.claire/worktrees/`, `.codex/worktrees/`, `.codex-worktrees/` — the same four the `.gitignore` and `config/worktree_gc.json` lists carry, pinned in step by `tests/test_agent_worktree_roots.py`) are excluded — you can neither commit nor safely delete another session's checkout — and a path that LEAVES `git status`, whether committed or newly ignored, stops counting (adding `.codex-worktrees/` to `.git/info/exclude` mid-session once turned 34 baselined directories into an unsatisfiable block: the remedy caused the block). Both halves are fail-closed — tracked content under those roots gates normally, and a tracked file the session DELETED is still reported ` D`, so it still blocks. An escape ladder now keeps an unsatisfiable gate from trapping a session forever: an EXTERNAL blocker exits at 2 consecutive OR 3 cumulative external blocks; ANY code (including internal ones — unmerged/ci_failed_unmerged/unpushed/uncommitted/unsafe_branch/guard_error) exits at 10 consecutive OR 15 total blocks. Every exit still requires an explicit `SHIP LOOP BLOCKED:` evidence report with `stop_hook_active` set (no first-attempt bailout); local cleanup, authentication setup, and waiting do not qualify. **A ratified ladder exit is REMEMBERED for the exact frozen state it excused (2026-08-19):** once the full ladder has fired for a `ci_failed` merged-head block, the key `ci_failed:<head>:<merge>:<sha256(reason)[:12]>` passes later Stops without demanding the identical report again — one evidence report per frozen state, not one per Stop (the 2026-08-19 authority-frozen main-red-repair session re-filed the same report dozens of times; operator complained). The reason digest is load-bearing: a rerun or late cron that binds a DIFFERENT red to the same shas mints a different key and re-blocks. A new merge gates fresh; internal codes and evolving states (`render_pending`) carry no key and are unchanged. An IN-FLIGHT covering render DEFERS rather than blocks (operator 2026-07-27) — a queued/running render whose head covers the merge satisfies the gate and the session proceeds to live verification, because the VPS's 3-min pull makes the merge live regardless, the shared lane owns the re-bake, and the nightly `scope=all` re-render is the backstop; a lane that NEVER started or that concluded FAILED with no in-flight successor still blocks. The CI gate is base-side-aware on BOTH sides of the merge. Merged head: a red that provably pre-existed on main — same check red on ≥2 independent sibling PR heads pre-merge, or a green ci.yml run on a main descendant — is excluded BY NAME. **An authority-frozen merged head is no longer unclearable forever (2026-08-19, DEC-AUTHORITY-FREEZE-CLEARS-ON-DESCENDANT-BASELINE):** a merged head whose semantic evidence records `authority_changed=true` (any edit in the CI-authority inventory — `scripts/**`, `.github/ci/**`, `.github/workflows/**`, `.claude/hooks/**`, top-level `*.py`, conftest/pyproject; `scripts/ci_authority_paths.py`) still may NOT use candidate-era semantic evidence or descendant unit healing to excuse its red, but it clears through exactly ONE lever — a completed ci.yml run concluding SUCCESS on a main descendant of the merge, i.e. proof of main under the merged authority itself (`gh workflow run ci.yml --ref main`, with the standard in-flight preflight). Scope is the freeze ALONE: the artifact's only non-clear signals must be the freeze itself (the emitter's `authority_self_excuse_refused` row) with ZERO classified blocking units — a `pr_regression`/`unknown` unit is your own red and is never blanketed by a baseline. Infrastructure ambiguity stays outside the path, an unanswerable probe keeps the freeze, and pending checks still outrank the clearing. Operator-grant markers (labels/comments/grant files) were evaluated and REJECTED as clearing evidence: the fleet shares one token, so any such marker is mintable by the session itself — self-excuse by construction. Pre-merge (armed but unmerged, the path every session now hits on every Stop): before an armed head's red is called yours, the guard asks whether main's own newest concluded `ci.yml`/`fences.yml` run is red on the same job NAME, and failing that whether the same name is red on ≥2 independent concurrent sibling heads; either answer files an ordinary `unmerged` block naming MAIN as the cause and its lever, never "fix the cause and re-run" — that message is how several sessions end up healing one pack in parallel, and a pack is ONE check so two partial heals can never both go green (#5037, §"Healing a red pack"). Fail-closed everywhere: a stale proof, a lone sibling, an undated red, or a probe that raises excuses nothing, the red stays yours, and the gap is NAMED. Neither side ever RELEASES — only the advice changes. Operator lever for a healed base: `gh workflow run ci.yml --ref main` clears every pinned merge with one green run — same discipline as above: preflight for an in-flight baseline first; a re-dispatch cancels the very proof every pinned session is waiting on.
- `render.yml` is a shared coalescing lane: one successful push render at a merge SHA or any later main descendant covers that merge through the workflow's dirty-scope union. Monitor the shared covering run. **A paired plain-copy asset PR needs no render at all** — a non-`.j2` `templates/<name>` that also ships as `site/<name>` (the pairs `scripts/check_template_site_sync.py` enumerates) is committed straight to main and served by the VPS's 3-min pull, so it is live in minutes regardless; the guard exempts it. You forfeit only the `?v=` re-stamp, which matters for the Caddyfile's `immutable` list (`theme.js`, `live.js`, `theme.css`, `product-nav-icons.css`, `onboard.*`, `landing.css`, `account.js`, `nav_market.js`, `supabase.js`, `data_base.js`, `chat*.css`, `assets/{css,landing}/*`) — warm caches there keep the old body until a later render re-hashes the referencing pages. Never cancel or manually re-run an in-progress `render`, `engine-render`, or `daily` merely to unblock this session; a long job inside its timeout is not evidence that it is wedged. Re-run only after an unsuccessful conclusion, diagnosis/correction, and with one session owning the recovery. **This is now hook-enforced (2026-08-13).** `.claude/hooks/gh_quota_guard.py` shape 6 denies `gh run cancel`/`POST …/cancel`/`…/force-cancel` aimed at `daily.yml`, `closing-bell.yml`, `asia-close.yml`, `render.yml`, `engine-render.yml`, `weekly.yml` — plus the two watchdog lanes `prophet-rescue.yml` and `nightly-liveness.yml` (2026-08-14), because killing a watchdog is worse than killing a bake: it removes the only thing that would have noticed — and fails OPEN when the run cannot be resolved. The prose above existed on 2026-08-12 and did not bind: a live fleet session force-cancelled the US nightly's recovery dispatches six times (receipt: `POST /actions/runs/31583415065/force-cancel`), which — stacked on the #5362 workflow-size strand the night before — served Prophet US's Aug-10 picks for two full sessions. **A cancel is invisible to every staleness instrument we own**, because a killed bake and a bake that never fired leave the same trace: nothing. Killing a genuinely wedged production run is an OPERATOR call — hand it over, don't take it. **Recovery etiquette (2026-08-14):** `scripts/prophet_rescue.py` is now the only auto-redispatcher of `daily.yml`, bounded to two attempts per night across all actors; before dispatching the nightly by hand, read the open `prophet-outage` issue (the rescue lane's receipts, including attempts whose POST created no run) and never dispatch while a `daily.yml` run is queued or in progress.

## Navigation source-of-truth (STANDING)

The site has exactly two global header families. Authenticated/product pages use
`templates/_site_nav.html.j2`; its inventory is
`templates/_navlinks.html.j2`, with behavior and appearance owned by
`templates/navigation-refresh.css`, `templates/nav_market.js`, and
`templates/theme.js`. Anonymous/corporate pages use
`templates/_public_nav.html.j2`, `_public_chrome_css.html.j2`, and
`_public_chrome_js.html.j2`; the hand-authored landing mirrors that family in
`templates/index.html`/`templates/landing.css` and is parity-guarded by
`tests/test_public_chrome.py`. Never create or locally resize a third page
header. `nav_prefix` may vary by directory depth; typography, geometry, menu
contents, search behavior, responsive breakpoints, and motion may vary only in
the shared family source.

## Model routing (STANDING — token economy)

The main session may run a frontier model (Fable/Opus). **Never let fan-outs inherit it.** Workflow `agent()` calls and Agent-tool spawns inherit the session model unless you pass `model:` explicitly — under ultracode that silently burns frontier tokens on mechanical work. Route every spawn:

| Tier | Use for | Never for |
|---|---|---|
| **fable** (main loop + gated spawns/stages) | planning, adjudication, rulings, merges, final synthesis; taste-as-deliverable design (heroes, new visual language) via the gate below; spawns ONLY via `orchestrator` + FABLE-WHY; workflow stages ONLY with a script-level FABLE-WHY (see below) | bulk ×N mechanical fan-outs, ungated workflow stages, anything passing the draft-and-review test |
| **opus** | reviews, judge/red-team critics, stats/math review, hard debugging, user-facing design/UI work (via `designer`) | bulk census, mechanical non-code sweeps |
| **sonnet** | **building code/PRs/refactors/tests (via `builder`)**, census/exploration lanes, mechanical non-code fan-out, doc drafts | final adjudication, user-facing design surfaces (route to `designer`) |
| **haiku** | trivial extraction/format sweeps | anything needing judgment |

Also set `effort: 'low'` on mechanical workflow stages; reserve high effort for verify/judge stages. Rule of thumb (operator 2026-08-17 — reverses the 2026-07-21 "no more sonnet building code" order): **Sonnet builds (via `builder`); Opus reviews and designs; Fable (main loop) plans/adjudicates/merges.**

**Design lane (operator order 2026-07-18 — "design sessions degraded"):** user-facing design/UI work is judgment work, never mechanical — do NOT route it to sonnet builders. Default home = `designer` (opus-pinned agent type; auto-loads the frontend-design skill + DESIGN_DOCTRINE.md). Taste-as-deliverable surfaces that fail the draft-and-review test (hero sections, new visual language, flagship page revamps) → do the design in the main loop, or spawn fable via the existing `orchestrator` gate with `FABLE-WHY: creative: <reason>`. The `builder` agent (sonnet-pinned again, see Build lane below) may implement a *fully specified* design (exact markup/CSS handed to it) — the choice of palette, type, layout, and copy is what must stay with `designer`/Fable. This design-lane restriction is a separate 2026-07-18 ruling and is UNCHANGED by the build-lane reversal below.

**Build lane (operator order 2026-08-17 — reverses the 2026-07-21 "sonnet design and building suck too much for our purposes" order):** code implementation — writing code, PRs, refactors, tests — routes back to **Sonnet**. The `builder` agent type is re-pinned Sonnet (spawns using it pass the guard without a `model:` param); Opus keeps reviewing (`reviewer`). A fully-specified design may still be implemented by the Sonnet `builder`, but design *choices* stay with `designer`/Fable (Design lane above, unchanged — that is a separate 2026-07-18 ruling). Masterplans that say "Opus builds via `builder`" now route to Sonnet automatically — the agent type is unchanged, only its model tier moved back down. **Out of scope for this reversal:** the autonomous metabolism build loop (`scripts/metabolism_build.py`, R-V4-2) stays Opus-pinned unless separately amended — this order covers interactive/orchestrated `builder` spawns only. Recorded as `agentos/decisions/DEC-SONNET-BUILDS-AGAIN.md`, which supersedes `DEC-OPUS-BUILDS-SONNET-EXPLORES-FABLE-GATED`.

**Fable spawn exception (gated):** a single `Agent` spawn may run fable ONLY as the triple `subagent_type: 'orchestrator'` + explicit `model: 'fable'` + a `FABLE-WHY: <orchestration|brainstorm|creative>: <specific reason>` line in the prompt (hook-enforced). Legit = fails the **draft-and-review test** — Sonnet-draft + Opus/main-loop-review would NOT recover the quality: open-ended judgment steering major downstream work, long-horizon orchestration with irreversible mid-task decisions, or taste-as-deliverable creative work. Topic importance alone does NOT qualify (important-but-mechanical → sonnet; critique of an existing artifact → opus). Never fable ×N mechanical fan-outs. **Workflow stages may route to fable (operator re-enable 2026-07-18, "fable ultracode") ONLY when the script carries the same `FABLE-WHY: <category>: <specific reason>` line** — reserve fable stages for judge/synthesis work where the draft-and-review test fails; default synthesis home remains the main loop after the workflow returns. FABLE-WHY lines are the audit trail: grep them periodically for drift.

Enforcement: a PreToolUse hook (`.claude/hooks/model_routing_guard.py`, wired in `.claude/settings.json`) denies Agent/Task spawns without an explicit model, `fable` spawns outside the orchestrator+FABLE-WHY gate, fable-pinned agent frontmatter outside that gate, and Workflow scripts whose `agent()` calls carry no `model:`/`agentType` routing (or route to fable without a script-level FABLE-WHY line). Model-pinned agent types are available: `builder` (sonnet, build lane restored 2026-08-17) for build stages, `reviewer` (opus) for review stages, `designer` (opus) for design stages — spawns using them pass the guard without a `model:` param; `orchestrator` (opus floor; fable only via the gate above, always with explicit `model:`).

**Direct-spawn routing (hook-enforced amendment, 2026-08-17 — semantic ROUTE contract):** every direct Agent/Task spawn declares a semantic `ROUTE: <class>` line; `.claude/agent-routing.json` (execution-policy registry, NOT a strategic control plane) maps each route to its ONE canonical custom agent and model — `extract`→`extractor`(haiku), `census`→`scout`(sonnet), `research`→`researcher`(sonnet), `draft`→`drafter`(sonnet), `analysis`→`analyst`(opus), `debug`→`debugger`(opus), `build`→`builder`(sonnet), `review`→`reviewer`(opus), `design`→`designer`(opus), `judgment`→main loop ONLY (never spawned), `orchestration`→`orchestrator`. The orchestrator seat runs either explicit `model: 'fable'` + FABLE-WHY (unchanged gate, work failing the draft-and-review test) or — for easier orchestration that does not need frontier judgment — explicit `model: 'opus'` with a prompt directive to load the `fable-mode` skill (`.claude/skills/fable-mode`), at roughly half Fable's price and with no FABLE-WHY because no fable is spent; the same skill lets an Opus MAIN session hold the orchestrator role. `model_routing_guard.py` denies missing/unknown routes, route↔agent/model mismatches, under-specified commissions (each route's required `SECTION:` labels live in the registry), and bypass via `general-purpose`/`Explore`/`Plan`/`fork`; a `SubagentStop` hook (`.claude/hooks/agent_return_guard.py`) blocks a routed worker ONCE if its final message misses the STATUS/RESULT/EVIDENCE/GAPS/DEVIATIONS packet, then lets the second stop through (no loops). Treat a guard rejection as feedback — fix the route or commission, never evade the registry to obtain a different model. Frontmatter pins are the runtime truth; `tests/test_agent_routing_control.py` keeps registry↔frontmatter from drifting.

## Context economy (STANDING — Fable burn is CONTEXT × TURNS, not thinking; measured 2026-08-06)

**The expensive thing is re-reading context, not reasoning.** Week of 2026-07-30→08-06, measured across 3,043 local transcripts: Fable = 1,902M weighted units (26% of all model burn; Opus was 62%). Of that Fable burn, **62% was cache READS, 21% cache writes, and only 17% output.** Cache reads are the *discount* (0.1× fresh input) — never try to "avoid caching", the alternative is paying 10× for the same tokens. What costs money is the product **context size × turn count**.

Per-turn cost floor = `0.1 × context`. A turn at 150k context costs ~15k units before the model does anything; at 800k it costs ~80k. The top session that week (`bf6ce626`, 220M = 11.6% of the entire week's Fable) ran **3,539 turns at a median 419k context, max 879k, over 43h and 16 git branches**. Its turns at ≥400k context were 52% of turns but **67% of its burn**; the 700k+ band cost 92k units/turn, **3× a turn at 100–200k**. Those same 3,539 turns held at 150k would have cost ~53M instead of 196M.

**Riding context up to auto-compaction is the most expensive possible pattern.** Auto-compaction fires near the ceiling, so every turn on the approach is billed at the ceiling rate. There is no configurable compaction threshold, and the model **cannot compact itself on demand** — so the only real controls are the first two bullets below, which govern context SIZE and turn count.

- **Delegate execution; the orchestrator adjudicates.** Measured: 76% of Fable's main-loop tool calls were `Bash`/`Edit`/`Read`/`Write` and delegation was **2.6%** of tool calls — Fable was grinding, not orchestrating. A subagent's context is **discarded on return** (only its report lands), so delegating keeps tool output out of the orchestrator permanently. Route per §Model routing: `ROUTE: build`→`builder` (sonnet), `ROUTE: review`→`reviewer` (opus), `ROUTE: census`→`scout` (sonnet) — direct spawns need the ROUTE contract; `Explore`/`general-purpose` are no longer routing homes.
- **Budget what enters context.** A tool result of size S landing at turn N is re-read on every one of the remaining turns. A 20k-token Bash dump at turn 500 of a 3,539-turn session costs ~6.1M weighted units downstream. Prefer targeted `grep`/line-ranged `Read` over whole-file reads; cap command output (`head`, `--limit`, `--jq`); never pull browser screenshots or full page dumps into an orchestrator — those belong in a subagent.
- **Durable state on disk — and a session may run as long as it stays useful (operator 2026-09-01; REPEALS "one session = one task boundary").** There is no task-boundary stop. A merged, live-verified wave is a CHECKPOINT, not a session end, and one session may carry a program end-to-end across many waves and many merges. The repealed rule forced every long workflow into a relay of amnesiac sessions, and re-establishing context in each successor cost more than the stop ever saved — the measured driver is context SIZE, which session length does not by itself determine. What survives is the durable-state half, now a WRITE rule rather than a STOP rule: keep program state in `research/*_CONTINUATION_HANDOFF_<date>.md` and `agentos/handoffs/` **as you go**, so the work survives a `/clear`, a crash, or an operator handoff — not so the session must end at one. Cost control is the two bullets above, and they are unaffected: a long session held near 150k is cheap, a short one riding 800k is not — so when context grows, delegate the next wave's execution rather than shortening the session. Context figures are advisory targets, never a stop trigger; checkpoint and hand off when context is genuinely heavy or when the operator clears. Reporting honesty is unchanged: a ratified `PARKED / HOLD-FOR-SOL` is still never described as merged, shipped, deployed, or live (§Shared workspace + completion), and DONE for ordinary work is still the merge.

**Explicit non-goal — do NOT save tokens by making Fable think less.** Output is only 17% of Fable burn, so cutting effort/reasoning degrades the model for at most a sixth of the cost. Keep `effort` where the task needs it. The savings are in *where work happens* and *how big the context is*, never in the quality of the thinking. Modelled on the measured top-12 sessions: delegating execution (−40% turns) plus holding context near 150k cuts those sessions ~66% — about **−28% of the week's Fable burn** — with the same work done.

## Spawn-handoff law (STANDING — quality does not travel by pointer; onboarding postmortem 2026-07-23)

A spawned/chipped session starts with ONLY its prompt + the target repo's CLAUDE/AGENTS file. The onboarding flow shipped broken-and-ugly despite a correct masterplan AND correct model tiers (Fable loop + Opus builders): reference shots existed only as prose, acceptance gates sat at §6/§7 of a referenced doc, the target repo's agent laws were 5 lines of Next.js warnings, and the PR self-merged with no visual artifact. Tiering is NOT the quality lever — binding context is. When commissioning build work (chips, handoffs, cross-repo especially):

1. **Acceptance gates INLINE in the spawn prompt**, phrased "not done unless": fresh end-to-end happy path with zero manual workarounds (a race you reload around is a bug you own); per-step visual crops vs the reference (light+dark+zh where applicable) posted in the PR body; entry points actually wired. A masterplan pointer is context, not enforcement.
2. **Reference images = committed files** (`mockups/refs/<program>/`) with paths in the prompt. Never hand off a look in prose — the session cannot see your screenshots.
3. **Design-spec-first**: flagship user-facing surfaces get exact markup/CSS pinned in the spawned session's main loop (frontend-design skill + target repo design docs) BEFORE builders assemble anything. "Component assembly" on unpinned design = house-idiom drift, even with Opus.
4. **No child-agent self-merge on first pass** of flagship UI — the spawned builder returns its PR + visual artifact to the commissioning main session. The main session reviews it and completes the normal squash-merge/live chain in the same task unless the operator explicitly requests a hold or a genuine check is red.
5. **Masterplans for user-facing builds carry ACCEPTANCE GATES as §0** at the top of the file, not buried mid-doc.
6. **Audit the target repo's CLAUDE/AGENTS file before spawning** — if it carries no design/verification laws, fix that first (charting-app `terminal/AGENTS.md` got its laws 2026-07-23).
7. **The build surface follows the FUNNEL, not the plumbing** — where the auth/billing machinery lives never decides where the user experience lives (onboarding sheet belonged to the landing, not the terminal app).

## Agent OS — the company knowledge plane (STANDING)

`agentos/` in THIS repo is the canonical cross-session, cross-model organizational memory for all three repos (macro / terminal / mastermind): workstreams `WS-*`, decisions `DEC-*`, discoveries `DSC-*`, handoffs. **Read `agentos/README.md` before writing there** (schemas: `agentos/schema/`; handoff protocol: `research/MASTERMIND_AGENT_HANDOFF_PROTOCOL.md`). It is a KNOWLEDGE plane, never a control plane (invariant I1): nothing in it gates, dispatches, schedules, or proves liveness — execution stays with the fleet hooks (sessions) and Mastermind `control_plane/` (processes), and a `claim:` is an advisory author's note, never evidence a worker is alive (`DEC:AGENTOS-CLAIMS-ARE-NOT-LIVE-ACTIVITY`). Never create a second store or a local mirror in another repo (`DEC:AGENTOS-HOME-IS-MACRO` — real cross-repo write friction is grounds to supersede that record, never to mirror silently).

- **Task start:** if the task belongs to an existing workstream (check `agentos/workstreams/`, or `grep -rl "<path you'll touch>" agentos/workstreams/` against `owns_paths`), read its `WS-*` record, its latest `agentos/handoffs/<WS-KEY>-*.md`, and the `DEC:`/`DSC:` records they cite. `do_not_redo` entries are binding unless refuted with new evidence. This is context, not permission — Charter, strategic state, authority map, DNR, fleet law, and the assigned directive still govern.
- **During work:** mint a `DSC-<KEY>` when you verify a durable, non-obvious fact future sessions materially need and no canonical doc already records (requires BOTH `falsifier` and `so_what`, else it is a log line); mint a `DEC-<KEY>` when an actual choice with durable consequences is taken — question, answer, rationale, alternatives rejected, evidence, reversibility. Not session logs, not trivia. Account-local memory is NOT company memory: cross-session facts graduate to `DSC-*`.
- **At stop:** a session that claimed a workstream, minted `DEC`/`DSC` records, or leaves work in a state another session must resume writes `agentos/handoffs/<WS-KEY>-<YYYY-MM-DD>.md` in the same PR — cold-stranger test; every `verified:` claim names its command; `do_not_redo` and `danger_areas` populated. The natural moment is right before the session stops. Update the `WS-*` record only when durable state materially changes (status/`next_action` at wave boundaries, not per commit). NOT paperwork: a one-file fix that merges clean with nothing learned needs no records.
- **Hygiene:** `python3 scripts/agentos.py validate` must exit 0 (schema is fail-closed; joins fail open). Cite as `WS:<KEY>`/`DEC:<KEY>`/`DSC:<KEY>` — the colon form, never row/line numbers. Never hand-edit the generated `docs/AGENT_OS_STATE.md` / `data/governance/agent_os_state.json` (nightly is the sole regenerator, `DEC:AGENTOS-NIGHTLY-IS-THE-ONLY-REGENERATOR`); never write `created`/`updated` fields — the generator derives them from git.

## House laws (short list; details in research/ masterplans)

- **Git:** branch off **fresh `origin/main`** (never reuse a squash-merged branch); finish via commit → push → PR → same-day squash-merge. Stash stack is repo-global — never bare `git stash`/`pop`. Main checkout is often occupied by other agents; work in worktrees, never touch main checkout's git state.
- **Context Index (advisory, CXI-R19):** for lookup/comprehension/collision questions — "where is X", "what does this stat on page Y mean", "was this killed / already built" — TRY `python3 scripts/context_index_query.py search "<q>" --mode adjudication` (~1s, cited packet; site-stat glossary lives in `docs/site_semantics/`) before broad greps/exploration. Advisory while benchmark gates are red (`research/context_index/BENCHMARK_RESULTS.md`); always open cited sources before acting on them.
- **Before proposing/adjudicating new work:** use `config/mastermind_programs.yml` / `docs/MASTERMIND_SYSTEM_MAP.md` to identify the owning program, repository boundary, and existing cross-repo contract; then read `docs/PROJECT_ACTIVE_BUILD_MAP.md` (all three repos), `docs/ACTIVE_BUILD_MAP.md` (Macro detail; regen `python scripts/build_active_build_map.py`), and `research/DO_NOT_REBUILD.md` (standing kills/forbidden designs). Don't re-propose in-flight or killed topics; adjudications that kill a topic append a row to the registry with a minted stable Key (`KILL-…` §1–2 / `LAW-…` §3 / `HOLD-…` §4; inside sections 1–4 — rows elsewhere are invisible to the compiler and hard-fail CI) AND commit the regenerated compiled blocklists in the same PR — a PostToolUse hook auto-regens them on registry edits; manual heal: `python3 scripts/check_blocklist_drift.py --fix`. **Cite registry rows as `DNR:<KEY>`** (e.g. `DNR:KILL-PROPHET-POP-MERGE`), never by row/line number — numbers shift on every append and row-number citations have already mis-resolved in the wild (2026-08-05).
- **Epistemics (gauntlet = PROMOTION gate, NOT a build gate):** context/data/detection/tagging infrastructure ships display-tier **freely** — a null NEVER blocks building or accrual; the gauntlet applies only when promoting to authority (rank/size/gate). A factor that is null as a *standalone* signal is **retained as a confluence input** (it may confirm other signals when they align) — non-standalone ≠ worthless. A kill closes the *specific construction tested*, not the search space: "not found yet" ≠ "does not exist" — keep searching for the ranker. AT PROMOTION only: display-only until gauntleted; pre-registered gates; nulls printed, not hidden. The word "validated" in user-facing text is CI-enforced (`scripts/check_validated_claims.py`). LLMs may only de-escalate calibrated keys — never originate signals, scores, or escalations. (Memory: `context-accrual-fundamental-goal`.)
- **Instrument verdicts are NOT market verdicts (operator 2026-08-09).** A chain/tripwire terminal state means its declared WINDOWS failed — never "the thesis is false". Report the scope ("no 22d rolldown yet", not "no peak"); relay a falsifier's prose note only as far as its receipt supports (a trailing-63d window is blind to a fresh peak for weeks — check the receipts' second derivatives); and when a display-tier state disagrees with the terminal asset's tape or a scored organ, the DUAL-READ leads the synthesis, the state verdict is the footnote. Cost of violating this: the engine narrated "no peak; restriction still building" nightly while the operator's real-rate-peak call (gold/PGM miners, best trade of 2026) was already +20% — receipts and the Neural-Web design seed in `research/CASE_STUDY_GOLD_REAL_RATE_PEAK_2026_08.md`.
- **Adjudication coverage gate (operator 2026-08-10):** statistical rigor (prereg/held-out/CIs) does not substitute for an adversarial pass on the CONCLUSION. Before presenting a discovered rule or promotion-bearing adjudication: run it against the motivating live exemplars + the CURRENT regime and lead with that answer; report episode-level honest-N (distinct episodes, not fires/dates) and whether today is in-sample of the winning cell; name who is missing from the panel (survivorship/delistings) before trusting cohort means; red-team make-or-break calls via an opus `reviewer` before presenting. The operator will often just agree — the main loop, not the operator, must catch the wrong-altitude rule. (Memory: `discovered-rule-must-cover-the-motivating-exemplars`; AGENTS.md §Adjudication coverage gate.)
- **Design (user-first law):** read `docs/DESIGN_DOCTRINE.md` AND invoke the `frontend-design:frontend-design` skill (Skill tool) before ANY user-facing surface work — the doctrine sets the content law, the skill sets the visual bar (deliberate palette/type/signature, no templated defaults); on conflict the doctrine wins. **The visual/composition constitution is `research/MASTER_PRODUCT_DESIGN_SYSTEM_V1.md`** (archetype per route, canonical components = the specimen `mockups/design_system/specimen.html`, tokens extend theme.css only, density budgets); migration/ratchet process = `research/DESIGN_MIGRATION_FACTORY_V1.md`. Design work routes per §Model routing Design lane (opus+ chooses; a fully specified spec is implemented by the Sonnet `builder`). Glance tier = state + plain-word stance under hard word budgets (banned vocab: internal state/study names, untranslated stats, raw slugs); technicals demoted to hover/popover/detail pages; every signal panel answers "so what do I do", even when the honest answer is "watch — don't chase". Plain-word null disclosure + Tier-2 receipt is the compliant "nulls printed" form. **Falsifier/refutation language is never front-facing (operator 2026-07-27, #3821):** tripwires keep evaluating in the background; user cycle surfaces show projection windows ("windows, not certainties — re-drawn nightly"), quiet "read being updated" chips, and "what we're watching" conditions — never "falsifier fired / thesis refuted / 证伪"; full verdicts live on the Calibration Lab (measurement.html) below the fold, and the nightly forward log keeps windows gradeable.
- **Theme art direction — required (dark and light are TWO art directions, not one skin; TP-0 2026-08-27):** dark and light share information architecture, component semantics, spacing/type scales, state meanings, user actions, data contracts, ordering/density law and interaction behavior — they do **not** have to share material treatment. Dark = command center (luminance depth, instrument calm, restrained glow); light = research workspace (cool canvas, white material, hairline discipline, shadow instead of glow). **Token substitution alone is never proof of a light design** — "the same CSS still renders once the tokens swap" is precisely the failure this law exists to stop. Every material UI packet must name DARK TREATMENT, LIGHT TREATMENT, which mechanisms intentionally differ, the reference/baseline, theme-specific degraded states, and the evidence matrix (dark/light × EN/ZH × desktop 1440 / mobile 390). A packet missing the light art direction or its evidence is `PARTIAL/BLOCKED`, never `PASS`: a builder stops and escalates rather than inventing a translation, and a reviewer's `PASS` requires **both** themes judged as designs (hierarchy, material depth, semantic color, responsive composition, EN/ZH parity) — functional browser success is necessary, never sufficient. **Substantive product styling may not be authored as an opaque runtime stylesheet system inside page/composer JavaScript** (a multi-kilobyte `style.textContent`, a parallel palette/token family, or duplicated light/dark branches invisible to the design checker). JS may mount/recompose canonical DOM, set state classes, select variants, and apply genuinely data-dependent inline geometry; governed CSS owns the material decisions. Enforced forward-only by `scripts/check_design_system.py --mode enforce-added`, `scripts/check_runtime_style_injection.py`, and `scripts/check_ui_visual_evidence.py` — inherited debt reports but does not newly block.
- **Ledgers:** nightly is the sole advancer of forward ledgers; intraday lanes discard `data/` writes.
- **Neural Web + Mastermind chat (orientation):** `engine/neuralweb/` is BOTH the signal-bus governance layer (synapse registry ~516 artifacts, 128 lobes, graded spine, constitution A0–A7 — masterplan `research/NEURAL_WEB_MASTERPLAN_BY_FABLE.md`) and the customer chat AI: `brain_gateway.py` serves `/api/brain/*` for the dashboard widget AND the Terminal (mm_brain.js loads cross-origin — all brain work ships from THIS repo); `ask_brain.py` is the older `/api/ask` sibling. Per-turn grounding = the Live Market State Packet (`market_packet.py`, aggregation-only, `MACRO_LIVE_DIR` ladder); investigation method = the Market Analyst doctrine (`analyst/*.md`, mtime-reloaded — prose edits go live with NO restart; `.py` changes need the `app/deploy/update.sh` restart regex, guard-enforced). Program home: `research/MASTERMIND_SUPERINTELLIGENCE_MASTERPLAN.md`. The LLM never originates signals/scores (A7); chat context reads product artifacts only, never repo internals (CXI-R23).
- **GitHub annotations must START the line (CI-guarded):** emit `::warning`/`::error`/`::notice` with a bare `print("::warning title=<slug>::<msg>", flush=True)` — NEVER through a logger. Every builder here logs with a prefixing format, so `log.warning("::warning ...")` emits `WARNING ::warning ...` and GitHub silently drops it: the call reviews as an alarm, runs clean, and produces nothing in the Actions summary. This shipped dead five times (#3487, #3515, #3562, #3563, #3570) before #3587 swept 69 sites and added `tests/test_gh_annotation_line_start.py`. `flush` is load-bearing (stdout is block-buffered when piped in CI). Modules that never run inside an Actions step (FastAPI request paths — `brain_gateway`, `download_quota`, `view_ratelimit`) are EXEMPT and listed in that test; check `app/`/`admin/` imports before converting an `engine/` module. Converting breaks any test asserting via `caplog` — switch it to `capsys` AND assert `line.startswith("::")` so the test pins the defect, not the wording.
- **GitHub quota is ONE shared bucket (hook-enforced; now the ONLY restraint on fleet polling).** Every session waits out its own merge, so this discipline is a hard rule, not advice. `gh` authenticates as a single account token, so REST's 5,000/hr `core` pool is shared by every parallel session, the babysitter lane, and the hooks — including `ship_loop_guard.py`, which spends ~4 REST calls per Stop (up to ~16 on the rarer Stop that has a RED to attribute, where it also probes main's own proof and the sibling heads) and **fails closed** when rate-limited, so over-polling blocks the very Stop the polling was for. That probe is paid only when there IS a red — a clean or pending head costs nothing extra, which is what keeps it affordable with the whole fleet armed at once — and its two main-proof URLs are identical for every session, so the hook's shared 30-second cache collapses them to one lookup per checkout family. `.claude/hooks/gh_quota_guard.py` (PreToolUse on Bash) denies the shapes that emptied the pool on 2026-07-26 and 2026-08-09: `gh run watch` at its **default 3s interval** (use `--interval 60`+), a `gh` call in a loop sleeping under 90s, `--paginate` over check-runs/jobs (~130 checks/PR), and re-dispatching a main proof workflow over one already in flight. The guard governs HOW you watch, never WHETHER you may — reading your own PR's check state is part of owning it to the merge, and no state outside the command line makes that read illegal. On you: preflight `gh api rate_limit --jq '.resources.core.remaining'` before any long watch; exactly ONE watcher per endpoint (a second watcher on the same run buys nothing and doubles the burn); an empty or 403 response is **not** a green result. REST and GraphQL are separate pools, so `gh pr view` working does not mean `gh api` will. A ci.yml run here takes 30–34 min — pace the wait to that, not to impatience; a run four minutes old cannot be finished.
- **A Stop-hook block is NOT a demand for a fresh poll (operator 2026-08-24, REPEATED 2026-08-27; shape 7).** The single most common quota burn is not a loop or a hot `--interval` — it is one `gh pr checks <n>` per Stop-hook cycle while a 30–45 min `ci.yml` run finishes. Measured 08-27: ~25 consecutive Stop cycles, one poll each, by a session that **already had a watcher armed at 150s** reporting every transition for free. The mechanism is worth naming because it is not laziness: the Stop hook fires on EVERY turn and escalates to "If the same genuine blocker persists after another attempt, finish with `SHIP LOOP BLOCKED:`", which reads as pressure to demonstrate a fresh attempt. It is not — **a blocked Stop is satisfied by a one-line hold note and no tool call.** Waiting on CI is explicitly not a qualifying blocker for the escape ladder, so there is nothing to prove by re-reading. **The tell:** three identical bucket counts in a row means every further poll is pure waste; and an armed watcher makes polling strictly redundant, because its notifications ARE the check. `gh_quota_guard.py` shape 7 attaches an `additionalContext` nudge to a repeat of the same status shape inside 300s (the operator's own number). It **allows** — a deny would contradict the standing rule that the guard governs HOW you watch and never WHETHER, pinned by `test_ci_observation_is_never_denied_for_owning_an_open_pull_request`. Escalating shape 7 to a deny is a RULING for the operator, not a refactor. Account-local memory does not travel between sessions or to Codex, which is why this is repo law in both files.
- **Hold notes do not END the Stop loop — go quiet through the ladder, not through repetition (operator 2026-08-28).** A one-line hold note satisfies the QUOTA rule above but is NOT terminal to the guard: the Stop hook blocks again seconds later, and a session that answers every block with another note types near-identical hold notes in a tight billed loop for hours (~100 such turns measured 2026-08-28 while a HOLD-FOR-SOL carrier lawfully waited out a queued CI field under an armed watcher — the cost is context × turns, and the notes carry zero information after the first). When a long external wait is owned by an armed watcher/cron and the guard keeps blocking, check the escape-ladder threshold (ANY code: 10 consecutive / 15 total); once met, end the turn ONCE with the literal `SHIP LOOP BLOCKED:` evidence report — literal first characters, naming the PR, exact head, check state, and watcher id + cadence — and then STAY QUIET: no per-Stop hold notes, no tailing your own watcher's output file between its ticks. Real events (watcher exit, cron fire, task notification, operator message) re-invoke the session; that is the only lawful cadence for a parked wait.
- **Worktree GC:** `scripts/worktree_gc.py` sweeps the fleet session-worktree roots per `research/WORKTREE_GC_POLICY.md` (report-first; deletion only while `config/worktree_gc.json` is `armed:true` — an operator ratification act; per-host launchd, never a runner workflow). It keeps anything locked, process-occupied, dirty, unpushed, open-PR, or active <7d — park a long-lived checkout with `git worktree lock --reason "<why>" <path>`. Age gates on STRONG signals only (reflog ENTRY epochs + session transcript mtimes): file mtimes are observer-stamped repo-wide — `reflog expire` sweeps every tree's logs/HEAD at once, and status sweeps/Finder stamp index/HEAD/dir — measured pinning 137/143 dead trees "fresh", so a file-mtime gate never opens.
- **A session worktree is planted under the checkout the SESSION was launched in (2026-08-20).** `.claude/hooks/worktree_create_sparse.py` used to derive its destination from `git rev-parse --git-common-dir`, which answers with the MAIN working tree's `.git` from every checkout of a clone — a per-clone CONSTANT. So every Claude worktree landed in `Macro Dashboard/.claude/worktrees/`, the one folder the workspace law tells sessions never to open, no matter which folder the operator opened; no session could escape it by launching elsewhere. Placement now follows `--show-toplevel`, climbed out of any session root (`SESSION_WORKTREE_ROOTS`) so a spawn from inside a session tree makes a SIBLING, never a nested child the GC's depth-1 orphan scan cannot see; `MACRO_LOCAL_ROOT` is the explicit override, the same lever the Grok hook honours. **Only the BYTES moved.** The clone is one clone: every worktree stays registered in `Macro Dashboard/.git/worktrees/`, so that folder is as undeletable as ever. `scripts/worktree_gc.py` expands its repo-relative roots under EVERY host checkout for the same reason — a tree the sweeper cannot see is a tree it can never reclaim, and this host has hit ENOSPC twice. Pinned by `tests/test_worktree_placement.py`.
- **Session worktrees are SPARSE by default (R8, 2026-08-13 disk incident).** `.claude/hooks/worktree_create_sparse.py` (wired `WorktreeCreate` in the checked-in `.claude/settings.json` — `settings.local.json` is globally gitignored and cannot carry fleet law) mints each worktree off fresh `origin/main` holding every tracked top-level dir EXCEPT those in `config/sparse_worktree.json`: `data/`, `site/`, `mockups/`, `verify_shots/`. Measured 2026-08-13: those are 3.31 of 3.8 GiB (**87 %**), so a tree costs ~0.35-0.57 GiB — an ~7-11× cut. This is the half of the disk incident the GC cannot reach: at ~40 new trees/day the ~330 GiB 0-3 d ACTIVE window is by definition not FINISHED work, so arming the sweeper (#5502) drains the done pool and only a thinner footprint shrinks the live one (`research/WORKTREE_GC_POLICY.md` §0 R8; the Studio was at 1.7 Ti/1.8 Ti, ~100 GiB free, after two receipted runner ENOSPC crashes). **OPT IN the moment you touch render, `site/`, a paired plain-copy asset, or `data/`: `python3 scripts/worktree_sparse.py full`** (worktree-scoped — siblings untouched), one tree via `… add site`, state via `… status`. Nothing is hidden (omitted paths stay tracked, `git status` stays clean, and a write into one is still reported — `ship_loop_guard.py`'s dirty snapshot keeps working). **But a write into an omitted tree TRUNCATES the committed artifact** — the real bytes were never on disk to extend, so an unredirected writer replaces rather than edits: measured 2026-08-13, a suite run with `MM_DATA_GUARD` off left `data/hk_southbound/holdings.parquet` at 45,157B against 7,295,941B committed and `data/trial_ledger.jsonl` −1,411 lines. **Never `git add -A` an unexpected `data/`/`site/` diff in a sparse tree**; `python3 scripts/worktree_sparse.py clean` (report-first, `--force` deletes) restores the omitted trees. Nothing is silently greened either: `scripts/check_template_site_sync.py` REFUSES on a sparse tree instead of printing "sync OK (0 pairs checked)", and pytest headers/summaries name the omitted trees plus the opt-in command, skipping only tests marked `needs_full_checkout`. **Do NOT run the full suite in a sparse tree** — measured 08-13 it yields 1,281 failures + 419 errors across 247 files (vs 68,776 passes) purely as artifacts; opt into full first (marking all 247 was rejected — it would mask real regressions in a tenth of the suite). **Never detect sparseness with `is_dir()`** — `data/` survives `git reset --hard` as a 0-byte husk; ask `scripts/worktree_sparse.missing_dirs()`.
- **Codex sparse parity (2026-08-15).** `.codex/environments/environment.toml` is the default Codex worktree setup and `.codex/hooks.json` provides a `SessionStart` fallback; both call `python3 scripts/worktree_sparse.py auto`. `auto` converts only a linked worktree under a session root (`.claude/worktrees/` and siblings) — never the occupied primary, and never the operator's designated local root, which is itself a linked worktree. It honors `config/sparse_worktree.json`'s `enabled` switch, and never re-applies over an existing sparse selection, so `add site` survives a later startup. Codex currently exposes setup/`SessionStart` only AFTER Git creates the worktree: the final tree is the same ~0.35–0.57 GiB, but unlike Claude's pre-checkout `WorktreeCreate` hook it can transiently write the full tree during creation. Codex project hooks require one-time trust of the exact checked-in definition.
- **Cursor IDE sparse parity (2026-08-15).** Cursor has no pre-checkout `WorktreeCreate`. `.cursor/hooks.json` runs `python3 scripts/worktree_sparse.py auto` on `sessionStart` and `workspaceOpen` (the latter covers `move_agent_to_root` into a new worktree). `auto` only converts a linked worktree under a session root (`.claude/worktrees/` and siblings); it will not sparsify the occupied primary or the operator's designated local root, which is itself a linked worktree.
- **Cursor CLI + Grok sparse parity (2026-08-15, AionUi mint 2026-08-18).** Cursor CLI / Agents Window runs `.cursor/worktrees.json` `setup-worktree-unix` after it creates the worktree. Grok Build runs `.grok/hooks/session_start_sparse.py` on `SessionStart` (Grok skips unknown hook events, so the Claude `WorktreeCreate` block is a no-op there). When the session already sits in a linked session worktree the hook calls `python3 scripts/worktree_sparse.py auto` — same post-checkout thinning and session-root fail-safe as Codex. When AionUi starts the session in an empty `grok-temp-*` directory, the same hook mints a sparse tree under `.grok/worktrees/` with `git worktree add --no-checkout` (Claude's pre-checkout shape) and writes `.session-worktree`. The always-trusted `~/.grok/hooks/` copy is what AionUi actually loads; the project-local file is the Grok TUI path and needs one-time `/hooks-trust`. Launch with `--worktree-base origin/main` (Cursor) or `--ref origin/main` (Grok); default base is current HEAD, not GitHub `main`.
- **Warp/Oz sparse parity (2026-08-22).** Warp has no SessionStart or WorktreeCreate event and no settings key that can run a project hook at session start. `.warp/hooks/session_start_sparse.py` is the mint; `.agents/skills/macro-sparse-worktree` is how a Warp session discovers it and must run it before editing. It reuses only a carrier positively bound to an unambiguous current Warp conversation/task identity: terminal/session IDs are not ownership proof. The branch/path are SHA-256 digests of the complete raw identity, so it is neither logged nor truncated/sanitized into a collision. An identity-less start mints a collision-resistant carrier rather than reusing `warp-session`. A new carrier is minted under `.warp/worktrees/<name>/` with `git worktree add --no-checkout` from freshly fetched `origin/main`, and it prints `WORKSPACE=<path>` so the session can `cd` there. Fetch or path/branch collision failures fail closed and never take over a foreign carrier. It never sparsifies the occupied primary or the operator local root, and it never writes `.session-worktree` into a git checkout (that pointer is dirt there; Grok only writes it into an empty `grok-temp-*` dir).
- **Healing a red pack: claim the lane FIRST, and heal the WHOLE pack (2026-08-07, #4850 closed unmerged).** A fleet-wide red is being worked by the whole fleet, so before writing a line: (1) identify every red job **by name** and confirm its pack (`python3 scripts/run_ci_pack.py --workflow .github/ci/legacy-jobs.yml --pack-index N --pack-count 12 --validate-only`) — never trust the pack index in a failure report, `run_ci_pack.py` rebalances whenever any job's weight moves; (2) `gh pr list --search "<filename>"` plus `docs/ACTIVE_BUILD_MAP.md` for an open lane on the files you are about to touch (the §"Before proposing new work" rule above is scoped to NEW work — it does not cover heals, which is exactly how this was missed). **A pack is ONE check, so two partial heals DEADLOCK**: with two independent reds in `ci-pack-3`, the ITR-only PR stayed red from the stale report and the report-only PR stayed red from ITR — neither could ever go green, so neither could merge. One PR must carry every fix the pack needs; cherry-pick a sibling with `-x` (authorship preserved) rather than rewriting it, and read its diff first — the sibling's version is often better. Sessions SHOULD heal. The “don’t propose in-flight work” line is for new features; heals are the exception (this bullet exists because #4850 was missed that way). One PR per pack stays — different packs still get separate heal PRs. **Missing hole (2026-08-14, #5715/#5717 deadlock then #5689 absorbed both):** if TWO (or more) packs are red on main AND required `ci-gate` needs every pack green AND `gh pr merge --admin` is refused by the repository ruleset, two whole-pack PRs still cannot land. Then ONE PR labelled `main-red-repair` must carry every pack heal that `ci-gate` is blocking. Do not wait. Do not open a second `main-red-repair`. That is not a repeal of one-PR-per-pack; it is the mutual-block exception. **Re-fetch `origin/main` and re-run the job line before pushing OR merging**: on a red main the tree moves in hours (5 heal PRs landed mid-session here), and a `merge-blocked` "real content conflict" on a heal PR usually means the heal already landed. Diff against fresh main (`git diff --stat origin/main HEAD -- <files>`; empty = already there) and **close rather than force through** — a superseded regeneration silently reverts the better fix that landed behind you (#4850's live-cache report would have reverted #4842's frozen-slice render).
- **Executive OS (cross-repo — the state lives in Mastermind, not here):** company strategic state is `config/strategic_state.yml` in `/Users/chriswong/Documents/Cluade/Mastermind` (phase, north star, P0 objectives, resource policy, standing constraints), read via `control_plane.strategic_state.load_strategic_state()` — a validated reader that raises rather than handing back an empty state. The worker contract (hierarchy Chairman Chris → AI CEO GPT-5.6 Sol → COO Fable → workers; governor = `config/authority_map.yml` + `control_plane/packet_gate.py` + the governance ledger + this repo's fleet guards; the six-layer source-of-truth order; the completion rule) is Mastermind `AGENTS.md`/`CLAUDE.md` § "Executive contract". **Macro fleet law is unchanged and still governs every session here** — ship loop, model routing, merge-on-green. **Do NOT create a second strategic state, control plane, or authority map in this repo**: `duplicate_control_planes` is a standing prohibition in that file, and a Macro-side copy read by a Mastermind runtime is exactly the cross-repo authority hop it forbids. Background: `research/EXECUTIVE_OS_PHASE0_CENSUS.md` (PR #5356) and `Mastermind/research/EXECUTIVE_OS_STRATEGIC_STATE_BOOTSTRAP.md` (§4 = what Phase 1B may and may not couple to).
- **Ops:** known-spurious CI: "Workers Builds: macro" red X is ignorable. Bilingual (EN/ZH) UI; no translated text in `title=` attributes (CI-guarded). Plain-copy page assets are PAIRED: editing `templates/<name>` (non-.j2 that also ships as `site/<name>`) requires the byte-matching site copy in the same PR — run `python -m scripts.check_template_site_sync --fix` (CI-guarded; render lanes self-heal post-rebase).

## Memory frontmatter contract

Memory files (account-local, `~/.claude/projects/<project>/memory/`) are one fact per file with frontmatter: `name` (kebab slug), `description` (one line, drives recall), `metadata.type` ∈ user | feedback | project | reference. Link related memories with `[[name]]`. Index each file as one line in `MEMORY.md`. Update/delete rather than duplicate.

## Obsidian brain vault

`Macro Dashboard/ObsidianBrain/` is a live symlink view over memory + research (Dataview MOCs). It is a *view* — edit sources (memory files, research docs), never the vault copies.

<!-- Reconstructed 2026-07-05: PR #1215 shipped this file as 0 bytes (commit message described vault pointer + memory contract; content was lost — the #1052 zero-byte failure mode). Rebuilt from the memory index + program docs, with the model-routing standing rule added. -->
