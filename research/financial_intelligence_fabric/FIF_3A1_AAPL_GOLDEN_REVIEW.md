# FIF-3A1 human golden review — AAPL FY2025 10-K

Compared against the captured primary document
`tests/fixtures/fundamental_forensics/aapl_10k_2025/members/aapl-20250927.htm`
(SHA-256 `548ae59778cf08ee0f2ee088e7ece20d947076c3c01f74d2d65db4c2777e436a`),
accession `0000320193-25-000079`, SEC accepted `2025-10-31T10:01:26.000Z`.
Displayed row/column composition is the three captured HTML tables, not raw
presentation order. Discrepancies are recorded, not normalized away.

## Statement titles and roles

| Type | Role URI | Reconstructed title | Filing HTML title | Match |
|---|---|---|---|---|
| income_statement | `http://www.apple.com/role/CONSOLIDATEDSTATEMENTSOFOPERATIONS` | CONSOLIDATED STATEMENTS OF OPERATIONS | CONSOLIDATED STATEMENTS OF OPERATIONS | yes |
| balance_sheet | `http://www.apple.com/role/CONSOLIDATEDBALANCESHEETS` | CONSOLIDATED BALANCE SHEETS | CONSOLIDATED BALANCE SHEETS | yes |
| cash_flow | `http://www.apple.com/role/CONSOLIDATEDSTATEMENTSOFCASHFLOWS` | CONSOLIDATED STATEMENTS OF CASH FLOWS | CONSOLIDATED STATEMENTS OF CASH FLOWS | yes |

HTML unit legend (operations): "In millions, except number of shares, which are reflected in thousands, and per-share amounts." Reconstructed numeric cells are scale-applied whole units (USD or shares), with `scale` preserved on the cell (typically `6` for million-USD rows). Displayed `416,161` equals reconstructed `416161000000` at scale 6.

## Columns / periods

Columns are the dates printed on each captured table, bound to filing contexts used by that table. Not support ≥ 5 plus newest-2/newest-3.

- Income statement: three duration columns `2024-09-29→2025-09-27`, `2023-10-01→2024-09-28`, `2022-09-25→2023-09-30`.
- Balance sheet: two instant columns `2025-09-27`, `2024-09-28`.
- Cash flow: three duration columns, same year-ends as the income statement. Beginning-cash cells are instant facts sitting in those duration columns; column periods bind from duration contexts in the same table (Net income), not from the first numeric row.

## Income statement displayed composition (24 rows)

Re-human-reviewed against `aapl-20250927.htm` CONSOLIDATED STATEMENTS OF OPERATIONS.

1. `Net sales:` abstract header
2. `Products` — dimensioned `us-gaap:RevenueFromContractWithCustomerExcludingAssessedTax` / `srt:ProductOrServiceAxis` / `us-gaap:ProductMember`; FY2025 `307003000000` reverses to displayed `307,003`
3. `Services` — same concept, `ServiceMember`; FY2025 `109158000000` / `109,158`
4. `Total net sales` — undimensioned total `416161000000` / `416,161` (occurrence_count 4)
5. `Cost of sales:` abstract header
6. `Products` — dimensioned `CostOfGoodsAndServicesSold` / ProductMember; `194116000000`
7. `Services` — dimensioned ServiceMember; `26844000000`
8. `Total cost of sales` — `220960000000`
9. `Gross margin` — `195201000000`
10. `Operating expenses:` abstract header
11. `Research and development` — `34550000000`
12. `Selling, general and administrative` — `27601000000` (unmapped; survives)
13. `Total operating expenses`
14. `Operating income`
15. `Other income/(expense), net`
16. `Income before provision for income taxes`
17. `Provision for income taxes`
18. `Net income`
19. `Earnings per share:` abstract header
20. `Basic` — `7.49`
21. `Diluted` — `7.46` (HTML label is `Diluted`, not a parenthetical unit suffix)
22. `Shares used in computing earnings per share:` abstract header
23. `Basic` share count
24. `Diluted` share count

Hypercube/table/axis/domain/line-item metadata rows do not appear. Presentation still prefixes `Statement [Table]` / Product and Service [Axis] before line items; the displayed tree ignores that prefix.

## Balance sheet displayed composition (35 rows)

Re-human-reviewed against CONSOLIDATED BALANCE SHEETS. Two instants only.

- Captions `ASSETS:`, `Current assets:`, `Non-current assets:`, `LIABILITIES AND SHAREHOLDERS’ EQUITY:`, `Current liabilities:`, `Non-current liabilities:`, `Shareholders’ equity:` are abstract headers, not values.
- `Accounts receivable, net` FY2025 `39777000000` reverses to `39,777` (`f-163`).
- `Cash and cash equivalents` `35934000000` / `35,934`.
- `Commitments and contingencies` is `xsi:nil` (`quality_state=nil`, value null).
- Common-stock share counts stay in the equity caption (`14,773,260 and 15,116,786 shares issued and outstanding, respectively`). They are not separate reconstructed rows.

## Cash flow displayed composition (35 rows)

Re-human-reviewed against CONSOLIDATED STATEMENTS OF CASH FLOWS.

- Row 0: `Cash, cash equivalents, and restricted cash and cash equivalents, beginning balances` = `29943000000` / `29,943`.
- Operating, then investing, then financing activity blocks in filing order.
- `Cash generated by operating activities` = `111482000000` / `111,482`.
- Same cash concept later as `... ending balances` = `35934000000` / `35,934`.
- Distinction is the captured HTML labels, not a second concept.

## Representative values (FY2025 / 2025-09-27)

| Statement | As-reported label | Reconstructed value | Filing display | Reverse |
|---|---|---|---|---|
| IS | Products (Net sales) | `307003000000` | 307,003 | dimensioned ProductMember fact; `307,003` in source span |
| IS | Services (Net sales) | `109158000000` | 109,158 | dimensioned ServiceMember |
| IS | Total net sales | `416161000000` | 416,161 | undimensioned `ix:nonFraction` containing `416,161` |
| IS | Total cost of sales | `220960000000` | 220,960 | duration fact |
| IS | Gross margin | `195201000000` | 195,201 | duration fact |
| IS | Selling, general and administrative | `27601000000` | 27,601 | unmapped; row survives |
| IS | Diluted | `7.46` | 7.46 | USD/share |
| BS | Accounts receivable, net | `39777000000` | 39,777 | `ix:nonFraction` id `f-163`, instant context `c-20` |
| BS | Cash and cash equivalents | `35934000000` | 35,934 | instant |
| CF | beginning balances | `29943000000` | 29,943 | HTML beginning-balances label; instant fact in duration column |
| CF | ending balances | `35934000000` | 35,934 | HTML ending-balances label |
| CF | Cash generated by operating activities | `111482000000` | 111,482 | duration |

## Mapping coverage (this golden tree)

Standardization is optional enrichment and respects the core catalog's
`consolidated_only` dimensional profile. Dimensioned ProductMember/ServiceMember
Net sales and Cost of sales rows keep as-reported value/concept/dimensions and
stay `unmapped`. Undimensioned `Total net sales` maps to `revenue`; undimensioned
`Total cost of sales` maps to `cost_of_revenue`. SG&A, vendor non-trade
receivables, other income/(expense), several cash-flow lines, and abstracts stay
`unmapped`. Mapped examples: revenue (undimensioned total only), cost_of_revenue
(undimensioned total only), gross_profit, diluted_earnings_per_share,
accounts_receivable_net, cash_and_cash_equivalents,
net_cash_from_operating_activities. Counts: IS mapped 13 / unmapped 11; BS
mapped 14 / unmapped 21; CF mapped 8 / unmapped 27.

## Ambiguity / failure behavior reviewed

Disagreeing same-concept/same-context/same-unit facts reach cell adjudication
and yield `quality_state=ambiguous` and null value, with competing fact ids.
Agreeing duplicates keep `occurrence_count`. Canonical issuer is
`ISS:US-XNAS-AAPL` via `IssuerMaster.securities_of_issuer`; a
`SUPERSEDED_DUPLICATE_MINT` sibling is not selected. Source CIK remains
`0000320193`. Requesting ticker `AAPL` or the CIK as `entity_id` is
unknown-entity. Top-level `authority` is `{"class":"context_only","display_only":true}`.
Delivery is committed golden fixture, `attested=false`,
`production_issuer_service=false`. This is not a production issuer service.

## Recorded discrepancies (not a generic segment engine)

1. **Presentation hypercube prefix still exists in the linkbase.** It is no longer displayed. The HTML Net sales hierarchy is the reconstructed tree. Not a generic dimensional engine: only the three captured primary tables are composed this way.
2. **Definition linkbase is retained and unused** beyond the facts/contexts already in the iXBRL parse. Product/Service dimensions appear because those cells are in the captured HTML table.
3. **Agreeing duplicate iXBRL occurrences** (visible + hidden) collapse to a representative with `occurrence_count>1`. Values agree; this is not first-row-wins of a conflict. Conflicting same-identity occurrences stay `ambiguous`.
4. **Scale/display.** HTML millions vs reconstructed whole USD. Scale is on the cell; the value is not re-rounded for display.
5. **Cash-flow beginning cash is an instant fact in a duration column.** Column periods come from duration contexts in the same table. The cash concept occurs twice in the presentation tree: beginning row is `periodStartLabel`, ending row is `periodEndLabel`.
6. **Dimensional Product/Service rows are as-reported, not consolidated metrics.** Core registry `consolidated_only` leaves those rows unmapped.
7. **Reported cells stay `direct`.** `formula_dependencies` may expose the filing calculation network; that does not mean Mastermind calculated the iXBRL value.

Prior review items 1 (hypercube displayed before Net sales) and 2 (share-count rows) are closed by the displayed-table composition.
