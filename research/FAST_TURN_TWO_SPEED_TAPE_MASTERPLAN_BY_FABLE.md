# Fast-Turn Response (FTR) — two-speed tape masterplan

**Status:** ADJUDICATED — awaiting operator ratification to dispatch builds
**Date:** 2026-07-09
**Owner program:** `fast-turn` (composes over policy-shock, TIL, Oracle, sector-pulse territory with fences recorded in §2/§3)
**Method:** 6-lane Sonnet recon over pipeline/detection-chain/case-law/machinery/thesis surfaces + inline Fable forensics → Fable synthesis → 2× Opus adversarial review (case-law lens A1–A7; stats/ops lens F1–F14) → amendments applied → this adjudication.
**Operator directive:** the 2026-07-08 Iran-strike oil spike flipped leadership into semis (AVGO +4.8% on 07-08, +4.4% premarket 07-09; defensives/healthcare sold) and no basket/allocation/rotation surface responded. "Make the engines and dashboards able to respond to these quick changes in pace and sector rotation… implement more live price structures and more frequent engine runs."

---

## §0 Executive ruling

The recon falsifies the operating theory ("the spike was too fast for the system to see"). The nightly at 02:00 UTC captured the 2026-07-08 closes the same evening; the semicap basket page is stamped `as_of: 2026-07-08`. The system **saw** the event. Four separate failures made it invisible:

1. **Construction latency (dominant).** Every surface the operator checked is built to be slow, by design:
   - The allocation "Prevailing narrative" is a 63/126/252d ensemble momentum rank with a **hard 21-trading-day skip** (`engine/narrative_rotation.py:47-48`, `SKIP_D=21`) — a 12-1-style strategic allocator. A regime break at day 0 cannot enter *any* momentum input until day 22; headline flip estimate 22–40+ sessions. Nothing on the page says this. It is labeled "current best place to allocate."
   - The basket neural score (semicap 51) weights trend 0.26 (5/20/60d relative returns), breadth 0.18 (% above 50d MA), and its macro leg is absent for semicap (no `_MACRO_PRIOR`/`_SECTOR_PROXY` entry) — the score renormalises over available legs (`engine/theme_scoring.py:38-39`), so it is macro-**blind** for this basket, not macro-dragged. The fast legs did fire on 07-08 (impulse +0.555, MTF +0.4) but carry 0.07/0.16 weight, and the crowding penalty (rs_pctile 0.857 ≥ 0.85) blocks ACCUMULATE outright. Flip estimate 5–15 sessions.
   - Oracle episode onset needs `accel_z_5d > 1.0` (5-day-smoothed) + 5 consecutive sessions to confirm: ≥6 sessions minimum. Cycle engines: 14% ZigZag, EMA(10) smoothing, weekly MACD: 5–10 sessions; `cycle_blocked` suppressed the only two `buy_soon` members (KLAC, LRCX).
2. **Disconnected shock plumbing.** `repricing_coherence` printed **SHOCK 75 on 2026-07-07 — the eve of the reversal** — and the shipped shock de-escalation reflex (#2015) posts its "ranks computed on pre-shock data, discount" banner on sector.html, subsector_rotation.html and the dashboard action board… but **not** on baskets.html, allocation.html, or any basket detail page — precisely the surfaces the operator reads for rotation. The flip-confirmation lens (26 flips, 58% fade at T+1) and the policy-lever card land on the dashboard only. `sector_pulse` (1d/5d rank/score deltas, heating/hot tiers — an already-shipped fast-turn read) is not wired to the baskets page at all.
3. **Staleness defect.** `data/allocation/latest_us.json` froze at `as_of: 2026-07-06`; the 07-08/07-09 nightlies advanced baskets but not allocation, while a manual run today produces changed bytes and `as_of: 2026-07-08`. The nightly `git add data/` pathspec is broad (daily.yml:207) and the file *did* commit on 07-07 — so the fault is **upstream in content**: the sub-build is invoked from `build_baskets.py:229` under an exit-0-always wrapper with no freshness alarm, and it has been failing (or computing on a stale input) silently since. Same silent-freeze class as the Oracle Time-Machine outage (#2002). W0 root-causes the actual exception from nightly logs; the git-add theory is REJECTED on evidence.
4. **Taxonomy blindness.** The operator's evidence ticker (AVGO) lives in `ai_semiconductors`; the checked page was `semicap_equipment` (16 small/mid WFE names; AMAT was **-5.4%** over the same 5 days). No surface aggregates the AI-capex complex into one tape view. An in-house complex grouping already exists (`engine/demand_capex.py:45-52`, `AI_CAPEX_THEMES` with directness tags); the TIL crosswalk (merged 07-09) is theme-level only — complex-level grouping is TIL-owned taxonomy work (§5 W5).

**And one thing the house must say honestly:** the empirical case law is that chasing day-2 violent flips is the minority trade — the flip lens base rate is **58% fade at T+1 (15/26)**, confirmed-tier rotation continuation is NULL both directions (Oracle constitution §IV), defensive-rotation lead FAILED OOS, sector-level standalone washout→turn triggers printed NULL (Oracle P8 P-W1/S-W3; DO_NOT_REBUILD §2 "Washout × turn"), and the house's own tactical report *The Relapse* (published 07-08) explicitly read the semis bounce as short-covering to sell. "Front-run the rotation" as an auto-escalating signal is both forbidden (TI-R5, PS-R1) and empirically unsupported at current n. What is legal, buildable, and was plainly missing is: **same-day/intraday/premarket visibility of the turn, honest staleness marking, shock-conditioned de-escalation on the pages that matter, complex-level aggregation, push notification of computed states, and forward-graded fast-turn detectors run as expected-NULL meters that could earn authority later** (PS-R8 path, earliest 2027). Detection speed with printed error rates IS the product (Oracle constitution). This program ships that.

> Product target: on the next shock-day leadership flip, the operator (a) gets a Discord ping when the turn-watch organ fires, (b) opens baskets.html — at 08:30 ET premarket or 30 minutes into RTH — and sees the live tape state of every basket and of the AI-capex complex, an explicit disagreement chip wherever the fresh tape contradicts the (clearly horizon-labeled, as-of-stamped) slow state, and the shock banner with T+1 fade base rates. Never a buy signal. Never a reordered rank.

## §1 Incident forensics of record (2026-07-07 → 2026-07-09)

| Reading | Value | Source |
|---|---|---|
| repricing_coherence 07-07 | **SHOCK 75** (driver_flip + breadth) | market_drivers backscan, policy-shock §9 |
| policy_lever 07-08 | QUIET — SMH/SOXX drawdown -12.77% vs -15% arm threshold; oil lever flatness 0.168 > 0.06 | site/policy_lever.json |
| oracle_state 07-09 | regime=None (no confirmed rotation complex) | site/basketdata/oracle_state.json |
| semicap_equipment 07-08 close | score 51 = trend -0.121, breadth -0.059 (pct50 0.312), impulse **+0.555**, macro absent, mtf +0.4, crowding_pen 0.442; reco=hold; KLAC/LRCX buy_soon but cycle_blocked | site/basket/semicap_equipment.html DETAIL |
| allocation page 07-09 | leader memory_storage, "LEADER FALTERING SHORT-TERM" (client-side JS), data frozen at 07-06; last data/allocation commit 07-07 08:22 | data/allocation/latest_us.json; templates/allocation_scorecard.js:84-89; git log |
| fresh build_allocation on 07-08 closes | as_of 2026-07-08, leader **still** memory_storage (SKIP_D=21) | run of record 2026-07-09, this adjudication |
| AVGO 07-08 | +4.83% (not a semicap_equipment member; ai_semiconductors) | price store |
| AMAT 07-02→07-08 | **-5.40%** (semicap_equipment member) | price store |
| The Relapse (07-08) | "memory-chip strength remains something you sell"; semis bounce = short-covering | site/report_relapse_jul8.html |

Latency decomposition verdict: data 0%, cadence ~10% (premarket/intraday invisibility), construction ~60%, plumbing/wiring ~20%, staleness defect ~10%. The fix portfolio below is weighted accordingly — **wiring and honesty first, new detectors second, cadence third; no changes to calibrated constructions without prereg.**

## §2 Anti-duplication record (what exists and is reused, not rebuilt)

| Capability | Existing home | Disposition |
|---|---|---|
| Intraday compute lane (30-min RTH, site/-only writes) | intraday-fastpath.yml: refresh_regime_if_stale + build_live_overlay (top-150 GTAA/conviction universe → site/live/overlay.json) + build_risk_state | EXTEND with one step + premarket cron window (W2); PS-R7 precedent. NOTE: overlay universe is capped at 150 — NOT the basket-pulse source |
| Full-universe browser quotes | live-quotes.yml (10-min snapshot → live-data branch quotes.json; universe = `scrape_site_symbols` over every `data-sym` the site emits) + site/live.js + worker/quotes.worker.js (deployed-but-unwired, LIVE_QUOTES_URL="") | REUSE as the basket-pulse price source (W2); universe auto-expands when W2 adds member spans. Worker wiring = OPEN-OPERATOR (§7) |
| Live tile recolor | HK heatmap MutationObserver on .nb-chg[data-sym] (#1986) | CLONE to US basket tiles (W3) — NOTE baskets.html currently emits zero data-sym spans; W2 adds them (template wave, plain-copy paired) |
| Shock read + de-escalation | market_drivers.snapshot() (canonical, TI-R1) → repricing_coherence → shock_deescalation (#2006/#2015) + site/live/shock_state.json | WIRE reach to basket/allocation surfaces (W1); no new classifier |
| Chase-vs-fade base rates | flip_confirmation lens (#2008), 26 events, 58% fade at T+1, forward qledger family live | SURFACE beside every fast-turn read, labeled as T+1 statistic (W1/W3) |
| Fast rank deltas | engine/sector_pulse.py heat tiers (1d/5d/20d deltas; phase-0 forward-edge kill → display-only) | WIRE to baskets page rows (W8); no new scorer |
| K-of-N ignition pattern | engine/hk_washout_watch.py (6-organ confluence, ledger, display-only) | PATTERN CLONE for US basket turn-watch (W4), legs re-scaled per review F3/F4 |
| Complex grouping (AI capex) | engine/demand_capex.py:45-52 `AI_CAPEX_THEMES` (6 themes, directness tags) | REUSE for W5 v1 (AI-capex complex only). General complexes = TIL-owned crosswalk extension (OPEN-TIL) |
| Theme vocabulary joins | config/theme_crosswalk.yml (TIL W0 PR-A, **merged**, theme-level only — no complex ids) + TIL W1/W2 | CONSUME ids read-only; FTR adds no vocabulary (FT-R10) |
| Rotation onset/episodes | Oracle (engine/oracle/episodes.py), NW-U6 sovereignty | UNTOUCHED except W7, an Oracle-governed registration-first tier proposal |
| Policy conditions | policy_lever ARMED/QUIET card (#2016), whitehouse sentinel (hourly), theme_thesis_registry (context-only) | WIRE card chip to basket surfaces (W1); thesis stays display/context per R-TIL-5 |
| Alert transport | scripts/notify.py (DISCORD_WEBHOOK_URL/DISCORD_WEBHOOK_WATCHLIST; watchlist sentinel #1810/#1818 ARMED) | REUSE for W10 IGNITION/disagreement pings; no new transport |
| HTF momentum durability (#1984) | engine/htf_durability.py — currently ORPHANED (no caller/DAG/template) | OUT OF SCOPE (owned by its own program); recorded so nobody double-builds |

## §3 Rulings (FT-R1 … FT-R13)

| # | Ruling |
|---|---|
| FT-R1 | **Two-speed law.** The tape layer never mutates, reorders, or re-times any calibrated slow key (neural score, allocation rank, Oracle states, cycle phases). Fast-vs-slow **disagreement is displayed, never auto-resolved**. A tape read may temper (stale-mark, caution-chip) — never direct. |
| FT-R2 | **Tier law.** Every FTR artifact ships display/shadow with authority blocks `may_rank/gate/size/escalate = false`. Consumers may de-escalate only (PS-R3 inherited). Gauntlet = promotion gate, not build gate; forward accrual unrestricted. |
| FT-R3 | **No beneficiary routing.** TI-R5/R-TIL-2 stand in full force. The shock-day relative-bid read prints *observed same-day returns* as a **same-session descriptive readout that does not persist as a rank, registers no NW/brain-consumable escalation field, and carries the T+1 fade base rate inline**. In W4 the shock-day leg is a **binary present/absent confluence flag, never a ranked ordering**. No beneficiary/casualty/shelter/front_run fields anywhere. No runtime shock→"buy this" mapping exists in FTR. |
| FT-R4 | **Conditions-framing only.** PS-R1/PS-R4 inherited: no administration-timing predictor, no intent classifier, no LLM-emitted probabilities. The Second-Act policy-floor thesis reaches surfaces only via the existing policy_lever card and theme_thesis registry (context-only), with the canonical disclaimer string. |
| FT-R5 | **Cadence law.** PS-R7 inherited: intraday and early-close lanes write `site/` only, with the fastpath's explicit data/-discard guard (`git checkout -- .` + narrow staging) replicated verbatim; nightly remains the sole advancer of `data/` ledgers. The W4 turn-watch ledger is a nightly writer. **Amendment 2026-07-14 (coherent-window fix):** "site/ only" is refined to the fastpath's actual ratified idiom — the early-close lane also carries the two *snapshot pointers* `data/regime/latest.json` + `data/market_state/latest.json` (the same two artifacts intraday-fastpath.yml has always committed on its regime self-heal ticks). Without them the provisional EOD state lived only in baked HTML and every subsequent render/engine-render regressed all market-state surfaces to the prior close (2026-07-13 56/MIXED ↔ 81/RISK_ON flip-flop); `engine/run.py` now also refuses a recompute whose price store lags the committed snapshot (no-regress guard, `REGIME_FORCE_RECOMPUTE=1` to override). Forward-grading **ledgers** (regime_history.parquet, `*_log.jsonl` appends, forward_log) remain nightly-only — the ledger law is untouched. |
| FT-R6 | **Oracle sovereignty.** NW-U6: rotation onset/episode/confirmation machinery is Oracle's. FTR builds basket-level *tape description* (no episode/onset/routing/lead-lag state machine fields — TI-R3 shape). Any faster onset tier is W7: a pre-registered Oracle amendment merged before any result is computed, under Oracle constitution rules (no Codex, bound verdict vocabulary), with Oracle-owner sign-off recorded before dispatch. |
| FT-R7 | **No silent recalibration.** Calibrated constructions (theme_scoring weights/legs, SKIP_D, Oracle thresholds, cycle params) are untouched in this program. Adding a macro prior for semicap-class baskets **changes the calibrated score** (the score renormalises over available legs — an added leg is a new input, not a repaired one) and is therefore W8b: a separate pre-registered trial with per-basket before/after score deltas printed. SKIP_D=21 is a *correct* momentum construction at its horizon — the failure was labeling, not the parameter. |
| FT-R8 | **Freshness contract.** Every first-class surface JSON carries `as_of`; every consuming page renders it; the nightly asserts key artifacts advanced to the expected NYSE session (`lib/nyse_calendar`, #1917 pattern) and fails LOUD (or stamps a visible STALE chip) otherwise. Exit-0-always sub-builds must emit a freshness heartbeat the sentinel checks. Early-close output is stamped "provisional close (delayed feed); nightly is canonical." Silent stale display is a defect class (this incident; #2002; #1917 precedents). |
| FT-R9 | **Grading law.** All fast-turn detectors accrue forward ledgers from ship date (no backfilled gradeable claims — PS-A2 precedent; historical backscans are site-artifact descriptive only). Grading unit is the **catalyst-day cohort, not the per-basket event** (co-firing baskets share members and a catalyst; per-event claims inflate N — DT-R14/ticker-cluster-time-confound law). The `basket_turn.v1` family is registered as an **expected-NULL forward meter** — the prior sector-level washout→turn construction printed NULL (Oracle P8 P-W1/S-W3; DO_NOT_REBUILD "Washout × turn") and this ledger tests whether the K-of-N confluence construction differs, at the pre-declared 21d horizon_role, excess-vs-SPY. Time-to-flip metrics use survival estimation with explicit censoring. Promotion question earliest 2027 at n≥8 distinct shock episodes (PS-R8). v1 thresholds frozen descriptively, amendments logged (PS-R9 pattern). **Amendment 2026-08-05 (dark-instrument disclosure, #4576):** the W4 organ shipped 2026-07-09 with the IGNITION state ARITHMETICALLY UNREACHABLE — `engine/basket_turn_watch.py` read every frame from `data/stocks/`, but SPY (a member of no basket) is only collected into `data/yahoo/`, so `spy_ret` was None on every run and the rs_z leg returned False unconditionally; IGNITION is gated on `K≥3 AND rs_z`. The `complex_confirm` and `shock_relative_bid` legs were dark for the same reason, so the organ scored 3 of its 6 legs. **The `basket_turn.v1` accrual window therefore starts at the 2026-08-05 plumbing fix, not at ship.** Zero IGNITION cohorts exist for 2026-07-09→2026-08-05 and that emptiness is an INSTRUMENT ARTIFACT — it is *not* evidence for the registered expected-NULL, and no grading run, promotion argument, or "confirms the prior null" reading may cite it. The pre-registered fire condition and every leg threshold are unchanged; only the benchmark's store path moved. Any W9 grading of this family must state the dark window and count n from the fix date. **Amendment 2026-08-05 #2 (member-coverage dark window, W-B of `PROPHET_US_MISSED_IGNITIONS_MASTERPLAN_BY_FABLE.md`):** the benchmark was not the only dark input. `engine/basket_turn_watch.py` also resolved every MEMBER out of `data/stocks/` alone (~235 names) while the 47 US baskets' active membership is 1,009 slots / ~683 distinct names — a member the store lacked was SILENTLY dropped from `closes_map`, with no error, no log line, and no artifact field. Measured over the same 2026-07-09→2026-08-05 window: **399 of 1,009 member slots read (39.5%); 37 of 47 baskets below 60% coverage; only 2 of 47 fully read.** During the exact week both baskets ignited, `gold_miners` read **1/12** members and `space_economy` **0/15** (its EW 1d return was `null`, not merely wrong). This was a SCORING defect, not only missing telemetry: `_leg_impulse_day` thresholds on the ACTIVE membership (`max(1/3 x N, 2)`) while counting only READABLE members, so an unreadable member raises the bar and lowers the count at once — at 1/12, `impulse_day` was unfireable on any tape. **Every basket's accrual therefore counts n from the member-ladder fix**, per leg family, exactly as the benchmark half does; a WATCH or non-WATCH verdict inside the window was scored on a fraction of its basket and the absence of a state is not evidence the state did not exist. Fix: member ladder `_DEFAULT_STORES = ("stocks", "baskets/ohlcv")` (stocks keeps first refusal; the `audit_universe.MembershipResolver` precedent) → **1,007/1,009 slots, 45/47 baskets fully read, 0 below 60%**; `members_read`/`members_total` now ship on every artifact row with a `::warning` below 60%. Fire condition, leg formulas and thresholds unchanged (2 baskets moved K by one `volume_confirm` leg, 0 changed state, `data_session` identical). Full era stamp: `data/basket_turn/README.md`. |
| FT-R10 | **Taxonomy law.** Complex-level views consume existing groupings (`AI_CAPEX_THEMES`) or a future TIL-ratified `theme_complexes` crosswalk extension. FTR creates no new theme vocabulary and does not edit TIL-owned config. Basket membership changes are out of scope. |
| FT-R11 | **Honest-copy law.** The allocation page headline block gains a horizon label ("strategic, 3–12m, skip-month momentum by construction — see Tape strip for the current session") in both languages; the tape strip carries "15-min delayed" and the T+1 flip fade base rate. The word "validated" is banned from all FTR copy (CI-enforced). |
| FT-R12 | **UI law.** W3 surfaces follow the terminal-ui-quality-bar law: mockups approved before build, browser-verified with prod-shaped data (including a synthetic shock-day fixture AND a stale-quote fixture), EN/ZH strings in data, no translated `title=` attributes, theme.js on any new page. Disagreement chips are gated on fresh quotes (suppress — never invert — when `stale` or outside a live session). |
| FT-R13 | **Notification law.** Alerts (W10) notify already-computed display-tier states verbatim (turn-watch IGNITION, disagreement events, shock banner activation) with de-escalation copy and the T+1 fade base rate inline. No alert may originate a signal, score, or escalation (A7 ORIGINATE ban); no alert copy may carry direction words beyond the state name. |

## §4 Architecture

```
                       EXISTING (unchanged)                          NEW (FTR)
 nightly 02:00 UTC ──► engine.run → theme_scoring → baskets ──► [W8: anatomy panel, sector_pulse chips]
                       narrative_rotation → allocation      ──► [W0: freshness sentinel + horizon labels]
                       oracle_offrender → episodes/panels   ──► [W7: registration-first tape-onset tier]
                       market_drivers → repricing_coherence ──► [W1: shock banner reach → baskets/allocation]
                       flip_confirmation (58% fade T+1)     ──► [W1/W3: base-rate chip beside every tape read]
 10-min all-day   ──►  live-quotes → quotes.json (universe =
                       every data-sym the site emits)       ──► [W2a: basket member spans ⇒ universe grows to ~700]
 30-min 07:00-16:30 ET  fastpath (cron extended premarket)  ──► [W2b: build_basket_pulse ← quotes.json → site/live/basket_pulse.json]
 browser          ──►  live.js                              ──► [W3: tile recolor + LIVE strip + disagreement chips (stale-gated)]
 ~17:20 ET (new)  ──►  [W6: early-close engine-render → provisional EOD state ~5h earlier, site/ only]
 nightly (data/)  ──►  [W4: basket_turn_watch K-of-N organ → data/basket_turn/ledger.jsonl + site artifact]
                       [W5: AI-capex complex rollup (AI_CAPEX_THEMES); general complexes OPEN-TIL]
                       [W9: catalyst-cohort qledger families + disagreement survival ledger]
                       [W10: Discord ping on IGNITION / disagreement / shock activation (notify.py reuse)]
```

## §5 Waves (dispatch order; Sonnet builds, Opus reviews, per house routing)

| Wave | PR | Contents | Deps |
|---|---|---|---|
| **W0 freshness + staleness fix** | PR-1 | Root-cause the `data/allocation/` **content** stall: instrument the exit-0 wrapper at `build_baskets.py:229`, pull the actual exception from the 07-08/07-09 nightly logs, fix. (The #1963 git-add theory is rejected — `git add data/` is broad and the file committed on 07-07.) Add **surface-freshness sentinel**: nightly step asserting `as_of == expected NYSE session` (`lib/nyse_calendar`, #1917 pattern) for allocation/baskets/oracle_state/sector_pulse artifacts immediately after each sub-build; loud WARN + visible STALE chip on miss. Fix `allocation_scorecard.js` `window.THEME` undefined-on-allocation.html bug. FT-R11 horizon labels. Plain-copy pairs synced (`check_template_site_sync --fix`). | none |
| **W1 shock-wiring reach** | PR-2 | Wire existing `site/live/shock_state.json` banner + policy_lever chip + flip-confirmation base-rate line onto **baskets.html, allocation.html, basket detail pages** (US first, then CN/HK/CA parity). Strictly de-escalation copy (PS-R3): "SHOCK (repricing coherence ≥70): scores & narrative computed on pre-shock data — discount until {expires}. Violent flips fade 58% at T+1 (n=26)." No reordering, no removal, no sizing. | none |
| **W2 basket tape layer** | PR-3 | **(a) Member spans (template wave):** add `.nb-px/.nb-chg[data-sym]` spans for every basket member to baskets.html.j2 + basket detail templates — this auto-expands the live-quotes scrape universe (`scrape_site_symbols`) to all ~683 members; verify live-quotes.yml runtime at ~700 symbols (≈35 Yahoo batches) stays inside its 10-min slot; plain-copy sync. **(b) `scripts/build_basket_pulse.py`:** consumes the already-fetched live-quotes `quotes.json` snapshot (NO new network) + membership.json → per-basket live EW %chg, session flag (pre/RTH/post), 1d/2d cumulative move, cross-sectional tape rank, offense-vs-defense live spread (flip_confirmation composite definitions, print-only intraday, no z claims), shock-day relative-bid readout (only when market_drivers oil_shock/geopolitical family active AND index down; same-session descriptive per FT-R3). → `site/live/basket_pulse.json`, site-only (FT-R5). **(c) Premarket coverage:** extend fastpath cron `*/30 13-20` → `*/30 11-20 * * 1-5` (07:00–16:30 ET); steps 1–3 stay RTH-gated as today; the pulse step runs on all ticks with the session flag. Record p95 runtime before asserting the <60s budget. | none |
| **W3 tape surfaces** | PR-4 | Mockups → operator approval → build (FT-R12). baskets.html: LIVE strip + per-row live chg + **disagreement chip** (`tape rank ↑N / slow state HOLD as-of {date}`), gated on fresh quotes (suppress on stale/holiday/weekend, LIVE_STALE_MIN=20); tile recolor cloning HK MutationObserver over the W2a spans; allocation.html: Tape panel beside Prevailing narrative; basket detail header live strip; premarket mode (before 09:30 ET, shows premarket EW move + "premarket, thin tape" label). Browser-verified incl. synthetic shock-day + stale-quote fixtures. | PR-3 |
| **W4 turn-watch organ** | PR-5 | `engine/basket_turn_watch.py` — EOD K-of-N confluence per basket (washout-watch pattern), legs v1 **size-normalized** (review F3/F4): impulse day (≥⅓ of active members +3%, floor 2), 1d cross-sectional rs z ≥ +2, breadth surge (z of 1-day Δpct50 vs the basket's own trailing 60d Δpct50 distribution ≥ 2, floor 2 crossings), volume confirm (EW dollar-vol ≥ 1.5× 20d median), complex confirm (≥2 sibling AI_CAPEX_THEMES/crosswalk-theme baskets with positive 1d rs z), shock-day relative-bid **binary flag** (FT-R3). States WATCH (K≥2) / IGNITION (K≥3 incl. rs-z leg), 2-day hysteresis (TI-R3 shape), descriptive names only. Nightly writer → `data/basket_turn/ledger.jsonl` + site artifact + synapse registration (tier display, horizon_role context). **Disclosure of record:** sector-level standalone washout→turn printed NULL (Oracle P8 P-W1/S-W3; DO_NOT_REBUILD §2 "Washout × turn") — this organ is a *different construction* (multi-leg confluence, basket granularity) shipped display-tier as an expected-NULL forward meter (FT-R9); it is not a revival claim. Backscan = site-artifact descriptive; forward ledger starts at ship date. Thresholds v1 frozen, amendment-logged; rs-z scaling re-derived at the 2026-10-09 clock once ≥30 clean sessions of frozen basket levels exist (current `data/basket_levels/us.parquet` has 6 rows with holiday artifacts — insufficient for calibration). **DARK 2026-07-09→2026-08-05 (#4576):** the SPY benchmark was looked up under `data/stocks/` where it never lands, so the rs_z, complex_confirm and shock_relative_bid legs could not fire and IGNITION was unreachable — see the FT-R9 amendment for what that voids. | PR-3 helpful, not required |
| **W5 complex tape rollup** | PR-6 | v1: aggregate basket_pulse + turn_watch to the **AI-capex complex only**, membership = existing `engine/demand_capex.AI_CAPEX_THEMES` (reuse, FT-R10). Complex strip atop baskets.html; "only green complex on a red day" chip (observed, same-session, descriptive). **General complex layer = OPEN-TIL:** a `theme_complexes` grouping is TIL-owned taxonomy (R-TIL-1/R-TIL-2); FTR files the request with the TIL program and ships the consumer only when TIL ratifies. No edits to config/theme_crosswalk.yml from FTR lanes. | PR-3 |
| **W6 early-close lane** | PR-7 | New workflow `earlyclose.yml` (own file — avoids #1891/#1840 daily.yml collisions): **17:20 ET** weekdays (clears the 16:45 ET optionshub launchd; add a skip-if-optionshub-running guard — RAM contention law, 192GB box), self-hosted; price-store freshness gate `--heal` (keyless, ~16s) → skip-if-not-advanced guard → engine.run + baskets/allocation/sector/oracle-panel re-render using a **new engine-render `scope=fast`** (to be added: baskets+macro clusters, no collectors/LLM/gex/EDGAR — the current scope enum has no `fast`) → **fastpath-style data/ discard guard (`git checkout -- .`, stage `site/` only) + `--autostash` rebase-push** (render-resurrection + ledger law, FT-R5/FT-R8). Output stamped "provisional close (delayed feed); nightly is canonical." Runtime measured on first dispatch before the schedule is committed (budget hypothesis 30–45 min; engine-render timeout 180 min is the ceiling). | none |
| **W7 Oracle tape-onset tier** | PR-8 | Registration-first (Oracle constitution): pre-register an UNCONFIRMED "tape-onset" display tier — raw accel_z_1d/2d crossing (no 5-day smoothing) with the historical false-positive rate printed on the panel, forward-graded, display-only column on Turn Desk/oracle panels. Registration merges before any result is computed. Oracle governance owns kill/keep; Oracle-owner sign-off recorded before dispatch. FTR W2–W5 do not depend on it. | Oracle owner sign-off |
| **W8 anatomy + pulse wiring** | PR-9 | Basket detail: component-anatomy panel (7 legs with values — why a 51 is a 51). baskets.html rows: sector_pulse heat chip + rank_delta_5d (existing display product, new consumer). **W8b (separate, prereg):** `_MACRO_PRIOR`/`_SECTOR_PROXY` entries for semicap_equipment + missing AI-complex baskets — this is a scored-construction change (FT-R7): pre-registered, per-basket before/after score deltas printed, operator-ratified before merge. | none |
| **W9 grading pack** | PR-10 | **Accrual starts 2026-08-05, not ship — the IGNITION cohort family has structurally zero rows before that date (dark organ, FT-R9 amendment #4576); grading runs must print the dark window and count n from the fix.** qledger families: `basket_turn.v1` — **one 21d claim per IGNITION-day cohort** (co-firing baskets grouped; excess-vs-SPY on the cohort EW; pre-declared 21d horizon_role; expected-NULL registration per FT-R9; cluster caveats printed). `tape_disagreement.v1` — disagreement day → outcome ∈ {slow-state-flipped, tape-faded, unresolved-censored} via survival estimation (Kaplan-Meier, explicit censoring; SKIP_D=21 makes allocation-flip-within-10d structurally ~0 — reported as a latency meter, not a tape-accuracy claim). Shock-day relative-bid follow-through descriptive stats. Placebo: random-basket + time-shifted sampler reuse (TIL W6 pattern). Clocks in §6. | PR-5 |
| **W10 alert wiring** | PR-11 | `basket_turn_watch` IGNITION events + tape_disagreement events + shock_state activation → existing `scripts/notify.py` Discord transport (FT-R13 copy contract: state name, evidence legs, as-of, T+1 fade base rate; no direction words). Nightly + fastpath-tick evaluation, dedup per state-day. | PR-5 (IGNITION), PR-3 (disagreement) |

## §6 Grading + clocks

- **2026-08-15** — W0 sentinel + W2 pulse sanity: zero silent-stale days since ship; fastpath pulse step p95 recorded and <60s; premarket ticks confirmed live (07:00–09:30 ET window populated).
- **2026-10-09** — joint read with policy-shock come-back: turn-watch firings ledger n-count; **rs-z and breadth-z scaling re-derivation** on ≥30 clean frozen-level sessions; threshold amendment window (log per FT-R9).
- **2026-10-15** — joint with TIL W6 first grading read: disagreement-ledger first honest print (survival curves per surface; allocation reported as latency meter).
- **2027 (earliest, n≥8 shock episodes)** — promotion question for any FTR key (PS-R8): pre-registered, placebo-controlled, alpha-over-factor-panel ruler, DT-R14/DT-R16 controls, catalyst-cohort N.
- Standing: every clock lands in the admin experiments registry (#1902 pattern).

## §7 Ops ledger

- **Render budget:** fastpath +1 step (<60s hypothesis, p95 measured before claimed; 15-min timeout headroom); premarket cron extension adds 4 ticks/day of the light path; earlyclose lane 30–45 min hypothesis at 17:20 ET, measured on first dispatch; W4 organ is adapter-cheap nightly. live-quotes at ~700 symbols ≈ 35 Yahoo batches — verify inside the 10-min slot on ubuntu-latest, else split alternate ticks.
- **Box contention:** earlyclose (17:20 ET) vs optionshub launchd (16:45 ET) — skip-if-running mutex mandatory (mac-studio RAM-freeze law).
- **Collision watch:** #1891/#1840 hold daily.yml + synapse.yml → W6 ships its own workflow file; W4's synapse registration sequenced after those merge or rebased-over (regen recipe). TIL lanes own config/theme_crosswalk.yml — FTR never edits it (FT-R10). Check ACTIVE_BUILD_MAP before each dispatch.
- **Plain-copy pairs:** allocation_scorecard.js / heatmap.js / live.js are templates↔site paired assets — every wave touching them runs `python -m scripts.check_template_site_sync --fix` (CI-guarded).
- **CI landmines honored:** inline-JS check gap (browser-verify rendered pages); "validated" word ban; EN/ZH + no translated `title=`; SIGNAL_BUS/dag regen if synapse touched.
- **OPEN-OPERATOR (no build until ruled):** (1) wire `LIVE_QUOTES_WORKER_URL` repo variable (60s edge-cache Worker vs current ~10–15 min effective browser latency) — requires confirming the Worker is deployed on the Cloudflare account; (2) tighten live-quotes.yml cron */10 → */5 (ubuntu minutes cost, trivial); (3) whether the premarket tape strip also surfaces on the public landing hub or stays on baskets/allocation; (4) Discord webhook target for W10 (reuse watchlist channel or dedicate a turns channel).
- **OPEN-TIL:** `theme_complexes` grouping request (W5 general case) filed with the TIL program owner.
- **BLOCKED-EXTERNAL:** none — all W0–W10 data sources are already in-house.

## §8 Rejected shapes (recorded; none rise to DO_NOT_REBUILD rows)

Auto rank-flip or score boost on shock days (laundered escalation, FT-R1/TI-R5); shock→beneficiary/shelter routing in any runtime path (TI-R5 stands; the relative-bid readout is fenced per FT-R3 and the W4 leg is binary); administration-timing predictor / intent classifier (PS-R1); LLM-originated tape states or escalations (A7 ORIGINATE ban); intraday `data/` ledger advancement (PS-R7/NW-U3); fused single-number "tape score" ranking baskets (WA-R1/R-TIL-3 shape — the pulse is a per-leg panel); changing SKIP_D/theme_scoring weights/Oracle thresholds inside this program (FT-R7 — W8b macro-prior is the disclosed prereg exception); rebuilding rotation onset outside Oracle (NW-U6); a fourth theme vocabulary or FTR edits to TIL-owned crosswalk config (FT-R10); backfilled gradeable claims (PS-A2 precedent); per-event grading of co-firing baskets (ticker-cluster time-confound law — catalyst cohorts only); un-disclosed revival of the killed sector-level washout→turn trigger (disclosed and re-framed expected-NULL in W4/W9 instead).
