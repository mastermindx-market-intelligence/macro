# Advanced Quant Methods — intake adjudication (by Fable)

**Status:** adjudicated 2026-08-05 · wave-1 build commissioned in this PR · wave-2 charters await operator ratification (driver's-seat packet)
**Intake:** operator-supplied external proposal "Advanced Quantitative Methods for Mastermind / Prophet" (12 method families: world model, dynamic graph transformer, joint distribution forecasting, online mixture-of-experts, counterfactual event engine, decision-focused training, invariant causal filtering, adversarial SDF, Hawkes/path-signature microstructure, transfer learning, grammar alpha lab, synthetic-market simulator).
**Method:** four repo census lanes + two literature-verification lanes (all citations browsed, not recalled), adjudicated against standing law. Receipts inline.

---

## §0 Acceptance gates for wave-1 (this PR)

Wave-1 is **diagnostic tier**: engine module + frozen-prereg Phase-0 + tests + this adjudication. Not done unless:

- G0.1 `engine/pick_forward_dist.py` is pure (no I/O), PIT-pinned by a truncate-and-recompute test, and reuses `engine/forward_dist.py` / `engine/validation.py` rather than re-implementing them.
- G0.2 `scripts/pick_forward_dist_phase0.py` carries a frozen pre-registration header written before results; gates G1–G4 print PASS/FAIL per scheme; results JSON + writeup committed. A failed gate is a result, not a defect.
- G0.3 Zero authority surface: no site/templates/dag/board changes, no fused numeric composite, no "validated" copy, trial-ledger entries appended.
- G0.4 No DO_NOT_REBUILD edits (four open lanes collide on the registry today: #4625/#4621/#4617/#4562; nothing here is a new kill — every negative verdict below traces to an existing ruling or an existing measured blocker).

---

## §1 What the intake is, and whether its evidence is real

The proposal is an honest, competent research synthesis — **not** a fabricated-citation document. Verification (all 20 citations fetched):

- **All arXiv/NBER IDs exist and are fairly represented**, including the 2026-dated ones (AlphaCFG 2601.22119; decision-focused-inflation 2605.01176; NBER w34861 "Machine Learning Meets Markowitz", Harvey et al., Feb 2026).
- **One overstatement found:** the path-signatures primer (1603.03788) contains **no finance examples** — its one worked example is digit classification. Cite it for signature theory only.
- **Recency risk is concentrated exactly where the proposal is most ambitious:** NewsNet-SDF's Sharpe 2.80 / "471% over CAPM" (2505.06864) is a ~3-month-old unreplicated preprint; AlphaCFG is ~6 months old, zero independent citations; the two decision-focused portfolio papers are 3–10 months old.
- **The deep-SDF headline deflates on inspection:** Chen-Pelger-Zhu (1904.00745, Management Science 2024) reports monthly Sharpe 2.68 train → 1.43 valid → **0.75 test**; no independent post-2020 re-run exists anywhere (open question, not a confirmed pass).
- **The strongest independently-evidenced family is distributional forecasting:** Baruník-Hronec-Tobek (2408.07497) is conditionally accepted at the *International Journal of Forecasting* with OOS portfolio value across global markets — genuinely independent of every other cited group.
- **Adversarial sweeps:** formulaic alpha mining decays/overfits by default (the field's own toolmakers concede it; no named system has survived an independent cost-realistic re-test). Stock-GNN gains have no cost-realistic independent replication; benchmark studies (LOBCAST 2024) show large performance drops on new data. Online expert aggregation verifiably beats the *best single expert* (Remlinger et al., journal-published) but "beats equal-weight" is **not** independently confirmed. Synthetic-data consensus: stress-testing yes, validation never. The foundation-model claim traces to a real paper (2511.18578) fairly paraphrased, in a still-settling literature.

Verdict on the document itself: use it as a map of the search space. Its own "what I would not prioritize" list (no off-the-shelf foundation models, no direct LLM predictions, no unconstrained RL, quantile-before-diffusion, no autonomous factor deployment) **already agrees with standing house law** — the disagreements are narrower than they look and are itemized below.

## §2 The seven walls every method was tested against

1. **Data entitlement.** `massive_stock_day` is the only wide US panel: 20,476 parquet files / 19,133 tickers per the store's own manifest (the committed worktree manifest's 20,677 is stale), 2021-07→2026-07, daily OHLCV+`transactions`, **unadjusted**; per-trade tape and NBBO both 403 (`collectors/massive_flatfiles.py`). FINRA ATS has a ~2.4y local hole; deep short-interest history is in-flight (#4627), today 2 weeks deep. EDGAR fundamentals cover ~1,550 of ~19,133 names (~8%) — the measured Quant Lab blocker.
2. **Compute.** Nightly render budget ~67 min, 4-core (HOUSE-U6: heavy compute off the render path, artifacts to R2). ML precedent is sklearn-only, ops-lane-only (`scripts/ops_train_flow_score.py` — "NEVER wired into daily.yml"). No torch/lightgbm anywhere, by design.
3. **Fusion law.** Fused composite scores in any scored path/ranking/sizing/escalation are FORBIDDEN (DNR row 45, MSP-R2, Signal-Commons positioning-fusion ILLEGAL; sole carve-out = PSI §3.1.2 display-tier equal-weight composite, operator-overridden 2026-08-03). The proposal's "final decision score" (R_online × S_causal × (E[r]−cost) − λ…) is exactly this forbidden shape.
4. **Origination law.** A7: LLMs never originate signals/scores/escalations (`constitution.py` refuses A7 unconditionally; `signal_foundry_blocklist.yml` BL-001 regex-enforces it at intake).
5. **Sizing law.** NWP-U18: no portfolio construction/sizing in this repo — no optimizer, no target weights, no hedge suggestions. Sizing belongs to the operator desk in the Mastermind repo (two-organisms law, NWC-U4).
6. **Adaptation law.** Online weight mutation is rejected doctrine: "at n = hundreds of episodes and dozens of signals, continuous weight updates are overfitting with a heartbeat" (`research/CENTRAL_BRAIN_BRAINSTORM_BY_FABLE.md`); PSI §11.4 "thresholds/weights NEVER mutate online." Lawful adaptation = quarterly, versioned, pre-registered reweighting + per-leg GO/NEUTRAL validity gates (`data/regime/anticipation_gate.json` pattern).
7. **Environment estimability.** The open lane #4621 measured that 5 of 6 regime axes cannot support environment-conditioned claims today (single-state or one-month coverage); the one estimable axis (`regime_at_entry`, 3-state, 64y) shows a **null** regime×family interaction (main effect 3.8× larger). Its frozen entry gates (MIN_COVERAGE 0.20, MIN_STATES 2, MIN_MONTHS_STATE 12) are the standing test any environment-partitioned method must pass first. (#4621 is unmerged — cited as an in-flight measurement, not re-litigated here.)

## §3 Method-by-method verdicts

| # | Method | Verdict | One-line reason |
|---|--------|---------|-----------------|
| 1 | Multimodal world model | **REJECT deep form · salvage exists** | N-insufficiency + fundamentals coverage 7.5% + fusion law; the "foundation" it demands (PIT asset-state store) already exists as US Context Vector (150 cols, #4540) / CN 88-col |
| 2 | Dynamic graph transformer | **ALREADY DEFERRED (CHF-R14) · lawful precursor chartered** | Learned graph models sit behind the 2027-01-15 CHF clock with unmet preconditions; intake auto-denies (`_CHF_DENY_TOKENS`); the missing training substrate (per-edge outcome ledger) is buildable display-tier now |
| 3 | Joint return distributions | **BUILD NOW (cross-sectional, empirical) · asset-level already specced · diffusion rejected-for-now** | Strongest independent literature + strongest house prior (#4626: risk levers pay, ranking levers don't); wave-1 build in this PR |
| 4 | Online regret-minimizing MoE | **REJECT online form · lawful form exists** | Adaptation law (wall 6); equal-weight null unproven in the literature; Calibration Hub + gates + quarterly prereg reweighting already implement the lawful version |
| 5 | Counterfactual event engine | **CHARTER (wave-2a) — genuine gap** | Event-study harness + PIT event stores exist; donor-pool synthetic control exists nowhere; Goldsmith-Pinkham & Lyu (2511.15123) is directly on point |
| 6 | Decision-focused training | **FORBIDDEN here (NWP-U18) · one lawful sliver chartered** | No optimizer/sizing in this repo; the DFL-inflation critique (2605.01176) independently supports the ban; board-level shared-driver overlap *disclosure* is the lawful sliver |
| 7 | Invariant causal filtering | **DEFERRED at the estimability wall** | Wall 7; era-split sign-stability is already a gauntlet gate (the time-axis version); regime-axis version waits for stamp accrual (quad floor ~mid-2027 earliest) |
| 8 | Adversarial SDF | **DEFER (research-tier)** | Test-period Sharpe 0.75/mo, no post-2020 replication; needs the fundamentals coverage we measurably lack; the practical question it answers ("alpha or risk-compensation?") is partially served by incremental-IC-vs-style-factors (MEASUREMENT_FLOOR) |
| 9 | Hawkes + path signatures | **BLOCKED at intended fidelity (entitlement)** | No tape/NBBO (403), no order book; OHLCV-grade slivers already exploited (`volume/transactions` studies, CN limit tape 2011→); LSR-P0 scoping law: an OHLCV-proxy null must never read as a kill of the tape-grade idea |
| 10 | Transfer/meta-learning | **SETTLED doctrine — nothing to build** | "Transfer the method, never the fitted answer" (CN→US handoff, PR #4544; 5-for-5 naive-transport failures); the proposal's transfer-risk gate is our direction fence, formalized |
| 11 | Grammar alpha lab | **EXISTS (Signal Foundry) — REJECT-REDUNDANT** | Grammar-constrained specs, 7-gate screen, frozen preregs, trial-ledger DSR, compiled kill blocklist all live; mining-volume upgrades are counter-indicated by the decay literature |
| 12 | Synthetic-market simulator | **REJECT as validator · stress-replay exists** | Literature consensus (stress-testing only, never validation); crisis-stress replay + block-bootstrap/permutation nulls already cover the lawful uses |

### Detail where the verdict has teeth

**#1 world model.** What the proposal actually requires first — "immutable feature snapshots, event timestamps, universe membership, model lineage" — is the part we already built: `data/us_prophet_rank/candidates/` (150 cols × full universe, monthly parts, stamp_date family key) and the CN 88-col sibling; S&P 1500 PIT membership to 2012; insider PIT panel 2006→. What it requires second (a learned multimodal encoder) fails three walls at once: sklearn-only/no-GPU compute posture, 7.5% fundamentals coverage on the wide panel, and Oracle Tier-C doctrine ("with n in the hundreds, a deep net is overfitting bait"). The buildable sliver is **historical-analog similarity on the transparent context vectors** (nearest-neighbor in named-feature space, display-tier, "resembles 27 prior episodes" with the episodes listed) — chartered wave-2c below. No embeddings required; embeddings are re-openable only with a concrete N argument per the meta-labeling precedent (`engine/meta_label.py` — the house template for admitting any ML technique: N-sufficiency, purged CV, default-off).

**#2 graph transformer.** The census found the honest gap: today's confluence graph is 98.6% static `feeds` edges; per-edge weights/timestamps/confidence don't exist; and **no ledger anywhere grades "edge fired → target moved X with lag L"** — which is precisely the training signal any learned propagation model would need, and also precisely the evidence CHF-R15 demands before the 2027 reconsideration. Wave-2b charters that ledger (deterministic, display-tier, single-writer JSONL folded nightly, AUTHORITY_BLOCK stamped). Event-conditioned *rule* activation extensions route through the existing `mechanism_pathways.py` idiom. Anything matching `_CHF_DENY_TOKENS` stays dead until the clock and preconditions say otherwise.

**#3 distributions.** Three sub-verdicts. (a) **Asset-level cone upgrade** (GARCH-skewed-t, Kupiec/Christoffersen coverage gates): already specced as `research/HEDGEYE_UPGRADE_MASTER_PLAN.md` §C.4 (P1, unbuilt) — that plan keeps ownership; do not re-charter. (b) **Cross-sectional pick-conditional cones: wave-1, built in this PR** — empirical analog-cohort quantiles (zero fitted weights, `forward_dist` kernel), five pre-registered conditioning schemes (trend / extension / vol / coil / trend×vol), H=21 primary verdict horizon, gates = calibration ∈ [0.72, 0.88] on the 80% band + pinball skill vs pooled-unconditional AND vs own-name-marginal baselines + cell-floor honesty. The Calibration Lab already grades cone coverage for cycles (0.80 nominal + recalibration multiplier) — per-pick cones join a measurement idiom that exists, and the surface language is already law ("windows, not certainties"). (c) **Joint conditional diffusion: rejected for now** — the proposal itself says quantile-before-diffusion; the single supporting paper is one-market/unreplicated; scenario generation has no lawful consumer here (walls 5, 6) and the validation use is barred by the synthetic-data consensus.

**#5 counterfactual events (wave-2a charter).** Build `engine/synthetic_control.py` as a *comparison-estimator* upgrade to the existing event-study harness (`scripts/backtest_event_priors.py`), not a new program: donor-pool weighted counterfactual per treated name (match on pre-event path/vol/beta/sector/size/liquidity), against the incumbent benchmark-adjusted CAR. **Positive control is pre-registered:** the S&P pure-add announce effect (+1.6% gross, t=4.6, already graded in `scripts/validate_index_reconstitution.py`) must remain visible under SC; **falsifier:** on the ClinicalTrials Phase-3 null family, SC must not manufacture an effect the random-date placebo kills. Event families with honest PIT stamps only (index adds; insider clusters 2006→; dilution filing-dates; earnings once #4624's Item-2.02 acceptance timestamps land). Heterogeneous-effects (causal forests / DML) are a later rung and route through CHF's instrument discipline; DML's foundations are the one part of this intake with Nobel-grade pedigree (w23564), but moderator-coverage is the binding constraint, not method quality.

**#6 decision layer.** NWP-U18 is dispositive for optimizers/weights/sizing. What survives: a **display-tier categorical overlap read on board picks** — "these 3 live picks share the same basket/factor cluster driver" — computed from the existing 9-factor exposure store (`engine/factor_exposure.py`, ~1,529 names) + basket taxonomy, folded by agreement (never a weighted sum), caution/de-escalation semantics only (RO-3). That is the lawful core of the proposal's "portfolio intelligence" scene, minus the sizing. Chartered wave-2d; PSI keeps ownership of everything touching the user's own book.

**#8 SDF.** If/when wanted, it enters as a **Quant Lab recreation** (the #4619 lab is the designated venue for external-model recreations with comparable rank-IC/HAC/FDR grading) — after fundamentals W2-A widens coverage, and with the in-sample-vs-test Sharpe distinction quoted honestly. Until then, the "is this alpha or risk-compensation" decomposition continues via incremental IC neutralized against owned style factors.

## §4 Wave plan

**Wave 1 (this PR, diagnostic tier):** this adjudication + pick-conditional forward-distribution Phase-0 (module, study on the real panel, tests, committed results). Ship gate: §0.

*Wave-1 result (2026-08-06, `research/PICK_FORWARD_DIST_PHASE0.md`; adversarially reviewed same day, review addenda §3b of that doc — the reviewer independently rebuilt the panel and reproduced every number exactly):* on 6,617 names / 3.31M name-days / 856 OOS dates (2023-01→2026-06, quarterly refits), **S3 vol-state is the finding**: pinball skill vs both baselines at H=21 (+1.09% vs pooled, CI [+0.5%, +1.7%] of baseline units, 73% of dates positive, p<0.0002 both baselines, survives a 10-test Bonferroni floor and refit-level clustering t=3.23) with 80%-band coverage 0.79. **S5 trend×vol also passed the pre-registered gates but is fragile** under review-grade stress (one-sided p 0.0074 vs the 0.005 Bonferroni floor; refit-cluster t=2.04) — read it as suggestive. S1 trend / S2 extension / S4 coil are CALIBRATED_ONLY. Gate honesty (review): G1's calibration band is non-discriminating at this design — the unconditional null itself covers ~0.79 — and G4 never binds at this cross-section width, so the verdicts rest on G2/G3, the two gates capable of failing; and B1's own-name floor is unreachable early in the store, so ~40% of "own-name" comparisons scored against the pooled baseline (G3 is partly G2). A committed negative control (`test_noise_partition_manufactures_no_edge`) shows a state-independent noise partition prices slightly *worse* than the marginal (review's 40-seed run: mean −0.0005, never exactly zero) — the harness manufactures no edge, and S1/S2 sit at noise-partition level, sharpening the S3 contrast. **Open question that gates the next rung:** outcomes are standardized by trailing 20d vol — a biased forward-vol forecast — so S3's gain may be correction of known vol mean-reversion rather than state information; the review notes the per-scheme delta ordering matches that mechanism's prediction exactly (every positive scheme is a trailing-vol transform). The discriminating experiment is a re-run with the shipped HAR forecaster (`engine/vol_forecast.py`) as the standardizer: edge survives → conditioning carries information; edge collapses → the finding is "use a better vol forecaster," which is itself shippable width intelligence. Effect sizes are statistically clean but economically small (~1% of baseline pinball) — both readings ship honest width. Next rung (separate PR, operator ratification): HAR-standardizer discriminator + a non-overlapping-origin coverage test, then a per-pick cone emitter, Calibration-Lab coverage grading, "windows, not certainties" surface copy.

*Wave-1.5 discriminator result (2026-08-06, `research/PICK_FORWARD_DIST_PHASE1_HAR.md`, PR #4674):* **VOL_FORECAST_CORRECTION** at the pre-registered rule. Under a state-flatter multi-scale standardizer, S3-vs-B0 fails D-1 (+0.00125 [−0.00024, +0.00288], one-sided p 0.054 — a marginal non-rejection) and retains R_rel = 0.385 of its edge against the frozen 0.5 band (raw-unit secondary 0.447; descriptive paired-bootstrap CI [−0.16, +0.57] straddles the band); S5 fails too; the loss concentrates in the vol-linked schemes (62–76%) while the non-vol schemes barely move. The mechanism is confirmed directionally, not one-for-one: removing 38.1% of the state-correlated scale bias removed 61.5% of the edge — ~24 points of the collapse are not accounted for by the bias-spread reduction alone (X-5; its bands are full-span hindsight cuts, disclosed). Two facts keep this a re-read rather than a proven negative: against the other pre-registered null (own-name B1), S3-under-HAR still excludes zero (+0.00248 [+0.00074, +0.00439], ratio 0.519 — above the band); and D-1's rejection margin is thin. Honest verdict: the B0-referenced vol-state edge does not survive a state-flat scale at the pre-registered bar; what remains is a residual own-name-referenced read and an unexplained remainder — neither promotable, both accruing questions for the fitted-forecaster rung. The X-4 null stands: the equal-weight blend is NOT more accurate (loses on correlation and dispersion); it discriminates by flatter cross-state bias. Emitter premise: **marginal cones on a width scale whose bias is state-uncorrelated**; the untested rung is a *fitted* forward-vol forecaster (HEDGEYE §C.2 owns it — X-4 is that plan's first measured motivation, and §C.2's "harness output" was previously aspirational). Calibration held (~0.79) under both scales — honest width ships regardless. Wave-1 reproduction: exact (bit-identical to the phase-0 artifact). Wave-1's test suite was dark in CI (no workflow ran it) — wired by #4674.

**Wave 2 charters (operator ratification requested — driver's-seat packet; each enters as its own PR with §0 gates):**
- **2a Synthetic-control event counterfactuals** — spec in §3#5. Entry gate: positive + negative controls pre-registered before the first real run. Effort M.
- **2b Neural Web edge-outcome ledger** — grade `confirms`/`leads`/mechanism-card edges prospectively (edge fired → target forward residual move, realized lag); single-writer JSONL, nightly fold, AUTHORITY_BLOCK. This is both a product read (which claimed linkages actually propagate) and the CHF-R15 evidence base. Effort M.
- **2c Analog-similarity read on context vectors** — k-NN in named-feature space over `us_prophet_rank`/`china_prophet_rank` stores; display-tier "historical analogues" with the analog list printed. Entry gate: leave-one-era-out sanity (analogs must not be dominated by same-month neighbors). Effort S-M.
- **2d Board shared-driver overlap disclosure** — categorical, factor/basket-cluster based, de-escalation only. Effort S.
- Sequencing note: 2a/2b are independent; 2c/2d touch board surfaces and should trail the current Prophet W7/scan-tier lanes to avoid collisions.

**Deferred with named re-open conditions:** #7 (per #4621's estimability gates — the axis to condition on must return `estimable`; quad floor ~mid-2027), learned graph models (#2 — the CHF-R15 Phase-3 clock 2027-01-15 requires a quarter of scout + null-library operation; the separate Phase-2 machine-registration clock 2026-10-15 requires ≥8 matured exit-(a)/(b) candidates), #8 (fundamentals W2-A + an honest external replication), #9 tape-grade microstructure (entitlement change), fitted quantile regression as cone upgrade (only if wave-1's empirical cones pass gates and a later prereg shows fitted widths beat empirical widths OOS — the sklearn `QuantileRegressor` path sketched in HEDGEYE §C.4 Track B).

**Rejected (no clock):** online expert-weight mutation; differentiable portfolio optimization in this repo; joint-diffusion scenario generation as validation; grammar-MCTS mining volume outside the Signal Foundry; direct LLM market predictions (A7 — the intake itself concurs); fused final decision score (§5).

## §5 The SignalPacket and the "final decision score"

The proposal's unified `SignalPacket` is adoptable as a **documentation target** — most fields map to artifacts that exist or are chartered above (distribution block → wave-1 cones; graph fields → 2b; counterfactual gap → 2a; analog similarity → 2c; portfolio impact → 2d categorical + PSI user-book surfaces; model reliability → Calibration Hub + gate files). Two fields are refused as specified: `recommended_size` (wall 5) and the multiplicative `Score_{i,t}` fusion (wall 3) — the latter ships only as separately-visible named primitives with categorical folds, per RO-2's parts-dict form. The intake's *intent* (richer, more honest per-pick output) survives intact; only the liftable-number packaging is refused.

## §6 Registry actions

None. No new kills are issued: every negative verdict above cites an existing ruling (CHF-R14/R15, NWP-U18, DNR row 45, A7/BL-001, Signal-Commons fusion rulings), an existing measured blocker (#4621 estimability, fundamentals coverage, tape entitlement), or a deferral with named re-open conditions — deferrals do not enter the registry. The DO_NOT_REBUILD file is deliberately untouched in this PR (four open lanes already collide on it).

---
*Census receipts: signal-family/ledger lane, data-plane/ML-infra lane, Neural Web lane, regime/gauntlet/kills lane (2026-08-05). Literature receipts: 20/20 citations fetched and verified, one overstatement (path-signature primer) flagged. This document commissions wave 1 and proposes wave 2; the operator ratifies, per the driver's-seat directive.*
