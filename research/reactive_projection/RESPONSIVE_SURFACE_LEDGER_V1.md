# Responsive Surface Ledger — V1 (R2 census + dispositions)

**Program:** `modernize-mastermind-architecture-20260830-sol-001` · completion operation `modernize-mastermind-reactive-production-completion-20260901-fable-001`
**Status:** architecture/product evidence, NOT a runtime control plane (freeze §7 anti-duplication law applies).
**Census base:** origin/main `88ac6cfd…` (2026-09-02, evidence path:line receipts in the census packet); dispositions by COO Fable under the Chairman completion override.
**Law:** every P0 surface finishes either `PROVEN_LIVE` at a justified cadence or explicitly `DURABLE_ONLY / NO FASTER USER JOB`. Nothing is forced into 60-second polling; a justified cadence can be the nightly.

| # | Surface | User job | Durable baseline (owner · cadence) | Current live producer today | Clocks shown today | Access | Disposition (V1) |
|---|---|---|---|---|---|---|---|
| a | Intelligence Hub (`site/intelligence_hub.html`) | cross-desk fused command center | `scripts/build_intel_hub.py` · daily.yml 18:30 ET | **R1A Market Pulse** — route-scoped controller, 60s cadence, `view=regular` chain (PROVEN_LIVE 2026-09-02); generic live.js retired from roster rows | baked as-of + live projection as-of in the instrument (added by R1A; page previously had NO visible clock) | anonymous shell; public quote-only API | **PROVEN_LIVE @60s** (R1A). Acceptance ruler accruing from 2026-09-02. |
| b | Macro/Risk flagship (`site/macro.html`) | regime / cycle / catalyst state | `scripts/build_site.py` (dashboard.html.j2 mode=macro) · daily.yml | live.js on 10 `.nb-px` nodes; `macrodata/release_forecast.json` fetcher; Prophet card partial | title bake-date; meta "updated daily"; no page-level live instrument | public.exact | **R2 CANDIDATE** — port the R1A pattern (page-level instrument + `data-*` clusters) in a later R2 vertical; until then live.js remains the interim producer. Justified cadence 60s for quotes; regime verdicts stay nightly (R3 territory, not quote cadence). |
| c | US stocks / Prophet (`site/us_stocks.html`) | buy-board / setups / Prophet card | `build_site.py` mode=stocks · daily.yml + `prophet-live.yml` intraday refresh (already market-hours cadenced) | live.js (6 nodes) + `basketdata/baskets.json` fetcher + prophet-live lane | title bake-date; Prophet card carries its own lane freshness | public.exact | **PARTIAL-LIVE already** (prophet-live owns board freshness — Breathing Platform's separately-ruled domain). R2 CANDIDATE for the quote-cluster pattern only; do NOT touch Prophet cadence/ownership (WS:BREATHING-PLATFORM). |
| d | Start hub (`site/start.html`) | signed-in global overview | `build_vector.py` · daily.yml | none (0 `.nb-px`; 1 inert live.js ref) | `asof` token present | public.exact | **DURABLE_ONLY (V1)** — a branded entry shell; no faster user job identified. Revisit only if R5 telemetry shows entry-surface staleness costs. |
| — | Anonymous root (`site/index.html`) | marketing landing | hand-authored template pair | none — fully static | none | public.exact | **DURABLE_ONLY** (explicit; correct for the funnel). |
| e | Stock dossier (`site/stock.html`) | per-ticker analyzer | `build_stock_library.py` · daily.yml + render lane | the estate's most live-wired page: `/api/dossier-quote` (shared projector since R1A-M), `live/flow_pulse.json`, flowtracker, per-symbol stockdata fetchers | real DOM `#r_asof` | anonymous shell; dossier API public | **PROVEN_LIVE (pre-existing pattern)** — the R1A shared projector now backs it (one quote-math owner). Disposition: converged; no rebuild. |
| f | Canada / China / HK boards | regional signal boards | `build_canada.py` (lane wiring gap noted) / `build_china.py` + `build_hk.py` · asia-close.yml morning slots | live.js (10/137/12 nodes); HK has one rotation fetcher | meta-only dates (CN has as-of text) | anonymous shell | **R2 CANDIDATES (later)** — china_stocks is the largest live.js surface (137 nodes) and the natural second vertical for the R1A pattern; regional-hours cadence, not US 60s. Canada's baking-lane wiring gap (no dedicated cron found for `build_canada.py`) flagged for the owning program. |
| g | Calibration Lab (`site/measurement.html`) | measured-not-asserted verdict lab | `build_measurement.py` · daily.yml + render lane | none (0 nodes) | timestamp token present | anonymous shell | **DURABLE_ONLY (deliberate)** — verdicts move nightly by design; intraday motion would misrepresent measurement cadence. |

## Standing findings the ledger carries
1. **The R1A pattern is the house pattern now**: page-level truth instrument (availability/freshness/session/coverage + as-of) + `data-*` clusters + one route-scoped controller + canonical-owner demand isolation. R2 verticals port it; nothing re-invents it (freeze §7).
2. **live.js is the interim generic layer, not the end state** on flagship boards: node counts (b:10, c:6, f:10/137/12) are the migration inventory. Retire it per-route as verticals port, exactly as R1A did on the hub.
3. **Every quote surface now shares ONE projector** (`app/public_quote_projection.py`) behind ONE Terminal owner (`view=regular` for page-scale batches).
4. **Baseline-clock gap pattern**: pages (b),(c),(f-CA/HK) show no visible as-of to users. Each R2 vertical adds the instrument's dual clock (baked + projection).
5. **Cadence justification is per-surface**: US quote surfaces 60s during RTH; Asia boards regional hours; regime/measurement verdicts nightly (that boundary is R3's materiality territory, never the quote poller's).

## Non-goals re-affirmed
No browser-wide event bus, no second quote plane, no SPA rewrite, no forced liveness on durable-only surfaces (freeze §7; DNR standing kills). Prophet/close-pass cadence stays with WS:BREATHING-PLATFORM; personalization stays behind WS:MARKET-OS prerequisites.
