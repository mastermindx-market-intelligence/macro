# Government Revenue Foresight — session handoff, 2026-08-11 ~evening Z

Successor to `research/GOVERNMENT_REVENUE_FORESIGHT_HANDOFF_2026-08-10.md` (wave landing + first-issuance adjudication) and `research/GOVERNMENT_REVENUE_FORESIGHT_ACCOUNT_HANDOFF.md` (wave specs, unchanged). This document records the first post-wave verification cycle, the defense20 graph publish, the surface pass, and the identity/vintage hygiene findings. **Re-census before acting.**

## §0 Where the program stands

1. **The #5268 correction regime is the ruled state** (merged 2026-08-10T18:19:50Z, after the prior handoff): the 8 issued snapshot rows are quarantined from active surfaces through a typed issuance-correction contract — ledger byte-identical (8 lines / 50,790 bytes / sha `920d840a…`), active candidates 0, corrected-only detail structured-410, history audit-visible, forward issuance eligible. This supersedes the prior handoff's "8 awaiting_crosscheck" surface state legitimately; the #5221/#5227 immutability rulings are honored by construction.
2. **§3.1 first-cycle verification PASSED** (2026-08-11): nightly collect + projection ran clean (`status: ok`), `coverage.award_events.rejected == 0` (610 validated / 500 visible), queue honestly zero-active, radar live with authority block + correction overlay. Zero new candidates is honest: the append-only action store accrued nothing new since 08-09T23:26Z.
3. **The ~20 action-rail candidate prediction is gated on exact-link coverage, not on a defect.** Award-level identity DOES attach at collect (82 `award_action_versions` rows carry `award_recipient_*` since the first post-#5090 collect), but only 1 of 35,222 action records resolves end-to-end into the reviewed graph; company-level exact links were 0/21 pre-defense20. The graph + panel-recall work below is the unlock.
4. **Four PRs shipped/armed today** (see §1): the #5331 clock-clamp + live-ledger defuses, #5363 schema re-tightening, #5369 radar surface pass, and the defense20-v1 graph publish PR **#5424** (DRAFT, sequenced on #5331 — hold + mechanical resume spec in its first comment).
5. **Collection panel recall is the measured next bottleneck**: red-team live checks proved ≥$6.7B of prime obligations attributable under the existing exact-join doctrine sit OUTSIDE the collected panel (LMT/Sikorsky $4.70B, GE registrant $1.85B + $1.54B, BA/Insitu, BA/Spirit, GE Aviation Systems, Unison). The panel (500-visible/610) is discovery-query-shaped, not corpus-shaped.

## §1 Today's landings

- **#5331** (pre-existing, retitled): `rewound()` clock clamp + **both live-ledger defuses** — incident tests read a frozen 8-row fixture (`tests/fixtures/government_revenue/issuance_incident_5fc18d5/`), the grader cohort pin decoupled. Detonation-proven bidirectionally (synthetic 9th row: pre-fix red at the exact pins, post-fix 33+141 green under the same append).
- **#5363**: `recipient_query_terms` restored to schema-required. Precondition proven fleet-wide (all 21 entities across every committed manifest site; single construction site emits unconditionally). The permissiveness-pinning test inverted to pin the requirement.
- **#5369**: radar surface pass — 10 label codes EN/ZH (clocks distinguished by *whose clock*: "Took effect"/生效日期 vs "We first saw it"/我们首次获知), delta-fold kills the `slice(0,4)` silent loss at source (+N-more disclosure for residuals), value-change direction from signed deltas only (▲/▼ + "Ceiling raised"/上限调高; no delta → no claim; compound disagreement → neutral headline; 红涨绿跌 via the site's own `--ink-up/--ink-down` swap), dossiers/briefcase raw-slug + frozen-English-receipt fixes. 50 govrev modules green.
- **#5424 (DRAFT, held on #5331)**: `recipient-graph:reviewed:2026-08-11:defense20-v1`, digest `11c4c586…` — BWXT onboarded (5 UEI edges, 4 independently parent-corroborated) + 57 refreshed exact edges; strict merge with published-rows-win (every defense19 row byte-identical: known_at, receipts, review provenance preserved); 3 conflicted GD rows dropped (WICO-parent conflicts); proposer registrant-key suffix bug fixed with regression tests (both sites via shared `registrant_join_key()`); manifest-equality test scoped to immutable identity; defense19 graph frozen into the incident fixture (see §2.5).

## §2 Rulings added this session

1. **IDV prime-cut widening: DEFERRED.** The bounded lane runs complete/non-partial at `selected_idv_limit: 24` (452 children, 23% of row budget, zero truncation). Marginal children cannot attribute to issuers while exact-link coverage is thin — widening now spends quota on unattributable context. Revisit after defense20 measurably raises resolution coverage.
2. **Variant C merge stands; the LHX/NOC phantom is a pre-existing published defect, retraction chartered separately.** The published defense19 graph itself contains `ownership:lhx:l3harris-technologies-inc` (issuer-as-own-wholly-owned-subsidiary) with `-de`-suffixed root entities. Attribution still resolves to the correct issuer through the phantom chain, so no user- or grader-facing wrongness ships. Correcting it requires re-parenting 7 published rows — a curated retraction act with its own accounting (row enumeration in §3.2), never a rider.
3. **GM GDLS (`identifier:gd:lfk5wlj8kt48`) retained**: published row, pre-existing `wholly_owned/1.0` claim on a by-name JV. Share-evidence pass chartered with the retraction.
4. **A reviewed manifest cannot re-time itself.** `config/government_revenue/candidate_historical_suppressions.v1.json` is a reviewed artifact; regenerating it on graph republish would stamp observation after review. Its equality test now compares the engine's own graph/clock-independent identity (all fields except `observed_known_at`, by exclusion so new contract fields compare by default); the artifact-to-artifact clock binding between the two frozen reviewed manifests is untouched.
5. **A frozen incident vintage freezes its GRAPH too.** `observation_id` embeds the reviewed-graph digest (`candidates.py:1692`), so a frozen ledger meeting a LIVE canonical graph re-times on every republish — that masked `test_same_clock_source_content_rotation_cannot_rebind_quarantined_history` behind the anti-backfill gate (protection intact, coverage blind). The incident happened under defense19; the defense19 graph bytes are now part of the frozen fixture. The projection suite must be green under a republished canon before any future graph PR ships.
6. **Proposer `no_exact_match` verdicts are panel-scoped** (instrument-verdict law applied to collection panels): GE's zero is true of the 111 collected rows, false of the world (red-team refetched GE's EX-21 and found exact-normalized USAspending matches with $3.4B+ obligations). Scope the language everywhere the verdict travels; never publish a panel zero as a world zero.
7. **Defense20 publish approval chain**: operator's original orchestration prompt named the step and its citation ("cite 2026-08-08 publish approval; #5040 prerequisite"); operator's live "complete them" directive executed it. Review protocol for the 62 additions: structural checks 268/268, normalizer reproduction 536/536, 14 edges live-verified against USAspending (11 clean, 3 conflicted → dropped), opus red-team BLOCK adjudicated finding-by-finding. The 203 defense19 rows carry their 2026-08-08 review act unchanged.

## §3 Next moves, in order

1. **Confirm the sweeper merges + live-verify**: #5331, #5363, #5369, defense20 PR. Live checks: radar renders the new labels/direction (spot EN+ZH), graph digest in `candidate_projection_status.json` flips to `11c4c586…` on the next cycle, workspace still validates under the re-tightened schema, `rejected == 0` holds. Then confirm exact-link coverage moves (BWXT UEIs should exact-link once collects re-observe: watch `by_exact_link_status`).
2. **Charter: govrev identity & vintage hygiene package** (one adjudication session; do NOT piecemeal):
   - LHX/NOC phantom retraction — full row enumeration: retire `legal:{lhx:l3harris-technologies-inc-de, noc:northrop-grumman-corp-de}` + their `issuer-identity:*-de` edges + phantom `ownership:lhx:l3harris-technologies-inc`; re-parent 7 published edges (`ownership:lhx:{l3harris-fuzing-and-ordnance-systems-inc, l3harris-global-communications-inc, l3harris-interstate-electronics-corp, l3harris-nexgen-communications-llc, l3harris-technologies-inc, l3harris-technologies-integrated-systems-l-p}`, `ownership:noc:northrop-grumman-systems-corp`); fold 10 LHX identifier rows onto the issuer root. Retraction accounting in the PR; curate admission is the gate.
   - GM GDLS economic-share evidence (or an honest JV relationship state if the schema grows one).
   - **The shared root cause**: `observation_id ⊃ graph_digest` re-times every frozen govrev artifact on republish (bit both the suppression manifest and the rotation test today). Decide: keep (vintage-scoped identity is an anti-rebinding feature; freeze fixtures accordingly — today's approach) vs decouple (identity change — touches settled rulings; needs full downstream census). Until then: **every new frozen fixture must include its graph bytes.**
   - Evidence-id content-addressing (UEI-keyed ids silently re-point receipts across proposals; 81 of 241 shared receipts cite a different award than defense19's under the same id — merge policy protected this publish; the scheme is the defect).
   - `parent_recipient_uei` as an official structured corroborator in the proposer (non-fuzzy, already fetched+hashed; corroborated 4/5 BWXT edges and exposed all 3 dropped GD conflicts; would make conflicts first-class instead of `conflicts: []` meaning "unchecked").
   - Suppression-manifest clock-source (`observed_known_at` from source observation, not graph clock) — needs a fresh review act on the regenerated manifest; never self-approve.
3. **Panel recall widening** (collection lane): the discovery queries miss Sikorsky ($4.70B), Insitu, Spirit AeroSystems, GE Aviation Systems, Unison, GE registrant UEIs (P+C). Extend `entities.json` query terms / alias coverage; the proposer will then propose the edges under the same doctrine. Also EX-21 extraction tail vocabulary (foreign forms: `S.a.r.l.` variant with spaces, `Sp. z o.o.`, `s.r.o.`, `LTDA`, `Sdn. Bhd.` — 9 real GE subsidiaries currently rejected as noise; low US-prime risk, real recall).
4. **Wave 10 rails 2/4/5** per the account handoff (DoD budget graph; SAM lifecycle — mind the SAM key's ~00–01 UTC quota gate, do not claim intraday freshness; recompete outcome). Each rail is its own chartered lane with the 7 acceptance gates; run as a session chain, not one session.
5. **Wave 11** (earnings/revenue translation) once central company-intelligence contracts are stable — per account handoff.
6. **Small follow-ups**: `dossiers.js` clock-label unification (test-pinned at `tests/test_government_revenue_dossier_ui.py:30,242`); `site/government-revenue-parity.css` ownership (no source twin in repo — chip filed; mono-on-words + Title-Case fight the new sentence-case copy); worksheet auditability (expose all extracted/rejected EX-21 lines + GE evidence URL/accession/sha — C6); `valid_to` retirement mechanism (BWXT Medical divestiture 2026-08-03 proves the staleness channel is live).

## §4 Traps this session added to the book

- **A graph-digest anywhere in a frozen fixture is a scheduled red on the next republish** — same family as the wall-clock literals, keyed to graph vintage. Grep new fixtures for digests before committing.
- **A masked guard can red as a `match=` mismatch while its protection holds**: the rotation test still refused the hostile edit — via the wrong (earlier) gate. Read WHICH error fired before concluding a guard works.
- **A published graph can contain the defect your fix targets** — the LHX phantom pre-dated the candidate that revealed it; "superset of published" and "correct" are different claims. Diff direction matters: candidate-vs-published disappearances can be the FIX being rejected by a conservative merge policy.
- **Proposer worksheets can assert world-scoped zeros from panel-scoped evidence** (GE `no_exact_match` "finished answer") — the same check that produces honest per-edge receipts produces overclaimed nulls; the panel is the instrument.
- **`evidence_id` keyed on a mutable upstream (UEI → one award, endpoint mutates) re-points silently across runs** — 81/241 receipts cited different award records than 3 days prior under identical ids.
- **A sweeper "not merging: check definitions changed" refusal heals by head-refresh** (merge main in / rebase), not by re-running stale checks.
- **The registrant self-reference in an EX-21 must match the registrant key** — `/DE/`-style suffixes on `registrant_name` mint phantom self-subsidiaries; the suffix-strip must apply at every key-derivation site (there were two).

## §5 Queue state at session close (~00:35Z 2026-08-12)

#5331 / #5363 / #5369 are armed on content-verified heads, oscillating with tonight's fleet churn: main's check definitions changed 4+ times (2ad4cbbd, a69a24aa, e21a265b, +), each change invalidates every armed proof, each refresh draws a full-suite run that exposes whichever foreign lane is currently red (observed serially: market-memory pack-6, infra-guards pack-10 [ship-loop-guard fence + cycle_pattern authority + historical symbols], options flow-surface, signal-contract pack-7, a transient fences HttpError, and a since-healed chat-nav drift). **Zero failures have ever been in these PRs' own diffs**; the 22:38Z full-180 main baseline was green. The sweeper + sibling actors have been refreshing/re-proving continuously — the queue converges when the workflow churn settles. If any of the three is still unmerged when you arrive: verify content (frozen-fixture sha `920d840a…` on #5331; required-list on #5363; `check_template_site_sync` on #5369), refresh the head against main, re-arm, move on. #5424's resume is its first comment.
