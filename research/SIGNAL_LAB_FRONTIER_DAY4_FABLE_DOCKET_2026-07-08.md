# Fable Day-4 docket — 7 candidates — 2026-07-08 (operator-ratified generation)

Small by design: quality over count, every candidate mines either a measured in-house
anomaly, a confirmed mechanism, or an asset only we hold. All prior rules apply
(receipts, PO-1..4, PO-1b, registry-first attestation, event-set verification law).

D4-01 FLAGSHIP — CN visible-supply absorption = strength (family cn_supply_absorption,
POSITIVE direction, fresh budget). Born from three sign-consistent anti-hypothesis
results (减持 windows +0.63%/21d t=2.4 with largest-tercile t=4.11; block clusters
+5.3%/21d t=8.5; deep-variant own-group only weakly negative). Construct: names with
ACTIVE disclosed supply (open 减持 window OR recent deep-discount block) whose price
holds/rises through the first 10 sessions of supply (absorption confirmation) → forward
21/63d vs own-group unconditional baseline (the F5-01 lesson: never cap-weighted proxy).
Counterparty: uninformed fear of disclosed supply. Moat: both tapes on disk, insight is
ours. CRITICAL CONTROL: absorption ≈ momentum — must survive momentum-neutralization
(the red-team should attack exactly here). Latent: CN-A (slot open — both prior
occupants directionally failed; this REVERSES them).
D4-02 US supply-absorption analog (family us_supply_absorption). Insider-sale clusters
(Form 4 store, 2006+) absorbed (price holds 10d) → continuation vs momentum-matched
baseline. Same thesis, deep US history, must beat momentum. Latent: US cross-sectional.
D4-03 Dealer-conditioned signed-flow residual — FORMAL PRE-REGISTRATION ONLY (no test):
freeze the W5-B promised construct now (signed premium residualized on NBBO-inferred
dealer positioning, per-underlying) with an auto-run trigger when the tape store reaches
100 names × 250 days. Removes post-hoc freedom later; consumes no test slot today.
D4-04 Bad-news-absorbed generalization (family badnews_absorption, US). Events from our
own stores where disclosed operational bad news (KEV entries, NHTSA investigations,
material 8-K items) is absorbed (0..+5d abnormal return ≥ 0) → 21/63d continuation vs
event-unconditional and momentum baselines. Mines the KEV drift-up finding; moat = our
event stores + the measured priors. Same momentum-confound attack applies.
D4-05 Credit ETF-flow vs HY-OAS divergence rescreen (SLF-058 revival from wave-1 local
queue; never advanced, mechanism-first now): flow thrust while spreads widen (or
outflow washout while spreads tighten) as de-escalation/conditioning input vs the
HY-OAS timer baseline that beat CMDI. Data: ETF flow store + FRED on disk.
D4-06 Extension-day family extension (INSIDE d2_rates_calendar_flows budget, variants
not new family): AGG/LQD/IEI last-business-day cells — does the confirmed TLT/IEF
index-extension flow generalize across the bond complex? Cheap, rides a confirmed
mechanism, honest small addition to an existing family grid.
D4-07 Breakout trade-size confirmation (family w5_trade_size sibling, mirror construct,
fresh cells): avg-trade-size EXPANSION z on 52w-HIGH breakouts (institutional
participation) vs breakouts on retail-sized tape → continuation quality. V2-at-lows
showed expansion = distribution; the mirror at highs is untested. Data ready
(transactions column). Must beat volume-z control (the exact control that killed V1).
