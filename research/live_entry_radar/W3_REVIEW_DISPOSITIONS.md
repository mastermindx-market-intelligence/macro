# W3 / PR-3 — independent adversarial review: findings and dispositions

**Reviewed artifact:** branch `claude/entry-radar-w3-pr3` at the post-implementation head (pre-fix baseline: 625 passed on the 11-suite entry-radar CI line; post-fix: 675 passed).
**Reviewer:** fresh-context Opus agent, commissioned with the 30-item attack list from the W3 commissioning handoff §32 plus A5 line-by-line fidelity, test-vacuity probes, and mutation controls. The reviewer did not build the code and fixed nothing.
**Adjudicator:** the W3 orchestrator (Fable main loop). Every BLOCKER/MAJOR was **reproduced with a receipt before any fix** (probes under the session scratchpad `review/` dir); fixes were implemented only per the adjudicated rulings below; each ruling shipped a named `test_W3_<n>_*` regression test.
**Outcome-blindness:** no forward outcome, replay return, MFE/MAE, false-start, or ranking result existed or was consulted anywhere in this cycle.

## Findings and dispositions

| id | sev | finding (compressed) | adjudicated ruling | fix + regression receipt | round-2 verify |
|---|---|---|---|---|---|
| W3-1 | BLOCKER-class | Price-basis disagreement between daily history and session tape fabricated a C1 arm (raw 2:1 tape vs adjusted dailies moved the arm 4h10m earlier and minted an event); only c2f gated on basis | Fix fully regardless of grade (W3's replay adapter is contractually adjusted-only per A5.0, but the engine must refuse, not trust): whole observation `unavailable` on basis disagreement, mirror of c2f's refusal; `daily_price_basis` added to features; §5 price-basis contamination test added | Fixed: raw-vs-adjusted now yields 234/234 unavailable, 0 C1/C2 events, verdicts={None}; CONTROL agreeing-bases byte-identical. `test_W3_1_*` ×3 | DEAD |
| W3-2 | MAJOR | C3 ARMED never expired (candidate minted 88 sessions post-arm, daily K 95.8 at fire) — frozen §10: ARMED without promotion expires after 15 sessions | Wire the §10 expiry into `run_c3` (EXPIRED at 15 elapsed sessions, no candidate at/after). Reviewer's alternative (re-check `leg.washed` at the turn) REJECTED — it would rebuild must-still-be-oversold semantics, the exact mistake A5.3 forbids for C2; A5.4's law is arm-then-first-post-arm-turn | Fixed: arm 2026-02-27 → EXPIRED 2026-03-20, 0 events; mutation control (clock removed) reproduces the late fire. `C3_ARM_EXPIRY_SESSIONS=15` rides `C3_SPEC` by value. `test_W3_2_*` ×4 incl. before/after mutation | DEAD |
| W3-3 | MAJOR | Every C2 reading carried `evidence_refs` to the C1 event, including 300/300 readings strictly before that event existed | Evidence may only cite events with `signal_ts <= observed_at`; guard test asserts the invariant corpus-wide | Fixed: pre-arm readings carrying refs 0/300. `test_W3_3_*` ×3 | DEAD |
| W3-4 | MAJOR | Firing-relevant constants rode no spec hash (ATR_LEN, MINUTE_BAR_SECONDS, REARM_K_FLOOR/K_SESSIONS/MAX_SESSIONS existed as prose or not at all); PIT-19 mutates spec keys and structurally cannot see un-keyed constants | Carry each by value in `INDICATOR_CORE`/`C1_SPEC` (rearm block nested, every value pinned by named-key test); add a guard sweeping module-level numeric constants into some spec block with an explicit allowlist for non-firing constants | Fixed; hashes moved lawfully pre-publication (see below). `test_W3_4_*` ×3 | DEAD (nested-aware re-probe) |
| W3-5 | MAJOR | `stale` was in the §5 vocabulary but unreachable; a 3-month-old daily feed produced `confirmed` readings and a measured non-fire | Freshness = continuity against the reference session calendar: last confirmed session must be exactly the session preceding the evaluated one; else `stale` + `condition_met=None` (readings validation extended: stale requires None, same rationale as unavailable) — for `build_observation_path`, `c3_daily_leg`, and C4 grain inputs | Fixed: April-history vs June-tape → availability={'stale'}, 0 events, verdicts={None}. `test_W3_5_*` ×8 across three suites | DEAD |
| W3-6 | MAJOR | `superseded_reading` dated an unknowable losing watch at its 3D-open `signal_ts` with `availability=confirmed` — the A5.6 knowability law violated in the loser branch | Mirror `c5_reading`'s knowable refusal: unknowable loser → unavailable + None + `knowability_basis="signal_known_ts (absent)"`; knowable loser keeps `condition_met=False` (evaluated, lost precedence) | Fixed; both branches tested. `test_W3_6_*` ×3 | DEAD |
| W3-7 | MAJOR | `build_radar_native_event` fenced the family but not the detector: `detector_id="C4_MTF_TURN@1"` under a lawful family was ACCEPTED (builder door and direct `EntryEvent` construction) | Single refusal source `STRATIFICATION_ONLY_DETECTOR_IDS` in `entry_events`; builder door and `EntryEvent` validation both refuse; `challengers.assert_can_fire` consumes the same tuple; PIT-13 gains both reproductions | Fixed: both doors refuse with the stratification-only message. `test_W3_7_*` ×4 | DEAD |
| W3-8 | MAJOR | C2 encoded pre-arm (mechanism out of scope) as `availability="unavailable"` while the input was present — 100% confusable with data-missing on the fixture; C3 encoded the same situation the opposite way | Harmonize C2 to C3's encoding: pre-arm → `condition_met=False` (the episode clause of the C2 condition is known-False; evaluators stay uncalled, preserving the c2c in-episode pivot proof), availability = input state, `features.eligible/pre_arm`; post-arm missing input stays unavailable+None; input state dominates when both hold | Fixed; three tests carry one-line TRUTH-CHANGE notes (documented encoding harmonization, not golden acceptance); the null-law meta-test rewritten to name where the null law is actually exercised | DEAD (incl. null-law coverage check) |
| W3-9 | MAJOR (vacuity) | Deleting the RTH filter left the whole battery green (fixture printed every RTH minute, so PIT-11 passed either way) | New fixture: premarket prints + open print-gap + postmarket; assert exclusion, None sampled intervals at the gapped open, and an in-test monkeypatch mutation control | Fixed. `test_W3_9_*` ×3 incl. the control | DEAD |
| W3-10 | MINOR | `rearm_eligible` wired to no engine path; two washouts 56 sessions apart folded into one episode in a multi-session `run_c1` path | Docstring-only by ruling: single-episode-per-path is by construction; the returned episodes are not a §10 episode ledger; §10 clocks belong to PR-4/PR-5 and `rearm_eligible` is their exported primitive | Documented; docstring pin test | verified |
| W3-11 | MINOR | Confirmed-but-empty 4H bucket silently dropped (no reading, no gap marker, series adjacency undisclosed) | Keep adjacency (no fabricated closes) but disclose: `completed_4h_gaps` in features + a `confirmed_empty` row in `C3Run.provisional` | Fixed + test | verified |
| W3-12 | MINOR | `DailyHistory` accepted a duplicated session index (silently lengthening the close series and moving K) | Raise on duplicate/non-monotonic normalized index | Fixed + test | verified |
| W3-13 | MINOR | c2f's non-positive-ATR guard existed in code but not in the hashed spec prose | Add "non-positive prior-confirmed ATR ⇒ unavailable" to the c2f spec string (rides the hash) | Fixed | verified |
| W3-14 | MINOR | C3 arm stamped as a bare date while candidate is a UTC instant (unsortable mix) | Arm at the session-open instant via `session_window_et`; same-session buckets stay eligible | Fixed + test | verified |
| W3-15 | MINOR | C1/C2 radar-native events carried `signal_known_ts=None` though a 1D-LIVE observation is knowable exactly at its instant | Pass `signal_known_ts=observed_at`; finality unchanged (live provisional) | Fixed + test | verified |

## Judgment-call verdicts (orchestrator adjudications attacked by the reviewer)

- **JC1** (`_prev` = per-variant lawful-observation memory; pre-arm memory advances): attack failed with proof — exhaustive search over 1,891,525 K-paths appended 2,024,860 pivots, 0 originating pre-arm; structural argument recorded in the review transcript.
- **JC2** (first fire per episode×variant is THE candidate): confirmed; 7 events on the fixture, 7 unique addresses.
- **JC3** (bucket confirmation via reference position, series-independent): confirmed and conservative; leading-drop invariance holds byte-identically.
- **JC4** (confirmed-daily mirror rides the spec + `c3_daily_leg`): accepted (thin) — the operative negative ("no `C1_CONFIRMED` arena detector") holds; registry has exactly six ids.
- **JC5** (transitive `engine.technicals` module load via `stock_technicals`): confirmed lawful — AST proof of no direct import anywhere in Radar W3; `stock_technicals.atr/true_range` source contains no `technicals.` reference, so Radar cannot reach the second RSI family through the ATR pair.
- **JC6** (synthetic fixtures by K-inversion on real NYSE dates): confirmed; provenance manifest machine-readable; the generator is committed as the receipt; no receipt-from-itself trap.
- **Watch-item W1** (stale rolling-min reach-back in `recent_washout`): REFUTED — `last_finite` reads the last element only; pandas `min_periods` default makes the trailing window NaN → unavailable, conservative in the right direction.

## Clean surfaces (attacked, no finding)

EOD-mutation immutability (C1/C2/C3/C4); no K<20-at-turn requirement (c2a/c2c fire at K≈24); c2c causal pivots; c2f sampled-low law (mutation caught); exactly six variants from one source object; C3 reads only the confirmed daily leg; pre-arm 4H turns cannot promote; bucket-confirmation boundary exact at `effective_end`; clipped-tail disclosure; early-close (Dec-24) lawful; extended-hours exclusion; C4 absolute anchor + snapshot immutability under later completion; C5 store byte-identical (no duplication, no minted events); address uniqueness; era-fence honesty (`pre_channel_reconstruction=False` for all radar-native events); no ticker-keyed thresholds; no strength numbers; no outcome machinery anywhere; no durable writer; no network/env/wall-clock in the engine; §16 protected paths diff-empty + AST-clean; F1 refused; spec-prose ↔ code agreement line-by-line (post-W3-4/13); determinism across processes and hash seeds; sparse-tree safety; the two W2 pin updates weaken nothing (G0's frozen hash literal untouched).

## Spec-hash record

Pre-fix → post-fix (moves are lawful: hashes were unpublished until this PR, no outcome ever attached):

```
C1_1D_LIVE_WASHOUT@1  55cb3269d02a02d1 → f0bbd6cf3a6e2339
C2_1D_TURN@1          fbaee598c99910fc → d8ba60a25cfa7400
C3_1D_4H_RECOVERY@1   f9487fce57130fa4 → d54dc1e55c4261c8
C4_MTF_TURN@1         c126fcd3f4727119 → dce21ac680233ee2
C5_BOTTOM_WATCH@1     13dec66345a0376c (unchanged — W3-6 was a code fix with no firing-relevant spec content)
G0_GREY_DOT@1         9be89a8acc8b905c (unchanged, W2 identity)
```
