# W4 / PR-4 — independent adversarial review: findings and dispositions

**Reviewed artifact:** the W4 live-evaluator branch at its post-implementation head — `engine/entry_radar/{live_eval,live_ledger,live_pack,vendor_minutes}.py`, `scripts/entry_radar_live{,_pack}.py`, the four VPS units, the `entry-radar-live.yml` backstop, and the seven W4 suites. Post-fix: **1,141 passed, 1 skipped** on the 18-suite entry-radar CI line, of which **67 are the named `test_W4R_*` regressions** this round added (1,136 / 63 before M8's open gap was closed — see the M8 row and round 1.5 §5). No pre-fix baseline is quoted: the W4 modules were new and untracked at review time, so there is no revision to re-measure against — the per-finding mutation evidence below is the receipt instead.
**Non-vacuity audit (round 1.5):** a second fresh-context Opus reviewer re-attacked the *regressions* rather than the code, reverting each fix in place and re-running. It found four that passed with the reported defect fully restored, plus one adjudicated ruling whose premise turned out to be false. Those are recorded in the rows below and in §"Round 1.5" — a regression that cannot fail is a receipt for nothing.
**Reviewer:** fresh-context Opus reviewer commissioned during the build wave; findings delivered with measured probes. The reviewer did not build the code and fixed nothing. Every CRITICAL and HIGH arrived with an executed reproduction against `tests/test_entry_radar_w4_pack.build()` — not an inspection — and the reviewer flagged its own line-number drift (`live_eval.py` grew 1876 → 1939 lines *during* the review), so each citation carries a code anchor as well as a line.
**Adjudicator:** the W4 orchestrator (Fable main loop). All 22 findings were ACCEPTED; four were accepted with a *narrowed or different* ruling than the reviewer proposed (C1, C2, H3, M1 — see the ruling column). Each ruling shipped a named `test_W4R_<id>_*` regression in the suite that owns the surface, with a mutation control wherever the guard is load-bearing.
**Outcome-blindness:** no forward outcome, replay return, MFE/MAE, hit rate, grade, or ranking result existed or was consulted anywhere in this cycle. The W4/W5 firewall was itself one of the surfaces attacked and carried no finding.

## Findings and dispositions

| id | sev | finding (compressed) | adjudicated ruling | fix + regression receipt | verify |
|---|---|---|---|---|---|
| C1 | CRITICAL | One vendor error `continue`d past a session, deleting it from the C3 4H series. `four_hour_turn` reads *positional* neighbours, so a transient 502 recomputed **every** histogram point and let `prev2/prev/now` straddle a calendar gap. Measured: a `C3Run` with 36 readings, no gap disclosure anywhere. Undetectable — `completed_4h_gaps` counts only fetched-and-empty buckets, and a never-fetched session contributes none | Reviewer proposed "refuse the name"; ruling **narrowed to refuse the LANE**. A name missing any session in its C3 episode window does not run C3 this pass: per-name reason `c3_incomplete_window` (new in `NAME_REFUSALS`), the missing sessions listed in the row receipt, readings `availability="unavailable"` per the null law (a fetch fault is missing input, not a non-fire). `run_c3` may only ever receive the complete contiguous session range; C1/C2 keep evaluating | `_run_c3` returns `(run, missing_sessions)`; caller refuses the lane at `live_eval.py:1681-1684`. `test_W4R_C1_a_missing_session_REFUSES_C3_rather_than_running_on_a_gapped_series`; CONTROL `…_C1_CONTROL_an_unbroken_window_still_runs_C3` | fixed + regression green (round 2 pending) |
| C1b | CRITICAL | `stats.update(_reader_stats(reader))` overwrote the pass-local C3 error count with the reader's own. `VendorMinuteReader.errors` increments **only** on a transport raise, so an aggregation or mixed-timezone fault — the ones `_bucket_from_dict`'s docstring warns about — left it at zero. Measured: 3 sessions errored, `stats['errors'] == 0`. The `c3_reader_errors` health reason could therefore never fire and the state never left `live` | Merge, never overwrite: the reader's counters ride a `reader_` namespace of their own and the pass-local count stays authoritative for the health reasons. Counters only the reader can keep (`cache_hits`, `empty`) fold in unprefixed as well. `PASS_OWNED_C3_STATS` is the mechanical statement | `PASS_OWNED_C3_STATS` (`live_eval.py:183`); namespaced merge at `:1991-1993`. `test_W4R_C1b_an_AGGREGATION_fault_still_reaches_c3_reader_errors` (an aggregation raise, not a transport one — the case the old code could not see); `…_C1b_the_reader_counters_ride_a_NAMESPACE_of_their_own` | fixed + regression green (round 2 pending) |
| C2 | CRITICAL | `LiveEvalError`, `ChallengerError`, `C3Error`, `LedgerError` are **siblings**, not subclasses, so the per-name `except LiveEvalError` caught none of them: one bad name raised out of `run_pass` and killed the whole pass. `ledger.apply_run` sat outside the try entirely. The script's top-level `except Exception` then returned **0** — the served artifact kept its previous body *and* its previous `health.pass.at`, stale-but-whole, with the session-grain freshness sentinel as the only alarm. A persistent per-name defect burned a full session, green | Widen the per-name catch to Exception **with classification**: the five named sibling classes → named reasons; bare `Exception` → `evaluator_error` with the exception class recorded. `ledger.apply_run` moves INSIDE the protected region. Ruling extended past the reviewer's: a top-level failure must **publish the refusal payload** with `health.state="failed"` — the declared-but-unproduced seventh state gets its producer — and exit **nonzero (6)**, never a silent 0 over a stale artifact | `NAME_ERROR_REASONS` + `error_reason()` (`live_eval.py:224-296`); `failure_payload()`; `scripts/entry_radar_live.py:347-365` returns 6. **Round 1.5**: the protected region now opens at the TOP of the per-name loop (`pack_daily_history`/`read_quote`/`basis_audit` were outside it), and `JournalRefused` splits off `LiveEvalError`. `test_W4R_C2_a_sibling_exception_darks_ONE_name_and_the_pass_survives` (6 params incl. both journal-split rows), `…_a_LEDGER_error_darks_only_its_name_so_apply_run_is_INSIDE_the_guard`, `…_every_classified_reason_is_an_ENUMERATED_refusal`, `…_the_failed_receipt_is_reachable_and_shaped_like_every_refusal`, `…_writes_NO_heartbeat_of_its_own`, **new** `…_the_SCRIPT_publishes_the_failed_receipt_and_exits_6` + `…_CONTROL_a_clean_script_pass_publishes_and_does_NOT_exit_6` | fixed + regression green (round 2 pending) |
| C3 | CRITICAL | `_c4_context` passed the **pass** session to `c4_snapshot` while stamping the reading at `episode.candidate_at`, which `_episode_path` deliberately places days earlier for an open episode. Measured: candidate `2026-08-11T15:00:00Z`, pass session `2026-08-17` → a row stamped `observed_at: 2026-08-11` and `market_session: 2026-08-17`, computed from bars through 2026-08-14 when the lawful cut was 2026-08-10. Four confirmed sessions of future information behind a reading stamped in the past; the row contradicts itself | Reviewer's option (a): `market_session` = the candidate's own session (`candidate_at[:10]`), so `confirmed_through` cuts lawfully and the row's two stamps agree. Option (b) — run `_c4_context` only for same-session candidates — REJECTED: it drops the context from every multi-day episode to fix a stamping bug. C4 is stratification only and cannot fire, so no firing decision moved; the defect was a published reading built from post-stamp data | `candidate_session = date.fromisoformat(str(episode.candidate_at)[:10])` at `live_eval.py:2043-2047`. `test_W4R_C3_a_prior_session_candidate_snapshots_at_ITS_OWN_session` (byte-compare against the snapshot computed at candidate time); CONTROL `…_the_pass_session_cut_is_a_DIFFERENT_block`; mutation `…_a_bar_confirmed_AFTER_the_candidate_cannot_move_the_snapshot` | fixed + regression green (round 2 pending) |
| H1 | HIGH | `state = "live" if (evaluated and not reasons)`. The reason ladder counted basis, quotes, pack integrity, C3 errors and cadence — **nothing about the readings themselves**. Measured on probe set `["STALE"]`: zero measurements taken, `condition_met=None` everywhere, `HEALTH.state = live`, `HEALTH.reasons = []`, `health.dark` all zeros. `dark` structurally cannot help: it counts dark names, and a null reading is not a dark name. The §0 null law failing at the one surface an operator reads | `live` becomes the NARROW state: it additionally requires ≥1 reading whose availability is not in {unavailable, stale} across the evaluated names. Add `null_readings` counters `{reading_unavailable, reading_stale, suppressed}` and the reason `reading_null:{n}/{evaluated}` when every evaluated name is null → `degraded` | `null_n` gate at `live_eval.py:2160-2169`; `null_readings` block in both health constructions. `test_W4R_H1_a_pass_in_which_EVERY_reading_is_null_is_not_live`; CONTROL `…_one_real_measurement_is_enough_for_live`; `…_the_null_counters_are_always_present` | fixed + regression green (round 2 pending) |
| H2 | HIGH | `append_point`'s return value was discarded. It returns `False` and records a refusal when `ts <= last_ts`, and `NAME_REFUSALS`'s `journal_refused` was produced only by the cross-pack `open_session` branch — so `health.dark[journal_refused]` structurally could not count an append refusal. Measured: pass 2 offering the same vendor ts at 67.82 against a tape holding 82.23 published `quote.price = 67.82` beside `sampled_close = 82.23`, `state: evaluated`, no reason. A 19% divergence between the advertised live quote and what the detectors actually read, disclosed only in a journal file on the VPS | Check the return. On refusal: per-name reason `journal_refused_point` (new in `NAME_REFUSALS`, counted in `health.dark`) **and** the published quote block must be the point the tape actually used, with the rejected point recorded on the row as `quote_rejected`. Keep evaluating — the tape is still lawful. Distinguish by **price delta**: a benign same-ts same-price republish (the steady illiquid case, which is most passes) raises no reason, because a counter that fires on the normal case stops meaning anything | `live_eval.py:1610-1611` sets the reason and captures `quote_rejected`; `NameResult.quote_rejected` threaded to the row. `test_W4R_H2_a_corrected_price_at_the_SAME_ts_is_named_and_the_row_shows_the_tape`; CONTROL `…_H2_CONTROL_a_benign_republish_at_the_SAME_price_raises_no_reason` | fixed + regression green (round 2 pending) |
| H3 | HIGH | `payload["suppressed"]` filtered over `ledger.suppressions` — the **whole history** — with no pass or session scope, and `_refuse` appended a new row on every call. Measured over four passes: 1, 2, 3, 4 rows in the payload and 1, 2, 3, 4 in the ledger. At ~78 passes/session one suppressed name ends the day with 78 identical rows in `episodes.json`; the count is quadratic in sessions × suppressed names, `compact()` never touches `_suppressions`, and `_ledger_hash` covers only `episodes` — so the growth was invisible to the content signal and inflated any §11 control-pool count by exactly the pass number | **BOTH halves**, and the reviewer's "worth raising with Builder A" is adopted as a ruling, not a suggestion: `live_eval` snapshots `mark = len(ledger.suppressions)` and publishes only `[mark:]`; `live_ledger._refuse` becomes IDEMPOTENT on `(ticker, detector_id, variant, session, would_have_armed_at)`. `reason`/`detail` stay outside the identity — the same would-be arm re-derived by a second rule is one fact. This is a Builder-A-module fix, allowed, keeping A's docstring register | `mark` snapshots at `live_eval.py:1781,1819`; `_suppression_key` + dedup at `live_ledger.py:177-186,723-729`. Evaluator half: `test_W4R_H3_repeated_passes_leave_exactly_ONE_suppression_row` (pit). Ledger half, **7 new**: `…_the_same_would_be_arm_records_exactly_ONE_suppression_row`, `…_the_dedup_SURVIVES_a_save_and_a_RELOAD`, `…_CONTROL_a_DIFFERENT_instant_is_a_DIFFERENT_fact`, `…_a_second_variant_at_the_same_instant_is_its_own_fact`, `…_a_CHANGED_reason_at_the_same_instant_does_not_re_record`, `…_the_suppression_identity_is_re_typed_from_the_ruling`, `…_an_arm_with_no_recorded_instant_stays_unrecorded` (ledger). Mutation-proved: reverting `_refuse` to an unconditional append reds 3 of the 7 | fixed + regression green (round 2 pending) |
| H4 | HIGH | The 4H bucket cache keyed on `(TICKER, session)` only — no `as_of`, no `pack_hash`, no adjustment epoch — while `AGGS_PARAMS` requests `adjusted: "true"`. Adjusted minute aggregates are **not** immutable: a split or large cash dividend retroactively rescales the vendor's whole history, so after a corporate action `run_c3` receives pre-adjustment cached closes spliced to post-adjustment fresh ones. That seam fabricates a 4H histogram move, which fabricates a turn — the W3-1 defect reintroduced in the one lane where no basis audit runs. The docstring ("a past session's minutes are immutable") was the load-bearing argument for a cache with no invalidation rule | Stamp each cached row `{pack_as_of, substrate_fingerprint}`; on mismatch against the current pack, invalidate **that ticker's whole cache** and refetch — not just the mismatched row, because the seam is what does the damage. Rewrite the docstring to state the invalidation rule: adjusted aggregates are not immutable across corporate actions, and the cache is safe because it is *vintaged*, not because minutes are | `_store_buckets`/`_load_buckets` stamp and check in `vendor_minutes.py`. `test_W4R_H4_a_moved_adjustment_basis_DROPS_the_whole_ticker_cache`; CONTROL `…_CONTROL_the_SAME_fingerprint_still_serves_the_cache`; `…_the_stored_row_records_the_basis_it_was_written_on`; `…_the_fingerprint_moves_with_the_ADJUSTED_CLOSE_and_nothing_else`; **new** `…_the_invalidation_is_the_WHOLE_ticker_not_the_moved_row` (warms three sessions — the one-session version passed on the per-session variant the ruling rejected) | fixed + regression green (round 2 pending) |
| M1 | MEDIUM | `IncrementalObservationBuilder`'s docstring claimed "one chain evaluation for the new interval instead of one per interval since the open", but a fresh builder is constructed per name per pass, so `self._chain` starts empty. Measured over seven passes: `chain_at` calls `[1, 2, 3, 4, 5, 6, 7]`. Cost per pass is O(intervals-since-open) and the session total O(intervals²) — at 15:55 over a ~1,500-name probe set, ~117,000 chain evaluations inside one 5-minute pass. Nothing in the suite measured it. So the module carried a second implementation of the oracle's per-tape body, with the parity risk that entails, for a benefit it did not deliver | Neither of the reviewer's two options. An in-memory memo **cannot** work here: the systemd lane is one oneshot process per pass, so a pass-spanning map dies with the process; and deleting the class loses the truncation semantics. Ruling: adopt the **JOURNAL-backed** design — today's derived Observations are journaled append-only and pack_hash-pinned exactly like points, and a pass computes chains ONLY for intervals with no journaled observation. The journal *is* the memo, which makes the O(1) claim true across process boundaries. The byte-parity pin against the oracle stays | Journaled observations in `SessionJournal` (`live_eval.py:577,708-736`), `observations(..., journaled=…)`. `test_W4R_M1_the_second_pass_evaluates_the_chain_only_for_NEW_intervals`, `…_a_restart_replays_the_journal_and_stays_byte_identical`, `…_a_journaled_interval_is_never_rewritten` — all three **vacuous at the pass level** (round 1.5: `journaled=None` left 250/250 green). **New** `…_M1_the_PASS_ITSELF_reads_the_memo_off_the_journal` drives two passes over a shared state dir against a control whose frozen observations are stripped and whose points are not; under the same mutation it is the only failure | fixed + regression green (round 2 pending) |
| M2 | MEDIUM | `observations()` silently diverged from the oracle when `tape.session != builder.session`: the builder cut `confirmed_through` on its OWN session. Measured on the same daily and tape — `inc.k = 15.25` vs `oracle.k = 65.46`, `confirmed_bars` 192 vs 48, no error. `_parity_case` always passes `session=tape.session`, so the byte-parity pin proved parity only on the diagonal; `live_eval` calls it on the diagonal too, so the defect was latent, not live | Raise `LiveEvalError` on the off-diagonal at the top of `observations()`, plus a negative test. The reviewer's two coverage gaps close in the same pass: the corpus tape was `_flat_tape(…, 90.0)` for all five tickers (one memo key on every parity case) and `now=None` always (the truncating branch compared against nothing) — so the parity corpus gains distinct per-ticker prices and a `now=` truncation compare | Guard at the head of `observations()`. `test_W4R_M2_an_OFF_DIAGONAL_tape_session_refuses`; `…_M2_parity_holds_on_a_MULTI_PRICE_tape_and_under_now_TRUNCATION` | fixed + regression green (round 2 pending) |
| M3 | MEDIUM | `CROSS_CHECK_EPSILON_REL = lp.PROOF_EPSILON_REL` (1e-4) while `lp.BISECTION_REL_TOLERANCE` is 1e-6 — a band **100×** the solver's own precision, justified by a comment naming a different quantity (`PROOF_EPSILON_REL` is the proof battery's straddle offset). `ThresholdSolution.rel_tolerance` carried the correct per-solution value and was never read. On a $100 name the justified band is ±$0.0001 and the implemented one ±$0.01, so any genuine pack/oracle disagreement inside 1 bp of the level reported `boundary_band` instead of `disagree` and no `pack_integrity` refusal fired. PIT-W4-15's control moves the level grossly and passed either way | `_boundary_band` reads `solution.rel_tolerance` — self-healing if the solver's precision moves — and `CROSS_CHECK_EPSILON_REL` is re-pointed at `lp.BISECTION_REL_TOLERANCE` rather than deleted, since it remains the documented fallback when a solution carries no tolerance | `live_eval.py:154` (re-pointed) and `:1010-1017` (reads `solution.rel_tolerance`). `test_W4R_M3_a_disagreement_at_TEN_TIMES_the_bisection_tolerance_is_a_DISAGREEMENT` — seeded inside the OLD band, so it is exactly the case the old guard swallowed | fixed + regression green (round 2 pending) |
| M4 | MEDIUM | `lp.basis_audit` ran before `if not quote.usable`, inverting design §2 (quotes step 4, basis step 5) and masking the real refusal. Measured with a premarket-stamped quote *and* a 2× `prevClose`: reported reason `basis_mismatch`, `health.dark = {basis_mismatch: 1}` — the quote problem invisible in both the row and the counters. In aggregate a dead quote lane would present as a universe-wide data-integrity page. Related: `basis_stats["audited_n"]` counted names that never reached the engine, so the `basis_unverified` guard could be satisfied by dark names alone | Restore the design §2 order — quote gate BEFORE the basis audit — and count `audited_n` only for names that reached the engine | Reordered in the per-name body; `audited_n` incremented past the quote gate. `test_W4R_M4_the_quote_gate_runs_BEFORE_the_basis_audit` (asserts the reason is the quote state); `…_M4_audited_n_counts_only_names_that_REACHED_the_engine` | fixed + regression green (round 2 pending) |
| M5 | MEDIUM | `_gate_arm`'s own docstring — "a blocked arm keeps its READINGS … dropping them would erase the evidence that the rule bit" — was falsified by the code: stripping the episode made `_has_active_lane` return `False`, so `_payload_row`'s compact branch emitted `lanes = {"nightly": …}` only. Measured: row reasons `['suppressed_by_rearm']`, lanes `['nightly']`, C1 readings in payload `False` while the `NameResult` held 1. The readings survived on the object and died at the artifact boundary — and the served payload is the only durable trace of a suppressed pass, since nothing is spooled and nothing committed | `_has_active_lane` returns True when `suppressed` is non-empty **or** any reading has `condition_met is True` | `_has_active_lane` (`live_eval.py:2277-2283`). `test_W4R_M5_a_suppressed_names_row_carries_its_C1_READINGS`; **new** `…_a_SUPPRESSED_lane_is_active_even_with_no_met_condition` — on the end-to-end fixture both clauses fire at once, so deleting the `suppressed` clause left it green | fixed + regression green (round 2 pending) |
| M6 | MEDIUM | `_write_heartbeat` was reached only on the `_evaluate` path; all five whole-cycle refusals returned before it. Measured — one good pass then three `stale_pack` refusals — the heartbeat stayed `('stale_pack', 2, '2026-08-17T13:50:00Z')` three times over. `seq` was an *evaluation* sequence presented as the pass counter, so "ran 78 times and refused" and "did not run at all" left the identical trace: the same failure signature the render-cancel rule exists for | Write the heartbeat on EVERY terminal path, carrying `state`; `seq` becomes the true pass counter and a separate `evaluated_seq` keeps the narrower reading | `_pass_block` (`live_eval.py:1200-1227`), `_beat_from` (`:1230-1236`), heartbeat on the refusal paths. `test_W4R_M6_three_stale_pack_refusals_ADVANCE_the_pass_counter`; `…_M6_the_cadence_gap_still_reads_off_the_refusal_heartbeats` (the property that could have regressed — `prev_gap_intervals` must not now read every refusal as a cadence miss) | fixed + regression green (round 2 pending) |
| M7 | MEDIUM | `--dry-run` wrote to the production state dir: the journal flush and `_write_heartbeat` ran unconditionally and the script gated only the spool, while the help text said "spool nothing, commit nothing, publish nothing". Measured: state dir before `[]`, after `['heartbeat.json', 'journal']`. An operator running `--dry-run` on the VPS without `--state-dir` — the natural invocation — appended a point to the live journal and bumped the heartbeat, corrupting the next real pass's `prev_gap_intervals` and `seq` | Thread `dry_run` into `run_pass`: no journal flush, no heartbeat, no state mutation. The reviewer's alternative (pass `state_dir=None` from the script) REJECTED — it changes what the rehearsal exercises, which is the one thing a rehearsal must not do | `dry_run` parameter on `run_pass`, gating the flush and the heartbeat. `test_W4R_M7_a_dry_run_leaves_the_state_dir_UNTOUCHED` (asserts the dir is empty); CONTROL `…_M7_CONTROL_a_wet_pass_writes_both` | fixed + regression green (round 2 pending) |
| M8 | MEDIUM | `bounded(start, session)` sat outside any try and raises `VendorMinutesError`, a `C3Error` sibling — so per C2 it killed the whole pass. `start = _session_back(anchor, 60)` against `MAX_WINDOW_SESSIONS = 180` means an ARMED-without-candidate C3 episode older than ~120 sessions makes every pass raise. Self-reinforcing: C3's arm expiry is internal to `run_c3`, and `apply_session_clocks` produces only CANDIDATE→RESOLVED, so the episode can never expire because the code that would expire it is the code that cannot run. Permanent wedge, exit 0, no payload | Clamp the window start to `MAX_WINDOW_SESSIONS - 1` back from today so a stale anchor DEGRADES rather than raises, and cover `VendorMinutesError` in the per-name catch (C2's fix). Ruling adds the tail the reviewer implied: the regression must assert the episode then **EXPIRES** on the clamped window rather than merely that the pass survives — a pass that survives forever while the episode wedges forever is the same bug one layer out | Clamp at `live_eval.py:1946-1950`; `VendorMinutesError` in `NAME_ERROR_REASONS`. `test_W4R_M8_a_130_session_old_C3_anchor_CLAMPS_instead_of_wedging_the_pass`; **replaced** `…_still_runs_the_S10_ARM_CLOCK` (called `fh.run_c3` directly — exercised neither clamp nor pass) with `…_the_clamped_window_RUNS_the_S10_ARM_CLOCK_through_the_pass`. **EXPIRY HALF (orchestrator ruling, round 2):** `apply_session_clocks` gains job (ii) — ARMED/TURNING with `candidate_at is None` → EXPIRED once `four_hour.C3_ARM_EXPIRY_SESSIONS` (IMPORTED, not re-minted — it rides `C3_SPEC` by value per W3-4) reference sessions have elapsed since `first_armed_at`'s session, stamped at the EXPIRING session's OPEN instant via the new `session_open_instant` (`run_c3`'s W3-2 convention), riding the SAME PendingDelta/spool path (`pass_id=entry_radar_pack`); `commit`'s existing terminal hook opens the §10 re-arm window unchanged. `live_ledger.py:1022-1077`. **DELETED** `…_OPEN_GAP_the_stale_LEDGER_episode_is_not_expired_by_the_replay`; its `health.dark[c3_incomplete_window]` assertion moved into the surviving clamp test. **5 new** (pit): `…_M8_a_130_session_old_LEDGER_episode_EXPIRES_and_the_unit_RE_ARMS` (parametrised ARMED/TURNING — wedge asserted first, stamp re-derived from `session_window_et`, re-arm earned, NEW episode address), `…_M8_the_clock_fires_at_EXACTLY_15_ELAPSED_SESSIONS_never_at_14` (0..14 ramp + the span is the spec's, imported), `…_M8_an_episode_that_HAS_a_candidate_is_NEVER_expired_by_this_path`, `…_M8_MUTATION_without_the_overlay_expiry_the_stale_episode_WEDGES` (CONTROL first; the co-committed CANDIDATE still RESOLVES, so the mutation is scoped), `…_M8_the_expiry_APPENDS_and_rewrites_no_history`. Mutation-proved three ways: disabling the branch reds 5 of 8 M8 ids, dropping the `candidate_at` clause reds the candidate test, stamping the CLOSE reds both parametrised ids | fixed + regression green |
| M9 | MEDIUM | `_gate_c2` returned no suppression flag and the C3 `_gate_arm` call discarded its own (`c3_run, _blocked = …`). Only `c1_blocked` reached `reasons`, so a C2- or C3-suppressed name carried no `suppressed_by_rearm` on its row and the evidence lived only in `payload["suppressed"]` — which per H3 was duplicated | `_gate_c2` returns `(run, blocked)`; OR all three flags into the row reasons | `_gate_c2` signature + the three-way OR at the reason site. `test_W4R_M9_the_row_reason_covers_the_C2_and_C3_gates_too` | fixed + regression green (round 2 pending) |
| M10 | MEDIUM | `_gate_c2` reconstructed `C2Run` field-by-field (`readings`, `episodes`, `events`, `fires`) instead of `dataclasses.replace`, silently dropping any field added later. `C2Run` is frozen+slots with all-init fields, so `replace` works — and `_gate_arm` already used it | `dataclasses.replace(run, episodes=…, events=…)`, matching `_gate_arm` | `replace` at the `_gate_c2` return. `test_W4R_M10_gate_c2_uses_replace_and_names_only_the_two_fields_it_changes` — an AST assertion, so a future hand-rolled reconstruction reds here rather than passing on today's field list | fixed + regression green (round 2 pending) |
| LOW (batch of 7) | LOW | (1) Module docstring named `PAYLOAD_KEY_ALLOWLIST`, a symbol that exists nowhere — the mechanism is `FORBIDDEN_KEY_TOKENS` + `FIREWALL_EXEMPT_KEYS`. (2) `ENTRY_RADAR_NO_PUBLISH` made `publish()` return `False`, so a documented rehearsal returned **3** ("produced no output"), reddening the timer on purpose; and the docstring's other NO_PUBLISH claim is honoured by `spool.NominationSpool._no_publish()`, not by anything in that file. (3) With no `ENTRY_RADAR_STATE_DIR` the backstop can only ever exit 5. (4) `stable_content` did not strip `health.inputs.c3_reader`, so a warm cache would flake any determinism assertion once C3 is enabled there. (5) `_reader_stats` injects `empty`, absent from the design §6 receipt shape. (6) `DURABLE_WRITES` was read by nothing, unlike both sibling scripts. (7) `if len(keep) == len(episodes): return run, False` discarded `blocked` — correct today only by accident of control flow | Fix all seven; regression tests only where behaviour (not prose) changed. On (5) the ruling extends the DOC rather than folding the key away: the enumeration was incomplete and the code is truthful. On (3) the plan doc also gains the backstop's operating caveats, since a lane that can now evaluate needs its cold-plane costs stated | (1) docstring corrected `live_eval.py:87`. (2) exit 0 with the rationale, `scripts/entry_radar_live.py:418-423`. (3) `ENTRY_RADAR_STATE_DIR` exported to `$RUNNER_TEMP` in `entry-radar-live.yml`, **6 new** `test_W4R_LOW_*` in the lane suite (export present, exported BEFORE both consumers, ONE shared path not two step-local copies, never a repo path, the header states the cold-start cost, and the resolution ladder driven for real). (4) `("health","inputs","c3_reader")` in `VOLATILE_PAYLOAD_PATHS` + `test_W4R_LOW_stable_content_ignores_the_c3_reader_counters`. (5) design §6 rewritten (below). (6) `durable_writes=` in the printed receipt. (7) `return run, blocked` + `test_W4R_LOW_gate_arm_returns_its_blocked_flag_on_every_path` | fixed + regression green (round 2 pending) |

## Round 1.5 — the non-vacuity audit, and what it changed

Reverting each fix in place and re-running is the only way to learn whether a regression is a receipt or a decoration. Five outcomes were not what the round-1 table claimed:

1. **M1's regression was vacuous, and provably so.** Setting `journaled=journal.observations_of(record)` back to `journaled=None` at the call site — i.e. restoring the exact O(intervals²) recomputation M1 reported — left **all 250 W4 tests green**. The existing test hands `journaled=` to the builder *in memory*, so it pins the builder API and never the pass wiring, which is the half the adjudication actually demanded ("the O(1) claim true across ONESHOT PROCESS boundaries"). Added `test_W4R_M1_the_PASS_ITSELF_reads_the_memo_off_the_journal`: two real passes over a shared state dir, with the control's frozen **observations** stripped from the journal file while its **points** are left intact — a fresh state dir would give the second pass a shorter tape and make the comparison run backwards. Under the reviewer's exact mutation it is now the only failure.
2. **C2's script half had no coverage at all.** No test in the repo called `scripts.entry_radar_live.run()` or `main()`, so "publishes the failed receipt and exits 6" — the entire point of the ruling — was asserted only by reading. Added `test_W4R_C2_the_SCRIPT_publishes_the_failed_receipt_and_exits_6` (drives the entrypoint, reads the published file off disk, checks the line-start annotation) with `…_CONTROL_a_clean_script_pass_publishes_and_does_NOT_exit_6` so it cannot pass on a script that always exits 6.
3. **C2's protected region still started too late — the finding was only half fixed.** `pack_daily_history`, `read_quote` and `basis_audit` all ran *outside* the per-name `try`, so a raise from any of the three still killed the pass for every other name. That is C2's own failure mode with a smaller blast radius, not a fixed one. The region now opens at the top of the loop body with `quote`/`audit` pre-bound so the handler can report how far the name got.
4. **`error_reason` mapped every `LiveEvalError` to `journal_refused`** — correct while the journal pin was the only one, and a lie the moment M2's off-diagonal tape-session guard raised the same base class: a builder bug would have sent an operator to the state dir to inspect a healthy file. Split out `JournalRefused(LiveEvalError)`, raised at the two journal sites; bare `LiveEvalError` now classifies as `evaluator_error`. Both rows are parametrised in the C2 regression, because the pairing is the property.
5. **M8's adjudicated premise was false, and the residual is an OPEN GAP.** The ruling said "run_c3's own expiry then fires on the clamped window — assert the episode EXPIRES rather than wedging". Measured through the real pass: the clamp works and the §10 clock does run, but it expires the episode the **replay itself minted**, never the pre-existing ledger record. `run_c3` is a stateless replay that counts §10 by position within the sessions it walks, and episode identity is `compute_episode_id(…, first_armed_at)` — so an episode armed 130 sessions ago, outside even the clamped window, can never be matched by any replay. With a gapped window nothing runs at all (C1's all-or-nothing rule), so the two rulings genuinely trade against each other. The old test called `fh.run_c3` directly on a hand-built window and would have passed with the wedge intact. Replaced with `test_W4R_M8_the_clamped_window_RUNS_the_S10_ARM_CLOCK_through_the_pass` (what is true) plus `test_W4R_M8_OPEN_GAP_the_stale_LEDGER_episode_is_not_expired_by_the_replay` (what is not, pinned so it cannot evaporate, and marked for deletion when closed). **Not fixed here**: expiring a ledger episode whose arm predates the window is calendar bookkeeping that belongs beside `apply_session_clocks`' CANDIDATE→RESOLVED, and moving a §10 lifecycle rule is an adjudication, not a wrapper fix. Consequence to know: such a record blocks its unit's re-arm (`live_episode_open`) indefinitely.

   **CLOSED in round 2** — the adjudication this finding asked for was taken, and the record above stands as written (it is what was true at round 1.5, not a mistake to rewrite). The orchestrator ruled that §10's expiry is frozen contract law and `run_c3`'s internal clock reaches only the episodes its own walk mints, so the overlay is the **second lawful enforcement point**: `apply_session_clocks` now transitions ARMED/TURNING → EXPIRED on the same reference-session calendar it already uses for CANDIDATE → RESOLVED, with the 15 imported from `four_hour.C3_ARM_EXPIRY_SESSIONS` rather than re-minted, the stamp at the expiring session's OPEN (matching W3-2), and no price, bar, or detector semantic touched. The OPEN-GAP test is deleted and five named regressions replace it (M8 row). The consequence sentence above no longer holds: an expired record opens its unit's re-arm window through `commit`'s existing terminal hook, which the parametrised regression drives all the way to a new arm.

Four further regressions were strengthened because a weaker variant of the fix passed them: **H4** (warmed one session, so a per-session invalidation — the variant the ruling rejected — passed byte for byte; now warms three and requires the whole ticker dropped), **M5** (both clauses of `_has_active_lane` fired at once on the end-to-end fixture; the `suppressed` clause now has its own unit-level test with no met condition anywhere), and the two LOW behaviour changes that shipped with no test at all — **NO_PUBLISH exiting 0** (with a control proving 3 stays reachable for a real sink failure) and the **`DURABLE_WRITES` printed receipt**.

**One honest limit, recorded rather than papered over.** H3's two halves are redundant by construction and cannot be discriminated from each other: `_refuse` is idempotent, `would_have_armed_at` is in the row identity (and an instant belongs to exactly one session), and the publisher caps at `[:1]`, so `mark`, the `session` key and the cap are each individually revertible with every assertion still green. The load-bearing guard is the ledger's idempotency, and that one *is* mutation-proved (reverting it reds three of the seven ledger tests). The evaluator-side belt is defensive, and the test says so.

## Documentation changes carried by this round

- **`W4_LIVE_EVALUATOR_DESIGN.md` §6** — the receipt shape was enumerated and had drifted from what ships. Now correct on five counts: seven health states (`proof_failed` was declared in `HEALTH_STATES` and missing from the doc), `pass.{evaluated_seq, prev_at}` (M6), the full ten-key `c3_reader` block with the pass-owned/`reader_*` split and *why* it is split (C1b), `basis.unchecked_n`, and the new `null_readings` block with the reason that makes `live` narrow (H1). Four short paragraphs follow the shape line explaining the four non-obvious ones — the enumeration is the contract, and a contract nobody can read is a contract nobody keeps.
- **`W4_DEPLOY_PLAN.md`** — new subsection "What the GitHub backstop can and cannot tell you", from Builder B's own flags plus LOW(3): the state plane is cold every run (so no §10 re-arm history and no warm bucket cache — a name the VPS would suppress can arm there); the `data/stocks` cone is the lane's heaviest checkout and a probe set outgrowing the committed store reads as a **quiet market rather than a broken checkout**, with `health.dark.no_substrate` named as the check; and cadence is not purchasable on GitHub, so the §15 cadence gate can only close on the VPS.

## Clean surfaces (attacked, no finding)

The reviewer explicitly cleared and recorded these so they are not re-derived: whole-cycle refusal ORDER (kill → window → pack-absent → pack-stale → proof_failed matches design §2 exactly, all five all-`unavailable` with zero transitions and zero spool, none touching the ledger); `ledger.apply_run` genuinely pure; spool-before-commit ordering, with `spool_then_commit` raising `SpoolReceiptRequired` rather than committing sink-less; the spooled receipt carrying the *pre-spool* health snapshot by construction (`_put` serialises immediately); `commit` partial-mutation unreachable from this caller (probe 10); the entire ledger API surface verified call-by-call, including `dataclasses.replace` validity on `C1Run`/`C3Run`; `now`-truncation in `observations()` prefix-only and correct; `_episode_path`'s journal replay strictly excluding today; `open_session`'s cross-pack pin and `replay`'s deliberate non-comparison of `pack_hash`; `c3_buckets` confirmation gating (only all-confirmed sessions cached or served, today's partial always re-derived); the **W4/W5 firewall** — `_c4_context`, `_nightly_lanes` and `_payload_row` emit no forward-knowledge shape, and `forward_knowledge_keys` subtracts exemptions by name as documented; `health["dark"]` producer coverage for all eight pre-existing `NAME_REFUSALS` entries; and `_c3_budget`'s determinism with the deferral named on the row rather than silent.

## Frozen-semantics record

No spec hash moved in this round. W4 is a live-evaluation wrapper over the unmodified W3 detectors, and every fix above is in the wrapper, the ledger, the reader, or the script:

```
C1_1D_LIVE_WASHOUT@1  f0bbd6cf3a6e2339   unchanged
C2_1D_TURN@1          d8ba60a25cfa7400   unchanged
C3_1D_4H_RECOVERY@1   d54dc1e55c4261c8   unchanged
C4_MTF_TURN@1         dce21ac680233ee2   unchanged
C5_BOTTOM_WATCH@1     13dec66345a0376c   unchanged
G0_GREY_DOT@1         9be89a8acc8b905c   unchanged
F1_FUSION             RESERVED — get_spec() raises NotYetSpecified (§4: F1 is not in V1)
```

Prophet firewall: the protected-path diff for this branch is byte-empty. Ledger law: `DURABLE_WRITES` is `()` and no `data/` path is written on any path in this round.
