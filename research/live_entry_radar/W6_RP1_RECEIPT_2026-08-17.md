# W6 RP1 — real-input proof (pre-merge)

**Status:** ACCRUING / RESEARCH PRIORITY. Not production commissioning.
**Policy:** `research/live_entry_radar/W6_RP1_POLICY.md` (`RP1`)
**Schema:** `mastermind.research_priority.v1`
**Pack:** W4 synthetic LivePack `pack_hash=10a4a650f5b20b36` (`tests.test_entry_radar_w4_pack.build`, as_of 2026-08-14, session 2026-08-17)
**Seam:** `live_eval._research_priority_board` (same function `_payload` calls)
**Frozen twice:** byte-identical JSON (`sort_keys=True`)

This is **not** a fitted ranking of W5 winners. W5 confirmatory results were known before RP1 was frozen and were not used to choose weights.

## What was run

1. **WASH recovery tape** (LIVE-10 shape) through production `IncrementalObservationBuilder` + `run_c1` + `run_c2`. C1 and five C2 variants armed. Rankable.
2. **VSHAPE** recovery observations with nightly `g0`/`c5` `condition_met=True` to exercise the live evaluator's nightly-lane seam. C1 did **not** fire on VSHAPE (K recovered). These rows prove G0 and C5 stay separate expert observations; they are **not** a Q5 earliness bonus.
3. **STALE** planted C1 episode on a name whose history is `stale` → unrankable `stale_observation`.
4. **SHORT** planted C1 episode on a name whose latest observation is `unavailable` → unrankable `unavailable_observation`.
5. **Quote `run_pass`** (`et_now(32)`, multiple 0.97): payload carries `research_priority` with `status=ACCRUING` and `population_n=0` (no developing episode; health `degraded` / `reading_null:5/5`). Honest empty board, not a low score.

SPY is not in the W4 pack, so `rs_60_vs_bench` is unavailable for every row (policy: never a probe-set median).

## Rankable board

`population_n=8`. Competition ordinal. Same-ticker variants share **name-level** measures (latest observation + confirmed daily), so they tie. That is RP1, not a collapse: six WASH rows remain six `(ticker, detector, variant)` identities.

| ordinal | ticker | detector | variant | lifecycle | priority_index | freshness |
|---:|---|---|---|---|---:|---|
| 1 | VSHAPE | C5_BOTTOM_WATCH@1 | — | CANDIDATE | 71 | obs `provisional` / history `confirmed` |
| 1 | VSHAPE | G0_GREY_DOT@1 | — | CANDIDATE | 71 | obs `provisional` / history `confirmed` |
| 3 | WASH | C1_1D_LIVE_WASHOUT@1 | — | CANDIDATE | 43 | obs `provisional` / history `confirmed` |
| 3 | WASH | C2_1D_TURN@1 | c2a_kd_cross | CANDIDATE | 43 | same |
| 3 | WASH | C2_1D_TURN@1 | c2b_k_slope | CANDIDATE | 43 | same |
| 3 | WASH | C2_1D_TURN@1 | c2d_hist_trough | CANDIDATE | 43 | same |
| 3 | WASH | C2_1D_TURN@1 | c2e_hist_curvature | CANDIDATE | 43 | same |
| 3 | WASH | C2_1D_TURN@1 | c2f_rebound_atr | CANDIDATE | 43 | same |

VSHAPE leads WASH because its confirmed `ret_*` / `proximity_120` and live `hist` / `k_minus_d` percentile higher in this two-name set. G0 and C5 on VSHAPE are identical because they read the same name-level measures — **no expert-family bonus**.

## Decomposition (WASH C1)

`status=ACCRUING` `policy_version=RP1` `known_at=2026-08-17T20:00:00Z`

| dimension | index | inputs | unavailable |
|---|---:|---|---|
| structural_quality | 36 | ret_20=-0.251145, ret_60=-0.355751, ret_120=-0.490378, proximity_120=0.509665 | — |
| reset_quality | 50 | structure_intact=0, pulled_back=1 | — |
| resilience_quality | 50 | oscillator_reset_intact=0 | rs_60_vs_bench |
| recovery_quality | 36 | rebound_atr=11.348677, hist=1.344486, k_minus_d=22.222222 | — |

`priority_index` = round(mean(36, 50, 50, 36)) = **43**.

Evidence refs include pack hash `10a4a650f5b20b36`, substrate fingerprint `83535991dcbc41dd`, and the C1 event id.

## Unrankable / null cases

| ticker | abstention | priority_index | ordinal | why |
|---|---|---|---|---|
| STALE | `stale_observation` | null | null | latest availability `stale`; not a 17/100 |
| SHORT | `unavailable_observation` | null | null | latest availability `unavailable`; missing ≠ 0 |
| quote `run_pass` board | (empty episodes) | — | — | no developing episode this snapshot |

## Firewalls checked in-suite

Determinism, input-order, ticker permutation, outcome injection (MFE/MAE/win rate/Q5 13.43), hotness, lobe count, C4/depth, G0 Q5 bonus, lifecycle independence, multi-expert, stale, unavailable, probability language, no LLM imports, six detector spec hashes unchanged:

`G0 9be89a8acc8b905c` · `C1 f0bbd6cf3a6e2339` · `C2 d8ba60a25cfa7400` · `C3 d54dc1e55c4261c8` · `C4 dce21ac680233ee2` · `C5 13dec66345a0376c`

## Prophet

`git diff origin/main -- engine/entry_signal.py engine/prophet_*.py` is empty.

## What this is not

Not W7. Not live-payload commissioning on the VPS. Not a claim that VSHAPE is a better trade than WASH. Sol reviews the PR against the frozen outcome/authority firewall before W6 can be marked done.
