# Healthcare Theme Intelligence Implementation Plan — consolidated R13 review candidate

> **For agentic workers:** Use `superpowers:subagent-driven-development` or `superpowers:executing-plans` only after the relevant review, live assignment and source-custody gates clear. Checkboxes below specify future work; none records execution.

**Goal:** Let the investor explain a Healthcare development, its economic transmission, evidenced participants, counterevidence and next useful observation through the existing Themes/GLP-1 journey, then expand through the twelve-family Healthcare research model.

**Architecture:** Repair the incumbent FDA source/consumer. Consume the existing shared GMI/K1/F04/Research Vault/rights/auth/publication infrastructure; add Healthcare meaning through the separately gated version-aware native contract and one closed Healthcare profile in the common transport/client. Products and contracts remain source-scoped descriptions unless their existing identity owner supplies an actual bridge.

**Tech Stack:** Existing admitted Python environment, JSON Schema, pandas/pyarrow, pytest, FastAPI, Jinja2 and vanilla JavaScript. No dependency-version changes solely to execute this plan.

**Spec:** `HEALTHCARE_SOURCE_INTEGRATION_DESIGN_R7_2026-09-23.md`, `HEALTHCARE_SHARED_ADMISSION_REVIEW_R8_2026-09-23.md`, and `HEALTHCARE_R12_SHARED_EXTENSION_SPEC_2026-09-24.md`. Operative shared decisions are #7780/5808854275 and #7870/5809358801. Their selected semantics are integrated below; the earlier R9/R10/R11 prose is historical explanation, not a conflicting execution recipe.

**Status:** REVIEW_PENDING. No independent Healthcare-plan approval, native v1.1 enrollment, Healthcare build START, live admission, merge, deployment or browser acceptance is asserted. This is an in-place consolidation of the existing plan, not a second operative plan. The R9 filename is retained for stable links. Original R9 bytes remain at commit `9b06712b56450b38f952d4bc30bfcd2dcfb91e0f`, blob `f58970d627a6334905bd42fa5994a25a2b00956a`; historical R9/R10 audits must read that immutable revision rather than this moving file.

## Chairman-selected execution boundary

The Chairman will manually initiate a **NEW Healthcare-specific Fable CEO session** from the later manual startup prompt and full packet. The existing Semiconductor Fable on #7870 is a shared-foundation dependency/compatibility counterpart, **NOT the Healthcare build receiver**. Do not dispatch, wake, reassign or ask that session to absorb Healthcare implementation. The new session has not been selected or started by this plan. No manual startup prompt is being issued as a live assignment by this file.

The research principal owns the domain research and planning until the explicit handoff. The future new Healthcare Fable owns Healthcare delivery orchestration, integration judgment, review adjudication and proof, using the admitted least-scarce capable engineering/test/review lanes. It must not delegate an open-ended instruction to rediscover Healthcare. Shared module ownership does not move merely because a new sector principal is started. A future live manual assignment authorizes only its stated scope and never overrides an incumbent source writer or effect-unknown operation.

## Exact source basis and precedence

Research pickup is Macro #7788 at `257d0d9a48d8b043572e78dce0098d529d5823e0`. Protected procedure was loaded from Mastermind `294b4c00ed668b497edb834be8108f14bc1bee8a`, Skillpack 1.0.1/bootstrap 1. At execution, repin current compatible procedure and read only material changes.

Shared #7870 source was observed at `b256aa6a756afa3308a534f1ec259ff0074d5154`, still draft/unmerged. Its changed-file census contains the native assertion/rights modules, K1 subtype, and `engine/market_ontology/semiconductor_theme_research.py` with its Semiconductor schema. That census did not establish an installed common API/client/private reader or Healthcare profile. Source presence is not production proof.

#7870/5809602368 acknowledges R12's versioned extension direction without objection and preserves Semiconductor B. The same return reports an original-source resolver and a guarded basket mount seam being developed. It does not enroll Healthcare or transfer the shared writer. #7669/5808986207 is a reported custody-ruling pointer to re-read before relying on the mount; this plan does not assert that the include has landed. #7462's store seam requires current reconciliation. Old PR numbers are navigation, not permanent locks or proof of current release.

Within this plan, the current Chairman boundary and operative R4/R12 decisions supersede conflicting historical recipes. A newer accepted shared implementation may resolve a named dependency; record the exact interface mapping once rather than authoring the old proposed alternative. Never silently change the user outcome, weaken privacy or reinterpret an earlier immutable record.

## Global Constraints

- Preserve `glp1_obesity`, graph identity `theme:glp1_obesity`, basket `obesity_glp1`, `state_of_themes.html` and `basket/obesity_glp1.html`. Resolve each in its own owner; a string prefix is not a security-identity receipt. Change no members or weights.
- First-release research is descriptive. No target price, underpricing claim, expected-return probability, stock rank/selection, entry gate, position size, alert delivery or trade action follows from this work.
- All native authority flags remain literal false. Keep source observations, derived calculations, reviewed synthesis and forward claims distinct.
- Keep publication grain, business applicability, analyst observation, native retention, review and admission clocks distinct. Unknown timezone or historical knowledge stays unknown.
- Preserve existing v1 input bytes, encoded outputs, revisions and source references. No auto-upgrade, re-mint, default unknown version or Healthcare hash/resolver fork.
- Candidate `theme_graph.curation_assertion.v1.1` is a branch of the same native owner and exported module; it is not enrolled by this plan. Signed/exact/non-numerical measurement is shared with other sectors, not a Healthcare utility copied into production.
- Source-only businesses and contracts can be useful without a verified stock link. Required security composition refuses without an owner-produced bridge; optional stock enrichment is omitted.
- Current detailed protected assertions and dependent narrative never enter public Git, Parquet, HTML/JSON, asset bundles, mirrors or persistent browser storage. House authorship and upstream display permission do not make the product public.
- Use the existing Research Vault private store under GMI ownership. The selected prefix is `theme_graph_private/v1/assertions/<curation_revision>.json`; registration, real reader, actual private configuration and writer proof remain required. No new bucket, global data-root change, foreign Earnings/RIO identity or parallel publication selector.
- Common routes are POST `/api/themes/v1/research/query` and POST `/api/themes/v1/research/evidence`. Preserve existing accepted Semiconductor behavior; unsupported Healthcare is an explicit refusal, never another sector's payload with a new title.
- `require_user` then `enforce_site_full(always=True)` precedes protected reads. Success and errors retain private/no-store, appropriate Vary for the actual credential carrier, nosniff and noindex/noarchive. Preserve Authorization variation; add Cookie where the accepted route actually admits cookies. No claim that one header alone is an access boundary.
- Retain the existing configured positive-only entitlement-store-outage grace, capped at 86400 seconds from the last successful positive observation. Do not raise smaller values; failed refresh does not renew grace. Source-rights withdrawal, fresh negative entitlement, existing invalidation and authentication failure remain independent refusals under their actual contracts.
- Source families come from admitted original-source lineage, not caller-supplied snapshots or empty lists. Missing required rights refuse the affected body/conclusion. Do not classify issuer-authored exhibits as government-authored merely because EDGAR hosts them.
- Corrections use existing immutable predecessor/history semantics. Permission withdrawal, a changed disclosure, revised analysis and a changed business contract are distinct events. Required-input invalidation suppresses the dependent current conclusion, not only the citation.
- Read/modify/write fencing and same-carrier reconciliation remain mandatory. A lost response never authorizes a new key, branch, receiver or blind retry. Preserve unaffected sectors; omitted input is not withdrawal.
- Every release requires a real consumer and scoped evidence. Infrastructure-only completion, source-code review as Healthcare-plan approval, and local arithmetic checks as application acceptance are prohibited.

## Review Focus

1. Required-source corrections or access changes must invalidate only the dependent conclusion, not leave unsupported text or remove unrelated-sector facts (T04/T07).
2. A non-numerical contract must remain useful without a manufactured zero/rate, while signed economic changes remain distinct from negative physical counts (T03/T06).
3. A nominally shared endpoint must not return Semiconductor fields for Healthcare, guess security identity or accept an unsafe citation URL (T05/T06).
4. Complete empty, partial download, stale observation and old source generation must remain distinguishable on the actual FDA chip (T01/T02).
5. A new Healthcare principal must not steal the shared writer, wait for unrelated future releases, or mistake plan checks for the first-release user proof (T08).

## Release and ownership map

| Release | Tasks | Useful outcome | Build responsibility and prerequisite |
|---|---|---|---|
| D1 | T01, T02 | Truthful incumbent supply observation and visible chip | Future dedicated Healthcare Fable coordinates the bounded repair after D1 plan review, actual assignment, source permission and feed custody. Independent of v1.1/private-profile readiness. |
| D2 | T03, T04, T05 | One real admitted Healthcare assertion reaches the existing protected page through the shared system | Shared owner supplies or accepts the one contract/private/transport extension; new Healthcare Fable consumes it and owns Healthcare integration. Actual shared custody, rights and private binding must clear. |
| D3 | T06 | Complete GLP-1 five-part economic explanation | D2; D1 wherever misleading legacy output is in the same journey; admitted minimal evidence and independent content review. |
| D4 | T07 | Real correction and a non-metabolic mechanism through the same system, with honest breadth navigation | D3 and a separately qualified second evidence package; no universal sector-completion claim. |
| Adjudication | T08 at each release | Exact-head independent review plus authorized real-path proof | Evidence denominator is release-specific; later unexecuted cases are never called passes or used to administratively block D1. |

D1 proof cases are the T01/T02 semantic cases plus the applicable T08 release, rights, no-trade and source-to-chip checks. D2 adds native/private/K1/profile cases. D3 adds the full investor-task cases. D4 adds correction/non-metabolic/coverage. A case spanning tasks is partly exercised at the earlier release only when its relevant behavior exists; the untested remainder stays unexecuted. No single large all-future-test command is the D1 release gate.

### Shared dependency acceptance record — consume, do not fabricate

Before T03–T05 implementation, the original shared owner or its lawfully assigned successor supplies: exact accepted source commit and owned paths; explicit v1/v1.1 discriminator and supported payload; native mint/reader signatures; registered private binding and classification; actual original-source family resolver; request/response profile and generation semantics; common client/mount contract; source-writer/collision disposition; independent review; and remaining runtime proof. These facts belong to the existing PR/Agent OS record, not a new receipt service. No adapter is synthesized from this checklist. Missing private-runtime proof can hold live admission while authorized synthetic source work proceeds.

### File responsibilities

| Path | Responsibility | Custody boundary |
|---|---|---|
| `collectors/fda_shortages.py` | Complete sweep and existing-cache observations | Healthcare D1 writer after reconciliation |
| `engine/fda_scarcity.py`, `engine/foresight_cascade.py`, `scripts/build_foresight.py` | Incumbent interpretation/visible consumer | D1 only; no schedule or trade-policy expansion |
| `tests/test_foresight_cascade.py`, proposed `tests/test_fda_shortages_generation.py` | Source meaning, acquisition and forward-history tests | D1 test scope |
| `engine/theme_graph/curation_assertion.py`, `contracts/theme_graph/curation_assertion.v1.schema.json` | Existing shared v1 functions/schema | Reuse; do not weaken or re-author in Healthcare |
| Proposed `contracts/theme_graph/curation_assertion.v1.1.schema.json` | Closed shared extension | Single assigned shared writer, not the active Semiconductor B release by default |
| `contracts/theme_graph/evidence.v1.schema.json`, `engine/theme_graph/store.py`, `scripts/check_theme_graph_contracts.py` | Native envelope and round-trip | Existing owner and current store custody |
| `engine/theme_graph/admission.py`, `engine/theme_graph/rights.py`, `config/theme_sources.yml` | Admission and byte-bound source rights | Consume actual shared source; source-specific approved amendment only |
| `engine/research_vault/r2_store.py`, `config/r2_delivery_plane_classification.v1.json` | Existing private primitives/registration | No primitive rewrite or global setting change merely for Healthcare |
| `contracts/evidence_foundation/vocabulary.v1.json`, `tests/test_evidence_foundation_contract.py` | Native identity/clock/read binding | Existing K1 owner |
| `engine/market_ontology/semiconductor_theme_research.py`, its schema and tests | Existing Semiconductor profile | Regression dependency, not a Healthcare template to rename |
| Proposed `engine/market_ontology/healthcare_theme_research.py`, `contracts/market_ontology/healthcare_theme_research.v1.schema.json` | Pure Healthcare profile within existing F04 composition | Candidate domain-specific files; shared route/client stay single |
| Shared route/client paths returned in dependency acceptance record | Common entitled transport and rendering | Never create the retired R9 alternatives alongside actual shared source |
| `templates/basket_detail.html.j2`, reported `templates/_basket_intelligence_mounts.html.j2` | Single guarded shared mount seam | Consume actual accepted seam; confirm #7669 ruling/landed blobs |
| Proposed `templates/_healthcare_theme_research.html.j2` | Data-free Healthcare shell partial | Only when the shared mount contract admits it; no current facts in HTML |
| Proposed `tests/test_healthcare_theme_research.py`, `tests/test_healthcare_theme_research_corrections.py`, `tests/test_healthcare_research_api.py` | Domain, correction and shared-profile integration | Tests bind the actual accepted interfaces, not a mock second API |

Proposed names are explicit candidate contracts, not claims they already exist. If the shared owner has an accepted equivalent, map that exact path/signature in this plan once before writing and delete the superseded alternative from the change list. This is a compatibility amendment, not permission to weaken an outcome.

## Common synthetic test conventions

All examples below use invented parties, products, numbers and dates. They are not company forecasts. Full candidate envelopes are generated by the original standard-library research helper `verify_r12_semantics.py` under `candidate_native_payloads`; those envelopes remain held and unstamped. Copy approved examples into test fixtures only after the version contract is accepted. A research helper must not become a production validator or source-admission service.

Native tests mint via existing `encode_assertion`, decode via `decode_assertion`, and compare equivalent stages. Local source bytes and their actual digest are created together. A fixture-only review or retention object must be rejected by live admission. Test fixtures for the actual shared reader/API are adapters to the accepted runtime, not replacements for its auth, rights or lifecycle. An unavailable dependency is an explicit test block, not a fallback fake implementation.

## Task T01 — Correct supply meaning in the incumbent visible consumer

**Files:** `engine/fda_scarcity.py`, `engine/foresight_cascade.py`, `tests/test_foresight_cascade.py`.

**Consumes:** source rows and T02's qualified-capture evidence. **Candidate interface:** `summarize_supply(rows, *, capture, now, max_capture_age) -> dict`, behind existing `compute_fda_scarcity` and `format_theme_feed_chip` compatibility entry points. **Produces:** per-row regulator status and manufacturer availability; source/capture freshness; descriptive aggregate; unknown coverage. No economic tone or shortage-derived rank.

- [ ] **Step 1 — Add the intended regression, not the old defect as an expected result.**

```python
from datetime import datetime, timedelta, timezone
from engine.fda_scarcity import summarize_supply

def test_current_available_does_not_become_resolved():
    now = datetime(2026, 9, 23, 12, tzinfo=timezone.utc)
    capture = {"qualified": True, "finished_at": now.isoformat(),
               "source_generation": "2026-09-23", "atomic_snapshot_proven": False}
    rows = [{"generic_name": "Synthetic A", "status": "Current",
             "availability": "Available", "package_ndc": "TEST-A"}]
    out = summarize_supply(rows, capture=capture, now=now,
                           max_capture_age=timedelta(days=2))
    assert out["source_status"] == "CURRENT_REPORTED"
    assert out["rows"][0]["manufacturer_availability"] == "Available"
    assert "glut" not in out["label"].casefold()
```

Add table cases: current+resolved retains mixture; discontinued remains distinct; unknown status remains observed-but-unclassified; qualified empty means no matching records; unqualified capture is unavailable. A mixture containing a known current row and unknown rows exposes both, not a complete national verdict. Fresh fetch with old/unknown source generation cannot label the source fresh. Exercise the existing chip as well as the pure summary.

- [ ] **Step 2 — Establish semantic red:** `python -m pytest tests/test_foresight_cascade.py -q`. Record the assertion/interface failure and distinguish environment/import failures from a discriminating regression.
- [ ] **Step 3 — Implement the bounded interpretation:** normalize regulator status separately from availability; preserve raw scope; produce `CURRENT_REPORTED`, `RESOLVED_REPORTED`, `DISCONTINUATION_REPORTED`, `MIXED_REPORTED`, `UNCLASSIFIED`, `NO_MATCHING_RECORDS` or `UNAVAILABLE` with separate freshness/coverage. Do not infer prices, supplier capacity or profit from these labels. Update any affected legacy machine-band consumer deliberately; no unreviewed enum-to-score coercion.
- [ ] **Step 4 — Run green and unchanged cascade controls:** use the same test command; render mixed, stale and unknown chips. Compare all unaffected recommendation/entry outputs to the owner baseline. T02 supplies the qualified real capture before D1 release.
- [ ] **Step 5 — Commit the owned source and tests:** `git add engine/fda_scarcity.py engine/foresight_cascade.py tests/test_foresight_cascade.py && git commit -m "fix(healthcare): preserve scoped FDA observations"`. Commit is not release.

## Task T02 — Qualify acquisition and forward history in the existing FDA collector

**Files:** `collectors/fda_shortages.py`, `scripts/build_foresight.py`, new `tests/test_fda_shortages_generation.py`. Existing artifact remains `data/fda/shortages.parquet`; do not publish live cache bytes without the source owner's permission.

**Candidate interfaces:** `collect_shortage_sweep(fetch_page, *, clock, page_size, max_pages) -> dict`; `save_shortage_observation(result, *, path, expected_predecessor) -> dict`; `read_shortage_observation(*, path) -> dict`. The predecessor argument binds the existing writer's receipt/fencing, not a new lock manager. Existing `fetch_shortages` and `load_shortages_cache` wrappers preserve compatible callers over the selected qualified observation.

**Result fields:** `qualified`, `rows`, `capture`, `failure_code`. Capture includes interval, source generation, raw/unique/reported counts, completeness and `atomic_snapshot_proven`. New generation rows and the bounded existing-artifact metadata retain current selection and last refresh; successful zero rows still have a capture receipt. Legacy rows have unknown capture time unless genuinely established.

- [ ] **Step 1 — Write late-page and stale-writer tests.**

```python
from datetime import datetime, timezone
from collectors.fda_shortages import collect_shortage_sweep

def test_late_page_failure_is_not_a_current_sweep():
    def fetch_page(skip, limit):
        if skip:
            raise OSError("synthetic second page failure")
        return {"meta": {"last_updated": "2026-09-23", "results": {"total": 2}},
                "results": [{"package_ndc": "TEST-A", "generic_name": "Synthetic A",
                             "status": "Current"}]}
    result = collect_shortage_sweep(fetch_page,
        clock=lambda: datetime(2026, 9, 23, 12, tzinfo=timezone.utc),
        page_size=1, max_pages=2)
    assert result["qualified"] is False
    assert result["failure_code"] == "PAGE_FAILED"
```

Add complete single page, complete empty, first-page outage, generation/count drift, repeated page, malformed rows, cap before total, absent stable key, changed status, disappeared row, legacy cache and failed metadata-write cases. In an interleaving test, older sweep A starts, newer B becomes current, then A finishes: A must not replace B or its last-refresh evidence. Atomic rename alone does not prove multi-writer fencing.

- [ ] **Step 2 — Establish red:** `python -m pytest tests/test_fda_shortages_generation.py -q`.
- [ ] **Step 3 — Implement the existing-owner transaction:** validate every page and acquisition bound before promotion; keep the previous qualified capture on failure with original dates; retain only a truthfully persisted failure receipt. Stage then atomically replace under the existing writer fence and verified predecessor. Without an admitted serialization/fencing mechanism, refuse concurrent promotion rather than inventing one. Matching counts describe a complete interval sweep, not snapshot isolation. A content-scoped identity cannot establish cross-generation absence. Disappearance never means resolution by itself. Retention growth stays subject to existing artifact/retention limits; no unbounded new history service.
- [ ] **Step 4 — Exercise D1's closed proof set:** run T01+T02 tests, incumbent wrappers and actual allowed collector-to-chip path. Record interval, counts, source-generation age, selected capture, failed-refresh behavior and exact visible output. Real-source acquisition and synthetic fault injection are different receipts.
- [ ] **Step 5 — Commit and seek D1 release review:** `git add collectors/fda_shortages.py scripts/build_foresight.py tests/test_fda_shortages_generation.py && git commit -m "fix(healthcare): qualify FDA sweeps and preserve observed history"`. D1 does not wait for v1.1, the Healthcare private profile or later-sector cases. It still needs its own review, source rights, current integration and authorized source-to-chip proof.

## Task T03 — Consume one version-aware shared contract and enroll Healthcare meaning

**Owner:** the single admitted shared-contract writer after explicit custody/assignment, coordinated as a dependency by the NEW Healthcare Fable. Do not expand Semiconductor B or create a competing implementation carrier merely because Healthcare needs an extension.

**Files:** existing `engine/theme_graph/curation_assertion.py`; unchanged v1 schema; proposed v1.1 schema; applicable native evidence/guard/K1 integration and `tests/test_theme_graph_curation_assertion.py` plus proposed `tests/test_healthcare_assertion_contract.py`.

**Consumes/produces:** the same exported `validate_assertion(payload, *, allow_unstamped=False)`, `curation_revision(payload)`, `encode_assertion(payload)`, `decode_assertion(value)` and `source_ref_for(payload)`. No new exported hash or mint. Explicit `schema` selects the branch. Unknown schemas refuse. Canonical content excludes only `curation_revision`; no vertical prefix or extra field stripping. Native v1.1 admission waits for persistence/reader support.

- [ ] **Step 1 — Freeze native legacy golden inputs and write extension tests.** Obtain actual v1 Robotics, source-only, Semiconductor industrial-context, null evidence cell and correction fixtures from the accepted shared module. Record their input bytes, encoded bytes, revisions and references before the extension. Use full R12 candidate envelopes for the proposed new cases.

```python
import copy
from engine.theme_graph.curation_assertion import (
    encode_assertion, decode_assertion, curation_revision, source_ref_for)

def test_mint_compares_equivalent_stages(candidate):
    # candidate is a complete approved synthetic v1.1 fixture, initially unstamped.
    before = copy.deepcopy(candidate)
    stamped = decode_assertion(encode_assertion(candidate))
    assert candidate == before
    assert stamped["curation_revision"] == curation_revision(candidate)
    assert encode_assertion(stamped) == encode_assertion(candidate)
    assert source_ref_for(stamped) == source_ref_for(candidate)
```

Add real retained-byte changes at same URL/date, source locator changes, stale stamp, wrong predicate/body, unknown keys/versions, malformed JSON/duplicate keys, size/depth bounds, boolean/nonfinite numbers, source-only identity, unknown timezone and correction tests. Native legacy goldens must remain byte-identical after the extension; a symbolic string test is insufficient.

- [ ] **Step 2 — Run red against the actual shared module:** execute its existing suite and `python -m pytest tests/test_healthcare_assertion_contract.py -q`. The research oracle is not the native implementation and cannot supply the PASS.
- [ ] **Step 3 — Implement only the reviewed closed extension:** add the explicit version branch under the same module. Non-numerical `observation` has `kind=non_numeric`, a state in `not_disclosed|qualitative|not_applicable|source_missing`, and source text; prohibit all number/currency/range fields in that branch. Numeric observations use bounded decimal strings (at most 30 integer and 12 fractional digits, no exponent/plus/whitespace), preserve source lexical precision and currency scale 0/3/6/9, and separate point from ordered interval with inclusivity. `physical_count` is a non-negative integer; physical quantity is non-negative; signed financial amount/change and applicable ratios preserve sign. A count decline is not a negative count. Rates/ratios/unit prices identify their denominator. Gross/net is only gross/net/null. Provenance is reported/derived/target, separate from disclosure. A royalty percentage branch is 0–100; other arrangements are held rather than forced into it.

`REPORTED_ECONOMIC_RIGHT` alone carries `economic_right`: source-scoped parties/direction; product/use/territory; arrangement state; separately named royalty/profit/upfront/milestone/service/cost components; payment denominators/tier method; deductions/costs/loss participation; triggers/dates; source limitations. `REPORTED_SUPPLY_STATUS` alone carries `supply_status`: jurisdiction/product/presentation, source identity strength, separate regulator status/availability, effective/source/capture clocks, completeness and unknowns. Neither creates a global product master. Preserve the existing authority flags, source/review envelope and backward compatibility; no enum coercion of the old R9 fixture.

- [ ] **Step 4 — Run native branch, decode, storage/K1 compatibility and golden tests:** reject mixed-version or unstamped stored rows; maintain null/absent legacy cells and exact prior IDs. Native digest/review claims require real local fixture receipts; a syntactically valid envelope is not live admission.
- [ ] **Step 5 — Integrate once under the shared owner:** commit only its admitted paths with independent contract review and actual v1 legacy comparison. Record accepted schema/functions/reader support in the dependency record. No schema-only D2 completion and no new Healthcare database or hash implementation.

## Task T04 — Consume the existing private publication and current-rights binding

**Files:** existing shared adapter returned by #7870 or its lawful successor; `engine/theme_graph/admission.py`, `engine/theme_graph/rights.py`, native store seam; reviewed `config/theme_sources.yml` and delivery-classification row. Consume `engine/research_vault/r2_store.py` primitives. Do not implement the retired R9 `prepare_generation`/selector alternative.

**Interfaces:** actual native adapter/writer/read functions named by the shared acceptance record; existing `load_registry_snapshot(path=None)` and `assert_current_emission_allowed(families, *, snapshot)`. The new original-source resolver maps admitted source route/publisher/representation to the existing registry; unavailable mapping refuses, never returns an empty permitting lineage. Its actual signature and permissions must be recorded before integration tests are bound. Naming a future adapter is not a binding receipt.

- [ ] **Step 1 — Write storage and effective-policy tests against the accepted adapter.** The test reader is the actual admitted accessor, spied only at its storage boundary. Stage immutable synthetic bytes, verify readback, then make them visible through the incumbent selection. Test identical-byte no-op, identity conflict, wrong bytes/length, oversized/deep payload, wrong source digest/review, duplicate public/private native identity, incomplete candidate, missing required lineage and omission of an unrelated-sector record. Exact-predecessor conflicts refuse; injected lost write responses require same-key readback before any retry.

```python
from engine.theme_graph.rights import load_registry_snapshot, assert_current_emission_allowed

def test_warm_policy_reads_new_bytes(tmp_path):
    import pytest
    from engine.theme_graph.rights import RightsRefusal
    p = tmp_path / "policy.yml"
    p.write_text("families:\n  synthetic:\n    rights_class: direct_display_ok\n", encoding="utf-8")
    allowed = load_registry_snapshot(p)
    assert_current_emission_allowed(["synthetic"], snapshot=allowed)
    p.write_text("families:\n  synthetic:\n    rights_class: internal_only\n", encoding="utf-8")
    denied = load_registry_snapshot(p)
    assert denied[0] != allowed[0]
    with pytest.raises(RightsRefusal):
        assert_current_emission_allowed(["synthetic"], snapshot=denied)
```

This primitive check is only a control. The actual route must also prove grant arrival, revocation, bounded policy reads, trusted complete lineage, pre-response change refusal and refusal before protected body reads. Do not manually clear caches to make a warm-process test pass.

- [ ] **Step 2 — Establish the missing behavior on the actual shared tests:** run `tests/test_theme_research_rights_refresh.py` and `tests/test_theme_research_private_binding.py` where present on the accepted source, plus the private-adapter suite returned in its record. Do not create a second adapter to satisfy an absent dependency.
- [ ] **Step 3 — Integrate the reviewed existing-owner binding:** register the selected prefix, retain canonical assertion identity, write/verify immutable objects before advertising references and use the incumbent fencing/selection. Preserve all unaffected sectors; current house product bodies remain private. Historical retention respects current source restrictions. Obtain effective policy from trusted deployed bytes before protected reads; check again at emission and refuse a mid-request revision change. No caller-selected source permissions or automatic public publication from `direct_display_ok`.
- [ ] **Step 4 — Qualify privacy without exposing real material first:** the authorized existing storage owner writes a harmless nonce, proves authenticated positive access to its known-existing key, tests applicable anonymous/custom-domain routes to that same key, and records actual bucket/domain controls. Only then may remaining gates admit a real protected source. Repeat exact-object positive/negative proof without publishing secret keys, private locators or body text. A nonexistent-key 404 or LocalStore result is not that proof. Record actual cache/grace/invalidation configuration under the R4 conditions; source-rights veto never receives entitlement grace.
- [ ] **Step 5 — Return the exact binding and scope:** independent review, source commit, actual adapter/registration/policy, tested routes/configuration, outcomes and residual holds. These results contribute to D2; they do not alone complete the investor workflow. Any shared implementation changes remain on the original lawful writer, not a new Healthcare publisher.

## Task T05 — Enroll a typed Healthcare profile through the common protected path

**Files:** shared router/client and mount paths from the accepted dependency record; proposed `engine/market_ontology/healthcare_theme_research.py` and `contracts/market_ontology/healthcare_theme_research.v1.schema.json`; existing K1 binding/tests; proposed `tests/test_healthcare_research_api.py`; data-free Healthcare partial only through the accepted shared seam.

**Candidate domain profile:** request discriminator `profile="healthcare"`; response schema `healthcare_theme_research.v1`. These are review candidates for additive registration in the existing closed router/profile mapping, not claims of callable endpoints. Exact accepted shared request fields/generation/cursor behavior must be bound in the dependency record. Preserve the Semiconductor profile byte/semantic contract. No arbitrary module imports, dynamic plugin registry or renamed Semiconductor object.

**Candidate pure domain interface:** `compose_healthcare_profile(*, theme_id, primary_basket_id, evidence_blocks, synthesis, coverage, identity_receipts, context) -> dict`. Context supplies the actual shared generation/policy/reference clocks; the profile creates no independent generation hash. Evidence and synthesis arrive from admitted owners. The function does no network, source writes, price lookup, security guessing or runtime language-model inference.

Required domain fields are schema, profile, theme identity, primary basket identity, the shared generation identity, evidence blocks, the five synthesis sections, coverage, limitations/degradations, and all-false research authority. Envelope naming/pagination/security headers follow the accepted common contract. No private storage keys or source-body dumps cross the wire. Public citation URLs use only approved HTTP(S) locations after scheme/host/control-character validation and existing safe-link policy.

- [ ] **Step 1 — Add actual route-before-read, typed-profile and UI tests.** Exercise both shared POST routes with an actual accepted synthetic request factory, not a parallel test-only endpoint.

```python
from fastapi.testclient import TestClient

def test_unauthorized_healthcare_request_does_not_read_private(mounted_app, valid_request, read_spy):
    # Fixtures bind the existing router/auth and its accepted request shape.
    body = dict(valid_request, profile="healthcare")
    r = TestClient(mounted_app).post("/api/themes/v1/research/query", json=body)
    assert r.status_code == 401
    assert read_spy.calls == 0
    assert r.headers["cache-control"] == "private, no-store"
```

Bind `mounted_app` to the actual shared router, `valid_request` to its closed accepted request and `read_spy` to the true private reader; do not stub out the auth/entitlement decision. Cover unknown profile, valid signed-in request, missing required identity bridge, unsupported evidence version, clock loss, stale generation/cursor, policy change before emission, oversized required evidence, 401/403/404/503, invalid route/body and unsafe citation URLs. Confirm unsupported Healthcare cannot return a Semiconductor response or present another theme's data.

- [ ] **Step 2 — Run red:** `python -m pytest tests/test_healthcare_research_api.py tests/test_evidence_foundation_contract.py -q`, plus the actual shared client/UI suite. A missing shared dependency is a held gate, not permission to invent it.
- [ ] **Step 3 — Enroll the closed profile once:** keep existing auth/entitlement before reads and source policy as a separate check. Bind one accepted generation, preserve K1's true physical reader and all native clocks, then recheck policy before serialization. Malformed required input refuses; optional source-only participants remain explicitly unlinked. The shared client uses the existing session owner and generation/AbortController protection. Logout/session change invalidates the request token before DOM clearing; late results never restore protected text. Render source text with textContent and validate citation links separately. No persistent browser payload cache.

The reported common mount seam is a guarded include of `templates/_basket_intelligence_mounts.html.j2` outside `#app`; consume it only after its actual accepted blob and #7669 disposition are reconciled. Healthcare's partial is data-free, uses existing tokens/navigation and does not replace the page header. Missing optional partials do not break other sectors. Preserve the existing Theme Tracker route and back-navigation.

- [ ] **Step 4 — Prove D2's real source-to-page path:** one allowed real Healthcare assertion passes native admission, actual private storage/reader, entitled API and the existing page. Test anonymous/expired/direct-route/error states, exact applicable public mirrors and logout race; include EN/ZH, light/dark, desktop 1440/mobile 390, keyboard use and long labels. Unknown upstream data is visible, not fabricated. Screen capture alone cannot prove source identity; retain the linked native/source receipts.
- [ ] **Step 5 — Complete D2 review with exact accepted interfaces:** shared implementation and Healthcare profile source ownership stay separate. No infrastructure-only success, mocked private-store acceptance or global no-leak claim from a handful of probes.

## Task T06 — Deliver the complete Healthcare economic explanation

**Files:** admitted Healthcare profile/composer/schema under the common owner, reviewed protected synthesis content, proposed `tests/test_healthcare_theme_research.py`, existing GLP-1 mount. Do not place current synthesis in public profile configuration.

**Consumes:** the minimal refreshed/qualified Lilly-Chugai economic-right and operating evidence identified in R7/R2, appropriate supplier/capacity counterevidence from R3, actual native versioned assertions, current policy and optional verified security bridges. Prior research figures are dated leads, not automatically fresh production inputs. **Produces:** ordered `change`, `mechanism`, `participants`, `counterevidence`, `next_observation`, exact dependencies and explicit limits.

- [ ] **Step 1 — Add domain and user-task regressions:** an unbound licensor is attributed but has no security/valuation/performance/portfolio enrichment; net-sales royalty is not profit share or manufacturing; qualitative tiers do not become a midpoint; costs/losses require disclosed participation; contingent payments remain contingent; product/territory scope never spreads to all products/countries; company-wide pricing is not a drug-level measure. Preserve repeated filing/release provenance without inventing independent confirmation. Current operating evidence without consensus can be described, but underpricing/target/expected return is unavailable.

```python
from engine.market_ontology.healthcare_theme_research import compose_healthcare_profile

def test_unbound_licensor_remains_useful_without_stock_guess(profile_inputs):
    # Complete synthetic profile inputs use Alpha/Beta and empty owner bridge receipts.
    out = compose_healthcare_profile(**profile_inputs)
    party = next(x for x in out["synthesis"]["participants"] if x["source_label"] == "Alpha")
    assert party["security_id"] is None
    assert party["stock_url"] is None
    assert party["valuation"] is None
    assert party["economic_basis"] == "royalty_on_net_sales"
```

- [ ] **Step 2 — Run red:** `python -m pytest tests/test_healthcare_theme_research.py -q` against the actual profile implementation, not the R12 research oracle.
- [ ] **Step 3 — Implement deterministic eligibility and reviewed synthesis:** required dependencies are explicit native IDs from the content review. Any unavailable, corrected, withdrawn, stale or rights-refused required assertion disables that dependent current conclusion. Eligible independent facts remain with their limitations. Never keep the old paragraph and remove only its citation. The initial narrative explains how treatment activity, realized pricing, cost, retained rights and capacity relief can diverge; it does not proclaim a winner or a bottleneck from one observation. Money passing through multiple participants is not multiple independent markets.
- [ ] **Step 4 — Prove the user job:** a reviewer who did not author the paragraph opens the existing journey and identifies the change, mechanism, participant, strongest counterargument and next discriminating observation without being supplied the answers. Record misunderstanding and repair copy/model mapping. Verify old FDA chip coherence under D1, unchanged members/weights/ranks/entry outputs, safe source URLs and late-response behavior.
- [ ] **Step 5 — Accept D3 only with the actual linked proof:** source capture/admission/version, rights, native read, visible explanation, independent user-task result and exact implementation head. An attractive evidence drawer or raw source list is not completion.

## Task T07 — Prove corrections and a distinct Healthcare business model

**Files:** existing shared correction/eligibility mechanism and Healthcare profile; proposed `tests/test_healthcare_theme_research_corrections.py`; existing evidence location. No new correction store or lifecycle.

**Candidate pure profile function:** `reconcile_healthcare_dependencies(*, current_assertions, prior_synthesis) -> dict`, under the Healthcare composer only when the common owner does not already expose the equivalent. It calculates eligibility and invalidating refs; it does not mint a second correction identity or mutate state. If the common equivalent exists, bind tests directly to it and omit this helper.

- [ ] **Step 1 — Add correction, access and preservation tests:** factual source correction, revised analysis, contract amendment and source-rights withdrawal must remain different causes. Corrected evidence invalidates dependent current synthesis; lawful history preserves the predecessor but cannot bypass current access rights. Adding Healthcare must preserve unchanged Robotics/Semiconductor records and a same-byte repeat must be a reconciled no-op.

```python
from engine.market_ontology.healthcare_theme_research import reconcile_healthcare_dependencies

def test_required_source_withdrawal_does_not_leave_current_conclusion():
    result = reconcile_healthcare_dependencies(
        current_assertions={"fixture-evidence-a": {"state": "withdrawn"}},
        prior_synthesis={"required_assertion_ids": ["fixture-evidence-a"]})
    assert result["current_conclusion_eligible"] is False
    assert result["invalidating_refs"] == ["fixture-evidence-a"]
```

The evidence selector is an explicitly synthetic test label, not a valid production ID or owner bridge. Native acceptance uses actual generated IDs and receipts.

- [ ] **Step 2 — Run red:** `python -m pytest tests/test_healthcare_theme_research_corrections.py -q`, plus the actual common correction/history suite.
- [ ] **Step 3 — Add the non-metabolic contrast using existing researched material:** qualify a provider/procedure case from R4/R5, preserving total versus same-facility revenue/cases, case mix versus price, period scope and noncontrolling interests. Do not substitute another drug royalty as the only contrast. Render all twelve research families with selective/unproductized depth; these are navigation labels, not new theme/basket IDs. No family becomes complete from one example.
- [ ] **Step 4 — Exercise real changed-input proof:** an actual amended/revised source reaches the visible profile, together with controlled synthetic withdrawal/error cases. No real correction available means the correction-specific real proof stays pending; an ordinary new observation is not relabeled a correction. Preserve unaffected profile/sector output and source chronology.
- [ ] **Step 5 — Accept D4 only for the proven scope:** record real versus synthetic changes, semantic review, native/source lineage and visible outcomes. The full Healthcare universe, current valuation layer and historical investment evaluation remain incomplete.

## Task T08 — Review, release and report under the new Healthcare principal

**Owner:** NEW Healthcare-specific Fable only after the Chairman's later manual delivery; independent reviewers remain separate from its builders. This plan gives no existing Fable a new task assignment. **Files:** existing implementation PR and Agent OS continuation/evidence homes; no new acceptance service.

- [ ] **Step 1 — Freeze each release candidate and exact evidence scope:** read current compatible source law, actual head/base/path custody and pending effects. Reconcile any already-started operation on its original carrier. Use one necessary Healthcare implementation carrier from then-current main after collision checks; never build by merging the research branch into production or duplicating #7870's shared work. Bind all Appendix A cases to their original IDs and record PASS/FAIL/BLOCKED/NOT_EXECUTED only with real evidence. Candidate/interface changes invalidate only affected evidence, not unrelated historical research.
- [ ] **Step 2 — Run dependency-closed tests:** D1 runs T01/T02 plus its relevant integration/no-trade/review/proof cases; D2 adds actual native/private/K1/profile suites; D3 adds domain/user-task; D4 adds correction/coverage/non-metabolic. Record actual accepted filenames and commands; missing required suites are blockers, not successful skips. Preserve signed/exact/non-numerical, source rights, known-object privacy and legacy v1 golden cases. Local tests, hosted CI, merge and browser proof remain distinct.
- [ ] **Step 3 — Obtain genuinely independent review:** consume any existing exact-package verdict with explicit scope/independence; do not infer it from a username, request or review of shared code. A later Fable can arrange bounded independent implementation review through its admitted fabric without redoing foundational Healthcare research. Resolve blockers on the original source owner; do not hide them under design approval. This author-side consolidated plan has no independent verdict yet.
- [ ] **Step 4 — Release only with actual authority and real-path evidence:** known source -> permitted retention/native admission -> private reader and current policy -> entitled common transport -> existing-page outcome; test intended error/logout/public-mirror boundaries. Record effective deployment and source/policy generation. CI green alone is insufficient; no automated merge or hidden credential ceremony is authorized here.
- [ ] **Step 5 — Return to the Chairman and durable owners:** report exact capability achieved, scope still held, independent review and proof references, unchanged trade authority, effects and next bounded action. Do not send the Healthcare program to the Semiconductor Fable as a substitute owner. The later manual startup prompt/packet is delivered to the Chairman; no automatic wake or background execution is created by a final message.

## Required source qualification for the first production slice

Retain the full original research corpus as decision support, but admit only the minimal source set needed for the first user task. For each source the existing owner records publisher/authorship, locator, observation/publication grain, allowed acquisition and retention, original-input family/representation, display versus redistribution, actual retained digest, correction handling, and the claims supported. Missing materiality or consensus never becomes a guessed input. Do not reopen a generic company census merely to postpone admission.

A new EDGAR registry row is not proof that every possible representation has the same rights. The SEC Website Dissemination statement expressly permits copying/distribution without the SEC's permission and asks for attribution. USAGov warns that government sites can contain protected privately authored material; the government-work definition depends on authorship and duties, not the hosting domain. Preserve both points. Qualify factual inputs/original analysis, excerpts, entire expressive documents and third-party attachments separately through the existing source-rights owner; do not impose a blanket ban on filings or a blanket full-body permission. This is a product-source qualification rule, not an adjudication of every document's legal status.

Primary documents inspected 2026-09-24: SEC Privacy Information, Website Dissemination (`https://www.sec.gov/about/privacy-information`); USAGov government copyright (`https://www.usa.gov/government-copyright`, page updated 2026-04-30); U.S. Copyright Office Title 17 chapter 1, sections 101/105 (`https://www.copyright.gov/title17/92chap1.html`). The existing Consumer finding is #7870/5809660585; coordinate its actual resolution rather than create another registry/request. No source license, registration or permission was changed in this research turn.

## Appendix A — preserved application acceptance requirements

All 60 IDs, original task assignments and required-result texts below are preserved from the R11 review matrix derived from R8/R9 and R10's canonical table. `NOT_EXECUTED` remains the product state. Clarifications above bind current interfaces, versioning, ownership and release consequences without claiming tests passed. Original scenario text and source provenance remain in the unchanged canonical R8/R9 records and R10 amendment. The table is a plan view of those requirements, not a new acceptance authority.

| ID | Original task assignment | Required result | Execution |
|---|---|---|---|
| R8-A01 | T06 | Preserve glp1_obesity and obesity_glp1 separately; no basket mutation. | NOT_EXECUTED |
| R8-A02 | T07 | Show selective depth and unresolved data; mint no new canonical theme or basket. | NOT_EXECUTED |
| R8-A03 | T06 | Retain source attribution; omit valuation/performance/portfolio linkage. | NOT_EXECUTED |
| R8-A04 | T05 | Refuse required security composition; a fixture ID never proves resolution. | NOT_EXECUTED |
| R8-A05 | T06 | Preserve parties, scope and denominator; do not label profit-sharing or manufacturing. | NOT_EXECUTED |
| R8-A06 | T06 | Keep numeric rate and EPS sensitivity unavailable; never substitute a midpoint. | NOT_EXECUTED |
| R8-A07 | T06 | Do not infer a production contract or exclusive supply relationship. | NOT_EXECUTED |
| R8-A08 | T06 | Keep trigger and effective period; do not accrue an unconditional rate. | NOT_EXECUTED |
| R8-A09 | T06 | Do not attribute the group pricing effect to an individual drug without evidence. | NOT_EXECUTED |
| R8-A10 | T03 | Preserve all clocks and their grain; do not backdate system knowledge. | NOT_EXECUTED |
| R8-A11 | T03 | Different immutable revision and explicit successor relationship; no old-ID overwrite. | NOT_EXECUTED |
| R8-A12 | T07 | Identify interpretation change separately from amended business reality. | NOT_EXECUTED |
| R8-A13 | T07 | Suppress the dependent current conclusion; retain lawful history without a rights bypass. | NOT_EXECUTED |
| R8-A14 | T03 | Preserve the literal/grain and unknown timezone; no guessed UTC conversion. | NOT_EXECUTED |
| R8-A15 | T05 | Do not present them as a compiled security-subject recipe unless the bridge is consumed. | NOT_EXECUTED |
| R8-A16 | T05 | Refuse/degrade that subtype; legacy evidence success does not qualify the richer body. | NOT_EXECUTED |
| R8-A17 | T06 | No automatic spread to all formulations or countries. | NOT_EXECUTED |
| R8-A18 | T01 | Retain Current and Available as different observations. | NOT_EXECUTED |
| R8-A19 | T01 | Preserve mixed evidence; never emit a theme-wide resolution/all-clear. | NOT_EXECUTED |
| R8-A20 | T01 | Use distinct machine semantics; never treat discontinuation as resolved supply. | NOT_EXECUTED |
| R8-A21 | T01 | Show observed-but-unclassified; do not report no matching records. | NOT_EXECUTED |
| R8-A22 | T02 | Do not promote partial data as the new qualified observation; expose failed refresh. | NOT_EXECUTED |
| R8-A23 | T02 | Distinguish complete empty observation from failed acquisition and stale cache. | NOT_EXECUTED |
| R8-A24 | T02 | Record the acquisition window/consistency checks; do not claim atomic snapshot proof. | NOT_EXECUTED |
| R8-A25 | T02 | Retain prior history and record current absence basis, not stale present-tense membership. | NOT_EXECUTED |
| R8-A26 | T02 | Mark historical coverage unavailable; begin truthful forward retention, not hindsight backfill. | NOT_EXECUTED |
| R8-A27 | T02 | Show its age and failed refresh; never replace source time with render time. | NOT_EXECUTED |
| R8-A28 | T04 | Apply the effective new revision within the accepted refresh contract; do not trust path cache. | NOT_EXECUTED |
| R8-A29 | T04 | Suppress dependent body; do not use legacy house basket rights as a blanket grant. | NOT_EXECUTED |
| R8-A30 | T05 | Entitlement denial precedes private body access; no credential or object-key leakage. | NOT_EXECUTED |
| R8-A31 | T05 | Release fails even if the main API is paywalled; use an independently approved safe shell. | NOT_EXECUTED |
| R8-A32 | T06 | Do not repopulate cleared/stale UI; no persistent private browser storage. | NOT_EXECUTED |
| R8-A33 | T05 | Preserve private-no-store and safe errors; storage details remain server-side. | NOT_EXECUTED |
| R8-A34 | T04 | Refuse the generation; any eligible older display must be explicit, not silent substitution. | NOT_EXECUTED |
| R8-A35 | T04 | Reconcile conflict on the same owner; no unconditional overwrite or parallel publisher. | NOT_EXECUTED |
| R8-A36 | T04 | Keep effect unknown; read exact key/predecessor before retry or another effect. | NOT_EXECUTED |
| R8-A37 | T04 | Enforce actual field/use scope; neither full-source laundering nor unsupported blanket approval. | NOT_EXECUTED |
| R8-A38 | T06 | Explain the operating change; withhold underpricing, expected-return and target-price claims. | NOT_EXECUTED |
| R8-A39 | T06 | Reader can state change, mechanism, participant, counterevidence and next observation. | NOT_EXECUTED |
| R8-A40 | T06 | Fail coherence until the incumbent output is repaired or explicitly superseded; preserve old valid controls. | NOT_EXECUTED |
| R8-A41 | T06 | Preserve two sources without automatically counting two independent confirmations. | NOT_EXECUTED |
| R9-A01 | T04 | Refuse conflicting identity; never pick an arbitrary authoritative copy. | NOT_EXECUTED |
| R9-A02 | T02 | Keep atomic_snapshot_proven=false and disclose the acquisition interval. | NOT_EXECUTED |
| R9-A03 | T05 | Abort response safely; do not mix the admitted and effective policy revisions. | NOT_EXECUTED |
| R9-A04 | T07 | Suppress/review the dependent current conclusion; preserve lawful predecessor history. | NOT_EXECUTED |
| R9-A05 | T03 | Schema fixtures do not establish actual owner resolution; live admission/enrichment refuses. | NOT_EXECUTED |
| R9-A06 | T05 | Return explicit unavailable/degraded scope; never silently truncate the required argument. | NOT_EXECUTED |
| R9-A07 | T01 | Retain both freshness dimensions; fresh acquisition does not prove fresh underlying evidence. | NOT_EXECUTED |
| R10-A01 | T03 | Native shared hash and source-ref identity are used without a vertical prefix or field-stripping fork. | NOT_EXECUTED |
| R10-A02 | T03 | Unstamped candidate, minted record and stale-stamp rejection are tested at equivalent stages. | NOT_EXECUTED |
| R10-A03 | T03 | Qualitative royalties use the accepted closed non-numerical branch; no unknown-to-zero or enum coercion. | NOT_EXECUTED |
| R10-A04 | T03/T06 | Signed financial changes survive the one shared measurement extension while negative physical counts remain refused. | NOT_EXECUTED |
| R10-A05 | T04/T05 | Actual policy bytes, complete source-family lineage, bounded reads and warm request behavior are verified at the shared route. | NOT_EXECUTED |
| R10-A06 | T04/T07 | A publication/correction retains unrelated verticals and treats identical already-current input as a reconciled no-op. | NOT_EXECUTED |
| R10-A07 | T02 | Existing writer fencing prevents an older FDA sweep or refresh outcome from overwriting a newer qualified generation. | NOT_EXECUTED |
| R10-A08 | T04/T05 | Upstream display permission cannot publish protected house-authored current content; approved entitlement behavior is separate. | NOT_EXECUTED |
| R10-A09 | T04/T08 | Privacy proof uses an exact known-existing object and actual bucket/domain controls, not a meaningless absent-key response. | NOT_EXECUTED |
| R10-A10 | T05/T06 | Unsafe citation URLs refuse; protected current narrative cannot enter public profile/configuration or persistent browser caches. | NOT_EXECUTED |
| R10-A11 | T08 | Each early release runs its dependency-closed proof set; future unexecuted tests neither falsely pass nor administratively block D1. | NOT_EXECUTED |
| R10-A12 | T03/T05 | Healthcare uses #7870's accepted shared module and common transport; no rival hash, private selector, route or client is added. | NOT_EXECUTED |

## Appendix B — full-sector coverage and investment roadmap

The following are research-family navigation and coverage obligations, not newly minted canonical themes or investable baskets. All retain `SELECTIVE_NOT_COMPLETE` until their specific consumer and evidence justify deeper coverage.

| Family ID | Research family | First distinctive question to deepen |
|---|---|---|
| R8-F01 | Therapeutic demand and disease markets | Which disease/patient/use context changes commercially accessible demand, rather than a headline patient count? |
| R8-F02 | Therapeutic technology platforms | Which modality/mechanism/asset has which evidence, rights, costs and substitutability? |
| R8-F03 | Diagnostics and precision medicine | How do test eligibility, clinical utility, reimbursement and laboratory workflows convert to paid use? |
| R8-F04 | Life-science tools | Is growth equipment installation, subsequent consumption, service, or inventory movement? |
| R8-F05 | Development and manufacturing services | Is the company qualified, contracted, utilized and cash-generative for the relevant process? |
| R8-F06 | Devices and interventional care | How do placement, procedure mix, use, disposables and provider economics differ? |
| R8-F07 | Healthcare software and AI | Which external customer and comparable workflow demonstrate retained productivity gains? |
| R8-F08 | Providers and care settings | Is change due to cases, acuity/mix, reimbursement, acquisitions, staffing or ownership share? |
| R8-F09 | Payers and access administration | Who bears claims risk, on which service period and risk-sharing arrangement? |
| R8-F10 | Distribution and healthcare infrastructure | How do pass-through drug values, retained fees, capital and cash collection interact? |
| R8-F11 | Cross-sector dependencies | Which specific input or economic relationship bridges sectors without double-counting demand? |
| R8-F12 | Adjacent animal-health lens | How do species, customer and payment differences change the model relative to human care? |


After D1–D4, expand by distinct economic mechanism: advanced therapeutics/diagnostics with clinical-population and analysis-version discipline; tools/manufacturing/devices with qualified involvement, use and cash; providers/payers/distribution/software/animal health with contract risk and comparable populations. All planned views should serve a task: sector/subtheme navigation, participant exposure, clinical/access/catalyst timeline, economic-right map, capacity/usage, and what-benefits-or-loses scenario exploration. A graph is not an investment thesis by itself. Healthcare overlaps Robotics/Semiconductors through evidence relationships without duplicating their physical BOM research.

Valuation layer V1 requires actual issuer/security bridges, contemporaneous price/consensus, financing/share counts and lawful historical retention. Without them no undervaluation, expected-return or target-price claim. Evaluation V2 prospectively records dated hypotheses, counterevidence and falsifiers through existing owners, with preregistered evaluation including losers, delistings, revisions, missingness, timing and realistic costs. Neither layer grants trade authority by implication.

The goal remains a maintained connection from scientific and operating change to retained company economics and changed expectations, with honest uncertainty and corrections. Source counts, artifact counts, long turns and schema passes are not the success metric.

## Consolidation and review boundary

This revision supersedes the old R9 task recipes in place: native v1 hash/mint coercions; the unapproved parallel private selector; the GET route; the old fixture; the all-future-suites D1 gate; and ambiguous Fable receiver wording. R10–R12 historical dossiers and all prior factual research remain unchanged. The sixty requirement texts retain their original wording and assignments; all prior local checks keep their historical scope.

The current plan is one review candidate. Shared extension/native profile acceptance, independent Healthcare plan review, later manual startup, actual source custody, production qualification and release remain separate. The later manual handoff will give the Chairman a startup prompt, this operative plan, research index, exact source revision and a compact open-gate list for a NEW Healthcare-specific Fable session. No handoff is sent to the incumbent Semiconductor Fable. No implementation is started by saving this file.
