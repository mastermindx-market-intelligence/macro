R-T01R2-01: T01 semantics accepted at 604a3755 (status taxonomy, seven-status chip, bilingual `t(label, label_zh)`, display-only tier, banned substrings absent) are NOT reopened — only the seven ruled repairs below; do not refactor `_label` into a framework, do not rename fields the template or the frozen probes read.
R-T01R2-02 (= T01R-B1): a visible string may never contain `None`, `nan`, `null` or an empty date; the no-generation variant is its own authored sentence in both languages.
R-T01R2-03 (= T01R-B2): capture qualification and refresh outcome are TWO facts — `qualified` describes the selected capture (from `selected_capture`), `refresh_failed` describes the last attempt (from `last_refresh`); `_UNAVAILABLE` is only for "no qualified capture on file"; a retained qualified capture keeps its evidence-derived status, band, tone and counts and discloses the failed refresh as a dated suffix.
R-T01R2-04 (= T01R-M2): the source generation is the label's freshness anchor; the acquisition clock is a secondary token and may never be the only one; an unparseable or missing generation on a qualified capture is DISCLOSED (`source generation unknown`), never silently dropped.
R-T01R2-05 (= T01R-M1, REJECTED — R-T01-12 stands): `max_capture_age` is optional and the production caller passes `None`, so `freshness.stale` is `None` there by design; a threshold would be an invented age limit. The freshness facts the chip states are the dated source generation (R-T01R2-04) and the acquisition clock as a secondary token. Do not add a budget constant.
R-T01R2-06 (= T01R-m2): resolved+discontinued mixtures render both counts; "resolved" alone is a claim of resolution the evidence does not support (discontinuation ≠ resolution).
R-T01R2-07 (= T01R-m4): `rationale` feeds `title=` and is an English-only contract enforced in code (ASCII assertion), because the CI rule forbids translated text in title attributes.
R-T01R2-08: every new test names the truthful rendered string; a test that only asserts `is not None` or `!= ""` is a defect; no real molecule names in new rows.
R-T01R2-09: commit and push after every numbered step; keep each exec under 12 minutes.
