R-T02R2-01: the previous round's step ladder (RED/GREEN/mutants/clean venv) is already accepted for the sweep/save/read semantics at ab640a6a; do NOT re-run or re-litigate it. This round is closure-only plus the receipt formatter.
R-T02R2-02: "widen paths" is NOT the fix here — the job may not declare collectors/_drip.py, collectors/edgar*.py, collectors/china_*.py or any path reached only through scripts.build_foresight; the fix is to stop importing the builder from the suite.
R-T02R2-03: the receipt line format is `fda_shortages: observation qualified=<bool> failure_code=<code|none> source_generation=<date|unknown> last_refresh=<iso|none> legacy=<bool> inconsistent=<bool>`.
R-T02R2-04: commit and push after step (2) and again after step (3); keep each exec under 15 minutes — a single long exec is how the previous round timed out.
R-T02R2-05 (= red-team T02-B1, `collectors/fda_shortages.py:~319`): `absent_since_generation` is stamped ONCE — the first complete sweep from which a key is absent — and never re-stamped on later sweeps; a key that reappears clears it (and gets `last_observed_generation`). Age = number of DISTINCT source generations observed since that stamp (keep a small ordered list of observed generations in the sidecar under `history_coverage.generations_seen`, capped at the last 120) — after 90 distinct generations the absent row is dropped and counted.
R-T02R2-06 (= T02-B2, `:~360-362`): retention drops ONLY rows whose `absent_since_generation` is set; a row present in the current sweep can never be dropped. A probe constructs 95 absent rows + one fresh present row and requires the present row to survive.
R-T02R2-07 (= T02-B3, `:261` vs `:268`): `read_shortage_observation` computes the parquet digest from the FILE BYTES and compares to the sidecar's `parquet_sha256` BEFORE calling `pd.read_parquet`; a torn/truncated parquet (digest mismatch or unreadable) returns `inconsistent=True, rows=None, capture=None` — it never raises. `load_shortages_cache` returns None on `inconsistent`. Wrap the read in `try/except Exception` as the last line of defence and never let ArrowInvalid escape into the build.
R-T02R2-08 (= T02-B4, `:~423`): a failed refresh (unqualified result, or any save that does not promote) writes ONLY `last_refresh`; `selected_capture`, `history_coverage`, `retention` and `predecessor` fields are copied through byte-for-byte from the existing sidecar even when the pair is inconsistent — the next QUALIFIED sweep is the only thing that repairs an inconsistent pair.
R-T02R2-09 (= T02-B5, `:163-180`, `:471-475`): every upstream field is type-checked in `_parse_record`/the sweep: `package_ndc`, `generic_name`, `status`, `availability`, `initial_posting_date`, `update_date` must be `str` (or None/missing → ""), lists/dicts/numbers → `MALFORMED_ROW` (sweep unqualified), never a TypeError; the sweep call in `fetch_shortages` sits INSIDE the wrapper's try/except so nothing raises into `scripts/build_foresight.py`.
R-T02R2-10 (= T02-M1, `:83`): `_parse_record` takes an explicit `fetched_utc` argument supplied by `collect_shortage_sweep` from its `clock()` start call (so rows of one sweep share one stamp and replays are deterministic); the legacy `_parse_record(rec)` one-argument form stays callable (default = the start clock passed in by the sweep; direct callers outside the sweep may pass `datetime.now(timezone.utc)` themselves).
R-T02R2-11 (= T02-M2, `:267-269`): a sidecar whose `parquet_sha256` is None AND no parquet on disk is a COLD START, not an inconsistency: `read_shortage_observation` returns `rows=None, capture=None, legacy=False, inconsistent=False` with `last_refresh` populated.
R-T02R2-12 (= T02-N1, `:~428`): `retention.dropped_absent_rows` is a cumulative counter read from the sidecar's `retention` block (not from `history_coverage`) and survives failed refreshes unchanged.
