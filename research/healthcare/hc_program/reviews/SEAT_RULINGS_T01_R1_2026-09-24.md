R-T01-01 (probe file is law): the six T01 probes in the frozen `tests/test_fda_supply_probes.py` (R8-A18, A19, A20, A21, R9-A07, R10-A11) define acceptance; read the file FIRST; where this packet and the probe file disagree, the probe file wins and you record the disagreement under DEVIATIONS.
R-T01-02 (chip visibility keys on source_status, not band): `format_theme_feed_chip` returns `None` ONLY when `scarcity_row is None`. For every `source_status` — including DISCONTINUATION_REPORTED, UNCLASSIFIED, NO_MATCHING_RECORDS and UNAVAILABLE, all of which map to legacy `band == "NONE"` — the chip renders with its honest label. The legacy `band` is kept solely for compatibility (CURRENT/MIXED → SHORTAGE_ACTIVE, RESOLVED → SHORTAGE_RESOLVED, all else → NONE, per the probes) and no longer decides visibility. Seat ratification: an observed discontinuation must stay visible (it was visible on main); silencing it would be an information loss.
R-T01-03 (template contract): `templates/foresight.html.j2` line ~581 renders `tfs.tone`, `tfs.rationale` (as `title=`), `tfs.label`. Keep `tone` (values: reuse the existing tone vocabulary; a neutral/mute tone for NO_MATCHING_RECORDS/UNAVAILABLE/UNCLASSIFIED, the existing active tone for CURRENT/MIXED, the existing resolved tone for RESOLVED, and a neutral tone for DISCONTINUATION) and `rationale` (plain-word source determination, EN only — `title=` attributes carry no translated text by CI law). Do not edit the template.
R-T01-04 (no economic wording anywhere): `label`, `label_zh`, `rationale`, `details`, log lines and any cascade `rationale` text derived from the chip may not contain the substrings "glut", "tell" (bans "intelligence"/"telling" too), "all-clear" or "catching up" — the probes substring-match case-insensitively on the chip AND the seat will grep the cascade row rationale.
R-T01-05 (two freshness dimensions): `freshness.stale` keys off CAPTURE age (`capture_age_s > max_capture_age`); `freshness.source_generation_age_days` is reported separately (None when generation unknown); the label may say "fresh" only when BOTH the capture is not stale AND the source generation is known and ≤ max_capture_age; a fresh capture with an old/unknown generation must not say "fresh" (R9-A07).
R-T01-06 (legacy/bare-DataFrame path): `compute_fda_scarcity(df)` with a DataFrame argument treats `capture=None`: rows are summarized, `freshness.capture_qualified is None`, `stale is None`, label carries "capture time unknown". `compute_fda_scarcity()` with no argument reads via `collectors.fda_shortages.read_shortage_observation(path=_shortages_path())` and passes the real capture (+ `last_refresh` merged as `freshness.failed_refresh` when `last_refresh["qualified"] is False`).
R-T01-07 (stage invariance is a hard gate): the FDA chip stays display-only. Quote identical `stage`/`entry` outputs for the same non-FDA inputs under CURRENT_REPORTED vs RESOLVED_REPORTED chips (the probe R10-A11 checks this) and do not touch `_stage`, `GLUT_BANDS` or any band input in `engine/foresight_cascade.py`.
R-T01-08 (bilingual parity): every EN label has a `label_zh` in plain Chinese with the same information (status, count, generation); no untranslated stats or internal state names; the seat's red-team reviews EN/ZH parity because no probe pins it.
R-T01-09 (existing tests): existing tests in `tests/test_foresight_cascade.py` that assert the old "glut tell" wording or the discontinued→SHORTAGE_RESOLVED mapping are updated to the new semantics with a one-line comment citing the plan case ID; do not delete a test — rewrite its expectation.
R-T01-10 (= review R-D1-02/F-D1-02, composition follows configuration, not acquisition): Tier P/W must not move with the FDA source's state. `compute_fda_scarcity` returns a row (never `None`) for EVERY theme in `MOLECULE_THEME_MAP` — an absent/inconsistent/unqualified observation yields `source_status = "UNAVAILABLE"` with the reason in `freshness.failed_refresh` — and, because the chip now renders for every status (R-T01-02), `_compute_tier` (unchanged) keeps configured themes on the physical desk regardless of outage. Add a test: cache absent → `glp1_obesity` chip is UNAVAILABLE and the cascade row's `tier` equals the tier obtained with a CURRENT_REPORTED chip.
R-T01-11 (= R-D1-03/F-D1-03, capture channel through the unchanged signature): `compute_fda_scarcity(df)` reads `df.attrs.get("fda_observation")` (set by the T02 wrappers, R-T02-17) as its capture channel; a frame without that attr is capture-unknown by construction: statuses are still derived from the rows, but `freshness.capture_qualified is None`, `stale is None`, the label ends with "capture time unknown" and `tone` is neutral. `compute_fda_scarcity()` with no argument calls `read_shortage_observation` directly. A frame whose attrs say `failed_refresh: True` or `inconsistent: True` yields UNAVAILABLE-with-retained-rows exactly like an unqualified capture.
R-T01-12 (= R-D1-05/F-D1-04, no invented age limit): `max_capture_age: timedelta | None = None`. With `None` (the production caller), `freshness.stale is None` and the chip states the facts only ("captured 3 d ago · generation 2026-09-20"); with a caller-supplied threshold (tests, the plan's example) `stale` is computed as ruled in R-T01-05. Never hard-code a days constant in production code.
R-T01-13 (= R-D1-11/F-D1-08, coverage is independent of status): `coverage.matched_rows` and `coverage.unclassified_rows` are computed from ALL matched rows before any status decision; one Current row plus one unrecognized row yields `source_status = "CURRENT_REPORTED"` AND `coverage.unclassified_rows = 1`, and the label renders the remainder ("FDA shortage: current (1) · 1 record unclassified"); the same for MIXED/RESOLVED/DISCONTINUATION with unclassified remainders. `molecules_with_rows` counts every molecule with any matched row, not only band-driving ones.
R-T01-14 (= R-D1-12/F-D1-09, ban scope + rendered HTML): the banned substrings are "glut", "tell", "all-clear", "catching up", "demand exceeds supply", "supply constraint lifted"; they may not appear in `label`, `label_zh`, `rationale`, `details`, log lines, or any cascade `rationale` fragment derived from the chip. Assert over the chip dict for every status AND over rendered HTML: render `templates/foresight.html.j2` through the existing template test harness in `tests/` (grep `foresight.html.j2` under tests/ for the fixture used; if none exists, render the template with a minimal context in a new test and record the harness gap under GAPS).
R-T01-15 (= R-D1-13/F-D1-10, ZH consumer): edit `templates/foresight.html.j2` line ~581 ONLY: render the visible label as `t(tfs.label, tfs.label_zh)` (the same helper the sibling chips on that row use); `title=` keeps `tfs.rationale` untranslated (CI law). Add `templates/foresight.html.j2` to OWNED FILES for that one hunk; the rendered-HTML test asserts the ZH label appears for CURRENT_REPORTED and UNAVAILABLE.
R-T01-16 (= R-D1-01, restated as the acceptance property): all seven statuses render a chip with distinct `label`, `label_zh`, `tone`, `rationale`, and `source_status`; a test enumerates the seven and asserts seven distinct labels and non-None chips.
