R-T02-01 (probe file is law): `tests/test_fda_supply_probes.py` is committed RED by the seat before you start; the T02 probes in it (R8-A22, A23, A24, A25, A26, A27, R9-A02, R10-A07) define acceptance. Read the file FIRST and implement to its literal expectations; where this packet and the probe file disagree, the probe file wins and you record the disagreement under DEVIATIONS.
R-T02-02 (clock discipline): `collect_shortage_sweep` calls `clock()` exactly twice — once before the first page (start) and once after the last page or the failure (finish); `capture["acquisition_interval_s"] = (finished - started).total_seconds()` as a float in SECONDS (the probes pin exact whole-second values such as 8 and 30). Never call `clock()` per page and never read the wall clock directly inside the pure function.
R-T02-03 (unqualified receipt keeps its own clock): a failed refresh writes `last_refresh.attempted_at` from the failed sweep's own `capture.finished_at`; the selected capture's `finished_at`/`source_generation` are never rewritten (R8-A27 asserts both).
R-T02-04 (absence semantics): a row present in the previous qualified parquet and absent from the new COMPLETE sweep keeps its `status` string byte-identical and gains `absent_since_generation = <new source_generation>`; a row present in the new sweep has `absent_since_generation` null (None/NaN — the probes do not pin the null type) and `last_observed_generation = <new source_generation>`. Never write "Resolved" or any status into an absent row.
R-T02-05 (legacy parquet): a parquet with only the incumbent 13 columns and no sidecar reads as `legacy=True`, `capture=None`; the first qualified save after it sets `history_coverage.earliest_qualified_generation` to THAT save's generation (no earlier generation is ever inferred from `fetched_utc`, `update_date` or `initial_posting_date`) and `legacy_rows_capture_unknown=True` stays true forever for that artifact.
R-T02-06 (predecessor digest): `expected_predecessor` compares against `hashlib.sha256(<sidecar bytes on disk>).hexdigest()`; absent sidecar ⇔ `None`. The sidecar is written with `json.dumps(..., sort_keys=True, indent=2)` so digests are reproducible; after a successful save the returned dict carries `"predecessor": <new digest>` so a caller can chain.
R-T02-07 (no economic wording anywhere): no string you add — log lines, docstrings, sidecar fields, failure reasons — may contain the substrings "glut", "tell" (this also bans "intelligence" and "telling"), "all-clear" or "catching up"; the probes substring-match case-insensitively.
R-T02-08 (CI job is red by design until T01): the new `healthcare-fda-supply` job runs the whole probe file; the six T01 probes (R8-A18, A19, A20, A21, R9-A07, R10-A11) stay red on your head. List them by name under GAPS as "owed to lane hc_t01_supply_meaning"; do not deselect, skip, xfail or reorder them.
R-T02-09 (drip is additive): `scripts/build_foresight.py` keeps calling `fetch_shortages()` exactly once; the receipt log line is plain `log.info` (no `::notice`); a failed refresh still returns the previous qualified frame to the cascade and changes no other output.
R-T02-10 (module-level defaults preserved): `SHORTAGES_URL`, `PAGE_SIZE`, `MAX_PAGES`, `PACE_S`, `_shortages_path`, `_fetch_page`, `_parse_record` keep their names and signatures (other modules and the probes' monkeypatches rely on them); pacing (`time.sleep(PACE_S)`) lives only in the wrapper's `fetch_page` closure.
R-T02-11 (= review R-D1-06/F-D1-05b, crash consistency of the pair): the parquet is replaced first, then the sidecar; the sidecar carries `parquet_sha256` (sha256 of the parquet bytes it describes). `read_shortage_observation` verifies that digest against the parquet on disk; a mismatch returns `{"rows": None, "capture": None, "last_refresh": <as stored>, "history_coverage": <as stored>, "legacy": False, "inconsistent": True}` so the consumer reports UNAVAILABLE rather than a half-promoted observation; the next qualified sweep repairs both files. No lock manager, no directory swap.
R-T02-12 (= R-D1-07/F-D1-05c, generation monotonicity): after the predecessor check (which runs FIRST, so the interleaving probe still sees `PREDECESSOR_MISMATCH`), `save_shortage_observation` refuses a qualified result whose `capture.source_generation` is older than the selected capture's generation, or whose `capture.started_at` is earlier than the selected capture's `started_at`, with `reason = "GENERATION_REGRESSION"`; an equal generation with a later `started_at` is accepted (same-day re-sweep).
R-T02-13 (= R-D1-08/F-D1-05a, declared second writer): `.github/workflows/daily.yml` (`git add data/`) commits the parquet+sidecar pair to git; the predecessor fence covers concurrent writers on ONE checkout only, and cross-checkout lineage divergence is caught by R-T02-11's digest check, not by the fence. You do NOT edit daily.yml; you state this limitation verbatim in the PR body's "Writer fence scope" paragraph and in the `save_shortage_observation` docstring.
R-T02-14 (= R-D1-10/F-D1-07, retention model): D1 keeps ONE row per stable key `(package_ndc, initial_posting_date)` with forward metadata `first_observed_generation`, `last_observed_generation`, `absent_since_generation`, plus `previous_status` and `status_changed_generation` (set when a re-observed key's `status` differs from the stored one; the stored `status` becomes the new one). No multi-generation history table is claimed — the docstring says "current rows plus absence/status-change metadata; not a history ledger". Retention cap is expressed in GENERATIONS: an absent row whose `absent_since_generation` is more than 90 distinct source generations old is dropped and counted in `retention.dropped_absent_rows`; `retention.row_cap` is removed from the sidecar schema (replace with `retention.absent_generations_kept = 90`).
R-T02-15 (= R-D1-14/F-D1-11, clocks): `capture.started_at/finished_at` supersede the per-row `fetched_utc`; `fetched_utc` survives only as a legacy row field and is never read as the capture clock; legacy rows carry `capture_known = False`.
R-T02-16 (= R-D1-15/F-D1-12,13): `CAP_BEFORE_TOTAL` fires when pages fetched == `max_pages` and `unique_count < reported_total` using the LIVE `meta.results.total`, never the module constant; `KEY_ABSENT` covers `package_ndc in (None, "")` or `initial_posting_date in (None, "")`, and two keyless records are never merged into one row — a keyless record makes the sweep unqualified (`KEY_ABSENT`) rather than silently colliding.
R-T02-17 (= R-D1-04/F-D1-03, boundary marker for compatible callers): every DataFrame returned by `fetch_shortages()` and `load_shortages_cache()` carries `df.attrs["fda_observation"] = {"capture": <selected capture or None>, "last_refresh": <dict or None>, "legacy": bool, "inconsistent": bool, "failed_refresh": bool}` (pandas `attrs`), so a bare-frame consumer can read the observation state without a second call; the sidecar remains the sole capture channel on disk.
