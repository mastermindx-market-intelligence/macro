# Header

**Reviewer.** Claude Opus reviewer child of session `1172846f`, read-only, did not author any Healthcare research or plan. Limitation: same account family as the commissioning Healthcare Fable seat; this is organizational independence from the *author*, not institutional independence from the *program*. No repo, `data/` or `site/` bytes were written; no `gh`, no git write, no network.

**Date.** 2026-09-24. **Operation.** `gmi-healthcare-fable-ceo-e2e-20260924-chairman-001`. **Return target.** Macro PR #7788 review discussion.

**Exact reviewed inputs (recomputed with `git hash-object` over the package bytes).**

| Package path | Blob recomputed here | Blob as declared |
|---|---|---|
| `research/healthcare/HEALTHCARE_IMPLEMENTATION_PLAN_R9_2026-09-23.md` | `23cd3788620c5ad26abb878dddbc29911b2fab6f` | commission: `23cd3788…` (match); **R11 §4 table: `f58970d627a6334905bd42fa5994a25a2b00956a` (MISMATCH)** |
| `agentos/handoffs/GMI-HEALTHCARE-MASTER-FABLE-CEO-HANDOFF-2026-09-24.md` | `452aba8ec398e6b10f4dcea9b931d21c658afcb6` | commission: `452aba8e…` (match) |
| `research/healthcare/HEALTHCARE_R11_REVIEW_CONSUMPTION_PACKET_2026-09-24.md` | `5dde2a3792a1c86d7cf5983c84341716e61edf09` | not declared to me |
| `research/healthcare/HEALTHCARE_R12_SHARED_EXTENSION_SPEC_2026-09-24.md` | `94c4d991c89e83f40dfb2aa5be8189382f1204e8` | not declared to me |
| `research/healthcare/HEALTHCARE_SOURCE_INTEGRATION_DESIGN_R7_2026-09-23.md` | `cb08945fce7b21cc91bc07fda7d8cbff90ec237b` | not declared to me |

Frozen package input as given: `e5789583de2428b6dbdaa26266e8fc8e0655789a`. Handoff at `81b67a83…` as given (not independently verified — see GAPS).

**Shared pin as cited by the package.** R11 §4: "Shared implementation input: #7870 at `45eb37bbf832e007e67ce2594674d6bfeeb3b880`." R9 §"Exact source basis": "#7870 source was observed at `b256aa6a756afa3308a534f1ec259ff0074d5154`, still draft/unmerged." The seat's frozen facts give #7870 head `0c7f637cc63c4d0c29232a0738f971c9eca3626a`. **Three distinct shared pins are in circulation for one dependency.** R11 §4 obliges the reviewer to "identify material movement if a newer shared head is consumed rather than silently reviewing different contracts"; I am recording the movement and did not review any #7870 bytes (out of scope, and the head is unlanded on main).

**Ranges read.** Handoff §§1 (17–28), 5 (88–104), 7 (115–126), 9 (137–144). R11 lines 64–130. R9 lines 15–101, 102–168, 322–339, 340–406; lines 169–321 at skim depth only. R7 lines 71–104. R12 §§5 (85–121), 6 (122–133), 8 (142–166). Incumbent (worktree HEAD `83223aa57a42`): `collectors/fda_shortages.py` 34–175 plus anchor grep; `engine/fda_scarcity.py` 30–75, 77–243; `engine/foresight_cascade.py` 40–80, 135–190, 290–370, 426–442 plus grep; `scripts/build_foresight.py` 95–120 plus grep; `templates/foresight.html.j2` 575–590; `.github/ci/legacy-jobs.yml` by grep; `.github/workflows/daily.yml` by grep.

**Scope of this verdict.** Independent Healthcare plan review, D1 in depth. NOT shared-compatibility review (I read no #7870 code). Not an authorization to merge, deploy, admit a source, expand budget, or trade.

---

# D1 verdict

## VERDICT: REQUEST_CHANGES (scope: D1 = T01 + T02 + applicable T08; independent Healthcare plan)

Not HOLD: every blocker below is repairable inside D1's own five files by seat-level rulings, with no external decision pending. Not ACCEPT: **two clauses of the seat's own frozen D1 interface contract are falsified against incumbent code** — the legacy band mapping makes the four truthful states the D1 acceptance cases exist to surface invisible, and the "chip is display-only" premise is false for `tier`. A builder handed this contract today would ship a D1 that passes its own unit tests and still fails R8-A20/A21/A23/A27 at the rendered page.

D1's direction is right and the R7 root-cause analysis is sound. The defects are in the *frozen interface contract the seat added on top of the plan*, not mostly in R9 itself.

| ID | Sev | Clause / req | Counterexample | Smallest fix | Distinguishing evidence a builder must produce |
|---|---|---|---|---|---|
| **F-D1-01** | BLOCKER | Seat contract, T01: "legacy `band` mapped deliberately (CURRENT/MIXED→ACTIVE, RESOLVED→RESOLVED, everything else→NONE)" vs R8-A20/A21/A23/A27, R9-A07, R7-F06 | `format_theme_feed_chip` returns `None` when `band == BAND_NONE` (`engine/fda_scarcity.py:215-216`), and the cascade only renders a chip when `theme_feed_summary` is non-None (`engine/foresight_cascade.py:429-437`; `templates/foresight.html.j2:581`). So `UNAVAILABLE`, `UNCLASSIFIED`, `NO_MATCHING_RECORDS` **and** `DISCONTINUATION_REPORTED` all map to `NONE` → chip `None` → **nothing renders**. A failed acquisition, a stale cache, a complete-empty sweep and an unrecognized status become one indistinguishable blank — precisely R8-A23. It is also a **regression**: today a discontinued-only theme *does* render a chip (`engine/fda_scarcity.py:176-179` band `SHORTAGE_RESOLVED`, label at `:228-229`) | Stop deriving chip visibility from the legacy `band`. Add `source_status` to the chip dict and make `format_theme_feed_chip` return a chip for every `source_status` except a deliberate not-configured case; keep `band` as a *machine-legacy-only* field with no visibility authority | A rendered-HTML test over all seven `source_status` values asserting seven distinct visible chip strings (EN and ZH), plus a screenshot/HTML crop for `UNAVAILABLE` and `NO_MATCHING_RECORDS` |
| **F-D1-02** | BLOCKER | Seat contract: "chip is display-only (verify in `_stage`/cascade that the FDA chip cannot move stage/entry)" — **false for `tier`** | `_compute_tier(numeric_band, theme_feed_summary)` returns Tier `P` **because the chip object exists** (`engine/foresight_cascade.py:59-74`, called `:459`, emitted `:466`), and the template splits the page on it (watch shelf at `templates/foresight.html.j2:588-590`). Combined with F-D1-01: an openFDA outage → `UNAVAILABLE` → `NONE` → chip `None` → `glp1_obesity` silently **demotes from the physical desk to the watch shelf**. An acquisition failure changes product composition. (The display-only claim *is* true for `_stage`: `glut_band` at `:174/:180` comes from `engine.glut_watch` at `:332-338`, never from `fda_scarcity` — I verified this and raise no finding there) | Compute tier from whether a per-theme feed is **configured** (`theme_key in MOLECULE_THEME_MAP`), not from whether a chip object materialized | A cascade test asserting `tier == "P"` for `glp1_obesity` across all seven `source_status` values *including* `UNAVAILABLE`, and a before/after diff of the Tier-W shelf membership |
| **F-D1-03** | BLOCKER | Seat contract, T01: "behind unchanged `compute_fda_scarcity`/`format_theme_feed_chip` signatures" while requiring `freshness{capture_qualified, capture_age_s, source_generation, stale, failed_refresh}` | `compute_fda_scarcity(df: pd.DataFrame \| None = None)` (`engine/fda_scarcity.py:77`) has **no parameter through which a capture can arrive**, and its docstring invites a bare frame (`:80-82`). The cascade also accepts an injected `fda_scarcity=` dict wholesale (`engine/foresight_cascade.py:295, 357`). Worse, `fetch_shortages` returns the **previous parquet frame** on failure with no marker (`collectors/fda_shortages.py:121-125` empty-endpoint path, `:147-154` exception path), so a caller holding that frame cannot tell a fresh sweep from a silent fallback — the staleness is invisible at the only boundary the legacy signature exposes | Rule that the bare-`df` path is **capture-unknown by construction**: it must yield `capture_known=False` and a `source_status` that can never be `CURRENT_REPORTED`/`RESOLVED_REPORTED`; the sidecar read is the *only* admitted capture channel | A test calling `compute_fda_scarcity(df)` with a rows-only frame and asserting the result is capture-unknown and not promoted, plus a test that a post-failure `fetch_shortages()` frame yields `failed_refresh=True` |
| **F-D1-04** | BLOCKER | R7-F05 required behavior: "Apply the incumbent freshness/eligibility contract; **no invented universal age limit**" vs seat contract `max_capture_age` | I searched for an incumbent contract to apply: `grep -n "STALE\|stale\|MAX_AGE\|max_age\|_AGE_D\|age_days\|freshness" engine/policy_calendar.py engine/foresight_cascade.py` returned **zero hits in both files**. There is no incumbent freshness contract on this path to inherit. `max_capture_age` as a required keyword with a caller-chosen `timedelta(days=2)` (R9 line ~119 test sketch) *is* the invented universal age limit R7-F05 forbids — merely relocated to the caller | Make the age verdict opt-in: `summarize_supply` always reports `capture_age_s` and `source_generation_age_days` as facts, and emits `stale` **only** when an owner-supplied threshold is passed; with no threshold, `stale=None` (unknown), never `False` | The builder cites `file:line` of the owner contract it inherits, or files a seat ruling naming the threshold and its owner; a test asserting `stale is None` when no threshold is supplied |
| **F-D1-05** | BLOCKER | Seat contract, T02: "`predecessor` = sha256 of prior sidecar bytes; mismatch refuses" as the realization of R9 T02 Step 3 "existing writer fence and verified predecessor" / R10-A07 | Three holes. (a) **A second writer exists that the fence cannot see**: `.github/workflows/daily.yml:638` runs `git add data/`, and `data/fda/shortages.parquet` is a committed 77,748-byte artifact re-committed nightly. On a fresh runner checkout the "prior sidecar" is whatever git last committed, which is a *different lineage* from the last local write; the hash matches and the refusal never fires. (b) **Two files, one rename each**: parquet and `<parquet>.observation.json` are separately renamed, so a crash between them leaves a sidecar describing a parquet that is not on disk. (c) **A predecessor hash proves no-lost-update, not generation monotonicity**: A reads S0, B reads S0, A writes S1, B refuses on S0, B retries against S1 and writes S2 — but if B's sweep *started earlier* than A's, B has just promoted the older generation with a perfectly valid predecessor chain. That is exactly R10-A07's prohibited outcome | (i) Swap one directory (or one file containing both) with a single rename; (ii) refuse promotion whenever the incoming `source_generation`/`started_at` is **older than the persisted selected capture**, independently of the predecessor match; (iii) declare the git lane a second writer in the contract and state that the fence does not cover it — or exclude the sidecar from the nightly's broad `git add` | The interleaving test R9 T02 Step 1 names, run in **both** orders (A-then-B and B-then-A), plus a test where the predecessor matches but `source_generation` regresses and promotion still refuses |
| **F-D1-06** | MAJOR | R10-A07 / R10-A11: "Each early release runs its dependency-closed proof set" | D1 has **no CI carrier**. `grep -c "test_foresight_cascade" .github/ci/legacy-jobs.yml` → `0`. Meanwhile `engine/fda_scarcity.py` and `engine/foresight_cascade.py` sit in `paths:` lists (`.github/ci/legacy-jobs.yml:8295/8299`, `12797/12801`, `14546/14547`) and `collectors/fda_shortages.py` in 2 more. So a D1 edit **triggers jobs that never execute D1's tests**; hosted CI green would be evidence of nothing | Wire `tests/test_foresight_cascade.py` and `tests/test_fda_shortages_generation.py` into a job's `run:` line **and** that same job's `paths:` in the one PR | The PR diff showing both the `run:` and `paths:` edits, and the named job green on the PR head. (House trap: a test added to a `run:` line without its `paths:` entry reds contract-delta on every later PR — both halves or neither) |
| **F-D1-07** | MAJOR | Seat contract, T02 retention: "`retention{row_cap 5000, dropped_absent_rows}`" vs R8-A25/A26 and R7-F09/F10 | The incumbent cache is a **latest-value merge**: `drop_duplicates(subset=["package_ndc","initial_posting_date"], keep="last")` (`collectors/fda_shortages.py:133-135`). Retaining an absent row with `absent_since_generation` *and* keeping one row per `(ndc, posting_date)` are contradictory — that key cannot hold two generations of the same record, so "forward history" collapses back to latest-value the moment the same NDC reappears. Separately, R7 §6 observed `meta.results.total=1601`; a 5,000-row cap is **~3 full censuses**, i.e. history is evicted after roughly three sweeps — not a history | Change the retention key to include the generation (e.g. `(package_ndc, initial_posting_date, source_generation)`) or state explicitly that D1 retains only *current + absence metadata*, not history; re-derive the cap in **generations**, sized against the observed 1,601-record census, and justify it against the committed 77,748-byte artifact budget | A test asserting two generations of the same `package_ndc` both survive; a measured post-N-sweep parquet byte size against the 77,748 B baseline |
| **F-D1-08** | MAJOR | Seat contract, T01 enum is flat vs R12 §6 (line ~131): "An unrecognized status alongside a current record may still establish that the observation contains a current record, but the aggregate must retain its unresolved status coverage" and R8-A21 | One `Current` row plus one unrecognized-status row has **no faithful value** in `{CURRENT_REPORTED, …, UNCLASSIFIED}`. `CURRENT_REPORTED` hides the unknown; `UNCLASSIFIED` hides the current record. The incumbent already fails this way and worse — coverage is derived from the *band-driving* molecule set (`zero_molecules`, `engine/fda_scarcity.py:157` and `:166`), so a molecule whose only row is discontinued or unrecognized is reported as "no shortage records found" (`:191`), which is R7-F07 | Keep `source_status` flat but require **independent** `unclassified_rows` and `matched_rows` counts, derive `coverage` from *matched* rows not *classified* rows, and require both in the visible chip | A test where one molecule has only an unrecognized row: `coverage` must show it matched, and the chip must not say "no records" |
| **F-D1-09** | MAJOR | Seat contract, T01: "no 'glut/tell' wording" — scoped only to the label; R8-A19, R7-F03 | The incumbent's economic wording lives in `rationale`, not only `label`: "glut tell" at `engine/fda_scarcity.py:167`, `:175`, `:183`, and the chip label at `:225`/`:229`. `rationale` is carried into the chip dict (`:240`) and rendered into the tooltip at `templates/foresight.html.j2:581` (`title="{{ tfs.rationale or '' }}"`). Worse, the plan's own Step-1 regression only asserts `"glut" not in out["label"].casefold()` (R9 line ~126) — a builder can pass that test with "glut tell" still in the tooltip and on the page. Also `:158` "demand exceeds supply" is an economic conclusion from a regulator status | Extend the ban to `rationale`, `label`, `label_zh` and every string that reaches the chip dict, and assert it over **rendered HTML**, not over `out["label"]` | A test that renders the foresight fragment for each `source_status` and asserts no `glut`/`tell`/`demand exceeds supply` substring anywhere in the emitted markup |
| **F-D1-10** | MINOR | Seat contract, T01 emits `label_zh`; bilingual EN/ZH UI is house law | `templates/foresight.html.j2:581` renders `{{ tfs.label }}` only. Sibling chips on the same row do use the translation helper — `t('exit-risk','退出风险')` at `:579`, `t('score held back','评分受限')` at `:583`. So `label_zh` is a contract field with **no consumer**: the ZH reader sees an English FDA chip | Render `t(tfs.label, tfs.label_zh)` at `:581` | A ZH-locale rendered-HTML assertion for at least `CURRENT_REPORTED` and `UNAVAILABLE`. (Note: `title=` must stay untranslated per house CI rule — put the ZH in the visible label, not the tooltip) |
| **F-D1-11** | MINOR | Seat contract, T02 capture clocks vs incumbent row stamp | `fetched_utc` is stamped **per record inside `_parse_record`** (`collectors/fda_shortages.py:75`), so rows from page 1 and page 17 of one sweep carry different times and none of them is `started_at` or `finished_at`. If a legacy consumer reads `fetched_utc` as the capture clock it will read a mid-sweep instant as the observation time — R8-A27's "never replace source time with render time" has a sibling failure here | State that `capture{started_at, finished_at}` supersedes `fetched_utc`, that `fetched_utc` is retained only as a legacy row field, and that legacy rows carry `capture_known=False` | A test asserting no consumer path derives freshness from `fetched_utc` |
| **F-D1-12** | MINOR | Seat contract, T02 `failure_code` includes `CAP_BEFORE_TOTAL` | The incumbent exits the page loop silently with no code at `collectors/fda_shortages.py:114` (empty page) and `:118` (`skip >= total`), and the cap is the module constant `MAX_PAGES = 25` × `PAGE_SIZE = 100` = 2,500 (`:35-36`) against R7's observed `total=1601`. Today the cap is not binding, but qualification keyed to the constant rather than the **live** `meta.results.total` will silently stop qualifying the day the census passes 2,500 | Fire `CAP_BEFORE_TOTAL` from `pages_fetched == max_pages and rows_collected < reported_total`, using the live reported total | A synthetic test with `reported_total=3000`, `max_pages=25`, `page_size=100` asserting `qualified is False` and `failure_code == "CAP_BEFORE_TOTAL"` |
| **F-D1-13** | MINOR | R9 T02 Step 3: "A content-scoped identity cannot establish cross-generation absence" vs the dedupe key | The stable key today is `(package_ndc, initial_posting_date)` (`collectors/fda_shortages.py:133-135`) and `package_ndc` is `rec.get("package_ndc") or ""` (`:63`) — **an empty string when absent**, so every keyless record collides into one row per posting date. The contract's `KEY_ABSENT` failure code must therefore cover records with an empty `package_ndc`, not only a missing field | Treat `package_ndc == ""` as absent for `KEY_ABSENT`, and never merge two empty-key records | A test with two distinct records both lacking `package_ndc` asserting neither is merged and the sweep reports `KEY_ABSENT` |

**Verified and raising no finding:** the FDA chip cannot move `stage` or `entry` — `_stage`'s `glut_band` (`engine/foresight_cascade.py:174, 180-182`) is sourced from `engine.glut_watch` (`:332-338`), never from `fda_scarcity`. The seat's display-only claim holds for the stage machine; it fails only for `tier` (F-D1-02).

---

# Whole-package verdict

## VERDICT: HOLD (scope: whole package D1–D4; independent Healthcare plan). Exact unresolved decision: **which #7870 head and which contract bytes the D2–D4 review is against.**

Three distinct shared pins are in circulation for one dependency (Header), the shared foundation is **not on main**, and R11 §4's own minimum-reading-set table declares a plan blob (`f58970d6…`) that does not match the plan bytes actually shipped in the package (`23cd3788…`). D2–D4 acceptance would be a review of contracts that cannot presently be read. This HOLD is on D2–D4 only; **it does not block D1**, exactly as R10-A11 requires ("future unexecuted tests neither falsely pass nor administratively block D1").

**Q1 — Compatibility.** *For D1: D1 has no shared-contract dependency at all.* T01/T02 touch only `collectors/fda_shortages.py`, `engine/fda_scarcity.py`, `engine/foresight_cascade.py`, `scripts/build_foresight.py` and their tests; none of those imports `engine/theme_graph/*`, `admission`, `rights`, `r2_store`, or the POST research routes. I state this explicitly rather than inventing a dependency. *For D2–D5: unanswerable today* — #7870 is unlanded, pinned three ways, and R9's "Shared dependency acceptance record" is correctly written as a checklist to be *consumed*, which means the acceptance decision has not been made and cannot be made by this review.

**Q2 — Economic and clinical meaning.** The strongest part of the package. R12 §5 keeps denominator, tier method, cost bearing, loss participation and `not_disclosed` genuinely separate, and its worked waterfall correctly shows royalty-on-gross (50) and profit-share-on-sales (200) as *wrong*, with 40/40 equality flagged as coincidence rather than interchangeable bases. R12 §6 holds regulator status ≠ manufacturer availability ≠ economic capacity. Residual risk is at the seam, not in the model: the D1 contract F-D1-09 lets the incumbent's "glut tell" and "demand exceeds supply" survive in `rationale`, so the *first shipped surface* contradicts the semantics R12 defends.

**Q3 — Source and publication integrity.** Interval-observation-not-atomic-census is correctly stated and `atomic_snapshot_proven=False` is pinned (R9-A02). Forward-only history with no hindsight backfill is correctly stated (R8-A26). **But** "current writer fencing" is unproved at exactly the point it matters: F-D1-05 shows the nightly's `git add data/` (`.github/workflows/daily.yml:638`) is a second writer outside any sidecar fence, and a predecessor hash does not order generations. R11 Q3 asks whether publication "uses current writer fencing" — the honest answer today is no.

**Q4 — Access and authority.** Correctly separated in the text (R9 Global Constraints: `require_user` then `enforce_site_full(always=True)`; the 86400 s positive-only grace that a failed refresh does not renew; R8-A30/A31/A33, R10-A08/A09/A10). Every one of those is `NOT_EXECUTED` and none is testable until D2's dependency lands. The R7 private-body release gate is correctly stated as a gate, not a claim. No finding beyond the HOLD.

**Q5 — User outcome and useful releases.** The four-release split is well drawn and D1's independence from v1.1/private-profile is correctly asserted in three places (handoff §5, R9 release map, R12 §8). **But D1 as currently contracted does not deliver its stated useful result** — "truthful existing FDA supply observation and visible consumer, *including incomplete/stale/mixed states*" (handoff §5) — because F-D1-01 makes incomplete, stale and unclassified states render as nothing. That is the single most important finding in this review: D1's acceptance sentence and D1's frozen interface contract disagree.

| ID | Sev | Clause / req | Counterexample | Smallest fix | Distinguishing evidence |
|---|---|---|---|---|---|
| **F-PKG-01** | MAJOR | R11 §4 minimum reading set: "the hashes select the research inputs" | The declared plan blob `f58970d627a6334905bd42fa5994a25a2b00956a` does not equal the package's plan bytes `23cd3788620c5ad26abb878dddbc29911b2fab6f`. A reviewer following R11 §4 literally would look for bytes that are not in the package | Correct the R11 §4 row to `23cd3788…` (or ship the `f58970d6…` bytes) and state which is canonical | Recomputed `git hash-object` output for every row of the R11 §4 table, posted with the review |
| **F-PKG-02** | MAJOR | R11 §4: "identify material movement if a newer shared head is consumed" | R9 `b256aa6a…`, R11 `45eb37bb…`, seat `0c7f637c…` — three heads, and the foundation is unlanded on main. D2's "Shared dependency acceptance record" therefore has no resolvable subject | Name one pin as the review subject with its date, and mark the other two as superseded navigation | A single declared pin in the D2 commission, and a diff summary if that pin later moves |
| **F-PKG-03** | MAJOR | R9 release map: "D3 … D1 wherever misleading legacy output is in the same journey"; R8-A40 | The dependency is stated but has **no test**. R8-A40 requires D3 to "fail coherence until the incumbent output is repaired" — with D1's chip silently absent under F-D1-01/02, D3 could pass its own cases while the same journey shows a demoted theme with no supply chip at all | Make R8-A40 a D3 gate that asserts the *rendered* D1 chip is present and truthful on `basket/obesity_glp1.html`, not merely that D1 merged | A D3 browser/HTML artifact showing the D1 chip in the GLP-1 journey |
| **F-PKG-04** | MINOR | T03 (skim): R12 §8 "Do not widen B's v1 enums or implement a Healthcare hash"; R10-A01/A12 | The prohibition is clear, but the plan's own file-responsibility table still lists `Proposed contracts/theme_graph/curation_assertion.v1.1.schema.json` as a Healthcare-adjacent deliverable while assigning it to "a single assigned shared writer, not the active Semiconductor B release by default" — the writer is named by exclusion, not by identity | Name the v1.1 writer, or record that it is unassigned and that T03 is blocked on that assignment | A cited acceptance return from the named shared owner |
| **F-PKG-05** | MINOR | T05 (skim): R12 §8 "B's response schema is not a generic medical schema"; R8-A16, R9-A06 | Correctly stated; the gap is that "unsupported Healthcare is an explicit refusal" has no named refusal shape (status code, body, headers) in either R9 or R12, so two builders would invent two refusals | Freeze the refusal shape when the shared route contract is accepted (D2), not before | The accepted route's refusal fixture, cited by path |
| **F-PKG-06** | MINOR | T06/T07 (skim): handoff §9 "user-task proof is a concise demonstrated answer to the five-part economic question"; R8-A39 | The five-part question is the acceptance criterion but has no scoring rubric — "reader can state change, mechanism, participant, counterevidence and next observation" is adjudicated by whom, against what? Without a rubric this is a taste judgment presented as a gate | Attach a five-row checklist to R8-A39 with a required evidence citation per row | The filled checklist with one source citation per row, produced at D3 |

---

# Checks actually run

These are **local Python test executions and static reads only**. No hosted CI, no browser, no deployment, no network, no product acceptance.

1. `cd <worktree>; PYTHONPATH=. python3 -m pytest tests/test_foresight_cascade.py tests/test_policy_calendar.py -q -p no:cacheprovider 2>&1 | tail -2`
   → `52 passed, 86 warnings in 5.61s`
   (Confirms the seat's stated baseline. This is a **characterization** baseline: it proves the incumbent's *current* behavior is green, and therefore that a truthful D1 will have to break some of these 52 before it can pass. It is not evidence of D1 correctness.)
2. `grep -c "test_foresight_cascade" .github/ci/legacy-jobs.yml` → `0` (no CI job runs D1's test file).
3. `grep -n "engine/fda_scarcity.py" .github/ci/legacy-jobs.yml` → lines `8295`, `12797`, `14546` (all `paths:` entries); `grep -c "collectors/fda_shortages.py" …` → `2`.
4. `grep -n "STALE\|stale\|MAX_AGE\|max_age\|_AGE_D\|age_days\|freshness" engine/policy_calendar.py engine/foresight_cascade.py` → **no output** (no incumbent freshness contract on this path; basis for F-D1-04).
5. `grep -n "glut_band\|fda_scarcity\|theme_feed_summary\|SHORTAGE_" engine/foresight_cascade.py` → established that `glut_band` is sourced from `engine.glut_watch` (`:332-338`) and that `theme_feed_summary` feeds `_compute_tier` at `:459` (basis for F-D1-02 and for clearing the stage/entry claim).
6. `grep -n "git add" .github/workflows/daily.yml` → `:638  git add data/ site/qledger/` (basis for F-D1-05(a)).
7. `git hash-object` over the five package files (table in Header).
8. `ls -l data/fda/shortages.parquet` → **absent in this worktree**. This is the sparse-worktree omission (`data/` is in `config/sparse_worktree.json`), *not* evidence the artifact is missing from the repo. I did not opt into a full checkout (read-only mandate), so I did not inspect the parquet's real bytes or row count.

---

# Unexecuted gates

- All **60 application requirements** (R8-A01…A41, R9-A01…A07, R10-A01…A12) remain `NOT_EXECUTED`. Nothing in this review moves any of them.
- `probe_r7_fda.py` was **not run** (out of scope). R7's eleven findings are consumed as the seat's established fact, corroborated by my own reads of `engine/fda_scarcity.py` and `collectors/fda_shortages.py`, not re-derived.
- No hosted CI run, no `gh` call, no PR state read, no merge, no deployment.
- No browser proof: entitled use, denied/anonymous, logout, delayed response, stale/missing data, correction, public-mirror boundary — none exercised.
- No real openFDA acquisition; no rights decision; no source admission.
- `data/fda/shortages.parquet` bytes not inspected (sparse; read-only mandate).
- The handoff commit `81b67a83…` and the frozen input `e5789583…` were taken as given; I verified file blobs only.
- No #7870 code read. Shared-compatibility acceptance is explicitly **not** covered by this verdict.
- Dark/light theme adjudication: **not applicable and not performed** — D1 changes chip *content*, not the material system. If the D1 implementation introduces any new chip styling, that becomes a material UI change and owes the dual-theme evidence matrix separately; it is not covered here.

---

# Smallest D1 repair set

Each is one seat ruling a build lane can execute.

- **R-D1-01** — Rule that chip visibility is keyed on `source_status`, not on the legacy `band`: `format_theme_feed_chip` returns a chip for all seven statuses and carries `source_status` in the dict. *(fixes F-D1-01)*
- **R-D1-02** — Rule that `_compute_tier` takes theme *configuration* (`theme_key in MOLECULE_THEME_MAP`), not chip presence, so an acquisition failure can never move a theme between the physical desk and the watch shelf. *(F-D1-02)*
- **R-D1-03** — Rule that the bare-`df` path of `compute_fda_scarcity` is capture-unknown by construction and may never yield `CURRENT_REPORTED`/`RESOLVED_REPORTED`; the sidecar is the sole capture channel. *(F-D1-03)*
- **R-D1-04** — Rule that `fetch_shortages`'s post-failure fallback frame is marked `failed_refresh=True` at the boundary where it is returned, so a bare-frame caller cannot read stale as fresh. *(F-D1-03)*
- **R-D1-05** — Rule that `max_capture_age` is optional and threshold-free by default: report `capture_age_s` and `source_generation_age_days` as facts, emit `stale=None` when no owner threshold is supplied. *(F-D1-04)*
- **R-D1-06** — Rule that the parquet and its sidecar are swapped by **one** rename (single directory or single file), never two. *(F-D1-05b)*
- **R-D1-07** — Rule that promotion refuses on `source_generation`/`started_at` regression independently of the predecessor hash. *(F-D1-05c)*
- **R-D1-08** — Rule that `.github/workflows/daily.yml:638`'s `git add data/` is a declared second writer the sidecar fence does not cover, and state the disposition (exclude the sidecar, or accept and document the limitation) in the D1 PR body. *(F-D1-05a)*
- **R-D1-09** — Rule that the D1 PR wires `tests/test_foresight_cascade.py` and `tests/test_fda_shortages_generation.py` into one CI job's `run:` **and** that job's `paths:` in the same diff. *(F-D1-06)*
- **R-D1-10** — Rule that the retention key includes `source_generation` (or that D1 retains current + absence metadata only, with no history claim), and that the cap is expressed in generations sized against the observed 1,601-record census. *(F-D1-07)*
- **R-D1-11** — Rule that `coverage` is derived from *matched* rows, not *classified* rows, and that `unclassified_rows`/`matched_rows` are emitted independently of `source_status` and both appear in the chip. *(F-D1-08)*
- **R-D1-12** — Rule that the no-economic-wording ban covers `rationale`, `label`, `label_zh` and every chip string, and is asserted over rendered HTML (banned: `glut`, `tell`, `demand exceeds supply`). *(F-D1-09)*
- **R-D1-13** — Rule that `templates/foresight.html.j2:581` renders `t(tfs.label, tfs.label_zh)`, with the ZH assertion in the visible label and nothing translated into `title=`. *(F-D1-10)*
- **R-D1-14** — Rule that `capture{started_at, finished_at}` supersedes the per-row `fetched_utc` (`collectors/fda_shortages.py:75`), which survives only as a legacy field with `capture_known=False`. *(F-D1-11)*
- **R-D1-15** — Rule that `CAP_BEFORE_TOTAL` fires from the live `meta.results.total`, not the `MAX_PAGES` constant, and that `KEY_ABSENT` covers `package_ndc == ""`. *(F-D1-12, F-D1-13)*

---

# New versus already-stated

**New in this review — not stated anywhere in R7/R9/R10/R11/R12 or the seat's contract:**

- F-D1-01 (the legacy band mapping erases four states at the chip because of `engine/fda_scarcity.py:215-216`) — the plan requires the states to be *distinguishable* and the seat's mapping makes them *invisible*; nobody had connected the two.
- F-D1-02 (`_compute_tier` makes the chip composition-bearing, `engine/foresight_cascade.py:59-74`) — the seat's contract asserts the opposite.
- F-D1-03 (the unchanged `compute_fda_scarcity` signature has no capture channel) — an architectural incompatibility inside the seat's own compatibility clause.
- F-D1-04 (no incumbent freshness contract exists to inherit, so R7-F05's instruction is unsatisfiable as written) — R7 assumed one existed.
- F-D1-05a (the nightly `git add data/` second writer) — R9 says "existing writer fence" without identifying who the existing writers are.
- F-D1-05c (predecessor hash ≠ generation monotonicity) — R9's interleaving case is stated; the insufficiency of the seat's chosen mechanism for it is not.
- F-D1-06 (D1 has no CI carrier; `run:`/`paths:` asymmetry) — R10-A11 states the requirement, nobody checked the carrier exists.
- F-D1-07 (the dedupe key contradicts forward history; 5,000 rows ≈ 3 censuses).
- F-D1-09 (the plan's own Step-1 assertion tests only `label`, leaving the tooltip unpoliced).
- F-D1-10 (`label_zh` has no consumer at `templates/foresight.html.j2:581`).
- F-D1-13 (`package_ndc or ""` collides keyless records).
- F-PKG-01 (R11 §4's declared plan blob does not match the shipped bytes).
- F-PKG-02 (three shared pins) — R11 §4 asked for this check; the answer is new.
- F-PKG-03 (R8-A40's D1→D3 coherence dependency has no test).

**Already stated by the plan as implementation gates — reported here for completeness, not as contradictions** (per R11 §7: "A missing future production receipt can remain an explicit pre-release gate without being misreported as a contradiction in the design"):

- All 60 requirements `NOT_EXECUTED` (handoff §9, R9 Appendix A).
- Source rights / admission decision outstanding (R7 §6, R9 "Required source qualification").
- Private-body release gate unproved (R7 §6).
- #7870 shared acceptance record not yet supplied (R9 "Shared dependency acceptance record").
- No independent verdict existed before this one (R9 T08 Step 3, explicitly).
- Atomic rename ≠ multi-writer fencing (R9 T02 Step 3, R12 §6) — the *principle* is stated; F-D1-05 is the new demonstration that the seat's chosen mechanism does not discharge it.
- Disappearance ≠ resolution; finite webpage retention ≠ API retention (R7 §5 closing, R12 §6).
- F-D1-08's aggregate problem is *named* in R12 §6 (`MIXED_WITH_CURRENT`, "not a national all-products statement"); what is new is that the seat's flat enum cannot express it.
