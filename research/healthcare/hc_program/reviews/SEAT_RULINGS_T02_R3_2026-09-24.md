R-T02R3-01: the rounds 1-2 semantics (sweep qualification, sidecar fence, retention, absence ageing, clock purity, receipt at the collector seam, measured closure) are ACCEPTED at 8d858bba — do not re-run their ladders, do not refactor, do not rename; touch only the lines the six ruled repairs need.
R-T02R3-02 (= T02R2-B1): `failure_code` in the receipt is the LAST REFRESH's code; `qualified=` is the last refresh's flag; a failed refresh over a previously qualified observation prints `qualified=False failure_code=<its code>` while `source_generation=` still names the selected (older) observation's generation.
R-T02R3-03 (= T02R2-B2): "never promoted" is necessary, not sufficient — a failed metadata write must also be NON-DESTRUCTIVE: the reader afterwards returns the previous `selected_capture` rows/capture (not `None`), `inconsistent=False`, and `last_refresh.failure_code=METADATA_WRITE_FAILED`.
R-T02R3-04 (= T02R2-M1): row-level malformation is a taxonomy outcome (`MALFORMED_ROW`, sweep unqualified), never an exception; the sweep's row `except` covers `TypeError, ValueError, AttributeError, KeyError, IndexError`.
R-T02R3-05 (= T02R2-M2/M3): every disk read in the read path (`pd.read_parquet`, sidecar `json.loads`) is wrapped; an unreadable parquet → `inconsistent=True, rows=None, capture=None`; an unreadable sidecar during a FAILED refresh → treated as `{}` for the copy-through and the refresh still writes only `last_refresh`.
R-T02R3-06 (= T02R2-M4): `source_generation` must be a non-empty string to qualify; add `NO_SOURCE_GENERATION` to the failure-code taxonomy (keep the existing codes' names and order); a generation-less capture stamps nothing.
R-T02R3-07 (= T02R2-m1): the PR's diff in `.github/ci/legacy-jobs.yml` is the `healthcare-fda-supply` block only; reordering other jobs' `paths:` is a defect even when alphabetical.
R-T02R3-08 (= T02R2-m2, REJECTED): do NOT add a test that imports `scripts.build_foresight` — that is the closure smear round 2 removed; the drip line's production wiring is proven by the nightly log (PRODUCTION_PROOF rung), not by a unit test in the exclusive job.
R-T02R3-09: `tests/test_fda_shortages_generation.py` edits are limited to the two ratifying assertions named in the packet; a passing-by-construction test is a defect, so each amended assertion must name the truthful value (the surviving observation, the real failure code), never `is not None` alone.
R-T02R3-10: commit and push after EVERY numbered step; a long silent exec is how round 1 timed out; keep each exec under 12 minutes and run the ~7-minute closure test once, last, alone.
