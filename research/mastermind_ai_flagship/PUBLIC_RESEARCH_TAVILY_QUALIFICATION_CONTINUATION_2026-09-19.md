# Mastermind AI R1 — Tavily public-research qualification slice

Date: 2026-09-19. Operation:
`mastermind-ai-public-research-tavily-qualification-20260919-sol-001`.
Parent program: `macro-mastermind-ai`; flagship design/continuity: Macro #7151.
Owner: Sol. Capability state at this source checkpoint: **BUILT_NOT_PROVEN**.

Procedure pin: protected Mastermind
`8ba7deedde164c90298d3e88785d98e02fa5e2d2`, Skillpack 1.0.1 /
bootstrap 1. This is a source qualification slice, not customer Brain search,
not a provider credential admission, not a signal engine, and not production proof.

## User and machine outcome

The user problem is that current Mastermind AI can answer from supplied/internal
context without actively closing material evidence gaps. R1's first machine
capability is deliberately narrower than customer wiring:

`minimal public research query -> external search -> selected source extraction ->
normalized evidence receipt that proves what was searched and what source content
was actually retrieved`.

The receipt must make these different states impossible to confuse:
search not configured, invalid public query, provider/search unavailable, no search
coverage, search snippet found but source not opened, source extraction failed,
source opened with bounded content, and partial source opening.

No model conclusion is generated by this slice. The later analyst must consume this
evidence; provenance does not substitute for synthesis.

## Why Tavily is the first qualification carrier, not a permanent routing decision

Official Tavily docs rechecked 2026-09-19 describe:
- POST `https://api.tavily.com/search`;
- search depths advanced/basic/fast/ultra-fast;
- a `finance` topic alongside general/news;
- date windows and provider-estimated published dates;
- advanced/basic/fast snippets, with provider answer/raw-content separately optional;
- POST `https://api.tavily.com/extract` for source content;
- extract basic/advanced modes, up to 20 URLs;
- query-aware extraction that reranks source chunks;
- per-URL `failed_results`, including partial/all failure under a successful HTTP response.

Those properties make search + actual source-content extraction testable with one
provider. Brave and other providers remain legitimate comparison candidates. This
source does not mint a provider winner or credential authority.

## Source architecture

New provider-neutral evidence boundary:
`engine/neuralweb/public_research.py`.

It is intentionally not registered in `brain_gateway.py` yet. That shared gateway
is owned by the separately held financial bridge (#7365), and R1 must prove retrieval
semantics before creating a customer-visible tool.

The module has three public stages:

1. `search_public`
   - requires a service key from the explicit argument or `TAVILY_API_KEY`;
   - validates a minimal public query (<=400 chars, no control characters);
   - bounds provider topic/depth/date/domain filters and results;
   - disables provider-generated answers and raw page content in search;
   - returns only normalized search snippets marked
     `source_open_state=not_opened`;
   - rejects unsafe/private-looking source URLs and duplicate canonical URLs;
   - records provider request ID and credits only when safely typed;
   - returns only a SHA-256 query fingerprint, not the raw query.

2. `open_public_sources`
   - accepts at most five safe public HTTP(S) domain URLs;
   - rejects credentials in URLs, localhost/private IP literals, private suffixes,
     unusual ports and malformed/control-character URLs before provider transport;
   - calls Tavily Extract only, never a local arbitrary-URL fetch;
   - distinguishes opened, failed, missing-result and transport-failed source states;
   - bounds extracted content;
   - marks all extracted text untrusted and explicitly non-authoritative;
   - labels query-reranked content `query_reranked_chunks`, and non-reranked
     extraction `bounded_page_extraction`;
   - always sets `full_document_reviewed=false`.

3. `investigate_public`
   - searches, selects only returned safe URLs, opens the top bounded candidates,
     then joins discovery metadata to extraction state;
   - no-search coverage, all-open-failed, partial-open and all-opened states remain
     different;
   - never calls a fallback provider/account.

The existing lifecycle, identity, model router, user memory, source stores, cache,
retry system, signal engines and trading authority are unchanged.

## Real machine consumer

`scripts/probe_public_research.py` is an operator qualification consumer. It
prints a machine-readable investigation receipt and exits fail-closed when no service
key is configured. It stores nothing and does no model synthesis.

The probe intentionally accepts only a PUBLIC query. This module is not a semantic
PII/secret classifier. The future server task resolver must create the minimal public
query and must never pass private portfolio text, notes, full conversations, account
identifiers, credentials or other tenant-private context to the external provider.

## Test-first evidence

Worktree:
`/Users/chriswong/Documents/Cluade/macro-main/.claude/worktrees/flagship-public-research-tavily-20260919-sol`.
Branch: `claude/flagship-public-research-tavily-20260919-sol`.
Creation base: `e2ab11d93cbba4605646eb13ee729b3433ea84c1`.

Baseline:
`tests/test_company_source_span.py tests/test_brain_exact_source_context.py`
-> 13 passed.

TDD sequence:
- missing module: 1 expected failing assertion;
- missing search implementation: 6 expected failures;
- search implementation: 7 total module/search cases passed;
- missing source-open implementation: 7 expected failures;
- search/open implementation: 14 total cases passed;
- missing investigation/CLI consumer: 6 expected failures;
- investigation/CLI: 20 total cases passed;
- hardening RED: canonical duplicate/result-limit, unsupported ultra-fast chunks and
  out-of-range scores each failed before repair; default redirect and injected-URL
  controls already passed once the test import was corrected;
- hardening repair: those five targeted cases passed;
- extraction content-scope RED: 2 failures before explicit
  `content_scope/full_document_reviewed` receipts;
- content-scope repair: those two cases passed;
- latest focused source-owner run:
  `tests/test_brain_public_research.py tests/test_brain_exact_source_context.py
   tests/test_company_source_span.py` -> **40 passed**.

No test made a Tavily network request. Search and extract provider calls use injected
fake transports. The operator probe is exercised without a key and proves a typed,
network-free unavailable receipt.

The new test file is registered in the existing `unrun-brain-gateway` CI job; no
new CI job or workflow was created. `run_ci_pack.py --validate-only` validated
214 legacy jobs, exit 0.

The differential contract-delta checker was started against this fresh base and is
a separate pending proof until its actual result is read. Do not infer success from
process start.

## Security / failure semantics

- Tavily API requests disable HTTP redirects at the service transport.
- Provider error text, raw response bodies, service keys and raw public queries are
  never surfaced in normalized error receipts.
- Search results can discover a URL but cannot claim the source was opened.
- Extract HTTP success with per-URL failures remains partial/unavailable as appropriate.
- A provider-injected URL not requested by the caller is ignored.
- Retrieved content is always `untrusted_content=true` and
  `source_instructions_authoritative=false`.
- Empty search is explicitly a coverage gap, not proof of absence.
- Provider-estimated publication date is labeled as such and remains subject to
  source-level event/publication/correction checks.
- Search provider scores outside [0,1] are not promoted as evidence strength.
- Identical canonical URLs are deduplicated before evidence selection.

## What is still unproven

- No Tavily service credential, account entitlement, budget or redistribution right
  is qualified by this source.
- No real Tavily search/extract request has run.
- Search quality versus Brave/Exa/OpenAI or another provider is unmeasured.
- No customer Brain tool or current-event task resolver is wired.
- No evidence-required resolver flag currently forces the model to investigate a
  current question.
- No model has consumed this public evidence or demonstrated evidence-dependent
  conclusion changes.
- No browser/customer workflow is proven.
- No private-data egress acceptance has been run against a real customer context.
- No legal/commercial ruling is made about retaining or redistributing provider
  results or source text.
- Nothing here grants signal, rank, size, gate, trade or forecast authority.

## Ordered continuation

1. Freeze this provider-qualification source and obtain independent technical review.
2. Conclude current-base contract/CI evidence on the exact source head.
3. Keep source DRAFT/HOLD if provider credential/budget/rights remain unqualified.
4. Through the existing service/workload owner, qualify one sanctioned Tavily service
   identity and cost envelope; do not use Chairman/developer-subscription credentials.
5. Run a small real canary that proves search execution, opened source content,
   partial/failure states and request receipts.
6. Compare the same frozen task set against at least one alternate provider before
   calling Tavily the production search backend.
7. Only after provider qualification and shared-gateway source custody are clean,
   expose provider-neutral search/open tools through the existing Brain dispatcher.
8. Add server-side evidence freshness/required-research state and prove a current
   company question changes or qualifies its conclusion when the material source is
   removed or changed.
9. Preserve bounded Published Research as zero-external-research.

Do not create another provider router, credential store, queue, retrieval cache,
research memory, transcript database or model planner while advancing these gates.

## Exact owning-suite integration proof

The first exact `unrun-brain-gateway` selector run returned 899 passed /
3 failed / 1 skipped. The three failures were not changed or skipped:
- `test_the_widget_handles_retract_and_the_pair_is_byte_identical` could not read
  `site/mm_brain.js`;
- two instant/context parity tests could not read
  `data/reference/security_master.parquet`.

Each failure explicitly identified the sparse checkout as omitting `site` or
`data`. The repository's own `scripts/worktree_sparse.py add data site`
materialized only those two trees. The identical selector was rerun unchanged.

Final exact existing Brain job selector:
**903 passed, 5 existing framework deprecation warnings, exit 0**.

This proves the new source/tests compose with the current owner suite in this
checkout. It is not hosted CI, a provider canary or customer acceptance.

## Current-base compatibility before source freeze

After the complete local source/test changes, origin/main moved once from the branch
creation base to `f986ecb2d6f9b5ed383819f7592ac692d7a5c590` via navigation PR #7360.
A bounded diff shows zero movement on all R1 candidate paths, including
`.github/ci/legacy-jobs.yml`. No ancestry-only merge/rebase is required to preserve
semantic correctness; exact-head hosted CI still owns current integration proof.

The local contract-delta process began before later hardening, continuation and CI
registration edits. Even if it returns, it is not final-head proof. Do not start a
duplicate while its existing process is unresolved; the PR's hosted exact-head
contract-delta gate will be authoritative for this frozen candidate.

## Independent review R1 and pre-canary hardening

One-shot read-only Opus review
`mastermind-ai-public-research-tavily-review-20260919-sol-001`
returned **PASS** on head `9a68fbd83736ac6b5fbc741ff4fa13d3077ff58f`,
with no blockers and one required pre-canary contract family.

The reviewer independently verified search/open separation, unsafe URL checks,
untrusted source semantics, partial/all-failed extraction states, no Brain wiring,
no duplicate control plane and the registered CI owner. It did not run pytest/git
or network requests.

### Provider-field verification

Current official Tavily Search docs were re-opened after review. They explicitly
document all three queried fields:
- `filter_by_published_date`;
- Search `include_usage`;
- and Tavily Extract docs explicitly document Extract `include_usage`.

The source therefore keeps those payload keys. This verification is documentation
evidence only; a real sanctioned provider canary remains required.

### Query privacy contract

The aggregate `investigate_public` call now requires exact
`query_scope="public_minimal"` before any provider transport. Invalid/missing scope
returns `invalid_public_research_scope` with zero transport calls. Every aggregate
receipt carries the scope marker.

The operator probe explicitly supplies `PUBLIC_QUERY_SCOPE`. This is an auditable
contract for the future server resolver; it is NOT a semantic classifier. The
resolver must still ensure that the actual query contains no private notes, portfolio
context, account identifiers, secrets or unnecessary conversation content.
`query_sha256` is correlation metadata only and is not a privacy control.

### URL/network boundary hardening

Abbreviated/hex/octal numeric IPv4 spellings such as `127.1.1`,
`0x7f.0.0.1` and `0177.0.0.1` are now rejected before provider transport.
Scheme-mismatched explicit default ports (`https:80`, `http:443`) are rejected
rather than silently rewritten.

This process still does not resolve caller-selected source DNS names locally.
Wildcard/rebinding-style public domain names remain the remote fetch provider's
network-safety responsibility. The application-side URL checks reduce obvious
unsafe inputs but do not falsely claim to prove Tavily's resolved destination.

### Result and machine-consumer semantics

Search now distinguishes:
- provider result count;
- rejected malformed/unsafe/duplicate rows;
- safe but unselected rows beyond the caller result budget;
- and bounded unprocessed provider rows beyond the adapter safety scan.

The operator probe now returns a distinct exit code 3 for partial source coverage,
rather than making partial and fully available investigations identical to scripts.

TDD before repair: 10 targeted review-follow-up tests failed.
After repair: those 10 pass. The complete R1 suite is now **37 passed**.

Fresh exact existing Brain owner selector after the review repair:
**913 passed, 5 existing warnings, exit 0**.
This supersedes the earlier903-pass source-head proof.

Because these changes alter R1 semantics after the first PASS, the repaired
immutable head requires a fresh exact-head review and fresh hosted CI before source
acceptance. The first PASS remains useful review history, not acceptance of changed
bytes.

## Exact-head re-review M1 repair — query scope is an attestation, not proof

The second exact-head Opus review of
`dbe6dfab8b7d3c213894943d20ca315cf470bd5d` returned **PASS with no
blockers** and one pre-canary major: receipts carried
`query_scope=public_minimal` but did not themselves say that this was an
unverified caller/server attestation. A downstream machine consumer could therefore
over-read the marker as proof that Mastermind had semantically removed private
context.

The repaired contract now carries:
- `query_scope=public_minimal`;
- `query_scope_basis=caller_attested_unverified`;
- and a receipt-local `limits` statement that the marker is a caller attestation,
  **not semantic privacy verification**.

The `investigate_public` docstring and gate comment state the same boundary. This
function still does not inspect PII, secrets, portfolio context or minimization
quality. That responsibility remains with the future server task resolver.

A valid `public_minimal` scope is now preserved even when later request bounds are
invalid, so receipts distinguish “scope attested but request malformed” from “scope
was refused.” A refused/missing scope has `query_scope=None` and
`query_scope_basis=None`.

TDD before implementation: three new tests failed:
- successful investigation lacked `query_scope_basis`;
- valid-scope/invalid-bounds receipt lost the scope marker;
- rejected scope lacked an explicit null basis.

After repair, those three pass. Full R1 qualification suite: **40 passed**.
Fresh exact existing Brain owner selector on the repaired source:
**916 passed, 5 existing warnings, exit 0**.

Current official Tavily docs were rechecked before this repair and explicitly
document Search `filter_by_published_date` / `include_usage` and Extract
`include_usage`; no source change was needed for those fields. Provider contract
still requires a sanctioned real canary before production qualification.

Because the aggregate receipt schema changed, the repaired immutable head requires
another exact-head independent review and fresh hosted CI. Earlier PASS reviews are
review history, not acceptance of the changed bytes.
