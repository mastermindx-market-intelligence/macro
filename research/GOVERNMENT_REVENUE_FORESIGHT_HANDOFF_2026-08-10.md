# Government Revenue Foresight — session handoff, 2026-08-10 ~13:00Z

Successor to `research/GOVERNMENT_REVENUE_FORESIGHT_HANDOFF_2026-08-09.md` (queue mechanics, §3 rulings, §6 worktree map — all still valid history) and to `research/GOVERNMENT_REVENUE_FORESIGHT_ACCOUNT_HANDOFF.md` (wave specs 10/11/12/13, unchanged). This document records the wave landing, the first issuance and its adjudication, and the corrected candidate timeline. **Re-census before acting.**

## §0 Where the program stands

1. **The entire 12-PR wave is MERGED** (2026-08-09 13:20–17:55Z): the reviewed defense19 graph (#5009), the three structural fixes (#5086 enum, #5085 snapshot families, #5090 action-rail identity), grader integrity + GRV_FA1 rail fence (#5038), ledger identity (#5040), 9E/9F (#5021), IDV bridge (#5014), SBIR (#5012), disclosure (#5013), amount guard (#4950). #5034 was closed superseded (main's hermetic construction + "never re-add a live ordering pin" ruling outranks it). #5131 healed the #4951 schema-requiredness red the same morning.
2. **The lobe HAS ISSUED; the immutable history stands, but active eligibility does not.** The 2026-08-10 04:15Z cycle (run 31354784751, commit 5fc18d5aac8) published the first 8 candidates: snapshot-rail HII, 4 `award_obligation_change` + 4 `award_ceiling_change`, `known_at` 2026-08-08T11:58:31 / `generated_at` 2026-08-10T04:15:07, ledger sha256 `920d840a…` (8 lines / 50,790 bytes). The projection state was absent in that run's root, so the frozen-clock anti-backfill guard never engaged — the rows would have been refused had state been present (a scratch dry-run on 2026-08-09 predicted refusal of these byte-identical observation IDs). Those exact ledger bytes remain permanent audit history.
3. **The adjudication (read before touching anything):** the exact eight issued identities are quarantined from active candidate surfaces by a separate, typed issuance-correction manifest. The application is an 8/8 exact source-and-row binding to commit `5fc18d5aac8`, the issued queue, projection state, and immutable ledger prefix. Active list/company surfaces contain zero incident rows; corrected-only detail returns structured `410 candidate_historical_issuance_quarantined`; history remains `200` with the original row and correction overlay. This is not deletion or a rewrite: the ledger remains byte-identical and append-only. #5247 and #5260 ultimately merged as zero-file ancestry reconciliations before the correction could land; those procedural no-ops did not adjudicate the product semantics and must not be treated as authority against the reviewed correction. The grader remains protected independently: these are snapshot-rail rows and GRV_FA1 is action-rail-fenced (#5038), so they abstain `family_rail_mismatch` and never grade.
4. **The incident's cause is closed on main** (via #5227, independent of #5247): a non-empty candidate ledger with no projection state now hard-raises (`non-empty candidate ledger has no projection state`) — the `prior_state=None` path that skipped the frozen-clock guard is unreachable except on a truly virgin root.
5. **Workspace truth post-wave**: `coverage.award_events.rejected == 0`, 610 validated / 500 visible (cap). The §1 gap-cycle transient (`rejected: 9`, 2026-08-09 13:28Z) occurred exactly as predicted and cleared with #5086's merge. The corrected projection is active 0 / quarantined 8 / appended 0; the ledger remains exactly 8 lines and 50,790 bytes.

## §1 Candidate timeline (dry-run-verified, then production-verified)

- The 8 first candidates match the composed-wave scratch dry-run of 2026-08-09 exactly (492 tests green; observation IDs byte-identical). Basis `source_record_recipient`, all HII — the 8-per-entity top-value sample concentrating recent modifications, as §4 of the prior handoff predicted.
- **The ~20 action-rail candidates arrive only after a post-#5090 COLLECT** — award-level identity (`award_recipient_*`, basis `award_level_recipient_at_collection`) is attached at collection time; accrued action rows carry none. Snapshot-only cycles are correct, not defective.
- A later observation may become active only when it has a genuinely new exact source identity and a forward clock. Candidate ID reuse alone never widens the eight exact correction tombstones. Zero new candidates on a cycle is still an honest result.

## §2 Rulings in force (unchanged + one addition)

All §3 rulings of the 2026-08-09 handoff stand (integration ruling, named-basis identity, GRV_FA1 rail fence, ledger identity, live-probe philosophy). The append-only law applies to the ledger, not to active eligibility: **issued rows remain immutable and audit-readable, while an exact, reviewed correction may quarantine them from active list, ranking, sizing, gating, Prophet, and signal surfaces.** Corrected-only detail is a machine-readable 410 that links to preserved 200 history; it is not deletion. Pre-issuance suppression remains a distinct contract for rows that have never been issued and must never be used to disguise an issuance correction.

## §3 Next moves, in order

1. **Nightly ~04:00Z collect, then one cycle:** verify that the exact eight correction identities remain quarantined and that only genuinely new exact source identities under forward clocks append. Expect the first action-rail candidates only once the collector's award-level identity lands on re-observed actions. Verify against §4 of the 2026-08-09 handoff (rejected==0 holds; grader admits action-rail only; snapshot rows abstain `family_rail_mismatch`; radar page renders with the authority block intact).
2. **Defense20 (BWXT)**: post-#4951 collects query the alias list; once BWXT recipients appear, run `scripts/propose_government_revenue_recipient_graph.py`, review the worksheet, publish via the curator citing the 2026-08-08 approval precedent (fresh operator ack preferred). If historical BWXT rows require non-issuance, create and review an exact pre-issuance suppression before materialization. Never improvise a blanket empty-ledger exception or reuse the HII correction manifest.
3. **Wave 10 rails 2/4/5**, **Wave 11**, **designer-lane surface pass** (SBIR/IDV/shadow-context labels, `semanticLabel` `delta_*` gaps, fact-grid `slice(0,4)`) — unchanged from the account handoff.
4. Small follow-ups still open: `ceiling_changed` sign-blindness; IDV prime-cut widening decision; the schema-requiredness re-tightening for `recipient_query_terms` (allowed only once the committed manifest carries it — check `freshness.award_events.ingest.coverage_manifest.entities[0]` first).

## §4 Traps this session added to the book

- **Name-based inherited-red exclusion admits new defects inside already-red packs** (#4951's 30 hidden failures; the GR3→GR0 import inversion same day) — diff head vs main failing-test LISTS before trusting any exclusion. Memory: `red-pack-hides-a-new-defect-behind-name-based-exclusion`.
- **A run-level "cancelled" is not a job-level fact** (daily.yml read as dead ×3 while 17/18 jobs succeeded).
- **Main-ref ci.yml/fences dispatches shared one cancel-in-progress group** — five baselines died serially before #5133/#5136/#5140; never dispatch while a proof is in flight.
- **Hosted-runner minutes are a fleet resource**: #5124's all-hosted move exhausted the free tier in hours and froze every ubuntu-latest job start (billing usage API is the diagnostic; the operator's spending limit is the lever).
- **A merged PR can be a deliberate no-op**: #5247 and #5260 had recovery ancestry but their final trees were reconciled to main and their squashes landed zero recovery files. A no-op proves only that no bytes landed; it is not a semantic adjudication and must never be cited as authority for the opposite product state. A PR body describes intent, while `git diff <merge>~1 <merge>` shows what actually landed.
- **`git checkout --theirs` takes the whole file, not the hunk** — it silently dropped a sibling amendment during a dry-run composition; resolve version-string collisions hunk-by-hunk, keep both amendment bodies.
- **A clean rebase can silently drop a sibling's landed edits** when branch commits rewrite regions the sibling touched — audit for main-lines absent from the branch, then clear suspects symbol-by-symbol (line counts over-flag refactors; the only true positive out of six suspects was zero after symbol checks).
