# Rotation W1: source acceptance, release still held

Decision owner: Sol, under current live Chairman authorization to implement the adaptive-rotation programme end to end. This record accepts the bounded local diagnostic implementation evidence; it does not accept production, a trading improvement, or the wider programme.

Implementation carrier: Macro PR7174, branch `claude/ssd-rotation-diagnostics-w1-20260915-sol-9178859f97e5e1b1`, immutable head `effdbd13cb78f2da3770733e85f64c93d395eb0d`.
Research/continuity carrier: existing PR7168. No implementation head is changed by this record, so its ongoing exact-head CI is not restarted for bookkeeping.
Protected procedure: Mastermind `7642aea155d2817219135b24246b55c1d7611c66`, compatible Skillpack1.0.1. That SHA identifies the separate procedure repository, not a Macro source-file or audit digest.

## Capability accepted locally

The existing admin Prophet tab and existing GET /api/prophet share one additive, read-only diagnostic adapter. Real local HTTP input and actual sidebar navigation demonstrated independent cutoff dates, historical ticker-match semantics, energy leader-only visibility, a non-energy control, and explicit unmeasured entry actionability/on-time conversion.
Evidence at the implementation head: `research/prophet_us_audit/rotation_w1_proof_2026-09-16/`.
Source audit bytes remained SHA256 `44ff637b623111d3061511aa368f1a5e457f1a4a85af6698cce650193a0f3151` before/after verification.

## Executed proof

- 147 focused source/renderer/integration/admin/inline-handler tests passed, with no skips.
- The real-tree JavaScript no-undef checker passed.
- Agent OS validation:1117 records, zero errors,97 pre-existing warnings.
- Contract-delta local result:0 introduced,0 inherited against `bd33e0340c7066673cfb81d17b57fab965623c5b`.
- The repaired manifest also passed hosted contract-delta on the exact head: run35053041597/job104657371942. Active main authority and trusted-executor admission passed. Executor packs remained queued when checked; source admission is not execution.
- Actual isolated Chrome browser at1440/390 widths: energy/control rows, differing dates, legacy disclaimer, unmeasured fields, no double-escaping, no digest clipping and no JavaScript exceptions. Unavailable/withheld/malformed/empty fixtures were separately labeled synthetic renderer checks.
- Existing admin is dark/English. No unimplemented light/Chinese mode or production result is claimed.
- Full changed-path diff from implementation pickup `142c8f6123b2a5da50e1ec95b6d0f7cb2167a009` contains only admin adapter/consumer/renderer, CI manifest, tests and research/handoff/proof. No engine, rank, admission, data ledger, plan, auth or runtime code was modified. Tracked working tree was clean at final readback.

## Independent checking and adjudication

An initial MiniMax helper ended with an unrecovered stdout result. Its lease was read back as released; no pass was credited.
A later MiniMax checker returned two alleged findings. The claim that backend visibility passed through a supplied source label was falsified by an executed in-memory counterexample at the reviewed head: `present_counts=0/0/3/0` with three members still yields `leader_only`, regardless of supplied visibility. The source computes classification at admin/prophet_diagnostics.py:117-130. The second point merely described intended actual-date precedence and was not a defect. Unsupported findings did not cause edits to correct code.
A separately admitted GLM native-pool code checker then returned `NO_REPRODUCIBLE_FINDING` at exact head `effdbd13cb78f2da3770733e85f64c93d395eb0d`, with18 reported in-memory behaviors, six commissioned source inspections, explicit scope gaps and a zero-write receipt. It independently reconfirmed count-derived visibility, null safety, five-date precedence, file/row bounds, duplicate rejection, historical-match semantics, no actionability authority, existing-consumer wiring and single-pass escaping. The parent-created PR-body draft was explicitly recognized as parent-owned and left untouched. Both final helper leases were verified released; no temporary helper watcher was commissioned.
Sol accepted this as bounded independent contract evidence, not a financial or release verdict. Its remaining scope gaps were supplemented by the parent changed-path diff and actual browser evidence. No broad repo-wide test pass is invented.

Nonblocking limitations remain explicit: the reader rejects final-component symlinks, not every symlink in an intermediate trusted repository directory; Python nonstandard NaN/Infinity input values degrade through strict field validation rather than universally raising a parse error; an arbitrary-value label helper fallback can over-escape but is unreachable from the adapter's closed visibility values and renderer validation. Do not market these as stronger guarantees than were tested.

## Remaining release gates

1. Hosted execution: trusted executor packs in run35053041597 await a `ci-linux` runner. Job104657720633 reported queued with no runner assigned. The repository-visible registry listed four macOS runners and none carrying ci-linux. This is not an exhaustive assertion about organization-scoped runner groups: the exact organization-group read was tool-blocked. Do not relabel a Mac, register another runner or switch execution planes as a bypass. The existing CI runner owner must establish permitted capacity and obtain actual pack/aggregate results.
2. Source-owner/current-base compatibility: the broader cross-PR census command was tool-blocked, not a successful no-collision check. Current integration/source-owner proof remains necessary. The GitHub integration also lacked access to the branch-protection read; no administrator override is authorized by this record.
3. Production acceptance: read-only SSH verified an active existing admin service and the normal updater's restart behavior. Scripted credential-based authenticated proof was blocked before execution. No credential/cookie was exposed, no production login or source deployment occurred. Use an approved authenticated proof path after lawful release. Local screenshots and an active old service are not production acceptance.
4. Distinguish unrelated/inactive statuses: Vercel reports its build-rate cap; the inactive pilot-authority context is not the successful active main context. Neither observation authorizes ignoring an actually binding gate.

Capability remains **BUILT_NOT_PROVEN**. Preserve Draft/HOLD, same source carrier, no merge-on-green or native auto-merge until remaining gates are adjudicated. This ruling supersedes the older WIP statements that the fixtures remain red, browser proof is absent and final contract checking has not returned; it does not erase those historical observations.

## Exact continuation

Restore/verify permitted ci-linux execution through the existing runner owner, obtain exact-head hosted and current-base/source-owner proof, then use the normal accepted merge/deployment path and real authenticated machine/browser verification. Do not redo the147 tests or local browser repair merely because the next chat is fresh; rerun only for material source/integration changes. W2's concrete source findings and next producer-to-consumer contract are in `ROTATION_W2_CLOCK_AND_EPISODE_INTEGRATION_2026-09-16.md`. No new adaptive trading model has been promoted.
