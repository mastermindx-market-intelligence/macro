# Rotation W1 local browser proof

This is local integration evidence, not a deployment or trading-performance claim.
Source code head: `368f8e50dadf6f3f1b020a534705827138b4cd20`.
The existing admin process served its real `/api/prophet` response; the browser used the actual Prophet sidebar control.
Energy Complex / equal-weight Energy are leader-only; AI Infrastructure is a non-energy control with buy/watch-lane presence. Entry actionability and timely conversion remain unmeasured.
The exact input audit digest is in `receipt.json`; it was identical before and after this read-only verification.

## What was checked

Desktop 1440 and mobile 390 widths, in the admin's actually implemented dark/English mode. The admin does not implement the main product's light/Chinese modes; no fabricated theme or language proof is claimed.
No JavaScript exceptions. The initial browser check exposed a clipped digest despite no document overflow; the repaired check measures the digest element itself. Table overflow is explicitly horizontal and described to the reader.
Source-unavailable, withheld baskets, malformed payload and genuinely empty baskets were injected only into the browser renderer. These are labeled synthetic failure checks, not production observations.
`147` focused and inline-handler tests passed. The real-tree JavaScript scope checker passed. Agent OS validation reported zero errors and 97 pre-existing warnings.

## Reproduction

Run the existing admin server from the accepted source checkout: `python -m admin --port 18787`.
Then run `node research/prophet_us_audit/rotation_w1_proof_2026-09-16/verify_browser.mjs` on the authorized Mac with installed Chrome. The verifier creates its own isolated headless profile and stops only its own browser process. No provider profile, typing, clipboard, or Tab traversal is used.
Restart the owned test admin process after asset changes: the observed running process retained old asset bytes. Recheck actual served bytes after production deployment; a merge or file copy alone is not proof.
All production, model, ranking, admission and ledger behavior remains outside this proof.
