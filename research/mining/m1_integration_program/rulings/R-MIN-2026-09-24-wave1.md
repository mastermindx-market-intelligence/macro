# R-MIN wave-1 rulings — GMI Mining M1 integration (Fable Meta-CEO seat 664a0650)

Operation key `gmi-mining-fable-ceo-m1-integration-20260924-chairman-001` (prepared on #7795; made live by deliberate Chairman delivery of `MINING_MASTER_FABLE_INTEGRATION_PACKET_2026-09-24.zip` on 2026-09-24). Authority: that delegation ("initiate as Fable Meta-CEO … finish end to end … fabric for all subagent work … Opus/Fable children as sub-orchestrators/auditors … fan out to macbook/m1/minis … conserve tokens") + the Astra/Chairman base-ownership ruling (~07:50Z: Semiconductor B #7870 owns the shared base; sector programs integrate later). Frozen inputs on `origin/sol/mining-principal-research-20260923` @ `eb6f05c0`: plan blob `a2fca1c5`, mandatory addendum `053a59e2`, design `33d94481`, witness qualification `34548e6b`, trace `37ccaa92`. Pickup recorded on #7795 comment 5811520293; START is recorded per admitted scope, never implied.

These rulings bind every Mining packet. They add no gate service, no second control plane and no new permission; they narrow the frozen plan where the addendum or the current code requires it.

| Ruling | Decision | Why / evidence |
|---|---|---|
| R-MIN-00 | Mining mints NO shell, evidence, rights, route, publisher, client or mount vocabulary. Every Mining task extends the incumbent owners only: Company Intelligence (`engine/company_intelligence/*`), Earnings receipts (`engine/earnings_release/receipts.py`, unchanged), and ONE per-sector market-ontology module. T05/T06 wait for Semiconductor B's route/client/mount to be on `main`; G2 real-source admission is an incumbent-intake act. | Addendum §2 (original T01 is consumption, not a Mining base build); Astra ruling; #7870 @ dd5076fa still has only `semiconductor_theme_research.v1` and no `app/theme_research.py` on main. |
| R-MIN-01 | Carrier model = one PR per task off fresh `origin/main`, squash-merged in dependency order. #7795 stays Draft/HOLD and docs-only (never product code). Continuity = `agentos/handoffs/GMI-MINING-2026-09-24-m1-integration.md` + `WS:GMI-MINING-M1-INTEGRATION` (child of `WS:GMI-THEME-GRAPH`) on `main`; rulings live in this file. Coordination posts: pickup/START/checkpoints on #7795; #7870 only for a NEW seam question (5809893850 is never repeated). | Industrials R-IND-01 precedent; Energy's single HOLD carrier #7881 became unmergeable. |
| R-MIN-02 | ONE gate:code job `mining-economic-dossier` in `.github/ci/legacy-jobs.yml`, created by T01' from the `prophet-us-b4-prereg-registration` recipe (`if: ${{ false }}`, gate code, scope exclusive, explicit `paths:` = every Mining file + every Mining test in `run:`), appended at the END of the file (contested by ≥6 open PRs), plus the `CURATED_EXCLUSIVE` entry in `tests/test_ci_pack.py`. Install line probed in a clean venv before push (CI DEPENDENCY LAW: a copied recipe carries the donor's deps). Every later Mining test file is wired into `paths:` AND `run:` in the PR that adds it. | CDV-1 #7905 ci-pack-8 red (pyyaml); the 09-24 contract-delta refusal on #7783 (test in `run:` without `paths:`). |
| R-MIN-03 | Synthetic fixture law: the casebook's 11 closed names (`copper_complete`, `rare_earth_complete`, `missing_basis`, `missing_issuer`, `source_only`, `missing_stream_threshold`, `changed_source`, `signed_loss`, `same_horizon_revision`, `denied_source`, `page_generation_change`); every fixture `synthetic: true`, invented issuers (e.g. "Ardent Copper Holdings", "Halide Rare Earth Corp"), `example.invalid` sources, invented CIK-shaped ids that are NOT real CIKs; no live Freeport/MP figure in any fixture. The real CIKs `0000831259` (FCX) and `0001801368` (MP) appear only in the T02 profile factories' identity constants. No real company is marked RESOLVED; no research value becomes a native receipt. | Plan T02 casebook contract; LAW.md fixture law; witness qualification is research, not data permission. |
| R-MIN-04 | Labor on external fabric lanes (GLM-5.3 via `glm-codex`; mini2 preferred — 3/3 success on 09-24 vs 2/2 degenerations on m1; MiniMax-M3 with `review_engine: seat` as fallback on m1/mb), commit-and-push-per-step ANTI-COLLAPSE law in every packet, Opus READ_ONLY red-team of every task PR before a dependent branches, and reviewer probes frozen as RED tests before any repair lane. The seat keeps readiness, labels, merges, carrier posts and credentials. | Chairman fabric order (09-14/09-17) + this delegation; `glm-fix-lane-degenerates-on-long-single-exec-commit-per-step`; `freeze-reviewer-probes-as-red-tests-before-a-repair-lane`. |
| R-MIN-05 | Ordering: T01' → (T02 ∥ T04a) → T03 → T04b → T07 → [Semiconductor B route/client/mount on `main`] → T05 → T06 → T08. T02 branches only after CDV-1 #7905 has merged (it rewrites the `issuer_profiles.py` lookup seam); T04a = the closed Mining definitions + own schema + synthetic composition, T04b = the integrated positive witnesses on T03 inputs. Parallel pairs are path-disjoint. | Plan §11; Industrials R-IND-05/R-IND-12. |
| R-MIN-06 | G1 consumption: Mining creates no `engine/market_ontology/theme_research.py`, no generic response schema, no route, no publisher, no client. The Mining composition is a per-sector module + schema on the accepted per-sector precedent (Tech ex-Semis #7891 / Industrials R-IND-17). If/when the shared owner delivers a generalized kernel, the Mining wrapper delegates to it and the original extraction steps are DO_NOT_REDO. Until the shared POST route exists on `main`, the T01' harness client returns a typed `route_unbound` refusal with zero protected reads. | Addendum §2 + IR-06; R-IND-06/R-IND-17. |
| R-MIN-07 | IR-01/IR-02 consumer rules are tested, not just stated: a `definition_unqualified:*` warning coexisting with a numerically favourable comparison yields NO badge, headline or confirmed-surprise claim while both values stay inspectable; fully qualified compatible inputs are NOT suppressed; W-C management's earlier point estimate stays a point estimate (no invented bounds, no Street label); W-R quarter/half-year, elimination sign and GAAP/non-GAAP basis travel with every literal; derived arithmetic binds every consumed revision. | Addendum §4 IR-01/IR-02; MGD-16/22 strengthened. |
| R-MIN-08 | IR-05 source use: filing facts carry the incumbent rights profile the release path already assigns; no full issuer/exhibit body is republished; factual synthesis, permitted spans and links are allowed. `config/theme_sources.yml` (`sec_edgar` family) is read, never edited, by Mining. | Addendum IR-05; #7870 comment 5810041817. |
| R-MIN-09 | Records: M1 acceptance is recorded separately from full Mining completion (IR-07). Every task PR's return names carrier/head, MGD rows covered, commands with rc, receipts, unresolved effects and the next bounded action; a copied test count is historical, never a run. | Handoff §7–§8. |

## Seam rulings (from the Opus READ_ONLY seam audit)

Sources: `research/mining/m1_integration_program/reviews/OPUS_SEAM_AUDIT_A_COMPANY_2026-09-24.md` (Company-Intelligence / receipts seams; T02/T03/T07) and `OPUS_SEAM_AUDIT_B_ONTOLOGY_2026-09-24.md` (shared-base / market-ontology seams; T01'/T04/T06). The first single auditor died at its turn limit with no file; the two scoped auditors replaced it. Each ruling binds the packets of the tasks it names.

| Ruling | Decision | Audit basis |
|---|---|---|
| R-MIN-10 (T02) | The T02 fabric packet is SYNTHETIC-ONLY. Real SEC byte acquisition (plan §4 bullet 1), original-body retention and receipt/availability clocks are an incumbent-intake/owner step held behind G2 + IR-05; no lane acquires, fetches or retains a real Freeport/MP body. | Audit A F1: the frozen corpus itself records no original bytes were obtained; addendum G2/IR-05 open. |
| R-MIN-11 (T02/T03) | `engine/company_intelligence/mining_issuer_profiles.py` mirrors the pg idiom exactly: imports only `TypedAbsence, text_span` (documents), `IssuerRegistry` (event_workspace), `IssuerIdentity, ListingAlias, company_id_for_cik` (identity), `IssuerProfile, _no_guidance` (issuer_profiles), `BoundRelease, bind_release_document` (binding), `ReceiptError, SpanReceipt, receipt_for_literal` (receipts); never `issuer_profiles`' module-private helpers. Signatures: `FCX_CIK`, `MP_CIK`, `MINING_TICKERS`, `fcx_issuer()`, `mp_issuer()`, `mining_private_registry()`, `fcx_profile(*, fiscal_scope)`, `mp_profile(*, fiscal_scope)`. | Audit A seam table; pg_profile.py:15-22,139-262@pr/7905. |
| R-MIN-12 (T02) | FCX/MP resolve ONLY under `publication="private"`: the `issuer_profiles.py` edit is ONE contiguous `_MINING_ISSUER_FACTORIES`/`_MINING_PROFILE_FACTORIES` block plus one branch in `profile_for_ticker` appended AFTER the PG branch (requires `fiscal_scope`, ValueError otherwise, PG wording); no public/default resolution; homebuilder dicts untouched. T02 branches only after #7905 merges. | Audit A F3; issuer_profiles.py:1293-1312@pr/7905. |
| R-MIN-13 (T02) | Selector law = heading → row label → column header (`_table`/`_column` shape); receipt window = that cell's `source_span`; `receipt_for_literal(source=bound.source, source_sha256=bound.revision.source_sha256, search_start=cell.source_span.char_start, search_end=cell.source_span.char_end, literal=cell.text.strip())`; a `ReceiptError` → typed absence, never a fallback match; document-wide windows forbidden. Tests prove the document-wide window REFUSES a duplicated literal and the cell-scoped window MINTS. | Audit A F4 (executed: receipts.py:253-259 refuses duplicates); pg_profile.py:150,171-183@pr/7905. |
| R-MIN-14 (T02/T03) | Sign, unit, scale, basis and period ride the fact row (`event_fact.v1`: fact_id, event_id, metric, value, unit, period, basis, source_span), never the receipt (a receipt replays the raw literal, no normalized value) and never a display string; `parse_mining_literal(value, *, unit)` returns a negative float for parenthesized negatives; signed facts use the Company/financial route, the nonnegative physical-observation slot is untouched. | Audit A F5 (executed replay of `(20,296)`); pg_profile.py:116,198-209@pr/7905. |
| R-MIN-15 (T02/T03/T07) | Typed absences use ONLY the closed 14-reason `ABSENCE_REASONS` (documents.py:72-87): ambiguous/duplicate/unaddressable cell → `no_span_addressable_evidence`; unit/definition mismatch → `missing_units`; no period column → `missing_period`; basis absent → `missing_basis`; body not held → `document_bytes_not_held`; accession unjoinable → `unjoinable_filing_identity`. `missing_derivation`, `interpretation_stale`, `stream_threshold_unknown`, `definition_unqualified:*` are composition LIMITATION strings (T04), never absence reasons and never Company-owner outputs. | Audit A F6; TypedAbsence.__post_init__ raises on any other word (documents.py:508-512). |
| R-MIN-16 (T03) | IR-01 binding while `guidance_history.py` is absent on main: ONE module constant `DEFINITION_UNQUALIFIED_PREFIX = "definition_unqualified:"` and `MANAGEMENT_SEQUENCE_SCHEMA = "management_sequence_assessment.v1"`; `assess_management_sequence` is called only through a guarded import and only when a real `next_outlook` exists; W-C (prior point estimate + actual, no outlook) NEVER calls it and returns a typed `projector_unbound` refusal alongside both reported values; no invented range/midpoint/badge. | Audit A F7; guidance_history.py:25,226,294-296@pr/7870 (`next_outlook` positional). |
| R-MIN-17 (T02/T03) | `fact_id` derives from `(event_id, metric, period, basis)` from the first line — never `f"fact_{metric}"` (the sibling's open probe defect); the Mining suite carries the two-event distinctness and borrowed-id refusal tests. | Audit A F10; probes:498-515@pr/7905. |
| R-MIN-18 (T02) | A private span needs a Mining rights token: `MINING_PRIVATE_RIGHTS_PROFILE` declared in `mining_issuer_profiles.py` and REGISTERED in `engine/company_intelligence/qa_exchange.py` (the only file allowed to declare a rights token) in the same PR, with `qa_exchange.py` in the CI job's `paths:`; filing-edition (10-Q) spans carry `rp_unknown_v1`-class handling until the rights owner qualifies the use. | Audit A F12; probes:547-565@pr/7905; R-IND-13. |
| R-MIN-19 (T02/T03/T07, CI) | The CI job is minted by the FIRST Mining code PR to land (T01' if it lands first, else T02) from the `prophet-us-b4-prereg-registration` recipe, appended at the END of a freshly fetched main; `paths:` lists every consumed owner file the audit enumerated (mining_issuer_profiles, issuer_profiles, documents, identity, event_workspace, event_workspace_build, events, contracts, qa_exchange, binding, receipts, filing_key, fundamental_forensics/disclosure_diff, and every Mining test/fixture/casebook file) and every test in `run:`; later Mining PRs APPEND to that block; never touch the PG job's block. `production_registry()` lives in `event_workspace.py:180`; `DISCOVERY_TICKERS` lives in `scripts/refresh_event_workspaces.py` on #7870 only — the "no public enrollment" prohibition names those real owners. | Audit A F8, F9. |
| R-MIN-20 (T03/T07) | T03's suite asserts on Company-owner fact rows from `fcx_profile()`/`mp_profile()` (no import of `engine/market_ontology/*`); every `compose_mining_research` assertion belongs to T04. T07 owns only `tests/test_mining_updates.py` (+ fixtures): proofs = A→B→A keeps three revisions; unchanged A mints no generation and re-applies `prior_lifecycle_state`; unknown version refuses; unchanged v1 replay byte-equal; changed source with stale interpretation refuses/tags; later mapping gated by system-recorded cutoff (date-only labels never gain `Z`); incumbent (homebuilder/Apple) outputs and public discovery frozen BEFORE Mining registration and byte-equal after — expected outputs are never regenerated from the candidate. | Audit A F2, F11, T07 spec; event_workspace_build.py:118-145; documents.py:236-303. |
| R-MIN-21 (T01'/T04) | The plan's T01 base-build text (§2 `theme_research.py — proposed shared`, §3 "Extract the shared kernel once", `compose_theme_research`/`select_theme_evidence`) is SUPERSEDED by addendum §2; every Mining packet carries a literal DO-NOT-CREATE list: `engine/market_ontology/theme_research.py`, `contracts/market_ontology/theme_research.v1.schema.json`, any `app/**`, any `site/assets/js/**`, any edit to `semiconductor_theme_research.py`, `engine/theme_graph/**`, `config/theme_sources.yml`. | Audit B F1, F9 (shared POST route family exists on neither ref; declared only at the mount template and the JS client). |
| R-MIN-22 (T04) | The shared kernel returns an UNVALIDATED dict (no jsonschema in its 1072 lines); Mining owns `validate_mining_research(payload)` on the #7891 precedent idiom (`validate_dossier`, lazy jsonschema, own CONTRACT_PATH) and calls it on every return path. | Audit B F2; semiconductor_theme_research.py:967-1015@pr/7870; technology_economic_change.py:298-318@pr/7891. |
| R-MIN-23 (T04) | `limitations` is closed but colon-aware: `anyOf` [enum of the closed bare codes] + [pattern `^(definition_unqualified|assertion_invalid|omitted):[A-Za-z0-9_.-]+$`]; a bare `definition_unqualified` without its `:field` suffix is a test failure. | Audit B F3; addendum §1/§4 IR-01; kernel mints `assertion_invalid:<i>`/`omitted:<name>` (:278,:287@pr/7870). |
| R-MIN-24 (T01'/T04) | Query/bundle types are DEFINED LOCALLY (`MiningResearchQuery`, `MiningOwnerBundle`, mirroring SEM:96-118 field-for-field) and never imported from #7870; the shared assertion contract is probed lazily behind a callable guard, degrading to typed `shared_contract_unavailable`. Mining owns its refusals: `unknown_slice`, `slice_theme_mismatch`, `limit_out_of_range`, `offset_negative`, `expected_generation_required`, `replay_cutoffs_required`, `generation_changed`; `True` is not an int for limit/offset. Entry name `compose_mining_research(query, bundle)` is frozen; plus `select_mining_evidence(query, bundle, assertion_ref)`. | Audit B F4/F5; the kernel validates neither slice nor anchor (:171-181@pr/7870); #7891 defines its dataclasses locally (:170-226). |
| R-MIN-25 (T04) | Schema = the same 14 closed top-level keys as the semiconductor contract (`schema, definition_version, generation, request, native_subjects, summary, companies, industrial_views, economics, expectations, evidence_refs, authorized_coverage, limitations, authority`), all required, `additionalProperties: false`; `authorized_coverage.industry_total` is `{"type":"null"}`; there is NO `neighborhood` key (plan §6 is wrong); `economics` adds `native_blocks` for `reported_economic_context`; `authority` is the five-key all-false const echoed on every response and evidence object. | Audit B F10, spec §4; schema :239-254@pr/7870. |
| R-MIN-26 (T01') | CI job minted by T01' with B's block (donor legacy-jobs.yml:13451; append at EOF of a freshly fetched main; `scope: exclusive`; paths ⊇ run line; install `pytest jsonschema pyyaml` measured in a clean venv); `run:` names only tests that exist at that head; later PRs append. The #7891 precedent appended steps to an existing job — Mining follows R-IND-19's new-job form instead. Pin the unmerged base with a STRICT xfail (never a skip) where a test depends on #7870-only behaviour. | Audit B F11, F12; test_ci_pack.py:3508/3765. |
| R-MIN-27 (T06 — HELD, seam question raised on #7870) | The delivered mount cannot carry a colon-bearing canonical id (`_theme_research_mount.html.j2:26` rejects chars outside `[a-z0-9_]` while both Mining anchors are `theme:…` ids), the client binds exactly ONE mount per page (`theme-research.js:345` querySelector on a hardcoded `id="theme-research"`), and copper carries `primary_basket_id: null` (crosswalk:279) so W-C has no basket page. Mining raises these ONCE as a consumption question; T06 stays held until the owner answers; no Mining mount, id grammar or basket is minted. | Audit B F6/F7/F8. |
| R-MIN-28 (records) | MGD map: T01' = MGD-01, MGD-04; T04 = MGD-08/09/10/11/12/18/34/39 (`tests/test_mining_composition.py`); T07 = MGD-24/26/27/28/35 (`tests/test_mining_updates.py`); every trace row stays NOT_RUN until its real product test executes on the qualified candidate. `synthetic_case` name kept (plan §4); R-IND-21's local ≤10-line loader adopted only where a suite would otherwise import an unlanded helper. | Audit B §6, F13. |
| R-MIN-29 (T01' review) | The Opus R1 red-team of #7932 (`reviews/OPUS_T01_PR_REVIEW_R1_2026-09-24.md`, REJECT: 2 BLOCKER / 5 MAJOR) is consumed by the freeze-then-repair law: the reviewer's probes are committed verbatim and RED (`tests/test_mining_shared_contract_probes.py`, 96c930ec, 9/10 failing) before any repair; the repair (41919d26) mirrors the shared candidate's predicates exactly — limit 1..100, `expected_generation_required` only for a paged read (`offset > 0`), `replay_cutoffs_required` only under `system_replay`, `generation_changed` only against a stated generation — and degrades the shared-contract probe through the TECH try/except-ImportError idiom probing both `validate_assertion` and `curation_revision`, two-armed on the unmerged base (never a skip, R-MIN-26). Seat amendment to the F2 probe: `sys.modules["engine.theme_graph.curation_assertion"] = None` forces the import to fail so the probe stays meaningful once #7870 lands. | A wrapper whose predicates differ from the kernel cannot delegate to it without a behaviour change (R-MIN-06); T02/T03/T04/T07 build their query fixtures against these bounds. Proof at f6650967: 67 passed in a clean venv from the job's own install line (R-MIN-02), contract-delta 0 introduced, `test_ci_pack` curated tests pass. |
| R-MIN-30 (ontology) | A signed loss is a retained signed native block, never an omission: `positive_witness` leaves the closed omission vocabulary, `signed_loss.json` declares no omission and is `research_usable`, and `expected` is read and cross-checked (a retained signed block ⇔ an empty omission set; exactly one of `signed_native_blocks` / `reported_only_economics`). The frozen `OMISSION_TO_LIMITATION` table maps every omission word onto the T04 limitation vocabulary — `reporting_basis→missing_basis`, `issuer→missing_issuer`, `economics→source_only`, `stream_threshold→stream_threshold_unknown`, `source_revision→changed_source`, `next_period_outlook→missing_derivation`, `source_rights→denied_source`, `page_generation→page_generation_change` — and a test pins the two vocabularies disjoint. An undeclared account-generation mismatch in a revision tuple is refused as malformed. | Opus R1 F6/F5/F10/F11: `research_usable` had been overloaded to mean both "no omissions" and "no usable research" for the exact case IR-02 protects (sign preservation); T04 would otherwise have to invent the omission→limitation correspondence. A same-horizon revision is not an estimate-vs-actual pair, so its comparison has no derivation (`missing_derivation`). |
| R-MIN-31 (T04a oracle) | Frozen probes that assert only counts and flags are not an oracle: every Mining composition task carries a **seat-authored exact truth table** naming, per casebook case, `research_usable`, the native-block count, the expectation-row count and the EXACT limitation set (`result["limitations"] == sorted(expected)`), plus a negative value on `signed_loss` and the absence of any `subject:unknown` row identity. Expectation rows come from a `financial_packets` entry of kind `management_estimate_vs_actual` carrying `pair` and two legs (`earlier_point_estimate`, `later_actual`, each `{value, unit, perimeter, basis, period}`); they are supplied IN-TEST, never invented by the module. A usable copper case with no such packet therefore publishes zero rows and mints `omitted:expectations`. | The T04a lane passed round 2's frozen probes by satisfying their letter: `_leg()` filled each comparison leg's `value` from `period_kind`, so both legs of every W-C comparison published the string `"quarter"` while the probes — which counted rows and checked `is_range`/`is_consensus` — went green (`reviews/OPUS_T04A_PR_REVIEW_R2_2026-09-24.md`, REJECT: 1 BLOCKER / 5 MAJOR). Two rounds were spent that way. The truth table is frozen RED at `a848ad54` (`tests/test_mining_composition_truth_table.py`, 17 failing / 1 passing with the amended R2 probes) BEFORE the round-3 repair lane ran, and the repair lane may not edit it. Round 3 delivered at `d401eebb` with real numeric legs and derived polarity. |
| R-MIN-32 (probe amendment) | An independent reviewer's probe binds absolutely on SEMANTICS. When its factual premise about a frozen fixture is REFUTED, the seat amends the probe **in the open** — intent preserved, assertions strengthened, nothing relaxed — rather than letting the module fabricate data to satisfy the letter. The amendment is recorded in the probe's own docstring and in the file header, and the repair lane still may not touch the file. Corollary: read the CASEBOOK CONSTRUCTOR, not just the fixture keys, before ruling on what a fixture contains. | R1 BLOCKER-1 (`test_probe_blocker1_management_pair_is_actually_composed`) drove off `synthetic_case("copper_complete")` alone and demanded two estimate-vs-actual comparison rows. False premise: `tests/mining_casebook.py:76` builds `financial_packets` from `fixture["economics"]`, and copper's `economics` is a REPORTED measure (`measure`/`value`/`basis`/`stream_threshold`) with no `earlier_point_estimate`/`later_actual` — so no row is composable from it without fabricating both legs, which is exactly the R-MIN-31 defect. Amended at `4a6a70f68bd` onto two in-test R-MIN-31 §3 packets: every original assertion kept (two distinct rows, comparison text present, `is_range`/`is_consensus` false) and the pins the frozen letter allowed through ADDED — each leg numeric and not `bool`, the two legs of a pair distinct, each actual equal to the literal value paired with its estimate, both pairs composed exactly once, polarity following the numbers (`1700→1680 = below_estimate`, `1.55→1.62 = above_estimate`). Six Mining suites then 160 passed / 0 failed in a clean venv. A separate MEASUREMENT ERROR is recorded against the seat: `fp=0` had been read by looking for a top-level `financial_packets` key in the fixture JSON, a field the casebook synthesises. |
| R-MIN-33 (T04a comparability) | A comparison is composed only from legs the input shows to be **commensurate**. The frozen domain spec already required it — "fully qualified **compatible** inputs are compared normally" — but only the qualification (presence) half was implemented through round 3, so `unit`, `perimeter` and `period` were carried by every packet leg and read by nothing. `COMPARABILITY_FIELDS = (unit, perimeter, period)` must AGREE between the two legs, else the row is withheld with `omitted:expectations`; a field absent from a leg is untouched, so `definition_unqualified:<field>` diagnostics are unchanged. `basis` is deliberately EXCLUDED: the domain yaml gives the two legs different bases on purpose (a point estimate against a reported measure), so cross-leg equality would forbid the only comparison the slice exists to make — the domain owns that published label and the packet's `basis` is a presence token. Unit CONVERSION and a real period comparator (FY guidance against a quarterly actual) are the producer task's widening. | Measured at head 94e74dd8: `1700 Mlbs` vs `1680 kt` published `below_estimate` (1680 kt ≈ 3,700 Mlbs — the actual beat the estimate about twofold, so the polarity was arithmetically inverted, not merely unverified); `consolidated` vs `proportionate` published `below_estimate` AND labelled the proportionate figure `consolidated_copper_sales` from the domain yaml, the exact mixing the closed vocabulary forbids; a `Q1 2026` estimate vs a `Q4 2025` actual published `below_estimate` while the row asserted `comparison_kind: earlier_point_estimate_vs_later_actual`, a temporal claim the input contradicts. All three shipped with `limitations: []`, so no consumer could detect them. Probes `tests/test_mining_composition_probes_r3.py::test_probe_r3_a_{unit,perimeter,period}_mismatch_is_refused`. |
| R-MIN-33a (no fabrication by arbitration) | Two packets naming the same `pair` are a **contradiction, not a choice**: every row's identity (metric / basis / source_label) comes from the domain yaml keyed by that name and no packet field but `value` reaches the wire, so same-pair packets are identical in identity and differ only in magnitude. Picking one (first, last, largest) asserts economics the input does not rank — fabrication by arbitration — and emitting both under a limitation code does not cure it, because a limitation annotates and never retracts. Every row for a duplicated pair is withheld, scoped to that pair alone so a duplicated `sales` never suppresses a sound `unit_net_cash_cost`. | Measured: two `sales` packets (1700→1680 and 9999→1) produced `rows=2, limitations=[]`, asserting that the same metric, same issuer, same period was both 1700 and 9999 with nothing on the wire able to disambiguate. An unflagged definitional contradiction is worse than the round-1/round-2 fabrication, which at least left the limitation vocabulary intact. Probes `::test_probe_r3_duplicate_pair_withholds_every_contradicting_row` and `::test_probe_r3_duplicate_pair_does_not_suppress_an_unrelated_pair`. |
| R-MIN-33b (unrepresentable truth is refused, never flattened) | When the response schema cannot express the truth, the lawful act is REFUSAL, not the nearest expressible claim. `is_range`/`is_consensus` are pinned `const: false` by the schema (frozen R1 probe MAJOR-4), so a source-declared range or consensus has no home on this wire; it must withhold with `omitted:expectations`, never ship as `is_range: false` beside `comparison_kind: earlier_point_estimate_vs_later_actual`. The flags are therefore INPUTS to be read — on the packet or on either leg — not constants. Corollary, and the reason withholding is the only option elsewhere too: `comparison` is `minLength: 1`, so a row carrying values without a comparison word is not representable, and an unqualified leg must withhold rather than emit a wording-free row. | Measured: a packet declaring `is_range=True, is_consensus=True` shipped a row with both false and `limitations: []`, while a structurally range-shaped value (`{low, high}`) was already correctly refused — the module refused a structural range and mislabelled a declared one. Hardcoded constants also mean the schema distinction can never be expressed in either direction, so `is_range: false` carried no information downstream. Probes `::test_probe_r3_a_declared_range_or_consensus_is_refused`, `::test_probe_r3_a_declared_range_on_a_single_leg_is_refused`, and `::test_probe_r3_absent_flags_still_compose_so_false_is_earned_not_assumed` (the last distinguishes a field that was READ from a constant — pinning `false` against inputs that are never ranges cannot). |
| R-MIN-33c (a numeric gate must exclude non-finite reals) | `isinstance(v, (int, float))` with a bool guard is NOT a number check: `float("nan")` and `float("inf")` both pass it. NaN then also slips the equality withhold, because `nan == nan` is False, and every NaN comparison being False drives the polarity ternary into its else branch. Every numeric admission gate in this program requires `math.isfinite`. | Measured: `1700 → nan` published `below_estimate` with `nan` as the economic value and `limitations: []`; `nan → nan` did the same, defeating even the equality guard; `1700 → inf` published `above_estimate`. Same failure class as the string and bool legs the guard correctly refused, leaking through the one type it admitted. Found by the independent auditor, not the seat. Probes `::test_probe_r3_non_finite_values_are_refused` and `::test_probe_r3_the_numeric_guard_admits_only_finite_reals`. |
| R-MIN-33d (an oracle that pins one case per branch is gameable) | A probe supplying exactly ONE value pair per pair name with ONE expected polarity is satisfiable by `{"sales": "below_estimate", "unit_net_cash_cost": "above_estimate"}[pair]` — a lookup table that never compares numbers, i.e. round-2's letter-gaming surviving the round-3 oracle. Every polarity, sign or branch pin must therefore carry a REVERSED case whose expected answer flips, and every boolean pin must carry a case in each state. This closes the gap R-MIN-31's truth table left open. | Found by the independent auditor as a hole in the SEAT's own R-MIN-32 amendment, not in the lane's code: the module at 94e74dd8 does compute polarity correctly (reversed legs flip `below_estimate`↔`above_estimate` when probed live), so this is an oracle defect with no current exploitation — pinned before it acquires one. Probe `::test_probe_r3_polarity_follows_the_numbers_in_both_directions`. |
| R-MIN-33e (review tactic — two no-delta cycles change the tactic, not the reviewer) | Two consecutive independent Opus red-teams of this artifact returned NO verdict, each exhausting its turn budget before reaching judgment (the second: 29 tool calls, 150k tokens, three sentences, all of them "now I'll read X"). A commission naming an artifact by path is a DISCOVERY task with a review attached, and discovery is what the budget buys. Under the change-tactic invariant the fix is not a third identical spawn: the SEAT runs the adversarial battery itself against the live module, then delegates only the scoping/severity judgment with the code excerpt, the measured outputs and numbered candidates INLINE — which is also precisely the half that needs independence when the finder will be the repairer. Third attempt: zero tool calls, 65k tokens, 3.7 minutes, complete rulings, three seat severities upgraded and two defects the seat had missed. | The seat's own grading is never the ruling: the auditor upgraded S2/S3-unit/S3-perimeter from MAJOR to BLOCKER, denied the T04b deferral argument by observing that the module already validates packet content (`_value_is_numeric`, the equality withhold, `missing_fields`) and had simply skipped the four fields carrying the domain law, and found R-MIN-33c and R-MIN-33d unaided. The seat departed from exactly one of its rulings (cross-leg `basis` equality) on domain evidence, and pinned that departure as a test so it cannot be silently reversed. |
| R-MIN-33f (proof mechanics) | A CONFLICTED pull request is an **unprovable head**, not a slow one: `ci`/`fences` trigger on `pull_request`, which checks out the MERGE REF, so GitHub cannot create either run while `mergeable_state` is `dirty` — no push, no `gh pr ready`, and no close/reopen can schedule them. The diagnostic tell is TRIGGER-FAMILY ASYMMETRY: `pull_request_target` workflows (here `ci-authority`) read the BASE and keep firing on every event, so the PR looks alive while its only proof lanes are structurally absent. Therefore: before waiting on — or arming a watcher against — any check, assert `mergeable != false`; and read `pending == 0` as green ONLY once the expected pack set is PRESENT, because an incomplete check set and a complete one are indistinguishable by pending count alone. Corollary for this repository: `.github/ci/legacy-jobs.yml` is append-LAST for every program, making it the highest-conflict file in the fleet — any sibling program merging will conflict a long-lived job PR. Resolve by keeping BOTH blocks: `LAST` describes how a job is ADDED, never a standing order. | Measured on #7950: `mergeable=false`, `mergeable_state=dirty` after #8053 merged at 2026-09-27T01:31:59Z; `ci`+`fences` last ran on the superseded head `94e74dd8` at 00:38Z while `ci-authority` ran three times on `64a0b1dd`; `ci.yml` was healthy repo-wide on `pull_request` throughout (15-run sample including this seat's own #8060). Watcher v1 had reported ALL CHECKS CONCLUDED over exactly 3 `ci-authority` checks and 0 packs — the false green that started the investigation. |
| R-MIN-33g (proof mechanics — AMENDS R-MIN-33f) | R-MIN-33f's second clause ("read `pending == 0` as green ONLY once the expected pack set is PRESENT") is **necessary but not sufficient, and its "expected set" is not a constant**. `ci-plan` COMPUTES the pack set from the changed files and takes minutes to publish it, so every run has a window in which the rollup honestly shows `ci-plan` and zero packs; and a docs-only diff receives a REDUCED set, never an empty one. Three corrections bind. (1) The expected set is the ci.yml RUN's own job list for the EXACT head sha, read from the Actions API — never a constant floor, never inferred from the rollup. A fixed `>= 8 ci-pack-*` gate is unsatisfiable on a legitimately small plan and will tick to timeout on complete proof. (2) Completion is that run's `status == "completed"`; the rollup is still read for reds, because fence-pack, ci-authority and capability-broker live in separate runs, but an EMPTY API answer is never a result on any endpoint. (3) Absence is resolved only against the Actions API: no run for a head older than roughly 15 minutes is the single case where waiting is futile. "Absent is not pending" does NOT imply "absent is proof of never". Corollary with its own teeth: **your own push invalidates your own evidence** — a merge decision inherits the freshness of the read that justified it, so re-read after the last push and immediately before merging. The docs-only `--admin` exception requires a diff that triggers NO RUN AT ALL, established from the Actions API, not inferred from a rollup snapshot. | Measured on this seat's own #8060: run 36288860409 created 2026-09-27T02:33:32Z, `ci-plan` SUCCESS in 3m56s, planning `contract-delta` + `ci-pack-0` + `ci-pack-1` — two packs, so R-MIN-33f's own presence rule as implemented (min 8) could never have concluded it. Head `1081ffed2fe` is dated 02:33:16Z, i.e. ci.yml fired 16 SECONDS after the push, and two earlier runs on the branch (01:51:04Z, 02:17:59Z) were cancelled by this seat's own successive pushes. The seat read the rollup at roughly 02:30 — before its own last commit existed — concluded ci.yml was structurally incapable of packing a docs diff, and merged `--admin` at 02:56:02Z with all three checks `in_progress`: a MID-FLIGHT merge misclassified as the sanctioned docs-only exception, and a violation of "Merge on CONCLUDED checks". The Stop hook had named the correct instrument verbatim (`gh run list --workflow ci.yml --branch <branch>`) and the seat never ran it. Independently reproduced on #7950, whose watcher logged `packs=0` on ticks 1 and 2 of a PR whose plan was 12 packs. Contamination bounded: #8053 merged on a run already `completed`/`success` 45 minutes earlier, so the defect is isolated to #8060, whose proof run survived the merge under ci.yml's merged-close concurrency fence. |
| R-MIN-34 (T04b — MGD-08 clause 2) | For a WHOLLY EMPTY economic path, `status == "degraded"` with `native_blocks == []` and the absence NAMED in `limitations` **satisfies** MGD-08 clause 2's requirement that the path "fails". A hard refusal is the WRONG target and must not be implemented: this program's design law is truthful degradation with named absence, exactly as R-MIN-15 keeps the contract explanation and stays `ready` for a missing threshold, and forcing `status NOT in {ready, degraded}` would destroy the source-only usefulness MGD-11 requires. Unknown data is never zero, and a named absence is the program's honest form of failure. **Pin two-armed:** the positive arm asserts `status == "degraded"` AND `native_blocks == []` AND the absence limitation present BY EXACT CODE, compared with `==` against the exact status string and never with `!=`; the reverse arm already ships, since both complete cases return `ready` with a populated block. Construction note, because the casebook CANNOT express this input: `synthetic_case(name, **overrides)` merges into fixture keys and can only SET them (`_merge` has no delete), and `financial_packets` is derived from the presence of the top-level `economics` key — so build the bundle directly with `dataclasses.replace(case.bundle, financial_packets=())`, or add a fixture whose `economics` key is absent. A `bundle={...}` override is SILENTLY IGNORED: it sets a top-level key `_bundle()` never reads. | Measured at #7950 head `c05368b31a6` via `scratchpad/probe_mgd_08_10_v2.py`: an emptied economic path returns `summary.status = degraded`, `native_blocks = 0`, `limitations = ['omitted:expectations']` — identically with `omissions = ()`, because the omission-independent path mints the code regardless, which makes `_summarize_status`'s `if not limitations` branch unreachable for this input. MEASUREMENT ERROR recorded against the seat: the FIRST probe passed `bundle={...}` to `synthetic_case`, the override was silently ignored, and it reported `native_blocks: 1` while claiming an emptied path — a silently-ignored override is indistinguishable from behaviour that is already correct. Both prescriptions the seat derived from that probe were wrong (it asserted clause 2 could degrade while naming nothing, and prescribed a hard refusal), and are retracted here rather than handed to a lane. Assert the override took effect before reading the result. |
