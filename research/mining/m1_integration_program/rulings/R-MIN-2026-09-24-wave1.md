# R-MIN wave-1 rulings — GMI Mining M1 integration (Fable Meta-CEO seat 664a0650)

Operation key `gmi-mining-fable-ceo-m1-integration-20260924-chairman-001` (prepared on #7795; made live by deliberate Chairman delivery of `MINING_MASTER_FABLE_INTEGRATION_PACKET_2026-09-24.zip` on 2026-09-24). Authority: that delegation ("initiate as Fable Meta-CEO … finish end to end … fabric for all subagent work … Opus/Fable children as sub-orchestrators/auditors … fan out to macbook/m1/minis … conserve tokens") + the Astra/Chairman base-ownership ruling (~07:50Z: Semiconductor B #7870 owns the shared base; sector programs integrate later). Frozen inputs on `origin/sol/mining-principal-research-20260923` @ `eb6f05c0`: plan blob `a2fca1c5`, mandatory addendum `053a59e2`, design `33d94481`, witness qualification `34548e6b`, trace `37ccaa92`. Pickup recorded on #7795 comment 5811520293; START is recorded per admitted scope, never implied.

These rulings bind every Mining packet. They add no gate service, no second control plane and no new permission; they narrow the frozen plan where the addendum or the current code requires it.

| Ruling | Decision | Why / evidence |
|---|---|---|
| R-MIN-00 | Mining mints NO shell, evidence, rights, route, publisher, client or mount vocabulary. Every Mining task extends the incumbent owners only: Company Intelligence (`engine/company_intelligence/*`), Earnings receipts (`engine/earnings_release/receipts.py`, unchanged), and ONE per-sector market-ontology module. T05/T06 wait for Semiconductor B's route/client/mount to be on `main`; G2 real-source admission is an incumbent-intake act. | Addendum §2 (original T01 is consumption, not a Mining base build); Astra ruling; #7870 @ dd5076fa still has only `semiconductor_theme_research.v1` and no `app/theme_research.py` on main. |
| R-MIN-01 | Carrier model = one PR per task off fresh `origin/main`, squash-merged in dependency order. #7795 stays Draft/HOLD and docs-only (never product code). Continuity = `agentos/handoffs/GMI-MINING-2026-09-24-m1-integration.md` + `WS:GMI-MINING-M1-INTEGRATION` (child of `WS:GMI-THEME-GRAPH`) on `main`; rulings live in this file. Coordination posts: pickup/START/checkpoints on #7795; #7870 only for a NEW seam question (5809893850 is never repeated). | Industrials R-IND-01 precedent; Energy's single HOLD carrier #7881 became unmergeable. |
| R-MIN-02 | ONE gate:code job `mining-economic-dossier` in `.github/ci/legacy-jobs.yml`, created by T01' from the `prophet-us-b4-prereg-registration` recipe (`if: ${{ false }}`, gate code, scope exclusive, explicit `paths:` = every Mining file + every Mining test in `run:`), appended at the END of the file (contested by ≥6 open PRs), plus the `CURATED_EXCLUSIVE` entry in `tests/test_ci_pack.py`. Install line probed in a clean venv before push (CI DEPENDENCY LAW: a copied recipe carries the donor's deps). Every later Mining test file is wired into `paths:` AND `run:` in the PR that adds it. | CDV-1 #7905 ci-pack-8 red (pyyaml); the 09-24 contract-delta refusal on #7783 (test in `run:` without `paths:`). |
| R-MIN-03 | Synthetic fixture law: the casebook's 11 closed names (`copper_complete`, `rare_earth_complete`, `missing_basis`, `missing_issuer`, `source_only`, `missing_stream_threshold`, `changed_source`, `signed_loss`, `same_horizon_revision`, `denied_source`, `page_generation_change`); every fixture `synthetic: true`, invented issuers (e.g. "Ardent Copper Holdings", "Halide Rare Earth Corp"), `example.invalid` sources, invented CIK-shaped ids that are NOT real CIKs; no live Freeport/MP figure in any fixture. The real CIKs `0000831259` (FCX) and `0001801368` (MP) appear only in the T02 profile factories' identity constants. No real company is marked RESOLVED; no research value becomes a native receipt. | Plan T02 casebook contract; LAW.md fixture law; witness qualification is research, not data permission. |
| R-MIN-04 | Labor on external fabric lanes (GLM-5.3 via `glm-codex`; mini2 preferred — 3/3 success on 09-24 vs 2/2 degenerations on m1; MiniMax-M3 with `review_engine: seat` as fallback on m1/mb), commit-and-push-per-step ANTI-COLLAPSE law in every packet, Opus READ_ONLY red-team of every task PR before a dependent branches, and reviewer probes frozen as RED tests before any repair lane. The seat keeps readiness, labels, merges, carrier posts and credentials. | Chairman fabric order (09-14/09-17) + this delegation; `glm-fix-lane-degenerates-on-long-single-exec-commit-per-step`; `freeze-reviewer-probes-as-red-tests-before-a-repair-lane`. |
| R-MIN-05 | Ordering: T01' → (T02 ∥ T04a) → T03 → T04b → T07 → [Semiconductor B route/client/mount on `main`] → T05 → T06 → T08. T02 branches only after CDV-1 #7905 has merged (it rewrites the `issuer_profiles.py` lookup seam); T04a = the closed Mining definitions + own schema + synthetic composition, T04b = the integrated positive witnesses on T03 inputs. Parallel pairs are path-disjoint. | Plan §11; Industrials R-IND-05/R-IND-12. |
| R-MIN-06 | G1 consumption: Mining creates no `engine/market_ontology/theme_research.py`, no generic response schema, no route, no publisher, no client. The Mining composition is a per-sector module + schema on the accepted per-sector precedent (Tech ex-Semis #7891 / Industrials R-IND-17). If/when the shared owner delivers a generalized kernel, the Mining wrapper delegates to it and the original extraction steps are DO_NOT_REDO. Until the shared POST route exists on `main`, the T01' harness client returns a typed `route_unbound` refusal with zero protected reads. | Addendum §2 + IR-06; R-IND-06/R-IND-17. |
| R-MIN-07 | IR-01/IR-02 consumer rules are tested, not just stated: a `definition_unqualified:*` warning coexisting with a numerically favourable comparison yields NO badge, headline or confirmed-surprise claim while both values stay inspectable; fully qualified compatible inputs are NOT suppressed; W-C management's earlier point estimate stays a point estimate (no invented bounds, no Street label); W-R quarter/half-year, elimination sign and GAAP/non-GAAP basis travel with every literal; derived arithmetic binds every consumed revision. | Addendum §4 IR-01/IR-02; MGD-16/22 strengthened. |
| R-MIN-08 | IR-05 source use: filing facts carry the incumbent rights profile the release path already assigns; no full issuer/exhibit body is republished; factual synthesis, permitted spans and links are allowed. `config/theme_sources.yml` (`sec_edgar` family) is read, never edited, by Mining. | Addendum IR-05; #7870 comment 5810041817. |
| R-MIN-09 | Records: M1 acceptance is recorded separately from full Mining completion (IR-07). Every task PR's return names carrier/head, MGD rows covered, commands with rc, receipts, unresolved effects and the next bounded action; a copied test count is historical, never a run. | Handoff §7–§8. |

## Seam rulings (from the Opus READ_ONLY seam audit)

Source: `research/mining/m1_integration_program/reviews/OPUS_PLAN_SEAM_AUDIT_2026-09-24.md`. Each ruling binds the packets of the tasks it names.

| Ruling | Decision | Audit basis |
|---|---|---|
| (pending audit) | | |
