# TradingView-Parity Charting Program — build docket

Status: ACTIVE PROGRAM (operator brief 2026-07-09 evening, with TV reference screenshots).
Repo: charting-app (`terminal/`). Quality bar: mockups/reference-screenshots-first, rendered
browser verification with prod-shaped data at 1400px, EN/ZH, design-token discipline
(v5: `--font-ui`/`--font-num`, var(--brand)/--up/--down, East-Asian flip). The operator's
TV screenshots are the design spec — builders copy TV's design mechanisms, not reinvent.

Sequencing constraint: pass-5 lanes (price-correctness, perf) are in flight on
ChartPanel-adjacent surfaces. Phase 0 ships immediately on disjoint-ish surfaces; Phases
1–3 wait for pass-5 merges (same files).

## Phase 0 — P0s + polish (immediate)

1. **Back-to-chart bug (P0)**: on the Analyst pane the "Back to chart" button does
   nothing. NOTE: unmerged terminal PR #4 moves this button — check whether the live bug
   is the same handler, merge-or-supersede #4 in the fix.
2. **Console 404 storm**: `data/<SYM>.intel.json` / `.opts.json` / `.fund.json` 404 for
   symbols outside the deep-coverage universe (KRUS, CNI, COKE, MCD, YELP, 9988.HK,
   crypto fund/opts …), AND the same missing file is re-requested repeatedly in one
   session (KRUS.intel dozens of times). Fix BOTH: (a) a coverage index (tiny
   `data/coverage.json` listing which per-symbol files exist, generated by the ingest
   that writes them; client consults it before fetching) and (b) client-side
   negative-cache so a 404 is never refetched in-session. 404s must go to ~zero on a
   watchlist full of uncovered names.
3. **Timeframe order**: the top-bar timeframe list renders mixed-up; sort chronologically
   (1m…m scale ascending: minutes → hours → D → W → M).
4. **Number font**: `.num` (price displays) is ugly. Replace with a proper tabular-nums
   numeric face (candidates: IBM Plex Mono/Sans tabular, Roboto Mono, JetBrains Mono —
   pick what reads closest to TV's crisp numerals; must support tabular alignment and
   look right at 30px+ headline sizes AND 10px table sizes; keep --font-num token).
5. **Brightness pass**: greys are too dark across the board — raise muted/secondary text
   tokens toward white (TV reads near-white). Token-level change (--muted etc.), verify
   contrast on dark panels on every major page, don't wash out the hierarchy.

## Phase 1 — chart picture-frame + quick settings (after pass-5 merges)

6. **Bottom bar** (TV parity, frames the chart): left = default range buttons
   `1D 5D 1M 3M 6M YTD 1Y 5Y All` + a small calendar/go-to-date affordance; right = live
   clock ticking each second in the VIEWER's local timezone rendered as `HH:MM:SS UTC±N`
   + `ETH` chip (extended-hours toggle; wire to the intraday ext param) + `ADJ` chip
   (adjusted data indicator; display-only if we only have adjusted). Thin bar, hairline
   border, sits under the chart region completing the frame.
7. **Top TF bar**: remove the grey container — buttons sit directly on the toolbar (TV
   style); active TF is a subtle highlight. Customizable set: an edit affordance (star/
   manage menu) letting the user choose which TFs pin to the top bar (persist per user in
   the existing layout/prefs store); bottom-bar defaults stay fixed.
8. **Quick-settings popup** (bottom-right of the chart, gear icon on the frame bar):
   exact TV menu — `Auto (fits data to screen)✓ · Lock price to bar ratio <ratio> ·
   Scale price chart only · Invert scale ⌥I` | `Regular✓ · Percent ⌥P · Indexed to 100 ·
   Logarithmic ⌥L` | `Move scale to left` | `Labels ▸ · Lines ▸ · Plus button✓` |
   `More settings…` → opens the Settings modal on the "Scales and lines" tab. Every
   toggle actually adjusts the chart (lightweight-charts price-scale APIs).

## Phase 2 — settings modal + drawing sidebar (after Phase 1)

9. **Settings modal**: TV-style large popup with left tab rail — Symbol, Status line,
   Scales and lines, Canvas, Trading (render "Coming soon"), Alerts/Events as
   applicable. Implement every non-trading setting for real (candle colors/borders/wick,
   status-line fields, scale placement/labels/lines, canvas background/grid/watermark,
   timezone/session). Persist per user.
10. **Drawing sidebar revamp**: larger icons, TV-grade spacing/grouping, hover states,
    submenu flyouts with the tool families. Keep every existing tool working.

## Phase 3 — snapshot system

11. **Snapshot rendering**: indicator names/values drawn INSIDE their own panels
    (top-left, like the live view) — not a stacked list at the image top; timestamp
    shows full date+time; strip the `_oracle` suffix from indicator labels; drop the
    "Candles" label; replace the "Mastermind Terminal" text watermark with the full
    logo asset from the top-left menu.
12. **Snapshot menu** (camera icon): `Download image · Copy image · Copy link ·
    Open in new tab` (TV's menu). **Copy link** uploads the PNG server-side to R2
    (`snapshots/<id>.png`, public bucket already serving live_flow) and returns
    `https://app.mastermind-x.com/x/<id>` — a share page route that renders the image
    with OG tags (TV's `tradingview.com/x/…` mechanic). Rate-limit uploads; id =
    short random slug.

## Phase 4 — data completeness

13. **After-hours data**: the quote hub's delayed Polygon feed already carries extended
    hours; pass-5 lane P adds close+AH semantics. Build on it: chart ETH session render
    (ext param already exists on /api/intraday) + header AH secondary line site-wide.
14. **Overview/Statistics tabs**: missing for many stocks in the deep-analysis
    dashboard — same root as the 404s (per-symbol fund/intel coverage is a small
    subset of the 8.7k universe). Extend ingest coverage pragmatically (top-N by
    liquidity + anything in any user watchlist), generate on a schedule; where a
    symbol is uncovered, the tab must say so honestly instead of rendering empty.
15. **Insider dashboard recency**: don't present ancient trades as the story (AAPL
    shows only 2025 trades). Window the display (~18 months), lead with "most recent
    trade: <date>" and an explicit "no insider trades in the last N months" note when
    quiet. Data exists in the intel files; this is presentation + a recency field.

## Phase 5 — TV gap analysis extras + full QA sweep

16. Research lane inventories TV capabilities vs ours (alerts UX, replay, multi-chart
    layouts, compare/overlay symbols, session breaks, countdown-to-bar-close on the
    price label, price-line options, keyboard shortcuts, symbol-info dialogs, watchlist
    columns/sparklines…) → adjudicated shortlist becomes Phase 5 build items.
17. **Full sweep**: multi-agent bug hunt across every Terminal page (desktop+mobile,
    EN/ZH): console errors, dead controls, layout breaks, stale/wrong data displays,
    slow interactions. Findings adversarially verified, then fixed in a close-out lane.

## Standing rules

- Every UI phase: builder gets the TV screenshots/described specs above; reviewer judges
  against them as a USER, with rendered screenshots at 1400px + narrow width.
- No new heavy deps without justification; lightweight-charts stays the engine.
- Deploys are orchestrator-gated (CDN chunk-availability check is now law).
- Honesty rules unchanged (basis badges, stale flags, no fake data in empty tabs).

## Phase 6 — watchlist & symbol intelligence (operator brief 2026-07-09 late, 5 TV screenshots)

18. **Watchlist flag ribbons**: each row can carry a colored flag rendered as a left-edge
    band + colored mark. Hover an unflagged row → an outline ribbon affordance appears;
    click = flag with the LAST-USED color (sticky default for smooth repeat use). Hover
    an existing flag → palette dropdown (TV palette: red, blue, green, yellow, purple,
    cyan) + trash to unflag. Persist per user with the watchlist.
19. **Composite basket symbols**: `META+AVGO+AMZN` in Symbol Search or Add Symbol
    creates an equal-weighted composite — chart series = aligned sum of constituent
    closes (TV "spread" semantics), ticker pane card titled with the expression,
    subtitle "Composite Symbols", live summed price + chg vs summed prevClose. Search
    result row shows type "spread". On a basket pane, the add-symbol dialog's FIRST and
    highlighted option = add-this-basket-to-watchlist. v1 guards: ≤10 constituents,
    US equities only, hide volume pane, no fake overnight line.
20. **History-aware search dialogs**: both dialogs open pre-populated with the user's
    chronological search history (most recent first, before any typing). The
    HIGHLIGHTED entry = the current chart's symbol when it's not yet in the watchlist —
    Enter adds it instantly (Add Symbol) / navigates (Symbol Search). Watchlist-member
    rows: flag mark left of the ticker; in Add Symbol they show trash ("Remove from
    Watchlist" tooltip) + crosshair target ("Go to Symbol" → jumps chart) instead of +.
    Footer hint: "⇧+Click or ⇧+Enter to add symbol and close dialog". History = local
    ring buffer (~50), recorded on search commits and symbol navigations.

## Phase 7 — right-click context suite (operator brief 2026-07-09 night, 5 TV screenshots)

21. **Chart context menu** (TV structure, NO trading rows per operator): Reset chart
    view ⌥R | ─ | Copy price <clicked price> · Paste ⌘V | ─ | Add alert on <SYM> at
    <price>… ⌥A (opens our alert dialog PREFILLED: condition Price/Crossing/<clicked
    value>) | ─ | Lock vertical cursor line by time (pins a dashed vertical line at the
    clicked timestamp w/ a lock marker on the time axis; toggle; persists per layout) |
    ─ | Table view · Object tree · Chart template ▸ | ─ | Remove N indicators (live
    count) | ─ | Settings… (opens the Phase-2 modal).
22. **Chart template ▸ submenu**: saved templates (click = apply indicator set +
    settings) + "Save as template" → popup titled "Save template as" (name input,
    Save/Cancel, overwrite confirm). Per-user persistence.
23. **Table view**: replaces the chart region — "<Company> · Table view" header +
    Back-to-chart; Download data (CSV); Date·TF | O/H/L/C | Change (+abs/+%) | Volume |
    one column per ACTIVE indicator (∅ when absent); newest first; active TF.
24. **Object tree**: right-rail tab listing the symbol node + every indicator with eye
    toggles + remove; sub-pane grouping; Data-window tab only if crosshair plumbing is
    free (no dead tab).

## Phase 8 — overnight/extended-hours pricing + watchlist columns (operator brief 2026-07-10 early, 2 TV screenshots)

25. **Session-price semantics (TV parity)**: from the close until the NEXT session's
    open, the primary price everywhere (watchlist Last, header, pane card) is the
    previous EOD close with its day change. Extended/overnight trade data is a
    SEPARATE secondary display: pane card shows a second price block beneath the
    primary — "<ext price> USD <chg vs close>" + "☾ Overnight" + its own
    "Last update at <ts>" — which DISAPPEARS at market open (TV's "Overnight via
    BOATS" mechanic; we label by our actual source, never a borrowed brand).
26. **Watchlist "Ext" column + column customizer**: watchlist ⋯ menu gains
    CUSTOMIZE COLUMNS (Last / Change / Change % / Volume / Extended Hours) and
    SYMBOL DISPLAY (Logo · Symbol/Name), persisted per user. "Ext" = extended/
    overnight chg% vs close (dash when no ext print).
27. **Overnight data source**: research free/grey options hard (Alpaca free ws +
    overnight feed availability, Finnhub, Twelve Data, Yahoo endpoints (grey ok),
    Tradier, Webull grey). v1 = Alpaca leg in the QUOTE HUB (server-side singleton →
    ONE connection multiplexed across ALL users; ≤30 dynamic subs, LRU by /quotes
    demand) behind ALPACA_API_KEY/ALPACA_API_SECRET (graceful keyless no-op) + best
    keyless fallback so Ext works before the key lands. FMP later.
28. **Golden Oracle indicator default OFF** — on by explicit user choice only;
    existing explicit saved prefs respected.
