# Market Ontology W5-D-M executor contracts (macro)

Verified in this worktree at `origin/main` `9a4a389c0d21d1ae4f195ec31e950eb5c07fb381` (`git rev-parse HEAD`). Operator commission W5-D-M: no build, no strategy, no network, no git writes. Every path:line below was opened or grepped in this session. Cheap fixture-only pytest on this head: `python3 -m pytest -q tests/test_sanctions_map_engine.py tests/test_sanctions_map_page.py tests/test_sanctions_map_nav.py tests/test_worldmap_base.py tests/test_ofac_sdn_collector.py tests/test_debt_maturity.py tests/test_commodity_supply_context.py tests/test_commodity_coverage_matrix.py` → **133 passed, 1 skipped, 83 warnings in 7.89s**. The skip is `tests/test_debt_maturity.py:1060` (`mockups/evidence/debt_maturity` not checked out — sparse-excluded).

Left alone (earlier commission, untracked): `research/market_intelligence_productization/MARKET_ONTOLOGY_W5_EXECUTOR_CONTRACTS_MACRO_2026-09-19.md`.

Do not re-decompose (open PRs / already-disposed today): MO-DELTA-006/010/032, MO-PAID-011/017/067, MO-DELTA-001; #7121 MO-DELTA-018, #7127 MO-PAID-062, #7128 MO-PAID-060, #7126 MO-PAID-064, #7110 MO-DELTA-029, #7122 MO-PAID-020, #7133 MO-PAID-088, #7125 MO-DELTA-011, #7117/#7134 F07, #7100/#7106/#7124 F11, #7132 F12, #7102 F06, #7131 F08.

No MiniMax CONTRACT in this packet. The rights-clear F02 slice and the sequenced F09 first-slice producer are already on this head; the remaining F09 materials chain is gated on a named source the Chairman/commercial seat owns.

## Summary

| row | disposition | tier | title |
|---|---|---|---|
| MO-DELTA-031 | RECORDS_MOVE | n/a | Sanctions map already paints OFAC on the world map |
| MO-PAID-059 | RECORDS_MOVE | n/a | Issuer pages already show when debt comes due |
| MO-PAID-040 | HOLD | n/a | Supply-chain layers wait on a named source |

---

### MO-DELTA-031 — [MO-B F02-POLICY-GEO] Sanctions map already paints OFAC on the world map
DISPOSITION: RECORDS_MOVE

EVIDENCE NOW:
- Ledger still says `NEW_BOUNDED_BUILD` / `NOT_BUILT` / `real_producer=NONE` / `real_consumer=NONE` / missing contract “base map + 4 layers”. Command: `rg '^MO-DELTA-031,' research/market_intelligence_productization/MARKET_ONTOLOGY_F00C_GRANULAR_CLOSURE_LEDGER_2026-09-02.csv` (CSV line 14). That sentence is stale against the files below.
- `engine/sanctions_map.py:1-6,20-23,163-187` — display-only leaf view-model; every country↔programme edge comes from human-reviewed `config/sanctions_ofac_programs.yml`; store is existing `data/sanctions_ofac` (`SDN_FILE`, `META_FILE`); never raises; nulls are `None`, never `0`. Command: `sed -n '1,6p;20,23p;163,187p' engine/sanctions_map.py`.
- `config/sanctions_ofac_programs.yml:1-6` — OFAC programme → ISO3 join; comment states country keys match the Natural Earth base map in `templates/_worldmap_base.html.j2` and that this is the **existing** geospatial identity plane, not a second country master. Command: `sed -n '1,6p' config/sanctions_ofac_programs.yml`.
- `templates/_worldmap_base.html.j2:1-7` — Natural Earth 1:110m Admin 0 SVG (`data-iso3` paths); regenerate helper is `scripts/gen_worldmap_svg.py`. Command: `sed -n '1,7p' templates/_worldmap_base.html.j2`.
- `templates/sanctions_map.html.j2:102-126,189-193` — bilingual page “Sanctions map” / “制裁地图”; includes `_worldmap_base.html.j2`; method copy: “Not on this map: shipping chokepoints, military sites, satellite imagery.” Command: `sed -n '102,126p;189,193p' templates/sanctions_map.html.j2`.
- `scripts/build_sanctions_map.py:1-4,36-48` — reads `engine.sanctions_map.build()`, paints rungs onto the existing SVG ISO3 set, writes `site/sanctions_map.html` via `lib.pages.write_page`. Command: `sed -n '1,4p;36,48p' scripts/build_sanctions_map.py`.
- `collectors/ofac_sdn.py:1-6,23-26` + `scripts/collect.py:173` — OFAC public SDN CSV → existing store `data/sanctions_ofac/`; keyless; off the render path. Command: `sed -n '1,6p;23,26p' collectors/ofac_sdn.py`; `sed -n '173p' scripts/collect.py`.
- Render-lane hook already present (do not add a second one): `config/dag.yml:2506` `scripts.build_sanctions_map`; `.github/workflows/render.yml:911`. Nav: `templates/_navlinks.html.j2:195`. Registry: `config/product_experience/page_registry_overrides.yml:688-693` (`macro:sanctions_map`, packet A-F02-1). Command: `rg -n 'build_sanctions_map|sanctions_map.html' config/dag.yml .github/workflows/render.yml templates/_navlinks.html.j2`.
- Merge `#6899` is an ancestor of this HEAD (`git merge-base --is-ancestor 8918fba56c23 HEAD` → YES): `8918fba56c23 [MO-AA1] A-F02-1: Base map + OFAC sanctions overlay (#6899)`. First-parent blame of the current file bytes is `6367d46a` (dashboard-bot nightly; same first-parent quirk W5-A recorded). The files are on `origin/main` `9a4a389c`.
- F02 `do_not_redo` still binds: no second event DB / geospatial object store / map identity plane (`research/market_intelligence_productization/MARKET_ONTOLOGY_F02_OWNER_SOURCE_RIGHTS_MAP_2026-09-05.md:80-87`). This slice reuses `_worldmap_base.html.j2` ISO3 + `data/sanctions_ofac/`; it does not mint a new plane. Military / maritime / satellite stay behind MO-PAID-048/049/050 (`PENDING_RIGHTS`; ledger CSV lines 21-23).
- The 2026-09-05 owner map’s UNRESOLVED-3a (“sanctions display not landed; `engine/sanctions_map.py` absent”) is stale on this head. Command: `sed -n '35,41p;75p' research/market_intelligence_productization/MARKET_ONTOLOGY_F02_OWNER_SOURCE_RIGHTS_MAP_2026-09-05.md`; `ls engine/sanctions_map.py templates/sanctions_map.html.j2`.
- Fixture tests green: `tests/test_sanctions_map_engine.py`, `tests/test_sanctions_map_page.py`, `tests/test_sanctions_map_nav.py:11-14` (exactly one nav anchor), `tests/test_worldmap_base.py`, `tests/test_ofac_sdn_collector.py` — included in the 133-pass run; no `data/` tree required.

LEDGER MOVE ON MERGE+LIVE: capability_state_c2 `NOT_BUILT` → `BUILT_NOT_PROVEN` in the records PR (code is on `origin/main` `9a4a389c`; this session did not curl the live site). Rewrite `real_producer` from `NONE` to `engine/sanctions_map.py` + `collectors/ofac_sdn.py`; rewrite `real_consumer` from `NONE` to `templates/sanctions_map.html.j2` + `scripts/build_sanctions_map.py`. Rewrite `missing_contract_or_proof` to “rights-clear slice landed (base map + OFAC overlay); military/maritime/satellite layers remain CONTEXT_ONLY behind MO-PAID-048/049/050”. After live proof: `BUILT_NOT_PROVEN` → `DONE` for the rights-clear slice only. Live readback: `GET https://www.mastermind-x.com/sanctions_map.html` → HTTP 200; body contains “Sanctions map” / “制裁地图” and “Not on this map: shipping chokepoints, military sites, satellite imagery”; it must not introduce a second map-identity store or a confidence/score chip.

AUTHORITY: `context_only`; F02 `do_not_redo` forbids a second event DB / geospatial object store / map identity plane. Do not add military, maritime, AIS, satellite, or chokepoint layers. Do not add calibrated confidence, ranking, or trade language. Do not start a second sanctions truth store.

---

### MO-PAID-059 — [MO-B F09-CAPITAL-MATERIALS] Issuer pages already show when debt comes due
DISPOSITION: RECORDS_MOVE

The sequenced **first slice** named by the ledger (bounded XBRL debt-maturity ingestion producer + tests) is on this head. This packet does **not** contract that slice again, and does **not** contract the remaining cash-burn / funding-gap assembly (commission bound: contract only the missing first slice; pair MO-DELTA-018 is open as #7121). The 2026-09-02 “no producer exists repo-wide” sentence is stale.

EVIDENCE NOW:
- Ledger: `NEW_BOUNDED_BUILD` / `NOT_BUILT` / `real_producer=PARTIAL-PREMISE: … instruments half NONE — no debt-maturity-schedule producer exists anywhere` / `real_consumer=NONE` / next child “first a NEW bounded XBRL debt-maturity ingestion producer … ONLY THEN the maturity-wall assembly primitive. Not buildable-now.” Command: `rg '^MO-PAID-059,' research/market_intelligence_productization/MARKET_ONTOLOGY_F00C_GRANULAR_CLOSURE_LEDGER_2026-09-02.csv` (CSV line 86). The instruments-half `NONE` clause is stale; `document_terms.py` remaining fee-table-only is still true.
- `engine/debt_maturity.py:1-21,32-39,163-177` — pure (no I/O) extractor over SEC companyfacts `us-gaap` tags; frozen six buckets are `LongTermDebtMaturitiesRepaymentsOfPrincipalInNextTwelveMonths` … `AfterYearFive`; identity is CIK-only; no score/rank/LLM. Command: `sed -n '1,21p;32,39p;163,177p' engine/debt_maturity.py`.
- `scripts/build_debt_maturity.py:1-24,75-79` — bounded ingestion producer (packet B-F09-3); docstring names ledger rows MO-PAID-059 / MO-DELTA-018; cache path `data/debt_maturity/cache/CIK<10-digit>.json`; only the six `BUCKETS` tags are requested. Command: `sed -n '1,24p;75,79p' scripts/build_debt_maturity.py`.
- `templates/_debt_maturity.html.j2:1-12` — “Debt coming due” / “到期债务” panel, `id="debt-maturity"`, `data-panel="debt_maturity.v1"`. Included from `templates/ticker.html.j2:1487`. Command: `sed -n '1,12p' templates/_debt_maturity.html.j2`; `sed -n '1487p' templates/ticker.html.j2`.
- `scripts/build_stock_library.py:24-30,4092-4098` — resolves `debt_maturity.v1` per ticker via `load_debt_maturity_facts` + `extract_maturity_ladder`. `collectors/edgar_facts.py:165-179` warms the same cache from the existing companyfacts fetch (no second SEC round trip; off-render; `.github/workflows/debt-maturity-drip.yml`). Command: `sed -n '24,30p;4092,4098p' scripts/build_stock_library.py`; `sed -n '165,179p' collectors/edgar_facts.py`.
- `tests/test_debt_maturity.py:1,24-36` — fixture-only (`tests/fixtures/debt_maturity/aapl_trimmed.json`); asserts the six `LongTermDebtMaturitiesRepaymentsOfPrincipal*` tags. Included in the 133-pass run (1 skip at `:1060` for sparse-excluded `mockups/`).
- `DebtInstrumentMaturityDate` is still unused: `rg DebtInstrumentMaturityDate engine scripts collectors tests` → **zero hits**. The landed producer implemented the bucket-tag family from the ledger’s concept list, not per-instrument maturity dates. That is the bounded first slice, not a missing producer.
- `engine/capital_structure/document_terms.py:1-8,59,1210` and `contracts/capital_structure_document_term_observation.schema.json:76-85` — still registration-fee-table-only (`scope` const `registration_fee_table_row`; closed name enum has no maturity concept). The 2026-09-02 C6-1 refusal to fabricate a schema still holds for **that** module; it is no longer the whole instruments story. Command: `sed -n '1,8p;59p;1210p' engine/capital_structure/document_terms.py`.
- Cash-burn / funding-gap assembly is **not** landed. `engine/capital_structure/projection.py:3-6,40-48` lists `cash_runway` under `UNAVAILABLE_CAPABILITIES`. `engine/capital_structure/biocatalyst_pit_adapter.py:42` lists `no_cash_burn_runway_or_dilution_calculation`. `engine/stock_fundamentals.py:524` still emits `cash` as `CashAndCashEquivalents` (fundamentals half remains real). Template grep for `cash burn` / `funding gap` under `templates/` on ticker/debt-maturity surfaces: none. Command: `sed -n '3,6p;40,48p' engine/capital_structure/projection.py`.
- Do not confuse with the **market** CCW wall: `engine/corp_credit.py:1158-1161` writes `data/corp_bonds/series/maturity_wall.parquet` as ETF-held par buckets. F09 row-accounting repair (ledger `adjudication_notes`) forbids treating that par as issuer debt outstanding or joining issuers by name/theme.
- History honesty: `git merge-base --is-ancestor 12e8bfd369a6 HEAD` → exit 1 (merge commit of #6921 is **not** on this first-parent). `git log -- engine/debt_maturity.py` first-parent-attributes the add to `6367d46a` (`data: nightly timings stock_briefs 2026-09-15`). `git log --all -- engine/debt_maturity.py` shows the feature commits `46f7cf77 feat(f09): issuer debt-maturity ladder from SEC XBRL companyfacts`, `b0fcb642` / `f248429a` (PR #6921 repairs). `docs/ACTIVE_BUILD_MAP.md:728` names `#6921` B-F09-3. The **files and tests are on this head**; this session does not claim the #6921 merge commit is a first-parent ancestor.
- Pair MO-DELTA-018 is the same sequenced child and is **out of this packet** (open #7121, commission exclusion; #7121 persists a #6921 cache). Do not re-decompose 018. Do not start a second cache or a second identity plane.

LEDGER MOVE ON MERGE+LIVE: capability_state_c2 `NOT_BUILT` → `PARTIAL` in the records PR. Rewrite `real_producer` from “instruments half NONE” to `engine/debt_maturity.py` + `scripts/build_debt_maturity.py` (six `LongTermDebtMaturitiesRepaymentsOfPrincipal*` tags, CIK-only; `document_terms.py` remains fee-table-only). Rewrite `real_consumer` from `NONE` to `templates/_debt_maturity.html.j2` via `templates/ticker.html.j2` + `scripts/build_stock_library.py`. Rewrite `missing_contract_or_proof` to “cash-burn / funding-gap assembly still absent (`cash_runway` is `UNAVAILABLE_CAPABILITIES`); first-slice XBRL producer is landed.” Rewrite `next_bounded_child` to: remaining assembly primitive only **after** first-slice live-proof **and** after open #7121 (MO-DELTA-018 cache persist) lands; not this packet. After live proof of the ladder: first-slice `PARTIAL` may be annotated `BUILT_NOT_PROVEN` for the producer; the row is not `DONE` until cash-burn runway is also live. Live readback (first slice only): `GET https://www.mastermind-x.com/AAPL.html` contains `id="debt-maturity"` / “Debt coming due” / “到期债务” and does not print a score, rank, or trade. This session did not curl.

AUTHORITY: `context_only`. Do not add calibrated confidence, ranking, size, or trade language. Do not sum ETF-held par with issuer outstanding. Do not join issuers by name or theme — CIK / `issuer_master` only. Do not add `DebtInstrumentMaturityDate` per-instrument identity in a records PR. Do not start a second companyfacts store. Remaining cash-burn assembly is **not** MiniMax-contracted here.

---

### MO-PAID-040 — [MO-B F09-CAPITAL-MATERIALS] Supply-chain layers wait on a named source
DISPOSITION: HOLD

HOLD — Chairman / commercial contract authority owns the missing named cross-layer physical-flow source (raw → chokepoint → refining → fabrication → distribution → end-market). None exists in `config/` with rights to document a ≥3-layer sourced chain for one commodity. Closest named pieces: EIA WPSR oil **one-layer** physical balance (`engine/commodity_supply_context.py`); Census HS6 **distribution-only** (`config/trade_flow_codes.yml`). Pair MO-DELTA-027. Gate record: `research/market_intelligence_productization/MARKET_ONTOLOGY_HALF_B_RIGHTS_AND_UPSTREAM_GATE_DOCKET_2026-09-06.md:69-73`.

EVIDENCE NOW (why this is HOLD, not a contract):
- Ledger: `NEW_BOUNDED_BUILD` / `PARTIAL` / `next_bounded_child=DOCKETED_TERMINAL_HALF_B; DEFER — named cross-layer physical-flow source required first` / acceptance “one commodity documents a >=3-layer sourced chain” / `source_rights=EIA public covered for oil; metals/ag/semis supply-chain source UNVERIFIED` / ceiling `display-only LEAF, never feeds scoring`. Command: `rg '^MO-PAID-040,' research/market_intelligence_productization/MARKET_ONTOLOGY_F00C_GRANULAR_CLOSURE_LEDGER_2026-09-02.csv` (CSV line 84). That DEFER is still true.
- Half-B docket `research/market_intelligence_productization/MARKET_ONTOLOGY_HALF_B_RIGHTS_AND_UPSTREAM_GATE_DOCKET_2026-09-06.md:69-73` — who can open it: Chairman / commercial contract authority; first slice only on gate open. Command: `sed -n '69,73p' research/market_intelligence_productization/MARKET_ONTOLOGY_HALF_B_RIGHTS_AND_UPSTREAM_GATE_DOCKET_2026-09-06.md`.
- Existing oil physical-balance producer is **one layer**, not a chain: `engine/commodity_supply_context.py:1-8,32-40` (EIA WPSR seasonal-anomaly stocks; display-only LEAF; “Physical balance ≠ price direction”). Consumer: `scripts/build_commodities.py:414-422`. Sibling: `engine/strategic_reserves.py:1-16` (US SPR + JODI levels, not a modeled supply-chain layer). Command: `sed -n '1,8p;32,40p' engine/commodity_supply_context.py`; `sed -n '414,422p' scripts/build_commodities.py`.
- `engine/commodity_coverage_matrix.py:1-10,20-26` — the only lawful source labels a coverage cell may cite are `EIA Weekly Petroleum Status Report` and `Daily price history (Yahoo)`. Command: `sed -n '1,10p;20,26p' engine/commodity_coverage_matrix.py`.
- Coverage matrix honest gap: `research/market_intelligence_productization/MARKET_ONTOLOGY_F09_COMMODITY_COVERAGE_MATRIX_2026-09-02.md:38-42` — “No physical supply-chain layer anywhere except oil.” Command: `sed -n '38,42p' research/market_intelligence_productization/MARKET_ONTOLOGY_F09_COMMODITY_COVERAGE_MATRIX_2026-09-02.md`.
- `ls engine/commodity_*.py | wc -l` → **13** files (ledger’s “11-file” count is stale; not a 3-layer source). Price/signal family exists; that is the PARTIAL the ledger already records.
- Named-source hunt in `config/` (excluding `ruling_graph.yml` / `synapse.yml`): `rg -n -i 'physical.flow|cross.layer|raw.?chokepoint|fabrication|end-market' config` → no 3-layer physical-flow source. Hits are `config/trade_flow_codes.yml:135,206` (HS6 import codes as a **distribution** leg, not a chain) and unrelated “fabrication”/chokepoint wording in other configs. `config/trade_flow_codes.yml:1-18` is a curated HS6 → theme map (Census), display/context tier. Command: `sed -n '1,18p' config/trade_flow_codes.yml`.
- F09 semis census (later than the ledger): `research/market_intelligence_productization/MARKET_ONTOLOGY_F09_SEMIS_SOURCE_CENSUS_2026-09-09.md:122-136` — “a public physical chain of three or more layers is **not nameable**”; raw collector VERIFIED-NEGATIVE; refining/fabrication COMMERCIAL-GATE; distribution is Census HS6 PUBLIC-BUILDABLE-DARK (`data/trade_flows` absent). Command: `sed -n '122,136p' research/market_intelligence_productization/MARKET_ONTOLOGY_F09_SEMIS_SOURCE_CENSUS_2026-09-09.md`.
- Fixture tests of the existing one-layer oil read and the coverage inventory are green (`tests/test_commodity_supply_context.py`, `tests/test_commodity_coverage_matrix.py` in the 133-pass run). They do not, and must not, be treated as the ≥3-layer acceptance test.

No MiniMax contract. No records rewrite that would claim a 3-layer source. Pair MO-DELTA-027 stays DEFER with this row.

AUTHORITY: `display-only LEAF, never feeds scoring`. When the gate later opens, the first slice is still “one commodity documents a ≥3-layer sourced chain” — never a scoring input, never a physical-financial arbitrage signal (F09 `do_not_redo` on MO-PAID-041 / MO-DELTA-030).
