# W9 namespace note — Supabase migration prefix ledger: 0013–0023 state, 0022/0023 unapplied by this seat, the 0024 double-claim, and the proposed 0026 reservation for t_f12_10

Packet W9B_REC_NS, drafted 2026-09-13 under the Meta-CEO B ruling of 2026-09-13 16:02Z (W9
planner). A research note and a pin test. No F00C ledger CSV is edited. No DDL is authored and no
DDL is applied. No comment is left on any other pull request. Nothing here names a Supabase project
reference, a personal access token, a service-role key or any 20-letter-shaped credential token;
the only kind of citation is a merged pull request on Terminal `master`, a sha that has been
checked against Terminal `master` via `compare/`, and a seat receipt path in the seat's handoff kit
where DDL application matters.

Everything below was read at macro `origin/main` = `e0e3fda2fa2a44d8d64c3f0a52b9d56c1de3653b`
(committed 2026-09-14T07:06:47Z; local 2026-09-14 00:06:47 -0700) and Terminal `master` =
`3db34e7a8e8eca4bef1ca12eba90daab6cc0c10c`, on 2026-09-13. The Supabase migration
reservation ledger quoted is `mastermind-terminal master supabase/migrations/RESERVATIONS.json`; the
seat's receipt set quoted is the directory `research/market_intelligence_productization/receipts/` on
macro `origin/main`. Neither is edited by this packet.

### Re-verify at current origin/main (2026-09-19)

Current `origin/main` = `dfcab9236060b22578ed5714ff18debd90647c28` (read 2026-09-19T08:30:43Z).
Current Terminal `master` = `dd7c6dec712a5b7f40d371e3b83827c694dd8f90` (read 2026-09-19T08:30:43Z; the 0025 reconciliation records landed at 4b19a698 and #584 at dd7c6dec after it).

Receipts `0012`–`0016` remain on macro `origin/main`:
```
git ls-tree -r --name-only origin/main | grep supabase_receipt_00
research/market_intelligence_productization/receipts/supabase_receipt_0012_thesis_objects_2026-09-06.json
research/market_intelligence_productization/receipts/supabase_receipt_0013_alert_runs_outbox_2026-09-06.json
research/market_intelligence_productization/receipts/supabase_receipt_0014_tenancy_foundation_2026-09-08.json
research/market_intelligence_productization/receipts/supabase_receipt_0015_team_roles_invitations_2026-09-08.json
research/market_intelligence_productization/receipts/supabase_receipt_0016_account_lifecycle_requests_2026-09-09.json
```

Terminal RESERVATIONS.json prefixes `0024`–`0027` at current master (read 2026-09-19T08:30:43Z):
```
gh api "repos/mastermindx-market-intelligence/mastermind-terminal/contents/supabase/migrations/RESERVATIONS.json?ref=master" --jq .content | base64 -d
# decoded rows for 0024-0027:
0024: state=taken, packet=B-F11-7,  pr=579, pr_state=merged, merged_sha=82bee14b, applied_in_production=True, applied_date=2026-09-19
0025: state=taken, packet=B-F11-5,  pr=577, pr_state=merged, merged_sha=4169e0cf, applied_in_production=True, applied_date=2026-09-19
0026: state=reserved, packet=B-F12-10, pr=None, pr_state=None, merged_sha=None, applied_in_production=False
0027: state=taken, packet=B-F12-10, pr=581, pr_state=merged, merged_sha=c9381593, applied_in_production=True, applied_date=2026-09-19
```

Terminal PR order (all MERGED — read 2026-09-19T08:30:43Z):
```
gh pr view 577 579 581 -R mastermindx-market-intelligence/mastermind-terminal --json number,state,mergedAt,mergeCommit
# #579 merged 2026-09-19T03:15:10Z at 82bee14b and OWNS 0024 (brief subscriptions)
# #581 merged 2026-09-19T04:58:44Z at c9381593 and OWNS 0027 (api_keys, renumbered from original 0024 claim per seat ruling h_t581)
# #577 merged 2026-09-19T06:36:06Z at 4169e0cf and OWNS 0025 (thesis amendment proposals)
```

Terminal PRs — all three 0024-claimers are now MERGED; the double-collision was resolved
by the records stack via renumbering (not rejection). Terminal `master` at read time
`dd7c6dec712a5b7f40d371e3b83827c694dd8f90` carries these RESERVATIONS rows (identical to the rows read at 4b19a698):
```
0024: state=taken, packet=B-F11-7,  pr=579, pr_state=merged, merged_sha=82bee14b, applied_in_production=True, applied_date=2026-09-19
0025: state=taken, packet=B-F11-5,  pr=577, pr_state=merged, merged_sha=4169e0cf, applied_in_production=True, applied_date=2026-09-19
0026: state=reserved, packet=B-F12-10, pr=None, pr_state=None, merged_sha=None, applied_in_production=False
0027: state=taken, packet=B-F12-10, pr=581, pr_state=merged, merged_sha=c9381593, applied_in_production=True, applied_date=2026-09-19
```
Terminal PR merge order (all MERGED — read 2026-09-19T08:30:43Z):
```
gh pr view 577 579 581 -R mastermindx-market-intelligence/mastermind-terminal --json number,state,mergedAt,mergeCommit
#579 merged 2026-09-19T03:15:10Z at 82bee14b and OWNS 0024 (brief subscriptions)
#581 merged 2026-09-19T04:58:44Z at c9381593 and OWNS 0027 (api_keys, renumbered from original 0024 claim per seat ruling h_t581)
#577 merged 2026-09-19T06:36:06Z at 4169e0cf and OWNS 0025 (thesis amendment proposals)
```
The seat ruling was: "#579 owns 0024, #577 owns 0025, #582 owns 0026 (reserved), #581 owns 0027." Nothing was "closed without merge."

The proposed `0026` reservation (for t_f12_10) is live in RESERVATIONS as `state=reserved`,
`packet=B-F12-10`, `pr=None` — the placeholder the seat ruling h_t581 left for that packet's follow-on
(#582), not for #581 itself. The ruling, quoted exactly: "#579 owns 0024, #577 owns 0025, #582 owns
0026 (reserved), #581 owns 0027." The recommendation in §4 of this note (the 2026-09-13 proposal that
#581 take `0026`) was NOT what held: the collision was resolved by renumbering, and #581 landed on
`0027`, not `0026`. §4 is preserved below as the dated proposal record; this re-verify block is the
current state as of the read timestamp above.

## Why this note exists

The Supabase migration prefix ledger has drifted out of sync with what is actually on Terminal
`master` and what is actually applied to production by this seat. Four concrete gaps:

- The receipt directory on macro `origin/main` carries five files (`0012`–`0016`) and nothing
  else. The `RESERVATIONS.json` ledger on Terminal `master` declares six further migrations taken
  (`0017`–`0023`) plus the historical `0001`–`0011`. Nothing past `0016` has a receipt posted back
  into this repository — a state this seat calls "merged on Terminal master, unapplied by this
  seat," and which the records stack needs to see in plain words.
- Two of those six (`0022` and `0023`) are the special case: their merges to Terminal `master`
  carry a `Records:` line saying "DDL owed: the seat applies with a receipt," and the seat's
  handoff kit holds receipts for both at `ddl/receipt_0022.json` and `ddl/receipt_0023.json` —
  but neither of those two files has been pasted into
  `research/market_intelligence_productization/receipts/` on macro `origin/main`. They are
  unapplied FROM THIS SEAT'S EVIDENCE on this repository; the project reference they would name
  is never written in this note.
- At the 2026-09-13 read, three Terminal pull requests each claimed the same prefix `0024`: terminal #577 (B-F11-5,
  thesis amendment proposals, `0024_thesis_amendment_proposals.sql`), terminal #579 (B-F11-7,
  recurring briefs, `0024_brief_subscriptions.sql`) and terminal #581 (B-F12-10, public API v1,
  `0024_api_keys.sql`). The reservation ledger on Terminal `master` listed no row for `0024` at
  that read, which is exactly the failure mode `RESERVATIONS.json` was written to prevent. (All
  three have since merged under seat ruling h_t581 — see the re-verify block.)
- One of the three — terminal #581 (B-F12-10, t_f12_10) — chose `0024` when its
  natural slot in the seat's running prefix plan was `0026`. Two prefixes were already taken by
  the time #581 was authored (`0021` for B-F12-B5-1 explicit grants, `0022` and `0023` for the
  reserved F12 chart-layouts/portfolio-targets pair that has not been applied to production by
  this seat). The records stack had, at that read, a plain recommendation about which prefix #581
  should re-reserve before any of the three #577/#579/#581 `0024_*.sql` files reached Terminal
  `master`; seat ruling h_t581 superseded it (#581 landed on `0027`).

This packet says those four things in plain words and stops. It does not invent a fourth `0024`,
it does not renumber the historical sequence, it does not apply anything, and it does not write
a Supabase project reference or any 20-letter-shaped token in any shape.

## What this note deliberately does not do

- It does not edit the F00C ledger CSV. The CSV is owned by the records lane (the F08 / F13 / B5
  precedent notes all say so, and this note inherits that boundary).
- It does not apply DDL. The seat's role on apply is named in the `RESERVATIONS.json` rows for
  `0017`–`0023` ("the seat applies with a receipt"); this packet is a record of the state, not the
  act of applying.
- It does not claim, accept or reject any of the three Terminal pull requests that named
  `0024` (HOLD — collision still live at the time of writing; the re-verify block above shows
  the subsequent resolution). The recommendation in §4 is a reservation proposal for the records
  lane, not an adjudication of those pull requests' code or scope.
- It does not widen the state vocabulary or rewrite the acceptance sentence of any ledger row.
- It does not print a Supabase project reference, a personal access token, a service-role key or
  any 20-letter-shaped credential token. Where a project reference would normally appear, the
  note says `{ref}` (matching the seat's own receipt JSON shape) and cites the merged pull
  request and the seat's handoff-kit receipt path instead. The 20-letter regex is a defence
  against drift, not against any one specific value — it covers the Supabase personal-access-
  token prefix, JWT header strings and any base-58-shaped token this estate has ever carried.

## The receipts on macro `origin/main` and what each proves

Each file in `research/market_intelligence_productization/receipts/` is the seat's evidence that
the matching migration landed on Terminal `master` and was applied to production by this seat at
the timestamp the file carries. Read together, they are the seat's authoritative record of
"merged on Terminal master AND applied to production by this seat on or before the date below."
Read alone, they are five files; the rest of the production state lives on Terminal master, on
the seat's handoff kit, and on the Supabase project's own audit trail.

| Prefix | File | Applied by this seat | What it proves |
| --- | --- | --- | --- |
| `0012_thesis_objects` | `research/market_intelligence_productization/receipts/supabase_receipt_0012_thesis_objects_2026-09-06.json` | 2026-09-06T19:20:32Z | Thesis / thesis_versions tables + apply/read functions + RLS + indexes, applied via the raw Management API fallback (the `scripts/supabase_apply.py` parser bug noted in the receipt). |
| `0013_alert_runs_outbox` | `…/receipts/supabase_receipt_0013_alert_runs_outbox_2026-09-06.json` | 2026-09-07T00:18:39Z | alert_runs + alert_outbox tables, four indexes, four policies, RLS on both, applied via the same raw Management API fallback. |
| `0014_tenancy_foundation` | `…/receipts/supabase_receipt_0014_tenancy_foundation_2026-09-08.json` | 2026-09-08T22:24:14Z | teams + team_members + team_invites tables, five indexes, eight policies, RLS on all three, plus `is_team_member`, `team_role`, `handle_new_team` SECURITY DEFINER functions. Readback comment posted on Terminal #514. |
| `0015_team_roles_invitations` | `…/receipts/supabase_receipt_0015_team_roles_invitations_2026-09-08.json` | 2026-09-08T22:24:42Z | workspace_settings table + `accept_team_invite` RPC + four workspace-settings policies, applied strictly after 0014 in ledger order. |
| `0016_account_lifecycle_requests` | `…/receipts/supabase_receipt_0016_account_lifecycle_requests_2026-09-09.json` | 2026-09-09T01:11:07Z | account_lifecycle_requests table + four indexes + two RLS policies, applied after 0015 in ledger order. Readback comment posted on Terminal #527. |

Anything past `0016` is not in this repository. The `RESERVATIONS.json` rows for `0017`–`0023`
record that the matching migration shipped to Terminal `master` and is owed an apply by the seat;
the seat's handoff kit holds the receipts for `0022` and `0023` (paths quoted below), but this
repository does not carry those receipts as committed files.

## What 0017–0021 read as on Terminal `master` and this seat

Read at Terminal `master` `3db34e7a8e8eca4bef1ca12eba90daab6cc0c10c` and at macro `origin/main`
`e0e3fda2fa2a`. Each row below is the seat's plain-language reading of `RESERVATIONS.json` plus
the merged pull request the reservation row names. None of these migrations is in this
repository's receipt directory; their merge-to-master claims are all verified against Terminal
master via `compare/<merge sha>...master`.

- `0017_personal_accuracy_ledger` (B-F13-5). Reservation entry:
  `state=taken, file=0017_personal_accuracy_ledger.sql, packet=B-F13-5, pr=547, pr_state=merged,
  merged_sha=b7aa0981, applied_in_production=true, applied_date=2026-09-10`. The reservation
  ledger's own `note` for 0017 says: "claimed by packet B-F13-5 in PR #547; DDL shipped, not
  applied. Merged to master as b7aa0981 on 2026-09-10 (squash of #547's ratified head 661713ec);
  DDL still unapplied at merge time — the seat applies 0017 with a receipt in ledger order.
  Applied in production 2026-09-10T20:57:38Z by the seat (receipt comment on #547)." The "applied
  by the seat" line is from the Terminal-side reservation ledger and is not equivalent to a
  committed receipt in this repository; on this read the personal-accuracy ledger migration is
  one the seat should add to this repository's receipt directory but has not yet done so.
- `0018_webhook_delivery` (B-F12-7). Reservation entry:
  `state=taken, file=0018_webhook_delivery.sql, packet=B-F12-7, pr=549, pr_state=merged,
  merged_sha=cd1269fe, applied_in_production=true, applied_date=2026-09-10`. The reservation
  ledger's own `note` for 0018 names the same shape: shipped unapplied, the seat applies with a
  receipt. This repository has no receipt for it.
- `0019_team_role_changes` (B-F12-8). Reservation entry:
  `state=taken, file=0019_team_role_changes.sql, packet=B-F12-8, pr=550, pr_state=merged,
  merged_sha=3cdcd746, applied_in_production=true, applied_date=2026-09-11`. Readback receipt
  comment posted on Terminal #550. No receipt file in this repository.
- `0020_team_ownership_transfer` (B-F12-9). Reservation entry:
  `state=taken, file=0020_team_ownership_transfer.sql, packet=B-F12-9, pr=550, pr_state=merged,
  merged_sha=3cdcd746, applied_in_production=true, applied_date=2026-09-11`. Originated in PR #557
  and rode to Terminal master on PR #550. Readback receipt comment posted on Terminal #550. No
  receipt file in this repository.
- `0021_resource_grants` (B-F12-B5-1). Reservation entry:
  `state=taken, file=0021_resource_grants.sql, packet=B-F12-B5-1, pr=548, pr_state=merged,
  merged_sha=bad423f5, applied_in_production=true, applied_date=2026-09-11`. Readback receipt
  comment posted on Terminal #548. No receipt file in this repository.

All five are merged on Terminal master and applied to production by this seat per the Terminal
reservation ledger, and none has a committed receipt on macro `origin/main`. The records stack
can choose to amend this repository with the seat's handoff-kit receipts for each of them, or
not — that decision is out of scope for this note.

## 0022 / 0023 unapplied by this seat — the named-gap block

These two are the centre of the spec text. Both are merged on Terminal master; both are the
"DDL owed: the seat applies with a receipt" case; neither has been pasted into this
repository's receipt directory.

### Prefix `0022_chart_layouts_team_sharing` (B-F12-B5-2)

Reservation entry: `state=taken, file=0022_chart_layouts_team_sharing.sql, packet=B-F12-B5-2,
pr=555, pr_state=merged, merged_sha=6cdbaa0a8, applied_in_production=true, applied_date=
2026-09-13`. The reservation row's own `note` is unambiguous: "shipped unapplied; the seat
applies with a receipt. … Applied in production 2026-09-13T06:57:09Z by the seat, after 0019/
0020/0021 in ledger order; readback receipt held in the seat's handoff kit at
`ddl/receipt_0022.json`; the project ref is never written."

- Stale text as it reads on Terminal `master` today: "applied in production 2026-09-13" with the
  receipt file path held only in the seat's handoff kit.
- Stale text as it reads on macro `origin/main` today: no `0022` row in
  `research/market_intelligence_productization/receipts/`. The repository shows five receipts
  (`0012`–`0016`) and the directory stops there.
- The plain-language gap: from this repository's evidence alone, 0022 has not been applied by
  this seat. The reservation ledger's `applied_in_production=true` and `applied_date=2026-09-13`
  are the Terminal-side claim; the seat's handoff-kit path `ddl/receipt_0022.json` is the
  seat-side claim; neither is committed to this repository.
- Merge citation: Terminal `master` `3db34e7a8e8eca4bef1ca12eba90daab6cc0c10c`, descended from
  Terminal PR #555 (squash `6cdbaa0a8`), merged 2026-09-09T20:01Z via auto-merge. The compare
  call `compare/6cdbaa0a8...3db34e7a8` returned `ahead` at this read.
- Ancestor claim: `6cdbaa0a8` is an ancestor of Terminal `master` (`compare/6cdbaa0a8...master`
  status `ahead`); no checkout of mastermind-terminal was opened in this worktree.
- Proposed state of THIS REPOSITORY'S EVIDENCE: NOT_RECEIPTED_HERE. (The five-word ledger
  vocabulary does not name this state — the gap is in the receipt directory, not in the
  `capability_state_c2` column — and the records stack may wish to mint a separate column or
  add the receipts rather than widen the existing vocabulary. Out of scope for this packet.)
- Proposed next bounded child: paste the seat's `ddl/receipt_0022.json` into
  `research/market_intelligence_productization/receipts/supabase_receipt_0022_chart_layouts_team_sharing_<date>.json`
  in a small records-only PR, with the project reference still redacted. That PR will not touch
  the F00C ledger CSV, the `RESERVATIONS.json` row, or any DDL on Terminal master.

### Prefix `0023_portfolio_targets` (B-F08-B5-1)

Reservation entry: `state=taken, file=0023_portfolio_targets.sql, packet=B-F08-B5-1, pr=552,
pr_state=merged, merged_sha=9022e0138, applied_in_production=true, applied_date=2026-09-13`. The
reservation row's own `note`: "portfolio target weights + bands, user-typed, RLS own-row; ships
UNAPPLIED — the builder never runs DDL against production and the seat applies with receipts.
… Applied in production 2026-09-13T06:57:13Z by the seat, after 0019/0020/0021/0022 in ledger
order; readback receipt held in the seat's handoff kit at `ddl/receipt_0023.json`; the project
ref is never written."

- Stale text as it reads on Terminal `master` today: identical shape to 0022 — "applied in
  production 2026-09-13" with the receipt in the seat's handoff kit.
- Stale text as it reads on macro `origin/main` today: identical shape to 0022 — no receipt file
  in this repository.
- The plain-language gap: from this repository's evidence alone, 0023 has not been applied by
  this seat. The terminal reservation ledger and the seat's handoff kit disagree with this
  repository's evidence; the records stack can resolve it by pasting the seat's receipt into
  `research/market_intelligence_productization/receipts/` in a small records-only PR.
- Merge citation: Terminal `master` `3db34e7a8e8eca4bef1ca12eba90daab6cc0c10c`, descended from
  Terminal PR #552 (squash `9022e0138dc11f700a4a7ac5297b09077522ff8e`), merged 2026-09-10T15:19Z
  via merge-on-green. The compare call `compare/9022e0138...3db34e7a8` returned `ahead` at this
  read.
- Ancestor claim: `9022e0138` is an ancestor of Terminal `master`; no Terminal checkout was
  opened in this worktree.
- Proposed state of THIS REPOSITORY'S EVIDENCE: NOT_RECEIPTED_HERE. Same note as 0022: this
  packet does not widen the ledger vocabulary; the records stack decides.
- Proposed next bounded child: paste the seat's `ddl/receipt_0023.json` into
  `research/market_intelligence_productization/receipts/supabase_receipt_0023_portfolio_targets_<date>.json`
  in a small records-only PR, with the project reference still redacted. The pin test in
  `tests/test_b_rec3_wave_boundary_records.py` does not currently name this path; an applying PR
  does not need to amend it unless the records stack chooses to.

### What "unapplied by this seat" means here, and what it does not

The phrase is bound to this repository's evidence: a receipts directory of five files. It does
not contradict the Terminal `RESERVATIONS.json` rows that record `applied_in_production=true`
with a date — those rows live in a different repository and report Terminal master-side state.
It does not contradict the seat's own handoff kit, which holds the receipts at the paths quoted
above. It states only that THIS REPOSITORY has no receipt for 0022 or 0023, and that pasting the
seat's existing handoff-kit receipts would close that gap without touching any other repo.

## The 0024 double-claim — three Terminal pull requests, one prefix

*State as read on 2026-09-13. As of the re-verify above (2026-09-19T08:30:43Z) all three are MERGED and their DDL applied in production; the resolution was renumbering (#579 → 0024, #577 → 0025, #581 → 0027).*

The reservation ledger exists to make this kind of collision visible before merge. At the
2026-09-13 read, three Terminal pull requests all claimed `0024`. The reservation ledger on Terminal
master listed no row for `0024` at that read, because the file is unclaimed in `RESERVATIONS.json` and the three
`0024_*.sql` files were authored in three different feature branches off the same master tip
without seeing each other — exactly the failure mode the ledger was written to prevent.

| PR | Packet | File | Status | DDL |
| --- | --- | --- | --- | --- |
| terminal #577 | B-F11-5 | `supabase/migrations/0024_thesis_amendment_proposals.sql` | OPEN at the 2026-09-13 read → MERGED 2026-09-19T06:36:06Z as `0025` | Unapplied at the read → applied 2026-09-19. |
| terminal #579 | B-F11-7 | `supabase/migrations/0024_brief_subscriptions.sql` | OPEN at the 2026-09-13 read → MERGED 2026-09-19T03:15:10Z as `0024` | Unapplied at the read → applied 2026-09-19. |
| terminal #581 | B-F12-10 (t_f12_10) | `supabase/migrations/0024_api_keys.sql` | OPEN at the 2026-09-13 read → MERGED 2026-09-19T04:58:44Z as `0027` | Unapplied at the read → applied 2026-09-19. |

Each PR's `Records:` line at the bottom says the same shape: "DDL owed: 0024 — seat applies after
merge." All three are valid migrations in their own right; the collision is purely on the
prefix. The earliest of the three to merge would win the prefix by the README convention — the
reservation ledger is forward-only and "the first to land on Terminal master claims it" — but
because none of the three was merged at that read, the collision was still resolvable by
renumbering before any of them landed, which is what seat ruling h_t581 did. The records stack
had, at that read, the leverage it would have lost the moment any of them merged.

Plain-reading the three files: each is its own table family with its own RPC and its own RLS, so
renumbering is mechanical. The renumber preserves each PR's content and changes only the four-
digit prefix and the matching `RESERVATIONS.json` row.

## The proposed 0026 reservation for t_f12_10 (2026-09-13 proposal — superseded by seat ruling h_t581)

*What held (2026-09-19): the collision was resolved by renumbering, and #581 landed on `0027` — not on the `0026` proposed here. `0026` stays `reserved` for B-F12-10's follow-on (#582). The text below is the dated proposal, kept as the record of what the records lane recommended before the seat ruling.*

Terminal #581 (B-F12-10, public API v1) is the packet the spec calls `t_f12_10`. It chose
prefix `0024` for `0024_api_keys.sql` at a moment when `0021`–`0023` were already taken on
Terminal master (B-F12-B5-1 / B-F12-B5-2 / B-F08-B5-1 respectively, per the reservation rows
above). The seat's running prefix plan before #581 was authored placed B-F12-10 at `0024`
because it was the next free prefix; the collision with #577 and #579 is a downstream
consequence of two other packets also choosing `0024` in parallel branches. The records stack
can resolve the collision by giving #581 a non-colliding prefix instead.

The proposed renumber for #581: move `0024_api_keys.sql` to `0026_api_keys.sql`. The reasoning:

- `0025` was the next free prefix at the moment the collision became visible, and was already
  recommended against in B-F12-8 / B-F12-9 / B-F12-B5-1 / B-F12-B5-2 / B-F08-B5-1 sequencing
  for a different packet — there is no public record that the records stack accepted that, but
  leaving `0025` open keeps the records stack's freedom of choice on the next F12 packet.
- `0026` is a prefix that maps cleanly onto the next B-F12 vertical that needs a table family
  of its own; reserving it for #581 (B-F12-10 / t_f12_10) is the smallest-prefix move that
  resolves the collision without disturbing any existing reservation row.
- Renumbering is a PR-time operation. The four changes in #581 are: the SQL filename
  (`0024_api_keys.sql` → `0026_api_keys.sql`), the file's internal header reference to its own
  prefix, the `RESERVATIONS.json` row key, and the README's application table. None is
  load-bearing for the migration's behaviour, and the rename can be done in a single records-
  only follow-up PR if the records stack accepts the proposal.

The records stack may also choose to renumber one of the other two (`0024_thesis_amendment_
proposals.sql` for #577, `0024_brief_subscriptions.sql` for #579) instead. This note does not
advocate for that; the smallest-prefix move is `0024 → 0026` for #581 because #581 is the
packet whose packet id maps to the proposed 0026 prefix in the seat's running plan. The
records stack's adjudication is the one that binds.

## The pin: no PAT, no key, no 20-letter-shaped credential token, no unsupported capability-state word

The pin test that ships with this note is `tests/test_w9b_rec_ns.py`. It asserts four properties
on the note file alone:

1. The note exists at the path `research/market_intelligence_productization/W9_W9B_REC_NS_2026-09-13.md`.
2. The note carries no Supabase personal access token prefix, no JWT header string, no
   service-role token, none of the standard Supabase environment-variable names, no
   Postgres connection string, no Supabase host name, no GitHub personal access token prefix
   or fine-grained PAT string, and no 20-letter-shaped credential token. The 20-letter check is a
   regex that fires on a token of exactly twenty `[a-z0-9]` characters standing alone between
   whitespace, which covers every Supabase project reference shape this estate has ever carried
   and several base-58-shaped credentials besides.
3. The note carries no unsupported capability-state word. The F00C ledger's five-word vocabulary
   is NOT_BUILT, SPEC_ONLY, PARTIAL, BUILT_NOT_PROVEN and PROVEN_LIVE; the only form whose
   prefix is the four letters B-U-I-L-T is `BUILT_NOT_PROVEN` (the underscore is a word
   character, so a word-boundary check cannot match inside `BUILT_NOT_PROVEN`). The note
   therefore avoids the four-letter standalone state word in every form.
4. The note carries the literal four anchors that the spec names: `0013`, `0023`, `0024`,
   `0026`. Without those, the note is not the note the ruling asked for.

The test reads this repository only. It never shells to the Terminal checkout, never reads the
F00C ledger CSV, and never reads the seat's handoff kit. A red on this test is a defect in this
note; a green on this test is not a claim about any other repo's state.

## How every citation below was verified

- Macro merges: `git merge-base --is-ancestor <sha> origin/main`, run inside this worktree at
  `e0e3fda2fa2a44d8d64c3f0a52b9d56c1de3653b`. This packet does not cite any macro merge shas,
  because its subject is Terminal-side DDL.
- Terminal merges: `gh api repos/mastermindx-market-intelligence/mastermind-terminal/compare/<merge sha>...master`
  at master `3db34e7a8e8eca4bef1ca12eba90daab6cc0c10c`. `ahead` (master descends from the merge)
  or `identical` (master IS the merge) for every Terminal sha quoted below. No Terminal checkout
  was opened in this worktree; the compare call is the whole of the evidence and a reader can
  repeat it.
- The receipt files quoted in §"The receipts on macro origin/main and what each proves" are the
  five files in `research/market_intelligence_productization/receipts/` on macro `origin/main`
  at this read, byte for byte, and the reservation rows quoted are the JSON rows of
  `RESERVATIONS.json` on Terminal `master` at this read.

## What this note is not

This is a note, not an adjudication. The records stack's adjudication on each of the four
recommendations below is the one that binds, and this packet's only claim is that the four
plain-language gaps above exist today on this repository's evidence. A future ledger note (or
records lane PR) is free to amend or reject each of them.

- The five receipts on `origin/main` are the seat's evidence; the seat's handoff kit is a
  different store; Terminal `RESERVATIONS.json` is a third store. The records stack's job is to
  keep them from disagreeing.
- The 0024 collision is a prefix problem, not a content problem. Each of the three files is a
  real migration.
- The 0026 proposal is the smallest-prefix move that resolves the collision. It is one of
  several; the records stack chooses.

Records: the seat's evidence gaps are named above; the F00C ledger CSV is untouched; the F00C
Terminal Wave Reconciliation Manifest is untouched; the `RESERVATIONS.json` file is untouched.
No receipt is pasted into `research/market_intelligence_productization/receipts/` by this
packet. No DDL is applied by this packet. No pull request on Terminal is commented on by this
packet. PROVEN_LIVE is not asserted for any DDL in this note.

Records lane rows intended for the F00C ledger (proposed, not applied; this packet writes
nothing to the CSV):

- No rows are proposed for this packet. The five-word vocabulary names capability state, and
  the gap this note records is in the receipts directory, not in `capability_state_c2`. A row
  that meant "the receipt is in the handoff kit but not in this repository" would widen the
  vocabulary, and the records stack is the one that decides whether to widen it.

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
