# Market Ontology F04 Explorer — Product, Intelligence and Experience Architecture Freeze

**Date:** 2026-09-04  
**Status:** `ARCHITECTURE_FREEZE / RECORDS_ONLY / NO PRODUCT EFFECT`  
**Architecture operation:** `marketontology-f04-explorer-architecture-20260904-sol-001`  
**Existing parent operation preserved:** `marketontology-f04-ontology-transmission-20260826-fable-001`  
**Linear projection:** `MAS-145`  
**Coordinating workstream:** `WS:MARKET-OS`  
**Dependent workstreams:** `WS:GMI-THEME-GRAPH`, `WS:ALPHA-INTELLIGENCE-INTEGRATION`, native TXI/rates/portfolio/Brain owners  
**Protected procedure:** `mastermindx-market-intelligence/Mastermind@22b36b830bd5560942186ada7597508f918696af` (`mastermind.sol_skillpack.v1`, version 1.0.1, bootstrap major 1)  
**Macro archaeology base:** `mastermindx-market-intelligence/macro@0007d955278c0456507bb4854eb85ddb41e2874e`  
**Terminal archaeology base:** `mastermindx-market-intelligence/mastermind-terminal@fadd8b82f03ecaabe8a86d693da89f27be096d9f`  
**Cognition route:** `CHAT_INCLUDED_DEFAULT / PRO_MODE_EXCEPTION / CROSS_SYSTEM_ARCHITECTURE`  
**Capability delta of this document:** `NONE`

> **SUPERSESSION NOTICE (2026-09-04).** Amendment 3,
> `MARKET_ONTOLOGY_F04_EXPLORER_ARCHITECTURE_AMENDMENT_3_REVIEW_CLOSURE_2026-09-04.md`,
> is the controlling review-closure erratum. Read it first. It binds the F00 consumption edge,
> authenticated shared navigation, request-time freshness, K1 evidence forms, honest sample
> denominators, theme art directions, protected dependency paths, and the eight-record Git
> census. Amendment 2 controls authenticated transport. In particular, the static payload in
> §6.3, the `Header` label in §7.1, the underspecified read model in §12, the vague/static X1
> paths in §23, and the corresponding acceptance clauses in §24.1 are superseded inline below.

---

## 1. Executive ruling

Mastermind should build the full **Ontology Explorer** experience shown by the Chairman's reference composition. The missing product is not a new intelligence plane. It is the user and machine composition layer that the existing Market Ontology F04 parent was created to own.

The product must be built **from the ground up as an experience**, while reusing rather than rebuilding the canonical intelligence estate:

- TXI staged transmission chains, episodes, lag windows, falsifiers and historical calibration;
- Rate & Inflation Transmission current state and display-only scenario sensitivities;
- GMI local/canonical theme graph and its point-in-time/correction contracts as those waves are accepted;
- K3-D economic-propagation hypothesis records only after their exact carrier is accepted;
- existing incorporation/dislocation and specialist-owner outputs;
- Market OS Portfolio and Watchlist truth;
- existing portfolio risk and per-name macro-sensitivity readers;
- the existing Brain gateway and user context handoff;
- the existing Terminal/Supabase User Plane for future private saves.

The governing sentence is:

> **Build the explorer; compose the intelligence. Do not rebuild the organs.**

This freeze creates no second graph, scenario engine, evidence store, identity resolver, portfolio store, user database, correction ledger, analytics transport, AI gateway, scheduler, lifecycle or authority plane.

---

## 2. Why the product was planned but did not appear

The original F04 mission already included interactive explanation paths, Transmission Gap Monitor, Second-Order Screener, Opportunity Radar/Map and dislocation context. The detailed 2026-08-28 completion plan then serialized the visible F04 product behind:

`D2C + D2D -> D2E -> W3B -> W3C -> F04-A -> F04-B -> F04-C`

That sequence protected owner semantics, but it made every useful F04 surface wait for the entire GMI frontier. Subsequent program-control and source-integrity work advanced without a product owner delivering the explorer. The result is a familiar estate failure: detection and substrate exist, but the operator-facing assembly does not.

This freeze corrects sequencing without weakening architecture:

- GMI continues its own lawful dependency order;
- K3-D remains on its exact held carrier;
- product features that actually depend on those owners remain gated;
- **owner-safe F04 verticals that can be built from already-live TXI/rates/portfolio/Brain substrates may proceed in parallel**;
- the first useful product milestone occurs after one real end-to-end vertical, not after a long run of infrastructure-only PRs.

---

## 3. Chairman outcome and 10/10 end state

### 3.1 Primary persona

An investor, analyst or portfolio manager trying to understand how one market shock, release, policy change, theme or company event can travel through assets, sectors, themes, companies and their own holdings.

### 3.2 Primary user job

> Something changed — or I want to test an explicit assumption. Show me how the change connects through markets, what has actually happened, what has not happened, which links are observed versus hypothesized, what would invalidate the path, and where my own exposure sits.

### 3.3 Machine/intelligence job

Compose owner-native facts and relationships with incompatible clocks, units, horizons, rights, correction semantics and epistemic status into one deterministic read model without:

- converting similarity into causality;
- converting a historical association into a forecast;
- converting an assumption into an observation;
- multiplying unrelated coefficients into false precision;
- copying canonical truth into a new graph or private store;
- allowing model prose to create privileged facts.

### 3.4 End-state journey

A paid user can:

1. open `/ontology.html` and see a truthful current trace rather than a generic dashboard;
2. search for a node, path or plain-language question;
3. select a real observed root or switch to an explicitly counterfactual scenario;
4. inspect a bounded directed path across macro, rates, FX, commodities, credit, equities, themes and securities;
5. see every node's current value, unit, owner, clocks, freshness, rights and correction state;
6. see every edge's relationship class, method class, horizon, evidence, uncertainty and invalidator;
7. distinguish what is active, waiting, expressed, halted, expired, unavailable or merely historically common;
8. compare the selected path with its inverse, alternatives and strongest invalidators;
9. overlay their canonical Portfolio or Watchlist without exposing or duplicating private state;
10. ask Mastermind to explain the selected evidence and receive exact source/owner citations;
11. save, reopen, share or replay the view where the canonical user and point-in-time owners support those actions;
12. observe honest abstention wherever the system cannot support the requested relationship or numerical response.

Completion is not a visually convincing graph. Completion is an evidence-governed research workflow that remains useful when the answer is uncertain or unavailable.

---

## 4. Value model

| Dimension | Value created |
|---|---|
| User | Compresses multi-desk market reasoning into one inspectable path with explicit uncertainty and personal relevance. |
| Machine | Creates a typed, bounded context packet Brain and other consumers can use without ingesting an untyped graph dump. |
| Research | Separates observation, mechanism, historical frequency, scenario assumption, response estimate, invalidation and outcome. |
| Signal safety | Prevents narrative causality, hidden double counting and unearned response precision from leaking into ranking or trading. |
| Product | Converts several dark intelligence organs into one premium daily workflow. |
| Distribution | Produces explainable, shareable and bilingual research compositions rather than isolated numerical cards. |
| Commercial | Gives the existing `site_full` entitlement a differentiated cross-asset workspace without inventing a new tier. |
| Data moat | Accumulates correction-safe path usage, evidence inspection, saved views, replay behavior and later prospective outcome learning. |

---

## 5. Current capability ledger

| Capability | State at freeze | Canonical evidence / ruling |
|---|---|---|
| Screenshot-level Ontology Explorer | `NOT_BUILT` | No current product implementation PR or production route exists. |
| F04 product architecture | `SPEC_ONLY` | Existing F04 parent and detailed completion plan. |
| TXI chain compiler/state machine | `PROVEN_LIVE` as display/context substrate | `engine/transmission_chains.py`, nightly state/episode artifacts, publication and existing consumers. |
| Existing Cascade Monitor on `/transmission.html` | `PROVEN_LIVE` but narrower than target | Static/linear TXI projection; not the interactive multi-owner workspace. |
| WTI oil-inflation-duration chain | `PROVEN_LIVE` as context state, not trade signal | Current `transmission_chains.v1` state plus historical calibration. |
| Rate/inflation cross-asset map | `PROVEN_LIVE` at display/context tier | Current owner state and measured transmission matrix. |
| Rate scenario sensitivities | `PROVEN_LIVE` at illustrative display tier | Same-window empirical sensitivities, explicitly not forward causal forecasts. |
| GMI theme graph | `PARTIAL` | Large live substrate; D2C/D2D/D2E/W3B/W3C not all accepted. |
| D2C PIT vintage completion | `BUILT_NOT_PROVEN / REQUEST_REPAIR` | Macro PR #6809 exact-head review identified transition and clock defects; existing carrier preserved. |
| K3-D economic propagation | `BUILT_NOT_PROVEN / HOLD` | Macro PR #6514 remains Draft/Hold and may not be treated as accepted owner semantics. |
| Portfolio population/import truth | `PROVEN_LIVE` | Market OS A1A/A1B accepted in production. |
| Watchlist/Portfolio risk core | `PROVEN_LIVE` for book structure | May contextualize user exposure; is not a macro shock engine. |
| Brain gateway | `PROVEN_LIVE` | One gateway serves Dashboard and Terminal. |
| Explorer-specific saved views | `NOT_BUILT` | Future User Plane vertical; no Macro JSON save store allowed. |
| Transmission Gap / second-order explorer | `PARTIAL / NOT PRODUCTIZED` | Must compose accepted owners and preserve research-only authority. |

### 5.1 Material current correction: D2C

D2C is no longer unstarted. It was placed, ACKed, STARTed and returned PR #6809. Sol's 2026-09-04 exact-head review requested repair because:

1. old live THS membership beliefs would survive beside new PIT edge IDs;
2. current concept-map knowledge could still be backdated through an older membership receipt;
3. no persisted two-clock reader proof demonstrated effective-date plus knowledge-cutoff behavior.

D2C remains on its existing operation, branch, PR, runtime and Slack carrier. This F04 freeze neither absorbs nor duplicates it.

---

## 6. Product topology and route ruling

### 6.1 New workspace route

The interactive composition host is frozen as:

`/ontology.html`

This is a distinct user job from the current rates/inflation desk page.

### 6.2 Existing `/transmission.html` remains intact

`/transmission.html` remains the canonical rates/inflation and TXI explanatory owner surface. It already has search/discovery value, current cross-asset reads, scenario cards, Cascade Monitor, bilingual copy and cross-links from other products. F04 must not perform a wholesale rewrite that makes an existing live product the shell for a broader multi-owner workspace.

Later bounded integration may add:

- `Open in Ontology` deep links from a chain/scenario on `/transmission.html`;
- `Source desk` links from `/ontology.html` back to the exact owner section;
- shared selected-node context through the existing Brain/page-context seam.

### 6.3 Access architecture

**Superseded transport:** Amendment 2 and Amendment 3 prohibit the static paid payload named
below. `/ontology.html` is a public-safe shell in the authenticated product navigation family;
current state is returned only by the authenticated read-only Macro API with private/no-store
headers. The bullets below are preserved as historical PR #6820 text; the
`/premiumdata/ontology_explorer.json` instruction must not be implemented.

The existing static-site commercial law remains:

- HTML is a public shell;
- paid analytical payloads are the product;
- `site_full` is the estate-wide paid feature;
- Essential and Pro both carry `site_full`;
- Pro's `chat_opus` remains a Brain-lane entitlement, not an Ontology entitlement.

Expected first-wave split, subject to exact implementation archaeology:

- public presentation: `/ontology.html`, `/ontology.css`, `/ontology.js`;
- paid current response: authenticated read-only Macro API under existing `site_full`, with
  private/no-store headers and no public/static twin;
- public shell contains methodology, controls, honest totals and a non-current instructional composition only;
- current market path data, evidence receipts and user overlay never leak into the anonymous HTML shell;
- no new entitlement feature or pricing tier is created by F04.

---

## 7. Experience architecture

### 7.1 Primary shell

The reference composition's information density is retained, but the semantics are strengthened.

| Region | Frozen behavior |
|---|---|
| Ontology workspace toolbar (below authenticated global nav) | Ontology identity, product-scoped node/path search, `LIVE TRACE` / `SCENARIO`, Ask Mastermind, tour, exact owner as-of and build clocks. It does not duplicate site navigation, ticker search, account, language, or theme controls. |
| View tabs | Today, Rate Path, Credit Channel, Dollar Regime, Equity Duration, Saved. |
| Filter rail | Relationship class, evidence/method class, active/invalidated/unavailable, My Portfolio, My Watchlist. |
| Trace narrative | What the selected path says, what changed, what has not transmitted, weakest supported link, most exposed covered holding, key invalidator. |
| Active paths | State, current hop, lag window, evidence method and user overlap. No generic confidence score. |
| Graph canvas | Directed, typed, bounded path graph grouped by native domain. |
| Inspector | Complete node/edge/path truth and provenance. |
| Scenario rail | Explicit assumption definition, magnitude, unit, horizon, path shape, origin class, compare-to-live, reset, save/share/analyze. |
| Ask Mastermind | Grounded explanation of the selected view; no edge or coefficient authorship. |

### 7.2 Deterministic layout, not decorative physics

Transmission paths are directed and ordered. The first product uses a deterministic layered layout:

- x-axis = path depth/hop order;
- y-axis = native domain and stable node order;
- cycles are not allowed to turn the selected path into an infinite graph;
- cross-links remain typed and visually secondary;
- node dragging changes view state only and never relationship truth;
- path expansion has explicit depth and node-count limits;
- exact layout is reproducible from the same view-model version.

A generic force-directed layout is rejected for the first release because it obscures order, changes between runs and makes visual proximity look semantic.

### 7.3 Desktop, tablet and mobile

Desktop is the command-center canvas. Mobile is not a scaled-down desktop graph.

- desktop: canvas + left path rail + right inspector + bottom scenario rail;
- tablet: canvas with inspector sheet and collapsible path rail;
- mobile: selected path as a vertical hop sequence, node/edge cards and an optional pan/zoom canvas sheet;
- keyboard users can traverse nodes/edges in path order and open the inspector;
- reduced-motion users receive no animated propagation dependency;
- dark and light themes are materially designed, not color-inverted screenshots;
- EN/ZH parity covers labels, states, nulls, invalidators, method descriptions and errors.

---

## 8. Epistemic architecture

### 8.1 Live Trace and Scenario are separate modes

#### `LIVE_TRACE`

Reports what current canonical owners actually observe. It may show owner-native measurements, chain states, historical context and current invalidators.

#### `SCENARIO`

Evaluates an explicit user assumption. The assumption never overwrites an observed node, never appends to an owner ledger, never opens a TXI episode and never becomes evidence that the market moved.

A scenario result is a pure derived evaluation tied to:

- assumption identity;
- owner generation/model versions;
- effective as-of;
- knowledge cutoff;
- method class;
- unit and horizon compatibility;
- scenario path definition.

`COMPARE TO LIVE` displays the assumption beside current state. It does not replace current state.

### 8.2 Relationship classes remain distinct

| Class | Meaning | May prove economic propagation? |
|---|---|---:|
| Graph 1 | Owner-backed economic/exposure/contractual mechanism | Only within native owner claim strength |
| Graph 2 | Similarity, thematic comparability or shared characteristics | No |
| Graph 3 | Residual market context/co-movement | No |
| TXI path | Versioned staged mechanism with observable tests and falsifiers | Context/hypothesis unless a native owner grants stronger status |

Graph 2 or Graph 3 may appear as contextual alternatives, but neither may fill an absent Graph 1 edge. Theme co-membership cannot become an economic relationship.

### 8.3 Method classes

Every numerical or relational assertion carries exactly one primary method class:

| Method class | Meaning | Permissible output |
|---|---|---|
| `OBSERVED_STATE` | Current native owner value/state | Native value and receipt |
| `DETERMINISTIC_IDENTITY` | Exact identity, membership or declared reference | Relationship only |
| `OWNER_MODEL` | Native owner's accepted model output | Output within that owner's contract |
| `EMPIRICAL_SAME_WINDOW_BETA` | Same-window historical co-movement sensitivity | Illustrative estimate with window/model caveat |
| `HISTORICAL_TRANSITION_RATE` | Frequency that a declared next condition followed a prior condition | Probability with N, span and regime cell; not causal confidence |
| `HISTORICAL_OUTCOME_DISTRIBUTION` | Outcomes following frozen historical episodes | Distribution/interval and concentration, not trade recommendation |
| `THRESHOLD_STATE_ONLY` | Assumption satisfies or fails a declared deterministic condition | State transition or partial/unknown only |
| `HYPOTHESIS_MECHANISM` | Versioned mechanism, condition and invalidator without numerical response authority | Direction/mechanism prose, no fabricated estimate |
| `UNAVAILABLE` | Missing identity, data, rights, method, unit, horizon or owner acceptance | Typed abstention |

A future nonlinear full-revaluation owner may add a new method class only through a separate architecture/owner ruling. F04 itself may not invent it.

### 8.4 Why `Conf >= 50%` is rejected

The reference control collapses different epistemic objects into one number. Mastermind instead filters by:

- observed / owner model / historical / hypothesis / unavailable;
- economic / similarity / residual context;
- active / waiting / expressed / halted / expired / unavailable;
- exact horizon and freshness.

A historical transition frequency of 69%, a model coverage score, an identity-resolution state and an LLM narrative confidence are not the same quantity and must never share one threshold control.

---

## 9. Scenario contract

### 9.1 Assumption shape

A scenario assumption contains:

```json
{
  "assumption_id": "content hash",
  "node_ref": "owner-native node identity",
  "shock_definition": "terminal_change_over_horizon",
  "magnitude": 20.0,
  "unit": "percent",
  "horizon": "3m",
  "path_shape": "terminal_only",
  "origin_class": "unspecified",
  "effective_as_of": "date/time",
  "knowledge_cutoff": "date/time"
}
```

Closed `shock_definition` examples:

- `terminal_change_over_horizon` for price/index changes;
- `absolute_change_bp_over_horizon` for yields/spreads;
- `average_level_shift` only where an owner explicitly supports it;
- `event_impulse` only where an event owner defines the impulse.

A magnitude without a path shape does not imply an intermediate moving-average slope, timing path, volatility path or source cause.

### 9.2 Propagation law

For each requested hop:

1. resolve exact input node and owner;
2. validate scenario definition, unit, horizon, range and origin requirements;
3. read the owner-native method and exact version;
4. evaluate that hop independently;
5. return an estimate, distribution, threshold state, mechanism-only result or typed abstention;
6. preserve uncertainty and missingness;
7. never recursively multiply arbitrary edge coefficients.

A combined path estimate is allowed only when one accepted owner explicitly models the joint path and its dependence structure. Otherwise the product shows hop-level results and states why a path-level point estimate is unavailable.

### 9.3 Current-correlation stress is not causality

An empirical factor/correlation propagation may be displayed only as an explicitly named stress method with:

- estimation window;
- current model version;
- covariance/factor source;
- shock compatibility diagnostics;
- supported instrument coverage;
- nonlinear/proxy limitations;
- no causal language.

### 9.4 Scenario side effects are forbidden

Running, comparing, saving or sharing a scenario may not:

- append TXI chain episodes;
- mutate current market state;
- modify Portfolio or Watchlist truth;
- originate a Prophet candidate;
- trigger an alert or trade;
- alter a calibration ledger;
- become a forward observation.

Any later transition from a saved scenario into a monitored thesis/condition is a separate explicit user action through the existing thesis/monitor owners.

---

## 10. WTI golden reference composition

WTI is the first vertical because the estate already contains the exact chain family, current observable receipts, historical hop calibration, inverse path and per-name duration substrate.

### 10.1 Live current truth at the freeze base

The current `oil_inflation_duration_derate` chain is `dormant`, not active:

- WTI 60-day return: approximately +13.9%, below the declared +25% threshold;
- WTI MA50 slope condition: true;
- 10Y breakeven 22-day change: +4bp, below the +15bp confirmation threshold;
- real 10Y 63-day rise and rising-direction condition: true at +31bp;
- QQQ relative to SPY over 63 days: negative, satisfying the terminal de-rate observation.

The correct narrative is therefore not “oil caused the current duration de-rate.” It is:

> Downstream rate and duration conditions are present, but the declared oil root and breakeven hop are not. The oil path remains dormant; another mechanism may explain the downstream state.

This is the exact kind of causal restraint the product must make visible.

### 10.2 Historical context already available

The current calibration artifact reports historical next-condition frequencies for the three declared WTI hops, with sample size and regime cells. These are `HISTORICAL_TRANSITION_RATE` outputs, not a generic confidence score or an expected return.

The same calibration carries a terminal cohort event study explicitly labeled descriptive and not a forecast. If that outcome distribution conflicts with the intuitive mechanism, the conflict remains visible. F04 may not suppress it to make the path look stronger.

### 10.3 Scenario examples

#### Scenario A — `WTI +20% over 3m`, terminal-only

- assumption is explicit;
- the declared +25%/60d oil-shock threshold is not met;
- terminal-only shape does not prove the MA50 slope path;
- chain state remains `SCENARIO_NOT_ARMED` or `PARTIAL_UNKNOWN`;
- no CPI, breakeven, yield or equity point estimate is printed.

#### Scenario B — `WTI +30% over 60d`, linear rising path, supply shock

- root threshold may be satisfied if every declared root condition is resolved;
- the next hop remains unobserved in Scenario mode;
- UI may show the historical oil->breakeven transition rate with N/span/regime cell;
- it may show mechanism and invalidator;
- it may not declare that breakevens will rise by a specific amount unless a separate accepted response owner provides that distribution.

#### Scenario C — `real 10Y +50bp over 63d`

- existing Rate & Inflation Transmission scenario beta may provide cross-asset same-window illustrative sensitivities;
- every value is labeled `EMPIRICAL_SAME_WINDOW_BETA`, not causal forecast;
- unsupported instruments or horizons abstain.

### 10.4 Later WTI response kernel

Magnitude-sensitive WTI->inflation/rates response is a later bounded owner/method child, not a frontend calculation. It must:

- identify supply, demand and residual oil-shock origins;
- define the exact shock variable and path;
- preserve horizon and release/observation clocks;
- model state dependence, asymmetry and era drift;
- report median/distribution, N, span and concentration;
- use walk-forward or otherwise leakage-safe evaluation appropriate to the claim;
- refuse a point estimate when identification or coverage is inadequate;
- remain research/display authority until prospective evidence supports anything stronger.

---

## 11. Canonical owner-adoption matrix

| Product need | Canonical owner/read path | F04 must not build |
|---|---|---|
| Staged chain state, hops, lag windows, falsifiers | TXI `transmission_chains.v1` | Second cascade tracker or episode ledger |
| Historical hop confirmation rates | `transmission_calibration.v1` | A second chain miner/grader |
| Current rate/inflation state | Rate & Inflation Transmission | New macro-state model |
| Rate scenario sensitivities | Existing scenario calibration | Generic universal coefficient engine |
| Per-name rate/duration exposure | `stock_macro_sensitivity` | New duration classifier |
| Theme/local topology and PIT membership | GMI accepted readers | New theme graph or crosswalk store |
| Economic propagation hypotheses | K3-D after acceptance | Fourth relationship graph or causal DAG |
| Incorporation/dislocation | Existing specialist owners | F04 incorporation database |
| Portfolio positions and weights | Market OS Portfolio | F04 holdings store |
| Watchlist memberships | Market OS Watchlist | F04 watchlist store |
| Book structure and `what-if-add` context | RiskCore | Treat RiskCore as macro shock response |
| Evidence, rights, clocks, corrections | Native owners + Evidence Foundation references | Universal evidence warehouse |
| Ask AI | Existing Brain gateway | `/ontology-ai` or second response log |
| Private saves | Terminal/Supabase User Plane | Macro JSONL/private graph store |
| Product telemetry | Existing `window.mmTrack` -> `/api/collect` -> `analytics_events` | New analytics endpoint/table |
| Runtime lifecycle/placement | Executive OS and accepted Capacity/RuntimeBinding owners | F04 job queue or router |

---

## 12. `ontology_explorer.v1` read model

**Amended contract:** Amendment 3 §§4–6 extend this illustrative shape. Every response binds
request-time deployed owner bytes through `source_manifest_hash`, distinguishes source staleness
from `DEPLOYED_CHECKOUT_LAG`, uses K1 `EvidenceRef` IDs and a validated bounded
`evidence_index[]`, and keeps hop episode N separate from overlapping terminal-cohort bar N.
Missing effective N, overlap treatment, interval, span, concentration, or current-regime mapping
is typed unavailable rather than inferred.

The explorer consumes canonical owners and emits one **derived projection**, never a truth store.

```text
ontology_explorer.v1
  meta
    generated_at
    effective_as_of
    knowledge_cutoff
    mode
    view_version
    owner_generations[]
  root
    node_ref
    owner_ref
    observed_state?
  assumption?
    assumption_id
    node_ref
    shock_definition
    magnitude
    unit
    horizon
    path_shape
    origin_class
  nodes[]
    node_ref
    owner_ref
    native_ref
    kind
    label
    observed_value?
    unit?
    effective_at?
    first_known_at?
    generated_at?
    freshness
    correction_state
    rights_state
    evidence_refs[]
  edges[]
    edge_ref
    from
    to
    graph_class
    method_class
    owner_ref
    native_construct
    input_unit?
    output_unit?
    horizon?
    estimate?
    interval?
    n?
    span?
    regime?
    state
    evidence_refs[]
    invalidator_refs[]
  paths[]
    path_id
    edge_refs[]
    state
    current_hop?
    weakest_leg?
    coverage
    combined_estimate?
    no_combined_estimate_reason?
  invalidations[]
  alternatives[]
  user_overlay?
    portfolio_generation_ref?
    watchlist_generation_ref?
    covered_subjects[]
    uncovered_subjects[]
    path_exposure[]
  degradations[]
  authority
    can_rank: false
    can_gate: false
    can_size: false
    can_trade: false
```

### 12.1 Clock law

`effective_as_of` and `knowledge_cutoff` are separate.

- current Live Trace normally uses the latest owner-effective state knowable at build/read time;
- historical replay requires owner facts effective by D **and** beliefs/observations known by the selected knowledge cutoff;
- a row reconstructed later does not become historically knowable merely because its `valid_from` is old;
- an owner without a lawful PIT reader returns `PIT_UNAVAILABLE` rather than current data backdated into the replay.

### 12.2 Correction law

F04 never edits owner truth. The derived projection:

- references owner generation/content identity;
- exposes amended, superseded, retracted and stale states;
- does not mutate a saved scenario when owner truth is corrected;
- on reopen, compares saved owner references to current superseding references and presents the delta;
- never silently replaces a historical replay's knowledge cutoff with current corrected truth.

### 12.3 Null law

Missing is not zero. Unknown is not neutral. Unsupported is not safe.

Every omitted value requires a typed reason. A path may remain useful even when one leg abstains, but the UI cannot imply a complete numeric propagation.

---

## 13. Inspector and narrative contracts

### 13.1 Node inspector

A node inspector shows:

- exact owner/native identity;
- current value and unit;
- effective/observed/first-known/generated clocks;
- freshness and source availability;
- rights/display tier;
- correction/supersession state;
- evidence references;
- scenario compatibility and supported definitions.

### 13.2 Edge inspector

An edge inspector shows:

- relationship and method class;
- source and target native identities;
- owner and native construct;
- mechanism, condition and lag window;
- units/horizon;
- historical N/span/regime cells or model version;
- estimate/distribution only where method permits;
- invalidators and expiry;
- why a stronger claim is unavailable;
- authority ceiling.

### 13.3 Trace Narrative

Narrative generation is deterministic first:

- state summary;
- observed/assumed root;
- most advanced confirmed hop;
- first unresolved or failed hop;
- strongest current invalidator;
- user exposure coverage;
- one sentence of owner-approved mechanism copy.

Brain may expand or answer questions over that packet. Brain prose is never stored as an edge or owner fact.

---

## 14. Ask Mastermind integration

The existing Brain gateway remains the only user-facing AI answer authority.

The explorer sends a bounded page-context packet containing:

- selected root/path/node/edge IDs;
- current mode and assumption;
- typed method/relationship classes;
- rendered evidence references;
- invalidators, abstentions and user-visible coverage states;
- no hidden graph dump and no private holdings beyond the minimum already-authorized user context.

Allowed jobs:

- explain why the path is dormant/active/invalidated;
- compare two owner-backed paths;
- summarize what would change the read;
- explain an unavailable numerical response;
- identify which selected holding is exposed and through which declared channel;
- cite the exact owner evidence.

Forbidden jobs:

- create a canonical node, edge, mapping or coefficient;
- infer identity from a fuzzy label and persist it;
- overwrite an owner correction state;
- convert narrative confidence into product confidence;
- rank, gate, size, originate or trade;
- silently save or monitor a scenario.

---

## 15. Portfolio and Watchlist integration

The explorer consumes canonical Market OS identities and weights. It does not own them.

For each covered holding/watch item it may show:

- exact path membership/exposure source;
- owner-native sensitivity or blast-channel membership;
- portfolio weight where the authenticated user has supplied it;
- book-context measures from RiskCore where relevant;
- coverage and missingness.

It may not show projected dollar P&L unless an accepted owner provides a compatible instrument response model for that scenario. RiskCore's `whatIf` answers how book structure changes when a position is added; it is not a macro shock propagation model.

Partial coverage is visible as:

- covered market value/count;
- uncovered market value/count;
- unresolved identities;
- owner/method reason for each abstention.

Telemetry never transmits raw holdings, weights, free-text research or full scenario payloads.

---

## 16. Save, share and replay

This is a later cross-repository slice.

### 16.1 Save

Private saves belong in the Terminal/Supabase User Plane and contain references, not copied market truth:

- user-owned saved-view ID;
- view/model version;
- selected root/path/filters/layout;
- explicit scenario assumption;
- effective as-of and knowledge cutoff;
- owner generation/content references;
- created/updated/archive revision history;
- optimistic concurrency and idempotency through existing User Plane patterns.

### 16.2 Reopen

On reopen:

- reproduce the saved view against its frozen references where available;
- show owner corrections/supersessions since save;
- allow `open as saved` versus `refresh to current` as explicit choices;
- do not mutate the saved revision merely because current truth changed.

### 16.3 Share

A share is an immutable, read-only, entitlement- and rights-safe projection. It must remove private portfolio/watchlist context unless the user explicitly creates an allowed sanitized share. It must never expose gated owner payloads to an unentitled recipient.

### 16.4 Replay

Replay uses separate effective and knowledge clocks. A source that cannot support historical knowledge state is visibly unavailable. Current membership or correction state may never be backfilled into an older replay.

---

## 17. Telemetry and learning

Use the existing first-party path:

`window.mmTrack -> POST /api/collect -> analytics_events -> config/growth_events.yml`

A future bounded telemetry child may add one closed wire type, for example `ontology`, with a closed event enum:

- `explorer_open`
- `root_select`
- `mode_switch`
- `path_select`
- `edge_inspect`
- `invalidation_open`
- `scenario_run`
- `portfolio_overlay`
- `evidence_open`
- `ask_ai`
- `save`
- `share`
- `replay`

Privacy contract:

- no free-text query or AI prompt;
- no raw holdings, weights or watchlist membership;
- no source document bodies;
- no unbounded `meta` vocabulary;
- opaque scenario/view IDs and closed node/path family IDs only;
- dedup semantics defined per event, not inferred later.

Success is not maximum interaction. The learning questions are:

- can a user reach one meaningful path quickly;
- do users inspect evidence/invalidators before acting;
- do scenarios lead to saved/reopened research;
- are abstentions understood rather than abandoned;
- does portfolio overlay materially improve repeated use;
- do corrections and stale-state warnings change behavior;
- do users ask more grounded questions with selected context;
- is there any incident of invented edge/response precision.

---

## 18. Failure-state experience

The following are first-class product states, not generic empty screens:

| State | Product behavior |
|---|---|
| `NO_ECONOMIC_PATH` | Show available similarity/context paths separately; do not promote them. |
| `UNRESOLVED_IDENTITY` | Preserve source-native label and show resolution owner/action. |
| `SOURCE_STALE` | Keep last-good visibly stale where owner allows; never present as current. |
| `SOURCE_UNAVAILABLE` | Show affected nodes/edges and preserve unrelated path legs. |
| `RIGHTS_RESTRICTED` | Explain that a source/field cannot be displayed; never proxy silently. |
| `UNIT_MISMATCH` | Refuse arithmetic and show each native value separately. |
| `HORIZON_MISMATCH` | Refuse combined estimate; permit separate horizon views. |
| `ASSUMPTION_OUTSIDE_SUPPORTED_RANGE` | Show owner-supported range and abstain. |
| `SHOCK_ORIGIN_AMBIGUOUS` | Show supply/demand/residual alternatives; no pooled causal claim. |
| `MODEL_WINDOW_INCOMPATIBLE` | Refuse correlated stress and name the window/model conflict. |
| `CYCLE_OR_DOUBLE_COUNT_RISK` | Stop path aggregation and identify repeated owner evidence. |
| `HISTORICAL_ONLY` | Show history with era/N; no current inference. |
| `SCENARIO_METHOD_UNAVAILABLE` | Keep mechanism/threshold context without a number. |
| `PIT_UNAVAILABLE` | Refuse replay rather than use current truth. |
| `OWNER_OUTPUT_SUPERSEDED` | Link predecessor/successor and show correction delta. |
| `PARTIAL_PORTFOLIO_COVERAGE` | Show covered/uncovered weights/counts and no whole-book conclusion. |
| `AI_UNAVAILABLE` | Explorer remains fully usable; only generative explanation is absent. |
| `SAVED_VIEW_CONFLICT` | Preserve both revisions and require explicit conflict resolution. |

---

## 19. No-rebuild and authority boundaries

F04 may not create:

1. a fourth graph or generic `RELATED` edge;
2. a universal causal DAG or propagation scalar;
3. a second TXI chain/episode/calibration store;
4. a second GMI theme/local-theme/PIT/crosswalk store;
5. a second identity or evidence plane;
6. a second rate/inflation, FX, credit, commodity, portfolio or risk model;
7. a generic scenario coefficient registry disconnected from owner models;
8. a second Portfolio/Watchlist state or private Macro save store;
9. a second Brain endpoint, transcript or response log;
10. a second analytics endpoint/table or arbitrary telemetry blob;
11. a Market Ontology-specific queue/router/watcher/lifecycle;
12. a fused opportunity/confidence/impact score;
13. rank, gate, size, trade, origination, `ENTRY_OPEN` or Prophet authority;
14. current-membership or current-correction backdating into a historical replay;
15. competitor code, text, data, assets, hidden interfaces or brand identity.

The explorer may satisfy competitor-inspired user jobs with original code, original design and lawful first-party/licensed data. Functional parity never requires pixel copying.

---

## 20. External method and workflow anchors

The architecture uses public precedent only as a method/job benchmark:

- Market Ontology's public materials validate the user jobs of tracing evidence through assets, second-order effects, invalidation, opportunity monitoring and portfolio exposure: <https://marketontology.com/decision-intelligence>.
- Palantir Object Explorer/Vertex validates global object search, linked-object traversal, selection inspectors and saved exploration as useful graph-workflow patterns: <https://www.palantir.com/docs/foundry/object-explorer/getting-started> and <https://www.palantir.com/docs/foundry/object-explorer/search-around>.
- AlphaSense validates the workflow of asking a scoped question and drilling into exact cited evidence rather than accepting an uncited generated answer: <https://help.alpha-sense.com/hc/en-us/articles/41666587181203-Interacting-with-Generative-Search>.
- BlackRock, MSCI and FactSet scenario materials distinguish historical replay, hypothetical shocks, factor/correlation propagation and full revaluation, and warn that model/window/instrument compatibility matters. F04 adopts the distinction, not any proprietary model: <https://www.blackrock.com/aladdin/products/aladdin-risk>, <https://www.msci.com/research-and-insights/blog-post/scenario-analysis-what-could-go-wrong>, <https://www.factset.com/solutions/portfolio-analytics>.
- NY Fed, Federal Reserve, ECB and IMF work demonstrates why oil-price movements must be decomposed by supply/demand/other origin and why inflation pass-through is state-, horizon- and economy-dependent. F04 therefore refuses one generic WTI causal coefficient: <https://www.newyorkfed.org/research/policy/oil_price_dynamics_report>, <https://www.federalreserve.gov/econres/notes/feds-notes/oil-price-shocks-and-inflation-20220602.html>, <https://www.ecb.europa.eu/press/economic-bulletin/articles/2022/html/ecb.ebart202204_01~f7a2f67429.en.html>, <https://www.imf.org/en/Publications/WP/Issues/2024/06/28/Oil-Price-Pass-Through-into-Inflation-550711>.

These sources do not confer data rights, predictive validity or authority. They sharpen the product and method questions Mastermind must answer independently.

---

## 21. Revised execution architecture

The parent remains one sustained F04 program. Each child creates one independently useful capability.

### X1 — WTI Live Trace

One paid user opens `/ontology.html`, selects WTI, sees the real current chain, node receipts, historical hop frequencies, invalidators, inverse path, truthful dormant/partial states and full inspector. The public shell and paid payload boundary are proven. One current real path plus one real abstention/contradiction is browser-proven.

### X2 — Scenario contract + real-rate numeric vertical

Add explicit Live/Scenario mode and immutable assumption identity. Prove threshold-only WTI scenarios and one existing real-rate `EMPIRICAL_SAME_WINDOW_BETA` scenario with compatible units/horizon. Scenario runs are side-effect-free and cannot create TXI episodes.

### X3 — WTI response-kernel research/implementation

After source and method freeze, build magnitude-sensitive conditional distributions by shock origin and horizon, or reject the numerical job if identification/coverage is inadequate. No frontend approximation.

### X4 — Portfolio/Watchlist overlay

Join canonical user identities to path/blast/sensitivity outputs with visible covered/uncovered state. No projected P&L without owner support.

### X5 — Cross-channel explorer

Add Rate Path, Credit Channel, Dollar Regime and Equity Duration views by composing native owners. No universal owner rewrite.

### X6 — Inspector + Ask Mastermind

Complete evidence drilldown and typed Brain context. Real selected-path questions return exact owner citations and preserve abstentions.

### X7 — Save/share/replay

Build private immutable view revisions in the canonical User Plane, rights-safe shares and two-clock replay where owners support it.

### X8 — Transmission Gap + second order

After exact dependencies are accepted, compose GMI, K3-D, incorporation/dislocation and specialist outputs into “what has not transmitted?” and second-order research workflows. Calibrated rank/impact/priced/trade semantics remain separately held.

### X9 — Integrated acceptance and learning

Prove production data, user and machine consumers, EN/ZH, dark/light, desktop/tablet/mobile, correction/degraded states, telemetry and repeated-use learning. Close only after a real user can complete the primary journey.

### 21.1 Parallelism ruling

- X1 and most of X2 may proceed while D2C repairs #6809 and K3-D remains held;
- X1 may read accepted TXI/rates owners only and must not import unaccepted GMI/K3-D semantics;
- theme/PIT replay features remain dependency-gated on accepted GMI readers;
- economic-propagation features remain gated on K3-D acceptance;
- opportunity authority remains gated on K2/K3/K5/Eval-OS promotion;
- no held dependency is bypassed by a visual placeholder presented as real.

---

## 22. Routing architecture

### Parent principal

```text
PREFERRED_AVENUE: Fable
RECEIVER_BINDING_MODE: CAPACITY_SELECTABLE
PLACEMENT_STATE: WAITING_CAPACITY / needs_placement
```

**WHY FABLE:** the parent requires sustained adjudication across many native owners, repositories, authority boundaries and dependency states. Without a principal, bounded workers are likely to create incompatible scenario semantics, duplicate owner adapters or a visually complete graph that narrows the product thesis.

Fable is the integrator and program principal, not the default coder. After this freeze, X1 and later bounded engineering children should route to the least-scarce capable Terra/CTO Sol/Opus avenue, with builder/reviewer separation where practical.

No current Fable provider session, RuntimeBinding, ACK, START or watcher is asserted by this document. A preferred avenue is not placement.

---

## 23. X1 frozen implementation boundary

**Amended boundary:** Amendment 3 controls this section. `/ontology.html` must use
`templates/_site_nav.html.j2` and its shared inventory/behavior family; current state comes from
the Amendment 2 authenticated API, never a gated static payload. The D2C PR #6809 and K3-D PR
#6514 literal protected paths are enumerated in Amendment 3 §7 and require a fresh expansion and
collision census before X1 `START`.

Expected current-owner-safe surface, subject to the child worker's exact current-main census:

**Create/modify:**

- `engine/ontology_explorer/**` or one current-owner-approved pure F04 composer namespace;
- `scripts/build_ontology_explorer.py`;
- `templates/ontology.html.j2`;
- `templates/ontology.js` / `templates/ontology.css` or current site asset convention;
- generated `site/ontology.html` and public-safe presentation assets only; no current payload;
- `config/site_access.yml` and matching Caddy boundary only as required for public presentation assets and paid payload;
- current nav/SEO/sitemap surfaces only after exact product-owner archaeology;
- focused tests and real browser evidence.

**Read only:**

- `data/transmission/chain_state.json`;
- `data/transmission/chain_calibration.json`;
- `data/transmission/latest.json`;
- `knowledge/transmission/**` through existing loader/owner contracts;
- existing Brain/nav/theme/tier-preview patterns.

**Protected from X1 edits:**

- `engine/transmission_chains.py` and chain episode/calibration owners;
- `engine/theme_graph/**`, `data/theme_graph/**`, D2C PR #6809;
- K3-D PR #6514 paths;
- Portfolio/Watchlist persistence and RiskCore math;
- Brain gateway behavior;
- Terminal/Supabase private persistence;
- rank/gate/size/trade owners.

If X1 needs an owner contract change, it returns to Sol/Fable instead of widening.

---

## 24. Acceptance and proof matrix

### 24.1 X1 minimum acceptance

Amendment 3 §§2–6 are additional minimum acceptance law: authenticated shared nav plus a
below-nav workspace toolbar; request-time source-manifest and deployed-checkout/pull-lag proof;
K1 evidence contract validation; episode-N versus overlapping cohort-bar-N disclosure; typed
effective-N/overlap/interval/span/concentration and regime-unavailable states; and visible
per-owner as-of/build clocks.

- exact deployed TXI/rates owner bytes -> request-time F04 derived read model -> authenticated API -> `/ontology.html` consumer;
- WTI current state reproduced exactly from owner receipts;
- downstream true nodes do not falsely activate an upstream-dormant path;
- historical transition rates display N/span/regime and are not labeled confidence;
- terminal outcome study remains separate from transition rates and mechanism prose;
- invalidators and inverse path resolve from canonical definitions;
- unavailable owner/method/rights/unit/horizon states render usefully;
- no duplicate graph/store/identity/correction plane;
- paid payload cannot be fetched anonymously or by Free;
- Essential and Pro can read through existing `site_full` semantics;
- EN/ZH, dark/light, desktop/mobile, keyboard and reduced-motion states work;
- exact deployed build identity and real browser proof;
- one machine consumer or bounded Brain context adapter reads the same derived contract;
- production proof is not replaced by screenshots of fixtures.

### 24.2 Full-program proof states

The final evidence matrix includes:

- Live current positive path;
- Live dormant/partial path;
- invalidated/halted path;
- no Graph-1 path;
- stale source;
- rights restriction;
- unresolved identity;
- unit mismatch;
- horizon mismatch;
- out-of-range assumption;
- ambiguous shock origin;
- historical-only result;
- no scenario method;
- current versus saved correction delta;
- PIT replay available and unavailable;
- full and partial Portfolio coverage;
- Brain available and unavailable;
- anonymous, Free, Essential and Pro access behavior;
- EN/ZH;
- dark/light;
- desktop/tablet/mobile;
- keyboard/reduced-motion;
- telemetry payload and privacy assertions.

### 24.3 Completion standard

**Truth:** fresh, source- and correction-safe owner facts.  
**Intelligence:** typed paths, historical context, scenario methods, alternatives and invalidators.  
**Product:** coherent premium workflow across real and degraded states.  
**Learning:** instrumentation that shows whether the workflow improves research, discovery, retention and decisions.

---

## 25. Architecture freeze / no silent redesign

The following decisions are frozen for implementation unless Sol issues a later explicit amendment:

1. existing F04 parent operation is preserved;
2. new product host is `/ontology.html`;
3. existing `/transmission.html` remains the TXI/rates owner surface;
4. Live Trace and Scenario are distinct modes;
5. assumptions never mutate owner state;
6. graph and method classes are explicit;
7. no generic confidence filter or path score;
8. no arbitrary coefficient multiplication;
9. WTI is X1's golden path and honest dormant/contradictory state is acceptable product truth;
10. paid payload uses existing `site_full`; no new entitlement tier;
11. Brain, Portfolio, Watchlist, telemetry and User Plane owners are reused;
12. Fable is parent principal capacity; frozen children route economically;
13. X1 ships before later infrastructure outruns it;
14. GMI/K3-D/opportunity features remain gated on their exact owners;
15. final completion requires production and learning proof.

---

## 26. Exact next action

**Current gate:** F00 consumed the exact eight-record PR #6820 return at
`C0BSBM78V1N/1788510607.305039 @ 1788522863.288509` and required one records-only repair.
X1 remains held until that repair is merged and a fresh planned-write collision census passes.

1. Keep D2C on its existing repair carrier and review the returned repaired exact head; do not create a successor.
2. Complete the single Amendment 3 records repair against the two existing review packets; do not commission a third full review.
3. After the repair is merged and the fresh planned-write census passes, F00/Sol may place the **existing F04 parent responsibility** with one concrete eligible principal or bounded X1 child; no retrieved record self-assigns one.
4. The Fable principal performs a fresh current-owner/path collision census and commissions X1 as a new bounded child to the least-scarce capable implementation avenue.
5. X1 stops at WTI Live Trace production proof and returns; it does not absorb Scenario, Portfolio, GMI, K3-D or save/replay.

This document authorizes no product build, worker START, merge, deployment or authority promotion by itself.
