# Independent Preservation Review Packet — Market Ontology

**Review target:** the preservation artifacts created by
`marketontology-final-preservation-census-20260828-terra-001` only.
**Required reviewer posture:** do not try to prove that earlier work was good. Try
to find a meaningful product state that it failed to preserve.

## Non-negotiable questions

1. Does the packet correctly refuse `SAFE_TO_LOSE_MARKETONTOLOGY_ACCESS` while the
   hash-identified 1,556-row archive remains absent?
2. Did the new indexed-route method identify a material state absent from a
   navigation-only census? If not, name the evidence that makes each claimed finding
   merely an alias of an already preserved contract.
3. Do the records preserve functional depth—fields, state transitions, persistence,
   and entitlement boundaries—or merely product names?
4. Is any material state missing from the samples, especially an orphan public route,
   a dynamic ticker/event family, an authenticated/navigation state that was lawfully
   unavailable, or a settings/drawer/tab interaction that must be recorded as
   inaccessible rather than assumed absent?
5. Does any candidate accidentally grant implementation, data, or commercial authority?

## Required independent method

Use one discovery method materially different from both:

- direct public product pages; and
- sitemap-indexed route-family sampling.

Examples include a manually visible authenticated navigation only if an already
authorized session exists, a public route-link graph from a distinct orphan page, or
a structured comparison of current route schemas against the 88/42 ledgers. Do not
create an account, submit credentials, call private APIs, use hidden interfaces,
evade robots/authentication, purchase anything, or take any financial action.

## Required result packet

Return exactly:

```text
STATUS: PASS | FAIL | BLOCKED
RESULT: <zero-new-finding challenge result>
EVIDENCE: <routes/artifacts and concise semantic facts>
GAPS: <missing preservation state or none>
DEVIATIONS: <none or explicit scope/access deviation>
SAFE_TO_LOSE_ACCESS: YES | NO with reason
```

`PASS` can never mean the historical archive is complete unless the original V5 CSV
and JSON artifacts have been imported and their hashes match. A zero-new-finding
result is still not an archive-import result.
