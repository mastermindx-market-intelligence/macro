# W9 / W9B-REC — F12 v1 Public-API: DEC DRAFT note vs closed #6925 (NOT APPLIED)

**Status:** DRAFT, NOT APPLIED · records-only docket.
**Date:** 2026-09-13 · **Lane:** W9 W9B-REC · F12 commercial/account scope
**Packet:** MO-B-REC · wave W9 (records-only)
**Source ledger:** `MARKET_ONTOLOGY_F00C_GRANULAR_CLOSURE_LEDGER_2026-09-02.csv` (read-only input)
**Decision-record companion (DRAFT, not applied):** `agentos/decisions/DEC-F12-V1-PUBLIC-API-VS-CLOSED-6925-2026-09-13.md`
**Closed PR:** #6925 (kept closed — this packet does NOT reopen it)
**Observed at:** 2026-09-13 · macro `main`

## 0. Where this lives

<!-- backlinks -->
- W9 planner ruling (2026-09-13 16:02Z): META-CEO B, packet `w9b_rec_f12_v1`.
- Tenancy foundation receipt of record: `research/market_intelligence_productization/receipts/supabase_receipt_0014_tenancy_foundation_2026-09-08.json` — applied `2026-09-08T22:24:14Z` (HTTP 201, `apply_result=ok`). Applied as already true; this packet does not re-apply, re-stamp, or rebroadcast it.
- Closed PR of record: #6925 (status: CLOSED, NOT MERGED). This docket does NOT reopen it. The DEC is drafted alongside this note for a future, separately commissioned, build packet; nothing in this packet triggers or unblocks that build.
- Companion dossiers (read-only inputs to this docket, none edited):
  - `research/market_intelligence_productization/MARKET_ONTOLOGY_F12_COMMERCIAL_ACCOUNT_SCOPE_2026-09-06.md` (F12 packet B-F12-6)
  - `agentos/handoffs/MARKET-ONTOLOGY-META-CEO-B-2026-09-11T12.md`

## 1. Plain-words answer in three sentences

The F12 v1 Public-API surface that #6925 proposed is a separate concern from the team-tenancy foundation that `0014_tenancy_foundation` already installed — the latter is a row-level Supabase schema artefact and is already applied as already true, while the former is an HTTP-facing surface that has not been designed, scoped, or commissioned. #6925 stayed closed for that reason; the correct next act is a DEC draft (this packet's companion) and a separately submitted, separately commissioned, F12-v1 build packet — not a silent reopen, not an unstamped reapply of `0014`, and not a regen of the F00C closure ledger. This docket records that decision state without applying it.

## 2. What is "applied as already true" here (verbatim)

`0014_tenancy_foundation` is applied. Receipt of record is the JSON named in §0. Its literal fields, quoted so this docket does not need to re-derive them:

- `label`: `0014_tenancy_foundation`
- `sql_file`: `0014_tenancy_foundation.sql`
- `applied_at`: `2026-09-08T22:24:14Z`
- `apply_status`: `201`
- `apply_result`: `ok`

The post-state recorded in that receipt — `teams`, `team_members`, `team_invites` tables; five indexes (`team_members_user`, `team_members_team`, `team_invites_team`, `team_invites_token`, `team_invites_team_email_pending`); six policies (`teams_select_member`, `teams_insert_self`, `tm_select_member`, plus three siblings) — is the SUPABASE-side reality as of the apply timestamp. This packet observes it; it does not mutate it.

## 3. What #6925 was, and why it stayed closed

#6925 was a Public-API v1 surface proposal for F12. The PR was CLOSED (NOT merged) prior to this docket. The close reason is recorded as the planner's standing refusal to conflate a Supabase schema migration (the G2 tenancy foundation, already installed by `0014`) with an HTTP-facing Public-API surface (the F12 v1 Public-API). The two answers differ in:

- surface (PostgreSQL row-level policy vs HTTP route surface);
- authority required (Supabase migration namespace + RLS policy review vs a separately commissioned F12-v1 build packet under a future META-CEO B ruling);
- consumer (macro-side `read_entitlement(user_id)` keyed on the individual user — see F12 §3 — vs an external developer token or signed-URL surface, which has no consumer of record today);
- lifecycle (#6925 closed at draft; `0014` is the applied, receipt-archived artefact).

This docket does NOT reopen #6925. A reopen would be a planning act, not a docket act; it is the planner's lever (META-CEO B ruling text), not this packet's.

## 4. What the drafted DEC says (and what it does NOT do)

The drafted companion DEC, named in §0, records four facts and nothing more:

1. The F12 v1 Public-API surface is NOT BUILT. No HTTP route, no token issuer, no signing key, no rate-limit surface, no docs page exists in this checkout under that name. A repo-wide case-insensitive grep for the literals `public_api`, `public-api`, `PublicAPI`, `/api/public`, `/api/v1/` returns the `engine/research_factory` lineage (`public_api` as a Python identifier in research-factory plumbing, see `engine/research_factory/`), which is unrelated to the F12 v1 surface and is not a v1 deliverable. A `site/`, `templates/`, `app/`, `admin/` grep for any of those literals returns 0 hits.

2. `0014_tenancy_foundation` is applied as already true (verbatim in §2). The DEC names the receipt by path and the apply timestamp; it does not re-stamp, does not reapply, and does not assert a fresh HTTP call to Supabase. The DEC says: the schema artefact is on disk and on the database; the receipt is the proof; the DEC records the observation.

3. The F00C closure-ledger row for F12 v1 reads `NOT_BUILT` (the F12 commercial/account scope rows are already docketed at `MARKET_ONTOLOGY_HALF_A_K_CHAIN_GATE_DOCKET_2026-09-06.md` as `DOCKETED`, not `BUILT`; the F12 v1 Public-API row is a separate row, currently `NOT_BUILT`). This packet does NOT edit the CSV. Blocklists are NOT regenerated by this packet. The compiled blocklist files in `docs/` and the `compiled_blocklists` artefact remain as they stand at the head commit; no `python3 scripts/build_blocklists.py` run is invoked here.

4. The DEC is a draft. It is not a closing decision. It is the planning artefact the next F12-v1 commission will read. A future, separately commissioned, F12-v1 build packet is the only path that may install the surface; that build packet must clear its own gauntlet (rights, source register, audit-trail, design review, dashboard surface check) and will have its own META-CEO B ruling text. This docket does not pre-empt that.

## 5. What this docket DOES NOT contain (R0 prohibitions, named)

- No F00C ledger CSV edit. The CSV at `research/market_intelligence_productization/MARKET_ONTOLOGY_F00C_GRANULAR_CLOSURE_LEDGER_2026-09-02.csv` is read-only input to this docket. No row is added, no row's status is changed, no row's `granular_disposition` / `capability_state_c2` / `real_consumer` / `evidence` fields are mutated by this packet.
- No #6819 reference. PR #6819 is unrelated to this docket and is not cited, not reopened, not relitigated here.
- No DDL. No `.sql` file is added or modified by this packet. No `CREATE TABLE`, `CREATE INDEX`, `CREATE POLICY`, `ALTER TABLE`, or `GRANT` text is emitted.
- No secrets / no project reference. No GitHub PAT, no service-account key, no Supabase service-role JWT, and no 20-letter lowercase `project_ref` literal appears in this file or its companion DEC. The 20-letter regex `re.compile(r"[a-z]{20}")` is the guard that keeps a real one from ever being committed by a later edit; the guard is the test, not the body.
- No positive BUILT assertion. This docket says `NOT_BUILT` where it says anything. Any claim that the F12 v1 Public-API surface is in fact BUILT is explicitly outside this docket's authority ceiling — that claim, if it ever lands, is the future build packet's, not this docket's.
- No blocklist regen. The compiled-blocklist artefact is unchanged by this packet.

## 6. Authority ceiling

This is a records-only docket. It records state, names the receipt, drafts a companion DEC, and states prohibitions. It does not grant, defer, or unblock any build authority. A future META-CEO B ruling text — drafted under a separately commissioned W9B-REC build packet — is the only path that may lower this ceiling. Until then, the F12 v1 Public-API surface is `NOT_BUILT`, `0014_tenancy_foundation` is `applied as already true`, and #6925 stays `CLOSED`.