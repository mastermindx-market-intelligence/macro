# Consumer Cyclical Economic-change Dossier — Implementation Plan R7

> **For agentic workers:** At execution time use the installed executing-plans or subagent-driven-development workflow after current source custody and admission are verified. The steps below use checkboxes. This document is a planning candidate, not a worker assignment, permission to edit another owner's branch, or release authorization.

**Goal:** Deliver useful, source-backed explanations of Consumer Cyclical economic changes on existing company pages, beginning with PLNT, extending to LTH, then adding LULU's dated guidance comparison and accepted theme navigation.

**Architecture:** Reuse Company Intelligence's bound-release and issuer-profile path; compose deterministic, receipt-bound explanations under the existing F04/STSI responsibility. Extend the existing private earnings generation and API together, with one publisher and pointer. The existing ticker page remains the entry surface. No new source, financial, metric, identity, event, publication, authentication or evaluation plane.

**Tech stack:** Existing Python/Decimal and pytest, existing earnings publication/store and FastAPI transport, existing Jinja ticker template and JavaScript client. No new vendor, paid API, LLM dependency or package installation is required by this plan.

**Spec:** `research/consumer_cyclical/CONSUMER_CYCLICAL_ECONOMIC_CHANGE_DOSSIER_DESIGN_R6_2026-09-23.md` at `c78370dcfbdea21b2a4ce1577082c45fce67c00d`, blob `6d3159738500e45f118c288f7ebfa470daade0ec`, read together with the R7 amendments below. R6 numerical oracles remain in `CONSUMER_CYCLICAL_R6_DESIGN_EVIDENCE_2026-09-23.json` at `911252113ae4481261bfd34fe1082f8381e5947b`.

**Disposition:** PRINCIPAL-REVIEWED PLANNING CANDIDATE / NATIVE-OWNER DECISIONS PENDING / NOT IMPLEMENTED. The current Chairman continuation authorizes this planning work; it is not represented as approval of another owner's interfaces. No independent design review, source admission, application run or Fable delivery is claimed.

**Carrier:** Macro #7804, `sol/consumer-cyclical-research-20260923`; operation `gmi-consumer-cyclical-research-20260923-sol-001`. Research cutoff September 23, 2026. Native source pin `8db6896dab2199a4b7fc61a005c225380cac7cd6`. Protected Mastermind pin `a7d2b3049e5cdc523e91e61a6e9d70a1cb911157`, Skillpack 1.0.1/bootstrap 1.

## Global constraints

- Preserve PLNT/LTH/LULU as contrasting acceptance cases, not a new basket. Preserve the entire R1–R6 sector mission and 14-family coverage map.
- Existing `event_workspace.v1` top-level keys and old profile output stay unchanged. New optional context is closed and validated when present; an absent context cannot support a new calculation.
- `authority=context_only`; ranking, selection, entry, gating, sizing and trading authority remain false. No consensus beat/miss shortcut.
- Exact source bytes, issuer, accession, fiscal event, economic interval, unit/scale, basis, precision and dependency references travel together. Research JSON values are not native receipts.
- Private objects remain in the existing private store. Preserve entitlement-before-read, private/no-store responses and the existing single publisher/pointer.
- The first Company Intelligence browser fetch remains event-workspace-first. Its existing narrow no-coverage fallback is not relaxed for this enhancement.
- No writes to #7426, its branch, shared templates or production sources from this research carrier. Future code needs current owner custody, normal review and release proof.
- Company-first proof is a partial milestone. Full three-case/theme acceptance requires LULU history and a real accepted GMI navigation chain.
- No elapsed-time promise, implied background work or assumption that a posted request is a received assignment.

## Review focus

1. A new two-record stage must not replace the complete private earnings catalog; the next normal wire publication must preserve the extension too. Task 4 owns the regression.
2. A release-only dossier must not inherit the transcript manifest's source clock or fabricate a transcript source. Tasks 1 and 4 own that regression.
3. An issuer added for private analysis must not be swept into a public publication list. Tasks 2 and 5 own the leak test.
4. SEC acceptance is not first public availability. No fixed lag or unlabeled timestamp becomes an intraday availability fact. Tasks 1 and 7 own the cutoff tests.
5. A browser response for one issuer/generation cannot populate another page or remain after logout; the existing event-workspace fallback contract remains unchanged. Task 5 owns those tests.

## 1. R6 review decisions and narrow supersession

**R7-D1 — dependency separation.** R6 section12 starts by listing several source-owner decisions together. The executable dependency graph now separates single-event PLNT/LTH work from historical LULU work. The v3 history index is not a prerequisite to planning, extracting or comparing one admitted release. It remains a prerequisite where a historical/as-known claim actually relies on its selection semantics. This does not waive source admission or privacy for either path.

**R7-D2 — publication needs whole-generation design.** R6 section9's record-version extension is necessary but insufficient. At the inspected pin, `prepare_private_publication` validates a complete stage, requires `context/latest.json`, and derives manifest source fields and `published_at` from the wire context manifest. Adding structured records alone would leave incorrect provenance and potential inventory replacement. Extend record and manifest validation together, retain the existing pointer and let its one current publisher assemble the entire generation. Never promote a Consumer-only replacement stage, append a second publisher, or invent a dummy transcript manifest.

**R7-D3 — name the source template.** The actual producer is `scripts/build_ticker_pages.py`; the page template is `templates/ticker.html.j2`. The existing Company Intelligence client is `site/assets/js/company-intelligence-dossier.js`. Its event-workspace-first contract remains intact. Generated `site/stocks/*.html` is evidence/output, not the primary source to hand-edit. A paid enhancement is an adjacent component, not a new dossier homepage.

**R7-D4 — clock claim correction.** SEC's Webmaster FAQ explicitly distinguishes acceptance from availability on sec.gov: publication often follows within1–3 minutes, can be later, and has no guaranteed lag or first-availability timestamp. The existing builder's acceptance-based `source_available_at` convention is not silently renamed. Obtain the source owner's annotation/selection decision, label acceptance as acceptance, retain real observation/publication clocks and decline unsupported intraday public-availability claims. A fixed three-minute offset is not proof. This supersedes any reading of R6's acceptance horizon as exact public availability, not the underlying issuer events.

**R7-D5 — private enrollment must be explicit.** Registry admission and public build selection are separate decisions. New profiles may be used for offline/private preparation only after permission; no existing public enumerator may automatically export the new paid fact closure. The public shell carries a valid route and access state, not hidden values in HTML, metadata, scripts or client caches.

**R7-D6 — preserve the content thesis.** The private catalog, clocks and validators support the actual job: explain economic change. Each visible section must still show the supported positive/negative/mixed interpretation, calculation, counterevidence and next observation. Shipping only metadata or a locked empty panel is not acceptance.

These are this principal's design-review findings. They are not independent review or acceptance of the shared source owner's code. The exact owner consultation is #7426/comment `5807032650` (`cc-r7-source-interface-review-20260923-sol-001`), posted and read back; no ACK or adjudication is inferred. #7426 remains held at `7bc04876747d773861b47519061279ae033a148d`. The earlier Li Auto proposal stays on #7331/comment `5758415772`.

## 2. Delivery boundaries

| Slice | Named useful result | Can start its planning without | Release still requires |
|---|---|---|---|
| V1 PLNT | Entitled user sees the exact advertising/revenue change and its limited economic meaning | LULU guidance history, a new theme | Source/privacy contracts, exact retained source, native extraction/composition, old-consumer regression, real API/browser proof |
| V2 LTH | Same workflow explains operating evidence and both sides of the H1 cash bridge | LULU history | V1 shared transport plus LTH source/profile, cash-sign/period tests and browser proof |
| V3 LULU | Two reporting events explain a change in the same management-guidance target | New annual or synthetic event | Accepted release-guidance and historical selection semantics, two real sources, correction/clock proof |
| V4 Theme journey | Investor reaches a covered dossier through a governed theme relationship | New identity/basket plane | Accepted GMI relationship and route, truthful coverage, cross-page proof |

Group Tasks1–5 into the smallest useful V1 release; do not open separate infrastructure PRs merely to split files. An incumbent owner may need an independent shared-contract PR, but it must name V1 as its consumer and preserve its existing release process. Task6 adds V2; Task7 adds V3; Task8 closes the broader journey. No PR, runtime child or deployment is created by this plan.

The first integration plan uses explicit external gates: `SOURCE_CONTRACT_ACCEPTED`, `SOURCE_CUSTODY_GRANTED`, `RETAINED_INPUTS_ADMITTED`, `PRIVATE_GENERATION_ACCEPTED`, `PUBLICATION_CUSTODY_GRANTED`, `PAGE_CUSTODY_GRANTED`, `GUIDANCE_HISTORY_ACCEPTED`, `GMI_RELATION_ACCEPTED`, and `NORMAL_RELEASE_GATES`. These are descriptive plan conditions, not new production enums, tickets, leases or control-plane records. Their actual receipts belong to the existing owners. Unknown remains unsatisfied.

## 3. File ownership and proposed interfaces

Existing modification points: `engine/company_intelligence/issuer_profiles.py`, `engine/company_intelligence/event_workspace_build.py`, `engine/company_intelligence/event_workspace.py`, `engine/earnings_narrative/private_publication.py`, `scripts/build_earnings_public_wire.py`, `scripts/publish_earnings_private_store.py`, `app/earnings.py`, `scripts/build_ticker_pages.py`, `templates/ticker.html.j2`, and the existing page asset pipeline. These are prospective changes under their owners, not current write scope.

Proposed focused files, only after owner approval: `engine/company_intelligence/measurement_context.py` validates the source owner's optional context; `engine/company_intelligence/consumer_cyclical_profiles.py` holds the three issuer-specific profiles; `engine/company_intelligence/economic_change.py` holds pure F04/STSI-governed composition; `site/assets/js/economic-change-dossier.js` renders the authenticated adjacent section. The composer is a read adapter inside an existing domain, not a financial engine or store.

Proposed callable signatures, not APIs claimed to exist today:

```python
from collections.abc import Mapping, Sequence
from dataclasses import dataclass
from datetime import datetime
from typing import Any, Callable

@dataclass(frozen=True)
class DossierReadPolicy:
    source_cutoff: datetime
    observed_cutoff: datetime
    clock_basis: str
    maximum_facts: int = 64
    maximum_results: int = 16
    maximum_bytes: int = 262144

@dataclass(frozen=True)
class DossierResult:
    status: str
    payload: Mapping[str, Any] | None
    reasons: tuple[str, ...]

def validate_measurement_context(
    fact: Mapping[str, Any], *, event: Mapping[str, Any],
    verify_span: Callable[[Mapping[str, Any]], None]
) -> Mapping[str, Any]: ...

def compose_economic_change(
    workspaces: Sequence[Mapping[str, Any]], *, policy: DossierReadPolicy
) -> DossierResult: ...

def validate_member_record_versioned(value: object) -> dict[str, Any]: ...
```

Ellipses here are type declarations, not an implementation stub to commit. Existing native objects, receipt validators and identity functions remain authoritative. An approved interface may rename a helper; update all dependent test/plan references in one revision, not with silent ad hoc adapters.

The proposed closed context uses `schema`, `period_start`, `period_end`, `period_kind`, `role`, `issuer_id`, `business_scope`, `definition_ref`, `definition_version`, `basis`, `unit_measure`, `unit_scale`, `value_decimal`, `source_precision` and `context_spans`. Guidance adds its explicitly named target interval. These field names are a candidate for owner acceptance, not an enrolled schema. Boolean numeric fields, non-finite values, unknown keys and unsupported versions are rejected. The accepted context must bind each input to its economic interval, role, issuer/business population, definition version, basis, signed source value and unit scale. A calculation reference includes issuer, event, source document/hash, workspace generation and fact identity. `fact_id` alone is not unique across events. A validated receipt for a number does not validate a header or its semantic role; preserve context receipts as well.

## 4. Task1 — source package and measurement contract

**Files:** New `tests/consumer_cyclical_support.py`, `tests/test_consumer_cyclical_measurement_context.py`; proposed `measurement_context.py`; existing `event_workspace.py` only under source-owner custody. **Consumes:** R6 four source bindings and existing BoundRelease/receipt/identity types. **Produces:** admitted input fixtures plus a closed source-context validator, not enrolled through this research PR.

- [ ] Have the source owner accept the explicit context and private-selection contract. Use consultation5807032650; preserve #7426's hold. A missing reply is not acceptance. A local fixture can still be prepared as unadmitted research evidence.
- [ ] Retain each selected exhibit through the existing source intake, with CIK/accession/exhibit name, raw-byte digest, actual retrieval/observation and the owner's aware acceptance field. Store outside public Git under the existing archive conventions. R6 research values cannot fill a failed fetch. PLNT is0001637207-26-000042; LTH0001869198-26-000041; LULU0001397187-26-000077 and0001397187-26-000126.
- [ ] Implement `load_source_case(case_id, root)` in the test-support file as an adapter over the incumbent manifest/document readers. It must verify the manifest, digest and source-role fields before calling the real binder; no hand-built replacement for a native receipt. Missing raw fixtures make the real-source acceptance test fail with a named missing-input result, not skip to green.
- [ ] Write the tests below before the helper. `case` is the fixture returned by that adapter, with its native `facts`, `event` and byte-replay callback. The fixtures and their failure mutations live in the test-support file, not production data.

```python
import copy
import pytest
from engine.company_intelligence.measurement_context import validate_measurement_context

@pytest.mark.parametrize("field,value", [
    ("period_kind", "instant"), ("role", "management_guidance"),
    ("unit_scale", 6), ("value_decimal", True),
])
def test_rejects_context_disagreement(case, field, value):
    fact = copy.deepcopy(case.facts["plnt_ad_revenue_current"])
    fact["measurement_context"][field] = value
    with pytest.raises(ValueError):
        validate_measurement_context(fact, event=case.event,
                                     verify_span=case.verify_span)
```

- [ ] Bind allowed period/role/scale to the extracted context evidence, not a universal rule that every metric has PLNT's shape. Test a valid zero separately; reject booleans, NaN, ambiguous columns, unsupported rounding conversion and metadata that disagrees with the literal value.
- [ ] Run `pytest -q tests/test_consumer_cyclical_measurement_context.py` red before implementation and green after it. Then run existing source-owner compatibility suites selected by that owner. Record real counts only after execution. Commit the owned source, tests and scoped evidence together.

## 5. Task2 — PLNT literal profile and private-only enrollment

**Files:** Proposed `consumer_cyclical_profiles.py`, new `tests/test_consumer_cyclical_profiles.py`; existing profile/registry call sites under Company Intelligence custody. **Consumes:** Task1 admitted source and context. **Produces:** PLNT direct facts via the existing `extract_release_facts` signature.

- [ ] Add a `plnt_profile()` factory using the current `IssuerProfile` type. Its release extractor receives the actual `BoundRelease`, native document/event IDs and fiscal period; it does not fetch network content, create fake transcript segments or calculate differences.
- [ ] Bind rows AND columns to quarter/year, table scale and exact source coordinates. Expected source values in USD thousands: revenue365223/340879; advertising revenue32922/22781; advertising expense32922/22777. Receipts must be generated from retained source bytes, not from this list.
- [ ] Write tests asserting all six values, their current/prior intervals and replayed context. Mutate an unrelated repeated32922, swap current/prior columns, substitute H1 for Q2, and change the issuer. Each mutation must fail or produce typed absence rather than a plausibly wrong number.
- [ ] Add private analysis identity through the current registry owner. Verify that existing public build-selection inputs are unchanged and the new complete paid fact closure is not written into the old public workspace nest. Do not weaken a public writer to gain private coverage.
- [ ] Run `pytest -q tests/test_consumer_cyclical_profiles.py -k plnt` red/green, then compare old accepted issuer payloads byte-for-byte with their frozen baselines. Preserve the default Apple profile behavior. Commit the bounded source change with these tests.

## 6. Task3 — deterministic economic explanation

**Files:** Proposed `economic_change.py`, new `tests/test_consumer_economic_change.py`. **Consumes:** accepted facts plus explicit read cutoffs; can be developed against synthetic adverse inputs without claiming real-source acceptance. **Produces:** `DossierResult` containing the proposed `economic_change.consumer.v1` view after owner enrollment.

- [ ] Use a fixed internal operation set: matched difference, signed sum, difference of changes, positive-denominator change ratio, matched range comparison and reconciliation difference. No user expression, `eval`, network access or LLM call. Use Decimal with explicit precision; preserve source scale and display rounding separately.
- [ ] Match issuer, role, definition/basis and economic scope. A current/prior difference deliberately permits different source intervals only under the declared comparable-period relation. A cash sum requires the same interval. A guidance comparison requires the same target interval, not the same reporting quarter.
- [ ] Emit direct input references and derived-result dependencies separately. Missing input suppresses its dependency closure, not unrelated results. Do not mix incompatible revisions within one selected event. Explicitly named cross-event inputs may have different generation IDs, but every selected version must satisfy its own source and observation cutoffs; a later revision cannot leak through either cutoff. Provenance is not sufficient for comparability.
- [ ] Add the following tests. `workspace_case` constructs explicitly synthetic event/fact fixtures or loads an admitted case; it never treats the synthetic branch as production. `result_value` reads one declared result and fails for missing or unavailable output. All three helpers belong to `tests/consumer_cyclical_support.py`: `workspace_case(ticker: str, omit: str | None = None) -> dict`, `result_value(result: DossierResult, key: str) -> str`, and `policy() -> DossierReadPolicy`. The policy uses aware fixture cutoffs; fixture source/observation clocks are explicitly synthetic. A helper must fail on an unknown key rather than manufacture a value.

```python
from tests.consumer_cyclical_support import workspace_case, result_value, policy
from engine.company_intelligence.economic_change import compose_economic_change

def test_plnt_preserves_residual_and_label():
    result = compose_economic_change([workspace_case("PLNT")], policy=policy())
    assert result_value(result, "advertising_net_change") == "-4"
    assert result_value(result, "total_revenue_change") == "24344"
    assert result.payload["results"]["revenue_change_ratio"]["label_key"] == \
           "ratio_of_reported_revenue_changes"

def test_missing_input_does_not_zero_fill():
    ws = workspace_case("PLNT", omit="plnt_ad_expense_prior")
    result = compose_economic_change([ws], policy=policy())
    assert result.payload["results"]["advertising_net_change"]["value"] is None
    assert result_value(result, "total_revenue_change") == "24344"
```

- [ ] Test zero and negative denominators, cross-issuer duplicate fact IDs, stale revisions, unknown operations and input/result/byte limits. Numeric conclusions must not contain organic growth, pricing power, normalized profit or a trade verb unless independently supported and permitted; the first slice does not support those claims.
- [ ] Render original, reviewed explanation templates: conclusion, calculation, counterevidence and one watch condition. Source-attributed statements and research hypotheses remain distinct. An unavailable calculation is not a bearish signal. Run `pytest -q tests/test_consumer_economic_change.py` red/green and commit.

## 7. Task4 — complete private-generation compatibility

**Files:** Existing `private_publication.py`, `scripts/build_earnings_public_wire.py`, `scripts/publish_earnings_private_store.py`, `app/earnings.py`; tests `test_earnings_private_store.py`, `test_earnings_api.py` plus new `tests/test_economic_change_publication.py`. **Consumes:** Task3 view and the complete incumbent stage. **Produces:** an owner-accepted versioned member record and manifest on the existing pointer/API.

- [ ] Freeze the actual old pointer/manifest and inventory as a test baseline. Design the v2 record/manifest together: old v1 record validators are unchanged; v2 exposes explicit kind/issuer/event/dependency/clock fields and the structured view. Keep wire context provenance attached to wire records. Release-only records bind their actual event/source closure; they do not reuse the wire clock.
- [ ] Use the one incumbent full-generation preparer. Supply additional dossier records as explicit inputs to its normal build. Never publish a two-record patch as the whole generation, or create a second latest pointer. The normal subsequent wire-only input refresh must retain/rebuild still-valid dossier records through the same owner inputs; accidental catalog omission fails publication instead of silently deleting them. A missing new source is different: retain the complete catalog and emit an explicitly unavailable dossier record, or an explicitly historical last-known record where the existing owner permits that behavior, while unaffected wire records continue. Do not block all earnings delivery for a lane-local Consumer source fault.
- [ ] Bound additions/deletions against the current complete inventory and explicit owner-approved removals. A permitted removal is evidence, not an empty-input side effect. All retained referenced objects remain digest-verified. Source correction regenerates only affected records while the complete manifest is reassembled.
- [ ] Preserve the incumbent serialized publication custody. Reconcile the expected predecessor before promotion using whatever fence the actual publisher/store supports. Do not claim compare-and-swap exists merely because a helper is named that way. If no distributed fence exists, require the already-owned single-writer path; do not invent a lock service. An ambiguous promotion is reconciled on the same pointer before another write.
- [ ] Extend the existing API's schema dispatch while retaining the same store and entitlement-first behavior. Old slugs return their exact old v1 record shape through the updated reader. An unsupported new schema fails closed before a writer can activate it; deploy compatible readers first. Independent versioning of an API URL is not required by the payload version.
- [ ] Test a complete baseline containing old recordsA/B and wire contextC, add PLNT, then run a normal wire refresh. A/B/C and valid PLNT must still resolve. Test a partial replacement, accidental deletion, mismatched source clock, corrupt body, concurrent stale predecessor, unsupported reader, malformed slug, anonymous/expired membership and write/readback failure. No store read is permitted before entitlement for private payloads.
- [ ] Run `pytest -q tests/test_earnings_private_store.py tests/test_earnings_api.py tests/test_economic_change_publication.py`. Record red/green/compatibility results. Immutable writes/readback precede pointer promotion; failure leaves the last verified pointer intact or explicitly EFFECT_UNKNOWN, never assumed rolled back. Commit through the private-publication owner.

## 8. Task5 — PLNT real user journey and release

**Files:** Existing ticker builder/template; proposed `site/assets/js/economic-change-dossier.js`; new `tests/test_economic_change_ticker_integration.py` and `tests/test_economic_change_browser.py`. **Consumes:** accepted private record route and source generation. **Produces:** V1 source-to-visible-result proof.

- [ ] Add a compact adjacent Economic change mount below the existing primary task in `templates/ticker.html.j2`; the existing Company Intelligence root and its fetch/fallback rules are unchanged. `build_ticker_pages.py` emits only eligible public route metadata, no paid numerical payload, economic summary or source spans.
- [ ] Client entry is proposed `mountEconomicChange(root, {loadEntitledRecord, authChanges})`. Both dependencies must come from the current site authentication integration; no token acquisition, credential storage or separate auth state machine. The renderer validates issuer, record kind, event/generation and numeric schema before display. Use text nodes for source text and finite, locale-aware display of server-computed values.
- [ ] Test wrong-issuer responses, delayed responses after ticker navigation, logout while a request is in flight, expired entitlement, malformed nested paths and unavailable generations. Abort/ignore obsolete responses, clear paid DOM on logout, never fall back to public source data for a failed private request.
- [ ] Prove the exact six native source facts and derived results traverse the actual release/build/private/API/template path. A synthetic fixture or manually pasted JSON cannot pass the real-path acceptance. Open each cited source coordinate and verify period/header context.
- [ ] At1440/820/390 widths, EN/ZH and dark/light, verify visible conclusion, calculation, limitation and watch condition; keyboard operation, focus, screen-reader labels, signs/scales and no horizontal overflow. No material sector-landing-page height increase. Failure in the panel must not break quote, charts or existing intelligence.
- [ ] Check generated production HTML/JSON/metadata, service-worker caches and anonymous object paths for the paid closure. Compare structured keys, bound source spans and new private content, not coincidental numeric strings; expressly published research summaries are not retroactively private production payloads. Freeze and compare identical-input selection/ranking/entry/sizing outputs. Run the new integration/browser suites and existing page-owner regressions, then normal exact-head review/CI and authorized production proof. Only then label V1 PROVEN_LIVE; parent remains incomplete.

## 9. Task6 — LTH on the proven V1 path

**Files:** The same profile/composer modules; existing proposed test suites. **Consumes:** admitted LTH release and V1 transport. **Produces:** V2 cash reconciliation plus source-supported operating context.

- [ ] Add `lth_profile()` with separately receipted H1 cash legs:408351,-523276,200191 and reported85266 in USD thousands. The source's signed capex is added once; no double negation. Quarterly operating evidence remains on its own interval, not summed into H1 cash.
- [ ] Test company-defined FCF85266, pre-sale-leaseback subtotal-114925 and reconciliation residual0. Mutate a cash leg to Q2, absolute-valued capex, proceeds counted twice and missing report definition. Reject the invalid calculation without hiding safe literal inputs.
- [ ] The explanation retains both cash views and the disclosed positive operating evidence. It cannot assert maintenance-only/normalized FCF, a causal margin explanation or returns on new-site cohorts not observed. Translate captions without changing numeric meaning.
- [ ] Run profile/composer regressions and the real LTH private HTTP/browser journey; update the same full generation without losing PLNT or legacy records. Release V2 only on its own actual proof.

## 10. Task7 — LULU after native guidance/history adjudication

**Files:** Incumbent source-owned guidance/profile hooks and the same composer/tests; no edits to #7426 from this research branch. **Consumes:** accepted response to consultation5807032650 and the two admitted release versions. **Produces:** V3 shared-target comparison with explicit historical claim scope.

- [ ] Consume the existing release-guidance callback and event/revision decision. Do not implement an alternate index, suppress prior-source identity or treat distinct documents as correction by hash alone.
- [ ] Preserve Q1 and Q2 reporting-event IDs. Compare FY2026 target end2027-01-31 through explicit event/document/generation/fact references. Prior range[11000,11150], revised[10350,10500] USD millions: bound changes-650/-650 and separation500. No invented annual results event or conflicting same-quarter alias.
- [ ] Test ordinary reissued guidance, documentary correction, parser correction, duplicate delivery and actual-outcome publication as distinct semantics. LULU's EPS adjustment bases are not automatically comparable; do not loosen consensus restrictions. Midpoints, when shown, are explicitly illustrative.
- [ ] Test source-acceptance horizon separately from observed-system and actually-served history. A source revision observed later must not leak into earlier replay. No exact first-public instant follows from acceptance, and no hardcoded delay guarantees it. If the native owner cannot supply the needed historical selection, current comparison is labeled as current observation of historical releases, not historical served proof.
- [ ] Run native history-reader, source profile, composer and real private/browser tests after accepted owner integration. Only accepted results can close the relevant history gate; green source CI alone cannot.

## 11. Task8 — theme integration, evaluation and Fable closeout

**Files:** Existing GMI/STSI relationship consumers and ticker/theme page owners; their exact accepted interfaces are selected at execution, not fabricated here. **Consumes:** V1–V3 plus actual accepted relationship records. **Produces:** V4 full three-case theme/company journey and complete bounded delivery evidence.

- [ ] Resolve actual issuer/security identity and any accepted theme relationship through existing owners. Missing relationships stay missing. No automatic Fitness basket, keyword-generated membership or same-sector equivalence. Company pages remain useful while this gate is open.
- [ ] Walk a real accepted theme-to-company path and back. Confirm the same interpretation, source versions and coverage on both surfaces; a stale theme card cannot relabel fresh company data.
- [ ] Obtain independent review of semantic correctness, privacy/old-consumer compatibility and user-task proof. Fable integrates through current custody and the least-scarce admitted engineering lane; Fable is not assigned routine source rediscovery by this plan.
- [ ] Preserve separate completion statements: V1, V2, V3, V4, bounded first-dossier delivery and the entire Consumer Cyclical program. All normal review/CI/release gates apply. Update Agent OS/PR evidence at actual accepted effects.
- [ ] Keep parent research open for international/undercovered families, retained cash/cohorts, ordinary failed recoveries, dated expectations/valuation and investment validation. No explanatory panel or passing parser grants predictive authority. Use the existing Evaluation owner; no shadow outcome ledger.

## 12. Validation and execution handoff boundary

R7 validates this PLAN: source pins, requirement coverage, typed interface declarations, dependency graph and hypothetical gate scenarios. Those checks are not execution of Tasks1–8. All148 earlier prospective application cases remain NOT_EXECUTED. No native module run was completed here: the one attempted local public-source download failed at DNS before byte acquisition; GitHub connector and web-source reads remained usable. No retry/failover or native success is inferred.

Before execution, the integrator must have exact accepted source/private/page contracts and source-custody receipts, the raw source packages, the approved plan/spec versions, actual resource/permission state and normal release path. The intended Fable handoff is not yet dispatched or consumed. Shared owner decisions are pending, not Chairman chores by default; continue safe independent planning/research while they are unresolved.

The companion `CONSUMER_CYCLICAL_R7_REVIEW_AND_DELIVERY_MAP_2026-09-23.json` maps every R6 acceptance case to a task and records the simulated dependency scenarios separately from product tests. The complete first-slice input package is R6 specification/evidence plus this plan and companion; R1–R5 provide the broader economic models, coverage and source limitations.

## Primary references

N1–N5 are immutable reads at Macro `8db6896dab2199a4b7fc61a005c225380cac7cd6`:
- N1 `engine/company_intelligence/event_workspace_build.py`, blob `69d39a58ecc9cfd6143192f29eb2e0f62016aeb5`, inspected lines180–310: existing callback, acceptance/observation and changed-source behavior.
- N2 `engine/earnings_narrative/private_publication.py`, blob `0ee93909693893f419f0109f9eba1994d94e2b46`, inspected lines345–585: full context/record manifest, shared source clock and verified writes.
- N3 `scripts/build_ticker_pages.py`, blob `de2ab71d33b0141dd18adc765584f8d2d1832fd5`, lines1–70: actual static ticker producer.
- N4 `templates/ticker.html.j2`, blob `14276ab2d09a6402e62a8f2fbb86f7c62689e3ec`, lines1–50: actual template and existing client assets.
- N5 `site/assets/js/company-intelligence-dossier.js`, blob `f211bb2aa8fa5cbfe78c522159402231c1267840`, lines1–110: frozen fetch/fallback behavior.
- P1 https://www.sec.gov/about/webmaster-frequently-asked-questions , Developers/lag and timestamp sections: acceptance is not first public availability; variable lag is not a guaranteed offset.
- P2 https://www.sec.gov/Archives/edgar/data/1637207/000163720726000042/0001637207-26-000042-index.htm : PLNT issuer/accession/exhibit binding.
- P3 https://www.sec.gov/Archives/edgar/data/1869198/000186919826000041/0001869198-26-000041-index.htm : LTH issuer/accession/exhibit binding.
- P4 https://www.sec.gov/Archives/edgar/data/1637207/000163720726000042/plntq22026pressreleaseex991.htm : PLNT literal source, not retained native bytes.
- P5 https://www.sec.gov/Archives/edgar/data/1869198/000186919826000041/lth-20260630xex991.htm : LTH literal source, not retained native bytes.
