# Theme recommendation reasons: source-bound continuation

## Mission and source

The Chairman's continuing US leadership / Prophet mandate requires useful
opportunity recognition without confusing broad risk, theme strength and entry
permission. This bounded slice repairs the explanation of an existing decision;
it does not promote a new score, ranking, entry rule or allocation policy.

Protected procedure: Mastermind `4ca1b97e65de9d4ba8c868b9d708fb7620a8a76f`,
protected master and compatible Skillpack 1.0.1/bootstrap 1, same-pin INDEX and
required skills loaded. Source base: macro
`7c6e35163c9f67087ffe174a7ab3810f47ce6a45`.
Carrier: `claude/theme-recommendation-reasons-20260921-sol`.
Direct execution rationale: PRINCIPAL_JUDGMENT / LOWER_TOTAL_OVERHEAD. No worker,
provider session, watcher, trade or production publisher was started by this slice.

Existing parent: `WS:PROPHET-US-V4-RECOVERY`. No new program or truth store.
Source custody is path-disjoint from the clock correction #7650, hottest-desk
#7520, candidate visibility #7572 and shared action-board owner #7076. The latter
already has lane-copy work: do not duplicate its `_us_act_now_board.html.j2` edit.

## Capability

The recommendation owner now returns the existing verb and the actual deciding
reason together. The existing `_reco` API remains a verb-only wrapper. The real
producer attaches the final explanation after its existing risk-off and momentum
cooling safeguards, preserving those guards unchanged.

Users can distinguish a long-trend block, macro constraint, crowding limit,
relative-strength model limit, measured own-price extension, actual deterioration,
and missing information. A positive theme recommendation does not manufacture a
clean entry: clear, unconfirmed and unavailable entry-quality reads have distinct
short bilingual explanations. The existing basket desk and detail renderers
consume these fields; the legacy non-entry fallback no longer falsely declares
that no constituent stock has an entry from a basket-level texture alone.

No scores, ranks, canonical recommendations, price data, membership, entry
permissions, portfolio rules or current market claims change. The relative-strength
limit remains in the incumbent policy and is now disclosed honestly; evaluating
its replacement is a different, promotion-bearing task.

## Proof already obtained

- Initial new reason tests: 34 failed against the untouched source.
- Reason, incumbent theme, conflict and leadership-split suites: 137 passed after the native consumer, legacy relative-strength wording and
  basket-detail contracts were included. Python 3.12 core/consumer subset: 129 passed.
- Frozen incumbent versus current policy: 10,584 boundary/input combinations,
  including missing extension and non-finite macro/crowding inputs, zero changes
  to recommendation verbs.
- Real committed September 18 theme payload: 49/49 final recommendations
  reproduced from available rounded inputs; all non-explanation payload bytes
  represented by the JSON object remain semantically identical.

`prove.py` reads exact Git objects and produces source hashes, before/after text
and explicit mismatches rather than rewriting a stored verdict. Its rounded-input
snapshot replay is not a full nightly rebuild, logged-at replay, deployed-browser
proof, or evidence of improved returns. The frozen policy test fixture is an
independent test-only control, not a second production policy.

## Presentation contract

This is a content repair, not a redesign. Existing dark command-center material,
light research-workspace material, geometry, type, color tokens and interactions
are retained. Both languages use the same reason identity and equally plain copy.
The new reason line stays within 14 English words. No translated text is put in a
`title` attribute; the existing LENS `data-tip-en/zh` channel carries the fallback.

Required browser checks: actual basket detail renderer with real stored theme
inputs, dark/light × EN/ZH × 1440/390; positive, held, unknown and legacy fallback;
no injected markup; real reason visible; no authority change. Browser replay now covers 48 captures: three source-bound theme cases, original
and candidate templates, both themes/languages and both widths. Candidate reasons
were visible in all 24 candidate cells, with zero page errors and zero page-level
overflow across the matrix. Unrelated live/API endpoints intentionally exercise
the existing unavailable-feed path, not an old Git-tracked quote substitute.
`browser/browser-proof.json` records inputs, rendered hashes and the actual text. Production rendering remains with its normal
owner, and no generated detail page is manually committed to simulate deployment.

## Parallel release state and continuation

#7650 remains on exact head `ad4a8c32a79e1e919021fa9efce9beea33fc2c7c`.
Its full hosted CI run 35660945127 and fences passed. An actual independent review
request was added for mastermindx-3 this turn; request is not execution/approval.
The legacy Vercel gate dependency was subsequently resolved by independently
approved #7478, merged at `111eb086380b36fcb4afa69add59877d1a939441`. The existing
merge-control owner executed successful real sweep 35677588845 at that exact
merge. The old external Git status may remain red, but is now nonbinding under
the accepted owner policy; no actual repository check was waived. The inactive `ci-authority/codex/merge-queue-pilot` status is
nonbinding on main under the existing `merge_on_green.py` policy; the active main
authority check stays binding. Source acceptance and real deployment are not
inferred from passing tests.

The native test suites and frozen control are registered in the existing CI owner;
no check is removed or weakened. Next: complete exact-head hosted CI/review and
authorized publication verification on this carrier. Resume the original #7650
release only against actual concluded checks, independent review and current
integration evidence. Preserve the unresolved shared-board and Prophet ranking
frontiers; no favorite-name exception can substitute for those capabilities.

MISSION_COMPLETE: false
CAPABILITY_STATE: BUILT_NOT_PROVEN

### Visual review follow-through

The first mobile capture exposed wrapping of the old long action badge. The
copy-only repair uses **WAIT FOR ENTRY / 等待入场** while its adjacent reason
preserves positive theme leadership. No color, layout or CSS rule changed.
Legacy relative-strength texture wording is translated as high relative strength,
not absolute price extension; its predicate and contested-state count are unchanged.
The original-template captures remain the comparison, not a fabricated before image.

### Additional source finding: two relative-strength gates

`engine/group_flow.py:prep_group` constructs `rs = lvl / bench` and
`rs_pctile = pct_rank_window(rs, z_lookback_d)`. This is the theme's own rolling
relative-price history, not a cross-sectional ranking among sectors. A misleading
source comment and the touched texture copy were corrected; no calculation changed.

`engine/basket_score.py:clean_entry` additionally requires `rs_pctile < 0.75`
(or an absent percentile under the incumbent rule), even when quality exceeds
0.6 and the tape is not breaking. This is separate from the 0.85 dominant-theme
recommendation cutoff in `_reco`. It explains how a theme can remain ACCUMULATE
while its clean-entry flag is false. This finding is not proof that all strong
trends should be chased: evaluate the two gate constructions separately, preserving
price-risk controls and the existing policy until a promotion decision is supported.
The clean-entry policy was not changed by this slice.

A direct synthetic discriminator held a rising 250-point price series, acceleration
0.6, breadth 80%, three new highs/no new lows and RSI 50 fixed. Only `rs_pctile`
changed: .74 produced clean-entry true / quality .9; .75 and .85 produced false /
quality .7. This invokes the unchanged `basket_score.clean_entry` owner. It isolates
the existing gate; it is not current market evidence or proof of superior returns.

### Release receipt

This capability is source PR **#7669**, initially checkpointed at implementation
`01a7996bacb018f5fa1f1b08ac8379fff2038c20`, with the current cumulative handoff
carried by its subsequent docs-only revision. It remains DRAFT / HOLD-FOR-SOL
pending full exact-head CI and independent review, followed by normal production
publication proof. A requested review is not execution or approval. The merged
#7478 dependency is complete and must not be recreated. Its successful production
sweep still disclosed an independent baseline/capacity backlog, not an excuse to
create a second release controller or launch duplicate baseline runs.


## Continuation: actual clean-entry attribution, not a threshold guess

Protected source was re-pinned to `Mastermind@ce18ed4f1eaa28e65a616a90aecca5c5ce1c5a2e`
(protected master, compatible 1.0.1/bootstrap 1). Current Macro input pin is
`f98f57f4ec7c2cc30cbd90d9a7a759d5ea69144c`; the relevant recommendation, clean-entry,
relative-strength, pulse, calendar and template owners have no semantic movement
from the previously recorded baseline. Original #7650 still has complete green CI
and no independent approval. #7669's earlier docs-head CI is not proof of this new
semantic change. The existing source carrier is preserved.

### New executable research capability

`audit_entry.py` invokes the **unchanged native `basket_score.clean_entry`** on the
exported price series, fingerprint and breadth of every theme in one immutable
snapshot. It first requires reproduction of the stored flag AND quality. Only then
it makes a second, copied research call changing the relative-strength input to
the closest representable float below the incumbent .75 boundary. It distinguishes
a lone boolean veto from a veto plus a quality-score penalty, and from other entry
conditions. No output changes any recommendation, score, ranking, entry permission
or data publication.

The existing `theme_extension` artifact supplies a separate own-price context,
using its existing close-based volatility units. It joins only on exact date,
identity and population count, and refuses mismatched/duplicate/missing/bad-band
input. **That does not prove an atomic generation or an identical member roster.**
No extension context receives trade authority, and a missing extension source does
not invalidate an otherwise reproducible entry decision.

The CLI output `entry-attribution.json` binds the two input files and both native
owners by SHA-256. Its data session is **September 18**, not today's tape:

- 49/49 native entry flags and quality scores reproduced; 0 unreproduced rows.
- Seven themes have an existing Enter/Accumulate recommendation.
- Three of those seven already pass clean entry: Mag7, Crypto & Digital Rails,
  and AI Neoclouds.
- Three are blocked solely by the relative-strength boolean gate despite quality
  already exceeding .6: AI Semiconductors (.7), Memory/HBM/Storage (.8), AI
  Infrastructure (.8). The other existing safeguards do not block these copied
  calls when the isolated research input is changed.
- The remaining constructive theme, AI Software, fails another native condition;
  removing the relative-strength veto would NOT clear it.

Separate source-published median-member extension observations for that date are
1.77 for AI Semiconductors, 1.83 for Memory/Storage, .87 for AI Infrastructure,
3.33 for Crypto & Digital Rails and 3.11 for Cybersecurity. The first two are labeled
`extended`, Infrastructure `normal`, and the last two `stretched` by their existing
owner. Crypto Rails nevertheless has a clean-entry flag. This establishes that
these existing measures answer different questions; it does **not** prove that a
particular extension cutoff is a better entry rule or that a basket-wide entry
read establishes an individual CPU stock's executable setup.

### Correction to the previous explanation

A relative-strength gate does not measure distance from a price trend, but another
source may independently establish price extension. Therefore the previous phrase
'price stretch is not established' was too broad. The source-bound explanation now
says: **'Relative-strength filter blocks entry; it does not measure distance above
the price trend.'** The regression first failed on the old wording and now passes.
Both language versions preserve this scope. This does not erase genuine price-risk
evidence or promote a counterfactual into production.

### Verification and next decision

169 Python 3.14 tests and an overlapping 161-test Python 3.12 subset pass. The
original 10,584-case recommendation comparison still has zero verb changes. The
32 added attribution tests cover source/frame identity, mismatched dates, duplicate
identities, missing/nonfinite/bool inputs, missing last prices, contradictory
stored verdicts, regional/coverage mismatches and unchanged underlying inputs.
The existing hosted test job runs these cases; its trigger allowlist now also
covers the research module. Fresh original-versus-candidate browser proof again
covers 48 cells with no page errors or page-wide horizontal overflow.

Reproduce:

```sh
python3 research/sector_pulse/recommendation_reasons_20260921/audit_entry.py \
  --source-ref f98f57f4ec7c2cc30cbd90d9a7a759d5ea69144c \
  --output /tmp/theme-entry-attribution.json
```

The next model experiment is now specific: independently evaluate the .75 entry
veto and the .85 recommendation veto against own-price risk measurements on the
same decision-time population. Reuse the existing calibration and episode/evaluation
owners; no new ranker, factor score or trial ledger. Freeze alternatives before
reading their outcomes, distinguish hindsight-curated theme evidence from a valid
point-in-time universe, count independent episodes rather than repeated dates, and
report false-breakout/adverse-excursion/turnover/cost outcomes as well as leader
capture. **This single snapshot is neither that evaluation nor authority to change
live thresholds.** Current CI/review/publication gates remain open.
