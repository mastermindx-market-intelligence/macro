# PR-code coverage completion for the recommendation workflow

## Actual capability

Four complete recommendation, stock-entry, entry-comparison and pulse-clock suites
now execute inside the existing `conviction-profile` **code** job. Their previous
`engine-render-guards` **data** registrations remain unchanged. No new job, controller,
score, recommendation, entry permission, publisher or runtime was created.

The earlier statement that green PR CI exercised these suites was incorrect. Their
registration was data-only. Source semantic review and local tests remain evidence,
but the old PR result is not retroactively represented as this new code-gate proof.

Procedure: protected `Mastermind@a7d2b3049e5cdc523e91e61a6e9d70a1cb911157`, Skillpack1.0.1/bootstrap1.
Original carrier: `macro#7669`, branch `claude/theme-recommendation-reasons-20260921-sol`.
Prior semantic head: `2c28d950aa9448fc878bb64d92b228a8f1952bde`.
All application/model/template/compiled-page bytes are unchanged by this increment.

## Repair and recovery

The existing two-file partial patch was reconciled on its original carrier before
continuation. Current path/command/runtime preflights were successful. A bounded
source-only call completed the measured missing declarations; the previous compound
source/test/venv call was not replayed wholesale. No configuration change, provider
switch, sandbox override, package installation or new environment was used.

The native scope reader, with real site inputs materialized, exposed two additional
literal data references. All five measured missing declarations are now present:
`engine/theme_extension.py`, `research/LABEL_FALTERING_PHASE0.md`,
`scripts/calibrate_baskets.py`, `site/basketdata/baskets.json`, and
`site/basketdata/theme_extension.json`.

The newly added registration test initially mistook Python's `-m pytest` launcher
for pytest's marker filter. The repaired test checks selection arguments only after
the launcher, and includes a negative marker-filter case. This repairs a new test;
it does not remove a prior product assertion. Seven registration cases distinguish
valid coverage from data-only, omitted suite, deselection, marker filtering, missing
scope and missing dependency.

## Proof actually obtained

- The changed native command passed **312 tests** on Python3.12.
- All **eight actual pytest commands** in the existing code job passed: **871 total
  test executions**, including those312, with zero skips in the accepted runs.
  Combined command wall time was147.25seconds. These are not871 new tests.
- The first whole-job pass stopped at five missing committed mockup/capital inputs.
  Exact original-HEAD fixtures were materialized. The first five successful commands
  were retained; only the failed command and its remaining successors were executed.
  No original test, source fixture or assertion was changed to clear those failures.
- Native single-job scope inference: **580 concrete paths, zero uncovered**. Scope
  only widened. All223 existing job identities at this branch base remain, and the
  historical data job is semantically identical.
- The source/test trigger probes are reachable through the existing PR workflow.
- All model, scoring, pulse runtime, template and paired UI asset blobs are unchanged.

The JSON receipts bind the manifest hash and exact commands. Logs normalize trailing
whitespace only. Execution used the already installed Python3.12 environment; this is
NOT an isolated-dependency or current-main integration proof. Hosted CI will install
its declared dependencies under the existing job. No package was installed locally.

## Acceptance still owed

Current-main integration, exact-head hosted semantic/contract checks, review of this
CI/test-only delta, normal publication and real served-workflow acceptance remain
separate. The old UI/source review is reusable only for unchanged semantic blobs.
This coverage work does not retune stock-cycle, conviction, strength, risk or entry
rules and is not performance validation of the larger Prophet programme.

The generic hidden research mount collision has been released to the current
Chairman-assigned Semiconductor B programme on #7870, in #7669/comment5807781684.
That is a shell-only permission outside #app, not permission to overwrite this
feature's JS or mutate this branch. Preserve the additive union on later integration.

The options scope dependency was already merged as #7864 (`8db6896d`), so duplicate
#7866 was closed without claiming its additional test/evidence files merged or its
capped reviewer delivered approval. #7650 was already merged as `c32f6948`; do not redo it.

**MISSION_COMPLETE: false.** Source is BUILT_NOT_PROVEN pending its release proof.
