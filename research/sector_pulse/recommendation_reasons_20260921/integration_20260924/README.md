# Current-main generated-page integration — prepared candidate

This candidate binds the existing #7669 coverage revision and the exact main source below.
It does not merge another branch's product implementation or change trading policy.

The original native three-way checkout terminated on a promisor-object retrieval failure.
Its eight partial worktree deletions were restored from the exact source commit; no active
merge state or process remained. The qualified coverage commit was pushed to the same PR.
Rather than replay that failed checkout, this finite build consumes its already-computed
native merge-tree result through a separate native Git INDEX ONLY (not another branch).
All automatic, non-conflicting source changes are retained. Only the 121 generated page
conflicts are resolved through the incumbent basket_score, template, externalizers, asset
optimizer and write_page finalizer. Assets are content-hashed from the integrated source.

Every input is the exact embedded DETAIL on the pinned main page. Prices, scores, ratings,
status, buys, coverage and non-explanation data are asserted equal. Source observation and
original generation stamps remain unchanged. No collector, live data alias or new publisher
is involved. No source checkout/index/ref is altered by preparation. The resulting native
Git merge commit has the two actual parents; neither ancestry nor source history is lost.

All 121 page invariants passed: 1,917 member rows and 1,878 assessed rows are preserved.
The attached proof identifies every input/output hash, date and asset hash. Dates are the
stored dates, NOT a freshness claim. The reviewed runtime source blobs are unchanged.

This commit is a candidate for integrated tests, independent review and normal hosted CI.
Preparation is not source-release, merge to main, deployment or product acceptance.

Source head: 76cb5371ec77a711bdb3fe33469e73d0ecdc3d18
Main input: d7711a0a08db8008ffe5975bfb3e6b242e4c70bd
Native automatic tree: 1cd7565c9e6030ef132f6c05a7eefe9af7e07a9c
Resolved page tree before evidence: 6047a4542ec3d82701c57c8db794cf2cbf0a3490

## Observed integrated result

Native fast-forward adoption of ef10b39a9e56cf912e6ae2be98ad0937c4716616
completed with exit 0 on the original worktree. The failed three-way operation was
not repeated. Source worktree then clean. Integrated seven-suite qualification:
269 passed on Python 3.12, no skipped tests. The earlier coverage-native-job
871-pass result remains bound to its own revision and is not counted twice here.

Browser proof: 40 actual compiled-page states across five regions, dark/light,
English/Chinese, desktop/mobile. Keyboard opening, preserved focus/open state
after the existing render() refresh, all member/reason DOM content, stock links,
page-wide overflow and page errors were checked. Zero page errors. Screenshots
and their hash-bound receipt are committed here. Representative US light/mobile
and China dark/desktop captures were visually inspected. Internal mobile table
scrolling remains possible for long reasons; that is not called full visible-text
fit. No new stylesheet was authored and the previously reviewed UI bytes remain
unchanged. All quoted data dates remain their source dates; US is September21,
not a newly collected session. No authenticated/live-production result is claimed.

Independent review of the coverage change and integrated source, current-base
hosted CI, normal merge/publication and served-workflow acceptance remain owed.
The generic research mount is separately coordinated with Semiconductor B #7870;
this source does not add its shell hunk or duplicate its intelligence pipeline.
