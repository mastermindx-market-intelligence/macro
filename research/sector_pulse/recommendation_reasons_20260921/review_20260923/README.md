# Independent-review repair — current stock-entry workflow

## Source, independence and acceptance boundary

Native reviewer session `d8f2dc35-3019-4fb4-80a3-68932c42ae44` reviewed source
`79c291a40684869facb160a20341ba7340d96b04` independently of the Chat-native builder.
Its capped investigation returned no report; one same-session, tool-disabled
terminal return supplied **PARTIAL**, not approval. Both raw native result receipts
are retained here. Reported model: claude-opus-5, first-party included Claude Code;
`permission_denials` empty. No source edits or GitHub reviewer impersonation.

This repair consumes that review. It does not override its PARTIAL ruling;
independent current-source re-review and hosted CI remain owed. The older
`publication_20260923/` and original browser evidence remain historical baselines,
not proofs that the new template is already deployed.

## Findings and repairs

1. **Opened-state evidence was only two of eight combinations.** The current
   `opened/results.json` now binds all eight dark/light × EN/ZH × desktop/mobile
   combinations for each of five real regional pages: 40 actual compiled states.
   Each screenshot includes the opened panel and its availability heading, not
   just a closed disclosure. Sixteen additional, visibly labeled synthetic states
   cover invalid-summary failure and mixed unavailable/theme-blocked members.
2. **Dark/light treatments were not explicit.** Separate treatments and visual
   adjudication follow below. No new stylesheet or parallel material system.
3. **Timing cards still called RS extension a price extension.** They now consume
   the same existing `rolloverReasonText` formatter as the contested explanation.
   Scores, reason predicates and indicator formulas are not changed.
4. **Chinese explanations mixed untranslated English conditions.** Both existing
   basket consumers now translate every current native rollover condition,
   including histogram values and consecutive decline counts. Unsupported future
   Chinese translations are explicitly unavailable rather than silently mixed.
5. **Unavailable rows appeared as theme-blocked.** Missing score evidence now gets
   its own row reason before the global theme explanation; the global theme status
   still blocks admission exactly as before. Available rows retain their theme
   blocker. Eligibility, buy rows/order/cap and coverage populations are unchanged.
6. **Malformed input made the hero anchor dead.** Validation failure now exposes
   a real, focusable status target with the same ID. Native state restoration also
   preserves focus on this error target after re-render; no phantom details state.

Seven new tests failed on the reviewed source and pass after repair. Existing
assertions remain intact; the timing-helper test now includes its actual formatter
dependency instead of executing a dependency-free excerpt that no longer exists.
The remaining reviewer nits (unreachable/missing-data prose and conservative
status-based watch wording) are not promoted into unrelated model changes.

## DARK TREATMENT

Purpose: the entry checks are a quiet instrument panel within the existing dark
research screen, not a new green/red recommendation board. The existing `.panel`
material is dark, near-black; cool hairline separators group stock rows without
bright tiles. Ticker labels carry slightly stronger emphasis than muted field
labels; the reason is the primary reading column. The existing semantic warning
heading communicates missing source data, while each unknown row stays neutral.
Theme-risk text retains the site's existing regional semantic color convention.
The opened disclosure and keyboard/status target use the native visible focus
outline. No glow is added to the table or to a nonexistent buy opportunity.

The actual dark-EN desktop and mobile, and dark-ZH desktop and mobile captures
are independently available. Desktop retains the broad two-column scan; mobile
retains both columns with wrapping reasons and at least40px activation targets.
The status-only failure has no empty table skeleton or fabricated counts. This
is a deliberately sparse analytical read, not a claim of a full-page redesign.

## LIGHT TREATMENT

Purpose: an opaque white research sheet on the existing cool page canvas. The
panel's subtle border, rounded edge and light material separate it from the page;
rows use gray hairlines rather than dark-theme glow or translucent black panels.
Dark blue-gray text, subdued labels and generous row spacing keep twelve repeated
unknown rows legible without implying twelve negative signals. The warning header
and visible keyboard outline remain semantic, not decorative colored score tiles.
Both light-EN and light-ZH are captured at desktop and mobile widths; the Chinese
mobile case wraps naturally and keeps the stock and explanation columns readable.

The invalid-summary state becomes one small neutral, focusable explanation; its
absence of rows is intentional, not missing screenshot coverage. Mixed coverage
keeps the distinct unavailable row next to the actual theme-blocked row, matching
the counts. Existing global assistant chrome is retained in the screenshots; this
slice does not claim to redesign or remove that floating site-wide control.

## Real data versus synthetic states

`real_compiled`: exact current generated file bytes, current stored source dates
(Sep21 for US/Canada; Sep22 for China/HK/International), original data preserved.
The all-unavailable US header is a real stored-input state, not a fabricated market
opinion. No stock assessment was restored by this UI repair.

`validation_failure`: only the runtime summary is intentionally made inconsistent.
The page shows a visibly labeled synthetic review fixture, not real coverage.

`theme_blocked_missing_fixture`: the unchanged admission owner evaluates two
explicitly fictitious members, one unassessed and one blocked by theme risk.
The capture is visibly labeled synthetic; no individual stock advice or production
claim is made. Both synthetic cases run through all eight visual combinations.

`verify_opened_states.py --out <directory>` uses native Chromium against the actual
compiled files, with live/API reads explicitly unavailable. It verifies the hero
activation, all member reasons, actual stock links, open/focus preservation and no
page-wide horizontal overflow. Expected missing optional-feed responses are not
misrepresented as a live-feed test. Authenticated/production acceptance is absent.

## Field and artifact integrity

`review-repair-page-proof.json` covers all121 output pages and1,917 member rows.
The one-off refresh used existing Jinja, page writer, externalization and asset
finalizer interfaces, not a new production builder. Before writing, every source
page was parsed and validated. For every page, status/buys/uncovered and all
non-explanation data were compared; all observation dates and original generation
stamps were retained. No collection, provider call, private-data fetch, new quote,
score, threshold, rank, sizing or membership change occurred.

Every current screenshot and page digest is bound in the new manifest. All existing
risk/entry/price functions outside the disclosed reason projection remain frozen.
The older immutable research studies are not rerun or promoted by these fixes.

Parent mission remains incomplete. Independent repair acceptance, current-head
hosted CI, source release and real publication are distinct outstanding gates.
