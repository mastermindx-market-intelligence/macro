# OTA W4 — Evidence-System Upgrade — Build Spec (pre-registered design)

**Program:** Oracle Turn Asymmetry ([masterplan](../ORACLE_TURN_ASYMMETRY_MASTERPLAN_BY_FABLE.md) §W4). Authored by Fable 2026-07-06 after a census of the parallel reversion-discovery program's builds. **Deduplication ruling:** the parallel program already shipped the reversion gate/gauntlet (`ORACLE_REVERSION_GATE_PREREG.md` + Amendment 1), the P0 forward ledger (`scripts/oracle_reversion_forward_ledger.py`, nightly), the P1 display sidecar (`scripts/oracle_reversion_state.py`), and froze the live promotion floor (`ORACLE_REVERSION_PROMOTION_PREREG.md`). W4 builds ONLY what remains.

## Supersession ruling (recorded, binding)
Masterplan W4.1 (modern-regime track for the 63d apparatus) and W4.3 (era-gate insufficient-history fix for the 63d apparatus) are **CLOSED — SUPERSEDED** by the operator-ratified reversion reframe: the 63d promotion apparatus is the wrong ruler for this program's signals and will not be polished. The reversion gauntlet already embodies the modern/OOS discipline (holdout split, regime scoping, tier-M sub-splits, coverage laws in the brainstorm pack). The masterplan status log records this.

## W4.a — P2: the reversion promotion scan (armed-not-fired)
Build `scripts/oracle_reversion_promotion_scan.py` EXACTLY per `ORACLE_REVERSION_PROMOTION_TRACK_DESIGN.md` §4.3 + §5 and the FROZEN floor in `ORACLE_REVERSION_PROMOTION_PREREG.md` (constants are not restatable here — the prereg is the source; do not re-derive or tune):
- Reads registry `reversion`-block compounds + the P0 ledger (`data/oracle/reversion_forward/<id>.jsonl`, matured rows only; regime-scoped n for single-regime signals per the prereg).
- `base_rate` = unconditional trailing 21-session win-rate on the same universe (computed as the prereg defines).
- Calls `engine/neuralweb/constitution.py::grant_authority` VERBATIM (never fork the gate). Writes `data/oracle/reversion_promotion_queue.json` (candidates for Fable/operator adjudication — **NEVER auto-promotes**) + `data/oracle/reversion_authority.json` (written ONLY by this deterministic scanner) + a `data/neuralweb/governance.jsonl` event per PROPOSED tier transition (append via the existing governance writer, engine-lane-serial law).
- Lapse law (Article 3): de-escalation proposals when silence/CI-decay conditions in the prereg hold — proposals only, same queue.
- Nightly step appended at END (after the current last step), loud-error pattern; dag.yml declaration; synapse registrations for queue + authority (infrastructure tier) and verification that state/ledger artifacts are already registered (add if the parallel session did not).
- **Armed-not-fired law:** with today's ledger (n≈0 matured), the scan must run green and print "no candidates — accruing (earliest projected per compound)". A refuse-early guard is NOT needed (unlike kernel batches) because the scan never auto-promotes; but a self-test mode (`--selftest`) with synthetic ledger fixtures must demonstrate: grant path, refuse path (n<floor), lapse path, and the never-auto-promote invariant (queue written, authority file UNCHANGED until a human-ratified flag flips — ratification mechanics: authority.json changes only when the queue row carries `ratified_by` set by a HUMAN-committed edit, checked by the scanner before applying).
- P1 sidecar (`oracle_reversion_state.py`) consumes `reversion_authority.json` if present so ratified tiers surface — verify its current code tolerates/reads it (the P1 header says authority belongs to P2); wire additively if absent.

## W4.b — Power-aware verdicts + kill-requeue (reversion apparatus)
1. `scripts/oracle_reversion_screen.py` outputs gain, on every leg-verdict line: achieved-power context as **MDE@80% (α=0.05, normal approx from observed per-entry σ and n)**; and a new printed class **UNDERPOWERED-ACCRUING** when a candidate FAILS legs on CI/placebo grounds while its point estimates are all in the passing direction and power<50% at the observed effect. This changes REPORTING only — gate thresholds are frozen and untouched.
2. `data/oracle/reversion_kill_requeue.jsonl` (append-only, keep-first by compound_id::killed_at): every UNDERPOWERED-ACCRUING fail writes a row `{compound_id, killed_at_asof, n_at_kill, point_estimates, requeue_at_n: 2×n}`. The promotion scan (W4.a) checks it and prints a "requeue eligible" line when a compound's screenable n (fresh screen on current panel) ≥ requeue_at_n — a REMINDER for the operator/Fable to re-screen; never auto-rescreens (counted-trials law: a re-screen is a new counted trial).

## W4.c — Cross-market replication interface
`research/oracle_asymmetry/W4_CROSS_MARKET_INTERFACE.md` (authored by Fable in this PR, not the builder): the registered interface any CN/HK replication must satisfy to count as out-of-universe evidence (same-grammar spec, market-local panel, reversion ruler, correlated-echo disclosure — cross-market event correlation printed next to any agreement claim). EXECUTION deferred to the china-alpha / hk-canada programs (domain owners); logged as deferred, not silent.

## Prohibitions
No changes to gate/floor constants anywhere (all frozen in their preregs); no changes to the 63d apparatus (superseded, left as-is for its own consumers); no LLM in any path; no auto-promotion of anything; grant_authority reused never forked; additive-only nightly/synapse/dag wiring with all local guards run before commit (synapse registry, signal-bus count+regen, dag conformance, validated-claims, pytest).

## Amendment log
- **2026-07-06 — Adjudicator countersign (Fable), post fix-round (1cbff1b999).** Audit found 1 blocker (scan declared in dag.yml but never invoked — dag conformance cannot see Python _step_ calls; now Step 16) + 3 majors (nightly duplicate governance events → dedup vs pending queue; ratification applied queue rows verbatim → now re-runs grant_authority on CURRENT live evidence at apply time and refuses/caps; UNDERPOWERED-ACCRUING fired on any >0 point estimate → now requires the FROZEN gate bars as the passing direction). All fixed + selftest extended (silence AND ci-decay lapse branches). Independent checker: clean, exact Wilson-gate agreement on grant/refuse/lapse fixtures. ACCEPTED-WITH-NOTE: the money-path gate's base_rate is sourced from the P1 display sidecar (PIT-computed; step order 11c<16 safe); REQUIRED INVARIANT: on missing/stale sidecar the scan must refuse grants (fail-closed) — verified present. LESSON (standing): dag-conformance does not cover intra-script Python step wiring; every new nightly step needs an invocation test.
