# OTA W3 — Grammar v2 + Steered Sequence Rounds — Build Spec (pre-registered)

**Program:** Oracle Turn Asymmetry ([masterplan](../ORACLE_TURN_ASYMMETRY_MASTERPLAN_BY_FABLE.md) §W3). Authored by Fable 2026-07-06. Grammar additions are REVIEWED changes (Oracle constitution §II); evaluator semantics are part of trial identity → `GRAMMAR_VERSION` 1.1.0 → **1.2.0**. All screening in this wave runs at the **reversion ruler** (`oracle_reversion_screen`, frozen gates) under the parallel discovery program's loop discipline (`ORACLE_REVERSION_HANDOFF.md`): ingest-dedup, screen legs 1–4, gauntlet legs 5–6, **mandatory redundancy audit** (≥15% novel entries vs every published base signal, else REDUNDANT — not published), publish only on Fable adjudication.

## Scope rulings (recorded)
- **BUILD:** `sequence` + `cooldown_sessions` primitives (§1). These unlock the handoff's under-explored families (V-bottom, MTF-confluence, washout→turn ladders) that simultaneous-AND cannot express.
- **DEFERRED with reasons:** cross-tier join (panel_m store gap + tier-M market-timing laws — revisit with the discovery program); `exit_on` evaluation variants (the reversion time-exit is the frozen ruler; W0 already measured exit variants descriptively); member-breadth panel columns (heavy compute on the render path — violates render-budget law without its own design); vol-compression family (no realized-vol panel column exists — logged as a new-column candidate, masterplan open queue).

## 1. Grammar v2 (engine/oracle/compounds.py — additive, reviewed)
1. **`sequence` rule:** `{"sequence": {"first": <rule>, "then": <rule>, "within_sessions": N}}` — fires on day t iff `then` evaluates true at t AND `first` evaluated true on ≥1 day s ∈ **[t−N, t−1]** (strictly before t; both causal on the node's own rows; `first` may be any v1 rule incl. nested all/any/episode_event; nesting `sequence` inside `sequence` is REJECTED at validation in v1.2). CAUSALITY LAW: the `first` lookback must never include t (else simultaneous-AND masquerades as sequence) — unit-tested with a fixture where first-and-then are true only on the same day (must NOT fire).
2. **`cooldown_sessions`:** optional top-level spec int — after a fire on a node, later fires on that node within N sessions are suppressed (deterministic left-to-right scan). Applies at `get_entry_dates` so screens/gauntlets/ledgers all inherit it.
3. `GRAMMAR_VERSION = "1.2.0"`; validation errors loud; v1.1 specs must evaluate BYTE-IDENTICALLY under 1.2 (regression fixture: A15's entry set unchanged apart from the params-hash version component). Update the brainstorm-pack grammar printout.

## 2. The registered round (Fable Tier-0 specs — frozen at commit; screening may not add/tune specs)
Eleven mechanism-first specs, families F-SEQ / F-DIP / F-DEST, all full-history columns, universe tier-s, screened at the reversion ruler with the frozen gates. Every screen appends to the trial ledger (mining legal because counted). JSON below is the registered batch (`research/oracle_asymmetry/w3_round_batch.json` is the machine copy the builder ingests — byte-equivalent to this list):

| id | entry_rule (compact) | cooldown | mechanism |
|---|---|---|---|
| SEQ_WASHOUT_THEN_KXD | sequence(first: washout_w>0; then: all[stochrsi_w_k gt value_col stochrsi_w_d, stochrsi_w_k lt 40]; within 15) | 10 | washout, THEN the K-over-D turn from oversold — the operator's ladder ordered causally |
| SEQ_VBOTTOM_ACCEL | sequence(first: vel_1w lt −0.015; then: accel_z crossed_above 0; within 10) | 10 | sharp weekly downdraft, THEN acceleration flips positive (V-bottom) |
| SEQ_CAPITULATION_PERSIST | sequence(first: ret lt −0.03; then: persistence gt 0.55; within 10) | 15 | one-day capitulation, THEN RS persistence resumes |
| SEQ_MTF_CONFLUENCE | sequence(first: stochrsi_w_k lt 20; then: accel crossed_above 0; within 8) | 10 | weekly oversold, THEN the daily velocity-spread flip (MTF confluence) |
| SEQ_RELIEF_THEN_TURN | sequence(first: hy_oas_chg_10d crossed_below 0; then: all[washout_w gt 0, stochrsi_w_k gt value_col stochrsi_w_d]; within 10) | 10 | credit relief FIRST, then the washout turn (orders the known winners causally) |
| SEQ_TLT_RELIEF_WASHOUT | sequence(first: tlt_ret_10d crossed_above 0; then: washout_w gt 0; within 10) | 10 | rate relief, THEN sector washout (rate-sensitive bottoms) |
| SEQ_DIP_RESUME | sequence(first: all[episode_event(in,onset,same,15,1), ret lt −0.01]; then: accel_z crossed_above 0; within 8) | 10 | dip inside an active IN rotation, THEN re-ignition |
| DIP_IN_EPISODE_K40 | all[episode_event(in,onset,same,15,1), stochrsi_w_k crossed_below 40] | 10 | pullback trigger while the sector's own IN rotation is active (F-DIP: condition≠trigger, B1 blindspot family) |
| DIP_IN_CONFIRMED_RET | all[episode_event(in,confirmed,same,20,1), ret lt −0.015] | 5 | −1.5% down day inside a CONFIRMED rotation — the "dip-buying inside confirmed episodes" the assessment flagged as unmeasured |
| DEST_OPP_OUT_LEADER | all[episode_event(out,onset,opposite,10,2), rs gt 0, stochrsi_w_k lt 60] | 10 | opposite complex bleeding, this node ALREADY leading and not overbought — the destination of displacement (vs A15 which buys the washed-out node) |
| DEST_OPP_OUT_TURN | sequence(first: episode_event(out,onset,opposite,10,2), then: accel_z crossed_above 0.5; within 10) | 10 | displacement FIRST, then this node accelerates — routed inflow proxy |

Pipeline per the handoff: ingest (dedup vs registry) → reversion screen legs 1–4 (`--dry-run` compounds dir) → 6-leg gauntlet for passers (time-exit frozen) → **redundancy audit** (entry-set overlap vs EVERY published base signal via `get_entry_dates`; ≥85% containment in any single one = REDUNDANT, do not publish) → results table with per-leg numbers + MDE/UNDERPOWERED-ACCRUING classes (W4.b) for fails. **Publication of passers happens ONLY in the Fable adjudication commit** (registry `reversion` block + `ORACLE_REVERSION_VALIDATED.md` row, per the handoff's format, re-verified numbers only).

## 3. Deliverables
`engine/oracle/compounds.py` v1.2 (+tests: sequence causality fixture, same-day non-fire, nested-sequence rejection, cooldown determinism, v1.1 regression) · `research/oracle_asymmetry/w3_round_batch.json` (the 11 specs, frozen) · `research/oracle_asymmetry/W3_ROUND_RESULTS.md` (per-spec legs 1–4, gauntlet legs for passers, redundancy overlaps, verdict classes) · trial-ledger rows for all 11.

## Prohibitions
No gate/floor changes; no spec additions or threshold tuning after this commit (the 11 are the round); no publication without redundancy audit + Fable adjudication; no tier-M in this round; grammar change reviewed via the workflow's Opus audit before merge.

## Amendment log
- **2026-07-06 — Adjudicator countersign (Fable), post fix-round (0a3ff8c600).** Audit blocker was REAL and outcome-reversing: `oracle_ingest_brainstorm` silently dropped `cooldown_sessions`, so run 1 screened cooldown-free — the apparent DEST_OPP_OUT_LEADER pass (asym 1.54) was an overlapping-fires artifact and is VOID (with cooldown: asym 1.09, FAIL); conversely SEQ_TLT_RELIEF_WASHOUT strengthens under dedup and is the round's sole 6-leg PASS (n=745, WR .672, asym 1.747, ret +2.37%, OOS .689/+3.71%, placebo +2.37%>p95 +1.16%). Calendar-vs-session cooldown bug also fixed (panel-positional gaps). A15 v1.1 byte-identity pinned exact (2367 fires). Builder rebuttals of the DIP recompute delta (auditor applied an exhausted_date filter not in the grammar) ACCEPTED — the grammar is the spec. **Publication ruling: SEQ_TLT_RELIEF_WASHOUT is PENDING-PUBLICATION — the fix-round redundancy audit covered only the 3 registry reversion-block signals; the handoff law requires overlap vs EVERY published base signal (10). Publication happens only after the full-10 check clears (<85% containment in each).** Round verdict for the rest: 9 substantive FAILs (asym gate binding on the F-SEQ family — sequencing raises entry counts and WR but not upside/downside ratio, EXCEPT when the macro-relief leg leads), zero UNDERPOWERED-ACCRUING. F-DIP verdict: dip-buying inside active/confirmed rotations FAILS at the reversion ruler at tier-S (B1's unmeasured family is now measured: NULL-at-gate).

- **2026-07-06 — PUBLICATION (Fable):** SEQ_TLT_RELIEF_WASHOUT published to the registry (reversion block, re-observed numbers only) + ORACLE_REVERSION_VALIDATED.md after the FULL 10-signal redundancy audit cleared (max containment 22.6% A15; union-novel 65.7%; tier-M disjoint). The other 10 round specs remain screened-fail of record in W3_ROUND_RESULTS.md + the reversion trial ledger.
