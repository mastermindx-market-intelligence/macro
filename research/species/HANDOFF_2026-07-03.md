# Setup-Species Program — Session Handoff (2026-07-03, post W0 Stage A)

> For the next orchestrator session (Fable role). Read this, then the canonical doc
> `research/SETUP_SPECIES_MASTERPLAN_BY_FABLE.md` — its §1 (Constitution), §1.6 (graveyard)
> and §7 (waves/governance) are FIXED INPUTS: runners may not soften gates or redefine
> metrics. Red-team provenance: `research/species/MASTERPLAN_REDTEAM_RECORD.md`.

## Where the program stands

Masterplan merged as macro#1097. **W0 Stage A is COMPLETE (6/6 agents):**

| wave | PR | status | delivered |
|---|---|---|---|
| W0.1a grading spine | #1100 | MERGED | `engine/grading.py`: `fwd_mfe_H`, terminal-state partition (`terminal_state()`, `clean15_126`/`clean8_21`, straddle→stop-wins), `cushion_incidence()` (competing-risk, all-fires denominator), PIT membership wired into `as_of_panel`, `SPINE_HORIZONS=(5,10,21,63,126)`. Pure additions; callers untouched. |
| W0.3 species registry | #1103 | MERGED | `engine/species_registry.py` + `data/species/registry.json`: 17 species seeded truthfully (6 validated incl. S1 COILED graded_bonus, T1–T4 gate_weight; 11 phase0). Experiments-tab additive mirror (idempotent, curated entries preserved). `research/species/README.md` prereg/report convention. |
| W0.4 cohort metrics | #1104 | MERGED | `engine/cohort_metrics.py` (display-only): peer_washout/reclaim/macd_turn %, Rubber-Band Score, coverage law (≥70% else null), within-cohort RS-rank daily series → `data/cohort_metrics/` (the S7 dependency). Runtime 1.9s. Live reading at ship: mean peer_washout 0.71. |
| W0.6 data plane | #1107 | MERGED | `collectors/massive_stock_day.py` + resumable backfill script (smoke-tested: 12,664 tickers/day; earliest available 2025-01-02); revisions `_one()` + eps dispersion fields (NOTE: revenue-revision drift structurally unavailable in yfinance — documented, not faked); `scripts/backfill_edgar_quarterly.py` (10-name validation done, repurchases populated); Polygon news tier-1 500 names live behind runtime budget check. |
| W0.5a regime_vector | **#1102** | **OPEN — REVIEW REQUIRED** | `engine/regime_vector.py` + run.py seam + coherence registration + `data/regime/regime_vector.parquet`. 37/37 tests. |
| W0.7 archetype v2 | **#1105** | **OPEN — REVIEW REQUIRED** | 13-bucket `_archetype()` (8 anchored), backward-compatible; PIT history parquet (19,487 rows, 1,331 tickers, FY2009–2025); phase-0 report `research/species/W0_7_ARCHETYPE_REPORT.md` (honestly caveated: fires span one 17-day rate-relief window). |

## Do next, in order

1. **Review + merge #1102.** One adjudication: the agent shipped `rate_pressure` panic
   escalation at radar sub-score ≥ 68.0 (caution floor), but masterplan §3.4's letter says
   "≥ its LOUD tier" — which is 78.0 (elevated). Ruling options: (a) change to 78.0 to
   match the frozen spec (default — spec letter wins; stamped definitions are irreversible
   ledger history), or (b) keep 68.0 AND amend §3.4 with a §8 status row recording the
   change before merge. Do not merge with the discrepancy unrecorded. Two minor flags are
   fine as shipped: JSON-path read for real10y_chg63; subsector/donor axes null until
   Stage B.
2. **Review + merge #1105.** Check backward compat (4-arg call preserved), precedence
   determinism, and that the phase-0 report proposes no gates (it shouldn't). The
   "cyclical = sector-keyed" deviation is reasonable and documented.
3. **Run two time-sensitive backfills** (agents built the machinery; runs need creds):
   - `python -m scripts.backfill_massive_stock_day` (~4h; needs `MASSIVE_S3_*` in .env).
     **URGENT — the entitlement window ROLLS; delay permanently loses the earliest days.**
     ~~After first full run: add `--dirs massive_stock_day` to the CI publish_r2 step.~~
     **CORRECTED 2026-07-03 (coverage-gap postmortem):** do NOT publish this dir from
     CI — runner checkouts hold only the two committed JSON files (parquets are
     gitignored), and a CI sync would clobber the R2 store (publish_r2 now refuses
     data-dir trees under 100 files). Publish from the store host (Mac main checkout)
     with `R2_*` creds: `python -m scripts.publish_r2 --dirs massive_stock_day`.
     The 2026-07-03 initial-backfill hole + the smoke-test resume-state poisoning are
     fixed (v2 gap-aware `processed_days` state); coverage is now tripwired by
     `scripts/audit_massive_store.py` (collect gate) + audit_r2's coverage probe.
     Entitlement floor probed 2026-07-03: **2021-07-06** (rolling ~5y, not 2025-01-02).
   - `python -m scripts.backfill_edgar_quarterly` (~9 min; run `--max 100` first). Then set
     S10/S11 `gating.come_back_on` to the completion date in `data/species/registry.json`.
4. **Dispatch W0 Stage B — ONE serialized PR per ledger** (NOT parallel: keep-FIRST
   append-only ledgers make merge-order damage permanent). Each PR threads together: the
   new grading columns + the regime_vector stamp (§3.4 per-market rules: US primary on US
   ledgers; own-market primary + `us_regime_vector` context column on CN/HK/CA;
   `vector_asof`+`staleness_hours` on asia-lane stamps) + species_id/archetype fields.
   Spec = masterplan §5.1 sub-tasks 2–5 + §7 Stage B. Order:
   a. `track_record` — FIRST fix storage (parquet is gitignored → CI clean wipes it; migrate
      to git-tracked append-only or un-ignore with size tripwire), THEN schedule
      `scripts/build_track_record.py` in daily.yml + sentinel git-add.
   b. `grade_us_board` — delete private forward-return/MAE code, route through
      `grading.forward_metrics` with before/after parity check; add 63d lane; fix the
      survivorship hole via `grading.resolve_series`/as-of panels.
   c. `board_ledger` (HK/CA) — grade() inner loop consumes forward_metrics; schema-union
      stamps; suspension rule preserved.
   d. `china_standout_track` — new axes as CN-NATIVE computations on `_t1_fill` (not
      inherited); new `_slice_table` stratifier columns (species_id, archetype, regime).
   e. `qledger` — `register_batch()` (its register() is O(file) per call; ~2,800 calls/day
      already) + migrate `_fwd_ret` to grading fill semantics with the asof→next-bar
      discontinuity STAMPED.
5. **Stage C: W0.2 near-miss capture** — only after Stage B lands for its target ledgers.
   Closed taxonomy = masterplan Appendix A (10 reasons; extension requires §8 row).
   Scope: US signal_gate near-misses (new), CA watch-strip appends (new), HK add `knife_z`
   to the already-appended schema (do NOT re-append — keep-first silently drops).
6. **W1** (after Stage B/C): S13 US reversal-sleeve phase-0 (cheapest science — existing
   deep panel, kill criteria pre-registered in §4); S3 retro-derivation with the Opus
   reconstruction check (fallback → W3); S6 OOS-basket phase-0 (interaction-marginal
   primary); S1 interim widening (already-priced unmapped names only); tailwind lead-lag
   inversion (FRONTRUN N3) with before/after lead-time; regime-directive display wiring.

## Standing context the next session must not lose

- Delegation: Fable = orchestrator/adjudicator; Opus = preplans/red-teams/reviews; Sonnet =
  code; Haiku = mechanical. Background agents in ISOLATED worktrees; branch off origin/main;
  small PRs; squash-merge same-day (standing approval). W0.x commit prefix convention.
- Every wave: `research/species/<wave>_PREREG.md` BEFORE runs, `<wave>_REPORT.md` after,
  §8 status row (`date | wave | verdict | PR# | artifacts`) in the masterplan.
- The owner's thesis (the program's spine): edge = entry quality (bottoms, rotation
  beginnings, mean reversion), safety net via low drawdown/dead-capital; multi-horizon
  grading with frozen horizon classes; NEVER return-alpha verdicts. HK adversarial by
  default. Hard gates banned (bonuses via blend_sorted). Graveyard §1.6 binding.
