# W1 S6 Phase-0 Pre-Registration — Failed-Fire Fuel (OOS basket panel)

*2026-07-04 · Setup-Species program §4 S6, §7 W1 · registered BEFORE the run
(this file's commit precedes the report). Expectations low — the registry says
so, and a clean kill is acceptable and pre-authorized.*

## Species under test

**S6 v1.0 — Failed-Fire Fuel.** Horizon class **rotational** (frozen; promotion
keys on `clean8_21`). Mechanism: a name whose own trigger stopped out ≥2 times
in the trailing 180 calendar days may be LATE-STAGE washout — serial failure as
a seller-persistence marker, i.e. reversal fuel rather than a blacklist.

**Evidence status, restated exactly:** the failed2 inversion is sign-stable but
IN-SAMPLE ONLY (wave-1 stocks panel: +1.8pp base3d / +3.2pp m2d_s3d — below the
+5pp bar). The graveyard already holds the OPPOSITE construction ("cried-wolf
veto — inverted in-sample"); this phase-0 is the honest OOS read of the
inversion, on the panel the durable-bottom program validated as its decisive
OOS set. The §5.2 near-miss ledger encodes the same hypothesis from birth
("rejection ≠ blacklist") — S6 is its first formal test.

## Construction (inherited VERBATIM — nothing re-tuned)

- **Panel:** the wave-2 OOS basket panel (`data/baskets/ohlcv/`, 2,336 names,
  eval 2015-01→, min 1,000 bars) — regenerated by the unmodified
  `research/entry_timing/wave2.py --panel baskets` harness (its wave-2 run was
  adversarially audited; a date-index leak was fixed BEFORE those runs).
- **failed2:** wave-1 definition verbatim (`wave1.py:735`): own trigger stopped
  out ≥2 times in the trailing 180 calendar days at fire time.
- **COILED:** the validated cohort-washout state, as computed by the harness
  (`feat["coiled"]`), unchanged.
- **Fires/fills/outcomes:** the harness's next-bar fill and barrier races,
  unchanged. Rotational primary `clean8_21` (+8% before −5% within 21 bars from
  the fill) is recomputed per fire from the same panel prices with the shared
  `engine.grading` constants — the wave-2 outcome columns are positional-window
  and S6's declared class is rotational (§1.1 horizon-class law).

## Registered trials (family `s6_failed_fire_fuel`, m=2)

The two wave-1 trigger variants, exactly as the in-sample evidence was split —
no new variant, no threshold search:

| trial | trigger variant |
|---|---|
| 1 | `base3d` |
| 2 | `m2d_s3d` |

Proportion primary → **no DSR** (§1.2: proportions cannot be DSR'd); the
controls are the episode-clustered p-value + BH-FDR across the W1 registered
family (m taken from the trial ledger: S13's 2 + S6's 2).

## Primary verdict (registered)

**Interaction marginal on the OOS basket panel:** within COILED fires,
`failed2=True` vs `failed2=False` spread on **clean8_21**, per variant, with
stop-out (stop5), dead-money, and the positional grid printed beside it
(context, never the verdict). Requirements to call the inversion REAL:

1. clean8_21 spread > 0 (failed2=T side higher) in BOTH variants;
2. episode-clustered p (circular block bootstrap over calendar-month episode
   blocks, B=2000) with BH-FDR q ≤ 0.10 across the W1 family;
3. n ≥ 300 per side within COILED;
4. both time halves sign-stable;
5. per-name majority (>50% of names with ≥2 fires on each side agree in sign).

**Secondary (context only, cannot promote):** standalone failed2 T/F over ALL
fires — the raw inversion without the COILED interaction.

## Pre-registered outcome paths

- All five requirements met in both variants → S6 advances to display-chip
  candidacy (ladder §1.3; nothing ships from this run).
- Spread ≤ 0 or sign-unstable or q > 0.10 → **S6 CLOSED permanently in §8**
  (clean kill; the graveyard gains the inversion beside the veto).
- `n_failed2_T < 300` within COILED → **UNDECIDABLE at the registered floor**:
  S6 stays `phase0` with `come_back_on` keyed to the near-miss ledger accruing
  (Stage C now captures every rejection with reasons — the denominator grows
  forward). No threshold loosening; the floor is the floor.

## What this phase-0 does NOT do

No gates (assert-no-gate law §1.5 — S6 is a *stratifier* candidate, not a
gate). No board wiring, no sizing, no return-axis verdicts. Survivorship: the
basket panel is the ~2014+ full-OHLCV store — carries the same
current-membership bias as every basket study; printed, not fixed here.
