# GMI Meta-CEO - shared consumer disposition

Date: 2026-09-24. Parent operation `gmi-thematic-research-meta-ceo-20260924-001`, Macro Draft/HOLD #7886. Present Chairman delegation retains GMI programme integration and scope/acceptance judgment with the Meta-CEO. This disposition answers existing Semiconductor receiver request #7780/comment5811066300 and its binding addendum5813739560; it does not create a new runtime operation, assign a replacement Fable, or take source-writer custody.

## Outcome and decision

An investor must be able to reach the appropriate thematic or company research, receive a useful source-qualified economic explanation even when optional valuation/guidance is unavailable, and inspect exactly which observation could change that explanation. The machine must preserve populations, units, business versus knowledge clocks, lineage and permission. No source taxonomy becomes a new canonical theme or basket merely to make navigation work.

SELECT OPTION A for a bounded additive shared-consumer build-out of requested items 1-4 through the incumbent Semiconductor foundation owner on #7870. This is a same-carrier source-scope ruling, conditional on its actual writer, admission, permission, dependency and effect fences. The existing principal controls integration and review; no fresh receiver, worker, quota, budget or release permission is minted. Routine engineering uses the existing admitted fabric rather than the principal personally doing all edits.

WHY retain the incumbent Fable: the live request concerns architecture-sensitive shared route/client, existing independent vertical composers and private/source-owner boundaries already under that principal's integration continuity. No competing principal or common-library writer is needed. This rationale does not prove runtime liveness or authorize a new Fable invocation. B2 work stays bounded and must not displace the two already-owed Semiconductor positive witnesses.

The closed profile/reported-economic-context requirement in item 5 remains a separately reviewed same-owner extension. It is not implemented by accepting a broader string or by relaxing the existing Semiconductor witness gate. Versioned curation changes remain under the previously qualified v1.1/AM3 owner and acceptance process.

## Current source evidence, not production proof

Inspected #7870 head `3e3a7956d014b8c50be7b197fdbea847cecfa641`:

- `app/theme_research.py`, blob `f3c70ffd67ccab5225189ec1a3106bd1ea38c11e`, query fields at lines145-160: only `hbm_packaging` and `sic_gan_specialty` accepted; transport imports the Semiconductor composer directly. Lines259-275: `load_authorized_owner_bundle` raises `PrivateStoreUnavailable`; a synthetic injected bundle is not its production binding.
- `engine/market_ontology/semiconductor_theme_research.py`, blob `fb86c5cc53afb4d4150349a798212c35d7f2fcd9`, `_build_economics` at lines792-895: the positive management comparison requires earlier guidance, an actual and later outlook. Absence produces `management_sequence_missing`. This is a legitimate specific witness requirement, not a universal gate on all useful reported economics.
- `contracts/theme_graph/curation_assertion.v1.schema.json`, blob `ff3928f0c54aa164ef8283d9da45af67e6a0d971`, lines145-174: current quantity/basis grammar does not provide an accepted signed/exact/qualitative extension. No caller may coerce losses, cash movements or unknown rights into positive physical quantities.
- `engine/theme_graph/rights.py`, blob `758937246e9a07ac150ba65123ebfa876bbc66c0`, states unknown-family fail-closed ownership. In the route at lines204-217, `family is None` currently keeps the assertion. This is an exact source discrepancy, not proof of a production disclosure.
- Current receiver checkpoint blob `2712c3e4126f0e96c59d987285fe6addc45b486e` and request5813739560 report private binding, slice scoping and historical identity limits. Their graph/identity census is attributed receiver evidence, not independently re-executed here.

The fresh source read does not establish deployed behavior or the absence of newer independent work. The receiver must reconcile its current head and pending effects before editing. Do not reset another writer or replay already integrated changes because this snapshot is older.

## B2: four bounded common-consumer hooks

### 1. Closed route dispatch

Use one explicit registration binding anchor, permitted slices, exact response schema/version, composer and evidence selector. Registration is trusted source/configuration under the existing owner; request/model text may choose only a registered value, never a callable/import/path/URL. Preserve authentication before parsing/lookup and existing private response/error semantics. Unknown or mismatched anchor/slice/profile combinations fail visibly without disclosing protected content. Do not create another router service or runtime registry database.

Keep the existing Semiconductor registration and witness behavior. Other domain owners supply their accepted composer contract; the shared writer serializes only the registration change after the dependency is available. A missing composer must not break or silently replace the existing one.

### 2. Configuration-driven reusable mount

Bind the generic mount to existing trusted render context/configuration for its exact anchor, slices, labels and allowed schema. Preserve the one shell/aggregator include and #7669 custody. Escaping, duplicate-mount behavior and unconfigured/missing-state behavior require tests. A per-vertical partial remains a lawful interim when already accepted; do not proliferate a second client or copied authorization logic.

### 3. Multi-anchor entry where an existing anchor is admitted

The Theme Tracker may compose several registered summaries through the common route/client. Failure or unavailability of one authorized domain should not erase unrelated available summaries. A missing basket must not be solved by creating a fake basket, node or crosswalk identity. A source-local subject and a company-first journey remain separately governed follow-on requirements, not automatically admitted canonical anchors.

### 4. Exact envelope binding, not regex acceptance

REVISE the proposed broad `*_theme_research.v1` regex. A matching name is not a validated contract. Bind each mount to an explicitly registered schema/version and check its required structure and all-false authority fields. Unknown schema/version, valid-but-wrong schema for the anchor, malformed fields and a client response for the wrong mount must be rejected. Shared envelope semantics can be reused, but a different Technology economic-change contract must not be renamed into a Semiconductor-shaped one just to pass the client.

## Binding addendum: preserve existing owners and honest limitations

**Private binding:** preserve the already qualified R4 architecture and actual private-store/publication/rights proof gates. An unbound adapter remains unavailable; no public-file, fixture, caller-locator or grandfathered-page fallback. Independent public-workspace construction may proceed under its existing grant, but it is not the complete private research journey.

**Unmapped source rights:** there is no grant to fail open. For the theme-research emission boundary, an assertion without a resolved source-family policy cannot proceed to composition, evidence selection or dependent prose. Use the existing rights resolver/owner, not a second allowlist or a default curated classification. Known permitted independent observations may remain visible. When dependent content cannot be separated safely, refuse that response. Do not relax a real rights denial for availability. Prove unknown-family, warm revocation, direct evidence selection and dependent-interpretation cases before release. This reconciles the transport with its existing owner doctrine; it does not change vendor rights or entitlements globally.

**Slice scope:** the two-entry roster is allowed only as an explicitly declared witness-selection fixture/limited cohort, carrying `slice_scope_unowned`. It must filter both the returned facts and their evidence consistently and never claim complete slice membership, market breadth, ranking or investability. It cannot satisfy canonical membership or a general universe-coverage proof. Durable relationships must use the existing GMI curation/identity/membership owners where admissible; this decision creates no slice-participant store and does not make all possible slice facts permanently private by fiat.

**Historical identity:** without a supported as-known identity mapping, fail closed on the affected `system_replay` request. Do not use a nightly rebuild timestamp as `mapping_learned_at`. `latest` may proceed with honest source scope; `source_history` must not be described as historical system belief. The existing identity/PIT owner must supply any future replay proof. No new identity-history plane.

**Company-first/no-basket entry:** consume Technology registration5811511383 and addendum5812377628 as requirements, not permission to invent a Technology canonical theme. Its domain composer and path-disjoint partial may continue under the original scope; actual `ticker.html.j2`/shared-shell writes still require the incumbent shell owner's concrete custody. B2 supplies reusable hooks, not a source-custody release for every template.

## Accepted Wave 1 consumer cases and next version boundary

A and B were accepted previously; C is now accepted after repair at `75bc15246babc84e5ed1a9925755b4862053d206`, acceptance `ba391af0633624e12375a1768ca9681a50f5d54a`, #7886 STOP5813740550. Do not implement the superseded C formula.

| Consumer job | Use incumbent capability | Remaining acceptance requirement |
|---|---|---|
| A: explain cash received with future supply obligations | Native financial/source facts plus qualified GMI relationship and F04 synthesis | Useful explanation without invented current earned margin or an obligatory management-guidance triple; signed/exact money remains with its native owner. |
| B: explain delivered care and a separately scoped cash subtotal | Native reported activity, payer evidence and financial periods | Do not join distinct historical patient, payer and issuer populations; show missing staffing/paid-use denominators beside the useful reported explanation. |
| C: explain deployment, energy and retained economics | Source-qualified operating facts plus native receipt-bound derivations | Measured kWh never multiplied by capacity again; period exposure, conditional-rate basis and cash/profit distinction survive into any eventual consumer. |
| Unmapped local theme | Existing source-local identity and rights/curation owners | Dossier navigation must not require fuzzy canonicalization or a manufactured tradable basket; permission for label and membership structure remain distinct. |
| Company-first Technology | Existing company identity and domain comparison contract | No fabricated theme/anchor to satisfy a basket-only entry point; closed profile and actual mount acceptance remain required. |

For the separately gated reported-economic-context/profile amendment, the same shared owner should return the exact proposed schema/version, native field/ref mapping and discriminating positive/negative fixtures. Preserve optional guidance and keep Semiconductor B's mandated comparison unchanged. Do not populate live signed/qualitative assertions through frozen v1 fields or copy a financial truth store. These are version-review inputs, not live data admission or an instruction to absorb all nine research cases into B2.

## Required proof and return

B2 completion requires at least two accepted domain registrations with distinct slice sets, one reusable client/route, exact envelope enforcement, current permission on query and evidence, coherent missing-data behavior and the real source-to-visible-result proof owed by each domain. Test unknown anchor, wrong slice/schema, absent dependency, unavailable domain alongside an available one, revoked/unknown rights, stale generation and unchanged legacy Semiconductor behavior. Unit/source checks alone are not production acceptance. Keep builder and reviewer independent under the existing admitted fabric.

Return compactly on the existing #7780/#7870 carriers: exact integrated head/blobs, owned path census, independent review, which hooks are built, real reader and user-proof status, unresolved gates, and what existing consumers may safely consume. Do not infer unattended execution, writer release, merge, deployment or acceptance from this scope ruling. Meta-CEO retains overall research integration; native owners retain implementation truth and custody.

Parent mission remains incomplete. No duplicate operation, queue, graph, state, evidence, rights, publisher, scheduler, identity or evaluation plane is authorized.
