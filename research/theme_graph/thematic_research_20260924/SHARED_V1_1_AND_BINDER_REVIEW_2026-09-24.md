# GMI shared extension and new binder - Meta-CEO disposition

Parent operation: `gmi-thematic-research-meta-ceo-20260924-001`; parent carrier #7886. Incumbent shared implementation: #7870, operation `gmi-semiconductors-fable-ceo-e2e-20260923-chairman-001`.

Protected procedure: Mastermind `a29161fa0a44cca9927afe042b5f7ea25aae1736`, compatible Skillpack 1.0.1 / bootstrap 1. Current Chairman continuation preserves Meta-CEO thesis, integration and acceptance responsibility. This is principal source/semantic review, not an implementation assignment, schema enrollment, rights grant, native test receipt or release.

## 1. Exact inputs and progress

Inspected #7870 head `e9e09d26c36e26b4975b5358f7935812f25284dc`, eleven commits after the previously inspected `e2f4d490915660fc6db64d750505309343e9e0e8`; the compact GitHub comparison lists sixteen changed paths. The new public owner-bundle loader is real source progress. Do not continue describing the route as an unconditional unbound stub.

- `engine/market_ontology/semiconductor_owner_bundle.py`, blob `41f8eaff84180fe0ba4899fb5853abee43271130`: reads current and preceding fiscal-period workspaces, checks issuer identity, projects public observations, refuses unsupported system replay and explicitly declares private assertions absent.
- `engine/market_ontology/theme_research_binding.py`, blob `048f871f07aea95a6b01fb2b91f4b6fd167dcaef`: shared unavailable exception; coverage absence is distinct from unavailable infrastructure.
- `app/theme_research.py`, blob `9654d465e94ab2294a6eea3748d0799925daa08e`: dispatches through the registration's loader and uses one rights snapshot. The inspected rights filter still retains unmapped-family assertions and copies interpretation blocks; the previously directed T04b closure remains required before affected live emission.
- `research/theme_graph/CURATION_ASSERTION_V1_1_PROPOSAL_2026-09-24.md`, blob `ca7223db241b82e13a15ce981716e0c3498f31b2`: revision 3 proposal. Its reported independent reviews are not independently replayed by this assessment.
- Governing Healthcare R12 source at `24744dba42576481e8c7ca2204307896ca6796b2`: `research/healthcare/HEALTHCARE_R12_SHARED_EXTENSION_SPEC_2026-09-24.md`, plus existing ruling #7870/5809358801. R12 preserves v1 and requests a common separately reviewed extension, not a Healthcare fork.

The current-head PR-triggered workflow lookup returned no runs. This is NOT proof that all possible check/event surfaces are absent and NOT green. Earlier failed run 36016682438 is evidence for its old exact head only; it does not establish the new head's result. No CI rerun was issued. No production read, browser proof or native application suite was executed here.

## 2. Decisions resolved now

### D1 - explicit one-sided source bounds are selected

Ratify the proposal's P13 design deviation narrowly: a source explicitly stating "at least 1,000 units" may be represented as a numeric interval with lower `1000`, lower inclusive true, upper null and upper inclusive null. This supersedes only a reading of R12 section 4.2 that would require two finite endpoints for every interval. It does not permit NaN, Infinity, arbitrary exponents, guessed bounds or auto-conversion of source absence into an inequality.

Both endpoints null remains unavailable/invalid as a numeric interval. Missing/undisclosed values use the non-numeric branch. Preserve the exact source scope, unit, direction and comparison basis. Do not compute a midpoint, upper estimate or implied distribution. Interval domain and sign rules still apply. This is a design decision; native schema, decoder, K1, reader, source-use and real-consumer acceptance remain owed.

### D2 - request routing and richer stored assertions are separate dependencies

Approve the separation of the company-first request/profile amendment from the enriched assertion-version amendment, within the SAME incumbent shell and native owners. A company-first workflow that consumes already-supported native evidence need not wait for unrelated royalty or supply-status fields merely because both proposals share a document. A workflow that actually needs v1.1 remains gated on it. This creates neither a new service nor a second implementation carrier.

Preserve exact registered profile/schema/view/subject semantics and the existing Semiconductor request behavior. Prove both the company-first query AND its evidence request, including the correct native evidence-reference format; the current generic evidence body is curation-reference-only, so merely adding a profile field cannot establish that native financial evidence is reachable. Do not re-mint native financial facts as curation assertions just to fit a request regex. No dummy canonical theme, basket or empty slice is acceptable. H1/H2 mounting dependencies remain real, and an empty legitimate slice set must not silently suppress the page.

This amendment's theme-anchor/company-profile pair does not cover the entire Finviz source-local universe. Keep unmapped local-theme access as an explicit subsequent coverage obligation; never force canonicalization to make a route work. It is not an additional condition for completing the currently bounded Semiconductor proof.

## 3. Finite proposal corrections before native version acceptance

Verdict on the whole v1.1 revision-3 proposal: REQUEST_CHANGES, limited to the issues below. Retain its valid signed-money, physical-change, source-target, non-numeric, legacy-version and no-payout distinctions. Do not restart the sector studies or broad proposal-review sequence.

### R1 - one executable contract, not contradictory instructions

Section 1 selects closed selected branches without wrapper `unevaluatedProperties:false`; section 3 and N9 still prescribe that wrapper. Section 1 and N42 require K1 owner-schema set EQUALITY, while sections 6 and 10 still say containment. The code table says `duplicate_local_selector`, while N16 expects `duplicate_component_selector`.

Freeze ONE native schema/rule source and derive the prose/test expectations from it. Keep exact K1 schema-set equality, not subset containment that silently permits a third unsupported schema. Keep selected-branch closure and its actual refusal path consistent. Choose one documented duplicate-selector code. Reconcile section 8's stale "H1 queued" statement with integrated H1; no reopening of accepted H1 is required. These are bounded contradictions to correct, not a reason for a fourth general review of unchanged content.

### R2 - empty intervals are not valid measurements

The proposed `lower <= upper` plus independent inclusivity flags admits equal endpoints with one or both excluded, an empty set. Require a nonempty domain intersection: equal finite endpoints are valid only when both are inclusive. For example, [5,5] can represent an exact bound; (5,5], [5,5) and (5,5) cannot. Apply this to integer-count intervals as well: a count interval must contain at least one allowed integer. Do not repair impossible source statements by inventing a point or changing inclusivity; retain an explicit contradictory/unsupported observation instead.

The owner adds focused positive/negative fixtures using the actual decoder/validator. This review has not executed those native tests.

### R3 - component-level unknown is not known-none

Section 4 assigns `net_of_selectors: []` to both no deductions and unknown deductions. A top-level `missing_material_terms: [component_dependency]` cannot locate the uncertainty when multiple components have different known/unknown bases.

Preserve per-component distinction between source-disclosed no deductions, disclosed dependencies, unknown dependencies and any explicitly partial list. Use the existing source-local component selectors and one narrow typed state or equivalent unambiguous representation; no global relationship registry. Consumers must not infer independence or a calculable waterfall from an empty list. Return a two-component witness in which only one component has unknown deductions, and show that the uncertainty cannot migrate to the other component. No payout/aggregation capability is added by recording the terms.

### Nonblocking coverage note - unit-price domain

The proposed generic `unit_price` class is non-negative. Do not interpret that choice as a universal economic law. A hypothetical source statement of -1.25 currency units per physical unit is not a physical count and must not be sign-flipped or relabeled as a period change. The owner may support signed source unit prices with explicit unit/basis rules, or keep a clearly declared narrower initial profile and preserve the original unsupported observation. This note does not hold unrelated Healthcare/B v1 work or authorize changing v1.

## 4. New public binder: qualify publication coherence before acceptance

The new loader is a useful addition, but source inspection leaves one concrete proof gap. `_serve_witness` independently calls the current reader and then the preceding-period reader. `_admit` appends event and generation identifiers; `load_semiconductor_owner_bundle` sorts their set into the fingerprint. This records what was seen but does not itself establish that the two observations belong to one admitted publication snapshot. It also accepts a missing receipt generation by simply not appending it.

This is a source-level acceptance gap, not an observed production corruption or a claim that no upstream protection exists. The incumbent must identify an actual owner-guaranteed request snapshot/pin or close the gap through that owner. A cache and a generation hash are not, by themselves, a demonstrated pin.

One discriminating proof: resolve current period from generation A; advance the owner publication to B before the prior-period read; make B change the relevant prior/current relationship. The response must use one owner-qualified snapshot, return the established refresh/unavailable outcome, or withhold the unqualified comparison. It must never present the mixed pair as a coherent same-publication comparison. Missing generation/receipt support also cannot be promoted to qualified comparison. Preserve a useful current-only view when the existing contract permits it; do not turn missing comparison evidence into invented history.

Run this against the actual native loader and owner reader seam, not only a composer fixture. No second snapshot service, request-time predecessor-chain scan, raw-reader bypass or unlimited retry is authorized. The existing source-history versus system-replay limits remain controlling.

## 5. Sequencing, acceptance and next return

The live critical path remains the previously directed integration repair, real reader/retention and private-publication qualification, fail-closed rights, and actual user/ordinary-refresh proof. Do not suspend those fixes to redesign all of v1.1. The proposal's finite corrections can be completed within its existing review work; the independent company-first dependency can proceed when its own accepted interface/custody gates clear.

Return: exact source revision and changed owned paths; the focused interval/component/schema-consistency proofs; company-first query/evidence compatibility map; and the binder's snapshot-owner guarantee or barrier-test result. Preserve independent review where required. Do not repeat already accepted A/B/C research, v1 payloads or unchanged semantic reviews. Do not edit the parent's records from a domain lane or create another watcher/worker/PR in response to this review.

This record resolves design questions and names bounded acceptance gaps. It proves no native schema enrollment, live rights admission, private publication, model forecast, merge, deployment or product completion. Parent mission remains incomplete. Source custody and the incumbent operation are unchanged.
