# Commodity calibration-origin disclosure — candidate, not a validated strategy

Operation: commodities-asset-read-r2-20260916-sol-001; existing PR #7224.
Current procedure: Mastermind eec5324c5205e8bad206512e0a936898e50b2408 / Skillpack 1.0.1.
Current Chairman instruction continues the approved asset-first repair. Sol remains
source writer through the existing attended Studio/Remote Desktop/gh carrier.
Direct execution reason: PRINCIPAL_JUDGMENT for the parameter-source versus model-
authority boundary; no independent reviewer had started this source modification.

## User and machine capability

The asset score now discloses whether its parameters came from stored calibration,
weak or unrated stored calibration, defaults, or unavailable evidence. The builder
resolves the calibration exactly once and passes that same object to the existing
scorer and disclosure. The shared asset-read carries this through the existing JSON,
card, detail and hub consumers. Expanding the disclosure reveals declared historical
return horizons and explicitly states that this is not win probability or current
entry validation. Missing horizons remain missing. Labels are reconstructed from
qualified typed metadata, not copied from caller-authored promises.

No engine, numerical score, confidence calculation, allocation target, posture,
indicator calendar, trade permission, provider, publication store or lifecycle is
changed. An unreliable stored flag is reported, not converted into a new trade gate.
The existing legacy reliable=true field is never used as calibration evidence.

## Verification and scope

The original 19 regression cases failed before implementation. Additional malformed
receipt and browser cases exposed four nonscalar-comparison failures, now fixed.
The complete exact R1+R2 source pair passes 483 tests, zero failures, with two
pre-existing NumPy warnings. These include 134 focused cases; the counts overlap.
Five deliberate regressions are detected: defaults falsely called stored, foreign
asset evidence accepted, numerical score changed, calibration loaded twice, and the
hub dropping the parameter origin.

Eight paired archived-input builder runs, with equal input hashes in each pair,
show all existing conviction fields and exposure targets unchanged under default
and stored parameters for gold/silver/copper/oil. This is compatibility proof using
September 15 inputs, not fresh data, chart parity, or predictive-performance proof.

The existing capture owner now supports --include-calibration alongside --full-page.
32 whole-page synthetic states pass across both languages/themes and desktop/mobile;
128 asset selections and calibration identities agree. Expanded parameter disclosures
are actually clicked and checked. Eight additional hub-component link journeys use
the real hub markup/styles and navigate to the commodity page and its asset details.
Global navigation/authentication/globe/live providers are not certified by fixtures.

R1 remains separate: only the calibration changes enter R2. Explicit three-way
source composition with R1 0b2493990358d44dee5b625176c5289eab9a04e4 produces the
byte-identical integration tested here; no R1 source was copied into this branch.
Exact source IDs and receipts are in the companion evidence JSON. Current-main
compatibility, independent review, concluded CI and served acceptance remain owed.

## Supersession and next action

The old platform-blocked calibration edit and its 19-case missing-function baseline
are historical, not the current source state. No gate was bypassed; the same original
carrier accepted these scoped edits after current-state reconciliation. Earlier
483-less test counts still describe their own earlier source, not this revision.

DRAFT / HOLD-FOR-SOL / BUILT_NOT_PROVEN. Do not merge, enable auto-merge or call this
live based on local tests. The next action is independent exact-source review and
existing CI/publication dependency resolution (#7198, #7215, #7242 and #7163), then
actual served commodity page/JSON/hub verification. The broader per-family macro
models and point-in-time/forward-validation program remain active, not completed.
