# NEURAL WEB — The Central Brain — Masterplan by Fable

**Program goal.** Build the dashboard's central brain: the meta-layer over ALL signals — technical, quantitative, qualitative, macro, options, alt-data, risk — that (1) gives every engine one shared language and documented wiring, (2) remembers every claim any engine makes and grades it, (3) learns which engines deserve trust in which regimes, (4) detects confluence and contradiction across the whole web, (5) reacts **within the live-tier cadence (sub-hour) by default, and in true minutes for the few reflexes that earn a push tier**, and (6) hosts a deliberative AI layer with real — but constitution-bounded, evidence-earned — governance authority over the engines. Named **Neural Web**.

**Naming disambiguation (binding).** **Oracle** (`research/ORACLE_MASTERPLAN_BY_FABLE.md`, executing in a parallel worktree) is the *rotation lobe*: sector/theme/subsector rotation history, episodes, Time Machine. **Neural Web is the whole brain**; Oracle is one — very rich — signal producer feeding it. Neural Web never re-implements rotation detection. Namespace: `engine/neuralweb/` (`oracle` is taken twice over; `cortex` was this program's working name in `CENTRAL_BRAIN_BRAINSTORM_BY_FABLE.md`, which this doc **supersedes** — its census and rejections carry forward here).

**Provenance.** Operator brain dump 2026-07-04 (+ ChatGPT "Neural Web" plan as context) → Fable adjudication → 8-agent census (workflow `wf_67ace3c1`, all claims file-evidenced) → draft → **5-lens adversarial red-team (workflow `wf_dd79661a`: epistemics, ops, census-fidelity, program-collisions, operator-ambition) → this v2, with 4 blockers and ~14 majors folded in.**

**STATUS: OPERATING (build-out complete 2026-07-05)**

---

## 0. In plain English

We asked: can our engines stop being disconnected Python scripts and become a living, learning, talking system — with a central brain that hears everything and can even adjust the engines, like the Red Queen?

The census answer is startling: **the organism already exists — it's just missing its nervous system.** We already have ~145 files that engines use to pass signals to each other (undocumented, mostly unversioned), six Claude/LLM "brains" running in production every night (each hard-clamped in code so it can only make us *more* cautious, never less), roughly 24 separate systems that remember predictions and grade them later, several fully-automatic learning loops already adjusting weights in production (safely, with do-no-harm gates), and even a mechanism where a signal must *earn* the right to override another by proving its track record. Nobody can see this whole picture at once — not the operator, not the engines, not the brains. That is the actual problem.

So Neural Web is not "build a brain from scratch." It is: **wire the brain we already have — then grow the mind on top.** One registry that documents every signal. One federated memory that grades every claim. One reliability table that says which engine to trust in which market weather. One graph that shows what confirms and contradicts what. One conductor that knows which engines depend on which data. Reflexes that react to events. And one deliberative AI on top — with supreme *governance* power (it can quarantine engines, rank what deserves attention, propose experiments, demote decaying signals) but zero *authorship* power (it can never invent a signal, a trade, or an escalation) — and even its governance authority is **earned by graded track record, exactly like every other signal in the house**. The Red Queen's omniscience and containment authority — with the constitution she lacked, and a probation period besides.

*Glossary for this doc:* **spine/qledger** = our two existing "what did we predict → what happened" memory systems (quantitative parquet / qualitative claims). **Gauntlet** = pre-registered backtest with committed pass/fail rules. **FDR** = the statistical correction that stops you fooling yourself when you test many things. **PIT** = point-in-time (no hindsight). **Tier ladder** = DISPLAY→SHADOW→CONFIRMER→SCORED, the promotion path every signal climbs by evidence. **can_force** = the existing rule where a radar earns override authority via its graded track record.

---

## 1. The operator's four questions, answered from evidence

**Q1 — "Will our engines ever cross-communicate? It's messy and undocumented."**
They already do, at scale: the census counts **≈145 distinct cross-engine artifacts (~95 unique names)** — 34 basketdata JSONs across 5 regions, 20 PIT archive streams, 10 alert streams fanning into `alert_triage`, 10 strategy verdict files, ~28 data/ snapshots, ~30 site JSONs, ~12 shared parquet families — plus a second, qualitative event bus (`engine/qbus.py` + `engine/qkernel.py`, 1,261 event keys, live since the Qualitative Intelligence program's W2). The bus is 100% file-mediated; the only dependency mechanism is build order (one module even states it verbatim about its own inputs — `engine/china_intel_bus.py:8`: "build-order is the only dependency" — and daily.yml's 1,020 lines of hand-ordered bash with no machine DAG carry the global version of the claim). The website itself is the IPC layer — engines read other engines' *published site JSON*. What's missing is exactly what the operator senses: no registry, no schemas on the most-consumed hubs (`baskets.json` and `flow.json` are bare; version-stamped at best elsewhere; only `site/signals/*` is jsonschema-enforced), no producer/consumer map, and real rot in the seams: two writers racing on `foresight_cascade.json`; `radar_plus` mutating a published artifact post-emit; the Terminal's intel feed starved since the R2 migration (it reads gitignored `site/stockdata` with no R2 leg); and the sector-pulse per-ticker chip wiring shipped in #1170 then **silently regressed 90 minutes later when #1193 deleted the injection block** — undocumented wiring means nobody noticed. **Verdict: the fix is registry + envelope + CI provenance gate — cheap, transformative, and it repairs found rot.**

**Q2 — "Engines are just gears, not live and breathing — can they become alive?"**
Reframe, with conviction: **the gears must stay gears; the *system around them* becomes alive.** Determinism is the moat — the entire epistemic architecture (PIT replay, `engine.run` deliberately recomputing full regime history each run "so the live signal and the backtest can never drift apart", the CI-enforced null registry, Oracle's Time Machine) only works because computation is dead and replayable. What should live: **sensing** (event tiers exist: whitehouse hourly, fastpath 30-min, live-quotes 10-min, a staleness self-heal born from three real incidents), **routing** (today: hand-ordered YAML, lanes that diverge deliberately AND accidentally — an engine change that reshuffles baskets never refreshes sector_central until nightly; a single changed *data input* cannot narrow the render at all), **memory/learning** (bounded loops already live), and **deliberation** (six brains already live). Liveness = conductor + reflexes + cortex, not 482 daemons.

**Q3 — "How important is this, and how does it go beyond Python scripts?"**
It is the **highest-leverage program on the board**, for a structural reason: every active program (Oracle, options alpha, setup species, qualitative intelligence, prebreakout, HK/Canada…) currently hand-builds its own propagation rails, its own ledger, its own wiring wave. That's why we have ~24 outcome-tracking systems in 9 schema families, **two substrates claiming universality** (`engine/spine.py` and `engine/qledger.py`, built by two different masterplans, with altdata adapted into *both*), and two event buses. Neural Web is the program that makes every other program cheaper — and it is also the *only* program whose subject matter is the connections themselves. "Beyond Python scripts" is not a language question — Python runs every institutional quant stack on earth. A script becomes an organ when it has: a contract (schema + registry entry), memory (graded ledger), a self-model (reliability profile), a place in the nervous system (bus + DAG), and a governor (cortex). That is architecture, not syntax.

**Q4 — "If we add a Fable/AI layer, is it supercharged?"**
Yes — and the census shows the hard parts are already proven in production: six clamped LLM brains (master_brain, ai_desk's 4-analyst adversarial panel, narrative_brain dual-model, risk_brain, whitehouse_brain, altdata_brain), a production MCP read-surface over the whole site/+data tree (Mastermind `bot_mcp.py` with deny-roots and leak audit), and an Opus self-correction loop (`risk_radar_review.py`) **already wired into daily.yml and running nightly as a config-gated no-op** (enabled:False + token + ≥30-graded gate) — arming it is a governance decision, not integration work. What the AI layer lacks today is *senses* (145 files, no coherent view), *shared memory* (fragmented ledgers), *a real runtime* (the current "cortex" is a single cached non-agentic completion, by default to a third-party model), and *governance rails*. N0–N4 give it senses and memory; W7b gives it an agentic frontier-model runtime; the constitution gives it bounded, earned authority. Then the AI layer stops being six scattered narrators and becomes a cortex.

## 2. Adjudication of the ChatGPT plan (context doc, graded against our reality)

**Adopt (it converges with house law or evidence):** SignalEvent standardization (its §2 L1 = our N0 envelope); engines-emit-don't-call (§4 — matches our file bus; we keep files, not brokers); "don't turn engines into webapps" (§5 — correct); reliability profiles conditioned on regime (§8 — our evidence *demands* this: sector_signals state base rates, stress-conditional cohesion_chg, EDGE's regime-conditional momentum weight .04–.28); lagging-signal detector (§10 — freshness gate + anti-chase penalties already validate the instinct); LLM-as-analyst-not-scorer (§14 — literally house law, clamped in code six times over); MVP ordering registry→outcomes→confluence→ML-later→LLM-last (§16 — correct and largely *already built*); engine health page (§12); "every engine answers one thing, the meta-layer decides how much it matters" (§18).

**Reject (it contradicts our evidence or physics):**
1. **The stack (§6).** Postgres/TimescaleDB/Redis/NATS/Kafka/Prefect/MLflow/Feast/pgvector/LangGraph — a decade of ops debt for a Mac-Studio-rendered static site + VPS. Our transport (files + parquet + R2 + git, with three live-tier precedents including force-added hot state on main every 30 min) already moves everything we produce. Boring wins.
2. **The hand-weighted master score (§9 Stage 1: "25% technical + 20% sector + …").** The census verdict lane is unambiguous: **no hand-weighted return composite has EVER validated here** — selection blend +0.0002 IC over momentum alone; signal_factory survivors = []; EDGAR composite NO-GO; Wave-6 0/8 promoted; DannyTrades validated only INVERTED. Meanwhile `engine/composite_score.py` already codifies the law: *equal-weight sector-neutral z-mean is the default and every fancier scheme must beat it out-of-sample.* A new invented-weights master score would be the exact laundering machine the BTC-override audit caught. Neural Web ships **measured** weights or none — and §4/N0 add the machinery that makes that law enforceable rather than cultural.
3. **The universal per-ticker bounce-probability meta-model (§9 Stage 3).** Momentum is dead everywhere tested; only 7 of 23 anticipation legs are OOS GO; the repo's honest identity (verdict lane) is *"a drawdown-control machine with one reversal edge in China."* A per-ticker P(bounce) model trained on "all signals at time T" is overfit bait at our n. What IS learnable: state-conditional base rates (already shipping), species×regime calibration (setup-species program owns this), risk-side composites (vol_regime DSR 0.9998, spvector 0.9994, risk_radar_intl 2.07× — the *strongest numbers in the repo are all defensive*).
4. **Missing entirely:** PIT discipline, FDR/multiple-testing (~50 engines × regimes × horizons is a false-discovery flood without shrinkage + pre-registered decision rules), validation tiers/provenance (our single most important invention), and the fact that ~70% of its MVP already exists here.

## 3. Census — the brain scattered on the floor (evidence base)

*Full agent reports in workflows `wf_67ace3c1` (census) and `wf_dd79661a` (red-team, which corrected three census claims folded in below).*

- **Bus (N0's substrate):** ≈145 artifacts + the qbus/qkernel qualitative event bus; ~⅓ schema-stamped; `as_of` near-universal; exactly ONE family jsonschema-enforced (`site/signals/*` via `validate_signals.py`). Layered freshness enforcement exists (healthcheck, signal_sanity FREEZE/STALENESS/DEGENERACY, china_intel_bus MAX_STALE_OK, keep-FIRST archives). `config/qual_ladder.yml` (29 fields: producer/artifact_glob/ladder_state/max_weight/scored_consumers) is the **native precedent for the Neural Web registry**.
- **Brains:** six LLM desks live nightly, all clamps one-directional IN CODE (risk_brain "may not be less cautious than the engine"; narrative/altdata clamp ENTER; whitehouse importance-floor + hallucinated-ticker gate; market_state's radar override can only lower). `master_brain` is the proto-cortex: fuses 15+ blocks with same-tape provenance (`_tape_family` anti-double-count), consumes six desks' *measured* track records + shadow desk weights, and stakes its own graded falsifiable theses — but it is **one stateless content-hash-cached completion per night, defaulting to DeepSeek V4 Pro** (`master_brain.py:1202`), not an agent. The existing "master signal" is `market_state.py`'s 0-100 (hand weights trend .24/risk .18/vol .16/breadth .16/liquidity .14/stress .12) — docstring says display-only, store section says SINGLE SOURCE OF TRUTH, consumed by sector_central + risk_state: **de-facto load-bearing with unvalidated weights** (a W3 measurement target, handled with respect).
- **Ledgers:** ~24 systems / ~40 stores / 9 schema families; desk_scorer's shape shared by 8 desks; spine (typed parquet + adapters + passports) and qledger (claims/grades + Wilson CI + promotion ladder) both claim universality. **Prior art that now governs W2:** Setup Species W0 Stage B (completed 2026-07-04, PRs #1139–#1180) already unified the five decision-facing ledgers by *federation* — shared one-grader spine axes threaded through market-native stores, explicitly NOT store migration. One-grader law mostly holds (`engine.grading.forward_metrics`; CN T+1 variant stamped). 106-experiment registry with come-back-date alerts is live — but it is a *display tracker for humans*: no machine-registration API, no generic evaluator.
- **Learning loops already closed (bounded, do-no-harm-gated):** market_state_tune (corroborator weights 0-12, F1 gate), risk_radar_intl_tune (odds surface, Brier gate), **can_force** (intl radar earns Market-State override: ≥30 graded, ≥8 alerts, ≥1.25× realized lift — *authority-by-track-record, the house-native Red Queen mechanism, though its gate is a point estimate: at minimum n it false-grants ~36-45% of the time, which §4 fixes with CI-lower-bound gates*), spine measured-IC alert-severity caps. Shadow desk weights: partial-pooled, arm-by-evidence predicate (Engine Fix's armory doctrine: pre-registered predicates auto-arm with notification). `risk_radar_review.py`: wired into daily.yml:593, runs nightly, self-noops on enabled:False — **when enabled it auto-writes calibration after a do-no-harm backtest, with no experiment entry or human promotion**, which §4's A6 must and does adjudicate.
- **Verdicts (the epistemic terrain):** null registry is CI-enforced (`check_validated_claims.py` fails builds on unbacked "validated" claims; 170 phrase-scoped allowlist entries). Verdicts get *reversed* (SUE promoted then demoted as shallow-window artifact). Passes get *capped* (SHAKEN passed OOS → display chip only). Cross-engine hard gating was **falsified** (China reversal gated by subsector state hurts in every era) — Neural Web must never hard-gate one engine on another without its own gauntlet.
- **Orchestration:** GitHub Actions YAML is the conductor (daily.yml 1,020 lines; bash clusters + one `wait`; no machine DAG; ~50 build modules; failures exit 0 by design with post-hoc tripwires). Render offers 6 page-family scopes plus all, but auto-narrowing only fires on template/builder pushes — **a single changed data input implies a full rebuild**. The intraday lanes diverge from nightly *deliberately* (GEX impure, policy_intent needs secrets) and *accidentally* (engine-render omits sector_central — live drift). Actions cron is documented "best-effort ~15-45 min late" in two workflow headers, and the fastpath header already names the graduation path: "at a true 10-min cadence it graduates to a native systemd timer on the box." Ledger law: nightly is the sole advancer of forward ledgers; intraday lanes deliberately discard data/ writes; whitehouse is engineered as the SOLE intraday ledger-writer to prevent append races.
- **Contracts:** §7 signal gate has 3-lane failure semantics (RED pre-merge / quarantine-continue nightly / annotate intraday). 15 paths-filtered CI jobs; 3 only-shrinks ratchets (passport ratchet fully drained). But: ~10% of 479 engine modules under any machine contract; enrollment = 7 bespoke mechanisms; several auditors built-but-unwired (claim_passport enforced by unit test only — and it scans only scored-consumer *engine modules*, leaving alert priority, board ordering, and attention surfaces structurally outside the perimeter, which §4 fixes); production deploy path guarded by exactly ONE check while the full guard battery sits in dormant pages.yml.
- **Cross-repo/product reality:** Mastermind is a **second committer** to this repo (pushes mastermind_snapshot.json to main 2×/day) and imports `engine/`+`lib/` as a Python library — function signatures are cross-repo API. `risk_brain.json` (the only sizing directive) has **zero readers** in Mastermind despite its docstring. Sector-pulse consumption is in-process by design (macro.html heat strip live via #1170); the JSON artifact has no file readers; the per-ticker chip path was regressed by #1193 (restore belongs to the **sector-pulse program**, not this one). Terminal intel/v1 bridge frozen since the R2 migration. `export_signal_contracts.py` has an *automated consumer* (Terminal conformance gate) but a *manual producer*. ≥11 independent Telegram senders, no shared lib; `alert_triage` computes priority but pushes nothing. The repo ships its own authed FastAPI SaaS tier (`app/main.py`, VPS :8000) — the natural host for Neural Web's interactive surfaces and its push-reflex intake.

## 4. The Red Queen, adjudicated — supreme power, earned and bounded

The operator wants the brain to hold "supreme power … both the brain hearing from the engines and the orchestrator able to adjust the engines." The movie actually argues our case: the Red Queen had unbounded authority, a narrow objective, and no constitution — the Hive died of it. Our own repo provides the corrective pattern natively: **can_force — authority is not configured, it is EARNED by graded track record, granted in code, bounded by clamps.** Neural Web generalizes that into the **Authority Ladder**, with three constitutional articles the red-team forced us to make explicit:

**Article 1 — No origination (A7).** No LLM may invent a signal, trade, or escalation. Inherited from six live clamps; never renegotiated.

**Article 2 — The perimeter is drawn by SURFACE, not by code path.** The operator's money path includes everything that *ranks or escalates*: buy strips, board ordering, top-setups, alert_triage priority inputs, push floors, the attention queue. All are registered as scored-path surfaces in synapse.yml. **Minimum tier to RAISE an ordering/priority/attention outcome = SHADOW-with-track-record; DISPLAY-tier signals may annotate, never rank.** (This closes the laundering hole the epistemics red-team found: a "display-only" signal that bumps an alert's priority is influencing money without a gauntlet.)

**Article 3 — Authority is granted on confidence bounds, lapses on silence.** Every authority gate (can_force-style) uses a **CI lower bound** (Wilson; lift lower bound > 1.0 at stated confidence), not a point estimate — at can_force's current minimums (≥8 alerts) a point-estimate gate false-grants ~36-45% of the time. Each grant's false-grant probability at its actual n is printed in the governance ledger entry. When evidence is insufficient or stale, authority **lapses** — it never persists on absence of evidence. (First application: altdata_brain's grandfathered `actionable: True` flag gets a spine track record and either clears an Article-3 gate or loses the flag.)

| Rung | Authority | Status today | Neural Web change |
|---|---|---|---|
| A0 OBSERVE | read every artifact, ledger, health status | bot_mcp (Mastermind-side), per-brain partial views | one coherent view (N1 world state + N0 registry) |
| A1 EXPLAIN | narrate on any surface; committee memos | 6 brains, briefs, tooltips | unified nightly memo + per-page provenance |
| A2 ATTEND | rank what deserves deliberation/operator attention | briefing.py priority_queue (Mastermind-facing) | cortex owns the attention queue — **after earning it** (below) |
| A3 DE-ESCALATE | lower LLM-facing escalations | house law, 6× in code | unchanged, inherited |
| A4 QUARANTINE | freeze a misbehaving engine's output (serve last-good + watermark) | circuit breaker (data sources), §7 quarantine lane (signals) | generalized to any registered producer; deterministic trips; cortex may *recommend*, breaker executes |
| A5 GOVERN TIERS | promote/demote signals across DISPLAY→SHADOW→CONFIRMER→SCORED | qledger ladder (gates nothing automatically), qual_ladder.yml, passport ratchet | one ladder vocabulary for the whole bus; promotions require gauntlet verdicts; demotions on measured decay are automatic (spine IC caps precedent) |
| A6 TUNE | adjust engine parameters/weights | two deterministic loops live; risk_radar_review wired-dormant; Engine Fix auto-arm doctrine | **two lanes, explicitly:** (i) *bounded deterministic auto-apply* — clamped, do-no-harm-gated, pre-registered loops (market_state_tune, intl_tune, Engine-Fix arming predicates) are hereby **ratified as standing A6 approvals**, required only to log every write to the governance ledger with quarterly re-audit; (ii) *LLM-proposed changes* — must land as machine-registered experiment entries with pre-committed gates and come-back dates; **application requires the deterministic do-no-harm harness AND a logged promotion event**. risk_radar_review arms only after its writes are rewired through lane (ii)'s registration+logging (a small change — its clamps and do-no-harm harness already exist). |
| A7 ORIGINATE | invent a signal, trade, or escalation | banned (clamps) | **permanently banned — Article 1** |

**The cortex earns its own authority (the red-team's sharpest point: ungraded attention is ungraded authority).** The cortex runs its first weeks in SHADOW: its attention queue is produced but not surfaced as ranking; **every queue item carries a falsifiable "this mattered" criterion** (subsequent escalation, realized move, verdict change) graded into the spine like any thesis. A2 and A4–A6 proposal rights arm only when its graded attention/thesis record clears an Article-3 gate. The Red Queen serves a probation, on the same terms she will later enforce.

Every A4–A6 action lands in an append-only **governance ledger** with the evidence attached (the btc-override registry pattern, generalized).

## 5. Architecture — the Neural Web (N0–N8)

**N0 — SYNAPSE: the signal registry + envelope.** Generalize `qual_ladder.yml` into `config/synapse.yml`: one entry per bus artifact — producer module, schema ref + version, freshness SLA, validation tier, authorized scored-path surfaces (Article 2), R2-vs-git placement, owner program, and a `weights: measured|hand|none` field so composite-weight debt is carried by the registry and ratcheted (only-shrinks) rather than policed by culture. Envelope = **in-place sibling keys, never a wrapper** (`schema_version/produced_by/produced_at/inputs_hash/tier` alongside existing keys — build_feeds copies byte-verbatim and extracts sub-objects; a wrapper would silently re-darken the risk_radar feed, the exact 2026-07-02 incident the feeds plane exists to prevent); `inputs_hash` computed over payload-ex-envelope so unchanged data stays byte-identical; sidecar `.envelope.json` for parquet/JSONL; W0 ships a conformance gate proving Mastermind's vendored readers + the Terminal manifest check pass against stamped fixtures. CI: unregistered cross-engine read = warn→fail (only-shrinks ratchet); Article-2 surface gate; auto-generated `docs/SIGNAL_BUS.md` + producer→consumer graph. W0 also *repairs found rot*: single-writer rule for foresight_cascade.json, radar_plus folded into its producer, export_signal_contracts wired into the nightly (automated consumer deserves an automated producer), qbus/qkernel registered as QI-owned rails. (Terminal-feed unfreeze + the #1193 sector-pulse restore ship as D7 hygiene PRs *before* W0; the restore belongs to the sector-pulse program.)

**N1 — WORLD STATE: the blackboard.** `data/neuralweb/world_state.json` (+ feeds-plane copy): the resolved one-page truth — market_state verdict + radar override outcome, regime quad, vol regime, breadth aggregates, rotation state (Oracle's payload when it lands), QI's macro/regime aggregate (consumed as an input signal — border ruling with QI W7), liquidity, data-health, active-alert census, live overlay staleness. `data/regime/latest.json` is today's de-facto hub with 12+ consumers; N1 composes the existing stores rather than replacing them; engines migrate reads gradually. One computation, one truth, stamped provenance — the cortex's primary sense organ.

**N2 — THE SPINE: one memory, by federation.** *(Revised per red-team: migration is the wrong architecture and collides with two active programs.)* The house pattern is already settled by Setup Species Stage B: **federated stores threaded with shared one-grader spine axes** — market-native fill rules preserved per-store, shared vocabulary (fwd_mfe horizons, terminal states, regime stamps, species_id) threaded through. Neural Web W2 therefore ships: (a) the **spine as the QUERY LAYER** — a read-side index/view over all federated ledgers answering *"what did any engine claim, and what happened?"* in one place; (b) shared-axis adoption for the un-federated long tail (desk_scorer family and stragglers); (c) qledger **stays QI-owned** — its ladder vocabulary becomes the house tier language (A5), its rows join the query layer via the existing adapter, and any substrate ruling is a **joint ruling co-signed with the QI program record**, sequenced after QI's armed W6 promotion monitor fires or with qledger semantics frozen meanwhile; (d) every ledger gets a synapse.yml entry: federated / adapted / exempt-with-reason. The universe-wide prediction log rides the shared axes.

**N3 — RELIABILITY KERNEL: the self-model, with its statistics specified.** From the spine query layer: per-engine × regime × horizon calibration with **hierarchical shrinkage** (`engine/pooling.py` precedent). The red-team's discipline, now binding: **cells are estimates-with-CIs, never "findings"**; only shrunken posteriors are consumable by any surface; any cell used to CHANGE behavior (severity cap, demotion) must clear a **pre-registered decision rule with a stated error rate and minimum-n floor**; the FDR family is defined per engine-family per **quarterly batch** (nightly refresh updates estimates; decision rules evaluate only at medium/slow cadence — no nightly significance-peeking). Freshness/decay curves per signal family; the lagging-signal detector (fired-but-extended, fired-but-breadth-unconfirmed, fired-in-hostile-regime). Consumers: passports, alert_triage caps (live precedent), master_brain prompt state, the HQ. **W3 line item: measure market_state's hand weights against the equal-weight z-mean baseline per composite law — verdict printed either way.** Ships display-first; nothing reweights money until gauntleted.

**N4 — CONFLUENCE GRAPH: the web itself.** Typed graph over the bus: nodes = signals, engines, tickers, baskets, sectors, regimes, theses; edges = confirms/contradicts/leads/feeds, regime-conditioned stability scores. *(Revised per red-team: the graphlib-sharing plan was unilateral and circular — Oracle's plan has no slot for adopting a shared library mid-execution.)* Ruling: **Oracle builds Oracle-native O1 untouched; Neural Web W4 consumes Oracle's edge-stability ledger and routing artifacts READ-ONLY as bus citizens; library extraction is deferred to a joint ruling at Oracle P7.** Both programs' trials already share the house-wide registered-trial CI (check_trial_registration) — that, not a shared library, is what "one FDR ledger" means today. The contradiction detector generalizes `briefing.py`'s divergences block to any edge pair. Hard law: **cross-engine confluence NEVER hard-gates a validated edge without its own gauntlet** (the China falsification); confluence states ship as context/annotation under Article 2 — they may not RAISE rankings until they hold SHADOW-with-track-record tier.

**N5 — CONDUCTOR: the machine-readable DAG, validate-first.** *(Revised per red-team: the lanes diverge deliberately on semantics inputs/outputs can't express — impurity, secrets, event-driven steps.)* **W5a:** `config/dag.yml` declaring each build module's inputs/outputs/flags (`impure`, `needs_secrets`, `event_driven`) + a **validate-only conformance checker** that diffs the declared DAG against the hand-written lanes and reds CI on drift — this alone catches the live sector_central gap and ends the hand-sync problem while preserving the lanes' deliberate semantics. Budget one nightly runtime file-access trace to verify declared inputs against reality before trusting them. **W5b (gated on W5a proving insufficient):** lane generation. Input-hash scoped recompute targets the data-input hole (today a changed input = full rebuild). Non-negotiables inherited verbatim: nightly is the sole ledger advancer; fail-open resilience preserved; impure builders excluded from scoped lanes.

**N6 — REFLEXES: the event layer, with honest latency.** Reflex registry (`config/reflexes.yml`): deterministic trigger → scoped chain → bounded action (alert / banner / feed patch / attention flag). **Ledger law fix (red-team blocker): each reflex appends to its own single-writer JSONL stream (`data/reflexes/<name>/firings.jsonl`, the whitehouse pattern); the NIGHTLY grader folds firings into the spine — no intraday writer ever touches spine parquet.** Latency, stated honestly: Actions cron is documented 15-45 min late; therefore the default reflex tier is **sub-hour** on existing lanes — and **W6b ships the push tier the fastpath header already names**: event-push intake on the VPS FastAPI tier (webhook/systemd timer) for the 2-3 reflexes where true minutes matter (whitehouse-class news, circuit-breaker trips), with measured end-to-end latency as the acceptance test. Unify outbound alerting: ≥11 Telegram senders route through alert_triage (which finally pushes) with dedup + priority floors — priority inputs being an Article-2 surface. Every reflex firing is graded like any other claim.

**N7 — CORTEX: the deliberative layer — constitution first, cognition second.** `master_brain` grows into the cortex in two waves:
- **W7a — the constitution:** Authority Ladder + Articles 1-3 in code; governance ledger; A6 lane (ii) rewiring of risk_radar_review then arming (config flip + secret + constitution entry — it's already wired and self-gated); altdata_brain actionable-flag review under Article 3; ratification of Engine Fix's arming-predicate doctrine as standing A6.
- **W7b — the cognition:** the runtime the ambition demands, specified: **frontier Opus-class model pinned for the cortex role** (the governance organ does not run on a third-party default; DeepSeek stays available for narration fallback), **an agentic MCP/tool loop** over spine + world_state + graph + governance ledger (bot_mcp generalized dashboard-side, deny-roots inherited), bounded tool-call budget, fallback to single-call mode on failure. The cortex runs **in its own downstream job** (`needs: engine`, the stock_briefs pattern) with its own commit lane — a slow or failed cortex can never gate the deploy. Deliverables: the attention queue **with its outcome grader** (§4), the nightly committee memo (what fired, what contradicts, what's decaying, what deserves the operator), contradiction hunts over N4, and the **hypothesis metabolism with real machinery**: (i) machine-registration schema on the experiments registry (pre-committed gate spec, come-back date); (ii) a **generic hypothesis evaluator** — spine/graph joint queries + the existing walk_forward harness — able to grade a proposed lead-lag or conditional claim without bespoke backtest code; (iii) **anti-mining rules**: machine trials are graded ONLY on data with as_of strictly after registration (zero lookback overlap — the cortex proposes FROM history, so it may never be graded ON it); hard proposal budget per cycle, retire-one-to-file-one beyond it; machine trials form their own FDR family so cortex volume can never raise the discovery bar for human programs.
Identity ruling stands: **two organisms, two brains** — master_brain-cortex governs the dashboard; Mastermind's Brain remains the portfolio organism's decision-maker, consuming Neural Web over the (now registered) corpus callosum: feeds-R2 outbound, mastermind_snapshot inbound.

**N8 — THE HQ: two products, not one.**
- **W8a — operator HQ (internal, fast):** engine-health board (run_status + registry + kernel: last run, freshness, recent accuracy, regime-fit), reflex firing log, bus graph, governance ledger view — the admin console made coherent.
- **W8b — the customer flagship:** the per-ticker/per-sector **committee view** (every line traced to a tiered, graded source — the ChatGPT §13 UX with our provenance discipline), the **living confluence graph** (confirm/contradict edges, staleness fades, new-change pulses), and the **ask-the-brain box**: an interrogable surface — *why did this fire? what's its track record in this regime? what contradicts it?* — backed by the cortex tool loop over spine/graph, served authed from the FastAPI tier. Customer story: a subscriber opens one page and sees not indicators but *a reasoned, graded, self-aware market picture no competitor can fake, because it's backed by four years of graded claims.* Costed honestly (5-8 sessions) and sequenced after W7b so it has a real brain to display. Pairs with Oracle's Time Machine as the two SaaS flagships.

## 6. What Neural Web will NOT be (pre-registered rejections)

1. **No message broker / DB migration** — files+parquet+git+R2 with in-place envelopes.
2. **No hand-weighted master score** — composite law + the synapse `weights:` ratchet; market_state's existing weights get *measured* (W3), not multiplied.
3. **No universal per-ticker bounce model** — species×regime calibration belongs to setup-species; Neural Web provides the spine and kernel they read.
4. **No LLM signal origination (Article 1)** — and no DISPLAY-tier influence on ranking/priority/attention surfaces (Article 2).
5. **No substrate migration wave** — federation with shared axes (the Setup Species precedent); qledger stays QI-owned; joint rulings for shared substrates.
6. **No cross-engine hard gates without individual gauntlets** — falsified pattern.
7. **No daemon-first reflexes** — scheduled lanes by default; the push tier (W6b) is scoped to reflexes that earn it, with measured latency as the acceptance test.
8. **No 482-engine big-bang enrollment** — top ~50 bus producers first; only-shrinks ratchet; the long tail joins as programs touch it.
9. **No ungraded authority** — every rung of the ladder, including the cortex's own attention, is graded in the spine and earned under Article 3.

## 7. Learning — the three metabolisms (honest version)

- **Fast (nightly):** spine grading (qledger grader, desk scorers, board ledgers — existing) + reflex-firing folds + kernel estimate refresh (estimates only — no decision rules fire nightly).
- **Medium (monthly):** walk-forward recalibration; experiment come-back processing (registry already alerts); shadow-arm evaluations; attention-grade review.
- **Slow (quarterly):** kernel decision-rule evaluation (the FDR batch); edge-stability refresh; tier promotions/demotions batch (human promotion events per foresight law; automatic decay demotions per spine-caps precedent); constitution re-audit — every A4–A6 action re-examined against its evidence.
- **The hypothesis metabolism (the genuinely new loop):** cortex reads spine + graph → proposes → machine-registers with pre-committed gates → the generic evaluator grades on strictly-post-registration data → survivors go to gauntlet → verdicts printed either way. Budgeted, FDR-fenced, walk-forward-only — discovery without self-deception, at the only data scale we honestly have.

## 8. Phased plan

House routing: **Sonnet builds, Opus reviews, Fable plans/adjudicates/merges.** Heavy computation off the render path. Each PR: fresh worktree off main, hermetic tests, bilingual UI, same-day squash-merge. No file contention with Oracle (disjoint namespaces; read-only consumption of its artifacts).

| Wave | Ships | Cost (sessions) |
|---|---|---|
| **D7 (pre-W0 hygiene, this week)** | Terminal-feed unfreeze (cross-repo, R2 leg); #1193 sector-pulse restore (routed to sector-pulse program); foresight double-writer fix; export_signal_contracts wired | 1-2, mostly small PRs |
| **W0 — Synapse** | synapse.yml over top ~50 producers (+qbus/qkernel registered); in-place envelope helper + conformance fixtures (Mastermind/Terminal); CI read-gate (warn) + Article-2 surface registry; bus doc + graph; radar_plus fold-in | 3-4 (after D7) |
| **W1 — World State** | world_state.json composing existing stores; migrate duplicate regime reads; feeds copy; QI-aggregate border ruling | 1-2 |
| **W2 — Spine federation** | query layer over federated ledgers; shared axes for the long tail; qledger joint ruling (sequenced after QI W6 fires, or semantics frozen); synapse entries for all ~24 systems | 3-5 |
| **W3 — Reliability Kernel** | shrunken-posterior tables + CIs; pre-registered decision rules + min-n floors; quarterly FDR batches; decay curves; lagging detector; market_state weight measurement; passport/cap consumers | 3-4 |
| **W4 — Confluence Graph** | typed graph; Oracle artifacts consumed read-only; contradiction detector; edge promotion under registered trials; Article-2-compliant confluence states | 3-4 |
| **W5a — Conductor (validate)** | dag.yml + conformance checker (reds CI on lane drift); runtime file-access trace; sector_central gap closed | 2-3 |
| **W5b — Conductor (generate)** | lane generation + input-hash scoped chains — **gated on W5a proving insufficient** | 3-4 if triggered |
| **W6 — Reflexes** | reflex registry; per-reflex JSONL firings (nightly spine fold); whitehouse + self-heal migrated; alert_triage becomes the push spine (11 senders unified) | 2-3 |
| **W6b — Push tier** | FastAPI event intake / systemd timer for minutes-class reflexes; measured end-to-end latency acceptance | 2 |
| **W7a — Constitution** | Ladder + Articles in code; governance ledger; risk_radar_review rewired-then-armed; altdata Article-3 review; Engine-Fix doctrine ratified | 3-4 |
| **W7b — Cognition** | Opus-class agentic cortex (own job, own commit lane); attention queue + outcome grader + shadow earn-in; committee memo; hypothesis metabolism (registration API + generic evaluator + anti-mining rules) | 5-7 |
| **W8a — Operator HQ** | health board, reflex log, bus graph, governance view | 2-3 |
| **W8b — Customer flagship** | committee view + living graph + ask-the-brain (authed, FastAPI) | 5-8 |

Dependency: D7 → W0 → W1 → W2 → W3 → {W4 ∥ W6→W6b} → W7a → W7b → W8b, with W5a parallel after W0 and W8a anytime after W3. Total ≈ 35-45 sessions — a mega-program on par with Engine Fix, amortized by every program that lands on its rails. **Gauntlet law binds throughout:** any Neural Web output that wants to be scored goes through an O6-style pre-registered gauntlet; failures ship display-with-null.

## 9. Risks & honest expectations

- **Unification fatigue.** W2 touches proud systems owned by active programs; the mitigation is now structural — federation not migration, joint rulings for shared substrates, adapters + exemptions, only-shrinks ratchets.
- **The kernel may mostly confirm nulls.** Expected. Even the null — "engine X has no measurable regime-fit" — is product (honest passports) and risk control (severity caps). The repo's identity is drawdown-control; Neural Web amplifies exactly that.
- **Governance theater.** Named and now funded: W7a arms a real tenant on day one (risk_radar_review, rewired); the cortex itself serves probation under Article 3; the constitution is exercised nightly or visibly idle.
- **Intelligence descoping.** The red-team's sharpest warning: when sessions run out, plumbing ships and cognition slips. Mitigation: W7b and W8b are separately costed waves with their own acceptance criteria — they cannot be silently absorbed.
- **Cross-repo drift.** Mastermind imports our function signatures; the Terminal consumes our manifests. Synapse registers both as external consumers with conformance checks; W0 ships the fixtures.
- **Scope seduction.** The border law: Neural Web owns *rails, memory, governance, and synthesis*; domain programs own *their signals*. Any wave that starts building a domain signal has left the reservation. (Applied three times already in this doc: sector-pulse restore → sector-pulse program; rotation graph → Oracle; species calibration → setup-species.)

## 10. STOP list — operator decisions

- **D1 — Adopt & sequence:** adopt Neural Web as a program; dispatch D7 hygiene now, W0 next. *(Recommended: yes.)* — *Plain English: say go, and this week we fix the broken feeds we found, then start the registry.*
- **D2 — Memory ruling (joint with the Qualitative Intelligence program):** federation architecture ratified; spine = query layer; qledger stays QI-owned with its ladder becoming the house tier vocabulary. *(Recommended as stated.)* — *Plain English: we have two memory systems built by two programs; instead of forcing one to die, we keep both and build one window that reads across everything.*
- **D3 — Two brains ruling:** master_brain→cortex for the dashboard; Mastermind Brain stays the portfolio brain. *(Recommended; supersedes the CORTEX brainstorm's one-brain lean — the census showed the split already exists and works.)* — *Plain English: the website gets its own brain; the trading bot keeps its own brain; they talk over a documented channel.*
- **D4 — Constitution ratification:** Articles 1-3 + the Ladder as written, including the cortex's own probation, the two A6 lanes, and the altdata_brain review. *(Recommended: ratify.)* — *Plain English: the AI can watch everything, explain everything, flag anything, and propose changes — but it must earn each power with a graded track record, it can never invent a trade, and every action it takes is logged with evidence.*
- **D5 — Conductor ambition:** validate-first (W5a), generation only if drift persists (W5b). *(Recommended: validate-first.)* — *Plain English: first make the machine CHECK that our build order matches what we declared; only rebuild the build system itself if checking proves not enough.*
- **D6 — HQ access:** operator HQ internal; customer flagship (committee view + ask-the-brain) as the premium SaaS surface, pairing with Oracle's Time Machine. *(Operator call.)*
- **D7 — Immediate hygiene dispatch:** Terminal-feed unfreeze, #1193 sector-pulse restore, foresight double-writer, export_signal_contracts wiring — standalone fast PRs this week. *(Recommended: yes.)*
- **D8 — Cortex model ruling:** frontier Opus-class pinned for the cortex/governance role (third-party default retained only for narration fallback). *(Recommended: yes — the governance organ should not think on a third-party discount model.)*

**Rulings received 2026-07-04 (operator):** D1 — adopted; D7 dispatched same day. D2 — ratified as stated (joint with QI). D3 — ratified. D4 — ratified (Articles 1–3 + the Ladder). D5 — ratified (validate-first). D6 — internal operator HQ; customer flagship ships as the premium SaaS surface. D7 — yes; note the #1193 sector-pulse restore had already landed via #1236, so three items were dispatched (Terminal intel/v1 R2 leg, foresight_cascade single-writer, export_signal_contracts nightly wiring). D8 — ratified: the cortex is pinned to a frontier Opus-class model; the third-party default is retained for narration fallback only.

## 11. Status log
- 2026-07-04 — Masterplan authored (Fable): operator brain dump + ChatGPT plan adjudicated + 8-agent census (`wf_67ace3c1`) → draft → 5-lens red-team (`wf_dd79661a`) → v2 with 4 blockers + ~14 majors folded (perimeter-by-surface Article 2; A6 two-lane ruling; spine federation replacing migration; Oracle read-only consumption; validate-first conductor; reflex JSONL ledger law; latency honesty + push tier; agentic cortex runtime + probation; hypothesis-metabolism machinery + anti-mining rules; HQ split). Supersedes `CENTRAL_BRAIN_BRAINSTORM_BY_FABLE.md` (CORTEX). **At operator STOP.**
- 2026-07-04 — **Operator rulings D1–D8 received; program adopted.** All recommendations adopted; D6 ruled: internal HQ + premium customer flagship. D7 dispatched same day (#1193 restore had already landed via #1236). Program status: **EXECUTING — D7 → W0.**
- 2026-07-04 — **D7 complete.** #1243 rulings; #1245 export_signal_contracts wired; #1246 foresight_cascade single-writer; Terminal intel/v1 R2 leg built, deployed to the VPS and verified (1,666/1,666 synced; charting-app master); root-cause bonus #1250 — the nightly collect push had been silently dying since 07-03 on an unstaged tracked qledger file (staging-gap), freezing stockdata/R2/Terminal at asof 07-01; fixed + CI invariant. **W0 PR1 dispatched** (this PR): synapse.yml registry over the top ~65 bus artifacts + loader + integrity gate.
- 2026-07-04 — **W0 PR2:** envelope helper (in-place siblings, hash-ex-envelope, byte-identity fast-path, parquet/JSONL sidecars) + conformance fixtures proving the build_feeds extraction, Mastermind anchor readers, and Terminal artifact-conformance checker all pass on stamped artifacts. No producer stamped yet — adoption is per-producer in later PRs.
- 2026-07-04 — **W0 PR3:** synapse read-gate shipped at warn tier (literal-path scan v1, only-shrinks baseline of 19 findings) + Article-2 hard gate for money-path surfaces; CI job synapse-read-gate.
- 2026-07-04 — **W0 PR4:** docs/SIGNAL_BUS.md auto-generated from synapse.yml; freshness-gated in tests/test_signal_bus_doc.py; CI job `neural-web` added to ci.yml with path triggers covering config/synapse.yml, scripts/gen_signal_bus_doc.py, docs/SIGNAL_BUS.md, engine/neuralweb/**, and the three test files — a config edit that produces a stale doc now fails RED before merging.
- 2026-07-04 — **W1 PR1:** data/neuralweb/world_state.json ships — composed one-page truth (verdict post-radar, regime quad, vol[scored_active honest], breadth aggregates, Oracle rotation read-only summary, liquidity, data-health, alert census; qi=null pending the joint QI border ruling; fail-open with printed gaps; first envelope producer adoption; feeds copy). Consumers migrate in PR2 (build_feeds + master_brain deferred: contract-critical / cortex-owned).
- 2026-07-04 — **W0 PR5:** radar.json single-writer restored — build_radar_plus now writes enrichment to radar_enriched.json (its own artifact) instead of mutating radar.json in-place; build_intelligence writes radar_news.json instead of cross-surfacing headlines onto radar.json; radar_panel.js fetches both sidecars and merges client-side; radar_ic.py reads edge_score from radar_enriched.json with one-cycle fallback; synapse.yml updated (known_extra_writers cleared, two new artifact entries registered); regression test enforces the single-writer contract statically and dynamically.
- 2026-07-04 — **W1 PR2:** world_state freshness extended to the express lane (engine-render recomputes regime intraday — the PR1 nightly-only premise was corrected) + first five consumers migrated behind a staleness-guarded fallback (notify, build_impulse, etf_pulse, regime_label, briefing); build_feeds and master_brain deferred by ruling (feeds contract-critical; master_brain is W7 cortex substrate). Equivalence + fallback + staleness tests per consumer. sector_rs and liquidity_overlay added to _compose_regime() so etf_pulse can consume world_state without a separate raw-store read; world_state.json seed regenerated. Synapse consumers list extended (five migrated modules added to world-state artifact); docs/SIGNAL_BUS.md regenerated.
- 2026-07-04 — **W5a:** `config/dag.yml` (step-granular lanes + cluster units + divergence ledger: 9 documented, 2 suspect) + validate-only conformance checker `scripts/check_dag_conformance.py` reds CI on undeclared lane drift; `tests/test_dag_conformance.py` (parser unit tests on real workflow excerpts, differ tests, selftest, live conformance assertion); `dag-conformance` job added to ci.yml triggered on `.github/workflows/**`, `config/dag.yml`, the checker, and the test file. **Sector_central gap fixed in engine-render**: runtime evidence from 2026-07-03 daily run shows build_sector_central took ~34 seconds (start 07:25:24Z, INFO "built site/sector_central.html" at 07:25:57Z) — well under the 3-minute threshold. `central()` function added to engine-render.yml scope=all (`spine; band; central; hub; us_pages; libs`); commit step globs `git add site/` only (engine-render.yml:340 verified), so `data/sector_central/calls.parquet` is discarded — nightly daily.yml remains the sole ledger advancer. Suspect list (2 entries: render cl_misc order/build_markets addition; engine-render cl_misc missing sector_cycles/country_cycles/cycle/markets) is only-shrinks. W5b (lane generation) remains gated on W5a proving insufficient.
- 2026-07-04 — **W2 PR1:** spine query layer ships — `engine/neuralweb/query.py` + materialized `data/neuralweb/spine_index.parquet` (read-side union: spine + track_record + HK/CA/CN boards + qledger claims⋈grades read-only + graded cycle rows; N rows from M sources at seed). Federation not migration: zero source ledgers modified; qledger substrate ruling remains OPEN pending joint QI co-sign (W6 monitor unfired — earliest projection 2026-08-29 — and semantics frozen since #1180, both escape hatches verified). Registration sweep for the 21 unregistered ledgers = PR2.
- 2026-07-04 — **W2 PR2 (completes W2):** registration sweep — 23 outcome ledgers added to synapse.yml (registry now 91 artifacts; every ledger the W2 scout found is a registered bus citizen: federated/adapted/unfederated-with-adapter/exempt each labeled in notes). W2 scope closed: query layer (PR1) + registrations (PR2); shared-axis retrofit for different-grain ledgers deliberately NOT done — they join via adapters; qledger substrate ruling remains OPEN for the joint QI co-sign.
- 2026-07-04 — **W3 PR1:** reliability-kernel ESTIMATES ship — (engine × regime × horizon) cells over the spine index, engine/pooling.py shrinkage reused verbatim (two-tier toward zero), Wilson-CI lower bounds, per-family arming status stored for display. Display-first law enforced: zero behavior-changing consumers; the quarterly pre-registered decision batch (PR2) is the only path to behavior. Regime-conditioned cells honest-but-thin (stamps accrue from 2026-07; deep 1962→ archive fills the marginals).
- 2026-07-04 — **W3 PR2:** quarterly decision machinery armed, not fired — pre-registered rule constants + BH FDR (alpha .10, one family per batch, trial-ledger-registered before evaluation, code-refuses early runs), first batch due 2026-10-01; kernel_decisions.json seeded empty (display-first law holds structurally). market_state hand-weights measurement: PIT component history does not exist (4 forward-log entries, components never logged) — logger patched to accrue per-component scores nightly from today; measurement pre-registered (h21 drawdown-concordance Brier, hand vs equal-weight, n>=250d) with come-back 2027-05-01. **Today's verdict, printed either way: INSUFFICIENT PIT HISTORY — accrual armed, honest verdict due ~2027-05.** Decay curves + lagging-signal detector remain W3 PR3.
- 2026-07-04 — **W3 PR3 (completes W3):** family decay curves (horizon shape + recency trend + staleness) and the lagging-signal detector (hostile-regime, breadth-unconfirmed, repeat-fire proxy for extension — honestly named) ship display-only as kernel_families.json + lagging_signals.json. W3 closed: estimates (PR1) + armed quarterly decisions (PR2) + diagnostics (PR3); first decision batch 2026-10-01; weights verdict accruing to ~2027-05.
- 2026-07-04 — **W4 (confluence graph + contradiction detector):** `engine/neuralweb/confluence.py` builds `data/neuralweb/confluence_graph.json` (schema: neuralweb.confluence_graph.v1) with typed nodes (engine, sector/complex, regime, thesis, episode-capped-50) and five edge families: feeds (from synapse.yml structural wiring), stable (Oracle graph_s Tier-S all-20 stable pairs + graph_m Tier-M complex-level cap READ-ONLY), leads (Oracle graph_m leadlag, honest null at Tier-M), contradicts (from contradiction detector), confirms (spine co-firing lift, MIN_N=10 floor, lift=null below floor). `engine/neuralweb/contradictions.py` implements 6 typed pairs: pair-A regime-quad vs market_state verdict, pair-B regime_vector growth vs risk_radar scare, pair-C oracle complex direction vs sector_central call tier, pair-D vol_regime low vs market_state RISK_OFF, pair-E briefing divergences ingest (read-only, top-5 snapshot), pair-F cross_asset_confirm diverge from latest.json — severity limited to note|tension (critical prohibited by Article 2). `engine/neuralweb/world_state.py` gains contradictions summary block (n, by_severity, top_pair_ids, display_only=True, fail-open). `scripts/build_confluence_graph.py` CLI added to dag.yml after build_kernel_diagnostics. `config/synapse.yml` gains confluence-graph as 95th artifact (tier: display, weights: none, scored_path_surfaces: []). `tests/test_confluence.py`: 30 hermetic tests covering all 6 pairs positive+negative, MIN_N floor, oracle-absent fail-open, envelope/determinism, world_state block. HARD LAW encoded in artifact payload: confluence never gates, never ranks, never raises a priority; all edges display_only=True. `research/neuralweb/w4w6_scout.json` committed (W4 lane). W4 closed.
- 2026-07-04 — **W6a:** reflex registry ships (`config/reflexes.yml`: 12 registered-only + 2 mirroring = 14 total reflexes; 2 begin mirroring to single-writer firings ledgers, staged per the staging law) + firings fold into the spine index ungraded-honest (`engine/neuralweb/query.py:adapt_reflexes()`; `ledger='reflexes'`, `outcome_graded=False` — grading design documented as future work, no fabrication) + `alert_triage` gains the push spine (`push_priority_alerts()`: priority floor + dedup window on `data/alert_triage/push_sent.jsonl`, config-gated OFF via `alert_push.enabled=false` — enabling is an operator Article-2 event). Whitehouse untouched. W6b (push tier + sender migrations) follows.
- 2026-07-04 — **W6b:** push spine ENABLED for ops-tier — the three noisiest raw senders now route through priority floors + per-lane dedup ledgers (operator noise strictly reduced; disable via `alert_push.enabled: false` in config.yml). Article-2 operator-attention event: `alert_push.enabled=true` is committed with a plain rationale in the config comment block — the migrating senders (basket_freeze, signal_sanity, healthcheck) already push raw to Telegram today with NO dedup; routing them through `push_ops_alert()` reduces noise strictly. The VPS minutes-class endpoint is NOT built — no reflex has a documented production miss; the gate text now lives in `config/reflexes.yml` `meta.vps_endpoint_gate` (evidence-gated like W5b). Signal-sentinel migrations (vector/commodity) = follow-up phase 3. Per-lane design: each sender owns its own `data/alert_triage/push_sent_<lane>.jsonl` (single-writer per file); the dedup window check reads ALL `push_sent*.jsonl` files (read-many, write-one) — no lock needed, no race. Files: `engine/alert_triage.py` (`push_ops_alert()` adapter + `_OPS_LANES` + `_ops_lane_path()` + `_load_recent_sends_all_lanes()`), `engine/basket_freeze.py` (churn_guard send replaced), `scripts/signal_sanity.py` (_notify replaced), `scripts/healthcheck.py` (_notify replaced), `config/synapse.yml` (+3 per-lane ledger artifacts: ops-push-basket-freeze, ops-push-signal-sanity, ops-push-healthcheck), `config/reflexes.yml` (header + meta.push_tier_law + meta.vps_endpoint_gate updated), `config.yml` (`alert_push.enabled: true` with operator rationale), `.github/workflows/daily.yml` (step comment updated + explicit `git add data/alert_triage/`), `tests/test_w6b_push_ops.py` (adapter behavior, severity mapping, dedup window, dispatch-always-when-disabled invariants tested, per-lane single-writer, cross-lane read-many, static guard for all 3 senders, config enabled state). Heartbeat-lane honesty: push_sent_healthcheck.jsonl is written on the runner but never committed (heartbeat.yml has contents:read, no commit step) — the 6h dedup window is INERT for that lane; dispatch-always; noise unchanged vs pre-migration.
- 2026-07-04 — **W4 refinement (operator feedback):** the contradiction detector is now flip-aware — a quad label opposing market_state while its own scale (flip_margin/transition_state/confidence) sits near the boundary is reported as label-lag (note), not tension; the scale fields ride in every record. Ground truth from the operator: the Goldilocks-vs-RISK_OFF record was a verge-of-Stagflation label lag, not a live contradiction. Live episode: flip_margin=0.05 (threshold 0.15), transition_state=TRANSITIONING, confidence=0.327 — all three indicators agreed the quad was near a flip. Kind vocabulary extended to include 'label-lag'; 'directional-opposition' is reserved for the deep-in-quad case where flip_margin >= threshold AND transition_state is STABLE/WEAKENING. Three new hermetic tests in tests/test_confluence.py (cases: near-boundary label-lag, deep-quad tension, near-boundary risk-on no-record); 33 tests total in that file. Fallback path in detect_contradictions updated to carry scale fields from regime/latest.json. Seeds regenerated: confluence_graph.json (7 contradictions: 2 notes, 5 tensions; pair-a is now label-lag/note), world_state.json (n=7, by_severity={note: 2, tension: 5}).
- 2026-07-04 — **W7a PR1:** the constitution in code — Authority Ladder A0-A7 (A7 refusal hard-coded), Article-3 grant evaluator (Wilson lower-bound + staleness lapse), governance ledger (neuralweb.governance.v1). First live act: can_force converted from point-estimate lift (false-grants ~45% at min n under the null) to Wilson-CI lower-bound lift (<6% under the same null) — authority-revoking direction; per-market impact printed in the PR. A6 lane-(i) tuners now log every auto-apply. Tenants (risk_radar_review rewiring+arming, altdata actionable Article-3 review) = PR2.
- 2026-07-04 — **W7a PR2 (completes W7a core):** the constitution governs its first two tenants. altdata_brain's grandfathered actionable:True is now EVIDENCE-DRIVEN through the Article-3 Wilson gate — today's honest verdict: **REFUSED (insufficient-n: n=5 independent date clusters < min_n=25 required)**; the flag auto-re-grants the night the track record clears (earliest: 2026-08-29, per qledger promotion_readiness). The ranked mastermind.json feed carries actionable:false/is_context_only:true until then; the Mastermind bot reads these flags per its own logic; feed SHAPE unchanged (additive fields only). Evidence mapping: n=n_dates (qledger independent date clusters, the canonical unit), hits=round(hit_rate × n_dates), base_rate=0.5 (sign-test floor for direction calls). risk_radar_review rewired through A6 lane-(ii) — every Opus proposal/apply/reject lands in the governance ledger with its pre-committed do-no-harm gate (F1 + hard clamps) — and **ARMED** (enabled:true in config.yml; the correct arm path is the committed config file, not _DEFAULTS; still self-gated behind min_graded=30 + F1 do-no-harm + hard clamps). Arming-predicate doctrine ratified as standing A6 law in engine/neuralweb/constitution.py (A6_LANES + A6_ARMING_PREDICATE_DOCTRINE data blocks — code is the constitution; no second YAML). config_arm governance event written to data/neuralweb/governance.jsonl at PR time (authored_by='w7a-pr2', evidence=self_gates). review_log.jsonl registered in synapse.yml (101st artifact). 21 hermetic tests (tests/test_w7a_tenants.py): Article-3 clearing/refusal/transition/schema, feed-shape, governance emit per path, fail-open, arming-state, A7 hard refusal.
- 2026-07-04 — **W7b PR1:** the cortex runtime ships on shadow probation — Opus-class pinned (D8), bounded read-tool loop over world_state/spine/kernel/graph/governance with bot_mcp-style deny-roots, three shadow-tier write surfaces (attention firings via the W6a reflex rails, committee memo, hypothesis inbox — metabolism lands in PR2), staleness-gated spend, single-call fallback, own Actions job + commit lane (a slow cortex can never gate the deploy). Probation is structural: A2 refused today by grant_authority, printed in the memo; A0/A1 unconditional. Files: engine/neuralweb/cortex.py (tool dispatcher + 7 read tools + 3 shadow write tools + staleness gate + budget enforcement + single-call fallback + constitution wiring + _ALLOWED_TOOLS whitelist A7 guard), scripts/run_cortex.py (thin CLI), tests/test_cortex.py (36 hermetic offline tests — all pass), config/reflexes.yml (+cortex_attention mirroring entry), config/synapse.yml (+cortex-memo + cortex-attention-firings + hypothesis-inbox artifacts), config/dag.yml (+cortex lane), .github/workflows/daily.yml (+cortex job with own commit lane, timeout-minutes:30, retry-rebase push loop, exit-0 always). Dry-run verified: scripted sequence (read_world_state → query_spine → read_contradictions → flag_attention → write_memo) against real artifacts, MOCKED model, produced correct memo.json + firings.jsonl with claim_id=d298ac6676d5fa23, is_context_only:true throughout.
- 2026-07-04 — **W8a:** operator HQ ships — the Neural Web admin tab: registry-wide SLA board, kernel + decision-gate status, reflex/cortex panel (memo, probation, hypothesis counts), governance ledger view. Composes committed artifacts only; deploy = push + VPS pull (+ systemctl restart admin.service noted in the PR body). Files: admin/neural_web.py (panel() — four sections: engine_health/reflex_log/bus_graph/governance; reads config/synapse.yml + config/reflexes.yml + data/neuralweb/* committed artifacts; SLA breach math across all 107 registered synapse artifacts; reflex registry summary + firings tail; confluence graph topology; governance ledger + cortex memo + probation status; fail-open per section), admin/server.py (+import neural_web + GET /api/neural_web route), admin/static/app.js (+TABS entry "Neural Web" + RENDER.neural_web with four collapsible sections A-D using existing card/kv/statpill primitives), tests/test_admin_neural_web.py (18 tests: no-engine-imports static check, panel smoke, all section shapes, SLA math, per-reflex fields, fixture-full, fixture-each-section-missing, push-tier candidates, kernel families armed, lagging flags — all pass), research/neuralweb/w8a_w6b_scout.json (scout evidence committed).
- 2026-07-04 — **W8b PR1:** the flagship page ships — committee view (per-ticker provenance sidecars: tier + ladder + kernel + staleness on every line), the living confluence graph (type-colored edges, graph-level refresh pulse, honest subset note), the cortex memo with probation banner, kernel family cards; auth-required (Supabase glass), plain-English framed, compliance disclaimer fixed. Ask-the-brain = PR2. Files: `engine/provenance_sidecar.py` (load_context + build_provenance: 6 per-source row builders covering altdata/radar/us_board/sector_central/oracle/fundamentals; fail-open per source; qual_ladder + kernel cell + spine_ref + staleness_days + is_display_only on every row; US only v1; universe vocabulary fixed for real spine_index shape), `scripts/build_stock_library.py` (+load_context call once in main() + build_provenance in deferred write loop, fail-open, injects into rec["view"]["provenance"]), `templates/committee.html.j2` (auth gate → Supabase MDXAuth glass → sign-in prompt → committee view: ticker search + provenance table with expandable source traces; living confluence graph: radial + force-relax layout, confirms/contradicts colored, feeds filtered, graph-level 24h pulse animation, honest subset note, pan drag, IntersectionObserver throttle; cortex memo: probation banner + is_context_only + summary + what_fired + contradictions + tool census; kernel family cards: IC + horizon curve + Wilson LB + armed badge + staleness; plain-English boxes + fixed compliance disclaimer; bilingual EN/zh; no title-attr i18n violations), `scripts/build_site.py` (+confluence_graph.json + kernel_families.json copy to site/neuralwebdata/; committee.html render; fail-open), `templates/_navlinks.html.j2` (+Committee View link in Policy & Quant Lab column), `site/neuralwebdata/confluence_graph.json` + `site/neuralwebdata/kernel_families.json` (site-served copies), `site/committee.html` (rendered), `research/neuralweb/w8b_scout.json` (scout committed). NVDA committee shows 4 sector_central rows: quantum_computing / mag7 / us_sector_tech / ai_infra. Tests: 38 hermetic tests in tests/test_committee_provenance.py — all pass (staleness helper, qual_state, kernel_cell, provenance row shape guarantees, fail-open load_context, page render smoke + i18n + auth + nav + disclaimer + is_context_only + graph + memo + kernel cards).
- 2026-07-04 — **W8b PR2:** ask-the-brain ships key-optional — POST /api/ask on the existing authed macro-api: request-scoped read-only cortex tools (write tools absent from the schema — Article 1 structurally), question-class budgets (why-fired: 6 / contradicts: 5 / regime: 3 / general: 8; hard cap: 8 calls, ~$0.55/question live), per-user hourly quota (default 10) + global daily (default 200) tracked in JSONL ledger under MACRO_API_STATE_DIR (/var/lib/macro-api/), advice post-filter (regex catches buy/sell/should+position → replaced with refusal + citation list), memo-quote fallback (degraded=True, serves world_state verdict + cortex memo excerpt matched to question keywords — works today with no key; operator arms live mode via ANTHROPIC_API_KEY in /etc/macro-api.env + systemctl restart macro-api). SSE streaming variant POST /api/ask/stream (tool turns synchronous, synthesis turn streamed). Key-optional ruling adopted: mode='memo-quote' is useful without the key. Inject-defences: 500-char cap, keyword reject (ignore previous instructions / system prompt / override / tool_result JSON shapes), read-only whitelist (structural — write tools not in schema), deny-roots guard in cortex._check_deny_roots, tool results data-only (never routed as instructions), system prompt instructs to ignore instructions in tool results. Files: engine/neuralweb/ask_brain.py (sanitize_question + _classify_question budget routing + _read_tool_schemas 7-tool read-only subset + _dispatch_read_tool whitelist + _check_and_increment_quota JSONL ledger + _memo_quote_response excerpt matcher + _post_filter_advice advice-pattern post-filter + _run_ask_loop + _run_ask_loop_stream SSE generator + ask() + ask_stream() public API), app/main.py (+AskRequest pydantic model, +POST /api/ask, +POST /api/ask/stream, +sys.path inject for engine imports, +DEPLOY NOTE in docstring), app/requirements.txt (+anthropic>=0.28,<1.0 + pydantic), tests/test_ask_brain.py (43 hermetic offline tests — all pass: routing budgets for all 4 classes; write tools absent from schema; dispatch refuses flag_attention/write_memo/stake_hypothesis; memo-quote with/without world_state/memo; per-user hourly quota enforced; global daily quota enforced; quota increments; quota fail-open on bad state dir; advice post-filter positive-control 'you should buy NVDA' → refused; price-target filter; recommendation filter; factual narration passes; citations shape + cap at 20; SSE stream no-key yields memo-quote event; SSE injection rejected; all sanitize_question cases (ignore instructions / system prompt / override / JSON shapes / length cap / empty / valid EN/ZH); _run_ask_loop single-turn; tool-call-then-end-turn; write-tool-call refused from census; full ask() flow with mocked build_providers), research/neuralweb/w8b_scout.json (scout evidence committed). Quota state dir setup: mkdir -p /var/lib/macro-api; the PR body documents this. The flagship page wires the box in PR3.
- 2026-07-04 — **W7b PR2 (completes the W7b core):** the hypothesis metabolism is live under anti-mining law — server-side registration (budget 3/week, retire-one-to-file-one, fdr_family=cortex, registered_at never model-supplied), the generic evaluator grades ONLY strictly-post-registration data against pre-committed gates (passes queue for the quarterly cortex FDR batch; promotion beyond shadow still requires the standard gauntlet), and the attention queue now grades nightly with the A2 earn-in running through grant_authority (refused today — probation printed). The cortex can now propose, be graded, and earn — never mine. Files: engine/neuralweb/metabolism.py (register_hypothesis + retire + budget enforcement + trial-ledger wiring + governance events), scripts/evaluate_cortex_hypotheses.py (generic evaluator: PATH A qledger/spine for lead_lag/sector_conditional/conditional_regime, PATH B walk_forward for entry_quality; strict as_of>registered_at filter at entry; pre-committed-gate-only verdict; article3_review governance event; FDR family='cortex' throughout), scripts/grade_cortex_attention.py (attention grader: realized_move/escalation/verdict_change falsifier classes; sidecar grades.jsonl; A2 earn-in via constitution.grant_authority; probation.json single source; governance events on transitions only), engine/neuralweb/cortex.py (stake_hypothesis now calls metabolism.register_hypothesis live; probation.json single source replaces inline evaluation; tool description updated to PR2 reality), engine/neuralweb/query.py (adapt_cortex_attention: 8th adapter; firings + grades join by claim_id; ledger='cortex_attention'; outcome_graded=True when graded+direction!=0; direction=0 stays ungradeable by design; LEDGER_ENUM extended), engine/experiments_registry.py (_refresh_cortex_evaluator hook + _load_machine_registry_entries + cortex_evaluator hook in _HOOKS; cortex_hypothesis kind auto-populates admin Experiments tab), config/synapse.yml (+machine-registry + cortex-attention-grades + cortex-probation artifacts; hypothesis-inbox PR1 note updated), config/dag.yml (evaluate_cortex_hypotheses + grade_cortex_attention steps added to cortex cluster), .github/workflows/daily.yml (two new steps in cortex job; commit lane expanded to cover machine_registry.jsonl + governance.jsonl), tests/test_metabolism.py (registration schema validation + server-side registered_at + budget enforcement: 4th registration rejected + retire-then-file works + governance events; evaluator strict post-registration filter fixture + sabotage test; pre-committed-gate-only verdicts; attention grading per falsifier class; grades join in adapt_cortex_attention; A2 evaluation refused today; transition-only governance events). A2 earn-in evaluation today: REFUSED — empty attention record (n=0 < min_n=25; hits=0 < min_events=8). Probation continues; A0/A1 unconditional.
- 2026-07-05 — **W8b PR3 — PROGRAM BUILD-OUT COMPLETE.** The ask-the-brain box ships on the committee page (live mode armed via the OAuth deploy workflow). Every wave of the masterplan is now live: D7 hygiene, W0 Synapse (registry + envelopes + read-gate + bus doc), W1 World State, W2 spine federation (287K-claim index), W3 reliability kernel (estimates nightly; decisions quarterly-fenced, first batch 2026-10-01), W4 confluence graph + flip-aware contradictions, W5a validate-first conductor (lane drift reds CI; W5b remains gated on W5a proving insufficient — it has not), W6a reflexes + W6b ops push spine (VPS minutes-tier evidence-gated), W7a constitution (Wilson-gated authority, governance ledger, two grandfathered powers already reviewed), W7b cortex on shadow probation + anti-mining hypothesis metabolism, W8a operator HQ, W8b customer flagship. **The program transitions from BUILD to OPERATE.** Standing clocks: cortex A2 earn-in accruing (n>=25 graded attention events); kernel decision batch 2026-10-01; market_state weights verdict ~2027-05 (component logging accruing); qledger substrate ruling open until QI W6 fires (~2026-08-29 projection); sender phase-3 migrations open.
- 2026-07-06 — **Live-activation wave (Codex docket adjudicated + shipped same-day):** Codex `research/NEURAL_WEB_LIVE_ACTIVATION_RESEARCH_BY_CODEX.md` verified by 6-lane census then adjudicated in `research/NW_LIVE_ACTIVATION_ADJUDICATION_BY_FABLE.md` (RUL-LIVE1..9): 4 of 5 lanes BUILD (2 improved), 1 KEEP. Shipped: **#1625 cortex honesty** (llm_auth-waterfall provider failover, structured run_status ok|warn|degraded|skipped + provider_attempts, context_stale engine-push-failure detector, admin status pill + committee bilingual degraded notice — the 2026-07-06 zero-tool-call-but-green run can never recur silently), **#1621 bottom sensors wired** (benchmark gate resolved 2.29s ≤ 30s; registry-cadence/workflow mismatch killed), **#1631 health.json two-phase vitals** (neuralweb.health.v1 derived from synapse.yml, r2-aware, envelope-sidecar row counts, workflow_conformance flags; engine job builds core → cortex job --refresh-cortex narrow-commits same-run cortex status per RUL-NW1 pattern), **#1637 deterministic daily brief** (neuralweb.daily_brief.v1: did_the_brain_run / what_changed / what_contradicted with no_longer_present-never-"resolved" / what_is_stale / operator_attention P1-P3 maintenance-only; as_of-keyed history upsert via cortex --finalize; trading-verb blacklist test + runtime scrub of upstream-sourced strings; qi structurally excluded; committee Daily Brief block client-side + admin section), **#1620 duplicate synapse count pin de-duplicated** (test_half_lives 113-vs-168 drift → floor+membership). Opus review catches, all fixed: fallback degraded:false mislabel (PR-A); dag-conformance per-job cortex-lane declaration (PR-C); upstream contradiction trading-verb passthrough via dead scrub guard, live repro built (PR-D). No new authority anywhere (health tier=infrastructure, brief tier=display, weights none); kernel FDR 2026-10-01 and cortex A2 (n≥25, hits≥8, Wilson-LB) locks untouched. Known ops item now VISIBLE by design: Mac Studio cortex provider credentials/network — nightlies report cortex degraded with provider evidence until the operator fixes it.
