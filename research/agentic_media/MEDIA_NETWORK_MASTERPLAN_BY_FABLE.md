# Media Network — automated publication estate (masterplan, docket D14)

**Program:** Agentic Media / Media Network · **Author:** Fable · **Date:** 2026-07-25 · **Status:** CHARTERED
**Rev 3 (operator ruling):** additional domains are **supported, not forbidden** (§7); the transformation rule lightens to its real form — cover any story you like, never lift another outlet's prose (AM-R4 rev 3), which the close-paraphrase validator already enforces as a *quality* gate. Masthead/ownership-disclosure mandates are struck.
**Rev 4 (operator directive, 2026-07-26):** two **named publications** are chartered now, each with a dedicated domain and an anchor X account (§1b): **Mastermind News** (`mastermindx.ai`) and **Mastermind Research** (`blog.mastermind-x.com`). §7's "earn the domain with velocity" sequencing is overridden for these two — the operator has ruled them in; §7 still governs any third property. W1 scope is unchanged (engine + two desks on `/blog/`); the properties stand up in **W1.5** and desk output cuts over there. A **PRESS-FEEDS** lane (extends docket D05 — wire-relay posture, its scrape-lane kills stand) supplies the live-feed spine: curated source register (Zerohedge + operator-identified high-quality sources), feed poller, headline wire for the Mastermind News X anchor, and story candidates for the Brief planner.
**Umbrella:** `AGENTIC_MEDIA_PROGRAM_BY_FABLE.md` (AM-R1 posture, AM-R4 transformation rev 3, AM-R7 measurement, AM-R9 monetization)
**Extends:** the D12 free estate (`scripts/build_free_content.py`, `content/seo/blog/*.md`, `templates/seo_article.html.j2`, `/blog/` already in `app/regwall.py` PUBLIC_PREFIXES + all Caddy matchers); Beacon SEO Director (`engine/marketing/seo_director.py`, weekly 0–100 audit); Search Console ingestion adapter (#3160 — currently dark, needs the operator's GSC service-account credential); Research Vault (`data/research_vault/catalog.json`); Chronicle packs (sibling masterplan).

Goal: turn the 6-post hand-written blog into an **automated daily publication** that earns search traffic, backlinks, owned audience, and subscription leads — then additional publications, on their own domains where that serves the brand, as each earns the content velocity to sustain one. The operator's Zerohedge observation is adopted as design: one strong publication per brand, one primary X account per publication.

## §0 ACCEPTANCE GATES

**Ledger law (all waves):** `data/press/published.jsonl` is a press-workflow-owned receipt ledger on the sanctioned outbox precedent — the press publish workflow is its sole writer, with explicit `git add` scoping in that workflow (mirror of marketing-publish.yml's pattern); no other lane appends.

**W1 (press engine + first two desks) not done unless:**
1. 10 consecutive generated articles (mixed desks) each pass the full validator suite (§5) with zero manual edits; validator failures quarantine (regenerate max twice, then drop the slot — a thin day beats a padded one).
2. **Source law (amended at W1 review):** a piece whose slot carries an **external** primary source (PRESS-FEEDS register/wire items) names that source by its full name and links it above the fold. Fully first-party pieces (engine/chronicle/vault-sourced) carry their receipts inline instead — dated first-party facts, fact-anchored — and may link only PUBLIC first-party pages from the config allowlist; linking a regwall-gated page as a source is a validator failure. The **our-value ≥40% gate is computed at sentence granularity** plus a per-desk minimum anchored-receipt count, printed in the `validator_report` stored in `data/press/published.jsonl`.
3. Every article carries a desk byline + the standing not-investment-advice footer (the footer is the one that matters — we publish market commentary).
4. Frontmatter exactly matches the existing contract (`family: article` for all three desks; `slug` == filename stem, no date prefix — dedupe lives in the published ledger, not the filename; required keys slug/family/title/description/published/updated per `scripts/build_free_content.py:_validate`), and the §5 suite pre-checks title/description lengths so drafts fail inside the press lane, not at the site builder.
5. `python -m scripts.build_free_content --check` is green AND **newly wired into CI** (paths-filtered on `content/seo/**` + the seo templates) — it is currently a manual command; W1 makes the drift law enforceable.
6. Posts render on `/blog/` with correct JSON-LD Article and RSS updated by the builder. **Sitemap ownership stated and honored:** the free-content builder does NOT write `sitemap.xml` — entries land on the next nightly render's core-sitemap pass; the press lane commits `content/seo/blog/` + `site/blog/` only (never `site/sitemap.xml`), accepting the indexing lag, and one press URL is verified present in `site/sitemap.xml` after the next nightly completes (end-to-end, once).
7. Zero regwall/Caddy edits needed (staying under `/blog/`).
8. Generation runs off the render path in a **new publish workflow modelled on `marketing-publish.yml`** (none exists for the free estate today — the current 6 posts are hand-run) with an explicit per-day token budget + circuit breaker; a kill switch (`PRESS_PUBLISH_ENABLED` repo variable, same pattern as the marketing publisher's #3361 switch) dark-ships the whole lane.
9. **Research Desk precondition:** vault SEO landing pages (`/research/<slug>.html`) confirmed live — they shipped dark from #3392 and were lit by **#3487** (2026-07-25, render-lane coverage + loud fail-soft); re-verify one live URL before the desk starts linking them at volume.
10. **Publication registry (rev 4):** `config/press.yml` declares the three publications (§1b) with the desk→publication mapping and per-publication domain/brand/anchor-account fields; every `data/press/published.jsonl` row carries `publication`. W1 output roots stay on `/blog/` — the cutover is W1.5's deliverable, not W1's.

**W2 (cadence + measurement) not done unless:** 2 weeks at target cadence with zero validator overrides; the operator has provisioned the GSC service-account credential and Search Console rows flow per URL into the D12 adapter store; Beacon weekly score not degraded vs pre-launch baseline; per-desk scorecards (sessions, impressions, CTR, email captures, D07-tagged trial attributions) rendering in admin.

**Any additional publication/domain (W4+) not done unless:** it has enough distinct desk output to sustain its own velocity (§7), the operator ratifies name/brand/domain, and — if it lives on a new path of the main site rather than its own domain — the regwall/Caddy dual-mirror is edited for the new prefix (`app/regwall.py:60` PUBLIC_PREFIXES + the Caddyfile not-path matcher lists) so it is never silently behind the regwall.

## 1b. Named publications (rev 4)

| Publication | Domain | Desks | Anchor X account | Content |
|---|---|---|---|---|
| **Mastermind News** | `mastermindx.ai` | The Brief (+ wire shorts once PRESS-FEEDS lands) | Mastermind News | market/news stories genuinely rewritten from the curated source register + live feeds, our data + Chronicle context added (AM-R4 rev 3); the X anchor posts breaking headlines as 1–2 sentence wire posts on the breaking-dispatch rail (#3478) plus article shares |
| **Mastermind Research** | `blog.mastermind-x.com` | Research Desk | Mastermind Research | state-of-the-art research-paper / institutional-report analysis from the vault catalog; weekly cross-report synthesis; links the vault `/research/<slug>.html` funnel |
| **Flagship blog** | `mastermind-x.com/blog/` | Editorial (W3) + existing evergreen | existing desk accounts | receipts-culture longform on the money domain; archive of pre-cutover Brief/Research pieces |

Estate rules: each publication is a real editorial identity (distinct front page, voice, cadence — never a near-duplicate of the flagship); AM-R4 + the §5 validator suite apply identically everywhere; corrections append visibly everywhere; the §5 house-style law (no AI-tell register) applies to every piece. The registry is config, not code: `config/press.yml` maps desks to publications, so the engine stays destination-agnostic and the W1.5 cutover is a config flip plus infrastructure, not a rewrite.

**W1.5 (property stand-up) ships:** publication front pages + article templates for both domains (designer lane — taste pinned before build); per-publication output trees, RSS, sitemap, JSON-LD; Caddy vhosts (the `mastermindx.ai` block **replaces** #3772's parked redirect; `blog.mastermind-x.com` is a new vhost + Spaceship DNS record); desk cutover flip in `config/press.yml`; and the **operator provisioning checklist** (DNS records; creation of the two anchor X accounts — naming per Persona lint; Buffer channels for them). Engines never block on operator steps: until cutover, both desks keep publishing to `/blog/`.

## 1. The three desks (start with two, add the third when Chronicle W1 lands)

| Desk | Content | Source spine | Cadence target | The honest wedge |
|---|---|---|---|---|
| **The Brief** (aggregation, attributed) | 300–600w: what happened, why it matters, what to watch — on the day's 3–5 most attention-worthy market stories | breaking/news desks + movers + chronicle short pack + our engine stats | 2–3/day (W1: 1–2) | speed + our-data receipts; every piece links its sources prominently (Techmeme/Axios idiom, AM-R4) |
| **Research Desk** (institutional coverage) | "What the street is telling clients": single-report **commentary** + weekly cross-report synthesis ("5 banks on positioning this week"), stance map by institution — the report's substance restated in our own words with our analysis on top; we don't republish source material or redistribute PDFs | vault catalog `summary_points` + chronicle medium pack | 1/day + 1 weekly synthesis | **nobody else automates this because nobody else has the vault**; links the vault `/research/<slug>.html` (live since #3487) → Pro funnel |
| **Editorial** (original thesis) | 900–1,500w pieces: named theses with mechanism, falsifier, and scheduled review; monthly "state of the story" epoch pieces | chronicle medium/long packs + engine surfaces; best pieces promoted to site Research Reports **behind a non-LLM-origination check** | 2–3/week | receipts-culture longform; the only desk allowed a strong voice, and it grades itself in public |

**Editorial thesis-provenance contract (mirror of Persona §4, LLM-origination law):** every named thesis in an Editorial piece carries a `seed_ref` to a calibrated engine/signal surface or a Chronicle narrative id; the LLM writes mechanism/prose/falsifier language *around* the seeded call and may de-escalate, never originate or strengthen it. The §5 validator hard-fails a draft whose thesis has no seed reference. Promotion into site Research Reports is an authority move and passes the same check plus main-loop review.

**On the "rewrite curated blogs" lane the operator asked for:** it is built — as The Brief. Covering the same stories another outlet covered is normal publishing and fully open; the piece is genuinely written by us, from the facts, with our data and Chronicle context added. The one mechanical line (§5 close-paraphrase detector) is that we never lift another outlet's prose — which is simultaneously the copyright-safe form and the "higher quality than the original" outcome the operator wants. What stays out: scaled programmatic "is $X a buy" article farms (guerrilla doctrine §2.4 rejects them and Google's scaled-content-abuse policy names them), and any desk publishing engine signals as advice (plain-word stance law carries over).

## 2. Why this earns traffic (and the failure it avoids)

Search rewards what the March-2024+ policy era punishes elsewhere: original data, first-party expertise, consistent publication identity. We have original data nobody else publishes (engine stats, vault coverage, receipts). The failure mode to avoid is the 2024–26 graveyard of AI content sites: high-volume derivative text, no identity, no proof-of-work → deindexed as scaled content abuse. Every desk's design forces the differentiator into the piece structurally (§0 W1 gate 2's computed our-value metric).

## 3. Identity + corrections

Each publication carries its own desk byline and editorial identity; there is no ownership-disclosure mandate (rev 3). The one editorial law we keep because it is our own moat: **corrections append visibly** (errata block on the piece), never silent-edit — same law as chronicle epochs, and the same receipts culture that makes the accounts credible. A short editorial/methodology page per publication is optional and desk-owned — build it where it helps trust, not as a compliance artifact.

## 4. Pipeline (`engine/press/`)

`desk_planner.py` (deterministic: pick today's stories/reports from source spines; dedupe vs `data/press/published.jsonl`) → `writer.py` (LLM per desk: prompt = desk contract + chronicle pack via the Chronicle-W2 injection helper + source facts + house style; Opus for Editorial, cheapest-passing for Brief per AM-R6) → `validators.py` (§5) → emits `content/seo/blog/<slug>.md` on the **existing frontmatter contract** (gate 4) → the existing free-content builder renders `site/blog/` + RSS (sitemap on next nightly, gate 6) → the **new press publish workflow** commits (gate 8). Ledger: `data/press/published.jsonl` (append-only: id, desk, sources[], seed_refs[], validator_report, urls — §0 ledger law).

## 5. Validator suite (the anti-slop gate, all deterministic)

**Corpus separation + precedence (so the gates don't fight):** fact-anchoring applies to **numbers and explicitly-attributed quotes** — every number/quote in the draft must appear in the supplied source facts (no originated numbers); attributed-quote spans are then **excluded** from the shingle computation. The **close-paraphrase detector** runs over the draft's *unattributed prose* against the **raw source document** (not the extracted fact strings); overlap above the declared threshold (shingle Jaccard, set in config, not raisable without an operator ruling) = hard fail.

Full suite: fact-anchor check; **thesis-provenance check** (Editorial: every named thesis carries a resolvable `seed_ref` — §1); quote-length lint (≤2 short attributed quotes per piece); **close-paraphrase detector** (the AM-R4 line — draft's unattributed prose vs the raw source document, above-threshold shingle overlap = hard fail) + 30-day self-similarity radar vs our own posts; **our-value ≥40% metric** (computed per §0 gate 2); banned-lexicon + advice-lexicon (Sentinel lists); not-investment-advice footer present; frontmatter pre-check (title/description lengths, family, slug); cheese-test voice validator (v3 port); length/structure budget per desk. Failures quarantine (max 2 regenerations, then drop the slot).

**House-style law (rev 4, operator: "advanced natural language").** Pieces read as sharp human financial writing, never AI boilerplate. The banned-lexicon list extends with an **AI-tell register** enforced at validate time like the rest of the lexicon law — seed list: "delve", "landscape" in the "in today's fast-paced/ever-evolving landscape" construction, "it's important to note", "it's worth noting", "in conclusion", "game-changer", "navigate the complexities", "underscore(s) the importance", "a testament to", "in the realm of", "moreover" as a paragraph opener, and formulaic "not only X but also Y" stacking (pattern-matched, budgeted at ≤1 per piece rather than banned outright). The list lives in config beside the Sentinel lexicons and grows by observation. No AI-authorship disclosure is required (rev 3 struck it) — quality is enforced by validators, not labels.

## 5b. W2R — research triage & tiering (rev 4 addendum, chartered 2026-07-27)

Operator directive: ~150 institutional reports enter the vault daily; gauge which deserve articles ("importance / relevance / quality / sensationalization") and rule the engine economics. Design, per house epistemics (engine originates scores, LLM may only de-escalate):

- **W-score (deterministic, engine-owned — `engine/press/research_triage.py`, weights in `config/press.yml`):** source-institution tier; cross-report cluster density (multiple institutions on one theme in a trailing window = importance); relevance overlap (report tickers/themes vs live movers, watchlists, hot chronicle threads); extraction quality (structure/depth of vault `summary_points` — a thin extraction cannot feed a good article); **attention potential** (headline-grade deltas: regime flips, big-name coverage, outsized moves) as a RANKING input only — the §5 validators still ban unearned urgency framing, which is what keeps the brand credible while competing for the same attention; novelty (inverse 30-day self-similarity vs our own published estate). Every report's score prints to the ledger — skipped reports carry their scores (nulls printed, not hidden).
- **LLM veto pass (de-escalate only):** cheapest-passing model reads the ranked head and may DEMOTE (thin/paywalled/duplicate) — never promote. Full 150/day coverage ≈ $5/mo.
- **Tiering:** top 2–3/day → Research Desk flagship analysis (chronicle medium/long packs + Neural-Web context — the moat; a bare summarizer is a commodity); next ~12 → desk notes (haiku-tier, 300–500w). Triage ranks ALL inflow daily regardless of armed slots; **publish volume is a GSC-evidence-gated config knob** (W2 scorecards), never a day-one 1,500/mo — a cold domain at that cadence is the scaled-content-abuse profile Google deindexes.
- **Engine economics (ruled 2026-07-27):** Claude API via the existing llm_auth waterfall — ≈$0.01/desk-note (haiku), ≈$0.06–0.13/flagship (sonnet/opus tier), <$40/mo at the most aggressive volume and roughly half via the Batches API. **ChatGPT Pro consumer subscription REJECTED** (no API access; automating the consumer app is ToS-fragile and headless-hostile; 5–15× the cost). **Local LLM REJECTED for prose lanes** (12GB-VRAM 7–14B class underperforms the validator suite on financial longform; new ops surface; ~$25/mo savings) — revisit only at ~10× volume or under a privacy constraint that does not exist for our own vault summaries.
- **No free web browsing in the writer** (fact-anchor law: every number traces to supplied sources); web enrichment arrives only as controlled fetches into the fact pool via the PRESS-FEEDS register.

## 6. Distribution

Native-first on X: each piece becomes a value-complete post (the stat/chart/claim in-post), with the link in a reply or card — link-bearing posts tend to see softer organic reach, which is a tactical reason to lead with in-post value (Buffer's subscription covers scheduled link posting, so X's per-post link pricing does not apply to us). The flagship desk account carries Brief/Editorial shares until the anchors exist; **the two rev-4 publications each get their anchor X account at W1.5 provisioning** (operator creates; Persona branded specs govern their voices) — Mastermind News additionally runs the wire lane: breaking headlines as 1–2 sentence posts on the breaking-dispatch rail, sourced from PRESS-FEEDS, at a cadence profile the Persona W2 resolver governs. Any third publication earns its account per Persona §2 cohort rules. Cross-account citation follows the umbrella §5 answer 2: a publication's own account and the flagship may each share a piece once with distinct framing; everything else is asynchronous and differently worded, radar-checked. RSS ships day one (already in the builder); newsletter digest joins the D07/lifecycle lane later (owned audience).

## 7. Additional publications + domains (rev 3 — supported)

Additional domains are a **sequencing and economics question, not a permission question**. The flagship `/blog/` estate goes first because it compounds the money domain's authority and needs zero infrastructure work (already crawler-public). A second property earns its slot when a desk has enough distinct output to sustain its own publishing velocity — a new domain starts at zero authority, so the binding constraint is content supply, not policy.

Practical guidance when we do spin one up: give it a real editorial identity rather than a near-duplicate of the flagship (distinct beat, distinct voice, distinct front page); avoid reciprocal link schemes between the properties built purely for ranking (natural, occasional, relevant links are normal — the operator already plans to reduce link-scheme usage); and give each domain enough velocity that it looks like a publication rather than a parked feed. Both models work — `mastermindx.co/.info/.org` variants for a family look, or fully separate brands for independence — and the choice is a brand call, not a compliance one. Suggested pace: one additional publication at a time, each proven on velocity + traffic before the next.

## 8. Monetization

Subscriptions first (Research Desk → vault Pro funnel is the designed lead path; D07 UTM attribution measures it). Ads per AM-R9's five-condition gate (Beacon floor + trailing-30-day our-value pass + density cap + operator ruling + commercial-viability floor). Affiliate/creator syndication per D11 when ratified.

## 9. Waves

| Wave | Ships | Model lane |
|---|---|---|
| **W1** | `engine/press/` planner+writer+validators, Brief + Research desks at W1 cadence, **new press publish workflow** + kill switch, published ledger, errata mechanism, `--check` CI wiring, admin Press panel (drafts/quarantine/scorecards read-only) | Opus `builder` for engine/workflow; admin panel + any `/blog/` template change via pinned markup or `designer`; runtime generation tiers per AM-R6 (Brief cheap, Editorial opus) |
| **W1.5** | Named-property stand-up (§1b): publication front pages + templates for `mastermindx.ai` and `blog.mastermind-x.com` (designer lane), per-publication output/RSS/sitemap/JSON-LD, Caddy vhosts (replace #3772's parked redirect), Spaceship DNS records + anchor X-account creation + Buffer channels (operator checklist), desk cutover flip | `designer` for surfaces; Opus `builder` for plumbing; operator: DNS + accounts — **engineering SHIPPED 2026-07-27**: both properties built + committed dark (empty-state trees, deterministic builder, Caddy vhosts commented; cutover = paired `cutover: true` + vhost uncomment in ONE commit, CI-enforced both directions and green-proven in the simulated cutover state). Remaining: operator DNS, anchor X accounts, Buffer channels, then the cutover PR per app/deploy/README.md |
| **PRESS-FEEDS** (parallel after W1) | Curated source register `config/press_sources.yml` (Zerohedge + operator-identified sources; wire-relay posture per D05 appendix — no scrape lanes), live-feed poller extending D05 ingestion (replaces the dead finviz provider path), headline→1–2-sentence wire lane into the outbox for the Mastermind News anchor, story candidates into the Brief planner. **Trump-wire source register + speed-posture amendment (operator 2026-07-27): [`../marketing_dockets/D05_ADDENDUM2_TRUMP_WIRE_FAST_SOURCE_SPINE.md`](../marketing_dockets/D05_ADDENDUM2_TRUMP_WIRE_FAST_SOURCE_SPINE.md)** — licensed wire push (Alpaca/Benzinga WS) + trumpstruth/CNN Truth-mirror feeds + VPS-resident runner + P2 speech desk; scrape kills stand | Opus `builder`; D05 is the docket of record |
| **W2R** (with/after PRESS-FEEDS) | Research triage & tiering per §5b: `engine/press/research_triage.py` W-score + haiku veto pass + tier knobs in `config/press.yml`; planner consumes the ranked shortlist; scores in the ledger | Opus `builder`; weights ratified in main loop |
| **W2** | Cadence to target, per-desk scorecards + Search Console wiring (credential precondition), weekly ops report in admin | Opus `builder` |
| **W3** | Editorial desk (needs Chronicle W1 narratives), thesis-provenance validator live, Research Reports promotion path (main-loop reviewed), newsletter digest | Opus `builder`; promotion review in main loop |
| **W4+** | Spin-out per §7 gates; zh-language desk evaluation | Ruling-gated; publication design via `designer`/Fable per AM-R6 |

## 10. Risks (printed)

- **Cold-start**: a 6-post blog has ~zero authority; expect months of thin search traffic. The Research Desk is the differentiated wager (unique corpus) and X-native distribution carries the interim. Scorecards make "it's not working" cheap to see; kill gates per desk at 12 weeks.
- **LLM cost**: bounded by per-day budget + circuit breaker; Brief on cheap models, Opus only where the validator suite demands it.
- **Vault dependency**: Research Desk inherits vault ingestion health (hourly lane) and the open MarketDesk licensing question (we write commentary + our own analysis rather than republishing source material, which keeps it inside normal press practice); `/research/` landing pages are live as of #3487.
- **Render-lane interplay**: the press lane never touches `site/sitemap.xml` (nightly owns it) and commits only its own paths — the stale-checkout clobber class (memory: render-lane-stale-checkout-clobber) is designed around, not discovered later.
- **Google policy drift**: Beacon weekly audit is the tripwire; any manual action = immediate cadence freeze + operator escalation (runbook line in W1).
