# Basic Materials R13 — Real identity qualification and navigation boundary

Date: 2026-09-24. Operation: `gmi-basic-materials-research-20260923-sol-001`. Research carrier: Macro #7796, `sol/basic-materials-research-20260923`.

**Result:** Two selected NYSE identity chains now have actual native-reader evidence. Nutrien's Canadian graph node is explicitly unresolved. The common stock-page route has been inspected in committed source, but no live browser or deployed stock-data proof was obtained. This is a completed bounded research qualification, not completion of Materials V0 or permission to publish private evidence.

## 1. The material advance

R12 left all selected company-navigation chains unqualified. This unit did not wait for a Fable reply and did not write another generic integration plan. It downloaded the actual committed identity inputs, imported the real GMI/Data OS modules in an isolated temporary directory, and called `resolve_graph_node_identity` without synthetic identities, transcribed function substitutes or mocked readers.

The exact Macro source/data pin is `3c93f8194f6c2cb19dad21c1347d8b3b8474aa31`. The Materials research branch remains at its own existing base; this inspection is not a rebase, deployment receipt or revision of the identity authority.

| Selected graph node | Native result | Issuer | Security | Listing |
|---|---|---|---|---|
| `co:us:WY` | RESOLVED | `ISS:US-XNYS-WY` | `SEC:US-XNYS-WY` | `US-XNYS-WY` |
| `co:us:NTR` | RESOLVED | `ISS:US-XNYS-NTR` | `SEC:US-XNYS-NTR` | `US-XNYS-NTR` |
| `co:ca:NTR.TO` | NOT_IN_MASTER | unavailable | unavailable | unavailable |

The two positive rows use `master_inception_exact`. Their issuer axes in the security master are also RESOLVED. The master records CIK `0000106535` for WY and `0001725964` for NTR. These values were read, not allocated from a ticker formula.

The reader's current materialized rows have `resolution_asof=2026-09-23` and `computed_at=2026-09-23T19:04:16Z`. Their master generation is `2026-09-21T03:19:29`, with symbol-directory and CIK snapshots dated September 21. The research observation on September 24 must not relabel those upstream clocks as September 24 observations.

The existing master and alias inputs contain 2,380 security rows and 6,035 alias rows. The graph has 3,882 nodes and 112,195 historical identity rows. Those are dataset row counts, not a claim of complete issuer or Materials coverage. Only the selected results and their provenance are retained in the research receipt; this work does not publish another copy of the data spine.

## 2. What the original sources establish

The Weyerhaeuser exhibit at SEC accession `0001193125-26-326124` names Weyerhaeuser Company and NYSE: WY. Its SEC directory belongs to CIK 106535, matching the issuer axis read from the native master. The selected document is dated July 30, 2026. This is strong source-attribution evidence for the chosen research example, not proof that a reviewed GMI curation object has already been retained and admitted.

The Nutrien Q2 issuer page remains the selected original for its economic example. The committed master supplies the native issuer/security/listing relationship and CIK. The research does not claim that this website page contains a native source-to-issuer receipt or that a title match alone is an approved production join.

The separation is intentional: an original disclosure, an accepted identity row and an admitted curation relationship are three different things. The first two now have substantially better evidence for the two NYSE subjects. The third remains a live-source admission requirement under the existing owner.

Originals consulted for identity context, not re-researched financial calculations:

- Weyerhaeuser: https://www.sec.gov/Archives/edgar/data/106535/000119312526326124/wy-ex99_1.htm
- Nutrien: https://www.nutrien.com/news/press-releases/nutrien-reports-second-quarter-2026-results-1753

No issuer report is reproduced in full. No investment recommendation, fresh price target, or current source-rights grant follows from these source reads.

## 3. Nutrien's Canadian node must remain separate

`co:ca:NTR.TO` genuinely exists in the graph. Its latest native result is NOT_IN_MASTER with the reason `no security-master or vendor-alias row resolves this symbol`. It is not an absent company label and must not be silently replaced with the NYSE row.

For the bounded first Materials view, the qualified NYSE identity is a viable candidate company target once route and source-admission proof are complete. That does not authorize a Canadian security link, a cross-listing equivalence, currency conversion or a Canadian portfolio-position join.

The required owner action for the Canadian path is to investigate/admit the missing security/listing evidence through the existing Data OS producer and bridge. A Materials-only ticker map would recreate the identity authority and hide the actual coverage gap.

The `US` market component in the verified listing key is not a new conclusion about corporate domicile or where all the company's assets operate. Company-level economics, listing market and asset geography remain separate dimensions in the Materials model.

## 4. The SEC intake map is not the identity authority

The committed `data/edgar/ticker_cik_ledger.json` has `WY: 106535` but no `NTR` entry. Meanwhile the actual Data OS master and GMI reader resolve the NYSE Nutrien issuer/security/listing.

This is an important integration distinction, not an excuse to overwrite either owner. An SEC intake helper's coverage must not downgrade a valid canonical identity to unresolved. Conversely, a resolved identity does not demonstrate that the intake pipeline collects the selected issuer's documents.

The Materials consumer should use the sanctioned GMI/Data OS reader for company identity and qualify the source collector separately. Do not populate a second CIK registry in the dossier to make these systems appear identical.

## 5. Real historical example: historical naming is not system-known replay

The same company produces two different, legitimate answers under different information sets:

| Question | Result | Actual inputs/time |
|---|---|---|
| What did the persisted bridge say by the end of August 20? | NOT_IN_MASTER | Latest row at `2026-08-20T18:50:58Z`, using master generation `2026-08-20T18:50:35` |
| What does the native historical-name resolver return for August 20 when run against this September snapshot? | RESOLVED | `asof=2026-08-20`, recomputed at `2026-09-24T07:43:04Z`, using master generation `2026-09-21T03:19:29` |

The identity module explicitly documents that an explicit `asof` reruns its naming algorithm over committed inputs, and that exact inception-code matching is as-of invariant. The experiment therefore does not establish a defect in that documented contract.

It does establish a consumer requirement: do not describe that historical-name query as a replay of what Mastermind knew then. R12's theoretical distinction now has an actual production-data example.

For a system-known historical evaluation, use the authorized historical input/recording vintages and retain unavailable identity when it was unavailable. A later identity resolution must not create apparent historical discovery coverage. The single selected persisted row here is evidence of the difference; it is not a new general replay service or a full portfolio backtest.

## 6. The stock-page route is source-qualified, not browser-proven

The committed `site/theme.js` supplies separate market routes. At line 667 the US search library is `stockdata/index.json` and its target is `stock.html`. The Canadian entry at line 670 targets `canada_stock.html`. At line 1089 the existing navigation composes the selected target plus `#` and the encoded selected symbol. `site/stock.html` line 1752 fetches a symbol-specific `stockdata/<safe>.json` record.

The committed vendor-alias rows associate the store symbols WY and NTR with the already-verified NYSE securities. Combining those observed records with the actual route source identifies these candidate destinations:

- `stock.html#WY`
- `stock.html#NTR`

This is not proof that an existing Materials card emits either URL, that the current deployment serves the corresponding record, or that a user can complete the browser journey. The additional native alias-resolver call was not executed; the evidence above is the observed alias records plus the already-executed canonical company resolver.

Three selected raw repository paths (`site/stockdata/index.json`, `site/stockdata/WY.json`, `site/stockdata/NTR.json`) returned HTTP 404 at the pinned Git revision. This establishes absence from those Git paths only. Data may be published separately; no conclusion that the live site or either stock is broken is warranted from those results.

## 7. Platform refusal and exact proof gap

A fresh anonymous Playwright verification action was blocked before it ran with: `This tool call was blocked by OpenAI because we couldn't determine the safety status of the request.` Read-only reconciliation showed no Playwright, browser or page objects had been created. That action is EFFECT_NONE, not an ambiguous site modification. It was not retried through another tool, model, account or browser.

A separate additional batch for alias resolution, other subjects and delivery classification was also blocked and stopped. It supplied no new evidence. Neither refusal is a claim that the whole project or all available tools are unavailable. Research persistence and the export/checking of already obtained observations continued.

The refused actions must not be rephrased or handed to another executor as a workaround. Browser and additional-query proof can resume only under a platform-permitted recovery, not merely because a new chat or model is available. The appropriate live-page acceptance remains owed; this report does not downgrade it to source inspection.

## 8. Native execution and integrity boundary

The successful reader ran on the authorized Studio through existing Python 3.11.10, using its already-installed pandas/pyarrow. No dependency was installed. The default Python 3.14 environment lacked a Parquet engine; Python 3.12 also lacked the required packages. These were environment qualifications, not defects in the application.

The isolated native import initially required the real `lib.dataos` dependency modules. Those exact pinned files were supplied, not mocked. The imported module path was inside the newly created temporary directory. Five focused native calls completed: three current company results, one explicit historical-name result, and one unknown-node refusal (`UnknownGraphNodeError`). This is not execution of the complete application or identity test suite.

The downloaded reader's Git blob was independently calculated as `8eefa1c2f5e5d3514f6487bf869f154ce2751011`, matching the exact-ref GitHub reader source. The other downloaded file sizes, Git-blob hashes and SHA-256 values are in the accompanying observations record. That record distinguishes hashes computed from retrieved bytes from independent connector verification; it does not claim a second attestation for every file.

The five data/input files remained byte-identical after the reader calls. The temporary checkout was removed, and the successful reader process exited with code 0. No user checkout, native production data, alias, policy, company identifier, source writer, shared code or stock output was modified. Null cells returned through pandas are normalized to JSON null in the research receipt, not stringified as identifiers or replaced by zero.

## 9. Consequences for the saved implementation plan

H4 is now partly qualified: two of the selected NYSE subjects have real committed native identity evidence. It remains open because all four subjects still need actual source-admission and published-route proof. H1/H2/H3/H5/H6/H7 are unchanged.

Use these two native chains when specifying the future Materials consumer. Do not regenerate their identifiers from names, reallocate the issuer, reject NTR merely because the separate SEC helper omits it, or assert Canadian equivalence. Bind the eventual response to the actual native input generations; refresh them through the same owners before a release.

The R11 shared-foundation adoption remains controlling. #7870 stays the existing shared implementation; this work adds no second assertion format, private store, current-rights service, generic API/client, K1 binder, evaluator or routing plane. No new Fable request was needed to obtain the native evidence. This unit did not poll or infer current upstream runtime state.

All 44 requirements, five source documents, four company subjects and V0–V4 ambition remain. These records are development evidence, not new live graph facts or accepted prediction features. A richer economic explanation still needs its own validation; correct identity does not establish an investable edge.

## 10. Exact next action

Incorporate the two verified native chains and the actual historical-mode contrast into the Materials consumer's existing input and evaluation requirements. After the shared interface/profile is accepted, the implementer should obtain native reviewed source references and a platform-permitted live company-route result for the selected subjects. The Canadian Nutrien path stays explicitly refused until its native owner supplies evidence.

Do not repeat this identity survey or the successful full-module read merely to produce another status report. Requalify only after a relevant source, data generation, identity or contract changes. Do not retry the refused browser/additional-query batch without an established permitted recovery. Independent economic/evaluation work and lawful shared-decision consumption remain possible; this phase boundary is not a mission-wide wait for Fable.

**FINALIZATION_CLASSIFICATION: CHECKPOINTED_CONTINUATION. MISSION_COMPLETE: false.** This is a substantial native-data/reader qualification boundary with a preserved partial route result, not a product release or a transfer of custody. The cumulative Agent OS checkpoint remains the recovery owner.
