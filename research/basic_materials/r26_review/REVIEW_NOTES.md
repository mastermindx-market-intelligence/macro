# Basic Materials R26 — offline dossier interaction study

Operation: `gmi-basic-materials-research-20260923-sol-001`  
Carrier: research-only Macro #7796, not the #7984 product branch.  
Disposition: **RESEARCH_PROTOTYPE / NOT_APPLIED_TO_PAPER / NOT_DEPLOYED / VISUAL_REVIEW_PENDING**.  
MISSION_COMPLETE: false.

## Artifact and intent

`materials_dossier_review.html` is a self-contained review aid, with no network dependencies or production data. Creation commit `91e4cc70dbb67613be00f59ed57b8392b43dcd26`, Git blob `9e8774f5c5d562ccfca87cbaacd4a3f5135e4895`, 37,786 UTF-8 bytes, SHA256 `a32ebf57630c30b407bab9da5fe63ca94ebd235496dbb56e02a41b9954a2fa71`. Exact-ref GitHub readback matched the locally computed complete-file blob.

The study makes R21's sector-first product tangible without filling absent evidence with invented figures. It implements review controls for English/Chinese, light/dark, three task views (Understand / Compare businesses / Check evidence), ten descriptive materials-family filters and four journey templates. The review-state selector has illustrative, disconnected, access-required and reader-unavailable states. None is a real account/rights check.

The four templates preserve the agreed proof subjects: Nutrien unit margin; NOVONIX qualification versus commercial progress with two distinct original documents; Wheaton/Antamina contractual participation rather than physical ownership; Weyerhaeuser actual investment cash versus adjusted distributions. The comparison contrasts the questions and inference limits, not unlike measurements or a universal score. Each inspect dialog describes required inputs, source scope, next evidence and interpretation limits. No source body, original report, security link, identity resolution, economics calculation or signal is supplied.

The source includes tab hash handling, arrow/Home/End navigation, dialog Escape/focus return and responsive layout rules. **These DOM interactions and rendered layouts have not passed browser verification.** They are authored behavior, not proven behavior.

## Design reference and custody

Read-only Paper reference: file `01M2WGNCX9475G79JRKJTCM08P` / MASTERMIND PAGES; shared-shell page `p-D-0`, artboard `UBT-0`, sidebar `UBU-0` (216px), topbar `UDM-0` (72px), tokens hash `bba69475`. This is structural inspiration, not an approved exact JSX port. Current house design/doctrine sources were read; the intended archetype is C-company/instrument_analyzer.

Light uses cool canvas, white panels, hairlines and shadow; dark uses graphite panel steps and no corresponding shadow. Existing Paper token values inform the standalone study. The file's local CSS/token aliases and inert global-navigation context are **not** production shared chrome. Any later implementation consumes the incumbent shell and `templates/theme.css`; do not copy this isolated shell into a third navigation/token system.

Paper Desktop 0.5.12 inspection/catalog succeeded with write-schema compatibility. During the read-only phase the active page changed from `p-E-1` to `p-P-1`, and a new `p-R-0` page appeared (26 to 27 pages). This is observed concurrent document movement, not proof of an identified writer lease. No Paper edit/page/artboard was created; exclusive canvas custody was not established. The local study does not count as Paper delivery.

## Verification actually executed

`verify_static.py` is an executable non-browser check using Python standard library and installed Node. Run `python verify_static.py` in this directory. Creation commit `9623a20ce3b071279217144fffb0ad11dea923e4`, blob `f1794e828697501e0637b19b1944f1143df31f53`; complete-file local Git blob matched readback.

Fresh execution exited 0: **20 checks passed, 0 failed**. Checks cover unique static IDs, three tab IDs, paired language keys, ten family labels, four distinct journey templates, paired journey fields, three steps/four disclosure sections, family bounds, no external markup URLs/network/storage calls, explicit restrictive CSP, disclosure presence, actual visible/total count usage, no common-score fields, viewport markup, complete JavaScript syntax and six pure count-copy cases. The generated `structure_verification.json` SHA256 was `f526cf189c0bbefd8c154751fb680202ebeb875fb85cbfeb2bd62d9300747242`.

The filter-count helper was added after an observed failing pure-copy check (missing explicit visible/total helper). Six English/Chinese cases for 4, 1 and 0 visible out of 4 then passed. The count is tied to the filtered array rather than always saying four. This is a local prototype-copy test, not an economic or production test.

The verifier parses source and executes only the authored data/count prefix in Node; it does not create a DOM, render, simulate a browser, visit a URL or test auth/rights. The six count cases are included in the 20-check report, not an additional 20+6 product-test claim. Historical R24/R25 scope tests are not rerun or added to this count.

### Browser attempt — explicit denial, not a pass

The bundled Playwright browser was absent. The installed system Chromium could launch, but the first local-file navigation returned `Page.goto: net::ERR_BLOCKED_BY_ADMINISTRATOR`. A single read-only diagnostic found `/etc/chromium/policies/managed/000_policy_merge.json` with `URLBlocklist: ["*"]`. No browser assertion or screenshot was reached. The attempted pre-count-fix HTML had SHA256 `1b8d1bce9a96f800b33260076908e4c85b9c83eb5ff34256f42c7370ad63c599`; neither that version nor the current version has visual proof.

**Browser checks 0; screenshots 0; visual acceptance false.** No policy changes, browser/host substitution, localhost rewrite, `set_content` workaround or navigation retry after the explicit denial. This is a boundary of this local browser environment, not a universal claim about Paper or every browser. Static Node checks were independent non-rendering checks, not a bypass.

## Remaining review and implementation gates

1. Obtain a permitted, source-bound visual-review context or exclusive Paper assignment through the existing owners, without evading the denied local browser action. Then inspect the exact artifact at 1440 and 390 widths, EN/ZH, light/dark; test tab/hash/filter state, keyboard, dialog focus, overflow and all degraded states. The authored browser harness in the portable package is unexecuted beyond the blocked navigation.
2. Resolve visual/copy issues before calling this an accepted design. Known review details: three static landmark aria-labels remain English; some controls use 12px text on mobile; Inter is a system fallback here, not proof of house self-hosted/CJK font delivery. The comparison uses non-semantic visual rows rather than a fully qualified accessible production table. No full accessibility, language or contrast claim.
3. Preserve R25's completed source-local proposal and existing owner request; no new equivalent scope experiment. Shared scope/ref/parser/K1/P5 acceptance, sector identity/outer-inner join and the authenticated private reader remain unreturned/unbound.
4. Product work requires separately reconciled Macro workspace/path custody. #7984's recorded cash-patch denial and frozen TDD order remain intact. Do not convert #7796 into a product branch or import #7870-only code onto an incompatible base.
5. Full acceptance still needs all five original documents/four real company journeys, retention/review/rights, actual identity/company/source navigation, private negative-path proof and independent release/browser proof. A client-side simulated denial is not access-control testing.

No product/shared schema, source record, identity, rights, membership, score, rank, trade, publisher, credential, CI workflow, worker or deployment changed. No Fable receiver, PICKUP_ACK or START is created by this study.
