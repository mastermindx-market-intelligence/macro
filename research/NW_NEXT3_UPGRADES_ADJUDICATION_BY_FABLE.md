# Codex "Next-3 Lobe Upgrade Plan" — Adjudication & Upgrade Program (by Fable)

**Ratified:** 2026-07-06
**Source docket:** `research/NW_NEXT3_LOBE_UPGRADE_PLAN_FOR_CLAUDE_BY_CODEX.md` (Codex, 2026-07-06; committed alongside this adjudication per the #1673 convention). Codex proposed five upgrades each for three organs it ranks 4/5/6 — Short-Side/Breakdown, Options Entry Intelligence, Decision-Quality/Operator Self-Model — plus a cross-lobe DecisionPacket schema and a six-PR build lane (PR-A..F).
**Method:** 4-lane Sonnet census (`wf_41406cd3`, file-level verification of every checkable Codex claim + in-flight collision map) → Fable draft → 2-lens Opus red-team (house-law/stats; build-executability) → Fable ratification. §2 prints census corrections to the Codex doc; §2.5 prints what the red-team falsified in this draft, per house law.
**Sibling programs (same day; ownership seniority where surfaces touch, in ratification order):** #1664 gap-map (owns BD-AVOID-1/PR-C2 [in-flight, worktree codexgap-c2 — prereg + stamper written; its `short_side` budget declares at stamper runtime on the Mac ops lane and is NOT yet on origin/main's trial ledger], operator capture/PR-C4 [not started], WAIT-GRID-1 [merged #1678], evidence panel [merged #1681]), #1666 next-five-lobes (owns DQ-2 grading harness [merged #1669], F-HZ-1 [#1671 open]), #1673 three-lobes (owns claim-accountability audit [#1683 open], `claim_reliability` bridge key [#1680 open], L6-P0, reflexivity wave), #1695 final-3 (ratified while this program drafted; owns TRIM-GRID-1, NET-REPLAY-1, the DISP-GATE-1 harness, ThetaData calibration — no surface overlap with the waves below; its RUL-F3.13 independently fences BD-AVOID-1 as a separate program).

> **In plain English:** Codex wrote a thoughtful institutional wish-list for three organs we already operate. Every number it cites checks out at file level — its evidence work is the best of the external memos so far. But it was written blind to the three sibling programs ratified the same day, so roughly half its build plan is already frozen, in flight, or merged, and two of its proposals are shapes standing law prohibits. What survives is genuinely valuable: measure what the options feature store can actually support and when (coverage audit + readiness forecast), price the avoid lens in avoided-drawdown vs missed-upside terms on the tape we already have (BD-ECON-1), record what the operator was *shown* and not just what they clicked (exposure log), and widen the breakdown grammar cheaply with one batch prereg (Phase-0b). We build those four now, register honest come-backs for the rest, and charter zero new lobes.

---

## §1 Executive verdict

**Codex's evidence census is honest** — all fourteen checkable data claims verified EXACT (BD-1/2/3 episode counts and stop-rate gaps to two decimals; the 403×28 state table; per-feature non-null counts; S-VOI 42/4 degeneracy). Its institutional frames (decision economics, exposure denominators, path-quality vocabulary, avoid-before-short discipline) are worth keeping and several are adopted below. **Its ownership map is stale** — written before/parallel to #1664/#1666/#1673, it re-proposes work those programs froze, dispatched, or merged the same day, and its two cross-cutting designs (multi-evidence conditioning, DecisionPacket chain) are the fused-escalation shape RUL-3 (#1664) and RUL-N2 (#1666) struck.

**Taxonomy:** nothing here charters a lobe. The two-lobe cap stays L1+L3. Codex's "lobes 4/5/6" are respectively an operating chartered lobe (L1 short-side), a program's connective tissue (options→NW), and a rail-consumer (DQ instrumentation) — all upgrades below are waves on existing tissue.

Dispositions (15 upgrades + DecisionPacket + PR lanes):

| Codex item | Verdict | Disposition |
|---|---|---|
| 4-U1 Phase-1 avoid ledger (PR-A/PR-B) | **DUPLICATE_OF_INFLIGHT** | #1664 PR-C2 BD-AVOID-1: prereg + stamper written in worktree codexgap-c2, ledger container committed (0 rows, 23 cols); its `short_side` budget declares at stamper runtime (Mac ops lane) and reaches origin/main's ledger only when PR-C2 merges and runs. The frozen spec is *stronger* than Codex's (≥8pp compensating gate, PIT random-bar control stream, maturity clocks 2026-10/2027-01, short-grade quarantine). No action; do not touch. |
| 4-U2 species expansion (4 preregs) | **GO-MODIFIED** → W-0B | One Phase-0b batch (3 definitions: S4⁻ Two-Clock Rollover, S5⁻ Coiled Breakdown, S13⁻ Leader Fade) on the same tape harness, ONE prereg, declared budget 3 (max()-floor semantics, RUL-U3a). Legal now: the masterplan gates expansion on "Phase-0 base rates", which exist. S7⁻ excluded (masterplan §3 'coordinate, don't duplicate' — S7 is registered two-sided in the entry-species program). EDGAR-gated species stay parked. |
| 4-U3 multi-evidence conditioning report | **KILL as specified; come-back** | As written ("BD × options × dispersion × froth" in one report) it is the prohibited conditioning shape and its dispersion axis has no historical PIT basis until DISP-GATE-1 (PR-B2) ships its reconstruction. The legal version is per-axis registered contrasts. Come-back 2026-10-15 (S-TOP_RISK accrual + B2 basis both resolve). |
| 4-U4 avoided-loss counterfactual | **GO-MODIFIED (build now)** → W-BE | The census lane called it premature; that conflates *forward accrual* with *retro decision-economics*. The Phase-0 tape (26,774 event rows + 80,322 controls) and the graded fire tape (replay_boarded) both exist NOW. BD-ECON-1: Fable-frozen prereg (committed in this PR), declared budget 6 (max()-floor semantics, RUL-U3a), symmetric-cost printing per RUL-N6, BD-2 generating-fire exclusion by construction, floors + DEFER branch, research-only. |
| 4-U5 execution firewall | **DUPLICATE_OF_MERGED** | Masterplan §1 permanent fences + §7 graveyard ARE the firewall. Codex's AVOID>TRIM>HEDGE>SHORT hierarchy adds vocabulary, not law. No action. |
| 5-U1 stamp/backfill/coverage (PR-C) | **SPLIT** | IV backfill = ruling A9 + W-E0 (owned; W-E0 already running, 87/88 roots at 15y). `pin_risk`=0 is BY DESIGN, not a defect (census root-caused: `opex_days≤5` gate; build date had opex_days=14). New stamp fields = W-OVC (come-back 2026-07-20). The unowned residue — a coverage audit + per-family gate-readiness forecast — is adopted → **W-OC (build now)**. |
| 5-U2 path-quality report (PR-D) | **DEFER to existing clock** | All live gate families are building_history (n_cond 0–42); an outcome-conditioned report today is empty or a contamination surface. The *readiness* half folds into W-OC; the *outcome* half reads at `options-entry-gate-maturation` (2026-10-15, already registered — no new registration). |
| 5-U3 pressure-map entry cards | **DUPLICATE_OF_MERGED** | `mastermind_context.py:373` already carries the `options_entry` lobe summarizer + per-candidate sparse options dict (`candidate_context[...]['options']`); `world_state` has the options_weather lobe; entry cards already carry gex_confirm CONFIRM/NEUTRAL/CAUTION + OPEX context. Codex proposed rebuilding what W-B shipped. No action. |
| 5-U4 top-risk → short-side handoff | **OWNED/BLOCKED** | #1664 verdict "DON'T (yet)"; S-TOP_RISK gate 2026-10-15; ABS-1b options-hostile arm already a named deferred item (#1666 §4). No action, no new registration. |
| 5-U5 analogue library | **REGISTER come-back** | Genuinely unowned and a good idea in research form — but premature until the W-E0 single-name manifest completes, and epistemically hazardous as a live surface (a retrieval outcome readout is a score in prose). Come-back 2026-08-15 with prereg-first + research-artifact-only fences (§3 RUL-U7). |
| 6-U1 exposure joiner (PR-E) | **GO-MODIFIED (build now)** → W-EX | The one missing DQ substrate: #1669 grades *actions* against qledger, but nothing records *exposures* (what the operator could have acted on). W-EX builds the denominator tape from committed site artifacts — their git history is the PIT tape (the RUL-C5 move) — so it needs no new capture UI and works without PR-C4; the alerts leg enriches when C4 lands. |
| 6-U2 action grader (PR-F half) | **DUPLICATE_OF_MERGED** | #1669 (merged today): 3 frozen contrasts, `fdr_family='operator'` budget 3, Wilson floor n≥25, BH imported verbatim from btc_override_ledger. Codex's version has no family, no floor, no frozen contrasts. No action. |
| 6-U3 reason taxonomy | **RECOMMEND-route** | Additive and low-risk, but its surface (admin capture UI) is #1664 PR-C4's territory and C4 has not started. Routed as a recommendation to the #1664 program; come-back 2026-07-27 to check C4 landed and dispatch a small follow-up PR if the C4 builder did not fold it in. |
| 6-U4 lobe impact attribution | **REGISTER come-back** | Requires exposure accrual (W-EX) AND #1669 past its floor. Come-back 2026-09-15. |
| 6-U5 weekly review + protocol | **REGISTER come-back** | Below the n≥25 floor it is an empty report. Codex also misidentified `admin/brief.py` as the deterministic brief — it reads the LLM briefs; the deterministic pattern is `engine/neuralweb/daily_brief.py` (#1637). At come-back (2026-09-15), the review extends the deterministic-brief pattern, never the LLM brief. |
| DecisionPacket schema (§6) | **REJECT as apparatus; fold the residue** | A canonical cross-lobe packet that routes short-side → options → decision-quality evidence is RUL-N2's prohibited chain in schema form, and a parallel event apparatus is exactly the sprawl the docket warns against. The useful residue — stable ids, as_of, surface, artifact refs on every exposure row — folds into W-EX's row schema, flat, with no cross-lobe conditioning fields. |
| Codex PR-A/PR-B | superseded by #1664 PR-C2 (above). |
| Codex PR-C/PR-D | split/deferred (above). |
| Codex PR-E/PR-F | W-EX + #1669 + come-backs (above). |

---

## §2 Census corrections to the Codex doc (printed per house law)

1. **EXACT (all fourteen checkable data claims):** BD-1/2/3 episode counts (1,330 / 19,891 / 5,553), 21d stop gaps (+0.58 / +9.66 / +16.81pp), 126d gaps (+5.30 / +8.69pp), control rates, short-favorable rates; state.parquet 403×28 with iv_rank_252=0, pin_risk=0, ivspread_rel=355, skew=386, net_doi=348, fresh_premium_mn=310, gamma_regime=385; S-VOI 42/4; `terminal_state_short` at grading.py:459. Codex measured before writing. One nit: the "126d short favorable near 26%" line generalizes over BD-1 (21.05%).
2. **STALE (ownership, the doc's central defect):** PR-A/PR-B re-propose #1664 PR-C2 (in flight — prereg + stamper + launchd plist in worktree codexgap-c2; empty ledger container already committed; budget declares at stamper runtime, not yet on origin/main's ledger). PR-F's grader half merged as #1669 *while this program's census was running*. "No grading harness / no exposure joiner / action_ledger.jsonl does not exist" are all stale or worktree-visibility artifacts (#1666 §0.4: capture is LIVE, gitignored by design).
3. **STALE (gate.json):** Codex read the pre-W-C v1 gate (47 stamped rows, 5 test families). Current v2 (origin/main): n_ledger_rows=950, n_stamped_rows=950, 8 families in per_family_status, all building_history. Codex's "950 stamped" was wrong at its write time and is coincidentally right now.
4. **MISSED SUBSTRATE (research verdicts):** "all families building history" is true of the live gate but omits that W-E1 historical verdicts EXIST (8/51 survive global BH α=0.10): GEXR era-dependent sign → vol-context only; CWIV Era3 alive; **skew-decel UNSUPPORTED (lone survivor points the wrong way); DOI dead at sector level**. Codex's 5-U4 handoff feature list ("rising skew", DOI-adjacent context) proposes ingredients the gauntlet already demoted; any future handoff study must carry those skeptical priors.
5. **MISSED SUBSTRATE (pressure-map):** `options_entry` lobe summarizer + per-candidate options dict already live in `mastermind_context.py`; options_weather lobe in world_state; gex_confirm cards on entries. 5-U3 would rebuild shipped tissue.
6. **MISSED NUANCE (BD-3):** Codex reads BD-3 as "strongest avoid evidence; still avoid-first." #1664 §0.7 recorded the sharper fact: BD-3 is the ONE definition where 21d short-favorable (37.53%) exceeds short-adverse (34.67%) — not cleanly avoid-only; it is the only latent short-species candidate. BD-AVOID-1 quarantines short-side grades accordingly.
7. **WRONG SURFACE (6-U5):** `admin/brief.py` is the admin reader for LLM-generated briefs (DeepSeek). The deterministic brief is `engine/neuralweb/daily_brief.py`. An Upgrade-5 built on admin/brief.py extends the wrong surface.
8. **BLIND SPOTS:** (a) the two-lobe concurrency cap — never mentioned; (b) SLF-001 (SEC FTD pressure, merged #1660 the same morning) printed NULL/NO-GO on short-side pressure territory — a standing prior Codex's short-side section should have carried; (c) the W-OVC docket (#1674, come-back 07-20) owns the state-table enrichments 5-U1 partially re-proposes; (d) `pin_risk`=0 has a by-design root cause Codex flagged as a gap to "populate."
9. **RIGHT AND KEPT:** avoid-before-short framing; exposure denominators as the missing DQ object; coverage-before-consumption for options; decision-economic pricing of the avoid lens; "if pin_risk is null say unavailable rather than neutral"; per-lobe attribution by decision utility not raw return; blame-free process-analytics framing.

## §2.5 Red-team corrections to this draft (printed per house law)

Process note, printed for honesty: seven suspected defects were identified and pre-fixed by the Fable author BEFORE the review, and the red-team was explicitly asked to adjudicate whether each was real or theater. Both Opus lenses (house-law/stats; build-executability) returned APPROVE_WITH_EDITS, judged **all seven pre-folded fixes real and correctly applied** (C3 co-primary for the BD-2 selection confound; month×ticker two-way cluster bootstrap; S4⁻ self-contained formula + BD-3 overlap obligation; exposure date = artifact `as_of` never run date — verified against daily.yml's collect→engine job split; frozen sha1 id recipe + schema-drift guard for board rows; readiness forecast gated to `unknown` below 14 days of arrivals; serial synapse merges), and falsified or forced the following NEW corrections, all folded:

1. **Budget semantics wrong (stats lens, major):** the draft framed `short_side` budgets additively ("+6", "+3", "family total 5"). `TrialLedger.log_declared_budget` keeps a per-family **max()**, not a sum (trial_ledger.py:106/174) — declared budgets are within-study BH floors and do NOT accumulate cross-study multiplicity. FIXED: RUL-U3a states the max() semantics; both preregs now mandate per-cell distinct-config logging so the family's `literal_n` accumulates honestly, and every output prints the literal count plus the max()-basis divergence note (the #1664 §0.5.6 convention). Acceptable only because all four family studies are descriptive/research-only (no DSR).
2. **Spurious BD-AVOID-1 budget claim (stats lens, major):** the draft asserted the AVOID-1 "+2 budget logged 13:26Z" as an on-ledger fact. False: the budget declares at stamper RUNTIME on the unmerged codexgap-c2 branch; origin/main's trial ledger carries only the Phase-0 n=3 row; the 13:26Z timestamp the census lane cited was a whitehouse-alert commit time. FIXED in header/§1/§2 — a census correction to our own census, printed.
3. **BD-3 confound overstated (stats lens, major):** the draft attributed the recent-stop confound to "BD-2 by construction; BD-3 via the breach." BD-3 does not condition on a prior stop (data check: only ~41% of BD-3-flagged fires have one). FIXED: the prereg's frozen rationale now attributes the confound to BD-2 only; C2 legitimately stands without a recent-stop control.
4. **BD-6 is not a per-ticker drop-in (build lens, major):** `dump_breakdown_events.py`'s detection loop is strictly single-name; S13⁻ needs cross-sectional sector context. FIXED: the prereg names a sector-panel pre-pass as an explicit build task, keys it to `data/breadth/ticker_sectors.parquet`, and drops the implication of a verbatim `detect_bd6(ticker, ...)`.
5. **rr_banner false premise (build lens, major):** the draft prescribed absence-handling for `rr_banner.json`; the file is ALWAYS present and git-tracked, carrying `{"alert": null}` when no extreme is active — and spells its date field `asof`, not `as_of`. FIXED in §5.2: the exposure signal is `alert != null`; the builder reads the `asof` spelling.
6. **Minors folded:** gate.json carries 8 per-family-status entries, not 10 (a printed census "correction" that was itself wrong — fixed); the W-OC readiness forecast cannot use "stamp arrival dates" (no such column on the ledger) and is respecified as new-stamped-fire cadence × trailing condition-hit share; the family-size drift row is relabeled a BH test-count drift (22 vs registered 28), not a bucket count; Phase-0b now carries a `derived_from_surface: bd_phase0_tape` contamination stamp; S13⁻ gets a flat-window std==0 guard; BD-4 gets a ≥273-bar warmup floor; BD-5's σ convention frozen to BD-1's implemented form; `event_date` string-dtype/trading-bar builder note added; the skip-policy net read must re-present the budgeted endpoint CI, never a fresh test.
7. **Pre-registration feasibility peek, disclosed:** before commit, the author ran a coarse floors-feasibility join on the BD-ECON-1 tapes; it confirmed floors (BD-2 ≈24.7k flagged fires; BD-3 ≈3.4k) and incidentally revealed coarse pooled magnitudes. No gate/endpoint/floor was changed in response; the peek is logged as a dated contamination disclosure in the prereg's Amendments block.

---

## §3 Rulings

- **RUL-U1 (zero charters):** no lobe is chartered; the cap stays L1+L3. All four builds are waves on existing tissue (L1 tape, options program audit rail, DQ rail). Same accounting as RUL-C1/RUL-N1.
- **RUL-U2 (ownership seniority):** where this program touches a surface owned by #1664/#1666/#1673, the earlier program owns it. Named: BD-AVOID-1 and everything in codexgap-c2 (untouchable until PR-C2 merges), PR-C4's capture UI, DISP-GATE-1's dispersion basis (any future BD×dispersion study consumes B2's reconstruction, never re-derives), #1669's frozen contrasts, W-OVC's stamp/state fields, the W-A/W-B three-lobes PRs in flight.
- **RUL-U3 (BD-ECON-1 lawful shape):** retro decision-economics on the existing Phase-0 tape is legal now because every input is already computed and graded; it is registered (family `short_side`, declared budget 6, logged BEFORE the run), carries `derived_from_surface: bd_phase0`, prints symmetric costs (avoided drawdown AND missed upside, RUL-N6), and is research-only — no chip, no gate, no board consumer. Its verdict changes which *forward* preregs get written; it grants zero live authority. The co-primary increment contrast C3 (BD-2's confound is by-construction; BD-3's is not — §2.5.3) is binding.
- **RUL-U3a (budget semantics, binding on both studies):** `log_declared_budget` is a per-family max() floor, not a sum (§2.5.1). Each study's declared budget protects only its own BH set; cross-study multiplicity within `short_side` is NOT captured by the declared budget — tolerable solely because every study in this family is descriptive/research-only. Both harnesses MUST log each verdict cell as a distinct ledger config so `literal_n` accumulates, and every report prints the family literal count with the max()-basis divergence note.
- **RUL-U4 (Phase-0b lawful shape):** a descriptive tape extension under the Phase-0 apparatus (same universe, liquidity floor, era window, episode collapse, paired two-sided grading, seeded random-bar controls, survivorship stamps; BD-6 additionally requires a cross-sectional sector-panel pre-pass — §2.5.4 — the single-writer and summary-v3 obligations unchanged). ONE prereg, three frozen definitions, declared budget 3 (RUL-U3a semantics), `derived_from_surface: bd_phase0_tape` stamped. The §6 reading guide of BD_PHASE0_PREREG applies unchanged, with one amendment inherited from #1664 RUL-3: any Phase-1 forward prereg arising from Phase-0b must carry a compensating gate at least as strict as BD-AVOID-1's ≥8pp. S7⁻ is excluded; EDGAR/news-gated species stay parked; no cross-definition selection statistic is computed.
- **RUL-U5 (W-OC read-only fence):** the coverage audit reads state.parquet, gate.json, the stamped ledger, and registry constants; it writes ONLY `data/options_entry/coverage.json` + its own docs table. It must not modify gate logic, thresholds, stamp columns (W-OVC's), or any masterplan registration. The known family-size drift (masterplan 28 vs gate.json 22 vs harness docstring 22) is PRINTED as a consistency row, not fixed here — the fix is W-OVC's first obligation. The unwired `audit_options_accrual.py` (synapse-listed consumer, never called from collect) IS wired in this PR — that is closure housekeeping, not new scope.
- **RUL-U6 (W-EX measurement-substrate-only):** the exposure log computes NO statistics, NO contrasts, registers NO trials. #1669's three contrasts stay frozen; any exposure-conditioned contrast is a future prereg (come-back 2026-09-15 registered). Storage per RUL-P10: row log gitignored host-local (`data/operator/exposure_log.jsonl`, append-only, deduped on (surface, surface_id, as_of)); committed summary `data/governance/operator_exposure_summary.json` (bounded 90 days, single-writer = the builder script). Exposure date = artifact `as_of`, never run date (§2.5 intro, verified against daily.yml job split).
- **RUL-U7 (analogue-library fences, binding on the come-back):** prereg BEFORE any neighbor search; era-partitioned; ETF/sector roots first; outputs are research artifacts only (`research/options/`), never a live per-candidate surface; any conditional-outcome table it prints is a declared contamination surface. A live analogue chip would require its own gate passage — the come-back note carries this.
- **RUL-U8 (merge discipline):** W0 merges first (provenance + preregs). W-OC and W-EX build in parallel pre-created worktrees but merge SERIALLY with synapse.yml rebase-check and pinned-count re-bump (`tests/test_signal_bus_doc.py`), yielding to any in-flight #1664/#1673 PR that lands first (their programs have seniority). W-BE and W-0B are Mac-local research lanes; their trial-ledger appends and summary artifacts ride their PRs; they merge serially after W0. All branches off fresh origin/main; same-day squash-merge; stash law respected.
- **RUL-U9 (LLM law):** nothing in this program lets an LLM originate, score, or escalate. W-EX rows are deterministic joins of committed artifacts; BD studies are arithmetic on frozen tapes.
- **RUL-U10 (language):** "avoid candidate," "building history," "descriptive," "come-back" used precisely; the word "validated" appears nowhere in any artifact this program ships; every report carries a plain-language box.

---

## §4 Wave plan

| Wave | PR | What | Model lane | Risk |
|---|---|---|---|---|
| W0 | PR-α | Codex source doc + this adjudication + BOTH Fable-frozen preregs (BD_ECON1, BD_PHASE0B) + 5 come-back rows in `data/experiments/registry_seed.json` | Fable (+ Opus red-team, done) | — |
| W-OC | PR-β | Options coverage audit + readiness forecast (§5.1): `scripts/audit_options_entry_coverage.py` + `data/options_entry/coverage.json` + collect wiring + `audit_options_accrual.py` wiring fix + synapse + tests | builder (Sonnet), reviewer (Opus) | LOW |
| W-EX | PR-γ | Operator exposure log (§5.2): `scripts/build_operator_exposure_log.py` + gitignored row log + committed summary + synapse + tests | builder (Sonnet), reviewer (Opus) | LOW-MED |
| W-BE | PR-δ | BD-ECON-1 (§5.3 + prereg): `scripts/research/bd_econ1_counterfactual.py` + Mac-local run + committed summary JSON + report | builder (Sonnet), **Opus stats review mandatory**, Fable verdict | MED |
| W-0B | PR-ε | BD Phase-0b (§5.4 + prereg): extend `scripts/research/dump_breakdown_events.py` with 3 definitions + Mac-local run + summary v3 | builder (Sonnet), **Opus stats review mandatory**, Fable verdict | MED |

Come-back registry rows (W0): `next3-analogue-library` (2026-08-15, RUL-U7 fences), `next3-reason-codes-after-c4` (2026-07-27), `next3-lobe-impact-attribution` (2026-09-15), `next3-weekly-decision-review` (2026-09-15, deterministic-brief pattern), `next3-bd-conditioning-census` (2026-10-15, per-axis only, consumes B2 basis + S-TOP_RISK gate state).

---

## §5 Frozen specs

### §5.1 W-OC — options coverage audit (PR-β)

`scripts/audit_options_entry_coverage.py`, an end-of-collect audit sibling of `audit_grading_closure.py` (wired via `run_as_collect_step()` in `scripts/collect.py` immediately after the existing audit calls). Read-only over `data/options_entry/state.parquet` (git or local; absent → printed, never fabricated), `data/options_entry/gate.json`, the stamped board ledger rows the gate reads, and the §4 family registry constants. Emits `data/options_entry/coverage.json` (committed, single-writer, vintage-stamped):

- per-feature non-null count + share (current snapshot) and per-ticker coverage;
- per-family stamp coverage on ledger rows: n_cond / n_base now, plus new-stamped-fire counts per ISO week (derived from ledger `as_of` — the ledger carries NO stamp-arrival column, §2.5.6);
- **readiness forecast per family:** projected date n_cond≥30 from (trailing new-stamped-fire cadence) × (trailing condition-hit share among recent stamped fires), emitted ONLY when ≥14 days of post-W-C stamped-fire history exist, else `"forecast": "unknown"` with the counts printed;
- **structural-null ledger:** iv_rank_* (A9, until W-E0-fed backfill), pin_risk (by-design opex_days≤5 gate — prints the gate, the current opex_days, and next OPEX date), gamma_regime per-name constancy caveat;
- **consistency rows:** BH test-count drift (registry 28 after #1674 vs gate.json `fdr_family.family_size`=22 vs validate docstring 22 — a test count, not a bucket count; printed, fix owned by W-OVC); stale `as_of` ages per source; `audit_options_accrual.py` now wired (same PR — requires a new `run_as_collect_step()` wrapper around its existing `audit()`, §2.5.6) with its last-run status surfaced.

Synapse: infrastructure tier, `horizon_role: context`, `scored_path_surfaces: []`; SIGNAL_BUS regen + pinned-count bump in-PR. Tests: fixtures only (synthetic state/gate/ledger), absent-store cases, forecast-gating case. Fences per RUL-U5.

### §5.2 W-EX — operator exposure log (PR-γ)

`scripts/build_operator_exposure_log.py`, run end-of-collect (reads the previously committed site artifacts; exposure date = artifact `as_of`, §2.5 intro). Row schema (flat; the DecisionPacket residue, no cross-lobe conditioning fields):

```json
{"schema":"operator_exposure.v1","ts_logged":"ISO","as_of":"YYYY-MM-DD","surface":"experiment|board_buy|board_watch|alert_wh|alert_rr","surface_id":"string","artifact":"path","exposure_kind":"standing|new|come_back_due"}
```

- Surfaces v1: `site/marketdata/experiments.json` (id verbatim; `come_back_due` when come_back_on ≤ as_of), `site/factordata/us_standouts.json` (id = `sha1(as_of|lane|ticker)[:12]`, lane strings verbatim; unknown lanes counted + logged per §2.5), `site/wh_banner.json` (alert id verbatim), `site/rr_banner.json` (**always present, git-tracked; carries `{"alert": null}` when no risk-off extreme is active — the exposure signal is `alert != null`, never file absence; NOTE its date field is spelled `asof`, §2.5.5**).
- Outputs: `data/operator/exposure_log.jsonl` (gitignored host-local, append-only, dedup on (surface, surface_id, as_of)) + `data/governance/operator_exposure_summary.json` (committed, counts by surface × day, bounded 90 days, single-writer).
- Join contract documented in the module docstring: action rows (`action_ledger.jsonl`) join on surface conventions; unmatched actions are counted, never dropped (mirrors #1669).
- Alerts leg enriches when #1664 PR-C4 ships machine-readable alert IDs on the feed; until then wh_banner slugs are the honest id.
- Synapse ×2 (row log gitignored-local; summary committed) + SIGNAL_BUS regen + count bump. Tests: fixtures; missing-artifact and schema-drift cases. Fences per RUL-U6: no statistics, no contrasts, no trials.

### §5.3 W-BE — BD-ECON-1 (PR-δ)

**The prereg (`research/short_side/BD_ECON1_PREREG.md`, committed in W0 BEFORE any harness exists) is the single numeric authority.** Shape mandated here: three pre-declared contrasts × two co-endpoints (stop-rate delta at h21 on `state_8_21`; `fwd_ret_21` delta as the economics endpoint) = **declared budget 6** in `fdr_family='short_side'`, BH q=0.10 within the declared set, logged before the run. C1 BD-2-flagged vs unflagged fires; C2 BD-3-flagged vs unflagged; **C3 (co-primary per §2.5 intro + §2.5.3): BD-2-flagged vs recent-stop-without-BD unflagged fires** — the increment over generic post-stop damage. Two-way month×ticker cluster bootstrap (§2.5 intro); year/tier splits DESCRIPTIVE multiplicity declared (WAIT-GRID-1 precedent). Economics block (arithmetic on the flagged cohort, no new cells): avoided `fwd_mdd_21` among flagged-stopped, foregone `fwd_ret_21`/`fwd_mfe_21` among flagged-clean, skip-policy net differential with CI, re-entry quality descriptive. Floors: ≥300 flagged fires AND ≥100 distinct BD episodes per definition; miss → P0-DEFER printed with arrival note. Outputs: `data/research/bd_econ1_summary.json` (committed, vintage-stamped) + `research/short_side/BD_ECON1_REPORT.md` (plain-language box; nulls printed). No site surface. Fires with `signal_date ≤ event_date` are never flagged (excludes BD-2's generating fire by construction).

### §5.4 W-0B — BD Phase-0b (PR-ε)

**The prereg (`research/short_side/BD_PHASE0B_PREREG.md`, committed in W0) is the single numeric authority** — it freezes the three definitions (S4⁻ Two-Clock Rollover, S5⁻ Coiled Breakdown, S13⁻ Within-Sector Leader Fade) with exact formulas, the S13⁻ sector-map anachronism declaration, and the S4⁻↔BD-3 overlap-matrix obligation (§2.5 intro). Apparatus: BD_PHASE0 (same script extended — BD-6 via a sector-panel pre-pass, §2.5.4 — same universe/liquidity/era/controls/grading/episode-collapse; summary schema bumps to v3; overlap matrix extends to six definitions). Declared budget 3 `short_side` logged before the run (RUL-U3a: per-cell configs logged, literal count printed). Reading guide: BD_PHASE0 §6 unchanged + the RUL-U4 compensating-gate amendment for any resulting Phase-1. Mac-local, off-render; Opus stats review before the report merges.

---

## §6 What this program does NOT do (scope fences)

- No lobe charters (cap stays L1+L3); no meta-models; no fused/composite scores; no sizing changes; no board chips or site surfaces from any wave here.
- No short execution, no borrow/squeeze modeling (masterplan §1 stands); no BD species goes near a scored path.
- No options gate/threshold/stamp changes (W-OVC owns); no options tissue consumption (accrual gates ~2026-10/12 stand); no analogue retrieval before its registered come-back + prereg.
- No new statistics on operator behavior (frozen #1669 contrasts only); no touching codexgap-c2, PR-C4 scope, or any in-flight sibling surface (RUL-U2).
- No kernel anything (Signal Commons R1 stands until 2026-10); no LLM-originated signals anywhere.
- Nightly render band: W-OC/W-EX add seconds-scale collect steps; W-BE/W-0B run Mac-local off-render. No new nightly graders.

## §7 Status log

- 2026-07-06: 4-lane census complete (every checkable Codex claim verified at file level; #1669 merged mid-census and the map updated). Draft written with seven pre-folded suspected defects; 2-lens Opus red-team (APPROVE_WITH_EDITS ×2) confirmed all seven real-and-fixed and added five majors + minors, all folded (§2.5); a census self-correction (spurious 13:26Z budget claim) printed. #1695 (final-3) ratified mid-draft and added to the sibling map — no surface overlap. Program ratified. W0 committing; W-OC/W-EX/W-BE/W-0B dispatching to builder lanes with Opus review.
- 2026-07-06 (close-out, same day): **ALL WAVES SHIPPED AND MERGED** — W0 #1698, W-OC #1703 (Opus review caught the builder deleting the #1673-owned claim-accountability collect step on a stale base — restored pre-merge; readiness forecasts honestly `unknown` at 1 day of post-W-C history), W-EX #1702 (first real run: 168 exposure rows — 124 experiments / 43 board / 1 alert / rr_banner honest-null), W-BE #1707, W-0B #1710 (completion pass fixed sector-map resolution + control-seeding contract, then full 372s run; BD-1/2/3 event rows preserved byte-identical, control-baseline drift quantified at −0.14pp/+0.63pp — immaterial).
- **Science outcomes, printed:** BD-ECON-1 = **NULL across all 6 cells** (C1 +1.9pp stop [CI −5.6,+10.4]; C3 increment ~0) — the avoid lens does NOT transfer to board fires at 21d; the entry stack already filters the damage; NO forward skip-prereg is warranted (pre-committed INCREMENT-NULL branch taken). Phase-0b = **all three species PARKED** (BD-4 two-clock rollover −12.45pp = stop rate LOWER than control, a sign-reversal against the breakdown hypothesis; BD-5 −1.69pp; BD-6 −0.76pp; none clear ≥5pp; none avoid-only); BD-4×BD-3 near-overlap 37.5%, no redundancy flag. The breakdown grammar does not widen — BD-2/BD-3 remain the only definitions with avoid evidence, and their live question stays with BD-AVOID-1's forward clocks. `short_side` family literal_n = 12 on the merged ledger.
