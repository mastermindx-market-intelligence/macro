# Options → Neural Web Entry Intelligence — masterplan (by Fable)

**Status:** ACTIVE (adjudicated + dispatched 2026-07-05). **Owner:** Fable main loop (rulings/merges); Sonnet builds; Opus reviews.
**Provenance:** external ChatGPT research report (`research/OPTIONS_NEURAL_WEB_ENTRY_ALPHA_REPORT_FOR_FABLE.md`, 2026-07-05) → 5-lane codebase census (7 agents) → 2 independent Opus red-teams (epistemics + architecture) → Fable adjudication. This document is the ruling of record; the report and chat transcript are not.

> **In plain English:** we have a lot of options machinery already built and accruing. This program does NOT invent new options alpha claims — it (1) fuses what exists into one honest read-table Neural Web can see, (2) wires options context into Neural Web as confirm/contradict evidence and de-escalation, (3) registers the new hypothesis buckets properly so they can earn authority the legal way, and (4) runs the first genuinely powered backtests on the 15-year ThetaData sector/index history we already own. Options will make Neural Web's entries *more durable* first (fewer stop-outs, fewer chases into walls/OPEX) and *more confident* second (visible options confluence on names it already likes). Direction-from-flow stays dead until the tape pipeline earns it.

---

## §1 Verdict on the external report

The report is directionally right and locally wrong. Right: options as **entry quality, not direction**; tactical-vs-durable separation; de-escalation as the strongest family; the missing connective tissue is a fused per-ticker/day state table; promotion only through gates. Wrong or stale, per census + red-team:

- Its two flagship outputs (`options_entry_quality_shadow` −3..+3, `options_convexity_shadow` 0–100) are **fused escalating composites — the exact shape Signal Commons R3 forbids**. Rejected (RO-2).
- It proposes kernel conditioning at 10–63D — **hard-blocked by Signal Commons R1** until the NW clocks (kernel-FDR 2026-10, weights ~2027-05). Denied (RO-11).
- Its Phase E ("buy Cboe open/close") **violates the W6 SKIP-ALL zero-spend user decision**; and the ThetaData tape it treats as future is already entitled with its signing sub-gate PASSED. Re-scoped (RO-9, RO-10).
- It treats as greenfield things that exist: `engine/neuralweb/reflexes.py` (exists, W6a), `engine/gex_confirm.py` (existing CONFIRM/NEUTRAL/CAUTION + OPEX suppression), `engine/opex.py` (opex_days), the W1.3 harness (stamp + validate + gate.json, shipped #1155-era). Phase C/A re-scoped to registration + reuse.
- It never confronts: iv_rank_252 structurally NULL (ruling A9), gamma_regime `structurally_constant=True` per single name (audit #29), S-VOI degeneracy (n_cond=42/n_base=4), the era-partition amendment, the enlarged-family FDR burden, or the house yardstick (2–4wk bounce + post-entry drawdown; A10 primitives only).
- Literature effect sizes (Pan-Poteshman >40bps/day, CW ~50bps/wk, XZZ ~10.9%/yr) are 2004–2010-era: they enter as **priors to gate, never as evidence to deploy**, and never on user-facing surfaces.

Data reality (census, 2026-07-05): skew snapshots 13 trading days / 386 tickers; ivspread 6 days / 355 tickers; polygon_gex 19 chain days / 388 names; flow accrual effectively 2 days old (secret outage 06-21→07-03); retro_grades 950 fires with 47 stamped; all gates `scored=false, building_history`. **Live accrual reaches nothing before ~Q4-26 — except the ThetaData historical store (24 roots × 15y, manifest COMPLETE), which is powered NOW at index/ETF/sector level.**

## §2 Rulings

- **RO-1 — `options_entry_state` ADOPTED AMENDED.** Build `engine/options_entry_state.py` + `scripts/build_options_entry_state.py` → `data/options_entry/state.parquet` as a **display-tier, raw-fields-only** per-ticker/day fusion table. It READS existing artifacts (polygon_gex summaries/chains via existing stamp outputs, options_flow, skew/ivspread snapshots, gex_confirm verdict, opex_days) and NEVER writes any ledger (A9 single-writer preserved). No composite/score columns of any kind. Per-field `as_of` + source + `evidence_quality ∈ {full,partial,thin,stale}`; iv_rank fields emitted as **structurally-null-until-A9-backfill** (not "thin" — architecturally absent). Missing gitignored stores → nulls, never fake-neutral; `_clean()` NaN discipline + `is not none` jinja guards; world_state `factor_weather` try/except→null-fallback pattern. Not a forward ledger: no forward-return columns, no nightly-advancement semantics.
- **RO-2 — Fused composites REJECTED (Signal Commons R3).** No `options_entry_quality_shadow`, no `options_convexity_shadow` as columns, scores, or ranks anywhere a reader can lift them pre-gate. The **tactical-vs-durable split is adopted as doctrine** (matches DURABLE_BOTTOM_FRAMEWORK). "Convexity" context may exist only as a display chip built from magnitude/OI/vol>OI primitives — no signed direction, no 0–100 score. Any summary of options state may only ever be a post-hoc roll-up of already-gated survivors.
- **RO-3 — Top-risk de-escalation ADOPTED as the flagship output.** De-escalation/caution-only is exactly what R3 permits survivors to become and what doctrine §2.1 already allows ("confirmers may only LOWER confidence"). S-TOP_RISK and S-SKEW_DECEL must be registered in OPTIONS_ALPHA_MASTERPLAN §4 (era specs + FDR family) BEFORE the harness computes them. The single-name negative-gamma leg is **dropped** (gamma_regime structurally_constant per name, audit #29). Never a short signal.
- **RO-4 — New bucket registrations with FDR accounting.** Register S-IVSPREAD-F (fire-conditioned ivspread_rel>0), S-SKEW_DECEL (skew high & falling at fire), S-TOP_RISK (extended + skew rising | puts-rich | put-OI-build), S-PIN_RISK (OPEX≤5d + long gamma + near wall/max-pain) in §4 with era-partition specs and an explicit **enlarged-family BH-FDR α=0.10** statement covering old + new buckets × eras × A10 primitives. S-VOI's original registration stands (accrues to its registered verdict) but is documented as degenerate (42/4); a stricter **S-VOI2** is registered as a NEW bucket, not a silent revision. No verdict below n≥30 per condition bucket. Gate currency = A10 ledger primitives ONLY (`post_cushion_breach`, `terminal_state_clean8_21`, `fwd_ret_{h}`, `fwd_mfe_{h}`) — "MFE fat-tail" is not a registered primitive; convexity-family grading uses `fwd_mfe_5/21` as-is.
- **RO-5 — NW spine adapter ADOPTED AMENDED.** `adapt_options_entry()` in `engine/neuralweb/query.py` + `options_entry` in LEDGER_ENUM, folding **ungraded-honest** (`outcome_graded=False`, like `adapt_reflexes`) until graded fires accrue; outcomes later JOIN the existing us_board `retro_grades.parquet` fwd columns (read-only; A9). Categorical clean/breach encode via the existing ±sentinel binary pattern (adapt_cortex_attention), never new COLUMNS. **No qledger writes** before the QI co-sign (~2026-08-29). Fail-open mandatory (missing store → gaps, zero rows, never raise).
- **RO-6 — Confluence edges: no ruling required, 4 adopted, 1 rejected, 1 deferred.** Display-only edges via existing confluence machinery need no Fable ruling (Entry-Stack §7 precedent; every edge `display_only=True`). Adopt: `price_bottom_signal CONTRADICTED_BY skew_rising`, `price_prebreakout CONFIRMED_BY ivspread_positive+call_doi`, `extension_signal CONFIRMED_BY skew_rising/call_wall_pin`, `price_bottom_signal CONFIRMED_BY skew_decel` (repointed from the rejected composite to the specific primitive). **REJECT the `AMPLIFIES` verb** — not a sanctioned edge type (feeds/stable/leads/contradicts/confirms); re-express or drop. **DEFER the oracle_rotation edge** to Oracle-program review (separate constitution).
- **RO-7 — Cortex read tools ADOPTED.** `read_options_entry_state`, `explain_options_context`, `query_options_confluence`, `list_options_contradictions` added to `_READ_TOOLS` behind the A7 whitelist/dispatch guard, 50KB cap, DENY_ROOTS intact. `list_options_contradictions` reads display-only edges, not any scored surface.
- **RO-8 — Reflex ADOPTED AMENDED.** Do NOT rebuild `reflexes.py` — register `options_flow_attention` in `config/reflexes.yml` (claim_family `reflex.options_flow_attention`, `is_context_only=True`, single-writer firings at `data/reflexes/options_flow_attention/firings.jsonl`, nightly collect fold). Triggers are **direction-free only**: vol>OI magnitude burst on watchlist/board names, spot-reaches-wall/flip proximity. Hard stale-guard: no-fire when the R2 live_flow feed is absent/stale (feat/flow-desk is UNDEPLOYED). EOD `site/flow` and intraday R2 `live_flow/` remain architecturally distinct.
- **RO-9 — Signing authority is PER-SOURCE.** Root `signing_gate.json` stays `direction_reliable=false` for tick-rule/minute-agg signing (0.41, dead — F7). The ThetaData tape sub-gate PASSED (agreement 0.8848, recovery 0.80 ≥ 0.75 bar): tape-derived direction may carry direction **only on artifacts produced from the tape pipeline once it exists** (options-alpha STOP-gate work, off-render-path, R2). Flipping the root gate for non-tape surfaces is DENIED.
- **RO-10 — Procurement DENIED; history unlocked instead.** No Cboe open/close purchase (W6 SKIP-ALL stands). The chartered path: (a) **W-E1 era-partitioned studies on the COMPLETE 24-root ThetaData store now** (SPX/SPY/QQQ/IWM/DIA + 11 sector SPDRs + SMH/SOXX/XBI/KRE/ARKK + NVDA; AAPL incomplete) — S-GEXR, sector skew/ivspread de-escalation conditioning, DOI persistence, with mandatory Opus stats review, era splits per amendment, house yardstick; (b) **W-E0 single-name backfill extension** (ops worktree, off render path, 8-concurrent/window-stall law) before any single-name cross-sectional historical claim.
- **RO-11 — Kernel conditioning DENIED** until the NW standing clocks (Signal Commons R1 restated; 2026-10 / ~2027-05). No options feature touches kernel cells before then.
- **RO-12 — Ownership boundaries.** The options-alpha program owns the options stack, §4 registry, and ALL stamp columns (A9 single-writer: only `scripts/stamp_options_state.py` writes stamps). This program owns the NW connective tissue (state table, adapter, edges, tools, reflex registration, world_state lobe). Oracle owns rotation surfaces — the sector-aggregation lobe and oracle edge require Oracle review. The stale `engine/options_flow.py:22` "both 403" comment (flagged by Signal Commons R6) is corrected in W-C as an options-alpha housekeeping item.
- **RO-13 — Oracle review COMPLETE (2026-07-05): edge + sector lobe remain DEFERRED with a concrete unblock path.** Adjudicated under ORACLE_CONSTITUTION.md (Fable = adjudication per its §V routing; this ruling is read-only w.r.t. Oracle law). Findings: (a) the confluence graph already consumes `oracle_state.json` read-only, so a display-only options edge is the LEGAL pattern; (b) Oracle §IV: rotation CONTINUATION is NULL — any options→Oracle edge may reference **onset surfaces only**; (c) the turn-asymmetry program's W1 (#1450) printed NULL on onset-quality conditioning (n=350) — a standing caution against evidence-free wiring; (d) sector options aggregates only begin accruing 2026-07-06 (#1501). Unblock requires ALL of: (1) ≥20 trading days of live sector-aggregate coverage on the 15 SPDR/industry anchors; (2) a PRE-REGISTERED episode-conditioning study — options state (sector CWIV/skew/DOI/gamma from the ThetaData 15y store) at the Oracle forward ledger's 173 rotation-episode onset dates — prereg drafted then Opus-reviewed BEFORE any computation, registered in this masterplan, honoring Oracle's forecast-language ban and trial-ledger accounting; (3) survivors wire as display-only onset-referencing edges, never continuation. W-F stays PARKED until (1)+(2) resolve.

## §3 How this raises Neural Web's signal power, honestly, by timeframe

| Horizon | What ships NOW (display/context) | What accrues → earns authority | What is blocked and why |
|---|---|---|---|
| **Intraday–2D** | Reflex attention (direction-free vol>OI bursts, wall/flip proximity) on board names; gex_confirm CONFIRM/NEUTRAL/CAUTION + OPEX-pin context on entry cards | Reflex firings graded like cortex attention (A2-style earn-in) | Signed tape direction (RO-9) until tape pipeline ships |
| **3–10D** | Contradiction/confirmation edges visible to committee + cortex; top-risk/pin-risk stamps on fires | S-TOP_RISK, S-PIN_RISK, S-SKEW_DECEL, S-IVSPREAD-F, S-VOI2 buckets → verdicts ~Q4-26 at n≥30; survivors → caution-only confirmers that LOWER confidence on bad entries (fewer stop-outs = durability) | Any positive confidence nudge before a gate passes (R3, §2.1) |
| **10–63D** | World-state "options weather" lobe (sector/index IV, skew, gamma regime from 15y-history roots) as de-escalation context | **W-E1 era-partitioned 15y studies (powered NOW)**: sector/index skew & ivspread conditioning, S-GEXR realized-vol; live S-CWIV/S-XZZ cross-sectional gates ~Dec-26; single-name history after W-E0 backfill | Kernel conditioning until clocks (RO-11) |

The user-facing promise this program can keep: Neural Web sees, on every candidate it already likes, whether options positioning **contradicts** it (de-escalate — legal today as display; behavioral after gates), and gets a **measured, era-honest** answer by Q4-26/Dec-26 on which options families actually reduce stop-outs and improve clean liftoffs. Confidence comes from visible confluence, not from an ungauntleted score.

## §4 Waves

_Model routing per CLAUDE.md: `builder` (Sonnet) builds, `reviewer` (Opus) reviews, Fable adjudicates/merges. One wave = one branch off fresh origin/main = one PR = same-day squash-merge. Nightly remains sole advancer of forward ledgers; heavy compute off render path → R2._

| Wave | Scope | Acceptance gate | Status |
|---|---|---|---|
| **W-A — Connector** | `engine/options_entry_state.py` + `scripts/build_options_entry_state.py` → `data/options_entry/state.parquet` (RO-1 column set: iv30, ivspread_rel+5d_chg+tone, skew+5d_chg+tone, doi slopes/pc from existing stamp sources, voi flags, gamma_regime+dist_to_flip (with structurally_constant caveat col), wall/max-pain distances, opex_days, pin_risk, gex_confirm verdict, evidence_quality, as_of/source per field; iv_rank cols structurally-null). Synapse registrations ×5: options-entry-state + options-entry-gate (display/shadow), options-skew-snapshots + options-ivspread-snapshots (**tier=display**, `scored_path_surfaces=[]`, explicit "display-only until validation_gate.validated==true" mandate), live-options-flow-current (display + stale-guard note, no ledger coupling). DAG: cl_gex band (light; measure runtime, must be <60s). Tests: CI-null (absent gitignored stores → all-null + evidence_quality=thin, never crash, never stale-non-null). | Every field has as_of/source/freshness/quality; missing data → nulls not fake-neutral; NO score/rank/sizing consumer changes; synapse CI check passes; runtime measured. | SHIPPED #1434 (state.parquet 388 tickers × 28 cols, runtime 2.65s; 5 synapse regs; 17 tests; review minors fixed pre-merge) |
| **W-B — NW wiring** | `adapt_options_entry()` + LEDGER_ENUM (ungraded-honest fold per RO-5); 4 confluence edges (RO-6, sanctioned verbs, display_only=True); 4 cortex read tools (RO-7); world_state `options_weather` lobe (factor_weather pattern: _compose, _clean, try/except null-fallback, display_only=True) reading state.parquet + 15y-history sector roots. Depends on W-A merge. | Adapter fail-open (zero-row on missing store); edges carry display_only=True and never rank; tools refuse outside whitelist; lobe null-safe on CI; spine_index builds clean with new ledger. | SHIPPED #1486 (adapter folds 388 ungraded-honest rows; 4 edges live — 3 buy-lane names contradicted by rising skew at ship time; 4 cortex tools; options_weather lobe null-heavy honestly, 4/19 weather roots covered locally; 17 tests + 277 NW-suite green; Fable self-review — subagent session limit) |
| **W-C — Prereg + harness extension** (options-alpha co-owned) | OPTIONS_ALPHA_MASTERPLAN §4 amendment: register S-IVSPREAD-F, S-SKEW_DECEL, S-TOP_RISK, S-PIN_RISK, S-VOI2 (era specs + enlarged-family BH-FDR α=0.10 statement; S-VOI documented degenerate). Extend `scripts/stamp_options_state.py` (SINGLE writer, schema-union, null-tolerant): + `opt_ivspread_rel`, `opt_skew`, `opt_skew_5d_chg`, `opt_opex_days`, `opt_pin_risk`, wall-distance pcts. Extend `validate_options_entry.py` with new buckets → per-family gate.json verdicts (all `building_history` until n≥30). Fix stale `options_flow.py:22` comment (R6 flag). Experiments registry: accrual entries with come_back_on dates (gates ~2026-10-15; skew validation ~2026-12-15). Adjacency registration: options vol>OI vs FALSIFIED equity-volume H4 (RUL-2 law). | Buckets registered in §4 BEFORE harness code runs them; n≥30 floor; A10 primitives only; tactical/durable separate; stamp coverage % printed honestly; no grading-column touches. | SHIPPED #1438 (5 buckets + FDR statement; 7 stamp cols; retry-gate fix — opex excluded from coverage gate; composed with P2.2 tape-flow stamps per-family at merge; 26+ tests) |
| **W-D — Reflex registration** | `options_flow_attention` in `config/reflexes.yml` + trigger implementation (direction-free: vol>OI z-burst on board/watchlist names, wall/flip proximity), stale-guard (no-fire on absent/stale R2 feed), firings JSONL + nightly fold verification. | Attention-only, is_context_only=True; zero fires on absent feed (verified); direction=0 on all firings; single-writer law respected. | SHIPPED #1433 (direction-free triggers; stale-guard verified; 0 live firings pending flow-desk deploy — honest) |
| **W-E — Historical gauntlet** | **E0 (ops, off-render):** extend ThetaData EOD/greeks/OI backfill from 24 roots to top-~100 liquid single names (8-concurrent/window-stall law; manifest-gated; R2/gitignored store). **E1 (research, runnable NOW):** era-partitioned studies on the complete 24-root store — S-GEXR (gamma regime → fwd realized vol), sector skew/ivspread de-escalation conditioning vs sector fwd drawdown/bounce (house yardstick), DOI persistence on sector ETFs. Per-era results + post-publication-decay commentary mandatory (era amendment). Opus stats review MANDATORY before any verdict prints. | Pre-registered cuts before looking; era splits per amendment; nulls printed; a claim alive only pre-2016 is dead; no user-facing "validated" without gate flip. | E1 SHIPPED #1440 (fix-round-2 FINAL: 8/51 survive global BH α=0.10 at horizon-aware HAC lags — GEXR 6/6 with ERA-DEPENDENT sign → vol-conditioning context only; CWIV.Era3.5d IC 0.041 adj-p 0.026 → S-CWIV alive; SKEW 1/27 with direction OPPOSITE the bullish decel hypothesis → SKEPTICAL prior on S-SKEW_DECEL; DOI 0/12 dead at sector level. Fable = round-2 stats reviewer of record). E0 CHARTERED (ops lane, not yet run; now the binding constraint on all cross-sectional reads) |
| **W-F — Sector aggregation → Oracle** | Sector options aggregates as NW/Oracle confluence. | — | **PARKED.** Unblocks: (a) W-E1 sector studies pass review, (b) Oracle-program review of the rotation edge, (c) per-ingredient phase-0s per R3. No fusion into a single sector score regardless. |

## §5 Parked list

| Item | Why | Unblocks when |
|---|---|---|
| Fused entry-quality/convexity scores | R3 forbidden shape (RO-2) | Never as pre-gate composites; post-hoc roll-up of gated survivors only |
| Signed flow direction on any surface | F7 + RO-9 (root gate false) | Tape pipeline ships (options-alpha STOP gate) → per-source tape authority |
| Kernel conditioning on options features | Signal Commons R1 (RO-11) | NW clocks 2026-10 / ~2027-05 |
| Oracle rotation edge + sector agg lobe | Oracle constitution boundary + data maturity (RO-6, W-F) | Oracle review + W-E1 pass + R3 phase-0s |
| Cboe open/close purchase | W6 SKIP-ALL (RO-10) | W6 §7 re-buy triggers only |
| Single-name historical CW/XZZ claims | Store has only NVDA (+AAPL partial) | W-E0 names backfill completes with manifest |
| Entry-stack fire stamping | No persistent fire ledger (dump_gate_fires.py = entry-stack W1 backlog) | gate_fires parquet lands → same single-writer stamp extension |

## §6 Status log

| Date | Event |
|---|---|
| 2026-07-05 | Masterplan adjudicated (Fable). Census: 5 Sonnet lanes, 63 verified claims, 45 surprises. Red-team: 2 Opus critics, 23 verdicts. Report's 6 requested rulings answered: 1 adopted-amended, 1 adopted, 2 rejected (fused composites), 1 not-required (edges), 1 denied+re-scoped (procurement). |
| 2026-07-05 | W-A/W-C/W-D dispatched in parallel (builder/Sonnet, isolated worktrees); W-B after W-A merge; W-E1 dispatched as research memo lane. All PRs Opus-reviewed pre-merge. |
| 2026-07-05 | **ALL WAVES SHIPPED SAME DAY**: W-A #1434, W-C #1438 (merge composed with concurrent P2.2 #1435 tape-flow stamps — per-family retry gates), W-D #1433, W-E1 #1440 (two adversarial-review rounds; round-2 stats fixes by Fable directly after the subagent session limit hit: horizon-aware HAC lag = max(auto, 2×h), per-study BH removed — global k=52 family is the only verdict surface, survivor-count prefix bug, era-aware decay commentary), W-B #1486 (Fable-built + self-reviewed, same limit). W-E1 headline: gamma regime = powered vol-conditioning context with era-dependent SIGN; CWIV Era3 5d alive; skew-decel bullish premise UNSUPPORTED (lone survivor points the wrong way); DOI dead at sector level. Next: W-E0 single-name backfill (ops), Oracle-program review for the deferred edge/lobe, gate verdicts ~Q4-26. |
| 2026-07-05 | **Post-close follow-ups:** W-E0 found ALREADY RUNNING in the ops lane (88 roots, 87 complete at 15y, single names AAPL/ADBE/AMAT/AMD… landing; GEV partial = 2024 spinoff, honest). Weather-roots coverage gap fixed #1501 (15 sector/industry ETF anchors added to the polygon gex universe, cap 360→375; options_weather lobe populates from 2026-07-06 nightly). RO-13 Oracle review completed — edge/lobe deferred with concrete unblock (20d accrual + pre-registered episode-conditioning study vs the 173-episode Oracle ledger). |
