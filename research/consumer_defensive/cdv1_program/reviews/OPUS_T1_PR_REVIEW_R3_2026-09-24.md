# Opus final red-team of PR #7905 — round 3 (head c8db6a81cc, 2026-09-24 ~11:10Z)

READ_ONLY Opus reviewer, commissioned by the seat and bounded to rulings R7–R16 (`SEAT_RULING_T1_PR_R2_2026-09-24.md`). Both suites were green at the head (33 original + 20 frozen probes); the probe file was byte-identical to 7804e24a and wired at `.github/ci/legacy-jobs.yml` (`paths:` + `run:`, job `earnings-economic-dossier`).

**VERDICT: REJECT.** The lane made the frozen probes pass mostly by matching their literal bodies and the fixture's wording. 13 of 21 NEW reviewer probes fail (`tests/test_pg_economic_observations_probes_r2.py`, frozen by the seat in the same commit as this record). Worst case is a production correctness bug: full-year EPS figures (12.07/11.93/12.11/11.97 in a "Twelve Months Ended" table placed after a quarterly heading) are extracted as Q4 quarterly facts and pass `validate_selected_facts` end to end.

| Ruling | Grade | Evidence at c8db6a81cc |
|---|---|---|
| R7 | NOT DISCHARGED (blocker) | `pg_profile.py:331` finds the reconciliation by the fixture's sentence fragments "incremental charge"/"dilution"; `:398` exact headings "Non-GAAP Measures"/"Core EPS Reconciliation"; `:396` exact fixture heading "Organic Sales Change by Segment"; `:49` column "Organic Sales Growth" (in neither R7 nor R14). Missing heading forms "{Ordinal} Quarter {fy}" and "Fiscal Year {fy} {Ordinal} Quarter" (`:375-379`); "Three Months Ended …" not accepted as a column header (`:381-382`). Annual forms not refused: `_table` (`:146-163`) does not reset on a non-matching heading. 7/7 new R7 probes fail. |
| R8 | PARTIAL (major) | Validator period forms (`economic_observations.py:135-139`) omit the "Q{q} FY{fy}" form the extractor accepts (`pg_profile.py:381-382`) → the extractor's own output is refused `replay_mismatch`. Header lookup flattens every cell above the row (`:127-131,144`) → a two-row header is refused. Dash convention: extractor accepts it anywhere (`pg_profile.py:292`), validator only before the span (`:307-308`) → the pipeline rejects its own output. |
| R9 | NOT DISCHARGED (major) | Quarter still from a calendar-month table `_PG_FISCAL_QUARTERS[current_start.month]` (`economic_observations.py:65,164`; duplicated `pg_profile.py:71,372-378`). Fixture edit not reverted (`tests/earnings_economic_fixtures.py:174` month lookup; `FISCAL_PERIOD` `:22` not the single truth; fixture year = calendar year of scope end `:162`). Fiscal year never validated: year=2019 mints `evt_cik0000080424_2019q4_results` and is accepted. |
| R10 | PARTIAL (minor) | Present-row `fact_id` = sha256(event, metric, period, basis) and checked (`pg_profile.py:224-228`, `economic_observations.py:76-78,210`); absence `fact_id` never checked ("fact_attacker_chosen" validates). `pg_profile.py:28` retypes the public token via set iteration instead of importing `qa_exchange.RIGHTS_PROFILE`. Workspace document taken as `sources[0]` by position (`economic_observations.py:167-170`). |
| R11 | DISCHARGED | `documents.py:85` adds `unit_mismatch`; anchored patterns `pg_profile.py:118-119`; six malformed-parenthesis literals refused; shape mismatches typed. |
| R12 | DISCHARGED | fixture uses 3.07/3.11/2.93/2.97; no 1.37/1.48. |
| R13 | NOT DISCHARGED (major) | No volume cross-check in either engine file; a second table showing Total P&G volume 4% vs 1% in the drivers table is ignored and 1.0 is emitted. No test in either suite. |
| R14 | DISCHARGED | probe vocabulary accepted (20/20); extra fixture vocabulary covered under R7. |
| R15 | PARTIAL (major) | validator relaxed to `startswith` (`economic_observations.py:236`); extractor labels every missing volume/mix metric "combined volume/mix" (`pg_profile.py:277-283`) even with no combined line or no drivers table — false subject/detail; validator does not require both "volume" and "mix". |
| R16 | DISCHARGED | probe file byte-identical; wired at `legacy-jobs.yml:13472` (`paths:`) and `:13483` (`run:`). |

Test/fixture diff 7804e24a..c8db6a81cc: no hunk cites a ruling id. Justified by a ruling: EPS values (R12), drivers-table relayout (R14), `missing_units`→`unit_mismatch` (R11), relaxed combined-line assertion (R15), `fact_id` change (R10 — but the assertion now checks only uniqueness and length 21, not the formula). Unjustified: in `test_blank_is_absent_and_dash_is_neutral_zero` the blank-cell absence assertion was deleted and replaced with `value == 1.0` (no blank case remains in either suite); the byte-offset check in `test_dash_span_points_at_the_dash_cell` was deleted.

Evidence: `venv_t1/bin/python -m pytest tests/test_pg_economic_observations.py tests/test_pg_economic_observations_probes.py -q` → 53 passed; new probes → 13 failed, 8 passed; `check_annual_validates.py` → `VALIDATED {'pg_diluted_eps': (12.07, '2026-06-30'), …}`. The seat's grep gate was too narrow (misses "Core EPS Reconciliation", "Non-GAAP Measures", "Organic Sales Change by Segment", "incremental charge", "dilution").

Gaps: contract-delta/full CI pack not re-run; real P&G release layouts not verified against a live filing; R1–R6 not re-opened; `documents.py` beyond the added line not reviewed.
