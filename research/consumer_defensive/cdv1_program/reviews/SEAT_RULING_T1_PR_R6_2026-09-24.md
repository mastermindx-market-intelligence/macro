# Seat ruling — PR #7905 round 6 (on `OPUS_T1_PR_REVIEW_R6_2026-09-24.md`, content head d5a9a9357a48)

Adjudicator: Fable Meta-CEO seat. Review ACCEPTED in full; head REJECTED. All 78 reviewer probes are frozen unchanged as `tests/test_pg_economic_observations_probes_r5.py` (single author = seat; 19 red at the freeze) and wired into the gate job in the same commit. Rulings R43–R48 bind round 9 on top of R1–R42. A repair lane may not edit any of the five frozen probe files.

- **R43 (colspan grid; spanning-header addressing).** A table is read as a column-aligned grid in which a spanning cell occupies every column its `colspan` covers (`_grid`, read from the cell's own markup at its span); the header band, column header forms, band period context, the column locator, the cell locator and the volume cross-check all read that grid. Band period context is classified per cell AND per column's stacked band text, so a label split over two rows ("Fiscal Year" / "2026", "Three Months Ended" / "March 31, 2026") is read whole (r37e–g). A header spanning several columns addresses, within the target row, the single spanned column holding a metric literal (the "$" / "3.07" split of EDGAR tables); several literals under one spanning header are ambiguous and absent (r37h, r37i). The whole-table refusal of R37 stands: a band naming a twelve-month period anywhere refuses the table (r37d).
- **R44 (period vocabulary).** Cumulative labels — six/nine/twelve months ended, year to date, year ended, full year, annual — are out of scope; "Quarter Ended <date>" and "Three-Month Period Ended <date>" are quarter labels compared date-for-date with the admitted quarter end, comma-insensitive (r35k, r35l). The pure-period sub-heading regex recognises the same forms.
- **R45 (one reconciliation paragraph).** `_reconciliation_paragraph` is the ONE selection of a bounded-text observation — under a reconciliation / non-GAAP heading, naming the row label, not carried under another quarter's heading, and within `PG_BOUNDED_TEXT_MAX` (240) characters — used by the extractor to bind and by the validator to replay: the receipt's byte span (converted to characters, never used as a character index) must lie inside that paragraph's span and the stored value must equal its text (r35f–i). A paragraph the parser never emits (hidden markup) cannot be selected.
- **R46 (the validator replays the SELECTION).** `locate_pg_observation` re-runs the extractor's own plan — admitted tables by heading rule, the exact-one cell rule, the header form and its period — over the caller-held source; a present row validates only when its receipt lies inside that cell, the replayed bytes are the cell's visible text (never markup or a comment inside the cell), and the header, column and period agree (r42d–f, r27, r42). `replay_table_layout` remains the shared cell confirm and enforces the visible-text rule itself.
- **R47 (statements).** A prior-period qualifier is a whole word (`prior`, `previous`, `year ago`, `last year`, `a year earlier`): "previously announced" and "priorities" do not suppress a conflict (r40c, r40d). A sentence counts when it names Total P&G anywhere and binds the total-volume measure to a growth verb and a percentage written as "%" or "percent" (r40e, r40h, r40i); sentences split on `.`, `;`, `!`, `?`.
- **R48 (structural combined subject; five frozen suites; wording).** The extractor labels an absence "combined" only when exactly ONE admitted drivers table presents a Volume/Mix column, the same condition the validator checks (r39d). Five frozen suites (probes, r2, r3, r4, r5) run in the gate job with the original suite. R41's sentence "no fixture literal names a month-day, a quarter ordinal or a year" is corrected to: no PERIOD FORM of the synthetic release is hardcoded; filing dates, the workspace `asof` and the ordinal map remain fixture constants. Commit messages name only hunks their diff contains.

Ruling-level gaps the reviewer raised and the seat's disposition: (a) the validator never checks that an absence is really absent — a present value can be hidden by a forged plain absence; ACCEPTED as a round-10 ruling candidate (validator re-extracts and refuses an absence whose metric it can uniquely address), not folded into round 9 because it changes the absence contract of every consumer; (b) a year-level drivers heading naming the admitted fiscal year counts as consistent for any quarter of that year — STANDS (R35: year-level signals are consistent, not quarter-binding; the table binds only through its own quarter context); (c) prefix-matched headings accept "…Outlook"/"…Guidance" tables — ACCEPTED as a round-10 candidate; (d) "Compared with the prior year, Total P&G volume increased 4%" is skipped under R40's own wording — STANDS as ruled (a qualifier before the verb scopes the statement); the cost is a missed conflict, never a wrong bind.

Process note: `parse_release_blocks` normalises with a placeholder accession and form "8-K"; block kinds, texts and spans do not depend on either (the reviewer's parity probe r35j pins this); section keys may, and are unused by this module.
