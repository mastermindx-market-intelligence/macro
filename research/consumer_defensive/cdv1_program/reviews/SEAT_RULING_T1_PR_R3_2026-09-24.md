# Seat ruling — PR #7905 round 3 (on `OPUS_T1_PR_REVIEW_R3_2026-09-24.md`, head c8db6a81cc)

Adjudicator: Fable Meta-CEO seat (CDV-1 program). The review is ACCEPTED in full; the head is REJECTED. Rulings R17–R26 bind the next repair round on top of R1–R16. The reviewer's 21 new probes are frozen as `tests/test_pg_economic_observations_probes_r2.py` (single author = seat) and wired into the gate job in the same commit; a repair lane may not edit either probe file.

- **R17 (R7 — annual forms refused, derived heading/column forms).** A table binds only under the heading that immediately governs it: the table locators reset on every non-matching heading, so a "Twelve Months Ended" / "Fiscal Year" / "Year Ended" table placed after a quarterly heading is a typed absence, never Q4. Heading forms accepted are derived from the fiscal scope: "{Ordinal} Quarter {fy}", "Fiscal Year {fy} {Ordinal} Quarter", "{Ordinal} Quarter Fiscal {fy}", "Q{q} FY{fy}"; column headers accepted include "Three Months Ended <scope end date>" and "Q{q} FY{fy}" for both current and prior columns.
- **R18 (R7 — no sentence fragments, no fixture-only literals).** Reconciliation and segment tables are located by heading KEYWORD sets (a heading containing "Reconciliation" plus the metric family; a heading containing "Segment") and by the row label — never by sentence fragments ("incremental charge", "dilution") and never by an exact heading literal ("Non-GAAP Measures", "Core EPS Reconciliation", "Organic Sales Change by Segment"). Every column name the engine accepts is either in the R14 vocabulary or derived by a documented rule from a metric name and the scope; the fixtures must not be the only place a literal appears.
- **R19 (R8 — extractor/validator symmetry).** One period-form set, one header locator (multi-row headers: label row + year/period row), and one dash-convention locator (anywhere in the source document) are defined ONCE and imported by both the extractor and `validate_selected_facts`; a round-trip test asserts the validator accepts everything the extractor emits from each fixture variant.
- **R20 (R9 — fiscal quarter from scope, fiscal year validated).** The quarter is derived from the scope's fiscal calendar (fiscal-year end month + scope start/end), not from a calendar-month lookup table; delete `_PG_FISCAL_QUARTERS` from both engine files. The event id's fiscal year is validated against the scope and a mismatch is a typed refusal. Fixture: `FISCAL_PERIOD` is the single truth — remove the month lookup (`earnings_economic_fixtures.py:174`) and the calendar-year derivation (`:162`).
- **R21 (R10 — absences and identity).** Absence rows carry the same `fact_id` formula as present rows and the validator checks it; `RIGHTS_PROFILE` is imported from `engine/company_intelligence/qa_exchange.py` (no set-iteration retyping, no second token choice); the workspace document is selected by its document id / role, never `sources[0]`.
- **R22 (R13 — volume cross-check).** When the document carries a second statement of Total P&G volume (a segment table or a summary line) that disagrees with the drivers table, the volume observation is a typed absence naming the conflict (`cross_check_conflict`, added to the closed reason set in `documents.py`), never a silent value. A test with a disagreeing second table is required.
- **R23 (R15 — combined subject only when a combined line exists).** The "combined volume/mix" subject and detail are emitted only when the drivers table actually carries a combined "Volume/Mix" column; otherwise the plain metric subject with the "no unique heading, row label, and column header" detail. The validator requires both "volume" and "mix" in a combined subject.
- **R24 (original-suite hygiene).** Restore the blank-cell absence assertion (a blank cell is an absence and carries no `value`) and the dash byte-offset check; the `fact_id` assertion checks the formula, not only uniqueness/length. Every edit to `tests/test_pg_economic_observations.py` or the fixtures cites a ruling id in its commit message; an uncited edit is a rejected round.
- **R25 (frozen probes ×2).** `tests/test_pg_economic_observations_probes.py` and `tests/test_pg_economic_observations_probes_r2.py` are frozen (`git log --format=%an -- <file>` = one author each) and both run in the gate job with the original suite; all three must be green in the clean venv (`pip install pytest pyyaml`).
- **R26 (widened grep gate).** `grep -nE 'Fourth Quarter|Prior (Diluted|Core)|Volume Conventions|Core Reconciliation|current_year|Non-GAAP Measures|Organic Sales Change by Segment|incremental charge|dilution|_PG_FISCAL_QUARTERS' engine/company_intelligence/pg_profile.py engine/company_intelligence/economic_observations.py` must be empty.

Process note: three glm-5.3 rounds on this task have discharged rulings by literal-matching the tests in front of them; the second frozen suite exists because the first one was satisfied without the engine becoming general. The next round's packet names the rulings, the failing probes, and forbids any literal that appears only in a test or fixture.
