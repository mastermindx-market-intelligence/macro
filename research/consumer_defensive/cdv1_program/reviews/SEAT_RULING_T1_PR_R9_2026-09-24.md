# Seat ruling — PR #7905 round 9 (on `OPUS_T1_PR_REVIEW_R9_2026-09-24.md`, content head af4212037b69)

Adjudicator: Fable Meta-CEO seat. Review ACCEPTED in full — including its four findings that the rulings themselves, not only the code, were built wrongly; head REJECTED. The reviewer's 52 probe cases are frozen as `tests/test_pg_economic_observations_probes_r8.py` (single author = seat; q06 adapted order-insensitively on the reviewer's own statement that its assertion expected the wrong table order while the ordinals were correct). Rulings R61–R65 bind round 12 on top of R1–R60 and SUPERSEDE the constructions named below. A repair lane may not edit any of the eight frozen probe files.

- **R61 (the parser owns row groups, HTML integer parsing and heading levels; supersedes R55's span construction).** A rowspan never crosses a row group: `TableCell.row_group` counts `thead`/`tbody`/`tfoot` boundaries (explicit or implied) and `_grid` drops carried cells at each boundary (q01). A span attribute is parsed exactly as HTML's rules for non-negative integers read it — leading whitespace and "+" skipped, the leading ASCII-digit run is the value, anything after it ignored, non-ASCII digits an error — then bounded to 1..64 (q02, q03; the frozen p03/p04 cases "２", "1_0", "0", "-2", "65" still read 1). R55's "plain ASCII digits only" wording is withdrawn: it diverged from the browser the release was written for. `DisclosureBlock.heading_level` records h1..h6 (0 for a promoted paragraph); additive, outside `to_dict` and ids.
- **R62 (fail-closed everywhere a period can be named — the rule, not a list of places; supersedes R56/R57's constructions).** (a) A governed text is "unknown" not only when it carries a period marker and no verdict, but also when it names the admitted quarter BESIDE a period-form marker (`ended|ending|month(s)|ytd|fiscal|fy`) that no recognised form consumed: "Three Months Ended June 30, 2026 and Three Months Ended March 31st, 2026" (caption), "… vs. Quarter Ended 31/03/2026" (heading) and a fiscal-dash reconciliation heading each refuse (q14, q15, q16). A bare "Quarter" band word over a readable stack is a caption word, not a second period (frozen r19c). (b) Fiscal-year labels read every form in use — "Fiscal '25", "FY-25", "FY’25", "FY 2025", "FY2025", "Fiscal Year 2025", "Fiscal 25" — and a label naming another year makes the text foreign even beside a scope quarter word (q12); the same forms naming the admitted year bind (q13). (c) A period-label paragraph is recognised through its decoration — every parenthetical, the word "unaudited", dashes, colons and semicolons, a leading "For the", edge punctuation, a trailing "and <year>" — and a short line that begins or ends with a pure period form (at most forty characters of other text) is a label too: "Segment data. Three Months Ended March 31, 2026" governs (q10 ×5, q11). (d) Below the band, ANY row holding no metric literal whose text names a period opens a section — one cell or several, any column, any colspan, beside a carried label or an "(unaudited)" cell (q17–q20); a "$"-only row names nothing (q21). R57's "exactly one text cell" construction is withdrawn.
- **R63 (forward-looking sections are a hierarchy; supersedes R59's per-heading construction).** A forward-looking heading (outlook(s), guidance, forecast/-s/-ed/-ing, expect/-s/-ed/-ing/-ation(s), project/-ed/-ing/-ion(s), target/-s/-ed/-ing, estimate/-s/-d/-ing, anticipate/-s/-d/-ing/-ion(s)) governs every table, reconciliation paragraph and volume statement after it until a heading of the same or a higher level (h-level as written; a promoted or plain-text heading counts as level 2); a period-label paragraph never releases it (frozen p31) and a lower-level topic heading does not either (`Outlook` > `Segment Results`, q26). A caption is a governing text (q27). A heading that names the admitted quarter's actual results is never forward-looking even when it also says "Outlook" (q29). The governing-heading admissibility test is replaced by this state; the reconciliation path honours it (q30).
- **R64 (spanning sub-cells; supersedes R58's key construction).** Under a shared spanning key, the band cells outside the key must all be a bare "$" or a parenthetical for the single-literal rule to apply; "As Reported" / "Restated" sub-cells tell the columns apart and the observation is absent (q22), while "(unaudited)" sub-cells do not (frozen p05).
- **R65 (eight frozen suites; adaptation; corrections).** Eight frozen suites (probes, r2–r8) run in the gate job with the original suite. q06 is adapted order-insensitively as recorded above. Corrections: R55's "as HTML does" claim about span parsing was false (R61); the round-11 message's "325 with them" total was wrong — the three companion suites add 59, so the total is 369; `af4212037b69`'s claims "a fiscal-year label naming another year makes the text foreign" and "strips … a trailing parenthetical" held only for the forms then probed.

Reviewer gaps and dispositions: the four ruling-construction flaws (R55 span parse / row groups, R57 one-cell fallback, R58 key, R59 scope) → R61–R64 as written; "not established whether failures were new or inherited" — moot, every shape is now frozen. Left open for round 13: `tfoot` before `tbody` (the group counter handles it; unprobed), a caption inside an unclosed `td`, binding values from nested tables, quarters other than Q4 across the vocabulary, the volume cross-check with colspan section rows, the plain-text table path, and round-6 disposition (a) (the validator refuses a forged plain absence it can uniquely address).

Process note: R61 changes the parser additively only — `TableCell.row_group`, `DisclosureBlock.heading_level` — and no existing reader changes (`tests/test_fundamental_forensics_disclosure_diff.py`, `tests/test_earnings_release_binding.py`, `tests/test_company_intelligence_refresh.py` green). The validator's acceptance of every wrong value the reviewer built follows from the shared scan: once the extractor refuses a shape, `_verify_pg_replay` refuses the same shape — the six validator probes now skip by the reviewer's own design because the extractor no longer binds the wrong value.
