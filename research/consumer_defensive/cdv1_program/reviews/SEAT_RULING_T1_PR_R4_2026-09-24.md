# Seat ruling — PR #7905 round 4 (on `OPUS_T1_PR_REVIEW_R4_2026-09-24.md`, head 635877f3df)

Adjudicator: Fable Meta-CEO seat. Review ACCEPTED; head REJECTED. Rulings R27–R34 bind the next round on top of R1–R26. The reviewer's 33 probes are frozen as `tests/test_pg_economic_observations_probes_r3.py` (single author = seat) and wired into the gate job in the same commit.

- **R27 (R17 — no fallback binding; heading-scoped period in the validator).** A table binds ONLY when its governing heading matches a scope-derived quarterly form; a "Total P&G" row under a non-matching or absent heading is a typed absence, never a fallback. A twelve-month / fiscal-year qualifier in the governing heading OR in the table's own header row refuses the table. For column-label metrics the validator compares the governing heading's period to the fact's period, not header text alone. `test_scope_quarter_matches_workspace_quarter` currently passes because of the defect: a Q3 scope over a Q4-dated document must be REFUSED, and the test's expectation is corrected under this ruling (cite R27).
- **R28 (R19 — one header locator).** The extractor and the validator call the SAME header-locator function with the SAME normalisation (case, whitespace, `&nbsp;`, double spaces); `replay_table_layout` is either used by both or deleted. The round-trip test includes lowercase, `&nbsp;` and double-space header variants.
- **R29 (dash convention must be visible text).** A dash-to-zero convention counts only when it is visible rendered text of the document body — never an HTML comment, `<script>`/`<style>` content, an attribute value, or a hidden element. Probe r19e binds this.
- **R30 (R20 leftovers).** The extractor itself refuses (typed) an event id whose fiscal year/quarter disagrees with the scope, before any fact is emitted; the fixture derives every period end date from `FISCAL_PERIOD` (any quarter), no hardcoded June 30.
- **R31 (R21 — named private profile).** The private rights profile is an explicit named constant, never derived by `min`/iteration over `RIGHTS_PROFILES`: if `qa_exchange.py` exposes a private-profile constant, import it; otherwise declare `PG_PRIVATE_RIGHTS_PROFILE = "<literal id>"` in `pg_profile.py` and assert at import time that it is a member of `RIGHTS_PROFILES` and differs from `RIGHTS_PROFILE` (typed error otherwise).
- **R32 (R22 — general volume cross-check).** The cross-check recognises any second statement of Total P&G volume: a header containing "volume" with any qualifier ("Volume Growth", "Total Volume", with/without acquisitions & divestitures), at any header-row depth, and a summary sentence binding "volume" to a percentage; it compares only same-period, same-basis statements (a prior-year comparison table or a dash in the second table is NOT a conflict). A repo-suite test with a disagreeing second table and one with a non-conflicting prior-year table are required.
- **R33 (R23 — subject checked alone).** The validator checks the absence subject on its own: a combined subject must equal `<metric> combined volume/mix` exactly and is accepted only when the drivers table carries a combined column; "… combined" alone is refused.
- **R34 (three frozen suites; citations).** `probes.py`, `probes_r2.py`, `probes_r3.py` are frozen (one author each) and all run in the gate job with the original suite; all four must be green in the clean venv. Every test/fixture hunk cites the ruling it serves in its own commit; a commit message may not claim a hunk the diff does not contain.

Process note: rounds 5c and 6 each discharged most of their rulings by real engine changes but left a fallback path or a second implementation the frozen probes did not reach; the third suite closes those paths. The R26 grep gate is unchanged.
