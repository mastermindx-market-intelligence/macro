# Opus red-team of PR #7905 — round 4 (head 635877f3df, 2026-09-24 ~13:00Z)

READ_ONLY Opus reviewer bounded to rulings R17–R26 (`SEAT_RULING_T1_PR_R3_2026-09-24.md`). All three suites green at the head (41 + 20 + 21 = 82 passed); both frozen suites byte-identical to their freeze commits and single-author; R26 grep empty. The reviewer wrote a third generation of 33 probes: **15 failed / 18 passed** (frozen by the seat as `tests/test_pg_economic_observations_probes_r3.py`).

**VERDICT: REJECT.** R17 (blocker), R19 (major), R22 (major) not discharged; R21, R23 partial; R20, R24 discharged with leftovers; R18, R25, R26 discharged.

| Ruling | Verdict | Evidence at 635877f3df |
|---|---|---|
| R17 | NOT DISCHARGED (blocker) | `pg_profile.py:290-304` `_drivers_table`: when no heading matches, a fallback binds the only table with a "Total P&G" row whatever heading sits above it; `:291-293` takes the first keyword match and never refuses a twelve-month qualifier. A drivers table only under "Twelve Months Ended June 30, 2026" is emitted as Q4 and validates; a twelve-month table placed first wins; a Q3 scope over a June-quarter table emits period 2026-03-31 and validates — because for column-label metrics the validator compares header text only (`economic_observations.py:114-116`). The repo's `test_scope_quarter_matches_workspace_quarter` (`tests/test_pg_economic_observations.py:319-328`) passes only because of this defect. Heading/column forms themselves work (Q1/Q2/Q3, prior-first, annual-heading reset). |
| R18 | DISCHARGED | keyword headings and reversed segment columns work; no fixture-only literal found. |
| R19 | NOT DISCHARGED (major) | two header locators remain: extractor `_column` (`pg_profile.py:335-351`, normalises case/whitespace at `:344`) vs validator `replay_table_layout` (`:132-190`, never called by the extractor) comparing raw header text (`economic_observations.py:115,118`) → lowercase, `&nbsp;` or double-space headers are emitted then refused `replay_mismatch`; the round-trip test uses only the fixture's spellings. Period-form set and dash pattern are shared once (correct). |
| R20 | DISCHARGED (leftovers) | no month table; `_fiscal_identity` (`pg_profile.py:216-225`); validator refuses wrong year/quarter (`economic_observations.py:138-142`). Leftovers: the extractor itself still emits under a wrong-year event id (only the validator refuses); fixture hardcodes June 30 (`earnings_economic_fixtures.py:43-44`) so it can only build Q4 documents. |
| R21 | PARTIAL (minor) | absence `fact_id` checked (`economic_observations.py:194-201`); document by role (`:143-151`); but `pg_profile.py:28-30` still derives the private profile by `min(... RIGHTS_PROFILES ... != RIGHTS_PROFILE)` — a second-token choice by iteration. |
| R22 | NOT DISCHARGED (major) | `_volume_cross_check` (`pg_profile.py:490-505`) recognises only header text "volume"/"volume with acquisitions & divestitures", only in the first two rows, never summary sentences → misses "Volume Growth", "Total Volume", three-row headers, summary paragraphs; raises FALSE conflicts on a prior-year comparison table and on a dash in the second table. No disagreeing-table test in the repo suite. |
| R23 | PARTIAL (minor) | extractor correct (`pg_profile.py:440-452`); validator checks subject+detail joined (`economic_observations.py:228-234`) so a forged subject "… combined" is accepted. |
| R24 | DISCHARGED (nits) | blank-cell absence, dash byte-offset and `fact_id` formula assertions restored; `blank_volume` fixture hunk cites R20 not R24; commit 635877 claims an R23 test hunk that is not in the diff. |
| R25 / R26 | DISCHARGED | frozen suites intact and wired (`legacy-jobs.yml:13471-13473`, `:13484`); grep empty. |

Surfaced for the seat (not charged to a ruling): a "dash means zero" note hidden in an HTML comment turns a "—" cell into 0.0 (probe r19e) — consistent with R19's literal wording ("anywhere in the source document") but a fabrication vector.

Evidence: 82 passed in the clean venv; `git diff … --stat` on both frozen suites empty; commits 2009ac4a (R17-R18), 4436e617 (R19), 2f7cd881 (R20), 5a20f4e6 (R22, touches only `documents.py:86`), 635877f3 (R23-R24); new probes 15 failed / 18 passed; `inspect_r4.py` shows `r17a … VALIDATES`, `r17c … VALIDATES`, `r22a_growth … VALIDATES`, `r22e cross_check_conflict`, `r22f_dash2nd cross_check_conflict`. Gaps: contract-delta/full pack not run; no live P&G layout check; R1–R16 not re-opened (no regression hit).
