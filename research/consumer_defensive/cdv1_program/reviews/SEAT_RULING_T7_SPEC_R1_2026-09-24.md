# Seat ruling — CDV-1 Task 7 design spec, round 1 (2026-09-24 ~08:05Z)

Fable Meta-CEO seat (session 251f88c8) on PR #7904 after the Opus read-only red-team (`OPUS_T7_SPEC_REVIEW_2026-09-24.md`, VERDICT: REJECT). The review's findings are ADOPTED; the seat rules the judgment items the reviewer left to the seat:

- **R1 Mount.** The Theme Tracker mount is DROPPED for CDV-1: `us_sector_staples` is an unmapped basket ("Not a theme", `config/theme_crosswalk.yml:509`), no such row renders, and adding one would put a sector basket on the foresight board with lane/stance scoring, which the plan forbids. The ONE mount for CDV-1 is the US sector page for XLP (`templates/sector.html.j2`, the real anchor after ~:309), gated by the sector symbol the template already receives, **collapsed by default** (a compact toggle row; the dossier mounts on first expand and destroys on collapse/logout). A themes-page entry is a later-milestone question, recorded as an open item, not built here.
- **R2 Assets.** The plan's `templates/earnings_wire/earnings-economic.js` path is AMENDED: sector pages do not load wire-folder assets (the wire build copies them only into its own `assets/` folder, `build_earnings_public_wire.py:969-976`, published by a different producer — an ordering and dead-link hazard). The JS and CSS ship as top-level PAIRED plain-copy assets `templates/earnings-economic.js` ↔ `site/earnings-economic.js` and `templates/earnings-economic.css` ↔ `site/earnings-economic.css`, the house idiom for page-scoped scripts (`templates/biocatalyst.js`, `government-revenue-dossiers.js`), served by the VPS pull with no render dependency and guarded by `scripts/check_template_site_sync.py`. The partial stays `templates/earnings_wire/_economic_dossier.html.j2` and is included by `sector.html.j2`.
- **R3 Stance.** The closed interpretation key set gains no field. The glance-tier stance sentence is a FIXED lookup keyed by the ordered set of `findings[].rule_id` (a pinned table in §5, first matching row wins, plus a default row) and the state chip is a fixed lookup on `selection.currentness`; both are rendering, never calculation. State-chip copy is plain-word: "Up to date" / "Newer filing not yet read" / "Freshness not confirmed" / "Not available" (ZH equivalents pinned).
- **R4 Two art directions.** The CSS must carry theme-scoped blocks (`:root[data-theme="light"]` / dark) that differ in named mechanisms: scrim vs no scrim, sheen vs hairline, button ring vs shadow, canvas step; degraded states per theme. Token-only swapping is not a light design (TP-0).
- **R5 Chips.** All three basis chips are semantically neutral (same neutral token family; distinguished by label, not color).
- **R6 Response contract.** The current-view response is the Task 6 wrapper `{generation_id, manifest_sha256, record_sha256, slug, interpretation, …}`; the key check runs on `interpretation`. The evidence request is `GET /api/earnings/v1/records/{slug}/economic-evidence/{fact_id}?generation=&manifest_sha256=&record_sha256=` from the displayed response; a click DOES fetch (once per open, aborted on close/auth change). Pin the JSON→DOM field map row by row.
- **R7 Host wiring.** `sector.html.j2` includes the partial and loads `/earnings-economic.css` + `/earnings-economic.js` (root-relative, matching how the page loads `../theme.js` today — use the same relative form the template uses); the init reads the issuer from a `data-economic-issuer` attribute the template renders; `fetchAuthenticated` follows `earnings-wire.js:58-70` (`sb.auth.getSession()` → Bearer, `cache: 'no-store'`); `onAuthChange` subscribes to `mdx-auth` and ignores `PREFS_SAVED` (`theme.js:3212`); `nativeIssuerId` is the ticker the API resolves through the native registry — the UI passes the symbol only.
- **R8 Density.** Collapsed row ≤ 44 px; expanded dossier `max-height` pinned at 390 px with inner vertical scroll (`overflow-y:auto`), no change to page height while collapsed; no header, no hero.
- **R9 Copy and a11y.** `reported_vs_organic_difference` wording is direction-neutral ("Reported and organic sales growth differ; see the bridge"); `missing_consensus` says the result cannot be read as a beat or a miss; ZH per the review (报告每股收益与核心每股收益, 已是最新, 一致预期); per-row bilingual accessible names on evidence buttons; the unentitled state carries a real sign-in control; accessible labels bilingual.
- **R10 §1 status.** §1 is now seat-ruled by this document; the spec's §8 questions are answered here; the fallback-token approach (specimen-only spacing/radius/motion tokens with inert fallbacks) is RATIFIED for this slice.

Repair round: fabric lane `cdv1_t7_spec_r2` on PR #7904; Opus re-review follows; STATUS flips to ADJUDICATED at the seat.
