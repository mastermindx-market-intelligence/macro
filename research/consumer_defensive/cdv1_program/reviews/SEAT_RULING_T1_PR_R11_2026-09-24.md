# Seat ruling — PR #7905 round 11 (on `OPUS_T1_PR_REVIEW_R11_2026-09-24.md`, content head 0f974054e6bb)

Adjudicator: Fable Meta-CEO seat. Review ACCEPTED in full — its wrong-bind findings (s01, s10 ×14, s11, s12, s18, s20 ×3, s21, s31 ×2, each accepted by the validator), its fail-closed findings (s02, s13, s15, s16, s17, s22, s23, s30) and all seven ruling-construction findings included; head REJECTED. The reviewer's 59 probe cases are frozen as `tests/test_pg_economic_observations_probes_r10.py` (single author = seat; s07 struck by R71, s25 struck by R73). Rulings R71–R75 bind round 14 on top of R1–R70 and REPLACE the constructions named below. A repair lane may not edit any of the ten frozen probe files.

- **R71 (drawn order per CSS 2.1 §17.2; replaces R66's "every thead, every tfoot").** Only the FIRST `thead` is the header group and only the FIRST `tfoot` the footer group; every other row group is drawn in source order. `_grid` applies this over the parser's `row_layout` — the parser is untouched (it records every group; the engine decides the drawing). s07 ("`rowspan="-0"` reads 0") is STRUCK: the HTML rules for parsing non-negative integers accept an optional "+" only, so a leading "-" is a parse error and the attribute takes its default of 1 — the reading the frozen p04 case already pins for `rowspan="-2"`.
- **R72 (period forms; amends R67).** Forms added: the fiscal-year-first Q form (`FY26 Q4`, `Fiscal Year 2026 Q4`, `fiscal '26 Q4`) and an ordinal quarter with an apostrophe year (`Fourth Quarter '25`, `’25`). The year that qualifies an ordinal quarter, and a fiscal-year label, compare to the FISCAL year alone (Q1 FY2027: "First Quarter 2026" is foreign, s11); a bare year is current when it is the fiscal year or the quarter-end calendar year (a column reads "2026") and PRIOR when it is the year before either — neutral in a heading ("2026 vs 2025", s14), the prior column in `band_context`. The caption-word forgiveness of R67 applies only when the WHOLE residual beside a quarter form is "quarter"/"quarterly" ("Next Quarter" refuses, s21). The residual token classes are widened (apostrophe years, half-year forms, TTM/LTM, calendar/CY, weeks, numeric and ISO dates, spaced or hyphenated Q-numbers, relative words); that lexicon is by nature a list, and it now matters only for band cells, captions and prose — for headings R73 makes it irrelevant.
- **R73 (positive admission by route; replaces R68 and R67's label constructions).** A table binds only under a governing heading — the immediate heading or label, or the enclosing topic — that DECOMPOSES completely: recognised period forms that are scope, year-level or prior-comparison forms (never annual, never foreign, nothing unreadable left over) plus words drawn entirely from ONE route's vocabulary containing that route's key word. Segment: key `segment(s)`; `results, result, organic, sales, growth, change, by, reportable, business, information, net, of, and`. Drivers: key `drivers`; `net, sales, change, driver, of, and, by`. Quarterly/EPS: no key, a scope form REQUIRED; `results, result, eps, diluted, net, earnings, per, share, common, consolidated, information, financial, income, statement(s), of, and`. Reconciliation: key `reconciliation(s)` or `non-gaap`; `of, gaap, measure(s), core, eps, to, diluted, net, earnings, per, share, common, and, financial`. Glue (for, the, vs, versus, compared, with, to, and, of, punctuation) is ignored. A bare fiscal-year label or bare year with no scope form is an annual heading for every route but drivers (year-level, R35). Every other heading admits nothing — "Supplemental Synthetic Data" (frozen r42e), "Segment Results vs. Expected" (frozen r25), "Looking Ahead: Segment Organic Sales", "Segment Organic Sales Goals", "Segment Sales Plan" (s20), "H2 2026 / TTM / CY2026 / 2026-03-31 … Segment Results" (s10: an unlisted token is a word outside every vocabulary, so the closed lexicon can no longer admit anything). s25 ("… Guidance Recap Segment Results" binds) is STRUCK: a forward word in a governing heading refuses whatever period it also names, exactly as frozen r25 refuses "vs. Expected"; refusing is fail-closed, never a wrong bind. HIERARCHY: the document TITLE is the FIRST heading block, whatever its level — judged for its period, never as a section (a masthead cannot refuse a release, s22); a later h1 governs like any heading (frozen r24). An ANCESTOR heading refuses on positive NON-results evidence only — a forward-looking word (vocabulary extended with "looking ahead" / "going forward"; still a list, so an unlisted forward cue in an ANCESTOR remains a known gap, while in the immediate heading it is refused by admission) or a period other than the admitted quarter; a neutral topic ("Overview") neither admits nor refuses (s23). The shallowest-open-level rule of R68 is kept. TWO-LAYER CONTEXT: headings carry a persistent period context (a heading that names a period sets it until another heading names one); paragraphs carry a context cleared at every heading. A period LABEL is a paragraph that carries a strong period token and is NOT a sentence — no terminal sentence punctuation, no metric literal — with no length cut-off (s15); it is classified in full, so an unreadable, foreign or cumulative label refuses (frozen r13 ×5), and a scope label sets scope only when the heading context is None or scope — it never re-opens a quarter a heading closed (s18). Prose refuses only on a READABLE foreign or cumulative form; an unreadable token or a scope mention in prose is prose (s16, s17). A label that is not a pure period form replaces the topic (R50 parity).
- **R74 (plain sub-cells; amends R69).** Matched on casefolded text. Plain = a bare "$", "(unaudited)", a footnote mark that is digits, ONE letter or asterisks — stacked marks "(1)(2)" and bare asterisks included. Two-letter marks such as "(cy)/(py)" or "(rp)/(cc)" tell columns apart and the observation is absent (s31); "(Unaudited)" is plain (s30).
- **R75 (ten frozen suites; corrections).** Ten frozen suites (probes, r2–r10) run in the gate job with the original suite: 456 passed + 22 skipped at the round-14 head; the parser, binding and refresh suites 59 passed. Corrections: `0f974054e6bb`'s claims "drawn order", "any leftover year, month, quarter number or period word is unknown", "Q-FY in every spelling" and "`_PLAIN_SUBCELL` allows only $, (unaudited) and footnote marks" held only for the shapes then probed (s01, s10, s13, s31); its "(6 skipped)" and "415 passed" were wrong — the ten suites gave 409 passed + 12 skipped and 468 with the three companion suites; R70's "356 + 59 = 415" was the pre-r9 total, not the total at that head. `eadf33f98ea`'s `TableCell.rowspan` field comment ("1 when absent or invalid") is stale now that 0 is kept — a parser comment left for the next parser-touching commit.

Reviewer construction flaws and dispositions: (1) every-thead/every-tfoot → R71; (2) closed residual lexicon → R73 (headings admit by grammar, so no lexicon can admit) with R72 widening what band cells and prose can read; (3) calendar year on ordinal quarters → R72; (4) caption-word erasure of relative quarters → R72; (5) label length cut-off and prose promotion → R73; (6) results-word override, closed forward vocabulary, masthead refusal → R73; (7) two-letter and case-sensitive marks → R74. Left open for round 15: nested tables inside `thead`, a caption inside an unclosed `td`, the plain-text table path, volume statements under a non-results section, non-Q4 quarters across the vocabulary, unlisted forward cues in ANCESTOR headings, band cells' reliance on the token lexicon, the stale `TableCell.rowspan` comment, and round-6 disposition (a).

Process note: this round changes `engine/company_intelligence/pg_profile.py` only; the parser is untouched.
