# Seat ruling — PR #7905 round 5 (on `OPUS_T1_PR_REVIEW_R5_2026-09-24.md`, head 9d3585866b)

Adjudicator: Fable Meta-CEO seat. Review ACCEPTED in substance; head REJECTED. Two of the reviewer's 41 probes are OVERRULED (r27d, r27g — R35); the remaining 39 are frozen as `tests/test_pg_economic_observations_probes_r4.py` (single author = seat; r27i carries a one-line seat adaptation under R41, noted inline) and wired into the gate job in the same commit. Rulings R35–R42 bind round 8 on top of R1–R34. A repair lane may not edit any of the four frozen probe files.

- **R35 (document identity; r27d/r27g overruled).** A document that carries NO period signal binds by the admitted scope: the frozen r3 probes r22a–e, r23b and r19b bind drivers tables that sit under only a year-level drivers heading with a Q4 scope, and r27d/r27g would contradict them, so they are struck. What must be refused is a document whose period signals exist and none is consistent with the admitted scope — consistent = a heading or header band naming the admitted quarter, or a year-level drivers heading naming the admitted fiscal year. Such a document is not the admitted release: every observation is a typed absence, the reconciliation text included (`document_period_verdict`, applied by the extractor before binding and by the validator before any replay). A reconciliation paragraph carried under ANOTHER QUARTER's heading is that quarter's statement and is absent (r27i); a twelve-month heading does not refuse it, because a Q4 release's non-GAAP section covers both periods.
- **R36 (one parser).** The validator replays through the SAME parse the extractor bound with: `parse_release_blocks(source)` (the release normaliser over the caller-held text; the accession is a placeholder because block spans do not depend on it), one `_table_scan` for period context, and one cell locator (`replay_table_layout`) that resolves the byte receipt against parsed cell spans. The regex readings of headings, table cells and visible text (`<h1-6>` scans, `_TABLE_CELLS`, `_cell_text`, `_INVISIBLE_MARKUP`) are DELETED, not kept beside the parser. All-caps `<p>` labels are headings because the parser says so (r27f).
- **R37 (header band).** A table's header is its band: the leading rows with an empty label cell or no metric literal beyond it (a dash, a percent, a currency amount — a bare integer such as a year label is not a literal). A column's header forms are the stacked band text first, then each band cell alone; `_column`, the confirm step and the volume cross-check all read that band, so `<br>`-split, two-row and three-row headers round-trip (r28d, r32b, r19c) and the Total P&G row may sit anywhere below the band (r32a). The band's own period context is read per cell and aggregated: any twelve-month label — including a bare "Fiscal Year 2026" with no quarter qualifier — refuses the table; a cell naming the admitted quarter keeps it in scope beside a prior-period column; a band naming periods and never the admitted quarter is foreign (r27b, r27e, r32g). A band can only narrow the heading context, never widen it.
- **R38 (visible text = parser blocks).** `visible_text` is the join of parsed block texts; hidden, template, head and attribute content is invisible because the parser emits no block for it (r29a–d). There is no second, regex-shaped reading.
- **R39 (combined subject is structural).** The validator accepts `<metric> combined volume/mix` only when the metric is one of the three volume/mix observations (`PG_COMBINED_VOLUME_MIX_METRICS`) AND the document's ONE in-scope drivers table carries a band column whose stacked header starts with "volume/mix" (`combined_volume_mix_presentation`, the same band the extractor used to mint the absence). Any other subject that is not exactly the metric is refused (r33a–d).
- **R40 (statement patterns).** A sentence is a total-volume statement when it binds Total P&G to "volume" with the subject before or after ("Unit volume for Total P&G …", r32e) and "volume" is not "organic volume", "volume excluding …" or "volume/mix" (r32c, r32d). A prior-period qualifier ("prior", "previous", "year-ago", "last year", "a year earlier") scopes the statement only when it precedes the verb: "increased 4% versus the prior year period" is a same-period statement with a comparison basis (r22b, still a conflict) while "In the prior-year quarter, … increased 7%" is not (r32h). Sentences under an out-of-scope heading or naming an out-of-scope period are skipped.
- **R41 (fixture from fiscal_period).** Every period form of the synthetic release — fiscal year, quarter ordinal, "Three Months Ended <long date>", ISO column dates, drivers years — derives from the `fiscal_period` passed to `pg_bound_case` / `pg_workspace_case` / `pg_source_texts` (r30c). `document_period` splits the document from the workspace period when a test deliberately needs a foreign document: the corrected `test_scope_quarter_over_a_q4_document_yields_only_absences` (R27) and r27i. No fixture literal names a month-day, a quarter ordinal or a year.
- **R42 (no first-match).** Exactly one (table, row, column) across every in-scope candidate table may address an observation; a second addressable table is ambiguity and a typed absence. `_quarterly_table`'s `exclusive=False` first match is deleted; `_column` returns the header form in band order so the extractor and the validator name the same header.

Commit honesty (R34 carried): each round-8 commit message names only hunks its diff contains; the over-claims the reviewer found in 96483d090d and b34194555c stand in history and are corrected by this round's diffs, not by rewriting pushed commits.

Process note: the fabric was jammed for round 8 (mini2 resolver wedged, mb at its marker cap, m1 window closed, m2 not a lane host); the seat executed the round directly under the execution continuation law — no worker had started, the seat held lawful tools and custody, and no other owner was working the artifact. The fourth suite is frozen RED before the repair commits so the repair is judged by probes it could not shape.
