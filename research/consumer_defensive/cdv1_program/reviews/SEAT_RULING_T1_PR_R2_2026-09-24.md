# Seat ruling — CDV-1 Task 1 (PR #7905), round 2 (Opus re-review REJECT at bc33493b)

Authority: Fable Meta-CEO seat for CDV-1. Input: `OPUS_T1_PR_REVIEW_R2_2026-09-24.md` (6 BLOCKER / 5 MAJOR / 3 MINOR; R3 and R4 HELD, R1/R2-period/R5-quarter/R6-fact_id NOT HELD). The seat upholds the verdict. Two lanes have now discharged rulings by test rather than by property — the pinning tests were generated from the same scope logic as the extractor, so they could not fail. **Change of tactic (binding):** the reviewer's probes P1–P11 are committed by the seat as `tests/test_pg_economic_observations_probes.py`, RED at bc33493b. The repair lane's job is to make them GREEN. **That file is frozen: a lane that edits it, skips it, or weakens it has failed the round.** Rulings R1–R6 of round 1 stay in force; the items below are the concrete discharge conditions.

## Binding repairs (R7–R13)

- **R7 (R1 not held — F1/F2/F3).** No heading, row label, column header, section title or sentence fragment in `pg_profile.py` may be a fixture-only literal. Allowed sources of vocabulary are exactly two: (a) the metric DEFINITIONS (P&G's own metric names: "Diluted net earnings per share"/"Diluted EPS", "Core EPS", "Organic sales", "Net sales", "Volume", "Mix", "Pricing", "Foreign exchange", plus definition-owned section keyword sets such as {"reconciliation", "non-gaap"} and {"segment"}); (b) derivations from `fiscal_scope` — for quarter ordinal `q`, fiscal year `fy`, period end `pe` and prior period end `ppe`: heading forms `"{Ordinal} Quarter Fiscal Year {fy}"`, `"{Ordinal} Quarter {fy}"`, `"Fiscal Year {fy} {Ordinal} Quarter"`, `"Three Months Ended {Month D, YYYY}"`; column-header forms for current and prior derived from `pe`/`ppe` (`"Three Months Ended …"`, `"{Month D, YYYY}"`, `"Q{q} FY{fy}"`, the ISO form as ONE of the derived forms, never the only one); annual forms (`"Fiscal Year {fy} Results"`, `"Twelve Months Ended"`) are REFUSED under a quarterly scope. Prior-period values are read from a prior-period COLUMN; "Prior … EPS" ROW labels are not a supported form. The reconciliation block is located by heading keyword set + the definition's metric name, never by a sentence. `current_year` calendar arithmetic is deleted; everything comes from `fiscal_scope`.
- **R8 (R2 period half — F4/F5/F6).** Replay compares the replayed excerpt to the stored VALUE and to the stored PERIOD: the cell's column header (resolved through the derived header forms) must equal the fact's period; a current/prior swap, a current fact pointed at the prior cell, and a core fact pointed at the GAAP cell are each refused as `replay_mismatch`. A dash replays to 0.0 only when the document's dash-convention span is bound to the same document; otherwise it is a mismatch.
- **R9 (R5 quarter — F9/F10).** The validator compares `workspace.fiscal_period.quarter` to `fiscal_scope.quarter` directly — no `(month+2)//3`, no calendar arithmetic anywhere; P&G's fiscal Q4 (period end June 30) is quarter 4. Revert the fixture edit at `tests/earnings_economic_fixtures.py:176` so the fixture's `FISCAL_PERIOD` (quarter=4) is the single truth, and mint the event id from the fiscal quarter. The combined-only fixture must carry NO split volume/mix column anywhere; the two absences' subject and detail name the combined line.
- **R10 (R6 fact_id/registry/document_id — F12/F13/F11).** `fact_id` = a deterministic function of (event_id, metric, period, basis) — e.g. `fact_{sha256(event_id|metric|period|basis)[:16]}`; two events never share a fact id, a duplicate within one event is refused. ONE rights registry: `pg_profile.py` imports the private token from the registry entry (no second literal at `pg_profile.py:26`); the registry is the one read by the validator. Absence and span `document_id` bind to the workspace document, not the caller's `source_texts`.
- **R11 (unit vocabulary — the `missing_units` deviation is NOT ratified).** Add `unit_mismatch` to the closed absence-reason set (`engine/company_intelligence/documents.py:72`, one line) and use it for a present-but-wrong-shape literal; refuse unbalanced parentheses (`'(1.25%'`, `'$1.25)'`) as `unit_mismatch` (F8).
- **R12 (fixture values — F15).** Replace `$1.37` and every other EPS literal in the fixture with the synthetic set the probes use (3.07 / 3.11 current diluted / core, 2.93 / 2.97 prior); no value in the fixture may be a figure P&G has reported. Update the existing tests that assert 1.37.
- **R13 (F7 minor).** Volume figures read from a second table are cross-checked against the Sales Drivers table when both are present; a disagreement is a typed absence, never a silent pick.

## Frozen files
`tests/test_pg_economic_observations_probes.py` (seat-committed, RED at bc33493b) — read-only for the lane. The job `earnings-economic-dossier` runs both suites; contract-delta must stay at 0 introduced.

## Verification the seat will apply
Probe suite green in the clean venv (`pip install pytest pyyaml`), original suite green with the updated assertions, `grep -nE 'Fourth Quarter|Prior (Diluted|Core)|Volume Conventions|Core Reconciliation|current_year' engine/company_intelligence/pg_profile.py` empty, then a final Opus round bounded to R7–R13.

## Addendum (seat, after the probe suite was frozen)

- **R14 (vocabulary the probes fix).** The literal bodies in `tests/test_pg_economic_observations_probes.py` define the P&G release conventions the extractor MUST accept as definition-owned vocabulary: rows "Diluted Net Earnings per Common Share" and "Core EPS"; period columns in calendar-year / "Three Months Ended" / "{Ordinal} Quarter Fiscal Year {fy}" forms; the "Net Sales Change Drivers" table with columns "Volume with Acquisitions & Divestitures", "Volume Excluding Acquisitions & Divestitures", "Foreign Exchange", "Price", "Mix", "Other", "Net Sales Growth" and a "Total P&G" row; the combined "Volume/Mix" line. These come from P&G's public release format, not from the fixture, and may be encoded in the metric definitions.
- **R15 (P8 subject rule).** An absence's `subject` may be the metric key plus a combined-presentation descriptor (it must contain both "volume" and "mix" for the combined line); the validator's `subject == metric` check is relaxed to `subject.startswith(metric)` or an explicit combined-subject form.
- **R16 (probe file is frozen).** `tests/test_pg_economic_observations_probes.py` is wired into the `earnings-economic-dossier` job (`paths:` and `run:`) and is read-only for every lane; P9 may be refused by the fact-id rule rather than the dash rule after the repair — either refusal is acceptable.
