# Opus adversarial review — T7 Dossier Design Spec (PR #7904 @ 30091a2679ff)

MODE: READ_ONLY. Artifact: origin/claude/cdv1-t7-design-spec:research/consumer_defensive/cdv1_program/design/T7_DOSSIER_DESIGN_SPEC_2026-09-24.md
Plan: 88970a1a:docs/superpowers/plans/2026-09-23-consumer-defensive-cdv1-implementation.md (Task 7 at plan:459-511; §2.4 :139; §2.6 :174)

(dimensions appended incrementally below)

## (4) Mount truth — FAIL (BLOCKER x2)

F4.1 BLOCKER — Theme Tracker mount is fictional and cannot become real lawfully.
- Spec §1.1 (line 15): "The Staples row is runtime-selected by `data-theme-id="us_sector_staples"` ... it is not one of the eighteen currently rendered foresight themes, so the build must add that row only through the existing theme-state owner".
- Truth: `templates/state_of_themes.html.j2:460` emits `data-theme-id="{{ th.theme_id }}"`; rows come from `scripts/build_state_of_themes.py:739` (`theme_state.json`) → loop `:833`; that file is emitted by `engine/neuralweb/thematic_state.py:473` `theme_cfgs = xwalk_data.get("themes", [])`. `origin/main:site/neuralwebdata/theme_state.json` has 18 ids, none staples.
- `config/theme_crosswalk.yml:509` lists `us_sector_staples` under `unmapped_baskets:` (section at :392) with reason "Equal-weight sector basket (GICS Consumer Staples). Not a theme." So §8 Q1's "the stable theme vocabulary/crosswalk does [have it]" is false-by-omission: the crosswalk explicitly EXCLUDES it. Admitting it would put a sector basket onto the foresight board with lane/stance/falsifier/scoring — contradicting plan Task 7.3 "must not add duplicate scoring or expand the main dashboard height".
- Correction: delete the Theme Tracker mount from this spec (XLP-only for T7), or re-route the "compact entry" to an existing surface that already carries Staples (e.g. the US sector rotation row, `engine/us_sector_rotation.py:766` "b-us_sector_staples", or `scripts/build_site.py:1899` basket page) — seat must name it; never admit a sector basket into theme_state.

F4.2 BLOCKER — Theme Tracker click handler collapses the drawer on every dossier click.
- Spec §1.1 (line 26): "The button is a real `<button>` so drawer expansion can ignore clicks originating on interactive descendants exactly as the current handler already ignores links (`state_of_themes.html.j2:688–695`)."
- Truth `state_of_themes.html.j2:689-694`: `if(e.target.closest('a'))return; card.classList.toggle('expanded');` — only `<a>` is ignored. A click on the toggle, any evidence button, the `<summary>`, or plain text inside the dossier toggles `.expanded` off and hides the drawer (`.theme-row.expanded .drawer{display:block}` :182). And the spec forbids changing it: "No other drawer cell ... may be changed."
- Correction (if a TT mount survives): spec must pin the handler edit, e.g. `if(e.target.closest('a,button,summary,[data-economic-root]'))return;`, and list it as a permitted change.

F4.3 MAJOR — TT root is an empty `<section>` while §3 says the partial is the only skeleton and IDs are literal.
- §1.1 line 21 `<section id="economic-dossier-staples" data-economic-root hidden></section>` vs §3 line 129 "use these IDs literally" (`id="economic-dossier"`) and the JS contract binding `[data-economic-state-chip]`, `[data-economic-demand]` etc. inside the partial. Builder must choose: include the partial, or JS-build the skeleton (latter = markup in JS).
- Correction: TT host (if kept) must `{% include %}` the same partial; state one ID rule.

F4.4 PASS (with gaps) — XLP slot anchor is real and expressible.
- `templates/sector.html.j2:300-309` = Chart panel; `:311` `{% if s.accumulation %}`. Context `s` built at `scripts/build_site.py:4247` `s = {"fund": fund, ...}`, rendered at `:4142`/`:4279` `tpl.render(s=s, ...)` over `config.load()["sponsors"]["sector_funds"]`. So `{% if s.fund == 'XLP' %}` is expressible.
- Gap (MAJOR, see (5)/(8)): sector page loads only `../theme.css` (:14), `../mtf.js`, `../theme.js` (:505-506). The spec never pins the `<link>` for earnings-wire.css or `<script>` for earnings-economic.js, their relative paths (earnings-wire assets are copied only to the earnings wire's own `assets/` dir, `scripts/build_earnings_public_wire.py:79-80,969-976`), nor the host init script that calls mount. Also the `.panel` class on the root: sector.html.j2:90 `.panel{...border-radius:10px...}` conflicts with spec's `.econ-dossier{border-radius:var(--r-panel,14px);padding;margin}` — two competing panel definitions on one element (cascade order unspecified). Spec §4.3 line 410 also says "the dossier itself adds no second enclosing box" while §4.2 gives it its own border/shadow/radius — contradiction.

## (3) Tokens — PARTIAL

- Receipt §4.7 lines 444-484 verified line-for-line against `origin/main:templates/theme.css` (3392 lines): every cited line number matches (e.g. :56 `--fs-h2:17px`, :139 `--glass-bg`, :372-377 ink rungs, :210 light surfaces). PASS.
- Fallback idiom honest: `theme.css` block ending ~:2060 states "DS-PR-0 FORWARD-COMPAT ... each such reference carries the specimen's own value as a var() fallback"; `.mx-empty` :2040 uses `var(--sp-6,24px)`, `.skel` :2059 `var(--r-ctl,8px)`. Specimen `mockups/design_system/specimen.html:22-26` values match spec lines 491-495. Absent from theme.css :root: all `--sp-*`, `--r-*`, `--t-*`, `--ease-*`, `--gap-grid` (grep of `--sp-N:` etc. definitions = 0). `var(--t-fast` and `var(--r-panel` have ZERO precedent uses in theme.css — the "already established" claim covers sp/r-ctl/r-btn/r-pill/ease-std/gap-grid, not t-fast/r-panel.
- MINOR: §4.2 line 352 uses `var(--link)` (outline) which is not in §4.1's allowed vocabulary (only `var(--ink-link, var(--link))`). `--link` exists (:78, :223) but the spec violates its own closed list.
- MINOR: fixed-geometry list omits `24px` (min-height:24px at :325/:345), `72%` (:331, :370), `.08em`, `72ch`, line-heights — builder lint against §4.1 would fail its own CSS.
- MAJOR: `.econ-drawer-scrim{background:color-mix(in srgb,var(--bg) 55%,transparent)}` (:369) mints a derived percentage, contradicting §4.4 line 422 "the component does not derive new percentages" and §4.4 line 421 "no dark scrim" in light — CSS gives light the same scrim; no light selector exists. Correction: use an existing overlay token or pin a `[data-theme="light"]` rule.
- Precondition (§4.7 line 498 "build lane must stop PARTIAL/BLOCKED if neither is true") is honest but makes T7 unbuildable as written until the seat ratifies; the seat should ratify the fallback idiom in the spec itself (it already ships in theme.css) and drop the gate.

## (1) Content law — PARTIAL

Passes: organic≠household consumption (§5.7 line 613 / §3 line 165); volume/mix≠pure volume (line 557 "Volume and mix combined", line 615); reported vs core separate rows + definition line (lines 87, 614); no "validated"/falsifier/buy/sell in any UI string (§5.9); stance "Watch the evidence — do not chase it." maps to DESIGN_DOCTRINE Law 1 vocabulary "Watch — don't chase" (doctrine :43). All ZH strings are real Chinese, no placeholders (lines 508-607).

F1.1 MAJOR — finding wording asserts a direction the rule does not carry.
- Spec line 592: `reported_vs_organic_difference | Reported sales rose, but the company-defined organic measure did not.`
- Plan §Task 3 (plan:301 region) "fixed rule IDs for headline/organic **difference**"; the rule fires on any difference. For P&G the common case is the reverse (FX headwind: organic up, reported flat/down) → the fixed sentence would be FALSE on the flagship issuer. Same risk for line 594 (fine, neutral) but line 592 is not.
- Correction: neutral wording "Reported and company-defined organic sales moved differently; currency, acquisitions or divestitures explain the gap." / "报告销售额与公司定义的有机销售额走势不同，差异来自汇率、并购或剥离。" — or require the server to emit a direction field and pin two sentences.

F1.2 MAJOR — glance stance has no source field.
- Spec line 69: "Stance copy is server-owned and selected by interpretation state"; only one stance sentence exists (line 512). Plan §2.4 closed key set (plan:143-146) has no stance/state key; spec §6.1 line 641 rejects any other top-level key. So the browser must either always print the default or infer one (forbidden). Correction: pin "stance = fixed default for every renderable state; state-panel lines for the rest" OR add a stance field to the Task-3/6 contract (plan change, seat ruling).

F1.3 MINOR — `missing_consensus` sentence overclaims. Line 597 "Consensus is unavailable, so this is not a beat or a miss." / "…因此这不是优于或低于预期。" states a verdict; truth is "cannot be judged". Correction: "Consensus is unavailable, so this cannot be read as a beat or a miss." / "缺少一致预期数据，无法判断是否超出或低于预期。" Also "共识数据" is unidiomatic; market term is "一致预期".

F1.4 MINOR — ZH defects: line 614 "报告口径与核心每股收益采用不同定义" is asymmetric (口径 vs 每股收益) → "报告每股收益与核心每股收益采用不同定义。"; line 575 "已更新" ("has been updated") ≠ "up to date" → "已是最新"; line 606 "利润率到现金的桥接" is calque → "利润率与现金流之间的衔接信息不可用。"

F1.5 MINOR — internal vocabulary on reader-facing tiers: footer "Accepted generation: {pin}" (line 626) and state chips "Currentness unverified"/"Newer source pending" (576-577) are pipeline words (doctrine Law 2 "Internal vocabulary"). Chip is Tier 1. Correction: chip EN "Latest filing not yet read"/"最新文件尚未读取" and "Freshness not confirmed"/"时效未确认"; move generation pin to the evidence dialog only (it is already there, line 255).

## (2) TP-0 two art directions — FAIL (BLOCKER)

- §4.2 (lines 318-404) contains ZERO theme-scoped selectors (`grep '\[data-theme' ` over the CSS block = 0). Every "difference" claimed in §4.3/§4.4 is the tokens re-keying: e.g. line 418 "white `--panel` on cool `--bg` ... existing light `--card-shadow`", line 420 "light's `--panel2` hover tint is present but quieter", line 422 "paints deliberately use the measured light ink twins already defined by theme.css". CLAUDE.md TP-0: "Token substitution alone is never proof of a light design — 'the same CSS still renders once the tokens swap' is precisely the failure this law exists to stop." The spec's own §4.1 line 314 allows theme-specific selectors, then writes none.
- Claimed-but-unimplemented light mechanism: §4.4 line 421 "no dark scrim" — CSS line 369 gives light the same `color-mix(var(--bg) 55%)` scrim; there is no light override. Line 421 is therefore false against the spec's own code.
- DS doc §12 (`research/MASTER_PRODUCT_DESIGN_SYSTEM_V1.md:587-612`) light contract items the spec does not implement: glass "brighter fill, airy shadow, crisp top sheen" (no sheen rule; `--gbtn-sheen` is allowed but unused); "raised/nested: tint step + hairline; shadow only if interactive" (evidence/close buttons get no light ring/tight shadow — dark and light identical); "Glow → ring, not glow".
- Degraded states are theme-agnostic (§4.5 lines 425-432); TP-0 requires theme-specific degraded states (e.g. light unavailable/unentitled dashed box on white needs a structure step, loading skeleton contrast on `#eef1f6`).
- Semantic color misuse (both themes, MAJOR): line 348 `tr[data-economic-basis="core"] .econ-basis{color:var(--ink-warn)}` paints the Core EPS definition chip in the WARNING hue — reads as "core is suspect" and contradicts line 87 "neither is labeled better, cleaner" and the state chip where `--ink-warn` means pending/error (line 327). Line 347 paints Organic in `--ink-link` — link hue on a non-link = affordance lie. Correction: all three basis chips neutral (`--muted` text, hairline pill), distinguished by the word (B13 grayscale test already demands text distinction); reserve `--ink-warn` for state only.
- Correction (repair): add an explicit `html[data-theme="light"] .econ-…` block pinning at least: scrim (none or `transparent`), drawer top sheen (`box-shadow: inset 0 1px 0 var(--gbtn-sheen), var(--glass-shadow)`), evidence/close button ring-on-hover (`border-color:var(--gbtn-brd-hover)` + `--popover-shadow` tight), dossier canvas step; and a dark block pinning its luminance mechanism; plus per-theme degraded-state notes. Until then: PARTIAL/BLOCKED per TP-0.

## (5) Contract fit — PARTIAL

Matches plan: mount signature (spec :47 = plan:463); six rule IDs (spec :97-102 = plan Task 3 "fixed rule IDs"); 14 top-level keys (spec :641 = plan:143-146); six authority booleans all-false; selectors data-economic-root/period/basis/rule/evidence/state (plan:469); textContent-only, no arithmetic, no storage/telemetry (§6.1, §6.4); AbortController + epoch (§6.2 = plan:489-499).

F5.1 MAJOR — closed-key check applied to the wrong object. Spec :641 "Reject any response whose top-level keys are not exactly `schema interpretation_id issuer …`". The API response is a WRAPPER: plan Task 6.1 test reads `shown['slug']`, `shown['generation_id']`, `shown['manifest_sha256']`, `shown['record_sha256']`, `shown['interpretation']['observations']` (plan:417-428). As written the component rejects every valid response. Correction: pin the response envelope keys (`slug generation_id manifest_sha256 record_sha256 interpretation` + whatever Task 6 freezes) and apply the 14-key check to `response.interpretation`.
F5.2 MAJOR — evidence flow self-contradicts. §6.3 :669 "An evidence click does no financial fetch. It opens the existing dialog with the evidence object referenced by that displayed observation" vs :671 "A subsequent evidence request uses the pinned values … to `/api/earnings/v1/records/{slug}/economic-evidence/{fact_id}`". Plan §2.6 (plan:178-180) makes the evidence route the source of span/header context. Correction: "click → open dialog in loading state → one abortable fetch of the evidence route with query params `generation`, `manifest_sha256`, `record_sha256` (plan:424-426) → render". Param names are currently unpinned.
F5.3 MAJOR — no field map. Nowhere does the spec say which JSON field feeds SERVER_LABEL / SERVER_PERIOD / SERVER_VALUE_AND_UNIT / basis / fact_id / clock values / finding sentence / evidence span, header, digest, precision. Builder must invent the projection. Correction: a table `DOM slot ← JSON path` for every placeholder, or declare it blocked on Task 6's frozen view schema.
F5.4 MAJOR — host adapters unspecified. Neither host loads a session client beyond theme.js (`sector.html.j2:505-506`, `state_of_themes.html.j2:651`); `window.MDXAuth` lives in `templates/theme.js:1905` and emits `mdx-auth` (theme.js:1883) — including `PREFS_SAVED` (theme.js:3212), which a naive epoch adapter would treat as an auth change and wipe the view. Prior art: `earnings-wire.js:58-70` (token), `government-revenue-dossiers.js:533-535` (`MDXAuth.onChange` else `mdx-auth`). Spec :54 cites only a test (`tests/test_earnings_api.py:258-269`) that pins string literals in earnings-wire.js. Correction: pin the exact host init script (file, placement, `fetchAuthenticated` body, `onAuthChange` that bumps epoch only on sign-in/sign-out user-id change), and where `nativeIssuerId` comes from (spec :53 `"<owner-bound value>"` — not in `s` context, `build_site.py:4247`; and it is never used by the ticker route).
F5.5 MINOR — `data-economic-mounted=""` in the partial (:136) vs "mark with `data-economic-mounted="1"`" and "removes `data-economic-mounted`" (:52, :59); a `hasAttribute` guard would treat the fresh skeleton as mounted.

## (6) Accessibility and focus — PARTIAL

OK: role=dialog + aria-modal, initial focus close, Escape/scrim close, focus return, real tables with th scope, 40px targets (§3 :292; B01/B02). Plan 7.1 test (plan:471-484) is satisfiable on XLP once F5.* are fixed.
F6.1 MAJOR — every evidence button gets the same accessible name (`aria-label="OPEN_EVIDENCE_FIXED_COPY"` :279 = "Open source evidence" ×24); screen-reader users cannot tell rows apart. Correction: aria-labelledby the row header + a hidden fixed span, or aria-label = fixed copy + " — " + row label via textContent composition.
F6.2 MINOR — aria-label is monolingual; no rule for EN/ZH switching on `langchange` (same for `<nav aria-label="Company and research links">` :228, EN-only). Correction: use aria-labelledby pointing at l-en/l-zh spans, or specify re-set on langchange.
F6.3 MINOR — §2.2 :83 says "One icon-only accessible button" but markup :280 has visible text "Evidence/证据". Pick one (text is better).
F6.4 MINOR — unentitled state promises an "owner-provided sign-in action" (:430) but the partial has no button slot; §8 Q2 still open.
F6.5 MINOR — heading order "page h1 → panel h2 → group h3" (:292) is false on Theme Tracker (drawer sections use h4; `state_of_themes.html.j2` template has no h1). Moot if TT mount is dropped.

## (7) Density — FAIL

F7.1 MAJOR — XLP is always expanded. §1.3 :58 "XLP page: mount once after DOM ready" → full two tables (up to 24 rows) + findings + missing + clock + links, on every XLP visit, including anonymous readers (unentitled panel). Plan Task 7.3 (plan:501): "No new hero or always-expanded full-sector report." §1.2's "no height change while hidden" (:40) is only true before mount. Correction: XLP gets the same collapsed disclosure (glance row: chip + stance + "Open …" toggle, 40px) and mounts/fetches on first expand.
F7.2 MAJOR — 390px budget unimplementable. §4.6 :436 "visible content budget is exactly: … three Demand rows; … one Earnings row … then inner rows scroll" — the CSS has no max-height/overflow-y anywhere (`.econ-table-scroll{overflow-x:auto}` :338, :397). Builder must invent a height. Correction: pin e.g. `.econ-table-scroll{max-height:…;overflow-y:auto}` per group, or drop the "inner rows scroll" promise.
F7.3 PASS — no header/nav added; TT collapsed cell is one 40px row inside an already-expanded drawer (not the main dashboard).

## (8) Builder-readiness — FAIL (choices a builder must still make)
1. Where the Staples compact entry lives (F4.1). 2. TT click-handler change (F4.2). 3. TT root: partial vs empty section, IDs (F4.3). 4. `<link>`/`<script>` tags, relative paths, and asset registration/copy target for earnings-wire.css + earnings-economic.js on `site/sectors/XLP.html` and themes page (not pinned; wire assets only copy to the wire's `assets/`, `build_earnings_public_wire.py:969-976`; `check_template_site_sync` pairing unstated). 5. Host init script + fetchAuthenticated/onAuthChange bodies (F5.4). 6. `nativeIssuerId` source (F5.4). 7. Response envelope + JSON→DOM field map (F5.1, F5.3). 8. Evidence route query-param names and fetch timing (F5.2). 9. Stance source (F1.2). 10. Direction of reported/organic finding (F1.1). 11. `.panel` (sector.html.j2:90, radius 10px) vs `.econ-dossier` (radius 14px, own padding/shadow) cascade, and §4.3 :410 "adds no second enclosing box" vs §4.2 :319 own border/shadow. 12. Light-theme overrides (F2). 13. 390px inner vertical height (F7.2). 14. aria-label language + per-row naming (F6.1-2). 15. Unentitled sign-in button markup/route (F6.4, §8 Q2). 16. Clock footer inner markup (spans/separators) and links-unresolved line placement. 17. Loading "keep panel height stable" with no height value. 18. Language re-render on `langchange` for dynamic textContent (:644 picks one language at render; toggling language afterwards is undefined). 19. `var(--link)` and 24px/72%/72ch outside the declared closed vocabulary (F3). 20. §8 open questions 1-7 all unresolved yet §1 is labeled "fixed; no builder judgment".

## (9) Plan contradictions / #7777 — PARTIAL
- #7777 (OPEN, "feat(sector-intelligence): add governed dossier contract") files: `.github/ci/legacy-jobs.yml`, `contracts/sector_intelligence/…`, `engine/sector_intelligence/contracts.py`, `tests/test_sector_dossier_contract.py` — no path overlap with the spec. PASS on paths.
- F9.1 MAJOR — spec §1 "Status: fixed; no builder judgment" asserts the accepted hook, but plan Task 7.1 (plan:467): "At the inspection pin this accepted hook is not yet proven. Fable must reconcile the current shared owner and add only the narrow hook on its lawful carrier." Plan says the root "is the existing shared panel slot" in "the accepted shared sector shell" (plan:7, 463). Hardcoding `{% if s.fund=='XLP' %}` into legacy sector.html.j2 is a seat ruling the spec presents as fact; it needs an explicit seat adjudication vs the STSI sector-dossier shell (#7777 lineage).
- F9.2 — "always-expanded full-sector report" (F7.1) and "compact entry" (plan:501) vs TT mount on a non-theme (F4.1).

## VERDICT: REJECT (repairable — re-review after the minimal list; not a redesign)

Minimal repair list (build-blocking order):
1. Drop the Theme Tracker mount (us_sector_staples is `unmapped_baskets` "Not a theme", config/theme_crosswalk.yml:509; emitter engine/neuralweb/thematic_state.py:473 reads only `themes`) — or seat names a real Staples-bearing surface. If any TT-style host survives, pin the click-handler guard (state_of_themes.html.j2:691) and include the same partial.
2. Write a real LIGHT art direction: theme-scoped CSS blocks (scrim, glass sheen, button ring, canvas step) + per-theme degraded states; neutralize basis-chip colors (no `--ink-warn` on Core, no `--ink-link` on Organic).
3. Pin the response envelope and apply the 14-key check to `interpretation`; add a JSON-path→DOM-slot table; pin evidence route params (`generation`, `manifest_sha256`, `record_sha256`) and click→fetch timing.
4. Pin host wiring: script/link tags + paths + asset registration; host init; `fetchAuthenticated` over `MDXAuth.client()`; `onAuthChange` bumping on user-identity change only (ignore PREFS_SAVED); drop or source `nativeIssuerId`.
5. XLP collapsed-by-default disclosure, mount on first expand; pin 390px inner max-height or delete the promise.
6. Copy: direction-neutral `reported_vs_organic_difference`; stance source rule; "cannot be read as a beat or a miss"/"一致预期"; ZH fixes (报告每股收益…, 已是最新); plain chip words for pending/unverified.
7. Per-row accessible names + bilingual aria labels; unentitled action markup.
8. Resolve §8 Q1-Q7 or mark §1 "PROPOSED — seat ruling" instead of "fixed"; ratify the fallback-token idiom in-spec; add `var(--link)`, 24px, 72%, 72ch to the closed vocabulary.
