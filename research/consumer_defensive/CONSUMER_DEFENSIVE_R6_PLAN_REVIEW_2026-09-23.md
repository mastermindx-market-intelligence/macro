# Consumer Defensive R6 — implementation-plan review

Commission date 2026-09-23. Principal self-review and persistence evidence, not independent technical review, product execution or investment validation.

Operation `gmi-consumer-defensive-research-20260923-sol-001`; Macro #7792, branch `sol/consumer-defensive-research-20260923`; parent `WS:GMI-THEME-GRAPH`. MISSION_COMPLETE: false. Research carrier remains Draft/HOLD.

## 1. Exact plan and authority

The Chairman's live response to the R5 written design was "good job, continue next turn". This continues the presented design and six clarifications into implementation planning. The current protected ACTIVE_EXECUTION also rejects duplicate administrative approval rounds for already commissioned routine work. This does not certify independent security/privacy acceptance or release gates.

Plan: `docs/superpowers/plans/2026-09-23-consumer-defensive-cdv1-implementation.md`.
Content commit: `a30ed45c6d991c4a4e412bd9114de63f1656e181`.
Git blob: `c7cfb3e0271bf4016276daa42e4203e8b1a0bff5`.
UTF-8 bytes: 70,156. Lines: 622. Words by whitespace split: 8,647.
SHA-256: `8c91c8f3a187d500b388de49a6ce637af0f3095ee19c26e9b03004acc7c5d2b6`.

Native GitHub exact-commit readback returned the same blob as the locally calculated Git object hash over all 70,156 bytes. This verifies the portable plan and committed plan are byte-identical, not merely similar in their headers.

Governing design: `docs/superpowers/specs/2026-09-23-consumer-defensive-economic-dossier-design.md` at `de3086fff1a883f2375ced3c9e8c713e8566f29b`; clarifications at `bc9c2426c693f478c007c517d44e69b88d4b1b4f`. Full program destination: `CONSUMER_DEFENSIVE_MASTERPLAN_R5_2026-09-23.md` at `5fa43c0585f979263a2a7e28e4b468077c14aa55`.

Protected procedure: `Mastermind@a7d2b3049e5cdc523e91e61a6e9d70a1cb911157`, Skillpack 1.0.1/bootstrap1. Current Macro implementation inspection: `dd4d965de7d52f4c68f6dbc984c7a05a70e0d62b`. The research branch was not rebased. Intermediate planning checkpoint `a8e86da1d75b3b480f6fc910437d697424ac3396` was committed/read back before the large plan write.

## 2. Material implementation findings

The plan is not another sector census. It resolves how the first economic explanation can join the actual native source and private publication paths.

1. The existing acquisition loop can continue past a failed newer results exhibit and select an older accessible one. The new private consumer needs the candidate outcome trace to distinguish current evidence from a fallback. Preserve legacy behavior by making this additive and opt-in; do not create another collector.
2. The private publisher constructs complete generations from a closed staging tree. A one-off additional object is not durable product integration. Normal subsequent runs must preserve the new native roles, reject accidental v2-to-v1 downgrade, and maintain valid legacy record/context catalogs.
3. Received HTTP bytes, decoded UTF-8 text used by native spans, metadata JSON and derived interpretation are distinct objects with distinct digests. A new source role must not pretend a JSON hash proves exact original source bytes.
4. An evidence drawer needs the generation, manifest digest, record digest and native fact selector of the displayed claim. A later current-pointer change must not make the drawer show a different source vintage.
5. Existing public workspace publication is not an acceptable transport for newly protected facts. The source/profile dispatch and actual public uploader both need the admission guard; a removable caller boolean is not sufficient.
6. Existing process locking is not proof of distributed single-writer safety. Preserve the current workflow/custody fence, and keep ambiguous pointer writes uncertain rather than blindly restoring an older pointer over a possible new writer.
7. The shared STSI #7777 candidate remains an open/unmerged five-file contract slice at `4122e3b7e1524482216fb156f201f8229c14011b`. It does not establish the accepted shared mount or authorize a parallel Consumer Defensive shell.

The exact native code paths and blobs are in the plan's source register. These are bounded source inspections, not a global runtime outage, privacy exploit or all-estate audit.

## 3. Self-review corrections made before commit

The first local draft's PG extraction signature omitted `fiscal_period`, while the actual `active_profile.extract_release_facts` call supplies it. The final plan uses the compatible signature.

The interpretation builder and its trusted validator now explicitly receive admitted fiscal scope. The four-date comparison scope cannot be reconstructed solely from one end date or overridden by a browser.

Task 4 now declares the exact readers required by Task 6, keeping private-publication edits on one writer grant. The evidence route carries both manifest and record digests rather than relying only on the moving current pointer or a loose generation label.

Six all-false derived authority keys are explicit. Native workspace context-only authority remains unchanged. Interpretation/profile revisions must come from actual code/semantic inputs, and unsupported historical rule versions cannot silently run current logic.

The four-workspace limit is a bounded active record/read constraint, not permission to delete historical generations. A native-only PG record does not invent a story-packet source, but it still participates in the real incumbent publication rather than creating a second pointer if legacy wire inputs are missing.

These are corrections and clarifications within the accepted design, not a material expansion into a new financial system or implementation of product behavior.

## 4. Executed verification

A fresh container Python run passed 32 authored-plan structural and syntax checks. The checks verified:

- exactly eight ordered unique tasks, each with files, interfaces and ordered checklists;
- all CDV1-01 through CDV1-36 mapped once, with valid task owners;
- five review-focus cases linked to tasks;
- immutable design, clarification, source and procedure refs present;
- no TODO/TBD/FIXME or filler placeholders;
- balanced fenced blocks and syntax parsing of all twelve Python snippets;
- RED-before-GREEN and test examples for Tasks 1-7;
- the corrected fiscal-period and fiscal-scope interfaces;
- Task 4's definitions for Task 6's exact evidence readers;
- manifest and record digest binding in the source-drawer test;
- twenty unique proposed selected metric keys and finite bounds;
- six explicit authority keys, no new current pointer, and downgrade/next-run tests;
- source fallback/currentness, shared mount, first-source and Fable-state limits;
- all seven proposed new test files present in the qualification commands;
- twelve viewport/language/theme combinations and explicit incomplete mission status.

The exact local plan blob then matched the native GitHub blob. A local QA JSON was written and read back successfully:
`/mnt/data/consumer_defensive_r6/CONSUMER_DEFENSIVE_R6_PLAN_QA.json`.
QA bytes: 4,085. QA SHA-256: `b393919ac533685ee882440c104fa0ce79cbc181d71a8fb456f6386a3f3268d2`.

These checks validate the authored plan's structure and snippet syntax. They do not execute the planned product functions, authenticate source inputs, validate live retention, certify data entitlements, test API/browser behavior, or establish investment performance. The test snippets are implementation instructions; their fixture helpers and product modules are planned work. All 36 product acceptance cases remain NOT_EXECUTED. Product tests run: zero. Independent review: not performed.

## 5. Four remaining action-local gates

G1 is acceptance of native PG metric/scope definitions and private admission. G2 is actual issuer/source/retention proof. G3 is compatible private-record/manifest evolution and verified readers before enabling v2 data. G4 is the accepted shared dossier mount.

These gate the actions depending on them, not all isolated coding/testing. Live writer/effect uncertainty remains a separate fence. The plan keeps a safe sequence: compatible readers first; qualified private producer next; real source/user path proof; then normal follow-on publication proof. No release permission is inferred from plan completeness.

## 6. Fable readiness and delivery boundary

The implementation plan is ready to accompany the existing research/design packet for the Chairman-selected Fable integration route. Fable's principal value is privacy/version/custody integration and end-to-end acceptance; routine extraction, UI and test implementation should use the existing eligible lower-scarcity fabric.

A directory discovery for Executive OS and Mastermind/Executive/Fabric returned no matching actionable connector in this conversation. That is an observed discovery limit, not proof that the company's runtime or capacity fabric is offline. No concrete Fable receiver, live capacity selection, runtime submission or delivery was verified. Do not substitute a broad Slack broadcast, guessed provider account or requested Chairman quota allocation.

The prepared integration packet may be preserved under Agent OS with WAITING_CAPACITY/needs_placement and NOT_DELIVERED. A concrete current approved placement/direct-delivery edge, pickup and separate START are still owed. No receiver-specific watcher is armed before a receiver exists. Retrieval of a packet alone is never assignment.

## 7. Effect and continuation

Only authored research, planning and organizational continuity documents changed. No product source, schema enrollment, source admission, live private payload, identity, basket, ranking, entry, sizing, trade, authentication, publisher configuration, CI policy, merge or deployment changed. No worker, Executive Attempt, Fable ACK/START or watcher created. EFFECT_UNKNOWN: none observed.

Keep #7792 Draft/HOLD. Do not create a redundant design-review ceremony; use the accepted scope and current owner gates. Next action is to carry this exact plan and preserved R1-R5 research into the bounded Fable integration packet, then complete concrete lawful placement and current source-custody reconciliation before product execution. The six-milestone Consumer Defensive parent mission is still incomplete.
