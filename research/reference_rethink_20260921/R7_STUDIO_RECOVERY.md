# R7 — Studio recovery and verified publication

Mission remains incomplete. Production has not changed. Carrier remains macro PR #7647 / `claude/reference-rethink-20260921`; no replacement branch or worker was created.

## Recovered effect

The original Mac-Studio worktree is reachable again. Its actual existing heading-repair commit is `d18ddc1e9623952374af670b36b6069298aac4a0`, parent `94b233d9db369d7ef693b620e0aad7624129f5e0`. The previous PR checkpoint incorrectly named `c3b2a849f929e15043f84da2ddd89eb5efb5e4d2`; original-carrier `git log` and the exact +20/-1 two-file patch establish the correct identity. The fix was not recreated, reset or rebased.

The exact prior artifact-download destination `r6-evidence/hosted-plan/` was absent, with no matching download process. A new same-carrier download request, made only after that reconciliation, was refused by the tool safety-status check; no alternate tool/host was used to retry it.

## Fresh targeted browser proof

`r7-evidence/heading-browser.json` records Chromium 151.0.7922.34 against the existing real 46-entry registry and its rendered owner validators. Four cases passed: English/Chinese × 1440/390. Each checked question selection → global alias search → correct heading and sole `market-regime` result, preserved focused input, matching query URL with no stale topic parameter, explanation opening, browser-back restoration, and no horizontal page overflow. No page errors were observed. Four fresh screenshots and the exact source-backed review HTML are included.

The view is bound by SHA-256 `13b63fe47d63a3636d4e4b9b8b2a84906a3610859a2ce3fff5fa12f10cf52492`. This is targeted proof of the heading repair, not a fresh full 184-case qualification, a new Python-suite run, Macro integration, or independent visual acceptance.

The existing commit was pushed once to the original branch. `git ls-remote` readback matched `d18ddc1e9623952374af670b36b6069298aac4a0`.

## Remaining boundaries

A fresh original-device local Macro diagnostic was explicitly refused by the tool safety-status check. The previous navigation discrepancy remains unresolved; Studio recovery does not clear that separate boundary. Native GitHub readback found no independent review return on #7647. Reference PR #6792 remains open/draft at `7853ffeaca41d4d8f33995b1453db2be134ce8a3`; its implementation/evidence must not be overwritten or represented as accepted. Production migration also still owes the independent Reference Integrity/design acceptance required by the existing migration factory; the Paper candidate remains provisional.

Next: consume independent design/reference review and reconcile the incumbent reference-path authority; resume the exact Macro comparison only after its tool safety boundary clears; then integrate the accepted page/help through the ordinary current-base CI and release path. Neither fresh mocks nor a second registry is needed. Keep Draft/Hold, no auto-merge, no deployment, and no autonomous execution claim.
