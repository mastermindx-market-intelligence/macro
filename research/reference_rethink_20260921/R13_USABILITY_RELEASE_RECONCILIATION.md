# R13 — Usability hardening and release reconciliation

**Mission remains incomplete. Production remains unchanged.** This wave retains only the user-ready source changes after a real-source visual inspection rejected a natural-language search experiment that spilled unrelated glossary rows beneath the intended answer.

## Capability delta retained at source subject `4ca095c5eef1d790f42c5ed60445425c96d60281`

1. **The page identifies itself immediately.** The working guide now opens with **Market Guide / 市场指南**, a plain promise — “Ask what a score or signal means. Get the answer in seconds.” — and “Start with a question.” A cold user no longer has to infer that “Understand the signal” is a glossary/help product.
2. **Contextual help can show the caller’s current reading.** A dashboard can supply bilingual current-reading text, an as-of label, and an allowed interpretation id. The help dialog renders that reading above the definition instead of making the user mentally reconnect a detached definition to the number they were looking at.
3. **The current reading and interpretation stay coherent.** Caller-supplied interpretation ids are accepted only if the referenced indicator actually exposes that interpretation. Unknown ids are ignored; they cannot manufacture a guide state. The current-reading text is inserted with `textContent`, so editorial/live strings remain inert.
4. **Context mode remains non-invasive.** The previously accepted R8 fix still leaves caller-owned host class/content, page title, URL and language state untouched through mount/open/dispose.

The earlier `da8119fd396b` natural-language search experiment was **reverted on the same carrier** in `4ca095c5eef1`. Real-source inspection of “what does risk 56 mean” showed the correct two answers first but also five unrelated keyword matches. The attempted suppression repair was explicitly refused before tool dispatch. Rather than ship a noisy answer experience or probe around the refusal, R13 removes the experiment from the candidate. The net diff against the published R8 head contains no natural-language-search feature.

## Fresh exact-head proof

At source subject `4ca095c5eef1`:

- `node --test tests/test_market_guide_client.cjs tests/test_market_guide_view.cjs` — **60 passed, 0 failed**.
- `python3 -m pytest tests/test_market_guide.py tests/test_market_guide_preview.py -q` — **68 passed**, with six inherited temporary-Chromium cleanup warnings.
- `git diff --check` — pass.
- Full source-backed Chromium qualifier in `r11-evidence/qualification.json` — **13 check groups passed**:
  - canonical registry + rendered-owner validators consumed, 46 entries;
  - keyboard search, aliases and query reload;
  - coverage-name recovery;
  - board-vs-indicator distinction;
  - opposite risk/strength semantics;
  - four-cell quadrant vs non-directional confirmation;
  - native dialog Tab containment/Escape/focus/scroll restoration;
  - browser back/forward state restoration;
  - unknown and zero-result recovery;
  - **184 detail journeys** (46 × EN/ZH × desktop/mobile);
  - **48 current-source screenshots** (home, score, risk, quadrant, transition, help × light/dark × EN/ZH × desktop/mobile);
  - no-JavaScript mobile fallback with all 46 definitions;
  - zero uncaught browser errors and zero external requests in exercised flows.

This is standalone/source-backed candidate proof, **not** actual Macro embedding, independent acceptance, merge, deployment or production proof.

## Product ruling: replace “Look up” with help where the question occurs

Current `templates/dashboard.html.j2` confirms that the six-item **Look up** rack is a separate footer row. The actual dashboard already has natural explanation surfaces:

- Market State Score — the live `ms-score` / score label;
- Regime Quadrant + Transition State — the regime pill / existing regime-context affordance;
- Risk Radar — the direct Risk Detail control and risk dialog;
- Evidence Matrix — the Evidence Breakdown dialog;
- Sector Heat — the Sector Temperature / heatmap workflow.

The production direction is therefore **not** to beautify the six-link glossary rack. Each explanation should move beside the instrument it explains, using the existing site **Lens** interaction (desktop popover + mobile bottom sheet + keyboard/touch + bilingual behavior) and a normal `reference.html#<id>` fallback link. The rack is removed only after all six explanations have verified in-context replacements; until then it remains as fallback navigation.

The full Market Guide keeps its richer native dialog/detail experience. Macro should reuse Lens rather than mount a second generic help-modal family.

## Canonical-content integration contract

Dashboard explanatory copy must project from `config/market_reference.yml`; it must not be copied into the dashboard template or generated by a runtime model. The full `scripts/build_market_reference.py` validator remains the authority for registry/anchor/source closure. A dashboard consumer projection may fail closed on the exact fields it renders and degrade to existing links; it must not become a competing validator or store.

A RED-first attempt to add that production consumer projection was explicitly refused before dispatch in this tool generation, so **no projection source change occurred**. Do not rephrase/retry or switch tools/providers to force it through. Resume only when that action’s platform boundary materially changes.

## Risk-Radar editorial finding

`engine/risk_radar.py` establishes: watch begins at 55; its calibrated 21-day probability is 13% versus an unconditional 17.8% base, and engine copy explicitly calls it an **early watch flag, not an edge**. The registry currently groups Calm + Watch as “no unusual pullback pressure.” That is directionally compatible with the below-base calibration but hides the “early heads-up” distinction. A RED test for clearer wording was written and proved the gap; the source-copy edit was then explicitly refused before dispatch and the RED was removed. No registry mutation is claimed. This wording correction remains a bounded editorial follow-up, not a risk-model or threshold change.

## Current-base and CI reconciliation

Current-base receipt: `r13-evidence/current-base.json`.

- Source subject: `4ca095c5eef1d790f42c5ed60445425c96d60281`.
- Observed `origin/main` during receipt: see receipt (main continues to move through bot/catalog commits).
- `git merge-tree --write-tree origin/main <subject>` succeeded; no merge/rebase was performed.
- No material guide/dashboard paths changed between the last examined main and that receipt’s main.
- The prior hosted CI failure on remote R8 head `93cb7f0b3f0e` was **not a guide behavior failure**. `contract-delta` refused because `options-payoff-lab-consumer` omitted `tests/test_options_skew_source_note.py` from its own declared paths. Current main contains fix `8db6896dab21` (#7864), and the latest-base merge-tree contains both that repair and the Market Guide CI enrollment.

## Source custody / outstanding gates

- PR #6792 remains OPEN/DRAFT at `7853ffeaca41d4d8f33995b1453db2be134ce8a3`; its reference-template/builder/evidence custody is not overwritten. Its latest consumed independent review remains REQUEST_CHANGES on its proof contract.
- PR #7859 remains OPEN/DRAFT on `templates/dashboard.html.j2`. Its consumed patch is in the Risk Detail/volatility area rather than the Look up rack, but it is still a same-file current-base integration dependency.
- PR #7647 has reviewer requests to `mastermindx-2` and `mastermindx-3`; no submitted review has been observed. Requested review is not START or acceptance.
- A fresh synthetic current-reading browser-evidence generation action was explicitly refused before dispatch after the source cleanup. The exact-head full standalone qualifier is green; earlier current-reading visual inspection remains design evidence only and is not rebound to the cleaned subject.
- Actual Macro browser embedding and live production proof remain unperformed.

## Next action

When the production consumer-projection action is permitted again, implement the smallest real slice: canonical registry → server-side Lens projection → Market State Score and Risk Radar in-context Lens controls with normal deep-link fallbacks; render current real Macro source; prove desktop/mobile, EN/ZH, focus/touch and host/navigation invariance; then continue the other four explanations and remove the Look up row only when coverage is complete.

Do not recreate Paper boards, the bilingual registry, the keyboard/focus repair, search-heading repair or R8 host-isolation fix. Do not reintroduce the rejected noisy natural-language search. Do not call standalone/synthetic proof actual Macro embedding, and do not self-approve #6792 or #7647.
