# Semiconductor B implementation plan — author review and proof limits

23 September 2026. Author-principal review of a proposed implementation plan, not independent acceptance or a product-test result.

## Exact artifacts and current stage

Plan: `docs/superpowers/plans/2026-09-23-semiconductor-theme-intelligence-implementation.md`.
Commit: `e9050b5982135ce1b597085697b00a8e597de60c`.
Git blob: `9145923019fdde5d37a5151e1a18b6f2c94f67b9`.
SHA-256: `df67f2bfc651f71305bcc2f70939d52a04a0f13e6aa9202c5f1e442723c20a58`.
Length: 78,567 UTF-8 bytes.

Specification: `docs/superpowers/specs/2026-09-23-semiconductor-theme-intelligence-design.md`, original commit `f9402fddf8f6c853ea0368ad47795db591b8f85e`, blob `65590f430362e0d9942f56f8cf8b358d9fb07d19`.

Carrier: Macro draft/HOLD PR #7780, branch `sol/semiconductors-research-20260923`.
Operation: `gmi-semiconductors-research-20260923-sol-001`; existing parent `WS:GMI-THEME-GRAPH`.
Procedure: Mastermind `4c1b3d389286df2a4b5b98a4d4491f2c8a1263f2`, Skillpack 1.0.1/bootstrap 1.
Planning source pin: Macro `2b2cae6a148f920b5c4e7ebd8df19ed14b159f5a`, not a rebase.

Chairman continuation after the presented written specification advanced the planning stage. The current gate is review of this implementation plan. No Fable commission, worker assignment, runtime START, source-custody transfer, product edit or deployment is claimed.

## Material author-review results

1. **No duplicate shared implementation.** The curation assertion and shared client are coordinated with Robotics #7773. The plan names shared files and reuses accepted successors rather than making semiconductor variants. Store #7462 and template #7669 custody remain exact execution gates; their existence does not permit takeover.
2. **Two positive witnesses cannot be replaced by refusals.** TSMC management history anchors HBM/packaging W-A; onsemi anchors non-AI W-B. Every witness must include a real industrial fact, correctly bound company route and prior-outlook/actual/new-outlook context. Missing optional consensus or unit-cost arithmetic may degrade, but missing all economics fails B acceptance.
3. **Input coverage is not assumed.** The inspected Earnings registry covers Apple and four homebuilders; this is one source function, not a whole-runtime census. T05 checks actual owner objects and enrolls only missing witness profiles. Parsers are not mistaken for physical data readers.
4. **Identity precision survives composition.** venue:symbol, native CIK/company and Data OS security IDs remain different. The existing 1970 alias default is not historical evidence. Source-only businesses have no invented CIK or company node. K1 cannot be made cross-type resolved by declaring a bridge name.
5. **Private authority precedes data.** T01/T03 require the actual approved GMI private binding and current source rights. No guessed environment variable, second parquet, new bucket, public fallback or caller-authored permission boolean substitutes for that binding. Source restrictions cannot be erased by calling a claim house-curated.
6. **Time and generation are operational requirements.** All view/drawer/page results use one authorized revision selection. Source-history and system-replay differ. A corrected fact invalidates its older explanation; missing yield does not erase a supported operational start.
7. **No accidental C scope.** Source-local selectors, scoped relations and correction lineage improve B now. All eight C1 entity-promotion/migration cases are explicitly deferred, with no B tasks assigned to them. The 48 B requirements remain unchanged from the exact specification.
8. **The existing user workflow is the consumer.** Current crosswalk anchors are reused without renaming non-AI companies into AI constituents. The actual compiled route and generated assets must be proved. A new schema, an API alone or fixture screenshots cannot close T11/T12.
9. **Task/test dependency direction is explicit.** Fixture loading imports F04 dataclasses lazily. Lower-level tests can exist before the consumer module. The API authorization test uses a principal path without a permissive override. The example string identity assertion is supplementary; exact schema/type parity must also be tested.
10. **Unchanged market behavior is independently checked.** T12 uses identical incumbent inputs for member/order/recommendation/entry/coverage comparisons. Research interpretation never changes weights or grants rank/gate/size/originate/entry authority.

## Clarifications for implementers reviewing the candidate

In T09, the prohibition on source URLs/body content concerns **error disclosure, diagnostics and unauthorized output**. Successful entitled evidence views may expose the exact source locator or allowed primary-source link under current source rights; otherwise the source-traversal user job could not work. Never make a private raw locator into a public URL.

The raw rights-registry digest is an internal policy-consistency receipt, not a public explanation of withheld rows. Any generation token exposed to a principal must derive only from the authorized selected inputs and applicable rights decision. Adding only a denied relationship with unchanged visible inputs and applicable rights must not change the visible generation or response. Current rights still apply to every emission, including historical selection.

The single `comparison_state` never implies that all three management-sequence periods are interchangeable. Compare prior guidance with the actual for its own period; compare revisions only for the same future horizon. Preserve the separately labeled next-period outlook without treating it as a surprise over the reported period. Native derivation receipts identify their exact input pair and financial basis.

These clarify the existing B requirements; they authorize no broader schema, ownership or execution scope. Any implementation that needs an incompatible contract revision returns to the relevant owner and plan/spec review.

## Actual offline checks

Commands run against the final local plan and bundled exact specification:

```text
python build_plan_matrix.py
python verify_plan.py
```

Observed result: **15/15 document checks pass; zero product tests**. The checker validates exact specification blob, verbatim requirement text in the matrix, 56 unique IDs, 48 B mappings with named task/test nodes, eight deferred C1 rows, 12 task IDs, reference closure, future/not-executed states, readable matrix inclusion, placeholder absence, Python-snippet syntax, two named positive witnesses, plan-review state, five review-focus entries and no prechecked execution steps.

Four deliberately corrupted variants were rejected: removed B requirement; C1 assigned to B; undefined task; falsely checked product-deployment step. These are document-checker negative cases, not application mutation coverage.

Fresh-directory reproduction used only bundled specification/plan/scripts. Both commands exited zero. The requirement JSON, rendered matrix and check-result JSON were byte-identical to the originals. Python snippets were parsed, not executed. No product modules were imported, no application tests ran and no native data was modified.

The final portable plan's calculated Git blob matches the canonical exact-commit readback. The raw-download tool returned no file because its URL-view prerequisite was not met; no successful HTTP download is claimed. Local text was aligned to the authored publication, then the complete Git-blob fingerprint and all checks were rerun. This is full-content fingerprint equality, not a claim of a separately downloaded file.

## Still-unproved execution prerequisites

Approved private source/storage/publication binding; actual witness native objects and issuer mappings; current shared schema and template/store custody release; actual route integration; current source rights and revocation behavior; independent review; current-base/CI qualification; both production browser witnesses; real revision-to-visible propagation. These remain explicit gates, not accepted because the plan is detailed.

The 28 fixture specifications and 48 planned B test/proof nodes are **NOT EXECUTED**. The eight C1 nodes are **DEFERRED**. No forecast quality, industry completeness, economic return, or source truth is established by the document checks.

## Next action

Chairman reviews the complete plan and these clarifications. After plan acceptance, prepare the final exact-revision Fable orchestration packet and perform its lawful delivery/assignment procedure. Do not rerun broad domain research, initiate C, merge the research PR or start product implementation from this review alone.

MISSION_COMPLETE: false. PLAN_STATUS: PROPOSED_FOR_REVIEW. Fable remains uncommissioned.
