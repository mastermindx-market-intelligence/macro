# Stock Identity W3AR Census / P2 Prereg Charter — 2026-08-30

**Operation:** `SI-W3AR-CENSUS-P2-PREREG-V1`  
**Parent program:** `SI-FABLE-COO-PROGRAM-20260828`  
**Authority:** Chairman's standing end-to-end Stock Identity completion mandate + Sol source-law ruling under protected Skillpack `Mastermind@5d6694a3e4d0a6c8e863f38d21d7840f869944df`.  
**State at freeze:** records/research only; no P2 draw, no P1 reread, no blind outcome read, no fit/ruler outcome read, no Prophet authority.

## 1. Observable mission

Produce the outcome-free evidence packet that decides whether a fresh PR-3 calibration epoch is scientifically feasible under the now-frozen two-clock replay law. The wave must derive family-by-family historical replay eligibility from the exact already-registered W2 method and required PIT inputs, census the genuinely untouched name pool, quantify eligible support by family/era/grain without reading localization outcomes, and preregister the future P2 draw/sample law. It stops **before** any P2 membership is drawn or revealed.

A successful terminal return is exactly one of:

- `GO_P2_PREREG` — source/input support and untouched-pool capacity are sufficient to freeze a fresh P2 registration for Sol review;
- `NO_GO_CALIBRATION_RECOVERY` — the existing lawful substrate is insufficient; do not manufacture a new historical source or weaken the method law;
- `BLOCKED_NEW_SOURCE_LAW` — feasibility depends on a genuinely new source/authority decision that this wave may not make.

## 2. Sol-ratified architecture — no longer a hypothesis

Historical research replay eligibility and live/prospective availability are separate epistemic clocks.

### 2.1 Historical replay eligibility

A family/date/name/grain is historically replay-eligible only when its **exact W2-registered construction** can be evaluated from inputs knowable at that historical point, with the registered causality/era semantics. The lawful basis vocabulary is closed for this wave:

1. `LEDGER_COVERAGE` — only where the committed ledger/store itself contains lawful historical rows/vintages; absence of a row is not automatically a zero or an earlier availability date.
2. `PIT_RECOMPUTE` — W2 explicitly registered historical recomputation through the producer's own pure/era-pinned function and every required input is reconstructable PIT.
3. `LOCKED_SPEC_BACKCAST` — W2 explicitly registered a Class-B locked-spec backcast; every row remains marked `spec_postdates_history=true` and the exact registered deviations/era pins are preserved.
4. `STORE_VINTAGE` — history exists only over an explicitly persisted source-state vintage range; availability is bounded by that store's real coverage, never by output fires.
5. `PRICE_REFERENCE` — only the frozen naive/reference constructions whose entire input is the lawful adjusted price plane and whose formula was preregistered as a reference construction.
6. `PROSPECTIVE_ONLY` — Class P or any W2 family explicitly adjudicated non-reconstructable. Historical eligibility is always false.

Unknown required source/context/input support fails closed. Generic `spec_hash` existence, current code existence, actual software deployment date, fire occurrence, first/last fire timestamp, event min/max, or a convenient output file may not substitute for the registered basis.

### 2.2 Live/prospective availability

The real deployment/emitter/known-at clock governs Radar/W7 prospective evaluation and any live product projection. It never licenses historical backfill. A retrospective backcast may be a lawful research object while truthfully remaining unavailable live at that historical date; those facts must be represented separately.

## 3. Family-law anchors to verify, not redesign

The worker must read the authoritative W2 registration and family registry and mechanically bind every current family to its already-registered method. Expected examples, subject to exact current-source verification:

- `grey_dot_macro` — `PIT_RECOMPUTE` only if the registered producer inputs are PIT reconstructable.
- `confirmed_buy` — ledger `buy` arm is `LEDGER_COVERAGE`; the separately named pre-filter `cb_3d_confluence` recompute arm may be `PIT_RECOMPUTE`. They must never be merged semantically.
- `rebuy` — ledger-only; no invented backcast.
- `reclaim_waiver` — `STORE_VINTAGE` over the committed nightly-state artifact's actual era only; no synthetic earlier context.
- `weekly_washout_turn` — committed ledger plus W2-registered truncated weekly pure-function recompute, with the two sources distinguished.
- `sea_event_classes` — explicit store/backfill coverage; derive denominator/coverage from source-store truth, never fire min/max.
- `rsi30_cross`, `low20d_bounce`, `stoch2w_cross` — `PRICE_REFERENCE` where the lawful adjusted price plane supports the registered formula.
- `tier_cascade_t1..t4`, `grey_dot_terminal`, `bottom_watch_terminal` — only the exact W2 Class-B `LOCKED_SPEC_BACKCAST`, retaining `spec_postdates_history=true` and all named deviations.
- STARTER state trio and every Class-P family — `PROSPECTIVE_ONLY`. The separately named replayable `starter_signature` is evaluated under its own registered construction and is never treated as the state trio.

The worker may discover a current family registry delta. It must classify it against current W2/source law; it may not silently invent a seventh basis type.

## 4. Untouched clean-pool law

The clean-pool census is deterministic and outcome-free. It may read partition/cohort **membership identifiers solely for exclusion/accounting**, but it may not read P1 constant values, P1 ruler results, blind per-name outcomes, localization outcomes, fit/rank results, or any future target variable.

Start from the canonical W1 evaluated-universe snapshot and remove, at minimum:

1. every frozen pilot/exemplar name;
2. Barrick `B` / every W1-A1 design-touched amendment name as applicable;
3. every `SI-SEALED-CAL-P1` member;
4. every blind-arm member;
5. every name later design-touched by Stock Identity research/debugging/repair in a way that could affect P2 constant design;
6. every unresolved ticker-identity / reused-ticker / price-plane contamination name whose history cannot be cleanly bounded PIT;
7. any name whose required lawful price plane cannot support the frozen geometry needed for calibration.

The worker must publish the exclusion taxonomy, counts, canonical membership/hash inputs, final clean-pool count, and a SHA256 over the canonical sorted eligible-name identifiers. **Do not publish or draw P2 membership.**

## 5. Outcome-free support census

For every family × era × grain, report support over the clean pool using source/input availability only:

- number of clean-pool names with historical eligibility;
- number with lawful price-plane coverage for the registered replay window;
- first/last eligible source/input dates and their basis **only when derivable from source/input coverage rather than output fires**;
- missing/blocked counts by typed reason;
- concentration by source basis / plane / era;
- whether the family is calibration-usable or structurally/prospectively unavailable;
- exact family registry/spec/era hashes used.

Do **not** compute C-LOC-R, C-LOC-D, recall, precision, false-start rate, localization distance, expert ranking, fit, or any outcome-derived statistic. This is a feasibility census, not a preview of the calibration result.

## 6. P2 preregistration proposal — freeze before reveal

If the outcome-free census supports recovery, return a complete proposed registration for Sol approval **without executing the draw**. It must define:

- P2 name and version;
- canonical eligible-pool hash and exclusion receipt;
- deterministic seed derivation and exact draw algorithm;
- stratification variables, if any, using only pre-outcome metadata already allowed by the research constitution;
- target sample size and minimum per-stratum handling;
- disjointness assertions against pilot/B, blind, P1 and design-touched sets;
- one-time-look semantics for the PR-3 constant family;
- recent-history guard / FIT-TEST protection consistent with existing law;
- exact constants allowed to be set and rule hashes that must already be frozen before the one-time read;
- abort conditions for insufficient replay/input coverage or degenerate calibration;
- artifact names/hashes/receipts that the later P2 draw must emit;
- explicit statement that blind remains untouched and P2 is excluded from later confirmatory Q1 grading.

The prereg must not choose a draw size or strata because they make expected outcomes look favorable. Only coverage, independence and estimation requirements may determine them.

## 7. Protected evidence / forbidden reads

Forbidden in this wave:

- P1 ruler constant values or rejected Attempt-1 localization result as design input;
- any P1 rerun;
- blind per-name expert/localization/outcome evidence;
- any P2 membership draw or reveal;
- outcome-driven inclusion/exclusion;
- per-name expert ranking/argmax;
- historical backfill of Class P;
- using #6678's unauthorized W3S cohort verdict as a Stock Identity design input;
- Prophet/rank/gate/size/trade authority changes.

The existence of the rejected P1 values in immutable audit artifacts does not authorize reading them for this wave. If a code path would open them incidentally, stop and replace that path with an outcome-free input.

## 8. No-rebuild / ownership boundaries

- Extend/read existing W2 replay registry/owners; do not create a second replay harness.
- If a machine-readable historical-eligibility projection is needed, it must be a derived Stock Identity artifact owned by the existing program and recomputable from W2 registry + source/input coverage; it is not a new authority plane.
- Live Entry Radar remains prospective expert-event owner.
- Existing identity, price, correction and qledger/evidence owners remain canonical.
- No new market-data provider or source authority in this wave.

## 9. Method and review

Deterministic:
- family method classification from W2/registry;
- clean-pool exclusions/hash;
- source/input coverage census;
- prereg draw algorithm/spec generation.

Statistical/model-generated:
- none required for the primary result. An LLM may assist code/research review but originates no eligibility fact and has zero authority.

Independent adversarial review must try to falsify at least:

1. a ledger-only family accidentally gaining history before its ledger;
2. fire timestamps being used as an availability denominator;
3. a current code/spec date being used to erase a registered Class-B backcast;
4. Class-P leakage into history;
5. a design-touched/P1/blind name entering the clean pool;
6. reading an outcome column while claiming an outcome-free census;
7. a hidden second replay/availability store;
8. a P2 membership draw occurring before Sol GO.

## 10. Acceptance and stop

Return to Sol with:

- exact current Skillpack/main/registry/masterplan/W2 pins;
- exact branch/PR/head if any code/records were needed;
- family-by-family method table and source/input proof;
- clean-pool exclusion ledger, count and hash;
- outcome-free family/era/grain support census;
- complete unexecuted P2 prereg proposal if feasible;
- independent review findings;
- CI/test evidence for any implementation;
- exactly one terminal recommendation: `GO_P2_PREREG`, `NO_GO_CALIBRATION_RECOVERY`, or `BLOCKED_NEW_SOURCE_LAW`.

**STOP before any P2 draw/read.** A `GO_P2_PREREG` return is not authority to execute P2. Sol must separately adjudicate/freeze the registration and commission the one-time draw.