# Bottom-Up Stock Identity & Expert Routing — Complete CEO Master Plan

**Date:** 2026-08-28  
**Chairman:** Chris  
**CEO owner:** Sol  
**Sustained COO:** Fable, once the shared Slack carrier is explicitly claimed  
**Program:** `WS:STOCK-IDENTITY`  
**Program operation key:** `SI-FABLE-COO-PROGRAM-20260828`  
**Protected Skillpack:** `mastermindx-market-intelligence/Mastermind@97f85ce5b84030faf4d291f988a1c642fb15e80a` (`mastermind.sol_skillpack.v1`, v1.0.0, bootstrap major 1)  
**Current Macro reconciliation pin:** `24ccea3fe482ab97c415db387f272b34c4852ed3`  
**Prior architecture carrier:** Macro PR #6529, same logical records carrier; this file extends that approved architecture and does not create a second program.

## 1. Final mission

Mastermind must understand an individual traded security as a changing behavioral process rather than as a permanent sector/archetype label. The system must be able to answer, point in time and without outcome audition:

1. what instrument this is and whether its ticker/issuer history is clean;
2. what its behavioral fingerprint is;
3. whether that fingerprint has entered a new identity epoch;
4. what coarse path state it is currently in;
5. how preserved entry-expert families behaved around comparable path-defined episodes;
6. whether the available evidence is estimable or requires ABSTAIN;
7. whether any structure-derived expert profile survives name-disjoint blind evidence;
8. whether the surviving profile continues to behave prospectively on frozen definitions;
9. what lawful downstream use, if any, Stock Identity has earned.

The program terminates in exactly one of three company-level outcomes:

- `NO_GO_KILL_ROUTING`: expert-routing claim is killed and durable DNR is recorded; descriptive identity/epoch context may remain zero-authority.
- `GO_DESCRIPTIVE_CONTEXT`: SIF/identity context is useful to research and machines but earns no Prophet ranking/gating authority.
- `GO_PROMOTION_CANDIDATE`: evidence is strong enough to justify a separate promotion charter. This is not promotion itself.

## 2. Non-negotiable laws

- `DNR:KILL-OUTCOME-AUDITION` remains total under every ruler. No own-name best-of-menu expert selection, hidden ticker keyed model, grid audition, or disguised equivalent.
- The first-arc personalization channels are only A/B/C from `DEC:SI-METHOD-LAW-CHANNELS`: global structure map, behavioral-neighbor pooling, bounded empirical-Bayes residual.
- Channel A is capacity-bounded per the original §2.3 controls: its effective parameter count `p_eff` is declared before any fit and must satisfy `p_eff <= floor(N_train_names / 10)` exact per training fold, with the functional form frozen at W3 (additive/monotone in a declared feature subset unless a richer form is separately preregistered) and the budget enforced on every training fold before any fit.
- Ticker is identity continuity, not predictive freedom.
- Sector/archetype may be a diagnostic/baseline, never the routing/grouping key.
- Live Entry Radar owns prospective expert events. Stock Identity consumes canonical `mastermind.entry_event.v1`; it never writes or forks that store.
- W2 historical replay remains the already-justified bounded historical reconstruction; no second replay framework.
- Sealed W1 calibration/blind partitions and W1-A1 GOLD/Barrick rulings remain intact.
- Class-P families are prospective only; no synthetic historical backfill.
- Missing, measured-zero, structural-absence, no-coverage, not-yet-available, stale, source-failed, identity-unresolved, unestimable, censored and abstain remain distinct typed states.
- All Stock Identity outputs remain authority-false until a later separate promotion charter grants more.
- Green CI, merge, Slack delivery, watcher activity and a Fable ACK are not scientific or production completion.

## 3. Current capability ledger

| Capability | State |
|---|---|
| W0 archaeology/research contract | `PROVEN_LIVE` |
| W1 Identity Atlas + sealed partitions | `PROVEN_LIVE` |
| W1-A1 GOLD/Barrick correction | `PROVEN_LIVE` |
| W2 expert replay/provenance | `PROVEN_LIVE` |
| W3 localization ruler | `NOT_BUILT` |
| W3 estimability/effective-N census | `NOT_BUILT` |
| terminated-instrument survivorship control | `NOT_BUILT` |
| real identity epoch detector | `NOT_BUILT` |
| PIT/final epoch duality | `NOT_BUILT` |
| W5 power + confirmatory fit | `NOT_BUILT` |
| behavioral neighborhoods / EB residual | `NOT_BUILT` |
| operational abstention / SIF | `NOT_BUILT` |
| real SIF machine consumer | `DARK_OR_DISCONNECTED` |
| real operator/research consumer | `PARTIAL` (W1 dossiers only) |
| Radar prospective expert substrate | owner-side `PROVEN_LIVE`; SI consumption `NOT_BUILT` |
| Stock Identity prospective evidence clock | `NOT_BUILT` |
| Prophet rank/gate/size authority | `REJECTED_BY_DESIGN` for this arc |

## 4. Program wave graph

### M0 — Records reconciliation and live shared carrier

**Mission:** make the approved architecture/master plan current and give one sustained Fable COO an exact operation/thread to work with Sol.

**Done when:** current Skillpack/main/collision state are pinned; PR #6529 is reconciled on the same carrier; this master plan and W3 implementation plan are immutable-referenced from one Slack top-level thread; Fable replies `ACK SI-FABLE-COO-PROGRAM-20260828`, states its pickup SHA, and confirms its exact continuation/watch capability.

**Does not prove:** W3 started, watcher runtime live, or any research result.

### M1 / W3A — Expert-independent localization ruler

Build executable ruler components and the two frozen composites without opening confirmatory fit conclusions. Outputs are deterministic, spec-hashed, authority-false, and accompanied by unconditional flooding/attribution blocks and grain/null controls. W3A also performs the one-time sealed-calibration constant-setting for the PR-3 family (`lambda_fs`, recall floor, composite constants) under rule-before-value discipline with TrialLedger-registered diagnostic grids and a logged fit-read look budget — fed by the Sol-authorized bounded calibration-fire substrate (same W2 replay machinery over the drawn `SI-SEALED-CAL-P1` roster only, calibration-purpose-only, authority-false; metric primitives and replay/family/spec hashes committed before any constant value is computed) — and freezes the Channel-A model constitution (declared feature subset, additive/monotone form, exact `p_eff` counting rule, per-fold `p_eff <= floor(N_train_names / 10)` enforcement) without fitting anything.

### M2 / W3S — Terminated-instrument survivorship control

The sampling frame starts from `config/delisted_symbols.yml` with `terminated_reason` recorded. Inventory existing canonical adjusted-history owners first. Produce at least five identity-resolved terminated U.S. instruments with full adjusted OHLCV and terminal/source/correction/rights receipts, or return `BLOCKED_NO_LAWFUL_DATA`. Survivor-only substitution is prohibited. An available cohort enters the W5 survivorship control/report; its performance does not independently pass or fail Q1 absent a future separately preregistered criterion.

### M3 / W3B — Estimability and dependence census

Before any fit table, produce per-cell episodes, fires, distinct calendar clusters, concentration, attribution/flooding, grain/feature/source coverage, explicit `UNESTIMABLE` reasons and the exact power-simulator input population. W3B binds W3A's metric schema but consumes only a typed outcome-independent support/coverage frame — cell inclusion/estimability may never depend on realized localization or composite quality, and a regression must fail if any realized-fit column can alter inclusion.

**W3 Measurement Release gate:** Sol accepts W3A + W3B + W3S status. W5 remains held.

### M4 / W4A — Identity epoch detector v1

Replace provisional `epoch_0` with the preregistered rolling-fingerprint change detector, explicit `knowable_from`, persistence/minimum-epoch law, stationary-bootstrap false-boundary calibration, injected-shift power and threshold plateau evidence.

### M5 / W4B — PIT/final epoch projection

Emit `epoch_pit` and `epoch_final`; only PIT can grade Q1–Q3. Truncation/replay tests must prove a future boundary cannot relabel a historical PIT decision row.

**W4 Identity Release gate:** Sol accepts real epochs before any confirmatory fit.

### M6 / W5P — Power/survivorship gate and blind finalization

On the exact post-exclusion pool, compute preregistered power. Blind membership may only prefix-shrink or extend through the frozen seeded procedure; no hand swaps. Output is exactly `RUN_Q1`, `NOT_RUN_UNDERPOWERED`, or `BLOCKED_SURVIVORSHIP`.

### M7 / W5Q — One confirmatory read

Execute Q1–Q3 exactly once when W5P says `RUN_Q1`, plus descriptive Q4. Use name-disjoint OOS, PIT epochs, both composites, all mandatory nulls, blind evidence, honest cluster-N, fixed multiple-testing law and terminated-instrument control. No rescue tuning.

### M8 / W5R — Nondelegable Sol consequence adjudication

Sol independently attacks leakage, identity hygiene, dependence, cadence artifacts, nulls, blind integrity and DNR compliance, then mechanically issues one of:

- `NO_GO_KILL_ROUTING`
- `ACCRUE_OR_REGRADE_ONLY`
- `LIMITED_DESCRIPTIVE`
- `GO_W6_DISPLAY_TIER`

A W5 kill closes the routing branch. Prospective work cannot rescue a killed historical construction.

### M9 / W6A — Licensed channels only

Only on `GO_W6_DISPLAY_TIER`, operationalize the channels actually licensed by W5. Every row prints component N, shrinkage weight, neighbor eligibility/overlap exclusions, instability and null separation. No sector/ticker fallback.

### M10 / W6B — Abstention and `stock_identity.sif.v1`

Build the SIF containing current PIT epoch, fingerprint reference, current bars-only state, drift indicator, conditional expert profile by episode type/tier, coverage/uncertainty and explicit abstain reasons. Never claim an unresolved live future episode anchor/type.

### M11 / W6C — Real zero-authority consumers

After fresh owner archaeology, create one bounded plan for two existing-owner seams:

1. a machine context consumer that asserts `authority.* == false` and has a differential proof that Prophet score/rank/gate/size/trade outputs are unchanged;
2. a visible research/operator consumer showing identity, epoch drift, estimability, expert-family evidence, abstention and source clocks.

No app/site path is preclaimed by this master plan; the current owner at W6C decides the lawful seam.

### M12 / W7A — Prospective enrollment

Reuse current Evaluation OS/qledger and the canonical Radar event path. Register frozen hashes/eras and start a real forward evidence clock. Preserve keep-first observations, source corrections and retrospective/prospective pool separation.

### M13 / W7B — Prospective shadow accrual

Accrue without peeking through preregistered floors. Measure coverage, abstention, epoch changes, family availability, drift and mature localization outcomes. Insufficient maturity remains `ACCRUING`.

### M14 / W8 — Final Sol/company ruling

At maturity, Sol reviews historical + prospective evidence + real consumer proof and records one terminal outcome: kill routing, descriptive context, or promotion candidate. Any Prophet influence requires a new separate charter and current Eval/Prophet gates.

## 5. Operator routing law

This program uses one sustained Fable COO because it is architecture-sensitive, long-lived and statistically adversarial. COO persistence is an organizational role held across bounded carriers and bounded sessions — durable state lives in the program records, not in any one session — and is not permission for a mega-session or mega-branch.

Fable routes child work by difficulty:

- **Luna / mechanical:** deterministic fixtures, schema conformance, repetitive artifact migration, low-ambiguity test ratchets.
- **Terra / standard engineering-research:** bounded implementation, data archaeology, refactors, ordinary debugging, independent review.
- **Codex:** preferred engineering surface for a frozen implementation wave; use Luna/Terra capability appropriate to the task.
- **Sonnet:** bounded multi-step Claude-environment execution where continuity matters.
- **Opus:** scarce specialist for difficult statistical/debugging/architecture-sensitive investigation and independent adversarial review after the wave is frozen.
- **Fable:** program control, collision management, decomposition, child routing, bounded merges/proof capture and return packets inside this master plan.
- **Sol:** architecture, no-rebuild rulings, W5R, final W8 acceptance, promotion/authority decisions.

No worker is asked to rediscover the full company or redesign the thesis from a broad prompt.

## 6. Shared Slack dialogue / watcher law

The program uses exactly one top-level Slack thread for the sustained Sol↔Fable program dialogue, bound to operation key `SI-FABLE-COO-PROGRAM-20260828`.

The parent message must carry `watch_mode="turn_watch_v1"` for future canonical compatibility and require Fable, before work:

1. reply `ACK SI-FABLE-COO-PROGRAM-20260828` in the exact thread;
2. read the entire thread;
3. state exact current Skillpack SHA, Macro pickup SHA and first child carrier(s);
4. keep all `PROGRESS`, `BLOCKED`, `DECISION_REQUEST`, `RESULT`, and Sol `RULING` / `CONTINUE` / `STOP` on the same thread;
5. after every nonterminal return, enter the approved exact-thread wait/watch path;
6. after every Sol continuation, reread the full thread, resume the same operation, and re-arm its wait/watch;
7. if it cannot maintain a watcher/wait, return `BLOCKED WATCH_UNAVAILABLE` instead of silently leaving;
8. on accepted terminal `STOP`, terminate its watcher and end the COO session.

Current canonical Worker Presence / Turn-Watcher is still records-only at protected Mastermind `97f85ce5...`; therefore neither side may claim automatic wake is production-proven. Any temporary continuity mechanism is attention-only, never lifecycle authority, and must be disabled on terminal completion.

## 7. Review/merge law

Every child PR:

- has one independently useful capability;
- pins current main and current accepted source law before pickup;
- names changed paths and collision check;
- uses discriminating tests for the exact forbidden mutation;
- keeps all Stock Identity authority false;
- returns exact head SHA, changed files, CI, real-input proof, unverified items and next gate;
- remains `BUILT_NOT_PROVEN` until its named real proof closes;
- never auto-rolls across a Sol hard gate.

Fable may merge reversible authority-neutral child PRs where current repository law permits after exact-head CI + independent review. Sol re-reviews at W3/W4 milestone bundles, every scientific/authority boundary, W5R and W8.

## 8. Program acceptance

The program is complete only when either a preregistered kill makes downstream GO-only work inapplicable or all applicable truth/intelligence/product/learning criteria are met:

- identity/ticker/correction truth is reproducible;
- terminated survivorship control is honest or routing is blocked;
- ruler/null/grain law is executable and outcome-independent;
- every fit prints honest episode + calendar-cluster N and abstains when unestimable;
- real PIT identity epochs exist and cannot future-relabel past rows;
- W5 power/Q1-Q3 obey the frozen one-look law;
- no outcome audition/ticker/sector hidden key survives;
- abstain is visible and means no adjustment, never veto;
- on a GO path one machine and one operator consumer use SIF through current owner seams while Prophet remains unchanged;
- a real prospective clock accrues canonical Radar observations on frozen definitions without peeking;
- instrumentation shows coverage, abstention, identity drift and mature localization outcomes;
- Agent OS carries one final terminal ruling with exact evidence hashes/eras and next lawful action.

## 9. Immediate execution frontier

1. Reconcile PR #6529 on the same carrier against current Macro main and the current Skillpack watcher amendment; do not open a replacement architecture PR.
2. Land this master plan plus `docs/superpowers/plans/2026-08-28-stock-identity-w3-measurement-release.md` on that carrier.
3. Create one top-level Slack program thread for `SI-FABLE-COO-PROGRAM-20260828` and have the Chairman attach/assign the selected Fable COO session to that thread.
4. Fable ACKs and claims the program; then W3A and W3S may open as separate child carriers after fresh collision checks. W3B may stage only after binding W3A's schema.
5. Sol and Fable remain on the same Slack thread through nonterminal returns; terminal completion ends both watchers/session continuity for this program.
