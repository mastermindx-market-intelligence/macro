# US Macro economic backdrop — bounded implementation

## Current proof epoch — reviewed-source repairs
This section supersedes earlier source-date wording and proof counts below.
The repaired production source is cdb5a7a27dd83f5a6afd9c9e8bd748bd5937affb.
First independent review was REQUEST_CHANGES; all three material findings plus the
optional-estimate false-negative were repaired with14failing discriminators first.
The full existing owner set now passes222tests, with no skips and no data-guard waiver.
The real normal-builder target is e93ed968875eae9803286b083d58ffb70f5f87e6af4816735e8fb90ecc8485b9.
Eight refreshed capture cells bind to cdb5a7a2. Twelve actual-page cases and three
investigation journeys pass, including visible model basis and44px source receipts.
Current EVIDENCE.yml owns canonical-repaired/manifest.json; d50 captures are historical.
The receipt distinguishes CPI reference month from source as-of. A missing/unsupported
fact date is explicitly Date unavailable, not silently omitted. Includes estimates is
shown for a present positive-weight simulated component, without changing owner math.
Full details are in REVIEW_REPAIR.md and CONTINUATION.md. Current-head hosted checks,
checkpoint obligations, review and release remain separate; this is not production.

Operation: `macro-briefing-economic-backdrop-20260907-sol-001`.
Source owner: Sol, under the current live Chairman instruction to finish the US Macro Briefing upgrade.
Protected procedural pin: Mastermind `ef02058ba9356808e41937dab054f00043f89c1e`, Skillpack 1.0.1/bootstrap 1.
Pickup base: Macro `3405309de9adf19c3fc7115a7a06a4b327772c92`.

## The capability
A reader sees Growth, Inflation and Financial conditions without opening three dialogs.
Each card carries the existing workspace state, one plain-language qualification,
at most two dated facts, and a real investigation link. The homepage reads existing
sealed workspace publications; it creates no model, score, producer or state store.

This is the second visible vertical in the approved `BRIEFING_UPGRADE_2026-09-06.md`
direction. The compact public risk-context repair remains the independent PR #6685,
head `2d5133eacf8d88b74ad9f201eaeb3917f3fba817`. Do not restore its rejected large panel
or combine its unresolved release with a claim that this row is already live.

## Single-owner architecture
`lib/macro_economic_backdrop.py` selects three entries from the existing suite registry,
uses its manifest/body/schema/digest reader, and consumes the existing shared view.
State labels pass through the owner unchanged. No copy of Macro Command P2's state-word
map is created. The small contradiction sentences describe existing typed owner metadata;
they are neither a model synthesis nor a trading instruction.

The normal `scripts/build_site.py` adds this view once to its existing VM, before its
optional developer cache. `templates/dashboard.html.j2` mounts the row only in macro
mode, after the market ticker. The existing order-1 rank plus DOM placement keeps it
before order-2 action/event cards. No controller, runtime stylesheet or navigation plane
is added. Three investigation URLs come from the existing suite page registry.

## Clock, null and failure behaviour
Only CURRENT/LATE_WITHIN_TOLERANCE owner health is usable. Required-leg failures,
stale/failed/rights-blocked source state, absent calculation cut and publication
integrity failures produce an explicit unavailable card, never a retained cheerful
state. The other two topics remain usable; investigation links stay present.

Facts carry their own exact reference periods. The footer shows the calculation cut,
not observation time, and never uses page build time to freshen a read. GDPNow is
explicitly an estimate; CPI facts are released headline/core year-on-year values,
not next-release forecasts. Zero, negative and absent values are distinct. Successful
cards omit redundant healthy-status captions; source health remains in the receipt
metadata. Stale/unavailable reasons remain visible. No unmeasured monthly change is
inferred from a same-cut or missing predecessor.

## Design acceptance versus production proof
The parent read both real #6685 baseline themes. The designer's first CSS had an
actual parser bug in a header comment; it produced zero rules and a 2193px row. The
comment-only fix produces 54 real Chromium rules. This failure is retained, not erased.

Final prototype V4 has twelve passing whole-page cases: 1440/768/390 × dark/light ×
English/Chinese. It verifies actual placement after the ticker and before action cards,
44px investigation targets, native keyboard receipts and no document overflow or
JavaScript page errors. Desktop row height is 226.8–237.4px, inside the 240px budget.
Four additional stale-growth fixtures verify that one unavailable topic does not erase
its neighbours. Local websocket/resource 404 console diagnostics are not called absent.

These previews are clearly classified as DESIGN_COMPOSITION, built over the frozen
#6685 page; they are not manually spliced product outputs. Production source uses the
normal builder and template. Canonical-source browser results are a separate receipt.
The full cached-data builder was invoked with its existing RENDER_NO_DRIP sentinel and
without provider API keys; unrelated public GDELT/RSS reads still occurred. This is
not a zero-network or full-estate-health claim.

## Canonical-page proof and current continuation
The real macro-target observer completed at 2026-09-07T01:38:43Z, with all five
consumed source hashes unchanged. Target SHA256:
`d8e8a1f6d92fe03cf22b6a0c8f0101e9ab65591e826fcbf73f75ac569ffc3df8`.
The existing capture tool subsequently produced eight actual anonymous dark/light,
EN/ZH, desktop/mobile cells. Twelve additional actual-page cases prove placement,
44px investigation links, owner facts/dates, native keyboard receipt opening and
three real topic-to-workspace journeys. This is no longer prototype-only proof.
The first journey probe read a destination before load had completed; an explicit
load/visible-headline wait fixed the probe, with no product or source mutation.

The canonical candidate is main-based and does not include the separate #6685
compact-risk change. Its combined-design preview remains distinct. Do not commit
a main-based generated macro.html over #6685; normal publication must compose
accepted source together. Actual rendered target bytes are retained in the owned
audit archive; source/screenshot/digest evidence is in this PR.

On continuation, Mastermind protected `6ce1e0104f43657b3d5fc333d848025e28a8173a`
was atomically loaded. Macro source comparison through `4bb1914ba6a4c4be4f00815c22ec3c49281f07d3`
found no governing law, dashboard, builder, shared view or registry movement.
The only CI movement adds release-hold-text tests to a disjoint existing job.
The current shared hub owner remains #6873/#6930/#6937, not this source operation.
