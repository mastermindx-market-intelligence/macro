PICKUP_ACK: `macro-economic-backdrop-6974-independent-review-20260907-sol-001` — read-only review complete, single return, no watcher/successor.

STATUS: PARTIAL

RESULT:
Commission-form verdict: **REQUEST_CHANGES**. The capability is architecturally sound — it is a genuine consumer of the sealed workspace publication (no new producer, classifier, score or mapper), it fails closed on every refusal path I could construct against the code, dark and light are separately art-directed in a governed Jinja CSS partial (not a runtime stylesheet), and the mount is macro-only and correctly placed. What blocks a clean PASS is the **temporal-honesty layer**: the card's own receipt makes a claim about what the fact dates mean that is false on two of the three cards, and an unusable fact date silently disappears with no test, no guard and no user-visible signal. Three MAJOR findings, six NONBLOCKING. No BLOCKER.

**MAJOR-1 — the receipt's date semantics are false on the Growth and Financial-conditions cards.**
`lib/macro_economic_backdrop.py:131-133` promises: *"Fact dates identify their reference periods; the footer is the calculation cut."* `lib/macro_economic_backdrop.py:76` sources that date from the metric's `reference_period`. But `engine/market_os/macro_workspaces/growth.py:865-870` fills `reference_period` for `gdpnow_growth`/`wei_growth` with `asof` (`growth.py:300`), which is the *same* value the composer writes to `generation.calculation_as_of` (`growth.py:491`). Concrete failing journey, visible in the committed canonical evidence: `mockups/evidence/macro-economic-backdrop/canonical-row-1440-dark-en.png` shows `GDPNOW ESTIMATE · 2026-09-04` and, in the same card's footer, `Calculated 2026-09-04` — the receipt asserts two different clocks where there is one artifact as-of. `financial_conditions.py:844`/`:828` do the same with a vintage as-of (`vintages.us10y.asof`, `vintages.hy_oas.asof` → `· 2026-09-03`). Only Inflation carries a true reference period (`inflation.py:728-731`, `observation_period` → `2026-07`), and the row renders all three identically, so the reader cannot tell a July CPI print from a Sept-4 nowcast timestamp. This inverts the commission's own boundary ("a month's reference period is not an observation timestamp; calculation … times are not source freshness") and it is the *accessible receipt*, the element the mission requires to be truthful. Additionally, GDPNow's real reference period is a quarter; a daily date under a "% annualized" figure invites "the economy grew 4.75% on Sept 4". **Smallest repair:** either state the weaker true claim in the receipt ("fact dates are the owner's dated reference for that reading — a reference period where the source has one, otherwise the source as-of"), or carry a per-fact date kind from the owner metric and prefix accordingly; add a test asserting the Growth card does not present `calculation_as_of` as a reference period.

**MAJOR-2 — an unusable fact period disappears silently instead of conveying temporal uncertainty (review question 2, confirmed).**
`lib/macro_economic_backdrop.py:39-50` admits only `YYYY-MM`/`YYYY-MM-DD`; `:76` returns `None` for anything else; `templates/_macro_economic_backdrop.html.j2:80` then renders nothing. Failing input: any of `reference_period = null`, `"2026-09-05T13:00:00Z"`, `"2026Q3"`. Result: a bare number with no date, sitting beside a sibling fact that *has* a date, inside a card whose receipt says fact dates identify their periods — the missing clock reads as "no clock needed", i.e. current. This is not hypothetical: the contract types `reference_period` as free `["string","null"]` (`contracts/market_os/macro_workspace_snapshot.v1.schema.json:525`) and the two non-inflation composers fill it from owner `asof` fields whose format nothing pins, so a producer switching to a timestamp silently de-dates four of six facts. No test covers it — `tests/test_macro_economic_backdrop.py:117` asserts periods only for Inflation. **Smallest repair:** when `usable` is true but `_period()` is `None`, do not emit the value as a plain dated-row fact — either degrade the fact to a disclosed "date unavailable" or suppress it like the unit-mismatch path; add a parametrized test over `None`/ISO-datetime/`"2026Q3"`.

**MAJOR-3 — the "accessible receipt" control is below minimum target size on mobile, and the evidence never measures it.**
`templates/_macro_economic_backdrop.css.j2:150-157` gives `.ebd-rc > summary` no min-height or padding: 11px text at line-height 1.45 ≈ 16px tall. The sibling control in the same footer is deliberately sized — `.ebd-go { min-height: 44px }` (`:165`, reasserted `:185`). So on the 390 build the honesty disclosure is a ~16px tap target (below WCAG 2.2 SC 2.5.8's 24×24), while the "Investigate" link next to it is 44px. `canonical-interaction.json` records `keyboard_receipt: true` for all twelve cases but no summary geometry, so "accessible receipt" is asserted rather than proven, and `IMPLEMENTATION.md:70-72` claims only the 44px *investigation* targets — correctly, which makes the omission visible. **Smallest repair:** `min-height: 32-44px` + block padding on `.ebd-rc > summary` inside the ≤899px block; add the measured summary box to the interaction receipt.

**NONBLOCKING**
1. `canonical-row-390-dark-en.png` / `canonical-row-390-light-zh.png`: the fact date breaks mid-token (`GDPNOW ESTIMATE · 2026-` / `09-04`; `10 年期实际收益率 · 2026-09-` / `03`), and the wrapped label desynchronizes the two fact columns' value baselines on the Financial-conditions card. Fix: `white-space: nowrap` on `.ebd-fact-p` (keeping the `·` with the date).
2. `lib/macro_economic_backdrop.py:104-107`: the refusal reason is the failing leg's *freshness*. A leg failing on `status` (e.g. `ABSENT` with `freshness: CURRENT`) records `source.reason = "CURRENT"` — a refusal reason that reads as health — and the stale-specific copy at `:54` fires only when the token happens to be `STALE_SOURCE`. Internal only (never rendered), but it will mislead the next consumer. Derive the reason from the failing condition.
3. `scripts/build_site.py:6785` runs the read unconditionally, including `mode == 'stocks'` renders where `templates/dashboard.html.j2:11400` never emits the row; each card also builds the *full* `macro_suite_view.build_view` for its workspace to read one `state_label`. Bounded, but it is 3 JSON parses + 3 whole page view-models on a render-budget-governed path.
4. `final-guards.json` runs `check_design_system.py --mode enforce-added`, `check_ui_visual_evidence.py`, `agentos.py validate`, `git diff --cached --check` — but **not** `scripts/check_runtime_style_injection.py`, one of the three guards house law names for this class of change. It would pass (the CSS is a Jinja partial included in the existing `<style>` at `dashboard.html.j2:2211`), so this is a receipt gap, not a defect.
5. Test coverage is asymmetric: `facts` is always length 2 whether or not a value resolved, so `assert len(card["facts"]) == 2` (`tests/…:61`) and `assert all(c["facts"] …)` (`:77`, `:194`) pass even if a metric_id or unit drifts and all four Growth/Financial facts silently render "Not provided". Only Inflation is pinned (`:117`, `:131`). One added assertion — each happy-path fact's `value.en != "Not provided"` — would pin metric_id + unit + freshness for all six.
6. `lib/macro_economic_backdrop.py:67-74` renders one string, "Not provided", for *no print*, *stale print*, *simulated print* and *unit mismatch*. Never optimistic, so no honesty break, but it discards the discrimination the commission asks to preserve.

**Falsification attempts that failed (no finding):** SIMULATED / HISTORICAL_AS_KNOWN cannot reach a headline — `_USABLE` (`:35`) excludes them and `:106` gates `availability.state`, `worst_freshness` *and* every required leg (the schema makes `freshness` a required non-null enum, `schema:410,415`, so no leg can slip through as a falsy value); duplicate metric ids refuse (`:119-120`, test `:183`); tampered body, missing manifest, and null `calculation_as_of` all refuse with tests (`:95`, `:106`, `:156`); zero/negative/null stay distinct (`:121`); build order is correct (`config/dag.yml:631-635` runs the workspace producer before `build_site`); CI ownership is real (`.github/ci/legacy-jobs.yml:13820-13840` owns the module, both templates and the test, and runs them).

EVIDENCE:
- Head under review: `d50deee6479e0820587456b0d2b5505c1f61af19` (base `3405309d…`), sole workspace `/Volumes/Mastermind/agent-workspaces/claude/14851c4656838a3b/sol-macro-economic-backdrop-20260907-afc3f1512bdbd93e`. Read-only: Read/Grep/Glob only; no shell, no git, no network, no write.
- Source read: `lib/macro_economic_backdrop.py` (full), `templates/_macro_economic_backdrop.html.j2` (full), `templates/_macro_economic_backdrop.css.j2` (full), `tests/test_macro_economic_backdrop.py` (full), `scripts/build_macro_suite_pages.py:200-380`, `engine/market_os/macro_workspaces/contract.py:1-151`, `contracts/market_os/macro_workspace_snapshot.v1.schema.json` ($defs + availability/headline/metric blocks), `growth.py`/`inflation.py`/`financial_conditions.py` metric+availability sections, `scripts/build_site.py:6785`, `templates/dashboard.html.j2:2211,11399-11400`, `.github/ci/legacy-jobs.yml:13817-13840`, `config/dag.yml:631-635`.
- Evidence read: `EVIDENCE.yml`, `canonical/manifest.json`, `canonical-interaction.json`, `canonical-target-receipt.json`, `final-owner-tests.json`, `final-guards.json`, `IMPLEMENTATION.md` (claim lines).
- Screenshot adjudication, **dark** (`canonical-row-1440-dark-en.png`, `canonical-row-390-dark-en.png`): command-centre reading holds — graphite `--panel` on the deepened canvas, depth carried by the luminance gap, fact strip separated by a rule alone, spine a downward fade; hierarchy is correct (quiet uppercase topic → state sentence as headline → plain-word caveat → two facts → one quiet clock + receipt + action); the amber caveat spine on Growth/Inflation is legible at 390 and the neutral spine on Financial conditions reads as "no tension", so tone is health-semantic, not directional. Type scale and density are within budget; nothing is truncated or ellipsised.
- Screenshot adjudication, **light** (`canonical-row-1440-light-en.png`, `canonical-row-390-light-zh.png`): judged as its own design, not a token swap — white `--panel` on cool paper with the edge doing the work (raised hairline + two-stop `--ms` shadow), the fact strip promoted to a drawn `--panel2` band, the spine solid rather than faded, and the unavailable card specified to recess to `--panel2` (CSS `:96-97`, not exercised by these captures). Four mechanisms differ on purpose and are documented at `_macro_economic_backdrop.css.j2:13-27`; hierarchy, order, budgets and copy are identical across themes. Light passes as a design.
- EN/ZH parity: ZH at 390 light keeps the same three-card order, the same state-first hierarchy and the same receipt/action pair, with the ZH typographic exceptions handled (`:55,104-105,110,129-130`). The one ZH-specific defect is the wrapped `10 年期实际收益率 · 2026-09-` label and the resulting column-baseline desync (NONBLOCKING #1).

GAPS:
- I executed no tests and visited no production surface. `final-owner-tests.json` carries only `command` + `exit: 0` + source hashes; the "206 passing / no skips" count lives in `final-owner-tests.log`, which I did not open and could not have re-run — that claim is **unverified by me**.
- I cannot compute hashes, so `source_unchanged: true` and the five `source_hashes` in `canonical-target-receipt.json` / `final-owner-tests.json` are unverified against the frozen head. This matters because `canonical/manifest.json` records `target.resolved_sha_or_none: 3405309d…` — the **base** commit — so the census artifacts are git-bound to the base and the source-hash bridge is the only tie to `d50deee6`. Parent must verify that bridge.
- The guard receipts reference `/tmp/sol-economic-final-diff-20260907-continuation.diff`, which is not committed; I cannot confirm the guarded diff equals the frozen head's diff.
- MAJOR-2's live severity depends on the production format of `regime_latest.asof` and the `vintages.*.asof` fields. The fixtures use `"2026-09-03"` (date), so the row is dated today; if any producer emits a timestamp the facts go undated silently. I could not observe production data.
- Not assessed (explicitly out of scope per the commission): the broader briefing redesign, the old giant panel, #6685/#6969/#6873 chains, and any release/merge disposition.

DEVIATIONS:
- Output labels follow the harness contract (`STATUS: PASS | PARTIAL | BLOCKED | FAIL`); the commission's verdict vocabulary is carried in RESULT as **REQUEST_CHANGES**. `PARTIAL` is the harness equivalent — material findings returned for adjudication, no PASS granted.
- No GitHub review, no source edit, no shell, no network, no merge action taken. PR remains Draft/Hold; all follow-up execution is the parent's.
