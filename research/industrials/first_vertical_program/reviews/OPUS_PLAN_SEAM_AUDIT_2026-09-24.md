# OPUS ADVERSARIAL SEAM AUDIT — GMI Industrials nine-task plan (T01-T09)
MODE: READ_ONLY. Route: review. Date: 2026-09-24.
Baseline main: worktree /Users/chriswong/Documents/Cluade/Macro Dashboard/.claude/worktrees/gmi-industrials-fable-handoff-0cb6a4 @ 10c0d79e2992 (SPARSE: data/ site/ mockups/ verify_shots/ absent).
B branch: origin/claude/ssd-semiconductor-theme-intelligence-b-impl-988406e131fd90b9 @ 70fde3c79956 (unmerged).
Artifacts attacked: handoff/2026-09-24-industrials-shared-foundation-increment-implementation.md + handoff/2026-09-24-industrials-handoff-plan-addendum.md
Findings F1..Fn below; each: severity, evidence path:line, task tag, recommended seat ruling, confidence.

## F1 — BLOCKER (evidence integrity, all tasks) — the audit/plan baseline is not `origin/main`
`git rev-parse HEAD origin/main` in this worktree: HEAD `8d02b184ddce`, `origin/main` `d9380e7c52e8` (`fix(macro): move volatility diagnostics into Risk Detail (#7859)`), and `git merge-base --is-ancestor HEAD origin/main` → `HEAD_NOT_ANCESTOR`. HEAD also MOVED mid-audit (`6586891a28bd` at call 1 → `8d02b184ddce` at call 2) because the nightly data-drip lane commits into this branch (`debt-maturity-drip: persist cache + statements … [skip ci]`). The commission's "the worktree IS origin/main 10c0d79e2992" is false on three counts (sha, ancestry, stability).
**Ruling:** every packet citation must be re-resolved against `origin/main` by sha, not "the worktree"; code citations in this report are verified code-path-identical to `origin/main` (see F1b). Never cite this tree's HEAD as main in a build packet.
Confidence HIGH. Tasks T01–T09.

## F2 — MAJOR (T02) — `IssuerProfile` has no fiscal-calendar or identity field; the tolerance lives in the refresh script
`engine/company_intelligence/issuer_profiles.py:155-178` — `@dataclass(frozen=True) class IssuerProfile` has EXACTLY four fields: `ticker: str` (174), `extract_release_facts` (175), `extract_transcript_claims` (176), `extract_guidance = _no_guidance` (177); docstring line 157: "Three callables; nothing here inspects `ticker` to change behavior." Fiscal convention lives on `IssuerIdentity` (`issuer_for_ticker` docstring, 1280-1288: "needs the CIK and `fiscal_year_end_month` before an event's fiscal period can be derived"), and the matching tolerance exists only on B as `+def _fiscal_period_tolerance_days(issuer: IssuerIdentity) -> int:` in `scripts/refresh_event_workspaces.py`.
**Ruling:** T02's "applicable fiscal-calendar declaration" is an `expo_issuer()/pnr_issuer()` → `IssuerIdentity(fiscal_year_end_month=…)` obligation plus the refresh-script tolerance; do NOT add fields to `IssuerProfile` and do NOT let a profile inspect its ticker (C1 invariant stated in the dataclass docstring).
Confidence HIGH. Task T02.

## F3 — MAJOR (T02) — there is no `register()` API: enrollment is two hard-coded dicts + an AAPL if-branch, and #7905 is already editing them
`issuer_profiles.py:1272-1277` `_HOMEBUILDER_PROFILE_FACTORIES: dict[str, Callable[[], IssuerProfile]] = {"DHI": dhi_profile, "PHM": phm_profile, "KBH": kbh_profile, "TOL": tol_profile}`; the sibling `_HOMEBUILDER_ISSUER_FACTORIES` is read at `1289`; `profile_for_ticker` (1293-1305) special-cases `"AAPL"` then falls to that dict; `__all__` starts `1307`. `gh pr diff 7905 --name-only` lists `engine/company_intelligence/issuer_profiles.py` and a NEW `engine/company_intelligence/pg_profile.py` — i.e. the accepted idiom for a new sector is a sibling `<issuer>_profile.py` module plus a lookup-table edit, and #7905 is editing the exact lines T02 must edit.
**Ruling:** T02 adds a separate contiguous `_INDUSTRIALS_ISSUER_FACTORIES` / `_INDUSTRIALS_PROFILE_FACTORIES` block and ONE lookup merge line each, never re-orders or rewrites the homebuilder dicts; and T02 rebases AFTER #7905 merges (serialization order in F14). B does not touch this file (`git diff origin/main <B> -- engine/company_intelligence/issuer_profiles.py` → empty), so the only collision is #7905.
Confidence HIGH. Task T02.

## F4 — MAJOR (T03, contradicts A14-06) — the incumbent fact constructor hard-codes a public-primary rights assertion
`issuer_profiles.py:304` `def _release_span_payload(*, document_id, bound: BoundRelease, receipt: SpanReceipt)` → line `314: rights_profile="rp_public_primary_v1",`; every fact from `_fact_present(...)` (319-343, which calls it at 341) inherits that constant. `engine/company_intelligence/documents.py:89` `RIGHTS_STATES = {"public_primary", "licensed", "internal_only", "unknown"}` and `SourceDocument.rights_profile: str = "rp_unknown_v1"` (167) / `rights_state: str = "unknown"` (168) default to UNKNOWN — the release path is the only place that asserts public-primary unconditionally.
**Ruling:** T03 may reuse `_literal_receipt`/`_find_table_after_heading`/`_find_row_by_label` verbatim, but reuse of `_fact_present` silently asserts the very rights claim A14-06 holds open for issuer-authored filing/exhibit text. T03 must either request a bounded `rights_profile` keyword from the owner of line 314 or hold emission of filing-body facts; it may not "inherit" the constant as evidence of clearance, and no verbatim filing body may be committed as a fixture.
Confidence HIGH. Tasks T03, T01(G-holds), T07.

## F5 — MAJOR (T03) — the typed-absence vocabulary the plan demands does not exist
`documents.py:72-87` `ABSENCE_REASONS: frozenset[str]` is CLOSED with 14 members (`no_source_document`, `no_transcript`, `no_primary_release`, `no_span_addressable_evidence`, `document_bytes_not_held`, `scanned_image_no_text_layer`, `unjoinable_filing_identity`, `speaker_unresolvable`, `slide_family_discontinued`, `superseded_by_duplicate`, `missing_basis`, `missing_units`, `missing_period`, `missing_source`). None expresses the plan's "ambiguous duplicate cells" or "release-only edition has no cash detail". Meanwhile `_fact_absent(*, fact_id, event_id, metric, detail: str, document_id)` (`issuer_profiles.py:345`) takes FREE-TEXT `detail`, so an Industrials-local reason string would pass silently and unvalidated.
**Ruling:** T03 maps ambiguous duplicates to the existing `no_span_addressable_evidence` (+ `missing_basis`/`missing_period` for context gaps) and records the mapping in the packet; minting an Industrials-local reason word is forbidden, and widening `ABSENCE_REASONS` is a bounded request to `documents.py`'s owner, serialized like any shared write.
Confidence HIGH. Task T03.

## F6 — MAJOR (T03) — no 10-Q/filing path exists; the filing edition must ride the release binder + document revision chain
`engine/earnings_release/binding.py:244-256` `def bind_release_document(*, cik, accession, body: str, form: str = "8-K", filing_date="", acceptance_datetime="", report_date="", exhibit_url=None, content_type=None, registry=None) -> BoundRelease` — docstring 257: "Normalize one Exhibit 99.1 body and bind it to its filing." `form` is NOT validated against any set (`266: form_text = str(form or "").strip() or "8-K"`). `grep -rn "10-Q|10Q|filing_key"` over `engine/earnings_release engine/company_intelligence` returns only `FilingKey` plumbing — no 10-Q code path anywhere. The incumbent edition mechanism is `SourceDocument.document_kind` (`documents.py:156`), `supersedes_document_id`/`superseded_by_document_id` (165-166) and `class DocumentRevisionChain` (236-245); the incumbent precedent for admitting a NEW document family is B's refresh-script work (`+def _results_six_k_manifest_admits`, `+def _resolve_exhibit_for_row`), not a second binder.
**Ruling:** T03's filing-enriched edition = `bind_release_document(form="10-Q", …)` + a distinct `document_kind` + `DocumentRevisionChain` for preliminary/final/filing predecessors. Do not write a second binder, do not mint one event per file, and note in the packet that `form` is unvalidated so the plan's "later filing is enrichment unless the source establishes a correction" is entirely caller-side logic.
Confidence MED-HIGH. Task T03.

## F7 — MAJOR (T04) — `qualify_operands`/`derive_result_cash`/`comparison_receipt` exist NOWHERE, and no producer of a comparison receipt is specified
`grep -rn "comparison_receipt\|def derive_\|def qualify_" engine` → zero `comparison_receipt` hits; every `derive_*` hit is unrelated (`engine/bar_derive.py:116`, `engine/options_signal_campaign.py:629`, `engine/intelligence_registry.py:432`, …); nearest `qualify_` is `engine/us_prophet_w3.py:757 qualify_paired_candidates`. The forensics house idiom is present and reusable: `engine/fundamental_forensics/detectors.py:24 _decimal(observation) -> Decimal`, `:28 _growth(current: Decimal, prior: Decimal) -> Decimal | None`, `:79 _finding(...)`, `:148 _not_evaluable(...)`, `:374 evaluate_detectors(...)`; `Decimal` is used across 10+ modules in that package.
**Ruling:** T04 writes `industrials_result_cash.py` FRESH (no import of private `_growth`/`_decimal`), but mirrors `_not_evaluable`'s typed-limitation shape and the string→Decimal convention, and declares a module `VERSION` (none of `detectors.py`/`context_projection.py` exposes one — so the plan's "formula version" is new, not inherited). Separately: the plan gives no PRODUCTION producer for `comparison_receipt` — T03 emits `event_fact.v1` + `SpanReceipt`, and `comparison(purpose, cells)` is a test helper (§4). T04's packet must name the producer (T05 selection output or an explicit T04-local constructor) or the module is only ever exercised by fixtures.
Confidence HIGH. Tasks T04, T03, T05.

## F8 — MAJOR (T02 acceptance commands) — B's discovery seam is a one-line tuple and an injected-profile parameter, not a `run_refresh` API
B adds (diff `origin/main`→B, `scripts/refresh_event_workspaces.py`, +313/-30): `+DISCOVERY_TICKERS: tuple[str, ...] = tuple(HOMEBUILDER_TICKERS) + ("TSM", "ON")` with the comment `+# keeps its value/meaning (C1) — ``DISCOVERY_TICKERS`` is built ON TOP of`, plus `+    profile: IssuerProfile | None = None,` / `+        profile = profile_for_ticker(ticker)` / `+    if profile is None:` and `+        issuer = issuer_for_ticker(ticker)` (test seam T05b, "a missing profile is REFUSED (fail closed)"). The plan's T01 harness contract `run_refresh(changes, fail_sources=())`, `members()`, `get(slug)`, `publish(changes)`, `read_count`, `client(entitled)` matches nothing in the incumbent refresh API.
**Ruling:** T02 extends B's single `DISCOVERY_TICKERS` expression with `("EXPO", "PNR")` (a one-line textual collision with B → rebase on B's merged form, never edit B's branch), and T01's harness WRAPS B's `issuer=`/`profile=` injection seam instead of inventing a refresh API; `run_refresh` must be documented as a test-only façade over the real entry point named in the packet.
Confidence HIGH. Tasks T01, T02.

## F1b — verification of F1's blast radius (code paths cited here ARE main-identical)
`git diff --name-only origin/main HEAD -- engine scripts tests contracts .github templates app` → exactly six files: `.github/ci/legacy-jobs.yml`, `engine/sector_intelligence/finance_overlap.py`, `templates/dashboard.html.j2`, `tests/test_finance_overlap.py`, `tests/test_ud_b2_w1_vw_fold.py`, `tests/test_vsb_surface.py`. None of `issuer_profiles.py`, `event_workspace.py`, `documents.py`, `identity.py`, `events.py`, `binding.py`, `receipts.py`, `fundamental_forensics/*`, `refresh_event_workspaces.py`, `app/earnings.py`, `private_publication.py`, `tests/test_ci_pack.py` drifts, so every line number in this report is valid for `origin/main`. **`.github/ci/legacy-jobs.yml` DOES drift (`6 insertions, 21 deletions`)** — all CI citations below were taken from `git show origin/main:.github/ci/legacy-jobs.yml`, and a T04 packet that edits the worktree copy would revert 21 lines of main.
Confidence HIGH. Tasks T04 (CI), all.

## F9 — BLOCKER (T06/T08) — the "accepted" UI consume-targets exist on NEITHER ref, and the B ref given for this audit is not #7870's head
`git ls-tree -r --name-only origin/main -- <p>` and the same at `<B>`: `app/theme_research.py` → ABSENT on main AND ABSENT at B; `templates/_basket_intelligence_mounts.html.j2` → ABSENT on main AND ABSENT at B. But `gh pr diff 7870 --name-only` lists `templates/_basket_intelligence_mounts.html.j2` AND `tests/test_basket_intelligence_mounts.py`. So #7870's live head carries the file the audit's B ref does not — three distinct "B" identities are in play: the addendum's R14-04 head `b256aa6a756afa…`, the commission's `70fde3c79956`, and #7870's current head.
**Ruling:** every Industrials packet pins #7870 by **exact head sha at consumption time**, never by branch name or by "the B foundation"; A14-02's consume-target (`_basket_intelligence_mounts.html.j2`) is unmerged and not present on the ref the plan was audited against, so T08 is `BLOCKED-ON-#7870-MERGE` and no T08 build lane may be dispatched. `app/theme_research.py` exists nowhere at all — plan §2 lists it as "accepted"; it is not.
Confidence HIGH. Tasks T06, T08, T07.

## F10 — BLOCKER (T06) — the shared research contract is sector-pinned and closed; Industrials cannot emit into it
B's `contracts/market_ontology/semiconductor_theme_research.v1.schema.json`: `"additionalProperties": false`, `"schema": {"const": "semiconductor_theme_research.v1"}`, `"definition_version": {"const": "2026-09-24.1"}`, and `required` includes `industrial_views`, `economics`, `expectations`, `authorized_coverage`, `authority`. The module pins the same (`+SCHEMA_ID = "semiconductor_theme_research.v1"`, `+DEFINITION_VERSION = "2026-09-24.1"`, `+AUTHORITY: dict[str, bool] = {"can_rank": False, …}` in `engine/market_ontology/semiconductor_theme_research.py`). The ACCEPTED sibling precedent is #7891 (Technology ex-Semis), which minted its OWN `contracts/market_ontology/technology_economic_change.v1.schema.json` + `engine/market_ontology/technology_economic_change.py` rather than reusing the semiconductor contract.
**Ruling:** T06 mints `contracts/market_ontology/industrials_theme_research.v1.schema.json` + `engine/market_ontology/industrials_theme_research.py` on #7891's accepted per-sector pattern; the plan's "thin domain adapter into the accepted shared component" is only legal for the **transport/route discriminator**, never for the response schema. Do not request a sector-neutral rewrite of a const-pinned v1.
Confidence HIGH. Tasks T06, T08.

## F11 — MAJOR (T06) — "industrial_*" is already SEMICONDUCTOR vocabulary on B
B's module defines `+def _industrial_row(assertion, selection, …)`, `+_CAPACITY_OWNER_FIELDS = frozenset({…})`, `+def _physical_fingerprint(row)`, `+_MAX_GRAPH_NODES = 40`, and the schema requires `industrial_views` — all meaning semiconductor capacity/configuration/stage physical views, not the Industrials SECTOR.
**Ruling:** the Industrials adapter must NOT use `industrial_views`/`_industrial_row`/`industrials_*` view keys; use `sector_views` or `operating_views`. A cross-sector reader that sees `industrial_views` in an Industrials payload will read semiconductor semantics.
Confidence HIGH. Task T06.

## F12 — MAJOR (T05) — `guidance_history.py` exists ONLY at B; its two tokens are already implemented there, contradicting A14-04's "pending"
`ls engine/company_intelligence/guidance_history.py` → "No such file or directory"; `git ls-tree origin/main` → ABSENT_ON_MAIN; at B → present. Its API (diff `origin/main`→B): `+SCHEMA = "management_sequence_assessment.v1"`, `+class GuidanceHistoryError(ValueError)`, `+def assess_management_sequence(`, `+def _classify_prior_vs_actual(`, `+def _derivation_status(`, and `+            limitations.append(f"definition_unqualified:{field}")`. `git diff origin/main <B> | grep -c management_sequence_missing` → **4**.
**Ruling:** A14-04's "treat `definition_unqualified:<field>` as a pending implementation result" is factually stale — it is implemented at B and merely unmerged. T05 therefore (a) may NOT `import engine.company_intelligence.guidance_history` until #7870 merges, (b) binds both token spellings through ONE module constant in `financial_dossier.py` so a spelling change costs one line, and (c) owns Industrials semantics in its own `assess_dossier_comparison(kind, inputs)` with `kind='management_sequence'` delegating only when the module is importable. The real acceptance name is `assess_management_sequence`, and the shared schema id is `management_sequence_assessment.v1` — use those exact strings in the packet.
Confidence HIGH. Tasks T05, T06.

## F13 — MAJOR (T04/CI) — the copied job recipe has three couplings a naive copy will miss
Donor at `origin/main:.github/ci/legacy-jobs.yml:13451` `prophet-us-b4-prereg-registration:` — the full shape is `if: ${{ false }}` / `gate: code` / `scope: exclusive` / `paths:` (the suite + every document it reads) / `timeout-minutes: 5` / `runs-on: ubuntu-latest` / `actions/checkout@v4` / `actions/setup-python@v5` py `3.12` / `run: pip install pytest` / `run: python -m pytest tests/test_b4_entry_policy_calibration_prereg.py -q`. Couplings: (1) `if: ${{ false }}` is mandatory — the file is a pack inventory read by `scripts/run_ci_pack.py`, not a GitHub-scheduled workflow; (2) `scope: exclusive` is asserted set-equal in `tests/test_ci_pack.py:3508 CURATED_EXCLUSIVE = {` … `:3765 assert declared == CURATED_EXCLUSIVE, sorted(declared ^ CURATED_EXCLUSIVE)` — a new exclusive job WITHOUT its `CURATED_EXCLUSIVE` entry reds ci-pack in the same PR; (3) `paths:` must list EVERY test file named in the `run:` line or `scripts/check_contract_delta.py` REFUSES on every later PR head (fleet law; also `scripts/run_ci_pack.py`, `scripts/audit_unrun_tests.py`). Deps: `pip install pytest` is right only for a stdlib/Decimal-pure suite — the pip lines in that file range from `pytest` alone to `jsonschema pytest pandas numpy pyyaml requests jinja2 pyarrow` (line 573); measure the import closure, and a dossier/contract suite that validates JSON Schema needs `jsonschema`.
**Ruling:** ONE new job `industrials-result-cash`, `gate: code`, `scope: exclusive`, `if: ${{ false }}`, appended as a contiguous block at the END of the job map, `paths:` = every test file in its run line + `engine/fundamental_forensics/industrials_result_cash.py` + `tests/industrials_result_cash_helpers.py` + `tests/fixtures/industrials_result_cash/**`, `pip install pytest` (add `jsonschema` only if the same job runs the T06 schema suite), and the `CURATED_EXCLUSIVE` entry in the SAME PR.
Confidence HIGH. Tasks T04, T06.

## F14 — MAJOR (T02 serialization) — `legacy-jobs.yml` is six-way contested and T02's stated acceptance command cannot run on main
`gh pr diff <n> --name-only` (filtered): **#7870** legacy-jobs.yml, semiconductor_theme_research schema+module, `engine/company_intelligence/guidance_history.py`, `scripts/refresh_event_workspaces.py`, `site/assets/{css,js}/theme-research.*`, `templates/_basket_intelligence_mounts.html.j2`, `tests/test_basket_intelligence_mounts.py`, `tests/test_issuer_profiles_a5a.py`, `tests/test_semiconductor_guidance_history.py`, `tests/test_semiconductor_native_identity.py`. **#7905** `.github/ci/legacy-jobs.yml`, `engine/company_intelligence/issuer_profiles.py`, `tests/test_ci_pack.py`. **#7881** legacy-jobs.yml. **#7891** legacy-jobs.yml + technology_economic_change (schema+module+test). **#7896** legacy-jobs.yml + tests/test_ci_pack.py. **#7900** legacy-jobs.yml. **#7887** none of the watched paths. Open Industrials PRs: **#7912** "agentos(industrials): first-vertical implementation checkpoint + wave-1 seat rulings", **#7789** "[RESEARCH/HANDOFF][HOLD] Industrials — Fable packet". Also `tests/test_semiconductor_earnings_intake.py` and `tests/test_semiconductor_guidance_history.py` are ABSENT_ON_MAIN (ls-tree), yet T02's checklist orders `Rerun … tests/test_semiconductor_earnings_intake.py, tests/test_semiconductor_guidance_history.py`.
**Ruling:** serialization order for T02 = **#7905 merges → #7870 merges → Industrials T02 branches off the resulting main**. Until #7870 merges, T02's rerun list is unsatisfiable and the packet must state that as a HELD gate, not as a step. `issuer_profiles.py` collides only with #7905; `refresh_event_workspaces.py`, `tests/test_issuer_profiles_a5a.py` collide only with #7870; `legacy-jobs.yml`/`tests/test_ci_pack.py` collide with five siblings → T04's CI PR rebases last and never reorders sibling blocks.
Confidence HIGH. Tasks T02, T04.

## F15 — MAJOR (T02) — `IssuerIdentity` cannot express EXPO's 52/53-week calendar; the exact date has exactly two legal homes
`engine/company_intelligence/identity.py:146-155` `class IssuerIdentity`: `company_id: str` (149), `display_name` (150), `fiscal_year_end_month: int` (151), `reporting_currency: str` (152), `listings: tuple[ListingAlias, ...]` (153), `issuer_kind: str = "general"` (154), `external_ids: Mapping[str, str]` (155); `__post_init__` line 158 rewrites `company_id` through `company_id_for_cik(self.company_id)` and 163-164 validates the month `1 <= month <= 12`. So the identity records a MONTH only. `engine/company_intelligence/events.py:147-159` `class FiscalPeriod`: `year`, `quarter: int | None`, `calendar_end: date | None` — and `documents.py:172 presented_fiscal_label: str | None`.
**Ruling:** T02's "stated July 3 period must not become June 30" is discharged by `FiscalPeriod.calendar_end` (exact date) + `SourceDocument.presented_fiscal_label` (as-printed label); the refresh tolerance is B's `_fiscal_period_tolerance_days(issuer)`. Add NO calendar field to `IssuerIdentity`; carry the CIK in `external_ids` and let `company_id_for_cik` mint `company_id` (which is exactly why "a CIK is not itself a Data OS security ID" is already true in code).
Confidence HIGH. Task T02.

## F16 — MAJOR (T03) — every extraction helper the plan reuses is module-PRIVATE and not exported
`issuer_profiles.py`: `_release_span_payload`:304, `_fact_present`:319, `_fact_absent`:345, `_literal_receipt`:361, `_find_paragraph_containing`:374, `_find_table_after_heading`:382, `_find_row_by_label`:392, `_nonblank_cells`:415, `_period_label`:419; `__all__` begins at 1307 and exports only public names (`DHI_CIK`, …). All four incumbent homebuilder extractors live INSIDE this module (`_dhi_extract_release_facts`:525, `_phm_…`:743, `_kbh_…`:920, `_tol_…`:1050) precisely so they can use those privates.
**Ruling:** plan §2's NEW `engine/company_intelligence/industrials_profiles.py` would import five underscore names across modules. Either (a) put `_expo_extract_release_facts`/`_pnr_extract_release_facts` + `expo_profile()/pnr_profile()` INSIDE `issuer_profiles.py` exactly as the four homebuilders are (accepts the #7905 textual collision, mitigated by a contiguous block at end of file), or (b) obtain the owner's promotion of the five helpers into a shared internal module in a separate PR first. Silently importing privates is forbidden; pick (a) unless the owner rules otherwise.
Confidence HIGH. Tasks T02, T03.

## F17 — MAJOR (T07, record-only) — the A14-01 shared POST route family exists nowhere; the incumbent private seam is small and already role-shaped
B's non-data code surface contains **zero `app/` files** (`git diff --name-status origin/main <B>` filtered: contracts/, engine/, site/assets/, templates/, tests/, .github/ only), and `app/theme_research.py` is ABSENT on both refs (F9). Incumbent: `app/earnings.py` (5,336 bytes) — `"Cache-Control": "private, no-store"`:24, `_private(response)`:38, `_private_error(`:42, `require_site_full_user(authorization: str | None = Header(default=None))`:59, `_build_store()`:70, `_current_manifest(store, *, force=False)`:83, `earnings_member_record(`:109 with `_user: dict = Depends(require_site_full_user)`:112, `earnings_member_record_private_not_found(`:146, `__all__ = ["require_site_full_user", "router"]`:157. `engine/earnings_narrative/private_publication.py`: `PrivateArtifact`:78, `PreparedPrivatePublication`:97, `validate_private_record(value, *, expected_slug=None)`:149, `_artifact(role: str, identity: str, body: bytes, *, maximum: int)`:212, `validate_private_manifest`:250, `prepare_private_publication(stage_dir)`:318, `_put_verified(store, *, key, body, maximum)`:467, `_pointer_for`:483, `validate_private_pointer`:494. `grep content_role` over both files → **zero hits**.
**Ruling:** A14-01 must be re-scoped: T07 either (i) extends the incumbent `app/earnings.py` router with one role discriminator carried by `_artifact(role=…)` + `validate_private_record`'s closed key set, or (ii) is HELD until the shared route owner actually ships `/api/themes/v1/research/{query,evidence}`. "Test through the actual shared route" is currently untestable — there is no route. `content_role` is NEW vocabulary, not an "accepted discriminator". A14-05's known-existing-object privacy test maps onto `earnings_member_record` vs `earnings_member_record_private_not_found` (109 vs 146) — the 404 route already exists, which is exactly why a 404 cannot be the privacy proof.
Confidence HIGH. Tasks T07, T01, T09.

## F18 — MINOR (T08, record-only) — the shared assets are hand-authored files inside a render-output, immutable-cached directory, and this tree cannot see them
B adds `site/assets/css/theme-research.css` and `site/assets/js/theme-research.js` while DELETING a hashed sibling `site/assets/css/384d3e15.css`. `site/` is on the Caddyfile `immutable` family (`assets/{css,landing}/*`) so a warm cache serves stale bytes until a render re-stamps `?v=`, and `site/` is absent from this sparse worktree (`config/sparse_worktree.json`).
**Ruling:** any T08 lane runs `python3 scripts/worktree_sparse.py add site` first, and must not hand-edit hashed artifacts; also flag for the T08 design review that `theme-research.js` must not carry a runtime stylesheet (house law: substantive presentation belongs in governed CSS) — unverified here, B's JS body was not read.
Confidence MED. Task T08.

## F19 — MINOR (T01) — the fixture/helper idiom exists only at B, and the plan's helper name diverges from it
B's `tests/semiconductor_research_helpers.py`: `FIXTURE_ROOT = Path(__file__).parent / 'fixtures' / 'semiconductor_theme_research'`, `def load_case(name: str) -> dict:`, `def load_bundle_case(name: str):`, and the refusal `if not isinstance(value, dict) or value.get('synthetic') is not True: raise ValueError('fixture must explicitly be synthetic')`; fixtures are `tests/fixtures/semiconductor_theme_research/{abf_substrate,cowos_variants,date_only_same_day,…}.json`. On `origin/main`, `grep -rl '"synthetic": true' tests/fixtures` → **zero hits**, so the convention is unmerged.
**Ruling:** plan §4's `case(name)` is renamed `load_case(name)` with B's explicit `synthetic is not True` refusal copied verbatim in spirit; fixtures under `tests/fixtures/industrials_result_cash/`. Do not depend on B's helper module (unmerged); duplicate the 6-line loader with a comment citing it.
Confidence HIGH. Task T01.

## F20 — MINOR (T04) — the forensics package has no `VERSION` convention to inherit
`grep -n "^def |^class |VERSION" engine/fundamental_forensics/detectors.py engine/fundamental_forensics/context_projection.py` returns no `VERSION` symbol in either file; the reusable idioms are `_decimal(observation) -> Decimal`:24, `_growth(current: Decimal, prior: Decimal) -> Decimal | None`:28, `_finding(`:79, `_not_evaluable(`:148, `_two_period_detector(`:174, `evaluate_detectors(`:374.
**Ruling:** T04 declares its own module-level `FORMULA_VERSION` (new, labelled new) and mirrors `_not_evaluable`'s typed-limitation return shape; it must NOT import `_growth`/`_decimal` (private, and `_growth` has no zero/negative-base refusal contract — the plan's `growth_pct` rule is stricter).
Confidence HIGH. Task T04.

---
## SEAM TABLE (task → owned files → reuse with signature+line → collisions → CI)

**T01 — dependency binding + fixtures.** Owns `tests/industrials_result_cash_helpers.py`, `tests/fixtures/industrials_result_cash/`, `tests/test_industrials_dependency_binding.py`, the `validate_delivery_inputs` stub in `engine/company_intelligence/financial_dossier.py` (NEW file, exists nowhere). Reuse: B's loader idiom `load_case(name)` / `synthetic is not True` refusal (`tests/semiconductor_research_helpers.py`, B-only, F19); harness wraps B's injection seam `profile: IssuerProfile | None = None` → `profile_for_ticker(ticker)` in `scripts/refresh_event_workspaces.py` (B-only, F8), NOT an invented `run_refresh` API. Collisions: none on main. CI: joins the T04 job's paths.

**T02 — issuer enrollment.** Owns `expo_profile()/pnr_profile()/expo_issuer()/pnr_issuer()` + the two extractors. Reuse: `IssuerProfile` `engine/company_intelligence/issuer_profiles.py:155-178` (4 fields only, F2); `_HOMEBUILDER_PROFILE_FACTORIES` dict `:1272-1277`; `issuer_for_ticker(ticker) -> IssuerIdentity | None` `:1280`; `profile_for_ticker(ticker) -> IssuerProfile | None` `:1293`; `IssuerIdentity` `engine/company_intelligence/identity.py:146-155` with `company_id_for_cik` at `:158`; `production_registry() -> IssuerRegistry` `engine/company_intelligence/event_workspace.py:180` (+ `apple_issuer`:156, `apple_registry`:176, `flagship_fiscal_period`:196); `FiscalPeriod.calendar_end` `engine/company_intelligence/events.py:159`; B's `DISCOVERY_TICKERS = tuple(HOMEBUILDER_TICKERS) + ("TSM","ON")` and `_fiscal_period_tolerance_days(issuer)`. Placement: prefer INSIDE `issuer_profiles.py` (F16), not a new module importing privates. Collisions: **#7905** (`issuer_profiles.py`), **#7870** (`refresh_event_workspaces.py`, `tests/test_issuer_profiles_a5a.py`, and the two semiconductor suites T02 is told to rerun — ABSENT_ON_MAIN). Order: 7905 → 7870 → T02.

**T03 — facts/documents.** Reuse: `bind_release_document(*, cik, accession, body, form="8-K", filing_date="", acceptance_datetime="", report_date="", exhibit_url=None, content_type=None, registry=None) -> BoundRelease` `engine/earnings_release/binding.py:244`; `BoundRelease` `:131-138`; `ReleaseRevision` `:80-90` (`filing_key`, `form`, `acceptance_datetime`, `report_date`, `document_id`); `receipt_for_literal(*, source, search_start, search_end, literal)` `engine/earnings_release/receipts.py:233`; `replay_receipt(receipt, *, source)` `:138`; `SpanReceipt` `:81-94`; `receipt_for_char_span(*, source, char_start, char_end)` `:191`; `SourceDocument` `engine/company_intelligence/documents.py:151-173`; `DocumentRevisionChain` `:236-245`; `text_span(*, document_id, document_version, segment_index, segment_text, start_byte, …)` `:373`; `SourceSpan` `:305-317`; `ABSENCE_REASONS` `:72-87` (closed, 14 members — F5); `RIGHTS_STATES` `:89`; `build_event_workspace(` `engine/company_intelligence/event_workspace_build.py:108` emitting `"schema": "event_fact.v1"` at `:274, :288, :324, :338`; private extractor helpers `issuer_profiles.py:304/319/345/361/382/392`. Blocked-by: F4 (rights constant at `:314` vs A14-06). Collisions: #7870 only via `refresh_event_workspaces.py`.

**T04 — signed derivations.** Owns NEW `engine/fundamental_forensics/industrials_result_cash.py` + `tests/test_industrials_result_cash.py`. Reuse (shape only, no private imports): `detectors.py:24/28/79/148/174/374`. `qualify_operands`/`derive_result_cash`/`comparison_receipt` exist **nowhere** (F7) — and the plan names no production producer for a comparison receipt. CI: new `industrials-result-cash` job per F13 (donor `origin/main:.github/ci/legacy-jobs.yml:13451`), `CURATED_EXCLUSIVE` entry `tests/test_ci_pack.py:3508` in the same PR. Collisions: `legacy-jobs.yml` (#7870/#7905/#7881/#7891/#7896/#7900) + worktree drift (F1b); `tests/test_ci_pack.py` (#7905/#7896).

**T05 — information time.** Owns `select_editions`/`assess_dossier_comparison` in `financial_dossier.py`. Consume-target `engine/company_intelligence/guidance_history.py` is **B-only**: `assess_management_sequence(`, `SCHEMA = "management_sequence_assessment.v1"`, `definition_unqualified:{field}`, `management_sequence_missing` (F12). Time fields available today: `SourceDocument.fetched_at/published_at/available_at` `documents.py:162-164`, `ReleaseRevision.acceptance_datetime/filing_date/report_date` `binding.py:86-87`, `supersedes_document_id`/`superseded_by_document_id` `documents.py:165-166`. Collisions: #7870 owns `guidance_history.py`.

**T06 — dossier + adapter.** Owns NEW `contracts/company_intelligence/financial_dossier.v1.schema.json`, `financial_dossier.py`, NEW `engine/market_ontology/industrials_theme_research.py` + its OWN `contracts/market_ontology/industrials_theme_research.v1.schema.json` per #7891's accepted precedent (F10); must avoid `industrial_views` (F11). Blocked-by: F9 (`app/theme_research.py` and `_basket_intelligence_mounts.html.j2` exist nowhere on the audited refs).

**T07–T09 (seams recorded only).** T07: incumbent private seam = `app/earnings.py:24/38/42/59/70/83/109/146/157` + `private_publication.py:78/97/149/212/250/318/467/483/494`; no `content_role` anywhere; A14-01's shared POST route family exists nowhere (F17). T08: `templates/_basket_intelligence_mounts.html.j2` + `tests/test_basket_intelligence_mounts.py` live only on #7870's head (F9); B's own mounts are `templates/state_of_themes.html.j2`, `templates/options.html.j2`, `templates/dashboard.html.j2` plus `site/assets/{css,js}/theme-research.*` (F18) — the plan/addendum never name `state_of_themes.html.j2`, which is where B actually mounted. T09: integration proof is unsatisfiable until #7870 and #7905 merge.

## OPEN PRs TOUCHING AN INDUSTRIALS-OWNED FILE
- **#7870** (DRAFT/HOLD, Semiconductor B): `legacy-jobs.yml`, `scripts/refresh_event_workspaces.py`, `engine/company_intelligence/guidance_history.py`, `engine/market_ontology/semiconductor_theme_research.py` + its contract, `templates/_basket_intelligence_mounts.html.j2`, `site/assets/{css,js}/theme-research.*`, `tests/test_issuer_profiles_a5a.py`, `tests/test_basket_intelligence_mounts.py`, `tests/test_semiconductor_{guidance_history,native_identity}.py`.
- **#7905** (Consumer CDV-1 T1): `engine/company_intelligence/issuer_profiles.py`, `.github/ci/legacy-jobs.yml`, `tests/test_ci_pack.py` (+ new `pg_profile.py`, `economic_observations.py`).
- **#7891** (Technology ex-Semis): `legacy-jobs.yml` + `engine/market_ontology/technology_economic_change.py` + contract (naming precedent).
- **#7881** (Energy), **#7896**, **#7900**: `.github/ci/legacy-jobs.yml` (+ `tests/test_ci_pack.py` for #7896).
- **#7887**: no watched-path overlap.
- Industrials-side: **#7912** (agentos checkpoint + wave-1 rulings), **#7789** (HOLD research packet), **#7886** (Wave1 review).

## GAPS (not audited within budget)
- `gh pr diff <n> -- <path>` is unsupported ("accepts at most 1 arg"), so **#7905's actual `issuer_profiles.py` hunks were not read** — the precise lines it edits in the factory dicts are unverified (F3/F14 assume the two dicts + `__all__`).
- EXPO CIK 0000851520 / PNR CIK 0000077360 are **not present anywhere** in `engine`, `config`, `contracts` (`grep -rn "851520|0000077360|77360"` → no hits) — unverifiable in this offline sparse tree; T02's G2 identity evidence stays a real gate.
- `engine/theme_graph/store.py` existence on main unverified; B ADDS `engine/theme_graph/{admission.py,curation_assertion.py}` and MODIFIES `rights.py`, so plan §2's "CONSUME ONLY shared foundations" list is partly unmerged too.
- `git show <B>:<path>` / `git cat-file -p <B>:<path>` returned the COMMIT object rather than blobs in this blobless promisor clone; all B content in this report was read through `git diff origin/main <B> -- <path>` instead (method note, not a finding).
- `theme-research.js` body not read → the runtime-stylesheet house-law check for T08 is unperformed (F18).
- T09 and the 56 requirement IDs were not enumerated against code.

## WORKING TREE
`git status --short` → empty (quoted in the return packet).
