# R-IND wave-1 rulings — GMI Industrials first vertical (Fable Meta-CEO seat c6467452)

Operation `gmi-industrials-fable-ceo-e2e-20260924-chairman-001`. Authority: Chairman delegation 2026-09-24 (deliberate delivery of the master packet) + Astra/Chairman base-ownership ruling 2026-09-24 ~07:50Z. Frozen plan blob `a5462dc7…`, addendum `a2416836…` (A14-01..07 supersede the plan on route/mount/comparison qualification). These rulings refine the plan where the code differs; they never widen it.

| Ruling | Decision | Why / evidence |
|---|---|---|
| R-IND-00 | Industrials mints NO shell, evidence, rights, route or mount vocabulary. T01–T06 extend Company Intelligence, Fundamental Forensics and Earnings owners only. T07–T09 wait for Semiconductor B's route family and `_basket_intelligence_mounts.html.j2` aggregator on `main`. | Astra ruling; #7870 head `70fde3c7` has no `app/theme_research.py`, no aggregator, no `theme-research.js` (ls-tree). |
| R-IND-01 | Carrier model = one PR per task off fresh `origin/main`, merged in dependency order; continuity = `agentos/handoffs/GMI-INDUSTRIALS-2026-09-24-first-vertical-implementation.md` on `main`; coordination posts on #7789. No single long-lived DRAFT/HOLD carrier. | CDV-1 precedent (#7880 model); Energy's HOLD carrier #7881 became permanently unmergeable. A merged slice is a checkpoint, never a completion claim. |
| R-IND-02 | ONE gate:code job `industrials-result-cash` in `.github/ci/legacy-jobs.yml`, created by T01 from the `prophet-us-b4-prereg-registration` recipe (`if: ${{ false }}`, gate code, scope exclusive, explicit `paths:`), listed in `CURATED_EXCLUSIVE`; every later task appends its suite to BOTH the job's `paths:` and its run line. Clean-venv install line proven per task. | A test named in a run line without a `paths:` entry reds contract-delta fleet-wide; a copied recipe carries the donor's deps (#7905 pyyaml). |
| R-IND-03 | Synthetic corpus law: 14 plan fixtures + `shared_query`, `shared_owner_bundle`, `foundation_only_proof` (17), every file `synthetic: true`, invented issuers, `example.invalid` sources, no live Exponent/Pentair figures, real fiscal endpoints (a July 3 quarter end stays July 3). No real company marked RESOLVED. | Plan §4; addendum A14-07; G2 stays held until real receipts. |
| R-IND-04 | Labor on external fabric lanes (GLM-5.3 via `glm-codex`, MiniMax-M3 fallback on m1/mb), commit-and-push-per-step law in every packet, seat/Opus READ_ONLY red-team of every task PR before dependents branch. Opus/Fable children only as sub-orchestrators/auditors; never as labor. | Chairman delegation text; GLM degeneration receipts 2026-09-24. |
| R-IND-05 | Task ordering: T01 → (T02 ∥ T04) → (T03 ∥ T05) → T06 → [wait for B] → T07 → T08 → T09. Parallel pairs are path-disjoint; `industrials_profiles.py` (T02/T03) and `financial_dossier.py` (T01/T05/T06) are serialized by merge order. | Plan §2 dependency graph + A14-07. |
| R-IND-06 | A14-01 consumed: the shared POST `/api/themes/v1/research/{query,evidence}` family is the ONLY external route; until it exists on `main`, `publication_harness().client()` returns a typed `route_unbound` refusal — never a stub route, never the old GET. | Addendum A14-01; R14-02. |
| R-IND-07 | A14-03/A14-04 consumed: the Industrials composition separates (a) qualified operating facts + result-to-cash derivations, (b) same-period source-edition history, (c) optional management history; missing (c) never suppresses (a)/(b); a limitation-tagged comparison is never a certified beat/miss, badge, growth estimate or headline. | Addendum; R14-01. |

## Seam rulings (from the Opus READ_ONLY seam audit)

Source: `research/industrials/first_vertical_program/reviews/OPUS_PLAN_SEAM_AUDIT_2026-09-24.md` (F1–F20, seam table, collision census). Each ruling binds the packets of the tasks it names.

| Ruling | Decision | Audit basis |
|---|---|---|
| R-IND-10 (T02/T03) | `engine/company_intelligence/industrials_profiles.py` IS created as plan §2 says, on the accepted sibling idiom of PR #7905's `pg_profile.py`: it imports only `IssuerProfile`/`_no_guidance` from `issuer_profiles.py` and builds its own table/row/literal helpers on the public primitives (`receipt_for_literal`, `SpanReceipt`, `BoundRelease`, `text_span`, `TypedAbsence`, `IssuerIdentity`, `ListingAlias`, `company_id_for_cik`, `IssuerRegistry`). It never imports the module-private helpers at `issuer_profiles.py:304-419`. The `issuer_profiles.py` edit is ONE contiguous `_INDUSTRIALS_ISSUER_FACTORIES`/`_INDUSTRIALS_PROFILE_FACTORIES` block plus one merge line in each of `issuer_for_ticker` (~:1280) and `profile_for_ticker` (~:1293), appended after the existing branches; homebuilder dicts untouched. | F3, F16, #7905 head (`refs/pull/7905/head`) |
| R-IND-11 (T02) | No field is added to `IssuerProfile` (four fields, `:155-178`; a profile never inspects its ticker) or `IssuerIdentity` (`identity.py:146-155`, month only). Exact fiscal endpoints ride `FiscalPeriod.calendar_end` (`events.py:159`) and `SourceDocument.presented_fiscal_label` (`documents.py:172`); the CIK lives in `external_ids` and `company_id_for_cik` mints `company_id`. | F2, F15 |
| R-IND-12 (T02) | Serialization: T02 branches only after #7905 has merged (it rewrites `profile_for_ticker`'s signature); the discovery-population step (B's `DISCOVERY_TICKERS` + `_fiscal_period_tolerance_days` + `profile=`/`issuer=` injection in `scripts/refresh_event_workspaces.py`) is a HELD sub-step until #7870 merges — T02 ships profiles, lookup and synthetic retention tests, and records the discovery step as a held gate, never as done. T02's rerun list drops the two Semiconductor suites that are absent on `main`. | F8, F14 |
| R-IND-13 (T03) | Release-edition facts may use the incumbent public-primary assertion the release path already makes; filing-edition (10-Q) facts carry `rights_profile="rp_unknown_v1"`/`rights_state="unknown"` through an Industrials-local span-payload helper with an explicit `rights_profile` keyword until the rights owner qualifies the use (A14-06). No verbatim filing body is ever a fixture. | F4 |
| R-IND-14 (T03) | Typed absences use ONLY the closed 14-reason `ABSENCE_REASONS` (`documents.py:72-87`): ambiguous duplicate cells → `no_span_addressable_evidence`; release-only cash detail → `no_source_document` with `missing_basis`/`missing_period` where the context gap is the cause. No Industrials-local reason word; widening the vocabulary is a bounded owner request. | F5 |
| R-IND-15 (T03) | The filing-enriched edition = `bind_release_document(form="10-Q", …)` (`binding.py:244`, `form` unvalidated) + a distinct `document_kind` + `DocumentRevisionChain` (`documents.py:236-245`) linking preliminary/final/filing predecessors; enrichment-vs-correction is caller-side logic in `industrials_profiles.py`; no second binder, never one event per file. | F6 |
| R-IND-16 (T04/T01) | `industrials_result_cash.py` is written fresh (no import of private `_growth`/`_decimal`), declares its own `FORMULA_VERSION`, mirrors `_not_evaluable`'s typed-limitation shape (`detectors.py:148`), and exports the ONLY production constructor `build_comparison_receipt(purpose, cells, *, checked, unknowns=(), transformations=())`. T01's `comparison()` helper builds the identical closed shape (`purpose`, `operand_refs`, `checked`, `unknowns`, `transformations`, `receipt_id`) and documents it as the contract; T05's selection/comparison functions are the production callers. | F7, F20 |
| R-IND-17 (T06/T08) | T06 mints Industrials' OWN `contracts/market_ontology/industrials_theme_research.v1.schema.json` + `engine/market_ontology/industrials_theme_research.py` on #7891's accepted per-sector precedent; the shared `semiconductor_theme_research.v1` contract is const-pinned and never a target; the "thin adapter" is legal only for the transport/route discriminator. View keys are `operating_views`/`sector_views`, never `industrial_views` (Semiconductor vocabulary on B). T08 is BLOCKED-ON-#7870-MERGE; every packet pins #7870 by exact head sha at consumption time. | F9, F10, F11 |
| R-IND-18 (T05) | `guidance_history.py` exists only on B: T05 binds `definition_unqualified:<field>` and `management_sequence_missing` through ONE module constant in `financial_dossier.py`, delegates `kind='management_sequence'` to `assess_management_sequence` (schema `management_sequence_assessment.v1`) only when importable, and otherwise returns the typed `projector_unbound` refusal. A14-04's "pending" is corrected: implemented on B, unmerged. | F12 |
| R-IND-19 (T01/T04, CI) | The `industrials-result-cash` job is appended as a contiguous block at the END of the job map, donor `prophet-us-b4-prereg-registration` (`origin/main:.github/ci/legacy-jobs.yml:~13451`), `if: ${{ false }}` / `gate: code` / `scope: exclusive`, `paths:` ⊇ every test file in its run line + module paths + fixtures glob, `CURATED_EXCLUSIVE` (`tests/test_ci_pack.py:~3508`, asserted `:~3765`) in the same PR, pip line measured per suite. `legacy-jobs.yml` is contested by #7870/#7905/#7881/#7891/#7896/#7900: lanes edit only the freshly fetched `origin/main` copy and never reorder sibling blocks. | F13, F14, F1b |
| R-IND-20 (T07/T01) | The A14-01 shared POST route family exists nowhere (B ships zero `app/` files); `content_role` is new vocabulary. T07 is HELD until the shared route owner ships it; the only alternative is a bounded owner-approved extension of `app/earnings.py` (`require_site_full_user:59`, `_private:38`, `earnings_member_record:109`) carrying the role through `private_publication._artifact(role=…)` (`:212`) and `validate_private_record` (`:149`) — a later ruling, not a packet. T01's `client()` typed `route_unbound` refusal stands. | F17 |
| R-IND-21 (T01) | Fixture loader is `load_case(name)` with B's explicit `synthetic is not True` refusal, plus `case = load_case` so the plan's verbatim tests import `case`; the 6-line loader is duplicated with a citation, never imported from B's unmerged helper. | F19 |
| R-IND-22 (all) | Every lane branches from `origin/main` fetched at lane start and quotes the sha; the seat worktree is never cited as main; the addendum's R14-04 head `b256aa6a`, the audit ref `70fde3c7` and #7870's live head are three identities — consume by exact sha only. | F1, F9 |

Corrections to the addendum recorded (not re-opened): A14-04 "pending" → implemented on B, unmerged (F12); A14-01 route family → non-existent on both refs (F17); plan §2 "accepted `app/theme_research.py`" → exists nowhere (F9).
