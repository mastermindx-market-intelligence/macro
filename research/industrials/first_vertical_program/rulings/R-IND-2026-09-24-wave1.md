# R-IND wave-1 rulings — GMI Industrials first vertical (Fable Meta-CEO seat c6467452)

Operation `gmi-industrials-fable-ceo-e2e-20260924-chairman-001`. Authority: Chairman delegation 2026-09-24 (deliberate delivery of the master packet) + Astra/Chairman base-ownership ruling 2026-09-24 ~07:50Z. Frozen plan blob `a5462dc7…`, addendum `a2416836…` (A14-01..07 supersede the plan on route/mount/comparison qualification). These rulings refine the plan where the code differs; they never widen it.

| Ruling | Decision | Why / evidence |
|---|---|---|
| R-IND-00 | Industrials mints NO shell, evidence, rights, route or mount vocabulary. T01–T06 extend Company Intelligence, Fundamental Forensics and Earnings owners only. T07–T09 wait for Semiconductor B's route family and `_basket_intelligence_mounts.html.j2` aggregator on `main`. | Astra ruling; #7870 head `70fde3c7` has no `app/theme_research.py`, no aggregator, no `theme-research.js` (ls-tree). |
| R-IND-01 | Carrier model = one PR per task off fresh `origin/main`, merged in dependency order; continuity = `agentos/handoffs/GMI-INDUSTRIALS-2026-09-24-first-vertical-implementation.md` on `main`; coordination posts on #7789. No single long-lived DRAFT/HOLD carrier. | CDV-1 precedent (#7880 model); Energy's HOLD carrier #7881 became permanently unmergeable. A merged slice is a checkpoint, never a completion claim. |
| R-IND-02 | ONE gate:code job `industrials-result-cash` in `.github/ci/legacy-jobs.yml`, created by T01 from the `prophet-us-b4-prereg-registration` recipe (`if: ${{ false }}`, gate code, scope exclusive, explicit `paths:`), listed in `CURATED_EXCLUSIVE`; every later task appends its suite to BOTH the job's `paths:` and its run line. Clean-venv install line proven per task. | A test named in a run line without a `paths:` entry reds contract-delta fleet-wide; a copied recipe carries the donor's deps (#7905 pyyaml). |
| R-IND-03 | Synthetic corpus law: 14 plan fixtures + `shared_query`, `shared_owner_bundle`, `foundation_only_proof` (17), every file `synthetic: true`, invented issuers, `example.invalid` sources, no live Exponent/Pentair figures, real fiscal endpoints (a July 3 quarter end stays July 3). No real company marked RESOLVED. | Plan §4; addendum A14-07; G2 stays held until real receipts. |
| R-IND-04 | Labor on external fabric lanes (GLM-5.3 via `glm-codex`, MiniMax-M3 fallback on m1/mb), commit-and-push-per-step law in every packet, seat/Opus READ_ONLY red-team of every task PR before dependents branch. Opus/Fable children only as sub-orchestrators/auditors; never as labor. | Chairman delegation text; GLM degeneration receipts 2026-09-24. |
| R-IND-05 | Task ordering: T01 → (T02 ∥ T04) → (T03 ∥ T05) → T06 → [wait for B] → T07 → T08 → T09. Parallel pairs are path-disjoint; `industrials_profiles.py` (T02/T03) and `financial_dossier.py` (T01/T05/T06) are serialized by merge order. | Plan §2 dependency graph + A14-07. |
| R-IND-06 | A14-01 consumed: the shared POST `/api/themes/v1/research/{query,evidence}` family is the ONLY external route; until it exists on `main`, `publication_harness().client()` returns a typed `route_unbound` refusal — never a stub route, never the old GET. | Addendum A14-01; R14-02. |
| R-IND-07 | A14-03/A14-04 consumed: the Industrials composition separates (a) qualified operating facts + result-to-cash derivations, (b) same-period source-edition history, (c) optional management history; missing (c) never suppresses (a)/(b); a limitation-tagged comparison is never a certified beat/miss, badge, growth estimate or headline. | Addendum; R14-01. |

## Seam rulings (from the Opus READ_ONLY seam audit)

R-IND-10.. are appended below once `reviews/OPUS_PLAN_SEAM_AUDIT_2026-09-24.md` is consumed.
