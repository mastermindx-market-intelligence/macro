# R-IND — T05 source editions and comparisons: binding rulings

**Task:** T05 of the frozen Industrials nine-task plan, operation
`gmi-industrials-fable-ceo-e2e-20260924-chairman-001`.
**Workstream:** `WS:GMI-INDUSTRIALS-FIRST-VERTICAL`.

**The task itself is NOT DISPATCHABLE — read R8 first.** T05's consumed inputs include T03
event/document refs; T03 consumes T02; and T02 is blocked on a shared seam this program does
not own (`issuer_profiles.py`, `event_workspace.py`, `refresh_event_workspaces.py`),
serialized behind #7870 and #7905. A lane dispatched on T04 alone does not fail loudly — it
**invents** the document-reference shape instead of returning `STATUS: BLOCKED`.

**Supersession inside this file.** R6 supersedes the plan-derived half of R2 with the T04
as-built surface; R7 adds the round-2/round-3 consumption rules. R2's quoted
`build_comparison_receipt` signature is the pre-cure one and is superseded: after the B6 cure
`checked` defaults to **None**, and an omitted `checked` certifies **nothing** (it used to
default to all-True, which certified every gate).

**R6–R8 were verified at PR #8062's cured head `0e3344a9071f` while that PR was still open.**
A dispatching seat re-verifies the exported names and the head sha in its own C0 gate rather
than trusting these lines.

**Why this lives in the repo rather than a session scratchpad.** These rulings bind a future
lane's behaviour. In a scratchpad they die with the session that wrote them, and the next
seat re-derives them — badly — from a plan that does not carry the as-built surface. Context:
`agentos/handoffs/GMI-INDUSTRIALS-2026-09-24-first-vertical-implementation.md`,
`research/industrials/first_vertical_program/reviews/OPUS_T04_REDTEAM_2026-09-26.md`,
`DSC:A-LANE-SELF-VERDICT-IS-NOT-A-CLOSURE-GATE`.

---

R1 (R-IND-18; audit F12). `engine/company_intelligence/guidance_history.py` lives on Semiconductor B (#7870); its accepted API is `assess_management_sequence(...)` with `SCHEMA = "management_sequence_assessment.v1"`, limitations `definition_unqualified:<field>` and `management_sequence_missing`. Check `git ls-tree origin/main engine/company_intelligence/guidance_history.py` in C0 and quote it. Bind BOTH token spellings through ONE module constant block in `financial_dossier.py` (`MANAGEMENT_SEQUENCE_SCHEMA`, `LIMITATION_DEFINITION_UNQUALIFIED_PREFIX = 'definition_unqualified:'`, `LIMITATION_MANAGEMENT_SEQUENCE_MISSING = 'management_sequence_missing'`); `assess_dossier_comparison('management_sequence', …)` delegates to `assess_management_sequence` ONLY when the module is importable (guarded import), otherwise returns `{'status': 'unavailable', 'reason': 'projector_unbound'}` — never a copied projector, never a fork. Industrials semantics (required-basis qualification BEFORE delegation, A14-03 separation) live in your function regardless of importability.
R2 (R-IND-16; audit F7). Comparison receipts come ONLY from `engine.fundamental_forensics.industrials_result_cash.build_comparison_receipt(purpose, cells, *, checked, unknowns=(), transformations=())`; `qualify_operands(purpose, cells, *, comparison_receipt)` is the qualification gate you call before any numerical comparison; `select_editions` and `assess_dossier_comparison` are the production callers (audit: the plan named no producer — you are it).
R3 (time fields on main; audit seam table). Publication/recorded clocks: `SourceDocument.fetched_at` / `published_at` / `available_at` (`documents.py` ~:162-164), `ReleaseRevision.acceptance_datetime` / `filing_date` / `report_date` (`binding.py` ~:86-87), predecessors via `supersedes_document_id` / `superseded_by_document_id` (`documents.py` ~:165-166) and `DocumentRevisionChain` (~:236-245). `source_cutoff` compares against the source-side clock (`published_at`, else `acceptance_datetime`), `recorded_cutoff` against `fetched_at`/`available_at`; a date-only value vs an instant cutoff on the SAME day → `same_day_boundary_unresolved` refusal unless the caller supplies `boundary='inclusive'|'exclusive'`; never midnight-default; commit clocks, signature dates and conference times are not publication clocks.
R4 (A14-03/A14-04; R-IND-07). The three groups are separate results: (a) operating facts + result-to-cash derivations, (b) same-period source editions, (c) optional management history. `management_sequence_missing` or `projector_unbound` in (c) leaves (a)/(b) untouched — test it. A limitation-tagged or `definition_unqualified:*` comparison is NEVER a certified beat/miss/badge/growth estimate/headline (`status` stays `'limited'`/`'unavailable'`, `derived` is None); `not_applicable` ≠ unknown.
R5 (audit F1). First action after `git fetch`: `git checkout -b claude/ind-t05-source-history origin/main`; quote `git rev-parse origin/main`; C0 proves T01–T04 are on main (ls-tree of the three module paths) else STATUS BLOCKED.
R6 (T04 AS-BUILT — supersedes the plan-derived half of R2; provisional until PR #8062 merges, re-verify the head sha in C0). `engine/fundamental_forensics/industrials_result_cash.py` exports EXACTLY five top-level names and nothing else: `FORMULA_VERSION` (= `"v1"`), `ALLOWED_FORMULAS`, `qualify_operands`, `derive_result_cash`, `build_comparison_receipt`. Real signatures — `build_comparison_receipt(purpose, cells, *, checked=None, unknowns=(), transformations=())`, `qualify_operands(purpose, cells, *, comparison_receipt, formula=None)` (note the `formula` keyword R2 did not have; pass it whenever you already know the formula so the qualifier can check that formula's own operand requirements), `derive_result_cash(formula, cells, *, comparison_receipt)`. `ALLOWED_FORMULAS` is the CLOSED seven-name vocabulary `growth_pct`, `margin_pct`, `paired_remeasurement`, `cash_after_capital_payments`, `cash_rollforward`, `segment_change_bridge`, `final_vs_preview` — an unknown name returns a refusal carrying `unknown_formula`, it does NOT raise, so T05 must branch on the returned status and never on an exception. `derive_result_cash` returns a closed dict (11 standard keys plus formula-specific additions) and propagates sign, unit, scale, currency and period from the first qualified operand; every receipt carries `formula_version`. T05 mints NO formula, NO eighth name, and NO second receipt builder: if a T05 comparison needs arithmetic outside those seven, that is a ruling request to the seat, not a local helper. `select_editions` and `assess_dossier_comparison` do not exist yet anywhere — they are T05's to write, in T05's own owned file.
R7 (T04 round-2 CONSUMPTION RULES — verified at cured head `0e3344a9071f`, binding on T05). (a) `qualify_operands` enforces a formula's OWN operand requirements only when you pass `formula=`; omit it and the empty-collection guard is inert (a zero-segment bridge qualified `ready` with no limitations). ALWAYS pass `formula=`. (b) An omitted `checked` argument to `build_comparison_receipt` now certifies NOTHING — every gate defaults False, and `checked=None` is the same receipt as `checked={}`. Declare each gate you actually verified; do not rely on a default, and never pass `checked` wholesale from another receipt. (c) The `checked` block is a DISCLOSURE, not the authority: `qualify_operands` refuses `currency_mismatch` / `scale_mismatch` from operand inspection regardless of `checked`, while `duration`/`perimeter`/`definition`-class gates DO consult the receipt — so a receipt cannot certify away a real operand difference, and equally cannot be left blank where the receipt is the only evidence of comparability. (d) `receipt_id` now digests purpose + refs + checked + unknowns + transformations, so its VALUES changed in round 2: pin no `receipt_id` literal, receipt snapshot or golden receipt file anywhere in T05.

R8 (ORDERING — seat correction 2026-09-26, binding on dispatch). T05 is NOT dispatchable on T04 alone. The frozen plan's T05 section states its inputs as "T03 event/document refs, native source/recorded/effective clocks and T04 operand qualification", and this packet's C0 gate additionally requires T02's `engine/company_intelligence/industrials_profiles.py` on main. An earlier seat note reading "T03 || T05" was wrong and has been corrected on the on-main checkpoint. If C0 finds `industrials_profiles.py` or T03's `tests/test_industrials_source_facts.py` absent from main, the lawful return is `STATUS: BLOCKED` naming the missing artifact — NOT an invented document-reference shape. Do not synthesise the shape of a T03 output in order to make `select_editions` callable: a fabricated input shape is the same defect family that got T04 refused twice (a missing input yielding a certified answer). The legacy-invariance run line may skip `tests/test_industrials_source_facts.py` only when it is genuinely absent, and must say so under EVIDENCE.
