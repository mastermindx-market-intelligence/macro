# Options intelligence: host topology and the actual product consumer

**Observation date:** 2026-09-11 UTC (September 10 US local).  
**Parent:** WS:OPTIONS-ALPHA-INTELLIGENCE-RECOVERY.  
**Existing research carrier:** Macro #7027, `codex/nightglass-public-research-20260910-sol`.  
**State:** research/source analysis; no installed repair, model promotion, new collector or trading authority.

## Capability delta

The earlier census and runtime trace already established an outdated scheduled producer, missing measured fields, a size-blocked episode publication, and two incompatible historical outcome commitments. This continuation does not repeat those censuses. It resolves another concrete dependency: the scheduled host's OI symlinks target an absent local volume, while a content-bearing tree at that path exists on the other connected Mac. It also maps the actual Options Alpha consumer and explains why merely repairing the producer cannot populate its current shadow view.

The commercial target remains the complete Nightglass-inspired research journey: unusual activity → defensible interpretation → evolving campaign → conditional price plan → still-viable entry or explicit abstention → exact-instrument evaluation. A healthier daemon is necessary where it supplies missing evidence, but is not that product.

## 1. Source and scope identities

| Purpose | Immutable source |
|---|---|
| Protected Mastermind Skillpack | `797cfd0b1001d9dfe6fe9030af80ecdab0e1220d` |
| Macro analysis | `43470274d542fdf0cfd59f77a938338f5778dea3` |
| Terminal analysis | `bad423f529d8460bbc83c67f92199ae4ff1b3163` |
| Existing research head at pickup | `5da9beca847ca6e4e504c4f79228c2f6f038580d` |
| Mini scheduled checkout observed | `edd07d7324534c81341ebcc4683173714c3b8509` |

INDEX, COLD_START, RECONCILE_STATE and CLOSEOUT were read at the same protected Skillpack revision, version 1.0.1/bootstrap 1 compatible. Current OA and Advanced Data source/consumer boundaries were read. No runtime Job, worker placement, source writer or new authority was created. Native read tools were qualified by device inventory and ping separately for each host.

## 2. The source tree is available on a different host, not at the scheduled host's path

At 02:46 UTC, the scheduled host `admins-Mini-652.ts.net lan` (device `37db60bd-f84d-4521-ae9e-47c575d9ba86`) reported:

- `/Volumes/STORAGE` and `/Volumes/STORAGE/macro-data/thetadata_eod` absent;
- `flow-ops-wt/data/thetadata_eod/eod`, `oi` and `greeks` are symlinks to that absent volume;
- the mount table contains no STORAGE volume;
- `diskutil list -plist external physical` exits zero with no external physical disks listed;
- `com.mastermind.liveflow` is bound to `/Users/chriswong/liveflow-ops-wt` and the existing interpreter/wrapper.

The source checkout remains the August 20 revision above. Its `engine/live_flow.py` retains SHA-256 `d1db3d21d70ba9898d564927a2e960a9a67e6755198ca89b43f81ab5aec8dfe0`, independently identified in the earlier native trace as predating the accepted measured implementation.

At approximately 02:49 UTC, separate successful file reads on `Mac-Studio.ts.net lan` (device `3f5ce987-e3eb-40a3-af9f-4b0ae54919cc`) found `/Volumes/STORAGE/macro-data/thetadata_eod` with actual `eod`, `oi`, and `greeks` directories and `_manifest.json`.

**What this proves:** the scheduled host cannot follow its local links, and a content-bearing candidate tree exists at the expected path on the other host. **What it does not prove:** disk UUID/physical identity, how or when it moved, complete retained data integrity, current licensed-writer placement, or authority to move a service or drive. A pathname is local to a host; identical path text is not a distributed data connection.

The Studio manifest reports D=2026-09-08, S=2026-09-04, finished_at=2026-09-09T01:00:17.451608+00:00, 369/375 AD-ready roots (0.984), 372 complete T1 roots, and 384 total roots. Its `healthy` status describes that recorded refresh. It does not prove current September 10/11 coverage or validate every Parquet file. No market rows were downloaded to the sandbox.

A Studio read of the exact `com.mastermind.liveflow.plist` path returned ENOENT. This is not an exhaustive absence-of-fleet claim. A later Theta Terminal plist read timed out and supplied no current license/writer census. The own Studio process-start attempt also supplied no accepted PID; later session/history reads did not identify it. Its existence/shutdown remain unresolved; it was not repeated or killed by guesswork.

### Recovery boundary

The existing source owner must qualify one permitted host/store/writer binding. Do not unplug a volume from a host merely because another host references its name; do not clone the large store, start a second ThetaData Terminal, silently map a different store, or re-arm the retired Studio fleet. The existing Advanced Data workstream expressly preserves one Terminal instance and the canonical resolver. A read timeout is not an authorization or a reason to bypass that ownership.

A fresh source-health receipt should identify the actual host, resolved store, eligible vintage and source writer. It must not copy the old healthy manifest into a newly timestamped wrapper.

## 3. The current Alpha screen is not the consumer of the new measured stream

The actual pinned path is:

`ProphetLanesView` → `OptionsAlphaView` → `flowGet("options_prophet_idx")` → existing `/api/flow` resolver → `site/options_prophet/index.json` → `normalizeOptionsAlphaPayload`.

The Macro producer `scripts/build_options_prophet.py` declares its inputs as Flow Leaders, Pick Lab, signing gates, positioning coverage/gates and optional context. It reprojects actual Pick Lab fires and existing ordering. It does not consume the new canonical measured event/campaign candidate feed.

The Terminal parser accepts only `options.prophet_shadow/v1`, `authority=display_only`, `mode=shadow`, the reviewed shape and valid clocks. It intentionally withholds direction, contract selection, entry, exits and Macro weight. Substituting `options.alpha_candidate_feed/v1` into this route will be rejected, not create a candidate product.

The sampled committed projection is dated September 9 and published September 10 at 08:20:00.972746Z. It contains no opportunities and no watchlist entries. Its information/execution readiness is false, Flow Leaders is reported missing/stale/cold/foreign, source alignment fails, the signing source is absent, and the two flow event books have zero fires. These are this pinned artifact's statements, not a fresh authenticated browser observation.

**Inference:** deployment of #6585 supplies missing source evidence but cannot by itself populate this distinct producer/parser contract. An unconditionally less restrictive shadow parser or fabricated score would hide the missing consumer rather than complete it.

The already frozen OA sequence names the appropriate future bridge: the separately reviewed derived `options.alpha_candidate_feed/v1` composer and its existing Terminal mount. Its candidate policy and settled-EOD participation retain their original gates. The shadow's deliberate withholding must not be mistaken for a defect that authorizes a new signal engine inside it.

## 4. Currentness must reach the component, not stop at the cache

`OptionsAlphaView` fetches on mount and on its no-payload error retry. Its inspected implementation has no live subscription or periodic evidence update while the successful view remains mounted. `flowClientCache.ts` has a 25-second stale-while-revalidate cache: its stale branch returns old data immediately and refreshes the Map asynchronously, but the Map has no notification to this component's React state.

The actual tab selector prefetches on hover, focus and selection. This often means a remount waits for an in-flight fresh read, so it would be wrong to claim every tab entry necessarily uses stale data. It does not supply continuous updates to an already mounted view. Reselecting an already selected lane can refresh the cache without invoking the component's fetch callback or remounting it.

The parser also calculates evidence age when normalizing. Merely advancing the clock does not recalculate an already stored `payload.stale`. These are source-level findings; no live browser stale-state incident was reproduced in this turn.

For the future live candidate workflow, acceptance needs a corrected or withdrawn frame to update the visible component, not merely a server/cache value. The existing SSE route already uses the shared `flowSource`/`flowBroadcast` machinery and admissible `f` keys with entitlements; its request-scoped `prophet_idx` exclusion must remain. Reuse an approved existing transport pattern instead of creating a second stream/control plane. The new candidate key/schema and rights classification still need explicit review.

A 30-hour aging policy on the existing nightly shadow is not an intraday-entry expiry rule. Retain source time, first availability, frame revision and entry viability as different facts. The solution is not to reduce every unrelated TTL or call a fresh wrapper a fresh trade.

## 5. A bounded status-presentation defect was identified statically

The UI's label translator recognizes `not_ready` as Pending. Its `statusTone` instead tests the substring expression `ready|fresh|aligned|passed|measured|scored` first. By direct reading, `not_ready`, `unmeasured`, `unscored`, `misaligned` and `not_passed` select the positive `ready` class; a string combining `blocked` and `ready` also takes the first positive branch. This is a text/style contradiction, not a recovered profitable-signal algorithm.

`readinessStatus` correctly prioritizes explicit false booleans, and those booleans protect the sampled current artifact. The finding therefore does not prove that today's committed sample displayed a false-ready card. The generic multi-node grouping helper also gives some unrecognized negative states the same priority as a positive state, but the current readiness cards each pass one producer-owned component. Do not overstate the multi-node case as a witnessed current-card incident.

An exact native JS evaluation request was platform-blocked. It was not rerouted, executed elsewhere, or counted as a failed regression. The cases in this packet are **proposed, unexecuted acceptance vectors**. Their static implications are not test or browser proof.

A bounded future correction should share exact status normalization between label and style, preserve false-boolean precedence, keep unfamiliar states non-positive, and bind the displayed reason to the state it describes. It must not alter underlying candidate selection, probability, trade authority or source thresholds. This is a presentation concern separate from earning Alpha predictive authority.

## 6. Do not turn every discovery into another prerequisite

The natural OA-1T proof runbook captures the measurement schema, coverage, source/decision/availability clocks, matching learning row, and OI fields with their actual availability. It does not authorize manual poller invocation, lowered floors, historical replay as live proof or retired-fleet rearming. The code measures NBBO from the trade/quote batch independently; unavailable OI remains null rather than a reason to fabricate or erase the measured block.

Accordingly, the OI topology recovery is necessary for its OI-dependent consumer questions and statistical input requirements, but must not be casually made a new universal prerequisite for observing disjoint valid trade/quote measurement delivery. The frozen training halt remains unchanged. Acceptance still follows the existing complete runbook; source evidence, consumer integration, calibration and issue authority are separate milestones.

| Work | Existing owner / scope | Completion evidence | Not unlocked by it |
|---|---|---|---|
| Measured producer deployment qualification | OA-1T source/deployment owner | Permitted installed identity, original state/rollback, natural event-to-ledger proof | Candidate composition, calibration, trading |
| Content-bearing OI source topology | Canonical ThetaData / Advanced Data owner | Correct host/source identity and eligible-vintage consumer read | Second store, second Terminal, retrospective OI truth |
| Candidate composer and live mount | Existing OA-1C Macro/Terminal waves | Approved policy/schema, real evidence-to-visible candidate, degraded/revision states | Probability or Issue Desk promotion |
| Status and mount-currentness review | Existing Terminal source owner | Exact source tests plus browser evidence, positive/negative cases | Re-ranking or source-law changes |
| Campaign digest/physical representation | Existing campaign/publication owners | Original-byte equivalence, negative refusal, restore/consumer proof | Waiver of the two incompatible historical prefixes |

These are separate source surfaces, not five new workstreams. Resolve current custody before starting any modifying child. Route bounded source tasks to the least-scarce capable existing avenue; do not make the Chairman allocate routine accounts.

## 7. Product and model implications

The durable unit should be a campaign revision with its own evidence cutoff, not a polished aggregate score over whatever happened to be available on a host. A source failure is not bearish evidence, low attention, an empty market or a losing trade. Preserve the complete eligible observation denominator and reason-specific missingness.

For measurement M1, evaluate what execution/package interpretation is supported and where it must abstain. For outcome M2, assess a specific decision-time revision, entry policy and horizon; price confirmation can arrive after the entry geometry is exhausted. For instrument M3, measure the chosen option/package and quantity/management policy, not an underlying return mislabeled option P&L. Those earlier model proposals remain unadmitted research, not features activated by this report.

The first useful user result can be descriptive: an actual event, clear measurement coverage, explicit uncertainty, a legitimately available price plan, and an entry-open/expired or quote-unavailable state. It need not wait for a fabricated confidence score. Equally, a server-side source repair is not complete product acceptance until its intended consumer can use it.

This preserves what is valuable in the Nightglass workflow while avoiding a false inference that its readable cards prove a superior secret model. No further member-site scraping, competitor data acquisition or public-clone execution occurred.

## 8. Preserved engineering evidence and artifact correction

The previous response linked an engineering packet that was not successfully created. This packet repairs delivery of the evidence: the original native `aggregate_receipt_v2.json` was read back and reconstructed **byte-identically**, with SHA-256 `dc20b34aa387f32c1974a631a748f81c1684cc7e3c967c9528ac39d18057c52a` (4,054 bytes). It is included as `results/RECOVERED_ENGINEERING_RECEIPT.json`.

Its dates and limits are unchanged: the earlier detached digest experiment reduced 200 repeated hash calls to one; it was not a complete campaign benchmark. Lossless compression and six-part reconstruction used the retained 100,471,221-byte generation, not the rejected 112.71-MB nightly candidate. Both historical mismatches remained rejected. No prior tests are reclassified as new tests, no missing source patch is claimed installed, and no full-repository or profitability verdict is inferred.

## 9. Exact next action and stop conditions

**Primary action:** qualify the current permitted source host, licensed writer and store binding against the observed two-host topology, then take only the existing-owner deployment/recovery action supported by that qualification. Preserve the existing deployment state, sole-writer controls and original natural-event proof. Do not issue a generic pull, copy the data store or re-arm a fleet to make an old proof task appear complete.

Independent research/source review may proceed for the explicit currentness/status cases and already prepared campaign cost/storage proposal, under their own custody and acceptance. The two historical prefix disagreements stay unresolved until the permitted evidence path establishes their cause. A blocked historical read or blocked JS evaluation is not retried through another tool, device or account.

A new session should not rerun the completed census, reimplement #6585, assume empty OI data was destroyed, ask for another Nightglass subscription to solve our host wiring, or infer a live candidate consumer from a fresh source commit. It should recover the precise host relationship and separate source/consumer obligations above.

No production source, service, symlink, mount, checkpoint, data store, scoring flag or trading authority was changed in this research pass. Full Agent OS validation, source review, browser proof and production acceptance are not claimed. The research PR remains draft.

## Selected source ledger

All repository paths are relative to the pins in section 1. Native observations are host-qualified in section 2.

- Macro `agentos/workstreams/WS-OPTIONS-ALPHA-INTELLIGENCE-RECOVERY.md`: blob `72dceab8272a80161f010d2ddaf909bc29a90850`.
- Macro `agentos/workstreams/WS-ADVANCED-DATA-OPTIONS.md`: blob `2166c974e617a07f712b9c7b6f08e6b70b7d7995`.
- Macro `ops/LIVE_FLOW_RUNBOOK.md`, lines 814–865: blob `2352cbc5fbac9b9a1e7f22cc8fe6c39c8432cd15`.
- Macro `scripts/build_options_prophet.py`: blob `bd537c1027bce9a74dfd60fda15943ce681a3b3e`.
- Macro `site/options_prophet/index.json`: blob `ffeeb491091ba1f06c5e2ac51c8573e44e3f3f21`.
- Terminal `OptionsAlphaView.tsx`: blob `010bb32fa66630af37baa4fbf470012b3ba727db`, SHA-256 `1a1bc5342ab000f82d0d76917d258df8660c3ad8df6caef0f37b5730168b62a5`.
- Terminal `optionsAlphaTypes.ts`: blob `ba6a2822234f99149227ca5ad2e8372db3cb68f5`, SHA-256 `9f282e9c85a02c961535849d8a0f1591e713dca464fec479a7d5c2b4c2a834c5`.
- Terminal `ProphetLanesView.tsx`: blob `fbdfc9c76987c35362c6561b6d4ee645ad2ab55c`.
- Terminal `terminal/lib/flowClientCache.ts`: blob `60d21525a91237c68154620dce17b44073a772fb`.
- Terminal `terminal/app/api/flow/stream/route.ts`: blob `6437c8cde6eee775adca4925016b389165d7322f`.
