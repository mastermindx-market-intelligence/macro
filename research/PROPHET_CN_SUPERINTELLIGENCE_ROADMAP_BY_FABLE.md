# PROPHET CN SUPERINTELLIGENCE — ROADMAP (BY FABLE)

**Date:** 2026-08-04 · **Trigger:** operator directive after the V3 Relay Engine
shipped ("use your full context — we have built a lot and not a lot of it is
deeply wired into Prophet; make it a world-class institutional stock signaling
superintelligence, connected to the Neural Web"). **Parent:**
`research/CHINA_PROPHET_LOSER_INTELLIGENCE_MASTERPLAN_BY_FABLE.md` (the audit,
the R-slate, G0.1–G0.8 — all gates inherited here unchanged). This roadmap is
the composition layer ABOVE that plan: what Prophet becomes when the rest of
the dashboard's organs are wired into it, in evidence order.

---

## §0 The fast-track evidence doctrine (answers the operator's pacing demand)

Nothing in this program waits a year. The "12-month study" (#4506) was a
BACKTEST over twelve months of committed history — it ran in seven seconds.
The only genuinely calendar-gated object is forward maturation, and the
program's standing answer to it is now doctrine:

1. **Retro stand-in first.** Every candidate wiring is tested against the
   frozen 407-episode V1 frame (PIT joins, production scorer) the day it is
   proposed — the pattern that produced §2.10, the flow battery, and the v3
   era-retro. A stand-in verdict exists in hours, not quarters.
2. **Out-of-era corroboration second** where history permits (the #4506
   pattern: 12 months, thousands of events, half-split robustness).
3. **Forward races confirm, never block.** Ratified changes go LIVE with the
   displaced definition grading in parallel and named tripwires (G0.8);
   the forward ledger's job is to catch a wrong ratification fast, not to
   delay a right one. First tripwire reads land at ~60 matured episodes
   (~3–4 weeks at current admission rates), and the W0 artifact prints the
   race nightly from day one — partial reads visible immediately.
4. **Coverage debts are named, not waited on.** Where a store lacks PIT depth
   (THS membership history, own-market regime), the collector starts accruing
   NOW (W-C) and the analysis states its lookahead caveat instead of stalling.

## §0.5 Regime context of the evidence base (operator + data, 2026-08-04)

The V1 audit era IS the Korea-semiconductor contagion window: CSI300 peaked
2026-06-25 (the board's first day was 06-30), fell −9.1% to a 07-17 trough, and
based flat through 08-03; the semis ETF (512760) fell −37% with its trough at
the last collected bar. Read the evidence accordingly:

- **Era-scoped findings** (quote with the regime attached): Tech's 66% loser
  rate, HOT-theme 42%, the same-day sector-cohort deaths, and the era-retro's
  89%-vs-62% R1 margin — all measured through a contagion-and-basing tape.
- **Structural findings** (tested outside the window): the chase/relay
  mechanics and relay ladder (12-month, both halves), the ordering anti-signal,
  re-admission churn, the wiring census, the exit-study conclusions (labeled).
- **R1's home turf vs challenge regime:** a patience/early-turn rule is a
  buy-the-correction rule — the era's own tail proves it (07-14..07-17 entries,
  buying the forming trough, ran 4–14% loser rates). Its challenge regime is a
  SUSTAINED momentum trend, where confirmed/continuation entries pay and
  demoting them costs — the US failure mirrored. The v2 shadow race, the
  continuation-watch ledger, and the named tripwires exist for exactly that.
- **Tripwire adjudication rule:** a v3-vs-v2 race tightening DURING a momentum
  rebound is regime-rotation information, not per-se proof the flip was wrong.
  Adjudicate with the regime attached; consider regime-conditional forms before
  a straight revert.
- **The regime store has recorded only Q3 for its entire 14-row life.** If the
  rebound is real it prints the store's SECOND regime — the unlock for
  regime-conditioned re-grading (the standing single-regime caveat in
  china_standout_track) and for P-SI-4's regime-scoped weights.

## §1 What "institutional-grade" means for an A-share picker, concretely

Six information layers a top desk systematically exploits, ranked by edge
durability, mapped to what this repo already owns:

| Layer | The edge | What we have | Wired into Prophet? |
|---|---|---|---|
| L1 Policy ignition (政策驱动) | State telegraphs sector priority; themes ignite around policy events | policy_watch, policy_transmission, event calendar, news intel, narrative radar | **No** (display/LEAF by design law) |
| L2 Flow footprints | Northbound, 龙虎榜 desk fingerprints, margin cycle, holder-count chips, blocks, national team | All collected nightly | **No** (zero consumers) |
| L3 Relay microstructure (打板/接力) | Leader-follower sequencing inside igniting themes; limit-tape structure | Limit tape derivable from OHLC; relay ladder measured (#4506) | **Yes — first wiring (v3 R3)** |
| L4 Mean-reversion harvesting | The washout-reclaim family on a structurally retail tape | The validated incumbent core + prime-window timing (v3 R1) | **Yes** (the record) |
| L5 Regime adaptation | Knowing when L4 flips to continuation (2014-15/2020-21 style) | own_market_regime PIT store (young), cn_global_beta, qvix, liquidity | **No** (stamps accrue, zero consumers) |
| L6 Risk architecture | Limit-mechanics-aware exits/sizing; catastrophe avoidance | Exit study (#4507), chase/relay demotion, micro safeguards, PSI program | **Partial** (v3 + display risk-line candidate) |

The superintelligence is not a new model. It is these six layers COMPOSED,
each behind its own graded evidence, each narrated, with a learning loop that
turns every loss and every miss into the next measurement automatically.

## §2 The architecture: sensory cortex → bounded authorities → learning loop

### P-SI-2 — The CN Context Vector (sensory cortex; display/ledger tier, ships freely)

One nightly per-name context block, assembled where the candidates already
flow (`build_china_library`) and stamped onto `china_prophet_shadow`
candidates.parquet so every future study joins it PIT:

```
context = {
  theme:   {membership, level, timing_state, basket_phase, relay_count_3d},
  flow:    {lhb_3d, northbound_z20, margin_d20, holder_trend, block_10d,
            buyback_active, turnover_pctile}   # axes that pass the battery
  policy:  {backed_flag, last_event_days}       # from policy_watch tags
  regime:  {own_market_quad, global_beta_state, qvix_regime}
  risk:    {chase_composite, limit_band, risk_line_-10pct}
}
```

Zero authority at birth. Its value is that the ENTIRE dashboard's knowledge
becomes joinable evidence the night it ships — the difference between "we have
the data somewhere" and "every episode carries its context forever."

### P-SI-2.5 — The stored-energy ranking doctrine + the Rank Effectiveness Ledger (added 2026-08-04, operator ordering directive)

**The ordering problem, stated first-principles:** the user reads the top of the
board, not all hundred rows. Selection quality (who is admitted) and ordering
quality (who is FIRST) are different instruments; V1 proved they can point in
opposite directions (good membership record, anti-predictive rank-IC +0.073).

**The doctrine the evidence converged on — rank by STORED energy, never
RELEASED energy.** V1's setup score ranked kinetic energy (recent thrust, day-0
pops, hot trails) — energy already spent; it put the loser cohort on top. Every
measured winning cohort is a stored-energy marker: deep dislocation with a
quiet tape (bottom-half turnover 17.7% loser rate vs 42.5% at p90), the
pre-confirmation timing states (bounce_wait 93% win — the coil before the
release), WARMING themes and Trough+ sectors (accumulating), early relay
(propagation just starting). Compression (squeeze/NR/vol-contraction — the
US Ignition Layer's S-COIL family, market-agnostic detectors in
engine/compression_signals.py) is the same theory's missing measurable axis.
Risk ordering falls out for free: stored-energy entries have structurally
bounded give-back (winner MAE −1.1% vs loser −13.7%) — one axis serves return
AND risk. Fences that bind any score iteration from this doctrine: depth ALONE
is a killed ranker (#1747-A3); no pre-onset universe claims (rows 114-115);
no composite black box (each leg separately measured, capped, revertible);
the armed/compressed state is never SURFACED as a user-facing signal
(ESX §9 / DT-R5 family — internal ordering features are a different
construction and must cite the fence).

**The Rank Effectiveness Ledger (ops-telemetry; the operator's
track-record-that-tracks-everything requirement).** The score grades ITSELF
nightly, per board_definition, never pooled:
1. Rank-IC ladder vs EVERY outcome dimension the spine already computes —
   excess_h10, fwd_mfe_5/10/21/63, MAE, catastrophic flag, terminal states,
   post-cushion breach — so "maybe top-ranked picks win on a dimension we
   don't track" becomes measured, not speculative.
2. Top-k ORACLE REGRET + capture@k: of each date's pool, how much of the
   best-possible top-6 did the score actually put on top. This is the direct
   metric for "buying the top future winners inside the shortlisted pool."
3. Risk/time profile by score tercile: loser rate, MAE, catastrophic rate,
   clean-liftoff share, time-to-payoff — the lower-risk and longer-duration
   lenses named explicitly.
4. Per-component attribution: every score leg's own IC nightly (glass-box —
   which leg earns its weight is public inside the artifact).
5. Wrong-sign tripwire: a definition whose excess-IC turns anti-predictive
   over ≥60 matured episodes pages with a revert proposal.
Evidence instruments feeding the next score iteration:
`rank_feature_battery.py` (within-pool ordering ICs, date-demeaned, dual-era
stability, winners-only magnitude) + the S-COIL CN port test. Score changes
remain one-axis-per-adjudication under P-SI-3; this section only fixes the
DOCTRINE and the measurement so ordering can never silently rot again.

### P-SI-3 — The bounded-authority ladder (one axis per adjudication, glass-box law)

Each context axis that shows separation follows EXACTLY the theme_timing
precedent: retro stand-in → out-of-era corroboration where possible →
operator-ratified bounded authority (a capped score component or a featured
prior, never a hidden blend) → displaced-definition shadow + named tripwire.
**No composite black box, ever**: the score stays a sum of individually
defensible, individually revertible legs. Current queue, in evidence order:

1. Flow axes — pending the battery's ranked verdicts (LHB desk quality first:
   it directly targets the relay bimodality — who bought the limit day).
2. Policy backing — retro test on the episode frame next (did policy-backed
   themes' picks outperform policy-vacuum themes'?), then a context chip, then
   the ladder.
3. Crowding/turnover percentile — the cheap always-available axis.

### P-SI-4 — Regime-conditional composition (the US lesson, pre-empted)

The US audit proved one entry family cannot serve two regimes. CN's washout
family thrives BECAUSE the tape mean-reverts — when CN turns trending (it
does, violently), today's Prophet would repeat the US failure in mirror image.
The continuation-watch ledger (W-C) is the counter-family accruing NOW; the
promotion trigger is regime-conditional: when `own_market_regime` shows a
sustained trending quad AND the continuation ledger's matured record clears
its prereg, the door opens with regime-scoped weight. Until the regime store
carries a second regime, this stays chartered, not built — the honest
data-gate, stated.

### P-SI-5 — Neural Web integration (both directions, lawful)

- **Prophet → NW:** every CN Prophet artifact registers as a synapse with its
  honest grade (W0 telemetry = ops-telemetry; track ledgers = graded records;
  v3 lanes = display+scored-definition). The miss-audit and tripwire states
  become first-class synapses so the brain can answer "why isn't X featured
  tonight" from artifacts, never from vibes.
- **NW → Prophet:** graded cross-lobe context (global-beta stress, liquidity
  regime, USD/CNH) enters ONLY through the P-SI-3 ladder like any other axis —
  the constitution's A7 ban stands: the LLM/brain never originates or escalates
  a signal; it narrates and de-escalates calibrated keys only.
- **Chat visibility:** the Live Market State Packet carries the CN board block
  (W-C), so the Mastermind chat explains picks, absences, demotions, and the
  v3-vs-v2 race in plain words on both languages.

### P-SI-6 — Cadence, honestly assessed

A-share ignition is intraday (seals by mid-morning), and the US has
prophet_live for intraday re-probes. CN's collectors are end-of-day, and
crucially **T+1 execution blunts the urgency**: a signal detected at the close
fills at the next open exactly as a midday detection would — the fill is the
same bar. The measured cost of EOD-only detection is therefore small for the
incumbent family; where it binds is relay-EARLY detection (being first into a
theme's second day). Verdict: intraday CN quotes are a data-acquisition
decision for the operator (cost vs the relay-early margin), NOT a silent gap —
chartered as an open question with the T+1 analysis attached, no build until
the data exists.

### P-SI-7 — The learning loop closes itself

W0 already grades nightly. The remaining half (W6): the postmortem taxonomy
auto-stamps every matured loser (chase / relay-late / theme-fade /
sector-cohort / churn / other) and every missed runner (funnel stage), and a
weekly governor emits a delta report whose findings arrive as DRAFT preregs —
machine-drafted, operator-adjudicated, never hot-patched. The system proposes
its own next experiments; authority still moves only by ratification.

## §3 What ships when

| Wave | Content | Tier | Status |
|---|---|---|---|
| Now | v3 era-retro stand-in verdict | research | building |
| Now | Flow/LHB ex-ante battery (kills checked) | research | building |
| Now | W-C: PIT membership history + continuation ledger + NW/chat visibility | shadow/ops/display | building |
| Next | CN Context Vector assembly + candidates.parquet stamping | display/ledger | after battery ranks axes |
| Next | Theme Tape CN + continuation strip (designer, mirrors US W2) | display | after W-C plumbing |
| Then | Policy-backing retro + first flow-axis ladder adjudication | scored (ratified) | after evidence |
| Gated | Regime-conditional continuation door | scored (prereg) | second regime in store |
| Gated | Intraday CN probe | data decision | operator call |

## §4 Fences (inherited + new)

All parent-plan gates G0.1–G0.8 bind every wave here. Additionally: no
composite/blended context score (each axis stands alone or not at all); no
LLM-originated signals anywhere (A0–A7); flow axes with prior phase-0 kills
enter only as differently-constructed axes with the kill cited; PSI (#4404)
owns portfolio sizing — this roadmap stops at per-name signaling; seasonality
stays Codex-owned; the US lanes stay untouched.

---

*Related: CHINA_PROPHET_LOSER_INTELLIGENCE_MASTERPLAN (parent),
PROPHET_US_TREND_INTELLIGENCE (the mirror), NEURAL_WEB_MASTERPLAN (registry
law), MASTERMIND_SUPERINTELLIGENCE_MASTERPLAN (chat), PSI (#4404, sizing).*
