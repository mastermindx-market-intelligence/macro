- an entitled production browser loads nonzero records;
- all five modes are exercised;
- one known trial dossier and receipt are accessible;
- peer ordering is exact;
- anonymous and wrong-tier states are correct;
- source and projection incidents are distinct;
- 24 browser cells are captured;
- freshness semantics are settled;
- no generic unavailable state appears without a reason code.

### Functional-parity beta is complete only when

- the parity ledger is at least 24/32 genuinely user-reachable jobs;
- Catalyst Radar, Market Pulse, Explorer, Dossiers, Change/First-Seen, Alerts, and API are operational;
- company/security/asset/trial identity is temporal and reviewed;
- cash/runway and catalyst calendars have useful coverage;
- stock dashboard and movers are live;
- data provenance and source locators are visible;
- watchlists and alerts work;
- every feature reports coverage and freshness.

### Alpha-lobe shadow is complete only when

- PIT feature snapshots are reproducible;
- event, financing, market, options, and regime features exist;
- scenario output includes uncertainty and abstention;
- retrospective tests include baselines, FDR, and leakage controls;
- every model is on a forward ledger;
- a BioCatalyst contribution packet is emitted;
- Prophet can replay the packet without browser math;
- no live order effect exists yet.

### Bounded Prophet integration is complete only when

- the authority decision is merged;
- shadow results meet frozen gates;
- the contribution is capped and reversible;
- only already-admitted candidates are affected;
- missing/stale/ambiguous evidence abstains;
- every rank move has a contribution trace;
- provisional, revised, and nightly states are explicit;
- rollback is one configuration change.

---

## 21. Paste-ready prompt for the next Codex session

> You are taking over BioCatalyst in `mastermindx-market-intelligence/macro`. Do not build a new feature, model, source adapter, schema family, or masterplan. The only objective of this session is to make the existing production five-mode BioCatalyst workbench successfully hydrate real private data for a signed-in entitled user and to prove it with browser-level evidence.
>
> Start from current `main`. Read:
>
> - `research/BIOCATALYST_HANDOFF_TO_CODEX_2026-08-15.md`
> - `templates/biocatalyst.js`
> - `templates/biocatalyst.html.j2`
> - `app/biocatalyst.py`
> - `templates/theme.js`
> - `config/site_access.yml`
> - `app/deploy/Caddyfile`
> - `app/deploy/macro-api.service`
> - `app/deploy/macro-biocatalyst.service`
> - `tests/test_biocatalyst_page.py`
>
> The shell already loads. The screenshots show Milestones, Trial Screen, Change Tape, and First-seen Tape falling into generic unavailable states. The handoff explicitly says authenticated browser proof is still owed. Treat this as one shared hydration incident until evidence proves otherwise.
>
> Required sequence:
>
> 1. Reproduce with a signed-in entitled browser. Capture network status, content type, response body, console output, active session state, and request IDs.
> 2. Run the same endpoint through the public domain, Caddy/origin, and localhost FastAPI with the same bearer token. Identify the first layer where the response differs.
> 3. Verify the active generation pointer, generation contract, row counts, and one known trial using the same reader the API uses.
> 4. Separate auth/entitlement health from source/collector/projection/API/frontend health.
> 5. Add stable machine-readable error codes. Do not collapse access, source stale, projection missing, contract invalid, network, and valid-empty into one message.
> 6. Remove the silent auth fallthrough: do not issue the private request until session bootstrap has resolved. A failed auth bootstrap must render a typed auth state.
> 7. Add a signed-in Playwright acceptance test proving nonzero rows for Trial Screen, Milestones, Change Tape, and First-seen Tape; a known peer cohort in exact order; a dossier and source receipt; correct anonymous lock state; and zero console errors.
> 8. Capture desktop/mobile, dark/light, EN/ZH, reduced-motion matrix or explicitly record the remaining cells.
> 9. Do not widen freshness budgets or switch freshness clocks to make the test green. Record source-content age and transport age separately and settle the governing semantics through a reviewed change.
> 10. Open small PRs. Include before/after screenshots, redacted HAR/response summaries, production-equivalent row counts, generation digest, tests, and rollback.
>
> Completion is not “tests passed.” Completion is an entitled browser showing real records in production-equivalent conditions with typed failure states and a reproducible evidence package.

---

## 22. Bottom line

The prior build did not create “nothing.” It created the hardest low-level part of a trustworthy temporal clinical-data platform.

It did, however, create the wrong product boundary and then mistake contract completion for customer completion.

The recovery path is not another total rewrite and not another endless governance program:

1. repair the shared hydration path;
2. re-charter the authority model;
3. build the temporal company/asset/security graph;
4. expand into modular parity surfaces;
5. consume the existing Macro/Terminal market and options estate;
6. build the asymmetry engine as a transparent scenario and evidence system;
7. shadow it;
8. let it contribute bounded evidence to Prophet only after it earns that role.

That path turns the existing work from a costly dead-end dashboard into the evidence spine of a differentiated biotech intelligence and alpha lobe.
