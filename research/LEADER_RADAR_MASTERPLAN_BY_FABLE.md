# Leader Radar — US single-stock leader-lifecycle desk — masterplan (by Fable)

**Status:** ADJUDICATED + DISPATCHED 2026-07-11. **Owner:** Fable main loop; Sonnet builds; Opus reviews.
**Provenance:** operator directive 2026-07-11 ("the most important lobe: holistically detect single-stock momentum leaders at the precipice of their launch, not when everyone has crowded in — GOOGL 2025 lone-alpha rerate, UNH now, META candidate, NVDA↔memory handoff; anticipate via basing/washouts, low-TF oscillator ramps, flow inflections, stock-to-stock leadership changes") → 5-lane census (leadership machinery, governance law, data feasibility, case-study field guide, external methods) → 2 Opus red-teams → this ruling of record. Companion artifact: `research/LEADER_LIFECYCLE_FIELD_GUIDE.md` (the 6-stage anatomy distilled from GOOGL/UNH/META/NVDA-memory + NVDA-2023/META-2023/LLY/GE/PLTR/AAPL/MSFT episodes).

> **In plain English:** the repo already owns the two ends of this problem — Winner Autopsy detects the *breakaway onset* (Detector-D: UNH has been in `continuation` for 51 days), and the Flow Leaders Desk (shipped today) detects *current* flow leadership. What's missing is the middle and the front: the **pre-breakaway states** (left-for-dead → quiet accumulation → catalyst window) and the **market context** (are we in a 1-2-winner regime? which extended subtheme is handing off to which basing hub name?). This program ships a deterministic per-name **leader lifecycle state machine** over a ~180-name core universe (Mag7 + AI infra/semis/memory/power/software + Dow 30 + NDX), a **handoff watch** (extended donor basket × basing recipient), a **leadership-regime classifier**, a **rerating watch** (revision inflection × compressed multiple — the single most consistently leading observable in every case studied), two forward-ledger books, and a Leader Radar page. Everything is display-tier states and chips — by constitutional law (direction is dead everywhere tested; NW-U29) this desk *describes stages*, prints its evidence, and earns any authority later through the Pick Lab shadow path.

---

## §1 What the census established (the ruling stands on these)

1. **Winner Autopsy is the engineering home.** `engine/winner_autopsy.py` Detector-D (per-axis AND: liquid ∧ rel-breakaway ∧ 63d-new-high ∧ volume-confirm) + 2,650-episode census (1999→, with durable_winner/blow_off/failed outcome labels) + live `breakaway_watch` states + PIT pre-onset features (rs_turn_21_63, days_below_200dma, updown_dollar_vol_ratio). It runs **research-lane only** — not nightly. This program is Winner Autopsy's "Layer 0" (pre-onset states) and productionizes its watch states nightly.
2. **The lobe cap is consumed** (L1 short-side + L3 dispersion). This ships as **waves on Winner Autopsy + Pick Lab + market context**, not a new NW lobe charter (chartering law; re-opens only if a slot frees).
3. **Direction is constitutionally dead** (NW-U29: momentum dead everywhere tested; the 7 OOS-GO anticipation legs are ALL drawdown-control). Legal shape = deterministic threshold-cascade **state machine** (stock_personality / breakaway_watch precedent), per-axis AND-gates (WA-R1), K-of-N chips, single measured sort quantities, forward ledgers. No probabilities, no fused scores, no composite admission gates (LH-R2).
4. **The field guide's strongest empirical finding:** the **estimate-revision inflection** (cuts → first upward flip) led price by 2–8 weeks in nearly every episode; the **capital-concentration fingerprint** (leader RS accelerating while same-sector peers' RS declines) is directly computable; catalysts cluster into 6 types; ignition without a Stage-2 foundation mean-reverts; crowding markers (analyst-buy saturation ≥85%, call-skew richness, extension >30% vs 50dma, basket-correlation >0.75) are the exit-side lens.
5. **Data reality:** RS time series per name does NOT exist (highest-ROI new store — trivial to build from `baskets/ohlcv` + `data/yahoo/SPY`); `memory_storage` basket exists (MU/WDC/STX/SNDK/NTAP) so the NVDA↔memory handoff is directly supported; 2D bars are plumbing-only (`bar_derive.derive_2d_ohlcv`, easy wire-in); 4H exists on the runner for 15/19 core names (mtf_monitor path, not in per-name stockdata); forward-PE history is only 22 days deep (self-seeding since 2026-06-13 — the rerating *trajectory* tracker matures ~Q4-26; the rerating *snapshot* conditions work today); revisions store (net_up_30d, est_chg_30d/90d, breadth) is live since 2026-06-16 but keeps no history; dispersion/regime.json ALREADY labels the narrow-leadership environment (currently: dispersion 77th pctile, avg corr 0.07 — "Selection pays"); `data/cohort_metrics` keeps only 4 rolling days (extend); options tape signed exists for 20 core names (120d).
6. **Flow Leaders Desk (shipped today, PRs #2216/#2217/#2224 + Terminal #82) is consumed, never duplicated** — its Board A answers "who leads NOW on flow"; this desk answers "who is staged BEFORE" and "what does the market regime imply".

## §2 Rulings

- **LR-R1 — Leader lifecycle state machine ADOPTED (display-tier).** `engine/leader_lifecycle.py` classifies each core-universe name into exactly one state: `SUPPRESSED → QUIET_ACCUMULATION → CATALYST_WINDOW → BREAKAWAY → LEADERSHIP → CROWDED` (+ `NONE`/`FAILED`), each a deterministic per-axis AND/threshold cascade over named observables (definitions frozen in engine constants, provenance-commented from the field guide + external dossier). BREAKAWAY/continuation states come from `winner_autopsy.compute_watch_states()` **by import** (Detector-D is canonical; re-implementation is a build error). Tri-state inputs; null never counts as False; every state row prints its per-axis evidence chips. No probability, no composite score, no direction claim anywhere (NW-U29). Vocabulary fences: "winner" only post-Detector-D (WA-R10); no "sponsorship" (WA-R3); no "money routing"/"capital suction" (TOP3-O2); no "validated" (CI).
- **LR-R2 — State definitions (frozen; field-guide §III provenance).**
  *SUPPRESSED:* RS-vs-SPY 63d slope < 0 for ≥ 3 months AND drawdown from 252d high ≥ 25% (or ≥ 12 months below 200dma) AND valuation cheap_pctile ≤ 40 (null-tolerant).
  *QUIET_ACCUMULATION:* (SUPPRESSED context within trailing 6 months OR base: 60d range compression ≤ 25% width) AND ≥ 2 of: **revision_positive** (net_up_30d > 0 AND est_chg_30d > 0 — a LEVEL, honestly named: the true *inflection* (cuts→first-up-flip) becomes computable only as the W2a revisions-history store accrues; the chip renames to revision_turn when Δ-of-state is live, ~Q4-26), rs_turn (RS 21d slope > 0 while RS 63d slope ≤ 0), accum_evidence (up/down volume ratio 50d ≥ 1.2 OR accumulation-day count 25d ≥ distribution-day count + 3 OR pocket-pivot within 10d), obv_divergence (OBV 63d new high while price below its own 63d high), insider_cluster (≥2 open-market buys 90d, altdata; context chip).
  **Threshold provenance (FL-R2 mirror):** dossier-derived: up/down ≥1.2, pocket-pivot 10d rule, monthly RSI ≥80, extension ≥25%, parabolic 4w>2×13w, analyst saturation ≥85%, basket-corr ≥0.75, George-Hwang high52w. Pre-registered-arbitrary (frozen; change = engine version bump + ledger reset; not tuned on any observed case): 25% drawdown, 3-month RS-slope window, cheap_pctile ≤40, 60d/25% base box, +3 accum-day margin, 5% gap / 2× volume, revision breadth ≥0.6, all RS windows, top-12 book size.
  **Determinism + hysteresis (frozen in W1 engine constants):** precedence cascade most-advanced-wins — FAILED-override > CROWDED > LEADERSHIP > BREAKAWAY > CATALYST_WINDOW > QUIET_ACCUMULATION > SUPPRESSED > NONE; a state is entered only after its conditions hold 2 consecutive sessions and exited only after failing 3 consecutive sessions (breakaway/continuation/failed keep winner_autopsy's own semantics). `days_in_state` anchors on the new `data/leader_radar/state_history.parquet` (per-name state-entry dates; radar builder is its single writer; seeded on first run).
  *CATALYST_WINDOW:* QUIET_ACCUMULATION (current or ≤ 21 sessions ago) AND an ignition trigger within 10 sessions: gap ≥ +5% on ≥ 2× 20d volume, OR breakaway_watch = `emerging`, OR RS-line 126d new high while price below its own 126d high (the RS-leads-price divergence).
  *BREAKAWAY / LEADERSHIP:* Detector-D onset / `continuation` with RS top-decile persistence ≥ 4 weeks (from the new RS store) plus the **concentration fingerprint chip**: leader 63d RS-accel > 0 while same-basket peer-median 63d RS < 0.
  *CROWDED:* LEADERSHIP context AND ≥ 3 of: extension vs 50dma ≥ 25% (or ≥ 90th pctile own history), monthly RSI ≥ 80, parabolic flag (4w return > 2× annualized-13w), valuation ≥ 80th pctile own 5y history, analyst buy-share ≥ 85% (finnhub), call-skew richness (rr_25d ≥ own 80th pctile where gex present), basket-correlation 60d ≥ 0.75 having risen from < 0.5.
- **LR-R3 — RS series store BUILT with FULL-HISTORY BACKFILL** (`data/rs_series/` parquet per core name; the builder computes the complete RS history from stored ohlcv (2014→) + `data/yahoo/SPY` on first run — NOT an accrual-gated append; acceptance: rs_series depth == ohlcv depth on run one, so RS-turn/persistence legs are live day one; close/SPY and close/sector-benchmark): unlocks RS-turn, RS-line-new-high-before-price, top-decile persistence weeks, peer-divergence fingerprint, handoff detection. George-Hwang high52w_prox (already computed) rides as the academic-grade LEADING chip.
- **LR-R4 — Handoff watch ADOPTED (descriptive relative-performance rotation ONLY).** **Extended-leg** = basket (from the curated subtheme set incl. `memory_storage`, `ai_semiconductors`, `semicap_equipment`, `data_center_power`, `ai_software`, `ai_neoclouds`) whose EW return puts it ≥ 90th pctile extension vs its own 200d trend AND basket RS 21d rolling over; **Basing-leg** = core-universe name in that theme's neighborhood in state SUPPRESSED/QUIET_ACCUMULATION/CATALYST_WINDOW with a D-or-2D MACD cross recent-or-approaching (ETA fields). Output = watch PAIRS with evidence, copy strictly price-implied ("relative-performance rotation watch"); no donor/recipient or capital-flow vocabulary anywhere (TOP3-O2); the killed flow-routing tensor is not extended. Boundary: reads `subsector_rotation.json` and `index_leadership.json` as context only, never recomputes their RRG math; Oracle owns sector-ETF rotation; this operates at basket/single-stock grain with oracle state read-only.
- **LR-R5 — Leadership-regime classifier ADOPTED (display, radar-page-resident).** `leadership_regime ∈ {narrow_leadership, broad_thrust, mixed}` from per-axis conditions (never fused): PRIMARY = the existing `dispersion/regime.json` (dispersion pctile + avg corr — already labels the environment) + breadth (pct_above_200); SECONDARY, coverage-gated = top-5 contribution share of NDX-universe 21d return (ohlcv × mktcap from stockdata `profile.mktcap_bn` Polygon-reconciled; printed with n_covered — ARM/ANET/QCOM/SMCI-class gaps disclosed, split-staleness caveat carried); Zweig-thrust flag (breadth store; ~18 fires since WWII — rarity printed, decorative honesty). The chip lives on the Leader Radar page ONLY — `macro_context.html` is NOT touched (weather-station framework laws; #2184 mockups binding).
- **LR-R6 — Rerating watch ADOPTED (LEGAL-UNEXPLORED territory, display).** Conditions (per-axis chips, no score): revision_turn + revision breadth ≥ 0.6 + multiple compressed (trailing_pe cheap_pctile ≤ 40 OR forward_pe below sector median) + catalyst recency (deterministic news count / earnings date proximity as *context*, Cremers-honest: pre-scheduled-earnings flow direction is unreliable) + optional buyback context (capital_allocation store). The PE-*trajectory* leg (multiple expanding N weeks) activates automatically as `stock_fundamentals/snapshots.parquet` accrues depth (self-seeding since 2026-06-13; checkpoint 2026-10). Revisions history persistence is chartered (W2) so revision-inflection becomes a computable series rather than a snapshot.
- **LR-R7 — Low-TF oscillator surface (RADAR-LOCAL).** 2D oscillator states computed inside `engine/leader_lifecycle.py` for the core universe only, via `close.resample('2B').last()` fed to the pinned `_tf_state` math (the `derive_2d_ohlcv` helper is NOT used — its own contract forbids feeding signal paths — and global `mtf_snapshot`/stockdata is NOT touched, avoiding a ~1.2 MB × 1,626-file bloat); 4H states for the core universe exposed into radar rows where runner intraday data exists (null-honest elsewhere; ANET/QCOM/ARM/SMCI gaps disclosed). Consumes existing `_tf_state` math + Flow Leaders' `htf_oscillators` (2W). Per A2 case law and the washout×turn kill: oscillator turn chips are display + state-machine *inputs for CATALYST_WINDOW context only* — they never qualify ledger fires alone and are never sort keys.
- **LR-R8 — Forward ledgers via Pick Lab (registry 25→27 — Flow Leaders #2224 is MERGED and main asserts ==25; single-writer preserved).** `plab_leader_precipice` — fires on a name ENTERING CATALYST_WINDOW with (revision_positive OR rs_line_nh) true — the OR-leg keeps revision-uncovered names (ARM/SNOW/NET/CRWV/NBIS/CORZ/IREN/ALAB/CRDO class) structurally eligible; coverage banner names them — (ruler `21d_spy_excess`, 63/126d descriptive context) and `plab_leader_onset` — fires on Detector-D BREAKAWAY entry (ruler `21d_spy_excess`). Both top-12, PL-R2/PL-R4/R-SP21 laws; **refire lockout: 21 sessions AND full de-escalation to NONE/FAILED before a ticker re-qualifies for the same book** (state churn cannot double-count an episode); fires inside `build_pick_lab.py`'s loop consuming the radar artifact's published fire flags; fire_date = artifact as_of (PIT law as FL-R8). Universe-exit rule: a name leaving the universe freezes its open forward windows at the last valid bar with `left_universe=true`.
- **LR-R9 — FDR family `leader_anticipation_v1` registered NOW, computed at maturity.** k = 4: the two books × 2 co-primary reads (WR-lift vs `plab_random_ctrl`, median 21d SPY-excess). BH α=0.10; DT-R14 time-preserving nulls; era law N/A for live-only reads (any historical extension = new registered family under DT-R16). **WA episode-census pairlets are NOT registered here** — red-team round 1 established that WA-R8 requires `winner_case.v1` schema files in `research/winners/cases/` (11 already exist, a different set) PLUS an Opus-reviewed fingerprint report PLUS an explicit WA-program ruling drawing on the LH-R12 shared budget (Σ=29/40); the pre-onset-state pairlet idea is ROUTED to the WA docket as a request, and this program's field-guide episodes may be converted to `winner_case.v1` files under WA's own process. Adjacencies: theme-membership admission REJECTED (not used); 13F positive STRUCK (13F = crowding-hazard context chip only, WA-R2); crowding composite KILLED (crowding markers stay per-axis chips); FRESH-BUY near-52w-high anti-predictive caveat carried on the high52w chip.
- **LR-R10 — Surfaces.** `site/leader_radar.html` + `site/leaderradar/radar.json` (bilingual, nav, theme.js, playbook accordion = the field guide condensed; honest coverage/null banners; kill-switch): lifecycle board (state, days-in-state, per-axis chips, de-escalation/crowding chips, 13F-crowding context, dt_contra contrarian caveat chip), handoff watch pairs, regime banner, rerating watch table, links to Flow Leaders/Pick Lab/stock pages. R2 mirror `leaderradar/radar.json` for a later Terminal tab (W5). Nothing feeds `board_ordering`/`top_setups`/conviction (CONST-ART2; Mastermind intake stays parked with the Flow Leaders one).
- **LR-R11 — Nightly productionization of watch states.** The radar builder runs `winner_autopsy.compute_watch_states()` on the core universe nightly (cheap subset; the full episode census stays research-lane). breakaway_watch parquet remains WA-owned; the radar consumes/refreshes states without touching WA's research stores' semantics (single-writer respected — radar writes its own artifact only).
- **LR-R12 — Universe (pinned).** `leader_radar_universe` = mag7 ∪ ai_infra ∪ ai_semiconductors ∪ semicap_equipment ∪ memory_storage ∪ data_center_power ∪ ai_software ∪ ai_neoclouds ∪ Dow-30 ∪ NDX (baskets_nasdaq membership) ∩ has-ohlcv, ≈ 150–190 names, config-listed. ETFs excluded. CN/HK out of scope. IPO-pipeline watch (SpaceX/Anthropic/OpenAI class) = a display note fed by `data/ipo/calendar.parquet` recency only — no pre-IPO signal claims.
- **LR-R13 — Boundaries.** Consumes (never re-implements): winner_autopsy, mtf_upturn, signal_gate tiers, coiled/washout_ctx, flow leaders json, dispersion regime, index_leadership, cohort_metrics (extended rolling retention chartered), stock_personality, smartmoney (context_only). No kernel (2026-10 gate); no NW authority wiring (CONST-ART2 + rails §10); no options tissue in any gate (GAP-U4 — options chips display-only); no S16/coiled conditioning tests (S-SQ family); no LLM in the path (Article 1); no theme-membership eligibility (LH-R2 residue); no sector-rotation prediction (Oracle territory; zero-sum RS identity).
- **LR-R14 — Estimator/honesty laws.** DT-R14/DT-R16 bind all future studies; ticker-cluster bootstrap forbidden; revision/fwd-PE stores' shallow history disclosed on-page; every chip whose input store post-dates 2026-06 carries a "young data" tag; failure-mode copy (value-trap test: "is the compression cyclical or structural?" — a question, printed as such, never auto-answered).
- **LR-R15 — Naming/copy.** "Leader Radar" / 领袖雷达. States are descriptive present tense; anticipation framing is "staged before onset", never "will lead"; base-rate honesty line from the external dossier ("even complete setups fail ≳40% individually; evidence accrues in the ledgers") on the playbook.

## §3 Waves

_One wave = one branch off fresh origin/main = one PR = same-day squash-merge. Sonnet builds in pre-created worktrees; Opus reviews; Fable merges._

| Wave | Scope | Acceptance gate |
|---|---|---|
| **W0+W1 — Field guide + engine** (PR-1) | This masterplan + `research/LEADER_LIFECYCLE_FIELD_GUIDE.md` (narrative field guide; `winner_case.v1` conversion routed to WA's process separately); `engine/leader_lifecycle.py` (pure: RS-series math + RS-line-NH flag, accumulation counters (up/down ratio, accum/dist day counts, pocket-pivot), extension/parabolic/crowding metrics, state machine with precedence cascade + entry/exit hysteresis + tri-state per-axis evidence, handoff pair detector, regime classifier, rerating-watch conditions, radar-local 2D `_tf_state` path); hermetic tests incl. state-transition + precedence + hysteresis matrix, tri-state nulls, field-guide replay fixtures (GOOGL/UNH shaped) | Pure/no-I/O; frozen constants with LR-R2 provenance comments; Kleene tri-state; exactly-one-state property test under overlapping conditions; no global mtf/cycles.py diffs |
| **W2a — Stores + builder** (PR-2) | `scripts/build_leader_radar.py` nightly (universe resolution; `data/rs_series/` FULL-HISTORY backfill then append; `data/leader_radar/state_history.parquet`; revisions-history append store; cohort_metrics date-file retention widened (commit more days — no engine prune exists); watch-states-by-import (radar assembles bench/sector inputs itself); radar.json artifact + kill-switch + stale SLA); registry 25→27 books + candidates loader (fires inside build_pick_lab loop); dag/synapse (+artifacts, SIGNAL_BUS regen, count tests); daily.yml step (engine job, after build_flow_leaders:533, before build_pick_lab:541) | rs_series depth == ohlcv depth on first run; consumption-not-recomputation test; refire-lockout test; runtime measured (<90s target); zero authority-surface diffs |
| **W2b — Page** (PR-3, after W2a) | `templates/leader_radar.html.j2` + render (lifecycle board, handoff watch, regime banner incl. LR-R5 chips, rerating watch, coverage/young-data banners incl. revisions-uncovered names); nav (+ `<nav class="site-nav">` wrapper); config block; R2 mirror `leaderradar/radar.json` | nav-gap/title-i18n/validated-claims green; rendered page node-linted locally (inline-js CI is site-only); browser screenshot with prod-shaped data |
| **W5 — Terminal tab** (charting-app PR, after W2b lives) | Options-Hub-style "Leader Radar" tab reading R2 `leaderradar/radar.json` (recipe = Leaders-tab precedent #82) | Fixture + absent-state handling; EN/ZH LEX |
| **W6 — Registered maturity work (NOT built now)** | FLW-style verdicts at PL-R4 floors (≥2026-10-15); WA fingerprint pairlets at WA's Layer-3; PE-trajectory leg activation (fwd-PE depth checkpoint 2026-10); 4H universe widening | Preregs frozen per LR-R9 before any computation |

## §4 Parked / rejected

| Item | Why | Revisit |
|---|---|---|
| Any leader probability/score | NW-U29 + R3 + LH-R2 | Never pre-gate |
| NW lobe charter | Two-lobe cap consumed | If L1/L3 slot frees + docket §1 bar |
| Mastermind conviction intake | GAP-U4 + CONST-ART2 (with Flow Leaders' parked intake) | ≥2026-10/12 + shadow track record |
| Sector-rotation prediction from stock signals | Oracle territory + zero-sum identity | Never |
| Short-squeeze/covering legs | No PIT short-interest history (L10) | ~2027+ |
| 13F as positive signal | WA-R2 struck | Never (context/crowding-hazard only) |
| Pre-IPO anticipation claims | No data; naming-risk | Display note only |

## §5 Clocks

- **2026-07-14:** first nightly radar cycle verify (states populated, RS store appending, books registered).
- **2026-08-10:** RS-store depth + state-transition sanity review (do states churn too fast? hysteresis tune = engine version bump); joint with Flow Leaders/IFT clocks.
- **2026-10-01/15:** kernel gate opens (no action unless chartered); PL-R4 floor checks for the 4 book-verdict cells; fwd-PE trajectory leg activation checkpoint; revisions-history depth check.
- **2027-01:** WA Layer-3 fingerprint pairlets review (with WA program).

## §6 Status log

| Date | Event |
|---|---|
| 2026-07-11 | Census (5 lanes). Masterplan adjudicated. Red-team round 1 (2 Opus critics, both SHIP-WITH-FIXES): WA-R8 unlock claim STRUCK (false — winner_case.v1 library is a separate 11-file set; pairlets routed to WA docket); revision chip renamed to level-honest `revision_positive`; precipice fire rule gains rs_line_nh OR-leg (9 revision-uncovered neocloud names stay eligible); precedence cascade + 2-in/3-out hysteresis + state_history store specified in W1/W2a; RS store = full-history backfill (day-one live); 2D radar-local (no global stockdata bloat); regime chip radar-page-only (macro_context fenced); threshold-provenance clause added; universe-exit + refire-lockout rules pinned; W2 split into W2a/W2b. Waves dispatched. |
| 2026-07-12 | LRV-W1 systems wave landed. See §7. |
| 2026-07-12 | LRV-R1(e): `analyst_saturated` (LR-R2 CROWDED chip e) wired from finnhub recommendation-trends via engine/analyst_revisions `consensus_pct`; synapse `analyst-targets` (yfinance .info) investigated and rejected as a buy-share source (consensus key only, no rating counts, zero committed history). LRV-R1(b)'s "same PIT feed" claim for analyst_buy_pct corrected. Coverage keys + banner line + young-data tag added; loader/chip/render tests added to the leader-radar-unit CI job. |
| 2026-07-16 | Operator overhaul order. 4-lane Opus audit; §8 adjudicated (LRV-O1..O8). PR-A truth layer shipped (canonical Zweig, as_of=data-through + freshness/degraded, hysteresis contiguity, days_in_state honesty, changed_today/near_trigger/fire_history, schema v2). PR-B page overhaul dispatched. W-O3 data unblocking queued; W-O4 reachability clock set 2026-08-15. |
| 2026-07-23 | Operator complaint ("radar tells us to buy TRV after it went parabolic and ADBE a falling knife — overhaul this garbage page"). Adjudicated LRV-O9 (§8): entry-read display layer + parabolic sign guard + stance-shelf Tonight's Focus. The state machine had worked as chartered (TRV was flagged Quiet build in June BEFORE its earnings gap); the failure was presentation — the exit-side lens (extension_extreme etc.), Cremers de-escalation, and SUPPRESSED-context trend chips were all computed per row and never synthesized into a glance verdict, so an extended post-gap fire rendered identically to a clean staged name. Also fixed the `--num` SF-Mono-first font stack (serif fallback in Chromium). |
| 2026-07-29/30 | `call_skew_rich` reassessed in the measurement lane before mass activation (~343 names crossed 21 real sessions 2026-07-30, post-#3977 session-true counting). Measured: the daily self-inclusive own-Q80 chip ≈ its mechanical coin (fire 21.1% vs permutation null 22.9%; exact null 5/21 = 23.8% at n=21); daily rr ~memoryless (median lag-1 AC −0.01); the one supra-null structure is market-wide co-firing (obs/null breadth SD 1.9–2.8×; 07-14 fired 37% of names at once). Adversarial review then reversed a first-pass claim: the persistence construction does **not** damp co-firing (common-grid breadth SD 0.0380 vs 0.0144 — it stretches the spike's footprint 1→5 sessions) and it makes noise stickier on both hysteresis paths (triples +163%, runs ≥3 +308%, the `EXIT_N=3` pathway the first pass never computed). Adjudicated LRV-R6 (§7) as three separable decisions: membership retained, construction replaced (window-excluded Q80 over ≥21 real *readings*, ≥3-of-5), and **the vote HELD** — `CROWDED_SKEW_CHIP_ARMED=False` pins the chip to None until a pre-registered n≥60 re-benchmark (~mid-Oct 2026) with three named arming criteria. Zero live states change at land time (verified). Evidence: `research/leader_radar_skew_chip/MEASUREMENT.md` §4. |

## §7 LRV-W1 systems wave — rulings of record

_Dispatched 2026-07-12. Covers input wiring, artifact additions, and data-reality decisions that arose during W1 implementation. State definitions, thresholds, K-of-N sets, fire rules, and hysteresis are frozen in the engine — no changes to those._

**Structural reachability note (LRV-R1a):** Wiring `rs_rank_history` + `peer_median_rs_63d` makes the LEADERSHIP state reachable for the first time. Pre-PR, both fields were always null on every LifecycleInputs instance, so `peer_divergence()` always returned None and the `conc is True` branch could never fire — LEADERSHIP was structurally unreachable. This PR does not change the LEADERSHIP definition; it removes a data-starvation blockage. Fire rules for the two Pick Lab books are untouched: precipice fires on CATALYST_WINDOW entry, onset fires on BREAKAWAY entry — neither reads LEADERSHIP._

### LRV-R1(a) — RS rank history + peer observables wired

Pre-registered-arbitrary constants (frozen; change = engine version bump): `RS_RANK_WEEKLY_CHANGE_WINDOW = 63` sessions for cross-sectional pct-rank input; `RS_LINE_GAP_HIGH_LOOKBACK = 126` sessions for the RS-line gap-from-high display chip.

Builder implementation: all RS series loaded once into a wide DataFrame; `diff(63)` → W-FRI resample → `.rank(axis=1, pct=True)` → per-name `DataFrame(rs_rank)` passed to `LifecycleInputs.rs_rank_history`. `LifecycleInputs.peer_median_rs_63d` wired from same-basket peer median of 63d RS slope (name excluded; primary basket = largest non-dow30/ndx basket). `LifecycleInputs.rs_accel_leader` wired as 21-session change of the name's own 63d RS slope series.

### LRV-R1(b) — insider_cluster wired from quarterly SEC aggregate

Available nightly-lane source: `data/sec_insider/insider.parquet` (index=ticker, columns: n_buys, n_sells, buy_usd, sell_usd, net_usd, quarter; grain = latest quarter per ticker). Chip definition implemented as: latest quarter-end ≤ 120 calendar days from as_of AND n_buys ≥ 2 → True; quarter present but n_buys < 2 → False; ticker absent or quarter stale → None.

**Gap acknowledged:** the LR-R2 spec calls for "≥2 open-market buys in 90d" at per-transaction grain. The available store is quarterly aggregate. Wired as-is (null-honest; chips that can't fire are None, not False — Kleene); per-transaction Form-4 PIT integration is a separate research path (see `sec_insider.py`). ~~`analyst_buy_pct` stays None (gap documented in builder comment) — depends on the same PIT feed not yet in the nightly lane.~~ *Corrected by LRV-R1(e): analyst buy-share never depended on the Form-4 feed (that conflated insider transactions with sell-side ratings); it is fed by finnhub rating counts.*

### LRV-R1(c) — call_skew_rich wired from options snapshots; young-data disclosure

Data reality: `data/options_skew/snapshots.parquet` contains 16 dates as of 2026-07-12 (history starts 2026-06-21). All names are below the 21-observation minimum threshold. Builder reads rr proxy = `atm_call_iv − otm_put_iv` (positive = calls richer); emits `skew_n_obs` in every row's context dict. ~~When `skew_n_obs < 21`, both `rr_25d` and `rr_80th_pctile` are null → `call_skew_rich` = None for all names currently. Chip becomes non-null automatically once history accrues; no code change needed at that point.~~ *Superseded by LRV-R6 (2026-07-29/30): the pre-activation measurement showed the daily self-inclusive construction is ≈ its mechanical coin, so the chip's vote is HELD (`CROWDED_SKEW_CHIP_ARMED=False`) pending an n≥60 re-benchmark. The construction behind the hold is a 3-of-5-session persistence condition against a window-excluded Q80 benchmark over ≥21 real readings (eligible at n≥26). "Becomes non-null automatically as history accrues" no longer holds — arming is a deliberate ruling, not a clock.*

**Sign convention fixed:** skew column in the parquet = `otm_put_iv − atm_call_iv` (verified on AAPL row: skew=−0.0137, atm_call_iv=0.2412, otm_put_iv=0.2275); rr proxy = −skew = positive when calls expensive.

### LRV-R1(d) — basket_corr_now / basket_corr_then wired

`_compute_basket_correlations()` computes mean pairwise 60-session daily return correlation for each theme basket. Guard: < 3 members with sufficient history → None. `basket_corr_now` = last 60 sessions; `basket_corr_then` = window ending 60 sessions ago. Assigned to all basket members via `ticker_primary_basket` map (largest non-dow30/ndx basket per name). dow30/ndx baskets excluded (large-cap indices do not have a single EW basket in this context).

### LRV-R1(e) — analyst_saturated wired from finnhub recommendation-trends; yfinance analyst-targets rejected

Source investigation (2026-07-12): the synapse `analyst-targets` store (`data/analyst/targets.parquet`, collectors/yf_analyst.py, yfinance `.info`) was evaluated as a buy-share source and **rejected** — it carries only the consensus `recommendationKey` string + `num_analysts` + price targets; no strongBuy/buy/hold/sell rating-category counts, so no buy-share is derivable (a consensus label cannot be converted into "% of analysts at buy-or-better"). The store has also never been committed (zero history in git despite `storage: git`).

The LR-R2 chip-(e) spec already named the correct source: **finnhub** `/stock/recommendation-trends` → `data/finnhub/recommendation.parquet` (collectors/finnhub_altdata.py; monthly strongBuy/buy/hold/sell/strongSell counts, ~120-name basket watchlist, append-only deduped on ticker+period). Wired via `engine/analyst_revisions.revision_map` (LR-R13: consume, never re-implement): `consensus_pct` = (strongBuy+buy)/total×100 is the buy-share LEVEL the engine compares to the frozen `CROWDED_ANALYST_BUY_PCT = 85`. The level-vs-delta doctrine in engine/analyst_revisions (revisions score the DELTA, never the level) is not violated: the crowding chip is exactly the saturation-of-the-LEVEL read the field guide pre-registered as an exit-side marker; no threshold or K-of-N change.

Null-honesty: store absent / ticker outside the finnhub watchlist / latest monthly period older than 45 days (`_ANALYST_MAX_AGE_DAYS`, current-or-previous month) → `analyst_buy_pct = None` → chip null, never False. Coverage disclosed in `coverage.analyst_covered` / `analyst_uncovered` / `analyst_note` and on the page banner; store-birth date is read from the parquet's `_first_seen` (never hardcoded — the 2026-06-20 charter date was not a data date; the collector was key-blocked until 2026-07-16, see W-O3) → young-data tag per LR-R14. Row context carries `analyst_buy_pct` + `analyst_n` so the displayed chip always has its receipt. Universe-coverage note: the finnhub watchlist (`basket_members(cap=120)`) is a strict subset of the radar universe (~150–190 names) — Dow-30-only names are the main uncovered class; widening the collector cap is a collector-lane decision, not a radar one.

### LRV-R2 — early_entry artifact added (additive; no state-definition change)

New `early_entry` key in radar.json payload. Rows: CW/QA state names + SUPPRESSED names with ≥1 True lead chip. Sort: deterministic tuple `(state_bucket CW=0/QA=1/SUP=2, −k_true, days_in_state asc nulls-last, ticker)`. No fused score anywhere in the sort (constitutional law). Fields: ticker, state, k_true, n_avail, days_in_state, fire_precipice, fire_onset, rs_line_gap_pct, display_chips.

### LRV-R3 — display_chips sub-dict added (additive; display-only)

New `display_chips` key on every main row. Fields: `revision_momentum_90d` (tri-state: est_chg_90d > 0 from revisions/latest.parquet), `eps_dispersion_norm` (raw float), `rs_line_gap_pct` (percent below RS line's 126d high; 0.0 = at high). These three observables are **permanently fenced**: they must not enter any K-of-N gate, state condition, or fire rule. `rs_line_gap_pct()` is defined in `engine/leader_lifecycle.py` with display-only provenance comment.

### LRV-R4 — handoff_context artifact added (additive)

New `handoff_context` key in radar.json payload. Per theme-basket fields: `basket`, `n_members`, `is_extended` (from `extended_baskets`), `rs_21d_slope_sign` (median of member 21d RS slope signs: +1/−1/0/None), `extension_pctile_vs_200d` (float 0..100 or None). The EW basket-close series is assembled from `ohlcv_map` member closes (aligned → ffill → mean); the percentile is computed by `basket_extension_pctile()` in `engine/leader_lifecycle.py` — the same helper used inside `extended_leg()` so the two callers cannot drift. Returns None when fewer than 50 non-null SMA200 observations are available.

### LRV-R5 — count_k_true_n_avail() defined once in engine (LRV-R1a)

`count_k_true_n_avail(state, evidence) → (k_true, n_avail)` added to `engine/leader_lifecycle.py`. Per-state chip-set mapping: QA=5-chip set, CW=3 ignition triggers, CROWDED=7-chip set, SUPPRESSED=4 core conditions, all others=full evidence dict. Used for the early_entry board and for `k_true`/`n_avail` fields on every main row. Docstring frozen; chip sets match the K-of-N definitions in LR-R2 exactly.

### LRV-R6 — call_skew_rich: construction replaced, vote held (2026-07-29/30; pre-activation measurement of record)

_Adjudicated from `research/leader_radar_skew_chip/MEASUREMENT.md` (reproducible: `measure_call_skew_chip.py`, seeded) one session before mass activation, then amended after independent Opus review (§4 of that doc). Measurement-lane amendment: the CROWDED 7-chip set and `CROWDED_K_OF_N=3` are untouched, and no state definition, threshold, or fire rule elsewhere changes._

**Finding.** As wired by LRV-R1(c), the chip fired when the latest rr ≥ `daily.quantile(0.80)` of the name's full accrued history **including today**. At n=21 that is the 17th-order-statistic rank event — mechanical probability 5/21 = 23.8% under exchangeability — and on the real store the chip is statistically indistinguishable from that coin (fire 21.1% vs within-name permutation null 22.9%; next-day persistence 28.3% vs 22.4%), because daily rr in the young store is ~memoryless (median lag-1 autocorrelation −0.01). The one decisively supra-null structure is market-wide co-firing (observed/null breadth SD 1.9–2.8×; 2026-07-14 fired 37% of all names simultaneously) — the wrong granularity for a per-name state chip. #3977's review measurement (62/350 = 17.7%) replicated exactly.

**Ruling — three separable decisions.**

1. **Membership: retained.** Confluence-input retention law; the skew↔crowding mechanism is untested, not refuted, and the store is 20 sessions old with a credible extraction defect (varying-tenor mean) behind the AC≈0.
2. **Vote: HELD.** `CROWDED_SKEW_CHIP_ARMED = False` (frozen engine constant) pins the chip to None, so it never enters `n_avail`, until the arming condition below is met. Retention does not compel a vote: the chip's only supra-null structure is a *market-wide* factor, and a market dummy inside a per-name k-of-n does not "confirm other signals when they align" — it aligns with the tape. The count keeps accruing in the row context; holding costs nothing.
3. **Construction: replaced, in place and tested behind the hold.** Session-true series (depends on #3977's `session_rows` filter, carried in the same PR), `dropna()` **before** the gate and the quantile so `min_obs` counts real readings not frame rows (vacuous-N-gate class); benchmark `rr_80th_pctile` = Q80 of prior history **excluding the last 5 observed sessions**, ≥21 readings → eligibility at n≥26; `skew_rich_last5` = count of that window ≥ benchmark; engine fires at `≥ CROWDED_SKEW_PERSIST_MIN (3)` of `CROWDED_SKEW_PERSIST_WINDOW (5)` once armed. `rr_25d`/`rr_80th_pctile` move from LifecycleInputs to row-context receipts — **not** a pairwise chip read; the chip is the count.

**Why the construction change alone was not enough (honesty rows, corrected after review).** The persistence form is the better *shape* — rarer, coherent episodes, immune to a one-day market spike — but on this store it turns a scattered coin into a **sticky** one, and stickiness is exactly what a state gate integrates: pairs +49%, triples +163% (the `HYSTERESIS_EXIT_N=3` pathway the first pass never computed), share of TRUE runs ≥3 sessions +308%. It also does **not** damp co-firing: on a common eval grid PERSIST's breadth SD is 0.0380 vs SELF's 0.0144 — 2.6× larger — because it cuts the 07-14 peak (37%→23%) while stretching that market-wide event's footprint from one session to five, supplying four consecutive `ENTER_N=2` confirmation windows at 16–23% board-wide. The first pass's "damped co-firing" and "halved standing TRUE-vote mass" claims were a grid confound and a marginal-rate-as-evidence error; both are retired.

**Arming condition (pre-registered).** Rerun the battery at **n≥60 real sessions (~mid-Oct 2026)**; arm only if all three hold: (i) PERSIST's fire rate separates from its within-name permutation null beyond ±2sd; (ii) common-grid breadth SD no larger than SELF's; (iii) cross-chip lift estimable and non-negative. Otherwise extend the hold or retire the construction. Arming = one constant flip plus a ruling row here.

**Parameters:** (3,5) came from state semantics (5 sessions = one trading week; k = its majority) and null math; nothing was fit to outcomes — none exist (`validation_gate.json` is `insufficient_history`), and zero live states change at land time (verified). The published (k,m) grid shows every neighbouring cell sitting at its own null, which is why the vote is held rather than re-parameterized. Follow-up lane (not scheduled): the loader's tenor-mean mixes a varying tenor set day to day — the first thing to fix if a future upgrade is to be *measurable*.

## §8 Overhaul audit + truth layer — rulings of record (2026-07-16)

_Provenance: operator complaint 2026-07-16 ("stale data — Broad advance with tech down so much today… audit this system and do a complete upgrade overhaul, not useful enough and lacking") → 4-lane Opus adversarial audit (regime/engine correctness, builder/ops freshness, design-doctrine compliance, charter-vs-shipped usefulness) → this adjudication. The complaint reproduced exactly: the page stamped build wall-clock "As of 2026-07-16" over data through 07-15 close, and the "Broad advance" label rode a degenerate Zweig flag._

### Audit facts (the rulings stand on these)

1. **`_zweig_flag` was degenerate:** it returned True when ANY single day in the trailing 10 breadth rows had adv-share ≥ 0.615 — measured to fire on **98.1% of all trading days since 1962**, while the page printed "~18 occurrences in the modern era". The 2026-07-10 session (adv-share 0.680) kept `broad_thrust` lit through the tech selloff. The canonical ZBT (10d SMA of adv-share crossing from ≤0.40 to ≥0.615 within 10 sessions) fires **70 times since 1962, 8 since 2020, last 2023-11-10** on our breadth store.
2. **`as_of` was build wall-clock**, never the data-through date (which the builder computed and discarded); no input store's own freshness was captured; `regime.json` was read one session stale *by construction* (build_dispersion_regime ran after build_leader_radar in daily.yml).
3. **The state-history freeze (#2605) left permanent scars:** `days_in_state` = trailing matched store ROWS (not sessions) → "Day 1" on all 173 names; hysteresis counted rows-not-sessions so confirmations could jump the 07-12/14 hole; pre-07-11 entry dates are unrecoverable.
4. **Structural darkness:** CATALYST_WINDOW = 0 names ever, `plab_leader_precipice` never fired (starved by the freeze, NOT a threshold problem — do not loosen LR-R2 constants); rerating_watch `multiple_compressed` null 11/11 (valuation fields absent); analyst/crowding lane fully dark (`data/finnhub/recommendation.parquet` absent in the collect lane — collector gap, not radar bug); handoff_pairs = 0 rows; top5_share mixed `r*cap` with bare `r` units on fallback and was computed over the radar universe vs LR-R5's NDX spec.
5. **Doctrine violations (Tier 1):** wall-clock as-of; undated regime stance with no stance word; "4 of 5 signals" K-of-N at rest; no what-changed-today view; no outcome receipts; field-guide ZH lines carrying bare EN slugs.
6. **False positive corrected:** the R2 mirror was NOT missing — `mirror_flow_idx --radar` (daily.yml L1166) publishes `leaderradar/radar.json`; W5's data source exists.

### Rulings

- **LRV-O1 — Canonical Zweig.** `_zweig_flag` = canonical ZBT transition (10d SMA ≤0.40 → ≥0.615 within 10 sessions); returns `(flag, fire_date)`; regime dict carries `zweig_fire_date` + `adv_share_10d`. The 0.40/0.615 numbers are canonical-definition constants, not LR-R2-frozen thresholds — no engine version bump. Page rarity copy must cite OUR measured base rate (70 since 1962 / 8 since 2020), not the folk "~18 since WWII".
- **LRV-O2 — as_of = price data-through (schema `leader_radar.v2`).** `as_of` is the SPY-store data-through date; `built_at` = wall clock; new `freshness` block (price/breadth/regime/revisions/state-history throughs + built_at). This also fixes the latent LR-R8 PIT bug (fires were dated the build day, not the data day). Consumers verified safe: pick-lab candidates read `rows[]` only; build_site `_asof_age_days` parses date-only. One `today` derived from `latest_date` threads all ledger row dating (kills the PT-local `date.today()` skew).
- **LRV-O3 — Regime honesty.** top5_share computed over the NDX slice per LR-R5 (spec compliance); uncovered names DROPPED from the cap-weighted sum (the mixed-units fallback is dead); <20 caps → pure `equal_all`. Regime dict discloses `breadth_universe_n` (~502) vs `desk_universe_n` (173) + `regime_as_of` — the label's breadth legs speak for the broad market, and the page must say so (PR-B).
- **LRV-O4 — Freshness guards.** Top-level `degraded[]` plain-string list (state-history/regime/breadth lagging price data); `history_since` + `history_gaps` disclose the store's birth and holes; healthcheck data-freeze witness added for `data/leader_radar/state_history.parquet` (the exact store that froze while every probe stayed green). daily.yml ordering fixed: dispersion regime builds BEFORE the radar reads it.
- **LRV-O5 — Hysteresis contiguity guard.** Entry/exit streaks only count across session-adjacent rows INCLUDING today's join (with `session_calendar` + `today` threaded); legacy call shape unchanged; `HYSTERESIS_ENTER_N/EXIT_N` untouched — correctness guard around the frozen rule, no version bump, no ledger reset.
- **LRV-O6 — days_in_state honesty.** Per-row `tracked_sessions` (honestly named; `days_in_state` kept as alias for template back-compat); new nullable `state_entry_date` column stamped on confirmed-state transitions going forward (old-schema files merge dtype-safe — tested against the #2605 crash class). Pre-2026-07-11 ages are unrecoverable; UI copy must say "tracked since", never imply real age (PR-B).
- **LRV-O7 — Delta/receipt artifact keys (display-tier, deterministic, no fused score).** `changed_today` {entered, exited (universe-gated against transient per-ticker failures), fired}; `near_trigger` (QA one-chip-away or ≥1 CW ignition chip true; `missing_chips` restricted to the state's own K-of-N set, False-only); `fire_history` (last 20 fire_log rows joined to pick-lab grades via `engine.pick_lab.ledger` — `ret_excess_spy` + matured/accruing, no invented letter grades).
- **LRV-O8 — Fire drought is a data scar, not a threshold problem.** LR-R2 constants stay frozen. Re-examine CATALYST_WINDOW reachability only after ≥20 contiguous sessions of healed state history (≈2026-08-15), through the LR-R2 change process if warranted.
- **LRV-O9 — Entry-read display layer + parabolic sign guard (2026-07-23; operator complaint provenance).** (a) `parabolic_flag` gains a POSITIVITY guard (both window returns > 0) — correctness guard around the frozen rule per the LRV-O5 idiom, no constant change: the unguarded ratio was vacuously true whenever ret_13w < 0, so the blow-off chip fired on post-crash bounces (45 of 173 names flipped True→False on fix day, incl. ADBE/CRM mid-drawdown) while remaining honest on true accelerations. (b) New display-tier `entry_read()` in the engine — deterministic categorical re-expression of ALREADY-computed chips (downtrend legs → `turnaround_watch`; extension_extreme/monthly_rsi_80 → `extended`; clean → staged/building/dormant; earnings de-escalation → caveat; `basis` names the driving chips). DISPLAY-ONLY FENCE (LRV-R3 idiom): never enters any K-of-N set, state condition, fire rule, or rows[] sort key. `parabolic` is excluded from the stretch set outside its chartered CROWDED context (fires trivially off a flat base = the fresh-ignition class the desk surfaces). (c) Page: Tonight's Focus rebuilt as stance shelves (Worth a look / Already moved—don't chase with the +X%-vs-50d receipt / Turnaround attempts—downtrend intact), entry flags on ignition+quiet lane cards, base-rate honesty footer at glance tier. Fires and ledgers untouched — an extended fire still accrues its Pick Lab window (evidence law); it just can no longer *render* as a celebration. |

### Wave plan

| Wave | Scope | Status |
|---|---|---|
| **PR-A — truth layer** | LRV-O1..O8 engine/builder/workflow/tests (this PR) | SHIPPED 2026-07-16 |
| **PR-B — page overhaul** | Doctrine-compliant regime capsule (stance word + event dating + "data through X · nightly" token); degraded/gaps banners; Changed-today strip; near-trigger shelf; fire/outcome receipts panel; K-of-N demoted to Tier-2 receipt (dot-bar + plain descriptor at rest); field-guide ZH slug fixes; young-data chip tags; measured Zweig rarity copy | DISPATCHED 2026-07-16 |
| **W-O3 — data unblocking** (separate PRs, not this session) | finnhub recommendation collector restore — **ROOT-CAUSED 2026-07-16**: the collector was fully wired all along (collect.py registry, daily.yml env, `git add data/` commit path, no gitignore block); the sole blocker was the `FINNHUB_KEY` repo secret, absent since the store's birth (nightly logs: `finnhub_altdata -> blocked: FINNHUB key not set`, zero commits ever under data/finnhub/). Operator added the secret 2026-07-16T23:59Z — 35 min AFTER that night's collect job snapshotted its secrets, so the first keyed run is the 2026-07-17 nightly. Expected on first accrual: `analyst_covered` ≈ 57/173 (watchlist∩universe measured 07-16); collect step grows ~5.5 min (360 calls at 0.9s pace); key VALUE unverifiable until the run — if it errors, the log shows fetch failures, not `blocked`. No lane change shipped: the adapter predates the collect_tail split and stays in the main collect pass per its registry placement. Remaining legs: rerating `multiple_compressed` valuation wiring; handoff_pairs made concrete (rotation-events rail idiom, LR-R4 vocabulary fences); intraday regime-capsule re-stamp lane (mirrors `refresh_regime_if_stale` precedent — re-stamps the regime block ONLY, never advances ledgers) | QUEUED (finnhub leg RESOLVED — verify 07-17 nightly) |
| **W-O4 — reachability review** | CATALYST_WINDOW/fire-rate review after ≥20 healed sessions | CLOCK 2026-08-15 |
