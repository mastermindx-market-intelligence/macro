# SCE V6 — initialization, value-state semantics and current source gate

Status: PARTIAL research continuation. No trading, production, worker or source-runtime authority.

Protected procedure for this continuation: Mastermind `f3f2d9155796876009f2d427bfdecc7ee7b63e74`, Sol Skillpack 1.0.1. This is the same records carrier and PR #7009. It extends, and does not erase, `SCE_HISTORY_ACCESS_AND_REPLAY_BOUNDARY_2026-09-09.md`.

## Material research ruling

The retained filter experiment's close agreement with the August printed threshold depended on a chart-supplied initial state. Warming the same coefficients from the retained historical price series materially changed the endpoint. A separate affine common-seed feasibility calculation tested a declared constant-EMA grid against the earlier and later uncensored image levels under three engineering vertical error budgets. No tested alpha/seed pair satisfied the entire path under those budgets. This is a bounded formula-family result, not a proof over all filters, all continuous parameters or all image/date/vintage uncertainty. It does not establish a defect in the creator's strategy.

An adversarial tolerance check on the same grid found path-only feasibility at a uniform vertical half-width of approximately 4.48659 pixels. That check does not impose the final printed rounding interval. Because increasing the four-pixel budget by about half a pixel restores a path fit, the narrow rejection must not be described as robust exclusion of the EMA family. The defensible ruling is that endpoint resemblance, image tolerance and chart initialization do not establish a recovered model.

The prior August model is not established as the unchanged September Systematic Core. A second independently observed, version-matched threshold and a complete state transition remain more informative than tuning portfolio CAGR. An initialization fitted from the target chart must not silently become a historically generated model state.

## Original value-state research consumer

A stateless local adapter now consumes two completed, calendar-selected profile facts with source IDs, revisions, original availability, profile kind, grid identity and integer-tick prices. Its CLI was exercised end to end on synthetic facts. Availability is unverified by default. Missing, incomplete, future-only, retracted, ambiguously revised, differently gridded or daily-proxy facts cannot create an ordinary hold/exit conclusion.

The exit-evidence predicate is an original research formulation, not a recovered Donald Jones rule or current nextSignals implementation. It has no position, order, sizing, lifecycle, producer or persistence authority. The real qualified profile feed is not connected or production-proven. Existing Macro daily-bar profile approximations keep their declared meaning and are not relabelled as traded-volume/TPO facts.

## Dated native source observation

A fresh read through the existing canonical ThetaData store resolver failed during this continuation. Native inspection found the existing root and manifest, but the EOD/OI/Greeks links pointed into the intended external volume, which was unavailable at the observation. This is a current access failure, not evidence of data destruction, a known cause, or revoked vendor entitlement. Earlier successful reads remain historical evidence; their old manifest does not establish current liveness.

An information-only observation was sent to the existing private CEO control room. It was explicitly not a commission, START, workstream reassignment or claim of an active recovery worker. No mount, path override, collector restart, subscription change, new Terminal or duplicate store was attempted. Detailed private host paths and receipts remain in the Chairman's research artifact, not this public record.

The existing source/host owner must reconcile the intended volume before a new stored-data study can run. That source dependency does not hold retained-input mechanism research idle and does not rewrite the historical AD-1T1 acceptance.

## Descriptive options-context boundary

A local 30-day, approximately 25-delta put-minus-call IV-skew selector and a mature 21-session label protocol were specified and synthetically tested. The underlying target is explicitly a source-price change, not an executable or total-return portfolio. The retained XLE split was checked against the issuer announcement and crossing windows are excluded until adjustment semantics are explicit. Calendar-eligible counts are not qualified empirical observations or independent sample sizes.

The empirical population was not extracted and no association, bootstrap or signal result is claimed. A remote file append was refused by the tool safety check; the incomplete staged file was not imported or completed through another writer. The earlier broad portfolio/P&L operation was not retried through another tool.

A separate algebraic check on retained prior-day Greeks and next-day investigative outlays confirms that equal premium is not equal reference delta/gamma. It is not contemporaneous matched-risk performance and creates no sizing recommendation.

## Verification and continuation

Ninety-two V6 local tests passed for affine feasibility, initialization, minimum-width sensitivity, profile clocks/revisions/nulls, closed JSON input, descriptive skew selection, label maturity, split crossing and reference-exposure algebra. These are private research-code checks, not this records PR's CI and not proof of alpha. An initially invalid zero-alpha test fixture was corrected to respect the existing open-interval alpha contract; the failed and final passing logs are retained.

Next research action: supply one real, source-qualified profile pair for a version-matched disclosed transition, run the existing stateless research consumer, and preserve its observed or unavailable result without outcome fitting. Separately seek a second independent numerical threshold before claiming a recovered core rule. The current source owner reconciles the intended existing volume in parallel; no recovery execution is inferred from the FYI.

Full SCE reconstruction, economic validation, forward evidence and useful product delivery remain unfinished. Source CI, merge, research acceptance, production and final strategy acceptance remain distinct.
