# GD-1 Current-Event Point-in-Time Replay — PRE-REGISTRATION

**Family:** `grey_deer_gd1_v1`
**Instrument:** read-only reconstruction over existing organs and first-available
source clocks. No production engine, no new score, no Prophet/Portfolio action.
**Packet:** Fable hardened execution packet
`GROK_GD1_HARDENED_SCIENTIFIC_REPLAY_PACKET_2026-08-19` (GD-1A + GD-1B).
**Freeze date:** 2026-08-19, before any August 2026 incident outcome column is
opened for this study.
**Registration SHA:** *filled in this file's §0 after the freeze commit lands.*
**Workstream:** `WS:GREY-DEER`
**Program parent:** `market-regime-risk` (context-only; this replay inherits
that authority class and grants none of its own).

This document freezes event species, hypotheses, outcomes, baselines, feature
families, missing-data law, effective-N, splits, and promotion metrics. Changing
any of those after outcome access requires a new prereg version and Fable
approval. The current incident may illustrate a construction; it may not be the
sole positive case that supports one.

---

## §0 Freeze attestation

### 0.1 What has not been read

Before this file is hashed, this session has not opened August 2026 organ
verdicts, Prophet board returns, PBOC August classification outcomes, SK Hynix
repair-path returns, or any other result column of the incident. Organ *source
code*, Temporal Data Standard, DNR rows, and already-published historical
auction-study reports (sample ending 2026-06/07) were read as priors.

### 0.2 Authority precedence (packet §2)

1. Grey Deer architecture freeze — **ABSENT from this checkout and from GitHub
   search** (no open/merged/closed Macro or Mastermind PR, issue, or tracked
   file named Grey Deer architecture / GD-1 / GD-H1). Brand-site PRs #797/#799/#833
   are unrelated. This gap is recorded, not filled. Hypotheses below are copied
   verbatim from the Fable command packet, not invented as a substitute freeze.
2. Fable Grey Deer command packet (this prereg's source for GD-H1–H8).
3. Mastermind Temporal Data Standard
   (`research/MASTERMIND_TEMPORAL_DATA_STANDARD.md`) and Market Memory
   as-known-at law.
4. Existing expert methodology / preregistration / DNR.
5. Historical code/artifact version effective at each observation.
6. Primary/official source clocks.

Do not invent a different taxonomy, score, or policy.

### 0.3 Registration SHA

Filled after `git rev-parse HEAD` on the commit that contains only this prereg
plus the workstream record and no incident-result artifacts:

```
registration_sha: 663fb02b500c966785628d52c709f0d40c27a146
registration_commit_time_utc: 2026-08-19T02:10:15-07:00
tree_sha: e826c87299ba8b7a0b64b232c91076fa7a94cb36
content_sha256_at_freeze: 13df565dadad787fe64ee00e67e6d1f9d04dff0fdc79702148fa321964ef17b3
```

---

## §1 Event species and window

**Species:** a duration × broken-leadership × cross-market contagion episode,
with optional Treasury-auction and PBOC-funding legs, and a later
issuer/policy repair attempt. The architecture is not tuned to one hindsight
story. Fragility, trigger, transmission, breakdown, contagion, and repair are
separate clocks.

**Begin:** no later than session `2026-08-01` (exchange-local, first regular
session on or after that date in each venue).

**End:** the first of:

- `REPAIR_CONFIRMED` — packet §17 persistence through two settled sessions
  across trigger, correlation, breadth, and exposed-cohort state;
- `REPAIR_FAILED` — retest/new low after the first repair impulse and the
  original episode re-armed;
- `REPAIR_UNRESOLVED_AT_CUTOFF` — evidence cutoff of this replay, declared
  explicitly.

Do not truncate at the selloff low.

**Historical support sample (required; current incident is not the only case):**
every NYSE session from `2016-01-04` through `2026-07-31` inclusive, plus the
matching Asia sessions, on which the frozen feature families can be built
point-in-time. Design-era calibration of any percentile/z cutoff uses only
this sample. The August 2026 window is confirmatory / coverage, not a
threshold-fitting set.

---

## §2 Hypotheses — frozen verbatim from the packet

Do not paraphrase these into a new taxonomy. Operationalizations below are
measurement rules, not restated claims.

### GD-H1 — duration × broken leadership

When leadership is already broken/dislocated, accelerating long yields increase
the hazard of a ≥3%/≥5% residual drawdown in high-duration equities over 1 and
3 sessions.

### GD-H2 — volatility acceleration conditional on fragility

Volatility/realized-variance acceleration adds information about near-term
transmission when leadership is already fragile, even if VIX level itself is
not historically extreme.

### GD-H3 — Treasury auction tail as conditional trigger receipt

Auction concession/tail has incremental information only when duration
fragility exists and long-yield/rate-vol confirmation follows. Standalone
“bad auction predicts equities” remains disallowed.

### GD-H4 — defensive rotation as transmission confirmation

High-beta breadth damage plus positive defensive residual strength is a
same-session confirmation of broadening transmission, not necessarily a
leading predictor.

### GD-H5 — China Prophet board-health confirmation

Cross-sectional collapse of the canonical China board adds incremental
real-time evidence of market-wide transmission beyond the index alone.

### GD-H6 — PBOC zero-operation sign is conditional

Zero 7-day reverse repo has no fixed sign. Adverse interpretation requires
net drain and funding/FX/equity transmission stress.

### GD-H7 — repair impulse versus durable repair

Issuer/policy support without cross-asset repair has elevated probability of
failed repair/retest versus a repair that broadens across trigger,
correlation, breadth and exposed-cohort state.

### GD-H8 — contagion sequence

Clock-ordered residual damage across regions adds descriptive and potentially
predictive information beyond one global-index return, but any lead claim
requires a mature PIT sample.

---

## §3 Operationalizations (measurement rules, not new claims)

All times are tz-aware UTC in storage. Session dates are derived from the
venue calendar (`lib/nyse_calendar.py`, `lib/cn_calendar.py`,
`lib/hk_calendar.py`, plus the Korea/Japan/Taiwan calendars already used by
the intl organs). A naive timestamp is refused (TDS §4).

### 3.1 Leadership already broken/dislocated

Use the **existing** `engine/leadership_crack.py` state machine
(`SCHEMA = leadership_crack.v1`), not a restated threshold:

- `leadership_state ∈ {CRACKING, BROKEN}`
- `dislocation` flag as the organ defines it (CRACKING/BROKEN and SPY within
  3% of its 63d max)

Authority at freeze: **display-only**. Observation and authority are recorded
on separate columns. A BROKEN print is not an authorized product action.

If the historical artifact at `t` is missing or is recomputed history written
after `t`, the organ is replayed from truncated inputs at the code SHA in
force at `t`. Divergence is explained; a later rewrite is not proof of
emission at `t`.

### 3.2 Accelerating long yields

Continuous features, design-era percentile gates only:

| id | construction |
|---|---|
| `d_dgs10_1d` | first-available DGS10 1-session change |
| `d_dgs30_1d` | first-available DGS30 1-session change |
| `d_dgs10_3d` | 3-session change |
| `d_dfii10_1d` | first-available 10y real yield change, if the vintage/release clock exists; else `UNAVAILABLE` |
| `accel_long_nom` | 1 if `d_dgs10_3d` **or** `d_dgs30_3d` exceeds the design-era (≤2026-07-31) 80th percentile of the same series |

No post-incident retuning of the 80th percentile. Missing real-yield vintage
→ GD-H1 real-duration leg `UNAVAILABLE`, nominal legs still testable.

### 3.3 High-duration equity residual

Residuals are vs the **same-session** SPY close-to-close return (US) or the
regional benchmark named below (Asia). No beta estimated on the incident
window.

US high-duration / high-beta book (existing instruments only):

- indexes: QQQ, SMH, SOXX, IWM
- `leadership_crack` cohort keys already in code:
  `ai_semiconductors`, `ai_infra`, `memory_storage`, `semicap_equipment`
- equal-weight check: RSP residual vs SPY (descriptive, not a gate)

Residual of name/basket `i` at horizon `h`:
`resid_i,h = (P_i,t+h / P_i,t − 1) − (P_bench,t+h / P_bench,t − 1)`.

### 3.4 Volatility acceleration (GD-H2)

Existing series only. **Absolute VIX thresholds are disallowed**
(`DNR:KILL-ABSOLUTE-VIX-THRESHOLDS`).

| id | construction |
|---|---|
| `vix_level` | VIX close, baseline only |
| `rv_spy_5d` | SPY 5-session realized variance |
| `drv_spy_5d` | 1-session change in `rv_spy_5d` |
| `vol_accel` | 1 if `drv_spy_5d` exceeds design-era 80th percentile |
| `vix9d` / `vvix` / VIX term | used only if historical depth **and** rights exist; else `UNAVAILABLE` |

GD-H2 tests whether `vol_accel` adds hazard information **conditional on**
`leadership_state ∈ {CRACKING, BROKEN}` vs the VIX-level baseline.

### 3.5 Auction tail / concession (GD-H3)

Open construction only (packet §13):

```
broken duration-sensitive leadership
× lawful auction-tail/concession observation
× subsequent long-yield/rate-vol confirmation
→ high-duration equity residual hazard
```

Two legs, never collapsed:

1. `auction_tail_wi` — stop-out minus when-issued, **only** if a lawful PIT
   WI series exists with `available_at`. If not, this leg is `UNAVAILABLE`.
   A daily yield difference is **not** renamed “auction tail”.
2. `auction_concession_proxy_v1` — separately named proxy: pre-window TLT
   close-to-close return from `t−3` through auction-day close. This is the
   construction already used in `reports/d2-rates-calendar-flows-phase0.md`.
   It is a proxy, not a tail.

`absorption_z` from `engine/treasury_supply.py` (bid-to-cover / indirect /
dealer z) may be attached as a **descriptive** covariate. It is **not** a
challenger: SLF-006 is already NULL/NO-GO
(`reports/slf006-auction-absorption-phase0.md`). Standalone “bad auction
predicts equities” remains disallowed. D2 auction-cycle rebound is already
null on its registered gates.

Long-yield / rate-vol **confirmation** after the auction: `accel_long_nom`
or MOVE/realized rate-vol acceleration on the auction session or the next
lawful US session. Confirmation is a later clock, not part of the trigger
receipt.

### 3.6 Defensive rotation (GD-H4)

Same-session confirmer, not a lead test unless a separate mature PIT sample
is later registered.

- high-beta breadth damage: share of the leadership-crack cohort with
  session residual vs SPY ≤ −2%
- defensive residual strength: equal-weight residual of XLU, XLP, XLV vs
  SPY > 0 on the same session

Both must print on the **same** US session to fire `gd_h4_confirm`.

### 3.7 China Prophet board-health (GD-H5)

Canonical China board = the production board definition in force at `t`
(do not rebuild with Grey Deer inputs). Metrics, all computed on the **raw**
candidate population:

- board median session return
- positive share
- share below −2 / −3 / −5%
- featured-lane median
- sector-concentration Herfindahl
- simultaneous invalidation count (existing invalidation flags only)

If historical **intraday** board quotes were not retained, GD-H5 intraday
leg is `UNAVAILABLE`. Screenshots are qualitative receipts only, never a
quantitative dataset. EOD board state, if committed at a lawful `served_at`,
may be used as an EOD confirmer and must be labeled EOD.

### 3.8 PBOC (GD-H6)

For every OMO day record separately (packet §14): instrument/tool, tenor,
announcement vs result, maturities, gross injection, net injection by tenor,
DR001/DR007 response, policy-rate relation, CGB/CNH response, A-share
breadth response.

Classify **only after transmission**:

`DEMAND_QUIET | SHORT_TENOR_DRAIN | TOOL_MIGRATION | MEDIUM_TERM_ROLLOVER | ACUTE_SUPPORT | UNKNOWN`

A zero 7-day reverse-repo print with soft funding rates is **not**
tightening. Zero 7-day has no fixed sign.

### 3.9 Repair (GD-H7)

Separate, in this order:

1. issuer-only (SK Hynix / named support name)
2. memory/semiconductor cohort
3. regional index (KOSPI, then Nikkei / TW / HSI / CSI300 as available)
4. US futures / next US cash session
5. rates / volatility
6. breadth / correlation
7. persistence to close and next close

`REPAIR_IMPULSE` = first official company/policy support event with a
primary `published_at`.
`REPAIR_CONFIRMED` = impulse plus broadening across (2)–(6) that persists
through two settled sessions.
`REPAIR_FAILED` = new low or retest of the impulse-session low in the
exposed cohort after the impulse, before confirmation.

A broad index bounce carried by one supported mega-cap with fragile
breadth is **issuer-only**, not confirmed repair. Report both.

### 3.10 Contagion sequence (GD-H8)

Regions, each on its **next lawful market clock** after the origin
observation: KR, JP, TW, HK, CN, US. A region “confirms breakdown” when
its benchmark residual vs its own 20-session median is ≤ −2% **or** its
existing Risk Radar / Market State organ (whichever is defined for that
venue) prints a deterioration state already in that organ’s vocabulary.

Descriptive sequence is always reportable. A **lead** claim requires a
mature PIT sample (design-era event-level N declared in §7) and is
otherwise `UNDERPOWERED — DISCLOSED`.

---

## §4 Primary outcomes — exact labels

Do not move these after the incident is inspected.

### 4.1 Broad market

For each of `{SPY, QQQ, IWM, RSP, EWY, EWJ, EWT, EWH, FXI}` and each
horizon `h ∈ {1,3,5,10,21}` sessions in that venue:

- `dd_h` = `min(P_{t+h}/P_t − 1, 0)` from the observation price
- `mae_h` = minimum close-to-close path return over `1..h`

Intraday MAE is used only if a genuine PIT intraday history exists;
otherwise the close path is labeled `mae_h_close` and intraday MAE is
`UNAVAILABLE`.

### 4.2 High-duration / high-beta residual

- `resid_dd_h` = `min(resid_i,h, 0)` vs the §3.3 benchmark
- `cohort_median_resid_h`
- `breadth_lt_m2` / `breadth_lt_m3` / `breadth_lt_m5` = share of the
  leadership-crack cohort with same-session residual ≤ −2/−3/−5%

Hazard labels for GD-H1 (binary, session-level):

- `y_resid_dd_ge3_1s` = 1 iff cohort-median residual drawdown ≤ −3% over 1 session
- `y_resid_dd_ge5_1s` = 1 iff ≤ −5% over 1 session
- `y_resid_dd_ge3_3s`, `y_resid_dd_ge5_3s` analogously

### 4.3 Crowded-winner liquidation

Prior leaders = names in the leadership-crack cohort that, at `t−21`, were
in the top tercile of 63d residual vs SPY (design-era definition, not
re-ranked on the incident).

- cohort median loss over each `h`
- downside correlation = average pairwise correlation of negative residuals
  over the 10 sessions ending `t+h`
- fraction of prior leaders with residual ≤ −5% over `h`
- failed-rebound frequency = share that are below their `t` price at `t+5`
  after a same-session bounce ≥ +1%

### 4.4 China board damage

The six metrics in §3.7, EOD and (if retained) lawful intraday snapshots.

### 4.5 Contagion

- `n_regions_confirm` by the next lawful clock of each region
- `dt_first`, `dt_second`, `dt_third` = time from origin observation to
  the 1st/2nd/3rd region confirmation

### 4.6 Repair

- `retest_new_low` after impulse (bool + timestamp)
- `breadth_resid_stabilized` (bool)
- `trigger_decelerated` (bool)
- `persist_1s`, `persist_2s`

---

## §5 Baselines

Every challenger must beat the relevant simple baseline, not just the
previous dashboard. Baselines are **existing organs or trivial rules**:

| id | baseline |
|---|---|
| `b_market_state` | slow Market State (`engine/market_state.py`) state at `t` |
| `b_risk_radar_us` | Risk Radar US |
| `b_risk_radar_intl` | Risk Radar CN/HK/intl |
| `b_anticipation` | Anticipation |
| `b_leadership_crack` | Leadership Crack state |
| `b_vix_level` | VIX close > design-era 80th percentile (level, not acceleration) |
| `b_rv_level` | `rv_spy_5d` > design-era 80th percentile |
| `b_rv_target` | simple realized-vol targeting: reduce risk when `rv_spy_5d` is above its 252d median |
| `b_ma_break` | SPY close below 50-session average |
| `b_prophet_none` | no-protection Prophet (raw board, no sidecar) |
| `b_always_reduce` | always-reduced-risk policy |

A complex interaction is **not valuable** if a trivial volatility rule
matches its capital protection at lower false-alarm cost.

No new composite regime score
(`DNR:KILL-COMPOSITE-REGIME-RELIABILITY-MONITOR`). No LLM contagion tags
(`DNR:KILL-LLM-CONTAGION-TAGS`). No absolute-VIX spike-and-fade
(`DNR:KILL-ABSOLUTE-VIX-THRESHOLDS`).

---

## §6 Feature families

Only existing fields and the named transforms above.

1. **Price/residual:** venue OHLCV, first-available, `BARS` profile.
2. **Leadership:** `leadership_crack.v1` legs and state.
3. **Rates:** DGS2/5/10/20/30, DFII10 when vintage exists, curve, MOVE if
   rights permit — `REVISABLE_RELEASE` on first-available / ALFRED
   `realtime_start`. Latest-revised FRED is forbidden in scored historical
   frames (TDS §0).
4. **Vol:** VIX and realized variance; short-dated/VVIX only with rights.
5. **Auction:** TreasuryDirect results (`published_at` ≈ 13:00 ET auction
   day); WI tail only if PIT; concession proxy separately named.
6. **Credit/funding:** existing HY OAS / HYG-TLT / NFCI fields already
   consumed by Risk State / Risk Radar, first-available.
7. **FX/oil:** DXY, USDJPY, WTI — event clocks for geopolitical/oil only
   from verifiable primary releases.
8. **PBOC:** existing `engine/china_pboc_stance.py` / OMO bulletin fields.
9. **Asia tape:** KOSPI, Samsung, SK Hynix, Nikkei, TW, HSI/HSTECH,
   SSE/CSI300/ChiNext/STAR, CNH, CGB — existing collectors.
10. **Prophet:** raw US and CN boards and plan state; sidecar only.
11. **Portfolio shadow (read-only):** `macro_risk`, `market_view`,
    `posture_decider`, `derisk` tripwire. No writes.

Article publication time is not event time when a primary release clock
exists.

---

## §7 Missing-data, clocks, and effective-N

### 7.1 Clock schema

Every load-bearing row carries the packet §11 columns:

`source_id, source_family, market, field_or_artifact, source_owner,
source_url_or_repo_path, event_time, measurement_end, available_at,
observed_at_mastermind, produced_at_mastermind, effective_session,
settled_at, revision_basis, rights_basis, historical_coverage_start,
intraday_available, quality_state, notes`

A source with unknown `available_at` **may not** support an anticipatory
claim. It may appear as a descriptive coincidence.

TDS coalesce: `known_at := coalesce(published_at, ingested_at)`;
`INTELLIGENCE` coalesces to `served_at`. `effective_at` is never a
knowability clock.

### 7.2 Missing-data behavior

- Missing required clock → row `quality_state = CLOCK_UNKNOWN`; the claim
  that needed it is `BLOCKED`, not guessed.
- Missing vintage for a revisable series → that series is dropped from the
  scored frame (fail closed). It is not filled from latest-revised.
- Missing organ artifact at `t` → replay from truncated inputs at the SHA
  in force; if definition/input vintage is unavailable and no lawful
  substitute exists → that organ is `BLOCKED` for that `t`.
- Missing rights → `RIGHTS_BLOCKED` and a named minimum lawful substitute.
- Partial cohort membership → report coverage; do not default missing
  names to zero residual.

### 7.3 Effective-N

Promotion-bearing (here: **research-adjudication**, never live authority)
reads use **event-level / episode-level N**, not fire-dates.

- A US session-day is one episode for US-only hypotheses.
- A multi-region contagion path is one episode (GD-H8).
- Clustered CIs: block by episode, not overlapping 21-day rows.
- Honest-N is printed next to every cell. A cell with `< 30` episodes or
  `< 10` distinct calendar months is `UNDERPOWERED — DISCLOSED`, not a
  pass or fail.

---

## §8 Splits, eras, and leakage control

- **Design / calibration era:** 2016-01-04 .. 2026-07-31. Percentile
  cutoffs and any z-windows are frozen here.
- **Confirmatory incident:** 2026-08-01 → repair terminal declared in §1.
  Coverage and illustration only.
- **Purged/embargoed time splits:** when a label uses horizon `h`, the
  next `h` sessions are embargoed from the subsequent training fold.
- **Leave-one-crisis-out** (pre-registered crisis windows, closed before
  the incident): 2018-Q4, 2020-02..2020-04, 2022-01..2022-10,
  2024-07..2024-08 (Yen/vol), 2025-08 (if the existing organ ledgers mark
  a distinct episode; else dropped and disclosed), 2026-06-23
  memory-unwind (named in `engine/risk_state.py` docstring) and
  2026-07-02 KOSPI/SK Hynix session (named in
  `research/SECOND_ACT_NOTE.md` as a prior episode — used as a **prior
  case**, not as the current incident).
- **No random row CV** over overlapping 21-day labels.
- **No latest-revised** macro in scored historical frames.
- **No EOD reconstruction of intraday facts.**
- Code definition at `t` vs current definition: replayed as **two**
  columns (`def_at_t`, `def_current_cf`). Never mixed silently.

---

## §9 Statistical treatment

- Block/bootstrap or episode-clustered CIs, B = 2000, 95%.
- Multiple-testing control: Benjamini–Hochberg inside each hypothesis
  family `{GD-H1, GD-H2, GD-H3}` (predictive family) and separately
  inside `{GD-H4, GD-H5, GD-H6, GD-H7, GD-H8}` (mostly confirmatory /
  descriptive). Cross-family pooling is not a gate.
- Calibration metrics (Brier, reliability slope) only if a probability
  output is produced; this prereg does **not** introduce one. Existing
  organ probabilities, if any, are evaluated as-is.
- Precision–recall for the rare hazard labels in §4.2, vs each baseline
  in §5, design era only.
- Capital-utility for Prophet sidecars: losses avoided, winners withheld,
  MAE avoided, upside forgone, board expected-shortfall change, time in
  protection, action churn (packet §15).
- Report nulls and sign inversions. Descriptive coincidence is labeled
  `COINCIDENCE`. Predictive lead is labeled `LEAD` only after the mature
  PIT sample and the baseline gate.

---

## §10 Decision rule (research only)

GD-1 grants **no** live market, Prophet, or Portfolio authority.

| result | what it means |
|---|---|
| PASS on design era + coverage on the incident | construction is eligible for a **GD-5 shadow** writeup; still no live action |
| FAIL | this operationalization closes; the search space does not |
| UNAVAILABLE / BLOCKED | named gap + minimum lawful substitute; no guessed number |
| UNDERPOWERED | accrues; not a pass |

A PASS that a trivial vol rule matches at lower false-alarm cost is
recorded as `REDUNDANT_WITH_BASELINE` and is not a GD-5 candidate.

The current incident cannot be the only positive case.

---

## §11 Temporal reconstruction protocol

For each replay timestamp `t`:

1. construct the source availability set as known at `t`;
2. refuse revisions/publications unavailable at `t`;
3. select the code definition actually in force, or replay the current
   definition as a **separate** counterfactual;
4. compute each existing organ from truncated inputs where reproducible;
5. compare with the committed historical artifact if one exists;
6. explain any divergence by source vintage, definition version, build
   ordering, stale carry, or unavailable history;
7. record authority separately from observation.

A historical artifact created later is **recomputed history**, not proof
the system emitted it at `t`.

---

## §12 Prophet counterfactual protocol

For each raw US/CN board session:

1. preserve exact board definition, raw population, rank, and lane;
2. never rebuild the board with Grey Deer inputs;
3. attach hypothetical vulnerability/policy dispositions as a **sidecar**;
4. measure outcomes for **all** candidates, withheld or not;
5. compute the packet §15 utility vector.

US defensive-composition question (packet §16): determine whether the
absence of technology from US actionable recommendations was (a) a
repeatable consequence of canonical candidate evidence, (b) an
entry-status/availability gate, (c) ranking/order, (d) data absence, or
(e) chance composition. Return the exact rejection/disposition
distribution for technology versus defensive sectors. Do not infer
“Prophet knew the crash” from a defensive shelf.

---

## §13 Existing organs to replay

Minimum set (packet §6). For each: code SHA/version, source fields,
source-as-of and available-at, output state, freshness, **authority at
that time**, current consumer path, and whether evidence was useful but
disconnected.

- Market State (`engine/market_state.py` and CN/HK/intl siblings)
- legacy Macro Risk State / fused Risk State (`engine/risk_state.py`)
- Risk Radar US / CN / HK / intl
- Anticipation; Velocity
- Rates Command; Transmission
- Leadership Crack (display-only)
- breadth / correlation / rotation organs
- Market Drivers
- deterioration / contagion
- PBOC OMO context
- China/HK regime/risk output
- US/China Prophet raw boards
- Mastermind Portfolio `macro_risk`, `market_view`, `posture_decider`,
  `derisk` — read-only

One-tick asymmetric escalation of a live risk band remains killed
(`DNR:KILL-ONE-TICK-ESCALATION`).

---

## §14 Causal-hypothesis ledger fields

For every proposed cause/mechanism:

- hypothesis
- role ∈ `precursor | trigger | amplifier | transmission | confirmer | contradiction | repair | invalidation`
- evidence for
- evidence against
- first-known time
- deterministic observation vs statistical inference
- current confidence wording
- what would change the conclusion

Do not force one cause. Expected structure: a vulnerable system hit by
interacting triggers.

---

## §15 Stop / BLOCKED

Stop and return `BLOCKED` rather than approximate when:

- intraday source history required by a claim does not exist;
- first-known clock cannot be established;
- rights do not permit the needed historical use;
- PIT membership/identity cannot be reconstructed;
- an existing organ cannot be replayed because its definition/input
  vintage is unavailable and no lawful substitute exists;
- the only way to make a hypothesis work is to tune on the current
  incident.

The correct output is a named data gap and the minimum lawful substitute.

---

## §16 Required outputs

Under `research/grey_deer/gd1/`:

1. this file
2. `GD1_SOURCE_CLOCK_LEDGER.csv`
3. `GD1_SOURCE_RIGHTS_AND_GAPS.md`
4. `GD1_EXISTING_ORGAN_REPLAY.json`
5. `GD1_EVENT_TIMELINE.md`
6. `GD1_CAUSAL_HYPOTHESIS_LEDGER.md`
7. `GD1_PROPHET_COUNTERFACTUAL.md`
8. `GD1_US_PROPHET_DEFENSIVE_COMPOSITION.md`
9. `GD1_REPAIR_ANALYSIS.md`
10. `GD1_RESULTS_AND_ADJUDICATION.md`
11. `GD1_REPRO_MANIFEST.json`
12. AgentOS discovery/handoff records

---

## §17 Explicit non-authority

GD-1 is research-only. It does not change production engines, does not
tune thresholds after outcomes are viewed, does not mint a new risk
score, does not originate LLM hazard labels, and does not authorize
Prophet or Portfolio behavior.

---

## §18 Standing priors already closed (do not re-test as if open)

- SLF-006 auction-absorption composite: NULL/NO-GO
  (`reports/slf006-auction-absorption-phase0.md`).
- D2 auction-cycle concession/rebound: registered gates fail
  (`reports/d2-rates-calendar-flows-phase0.md`).
- Standalone “bad auction predicts equities”: disallowed (packet §13).
- Absolute VIX thresholds: `DNR:KILL-ABSOLUTE-VIX-THRESHOLDS`.
- LLM contagion tags: `DNR:KILL-LLM-CONTAGION-TAGS`.
- Composite regime-reliability fusion:
  `DNR:KILL-COMPOSITE-REGIME-RELIABILITY-MONITOR`.

These priors are **not** August 2026 outcome columns. They are closed
historical constructions this replay must not revive under a new name.
