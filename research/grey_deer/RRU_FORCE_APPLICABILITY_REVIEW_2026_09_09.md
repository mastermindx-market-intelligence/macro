# RRU force applicability: candidate defect closed, production release still held

Parent `risk-radar-all-regions-upgrade-20260908-sol-001`; same Macro PR6989 / MAS-258.
Starting research head `96251ae9c5d9dc95b57378e4bb073d4badc7a3e3`.
Source fixture `eb9e91961ddc4f3043d0dad358602525e66eccda`.
Procedure `Mastermind@63e3ddec6d8819dffe6509f3d13d41f2e94b9709`, Skillpack1.0.1/bootstrap1.
Program state PARTIAL; all amended product code remains uninstalled research evidence.
No Executive Job, worker, watcher, policy transition, production write, merge or deploy.

## User/machine result in the candidate

The corrected descriptive reading no longer borrows the old calculation's permission
and silently changes the market-state dial. One pure predicate in the existing audit
owner qualifies all ten known grant-attachment assignments in five existing owners.
The existing projection and international override use that same qualification.
A raw legacy scorecard stays intact as earlier evidence, not current permission.
The card explicitly labels that earlier record rather than claiming it moves the verdict.

No composition-bearing construction is promoted by this change. Null, malformed,
unknown or self-described reviewed metadata cannot activate authority. A future
promotion requires reviewed construction-compatible evidence at the existing owner.
Unmarked legacy snapshots preserve both their permission and their projection shape.
The prior edge test requiring a modern effective permission to remain true was
contradictory to the published release RED; its source-immutability assertion survives,
while effective permission and reported permission are now explicitly distinguished.

## The policy boundary is not a second lifecycle

At the fixture, `_radar_override_intl` recomputes a per-snapshot score ceiling. It does
not carry a policy ID, expiry, or persisted restriction lifecycle. The frozen envelope
composer emits no named policies; that does not prove the installed product has none.

Refusing an unmatched current grant is not an expiry or release event. The candidate
never reads, erases, expires or creates a named capital policy. Existing input policy
objects and independent override entries survive unchanged. Installed restrictions,
if any, still require their actual owner's transition before a source cutover.
Do not invent a policy ledger to represent a stateless snapshot cap.

## Executed proof and exact boundaries

`RRU_FORCE_FULL_CANDIDATE_2026_09_09.txt`: native process54106, terminal exit0,
235 tests in one invocation, zero failures/errors, original source bytes unchanged.
It executes all five amended engine modules in memory, not only selected functions.
It includes the formerly separate failing force test, now passing with its legacy control.

The new30-case applicability suite covers ten profiles, malformed metadata, all ten
assignment statements and legacy/provenance controls. Its initial29-case RED had27
assertion failures, one malformed-boolean AttributeError and one control; that error
is disclosed rather than counted as an assertion. Subsequent record-label and legacy-
shape tests each went from one failure to green before the final combined proof.

Sixteen actual import/lane/attachment-block tests cover four nightly/read-only owners.
The original blocks fail8 cases and pass8 legacy controls; amended blocks pass16/16.
The block executes the real source but ledger, scorecard and tuner effect seams are
mocked. The two China-live assignments are statement-tested, not full-live-run tested.

Eight added full `market_state_snapshot` tests went from7 failures/1 legacy control
to8 passes. On the synthetic actual-CN-profile fixture, measured78 remains78 for the
unpromoted reading; a valid legacy grant still gives26. Separate explicitly declared
harness profiles prove stress/dislocation caps41 and alert/regime caps59 survive.
Those harness guards are NOT asserted to be enabled on CN; actual CN override flags
remain empty, verified by a separate test. No portfolio, ledger or market feed is used.

## Browser and portable patch evidence

The v2 browser receipt covers48 cases: four CN/legacy states at1440/768/390,
dark/light and EN/ZH. All semantic, overflow, help, language and keyboard checks pass.
All48 PNG hashes were recomputed and candidate hashes match the235-test receipt.
Four actual frames were visually inspected: dark complete, light-ZH partial mobile,
light-EN unavailable mobile and dark legacy. They preserve bounded state meanings.
This is not premium final-design, complete-route, entitlement or production acceptance.
The minimal legacy fixture lacks its dominant label and retains the old calm fallback;
do not interpret that caption as a real-market observation or a new legacy fix.
Only four inspected PNGs plus the complete receipt are committed; all48 remain local.

The exporter generated a14-path candidate.patch plus per-path before/after hashes.
A real patch-tool replay on fresh fixture copies reproduced every candidate byte,
with zero reject/extra files and unchanged original sources. Native process57261 exit0.
This is patch fidelity, not a current-main merge proof or application to a worktree.
Final export home: `rru_force_patch_export_20260909_v2/` beside this document.

## Release remains held, with a different next action

The old assertion that the candidate's actual-force test is still red is superseded
only by the proof above. The original production source has not been repaired here.
Source custody, current integration, real-input applicability, independent full-candidate
review, installed-policy transition and real publication/browser proof remain open.
The attempted seven-path current-main comparison to3dda3326 was platform-blocked;
it was not retried or rerouted. Current compatibility is unknown, not inferred.

Next: review and integrate this exact producer/audit/attachment/consumer patch under
lawful source custody, then prove canonical-input publication without transferring a
legacy grant or silently releasing any installed independent policy. Do not rebuild
the now-tested gate or use green research as permission to deploy.

The separate PR6685 compact homepage remains with its incumbent. Independent recovery
observations still need visible presentation when the composite comparison is unavailable.
Source-clock hardening, the China wrong-gauge summary and earlier-turn validation remain
part of the full outcome. No new review worker, watcher or successor child was created.

## Recovered outer-consumer blocker — precedes release review

The same-turn untracked records already included DEC:RRU-FORCE-GRANT-IS-SNAPSHOT-SCOPED
and DSC:RRU-INTERNATIONAL-ADAPTER-DROPS-QUALIFIED-READING; both were recovered and read,
not recreated. A fresh local source read confirms `_radar_display` returns None whenever
state or h21 odds are absent. Therefore the modern candidate, which withholds unreviewed
odds, can disappear before reaching the actual international card. Its wrapper also
needs qualification rather than an unconditional calibrated-odds heading.

This is a source-proven integration gap, not a passing full-page test. The existing
RRU_INTL_ADAPTER_SCOPE and57-line TEST_DRAFT retain the intended next work. The final
harness append and an inherited-test enumeration were platform-blocked; neither was
retried. The draft is text, not executed acceptance. This adapter is outside the14-path
export and must be closed before that export can become a complete all-region integrity
release. It is now the first technical next action; force qualification is not re-opened.

Publication hygiene: the initial unified patch contained blank context markers that
Git whitespace checks flag when the patch is stored as text. The final exporter emits
explicit zero-context unified diffs and requires the recorded exact before hashes.
Its fresh-copy replay again matched all14 candidate files (process70736 exit0). This
changes patch formatting only; no product source or test expectation changed. The
first context-rich export remains local, untracked. A RED receipt had six trailing-
whitespace lines; raw local bytes and both hashes survive in the normalization receipt.
