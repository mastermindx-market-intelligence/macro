# Risk Radar: source-backed causes and live stress

Chairman continuation: continue the next useful capability while release work waits.
Operation risk-radar-explanations-live-20260920; fresh main base 96d3ddf6d500796573c0269da867409026586d94. Protected Skillpack 67060054ff9154a8f21b87f53ad859d30669131b, 1.0.1/bootstrap 1, loaded and compatible. Direct principal rationale: PRINCIPAL_JUDGMENT (display of pending/live/settled risk must not manufacture authority) and LOWER_TOTAL_OVERHEAD for this bounded consumer slice. No worker started or transferred.

## User journey and acceptance
Existing Risk building button keeps its radar word and score, gains a separate short internals caveat, and opens the same member-gated Radar. Source-native reasons replace generic text. The settled trend/stress and settlement date remain unchanged; a live provisional stress row names the candidate and uses the producer-owned pending state, never a browser-generated threshold. The one existing poller owns all live display; no extra fetch, timer, event store, policy, model, or score.

Unavailable, unknown, future, expired, failed, mismatched-bundle and older-session reads cannot paint a healthy live state. On error after a real live display, restore the settled button hint, remove the live timestamp/read and disclose the fallback. Preserve source coverage and older-input caveats. Never infer that absence of a warning means a trade is permitted.

## Design
DARK TREATMENT: retain Risk Radar luminance surfaces; cause text uses existing ink, severity stays independent of directional trend color. LIGHT TREATMENT: retain the quiet hairline inset, no glow or high-contrast new panel. Shared architecture, language parity, ≤14-word cause target for the current two-source case, existing button target and modal controller. No new container on the dashboard. Reference is the accepted 7467 design and Chairman screenshots.

## Scope and proof
Only existing risk partials, button call sites, live consumer and its paired asset; regression tests extend the CI-enrolled risk integration suite. Engine/composer/builders, policy promotions, radar scores, scientific calibration and GD-3 acceptance stay unchanged. Held 6685 is an alternative public-hero placement, not an input or authority for this operation; do not merge or overwrite it. PR 7236 owns separate numerical/breadth work; PR 7036 owns Release Radar.

Required: real source fixture → SSR/button → same live consumer → visible provisional state; timeout/fallback/expiry recovery; source-state/locale parity; keyboard and responsive browser matrix; exact production asset proof. No authenticated production claim from a forced fixture. Parent removal 7467 is merged and published independently; do not reuse its branch or repeat its release.
