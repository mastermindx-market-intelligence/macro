# China integration: restore dependency-selected verification

Operation: china-participation-context-20260921-sol-001; original locked Studio carrier / PR7622.
Pickup: a23644a91453e84488acbd1d37d8e29aa51564a4.
Procedure: Mastermind@1a7d400294b0d37c460b963b8865b40a23173b58; Skillpack1.0.1/bootstrap1.
Chairman continuation retains exceptional review waiver and explicitly deferred CI/release.
Direct rationale: LOWER_TOTAL_OVERHEAD for one existing job's exact dependency list.

## Defect and bounded effect

The prior P2 integration identified18 uncovered paths in the conviction-profile job.
The existing whole-manifest ownership test independently failed on that job at pickup.
The canonical selector confirmed0/18 of those single-file changes selected the job.
Twenty-one new registered cases independently reproduced18 failures and3 negative controls.
The failures concern dependencies actually reached by the job's China render tests,
including participation, live-feed scripts, saved-value projection and shared risk dialogs.

The repair adds those18 literal paths to that existing exclusive declaration only.
No new job, wildcard, queue, runner, workflow trigger, gate, command, pack algorithm,
model, source collector, renderer or public-page change is introduced. All218 job
objects remain structurally identical except that one paths-list extension.
Main reference bbd02daf6ae9cc00e6c1c50f7559f4d8e873f3ac lacked those18 explicit paths;
that bounded observation is not a full-base compatibility proof or a waiver of any check.

The full218-job canonical selector now selects the intended job for all18 cases,
with the declared path as the reason. Three unrelated path controls still do not
select that job. P2's evidence module, registered suite and frozen test fixture also
select their existing engine-render-guards owner, which remains unscoped/always-on;
that separate behavior is unchanged and needed no additional manifest patch.

## Final verification

The final focused invocation passed26 tests with0 failures and0 warnings (process45124,
exit0). It includes the existing whole-manifest transitive-closure guard, declared-scope
coverage and fail-safe selector controls, plus21 new selection/negative-control cases.
The earlier two RED runs each reported16 warnings from permission-denied cleanup of
old shared pytest directories. Nothing there was removed or re-permissioned; the final
run used this operation's own evidence-directory test space and emitted no warnings.
The original843-test P2 run is historical evidence, not added to this count or rerun.

All218 job definitions, commands, gates, runner selections and trigger behavior are
unchanged except the one exact dependency extension. No product Python, JavaScript,
template, input or public-page update was made. The prior page fingerprint remains
`e8d0a5b7e14a48eeda51a77061ce52366447d3835a9a7701f36cc0ec5aaa451d`.
The source/log/selector fingerprints and immutable main reference are in the sibling JSON.
This is local qualification, not hosted CI, a full current-main merge proof, or deployment.

## Remaining boundaries

The P0 sizing correction still has no returned source head. No duplicate request or
writer was created. Original NBS29, weight-refresh16 and render-write54 cases remain
pending under their separate action holds; the earlier normalization/browser and P2
notification-preparation refusals remain intact. A new weighted-window diagnostic was
refused before dispatch; no result is inferred and that effect was not retried.
The inherited24 auxiliary outputs and4 screenshots stay excluded. No cleaning, source
reset, release arm, worker dispatch, shared runner change or automated wake occurred.
This closes the known18-path test-selection gap, not the China accuracy mission.
