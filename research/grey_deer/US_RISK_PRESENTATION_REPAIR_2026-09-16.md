# US risk presentation repair — September 16, 2026

## Outcome and authority

The user should distinguish a supportive index-level composite from weak participation,
fragile risk evidence and actual authorized capital policy in seconds. A 61-point blend
must not claim that breadth confirms when its breadth component is 14. Machines retain
the original score, historical definition and authority flags; this repair introduces no
new ranking, sizing, probability, trade gate, risk engine, state store or event history.

Current Chairman direction also rejects the large independent Grey Deer container:
integrate its intelligence into Risk Radar and the score explanation, rather than
replacing it with another standalone branded card. Internal state/hazard/policy separation
is retained. Public explanation must not acquire a new registration gate.

Protected procedure: Mastermind `5ee11ab1e993616f3568cfca4069cb21fa61fd8f`,
Skillpack 1.0.1 / bootstrap 1. Implementation base: Macro
`ec44523d17eedeea35bdf4513a0d4843a4e81a64`. Source namespace belongs to existing
`WS:GREY-DEER-RISK-INTELLIGENCE` / `market-regime-risk`; no new workstream is created.

## Source custody and integration boundaries

This continuation owns only its managed workspace / branch:
`claude/ssd-us-risk-breadth-integrity-20260916-sol-9a09b2c2fa2f1eb8`.
Direct principal intervention is justified by the tightly coupled interpretation decision
and bounded reproduction; an unadmitted raw provider spawn is not a delegation substitute.

PR #6685 remains on frozen head `151e88528f5d269fd83945329392a4660a1d42f4` and its
original `macro-gd-6685-sol-repair-20260906-001` carrier. Sol recorded the Chairman's
integration amendment there in comment 5705259071, after reading comment 5582159769.
Its accepted compact/public source is reusable, not to be redesigned or copied into a
second release to evade its continuity/release hold. No custody transfer was made.

PR #7040 remains Draft at `c9a73e3882718464043320f4e1c6c76e6b2b0ea3`.
Its velocity / Chinese-copy repairs must survive later composition. This repair does not
edit its branch, alerts, audit module, shared Radar card, or vector builder. The shared
`engine/market_state.py` delta here is the generic headline wording only. The dashboard
ladder and hero wiring must later compose with #6685/#6974, not replace their whole files.

## Implemented candidate, not production acceptance

1. **Missing evidence is not calm.** `engine/risk_radar.py` adds reading availability and
   separate display fields, preserving legacy score/band arithmetic. The existing US
   scare ladder also recognizes zero coverage in older stored artifacts, printing
   Unavailable / 不可用 and no zero-risk bar. A genuinely observed zero remains zero.
2. **Domain exit is not repair.** Unavailable scares are excluded from current fading/
   warm-driver narration and from the receding-scare choice. Historical score series,
   primary intensity, overall state, probabilities and authority remain separate.
3. **One downstream interpretation.** `lib/risk_presentation.py` qualifies the measured
   score using its existing component tones plus a same-session, same-market, fresh
   canonical risk envelope. It never reads policy count as market support. A mismatched
   envelope cannot supply today's hazard; a Risk-off verdict is not softened by Fragile.
4. **Both delivery paths.** Settled `build_site` and live `build_risk_state` consume that
   same pure formatter. The primary/ledger words, thesis and primary-action copy use
   the projection. The live client accepts a matching typed projection; incomplete or
   legacy feeds use scoped composite wording rather than restoring an all-clear.

The source/live label can therefore say Fragile while the measured number remains 61.
The subline explicitly calls that number a measured blend, not a probability. This is
not a newly calibrated market score, a crash forecast, or a promotion to trading authority.
The old large Grey Deer mount has NOT been removed in this candidate: #6685's incumbent
compact-source/release gates must be reconciled, then the final integrated journey rendered.

## User journey / time / null behaviour

At glance: qualified condition, explicitly measured score, one short explanation.
Risk detail: retain source evidence, observation clocks and availability rather than
averaging disagreement away. Live quotes must not restore generic risk-on instructions.
The envelope clock is settled even alongside a live composite; mismatched dates, market,
schema or freshness cannot claim current hazard. Missing is distinct from observed zero.
No retrospective historical rewrite and no new data vendor or live trading action.

## Continuation R1 — recovered candidate, additional boundary repaired

Recovered the same managed branch after the failed turn; no replacement worktree,
child, source custody, or PR was created. Relevant source/dependency paths between
base ec44523d and current main f5a4d63f are unchanged.

An additional regression exposed a stale-reading gap: when BOTH internals legs
become unavailable, the scare drops out of the current list but trajectory drivers
could still call yesterday's value warm/fading. RED: 1 failed, 31 passed. The minimal
repair skips driver narration when the final observation is NaN; numerical history,
intensity, probability and authority arithmetic remain unchanged. GREEN: 32 passed,
exit 0, including server/live presentation and the two domain-exit constructions.

The first validation attempt reached 30 passing cases but was interrupted in pytest's
shared temporary-directory cleanup; it is NOT counted as a successful run. Subsequent
runs use unique worktree-owned --basetemp paths. No shared temporary tree was manually
removed and no CI run was cancelled or restarted.

Current #6685 head remains 151e885. Its fresh checks now show ci-authority/main SUCCESS,
but trusted-executor-pack-9 and ci-gate FAILURE. The historical admission failure is
not repeated as the current blocker. Its source/release hold remains; no duplicate
compact-panel implementation or production publication has been performed.

Next: owning regression tests, differential CI coverage, exact candidate proof and
canonical publication/review. The main-to-Radar integration remains a composed release
with #6685, not permission to bypass that incumbent's gates.

## Continuation R2 — resume here, not from the original audit

Same branch/base and writer; no commit, PR, merge or deployment of this candidate.
Real stored September 15 inputs through the candidate formatter and actual Jinja macros
produce Fragile while preserving score 61 / measured RISK_ON and presentation-only
status. Receipt: US_RISK_PRESENTATION_REAL_INPUT_2026-09-16.json in this directory.

Server/live legacy subline parity was additionally RED (four failures), then repaired
in the paired JS copies and actual template-owner guard. Concluded focused validation:
38 passed, exit 0. No old unconditional all-clear returns on a legacy live refresh.
Two NEW stale/degraded-input cases then failed as intended: the candidate still allows
broad-confirmation copy when all supplied tones are good but stale_inputs or
 degraded_components are nonempty. The attempted formatter repair was BLOCKED by the
tool before execution; readback confirmed lib/risk_presentation.py remains unchanged.
Do not claim these two cases passed or reproduce the blocked edit through another tool.

#6685's precise blocker is missing Jinja2 in synapse-read-gate, run 34193919678,
job 101957882722, not the historical main authority refusal (now successful).
Same-carrier bounded REQUEST_REPAIR delivered as comment 5705732215. No receiver
pickup/START or repair result is claimed. Original Draft/HOLD and source custody remain.

Canonical sparse data opt-in terminated at its own 300-second timeout; helper's repair
check found no truncated tracked file. Exit 1, so full data readiness is NOT proven.
Contract census PID4053 (parent4010) was last observed running; its tool session vanished,
not the process. Result path .pytest_cache/us-risk-contract-r1.log was still empty.
Next: reconcile that exact process/log, restore data through the canonical helper only
after current-state inspection, then finish the blocked freshness repair when permitted,
wire/test CI coverage, and publish the tested candidate for review. Do not restart the
audit, duplicate #6685, cancel shared CI, or treat this uncommitted candidate as shipped.

## Continuation R3 — freshness repair verified

Protected procedure now 4537f066775c73d305f82acf0643701f01f5e53c; INDEX and
required skill/source-law blobs are unchanged from the previous accepted pin.
Same managed source branch and carrier; current live Chairman continuation retained.
Fresh RED: the two stale/degraded-input cases fail with qualified=false. The small
formatter correction executed successfully on the same Remote Desktop Commander
carrier after source readback. Fresh GREEN: 40 passed, exit 0 (freshness-r3-green).
Component-level degradation also prevents broad confirmation; original score and
verdict remain untouched. A matching Fragile hazard still leads incomplete-input copy.

The previous contract census completed: 1 introduced, 0 inherited; the exact missing
path is lib/risk_presentation.py in intelligence-registry. Added that named path and
registered the three new suites plus copy parity in existing gate:code
unrun-template-chips; Node availability is explicitly checked, not skip-only proof.
No new job or gate weakening. Current main 0fa09ade7ef485935860866ff3342a7492b550d4
has a disjoint dashboard rank-colour repair and material manifest widening in
conviction-profile; current-base integration/coverage must preserve those changes.

The original data opt-in failure is reconciled (old process absent, no truncated
files reported). Same canonical add_dirs helper is running with its supported 900s
budget in this worktree; all dirty-path/repair safeguards retained. Log:
.pytest_cache/us-risk-data-optin-r3.log. Do not duplicate while it runs.
#6685 still has no opposite-side return after request 5705732215; head 151e885 is
unchanged. No source transfer, merge, live fix or panel removal is claimed.

R3 full-data result: canonical data opt-in exited 0, materialized data; no longer
blocked on the previous sparse-data timeout. Owning validation with actual restored
data concluded 313 passed / exit 0 across 12 targeted files, including Radar,
recovery, audit, shared card/dialog wiring, live session floor and new presentation
cases. Log .pytest_cache/us-risk-owning-r3.log. Static compile, diff whitespace and
paired JS byte equality also passed. These are candidate tests, not deployed proof.
Next: preserve the candidate in Git, compose with the current material manifest and
disjoint dashboard change, then current-base contract proof and canonical page render.

## Continuation R5 — provenance repaired; publication candidate

Recovered HEAD a838c0249ec450d86af103f7010f7210ba409c13; R3 source was already
committed as 19293db7a2d1 and composed with main 0fa09ade. Do not redo either step.
R4 uncommitted work additionally covered persisted freshness flags and distinguished
capped scores from their original measured blend, including unknown originals and
legacy feeds. R4 logs record discriminating RED then GREEN; source is retained.
Fresh R5 validation: 314 passed, exit 0 across the exact 12 files in
.pytest_cache/us-risk-owning-r5.log; paired JS equality and node syntax also pass.
This test selection differs from R4's 320-case selection; neither is a full suite.

Current main 27ec910d04fc81692cf9640347ac31fd3de40170 changes none of the owned
source, manifest, renderer or named dependency paths since integrated main 0fa09ade.
R3 contract result remains 0 introduced / 1 inherited (unrun-picks-boards: site/theme.css).
Latest-base composition/hosted checks remain release requirements, not implied PASS.

Canonical render R3 wrote the local macro page through the real builder. Its 61-point
score survives; the envelope excludes the stale September 15 measured snapshot because
expected_asof is September 16. The actual page correctly says Confirmation incomplete.
This is an intended freshness boundary, NOT permission to fake a current Fragile read.
Local browser capture was blocked before execution by the platform safety check;
both intended output paths are absent. Do not route around or claim browser proof.
Generated data/site byproducts are preserve-only and excluded from source staging.
#6685 remains at 151e885 with no return after 5705732215; #7040 remains at c9a73e.
Panel removal, composed browser proof, production publication and acceptance remain open.

## Continuation R6 — published carrier and final semantic repair

Source candidate c189320c6b946ae314908f20b842283ff75bb342 is pushed on the
original branch and published as Draft/HOLD PR #7236. No source custody was moved.
Its current-base merge-tree is 0ba6e50f1d3dbe183dbcb3daf48157a4f1a28759.
Self-review found two additional wording defects, reproduced by four failing cases:
missing auxiliary inputs hid an observed breadth warning, and a capped Risk-off
headline falsely attributed that state to the original still-supportive blend.
The five-line formatter repair preserves weak breadth in incomplete-data copy and
names the risk constraint separately from the underlying blend. No score/authority
change. Fresh focused GREEN: 51 passed. Fresh 12-file owning GREEN: 318 passed,
exit 0. Source hashes and commands: US_RISK_PRESENTATION_R6_EVIDENCE_2026-09-16.json.
Resume from agentos/handoffs/GREY-DEER-RISK-INTELLIGENCE-2026-09-16-US-BREADTH.md
and #7236's current head, not the original chat audit. The parent remains active.
