# China integrity: sealed real-builder output

## What this proves
The actual no-network production builder, not a synthetic view-model, rendered
this repair from an immutable export of Macro commit
39f84dd389be555c267452e2444e57b6552efd07. Implementation bytes are unchanged from
fc7202ffeef9a3a178751c8b95b12f483a3f2711. Later d0f41e32991c documentation repairs
are preserved; no implementation work was replaced.

The prepared `site/china.html` is 443,300 bytes, SHA-256
929ca66df4ccff040275e0a79d02c3ad65bbebb3e97aa5944512b350157cb065.
It contains the real stored-snapshot risk card: score94 remains unchanged,
Economic slowdown reads Weak/疲弱, and descriptive Neutral posture and separately
owned live headline remain present. This does not validate94 as a forecast.

## Execution
The export contains only the exact tracked revision, no credentials or live
workspace state. Inside it, the normal builder ran with
`CHINA_FAST_RENDER=1 RENDER_NO_DRIP=1 OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1`.
No production data store, forward ledger or server was written. Temporary derived
data stayed in the export; only the intended primary-page artifact is prepared
for publication through this PR and the incumbent deployment path.

The existing `capture_page_evidence` owner and installed Chrome captured eight
actual built-page cases: desktop/mobile x English/Chinese x dark/light. All eight
captured, with zero console errors, failed responses or document horizontal
overflow. The page digest was checked before and after capture and did not change.
See `mockups/evidence/china-frozen-real-build-7592/build-proof.json`, its manifest,
and the content-addressed screenshots. This is local real-input proof, not a live
production or authenticated-session claim.

## Rejected proof and limits
The first local build completed, but a separate continuation changed the same
worktree and restored its generated output before capture. Its eight screenshots
showed the old committed page. They were rejected, preserved outside Git as
`rejected-local-capture`, and are not acceptance evidence. The immutable export
and before/after digest check close this specific proof race; no new worktree
scheduler, source branch or publication owner was created.

The offline build does not refresh quotes or news. The legacy news panel suppresses
live fetches in this mode and can show its explicit unavailable/quiet fallback;
full news coverage and exchange-session freshness are not established here.
The original participation/dispersion and calibration programme remains open.
Dark and light desktop captures were visually inspected: the real score history,
full card rows, economic-slowdown label and qualified posture remain readable.

## Release gate
Merged PR7597 (4183c5d564853d4796798bf19270f906adb5c9b5) is already an ancestor of
this candidate. It closed the former P0B browser-evidence dependency; do not wait
for or redo PR7578's superseded manifest repair. The remaining gate is this PR's
current CI/integration, followed by normal release and verified VPS publication.
The Chairman review exception remains a waiver, not a fabricated approval.
No merge or deployment is claimed by this evidence packet.

## Final artifact checks
The China page passed the existing inline-JavaScript/handler parser and explicit
Market State coherence guard. Template/site synchronization passed99 pairs.
AgentOS validation passed1167 records with zero errors (63 existing warnings).
The full-site coherence scan still fails on unchanged `site/macro.html` because
its score-node metadata is not accepted by the legacy parser. That is the separate
remaining parser fix on PR7578, not the superseded P0B receipt task and not a China
page failure. This packet does not waive or falsely green that global guard.
The final staged diff check also reports21 whitespace-only blank lines emitted
by the canonical Jinja renderer in the generated page. The artifact is retained
byte-for-byte rather than silently postprocessed after its browser/hash proof.
No clean-whitespace claim is made for this generated artifact; no required CI
check has been removed, reclassified by tooling or bypassed.
