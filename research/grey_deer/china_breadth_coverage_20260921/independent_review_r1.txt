PICKUP_ACK: china-breadth-pr7592-independent-review-20260921-sol-001 — received, no prior reviewer started, read-only custody accepted.
START / REVIEW_ONLY: adversarial read of `collectors/china_breadth.py`, `tests/test_china_breadth_coverage.py`, `collectors/breadth.py`, `collectors/base.py`, `lib/store.py`, `config.yml`, `.github/ci/legacy-jobs.yml`, `.github/workflows/ci.yml`, and the grey_deer README at the frozen worktree. **No tests were executed** (no process/execution tool in this seat; read-only tools only). No git command was run.

STATUS: PARTIAL

RESULT:
The safeguard is real and mostly well-aimed: on the `full_history` path it does enforce latest-row coverage from `iloc[-1]` only, it does strip unrequested symbols, it does reject non-finite/non-positive prices, it does gate MA eligibility, and it does refuse to publish when the inherited `compute()` drops the latest row — all before any cache/constituents write, so Standard 4 holds through `run_adapter` (failure → `FetchResult(status="failed")`, no `store.upsert`, previous aggregate intact). CI wiring is valid (not a finding): `china-board-breadth`'s `if: ${{ false }}` is the repo-wide pack convention (`scripts/run_ci_pack.py:82 DISABLED_IF`; 218/218 jobs carry it), the job is `gate: code` with no `scope:`, `ci.yml` line 19 triggers on `"**"`, and the job's install line covers every transitive import of the new suite.

Two **major** findings stand against the review standard, plus three **minor** and one **nit**. No blocker. Not a PASS.

- **MAJOR-1 — Standard 1 is not enforced on the incremental (nightly) path; the cache can supply the latest row.** `collectors/china_breadth.py:62` merges via `BreadthAdapter._merge_refreshed`, whose first act is `fresh.combine_first(cached)` (`collectors/breadth.py:438`). The coverage gate at `china_breadth.py:81` then reads `closes.iloc[-1]` on the **merged** frame, so any latest-date cell the fresh pull failed to return is silently backfilled from the previous run's bytes before it is counted. Counterexample: an earlier same-day lane writes a cache containing 2026-09-21 for all 82 curated names (five workflows cache this file — `daily.yml:224`, `asia-close.yml:222`, `closing-bell.yml:218`, `render.yml:481`, `earlyclose.yml:162`); the next lane's `"1mo"` pull returns nothing for 2026-09-21 (batch retries exhausted at `breadth.py:400-415`, which swallows the failure and simply omits the batch); `combine_first` restores the whole 09-21 row; `live_cols = 82`, `coverage = 1.0`, every MA gate passes, `breadth.index[-1] == closes.index[-1]`, and the adapter reports a successful **current** publication assembled entirely from prior-run data. The README's headline claim ("may no longer qualify a latest observation using historical availability", README:4-6) is therefore true only for `full_history=True` and for the no-cache fallback. Remedy direction: derive `live_cols` for the latest date from `fresh`, not from `merged`.
- **MAJOR-2 — Standard 5: no test ever exercises the merge path with a cache that contains the latest date, and the one test that touches the merge path decays off it by wall clock.** `tests/test_china_breadth_coverage.py:109` seeds the cache as `frame.iloc[:-1]` — deliberately excluding the latest row, i.e. exactly the case MAJOR-1 lives in. Worse, that test (`:106-117`) is the only one reaching `_merge_refreshed` at all, and `china_breadth.py:60` computes `age` against `pd.Timestamp.utcnow()` while the fixture pins `end="2026-09-18"` (`test:24`), cache tip 2026-09-17. From ~2026-10-02 onward `age > 14`, the branch at `:61` is skipped, and the test silently exercises the full-window re-download instead — still green, now covering nothing it was written to cover. Counterexample: advance the clock 15 days; the suite stays 100% green with zero coverage of the cached-merge branch.
- **MAJOR-3 — `np.isfinite` is evaluated before the empty guard, so the `full_history` empty-download case raises `TypeError`, not the intended coverage `RuntimeError`.** `china_breadth.py:78` runs `closes.where(np.isfinite(closes) & (closes > 0))`; the empty check is one line later at `:79-80`, and the live-path check at `:68` does not cover `full_history=True`. Counterexample = the suite's own case: `_download_closes → pd.DataFrame()` (`test:92`) with `full_history=True`; `pd.DataFrame().reindex(columns=tickers)` (`:77`) yields 40 **object**-dtype columns, and `np.isfinite` has no object loop → `TypeError: ufunc 'isfinite' not supported for the input types`, which `pytest.raises(RuntimeError, match="coverage")` (`test:93`) does not satisfy. Either `test_empty_download_fails_before_any_publication[True]` is red at this head, or the README's "84 passed" receipt (README:24) was taken on a tree where this ordering differed — see MINOR-3, which shows the receipt's base is not this head's base. I could not run the suite to disambiguate. Fix is one line: move the emptiness check above `:78`, or coerce with `apply(pd.to_numeric, errors="coerce")` first.
- **MINOR-1 — the effective latest-day floor is not the documented `min_coverage`, and the README/code comment says it is.** `config.yml:6407` documents `min_coverage: 0.6` as "fail the run if < this fraction of curated names resolve", and `china_breadth.py:90-91` says "Keep the existing minimum-coverage policy". But `:100-101` now hard-fails whenever `compute()` drops the latest row, and `compute()`'s partial-row rule is `n_members >= 0.8 × n_members.rolling(20).max()` (`breadth.py:642`). The real latest-day floor is `max(0.6·82, 0.8·recent_max)` ≈ 60 names, and the 60–79% band converts a previously graceful degrade into a hard source failure. Three consecutive such nights trip the circuit breaker (`base.py:21`, `:558`) and mark `china_breadth` **dead** for 20h windows — an operational consequence the README does not state. The test that pins this (`test:41-46`) confirms the 0.8 rule is the binding constraint, not 0.6.
- **MINOR-2 — MA eligibility is `notna().all()` over the trailing window, which a single A-share suspension voids.** `china_breadth.py:93-94`: one 停牌 session anywhere in the last 200 rows disqualifies that name from `usable` entirely. It is *consistent* with `compute()` (`min_periods=window`, `breadth.py:625-626`), so this is not a math error — but it makes the gate a hard run failure with a correlated trigger (suspension clusters around Golden Week / index events), against a receipt that already sits at 75/82 = 0.915 (README:38-40).
- **MINOR-3 — the committed evidence's base is not the head under review.** README:18 records base `cf2aae0beefb3e7dbb15e4ec672d8c288492dd13`; the commission states this artifact is `f03d40273145356bde2f73ef8b979f5e5a9754c6` based on `fbdd7ae4e3db10868be4f86bef4edd067e478cb3`. README:19 likewise records skillpack `3e66e43258f34bd…` against the commission's `5103486e8d8c46…`. Every quantitative receipt in the README ("9 failed, 2 passed", "84 passed", the canary counts) is therefore attributed to a tree that is not the reviewed head; it is not proof for `f03d4027…` until re-run and re-stamped.
- **MINOR-4 — reindex materializes all-NaN columns into a cache two downstream builders read as source of record.** `:77` reindexes to all 82 configured tickers, and `:105` persists that frame. The canary shows 76 resolvable columns of 82 (README:38), so the cache now carries 6 permanently all-NaN columns that previously were simply absent, into `scripts/build_china.py:532` and `scripts/build_china_library.py:1234`. No test asserts persisted-cache shape: `test:71-77` (unrequested-symbol exclusion) runs `full_history=True`, which skips the cache write at `:104-105` entirely, so the accepted-path cache contents are unasserted in both directions.
- **NIT — runner tests are not hermetic w.r.t. `run_status.json`.** `run_adapter` → `_breaker_state()` → `store.read_status()` resolves via `Path(config.ROOT) / config.load()["storage"]["run_status_file"]` (`lib/store.py:187-188`), which `monkeypatch.setattr(config, "data_dir", …)` (`test:124`, `:143`) does **not** redirect — those two tests read the repository's real tracked status file. Harmless today only because both pass `full_history=True`, which bypasses the breaker at `base.py:558`; a `full_history=False` variant added later would inherit live circuit-breaker state and could return `dead` instead of `failed`.

Standards adjudicated clean, no finding: Standard 2 (invalid/nonpositive rejected pre-write — `:78`, `:85-87`, `:92-98` all precede `mkdir` at `:103`); Standard 3 (`:100-101`); Standard 4 (`base.py:565-631` failure path performs no `store.upsert`; `_path` resolves lazily via `config.data_dir()` at `lib/store.py:29-33`, so the tmp_path redirection is genuine); Standard 6 respected — I raised no calendar/full-market/forecast requirement.

EVIDENCE:
- `collectors/china_breadth.py:62`, `collectors/breadth.py:438` — `fresh.combine_first(cached)` upstream of the `iloc[-1]` gate at `collectors/china_breadth.py:81`.
- `.github/workflows/daily.yml:224`, `asia-close.yml:222`, `closing-bell.yml:218`, `render.yml:481`, `earlyclose.yml:162` — five lanes persisting `data/china_breadth/_closes_cache.parquet`, establishing same-day re-run as routine.
- `tests/test_china_breadth_coverage.py:109` (`frame.iloc[:-1].to_parquet`), `:24` (`end="2026-09-18"`) vs `collectors/china_breadth.py:60` (`pd.Timestamp.utcnow()`) and `:61` (`age <= 14`).
- `collectors/china_breadth.py:77-80` ordering; `tests/test_china_breadth_coverage.py:89-95`.
- `config.yml:6403-6407` (`min_coverage: 0.6`, `ma_windows: [50, 200]`, `lookback_days_live: 1100`) vs `collectors/breadth.py:642` (0.8 rolling-20 rule) and `collectors/china_breadth.py:100-101`; `collectors/base.py:21`, `:558`.
- `research/grey_deer/china_breadth_coverage_20260921/README.md:18-19`, `:24`, `:38-40`.
- `scripts/build_china.py:532`, `scripts/build_china_library.py:1234` — cache consumers.
- `lib/store.py:29-33` (lazy `data_dir`), `:187-188` (ROOT-based status path).
- CI wiring verified sound: `.github/ci/legacy-jobs.yml:3892-3911`, `scripts/run_ci_pack.py:82-91`, `.github/workflows/ci.yml:15-19` (`paths: - "**"`).

GAPS:
- No tests executed and no git command run — MAJOR-3 and the README receipt status could not be confirmed empirically; they are argued from the pandas/numpy object-dtype contract and from the base-sha mismatch.
- I did not enumerate the PR diff; the review covers the files named in the commission plus the transitive dependencies I opened. The README's "changes no page template or live data" is unverified by me.
- Whether `scripts/build_china_library.py` tolerates all-NaN cache columns (MINOR-4) is unverified.
- Whether the changed step command at `legacy-jobs.yml:3911` invalidates any committed `ci_semantic_proof` baseline is unverified; the census record `agentos/discoveries/DSC-CI-SEMANTIC-PROOF-MANIFEST-CENSUS.v1.json:1438-1445` keys on step **name**, which is unchanged, so the likely answer is no.
- Whole-frame expected-session freshness remains unproven, per Standard 6 — explicitly not raised as a requirement here.

DEVIATIONS:
- None from the commission's boundaries: read-only tools only; no edits, no git writes, no network, no CI or account mutation, no blocked parent operation executed; no plan file written.
- No hosted-green claim, no formal GitHub approval, no production proof is asserted anywhere above.
- Contract note: `PICKUP_ACK` and `START / REVIEW_ONLY` precede the five required labels as the commission directs.
- SESSION END: PROVEN_OUTCOME (finite review delivered; no successor or watcher work assigned).