# GD-1C Commission — leadership_crack.v1 design-era reconstruction (research-only)

**Commissioned by:** Sol continuation ruling 2026-08-19 → Fable COO → Grok research operator
**Wave:** `WS:GREY-DEER-RISK-INTELLIGENCE` GD-1C · research-only, under
`research/grey_deer/gd1c/`. **Prerequisite for any GD-5 build** (GD-1 promoted nothing —
`DEC:GD1-ACCEPTED-NO-PROMOTION`).

## Mission

Reconstruct `leadership_crack.v1` over the design era (GD-1's named window:
2016-01-04..2026-07-31) by labeled truncate-and-recompute, and test the
**already-frozen GD-H1 and GD-H2 interactions** (duration × broken leadership;
volatility acceleration conditional on fragility) with **episode-level effective N**.
GD-1's statistical treatment law applies unchanged (purged/embargoed splits, era splits,
leave-one-crisis-out, block bootstrap, multiple-testing control within the family).

## Hard rules (each is a stop condition, not advice)

1. **Fresh prereg, hash-pinned before outcome access** — a new registration under
   `research/grey_deer/gd1c/GD1C_PREREG_<date>.md`; GD-1's prereg does not stretch to
   this sample. Hypotheses stay exactly GD-H1/GD-H2 as frozen — refinements need Fable
   approval and a new prereg version, per the GD-1 packet.
2. **`def_current_cf` labeling** — any reconstruction using CURRENT index/cohort
   membership is a separate counterfactual lane labeled `def_current_cf`, never mixed
   silently with PIT rows.
3. **BLOCKED beats guessed** — if PIT membership cannot be reconstructed for the primary
   test, return BLOCKED with the named gap and minimum lawful substitute. The
   current-membership lane may still run, labeled, as secondary evidence only.
4. **August 2026 may not choose thresholds.** Design-era percentiles are frozen on
   ≤2026-07-31 data; the August incident is out-of-design-sample evidence only.
5. **No GD-5A/B/C build begins unless the applicable hypothesis clears the promotion
   gate** (command packet §12 detection/advisory thresholds). Clearing reopens exactly
   that hypothesis's build — nothing else.

## Deliverables (under research/grey_deer/gd1c/)

Prereg; reconstruction manifest (code SHAs, input vintages, membership basis per row);
episode ledger with effective-N accounting; GD-H1/GD-H2 results with calibration and
lead-time distributions per the §12 gate fields; explicit PASS / FAIL / UNDERPOWERED /
BLOCKED verdict per hypothesis; AgentOS handoff citing `WS:GREY-DEER-RISK-INTELLIGENCE`
(the canonical identity — do not mint a new workstream).

## Acceptance

Fable scope review + independent adversarial reviewer on any PASS; Sol approves any GD-H
change. A PASS feeds `DEC:GD1-ACCEPTED-NO-PROMOTION`'s reopen clause; a FAIL/UNDERPOWERED
keeps GD-5 closed and names what sample would change it.
