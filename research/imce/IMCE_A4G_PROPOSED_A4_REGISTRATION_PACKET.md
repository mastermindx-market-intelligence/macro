# IMCE-A4G / A4P — A4 Registration Packet (EXECUTED, REGISTERED)

**STATUS: EXECUTED — REGISTERED at A4 (IMCE-03), 2026-08-22.** The three `declared_budget` rows below have been appended to `data/trial_ledger.jsonl` (Act 2, §6). All three `rf.cycle_pattern.imce_*` families named in this document are registered. Registration was performed exactly per this section (§4a) and §6's four acts, under Sol's fourth-gate conditional authorization (§6) and `agentos/workstreams/WS-CYCLE-PATTERN-ISSUER-MECHANISM.md`'s A4 wave.

**Wave:** A4G, regenerated by A4P (ruling 7, 2026-08-21), regenerated again by A4P.1 (ruling R7, 2026-08-22). Records-only. **This document proposed exact byte-ready row contents; A4 executed them verbatim — the three families are now registered.**
**Authority:** amended contract V1.2.1 (`IMCE_PREREGISTRATION_AND_EVALUATION_CONTRACT_V1.md`), as amended by `IMCE_A4G_AMENDMENT_LOG.md` (AG1–AG18, AP1–AP8, A4P.1 R1–R7); contract §1 (cell budgets, A5), §11 (multiplicity and trial budget), §15/§15a (stop condition, two-commit discipline).
**Purpose:** the mandatory Sol deliverable (e) — the exact proposed A4 registration packet: the three `rf.cycle_pattern.imce_*` `declared_budget` row contents as they would be written, byte-ready and executed verbatim, marked EXECUTED — REGISTERED; plus the criteria-commit checklist (criteria commit strictly before any outcome access); plus (new, A4P.1 R4) the complete A4 registration state-transition table, frozen verbatim-or-abort.
**Regeneration note [AP7, A4P binding; regenerated again A4P.1 R7]:** every row's `reason` string is rewritten to reflect AP1 (target mapping), AP2 (uniform B≤3 basis across all six cells, superseding the old "≤5 general / ≤3 cancellation-scoped" split), and now **A4P.1 R2/R3** (historical v0 population reworded to `named_subset_basis: [PHM, KBH]` — no "pooled homebuilder stratum" — and the sync family's market-risk cell renamed to the canonical `forward_63_trading_day_drawdown_tail`). Because `config_hash` is a pure function of `(family, n, reason)`, **every row's hash below is recomputed from the new reason string — none is carried forward from the A4P/V1.2 version.** See EVIDENCE in this wave's return packet for the exact recomputation commands and the before/after hash values; the three superseded V1.2 hashes (`d4fb6b5f517fe32c`, `f76dc44e1f5edc18`, `3eff3ee65158e41b`) are recorded, never silently dropped, in `IMCE_A4G_AMENDMENT_LOG.md`'s A4P.1 section.

---

## 1. Row schema (verbatim from `engine/trial_ledger.py`)

`TrialLedger.log_declared_budget(n, family=..., reason=...)` appends exactly this row shape to `data/trial_ledger.jsonl` (the canonical, committed, append-only ledger — `engine/trial_ledger.py` `DEFAULT_PATH = Path("data") / "trial_ledger.jsonl"`):

```json
{
  "ts": "<ISO8601 UTC, set automatically by datetime.now(timezone.utc).isoformat() at the moment of actual registration>",
  "family": "<family name, string>",
  "kind": "declared_budget",
  "n": "<int, the declared multiple-testing budget = frozen cell count>",
  "reason": "<string>",
  "config_hash": "<sha1(f'{family}\\x00{canon({\"__declared_budget__\": n, \"reason\": reason})}').hexdigest()[:16]>"
}
```

`config_hash` is a pure function of `(family, n, reason)` — it does not depend on `ts`, on any outcome, or on any `data/` content. It has been **recomputed exactly** below for each of the three proposed rows (AP7 regeneration — the reason strings changed from the A4G version, so the hashes changed with them), using the repo's own `_canon`/`_hash` functions (`json.dumps(..., sort_keys=True, default=str, separators=(",", ":"))` then `sha1(family + "\x00" + canon).hexdigest()[:16]`), verified against `engine/trial_ledger.py` lines 53–62 and 159–190 — no engine code was executed to write to any ledger; the hash was computed as a standalone pure-function check (see EVIDENCE in this wave's return packet for the exact Python invocation and output). **Only `ts` is genuinely unknowable in advance** (it is stamped at the moment of actual registration) and is left as an explicit placeholder rather than invented.

---

## 2. Proposed row — `rf.cycle_pattern.imce_phase_v0`

```json
{
  "ts": "<set at actual registration>",
  "family": "rf.cycle_pattern.imce_phase_v0",
  "kind": "declared_budget",
  "n": 3,
  "reason": "IMCE preregistration V1.2.1 (A4P.1-amended) - 3 cells frozen [A5]: 3 state targets x historical v0 population named_subset_basis [PHM, KBH] (prospective v0 eligible pooled cohort [DHI, PHM, KBH, TOL]) x contrast [M vs family/age prior], all 3 mapped to order_softness [AP1]; predetermined underpowered_accruing [A2]; block basis <=3, cancellation-scoped, uniform across all 6 v0 historical cells [AG5/AG6/AP2]; criteria commit precedes outcome access [G8-B1]. See research/imce/IMCE_PREREGISTRATION_AND_EVALUATION_CONTRACT_V1.md and research/imce/IMCE_A4G_AMENDMENT_LOG.md.",
  "config_hash": "a3b8ac5c0d0205cb"
}
```

**Cell definition (unchanged from contract §1, population wording corrected [A4P.1 R2]):** 3 state targets × historical v0 population `named_subset_basis: [PHM, KBH]` (prospective v0 eligible pooled cohort `[DHI, PHM, KBH, TOL]`, three-row label truth table — was "pooled homebuilder stratum") × contrast [M vs family/age prior]. **Target mapping SETTLED [AP1]:** all 3 targets track `order_softness` — (a) next cohort state +1rp, (b) next cohort state +3rp, (c) false-repair/relapse within 3rp. Deterministic, outcome-independent construction: `IMCE_A4P_ORDER_SOFTNESS_STATE_CONSTRUCTION_V1.md`. **Settled block basis [AP2, supersedes the A4G MAJ-1 partial settlement]:** all 3 cells in this family are B≤3-entire cancellation-scoped cells under the cell-level ruling in `IMCE_A4G_SIX_CELL_DISPOSITION.md` §1 (Cells 1–3) — the general B≤5 cap describes no cell in this family. **No open item remains for this row** — the AG14/MAJ-5 D5-state mapping that A4G left open is now closed by AP1; this row's `n=3` cells are individually itemizable at actual registration without further design choices.

---

## 3. Proposed row — `rf.cycle_pattern.imce_sync_v0`

```json
{
  "ts": "<set at actual registration>",
  "family": "rf.cycle_pattern.imce_sync_v0",
  "kind": "declared_budget",
  "n": 2,
  "reason": "IMCE preregistration V1.2.1 (A4P.1-amended) - 2 cells frozen [A5]: targets {next_local_state_1rp, forward_63_trading_day_drawdown_tail} x contrast [M+R vs M], next_local_state_1rp mapped to order_softness [AP1]; historical v0 population named_subset_basis [PHM, KBH] (prospective v0 eligible pooled cohort [DHI, PHM, KBH, TOL]); predetermined underpowered_accruing [A2]; block basis <=3, cancellation-scoped, uniform across all 6 v0 historical cells [AG5/AG6/AP2]; criteria commit precedes outcome access [G8-B1]. See research/imce/IMCE_PREREGISTRATION_AND_EVALUATION_CONTRACT_V1.md and research/imce/IMCE_A4G_AMENDMENT_LOG.md.",
  "config_hash": "1d69c1fa6b897b6a"
}
```

**Cell definition (naming normalized [A4P.1 R3]):** targets {`next_local_state_1rp`, `forward_63_trading_day_drawdown_tail`} × contrast [M+R vs M] — was `forward_63d_drawdown_tail`; both market-risk cells (this family's Cell 5, and `imce_risk_v0`'s Cell 6) bind to the same canonical 63-trading-day QLedger ruler, family prefix distinguishes the tests. Only the primary incremental comparison (M+R vs M) supports the synchronization claim (contract §5); secondary comparisons are print-only, zero budget, outside the FDR partition [A7]. **`next_local_state_1rp` (Cell 4) is mapped to `order_softness` [AP1]** — the same next-period cohort state as the phase family's 1-reporting-period target. **Settled block basis [AP2]:** both cells in this family — Cell 4 (`next_local_state_1rp`) and Cell 5 (`forward_63_trading_day_drawdown_tail`) — are B≤3-entire, uniformly, per `IMCE_A4G_SIX_CELL_DISPOSITION.md` §1. Cell 5's basis was conditional at A4G (pending its `M_t` feature composition); AP2 settles it: `order_softness` is in the basis for every registered cell, so Cell 5 is B≤3, not the previously-possible B≤5.

---

## 4. Proposed row — `rf.cycle_pattern.imce_risk_v0`

```json
{
  "ts": "<set at actual registration>",
  "family": "rf.cycle_pattern.imce_risk_v0",
  "kind": "declared_budget",
  "n": 1,
  "reason": "IMCE preregistration V1.2.1 (A4P.1-amended) - 1 cell frozen [A5]: forward_63_trading_day_drawdown_tail x [M vs family/stratum prior]; historical v0 population named_subset_basis [PHM, KBH] (prospective v0 eligible pooled cohort [DHI, PHM, KBH, TOL]); predetermined underpowered_accruing [A2]; block basis <=3, cancellation-scoped, uniform across all 6 v0 historical cells [AG5/AG6/AP2]; criteria commit precedes outcome access [G8-B1]. See research/imce/IMCE_PREREGISTRATION_AND_EVALUATION_CONTRACT_V1.md and research/imce/IMCE_A4G_AMENDMENT_LOG.md.",
  "config_hash": "309d76c3a8dfbb5c"
}
```

**Cell definition (unchanged):** `forward_63_trading_day_drawdown_tail` × [M vs family/stratum prior]. Market grading uses QLedger's 63-trading-day ruler and canonical exchange calendar (contract §5). **Settled block basis [AP2]:** B≤3-entire — same reasoning as sync Cell 5 (§3 above). See `IMCE_A4G_SIX_CELL_DISPOSITION.md` §1 (Cell 6).

---

## 4a. A4 STATE TRANSITION (frozen, verbatim-or-abort) [A4P.1 R4]

**What this section is:** Sol's fourth-gate ruling 4 requires the packet to say exactly what becomes true at
registration — every registration-state field, its exact current text, and its exact post-registration text,
so A4 proper is pure execution with zero remaining discretionary choices on any status field. **This section
performs NONE of these flips.** Every "current (A4P.1)" cell below is byte-verified against the actual
committed text at A4P.1 HEAD (this wave's own commit) — `IMCE_PREREGISTRATION_CANDIDATE_V1.yaml` and
`IMCE_PREREGISTRATION_AND_EVALUATION_CONTRACT_V1.md` both stay `candidate_not_registered`/unregistered after
this wave lands, exactly as before. A4 executes every row below verbatim, in order, or aborts back to Sol —
the same discipline §7's "execute verbatim or abort" rule already binds the rest of this packet to.

### 4a.1 Three placeholder procedures, frozen deterministically [expanded, red-team round 1 — FIX-9/FIX-10]

Three fields/tokens below cannot be given a literal value today because their inputs do not exist until the
moment of the actual A4 commit. All three procedures are frozen exactly, so A4 makes no choice about how to
compute them:

1. **`registration.repository_pin_observed`:** the full 40-hex-character SHA of `origin/main`, observed at
   A4 pickup, via `git rev-parse origin/main`, **recorded before any A4 edit is made** (so the pin describes
   the tree A4 branched from, not a tree A4 itself has already modified).
2. **`registration.config_hash`:** the git **blob** SHA-1 of `research/imce/IMCE_PREREGISTRATION_AND_EVALUATION_CONTRACT_V1.md`
   **AS STAMPED REGISTERED in the A4 commit** — computed via `git hash-object research/imce/IMCE_PREREGISTRATION_AND_EVALUATION_CONTRACT_V1.md`
   **AFTER** applying this table's MD transition edits (§4a.2 below) and **BEFORE** writing the YAML
   (§4a.3 below). The hash lands **only** in the YAML; the MD file never contains its own hash — no
   self-reference. This is a different, broader hash than the three row-level `config_hash` values in §2–§4
   above (which cover only `(family, n, reason)` each). **Mandatory verification step, closes a silent-
   invalidation hole (FIX-10):** immediately after the A4 registration commit is created — and before any
   push — re-run `git hash-object research/imce/IMCE_PREREGISTRATION_AND_EVALUATION_CONTRACT_V1.md` against
   the actually-committed head and confirm it EQUALS the value just written into
   `registration.config_hash`. **On any mismatch (e.g. the contract MD was edited again, later, in the same
   commit or PR, after the hash was computed) A4 ABORTS — it does not push — and repairs the hash before any
   further action.** This closes the class of defect where a late MD edit silently invalidates an
   already-recorded hash without anyone re-checking it.
3. **`<A4 registration commit date>`** (packet §4a.4, §0 opening line): the UTC calendar date, ISO-8601
   `YYYY-MM-DD` format, observed at the moment the A4 registration commit is created — procedure: `date -u
   +%F`. Frozen so no ambiguity exists between a local-timezone date, a push timestamp, or an authorship date;
   the registration commit's own creation instant, in UTC, is the sole source.

### 4a.2 Contract MD (`IMCE_PREREGISTRATION_AND_EVALUATION_CONTRACT_V1.md`) — exact transitions

| Site | Current (A4P.1, unchanged by this wave) | A4-time replacement |
|---|---|---|
| Status header, opening line | `**Status:** \`candidate_not_registered\`. This document has not been registered and no real outcome evaluation has been run. Registration (repository re-pin, \`config_hash\`, trial-ledger \`declared_budget\` rows) is a future act — IMCE-03 / A4.` | `**Status:** \`registered\`. This document was registered at A4 (IMCE-03). Repository pin, \`config_hash\`, and the three trial-ledger \`declared_budget\` rows are recorded in \`IMCE_PREREGISTRATION_CANDIDATE_V1.yaml\` \`registration\` block and in \`data/trial_ledger.jsonl\`. No outcome evaluation has been run.` |
| §15a, "Freeze location" bullet | `- **Freeze location:** this document (V1.2.1) plus future trial-ledger \`declared_budget\` rows — IMCE-03 / A4 work, proposed-not-registered in \`IMCE_A4G_PROPOSED_A4_REGISTRATION_PACKET.md\`, not yet performed as an actual registration act. The deterministic \`order_softness\` construction is frozen normatively in \`IMCE_A4P_ORDER_SOFTNESS_STATE_CONSTRUCTION_V1.md\` [AP1].` | `- **Freeze location:** this document (as registered) plus the three trial-ledger \`declared_budget\` rows, appended at registration — IMCE-03 / A4 act, executed per \`IMCE_A4G_PROPOSED_A4_REGISTRATION_PACKET.md\` §2–§4/§4a. The deterministic \`order_softness\` construction is frozen normatively in \`IMCE_A4P_ORDER_SOFTNESS_STATE_CONSTRUCTION_V1.md\` [AP1].` |
| §15a, "Repository pin" bullet | `- **Repository pin:** re-pinned at registration, a future act. No commit SHA in this document or its YAML projection is asserted as "registered" today.` | `- **Repository pin:** \`<repository_pin_observed, §4a.1 procedure 1>\`, recorded in the YAML \`registration.repository_pin_observed\` field at the moment of the A4 commit.` |

### 4a.3 YAML (`IMCE_PREREGISTRATION_CANDIDATE_V1.yaml`) — exact transitions

| Site | Current (A4P.1, unchanged by this wave) | A4-time replacement |
|---|---|---|
| Top-level `status` | `status: candidate_not_registered` | `status: registered` |
| Registration comment block (3 lines, immediately above `registration:`) | `# Registration is a future act (IMCE-03): repository pin and config_hash are`⏎`# recorded only at registration, never before. No SHA in this file is`⏎`# "registered" today. [A25]` | `# Registered at A4 (IMCE-03): repository pin and config_hash are recorded`⏎`# below, stamped at the moment of the actual registration commit. [A25]` |
| `registration.registered` | `  registered: false` | `  registered: true` |
| `registration.repository_pin_observed` | `  repository_pin_observed: null` | `  repository_pin_observed: "<§4a.1 procedure 1 output — the full 40-hex origin/main SHA observed at A4 pickup>"` |
| `registration.config_hash` | `  config_hash: null` | `  config_hash: "<§4a.1 procedure 2 output — the git blob SHA-1 of the AS-STAMPED-REGISTERED contract MD>"` |
| `registration.freeze_location` (2nd list item) | `    - future trial-ledger declared_budget rows (IMCE-03, not yet performed)` | `    - trial-ledger declared_budget rows (appended at registration)` |
| Top-level `requires_fable_adjudication` | `requires_fable_adjudication: true` | `requires_fable_adjudication: false` |
| `trials[0].status` (`rf.cycle_pattern.imce_phase_v0`) | `    status: not_declared` | `    status: declared` |
| `trials[1].status` (`rf.cycle_pattern.imce_sync_v0`) | `    status: not_declared` | `    status: declared` |
| `trials[2].status` (`rf.cycle_pattern.imce_risk_v0`) | `    status: not_declared` | `    status: declared` |

### 4a.4 This packet's own registration-state prose — exact transitions [MAJ-1 fix, red-team round 1]

**Red-team finding, confirmed:** the original version of this subsection covered only §0's opening bold
STATUS line. Executing that one row verbatim would leave `**STATUS: EXECUTED — REGISTERED…**` sitting directly
above the UNTOUCHED remainder of the same paragraph ("No `data/` write has occurred… Registration is a
separate, future act…") and above three more live sites (the H1 title, the `Wave:` line's closing clause, and
the `Purpose:` line's "PROPOSED — NOT REGISTERED" marking) — producing exactly the defect Sol's ruling 4
forbids: *"a registered bit nested inside a document that still calls itself candidate/not-declared."* Every
remaining live registration-state site in THIS packet is now enumerated below, byte-exact.

| Site | Current (A4P.1, unchanged by this wave) | A4-time replacement |
|---|---|---|
| H1 title (line 1) | `# IMCE-A4G / A4P — Proposed A4 Registration Packet` | `# IMCE-A4G / A4P — A4 Registration Packet (EXECUTED, REGISTERED)` |
| §0 opening paragraph (line 3, full) | `**STATUS: PROPOSED — NOT REGISTERED.** No \`data/\` write has occurred. No \`declared_budget\` row has actually been appended to \`data/trial_ledger.jsonl\`. No \`rf.cycle_pattern.imce_*\` family is registered by this document or by any document in the A4G/A4P wave sequence. Registration is a separate, future act (A4 / IMCE-03), requiring its own wave approval per \`agentos/workstreams/WS-CYCLE-PATTERN-ISSUER-MECHANISM.md\`.` | `**STATUS: EXECUTED — REGISTERED at A4 (IMCE-03), <A4 registration commit date, §4a.1 procedure 3>.** The three \`declared_budget\` rows below have been appended to \`data/trial_ledger.jsonl\` (Act 2, §6). All three \`rf.cycle_pattern.imce_*\` families named in this document are registered. Registration was performed exactly per this section (§4a) and §6's four acts, under Sol's fourth-gate conditional authorization (§6) and \`agentos/workstreams/WS-CYCLE-PATTERN-ISSUER-MECHANISM.md\`'s A4 wave.` |
| `Wave:` line, closing clause (line 5) | `**This document proposes exact byte-ready row contents; it registers nothing.**` | `**This document proposed exact byte-ready row contents; A4 executed them verbatim — the three families are now registered.**` |
| `Purpose:` line, marking clause (line 7) | `byte-ready, clearly marked PROPOSED — NOT REGISTERED;` | `byte-ready and executed verbatim, marked EXECUTED — REGISTERED;` |

**After this table executes:** every registration-state field across all three documents (contract MD, YAML,
and this packet — including this packet's own title, opening paragraph, `Wave:` line, and `Purpose:` line)
reads `registered`/`true`/`declared`/`EXECUTED` consistently, `repository_pin_observed` and `config_hash` are
both non-null, `requires_fable_adjudication` is `false`, and no remaining live prose in ANY of the three
documents calls a declared-budget row "future," "not yet performed," "proposed," or "PROPOSED — NOT
REGISTERED" — satisfying Sol's ruling 4 verbatim: **one state, not a registered bit nested inside a document
that still calls itself candidate/not-declared.** A post-edit registration-state census (contract MD, YAML,
and this packet together) confirms zero uncovered live sites remain outside this §4a's own transition tables.

### 4a.5 Cross-reference to the four A4 acts (§6 below)

This table is executed as part of **Act 3** ("Stamp the final registered contract hash/state") of §6's four acts,
using the placeholder procedures §4a.1 froze. Act 1 (observe repository pin) produces the `repository_pin_observed`
value this table's YAML row consumes; Act 2 (write the three `declared_budget` rows) is §2–§4 above, unchanged by
this table; Act 4 (verify criteria-commit-predates-outcomes) is unaffected by any field in this table.

---

## 5. BH-FDR partition — already frozen by the contract; not a separate A4 act [AP8, M3(a) fix]

**Corrected — the original draft of this section invented a fifth act (a separate "FDR-partition registration"
JSON write, with an "enforced assertion" requirement) that is not one of Sol's four A4 acts and contradicted
Sol's own ruling 5 ("do NOT build any new FDR-partition writer, schema, ledger, or store" — the ORIGINAL A4P
commission, not merely AP5's restatement of it). That invented act is deleted here, not merely softened.**

Per contract §1/§11 (A6, AP8): the single BH-FDR partition `imce_hist_v0`, `q = 0.10`, over the union of the
six registered cell IDs (contract §11's enumeration — see also `six_cell_ids_union` in the YAML) is **already
fully specified by the frozen contract text itself.** It requires **no separate write, no separate JSON
artifact, and no separate "registration act" at A4** — the three `declared_budget` rows (§2–§4 above) already
cite `imce_hist_v0`/q=0.10 in their `reason` strings, and that is the entire extent of what A4 touches on this
topic.

**The runner obligation is binding on a FUTURE evaluation-runner wave, not on A4 [AP5, unchanged in substance
— restated here without the contradiction]:** contract §15/§15a already records, as a registration STOP
CONDITION (not code, not an A4-time artifact), that the future historical-cell runner must assert exactly the
six registered cell IDs before applying BH correction, and apply exactly one correction at q=0.10 over their
union. **This obligation binds whichever session eventually builds and runs the evaluation runner — a
separate, later, separately-authorized wave — never A4 registration itself.** A4 registration's only relevant
duty is to have this stop condition already recorded in the frozen contract before any outcome access occurs
(checklist §6 below verifies this is already true, not that A4 must build anything to make it true).

**No FDR-partition writer is built at A4, per Sol's ruling 5, restated without contradiction:** `TrialLedger`
(`engine/trial_ledger.py`) owns only the three `declared_budget` rows (§1–§4 above) — it has no
FDR-partition-registration mechanism, and A4 does not add one. If and when a future evaluation-runner wave
needs a persisted FDR-partition record, that wave designs and builds it as its own scoped work, subject to its
own review — not inherited from, or required by, this packet.

---

## 6. What A4 registration actually DOES — exactly Sol's four acts [AP8, M3 fix; cross-referenced A4P.1 R4]

**Corrected — the original draft of this section listed 15 checklist items without distinguishing "A4 must DO
this" from "this is already true and A4 merely verifies it," which is how the FDR-writer contradiction (§5,
M3(a)) crept in. A4 registration, as a discrete act, performs EXACTLY four things — no more:**

**Sol fourth-gate verdict, 2026-08-22 (relayed verbatim in the A4P.1 commissioning instruction) — explicit
conditional authorization to start A4 proper, recorded here so A4 can cite it without a further roundtrip:**
*"If—and only if—A4P.1 lands exactly as above with no new substantive finding, you do not need another Sol
roundtrip to start A4 proper. This message is the explicit conditional authorization."* A4 opens under this
authorization only if this wave (A4P.1) landed exactly as frozen — any new substantive finding discovered
while executing A4 reopens the question, it does not waive it.

1. **Observe the current repository pin** — read the exact commit SHA of `origin/main` at the moment of
   registration and record it as the `repository_pin_observed` value (contract §15a; procedure frozen exactly
   in §4a.1 above).
2. **Write the three frozen `declared_budget` rows to `data/trial_ledger.jsonl`** — the exact byte contents in
   §2–§4 above, `ts` populated at the moment of the actual write, `config_hash` matching the values already
   computed above (not recomputed at registration time — they are a pure function of `(family, n, reason)`,
   already fixed).
3. **Stamp the final registered contract hash/state** — compute and record `config_hash` (contract §15a; all
   three placeholder procedures frozen exactly in §4a.1 above, including the `<A4 registration commit date>`
   procedure and the mandatory post-commit hash-verification-or-abort step) over the actually-committed
   contract content (a different, broader hash than the three row-level `config_hash` values in §2–§4, which
   cover only `(family, n, reason)` each), and execute every byte-exact old→new pair in §4a.2/§4a.3/§4a.4
   above — including `registration.registered: true` in the YAML projection and this packet's own STATUS
   header. **Before pushing, re-verify per §4a.1 procedure 2's verification step** — a mismatch aborts and
   repairs, it never pushes silently.
4. **Verify the criteria commit predates any outcome access** — an explicit, checked statement in the
   registration commit message or record that no forward return, drawdown, Brier score, calibration
   statistic, p-value, correlation, or regression was computed on any of the six cells before acts 1–3 above
   were committed.

**Nothing else is an A4 act.** In particular: A4 does **not** build an FDR-partition writer (§5); A4 does
**not** need to "publish" a come-back date, freeze a bootstrap seed, settle a prospective-share value, mint
cell IDs, or resolve the D5-state mapping — **all of that is already done, by A4G and A4P, in the frozen
contract this packet points to.** A4 registration's job is narrow: pin the repo, write three rows, stamp one
hash, and confirm the ordering. Everything below is **precondition verification** — confirming those prior
decisions are already true in the committed contract — never a new decision A4 itself makes.

### Precondition verification (already true in the frozen contract; A4 confirms, does not decide)

- [ ] Contract V1.2.1 frozen and committed to `origin/main`, with its YAML projection.
- [ ] All 18 A4G rulings, all 8 A4P rulings, and all 7 A4P.1 rulings (R1–R7) encoded in `IMCE_A4G_AMENDMENT_LOG.md`.
- [ ] Treasury CMT source-rights disposition is `GO_LIMITED` (A4P.1 R5) — scope/basis frozen; this closes the prior wave's escalation item 5; storage/reuse ingestion design remains future work, out of scope for A4.
- [ ] Boundary receipts remain honestly open (A4P.1 R6) — `month_boundaries_receipt_status: proposed_not_yet_receipted` unchanged; no receipt fabricated to close this precondition.
- [ ] Six-cell disposition (`IMCE_A4G_SIX_CELL_DISPOSITION.md`) shows no open item; all six cells settled B≤3 (AP2); the six cell IDs match contract §11 / YAML `six_cell_ids_union` exactly, ratified and naming-normalized by Sol's fourth gate — **[AP8, M2; A4P.1 R3 — inlined here for a self-contained packet]:** `imce_phase_v0.next_order_softness_1rp`, `imce_phase_v0.next_order_softness_3rp`, `imce_phase_v0.order_softness_false_repair_3rp`, `imce_sync_v0.next_order_softness_1rp`, `imce_sync_v0.forward_63_trading_day_drawdown_tail`, `imce_risk_v0.forward_63_trading_day_drawdown_tail`.
- [ ] `order_softness` deterministic construction frozen (`IMCE_A4P_ORDER_SOFTNESS_STATE_CONSTRUCTION_V1.md`) — no outcome-selected threshold, no grid search, no issuer-specific tuning; block admissibility deferred to the contract's own block list (§3.0); ≥2-issuer cohort floor in force; fail-closed era-coverage gate applied to DHI/TOL.
- [ ] Source/boundary table recorded, receipt statuses honest (`IMCE_A4G_SOURCE_BOUNDARY_TABLE.md`) — ruling 6 is OPEN/partial, not satisfied (§6 of that document); no boundary asserted without a citation.
- [ ] `n_effective_blocks` computation path: B ≤ 3, uniform across all six registered cells (AP2); no ρ-based estimator used as N; `n_issuer_precision_diagnostic` is the frozen field name (AP8, no longer TBD).
- [ ] All 3 `imce_phase_v0` targets and `imce_sync_v0` Cell 4 mapped to `order_softness`, with a registered deterministic construction (AP1) — satisfied.
- [ ] Bootstrap: 800 draws, seed 7, registered-block-cluster unit (not date/month) — frozen (AP4, corrected AP8).
- [ ] Preregistered minimum prospective share: 100% — frozen (AP3).
- [ ] Promotion clock published on the zero-historical-credit basis: **~2160** (AP8, M4) — the historical-block-count illustrations (~2149, ~2153, ~2145) are non-promotion diagnostics only, never cited as the promotion timeline.
- [ ] The future evaluation-runner's obligation (assert exactly the six cell IDs, one BH correction, no seventh cell) is recorded as a contract §15/§15a stop condition — binding on that FUTURE wave, not on A4 itself (§5, M3(a)).

**Only after every precondition above is confirmed true, AND acts 1–4 are performed in order, may any
runner/outcome commit occur.** This document performs none of the four acts and confirms none of the
preconditions — it is the proposed content for a future session to execute.

---

## 7. What this document does NOT do

- Does not write `data/trial_ledger.jsonl`.
- Does not access any outcome, return, drawdown, Brier score, or price series.
- Does not register or write any FDR-partition artifact — none is needed at A4 (§5, M3(a) fix); no writer is built by this wave or required of A4.
- Does not compute the contract-level `config_hash` (only the three row-level `config_hash` values, which are a narrower, different hash over `(family, n, reason)` only).
- Does not itself constitute A4 registration — it is the proposed, byte-ready content for a future, separately-authorized registration act.

**[AP8, M3(c) fix] A4 executes this packet VERBATIM, or aborts back to Sol — there is no amend-at-execution
path.** The original draft's closing line ("execute verbatim or amend before executing") implied A4 itself
could revise this packet's content on the fly; that is deleted. If a future A4 registration session finds
this packet's content stale, wrong, or incomplete against the then-current contract, its only options are (a)
execute exactly what is written here, or (b) stop and escalate the discrepancy back to Sol for a new ruling —
never to silently amend the packet and proceed as if that amendment had been authorized.

---

**This document authorizes nothing beyond itself.** The next authorized act on this family is actual A4 registration (IMCE-03), performing exactly the four acts in §6 above, as its own separately-authorized wave.
