STATUS: PASS

RESULT:
PASS for the bounded changed visual/evidence surface, judging dark and light separately as designs. No blocker and no major finding. One **minor** and two **nits** below; the previously named release gates are untouched and remain open.

Prior-finding dispositions
- **MAJOR-1 (named art directions + full dual-theme matrix) — CLOSED.** `DESIGN.md:14-20` (DARK TREATMENT), `:22-33` (LIGHT TREATMENT + the three intentionally differing mechanisms), `:48-55` (theme-specific degraded states), `:58-67` (baseline + matrix). This is not token substitution: light-only material exists in governed CSS — `templates/basket_detail.html.j2:235-238` (`html[data-theme="light"]`: card shadow, top rule demoted from the 2px `--info` gradient at `:231-232` to a 1px `--line` hairline, `.mo-stat` white `--panel`, `.mo-stat.note` cooler `--panel2`). All 8 normal + 8 missing units screenshots and all 8 benchmark-gap + 4 positive-pair images exist on disk (Glob over both folders returned 28 PNGs) and each state in `P1_MEMBER_UNITS_2026-09-17/receipt.json` now carries both `screenshot` and `missing_screenshot` with sha256 (e.g. `:96-99` en/light/1440).
- **MINOR-1 (evidence could not discriminate relative from raw) — CLOSED.** `page_proof.json:32-54`: fixture member A, `benchmark_return 0.0341456`, `raw 0.25`, `relative 0.2158544`; `benchmark-positive-raw_daily_change-en-dark-1440.png` renders **+25.0%**, `benchmark-positive-benchmark_relative_daily_change-en-dark-1440.png` renders **+21.6 pp** (25.0 − 3.41 = 21.59 ✓), ZH/light/390 pair mirrors it as "+21.6 个百分点" (`:74-75`). Labelled `"controlled positive test fixture; not market data"` (`:41,62`) in a separate `controlled_positive_generation` block (`:24-31`). Real current input is honestly null, not fabricated: every units state reports `relative_observed: 0` with the row text "Unavailable / Benchmark unavailable; relative result withheld" (`receipt.json:13,75,199`), confirmed visually in `member-evidence-en-light-1440.png` and `member-evidence-zh-light-1440.png`.
- **MINOR-2 (Why off-screen at 390) — CLOSED.** `templates/basket_detail.html.j2:287-305`: `min-width:0`, `display:block`, stacked `tbody tr` grid, `overflow-x:hidden`, `.why{grid-column:1/-1}`, plus `.mo-mobile-label` re-exposed. Geometry now proves containment rather than mere DOM text: every 390 state has `scroll_width == client_width == 340` and `why_right` 341.2 (EN) / 320.7 (ZH) inside `right: 366` (`receipt.json:115-126,177-188,239-250`). Visually confirmed: `member-evidence-en-light-390.png` (AMAT/AMD/ANET rows each show "Why  Benchmark unavailable; relative result withheld" in full) and `member-unavailable-zh-dark-390.png` (CBRS, "可用 / 最低要求 86 / 200", "说明 历史不足，无法计算该口径").
- **NIT-1 (History-used ratio read as completion) — CLOSED.** Relabelled "Available / minimum" / "可用 / 最低要求" at `:1212` (cell, with mobile label) and `:1277` (header), pinned by `tests/test_theme_detail_member_observations.py:615-616`; rationale `DESIGN.md:41-42`.
- **ce16 MAJOR-2 (#7211 current-run/validator wiring) — unchanged, out of scope**; correctly a release gate, not a presentation defect. **ce16 MINOR-3 (unmeasured artifact cost) — partially closed**: `companion_bytes 2602710`, `rendered_detail_bytes 14334238`, `compute_seconds 28.73`, `render_and_validate_seconds 1.912` with the explicit disclaimer `"one current local build, not incremental overhead or production budget proof"` (`page_proof.json:10-14`). Nightly churn/render-budget impact is still unmeasured — informational follow-up only.

New findings
- **MINOR — desktop header/content alignment on the two prose columns.** Inherited `th{text-align:right}` (`templates/basket_detail.html.j2:66-67`) meets the new left-aligned Result chips and Why prose, so the "Why" header sits at x≈1285 while reasons begin at x≈713, and "Result" sits at x≈341 above chips at x≈182 (`member-evidence-en-light-1440.png`, identical in `member-evidence-en-dark-1440.png` and `member-evidence-zh-light-1440.png`). Semantics are intact (`headers=`/`scope=col`, `:1210-1212,1277`) and the false-sortable affordance is correctly suppressed by `.mo-table thead th{cursor:default}` (`:262`); this is scan cost, not a data or a11y defect. Cheap fix: left-align `#mo-col-result`/`#mo-col-reason` headers.
- **NIT** — the global "Ask Mastermind" FAB overlays the last visible row's reason at 390 (`member-evidence-en-light-390.png`, APH row). Pre-existing site chrome over a scrolling container, not introduced here.
- **NIT** — native `<select>` clips "…(pp)" at 390 (`member-evidence-en-light-390.png`); no information loss, since the caption repeats "Member daily change minus benchmark (pp) · 24 shown".

EVIDENCE:
- **Dark as a composition** (`member-evidence-en-dark-1440.png`, `member-unavailable-zh-dark-390.png`): graphite panel, 2px `--info` accent edge identifying the section (not direction), recessed `--panel2` stat tiles and definition tile, hairline rows, tabular numerals, neutral bordered "Unavailable/不可用" chips, no glow/motion/new palette. Hierarchy reads eyebrow → question headline → controls → 5-part summary → roster → collapsed receipts.
- **Light as a composition** (`member-evidence-en-light-1440.png`, `member-evidence-zh-light-1440.png`, `member-unavailable-en-light-1440.png`, `member-evidence-en-light-390.png`): white research sheet on cool canvas with card shadow, neutral top hairline (accent edge deliberately absent), **white** stat tiles against the cooler definition/caption/header surface, hairline discipline instead of luminance depth. Missing state stays a neutral bordered label with plain reason — no warning color that could read as a signal. Material depth and semantic color differ from dark by design; IA, type, state meaning and interaction are identical.
- **EN/ZH parity**: ZH desktop light (`member-evidence-zh-light-1440.png`) and ZH dark 390 carry the same five tiles, same column set, same chip semantics with translated labels ("可用 / 最低要求", "说明", "不可用"); no CJK overflow or truncation seen.
- **No aggregation/filter rewrite**: `member-unavailable-en-light-1440.png` shows OBSERVED 23/24, UNAVAILABLE 1, VISIBLE **1 / 24** under the "Unavailable" filter; `benchmark-positive-*-en-dark-1440.png` shows OBSERVED 6/6 with VISIBLE 1/6 under a search — visibility changes, population does not. `receipt.json` repeats `catalogue: 24`, `source_data_unchanged: true`, `script_errors: []` in all 8 states.
- **No design-system bypass**: all `.mo-*` material is governed CSS at `templates/basket_detail.html.j2:228-308` on existing tokens; a grep for `style.textContent` / `createElement('style')` in that template returned no match; the new JS only emits DOM with roles/headers (`:1210-1212,1275-1277`).
- **Inspected (not executed) test**: `tests/test_theme_detail_member_observations.py:594-616` asserts stacked-row labels, `headers=` bindings, "Not enough history" reason presence, `role` attributes, EN+ZH "Available / minimum", and source invariance (`unchanged is True`).

GAPS:
- I cannot run git, tests, validators or a browser. Source identity at commit `1228779a…` is **parent-attested**, not personally verified; I reviewed bytes on disk in this worktree. Receipts/tests are assessed as inspected evidence, not reproduced results.
- Timing/byte figures are one local run and are explicitly not incremental overhead or a production budget claim; nightly churn of the ~2.6 MB companion remains unmeasured.
- Image adjudication used 8 of the 28 committed PNGs (both desktop light EN/ZH, light degraded desktop, light mobile EN, dark desktop EN, dark mobile ZH, both positive-pair EN/dark frames); the remaining cells were judged from their receipt geometry/text rows, not pixel-inspected.
- Release gates untouched and unclaimed: #7211 current-run/validator publication wiring, full hosted CI, and authenticated production proof.

DEVIATIONS:
- None. Read/Grep/Glob only; no edits, commands, tests, browser, network, MCP, agents, GitHub actions or merge. No next-wave authority, watcher or polling assumed; this response ends the finite assignment. Parent retains acceptance authority.
