STATUS: PARTIAL

RESULT:
Reviewed frozen head as committed in the working tree at `/Users/chriswong/Documents/Cluade/macro-main/.claude/worktrees/sector-cycle-p1-member-observation-20260917-sol` (commission SHA `ce16dc6dc58acf8d9006ab0095c2714702b6bbbe`; packet names code commit `13721803f7aec41735c9c3bda547c578d24f4561`). Read-only inspection only — no Bash/git/test execution, so I could not independently confirm the checkout equals that SHA (see DEVIATIONS).

Verdict: **PARTIAL — no blockers found in the code; the bounded P1 capability is substantively correct, but the dual-theme evidence required for a `PASS` is incomplete.** I attacked the benchmark-pair repair, the receipt/basis binding, the null taxonomy, the legacy-isolation claim and the UI units/missingness and failed to falsify any of them. The verdict is capped by an evidence/art-direction gap, not by a defect.

**Findings**

- **MAJOR-1 (caps verdict; dual-theme evidence incomplete + no named light art direction).** The committed matrix asserts 8 states but retains only 2 screenshots per evidence set, and **light desktop 1440 is never captured in either set** — `P1_MEMBER_UNITS_2026-09-17/receipt.json:36-49` (en/light/1440) and `P1_BENCHMARK_PAIR_2026-09-17/page_proof.json:38-46` (en/light/1440) carry no `screenshot` key; the only light artifact anywhere is the 390 px ZH crop. The handoff packet (`agentos/handoffs/GMI-THEME-GRAPH-2026-09-17-…md:12-24`, `25-61`) names no DARK TREATMENT, no LIGHT TREATMENT, no intentionally-differing mechanism and no degraded light state; the only light-specific declaration in the whole feature is `templates/basket_detail.html.j2:233` (`html[data-theme="light"] .mo-band{box-shadow:var(--card-shadow)}`) — everything else is token substitution, which TP-0 says is never proof of a light design. Concrete failing scenario this evidence cannot see: the 5-column `.mo-summary` grid (`:247-248`) and the `.mo-band::before` `--info` gradient (`:231-232`) only exist in the desktop composition; a light-desktop regression (gradient bar invisible on the light `--panel`, or the 210px "Definition" cell crushing the four stat tiles) would leave every committed receipt green, because the browser assertions are DOM text/`section_width` reads, not visual adjudication. Repair is cheap: capture the six missing cells (or an `EVIDENCE.yml` under `mockups/refs/`) and state the light treatment.
- **MAJOR-2 (release dependency, already named by the commission — restated with evidence).** `scripts/check_group_member_observations.py` has **no production caller**: a repo-wide grep for `check_group_member_observations` / `current_run_errors` returns only `tests/test_theme_detail_member_observations.py:20,398-408` and `research/.../verify_pages.py:22`. So `current_run_errors` (`:213-222`) — the only thing that proves *this* invocation produced the companion — gates nothing today, and the renderer accepts any companion that digest-matches the `pulse.json` bytes **sitting next to it** (`scripts/build_theme_detail.py:228-240`). Failing scenario: a night where `group_pulse.run()` never executes leaves both files from yesterday, mutually consistent, and the page publishes yesterday's member evidence under its own (correct but stale) `as_of`. Not a P1 code defect — the checker is written precisely for this — but the gate is not wired in this head. Correctly owed to the #7211 publication owner.
- **MINOR-1 (evidence cannot discriminate relative from raw).** `P1_MEMBER_UNITS_2026-09-17/receipt.json:10-11` (and all eight states) captures one member, AMAT, whose raw is `-1.4%` and relative is `-1.4 pp` — identical magnitude. The retained screenshot/receipt therefore cannot distinguish "benchmark subtracted" from "raw copied into the relative cell", which is the exact failure the repair exists to close. The **code** is proven elsewhere (`tests/test_group_member_observations.py:920-928` asserts `relative == raw − bench_return` member-by-member, plus the `zero_relative` control at `:927-928`), so this is an evidence weakness, not a defect. Repair: capture a member where the two visibly differ.
- **MINOR-2 (responsive composition; P1's core promise is off-screen at 390).** `.mo-table{min-width:720px}` inside `overflow:auto` (`templates/basket_detail.html.j2:253-254`) pushes the **"Why" column — the entire missingness disclosure — completely off-screen on mobile with no scroll affordance**; `member-evidence-zh-light-390.png` shows "使用历" truncated mid-word and no Why column at all. The functional receipt cannot catch this: `receipt.json:118` records `section_width: 366` and asserts the row text, which a DOM read returns regardless of visibility. On a surface whose stated job is "see accurate metric-specific observed/unavailable sets **and reasons**", the reason is reachable only by horizontal scroll.
- **MINOR-3 (wire bytes of the new artifact are unreported).** `site/basketdata/` carries no `.gitignore` entry, so `member_observations.json` becomes a tracked, nightly-churning artifact, yet neither `page_proof.json` nor `receipt.json` records its byte size or any `elapsed_s` delta — the packet reports byte honesty for the *legacy* pulse only (`source.legacy_pulse_bytes`). Order-of-magnitude from the real-input run (49 groups × ~24 members × 7 metrics × 9-key cells) is ~1–2 MB/night of new tracked churn, plus per-compute full-panel hashing/copies in `normalized_frame_receipt` (`engine/group_member_observations.py:156-198`) over `closes`, `rets`, `spy_adj`, `volumes`. Estimated, not measured — flagged as a render-budget/disk follow-up given this host's two ENOSPC incidents, not as a finding of harm.
- **NIT-1.** "History used" prints `observations_available / observations_required` where *available* is counted over the metric **window** but *required* is `minimum_observations` (`engine/group_member_observations.py:486-503`), so legacy_trend_50 can read "43 / 25" or "50 / 25". Honest, but the ratio invites misreading as a completion fraction.

EVIDENCE:
Falsification attempts that **failed** (i.e. the artifact held):
- *Benchmark pair.* `engine/group_pulse.py:1423-1426` refuses unless both of the last two **owner-panel** rows carry finite, positive, *actual* benchmark closes — `bench.reindex(panel["index"][-2:])` with **no** ffill, so a missing current row, missing prior row, stale tail, future-only index, NaN/0/negative/inf all refuse. On the accepted path the legacy `ffill()` at `:465` is provably a no-op for the two rows that produce `bench_ret` at `as_of`, so the observed relative value is the true two-close difference. `closes` is `sort_index()`-ed at `:416`, so `index[-2:]` is genuinely the last two sessions and `as_of == index[-1]`.
- *Raw preserved independently.* `engine/group_member_observations.py:537-556`: `needs_benchmark=False` for `raw_daily_change`, so a benchmark refusal never suppresses raw; pinned at `tests/test_group_member_observations.py:879-882`.
- *Provenance cannot be laundered.* I tried to construct an observed relative cell sourced from the close panel: `project_group_members` falls back `relative_receipt → price_receipt` (`:410-412`), but `validate_member_bundle:981-989` relaxes the expected basis **only** when `estimability_reason == "benchmark_unavailable"`, so an observed value bound to a non-relative receipt raises at `assemble_member_bundle:633-635`. Closed.
- *Missing/zero/false stay distinct.* Engine `_metric_cell:303-320`; validator "null value cannot have OK null_reason" (`:671-683`); UI `moCellValue` tests `cell.value==null` before `typeof 'boolean'` (`templates/basket_detail.html.j2:1145-1158`); pinned `tests/test_theme_detail_member_observations.py:556-591` (`+0.0` renders, `%` never appears on a pp cell, null never becomes `0.0`).
- *Units.* `benchmark_relative_daily_change` renders `(v*100).toFixed(1) + ' pp' / ' 个百分点'` (`:1155-1156`), labelled "(pp)" (`:1128`) with the "not a relative-price return" note (`:1195-1197`); raw renders `%` via `fmtPct` (`:397`). Confirmed visually in `member-evidence-en-dark-1440.png` (dark) and `member-evidence-zh-light-390.png` (light/ZH).
- *Legacy isolation.* `_assert_legacy_match:370-388` forces every legacy metric to equal the published pulse share/denominator/numerator; `tests/test_group_member_observations.py:893-909` recomputes the legacy pulse with its **own** `bench_ok=True` gate and asserts byte-identical wire output — the strict companion gate provably does not leak into legacy. Action/scoring roster stays the legacy `members` list (`scripts/build_theme_detail.py:306-315`), with `tests/test_theme_detail_member_observations.py:153`.
- *Strict vs legacy naming.* `templates/basket_detail.html.j2:1123-1126` ("Legacy 50-day breadth" vs "Strict 50-session breadth") plus distinct method notes `:1198-1203`; `tests/test_group_member_observations.py:474,520`.
- *Stale-state.* `engine/group_pulse.py:1722-1734` writes `pulse.json` unconditionally but the companion only when *this* invocation assembled one; a surviving stale companion then fails the consumer's digest binding (`scripts/build_theme_detail.py:235`) and the section disappears rather than showing yesterday's members under today's pulse (`tests/test_group_member_observations.py:448`).
- *Embedded DETAIL binding is structural, not metadata-deep.* `scripts/check_group_member_observations.py:115-158` parses the single `const DETAIL =` literal, rejects duplicate keys and non-finite constants, and compares `canonical_json_bytes(payload["member_observations"])` against the companion — `true` vs `1` cannot pass. This is a real static-publisher defect check, not a hypothetical-attacker check; I did **not** elevate the "malformed arbitrary JS" surface, which is not reachable from an owned template.
- *No design-system bypass.* All `.mo-*` material is governed CSS inside the template's `<style>` (`templates/basket_detail.html.j2:228-284`) using existing tokens; the added JS mounts DOM and sets state only — no `style.textContent`, `createElement('style')` or `insertRule`, so `check_runtime_style_injection` signatures do not fire and `check_ui_visual_evidence._is_material_inline_style` (`:346-349`) does not trip (added lines sit *inside* an unchanged `<style` tag). The mechanical gate is therefore green; MAJOR-1 is a house-law/reviewer finding, not a CI red.
- *Non-finding I explicitly cleared.* The duplicate `cls` (`:399` `const` vs `:1598` `function`) is not a redeclaration error — `:1598` is inside the IIFE opened at `:1592`. The red/green inversion between the EN-dark and ZH-light crops is the site-wide CN colour convention driven by `data-lang`, not a theme defect introduced here.
- *Both suites are CI-wired*: `.github/ci/legacy-jobs.yml:13900` (`tests/test_group_member_observations.py`) and `:13959` (`tests/test_theme_detail_member_observations.py`). The parent's reported runs are consistent with these jobs; I did not execute them.

GAPS:
- Cannot confirm the checkout equals `ce16dc6d…` (no Bash/git). Reviewed bytes-on-disk at the named path.
- Light **desktop** 1440 and the EN-light / ZH-dark cells have no committed image; light was adjudicable only at 390/ZH, where it reads as a legitimate research-workspace surface (white cards on cool canvas, hairline borders, shadow instead of glow, muted uppercase labels) — hierarchy and EN/ZH parity are sound in the two crops I could see.
- Did not run tests, the validator, the evidence scripts, or a browser; the parent's results are assessed as claims plus code, not reproduced.
- Companion artifact size, `elapsed_s` delta and any render-budget impact are unmeasured (MINOR-3 figure is an estimate).
- Non-US regions are unserved by design (`scripts/build_theme_detail.py:224-225`); I treated that as bounded P1 scope, not a gap.
- Untracked `__pycache__/*.pyc` sit under `research/sector_cycle_revamp/evidence/P1_BENCHMARK_PAIR_2026-09-17/`; I could not check tracking status without git. Worth the author confirming they are ignored, not committed.

DEVIATIONS:
- None from the commission's scope: read-only, single invocation, no Bash/network/MCP/agents, no GitHub mutation, no source change, no test execution, no approval action granted or implied.
- `PASS` was withheld solely on MAJOR-1 per the standing theme-art-direction law (incomplete dual-theme evidence ⇒ `PARTIAL`, never `PASS`). Nothing here waives the two known release dependencies (#7211 current-run/validator/preflight integration and deployed authenticated proof; queued main CI), and this review makes no claim about either.
- Terminal for this assignment. No successor work, no watcher, no polling.
