# Sector/Cycle R6 — shared Atlas reference, not production integration

**Parent mission ACTIVE. Independent reference implementation tested; production P1 remains unstarted and unadmitted.** This is a records-only continuation on research PR #7234. Keep Draft/HOLD. No change to current source owners, #7211 custody, runtime admission, source rights or predictive authority.

## Mission and evidence pins

The Chairman's outcome remains the integrated Sector/Cycle Intelligence revamp: native Grid/Clusters/Bubbles, structural and thematic granularity, complete standardized group journeys, better participation/rotation/economic/regime intelligence and separately earned Prophet contribution. The laboratory is not the advertised market universe or the finished system.

- Protected procedure: Mastermind `eec5324c5205e8bad206512e0a936898e50b2408`, Sol Skillpack 1.0.1/bootstrap 1. Required procedure blobs are unchanged from the prior full reads.
- Macro read pin: `3daf739affa12a43a6b3a0feb3b4c57154587756`.
- Research carrier: #7234 / `sol/sector-cycle-observations-r4-20260916`, pre-R6 head `21a8eb74a53ed765fcf0931ee7ab3fe0b7077b7c`. Closing pre-write metadata returned Draft/open/unmerged and `mergeable:false`; release/integration readiness is not established.
- Publication repair: #7211, inspected open/unmerged at `7e5d3ef135e1fb0bd19a21c1c0647f8ec6ad06d8`. No new scheduler/publisher and no takeover of that carrier.

## Execution boundary

A small Mac process/read probe and bounded worktree metadata inspection succeeded. The subsequent managed-worktree hook read was blocked by the platform before execution. No managed worktree, implementation branch, production source edit or worker was created. The blocked request was not retried through another host/tool/carrier.

The independent sandbox Atlas experiment does not execute that refused operation. No browser navigation was attempted in R6 after the earlier browser denial. No Executive ingress or eligible worker binding was established; this does not establish that the entire fabric is unavailable. Direct work retained PRINCIPAL_JUDGMENT for view/measurement semantics and NO_ELIGIBLE_PRE_EFFECT_WORKER for the bounded synthetic experiment. No Fable/provider session, Job or watcher was started.

## Capability delta

Before: the R5 laboratory had a member-observation contract and inspection specimen, but no shared large-universe representation engine.

After: pure JavaScript Grid, deterministic grouped circle Clusters, metric Bubbles and full-table data consume one immutable analytical snapshot. A Python adapter consumes the unchanged R5 synthetic validator and original-byte binding. The authored self-contained HTML includes those representations, search/filter, explicit rescope, group/member inspection, methodology and EN/ZH plus dark/light controls. Browser/DOM/layout/visual acceptance is not claimed.

### Shared population and measurement rules

- Representation changes, color selection and display filters do not change the analytical population, benchmark or aggregate. Explicit rescope creates a separate subset and preserves its input source.
- Group appearances, unique securities and known issuer identities are separate counts. Parent theme membership deduplicates securities shared by children. A source without issuer identity does not gain one from its ticker.
- Group indexing is built once. The large fixture does not rescan every security against every group.
- A frozen cohort is not presented as an ordinary reusable query link. Its exported research context includes exact member IDs and the input reference. Pure history transitions are tested; browser history/focus and account sync are not.

### Geometry rules

- One declared area scale across cells; no painted-radius floor that silently changes cap ratios. Minimum hit footprints are separate interaction geometry.
- Unknown/proxy capitalization uses a distinct unavailable mark, not a fabricated cap circle or bucket.
- Laboratory cap buckets accept only positive USD listing-equity values. Research defaults are mega >=200B, large >=10B, mid >=2B, small >=300M, micro >=50M, nano >0, else unknown. These are not verified Finviz equality rules or accepted Mastermind policy.
- Circle placement is a deterministic shelf-packing reference, not optimal packing, statistical clustering, correlation or causality.
- Bubble points with missing coordinates stay in a separate tray. The explicit central range retains outliers as counted edge markers and in the complete table; full range and invertible signed-log axes do not alter measurements.

### R5 adapter and authority rules

- Reuse the unchanged prior laboratory validator and original bytes; do not recreate the observation producer. This is NOT a production Group Pulse/GMI adapter.
- Seven scenarios: baseline, corrected, benchmark missing, absent, checksum mismatch, invalid contract and thin coverage.
- All 24 catalogue records survive absent/invalid companions. Effective date and measurements remain null when no valid observation exists.
- R5 supplies no capitalization, structural classifications, subthemes or canonical issuer identity. Those fields remain unknown. Daily returns do not become invented monthly returns.
- Explicit unit conversion only. Arithmetic benchmark-relative differences display percentage points, not percentage return. Boolean conditions use binary semantics; false/zero remain observed.
- Per-security plotting does not grant aggregation. R5 admits the strict-trend aggregate with five-member/60% floors, not a new mean-return measure. The adapter preserves the source's permission and denominator rules.
- View/contract statuses are laboratory states, not new Data OS missing-value enums or runtime authority.
- Synthetic checksum consistency is not production authentication or source-rights approval.

## Executed verification

Command in the conversation sandbox: `python verify_all.py` (Python 3.13.5; Node v22.16.0).

- 69 pure Node tests, including model/geometry/state and static copy tests.
- 12 Python adapter/build tests.
- Eight deliberate forbidden mutations detected by specific assertions, not import/syntax errors: proxy-as-cap; zero-as-positive; filter-recalculates-layout; missing-coordinate-as-zero; point-field-promoted-to-aggregate; ignored overlap filter; unavailable-size-faked; parent double-counts shared children.
- 18 geometric scale cases: 1,600/6,000/12,000 securities x industry/subtheme x Grid/Clusters/Bubbles.
- At 6,000 securities, the overlapping subtheme Grid/Clusters contain 7,129 appearances while unique securities remain 6,000.
- Maximum 14,258 appearances at 12,000 securities. 1,047,791 pair-separation checks across cases.
- Two consecutive standalone HTML builds byte-identical, 133,675 bytes.

These are mathematical/state/source tests, NOT browser throughput, visual acceptance, repository CI, real-data replay or predictive validation. Browser tests passed 0; DOM executed false; screenshots 0; real market inputs 0; production source changes 0; new provider/worker calls 0; trade authority none. The mutation cases are not asserted production defects.

Artifact SHA-256:

- `atlas_preview.html`: `cb3d35f96b5c3db885bbed684e44f24e4336cd6593cef6914f1b688cf143ab40`.
- `atlas_core.js`: `fabab397ce1f0ba358894aa52331f045d47aa77177b4a3fe1cfe25d396734ad9`.
- `atlas_controller.js`: `1e3cb4183492b0e952196d671aab108fe713e242c9fa8850cec49e5a610a7d55`.
- `atlas_ui.js`: `9045cc1334dedd17c8381f92f9300a40e3435fc45724115af742458c3604d4b5`.
- `r5_adapter.py`: `a3ad4e4ec71f222a65b667d2fbd3a262055d63b73ee1b4101d8cd5cca25f3b16`.
- `evidence/verification.json`: `f56828ddffaae24094a2eaeb7f8cf97fd8621a2d3642d957d5bc47d7ee9dc006`.
- `evidence/mutation_results.json`: `7615c04ca8f79e7466239bc0a87c6c29861d8b34a0ea44fc38d756ddb241a54a`.

The complete source, tests, synthetic inputs, RED/repair evidence, specimen and exact results are in the R6 conversation bundle. This record preserves the decision-relevant outcome; it does not claim that the laboratory code is committed to the production repository.

## Exact next action and stop condition

Establish the permitted source-owned managed-worktree/execution route and reconcile the accepted #7211 interface plus current planned-write collisions. Do not retry the denied hook read via another carrier. Keep #7234 research-only.

Then implement the scoped P1: capture full per-member evidence from the SAME successful Group Pulse invocation; consume it in the existing basket detail; leave legacy action/scoring inputs unchanged; extend the existing publication validation and trigger closure. A failed current invocation cannot reuse matching old files as fresh success. Source rights, canonical identity and null semantics remain with their existing owners.

After P1, adapt a compact accepted real read model to the shared views, obtain the real hub/member/company/return journey and dark/light x EN/ZH x desktop/mobile browser evidence, and prove advertised catalogue/lower-cap coverage. Do not widen into a new backend or silently promote context into ranking.

The source execution lane is blocked; the independent Atlas reference phase is at its verified boundary. Parent mission active. No background process is claimed. Next session must not repeat R1–R5 archaeology, R4 replay, this synthetic reference engine or old browser probes. Economics, regime, persistence and Prophet promotion remain original existing-owner programme work, not waived requirements.
