# Thematic Intelligence Layer (TIL) — adjudication + masterplan

**Status:** RATIFIED — build dispatched
**Date:** 2026-07-09
**Owner program:** `thematic-intelligence` (new; composes over qualitative-intelligence, oracle, and neural-web territory with fences recorded in §2)
**Method:** Codex memo (`research/CITRINI_THEMATIC_NEURAL_WEB_UPGRADE.md`) → 8-lane Sonnet census + Opus adversarial verify → 5-lens Opus ideation panel (41 ideas) + Opus xhigh judge (13 kills, ranked top-12, 3 combos) → this Fable adjudication.
**Operator directive:** build the capability to identify and front-run asymmetric themes Citrini-style; CitriniResearch Institutional access ($50k/yr, redistribution rights) is now live and must be integrated as an evidence source (§7).

---

## 0. Executive ruling

The Codex memo's diagnosis is right and its inventory is wrong. The house does **not** lack thematic machinery — the census found a ~4,900-line, 18-theme foresight desk with a live stage machine (PRECIPICE/BROADENING/RE-RATING/GLUT-RISK/WATCH), a deterministic thesis monitor (9 open / 3 BROKEN), a forward-graded ledger (45 rows accruing), a demand desk with 40 open falsifiable theses, a divergence radar with an IC harness (113 theses, 3,860 snapshots), 46 scored baskets with lifecycle labels, an 8-leg real-activity fuser, and 268-subsector RRG rotation. What the house lacks is **civilization between the organs**:

1. Three uncoordinated theme vocabularies (foresight's 18, baskets' 46, Finviz's 40/268) with no crosswalk — the root cause of every silo.
2. No theme-level thesis objects (mechanism / winners / losers / falsifiers as queryable records).
3. No persisted phase-state history (stages recomputed nightly, transitions mostly unlogged).
4. No generalized beneficiary/loser graph (one hardcoded AI-capex chain; 3 macro pathways).
5. No per-leg asymmetry read, no loser maps, no cross-theme terminal, no NW citizenship (themes are invisible to world_state, ask_brain, cortex, mastermind_context).
6. Accountability theater in three places: `earliness_log.jsonl` is written and read by nothing; qledger `falsifier`/`check_by` fields are stored and never evaluated; foresight's ledger logs only 2 of 5 stages.

TIL is therefore **connective tissue plus honesty machinery, not new signals**. Everything ships display/shadow tier freely (gauntlet = promotion gate, not build gate); the grading waves are what let any of it ever earn more.

> Product target (unchanged from the Codex memo, now enforceable): when asked "what is the next asymmetric theme?", the system returns a ranked *investigation packet* — theme, phase, mechanism, evidence, contradictions, crowding, entry cleanliness, falsifiers, maturity, and what data would change the view. Never a stock pick.

## 1. What was already ours (anti-duplication record)

| Codex/panel proposal | Existing home | Disposition |
|---|---|---|
| Theme phase model | `foresight_cascade.py` stage machine + `radar.py` lifecycle + `theme_scoring` labels | REUSE; TIL adds crosswalk + persisted history only |
| Thesis monitor / kill criteria | `thesis_monitor.py` + `deterministic_theses.jsonl` (100 rows) + `falsifier_tripwires.py` DSL | REUSE; TIL wires, does not rebuild |
| Real-activity evidence | `theme_activity.py` 8 legs | REUSE |
| Attention-vacuum detection | `foresight_divergence.py` hidden-opportunity quadrant (money high / narrative quiet) | ALREADY SHIPPED — killed as a "new" idea in panel; only the options-tape witness is net-new (§6 combo) |
| Driver→pathway graph pattern | `mechanism_pathways.v1` (nodes/edges/authority/EN-ZH) | PATTERN REUSED at theme granularity (sibling organ) |
| Per-stock thesis registry | `entity_thesis_mechanism_registry` + long-hold program (G1/F3/SUE/moat_falsifiers) | FENCED OUT of TIL entirely |
| Forward grading | `qledger` (10.9k claims, Wilson-CI) + `foresight_grader` (30/60/90d PIT) | REUSE verbatim |
| Docs Codex cited as prior art | `CAUSAL_DISCOVERY_*` and `NEURAL_WEB_CONTEXT_ORGANS_*` | DO NOT EXIST in repo — citation error, recorded |

## 2. Rulings (R-TIL-1 … R-TIL-9)

**R-TIL-1 — Not a lobe; theme ≠ stock.** Thematic intelligence decomposes into an organ cluster + waves (Claim-Reliability precedent: misfiling waves as lobes is how sprawl happens). The DO_NOT_REBUILD "thesis lobe" KILL (#1666/#1669/#1671) closed the *per-stock Long-Term-Thesis/Expectations-Drift* construction; theme-level thesis objects are a different construction and are legal. **Fence:** the theme thesis ledger holds zero per-stock thesis records; anything thesis-shaped about an individual name routes to the long-hold program's amendment process.

**R-TIL-2 — Beneficiary/loser graph is a curated taxonomy, never a rotation call.** The graph ships as PR-reviewed config (TI-R2 shape: versioned membership + one-line rationale per edge) rendered display-only, mirroring `mechanism_pathways.v1` conventions. The TI-R5 kill stands in full force: no runtime "shock/phase active → beneficiary now" mapping may exist; graph context may temper, never direct; no board ordering, no brain-prompt escalation, no beneficiary field on any scored path. Loser legs are AVOID-shaped evidence (short-side lobe ruling: L1 is AVOID-not-SHORT).

**R-TIL-3 — No fused asymmetry number.** WA-R1 precedent: the asymmetry surface is a per-leg component panel; no master score, no weighted composite, no rank derived from it. Any pairwise leg interaction requires its own prereg naming both parents (factor-kill-interaction law).

**R-TIL-4 — Ownership/attention legs are hazard context only.** NEXTL-U13: ownership-style data may never be a positive signal; it survives as crowding-hazard context. The attention-vacuum construct is already shipped (divergence radar hidden-opportunity quadrant) — TIL surfaces it, does not rebuild it.

**R-TIL-5 — LLM = extraction with receipts, nothing else.** LLM use in TIL is limited to structuring human-originated text (Citrini articles, filings language) into records carrying exact-substring citations validated deterministically (char-span check). No LLM-originated signals, scores, escalations, or phase calls. De-escalation of calibrated keys only, per house law.

**R-TIL-6 — Grading law.** All TIL artifacts are display/shadow. Promotion of anything (phase-transition value, asymmetry legs, Citrini-follow) requires: pre-registered trial on the accrued PIT tapes; placebo control (random-theme + time-shifted, extending the qledger placebo sampler); hits defined as **alpha over a free factor panel, not raw excess** (the selection-effect test — Citrini's own claim, now our ruler); era law and time-preserving nulls apply. First eligible reads ~2026-10+.

**R-TIL-7 — Citrini is a graded evidence source, not an oracle.** DannyTrades precedent: external analysts get graded, not trusted. Signal Commons W6 (paid-data SKIP-ALL) does not bind data the operator already purchased (ThetaData precedent). Positioning-fusion law binds hard: Citrini weights/changes are display/context + PIT tape; any "follow their adds" construct is a future pre-registered trial on the accrued tape. **Ingestion mechanics are OPEN-OPERATOR** (§7.4) — no credential handling, no scraping, no docsend/substack/citrindex fetcher ships until the operator rules on the four pending access questions.

**R-TIL-8 — Scenario lab deferred, curated-only.** Citrini-style scenario maps ("who is synthetically short theme X") are legal only as PR-reviewed curated configs with `not_forecast: true` + probability bands, display-only, class-level. DEFERRED to a post-core wave; no runtime scenario→position mapping ever.

**R-TIL-9 — New-collector law.** New public-trace legs (DOL PERM/LCA, USITC/Census trade, ClinicalTrials theme rollup, phrase-velocity) must be: keyless/free, PIT-clean, off the render path (collect lane or R2), entity→ticker joins via the existing WARN/sponsor fuzzy-map pattern, and each lands as *evidence into existing organs* (theme_activity SOURCES, discovery feeds) — never as a new parallel scorer.

## 3. Architecture

```
                      config/theme_crosswalk.yml          (W0 — canonical theme ids ↔ foresight/baskets/subsectors/citrini)
                      config/theme_thesis_registry.yml    (W1 — curated theses: mechanism/winners/losers/falsifiers)
                      config/theme_pathways.yml           (W2 — curated beneficiary/loser chains)
                                 │
   foresight_cascade ─┐          ▼
   theme_scoring ─────┤   engine/neuralweb/thematic_state.py     → data/neuralweb/theme_state.json
   radar/radar_plus ──┼── engine/neuralweb/theme_thesis.py       → data/neuralweb/theme_thesis_ledger.jsonl
   narrative_emergence┤   engine/neuralweb/theme_pathways.py     → data/neuralweb/theme_pathways.json
   theme_activity ────┤   engine/neuralweb/theme_asymmetry.py    → data/neuralweb/theme_asymmetry.json
   subsector_rotation ┘          │                                 data/neuralweb/theme_phase_history.jsonl (PIT tape)
                                 ▼
              site/neuralwebdata/theme_state.json (+ projections) → templates/state_of_themes.html.j2 → site/state_of_themes.html
                                 ▼
              world_state `thematic_state` block · ask_brain read tools · cortex tool schema · mastermind_context summarizer
```

All builders: `envelope.stamp()`, atomic write, exit-0-always, tolerant readers, EN/ZH strings in data, authority blocks with `may_rank/gate/size/escalate = false`.

## 4. Core waves (dispatch order)

| Wave | PR | Contents | Deps |
|---|---|---|---|
| **W0 state spine** | PR-A | `config/theme_crosswalk.yml` (canonical ids; foresight 18 ↔ baskets 46 ↔ Finviz subsector keys; citrini column reserved) + `thematic_state.py` composition organ + `theme_phase_history.jsonl` PIT tape (hash-chained, nightly sole advancer) + foresight `_append_ledger` fix to log ALL five stages (executes FORESIGHT_DESK_UPGRADE item 0a charter — attribution, not re-charter) + subsector↔basket bridge keys | none |
| **W0b quick win** | PR-B | WARN-Act wiring (judge #2): orphaned `warn_notices.py` output → `theme_activity` signed loser-cohort leg + employer→ticker fuzzy map; display/confluence only | none |
| **W1 thesis ledger** | PR-C | `theme_thesis_registry.yml` seeded for all 18 themes (mechanism, variant-perception, winner/loser *classes*, machine-checkable falsifiers) + `theme_thesis.py` builder → append-only ledger + falsifiers compiled through `falsifier_tripwires` DSL into `thesis_monitor` | PR-A ids |
| **W2 pathways graph** | PR-D | `theme_pathways.yml` curated chains (driver→bottleneck→beneficiaries→implementers→losers, first/second/third order, confidence + evidence source per edge) + `theme_pathways.py` organ (mechanism_pathways conventions) + purity-weighted cross-theme node-collision map (judge combo 3: flags when N baskets' apparent confluence runs through one ticker) + LBNL queue decomposition (judge #5) as evidence enrichment | PR-A ids |
| **W3 asymmetry panel** | PR-E | `theme_asymmetry.py` per-leg panel: bottleneck tightness (T1 FRED + activity), stale-consensus gap (judge #11: real-activity direction × analyst-revision trajectory × revision dispersion), cyclical dislocation (drawdown vs RS), entry cleanliness (extension grades), crowding hazard (crowding_pen + divergence quadrant), falsifier clarity (from W1), orthogonality (covariance spine). NO composite. | PR-A, PR-C |
| **W4 terminal** | PR-F | `state_of_themes.html` — mockups approved BEFORE build (terminal-ui-quality-bar law; browser-verified with prod-shaped data); cross-theme matrix, phase timelines (from the tape), pathway graph view, asymmetry panels, thesis health, weekly delta strip; EN/ZH; theme.js menu | PR-A..E data |
| **W5 NW citizenship** | PR-G | synapse.yml entries + dag.yml + SIGNAL_BUS regen + world_state `thematic_state` block + ask_brain `read_theme_state`/`read_theme_thesis`/`read_theme_pathways` + trigger terms + cortex `_tool_schemas` + mastermind_context summarizer. **Scheduled LAST-of-core** to dodge open-PR synapse collisions (#1891/#1840); rebase + regen recipe applies | PR-A..E; #1891/#1840 merged or rebased-over |
| **W6 grading pack** | PR-H | Crowd-arrival lead-lag grader over `earliness_log.jsonl` (judge #3 — the front-run receipts); falsifier auto-evaluation engine over qledger check-specs (judge #4); random-theme + time-shifted placebo tape (judge #8); phase-transition PIT input-vector snapshots (judge #10); theme-return factor decomposition redefining hits as alpha (judge #6). Come-back clock: first promotion-eligible read **2026-10-15** | PR-A tape |

## 5. Extension waves (chartered, build after core)

| Wave | Contents | Notes |
|---|---|---|
| W7 hiring-intent | DOL PERM/LCA bulk ingestion (judge #1) → per-basket hiring-intent leg in theme_activity | R-TIL-9; fills demand desk's own named gap |
| W8 trade-flows | Census/USITC HS-code import trace (judge #9) for onshoring/substitution themes | R-TIL-9 |
| W9 discovery v2 | EDGAR unsupervised phrase-velocity (judge #7) + GitHub/npm theme-level adoption velocity as two-source discovery confluence (judge combo 2) → feeds theme_discovery candidates | discovery stays probation-gated |
| W10 clinical | ClinicalTrials.gov theme-level pipeline density / phase-migration (judge #12) | reuses per-stock collector |
| W11 options witness | OI-concentration + options-tape independent witness on theme crowding (judge combo 1) | ThetaData runner-local store; display-only |
| W12 scenario lab | Curated scenario configs per R-TIL-8 | deferred by ruling |

## 6. Panel kills (program-internal record; no DO_NOT_REBUILD rows — none rise to registry level)

Attention-vacuum standalone (3 lenses — already shipped in divergence radar); state-of-themes/weekly-delta/ask-brain/thesis-cockpit restatements (already planned); loser-graph restatement (in W2); investigation-packets as separate build (folds into W4); attention-router (WA-R1 exposure — a ranked attention queue is a fused conviction score in disguise); same-trade beta unmasking (era-confound); valuation-quadrant standalone (composition without new content); post-mortem ledger (premature until graders accrue — revisit post-2026-10); segment-revenue purity standalone (folded into W2 collision map as weight input).

## 7. Citrini Intelligence Desk (CITR family — design ratified, ingestion OPEN-OPERATOR)

### 7.1 Stance
CitriniResearch Institutional (Substack research + daily docsend portfolio changes + Citrindex app: 188 baskets, holdings, weight changes, rankings, macro trades) enters the house as a **graded external evidence organ** per R-TIL-7. The tape starts as early as mechanics allow — every untaped day is unrecoverable history.

### 7.2 Waves (paper design; build gated on §7.4)
- **CITR-0 PIT tape:** daily snapshots — per-basket + per-security weights, options overlays, cash/net/gross exposure, basket library (inceptions, returns), rankings. Append-only `data/citrini/` (small; git).
- **CITR-1 change events (deterministic):** weight deltas, adds/drops with size, new-basket launches, derisking reads (net/gross/cash trend + hedge overlays), theme-weight migration, conviction (weight × concentration) → `citrini_events.jsonl`.
- **CITR-2 crosswalk:** their 188 baskets ↔ `theme_crosswalk.yml` ids; their memberships become `source: citrini` evidence edges in W2's graph; their holdings vs our basket membership diffs = candidate membership upgrades (operator-reviewed).
- **CITR-3 substack extraction:** R-TIL-5 contract (exact-substring citations, char-span validated) → external thesis records in the W1 ledger, `source: citrini`, thesis-change events on approach pivots.
- **CITR-4 forward grading:** every change event + extracted call → qledger `citrini_*` claim families, standard horizons, per-basket/theme calibrated track record. Two registered future studies on the accrued tape: (a) lead-lag — do their adds precede our phase transitions or vice versa; (b) retro-benchmark — grade our discovery engines against their 188 basket inception dates ("would we have flagged GLP-1 before their basket existed?").
- **CITR-5 NW wiring:** `citrini` block in thematic_state, ask_brain `read_citrini_book`, State-of-Themes "Citrini lens" column, and **Citrini-vs-house contradiction records** (their book adds vs our GLUT-RISK stage → confluence contradiction, investigation packet).

### 7.3 Fences
Positioning never fused (Signal Commons law); display/context + tape only; LLM extraction-only; per-DannyTrades the source is graded before it is believed; raw content is not republished — surface scope pending §7.4 Q4.

### 7.4 OPEN-OPERATOR (four dismissed questions — awaiting ruling; no build until answered)
1. Citrindex automated auth (stored session on runner / manual export drop / Chrome-assisted).
2. Docsend: ingest or skip as Citrindex-superset.
3. Substack path (session fetcher / email forwarding / manual drop).
4. Display scope for Citrini-derived content (operator-authed only vs public, notwithstanding redistribution rights).

**RESOLVED — operator ruling 2026-08-11** (recorded via the GMI program, `research/GLOBAL_MARKET_INTELLIGENCE_MASTERPLAN_BY_FABLE.md` §10.3): *"existing themes from Citrini can be used, but we no longer use their feeds."* All four access questions are therefore moot: **CITR-0/1/3/4 (live tape, change events, substack extraction, forward grading of their changes) are CLOSED — no feed ingestion ever ships.** The surviving asset is the already-in-hand basket/theme **definitions**, usable as operator-reviewed curated crosswalk evidence (`source: citrini`; R-TIL-7's display/context posture unchanged; no live holdings diffs). CITR-2 survives in that definition-only form; `theme_crosswalk.yml`'s `citrini_basket_ids` column may be filled from the in-hand definitions.

## 8. Ops ledger
- **BLOCKED-EXTERNAL:** `GRANTS_GOV_API_KEY`, `SAM_API_KEY` repo secrets absent (two pre-award activity legs dark); runner `ANTHROPIC_API_KEY` unverified for `foresight_analyst`/`narrative_brain` (LLM lanes dark by default).
- **Collision watch:** #1891/#1840 hold synapse.yml + `engine/neuralweb/query.py` — W5 sequenced last-of-core.
- **CI landmines honored:** SIGNAL_BUS byte-match regen; dag conformance + synapse-count pins; template_site_sync (avoided — page is .j2-rendered); "validated" word ban; no translated `title=` attributes; inline-JS check gap (browser-verify the rendered page, not just CI).
- **Render budget:** composition organs are adapter-cheap (nightly-safe); all new collectors off render path per R-TIL-9.

## 9. Clocks
- **2026-08-01** — first foresight forward-ledger grades mature; check grader output is non-empty.
- **2026-09-17** — first radar IC 63d grades mature.
- **2026-10-15** — W6 come-back: first promotion-eligible read on phase-transition value + asymmetry legs (placebo-controlled, alpha-ruler); post-mortem ledger revisit.
- **On CITR-0 + 90d accrual** — register CITR lead-lag + retro-benchmark preregs.
