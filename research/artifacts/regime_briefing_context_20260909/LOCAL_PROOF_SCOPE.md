# Local consumer proof — not production adoption

Input source: macro a4d33f32dad140acdfa081b21f55ad6d8dcb94d4, data/regime/latest.json.
The exact existing reader and actual saved-output/renderer code run against that committed input.
Synthesis and translation are stubbed with a previously saved brief. The macro thesis append is
intercepted. No product model/provider call, production write, source accrual or portfolio change occurs.

The page's old narrative prose is fixture content, not newly generated wording. It is NOT evidence
that the new prompt makes a live model comply, improves forecast quality, or resolves all narrative
contradictions. What is proven locally is the deterministic note, source-age interpretation, saved
field overwrite, bilingual rendering, escaped details and component composition.

The real input is correctly historical under its own September8 source date and existing30-hour SLA.
The separate current/disagreement example modifies probabilities and uses an explicitly named earlier
reference clock. It is a controlled fixture, not a current market claim. Invalid and missing cases
produce an explicit unavailable note. Original vintage correctness remains uncertified in every case.

Reproduce with reproduce_local_consumer.py --output-root NEW_DIRECTORY_OUTSIDE_CHECKOUT.
The output must not already exist. local_consumer_proof.json records exact input and source hashes.

Canonical browser evidence is produced by the existing scripts/capture_page_evidence.py and its real
Playwright driver, not a new manifest schema. The test-only wrapper blocks non-local requests. Both
full-template scenarios carry all eight desktop/mobile x EN/ZH x dark/light REST captures. A1200ms
settling period lets the existing1100ms theme animation finish; initial in-animation shots are not used.
Static assets and fonts are exact repository copies in the isolated fixture site; none are a new design.
Authenticated production access and newly generated model quality remain explicit gaps.
