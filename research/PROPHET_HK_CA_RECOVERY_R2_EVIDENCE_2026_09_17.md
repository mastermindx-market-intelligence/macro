# HK/Canada recovery R2 — executable evidence and release frontier

Operation: `prophet-hk-ca-end-to-end-recovery-20260917-sol-001`.
Existing owner: `WS:PROPHET-HK-CA-REVAMP`. Parent recovery carrier: Macro PR #7248.
Protected procedure: `Mastermind@42d210bc07a75234092ff5be71f6038ccacaa884`, Skillpack 1.0.1/bootstrap 1.
Analysis source: `macro@fdc881221cbf2029360da5481d62b0f558232caa`.

**Status: verification and candidate-repair preparation, not a repaired production product.** No application source branch, entry policy, ranker, provider, Executive Job, credential, live service, Caddy deployment or production page was changed. Native GitHub records/review comments are the only organizational publication effects. The proposed shell-asset patch exists in isolated analysis scratch, not an implementation carrier.

This is a continuation of `research/PROPHET_HK_CA_END_TO_END_RECOVERY_2026_09_17.md`, not the separate global Prophet model-research Run 2. Do not repeat the original broad investigation.

## 1. Canada #7018: exact repair remains unconsumed

The branch was freshly read again after the proof work and still identifies `f8779cbaf329266df3e1e4a573158b8b8af1c74b`. The original repair remains comment 5710276501 on #7018: add `scripts/canada_theme_action_map.py` to the EXISTING `conviction-profile` job's declared `paths:` in `.github/ci/legacy-jobs.yml`. The observed current-head `contract-delta` failure is check 105087538534 in run 35185804120. No implementation or worker consumption is inferred from the request.

A bounded proof executed the actual `_glob_to_regex` / `_matches_any` functions extracted from `scripts/run_ci_pack.py` at that exact head. The real manifest does not cover the dependency. Adding the one exact path in an in-memory copy changes the native match from false to true, preserves all original paths and `scope: exclusive`, leaves the total at 213 jobs, and changes only `conviction-profile`.

This establishes the precise missing declaration and a discriminating repair direction. It is NOT a full import-closure recomputation or a passing new-head `contract-delta` run. No manifest was edited in the source branch. Do not add another job, disable the guard, narrow coverage or excuse this as an inherited failure.

#7163 remains the existing HK publication/sector-link successor. Neither carrier's source writer was replaced. A public-shell asset repair is a distinct source surface and need not inherit a fictitious logical dependency on the Canada theme map, but still requires current collision and source-custody qualification.

## 2. #6996: current trust impact, accepted review preserved

Exact head: `30da44be695d5b7b249d0180f3af6d21ed5bad09`.
Operation: `prophet-entry-wait-copy-20260908-sol-001`.
Source path: `engine/entry_signal.py`.
Current-main/pre-repair blob: `42f2e8b0069cccb82282ac66fea8ba0c72e42d3c`.
Candidate blob: `93cc3cf7dd68c26736b52c19302c12c2199579c2`.

Three of the seven Canada shortlist rows in the September 15 artifact are exact witnesses: DSG.TO, GIB-A.TO and TOU.TO. They carry `status=await_confluence`, `confluence_gated=true`, and `act_level=1`, while their action/next-trigger wording still says the entry window is open or to take a half position. These are artifact-level contradictions, not trading recommendations or evidence of user losses.

**Production-surface limit:** those longer instruction strings were not found in the fetched anonymous Canada HTML. Its embedded table carries simpler status fields. Do not turn these witnesses into a claim that the authenticated Canadian hover or lookup journey was reproduced.

The existing candidate fixes the actual shared producer: only the three copy fields change under the existing gate. It does not alter status, predicate, score, sizing, geometry, rank, chronology or nongated instructions. A Canada-only frontend workaround would duplicate the repair and leave other consumers inconsistent.

### Executed source-snapshot campaign

Studio analysis PID 13458, Python 3.14, exact immutable source and owning test cases:

- Candidate tests against current-main implementation: 4 intended copy failures / 20 passed, exit 1; the two renderer cases were explicitly excluded while their actual dependencies were obtained.
- Same bounded cases against candidate: 24 passed, exit 0.
- Full owning test file plus its real China shelf/Jinja/i18n dependencies from the candidate head: **26 passed, zero deselected**, exit 0, 0.68 seconds.

The snapshot harness bound the actual compiled modules into a minimal package namespace. This proves the exercised code and real Jinja propagation; it is not a whole-repository import/conftest, hosted-CI, browser or production test. No source worktree was changed. The analysis REPL later ceased to have an available tool session; a requested exit could not establish its prior exit status. Do not reuse or kill that PID blindly. No source-modifying process or worker launch depended on it.

### Do not reopen an already accepted review

Comments 5591002084 and 5593104002 on #6996 already accepted the native semantic PASS and terminally closed its finite reviewer. An empty GitHub reviews array did not erase that acceptance. This turn added GitHub review COMMENT **5232731424**, anchored to the same head, preserving the accepted review and adding the executed evidence above. It is not a fresh approval requirement, a source-writer release or permission to merge.

The inspected exact-head ordinary and trusted executor packs, ci-gate, contract-delta and fences have now concluded success; the old all-packs-queued note is historical. `ci-authority/codex/merge-queue-pilot` still reports failure; its applicability was not waived. The observed merge ref `9c7fd5d1c253adb8f180125c2076679f06ce9305` binds older base `c359ed4403c9b8a8d097a8839c99448188618298`, not the analysis-main pin. Current integration/release proof remains separate.

### Source Continuity already has an owner and sequence

The protected adapter blob `e547b9d08ccc66100ad9e0e1c49a79cab8067e28` declares a 256-PR census ceiling; a bounded GitHub search returned 285 open Macro PRs with `incomplete_results=false`. This is serviceability evidence, NOT a newly executed official adapter refusal or a substitute receipt.

The current owner is Mastermind **issue #346**, not a PR. Its latest inspected comment, **5708496784** (2026-09-17 04:24:36Z), already names the sequence: #710 source writer/review, subject-PR semantic-revalidation amendment, protected #685 proof/release/install, then **Macro-300**. Do not create a competing census implementation or locally increase the verifier's limits. The comment's synthetic candidate receipt is explicitly not protected release authority.

#685 was freshly read: it is still Draft/HOLD at `b1d2012ab5ca2565af40a66635d3f2507cf4b5b6`. Its feature is existing volume/free-space policy enforcement, **not** support for choosing the Macro repository. Do not claim that its storage feature solves the separately unproven Macro workspace route.

## 3. Measured coverage replaces guessed explanations

| Inspected boundary | Hong Kong, September 16 | Canada, September 15 |
|---|---:|---:|
| Published source population for this stage | 156 beta/scoreboard source rows | 215 scored candidates |
| Analyzed HK rows / aligned Canadian rows | 156 analyzed | 1 aligned |
| HK exclusions between these two stages | 0 | Not the same population metric |
| Main-board rows | 1 | 7: 1 aligned + 6 near |
| Watch rows | 8 | 8, all knife warnings |

For this HK build, the main list is already one row BEFORE the later risk demotions. The observed 156-to-156 record/history coverage boundary and subsequent demotions therefore do not explain the one-card width. The upstream beta/universe restriction is still a separate unmeasured boundary. `health=null` is not proof every optional feature or source is fresh.

HK also emits **62 washout-watch rows**: 16 `ignition_watch`, 46 `washout_watch`; all are `display_only=true` and carry a September 16 row stamp. Nine overlap the earlier 59-name multi-list census, so they add 53 distinct names, producing **112 distinct names across all inspected lists**. This includes risk and historical/refusal material, not 112 current opportunities and certainly not 112 BUYs. A row stamp does not prove all contributing organs have the same freshness.

The product implication is to compose useful current discovery and monitoring coverage, not fill the main view with every historical or dangerous name. Keep refused/recently-fired history and risk context explicitly separate. No buy/watch population or score was changed by this census.

Canada's 215 is a scored-candidate denominator, not the entire Canadian exchange. The artifact supports one aligned and six near rows; the possible near-overflow gap remains unmeasured and must not be presented as a counted current loss. The native first-five display restriction is separate. Seven visible cards in the unstyled anonymous browser observation below are not proof that the intended authenticated first-five restriction was fixed.

## 4. Real public first-load failure: required shell assets are gated

A fresh isolated headless Chromium context, with no existing profile or authentication state, opened both actual production URLs. Both pages returned HTTP 200. At 1440x1080, the inspected HK document was 8,352 pixels tall; its washout section began around y=5,304. The Canadian document was 2,402 pixels tall. There were no captured page exceptions, but that did not establish asset completeness or a working interface.

The HK screenshot visibly contained native unstyled source/view controls and an overlong, partly styled shell. A follow-up resource check established:

| Actual public request | Result |
|---|---|
| `stock-dashboard.css?v=39067818` | 401, application/json, `authentication_required` |
| `hk-stock-v36.js` | 401, application/json |
| `canada-stock-v36.js` | 401, application/json |
| `theme.css` | 200, text/css |
| `dashboard-icons.js` | 200, JavaScript |

This is a real anonymous first-frame/access-boundary problem, **not authenticated customer-browser proof and not a complete explanation of the Chairman's logged-in screenshots**. The English anonymous washout view also retained Chinese signal chips; that is an observed parity concern, not an invented translated signal.

The source explanation is coherent with the responses. `config/site_access.yml` says HTML shells are public, while static assets are default-deny unless reviewed public. All three new dashboard assets are absent from that public registry and from the corresponding Caddy lists. Thus valid HTML and old shared chrome can load while the newer dashboard styling/interaction assets are refused. The exact deployed Caddy commit was not inspected; do not claim an installed-revision proof.

### Bounded candidate repair, not applied

A two-file proposal was prepared in analysis scratch:

1. Add exactly `/stock-dashboard.css`, `/hk-stock-v36.js`, and `/canada-stock-v36.js` to the existing `public.exact` registry in `config/site_access.yml`.
2. Add those same exact paths to the existing four Caddy matchers: `@reg_asset`, `@reg_asset_err`, `@public_static`, and `@public_versioned`.
3. Add no wildcard, do not relocate gated assets under an already-public wildcard to bypass review, and do not change private payload, per-ticker, registration, paywall or API policy.

This is a **security-sensitive static-asset classification proposal**, not a granted policy waiver. Actual asset-content review, current collisions, existing full serving-boundary/negative tests, qualified source custody and normal release remain required. The first-frame repair may not expose premium payloads as a side effect.

Executed proof, Studio PID 57686, exit 0:

- Both original and proposed configurations pass installed Caddy v2.11.4 `adapt`.
- The same analysis input filename was used for both passes, preserving Caddy's automatic config-file-hide behavior rather than normalizing it away.
- Four source matcher locations become eight path-list changes after the shared snippet expands across the two configured hosts.
- Every changed adapted list adds only the three exact assets. All other adapted configuration is equal; all other YAML policy is equal.
- The actual existing `test_caddy_public_boundary_matches_policy_exactly` and its helper, extracted from test blob `d9964f66da90fb2f402b8b975b2ad677b92afd61`, pass on both original and proposal. This is the one existing alignment check, not the whole boundary suite.
- The required-assets presence witness changes from three absent to three present. The old alignment test alone cannot detect a coordinated omission from both lists, so implementation still owes a dedicated shell-dependency regression.

No server was started/reloaded and no application source branch was edited. Native parsing and exact delta checks are not runtime request or production proof. The full proposal patch is retained in the owned analysis scratch with SHA-256 `472c5e5f9e0c160c65c07cd0fd4791fb6918dbbe8578c97264dcf392c2b510af`.

## 5. Evidence locations and integrity

Studio scratch root: `/tmp/sol-hkca-ceo-20260917/continuation-r2`.
The immutable source refs and bounded methods above remain the recovery basis if scratch expires; it is not another canonical state store.

| File | SHA-256 |
|---|---|
| `hk_standouts.json` | `42ba81f0c1ef930b1335fc46174376c2fb35c0b93b2b8cafce786a4150cb52fc` |
| `canada_standouts.json` | `e24c41c6a52e02803bcc0484f1b76f9e559948ce2711a60d00bfa224b1a56b24` |
| `public-browser-observation.json` | `d3fbfd3a6acb0057ab3f62e1fe306d9eb40784027c6723c79d4ad1c90d211cea` |
| `hk-public-desktop.png` | `e8964ec5871d3d77357c629f368ea049e160593227d7011eb9185c177d14c883` |
| `canada-public-desktop.png` | `a31fb658fa65f6057624ca58a9b5f8ccb9f2fdc07c9d77b36287b5e572d574cb` |
| `public-shell-assets-proposal-proof.json` | `9dcd3d64ea93cac65b09dd733881ac75a27d0f5e73954f5d43fbba2e50d4bddf` |

## 6. Exact continuation, not another diagnosis

Primary existing-source action: #7018's incumbent applies the one-path manifest repair and returns exact-head contract-delta/CI evidence; #7163 follows its recorded release sequence. #6996 independently needs current Source Continuity/current-base/release reconciliation and ordinary publication, not another implementation or full semantic review.

The public-shell proposal is separately ready for bounded source implementation once an existing qualified Macro source route/receiver and a fresh collision census are available. Preferred avenue is Terra; without a receiver it is WAITING_CAPACITY, not a dispatched child. Preserve review of the static-asset security boundary and negative payload-access cases.

The last inspected Executive snapshot remains unarmed (`do_not_submit=true`, zero workers/attempts). The installed attended launcher remains Mastermind-bound; no supported Macro-bound receipt was obtained. Neither limitation was bypassed. Existing Source Continuity/environment/Fabric owners retain their own repairs; do not invent a replacement installer, queue or verifier.

The broader coherent-opportunity UI and regional intelligence upgrade remain in scope and unbuilt. Completion still requires normal source-to-publication delivery, authenticated production behavior, dark/light and EN/ZH responsive proof, correct data/null/correction behavior, and meaningful learning through the existing owners. More visible watch rows, green tests, a proposal patch, or this checkpoint do not satisfy that end state.
