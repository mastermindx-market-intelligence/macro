# Mastermind AI Superintelligence Masterplan — the Analyst OS

**Status:** RATIFIED · P0 ships with this PR · Waves W1–W3 chartered below
**Date:** 2026-07-29 · **Author:** Fable main loop (recon: 4 sweep agents over both repos)
**Trigger:** operator handoff `mastermind_ai_market_reasoning_fable_handoff.md` (ChatGPT Sol conversation, 2026-07-29) — a reference-quality cross-asset answer (bear-steepening stress day) plus ~15 proposed components to close the gap between generic frontier chat and Mastermind AI.
**Companion doctrine:** the handoff's own thesis, adopted as this program's north star — *the moat is not the backbone model; it is an analysis operating system that knows what to observe, what to retrieve, how to test competing explanations, and how to convert evidence into a concise market narrative.*

---

## §0 ACCEPTANCE GATES

**P0 (this PR) is not done unless:**
1. A market-analysis question on the chat surface receives the analyst treatment end-to-end: the system prompt carries the Market Analyst doctrine block (routed by triggers, protocol always-on), the per-turn grounding is the structured Live Market State Packet (TAPE/CURVE/FLAGS/EVENTS/DRIVERS/RATES DESK/VOL/BREADTH/DESK READ/WATCH with per-section freshness stamps), and the model can call `get_market_events` + `search_research` (tier-gated).
2. `tests/test_brain_gateway.py`, `tests/test_brain_doctrine.py`, `tests/test_ask_brain.py` pass UNCHANGED (additive-only prompt changes; existing pins intact), plus new suites `test_brain_analyst_doctrine.py`, `test_market_packet.py`, `test_brain_market_intel.py` green.
3. Leak law extended: analyst-doctrine sentinels join the gateway leak screen; a doctrine echo in an answer is filtered exactly like technician-doctrine echoes today.
4. No epistemics regressions, mechanically guarded: no numeric confidence in the packet render (`test_market_packet` regex guard), no effect-chain fields in events output (whitelist test), no invented odds in doctrine bodies (regex guard).
5. Fail-soft everywhere: every packet block and both tools degrade to absent/empty on missing artifacts — a dev checkout with no `site/live/` still boots and answers.
6. Live verification after merge + VPS deploy: macro-api restarted, a guest `POST /api/brain/stream` turn on "why is the market moving today" returns SSE with a grounded answer, and journalctl shows no new errors. Packet content verified present in the prompt path via a local build against the deployed checkout.
7. Token cost bounded: packet ≤4200 chars (~1.1k tokens) with budget-drop test; doctrine block ≤12.5k chars; both ride prompt-cache-friendly positions (system prompt cached; packet rides the user turn as today's digest does).

**Every later wave inherits:** gates INLINE in its spawn prompt; no child-agent self-merge of flagship surfaces; display-tier freely, gauntlet only at promotion; "validated" never in user-facing copy; falsifier language never front-facing (windows/conditions, "what would change the read").

---

## §1 Objective and reference standard

Make Mastermind AI answer live-market questions at or above the handoff's reference answer, using our proprietary estate as the edge. The reference behavior, distilled into testable properties:

1. Read the tape's *pattern* before naming a cause (regime from structure, not from one ticker).
2. Eliminate regime families explicitly ("bonds down too — kills the recession-panic story").
3. Verify the day's catalyst against a live events feed — never infer causes from prices alone, never start from a headline.
4. Explain the confusing leg mechanically (duration arithmetic: 13bp × ~16y duration ≈ −1.6% TLT).
5. Test the story cross-asset and name the leftovers (semis −4% = a second, independent driver).
6. Separate observed facts / desk-calibrated readings / the model's own inference.
7. End forward with conditional signposts, never certainty or odds.
8. Keep the house voice: plain words, user's numbers quoted back, stance line, [NEXT].

Benchmark case (frozen, from the handoff §12): indices −1.7..−2.3%, 1Y −5.1bp, 2Y −1.7bp, 5Y +4.6bp, 10Y +8.6bp, 20Y +12.9bp, TLT −1.65%, TIPS ~+1bp → expected: inflationary/term-premium stress family, front-vs-long split explained, TLT move mechanically reconciled, oil/news check performed, semis flagged as second story, conditional signposts. Gold-standard synthesis sentence: "the market is pricing weaker near-term growth but worse long-term inflation and fiscal credibility."

## §2 Ground truth (recon summary — what already exists)

- **One gateway, two surfaces.** `engine/neuralweb/brain_gateway.py` (6.4k lines) serves `/api/brain/*` for BOTH the dashboard widget (`mm_brain.js`) and the Terminal (BrainWidget proxies with Supabase auth). The Terminal-side copilot route is deprecated. All P0 work lands in this repo only.
- **Lanes:** fast = DeepSeek V4 Pro (haiku fallback), pro = GPT-5.6 Sol high (codex) → Opus 5 high (oauth) → anthropic; research mode = pro + tool_budget 20. Vision = Pro lane, Claude-routed, ≤4 images — screenshots already flow today on Pro.
- **Tools:** ~43 native tools (no MCP): 24 NW read tools (world_state, spine, contradictions, themes, china, factors, options…) + ~20 gateway tools (quote, symbol context/intel/backtest, screen, fundamentals, earnings, insiders, congress, smart money, movers, house view, watchlist, portfolio brief, charts). `ask_brain` (`/api/ask`, older sibling) has a deterministic question→seed-tools classifier with per-class budgets; the gateway does not (model picks freely under `tool_budget`).
- **Grounding today:** `_grounding_digest()` = ~10 lines of nightly master_brief prose + world_state regime label. No tape, no tenors, no cross-asset numbers, no events. This is the precise gap behind "ChatGPT knew about the oil shock and semis selloff and our AI wouldn't."
- **Doctrine infra:** trigger-routed markdown library with budgets, leak sentinels, fingerprints (`doctrine.py` + 11 technician modules) — fires only on Terminal chart sessions today.
- **Data estate (all display-tier, already computed):** live plane `site/live/` on VPS (`/var/lib/macro-live/public/live`, timers 60s–5min): quotes (indices/futures/VIX/^TNX/oil/gold/copper/DXY/crypto), breadth v1, live market_drivers (typed shock read, EN/ZH), risk_state, shock_state, wires.json (live news rail, wires.v1: ts/en/zh/salience/corroboration/source, 48h window). Nightly: full FRED curve (DGS1..30, T10Y2Y/T10Y3M, DFII5/10 real yields, T5YIE/T10YIE breakevens, THREEFYTP10 term premium), `rates_command.v1` board (policy rate, implied path, **curve_regime_key** e.g. `bear_steepener`, term_premium_dir), vol_regime (VIX/MOVE/VVIX/VRP/term structure state), credit (BAML IG/HY OAS + credit_momentum), crossasset regime, sector/basket/leader/breadth/rotation artifacts, world_state (40+ lobes), track_record with per-desk hit rates + Wilson CIs (news salience desks are GRADED).
- **Research vault:** committed catalog (346 items, ~70/day cap, 3–4 bullet summaries each, institution/side/date) + private-R2 FTS corpus + product API. Not wired to chat.
- **Neural Web:** signal-bus governance layer (516 registered artifacts, 128 NW lobes, 336k-row graded spine, 500-ruling case law, constitution A0–A7). Already feeds chat via read tools; retrieval is exact-field filter only (no ranking); content honest but uneven (0/35 causal edges validated; some thesis nodes empty). NW is absent from CLAUDE.md — a discoverability gap.
- **Telemetry:** `mastermind.response_log.v1` on R2 per answer (question/answer/tools/tokens/latency/thinking traces) + manual admin grading sidecar. No automated answer-quality harness.
- **User context:** watchlists/portfolio_positions/entitlements in Supabase + brain threads; no per-user prefs/memory beyond threads; OIP session-digest (Terminal→EOD) is PLAN ONLY; trade_episodes exist unwired.

## §3 Adjudication of the handoff, component by component

| # | Handoff proposal | Verdict | Ruling |
|---|---|---|---|
| 1 | Market Analyst Skill (investigation protocol) | **ADOPT — P0** | Highest-leverage gap. Implemented as a second doctrine library (`engine/neuralweb/analyst/`, 9 modules: protocol + 5 lenses + 3 playbooks), trigger-routed on ALL pages/modes, per-lane autonomy dial (fast = tight sequence; pro = same evidence bar, more exploration). Preserves stance/[NEXT]/language/proprietary/recommendations-enabled laws. |
| 2 | Live Market State Packet | **ADOPT — P0** (as aggregation, not new computation) | `engine/neuralweb/market_packet.py` aggregates EXISTING artifacts (live plane + nightly boards) into ≤4200 chars with per-section freshness stamps and deterministic FLAGS (curve shape from tenor Δbp; stocks+long-bonds-both-down; dollar+gold both up; oil shock day; VIX spike). Replaces `_grounding_digest` internals. NO fused regime score, NO new cron (per-request assembly, 60s cache). |
| 3 | Regime "confidence: 0.82" fields | **KILL** | CHF-R14/RF-16: LLM/authored numeric confidence forbidden. Regime candidates ride as named states + the evidence that fired them. The engine's own calibrated qualitative word (market_drivers `confidence: medium`) passes through unchanged. |
| 4 | Fresh 18-regime taxonomy | **ADAPT — crosswalk, don't re-vocabulary** | TI-R1 (parallel shock classifier REJECT-REDUNDANT) + MSP-R2 (regime authority chain = risk_radar→market_state→regime_vector; fusion forbidden). The doctrine's stress-day families are TEACHING structure for the LLM's narration, anchored to packet states; no new engine vocabulary, nothing persisted. |
| 5 | Event-normalized news w/ authored first/second/third-order effects + fit scores | **ADAPT — facts only** | TI-R5 kills shock→beneficiary maps as brain feeds; NAR-R4 keeps LLM frame tags display-only. Ship `get_market_events` over the EXISTING wires.v1 rail (ts/headline/salience/corroboration/source) + nightly news digests. Causal chains are narrated by the model at answer time (display), never shipped as feed fields — output field whitelist is test-enforced. |
| 6 | Research vault as retrieval layer (never wallpaper) | **ADOPT — P0** | `search_research` over the committed catalog (lexical score over title/summary/institution + recency decay + top_pick bonus, top-k summaries only). Insider/Pro tiers (mirrors vault product gating). Answers attribute views to institutions — "the street's view," never the desk's. Full-report escalation deferred (rights + quota; W2). |
| 7 | Historical analogue engine | **ADAPT — W2** | Substrate exists (regime_vector parquet with hysteresis history, chronicle timeline, 50y OHLC, sector_cycles.json). Deterministic feature-similarity over stored daily state → dated episodes + what followed, printed with honest N (display-tier). NEVER "8 of 9 times" authority claims; promotion needs the gauntlet. |
| 8 | Intent & freshness classifier | **ADAPT** | P0: the doctrine's freshness law (live questions → packet TAPE + events check; stable questions → no tool spend) + trigger routing IS the v1 classifier, zero latency. W1: port ask_brain's deterministic `_classify_question` seeding into the gateway so weak models get seed tools per question class. No LLM pre-classifier (cost/latency). |
| 9 | Three memory layers | **PARTIAL** | (a) Permanent doctrine = the analyst library (P0). (b) Rolling market-state = already the nightly artifacts + chronicle; intraday freshness = the packet; do NOT build a parallel narrative store (CXI-R12 names that degenerate form). (c) User/portfolio = watchlist/portfolio tools exist; session digest + trade-memory wiring + prefs = W3 (OIP E1 pattern, options lane owns its half). |
| 10 | Model-specific orchestration (DeepSeek constrained, Sol autonomous) | **ADOPT — P0 (light)** | One shared doctrine (same evidence standards) + `lane_dial()` one-liner per lane. Matches the handoff's "one spec, tuned autonomy." Deeper per-model gates (mandatory verification steps for fast lane) = W1 after response-log evidence. |
| 11 | ~40 new MCP endpoints | **MOSTLY ALREADY BUILT / KILL the sprawl** | ~70% exist as native tools. P0 adds exactly 2 (`get_market_events`, `search_research`); W2 adds ≤2 (analogues, curve detail). Tool sprawl degrades DeepSeek tool choice under budget 5 — curation beats coverage. No MCP protocol layer (native tools are the house shape). |
| 12 | Packet refreshed "every few minutes" server-side | **ADAPT** | Sources already refresh on their own timers (60s–5min live plane). Packet assembles per-request with a 60s cache — no new cron, no ledger writes (chronicle gate 5: intraday lanes never advance stores). |
| 13 | Screenshot/market-state extractor | **ALREADY EXISTS (Pro vision) + doctrine** | Vision rides Pro (Claude-routed). P0 adds the tape-reading playbook (yield-green-means-price-down law, futures-vs-cash, mechanical duration check). Deterministic OCR pipeline: NOT NEEDED v1; fast-lane vision = W3 if DeepSeek ships usable vision. |
| 14 | Evaluation rubric + benchmark + regression | **ADOPT — W2** | Offline harness over response_log + frozen benchmark days (handoff case + replayed sessions from chronicle/regime history). LLM-judge scores are internal QA telemetry only — never product copy (CI "validated" guard stands). Weekly regression per lane; rubric in §9. |
| 15 | "Most rising/falling baskets" intelligence layer | **ALREADY EXISTS** | `get_movers`, standouts/leaders artifacts, basket_pulse live. Packet surfaces breadth + (W1) leaders line; tool already answers depth. |

**Independent additions beyond the handoff (this program's own):**
- **Deterministic anomaly FLAGS in the packet** — the "very weird day" combos (stocks+long bonds down; gold+dollar up; curve shape) computed from tape arithmetic and named with their inputs. The user's exact confusion type answers itself from the grounding.
- **Provenance discipline** — every packet section carries its own asof stamp; stale sections say so; the doctrine's honesty law makes the model disclose staleness rather than dress it as live.
- **Graded-desk citation** — news salience desks carry track-record state (hit rate + Wilson CI in qledger); W1 surfaces desk reliability wording ("a desk with a real hit-rate record flags this as high-salience") without numeric invention.
- **Street-view clustering (W2)** — when N≥3 fresh vault reports hit one theme, `search_research` gains a "street clusters" mode (retrieval summary, not authority).
- **Bilingual state passthrough** — market_drivers ZH labels ride the packet so zh answers reuse the desk's own translations.
- **NW discoverability fix (W1, docs)** — CLAUDE.md gains a Neural Web pointer paragraph; the chat is NW's biggest consumer and future sessions keep rediscovering it from zero.

## §4 Architecture — the Mastermind Analyst OS

```
user turn (message [+ images, Pro] [+ page/symbol context])
   │
   ├─ system prompt  = BRAIN_SYSTEM_PROMPT (voice/honesty/stance — unchanged)
   │                   + CONTRADICTION directive (unchanged, de-escalation only)
   │                   + ANALYST DOCTRINE block (protocol always; ≤3 routed lenses/plays; all pages)
   │                   + lane dial (fast: tight sequence · pro: deeper pass)
   │                   + technician doctrine (terminal chart turns, unchanged)
   │                   + language directive (unchanged)
   ├─ user turn      = [CURRENT DASHBOARD STATE] ← market_packet.digest()
   │                   TAPE · CURVE · FLAGS · EVENTS · DRIVERS · RATES DESK · VOL ·
   │                   BREADTH · CROSS-ASSET · DESK READ · WATCH  (stamped, budget-dropped)
   │                   + [CURRENT TICKER STATE] when a symbol resolves (unchanged)
   ├─ tools          = existing 43 + get_market_events + search_research (tier-gated)
   └─ answer         = diagnosis → chain → cross-asset check → what to watch → STANCE → [NEXT]
```

**Boundaries that make this legal:** packet = pure aggregation of engine states with provenance (A0 observe); doctrine = HOW to investigate, never WHAT is true; the model may rank ITS OWN hypotheses and must de-escalate on desk-reading conflicts (contradiction directive unchanged); events/vault tools return facts/summaries with attribution; nothing the model produces persists into any organ state (NAR-R4); public surfaces never see repo internals (CXI-R23 — packet sources are all product artifacts).

**The Neural Web rework, precisely stated:** NW already computes the intelligence; the chat-facing rework is *curation + routing*, not graph rebuilding. P0 gives the always-on curated projection (packet) and two retrieval tools. W1 gives question-shaped tool seeding (port of ask_brain's classifier). W2 gives ranked retrieval (top-k relevance on spine/graph reads, symbol-scoped subgraphs) replacing whole-graph dumps. Content thinness (causal cards, thesis nodes) remains the metabolism program's lane — not chat's.

## §5 Waves

**P0 — this PR (built by 3 opus builders + main-loop integration):**
B-A analyst doctrine library + 9 modules + lane dial + tests · B-B market_packet (live-dir ladder MACRO_LIVE_DIR → /var/lib/macro-live/public/live → site/live; TAPE/CURVE/FLAGS + nightly boards + EVENTS line; ≤4200 chars; 60s cache) + ^IRX/^FVX/^TYX added to the live-quotes CORE universe + tests · B-C get_market_events + search_research + schemas + tests · main loop: gateway wiring (doctrine block all pages, lane dial, digest swap, 2 tool registrations + dispatch + tier gates, leak-screen extension), integration tests, masterplan, ship loop.

**W1 — routing + parity (1 session):** port `_classify_question` seeding into the gateway (per-class seed tools + budgets for the fast lane); wire the analyst doctrine into `ask_brain` (`/api/ask`); LEADERS line in packet (standouts/flow_leaders); graded-desk reliability wording on events; zh render audit of packet labels; CLAUDE.md NW pointer; register packet + tools in `config/synapse.yml` with consumers.
**W1 build-time amendment — curve-tenor feed diagnosis (2026-07-30, #4005):** the chartered "^TNX never resolves into quotes.json" premise was FALSE — a vintage skew (repo-committed snapshots froze 07-27 at the VPS-timer cutover; ^TNX entered DISPLAY_SYMBOLS only 07-29 via #3963). VPS probe: all five packet symbols (^IRX ^FVX ^TNX ^TYX ^VIX) resolve 5/5 via Yahoo spark; feed live 34/34 since P0 merged. The REAL defect was packet-side: `_YIELD_SCALE=10.0` assumed the CBOE ×10 convention while the spark feed delivers percent DIRECTLY — 10Y rendered 0.46% *inside* the 0–20 sanity band (silently wrong in every grounding turn) and bp moves understated 10× (FLAGS could not fire below a real ~30bp spread move). Fixed with pair-level `>15` scale detection (`_yield_pct_pair`, mirroring `templates/live.js tnxPct()`) + Δ×100. **Standing law:** yield-index units are FEED-DEPENDENT in this product (spark/quotes.json = percent-direct; /ws/tape relay = ×10) — every future consumer scale-detects at the pair level and tests BOTH conventions plus the 2026-07-29 probed numbers (see `tests/test_market_packet.py::test_yield_scale_detection_handles_both_conventions`).

**W1 SHIPPED (2026-07-30, #4009):** fast-lane TOOL PLAN seeding (ask_brain's `_classify_question` seeds + events/research nudges; gated lane=fast · mode=chat · page≠terminal — guidance, never enforcement); analyst doctrine on `/api/ask` PLUS that surface's first output leak screen (`_leak_screen_ask` — analyst sentinels + prompt-opener echo → the standard proprietary refusal, EN/zh by question; NOT the removed advice refusal, which stays a no-op per the operator's 2026-07-26 directive); LEADERS packet line from `site/live/basket_pulse.json` (the only group-grain source carrying a day move — sorted on `live_ew_chg_pct`, never `tape_rank`, which ships null on the eod fallback); `digest(root, lang)` zh rendering restricted to DESK-precomputed Chinese (drivers `_zh` fields, wire item `zh`, `curve_regime_label_zh` — the zh audit found no canonical desk vocabulary for the remaining phrases, so the LANGUAGE directive governs those); `mastermind:packet` registered in synapse `external_consumers` on the 5 registered sources + SIGNAL_BUS regen; CLAUDE.md Neural Web orientation bullet. **Governance finding:** the live plane is largely UNREGISTERED in synapse.yml (quotes/breadth/market_drivers/basket_pulse/risk_state and master_brief under either path have no entry) — a later governance pass, not a W1 blocker.

**W2 — depth (1–2 sessions):** historical analogue tool over regime_vector parquet + chronicle (feature vector: curve shape, driver type, vol regime, breadth; returns dated episodes + forward paths with honest N, display-tier); eval harness (frozen benchmark days incl. the §1 case; LLM-judge rubric §9 offline over response_log; weekly regression per lane; admin surface); vault full-report escalation ruling (operator: rights + quota); street-clusters mode; curve-detail tool (FRED tenors + spreads + real/breakeven decomposition on demand); **brain-only ranked wire sidecar** — the news desk DECLINED salience on public wires.v1 (correct: internal ranking numbers never ride user-fetchable payloads — their leak law, ruled 2026-07-30 in the Intelligence Desk V2 lane); the sanctioned shape is a daemon-written sidecar at a NON-public path (/var/lib/macro-live/state/…), spec'd jointly with that desk, consumed by get_market_events via its existing dir ladder. Until then wire events order by recency (honest); source_name ships additively on wires.v1 (#4006).
**W2 build-time design record (2026-07-30, recon-driven — four sweep agents):** (a) The analogue substrate is `data/regime/regime_history.parquet` (25,719 rows to 1927; quad/liquidity/cycle from 1967) + breadth.parquet (1962+) + T10Y2Y/T10Y3M (1976/82+) + _VIX (1990+) — NOT `regime_vector.parquet` (17 rows, July 2026+) and NOT the chronicle (state_log 4 rows; a news/flip ledger, not a state vector). House precedent mirrored: `scripts/build_china_analogs.py` (z-features + one-hot state mismatch, 120d exclusion, 60d diversity, PIT forwards, display-only disclaimer — shipping nightly, no kill rows). US form: on-demand in `engine/neuralweb/brain_analogues.py` (no cron, no dag.yml, no render weight; VPS API venv verified to carry pandas/pyarrow + all substrate files). 10-dim uniform-weight vector: growth z, inflation z, quad/liquidity/cycle mismatch, 2s10s level+Δ63d z, 3m10y z, log-VIX z, breadth-200 z; eligible window = full-feature intersection (~1990+), stated in output. (b) Eval harness: judge = llm_auth deepseek-first (earnings_qual idiom); weekly `.github/workflows/brain-eval.yml` Sunday 13:00 UTC on macstudio, deliberately NOT a dag.yml lane (dag-conformance only inspects declared lanes); auto scores merge into the admin eval sidecar under `auto_*` keys with the classify_contradictions non-clobber idiom; benchmark = frozen §1 case at `engine/neuralweb/eval/benchmark_bear_steepener_2026-07-29.json` replayed through the real doctrine block + a frozen packet-style digest fixture. Scores are internal QA telemetry only, never product copy. (c) Curve-detail tool: pure slicing of `engine.yield_curve.snapshot()`'s ~14KB `yield_curve` block (transmission/latest.json first, regime/latest.json fallback — both PARENTS exceed cortex read_artifact's 50KB cap, which is why the tool exists) + live 4-tenor overlay via the packet + world_state rates/expectations levels; ≤8KB curated output; open tier. (d) Street-clusters: vault catalog tags/tickers/desk are EMPTY (0/374) — deterministic token-overlap clustering over title+summary_points with institution diversity (≥3 reports, ≥2 institutions), a `mode:"clusters"` param on search_research (inherits its Insider/Pro gate). (e) **wire_rank.v1 sidecar contract (canonical):** `{"schema":"wire_rank.v1","updated_at":ISO,"ids":[best-first]}` — position IS the rank, zero numbers in the file; ladder `$MACRO_LIVE_STATE_DIR` → `/var/lib/macro-live/state/` → `data/marketing/press/` (dev); reader ignores >45-min-old sidecars and falls back to recency; producer = news desk daemon (spec messaged to their session 07-30). (f) W1-live-verification fixes folded in: zh stance doctrine forms in `_language_directive` (canonical map = engine/i18n.py; Goldilocks=理想增长 per master_brain's normalization — 金发姑娘 is an explicitly retired competing form), analyst protocol.md v2 anti-leak sharpening (no internal series pairs/z-scores in prose, any language), update.sh verified-restart block (PID transition logged, one retry — the old `|| true` destroyed the evidence of the 07-30 silent-restart failure). Deferred to operator: vault full-report escalation (rights + quota ruling still owed).

**W2 SHIPPED (2026-07-30, this PR — 3 Opus builders + main-loop integration; 1,839 tests green on the branch):** (1) `engine/neuralweb/brain_analogues.py` — `get_historical_analogues`, 10-dim China-idiom retrieval over 9,053 candidate days (1990+), 26 tests; query falls back to the latest COMPLETE feature day with a `query_lag_note` stamp (build-time amendment — a one-day FRED lag degrades to yesterday-stamped-honestly, never a dark tool); Insider/Pro execution gate. (2) `engine/neuralweb/brain_curve.py` — `get_curve_detail`, ~3.5KB curated slice (spreads w/ percentiles, real/breakeven decomposition, PCA renamed `*_var_explained`, recession suite, forwards), TI-R5 fence keeps the block's favored/pressured ETF lists and per-ETF IC verdicts OUT (whole-payload tests), `policy_gap` disambiguated to `policy_pricing_2y_minus_funds_pp`, 38 tests; open tier. (3) Street-clusters: the SPEC'D bag-of-words clustering was built, MEASURED against the real catalog, and REJECTED by the builder for fabricating convergence (51-report "hike fed rate" cluster of unrelated notes — multi-topic morning briefings are token hubs); shipped construction = title-names-the-term membership with ≥0.5 support-set merging, every emitted claim literally checkable, pinned by a real-catalog test. (4) wire_rank.v1 sidecar READER live + inert (no writer yet; spec messaged to the news desk session). (5) Eval harness: `response_eval.py` (rubric §9, HARD_FAIL_TAGS doctrine_leak/refusal_regression override the judge, unparseable judge = unjudged-never-zero, zh stance checkable via the engine/i18n.py doctrine six with CJK line-anchoring — unanchored 忽略 matched inside 不要忽略…), frozen benchmark WITHOUT the bear-steepener gloss (a fixture stating the conclusion makes the headline property unfalsifiable), `scripts/run_brain_eval.py` weekly via `.github/workflows/brain-eval.yml` (Sun 13:00 UTC, macstudio, NOT a dag lane), judge = deepseek-first llm_auth with `codex_provider: False`, auto_* non-clobber sidecar merge, admin Auto-QA card; 73 tests, green in the exact thin CI venv. (6) Main loop: gateway wiring (allowlists, dispatch w/ gates, schemas, labels, curve/analogue seed nudges), zh stance doctrine forms in `_language_directive`, protocol.md v2 anti-leak, update.sh verified-restart + regex additions, synapse consumers ×5, CI pack + trigger wiring.

**W2 LIVE-VERIFIED + W2.1 heal (2026-07-30):** VPS runbook PASS — the EN guest probe reached the program's chartered bar (model called get_curve_detail unprompted → real-yield term-premium decomposition, real 10Y 2.41% @ p99, breakevens anchored, every figure artifact-traced); zh stance 观察—勿追高 live (W1-C verified). Runbook laws learned: /opt/macro is SHALLOW depth-1 (ancestry checks always fail — blob-hash equality is the proof); cookieless localhost guest probes are denied as unmeterable and FAKE `quota_exhausted` (send `Cookie: mm_aid=`). W2.1 (same day): (1) fast-lane max_tokens 2000→4000 (config/brain.yml + `_FAST_MAX_TOKENS`, both call sites pinned) — a heavy-grounding turn (51.5k in) died with thinking-only content in a TOOL ROUND at exactly the cap, shipped the "temporarily unavailable" stub with `degraded:false` and ZERO server logs (the :4754 comment's logging promise had no data path; the synthesis path logs, the tool-round path didn't); now `stub_shipped` → log.warning with lane/model/phase/stop/tokens + honest `degraded:true` (widget audit: mm_brain.js never reads the flag; response_log early-returns empty answers — flagged, not plumbed). (2) zh digest state-token substitution in market_packet zh renders (DESK/WATCH/CROSSASSET sections): Goldilocks=理想增长 (master_brain `_ZH_LEXICON_FIXUPS` normalization target; 金发姑娘 retired, alert_triage's 金发经济 overruled), Reflation=再通胀 etc.; whole-token, case-sensitive, EN byte-identical (pinned). (3) analogues payload gains `tier: "display"` parity.

**W3 — memory (1–2 sessions):** chat session digest (OIP E1 pattern — privacy law: system events only); trade-memory read tool (`trade_episodes` — "you asked about TLT last week when…"); per-user prefs (depth/language) in profiles; fast-lane vision when DeepSeek supports it.
**W3 build-time design record (2026-07-30, recon-driven — three sweep agents):** (a) **The session digest needs NO new storage.** The OIP E1 pattern translated to chat = a DERIVED, REBUILDABLE read-time summary of the canonical per-user store — and chat's canonical store already exists (`brain_threads`/`brain_messages`, Supabase service-role PostgREST, 12-turn history cap, global per user, no TTL). `recall_sessions` derives {symbols, stances, topics} per recent thread from titles + ASSISTANT-side content, per-user-scoped exactly like get_watchlist; guests get a sign-in note. This satisfies CXI-R12 by construction (no second store, nothing hand-written), honors "system events only" (assistant/system-side fields, never user prose as stored knowledge), and dodges the manual-Supabase-migration operator gate entirely (scripts/deploy/*.sql are applied by hand in the SQL editor — a new table would have gated the feature on an operator step). Assistant `_append_message` calls start stamping `meta={tools, symbols}` (system events) to enrich future recalls. (b) **`get_trade_episodes`** mirrors the watchlist idiom over `public.trade_episodes` (RLS-shaped multi-tenant; today only the operator's admin console writes it, so most users honestly see an empty journal); output whitelists ticker/side/dates/outcome/thesis-as-the-user's-own-words + autopsy summary/lesson ONLY — `evidence_packet` and all other autopsy internals never emitted (whole-payload test); research_only framing preserved; ungated for signed-in users (it is their own data). (c) **Prefs:** `lang`/`theme` already live in Supabase Auth `user_metadata` (account page writes via POST /api/account/prefs; email_prefs mirror); W3 adds `brain_depth` (concise|standard|deep) to the same store via a shared `lib/user_prefs.py` (GoTrue admin fetch-merge-PUT lifted from account_prefs), threads prefs into both chat routes at ZERO added latency (the require_user fetch already holds user_metadata and was discarding it), applies lang as a MIDDLE fallback in turn-language resolution (message content wins, then context.lang, then account pref) and depth as one system line before the analyst block. `set_chat_preference` tool lets the user say "answer in Chinese from now on" / "keep it short" — enum-only writes, guests refused. (d) **Fast-lane vision stays Pro** — DeepSeek capability remains UNPROBED (the exclusion is a `model.startswith("claude")` check, not a tested limit; live verification will probe once); the W3 fix is honesty: a gated-drop turn now carries a system note so the answer acknowledges the attached image instead of silently ignoring it (the silent drop left NO trace anywhere — worse than the gate itself).

**W3 SHIPPED (2026-07-30/31, this PR — 2 Opus builders + main-loop wiring; 2,110 tests green on the branch):** (1) `engine/neuralweb/brain_user_memory.py` — `recall_sessions` (own recent sessions: symbols/stances/topics per thread, derived at read time from brain_threads/brain_messages; timestamptz-explicit window, stance matchers REUSED from response_eval — never forked; 68 tests) + `get_trade_episodes` (own journal, autopsy summary/lesson only, `evidence_packet` whole-payload-fenced, `research_only` stamped as a literal so a row cannot promote itself); assistant `_append_message` now stamps `meta={tools, symbols}` (chat = both; stream = symbols-only — the loop keeps messages internal, honestly documented). (2) Prefs: `lib/user_prefs.py` (closed enums lang/theme/brain_depth, merge-safe GoTrue admin write, no baked project URL, failed-read-refuses-write), `brain_depth` on POST /api/account/prefs, prefs threaded into both chat routes at zero added latency (require_user already held user_metadata), account lang = MIDDLE fallback via `_turn_lang` wrapper (30-case equivalence grid; `_expected_lang` byte-unchanged), depth = one system line before the analyst block, `set_chat_preference` tool (enum-only, guests refused). Server-side turn facts ride a reserved `_server` context key STRIPPED from inbound requests — a client cannot forge depth or a fake image apology — and allowlisted OUT of the response log. (3) Image-gate honesty: a tier-gated drop now injects a one-line system note so the answer acknowledges the attachment; the drop itself unchanged (pinned test intact). (4) Defect found + PINNED for a follow-up chip (not fixed — pre-existing, out of zone): `_expected_lang` counts tickers as English prose, so "XLF vs XLV" flips a zh user to English against its own docstring. DeepSeek vision remains unprobed; Pro-routing stands.

**W3 LIVE-VERIFIED (2026-07-31 01:1x–01:3xZ, 8-step runbook PASS, no levers; deploy self-updated then restarted through its own update — F4):** all three tools registered live (52-tool surface), module smokes honest incl. a real Supabase read, image-gate note VERIFIED verbatim ("I can't read the attached image — image reading is a Pro feature, so I'll answer from the live dashboard data"), plain probe grounded with stance. **VISION VERDICT (charter question CLOSED): DeepSeek v4's anthropic-compatible endpoint does NOT accept image blocks and fails SILENTLY — HTTP 200, the image is replaced server-side with a literal `[Unsupported Image]` text placeholder (input_tokens: 18). Fast-lane vision therefore stays Claude-routed indefinitely, and the image gate is load-bearing, not defensive: a naive pass-through would return confident hallucinations about an image the model never saw. Re-probe only when DeepSeek announces vision.** Findings chipped to their owners: /api/health's `commit` field shells git per-request and diverges from the running process's build in both directions (blob-hash + MainPID is the only sound deploy proof); founding_pro offer 503 (Stripe not provisioned) on a live revenue surface; raw enum tokens (RISK_OFF/CAUTION) from contradictions reaching guest prose.

**W4 — operator rulings wave (2026-07-31, all three RULED by the operator in session):** (1) **Vault full-report escalation APPROVED for Pro** — `search_research mode="report"`: catalog metadata + public excerpt + corpus-body slice capped at 12,000 chars (deliberately far under the stored 60k; the excerpt caps' operator-owned-risk-surface framing in excerpt.py governs), Pro-only at dispatch (Insider keeps summaries — mirrors the product's `_VIEW_TIERS={"pro"}`), charges the product's own `view_ratelimit` per corpus serve with a per-user synthetic ip key (chat must never bypass vault quotas), attribution + not-for-redistribution instruction in the payload; corpus reader lifted into engine/research_vault/corpus.py so ONE process cache serves both the product API and chat. (2) **Vision → codex-first** ("do not use claude routing") — the codex adapter gains a real image path (base64 blocks → temp files inside the CLI sandbox cwd + `tools.view_image=true` for that call only + cleanup in finally; https URLs never fetched server-side), `_pick_vision_provider`/`_vision_providers` prefer `name=="codex"` with claude as failover-only; motivation = attached-subscription cost basis vs metered Anthropic. The W3 probe's finding stands: DeepSeek remains never-vision. (3) **zh wire translation → codex gpt-5.6-terra** (operator said "Terra/Luna"; Luna stays FENCED — the standing marketing tier ruling + `test_luna_never_writes_a_user_facing_word` make translated headlines Sol/Terra territory, and Terra already runs the hot-tape wire): news_translate gains `provider: codex`, config.yml flips to terra with deepseek kept as commented fallback, marketing-press-feeds.service gains CODEX_HOME/CODEX_PROVIDER_ENABLED (it had NO codex reach), daemon preflight checks `translator_ready()` instead of the DEEPSEEK_API_KEY secret. This closes the zh-headline provisioning question WITHOUT the metered key.

**W4 SHIPPED (2026-07-31/08-01, this PR — 3 Opus builders + main-loop wiring; 1,330 tests green on the branch + Luna fence):** (1) `search_research mode="report"` — Pro-only dispatch gate (Insider gets an honest `pro_required` and keeps summaries); catalog-existence check BEFORE the corpus (a hallucinated id can never probe the store); excerpt + 12k-char body slice with in-budget truncation marker; ONE `view_ratelimit` debit per served body via the `"brain:"+user_id` synthetic ip key (the builder proved a blank ip would have collapsed every chat user into one `'noip'` hourly bucket — per-user key verified against the REAL limiter); corpus machinery lifted to `engine/research_vault/corpus.py` (one process-wide cache; app/research delegates; a raising store now degrades instead of 500ing, honoring the router's own never-raise docstring); corpus warmed at API startup off the request path. (2) Codex vision — images staged as files in a per-call sandbox tempdir, `tools.view_image=true` for that call only, cleanup in finally proven for success/failure/mid-staging, https URLs never fetched (SSRF fence), no base64 in the prompt; `_vision_providers` codex-first with the claude chain as in-turn failover (+ Pro-claude borrowed as failover TAIL when the lane has codex but no claude — previously the borrow fired only on an empty chain). PAPER-VERIFIED ONLY against the CLI — live verification must run one real codex vision call before Pro trust. (3) zh wire translation — per-lane override, NOT global (builder-caught regression: the global flip would have darked the nightly title_zh lanes that hold the DeepSeek repo secret but no codex CLI): daemon `_zh_cfg` overrides provider→codex/gpt-5.6-terra/effort-low with press_sources.yml flip-back keys whose model default FOLLOWS the resolved provider (a flat pin would send terra to the deepseek endpoint after a flip-back); `translator_ready()` replaces the key-only preflight; marketing-press-feeds.service gains CODEX_HOME/CODEX_PROVIDER_ENABLED; `zh_timeout_s` 20→45 (CLI spawn + model turn); codex cost attribution fixed to subscription basis; two previously-DARK translate suites wired into the marketing pack. Deploy preconditions on the box: the unit must already be installed (update.sh reinstalls, never installs) and `/var/lib/macro-codex/auth.json` must exist (macro-api's StateDirectory owns it).

**W4 MERGED + LIVE-VERIFIED (2026-08-01, PR #4161 → merge `1f841111c3f`):** The merge sat pinned ~a day behind the 1,702-page conflict-marker incident (a dirty PRIMARY checkout committed stash-collision wreckage site-wide via render-sync — separate incident session healed main; see memory `render-sync-commits-dirty-primary-checkout-sitewide`); after the heal, one forward-merge (site-page conflicts resolved to main's re-rendered side) went green and the sweeper merged. **Pre-merge de-risk (real calls on the box, before the code landed):** codex-cli 0.145.0 accepted the provider's exact flag set and read a staged `./img_0.png` under `--sandbox read-only` ("Red" for a red square), and `gpt-5.6-terra` resolved on the attached auth (clean idiomatic zh headline). **Post-merge on the VPS:** deployed HEAD = the merge commit itself; all six W4 blob hashes match `origin/main` exactly (incl. `brain_gateway.py` `a1a6cc44`); the verified-restart block bounced macro-api AND the press daemon at 07:48 UTC, and the deploy synced the new unit file (runtime env carries CODEX_HOME + CODEX_PROVIDER_ENABLED — the "unit must already be installed" precondition held). Live proofs: (1) **report-mode** — real Goldman doc served an 11,903-char body slice, `body_truncated: True` with the in-budget continuation marker, attribution note, and a REAL `view_ratelimit` debit (`remaining: 59/60`); gate legs `pro_required` (no identity), `report_not_found` (bad id), and the honest degraded note on a corpus doc with a genuinely empty body (126/513 corpus docs carry no text layer — the "not reachable" wording fires for empty-body docs too, which is honest but worth knowing when reading probe results); macro-api's startup warm proven (20MB corpus in its PrivateTmp at boot — note PrivateTmp=yes means the service cache is invisible to shell probes, which fetch their own copy). (2) **Vision through the DEPLOYED provider** — base64 → staging → CLI → "Red", zero leftover `/tmp/codex_vision_*` dirs. (3) **zh wire lane** — deployed `_zh_cfg` resolves codex/`gpt-5.6-terra`/45s, `translator_ready()` True, and a real `translate_to_zh` returned 强劲就业报告公布后，美国国债收益率大涨. wire_rank sidecar check stays deferred: producer PR #4067 (news desk) unmerged; the reader degrades by design.

**W5 — latency wave (operator order 2026-08-01: "upgrade Mastermind AI to become much faster in latency", per the Deepvue teardown `research/DEEPVUE_COMPETITIVE_TEARDOWN_AND_MASTERMIND_BUILD_DOCKET_2026-08-01.md` §6).** Teardown A/B measured live: headers <1s, no-tool round 3.74s, but 52–77s to first visible answer — the cost is orchestration, not model or network. Recon (2 lanes, verified against post-W4 main) confirmed every §6.6/§6.8 claim and found the levers: (a) Phase-2 synthesis ALREADY streams from the SDK (`text_stream`) but forwards only a char-count beat — the buffered single delta was a DELIBERATE prior law (`research/mastermind_transparency_latency/SPEC.md` §D, operator 2026-07-26) protecting the advice filter, which is today a documented NO-OP → the fence is fossilized and this wave SUPERSEDES it (spec amended in-place; leak-screen authority preserved); (b) the client (`mm_brain.js` MdStream) is already multi-delta capable — accumulate-drain queue with diff-paint, so server-side streaming needs almost no client work beyond a `retract` handler; (c) the leak screen is 12 static exact substrings ≤~50 chars → a 256-char holdback window + mid-stream scan + `retract` event + final full-answer screen preserves the guarantee; (d) tools execute SEQUENTIALLY (no concurrency anywhere; get_watchlist/get_portfolio_brief = 1–3 serial Supabase GETs, get_quote = live HTTP ≤3s); (e) the messages array is fully UNCACHED across rounds (system+tools have breakpoints, history/packet/tool-results resend whole every round — the documented 51.5k-token turn); (f) `render_inline_chart` feeds its ≤60KB raw SVG back into the model-visible tool_result (~15k tokens re-sent every round) while the client already receives the SVG via the separate `chart` event — pure waste; (g) an instant-fact hook exists after prescreen/before providers in both entrypoints where question+context+tier+quota are all resolved. **Contracts:** S = true token streaming (flush ≥120 chars or 350ms, leak+`[NEXT]` holdbacks, retract, final-screen belt-and-suspenders, byte-identical reassembly gate); T = tool economics (parallel tool execution with order preservation + write-tool denylist, SVG fence to a compact stub, one ephemeral breakpoint on the initial messages tail, earnings-ledger TTL cache, one-batch advisory nudge); I = deterministic instant-fact router (quote/level asks EN+zh, hard bias to false negatives, minimal no-doctrine no-tools prompt, quote+as-of as data per A7, silent fall-through to the deep loop on ANY failure); M = per-turn latency dict (`route/ttfv_ms/rounds[]/synthesis_ms/total_ms`) on `done.usage.latency` + response_log, and `scripts/brain_latency_bench.py` preserving the teardown's three prompts (stdout only — ledger law forbids ad-hoc data/ writes). **Deliberately NOT touched (measured tradeoffs kept):** DeepSeek fast-lane thinking stays ON (config comment + regression tests pin it — the Flash/thinking-off route measurably degraded ticker synthesis; round REDUCTION comes from batching + the instant lane instead); `/api/ask` keeps its buffered shape (no run wrapper, separate wave if ever); the citation envelope (packet/curve/events/vault provenance invisible to both extractors — recon documented four unrecognized shapes) is a QUALITY wave, W6 candidate, not latency. Codex-served pro synthesis streams as one chunk (the CLI's text_stream is fake) — Contract S degrades to today's behavior there by construction.

**W5 MERGED #4220 (2026-08-02, merge `63e13068be3` via sweeper; 530 new-suite + 2,576 downstream green; 3 Opus builders + main-loop integration of a 4-hunk brain_gateway conflict).** Live A/B, same bench, VPS localhost, cold: broad 62.3s→42.7s done, native 24.3s→18.5s (tool economics working), the plain price ask completes 3.7s (never completed pre-W5), `done.usage.latency` live with per-round/per-tool timings. **Two gaps surfaced honestly → W5.1:** (1) `n_deltas=1, ttfv==done` everywhere — Contract S streamed only the budget-exhaustion Phase-2 pass, but real turns end with the answer written INSIDE the final tool round (blocking call); (2) the instant lane fell through on every probe — its dateless-refuse gate caught a PRE-EXISTING reader defect (`_tool_get_quote` site_quotes branch asked for `as_of`; the plane writes per-row `ts` + file `asof` — every quote from that source had shipped dateless since before the wave), plus single stocks have NO live quote source on the VPS at all (34-symbol plane; chipped as a data lane). **W5.1 (this PR):** reader fix (ts→ISO as_of + change_pct/prev_close; market_packet audited clean — correct key); round streaming via ONE shared `_StreamGate` (Phase-2's inline block deleted into it — flush/holdback/sentinel/[NEXT]/beat in one implementation; commit_chars horizon 200, Phase-2 passes 0), streaming candidate walk on rounds mirroring `_create_failover`'s failover contract, narration-past-horizon wiped by empty retract, one-shot blocking retry safety net if a provider rejects stream+tools. **Pre-merge de-risk (real calls on the box): DeepSeek's anthropic-compat endpoint serves `stream=true` WITH `tools` — text answer streamed 7 chunks/end_turn; tool-call shape emitted ZERO text chunks (reasoning rides thinking blocks, stop tool_use) — the horizon design's core assumption confirmed in production traffic.** Integration traps recorded: holdback 256 > horizon 200 means first flush ≈ 376 chars; `streaming.commit_chars` is readable but deliberately NOT in shipped brain.yml (a config-shape pin test enumerates the block's keys); round deltas change the bench's ttfv semantics vs the W5 baseline — that is the point.

**W5.1 MERGED #4253 + LIVE-VERIFIED (2026-08-02, merge `a88279bc8bf` via sweeper after a base-side pin: the BioCatalyst suite (#4251, another lane) structurally timed out ci-pack-0's 8-min budget — 7:55 locally all-green, runner at 13% — main itself red repo-wide; chipped urgent (task_d9ed167e) and healed by that lane the same morning. En route also healed one stale-merge-ref red (unsubscribe page drift) by head advance.** Deploy proven (blob-hash + restart 09:36Z; the single-stock quote chip #4250 landed independently right after the merge and rode the same deploy). **Final bench (same instrument, VPS localhost, cold + warm):** `"What's AAPL trading at?"` → **route=instant, 2.09s/2.8s, ZERO tools — beats the teardown's Deepvue 2.14s reference**; broad = **136–150 delta events** (real token streaming live), native 46–55. **Honest trade-off, measured with a same-minute A/B on the box (blocking /api/brain/chat vs streamed /api/brain/stream, same broad prompt): streamed rounds cost ~+30–50% total wall clock on DeepSeek's anthropic-compat endpoint (63.8s blocking vs 93.8s streamed) — their streamed generation is paced slower than blocking.** What the user feels: ttfv ≈ unchanged ~40s on deep turns (the tool rounds), then the answer WRITES LIVE instead of a spinner + paste; totals stretched. **W6 levers recorded, not built:** (1) round-1 is virtually always a pure tool round that displays nothing — reverting IT to blocking reclaims most of the streamed overhead with zero visible loss; (2) deep-turn ttfv (~40s of rounds) is the remaining prize: history/grounding trims + batching; (3) DeepSeek native-API streamed throughput vs compat worth one probe. Widget visual check: dashboard is regwalled and login is operator-only — server-side wire proof + structural widget tests stand; operator sees live writing on next dashboard use. Citation envelope remains the W6 quality candidate.

**Dependencies flagged, owned elsewhere:** earnings calendar staleness (3/1364 fresh — OIP W0 gate) degrades `get_earnings`; wires.json production health = E-waves lane; MOVE/SKEW collectors absent (vol packet line already flags what's missing).

## §6 Epistemics compliance (checked against the kill registry)

TI-R5 ✔ events carry no authored effect chains (whitelist test) · TI-R1 ✔ no parallel shock vocabulary; market_drivers is the shock read; LLM never writes classifications into calibrated keys · MSP-R2 ✔ no fused regime score anywhere; states ride side-by-side · CHF-R14/RF-16 ✔ no numeric confidence (regex-tested) · PS-R1/4 ✔ conditional signposts, no event probabilities · NAR-R4 ✔ model output never persists to organ state · CXI-R23 ✔ packet/tools read product artifacts only; context_search stays operator-allowlisted · CXI-R12 ✔ no parallel knowledge store; retrieval = existing canonical artifacts · Chronicle gate 5 ✔ no intraday ledger writes (read-only packet, in-process cache) · Recommendations-enabled (operator 2026-07-26) ✔ doctrine reinforces the direct call; no advice-refusal reintroduced · Falsifier language law ✔ doctrine says "what would change the read," never "refuted" · RUL-NW4 supersession respected (directional verbs allowed).

## §7 Cost & latency budget

Packet ≈ ≤1.1k tokens/turn (was ~0.3k digest) — fast lane worst case +$0.0002/turn at DeepSeek rates; Pro rides cached system prompt so marginal cost is the user-turn tokens only. Doctrine block ≈ ≤3.2k tokens system-side but participates in Anthropic prompt caching (system block cached; cache key changes only on doctrine edits). Two new tools add zero cost until called; both are local-disk reads (<5ms). No new crons, no render-path weight (packet assembles per request from artifacts other lanes already produce). Guard rails: existing per-lane tool budgets, monthly token ceilings, and the packet's own char budget.

## §8 Collision & dependency map (at ratification)

Open PRs: none touch gateway/NW/news/vault engines (#3965 touches theme.js chrome — watch only). news.html estate: consumed read-only via wires.v1 + site/news digests; the news.html upgrade session keeps surface ownership — this program is a consumer, coordinate only if wires.v1 schema changes. OIP masterplan owns options session-digest (E1) and the earnings freshness fix; this program reuses the pattern in W3, never builds a parallel digest. Mastermind trading bot (separate repo) consumes `nw_mastermind_context.json` — untouched by this program.

## §9 Evaluation rubric (W2 harness; internal QA only)

Regime identification 20 · use of user-supplied data 10 · catalyst verification (events checked before concluding) 10 · cross-asset consistency + leftovers named 15 · mechanical translation (duration/curve arithmetic) 10 · fact/desk/inference separation 10 · conditional signposts + invalidation 10 · voice compliance (stance, [NEXT], no machine text, language law) 15. Pass ≥80. Failure modes tracked as tags: headline-first, single-cause forcing, yield-direction misread, stale-as-live, invented odds, refusal regression, doctrine leak. Scores live in the response-eval sidecar; never in product copy.

## §10 Explicitly rejected forms (already covered by standing kills — no new registry rows)

Engine-authored beneficiary/casualty event maps (TI-R5) · a new regime classifier vocabulary or fused regime confidence (TI-R1, MSP-R2, CHF-R14) · LLM-emitted geopolitical probabilities (PS-R4) · always-inject research-vault wallpaper (context stuffing; retrieval only) · a second hand-curated knowledge store for chat memory (CXI-R12) · public internals retrieval (CXI-R23) · an LLM pre-classifier gate on every turn (cost/latency; deterministic routing suffices).
