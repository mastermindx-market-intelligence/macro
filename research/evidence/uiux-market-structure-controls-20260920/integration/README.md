# Market Structure — current-base release qualification

Mission: publish the existing accessible chart-window and copy repairs on PR #7437 using the VPS, not Vercel. Parent site-wide sweep remains incomplete.
Authority: current Chairman continuation; protected Mastermind Skillpack 3e66e43258f34db240d5bff76f54148c7af84ee4; DEC-UIUX-VPS-ONLY-DELIVERY-20260920. Same incumbent source writer, no child or custody transfer.

Source: d4a25629b2f300a24df321bf82cbb6304e646ef2. Material integration base: 4f4f64931d6e33d61b901c313132a5116e3f0c32, which contains the accepted shared repairs and actual Unified Dashboard suite registration. No feature-template conflict or change. The one generated-page conflict was CSS externalization: the canonical shared ink stylesheet 080bfc12 exactly equals the prior inline ink body; it is retained alongside the exact source-owned range-controls style block. No wholesale ours/theirs resolution, new stylesheet interpretation, data/model/ranking or auth change occurred. Asset references are normalized by scripts.optimize_assets.make_optimizer and lib.pages.write_page.

Proof: 89 scoped tests passed; eight native desktop/mobile × EN/ZH × dark/light interaction cases and the storage-denial case pass. Existing verifier checks all 11 window choices against the original geometry baseline, selection/persistence, keyboard/touch, date bounds and language events. Integrated template and inline history/drawing scripts are unchanged. Evidence: asset-composition.json, tests.txt, browser/results.json and PNGs.

Current status: locally qualified, not hosted-green, merged, deployed or production-accepted. Consume concluded new-head CI and current-base compatibility before expected-head merge; then existing locked macro-update and verify_ranges.py on the public site. Preserve old failure receipts; no Vercel action, extra PR or blind retry. Prior controls and copy work are DO_NOT_REDO.
