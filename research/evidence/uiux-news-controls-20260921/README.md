# News — localized, truthful filter controls

Mission incomplete: improve the main-site frontend through bounded grouped fixes, Web Chat only, VPS-only publication. Current Chairman continuation authorizes this path-disjoint lane while Basket #7589 continues its hosted CI. Protected Mastermind Skillpack 3e66e43258f34db240d5bff76f54148c7af84ee4 / 1.0.1; COLD_START, ACTIVE_EXECUTION, WEB_CEO_DELEGATION, RECONCILE_STATE, REVIEW_RETURN and CLOSEOUT loaded at that same pin.

Carrier: claude/uiux-news-controls-20260921 at 597bedad6cf1240150a5804522b1b75fe3baa2f3. No existing News template/page writer in current exact-file plus title census. No child, no transfer. Direct bounded implementation: LOWER_TOTAL_OVERHEAD; the browser diagnosis and native-interaction repair form one small outcome. Source paths: templates/news.html.j2, paired site/news.html and its page-local stylesheet, existing tests/test_news_page_render.py.

Observed on public news.html via native Settings language switch: 45 shown but only 24 visible headlines; after EN→ZH the search placeholder, search accessible label, count, relative timestamps and guest Intelligence Desk/Wire states remain English. Topic filters have no selected semantics and 27–29px touch height. Source also excludes title_zh from the search index. Evidence before.json and mobile-lang.png. Hypothesis: listeners subscribe to window, while the shared language event is dispatched on document without bubbling. No request to widen authentication: registered-only data must remain gated, and failure states must remain honest.

Bounded repair: reuse existing filter state; correct display counts; support searching displayed Chinese headlines; give native filter buttons pressed-state and useful hit areas; translate dynamic UI on the existing document language event; add a clear-filter empty-state recovery. Preserve source items/order/timestamps/URLs, backend ranking, payload schemas, polling, permissions and all publication gates.

Next: reproduce with regressions, fix shared template + paired artifact as one batch, canonical dual-theme screenshots and native keyboard/touch/state checks, then one PR and concluded exact-head CI before VPS release. Completed prior UI batches remain DO_NOT_REDO; #7589 remains separate.

## Implemented boundary and design
The existing native topic/filter state is reused. Counts distinguish displayed and matching headlines; the existing Chinese title participates in the escaped search index. The correct document language event refreshes placeholders, accessible names, counts, times and guest feed states without clearing the query or resetting the selected lane. Native Clear filters reuses the All button and returns focus to search. Toolbar buttons expose pressed state and have 40px hit height; all five topic labels stay on one row at 390px.

The first screenshot pass additionally found that grid/flex styles overrode native hidden on the gated Intelligence/Wire panels. Scoped styles now honor hidden, and the duplicated guest health banner is suppressed while its single status message remains. The guest copy no longer invents an expired session; the header uses neutral Story updates and a nine-word description instead of a live-story-graph claim and a long operational paragraph. Access decisions, endpoint URLs and polling are unchanged.

Dark treatment: retain the existing subdued newsroom surface and editorial hierarchy; selected topic uses inverse paper/ink, with a visible focus outline rather than another accent family. Light treatment: retain the white paper panel on the deeper neutral canvas, dark selected-state ink and legible labels; no dark-glow conversion or new color palette. The larger controls do not widen the viewport. Guest states show one useful message, not empty metrics or a duplicate status block. Eight native screenshots record their initial language in filenames and finish after switching to the opposite language; canonical screenshots record the explicitly observed final language.

## Proof
221 tests passed in the existing News render, reject-leak, translation, product-chrome, Intelligence Desk and live-wire suites. Eight new regression checks remain in the already CI-owned tests/test_news_page_render.py; no additional CI suite or watcher. The five original regressions failed on the original source while 39 existing render tests passed (red-tests.txt). An intermediate CSS edit accidentally began a Jinja comment with adjacent braces/hash; the failing tests caught loss of downstream styles. style-parse-failure.txt preserves the failure; the corrected template renders canonical inherited CSS, rather than copying or minifying it by hand.

verify_controls.py passes eight desktop/mobile × EN/ZH × dark/light native interaction cases: Tab/Enter/Space and touch, topic selected state, search by a displayed Chinese headline, real Settings language change, query/order retention, guest-state translation, exact shown/matched counts, Clear filters focus and Show more. Local registered-only feed responses are explicitly simulated 401; no authenticated production-path claim. All cases have zero uncaught JS errors and no document horizontal overflow.

Canonical capture is 24 rest/search-focus states across desktop/tablet/mobile, EN/ZH and dark/light, written to mockups/evidence/uiux-news-controls-20260921. Capture tool anonymity and unavailable authenticated states remain explicit. The latest content and CSS, not an earlier screenshot revision, must satisfy the visual/design gates before commit. Source-invariants preserves headline links, timestamps, original lane/index/order, and identical paired interaction script. Model inputs, scores and backend data are not touched.

## Accepted prior release and current next action
Basket #7589 is now PROVEN_LIVE at b4c3db210e5be5c62efdee1fafe742064baacb5e. All 121 origin pages match; the original no-interception native public verifier passes all16 cases. Verified canonical acceptance: https://github.com/mastermindx-market-intelligence/macro/pull/7589#issuecomment-5756511503. The released late-load focus repair, #7497 and earlier Market Structure/Intelligence Hub releases remain DO_NOT_REDO.

This News batch is not deployed. Finish current-head visual/release qualification, one grouped commit/push/PR, and concluded repository CI before expected-head release via the existing VPS updater. Preserve no-Vercel direction. No automatic Web-chat wake, transfer or background principal execution is implied. Resume in this Web Chat program at this exact source carrier. The parent site-wide mission remains incomplete.

## Current continuation — collision preserved 2026-09-21

MISSION_COMPLETE: false. Current protected Skillpack: Mastermind@0f4aeae4d25f3770474a7ac70e0ce12af6e2d075 (1.0.1), with identical RECONCILE_STATE/REVIEW_RETURN/CLOSEOUT to the prior accepted pin; required active/delegation skills consumed.

Reconciliation: Basket #7589 is already accepted PROVEN_LIVE at b4c3db210e5be5c62efdee1fafe742064baacb5e and is DO_NOT_REDO. Fresh main 1dc11fb3eb326393c803bc2eff408db89bb91bc1 has no changes to this News source/dependency closure since the original base. A new independent active source writer #7591, sol/news-mobile-triage-20260921 at e1a137e119efc7cd96f2ba58a42049d8368c35d4, now overlaps templates/news.html.j2 and tests/test_news_page_render.py. This was not present at the original pickup.

Disposition: freeze this existing source artifact and publish it only as a recoverable integration package; no competing PR, no edits to #7591 and no duplicate release. Unique candidate delta to reconcile includes document-vs-window language-event handling, Chinese-title search indexing, localized guest/error states, native hidden-state CSS and single guest-state messaging. Overlapping count, reset and control sizing must be reconciled once, not stacked blindly. The #7591 owner retains its existing review/publication gates. Its reported browser safety restriction must not be rerouted through this carrier. No source custody or provider binding is transferred by this note.

Next: notify #7591 with the exact frozen source and proof refs for owner-led composition. Continue only an independent non-colliding UI lane in this Web Chat. Re-enter News only after current owner reconciliation; preserve all stored source items, permissions, failure distinctions and the VPS-only delivery rule. No child or automatic wake.
