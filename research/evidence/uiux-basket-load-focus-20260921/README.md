# Basket Detail — late-load focus repair

Follow-up to released #7497. Parent UIUX mission incomplete. Current Chairman continuation, VPS-only decision; protected Skillpack 3e66e43258f34db240d5bff76f54148c7af84ee4. Direct bounded repair: LOWER_TOTAL_OVERHEAD; no child or custody transfer. New carrier claude/uiux-basket-load-focus-20260921 at main 0d9d836cda058f3f59507e3b7b7fed92b05ef7dc; the merged predecessor stays untouched.

A deferred non-data image lets a visitor sort before window.load. The shared wrapTables hook then reparents the focused TABLE, losing keyboard focus. before.json preserves the actual appendChild stack, unchanged row order and BODY focus. The language-switch assertion only exposed the timing; translation is not the cause.

Repair: give the existing holdings .ts wrapper the existing canonical .tbl-scroll class. Shared setup skips that already-wrapped table. One class token in the canonical template and all 121 generated pages; no new wrapper, focus handler, observer, timer, ranking/data/auth or shared-theme change.

Required proof: existing suites plus regression, native delayed-window-load browser matrix, canonical dual-theme evidence, concluded CI, expected-head merge and public VPS proof. Do not mark this follow-up shipped from source tests. wh_banner.js remains correctly member-only (anonymous 401); no access change.

## Verified implementation
161 targeted tests passed, including the added regression inside the existing FTR W3 CI-owned suite (no new unregistered suite). All 16 real-deferred-window-load cases passed across US/China, desktop/mobile, EN/ZH and dark/light. After load the exact focused node remains connected and focused; score disclosure and row order remain unchanged; no nested scroll wrapper. The existing sorting verifier independently passed 16 native cases plus four asynchronous-data cases.

source-invariants.json proves all 122 template/page files are byte-identical to base after reversing the single wrapper class addition; therefore all data, model code, HTML structure, shared assets, ranking, and authentication remain unchanged. The first new verifier run had a Python Playwright callback-signature error (the Request occupied a default capture argument); verifier-initial-error.txt preserves it. The corrected callback takes both actual route arguments; no product failure or initial passing run is claimed.

## Parent release progress
Market Structure #7437 and Intelligence Hub #7361 have now been merged and PROVEN_LIVE via the original VPS publisher and native public-browser checks. Receipts: #7437 issuecomment-5756042347; #7361 issuecomment-5756042584. All 121 Basket files from #7497 are deployed, but the late-load focus race found during public acceptance remains a narrowly identified obligation until this follow-up ships. Neither two completed releases nor prior feature work should be repeated.

## Current next action
Finish canonical dual-theme visual proof, commit/push this single grouped follow-up, conclude its repository CI and exact-head release, then run the existing unmodified verify_sorting.py on the real public VPS URLs. No new scheduler or autonomous wake; same source carrier until effects reconcile. No source edits to the already-merged predecessor.

Canonical capture: 48/48 rest/focused-sort states across both routes, desktop/mobile/tablet, EN/ZH and dark/light. The canonical capture tool emitted its default data/product_experience manifest/smell paths; those newly generated outputs were relocated into this dedicated evidence receipt directory and only their temporary default outputs restored. Screenshot file references were rebased with the capture tool’s own os.path.relpath rule; all 48 PNG bytes, their SHA-256 values, captured states and source metadata are preserved and resolve correctly. capture-relocation.json records the original/default and relocated manifest digests and exact reference mapping. No historical/global evidence was overwritten in the committed result.
