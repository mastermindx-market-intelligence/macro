# Reports archive — keyboard, touch and locale controls

MISSION_COMPLETE: false. Bounded child capability: the archive uses native topic controls, follows the shared document language event, and returns focus after clearing an empty search. User-authorized grouped Web Chat UIUX sweep; VPS-only release, no Vercel operation. One source owner, no worker, transfer or automatic Web-chat wake.

Source carrier: claude/uiux-reports-controls-20260921, proposed from macro main f7539ad7e4e22cd889ff6d75190c2e1d686a0a53. Exact template/page plus recent open-PR and title census found no incumbent Reports writer. News #7591 remains held with its own owner; it is not modified or browser-probed here. Alternative Data #7600 is a separate current release. Basket #7589 and prior accepted UIUX releases are DO_NOT_REDO.

Direct execution: LOWER_TOTAL_OVERHEAD. During #7600 release waiting, regular-file content reads in the Studio-linked source checkout stalled (git rev-parse and .gitfile content; metadata reads worked). No reset, new clone, altered permissions or duplicate carrier was attempted. The independent Reports lane uses GitHub's native content-addressed Git objects and one new expected source branch. All before/after source is fetched from that exact canonical GitHub base. Local files used for tests are explicitly standalone fixtures, not a Git worktree, source authority or publisher. Current API reads and existing approved VPS access remain independent of the stalled local filesystem.

Public reproduction (before.json): English search, options and result count remain English after real keyboard activation of the shared language switch sets html to Chinese. Topic controls are span role=button with no pressed state and 23.5px height.

Repair: native button topic controls with aria-pressed; one native activation path (remove span keydown shim); minimum 40px control heights/44px topic widths and canonical focus/font tokens; locale refresh through existing document langchange event, updating placeholders, accessible names, options and count without losing query, tag, sort or URL; clear-filter recovery returns focus to Search. No report source, title, timestamp, href, chronology or metadata is changed. All seven archive node subtrees are byte-equivalent after parsing; candidate.json carries source/page/CSS hashes and canonical template/client parity.

Dark treatment: preserve the subdued archive cards and restrained timeline, use the existing accent for selected filters and a visible keyboard outline. Light treatment: preserve the white material cards, hairline controls and cool canvas; the same outline is readable without adding dark glow. No parallel palette, shared navigation edit, or runtime stylesheet was added.

33 tests pass in the existing CI-owned tests/test_reports_timeline_ui.py, including five added regression functions and actual-client Node execution. Four functional regressions fail on an exactly paired original template/page/CSS fixture; the canonical binding check correctly passes on that baseline. The early mismatched-template-only red probe was discarded as proof for the CSS-binding test; no extra defect is claimed from it. A design gate caught literal font-family:inherit in the newly added rule; the final selector uses the existing --font-ui token and does not change any pre-existing font rule.

12 native browser cases pass: 1440 desktop, 390 mobile, 320 narrow mobile × EN/ZH × dark/light. Actual Tab/Enter/Space/taps, one history write per activation, Back/Forward restoration, native language switch, query/sort/URL retention, chronological report order and clear-search focus are exercised. All report counts remain seven. No JS exception or document overflow occurred. These are candidate fixtures with public static assets and explicit anonymous denials for member-only assets/APIs; not production acceptance. Captured filenames record initial language; native cases finish in the opposite language after the tested switch.

Canonical capture: scripts/capture_page_evidence.py at the same source base, with its actual manifest and limitations intact. The source fixture has no Git checkout identity; source/page digests, not a fabricated resolved HEAD, bind it to this candidate. All native evidence and screenshots are preserved; final canonical matrix/gate result will be appended once capture concludes.

Next: conclude source/evidence gates, submit this grouped source head through the one GitHub branch/PR, consume real first-party CI, and only after expected-head acceptance publish using the existing VPS updater. Required public verification must repeat the native keyboard-language and filter/recovery journey with no page/data interception. Do not waive first-party CI or claim this fixture is live.

## Final source/evidence boundary
Canonical capture completed 24/24 states across 1440/768/390 viewports, EN/ZH and dark/light. Every manifest image digest was rechecked; the existing visual-evidence gate passes. Design enforce-added reports zero new blockers. 33 tests and 12 native fixture cases remain green on the final b219cddc page stylesheet; the rerunnable verifier accepts --site-dir for local fixtures or --base-url for anonymous production with no interception. The expected source scope remains template/page/page CSS plus the existing registered test suite; no CI lane was added. The hosted repository CI and VPS/public release are still owed.
