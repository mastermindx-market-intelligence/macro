# ETF coverage — truthful relative age and native help

MISSION_COMPLETE: false. CAPABILITY_STATE: BUILT_NOT_PROVEN. Existing Chairman-authorized Web Chat macro-site UIUX sweep; VPS-only. Direct bounded execution is LOWER_TOTAL_OVERHEAD; no worker, custody transfer, new scheduler, runtime writer or publication system. Current protected procedure: Mastermind@9a7ed19091dd82609f8ee405687f16c861c4d8c1, Skillpack 1.0.1 / bootstrap 1, atomically loaded.

## User journey and source
Find the tracked fund coverage, understand whether its saved history is near the directory's latest snapshot, and open a short explanation using keyboard or touch. Do not imply that a comparative gap is a filing within three days of the present clock.

Pickup source: Macro@49fd490f88e1acb8a050397cc8612f2c68c765d0. Sole intended source carrier: claude/uiux-etf-coverage-20260922. Repository permission, targeted title/active-map checks and 100 recent open PR file checks were performed; no ETF writer was found. The null file list on #7595 was separately reconciled to zero changed files. Governing source/asset closure is recorded in the publication preflight. No linked repository reset, clone, permission change or migration was attempted; the working directory is an immutable-source test fixture, not a Git workspace or alternate source owner.

## Implemented scope
- Replace the inaccurate “filed in the last three days” count label with “near latest snapshot”. The existing 0–3-day threshold and all current counts are unchanged; the help states that the reference is the newest saved fund snapshot, not today.
- Move the long hover-only question mark out of the coverage summary. One native, initially collapsed “How to read coverage” disclosure provides the 40-word explanation and Chinese equivalent. The fund directory itself remains open by default on both gated and ungated builds.
- Missing history says “No snapshots”; history with no usable date says “Date unknown” and uses the neutral state rather than a stale red classification. Existing dated age thresholds are preserved.
- Keep the coverage heading and statistic chips legible at 320 pixels. Help has a 40px target and visible keyboard focus.

All 108 current fund identities, names, snapshot counts and group/order are preserved. No producer, data payload, premium row, score, rank, auth/session logic, endpoint or shared script/material source is edited. No JavaScript is added. The current public emitter's theme.js cache-query update from c907a6b8 to 3ffc953f is retained; the underlying shared JS blob is unchanged across this integration.

## Art direction
DARK: keep the existing quiet instrument shelf, restrained link/focus accent and original freshness semantics. No new glowing panel or palette. LIGHT: keep the white research shelf, hairline hierarchy and the shared native disclosure's subtle material. The short title and count remain separate rows on small screens; the explanation is optional rather than another permanent paragraph. Keyboard focus and native touch activation work in both directions. The mobile tap-highlight was inspected separately from the settled CSS; settled images are preserved.

## Proof and limits
35 tests pass in the existing CI-owned test_etfs_gate suite. Three existing tests needing the full builder/engine stack were not executed in this standalone fixture; three shipped-private-payload checks skip because no private payload was copied. These are explicit local coverage limits, not claimed repository failures or waived hosted checks. Twelve new executed cases cover comparative timing, native disclosure, missing/undated history, unchanged dated bands, empty coverage, counts and source/published CSS parity. Five discriminator cases fail against the paired original source.

Twelve native browser cases cover desktop 1440 / mobile 390 / narrow mobile 320 × EN/ZH × dark/light. They exercise Enter, Space, touch, the actual shared language control, nested disclosure state and preserved 108-fund order. All have zero document overflow and zero uncaught JS exceptions. Final browser runs use the exact integrated page with the accepted asset reference and the real shared font/script files. No page or response interception is used. The local fixture intentionally has no premium ETF payload; its 404 is recorded and is not authenticated production proof.

The canonical capture_page_evidence.py producer records 36 rest/focus/hover states across desktop/tablet/mobile × both languages/themes. Images are content-addressed and checked against every recorded digest. Canonical capture preceded only the accepted cache-query refresh; shared script bytes, page style and all rendered coverage content were identical. The exact integrated page separately passed the native matrix. The capture truthfully has no Git-workspace identity: original source/base and final page digests bind this fixture, not a fabricated checkout. The original misplaced capture attempt is not the acceptance manifest; the final producer was run with explicit local manifest/smells paths and hover/focus states. Design and UI visual-evidence gates pass without allowances.

Local rerun:
```
python3 -m pytest tests/test_etfs_gate.py -q -k coverage_
python3 research/evidence/uiux-etf-coverage-20260922/verify_coverage.py --site-dir site --output-dir /tmp/etf-coverage-proof
```
The same verifier accepts `--base-url https://www.mastermind-x.com` for owed public proof after accepted release; it does not intercept responses.

## Continuation and stops
Next: publish this exact source/evidence on the one intended branch, consume its existing repository CI/fences, qualify the current-base integration, and perform expected-head source acceptance before normal VPS publication and public proof. This is not a production acceptance receipt.

Reports #7634 and Crypto #7645 are separate existing carriers. Crypto's denied source-read family and Stage's denied ownership lookup remain held; no request is rerouted. The failed Market Memory context diagnostic is also not retried. A runtime-owner incident comment is evidence, not by itself a global release prohibition: any Reports static-release ruling must use current source law, exact source checks, and observed normal public publication, while preserving W2C activation gates and making no runtime-recovery claim. No manual updater invocation or alternate publisher is part of this ETF batch.

Alt Data #7600, Basket #7589 and earlier accepted UI batches remain DO_NOT_REDO. No background Web turn, child or automatic wake is claimed. Intended continuation is this Web Chat on the exact source carrier; the parent site-wide programme remains unfinished.
