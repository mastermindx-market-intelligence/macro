# Basket UI continuation: state, readable values, mobile flow layout

Mission: keep the existing basket research experience readable and stable while grouped UI fixes await lawful VPS delivery. Current Chairman continuation remains the authority; no Vercel operation, authentication change, model/rank/score change, or replacement PR.

Carrier: PR #7497, `sol/uiux-basket-score-resilience-20260920`; incumbent before this patch `49a568199d738ce4cdab878b42d6587fc13870e1`. Loaded protected Skillpack `f3d187976e083f9b102fc49696395b5c1521ebb5`; later protected `23061ab70a7fb79636b7962d9b440a3de23fe016` is byte-identical over the governing Skillpack/review-reuse files. Direct execution: LOWER_TOTAL_OVERHEAD on the incumbent source and regression owner.

## Before / after

- Actual Chinese mobile probe: score disclosure was open before clicking Potential and closed afterward. Native details state and summary focus now survive the full render and optional-widget reinjection paths. No new persistence/event/control owner.
- Chinese Timing & risk values printed `0mo`, `neutral`, and `high`, despite source-language labels already existing. Values now use source labels and concise units; a downtrend reads No uptrend. Missing booleans and bands remain unknown, not negative entry decisions or green low-risk readings.
- Three mobile flow statistics were compressed beside the volatility card into near-vertical text. A static, page-scoped grid gives volatility its own mobile row and keeps the three flow metrics together. Existing font, material and semantic color treatments are retained.

## Method, nulls, and constraints

`DETAIL` is byte-identical in all 121 generated pages. Auth routes, requests, inputs, ranking, sort algorithm and scores are unchanged. Source labels/reasons are escaped; unsupported shapes do not stringify objects. Native details remains the open/closed state owner. The old runtime, not a second event bus, restores state. The tiny static CSS block remains inline under the existing 1KB externalization threshold; no runtime stylesheet or new palette.

Dark treatment: existing calm instrument panels, muted supporting text and semantic accent borders. Light treatment: existing white research panels, cool canvas and hairlines. The responsive layout is shared, not a new skin; compare the prior #7497 flow screenshots and `basket-before.json`. Source-bound warnings about model limitations remain visible.

## Proof

Five new CI-owned regressions discriminate the previous source; 156 scoped assertions pass. `verify_resilience.py` performs actual summary clicks and sorting in 16 US/China × desktop/mobile × EN/ZH × dark/light cells, forces optional-data 401s only on the local fixture, checks both open and closed preservation, translated source values, minimum flow tile widths, one disclosure, no invented live strip, no page errors or horizontal overflow. It does not impersonate a member.

`verify_refresh_state.py` holds the real local cross-market response until after user interaction, then releases its unchanged committed JSON. Four additional cases verify both open/closed state and keyboard focus survive a genuinely asynchronous render. Canonical rest/focus matrix lives at `mockups/evidence/uiux-basket-resilience-continuation-20260920/`.

## Remaining release gates / next action

This patch is BUILT_NOT_PROVEN, not a production claim. The last exact-head hosted failure on #7497 was the shared Research Screener/theme fingerprint mismatch; #7361/#7437 additionally carry the #7469 unregistered suite and HK/Canada receipt drift. Their accepted repair owner remains the existing #7469 follow-up; active light-theme #7466 owns overlapping theme/screener paths. Do not copy those source changes into this UI branch, alter hash assertions, bypass red checks, or restart unchanged CI in a polling loop.

After accepted shared-owner repair, obtain current-base integration proof, concluded binding CI and expected-head merge. Publish through the existing locked VPS updater and run the verifier on the public URL without interception. Production proof must distinguish expected authentication-required optional requests from actual UI failure. Do not redo the already-live Bonds, main hub, Government Revenue, Macro Context or Policy Watch releases. Do not claim the new Basket/Intelligence Hub/Market Structure repairs live until proved.
