# Basket Detail — current-base release qualification

Mission: finish the existing grouped frontend repair on PR #7497 through VPS-only publication; the wider sweep remains incomplete.
Authority: current Chairman continuation; DEC-UIUX-VPS-ONLY-DELIVERY-20260920; protected Mastermind Skillpack 3e66e43258f34db240d5bff76f54148c7af84ee4 (1.0.1). Principal release judgment on the same source carrier, no worker or custody transfer.

## Reconciled finding
The earlier report against main 19fb4501 found two stylesheet fingerprints. Main 4f4f64931d6e33d61b901c313132a5116e3f0c32 already contains the complete regional rebuild: its actual integration with candidate 51e616f0e7339241ab6a2ed125ab53282ee48b12 has canonical f2599f8e on all 121 pages. No additional generated-page rewrite was necessary or performed.
The preceding candidate used fb966e77, a whitespace-only hand-projected variant of the canonical template style. The existing CI-owned parity guard now requires exact canonical template-to-CSS bytes as well as the shared content hash. No tests are waived.

## Verified effect
160 targeted tests passed. All 16 US/China × desktop/mobile × EN/ZH × dark/light native-interaction cases and four delayed-response focus/order cases passed. The Basket template is byte-identical to the preceding candidate: no new sorting, model, payload, layout, auth or endpoint change. Current-main data/chrome updates are preserved.
Evidence: css-bindings.json, tests.txt, browser/keyboard-results.json and screenshots. before-test.txt is a SUCCESSFUL current-base preflight, not a claimed failing reproduction; css-bindings.json records the immutable former-head mismatch.

## Remaining gate / do not redo
This is material current-base integration, not an ancestry-only refresh. No hosted-green/merge/deployment/production claim follows from local proof. After one grouped push, consume concluded exact-head checks and current-base compatibility; only then expected-head merge, existing locked macro-update publication, and real public native-control proof. Do not redo prior feature work, re-grade data, edit private-access rules, use Vercel, create a replacement PR, or override red checks. No child or autonomous wake exists.
