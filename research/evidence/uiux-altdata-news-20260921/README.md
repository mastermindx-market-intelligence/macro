# Alternative Data — truthful related-news states

MISSION_COMPLETE: false. Existing Chairman-authorized grouped Web Chat frontend sweep; VPS-only, no Vercel. Current compatible protected Skillpack Mastermind@0f4aeae4d25f3770474a7ac70e0ce12af6e2d075 / 1.0.1. Direct diagnosis/repair is LOWER_TOTAL_OVERHEAD; no child, source transfer, external provider or new control plane.

Source carrier: claude/uiux-altdata-news-20260921, base 1dc11fb3eb326393c803bc2eff408db89bb91bc1. Exact-file current PR census found no writer over the Alt Data template/page/producer. News #7591 is a separate held carrier; this work does not edit it or reroute its reported browser restriction. Frozen News integration package a7bf24dbbe1f4a35d7de0272c615490f6c46a34b is routed at issuecomment-5757925068. Basket #7589 and prior releases are DO_NOT_REDO.

User job: read related headlines or understand why they cannot currently be shown, without confusing authentication, outages and genuine empty snapshots. Public reproduction: the three existing same-origin requests return 401 yet the page promises the feature will be built tomorrow. Evidence before.json/PNG. The candidate must distinguish these states, offer explicit retry for temporary failures, use available Chinese headlines and avoid raw duplicate signal scores/implementation prose.

Bounds: keep existing three GET endpoints, source order/caps, data schemas and access rules. No credentials, authentication widening, model/rank change, source-data mutation, new polling or automatic retry. A native Retry reuses one in-flight load; language changes update messages without a network request. Only HTTP(S) source links are interactive; untrusted labels remain escaped.

DARK TREATMENT: existing quiet instrument card, restrained muted context label, existing token-driven link/focus ink. LIGHT TREATMENT: existing white research card on neutral canvas, hairline rows and plain source labels, not a dark glow repaint. Both retain the same source order and states; native loading/empty/error/retry controls are mirrored EN/ZH. No new palette or shared header/style changes.

Acceptance: discriminating executable page-script tests on 401/403/404/503/network/invalid JSON/invalid shape/empty/related/fallback/retry, unchanged surrounding source/data, eight native browser theme/locale/device cases plus delayed/manual recovery, canonical visual evidence, exact-head repository checks and VPS-only public proof. Fixtures are explicit simulated responses, never authenticated-production proof. Next: finish this bounded repair, one grouped PR, then gate and publish. Prior completed work must not be redone.


## Verified candidate boundary
The related-news widget now keeps 401 (sign in), 403 (restricted), temporary/malformed failures, valid empty data, valid related news and valid market fallback separate. A missing per-name feed never becomes evidence that no related stories exist. Existing source-provided Chinese titles remain visible in Chinese mode; absent translations retain their supplied original title. The repeated raw news-side signal score and unsupported overlap interpretation are removed from this secondary panel; the underlying conviction board remains byte-identical.

Manual Try again reuses the existing three GET paths, with one in-flight batch and no polling or automatic retry. The first real-browser pass caught a focus failure from setting native disabled on the focused Retry button. The corrected native button uses aria-disabled plus the explicit existing busy guard, preserving focus during the request and returning it to Open News only when Retry disappears and still owns focus. Moving focus elsewhere is not overridden. The initial failing browser log is retained, as is a separate verifier-only response-read mistake that was corrected before final proof.

83 targeted tests pass in the existing CI-owned Alt Data price-truth/intelligence/hygiene and product-chrome suites. Twelve new executable page-script cases failed against the original script; the canonical page/CSS parity regression is a thirteenth case. No new pytest suite or parallel CI job was created. Tests cover authorization, network/status/JSON/shape failures, valid empty results, honest fallback, escaped source content, safe HTTP(S) links, original order, language changes without fetching, and a duplicate retry attempt while one request is active.

Eight native browser cases pass across desktop/mobile x EN/ZH x dark/light. Each uses explicit local 401 and 503 fixtures followed by delayed illustrative success; real Settings/keyboard/touch operations prove localized statuses, non-duplicating Retry, focus recovery, title order and Chinese title visibility. These fixtures are not authenticated live data or production acceptance. All eight report zero uncaught JS exceptions and zero document horizontal overflow. Public no-interception proof remains owed after accepted release. verify_news.py --base-url https://www.mastermind-x.com exercises the actual anonymous gate without replacing responses.

Canonical visual evidence records 24 rest/retry-focus states across desktop/tablet/mobile, EN/ZH and dark/light. The static local snapshot lacks the optional endpoint payloads, so those captures truthfully show request-unavailable state; native fixtures separately show sign-in and related-data states. The manifest records its real access gaps. Screenshot review preserves the existing white research card/hairline hierarchy in light and quiet instrument card in dark, with the same links, states and focus behavior.

Before publication, current main 96e5b35ae123d196694d4576d9c4f94fc7767971 has no material movement in this page's source/producer/report-base/theme/dependency/governing closure. Do not create an ancestry-only refresh. Exact post-commit source identity and current integration tree must be read back, then repository CI must conclude before any merge or VPS update. No Vercel use, no bypass of first-party checks, no current production claim.

Standards references used for the implementation: MDN Using the Fetch API (HTTP errors require explicit status handling) and WAI ARIA22 (status updates without moving focus). These references guide UI behavior, not authorization or investment decisions.

Continuation: this Alt Data batch remains BUILT_NOT_PROVEN until exact-head checks, accepted latest-base integration and VPS/public proof. Resume this Web Chat on the same source branch. News source is frozen at a7bf24dbbe1f4a35d7de0272c615490f6c46a34b and routed to #7591 via issuecomment-5757925068; do not publish it as a competing PR or claim its acceptance. Basket #7589 and prior releases remain DO_NOT_REDO. No child or automatic wake exists. The parent UIUX programme is incomplete.

Final pre-publication gates: visual evidence PASS (24/24 real captures with every image digest verified); design-system enforce-added PASS (zero newly blocking findings); runtime-style injection PASS after satisfying its normal full-site checkout precondition (195 JS files scanned, no new allowance); Agent OS validation PASS (zero errors, inherited warnings remain). The discovered record now cites the actual runnable regression command. No suite is waived.

## Current release reconciliation (21 September 2026)
Prior head 5b527312a216db984928a0f2e7b7db8454fda5a6 completed all first-party CI. Current main f7539ad7e4e22cd889ff6d75190c2e1d686a0a53 caused a real generated-page conflict in five shared-asset refs. This same-carrier merge consumes those refs only, not a cosmetic ancestry refresh. Candidate template, client logic, existing tests and page CSS are unchanged. release/integration.json binds both sources and exact resulting page. 83 tests and all eight native fixture cases pass with current assets; screenshots and logs are preserved in release/. No model, board data or authorization changed.
Next: push this reconciled head once, consume its new exact-head repository checks, then expected-head merge and VPS/public proof. The surrounding News collision remains held with #7591; do not duplicate or route around that owner's reported browser restriction. All accepted Basket releases remain DO_NOT_REDO. No unresolved effect or child. The wider mission is incomplete.
