# Transmission scenario details — native controls and bilingual assets

MISSION_COMPLETE: false. CAPABILITY_STATE: BUILT_NOT_PROVEN. Existing Chairman-authorized Web Chat grouped UIUX programme; normal VPS-only release. Direct scoped work is LOWER_TOTAL_OVERHEAD. No external worker, source transfer, runtime/auth operation, new control plane or alternate publisher.

## Capability and source
Public transmission.html showed four noninteractive '+1 more' DIVs. They were not keyboard/touch controls and their remaining asset was absent from the rendered page. The scenario asset names also remained English in Chinese mode. Public paired source SHA256 ccb2acd55c4050ab59a9f2bf1a42556e00810266619d3be05986bfeececcc0b9 was observed at desktop/mobile sizes. This is a frontend defect, not missing scenario calculations.

Original implementation source: c159115451e5dd5f33a2565dac2c7e9221b03ee1. Qualified current-data integration base: 668237947e016f679782e41e61c91c9133a5ea99. Protected Mastermind procedure: 89582a372aa2a57ec500868ce6d79cd156219445, Skillpack1.0.1/bootstrap1; continuing/review/reconciliation/closeout companions fetched at that exact commit and material compatibility checked. Source carrier: claude/uiux-transmission-scenarios-20260923. The fixture is not a Git workspace or separate source owner. The existing builder remains the canonical emitter.

The first four rows per side stay visible. Native Show more/Show fewer disclosures expose only the remaining source-supplied entries. Chinese names reuse the same snapshot's tx.transmission asset-label map; an explicit row translation wins, and absent translations honestly fall back to English. Long names wrap rather than disappearing. Native summary controls are at least40px tall, keep focus/open state across language changes, and use a real visible2px focus outline.

All28 original glance rows retain their order, signed values and bar geometry. The full32 rows are now reachable, using the same cross-side normalization and unchanged producer caps. The latest normal render's Cascade Monitor state and build timestamp are preserved, not restored from the older page. Its dollar-channel metadata refresh does not change scenario rows or their canonical name map. All4 scenario titles, assumed shocks, historical-illustration caveat, source records, other sections, shared scripts, authentication/access rules and model logic remain unchanged. No JavaScript, network request or asset dictionary was added. No engine/producer code was edited; incumbent#7593's PIT research/builder work remains separate.

## Art direction
DARK: preserve the existing instrument cards, channel rails and muted directional bars. The disclosure is plain text within its side, not a new glowing card; its foreground-based focus outline remains visible. LIGHT: retain the white research cards and hairline structure; no dark glow transferred into a tinted panel. The control's neutral ink and clear outline work against the light surface. At320px labels wrap while numeric columns stay intact. Other card composition, fonts, palette and animation are untouched.

## Verification and counterexamples
34 tests pass in existing CI-owned tests/test_transmission_publish.py, including16 added cases for0/1/4/5/7 rows per side, canonical Chinese names, explicit-label priority, fallback, escaping, signed values, shared scale, native control contract and exact emitted CSS parity. Original paired source fails14 of the new checks; the meaningful behavior discriminators include absent fifth rows, missing Chinese labels and absent native controls. No new test suite or CI job was introduced.

12 actual-client cases cover desktop1440, mobile390 and narrow320 × EN/ZH × dark/light. They exercise native Enter/Space/touch, real shared Settings language switching, expanded-state preservation, every32-row identity/value, individual name/number bounds, and actual computed focus outline. All pass with zero JS exceptions, zero failed requests, zero page overflow and no clipped scenario rows. These are local immutable public-page fixture results, not production acceptance or private-data/authenticated tests. All shared fonts/scripts used by the page were present; no font/private payload is included in this source change.

24 canonical capture_page_evidence states cover desktop/tablet/mobile × EN/ZH × dark/light at rest and real summary focus. Screenshots and tool metadata are preserved in mockups/evidence/uiux-transmission-scenarios-20260923. Additional native-open screenshots are alongside this packet. The producer records no Git identity for the fixture; exact source/base and page hashes are the attribution, not a fabricated checkout.

Screenshot review caught a missing token in the initial candidate: --link is not supplied on this standalone page, so a syntactically declared focus rule still computed as outline-style:none. That candidate was rejected. The final uses the existing ink fallback/currentColor, and the verifier requires an actual solid2px focus outline. CSS source/output parity also rejected a whitespace-mismatched generated asset; final CSS is rendered exactly with the existing builder C values, not a second palette.

Final page SHA256: 8d4bca30204a2a181cfcbff8b74227175fce453a56c32e3683b56bd3271d36aa. Canonical page CSS: 807431ed.css. Source/template CSS is rendered with the existing builder's palette and compared byte-for-byte (apart from outer whitespace); file fingerprint is verified.

## Rerun and release
```
python3 -m pytest tests/test_transmission_publish.py -q
python3 research/evidence/uiux-transmission-scenarios-20260923/verify_scenarios.py --site-dir site --output-dir /tmp/transmission-proof
```
After accepted current-base checks/merge, the same verifier supports --base-url https://www.mastermind-x.com and performs no response interception. Production proof is still owed. Consume exact-head hosted checks, classify current-base changes, preserve newer data if rendering advanced, use expected-head merge and normal existing VPS publication. Never manually retry the separate failing runtime updater or use Vercel.

## Continuity and non-goals
The ETF#7697 source-publication refusal, Crypto#7645 source-read refusal and Stage ownership refusal remain scoped holds in the unchanged Studio generation. This independent Transmission operation is not a retry, replacement or re-home of those operations. Reports#7634, Alt Data#7600, Basket#7589 and earlier accepted batches remain DO_NOT_REDO. Forex#7409, News#7591 and Flow Velocity#7072 remain incumbent-owned. No autonomous Web turn/wake is claimed. The exact next action is release qualification and public proof of this grouped repair, not redoing accepted code.
