# Signal Lab explicit benchmark-label repair (W2B)

Operation: signal-lab-benchmark-labels-20260916-sol-001. Base 3daf739affa12a43a6b3a0feb3b4c57154587756. Procedure Mastermind eec5324c5205e8bad206512e0a936898e50b2408 / skillpack 1.0.1/bootstrap 1. Current Chairman approval continues the existing Signal Lab revamp.

## Mission and bounded design
Make excess-return research measure the asset simple return minus an explicitly named price-benchmark return over identical target-clock endpoints. A declared price benchmark is not the top-level strategy baseline. Missing/ambiguous old declarations are rejected; no benchmark is guessed and no historical result is rewritten. This extends the existing spec, screen, evaluator, result and trial records, not another identity/store/queue.

Explicit target.benchmark={path,column}; target column is also mandatory. Paths are repo-relative and tracked. Same resolved file/column is rejected. The price benchmark is buy-and-hold only in this slice: strategy benchmarks/cash/FX conversion and economic/rights certification are separate work. Exact endpoint matching; no row-grid compression, implicit column fallback, interpolation or fill. Invalid asset prices/clocks stop evaluation; unavailable benchmark endpoints remove only affected windows. Target horizon remains target price bars, not calendar days.

Version the label in the result and existing trial configuration. Legacy trial history remains intact; a corrected-label evaluation is a distinct attempted configuration, not a retroactive rewrite. Admission thresholds and existing feature-lag construction are untouched. #7190 supplies the separate numerical-clock repair and #7239 the complete construction identity; their frozen source branches are not modified. Prove combined source before release.

## Ownership / gates
Direct Sol reason: PRINCIPAL_JUDGMENT for resolving target-vs-strategy semantics and NO_ELIGIBLE_PRE_EFFECT_WORKER; current independent-review route is unbound/unreachable. Source carrier is Remote Desktop Commander, exact Mac-Studio 3f5ce987-e3eb-40a3-af9f-4b0ae54919cc, canonical Macro sparse worktree. No provider, service, runtime Job, collection, notification, discovery activation, production write or trade authority.

## Acceptance / order
RED on original evaluator -> minimal producer/consumer repair -> manual arithmetic and exact-calendar negative controls -> immutable old-input comparison -> independent review, binding CI, composed source release -> installed research-only proof. Until then BUILT_NOT_PROVEN. Every original result and the two affected legacy candidate declarations are do-not-rewrite. No speculative auto-migration.

## Continuation
Fresh-pin only material source, recover this branch and pending source effects. Current next action: run the new regression suite against the original evaluator before modifying production source. The parent WS:SIGNAL-LAB-REVAMP remains in #7190; do not mint another parent. Full spec/result-generation freeze and executable maturation remain downstream.

## Material CI discovery
The trusted PR executor passes --gate code, and the existing causal-factory job is gate:data. Its numerical test enrollment was therefore not a binding PR execution claim. W2B adds one hermetic signal-foundry-regressions code job to the EXISTING legacy manifest/executor, with tests/test_sf_*.py so the already-published clock/identity tests are also included when composed. No workflow, runner, schedule, scope-exclusive exclusion, admission bypass or duplicate CI authority is created. The actual load_legacy_jobs(gate=code)/select_jobs assertion failed before the repair. Existing data-tier job and predecessor enrollments stay intact.

Shared compiler hint now explains the conditional explicit benchmark contract. Both existing local filing functions accept an explicit price comparison and refuse a legacy ambiguous one without any provider invocation. Old valid-spec test fixtures gained a real benchmark column; no acceptance assertion was weakened.

## Verified implementation checkpoint
The original 32-case regression run returned 31 failures / 1 pass. The explicit benchmark repair now passes all 168 currently present test_sf_*.py cases; eight in-memory deliberately faulty versions are caught by assertion failures. Two old valid-spec fixtures gained an explicit comparison column without weakening assertions. The shared compiler and both existing file-only proposal paths are exercised without provider work.

Pinned QQQ/SPY close_price inputs from 3daf739affa12a43a6b3a0feb3b4c57154587756 agree with independent scalar arithmetic across 34,385 labels over 5/10/21/63/126 target bars (maximum error 5.56e-17). These are label arithmetic checks, not a signal backtest. SF-0003 and SF-0011 retain their original ambiguous declarations and are refused; no automatic benchmark migration or historic write. Reproduction: python3.12 research/evidence/signal-lab-benchmark-labels-20260916/recheck_labels.py --output .pytest_cache/<new-private-directory>.

The four-source test tree a029ed8dfa5cbff94d5fe57d0630a9eff31e1ff7 composes this candidate with #7190/#7212/#7239. 563 selected tests passed with zero failures/errors/skips. Thirteen exact function-body/file comparisons preserve predecessor ownership and the new label contract. Initial virtual merge exposed two context conflicts; localizing only this candidate's screen import and leaving shared clock-documentation edits with #7190 resolved them. No actual branch merge or predecessor edit occurred. Detailed hashes/commands reside in the existing evidence directory.

Frozen source verification: .pytest_cache/benchmark-frozen-002.xml (168 passed); .pytest_cache/four-candidate-test-001/four-candidate-test-001.xml (563 passed); .pytest_cache/benchmark-mutations-final-002/receipt.json (8/8). The exact final contract checker is currently running under native PID56124 and will be consumed before publication; do not duplicate it. Intermediate contract runs exited 0 but are not substituted for its source-frozen receipt.

## Release and next dependency
Source release requires independent exact-head numerical/contract review, the new code-gated test unit actually executed in concluded hosted CI, exact four-candidate integration and source/effect reconciliation. Installed research-path proof remains a separate gate; no new verdict may claim alpha from this patch. Release should compose the existing clock and identity repairs rather than treating baseline-only execution as the finished instrument.

Do not reset trial counts, rewrite old candidates/results, create another Agent OS workstream or widen the pending two-target review placement request silently. Full spec freeze and result generations are not delivered here: the inherited per-ID result writer can still replace a same-ID result in an authorized future run. That limitation is an activation blocker for autonomous evaluation until the next correction-safe result-generation vertical is accepted. Data rights, comparable price bases, availability/vintages, tradable strategy and prospective validation remain their own evidence.

Current next action: consume PID56124, bind its source hashes, then commit/push/open one draft/HOLD candidate on this exact branch. Preserve the parent and all three previous PRs.

Publication sequencing update: the exact new code-job dependency list was installed in a fresh private venv, and its full 168-case Foundry command passed there as well. The redundant source-frozen contract census remains running under PID56124 (its child/base worker were observed alive). The draft may be published with that pending state explicit rather than delaying hosted validation; publication is not release and no check is waived. Consume the same process result and bind it in a PR checkpoint, without restarting it or changing source while it runs. Source release/activation remains held.

Review-route delta: Secretary names the existing tailnet-only Serve front door to the Studio loopback gateway; its own MCP/Serve path still fails to load/reach it. No reviewer assigned. An attempted current routing-companion read for a same-root response was platform-blocked (no process/no effect); no alternate-carrier retry, network/service change or new delegation occurred. Source procedure had already loaded compatibly at the pin above; this does not claim that the optional routing action completed.

## Hosted enrollment correction
The actual hosted contract-delta log (run35175949020/job105057570424) reports the new benchmark test filename unwired. The wildcard command executes it but is not a literal suite enrollment recognized by the existing census. Add the exact benchmark file alongside the retained wildcard on the same code-gated job. The resulting duplicate invocation of this one small suite is deliberate; wildcard coverage of composed clock/identity files is retained. No test/gate waiver or new workflow. Source remains unreleased, original headc9c641fb747b. This bounded correction does not touch the active D2 research capability branch.
