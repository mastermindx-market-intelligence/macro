# D1 → D3/W3B MERGE-ORDER RECOMMENDATION (recommendation only — no implementation)

**Question (wave-graph §4.6):** exactly which workstream writes `theme_state/v1`, in what sequence relative to D2, and which paths are prohibited to the other lane?

## Recommendation

1. **GMI W3B writes `theme_state/v1`.** Grounds: GMI owns graph topology + theme state by frozen owner map; the producer machinery (`engine/theme_graph/`, `data/theme_graph/state/` target), the probation plane, and the rights gates are all GMI-owned; and the census shows the hard part of ThemeState is membership/identity/PIT semantics — graph-owner territory — not Prophet-side projection.
2. **Sequence: D2 BEFORE W3B.** The census makes this non-optional: (a) graph membership is ticker-string-keyed — ThemeState computed on it inherits reused-ticker/ETF defects (two live in the data now); (b) 73% of the union has no thematic membership — ThemeState over today's memberships is semantically thin exactly where V4 needs it; (c) crosswalk/THS planes are CURRENT_SNAPSHOT_ONLY — velocity/acceleration on them would be un-replayable. D2 (identity-grain repair + canonical mapping breadth via probation + PIT membership discipline) is the substrate wave; W3B computes on its output.
3. **V4-D3 becomes the CONSUMPTION-CONTRACT wave, not a build wave:** after W3B ships `theme_state/v1`, D3 wires the Prophet-side read (family status vocabulary, coverage bands, ACCRUING behavior) and the intelligence-vector join. If Sol prefers to keep the D3 label for the GMI-side build, then D3 executes INSIDE the GMI workstream with a V4 acceptance reviewer — either way ONE lane builds.
4. **The Neural Web `thematic_state` lineage must be reconciled IN W3B's charter** — supersede, extend, or explicitly fence it (`engine/neuralweb/thematic_state.py`, `data/neuralweb/theme_state.json`, `theme_phase_history.jsonl`, 18-theme vocabulary, OBSERVED_PIT since its start date). Three theme-state vocabularies (neuralweb + W3B + any V4 projection) is the failure mode; two is the maximum tolerable (owner engine + its V4 projection), one truth store is the requirement. Its bitemporal hash-chained history is genuinely useful prior art for W3B's PIT design.
5. **Path prohibitions while W3B is active:** V4 lanes may not touch `engine/theme_graph/**`, `data/theme_graph/**`, `config/theme_sources.yml`, `config/theme_crosswalk.yml`, `engine/neuralweb/thematic_state.py`; GMI's W3B may not write Prophet projection surfaces (`prophet.intelligence_vector` emitters, board/UI files) or the V4 records plane. The owner-surface formulas (`engine/subsector_turn.py` z-primitives) are citable prior art for W3B but that engine stays the rotation surface's.
6. **Rights rider:** until the Finviz derived-display tier is ruled (RIGHTS_DECISION_REQUIRED #1), W3B's outputs computed from Finviz/THS memberships are internal/display-context only — the rank path (E1) cannot consume them publicly, and per GMI G0.11 they may not feed any ordering path without a GMI-side ruling regardless.

**Status:** recommendation for Sol's adjudication with GMI. No D3/W3B work was started in D1.
