# V4-0A PACKET — INDEPENDENT ADVERSARIAL REVIEW AND DISPOSITIONS (2026-08-17)

**Reviewer:** fresh-context opus `reviewer` agent (+4 verification sub-agents), commissioned per the 0A handoff's ten attack vectors, with receipt spot-checks against pinned main `fc0557bb0873`. **Every CRITICAL/HIGH finding is dispositioned below and the fixes are applied in this same PR (#5832).** ~30 receipts spot-checked: 24 CONFIRMED exact, 2 PARTIAL, 5 WRONG (all five corrected).

## Findings and dispositions

| Sev | Finding (reviewer's claim, verified) | Disposition (applied) |
|---|---|---|
| CRITICAL | "No episode registry exists" is false at the stated altitude: Radar ships a runtime episode ledger — `engine/entry_radar/live_ledger.py:168` `compute_episode_id(ticker, detector_id, variant, first_armed_at)`, keyed store, terminal states — solving the same re-arm-address problem | **FIXED** — ledger row 30 evidence corrected; freeze §3 gains the grain reconciliation: Radar's ledger is per-(ticker, detector, variant) EPHEMERAL runtime state (`/var/lib/macro-live/`, explicitly "not the durable evidence store"); V4's `pe:` plane is per-(security, epoch, structural anchor) DURABLE candidate identity. B1 extends, joins, and never rebuilds the Radar ledger |
| CRITICAL | MP-1 gate G-A is NOT satisfied in production: `HEAD:site/prophet/index.json` carries no `lifecycle_state`/`lifecycle_counts` — the producer (#5506) merged 08-14 ~17h AFTER the last checkpoint, and the outage has blocked every nightly since. Packet asserted "all three gates satisfied" in five places | **FIXED** in all five sites — G-B/G-C satisfied; **G-A = producer merged, blocked on A1's first published nightly** (law 24). B5 gains an explicit dependency on A1's published artifact |
| HIGH | B1 not executable as specified: `mastermind.entry_event.v1` has no `episode_id` field and its 21-field list is frozen (`entry_events.py:292-314,595-598`) — the event→episode join key does not exist and no wave owns the contract change | **FIXED** — B1 precondition added naming the join-key gap; a merge-order ruling (wave-graph §4) must pick the owner: Radar ships `entry_event.v2` with episode linkage, or B1 documents a deterministic reconstruction join on `(ticker, detector_id, signal_ts)` |
| HIGH | Freeze claimed the 25 laws "ratified verbatim" without reproducing them; laws 3/12/13/15/20 (incl. no-LLM-origination) reached no builder — waves spawn with the freeze, not the masterplan | **FIXED** — all 25 laws now inlined verbatim in freeze §2 |
| HIGH | B3's "no client inference" promise can't be delivered: the fourth derivation (client-only KNIFE bucket, `templates/stocktable.js:1149`) lives in a file no wave owned; it is also a paired plain-copy asset | **FIXED** — `templates/stocktable.js` + byte-paired `site/stocktable.js` added to B3's owned paths with the pairing law noted |
| HIGH | B-15…B-19 + CONFIRMING/CONFIRMED receipts were stale copies of the 08-11 audit: at pin, the CONFIRMING/CONFIRMED constants were DELETED by the §J.9(c) ruling (`us_early_turn.py:960-963`) and the chase leg is now gated on a LIVE fire (`:1026`) — the source asserts the opposite of B-19 | **FIXED** — CURRENT_STATE §5 + ledger rows 12/32 re-resolved at pin: labels kept as the audit's vocabulary, with the at-pin status that several appear already closed; B2 opens by re-resolving each rather than assuming any remain open |
| HIGH | `research/PROPHET_RULING_J9C_J10_LIFECYCLE_CELLS.md` — the ruling that deleted those constants, established `lifecycle_state` as the public lifecycle vocabulary, and produced #5506 — was cited nowhere; B3 sits directly downstream of it | **FIXED** — cited in freeze §4/§12; B3 must compose the four state fields with `lifecycle_state`, not re-decide it |
| MEDIUM | `UNAVAILABLE_DATA` maps to no lane though it is the mandatory fail-closed output; "Invalidated/Expired" secretly derives from two planes | **FIXED** — freeze §4 now rules both: UNAVAILABLE_DATA rows are never green, always visible in All Candidates with an explicit data-unavailable chip and counted in the header's degraded summary; Invalidated/Expired documented as the one two-plane lane |
| MEDIUM | F6_MACRO_REGIME pooled with F3/F7 as "structurally absent" — the source says F6 is STRUCTURALLY EXCLUDED (row-constant, cross-sectionally degenerate; router axis only) and warns not to blur exactly this | **FIXED** — both cells corrected; D-lane/E1 barred from treating F6 as fillable |
| MEDIUM | Dependency disagreements between wave table, mermaid, WS record (B1, D5, A1, D6) | **FIXED** — table = single source of truth; mermaid regenerated; B1 deps unified to A1, D5 to D1 (theme family ACCRUING until D3), D6 to D5+EIOS |
| MEDIUM | Active anon full-board leak scheduled ~25 waves out | **FIXED** — promoted to an unsequenced standalone repair (not one of the 29 waves), executable any time after 0A via the ETF-desk `premiumdata` pattern; operator chip filed |
| MEDIUM | B5 widens surfaced population under an unchanged `SELECTION_ERA` — pre/post-B5 v3 records would span visibility regimes | **FIXED** — freeze §7 gains the visibility-regime rule: new V4 surfaces accrue in candidate-episode cohorts (never into v3 board cohorts); any change to the v3 board's own visible population mints an era/definition boundary |
| MEDIUM | `stage` — the most-collided term — missing from the binding vocabulary table | **FIXED** — row added (6 simultaneous meanings incl. the frozen `entry_event.v1` field and MP-1's banned-word rule) |
| MEDIUM/LOW | Receipt corrections: `daily.yml:2280`→`:2270` (3 sites); heading subtitle attribution → `dashboard.html.j2:15929,15957`; `_STAGE_BY_LANE` is MP-1's label for the inline dict at `:16035`; "R3/R4"→R3 (no R4 crops exist); `tier_preview.js` caps at `:27`; "eras never relabeled" cite → masterplan §6.5(6) (law 19 = no pooling); ledger row order; V3 admin comparison view (§6.5(5)) assigned to C2 | **ALL FIXED** |

## Reviewer's verdict (verbatim conclusion)

"The packet's spine is sound … nothing grants an LLM, a theme score, or a learned model any authority it has not earned, and nothing lets intelligence waive availability. … All seven CRITICAL/HIGH items are fixable in the packet text plus two wave-graph edges without reopening any frozen decision. Fix the two CRITICALs and the five HIGHs, correct the `daily.yml:2280` receipt and the `UNAVAILABLE_DATA` lane gap, and this is safe to open as the V4-0A PR."

Items the reviewer confirmed sound with no change needed: the Theia ruling (rights-safe, strictest option), all alt-data rights rows, E3–E6 gauntlet gating, wave count (29 exact, critical paths byte-faithful to §22), mixed-era discipline, the two-grain shadow design, the three DNR rows relied upon, and the parking of genuinely-unresolved items as unresolved.

## Second lane: owner-map / DNR collision sweep (reported after the first lane; ALL dispositioned in this PR)

| Sev | Finding | Disposition (applied) |
|---|---|---|
| BLOCKER | Six wave paths collide with REGISTERED `owns_paths` of active workstreams with zero rulings filed — above all, `engine/prophet_*.py` is registered to `WS:PROPHET-US-ENTRY-TIMING` (triple-confirmed), making the owner map's "not sibling-owned" §2 header false; the wave-table paths are invisible to `scripts/agentos.py`'s collision detector | **FIXED** — wave-graph §4 now carries nine dated rulings (B2/B3/B4 joint under ENTRY-TIMING; A1/A2 as AVAILABILITY's continuation; B6/B7 under Radar incl. inheriting W9's own deps W6/W8; E1 inside Fusion post-adjudication; 0B under AGENT-OS); owner map §2 header corrected + ENTRY-TIMING row added; both workstreams added to `depends_on`; §4.8 promotes wave paths into `owns_paths` as waves start |
| BLOCKER | Five live systems doing the "new" contracts' jobs were absent from the packet: `entry_signal.py` (buy zone/chase/stop semantics), `live_ledger.py` (runtime episode registry), `us_candidate_lanes.py` (operator-commissioned lossless candidate pool — falsifies the "no all-candidates plane" premise), `freshness_sentinel.py` (reader-visibility + first-fresh settlement receipts), `prophet_doors.py` (preregistered origination doors); plus `prophet_integrity.py` reconstruction provenance and the `market_memory_pit` manifest pattern | **FIXED** — all bound into owner map §2 as prior art with reconcile-or-supersede obligations on B4/B1/B5/C1/A2/A1/A3; ledger rows 30/31/33/44 corrected (NOT_BUILT → PARTIAL with the prior art named); CURRENT_STATE §10.19 registers the set |
| BLOCKER | The masterplan carries zero `DNR:` citations and two §1 FORBIDDEN rows are engaged unconfronted: `KILL-FUSED-COMPOSITE` (E1's fused conservative priority) and `KILL-POSITIONING-FUSION` (D5/D7/E1 positioning families) | **FIXED** — freeze §12.7 opens the confrontations by name: E1 requires an explicit adjudication (same-construction lineage under Fusion, or a further amendment) BEFORE spawn; positioning families enter only under that lineage or stay display; wave-graph §4.4/§4.9 make the gates standing; freeze §13 now states the registry, not the freeze, is the compliance surface |
| MAJOR | Further DNR near-misses: `KILL-WASHOUT-TURN` (monotone emergence ladder), `KILL-LLM-ORIGINATION`/`FRAME-TAGS` vs law 20, `KILL-NIGHTLY-HARD-GATE` vs A3's "refuses mixed bundle", `KILL-FRESH-TICKS-WINDOW`, `KILL-PROPHET-POP-MERGE` second clause, `KILL-OUTCOME-AUDITION` (E4/E6), `KILL-OFFHORIZON-VERDICTS` (C1 H10/H42), `HOLD-IGNITION-SURFACES` honest-null; GMI G0.11 fences ThemeState from ordering paths; `DEC:LER-SEPARATE-SYSTEM` wall vs B1/B7 | **FIXED** — each has an explicit clause in freeze §12.7 (emergence depth is a prohibited rank input; law 20 operates UNDER the LLM kill rows; A3 = loud disclosure never fail-dark; FRESH_TICKS invariance receipts; lawful pop-merge form stated; theme family display-only in rank until a GMI-side G0.11 ruling; the LER promotion DEC is owed before any radar-derived field influences rank/gating) + wave-graph §4.6/§4.7 (nightly-advancer law stated) |
| MAJOR | Factual errors: `N_CANDIDATES=None` stated un-nuanced in the owner map + WS do_not_redo; B2's owned-path line numbers stale (`:4018,:4318-4319` → actual wiring `:4262,:4278,:4410-4412,:4490`); "ten frozen decisions" (the freeze numbers nine; the tenth lives in the wave-graph doc); D6/E6 dep mismatches + two stale mermaid edges; `WS:EVAL-OS-*` glob is not a citable key; Fusion 8-path paraphrase dropped 2 of 8; `site/turn_watch/` not taken into `owns_paths`; TURN WATCH already publishes `beyond_cap` (the "anti-TURN-WATCH clause" naming is about the un-shipped SURFACE, not the engine) | **ALL FIXED** — nuanced cap wording everywhere; line numbers corrected; nine+one wording; deps/mermaid unified to the table; EVAL-OS row cites the real keys; all 8 Fusion paths enumerated; `site/turn_watch/` owned; TURN WATCH couplings (Stock-Identity replay registry row, `us_leader_pullback_coverage` private-helper import, 4 production wiring sites) noted for the B5 handoff |
| MAJOR | A1 handoff unilaterally froze another workstream's registered files ("nothing else touches these until you merge") | **FIXED** — §3 rewritten: rescue/liveness files are AVAILABILITY property; A1 executes as its continuation and updates its record in-PR |
| MAJOR | Duplicate repair claim: Fusion PR-1 claims the context-vector accrual fix while the owner map assigns the (correctly-dated) stall to A1 | **FIXED implicitly by §4.2 + noted here**: A1 owns the CURRENT (08-14→) stall; the Fusion record's stale 08-07 claim is flagged to V4-0B reconciliation |
| CHECKS OUT | Fusion `owns_paths` = exactly 8; all 10 cited DEC/DSC keys exist and are content-accurate; all 3 named DNR keys exact and compiler-visible; TURN WATCH orphan claim true; wave count 29 everywhere; `KILL-STAGE-WIN-GATE` respected; citation forms lawful; validate exit 0 | no change needed |

**Combined verdict after remediation:** every BLOCKER/CRITICAL/HIGH from both lanes is dispositioned in this PR's text; the E1 and D3 gates are now explicit preconditions rather than silent assumptions; no frozen decision was reopened.
