# Prophet US — Fable Meta-CEO end-to-end execution handoff

**Revision R6 · September 23, 2026 · Existing program: Prophet US**

**Program mandate:** execute the complete flagship upgrade, not just a review or a first PR. **Intended program accountable owner: Fable Meta-CEO.** **Delegated execution: the existing external fabric only.** **Mandatory return to Astra: none.** The Chairman remains final authority. This document records the current Chairman instruction; it does not manufacture runtime admission, source custody, a receiver acknowledgment, an experiment result, or a filled trade.

**Stable handoff/program operation:** `prophet-us-fable-meta-ceo-20260923-001`.
**Canonical organizational parent:** Macro #6805 / `WS:PROPHET-US-V4-RECOVERY`.
**Delegation and preparation record:** Macro #6805 comment `5793406610`.
**Baseline:** exact R5 packet, SHA-256 `ded2d954327f30ade2430689d11e715c1d1cf292e45e6347c3752d7865025635`.

## 1. The assignment and the change in responsibility

The Chairman has explicitly asked Fable to act as Meta-CEO, own the entire build end to end, use the wide external subagent fabric rather than doing routine work personally, and be the last-resort problem solver for the hardest issues. Built-in/native subagents, including Opus/Sonnet/Haiku child agents, are prohibited for this mission. The prohibition also excludes native internal-agent fallbacks under another model label or a wrapper. It does not prohibit Fable's own reasoning or ordinary deterministic tools such as a shell, compiler, browser, or test runner.

This is a scoped program delegation, not appointment as company Chairman and not an instruction to create a second executive runtime. It replaces the mandatory Astra/Sol program-judgment dependencies in R5. Fable may adopt and refine the plan, decide the remaining bounded questions, commission research, approve designs and code-level plans, resolve technical-owner disagreements, accept independent review, authorize gated release through its incumbent owner, and perform program acceptance. None of those decisions requires another Astra turn.

Astra consultation is optional, never a prerequisite or stop condition. Existing references to an Astra research session, Sol program approval, or an Astra final review in archived R1–R5 are historical routing instructions. For this program, Fable now performs or commissions that responsibility. Independent reviewers still perform independent review. Source owners still own their contracts. Portfolio/Risk still owns actual capital and positions. Current security, budget, custody, rights, and runtime laws continue to apply.

The integrated R5 design is the execution baseline, not an immutable collection of unchangeable hypotheses. Review it once against the current dependencies. Fable can amend an implementation detail, choose a justified research alternative, or reject a failed mechanism without returning to Astra, while recording why and preserving the product job and original evidence. An actual material scope reduction, a new prohibited capability, real-money authority, or new spending outside the approved envelope goes to the Chairman, not Astra.

## 2. The outcome: what the completed product must let the user do

Build one advanced stock-signals and intelligence workspace. The user should be able to move from a market or group change to a suitable security and strategy, understand why the opportunity exists, distinguish research interest from current entry permission, inspect risks and counterevidence, deliberately save or manage the opportunity, and later inspect what Prophet actually knew and how the decision performed.

The first three sleeves are Early Leadership / Sector Rotation, Quality Earnings / Expectation Revision, and Cycle Capture. They share identity, episodes, evidence, prices, publication and account infrastructure; they do not share one universal score or a forced holding rule. The tactical sleeve's 2–15-session field is an incumbent new-entry identity, not permission to impose a 15-session hold on other sleeves. Proposed research horizons remain proposals until registered: EL common-origin H10 with H5/H15 supporting; Earnings H42 with H21/H63; Cycle H252 with H126/H504.

The six product jobs are Action Desk, Early Radar, All Candidates, Themes & Propagation, Track Record, and Health & Receipts. A coherent episode drawer connects them. Watch, Pass/reason, thesis/milestone, existing-position review, alerts and outcome history must mean what their real owner actually stores. A Watch action is not a purchase. A saved ticker is not automatically a saved cloud episode thesis.

A useful initial research release is allowed before predictive promotion. It is not the finished flagship. Do not stop after a fresh board, a working API, a mockup, a single merged PR, or a successful local backtest. Finish the declared capability and scientific work, or retain exact incomplete obligations with their lawful durable owner. Failed scientific hypotheses cannot be made positive by authority. They also cannot silently justify deleting the promised intelligent-selection job: use the retained control, narrow the unsupported claim, and pursue an admissible alternative through the same program.

US is the execution scope. Preserve shared interfaces for CN/HK/CA, but do not start their rollout while US remains incomplete. Real-time options orders, portfolio sizing, leverage and autonomous brokerage execution are not granted by this handoff.

## 3. Read order and document precedence

Start with this handoff, `FIRST_WAVE_RUNBOOK.md`, `DECISION_RESOLUTION_PLAYBOOK.md`, and `CURRENT_CARRIER_FRONTIER.json`. Read `effective/PROPHET_US_MASTER_PLAN_R6.md`, then the effective build and research catalogs. Read relevant R1–R4 sections for a disputed mechanism or methodology, not the entire old chat.

Use the current protected Mastermind procedure as procedure, the live Chairman delegation as current program intent, and current accepted technical contracts as implementation authority. R6 supersedes only the orchestration/decision responsibilities and the explicitly documented activation clarifications. It does not turn a proposed strategy or experiment into an accepted result. `baseline_r5/` is immutable historical evidence; its old resume/routing instructions are not the active command plan. The effective documents are clearly labeled as a prepared execution specification, not a new control plane.

Protected procedure at preparation: Mastermind `bf764f494b9cd0ecede6234bb472c3344c8e77cc`, Skillpack 1.0.1, bootstrap-major 1. Macro preparation snapshot: `fc5c1fc49e4f552d8e3e55b5528a6d280d7415e8`. These are reproducibility pins, not a command to build on a stale base. At pickup and before each modifying phase, fetch protected `master`, record its exact SHA, and load the required same-SHA skills. Read the current target repository's agent instructions. Do not promote this package's copies or an old CLI example into current law.

The package includes an Agent OS decision record ready for owner-validated publication under the existing workstream. Publish/adopt it through the current documentation/source path; do not replace the whole workstream record or invent a new owner enum. Link the existing workstream to the scoped delegation and current execution plan. A documentation PR can record current Chairman intent without making any strategy live.

## 4. Pickup is not an implicit takeover of existing work

A downloaded archive or a GitHub search result does not select a concrete receiver. A deliberate live delivery with execution intent to the actual eligible Fable session supplies the assignment. The supplied `FABLE_START_PROMPT.md` is intended for that event, not for broadcasting to many unbound sessions. The binding mode for this new program handoff is CAPACITY_SELECTABLE before START; existing exact-session children keep their stronger binding requirements.

Upon actual delivery, acknowledge the exact operation and actual session identity. Read the current cumulative record and reconcile any previous pickup/START under this operation. Establish the current runtime/source binding and durable return path using the accepted owner. Emit a separate truthful START only after its gates clear. Do not demand another Astra approval or redundant Chairman claim after a deliberate live assignment. Do not fabricate a machine lifecycle state that the installed schema does not expose.

First reconcile existing PR writers, reviewers, and any effect uncertainty. Being Meta-CEO makes Fable accountable for resolving the dependency; it does not seize a worker's dirty worktree, cancel a running attempt, or erase a refused result. An existing PR with no known START is not necessarily safe to replace: verify its original carrier. An EFFECT_UNKNOWN modification remains on its original operation and transport until resolved. No blind resubmission with a new operation key and no provider/account failover.

The preparation session did not start Fable or any child. It did not reserve a concrete provider account, discover all running workers, or certify current quota. The three refreshed PR heads are navigation facts only. At action time, native head/tree/check/review receipts and Runtime/source custody take precedence over prose in a PR body.

## 5. External-fabric-only delegation: enforce it operationally

Fable is the CEO orchestrator, not the default implementer. For every bounded implementation, extraction, quantitative replication, test repair, documentation, or review task, first use the least-scarce capable **eligible external-fabric** route. Prefer the current registered standard engineering avenue for ordinary work and an eligible harder specialist for difficult bounded work. Capability role is not a hardcoded provider slug. Current Capacity/Model Router chooses among routes that actually exist and satisfy the exclusion.

Do not use Claude's native Task/Agent/Agent Teams spawning to create Opus, Sonnet or Haiku workers. Do not use internal Codex subagents as an automatic fallback. Do not ask an external worker to tunnel the task into built-in subagents. Every descendant inherits the external-only restriction. Existing generic skills that recommend native subagent-driven execution are subordinate to this mission-specific restriction; preserve their test and review discipline through the external fabric instead.

The external-fabric architecture reference at the preparation pin describes the existing Executive five-tool ingress (`executive_state`, `executive_inbox`, `executive_job`, `ceo_intent_status`, `submit_ceo_intent`). It is a source reference, not proof those tools are mounted or production-ready on Fable's host. Inspect the actual current registered surface and its current accepted schema. Do not invent `fabric.spawn`, a provider socket call, credentials, or extra submit parameters to implement this instruction.

Before a child effect, the canonical placement/admission evidence must establish a compatible external route, current account/host eligibility, permitted source grant, budget, and return binding. Where the existing request schema has a supported route restriction, use it. Where it does not, obtain the restriction through the existing eligibility/placement owner and verify the resulting route. A prose flag in this packet alone is not runtime enforcement. Refuse to start the child when external-only placement cannot be proved; do not convert that into an internal fallback.

If one route is unavailable, inspect another already-approved external route before concluding the fabric is unavailable. If the supported ingress is absent, use another currently approved owner-native/manual route only when its law and effect state permit it. Never create a second queue. If all eligible external routes are unavailable, freeze those child lanes, preserve their ready work, and advance principal-owned judgment, review or source-reading that is safe. Restore the existing owner or raise the precise authorization/capacity issue. Do not serially implement the whole program as an excuse for a broken delegation path.

### Fable's direct-work exception

Fable may personally solve a bounded hardest residual problem after identifying why qualified external specialists cannot resolve it, or when the decision intrinsically belongs to the principal. Record the specific unresolved question, prior distinct attempts or unique principal requirement, exact source/operation, permitted direct effect, stop condition, and independent review requirement. Two equivalent failed loops call for a different hypothesis, worker capability or principal intervention—not endless retries. Once the hard uncertainty is resolved, return routine work to the external fabric.

An urgent integrity incident may require the existing release owner's fail-closed containment before a delegate can act. That is not a waiver to trade, seize another writer, or invent a recovery control plane. Direct code authored by Fable still needs a genuine non-author review before acceptance when required.

### Review and concurrency discipline

Select reviewers by capability and independence, not by a matching model label. A reviewer must not be the author or the same substantive reasoning context repackaged as a different identity. Cross-provider review is useful when permitted, but is not a universal requirement where a qualified independent route exists.

Limit fanout by ready path-disjoint work, admitted global capacity, approved expenditure including repair, and the ability to consume returned evidence. Reserve review capacity before opening more builders. No fixed invented global worker count is a grant. Do not keep a Fable session polling unchanged CI; use the existing durable run/return mechanisms and advance another independent decision while evidence accrues.

## 6. The first operating cycle

Run local archive integrity verification with `python verify_handoff.py`. It needs only Python's standard library, writes no product state, performs no network calls, and does not run market experiments. Read the report's scope; a PASS verifies the handoff, not Prophet. If the archive or a baseline digest differs, reconcile that artifact rather than silently using a partly reconstructed plan.

Recover only the current sources for the chosen first unit. Inspect protected procedure, the canonical workstream/delegation, and native state for #7180, #7572 and #7581. Do not recensus every project. Fetch a broader related PR only when a dependency in the actual first unit requires it. Confirm the concrete source-custody and review carrier before editing.

Have an independent external reviewer assess the R6 delegation/architecture delta and highest-risk scientific seams. Fable consumes the review, resolves findings through D01–D12 and adopts bounded scope. This is not a request for an external worker to redesign the company. Safe already-authorized incumbent work continues under its existing scope; a documentation review does not retroactively block previously accepted work.

Publish the exact adopted source plan and Agent OS record through the existing owning documentation process. Preserve `baseline_r5/` unchanged, use a normal reviewed branch, and record accepted commit/digests. Do not write directly to protected main merely because a contents API is callable. The plan's source adoption and source-worker grants are different effects. If this prepared handoff already has a source carrier, reuse it rather than create a second one.

Then bind the first ready work: source validity on #7581; completed-session/publication and candidate visibility on #7180/#7572; or path-disjoint Earnings/D5/source readiness. `FIRST_WAVE_RUNBOOK.md` specifies the exact starting questions, source paths, negative cases, review and natural-proof obligations. A source-interface decision that enables a concrete integration is progress; repeating the entire architecture census is not.

## 7. End-to-end program order and branch-level readiness

The effective B00–B28 catalog preserves all 29 units, their dependencies and required outcomes. The associated work cards map every unit to the original requirement and acceptance evidence. These are work specifications, not 29 already-admitted runtime jobs. Each receives an actual child identity from the existing owner when dispatched.

The four paths are source/truth delivery, scientific research, strategy mechanics, and product/release. Advance them in parallel only when dependencies and write ownership permit. The early product can show a complete research view with an honest B4 refusal before models are promoted. A model artifact can be developed in shadow before its UI is released. Neither grants the other's authority.

Do not overinterpret aggregate build dependencies. B03 can finish a current lossless board-pool view without waiting for every new B1 anchor species; it must keep board rows and canonical episodes distinct. B04 can expose a source-bound partial dossier without waiting for optional families. B06 historical board-row research uses its original grain and can progress without newly added anchors or live B4; an episode-policy claim does require genuine B1 identity, and prospective B4 capture retains all of its own live gates. B07 may deliver a research-to-watch journey showing a truthful unavailable verdict; it must not imply that entry mechanics are accepted. B11's offline challenger research does not wait for a customer interface; its live rank promotion does.

These clarifications remove false global stalls. They do not weaken source quality, identity, entry, entitlement or promotion requirements. The effective activation catalog records separate research/deployed/promotion branches. When a further false dependency is discovered, Fable records the exact branch semantics before changing it; it never deletes a gate merely to reach a desired outcome.

Every build ends with an observable user/machine capability, not only plumbing. The final dependency chain includes all three sleeve dispositions, full UI, durable user actions, alerts, honest measurement, reliability, portfolio context, independent review, actual release, ordinary scheduled refresh and safe rollback. Deferred additional sleeves stay in the roadmap with explicit research results and next criteria; they do not disappear silently.

## 8. What must be present in every dispatched work packet

Use the installed capability-package/worker-brief producer, not a newly invented runtime compiler. Supply the smallest sufficient packet: parent/unit purpose; exact source/head/tree and accepted interface decision; existing operation/PR and ownership; write allowlist and read dependencies; expected input/output semantics; complete positive and negative user journey; test and real-proof requirements; budget and retry terms supplied by the existing owner; external-only route restriction; reviewer exclusion; stop condition; and exact return target.

The per-unit work cards are ready inputs to that producer. They are not executed by running a shell loop. An executor must bind current runtime IDs, actual permitted files and current accepted signatures from the source before modifying. Unknown runtime identities are not placeholders to guess. The packet owns the procedure for resolving them; it does not claim they already exist.

Do not give every worker the full research archive. A worker gets its card, referenced evidence sections and accepted dependency artifacts. It reads relevant current source itself. The result should contain exact operation/attempt/head/digest, capability changed, tests actually run, separate review and live-proof status, unresolveds and next action. A full conversation or shell transcript is not a normal return.

Before ending a child, preserve command-backed source continuity on its existing carrier. A commit, push or CI result is not completion of a user-facing task. On return, Fable should decide ACCEPT/REPAIR/HOLD/REJECT within the existing dialogue semantics and name the evidence. Never manufacture an Executive enum from this document. For a terminal child, explicitly close the dialogue and disarm its temporary wait mechanism through the current owner.

## 9. Research execution without an Astra dependency

Fable owns Q01–Q24. External quantitative specialists may audit source histories, implement analysis, reproduce papers and evaluate registered experiments. Fable retains mechanism selection, the strongest counterargument, interpretation of conflicts, and final scientific judgment. This preserves the earlier concern about weak delegated research without retaining another CEO as a required bottleneck.

Research must produce one of: supported bounded conclusion; rejected mechanism; justified narrower alternative; or specifically unidentifiable/inconclusive result with the next data/test needed. A formatted report with no decision is not enough. An important source or new method may change the research plan, but not retroactively the frozen held-out test. Reuse and cite primary research, lawful data, and original implementation. Do not copy proprietary code, assets, corpora or branding.

Before any formal outcome read, close the applicable D03/D06/D07/D08 decisions. Freeze the exact population, evidence vintages, label grain, entry/exit/cost conventions, primary and supporting endpoints, comparison count, economic margins, uncertainty method and formal-read rules. The existing #7751 study has its own registration and no-backfill rules; eight labels were previously found to represent six unique configurations. Do not silently substitute thresholds or count aliases as independent N.

Historical or current data access is not proof of point-in-time suitability. Preserve observed-as-run, lawful replay and retrospective diagnostic labels. Delistings, missing prices, no entry and administrative censoring are different. Fixed-horizon source grades remain immutable. A later source correction may change current knowledge without changing the earlier decision record.

The original and current-landmark forecast problems remain separate. Model learning, member selection, entry timing and management must not take credit for each other's change. Use competitive transparent baselines and admissible nonlinear/hierarchical/sequence/graph challengers on common populations. A rank relevance output is not a probability. A payoff distribution is not current permission to enter. A long-term narrative is not a reason to rewrite a losing tactical trade as an investment.

Research may need long natural accrual. Keep it on the existing durable evaluation owner with a real return path and advance useful independent product/science work. Never simulate elapsed market time, hide insufficient information, claim the project finished because data are accruing, or return to Astra simply to wait.

## 10. Decision closure and principal escalation

All twelve originally open decisions now have Fable as accountable decision authority within this delegated program. `DECISION_RESOLUTION_PLAYBOOK.md` supplies the specific evidence, owner consultation, permissible choices, ordered resolution method, output and blocked consumers. It is not a second authority registry. Adopt decisions through existing source/document owners and record their exact version.

Fable can choose implementation sequencing, technical interfaces within owner boundaries, research baselines, future policy versions, experimental economic margins, design directions, task assignments through Capacity, and staged product claims supported by evidence. It can accept or reject an exact reviewed change and request the normal release owner to deploy. It cannot grant itself credentials, expand a real-money risk limit, alter protected source without its gate, override a court or contract, or treat untested science as true.

The escalation chain is external worker → appropriate external specialist/reviewer → Fable principal. There is no obligatory Astra stage. Direct Chairman escalation is reserved for an actual human ceremony, unavailable permission requiring its owner, new purchase/legal commitment beyond the current envelope, materially expanded or deleted product scope, or a reserved account/real-money act. Present one exact decision, evidence, recommended choice and impact; do not ask the Chairman to do routine debugging, select accounts, or relay worker messages.

A technical failure, inconvenient API, uncertain method, or difficult integration is normally Fable's problem to resolve, not a human gate. A refusal from a tool is not permission to try a hidden alternate credential path. Probe the needed action class once per stable connection and preserve the exact result; changes in resource, permission or source can justify re-probing.

## 11. Quality gates from source to served product

Required proof has five layers. Document integrity verifies that the specification is complete and coherent. Source-level tests discriminate positive and negative behavior. Integration tests run the real owner chain on representative inputs. Browser tests use actual entitled and unentitled paths, not a simulated callback passed off as a login. An ordinary subsequent refresh proves the feature survives the real scheduled path, with current versions and no stale false-green fallback.

Every user-facing change includes desktop/mobile, dark/light and English/Chinese views, keyboard and focus behavior, missing/empty/degraded/invalidated states, and private-data isolation. Use the existing design system and canonical capture producer. Visual taste review is not replaced by a no-console-errors screenshot. The exact feature flag or release control must come from the incumbent owner, not a new flag system.

The first useful capability should be tested with actual saved-state readback. A successful optimistic click is insufficient. Watchlist membership and portfolio holdings must remain separate; local-only notes must not be advertised as cloud thesis persistence. Sources, scores, Availability and plan state must bind coherent generations, including same-date corrections.

Promote model, entry policy, hold policy and interface independently under their accepted evidence. Release receipts bind source commit, model/dataset/calibration versions where applicable, policy definition, emitted artifacts, actual served versions and user proof. Old body text or green CI on an ancestor is not acceptance of a newer head. No pipeline step concluding SUCCESS proves that an unrelated later publisher consumed its output.

Rollback withdraws only the changed authority/view through existing release control. Preserve historical decisions, grades, user state and incumbent operation identities. Never restore a cached old ENTRY_OPEN because it was once green. Reevaluate mandatory live facts; if they are unavailable, show the honest refusal. Irreversible migration requires a reversible staged plan or separately authorized recovery procedure before execution.

## 12. Long-running orchestration and continuity

Fable keeps a compact frontier: mission; delegated authority; current adopted source/spec; latest accepted effects; pending effects; active children/returns; exact blockers; DO_NOT_REDO; next ready unit. Agent OS carries durable organizational continuity. Runtime carries attempts and placement. GitHub carries exact source/test/review evidence. Linear is projection and Slack is transport. Do not mirror all these into a homemade status database.

Checkpoint after material decisions, merges, production proofs or delegation changes, and before slow/effectful work. Use an existing cumulative handoff/record, not an ever-growing chain that requires the next session to read all previous chats. On context rotation, preserve actual source custody and unknown effects. New context is not a writer release. An observed repeated session failure or Thinking failed requires safe reconciliation, durable checkpoint, sealing the affected chat and continuation on a freshly admitted surface.

At natural waits, use actual durable execution, monitoring and return owners. Do not keep generating to occupy a time target. A running child requires positive START/RUNNING proof and a lawful return route; a planned dispatch or QUEUED receipt is not enough. When a return arrives, consume it and advance its unlocked dependency rather than wait for all siblings.

No-delta rule: after two equivalent cycles without a useful result or resolved uncertainty, change hypothesis, route, lane or owner. Rewording the plan, minting a new commit with unchanged behavior, or retrying identical tests does not reset this rule. Shared CI starvation goes to its infrastructure owner; do not invent a second runner/queue.

## 13. Completion and honest launch claims

Fable can perform final **program** acceptance without returning to Astra. Before doing so, prove the advertised user tasks on the real path; the three sleeve capabilities at their declared authority; source and model traceability; actual user decision persistence; controlled new-entry versus holding behavior; an honest complete record; measured reliability; independent review; normal refresh; and safe rollback.

Negative scientific findings are accepted research outcomes, not positive product evidence. An unpromoted sleeve may be shipped as clearly labeled research where it is useful, but the overall mission cannot be called fully achieved if a promised intelligent selection/entry capability is merely absent. Record the remaining job and continue the best admissible route. Explicit Chairman scope change is different from silent omission.

No discretionary portfolio trade is part of this acceptance. Even a promoted signal system does not gain authority to originate or size real trades by generating compelling text. Final communication should state what works, exact proof, limitations, pending natural evidence, and why the declared scope is or is not complete.

## 14. What this preparation actually established

The exact R5 archive and its 265 local integrity checks were reverified. The R6 package preserves that baseline, supplies an effective Fable-governed master, remaps all 29 build units / 24 research packets / 12 decisions, and retains all 126 future acceptance objects. It adds handoff execution and route-safety acceptance requirements without marking product tests passed.

Current bounded native metadata reads found #7581 Draft at `39ef2cd48e091d90f12771aab142c2197022aa79`, #7180 Draft at `a991d4d22a933ca8953a08260352cc4b30e2ee8d`, and #7572 Draft at `5e43db462b5ffa4912874c0be32549f8e26baeda`. These reads did not prove writer idleness, CI on those heads, independent approval or deployed behavior. Other carrier statuses in R5 remain explicitly dated navigation.

GitHub source-write actions were discovered in this preparation, but no repository source mutation or independent source-custody boot was executed. There is no claim that Extra High intrinsically grants all write classes. Comment creation/readback was actually exercised. The self-contained canonical checkpoint records the delegation; the included full source package and Agent OS draft still require owner-validated source adoption under B00.

Fable has not been sent this packet through a selected native session or acknowledged it at preparation time. The intended next event is deliberate delivery of `FABLE_START_PROMPT.md` plus the full archive to one eligible Fable session. The exact current Chairman delegation removes the need for another Astra round; runtime and existing source gates remain real.
