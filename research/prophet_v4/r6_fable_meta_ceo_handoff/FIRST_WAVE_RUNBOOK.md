# First wave — exact next work, not another broad audit

This is Fable's first operating cycle after a deliberate live assignment. It is subordinate to current protected procedure and the R6 scoped delegation. Each child uses only the existing external fabric. All existing source/runtime operations stay on their original carrier until reconciled. No worker is started by this file.

## Phase 0 — verify, bind, and adopt once

1. Run `python verify_handoff.py` at the extracted root. Read the report. Stop artifact adoption on a mismatch; preserve the received bytes and reconcile against the canonical R6 comment and manifest rather than reconstructing by memory.
2. Acknowledge `prophet-us-fable-meta-ceo-20260923-001` with actual receiver identity. Confirm whether this operation already has a pickup/START. If another receiver may have begun, reconcile before changing binding.
3. Read current protected `docs/sol_skills/INDEX.md`, pin exact SHA, and load required skills and universal companions from that same SHA. Read current Macro/other affected repository agent instructions. The preparation source pins are historical evidence only.
4. Read `WS:PROPHET-US-V4-RECOVERY`, its latest relevant continuation, the exact #6805 R6 delegation record, and native heads/state for the first needed carriers. Do not read every old thread or recensus unrelated projects.
5. Discover current supported external-fabric ingress, placement constraints, source grants and result-return mechanisms. Verify the selected child route is actually external before any effect. A installed CLI/provider profile or source-design diagram is not proof of runtime admission.
6. Have a qualified independent external reviewer check the R6 delta and first-wave interfaces. Fable adjudicates findings and adopts the executable baseline. Existing already-authorized work need not await a new review of unchanged old science; changed scope does.
7. Publish the adopted exact packet and included Agent OS record through the existing source owner and a reviewed documentation carrier. Preserve the old R5 bytes. Record the accepted version and point current workstream continuation at it; do not replace a schema owner or invent a second program key.
8. Prepare the first eligible child packet from A/B/C below. Supply its current head, custody, permitted files, exact evidence expectations, external-only restriction and reviewer exclusion. Establish a real durable return, then emit START only under the current owner gates.

A problem in one step blocks only the action that needs it. Safe read-only source reconciliation, evidence analysis, or preparation of a separate path can continue. A source deny or EFFECT_UNKNOWN does not authorize another carrier or credential fallback.

## Packet A — source-session validity into live B4

**Existing carrier:** Macro #7581. **Preparation head:** `39ef2cd48e091d90f12771aab142c2197022aa79` (Draft); refresh before action. **Existing finding:** comment `5789777976`. **Requirements:** D01/D04/D08/D10; B01/B02/B05 as relevant. This is source/entry integration, not a new trading signal.

**Read scope:** `engine/prophet_entry_availability_sources.py` on the exact PR head; `engine/prophet_entry_availability.py`; `engine/prophet_entry_policy.py`; `engine/prophet_strategy_definition.py`; the current `build_stock_library` signal-gate writer and native emit contract; the existing quote/basis owners. Inspect only the dependent #7584/#7734/#7738/#7751 when the next fact requires them. Their current state is not assumed from R5.

**Known issue, not a new production outage claim:** the characterized adapter required confluence `as_of` to equal the live decision market session and emission to precede the decision. Last-completed-session evidence and a current RTH quote therefore did not compose in that function. R3 tested only an extracted function with explicit helper stubs, not the full deployed path. Do not re-run that isolated probe merely to create activity.

**First discriminating investigation:** obtain the real producer emission/scheduling evidence. Does a current-session qualified confluence receipt genuinely exist before a normal intraday decision? If yes, prove its current-source path and retain the rule. If no, have the incumbent confluence/policy owner define the source-date, known/emitted-at, valid-for interval, expiry and revocation contract for last-completed-session evidence. Fable chooses and records the evidence-supported integration. No source date relabeling or naked check deletion.

**Allowed implementation:** the narrow accepted validity/owner-fact composition, regression tests and necessary source contract documentation, on the incumbent operation. Changes to shared source/policy files require their source ownership reconciled explicitly. Do not bundle extra ranking features, changed technical thresholds, generic injected gate facts or a second adapter.

**Required discriminating cases:** genuine same-session predecision positive receipt; valid prior completed-session receipt under an explicit policy; expired prior evidence; late emission; source retraction; missing/noneligible compact source remaining UNKNOWN where negative proof is absent; calendar holiday, shortened close and out-of-era behavior; unresolved identity/basis; source health failure; risk/liquidity/gap requirements not yet positive; exact runtime-adapter-to-B4 response. Do not test by forcing a named stock into ENTRY_OPEN.

**Review and release:** reuse the existing review operation after state reconciliation, rebind pre-START only when lawful, and obtain a genuine non-author exact-head verdict. Current `mergeable=false` metadata is not itself a proven source conflict; verify the actual pair. Source changes invalidate their affected evidence, not every unrelated program. Follow ordinary hosted tests, current-base integration, source acceptance, release and actual RTH proof. Until then show the correct experimental/unavailable state.

**Complete A when:** the accepted source-owner chain expresses the intended source/decision validity without falsifying timestamps or bypassing a gate, and the required real runtime/normal-session proof exists. An isolated unit test or a semantic doc alone is not that completion.

## Packet B — complete, coherent candidate-to-watch journey

**Existing carriers:** #7180 and #7572, kept separate. **Preparation heads:** `a991d4d22a933ca8953a08260352cc4b30e2ee8d` and `5e43db462b5ffa4912874c0be32549f8e26baeda` (both Draft). Their bodies include older qualification heads; do not promote those proofs to the current heads.

**Read scope:** the actual changed files on each PR; `scripts/build_stock_library.py`; `scripts/build_prophet.py`; `engine/us_candidate_lanes.py`; `scripts/build_site.py`; current Prophet templates and premium payload split; existing `app/prophet_lab.py`; `templates/watchstore.js`; `WATCHLIST.md`; current capture/evidence producers. Source paths are routing hints, not a write allowlist: bind the exact current touched paths in the admitted child.

**First action:** reconcile the original writer/review operation, current exact diff and source freshness contract. Preserve the candidate-pool and board bytes as producer-owned inputs. A real unscored cap-displaced candidate is a valid positive UI case; never invent a score or B1 identity to make its drawer look complete.

**Required journey:** accepted owed-session input → ranked-board/candidate-pool exact bytes → private projection and coherent same-date corrections → actual entitled browser search/filter → understandable displacement/refusal/missingness → deliberate selected-list Watch → actual owner readback. No portfolio position changes as a side effect.

**Required negative cases:** anonymous and unentitled requests disclose only the intended preview, not a DOM-hidden full list; partial protected tail remains a declared partial result; wrong-generation hydration does not merge; same-date changed content updates; duplicate hydration does not duplicate rows; zero/no-match results remain truthful; unknown B1 mapping is not guessed; stale mandatory facts suppress current entry permission; optimistic save failure is not success; list/account changes cannot affect another list/account; local-only notes are not advertised as durable cloud thesis.

**Visual proof:** real desktop/mobile × dark/light × English/Chinese, keyboard search/expansion and preserved focus/selection. Use the existing design system and capture producer. A simulated authorized callback is component proof only. Do not count it as production login.

**Release:** source-bound publication and visibility can complete independently where their real contracts allow. A held new-anchor expansion must not delay existing board-pool search; it does prevent false claims about a complete canonical episode population. Research UI may show an accepted unavailable B4 result without suggesting entry is promoted. Preserve exact producer, served and actual user-action receipts through the next ordinary refresh.

**Complete B when:** a real entitled user can find and understand a previously displaced/blocked opportunity and save to the intended list, on accepted current source bytes, with all negative/private-state cases and ordinary refresh satisfied.

## Packet C — useful Earnings/D5 and Cycle evidence, without waiting for a final model

**Existing owners:** Earnings Intelligence / Filing Forensics, D5, GMI/Group Reads, Data OS, Evaluation and private product delivery. Use the current `CONTRACT_AND_OWNER_MAP.md` and original workstreams. Do not create a new research warehouse or data provider.

**Read scope:** `engine/prophet_lab/intelligence_vector.py`; `engine/company_intelligence/event_workspace.py`; `engine/neuralweb/company_intelligence_reader.py`; the accepted D5 amendments and Earnings contracts; actual financial-source and industry-series owners. Use the source version required by the first real consumer.

**Known limits to preserve:** D5's inspected allowlist was revenue delta, revenue fact and revenue-growth guidance; current event discovery was not a complete historical event population. The source is not already an EPS, margin, cash-flow or contributor-revision engine. Current strategy definitions do not silently accept Earnings/Cycle labels. New source/strategy semantics must be versioned through their owners.

**First bounded outcomes:** one real issuer-event dossier with compatible observed quantities and typed absent expectations, and one real source-backed funding/per-share scenario whose financing assumptions are explicit. An unlicensed consensus branch can remain held while real issuer facts are usable. Select the Cycle readiness domain by actual source/mapping/coverage quality before strategy returns. Broad industrial/electronics proxies must not be renamed as granular subtheme data.

**Evidence requirements:** exact issuer/security/event, fiscal period, GAAP/adjusted basis, currency/units, source span/revision and publication/observation clocks; current versus original belief; contributor composition and horizon rollover when estimates exist; sector economics → issuer funding schedule → original-share value. Preserve failed firms and canceled equity in research eligibility. No guessed beat/miss or survival probability.

**Proof:** execute the real owner adapter, not a pasted fixture as the only consumer. Show missing, corrected, late-observed and conflicting-basis cases. Obtain the actual private user-visible dossier. Forecasts, entries and model promotion remain separate later capabilities. No protected outcomes need to be opened to prove this first source-backed utility.

**Complete C when:** a real user can understand comparable operating change or a financing/per-share risk through the existing evidence path, with provenance and limitations, without fabricated model confidence or an unauthorized strategy state.

## After the first three packets

Use the full effective B00–B28 graph and Q01–Q24 docket. Do not stop at A/B/C. Maintain the 36 requirements, all 126 inherited acceptance entries and the added R6 handoff/operating cases. Bind any new source unit only when its predecessor evidence and current custody permit; route routine tasks externally, resolve actual hard questions as Fable, and continue through all three sleeves, full interface, registered science, alerts/actions, truthful measurement, reliability, final review, cutover and ordinary refresh.

An existing running or uncertain child never becomes safe to duplicate because this runbook lists its subject. A returned child receives an explicit disposition and continuation/terminal edge through the existing dialogue owner; accepted work is DO_NOT_REDO absent material invalidation.
