# B02 — Canonical episode registry census (read-only)

Lane `pu_b02_episodes` · Prophet US R6 wave-1 · operation `prophet-us-fable-meta-ceo-20260923-001` (Macro #6805). Read-only census of the CURRENT state of `prophet.candidate_episode/v1` (owner B1) and of what "qualify extra anchors" (R6 D02) would actually require, so the seat can sequence B02 → B03/B04 without re-deriving B1.

**SOURCE_SHA:** `aba2c115bafa7557bcee07ea7f64f262648f593f` (detached HEAD == origin/main, `git rev-parse HEAD`). Citations `path:line@aba2c115bafa` are main. R6 work-card/register citations use `@ab8c72c2afd0` = head of `claude/prophet-us-r6-adoption-records` (see BLOCKED-1: the branch the spec named does not exist). Worktree is SPARSE (`/*`, `!/data/`, `!/site/`, `!/mockups/`, `!/verify_shots/`); no `data/` blob was read or materialized, so store contents are UNKNOWN where noted. No engine/, scripts/, tests/, templates/, .github/ or data file was modified — this record is the only write. Labels: OBSERVED (ran/read) · INFERRED (reasoned from observed) · UNKNOWN (missing artifact named).

## Q1 — Episode identity, producer, path, and the surrogate ban

**OBSERVED — the key.** `episode_id()` mints `pe:<security_id>:<identity_epoch>:<anchor_token>:<generation>` (engine/us_candidate_episode.py:218-225@aba2c115bafa). Four fields, no ticker, no date: (1) `security_id` — the exact Data OS `SEC:` value, `_security_id` delegating to the Data OS parser (engine/us_candidate_episode.py:171-174@aba2c115bafa); (2) `identity_epoch` — non-empty string, today always `epoch_0`, explicitly `provisional`, bound to `stock_identity.fingerprint_spec.v1` and its exact spec hash (engine/us_candidate_episode.py:47-48,179-188@aba2c115bafa); (3) `anchor_token` = `"sa:" + sha256(canonical_json(canonical_anchor(anchor)))[:24]` (engine/us_candidate_episode.py:214-215@aba2c115bafa), where `canonical_anchor` is exactly `{kind, time, price, basis}` and deliberately EXCLUDES `source_receipt` from identity (engine/us_candidate_episode.py:195-212@aba2c115bafa); (4) `generation` — canonical positive integer, incremented only on a legal re-arm (engine/us_candidate_episode.py:220-222,919@aba2c115bafa). `_parse_episode_id` re-validates the envelope: 7 colon parts, `parts[0]=="pe"`, `parts[1]=="SEC"`, `parts[4]=="sa"`, 24-char lowercase-hex digest, canonical positive generation (engine/us_candidate_episode.py:303-321@aba2c115bafa). The projected row carries 24 fields (engine/us_candidate_episode.py:382-441@aba2c115bafa); `ticker_at_observation` is required provenance/display but never part of the key.

**OBSERVED — the producer that mints it.** One producer: the OPENED branch of `reconcile_observations` (engine/us_candidate_episode.py:912-951@aba2c115bafa), reached only from `scripts/reconcile_us_candidate_episodes.py` via `_load_and_build` (scripts/reconcile_us_candidate_episodes.py:403-459@aba2c115bafa) → `reconcile()` (:624-644) → `_publish_transaction` (one immutable generation + one atomic `HEAD.json` replacement, :593-622). It is the sole durable writer: schedule-only DAG step `reconcile_us_candidate_episodes` in job `us_prophet_ledgers`, after Door emission and before every forward grader/W3 (config/dag.yml:1798-1801@aba2c115bafa; pinned by tests/test_us_candidate_episode_wiring.py:138,151,161,175@aba2c115bafa). Durable writes refuse without the nightly lane gate (scripts/reconcile_us_candidate_episodes.py:626-628@aba2c115bafa).

**OBSERVED — intake / reconcile path.** `_load_intakes` reads exactly four sources, once each (scripts/reconcile_us_candidate_episodes.py:128-147@aba2c115bafa): `turn_watch_observations` (latest `data/us_prophet_rank/episode_inputs/turn_watch/*.json`), `candidate_observations` (`data/us_prophet_rank/candidates/*.parquet`), `door_observations` (`data/prophet_doors/flags.jsonl`), `radar_observations` (`data/entry_radar/forward.parquet`). Then intake → `reconcile_observations` → `apply_commands` (corrections/retractions/supersession) → `build_all_candidates` → validated generation → atomic HEAD swap. Source accounting must balance (`input == mapped + suppressed`) or the run aborts (scripts/reconcile_us_candidate_episodes.py:429-432@aba2c115bafa).

**OBSERVED — the rule that forbids ticker/date surrogate episodes.** Operative code line: an observation with no anchor can never open an episode — `if active is None and canonical is None: → MISSING_STRUCTURAL_ANCHOR` (engine/us_candidate_episode.py:907-911@aba2c115bafa); since `episode_id()` requires an anchor token (:218-225), a ticker/date row has no construction path to an identity at all, and may only ATTACH to an already-ACTIVE episode (:953-980) or be suppressed. Reinforced at read time three times: `_row_from_open` recomputes the id from identity+anchor and refuses a mismatch (engine/us_candidate_episode.py:404-406@aba2c115bafa), as do the row validator (:465-466) and the sole projection reader (:1077-1078). WS `do_not_redo`: "Runtime D5 requires the owner-issued canonical prophet.candidate_episode/v1 from B1. **Never alias mastermind.live_entry_episode.v1 or mint ticker/date surrogate episodes.**" (agentos/workstreams/WS-PROPHET-US-V4-RECOVERY.md:104-105@aba2c115bafa), mirrored at agentos/handoffs/PROPHET-US-V4-RECOVERY-2026-08-28-b1-acceptance.md:67@aba2c115bafa. Decision: `DEC:PROPHET-B1-CANONICAL-EPISODE-BINDINGS` R1 (exact Data OS SEC/ISS, no Prophet-local identifier) at agentos/decisions/DEC-PROPHET-B1-CANONICAL-EPISODE-BINDINGS.md:9-11@aba2c115bafa, with the rejected alternatives "Key episodes by ticker/date or by an expert detector identifier" (:48-53) and "Let candidate snapshots, Doors, or unanchored Radar open episodes" (:58-62). R6 restates it twice: B02 "Release Limit: No surrogate ticker/date episode, replay-as-live promotion or repair by reopening an immutable ordinary source key" (work_cards/B02.md@ab8c72c2afd0) and D02 `forbidden_shortcut: "Ticker/date surrogate episodes, future extrema, silent retries of immutable source keys, or parallel lifecycle store."` (effective/DECISION_REGISTER.json D02@ab8c72c2afd0).

## Q2 — Anchor species today

**OBSERVED — exactly ONE species can open an episode: `turn_watch_reset_low`.** `turn_watch_observations` says so in its own docstring: "only this source provides a structural anchor" (engine/us_candidate_episode_intake.py:177-178@aba2c115bafa). It builds `{"kind":"turn_watch_reset_low","time":<reset_low_date close>,"price":<reset_low>,"basis":"adjusted_close","source_receipt":<turn_watch document digest>}` (engine/us_candidate_episode_intake.py:252-253@aba2c115bafa) only after: schema + `content_sha256` receipt match (:196-213), at least one `triggers[*].fired` (:220-224 → `MISSING_TRIGGER`), every fired trigger `evaluated` (:225-229 → `UNEVALUATED_TRIGGER`), a derivable trigger clock (:230-241), and a complete `reset.reset_low` + `reset_low_date` (:243-247 → `MISSING_RESET_LOW`). `intake_class` is `"technical_emergence"` (:256).

**OBSERVED — every other nomination source is attach-only (`anchor=None`).** All three pass through `_unanchored_batch`, which hard-codes `anchor=None` (engine/us_candidate_episode_intake.py:308-346, esp. :333@aba2c115bafa):

| Nomination source (code path) | Schema | Source key | Opens an episode? |
|---|---|---|---|
| `turn_watch_observations` (intake:177-269) | `prophet.candidate_episode_input.turn_watch/v1` | content hash of `{data_session,row}` (intake:122-123,217) | **YES** — `turn_watch_reset_low` |
| `candidate_observations` (intake:348-366) | `us_prophet_rank.candidates/v1` | `candidate:{stamp_date}:{ticker}:{board_definition}` (intake:361-363) | no |
| `door_observations` (intake:368-382) | `prophet_doors/v1` | `doors:{date}:{door}:{ticker}` (intake:377-379) | no |
| `radar_observations` (intake:384-392) | `mastermind.entry_event.v1` | `episode_address` (intake:390) | no — attaches as `EXPERT_EVENT_ATTACHED` |

Attach semantics: `EXPERT_EVENT_ATTACHED` when `expert_event_id` is present, else `OBSERVED` (engine/us_candidate_episode.py:953-980@aba2c115bafa). Expert attachment is hard-pinned to Radar — `source_system=="entry_radar"` AND `source_schema=="mastermind.entry_event.v1"` AND `expert == source_event_id`, else `EpisodeContractError` (engine/us_candidate_episode.py:963-971@aba2c115bafa; retry path :776-797).

**OBSERVED — R6 "extra expert anchors" (D02) with NO anchor intake today.** (a) **Entry Radar experts G0/C1–C5** (`mastermind.entry_event.v1`; engine/entry_radar/entry_events.py:62,720-761@aba2c115bafa, plus `g0_adapter.py`, `c5_adapter.py`, `detectors.py`): a *nomination* intake exists but is attach-only and there is **no structural anchor producer**; its store is also absent from main — `git ls-tree -r --name-only HEAD data/entry_radar` returns only `data/entry_radar/ledger_state.json`, no `forward.parquet`, so `radar_observations` degrades to `MISSING_SOURCE_FILE` (engine/us_candidate_episode_intake.py:385-386@aba2c115bafa). Registry row is `status: PROPOSED`, `STAGED_NOT_ARMED` (config/dataset_registry.yml:696-728@aba2c115bafa). (b) **Radar runtime episode ledger `mastermind.live_entry_episode.v1`** (engine/entry_radar/live_ledger.py:91@aba2c115bafa): **no intake of any kind**, and aliasing it into B1 is forbidden (WS:104-105; research/prophet_v4/D1_D5_READINESS_RULING.md:35@aba2c115bafa). (c) **Doors T/R (re-arm)**: intake exists, no anchor; DEC R4 rules "Door R is not B1 re-arm authority" (agentos/decisions/DEC-PROPHET-B1-CANONICAL-EPISODE-BINDINGS.md:18-22@aba2c115bafa). (d) **Context Vector candidates**: intake exists, no anchor; rejected as an opener (DEC :58-62). (e) **Options-flow episodes** (`data/options_signal_episode/`; engine/options_episode_coverage.py:1-63@aba2c115bafa): a different episode family, no B1 intake.

**INFERRED.** "Qualify extra anchors" is therefore not "wire up an existing anchor producer" — no second anchor producer exists anywhere in the tree. It is "close and version the anchor vocabulary, then admit one new species through its source owner."

## Q3 — Consumers today, and what a silent species admission does

**OBSERVED — who reads episodes, and the function each calls.**

| Consumer | Entry point | Function called |
|---|---|---|
| Prophet Operator Lab / D5 API `GET /api/prophet/lab/v1/episodes/{episode_id}/intelligence` | app/prophet_lab.py:254-296@aba2c115bafa | `load_candidate_episode_store_snapshot` (app/prophet_lab.py:56,279); root `data/us_prophet_rank/episodes` (:64), env override `PROPHET_LAB_EPISODE_STORE_ROOT` (:271) |
| D5 evidence families | engine/prophet_lab/intelligence_vector.py:166-168@aba2c115bafa | lazy `load_candidate_episode_store_snapshot`; `_validate_b1_episode_identity` recomputes the id from identity+anchor (:260-291); `episode_ref` pins `generation_id`, `opened_session`, `anchor_time` (:967-977) |
| B3 candidate-state projection | engine/prophet_candidate_state.py:14,314,363-386@aba2c115bafa | consumes `snapshot.generation.episodes`; parses ids via `_parse_episode_id as _parse_b1_episode_id`; refuses any non-`prophet.candidate_episode/v1` schema (:314) |
| B3 owner-fact adapters | engine/prophet_candidate_state_sources.py:21,26,186-187,309-353@aba2c115bafa | imports `CandidateEpisodeStoreSnapshot`; pins `peg:<64hex>`; carries radar `episode_address` as `PROVISIONAL_UNVERSIONED_EPISODE_ADDRESS` |
| The writer's own read | scripts/reconcile_us_candidate_episodes.py:222@aba2c115bafa | `load_candidate_episode_store_snapshot` |
| B4 strategy definition / availability | engine/prophet_strategy_definition.py:73,88-92,140-146@aba2c115bafa | declares `candidate_owner_schema: prophet.candidate_episode/v1`, `candidate_source: EXISTING_CANONICAL_B1_ONLY`, `may_create_candidate_episode: False`, `may_widen_population: False`; requires `candidate_episode_id` + `candidate_generation_id`; engine/prophet_entry_availability.py:458,497 |
| Pinned downstream fixture | tests/fixtures/us_candidate_episode/downstream_consumer.py:6,17@aba2c115bafa | `load_candidate_episode_store` |

**OBSERVED — who does NOT read episodes (two spec-named suspects cleared).** **B2 disposition is not an episode consumer:** `grep -c "episode" engine/prophet_b2_disposition.py` → `0`; it is a frozen B-15..B-19 launch-review *records* matrix that "carries NO authority — nothing in engine/ or scripts/ may import it" (engine/prophet_b2_disposition.py:1-8@aba2c115bafa), a boundary the suite pins (tests/test_prophet_b2_disposition.py:448-449@aba2c115bafa). **Grading has zero episode coupling:** `scripts/grade_us_prophet_candidates.py` and `engine/us_prophet_grades.py` contain no `episode` reference at all; the grader's frozen grain is `(stamp_date, ticker, board_definition, horizon)` (engine/us_prophet_grades.py:56@aba2c115bafa; config/dag.yml:1806-1810@aba2c115bafa). **INFERRED:** the WS objective's "cohort-honest grading of every episode" (agentos/workstreams/WS-PROPHET-US-V4-RECOVERY.md:5-13@aba2c115bafa) is not yet wired to B1 episodes — grading still runs on the board/candidate grain. **UI collision worth naming:** the dashboard "episode chip" is NOT a B1 episode — `scripts/build_site.py::_us_prophet_episode_map` (scripts/build_site.py:5314-5340@aba2c115bafa) derives a per-TICKER ordinal over the plan book, consumed by templates/_us_prophet_plan_cards.html.j2:24-26,86-95@aba2c115bafa via templates/dashboard.html.j2:16737@aba2c115bafa. Four distinct "episode" notions live in this tree (B1 canonical, Radar `live_entry_episode`, options-flow, plan-book ordinal); B03/B04 UI work must not conflate them.

**OBSERVED — SILENTLY WIDENS: the anchor vocabulary is open.** `canonical_anchor` requires only that `kind` be a non-empty string (engine/us_candidate_episode.py:195-212@aba2c115bafa); no allowlist exists anywhere (`rg "ANCHOR_KIND|anchor_kind|kind.*frozenset" engine scripts config` returns no episode hit). PROBE P1 mints a valid `pe:` id for a made-up kind `entry_radar_expert_fire`. Consequence: a new species passes the pure core, the row validator (:447-483), the sole reader (:1071-1078), D5's `_validate_b1_episode_identity` (it recomputes the same id, :260-291) and B3 (which never reads the anchor). D5 would then emit closed evidence families for a species it never adjudicated, and B4's `may_widen_population: False` would be violated in substance while its text still reads `EXISTING_CANONICAL_B1_ONLY`. The only visible trace is the free-text `structural_anchor.kind`, because `intake_classes` is the same literal `"technical_emergence"` for all four sources today (engine/us_candidate_episode_intake.py:256,333@aba2c115bafa).

**OBSERVED — BREAKS, fail-closed: there is no version lane.** One global `DEFAULT_DEFINITION_ERA` (engine/us_candidate_episode.py:33@aba2c115bafa) is stamped on every event (scripts/reconcile_us_candidate_episodes.py:413,419,441@aba2c115bafa); `build_all_candidates` takes the projection era from `ledger[0]` (engine/us_candidate_episode.py:1016@aba2c115bafa); `load_all_candidates` REFUSES a document era ≠ the module constant (:1038) and any row era ≠ the document era (:1062). PROBE P3: a two-era ledger yields a projection the sole canonical reader refuses with "episode definition era differs from document". Conversely, moving the constant strands history: `_assert_ordinary_source_retry_matches` re-mints the committed event with the RUN's era and compares canonical bytes (engine/us_candidate_episode.py:724-812@aba2c115bafa), so every already-committed ordinary source key would fail "ordinary source key reused with different committed bytes". A new species under the SAME era is invisible; under a NEW era it bricks the projection.

**OBSERVED — also breaks / constrains.** Registry pins: all six B1 datasets are `version: "1.0.0"` (config/dataset_registry.yml:747,776,807,836,866,893@aba2c115bafa) and the wiring suite pins those rows (tests/test_us_candidate_episode_wiring.py:234,252,279,294,304@aba2c115bafa), so a new species at the same version string is drift no test can see. One active slot per identity: `project_events` refuses two ACTIVE episodes for one `(security_id, identity_epoch)` (engine/us_candidate_episode.py:640-650@aba2c115bafa; PROBE P2). **INFERRED:** a second species does not ADD episodes for a name already open — it competes for the single active slot by `known_at` order, or suppresses `ACTIVE_EPISODE_DIFFERENT_ANCHOR` (:902-906). That is the real semantic cost of "extra anchors", and it is a D02 ruling, not a code change.

## Q4 — Registry state as far as committed artifacts show

**OBSERVED — schema versions (all `/v1`).** `prophet.candidate_episode_event/v1`, `prophet.candidate_episode/v1`, `prophet.all_candidates/v1`, `prophet.candidate_episode_head/v1`, `prophet.candidate_episode_generation_manifest/v1`, `prophet.candidate_episode_suppression/v1`, `prophet.candidate_episode_reconcile_receipt/v1`, `DEFAULT_DEFINITION_ERA = "candidate-episode-v1-2026-08-25"`, `STOCK_IDENTITY_SCHEMA = "stock_identity.fingerprint_spec.v1"` (engine/us_candidate_episode.py:26-48@aba2c115bafa). Closed enums: 8 event types (:34-43), 5 states = `ACTIVE` + `RESOLVED/INVALIDATED/EXPIRED/RETRACTED` (:44-46), 10 suppression reasons (:61-72), 9 patchable fields (:50-60). Dataset-registry `version: "1.0.0"` ×6 (config/dataset_registry.yml:747,776,807,836,866,893@aba2c115bafa).

**OBSERVED — counts in fixtures.** `tests/fixtures/us_candidate_episode/all_candidates.json`: schema `prophet.all_candidates/v1`, `definition_era: candidate-episode-v1-2026-08-25`, `coverage {episodes: 2, active: 1, suppressed_inputs: 0}`, `generated_from {event_count: 3, ledger_sha256: sha256:6ab123576c3e7143b9d064ea9f11fac9cf6218f5d62653bfc062839a94a3efcc}`; rows `pe:SEC:US-XNAS-AAA:epoch_0:sa:4e6a5fad9d2edb06f30d3e31:1` (ACTIVE) and `pe:SEC:US-XNAS-BBB:epoch_0:sa:28ccb7f645731719def3c964:1` (RESOLVED), both `intake_classes: ["technical_emergence"]`, both anchors `kind=turn_watch_reset_low, basis=adjusted_close`. Intake fixtures: `tests/fixtures/us_candidate_episode/intake/{turn_watch.json,security_master.json,vendor_aliases.json}`. `tests/fixtures/prophet_lab/` carries **no** episode store (only `observation_baseline.json`, `prophet_index/`, `radar_spool/`, `stockdata/`).

**OBSERVED (git tree only) / UNKNOWN (blob contents) — is `data/us_prophet_rank/episodes/` the live store?** Yes: that is the live store path, resolved by readers at app/prophet_lab.py:64@aba2c115bafa and scripts/reconcile_us_candidate_episodes.py:405,595@aba2c115bafa, and it IS committed on main — `git ls-tree -r --name-only HEAD data/us_prophet_rank/episodes` → **65 paths** = `HEAD.json` plus **9 generation directories**: `peg:18dfbb8a9d71`, `peg:26ee50a6df9a`, `peg:5a1d60fdd7f6`, `peg:81cd578b9e4a`, `peg:881d604cc569`, `peg:9afeb4f89ecc`, `peg:c025bb50c45f`, `peg:d83eea1cc128`, `peg:f64f07f5919e`. **UNKNOWN — which generation `HEAD.json` selects today, and today's live counts.** Missing artifact: the blob `data/us_prophet_rank/episodes/HEAD.json` (and the `latest_receipt.json` of the generation it names); this lane is sparse and does not read `data/` artifacts — only git tree names were listed, no blob content. Expected path chain: `data/us_prophet_rank/episodes/HEAD.json` → `generations/<HEAD.generation_id>/{manifest.json,all_candidates.json,current.parquet,latest_receipt.json,events/<month>.jsonl,suppressions/<month>.jsonl}` (config/dataset_registry.yml:762-905@aba2c115bafa). Only `HEAD.json` selects the canonical generation; newest-directory selection is forbidden (agentos/handoffs/PROPHET-US-V4-RECOVERY-2026-08-28-b1-acceptance.md:69@aba2c115bafa).

**OBSERVED (committed record, not re-measured here) — last accepted counts.** Generation `peg:c025bb50c45f319f989a4848249b8a85b65354143e3262f2ad09d07841311b08`, **467 lawful SEC/ISS episodes, 915 events, 5,435 suppressions**, zero duplicate episode/event/source identities, from natural scheduled run `33147282433` at head `24ccea3fe482`, durable main commit `a8ee11ba0e48` (agentos/handoffs/PROPHET-US-V4-RECOVERY-2026-08-28-b1-acceptance.md:37-46@aba2c115bafa; mirrored at agentos/workstreams/WS-PROPHET-US-V4-RECOVERY.md:203-212@aba2c115bafa). That generation IS one of the 9 committed directories (OBSERVED above), so the accepted generation is still on main. **UNKNOWN:** whether HEAD still selects it — 8 sibling generations exist and only `HEAD.json` decides.

**OBSERVED — was the B1 natural-acceptance probe verdict ACCEPTED? YES, but cite the handoff, not the probe.** `research/prophet_v4/B1_NATURAL_ACCEPTANCE_PROBE.md` contains **no verdict**: it is the read-only probe plus its own scope limits — headings `# B1 natural-acceptance probe` (:1), `## Why it exists` (:8), `## How to run it` (:17), `## Status when this was written` (:29-38 — validated against a generation produced by B1's own nightly test, "30 checks, 0 fail"; against `origin/main` at that time it "correctly reports the store as absent"), `## What this probe does NOT cover` (:41-65 — four named gaps: run identity/ancestry, receipt↔run binding, retry/idempotence, provisional Radar lineage), `## Interpreting a failure` (:67), `## The probe` (:75). The verdict is `ACCEPTED / PROVEN_LIVE.` — "D5's canonical-episode dependency is cleared" (agentos/handoffs/PROPHET-US-V4-RECOVERY-2026-08-28-b1-acceptance.md:77-82@aba2c115bafa), echoed by WS wave b1 `status: done` + `ACCEPTED / PROVEN_LIVE` (agentos/workstreams/WS-PROPHET-US-V4-RECOVERY.md:198-212@aba2c115bafa).

**OBSERVED — two record/code drifts to settle before sequencing.** (1) config/dataset_registry.yml:731-732@aba2c115bafa still says the six contracts "are PROPOSED until the first ordinary scheduled nightly creates and validates the production generation", and all six still read `status: PROPOSED` (:735,764,795,824,854,882) — after the 2026-08-28 acceptance and with 9 generations committed. (2) WS waves `b3`/`b4` read `status: todo` (agentos/workstreams/WS-PROPHET-US-V4-RECOVERY.md:221-228@aba2c115bafa) while their modules are merged on main with tests: `engine/prophet_candidate_state.py` (B3, `DEFINITION_ERA = "candidate-state-v1-2026-09-18"`, :17-18) + `engine/prophet_candidate_state_sources.py` + `tests/test_prophet_candidate_state{,_sources}.py`, and `engine/prophet_entry_availability.py` (B4).

## Q5 — Do B03 / B04 need new anchor species for a first version?

**B03 (lossless searchable field) — NO.** The card scopes B03 to "Incumbent #7572; `engine/us_candidate_lanes.py`, `scripts/build_site.py`, existing dashboard/premium-payload source paths", inputs "Accepted board/candidate generation, source-qualified pre-B1 observations, B1 relations, entitlement and honest null rank", `Depends On: B00, B02` (work_cards/B03.md@ab8c72c2afd0). Nothing there needs a second species. OBSERVED: `engine/us_candidate_lanes.py` has **zero** episode coupling (`grep -c "candidate_episode\|episode_id"` → `0`) and is a display-tier partition behind a file-boundary authority fence (engine/us_candidate_lanes.py:1-38@aba2c115bafa). OBSERVED — a board-pool row is already DISTINCT from a canonical episode on three axes: **(a) grain** — a board/candidate row is keyed `(stamp_date, ticker, board_definition)`, literally the intake source key `candidate:{stamp_date}:{ticker}:{board_definition}` (engine/us_candidate_episode_intake.py:361-363@aba2c115bafa) and the grader's frozen grain (engine/us_prophet_grades.py:56@aba2c115bafa), while a canonical episode is keyed `pe:SEC:…:epoch_0:sa:<anchor>:<gen>` (engine/us_candidate_episode.py:218-225@aba2c115bafa); **(b) authority** — the board row arrives with `anchor=None` (engine/us_candidate_episode_intake.py:333@aba2c115bafa) so it can only attach (engine/us_candidate_episode.py:953-980@aba2c115bafa) or suppress `MISSING_STRUCTURAL_ANCHOR` (:907-911) and can never mint an identity; **(c) shape** — an episode row carries `opened_at`, `opened_session`, `episode_state`, `terminal_reason`, `structural_anchor`, `rearm_of`, `generation`, `identity_epoch_state` (engine/us_candidate_episode.py:415-441@aba2c115bafa), whereas a pool row carries `lane`, `lane_reasons`, `display_rank`, `pool_rank` and an explicitly **null** `prophet` score (engine/us_candidate_lanes.py:40-60@aba2c115bafa). INFERRED: B03 v1 = today's episode set (467 at last acceptance) rendered alongside board-pool rows **kept on their own grain**, joined by `security_id` + session, never by merging grains; the only thing B03 owes B02 is honest labeling of which rows are canonical episodes vs pool rows plus source-layer exclusion reasons (its own card output line).

**B04 (episode evidence dossier) — NO.** The card scopes B04 to `engine/prophet_lab/intelligence_vector.py`, `app/prophet_lab.py` and existing D5/source-family adapters, inputs "Atomic B1 generation, admissible source revisions, owner evidence lanes and exact original/current cutoff", `Depends On: B00, B02`, and decision dependencies **D03, D07, D10 — not D02** (work_cards/B04.md@ab8c72c2afd0; effective/REQUIREMENT_CROSSWALK.json R02/R21@ab8c72c2afd0). Both scoped files already read the store (app/prophet_lab.py:279@aba2c115bafa; engine/prophet_lab/intelligence_vector.py:166-168@aba2c115bafa). D03 ("Real source histories and rights") and D07 ("Source and model-vintage eligibility") are both `OPEN_TECHNICAL_DECISION_FABLE_AUTHORIZED_TO_RESOLVE` and both list B04 in `blocked_consumers` (effective/DECISION_REGISTER.json@ab8c72c2afd0) — those, not anchors, are B04's real gate. **Caveat (INFERRED):** B04 renders whatever species the store contains, and because `_validate_b1_episode_identity` merely recomputes the id (engine/prophet_lab/intelligence_vector.py:260-291@aba2c115bafa), a species admitted without a version bump would flow straight into a "closed" evidence dossier. B04 needs B02's species/era discipline; it does not need a new species.

## Q6 — Smallest bounded B02 unit

**Species with the best current evidence: an Entry Radar expert fire (`mastermind.entry_event.v1`, G0/C1–C5) carrying a Radar-owned structural anchor.** INFERRED from four OBSERVED facts — it is the only extra expert that already has (a) a registered intake path (engine/us_candidate_episode_intake.py:384-392@aba2c115bafa), (b) an accepted ruling on its relation target (`DEC:PROPHET-B1-CANONICAL-EPISODE-BINDINGS` R3, agentos/decisions/DEC-PROPHET-B1-CANONICAL-EPISODE-BINDINGS.md:13-18@aba2c115bafa), (c) an existing B3 source adapter (engine/prophet_candidate_state_sources.py:309-353@aba2c115bafa), and (d) a named source owner `WS:LIVE-ENTRY-RADAR` (config/dataset_registry.yml:699@aba2c115bafa). The alternatives are already ruled out: Doors R is denied re-arm authority (DEC R4 :18-22), candidate snapshots and unanchored Radar are rejected as openers (DEC :58-62), the options-flow episode is a different family, and the Radar runtime ledger may not be aliased (WS:104-105).

**Hard precondition (OBSERVED blocker).** The Radar relation key is `episode_address`, which "uses `event_id` when present, otherwise the legacy `ticker|detector_id|decision_session` fallback" (config/dataset_registry.yml:718-728@aba2c115bafa; scripts/reconcile_entry_radar.py:437-451@aba2c115bafa). Anchoring on that fallback would be a ticker/date surrogate — exactly D02's `forbidden_shortcut`. `WS:LIVE-ENTRY-RADAR` must first freeze and validate the forward-projection contract so the B1-consumed relation is the exact immutable `event_id` (config/dataset_registry.yml:724-728@aba2c115bafa), and `data/entry_radar/forward.parquet` must exist.

**Minimal versioned change** (owner: B1 as contract owner with `WS:LIVE-ENTRY-RADAR` as source owner; Fable decides D02; matches the B02 card Owner Scope — `engine/us_candidate_episode_intake.py`, `engine/us_candidate_episode.py`, `engine/prophet_candidate_state_sources.py`, `scripts/build_turn_watch.py`, work_cards/B02.md@ab8c72c2afd0):
1. **Close the vocabulary first (the real fix, and it needs no D02 ruling).** Add `ANCHOR_KINDS = frozenset({"turn_watch_reset_low"})` beside the other closed enums (engine/us_candidate_episode.py:34-72@aba2c115bafa) and enforce membership inside `canonical_anchor` (:195-212). No-op for every committed row (both fixture anchors, and the accepted generation's only species, are `turn_watch_reset_low`), and it converts today's silent widening (Q3) into a fail-closed refusal.
2. **Add the species.** `ANCHOR_KINDS |= {"entry_radar_expert_fire"}` plus a new `radar_anchor_observations()` in engine/us_candidate_episode_intake.py building the anchor ONLY from Radar-owned fields already in `mastermind.entry_event.v1` (detector id + the detector's own price/time geometry — never a later extremum, per D02 `forbidden_shortcut: future extrema`), keeping `source_receipt` as the exact once-read file receipt (mandatory at read time, engine/us_candidate_episode.py:1076@aba2c115bafa) and giving it an `intake_class` distinct from `"technical_emergence"` so `intake_classes` finally discriminates species (engine/us_candidate_episode_intake.py:256,333@aba2c115bafa).
3. **Version without touching era or ids.** Bump the dataset-registry `version` for `prophet.us.candidate_episode.{events,current,all_candidates}` (config/dataset_registry.yml:776,836,866@aba2c115bafa) and add any species-specific refusal as a new member of the closed `SUPPRESSION_REASONS` (engine/us_candidate_episode.py:61-72@aba2c115bafa). **Do NOT change `DEFAULT_DEFINITION_ERA`** (:33): PROBE P3 shows a two-era ledger makes `all_candidates.json` unreadable by the sole canonical reader (:1016,1038,1062), and moving the constant instead fails every committed ordinary source key at :724-812. D02's required output demands "no changed historical IDs" (effective/DECISION_REGISTER.json D02@ab8c72c2afd0) — a new `kind` yields a new `anchor_token` and hence a new episode_id for the NEW species only, leaving existing ids byte-identical (engine/us_candidate_episode.py:214-225@aba2c115bafa).
4. **Files.** `engine/us_candidate_episode.py`, `engine/us_candidate_episode_intake.py`, `scripts/reconcile_us_candidate_episodes.py` (only `_load_intakes` :128-147 and `_source_accounting` :380-395 for a fifth source), `config/dataset_registry.yml`. **No new DAG step** — B1 stays the sole writer (config/dag.yml:1798-1801@aba2c115bafa).
5. **Tests.** The four suites the CI job already runs: `tests/test_us_candidate_episode.py`, `tests/test_us_candidate_episode_intake.py`, `tests/test_us_candidate_episode_reconciler.py`, `tests/test_us_candidate_episode_wiring.py` (tests/test_us_candidate_episode_wiring.py:227-233@aba2c115bafa).
6. **Negative cases** (each a new test; neighbouring pins named so the new ones are not redundant): **duplicate episode** — two ACTIVE for one `(security_id, identity_epoch)` is refused (engine/us_candidate_episode.py:640-650@aba2c115bafa; PROBE P2; existing pin tests/test_us_candidate_episode.py:387@aba2c115bafa), a second species on an already-open name suppresses `ACTIVE_EPISODE_DIFFERENT_ANCHOR` (:902-906; existing pin :300), and a duplicate `episode_id` in the projection is refused (:1057-1059). **Anchor without identity** — `IDENTITY_UNRESOLVED`/`ISSUER_UNRESOLVED` fire before any anchor is considered (engine/us_candidate_episode_intake.py:142-148,155-160@aba2c115bafa), so an anchored observation whose identity does not resolve must still suppress and never open; an unanchored observation with no ACTIVE episode → `MISSING_STRUCTURAL_ANCHOR` (engine/us_candidate_episode.py:907-911@aba2c115bafa; existing pin tests/test_us_candidate_episode.py:280-291@aba2c115bafa). **Retracted anchor** — `RETRACTED` needs `correction_of` + non-empty `reason`, its target must be `OPENED`/`OBSERVED`/`EXPERT_EVENT_ATTACHED`, and retracting an `OPENED` event removes the row from the projection entirely (engine/us_candidate_episode.py:580-588,610-613@aba2c115bafa; existing pin tests/test_us_candidate_episode.py:850@aba2c115bafa); the new case is that a retracted Radar anchor must release the single active slot so a later legal re-arm opens `generation+1` (:912-921) and must not resurrect the retracted `source_event_id`, whose key stays owned (`validate_ordinary_source_ownership` :682-706). **Old source key, new bytes** — `_assert_ordinary_source_retry_matches` (engine/us_candidate_episode.py:724-812@aba2c115bafa; existing pin tests/test_us_candidate_episode.py:168@aba2c115bafa) and the persisted-suppression variant (tests/test_us_candidate_episode_reconciler.py:364@aba2c115bafa). **Ticker rename** — identity resolves membership at the observation session (engine/us_candidate_episode_intake.py:142-148@aba2c115bafa) so `security_id`, and hence `episode_id`, is stable across a rename (existing pin tests/test_us_candidate_episode.py:821@aba2c115bafa). **Occurrence vs observation clock** — `occurred_at <= known_at <= recorded_at` (engine/us_candidate_episode.py:279-281@aba2c115bafa), `opened_at = max(anchor.time, known_at)` (:921-925), and `recorded_at` excluded from event identity (:227-241).

## EVIDENCE — commands and captured tails

All run in the lane worktree at detached HEAD `aba2c115bafa`; rc is the command's exit status.

```
$ git rev-parse HEAD
aba2c115bafa7557bcee07ea7f64f262648f593f                                                    rc=0
$ git sparse-checkout list
/*   !/data/   !/site/   !/mockups/   !/verify_shots/                                        rc=0
$ wc -l engine/us_candidate_episode.py engine/us_candidate_episode_intake.py \
        scripts/reconcile_us_candidate_episodes.py engine/prophet_b2_disposition.py
1551 / 392 / 665 / 439   (3047 total)                                                       rc=0
```

Named test files only (single files, `-q -p no:cacheprovider`; no full-suite run, no Playwright run):

```
$ python3 -m pytest tests/test_us_candidate_episode_intake.py     -q -p no:cacheprovider
19 passed in 2.12s                                                                          rc=0
$ python3 -m pytest tests/test_us_candidate_episode.py            -q -p no:cacheprovider
33 passed in 5.47s                                                                          rc=0
$ python3 -m pytest tests/test_us_candidate_episode_reconciler.py -q -p no:cacheprovider
56 passed in 7.12s                                                                          rc=0
$ python3 -m pytest tests/test_us_candidate_episode_wiring.py     -q -p no:cacheprovider
16 passed in 3.27s                                                                          rc=0
$ python3 -m pytest tests/test_prophet_b2_disposition.py          -q -p no:cacheprovider
26 passed in 0.99s                                                                          rc=0
```

150 passed, 0 failed across the five named files (the four B1 suites CI runs + the B2 records suite). No other suite was run.

Store census from the git tree only (no `data/` blob read), plus consumer-coupling counts:

```
$ git ls-tree -r --name-only HEAD data/us_prophet_rank/episodes | wc -l
65                                                                                          rc=0
$ git ls-tree -r --name-only HEAD data/us_prophet_rank/episodes | grep -o 'peg:[0-9a-f]\{12\}' | sort -u
peg:18dfbb8a9d71  peg:26ee50a6df9a  peg:5a1d60fdd7f6  peg:81cd578b9e4a  peg:881d604cc569
peg:9afeb4f89ecc  peg:c025bb50c45f  peg:d83eea1cc128  peg:f64f07f5919e                       rc=0  (9 generations + HEAD.json)
$ git ls-tree -r --name-only HEAD data/entry_radar
data/entry_radar/ledger_state.json                                                          rc=0  (no forward.parquet)
$ grep -c "episode" engine/prophet_b2_disposition.py
0                                                                                           rc=0
$ grep -c "candidate_episode\|episode_id" engine/us_candidate_lanes.py
0                                                                                           rc=0
$ python3 -c "import json;d=json.load(open('tests/fixtures/us_candidate_episode/all_candidates.json'));…"
schema: prophet.all_candidates/v1    definition_era: candidate-episode-v1-2026-08-25
coverage: {"active": 1, "episodes": 2, "suppressed_inputs": 0}
generated_from: {"event_count": 3, "ledger_sha256": "sha256:6ab123576c3e…3efcc"}
pe:SEC:US-XNAS-AAA:epoch_0:sa:4e6a5fad9d2edb06f30d3e31:1 ACTIVE   ['technical_emergence'] turn_watch_reset_low
pe:SEC:US-XNAS-BBB:epoch_0:sa:28ccb7f645731719def3c964:1 RESOLVED ['technical_emergence'] turn_watch_reset_low  rc=0
```

In-memory contract probes (pure engine calls via `python3 - <<PY`; nothing written, no repo state changed):

```
P1 new anchor kind mints with NO version bump:
   tw   : pe:SEC:US-XNAS-XYZ:epoch_0:sa:182b643d791f3e4b71d9e668:1
   radar: pe:SEC:US-XNAS-XYZ:epoch_0:sa:73a5671fb0e9ee4806bfb574:1   (kind entry_radar_expert_fire)
   tokens differ: True                                                                     rc=0
P2 two ACTIVE episodes, one (security, epoch), two anchor species:
   REFUSED: two active episodes exist for one security identity epoch                       rc=0
P3 mixed definition_era ledger -> sole canonical reader:
   projection definition_era : candidate-episode-v1-2026-08-25
   row eras                  : ['candidate-episode-v1-2026-08-25','candidate-episode-v2-2026-09-23']
   anchor kinds on rows      : ['entry_radar_expert_fire','turn_watch_reset_low']
   load_all_candidates       : REFUSED -> episode definition era differs from document       rc=0
```

R6 card / register retrieval (branch substitution — see BLOCKED-1):

```
$ git fetch origin claude/prophet-us-r6-meta-ceo-adoption
fatal: couldn't find remote ref claude/prophet-us-r6-meta-ceo-adoption                       rc=128
$ git show FETCH_HEAD:research/prophet_v4/r6_fable_meta_ceo_handoff/work_cards/B02.md
fatal: invalid object name 'FETCH_HEAD'.                                                     rc=128
$ git ls-remote --heads origin | grep -i r6
ab8c72c2afd03ee6bc71c423c47ed916427729a6  refs/heads/claude/prophet-us-r6-adoption-records
53276718a7f35c83b2618c60a724a31faee5cbc8  refs/heads/claude/prophet-us-r6-wave0-records      rc=0
$ git show ab8c72c2afd0…:research/prophet_v4/r6_fable_meta_ceo_handoff/work_cards/B02.md
# B02 — Preserve canonical episodes and qualify extra anchors …                              rc=0
```

## BLOCKED

1. **The R6 card branch named by the spec does not exist.** Exact commands and errors: `git fetch origin claude/prophet-us-r6-meta-ceo-adoption` → `fatal: couldn't find remote ref claude/prophet-us-r6-meta-ceo-adoption` (rc=128); then `git show FETCH_HEAD:research/prophet_v4/r6_fable_meta_ceo_handoff/work_cards/B02.md` → `fatal: invalid object name 'FETCH_HEAD'.` (rc=128). Resolution taken: `git ls-remote --heads origin | grep -i r6` shows the only R6 branch carrying that tree is `claude/prophet-us-r6-adoption-records` @ `ab8c72c2afd03ee6bc71c423c47ed916427729a6`, whose `git ls-tree` lists `research/prophet_v4/r6_fable_meta_ceo_handoff/work_cards/B02.md` (plus B03/B04, `effective/DECISION_REGISTER.json`, `effective/REQUIREMENT_CROSSWALK.json`). All R6 citations here use that exact sha. The sibling `claude/prophet-us-r6-wave0-records` @ `53276718a7f3` carries `research/prophet_v4/r6_program/{rulings,wave0}/` but no `work_cards/`. The lane's named output path `research/prophet_v4/r6_program/wave1/` does not exist on either branch; it was created here as the spec directs.
2. Nothing else blocked. No engine/scripts/tests/templates/.github/data file was touched; the four named engine/script files and their five test files all read clean and the five suites pass.

## NOT_ESTABLISHED

Each UNKNOWN names its missing artifact.

1. **Which generation `HEAD.json` selects today** — missing artifact: the blob `data/us_prophet_rank/episodes/HEAD.json` at `aba2c115bafa` (sparse worktree; `data/` blobs not read by this lane). 9 candidate generations are committed; only that file decides.
2. **Today's live episode / event / suppression counts** — missing artifacts: `data/us_prophet_rank/episodes/generations/<HEAD.generation_id>/all_candidates.json` and `latest_receipt.json`. The 467 / 915 / 5,435 figures are the 2026-08-28 acceptance record, not a 2026-09-23 measurement.
3. **Whether the accepted generation `peg:c025bb50c45f…` is still HEAD-selected** — same missing artifact as (1).
4. **Whether the B1 nightly writer has run since 2026-08-28, and with what receipt** — missing artifacts: the run-level facts (`gh run view` on the `daily` schedule) and the per-generation `latest_receipt.json`. This lane made no `gh` run/CI query (no CI polling permitted).
5. **Whether `data/entry_radar/forward.parquet` has ever been produced** — only `data/entry_radar/ledger_state.json` is committed at `aba2c115bafa`, the registry row is `PROPOSED`/`STAGED_NOT_ARMED`, and the probe document expects the Radar intake to degrade. Missing artifact: any committed `forward.parquet` bytes, or a Radar-owner frozen forward-projection contract.
6. **The Radar experts' own structural-anchor geometry** (what price/time a G0/C1–C5 fire would freeze as an anchor) — missing artifact: no anchor producer exists under `engine/entry_radar/` (OBSERVED: only `episode_address` join keys are present), and D02 is still `OPEN_TECHNICAL_DECISION_FABLE_AUTHORIZED_TO_RESOLVE`.
7. **Incumbent PR #7572 state** (B03's named incumbent carrier) — missing artifact: no `gh pr view` was run in this lane (the `gh` budget is reserved for delivery). Its head, files and review state are not established here.
8. **Whether any consumer outside this repo (Terminal / charting-app) reads B1 episodes** — missing artifact: the connected Terminal repository was not opened by this lane; only macro-side consumers were censused.
