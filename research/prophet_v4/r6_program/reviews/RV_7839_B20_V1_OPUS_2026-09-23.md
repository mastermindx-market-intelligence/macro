# RV-7839 — Independent adversarial review of the B20 design packet v1 (Opus, read-only)

- Artifact: `research/prophet_v4/r6_program/wave1/B20_DESIGN_PACKET_V1_2026-09-23.md` @ `366533a9c21f` (PR #7839)
- Reviewer: native Opus `reviewer` child of the Fable Meta-CEO seat (48cdfd56), `MODE: READ_ONLY`, commissioned 2026-09-23
- Standards held: `docs/DESIGN_DOCTRINE.md`, `research/MASTER_PRODUCT_DESIGN_SYSTEM_V1.md`, `research/DESIGN_MIGRATION_FACTORY_V1.md`, theme art-direction law, plain-language law, two-header navigation law, R6 B20/D09 items, `DEC:PROPHET-US-D02-EPISODE-ADMISSION-GATED`, `DEC:PROPHET-US-D05-ENTRY-WATCH-PERSISTENCE`, `DEC:PROPHET-US-D11-RELEASE-PATH-INCUMBENT-CONTROLS`
- Status returned: PARTIAL (turn-capped; gaps named below). Seat disposition: `research/prophet_v4/r6_program/rulings/R6-B20-01_DIRECTION_2026-09-23.md`.

## Verdict returned to the seat

APPROVE WITH REQUIRED REPAIRS. The direction is sound: one six-destination workspace with a list and evidence drawer, Watch as the only live action, and honest nulls. But the packet is not a D09 closure artifact. As a design deliverable it stays PARTIAL/BLOCKED until repairs R1–R12 land.

## Findings

**BLOCKER B1 — green is used for things other than an open entry.** l.139 sends "positive/available/healthy" to `--ink-ok`; l.83 calls a healthy delivery green; Flow 3 (l.28) reads availability from the old `p.entry_status` / `entry_signal.status` fields (`buy_now`, `buy_soon`…) instead of `availability_state`. Breaks the rule that green appears only where the server says `availability_state == ENTRY_OPEN`, "no green accents on maturity, scores, or themes" (`research/prophet_v4/EXPERIENCE_REFERENCE_COMPOSITIONS.md:21`; `agentos/workstreams/WS-PROPHET-US-V4-RECOVERY.md:9`). Ignores `DEC D11:29` ("No producer persists `ENTRY_OPEN` today"). A builder following l.28 + l.139 would bring back the retired "Live now" defect.

**BLOCKER B2 — neither theme is actually designed, and there is no evidence matrix.** §5 (l.133–140) and §6 (l.142–149) are page-wide prose restating `MASTER_PRODUCT_DESIGN_SYSTEM_V1.md:589–609`; no surface gets its own dark or light treatment; none of the 12 ASCII wireframes (l.155–321) depends on theme. Missing: theme-specific degraded states, a reference/baseline, committed mockups, a capture plan. Required by CLAUDE.md §"Theme art direction — required"; D09 step 3 (`DECISION_RESOLUTION_PLAYBOOK.md:117`); closure artifact captures (`:120`); `MASTER_PRODUCT_DESIGN_SYSTEM_V1.md:611–613`; `docs/DESIGN_DOCTRINE.md:163–167`.

| Matrix | Cells |
|---|---|
| Required (6 surfaces × 2 themes × 2 languages × 2 widths) | 48 |
| Fully specified | 0 |
| Layout only, theme-neutral, English placeholders (l.332) | 12 |
| Themes as per-surface designs | 0 |
| Chinese compositions | 0 |

**BLOCKER B3 — internal decision IDs, jargon and an unbackable promise in disabled-action text.** l.355 "Thesis actions arrive after D05 maps their owner" (repeats for Milestone, Review); l.346 "Waiting for its owner to be connected." `DEC D05:10–11` requires plain-word "not saved yet" text; `DESIGN_DOCTRINE.md:52–54` bans ruling IDs from glance copy; "arrive after" promises a feature blocked on an operator-ratified database change (`DEC D05:52–54`). l.355 contradicts itself: Reason/Note listed as disabled while its reason says "Notes are local only".

**MAJOR M1 — vocabulary incomplete, no stances.** §3 (l.88–108) maps only ENTRY_OPEN, unavailable, invalidated; omits NOT_READY, APPROACHING_ENTRY, WAIT_PULLBACK, RAN_DONT_CHASE, UNAVAILABLE_DATA (`engine/prophet_entry_availability.py:35–45`) and the frozen lane labels (`EXPERIENCE_REFERENCE_COMPOSITIONS.md:19`). No state is mapped to a doctrine stance (`DESIGN_DOCTRINE.md:40–47`) although DecisionRow requires a stance verb (`MASTER_PRODUCT_DESIGN_SYSTEM_V1.md:544`).

**MAJOR M2 — DecisionRow overloaded.** l.118/l.40 put a six-field glance into `.mx-chg-row`, whose contract is name · change ≤14 words · stance verb · chevron (`:544`).

**MAJOR M3 — "canonical" components have not shipped.** `.mx-tbl`, `.mx-tabset`, `.mx-callout`, `.mx-disc`, `.mx-rail` exist only in the specimen's "PROPOSED — DS-PR-0" block (`specimen.html:8–16, 95–106`); theme.css count 0. `.qual2` 0 in `templates/`. `--ink-tier`/`--mx-tier-accent` live in `tier_preview.css`, not theme.css (contradicts l.14). "NEW count: 3" (l.131) is too low; real new-landing work ≥ 8 components (`:561–566`).

**MAJOR M4 — EpisodeDrawer breaks the disclosure law.** Drawers are reserved for cross-page utilities (`:392`); Prophet detail is already the `/prophet/<T>` page (`:460`, `:488`). l.128 needs an archetype ruling and a defined relationship to the detail page.

**MAJOR M5 — route count and archetype per route unresolved.** l.35 "one workspace, not six boards" vs l.37–78 assigning three `discovery_board`, two `intelligence_desk`, one `instrument_analyzer`; archetypes are per route, identity devices never borrowed (`:425`). Track Record labelled instrument (l.70) but measurement maps to editorial F (`:488`). Radar/All Candidates lack the count ladder (`:436`/`:557`). The six destination tabs (l.336) appear in none of wireframes 7.1–7.12, including 390px ZH.

**MAJOR M6 — navigation and above-the-fold budget.** A workspace header band ("MARKET ▾ STRATEGY ▾ … [Research | Promoted]", l.159, l.174–176) adds a second market selector although l.114 says the market menu belongs to the shared nav — risks a third header (CLAUDE.md §Navigation). On mobile three header lines precede the answer (`:361–362`).

**MAJOR M7 — "Promoted view" toggle has nothing behind it.** l.159/l.337 offer a switch to a view that does not exist; the only strategy is `scientific_status: CONTROL_ONLY` (`engine/prophet_strategy_definition.py:67`). "Promoted" is internal gate vocabulary; a switchable view leans toward the view/release plane `DEC D11:7–9` forbids.

**MAJOR M8 — required D09 and R29 content missing.** D09 step 2 (`playbook:116`) asks the design to distinguish three sleeves and held-position management; no holdings surface. D09 step 4 (`:118`) requires tasks §10 (l.359–368) omits: find a displaced candidate, understand a refusal, distinguish sleeve vs holding. R29 onboarding absent. Strategy selector has one real strategy (`prophet_strategy_definition.py:22`).

**MAJOR M9 — live-update interactions cannot happen today.** §8 (l.325–327) designs invalidation announcements and an "Updated" reorder button; l.140 adds a pulse. B4 availability has no template consumer (0 template hits); the live plane is staged, not armed (`DEC D11:49–51`). Static nightly-bake behaviour is never designed as the default. Motion law `:312–314`; l.140 contradicts l.136.

**MAJOR M10 — Watch state machine incomplete against D05.** Missing per-list "pending" marker with retry, unwatch with list-scoped tombstone, and "a read must not paint Saved" (`DEC D05:11–14`). §9 (l.353–354) covers only Saved, failure, local-only. Row-level "Saved" and test 4 (l.364) imply an episode save; WatchStore saves symbols (`DEC D05:8–9`).

**MAJOR M11 — pipeline jargon and telemetry in user copy.** "Producer nomination / 来源提名" (l.100, l.51); "Tracked episode / 已跟踪轮次" (l.101); "The producer did not publish a rank" (l.59); "The early source has not published this session" (l.52); "Nothing here is a substitute read" (l.42, l.340); `UNAVAILABLE_FIELD` rendered as a raw slug (l.52, l.68). Violates `DESIGN_DOCTRINE.md:52–57`, `MASTER_PRODUCT_DESIGN_SYSTEM_V1.md:405–408`.

**MAJOR M12 — Chinese coverage and quality.** No ZH for §2.2–2.6 empty/degraded strings, §9 disabled reasons (l.355), the "Saved" readback (l.354). Unnatural: "已跟踪轮次", "下一收盘后" (l.338). Standard `:624–632`, `DESIGN_DOCTRINE.md:155–157`.

**MAJOR M13 — locked/entitlement states not designed.** l.58/l.60 mention "entitled detail" and "protected facts" but no `.mx-tier-gate--prophet` / TierLock (`:551`) and no preview-row state; never names the rollout control `DEC D11:9–11` requires (`config.yml: us_board_gate.panels`, `panel_preview_rows`, `premium.enforced_early`, plans-page disclosure).

**MAJOR M14 — binding prior composition base ignored.** No citation of `EXPERIENCE_REFERENCE_COMPOSITIONS.md` §2/§10 or `research/migration_packets/MP-1-prophet-board.md` (7-cell ladder, bans on "stage/阶段" and blended confidence numbers `:142`, crops in `mockups/refs/institutionalize/us_stocks/`). `SUPERSESSION_MAP.md` does not supersede them. Ladder vs availability-lane reconciliation (`EXPERIENCE_REFERENCE_COMPOSITIONS.md:56`) left open.

**Minor:** m1 empty states ~17 words, over the 14-word budget (l.42, l.339–340; `DESIGN_DOCTRINE.md:25, 82–88`). m2 Themes and Health have no stance; Health's audience conflicts with "operators only" (`EXPERIENCE_REFERENCE_COMPOSITIONS.md:13`). m3 l.325 overloads ↑/↓ across list, drawer, list-switch with no ownership rule. m4 the packet's EVIDENCE section (l.389–392) promises commands and records none; l.15 cites the macro spine specimen as a Prophet pattern.

## Preserved strengths (a builder must keep)
1. Honest nulls: "No read yet" never shown as Wait (l.28, l.105); a null rank stays null (l.57, l.59); unowned fields marked, never invented (l.26–31).
2. Three-cause empty Action Desk from R6 §14.1 (l.42), warning treatment only for required-source loss (l.374).
3. Watch is the only live action, through WatchStore, owner readback, no Prophet-only store (l.353–356); disabled controls visible with a reason, never upsell-shaped.
4. Producer nomination kept separate from tracked episode, no lifecycle color on nominations (l.51, l.122) — fits D02's closed vocabulary.
5. Hydration refuses a mismatched generation (l.327); the track record never pools into one win rate (l.73).

## Promises the current codebase cannot back
B4 availability on any surface (no template consumer) · a three-sleeve selector (one strategy, CONTROL_ONLY) · a Promoted view · live invalidation/pulse/"Updated" reorder (staged, not armed, `DEC D11:49–51`) · Pass/Reason/Thesis/Milestone/Review and the "arrive after" promise (`DEC D05:10–11, 52–54`) · episode-level save (`DEC D05:8–9`) · Radar rows becoming tracked episodes (`DEC D02:15–17, 51–53`) · the frozen original decision / "then-known vs later" (l.31, l.74–76; UNAVAILABLE_FIELD) · group economics, event/expectation detail, tail estimates (l.26–29, l.68) · canonical `.mx-tbl/.mx-tabset/.mx-callout/.mx-disc/.mx-rail/.qual2` (none in theme.css) · not verified: forecast/calibration producer (l.99, l.375), publication-generation identity (l.84, l.327).

## Required repairs as returned
Design judgment (designer or Fable): R1 (B2) per-surface dark/light + degraded states + baseline + mockups/evidence plan; R2 (M1, M2) state→stance→color→row anatomy, green ENTRY_OPEN only; R3 (M4, M5) drawer vs detail page, route topology, archetype per route, tab placement 1440/390; R4 (M6) header band vs shared nav; R5 (M8) holdings/sleeve surface + onboarding; R6 (M9) static bake as default; R7 (M14) reconcile with MP-1 and ERC.
Mechanical (builder from this text): R8 (B1, B3) rebind to `availability_state`, remove green from health/direction, rewrite disabled reasons to "Not saved yet / 暂未保存"; R9 (M3) recount new components (≥8), scope theme.css landing; R10 (M7) replace the toggle with a read-only "Research only" chip; R11 (M8, M10) add the missing D09 tasks and the pending/unwatch/tombstone states + ticker-level "Saved" copy; R12 (M11–M13, m1) remove jargon/telemetry, complete ZH twins, add TierLock, name `us_board_gate.panels`, fit word budgets.

## Evidence (reviewer's receipts)
- `git fetch origin refs/pull/7839/head:refs/remotes/pr/7839` → `366533a9c21f`; `git show … | cat -n` (402 lines).
- theme.css counts: `mx-tbl` 0, `mx-tabset` 0, `mx-rail` 0, `mx-disc` 0, `qual2` 0, `mx-callout` 0, `--ink-tier` 0, `--mx-tier-accent` 0; specimen PROPOSED block `specimen.html:15–106`; `qual2` only in the specimen.
- `git grep -E 'ENTRY_OPEN|availability_state'` → `engine/` only, no templates. `prophet_strategy_definition.py`: single `STRATEGY_ID`, `CONTROL_ONLY`.
- Banned-vocabulary grep over packet l.86–108 and l.330–357: owner 12 · receipt/Receipts/凭据 11 · producer 6 · episode 5 · D05 3 · nomination 3 · calibrated 2 · promoted/晋级 3 · substitute read 1 · 轮次 1 · stage/阶段 0.

## Gaps (reviewer-declared)
Did not verify the packet's citations into `_prophet_card`, `_us_board_cards`, `_us_prophet_plan_cards`, `build_site.py`, `build_prophet.py`, `watchstore.js`, `watchlist.js`; did not render the specimen or any page (sparse tree); did not read master plan §20, work cards B07/B14/B17/B25, or the r6_program D05/D11 censuses beyond the DEC summaries; CLAUDE.md law citations by section, not line; turn-capped.

## Deviations
None. Read-only throughout: fetch into a remote-tracking ref, show, grep, scratch copy of the specimen. No edits, commits, comments, labels, or spawns.
