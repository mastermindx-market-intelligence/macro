# D03 Issuer-and-Event Source-Readiness Census

## SOURCE_SHA

- OBSERVED: `SOURCE_SHA = a1a0a05e6adcdf007170bceaa7fbcd3323e333fc`; command `git rev-parse HEAD`; output and rc=0.
- OBSERVED: HEAD was detached at that commit. This round-2 skeleton was subsequently pushed to `claude/pu-w2-d03-issuer-source-census` at `d17df01ed4337484f3c7ae60bea559e917fa5fdb`; all code/config citations use blob evidence from SOURCE_SHA.
- OBSERVED: the record path is `research/prophet_v4/r6_program/wave2/D03_ISSUER_EVENT_SOURCE_READINESS_CENSUS_2026-09-23.md`.

## ANCHORS READ

- OBSERVED, code/config contracts only: `engine/stock_identity/`, `engine/ledger_identity.py`, `engine/institutional_census/`, `engine/earnings_release/`, `engine/earnings_catalyst.py`, `engine/earnings_qual.py`, `engine/group_earnings.py`, `engine/earnings_transcript_intake.py`, `engine/earnings_blackout.py`, `engine/prophet_candidate_state_sources.py`, `engine/us_candidate_episode.py`, `engine/vintage_stamp.py`, and reviewer-named expectation plane `collectors/equity_revisions.py`.
- OBSERVED: governing files and rulings `config/identity_seams.yml`, `config/theme_graph_identity_breaks.yml`, `config/dataset_registry.yml`, `config/press_sources.yml`, `config/narrative_sources.yml`, `config/release_forecast_model_registry.yml`, `research/prophet_v4/r6_program/rulings/SEAT_RULING_R6-C-01_2026-09-23.md` and `SEAT_RULING_R6-D05-01_2026-09-23.md`.
- OBSERVED, verbatim boundary: D03 requires exact dates/semantics/vintage lineage/coverage/delistings/rights and costs, forbids backdating current sets, guessed lags, relabeled broad industry series and assumed licenses (`research/prophet_v4/r6_fable_meta_ceo_handoff/effective/DECISION_REGISTER.json:62-88@a1a0a05e6adc`). Q01 requires exact revisions and owner clocks, explicit correction/current-discovery/late-capture controls, and refuses any PIT path depending on a current body, later membership, future label or unproven clock (`research/prophet_v4/r6_fable_meta_ceo_handoff/effective/RESEARCH_DOCKET.json:6-15@a1a0a05e6adc`). C-01 accepts consensus as unlicensed, requires truth about mixed `fact:revenue` units, and does not authorize dispatch or promotion (`research/prophet_v4/r6_program/rulings/SEAT_RULING_R6-C-01_2026-09-23.md:7-26@a1a0a05e6adc`); D05 neither promotes a signal nor authorizes DDL (`research/prophet_v4/r6_program/rulings/SEAT_RULING_R6-D05-01_2026-09-23.md:18-21@a1a0a05e6adc`).
- OBSERVED, measurement anchor: `research/prophet_v4/r6_fable_meta_ceo_handoff/baseline_r5/inputs/PROPHET_US_MEASUREMENT_R2.md:3-12@a1a0a05e6adc` binds R2 as a proposed, unregistered measurement design rather than a trial, production implementation, source repair or authorization. Its source/coverage/rights boundary requires named identity epochs, immutable generations and real owner clocks (`research/prophet_v4/r6_fable_meta_ceo_handoff/baseline_r5/inputs/PROPHET_US_MEASUREMENT_R2.md:58-84@a1a0a05e6adc`); historical research remains bounded by fields, dates, rights, missingness, corrections and consumers (`research/prophet_v4/r6_fable_meta_ceo_handoff/baseline_r5/inputs/PROPHET_US_MEASUREMENT_R2.md:278-294@a1a0a05e6adc`). VERDICT CHANGE: no Q1–Q6 verdict change; it confirms that a code/store capability is not historical corpus readiness.
- OBSERVED, early-leadership anchor: `research/prophet_v4/r6_fable_meta_ceo_handoff/baseline_r5/inputs/PROPHET_US_EARLY_LEADERSHIP_R3.md:5-7@a1a0a05e6adc` binds R3 as proposed/not registered and fixes US as the proving market while deferring CN/HK/CA. Its episode/coverage boundary distinguishes owner-native pre-B1 sightings and suppressions from canonical B1 episodes and forbids surrogate origins (`research/prophet_v4/r6_fable_meta_ceo_handoff/baseline_r5/inputs/PROPHET_US_EARLY_LEADERSHIP_R3.md:35-47@a1a0a05e6adc`, `research/prophet_v4/r6_fable_meta_ceo_handoff/baseline_r5/inputs/PROPHET_US_EARLY_LEADERSHIP_R3.md:63-68@a1a0a05e6adc`); entitlement and field-level vendor rights are separate gates (`research/prophet_v4/r6_fable_meta_ceo_handoff/baseline_r5/inputs/PROPHET_US_EARLY_LEADERSHIP_R3.md:290-294@a1a0a05e6adc`). VERDICT CHANGE: no Q1–Q6 verdict change; it reinforces that bounded rosters and current entitlement do not become universal PIT coverage.

## Q1 IDENTITY HISTORY

- VERDICT: **FOUND, but split and incomplete.** Dated ticker aliases and two curated event registries exist; issuer lineage and general lifecycle history do not.
- OBSERVED: the registered spine has five PRODUCED, on-demand, DOMAIN_AUTHORITY datasets: `reference.security_master` (grain `[security_id]`; mint-once; issuer state/evidence; effective and ingest clocks; superseded duplicate-mint state), `reference.vendor_aliases` (dated aliases), `reference.issuer_master`, and append-only issuer/security migration receipts (`config/dataset_registry.yml:252-566@a1a0a05e6adc`).
- OBSERVED: issuer CIK linkage is explicitly not historical. The master's own notes say “Historical issuer lineage is explicitly UNAVAILABLE” and that CIK evidence is a current-registrant observation with no `asof` (`config/dataset_registry.yml:360-365@a1a0a05e6adc`); `IssuerMaster` answers only which securities an issuer owns TODAY (`config/dataset_registry.yml:476-485@a1a0a05e6adc`).
- OBSERVED: the identity seam declares no general lifecycle model (`config/identity_seams.yml:49-79@a1a0a05e6adc` and `config/identity_seams.yml:94@a1a0a05e6adc`). Its nullable security state models only a duplicate-mint correction, not delisting existence.
- OBSERVED: `reference.vendor_aliases` has grain `[vendor, vendor_symbol, valid_from]`, an inclusive/open `valid_from`, and an exclusive/open `valid_to`; it explicitly distinguishes historical naming spaces (`yahoo`, `membership`, `ledger`) from current fetch/store catalogs (`config/dataset_registry.yml:383-446@a1a0a05e6adc`). Thus a reader can answer what a given space called a security on a date for covered names and dates.
- OBSERVED: `reference.issuer_migrations` and `reference.security_migrations` are append-only correction receipts with old/new values, reasons, evidence and migration time—not a general event source (`config/dataset_registry.yml:487-565@a1a0a05e6adc`).
- OBSERVED: curated ticker continuations are in `config.yml` `quality.ticker_key_migrations`; SATS→ECHO is machine-readable, while EQR→VMRK is deliberately withheld pending adjudication (`config.yml:3978-3997@a1a0a05e6adc`). The current-only ledger reader resolves transitively to today's key and exposes no dates (`engine/ledger_identity.py:125-179@a1a0a05e6adc`).
- OBSERVED: `config/delisted_symbols.yml` is a curated acquisition/delisting registry with six symbols (AVB, CTRA, FBRX, LEG, TPH, TWO). The first five entries and their verified clocks, consideration, successor fields and SEC/exchange receipts are visible in the file's admitted range (`config/delisted_symbols.yml:48-134@a1a0a05e6adc`); the sixth (TWO) and the deliberate still-printing-liquidation boundary are in the file's closing range (`config/delisted_symbols.yml:159-170@a1a0a05e6adc`, `config/delisted_symbols.yml:197-224@a1a0a05e6adc`). No seventh row is asserted.
- OBSERVED: theme-graph identity breaks are append-only, operator-ratified ticker-reuse splits, with break date, retired node, new epoch, evidence and ratification; ABX and GOLD are the only rows (`config/theme_graph_identity_breaks.yml:1-79@a1a0a05e6adc`).
- OBSERVED: the institutional census source is SEC 13F evidence, not an issuer-lifecycle table; its source contract preserves as-filed filing/submission/cover-page evidence with SEC source clocks and amendment forms (`engine/institutional_census/sec_sources.py:1-46@a1a0a05e6adc`, `engine/institutional_census/sec_sources.py:139-175@a1a0a05e6adc`).
- INFERRED: at a past date, PIT alias identity is reconstructable only from dated alias/history registries; issuer-level identity is not. A delisting event can be found only if manually registered, and ticker reuse only if ratified. A reader must fail closed for uncovered names/dates.
- UNKNOWN: first-date coverage counts versus the original historical universe. The sparse worktree lacks `data/reference/security_master.parquet`, `data/reference/vendor_aliases.parquet`, `data/reference/_receipt.json`, the sidecar manifests and all historical bars; exact row counts, dead-issuer coverage and empirical first dates require those artifacts. Exact command: `python3 - <<'PY'\nimport pandas as pd\nfor p in ['data/reference/security_master.parquet','data/reference/vendor_aliases.parquet']:\n d=pd.read_parquet(p); print(p,len(d),d.columns.tolist(),d.head())\nPY` (not run; artifacts absent). Counts in prose configs are not universe coverage.
- UNKNOWN: general CUSIP/FIGI change events. SOURCE_SHA has no CUSIP or FIGI columns in the registered identity schema; text references only prove that the seam reasons about symbols/CUSIP in rename handling (`git grep -ni 'cusip\|figi' config/dataset_registry.yml config/identity_seams.yml lib/dataos/identity.py scripts/build_security_master.py @a1a0a05e6adc`). A dedicated identifier-change history would require an entitled identifier/corporate-actions source plus event/validity clocks.
- UNKNOWN: historical bankruptcy, spin-off and merger event families. The delisted config records six combinations of acquisition/delisting/successor, and theme breaks record two reuse splits; neither is a general corporate-action registry. Distinguishing a true merger successor from merely a later owner of the symbol requires the same missing issuer/event lineage.

## Q2 EARNINGS / FINANCIAL FACTS

- VERDICT: **FOUND as several unequal planes.** EDGAR filings/release bodies and transcript revisions support bounded PIT replay; the Nasdaq current calendar/surprise cache does not; group reactions and close panels are governed separately; guidance is diagnostic unless its source history is proven; the B1 episode writer itself consumes candidate/radar/identity inputs, not an earnings fact store.
- OBSERVED, Nasdaq calendar: one full sweep creates one `as_of` freshness stamp, but the calendar loop starts today and looks forward across 66 weekdays (`collectors/equity_earnings.py:31-41@a1a0a05e6adc`, `collectors/equity_earnings.py:158-181@a1a0a05e6adc`). It stores `next_date`, `next_time`, `eps_forecast`, surprise JSON and `surprises_as_of` (`collectors/equity_earnings.py:399-424@a1a0a05e6adc`). Surprise payloads carry fiscal-quarter end, report string, EPS, consensus and percent surprise (`collectors/equity_earnings.py:184-194@a1a0a05e6adc`), but are refreshed under a 120-name oldest-stamp rotation (`collectors/equity_earnings.py:219-247@a1a0a05e6adc`). CLASSIFICATION: current-only discovery plus retrospective diagnostic snippets; **not PIT** and not a guaranteed full history.
- OBSERVED, EDGAR 8-K dates: `filing_date` is the PIT-visible proxy, `acceptance_datetime` gives sub-day availability, and the one-shot historical backfill stores `(ticker,cik,accession,form,filing_date,acceptance_datetime,report_date,items)`, deduped by `(cik,accession)` (`collectors/edgar_earnings_8k.py:1-45@a1a0a05e6adc`, `collectors/edgar_earnings_8k.py:261-310@a1a0a05e6adc`). CLASSIFICATION: admissible PIT event/date replay for filings captured; processing/ingest latency is OBSERVED only if a downstream capture clock is present and UNKNOWN here from code alone.
- OBSERVED, release bodies: a filing is `(cik,accession)` and an earnings event is `(cik,report_date)`; source acceptance is normalized to UTC and explicitly distinguished from processing (`engine/earnings_release/binding.py:14-28@a1a0a05e6adc`, `engine/earnings_release/binding.py:57-76@a1a0a05e6adc`). `ReleaseRevision` stores form, filing date, acceptance, report date, body SHA and supersede link; `8-K/A` status is source-assigned (`engine/earnings_release/binding.py:79-128@a1a0a05e6adc`). Grouping attaches a correction to its reported event without date-window guessing (`engine/earnings_release/binding.py:331-380@a1a0a05e6adc`). CLASSIFICATION: admissible PIT replay when exact source bytes and revisions are captured; otherwise a diagnostic parser contract.
- OBSERVED, figures: deterministic extraction refuses a number without basis, units, period and source; roster includes revenue and revenue-guidance bounds; units can be `usd_thousands`/`usd_millions`/`usd_billions`; currency is declared rather than assumed; every figure carries an exact replayable receipt (`engine/earnings_release/figures.py:1-102@a1a0a05e6adc`, `engine/earnings_release/figures.py:239-323@a1a0a05e6adc`, `engine/earnings_release/figures.py:374-412@a1a0a05e6adc`, `engine/earnings_release/figures.py:476-502@a1a0a05e6adc`). Revenue is therefore semantically unit-bearing, **not normalized**. C-01 explicitly commissions admission-time normalization or mixed-unit refusal (`research/prophet_v4/r6_program/rulings/SEAT_RULING_R6-C-01_2026-09-23.md:20@a1a0a05e6adc`). CLASSIFICATION: body facts are PIT replay where source bytes exist; cross-document comparability is prospective/contract-only until C-UNITS lands.
- OBSERVED, event workspace: schema `event_workspace.v1` separates `observed_at` from `source_available_at` (`engine/company_intelligence/event_workspace.py:1-9@a1a0a05e6adc`, `engine/company_intelligence/event_workspace.py:269-276@a1a0a05e6adc`); all authority flags are false; `consensus_unlicensed` is a typed warning and beat/miss/basis-match is forbidden without licensed consensus (`engine/company_intelligence/event_workspace.py:54-106@a1a0a05e6adc`, `engine/company_intelligence/event_workspace.py:286-344@a1a0a05e6adc`). v2 manifests chain each immutable generation to predecessor ID and manifest SHA (`engine/company_intelligence/event_workspace.py:36-97@a1a0a05e6adc`, `engine/company_intelligence/event_workspace.py:503-602@a1a0a05e6adc`). CLASSIFICATION: observed-as-run with immutable generation lineage, not retrospective PIT recovery of events it never observed.
- OBSERVED, transcripts: the global index/body plane validates identity and canonical body SHA, queues changed hashes as new revisions, and keeps failed completion state (`engine/earnings_transcript_intake.py:1-125@a1a0a05e6adc`, `engine/earnings_transcript_intake.py:287-385@a1a0a05e6adc`, `engine/earnings_transcript_intake.py:447-492@a1a0a05e6adc`). Terminal-issued span receipts bind ticker/event/fiscal transcript, document/segment hashes and byte bounds, and resolve only the selected revision (`engine/earnings_transcript_intake.py:505-570@a1a0a05e6adc`, `engine/earnings_transcript_intake.py:620-690@a1a0a05e6adc`). CLASSIFICATION: content-addressed replay for archived bodies; first-use and archive backfill latencies are UNKNOWN without terminal/index records or data artifacts.
- OBSERVED, guidance: `engine/guidance_gap.py` consumes `data/edgar/guidance_hits.parquet`, requires ticker/direction/file date, reports recent hits and rolls a coarse raise/cut band over 90 days; its own header calls it a coarse directional band, not a numeric beat/miss, and notes no sentence-level negation handling (`engine/guidance_gap.py:1-42@a1a0a05e6adc`, `engine/guidance_gap.py:45-123@a1a0a05e6adc`). Release-body extraction is stricter and creates typed absences when guidance lacks explicit basis, magnitude or period (`engine/earnings_release/figures.py:744-840@a1a0a05e6adc`). CLASSIFICATION: current/diagnostic for guidance-gap; body guidance is replay only with the exact release bytes and remains non-normalized.
- OBSERVED, live blackout authority: `engine/earnings_blackout.py` reads `data/earnings/earnings.parquet`, keys by ticker, and requires `next_date >= today`; passed dates never veto (`engine/earnings_blackout.py:1-18@a1a0a05e6adc`, `engine/earnings_blackout.py:200-308@a1a0a05e6adc`). It vetoes only within 0–3 trading days and only when the row's `as_of` is no more than 10 trading days old; missing store/ticker, stale row, missing/as-of-unparseable row or calendar gap fails open (`engine/earnings_blackout.py:46-76@a1a0a05e6adc`, `engine/earnings_blackout.py:225-308@a1a0a05e6adc`). `next_time` is carried for display and is explicitly excluded from the verdict (`engine/earnings_blackout.py:15-18@a1a0a05e6adc`). CLASSIFICATION: live/current-only consumption of a mutable freshness store; not a historical earnings-fact replay contract.
- OBSERVED, display journey: `engine/earnings_catalyst.py` is pure display math over an existing blackout assessment plus closes and surprises (`engine/earnings_catalyst.py:1-15@a1a0a05e6adc`). It emits holiday-exact signed `days_to_report`, a 7-session soon flag and a tri-state `stale` derived from `as_of`; age >10 sessions suppresses countdowns (`engine/earnings_catalyst.py:50-60@a1a0a05e6adc`, `engine/earnings_catalyst.py:96-164@a1a0a05e6adc`). It derives the most recent passed report from the forward calendar's carried past date and/or Nasdaq `reported` dates (`engine/earnings_catalyst.py:198-219@a1a0a05e6adc`). Reaction semantics are deterministic: an after-hours token prices the next session; a pre-market or unknown token prices the report session; move is day-0 close divided by prior close (`engine/earnings_catalyst.py:239-309@a1a0a05e6adc`). The full board row exposes a fresh chip only within 14 sessions and otherwise a stale/out-of-window disclosure (`engine/earnings_catalyst.py:312-394@a1a0a05e6adc`). CLASSIFICATION: prospective display, observed-as-run, not PIT evidence.
- OBSERVED, group journey inputs: `engine/group_earnings.py` explicitly consumes Nasdaq `data/earnings/earnings.parquet`, deep EDGAR `data/edgar/earnings_8k_dates.parquet`, guidance hits, `data/revisions/latest.parquet`, member `data/baskets/ohlcv/<T>.parquet`, and Yahoo SPY closes from `lib.store`; it opens no network at build time (`engine/group_earnings.py:31-48@a1a0a05e6adc`). It uses EDGAR acceptance time to classify reactions: before 14:30 UTC prices the first session on/after that date, at/after 14:30 UTC prices the first later session; an untimed Nasdaq `reported` date is after-hours unless a timed 8-K within three sessions wins (`engine/group_earnings.py:52-62@a1a0a05e6adc`). A member counts reported when the latest reaction is within 75 sessions; results require at least four classified members (`engine/group_earnings.py:64-77@a1a0a05e6adc`). Its drift/sympathy legs use 5/75-session and 8-quarter windows with explicit floors and n disclosures (`engine/group_earnings.py:91-106@a1a0a05e6adc`). CLASSIFICATION: display/context-only (`engine/group_earnings.py:1-20@a1a0a05e6adc`), prospective analysis over mixed planes—not by itself PIT replay. EDGAR dates are bounded PIT; Nasdaq surprises/current revisions are diagnostic/current discovery; adjusted basket OHLCV carries vendor recomputation semantics discussed under Q5.
- OBSERVED, group coverage/correction limits: the module's own 2026-08-08 measurement says the 8-K spine is deep but Nasdaq surprise history is thin—715 of 1,976 names and 201 of 653 live members—and `data/edgar/eps_quarterly.parquet` is deliberately not substituted because it lacks consensus (`engine/group_earnings.py:108-117@a1a0a05e6adc`). The nightly sympathy ledger is one immutable row per `(basket_id,event_date,reporter_ticker)` and may add event days but never rewrite existing ones (`engine/group_earnings.py:127-129@a1a0a05e6adc`). Exact current completeness is UNKNOWN because `data/` is absent.
- OBSERVED, family boundary: `engine/earnings_qual.py` also contains the cold-start earnings-call scoring/ladder consumed by the Stage surfaces. It is distinct from the calendar/blackout family above and is censused in the following subsection.

### Earnings-call history ladder (repair r3)

- VERDICT: **PARTIALLY FOUND, prospective-only overlay.** The committed historical fallback plane and the live scoring contract are structurally known; actual row/quarter coverage, the R2 history store, transcript corpus, manifest and provider rights are UNKNOWN in this sparse read-only lane. The ladder is display/context-only and cannot clear historical B04/B08/B09 input admissibility from code alone.
- OBSERVED, `data/earnings_calls/scores.parquet` (R2-transport, gitignored): the producer store lives at that path (`engine/earnings_qual.py:1217-1219@a1a0a05e6adc`) and has explicit ticker/quarter/year/source, model scores and confidence, summary/highlights/tags, body and revision hashes, source identity/update metadata, prompt/schema versions, context/degraded state, and provider-fallback provenance (`engine/earnings_qual.py:1194-1214@a1a0a05e6adc`). A strict read accepts absent/unreadable stores as schema-valid empty frames and rejects a manifest-mismatched transport generation (`engine/earnings_qual.py:1231-1246@a1a0a05e6adc`, `engine/earnings_qual.py:1835-1899@a1a0a05e6adc`). Every fresh score is stamped at scoring time and is always context-only (`engine/earnings_qual.py:920-947@a1a0a05e6adc`); sentiment/performance are clipped to signed 1 and 0–10 and incomplete scores remain degraded (`engine/earnings_qual.py:1060-1079@a1a0a05e6adc`). The CLI names this the cold-start/ops convenience and explicitly not the render path (`engine/earnings_qual.py:3397-3418@a1a0a05e6adc`). GENERATION VERSUS TRANSPORT CLOCK: a row is generated when a transcript/8-K score succeeds and `score_new` writes it; it becomes checkout-visible only after a later manifest-last R2 transport or a deliberate producer merge (`engine/earnings_qual.py:1359-1375@a1a0a05e6adc`, `engine/earnings_qual.py:3397-3418@a1a0a05e6adc`). CORRECTIONS: producer merges/upserts replace the single current score row for the same stable `source_record_id` (legacy ticker/quarter/year/source otherwise), prefer healthy over degraded rows, then use source/scoring freshness and arrival order (`engine/earnings_qual.py:1290-1330@a1a0a05e6adc`, `engine/earnings_qual.py:1359-1375@a1a0a05e6adc`); it is a latest-row store without an observable historical-vintage chain. COVERAGE: exact issuers/quarters are UNKNOWN (`test -e data/earnings_calls/scores.parquet` → absent; no producer run). RIGHTS/COST: NOT REGISTERED—no source entitlement, provider right or cost row was observed for this scored-output store in `config/dataset_registry.yml` or `config/press_sources.yml`. ADMISSIBILITY: scores alone are **prospective-only context**, not B04/B08/B09 historical evidence.
- OBSERVED, `data/earnings_calls/transcripts/*.json`: the local cold-start input contract is flat JSON with ticker, quarter, year, call date and text; malformed or non-dict files are skipped (`engine/earnings_qual.py:1416-1438@a1a0a05e6adc`). `score_new(auto)` prefers these inputs and only fills the batch with EDGAR 8-K text when transcripts are absent or incomplete (`engine/earnings_qual.py:1593-1636@a1a0a05e6adc`, `engine/earnings_qual.py:1661-1688@a1a0a05e6adc`). A separate Terminal archive plane carries full text, revision/body hashes, content hashes and span byte bounds (`engine/earnings_transcript_intake.py:430-502@a1a0a05e6adc`, `engine/earnings_transcript_intake.py:505-570@a1a0a05e6adc`), while the Finnhub metadata catalog appends provider IDs with first-seen clocks and fetches no nightly bodies (`collectors/finnhub_transcripts.py:1-21@a1a0a05e6adc`, `collectors/finnhub_transcripts.py:64-83@a1a0a05e6adc`). GENERATION VERSUS TRANSPORT CLOCK: a JSON is generated when a later file supplier places it; scoring occurs only when `score_new` runs; its scored output lands when the scores row is written/published as above. CORRECTIONS: the flat scorer does not maintain a JSON revision graph—the text hash prevents rescoring, while a changed filename/body can create a new source event; the archive plane does version by transcript/revision IDs. COVERAGE: exact corpus/issuers/quarters are UNKNOWN (`find data/earnings_calls/transcripts -type f -name '*.json' 2>/dev/null | wc -l` → absent/no path). RIGHTS/COST: NOT REGISTERED for the local `earnings_calls/transcripts` scorer contract; `config/press_sources.yml` registers only a hearsay X account that quotes calls (`config/press_sources.yml:293-295@a1a0a05e6adc`) and Finnhub's collector code records plan/permission limits but no entitlement/cost receipt (`collectors/finnhub_transcripts.py:11-20@a1a0a05e6adc`). ADMISSIBILITY: flat JSON is prospective-only and archive history is **UNKNOWN** pending corpus/receipt evidence; not FOUND for historical B04/B08/B09.
- OBSERVED, `data/earnings_calls/manifest.json`: it is the published-generation commit marker for transported scores/history (`engine/earnings_qual.py:1249-1256@a1a0a05e6adc`, `engine/earnings_qual.py:1799-1806@a1a0a05e6adc`). Validation requires schema v2/v3, hashes the manifest's score/history MD5s into a generation ID, checks immutable R2 v3 keys, reconciliation arithmetic and per-block MD5/bytes/rows/tickers (`engine/earnings_qual.py:1835-1899@a1a0a05e6adc`). A producer mutation intentionally invalidates the old marker (`engine/earnings_qual.py:1333-1337@a1a0a05e6adc`). GENERATION VERSUS TRANSPORT CLOCK: the marker is written after a transport generation is materialized; consumer visibility and strict validation begin with that marker-last publication. CORRECTIONS: a new manifest publishes a new generation; the old marker is removed and the manifest does not itself preserve a vintage history. COVERAGE: manifest absence currently proves no R2 generation in this checkout, not zero rows elsewhere; row/issuer/quarter coverage is UNKNOWN (`test -e data/earnings_calls/manifest.json` → absent). RIGHTS/COST: NOT REGISTERED as a source; it is a transport integrity artifact. ADMISSIBILITY: a valid manifest can qualify transported generations but is **UNKNOWN** here; it is not independently sufficient for B04/B08/B09.
- OBSERVED, `data/stage_analysis/backfill/earnings_calls.parquet` (legacy backfill): the surface comment says it is a committed EquityDesk seed with one company-quarter row over full history, 2019→now, alongside `ec_industry.parquet`, and going forward merges with the live score store (`engine/earnings_qual.py:1694-1718@a1a0a05e6adc`). In the actual reader ladder, however, the canonical first tier is manifest-validated `data/earnings_calls/history.parquet`, followed by this legacy full-history parquet and then two committed overview/score-seed fallbacks (`engine/earnings_qual.py:1750-1776@a1a0a05e6adc`, `engine/earnings_qual.py:2456-2523@a1a0a05e6adc`). Thus the named `backfill/earnings_calls.parquet` can serve only when the R2 history is absent/unvalidated; its projection is marked ready and its latest call age controls ready/stale (`engine/earnings_qual.py:2310-2347@a1a0a05e6adc`). The importer records all 6,536 overview rows/all regions and normalizes EquityDesk sentiment/performance with pinned formulas (`scripts/import_equitydesk_backfill.py:1-38@a1a0a05e6adc`); it deduplicates score seeds by ticker/quarter/year/source and writes the committed seed (`scripts/import_equitydesk_backfill.py:578-599@a1a0a05e6adc`). Its join accepts exact document/tradingview tickers, then cleans exchange suffixes as a fallback (`scripts/import_equitydesk_backfill.py:160-198@a1a0a05e6adc`). GENERATION VERSUS TRANSPORT CLOCK: the EquityDesk snapshot was generated outside this repository and imported when the script ran; checkout visibility is import/checkout time, not call time, and the observed importer has no per-row acquisition clock. CORRECTIONS: importer dedupe keeps the last source row per legacy identity; the reader prefers later tiers by ladder order and overlays only genuinely new ticker/date score events (`engine/earnings_qual.py:2350-2453@a1a0a05e6adc`). It rewrites the current projection rather than preserving per-import vintage. COVERAGE: claimed shape is one row per company-quarter, full history 2019→now, all-region overview input, but actual issuers/quarters/delisting inclusion are UNKNOWN because this sparse checkout has no data artifacts (`test -e data/stage_analysis/backfill/earnings_calls.parquet` → absent; importer source receipts are external `overview.json`/`earnings.json` and were not read). RIGHTS/COST: NOT REGISTERED—no EquityDesk license/cost row exists in `config/dataset_registry.yml` or `config/press_sources.yml`. ADMISSIBILITY: **UNKNOWN/not FOUND for historical B04/B08/B09** from this lane; structure is diagnostic/display context and lacks acquisition/rights provenance.
- OBSERVED, projected overview / score-seed fallback: absent or rejected history falls to a one-recent-call overview or compact score seed so a fresh checkout does not silently render zero rows (`engine/earnings_qual.py:1754-1776@a1a0a05e6adc`). The compact seed projects live `sentiment` to `earnings_call_sent` with `sent * 18 + 12`, performance to `perf * 2.4 - 12`, combines them, and enriches only a current issuer/GICS dictionary (`engine/earnings_qual.py:2194-2253@a1a0a05e6adc`). The overview lane drops nightly non-vendor rows and often leaves fiscal period null (`engine/earnings_qual.py:2254-2276@a1a0a05e6adc`). Both fallback tiers are typed degraded, not ready (`engine/earnings_qual.py:2308-2347@a1a0a05e6adc`); published EC tone converts the desk-30 projection through neutral 12 (`engine/earnings_qual.py:2073-2096@a1a0a05e6adc`). GENERATION VERSUS TRANSPORT CLOCK: projection happens at reader/build time from committed fallback stores, with no fresh acquisition event. CORRECTIONS: each read projects the current fallback; no projection vintage is retained. COVERAGE: one recent call per covered name for overview and whatever seed names/quarters survive; exact counts and original-universe/delisting boundary are UNKNOWN in the sparse tree. RIGHTS/COST: NOT REGISTERED. ADMISSIBILITY: **NOT FOUND for historical B04/B08/B09**—explicit degraded display fallback only.
- OBSERVED, `earnings_call_sent` definition: the projected compact-seed field is literally `(sentiment * 18.0 + 12.0).clip(0.0, 30.0)` (`engine/earnings_qual.py:2217@a1a0a05e6adc`), with combined tone+performance (`engine/earnings_qual.py:2219-2221@a1a0a05e6adc`). The publisher documents native desk30 as approximately −10..30 with neutral 12 and inverts `sentiment = clip((sent − 12) / 18, −1, 1)` (`engine/earnings_qual.py:2073-2085@a1a0a05e6adc`). This projected 0–30 scale is deliberately distinct from the live scorer's signed −1..1 `sentiment` (`engine/earnings_qual.py:2079-2085@a1a0a05e6adc`), and the kill registry explicitly forbids re-pointing a calibrated EC gate at that different construction (`config/compiled_kill_registry.yml:823@a1a0a05e6adc`).
- UNKNOWN, actual ladder coverage: every exact issuer/quarter, original-universe membership, delisted-name inclusion/exclusion and row count needs the R2 history, legacy backfill, overview and score-seed stores plus their manifests/import receipts; the only files under the named tree in SOURCE_SHA are committed overview and score-seed fallbacks (`git ls-tree a1a0a05e6adcdf007170bceaa7fbcd3323e333fc data/stage_analysis/backfill` → the two fallback parquet files), and the run commands above found none of the named stores in the sparse checkout. No producer was run.
- OBSERVED, B1 writer inputs: the registered episode event store names security master, vendor aliases, issuer master, turn-watch input, context-vector candidates, door flags and entry-radar forward events as inputs; known-at precedes recorded-at, identity must be pinned, and present state may never be projected backward (`config/dataset_registry.yml:762-791@a1a0a05e6adc`). The reducer validates B1/turn-watch relations with occurred/known/recorded clocks and hash receipts (`engine/prophet_candidate_state_sources.py:170-280@a1a0a05e6adc`). CLASSIFICATION: prospective/observed-as-run episode ledger; historical intake requires pinned identity and is blocked by the current-only issuer seam unless an independent pinned path supplies it.

## Q3 COMPARABLE EVENTS AND EXPECTATIONS

- VERDICT: **PARTIAL.** A macro CPI expectation/vintage route is PIT-qualified; a prospective Yahoo/yfinance EPS/revenue expectation accrual exists alongside a legacy revision-breadth current plane; issuer licensed consensus remains absent; option OI and press/narrative streams are source-availability or current accrual planes, not comparable-event replay histories.
- OBSERVED, macro forecasts: `release_forecast_model_registry.v1` contains only `coherent_ridge_v1`, targeting CPI headline/core and marked `shadow_candidate` (`config/release_forecast_model_registry.yml:1-13@a1a0a05e6adc`). Preregistration is frozen before forward accrual and performance is excluded from the spec (`config/release_forecast_model_registry.yml:15-21@a1a0a05e6adc`). Its target is the ALFRED same-release vintage proxy, requires completed parity, forbids cross-vintage reconstruction, and binds history/parity/completion receipts by path/hash/bytes/evidence clock (`config/release_forecast_model_registry.yml:23-66@a1a0a05e6adc`). Features require release date no later than decision as-of and exact period rules (`config/release_forecast_model_registry.yml:87-123@a1a0a05e6adc`). CLASSIFICATION: admissible PIT replay for the declared macro vintage chain; **not issuer-grain**.
- OBSERVED, legacy Yahoo revision breadth: `collectors/equity_revisions.py` states that Yahoo supplies only the current snapshot, no history; `latest.parquet` is the live score and `history.parquet` is append-only daily snapshots for forward accrual (`collectors/equity_revisions.py:6-12@a1a0a05e6adc`). The legacy row carries 30/90-day EPS revisions and drift, reviser/covering counts, dispersion and forward revenue estimate fields; revenue 30/90-day revision columns are structurally unavailable and omitted (`collectors/equity_revisions.py:14-48@a1a0a05e6adc`). The drip refreshes at most a bounded stale-name batch, keeps `latest` keyed by ticker, and appends/dedupes `(date,ticker)` snapshots to history (`collectors/equity_revisions.py:729-790@a1a0a05e6adc`). CLASSIFICATION: `latest` is current-only discovery; `history` is forward PIT accrual from collection dates onward. It cannot reconstruct snapshots missed by the drip and has no proven provider publication clock in that legacy plane.
- OBSERVED, SRC-A1 EPS/revenue expectation observations: `collectors/equity_revisions.py` also writes additive `expectation_observations.parquet` and `expectation_attempts.parquet`; observation metrics are EPS and revenue with observation types `average`, `median`, `high`, `low`, `covering_analyst_count`, `growth` and `year_ago` (`collectors/equity_revisions.py:84-106@a1a0a05e6adc`). Each row stores provider/record class, payload hash, raw horizon, provider `period_end`, typed missingness, aggregation level, four distinct clocks, correction lineage and `rights_class: UNKNOWN` (`collectors/equity_revisions.py:285-365@a1a0a05e6adc`). The documented matrix defines `source_effective_at`, `source_published_at`, `provider_observed_at`, `system_observed_at` and `market_session` as non-aliases, requires append/supersede-only corrections, and forbids contributor inference and current-snapshot backfill (`research/alpha_intelligence/expectation_market_dynamics/DATA_CLOCK_RIGHTS_MATRIX.md:76-96@a1a0a05e6adc`, `research/alpha_intelligence/expectation_market_dynamics/DATA_CLOCK_RIGHTS_MATRIX.md:98-112@a1a0a05e6adc`, `research/alpha_intelligence/expectation_market_dynamics/DATA_CLOCK_RIGHTS_MATRIX.md:31-39@a1a0a05e6adc`). The implementation rejects a historical `system_observed_at` more than 60 seconds from now as a current-snapshot backfill (`collectors/equity_revisions.py:425-448@a1a0a05e6adc`). Changed same-period values supersede; differing provider period ends begin a new original instead of a false fiscal rollover revision (`collectors/equity_revisions.py:384-423@a1a0a05e6adc`). CLASSIFICATION: prospective, append-only, bounded PIT if started before any claim; `source_effective_at/source_published_at` are currently null because Yahoo estimate accessors do not expose them, so known-at is collector/system time, not provider publication time.
- OBSERVED, issuer licensed consensus: the event workspace types consensus as unlicensed and forbids beat/miss without basis match (`engine/company_intelligence/event_workspace.py:54-106@a1a0a05e6adc`, `engine/company_intelligence/event_workspace.py:338-344@a1a0a05e6adc`); C-01 ratifies the permanent absence and assigns the silent rights block to EIO (`research/prophet_v4/r6_program/rulings/SEAT_RULING_R6-C-01_2026-09-23.md:14@a1a0a05e6adc`). The Nasdaq surprise rows include consensus but are current-only drips (`collectors/equity_earnings.py:184-194@a1a0a05e6adc`, `collectors/equity_earnings.py:365-424@a1a0a05e6adc`). CLASSIFICATION: licensed consensus NOT FOUND; Nasdaq consensus diagnostic only, not a revision-history expectation source; Yahoo is a prospective accrual, not an entitlement-cleared expansion.
- OBSERVED, option OI: the Massive SPY option snapshot config explicitly says its open-interest basis is end-of-last-trading-day, qualitative only, unauthenticated by response, and that measurement/publication date/SLA are unavailable; scope is first page, source availability only, future only, with calendar and OI projection forbidden (`config/market_memory_option_oi_source.v1.json:18-40@a1a0a05e6adc`). CLASSIFICATION: prospective source-availability signal only; **not PIT**, not an event history, not issuer grain.
- OBSERVED, market-memory SPY experience: v1 requires local observation before a cutoff and a same-head sandwich under a monotone append-only owner protocol, with no fabricated market close; v2 requires three stable REST observations in a D+1 04:00–04:05Z seal and marks the evidence policy prospective-only and training-ineligible (`config/market_memory_spy_experience_registration.v1.json:1@a1a0a05e6adc`, `config/market_memory_spy_experience_registration.v2.json:1@a1a0a05e6adc`). CLASSIFICATION: prospective-only accrual with provenance clocks; not a historical comparable-event source.
- OBSERVED, press/narrative: `press_sources.yml` ships dark by default and permits only documented published endpoints—mirror RSS/JSON archive and a read relay—not first-party scraping, browser automation, cookie auth or HTML scraping; corroboration distinguishes a direct-quote primary from at least two independent sources for hearsay (`config/press_sources.yml:1-23@a1a0a05e6adc`). Poll intervals are present (75 seconds hot, 6 hours archive backfill) but no immutable revision/publication vintage contract is declared (`config/press_sources.yml:42-64@a1a0a05e6adc`). `narrative_sources.yml` declares RSS/relay/search/EDGAR-velocity lanes and a 90-day EDGAR velocity window (`config/narrative_sources.yml:1-60@a1a0a05e6adc`, `config/narrative_sources.yml:138-208@a1a0a05e6adc`). CLASSIFICATION: prospective/event-detection and diagnostic accrual; not PIT comparable-event replay unless a separate immutable receipt chain is added.
- OBSERVED, entry radar: the registered forward-events row is PROPOSED/`STAGED_NOT_ARMED`, uses a nightly decision session plus `signal_known_ts`, and requires freezing an immutable event-ID contract before production; B1 may relate but not mint or promote it (`config/dataset_registry.yml:696-728@a1a0a05e6adc`). `config/entry_radar.yml` layers index membership, liquidity and attention admission with producer staleness and unavailable-producer retention (`config/entry_radar.yml:43-117@a1a0a05e6adc`). CLASSIFICATION: prospective event discovery, no comparable-event history and no production contract at SOURCE_SHA.
- INFERRED: the only comparable-expectation branch currently ready for historical replay is the declared macro CPI vintage path. Yahoo observations are accrual candidates but are not rights-cleared. Comparable issuer events need either exact captured release/event generations with known-at clocks or a newly entitled consensus/corporate-event vendor that records revision and publication clocks. Those sources are named only, not present.
- UNKNOWN: provider contract, revision frequency, revision history, coverage strata and costs for issuer consensus or corporate comparable-event vendors. Required command/artifact: an executed vendor entitlement plus source documentation containing revision/publication semantics and a licensed bulk historical endpoint; no such document or dataset row is present at SOURCE_SHA. For Yahoo, actual coverage/attempt counts require `data/revisions/*` artifacts, which are absent.

## Q4 GROUP / PEER MEMBERSHIP

- VERDICT: **PARTIAL.** Curated-basket/THS histories and S&P 1500 intervals can answer as-of queries; theme graph backfills are labeled reconstruction; GICS sector/industry is current-only and must never be backdated.
- OBSERVED, basket PIT store: one row per `(snapshot_date,basket_id,ticker)`, keep-first, append-only; content-dedup stamps only changed sets; member `added`/`removed` dates support reads between snapshots; each suite has a fail-closed collection lane (`engine/basket_membership_pit.py:14-59@a1a0a05e6adc`, `engine/basket_membership_pit.py:108-145@a1a0a05e6adc`, `engine/basket_membership_pit.py:440-489@a1a0a05e6adc`, `engine/basket_membership_pit.py:492-529@a1a0a05e6adc`).
- OBSERVED, as-of semantics: `members_asof` selects the newest stored snapshot no later than the requested date and returns `pit=True` only on that basis. If uncovered, it falls back to current membership and returns `pit=False`, with an explicit warning that using it contaminates the study (`engine/basket_membership_pit.py:630-700@a1a0a05e6adc`). This is a compliant refusal design, not permission to backdate.
- OBSERVED, coverage limits: the CN side was seeded from two dated sidecars whose shapes differ—a raw vendor concept dump and a seeded membership copy—so cross-boundary drift conflates population change with the seeding cap (`engine/basket_membership_pit.py:58-80@a1a0a05e6adc`, `engine/basket_membership_pit.py:532-583@a1a0a05e6adc`). The US curated suite is advanced by the nightly lane; its earliest snapshot must come from the store artifact, which is unavailable in this sparse tree.
- OBSERVED, theme graph: `MEMBER_OF` edges are intervals `[added,removed)`; backfills carry `era="reconstruction"`, `belief_time=run date`, and separate real dated changelogs from a seed constant that marks a series start rather than a join date (`engine/theme_graph/materialize.py:1-29@a1a0a05e6adc`, `engine/theme_graph/materialize.py:332-361@a1a0a05e6adc`). The store keeps the opening row and closes an interval in a later belief row; current collapse uses max belief deterministically (`engine/theme_graph/store.py:1-15@a1a0a05e6adc`, `engine/theme_graph/store.py:114-121@a1a0a05e6adc`, `engine/theme_graph/store.py:278-290@a1a0a05e6adc`). CLASSIFICATION: PIT only where an observed/dated source row exists; reconstruction rows are diagnostics, never historical knowledge.
- OBSERVED, S&P 1500: `scripts/midsmall_pit.py` reconstructs intervals from Wikipedia current constituents plus changes logs and unions S&P 400/600 with an existing S&P 500 PIT store. It states the coverage ceiling—S&P 400 from about 2012, S&P 600 from about 2019—and marks pre-log members as “at least since” the log floor rather than recovering earlier starts (`scripts/midsmall_pit.py:1-23@a1a0a05e6adc`). The interval walk reconciles open intervals to current constituents and classifies unplaceable adds as orphan additions (`scripts/midsmall_pit.py:52-102@a1a0a05e6adc`). CLASSIFICATION: bounded PIT replay from log start; earlier history and unplaceable changes are irreducible gaps.
- OBSERVED, GICS/current sector: `engine/qledger.py` reads a current `membership.parquet` mapping only `ticker` to `sector`, caches it, and performs no as-of query (`engine/qledger.py:1515-1560@a1a0a05e6adc`). EquityDesk earnings backfill carries GICS sector/industry-group/industry/subindustry fields on rows, but those are row attributes without an historical membership interval contract (`engine/earnings_qual.py:2456-2473@a1a0a05e6adc`). CLASSIFICATION: current-only/diagnostic; **current GICS backdated is forbidden**.
- INFERRED: a compliant historical sector/industry membership table needs (1) issuer/security identity keyed with effective and knowable dates; (2) original vendor publication/announcement clock separate from effective date; (3) immutable add/remove or revision receipts; (4) coverage against the original universe including delisted issuers; and (5) rights for storage, model use and redistribution.

## Q5 RIGHTS

- Answer format: five rights are Acquisition / Processing / Storage / Model use / User redistribution. “Recorded” means the repository states it; absence is UNKNOWN, never an inferred license.

### SEC EDGAR filings and 13F evidence
- OBSERVED: acquisition is by public SEC endpoints with documented pacing and parsing (`collectors/edgar_earnings_8k.py:173-260@a1a0a05e6adc`; `engine/institutional_census/sec_sources.py:41-78@a1a0a05e6adc`).
- INFERRED: processing may retain and normalize as-filed content because the contracts explicitly preserve source artifacts, hashes and clocks (`engine/earnings_release/binding.py:79-128@a1a0a05e6adc`; `engine/institutional_census/models.py` and storage receipts enforce immutable object digest/length).
- UNKNOWN: no `license`/`rights` field specifically grants processing or model use for EDGAR data was found in the inspected SOURCE_SHA rows; U.S. government public-domain status is plausible but not recorded here, so D03 forbids assuming it. Required: a rights register row quoting the source terms owner’s recorded determination.
- OBSERVED: store receipts and bounded immutable object checks are contract-level storage mechanisms (`engine/institutional_census/storage.py:86-134@a1a0a05e6adc`, `engine/institutional_census/storage.py:157-175@a1a0a05e6adc`), not a rights grant.
- UNKNOWN: user redistribution. No EDGAR-specific redistribution field was observed in the inspected code/config.

### Nasdaq earnings calendar/surprise
- OBSERVED: acquisition is from public Nasdaq API endpoints, bot-wall-aware, with browser-like headers and pacing (`collectors/equity_earnings.py:66-77@a1a0a05e6adc`, `collectors/equity_earnings.py:116-135@a1a0a05e6adc`, `collectors/equity_earnings.py:158-180@a1a0a05e6adc`).
- UNKNOWN: processing, storage beyond local parquet, model use and redistribution. `config/dataset_registry.yml` at SOURCE_SHA has no Nasdaq earnings row and the collector has no rights field. A registered rights determination is required before user-facing or model-facing expansion.

### Yahoo/yfinance EPS and revenue expectations
- OBSERVED: acquisition is direct `yfinance` accessors for `earnings_estimate`, `revenue_estimate`, `eps_revisions`, and `eps_trend`, with bounded drip and guarded accessors (`collectors/equity_revisions.py:453-477@a1a0a05e6adc`, `collectors/equity_revisions.py:552-620@a1a0a05e6adc`, `collectors/equity_revisions.py:729-757@a1a0a05e6adc`).
- OBSERVED: processing records typed missingness, payload hashes, period anchors, append-only lineage and explicit refusal of backfilled `system_observed_at`; it does not infer contributor identity or fiscal mappings (`collectors/equity_revisions.py:285-365@a1a0a05e6adc`, `collectors/equity_revisions.py:384-423@a1a0a05e6adc`, `collectors/equity_revisions.py:425-448@a1a0a05e6adc`).
- OBSERVED: storage is local `latest.parquet`, append/dedupe `history.parquet`, and additive `expectation_observations.parquet`/`expectation_attempts.parquet` (`collectors/equity_revisions.py:439-448@a1a0a05e6adc`, `collectors/equity_revisions.py:543-548@a1a0a05e6adc`, `collectors/equity_revisions.py:738-790@a1a0a05e6adc`). The SRC-A1 contract names these physical artifacts and forbids drift in legacy semantics (`research/alpha_intelligence/expectation_market_dynamics/DATA_CLOCK_RIGHTS_MATRIX.md:48-63@a1a0a05e6adc`).
- UNKNOWN: model use. SRC-A1 stores `rights_class: UNKNOWN`, and its matrix says unlicensed/rights-blocked are lawful outputs—not grants (`collectors/equity_revisions.py:363@a1a0a05e6adc`; `research/alpha_intelligence/expectation_market_dynamics/DATA_CLOCK_RIGHTS_MATRIX.md:3-9@a1a0a05e6adc`). No executed Yahoo entitlement or permitted-use determination was found.
- UNKNOWN: user redistribution. No Yahoo/yfinance expectation rights row was found. The separate Yahoo price row records `licensing: vendor_terms_personal_use` (`config/dataset_registry.yml:88-105@a1a0a05e6adc`), but that price registry row cannot be transplanted to expectations; expectation redistribution therefore remains UNKNOWN.

### Basket/SPY closes and benchmark store
- OBSERVED: acquisition is recorded for the Yahoo price family as yfinance (`config/dataset_registry.yml:88-105@a1a0a05e6adc`); the group module reads basket OHLCV files and `yahoo/SPY` from the local store (`engine/group_earnings.py:45-48@a1a0a05e6adc`).
- OBSERVED: processing/storage are parquet upserts with old rows retained and new rows winning date collisions; adjusted series may instead overwrite their refreshed overlap (`lib/store.py:60-103@a1a0a05e6adc`). Registry notes explicitly say Yahoo re-adjusts history at each fetch and `adjustment_asof` is missing (`config/dataset_registry.yml:129-133@a1a0a05e6adc`).
- OBSERVED: the committed discovery `DSC:BASKET-OHLCV-REWRITES-HISTORY-NIGHTLY` records that nightly full-history `auto_adjust=True` downloads rewrite historical adjusted values; returns/ratios are preserved to floating precision, but exact equality is not stable (`agentos/discoveries/DSC-BASKET-OHLCV-REWRITES-HISTORY-NIGHTLY.md:3-30@a1a0a05e6adc`). The same discovery links a recorded adverse Yahoo terms fact for the price spine (`agentos/discoveries/DSC-COMMODITY-AND-FX-PRICE-SPINE-YAHOO-VENDOR-TERMS-ARE-RECORDED-ADVERSE.md:50-65@a1a0a05e6adc`).
- UNKNOWN: model use and redistribution for `baskets/ohlcv` and `yahoo/SPY` specifically. The binding in-repo fact is the recorded `vendor_terms_personal_use` row plus the adverse-use finding; a family-specific clearance for model use or user redistribution is not present.

### Massive
- OBSERVED: the operator record says Mastermind holds full licensing and distribution rights under an Enterprise Market Data License and Redistribution Addendum effective 2026-08-09; the executed contract is private and commercial terms must not be quoted (`research/licenses/MASSIVE_ENTITLEMENT_RECORD.md:8-17@a1a0a05e6adc`).
- OBSERVED: across the enterprise scope, display, external redistribution raw/derived, derivative products, AI/ML training/fine-tuning/evaluation/retrieval/grounding/feature engineering/embeddings/inference, end-user export, archival retention and post-term use of delivered history/derived materials are confirmed (`research/licenses/MASSIVE_ENTITLEMENT_RECORD.md:19-49@a1a0a05e6adc`).
- OBSERVED: dataset-specific vendor conditions can override this general record and written vendor designations are authoritative per feed; attribution is optional but preferred (`research/licenses/MASSIVE_ENTITLEMENT_RECORD.md:50-60@a1a0a05e6adc`). Therefore each specific Massive feed still needs its per-dataset entitlement check.

### Curated baskets and theme graph
- OBSERVED: `mastermind_curated` is `direct_display_ok`, `auth_class: house`, first-party curation, with no external rights (`config/theme_sources.yml:21-28@a1a0a05e6adc`). The builder maps those suites to this family and derives new receipt booleans from the rights registry (`engine/theme_graph/materialize.py:70-89@a1a0a05e6adc`).
- OBSERVED: Finviz themes and THS concepts are `unresolved`, are treated internal-only for new GMI emissions, and refuse public emission; Finviz notes paid export/API and resale restrictions, while THS is a receipted scrape (`config/theme_sources.yml:29-41@a1a0a05e6adc`). Thus processing/internal analysis may be governed by the internal posture, model use and redistribution remain ungranted.
- OBSERVED: S&P Kensho/Theia are reserved, unresolved rows only; Theia requires a commercial license and S&P Kensho has no bulk constituent route (`config/theme_sources.yml:43-52@a1a0a05e6adc`).

### Consensus estimates
- OBSERVED: unlicensed is a typed warning; beat/miss is forbidden absent licensed consensus (`engine/company_intelligence/event_workspace.py:99-106@a1a0a05e6adc`, `engine/company_intelligence/event_workspace.py:338-344@a1a0a05e6adc`). C-01 ratifies this permanent absence and requires the family rights block to say so (`research/prophet_v4/r6_program/rulings/SEAT_RULING_R6-C-01_2026-09-23.md:14@a1a0a05e6adc`). Therefore acquisition, processing, storage, model use and user redistribution are all unavailable under current recorded rights.

### Transcripts
- OBSERVED: every company source span is forced to `authority=context_only` and transcript evidence carries `rights_profile: rp_public_primary_v1` (`engine/earnings_transcript_intake.py:516-529@a1a0a05e6adc`, `engine/earnings_transcript_intake.py:664-690@a1a0a05e6adc`; `engine/company_intelligence/event_workspace.py:260-265@a1a0a05e6adc`).
- UNKNOWN: what `rp_public_primary_v1` authorizes for acquisition, persistence, model use and user redistribution. The string alone is not a rights grant. A transcript rights register/contract is required.

### Press, narrative and search
- OBSERVED: press acquisition is restricted to documented published endpoints and relay APIs, with no first-party scraping; X access is via a paid read relay keyed by an environment variable (`config/press_sources.yml:14-23@a1a0a05e6adc`, `config/press_sources.yml:66-76@a1a0a05e6adc`).
- UNKNOWN: processing, storage, model use and redistribution. The file records mechanics/corroboration law but not rights. Narrative RSS/EDGAR velocity and Google Trends likewise have no rights fields in the inspected source; `narrative_sources.yml` says Trends is display-only in its implementation note (`config/narrative_sources.yml:138-148@a1a0a05e6adc`), which is a product guard, not a redistribution/model-use grant.

## Q6 GAPS + MUST-NOTS

- UNKNOWN → historical universe/delisting coverage counts. Need `data/reference/security_master.parquet`, `data/reference/vendor_aliases.parquet`, `data/reference/*_receipt.json`, `config/delisted_symbols.yml` cross-checks and the original universe snapshots. Verify per historical cut: master/dead rows, aliases covering the cut, and delisted names present. Sparse-worktree guard: do not write into omitted `data/`; use the sparse tool to opt in if a later session opens these artifacts.
- UNKNOWN → historical issuer CIK lineage and identifier changes. Need an as-of corporate-actions/identifier history keyed by issuer/security with CUSIP/FIGI old/new, effective and announced clocks, change reason and successor. SOURCE_SHA records current-registrant CIK only (`config/dataset_registry.yml:363-365@a1a0a05e6adc`) and no identifier event schema.
- UNKNOWN → general merger/spin-off/bankruptcy event history. Need an event registry with event type, announced/effective/known clocks, issuer and security lineage, successor/predecessor, source receipt and correction graph. Six manual delisted acquisitions do not establish coverage.
- UNKNOWN → actual Yahoo expectation coverage and attempt outcome distributions. Need `data/revisions/latest.parquet`, `history.parquet`, `expectation_observations.parquet`, and `expectation_attempts.parquet`; run coverage by ticker/date/status and compare legacy snapshots with SRC-A1 observations. No producer may be run in this read-only lane.
- UNKNOWN → Nasdaq historical earnings expectations/surprise completeness and revision chain. Need a licensed historical endpoint or a daily append process begun before any historical claim, storing announced/reported/observed clocks and revisions. Current sweep/drip cache cannot recreate missing snapshots.
- UNKNOWN → licensed issuer consensus. Need an entitlement that specifically permits acquisition, processing, storage, model use and user redistribution, plus per-estimate timestamps, revisions and basis. Untyped parquet values or a price/account label do not satisfy D03.
- UNKNOWN → release/transcript corpus coverage and first-use latency. Need committed stores or manifest/receipt sets: release body hashes, report/acceptance dates, transcript index revisions, body hashes and first observed timestamps. The parser contracts alone cannot prove coverage.
- UNKNOWN → rights determinations for EDGAR, Nasdaq, Yahoo expectations, transcripts, press/search and narrative families. Need each source owner’s explicit answers for acquisition, processing, storage, model use and redistribution—five answers, not one license label.
- UNKNOWN → actual costs and subscription/account entitlement boundaries. D03 names source costs as required evidence, but no cost field was observed in the inspected source contracts and credentials/accounts were intentionally not read.
- UNKNOWN → actual store rows/snapshots because `data/` is absent. Every finding above is a code/config capability and contract, not proof that a production artifact is populated, fresh or complete.
- MUST-NOT REFUSED: did not backdate current membership, event sets, GICS assignments or issuer identity; did not infer a statutory or publication lag; did not relabel broad sector rows as granular peer history; did not assume a license from source availability, a rights-profile string, or a Yahoo price-license row.
- MUST-NOT REFUSED: did not run engine producers, mutate stores, read `data/`, credentials, `.env` or key files; did not open, compute or cite any strategy return, trade outcome or protected outcome artifact.
- MUST-NOT REFUSED: did not dispatch nightly work, accelerate B1, promote a signal, mark a PR ready, apply DDL, or treat the C-01/D05 rulings as authority grants beyond their written scope.

## EVIDENCE INDEX

- `git rev-parse HEAD` → `a1a0a05e6adcdf007170bceaa7fbcd3323e333fc`, rc=0. SOURCE_SHA.
- `git fetch origin main` → rc=0; `git merge-base HEAD origin/main` → the same SOURCE_SHA.
- `git status --short --branch` → detached HEAD, clean at the skeleton; rc=0. The exact pre-push delivery receipt is `git rev-parse HEAD`, captured after the final delivery commit and before `git push`.
- REPAIR R3 HEAD: this final pushed head is recorded in the PR body and in the delivery report. All reproduction totals in this index are run after the final record-only commit and before push.
- `git ls-tree -r --name-only a1a0a05e6adcdf007170bceaa7fbcd3323e333fc | rg '^engine/(stock_identity/|institutional_census/|earnings_release/|...)'` → all named anchors resolved at SOURCE_SHA, rc=0.
- Repeated blob/line evidence used `git show <SOURCE_SHA>:<path> | nl -ba` and targeted `git grep -n ... <SOURCE_SHA> -- <paths>`, rc=0 for every cited range; no working-tree file was substituted for a source commit blob.
- Repair-r3 searches used `git show a1a0a05e6adcdf007170bceaa7fbcd3323e333fc:engine/earnings_qual.py | nl -ba`, `git grep -n ... a1a0a05e6adcdf007170bceaa7fbcd3323e333fc -- engine/earnings_qual.py`, `git show ...:scripts/import_equitydesk_backfill.py | nl -ba`, and `git ls-tree ... data/stage_analysis/backfill`, rc=0; the sparse-store existence commands are recorded above as UNKNOWN evidence. The in-record token checks for `scores.parquet`, `transcripts`, `manifest.json`, `backfill/earnings_calls.parquet`, `earnings_call_sent`, `PROPHET_US_MEASUREMENT_R2.md` and `PROPHET_US_EARLY_LEADERSHIP_R3.md` are `record_required_tokens.py -- 0`, rc=0.
- Repair-r3 citation validation: `record_citation_validator.py` → parsed 206, valid 206, failures 0; rc=0.
- Reproduction receipts at the final pre-push head: `wc -l research/prophet_v4/r6_program/wave2/D03_ISSUER_EVENT_SOURCE_READINESS_CENSUS_2026-09-23.md` → 165; `git diff --stat origin/main...HEAD` → exactly this one record file with 165 additions; totals are restated in the delivery report.
- Targeted searches covered identity, clocks, revisions, rights, closes, membership and protected-outcome avoidance. Negative searches for CUSIP/FIGI schema and general lifecycle events are reported as UNKNOWN rather than evidence of impossibility.
- `git diff --check HEAD^ HEAD` after each content commit → no output, rc=0 (whitespace check only; no test or producer was run because this PR changes one Markdown record only).
- Sparse-worktree guard: no `python3 scripts/worktree_sparse.py full/add`, no read or write under `data/`, `site/` or `mockups/`.
