# Seat ruling R6-D05-01 — durable user-action owners (D05)

Seat: Fable Meta-CEO (session 48cdfd56). Operation `prophet-us-fable-meta-ceo-20260923-001`. Date 2026-09-23.
Input: `research/prophet_v4/r6_program/wave0/D05_USER_ACTION_OWNER_CENSUS_2026-09-23.md` (external lane `pu_d05_persistence`, m1, read-only; lane PR #7816 closed as superseded, record cherry-picked `-x` onto #7811).
Law applied: R6 §16 (“verify the actual owner result before announcing success; local-only behaviour cannot be advertised as cloud persistence”); DESIGN_DOCTRINE plain-word law; no production DDL without operator ratification.

## Dispositions

| Item | Finding (census) | Ruling | Consequence |
|---|---|---|---|
| Q1 owner table | Watchlists / portfolio scopes have a real cloud owner (`watchlist_symbols`, `portfolio_*`); Pass / thesis / milestone actions have NO durable owner today | ACCEPT | Pass/thesis/milestone controls stay honestly DISABLED (plain-word “not saved yet”) until an owner exists; never advertised as saved. |
| Q2-1 read is painted as a write | Signed-in `pull()` success dispatches `synced` → chip `saved` (“Saved to your Mastermind account”) after a READ | ACCEPT as forbidden case | Build unit `pu_w1_watch_honesty` (1): read → `clean`; `saved` only after a landed write. |
| Q2-2 failed push is dropped | `pushList()` failure → `offline` pill, no retry queue, chip still promises write-through | ACCEPT as forbidden case | `pu_w1_watch_honesty` (2): per-list pending marker in the existing blob; retry on `online` and at next `pull()` before merge. |
| Q2-3 failed unwatch resurrects | Failed DELETE leaves the cloud row; next union-merge restores it and paints `saved` | ACCEPT as forbidden case | `pu_w1_watch_honesty` (3): list-scoped tombstone; merge excludes it; DELETE retried; cleared on success and on sign-out. |
| Q3 alerts | `holdings_material_change` alert kind has no producer | ACCEPT | Disable the alert kind at the surface (honest absence) OR give it a producer — a build decision for the alerts owner, recorded as open item D05-b; no user-facing promise until one lands. |
| Q4 durable thesis | `public.trade_episodes` extension with a new `source` enum value is the least-new-surface route for an episode thesis | ACCEPT as candidate owner | Production DDL = operator-ratified step (EXACT_HUMAN_GATE); until ratified, thesis capture stays DISABLED, not local-only. |

## What this ruling does not do
- It does not promote any Prophet signal, change rank/gate/size, or touch the D5/B1 science surfaces.
- It does not authorize DDL; the `trade_episodes` route is a candidate, ratified separately.
- It does not ask the fabric to redesign the store; the build packet is a frozen three-defect spec with RED→GREEN tests and two mutants.

## Open items carried
- D05-b: `holdings_material_change` producer-or-disable (owner: alerts surface).
- D05-c: owner mapping for Pass / thesis / milestone (blocked on the DDL gate above).
