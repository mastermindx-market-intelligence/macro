# Seat ruling R6-B02-01 — canonical episode registry: anchor vocabulary and D02 (extra expert anchors)

Seat: Fable Meta-CEO (session 48cdfd56). Operation `prophet-us-fable-meta-ceo-20260923-001`. Date 2026-09-23.
Input: `research/prophet_v4/r6_program/wave1/B02_EPISODE_REGISTRY_CENSUS_2026-09-23.md` (external lane `pu_b02_episodes`, m1, read-only at `aba2c115bafa`; lane PR #7824 closed as superseded, record cherry-picked `-x` here).

## Findings adopted (OBSERVED by the census)
- Exactly ONE anchor species opens an episode today: `turn_watch_reset_low`; every other nomination source is attach-only (`anchor=None`). No second anchor producer exists anywhere in the tree.
- The anchor vocabulary is NOT closed: `canonical_anchor` accepts any `kind`, so a silently widened species would be admitted without refusal (the census's Q3 silent-admission finding).
- B03 (lossless searchable field) and B04 (episode evidence dossier) need NO new anchor species for a first version; their only B02 dependency is the closed vocabulary.
- The best-evidenced candidate for an extra species is an Entry Radar expert fire (`mastermind.entry_event.v1`, G0/C1–C5), but its relation key still falls back to `ticker|detector_id|decision_session` when `event_id` is absent — a ticker/date surrogate, which is exactly D02's forbidden shortcut — and `data/entry_radar/forward.parquet` has never been committed.
- Record drift: `config/dataset_registry.yml` still marks the six B1 contracts `PROPOSED` after the 2026-08-28 natural acceptance (467 episodes / 915 events / 5,435 suppressions, generation `peg:c025bb50…`), and WS waves b3/b4 read `todo` while their modules are merged.

## Ruling
1. **B02 step 1 — close the vocabulary now (no D02 ruling needed; commissioned as build unit `pu_w1_b02_anchor_vocab`).** `ANCHOR_KINDS = frozenset({"turn_watch_reset_low"})` beside the other closed enums in `engine/us_candidate_episode.py`, enforced inside `canonical_anchor`; a foreign kind is a fail-closed refusal with a new closed `SUPPRESSION_REASONS` member, never a silent admission. No-op for every committed row. `DEFAULT_DEFINITION_ERA` and episode ids are untouched (a two-era ledger makes `all_candidates.json` unreadable by the sole canonical reader). The six registry rows flip `PROPOSED → ACCEPTED` citing the 2026-08-28 acceptance handoff in the same PR.
2. **D02 — RESOLVED IN PRINCIPLE, admission GATED.** The first extra species is `entry_radar_expert_fire`, anchored ONLY on Radar-owned fields already in `mastermind.entry_event.v1` (the detector's own price/time geometry — never a later extremum), with its own `intake_class`, a dataset-registry `version` bump and no new DAG step (B1 stays the sole writer). It may be admitted only when BOTH hold: (a) `WS:LIVE-ENTRY-RADAR` has frozen and validated the forward-projection contract so the B1-consumed relation key is the exact immutable `event_id` (the legacy fallback must be refused, not tolerated), and (b) a committed `forward.parquet` exists. Until then no code for step 2 is written; the gate is recorded here and in the D02 decision record.
3. **B03 and B04 are unblocked from D02.** They proceed on the closed vocabulary alone (B03 on incumbent #7572 after its review; B04 after D03/D07 pre-registration).
4. **Record truth.** WS waves b3/b4 status is corrected at the next records boundary; the registry status flip rides with step 1.

## Open items carried
- B02-a: today's HEAD-selected generation and live counts (sparse lane could not read `data/`) — verify from the next nightly receipt, not by dispatch.
- B02-b: whether Terminal/charting-app reads B1 episodes — cross-repo census when B03 touches the payload.
