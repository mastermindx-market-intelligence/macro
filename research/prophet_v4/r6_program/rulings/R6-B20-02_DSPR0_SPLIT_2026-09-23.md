# Seat ruling R6-B20-02 — DS-PR-0 is split into 0a (neutral landing), 0b (rebinds) and 0c (consumer reconciliation)

Fable Meta-CEO seat (session 48cdfd56), operation `prophet-us-fable-meta-ceo-20260923-001`, 2026-09-23. Authority: DEC:PROPHET-US-FABLE-META-CEO-DELEGATION; work card B20 (owner scope: shared navigation, `templates/theme.css`, design-system components, Prophet route composition). Binding prior: `research/DESIGN_MIGRATION_FACTORY_V1.md` §5 row DS-PR-0 (including its collision gate: "any new `.mx-*` hit blocks"); `research/MASTER_PRODUCT_DESIGN_SYSTEM_V1.md` §2.2; `research/prophet_v4/r6_program/wave1/B20_DESIGN_PACKET_V1_2026-09-23.md` §4; `R6-B20-01` R9.

## Findings (verified on origin/main; evidence in `wave2/DSPR0A_LANDING_RECORD_2026-09-23.md`)
1. Packet PR-0 has landed (`.mx-ladder`, `.mx-chg-row`, `.mx-empty`, the `--fs-*` scale; #6055 ported `.skel`/`.mx-error`), so DS-PR-0's stated dependency is satisfied; the factory's 2026-08-12 "blocked on Sol §J.9" note is superseded by that landing and by the Chairman's 2026-09-06 override of Sol-era holds inside this program's scope.
2. The seat's own packet carried a broken collision scan (`\b` in POSIX ERE), which is why the lane reported 0 collisions. The corrected census shows `.mx-sec`, `.mx-vh`, `.mx-callout` and `.mx-disc` already in live templates and baked pages (bonds, dashboard, the unified hero → intl, sanctions_map, us_stocks, risk_envelope_live.js), and shows the planned token names already consumed estate-wide with bare references and divergent fallbacks (`--r-card` 12px vs 10–16px fallbacks; `--r-ctl` vs 2–10px; `--t-fast` vs .14s; bare `--sp-2/--sp-3/--r-btn/--r-pill`).
3. Landing a consumed name re-renders every bare or divergent consumer. That is the design system's purpose, but under the theme art-direction law it is a visible change owing the dark/light × EN/ZH × desktop/mobile evidence matrix per affected page — not something a token-landing PR may do silently.

## Ruling
- **DS-PR-0a** (PR #7849, re-scoped by the seat): lands only what is pixel-neutral by construction — `--sp-1/4/5/6/7/8`, `--gap-grid`, `--t-med`, `--t-slow`, `--ease-std`, `--ease-lift`, `--shadow-hover`, `--ser-1..4` (+light), `--ink-tier` (+light), the `--ink-prov` alias, and the `.mx-tbl`/`.mx-tblbox`/`.mx-tabset`/`.mx-rail` primitives (with value-equal fallbacks where they reference a deferred token). No existing declaration changes; `dashboard.html.j2` is untouched.
- **DS-PR-0b** (later, reviewed alone): the `html body` var() rebinds of the vector-polish block and component radii, the `.eyebrow`→`--fs-label` snap, the `_icons` stroke-1.8 reconciliation, the specimen's duplicate-rule cleanup. Owes the evidence matrix.
- **DS-PR-0c** (new, per-page migration units under the factory's packet discipline): `--sp-2`, `--sp-3`, `--r-ctl`, `--r-btn`, `--r-card`, `--r-panel`, `--r-pill`, `--t-fast`; the `.mx-vh`/`.mx-sec`/`.mx-callout`/`.mx-disc` families (reconcile each page-local copy to the canonical rule, or rename the page-local class); the zh estate rule; `dashboard.html.j2`'s `--sp-8:32px`→`--sp-7`. Each unit owes the evidence matrix for the pages it re-renders and lists the consumers it snaps.
- **B20 build consequence:** B20-1 onward may start on 0a's merge sha. B20 components must not depend on a deferred token without a value-equal fallback until 0c lands it; any B20 unit needing `.mx-sec`/`.mx-callout`/`.mx-disc`/`.mx-vh` builds against the specimen-local rule and names 0c as its dependency.
- Every future packet's collision or consumer scan must use explicit character classes (`(^|[^A-Za-z0-9_-])name([^A-Za-z0-9_-]|$)`), never `\b`, and must be run against origin/main before dispatch with the expected count stated.

## Reversibility
0a is reversible by a single revert; nothing consumes its additions until B20-1 lands.
