<!-- GENERATED by tools/build_ledger.py — do not hand-edit.
     Source of truth: the frozen R3 review record vendored in r3_source/.
     Regenerate: python3 tools/build_ledger.py -->

# Prophet Board — R4 closure ledger

Answers the **R3 verdict `REVISE`** (PR #5552) issued over artifact `6ad6b51bc351`.

This file is **generated**, not written. `tools/build_ledger.py` joins every finding in the frozen R3 record against this cycle's disposition table and **hard-fails** if the join is not total in both directions. A finding cannot be dropped by forgetting it; dropping it breaks the build.

| R3 record rows | R4 dispositions | Condition-cited ids |
|---|---|---|
| 41 | 41 | 12 |

**This is a closure pass, not a redesign.** The successful R3 architecture is preserved: chart-first card, live quote/change slot, Priority, plan-book Setups, lifecycle ladder, one universe/count law, sourced Groups, caution carrier, stance-branched zone, light-mode card plane, bilingual behaviour, Candidates separation, compact no-prose philosophy.

## Frozen source

| File | sha256 |
|---|---|
| `r3_source/verdict.yml` | `af6953df241c9df881aa5877f5f68280…` |
| `r3_source/reviews/product_regression.yml` | `eb580945883ad943ef1c91c242f61304…` |
| `r3_source/reviews/visual_taste.yml` | `47eb4fee34c6d3de8a14b2ebdab4c264…` |

## BLOCKING — verdict.blocking_findings (10)

### `PRC-301` — implement · W1

> **R3 finding:** The card-to-name-detail link is deleted outright, with no capability id and no disposition

*Source: product_regression* · pass-1 `blocker` · pass-2 `upheld` · verdict `upheld_revise`

**Verdict note.** Card→name-detail navigation. Authority-verified: root is <article> (board.js:286), cards emit data-id and never id, the only card href is "#id=" on the 2 rows carrying `newer`, and the only id= attributes are five section anchors — no consumer resolves it. PRECISION CORRECTION to the receipt's wording: location.hash IS present at board.js:804, but as a WRITE of the lifecycle filter, never a read; "nothing reads location.hash" is imprecise and the finding stands on the corrected wording. Not BLOCKED_DATA: stock.html#TICKER is live today and the payload carries id/tk on 179/179. Third consecutive revision.

**R4 disposition.** Card routes to the name's detail surface. The whole card is a real link to stock.html#TICKER; the internal caution/episode/tip controls stay reachable and are not nested inside the anchor.

**Proof handles:** `R1 card-link`, `R1b card-link-a11y`, `R1c card-link-no-nesting`

### `PRC-302` — implement · W1

> **R3 finding:** The anonymous gate promises entry, target and void levels that no card at any tier renders

*Source: product_regression* · pass-1 `blocker` · pass-2 `upheld` · verdict `upheld_revise`

**Verdict note.** The anonymous gate's copy contract. Resolves auth.anon_gate_copy to NOT warranted: board.js:437-439 promises "entry, target and void levels included"; card() never emits them; only the banned table tier does; and the single unlocked card reads "No read yet" / "No zone — stand aside". Either the copy matches what a subscriber lands on, or the card tier's level ban is re-adjudicated — not both.

**R4 disposition.** Anonymous gate copy states what the paid Board actually provides — full setups, stance, live quote/change, Priority, lifecycle, zone and cautions — and no longer promises entry/target/void levels the card tier bans.

**Proof handles:** `R2 anon-gate-truth`, `40-anon-locked-dark-en`, `41-anon-locked-light-zh`

### `PRC-305` — implement · W1

> **R3 finding:** Degraded-freshness disclosure is deleted; the header can only ever assert "Settled close"

*Source: product_regression* · pass-1 `blocker` · pass-2 `upheld` · verdict `upheld_revise`

**Verdict note.** Degraded-freshness disclosure. The header can only ever assert "Settled close" (board.js:724-746); no stale/behind/degraded path exists. Production carries the amber behind-the-tape banner in all four board captures plus a "● Delayed" pill. Not BLOCKED_DATA — the artifact holds three vintages and chooses to print one unconditional stamp. No capability id exists for it, which is itself the §1 defect: there is no implicit deletion. Third consecutive revision.

**R4 disposition.** Behind-the-tape state restored with a capability id, reusing production's freshness producer. Fresh renders 'Settled close'; behind renders the ranking vintage, the sessions-behind count and a verify-live-price instruction, EN + ZH.

**Proof handles:** `R3 freshness-fresh`, `R3b freshness-behind`, `R3c freshness-bilingual`, `80-stale-dark-en`, `81-stale-light-zh`

### `PRC-306` — implement · W1

> **R3 finding:** 77 of 179 plan rows can never be rendered as a card in any state or filter; their only representation is a seven-column table that drops priority, stance, zone, chart, caution, sector and company name

*Source: product_regression* · pass-1 `blocker` · pass-2 `upheld` · verdict `upheld_revise`

**Verdict note.** 77 of 179 rows (43%) card-unreachable in every state and filter. Authority recomputed the union independently at 102/179 reachable — matching the receipt exactly. GRID_CAP=40 and the seven table columns are literals, and null-priority rows sort last, so the 86 unscored rows are pushed below the cap by construction. This falsifies card.priority_score: RETAIN and task.readiness_rank: BETTER for most of the book. G-D governs what ships to PRODUCTION; it does not license a frozen REFERENCE to have no card representation for 43% of its own population. Production instead offers in-place "Show 15 more / Show all".

**R4 disposition.** Full-book reachability restored. The 40-card viewport stays as the initial density budget but is no longer a ceiling: in-place 'Show more / Show all' expands the grid to every row of the active partition. Table is an alternate representation, not the only route to the tail.

**Proof handles:** `R4 expand-more`, `R4b expand-all`, `R4c full-book-card-reachable`, `90-expanded-dark-en`

### `VTC-301` — implement · W2

> **R3 finding:** The canonical desktop grid's dominant texture is a 54%-empty card — and the void is a layout choice, not a data consequence

*Source: visual_taste* · pass-1 `blocker` · pass-2 `upheld` → `blocker` · verdict `upheld_revise`

**Verdict note.** The canonical grid's dominant texture is a half-empty card — 20 of 40 canonical cards, 23 of 40 under the Ready filter, ~97px of uniform void each. Caused by .pv-grid align-items:stretch (board.css:367) + .pv-zn margin-top:auto (:553) against a 24px .pv-nochart (:458) versus a 74px chart. Explicitly NOT excused by the enrichment gap: the artifact renders the identical chartless content as clean ~78px cards in the fallback lens and at 1-column mobile. This is the layout bill for the (correct) one-universe cure, and it was not paid.

**R4 disposition.** Mixed-grid degradation fixed at desktop. The chartless placeholder is equalised to the 74px chart height (the pattern board.css already uses at 720px) and renders as an intentional, visibly null hero — not a stretched void.

**Proof handles:** `R5 nochart-geometry`, `R5b nochart-intentional-null`, `01-desktop-dark-en`

### `PRC-303` — implement · W1

> **R3 finding:** The rank-1 card contradicts itself and contradicts production — "No read yet" and "No zone" on a name the payload calls FRESH BUY, beside a "trigger fired" chip and a caution naming the buy zone it denies

*Source: product_regression* · pass-1 `blocker` · pass-2 `downgraded` → `major` · verdict `upheld_revise`

**Verdict note.** DOWNGRADED by its own critic at pass 2 (blocker -> major) and recorded here because a downgrade is not a withdrawal: RIG §7 requires every blocker surviving its own second pass to carry a resolution. The reveal supplied a real fact — production's GPCR "BUY" came via recommended_action, an engine that disclaims order authority, so refusing it is a correct de-escalation and the "the read was obtainable" component is withdrawn. What survives, and what this authority upholds, is the on-card INCOHERENCE, which is new in R3 and was created by curing r2's PRC-201: ⚡Triggered + "No read yet" + "No zone — stand aside" + a caution reading "Don't chase above the buy zone", all on the rank-1 ★Featured card. Authority-verified in the fixture. Carried as revision work, not as a gate.

**R4 disposition.** No-read / zone / caution incoherence resolved. A no-read card keeps its independently sourced trigger event, but caution text that references a buy zone is suppressed or rewritten when the card carries no zone. No zone is fabricated.

**Proof handles:** `R6 noread-caution-coherence`, `R6b noread-keeps-trigger`

### `VTC-302` — implement · W2

> **R3 finding:** Chart-stroke salience is inverted against actionability in both themes — BUY is the dimmest mark on the board

*Source: visual_taste* · pass-1 `blocker` · pass-2 `downgraded` → `major` · verdict `upheld_revise`

**Verdict note.** DOWNGRADED by its own critic at pass 2 (blocker -> major) on new pass-2 evidence rather than on the rationale: 8 of the 11 canonical BUY cards also carry the --pv-buy pinned ring and solid badge, the loudest card-level treatments on the page, so "BUY is the dimmest mark on the board" is a stroke-channel claim the whole card contradicts. Recorded here because the residue is real and conditioned — 3 non-featured BUY cards, and any follower surface without a Featured cohort inherits a bare inversion that flips with the theme. Authority-measured independently by pixel sampling; see authority_spot_verification. Not a gate; the remedy is recorded in the receipt and in the conditions below.

**R4 disposition.** Stance salience inversion cured by adopting the critic's luminance-ordered ramp. BUY > WAIT > NO-READ in all four theme x language quadrants, with --pv-buy still derived from --up so the zh flip stays structural.

**Proof handles:** `R7 stance-ramp-order`, `R7b stance-ramp-quadrants`

### `DA-001` — amend_doc · W3

*Source: authority (verdict)* · pass-1 `blocker` · verdict `upheld_revise`

**Verdict note.** AUTHORITY-ORIGINATED, corroborated independently by both critics at pass 2. DESIGN_NOTES ships two contradictory positions unmarked: §0a.C ("One universe, no view exemption") is what the code implements (board.js:106; isRef is declared and never read), while §0b.1 still states the repealed rule "A 60%-暂无判断 board must not become the flagship reference" as the justification for the removed mechanism, and §7 still describes the repealed behaviour. The reversal is CORRECT on the merits — a design-authority blocker (PRC-203) outranks an internal design note, and a reference that lies about its own counts is worse than one that shows an unflattering book — but RIG §9 requires it be explicit. A migration builder reading this document cannot tell which paragraph is law. Amend §0b.1 and §7 to record that the state=paid exemption was withdrawn under PRC-203, or strike §0b.1. Leaving a repealed principle in the rationale of a document that becomes law is how the repealed version gets rebuilt later.

**R4 disposition.** DESIGN_NOTES §0b.1 and §7 amended: the state=paid exemption and the '60% no-read must not become flagship' mechanism are recorded as SUPERSEDED by PRC-203's one-universe count law, with G-D named as how full fidelity catches up.

**Proof handles:** `R8 design-notes-no-contradiction`

### `DA-002` — implement · W2

*Source: authority (verdict)* · pass-1 `blocker` · verdict `upheld_revise`

**Verdict note.** AUTHORITY-VERIFIED from Reviewer B's pass-2 NEW-A. The r2 verdict's non-waivable condition — stance values distinct from the direction tokens, "so a Buy card at a down tape is not two direction inks" — is only HALF implemented. It was applied to the chart hue (--pv-buy darkened) and not to the chip: dark :root sets --ink-mix-up:100% (board.css:45), so --ink-up IS --up; .pv-buy routes --pvh-ink: var(--ink-up) (:392); the solid BUY chip paints background: var(--pvh-ink) (:483); and the up-change text paints color: var(--ink-up) (:506). Both resolve to #45b873. In dark mode the Buy card is still two direction inks. A condition recorded as non-waivable may not be discharged on one of its two surfaces.

**R4 disposition.** The r2 non-waivable stance-distinctness condition discharged on the CHIP as well as the chart: dark --ink-mix-up moves off 100%, so the solid BUY badge is no longer byte-identical to the live positive-direction token.

**Proof handles:** `R9 stance-chip-not-direction-ink`

### `DA-003` — implement · W3

*Source: authority (verdict)* · pass-1 `blocker` · verdict `upheld_revise`

**Verdict note.** The artifact's own three-way comparison argues honestly about the two variants it rejects and dishonestly by omission about itself. Its lede claims all three columns render one real plan row "from the SAME committed payload", while all three hardcode pv-hold (compare.html:107,159) — FTI's payload stance is `wait`, and HOLD is the single stance whose --pvh is --muted, which neutralises exactly the stance-ink question VTC-302 and the r2 condition are about. The production column is rendered in the challenger's stylesheet (.pv-trg is not scoped into .cmp-prod, so production's ⚡Triggered shows R3's amber where the real production artifact renders it green), making an entire class of regression invisible by construction. And production gets a 3-item "what must go", v1 a 6-item, the revised column none. An adjudication artifact that assigns debits only to the variants it is replacing is not evidence; it is advocacy. It also never draws the modal card — the chartless, no-read row the grid is actually made of.

**R4 disposition.** Compare page repaired as evidence: FTI renders at its true payload stance, production-only styles are scoped into .cmp-prod so production shows production semantics (including the green Triggered hue), the revised column carries an explicit costs/tradeoffs list, and a second chartless/no-read specimen is added.

**Proof handles:** `R10 compare-true-stance`, `R10b compare-scoped-prod`, `R10c compare-revised-costs`, `R10d compare-nochart-specimen`

## AUTHORITY — task-matrix and authority-delta rows (5)

### `task.open_name_detail` — implement · W1

> **R3 finding:** verdict_stands: WORSE

*Source: verdict task_matrix* · verdict `upheld_revise`

**Verdict note.** Drives PRC-301. Not waived; the capability has no disposition and must acquire one.

**R4 disposition.** Capability acquires a disposition via PRC-301: the card routes to detail. No longer WORSE with no disposition.

**Proof handles:** `R1 card-link`

### `task.staleness_awareness` — implement · W1

> **R3 finding:** verdict_stands: WORSE

*Source: verdict task_matrix* · verdict `upheld_revise`

**Verdict note.** Drives PRC-305. Not waived.

**R4 disposition.** Capability acquires a disposition via PRC-305: the behind-the-tape state gets a slot, a capability id and a producer.

**Proof handles:** `R3b freshness-behind`

### `task.momentum_context` — accept_blocked_data · W3

> **R3 finding:** verdict_stands: WORSE

*Source: verdict task_matrix* · verdict `accepted_as_blocked_data`

**Verdict note.** The change pill is at parity; only the sparkline half is degraded, on 134/179 rows, and that gap is a genuine BLOCKED_DATA with a named contract, a measured coverage number and an operator-ruled escalation (G-D, #5541). ACCEPTED as a data dependency and NOT counted as a design regression — with the express exception of its layout consequence, which is VTC-301 and is not excused.

**R4 disposition.** ACCEPTED as a data dependency (G-D / #5541), exactly as the R3 verdict resolved it — with the express exception of its layout consequence, which is VTC-301 and IS cured here.

**Proof handles:** `R5 nochart-geometry`, `R22 enrichment-gap-disclosed`

### `auth.anon_gate_copy` — implement · W1

*Source: verdict authority_delta* · verdict `upheld_revise`

**Verdict note.** Unwarranted. Drives PRC-302.

**R4 disposition.** Resolved by PRC-302: the gate copy is brought to what a subscriber actually lands on. The card-tier execution-level ban is NOT reopened.

**Proof handles:** `R2 anon-gate-truth`

### `auth.table_levels` — implement · W1

*Source: verdict authority_delta* · verdict `upheld_revise`

**Verdict note.** Unwarranted as shipped. The tier split is not defensible in its current form: the table keeps the three execution numbers the card tier banned and drops the four decision fields the board actually ranks on. Either the table carries stance, Priority and zone and drops the levels, or the card-tier ban is re-adjudicated on the record.

**R4 disposition.** Resolved by the PRC-306/PRC-313 table redesign: the table carries stance, Priority and zone and drops the execution levels. The card-tier ban stands unamended.

**Proof handles:** `R15 table-decision-fields`, `R15b table-no-execution-levels`

## CARRIED MAJOR — revision work per verdict conditions (12)

### `PRC-307` — implement · W3

> **R3 finding:** board.track_record_strip is filed RELOCATE to a destination that renders no numbers at all

*Source: product_regression* · pass-1 `major` · pass-2 `upheld`

**R4 disposition.** Quantitative track record restored on the page from its real producer, replacing the RELOCATE-to-four-empty-links claim. The page ends on evidence carrying numbers.

**Proof handles:** `R11 track-record-numbers`

### `PRC-309` — implement · W2

> **R3 finding:** The chart null is hidden rather than printed, and the stated interim ("slot at chart height") is not what the artifact does

*Source: product_regression* · pass-1 `major` · pass-2 `upheld`

**R4 disposition.** The chart null is PRINTED rather than hidden: the equalised no-chart hero states its own absence instead of rendering a decorative empty strip.

**Proof handles:** `R5b nochart-intentional-null`

### `PRC-310` — implement · W1

> **R3 finding:** "the six cells below add up to this number" is asserted while the surface simultaneously discloses that one of the six has no published value

*Source: product_regression* · pass-1 `major` · pass-2 `upheld`

**R4 disposition.** 'the six cells below add up to this number' is only asserted where the six cells are actually on screen; the claim is not repeated over a surface that contradicts it.

**Proof handles:** `R12 headline-claim-scope`

### `PRC-311` — implement · W1

> **R3 finding:** "What changed today · N opened in the last day" is computed over the whole book and printed inside every filtered view

*Source: product_regression* · pass-1 `major` · pass-2 `upheld`

**R4 disposition.** 'What changed today' stays a board-level fact: rendered on the unfiltered Board, hidden when a lifecycle filter is active rather than sitting a full-book number inside a filtered frame.

**Proof handles:** `R13 chgstrip-unfiltered-only`

### `PRC-312` — implement · W1

> **R3 finding:** Two prescriptive vocabularies collide on the card — 27 "Entered" plans carry a "Wait" stance chip

*Source: product_regression* · pass-1 `major` · pass-2 `upheld`

**R4 disposition.** Entry stance and lifecycle made to coexist semantically. The five sourced entry verbs are preserved (no sixth stance, no fallback to recommended_action); the axis is labelled so WAIT reads as an entry verb, not a verdict on an already-entered position. The Ready gloss is amended so it states the lifecycle fact rather than contradicting a sourced Triggered event.

**Proof handles:** `R14 stance-axis-explicit`, `R14b ready-gloss-no-trigger-contradiction`

### `PRC-313` — implement · W1

> **R3 finding:** The table prints unvalidated execution levels as the sole representation of 77 rows, with no direction, no priority and no sanity check

*Source: product_regression* · pass-1 `major` · pass-2 `upheld`

**R4 disposition.** Closed by the PRC-306 table redesign: the Board table carries the decision fields (ticker, stance, lifecycle, Priority, quote/change, Zone, episode/as-of) with honest dashes, and Entry/T1/Void are removed — exact execution geometry belongs to detail.

**Proof handles:** `R15 table-decision-fields`, `R15b table-no-execution-levels`, `60-table-invalidated`

### `PRC-314` — implement · W1

> **R3 finding:** Resolved plans render as live — an active zone range, a live price, and a signed green change

*Source: product_regression* · pass-1 `major` · pass-2 `upheld`

**R4 disposition.** Resolved wins precedence: a resolved card suppresses the active zone, live change and Priority treatments that imply an open plan, and takes the closed-record treatment first.

**Proof handles:** `R16 resolved-precedence`, `27-filter-resolved`

### `VTC-303` — implement · W2

> **R3 finding:** The Featured treatment renders two different hues 1px apart, half-reverting the pinned-Featured ruling

*Source: visual_taste* · pass-1 `major` · pass-2 `upheld` → `major`

**R4 disposition.** Featured pinning completed: the .pv-featured::before rail takes var(--pv-buy) like the ring, glow and lift, so no grey stance rail sits inside a green/red Featured aura.

**Proof handles:** `R17 featured-rail-cohort`

### `VTC-304` — implement · W2

> **R3 finding:** Amber now carries three contradictory meanings on one card; production reserved it for caution alone

*Source: visual_taste* · pass-1 `major` · pass-2 `upheld` → `major`

**R4 disposition.** Amber stops carrying three meanings. Triggered returns to the verb/stance hue as production ships it; the caution carrier keeps amber; WAIT no longer shares one semantic ink with both Triggered and caution.

**Proof handles:** `R18 trigger-verb-hue`, `R18b wait-not-caution-ink`

### `VTC-306` — implement · W1

> **R3 finding:** The anonymous shop window is a card that says "No read yet" and "No zone — stand aside"

*Source: visual_taste* · pass-1 `major` · pass-2 `upheld` → `major`

**R4 disposition.** The anonymous shop window no longer specimens a card that says 'No read yet' / 'No zone' while the gate advertises the product; the unlocked specimen is reconciled with the gate copy.

**Proof handles:** `R2b anon-specimen-reconciled`

### `VTC-307` — implement · W3

> **R3 finding:** The page decays to nothing — it ends on five empty pills and four bare links with no number anywhere

*Source: visual_taste* · pass-1 `major` · pass-2 `upheld` → `major`

**R4 disposition.** The page stops decaying to furniture: the empty Market Context shell is removed unless bound to real content, and Evidence carries real numbers rather than four bare links.

**Proof handles:** `R11 track-record-numbers`, `R19 no-empty-section-shell`

### `VTC-308` — implement · W2

> **R3 finding:** Multi-episode and Resolved render ~20 visually identical husk cards, each printing "PRIORITY —"

*Source: visual_taste* · pass-1 `major` · pass-2 `upheld` → `major`

**R4 disposition.** Sparse archive rows use the right form: Resolved and severely data-sparse episode rows render in the compact form the fallback treatment already proves, not 5-across full-card husks repeating PRIORITY —.

**Proof handles:** `R20 sparse-compact-form`, `32-multi-episode-resolved`

## CARRIED MINOR — upheld hygiene (13)

### `PRC-304` — disclose · W1

> **R3 finding:** Every price-change value in every crop is a hash of the ticker string, rendered with direction ink

*Source: product_regression* · pass-1 `major` · pass-2 `downgraded` → `minor`

**R4 disposition.** The per-ticker demo change overlay stays the only simulated number on the page and stays marked data-mock-live, but it is disclosed on the surface rather than only in DESIGN_NOTES, so a reader of a crop cannot mistake it for a live tape.

**Proof handles:** `R21 mock-live-disclosed`

### `PRC-308` — accept_blocked_data · W3

> **R3 finding:** Company name and sector disappear on 134/179 rows with no capability id, no disposition and no disclosure

*Source: product_regression* · pass-1 `major` · pass-2 `downgraded` → `minor`

**R4 disposition.** Company name and sector absent on 134/179 rows is ACCEPTED as the same G-D publication gap (#5541), not cured here. Recorded with its coverage number rather than silently absorbed.

**Proof handles:** `R22 enrichment-gap-disclosed`

### `PRC-316` — implement · W1

> **R3 finding:** An unmapped payload slug ships as user-facing copy in both languages

*Source: product_regression* · pass-1 `minor` · pass-2 `upheld`

**R4 disposition.** Every Groups recommendation slug is mapped in both languages; no raw payload slug (e.g. 'enter') can leak into user-facing copy in either language.

**Proof handles:** `R23 groups-slug-mapped`

### `PRC-317` — implement · W1

> **R3 finding:** Groups prints ranks from an undisclosed population

*Source: product_regression* · pass-1 `minor` · pass-2 `upheld`

**R4 disposition.** Groups rank is hidden unless its denominator and provenance are known; an unranked population does not print an authoritative-looking ordinal.

**Proof handles:** `R24 groups-rank-denominator`

### `PRC-318` — implement · W1

> **R3 finding:** Zero-width zones print as ranges

*Source: product_regression* · pass-1 `minor` · pass-2 `upheld`

**R4 disposition.** A zero-width zone renders as a single price, not as a range whose endpoints are equal.

**Proof handles:** `R25 zero-width-zone`

### `PRC-319` — disclose · W3

> **R3 finding:** Three of the seven ruled lifecycle cells have no card rendering anywhere in the artifact, while proposal_screenshot_gaps asserts there are none

*Source: product_regression* · pass-1 `minor` · pass-2 `downgraded` → `minor`

**R4 disposition.** Lifecycle cells the real payload cannot exhibit get an honest recorded screenshot gap in DESIGN_NOTES rather than an implied-but-missing capture.

**Proof handles:** `R26 screenshot-gaps-recorded`

### `PRC-320` — amend_doc · W3

> **R3 finding:** The reference's own frozen reconciliation receipt is arithmetically wrong against its own fixture

*Source: product_regression* · pass-1 `minor` · pass-2 `upheld`

**R4 disposition.** Every stale numeric statement in DESIGN_NOTES is refreshed after the fixture rebake, including the §5 candidate arithmetic and the §7 '.actcol idiom' claim.

**Proof handles:** `R27 design-notes-numbers-fresh`

### `PRC-321` — implement · W2

> **R3 finding:** The mobile ladder collapses the separation that carries "Resolved sits outside the total"

*Source: product_regression* · pass-1 `minor` · pass-2 `upheld`

**R4 disposition.** Resolved stays physically outside the live partition at 390px — the mobile ladder keeps the separation that carries 'Resolved sits outside the count'.

**Proof handles:** `R28 mobile-resolved-separation`, `42-anon-locked-390-dark`

### `VTC-305` — implement · W2

> **R3 finding:** The lifecycle mark is decorative at shipped size, and two of the seven ruled cells are pixel-identical

*Source: visual_taste* · pass-1 `major` · pass-2 `downgraded` → `minor`

**R4 disposition.** The lifecycle mark is made legible at shipped size and the two marks that rendered from one rule are given distinguishable forms.

**Proof handles:** `R29 lifecycle-mark-distinct`

### `VTC-309` — implement · W1

> **R3 finding:** Candidates restates six names already on the grid, contradicting its own copy, and its shelf pills carry prose tails

*Source: visual_taste* · pass-1 `minor` · pass-2 `upheld` → `minor`

**R4 disposition.** The Candidates sample stops needlessly duplicating already-visible Setup names where the real pool permits a distinct sample.

**Proof handles:** `R30 candidates-sample-distinct`

### `VTC-310` — implement · W2

> **R3 finding:** ~35 lines of the stylesheet describe a five-lane action grid the artifact never renders

*Source: visual_taste* · pass-1 `minor` · pass-2 `upheld` → `minor`

**R4 disposition.** The dead .actcol / actiongrid stylesheet block is removed now that it is not normative, and the DESIGN_NOTES claim that it is the live component is corrected with it.

**Proof handles:** `R31 no-dead-actcol-css`

### `VTC-311` — implement · W2

> **R3 finding:** The light+zh stance ramp is a cascade side effect, not a design — it inherits the dark-mode deepeners

*Source: visual_taste* · pass-1 `minor` · pass-2 `upheld` → `minor`

**R4 disposition.** The light+zh stance ramp is authored explicitly rather than emerging from cascade source order, so the quadrant is a design rather than a side effect.

**Proof handles:** `R7b stance-ramp-quadrants`, `R32 light-zh-explicit`

### `VTC-312` — implement · W1

> **R3 finding:** The page's headline claim invites an addition that does not visibly work

*Source: visual_taste* · pass-1 `minor` · pass-2 `upheld` → `minor`

**R4 disposition.** The headline claim no longer invites an addition that does not visibly work: the asserted arithmetic is either demonstrable on the surface that states it, or the claim is scoped.

**Proof handles:** `R12 headline-claim-scope`

## WITHDRAWN at the R3 second pass (1)

### `PRC-315` — no_action · —

> **R3 finding:** The Regime/Breadth/Posture removal is filed as an ADDITION, and its authority-delta row misstates production's baseline

*Source: product_regression* · pass-1 `major` · pass-2 `withdrawn`

**R4 disposition.** WITHDRAWN by its own critic at the R3 second pass (the Regime/Breadth/Posture removal is correctly filed). Carried here only so the ledger's coverage of the record is total; no R4 work is owed.

## Preserved as separate production blockers — NOT waived here

- **`G-D`** (PR #5541, open) — The plan book's actionability axis reaches 61/179 and enrichment 45/179. Diagnosed as a PUBLICATION gap, not an availability gap (entry_status is an admission stamp being read as a live measurement; the join used a screener population against a ledger population; records exist for 162 of 166 plan tickers). Approving or revising a VISUAL REFERENCE does not waive this, and it is not a reason to weaken the reference.
- **`overtime_producer_contradiction`** (PR #5540, open) — Outcome B — vocabulary defect. The producer is correct: age_days is anchored to signal_date while days_elapsed/tau and horizon expiry are anchored to plan_clock_date(), so 'past its horizon' computed by a reader disagrees with phase=overtime by construction. Held open, not cured by hiding. Not waived by this verdict.

Approving or revising a visual reference does not waive either, and neither is a reason to weaken the reference.

## Resubmission

Per the R3 verdict's final condition, resubmission is a **new frozen SHA with fresh critic receipts**. This cycle self-approves nothing: it produces the SHA and stops.
