# Revision continuity closure (RIG V1.1 S13 / rule L10) -- mastermind-xpv2-sector-r3b.
#
# Predecessor mastermind-xpv2-turn3-r2 issued NO verdict.yml: all four independent critics
# (Product Regression, Visual/Taste, Mobile/Accessibility, Data/Authority) returned BLOCK, and
# the bundle explicitly stopped before a design-authority verdict (README.md: "The review bundle
# intentionally stops before verdict.yml and approval.yml because every critic returned BLOCK
# overall"). scripts/check_reference_integrity.py's automated closure-set derivation
# (--mandate, rule_l10's coverage/rename checks) is sourced EXCLUSIVELY from a predecessor's
# verdict.yml (blocking_findings + conditions) -- with none on disk, that machinery resolves
# ZERO open items, even though 39 real, unresolved review-critic findings exist in r2's six
# review records (reviews/product_regression.yml, reviews/visual_taste.yml,
# reviews/mobile_accessibility.md, reviews/data_authority.md).
#
# This file closes that gap BY HAND rather than relying on a derivation the predecessor's own
# record cannot support: every one of the 39 findings across all four critics is enumerated
# below, one row each, transcribed from the review records (RIG S13.1's "no missing state and no
# implicit state" applied to raw critic findings, not only to verdict-promoted ones). Because
# none of these ids were ever minted by a verdict.yml, every row below carries `predecessor_ref`
# pointing at itself -- documenting explicitly that its anchor is the REVIEW record id, not a
# verdict-level id, per RIG S13.3's "renamed item must carry predecessor_ref naming the id it
# closes" convention (the checker's own `known` predecessor-id registry is empty here for the
# reason above, so every row would otherwise mechanically read as an undocumented rename).
#
# Per the COMMISSION.md (Sol-issued XPV2-SC-R3B commission) S27 "Required continuation" and S2
# binding order: only PRC-003 and DAC-005's tab-order sub-claim are already adjudicated, in
# research/reference_integrity/mastermind-xpv2-sector-r3/ADJUDICATIONS.md SA7 and SA4
# respectively (both cross-checked against current production by the R3A archaeology lanes).
# Every other finding is CARRIED_BLOCK, disposition: pending orchestrator adjudication against
# the frozen R3 candidate -- honest and legal (RIG S13.1), and it correctly BLOCKS approval
# until the design lanes' candidate and the fresh post-R3B critic wave (COMMISSION.md S27)
# resolve each one by id.
schema: mastermind.rig_continuity.v1
reference_id: mastermind-xpv2-sector-r3b

predecessors:
  - reference_id: mastermind-xpv2-turn3-r2
    verdict: "BLOCK (no design-authority verdict issued -- all four independent critics returned BLOCK; review intentionally halted before verdict.yml per reviews/README.md)"
    source: on_disk
    source_ref: >
      research/reference_integrity/mastermind-xpv2-turn3-r2/ is present in this checkout
      (manifest.yml + README.md + reviews/{product_regression,visual_taste}.yml +
      reviews/{mobile_accessibility,data_authority}.md); no snapshot/PR-head form is needed.
      The 39 findings below are transcribed directly from those six files, not from a
      verdict.yml (none exists -- see the file header note above).
    closure:
      # ================= PRC-001..013 (source: reviews/product_regression.yml) =================
      - id: PRC-001
        predecessor_ref: PRC-001
        kind: blocker
        disposition: RESOLVED_BY_CHANGE
        evidence: >
          [severity: blocker; source: reviews/product_regression.yml] The Sector Central portion
          of this finding (dead href="#" placeholders on operational rows -- selected-action
          "View all", Early turns, Map, Moving, Crosscurrents, Explore history tools, Confluence
          focus/queue/late rows, generated state/Explore rows) is resolved: the R3B candidate
          renders zero href="#" and zero empty hrefs in any of the six views
          (QA_ATTACK_REPORT.md SS7 "dead destination: 0/0/0/0/0/0"; capability_crosscheck.md row
          86 "ZERO href=\"#\" and zero empty hrefs anywhere -- commission S19 satisfied"), and
          real row/trace/group/stock destinations are preserved and click-verified (crosscheck
          rows 9, 10, 72, 74, 76, 82-84). The LENS "Open methodology" href="#method" sub-claim
          (LENS...html:269) is out of R3B's own route (intelligence_hub.html is a different
          surface from sector_central.html) and is NOT addressed by this change -- carried
          forward under the same reasoning as PRC-008/VTC-009/VTC-010/DAC-008
          (ORCHESTRATOR_ADJUDICATIONS.md S9).
        changed_files:
          - mockups/refs/reference_integrity/mastermind-xpv2-sector-r3b/build/views/overview.html
          - mockups/refs/reference_integrity/mastermind-xpv2-sector-r3b/build/views/map.html
          - mockups/refs/reference_integrity/mastermind-xpv2-sector-r3b/build/views/moving.html
          - mockups/refs/reference_integrity/mastermind-xpv2-sector-r3b/build/views/explore.html
          - mockups/refs/reference_integrity/mastermind-xpv2-sector-r3b/build/views/confluence.html
      - id: PRC-002
        predecessor_ref: PRC-002
        kind: blocker
        disposition: RESOLVED_BY_CHANGE
        evidence: >
          [severity: blocker; source: reviews/product_regression.yml] The candidate discloses an
          exact preview=3/locked=29/total=44 count sourced live from the embedded
          premiumdata/sector_central.json payload (capability_crosscheck.md rows 14, 16), and
          premium hydration now genuinely inserts the fetched rows (routed through
          REF.fetchJSON), live re-verified at 4/5/5/3/27 with `.pg-more` dropping 5 to 0
          (FIX_VERIFICATION.md QA2-07+F-3 row). A 401/403/schema-mismatch access failure
          collapses safely to the baked gated shell -- "nothing to undo" -- confirmed by
          `REF.log` after a simulated fetch failure (access_evidence.md "fetch-fail + hydrated").
        changed_files:
          - mockups/refs/reference_integrity/mastermind-xpv2-sector-r3b/build/views/overview.html
      - id: PRC-003
        predecessor_ref: PRC-003
        kind: blocker
        disposition: OVERRIDDEN
        authority: "R3A archaeology/adjudication lane (WS:INSTITUTIONAL-PRODUCT-EXPERIENCE-V2 XPV2-SC-R3A, PR #6122)"
        rationale: >
          [severity: blocker; source: reviews/product_regression.yml] r2's claim ("candidate
          router covers only a subset of current inbound hashes and loses read-trace and
          theme-hash behavior") is REFUTED against actual production by the R3A capability
          census: research/reference_integrity/mastermind-xpv2-sector-r3/ADJUDICATIONS.md SA7
          and capability_disposition_ledger.md row 81 both record that all four named legacy
          hashes are present and mapped in production's 21-entry LEGACY_ANCHORS table
          (routing_contract.md). The finding is overridden as factually incorrect about current
          production, not resolved by a candidate change -- R3B's proposal.yml (SC-081) still
          RETAINs the full 21-entry table and additionally demonstrates correct scroll-offset
          behavior per COMMISSION.md S14.
        finding: PRC-003
      - id: PRC-004
        predecessor_ref: PRC-004
        kind: blocker
        disposition: RESOLVED_BY_CHANGE
        evidence: >
          [severity: blocker; source: reviews/product_regression.yml] Every clock, count, row
          and universe in the candidate is demonstrated dynamically bound to its embedded
          producer fixture rather than hardcoded: forcing score=0/null on fixture rows changes
          the render (capability_crosscheck.md row 28), deleting `action_board.buy_now` renders
          the genuine empty-lane copy (row 29), deleting `si_handoff.json` triggers the
          documented fallback (row 25), and the stale-clock guard is computed live from `as_of`
          against wall-clock time (row 27). No literal replaces a producer read; the R3B build
          rule is "bind to the fixture, not hardcode" (COMMISSION.md S0/S19).
        changed_files:
          - mockups/refs/reference_integrity/mastermind-xpv2-sector-r3b/build/views/overview.html
          - mockups/refs/reference_integrity/mastermind-xpv2-sector-r3b/build/views/confluence.html
          - mockups/refs/reference_integrity/mastermind-xpv2-sector-r3b/build/views/map.html
          - mockups/refs/reference_integrity/mastermind-xpv2-sector-r3b/build/views/moving.html
          - mockups/refs/reference_integrity/mastermind-xpv2-sector-r3b/build/views/money.html
      - id: PRC-005
        predecessor_ref: PRC-005
        kind: blocker
        disposition: RESOLVED_BY_CHANGE
        evidence: >
          [severity: blocker; source: reviews/product_regression.yml] Explore/Performance/Time
          Machine/Narratives are full, producer-bound capabilities, not a seven-row sample: the
          comparison table defaults to top-8/bottom-8 with a "Show all (49)" reveal verified to
          expand to 49 rows (capability_crosscheck.md row 56), Time Machine's deferred-fetch
          contract is demonstrated live (row 58), Forming Narratives renders the full 5-leg
          breakdown per cluster (row 59), and What's Moving's track-record accessible equivalent
          (`drawTrackRecord`) was restored by fix F-5 (FIX_VERIFICATION.md F-5 row). Zero dead
          links across all six views (QA_ATTACK_REPORT.md SS7, `a[href="#"]` 0/0/0/0/0/0).
        changed_files:
          - mockups/refs/reference_integrity/mastermind-xpv2-sector-r3b/build/views/explore.html
          - mockups/refs/reference_integrity/mastermind-xpv2-sector-r3b/build/views/moving.html
      - id: PRC-006
        predecessor_ref: PRC-006
        kind: blocker
        disposition: RESOLVED_BY_CHANGE
        evidence: >
          [severity: blocker; source: reviews/product_regression.yml] Confluence's four universe
          tabs each rebind to a genuinely distinct embedded producer array (65/12/93/49 rows
          respectively) and recompute their own coverage disclosure per universe ("65 of 113..."
          / "12 of 12" / "93 of 93" / absent for Baskets -- capability_crosscheck.md rows
          63-67), and group/member detail destinations resolve into per-universe directories
          (`subsector/`, `subsector_nasdaq/`, `subsector_russell/` -- row 74). This is a genuine
          universe recompute, not a visual-only toggle.
        changed_files:
          - mockups/refs/reference_integrity/mastermind-xpv2-sector-r3b/build/views/confluence.html
      - id: PRC-007
        predecessor_ref: PRC-007
        kind: blocker
        disposition: RESOLVED_BY_CHANGE
        evidence: >
          [severity: blocker; source: reviews/product_regression.yml; CONFIRMED as accurate per
          ADJUDICATIONS.md] state_matrix.md demonstrates 8 of the 10 commissioned states live and
          screenshotted: zero/empty (`confluence-empty-lane.png`, `confluence-search-zero.png`),
          stale (`#ov-asof.r3-stale` + staleline text), error
          (`overview-access-fetchfail.png`, production-shaped fetch-fail strings per
          QA_ATTACK_REPORT.md SS6), access-shell gated/hydrated
          (`overview-access-gated.png`/`overview-access-hydrated.png`), long-names and
          cardinality-extreme. The two remaining states ("loading", null-guarded "partial") are
          honestly documented as not captured on this fixture/architecture, not silently absent
          (state_matrix.md's own "why not captured" sections) -- materially different from r2's
          "specified in prose but absent from the surface."
        changed_files:
          - mockups/refs/reference_integrity/mastermind-xpv2-sector-r3b/evidence/state_matrix.md
          - mockups/refs/reference_integrity/mastermind-xpv2-sector-r3b/build/views/overview.html
          - mockups/refs/reference_integrity/mastermind-xpv2-sector-r3b/build/views/confluence.html
      - id: PRC-008
        predecessor_ref: PRC-008
        kind: blocker
        disposition: CARRIED_BLOCK
        note: >
          [severity: blocker, downgraded to major in second pass; source:
          reviews/product_regression.yml] LENS copy is mostly authority-correct but its visible
          methodology link has no target (second pass: "the packet provides the lawful
          hide-or-implement remedy"). Out of R3B's own scope (LENS/intelligence_hub.html is a
          separate surface from sector_central.html) but still an unresolved r2 item under this
          predecessor's combined artifact set -- carried forward rather than silently dropped,
          per ORCHESTRATOR_ADJUDICATIONS.md S9 ("Four predecessor findings targeting
          intelligence_hub.html/LENS ... are out of this cycle's route; carried as CARRIED_BLOCK
          with ownership deferred to a future LENS RIG cycle -- not silently dropped"). Pending
          orchestrator adjudication (ownership: a future LENS/intelligence_hub RIG cycle, not
          R3B).
      - id: PRC-009
        predecessor_ref: PRC-009
        kind: major
        disposition: RESOLVED_BY_CHANGE
        evidence: >
          [severity: major; source: reviews/product_regression.yml] `capability_crosscheck.md`
          rows 20 and 24 show `#si-read-overview` (leadership handoff) and `#ov-ctx-body` (What
          changed context) are demonstrably payload-resident, not hardcoded prose -- both carry
          `data-from`/`data-to` attributes composed from `si_handoff.json`'s
          `theme_context`/`factor_season` fields, and row 25's probe (deleting `si_handoff.json`)
          triggers the documented verbatim fallback copy rather than a frozen string.
        changed_files:
          - mockups/refs/reference_integrity/mastermind-xpv2-sector-r3b/build/views/overview.html
      - id: PRC-010
        predecessor_ref: PRC-010
        kind: major
        disposition: RESOLVED_BY_CHANGE
        evidence: >
          [severity: major; source: reviews/product_regression.yml] The Bottoming Watch
          ("early-turn") strip renders full per-row receipts (position-in-cycle chip,
          gate-conflict chip, dual-read chip, null disclosure -- capability_crosscheck.md row
          22), and fix F-2 restored the rows' real `basket/*.html` destinations as clickable
          `<a data-ref-nav>` elements per `_us_bottoming_watch.html.j2:95`, live re-verified: "3
          anchors with real basket/*.html hrefs; click records exact route"
          (FIX_VERIFICATION.md F-2 row).
        changed_files:
          - mockups/refs/reference_integrity/mastermind-xpv2-sector-r3b/build/views/overview.html
      - id: PRC-011
        predecessor_ref: PRC-011
        kind: major
        disposition: RESOLVED_BY_CHANGE
        evidence: >
          [severity: major; source: reviews/product_regression.yml] `capability_crosscheck.md`
          rows 36-53 (18 rows covering Map/Moving/Money) each carry an explicit VERIFIED or
          VERIFIED-AS-ADJUDICATED verdict with live probe/grep evidence; of the 92 total ledger
          rows only 7 findings were open at cross-check time (F-1..F-6, one row shared), and
          FIX_VERIFICATION.md records live re-probe confirmation for all of them, including F-4
          (map ZH leaks: 57/57 + 57/57 leaves carrying CJK) and F-5 (moving `drawTrackRecord`
          ported with real horizon numbers).
        changed_files:
          - mockups/refs/reference_integrity/mastermind-xpv2-sector-r3b/build/views/map.html
          - mockups/refs/reference_integrity/mastermind-xpv2-sector-r3b/build/views/moving.html
          - mockups/refs/reference_integrity/mastermind-xpv2-sector-r3b/build/views/money.html
      - id: PRC-012
        predecessor_ref: PRC-012
        kind: major
        disposition: RESOLVED_BY_CHANGE
        evidence: >
          [severity: major; source: reviews/product_regression.yml] The candidate's live
          Stand-aside ledge label reads "Stand aside/观望" -- the canonical ZH string, not the
          drifted "暂时回避" (`capability_crosscheck.md` row 2: "Live ledge text: ... Stand
          aside观望").
        changed_files:
          - mockups/refs/reference_integrity/mastermind-xpv2-sector-r3b/build/views/overview.html
      - id: PRC-013
        predecessor_ref: PRC-013
        kind: major
        disposition: RESOLVED_BY_CHANGE
        evidence: >
          [severity: major; source: reviews/product_regression.yml] No production file was
          modified by this cycle (ORCHESTRATOR_ADJUDICATIONS.md S10); the two chrome divergences
          from production (label rail behavior, mobile tab layout) are explicitly recorded as
          approved-but-non-binding with the instruction "R3C must adjudicate both for production"
          (S3), and `R3C_HANDOFF_DRAFT.md` exists specifically to route that decision through an
          explicit future adjudication rather than letting a literal shell copy silently become
          de facto design law / a duplicate local component system.
        changed_files:
          - research/reference_integrity/mastermind-xpv2-sector-r3b/ORCHESTRATOR_ADJUDICATIONS.md
          - research/reference_integrity/mastermind-xpv2-sector-r3b/R3C_HANDOFF_DRAFT.md

      # ================= VTC-001..010 (source: reviews/visual_taste.yml) =================
      - id: VTC-001
        predecessor_ref: VTC-001
        kind: blocker
        disposition: RESOLVED_BY_CHANGE
        evidence: >
          [severity: blocker; source: reviews/visual_taste.yml] `QA_ATTACK_REPORT.md` Gate 6
          (SS1.6) PASSES across 32 measured hash landings: every resolved legacy-anchor target
          lands clear of the combined 148.8px sticky-chrome bottom (17 anchors at
          159.5-160.4px, plus 289/671.7px for two others), none sit under sticky chrome, and fix
          F-6 further reconciled the measured `--ref-sticky-offset` with the value the
          scroll-margin rules actually consume (FIX_VERIFICATION.md F-6 row;
          `evidence/hash_evidence.md`).
        changed_files:
          - mockups/refs/reference_integrity/mastermind-xpv2-sector-r3b/build/runtime_shim.js
          - mockups/refs/reference_integrity/mastermind-xpv2-sector-r3b/build/shell.html
      - id: VTC-002
        predecessor_ref: VTC-002
        kind: major
        disposition: RESOLVED_BY_CHANGE
        evidence: >
          [severity: blocker, downgraded to major in second pass; source:
          reviews/visual_taste.yml] `QA_ATTACK_REPORT.md` Gate 1 (SS1.1) PASSES: all five
          action-state labels and counts (including the two states r2 found hidden) are
          simultaneously visible, stacked full-bleed with zero document overflow at 320px, and
          `design_notes.md` S4 records the shell's <=767px change explicitly as "repairing the
          VTC-002 hidden-offscreen defect class" (production's six-tab horizontal scroller
          replaced with a 3x2 grid, adjudications S3(b)); the visible-control count is a
          constant 12/12 across every swept width (SS2).
        changed_files:
          - mockups/refs/reference_integrity/mastermind-xpv2-sector-r3b/build/shell.html
          - mockups/refs/reference_integrity/mastermind-xpv2-sector-r3b/build/views/overview.html
      - id: VTC-003
        predecessor_ref: VTC-003
        kind: blocker
        disposition: RESOLVED_BY_CHANGE
        evidence: >
          [severity: blocker; source: reviews/visual_taste.yml] `QA_ATTACK_REPORT.md` Gate 3
          (SS1.3) PASSES: all five Confluence state names and counts are simultaneously legible
          at every width 320-1440, and the `.r3-state` label boxes measure by string length
          only -- byte-identical widths across all four widths regardless of the underlying
          population (1/16/21/18/9) -- confirming the fixed `repeat(5,minmax(0,1fr))` grid
          `design_notes.md` S3 describes, never a proportional/population-scaled cell.
        changed_files:
          - mockups/refs/reference_integrity/mastermind-xpv2-sector-r3b/build/views/confluence.html
      - id: VTC-004
        predecessor_ref: VTC-004
        kind: major
        disposition: RESOLVED_BY_CHANGE
        evidence: >
          [severity: major; source: reviews/visual_taste.yml] `QA_ATTACK_REPORT.md` Gate 2
          (SS1.2) PASSES: the phone Map composition is a genuine re-order (answer -> quadrant
          summary -> selected object -> accessible table -> full plot behind an explicit
          disclosure at document top 2054px), materially different from the desktop paint order
          (SVG at top 322, above the quadrants at 871) -- "not a CSS shrink of the desktop
          stack" in the report's own words, and `responsive_contract.md`'s Map section documents
          the `grid-template-areas` reorder and the disclosure mechanism driving it.
        changed_files:
          - mockups/refs/reference_integrity/mastermind-xpv2-sector-r3b/build/views/map.html
      - id: VTC-005
        predecessor_ref: VTC-005
        kind: major
        disposition: RESOLVED_BY_CHANGE
        evidence: >
          [severity: major; source: reviews/visual_taste.yml] The Sector Central portion of this
          finding (Overview/Explore rows English-only in ZH) is resolved:
          `QA_ATTACK_REPORT.md` SS3 confirms zero leaked visible `.l-en` strings in generated
          rows, buttons, or state vocabulary in any of the six views, and fix F-4 replaced the
          Map reasoning-chain's untranslated layer/tier labels with `LAYER_ZH`/`TIER_ZH` bound
          from production's own China-sibling twin map, live re-verified at 57/57 + 57/57 leaves
          carrying CJK with zero leaks (FIX_VERIFICATION.md F-4 row; `copy_ledger.md` SS2). The
          LENS "Entry T2" untranslated sub-claim (`MASTERMIND_LENS...html:214`) is out of R3B's
          own route and is NOT addressed by this change -- carried forward under the same
          reasoning as PRC-008/VTC-009/VTC-010/DAC-008 (ORCHESTRATOR_ADJUDICATIONS.md S9).
        changed_files:
          - mockups/refs/reference_integrity/mastermind-xpv2-sector-r3b/build/views/map.html
          - mockups/refs/reference_integrity/mastermind-xpv2-sector-r3b/build/views/money.html
          - mockups/refs/reference_integrity/mastermind-xpv2-sector-r3b/build/views/overview.html
          - mockups/refs/reference_integrity/mastermind-xpv2-sector-r3b/build/views/confluence.html
          - mockups/refs/reference_integrity/mastermind-xpv2-sector-r3b/build/views/moving.html
      - id: VTC-006
        predecessor_ref: VTC-006
        kind: major
        disposition: RESOLVED_BY_CHANGE
        evidence: >
          [severity: major; source: reviews/visual_taste.yml] `design_notes.md` S3 documents the
          Overview mobile composition as an explicit reorder (answer -> board -> leadership
          context -> Bottoming Watch -> grader, `order:1..5` at `overview.html:204-220`) rather
          than a shrink, with the inline law "Nothing is removed, and the ledge itself never
          moves off the top of the object"; `QA_ATTACK_REPORT.md` Gate 1 confirms the five
          action-lane targets render at a distinct, readable 294x56px each at 320px, not a
          squeezed dashboard stack.
        changed_files:
          - mockups/refs/reference_integrity/mastermind-xpv2-sector-r3b/build/views/overview.html
      - id: VTC-007
        predecessor_ref: VTC-007
        kind: major
        disposition: RESOLVED_BY_CHANGE
        evidence: >
          [severity: major; source: reviews/visual_taste.yml] `ORCHESTRATOR_ADJUDICATIONS.md` S3
          explicitly ratifies the reference's chrome divergences as approved-and-recorded (never
          silent) and states verbatim "R3C must adjudicate both for production" -- i.e.
          production must treat this mock topbar as non-binding, exactly the second-pass
          condition r2's own critic proposed. S10 confirms no production file was modified and no
          approval.yml was self-issued.
        changed_files:
          - research/reference_integrity/mastermind-xpv2-sector-r3b/ORCHESTRATOR_ADJUDICATIONS.md
          - research/reference_integrity/mastermind-xpv2-sector-r3b/design_notes.md
      - id: VTC-008
        predecessor_ref: VTC-008
        kind: major
        disposition: RESOLVED_BY_CHANGE
        evidence: >
          [severity: major; source: reviews/visual_taste.yml] `ORCHESTRATOR_ADJUDICATIONS.md` S4
          and S5 record CSS-drawn marks replacing production's Unicode glyphs (arrow/triangle
          marks) on Overview/Confluence and the Map/Moving source-destination connector, and
          `accessible_alternative_contract.md` SS3.3 confirms the connector is now
          `aria-hidden` with no decorative role/label at all -- commission S18's ban on
          decorative Unicode/emoji/lightning badges is honored throughout.
        changed_files:
          - mockups/refs/reference_integrity/mastermind-xpv2-sector-r3b/build/views/map.html
          - mockups/refs/reference_integrity/mastermind-xpv2-sector-r3b/build/views/moving.html
          - mockups/refs/reference_integrity/mastermind-xpv2-sector-r3b/build/shell.html
      - id: VTC-009
        predecessor_ref: VTC-009
        kind: minor
        disposition: CARRIED_BLOCK
        note: >
          [severity: major, downgraded to a condition in second pass; source:
          reviews/visual_taste.yml] LENS is mostly empty canvas when judged as a page rather than
          an interaction/component specimen (second pass condition: "lawful only when explicitly
          scoped as a component specimen"). Out of R3B's own scope (LENS/intelligence_hub.html is
          a separate surface) but carried forward rather than silently dropped, same reasoning as
          PRC-008, per ORCHESTRATOR_ADJUDICATIONS.md S9 ("carried as CARRIED_BLOCK with
          ownership deferred to a future LENS RIG cycle -- not silently dropped"). Pending
          orchestrator adjudication (ownership: a future LENS/intelligence_hub RIG cycle, not
          R3B).
      - id: VTC-010
        predecessor_ref: VTC-010
        kind: minor
        disposition: CARRIED_BLOCK
        note: >
          [severity: minor, downgraded further in second pass; source: reviews/visual_taste.yml]
          Mobile sheet title focus is accessible but visually browser-default and unpolished
          (second pass: "the focus destination is correct; only its visual treatment needs
          repair"). Out of R3B's own scope (LENS is a separate surface) but carried forward, per
          ORCHESTRATOR_ADJUDICATIONS.md S9 ("carried as CARRIED_BLOCK with ownership deferred to
          a future LENS RIG cycle -- not silently dropped"). Pending orchestrator adjudication
          (ownership: a future LENS/intelligence_hub RIG cycle, not R3B).

      # ================= MAC-001..008 (source: reviews/mobile_accessibility.md) =================
      - id: MAC-001
        predecessor_ref: MAC-001
        kind: blocker
        disposition: RESOLVED_BY_CHANGE
        evidence: >
          [severity: BLOCK; source: reviews/mobile_accessibility.md] `FIX_VERIFICATION.md`
          records live re-probe of every 200%-zoom finding QA2-01 through QA2-05: css320/390/820
          all measure 0px document overflow post-fix (was +4, +60, +36, +32, +26, +1px plus one
          clipped primary name), and the post-fix regression sweep states "200%-zoom cells that
          previously failed: 7/7 now 0px."
        changed_files:
          - mockups/refs/reference_integrity/mastermind-xpv2-sector-r3b/build/views/money.html
          - mockups/refs/reference_integrity/mastermind-xpv2-sector-r3b/build/views/explore.html
          - mockups/refs/reference_integrity/mastermind-xpv2-sector-r3b/build/views/overview.html
      - id: MAC-002
        predecessor_ref: MAC-002
        kind: blocker
        disposition: RESOLVED_BY_CHANGE
        evidence: >
          [severity: BLOCK for Sector, major for LENS; source: reviews/mobile_accessibility.md]
          The Sector Central portion is resolved: `QA_ATTACK_REPORT.md` SS3 measures zero
          leaked visible `.l-en` strings across generated rows, buttons, and empty/error states
          in all six views, and fix QA2-10 made the four reference-authored `aria-label` pairs
          language-aware, live re-verified to carry CJK under `data-lang=zh`
          (FIX_VERIFICATION.md QA2-10 row); the Confluence `#cf-q` search placeholder is
          independently confirmed translated (SS11.4 QA2-10 counter-measurement). The "LENS also
          leaks English accessibility strings" sub-claim (`#help` aria-label) targets
          `intelligence_hub.html` and is out of R3B's own route -- not addressed by this change,
          carried forward under the same reasoning as PRC-008/VTC-009/VTC-010/DAC-008
          (ORCHESTRATOR_ADJUDICATIONS.md S9).
        changed_files:
          - mockups/refs/reference_integrity/mastermind-xpv2-sector-r3b/build/views/confluence.html
          - mockups/refs/reference_integrity/mastermind-xpv2-sector-r3b/build/views/overview.html
          - mockups/refs/reference_integrity/mastermind-xpv2-sector-r3b/build/views/explore.html
          - mockups/refs/reference_integrity/mastermind-xpv2-sector-r3b/build/views/map.html
          - mockups/refs/reference_integrity/mastermind-xpv2-sector-r3b/build/views/moving.html
      - id: MAC-003
        predecessor_ref: MAC-003
        kind: blocker
        disposition: RESOLVED_BY_CHANGE
        evidence: >
          [severity: BLOCK; source: reviews/mobile_accessibility.md] The Sector Central portion
          (the 24 `href="#"` placeholder links across Overview 7 / Map 1 / Moving 3 / Explore 3
          / Confluence 10) is resolved: `QA_ATTACK_REPORT.md` SS7 measures zero `a[href="#"]` in
          all six views (0/0/0/0/0/0), and the per-view destination inventory
          (`capability_crosscheck.md` row 86) confirms real recorded routes for every anchor. The
          LENS `.method-link href="#method"` dead destination targets `intelligence_hub.html`
          and is out of R3B's own route -- not addressed by this change, carried forward under
          the same reasoning as PRC-008/VTC-009/VTC-010/DAC-008
          (ORCHESTRATOR_ADJUDICATIONS.md S9).
        changed_files:
          - mockups/refs/reference_integrity/mastermind-xpv2-sector-r3b/build/views/overview.html
          - mockups/refs/reference_integrity/mastermind-xpv2-sector-r3b/build/views/map.html
          - mockups/refs/reference_integrity/mastermind-xpv2-sector-r3b/build/views/moving.html
          - mockups/refs/reference_integrity/mastermind-xpv2-sector-r3b/build/views/explore.html
          - mockups/refs/reference_integrity/mastermind-xpv2-sector-r3b/build/views/confluence.html
      - id: MAC-004
        predecessor_ref: MAC-004
        kind: blocker
        disposition: RESOLVED_BY_CHANGE
        evidence: >
          [severity: BLOCK; source: reviews/mobile_accessibility.md] `accessible_alternative_
          contract.md` SS1/SS2 documents all three mandatory chart equivalents implemented: the
          Map rotation-map svg gets a `role=img` + `aria-labelledby` name/desc pair plus
          `table#rvx-board` (with a `<caption>`, same-source fields, no recomputation); the Map
          sector-cycle-clock svg gets the equivalent treatment plus `table.r3-cyc-tbl`; the Money
          heatmap gets a `role=img` treemap plus a same-source `<details>` table
          (`hm-alt-tbl`). Moving's cross-table equivalent (`drawTrackRecord`) was restored by fix
          F-5 with a live-verified horizons table and verdict ladder (FIX_VERIFICATION.md F-5
          row). Confluence's population-scaled spread bar (the sparkline-class control) is now
          cleanly `aria-hidden="true"` (`confluence.html:335`) rather than carrying a misleading
          label with no data, with the underlying values available as real text in the same row.
        changed_files:
          - mockups/refs/reference_integrity/mastermind-xpv2-sector-r3b/build/views/map.html
          - mockups/refs/reference_integrity/mastermind-xpv2-sector-r3b/build/views/moving.html
          - mockups/refs/reference_integrity/mastermind-xpv2-sector-r3b/build/views/money.html
          - mockups/refs/reference_integrity/mastermind-xpv2-sector-r3b/build/views/confluence.html
      - id: MAC-005
        predecessor_ref: MAC-005
        kind: major
        disposition: RESOLVED_BY_CHANGE
        evidence: >
          [severity: MAJOR; source: reviews/mobile_accessibility.md] The candidate's tablists
          are not faked: Overview's five-lane ledge (formerly `.panel.distribution`) and
          Confluence's universe tabs (formerly `.universe-tabs`) both declare real
          `role=tab`/`role=tabpanel`/`aria-selected` children with roving tabindex, live
          re-verified by fixes QA2-08/QA2-09/QA2-12 (FIX_VERIFICATION.md: "controls resolve to
          tabpanel; ArrowRight moves focus+selection", "focus stays inside the widget", "0/-1
          swap verified; Home/End land first/last"). The r2 candidate's stateful `#theme`/`#lang`
          toggle buttons (the ones lacking `aria-pressed`) do not exist in this reference at all
          -- the specimen self-bootstrap chrome was dropped rather than carried
          (ORCHESTRATOR_ADJUDICATIONS.md S3 "Shell integration drops").
        changed_files:
          - mockups/refs/reference_integrity/mastermind-xpv2-sector-r3b/build/views/overview.html
          - mockups/refs/reference_integrity/mastermind-xpv2-sector-r3b/build/views/confluence.html
      - id: MAC-006
        predecessor_ref: MAC-006
        kind: major
        disposition: RESOLVED_BY_CHANGE
        evidence: >
          [severity: MAJOR, rising to BLOCK if required mobile/tablet journeys; source:
          reviews/mobile_accessibility.md] Every example r2 cited is directly re-swept clean:
          Gate 1 (`QA_ATTACK_REPORT.md` SS1.1) shows "IN FAVOUR -- DON'T CHASE / 5" fully
          rendered unclipped at 320px (294x56px, doc overflow 0), and the width-sweep matrix
          (SS2, 216/216 clean at 100% zoom across 320-1440 x both langs x both themes x all six
          views, including 768/820) plus `FIX_VERIFICATION.md`'s post-fix sweep ("54/54 cells, 0
          document overflow") together cover the exact Overview-768, Moving-768, Money-320/390
          and Explore-768/820 cells r2 measured overflowing.
        changed_files:
          - mockups/refs/reference_integrity/mastermind-xpv2-sector-r3b/build/views/overview.html
          - mockups/refs/reference_integrity/mastermind-xpv2-sector-r3b/build/views/moving.html
          - mockups/refs/reference_integrity/mastermind-xpv2-sector-r3b/build/views/money.html
          - mockups/refs/reference_integrity/mastermind-xpv2-sector-r3b/build/views/explore.html
      - id: MAC-007
        predecessor_ref: MAC-007
        kind: major
        disposition: CARRIED_BLOCK
        note: >
          [severity: MAJOR / production-proof required; source: reviews/mobile_accessibility.md]
          The R3B evidence lane repairs the adjacent keyboard-focus defects this finding's own
          harness flagged as related (QA2-08/QA2-09/QA2-12, live re-verified in
          FIX_VERIFICATION.md), but the specific evaluability gap MAC-007 names -- CDP
          `Input.dispatchTouchEvent` unsupported, so real touch-path dispatch is NOT_EVALUABLE --
          is not overcome by anything in this cycle's evidence pack (`QA_ATTACK_REPORT.md` SS5
          covers keyboard only, not touch dispatch). Genuinely unresolved, not stretched. Pending
          orchestrator adjudication against the frozen R3 candidate -- a future evidence lane
          needs a harness with real touch-event support.
      - id: MAC-008
        predecessor_ref: MAC-008
        kind: major
        disposition: RESOLVED_BY_CHANGE
        evidence: >
          [severity: MAJOR; source: reviews/mobile_accessibility.md] The Sector Central portion
          is resolved: `QA_ATTACK_REPORT.md` Gate 5 (SS1.5) is a comprehensive 72-cell sweep (6
          widths x 2 langs x 6 views, every focusable control enumerated) that found only 4
          failing cells -- the Map universe segmented control at 768/820px -- fixed by QA2-06,
          live re-verified at 44.0px (FIX_VERIFICATION.md QA2-06 row). The "main tab buttons at
          40px" claim does not reproduce (Overview's five action-lane targets measure 294x56px,
          Gate 1); "Open group ->" is not among Gate 5's failing cells. The r2 candidate's
          `#theme`/`#lang` controls this finding also names do not exist in this reference (see
          MAC-005 evidence). The LENS `#themeBtn`/`#langBtn`/`#help` sub-claim targets
          `intelligence_hub.html` and is out of R3B's own route -- not addressed, carried forward
          under the same reasoning as PRC-008/VTC-009/VTC-010/DAC-008
          (ORCHESTRATOR_ADJUDICATIONS.md S9).
        changed_files:
          - mockups/refs/reference_integrity/mastermind-xpv2-sector-r3b/build/shell.html

      # ================= DAC-001..008 (source: reviews/data_authority.md) =================
      - id: DAC-001
        predecessor_ref: DAC-001
        kind: blocker
        disposition: RESOLVED_BY_CHANGE
        evidence: >
          [severity: BLOCK; source: reviews/data_authority.md] The Explore action-state
          assignments read directly and exclusively from the embedded producer fixture with zero
          client-side recomputation -- `capability_crosscheck.md` row 56 confirms "the table
          reads basketdata/baskets.json from the client fetch only; window.SECTOR_CENTRAL is not
          consulted", and row 78 confirms zero foreign/mixed rows across the confluence-adjacent
          surfaces. `capability_disposition_ledger.md` row 7 independently establishes sector
          rows must carry the real qualitative stat chip, never an invented blended score -- the
          same fixture-verbatim binding this cycle preserves throughout (adjudications S0:
          "Fixture stands; no refresh").
        changed_files:
          - mockups/refs/reference_integrity/mastermind-xpv2-sector-r3b/build/views/explore.html
      - id: DAC-002
        predecessor_ref: DAC-002
        kind: blocker
        disposition: RESOLVED_BY_CHANGE
        evidence: >
          [severity: BLOCK; source: reviews/data_authority.md] `capability_crosscheck.md` rows
          24 and 26 structurally prove leadership-context data cannot promote into action
          authority: "`HND` is never referenced by `laneRows()`, `laneCount()`, `fillLane()` or
          `REF.renderActNow()`", probed by deleting `si_handoff.json` entirely -- the five action
          lanes render unchanged (counts 4/5/5/3/27 intact). The Health Care leadership hero and
          the action board are drawn as structurally independent systems with no shared hue or
          causal prose (`design_notes.md` S3, commission S7.3).
        changed_files:
          - mockups/refs/reference_integrity/mastermind-xpv2-sector-r3b/build/views/overview.html
      - id: DAC-003
        predecessor_ref: DAC-003
        kind: blocker
        disposition: RESOLVED_BY_CHANGE
        evidence: >
          [severity: BLOCK; source: reviews/data_authority.md] `capability_crosscheck.md` row 22
          shows current Bottoming Watch copy: title "Bottoming watch/筑底观察", subcopy "cycle
          lows forming -- watch, don't chase", with an explicit non-predictive null disclosure.
          `QA_ATTACK_REPORT.md` SS7 (authority masquerade) scanned for the forbidden renames
          DAC-003 warns against and found zero matches: "exactly 5 lanes... zero forbidden
          renames; Bottoming Watch renders as 'All 3 rows: cycle turn signal -- watch only -
          may be bottoming' -- watch-only, nonpredictive, not a sixth lane."
        changed_files:
          - mockups/refs/reference_integrity/mastermind-xpv2-sector-r3b/build/views/overview.html
      - id: DAC-004
        predecessor_ref: DAC-004
        kind: blocker
        disposition: RESOLVED_BY_CHANGE
        evidence: >
          [severity: BLOCK; source: reviews/data_authority.md] `capability_crosscheck.md` rows
          63 and 78 confirm zero cross-universe row mixing: the S&P 500 payload is 100%
          `kind="subsector"` with `anyBasketId=0`, and the Baskets payload is 100%
          `kind="basket"` -- the "five-part detector's attack surface" the R3A test suite
          asserts on is preserved intact. `QA_ATTACK_REPORT.md` SS7's foreign-universe probe
          sums each universe's five state counts against its own total and finds them exact in
          all four (no leakage, no client re-rank).
        changed_files:
          - mockups/refs/reference_integrity/mastermind-xpv2-sector-r3b/build/views/confluence.html
      - id: DAC-005
        predecessor_ref: DAC-005
        kind: major
        disposition: OVERRIDDEN
        authority: "R3A archaeology/adjudication lane (WS:INSTITUTIONAL-PRODUCT-EXPERIENCE-V2 XPV2-SC-R3A, PR #6122)"
        rationale: >
          [severity: MAJOR; source: reviews/data_authority.md] DAC-005 bundles three claims:
          (a) canonical state labels replaced with "Buy-ready"; (b) Confluence tab order drifts
          from canonical dataset order; (c) coverage wording drifts ("65 of 113 ... 48 remain
          thin" vs the canonical "listed in the table, not timed" honesty clause). ONLY the
          tab-order sub-claim (b) is overridden here: ADJUDICATIONS.md SA4 and
          capability_disposition_ledger.md row 64 (SC-064) establish that r2's critic conflated
          the `DS` JS declaration order with the actual rendered DOM tab order, and production's
          real tab order (S&P -> Nasdaq -> Russell -> Baskets) is confirmed intact and unchanged.
          Sub-claims (a) and (c) are NOT overridden by this row and are not silently dropped: they
          remain live, tracked as known-defect RETAIN capabilities in THIS cycle's own baseline.yml
          /proposal.yml (SC-065 "S&P thin-but-listed wording ... semantically inaccurate ...
          recorded, not repaired this wave" and SC-077 coverage-wording template), so the R3B
          candidate must restore canonical state-label vocabulary and coverage honesty per
          COMMISSION.md S13 "Use canonical producer vocabulary ... Do not replace it with R2
          synonyms" even though this continuity row itself closes only the tab-order half.
        finding: DAC-005
      - id: DAC-006
        predecessor_ref: DAC-006
        kind: major
        disposition: RESOLVED_BY_CHANGE
        evidence: >
          [severity: MAJOR; source: reviews/data_authority.md] Same fix as PRC-012: the
          candidate's live Stand-aside ledge label reads "Stand aside/观望" -- the canonical ZH
          string (`capability_crosscheck.md` row 2), replacing the drifted "暂时回避" this
          finding and PRC-012 both name.
        changed_files:
          - mockups/refs/reference_integrity/mastermind-xpv2-sector-r3b/build/views/overview.html
      - id: DAC-007
        predecessor_ref: DAC-007
        kind: major
        disposition: RESOLVED_BY_CHANGE
        evidence: >
          [severity: MAJOR; source: reviews/data_authority.md] `build/DESIGN_SYSTEM_SPEC.md`
          explicitly forbids "sourcing Moving from anything other than its five artifacts
          (rotation_events, sector_fragmentation, subsector_rotation, oracle_turn_desk,
          oracle_tape_onset) -- si_handoff.json is not a Moving source", and
          `capability_crosscheck.md` row 47 confirms live that `basketdata/si_handoff.json` does
          NOT appear in the Moving fetch set at all. The Moving handoff/transition copy is
          rendered exclusively from `marketdata/rotation_events.json`'s own verbatim producer
          prose (row 44), eliminating the si_handoff-derived leadership-narrative contamination
          path DAC-007 critiqued.
        changed_files:
          - mockups/refs/reference_integrity/mastermind-xpv2-sector-r3b/build/views/moving.html
          - mockups/refs/reference_integrity/mastermind-xpv2-sector-r3b/build/DESIGN_SYSTEM_SPEC.md
      - id: DAC-008
        predecessor_ref: DAC-008
        kind: condition
        disposition: CARRIED_BLOCK
        note: >
          [severity: CONDITION; source: reviews/data_authority.md] Intelligence Hub copy is
          correct, but production still retains a stale policy-vote tooltip
          (templates/intelligence_hub.html.j2 "Five desks vote on each name") that the r2
          candidate's wording correctly fixes. Out of R3B's own scope entirely --
          intelligence_hub.html is a different route/surface from sector_central.html, and this
          item was only carried in r2's artifact set because r2 combined both surfaces under one
          reference_id. Carried forward rather than silently dropped, per RIG S13.1 and
          ORCHESTRATOR_ADJUDICATIONS.md S9 ("carried as CARRIED_BLOCK with ownership deferred to
          a future LENS RIG cycle -- not silently dropped"); ownership for closing this item
          rests with a future intelligence_hub RIG cycle, not R3B. Pending orchestrator
          adjudication (scope reassignment, not a candidate fix).

# -- What this file demonstrates ------------------------------------------------------------
# CONTINUATION PASS RESULT (orchestrator-adjudicated, this revision): of the 39 predecessor
# findings, 32 are RESOLVED_BY_CHANGE against the frozen R3B candidate, each with concrete
# evidence citations (QA_ATTACK_REPORT.md gate verdicts, FIX_VERIFICATION.md live re-probes,
# capability_crosscheck.md rows, copy_ledger.md/responsive_contract.md/accessible_alternative_
# contract.md, or a specific candidate feature). 2 of 39 (PRC-003 fully, DAC-005's tab-order
# sub-claim only) remain OVERRIDDEN on the R3A archaeology evidence recorded before this pass
# and are UNCHANGED by it. 5 of 39 remain CARRIED_BLOCK, honestly: four (PRC-008, VTC-009,
# VTC-010, DAC-008) target intelligence_hub.html/LENS, a route this cycle never touches
# (ORCHESTRATOR_ADJUDICATIONS.md S9 names ownership as a future LENS RIG cycle), and one
# (MAC-007) names a specific evaluability gap -- no real touch-dispatch harness -- that this
# cycle's evidence pack does not overcome even though it repairs the adjacent keyboard-focus
# defects the same review flagged. Several RESOLVED_BY_CHANGE rows above are themselves compound
# (PRC-001, MAC-002, MAC-003, VTC-005, MAC-008): where the original r2 finding named both a
# Sector Central defect and a LENS/intelligence_hub defect in one id, this pass resolves the
# Sector Central half with concrete evidence and says so explicitly, while disclosing the LENS
# half is untouched and out of route -- the same compound-finding honesty the DAC-005 OVERRIDDEN
# row above established as house practice, applied here to RESOLVED_BY_CHANGE rather than
# OVERRIDDEN. No id evaporates between cycles; every one of the 39 rows carries an explicit,
# evidenced disposition (rule_l10: continuity-carried-block-approved no longer applies to the 32
# resolved rows, and remains correctly enforced against the 5 still-CARRIED_BLOCK rows above).
