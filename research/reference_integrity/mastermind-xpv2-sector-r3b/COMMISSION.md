# XPV2-SC-R3B — Sector Central Institutional Reference Design Commission
## Sol-issued final commission after R3A authority/capability freeze

**Program:** `WS:INSTITUTIONAL-PRODUCT-EXPERIENCE-V2`  
**Wave:** `XPV2-SC-R3B`  
**Authority:** Sol, AI CEO  
**Final authority:** Chairman Chris Wong  
**Status:** `AUTHORIZED_TO_START`  
**Production implementation authority:** `NONE`  
**Reference authority:** R3A binding pack + current doctrine + frozen fixture  
**Recommended route:** Fable orchestrator with delegated Opus design lanes  
**Repository:** `mastermindx-market-intelligence/macro`  
**R3A merge:** PR #6122 / `f4305a4485f6df061f3a55ca6f1b0e3e53c06f66`  
**Frozen producer fixture capture:** `4c55fe433490adfd75fd901ef25f5793db2202db`  
**R2 visual artifact:** inspiration only, never product/data authority

---

# 0. Mission

Produce the **R3 institutional visual reference** for the entire US Sector Central six-view workspace:

1. Overview
2. The Map
3. What's Moving
4. Money & Breadth
5. Explore
6. Confluence

The reference must achieve the visual quality target of Mastermind-X Institutional Product Experience V2 while preserving every R3A-bound capability and every producer/authority boundary.

The observable outcome is:

> A cold user can move through all six Sector Central tasks on desktop and mobile and experience one coherent, premium institutional product, while every number, state, action, universe, route, access boundary, null and clock remains faithful to the frozen canonical producers.

This is a **reference-design wave**, not a production migration.

Do not modify production templates, engines, ranking, access, routes, global navigation or site behavior in this wave.

R3B stops with:

- a frozen interactive reference artifact;
- its producer-bound fixture integration;
- evidence;
- RIG successor packet;
- exact production-migration handoff draft.

Production implementation is a later wave after fresh independent RIG approval and Sol adjudication.

---

# 1. Why this matters

R2 proved that Mastermind can look dramatically better, but four independent critics blocked it because attractive simplification crossed into:

- wrong producer authority;
- missing user journeys;
- dead destinations;
- incomplete access behavior;
- incomplete routing;
- mobile squeeze;
- accessibility regressions;
- bilingual gaps.

R3A exists specifically so the R3 designer never has to guess those contracts.

Your job is therefore **not** to rediscover what Sector Central means.

Your job is to turn the frozen R3A truth into an exceptional product composition.

---

# 2. Authority and precedence

Read all of these before drawing.

Binding order:

1. Current Sol/Chairman instructions in this commission.
2. `research/reference_integrity/mastermind-xpv2-sector-r3/ADJUDICATIONS.md`
3. `research/reference_integrity/mastermind-xpv2-sector-r3/R3_DESIGN_BRIEF.md`
4. `research/reference_integrity/mastermind-xpv2-sector-r3/capability_disposition_ledger.md`
5. `research/reference_integrity/mastermind-xpv2-sector-r3/producer_binding_matrix.md`
6. `research/reference_integrity/mastermind-xpv2-sector-r3/routing_contract.md`
7. `research/reference_integrity/mastermind-xpv2-sector-r3/access_hydration_contract.md`
8. `research/reference_integrity/mastermind-xpv2-sector-r3/fixture/` and its provenance/receipts
9. Current `docs/DESIGN_DOCTRINE.md`
10. Current `research/MASTER_PRODUCT_DESIGN_SYSTEM_V1.md`
11. Current design-system specimen and shared `.mx-*` primitives.
12. Rejected R2 visual candidate **for visual direction only**.

If current production source materially contradicts R3A, STOP and return the mismatch. Do not silently update the fixture or reinterpret an adjudication.

---

# 3. Current-state verification before design

Before the design lanes start:

1. Rebase/read current `origin/main`.
2. Run the R3A fixture attack suite.
3. Verify R3A reference files are intact.
4. Compare current relevant producer/template contracts against the R3A merge.
5. Separate:
   - nightly/data value movement — expected, does NOT invalidate the fixture;
   - semantic producer/schema/route/access changes — may invalidate a binding and must be adjudicated.
6. Confirm no open PR currently owns Sector Central markup/reference scope.

**Do not refresh the frozen fixture merely because nightly values moved.**

The fixture is intentionally frozen for reproducibility.

Refresh requires a Sol/Fable adjudication that a canonical producer contract materially changed.

---

# 4. Commission decisions now resolved

## 4.1 One program, delegated design lanes

R3B is **one orchestrated reference program**, not six unrelated page commissions.

Reason:

- all six views share the same route;
- information hierarchy must be coherent;
- visual language must be identical;
- action versus context authority must remain consistent;
- mobile/task navigation is shared;
- independent view teams without one design lead would recreate local mini design systems.

However, Fable main must **not** personally design all six views.

Use delegated subagents.

### Principal Design Lead — Opus

Owns global page grammar, typography, geometry, shell integration, view navigation, component decisions, cross-view coherence, and final synthesis.

### Lane D1 — Overview + Confluence

Both carry Action authority.

Own action hierarchy, state selectors, gated content presentation, contextual conflicts, and mobile action composition.

### Lane D2 — Map + Moving

Both explain rotation/position context.

Own spatial/map language, event transition language, accessible chart alternatives, and context-only visual weight.

### Lane D3 — Money & Breadth + Explore

Both are dense evidence/research workspaces.

Own lists versus tables, breadth/flow, selected detail, historical/research organs, and mobile data density.

### Lane QA — Responsive/A11y design attack

Separate from designers.

Own design-stage attack for 320–820, 200% text, EN/ZH, target size, chart alternatives, and keyboard semantics.

This is **not** a final RIG critic seat. Later independent critics must be fresh.

Fable main:

- freezes scope;
- delegates;
- reconciles contradictions;
- maintains capability ledger;
- rejects authority drift;
- synthesizes final reference.

Do not use Fable main context for repetitive markup/CSS work.

## 4.2 Reference images

No additional external reference-image hunt is required to start.

### Positive visual direction

Use the rejected R2 candidate:

`mockups/refs/reference_integrity/mastermind-xpv2-turn3-r2/proposal/MASTERMIND_SECTOR_CENTRAL_V2_R2_FREEZE_CANDIDATE.html`

May inherit:

- calm SaaS/fintech spacing;
- resource-list treatment;
- reduced card inflation;
- strong typographic hierarchy;
- full-name emphasis;
- restrained dark/light aesthetic;
- simpler page answers;
- premium institutional tone.

Must NOT inherit:

- any hardcoded business fact;
- fake product topbar;
- Unicode/glyph icon system;
- dead hrefs;
- proportional Confluence label cells;
- shrunken mobile Map;
- incomplete ZH;
- altered authority;
- altered lane copy;
- local routing;
- static fake universes.

### Canonical design direction

Use current Mastermind design-system specimen and doctrine.

Do not invent a competing shell.

### Negative baseline

Current production Sector Central is evidence of capability and interaction—not a visual precedent.

## 4.3 Orchestrator

Use **Fable**.

This is a genuinely hard cross-view architecture/design integration problem and is an appropriate use of Fable.

Fable must delegate creative production to Opus design subagents.

## 4.4 Fixture

Keep the frozen fixture captured at:

`4c55fe433490adfd75fd901ef25f5793db2202db`

Do not rebake by default.

Its purpose is reproducible visual and authority review.

---

# 5. Product north star

The reference must embody:

> **Quiet Conviction**

Sector Central should feel like an institutional research terminal that has already decided what deserves attention.

At rest:

- less prose;
- fewer boxes;
- fewer repeated badges;
- stronger names;
- stronger relationships;
- clearer hierarchy;
- deeper evidence one step away.

The product must not communicate intelligence by exposing internal machinery.

The six views should feel like six instruments on one desk—not six independently vibe-coded mini-sites.

---

# 6. Global page architecture

Do not redesign global Mastermind navigation here.

Render Sector Central inside:

- the real/canonical product shell where practical; or
- a clearly quarantined reference frame that cannot be mistaken for proposed global chrome.

Do not reproduce R2's hand-built topbar as design law.

## 6.1 Workspace task order

Preserve:

1. Overview
2. The Map
3. What's Moving
4. Money & Breadth
5. Explore
6. Confluence

Use the existing canonical hash/workspace law.

## 6.2 Desktop

Expose:

- page/view answer;
- one dominant object;
- supporting evidence;
- a clear deeper path.

Do not give every module equal box weight.

## 6.3 Mobile

Mobile recomposes.

It does not scale desktop down.

Every view needs a deliberate phone composition.

---

# 7. Critical authority law

## 7.1 Action-authority views

Only:

- Overview Act-Now board;
- Confluence

carry gated/timed Action authority according to R3A.

## 7.2 Context views

- Map;
- Moving;
- Money;
- Explore

must not visually masquerade as another action board.

Primary colors, typography and CTAs remain lower-authority than graded Action controls.

## 7.3 Overview dual-read

Overview hero context and the Act-Now board are independent systems.

The reference must make that independence understandable.

Recommended pattern:

**Leadership context**  
A concise contextual read.

**Action board**  
Exact gated states below.

Do not use arrows, shared color, or prose that implies contextual leadership caused an action state.

---

# 8. Overview — design contract

## Primary question

> What should I do right now?

## Required capabilities

Preserve all R3A RETAIN capabilities including:

- hero context;
- exact five rendered action columns from six producer keys;
- exact counts;
- exact row order;
- score/performance/reason;
- row destination;
- premium full-count/preview/hydrate behavior;
- `#read-*` trace;
- Bottoming Watch;
- self-grader/Track Record;
- fold/show-more;
- stale/null/access behavior.

## Action states

Preserve exact producer terms and bilingual labels.

Six keys:

- `buy_now`
- `buy_soon`
- `on_the_run`
- `take_profits`
- `hold`
- `avoid`

Five visible columns because hold+avoid share Stand aside / `观望`.

Do not change vocabulary for design convenience.

## Desktop composition

Preferred:

1. contextual page answer;
2. quiet context metadata;
3. exact five-state distribution;
4. selected-state resource list;
5. secondary almost-ready/up-next content only if producer-backed;
6. Bottoming Watch full-width watch strip;
7. Track Record / grader deeper.

Avoid many equal-weight boxes.

## Mobile action selector — Sol amendment

R2's horizontally clipped five-state strip is prohibited.

At phone widths, all five visible state labels and counts must be discoverable **without hidden offscreen states**.

Recommended target:

- compact two-column state selector;
- first four states in two rows;
- Stand aside spanning full width;
- each target ≥44px;
- count aligned right;
- selected state uses weight/underline/surface rather than a giant tile.

Alternative designs are allowed only if:

- all five states are visible without horizontal scrolling;
- labels remain legible at 320px and 200% text;
- selected rows still appear early enough for the task.

## Bottoming Watch

Keep the name and authority.

It is:

- watch-only;
- display-only;
- nonpredictive;
- not an entry pipeline;
- not a sixth action lane.

Do not render hidden `signal` or `timing_state` fields.

Do not call it Early Entry, Early Turn Pipeline, Upgrade Watch, or similar.

The nonpredictive/null disclosure remains available in human language.

---

# 9. The Map — design contract

## Primary question

> Where does each group sit right now, and what is context-worth-watching?

No primary call to action.

## Desktop

The map/chart can remain the dominant object.

Support:

- clear quadrant labels;
- restrained annotation;
- selected object detail;
- exact current context fields;
- access to full underlying list/table.

## Known reco conflict

Map `reco` fields are marked:

`CONFLICT (context surface rendering action vocabulary)`

Preserve capability but **do not amplify it**.

Do not enlarge, brighten, make it a primary CTA, or make it equivalent to Overview action lanes.

Treat it as tertiary context beneath the existing disclaimer.

Do not “fix” or re-author its semantics in R3B.

## Mobile — required redesign

Do not shrink the full desktop map to phone size.

Phone default should prioritize:

1. concise context answer;
2. quadrant summary / strongest groups;
3. selected object;
4. accessible ranked/list representation.

The full map may remain further down the same view, behind an explicit disclosure, or in a governed detail sheet, provided the capability remains reachable in the same view.

## Accessible equivalent — mandatory

Add a table/list projection of the same `window.SECTOR_CYCLES` fields used by the sector-cycle chart.

No new rank/state calculation.

---

# 10. What's Moving — design contract

## Primary question

> What changed recently?

Context only.

## Critical source correction

Moving does **not** consume `si_handoff.json`.

It consumes exactly:

- `marketdata/rotation_events.json`
- `marketdata/sector_fragmentation.json`
- `marketdata/subsector_rotation.json`
- `basketdata/oracle_turn_desk.json`
- `basketdata/oracle_tape_onset.json`

Do not invent a leadership narrative from other payloads.

## Composition

Strong R2 visual ideas may survive:

- compact source → destination transition rows;
- recent-change strip;
- Crosscurrents comparison;
- quiet closed history;
- focused Desk Watch.

Every row/event comes from the bound fixture.

No action language.

---

# 11. Money & Breadth — design contract

## Primary question

> Where is money actually flowing, and how broad is the market?

Context only.

Preserve real production organs and clocks.

Do not fuse them into a new health/action score.

## Desktop

Prefer:

- one answer;
- three/four key reads;
- flow list;
- breadth object;
- heatmap/market participation;
- deeper supporting detail.

Avoid a panel mosaic.

## Mobile

No document overflow at 320, 360, 390, 430, 768, 820.

Stack/compress hierarchically.

## Accessible equivalent — mandatory

The market-heat treemap gets a table/text equivalent driven from the same `marketdata/sp500_heatmap.json` fields.

No alternate scoring.

---

# 12. Explore — design contract

## Primary question

> Let me look under the hood.

Explore is a real research workflow, not a sample table.

Preserve end-to-end:

- search/filter;
- current table;
- selected performance chart;
- top/bottom default behavior;
- Show all;
- Time Machine;
- Forming Narratives;
- Track Record;
- existing destinations;
- deferred chart mount behavior;
- relevant access/null states.

## Desktop

Target:

1. search + filters;
2. comparison table;
3. selected detail/performance;
4. research/history tools grouped deeper.

## Mobile

Do not squeeze a wide table.

Use responsive labelled rows or reduced priority columns with local table scroll only where genuinely necessary.

The page itself never overflows.

## Forming Narratives

Deterministic rank/score is deterministic.

Only `ai_watch` is model-originated.

Where `ai_watch` renders, visibly label it:

**Model analysis** plus approved ZH twin.

Do not label the entire panel AI-generated.

---

# 13. Confluence — design contract

## Primary question

> Which subsectors or baskets have timing support?

Action authority.

## Universe order

Preserve:

1. S&P
2. Nasdaq
3. Russell
4. Baskets

## State vocabulary

Use canonical producer vocabulary from the R3A binding pack.

Do not replace it with R2 synonyms.

## Distribution — Sol amendment

R2 proportional labelled cells are rejected.

All state labels and counts must remain legible simultaneously.

Use fixed readable state/count cells plus an optional secondary proportional visualization.

Text labels/counts may not be scaled by population.

## Active-universe law

Active universe controls:

- counts;
- distribution;
- coverage;
- queues;
- group rows;
- details;
- members;
- stock rows.

Never mix rows from another universe.

## Coverage

Do not author generalized coverage prose.

Render only fields/wording permitted by the active universe and R3A matrix.

Baskets has no gateable/thin producer field.

Do not invent a Baskets thin disclosure.

## Details

Preserve group detail, member stocks, stock detail, producer order, relevant caps, and full-table behavior.

## Mobile

All four universe selectors remain available.

State labels/counts remain visible without hidden horizontal continuation.

Then show:

- selected/Entry-now focus object if present;
- Tailwind queue;
- Late/Headwind compact list;
- deeper group/member detail.

No desktop two-column squeeze.

---

# 14. Routing and sticky-scroll Sol amendment

R3A records routing seams as not repaired.

R3B may not rewrite the production router.

However, one critic-blocking UX defect becomes a **required reference behavior**:

> canonical and legacy hash landings must not place the page/view answer underneath sticky chrome.

Reference must demonstrate correct scroll-offset behavior.

If production requires a router/CSS repair later, file it explicitly in R3C.

Other recorded seams remain documented and must not be claimed fixed without adjudication.

---

# 15. Access and premium behavior

Only Overview Act-Now is premium-gated.

Preserve exactly:

- full counts from full board;
- preview rows;
- locked row count;
- sign-in disclosure;
- authenticated hydrate;
- `tier_payload.v1` validation;
- 401/403/5xx/offline no-op preserving baked preview;
- fold rebuild.

Do not invent access-denied banners, locks on other views, or different teaser counts.

Evidence:

1. ungated/full;
2. gated preview;
3. successful hydrate.

All fixture-driven.

---

# 16. States

Render only states backed by current contracts.

Required demonstrations where supported:

- loading;
- zero;
- empty;
- stale;
- partial;
- error/unavailable;
- access shell;
- hydrated;
- long names;
- one/few/many/extreme cardinality.

## Correction/revision

`BLOCKED_DATA`.

No correction badge/history can be invented.

## Confluence staleness

Do not invent a freshness threshold.

Reference must not imply stronger enforcement than the producer owns.

---

# 17. Responsive and accessibility acceptance

## Automated width sweep — all six views

- 320
- 360
- 390
- 430
- 768
- 820
- 1024
- 1280
- 1440

At every width:

- no document-level horizontal overflow;
- no clipped state label;
- no clipped primary name;
- no off-screen control;
- no hidden-only capability.

## 200% text zoom — strengthened requirement

All six views must survive automated 200% text/browser zoom checks at:

- 320
- 390
- 768
- 820

No document overflow or clipped primary content.

Capture visual proof at least for:

- Overview 320/390;
- Map 390;
- Moving 390;
- Money 390;
- Explore 390;
- Confluence 320/390;
- one 768/820 dense state.

## Touch targets

Repeated/primary mobile targets ≥44 CSS px.

## Language

ZH parity includes:

- generated rows;
- buttons;
- placeholders;
- empty/error states;
- accessible labels;
- dialog/sheet labels;
- chart/text alternatives;
- state vocabulary.

## ARIA

Do not fake tab semantics.

Use proper tab semantics for tabs, group/toolbar/pressed semantics for filters/segments.

## Chart accessibility

Every customer-relevant chart has:

- accessible name;
- concise textual takeaway;
- data/list/table equivalent for nonvisual access.

---

# 18. Iconography and visual detail

Use sanctioned monoline Mastermind icons/shared icon partials.

Do not use decorative Unicode stars, emoji section icons, arcane glyphs, lightning emoji badges, or improvised sci-fi marks.

Country flags remain data/context where house law permits.

Sci-fi feeling comes from precision, chart quality, spatial depth, microinteraction and instrumentation.

---

# 19. Reference artifact architecture

Create a **reference-only** interactive artifact.

Do not edit production Sector Central files.

Suggested location:

`mockups/refs/reference_integrity/<new-sector-r3-reference-id>/proposal/`

Do not invent the RIG ID blindly.

Use current Reference Integrity tooling and the actual R2 predecessor record to determine lawful successor naming/continuity.

Reference must:

- consume frozen fixture;
- never fetch live `site/`/`data/`;
- not recompute authority;
- include EN/ZH;
- support light/dark;
- support all six views;
- expose required states;
- use real destinations represented from fixture/contracts.

Where an actual destination cannot execute inside standalone reference, use a noninteractive reference row with real destination metadata or a fixture-backed mock navigation handler that records the exact route.

Never use `href="#"`.

---

# 20. R2 continuity

R2 is rejected predecessor, not authority.

New RIG cycle must account for all surviving R2 findings.

Do not reconstruct continuity from memory.

Use current RIG continuity/mandate tooling.

Every unresolved R2 item gets a lawful disposition.

R3A is authority substrate, not approval.

---

# 21. Deliverables

R3B produces:

1. Interactive six-view R3 reference.
2. Design notes.
3. Pure display-tier fixture adapter/projection.
4. EN/ZH copy ledger for new display copy.
5. Responsive behavior contract per view.
6. Accessible-alternative contract.
7. State matrix evidence.
8. Capability cross-check against all 90 RETAIN and 2 BLOCKED_DATA entries.
9. Hash/deep-link evidence.
10. Access/hydration evidence.
11. RIG successor manifest/proposal/continuity packet.
12. Evidence crop index.
13. Exact R3C production-migration handoff draft.

Do not produce `approval.yml`.

Do not self-issue final verdict.

---

# 22. Evidence matrix

Minimum primary visual matrix:

For all six views:

- 1440 dark EN
- 1440 light EN
- 1440 ZH
- 390 dark EN
- 390 ZH

30 primary captures minimum.

Additional:

- 320 stress Overview
- 320 stress Confluence
- 820 dense/tablet Explore
- 820 dense/tablet Money
- Bottoming Watch watch-only state
- access preview
- hydrated Overview
- zero/no-action state if fixture supports it
- accessible Map alternative
- accessible heatmap alternative
- each Confluence universe structural proof
- long-name proof

Programmatic checks supplement, never replace screenshots.

---

# 23. Acceptance — product

Cold-reader tasks:

### Overview
Distinguish contextual leadership from gated action; identify exact lanes; open/select a real row; understand Bottoming Watch watch-only; reach grader/record.

### Map
Understand placement; select a group; reach detail; access nonvisual equivalent; never mistake it for action board.

### Moving
Identify recent changes from correct five artifacts; inspect rotation/fragmentation; reach destination/history; no invented action.

### Money
Understand flow/breadth; distinguish disagreement; inspect heat/participation; use accessible alternative.

### Explore
Search/filter; compare; inspect performance; reach Time Machine and Track Record; see model field correctly labelled.

### Confluence
Switch all four universes; see matching counts/rows; inspect group/member/stock detail; understand supported coverage; no foreign-universe rows.

---

# 24. Acceptance — authority

Reference verification must catch:

- action lane drift;
- context sector promoted to Buy now;
- Bottoming Watch escalation;
- foreign-universe Confluence row;
- producer order mutation;
- missing legacy hash;
- dead destination;
- access split collapse;
- null→zero collapse;
- wrong Moving source;
- false correction state;
- fabricated Baskets coverage;
- client rank/count/action recomputation.

R3A attack suite is the floor.

Do not weaken it.

---

# 25. Acceptance — visual/taste

Principal Design Lead explicitly reviews:

- first-screen hierarchy;
- panel count;
- whitespace;
- full-name readability;
- chart prominence;
- repeated caveats;
- excessive chips;
- authority-weight differentiation;
- mobile independence;
- dark/light quality;
- institutional versus playful styling.

No view passes merely for being cleaner than production.

Target:

> credible as a flagship product from a billion-dollar institutional fintech company.

---

# 26. Stop condition

Stop when:

- one coherent R3 candidate exists;
- design-stage gates pass;
- RIG packet is frozen;
- candidate SHA is stable;
- evidence matrix exists;
- capability cross-check is complete;
- no known blocker is hidden;
- production is untouched.

Return reference plus R3C handoff draft to Sol.

Do not merge production UI, dispatch production migration, self-approve, or reuse R3B authors as later independent critics.

---

# 27. Required continuation

After R3B returns, Sol fans out fresh:

1. Product Regression critic
2. Visual/Taste critic
3. Mobile/Accessibility critic
4. Data/Authority critic

Only after those reviews and Sol approval of the exact R3 SHA may production migration begin.
