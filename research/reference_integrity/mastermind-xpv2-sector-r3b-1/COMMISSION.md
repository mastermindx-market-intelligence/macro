# XPV2-SC-R3B.1 — Surgical Reference Correction Commission

**Program:** `WS:INSTITUTIONAL-PRODUCT-EXPERIENCE-V2`  
**Wave:** `XPV2-SC-R3B.1`  
**Authority:** Sol  
**Predecessor:** `mastermind-xpv2-sector-r3b` @ `dc84f78cddf04d9be90e9249126f9767de5725a6`  
**Predecessor verdict:** `REVISE`  
**Production changes:** forbidden  
**Recommended operator:** return to the R3B Fable orchestrator; delegate bounded Opus design and Codex QA lanes

## Mission

Produce one successor reference that fixes only what the fresh four-critic cycle proved defective.

The R3B product architecture, information hierarchy and visual grammar are frozen. This is a correction cycle, not a redesign and not R3C.

## Pre-start sequence

1. Integrate the four review-only critic PRs after rebasing to current main:
   - #6233 Product Regression
   - #6231 Visual/Taste
   - #6234 Mobile/Accessibility
   - #6228 Data/Authority
2. Confirm their diffs remain review/evidence only.
3. Commit the Sol predecessor `verdict.yml` and set R3B manifest status to `revise`.
4. No `approval.yml`.
5. Run the current RIG mandate/continuity tool against `mastermind-xpv2-sector-r3b`.
6. Create a lawful successor reference ID (prefer `mastermind-xpv2-sector-r3b-1` if accepted by current tooling).
7. Never edit the frozen R3B artifact in place.

## Operator decomposition

- **Lane A — authority hero repair:** Opus/Codex pair.
- **Lane B — accessibility + token repair:** Opus/Codex pair.
- **Lane C — verification hardening:** Codex.
- **Lane D — evidence recapture:** mechanical agent after A/B/C converge.
- Fable main adjudicates only and prevents scope creep.

## Do-not-reopen list

Do not redesign:
- six-view navigation/task order;
- State Ledge concept;
- action/context authority split;
- Overview five-state structure;
- Bottoming Watch role/placement;
- Map reserved-hue law;
- mobile Map quadrant recomposition;
- Moving five-artifact binding;
- Money core composition;
- Explore task architecture;
- Confluence universe order/distribution architecture;
- active-universe isolation;
- premium arithmetic;
- canonical router/hash law;
- sanctioned icon system;
- mobile workspace nav at <=359: **2 columns × 3 rows**.

## Fix packet

### R3B1-01 — restore active regime sizing
Source: `basketdata/baskets.json -> theme_intel.regime_sizing`.

At frozen fixture: active=true, gross_scalar=0.81.

Render `positions sized to 81% / 仓位缩至 81%` with the production inert-when-calm rule. Keep this visually distinct from graded action lanes. Do not invent local sizing logic.

Mutation: removing sizing while active must fail.

### R3B1-02 — restore leadership methodology caveat
Restore a compact Explain/receipt containing both:
- most recent ~3 weeks skipped by construction;
- shape read only, not a forecast.

No paragraph tooltip. EN/ZH, keyboard/touch reachable.

Mutation: deleting either clause must fail.

### R3B1-03 — restore migration note
Bind `theme_context.migration.note_en/note_zh` and show it compactly in the Money-is-rotating context block.

### R3B1-04 — restore playbook destination
Restore real `allocation.html` / `Open the playbook →` + native ZH twin. No fake recorder-only href.

### R3B1-05 — restore hero enrichment
Preserve subordinate producer-backed:
- outgoing 20d/5d relative-performance context;
- incoming 20d / climbing-fast context where present;
- 49 themes / 15 categories counts.

### R3B1-06 — normalize context score terminology
For `theme_intel.themes[].score`, use **Strength / 强度** in Overview-context and Map. Do not rename the independent action-board score.

### R3B1-07 — correct S&P thin coverage copy
Payload-bound wording must state that gate-dropped thin subsectors are omitted from the timed table, not listed there.

Recommended EN semantics:
`65 of 113 subsectors have enough live data to time. 48 are too thin to time and are omitted from the timed table.`

Author a native ZH twin. Do not render this for Baskets because the producer lacks gateable/thin fields.

### R3B1-08 — Moving help controls
For all three:
- effective target >=44x44;
- unique localized accessible name naming the concept;
- real DOM explanation with stable ID;
- valid expanded/controls relationship or equivalent;
- keyboard + coarse pointer/touch activation;
- accessible EN/ZH explanation;
- persistent disclosure gets lawful dismiss behavior.

Do not mint another global tooltip controller. Use the governed Explain pattern or a reference-local equivalent that can later bind to canonical LENS.

### R3B1-09 — severe zoom clipping
Fix surviving Money semantic clipping at commissioned 320-physical/200%.

Then sweep all six views at 320, 390, 768, 820 under the same 200% method in EN/ZH.

Require:
- zero document overflow;
- zero painted semantic clipping;
- zero clipped primary controls/names.

Do not rely only on scrollWidth.

### R3B1-10 — document language
Initial EN: `<html lang="en">`.
Chinese mode: `<html lang="zh">` or the current house-approved more precise Chinese tag.
Runtime toggle updates both `lang` and `data-lang`, including localized ARIA, and view/hash repaint must not reset it.

### R3B1-11 — contrast/type repair
Close all of:
- ZH-dark action/risk state labels 4.26–4.45:1;
- light `Still measuring / 测量中` 3.61:1;
- live `20d vs market` label 3.91 dark / 3.43 light.

Rules:
- >=4.5:1 at actual size;
- live type >=10px, no 9px exception;
- semantic token route only;
- no page-local literal-color patch;
- EN remains passing;
- dark/light + EN/ZH measured.

If production later requires a shared token amendment, record a separate bounded Design-System dependency for R3C. Do not edit production theme.css here.

### R3B1-12 — duplicate IDs
Remove the 22 repeated `id="ref-data"` values. Keep `data-path` as registry key or generate unique IDs. Run duplicate-ID and ARIA-reference audits.

### R3B1-13 — Confluence stock-picks 0.xx label
Identify the exact producer field and give it an accurate label/header + ZH twin. Do not call it Score/Probability/Confidence/Strength unless the producer contract explicitly says so. If ownership is ambiguous, stop and return the field/path to Sol.

### R3B1-14 — completeness proof hardening
Replace candidate-self-enumeration checks with bidirectional inventory checks:

`expected production inventory -> candidate inventory`
and
`candidate inventory -> allowed production/projection inventory`.

At minimum pin:
- sizing directive;
- method caveat;
- migration note;
- allocation destination;
- hero enrichment;
- all per-view working destinations.

Mutation suite removes each independently and must get a unique red. A guard true before the feature is not a guard.

## R3C-only conditions — record, do not implement

- Time Machine live oracledata episode/chunk fetch.
- Real `whenAuthSettled` and authenticated hydrate path.
- Real `/premiumdata/` 403/offline behavior.
- Current `si_workspace.js` router law.
- Heatmap `stock_url` bytes.
- No Baskets thin/gateable invention.
- No correction/revision invention.
- Upstream category_zh gap.
- Upstream generic reco_why copy quality.
- Site-wide relative-unit typography debt.

## Continuity

Generate successor continuity mechanically. For every predecessor item use exactly one lawful disposition:
- RESOLVED_BY_CHANGE
- CARRIED_BLOCK
- OVERRIDDEN
- SUPERSEDED

In prose, use fully-qualified identities: `mastermind-xpv2-sector-r3b::<role>::<id>`.

## Evidence at successor freeze

Old R3B screenshots become stale for approval once the SHA changes.

Recapture at minimum:
- Overview 1440 dark EN, 1440 light ZH, 390 EN/ZH with sizing, caveat, migration note, playbook, enrichment.
- Moving help controls desktop + 390.
- Chinese document-language probe.
- Money severe zoom.
- contrast proofs for ZH-dark action labels, Moving light status, 20d-vs-market.
- Confluence labelled stock-picks figure.
- duplicate-ID/ARIA report.
- all six views 1440 dark EN, 390 dark EN, 390 ZH.
- all commissioned severe-zoom cells programmatically.
- canonical/legacy hash smoke, gating/hydrate smoke, all four Confluence universes.

## Acceptance

Must pass:
- R3A attack suite 59/59 unchanged;
- successor reference verifier;
- RIG checker;
- deterministic build hash;
- bidirectional capability inventory;
- unique-kill mutations for restored hero capabilities;
- contrast matrix;
- duplicate IDs zero;
- accessible explain controls;
- severe zoom matrix;
- EN/ZH document language;
- no new `href="#"`;
- no production-path diff.

## Stop condition

Stop at a new frozen successor SHA with status `in_review`, fresh evidence, green R3B.1 checks, and an R3C draft carrying only live-integration conditions.

Do not issue approval, self-verdict, edit production, or start R3C.

## Final review

All current critic receipts become stale for the new SHA. After freeze use **four new fresh critic seats**:
1. Product Regression
2. Visual/Taste
3. Mobile/Accessibility
4. Data/Authority

The current critics may verify fixes during the build but may not be the final approval seats.
