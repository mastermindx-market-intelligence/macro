# Product Regression critic — quarantined first pass

- Frozen at: `2026-08-24T10:14:57Z`
- Dispatch head: `0e542f3eda09721f8a255a08bb9db09070090871`
- Frozen SHA: `d0830a374795925ee1e55b66c0cc42e329ac172d`
- Candidate SHA-256: `4adb4b6245e8e4aa5b68c850a615461327c0a5d2e672e4c203d6ba32a3b8d53c`
- Candidate size: `5,506,871` bytes
- Carrier: PR `#6337`, draft, `claude/xpv2-sc-r3b2-build`; PR `#6336` excluded without reviewing its bytes
- Inputs before freeze: production-before PNGs; `baseline.yml` capability/task manifest; exact candidate HTML; current producer/design/RIG drift diff. No continuity, adjudication, build/evidence receipt, prior verdict/review, R3A pack, doctrine, design-system, or RIG-law prose was opened.

## Independent observations

1. **No first-pass blocker found in the desktop six-view task path.** Direct headless-Chrome renders of every canonical hash at 1440 show one dominant answer/object per view and visibly retain the primary destinations and reads: five-state Overview board, Map chart plus selected-group path, Moving rotation/crosscurrent reads, Money breadth/flow, Explore filters/table, and Confluence universe/stock/group paths.
2. **The reference materially lowers first-screen reading cost versus production-before.** The candidate replaces very long production boards with a concise answer and staged disclosure while retaining deeper paths in the DOM/source inventory. This is a strength to preserve, conditional on the post-reveal broad smoke proving that the compression did not remove destinations.
3. **Access logic is structurally scoped to Overview.** Source inspection finds the three explicit states (`gated`, `hydrated`, `ungated`), hydration from `premiumdata/sector_central.json`, fold restoration, disclosure removal, and fail-open preview behavior. Other views read embedded/ungated fixtures. Runtime state switching was not independently automated before freeze, so state-specific DOM cardinalities remain `NOT_EVALUABLE` at this point.
4. **Routing is structurally present.** Six canonical view links, legacy-anchor resolver, theme/read deep-link handling, unknown-hash fallback, and scroll-margin boundary are present. A fully automated route matrix remains to be run after reveal; source presence alone is not credited as closure.
5. **Plain `Track record` reads intentionally in Moving.** It is a sober section label beside reliability/rank-fit explanation rather than a stranded replacement character; no product penalty found.
6. **Mobile screenshot limitation, not a finding.** Local Chrome CLI enforces an approximately 500-CSS-pixel minimum while cropping requested 390px screenshots, producing apparent right-edge clipping. Because that harness does not represent a 390px CSS viewport, those PNGs cannot support a mobile regression claim. Mobile geometry and label scan cost remain `NOT_EVALUABLE` until a true viewport-emulation probe is available.

## Provisional finding ledger

- No stable adverse finding frozen.
- Strength `PR-S01`: desktop information hierarchy is substantially faster than production-before without obvious loss of the six primary jobs.
- Limit `PR-L01`: true 390/320 viewport behavior and automated access/routing cardinalities not independently evaluable with the pre-reveal local Chrome CLI setup.

## Hash-bound scratch

Desktop render hashes are recorded by `shasum -a 256 critic_return/scratch/*.png`; the candidate hash above was recomputed immediately before this freeze. The 390px-named files are retained only as a tool-limit receipt and are explicitly non-evidence for clipping.
