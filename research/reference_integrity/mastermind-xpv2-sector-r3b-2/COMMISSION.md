# XPV2-SC-R3B.2 — Final Surgical Reference Closure Commission (as executed)

> **CHAIRMAN / USER-AUTHORED EXECUTION DIRECTIVE (2026-08-23) — governs over any conflicting
> disposition text below.** Continue this original Fable R3B/R3B.1 orchestrator session.
> Re-pin current main and the protected Sol Skillpack. Integrate review-only PR #6279 as
> evidence. Write the predecessor R3B.1 verdict.yml as REVISE using the durable critic
> dispositions Product Regression = PASS_WITH_CONDITIONS, Mobile/Accessibility =
> PASS_WITH_CONDITIONS, Data/Authority = PASS_WITH_CONDITIONS, Visual/Taste = BLOCK
> (PWC / PWC / PWC / BLOCK). Do not restate those durable dispositions as BLOCK / BLOCK /
> BLOCK / BLOCK; the overall Sol verdict remains REVISE. Bind each lane to its recovered
> durable receipt and never fabricate a missing independent receipt.
>
> Then flip the R3B.1 manifest to revise, run the current RIG mandate/continuity path,
> create the lawful R3B.2 successor, implement exactly B2-01 through B2-11 below, capture
> fresh evidence, and freeze a new immutable R3B.2 SHA with manifest status in_review. Stop
> there and return the new frozen SHA, candidate SHA-256, PR and evidence packet. Do not
> dispatch any Product/Visual/Mobile/Data final critics; their receipts must be fresh
> against the new SHA and Sol will commission them after reviewing your return. Do not
> self-approve, start R3C, touch production, change sc_flows producer bytes, or widen scope.

**Program:** `WS:INSTITUTIONAL-PRODUCT-EXPERIENCE-V2`
**Wave:** `XPV2-SC-R3B.2`
**Authority:** Sol
**Predecessor reference:** `mastermind-xpv2-sector-r3b-1`
**Predecessor frozen SHA:** `0667f800764bf210af6c1237ccf0b5f0a71b4af2`
**Predecessor candidate SHA-256:** `fec05b058fbc9dbe29744ad015b7ee9cd9baa5cb85bbbde739daa8b97644cf70`
**Predecessor verdict:** `REVISE`
**Production changes:** FORBIDDEN
**R3C:** NOT AUTHORIZED
**Recommended operator:** original Fable R3B/R3B.1 orchestrator, with bounded subagents
**Current main at Sol dispatch preparation:** `59fed333c06359ee82e3b8a533f3c4c929fa92fa` — RE-PIN at pickup
**Protected Sol Skillpack:** `mastermindx-market-intelligence/Mastermind@e1101eb2c1f17d801d480ded497b3fc1bb0ef18b`

## Observable mission

Freeze one corrected Sector Central reference in which the five surviving final-critic
blocker classes are closed at every commissioned viewport/language/theme state, without
changing the six-view architecture or touching production.

This is the final reference-correction wave.

## Why this matters

R3B.1 fixed almost the entire rejected R3B defect set. The remaining defects are small in
implementation size but cannot become migration law:
- one producer measure is presented as two different instruments;
- one encoded emoji bypasses the icon guard;
- one candidate-owned heatmap cannot prove readable text across its bins;
- one Chinese view presents a long English paragraph under `zh-CN`;
- one desktop fix disappears on mobile and strips figure labels/accessibility.

## Authority precedence

1. Current Chairman launch intent for XPV2.
2. Current protected Sol Skillpack pinned at pickup.
3. Current Macro `main` and current accepted source/producer contracts.
4. Sol's final R3B.1 adjudication.
5. R3B.1 final critic receipts against `0667f800...`.
6. R3B.1 `COMMISSION.md`.
7. R3A authority/capability binding pack.
8. R3B/R3B.1 rationale only where not contradicted by critics or higher authority.
9. Rejected R2 only as historical/visual evidence.

If a newer source law lands, stop and reconcile rather than rebaking semantics silently.

## Pre-start durability / collision gate

Before editing the successor reference:
1. Re-pin current `origin/main`.
2. Verify #6248 / `780cbcf6...` remains an ancestor.
3. Verify frozen R3B.1 candidate identity.
4. Verify open final Visual/Taste evidence PR #6279.
5. Persist the exact final Product Regression critic receipt.
6. Persist the exact final Mobile/Accessibility supplemental report.
7. Persist the exact final Data/Authority supplemental report.
8. Do not have Fable/Sol manufacture a missing independent receipt.
9. If a critic receipt is unrecoverable, rerun only that seat against the frozen R3B.1 SHA.
10. Commit predecessor `verdict.yml` = REVISE and manifest = `revise`.
11. Run current RIG mandate/continuity tooling.
12. Create a lawful successor ID, preferably `mastermind-xpv2-sector-r3b-2` if accepted by the checker.
13. Search for overlapping R3B.2 work again immediately before claim.

*(Execution note: gate items 5–9 were satisfied before this file landed — the three
chat-lost seats were lawfully RE-RUN against the frozen SHA and merged review-only as
PR #6309; Visual/Taste was filed by its critic as PR #6279 and integrated on Chairman
release. Durable dispositions: PWC / PWC / PWC / BLOCK.)*

## Operator decomposition

Fable main orchestrates only.

- **Lane A — authority/copy identity:** B2-01, B2-04, B2-08, B2-09.
- **Lane B — responsive/a11y geometry:** B2-05, B2-06, B2-07, B2-10, B2-11.
- **Lane C — heatmap measurement + guard hardening:** B2-03.
- **Lane D — icon / decoded-output guard:** B2-02.
- **Lane E — evidence recapture:** mechanical, after A-D converge.

No R3B.2 author may occupy a final critic seat.

## Immutable architecture — do not reopen

Do not redesign six-view navigation/task order, State Ledge, action/context authority
split, five Overview action states, Bottoming Watch, Map reserved-hue law, mobile Map
recomposition, Moving source family, Money hierarchy, Explore workflow, Confluence
universe order/distribution, premium arithmetic, router/hash law, mobile 2×3 navigation,
sanctioned icon system, or ZH direction-color inversion.

Do not change producer ranking, lane assignment, state, counts, class or ordering.

## Exact fix packet

### B2-01 — one producer measure, one customer term
Trace every customer-visible use of `theme_intel.themes[].score`.

Canonical customer label: **Strength / 强度**.

Remove copy claiming the action-board value is a separate producer field. Build a
producer-path→customer-label map and fail if one producer path maps to multiple customer
terms without an explicit authority adjudication. Mutation: change one occurrence back to
Score; unique red.

### B2-02 — decoded emoji closure
Remove the encoded chart emoji from Track record. Prefer plain `Track record / 跟踪记录`.

Guard literal emoji, decimal/hex numeric entities, decoded DOM text, accessible names and
generated content where measurable.

### B2-03 — candidate-owned heatmap readable ink
This applies to the reference `.hm-t` color-bin heatmap, not the byte-verbatim `sc_flows`
producer fragment.

For every actual bin/theme/language:
- determine effective painted background;
- choose lawful foreground deterministically;
- require normal-text contrast >=4.5:1;
- do not use text-shadow as the compliance mechanism;
- preserve visual bin ordering.

If the field cannot be measured reliably, give labels a measurable high-contrast substrate
while keeping the data color visible. The final verifier must treat these cells as
blocking, not exclude them as `unmeasurable`.

### B2-04 — ZH language-of-parts
The visible grader/pre-freeze note cannot be substantial English prose inside inherited
`zh-CN`.

Preferred: native ZH display projection with identical governed meaning. If raw English
must remain, wrap it with `lang="en"` and identify it as source-language content.

Add a scanner for visible substantial Latin prose under ZH while allowing
tickers/acronyms/units.

### B2-05 — mobile figure labels / VTC1-001
Audit all 18 `.r3-fig` instances.

At every width:
- every figure has an accessible name;
- desktop column header can provide visual naming only while visible;
- at <=640, if `.r3-cols` disappears, each figure gains an inline visible label or the
  legend is recomposed.

Explicitly prove Overview `Strength 76 / 强度 76`, Confluence `Conviction / 综合把握` at
320/390, and meaningful AT names for all other figure classes.

### B2-06 — treemap group-label fit
At 320/390 no group/sector label overlap. Primary identity wins. Secondary group label may
drop if it cannot fit; accessible/list equivalent retains full information. Use painted
width, not character count.

### B2-07 — mobile heatmap interaction
At <=640, 503 small tiles must not be required individual tap targets. Prefer the heatmap
as context visualization plus the existing large Browse/List escape hatches. If a tile
stays interactive, it must meet the applicable target floor. Same information remains
reachable in the same view.

### B2-08 — rank scope next to rank
Move `Rank across all groups` / native ZH twin into the immediate Map/Themes rank
label/header.

### B2-09 — recent-wrong ink
`RECENTLY WRONG (LOGGED)` must use neutral history/error ink, never reserved
up/positive/Buy color.

### B2-10 — remove meaningless mobile ramp
When state cells stack vertically, suppress the horizontal five-segment ramp if it no
longer maps to the cells. Do not replace it with decoration.

### B2-11 — receipt stays with wrapped headline
Use flow-aware placement so the Overview receipt belongs to the whole statement after
wrapping. Test 320/390 EN/ZH and large text.

## Explicit non-goals / upstream debt

Do not change:
- `sc_flows` producer fragment bytes;
- production `theme.css`;
- production Forward-track-record source;
- producer REGIME BUY vocabulary;
- producer `reco_why`;
- category_zh source gap;
- live Time Machine;
- auth settlement;
- premium API;
- production router;
- any engine/rank/state/action logic.

Carry each as an explicit R3C/upstream dependency.

## Data / time / null / correction law

Unchanged:
- rank/order/state/action/counts producer-owned;
- no local recomputation;
- zero != missing;
- no invented Confluence staleness threshold;
- no correction/revision UI;
- no Baskets thin/gateable disclosure;
- premium full counts remain canonical;
- reference hydrate is only a harness;
- nightly value churn does not justify fixture refresh.

## Verification hardening

Required discriminating checks:
1. producer-path→label uniqueness;
2. decoded-render no-emoji;
3. heatmap per-bin readable-ink measurement included in blocking summary;
4. ZH language-of-parts;
5. all `.r3-fig` visible/AT naming by breakpoint;
6. treemap painted-label collision;
7. mobile tiny-interactive-tile census;
8. rank-scope proximity;
9. neutral recent-wrong computed color family;
10. mobile ramp absent when stacked;
11. headline receipt geometric association.

No check may pass over an empty selector set without a nonzero census assertion.

## Evidence matrix

Mandatory changed-state captures:
- Overview 1440 dark EN;
- Overview 390 dark EN and ZH;
- Overview 320 ZH at large text;
- grader note in ZH;
- Confluence 1440/390/320 with Conviction;
- Map 390 rank scope;
- Moving Track record without emoji;
- Moving recently-wrong state;
- Money heatmap 320/390/820 dark/light EN/ZH;
- treemap collision stress 320/390;
- mobile Browse/List escape hatch;
- wrapped headline receipt 320/390.

Programmatic:
- all six views at 320/390/768/820 severe zoom;
- all six at 1440 dark/light EN;
- all six at 390 EN/ZH;
- canonical+legacy hash smoke;
- gated/hydrated/ungated;
- Confluence four universes;
- duplicate IDs zero;
- `href="#"` zero;
- decoded emoji census;
- language-of-parts census.

## Acceptance

Must pass:
- R3A 59/59 floor;
- successor verifier;
- RIG checker;
- deterministic double-build;
- zero production-path diff;
- bidirectional capability inventory;
- all new unique-kill mutations;
- zero candidate-owned heatmap contrast failures;
- zero substantial unmarked English prose in ZH default UI;
- zero naked `.r3-fig` values at 320/390;
- zero decoded emoji;
- zero relevant mobile overlap;
- no duplicate authority/state/control plane.

## Stop condition

Stop when the R3B.2 successor is frozen at a new immutable SHA, manifest=`in_review`,
evidence is fresh, continuity is complete, and the R3C draft carries only remaining
production/live/upstream conditions.

Do not self-approve, mint `approval.yml`, edit production, start R3C, or widen into
site-wide design-system work.

## Final review after R3B.2

The SHA changes, so R3B.1 receipts become stale.

Use four **fresh** final seats again, but make the review **delta-focused**:
1. Product Regression — changed surfaces + capability/routing/access smoke.
2. Visual/Taste — changed surfaces + all-six-view first-viewport smoke.
3. Mobile/Accessibility — changed responsive/a11y surfaces + severe-zoom smoke.
4. Data/Authority — changed labels/projections + full authority-invariant smoke.

They still review the exact new SHA independently under rationale quarantine.

If all candidate-owned blockers are closed, Sol should adjudicate approval rather than
commission another revision for cosmetic residue. Only a genuinely material new
candidate-owned blocker warrants R3B.3.
