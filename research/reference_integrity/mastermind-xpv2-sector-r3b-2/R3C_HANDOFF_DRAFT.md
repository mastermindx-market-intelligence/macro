# R3C HANDOFF DRAFT — mastermind-xpv2-sector-r3b-2 successor (DO NOT START)

> **DO NOT START.** R3C production migration is NOT AUTHORIZED (Sol FINAL CONTINUATION
> HANDOFF; Chairman directive: "No production, no R3C"). This draft exists so the frozen
> R3B.2 successor carries only the remaining production/live/upstream conditions, per the
> handoff's stop condition. It becomes actionable only after the four fresh delta-focused
> final critic seats review the frozen R3B.2 SHA and Sol issues an approving
> design-authority verdict naming that exact SHA.

## §1 Live-integration conditions (carried forward, unchanged in kind)

R3C must implement from the real platform, never from the reference harness:
- Time Machine live `oracledata` episode/chunk fetch (the reference's manifest-only TM is
  disclosed, lawful reference behavior — PRC-001 lineage).
- Real `whenAuthSettled` and the authenticated hydrate path; real `/premiumdata/`
  403/offline behavior (the reference hydrate is only a harness).
- Current `templates/si_workspace.js` router law at migration time (re-verify the byte
  contract then; do not assume the frozen mirror).
- Heatmap `stock_url` producer bytes for tile destinations.
- No Baskets thin/gateable invention; no correction/revision UI invention (both
  BLOCKED_DATA rulings stand).

## §2 Upstream producer/product debt (Sol handoff §6 — recorded, NOT absorbed by R3B.2)

- `PRC1R-U01`: production-owned "Conviction" naming collision — two different
  measures/scales under one customer word (`combined_score` 综合把握 vs
  `conviction.score` 信心). Engine/template naming repair.
- `PRC1R-U02`: producer English-only grader/category strings under ZH
  (`grader.pre_freeze_note` note_zh gap, `category_zh` gap, language-of-parts
  implementation). Producer/R3C owner; the reference may not mint producer translations
  (old B2-04 withdrawn on exactly this ground).
- `sc_flows` byte-verbatim fragment: real measured contrast debt (75 cells, worst
  ~2.24:1) plus magnitude compression from the tint ceiling. Repair at the
  producer/design-system owner without breaking the sha256 receipt.
- **Candidate-owned heatmap colour-field text (`.hm-t`, ~440 shadowed glyphs): the axis
  is UNMEASURED** by the flat-surface contrast method (durable Mobile seat). R3B.2
  reports it honestly and never converts UNMEASURED into PASS; R3C owes a
  human/appropriate-method legibility review and, only if proven necessary, a bounded
  design-system platform treatment.
- Production `Forward track record: Validated` badge/21d pairing beyond the
  reference-local B2-15 qualification (the badge's own semantics are stronger than the
  context-only / 5d-proven evidence warrants; `sector_central.html.j2:3460-3468` scope
  mismatch); the overloaded tri-valent production "validated" vocabulary (DA1-04).
- Production `REGIME "BUY"` vocabulary: must remain context-only in presentation.
- Repeated producer `reco_why` copy quality (8 distinct strings over 105 rows).
- Production hero `fmtPct`-on-fraction unit defect (same byte rendered at two magnitudes;
  the reference renders one consistent board-scale value).
- Production `templates/subsectors.js:221-222` "listed in the table" falsehood — proven
  wrong against a real payload by this reference lineage; still ships to customers.
- `DA1-05` "All subsectors 65" false universal (production's own string).
- Site-wide relative-unit typography debt (VTC1-era advisory; type ramp is 100% px).

## §3 Scroll/anchor + chrome seams (inherited from the R3B/R3B.1 drafts)

- `--ref-sticky-offset` scroll-offset behavior is required reference behavior; R3C
  implements the production equivalent (near-bottom targets land at the natural scroll
  ceiling — accepted geometry, R3B FIX_VERIFICATION F-6).
- `#sc-top` legacy anchor: recorded seam (20/21 scroll-targets; the one absence is
  documented in build/README_BUILD.md).
- The mobile-nav chrome divergence needs a production adjudication: 3 cols × 2 rows at
  360–767 and the Sol-RATIFIED 2 cols × 3 rows at ≤359 (MAC-004 override; immutable for
  the reference).

## §4 What R3C must NOT re-litigate

The do-not-reopen list (handoff §8) verbatim: six-view order, State Ledge, authority
split, five action lanes + producer order/counts, Bottoming Watch, Map reserved-hue law,
mobile Map recomposition, Moving five-artifact binding, Money hierarchy, Explore
workflow, Confluence universe law + active-universe isolation, premium arithmetic,
router/hash law, 2×3 mobile nav at ≤359, sanctioned icon system, complete ZH
direction-color inversion, all producer rank/state/class/order/count authority. Plus the
Sol-settled label law: `theme_intel.themes[].score` = **Strength / 强度** everywhere;
`reliability=low` = **Low confidence / 低置信度**; the word "thin" belongs to
`coverage.n_thin` alone.
