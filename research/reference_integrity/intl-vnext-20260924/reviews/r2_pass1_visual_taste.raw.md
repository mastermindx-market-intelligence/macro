role: visual_taste
reviewer_identity: "claude-opus-visual-taste-intl-r2-20260924"
pass: 1
artifact_sha: "62d710eedb9e5cc2a9d9f3e17fc02cc302de39f9"
verdict: BLOCK
findings:
  - id: VTC-R2-001
    severity: blocker
    finding: "Light is still a token swap, not a light design, because the artifact deletes the estate's light contrast plane: reference-r2.html:94,175-177 port production templates/intl.html.j2:73,75-77 essentially byte-for-byte except that every `color:var(--ink-down, var(--down))` / `var(--ink-up, …)` / `var(--ink-warn, …)` is stripped to the raw token — the file declares ZERO `--ink-*` while production declares 51; site/theme.css deepens the light plane on purpose (`--ink-mix-up:62% --ink-mix-down:84% --ink-mix-warn:62%`, `--ink-up: color-mix(in srgb, var(--up) var(--ink-mix-up), var(--text))`) because the raw hues fail on white — light `--up:#1f9a55` and `--warn:#b9791a` each measure ≈3.6:1 on `--panel:#ffffff`, and they carry 11px/700 text in every `.chip.warn/.caut/.ok` and 12px in `.pcard-decomp .v`, so in proposal-r2-desktop-light-en.png essentially every state chip and every direction number sits under the 4.5:1 floor the estate already paid for; `.rrg-weak` (:239) compounds it with white on light `--orange:#c4781f` ≈3.47:1 at 10.5px/700 on the three Weakening pills actually visible in that capture."
  - id: VTC-R2-002
    severity: blocker
    finding: "The ZH state/severity chips ride the direction pair and invert, against the artifact's own written promise: reference-r2.html:65 says 'swap direction ink only, never state/severity ink' and :66 flips `--up:#e06464; --down:#45b873`, but :175 paints `.chip.warn` from `--down` and :177 paints `.chip.ok` from `--up` — so in proposal-r2-desktop-dark-zh.png the Act 2 market-state chips 崩跌损伤未修复 (KR), 正在破位 (JP) and 下行趋势 (IN) render GREEN where the EN capture renders them red, and the same class carries the Act 4 fragility warnings (:834-836, :846-848 债务占GDP 102% 且上升) and the Act 6 央行 加息 chips (:1027-1029), while `.chip.ok` turns 低 crash-pressure (:799,807,815) and 降息 (:1030) red; site/theme.css's zh block states the governing law verbatim ('Status colours … encode health/danger, NOT price direction, so they are deliberately left alone'), and the reference newly extends the flipped pair into three categories production never routed through it (fiscal fragility tags, central-bank stance, pressure severity) — a crash state printed green in one of two shipped locales is a wrong reading, not a parity nit."
  - id: VTC-R2-003
    severity: major
    finding: "The rotation table shows two evidence columns that cannot reconcile to the rank it sorts by, and its plain-word rationale argues the opposite of the number beside it: engine/intl_rotation.py:21-31 scores `gov*(0.5*rs20 + 0.3*rs10 + 0.2*rs5) − ob_penalty − macd_penalty`, but reference-r2.html:669-705 prints rank, rs20 and rs5 only, leaving rs10 (30% of raw), the gov multiplier and the ob_penalty magnitude invisible — so the board reads visibly non-monotone (🇹🇼 TW owns the best rs20 on the page, +6.87, at #2 behind KR's +5.03; 🇬🇧 GB, the ONE row carrying no 'held back' note, sits #5 beneath 🇯🇵 JP at −1.93 and 🇪🇺 EZ at −2.77 despite a better −1.45), and because `gov` multiplies a NEGATIVE raw score toward zero, the muted note 'held back — breaking down' at :688 in fact describes the multiplier that RAISED Japan above the unpenalised UK; this re-creates VTC-007's distrust mechanism in a subtler, harder-to-catch form inside the panel that most loudly claims to show its work."
  - id: VTC-R2-004
    severity: major
    finding: "The Material Risk Radar's headline quantitative column is a tautology in all seven rows and contradicts the ordinal next to it: :629,632,635,638,641,644,647 each print value and base rate identical (~29 vs ~29, ~31 vs ~31, ~27 vs ~27, ~20 vs ~20, ~19 vs ~19, ~18 vs ~18, ~15 vs ~15) and the caveat at :651 concedes 'own history only for every row above', so a full column of a core act is a comparison affordance with nothing to compare and reads to a user as a rendering bug; worse, the severity ordinal beside it inverts against it — 🇯🇵 JP carries the board's HIGHEST odds (~31%) at the second-mildest state 'Watch', while 🇦🇺 AU at the LOWEST (~15%) is rated 'Caution', above 🇮🇳 IN 'Calm' at ~18%. The artifact already knows the right move here and uses it twice elsewhere (`null-note`, :852 and :1079); an honest-absence state belongs in this column, not a two-number cell."
  - id: VTC-R2-005
    severity: major
    finding: "The route loses every time-series and every spatial plot: reference-r2.html contains one `<svg>` in the whole file (:461-467, the 66/100 dial) against 17 svg/spark/canvas marks in templates/intl.html.j2, and the now-valid baseline shows exactly what goes — baseline-r2-desktop-light-en.png's 'Performance in US dollars' panel carries seven per-market 3-month price sparklines plus the `local equity · currency tailwind · USD return` legend and a 1M/3M/6M/12M/YTD horizon control, all of which `.pcard` (:541-610) replaces with three static numbers, and the RRG scatter becomes a word table that still keeps the plot's name, 'US-vs-world quadrant (RRG)' (:713). For a user job whose verb is 'identify where leadership and risk are CHANGING', eleven tables and one dial have traded the shape of the change for prose about the change; VTC-005 is materially reduced but not cured."
  - id: VTC-R2-006
    severity: major
    finding: "Undisclosed bar magnitudes sit inside the very panel that lectures about undisclosed magnitudes: the Tier 1 sender bars are hard-coded 100% / 95% / 90% (:775-777) directly above a caveat stating 'no raw spillover unit printed at rest' (:778), and the crash-pressure bars are 61% / 58% / 57% (:800,808,816) with no unit, legend or axis — a 61%-filled bar rendered beside a 'Low' chip reads as a contradiction in proposal-r2-desktop-dark-en.png. The file header (:6-10) claims 'Nothing here is fetched, computed, randomised or invented'; three round, evenly-stepped bar lengths tracing to no published field are precisely the class of figure that claim cannot cover, and they appear two acts after the page correctly refuses to print an unsourced certainty number."
  - id: VTC-R2-007
    severity: minor
    finding: "The ZH tracking repair covers 3 of ~10 affected selectors: :75 zeroes letter-spacing for `.eyebrow`, `.act-eyebrow` and `h2` only, while `.vk` (.04em, :204), `.pcard-decomp .k` (.03em, :218), `.radar-table th` and `.rot-table th` (.03em, :223,232), `.tier-label` (.05em, :248), `.insp-metric .k` (.03em, :279) and `.page-head h1` (−.01em, :146) all carry Hanzi in the committed zh captures (市场 · 状态 · 本币 · 轮动排名 · 第一层 · 主要冲击输出方) and keep Latin tracking; `.pcard-decomp .k` is additionally 9.5px, below theme.css's smallest ramp step (`--fs-micro:10px`), which is exactly where compactness stops being dense and starts being unreadable — and it degrades worse for CJK glyphs than for the Latin 'USD/FX' labels it was sized for."
  - id: VTC-R2-008
    severity: minor
    finding: "The harness's own 390px control does not preview the 390px design: `.stage.mobile-preview{max-width:390px}` (:136) narrows the stage inside a desktop viewport while every recomposition rule is a viewport `@media` (:339-356), so pressing '390px' renders the four-column `.pulse-grid`, the `repeat(4)` vitals/`insp-metrics` and the `min-width:560px` rot-table crushed into 390px — the opposite of what the real-viewport mobile PNGs correctly show. The captures are sound; the apparatus beside them invites the wrong adjudication. Related sloppiness at :346: `@media (max-width: 480px), (max-width: 390px)` — the second query is wholly subsumed by the first."
  - id: VTC-R2-009
    severity: minor
    finding: "Forced states are adjudicated in dark/EN only (proposal-r2-desktop-dark-en-{loading,empty,error,stale}.png), with no light counterpart for the one presentation family that is pure surface: the skeleton is `linear-gradient(90deg, var(--panel2) 25%, var(--line) 37%, var(--panel2) 63%)` (:314), which on the light plane is #eef1f6 → ≈#cdd0d6 → #eef1f6, roughly a 1.1:1 sweep that will read as a static grey slab rather than a loading animation, and `body.is-stale .real-content{opacity:.82}` (:334) fades already-marginal light ink further (see VTC-R2-001). The loading caption also overclaims: ':415 says 'skeleton shown at true content geometry' while the skeleton is two equal 120px blocks (:407-410) against Act 1's actual 1.1fr/1fr gauge-plus-vitals composition (:182,202)."
  - id: VTC-R2-010
    severity: minor
    finding: "The compression strategy's central promise — 'every remaining job stays reachable' (:1001) — is asserted by six affordances that in the fixture resolve to the page you are already on: `href=\"intl.html\"` at :1034, :1047, :1059 and :1070-1072 ('+3 more … open full central bank desk', 'Open full credit & bond desk', 'Open full rates & curve desk', 'Open full correlation matrix', 'Open regime map', 'Open ranked reference views'). Only :1082-1084 point somewhere real. Separately, docs/DESIGN_DOCTRINE.md:175-177 bans emoji as UI icons and names `templates/_icons.html.j2` (which exists) as the replacement; the reference makes flag emoji the sole market identifier in all six acts at 11–26px (`.insp-hd .flag{font-size:26px}` :274, `.frag-flag{font-size:20px}` :261). That matches production and is not a regression, but an institutionalization reference re-commits the banned device at enlarged size instead of retiring it."
  - id: VTC-R2-011
    severity: minor
    finding: "One named quantity still prints two answers inside one panel — the VTC-008 family recurring at reduced scale: :477 reads 'Dragged by 🇯🇵 Japan · 🇮🇳 India · 🇪🇺 Eurozone' while the adjacent right column at :488 reads 'dragged by Japan, India', ~400px apart in the same `.panel` and both visible in a single glance in proposal-r2-desktop-dark-en.png. On a page whose declared identity is engine-owned receipts, a two-name and a three-name version of the same drag list in the same card is the one defect the hero cannot afford."
strengths:
  - "The blocked pass-1 evidence gaps are genuinely closed, not papered over: the matrix is now a full 8 cells (desktop/mobile × dark/light × en/zh) plus four forced states; baseline-r2-desktop-light-en.png paints the entire 1440×6387 production page instead of ~35% of it, so a real before/after comparison is finally possible; and the account/live plane that borrowed sophistication in R1 is gone outright — zero matches for Saved / Alert settings / Pin market / desk brief / Export / Change lens / LIVE DESK anywhere in the file."
  - "ZH is authored as a real locale, not a bilingual literal: every string on the page carries paired `.l-en`/`.l-zh` spans including the caveats, honest-nulls and methodology receipts (e.g. :826-827's full Berg & Pattillo base-rate disclosure in both languages), the zh body gets a proper PingFang/Hiragino/YaHei stack (:71-72), and proposal-r2-desktop-dark-zh.png shows full compositional parity with the EN capture at identical act rhythm."
  - "The USD/local/FX decomposition that VTC-006 demanded is not merely present but arithmetically sound in all seven cards — KR −5.3+9.8=+4.5, TW +3.4+0.8=+4.2, JP −7.2+2.4=−4.8, EZ +0.7+0.5=+1.2, GB +2.2−0.9=+1.3, IN −2.6+0.7=−1.9, AU −0.9+1.2=+0.4 — on one declared 3-month window, with the rank badge explicitly disclaimed as rotation rank rather than a performance ordering (:538)."
  - "The two vocabularies VTC-009 flagged as fused are now separated structurally AND defended from both sides: turn state lives in :669-705, the RRG quadrant in :717-729, and each panel carries a caveat naming the other (:707-708, :714-715) — a rarer and better fix than merely splitting the columns."
  - "Honest-absence is treated as a first-class designed state rather than a hole: :852 discloses the five economies with no concurrent fragility warning, :910 volunteers that Bank of Korea is outside this session's 4-of-7 central-bank sample, :990 nulls balance-sheet data to Fed/ECB/BoJ only, :1045 caps EM spread history at the vendor's Jul-2023 start, and :1079 prints a zero-row sector board as a null instead of hiding the panel — and the 4+3=7 bank arithmetic between :910 and :1034 actually reconciles."
  - "The presentation is not a design-system bypass: the material system is a static `<style>` block in `<head>` (:26-359) with a documented token port, the only JavaScript (:1099-1154) flips `data-theme`/`data-lang` attributes and toggles harness classes and inspector tabs, and DESIGN_DOCTRINE's named dark-first hazard — the aurora backdrop — carries an explicit light counterpart at 7%/5% versus dark's 18%/12% (:52-53, :62-63) rather than being left to become a pastel stain."
  - "The six-act rhythm (accent rail, numbered chip, title, one-line purpose eyebrow) holds identically across all four viewport×theme cells and gives the route a scanning spine production's flat `sec-h` stack does not have; at 390px the recomposition is real single-column stacking with tables scrolling inside their own containers rather than the page, which is the correct call for the rot/cb tables."
