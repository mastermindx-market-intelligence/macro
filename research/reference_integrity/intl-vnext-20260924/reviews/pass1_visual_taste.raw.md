role: visual_taste
reviewer_identity: "claude-opus-visual-taste-intl-20260924"
pass: 1
verdict: BLOCK
findings:
  - id: VTC-001
    severity: blocker
    finding: "No light-mode proposal evidence exists — all five frozen images at f3873dcb are `-dark-en` (proposal-hero/desktop/rotation/transmission/country-inspector-dark-en.png) while the baseline committed a full 8-cell matrix including baseline-desktop-light-en.png and baseline-mobile-light-zh.png; light is not a recolor here because the material system is dark-native (amber display type ~#E8B86B on near-black in the hero, and tinted low-alpha risk surfaces carrying depth on the Japan/South Korea cards, the three Risk Radar wells, the `05 Reversal risk rises` chain cell and the `CURRENT STANCE / Stand aside` well), all of which invert to pastel washes and fail large-weight contrast over white, so light is an unadjudicated art direction, not a derived state."
  - id: VTC-002
    severity: blocker
    finding: "No mobile proposal evidence at any viewport, against baseline capability `shell.theme-language-responsive` marked importance: core (baseline.yml:128-131) and four committed baseline mobile captures at 390px; the composition is specifically hostile to 390px in three named places — the 6-column `International risk map` (COUNTRY/FX/CREDIT/EXTERNAL/POLICY/MARKET), the 5-column `Where capital is moving` table (MARKET/20D AGO/NOW/MOMENTUM PATH/STANCE) whose entire argument is the horizontal momentum-path bar, and the 5-step left-to-right arrow chain `01 Dollar strengthens → 05 Reversal risk rises`, none of which has a committed narrow form."
  - id: VTC-003
    severity: blocker
    finding: "No Chinese proposal evidence, and the design's whole secondary hierarchy is carried by a Latin-only device — uppercase plus wide letter-spacing on THE GLOBAL CALL, SIGNAL STACK, CURRENT POSTURE, CONFIRMS IF / INVALIDATES IF, TIER 1 · ORIGIN STRESS, SENSITIVITY TO ANOTHER +1σ DOLLAR MOVE, RECENT STATE EVENTS — which CJK cannot express, while production carries real per-locale strings through `t()` (templates/intl.html.j2:678, 857, 923, 1490) and `l-en`/`l-zh` spans (:1856); the hero eyebrow is additionally frozen as the always-bilingual literal `GLOBAL MARKETS · 国际市场` inside the EN capture, which contradicts that per-locale contract and leaves ZH parity entirely unadjudicated."
  - id: VTC-004
    severity: blocker
    finding: "The 'production-before' side of the comparison is unusable: baseline-desktop-light-en.png is a 1440×6387 canvas that is blank white from roughly y2200 to y6387 (~65% unpainted), baseline-desktop-dark-en.png is blank dark over the same range, and baseline-mobile-dark-zh.png paints only ~2,580 of 11,959px (~78% blank); production's own 16 section headings (templates/intl.html.j2:923 Cross-country comparison, :985 Rates & curve desk, :1490 Central bank desk, :1646 What's driving the dollar?, :1714 Fragility map, :1813 Where crash pressure is flowing, :1880 Regime map, :1901 Cross-market correlation, :1928 Where each economy ranks, :1967 sector rotation, :1991 Prophet Stock Signals) appear in none of them, so no before/after judgement of density, hierarchy or compression rests on a valid baseline."
  - id: VTC-005
    severity: major
    finding: "The proposal's felt improvement is substantially deletion, not compression: of the 16 capabilities in baseline.yml:67-131, the frozen desktop image surfaces no affordance whatsoever for market.macro-comparison, rates.curve-desk, policy.central-banks, risk.pressure-flow, reference.regime-correlation-ranks, action.sector-rotation or action.prophet-stocks (the last of which also owns intl_stocks.html behind `mode == stocks`, baseline.yml:14), and reduces risk.credit-bonds to a single `CREDIT` column token; a four-section page that dropped nine engine surfaces has not proven it scans better than a sixteen-section page."
  - id: VTC-006
    severity: major
    finding: "Global Pulse breaks the one comparison the page exists to make and the one contract production states explicitly: production decomposes usd/local/fx with the legend `local equity + currency = USD return` (templates/intl.html.j2:858, help text :857, JS :2121/:2156), but the proposal grid never shows the FX term at all, and its secondary line is not even the same metric per cell — Japan/Eurozone/India/UK read `Local +8.1% · 1M`, `Local +3.3% · 1M`, `Local +3.5% · 1M`, `Local +1.4% · 1M` while Taiwan reads `Breadth 41% · off high −6.2%` and South Korea reads `20D · off high −15.2%`; so the two markets whose story the page says is currency (`Asia FX bears the pressure`, `KRW · 20D −3.1%`) are exactly the two with no currency decomposition, and −8.4% at 20D is ranked in-grid against +6.8% at 1M under a header control set to `1M`."
  - id: VTC-007
    severity: major
    finding: "The Global Pulse grid contradicts its own visibly-active control: `SORT BY [Rotation rank]` is the selected segment, yet reading order is Japan #1, Eurozone #2, India #3, then Taiwan #5, South Korea #7, United Kingdom #4 — the rank badges are present and out of sequence, which trains the eye to distrust every other ordering claim on the page, including the `Where capital is moving` table directly below that is correctly rank-ordered."
  - id: VTC-008
    severity: major
    finding: "Internal numeric contradiction inside one scroll: the hero SIGNAL STACK asserts `Global breadth 58% / 8 of 14 markets above trend`, while the Transmission chain cell `04 Breadth narrows` asserts `41%` with the sublabel `leaders carry index` — 41% is Taiwan's single-market breadth, reused verbatim from the Taiwan card and the Risk Radar `masking breadth at 41%`; a page whose identity is engine-owned receipts cannot print two different values for the same named quantity two sections apart."
  - id: VTC-009
    severity: major
    finding: "Two accepted-but-distinct production vocabularies are fused into one visually identical badge run, against the lineage requirement to reuse accepted states and stance vocabulary (baseline.yml:52-54): production keeps RRG quadrants Leading/Improving/Weakening/Lagging as a separate axis (templates/intl.html.j2:884-885, :904) from engine market state (build_intl.py:268/738, engine.intl_market_state), but Turn candidates renders `LAGGING → IMPROVING`, `LEADING → WEAKENING`, `PARABOLIC → BREAKING` and `BASING → IMPROVING` in one column with one pill treatment, implying a single ordered scale on which PARABOLIC, LEADING and BASING are comparable — which they are not."
  - id: VTC-010
    severity: major
    finding: "Unearned precision proliferates faster than the page explains it: one screen carries `81% CONFIDENCE`, `Risk budget 63 / 100`, `Global breadth 58%`, per-candidate `69% / 78% / 91% / 64% confidence`, `91% CONF`, `DATA HEALTH 97%`, and a `SENSITIVITY TO ANOTHER +1σ DOLLAR MOVE` strip reading `Korea 88 · Taiwan 72 · Japan 29` with no unit, no scale and no legend anywhere in frame — seven 0-to-100-shaped quantities with no shared meaning, which is the opposite of the restraint the same page earns with its own `They are not fused into one opaque score` footnote in the risk map."
  - id: VTC-011
    severity: major
    finding: "The frozen reference sells a product identity the route is declared not to have: `Saved 6`, `Alert settings`, `Pin market`, `Copy desk brief`, `Generate desk brief`, `Export desk view`, `Change lens`, `LIVE`, `LIVE DESK` and `UPDATED 16:28 ET` appear across the hero, inspector and footer, but templates/intl.html.j2 contains none of these affordances (the only matches for those tokens are `top_exporters`/`exporting risk` at :1841-1856), production is a static bake (scripts/build_intl.py), and baseline.yml:26-27 records the access gap as `anonymous; no credentials or premium payload are introduced` — so a meaningful share of the proposal's perceived sophistication is borrowed from an account/live plane excluded by its own baseline."
  - id: VTC-012
    severity: minor
    finding: "Vocabulary redundancy the eye trips on in-grid: India is badged `STEADY` and United Kingdom `NEUTRAL` in adjacent cells of the same six-card grid with identical neutral pill treatment, two words for one middle condition; likewise the International risk map prints LOW / WATCH / HIGH throughout but introduces a fourth token `MID` exactly once (South Korea · POLICY), so the reader must infer a four-level scale from a single instance."
  - id: VTC-013
    severity: minor
    finding: "`Global breadth 58%` is labelled `8 of 14 markets above trend`, and 8/14 = 57.1%, which rounds to 57 not 58; trivial in isolation, but it is printed inside the SIGNAL STACK card whose whole rhetorical job is to show the receipt behind the number, and the footer independently confirms the denominator as `14 equity markets`."
  - id: VTC-014
    severity: minor
    finding: "Right-rail composition under-fills at the page's closing move: in proposal-country-inspector-dark-en.png the International playbook column runs WATCH NEXT to ~y560 of an ~855px card and then holds ~230px of empty surface before the `Generate desk brief` / `Alert settings` buttons, so the panel that is supposed to be the page's conclusion reads as the emptiest block in the layout; Turn candidates carries a smaller version of the same void below the Brazil row."
strengths:
  - "The falsifier line is the best idea in the artifact and it is structural, not decorative: `The call flips to mixed if breadth falls below 44% while the dollar accelerates` in the hero recurs as `CONFIRMS IF −18% off high / INVALIDATES IF Reclaims MA20` in the Risk Radar and as `CONFIRMS THE BREAK` / `INVALIDATES THE CALL — Reclaims MA20 with breadth > 50%` in the inspector, which converts a verdict into a testable condition and gives the route a genuine desk identity production does not have."
  - "The hero's three-column split maps cleanly onto the three clauses of the stated user job — THE GLOBAL CALL (regime), SIGNAL STACK (where evidence is changing), CURRENT POSTURE (bounded posture) — and answers the primary question above the fold, where production buries the verdict beneath a 61/100 dial and a paragraph of reading instructions (`Read the dial with care: …`)."
  - "`Where capital is moving` with explicit 20D AGO → NOW columns, a signed delta and a colored momentum path is a materially better rank-change visualization than production's undifferentiated slider rows, and its footer sentence `Leadership is migrating, not broadening` states the conclusion the table is actually evidence for."
  - "The Tier 1 · ORIGIN STRESS / Tier 2 · US TRANSMISSION pairing preserves the masterplan's origin-versus-US-transmission separation (baseline.yml:50-51) rather than collapsing it, and the risk-map footnote `Fast market stress tells us whether pressure is igniting; slow fundamentals tell us who is fragile. They are not fused into one opaque score` is exactly the right restraint, stated in plain words."
  - "Section rhythm is disciplined and repeatable — accent bar, title, one-line purpose subtitle, right-aligned scope control — applied identically across Global Pulse, Rotation & turns, Transmission & fragility and Country inspector, which is a real scanning gain over production's flat `sec-h` stack."
  - "Consistent mono treatment for every numeric and micro-label gives the dark composition a coherent instrument-panel voice, and the semantic accent set (green constructive / amber turning / red breaking) is applied consistently across cards, sparklines, rotation bars and the legend rather than decoratively."
