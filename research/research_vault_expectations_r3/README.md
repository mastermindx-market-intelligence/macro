# Comparable research changes — offline research reference

This directory is research evidence under draft PR #7182, not product implementation. The overall institutional-intelligence product remains `SPEC_ONLY`. Do not import the reference into a running service or use its fixture flags as permission/source authority.

## Current result and reproduction

Read `CHUNK5_EXECUTABLE_ANALYST_CONTRACT.md` for the source-attributed versus reconstructed-change decision, explicitly typed bank forecasts, target semantics, generated answer preview, actual evidence and remaining source/consumer gate. Earlier `CHUNK3_REPORT.md` and `CHUNK4_SOURCE_QUALIFICATION.md` retain their dated findings and limitations.

Tested locally with Python 3.13.5 and the standard library:

```sh
cd research/research_vault_expectations_r3
python run_analyst_assay.py --out /tmp/research-vault-r5-assay
```

Use a fresh output directory. This runs the 53 issuer/reference tests and 58 analyst/consumer challenges, and writes a receipt, test log, five-case JSON result packet and deterministic Markdown preview to that directory. It performs no network, LLM, credential, database, browser, workflow, notification, trade or production action. `verification_r5.json` records the retained run: **111 tests, no failures/errors/skips**, at 2026-09-16T05:26:54.008915+00:00. Future runs have different timestamps and potentially Python versions.

`BANK_CASE_ANSWER_PREVIEW.md` is an actual generated offline preview, not the Brain's production answer. It explicitly discloses manually normalized inputs and unproven original PDF/body identities, vault matching and commercial permissions. The case-specific renderer is not a general financial-answer formatter.

`mutation_checks_r5.json` records six targeted local code mutations rejected by selected assertions. This is not an independent external review or six more passing product tests; its portable one-off recipe is included in the companion conversation package.

The legacy command remains available for only the original 53-test subset:

```sh
python run_assay.py --output-dir /tmp/research-vault-r3-subset
```

`verification.json` and `verification_r4.json` are historical receipts for their named earlier code revisions; do not claim their hashes describe the later comparator. The current R5 receipt carries the exact comparator, fixtures, runner and test hashes.

## Source cases and proof boundaries

`public_control_case.json` contains six manually normalized observations from five official Alphabet investor-relations sources. These are issuer guidance/actuals, not broker forecasts or parser results.

`analyst_control_case.json` contains five manual ING forecast observations. It preserves analyst forecasts versus issuer guidance, fixed-year targets versus relative tenors, quoted old values versus independently captured earlier statements, repeated change versus a newly captured assertion, and exact unknown source/availability fields. The August observation is carried from R4; June and September source material was revisited in R5. No immutable source-byte capture is claimed.

The reference permits source-reported change calculations without inventing a previous original. It does not infer complete history, immediate predecessors, fixing conventions or calendar dates from rolling tenor labels. A supported comparison is not a calibrated forecast or action recommendation.

The earlier paired NVIDIA-related vault pages establish repeated rendered content under different labels and IDs, not original-PDF equality or an executed live erroneous cluster. Never globally alias S&T to Goldman or delete by title. The earlier catalog arithmetic inconsistency remains unresolved at original-source level; no automatic numeric repair is authorized.

The 111 reference tests do not execute the earlier 120-task model benchmark or 26-case production conformance plan. Synthetic hashes only exercise locator structure. `allowed_for_assay` and `review_state` cannot authenticate users, prove licenses or certify provenance. Analyst operational point-in-time admission remains explicitly unsupported by this arithmetic extension.

## Exact next step

Qualify one existing permitted current original report at a recorded publication generation, preserve its real catalog/source/body identity and locator, and prove a source-attributed or source-reported-change answer through the existing Brain/viewer. Two separately admitted originals remain necessary for an independently reconstructed change, not for every accurately attributed single-source answer. This refines only the proposed sequence, never access, source, licensing or production gates.

#7079 owns the incumbent evidence release; #7045 owns source-type/rating hygiene. Do not modify either unilaterally. Require actual allowed/denied/no-match/quota, source-version/correction and viewer proof before customer-facing completion. Prophet explanation must leave signal artifacts unchanged. Do not redo collector recovery or create another corpus, graph authority, chatbot, quota system or acquisition process.
