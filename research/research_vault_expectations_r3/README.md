# Comparable research changes — chunk 3 reference assay

This directory is research evidence under draft PR #7182, not product implementation. The overall institutional-intelligence product remains `SPEC_ONLY`. Do not import the reference into a running service or use its fixture flags as permission/source authority.

## Read and reproduce

Read `CHUNK3_REPORT.md` for the real source-origin ambiguity, manually normalized Alphabet guidance control, comparison contract, acquisition implication and remaining original-source gate.

With Python 3.10 or later and the standard library:

```sh
cd research/research_vault_expectations_r3
python run_assay.py --output-dir /tmp/research-vault-r3-assay
```

This runs `test_revision_oracle.py` and writes a receipt and test log only beneath the explicitly selected output directory. It performs no network, LLM, credential, database, browser, workflow, trade or production action. `verification.json` is the retained observed local run: 53 tests, no failures/errors/skips. A fresh run can have a different timestamp and Python version.

`public_control_case.json` contains numerical facts manually normalized from five official Alphabet investor-relations sources. It is not a broker forecast series or a parser benchmark. Document labels are local fixture identifiers. Original PDF/body hashes and historical system availability remain unknown; the historical-availability probe deliberately rejects the six real control observations.

The paired NVIDIA-related vault pages establish repeated rendered content under different labels and IDs, not original-PDF byte equality or an executed live erroneous cluster. Never globally alias S&T to Goldman, delete a source by title, or count different labels as independent evidence.

## Boundaries and next action

The 53 reference tests do not execute the earlier 120-task model benchmark or 26-case production conformance plan. Synthetic hashes only exercise locator structure. The fixture fields `allowed_for_assay` and `review_state` cannot authenticate users or prove source rights. The reference does not verify trusted source custody and is intentionally not a product admission path.

Next qualify one existing permitted original-source pair at a recorded publication generation, preserve actual source/body hashes and locators, and integrate through the existing #7079 Brain/Vault path with the #7045 hygiene owner reconciled. Then prove the real allowed/denied/no-match/quota and source-opening journey before any customer-facing completion claim. Do not redo the completed collector recovery or start another corpus, graph authority, chatbot, quota system or acquisition process.
