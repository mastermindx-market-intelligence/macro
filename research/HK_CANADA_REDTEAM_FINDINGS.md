======================================================================
CRITIC: quant
======================================================================
VERDICT: The plan does NOT survive attack as a validation program, though its honesty/UX/safety spine (W0) is sound and shippable. Three load-bearing data claims that the phase-0 batteries depend on are false or contradicted by in-tree evidence: (1) the CN A-share deep store starts 2021-06-15, not 2006 — so H3's "monthly 2006→ per-pair A/H" panel cannot be built as specified; (2) the "already accruing / matures mid-July" forward ledgers span only 5-6 calendar days and grade the name-potential score, NOT any §3/§4 edge leg or the standout board — so the promised "live forward twin for every phase-0" does not exist and cannot mature on the W-schedule; (3) H4 (within-sector reversal) is not "never tested in-house" — the very report the plan cites (reports/hk-residual-alpha-phase0.md) already ran rev_st: IC 0.019, t_HAC 1.52, q_FDR 0.32 full / t 0.49 modern, and best-signal quintile-L/S DSR of 0.07-0.18 against a DSR≥0.90 bar. Given HK's beta-dominated, large-cap-skewed 157-name panel, H4 is a near-certain NO-GO the plan mislabels as a 50/50 open question. On power, at least 4 of the ~16 batteries (H3, H5, C6, C1-name-tier) cannot reach DSR≥0.90 on independent-episode counts and will resolve ACCRUE at best — which the plan's own kill-criterion (§9: "if batteries produce zero GO legs, reframe as honest screens") should force it to confront up front, not after 4 waves of collector-building. The true trial count is materially higher than the stated ~16 once sub-trials, the §5.3 gate re-parameterization, and the ported log-and-grade legs are counted. Before shipping: fix the C7 keystone (harness doesn't exist; define the UNDEFINED-verdict fallback), correct the H3 data premise, re-scope H4 as confirm-a-kill not discover-an-edge, and honestly re-budget which batteries are decision-grade vs accrue-only.
--- FATAL (3) ---
[§3.1 H3 (A/H discount) + §1.1 data reality + W1/W2] H3's phase-0 spec ('monthly, 2006→ where pairs exist', 'extend per-pair premium panels using hk_stocks H-closes (2000→) × A-closes from the CN deep OHLC store, shipped') is not buildable. The CN deep A-share close store (data/china_search/closes.parquet) starts 2021-06-15 — a 5-year window, not back to 2006. The only A/H artifact in-tree (data/hk_ah_official/ah_premium.parquet) is the aggregate HSAHP INDEX (single column, 2021-06-15→), not a per-pair panel. So the A-leg needed to build per-pair discounts before 2021 does not exist in-tree and the plan names no free source to backfill A-share closes to 2006.
   EV: data/china_search/closes.parquet shape (1225,1514) date min 2021-06-15; data/hk_ah_official/ah_premium.parquet is single col 'hsahp' 2021-06-15→2026-07-03; no data/hk_ah/ dir exist
   FIX: Either (a) re-spec H3 as a 2021→ monthly test (~55 monthly cross-sections, ~5 independent yearly episodes — state it CANNOT reach DSR≥0.90 and is ACCRUE-only), or (b) add an explicit free A-share deep-history collector (e.g. akshare A-close backfill to 2006) to W1 with its own freshness/depth report BEFORE claiming a 2006→ panel. Do not carry '2006→' in the spec until the A-leg exists.
[§4.1 C7 (momentum keystone) + §4.1 C7 verdict logic] C7 is called 'the single highest-leverage honesty fix' and 'a verbatim reuse' of a harness — but scripts/canada_residual_alpha_phase0.py does NOT exist (only the HK version does). It is a build, not a reuse, and the plan under-scopes it. Worse, the verdict logic is a binary (Outcome A GO → keep α; Outcome B NO-GO → swap to C1/C2/C3), but on a 5y/219-name CA panel BOTH total-return and residual momentum can fail DSR≥0.90 (exactly what happened to HK: mom_tot DSR 0.10-0.43, all FAIL). The plan gives no rule for the most likely outcome — BOTH legs NO-GO and the C-stack not yet validated (C1/C2/C3/C5 all still pending in W2/W3) — which leaves the live CA board rank UNDEFINED at W4.
   EV: scripts/canada_residual_alpha_phase0.py absent (only scripts/hk_residual_alpha_phase0.py present). HK analog reports/hk-residual-alpha-phase0.md shows mom_tot net-Sharpe DSR 0.10 (
   FIX: (1) Add 'build canada_residual_alpha_phase0.py' as an explicit W2 task, not a reuse. (2) Define the UNDEFINED-verdict fallback explicitly: if both momentum legs NO-GO and no C-leg has yet GO'd, the board must fall back to a NON-ranked, tier='screen' exposure list (alphabetical or liquidity-ordered) with the composite chip suppressed — i.e. Path A — until a C-leg graduates. State this in §4.1 and §8 W4 so the board is never silently left ranking on the retired prior.
[§1.3, §5.4, §7.5 (forward ledgers as graduation machinery)] The whole graduation machine rests on 'HK ledger 365 calls / CA 1,170 calls already accruing; 21d read ~mid-July-2026' and 'every future phase-0 has a live forward twin.' In-tree, data/name_score/hk_calls.parquet has 6 unique dates (2026-06-28→07-03) and ca_calls.parquet 5 unique dates — one week old. The '365/1,170' are name-DAYS, not independent forward outcomes; the 21d read has ~0 matured cohorts, not one maturing mid-July. And these ledgers grade the name-POTENTIAL score (cols: score/tier/fuel/trigger/level), NOT the standout board rank NOR any §3/§4 edge-leg z — exactly HKCA-12. So the promised 'live forward twin for every phase-0' does not exist yet and §5.4's 'add fields (edge-leg z's, ignition state)' must run for months before any edge has a forward record.
   EV: data/name_score/hk_calls.parquet shape (438,7) unique dates=6, range 2026-06-28..07-03; ca_calls.parquet (1170,7) unique dates=5. Columns are name-potential fields only. Audit HKCA
   FIX: Restate the ledger status honestly: name-potential ledger is ~1 week old, NOT maturing mid-July; standout/edge-leg ledgers do not exist yet. Move '§5.4 add edge-leg z fields to the ledger' to W0/W1 so the forward twin STARTS accruing before the batteries run, and push all 'graduate on forward agreement' milestones out by the true 21d-from-first-write date (earliest ~3 weeks after W1 lands, i.e. ~late-Aug), not mid-July. Otherwise W7 'calibrate from matured ledgers' has no matured data.
--- MAJOR (5) ---
[§3.1 H4 (within-sector reversal) — 'never tested in-house', prior '50/50'] H4 is framed as an untested mechanism with a 50/50 prior, but the plan's own cited report already tested short-term reversal on the HK panel. rev_st: IC 0.0187, t_HAC 1.518, q_FDR 0.3225 (full), and t_HAC 0.493 (modern) — does NOT survive FDR; and the best signal's quintile L/S DSR is 0.07-0.18 vs the DSR≥0.90 bar. HK's cross-section is explicitly 'beta-dominated' and large-cap-skewed, while the literature reversal effect the plan cites lives in 'small/illiquid' names the panel admits it 'may not hold.' So H4 is a near-certain NO-GO being mislabeled as an open discovery. Re-running it as specified (monthly, 2000→, whole panel) reproduces the already-failing rev_st with a slightly different neutralization and burns a trial + a wave slot.
   EV: reports/hk-residual-alpha-phase0.md: rev_st IC 0.0187 t_HAC 1.518 q_FDR 0.3225 full / t_HAC 0.493 modern; no reversal signal in FDR survivors (only acc_res). closes_deep 157 names,
   FIX: Re-scope H4: state up-front it is a CONFIRM-a-kill on the aggregate panel and can only plausibly GO in the low-ADV tercile (the one honest sub-test). Pre-register the low-ADV tercile as the primary hypothesis, whole-panel as the (expected-fail) control, and set the honest prior to 'likely NO-GO on our large-cap panel — a scope finding, budget accordingly.' Do not spend a full wave treating it as 50/50.
[§3.1 H1 phase-0 — Connect-eligibility universe + survivorship] H1's universe is 'Connect-eligible ∩ our 157-name panel' over 2017→2026, but Connect eligibility (Stock Connect southbound list) CHANGES over time and membership is only known ex-post — using today's eligible list back to 2017 is a look-ahead/PIT violation (names added to Connect later, or removed, bias the panel toward survivors that stayed eligible). Compounded with the panel being current-constituents only. The plan stamps survivorship generically but names no point-in-time Connect-eligibility source; the akshare/Eastmoney backfill gives holdings, not the historical eligibility roster.
   EV: Plan §3.1 H1: 'universe = Connect-eligible ∩ our 157-name panel (~120+ names), 2017→2026.' No PIT eligibility roster in the data inventory (§1.1); southbound holdings.parquet has 9
   FIX: Source or reconstruct point-in-time Connect southbound eligibility (HKEX publishes historical inclusion/exclusion effective dates) and gate each week's cross-section on the roster AS-OF that week. If unavailable, restrict H1 to names continuously eligible over the whole window and explicitly bound the survivorship bias (report effect on the continuously-eligible subset as a lower bound, not the full panel as an upper bound).
[§4.1 C1 sector-tier power — '~40-60 regime flips per commodity'] C1's sector-tier phase-0 leans on '~40-60 regime flips per commodity' as the event count, but slope_z sign-flips over 25y are heavily autocorrelated and clustered — a genuine commodity regime-switching model yields ~6-12 independent bull/bear cycles per commodity over 25y, not 40-60 independent episodes. The event-study t-stat and DSR computed on 40-60 overlapping post-flip windows will be badly overstated unless block-bootstrap effective-T collapses them to the true episode count (which the plan's own validation.py supports via t_eff, but the spec quotes the inflated raw count as if it were power).
   EV: Plan §4.1 C1(a): 'forward 2-8w sector-ETF excess... ~40-60 regime flips per commodity.' engine/validation.py deflated_sharpe uses t_eff (block-bootstrap effective sample) precisely
   FIX: Pre-register the regime-episode definition (min-duration + hysteresis) so flips are de-clustered into independent cycles, report effective-N as the episode count (~6-12/commodity, ~20-36 pooled across CL/GC/HG), and run DSR with t_eff from bootstrap_effective_t. State honestly that even the sector tier is borderline-powered and may resolve ACCRUE.
[§9 Multiplicity + §2 'trial-budget discipline' — the '~16 trials' claim] The stated '~16 pre-registered trials across 2 markets, BH-FDR within each battery' undercounts the real multiple-testing surface. Counting the sub-trials the specs themselves enumerate: HK families H1{Δ1w,Δ4w,Δ-accel}=3, H2{level,Δ4w,list-add}=3, H3{level,1yΔ}=2, H4{whole,low-ADV split}≈2, H5=2 → ~12 HK sub-trials; CA C1{flip,catch-up,beta-persistence}=3, C3{1w,4w}×sector-pairs, C5{Δ,level}=2, C6=1, C7{total,residual}×{names,sectorETFs}=4, C4=1, C2=1 → ~14+ CA. Plus §5.3 gate re-parameterization (per-market × 4 tiers), §5.1 ignition (4 components), and §5.3 ported washout_2w/extension 'log-and-grade' — each a selection surface. BH-FDR *within* family does not control the *across-family* selection when the program cherry-picks which markets/legs to promote. Real family-wise trial count is ~30+, and the cross-family selection is uncontrolled.
   EV: Plan §3.1/§4.1 sub-trial lists as above; §9 states '~16'; §6 constitution controls FDR only 'across the family,' not across batteries. §5.3 adds hand-free gate re-parameterization;
   FIX: Publish the full trial ledger (all sub-trials + ported log-and-grade legs + gate re-param as trials) and apply a program-level DSR n_trials (via the ledger= path in deflated_sharpe, which the ratchet already enforces) counting EVERY config tried across both markets, not just within-family. State the corrected count (~30+) in §9.
[§3.1 H1/H2 & §4.1 all — sector-neutralization PIT] Multiple legs specify 'sector-neutralized' / 'within-sector' ranking (H1 secondary, H2, H4, C1 name-tier, C5), but §1.1 states HK has 'no HSCI sector indices' and the sector maps used are today's classification. Neutralizing 2017-2026 cross-sections with a 2026 sector map is a mild look-ahead (reclassifications, sector migrations) and, for HK, there is no validated sector taxonomy in-tree at all — the '13 breadth sectors' are the current curated map. This silently leaks and is inconsistent across markets.
   EV: Plan §1.1: 'no HSCI sector indices' (HK). §3.1 H1 'sector-neutralized secondary', H2 'within-sector', H4 'within-sector'. Sector membership sourced from current curated baskets (au
   FIX: Pin a point-in-time sector map (GICS/ICB effective-dated) for neutralization, or, where none exists (HK), run the primary test WITHOUT sector-neutralization and treat the sector-neutral variant as a secondary robustness check counted as its own trial. Document the taxonomy source and its PIT status per leg.
--- MINOR (4) ---
[§1.1 'closes_deep 40y index is cosmetic — per-name data starts 2000'] This self-characterization is itself inaccurate and under-sells available depth: closes_deep.parquet actually carries real per-name history for 101 of 157 names before 2010 and 67 names before 2005 (back to 1986). The 'cosmetic' framing may cause the team to needlessly interim-cap H4/H3 at 3y when a genuine ~67-name pre-2005 panel exists for a deeper (if survivorship-heavy) reversal test.
   EV: closes_deep.parquet 9815 rows 1986→; cols with first-valid <2010 = 101/157; <2005 = 67/157.
   FIX: Correct §1.1 to 'closes_deep has real per-name depth (67 names pre-2005, 101 pre-2010) but is survivorship-selected and stale (2026-06-18); usable for a deeper reversal control with the bias stamped.' Use it for H4's deep control instead of a 3y interim.
[§4.1 C2 (SEDI) phase-0 'thin, now: 2025→ cross-sections'] C2's 'directional read only' on 2025→ data is below any decision threshold — with ~1.5y and most names 2025+ only, cluster events (≥2 buyers/60d) will be a handful, effective-N in the low tens of events. Reporting a 'directional read' risks it being cited later as soft evidence to wire the leg. The append-only fix is correct but the come-back date (~3y accrual) means C2 cannot inform W4 wiring at all.
   EV: Plan §1.1: SEDI '~1.5y usable, 201/233 names 2025+ only' (yfinance 150-row cap). §4.1 C2 phase-0 '2025→, directional read only.' data/canada_insider/insider.parquet present but cap
   FIX: Explicitly mark C2 as ACCRUE-only with no W4 wiring path; drop the 'thin phase-0 now' directional read or label it non-evidential (exploratory), so it cannot be laundered into a promotion.
[§4.1 C6 (tax-loss seasonal) — power] 25y of Decembers = ~25 December→January episodes, and they are one-per-year (fully independent but only ~25). A seasonal Dec-Jan rebound test at DSR≥0.90 on ~25 episodes is under-powered by construction; it will almost surely land ACCRUE/marginal. The plan lists it as a phase-0 with a sizing-tilt ship, risking a display tilt on an untested seasonal.
   EV: Plan §4.1 C6 '25y sector/size panels... 1 trial'; ships as 'seasonal window chip + sizing tilt in Dec-Jan.'
   FIX: State the ~25-episode ceiling and that C6 cannot reach the bar; ship the chip as display-only 'descriptive seasonal' with NO sizing tilt until many more Januaries accrue (this is a decades-long accrual, not a phase-0 GO candidate).
[§7.5 / §8 'HK mid-July' track-record panel milestone] Ties into the fatal ledger finding but as a schedule bug: the track-record panel 'reading real ledger data' at W6 and 'HK mid-July' assumes matured 21d cohorts by mid-July from a ledger whose first write is 2026-06-28. 21d forward from 2026-06-28 is ~2026-07-19 for the FIRST cohort only, with N=1 date — not a readable hit-rate. The '≥6 IC dates mature' gate in §7.5 won't be met until ~6 trading weeks after first write.
   EV: hk_calls.parquet first date 2026-06-28; §7.5 'hit rates by tier/band once ≥6 IC dates mature — HK mid-July.'
   FIX: Recompute the milestone: ≥6 matured 21d IC dates from a 2026-06-28 start lands ~late-Aug/Sep, not mid-July. Update §7.5 and §8 W6 accept criteria accordingly.
--- MISSING (5) ---
+ A per-battery explicit effective-N (independent-episode) table with the pass/accrue verdict PRE-STATED. The plan asserts 'honest effective-N' as a principle but never computes it for a single test, so it cannot tell which of the ~16 batteries are decision-grade (can reach DSR≥0.90) vs structurally accrue-only. On the numbers, H3 (~5 yrs), H5 (regime-split of 22y = few regimes), C6 (~25 Decembers), C1-name-tier (5y within flagged sectors), and C2 (1.5y) cannot reach the bar — the plan should say so up front rather than discovering it after 4 waves.
+ A concrete survivorship bias-BOUNDING method for H4 reversal, beyond the generic 'stamp it.' For reversal specifically the delisted losers are the exact names the effect would have shorted/avoided; their absence inflates the long-short. The plan needs a bound (e.g. worst-case impute delisted names at -100% at the reversal-buy point, or restrict to a survivorship-clean sub-window) — not just a stamp that says 'upper bound.'
+ A defined fallback for the UNDEFINED board-rank state when C7 returns NO-GO/NO-GO and no C-leg has GO'd (the most probable outcome given the HK precedent). §8 W4 rebases 'per C7 verdict + surviving C-legs' with no branch for 'nothing survives.'
+ A PIT source for Connect southbound eligibility (H1) and for historical A/H pair eligibility (H3 pair-map expansion 12→40+ is done with today's akshare A/H list = hindsight pair selection; the same look-ahead that the plan itself flags for baskets in HKCA-2).
+ The parallel-program collision test is asserted ('cross-market test suite must stay green') but no named test target or the specific shared symbols at risk (stock_score._WEIGHT_PRIOR, signal_gate thresholds, engine/baskets_region.py seed_date) — §5.2 changes baskets_region which is shared with intl baskets (audit App B), so an HK/CA basket-OHLCV persistence change can silently move intl basket outputs. No regression assertion covers intl.
======================================================================
CRITIC: hk
======================================================================
VERDICT: The plan does NOT survive attack on its HK pillar. The single load-bearing HK leg — H1 southbound holding-Δ, explicitly named the "flagship build" and the top of the edge stack — rests on a data claim I falsified by direct fetch: the plan asserts per-name southbound holdings backfill "2017→ (~480 weekly cross-sections)" via akshare/Eastmoney, but the collector's own upstream (Eastmoney RPT_MUTUAL_STOCK_HOLDRANKS) is a strict 2-year rolling window (earliest available date = 2024-07-03; 2017/2019/2023 all return 0 rows), and every akshare stock_hsgt_* holdings endpoint is NORTHBOUND (mainland holding A-shares), not southbound. The real ceiling is ~104 weekly cross-sections and only if scraped today — the persisted store has 9 days. That is a 4.6x overstatement of the phase-0 n on the leg the whole HK thesis leans on, and it silently converts H1 from "backfill + test now" to "accrue-forward, verdict ~2027." The A/H leg (H3) has the same disease: its "monthly, 2006→" panel is impossible on the cited CN store (china_search/closes.parquet is 2021→, HSAHP official is 2021→). Beyond data, the plan has a market-microstructure blind spot the prompt flagged: a nightly-rendered, read-next-morning dashboard cannot capture a weekly-horizon demand-pressure effect whose edge is largely gone by the next open — the plan never addresses execution-horizon decay for H1/H2. Before shipping: re-scope H1 and H3 to their true ~2y depth (or demote both to accrue-forward and say so on the page), state the pre-2018 SOFR proxy for H5, and honestly price H4 as probably-DOA on a 157-mega-cap panel. The honesty-spine and UX/W0 waves are sound and worth shipping regardless — but the "flow-structure edge stack" that justifies Path B is, on today's data, one thin leg (H4, 50/50 by the plan's own prior) plus three legs with broken or overstated backtest depth.
--- FATAL (1) ---
[§1.2 Free data table + §3.1 H1 + W1] H1 southbound backfill depth is overstated ~4.6x and the named backfill channel does not exist. The plan claims per-name southbound holdings '2017-03-17→' (free-data table, 'verified-by-fetch') and '~480 weekly cross-sections' 2017→2026 (§3.1 H1 phase-0), via 'akshare/Eastmoney (stock_hsgt_*; the current collector's own upstream)'. Both are false. (a) The collector's actual upstream, Eastmoney RPT_MUTUAL_STOCK_HOLDRANKS, is a STRICT 2-YEAR ROLLING WINDOW: I fetched it directly — earliest available HOLD_DATE = 2024-07-03; queries for 2017-06-01, 2019-06-03, 2023-07-03, 2022-01-04 all return 0 rows; 2024-07-03 and 2025-01-06 return data. (b) akshare stock_hsgt_hold_stock_em / individual_detail_em / stock_statistics_em are all NORTHBOUND (沪深港通持股 = mainland holding A-shares; market∈{北向,沪股通,深股通}; individual_detail default symbol '002008' is an A-share) — none expose southbound (港股通) per-name holdings history. stock_hsgt_institution_statistics_em has a '南向持股' option but returned None (errored) and is institution-aggregate, not per-name holdings. So the true H1 backtest ceiling is ~104 weekly cross-sections (2024-07→now), ONLY if scraped today; the persisted store has 9 daily dates.
   EV: Direct fetch: RPT_MUTUAL_STOCK_HOLDRANKS INTERVAL_TYPE=1 ascending-sort earliest row HOLD_DATE=2024-07-03; HOLD_DATE='2017-06-01 00:00:00'→0 rows, '2024-06-03'→0, '2024-07-03'→1. a
   FIX: Correct §1.2 and §3.1 to the real depth: southbound per-name holdings = ~2y rolling (2024-07→) via RPT_MUTUAL_STOCK_HOLDRANKS date-paging (datetime filter format 'YYYY-MM-DD 00:00:00'), NOT 2017. Either (a) re-spec H1 phase-0 as ~104 weekly cross-sections with survivorship + short-window caveats and DROP the 'literature 25.8%/yr weekly L/S' framing as unbacktestable at that n, or (b) demote H1 to accrue-forward from today (come-back ~2027-07 for a 3y read) and stop calling it the 'flagship build.' Remove the false akshare stock_hsgt_* backfill claim. Investigate HKEX CCASS 'shareholding search by date' as the ONLY route to pre-2024 southbound holdings (it is per-participant CCASS, requires reconstructing the mainland-Connect nominee accounts — materially harder than the plan implies; the plan's cited mutualmarket.aspx endpoint returned HTTP 500 for 2017/2018/2020 in my probe).
--- MAJOR (6) ---
[§3.1 H3 (A/H) + §4.1/W2] H3's phase-0 spec 'monthly, 2006→ where pairs exist' is impossible on the cited in-tree data. The plan says to extend the A/H premium panel using 'A-closes (CN deep OHLC store, shipped)' and test discount-percentile Q-spread 2006→. But the CN store it points to (data/china_search/closes.parquet, the wide A-close panel) starts 2021-06-15 (5y), and the official HSAHP index (data/hk_ah_official/ah_premium.parquet) is also 2021-06-15→. Deep A-history exists ONLY in per-name data/china_stocks/*.parquet (1997/2003/2008→), which the plan does not cite and which still needs matched H-side closes for each of 40 pairs — and the H-side matched history + the 12→40 pair expansion is the binding constraint, not the A-side.
   EV: data/china_search/closes.parquet idx 2021-06-15→2026-07-03; data/hk_ah_official/ah_premium.parquet (hsahp) 2021-06-15→; sampled data/china_stocks/*.parquet ranges 1997-2008→ (deep,
   FIX: Re-scope H3 to the true matched-pair depth. State per-pair start dates (A inception via china_stocks/* ∩ H inception via hk closes_deep) rather than a blanket '2006→'. Acknowledge the panel is unbalanced (few pairs pre-2015) and that the discount signal over 2021-2026 is heavily confounded by the southbound dividend-tax rumor cycles and the 2022-24 China-risk repricing — a size-controlled monthly test on ~40 pairs over 5y is ~60 usable monthly cross-sections, not a 20-year panel; treat H3 as ACCRUE/modest by its own honest prior.
[§3.1 H1/H2 mechanism vs delivery vehicle] Execution-horizon decay is never addressed. The cited literature effect (southbound holding-Δ, weekly L/S ≈25.8%/yr) is a WEEKLY-horizon demand-pressure signal. The product is a nightly-rendered dashboard the user reads next morning; southbound holdings disclose T+1 after HK close, so by the time a name surfaces the demand shock is already ≥1 session stale and, per the demand-pressure mechanism, largely impounded by the next open. The plan's own honest prior even says 'demand-shock decay means fast horizons' — but then still ranks names on it and ships a card chip, with no analysis of whether ANY tradable edge survives the disclosure+render+next-open lag.
   EV: §3.1 H1 honest prior: 'demand-shock decay means fast horizons.' engine/hk_southbound_stocks.py signal() ranks accum_z cross-sectionally with no forward-horizon / implementation-lag
   FIX: Add an implementation-shortfall gate to H1/H2 phase-0: test forward returns measured from the NEXT open after disclosure (not close-to-close from the signal date), across 1w/2w/4w holds, and report the edge net of the ~1-2 session lag the dashboard imposes. If the surviving 1w edge is inside costs+lag, H1 is a context/positioning chip only (which the card copy should say), never a ranker.
[§3.1 H2 (shorts)] The plan under-weights two HK short-data killers the prompt named and partly conflates flow vs position. (a) SFC reportable short positions are ≥0.02%-of-issued POSITIONS published WEEKLY with a T+7 (calendar) lag — the plan's phase-0 'weekly panel 2013→, short-% level and Δ vs forward 4w' never accounts for the T+7 staleness, so a 4w-forward test on a signal already 7 days old overstates tradable edge. (b) The plan lists both 'SFC weekly reportable shorts' (positions) and 'HKEX sstoday' (short-SELL TURNOVER = daily flow) in the same H2 leg without stating they are different quantities measuring different things; sstoday is not a position and cannot backfill the SFC position series.
   EV: §3.1 H2 data line lists 'SFC weekly reportable shorts (free, ~2012→)' AND 'HKEX sstoday daily accrual' as one leg; §1.2 correctly notes sstoday is 'today-only' turnover but H2 does
   FIX: Split H2 into two explicitly-typed sub-legs: (i) SFC reportable-short POSITION level/Δ, tested with the forward window starting T+7 after the position date (honest lag), skew-flagged as large/liquid-only (≥0.02% threshold ⇒ mega-cap bias on our 157-name panel); (ii) sstoday short-SELL-TURNOVER ratio, accrue-forward only. State they are non-substitutable.
[§3.1 H4 (reversal) scope] H4 is very likely DOA on our panel and the plan's '50/50' prior is too generous. The literature reversal effect concentrates in small/illiquid/dual-traded names; our HK OHLC panel is 157 MEGA-CAPS (Tencent, Alibaba, HSBC, ICBC, BOC…), and there is NO free small-cap HK panel in-tree (data/hk_full_breadth is a 5×4 aggregate, not a name panel). The plan's own 'liquidity-split (low-ADV tercile vs rest)' is toothless when the entire universe is the top ~30% of HSCI by liquidity — the low-ADV tercile of 157 mega-caps is still liquid.
   EV: data/hk_search/closes_deep.parquet = 157 names (0700/9988/HSBC/ICBC/…); data/hk_full_breadth/breadth.parquet = (5,4) aggregate only; no wide small-cap OHLC store found.
   FIX: State the honest probability H4 is a scope-null (high) up front, and either (a) source a small-cap panel via the deep HK OHLC collector across the full HSCI (~500 names) BEFORE running H4 — otherwise the test cannot see the literature's effect — or (b) reframe H4 as a large-cap contrarian probe with explicitly lowered expectations, and pre-commit that a null is a scope finding, not a mechanism kill (the plan says this but buries it).
[§3.1 H5 (peg-liquidity) + §1.2] HIBOR−SOFR cannot be computed 2002→ as the phase-0 requires: SOFR (FRED SOFR / FRBNY) only begins 2018-04. The plan's H5 phase-0 is a 'regime-split of HSI forward returns … 2002→' using 'SOFR/Fed funds from FRED,' but gives NO pre-2018 proxy, so ~16 of the ~24 years of the requested regime split have no SOFR leg.
   EV: engine/funding_stress.py:30 SERIES uses FRBNY SOFR (FNYR-SOFR-A) which starts 2018; no pre-2018 SOFR/proxy series is configured for HK.
   FIX: Specify the pre-2018 proxy explicitly (3M USD LIBOR or effective fed funds / OBFR as the USD leg before 2018-04, spliced to SOFR after), or restrict the H5 regime-split to 2018→ and drop the '2002→' claim. Note the splice introduces a regime discontinuity that must be handled in the regime-labeling.
[§1.3 / §5.4 ledger machinery ('live forward twin')] The plan repeatedly leans on name_score_grader HK/CA ledgers as the graduation engine ('HK ledger 365 calls / CA 1,170 calls already accruing; 21d read ~mid-July') and claims every future phase-0 gets 'a live forward twin.' But these ledgers (i) have only 5-6 unique dates (HK 2026-06-28→07-03; CA 2026-06-29→07-03), i.e. daily snapshots of the same names, NOT hundreds of independent forward-gradeable episodes; and (ii) contain NO outcome/forward-return column at all (cols: date,ticker,score,tier,fuel,trigger,level). They grade the name-POTENTIAL score, not the standout RANKING (audit HKCA-12 says exactly this). So the 'forward twin' the plan promises for H1-H4/C1-C7 does not yet exist as a gradeable structure.
   EV: data/name_score/hk_calls.parquet shape (438,7), 6 unique dates, no outcome col; ca_calls (1170,7) 5 unique dates, no outcome col. Audit HKCA-12: 'no forward-outcome log for standou
   FIX: Before W2/W3 rely on ledgers, add the actual forward-grading write path (next-bar fill + horizon returns) to name_score_grader for HK/CA, and correct the '365/1,170 calls' framing to 'N names × ~6 daily snapshots, grading not yet wired.' The '21d read ~mid-July' is only for the name-potential score's first maturation, not for any §3/§4 leg.
--- MINOR (3) ---
[§4.1 C1 commodity transmission] The '25y sector-ETF tier … ~40-60 regime flips per commodity' is uneven: XBM.TO (base-metals/copper, the plan's copper→XBM pair) starts only 2012-01-24 (~14y), while XEG/XGD/XFN reach 2001. So the copper transmission test has ~40% less history than stated and materially fewer regime flips.
   EV: data/canada/XBM.TO.parquet range 2012-01-24→2026-06-30 (3623 rows) vs XEG.TO/XGD.TO/XFN.TO 2001-03→ (~6340 rows).
   FIX: State per-pair ETF inception in the C1 pre-reg; use GSPTSE-relative sub-sector or a copper-miner basket if a longer copper-equity proxy is needed, or cap the copper leg's claimed n to its true ~14y.
[§1.2 CIRO 2012 claim] C5 CIRO consolidated-short 2012→ is asserted 'likely (verified format; archive depth to confirm)' but the plan's W1 accept-criteria treat it as landed depth. No CIRO archive fetch was demonstrated; the 14y×1500-securities depth is unverified and the CIRO CSV archive is known to have moved/rebranded from IIROC, risking dead historical URLs.
   EV: §1.2 confidence column says 'likely … archive depth to confirm'; no CIRO store present in data/ (grep found none).
   FIX: Add an explicit W1 spike to fetch one historical CIRO CSV (e.g. 2013) and confirm archive depth BEFORE committing C5's '2012→ ~330 semi-monthly cross-sections' phase-0 n; fall back to accrue-forward if archive is shallow.
[§5.1 sector-ignition (HK)] HK sector ignition leans on 'southbound sector-agg Δ once H1 panel lands' — but per the fatal finding H1 lands with only ~2y (or 9 days) of depth, so the HK ignition flow-leg inherits the same thin/rolling-window limitation and cannot be graded on any meaningful history at launch.
   EV: §5.1 flow leg 'HK: southbound sector-agg Δ once H1 panel lands'; H1 depth ≤2y per fatal finding.
   FIX: Ship HK ignition without the southbound flow-leg initially (breadth-thrust + RS-slope only), add the flow-leg as accrue-forward once ≥1y of southbound history exists.
--- MISSING (5) ---
+ No HK-native placement/rights-issue dilution mechanism in the edge stack. HK's most distinctive and highest-frequency stock-specific run-KILLER is the discounted top-up placement / rights issue (HK issuers place new shares at a discount with far lighter disclosure than the US); a name 'about to run' that prints a placement gaps down. H6 mentions 'placement dilution flags' as a display chip only, deferred with 'thin n' — but placements are frequent enough across 157 large-caps to backtest a post-placement drift/avoidance signal, and as a DEMOTE gate (analogous to CA's C8) it needs no validation to add value. This is arguably a stronger, more testable HK-native edge than H4 reversal and should be promoted from a display chip to a first-class risk-gate leg.
+ Stock-Connect INCLUSION/exclusion events are absent as a mechanism. Southbound-eligibility additions (semi-annual Connect list reviews) are a documented one-off demand event (the mainland crowd can now buy the name) — a clean, dateable event study with real n over 2016→, and it is the causal front-run of the very H1 flow the plan flagships. The plan lists 'designated-list additions' for shorting (H2) but omits the far more impactful Connect-inclusion event on the buy side.
+ Mainland policy-catalyst windows / China-ADR homecoming flows are not modeled as HK-specific conditioners. HK-listed China names run on Beijing policy pulses (PBoC/Politburo/regulatory relief) and on ADR-homecoming secondary-listing flows — the dominant idiosyncratic HK run-driver for the mega-cap tech names that make up much of the 157-panel. The stack's only conditioners are peg-liquidity (H5) and global-beta; a mainland-policy-window overlay would condition H1/H4 efficacy more than HIBOR−SOFR does.
+ No statement of the SFC reportable-short universe coverage against our 157 panel. Because the ≥0.02%-of-issued threshold and mega-cap skew mean the SFC series covers mostly the largest names, the plan should quantify how many of the 157 actually appear in the weekly SFC file before spec'ing H2's cross-sectional n.
+ No handling of trading suspensions / long halts in the HK forward-return methodology. HK names suspend for weeks (deals, going-private, regulatory) far more than US; the H1/H3/H4 forward-return computation needs an explicit suspension/next-valid-print rule or the backtests silently forward-fill through halts and fabricate returns. The plan's hygiene layer lists a 'suspension check' for display but not for the backtest fill logic.
======================================================================
CRITIC: ca
======================================================================
VERDICT: The plan survives as a program skeleton but its Canada pillar (section 4) is over-sold on two of its three backtestable-today flagships and mis-scoped on two seasonals. My own 25y in-tree event studies partly VINDICATE the C1 sector tier (XEG +1.4-2.2pct / XGD +2.7-3.4pct excess vs GSPTSE after debounced commodity regime-turns, t about 2.0-2.8, n=43/50), so C1 is not dead. But the plan's DIFFERENTIATING bets — the C1 NAME tier catch-up gap and C3 (US leads TSX at t+1..t+5) — are both refuted by the data: miners do not lag their metal (GDX-to-XGD 96pct contemporaneous, t+1 residual -0.06; gold-to-XGD t+1 -0.10), and after stripping contemporaneous beta the US-to-CA t+1 lead is +0.11/-0.06/+0.11 across energy/gold/financials, near-zero and sign-inconsistent, a trial-slot sink. C5 (CIRO) and C2 (insider) rest on a false premise: canada_fundamentals shortRatio is a single live snapshot (asof last 5 days, one row per name), not a backtestable panel, and insider history is 99.6pct post-2025-01-01, so neither can validate before about 2028. C6/C8 target small-cap/venture mechanisms on a board with ZERO TSXV names. MUST change before shipping: (1) demote C1 to sector-tier-only or give catch-up gap a falsifiable spec; (2) cut/defer C3; (3) correct the shortRatio-history claim and re-time C2/C5 honestly; (4) shrink/cut C6/C8; (5) reconcile the ~16-trials count, which my count exceeds; (6) add the missing CA-native bank-earnings-clustering plus BoC-rate-window mechanism.
--- FATAL (0) ---
--- MAJOR (6) ---
[4.1 C1 name tier / catch-up gap] The differentiating name-tier bet is that high-beta resource names LAG the commodity and catch up. My 25y weekly lead-lag refutes the lag premise: gold miners are 96pct contemporaneous with gold and the t+1 residual is NEGATIVE (GDX-XGD resid t+1 -0.06; gold-XGD raw t+1 -0.10; XGD-leads-gold t-1 +0.077 exceeds gold-leads-XGD t+1). Miners anticipate, not lag. Oil-XEG t+1 only +0.026, negative by t+2. The catch-up gap is not sign-confirmed and its honest prior understates that the empirical sign is against it.
   EV: data/yahoo/GC_F,CL_F.parquet vs data/canada/XGD.TO,XEG.TO.parquet: contemp gold/XGD 0.755, t+1 -0.096; oil/XEG contemp 0.640, t+1 +0.026; beta-stripped GDX-XGD resid[t+1] -0.057.
   FIX: Ship C1 sector-tier-only (data supports t about 2). Drop the name-tier leg or pre-register an exact falsifiable catch-up-gap spec (residual regression, horizon, expected SIGN, held-out post-2013 half) and warn the naive sign is currently against it.
[4.1 C3 US-lead interlisted/sector] C3 spends a full trial family on a same-timezone US-TSX lead that is nearly all contemporaneous beta with a near-zero, sign-inconsistent residual — the exact not-worth-a-slot case. The plan's own prior admits it is mostly contemporaneous yet allocates the slot.
   EV: 25y weekly beta-stripped US-CA t+1: XLE-XEG +0.112, GDX-XGD -0.057, XLF-XFN +0.117 (contemp 0.86/0.96/0.80). GDX/XGD 96pct contemporaneous; signs disagree.
   FIX: Cut C3 from W2 or fold it into C1 as a covariate, not its own family. Reallocate to bank-earnings clustering.
[1.1 + 4.1 C5 shortRatio claim] Plan and audit assert canada_fundamentals holds a point-in-time shortRatio usable as a C5 baseline. It is a single live snapshot (asof last 5 days, one row per name) with NO history to backtest. C5 compare-to-existing-shortRatio cannot run historically; CIRO and the fundamentals shortRatio share the same no-history defect, hidden by implying one is a ready baseline.
   EV: data/canada_fundamentals/fundamentals.parquet: shortRatio present for 223/240 names but asof.nunique()=5 (2026-06-28..06-30) — a snapshot.
   FIX: Correct 1.1 to say shortRatio is snapshot-only. Reframe C5: CIRO value is 14y of backtestable history the snapshot lacks — but verify CIRO archive depth to 2012 by actual fetch before committing the collector, and drop the fundamentals-baseline comparison.
[4.1 C2 SEDI insider timeline] Insider data is 99.6pct post-2025-01-01. Accrue-to-3y-then-phase-0 does not validate until about 2028, and the thin-now phase-0 is one regime. The recon may also have missed a deeper free mirror.
   EV: data/canada_insider/insider.parquet: 19,220 dated rows 2020-05..2026-06, frac<2025-01-01 = 0.004; yfinance 150-row cap confirmed (median 78 rows/name).
   FIX: State the realistic C2 validation date (about 2028). Before conceding accrue-only, actually test canadianinsider.com weekly summaries and INK Research free tier for a deeper free SEDI mirror; if either yields multi-year net-buy summaries C2 validates far earlier — else say so.
[4.1 C6 tax-loss + C8 flow-through/TSXV] Both live in small-cap/venture names almost entirely OFF this 219-name TSX-large-cap board. C8 proposes a TSXV-listing demote but the board has ZERO .V tickers. Tax-loss rebound is a small/micro-cap January effect largely absent on a TSX60-style board (RY 8.2pct, TD, Enbridge, Shopify). Both cost a trial plus complexity for near-nil relevance.
   EV: data/canada_search/members.parquet: 219 names, 0 .V, all .TO; largest weights banks/Enbridge/Shopify. Caveat: 133 names <0.2pct weight incl. small TSX miners, so a partial small-ca
   FIX: Cut C8 TSXV branch (no venture names); keep only generic ADV/dilution hygiene. Scope C6 to the sub-0.2pct-weight tail only (test if the effect exists there) or defer — do not spend a trial on a board-wide Dec-Jan tilt.
[4 missing CA-native mechanism] The most board-relevant CA-native catalyst is omitted. Canadian banks report on a tightly CLUSTERED calendar (late Feb/May/Aug/Nov within about 2 weeks) and Financials is 22 names, 2nd-largest sector (about 29pct weight). BoC rate-decision windows (8/yr fixed) drive the rate-sensitive Financials/Utilities/REIT sleeve. C4 is a generic per-name streak leg ignoring the sector-clustered bank season.
   EV: members.parquet: Financials 22, Utilities 14, Real Estate 18; data/canada_earnings + data/canada_macro/policy_rate.parquet in-tree.
   FIX: Add: (a) bank-earnings-season window event study around clustered big-6 dates plus PEAD drift on bank beats; (b) BoC rate-decision conditioner for the rate-sensitive sleeve. Both use in-tree data and match the board far better than C6/C8.
--- MINOR (2) ---
[4.1 C1 40-60 flips + 9 ~16 trials] The flip count is inflated unless un-debounced (raw NEG-POS = 81 oil / 89 gold, whipsaw-heavy); debounced to independent episodes (>=4wk hold) = 43 oil / 50 gold, the LOW end of the claimed range and the true effective-N. Separately ~16 trials undercounts: explicit labels sum to HK 13 plus CA visible 6 before C3/C7/C2 families.
   EV: Debounced in-tree: 43 oil / 50 gold independent episodes. Trial labels grepped: (3),(3),(2),(3),(2),(3),(2),(1) plus uncounted families.
   FIX: Restate C1 event-N as ~43-50 independent episodes and use HAC/overlap-robust SEs. Recount and publish the true trial total; if >16, cut trials or add batteries.
[9 C1 event-study statistics] My C1 sector-tier replication gives t about 2.0 (oil) / 2.2-2.8 (gold) with NO HAC and NO overlap correction on 2-8wk overlapping windows — right at the DSR>=0.90 door. The sector-tier-likely-GO prior may not survive the house overlap-robust bar.
   EV: XEG 4w t=+2.00 n=43; XGD 4w t=+2.42 n=50, uncorrected; overlapping windows inflate t.
   FIX: Pre-register C1 sector tier with block-bootstrap/HAC on non-overlapping episode returns and require DSR>=0.90 before any GO.
--- MISSING (6) ---
+ A falsifiable definition of the C1 catch-up-gap residual (regression, horizon, expected sign) — currently a hand-wave the lead-lag data contradicts.
+ Honest validation DATES for accrue-only legs (C2 about 2028; C5 CIRO depends on unverified archive depth) so they are not implicitly sold as near-term.
+ A CA-native bank-earnings-season / BoC-rate-window mechanism, despite Financials being the 2nd-largest board sector and the most calendar-clustered TSX catalyst.
+ A gold-CAD interaction leg: gold and CAD jointly move the board (materials+energy+FX); their interaction is a plausible conditioner the generic commodity/CAD overlay does not decompose.
+ TSX index-rebalance (S&P/TSX Composite quarterly + TSX60) add/delete effects — a board-relevant event edge the plan includes for HK (H6 HSI rebalance) but omits for Canada.
+ Verification by actual fetch of CIRO Consolidated Short Position Report archive depth to 2012 and its true lag/cadence — marked likely-to-confirm yet scheduled as a W1 collector as if confirmed; a ~2-week lag plus semi-monthly cadence must be shown to leave tradeable signal for a NIGHTLY dashboard.
======================================================================
CRITIC: feasibility
======================================================================
VERDICT: The plan is directionally sound and unusually honest, but it CANNOT ship as scoped this week and has two load-bearing feasibility errors that will silently underdeliver. (1) Its keystone honesty fix — C7 "run canada_residual_alpha_phase0, a verbatim reuse of the harness that ran the HK kill" — is false: scripts/residual_alpha_phase0.py is hardwired to the US breadth panel (US _closes(), GICS→SPDR sector map, no --market flag, no HK/CA loader). C7 needs a real fork before W2, and if that fork is under-built the "single highest-leverage honesty fix" produces a mis-specified verdict. (2) The graduation machinery the plan leans on (W6 track-record panel, W7 gate-tier calibration, _WEIGHT_PRIOR recalibration) is fed by name_score ledgers that currently hold only 5–6 unique call-dates, grade the name-POTENTIAL score (not the standout rank or the new edge legs), and carry no forward-return columns yet — so W6/W7 have almost nothing to read and the "mid-July" maturity applies to the wrong scoreboard. Add the collision reality: a same-day-chartered, still-active china_alpha program is revamping CN stocks/baskets through the SAME shared engines (stock_score, signal_gate, baskets_region) with the SAME COILED/washout/basket thesis, and build_hk_library + build_china_library run in one asia-close loop — "market-parameterized" is not a merge-conflict protocol. Finally, every new W1 store is NOT gitignored, so the nightly `git add data/` will auto-commit and grow the repo with append-only backfill stores, exactly the sentinel-staging class the memory warns about. BEFORE shipping: build the CA/HK residual+reversal harness forks and prove they load regional panels; add real standout/edge-leg grading (not just name-potential) or drop the W6/W7 maturity promises to honest come-back dates; publish a concrete file-ownership map + merge-order pact with china_alpha; and decide R2-vs-git for every W1 store with .gitignore + audit_r2 tripwires added in W0/W1, not later.
--- FATAL (2) ---
[§4.1 C7 + §8 W2 + §1.3] C7 (the 'single highest-leverage honesty fix') is described as a 'verbatim reuse' of scripts/residual_alpha_phase0.py ('the harness already ran the HK kill'). It did not: that script is hardwired to the US panel — it loads US breadth via _closes()/_closes_deep() (data/breadth/_closes_deep.parquet), maps tickers via GICS→SPDR US sector ETFs, and exposes NO --market/--panel flag. The HK kill in reports/hk-residual-alpha-phase0.md was a separate one-off. There is NO scripts/canada_residual* harness in tree.
   EV: scripts/residual_alpha_phase0.py:262-304 argparse has no --market; _closes_deep() reads breadth/_closes_deep.parquet; sector map is US SPDR. `ls scripts/canada_residual*` → no matc
   FIX: W2 must include an explicit harness-fork task: parameterize residual_alpha_phase0 (or fork it) to accept a market panel loader (hk_search/closes_deep.parquet, canada close panel) + a regional sector map (TSX/HSCI sectors, not GICS→SPDR). Estimate this as real engineering, not a config flip, and gate C7's verdict on the fork being validated against the known HK-kill result first.
[§5.4 + §7.5 + §8 W6/W7] W6 'track-record panel reading real ledger data' and W7 'gate-tier hit-rate tables from matured ledgers' + _WEIGHT_PRIOR recalibration are structurally under-fed. The HK/CA name_score ledgers (a) span only 6 and 5 unique dates (2026-06-28→07-03), (b) contain NO forward-return/grade columns yet (grading is prospective at 21/63d), and (c) grade the name-POTENTIAL score (cols: score/tier/fuel/trigger/level), NOT the standout board ranking or the new edge legs the plan wants to calibrate. So 'HK mid-July' maturity buys ~6 dates of the WRONG scoreboard.
   EV: data/name_score/hk_calls.parquet 438 rows / 6 dates, ca_calls.parquet 1170 rows / 5 dates, no grade/fwd/ret/outcome columns; engine/name_score_grader.py:29 _HORIZONS_D=(21,63); HKC
   FIX: Either (a) add standout-rank + edge-leg forward grading NOW (W0/W1) so a real scoreboard accrues from day one, or (b) demote W6 track-record and W7 calibration to honest come-back dates (earliest 21d read ≈ 2026-07-19 for name-potential only; standout/edge-leg reads not before ~Aug given legs wire at W4). Stop implying W6 ships a populated panel this week.
--- MAJOR (4) ---
[§2.5 + §5.7 + §8 standing rule] Shared-engine collision with china_alpha is under-mitigated. china_alpha is chartered SAME DAY (2026-07-03), still in P1, with P3+ waves pending, and its thesis is identical ('about-to-run', COILED/washout, basket revamp) touching the same shared modules (stock_score, signal_gate, baskets_region). It has already shipped COILED-FIRE to the CN board via shared engines (#864/#869). Both programs do same-day squash-merges. 'All edits market-parameterized' is a coding style, not a conflict-avoidance protocol.
   EV: research/china_alpha/PROGRAM.md (charter 2026-07-03, P3 pending); commits b0a9f77e17/8ccffd9c17 ship CN COILED through shared engines; asia-close.yml:131 runs build_china_library A
   FIX: Add a concrete pact: (1) a file-ownership map naming which module regions each program may edit; (2) china_alpha owns baskets_region + COILED/washout wiring, HK/CA program consumes via market-keyed params only; (3) a mandatory rebase-onto-latest-main + cross-market test-suite run in every PR (the plan mentions the test suite but not the ordering discipline); (4) sequence: do not merge HK/CA W4/W5 basket work until china_alpha's baskets_region changes land, or coordinate a single owner for that file.
[§8 W1 + §5.2] New W1 data stores are NOT gitignored, so the nightly `git add data/` (asia-close.yml:73, daily.yml:134/761) will auto-commit them and re-commit/grow append-only backfill stores every run — repo bloat + the sentinel-staging/rebase-race class. The plan never says which W1 stores go to R2 vs git, despite the repo's explicit R2 data-plane pattern for heavy per-ticker stores.
   EV: `git check-ignore data/hk_southbound/... data/canada_insider/...` → not ignored; asia-close.yml already R2-publishes chinastockdata/hkstockdata (lines 152-170) with audit_r2 tripwi
   FIX: In W0/W1 decide per store: append-only backfills (southbound 2017→, CIRO 2012→, SFC, A/H panel) go to R2 like the existing hk/cn stores, with .gitignore entries + a scripts.publish_r2 --dirs addition + audit_r2 anchors; only small, slow-changing artifacts stay in git. Update .gitignore and sentinel git-add scope in the SAME wave the store is created, not later.
[§8 W2] W2 wastes a pre-registered trial slot on H3 under the plan's own trial-budget rule. W2 runs H3 A/H phase-0 'on extended panel from W1 if landed, else 3y interim'. A 3y-interim H3 run is a spent trial that the plan's own §1.1 says is crippled (~3y history, 12 pairs) — and BH-FDR within the family counts it. If W1's A/H extension slips, W2 either burns a slot on a known-weak test or blocks.
   EV: §8 W2 hedge text; §1.1 'A/H effective ~3y (12 pairs)'; §3.1 H3 needs 2006→ + 40+ pairs; §5/§6 trial-budget + BH-FDR discipline.
   FIX: Make H3 strictly W3 (post-collector), gated on the W1 A/H panel extension + pair expansion landing. Remove the '3y interim' fallback — a known-underpowered run is not worth a family slot. Only H4 reversal (25y, in-tree) and H5 peg-liquidity truly qualify for W2 on in-tree data.
[§8 W0 + HKCA-13] Canada sector drill-downs ship empty and W0 doesn't fix it. build_canada.py:319 reads data/canada_breadth/_closes_cache.parquet, which is gitignored (rebuild-only, R2/nightly). In any wave worktree or partial render this cache is absent → _build_sector_pages falls back to empty holdings. The plan's W0 'all four pages render' acceptance can pass with hollow Canada sector drilldowns.
   EV: `git check-ignore data/canada_breadth/_closes_cache.parquet` → IGNORED; HKCA-13 flagged it UNVERIFIED; the plan never lists the breadth close-cache as a W0/W1 dependency.
   FIX: Add to W0 acceptance: verify canada_breadth close-cache is populated at build time (from R2 or a rebuild step) and the sector drill-downs render non-empty; if genuinely R2-only, add the same audit_r2 anchor + health banner the plan mandates for other stores.
--- MINOR (3) ---
[§0 + §5.2 + §1.1] 'HK basket OHLCV never built (data/baskets/ohlcv/hk_* = 0 files)' is imprecise and risks a mis-scoped W5. The shared data/baskets/ohlcv/ dir DOES contain many parquet files (e.g. MHK.parquet, A.parquet). The real gap is HK-thematic-basket LEVEL series, but the blanket '0 files' claim could mislead an executing agent into rebuilding infra that partially exists.
   EV: `ls data/baskets/ohlcv/` shows MHK.parquet, A.parquet, AAL.parquet, ... present.
   FIX: Restate the gap precisely: 'no HK thematic-basket level/OHLCV series (baskets_hk membership has no persisted level path)', and have W5 confirm which basket-level artifacts already exist before building.
[§8 W1 render budget] The plan estimates no wall-clock for the added collectors and never states they run OFF the render critical path. If any W1 collector (southbound backfill, CIRO, SFC, sstoday) is invoked from build_hk/build_canada during render, it adds network to a lane the profiler shows already discards data/ writes (pure waste), risking regression of the RENDER_NO_DRIP discipline.
   EV: render-pipeline-perf-profile memory: build_stock_library drips were pure waste in render lane, gated by RENDER_NO_DRIP=1 (#645); asia-close collect runs via scripts.collect --group
   FIX: Mandate all W1 collectors live in the collect lane (scripts.collect --group asia / a new group), gated out of render via RENDER_NO_DRIP or by only reading committed/R2 stores at build time. Add an explicit 'no new network in the render lane' acceptance line.
[§4.1 C2 / §1.1] The SEDI 'append-only fix' has a lower ceiling than the plan implies. The 150-row cap is inside yfinance's returned payload per ticker (the cache stores one JSON payload per ticker, replace-on-fetch). Append-only only accrues NEW transactions going forward; it cannot recover pre-2025 history. The plan's ~3y accrual timeline is honest, but C2's phase-0 'now' on 2025+ data is thin to the point of near-uselessness for a selection leg.
   EV: engine/canada_insider.py:220-235 stores {ticker,payload,asof} one row/ticker; insider.parquet is 233 rows (one per ticker); yfinance get_insider_transactions cap.
   FIX: State explicitly that C2 is ACCRUE-only until ~2028 for a real backtest; the 'thin phase-0 now' is directional colour, not a graduation path. Consider a SEDI-native backfill source if C2 is to matter this program, or drop C2 from the trial budget.
--- MISSING (5) ---
+ A realistic total-program wall-clock. 8 waves with pre-reg-before-run discipline, harness forks (C7/H4 not verbatim), collector backfills with tripwires, ledger maturation gates (21–63d), and a UX overhaul cannot ship 'this week'. Only W0 (truth/safety) + W1 collector scaffolding + W2 in-tree phase-0s (H4/H5 + CA C1/C3) are honestly this-week work. W3 gates on collectors landing; W4 gates on W2/W3 verdicts; W6/W7 gate on ledger maturity (weeks). The status log should carry come-back dates per wave.
+ A merge-order / file-ownership contract with china_alpha beyond 'market-parameterized' — who owns baskets_region.py, stock_score._WEIGHT_PRIOR, signal_gate when both programs edit them same-day.
+ An explicit R2-vs-git decision table for every W1 store, with the corresponding .gitignore + scripts.publish_r2 --dirs + audit_r2 --anchors additions, mirroring the existing chinastockdata/hkstockdata pattern.
+ Per-collector failure-mode + tripwire spec. §9 mentions southbound fragility generically, but each of CIRO (twice-monthly CSV format drift / archive depth 'to confirm'), SFC (weekly schema), sstoday (today-only, silent-empty on holidays), and akshare southbound (documented hist_em empty / UA blocks / rss-cache masking) needs a named freshness stamp + audit tripwire, not a blanket 'fail-closed' principle.
+ A concrete adapter plan for the phase-0 harnesses (residual + china_reversal_phase0) to load HK/CA panels — treated as its own W2 sub-task with acceptance = reproduces the known HK residual kill on the HK panel before running CA.
======================================================================
CRITIC: product
======================================================================
VERDICT: The plan does NOT survive attack against the owner's bar ("a dashboard full of incredible picks, ripe for the picking"). Its engineering rigor is real, but as a PRODUCT it front-loads 4-6 weeks of invisible collector/phase-0 work behind a validation gate whose maturation timeline is mis-costed, and it never specifies the ONE thing the owner asked for: the assembled "ripe list" during the pre-validation window. Three load-bearing claims are factually wrong on inspection: (a) the name_score ledgers the whole graduation machine depends on started 2026-06-28/06-29 — SIX days ago, not weeks — so the promised "21d read mid-July" is optimistic and a STABLE read is August+, and worse, that grader grades the per-name POTENTIAL score, NOT the standout board ranking the boards actually display (HKCA-12 is still unaddressed); (b) the C7 "keystone honesty fix" is billed as a "verbatim reuse" of residual_alpha_phase0.py, but that script has no --market flag and hardcodes SPY/US-breadth/GICS/SPDR — HK needed a bespoke hk_residual_alpha_phase0.py, so C7 is net-new code, not free; (c) hk_edge() already exists and already ranks names by a sb/ahv/bnrs/fit stack with a full deterministic board order — the plan's "rebuild hk_edge around surviving legs" describes re-deriving what ships today, so absent a GO leg the user sees essentially TODAY'S board with more desaturated chrome and more "not a validated buy" sentences for months. Before shipping, the plan MUST: (1) specify the pre-validation "ripe list" board-order contract deterministically; (2) re-baseline every ledger-maturation date against the real 06-28 start and stop conflating the POTENTIAL grader with board grading; (3) re-cost C7/all phase-0 as bespoke; (4) define the honest-but-exciting card lead and a concrete end-of-program acceptance test (drafted in Missing).
--- FATAL (3) ---
[§5.4 + §7.5 + §1.3 (ledger maturation as the graduation engine)] The entire program's promise — boards get 'better truth' via forward ledgers that graduate legs and populate the track-record panel — rests on ledgers that are essentially EMPTY. Verified: data/name_score/hk_calls.parquet spans only 2026-06-28→07-03 (438 rows, 73 tickers, 6 dates); ca_calls.parquet spans 2026-06-29→07-03. §1.3 asserts 'HK ledger 365 calls / CA 1,170 calls already accruing; 21d read ~mid-July.' The 1,170 is just 5 days × ~234 names; these are NOT independent forward observations. grade() needs ≥5 distinct IC dates each with ≥5 names AT A 21d HORIZON — the earliest call (06-28) matures 21 trading days later (~07-27), and you need 5 such matured cross-sections, so a real HK rank-IC read is AUGUST, not mid-July; 63d read is late September/October. §7.5 promises the track-record panel reads 'real ledger data ... HK mid-July' — it will show 'accruing' at W6.
   EV: python read of both parquets: HK dates ['2026-06-28'..'2026-07-03']; grade() in engine/name_score_grader.py requires len(recs)>=5 and sub['score'].nunique()>=5 per date for an IC, 
   FIX: Re-baseline EVERY maturation date in §5.4/§7/§8-W7 against the true 06-28/06-29 ledger start. State plainly: no HK/CA rank-IC is readable before ~August (21d) / ~October (63d); the W6 track-record panel ships in 'accruing' state; W7 graduation is a 2026-Q4 event, not July. If the owner wants visible graded picks sooner, add a shorter (5-10d) provisional horizon explicitly labeled provisional.
[§5.4 / HKCA-12 (which ledger grades WHAT)] The plan treats name_score_grader as the forward twin that will validate the BOARD, but that grader grades the per-name POTENTIAL/buy-readiness score (tiers primed/setting_up/watch/no_setup), NOT the standout board RANK that the four pages actually display. The audit's own HKCA-12 says exactly this: 'Neither build_hk_library nor build_canada_library writes a forward-outcome log for its standout picks ... HK/CA have name_score_grader for the name-potential score only, not the standout ranking.' The plan cites HKCA-12 nowhere in §5.4 and adds fields to the wrong ledger. So the board a user sees ranked can still never be graded by the machinery the plan leans on.
   EV: engine/name_score_grader.py docstring: 'self-grader for the per-name POTENTIAL score'; _BUY_TIERS=('primed','setting_up'). build_hk_library compute_hk_standouts ranks by comp()/_co
   FIX: Add an EXPLICIT standout-board forward ledger (log the ranked buy list with as-of close each render, grade board-position vs forward excess) as its own W-item. Do not claim the name_score ledger validates board rank. Then the 'validated' tier on the boards has a real scoreboard behind it.
[Missing 'assembled pick' layer / §3.0-§3.1, §7 (the ripe-list contract)] The owner's goal is ONE ranked 'ripe list.' The plan enumerates legs (H1-H6, C1-C8), a conditioner/edge/entry/hygiene stack, and a separate §5.1 ignition board — but never specifies the deterministic board-order function for the pre-validation window. Meanwhile a full deterministic order ALREADY exists in code (compute_hk_standouts: comp() → bottoming-alignment gate → _combine_key percentile+confluence-weight → entry_open_first). The plan says W4 'rebuild hk_edge() around surviving legs' — but hk_edge() already fuses sb/ahv/bnrs/fit into one z. If W2/W3 yield zero GO legs (the plan's own honest prior for several), W4 changes NOTHING to rank, so weeks 1-12 the user sees today's board with MORE desaturation and MORE 'not a validated buy' copy — strictly LESS exciting than today, never more.
   EV: build_hk_library.py:773-808 (existing comp/_combine_key/entry_open_first order); engine/hk_stock_signals.py:150-193 (hk_edge already assembled); plan §3.0/§4.0 describe the stack b
   FIX: Add a §3.0.1/§4.0.1 'Pre-validation ripe-list contract': the exact, coder-implementable board-order for screen-tier state — e.g. order = (bottoming-alignment tier) then (edge-stack z percentile) then (entry-window openness), with an explicit entry-window field ('entry open now / pullback zone X-Y / wait for weekly turn') on every card. Make it a W0/W1 deliverable so week-1 users get a sharper ranked list REGARDLESS of any GO. State that if zero legs graduate, THIS is the permanent product and it is still better than today.
--- MAJOR (5) ---
[§4.1 C7 + §8-W2 ('the harness is a verbatim reuse', 'single highest-leverage honesty fix')] C7 claims canada_residual_alpha_phase0 is a verbatim reuse of scripts/residual_alpha_phase0.py. It is not. That script has NO --market argument; it hardcodes the US benchmark (_yahoo_ret('SPY')), reads the US breadth close cache (_closes()), US GICS (_names_sectors()), and SPDR sector ETFs. HK required a separate hand-written scripts/hk_residual_alpha_phase0.py; China has china_residual_alpha_phase0.py AND china_residual_alpha_deep.py. So 'verbatim reuse' understates C7 to near-zero cost when it is actually a new bespoke script (TSX benchmark ^GSPTSE, CA sector map, 5y-only names). The 'highest-leverage honesty fix' is real but mis-costed, and it competes for the same W2 agent slot as five other CA batteries.
   EV: scripts/residual_alpha_phase0.py:262-345 (no --market; SPY/_closes/_names_sectors/SPDR hardcoded); existence of scripts/hk_residual_alpha_phase0.py and two china_ variants; hk repo
   FIX: Re-label C7 (and every 'reuse the harness' claim in §3-§4) as 'port the harness parameterized per market (new benchmark/sector-map/close-store)'. Budget it as net-new code. Given CA names are 5y-only (2021→), also state that the CA residual test cannot run the 25y deep panel HK used — the deep leg is sector-ETF only.
[§3.1 H4 + H3 (within-SECTOR reversal / A-H, needs HK sector labels & deep panel)] H4 (within-sector short-term reversal) and H3 secondary (sector-neutralized) both require per-name HK sector labels, but §1.1 admits 'no HSCI sector indices' and I found no GICS/sector map in engine/hk_stock_signals.py; the HK sector grouping the boards use comes from 10 hindsight-curated baskets (data/baskets_hk/membership.json), which §5.2 itself flags as look-ahead-biased. So 'within-sector' neutralization for H4/H3 is built on the same curated membership the plan is trying to de-bias — circular. Separately, H4 claims '157 names × 25y ... ~310 rebalances' but only 76 of 157 names have data ≤2005 (verified), and the panel is current constituents (survivorship) — the cross-section thins to ~59-73 names (matching the existing hk residual report), so the deepest-quintile reversal test runs on ~12-15 names per quintile.
   EV: grep: no sector/GICS/industry in engine/hk_stock_signals.py; data/baskets_hk/membership.json = 10 curated baskets; python: 76/157 HK names reach ≤2005; reports/hk-residual-alpha-ph
   FIX: Name the HK sector source explicitly (and if it's the curated baskets, run H4/H3 UNconditioned first, sector-neutral only as a labeled secondary with the curated-membership caveat). Restate H4's effective N honestly: ~59-73 names, ~12-name quintiles, current-constituent survivorship → effect size is an upper bound and DSR will be brutal on ~15 names/quintile.
[§7.1/§7.2/§7.3 vs owner goal (does honesty chrome make pages feel DEAD?)] The audit explicitly warned about caveat fatigue (~6 caveats/HK page) and the plan's fix is to (a) desaturate screen/context chrome, (b) shrink the score chip to a rank pill, (c) add a 'positioning candidate, not a validated buy' sentence to EVERY card, (d) tag every why-now chip 'accruing/context'. For months (per the ledger reality above) HK and CA are BOTH non-validated, so EVERY card on both stock pages and both basket pages is desaturated + hedged. That is the opposite of 'incredible picks ripe for the picking' — it reads as a museum of disclaimers. The plan never defines what the card LEADS with to stay exciting.
   EV: §7.1-7.4; §0 'boards get better in two ways' but both depend on validation that won't arrive in the visible window; audit §5 caveat-fatigue finding.
   FIX: Define the card lead explicitly: LEAD with the concrete mechanism state in active language ('Mainland crowd added 0.8% of float in 2w — heaviest in 6mo · H trades 12% under its A twin · weekly base holding') and the ENTRY WINDOW, with the tier as a small honest badge, NOT a desaturating filter over the whole card. Reserve desaturation for the composite SCORE only, not the setup narrative. The honest-but-exciting middle = 'here is a real, named, fresh mechanism + a price to act at; we haven't yet PROVEN it precedes runs — scoreboard building.'
[§5.2 + HKCA-3 (four-page product coherence: fresh stocks vs stale baskets)] After W6 the four pages must tell ONE story, but the data feeding them diverges. HK stock cards run on fresh prices (verified 2026-07-03) while the basket tailwind axis on those SAME cards reads closes_deep.parquet stale at 2026-06-18 (verified 15 days old) — a card can show a 'theme tailwind' 2 weeks out of date next to a live cycle. §5.2 promises to persist HK basket OHLCV (0 files today) and W0 promises 'closes_deep nightly refresh,' but the tailwind axis, ignition (§5.1), and basket-MTF all depend on that refresh actually landing and staying fresh — there is no freshness GATE that suppresses the axis when stale, only a 'refresh cadence' aspiration. If the nightly refresh silently lags again, the same contradiction returns and the stock page and basket page tell different stories.
   EV: python: data/hk_search/closes_deep.parquet last idx 2026-06-18 vs hk_stocks *.parquet 2026-07-03; audit HKCA-3; §5.2 'basket OHLCV = 0 files today'.
   FIX: Make the tailwind/ignition axis HARD-GATED on basket-price age (suppress + show 'basket prices N days stale' when age > threshold), not merely refreshed on a cadence. Add this gate to W0 acceptance so page-coherence is enforced by code, not by a cron that already failed once.
[§8-W4 (wire-the-survivors wave assumes survivors exist)] W4's acceptance criterion is 'every scored leg cites its report; refuted legs deleted; boards regenerate with visibly re-based ranks.' But §0 and §9 both concede many legs SHOULD fail, and §9's kill line admits 'if batteries A+B produce zero GO legs, the desks reframe permanently as honest screens.' There is no branch in the wave plan for the likely outcome that W4 has few or zero survivors — in which case W4 does no re-ranking and the 'visibly re-based ranks' acceptance test is unmeetable, and the schedule implies value delivery that may not materialize.
   EV: §8-W4 acceptance vs §0 honest prior 'several candidates should fail' and §9 'zero GO legs' contingency.
   FIX: Split W4 acceptance into two documented branches: (A) ≥1 GO → re-rank + cite; (B) zero GO → ship the pre-validation ripe-list contract (from the Fatal fix) as the PERMANENT board, with the honest-screen framing, and treat THAT as success. Tie the end-of-program acceptance test (below) to whichever branch fires.
--- MINOR (4) ---
[§4.1 C2 / §8-W1 (SEDI append-only fix accrues from ~zero)] C2 depends on fixing the SEDI collector to append-only so history accrues, but §1.1 says 201/233 names are 2025+ only due to the yfinance 150-row cap. Append-only from now means CA insider gets a usable ~3y panel around 2027-2028, and the 'thin phase-0 now (2025→)' is directional-only on <1.5y. The plan lists C2 as an edge-stack member (§4.0) which overstates its near-term contribution.
   EV: §1.1 CA: 'SEDI insider ~1.5y usable history (yfinance 150-row cap; 201/233 names 2025+ only)'; §4.1 C2 tier path 'accrual to ~3y'.
   FIX: Move C2 to a clearly-labeled 'accruing, ~2027 maturity' slot in §4.0 rather than listing it inline with backtestable-today legs (C1/C3), so the near-term board isn't implied to lean on it.
[§1.3 / §5.4 (registry_seed maturation criteria for the new experiments)] The plan says register every phase-0 in data/experiments/registry_seed.json with come_back_on dates, but with the true ledger start (06-28) and the mis-stated 'mid-July' anchor propagating into the registry, the admin come-back alerts will fire too early and read 'accruing,' training the operator to ignore them (alert fatigue on the ops side, mirroring the caveat fatigue on the user side).
   EV: §5.4 registry registration; §1.3 mid-July anchor shown false above.
   FIX: Set come_back_on dates from the real horizons (21d≈late-July earliest single grade, stable read Aug; 63d Oct) and add a 'min IC dates' gate to the alert so it only pings when grade() would actually return a number.
[§7.8 / HKCA-13 (canada _closes_cache absent — sector drill-downs)] HKCA-13 (Canada sector drill-down depends on _closes_cache.parquet, verified absent) is not addressed anywhere in the waves. If it is genuinely sparse (not just R2), the Canada sector pages ship empty drill-downs regardless of all the new legs — a visible hole on the CA product.
   EV: python: data/canada_breadth/_closes_cache.parquet ABSENT; audit HKCA-13; no W-item references it.
   FIX: Add an explicit W0 verification: confirm _closes_cache populates at build from R2; if not, either wire the CA breadth cache build or degrade the sector drill-down with a health banner (per §7.7).
[§3.1 H1 southbound (per-name backfill fragility vs the flagship billing)] H1 is the 'flagship build' and the headline HK thesis, but it depends entirely on an akshare/Eastmoney backfill the plan itself flags as fragile (§9), and today only 9 dates of per-name southbound exist (2026-06-18→07-02, verified). If the backfill is infeasible, §9 demotes H1 to accrue-forward with come-back ~2027-01 — meaning the FLAGSHIP HK edge may deliver nothing to the board until 2027, undercutting the whole 'HK thesis = flow' framing for the visible window.
   EV: python: data/hk_southbound/holdings.parquet dates 06-18→07-02 (9 days); §9 'if backfill infeasible → H1 demotes to accrue-forward (come-back ~2027-01)'.
   FIX: State a hard go/no-go date for the akshare backfill EARLY in W1 with a named fallback, so the program knows by W1-end whether its flagship HK edge is 2026 or 2027 — and adjust the HK product framing accordingly instead of leading the executive thesis with a leg that may not land.
--- MISSING (6) ---
+ A deterministic pre-validation board-order specification (the 'ripe list' contract) a coder can implement without a GO leg — currently the plan only lists legs and a stack diagram, while a full order already exists in build_hk_library.py that the plan neither cites nor commits to.
+ A concrete end-of-program acceptance test for 'goal met.' DRAFT: (1) each stock page surfaces ≥5 ranked names/day, every one carrying (a) a named fresh mechanism state, (b) an explicit entry window (open-now | pullback X-Y | wait), (c) a tier badge; (2) a standout-board forward ledger exists and shows ≥N graded calls with the buy-tier 21d hit-rate and rank-IC printed on-page (not 'accruing'); (3) target: buy-tier 21d hit-rate ≥55% AND rank-IC>0 with same-sign split-half for at least ONE market, OR an explicit honest-screen reframe if neither market clears; (4) ZERO contradictory copy across panels (automated check: no card asserts validated+unvalidated, no chip implies an edge the rank ignores — kills HKCA-6/7 permanently); (5) no card shows a tailwind/ignition axis whose basket prices are >3 trading days staler than that card's own price. Nothing weaker than (2)-(4) should count as 'goal met.'
+ An explicit standout-board (not name-potential) forward ledger — HKCA-12 is cited in the audit but the plan wires only the name_score grader.
+ A branch in the wave plan for the zero-GO outcome that the plan's own §9 says is plausible — W4/W7 acceptance criteria assume survivors.
+ Corrected, evidence-based maturation dates throughout (ledgers started 06-28/06-29, not weeks ago; 'mid-July HK read' is not supported).
+ Confirmation that HK per-name sector labels exist from a NON-hindsight source before H3/H4 are specified as 'within-sector' — otherwise the sector-neutral legs inherit the curated-membership look-ahead the plan is trying to remove.