# Theme Intelligence Lane F — Independent Acceptance Preregistration

**Operation:** `theme-intelligence-f-evaluation-and-independent-acceptance-20260919-sol-001`
**Lane:** F — Evaluation and Independent Acceptance
**Integration lead:** Lane A
**Frozen source subject:** Macro `68f80a8edf78966a3a89e1294038654944e9c217`
**Procedure pin:** Mastermind `9e796168b467c17d9853f139c4e4a6ccdf3a3a87`
**Registration state:** `PREREGISTERED_SOURCE_ACCEPTANCE`
**Parameter fits / searched configurations:** `0 / 0`

## 1. Observable outcome

The first vertical is accepted only when a deterministic evaluator reads the real committed production functions, registry, published shared artifacts, and served State of Themes card from one immutable Git subject; reproduces the two known semantic incidents; kills the preregistered false-green mutations; and emits a byte-reproducible receipt without changing any production engine, renderer, policy, data store, authority, or publication path.

This registration is an evaluation artifact, not a second ThemeState producer, event log, lifecycle, prediction service, favorable track-record store, publication plane, or release authority.

## 2. Frozen inputs and point-in-time boundary

The source subject above is the only input generation for the first vertical. The evaluator must use Git object bytes from that subject for:

- `scripts/build_state_of_themes.py`
- `engine/neuralweb/theme_thesis.py`
- `config/theme_thesis_registry.yml`
- `site/basketdata/foresight_cascade.json`
- `site/neuralwebdata/theme_thesis.json`
- `site/neuralwebdata/theme_state.json`
- `site/neuralwebdata/theme_asymmetry.json`
- `site/basketdata/theme_lanes.json`
- `site/state_of_themes.html`
- `data/neuralweb/theme_state.json`

The first vertical does **not** use market-price returns, revised macroeconomic history, current membership substituted for historical membership, delayed news, future closes, or browser/deployment observations. Consequently it can prove source/artifact semantic correctness or failure, but it cannot prove predictive edge, profitable entry, deployed-byte parity, or prospective learning maturity.

For later A–E exact-head checks, observation time, source availability time, computation time, and publication/first-visible time remain separate. A correction must preserve first-observed and first-displayed history; it may supersede evidence but may not rewrite those clocks.

## 3. Eligible universe, sessions, horizons, and controls

### First vertical

The deterministic semantic universe is the closed matrix in `incident_cases.v1.json`. It covers:

- WATCH without transition evidence versus true invalidation;
- clean early PRECIPICE versus independently evidenced crowding/cooling;
- fired-falsifier priority;
- high-crowding and crowded-and-fading priority;
- BROADENING, RE-RATING, ACCELERATING, CORRECTING, hidden-opportunity WATCH, and quiet WATCH controls;
- unchanged rank/gate/size/escalate authority.

No session or return horizon is inferred for this source-only vertical.

### Prospective/product evaluation after lane return

Before any B/C candidate fit or promotion decision, the candidate owner and Evaluation owner must freeze the canonical theme/group/instrument universe, canonical membership/exposure basis, regular-versus-extended session handling, closed/provisional bar law, and outcome horizons. Proposed statistical thresholds remain proposals until accepted; this registration grants none.

Negative controls must include failed breakouts and negative themes, not only AI semiconductors. Overlapping horizons, shared constituents, shared events, and duplicate evidence families must be purged, embargoed, clustered, or reported as dependent rather than counted as independent confirmation.

## 4. Baselines and estimands

Later candidate evaluation must report, separately:

1. detection recall;
2. research-capacity precision;
3. semantic false alerts;
4. evidence-to-visible latency;
5. entry-qualified-to-visible latency;
6. MAE, MFE, net return, and calibration only after their outcome clocks mature.

Economic-thesis validity is separate from a profitable rebound. Comparators are simple relative strength plus the incumbent Foresight, Radar, and Confluence outputs. Existing published leadership persistence in #7064 and the sector control in #7095 remain held research controls; neither is an expected-return result or production authority.

## 5. False-alert budget

For deterministic semantic acceptance the budget is **zero**:

- zero invalidations may be emitted solely because the current stage is WATCH when no accepted transition evidence proves degradation from the required prior state;
- zero clean PRECIPICE observations may be labeled crowded/cooling solely because the lifecycle stage is PRECIPICE;
- zero authority flags may change from the incumbent false/display-only state.

Prospective alert-rate thresholds are intentionally not invented here. They must be frozen with the returned B/C candidate definitions and accepted by the existing Evaluation/promotion owner before a fit or promotion read.

## 6. Mandatory discriminator registration

| Discriminator | First-vertical state | Acceptance requirement |
|---|---|---|
| WATCH vs true invalidation | active | No transition evidence means no invalidation; an actual accepted transition must discriminate. |
| Early PRECIPICE vs crowding | active | Clean PRECIPICE is early; independent high crowding/fading evidence may still force caution. |
| Two observations vs repeat renders | awaiting returned producer contract | Rendering the same source observation twice is one observation. |
| Mixed parent/subtheme strength | awaiting returned composition contract | Parent state cannot overwrite a disagreeing child state. |
| Duplicate evidence families | awaiting returned consumer contract | Reused source evidence counts once. |
| T1 / extension / pending confirmation | awaiting returned leadership contract | Pending is not confirmed and extension is not a new T1. |
| Group/member qualification mismatch | awaiting returned entry contract | Group qualification cannot silently set member entry permission. |
| Stale and partial source data | active for artifact health; expands on lane return | Null/stale/partial stays typed and cannot become zero or confirmation. |
| Split/dividend/holiday boundaries | awaiting price-dependent lane return | Adjustment and market-session basis must be explicit. |
| Correction-safe first-seen history | awaiting versioned contract | Corrections supersede; first-seen/first-visible clocks survive. |
| Authority invariance | active | `may_rank`, `may_gate`, `may_size`, and `may_escalate` remain false. |

An awaiting row is `NOT_YET_PROVEN`, never a pass.

## 7. Mutation/discriminator plan

The acceptance specification must kill at least these false-green mutations:

1. treat current WATCH membership as proof of a RE-RATING → WATCH regression;
2. map clean PRECIPICE to caution;
3. allow fired falsifiers to lose priority to an early label;
4. ignore independently high crowding on PRECIPICE;
5. count a published artifact/card mismatch as acceptable;
6. allow any rank/gate/size/escalate authority flag to become true;
7. repair only the synthetic PRECIPICE row while the real Medical Devices path falls to quiet;
8. drop the Foresight `asof` clock while calling artifact health complete;
9. coerce a stale/null leg to numeric zero and a low-risk band;
10. accept mixed `data/` and served `site/` ThemeState generations.

The mutation run operates on evaluator inputs or temporary source copies only. It never edits A–E production files or commits generated product data.

## 8. Gates and interpretation

- A deterministic `REJECTED_CURRENT_SOURCE` result is a successful evaluator outcome when it proves a real defect; it is not a successful product acceptance.
- Exact artifact reproduction and mutation discrimination accept the Lane F harness only.
- Lane A coordinates repair disposition and shared-contract integration.
- The lawful source/release owner decides merge, deployment, browser proof, and product acceptance.
- Unknown or unmatured prospective outcomes remain unknown.

## 9. Independent-review hardening amendment — 2026-09-20

**Review-hardening procedure pin:** Mastermind `b75a491db408892dfe6fe7c4bb9d40cfad8efcb3`
**Fit/search change:** none (`0 / 0` remains controlling)

External review found that the original synthetic clean-PRECIPICE row could be
false-greened without proving the real renderer path. This structural amendment does
not change the frozen subject, inspect outcomes, tune a threshold, or authorize a
production repair. It adds two immutable served-owner inputs and requires the real
Medical Devices observation (`PRECIPICE`, no fired falsifier, medium crowding, no
divergence) to traverse production classifier → lane artifact → served card. A repair
that merely removes PRECIPICE from the caution branch but leaves this real case quiet
is rejected.

The active stale/partial row is now executable rather than prose-only: raw `asof`,
`as_of`, and `generated_at` fields stay distinct and typed; the `data/` and served
`site/` ThemeState copies must be byte-identical; and an actual stale/null asymmetry
leg must remain stale/null rather than zero or confirmation.

The existing `RE-RATING → working` matrix row remains frozen as a fixture, but the
shared product interpretation is explicitly `LANE_A_ADJUDICATION_REQUIRED`: the
incumbent Foresight owner also describes RE-RATING as late / await dislocation / do
not chase. Until Lane A and the semantic owner decide whether “working” means thesis
health only and preserve a separate entry posture, a repaired in-memory candidate may
clear the two known defects but may not be labeled fully acceptable source semantics.
