# Rates & Inflation Command + Macro Release Intelligence — Recovery and Completion Freeze

Date: 2026-08-27
Status: **SOL CEO RECOVERY / ARCHITECTURE FREEZE — records only**
Owner: Sol CEO under explicit Chairman end-to-end assignment
Protected Sol Skillpack pin: `mastermindx-market-intelligence/Mastermind@6f1bc3dd39f1ebecd3c22e44aa11ca7a13fa5182`
Macro recovery base observed for this carrier: `0758de6b9a7e9e920a6f44e4c1abcd62dbf8074e`

This record supersedes any assumption that the July `RATES_INFLATION_COMMAND_MASTERPLAN_BY_FABLE.md` wave-status table is current execution truth. The July masterplan remains the product/authority source unless explicitly superseded below; current implementation, runtime evidence, and later program law control current-state claims.

## 1. Chairman outcome and 10/10 end state

The product job is one premium command surface that answers, without forcing the user to stitch together separate desks:

1. **What is inflation doing now, and what components are driving it?**
2. **What releases are next, what is Mastermind's experimental/validated forecast state, and what changed at the print?**
3. **What is the rates curve doing across horizons, including level, velocity, acceleration and turn/watch state?**
4. **How is that rate/inflation state transmitting into asset classes, sectors, cohorts and ultimately names?**
5. **What is dealer/OPEX structure doing to path/holdability around the same horizon?**
6. **What does the market price for the Fed path, how does it compare with dots/policy conditions, and what changed?**
7. **What is the honest forward-path synthesis, with receipts, nulls, staleness and disagreement visible?**
8. **What has the system actually learned prospectively, and which hypotheses remain experimental, null, held or killed?**

The machine job is to maintain one correction-safe, point-in-time evidence chain from official release truth + rate/curve/options inputs -> descriptive intelligence organs -> one composed context artifact/UI -> forward ledgers/evaluation, without creating another release, calendar, options, transmission, policy, risk, learning or lifecycle system.

Completion does **not** require forcing an experimental forecast or dealer hypothesis into authority. A lawful null/held state with an active forward evidence clock is a valid product state; laundering it into a signal is not.

## 2. Current capability ledger

| Capability | Status | Current truth / consequence |
|---|---|---|
| Release Radar official truth, receipt and publication lifecycle | **PROVEN_LIVE / health-sensitive** | Existing `engine/release_forecast*`, official adapters, immutable/repairable truth and `data/release_forecast/latest.json` are current and production-oriented. Recent corrections prove the health path matters; final program acceptance still requires fresh live-path proof. |
| MRI current forecast efficacy | **PARTIAL** | Current artifact explicitly says legacy cross-vintage target epoch is experimental, coherent-target refit has no valid current projection, clean forward CPI `n=0`, and accuracy claims are withheld. Do not resurrect superseded old backtest confidence. |
| Claims forecast beyond benchmark | **REJECTED_BY_DESIGN** | Prior attempts lost to naive baselines; claims remains benchmark-only unless a separately adjudicated future hypothesis earns promotion. |
| Release Radar / Events user experience | **BUILT_NOT_PROVEN** | Merged and historically used, but current production/browser proof was not established in this recovery pass. |
| Whole-market options/dealer historical surface | **BUILT_NOT_PROVEN** | `data/options_surface/{index_etf,sector_etf,industry_etf}.parquet` exists with backfill state covering 2017-2026 for the major roots. Current theta-ops liveness is not proven by committed history alone. |
| OVC vanna/charm/concentration features | **BUILT_NOT_PROVEN** | W1 feature extraction exists. Authority remains display/context only. |
| OPEX risk/context composer | **PARTIAL** | `engine/opex_risk.py` exists, but its current `event_collision` slot is still hard-coded `None` and says W4 is not built. W3 and merged W4 are disconnected. |
| Event-window engine | **PARTIAL** | `engine/event_window.py` exists, but CPI/PPI/NFP history is not a complete historical PIT release-date spine; current calendar fallback is CY2026. Context only by law. |
| Calendar/OPEX directional signal | **REJECTED_BY_DESIGN** | `DNR:KILL-CALENDAR-GATED-RISK` and RIC law prohibit event/OPEX proximity from rank/score/size/gate authority. |
| Dealer-load-extreme promotion lane | **SPEC_ONLY / HELD** | Calendar-agnostic magnitude hypothesis is preregistered and must accrue >=12 months plus its frozen gauntlet. Earliest evidence maturity is approximately 2027-07; no early promotion. |
| Yield-series momentum organ | **NOT_BUILT** | HS1 research exists and DGS20 is already supplied by CCW as `us20y`, but the RIC multi-horizon yield momentum/turn organ itself is absent. |
| Rate/inflation transmission map | **PROVEN_LIVE (display/context)** | Canonical `engine/rate_inflation_transmission.py` + `data/transmission/latest.json` already provide calibrated multi-asset pass-through. Its scored gate found no robust leg fit for authority. |
| Original W6 parallel rates->cohort engine | **REJECTED_BY_DESIGN as a new system** | Later Transmission Intelligence makes a second transmission engine duplicative. RIC may extend/compose canonical transmission only for missing cohort/horizon views. |
| Forward Path board | **PARTIAL** | `rates_command.v1` exists, but shipped before W5/W6 completion, still notes ACM pending, and the current artifact carries stale policy intelligence (`intel_as_of` 2026-07-13, 44 days stale at build). |
| Unified Rates & Inflation Command UX | **PARTIAL** | Releases, Fed Path, transmission and related reads exist on separate seams; one coherent premium end-to-end desk with all required states is not proven. |
| Unified RIC scorecard / learning enrollment | **NOT_BUILT / PARTIAL foundations** | Individual organs have forward ledgers/sensors; no canonical `ric_scorecard` exists. Aggregate existing evidence; do not create a new learning plane. |
| Historical priors HS1/HS2 | **PROVEN research, not product authority** | Yield-turn catalog and event-reaction studies exist. HS2 corrected the assumption that CPI days are generally higher realized-vol days. |
| Historical dealer/OPEX priors HS3/HS4 | **NOT_BUILT** | W2 history now exists, so the previously blocked research can be executed. |
| End-to-end production reliability/proof | **PARTIAL** | Recent target/vintage regressions, current stale policy prose and unproven theta liveness prevent final acceptance. |

## 3. Stale or false recovery records found

1. `docs/PROJECT_ACTIVE_BUILD_MAP.md` is an old August 11 snapshot and must not be used as current collision truth. The newer active-build map and direct GitHub PR reads control.
2. The July RIC wave-status/sequence is stale: W7 Forward Path landed while W5 was absent and W6 had been overtaken by the later canonical Transmission program.
3. `engine/opex_risk.py` contains a stale implementation assertion that W4 is not built; current main has W4 but the integration slot remains null.
4. Old MRI benchmark/accuracy narratives are superseded by the August target-epoch and defect-repair truth. Current `release_forecast/latest.json` withholds accuracy and reports clean aligned CPI forward `n=0`.
5. The Forward Path artifact is fresh as a file but carries stale policy intelligence from 2026-07-13; file freshness is not semantic freshness.
6. A committed options-surface backfill is not proof that the current theta-ops collector/builder is live.
7. Slack delivery or channel membership is not worker execution. Current Autonomy V1 law freezes absent-recipient commissions; no Rates/Inflation carrier or ACK was found.

## 4. Architecture and no-rebuild ruling

### Canonical ownership

- **Release intelligence:** existing MRI `engine/release_forecast*`, official release adapters, release ledger and Release Radar. Extend; never create `release_lab` or a second forecast truth store.
- **Scheduled events:** `engine/event_calendar.py` is the canonical upcoming calendar. Historical PIT release-date work must extend/consume this release/event family and official-vintage receipts, not create a second calendar.
- **OPEX calendar:** `engine/opex.py` remains the expiration owner; event-window code delegates.
- **Options/dealer data:** current ThetaData options plane + `engine/options_surface.py` and its committed aggregate stores. ThetaData is the Chairman-ruled canonical source (`DEC:AD-OPTIONS-CANONICAL-SOURCE-THETADATA`); the data plane is owned by active `WS:ADVANCED-DATA-OPTIONS`, and options-intelligence recovery is separately owned by `WS:OPTIONS-ALPHA-INTELLIGENCE-RECOVERY` (OA0 freeze, landed on main 2026-08-27 after this record's recovery base). RIC composes these planes as a consumer; it does not adjudicate options-domain ownership. Do not resurrect retired Polygon assumptions or add a parallel surface history.
- **Risk:** existing Risk Radar remains the risk owner. Event/OPEX context never enters `_SCARES` or sizing/gating.
- **Rates data:** existing FRED/curve collection. DGS20 ownership is CCW / alias `us20y`; RIC consumes it.
- **Transmission:** `engine/rate_inflation_transmission.py`, `stock_macro_sensitivity.py`, and the later Transmission Intelligence program. No second causal/transmission brain.
- **Policy/Fed:** existing Fed-path/policy-condition organs. RIC reads and composes; no policy timing predictor.
- **Learning/evaluation:** existing forward ledgers, sensors, lobe/metabolism and evaluation laws. RIC may publish an aggregate scorecard/view, never a second evidence-clock control plane.
- **Execution/lifecycle:** Executive OS is the only Job/Attempt/Worker/Event authority; Agent OS is durable organizational memory; GitHub is implementation/evidence; Slack is transport/attention.

### Authority ceiling

All RIC/MRI/OPEX/event/transmission composition remains display/context/research authority unless a specific preregistered hypothesis separately passes its forward promotion gauntlet. LLMs may narrate, critique and de-escalate; they do not originate numerical forecasts, event direction, risk rank, trade direction or sizing.

## 5. Completion wave graph

The old W-number order is replaced for continuation purposes by capability lanes. Existing work is preserved; this is a coordination graph, not a rebuild.

### F0 — Recovery, freeze and durable ownership

**Outcome:** current truth, architecture boundaries, acceptance contract and Agent OS continuation are recoverable. This records-only carrier is F0. No runtime/product behavior changes.

### F1 — Release + event truth and intelligence

**Depends on:** F0.

**Outcome:** one PIT-correct release/event object spans historical and live use, with canonical schedule provenance, official first-print/revision receipts, pre/post event descriptive reaction context, explicit model epoch, confidence/null/experimental state and clean evidence-clock behavior. Repair historical CPI/PPI/NFP event-window coverage by extending the existing calendar/release family. Finish current MRI health gaps without retuning to outcomes or inventing consensus. Preserve claims benchmark-only and CPI clean-forward withholding until evidence matures.

### F2 — Dealer/OPEX state and historical priors

**Depends on:** F0. Parallel with F1/F3.

**Outcome:** prove current ThetaData surface liveness; connect W4 event collision into the existing W3 composer without making it directional; verify OVC consumers; execute HS3/HS4 against the 2017-2026 surface history with preregistered rulers; keep dealer-load-extreme promotion held until its own maturity gate.

### F3 — Yield momentum + canonical rate transmission

**Depends on:** F0. Parallel with F1/F2.

**Outcome:** add the missing multi-horizon yield level/velocity/acceleration/turn-watch organ using existing collected series (including CCW `us20y`), then extend/compose canonical Transmission only for the missing cohort × horizon reads needed by RIC. No new collector and no parallel transmission engine.

### F4 — Forward Path composition

**Depends on:** F1 + F3.

**Outcome:** rebuild the Forward Path composition from the canonical fresh inputs, remove stale-policy presentation, consume yield/Transmission results, preserve transparent disagreement/nulls, and only add ACM/term-premium work if no canonical equivalent already owns it. The board must expose source-as-of and semantic staleness per component, not just file freshness.

### F5 — Unified premium Rates & Inflation experience

**Depends on:** F2 + F4.

**Outcome:** one coherent responsive/bilingual desk with: Latest; Rates & Curves; Releases & Response; OPEX & Dealer State; Transmission; Policy/Fed; Forward Path; Evidence/Learning. Tier 1 says what changed/why it matters/what to watch; Tier 2 carries mechanics and receipts. Null/stale/partial/experimental/outage states are designed, not hidden.

### F6 — Evaluation, learning and historical prior enrollment

**Depends on:** can start schema/read-only after F0; final acceptance depends on F1-F4 outputs.

**Outcome:** aggregate existing forward ledgers and sensors into one RIC evaluation view keyed to canonical evidence clocks. Preserve each organ's ruler and promotion law. Enroll lawful research/evaluation consumers through existing learning infrastructure. No unified score may become market authority.

### F7 — Production reliability and acceptance

**Runs through every wave; final after F5/F6.**

**Outcome:** prove real scheduled-release hot paths, routine daily builds, theta surface freshness, semantic freshness alarms, fail-closed/fail-open behavior, corrections, rollback and one real user/browser journey. A green CI, merged PR, committed JSON or Slack message is not this proof.

Graph: `F0 -> {F1,F2,F3}; {F1,F3}->F4; {F2,F4}->F5; {F1,F2,F3,F4}->F6; {F5,F6}->F7` with production-proof obligations attached to each predecessor wave.

## 6. Exact first commission set — READY, NOT ACTIVE

Current accepted Autonomy V1 law does not allow absent-recipient Fable delivery to be called execution. Therefore the first three sustained lanes are frozen here but remain **UNCLAIMED** until canonical Executive admission/worker routing produces an actual carrier/session and that receiver ACKs the exact operation.

### Commission RIC-F1 — Release/Event Truth & Intelligence

**Mission:** make historical/live release-event truth one correction-safe, PIT-consistent capability consumed by Release Radar and event-window context, while preserving current experimental forecast status.

**Why:** RIC cannot reason about event windows or learn prospectively if historical dates, first-print truth, target epochs and current release context do not share one lawful spine.

**Precedence:** protected Sol Skillpack pin above -> `DO_NOT_REBUILD.md` -> current Agent OS decisions -> this freeze -> MRI masterplan -> implementation. Later source/repair evidence beats old benchmark prose.

**Scope:** Macro only; `engine/event_calendar.py`, `engine/event_window.py`, existing `engine/release_forecast*`/official adapters/builders/tests/data contracts, and narrowly necessary UI consumers. Reuse existing stores.

**Non-goals:** no new release system/calendar; no street-consensus fabrication; no model tuning to outcomes; no claims resurrection; no directional event signal; no policy timing; no authority promotion.

**Journey:** before release -> see exact schedule/provenance + experimental forecast state; at print -> official first-print receipt/revision identity; after print -> surprise/decomposition/reaction context; history -> same event identity and clock semantics; failure -> explicit unavailable/stale/partial state.

**Method:** deterministic official truth/schedule/receipt mechanics; statistical forecast/evaluation only under existing MRI epochs; model-generated text narrative only, never numerical origination.

**Acceptance:** tests for date/provenance/correction/look-ahead/null cases; real historical replay; one real recent official release through the production builder into the actual Release Radar/event consumer; exact source clocks and target epoch printed; no authority booleans raised.

**Stop:** stop if work requires a second calendar/store, retrospective relabeling of a forward trial, or a new promotion ruling. Return PR + exact head + tests + real-input production proof + remaining evidence-clock state.

### Commission RIC-F2 — Dealer/OPEX State

**Mission:** turn the existing W1/W2/W3/W4 pieces into one current dealer/OPEX context capability and complete HS3/HS4 without directional laundering.

**Why:** the current implementation literally leaves W4 disconnected from W3, and committed history is not proof of live theta operations.

**Precedence:** protected Skillpack -> DNR calendar-gated-risk kill -> this freeze -> RIC masterplan/prereg -> existing options/OPEX implementations.

**Scope:** Macro only; current ThetaData/options-surface pipeline, `engine/opex_risk.py`, `engine/event_window.py`, `engine/opex.py`, existing OVC stamps/consumers, HS3/HS4 research/tests. Respect any current Intraday Flow OPEX ownership (`WS:INTRADAY-FLOW-P0-RECOVERY` owns `engine/opex.py`) and the active options-domain owners `WS:ADVANCED-DATA-OPTIONS` and `WS:OPTIONS-ALPHA-INTELLIGENCE-RECOVERY`; do not duplicate their paths.

**Non-goals:** no directional OPEX signal; no `_SCARES`; no weighting/fused 0-100 score; no dealer-sign certainty; no early dealer-load promotion; no second surface store/provider path; no charm-flow directional narratives (`DNR:KILL-CHARM-NARRATIVES`).

**Journey:** user sees current cycle phase, dealer concentration/load, vanna/charm context, event collision and historical base-rate caveats with freshness/coverage; missing/stale theta data yields explicit degraded state, not a plausible stale read.

**Method:** deterministic feature/state composition; preregistered statistical event studies for HS3/HS4; narrative only after facts exist.

**Acceptance:** W3 consumes W4 collision correctly; 2017-2026 historical studies are reproducible; live theta surface freshness is proven on the real production lane; real OPEX/event date flows to actual UI/context consumer; authority remains false.

**Stop:** stop on provider/source ambiguity, collision with an active Intraday Flow, Advanced-Data/Options or Options-Alpha owner, prereg change after outcomes, or any request to promote dealer-load early. Return exact carrier/evidence packet.

### Commission RIC-F3 — Yield Momentum & Canonical Transmission

**Mission:** deliver the missing multi-horizon yield/turn capability and the specific cohort × horizon reads RIC needs by extending the canonical Transmission organ, never by creating another one.

**Why:** W5 is absent; the original W6 design is stale because later Transmission Intelligence already owns calibrated pass-through and per-name rate sensitivity.

**Precedence:** protected Skillpack -> Transmission/DNR law -> this freeze -> RIC masterplan -> existing rate/transmission implementations.

**Scope:** Macro only; existing rate/FRED feature frames, CCW `us20y`, a narrowly scoped yield-momentum module/contract, `engine/rate_inflation_transmission.py` and existing transmission consumers/tests. Extend existing artifacts when contract-compatible.

**Non-goals:** no DGS20 collector; no new transmission/causal brain; no score/rank/size authority; no retrospective yield-turn signal promotion; no duplication of stock macro sensitivity.

**Journey:** user sees 2y/5y/10y/20y/30y level + multi-horizon velocity/acceleration/turn-watch, then a received/transmitted view across the approved cohorts and horizons with sample/evidence caveats; null cohorts remain null.

**Method:** deterministic time-series transforms for level/velocity/acceleration; preregistered historical statistics for descriptive base rates; canonical measured transmission coefficients; model text only for explanation.

**Acceptance:** existing collected series only; tests for sparse history/nulls/time alignment/corrections; HS1 is treated as prior evidence, not authority; real current rates input flows through canonical transmission into an actual consumer; no second owner/artifact family appears.

**Stop:** stop if required functionality would fork Transmission ownership or add a data collector already owned elsewhere. Return PR/evidence/production packet and exact residual for F4.

## 7. Execution truth at recovery

- F0 recovery carrier: **claimed by Sol as this single GitHub records carrier; not accepted until review/merge**.
- RIC-F1: **READY / UNCLAIMED**.
- RIC-F2: **READY / UNCLAIMED**.
- RIC-F3: **READY / UNCLAIMED**.
- F4-F7: **NOT COMMISSIONED**; dependencies are not yet satisfied.
- No Slack message, Fable branch, Executive Job or carrier ACK for Rates/Inflation was found during recovery.

## 8. Production acceptance definition

The program is accepted only when all of the following are simultaneously true:

**Truth:** official releases and corrections are PIT-safe; rate/options inputs name their source clocks/freshness; stale/partial/outage states are visible; no silent vintage or semantic-staleness defects remain.

**Intelligence:** release forecast status is calibrated to the actual evidence epoch; yield momentum and transmission are descriptive at their proven ceilings; OPEX/event context is non-directional; Forward Path exposes disagreement/nulls rather than manufacturing certainty.

**Product:** one coherent Rates & Inflation workflow works end-to-end on real data across desktop/mobile-relevant layouts and all failure states. A user can move from current state -> next release -> reaction -> curve/yield -> dealer/OPEX -> transmission -> policy -> Forward Path -> evidence without reconstructing the system manually.

**Learning:** every promoted or experimental claim has a named ruler, horizon, evidence-clock start, forward ledger and promotion law; unified reporting aggregates those clocks but cannot rewrite them.

**Operations:** daily and release-hot-path producers are live and fresh; the options surface proves current production advancement; corrections/retries are idempotent; health alarms distinguish stale source, stale semantics, partial input and hard failure.

**Proof:** at least one real official release and one normal non-release day traverse real production inputs -> real production builders/services -> real artifact/context -> actual user-visible consumer, with receipts/screens/browser evidence as appropriate. Merge/CI alone is insufficient.

**Authority:** all existing authority ceilings and DNR kills remain intact. No calendar/OPEX directional laundering, no policy timing predictor, no LLM numerical origination, no hidden fused score, no duplicate control plane.

## 9. Next CEO action

After this recovery carrier is independently reviewed and accepted, submit RIC-F1/F2/F3 through the canonical Executive admission/routing path as three disjoint operations. Treat a lane as active only after the concrete Fable/worker carrier/session ACK is visible in canonical runtime/transport evidence. Sol remains owner of F4-F7 integration, adversarial review and final production acceptance.
