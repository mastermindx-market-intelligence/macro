# Signal Lab construction identity repair (W2A)

Operation: signal-lab-spec-identity-20260916-sol-001. Parent: existing research-factory / WS:SIGNAL-LAB-REVAMP authored in pending #7190; do not duplicate the workstream. Base: 27ec910d04fc81692cf9640347ac31fd3de40170. Current procedure: Mastermind 4537f066775c73d305f82acf0643701f01f5e53c.

## Mission / authority
Current Chairman continuation of the approved Signal Lab revamp. Direct source ownership reason: NO_ELIGIBLE_PRE_EFFECT_WORKER; the existing Capacity owner has returned ROUTE_UNREACHABLE and no reviewer/builder is assigned. One native source carrier: Remote Desktop Commander / Mac Studio 3f5ce987-e3eb-40a3-af9f-4b0ae54919cc. W1 #7190 and W3A #7212 keep their exact frozen branches and release gates.

## Design freeze
Upgrade the existing construction hash, screen and two filing helpers rather than add an identity registry. Version 2 binds ordered data declarations (including selected columns and availability declarations), complete feature/target declarations, market, universe and baseline. Normalize defaults that the evaluator already applies. Display/author text and gate retuning do not create novelty; gate-freeze remains the separate existing gates_hash contract. The hash identifies a declared construction, NOT a data vintage, point-in-time certification, executed result or independent economic idea.

Legacy complete specs are projected into the new identity in memory; never rewrite the historical candidate or trial ledgers. Opaque old hashes that collide with the legacy coarse fingerprint cannot prove novelty and remain held as incomplete evidence. Corrupt ledger contents or an unhashable construction must not disable novelty checks. Both filing helpers stamp code-computed hashes/version and preserve rejected definitions for future deduplication. No generation/provider call is authorized by testing their deterministic file helper.

The numerical harness and trial-ledger implementation stay untouched. Its existing config hash will see the new construction hash on a future evaluation, so rerunning an old spec may conservatively add a versioned test configuration; it never resets or decreases old family counts. No historical re-evaluation or migration is performed by this slice. Separate benchmark-label semantics and freeze-on-evaluation enforcement remain W2B; do not mislabel W2A as all of W2.

## Acceptance
Demonstrate RED → GREEN on source/column/availability/ordering/baseline collisions, legacy duplicate and uncertainty cases, corruption, backend stamps and same-batch duplicate filing. Replay all 22 committed candidate definitions read-only and report reclassification. Preserve existing numerical thresholds and all canonical data bytes. Run applicable CI contracts and independently review before release. No auto_loop/CODEX_MODE/Workspace Agent activation, merge, production or trade authority is implied.

## Continuation
Next: run the new tests against original source, implement the bounded identity flow, then verify full producer-to-screen-to-filer path and register CI in its existing owner.


## Verified implementation checkpoint

The first new suite returned 20 assertion failures and one pass on the unmodified source. After repair, the nine focused Foundry files return **156 passed**; the existing Signal-lane compatibility subset returns **21 passed** with its fake nonrepository test fixtures isolated from ancestor Git discovery. Eight fault-injected variants are caught by assertions, without source edits. The full original contract checker against base `27ec910d04fc81692cf9640347ac31fd3de40170` returns **0 introduced / 0 inherited, exit 0**. Exact commands, source digests and limitations are in `research/evidence/signal-lab-spec-identity-20260916/verification.json`.

The 22 committed legacy rows all reproduce their stored v1 hashes and all map to 22 distinct v2 definitions. Every original definition remains recognizable in the projected prior set. Eighty-eight controlled modifications of source path, column, availability declaration or baseline distinguish under v2 despite colliding under v1. The canonical ledger is never edited by this replay; no test is run on market returns and no proposal is admitted to production.

Both existing filing helpers now overwrite caller-supplied identity fields. Hashable rejected definitions are retained for future duplicate detection. A malformed noncanonical computational payload is recorded as an explicit rejected identity error without injecting NaN/object data into subsequent identity reads. Gate hashes remain unchanged. V1 incomplete hash collisions stay conservative holds. V2 stored-identity disagreement or unreadable/corrupt prior JSON cannot silently disable the novelty gate.

## Compatibility limits and failed attempts

The initial wider compatibility run returned 231 passed / 13 failed, in unchanged case/signal legacy tests. Three Signal fake-repository tests were rerun against an exact-base source archive; they also failed on the original source, with ancestor Git and import-order behavior disclosed in `compatibility-attribution.json`. With correct private fixture Git isolation, the coherent existing Signal subset passes all 21 cases. The ten Case-lane failures are outside the changed modules and remain unresolved, not waived; a whole-repository or pristine full-baseline pass is not claimed. A first in-memory attribution probe had import interference and is not used to prove equivalence.

One optional new trial-history regression write was blocked before a process ID was returned. A same-carrier file read verified that test was absent; it was not retried through another tool. Existing idempotent-ledger tests in the final suite passed. The implementation does not change trial-ledger code; an old-to-new config migration remains conservative accounting, not a claim of new independent trials.

## Review owner and next actions

The Secretary returned the supported Capacity / Operator-Continuity interface and an actual `ROUTE_UNREACHABLE` delivery blocker on the existing root `C0BSBM78V1N/1789597812.452299`. Sol consumed that return with same-root ruling `1789605308.737709`. No reviewer is assigned, ACKed or STARTed; the original placement request is for #7190 and #7212 only and is not silently widened by this source slice. No raw provider/Workspace Agent/Codex worker was launched. File helpers named Codex are tested deterministically, not used to spend provider capacity.

Source custody: this task's one branch/worktree. Exact branch publication lookup is `claude/signal-lab-spec-identity-20260916-sol-001`; resolve/reuse its unique PR before creating anything. Source status is BUILT_NOT_PROVEN until accepted independent review, current candidate/integrated binding CI and source reconciliation. Production acceptance separately requires the installed existing screen/filer to distinguish real declared constructions and preserve its prior-history behavior. Neither acceptance event grants forecasting/trade authority.

After publishing: preserve the source head; combine with the existing #7190/#7212 candidates in an isolated immutable tree and verify the actual imported interfaces before release. Then advance W2B (benchmark/label semantics and full frozen-spec/result-generation enforcement), followed by actual experiment maturation through existing collectors/graders. Do not call this W2A slice a complete Signal Lab or a completed W2.

Do not redo the original audit, rewrite historical results, overwrite candidate IDs, reset family trial accounting, create another registry/queue, modify the other two source branches, enable auto_loop/CODEX_MODE, select an unbound reviewer account, or re-run concluded #7190 CI. The existing WS:SIGNAL-LAB-REVAMP parent is still authored in pending #7190; this repository research continuation is intentional until that parent is published, not a second workstream record.


## Publication and integration correction

Initial source `a98441480a8203a3d2d444f9fc00a1e876f48a74` is published as draft/HOLD PR **#7239**. A virtual combination with the two existing repair heads detected a textual conflict: both the numerical repair and this slice appended their new CI test step at the end of `causal-factory`. No actual merge, branch adoption or predecessor edit occurred. The identity step is moved before the unchanged legacy suite, still after dependency installation. A structural YAML comparison confirms every job, path, dependency and command is retained; only this new step's position changes. The identity suite passes **27 tests** after this correction. Numerical/filing source bytes are unchanged. The exact three-candidate integration result belongs in the source PR checkpoint after the new immutable head is known.
