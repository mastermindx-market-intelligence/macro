# Personality Signal Suite — masterplan (bottom-ID toolkit + idea foundry)

Status: CHARTERED FOR EXECUTION (operator order 2026-07-25; authored by Fable as the
"now what" after PTT-W1/§7/§8). Program home:
`research/PERSONALITY_TIMING_TAILORING_HANDOFF_FOR_FABLE.md` (§1-§8 = evidence base;
this masterplan is its forward plan). Operator's ask, verbatim core: *"find the
pattern of how each stock's bottoms are identified, what indicators help identify
those bottoms and the timeframes … a multi suite of different indicators for calling
bottoms … assess whether confluence firing improves win rate … automated creative
brainstorming of new signals."* Plus two surfaces flagged: tech_lab.html#combos
("looks hopeful, not sure it works") and us_stocks_lab.html ("measures all stocks
as if one size" — correct suspicion).

## §0. ACCEPTANCE GATES (receiving session: not done unless…)

1. §2 constraints read and each wave's outputs graded on the TIMING ruler
   (charter §7: MAE primary, proximity co-primary, per-metric random-day nulls;
   fwd63 confirmatory only). Any doc/surface copy obeys R-W1T-3: "reset
   confirmed / shallow-risk entry", NEVER "bottom called"; no home-rung claim
   without its ruler stated.
2. Nothing re-proposes the two-ruler audition kill (DNR §2): no per-name
   best-of-grid selection anywhere, including inside combo search. Measurement
   derives; events validate; grids are pre-pinned budgets.
3. W-LAB audit findings appended to this file (§5 verdict block) BEFORE lab
   changes ship; lab upgrades are display-tier and ship freely after audit.
4. W-FOUNDRY composes the EXISTING organs (metabolism build loop, CHF
   proposal tier, trial_ledger FDR families, cortex 3/week chokepoint QS-U2) —
   building a parallel auto-backtest farm or auto-registration lane violates
   CHF-R2 (machine-registration family DEFERRED, Phase-2 clock 2026-10-15) and
   is REJECTED in advance. Foundry output = prereg DRAFTS for adjudication.
5. Every new signal-family study: wrong-ruler check in the header BEFORE
   statistics are chosen (§7 item 5); trial-ledger family registered BEFORE
   outcomes are read (personality phase-0 precedent, R-SP11).
6. Same-day PR chain per house law; memory updated
   (`personality-timing-tailoring` file + index line).

## §1. What this program now knows (do not re-derive)

- Structure-tailoring (measure bars → derive rung) is the ONLY fitting method
  above the random floor, CI-clean on timing metrics (+5.87pp proximity vs
  random, +8.72pp vs audition — charter §8). Audition = two-ruler kill.
- These tools CONFIRM RESETS, they do not call bottoms: 48-66% fire after the
  trough; MWR census 0/13 within 5% of the low, 11/13 after-trough with median
  MAE −0.64% (vs −3.85% ambient); the only before-trough fires were the 2022
  catastrophes (and the accelerating-tightening veto kills exactly that class).
  Lateness IS the safety. Perfection framing is retired; shallow-adverse
  confirmation is the product.
- Class altitude (vol×trend terciles) has not separated → W4 class gate
  profiles UNLICENSED until a class variable separates under the timing ruler
  (try the PIT personality labels before inventing clusterings).
- Honest ceilings: random-day GOOD base-rate 69% (fwd63 lens); best validated
  construction ~77% (91% ex-2022-class under the veto). Anyone's 90%+ claim is
  in-sample selection until proven otherwise.

## §2. Binding constraints (violations have killed programs)

Timing ruler + copy law (charter §7/§8, R-W1T-1..3) · two-ruler audition kill ·
measurement-first governing law (§1b) · era-split DT-R16 · ticker×time cluster
bootstrap DT-R14 · multiplicity: every grid/combo budget pre-pinned and counted;
combo search discloses N-searched (tech_confluence already does — keep it) ·
graded-board contamination row (nothing enters `us_board_ledger` population) ·
DON'T-TEST rows: rotation×cycle entry-confluence; rs member-dispersion gates ·
PSF timing-blend null (blended conviction×timing ranking FORBIDDEN) · LLM
origination ban (CONST-ART1): LLMs draft PROPOSALS and de-escalate; never emit
signals/scores into calibrated keys · CHF-R2 deferral + QS-U2 cortex 3/week ·
horizon law: verdicts only at registered horizons (timing metrics register
±31td window, 20-25d reversion-capture; defensives fwd126 where chartered).

## §3. Wave map

**W2 — Personality Timing Codex (licensed; build first).** Per-name measured-
structure store `data/personality_timing/codex.parquet` + synapse registration
(display tier): reversion-by-scale ρ-ladder (the W1b measurement), derived rung
PER RULER (charter §8: rungs are ruler-dependent — store both, label both),
stationarity score (split-era parameter drift), structural flags ("washouts-
continue" ~6% class from W1; "reset-confirmer lateness" percentile; fwd126-class
for slow defensives), vol/trend/beta, PIT personality label join. World-state
lobe (honest-null, darkpool discipline). This is the pattern-of-each-stock's-
bottoms deliverable in its lawful form: measured, ruler-labeled, displayed.

**W3 — Prophet tailored-gate shadow (licensed; candidate pinned §8: Stoch-RSI at
the structure-derived rung).** Shadow lane logs uniform-gate/tailored-gate
disagreements both directions; dual-ruler grading; quarterly miss-rate readout
appended to the charter. First readout ≈ 3 months post-ship. Promotion of any
gate change = own prereg + ruling (contamination row absolute).

*[W3 SHIPPED 2026-07-25 — engine/personality_gate_shadow.py, wired into the
US-nightly lane (engine/run.py, after mag7_washout_shadow). Uniform gate =
Stoch-RSI<20 cross on the fixed 2W rung (the incumbent mag7_washout / index_momentum
construction); tailored gate = the same S-family cross at each name's codex
`rung_derived` (R-W1T-2). Forward ledger data/personality_timing/gate_shadow.jsonl
+ display state gate_shadow_state.json; both nightly-gated, dual-ruler grading
(fwd63 + timing mae63/prox/td_to_trough) deferred to 63td maturity. First quarterly
miss-rate readout due ≈ 2026-10-25 (appended to this §W3 block). No live gating
changed.]*

**W-LAB — lab audit + personality upgrade (operator-flagged surfaces).**
Audit BEFORE changing (append verdicts to §5):
  (a) `tech_lab.html#combos` + `engine/tech_confluence.py` (+ build_tech_lab*,
      build_confluence_screener): what ruler do combo stats use (almost
      certainly fwd-return — pre-§7); is the search disclosure ("N combos
      searched") surfaced in UI; do combo picks respect same-direction/jaccard
      guards; does anything violate R-W1T-3 copy law ("bottom" language)?
  (b) `us_stocks_lab.html`: confirm the one-size pooling critique — which stats
      pool all names; whether per-class (codex label) cuts are feasible from
      existing payloads.
Upgrades (display-tier, ship freely post-audit): re-grade lab stats under the
timing ruler alongside the legacy ruler (dual columns, labeled); add codex-
class cuts to us_stocks_lab (per-personality tables instead of pooled-only);
combos tab gains a TIMING-ruler column + a "reset-confirmer vs bottom-caller"
badge per combo. The labs become the public face of the codex.

**W-SIG — the multi-suite expansion (new indicator families).** Families enter
ONLY as: (i) MEASUREMENTS into the codex (new structural axes), then (ii) ONE
pre-registered mechanism hypothesis each, timing-ruler graded, trial-ledger
family, small pinned grid. Seed candidates for the brainstorm (§4) to refine —
each needs a mechanism sentence before it earns a prereg: volume-capitulation
class (exhaustion volume at reset); volatility-structure class (vol-of-vol
collapse after flush); range-structure class (undercut-and-reclaim of a prior
low); breadth-confirm class (member/sector participation at the turn — NOT
rs-dispersion, that's a DON'T-TEST); options-tape class (existing flow stores;
positioning-fusion ban applies to any scoring). Per family: does it improve
PROXIMITY/MAE over the incumbent Stoch-RSI@derived-rung, and does it fire
EARLIER without importing the 2022 early-fire failure class?

**W-CONF — confluence, done honestly.** Question pinned: does requiring
2-of-N families (fired within a ±2-bar window at each name's derived rung)
improve MAE/proximity and shrink the FAIL tail vs the single best family — at
the cost of how many missed resets? Budget: pairs among ≤4 validated families
ONLY (≤6 pairs + ≤4 triples, pre-pinned; reuse tech_confluence machinery with
timing-ruler grading). Boundary rows cited: rotation×cycle DON'T-TEST, PSF
timing-blend null (no blended rankings — confluence is a GATE condition, never
a score). This is the operator's "confluence improves reliability" test, run
once, properly, instead of mined forever.

**W-FOUNDRY — automated creative brainstorming of new signals (the system).**
Composes existing organs; generates QUESTIONS, never signals:
  1. **Seed miner** (nightly/weekly job): scans codex + shadow ledgers + lab
     stats for anomaly seeds — names/classes whose resets evade ALL current
     families (residual misses), personality cells with no validated tool,
     recurring near-miss patterns in the W3 disagreement ledger.
  2. **Draft generator** (LLM, proposal tier per CHF Article 1): turns seeds
     into STRUCTURED PREREG DRAFTS — {mechanism sentence, construction,
     ruler = timing set, falsifier, grid budget, kill criteria} — written to
     `research/foundry/DRAFT_*.md`. LLM never touches calibrated keys; the
     signal_foundry_blocklist (compiled from DNR) pre-filters drafts that
     re-propose killed constructions.
  3. **Chokepoint**: drafts flow through the EXISTING metabolism/cortex intake
     (≤3/week, QS-U2) for Fable/operator adjudication — approved drafts become
     numbered preregs; rejected drafts append one line to a foundry ledger with
     the rejection reason (the ledger itself becomes brainstorm memory).
  4. **Harness**: approved preregs run on the standard W-SIG rails (trial
     ledger, timing ruler, era split, cluster bootstrap); results append to
     codex/DNR. NO auto-execution of unapproved drafts — the deferral clock
     (CHF-R2, 2026-10-15) governs any future automation of step 3.
  Creative quality lever: the draft generator runs as a judge-panel brainstorm
  (multi-lens: microstructure, flows, vol-structure, behavioral, cross-asset,
  seasonality) — distinct lenses beat one prompt; taste-level sessions may use
  the orchestrator/FABLE-WHY gate per routing law.

**Far waves (explicitly NOT licensed now):** W4 per-class gate profiles (needs
a separating class variable); per-name SIZING (authority tier — full gauntlet +
ruling; nothing in this program touches sizing until then).

## §4. Brainstorm mandate (first session task, before any building)

Run a structured creative brainstorm to seed W-SIG families + foundry lenses:
multi-lens judge panel (workflow pattern; lenses above + "what does the tape
do at REAL resets that no oscillator sees"), each lens returns candidate
mechanisms with a falsifier each; panel dedups against DNR/blocklist + the
§3 seed list; output = ranked family shortlist (≤4 to carry into W-SIG) +
foundry lens config. Routing: panel = opus reviewers; synthesis = main loop.

*[EXECUTED 2026-07-25 — 7-lens opus panel, 33 candidates, all 12y-testable.
Output of record: `research/PSS_WSIG_SHORTLIST_BY_FABLE.md` (W-SIG slate =
F1 down-volume envelope decay · F2 overnight-vs-intraday decomposition flip ·
F3 idiosyncratic residual reset · F4 downside-vol asymmetry flip · shared
C32 decline-deceleration terminality gate as the pre-registered conditioner
in all four preregs) + `research/foundry/LENSES.md` (foundry lens config).]*

## §5. W-LAB audit verdicts — (append here before changing the labs)

*Audit executed 2026-07-25 (sonnet read-only census, file:line-cited; adjudicated
by the main loop). Findings and rulings:*

**V-LAB-1 (combos ruler — confirmed pre-§7).** `engine/tech_confluence.py`
grades combos on forward returns at the {10,21,42,63}td ladder; ranking =
Wilson lower bound of test-half month-collapsed fwd21 win rate minus the
random baseline (split 2018-01-01, test-half-only ranking). MAE exists as a
secondary field (`mfe_mae_med`); proximity-to-low does not exist. RULING:
upgrade adds a TIMING-ruler column pair per combo — median MAE63 and
within-5%-of-±31td-low rate, each shown NEXT TO the legacy fwd21 column and
labeled by ruler; legacy column is retained (dual-ruler display, no silent
ruler swap). Display tier; no gauntlet claim.

**V-LAB-2 (search disclosure — PASS, no action).** "N combos · searched X
candidates" footer + receipt tooltip (`search_caveat`) are wired
(`tech_lab.html.j2:1406-1435`, `build_tech_confluence.py:532-557`). Fail-soft
'—' when artifact stale is acceptable.

**V-LAB-3 (guards — PASS, no action).** Same-direction mining
(`tech_confluence.py:563`) and pooled-Jaccard forbidden pairs at 0.90
(`:531-542`) are active; forbidden-pair count is recorded in the artifact.

**V-LAB-4 (copy law — in-lane PASS; badge ordered).** No bottom-caller copy
in the tech_lab/confluence or us_stocks_lab lanes; chart-formation taxonomy
names ("double bottom", "pivot low forms") are defensible as formation names
and stay. RULING: per masterplan, each combo gains an explicit
"reset-confirmer / early-window" badge derived from its measured median
td_to_trough sign convention under the timing ruler — the framing becomes
explicit instead of implicit. (Out-of-lane "buy the bottom" strings exist in
`vector_allocation.html.j2:280` and `report_second_act.html.j2:1521` — out of
W-LAB scope, logged for a separate copy sweep.)

**V-LAB-5 (us_stocks_lab pooling — confirmed; cuts ordered).** All Pick Lab
scoreboard stats are one-size pools over each book's fires
(`engine/pick_lab/book.py:259-268` and NAV/lift machinery). RULING: add
codex-class cuts as display-tier stratified tables — by `rung_derived`
(populated for all 1,631 codex names; join fires on ticker) and by
`archetype` (populated for the 172-name labeled subset; NaN names go to an
explicit "unlabeled" row, never silently dropped). Per-class rows carry their
own n; rows under a minimum n print the n and suppress the rate (plain-word
null disclosure), never a rate on single-digit fires.

**V-LAB-6 (codex label coverage — clarified, not a defect).**
`archetype`/`chart_primary` are populated only for the personality-labeled
subset (172/202 of 1,631) by construction of the PIT label join; the census's
NaN sample rows were unlabeled names. Rung cuts are universe-wide today;
archetype cuts are subset-scoped until labels widen.

**Runtime budget.** Combos re-grade adds MAE/prox computation to the
`tech_lab_offrender` job (40-min timeout, ~8-min current miner) — off the
render path, headroom confirmed. Pick Lab cuts are joins on existing
payloads — no new heavy computation.

## §6. Routing & budget

Opus builds (builder agent), opus reviews; sonnet only for non-code census
sweeps; Fable main loop plans/adjudicates/merges; fable workflow stages only
with FABLE-WHY. Compute: codex + lab re-grades are panel-scan class (~minutes);
foundry jobs are nightly-lane additions (respect render budget — heavy compute
off the render path, artifacts to R2 if large).
