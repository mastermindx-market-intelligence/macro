# TOPA OOT Prereg — Untouched Out-of-Time Replication (post-2026-07-02 tape)

**Frozen:** 2026-09-01, committed on the R0b carrier (PR #6723 lineage) BEFORE any OOT
confirmatory effect estimate exists anywhere in the repository; the §8 log is append-only
(corrections are new entries, never edits). **Family:** `top_anatomy_oot`.
**Authority:** `DEC:TOPA-OOT-COMPLETION-ARCHITECTURE` · `research/top_anatomy/TOPA_OOT_COMPLETION_MASTERPLAN_2026-08-28.md`
§5 · operation `topa-r0b-winner-health-restore-20260901-coo-001` (Chairman-override
continuation of `TOPA-COMPLETION-20260828`, carrier `C0BSBM78V1N/1787897388.518689`).
**Display tier throughout; AVOID-not-SHORT permanently** (`DNR:KILL-DIRECTIONAL-SHORTING`);
no off-horizon verdict (`DNR:KILL-OFFHORIZON-VERDICTS`); no Prophet population contact
(`DNR:KILL-PROPHET-POP-MERGE`). This freeze was adversarially reviewed (opus reviewer)
BEFORE commit; all findings are incorporated below and receipted in §8.

## §0 The question and the inherited state (immutable inputs, not re-litigated)

Does the frozen-tape scientific state replicate on the first genuinely untouched tape?
The inherited state this prereg tests — and may lawfully revise ONLY through the machinery
below — is exactly:

- **B3/F3/B2-cluster ("ore body") claim CLOSED** on the 2026-07-02 tape at the registered
  grain (AM-v2: B3 `P1-NOT-SUPPORTED` on the only floor-clearing disjoint panel under a
  VALID instrument). CLOSED means *not established* — failure to confirm, never
  demonstrated absence (AM-v2 §7 G0.5 correction (b) is binding language law here).
- **B2 is panel-split, not "surviving" in general:** it survives anchor+age matching on
  readable ATRZ at materially reduced magnitude — AM2 ATRZ `+1.499 [+0.473, +2.389]
  q=0.011`, ratio 0.38× of W2's unmatched ATRZ *point* `+3.92` (interval `[+2.80,+4.79]`);
  but AM2 B2 on PRIMARY was `NOT-SUPPORTED` (`+0.127 [−0.753, +0.955] q=0.455`), and
  phase-1's era-separation debt on B2 remains UNDISCHARGED (AM-v2 §7 G0.5 (d): the
  discovery cohort is unreadable-at-this-power; the debt transfers to panels where B2
  stands). R63 was UNDERPOWERED (31 episodes).
- **F1 is `UNDECIDED-HERE`** with readability left open (AM-v2 §7 G0.5 (c)); the AGEFREE
  construction has one-directional power and its adverse-bias direction was a *measured*
  property of that cohort, not a law of the instrument.
- **F3 is a matching variable**, dissolved into the instrument; never graded.
- **Seven of nine frozen AM-v2 registered cells were MATCH-STARVED** (AM-v2 §7 (6)) —
  directly relevant to whether the OOT floors are reachable; starvation is the
  instrument's printed cost, never a redesign license.

W2, Phase-1 and AM-v2 are immutable historical evidence (masterplan §1, §4.8): nothing
here reruns them, and no frozen-tape number is recomputed as new evidence.

## §1 OOT boundary, cohorts, and pools (the only new construction content)

**First untouched session: 2026-07-03** (masterplan §5.1). All frozen-tape research used
sessions ≤ 2026-07-02; every post-boundary bar is untouched by any Top Anatomy
construction, threshold choice, or adjudication to date.

- **OOT-STRICT (the DECIDING cohort).** A case episode belongs to OOT-STRICT iff
  (a) its §4.4 peak date satisfies **peak − 21 sessions ≥ 2026-07-03** — so all three
  registered snapshot offsets `{21,10,5}` fall inside the OOT window regardless of the
  episode's EXT-day pattern — and (b) it has **≥1 existing snapshot day** (a snapshot
  exists only if that day is an EXT day, phase-0 §4.5). Episodes peaking inside the first
  21 OOT sessions are BRIDGE by construction. This closes the snapshot-existence
  selection door: cohort membership never depends on *which* snapshots happen to exist,
  only on the peak's position, and zero-snapshot episodes are never counted as
  candidates.
- **OOT-BRIDGE (supporting, contamination-caveated, never deciding, never graded).**
  Case episodes with §4.4 peak ≥ 2026-07-03 that fail the STRICT rule (peak inside the
  first 21 OOT sessions, or any snapshot day ≤ 2026-07-02). Printed beside STRICT with
  its own counts; the §6 matrix is **never** applied to OOT-BRIDGE.
- **Control candidate pool (declared moved variable, resolving an inherited boundary
  interaction).** AM-v2 §1 draws ONE seeded day per continued control episode "from ALL
  its continued-EXT days". Out of time, the pool is **restricted BEFORE the episode-first
  draw**: only continued-EXT days ≥ 2026-07-03 with resolved race labels are candidates;
  a control episode with zero post-boundary resolved continued-EXT days is excluded (its
  exclusion counted). Restrict-then-draw changes the draw distribution vs the frozen
  construction and is therefore declared as a moved variable in §2.
- **Cohort independence.** STRICT and BRIDGE are fully independent runs: independent bin
  edges, age-tercile strata, caliper pools, and seeded draws. No BRIDGE observation —
  case, control, or edge-defining candidate — enters any STRICT quantity.
- **History law.** PIT feature windows and eligibility history (≥260 prior sessions,
  MA/ATR/r_k windows) may reach into pre-boundary bars: features are *inputs measured at
  the observation day*. What must be post-boundary is the observation day itself and the
  outcome resolution (naturally satisfied: peaks ≥ 2026-07-03 resolve strictly after).
- **Panel disjointness window.** R63/ATRZ disjointness inherits W2 §4's zero-shared-EXT-day
  rule evaluated over the **whole tape** (identity-segment, session sets over the full
  series), matching W2's own construction — so panel membership is a property of the
  episode, not a boundary artifact. Declared explicitly because a straddling episode
  admits two readings; this is the resolution.

## §2 Frozen inherited construction (by exact reference; re-derivation forbidden)

Everything below inherits verbatim; this document intentionally does not restate formulas
— the referenced frozen texts govern wherever a compressed restatement here differs:

- **Tape/universe/eligibility/identity:** `data/massive_stock_day` (sole market-history
  store; separately owned), phase-0 prereg §3 verbatim — split repair with OHLCV factor
  carry, raw-level eligibility (close ≥ $3, median 21d $vol ≥ $2M, ≥260 prior sessions),
  split-day exclusion, identity segments at >60-session gaps AND ≥3.0× residual
  single-day up-jumps (track W), NASDAQ test-symbol disclosure, instrument/dead-name
  census printed.
- **Extension definitions:** PRIMARY `r126 ≥ +0.50` + near-high (phase-0 §4.1); R63
  `r63 ≥ +0.35` + near-high; ATRZ `(c−MA200)/ATR63 ≥ 6` + near-high (W2 §1).
- **Episodes:** phase-0 §4.2 (contiguous EXT days, gaps ≤ 21 merged; case episodes need
  ≥5 EXT days; micro-spell days stay in the control pool).
- **Day-level races:** phase-0 §4.3 (−20% from post-d running peak vs +15% from entry,
  first-to-fire, TOPPED wins the unreachable tie, ≤250 sessions, else CENSORED).
- **Episode peak / terminal top (sealing):** phase-0 §4.4 (peak = argmax close over
  [start, end+63]; TOPPED iff min close over [peak, peak+126] ≤ 0.80×close(peak) with no
  intervening close above the peak **before that −20% print**; else SURVIVED; censoring
  stated at the data edge).
- **Feature library:** phase-0 §4.6, all 36 PIT features, formulas frozen.
- **Matcher / age construction:** AM-v2 §1 verbatim — AM2 (frozen W4 key + episode-age
  tercile stratum + episode-first control draw + hard anchor caliper ≤2 with the
  registered ≤1 escalation and the SIGNED validity rule that GOVERNS) and AM2-AGEFREE.
  All AM-v2 §7 plumbing operationalizations carry (both-arms-invalid mirror, per-arm
  era/E4, arm-restricted validity receipts, era-cell leg list, day-weighted sensitivity
  scope), and AM-v2 §5's non-binding sensitivities are inherited by name: caliper ≤4,
  NN cap 8, and the AM2 day-weighted candidate-sampling arm (which carried the B3
  mechanism reconciliation AM-v2 §7 G0.5 PASS 2 made binding — it informs mechanism
  language only, never a grade).
- **Estimator/aggregation/inference:** phase-0 §4.5 — matched-set Δ, episode-first
  collapse, episode-peak calendar-month block bootstrap **B = 2000**; ticker-cluster
  bootstrap printed as report-only robustness beside every registered cell (phase-0 §4.5
  convention; masterplan §5.5).
- **Bin edges:** population-relative, recut within each cohort × panel's own candidate
  pool (the inherited W2/phase-1/AM-v2 convention), subject to the §5 pinning rule —
  edges are cut ONCE on the pinned pool, never on a moving read-date pool.
- **Era machinery:** phase-1 §5 recipe (MANDATORY; three contiguous calendar blocks of
  as-equal peak-month counts, per panel, edges printed; per-era point+CI beside every
  registered cell; latest-era wrong-sign on a supported cell ⇒ `-ERA-CAVEAT`; B2 fade
  fence printed on every B2 cell) — over the OOT cohort's own peak months only.
- **Instrument freeze (inherited from AM-v2 §2):** `engine/top_anatomy.py` byte-frozen at
  the freeze-time main for every OOT result commit (`git diff origin/main -- engine/`
  empty at each); harness plumbing may extend `scripts/research_top_anatomy_phase0.py`
  with an `--oot` construction flag and the §1 boundary/pool filters ONLY, committed
  before any full-panel result; cache identity stamps extended with the OOT construction
  keys + boundary + seed (present-and-equal hard-check, AM-v2 `_p1_assert_identity`
  procedure); a leaf-by-leaf determinism re-run of one registered cell precedes
  adjudication (AM-v2 §7 procedure — clock fields only may differ).
- **Compute wall (inherited verbatim from W2 §6 / AM-v2 §6):** research wall 12 h off the
  render path; if exceeded, the lawful fallbacks are coarser parallelism or a smaller
  sensitivity set — **never a silent cap, never a post-hoc subsample. No new subsampling
  rule may be introduced after this freeze.**

**Moved variables vs AM-v2 (enumerated exhaustively; declared before any number):**
1. the tape window (§1 — the point of the study);
2. the seed: **20260901** (fresh draws for OOT; every stochastic step uses it);
3. the control candidate pool: restrict-then-draw at the boundary (§1);
4. store vintage/correction behavior: AM-v2 read the pinned 2026-07-02 vintage
   (`--allow-stale`); OOT reads a current, restatable store whose pre-boundary bars feed
   PIT features — the §5 receipt pins the exact store state (manifest hash + last
   session) per look, and any owner-side restatement between looks is disclosed in §8;
5. universe drift: the eligible universe grows with the tape; candidate pools are
   whatever the pinned store yields under the frozen eligibility rules;
6. the split-repair surface: `_COMMON_SPLITS` covers reverse splits only to 1:10
   (phase-0 §6), so new corporate actions can fabricate extensions — the inherited ≥3.0×
   residual up-jump identity break governs, and the §5 receipt prints the affected-segment
   census for the OOT window.
Nothing else moves. Any report contrast against frozen-tape numbers carries items 1–3 as
moved variables explicitly.

## §3 Registered confirmatory cells (declared before any OOT number exists)

Panels: **PRIMARY / R63-DISJOINT / ATRZ-DISJOINT**, OOT-STRICT cohort only.

| Leg | Direction | Construction | The question it answers |
|---|---|---|---|
| `B2_rsi14` | + | AM2 | replication: does the frozen-tape effect appear out of time? |
| `B3_rsi14_chg10` | + | AM2 | cluster reopen-or-retain: the only lawful reopening evidence for the CLOSED claim |
| `F1_episode_age` | − | AM2-AGEFREE | status: one-directional power inherited (SUPPORTED meaningful; non-support UNINFORMATIVE-BY-DESIGN) |

- **Cells:** AM2 {B2, B3} × 3 panels + AGEFREE {F1} × 3 panels = **9 registered cells**,
  all printed with delta + CI + q regardless of outcome. F3 = validity diagnostic, never
  graded; F1 under AM2 = stratification diagnostic, printed ungraded.
- **Multiplicity:** BH-FDR **q ≤ 0.10 within each (panel × construction) family** — of
  exactly 2 (AM2) and exactly 1 (AGEFREE) — one-sided p in the declared direction off the
  registered bootstrap draws. Verbatim inheritance; no new families.
- **Prior-effect comparators (transcribed in full, per panel):**

  | Panel | Frozen AM2 B2 | W2 B2 interval | Phase-0 B2 prior |
  |---|---|---|---|
  | PRIMARY | `+0.127 [−0.753, +0.955]` (NOT-SUPPORTED) | — (W2 had no primary arm) | `+1.292 [+0.441, +2.140]` |
  | R63 | `+3.351 [+0.130, +5.517]` (UNDERPOWERED, 31 ep) | `[+2.56, +5.25]` | — |
  | ATRZ | `+1.499 [+0.473, +2.389]` (below-but-supported) | `[+2.80, +4.79]` | — |

  Per readable panel, state whether the OOT AM2 B2 point falls inside the frozen AM2
  interval and (where one exists) inside W2's interval; on PRIMARY the phase-0 estimate
  is the named prior.
- **Registered ratio (resolving an inherited ambiguity, declared as a resolution):** the
  ONLY registered ratio is `OOT-AM2 point / frozen-AM2 point` on the same panel, printed
  un-CI'd with the §2 moved variables named beside it. No other ratio is registered;
  matched-vs-unmatched and any other cross-design ratio remain banned (AM-v2 §7 G0.5
  (a)).
- **Grades:** `OOT-REPLICATED` / `OOT-NOT-CONFIRMED` / `OOT-UNDERPOWERED`, with
  `MATCH-STARVED` (AM-v2 §5) and `UNDERPOWERED-BY-CONSTRUCTION-FAILURE` (AM-v2 §1)
  carried beside grades exactly as inherited.
- **Exploratory:** the full 36-feature two-sided tables run for both constructions and
  all panels, BH within family, capped EXPLORATORY-DISCOVERY, **read from FULL tables
  only (never `separating` survivor lists)**; pre-named reads carry forward
  (`E3f_rs_peak_lag`, `F2_drawdown_in_episode` — do the anchor-family wrong-sign shadows
  reappear out of time?). Exploratory findings can nominate a fresh prereg, never harden.

## §4 Floors and maturity (tighten-only law)

Inherited floors per registered cell (masterplan §5.3 — tightened here where noted,
never loosened): **≥12 distinct episode-peak months** · **≥100 matched topped episodes**
· match rate printed, <50% adds MATCH-STARVED · AM-v2 signed validity rule governs each
(construction × panel).

**Sealing/maturity law (inherited timing, made explicit for OOT):**
- a case episode enters the candidate set only when §4.4 SEALS it (TOPPED via the −20%
  print with no intervening higher close, or SURVIVED via the full [peak, peak+126]
  window observed). Unsealed = IMMATURE, counted, never graded;
- a drawn control day's §4.3 race must have resolved, else it is CENSORED, counted,
  never imputed; races may take up to 250 sessions — the ruler is never shortened.

**Truncation/speed-selection bias (declared pre-results, with a tighten-only floor):**
on a tape truncated at read time, sealed cases are enriched in *fast* toppers and
resolved controls in *fast* continuers, concentrated in the most recent peak months.
Declared direction per registered leg: for B2 and B3 (+; heat legs) and for F1 (−; young
age) this selection plausibly biases the case−control delta TOWARD the registered
direction — i.e. **anti-conservative for all three registered cells** — so completeness
is load-bearing, not cosmetic. New tighten-only floor: within EVERY era block of a graded
cell, ≥80% of candidate case episodes must be sealed AND ≥80% of drawn control days'
races resolved; a cell failing this in any block is `OOT-UNDERPOWERED`. The §5 receipt
prints the per-era sealing/resolution census.

**Era blocks (MANDATORY, no activation condition):** era blocks always run on any cell
clearing the floors above, per the phase-1 §5 recipe — with ≥12 distinct peak months a
three-block split always exists. A cell below the floors is `OOT-UNDERPOWERED` and
prints no era cells. The `-ERA-CAVEAT` modifier can therefore never be silently deleted.

**Earliest-possible timing, stated so nobody cycles the gate early:** sealing arithmetic
alone permits the first SURVIVED seals around 2027-01 (126 sessions from 2026-07-03),
but the **binding** constraint on any graded cell is the ≥12 distinct-peak-month floor —
peaks spanning 2026-07 through at least 2027-06, plus their sealing windows — so no
registered cell can clear before roughly **2027-07**. A receipt printing
`final_verdict_eligible: false` throughout 2026 is the designed behavior.

## §5 Deterministic maturity receipt, the single-look rule, and the computation gate

Before ANY final inference, a deterministic receipt is published containing exactly:

- OOT start (2026-07-03) and end observed (the pinned store's last session);
- the pinned store state: `_manifest.json` content hash + last session date + shard
  count (the pin that makes "the pool" a fixed object rather than a read-date variable);
- candidate case episodes per (cohort × panel): STRICT and BRIDGE separately;
- **episode-level** counts per (cohort × panel): {TOPPED, SURVIVED, IMMATURE-UNSEALED} —
  unit: distinct sealed/unsealed case episodes;
- **day-level** counts per (cohort × panel): {TOPPED, CONTINUED, CENSORED} — unit: race
  labels on candidate observation days (phase-1 §2 labeled-unit discipline; the two
  blocks are never merged);
- matched episodes under the frozen matcher: the matcher runs whenever candidate case
  episodes ≥ 1 (seed 20260901); below that `matcher_run: false` with
  `reason: zero_candidates` — no other skip threshold exists;
- distinct episode-peak months per (cohort × panel);
- match starvation (rate vs the <50% line);
- per-era sealing/resolution completeness census (the §4 ≥80% floor's inputs);
- outcome-horizon maturity per the §4 sealing/resolution law;
- construction diagnostic readiness (whether the AM-v2 validity rule is evaluable);
- delisting/identity-break census inside the OOT window (who is missing, printed),
  including the split-repair affected-segment census (§2 item 6);
- **`final_verdict_eligible` PER CELL**, keyed `(cohort × panel × construction × leg)`,
  each carrying its floor rows (≥12 months / ≥100 matched / completeness / validity
  precondition) and a `reasons` list naming every unmet floor. There is no top-level
  eligibility scalar; OOT-BRIDGE cells are permanently `eligible: false` with reason
  `bridge_never_graded`.

The receipt reads COUNTS only — no feature deltas, no effect estimates, no grades. While
any registered cell is ineligible the exact scientific state is
**`OOT_ACCRUING_NO_VERDICT`** and no model, Sol, Fable, worker, or Chairman-pressure
reading may upgrade it.

**Single-look rule (closes the optional-stopping door):** the FIRST receipt printing
`final_verdict_eligible: true` for a cell **PINS** that cell's store state (manifest
hash + last session), candidate pool, bin edges, strata, caliper pools, and seeded
draws. §6 is computed ONCE per cell on that pinned pool, committed, and never recomputed.
Any later evaluation of the same leg is a NEW preregistration with its own family and an
explicit look-count disclosure. Every receipt emission is logged in §8 with its pinned
store state; receipts are re-emittable while ineligible (they only count), but an
eligible receipt is terminal for its cell's pool definition.

**Computation gate (closes the pre-gate door):** no registered-cell delta, CI, p/q,
grade, or era cell over any post-2026-07-02 observation may be computed, stored,
printed, or read before that cell's receipt prints eligible; the harness hard-gates on
the committed receipt artifact. An accidental pre-gate computation is disclosed in §8 as
a look and consumes that cell's single look.

## §6 Decision matrix (pre-written; wording binds; STRICT cohort and panels only)

Executed once per cell under the §5 single-look rule, only when that cell's receipt row
prints `final_verdict_eligible: true`. "Panel" below always means PRIMARY / R63 / ATRZ;
the matrix is never applied to OOT-BRIDGE. Report separately by panel; no pooled
cross-panel or cross-tier scalar; no "overall Top Anatomy score".

- **B2, per readable panel:**
  - `OOT-REPLICATED` AND point inside the frozen AM2 interval → "B2 replicates out of
    time at a magnitude compatible with the frozen matched estimate."
  - `OOT-REPLICATED` AND point below the frozen interval → "B2 replicates directionally
    at reduced magnitude" + the registered ratio (§3).
  - `OOT-REPLICATED` AND point above the frozen interval → "B2 replicates directionally
    at greater magnitude than the frozen matched estimate" + the registered ratio; no
    strengthening language beyond that sentence (a bigger point is not a better claim).
  - `OOT-NOT-CONFIRMED` on a floor-clearing panel → "B2 failed to confirm out of time on
    this panel" — failure to confirm, with the CI printed; NOT evidence of absence unless
    the CI excludes the frozen point, in which case say exactly that. **On a
    floor-clearing DISJOINT panel this additionally fires the dormant phase-1 §7.2
    surface-copy correction obligation (AM-v2 §3; §7 G0.5 (d)): W2b tier copy and any W1
    surface leaning on B2 are corrected in the same wave.**
  - `OOT-UNDERPOWERED` → no directional sentence; floors did the honest work.
- **B3 (cluster reopen/retain):**
  - `OOT-REPLICATED` on a floor-clearing disjoint panel under a VALID instrument → "the
    CLOSED cluster claim REOPENS as live out-of-time evidence" — an evidence-state
    change only; any authority change still requires the separate promotion gauntlet
    (masterplan §10).
  - anything else → "the cluster claim remains CLOSED; out-of-time evidence did not
    reopen it" (retain-closed; never upgraded to "proven absent").
- **F1 (AGEFREE):** the OOT case-vs-control age receipt (medians/quartiles) prints
  beside every AGEFREE cell (AM-v2 §1 convention). If the receipt shows control ages
  skewing YOUNG (the adverse direction, as measured on the frozen cohort):
  `OOT-REPLICATED` → "F1 earned its first anchor-clean support, out of time, against
  measured adverse bias"; non-support → UNINFORMATIVE-BY-DESIGN, verbatim. If the OOT
  receipt shows the bias direction NEUTRAL or REVERSED, the one-directional-power
  framing does not apply: grade mechanically and state the measured age gap beside the
  grade with no adversity claim. F1's standing state remains `UNDECIDED-HERE` unless a
  supported read lands under measured-adverse or neutral conditions.
- **R63 panel:** report match starvation/power first; a starved R63 decides nothing in
  either direction and says so.
- **Construction failure** (both caliper arms invalid on a panel): every cell on that
  panel carries `UNDERPOWERED-BY-CONSTRUCTION-FAILURE`; no directional conclusion.
- **Era instability:** latest-era wrong-sign on a supported cell ⇒ the inherited
  `-ERA-CAVEAT` suffix and its sentence beside the grade.

## §7 Separation of descriptive/operational reads (computational test, not a label)

Per masterplan §5.2, the roster/exemplar census and the Winner Health product
restoration are descriptive/operational and may run while this prereg stands. The fence
is computational, not categorical: **no computation forming a case-vs-control contrast,
matched delta, confidence interval, p/q value, or directional sign test over
post-2026-07-02 observations may run outside §6, regardless of the surface it renders
on.** Analog-library reads print per-name history only (this name's own analogs from the
frozen library), never a post-boundary cohort difference. The nightly `winner_health.v2`
artifact and the §5 receipt are counting/display reads under this test. The first
confirmatory OOT effect computation in this repository must postdate this document's
freeze commit and be receipted in §8.

## §7b Report contract (inherited house form)

Artifacts: `data/research/top_anatomy_oot_{panel}_{am2,am2_agefree}_summary.json` +
`reports/top-anatomy-oot.md`. The report LEADS with the §1/AM-v2 validity diagnostics,
then labeled-unit censuses (phase-1 §2 three-n discipline: distinct episodes / case
snapshots / control days), then the registered table with grades + carries + era
receipts + the per-era completeness census, then exploratory full-table reads, then
adjudication strictly through the §6 branches. The coverage gate applies: lead with what
the verdict means for the motivating live exemplars and the current regime; name who is
missing from the panels (delistings, identity breaks, starved cells). **G0.5 adversarial
review (opus reviewer) is MANDATORY before presenting; its corrections are binding and
enter §8 append-only.**

## §8 Append-only execution log

- 2026-09-01 — Draft written; pre-freeze adversarial review (opus reviewer) commissioned
  and returned **7 blockers / 14 must-fix / 8 nits** (all transcribed numbers verified
  correct; defects were construction-level). ALL findings incorporated before freeze:
  single-look rule + per-cell pinned pools (§5), pre-gate computation ban (§5), STRICT
  boundary tightened to peak−21 ≥ 2026-07-03 with ≥1 existing snapshot (§1),
  restrict-then-draw control pool declared as moved variable (§1/§2), exhaustive
  moved-variable enumeration + instrument byte-freeze + cache identity + determinism
  re-run (§2), compute wall + no-post-freeze-subsampling imported verbatim (§2), full
  comparator table incl. B2-primary NOT-SUPPORTED and the undischarged era-separation
  debt (§0/§3), registered-ratio definition as an explicit resolution of an inherited
  ambiguity (§3), truncation-bias direction declared anti-conservative for all three
  registered legs + per-era ≥80% sealing/resolution completeness floor (§4, tighten-only),
  era blocks unconditional (§4), two labeled-unit receipt blocks + matcher-runs-at-≥1
  rule + per-cell eligibility keying with BRIDGE permanently ineligible (§5), B2
  outside-above branch + dormant §7.2 revival trigger + F1 measured-adversity
  conditioning (§6), computational carve-out test (§7), report contract + mandatory G0.5
  (§7b), disjointness window pinned to whole-tape (§1), earliest-eligible ~2027-07
  stated with the ≥12-month span as binding (§4), and the nit corrections (0.38× is vs
  W2's unmatched point +3.92; MATCH-STARVED cites AM-v2 §5; ticker-cluster robustness
  cites phase-0 §4.5/masterplan §5.5; §4.4 "before that −20% print" restored; F1 status
  word `UNDECIDED-HERE`; freeze verification method = `git ls-files` census showing no
  `top_anatomy_oot` artifact in the tracked tree, this worktree being sparse).
  No registered floor was loosened; five protections were tightened. Seed 20260901.
  FROZEN at this commit, before any OOT confirmatory effect estimate exists in the
  repository. Authority: Chairman override of 2026-09-01 (carrier ts
  `1788307821.689309`) under program `TOPA-COMPLETION-20260828`.
- 2026-09-01 — RECEIPT-EMITTER OPERATIONALIZATIONS (append-only; declared with the
  emitter commit, before any confirmatory number; none moves a registered quantity —
  each records a §5 implementation choice the frozen text left open):
  (a) **Sealing read** (`scripts/research_top_anatomy_oot_receipt.py::_seal_state`): §4's
  sealing law is read literally off `episode_peaks`'s own flags — a TOPPED label seals
  only when the peak-search window itself is final (`peak_window_truncated` false; an
  open window means a later higher close could still move the peak and void the print);
  a SURVIVED label whose [peak, peak+126] window ran off the data edge is
  CENSORED-AT-DATA-EDGE, never evidence of survival.
  (b) **Dark-segment census definition**: "who is missing" = identity segments that
  traded inside the OOT window but stopped printing before the store's last session
  (dark), plus identity-break segments first appearing inside the window — a receipt-side
  descriptive definition, not an engine function.
  (c) **Emitter posture**: the current emitter can never print `eligible: true` — its
  cell rows always carry `completeness_floor_not_evaluable_no_era_blocks` and
  `validity_precondition_not_evaluable_matcher_not_run` because era-block construction
  and real AM2 matching are deliberately not implemented in a counting read. Activating
  them is harness work for the wave that approaches the floors; activation is itself a
  §8-entry event and changes no registered quantity.
  (d) **Monthly completeness basis**: the per-peak-month sealed/unsealed and day-level
  resolution census is the declared input basis for the §4 ≥80% per-era completeness
  floor once era blocks exist.
- 2026-09-01 — FIRST RECEIPT EMITTED (UTC 2026-09-02 artifact date):
  `data/research/top_anatomy_oot_receipt_2026-09-02.json` (+ .md). State:
  **`OOT_ACCRUING_NO_VERDICT`, 0/18 cells eligible.** Pinned store: last session
  2026-08-28, 20,934 shards, manifest_sha256 null (`manifest_not_present_in_checkout` —
  local sparse checkout; production emits the hash). Candidates accruing: STRICT
  221 primary / 238 r63 / 854 atrz (BRIDGE 73/106/237; excluded_no_snapshots
  196/236/479) — every candidate `immature_unsealed`, zero sealed anywhere, matcher
  `zero_candidates`, distinct sealed-topped peak months 0. Delisting census: 256 dark
  segments, 87 in-window identity breaks (examples printed in the artifact). Exactly the
  designed early-life behavior; nothing here reads any effect estimate.
