# TOPA Phase-0 Prereg — Extended-Move Anatomy: topped vs continued

**Frozen:** 2026-08-10 in commit `06a6231fc43960553f3c906a6d6e098502fe9c1c`, before any harness or result artifact existed in the repository; pre-outcome clarifications are append-only in §6.
**Family:** `top_anatomy_p0`
**Authority:** `research/TOP_ANATOMY_MASTERPLAN_BY_FABLE.md` §4–§5 (W0); parent docket `research/SHORT_SIDE_MASTERPLAN_BY_FABLE.md` (L1) — S1⁻/S2⁻/S13⁻ absorbed at episode scale.
**Harness:** `python -m scripts.research_top_anatomy_phase0 --data-root <primary>/data` (module named here per house convention; committed outputs: `data/research/top_anatomy_p0_summary.json` + `reports/top-anatomy-phase0.md`).

## 0. Prior information and trial budget

Adjacent priors on file: sector-ETF scope — extended-then-rolling-over is the only honest topping gate (`engine/sector_signals.py`, 1998–2026); onset scope — no t0 feature separates kept-going from blow-off (W3/W4, closed); W4 found `realized_vol_63d` and `updown_dollar_vol_ratio` are motion-not-quality signatures. No in-repo prior exists on mid/late-life extended-day separation — this is the first construction on that population (ORE-law: first construction, mapped before any kill).

**Trial budget (declared):** 36 features × 1 primary contrast (E1, track W) = 36 tests, BH-FDR q ≤ 0.10 **within each of 6 families**; E1b is 3 nested pooled models (extension-only, +realized-volatility nuisance control, full fixed library plus that nuisance control) × 2 CV schemes = 6 fits, no hyperparameter grid (fixed L2 logistic, C=1.0, standardized); 2 extension-definition sensitivity arms are report-only (no registration claims); E2/E3/E4 are descriptive profiles of E1 survivors (no new tests). No `deflated_sharpe`, no Sharpe claims. All stochastic resampling/fold assignment uses seed `20260810`.

## 1. Hypothesis (one, mechanism-named)

**H1 (incremental demand exhaustion is PIT-visible):** among EXTENDED (name, day) observations, features measuring demand absorption and leadership decay — relative-strength deceleration/peak-lag, effort-vs-result deterioration, up/down dollar-volume asymmetry, participation/maturation structure — separate days that resolve TOPPED from days that resolve CONTINUED, *beyond what extension magnitude and realized volatility themselves separate* (they are matched away). Mechanism directions are pre-declared where §4's table names a direction; fields explicitly marked exploratory remain two-sided, discovery-only candidates.

## 2. Wrong-ruler check

The bottom-side PSS §7 lesson mirrored: topped/continued race labels alone (a return ruler) are not sufficient to call a warning "good" — a fire with large remaining upside is a bad warning even if the episode eventually tops. Phase-0 therefore reports, for any E1-surviving feature thresholded at the direction-aligned control tail (P90 when higher means TOPPED; P10 when lower means TOPPED): median **remaining upside to episode peak**, **peak proximity** (within 5% of peak price), **time proximity** (within ±10td of peak), and **forward-63td excess** vs the all-extended-days null — per-name-first, month-block bootstrapped. For an exploratory field, the observed-sign tail is printed but remains discovery-only. These are report metrics, not additional registered tests.

## 3. Data and universe (frozen)

- **Track W (registration):** `data/massive_stock_day/*.parquet`, split-adjusted via `scripts/replay_standout_pipeline.split_adjust`, with the recovered factor carried to open/high/low (divide) and volume (multiply) so dollar volume is invariant; dividends remain unadjusted. Days 2021-07-06 → run date. Eligibility at day d uses the **raw as-printed** close_d ≥ $3 and raw median 21d close×volume ≥ $2M, plus ≥ 260 prior sessions of identity-segment history; a split-factor step day is ineligible, while the preceding day is not removed using future information. Modeled features use the repaired frame. Because every frozen feature is ratio/standardized and repaired close×volume is invariant, future split factors must not change a pre-split feature value; a synthetic full-series-vs-prefix parity test at every feature family is a hard pre-run gate. Common shares and ADRs are admitted as-listed; no derivative/warrant/unit filtering beyond the price/liquidity floors (stated limitation), and the actual instrument/dead-name census is printed rather than inferred from file count.
- **Track D (era context):** `engine/price_ladder.py` adjusted rungs (`baskets_ohlcv` ∪ `yahoo` ∪ `data_stocks`, first-rung-wins per name), 1997-01-01 → run date, same floors. Survivorship tilt disclosed in every table that uses D. Features requiring open/volume are null where a rung lacks them; per-feature coverage counts printed; a feature with <60% coverage on a track is not interpreted on that track (coverage floor, house law).
- **Cross-sections** (universe medians for RS features): computed per-day over that day's PIT-eligible names within the same track.

## 4. Frozen construction

### 4.1 Extended day (primary)

`EXT(d)` ⇔ `r126(d) ≥ +0.50` AND `close(d) ≥ 0.90 × max(close over trailing 252 sessions)` AND eligibility floors (§3).

Sensitivity arms (report-only): (a) `r63 ≥ +0.35` with the same near-high term; (b) `(close − MA200)/ATR63 ≥ 6` with the same near-high term.

### 4.2 Episode

Contiguous EXT days per name, merging gaps ≤ 21 sessions. Episodes with < 5 EXT days are kept in the tape but excluded from E1 (micro-spells; count printed).

### 4.3 Day-level race label (primary outcome)

From each EXT day d with entry price c_d: scan forward ≤ 250 sessions.
- **TOPPED(d)** if close first falls to ≤ 0.80 × (running max close from d onward) — i.e. a −20% drawdown from the post-d peak — before close ever reaches ≥ 1.15 × c_d **measured from the race state at that moment** (formally: walk forward; track running_peak; TOPPED fires the first day close ≤ 0.80×running_peak; CONTINUED fires the first day close ≥ 1.15×c_d; whichever fires first wins; if the same day satisfies both, TOPPED wins — conservative).
- **CONTINUED(d)** if the +15% barrier fires first.
- **CENSORED(d)** otherwise (horizon end or data end, incl. delisting without a −20% print — delisting with a terminal collapse will fire TOPPED naturally; counts per class printed).

Display-grade auxiliary outcomes (descriptive only, no tests): max forward drawdown from post-d peak at 21/63/126td; fwd returns 21/63/126td; drawdown ≥{10,15,30}% event flags.

### 4.4 Episode peak and terminal top (for lead-time anchoring)

`peak(e)` = argmax close over [episode_start, episode_end + 63 sessions]. Episode **TOPPED** if min close over [peak, peak + 126 sessions] ≤ 0.80 × close(peak) with no intervening close > close(peak) before that −20% print; else **SURVIVED** (censoring stated at data edge). `days_to_peak(d) = peak_date − d` in sessions (positive = d precedes peak).

### 4.5 Matched-control design (E1, primary)

- **Cases:** for each TOPPED episode (per §4.4) on track W, snapshot days at offsets `days_to_peak ∈ {21, 10, 5}` (three pre-declared snapshots; a snapshot exists only if that day is an EXT day — counts printed per offset).
- **Controls:** EXT days (from other names) whose day-level race label (§4.3) is CONTINUED, drawn from the same calendar quarter, same r126 quintile (within-track, within-quarter), same rv63 tercile, same 21d-dollar-volume tercile. Up to 4 controls per case, nearest-neighbor by |Δr126| then |Δrv63| within the bucket, sampled without replacement within a case; cases with zero eligible controls are dropped and counted.
- **Estimator (W4-inspired scaffold, not W4 estimator parity):** per-feature matched-set Δ = case − mean(finite controls), defined only when the case and at least two controls are finite for that feature. Aggregate per episode first as the median Δ over its available {21,10,5}-session snapshots, then take the median across distinct episodes; headline N is distinct episodes and the report also prints case-set N. The primary CI resamples **episode-peak calendar-month blocks** (B=2000, seed `20260810`); ticker-cluster bootstrap uses the same B/seed and is report-only robustness. All 36 tests are two-sided; BH-FDR is applied within each family to the block-bootstrap sign-tail p-values at the episode-aggregated level. A registered separation requires ≥12 distinct episode-peak months, the 95% block CI to exclude 0, BH q ≤ 0.10, and — where a direction was declared — the observed sign to match it. Exploratory fields may be labeled discovery-only separators in either direction but cannot become DETECTION-grade or support authority without a fresh confirmatory prereg. Per-offset profiles are descriptive.

### 4.6 Feature library (36 features; all PIT at d; formulas frozen)

Let c=close, o=open, v=volume, $v = c×v; MA_k = k-session simple MA of c; ATR_k = k-session mean true range; r_k = c_d/c_{d−k} − 1; rv_k = std of daily log returns over k sessions (annualized ×√252); slope_k(x) = OLS slope of x on t over k sessions; eqw index = per-day median cumulative-return index of the track's PIT universe; rs_line = c / eqw_index (rebased per name).

**A. Extension & geometry (8):** A1 r21; A2 r63; A3 r126; A4 r252; A5 (c−MA50)/ATR21; A6 (c−MA200)/ATR21; A7 late_gain_share = (ln c_d − ln c_{d−21})/(ln c_d − ln c_{d−126}) when denom > 0.05 else null; A8 trend_r2_63 = R² of ln c on t, 63s. *Direction: higher ⇒ TOPPED for A5–A7; A1–A4, A8 exploratory (matched-away in part; sign not pre-committed).*
**B. Momentum & acceleration (6):** B1 accel = r21 − r21 lagged 21s; B2 RSI14 (Wilder); B3 RSI14 − RSI14 lagged 10s; B4 newhigh63_rate21 = share of last 21 sessions with c = 63s max; B5 upday_rate21; B6 max consecutive-up-day streak in 21s. *Direction: B1,B3 lower ⇒ TOPPED (deceleration); B2,B4–B6 exploratory.*
**C. Volatility structure (6):** C1 rv21; C2 rv21/rv63; C3 semivol_down/up 63s (std of negative vs positive daily returns); C4 ATR21/c minus its value 21s ago; C5 gap_freq21 = share of last 21 sessions with |o/c_prev − 1| > 2% (null without opens); C6 mean(TR,5)/mean(TR,63). *Direction: higher ⇒ TOPPED for C2,C3,C4; C1,C5,C6 exploratory.*
**D. Volume & effort (6):** D1 dvol_z = z of ln(mean $v, 21s) vs its own trailing 252s distribution; D2 slope_21(ln v); D3 updown_dvol_ratio21 = Σ($v | ret>0)/Σ($v | ret<0) over 21s; D4 Δ21 progress_per_effort, where ppe = r21 / mean21(v/mean252(v)); D5 corr21(v_z, |ret|); D6 churn21 = share of last 21 sessions with v > 1.5×mean252(v) AND |ret| < 0.5×std63(ret). *Direction: lower ⇒ TOPPED for D3,D4,D5; higher ⇒ TOPPED for D6; D1,D2 exploratory.*
**E. Relative strength (5):** E1f xr63 = r63 − median universe r63; E2f xr21 likewise; E3f rs_peak_lag = sessions since rs_line's 63s max; E4f price_rs_gap = rs_peak_lag − (sessions since c's 63s max); E5f rs_decel = slope_21(ln rs_line) − slope_21(ln rs_line) lagged 21s. *Direction: higher ⇒ TOPPED for E3f,E4f; lower ⇒ TOPPED for E5f; E1f,E2f exploratory.*
**F. Maturation & structure (5):** F1 episode_age (sessions since episode start); F2 c/max(c in episode so far) − 1; F3 sessions since last 63s high; F4 deepest dip in last 21s vs running 63s max; F5 reclaim_speed = sessions the most recent completed ≥5% intra-episode dip took to reclaim its pre-dip high (null if none/unreclaimed; unreclaimed-flag printed). *Direction: higher ⇒ TOPPED for F1,F3; lower (more negative) ⇒ TOPPED for F2,F4; F5 higher ⇒ TOPPED.*

### 4.7 Pooled increment (E1b)

Day-level TOPPED vs CONTINUED on all EXT days (not just matched sets), track W. Models: (M0) r126 alone; (M1) M0 + rv63; (M2) M1 + the other 35 frozen features (all §4.6 features, with A3/r126 included only once). Fixed logistic, L2 C=1.0. Every fold fits medians and standardization parameters on its training rows only; test-fold nulls use those training medians, no missingness indicators are added, and no full-sample preprocessing is permitted. CV-A: 5-fold grouped by raw ticker (all identity segments of a reused ticker stay together). CV-B: expanding walk-forward by calendar quarter with a 250-session label purge/embargo between train end and test start, matching the maximum race-label horizon. Metric: AUC (day-level) and episode-level AUC (episode max probability vs episode outcome), with episode-block bootstrap 95% CIs for each AUC and paired ΔAUC. Claim of interest: AUC(M2) − AUC(M1), both CV schemes, sign-consistent; E1b remains descriptive and creates no additional registered test. No feature selection inside folds beyond the fixed set (no tuning).

### 4.8 Lead-time and ordering (E2/E3, on E1 survivors only)

With `days_to_peak = peak_date − d` (positive before the peak), E2 reports median matched Δ in four disjoint windows: **EARLY** +22..+63, **MID** +6..+21, **LATE** +1..+5, and **POST-TOP CONFIRMATION** 0..−5 (peak day through five sessions after). A direction-declared survivor takes the earliest pre-peak label whose episode-block CI excludes 0 in the declared sign; if only the last window separates it is labeled **POST-TOP CONFIRMATION**. Exploratory separators keep an `EXPLORATORY` prefix and cannot receive DETECTION grade. E3 reports within-TOPPED-episode median first-crossing order at direction-aligned control-tail thresholds (P90 for higher-risk, P10 for lower-risk; descriptive sequence table).

### 4.9 Era & cap stability (E4)

Descriptive sign-stability tables of E1 survivors: track W eras {2021H2–2022, 2023–2024, 2025–2026}; track D eras {1997–2003, 2004–2012, 2013–2020, 2021–2026} for features computable on D; dollar-volume terciles as the cap proxy (massive store carries no shares outstanding — stated).

## 5. Gates and decision rules

- **This is a discovery phase-0: there is no program kill on a null.** Outcomes bind copy and sequencing, not existence:
  - **≥1 family with ≥1 direction-declared DETECTION-grade survivor** (E1 pass + E2 label EARLY/MID/LATE, i.e. any pre-peak bucket) ⇒ a separate Wave-2 hazard-model prereg may be chartered; Wave-1 copy may cite the survivor legs as "what we watch" (display-tier, no probabilities). An exploratory separator can only nominate a fresh confirmatory prereg.
  - **Survivors exist but all label POST-TOP CONFIRMATION** ⇒ they may power only the `breaking`-state legs in Wave 1; no "early warning" copy anywhere.
  - **Zero survivors** ⇒ Wave 1 ships descriptive-only (states + episode-library base rates, no separation claims); Wave 2 pivots to cross-sectional/theme constructions (different construction, fresh prereg; this row closes only the single-name-OHLCV matched-contrast construction).
- **Integrity gates (hard, from masterplan §0):** G0.2 delisting verification (≥3 named dead tickers in the W tape with terminal bars); G0.3 episode honest-N on every table; G0.4 lead-time labels mandatory; G0.5 today's-tape coverage appendix + Opus red-team before presentation.
- Any deviation from this construction discovered mid-run (data defect, degenerate bucket) is recorded in §6 with its trigger BEFORE outcomes are read at the affected step; silent re-pins are forbidden.

## 6. Ratification log (append-only)

- 2026-08-10: Frozen at charter. No results computed. (Deviations, if any, and results-time entries append below with dates.)
- 2026-08-10 (pre-outcome deviation, data defect — trigger recorded before any label was computed): the massive store keys by TICKER and tickers are reused across corporate identities (verified: `BBBY.parquet` = old Bed Bath & Beyond through 2023, an 850-day gap, then a different company from 2025-08-29). **Rule added:** every ticker's series is split at interior gaps > 60 sessions into identity segments treated as independent names (own history floor, episodes, PIT windows truncated at segment start; nothing crosses a boundary). Segment counts printed in the summary. This is an identity repair, not a construction change; race/feature definitions are untouched.
- 2026-08-10 (pre-outcome reconciliation clarification): repository audit confirmed commits `06a6231fc43960553f3c906a6d6e098502fe9c1c` and `7895028668d245509b64f843dc35e5185655370e` contain neither the named harness nor TOPA result artifacts. Before implementation, this entry (a) corrected E2 bucket signs to the already-frozen positive-before-peak convention; (b) made E1 episode-first aggregation explicit; (c) replaced the inaccurate "W4 estimator parity" claim with the actual W4-inspired scaffold; (d) fixed E1b as nested M0/M1/M2 models with train-fold-only preprocessing and a 250-session label embargo; (e) made two-sided exploratory features discovery-only; and (f) pinned raw-level eligibility, full-OHLCV split-factor carry, split-day exclusion, and feature-prefix parity. No feature, threshold, label, outcome, or trial count changed, and no result was read.
- 2026-08-10 (pre-full-run notes, recorded at engine completion, before any full-run outcome was read; smoke runs on ≤400-ticker slices only):
  (i) **§4.3 tie clause unreachable:** under the frozen 0.80/1.15 barriers the same-day tie cannot occur (a tie requires the running peak ≥ 1.4375 × entry close, at which bar the +15% barrier had already fired). The clause stays implemented and test-pinned with degenerate barriers; no result may be attributed to it.
  (ii) **Track D structural coverage fact:** under first-rung-wins, `baskets_ohlcv` (zero bars before 2014-01-02) resolves nearly the whole D universe; only ~257 names fall through to deeper rungs. The prereg's 1997–2003 / 2004–2012 D eras will therefore carry a handful of names and print as thin/null cells — a structural property of the ladder, disclosed here pre-outcome, not a re-pin. The D universe also admits liquid ETFs under the floors-only eligibility (stated limitation; disclosed in the report).
  (iii) **E2 bucket population mechanics:** the three registered case offsets {21,10,5} populate only two of §4.8's four lead-time buckets; the harness additionally samples up to 2 EXT days per (TOPPED episode, bucket) — earliest-first, deterministic — to populate the descriptive E2/E3 profiles. E1 registration remains exactly the three frozen offsets.
- 2026-08-10 (pre-full-run, engine compliance pass): the W0 engine was reconciled against the reconciliation-clarification entry above BEFORE the full harness run: raw-level eligibility with split-day exclusion, split-factor carry to open/high/low (divide) and volume (multiply), episode-first E1 aggregation (median over an episode's snapshots, per the frozen §4.5 word — a commissioning paraphrase saying "average" was rejected in favor of the frozen text), nested E1b (M0/M1/M2) with train-fold-only preprocessing and the 250-session label embargo, an episode-peak-month registration floor (≥12 peak-months), and the feature full-series-vs-prefix parity hard gate implemented as tests and as a runtime pre-label assertion. Gate mechanics note: the parity gate holds the cross-sectional eqw index fixed across both sides (per-name repair leakage is the target; a one-name universe degenerates the index into the name's own return line — artifact receipt in the `_parity_side` docstring). No outcome was read before this pass completed.
- 2026-08-10 (pre-full-run independent audit, after slice smoke only): exact-code review found and repaired implementation shortcuts before any full-panel outcome was computed. E1b and the ruler now use **all** EXT days (not a 1-in-5 sample); the CONTINUED control pool includes micro-spell days (the five-EXT-day floor applies only to case episodes); E1f/E2f subtract the literal same-day median universe r63/r21 while E3f–E5f retain the separate eqw-index line; B6 is bounded to the trailing 21-session window and C3/D6 use the prereg's simple daily-return definition. E2 lead labels now follow their frozen descriptive rule (CI exclusion in the declared sign, without a second FDR/min-month gate); every day- and episode-level E1b AUC/paired increment carries episode-block intervals; ruler shares aggregate as within-name shares and its intervals resample episode-peak months. A missing scikit-learn dependency now fails the required E1b run closed instead of emitting an incomplete "successful" artifact. These are compliance repairs to the already-frozen formulas and populations, not new features, thresholds, outcomes, or arms. The inert W1 template's optional percent null was also changed from a false `+0%` to an honest dash; it consumes no outcome.
- 2026-08-10 (POST-RUN data-defect discovery → labeled repair arm `sanity-segmented`, declared BEFORE the repair arm was computed): run-1's today's-tape appendix surfaced fabricated extensions — single-day close ratios of 10.3x–123x survive the split repair because `_COMMON_SPLITS` (scripts/replay_standout_pipeline.py) covers reverse splits only to 1:10, so penny-stock reverse splits of 1:15..1:125 pass through unrepaired (verified: AGL +2,361% 2026-03-31, COOK +5,469% 2026-03-18, IMUX +1,034%, PAVS +12,212% and +8,325%, ALIT +2,352% — all consistent with unflagged reverse splits). Fabricated up-jumps enter r126 and manufacture fake EXT days/episodes/races. **Repair rule (identity mechanics, asymmetric by design):** any residual single-day close ratio ≥ 3.0 in the REPAIRED series becomes an identity break (same segmentation machinery as the 60-session gap rule — no window, race, or feature may span it). The down side is deliberately NOT screened: forward splits are repaired to 20:1 by the existing list, and large one-day collapses are real events this study must keep. Cost disclosed: genuinely real ≥+200% single days (rare hyper-squeezes) also break, and their post-event segments re-enter only after the 260-session history floor; counts of affected segments/EXT days/episodes are printed in the repair-arm report. Run-1 results above this entry are retained as the `pre-repair` arm for audit; the `sanity-segmented` arm is the headline arm. No repaired-arm outcome was read before this entry was recorded.
- 2026-08-10 (instrument reconciliation, recorded at reconciliation): two implementation lines of the declared `sanity-segmented` arm existed in parallel. Run-2 was executed on the PRE-AUDIT variant (the `2abbe7fff02`-line worktree code) before the independently audited line — the one carrying the compliance repairs recorded in the "pre-full-run independent audit" entry above (all-EXT-day E1b and ruler, micro-spell days in the CONTINUED control pool, literal same-day cross-sectional medians for E1f/E2f with the eqw rs_line retained for E3f–E5f, window-bounded B6, the prereg's simple daily-return C3/D6, frozen-rule E2 lead labels, episode-block intervals on every E1b AUC and paired increment, within-name ruler shares with peak-month resampled intervals, and a missing scikit-learn failing the required E1b run closed) — was recognized as canonical. The reconciled instrument is those audit repairs plus the declared ≥3.0x residual up-jump identity rule from the entry above, with that rule applied to track W only (adjusted track D stays gap-only, as run-1 was). Run-3 on the reconciled instrument is the HEADLINE arm; run-2 is retained as a cross-check arm with its summary preserved at `data/research/top_anatomy_p0_summary_run2_preaudit.json`, and run-1 remains the `pre-repair` audit arm at `data/research/top_anatomy_p0_summary_prerepair.json`. The pre-repair panel cache no longer exists on disk (run-2 rebuilt it in place under the repaired identity rules), so run-1's measured contamination counts are carried forward from that preserved artifact with an explicit provenance line rather than recomputed; the audit code refuses to report a sanity-segmented panel's structural zeros as a measurement. No frozen quantity, threshold, population, or outcome rule changed; the only display change is the today's-tape appendix, now uncapped so the whole extended cohort prints (G0.5 is a coverage gate, and a truncated appendix is what would have hidden the run-1 defect).
- 2026-08-10 (date-convention clarification, no construction content): every §6 entry above is dated by the LOCAL session date (America/Los_Angeles) on which it was recorded, while every artifact stamps a UTC run date (`run_date`/`run_timestamp_utc`). The two differ by up to one calendar day, so a §6 entry dated 2026-08-10 legitimately precedes a summary stamped 2026-08-11; entry order in this log, not the printed date, is the record of what was declared before what. No entry above is edited by this clarification.
- 2026-08-11 (W2 tier-widening preregistration, separate document): the §12-chartered lower-extension arms (`r63≥+0.35`, `(c−MA200)/ATR63≥6` — both declared in the engine pre-results and counted in §4.1's sensitivity census) are preregistered as wave W2 in `research/top_anatomy/TOPA_W2_PREREG.md`, frozen before any W2 result exists: same tape vintage (2026-07-02), track W only, frozen pipeline, five phase-0 survivors as one-sided confirmatory hypotheses in their observed directions, DISJOINT (zero EXT-day overlap with primary) panels primary, exploratory set capped at EXPLORATORY-DISCOVERY. This entry records the pointer; no phase-0 quantity above changes.
