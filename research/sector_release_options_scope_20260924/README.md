# Restore the existing Options test owner's declared coverage

## Observable capability and source boundary

The normal PR contract-delta/CI planner can read the current manifest again and
execute the existing complete Options source-note suite. One literal declared
path was omitted while its test command remained present. No test, code/data gate,
job, command, dependency, timeout, budget or policy is removed or relaxed.

Source base: `macro@ca745ef0d7e3294857f1231e93d480c2585b1127`.
Protected procedure: `Mastermind@a0b31114d5d645e73da1333f7258092034a54c34`,
compatible Skillpack1.0.1/bootstrap1. Current Chairman continuation covers the
US sector/Prophet outcome; this is its exact release dependency, not an Options
model change or a new CI programme. Direct execution: CRITICAL_PATH_SHORTCUT.

## Exact failure

Current #7798 run35942793898, contract-delta/check107454672001 refused computation:
`options-payoff-lab-consumer` reads `tests/test_options_skew_source_note.py`
but its exclusive paths omit that file. The job mapping is identical on that
feature's base/head; it is not a new card defect. The source-note suite arrived
in merged#7783; later scope curation#7854 missed its path.

The shared #7802/ADMIN-BASELINE-SCOPE-20260923 remains the main-red-repair owner.
Its locked unpublished composition is not modified or taken over. The disjoint
Options-job path correction is returned for its current-source integration;
no second main-red-repair label, controller or baseline dispatch is created.
Original #7669's partially authored and platform-blocked code-registration
correction is not replayed or bypassed by this independent one-path repair.

## Before/after and proof

A new test in the already-executed source-note suite checks the actual existing
code job, full invocation without deselection and exact exclusive scope.
It fails on the incumbent with `source-note suite is named but unreachable`.
Adding the omitted literal path makes all30 source-note/payoff-consumer tests
pass under Python3.14 and Python3.12 (overlapping runs; no skips).

The native `load_legacy_jobs` now accepts all227 jobs. An independent structural
comparison confirms only that existing job's paths change and only by one
addition; execution commands, all other jobs, dependency lists and timeouts are
identical. Native nonexecuting scope inference of that job derives18concrete
paths, all18covered, no missing path; unchanged job weight6. This is not a
rerun of the former UK-source packing command or a full main-baseline proof.
The existing regression detects the original absence directly; no assertion is
weakened and no parser source is edited.

## Remaining acceptance

Exact-head hosted CI, independent source review and accepted integration/merge
through the existing owner are still required. The broader main baseline may
have other failures; the clean manifest proof does not declare them fixed.
Normal publisher and actual live recommendation/stock assessment proof remain
separate. No product, data, model or entitlement behavior changed.

CAPABILITY_STATE: BUILT_NOT_PROVEN
MISSION_COMPLETE: false
