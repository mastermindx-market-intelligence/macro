# CN-Limit R6 — Fable Execution Command Packet

**Prepared:** 2026-08-19  
**Program:** `WS:CN-LIMIT-ALPHA`  
**Role:** Principal builder/orchestrator  
**Repository:** `mastermindx-market-intelligence/macro`  
**R6 reconciliation pin:** `b556c73a8d5c83a4babee326adc6fad46b6aa6a7`  
**Governing architecture:** `CN_LIMIT_R6_FINAL_ARCHITECTURE_FREEZE_2026-08-19.md`  
**Immediate authorization:** **R6-0 only**

---

## 0. Command

Execute the CN-Limit program without shrinking the thesis and without widening any individual PR. The immediate mission is `R6-0`, a records-only durable landing. All later commissions in this packet are spawn-ready **but dormant** behind their stated gates.

The outcome is not “code exists.” The outcome is a lawful company capability that works through canonical data, Prophet, exact outcomes, and a real consumer, while preserving a clean prospective comparison.

## 1. Authority and precedence

1. Chairman Chris Wong
2. Sol R6 final architecture freeze and R6 registry
3. This command packet
4. R5 integration architecture
5. R4 preregistration
6. R3 ontology/registry
7. R2 causal architecture
8. R1 evidence map
9. Existing canonical owner contracts in the repository

Where a current owner contract conflicts with a stale path citation, preserve the owner contract and return a reconciliation ruling. Do not silently move authority.

## 2. Verified current state

- Current reconciliation pin: `b556c73a8d5c83a4babee326adc6fad46b6aa6a7`.
- `engine/china_board_rank.py` remains the sole live CN Prophet ranking/admission authority.
- `data/china_prophet_rank/candidates.parquet` remains the canonical full-universe candidate research plane.
- `data/china_standout_track/board.parquet` remains the selected-board/T+1 path ledger.
- Exact-plane foundation and range shards are merged but fail-closed on rights/live-canary gates.
- Main-board risk-warning event logic is still wrong after 2026-07-06 and remains P0.
- China Alpha Intelligence PR #5953 and its prerequisite PR chain remain open/nonmergeable; PR-0B is not live.
- R5→R6 repo movement added a fixture-based Prophet Operator Lab for US/Radar. It owns none of the China paths in this program and is fenced off.
- P-B2/P-B3 are frozen null/uninformative and cannot be reopened.

## 3. Global no-go boundaries

- No restored adjusted-plane artifact, row, probability, grade, page, reader, or reproducer.
- No duplicate candidate store, grader, identity, event store, Intelligence composite, or lifecycle.
- No candidate-plane runtime edit before PR-0B lands and is proven live.
- No full-A gate constant change without reviewed canary/throughput/correctness evidence.
- No static-washout rescue or unordered factor-count scorer.
- No same-session seal/LHB leakage for the same first-board target.
- No manipulation/insider labels from charts.
- No public probability, score, gate, or size authority.
- No Prophet Operator Lab shortcut.

## 4. Program-wide acceptance law

Every PR must contain one independently useful capability, a real consumer, tests that detect the incident class, and a real production path proof. A foundation-only PR is accepted only when its sole user is a machine capability that works in the same PR; otherwise pair it with the first vertical or do not build it.

---

## R6-0 — Durable final-freeze landing

**Status:** `AUTHORIZED_NEXT`  
**Owner:** Fable  
**Dependencies:** none

### Observable mission

Land the R1–R6 canonical research, architecture freeze, registries, decisions, workstream update, handoff index, and exact next action as records-only repository state.

### Why it matters

A fresh Mastermind session can discover and execute the CN-Limit program without reconstructing this conversation.

### Exact scope

- research/cn_limit/** (new canonical R6 package)
- agentos/workstreams/WS-CN-LIMIT-ALPHA.md
- new AgentOS decisions/discoveries/handoff
- program/system map only if canonical generator requires it

### Explicit non-goals

- no engine, collector, workflow, data, template, site, score, rank, gate, size, probability, or authority change

### Complete user and machine journey

1. Resolve the canonical source population and definition era before computing anything.
2. Produce the named capability through the existing owner-native path.
3. Persist only the projection this wave is authorized to own.
4. Expose a real internal consumer or audit receipt that reads the new output.
5. Carry unavailable, stale, correction, and quarantine states visibly.
6. Prove the result through the real settled production path.
7. Update AgentOS and write the continuation handoff before stopping.

### Data, contract, time, null, and correction behavior

- No event or source date may be treated as known before its lawful publication/first-seen/system clock.
- Missing, not covered, stale, corrected, quarantined, and inapplicable are distinct states; none maps to zero.
- Existing candidate/outcome identities and owner schemas are extended or referenced, never forked.
- A correction is additive. An already-published prediction or candidate observation is never silently overwritten.
- Every run binds exact input/source digests and definition hashes.
- Partial sessions, unresolved identities, or incomplete coverage fail closed for the affected output.

### Deterministic, statistical, and model-generated boundary

- Deterministic rules, identities, clocks, and state machines follow the frozen R3–R6 contracts.
- Statistical estimation is permitted only in waves whose mission explicitly names R4/G6 evaluation and only after preregistration.
- LLMs may summarize evidence for internal review but may not originate facts, actor identity, feature values, labels, rankings, or authority.

### Ordered implementation sequence

1. Reconcile current main and open PR collisions against this packet.
2. Freeze the exact file/path allowlist and definition hash.
3. Implement the narrow vertical and mutation-detecting tests.
4. Add the real consumer/audit in the same PR.
5. Run targeted and closure/owner tests.
6. Obtain independent adversarial review.
7. Merge under normal governance.
8. Execute and record the real production proof.
9. Update the workstream state honestly: merge = `BUILT_NOT_PROVEN`; real proof = `PROVEN_LIVE`/`done` where applicable.

### Acceptance tests

- byte-faithful landing or explicitly reconciled paths
- AgentOS validation 0 errors
- one canonical R6 index
- stale-instruction sweep
- no runtime diff

### Required real production proof

Merged records discoverable on origin/main; no runtime production proof claimed.

### Stop condition

Stop after merge and repository discovery proof. Do not begin a runtime wave in the same PR.

### Required continuation handoff

Return `STATUS / RESULT / EVIDENCE / PRODUCTION PROOF / GAPS / DEVIATIONS / DISCOVERIES / EXACT NEXT ACTION`. Name every changed path, definition hash, source clock, unavailable state, and owner collision. Do not launch the next wave in the same session unless this packet explicitly marks it parallel and its dependencies are already proven.

## P0-ST — 2026 main-board risk-warning rule parity and replay

**Status:** `DORMANT_UNTIL_R6_0_MERGED`  
**Owner:** Fable with China microstructure owner  
**Dependencies:** R6-0

### Observable mission

Correct the effective-dated SSE/SZSE main-board risk-warning band from ±5% to ±10% beginning 2026-07-06 and re-derive every affected canonical downstream artifact with correction receipts.

### Why it matters

Current risk-warning event labels, breadth, failed seals, ladder counts, and microstructure packets become legally correct after the rule change.

### Exact scope

- config/cn_limit_rules.yml
- engine/china_microstructure.py
- parity/rule tests
- owner-native re-derivation scripts/stores
- downstream affected-row census and receipts

### Explicit non-goals

- no full-A gate unlock
- no CN-Limit feature/model work
- no tolerant-label promotion
- no unrelated ST collector redesign

### Complete user and machine journey

1. Resolve the canonical source population and definition era before computing anything.
2. Produce the named capability through the existing owner-native path.
3. Persist only the projection this wave is authorized to own.
4. Expose a real internal consumer or audit receipt that reads the new output.
5. Carry unavailable, stale, correction, and quarantine states visibly.
6. Prove the result through the real settled production path.
7. Update AgentOS and write the continuation handoff before stopping.

### Data, contract, time, null, and correction behavior

- No event or source date may be treated as known before its lawful publication/first-seen/system clock.
- Missing, not covered, stale, corrected, quarantined, and inapplicable are distinct states; none maps to zero.
- Existing candidate/outcome identities and owner schemas are extended or referenced, never forked.
- A correction is additive. An already-published prediction or candidate observation is never silently overwritten.
- Every run binds exact input/source digests and definition hashes.
- Partial sessions, unresolved identities, or incomplete coverage fail closed for the affected output.

### Deterministic, statistical, and model-generated boundary

- Deterministic rules, identities, clocks, and state machines follow the frozen R3–R6 contracts.
- Statistical estimation is permitted only in waves whose mission explicitly names R4/G6 evaluation and only after preregistration.
- LLMs may summarize evidence for internal review but may not originate facts, actor identity, feature values, labels, rankings, or authority.

### Ordered implementation sequence

1. Reconcile current main and open PR collisions against this packet.
2. Freeze the exact file/path allowlist and definition hash.
3. Implement the narrow vertical and mutation-detecting tests.
4. Add the real consumer/audit in the same PR.
5. Run targeted and closure/owner tests.
6. Obtain independent adversarial review.
7. Merge under normal governance.
8. Execute and record the real production proof.
9. Update the workstream state honestly: merge = `BUILT_NOT_PROVEN`; real proof = `PROVEN_LIVE`/`done` where applicable.

### Acceptance tests

- official-source effective-date receipt
- boundary tests 2026-07-05/06 both venues
- exact affected-row census
- old/new event comparison
- bounded replay
- downstream parity and correction receipts
- real asia-close proof

### Required real production proof

Real post-repair asia-close writes rule-correct packets; affected stores and public/internal consumers show the 10% regime; no stale 5% label remains.

### Stop condition

Stop if owner paths or official rule interpretation conflict; return a ruling packet rather than guessing.

### Required continuation handoff

Return `STATUS / RESULT / EVIDENCE / PRODUCTION PROOF / GAPS / DEVIATIONS / DISCOVERIES / EXACT NEXT ACTION`. Name every changed path, definition hash, source clock, unavailable state, and owner collision. Do not launch the next wave in the same session unless this packet explicitly marks it parallel and its dependencies are already proven.

## DEP-CAI — China Alpha Intelligence freeze, PR-0B telemetry, rights and identity dependency closure

**Status:** `BLOCKED_OPEN_PRS`  
**Owner:** Fable in China Alpha Intelligence program  
**Dependencies:** R6-0

### Observable mission

Rebase/adjudicate PR #5953 and prerequisite PRs; merge the canonical architecture; execute PR-0B and prove full Intelligence anatomy on real candidate rows before CN-Limit touches the candidate writer.

### Why it matters

Canonical candidate rows preserve the incumbent v4 Intelligence anatomy required for honest G4 ablation and champion-era reconstruction.

### Exact scope

- PR #5953 chain
- PR-0B commission
- RIGHTS-0
- PR-0D identity; P1 visits only after rights GO

### Explicit non-goals

- no CN-Limit fields yet
- no rival visit tape
- no rank change in PR-0B
- no historical served-value fabrication

### Complete user and machine journey

1. Resolve the canonical source population and definition era before computing anything.
2. Produce the named capability through the existing owner-native path.
3. Persist only the projection this wave is authorized to own.
4. Expose a real internal consumer or audit receipt that reads the new output.
5. Carry unavailable, stale, correction, and quarantine states visibly.
6. Prove the result through the real settled production path.
7. Update AgentOS and write the continuation handoff before stopping.

### Data, contract, time, null, and correction behavior

- No event or source date may be treated as known before its lawful publication/first-seen/system clock.
- Missing, not covered, stale, corrected, quarantined, and inapplicable are distinct states; none maps to zero.
- Existing candidate/outcome identities and owner schemas are extended or referenced, never forked.
- A correction is additive. An already-published prediction or candidate observation is never silently overwritten.
- Every run binds exact input/source digests and definition hashes.
- Partial sessions, unresolved identities, or incomplete coverage fail closed for the affected output.

### Deterministic, statistical, and model-generated boundary

- Deterministic rules, identities, clocks, and state machines follow the frozen R3–R6 contracts.
- Statistical estimation is permitted only in waves whose mission explicitly names R4/G6 evaluation and only after preregistration.
- LLMs may summarize evidence for internal review but may not originate facts, actor identity, feature values, labels, rankings, or authority.

### Ordered implementation sequence

1. Reconcile current main and open PR collisions against this packet.
2. Freeze the exact file/path allowlist and definition hash.
3. Implement the narrow vertical and mutation-detecting tests.
4. Add the real consumer/audit in the same PR.
5. Run targeted and closure/owner tests.
6. Obtain independent adversarial review.
7. Merge under normal governance.
8. Execute and record the real production proof.
9. Update the workstream state honestly: merge = `BUILT_NOT_PROVEN`; real proof = `PROVEN_LIVE`/`done` where applicable.

### Acceptance tests

- mergeable rebased architecture
- single-compute telemetry
- ordering-invariance
- real asia-close candidate rows with non-null intel anatomy and explicit unavailable reasons
- workstream proof state done only after real write

### Required real production proof

Newest canonical candidate rows carry the complete intel_ family and coverage/fallback anatomy under one served definition.

### Stop condition

CN-Limit candidate-plane work remains blocked until PR-0B is PROVEN_LIVE, not merely merged.

### Required continuation handoff

Return `STATUS / RESULT / EVIDENCE / PRODUCTION PROOF / GAPS / DEVIATIONS / DISCOVERIES / EXACT NEXT ACTION`. Name every changed path, definition hash, source clock, unavailable state, and owner collision. Do not launch the next wave in the same session unless this packet explicitly marks it parallel and its dependencies are already proven.

## DEP-EXACT — Exact-plane technical readiness, live canary, range campaign, and completeness

**Status:** `TECHNICAL_CANARY_REQUIRED`  
**Owner:** Fable with exact-plane authority owner/operator  
**Dependencies:** R6-0

### Observable mission

Run the live technical canary, promote the operational range campaign only through reviewed technical gates, and produce a complete sanitized exact-plane manifest. TuShare licensing/compliance is `CHAIRMAN_VERIFIED_PRIVATE / SATISFIED` (`DEC:CNLI-TUSHARE-COMPLIANCE-IS-CHAIRMAN-VERIFIED-PRIVATE`); its evidence is confidential and outside coding scope, so no licensing artifact is part of this wave.

### Why it matters

Authority-grade full-A nominal daily, exact legal limits, sessions, suspensions, ST state, and PIT universe become available for target construction.

### Exact scope

- existing fail-closed spine and range-shard path
- the bounded canary path (`collect(canary=True)` / lane `mode=canary`): real, hard-bounded (<=12 requests, <=5 calendar days, no `allow_bulk`), permitted while `BULK_HISTORICAL_BACKFILL_READY` is still `False`, and refusing a documented row cap rather than starting the unproven ticker-range campaign
- live canary parity, throughput, and correctness evidence
- reviewed `BULK_HISTORICAL_BACKFILL_READY` promotion on that evidence
- completeness manifest

### Explicit non-goals

- no request for, inspection of, or gating on the private compliance agreement
- no reintroduced authorization-receipt/trust-allowlist/license-document gate
- no gate constant edits without reviewed technical evidence
- no public redistribution of raw vendor data
- no CN-Limit model

### Complete user and machine journey

1. Resolve the canonical source population and definition era before computing anything.
2. Produce the named capability through the existing owner-native path.
3. Persist only the projection this wave is authorized to own.
4. Expose a real internal consumer or audit receipt that reads the new output.
5. Carry unavailable, stale, correction, and quarantine states visibly.
6. Prove the result through the real settled production path.
7. Update AgentOS and write the continuation handoff before stopping.

### Data, contract, time, null, and correction behavior

- No event or source date may be treated as known before its lawful publication/first-seen/system clock.
- Missing, not covered, stale, corrected, quarantined, and inapplicable are distinct states; none maps to zero.
- Existing candidate/outcome identities and owner schemas are extended or referenced, never forked.
- A correction is additive. An already-published prediction or candidate observation is never silently overwritten.
- Every run binds exact input/source digests and definition hashes.
- Partial sessions, unresolved identities, or incomplete coverage fail closed for the affected output.

### Deterministic, statistical, and model-generated boundary

- Deterministic rules, identities, clocks, and state machines follow the frozen R3–R6 contracts.
- Statistical estimation is permitted only in waves whose mission explicitly names R4/G6 evaluation and only after preregistration.
- LLMs may summarize evidence for internal review but may not originate facts, actor identity, feature values, labels, rankings, or authority.

### Ordered implementation sequence

1. Reconcile current main and open PR collisions against this packet.
2. Freeze the exact file/path allowlist and definition hash.
3. Implement the narrow vertical and mutation-detecting tests.
4. Add the real consumer/audit in the same PR.
5. Run targeted and closure/owner tests.
6. Obtain independent adversarial review.
7. Merge under normal governance.
8. Execute and record the real production proof.
9. Update the workstream state honestly: merge = `BUILT_NOT_PROVEN`; real proof = `PROVEN_LIVE`/`done` where applicable.

### Acceptance tests

- authorization scope valid
- trust root code-reviewed
- live canaries exact-schema/receipt-bound
- range recovery proven
- zero unknown/name-orphan/completeness failures
- private store integrity

### Required real production proof

A real licensed campaign completes with immutable request/artifact receipts and a sanitized manifest that closes every contract gate.

### Stop condition

Stop on any rights ambiguity, source cap ambiguity, unknown row, identity orphan, or parity failure.

### Required continuation handoff

Return `STATUS / RESULT / EVIDENCE / PRODUCTION PROOF / GAPS / DEVIATIONS / DISCOVERIES / EXACT NEXT ACTION`. Name every changed path, definition hash, source clock, unavailable state, and owner collision. Do not launch the next wave in the same session unless this packet explicitly marks it parallel and its dependencies are already proven.

## DEP-ID-ELIG — Canonical China identity, PIT membership, and eligibility overlay

**Status:** `BLOCKED_EXACT_AND_IDENTITY`  
**Owner:** Fable with Data OS/GMI/eligibility owners  
**Dependencies:** DEP-EXACT, DEP-CAI

### Observable mission

Produce the point-in-time eligible security-session population and time-valid sector/theme edges without creating a CN-Limit identity or membership plane.

### Why it matters

R4 U0/U1 populations, carrier matching, sector relay, and exact denominators become survivorship- and era-honest.

### Exact scope

- canonical security/listing IDs
- listing/delisting/ST/suspension/IPO/no-limit states
- PIT sector/theme edges
- eligible_at_risk projection

### Explicit non-goals

- no ticker-only identity
- no current-membership backfill
- no new company-event store
- no ranking

### Complete user and machine journey

1. Resolve the canonical source population and definition era before computing anything.
2. Produce the named capability through the existing owner-native path.
3. Persist only the projection this wave is authorized to own.
4. Expose a real internal consumer or audit receipt that reads the new output.
5. Carry unavailable, stale, correction, and quarantine states visibly.
6. Prove the result through the real settled production path.
7. Update AgentOS and write the continuation handoff before stopping.

### Data, contract, time, null, and correction behavior

- No event or source date may be treated as known before its lawful publication/first-seen/system clock.
- Missing, not covered, stale, corrected, quarantined, and inapplicable are distinct states; none maps to zero.
- Existing candidate/outcome identities and owner schemas are extended or referenced, never forked.
- A correction is additive. An already-published prediction or candidate observation is never silently overwritten.
- Every run binds exact input/source digests and definition hashes.
- Partial sessions, unresolved identities, or incomplete coverage fail closed for the affected output.

### Deterministic, statistical, and model-generated boundary

- Deterministic rules, identities, clocks, and state machines follow the frozen R3–R6 contracts.
- Statistical estimation is permitted only in waves whose mission explicitly names R4/G6 evaluation and only after preregistration.
- LLMs may summarize evidence for internal review but may not originate facts, actor identity, feature values, labels, rankings, or authority.

### Ordered implementation sequence

1. Reconcile current main and open PR collisions against this packet.
2. Freeze the exact file/path allowlist and definition hash.
3. Implement the narrow vertical and mutation-detecting tests.
4. Add the real consumer/audit in the same PR.
5. Run targeted and closure/owner tests.
6. Obtain independent adversarial review.
7. Merge under normal governance.
8. Execute and record the real production proof.
9. Update the workstream state honestly: merge = `BUILT_NOT_PROVEN`; real proof = `PROVEN_LIVE`/`done` where applicable.

### Acceptance tests

- one row per canonical security-session
- reason-coded exclusions/quarantines
- delisted and suspended names retained lawfully
- time-valid membership
- cross-owner referential integrity

### Required real production proof

Exact population counts and exclusion reasons reconcile to source manifests across representative eras/boards and a current real session.

### Stop condition

Stop if an issuer/listing or membership edge cannot be resolved point-in-time; mark unavailable, never infer.

### Required continuation handoff

Return `STATUS / RESULT / EVIDENCE / PRODUCTION PROOF / GAPS / DEVIATIONS / DISCOVERIES / EXACT NEXT ACTION`. Name every changed path, definition hash, source clock, unavailable state, and owner collision. Do not launch the next wave in the same session unless this packet explicitly marks it parallel and its dependencies are already proven.

## I1A-T1 — First candidate-anatomy vertical: downside-impact asymmetry

**Status:** `BLOCKED_DEPENDENCIES`  
**Owner:** Fable builder after dependency gates  
**Dependencies:** P0-ST, DEP-CAI, DEP-ID-ELIG

### Observable mission

Compute one frozen deterministic feature, project it prospectively onto canonical candidate rows, and ship a real audit consumer that measures coverage, missingness, carrier distribution, and Prophet overlap—without changing order.

### Why it matters

Mastermind begins accruing the first lawful transition feature on every Prophet-scored candidate under immutable clocks and definition eras.

### Exact scope

- one feature formula from R3/R4
- candidate-plane nullable fields after PR-0B seam
- coverage/overlap audit receipt
- tests and real asia-close proof

### Explicit non-goals

- no prediction probability
- no rank/order/admission
- no generic feature framework without the real feature/consumer
- no raw licensed copy

### Complete user and machine journey

1. Resolve the canonical source population and definition era before computing anything.
2. Produce the named capability through the existing owner-native path.
3. Persist only the projection this wave is authorized to own.
4. Expose a real internal consumer or audit receipt that reads the new output.
5. Carry unavailable, stale, correction, and quarantine states visibly.
6. Prove the result through the real settled production path.
7. Update AgentOS and write the continuation handoff before stopping.

### Data, contract, time, null, and correction behavior

- No event or source date may be treated as known before its lawful publication/first-seen/system clock.
- Missing, not covered, stale, corrected, quarantined, and inapplicable are distinct states; none maps to zero.
- Existing candidate/outcome identities and owner schemas are extended or referenced, never forked.
- A correction is additive. An already-published prediction or candidate observation is never silently overwritten.
- Every run binds exact input/source digests and definition hashes.
- Partial sessions, unresolved identities, or incomplete coverage fail closed for the affected output.

### Deterministic, statistical, and model-generated boundary

- Deterministic rules, identities, clocks, and state machines follow the frozen R3–R6 contracts.
- Statistical estimation is permitted only in waves whose mission explicitly names R4/G6 evaluation and only after preregistration.
- LLMs may summarize evidence for internal review but may not originate facts, actor identity, feature values, labels, rankings, or authority.

### Ordered implementation sequence

1. Reconcile current main and open PR collisions against this packet.
2. Freeze the exact file/path allowlist and definition hash.
3. Implement the narrow vertical and mutation-detecting tests.
4. Add the real consumer/audit in the same PR.
5. Run targeted and closure/owner tests.
6. Obtain independent adversarial review.
7. Merge under normal governance.
8. Execute and record the real production proof.
9. Update the workstream state honestly: merge = `BUILT_NOT_PROVEN`; real proof = `PROVEN_LIVE`/`done` where applicable.

### Acceptance tests

- exact formula and support rules
- no bar after cutoff
- single compute
- null reasons not zero
- candidate writer keep-first preserved
- all board outputs byte-identical
- real candidate rows and audit receipt

### Required real production proof

One real settled asia-close writes the feature anatomy across the full canonical candidate population; audit consumer reproduces counts and source digests.

### Stop condition

Stop on PR-0B collision, candidate digest ambiguity, stale/partial session, or inability to preserve correction receipts.

### Required continuation handoff

Return `STATUS / RESULT / EVIDENCE / PRODUCTION PROOF / GAPS / DEVIATIONS / DISCOVERIES / EXACT NEXT ACTION`. Name every changed path, definition hash, source clock, unavailable state, and owner collision. Do not launch the next wave in the same session unless this packet explicitly marks it parallel and its dependencies are already proven.

## I1A-T2 — Bad-day resilience vertical

**Status:** `DORMANT`  
**Owner:** Fable or bounded builder under Fable review  
**Dependencies:** I1A-T1

### Observable mission

Add market-only bad-day resilience as a second independently useful candidate-anatomy vertical; sector channel remains unavailable until PIT membership is proven.

### Why it matters

Candidate rows show whether external down sessions have become less damaging.

### Exact scope

- one feature, market channel, candidate projection, audit consumer

### Explicit non-goals

- no sector backcast
- no feature stacking score

### Complete user and machine journey

1. Resolve the canonical source population and definition era before computing anything.
2. Produce the named capability through the existing owner-native path.
3. Persist only the projection this wave is authorized to own.
4. Expose a real internal consumer or audit receipt that reads the new output.
5. Carry unavailable, stale, correction, and quarantine states visibly.
6. Prove the result through the real settled production path.
7. Update AgentOS and write the continuation handoff before stopping.

### Data, contract, time, null, and correction behavior

- No event or source date may be treated as known before its lawful publication/first-seen/system clock.
- Missing, not covered, stale, corrected, quarantined, and inapplicable are distinct states; none maps to zero.
- Existing candidate/outcome identities and owner schemas are extended or referenced, never forked.
- A correction is additive. An already-published prediction or candidate observation is never silently overwritten.
- Every run binds exact input/source digests and definition hashes.
- Partial sessions, unresolved identities, or incomplete coverage fail closed for the affected output.

### Deterministic, statistical, and model-generated boundary

- Deterministic rules, identities, clocks, and state machines follow the frozen R3–R6 contracts.
- Statistical estimation is permitted only in waves whose mission explicitly names R4/G6 evaluation and only after preregistration.
- LLMs may summarize evidence for internal review but may not originate facts, actor identity, feature values, labels, rankings, or authority.

### Ordered implementation sequence

1. Reconcile current main and open PR collisions against this packet.
2. Freeze the exact file/path allowlist and definition hash.
3. Implement the narrow vertical and mutation-detecting tests.
4. Add the real consumer/audit in the same PR.
5. Run targeted and closure/owner tests.
6. Obtain independent adversarial review.
7. Merge under normal governance.
8. Execute and record the real production proof.
9. Update the workstream state honestly: merge = `BUILT_NOT_PROVEN`; real proof = `PROVEN_LIVE`/`done` where applicable.

### Acceptance tests

- pre-t bad-session threshold
- trailing beta only
- market benchmark completeness
- same invariance/proof as I1A-T1

### Required real production proof

Real nightly rows plus audit receipt.

### Stop condition

Unavailable benchmark or insufficient bad sessions returns explicit NOT_COVERED.

### Required continuation handoff

Return `STATUS / RESULT / EVIDENCE / PRODUCTION PROOF / GAPS / DEVIATIONS / DISCOVERIES / EXACT NEXT ACTION`. Name every changed path, definition hash, source clock, unavailable state, and owner collision. Do not launch the next wave in the same session unless this packet explicitly marks it parallel and its dependencies are already proven.

## I1A-T3 — Constructive-volume asymmetry vertical

**Status:** `DORMANT`  
**Owner:** Bounded builder under Fable review  
**Dependencies:** I1A-T1

### Observable mission

Separate constructive from destructive use of volume and beat the raw-volume negative control.

### Why it matters

Candidate rows distinguish selective demand from generic volume spikes.

### Exact scope

- one feature, raw-volume control, candidate projection, audit consumer

### Explicit non-goals

- no hidden-actor label
- no generic volume bullishness

### Complete user and machine journey

1. Resolve the canonical source population and definition era before computing anything.
2. Produce the named capability through the existing owner-native path.
3. Persist only the projection this wave is authorized to own.
4. Expose a real internal consumer or audit receipt that reads the new output.
5. Carry unavailable, stale, correction, and quarantine states visibly.
6. Prove the result through the real settled production path.
7. Update AgentOS and write the continuation handoff before stopping.

### Data, contract, time, null, and correction behavior

- No event or source date may be treated as known before its lawful publication/first-seen/system clock.
- Missing, not covered, stale, corrected, quarantined, and inapplicable are distinct states; none maps to zero.
- Existing candidate/outcome identities and owner schemas are extended or referenced, never forked.
- A correction is additive. An already-published prediction or candidate observation is never silently overwritten.
- Every run binds exact input/source digests and definition hashes.
- Partial sessions, unresolved identities, or incomplete coverage fail closed for the affected output.

### Deterministic, statistical, and model-generated boundary

- Deterministic rules, identities, clocks, and state machines follow the frozen R3–R6 contracts.
- Statistical estimation is permitted only in waves whose mission explicitly names R4/G6 evaluation and only after preregistration.
- LLMs may summarize evidence for internal review but may not originate facts, actor identity, feature values, labels, rankings, or authority.

### Ordered implementation sequence

1. Reconcile current main and open PR collisions against this packet.
2. Freeze the exact file/path allowlist and definition hash.
3. Implement the narrow vertical and mutation-detecting tests.
4. Add the real consumer/audit in the same PR.
5. Run targeted and closure/owner tests.
6. Obtain independent adversarial review.
7. Merge under normal governance.
8. Execute and record the real production proof.
9. Update the workstream state honestly: merge = `BUILT_NOT_PROVEN`; real proof = `PROVEN_LIVE`/`done` where applicable.

### Acceptance tests

- session classification fixed before outcomes
- support floors
- raw-volume control emitted beside candidate
- invariance/proof

### Required real production proof

Real nightly rows plus control-comparison audit.

### Stop condition

If the construction collapses to raw volume under frozen anatomy, retain only the control and close the candidate.

### Required continuation handoff

Return `STATUS / RESULT / EVIDENCE / PRODUCTION PROOF / GAPS / DEVIATIONS / DISCOVERIES / EXACT NEXT ACTION`. Name every changed path, definition hash, source clock, unavailable state, and owner collision. Do not launch the next wave in the same session unless this packet explicitly marks it parallel and its dependencies are already proven.

## I1A-T4 — Reclaim-and-hold quality vertical

**Status:** `DORMANT`  
**Owner:** Fable due state-machine and pivot overlap complexity  
**Dependencies:** I1A-T1

### Observable mission

Project the frozen component vector and interpretable state without fitting a scalar or duplicating Prophet entry logic.

### Why it matters

Candidate rows reveal reclaim speed, hold quality, adverse excursion, and failure frequency.

### Exact scope

- component vector, state, pivot provenance, overlap audit

### Explicit non-goals

- no fitted score
- no post-cutoff hold
- no copied entry status

### Complete user and machine journey

1. Resolve the canonical source population and definition era before computing anything.
2. Produce the named capability through the existing owner-native path.
3. Persist only the projection this wave is authorized to own.
4. Expose a real internal consumer or audit receipt that reads the new output.
5. Carry unavailable, stale, correction, and quarantine states visibly.
6. Prove the result through the real settled production path.
7. Update AgentOS and write the continuation handoff before stopping.

### Data, contract, time, null, and correction behavior

- No event or source date may be treated as known before its lawful publication/first-seen/system clock.
- Missing, not covered, stale, corrected, quarantined, and inapplicable are distinct states; none maps to zero.
- Existing candidate/outcome identities and owner schemas are extended or referenced, never forked.
- A correction is additive. An already-published prediction or candidate observation is never silently overwritten.
- Every run binds exact input/source digests and definition hashes.
- Partial sessions, unresolved identities, or incomplete coverage fail closed for the affected output.

### Deterministic, statistical, and model-generated boundary

- Deterministic rules, identities, clocks, and state machines follow the frozen R3–R6 contracts.
- Statistical estimation is permitted only in waves whose mission explicitly names R4/G6 evaluation and only after preregistration.
- LLMs may summarize evidence for internal review but may not originate facts, actor identity, feature values, labels, rankings, or authority.

### Ordered implementation sequence

1. Reconcile current main and open PR collisions against this packet.
2. Freeze the exact file/path allowlist and definition hash.
3. Implement the narrow vertical and mutation-detecting tests.
4. Add the real consumer/audit in the same PR.
5. Run targeted and closure/owner tests.
6. Obtain independent adversarial review.
7. Merge under normal governance.
8. Execute and record the real production proof.
9. Update the workstream state honestly: merge = `BUILT_NOT_PROVEN`; real proof = `PROVEN_LIVE`/`done` where applicable.

### Acceptance tests

- pivot known by decision session
- pending states explicit
- Prophet-overlap ablation fields
- invariance/proof

### Required real production proof

Real nightly rows and reference-case state receipts.

### Stop condition

If pivot or completed-reclaim identity cannot be stable point-in-time, do not emit a state.

### Required continuation handoff

Return `STATUS / RESULT / EVIDENCE / PRODUCTION PROOF / GAPS / DEVIATIONS / DISCOVERIES / EXACT NEXT ACTION`. Name every changed path, definition hash, source clock, unavailable state, and owner collision. Do not launch the next wave in the same session unless this packet explicitly marks it parallel and its dependencies are already proven.

## I1B-MEASURE — Exact target plus first immutable prediction/grade sidecar vertical

**Status:** `BLOCKED_EXACT_AND_FEATURE_ACCRUAL`  
**Owner:** Fable  
**Dependencies:** DEP-ID-ELIG, I1A-T1

### Observable mission

Ship the referential sidecar with one real feature, the R4 H10 exact target, access dispositions, predictions written before outcomes, and correction-safe grades—never an empty generic foundation.

### Why it matters

Mastermind can perform an authority-free, exact, prospective test of one CN-Limit feature against canonical candidate and outcome identities.

### Exact scope

- risk-set observation/prediction/grade/correction logical parts
- monthly immutable partitions
- one feature and target
- real audit consumer

### Explicit non-goals

- no second candidate population
- no score/rank/gate
- no imported withdrawn rows
- no historical seed from adjusted plane

### Complete user and machine journey

1. Resolve the canonical source population and definition era before computing anything.
2. Produce the named capability through the existing owner-native path.
3. Persist only the projection this wave is authorized to own.
4. Expose a real internal consumer or audit receipt that reads the new output.
5. Carry unavailable, stale, correction, and quarantine states visibly.
6. Prove the result through the real settled production path.
7. Update AgentOS and write the continuation handoff before stopping.

### Data, contract, time, null, and correction behavior

- No event or source date may be treated as known before its lawful publication/first-seen/system clock.
- Missing, not covered, stale, corrected, quarantined, and inapplicable are distinct states; none maps to zero.
- Existing candidate/outcome identities and owner schemas are extended or referenced, never forked.
- A correction is additive. An already-published prediction or candidate observation is never silently overwritten.
- Every run binds exact input/source digests and definition hashes.
- Partial sessions, unresolved identities, or incomplete coverage fail closed for the affected output.

### Deterministic, statistical, and model-generated boundary

- Deterministic rules, identities, clocks, and state machines follow the frozen R3–R6 contracts.
- Statistical estimation is permitted only in waves whose mission explicitly names R4/G6 evaluation and only after preregistration.
- LLMs may summarize evidence for internal review but may not originate facts, actor identity, feature values, labels, rankings, or authority.

### Ordered implementation sequence

1. Reconcile current main and open PR collisions against this packet.
2. Freeze the exact file/path allowlist and definition hash.
3. Implement the narrow vertical and mutation-detecting tests.
4. Add the real consumer/audit in the same PR.
5. Run targeted and closure/owner tests.
6. Obtain independent adversarial review.
7. Merge under normal governance.
8. Execute and record the real production proof.
9. Update the workstream state honestly: merge = `BUILT_NOT_PROVEN`; real proof = `PROVEN_LIVE`/`done` where applicable.

### Acceptance tests

- exact schemas
- candidate and board digest binding
- keep-first contradiction abort
- no-fill visible
- calendar and outcome correction branches
- nightly sole advancer
- real end-to-end row maturity

### Required real production proof

Real candidate input → pre-outcome prediction row → later exact grade/correction receipt → audit report; all ids/digests reconcile.

### Stop condition

Stop if a full-market target is unavailable or the sidecar starts enumerating candidates independently.

### Required continuation handoff

Return `STATUS / RESULT / EVIDENCE / PRODUCTION PROOF / GAPS / DEVIATIONS / DISCOVERIES / EXACT NEXT ACTION`. Name every changed path, definition hash, source clock, unavailable state, and owner collision. Do not launch the next wave in the same session unless this packet explicitly marks it parallel and its dependencies are already proven.

## M2-R4-BATTERY — Frozen historical/rolling-origin R4 active-family battery

**Status:** `BLOCKED_DATA_AND_PREREG`  
**Owner:** Fable research orchestrator; Grok fixtures/controls  
**Dependencies:** I1B-MEASURE, I1A-T2, I1A-T3, I1A-T4

### Observable mission

Freeze formulas, populations, baselines, controls, nulls, folds, metrics and sample floors before opening exact outcomes; then run once and issue advance/kill/accrue verdicts.

### Why it matters

The program knows which, if any, transition family adds exact first-board hazard beyond carrier and Prophet under valid nulls.

### Exact scope

- four primary features
- secondary compression only after overlap audit
- five/six baselines
- nine controls
- U0-U5
- R4 models/nulls

### Explicit non-goals

- no interaction rescue of failed main effects
- no threshold shopping
- no featured-only headline
- no production score

### Complete user and machine journey

1. Resolve the canonical source population and definition era before computing anything.
2. Produce the named capability through the existing owner-native path.
3. Persist only the projection this wave is authorized to own.
4. Expose a real internal consumer or audit receipt that reads the new output.
5. Carry unavailable, stale, correction, and quarantine states visibly.
6. Prove the result through the real settled production path.
7. Update AgentOS and write the continuation handoff before stopping.

### Data, contract, time, null, and correction behavior

- No event or source date may be treated as known before its lawful publication/first-seen/system clock.
- Missing, not covered, stale, corrected, quarantined, and inapplicable are distinct states; none maps to zero.
- Existing candidate/outcome identities and owner schemas are extended or referenced, never forked.
- A correction is additive. An already-published prediction or candidate observation is never silently overwritten.
- Every run binds exact input/source digests and definition hashes.
- Partial sessions, unresolved identities, or incomplete coverage fail closed for the affected output.

### Deterministic, statistical, and model-generated boundary

- Deterministic rules, identities, clocks, and state machines follow the frozen R3–R6 contracts.
- Statistical estimation is permitted only in waves whose mission explicitly names R4/G6 evaluation and only after preregistration.
- LLMs may summarize evidence for internal review but may not originate facts, actor identity, feature values, labels, rankings, or authority.

### Ordered implementation sequence

1. Reconcile current main and open PR collisions against this packet.
2. Freeze the exact file/path allowlist and definition hash.
3. Implement the narrow vertical and mutation-detecting tests.
4. Add the real consumer/audit in the same PR.
5. Run targeted and closure/owner tests.
6. Obtain independent adversarial review.
7. Merge under normal governance.
8. Execute and record the real production proof.
9. Update the workstream state honestly: merge = `BUILT_NOT_PROVEN`; real proof = `PROVEN_LIVE`/`done` where applicable.

### Acceptance tests

- pre-outcome hash freeze
- mutation-detecting leakage battery
- Holm/BH rules
- rolling folds
- commercial access metrics
- complete ore ledger

### Required real production proof

Reproducible exact-plane run and independent adversarial review; result receipts byte-stable under identical inputs.

### Stop condition

A NULL or kill verdict is complete. Do not reopen except through a new preregistered construction.

### Required continuation handoff

Return `STATUS / RESULT / EVIDENCE / PRODUCTION PROOF / GAPS / DEVIATIONS / DISCOVERIES / EXACT NEXT ACTION`. Name every changed path, definition hash, source clock, unavailable state, and owner collision. Do not launch the next wave in the same session unless this packet explicitly marks it parallel and its dependencies are already proven.

## I1C-G6 — Prospective G6 measurement shadow

**Status:** `BLOCKED_SURVIVING_FEATURE`  
**Owner:** Fable  
**Dependencies:** M2-R4-BATTERY

### Observable mission

Accrue exactly one preregistered challenger prospectively until the R4 floors are met; evaluate without model shopping or live authority.

### Why it matters

A surviving feature earns or fails `ELIGIBLE_FOR_SHADOW_IMPLEMENTATION_COMMISSION` on real future evidence.

### Exact scope

- one definition era
- ≥120 candidate sessions
- ≥2 regimes
- ≥60 exact first-board events
- access/calibration/coverage monitoring

### Explicit non-goals

- no partial peeks
- no retuning
- no public probability

### Complete user and machine journey

1. Resolve the canonical source population and definition era before computing anything.
2. Produce the named capability through the existing owner-native path.
3. Persist only the projection this wave is authorized to own.
4. Expose a real internal consumer or audit receipt that reads the new output.
5. Carry unavailable, stale, correction, and quarantine states visibly.
6. Prove the result through the real settled production path.
7. Update AgentOS and write the continuation handoff before stopping.

### Data, contract, time, null, and correction behavior

- No event or source date may be treated as known before its lawful publication/first-seen/system clock.
- Missing, not covered, stale, corrected, quarantined, and inapplicable are distinct states; none maps to zero.
- Existing candidate/outcome identities and owner schemas are extended or referenced, never forked.
- A correction is additive. An already-published prediction or candidate observation is never silently overwritten.
- Every run binds exact input/source digests and definition hashes.
- Partial sessions, unresolved identities, or incomplete coverage fail closed for the affected output.

### Deterministic, statistical, and model-generated boundary

- Deterministic rules, identities, clocks, and state machines follow the frozen R3–R6 contracts.
- Statistical estimation is permitted only in waves whose mission explicitly names R4/G6 evaluation and only after preregistration.
- LLMs may summarize evidence for internal review but may not originate facts, actor identity, feature values, labels, rankings, or authority.

### Ordered implementation sequence

1. Reconcile current main and open PR collisions against this packet.
2. Freeze the exact file/path allowlist and definition hash.
3. Implement the narrow vertical and mutation-detecting tests.
4. Add the real consumer/audit in the same PR.
5. Run targeted and closure/owner tests.
6. Obtain independent adversarial review.
7. Merge under normal governance.
8. Execute and record the real production proof.
9. Update the workstream state honestly: merge = `BUILT_NOT_PROVEN`; real proof = `PROVEN_LIVE`/`done` where applicable.

### Acceptance tests

- immutable rows before outcomes
- era and correction integrity
- complete candidate coverage
- predeclared verdict logic

### Required real production proof

Real prospective dossier with all floors and source receipts; independent review reproduces verdict.

### Stop condition

If floors are not met, state ACCRUING; if integrity breaks, quarantine era; never extrapolate.

### Required continuation handoff

Return `STATUS / RESULT / EVIDENCE / PRODUCTION PROOF / GAPS / DEVIATIONS / DISCOVERIES / EXACT NEXT ACTION`. Name every changed path, definition hash, source clock, unavailable state, and owner collision. Do not launch the next wave in the same session unless this packet explicitly marks it parallel and its dependencies are already proven.

## I2A-ORDER — Fixed-candidate off-path ordering shadow

**Status:** `BLOCKED_G6`  
**Owner:** Fable  
**Dependencies:** I1C-G6

### Observable mission

On a frozen U3 raw-buyable set, compute a coverage-atomic challenger order and replay the same Prophet admission rule and caps for counterfactual shelf allocation.

### Why it matters

Mastermind can observe whether limit-hazard information improves scarce shelf allocation without changing production.

### Exact scope

- paired candidate references
- champion/challenger ranks
- same-rule/same-cap shelf
- comparison session/candidate records

### Explicit non-goals

- no live rank
- no changed candidate set
- no admission rule change
- no per-row fallback mixing

### Complete user and machine journey

1. Resolve the canonical source population and definition era before computing anything.
2. Produce the named capability through the existing owner-native path.
3. Persist only the projection this wave is authorized to own.
4. Expose a real internal consumer or audit receipt that reads the new output.
5. Carry unavailable, stale, correction, and quarantine states visibly.
6. Prove the result through the real settled production path.
7. Update AgentOS and write the continuation handoff before stopping.

### Data, contract, time, null, and correction behavior

- No event or source date may be treated as known before its lawful publication/first-seen/system clock.
- Missing, not covered, stale, corrected, quarantined, and inapplicable are distinct states; none maps to zero.
- Existing candidate/outcome identities and owner schemas are extended or referenced, never forked.
- A correction is additive. An already-published prediction or candidate observation is never silently overwritten.
- Every run binds exact input/source digests and definition hashes.
- Partial sessions, unresolved identities, or incomplete coverage fail closed for the affected output.

### Deterministic, statistical, and model-generated boundary

- Deterministic rules, identities, clocks, and state machines follow the frozen R3–R6 contracts.
- Statistical estimation is permitted only in waves whose mission explicitly names R4/G6 evaluation and only after preregistration.
- LLMs may summarize evidence for internal review but may not originate facts, actor identity, feature values, labels, rankings, or authority.

### Ordered implementation sequence

1. Reconcile current main and open PR collisions against this packet.
2. Freeze the exact file/path allowlist and definition hash.
3. Implement the narrow vertical and mutation-detecting tests.
4. Add the real consumer/audit in the same PR.
5. Run targeted and closure/owner tests.
6. Obtain independent adversarial review.
7. Merge under normal governance.
8. Execute and record the real production proof.
9. Update the workstream state honestly: merge = `BUILT_NOT_PROVEN`; real proof = `PROVEN_LIVE`/`done` where applicable.

### Acceptance tests

- champion discovery at runtime
- effective-era key
- board/candidate digest binding
- coverage atomicity
- production bytes invariant
- counterfactual outcomes share one grader

### Required real production proof

Real nightly shadow comparisons with canonical board hash; deleting the challenger leaves all production artifacts byte-identical.

### Stop condition

Any population, grade, or authority divergence invalidates paired-row treatment and requires Sol ruling.

### Required continuation handoff

Return `STATUS / RESULT / EVIDENCE / PRODUCTION PROOF / GAPS / DEVIATIONS / DISCOVERIES / EXACT NEXT ACTION`. Name every changed path, definition hash, source clock, unavailable state, and owner collision. Do not launch the next wave in the same session unless this packet explicitly marks it parallel and its dependencies are already proven.

## A5-DECISION — Potential bounded live ordering decision

**Status:** `NOT_AUTHORIZED`  
**Owner:** Sol/Chairman with Fable evidence packet  
**Dependencies:** I2A-ORDER

### Observable mission

Decide—not implement by default—whether one challenger earns bounded ordering authority inside unchanged Prophet admission.

### Why it matters

Only if explicitly approved: live order may use a proven limit-hazard input while Prophet retains admission and execution authority.

### Exact scope

- decision record and, only after approval, separate build commission

### Explicit non-goals

- no automatic promotion
- no score/gate/size/public probability

### Complete user and machine journey

1. Resolve the canonical source population and definition era before computing anything.
2. Produce the named capability through the existing owner-native path.
3. Persist only the projection this wave is authorized to own.
4. Expose a real internal consumer or audit receipt that reads the new output.
5. Carry unavailable, stale, correction, and quarantine states visibly.
6. Prove the result through the real settled production path.
7. Update AgentOS and write the continuation handoff before stopping.

### Data, contract, time, null, and correction behavior

- No event or source date may be treated as known before its lawful publication/first-seen/system clock.
- Missing, not covered, stale, corrected, quarantined, and inapplicable are distinct states; none maps to zero.
- Existing candidate/outcome identities and owner schemas are extended or referenced, never forked.
- A correction is additive. An already-published prediction or candidate observation is never silently overwritten.
- Every run binds exact input/source digests and definition hashes.
- Partial sessions, unresolved identities, or incomplete coverage fail closed for the affected output.

### Deterministic, statistical, and model-generated boundary

- Deterministic rules, identities, clocks, and state machines follow the frozen R3–R6 contracts.
- Statistical estimation is permitted only in waves whose mission explicitly names R4/G6 evaluation and only after preregistration.
- LLMs may summarize evidence for internal review but may not originate facts, actor identity, feature values, labels, rankings, or authority.

### Ordered implementation sequence

1. Reconcile current main and open PR collisions against this packet.
2. Freeze the exact file/path allowlist and definition hash.
3. Implement the narrow vertical and mutation-detecting tests.
4. Add the real consumer/audit in the same PR.
5. Run targeted and closure/owner tests.
6. Obtain independent adversarial review.
7. Merge under normal governance.
8. Execute and record the real production proof.
9. Update the workstream state honestly: merge = `BUILT_NOT_PROVEN`; real proof = `PROVEN_LIVE`/`done` where applicable.

### Acceptance tests

- prospective champion/challenger evidence
- access and capacity noninferiority
- drift/rollback design
- Chairman/Sol approval

### Required real production proof

Separate later wave; not defined as complete in R6.

### Stop condition

R6 grants no A5 authority.

### Required continuation handoff

Return `STATUS / RESULT / EVIDENCE / PRODUCTION PROOF / GAPS / DEVIATIONS / DISCOVERIES / EXACT NEXT ACTION`. Name every changed path, definition hash, source clock, unavailable state, and owner collision. Do not launch the next wave in the same session unless this packet explicitly marks it parallel and its dependencies are already proven.


---

## Appendix A — Review checklist before every merge

- Primary persona/machine completes the intended task.
- Full thesis preserved while PR scope remains narrow.
- No spec or empty store called shipped.
- No foundation substituted for product/intelligence.
- Precision and clocks match the claim.
- Existing owners reused, not copied.
- Real production result observable.
- New capability named precisely.
- Negative controls and unavailable states retained.
- Durable discovery and exact continuation recorded.

## Appendix B — Exact next action

Start `R6-0` only. The PR is records-only. After it merges, return to Sol with the merged SHA, changed paths, AgentOS validation, stale-reference sweep, and the exact proposed commissions for `P0-ST`, `DEP-CAI`, and `DEP-EXACT`. Do not start any of those implementation waves inside R6-0.
