# P0-ST — Real Asia-Close Production Proof (2026-08-20)

**Wave:** P0-ST (WS:CN-LIMIT-ALPHA, R6 command packet §P0-ST)
**Repair PR:** #6047, squash-merged `609d883506b3` (2026-08-20 ~07:44Z)
**This record:** the required real post-merge asia-close production proof. Records-only; the
frozen build-time receipt `P0_ST_BAND_REPAIR_RECEIPT_2026-08-19.{md,json}` is untouched.
The sibling JSON (`P0_ST_PRODUCTION_PROOF_2026-08-20.json`) is the post-run replay output,
regenerated by the merged instrument `p0_st_band_replay.py` against the post-run stores.

## 1. The run

- Workflow: `asia-close.yml`, run ID **32348780228** (`workflow_dispatch`, dispatched at the
  08:26 UTC data floor; the day's scheduled slots no-opped on the gate's day-window).
- Checkout head `e2ba19c17d73` — verified descendant of merge `609d883506b3`
  (`git merge-base --is-ancestor` = yes).
- Jobs: gate success; **asia success**, completed 2026-08-20T15:20:19Z on the self-hosted
  Mac Studio production runner.
- Production commit written by the run: **`baf4cf7c9291`** "engine: asia dashboards
  2026-08-20" (touches `data/china_microstructure/*` and `site/chinastatedata/*`), plus
  `3b1db927f71e` (nightly timings). Both on `origin/main`.

## 2. Owner-native output reports the post-2026-07-06 regime

`site/chinastatedata/microstructure.json` as committed by the run (`as_of: 2026-08-20`):

- `metadata.st_band_regime = {main_st_width_pct: 10.0, effective_from: "2026-07-06",
  prior_width_pct: 5.0, receipt: research/cn_limit/P0_ST_BAND_REPAIR_RECEIPT_2026-08-19.md}`
- `st_flags_note` ends: "From 2026-07-06 the main-board risk-warning band is ±10% (2026
  trading-rules revision), identical to ordinary main-board names."
- 81 name packets; width set = **{10.0, 20.0}** — no packet carries the retired 5%.
- No current standout packet is a risk-warning name (honest absence, not a vacuous pass);
  the engine-level width for main-board ST names on covered dates is pinned to 10% by the
  merged boundary tests and demonstrated on real names in §4.

## 3. Post-run stores carry no stale 5% interpretation

`data/china_microstructure/limit_events.parquet` after the run: **60,610 rows** (advanced
through 2026-08-20), **0 rows at `limit_width == 5.0`** across the store's full 2011→ depth.
Breadth/failed-seal/ladder aggregates derive from these rows; no 5%-band interpretation
survives anywhere in the produced artifacts.

## 4. Post-run bounded replay — the repair mattered on its first production session

The run's collector also refreshed `data/china_st/st_snapshot.parquet` (100 → **207** names,
`asof: 2026-08-20` — the build-time receipt's stale-snapshot gap self-healed). That growth
changed the affected universe from `['600079.SS']` to **`['600079.SS', '600745.SS']`**
(*ST闻泰, newly risk-warning), so the build-time expectation pin correctly reads
`affected_universe_matches_expectation: false` — a real state change, reported rather than
forced.

Replay over 2026-07-06 → 2026-08-20 (34 sessions each, first day genuinely scored per the
#6047 M2 fix; `detector_scoring_matches_session_table: true` both names):

- **600079.SS** — both arms empty; `diff.identical: true`. Unchanged from build time.
- **600745.SS** — the arms **diverge**: the current (lawful ±10%) arm emits exactly one
  event (`2026-07-20 failed_down_seal @10.0`); the superseded always-5% arm would have
  emitted **17 phantom events** — including phantom `sealed_up` (07-10, 08-11) and
  `sealed_down` (07-14, 07-16, 07-20) full seals at the retired width, i.e. wrong
  event/breadth/failed-seal/ladder rows on every one of those sessions.
- **Store parity:** the store's post-07-06 rows for 600745.SS equal the current arm
  **exactly** (that single 10% row). **Zero corrections are required to any produced
  store.** The top-level `zero_corrections_required` flag in the JSON reads `false` by its
  frozen definition ("both arms identical for every affected name") — post-merge that
  non-identity is the *measured proof the repair is consequential in production*, not a
  store defect: had #6047 not merged before this run, tonight's write would have persisted
  those 17 phantom rows.
- **Non-vacuity:** the #6047 M3 guard operated on real input and flipped on real divergence
  (`bool(per_name) and all(identical)`); nothing passed vacuously.
- `definition_hash_sha256` unchanged from the merged receipt:
  `e0c70f39f62e7639355128644f872c4e992699524bbdca775f24f1e1ad45e4a4`.
- **Machine-readable conclusion:** the frozen replay predicate above is preserved
  unmodified as generated evidence; the unambiguous production conclusion
  (`rule_arms_identical: false`, `produced_store_matches_lawful_arm: true`,
  `produced_store_corrections_required: false`, `repair_consequential: true`,
  `superseded_only_event_count: 17`) is carried by the wrapper receipt
  `research/cn_limit/P0_ST_PRODUCTION_CONCLUSION_2026-08-20.json`
  (`cn_limit.p0_st_production_conclusion.v1`). Machine consumers of the store's
  correctness read the wrapper, never the replay predicate.

## 5. Quarantine-lift scope (binding)

The lift recorded in WS-CN-LIMIT-ALPHA covers **only** the P0-ST rule defect (main-board
risk-warning band era from 2026-07-06 in the canonical owner path). It implies **no**
exact-plane authorization, **no** full-A gate change, **no** tolerant-label promotion, and
**no** relaxation of `DNR:KILL-CN-ADJUSTED-TAPE-LEGAL-LIMIT`, which binds exactly as before.

## 6. Gaps carried forward (unchanged by this proof)

- Pre-2026-07-06 ST-width blindness (store-coverage caveat) stands; historical ST
  membership backfill remains out of scope (P0-ST non-goal).
- The raw store remains the ~1,848-name survivor slice; census statements bind that
  universe. 600745.SS's arrival shows the affected universe is dynamic as ST membership
  and the raw universe evolve — future full-A work re-measures it (behind its own gates).
