# TOP ANATOMY — Extended-Move Maturation & Top Recognition — Masterplan (by Fable)

**Chartered:** 2026-08-10, from an operator-supplied commissioning brief (not a repository-tracked authority): *"use the entire historical record as a library of lived market experience, systematically reverse-engineer what was observable before historical tops, and convert those recurring precursors into a real-time recognition system."*
**Status:** CHARTERED — the phase-0 prereg is frozen; engine, results, and any display surface require separate reviewed PRs.
**Docket relationship:** this program is the **extended-move maturation arm of the Short-Side / Breakdown Intelligence lobe** (`research/SHORT_SIDE_MASTERPLAN_BY_FABLE.md`, docket L1). It absorbs species S1⁻ (Cohort Euphoria Distribution), S2⁻ (Donor Exhaustion) and S13⁻ (Within-Sector Leader Fade) at episode scale — those rows graduate here rather than getting parallel preregs. All L1 scope fences and evidence constraints are inherited (§3, §6).
**Family (phase-0):** `top_anatomy_p0` — prereg `research/top_anatomy/TOPA_PHASE0_PREREG.md`, frozen before any result is computed.

---

## §0 ACCEPTANCE GATES (not done unless)

- **G0.1 Freeze before results.** The prereg commit precedes the first harness run in git history. Any construction change after first results is a new, labeled arm — never an in-place edit.
- **G0.2 Survivorship honesty.** The Wide track's inclusion of dead names is *verified, not assumed*: the phase-0 report names ≥3 known 2022–2024 delistings found in the tape with bars through their final trading day. The Deep track's survivorship tilt is disclosed in every artifact and report section that uses it ("who is missing: names that topped and died before basket curation" — per house law `discovered-rule-must-cover-the-motivating-exemplars`).
- **G0.3 Honest N.** Every E1 feature headline carries episode-level N (distinct episodes, never fires/ticker-days), an episode-month-block bootstrap CI, and BH-FDR correction within feature family; E1b AUC and ΔAUC carry episode-block CIs. Nulls print as counts ("k of 36 separate").
- **G0.4 Lead-time honesty.** Every claimed discriminator carries a lead-time profile. A feature separating only in the {peak..peak+5td} bucket is labeled **POST-TOP CONFIRMATION** and may never be described as detection; the distinction is explicit in the report's tables.
- **G0.5 Coverage gate.** The report LEADS with the discovered discriminators run against the motivating live exemplars and the current regime (2026-08 tape: the extended gold/PGM-miner cohort per `research/CASE_STUDY_GOLD_REAL_RATE_PEAK_2026_08.md`, and the extended AI leaders) — display-tier readout, no authority. A red-team pass by an Opus `reviewer` happens BEFORE results are presented (house adjudication coverage gate, operator 2026-08-10).
- **G0.6 Tier discipline.** Everything ships display-tier: no rank, gate, size, or escalation anywhere; no blended composite score on any ranked surface (constitution Article 2; Intel-Hub audit restatement). Promotion to any authority requires a separate future prereg, the L1 ladder (§5 of the short-side masterplan), and an explicit operator ruling. **No short calls ever** (`DNR:KILL-DIRECTIONAL-SHORTING`); **no exit rules ever** (L1 §7 graveyard: "drawdown control is an ENTRY problem" — our outputs are entry-side avoidance or descriptive winner-health context only).
- **G0.7 Surface law.** The future Wave-1 surface adopts the binding lessons recorded by `HOLD-IGNITION-SURFACES`: its display gate is declared in this doc §5 before the surface exists; it has an honest-null state ("nothing maturing — that is a finding"); it never force-ranks a dead tape; falsifier register never appears front-facing; "validated" never appears; EN/ZH parity; design doctrine + frontend-design skill before any markup. This charter does not itself clear a future implementation's review or operator gate.
- **G0.8 Ship loop.** Each wave lands commit → push → PR → CI → same-day squash-merge → live verification. No child-agent self-merge of the flagship surface.

## §1 Objective and thesis

The stack has strong bottom/entry perception (bottom radar, turn watch, ignition, reversal cohort) and index-level top perception (froth_fragility, risk radar). It has **nothing that watches a winner mature**. The operator's framing: most investors have systems for "what should I buy?" and almost nothing for **"when has my winner changed character?"**

Thesis (from the commissioning study, house-fitted):

1. Tops are **state transitions**, not chart patterns: healthy trend → extension → excess → weakening incremental demand → distribution → breakdown. The chain differs by episode; the states are recurring.
2. **Extension is a prerequisite for some tops, not proof of a top** ([Greenwood, Shleifer & You, *Bubbles for Fama*, JFE 2019](https://www.hbs.edu/ris/Publication%20Files/Bubbles%20for%20Fama_e680848b-4f23-478a-b6cd-e5564e6a431b.pdf): industry run-ups do not on average predict low returns, while run-up attributes help distinguish crash outcomes). Therefore the core scientific object is the **contrast**: extended-that-topped vs extended-that-continued — never "stocks that collapsed vs average stocks."
3. Hindsight is used **aggressively for labels** (episode discovery, peaks, outcomes) and **never for features** (point-in-time only, enforced by test).
4. Detecting danger early and timing an exit are different problems; we only claim the first, and only at the tier the evidence supports.

Primary economic questions (in order of product value): has this extended move entered a statistically abnormal fragility regime; what did historically similar episodes do next (analog memory, honest base rates); what is deteriorating right now, in plain words (maturation legs); and at cohort level, how much of a theme is maturing simultaneously (tomography — Wave 2, reading existing basket organs).

## §2 Ground truth — what the data can actually support

| Track | Store | Universe | Span | Volume | Adjustment | Survivorship |
|---|---|---|---|---|---|---|
| **W (Wide)** — registration track | `data/massive_stock_day/` (R2-canonical; commissioning snapshot ~20.5k parquets, actual run census required) | commissioning snapshot ~20,764 as-listed US ticker files; security-type and dead-name coverage are not assumed | 2021-07-06 → run date | yes (+transactions) | raw OHLCV; prices repaired via `split_adjust`, factor carried to OHLCV, raw printed price/dollar-volume used for eligibility; dividends unadjusted | candidate low-survivorship-bias pull; registration is blocked until G0.2 verifies terminal dead-name coverage |
| **D (Deep)** — era-stability track | `engine/price_ladder.py` adjusted rungs: `baskets_ohlcv` (~2.8k) ∪ `yahoo` (~1.1k) ∪ `data_stocks` (~240) | ~3k curated names | 1997 → present (per-name varies) | partial (rung-dependent; nulls printed with coverage counts) | split+div adjusted (yfinance) | **TILTED — curated-current universe; names that topped and died are underrepresented; topped-arm severity understated. Disclosed everywhere used.** |

- **W is the only track that can register a claim.** D exists to answer "does the W-era finding hold in 1999/2007/2015/2020?" for features computable there — confirmatory context, never standalone registration (its tilt is unfixable in-repo today; the ~18% dead-price recovery ceiling in `scripts/research/gate0_survivorship.py` is on file).
- **W's 5.1-year window is era-rich for this question**: it contains the 2021 speculative-top cohort (the densest modern blow-off sample), the 2022 bear, the 2023–24 AI run-up, and the 2025–26 regime.
- **CN is deferred** — the full-A TuShare spine is contract-only and authorization-gated (`research/CN_TUSHARE_FULL_A_SPINE_CONTRACT_2026-08-08.md`); the curated `china_search` panel has append-only-retention caveats. A CN wave charters only when the spine is live.
- Non-price planes are out of phase-0 scope by data reality, not by choice: PIT revisions start 2026-06-16; PIT short interest ~2027+; options OI snapshots are non-backfillable; FINRA daily short volume is PIT-safe but thin. They enter at Wave 2+ as incremental-value tests over the OHLCV baseline, exactly the LSR/exit-crowding pattern.
- Heavy compute runs **locally, off the render path** (`python -m scripts.research_top_anatomy_phase0 --data-root <primary-checkout>/data`), caches under `<data-root>/research/top_anatomy_p0/` (gitignored), committed outputs are small vintage-stamped JSON + `reports/top-anatomy-phase0.md`.
- **Refresh the W store mirror before every local pass (audit 2026-08-10).** The primary checkout's `data/massive_stock_day/` is an unmaintained local mirror: the store went R2-canonical on 2026-07-29 (nightly collect-job round-trip restore → incremental → publish back), nothing refreshes the local copy, and it froze at latest_date 2026-07-02 — phase-0 and the first Winner Health real-store pass both read that frozen tape. Before any `--data-root` run, refresh the owning checkout with `python -m scripts.fetch_r2 --dirs massive_stock_day --workers 24`, and only while no sibling pass is mid-scan (per-file replacements under a running reader can yield a mixed-vintage panel). Verify with `data/massive_stock_day/_manifest.json` `latest_date` ≈ last completed trading day.

## §3 Prior art and collision map (what we extend, what we must not touch)

| Asset | Scope | Relationship |
|---|---|---|
| `engine/froth_fragility.py` | INDEX/macro top-risk quadrant; owns "hidden distribution under a pinned index" (`DNR:KILL-IBD-DISTRIBUTION-DAYS`) | READ (via `latest["froth_fragility"]` — import-locked), never rebuild. TOPA is per-NAME; froth is the macro backdrop chip on our surface. |
| `engine/sector_signals.py` | Sector-ETF EXTENDED→TOPPING→SELL state machine, calibrated 1998–2026 | Benchmark + inherited law: **"topping is gated on 'extended', never on the bare down-cross"** (down-cross from non-extended = bounce trap, +0.29% vs −1.34%). Our state machine keeps this invariant at name scope. |
| `engine/cycles.py` LADDER (`TOP WATCH`/`ROLLING OVER`) | per-name short-cycle (daily-band) oscillator states feeding action_board avoid tags | **Namespace fence.** Different timescale (days, oscillator) vs ours (weeks-months, episode maturation). Our state keys use the `mat_*` prefix and never collide with LADDER vocabulary or keys. |
| `engine/roc_blowoff.py` | per-name blow-off (terminal velocity) risk chip, board display law `ZERO_SCORE_AUTHORITY` | READ + print as a leg where useful; TOPA adds the episode/maturation dimension it lacks (RS decay, effort-result, participation, analog memory). |
| `engine/theme_crowding.py`, `engine/basket_breadth_divergence.py`, `engine/basket_score.rollover_risk` | per-BASKET crowding/exhaustion/rollover | Wave-2 tomography READS these organs; TOPA never re-derives basket physics. |
| `engine/stage_analysis.py` (Stage 3) | per-name Weinstein stage context, display-only | Adjacent coarse vocabulary; our surface may print the stage chip; no logic shared. |
| Winners program W3/W4 (`research/winners/`) | onset-quality fingerprints — closed NULL twice | **Population fence:** W3/W4 condition on t0 = breakaway START; TOPA conditions on mid/late-life EXTENDED days. We reuse the **W4 resampling scaffold** (`research/winners/W4_CONTROLS_STUDY_SPEC.md` §4: matched-set Δ, month-block bootstrap, ticker-cluster robustness, gate-matching), not its exact estimator: TOPA freezes different controls, within-episode aggregation, multiplicity rule, and bootstrap budget in its own prereg. The closed W3/W4 verdicts are never reopened. |
| Short-side L1 (`SHORT_SIDE_MASTERPLAN_BY_FABLE.md`) | breakdown species, AVOID-not-SHORT | Parent docket. S1⁻/S2⁻/S13⁻ absorbed here. Inherited: RUL-P6 (asymmetry is a question), §6 evidence constraints (no PIT SI as evidence; FINRA daily short volume OK with depth stated; GEXR context-only; froth import-lock), §7 graveyard (EMA8 auto-sell dead; exit-rule routing NO-GO; SI-crowding display-only). `DNR:KILL-BD-ECON1` (avoid lens ≠ board fires) and `DNR:KILL-BD4-SPECIES` (sign-reversed, parked) stand. |
| `engine/us_turn_watch.py` | upturn deck, display-tier, recall-optimized, "operator is the second-stage filter" | **Design mirror for the Wave-1 surface** (same philosophy, opposite side): rows are context, not picks; most rows go nowhere by design; nulls printed beside plain-word reasons; own site root. |
| `engine/ignition_audit.py` | forward self-grading ledger pattern | Wave-1 forward log copies its mechanics: append-only JSONL, idempotent by (asof, key), nightly-lane-gated, TP definitions pre-registered inline before any grade matures. |
| `research/EXIT_CROWDING_PHASE0_PREREG.md` | options-tape exhaustion legs | Coordinate at Wave 2+ (their L1–L3 blocked on data); we do not duplicate their legs. |
| Prophet | graded board | **Hard fence:** `DNR:KILL-PROPHET-POP-MERGE` — nothing from TOPA touches the graded-board population; any future linkage is presentation-tier only. |

## §4 Definitions (shapes here; exact frozen numbers live in the prereg)

- **Extended day:** trailing 126d return above a hard floor AND price still near its 252d high (the "still extended, not already broken" invariant from sector_signals). Tradeability floors on price and median dollar volume. Two pre-declared sensitivity variants, report-only.
- **Episode:** contiguous extended days per name, short gaps merged. Episode-level N is the honest N everywhere.
- **Day-level race label:** from extended day d, which comes first within the horizon — drawdown of ≥X% from the post-d running peak (**TOPPED**) or a further ≥Y% gain over close_d (**CONTINUED**); else **CENSORED**. Asymmetric X/Y chosen on economics (giving back a fifth of the position vs missing moderate further upside).
- **Episode peak / terminal top:** the episode's maximum close (within episode + trailing buffer); the episode "topped" if price subsequently loses ≥X% from that peak within the sealing window without an intervening new high. `days_to_peak(d)` anchors all lead-time analysis.
- **The top ruler** (mirror of the PSS §7 bottom-timing ruler — MFE/peak-proximity, not MAE/trough): a warning's quality is measured on (i) **remaining upside** from warning day to episode peak (median, vs all-extended-days null), (ii) **peak proximity** (share of warnings within 5% of peak price), (iii) **time proximity** (share within ±10td of peak), (iv) **forward 63d excess** after warning vs extended-day base. Never fwd-return-only grading (wrong-ruler trap, `DNR:KILL-OUTCOME-AUDITION` lesson).
- **Feature library:** 36 point-in-time OHLCV features in 6 families (geometry, momentum/acceleration, volatility structure, volume/effort, relative strength, maturation/structure) — exact formulas in the prereg. PIT discipline is test-enforced (features at d use only ≤d data; cross-sectional medians use only same-day PIT universe).
- **Archetype tags (descriptive-only in phase-0):** verticality/turnover-based blow-off vs grind tags stored on episodes for stratified reads; a real taxonomy hardens in Wave 2 only if phase-0 shows archetype-dependent structure.

## §5 Waves

**W0 — Truth engine + phase-0 anatomy (separate implementation/results PRs).**
`engine/top_anatomy.py` (pure, importable: episode extraction, race labels, top ruler, feature library, matched-control assembly) + `tests/test_top_anatomy.py` (synthetic-bar tests incl. a PIT-leak guard and a race-label truth table) + `scripts/research_top_anatomy_phase0.py` (harness: tape build W+D, experiments E1–E4, today's-tape appendix) + committed summary JSON + `reports/top-anatomy-phase0.md`. Experiments: **E1** matched-pair per-feature separation (registration track W; D confirmatory), **E1b** pooled AUC increment over extension-plus-volatility baseline (ticker-grouped CV + walk-forward on W), **E2** lead-time profiles with the EARLY/MID/LATE/CONFIRMATION taxonomy, **E3** precursor ordering (descriptive), **E4** era × cap-proxy stability. Verdict semantics are DISCOVERY (ORE-law compliant): a null closes constructions, not the search space; zero surviving features ⇒ Wave 1 ships descriptive-only copy and Wave 2 pivots to the cross-sectional plane.

**W1 — "Winner Health" display surface + nightly states (separate PR after W0 adjudication).**
Display gate (declared NOW, satisfying G0.7): the surface may ship immediately **because** it makes no predictive claim — every element is either (a) a present-tense descriptive fact (extension percentile, RS-peak lag, effort-result trend, states), (b) an episode-library base rate with honest N and track disclosure ("of N similar historical episodes, k topped within 63td — library, not forecast"), or (c) an explicit null ("nothing maturing today"). Any *predictive* claim (hazard %, calibrated warning) requires the Wave-2 gauntlet and does not exist in W1.
Components: `engine/top_maturation.py` (nightly per-name maturation states `mat_state ∈ {none, trend, extended, watch, thinning, breaking}` — computed deterministically from the W0 feature library; state names avoid LADDER vocabulary), analog-memory retrieval (nearest historical episodes from the W0 tape with outcomes), `scripts/build_top_maturation.py` (daily.yml parallel band + `config/dag.yml` parity), forward log `data/top_maturation_log/` (ignition_audit pattern — accrual starts day one so any future promotion has grades), page `templates/winner_health.html.j2` → own site root, EN/ZH, designer-lane build. Synapse registration `tier: display` + `external_consumers: [mastermind:context]` (auto-manifest lobe tier), `docs/SIGNAL_BUS.md` regenerated.

**W2+ — future work, not authorized by this charter:** cross-sectional tomography (reading theme_crowding/basket organs), options/short-volume incremental legs (with exit-crowding), hazard-model + promotion prereg (only if W0 finds lead-bearing structure), Prophet presentation-tier linkage, CN wave (spine-gated), rich-tier lobe summarizer + market_packet block, archetype taxonomy hardening, and false-top analog UX ("what was different" pairs).

## §6 Epistemics & standing-law compliance

- Display-tier work may proceed only after its declared surface gates; the authority gauntlet applies at promotion. Nulls never block honest measurement or accrual, but a null factor may remain only as a separately labeled descriptive leg — never rank, gate, size, escalation, or unlabeled confluence authority.
- "Validated" never appears user-facing without allowlist backing (CI-guarded); falsifier register never front-facing (windows-not-certainties copy); ZH copy in templates AND builders; no translated `title=`.
- LLMs originate nothing here: every state, label, and base rate is deterministic math over bars (constitution Article 1 untouched; the payload carries its own standing-law string per house convention).
- Instrument verdicts are not market verdicts (operator 2026-08-09): a TOPPED race label is a statement about the declared X/Y/horizon race, and all copy scopes it that way ("no −20% event inside the window," never "the move is over").
- Trial budget: 36 features × 1 primary test, 2 sensitivity arms report-only, no grid search, no `deflated_sharpe` (no Sharpe claims in phase-0) — declared in the prereg; `check_trial_registration.py` not triggered.
- Kill-registry check at charter: no DNR row forbids per-name extended-move maturation research; adjacent rows honored as scoped in §3 (`KILL-DIRECTIONAL-SHORTING`, `KILL-IBD-DISTRIBUTION-DAYS`, `KILL-ONSET-FINGERPRINTS`, `KILL-VOLUME-FINGERPRINTS`, `KILL-STAGE-WIN-GATE`, `KILL-BD-ECON1`, `KILL-BD4-SPECIES`, `KILL-PROPHET-POP-MERGE`, `KILL-FORCED-CALLS`, `HOLD-IGNITION-SURFACES`).

## §7 Cost & latency budget

Phase-0 compute: local Mac Studio, off-lane (est. tens of minutes: ~20k-name parquet scan ≈ 50s cached per LSR precedent; feature panel ~20M rows × 36 features vectorized per-ticker). Render budget impact of W1: one builder in the daily parallel band (target <60s: ~2–5k active names × vectorized features over trailing 400d) + one page build; zero collectors; zero LLM. Forward log: one JSONL append per night. R2: none needed in W0 (caches are local-gitignored); episode tape mirrors to R2 only if it must travel.

## §8 Collision & dependency map (audited 2026-08-10)

- Merged PR #5196 (Prophet US reversal_member cohort, ANTICIPATION §6.9) — prior art on `main`, different files; any future TOPA surface remains presentation/context-only and cannot alter its graded population.
- Merged PR #5162 (tushare cyq_chips distribution plane) — prior art on `main`, CN data plane; no overlap until a separately chartered CN wave.
- This charter PR is docs-only. Future waves must re-audit live lanes and reconcile onto current `origin/main` before push.

## §9 Evaluation rubric (what would make each wave a success)

- **W0:** tape exists with ≥800 W-track episodes; G0.2–G0.5 satisfied; the E1/E2 tables answer "does anything PIT-observable separate topped from continued before the peak, beyond extension itself?" honestly in either direction. A confident, well-powered null is a success (it re-scopes W1 copy and Wave-2 priorities cheaply — the study doc's own Phase-I logic).
- **W1:** page live, bilingual, doctrine-clean; states computed nightly inside budget; forward log accruing; honest-null day renders correctly; operator can answer "which of my winners changed character?" in one glance with receipts on hover.
- **Program:** TOPA becomes the house's negative-space perception layer — states/legs may feed the action board and theme health only as display context. This charter authorizes no Prophet graded-board population, rank, gate, size, or veto contact; any such proposal requires a fresh authority prereg and operator ruling.

## §10 Explicitly rejected forms (standing kills honored; do not re-propose)

Directional shorting in any form (`DNR:KILL-DIRECTIONAL-SHORTING`); exit/auto-sell rules (L1 §7); a single blended "top score" ranking any surface (Article 2; Intel-Hub audit restatement); exact-peak-tick prediction as an objective (top zones only); IBD distribution-day counting (`DNR:KILL-IBD-DISTRIBUTION-DAYS`); re-testing onset-quality fingerprints (`DNR:KILL-ONSET-FINGERPRINTS`, `KILL-VOLUME-FINGERPRINTS`); Stage-2/EC as win-rate gates (`DNR:KILL-STAGE-WIN-GATE`); operator-forced un-gauntleted directional calls on any TOPA surface (`DNR:KILL-FORCED-CALLS`); predictive claims sourced from the Deep track alone (survivorship tilt); surfacing ahead of the declared display gate (`HOLD-IGNITION-SURFACES` lesson).

---

*Execution records are appended per wave below this line.*

## §11 Execution record

- 2026-08-10: Chartered. W0 prereg frozen in commit `06a6231fc43960553f3c906a6d6e098502fe9c1c`; the pre-outcome ticker-identity repair was appended in `7895028668d245509b64f843dc35e5185655370e`. Neither tree contains the named harness or a TOPA result artifact. This reconciliation remains docs-only.
- 2026-08-10 (W0 engine + W1 design freeze): truth engine built (75 tests incl. PIT-leak and full-series-vs-prefix parity gates), reconciled to the hardened prereg pre-run; Winner Health surface design frozen (spec + inert template; copy tables gated by the main loop). Phase-0 run-1 executed (598s; 8,461 W segments / 7,208 episodes) and surfaced a data defect via its own today's-tape appendix: reverse splits deeper than 1:10 survive the `_COMMON_SPLITS` repair and fabricate extension. Repair arm `sanity-segmented` declared in prereg §6 BEFORE recomputation (identity break on residual ≥3.0× single-day up-ratio; down side deliberately unscreened); run-1 preserved as the `pre-repair` audit arm (contamination: 831/8,461 segments, 3,089 EXT days, 257 episodes, 129 matched cases, 7 of 232 today's-tape names). Run-2 (headline arm) executing at record time.
- 2026-08-10 (G0.5 coverage-gate finding, recorded pre-red-team): under the frozen extension definition, ZERO of the motivating live exemplars (gold/PGM-miner cohort; AI mega-caps) are in the extended roster as of the tape vintage (2026-07-02) — the tape is five weeks stale AND the +50%/126d near-high bar structurally excludes big-cap winners. Consequence, ruled now: the W0 findings' domain is extreme movers (median small/mid-cap); nothing in W1 may imply the anatomy generalizes to mega-cap winners; **extension-tier widening for the surface (display bands below the frozen bar, with honest no-analog states or separately-stratified libraries) is chartered as an explicit Wave-2 item**, motivated by exactly this gap. (Run-3's uncapped 224-name appendix refines the census: a thin large-cap contingent IS present — roughly a dozen S&P 500 names led by refiners, managed care, and security software — but the miner family remains entirely absent; the gap and the Wave-2 charter stand.)
- 2026-08-11 (instrument fork discovered → reconciliation → run-3 headline): while run-2 executed locally, PR #5267's squash was found to carry TWO commits from an independent pre-merge audit (prereg §6 "pre-full-run independent audit" entry) — compliance repairs the local line lacked (literal same-day cross-sectional medians for E1f/E2f, B6 bounded, E1b + ruler on ALL EXT days, micro-spell days in the control pool, within-name ruler shares, sklearn fail-closed) plus its own W-scoped implementation of the declared ≥3.0× up-jump rule. Run-2 was therefore executed on a pre-audit instrument variant: demoted to a labeled cross-check arm (`top_anatomy_p0_summary_run2_preaudit.json`), never headline. The reconciled instrument (audit line + ported contamination-audit reporting + union jump-test family) is the registration instrument; run-3 on it (2,119s; W 9,725 segments / 6,921 episodes / 2,154 E1 episodes / 47 peak-months) is the HEADLINE arm. The three arms give two clean one-variable comparisons: run-1→run-2 (same estimator, panel repaired) removed the fabricated separations (W 5→2 survivors); run-2→run-3 (same panel, estimator audited) recovered three (2→5), including the REGISTERED `D3_updown_dvol_ratio21` (−0.066 [−0.150, −0.003], q=0.088, MID class, era- and tercile-sign-stable). Scope note: run-2 had applied the jump rule to both tracks; run-3 scopes it to track W's repair path per the §6 declaration (D is vendor-adjusted context and never registers). E1b walk-forward increment +0.004 [−0.008, +0.015] — no probability claim; ruler: all five surviving legs fire with median 10–13% remaining upside and (four of five) positive fwd-63 excess — anatomy, not timing. Red-team (G0.5) commissioned before presentation; W1 ships display-tier counting only.
- 2026-08-11 (G0.5 red-team returned NOT PRESENTABLE → all findings applied; corrections binding on every earlier entry): 8 blockers + 15 must-fix, none requiring a re-run. The material corrections: (1) **the measurable window is 2022H2–2026, not 2021–2026** — the store begins 2021-07-06 and the 260-session in-segment floor means no extended day can exist before 2022-07-18, so §2's "era-rich window containing the 2021 speculative-top cohort" justification is UNMET (that cohort is in the store but structurally unmeasurable; 20.7% of the tape carries zero extended days; the "2021H2-2022" era cell is really Aug–Dec 2022 on 5 peak-month blocks); (2) the preceding entry's "era- and tercile-sign-stable" for D3 overstated — sign-stable in point estimate only; the primary block CI excludes zero in 2 of 6 stability cells and NOT in the current 2025–26 era; (3) run against today's 224-name cohort, D3 fires on 15 names (6.7%, below its 10% control-tail base rate) — the current regime is quiet on the registered leg, and that read now LEADS the report's cohort paragraph; (4) episode-level E1b increments (prereg-declared, initially unprinted) are sign-INCONSISTENT with one arm significantly negative — printed; walk-forward M2 does not beat r126 alone (−0.006); (5) the run-1↔run-3 survivor-set identity means the contamination-vs-estimator interaction is UNIDENTIFIED (pre-repair panel × audited estimator never run) — the movement story is stated as observation, not causation, and the post-outcome timing of the headline-arm designation is disclosed with its pre-run audit-log mitigation; (6) the wrong-sign "ore body" (F1/F3/B3) carries newly-computed four-window profiles plus the mechanical counter-explanations (peak-anchor construction, length-biased day-weighted controls) — phase-1 is conditioned on an anchor-matched control design. Reviewer's post-fix verdict: the surviving conclusion is defensible and stronger — one direction-declared leg registered on a 2022H2–2026 fast small/mid-cap cohort, lead-time lower quartile post-peak, no current-era significance, pooled model beaten by trailing return alone out of sample; anatomy, printed with its nulls.
- 2026-08-11 (W2 tier-widening kickoff — prereg frozen pre-results): `research/top_anatomy/TOPA_W2_PREREG.md` committed on `claude/topa-w2-tier-widening` (off main 082f7c285c7) BEFORE any W2 computation. Two arms (`r63≥+0.35` = 151,325 EXT days / 63.0% primary-overlap; `(c−MA200)/ATR63≥6` = 652,410 / 23.0%), track W only, same 2026-07-02 vintage (staleness chip not yet landed — same-tape comparability ruled a feature), frozen pipeline/seed, five confirmatory one-sided legs (A4−, B2+, C6−, D1−, D3−) graded W2-CONFIRMED / W2-PARTIAL / W2-NOT-CONFIRMED with DISJOINT panels primary, coverage gate (miners + AI-leader census) leading the report, 12h-wall per-arm deferral fallback, G0.5 red-team before presentation. Scope note recorded at freeze: W2-CONFIRMED means cross-tier transfer on the same 2022H2–2026 tape, NOT out-of-time replication — that stays open until the store extends. Surface widening (W2b) explicitly deferred until the W2 report exists.
- 2026-08-11 (W2 arms executed → report drafted, red-team pending): both arms ran the frozen pipeline on the frozen vintage inside the wall (R63 1,065 s / ATRZ 2,669 s; census quantities reproduced exactly; parity gates 9.5e-15; no deferral). Coverage gate: NO exemplar extended on the 2026-07-02 vintage (stale-tape fact — the miner rally post-dates the store), but historically the widened bars admit the chartered cohort almost completely (R63 29/30 watched exemplars hold an episode, GOLD alone never; ATRZ 30/30). Confirmatory: `B2_rsi14` W2-CONFIRMED on BOTH arms (disjoint +3.87/+3.92 RSI points, q=0.0025 — heat travels, at ~3× the phase-0 effect); `D3_updown_dvol_ratio21` (phase-0's registered leg) does NOT travel (R63 disjoint sign-flip +0.152 q=0.85; ATRZ right-sign null) — the dollar-volume anatomy is tier-local; `A4_r252` REVERSES on ATRZ disjoint (+0.021, CI excluding zero on the wrong side) — the "young" leg is tier-local too; C6 NOT-CONFIRMED on ATRZ. The heat/late-gain family (A1/A5/A7/E1f/E2f + B4/B5) separates on both disjoint panels — phase-0's wrong-sign ore body is cross-tier structure, feeding phase-1's anchor-matched charter. E1b: R63 unchanged (no OOS increment); ATRZ walk-forward POSITIVE (+0.041 day / +0.020 episode FULL; +0.011 day DISJOINT) with the printed caveat that the r126 baseline is structurally weakest on the one arm not defined on trailing return — recorded as gauntlet-pipeline lead, zero authority. Adjudication: W2b chartered with a stratified-library MANDATE (no cross-tier threshold reuse, ever); phase-1 inherits B2's disjoint baselines as anchor findings; out-of-time replication stays the binding open question pending the store refresh (#5319 landed its fail-closed stale-read guard mid-wave; harness threads the explicit override for the deliberately frozen vintage). G0.5 red-team commissioned before presentation; corrections binding.
- 2026-08-11 (W2 G0.5 red-team → NOT PRESENTABLE → all findings applied; corrections binding on the previous entry): 8 blockers / 13 must-fix / 8 nits, the sharpest being MY report errors, not instrument errors — grouped E1b increments pasted into two walk-forward cells (flipping the primary panel's sign), the E1b baseline mislabeled r126-only (it is M2−M1 over r126+rv63, and ATRZ's walk-forward baselines sit at chance ≈0.50 — phase-0's own ground for not crediting track D), two false "first in program" superlatives (phase-0's W track already carried episode-wf +0.029 positive and negative-excess ruler cells), stale pre-merge walls (record = 841/2,528 s), "tier-local" overclaimed from power-limited non-rejections (every D3/C6/D1 disjoint CI contains phase-0's estimate; only A4's ATRZ reversal affirmatively excludes it — the W2b stratified-library mandate now rests on A4), "~3× phase-0's effect" dropped (nested panels, re-cut bins, no CI on a cross-design ratio; B2's four cells 0.76/3.87/2.02/3.92 reported as heterogeneity), the roster-read threshold renamed control-tail (P90 of matched-continued; correct bases 12.5%/10.1% → 1.5×/1.7×, not 1.7–1.9× vs 10%), and — the review's most valuable find — the REAL phase-0 ore body F1/F3/B3 replicates on ALL FOUR panels (ATRZ disjoint F1 −17.0, F3 −2.75, B3 +4.67; all q≤0.01) but was invisible to the draft because the survivor list is sign-gated; it now leads §4 with the anchor/length-bias counter-explanations attached and phase-1 re-chartered around it plus mandatory era stratification (E3/E4 were prereg-omitted; no era receipt exists for B2). Reviewer independently reproduced all 20 confirmatory cells and all 16 E1b fields to float precision against the preserved pre-merge artifacts. Post-fix report is the shipped text.
- 2026-08-11 (W2 second-pass corrections, append-only): re-verification returned 2 residual blockers (four fresh R63-FULL CI transcription errors incl. a cross-arm copy; roster-read bases wrong in MY deviation — `n_null_days` is the TOTAL EXT-day count, so the right rates are 14.2%/11.2% and elevations ≈1.34×/≈1.50×, superseding this log's earlier "12.5%/10.1% → 1.5×/1.7×"), 3 must-fix (A4 excludes phase-0's effect on BOTH disjoint panels — attenuation on R63, reversal on ATRZ; the W2b stratified-library mandate now cites both; F1 halves R63 FULL→DISJOINT so ore-body growth is 5-of-6 pairs; two flattering roundings in the ATRZ-disjoint E1b row), 2 nits (two missing flat ruler legs; banner attribution) — all applied. Reviewer confirmed everything else exact: 20/20 confirmatory deltas+q+grades, §4's 12 ore-body cells and 24 CI bounds, §5's 16-cell E1b table (30/32 bounds), phase-0 anchors, composition figures, survivorship, F5 floor, determinism scope.
- 2026-08-11 (phase-1 kickoff — prereg frozen pre-results): `research/top_anatomy/TOPA_PHASE1_PREREG.md` committed on `claude/topa-phase1-anchor-matched` (off main 074caa5f9f7) BEFORE any phase-1 computation. The wave decides artifact-vs-anatomy for the W2 §4 ore body (F1/F3/B3) and B2's duration-unmatched confirmations via TWO purpose-built control constructions, the only moved variable: AM (anchor-matched — controls restricted to fresh-63d-high days, episode-first sampled; discharges the peak-anchor asymmetry AND length-biased day-weighting; F3 becomes the printed matching diagnostic) and DM (duration-matched — frozen W4 key + episode-age tercile, episode-first sampled; discharges "younger = hotter"; F1 becomes the diagnostic). 18 registered cells (AM: F1−,B3+,B2+ / DM: F3−,B3+,B2+ × primary / R63-disjoint / ATRZ-disjoint), BH q≤0.10 within each family of 3, seed 20260811, B=2000, ≥12 peak-month + NEW ≥100-matched-episode floors, match-rate printed, era stratification MANDATORY on every cell (three printed calendar blocks + latest-era grade modifier + the phase-0 B2 fade fence — the W2 omission not repeated). Leg synthesis declared at freeze: P1-ROBUST requires support on BOTH disjoint panels in the registered construction(s); B2's registered anchor comparison against W2's CIs decides subsume/partial-artifact/re-classify. Collapse is a result: if the ore body dies under AM, "matching geometry, not anatomy" is the headline. W2b (surface widening) proceeds in parallel under its own lane; nothing in phase-1 blocks it because W2b inherits only display-tier counting either way.
- 2026-08-11 (phase-1 SHIPPED — PR #5372 merged b0001fb1b4c, three-pass G0.5 → PRESENTABLE): the AM construction FAILED informatively — the frozen {21,10,5} snapshot geometry puts case anchors at median 1–2 days-since-high, so controls pinned at exactly 0 REVERSED the asymmetry; all nine AM cells adjudicated UNDERPOWERED-BY-CONSTRUCTION-FAILURE by override of the pre-declared magnitude-only collapse rule (which returned no-failure), with the displaced registered kill and the W2b conditionality printed in report §3.2. DM answered its half: B2 P1-SUPPORTED on primary (+0.952) and ATRZ disjoint (+2.885, inside W2's CI by 0.085 at its floor); F3 supported on both readable panels (anchor caveat live); B3 dies on primary with the kill located (non-binding day-weighted arm) in the length-biased-SAMPLING half; R63 disjoint underpowered (n=90 vs 100 floor) with declared signs at q=0.0005 — no directional claim. Full-table exploratory: E3f/F2 wrong-sign 3/3 inside the clean DM arm (anchor-family). NO leg P1-ROBUST. AM-v2 (controls matched to the case anchor-distance DISTRIBUTION) chartered MANDATORY before any ore-body conclusion hardens. Post-red-team instrument note: the harness's `candidate_pool` parameter was renamed `control_candidates` after CI's Prophet authority-leak fence flagged the homonym (behavior-neutral; commit 0b0b563637d; artifacts untouched). Both fence reds this wave were content-adjacent, not content-defects: the homonym, and W2b's line-shift of an allowlisted `.ffill().bfill()` exemption (re-reviewed at its new line per the guard's own instruction).
- 2026-08-11 (W2b SHIPPED — PR #5374 merged 411b7d0b97a): Winner Health widened to three tiers per the A4-grounded stratified-library mandate — primary board byte-identical on identical inputs (G-3), r63 "Fast three-month runs" + atrz "Far above trend" with per-tier libraries/thresholds (`ext_variant`-stamped pairs, cross-tier reads refused by test), §6 B `no_read` rows + §6 C tier `unread` band (accepted on primary as the argued exception — they fire only where today's page is wrong), ATRZ mixed-group banner, A4 NOT introduced on either new tier, heat leg leads the new tiers' key order, A→Z neutral sort, ZH `name_zh: null` → "None" defect fixed, +54 s nightly-lane delta (~1.3% of budget), 164.5 KB gzip vs 400 KB budget. Build-lane catches worth the record: close-only ATR would have admitted ~2× names vs the library's own definition (high/low added to the MASK path only); the caption character-budget was an inverted width proxy (13-char fits where 12-char wraps) — captions now frozen with measured pixel widths and the r63 caption re-pinned twice to "THREE-MONTH" (87px) via dated spec amendments. Forward ledger deliberately stays primary-only; widening it is a separately gradeable act. Live: site copy shipped in the squash; VPS pull serves it within minutes; nightly re-bakes thereafter.
- 2026-08-12 (AM-v2 SHIPPED — the phase-1-chartered decisive construction): controls matched to the case anchor DISTRIBUTION via a per-case-snapshot hard caliper (`|Δ days_since_63d_high| ≤ 2` at NN time on the frozen `episode_id@days_to_peak` pairing), DM's age tercile retained (AM2) plus an age-free arm for F1's only clean read; SIGNED governing validity rule with pre-registered ≤1-caliper escalation — designed so no adjudicator override could be needed, and none was: all six (construction × panel) cells VALID at both calipers first try (residual anchor gap ≤0.0625 sessions vs anchors −2.25/−2.75; the caliper-guaranteed clauses disclosed as such, the empirical content being the no-reversal clause; the 0.00 diagnostic is residual-gap symmetry, not exact-anchor pairs — realised per-pair |Δanchor| median 1.0). **The registered kill fires at its registered grain: the ore-body anatomy claim is CLOSED on this tape as a cluster claim** — B3 P1-NOT-SUPPORTED under the valid instrument on ATRZ (+1.530 [−0.884,+3.604] q=0.1165, the only floor-clearing disjoint panel; R63 dark at 31 episodes) and primary (+0.133); fragility receipts printed (CI contains DM's +3.268 and overlaps W2's interval; ticker-clustered CI [+0.470,+2.721] excludes zero with ticker-p 0.003 — fragility not confined to the interval; failure-to-confirm ≠ demonstrated absence); F3 = the matching variable, never gradeable; F1 = undecided (AGEFREE's documented adverse bias realized — marginal age gaps +14/+5/+11 sessions — uninformative-by-design, never a kill). Mechanism color: the day-weighted arm reads B3 positive on all three panels even with anchors matched — B3's dependence, where it exists, lives in length-biased sampling; neither mechanism proven. **B2 survives anchor+age matching on ATRZ** (+1.499 [+0.473,+2.389] q=0.011, era-stable) at the registered same-design ratio 0.38× W2's point (phase-1 DM: 0.74×; joint matched-design share ≈62%, the DM→AM2 step un-CI'd and carrying re-draw noise — the fresh seed was a second declared moved variable) — **the §7.2 surface-copy revival does NOT fire** (trigger was B2 NOT-SUPPORTED on a floor-clearing disjoint panel); obligation dormant, not retired. Match starvation is the caliper's printed cost (7 of 9 registered cells; the caliper drops the upper half of the case anchor census — estimates describe near-63d-high case snapshots). E3f persists under anchor matching (the one surviving wrong-sign thread, exploratory-capped); F2 mixed. G0.5: FOUR passes (6BL/15MF/5N → 1BL/7MF/4N all rewrite-introduced → 1BL/0MF/3N → **PRESENTABLE 0/0/0**), the nine registered-cell rows byte-identical across every draft — all corrections landed in prose; prereg §7 carries the append-only corrections including a withdrawn cross-design ratio. Both measured counter-explanations (duration, anchor) are now instrumented on this tape; **the program's only remaining question is out-of-time** — first post-store-refresh wave re-reads vintage rosters + exemplar census before anything else; no further on-tape construction chartered.
