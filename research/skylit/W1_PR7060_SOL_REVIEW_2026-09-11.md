# W1 PR7060 Sol review — connected vertical required

Recorded: 2026-09-11 UTC. Accountable seat: ceo-sol.
Records operation: `skylit-integration-ruling-20260910-sol-001`.
Product child: `us-sector-participation-w1-20260910-sol-001`.
Exact carrier: Mastermind X / `C0BSBM78V1N` / `1789063697.492969`.
Builder receiver: Claude8 desktop conversation `local_5d8fbb60-c212-4162-9e9c-5cb6b1a41ce8`.
Implementation PR: Macro #7060, semantic head `16d0cc47368705cdb4ad7c7dc2ba588f5f327fef`, base `43470274d542fdf0cfd59f77a938338f5778dea3`.
Review receipt: GitHub review `5174640493`, `CHANGES_REQUESTED`.
Procedure pin for the review: protected Mastermind `797cfd0b1001d9dfe6fe9030af80ecdab0e1220d`, Skillpack 1.0.1 / bootstrap 1.

## 1. Verdict and capability state

**REQUEST_CHANGES. Current product capability: PARTIAL / disconnected / not accepted.**

PR7060 adds a licensed daily-window helper, a pure 20-session calculation, an artifact-metadata reader, tests and CI registration. It does not yet create the commissioned user capability. The licensed helper is not invoked by the actual US S&P breadth path; no derived participation artifact is produced; the consumer returns metadata rather than the numerical/member evidence; and the Money & Breadth calendar, selected-cell detail and existing research-link journey are absent.

Do not split the missing UI/consumer into a second feature PR. Complete the originally commissioned producer -> derived product -> actual consumer -> user journey in PR7060 with the same effectful writer. Live full-roster acquisition and deployment remain separately gated; implementation and hermetic connected-path tests are authorized.

The worker performed source edits before a discrete source-writing START. Preserve that chronology failure. Do not backdate, undo/replay or create a replacement writer to cosmetically repair it. The same writer remains sticky until reviewed result, terminal Sol STOP, child-source removal and branch-writer release.

## 2. What the exact head contains

Changed paths at head `16d0cc47368705cdb4ad7c7dc2ba588f5f327fef`:

- `.github/ci/legacy-jobs.yml`
- `collectors/breadth.py`
- `scripts/build_sector_central.py`
- `tests/test_sector_central_participation.py`

The old `compute_sectors()` 50/200 implementation remains structurally separate. The new methods are inherited by all BreadthAdapter subclasses, so activation must be explicitly limited to the real US S&P reference owner. Constructor tests and `co_names` inspection are insufficient once actual invocation is wired; exercise the real orchestration path with network and writer spies for the S&P owner and all six siblings.

The worker reports local RED->GREEN and 54 focused passes. Those reports remain evidence to verify, not acceptance. GitHub fences run `34556791182` succeeded on the exact head; the full CI run `34556791381` was queued at the review checkpoint. Vercel reported a deployment failure; no preview or production proof is inferred from that check.

## 3. Blocking findings

### B1 — no real producer/consumer/user capability

`licensed_daily_window()` and `compute_sector_participation_20()` are deliberately unreachable from `fetch()`. Nothing writes `sector_participation_20.parquet`. `read_sector_participation_20()` returns availability metadata only. The UI and selected constituent evidence are deferred. This is foundation without the product.

Repair in the same PR:

1. Invoke the new input path only for the existing US S&P owner, never inherited regional/size siblings.
2. Create one bounded derived generation carrying summary cells, dated member states, exclusions, source/basis/reference identity and clocks.
3. Compose it after the Sector Central grader boundary.
4. Render the calendar and selected-session constituent journey inside existing Money & Breadth.
5. Preserve existing links, view/hash behavior, entitlement, language/theme and responsive behavior.

The builder must not acquire market data. A live provider run is not required to implement and test the real call chain with injected responses and isolated roots.

### B2 — denominator and identity are false under missing columns

The current code sets `expected_n = len(tick)`, where `tick` contains only reference members present as price columns. A missing whole security disappears from expected membership.

Executed counterexample: ten reference names, five supplied columns -> reported expected 5, eligible 5, participation 100%. Required result is expected 10, eligible 5, percentage withheld, with five missing-name exclusions.

A sector with zero covered columns currently disappears. It must remain visible as unavailable with expected membership and exclusions. Duplicate symbols and conflicting sector membership must be rejected or resolved by the existing identity owner, not silently grouped. An isolated Boolean inside an object column currently survives numeric coercion; input validation must be per observation, not only per dtype.

### B3 — returned source data is asserted rather than qualified

The helper sends an adjusted request, then stamps its result as adjusted without validating the response envelope. Exact-head fake-response challenges showed acceptance of:

1. a different returned ticker;
2. `adjusted=false`;
3. body status `NOT_AUTHORIZED` while rows are present;
4. an unexpected `next_url` / truncated response;
5. conflicting duplicate timestamps;
6. a row outside the requested range;
7. a nonfinite close;
8. a Boolean close;
9. a negative close.

Validate the returned security, status, adjusted basis, declared counts/continuation, unique session mapping, requested bounds and close domain. Preserve request/response identity and acquisition time without exposing credentials.

A qualified complete envelope may still have a genuine historical data hole. Reindex that name to expected sessions and preserve the hole so only affected 20-session cells become unavailable. Do not erase all otherwise valid history, splice a partial response onto an old basis or introduce unbounded pagination/retry behavior.

### B4 — artifact availability and clocks are fabricated

The current builder reader marks any nonempty parquet `available=true` and hardcodes `split_adjusted`, even if the file contains only a single `eligible_20` test column. It does not carry the values the UI needs, verify the artifact method/source/roster identity, preserve member evidence, or distinguish source date from file/build time.

Define and validate a complete derived contract. At minimum it must bind:

- method/version and 20-session rule;
- reference roster/version and selected-reference reconstruction disclosure;
- price source and split-adjusted basis;
- requested observation range and latest expected completed session;
- per-name source/response status and holes;
- per-sector/session above, eligible, expected, percentage/null and exclusion counts;
- same-generation dated constituent state and existing destination identity;
- source/acquisition/computation/publication clocks;
- generation identity sufficient for the consumer to refuse summary/detail mismatch.

A missing last session cannot be hidden by deriving the observation window only from returned rows. A fresh build timestamp cannot make stale source current. Do not use default append/`combine_first` semantics to fill newly unavailable cells from an older generation.

### B5 — integration and sibling-isolation evidence is incomplete

After wiring the real producer, test the actual US breadth entrypoint with old dependencies stubbed and all writers redirected. It must make the expected W1 calls and derived write. Execute every sibling adapter through its real inherited `fetch()` path with W1 provider and writer spies; each must perform zero W1 requests and zero W1 projection writes, including when W1 configuration/key is absent or its provider fails.

Keep the existing lightweight Sector Central import invariant. Register tests in a real code-gated plan with the true dependency closure. The manifest placement and reported pack number do not by themselves prove execution; preserve the exact selected-plan and executed-step receipt.

Remove the unrelated `Timestamp.utcnow()` replacements unless a separately evidenced in-scope regression requires them. Narrow the PR's blanket legal statement about every adjusted store to the established fact: qualified licensed provenance was not established for the inspected adjusted stores. This review makes no blanket legal determination about all company data.

### B6 — the current W1 test is absent from the authoritative PR CI plan

This is now proven from the immutable workflow artifact, not inferred from YAML adjacency.

PR7060 registers `tests/test_sector_central_participation.py` only in `unrun-russell-breadth`, which declares `gate: data`. The actual PR workflow at current main invokes both planning and execution with `--gate code` (`.github/workflows/ci.yml` lines 4596, 4912 and 4935 at `7bc84fc96b302007d5b81525790562b3728b1341`).

Exact-head workflow run `34556791381` published `trusted-ci-plan` artifact `10182883614`, digest `sha256:574db9107d44daadff8ccceb91a197527724df3f718e9bc9bc13d11600aa167f`. Its `plan.json` binds subject head `16d0cc47368705cdb4ad7c7dc2ba588f5f327fef`, tested merge tree `de175773e1ad1cef104357c84cda1f81e09ad126`, base `43470274d542fdf0cfd59f77a938338f5778dea3`, and `authority_changed=true`. The plan contains 139 code-gated jobs, but contains neither `unrun-russell-breadth`, `test_sector_central_participation.py`, nor the W1 step proof/name. The identical absence was observed in candidate plan artifact `10182862381`.

Therefore the worker's local statement that the W1 suite was selected into pack 9 is not an execution receipt for the actual PR merge-gate plan. The full run can complete without executing this test. At the latest observation, plan/admission jobs succeeded and all twelve trusted-executor packs were queued; that queue state does not cure the missing W1 proof.

The candidate manifest has only one existing `gate: code` job whose install line already contains yfinance (`china-search-universe`); it is not a natural semantic owner for this US product. The bounded repair must either:

1. create one small code-gated W1/Sector Central job with the exact minimal dependency closure and path ownership, preferably sharing an existing identical install environment so it adds no runner or redundant setup; or
2. split/import-isolate the suites so actual breadth orchestration is exercised in a code-gated yfinance-capable owner and builder/UI tests run in their existing relevant code-gated owners.

Do not leave the tests in a data-only job, merely duplicate them there, or widen an unrelated China job without explicit subject-ownership evidence. The repaired authoritative plan must name the W1 proof, and the executed semantic fragment must show its result on the exact repaired head.

## 4. Independent falsification receipt

A bounded read-only Studio process `89954` loaded the exact Git blobs and executed the actual candidate method AST bodies with fake HTTP and in-memory prices. It did not import/run the collector acquisition, write source/data, invoke the worker worktree or contact the provider.

Exact blobs:

- candidate `collectors/breadth.py`: `d0ce63d1d7e2db03178cd8924fb5d12fef865e54`
- calendar: `0ece6439ffe4b081ee7a268fe99b69e1de1216a3`
- candidate builder: `0f9b9bda5c3306842a71d1abfb667e9e792e5229`

Result: twelve selected challenges, twelve unmet requirements; exit 0, runtime 1.12 seconds. This count is a targeted falsification set, not a representative defect rate or full review.

Observed failures:

- missing whole symbols: expected5/eligible5/100% instead of expected10/eligible5/null;
- mixed Boolean cell: eligible10 instead of9;
- zero-covered sector omitted;
- wrong response ticker accepted;
- response `adjusted=false` accepted and relabeled true;
- `NOT_AUTHORIZED` body with rows accepted;
- unexpected continuation accepted;
- conflicting duplicate session silently overwritten;
- out-of-window row retained;
- infinite close accepted;
- Boolean close accepted;
- negative close accepted.

## 5. Current-base and transport state

Macro main was observed at `a2d3f8e11c9d61c60cd89994169cd06eea9abd18`, eight commits beyond the PR's pickup base. The observed movement is dominated by generated basket-data changes, but current-base integration/collision proof remains the builder's obligation at repair return. Do not create an ancestry-only commit or merge current main into the source branch merely to make it appear current.

Sol posted the full same-carrier repair at `1789096018.269229`. Secretary's first native delivery attempt was safety-denied; native readback established empty composer and no retained message, so delivery effect was known none. The current Chairman then directed this session to continue. Sol recorded explicit payload/destination consent at `1789103018.475449`: one delivery of the existing repair only, to the same verified Claude8 conversation, no alternate receiver or payload widening. Secretary subsequently held the retry because the native safety layer still required direct, action-specific user approval; no retry or alternate path occurred.

## 6. Continuation and acceptance boundary

Exact next action: after direct action-specific Chairman approval of the exact transmission, the same receiver consumes repair `1789096018.269229`, fresh-reads this carrier, emits a truthful repair START with actual worktree/head/path identity, establishes `CHECKPOINT_VERIFIED`, and repairs PR7060 through the complete connected vertical. After its nonterminal RESULT it re-arms the existing continuation and waits for Sol.

Return must contain the immutable repaired head/tree/base, changed-file and source-effect census, checkpoint, RED/GREEN and actual code-gated CI evidence, exact derived schema and clocks, connected-path tests, browser evidence and remaining real-input/release gates.

No Ready, merge, deployment, full-roster live acquisition, W2, trading authority or worker replacement is authorized. W1 acceptance still requires independent semantic review, current-base integration proof, qualified real inputs, old 50/200/grader/entitlement invariance, actual served-browser/publication proof and terminal dialogue cleanup.
