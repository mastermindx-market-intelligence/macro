# W1 source implementation ruling and consumed preflight

Recorded: 2026-09-11 UTC (September 10 in the Chairman's Pacific-time operating day).
Accountable seat: ceo-sol. Records operation: `skylit-integration-ruling-20260910-sol-001`.
Product child: `us-sector-participation-w1-20260910-sol-001`.
Exact carrier: Mastermind X / `C0BSBM78V1N` / `1789063697.492969`.
Records carrier: Macro PR7035 / `sol/skylit-sector-integration-20260910`.
Procedure: protected Mastermind `964bd8e7b30c91e5e83caee0ba37513ba7d07e70`, Skillpack1.0.1/bootstrap1.
Inspected implementation: Macro `14dc3b38111081a0d559d27b9c36ab995c8aa441`; Terminal `9b7e7a41ad28ab43f74681960c1d5e4758389ffa`.

## 1. What is now true

The old statement that W1 has no receiver is superseded. The existing placement owner delivered exactly one assignment to Claude8 desktop conversation `local_5d8fbb60-c212-4162-9e9c-5cb6b1a41ce8`, title "Sector Central W1 preflight analysis". Placement receipt1789080002.429829 records Sonnet5/Extra on included capacity, implementing the Terra capability avenue. This is not Fable, a new account, or a paid-API allocation.

The worker's first return1789080626.612169 combined pickup with its read-only result and lacked a separate START. Secretary's independent GUI reconciliation1789081638.904849 confirmed the same desktop conversation and registered continuation. The worker's scratch id is a different namespace, not proof of a duplicate and not a verified Code UUID. The historical missing START remains a chronology gap; no backdated receipt or duplicated investigation repairs it.

Sol consumed that return with REQUEST_CHANGES/CONTINUE1789086875.831989. Secretary delivered the continuation once in1789087500.946659. The receiver emitted a separate new START1789087528.375289 before this repair, then RESULT/HOLD1789087661.181939. This establishes actual bounded preflight work, not product-source implementation.

Sol then issued source-direction ruling1789088118.422639 and supporting evidence1789088819.696629 on the same root. The source-writing stage is conditionally approved after current exact-path custody. At this record's checkpoint, no later product-writing START or implementation result had been consumed. Do not label delivery as code, claim no unobserved local effects after the instruction, or place another writer. The product capability remains SPEC_ONLY.

## 2. Source architecture decision

**Add the required licensed-history read to the existing US breadth owner.** Use the existing Adapter HTTP and configuration/credential mechanisms, the existing collection schedule and publication path, and the existing reference-roster identity. Do not create a second collector class, general-purpose price database, calendar, corporate-action engine, credential pool, retry service, or event/control plane.

The read requests complete bounded daily-history windows with `adjusted=true` from the already-licensed stock source. Each response must bind to the requested security and dates, disclose its split-adjusted basis and acquisition identity, and preserve missing/invalid observations. Re-fetch the whole required window for that snapshot: do not append revised adjusted rows to an old differently adjusted cache. No W1 raw-price or raw-response store is added. Only the derived participation/constituent-state product and its source/basis/coverage information are persisted through the existing output pattern.

All old 50/200-session calculations and acquisition/output semantics stay unchanged. This is an additional explicitly named measurement, not a silent conversion of the old breadth estate. The Sector Central builder reads the derived product; it does not acquire market data. W1 context is composed after the existing grader boundary and never enters the old conviction object or carries old premium-board rows into a public payload.

The actual journey remains Money & Breadth -> selected completed-session cell -> matching numerator/denominator/exclusions and dated constituent states -> existing research destination. Preserve all existing view/hash, entitlement, language/theme and mobile behavior. Reconstructed selected-roster history remains distinct from historical membership or original publication replay.

### Why this is not a new data platform

The licensed provider already supplies split-adjusted aggregates; a new corporate-action implementation is unnecessary for this measurement. Existing implementations prove the request shape but are not qualified W1 production sources:

- Macro `scripts/entry_radar_vendor.py`, blob `8d0b533f6cac2d884954b6d207bdc48d3c7ce916`, is a bounded replay client with its own outside-repository session cache. Do not convert that cache into a new production authority.
- Terminal `ingest/polygon_bars.py`, blob `1c22b0b0f3ec9e07a73b5bed65b97d23eeac50e0`, rounds chart values and appends only newer bars. Its chart envelope is not complete adjustment-vintage or correction-safe window proof.
- Macro `collectors/polygon_options.py` explicitly retires that options source. Do not revive its options collector or entitlement problem for a stock-price feature.
- `collectors/base.py`, blob `420f96134b7835987d5e0d9a48ee52ac2c4f0148`, already owns shared HTTP/retry/redaction behavior. `lib/config.py`, blob `b6fc888269876242597da354aaa726c20ed95802`, already owns local/environment configuration. Reuse, do not duplicate.

Official source: https://massive.com/knowledge-base/article/is-massives-stock-data-adjusted-for-splits-or-dividends . It describes split adjustment, not dividend adjustment. Engineering entitlement remains the existing `research/licenses/MASSIVE_ENTITLEMENT_RECORD.md`, blob `3969a9aae918141b51baffbb0e17d8a2ec2485a0`; the private agreement is not requested or disclosed.

The worker's two proposed shortcuts were rejected: existing Yahoo usage is not proof of data-use rights, and A13's permission to display an unavailable state is not permission to compute an adjusted-price indicator from raw data with a disclaimer. "No qualified licensed provenance established" is the supported conclusion for the inspected adjusted stores, not a blanket legal conclusion about all company data.

## 3. Real qualification boundary, not another research loop

The source adapter must have finite invocation and response bounds; use existing provider controls, no extra retry/polling plane. Full-roster acquisition, collector runs and production changes require the applicable owning-path clearance, not merely this source design. Fake responses are sufficient for coding and adversarial failure tests, not real-data release.

A bounded diagnostic on the authorized Studio reused the exact existing `scripts/massive_entitlement_probe.py` blob `310d8a599d0c6759428bb7044868ab5d297ab647` and its sanctioned key resolver. It returned `NO_EXISTING_KEY`, **zero network requests**, process42256 exit2/runtime0.26s. No key material, raw price data, source file or store was written. Do not interpret this one checkout's environment as the company lacking an account, and do not hunt/copy another worker's secrets.

The inspected protected workflow already supplies both `POLYGON_API_KEY` and `MASSIVE_API_KEY` to the existing `run collectors` step: `.github/workflows/daily.yml` lines310-323, blob `f2e26d58f30212c31260760e71a71126145ed1fe`. No new workflow secret exposure is needed. Configured secret references are not proof that the runtime value or endpoint entitlement currently succeeds; a bounded live qualification through that owner remains required before release.

## 4. Source custody and CI

Checking only PR6990 and this records PR does not clear current changed paths or local writers. The selected receiver must use the accepted Source Continuity/current-owner path, declare exact files/hunks, and work only in its own isolated worktree. The shared checkout was on foreign branch `claude/mo-a-2-a-f02-w2-3`; never edit it for W1. No alternative verifier is commissioned.

Candidate source boundary remains the existing `collectors/breadth.py`, `scripts/build_sector_central.py`, `templates/sector_central.html.j2`, a narrow lazy-asset addition in `templates/si_workspace.js` if needed, namespaced participation partial/assets, owning tests and a narrow existing CI registration/dependency change. Actual names/hunks require current custody; this list is not proof no collision exists.

The worker correctly repaired its first CI claim: a builder trigger does not execute a new test file. The local YAML identified the existing command `python -m pytest tests/test_sector_central.py -q` inside `unrun-scoring-engine`; its install list lacked yfinance even though directly importing collectors.breadth imports yfinance. A new suite needs an executed code-gated home with the actual import dependencies. Do not install the whole application, infer execution from adjacency, or silently place code tests only in a data-health lane.

`run_ci_pack.py`, blob `686da921d8d5dbc264d6a5b94d4d7ad1fdf31272`, documents that the authoritative plan selects `gate: code` for merge gating and consumes the disabled legacy definitions. Therefore neither legacy `if:false` alone nor an adjacent test command is current execution proof. The exact new step and its selection must appear in the real plan/execution receipt. The local legacy file differed from the inspected main blob; refresh current hunks before writing.

A supplementary Sol host command to inspect job gate metadata was platform safety-blocked. It returned no PID/result and was **not rerun or routed through another tool, account, device or worker**. That helper supplied no gate evidence. The selected worker's previously assigned CI acceptance obligation remains distinct; no blocked helper or payload is delegated for reproduction.

## 5. Numerical feasibility evidence

Separate from the earlier28 research checks, a research-only exact-input comparator was developed against discriminating tests in the ChatGPT sandbox. The naive baseline produced **5 failures / 22 passes**; the exact integer-common-denominator comparison produced **27 passes / zero failures**. Cases include constant decimal windows, genuine one-ULP boundary changes, invalid numeric values, exact length, overflow-resistant comparison and2,000 seeded windows checked against a Fraction oracle.

This proves a mathematical feasibility point, not a bug in the current production pandas implementation. It compares the supplied binary numeric values exactly; source decimal precision and market measurement uncertainty remain separate questions. An arbitrary broad epsilon is not needed to make constant-window behavior correct.

The comparator processed126,756 synthetic windows (503 x252) in1.117911seconds in the sandbox. This is not full collector/UI performance or Mac/production performance. No claim of stock returns, current market coverage, model calibration or shipped functionality follows.

Evidence source hashes: `window_compare.py` SHA256 `55cbd2f1944d1430703dc6254563ec14916c8d4864e5bf28a990d2b7bc68fa53`; `test_window_compare.py` SHA256 `df4272395aaa0c99ec21adb60cd6f92209aa32d513d5f58f704e8662f5cc8b41`. The runnable probe, red/green logs and measured result are in the user-facing continuation package. It is an acceptance oracle/feasibility aid, not competing production source.

## 6. Continuation and completion

The exact next action is consumption of ruling1789088118.422639 by the already selected receiver, current path/custody qualification, and a truthful product-source START before its first edit. Then RED tests -> licensed-read extension and pure computation -> derived publication -> actual Money & Breadth consumer -> one Draft/HOLD source PR. No second broad research approval or Chairman account allocation is needed.

The existing Secretary-owned ACTIVE15m aggregate, task `01a05a89-cb19-7162-99c4-54ffdc714cf1`, remains the registered continuation resource; this record does not create or operate another watcher. Sol must consume a return with same-root CONTINUE/repair or terminal STOP. Source-worker STOP and parent-program completion remain separate.

Current product acceptance still requires qualified real inputs, old-field/grader/access invariance, actual code-gated test execution, independent review, and real served-browser/publication proof. Records, synthetic checks and a conditional source instruction do not satisfy it. The existing records PR remains Draft/HOLD; no source merge, deployment, trading authority, other-worker release or whole-program completion is authorized by this record.
