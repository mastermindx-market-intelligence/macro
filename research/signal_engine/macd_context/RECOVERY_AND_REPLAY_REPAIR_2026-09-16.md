# MACD research recovery: historical source restored, replay diagnostics repaired

## Outcome and limits

The failed chat turns did not deliver work. This recovery closes two operational blockers rather than repeating the completed crossover studies: historical Git ancestry is restored, and a canonical research-accounting append is implemented in the owned PR workspace. A reproduced replay error is also repaired: an upstream no-result is no longer mislabeled as a date-truncation failure.

The broader MACD/Prophet accuracy program remains PARTIAL. No calibrated probability, accepted indicator replacement, stock selector, trading-policy improvement or production/frontend change is claimed. New recorded-score outcome reads remain held under their frozen contract. Source tests do not substitute for independent scientific review.

## Sources, custody and authority

Current live Chairman requested completion. Protected Skillpack: Mastermind@a78b8fe23d8e1ed129880ac47e97ebe96afa8aea; INDEX, COLD_START, ACTIVE_EXECUTION, WEB_CEO_DELEGATION, RECONCILE_STATE and CLOSEOUT loaded from that revision; schema1/version1.0.1/bootstrap1 compatible.
Macro source pin: de36a37a3a4738e5cd59db31345790002f2582db. Existing PR7177 starts this recovery at984a14537259626628dea5636bd5b7fe21f25958. Previous publication effects were reconciled to that head before new source work. No replacement PR was created.
Research operation remains macd-context-cycle-attribution-20260915-c3 on Remote Desktop Commander, Mac Studio3f5ce987-e3eb-40a3-af9f-4b0ae54919cc. Native code/accounting work uses the existing approved external-SSD workspace helper and its registry/receipt, not an ad hoc worktree or modified wrapper.
Managed workspace: /Volumes/Mastermind/agent-workspaces/claude/14851c4656838a3b/pr-7177-e58499258369472e. Helper input: existing Macro root, name pr-7177, session macd-cycle-research-publication-20260915-c3. Initial head and clean state were verified. No other worker's files or processes were edited.
Direct-work rationale: CRITICAL_PATH_SHORTCUT for source recovery; PRINCIPAL_JUDGMENT for scientific scope. Executive OS retains lifecycle, Agent OS continuity, GitHub evidence, Evaluation/TrialLedger accounting and promotion. No new control plane, provider session or worker was created.

## 1. Historical ancestry: fixed and proven through the existing resolver

The shared Git object database reported shallow=true. One additive fetch through existing origin completed exit0: --unshallow --filter=blob:none --no-tags --no-write-fetch-head --no-auto-maintenance --no-write-commit-graph origin main. No authentication or permission setting changed.
Afterward shallow=false. Shared checkout HEAD20c950b081773cd5ecc04815275c28b32e49b879 and origin/main de36a37a3a4738e5cd59db31345790002f2582db remained unchanged at verification. No checkout reset, merge, rebase or index edit occurred.
The unchanged Prophet resolver now resolves session2026-07-17 at bake slot2026-07-17T22:30:00Z to ff745b1ab54256b0188688cc5815e6675e4edef5, ancestor_of_origin_main. Registry, gap and session checks passed. Resolver success is not control-fidelity or policy success.

## 2. The next replay failure was reproduced, not bypassed

The ordinary --control dry run, without --execute or any fidelity waiver, reached the alpha pre-step. It produced {"as_of":null,"n":null,"ok":false} for the reference board's July15 control date and refused before computing board fidelity. A second diagnostic run retained only the harness-owned vintage tree; it reproduced the same failure.
The old exception claimed that the prices had not truncated correctly. That conclusion does not follow from a missing result. Static source admits several possible upstream causes; the specific real cause is NOT established.
A direct diagnostic calculation in the retained vintage tree was blocked by the platform. It was not retried, re-encoded or moved to another carrier. In particular, the 'no close matrix' text used by a regression fixture below is synthetic, NOT a measured cause of the real failure.
The real replay remains blocked before fidelity. No board, plan, candidate policy or live store was minted, and no control threshold was weakened.

## 3. Bounded implementation repair and complete regression proof

scripts/prophet_pit_replay.py::build_alpha now checks that the child JSON is an object, then recognizes explicit ok=false as an upstream no-result. It raises PitReplayRefused with the bounded final20 stderr lines (or an explicit absent-diagnostic message) before date validation. Valid-output receipt fields and genuine date-mismatch rejection remain unchanged.
New tests/test_prophet_pit_replay_alpha_result.py has five synthetic cases: missing result with diagnostic; genuine wrong date; valid output preserving receipt; wrong JSON object shape; failing subprocess. These tests do not execute the denied real-data diagnostic.
RED against unchanged source: two failures and three passes, exactly the missing-result classification and wrong-shape failure. After the repair, all five pass.
Full suite first reported185passed/twofailed because the sparse workspace omitted committed gap/legacy-receipt fixtures. Their exact HEAD versions were materialized locally; tests and fixture contents were not altered to obtain a pass. The rerun uses an isolated pytest directory rather than trying to clean unrelated worker caches.
Final replay suite:187passed, process52793 exit0. Scope: the existing test_prophet_pit_replay.py plus the five new regression tests. This is local/source proof, not repository CI, independent review or successful historical control fidelity.
Repaired replay source SHA256:9ffbab91ed1919e2cbd2bbb5ff41da68b47e3a589d1f01510a4e8cfc7afe01b4.

## 4. Research accounting: actual append, not the stale proposed patch

The existing canonical TrialLedger owner was used in the isolated PR checkout. Base ledger bytes were verified against the pinned default branch and preserved as an exact prefix. The temporary-copy test verified the append and zero-byte change on an identical second call.
Seven rows were added: one explicit retrospective minimum floor of6502 and six separately itemized, not-yet-executed recorded-score configurations. The floor includes the previous6496 mechanically counted diagnostic views and six proposed ranker/basis strata. It is not a claim of6502 independent tests, complete search-history certification, corrected significance or scientific acceptance. A broader accepted exposure count may only raise it.
Before:1674rows, SHA2567a82e5b7766a7f2bbeb9e8c46bb57ab0b9a1b32d262a853944d093645769f119. After:1681rows, SHA2567b6d1d8d3c7346e460631383158095ddc25e95e8c38c5fc25d77af951ebd7708. Literal configs6; effective_n6502; repeated append adds zero. Timestamps are actual recording times, not backdated preregistration.
ACCOUNTING_RECONCILIATION_2026-09-16.json records exact source, prefix preservation, repeated-call proof and the configs. Default-main application is false until a reviewed merge; do not call a branch append a merged canonical record. The old6235 patch is not applied.

## 5. Review, scientific holds and next action

Canonical TrialLedger tests:23passed, process63639 exit0. Together with the187 replay tests,210 local tests passed in the two final suites. This is not a full-repository CI claim.
A native GitHub Copilot review request was attempted once on PR7177. Subsequent review and requested-reviewer lists were empty. Therefore no started/completed independent review or approval is claimed, and the request is not a substitute for scientific review. No manual-provider fallback was spawned.
Recorded-score association remains unexecuted: the frozen d21/H21 contract, historical publication eligibility, independent review and supported population requirements are unchanged. The completed datasets, original/expanded crossover results, benchmark overlays and48 contrasts were not rerun or relabeled.
The previous 'shallow history' and 'no approved Macro workspace' blockers are superseded by this recovery. The next actual blocker is the upstream no-alpha result plus the diagnostic platform restriction, not a continuing inability to resolve July17.
Exact next operation: once the named diagnostic restriction is cleared, inspect the retained canonical control's upstream alpha failure and rerun the same control only after a material cause is addressed. Preserve all fidelity/date/chronology checks. Publish/merge source-only fixes only on exact-head verification and the existing review/hold release; do not treat that merge as evidence of predictive value.
The parent accuracy program still needs supported common-opportunity tests and prospective validation before any Prophet/Terminal ranking, entry, size, forecast or hero-probability change. Existing TOI/Temporal Grain/Elliott holds, DNR:KILL-OUTCOME-AUDITION, and source-specific ownership remain.

## Recoverable evidence

Existing host study root ends in macd-context-first-look-20260915-c3/cycle_extension_v2. New control artifacts: replay_control_20260916_r1 and replay_debug_20260916_r2. The latter retains only the canonical harness's own vintage directory at ff745b1ab542 for later authorized diagnosis. No background process is inferred from a retained directory.
The managed PR workspace and its source head are the only writer custody for this new code/accounting delta. Shared macro-main working files and all original grade/crossover records remain unchanged. Exact publication commit and final process dispositions are recorded in the existing Agent OS handoff and local CONTINUATION.md after push reconciliation.
