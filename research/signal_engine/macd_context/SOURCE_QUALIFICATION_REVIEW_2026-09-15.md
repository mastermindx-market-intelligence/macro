# MACD continuation — Prophet source qualification and label semantics

Date: 2026-09-15 America/New_York (tool receipts cross into September 16 UTC).
State: **PARTIAL research; zero production authority.** This review adds no new market-return estimate, confidence interval, live signal, trade rule, grader, identity system or probability.

## Verified delta

The previously absent comparison core is now implemented locally and passes its two synthetic tests. Its final output-writing/CLI block was refused by the platform; the 87-line module remains incomplete and no new market contrasts were run. Separately, the existing Prophet outcome-label owner was recovered and six synthetic contract tests passed against its exact current source. A bounded audit of the local graded ledger established its actual population, horizon and metadata coverage, and proved that this local copy differs from current GitHub main.

This is source/data qualification, not a new predictive result or independent scientific acceptance. Do not rerun the completed 360,571-observation replay or recite its numbers as a new outcome of this turn.

## 1. Exact sources and lineage

- Protected Skillpack: `Mastermind@7642aea155d2817219135b24246b55c1d7611c66`; INDEX, COLD_START, ACTIVE_EXECUTION, RECONCILE_STATE and CLOSEOUT read from that revision; schema v1/version 1.0.1/bootstrap 1.
- Macro main inspected: `c70caf6b4d4191eb5da8d213dd0d563b48d6242b`.
- Existing publication carrier: PR #7177, branch `sol/macd-cycle-research-20260915-c3`, previous head `41d5ee82289d8c29e2f024e28d5007bc41a14dc4`. The PR was open draft and unmerged.
- Research carrier remains Remote Desktop Commander, Mac Studio `3f5ce987-e3eb-40a3-af9f-4b0ae54919cc`; operation `macd-context-cycle-attribution-20260915-c3`. No settings, credentials or permissions were changed. No worker or watcher was commissioned.
- Direct-work rationale: PRINCIPAL_JUDGMENT for population/metric adjudication and LOWER_TOTAL_OVERHEAD for bounded existing-source checks. No approved Executive submission tool was discovered for a separate review worker; no dispatch is claimed.

The original frozen equity study ends on 2025-12-31. The local graded Prophet board history examined here begins in June 2026. They are different evidence streams, not directly joinable historical events.

## 2. Local graded history: measured coverage, not current-production status

Inspected file: `/Users/chriswong/Documents/Cluade/macro-main/data/us_board_ledger/retro_grades.parquet`.
Local Git blob: `47c1e2886fb7689488082f7cf6be337535490502`.
Local SHA256: `905121ee814f845c4476f4919bb650e4997a53aa9215a383c66c6ce72aba144d`.
Local size: 673,250 bytes.

Current-main directory metadata identifies a **different** blob, `67e2541add91fa335918c95e098d9aec0fcf7030`, size 778,145 bytes. That exact blob was read through local Git object access without modifying the checkout. A subsequent call to analyze it was blocked. No current-main coverage statistics were obtained or substituted from the older local file. None of the following counts describes current production freshness or the latest canonical dataset.

The local copy contains 8,116 grades and 87 columns, covering 1,009 tickers, 37 board dates, and 3,993 distinct `(as_of, lane, ticker)` rows. Its dates span 2026-06-15 through 2026-08-27. There are zero duplicate `(as_of, lane, ticker, horizon)` keys, zero missing entry dates, and zero entries on or before their board date. Every stored grade has a non-null return.

| Horizon | Grades | Distinct board dates | Earliest board | Latest board |
|---|---:|---:|---|---|
| 5 sessions | 3,993 | 37 | 2026-06-15 | 2026-08-27 |
| 10 sessions | 2,747 | 27 | 2026-06-15 | 2026-08-20 |
| 21 sessions | 1,376 | 17 | 2026-06-30 | 2026-07-31 |

No 63-session row is present in this local copy. This is a coverage observation, not evidence of a failed current grader. Maturity and source-version checks come before outage claims.

The buy lane alone contains 2,062 five-session grades over 37 dates/797 tickers, 1,365 ten-session grades over 27 dates/610 tickers, and 675 twenty-one-session grades over 17 dates/352 tickers. Watch, leaders, laggards and ran lanes are also present in the full file; treating all 8,116 rows as independent buy trades would be wrong.

Only 178 of the 1,009 local-ledger names occur in the frozen MACD research universe. They account for 1,354 grade rows. There are zero ledger rows dated before 2026, hence zero direct event-date overlap with the frozen 2010–2025 study. The raw crossover results therefore remain raw crossover research, not a replay of historical Prophet admissions.

### Price-basis and provenance coverage

| Local price-basis stamp | Rows | Share |
|---|---:|---:|
| adjusted | 5,358 | 66.02% |
| unverified_pre_20260806 | 1,342 | 16.54% |
| null | 950 | 11.71% |
| unadjusted | 466 | 5.74% |

Unverified is not presumed incorrect. The source README explains the correction eras and frozen-history policy. Preserve and stratify these groups; neither silently pool them nor discard them after examining performance. Among the 675 local buy-lane H21 grades, 604 are adjusted, 37 unadjusted and 34 unverified-pre-boundary.

The inspected schema lacks `board_definition`, `candidate_id`, `event_id`, `policy_version`, `anchor_era`, `signal_maturity` and `model_version`. These may be recoverable from canonical snapshots and version history; absence here is not proof that the entire platform lacks them. A champion/challenger comparison must resolve the required provenance rather than infer it from a current label.

`entry_status` is null on 766 rows. A reason is present for 269 of those, leaving 497 with neither status nor reason in this local copy. Sector is populated on all 8,116 rows; `vol_regime` and `regime_vector_degraded` on 6,956; `tier_cascade` on 2,502; `align_tier` on 1,325. Unknowns and field-start dates must remain visible. Do not fill historical gaps with today's state.

## 3. Reuse the existing outcome owner; its hit is not raw profitability

Recovered owner: `scripts/prophet_fusion_labels.py`, current Git blob `6f18763a30b14b28980a8140aeb8b9dd485855fb`. The local source was byte-verified against that blob before execution. This module defines the research label frame over existing board-ledger and Prophet-rank stores; it is not a new evaluator proposed by this study.

Its `hit`/O2 implementation is **`excess_spy > 0`**, not `ret > 0`. This is a valid relative-performance outcome, but it cannot be compared interchangeably with the positive-price-return percentages in the MACD reports.

Synthetic source-executed demonstration, not market observations:

| Synthetic case | Stock return | SPY return | Excess | Existing hit |
|---|---:|---:|---:|---|
| Stock loses, but less than SPY | -5% | -10% | +5% | true |
| Stock gains, but less than SPY | +3% | +5% | -2% | false |
| Outcome unavailable | unknown | known | unknown | null |

Therefore report raw/net profitability, benchmark-relative selection and target-before-stop success as separate outcomes, through their existing owners. Never relabel an old stored outcome or infer that a higher relative hit rate means fewer cash losses. A future comparison must declare exactly which outcome it uses.

The source explicitly defers entry quality and confidence calibration. The synthetic receipt confirmed `deferred_heads = [entry, confidence]`. This module cannot supply validated trade-entry success or calibrated numeric confidence by merely reusing its O2 field. Entry outcome ownership remains with Live Entry Radar under the accepted contract; no rival entry grader is introduced here.

## 4. A real historical null era must not be backfilled into training

Current `data/us_board_ledger/disclosed_gaps.json`, blob `3881c93009d169fe953d79e142715afff1f7ba8e`, documents the 2026-08-01 through 2026-08-06 frozen-factor era, including trading dates August 3–6. The board published using factors stuck at July 31 while entry prices advanced. The recorded incident is historical and has a documented repair; it is not evidence of a present outage.

The accepted record labels this era ungradeable and not normally backfillable. Reconstructing a different, freshly ranked board would require a separately identified replay, not silently replacing what actually published. This matters directly to regime research: an apparent regime failure must not unknowingly be a mixed-vintage software incident.

The label owner reads the gap record and excludes this era. A synthetic row dated 2026-08-04 was actually excluded, with one exclusion reported. The missing/malformed-gap-file fallback also exists in source, but this turn's executable test used the verified real metadata file rather than claiming a fallback-path test.

## 5. Executable contract proof

New local test file: `cycle_extension_v2/test_prophet_label_semantics.py`, SHA256 `eff51c6e5b09130634c0431a76486c3fc8ab8e17be6e798cf869d85d094f4355`.
Command: `python3 -B <artifact-root>/cycle_extension_v2/test_prophet_label_semantics.py --repo-root /Users/chriswong/Documents/Cluade/macro-main`.

The command pins both label-source and gap-metadata blobs before loading them. It reads no market-price or outcome store. Process 7556 completed exit 0 with six tests passing: relative-hit semantics; unknown is not a loss; entry/confidence remain deferred; mixed price bases raise `PriceBasisPoolRefusal`; empty frames raise `StoreEmptyRefusal`; disclosed-null-era exclusion.

These are source conformance tests, not independent scientific acceptance or live consumer proof. The existing owner distinguishes mixed-era pooling from merely carrying multiple eras as strata. Reuse this behavior instead of creating another grader or erasing source groups.

## 6. Comparison implementation and exact unfinished boundaries

`review_comparisons.py` now exists with 87 lines, SHA256 `33321525eadcef032e7dc70397bbe0b171e6dc9622929b6118ee981ebd4d6bd3`. It implements the joint-year pooled and shared-date comparison core and passes the two existing synthetic tests. The final output-writing/entry-point append was blocked, and direct readback confirmed that it is absent. The module is **PARTIAL, not a runnable completed market study**. Do not call its main function or reconstruct the refused output through another route.

The separate current-main ledger analysis call was also blocked after the exact Git object was retrieved. Its content was not analyzed through an alternative tool or carrier. Local-copy counts above are explicitly not a substitute for that missing current-source audit.

The proposed 6,235-cell TrialLedger reconciliation remains a proposal, not applied canonical registration or an independently meaningful test count. Current GitHub update_file accepts full replacement text, not an append patch; the attended workspace route inspected earlier is Mastermind-scoped. Current protected documentation explicitly keeps Macro writable Executive workspaces outside that route's accepted scope. No raw worktree, source-wrapper repointing, shared checkout write or alternative authentication path was used. The patch's administrative meaning, source custody and actual application remain outstanding. No new ledger is created.

No separate reviewer accepted the research in the observed PR discussion. Vercel preview failures in that discussion are infrastructure/deployment feedback, not scientific reviews; this research makes no deployment claim.

## 7. Next bounded admission contract

Before a Prophet challenger run, resolve one source-qualified frame using existing `load_frame`/`build_labels` and canonical snapshot/version provenance. Its acceptance must name exact store/blob, event and observation clocks, champion version, lane/population, horizon maturity, price-basis strata, source exclusions and an explicit outcome identifier. A missing or unprovable item is a typed unknown/refusal, not a substitute value. The reconstructed board-ledger frame's own survivorship warning remains attached; code comments describing a store as historically empty are not current liveness evidence.

Keep three evidence classes separate: 2010–2025 descriptive crossover research; the actual 2026 candidate/outcome history; and later prospective challenger accrual. Data availability is not approval to join different eras or definitions. Two timing policies with different fills/exits need policy-specific outcomes under the existing grading owner, even when they share a candidate episode.

Primary continuation: complete the current-source frame qualification and approved retrospective-accounting application, then independently review the published evidence and finish the already-declared contrasts only after the exact execution gate clears. Preserve the no-promotion hold and do-not-redo completed replays. Do not build a hero probability, replace RSI-MACD, optimize per-name winners, or reopen held TOI/Temporal Grain/Elliott outcome waves by implication.
