# 300363.SZ — post-selection pipeline forensic — quantitative claims quarantined

**Status (2026-08-09 re-audit): `STOP_SHIP_UNVALIDATED_QUANTITATIVE_EXHIBIT`.**
**Authority:** none. This outcome-selected case may be used only to inspect record lineage and
failure modes. It is not evidence for a candidate, rank, gate, Prophet/Neural Web context,
position, execution, return, or legal limit-band verdict.

## Why the legacy quantitative account is withdrawn

The earlier version treated `data/china_stocks/300363.SZ.parquet` as nominal exchange-price
evidence. That is false under the repository's collector contract:

- `collectors/china_stock_prices.py` uses the shared Yahoo OHLC collector whose default is
  `auto_adjust=True`; the stored history is a split-adjusted total-return plane.
- A split-adjusted price series cannot prove the CNY 0.01 legal price for a historical session,
  reconstruct an exact upper/lower band, or distinguish a legal seal, touch, retreat, lock, or
  fill from a tolerant return proxy.
- `research/CN_TUSHARE_FULL_A_SPINE_CONTRACT_2026-08-08.md` explicitly quarantines the legacy
  Yahoo event artifact. The current canonical Wave-2 receipt remains
  `BLOCKED_SUBSTRATE_NO_STRATEGY_MEASUREMENT`: no authorized, complete, promoted TuShare
  measurement plane exists in this repository.

Therefore all earlier exact OHLC, percentage-move, forward-return, legal-limit, locked-board,
fillability, score-decomposition, rank-counterfactual, and entry-price conclusions for this
name are withdrawn. They must not be quoted, recombined, or used as labels in another dataset.
Tolerance to a reconstructed percentage band does not cure the source defect.

## Non-price forensic questions that remain inspectable

The committed stores can still support a plumbing audit, provided every statement is labelled
post-selection and is re-read from its pinned artifact before reuse:

1. Same-date candidate/shadow and board artifacts carried contradictory eligibility states.
2. Multiple Asia-lane runs could feed different keep-first stores without a common `run_id`, so
   a stored rank vector need not represent one published board.
3. The bot grounding path exposed aggregate counts rather than a per-ticker decision receipt;
   no position or execution artifact was found for the reported trade leg.
4. Forward-ledger price fields changed when source bars healed, and the declared fill basis did
   not bind the observed value. A published result was therefore not point-in-time immutable.
5. Component schemas and the aggregate score could diverge across board versions.

Those are defect hypotheses and lineage observations, not proof that the name should have been
selected. Because the case was chosen after its outcome was known, it has zero promotion or
replication authority even after its price substrate is repaired.

## Hard gate before any exact legal-band or return verdict

An exact case may be rebuilt only from the canonical full-A event plane described by
`research/CN_TUSHARE_FULL_A_SPINE_CONTRACT_2026-08-08.md` and
`research/cn_limit_alpha_sol/W2_BAND_PROGRESS_CONSTRUCTION_PROTOCOL_2026-08-08.md`:

1. authorized TuShare **unadjusted `daily`** rows and same-key vendor **`stk_limit`** rows;
2. one promoted-generation manifest with verified hashes, exact session/security identity,
   point-in-time lifecycle/ST/no-limit state, and known corporate-action reference state;
3. one-to-one daily/limit joins with integer-cent OHLC, previous-close equality between the two
   endpoints, inclusive OHLC containment inside the vendor bounds, and no missing limit source;
4. legal upper touch only when `high_cents == up_limit_cents`, and legal sealed close only when
   `close_cents == up_limit_cents` — exact integer-cent equality, never `>=`, rounded returns,
   or a tolerance band; and
5. immutable signal/run/entry receipts with a common `run_id`, an attested T+1 session clock,
   and an execution/fill model kept separate from daily-bar tradability proxies.

If any gate is absent, the only permissible result is
`BLOCKED_SUBSTRATE_NO_STRATEGY_MEASUREMENT`. A future valid reconstruction must use a frozen
full-board cohort and pre-registered comparison; this outcome-selected name cannot authorize a
Prophet weight, species lane, score, rank, signal, or trade by itself.
