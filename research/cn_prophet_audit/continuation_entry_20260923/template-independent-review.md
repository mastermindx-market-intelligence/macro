STATUS: PARTIAL

RESULT:
Reviewed head as committed in `/Volumes/Mastermind/worktrees/cn-act-now-coherence-20260921` (PR7567, `887917fbb1190ddee5dcfc76de557ddb195d0c7b`; baseline `8229b1efea8f0270ad76a56e1a7634894c73f8bc`). **No source blocker and no source major remains.** All three prior majors are closed at source, and dark and light each pass independently as designs against the committed 48-board + 24-hover matrix. PARTIAL is owed to four minor findings and to the still-open hosted/current-base/production gates the artifact itself declares (`evidence.json:4` `BUILT_NOT_PROVEN`, `:9` `release_allowed:false`, `:103-108`) — not to a defect in the repaired logic. I executed nothing; all test counts below are read from committed logs.

**Prior finding disposition (research/cn_prophet_audit/continuation_entry_20260923/independent-review.md)**
1. Pre-settle freshness outage — **CLOSED (as disclosure).** `engine/china_act_now.py:706-707` adds `UNSETTLED` (`day > expected and day == now CST date`), `:780-783` renders it as `SESSION SETTLING`/`交易日数据待确认` instead of `DATA UNAVAILABLE`; pinned `tests/test_china_act_now.py:1307-1314`; captured 8 board + 8 hover frames (`browser/proof.json:540-651`, `:1038-1165`). Residual (not a defect): the pre-settle window still empties Buy Now, which is the intended conservative calendar rule (`:707` comment).
2. Stale affirmative figures surviving the guard — **CLOSED.** `:774-779` nulls `score/rel20/rel5/breadth_pct50/leadership/leaders_*/n_members/phase/osc_slope/pos/rs_63d/rs_rank/organ_*`, empties `reasons`, pops `entry_route`; pinned `tests/…:1287-1304` (asserts `anv2-score-hi` and `+13.7`/`+21.5` absent) and confirmed in the captures — `hover-missing-members-*` and `hover-settling-*` carry no score ring, no stats, no leaders (`proof.json:917`, `:1045`).
3. Evidence matrix proved only the happy path — **CLOSED.** Six scenarios (current, missing-members, source-conflict, final-demotions, settling, duplicate-evidence) × dark/light × EN/ZH × 1440/390 = 48 board captures, `overflow:false` and `page_errors:[]` on every one (`proof.json:75-779`). Residual minor: hover captures exist for only 3 of 6 scenarios (`proof.json:781-1165` — no popover frames for source-conflict, final-demotions, duplicate-evidence).
4. Degraded chip painted with the HOLD recommendation token — **CLOSED.** `templates/_china_act_now_board.html.j2:230-231` emits a bare `.anv2-chip`; pinned `tests/…:1304`, `:1352`.
5. Silent exception swallow — **CLOSED at log level.** `:708-711` now logs `log.debug(type(exc).__name__)` plus `log.info("China theme action availability: %s")` on every non-CURRENT status. No user-facing `notes` entry (nit, see below).
6. Second recommendation vocabulary in the display layer — **CLOSED for the verb path.** `:788-789`, `:800-801` take `td.get("reco_en")/td.get("reco_zh")`; pinned `tests/…:1317-1322`. Residual minor B/1 below.
7. Buy Now no longer score-descending — **CLOSED.** `:817-824` re-sorts THEME rows by score desc with non-THEME rows appended after; pinned `tests/…:1325-1335`.
8. Mobile chrome overlap — **still visible** (`candidate-light-zh-390.png`, assistant orb over 有色金属). Host chrome, not authored here. Nit, unchanged.

**New findings (all minor)**

- **A (minor — unsupported claim in a degraded state).** `engine/china_act_now.py:781-783` + `templates/_china_act_now_board.html.j2:78` emit "Wait for the completed session data. **This is not a change in the theme thesis.**" / "请等待交易日数据确认；**这不表示主题逻辑发生变化**。" for *every* `UNSETTLED` theme row, unconditionally. Under `UNSETTLED` the guard deliberately refuses to trust `td["reco"]` (`:748` `coherent = status == "CURRENT" …`), so the card simultaneously declines to read the verdict and asserts the verdict is unchanged. The same-artifact producer can already carry `hold/trim/avoid` in that window (the demotion scenario, `browser_proof.py:64-67`), in which case the sentence reassures over a defensive change. Rendered in all 8 settling hover frames (`proof.json:1045`, `:1077`). Smallest repair: drop the second sentence, or gate it on `final in {"enter","accumulate","hold"}`.
- **B (minor — JSON contract carries a verb the same object declares unavailable).** The neutralising `row.update(reco=None, reco_en=…, reco_zh=…)` is nested under `if lane == "wait_pullback"` (`engine/china_act_now.py:780-783`). An `UNAVAILABLE`/`UNSETTLED` THEME row that stays in `reduce_avoid` therefore keeps `reco="avoid"`, `reco_en="AVOID"`, `reco_zh` beside `theme_decision.status == "UNAVAILABLE"` in the published `display_lanes`. Nothing user-facing is wrong today — the board template masks it (`:73-75`, `:169-170`, `:230-231`; pinned `tests/…:1338-1352`) and both includes use that one partial (`templates/china.html.j2:1719`, `templates/sector_central_china.html.j2:959`) — but the guard lives only in the template, so any other consumer of `act_now_cn.json` inherits an unqualified verb. Smallest repair: unindent the `row.update(reco=None, …)` out of the `lane == "wait_pullback"` branch.
- **C (minor — evidence fidelity limits the ZH-parity claim).** The harness stubs `tr=lambda text: text` (`browser_proof.py:84`) and calls `assemble_act_now([], source, cycle, observed_at=at)` with no `member_names` (`:80`). Consequently the committed ZH captures show untranslated English inside the ZH card — `proof.json:821` "20d +1.5% vs CSI 300 · all timeframes aligned up" under 领先度判断 — and LEADERS as bare tickers "688428.SS · 300347.SZ · 002821.SZ" (`proof.json:789`, `:821`), which `engine/china_act_now.load_member_names` (`:632-634`) itself calls banned user-facing copy under DESIGN_DOCTRINE Law 2. These are harness artifacts, not source defects at this head, but they mean the matrix does **not** prove ZH parity of the reason/sub line or the leaders line in the affirmative CONTINUATION card — the one card the change creates. Smallest repair: state the limitation in `proof.json`, or re-capture with the real `tr`/`member_names`.
- **D (nit).** Duplicated degraded label pairs with nothing binding them: `SESSION SETTLING`/`DATA UNAVAILABLE` and their ZH twins are authored in the engine (`:782-783`) *and* independently in the template (`:170`, `:231`). The template wins in HTML, so a drift would be invisible on the page while still shipping in `act_now_cn.json`. Related nit: a board-wide `UNAVAILABLE` still writes no entry into the rendered `notes` channel (`templates/_china_act_now_board.html.j2:61-63`), so the operator signal is log-only.

**Attacks that failed to falsify the artifact (no finding)**
`reduce_avoid` can never be promoted (`:784-785`, pinned `tests/…:1193-1201`); a conflicted theme with an affirmative final is rewritten to `CONFLICT — WAIT` (`:790-792`) and a final hold/trim/avoid outranks a stale affirmative lane (`:786-789`, `tests/…:1147-1155`); unknown/missing final verbs cannot show Current (`:760`, `:769`, `tests/…:1355-1365`); `aggregate_eligible is True` rejects `1`/`"true"` and `effective_as_of`/`mtf`/`tape` future dates fail closed (`:753-759`, `tests/…:1232-1276`); duplicate theme ids drop out (`:712-721`); naive clocks refuse (`:697-698`, `tests/…:1227-1229`); raw `lanes` and `source_reads` are untouched (double `deepcopy` `:271-274`, `:741`; `tests/…:1117-1129`, `:1299-1300`; `proof.json:19-20`, `browser_proof.py:35-36`); no `KeyError` on lane rewrite — `assemble_act_now` always passes all four lane keys (`:591-599`); BASKET/SECTOR rows bypass the guard unchanged (`:742-744`), preserving cycle-only BASKET behaviour; `tone: ''` on a degraded card yields a genuinely class-less neutral tag and takeaway (`templates/_decision_card.html.j2:228`, `:236`, `:289`). No new CSS, no new palette, no runtime stylesheet: the degraded states reuse the existing `.anv2-chip` base class, and the template's pre-existing inline `<style>` (`:18-37`) is unextended — no design-system bypass.

**Dark, adjudicated as a design** (`candidate-dark-en-1440.png`, `candidate-final-demotions-dark-zh-390.png`, `hover-settling-dark-en-1440.png`): graphite lane panels sit on a near-black canvas with a single saturated accent rule at each lane's top edge; hierarchy runs name → score numeral at the right optical anchor → outlined status chip → 11px muted stat line, and luminance (not border weight) does the separating. The degraded card collapses proportionally — name, one neutral outlined chip, one "DATA STATUS" kicker — while the hover popover keeps two material planes (card + inset WHAT MATTERS block), so an empty-data state still reads as an instrument, not a stub. Semantic colour is honest: in a degraded card the only chromatic element left is the theme identity glyph, explicitly carved out by this commission; status ink is neutral. No glow added.

**Light, adjudicated as a design** (`candidate-source-conflict-light-en-1440.png`, `candidate-light-zh-390.png`, `hover-missing-members-light-zh-390.png`): white research cards on a cool grey canvas; the separation mechanism genuinely changes — hairline borders and a soft shadow replace dark's luminance step, chips become grey-ink outlines on white rather than inverted dark chips, and score numerals take a denser weight to hold contrast. The degraded light popover uses a cool inset surface for the WHAT MATTERS block against the white card, i.e. a second material mechanism, not the dark treatment with swapped tokens. Responsive composition at 390 stacks lanes with headers, counts and the 4-row cap plus 查看全部 13 intact; `overflow:false` on all 48 frames. EN/ZH: every new string is an authored `t(en, zh)` pair (`:76-79`, `:170`, `:231`) and appears in ZH in the captures; the red/green inversion is the pre-existing documented Asia-convention token swap, not a defect.

EVIDENCE:
- `engine/china_act_now.py:689-725` (`_theme_display_context`: tz/ISO/session/duplicate/stale/UNSETTLED gates, `:708-711` logging), `:728-825` (`_continuation_display_lanes`: `:748-760` coherence, `:761-768` `theme_decision`, `:769-783` neutralisation, `:784-792` defensive/conflict precedence, `:793-815` continuation gate, `:817-824` ordering), `:591-599` lane keys + call site
- `templates/_china_act_now_board.html.j2:72-79` (`_unavailable`/`_settling`, read + takeaway overrides), `:169-174` (`_display_tag`), `:184` kicker, `:230-234` neutral chip, `:224` score chip gated on `row.score is not none`; `templates/_decision_card.html.j2:228`, `:236`, `:289` (empty tone → neutral)
- `tests/test_china_act_now.py:1287-1304`, `:1307-1314`, `:1317-1322`, `:1325-1335`, `:1338-1352`, `:1355-1365` (new degraded/settling/ordering/vocabulary/neutral-status cases); `:1147-1201`, `:1227-1284` (retained fail-closed cases)
- `research/cn_prophet_audit/continuation_entry_20260923/browser/proof.json:16-20` (frozen input, clock, raw lanes unchanged), `:21-74` (baseline vs candidate lanes, one continuation `cn_pharma_cxo`), `:75-779` (48 board captures, six scenarios), `:781-1165` (24 hover captures, three scenarios), `:1167-1172`
- `research/cn_prophet_audit/continuation_entry_20260923/evidence.json:17-32` (319 passed / 0 failed across seven owner suites — read from `template-owner-tests.txt`, not executed), `:33-34` (4 red / 1 already-green added degraded cases), `:53-63`, `:103-108`
- `research/cn_prophet_audit/continuation_entry_20260923/browser_proof.py:80-84` (identity `tr`, no `member_names`), `:70-73` (scenario/clock set), `:109-129` (hover scope)
- Images inspected: `candidate-dark-en-1440.png`, `candidate-light-zh-390.png`, `candidate-source-conflict-light-en-1440.png`, `candidate-final-demotions-dark-zh-390.png`, `hover-missing-members-light-zh-390.png`, `hover-settling-dark-en-1440.png`

GAPS:
- **Production/hosted acceptance remains entirely owed** and is not claimed here: `evidence.json:103-108` lists current-head hosted CI, integrated current-base proof, and authenticated full-path production proof as outstanding; `:72` `deployed:false`. No production PASS is declared.
- I ran no tests and no commands (METHOD forbids shell). The 319/0 count, the log sha256 set (`evidence.json:32`, `:36-50`) and the three source sha256 claims (`evidence.json:12-16`, `proof.json:1167-1171`) are **read**, not reproduced; I also could not verify that the working-tree bytes I reviewed equal commit `887917fb…` — I reviewed the checkout at the named worktree.
- Hover/popover evidence covers 3 of 6 scenarios in each theme; the `CONFLICT — WAIT`, final-demotion and duplicate-evidence decision cards are adjudicated from board captures only.
- ZH parity of the reason/sub line and the leaders line in the affirmative CONTINUATION card is **unproven** by this matrix (finding C); the harness's identity `tr` makes those strings pass through untranslated.
- The real-input scenarios exercise theme rows only — `proof.json` passes `[]` sectors and (except duplicate-evidence) `[]` cycles, so mixed sector/theme composition and a non-empty Bottoming Watch lane are not shown in either theme. Consistent with the commission's "no mixed-sector/full-page claim".
- Whether `theme_intel["stale"]` is ever written upstream was not re-checked this round; that arm of the gate (`:703`) may still be vacuous in production (carried forward from the prior review, unresolved).

DEVIATIONS:
- Read/Grep/Glob only; no edits, no shell, no test execution, no remote/API/credential calls, no delegation, no watcher or successor task. Findings touching `lib/cn_calendar.py:159-190`, `engine/theme_scoring.py:1244`, `scripts/build_china.py:1398`, `templates/_decision_card.html.j2` and the two including templates are cited as the contract surface of the three in-scope files, not as an audit of those files.
- I did not reopen accepted unrelated design (Asia-convention colour swap, theme identity glyph colour, assistant-orb chrome) beyond restating the one unchanged nit.
- Source/design adjudication is reported above and separately from the outstanding hosted/production gates, per ORDER. Terminal report returned to the same parent interaction; no follow-on work taken.
