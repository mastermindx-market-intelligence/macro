STATUS: PARTIAL

RESULT:
Source logic is sound in its core claim — the derived dual-read/conflict clearing and the single bilingual note work as specified, raw lanes/source_reads are untouched, and no score/threshold/timeline/ranker/eligibility/source-owner was changed. I found **no blocker**. I found **2 major** findings (both in the presentation/evidence half, not the engine) and **6 minor / 2 nit** findings. Under the dual-theme art-direction law this cannot be `PASS`: the new note ships with zero CSS of its own, dark and light differ only by an inherited ink token, no art direction is declared, and one of the note's two required variants appears in **no** browser capture in either theme, language, or viewport.

**Do the fixes close the prior findings?**
- *Derived dual-read/conflict advice on UNAVAILABLE rows* — **CLOSED** on the evidence inspected. `engine/china_act_now.py:791-798` clears `dual_chip_en`/`dual_chip_zh` and sets `dual_read=False`, `action_disagreement=False`, `reasons=[]`; the template gates the row chip (`:238`, `:242-244`) and the hover trust row + takeaway (`:144-147`, `:206-208`) on exactly those fields. Raw immutability holds via `deepcopy` at `:758` and is pinned by `tests/test_china_act_now.py:1406-1407`. Routing is provably unaffected: `action_disagreement` is only read in the coherent branch (`:809`) while the write is in the non-coherent branch (`:786-802`).
- *CURRENT qualified dual-reads/conflicts stay visible* — **CLOSED at data level only**. `:803-811` untouched; controls at `:1442-1450` and `:1048-1060`. Neither control asserts the chip actually renders (see minor 6/7).
- *One bilingual data note* — **PARTIALLY closed**. It exists, is bilingual, and is provably singular (`:1419`), but its partial/wholly discrimination is over-scoped (majors below are visual; minors 1–2 are copy), and the partial variant is unproven in the browser matrix.

EVIDENCE:

**SOURCE findings (engine/template/tests)**

1. *(minor — over-scoped disclosure)* `engine/china_act_now.py:607-611` emits `"…lane labels are not current recommendations."` / `"…分栏标签不代表当前建议。"` board-wide, but the lanes visibly carry sector rows the code never evaluated. In `mixed-browser/candidate-settling-dark-en-1440.png` the BUY NOW lane under that sentence shows **Real Estate / Defense & Military / Automobiles with affirmative `BUY NOW` chips**, and `candidate-missing-members-light-en-1440.png` shows the same. The note therefore mis-describes current sector advice. Direction is conservative (it disclaims rather than certifies), which is why this is minor, not major — but the second clause needs theme scoping.

2. *(minor — the "without certifying sector data" clause)* `engine/china_act_now.py:604-605` instructs `"use only cards with current data."` / `"仅参考数据有效的卡片。"`. A data-status marker exists **only** on THEME rows: `templates/_china_act_now_board.html.j2:76` derives `_unavailable` solely from `row.theme_decision.status`. SECTOR rows and non-theme BASKET rows carry no marker at all (`_continuation_display_lanes` passes them through unchanged at `:759-761`), so "cards with current data" silently includes every sector card. That is exactly the certification the review standard forbids, in the partial branch.

3. *(minor — unreachable discrimination / latent trap)* `:606` reads `all(d["source_status"] == "UNSETTLED" for d in unavailable)` as if `source_status` varied per row. It cannot: it is the single value returned by `_theme_display_context` (`:754`) and stamped identically on every row (`:782`). When that value is `UNSETTLED`, `coherent` is False for **every** theme row (`:765`), so `len(unavailable) == len(decisions)` always and the partial branch at `:603` is unreachable while settling. The settling-vs-partial distinction the standard asks for therefore cannot be expressed for a partially-settling board, and any future per-theme `status` would silently select the wrong message with no test to catch it.

4. *(nit — dead predicate)* `:777` already folds `final in {...}` into `coherent`, so the `or final not in {...}` half of `:786` can never be the deciding term.

5. *(minor — silent contract widening of a persisted artifact)* `:791-795` writes `row[field] = None` for fields a theme row may never have carried (`rs_rank`, `phase`, `pos`, `osc_slope`, `organ_state`). Those rows are serialized to `site/chinabasketdata/act_now_cn.json` (`scripts/build_china.py:1408-1412`) and re-read by a second builder (`scripts/build_china_sector_central.py:120-135`) rendering the same template. The template is null-tolerant (`:171`, `:227`, `:246`, `:251`) so nothing breaks in what I read, but the published row key-set now differs by degradation state. Worth a line in the contract doc.

6. *(minor — test receipt gap)* `continuation_entry_20260923/derived-advice-red.txt:69-74` records exactly 4 failures = **2 test names × 2 clock params**, with "142 deselected". `tests/test_china_act_now.py:1422-1439` (`test_data_note_is_partial_without_disqualifying_the_current_theme`) and `:1442-1450` (`test_current_defensive_dual_read_is_preserved`) have **no recorded pre-repair run**. The former would have raised `KeyError: 'theme_data_note'` pre-repair, so it does kill the old behaviour — but that is my inference, not a receipt. Claim "4 new tests failed before repair" in `review-adjudication.md:76` is supported for 2 of them.

7. *(minor — render assertions asymmetric)* The negative side is render-asserted (`:1408-1410`: no "may be bottoming", no "longer trend still says reduce"). The positive controls are **data-only**: `:1449-1450` asserts `dual_read is True and reco == 'avoid'` and `:1057-1060` asserts `action_disagreement is True`, neither asserting that `anv2-chip-washout` / "Conflicting actions" actually renders (`templates/_china_act_now_board.html.j2:238`, `:242-244`). An over-clearing regression in the *template* would pass every test.

8. *(confirmed, not a finding)* The qualified-conflict fixture retains its assertions. `tests/test_china_act_now.py:1048-1060` re-bases onto `_continuation_fixture(clean=True)` with the rationale stated in-comment at `:1049-1050`, and the three original assertions (`buy_now == []`, `action_disagreement is True`, `len(source_reads) == 2`) are intact. No assertion waived — `review-adjudication.md:76` is accurate here.

**DESIGN findings (dark and light adjudicated separately, as designs)**

9. *(major — light art direction absent; dark treatment absent too)* `templates/_china_act_now_board.html.j2:62` is the entire presentation: a bare text node dropped into `.anv2-notes`, whose only rule is `templates/theme.css:1631` `display:flex; flex-wrap:wrap; gap:6px; margin-bottom:8px` — a **chip-row container**, not a prose container (with a single anonymous flex item, `gap` and `flex-wrap` are inert). No `.muted`, no font-size, no icon, no border, no surface, no semantic tone. Contrast the sibling line `:65`, where every existing note is wrapped in `<span class="muted" style="font-size:11px">⚠ …`.
  - **Dark** (`candidate-settling-dark-en-1440.png`): renders at full body size in near-full `--text` ink, immediately under the H2 and above the lane grid. It is the second-loudest element on the board — louder than every lane subtitle, and louder than the ⚠ notes it is stacked above, inverting that pair's hierarchy. No luminance depth, no restrained tint, no instrument-calm treatment. A degraded-state disclosure carrying no semantic color at all.
  - **Light** (`candidate-settling-light-zh-390.png`, `candidate-missing-members-light-en-1440.png`): geometrically and materially **identical** — same size, same weight, same position, same absence of container/hairline/shadow. The only delta is the inherited ink token flipping. There is no light-specific mechanism (no hairline rule, no white material card, no shadow) and correspondingly nothing named as intentionally differing. This is token substitution presented as a light design, which the theme art-direction law names as the exact failure it exists to stop.
  - Not a runtime-stylesheet bypass: no JS styling was added, so that blocker clause does not fire. The `<style>` block at `:18-37` is pre-existing and untouched by this change.

10. *(major — evidence matrix does not cover the artifact under review)* `mixed-browser/proof.json:125-829` records 72 captures whose per-capture fields are `continuationRows`, `buyRows`, `continuationInWait`, `overflow`, `page_errors`, `actualTheme`, `actualLanguage`. **Not one field records the note's presence, absence, or text.** And the **partial** variant is captured nowhere: `current` → `theme_data_note: null` (`mixed-browser/board.json:4220`); `settling` and `missing-members` → all theme rows unavailable, so both render the *wholly-unavailable/settling* copy (visually confirmed in the two captures I opened); `source-conflict`, `final-demotions`, `duplicate-evidence` → CURRENT, no note. So one of the two variants the review standard requires exists only in a unit assertion (`tests/test_china_act_now.py:1436`) and has never been rendered in any theme, language, or viewport.

11. *(minor — no art-direction declaration)* Nothing in `mixed-browser/proof.json` or the entry artifacts I read names DARK TREATMENT, LIGHT TREATMENT, which mechanisms intentionally differ, the reference/baseline, or theme-specific degraded states for the new element. `proof.json:22` documents the harness CSS bundles only.

**Confirmed non-violations of the "no new …" clause:** `stock_entry_permission` still hard-False (`:784`); sort key `theme_score` unchanged (`:838-843`); no numeric constant, lane threshold, calendar rule, or eligibility predicate added; `_theme_display_context` (`:706-742`) appears untouched by this change; `scripts/build_china.py:1398-1404` still calls `assemble_act_now` without `observed_at`, so production keeps `datetime.now(timezone.utc)` (`:710`).

GAPS:
Uninspected, stated plainly — these bound every verdict above.
- **I never saw the literal cf9↔5cc diff.** No shell/git was available. Everything is read against the cf9 working-tree bytes plus `derived-advice-red.txt` and `review-adjudication.md`. Any statement of the form "this changed" is inference from those receipts, not from a diff.
- **Capture freshness is unverified.** `mixed-browser/proof.json:1217-1220` pins sha256 for `engine/china_act_now.py`, the template, and the test file; I cannot compute sha256 with Read/Grep/Glob. If any byte landed after capture, the entire visual matrix is stale and findings 9–10 (and my "unchanged mixed composition" read) are void. **This is the single highest-value parent check.**
- **4 of 72 PNGs opened**: `candidate-settling-dark-en-1440`, `candidate-settling-light-zh-390`, `candidate-light-zh-390`, `candidate-missing-members-light-en-1440`. Unviewed: settling dark-390, all zh-1440, all 24 `hover-*` crops, and the `source-conflict` / `final-demotions` / `duplicate-evidence` scenarios entirely.
- **Green receipt not opened.** I read `derived-advice-red.txt` in full; I did **not** open `derived-advice-qualified-tests.txt` / `final-tests.txt`. The 331-pass figure is taken as a read receipt per the commission, not verified.
- **No execution of any kind**: no pytest, no render, no `check_design_system.py --mode enforce-added`, no `check_runtime_style_injection.py`, no `check_ui_visual_evidence.py`, no `check_template_site_sync.py`. Note this worktree is sparse (`derived-advice-red.txt:68`), so template↔site pairing could not be checked even in principle.
- **Consumer trace incomplete** for finding 5: I read the writer (`build_china.py:1408-1412`) and the reader call site (`build_china_sector_central.py:120-135`) but did not enumerate every consumer of the published row key-set.
- **Production gaps (parent's, not source defects):** no live/hosted verification, no CI pack result, no current-base check, no rendered-page proof on either `china.html` or `sector_central_china.html` — and the second page renders this note from a *persisted* `act_now_cn.json`, so a pre-cf9 artifact there yields no note (safe, via `.get` at template `:61`, but unproven).

DEVIATIONS:
- The commission named `continuation_entry_20260923/…`; the actual path is `research/cn_prophet_audit/continuation_entry_20260923/…`. Resolved by Glob, no scope change.
- Method honoured: Read/Grep/Glob only. No edits, shell, network, tests, credentials, delegation, or queue. Read-only throughout.
- I did not open `5cc-independent-review.md` at all. Findings 1/2/4 were reachable from `review-adjudication.md:76` and the red receipt, so the read was unnecessary; consequently my "closes the prior findings" judgement is against the commission's own restatement of them, not against that document's text.
- I did not reopen the closed A/B/C fixes, and I propose no trading-model change. Finding 3 touches `_theme_display_context` only to characterise reachability of the new note branch — no archaeology performed on it.
- Per standing house law, missing light art direction plus incomplete dual-theme evidence forces `PARTIAL`; I did not exercise discretion to pass the source verdict through on the strength of the engine logic alone.

Returning to parent for adjudication and repair. No further action; awaiting exact-session STOP.
