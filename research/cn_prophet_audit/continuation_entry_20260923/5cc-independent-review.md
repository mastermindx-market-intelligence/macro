I reviewed the frozen artifact (engine, template, tests, receipts, and the dark/light × EN/ZH captures) read-only via Read/Grep/Glob.

STATUS: PARTIAL

RESULT:
The three prior majors (A/B/C) are genuinely closed at this head, and the mutation evidence is real. But one **major** and three **minor** findings remain, all in the degraded-state path that ATTACK (B) names. No blocker; no stock-admission/scoring/sizing change; no design-system bypass.

**A — settling copy: CLOSED.** The predecessor's "This is not a change in the theme thesis" assertion is gone. `templates/_china_act_now_board.html.j2:78` now emits "Wait for the completed session data. No new entry is confirmed." / "请等待交易日数据确认；暂无法确认新的入场机会。"; `:76` read override "Session not yet settled"/"交易日数据尚待确认". Pinned by `tests/test_china_act_now.py:1374-1382`, which asserts the absence of both the EN and ZH stability sentences across `final ∈ {accumulate,hold,trim,avoid,None}` — and `minor-review-red-qualified.txt:161-165` shows all five failing on the pre-repair source. Real kill, not a tautology.

**B — reduce_avoid neutralization: CLOSED for the reco, NOT for its derived chips.** `engine/china_act_now.py:769-782` is now unindented out of the `wait_pullback` branch, so an UNAVAILABLE/UNSETTLED theme that stays in `reduce_avoid` gets `reco=None`, `reco_en="DATA UNAVAILABLE"`, evidence retained in `source_reads`. Pinned `tests/…:1338-1358` (`avoid-reduce`/`trim-reduce`), red pre-repair at `minor-review-red-qualified.txt:159-160`. See finding 1 for what the sweep still misses.

**C — proof honesty: CLOSED and materially improved.** `browser_proof.py:18,95` uses the real `engine.i18n.tr`; `:31-35` loads member names through the actual `engine.china_act_now.load_member_names` on the same frozen bytes (`proof.json:20-24`, count 280). The captures confirm it: `hover-current-dark-en-1440.png` shows "InnoCare · Tigermed · Asymchem", `hover-current-light-zh-390.png` shows "诺诚健华 · 泰格医药 · 凯莱英" — the predecessor's bare-ticker artifact (`template-independent-review.md:20`) is gone. The residual English numeric fallback is truthfully declared (`proof.json:19`, `evidence.json:64`) and matches what renders; see finding 3.

**Preservation checks that failed to falsify (no finding):** defensive precedence (`:783-784`, pinned `:1193-1201`); conflict→`CONFLICT — WAIT` (`:789-791`); final hold/trim/avoid outranking a stale affirmative lane (`:785-788`); unknown/missing verbs → UNAVAILABLE (`:760`,`:769`, pinned `:1362-1371`); `aggregate_eligible is True` identity check and `effective_as_of`/`mtf`/`tape` date agreement (`:753-759`); duplicate theme ids dropped (`:712-721`); naive-clock refusal (`:697-698`); raw lanes and `source_reads` immutable (double `deepcopy` `:271-274`,`:741`; `proof.json:25-26`); BASKET/SECTOR cycle-only bypass (`:742-744`). Ordering: I specifically tried to falsify `:821-823` as a sector-reordering regression and could not — themes are appended to `buy_now` before sectors (`:349-351` vs `:386-399`) and `_display_lanes` preserves index order, so "themes first, non-themes after" reproduces the pre-existing arrangement.

**Dark, adjudicated as a design** (`candidate-settling-dark-en-1440.png`, `hover-settling-dark-en-1440.png`, `hover-current-dark-en-1440.png`, `hover-settling-dark-zh-390.png`): graphite lane panels on near-black, one saturated accent rule per lane head, separation carried by luminance step. The degraded row collapses proportionally to name + one neutral outlined chip; the popover keeps two material planes (card + inset WHAT MATTERS block) so a zero-data state still reads as an instrument. Semantic ink is honest in the degraded card — the status chip is achromatic, not a repurposed hold/avoid token.

**Light, adjudicated separately as a design** (`candidate-settling-light-zh-390.png`, `hover-settling-light-en-1440.png`, `hover-settling-light-zh-390.png`, `hover-current-light-zh-390.png`): the separation *mechanism* genuinely changes — white cards on a cool grey canvas with hairline borders and a soft shadow replace dark's luminance step; chips become grey-ink outlines on white rather than inverted dark chips; the WHAT MATTERS inset is a cool tinted surface against white, a second material, not dark-with-swapped-tokens. At 390 the four lanes stack with headers, counts and the 4-row cap plus 查看全部 13 intact. This is a light art direction, not token substitution.

**EN/ZH parity:** every new string is an authored `t(en, zh)` pair (`:76-79`, `:170`, `:231`) and each appears in ZH in the captures. The red/green inversion on the ZH cards and on `.row-pop-mark--theme` is the documented A-share convention swap (`templates/theme.css:235-251`), not a defect — I verified it is language-keyed, not theme-keyed, by comparing `hover-settling-light-en-1440.png` (green mark) against `hover-settling-dark-zh-390.png` and `hover-settling-light-zh-1440.png` (both red).

**No design-system bypass:** the degraded states reuse the existing `.anv2-chip` base class and the canonical `_decision_card.html.j2` macro; the template's pre-existing inline `<style>` (`:18-37`) carries only the tape chip, name-link and hover-mark rules and is unextended by this change. Nothing is authored as a runtime stylesheet in page/composer JS.

EVIDENCE:

**1. MAJOR — `dual_read`/`dual_chip_*` survive the UNAVAILABLE sweep, producing a card that says "DATA UNAVAILABLE" and simultaneously asserts the neutralized reduce verb.**
- `engine/china_act_now.py:775-777` — the null sweep lists 16 fields but omits `dual_read`, `dual_chip_en`, `dual_chip_zh`.
- `engine/china_act_now.py:434-441` sets exactly those three on `reduce_avoid` rows, and `:416-418` documents that this is the designed theme path: *"theme reduce ids like 'cn_baijiu' match basket cycle ids like 'b-cn_baijiu' (FT-R1 dual-read fix)"*. `_display_lanes:253,270` gives `reduce_avoid` priority 0, so the projected card for such an id **is** the THEME row carrying `dual_read=True`.
- `templates/_china_act_now_board.html.j2:141-144` still appends the hover trust chip `⇔ may be bottoming` / `⇔ 洗盘转向`, and `:239-241` still renders the row chip whose `data-tip-en` reads *"the longer trend still says reduce, but the recent price action shows early signs of a bottom… it does not cancel the reduce signal."* Neither is gated on `_unavailable` (`:73`), unlike the tag (`:169-170`), the chip (`:230-231`), the read (`:76`) and the takeaway (`:78`), which all are.
- Reachability is the ordinary nightly path, not an edge: whenever theme status ≠ CURRENT (stale feed, weekend, pre-settle) **every** theme row is UNAVAILABLE — see `candidate-settling-dark-en-1440.png`, where all 13 Reduce/Avoid rows are degraded at once.
- Unproven in both gates: `tests/test_china_act_now.py:1338-1358` builds no bottoming duplicate, and `browser_proof.py:82-84` passes `[]` cycles for every scenario but `duplicate-evidence` (`proof.json:35,61` — `bottoming_watch` empty in baseline and candidate), so no capture and no test exercises it.
- *Smallest repair:* in the same block, add `"dual_chip_en", "dual_chip_zh"` to the tuple at `:775-777` and set `row["dual_read"] = False`.

**2. MINOR — `action_disagreement` chip survives the same sweep.** `engine/china_act_now.py:275-277` sets it on every projected row; `:775-777` does not clear it; `templates/_china_act_now_board.html.j2:235` renders "Conflicting actions"/"行动信号分歧" with no `_unavailable` gate. On a neutralized card the chip is orphaned — it names a disagreement between two actions the card no longer displays, both drawn from the session just declared unqualified. *Smallest repair:* `{% if not _unavailable and row.get('action_disagreement') %}` at `:235`, or clear the flag alongside finding 1.

**3. MINOR — untranslated English stat now PROVEN inside the ZH continuation card.** `hover-current-light-zh-390.png` renders, under 领先度判断, the line `20d +1.5% vs CSI 300 · 多周期共振向上` — one reason translated, one passed through in EN by `tr()` at `templates/_china_act_now_board.html.j2:158-161`. This is inherited (the reason line predates the change) and the artifact declares it truthfully (`proof.json:19`; `evidence.json:64`), which satisfies ATTACK (C). The finding is that predecessor 887 could only call this *unproven* and a *harness artifact* (`template-independent-review.md:20,43`); with the real translator now wired, it is proven **present**, on the one card this change creates, against the doctrine's ban on untranslated stats in user-facing copy. *Smallest repair:* emit `reason_zh` from the theme producer, or add the reason pattern to `engine/i18n` — no template or layout change.

**4. MINOR — a whole-board degrade is disclosed only to the log, while lane subtitles keep asserting direction.** `engine/china_act_now.py:710-711` emits `log.info("China theme action availability: %s")` and nothing else; the rendered `notes` channel (`templates/_china_act_now_board.html.j2:61-63`) receives no entry. `candidate-settling-dark-en-1440.png` and `candidate-settling-light-zh-390.png` show the consequence: 17 data-less rows, with "Trend weakening — consider trimming" / "趋势转弱 — 考虑减仓" still printed over 13 of them and "Fresh setups and continuing leaders" over an emptied Buy Now. Row-level neutralization is complete; the lane-level stance is not. (Predecessor filed this as a nit, `template-independent-review.md:21`; the settling scenario promotes it, because the state is now the entire board rather than one row.) Keeping the rows in the defensive lane is commission-mandated and correctly conservative, so the repair is disclosure, not re-lane-ing. *Smallest repair:* append one board-level note when `_theme_display_context` returns a non-CURRENT status.

**5. NIT — `.row-pop-mark--theme` keeps its bullish `--up` tint on a degraded card.** `templates/_decision_card.html.j2:85-88` colours the identity glyph with `var(--up)` regardless of tone, visible as the green ring in `hover-settling-dark-en-1440.png` and the red (ZH-inverted `--up`) ring in `hover-settling-light-zh-390.png`. Inherited identity chrome, flagged only because `templates/_china_act_now_board.html.j2:75` deliberately blanks the adjacent tag tone and stops one element short. No redesign implied.

GAPS:
- I executed nothing. `evidence.json:19-20` (324 passed / 0 failed, seven owner suites) and `minor-review-owner-tests.txt:6` are read, not reproduced; the four `source_sha256` values (`evidence.json:12-16`) and `proof.json`'s own hashes are read, not recomputed. Per FROZEN SOURCE I make no independent Git-identity claim — I reviewed the working-tree bytes at the named worktree and cannot assert they equal `5ccbf0ca…` or `06db2fed…`.
- Hover evidence covers 3 of 6 scenarios (`current`, `missing-members`, `settling`) per theme/lang; `source-conflict`, `final-demotions` and `duplicate-evidence` decision cards are adjudicated from board captures only.
- The reduce_avoid × bottoming overlap (finding 1) and the buy_now × reduce_avoid `action_disagreement` overlap (finding 2) are absent from both the test suite and all 48 board / 24 hover captures — they are argued from source and from `:416-418`'s own statement of intent, not from a rendered frame.
- No sector rows and (except `duplicate-evidence`) no cycle rows enter any scenario (`browser_proof.py:79-84`), so mixed sector/theme composition and a populated Bottoming Watch lane remain unshown in either theme — consistent with the commission's no-mixed/full-page scope.
- Whether `theme_intel["stale"]` is ever written upstream was not re-checked; that arm of the gate (`:703`) may still be vacuous (carried forward from `template-independent-review.md:45`, still unresolved).
- Release gates the artifact itself declares outstanding are not granted here: `evidence.json:9` `release_allowed:false`, `:74` `deployed:false`, `:105-110` current-head hosted CI, integrated current-base proof and authenticated production proof. `evidence.json:120` also records the current-candidate review as owed; this report is that review of source and design only.

DEVIATIONS:
- Read/Grep/Glob only. No edits, shell, tests, network, credentials, delegation, watcher, successor, or source takeover. No polling.
- I inspected six hover PNGs rather than the four named (adding `hover-settling-light-en-1440.png` and `hover-settling-dark-zh-390.png`) and two board PNGs (`candidate-settling-dark-en-1440.png`, `candidate-settling-light-zh-390.png`), solely to isolate whether the mark/ring colour split was theme-driven or the documented ZH convention swap, and to adjudicate the revised chip copy in both themes. No full-page, live, touch, or complete untranslated-source parity claim is made.
- Findings citing `templates/_decision_card.html.j2`, `templates/theme.css` and `engine/china_act_now.py` outside 689-825 are cited as the contract surface of the in-scope files, not as an audit of those files.
- I did not reopen accepted inherited chrome beyond the single nit above, and proposed no trading-policy change; every repair named is confined to the existing neutralization block or one template guard.
