# Swing proxy pilot v1 - pre-outcome contract

Parent WS:RATES-INFLATION-COMMAND; original operation rates-direction-20260924-sol-001 / PR 7909. Chairman's supplied-indicator comparison continues. Source at start f147b2da6c9fdf69f0ef722ef5b486f271bb5e2f. Procedure pin Mastermind 819abc8c23609cdded2b33f6e1bfc7854bd5c847, Skillpack1.0.1/bootstrap1; freshly retrieved procedure blobs match the previously loaded full instructions. Direct reason PRINCIPAL_JUDGMENT for the experiment and LOWER_TOTAL_OVERHEAD for this small research-only consumer. No worker or production change.

## Scope ruling

Native Pine parity and TVC:US10Y data remain mandatory for claiming exact chart replication. They need not block a separately named, non-promotable feasibility diagnostic using the existing uploaded_formula_probe.calculate reference and the already captured Yahoo ^TNX sample. This narrowly changes the earlier sequencing, not the scientific or production acceptance gates. No default settings, old results, indicator owner, collector, or prospective clock are changed. This pilot is NOT an equity strategy, exact chart replication, live forecast, or a source of trade authority.

## Fixed data and bar convention

Only the original tn x_raw capture (actual filename tnx_raw.json) SHA256 687b93d52beeb141dbfc3ea3eff787b67da7b36daa8effedc19568cbe544d070, captured 2026-09-24T12:51:03Z. Keep vendor history corrected-as-captured; its date labels do not certify historical availability. Units explicitly assumed Yahoo percent-yield convention, 100bp per source unit, consistent with current incumbent macro_catalog and captured quote range; not a claim about raw Cboe index scaling.

Use each day's provider regular tradingPeriod start/end. Accept only exact expected hourly start slots with finite genuine OHLC and valid extrema. Last source bar may be a scheduled shorter closing stub. Exclude off-grid snapshot records and not-yet-closed intervals; never infer closure from row count or last price. Group two hourly slots anchored at the provider session start into 120-minute bars; retain a scheduled closing stub as a separate shorter closed bar with explicit duration. Missing expected slots remain missing, never compress or synthesize. No overnight bars are invented. Provider sessions are not independently authenticated exchange calendars.

## Frozen targets and configurations

Research window = next 12 completed session-anchored bars, after one complete bar of decision/execution delay. Entry reference = close of that delay bar. This prevents using the signal bar's already-known high/low as future performance and avoids claiming a fill at the same close. Threshold frozen at signal origin = max(5bp, trailing24-bar standard deviation of close changes * sqrt(12)). Source unit multiplier100. The barrier does NOT rescale after origin.

Grade first OBSERVED-session upper/lower touch from underlying hourly extrema, not continuous 24-hour Treasury first passage. Both touched in the same hourly row => ambiguous; neither => no_hit. Missing required slots or unfinished full evaluation horizon => censored. Even an early hit is not scored until the whole fixed horizon has matured. Overnight unobserved excursions are outside this target. Intrabar path order is not fabricated.

Features: supplied-default research M line/signal crossover; P native strict previous-K extreme-zone crossover; R equality-inclusive K/D crossover (explicit proposed rule, not a native source alert). Keep each most recent direction for at most3 bars, only while current line/signal alignment agrees. Combinations M,P,R,MP,MR,PR,MPR are positive/negative only if all selected members have that same nonzero state, otherwise0. No parameter search, alternative trigger rescue or retrospective phase choice. Initial80 bars are warmup, not proof of native parity.

Nine registered configurations: unconditional class-frequency benchmark; trend/volatility benchmark; seven oscillator additions. Primary comparison fixed in advance = MPR versus trend/volatility benchmark on common eligible forecast origins. Other six are secondary. All nine remain in the existing TrialLedger family ric_swing_proxy_v1. Model = chronological class frequencies with Laplace1 unconditional prior; trend5-bar sign and high/low trailing24 volatility (median computed on eligible training history only), shrinkage12 observations toward unconditional; each oscillator state shrinks toward its matching trend/volatility baseline with the same12. No hyperparameters fitted or selected.

Training uses only origins whose fixed target-end is strictly before current origin; minimum40 fully matured unambiguous labels after warmup. No future labels, future volatility quantiles or random split. This entire June-September2026 sample is SEEN/developmental; no blind holdout claim. Point-in-time eligibility remains false. Primary score: sum-of-three-classes Brier (range0..2); also log loss, active-state coverage, common-date loss differences. Report descriptive HAC on date-averaged paired losses with lag3 via incumbent engine.validation; no inferential promotion. Count greedy non-overlapping active episodes separately, with origin after previous fixed target-end. Never call daily rows independent. Adequacy gate: >=50 non-overlapping active episodes PER candidate before any comparative ranking; this small pilot may fail it.

## Proof and stop boundaries

TDD synthetic tests precede market outcome grading: source clocks/stubs/missingness, future-prefix invariance, target exclusion of signal/delay bars, hourly ambiguous order, purged training, common origins, probability normalization, formula identity, repeat-free trial registration and frozen-byte refusal. Freeze source/spec/tests before registration and first outcome read. Persist all candidate outputs and nulls, no best-only report. Raw vendor payload stays private; publish code, aggregate evidence and hashes. Real forward evaluation, native Pine golden values, independent review, longer source coverage, RIC UI/machine integration and equity transmission remain separate obligations.
