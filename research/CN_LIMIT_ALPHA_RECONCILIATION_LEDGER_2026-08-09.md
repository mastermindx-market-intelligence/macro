# CN LIMIT ALPHA — Claude×Codex reconciliation ledger (2026-08-09)

**Authority:** operator order 2026-08-09 (in-session): Codex work on this project is ENDED;
the Claude lane integrates the Codex handoff into its own findings and data and is now the
**master session for everything limit-move**. Source: `/Users/chriswong/Downloads/
CN_LIMIT_ALPHA_CODEX_TO_CLAUDE_HANDOFF_2026-08-09.md` (read in full; its §11 reconciliation
rules are adopted verbatim as house law and applied below). The §9 dual-lane blindness is
DISSOLVED — this ledger is the reconciliation event the charter reserved for the operator.

## §1 Preservation receipt (perishables secured first)

- Dirty range-shard campaign preserved per handoff §5.1: wip commit `8143a003550` on
  `codex/cn-tushare-range-shards` (5 files, +1,936/−176), pushed to origin. NOT rebased —
  the finishing lane owns that, with the handoff's four known blockers.
- Four clean local-only Codex branches pushed to origin unchanged:
  `claude/cn-limit-sol-w1-data-integrity` (32a51fac8e0), `claude/cn-limit-sol-w1-continuation`
  (1250c2ac615), `claude/cn-limit-sol-w1-onset` (df064087702),
  `claude/cn-limit-alpha-forward-ledger` (93149e56609).
- Integrity check: the preserved 71,692-event tape hashes exactly to the handoff's attested
  SHA-256 (`a9785b95…484314`). The preserved branches are the artifacts the handoff describes.

## §2 Construction-by-construction reconciliation (handoff §11 rules applied)

**CONVERGENT KILLS — the strongest evidence either stack has produced.** Two fully
independent stacks (different code, different healed tapes, different cost models, different
accounting: Codex inclusive cash-book at 60 bp; Claude fillability-censored locked-exit books
at 15 bp) reached the same signs on the same families:

| Family | Codex verdict | Claude verdict | Status |
|---|---|---|---|
| Unconditioned daily continuation riders | KILL — locked/replay N=1/2 seal-exit −0.7491%/event, dc-CI [−1.10,−0.39]; self-financing NAV 0.2975 | KILL — E1/E3 lose everywhere, anti-monotone in P (W1 §6.1) | **CONVERGENT** |
| Daily onset books (model-ranked) | NO-GO — O1/fixed-rank/O3 negative; vendor-rich audit WORSE than replay (−1.115% O1) | KILL — all 52 positive paper cells flip negative implementable (W3-C §6.4) | **CONVERGENT** |
| The auction prices public information | F1 confirmed (their vendor-rich degradation) | F1 confirmed + mechanism measured (access rationing, §6.4) | **CONVERGENT** |

Per §11.1 these are sign-level convergences across non-identical constructions — the kills
reinforce; the numbers are NOT poolable.

**CLAUDE-ONLY territory (Codex never tested — my adjudications stand, now with an explicit
universe caveat, §4):** the regime axis (W3-B), weakness entries 回封/龙回头 (W2-B), window
targets + foresight premium (W3-A), fillability mechanism curves (W3-B/C), forward-ledger
calibration (L3).

**CODEX-ONLY territory (theirs stands, now mine to run):** the exact-cent band-progress
construction packet (merged, BLOCKED_SUBSTRATE — runnable once the exact plane lands); the
range-shard whole-market backfill campaign (preserved, 4 blockers); the
measurement-eligibility overlay (design frozen in handoff §5.2); the universe/procurement
audit (§4 below); the legacy-tape integrity rebuild (§3 below).

## §3 The two healed tapes — adjudication

Both stacks healed the same 60,428-event legacy store, differently: mine (#5059, armed) →
**71,463** events; Codex (preserved branch) → **71,692** events (+11,432/−168 keys vs
legacy; also repairs my heal did not attempt: registration-era IPO no-limit correction,
positive-volume, listing-session, contiguous-session-streak, 285 restored pre-2020 ChiNext
events). Codex's own authority warning applies to BOTH: the upstream plane is split-adjusted
Yahoo with Python-round detection — **neither tape is exact legal-limit truth.**

Ruling: (a) #5059 stays the armed display-tier working tape — every W1–W3 receipt stamps it
and stays internally consistent; (b) the Codex tape's ADDITIONAL repair classes become the
SPEC for the exact-plane event rebuild (their §7 integer-cent taxonomy + stk_limit joins);
(c) no analysis may mix the two tapes (§11.1 — vintage is part of construction identity);
(d) final event authority = the TuShare exact-cent plane, post-backfill. Exploratory delta
work (which repair classes move which W1–W3 numbers) is sanctioned as display-tier ore.

**AMENDED 2026-08-10 — the invalidation is ENFORCED (STOP-SHIP compliance).** The later
`research/CN_LIMIT_ALPHA_SOL_ADJUSTED_PRICE_STOP_SHIP_2026-08-09.md` withdrew the
adjusted-plane rebuild artifacts in BOTH vintages. #5059 was deliberately pared before
merge (squash `dbf7454f314` landed the independent zt_pool heal only), but #5198 then
mistakenly treated four content-empty safety merges as permission to re-land 14 withdrawn
artifacts. Byte-exact transport did not cure the invalid substrate. This reconciliation
removes those 14 paths, their direct W2/W3 executable/result descendants, and the #5205
forward-ledger page from the current tree; Git history retains them for archaeology only.
The two continuation handoffs are tombstoned and the historical masterplan is visibly
superseded. Ruling (a) is VOID — there is no healed working tape. Ruling (b) survives only
as qualitative design input for a fresh exact-plane preregistration; (c)/(d) stand. No
withdrawn tape, receipt, model, result, forward seed, reproducer, reader, or rendered surface
may be restored or used as numerical evidence. Registry row:
`DNR:KILL-CN-ADJUSTED-TAPE-LEGAL-LIMIT`.

## §4 Universe truth — F3 upgraded from threat to measured fact

Codex quantified what my receipts only stamped: the 1,842-name store covers **35.37%** of
active SH/SZ names (0 of 329 BSE), median cached cap 187.7亿 vs 37.85亿 omitted, and only
**36.09%** of canonicalized zt-pool names. Every Claude probability, ladder, dial, and
fillability number is a LARGE-CAP-SLICE statistic. This does not retract the structure —
it bounds its denominator. F3 re-measurement on the full-A plane is therefore not one item
among many: it is the gate between everything measured so far and any promotion beyond
display tier. Their small-cap note is adopted as stated: a sampling-gap prior, NOT proven
alpha; PIT-cap strata by board, never current-cap.

## §5 Exact-limit taxonomy — future exact-plane work only

The handoff §7 integer-cent taxonomy (sealed `close_cents == up_limit_cents`; exact touch;
炸板; near-limit progress; quarantine classes) remains design input for a fresh exact-plane
construction, together with its entry/exit clock discipline. The earlier tolerant-grade
carve-out is VOID: its adjusted-price forward ledger was withdrawn and is removed from the
current tree, so it must not be graded now or beside a future exact-cent ledger. Any future
ledger requires a fresh preregistration bound to authorized TuShare `daily` plus same-key
`stk_limit`, the complete PIT universe, exchange half-up validation, and a new provenance
receipt. Historical W1–W3 grades are archaeology, not authority.

## §6 Two forward ledgers, one Monday

Both stacks historically seeded predictions for entry session **2026-08-10**: Claude 100
rows (`onset_v1`) and Codex 5,352 rows. **AMENDED 2026-08-10 (STOP-SHIP compliance): grade
NEITHER ledger.** Both depend on the withdrawn adjusted-price construction. #5198's
mistaken re-land of the Claude model and 2,100-row ledger is removed here; the Codex seed
remains history-only. Neither may be advanced, reconciled, copied into a new ledger, or
written into the program record. The preserved Codex engine may inform requirements such
as stable identity, keep-first contradiction detection, and exact-session accounting, but
it is not adopted executable authority. A future advancer must be implemented and
pre-registered against the authorized exact plane, with no imported rows or grades from
either withdrawn seed.

## §7 Protocol adoptions

- Handoff §11 reconciliation rules: adopted verbatim, permanent.
- Vendor-rich audit window (2026-06-15→2026-08-07) joins the evaluation battery for every
  future construction (their O-family replay-vs-audit gap — audit ALWAYS worse — is the
  strongest known argument that replay-only evaluation flatters).
- Their frozen split governs their constructions; mine governs mine; re-measurements on the
  exact plane use their §8 split going forward (train 2011–2019, calib 2020–2023, locked
  test 2024-01→2026-06-12, audit, prospective from 2026-08-10, 10-session purges).
- Their §10 ore list is MERGED into the program ore ledger (union, deduped); their §2
  instruction stands: kills close constructions, never the space.

## §8 Re-ranked master queue (merges handoff §12 with masterplan §8)

1. **Finish range shards** (Opus lane; handoff §5.1 recipe + its four named blockers;
   rebase AFTER the wip preservation, which is done).
2. **Full-A spine backfill** — flip `BULK_HISTORICAL_BACKFILL_READY` under the
   operator-attested authority (align the spine's empty allowlist to the attestation gate
   mode from the wiring lanes), then run daily/daily_basic/stk_limit whole-market history
   with completeness receipts. Gated on: range shards finished + local token refreshed.
3. **Eligibility overlay** (build from handoff §5.2 frozen design; its listed factual gaps
   are the overlay's quarantine classes, not reasons to wait).
4. **F3 re-measurement on the exact plane** — the W1–W3 core (ladder, gap, dial,
   fillability, weakness, windows) re-run full-universe, integer-cent, eligibility-gated.
   THE program gate to anything beyond display tier.
5. **Minute/auction/chip planes** — wiring lanes A/B/C (running; Lane C shipped #5162).
6. **Fresh exact-plane ledger + advancer** (§6 above), only after the substrate and a new
   preregistration exist; never grade or import either withdrawn ledger.
7. **Band-progress packet run** on the exact substrate (their merged, frozen construction).
8. Then per masterplan §8: intraday battery against the foresight premium, C11/C15,
   v0-corrections (post-#4999), theme-relay (blocked on THS).

## §9 Operational notes

- **Token:** the Actions secret is ALIVE (their §6: refreshed token exercised paid planes in
  a GitHub run; confirmed independently — china_a_val advanced in the 2026-08-09 asia
  commit). The LOCAL `.env` token is DEAD (Lane C: 40101 on premium AND regular endpoints).
  Local probes/backfills wait on an operator refresh of `.env`; nightly lanes are unaffected.
- The Codex handoff's §9 license statement matches the operator's in-repo attestation
  (foundation doc, SHA ad48d044…); the license question is closed everywhere.
- One Codex protocol debt is recorded as satisfied on the Claude side: the blinded
  brainstorm they never produced exists here (masterplan §5, merged) — under single-master
  operation the requirement is met by the surviving lane's artifact.
