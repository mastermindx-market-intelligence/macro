# Independent T2 and section-7 formation clocks

## Capability and bounded contract

This candidate continues the Chairman's US Prophet availability recovery under the registered engine owner, WS:PROPHET-US-ENTRY-TIMING. It repairs the demonstrated cross-event ordering assumption, not ranking, eligibility, geometry, confirmation, identity or the market calendar. Protected procedure: Mastermind b14982837cc8146e3dc49e5862558ee399a1aa3d. The implementation contract was bounded in #7180 comment 5711161027, following causal proofs 5710341615 and 5710423319.

T2 is an independent fired 2D-cross conjunction. The later section-7 hold marker supplies the immutable plan's formation/ID anchor. For EBAY, PG and CPRI, the real T2 event is September 11, formation/marker bucket September 14, and marker knowability plus current observation/price basis September 16. Their two events cannot share an unconditional formation<=event rule. The correction reader repeated that rule, so repairing origination alone was insufficient.

## Optional closed source evidence

Only NEW reversed T2/section-7 records receive `temporal_lineage`. Already ordered and legacy records keep identical output. The schema is `prophet.t2_section7_lineage/v1`, with exactly: schema, asset, hold_anchor, hold_anchor_src, hold_provisional, marker_type, marker_date, marker_known_date, tier_event_date, observed_date, price_basis_date.

The producer derives those values from the real source candidate; an input envelope is ignored. This is structural evidence inside an immutable plan, not a signature, credential, caller-granted exception or second authenticity plane. Existing full source/plan publication receipts remain the source authority.

The one relation lives in the existing integrity module:

`event < hold_anchor = marker_date <= marker_known <= observed = price_basis <= recorded_at`

The source must be an actual nonprovisional T2 observation and nonprovisional hold, hold source take, buy/rebuy marker, and canonical NYSE date labels. Asset and all projected clocks must match the evidence, with T2 confirmation null. Recorded/publication date may be later than the market date. Origination, plan and ledger readers, permitted corrections, and normal/degraded index projections use the same validator.

Lineage is intentionally not in either correction allowlist. Old records cannot gain it through annotations; a correction that contradicts a bound clock fails closed. Unknown, missing, null or malformed lineage grants no exception. No plan ID, old publication, source date, ledger event or correction permission is rewritten. T1 and projected tiers keep their original rules.

## Observed verification

The first fixture run also encountered the pre-existing #7180 completed-session bug. To isolate THIS date relation, the fixture was bound to the completed-session run date. Then 22 intended failures were observed before implementation. Six raw-ledger reader failures were observed before that consumer was wired.

The final source suite passed 288 tests across bridge, integrity, Arena, Arena-clock parity and chronology-audit owners. Three forbidden mutants were rejected: skipping raw evidence checks, removing marker-order checks, and dropping forward-ledger lineage. The new tests live inside their existing registered suites. All 11 affected curated CI owners cover their actual inferred dependency closures; no new CI owner was created.

The combined recovery source passed 395 tests across seven suites, with zero changed Python source files among 5,851 checked, zero provider calls and no original data writes. The geometry and chronology tests initially conflicted only at their shared appended tail. Moving only this candidate's test block fixed composition; the peer's geometry code and tests were not edited.

## Full saved-board and correction path

The actual-board receipt compares all 42 preserved September-16 buy rows, with the SAME empty existing-ID and active-plan sets. Baseline already includes protective geometry. Baseline returned 20 objects; candidate returned 23, adding exactly EBAY/PG/CPRI-BULL-20260914. All 20 common plan dictionaries are unchanged. All 26 admitted survivors remain accounted for, zero unaccounted, with HON/RBA/TRN still explicitly refused for wrong-side protection.

All 23 returned plans pass the real later-correction consumer with an unrelated private annotation. The new records keep event September 11, formation/ID September 14, and observation/price September 16; no recency was manufactured. These controlled counts are NOT 23 new production recommendations, fills or trades.

The comparison runs the exact prior bridge in the same dependency context as the candidate, preventing machine-path differences from being mistaken for semantic changes. The large preserved price/board snapshot was read only. No provider refetch, whole-library rebuild, production write, workflow dispatch or sibling branch edit occurred.

## Exact source and reproduction

Source base: e4a154c6390de8ac6fb88b586e9fd399dba557c6.
Semantic code: dd667ed230d58f41b4bffd057b1ca1b678ef862f.
Own test placement: 3c381e96875ab76944a31deaeb0013962c5d90f0; runtime source is unchanged.

Run: `python -m pytest -q tests/test_prophet_bridge.py tests/test_prophet_integrity.py tests/test_prophet_arena.py tests/test_prophet_arena_clock_parity.py tests/test_prophet_plan_chronology_audit.py`.

Exact source trees, source hashes, actual clocks and comparisons are in the adjacent JSON receipts. Original private drivers and receipts are on Studio under `~/.cache/mastermind-proof/prophet-independent-t2-clocks-20260917-sol-001/`, including `actual_clock_comparison.py` and `run_integrated_tests.py`.

## Release boundary

BUILT_NOT_PROVEN / DRAFT / HOLD-FOR-SOL. This branch does not adopt or merge #7180, #7254, the archive, or TURN WATCH protocol repairs. Conclude exact-head CI/security and legitimate independent review, reconcile latest-base source intersections, and have one acknowledged integration owner prove the real new-session candidate/plan/history and authorized served/browser journey. Do not self-approve independent review, fabricate a new date for freshness, or dispatch another rescue because these isolated tests passed.
