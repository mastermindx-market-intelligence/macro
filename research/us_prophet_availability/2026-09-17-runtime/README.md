# Same full TURN WATCH output, lower runtime

## Capability and boundary

The source repair removes redundant computation from the existing TURN WATCH/confluence/leader engine path. On the preserved real-input universe the exact final engine source completed in **556.560 seconds** (513.936 CPU seconds), versus the earlier matched-artifact computation of **1,047.25 seconds**: about **46.9% less wall time**. The 600-second builder comparison is satisfied in this observation. The separate 480-second early-warning threshold still fires and is not changed or declared satisfied.

This is a full-universe, exact-output, isolated runtime proof—not a production deployment, a fresh provider collection, a blanket nightly runtime guarantee, or proof of market edge. The earlier 1,047.25-second baseline is the immutable public artifact from the preceding real-input phase, not a newly repeated full baseline in this turn. System load differed across observations; future wall time must be measured on the real deployment path.

## What stayed identical

The computation used all **6,102** names with **no universe limit**; **5,635** met the existing history floor, **2,901** triggered, and the unchanged public display cap retained 40 while 2,861 remained in the uncapped intake. Every public field except the truthful runtime measurement and every complete uncapped observation row compared exactly equal to the preserved baseline. This includes indicator values, triggers, rankings, null states, source/session dates, explanations and order. All 6,169 input hashes (price archive plus basket inputs) remained unchanged, as did the original public/private files and source code throughout the timed run.

Final engine source: `788462d59e60f6ec311b46c0ff23a45f0c186793`. Later commits change CI ownership/evidence only; do not relabel the measured source. Exact source/data/output digests, coverage and stage timings are in `full-universe-runtime.json`. The full input manifest and unchanged original prices stay in the private proof tree; no market corpus is copied into this PR. The runtime-only artifact hash changes by design; semantic artifact and complete observation comparisons are separately recorded.

## Three equivalent optimizations

1. The existing completed-bar helper uses native timestamp aggregation instead of constructing and dropping nulls from a Python Series for every historical calendar bucket. Value resampling, observation timestamps, dtype/index behavior, incomplete-tail exclusion and the absolute-fortnight implementation are preserved. Empty/all-null paths preserve the old inferred datetime unit.
2. The bulk TURN WATCH evaluator computes all triggers for every graded name and preserves the complete relative-strength cross-section. It skips explanatory details only after a name has no triggered leg and the existing consumer would immediately discard its row. Standalone evaluation is complete by default; every triggered private observation remains complete even when outside the public display cap.
3. The existing leader state machine computes six finite-value masks once per input array instead of repeatedly checking them as scalar values on every warm historical row. The original cold-leg diagnostic, state transitions, thresholds, episode timing and output schema are retained.

No new queue, worker pool, cache, source, calendar, state machine, identity registry or trade authority is added. No ranking, sizing, admission or universe rule is changed.

## Discriminating verification

Before implementation, the calendar callback tests failed as intended while exact reference-value cases passed. The repaired helper has exact-reference coverage across clean/missing/all-null/empty/unsorted/duplicate/partial-tail inputs and three timezone treatments. An additional fixed-seed 160-case comparison covers five calendar rules and four timestamp units.

Actual bulk tests fail if discarded explanatory work returns, preserve all cross-sectional participants, preserve basket-only triggers and uncapped output, and retain complete standalone untriggered evaluation. A forbidden early-return removal was detected in an isolated copy. The warm-check performance regression failed on the preimage; six missing-leg controls and 48 full-state-frame comparisons preserve exact state, numeric, null and diagnostic behavior. Reintroducing the old scalar loop is detected.

The final direct five-suite owner battery passed **410 tests**. Earlier full-universe candidates took 889.146 and 629.373 seconds; their outputs were also identical, but they did NOT meet the 600-second comparison. These stages are retained in `measured-stages.json`, not silently promoted to success.

A broader integration run discovered two separate issues: our duplicate registration of the existing TURN WATCH test owner, and an omitted `site/theme.js` in the private snapshot. The source correction removes only the duplicate test registration, preserving the sole existing `washout-turn-organ` owner. The snapshot repair materializes the exact already-committed asset rather than changing templates/tests. The two affected suites then passed 38 tests; the complete corrected rerun passed **996 tests across 24 suites**, with zero skips or source mutations and 38 existing pandas deprecation warnings. `integrated-tests.json` binds the exact tree, suites and observed output. No check is waived.

## Coexistence and release

The exact seven-source composition retains the existing #7180, #7187, #7200, #7206, #7227 and #7235 implementations and adds this runtime branch without manual conflict resolution. Exact heads/base/tree are in `source-composition.json`. This is compatibility evidence only, not a merge or approval of any peer's remaining defect. #7206's known Boolean-price gap, #7187's source-copy repair, other pending review/CI and the explicit prospective v2 adoption boundary remain separate.

At 03:08 UTC September 17, #7200's twelve trusted packs and selected CI/security gates were all SUCCESS, but zero independent reviews had been submitted. A material update was sent on the existing placement thread (`C0BSBM78V1N/1789587618.242409`, update `1789614555.307319`); no new reviewer, ACK, START or runtime Job was invented.

This runtime source must remain draft/HOLD pending concluded exact-head CI and independent review. Source authors cannot supply their own independent review. A single acknowledged integration/publication owner must then adopt the accepted sources, verify current data and the prospective input-protocol transition, publish canonically, and check the actual candidate/plan/premium/browser journey. The 480-second warning and production-host timing remain visible requirements.

## Reproduction and continuation

Original private proof root on the Studio: `~/.cache/mastermind-proof/turn-watch-runtime-20260917T0220/`. `budget-runtime-proof.py` binds the exact source and preserved input tree and compares all actual public/private observations. Its wrappers only accumulate stage timings/counters; returned values are unmodified. `budget-result.json` and `budget-exit.json` record completion. The broad integration uses the exact immutable composition snapshot, not an active peer worktree.

Procedure pin: Mastermind `eec5324c5205e8bad206512e0a936898e50b2408`, compatible Skillpack 1.0.1. Capability remains **BUILT_NOT_PROVEN** until release and actual served-path acceptance. This phase owes no provider call, workflow retry, production data mutation, or source-custody transfer.
