# Returned split-repair coherence correction

Operation remains `prophet-us-current-close-collector-repair-20260916-sol-001`, same PR #7200 / `claude/prophet-us-current-close-collector-20260916`. Current Chairman continuation and protected Mastermind `aacf3df5a47ca37ce71cd47a3bd7caea81ad4cd2` govern this source correction. Original direct source custody was reconciled against matching local/remote head, clean files, no Git lock, no active cwd writer, and the committed direct-operation handoff. No delegated worker was reassigned or newly STARTed.

## Before / after

At preimage `f6d1b8632aa353c213b1f39cda63a38d30949e79`, the initial download validated completed closes but the split-repair re-download did not. A historical-only Close could replace a name while populated current companions survived. A corrected half-basis Close could coexist with the initial old-basis High when the replacement omitted that field. Review 5233152327 and native confirmation 5711674324 establish those six real-adapter synthetic failures.

Semantic correction `196e0731b30d6b8c303302eff98954e32079856c` passes the SAME captured completed-session reference through the existing split-repair call. For the three US S&P collectors, unavailable required repair prices or a replacement companion that would fall back to an older basis now fails BEFORE cache writes. It does not erase old history, invent companion values, or select another provider. The existing health consumer reports failure with all prior cache bytes intact. The normal 80% floor still accepts four of five current names; a valid partial split replacement keeps absent-current companions missing. Final selected companions are checked against the final Close selection before persistence. Newly returned companion fields are retained rather than discarded merely because the initial response lacked that field.

The legacy/non-US/Russell call shape and failed-repull behavior stay unchanged. The new keyword is optional and passed only for the already guarded US fetches. Four existing test fakes accept that explicit context; their original assertions remain. The dated source fixture also freezes its cache-age clock, so the test does not silently stop exercising the seam path two weeks later. No calendar, score, rank, universe, retry budget, workflow, authority, production store or plan history is changed.

## Exact verification

The new selected regression set was **16 failed / 3 passed** before implementation. The complete four-suite native battery is now **105 passed**. It includes the six original failures, whole Low/Volume omissions, a missing replacement cell, valid coherent replacements, partial-universe acceptance, under-floor refusal, new companion propagation, final selection coherence, and the real health consumer. The actual seam detector and production fetch/download/merge/store methods run unchanged; only provider data, constituent references and test clocks are fixtures.

Three forbidden mutations are caught: dropping the captured-session argument, skipping the replacement-companion check, and omitting final companion resealing. Restoring the candidate returns the native suite green. No skipped tests in this battery. `mutations.json` preserves each exact result.

The existing geometry/independent-clock recovery tree combines without manual conflict with this semantic head. On the resulting immutable tree `9446623a23ead3a3b7597a06c96ab66c7562a998`, **467 tests across ten suites passed**, and all 6,447 checked Python source files remained unchanged. This is source-consumer integration proof, not hosted CI or production acceptance. Current-main composition is a separate receipt in `composition.json`; it does not advance any branch.

## Preserved real-input replay, not a new collection

The real native collectors were rerun with the previous pinned cache seed and the saved provider-output matrices from source `2ecf3db94d131cf8c59fe685ce7d1aca2e3509c2`. Network was blocked; the historical experiment's calendar and delay were fixed locally. The real detector selected split-repair names in all three groups. All three calls succeeded and preserved the same **500 / 599 / 400** completed-price counts. Every requested current Close/High/Low/Volume value and null position matches the selected recorded response. The 18 original proof files remain hash-identical. These processed matrices are NOT raw vendor responses and this is NOT fresh-market or live-provider acceptance. The original earlier provider receipt is unchanged and has not been re-stamped as proof of this new head.

## Release boundary

BUILT_NOT_PROVEN. This resolves the returned source behavior but changes semantics, so the new immutable head needs independent full-PR review and current-head CI/security; old green checks do not transfer. The original unconsumed review-placement request remains the only review-placement operation. The source author cannot provide its independent approval. Current peer #7206 and all other branches keep their source owners. No workflow was rerun, no deployment triggered, and no production data or user account changed. The parent remains incomplete until one reviewed integrated producer-to-plan/history/publication/browser path is proven.
