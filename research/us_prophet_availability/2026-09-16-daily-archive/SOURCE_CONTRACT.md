# Daily-current Yahoo archive: same writer, whole-scope attempt

Mission: remove the six-day refresh-selection bottleneck from the existing Yahoo archive consumed by TURN WATCH, without substituting price basis or another source/publication/identity plane. This continues WS:PROPHET-US-AVAILABILITY under the current Chairman direction. Mastermind procedure pin: `4537f066775c73d305f82acf0643701f01f5e53c`. Base: `bd93ee84583e6c6bea2b342bbf38cba0b9eb23e9`.

The existing daily producer is `scripts/backfill_yahoo_universe.py`, invoked once by daily.collect. The default six-day behavior remains unchanged for ordinary callers. Explicit `--refresh-all` selects every eligible unmaintained archive name, keeps the existing backfill priority, and lets refresh use any remaining budget even when the backfill queue exactly filled its cap. `--refresh-cap` and the explicit all-scope flag cannot be combined ambiguously. Collector-maintained and parked names keep their original exclusions. Unresolved ownership does not become an empty exclusion set.

The daily consumer selects this mode with batch size 160. Backfill cap stays 400; the shared wall-clock budget stays 480 seconds; the same full-history extraction, validation, price adjustment, source writer, state file, failure/parking accounting, grace policy and schedule remain unchanged. The guard checks budget before a new batch; an individual in-flight provider request is not made hard-cancellable by this change.

This deliberately increases the intended per-night symbol coverage from the periodic slice to the whole eligible archive. It is NOT a claim of unchanged vendor request volume, a guarantee of latency on every future night, or proof all names will return current prices. `refresh_scope=all_unmaintained` is an intent disclosure; existing attempted/stored/refused/budget fields retain their literal meanings. Missing or refused names are not deleted, fabricated, classified as dead, or counted as successful current coverage.

A full-scale private experiment exercised the unchanged existing writer at this base with equivalent explicit parameters (refresh_cap 6000 selects all 5348 eligible names): 425.48 seconds inside its 480-second budget; 5348 attempted, 5224 stored/refreshed, 4652 with a finite positive Sep-16 close and 696 still not current. No configured-collector files changed; row counts did not shrink. This proves feasibility on this observed host/run, not the candidate's deployment. Exact raw machine receipt remains in `~/.cache/mastermind-proof/yahoo-daily-full-budget-20260916T2330/`. No production files or workflow run were changed by the experiment.

Eight new producer/planner regressions and the actual daily-consumer regression were observed failing before repair. Three forbidden variants (restored backlog starvation, restored periodic selection, bypassed shared budget) were caught. Restored source: 94 tests passed, one existing skip. The new code-gate CI owner runs the existing archive suite; all previous jobs remain unchanged.

The real full TURN WATCH and nonwriting B1 consumer proof is being performed only in a private data tree with the explicitly staged #7227 v2 source. Default registry and production protocol remain v1. Do not merge/activate this daily-consumer change independently of coherent session data, accepted prospective protocol adoption, current-base integration, independently reviewed source and concluded checks. #7200, #7206, #7187, #7180 and #7227 retain their separate source owners and acceptance boundaries.

Direct execution reason: CRITICAL_PATH_SHORTCUT. This bounded extension to the existing selector/consumer is smaller than dispatch and review overhead while the known capacity route is blocked; no runtime worker or new control service was originated. Capability is BUILT_NOT_PROVEN. Production acceptance requires actual coherent current data -> TURN WATCH -> candidate-history replay -> canonical publication -> authorized served-payload/browser proof.

## Attempt receipt is not storage freshness

The all-scope mode captures the existing NYSE completed-session reference once and adds outcome counters to its existing last-run report: planned, actually requested, unattempted, current accepted responses, and accepted-but-noncurrent responses. `refreshed` still means persisted, not newly current. The new current counter inspects the accepted response’s exact completed row before any retained same-date history can be mistaken for a new observation. A transport failure counts as an attempted request but not as returned current data. Default-mode report shape and data acceptance are unchanged; these disclosures add no new source-admission policy or clock authority. The stored-old-frame, unattempted-budget and transport-refusal regressions were RED before their implementation.

The full private actual-source TURN WATCH run reached September 16 without a universe cap: 6102 names, 5635 graded, 2901 triggered observations, existing 40-card display cap. Its 1047.53-second runtime exceeded the builder’s existing 600-second warning budget; this is a visible remaining runtime issue, not a passed latency contract. Old generation/input files were preserved. Its exact v2 source is the staged #7227 head, not production protocol adoption.
