# Actual daily archive → TURN WATCH → candidate-history proof

This is an isolated real-provider/real-consumer result, not a deployment. The source-data seed is Macro `bd93ee84583e6c6bea2b342bbf38cba0b9eb23e9`. The staged archive selector/receipt source is `21ddf66138239dc1b1e927aa743a0ca6911b640e`. The explicit, private TURN WATCH v2 source is #7227 `0e57dc01e494475802c62fc664924236544abb90`. No production registry or workflow was activated.

## Observed capability

The unchanged canonical full-history archive writer attempted all **5,348 eligible unmaintained names in 425.48 seconds**, inside its existing 480-second budget, using its existing explicit cap and batch parameters. It stored 5,224 responses; **4,652 had a valid September 16 close**, with 696 not current. It changed no configured-collector files. A separate pass through the actual regular Yahoo collector returned 742 symbols, 740 with a valid completed close, without modifying the archive-owned files. The two ownership scopes coexisted rather than being silently merged.

Comparing the exact original archive bytes against the resulting stores checked **6,167 files** with **no lost historical dates/files or invalid indices**. This proves retained dates and schema/index behavior, not independent certification of every provider corporate-action adjustment. Existing provider refusals and 19 ratio warnings remain disclosed.

The actual full TURN WATCH builder then processed **6,102 names without a universe limit**, graded 5,635, emitted 2,901 triggered observations and retained the existing 40-card display cap. Its `data_session` and maximum session both reached **2026-09-16**. That is not a claim that every archived security had a current close or that all triggered observations are tradable. The builder took **1,047.53 seconds**, exceeding its existing 600-second warning budget; latency is not accepted here.

The full unchanged B1 history reconciler, with the staged v2 intake and real new sidecar, succeeded in **nonwriting replay mode**. It computed **298 additional observation events** (233 TURN WATCH, 64 candidate, 1 door), with lossless suppression accounting, no changed inputs and no history writes. The selected preexisting 3,840-event generation was preserved. These are observation events—not new plans, picks, transactions, or production writes.

## Real-data harmless-retry proof

A separate private generation fixture reused those actual observations with the same explicit fixture-only admission pattern as the committed v2 tests. It did not change any live environment/registry or the original input snapshot. The first fixture write added 298 events. Changing only the artifact runtime and replaying added **zero** further events, retained exact event/projection hashes, and retained changed source-file provenance in a new receipt-bearing generation. All prior generation files survived. Changing the selection definition was refused by the ordinary-source guard and left the selected generation intact. See `actual-data-idempotency.json` for the precise nonproduction boundary.

## Why this candidate is small

`--refresh-all` only changes which already-eligible names the existing refresh phase attempts. The daily invocation uses batch160 with the original cap400 and budget480; full-history retrieval, dual price basis, canonical store writer, provenance, parking, time-budget ownership, and schedules are retained. The original six-day mode stays the default for other callers. The all-scope report distinguishes attempted, unattempted, current returned and noncurrent returned observations; retained old data cannot turn a stale response into a current one.

More symbols per night does mean more intended vendor work. The measured run demonstrates feasibility, not a promise of identical future latency or unchanged request volume. The soft deadline remains checked before each new batch; an already-started network request is not made hard-cancellable by this change. The full routine job and TURN WATCH runtime still need production-path validation.

## Source verification and integration

The final six-source composition is private commit `647fbf431bd17da54edf0ba5952daa5ef53c787e`, tree `3ed68621d2c69f0e364044efcc1d08be249f1848`, against base `c43c2fc11582af9fe13c2455ae1716f5b7be1762`. It combines #7180/#7187/#7200/#7206/#7227 and this archive candidate without manual conflict resolution, retaining all four relevant code-test owners. **582 tests passed across 17 suites**, no skips and no source changes. Existing #7206 Boolean-validation and peer release findings remain blockers; green integration does not waive them.

Producer/consumer tests were observed RED before implementation. Three deliberately broken selection/budget variants were rejected. Current-return tests separately reject falsely counting retained same-date prices as fresh observations and classify transport failures correctly. The existing historical SPY schema test was rerun with its exact integrated input rather than silently accepted as skipped.

## Reproduce / continuation

The private original experiment and full-input scripts remain at Studio `~/.cache/mastermind-proof/yahoo-daily-full-budget-20260916T2330/`: `run_experiment.py`, `run_regular_producer.py`, `run_turn_watch.py`, `run_b1_real_inputs.py`, and `run_actual_input_idempotency.py`. The original archive and raw provider output stores remain there, not in this PR. This packet contains explicit source/output identities, summary receipts and failure boundaries, not redistributed market corpora.

Read the exact receipts beside this file. The original real provider experiment used the unchanged writer with equivalent all-scope CLI parameters; do not relabel it as a new live run of the staged selector. The source tests and integrated tree bind the selector itself. Default production schema remains v1. Release requires independent review, all concluded checks, an accepted genuinely new-session v2 cutover, coherent source data, one reconciled publication owner, and authorized actual candidate/plan/premium-browser proof.
