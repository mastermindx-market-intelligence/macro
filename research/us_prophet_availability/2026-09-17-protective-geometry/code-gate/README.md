# Mandatory PR execution of the actual protection/refusal regressions

At exact prior head `0a105d9a516ece178841aa20924fda581339e8a2`, the bridge and why-not suites were directly registered only under data-gated jobs. The local mathematical and receipt proofs were real, but a normal code-only pull-request plan did not directly execute these newly added cases.

This amendment adds one run step to the existing **washout-turn-organ / gate:code** job. It selects the source-owned protective-geometry cases, their existing waiting-reset positive controls, and the current unbuilt-plan copy regressions. The exact command passes **63 tests**. All old data-gated commands remain. Parsed YAML equality verifies that no other step, job, dependency, timeout, gate, runner or scheduler changed. No production code or test assertion changed in this amendment.

The actual code-selected command was also challenged in separate processes without editing tracked source. Restoring the exact old `compute_geometry` body yields **34 failed / 29 passed**; substituting the old false-success message yields **3 failed / 60 passed**. Restoring the current source returns **63 passed**. The first geometry harness used the wrong private symbol and failed before tests; that is explicitly not counted as a kill.

This is not a new CI job or a duplicate registration control plane. Existing #7066 retains its separate older receipt-subset carrier, and #7059 its held alternative wording; neither source branch was changed. This step executes this PR's actual protection/refusal contracts, using the existing test functions and existing code executor. Their pending source integration must not silently overwrite the chosen current copy.

Current Chairman continuation and protected `Mastermind@8b231e8267f09cfb002ed3e87bec14906dce1720` govern the same #7254 source operation. Exact-head hosted execution and independent full-PR review are still owed; local tests and this registration do not authorize merge, release or production claims.
