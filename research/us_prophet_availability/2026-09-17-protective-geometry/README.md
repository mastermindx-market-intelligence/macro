# Protective geometry: current source repair, not a new entry policy

## Capability and cause

The full recorded September-16 board/plan experiment produced bullish HON, RBA and TRN plans whose selected protective stop was above both spot and the lower waiting-entry band. The same management pass marked all three invalidated. `compute_geometry` accepted the historical hold invalidation, and `abs(entry - invalidation)` concealed the wrong side. The trade-plan schema checker only validated structure. This repair refuses contradictory protection before a new plan can be returned. It does not rewrite old plan or ledger files.

## Original carrier and accepted boundary

Operation `prophet-protective-geometry-20260917-sol-001`; branch `claude/prophet-protective-geometry-20260917`; base `8b688809239d760be0cdcf8cd64f0d6f7ee05316`. Parent recovery is `WS:PROPHET-US-AVAILABILITY`; existing engine owner is `WS:PROPHET-US-ENTRY-TIMING`. Procedure pin `Mastermind@42d210bc07a75234092ff5be71f6038ccacaa884`. Current Chairman continuation resumes the original direct operation, not a different worker or substitute branch.

The interrupted checkout was reconciled on its same carrier: no process/lock holder or published branch existed; 6,370 materialized files were checked against the original Git tree, with only the final zero-byte interrupted file incomplete. Empty lock and incomplete bytes were preserved as local evidence. The index and only missing source files were restored; the resulting original checkout was clean before edits. No global reset, sibling checkout modification or provider process was used.

## Implementation

The existing bridge validates consumed numbers before coercion (Python/NumPy Booleans are not prices), retains the existing stop-selection formulas, and requires signed risk to be finite and strictly positive. Known invalid protection is refused; it is never replaced with a different stop. The published rounded stop/targets/risk must retain positive finite values and their correct ordering. An explicit waiting-entry band must remain on the safe side of that same protective stop. Entirely unavailable protection or zone values preserve the existing null behavior; absence is not invented precision.

The existing `stage=geometry` refusal-accounting path records every affected candidate. Original candidate admission, ranking, open-plan/duplicate-ID rules, entry dates, tier event dates, historical identities, option selection and position-sizing authority remain unchanged. No new schema, risk manager, correction store or validator service was introduced. All three separately observed chronology refusals remain in place pending their own evidenced date-contract repair.

## Verification

The initial targeted preimage run had 36 intended failures and three valid-price controls passing. After repair, 279 tests passed across the existing bridge, Arena, clock-parity and integrity owners. Two pre-existing fixtures were corrected to describe their intended valid geometry: the point-in-time test formerly used a stop equal to entry (it now contains an actual future-only low), and the no-history fixture's default 90 stop exceeded its 42 entry (now explicit 38). No test was removed or skipped.

An exact-preimage differential over 1,500 deterministic valid BULL/BEAR inputs preserved every geometry result. Removing the waiting-zone call and accepting Boolean numbers were detected. A rounded-stop mutation initially survived; explicit cases with positive unrounded risk but equal serialized entry/stop were added and then detected that mutant. `focused-tests.json` discloses those results rather than hiding the initial coverage gap.

## Actual saved-board replay

The exact measured board SHA-256 `3cfa5b63b84b8010e53b17fef781ead9814083189fbf7d191f7b21e80a75588a` was replayed through the real origination function against the recorded seven-source recovery integration plus this change. No data was refetched; network access and writes into the prior real-input snapshot were prohibited.

The complete 42-row buy population retained 26 admitted survivors. Both controls intentionally used empty existing-ID/open-key sets to isolate the producer; these counts are not a production new-plan count. Preimage emitted 23 plan objects and refused the three chronology rows. Repaired source emitted 20 objects and additionally refused exactly HON/RBA/TRN for protective geometry. Every one of the 20 shared plan dictionaries was identical. No new IDs appeared; all 26 survivors were accounted for; the canonical intake check reported no breach.

An initial cross-root comparison differed only in an inherited earnings-input path string. The final receipt uses the same composite code/dependency context and exact preimage bridge bytes, so unchanged-output evidence is not based on silently stripping that field. The transient preimage swap occurred only in the private test snapshot and was restored in a finally block; the source carrier was never patched for the comparison.

## Integration and release

Semantic commit `6af030bdd7777901b00c170d4014a168cf787731` combines cleanly with recorded seven-source commit `3f41c92e98eb6d536ae20534c70aedb626996baf` into tree `68e785e61c7778a2e7dbe464c89f044f1fef6078`. It also combined with current-main pin `eef4e872f287c266e43247426d286ebc85af499f`; these immutable identities are in the receipt. No merge of main into the branch merely for ancestry was made. Latest-base CI is still a distinct gate.

Status: BUILT_NOT_PROVEN. Independent review and all binding checks must conclude before Sol releases the hold. A single acknowledged integration owner must then prove the actual candidate/plan/history/premium/browser journey. This source is not deployed, the chronology contract is not activated, and the old full-data experiment is not being relabeled as post-merge proof.

## Expanded consumer check and final fixture repair

The eight additional direct-call suites initially returned 444 passed, 6 failed and 27 existing sparse-artifact skips. An isolated interpreter loading the exact original bridge from the base, with unchanged dependencies, reproduced four of those failures: two absent sparse artifacts, an existing earnings-horizon expectation, and an existing management-state wording expectation. Those four are not waived or called green here, and no unrelated earnings/management source was edited.

The other two failures were old stretched-reset fixtures with a 90%-of-spot stop ABOVE the actual lower reset band. Their zone/price tests now explicitly construct valid protection below that band, preserving all original assertions. A new regression retains the old contradictory stop and proves the existing lossless geometry-refusal path. The final bridge/Arena/integrity/clock and reset-consumer run is **282 passed in 9.25s**. These test-only corrections do not change any source stop, candidate data or production policy. `final-consumer-tests.json` and `preimage-adjacent-failures.json` bind the scope and actual outcomes.
