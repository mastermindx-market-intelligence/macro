# Prophet P0B current-source evidence rebind

The deterministic stock-dashboard fixture receipt on protected Macro main still bound the pre-#7037/#7056 `engine/i18n.py` bytes. The current recipe reproduced every HK/Canada rendered output byte unchanged but emitted the current integrated i18n digest.

This maintenance slice updates only the live source binding and the closed downstream receipt chain: rendered fixture → two mobile-layout receipts → bounded repair extension manifest. Historical P0B baseline/target claims, screenshot hashes, output hashes, populations, routes, browser case counts, product templates and runtime source stay unchanged.

The existing self-binding suite was RED before the repair. After the closed chain was updated, the complete `tests/test_stock_dashboard_first_frame.py` owner passed 114 tests. This is repository evidence repair, not production/browser acceptance or deployment.
