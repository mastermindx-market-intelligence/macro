# TOI W1 Report — 2026-09-13

## Verdict

`PARTIAL` — P0/P1 is normalized and the first P2/archive normalization wave is now machine-checkable at 32 passports, but W1 as commissioned is not complete and W3 remains blocked.

## Capability gained

Before this carrier, W1 had no current machine-readable TOI method passport set. This branch now provides:

- **32 method passports** using `toi.method_passport.v1`: 12 P0, 11 P1, 7 P2, 2 archive;
- creator/official/primary source receipts with explicit rights class, including three Elliott/ordered-path research receipts with hybrid-model limitations preserved;
- alias/equivalence normalization distinct from dependency clustering across all 32 current passports;
- exact local implementation paths and signal IDs for **27** methods and explicit `missing` state for **5**;
- explicit ownership routing for incumbent Setup Species, Terminal-display/adaptive families, later-context path research and TOI W3 candidates;
- fail-closed validators plus hostile tests for schema, source, rights, equivalence and exact-local identity corruption.

This materially shrinks W3's rediscovery/search space while preserving negative evidence, incumbent ownership and missing implementations instead of manufacturing completeness.

## Current method set

### P0 — first-vertical core (12)

`toi.bb_bandwidth`, `toi.bb_kc_squeeze`, `toi.natr_percentile`, `toi.hvr`, `toi.nr7`, `toi.range_expansion`, `toi.donchian_breakout`, `toi.donchian_fakeout`, `toi.fractal_swing_structure`, `toi.benchmark_rs`, `toi.cmf`, `toi.rvol`.

### P1 — adjacent controls/context (11)

`toi.support_resistance`, `toi.adx_dmi`, `toi.choppiness`, `toi.momentum_12_1`, `toi.momentum_acceleration`, `toi.high52`, `toi.obv`, `toi.inside_bar`, `toi.ulcer`, `toi.round_levels`, `toi.short_reversal`.

### P2 — later/context/control families (7)

`toi.ma_trend`, `toi.rsi_oscillator`, `toi.kama_er`, `toi.supertrend`, `toi.connors_rsi`, `toi.geometric_double_pattern`, `toi.ordered_path_elliott`.

### Archive/comparator families (2)

`toi.macd_stoch_incumbent` is already owned by the existing Setup Species incumbent and is not a new W3 hypothesis. `toi.fibonacci_retracement` is retained as a specificity control/archive family because the empirical literature does not establish Fibonacci-zone behavior as distinct from non-Fibonacci zones.

Twenty-seven of 32 are exact local implementations. Five are intentionally `missing`: support/resistance, round-number controls, short-horizon reversal, Fibonacci retracement and ordered-path/Elliott. Missing means not implemented, not rejected and not permission for W1 to build the method.

## Source state

P0/P1 source law is machine-enforceable: every current P0/P1 passport resolves at least one registered primary/official source receipt, and licensed receipts require an explicit repository rights reference. Practitioner sources may be recorded but do not satisfy that gate.

The source registry now includes creator/official Bollinger material, TA-Lib formula documentation, primary momentum/reversal/52-week literature, New York Fed support/resistance/order-clustering evidence, public bar-grammar methodology, Fibonacci negative evidence, three Elliott/ordered-path studies, and the Massive internal rights receipt.

The Elliott receipts are deliberately qualified. Vantuch et al. provides an explicit pattern-detection algorithm; Jarusek et al. and Atsalakis et al. are hybrid neural/neuro-fuzzy systems. Those studies support testing an ordered-path representation, **not** assuming a universal deterministic five-wave law or granting any forecast authority.

The inside/outside-bar passport still cites the broad formula catalog in its current record; `SRC-STRAT-PUBLIC` is registered as stronger public-methodology context. That source-quality refinement remains visible debt rather than being hidden by the new P2 expansion.

## Equivalence and dependency state

Equivalence classes answer "same method / subtype / alias?" while dependency clusters answer "likely overlapping evidence family?" They remain separate. The 32 methods resolve into **29 equivalence classes**. Donchian breakout/fakeout, support-resistance/round-level control, and medium-term momentum/acceleration remain explicit within-family relationships rather than independent votes.

The P2 wave makes several previously implicit dependencies visible:

- moving-average crosses and ribbons are one moving-average-trend evidence family;
- classic RSI and Connors RSI share `rsi_mean_reversion` dependence and cannot be counted as independent confluence;
- MACD/StochRSI is an incumbent Setup Species comparator, not a new method birth;
- KAMA/ER and SuperTrend remain adaptive context/display families under existing entry-stack blocks;
- geometric double patterns stay within broad pattern-structure dependence;
- Fibonacci and ordered-path/Elliott remain separate later-context geometry families, with generic causal swing geometry as the ordered-path baseline.

## DNR and ownership state

`DNR:KILL-OUTCOME-AUDITION` is explicitly attached to short reversal and ordered-path/Elliott. It forbids per-name best-of-grid timer/count selection while leaving generic causal measurement available as a research control.

Existing `RUL-33` adaptive/oscillator entry-stack blocks are preserved in the passport failure/ownership fields without inventing a new stable DNR key. KAMA/ER, SuperTrend and Connors RSI therefore remain display/context families rather than silently becoming W3 candidates.

Owner boundaries remain unchanged:

- Setup Species is the sole scientific species registry.
- Live Entry Radar owns true tactical 5m/RTH entry events.
- Terminal is display/parity evidence, not TOI occurrence authority.
- Signal Foundry / Research Factory remains the candidate automation harness.
- W1 creates no second registry, event store, minute plane, replay system or promotion clock.

## Cycle-forecasting ruling now encoded in W1

The prior cycle-methodology work is now represented durably as `toi.ordered_path_elliott` with `P2 / toi_later_context / missing` state. Its contract is deliberately broader and more falsifiable than a literal Elliott count:

1. derive only causally confirmed pivots with `extreme_time` separate from `known_at`;
2. enumerate a small preregistered ordered-leg grammar rather than one asserted count;
3. retain multiple candidate parses or abstain under ambiguity;
4. compare parse-marginal path features against **generic causal swing geometry with the same feature budget**;
5. forbid per-name outcome-audition, post-hoc relabeling and Fibonacci specificity assumptions.

This preserves the strategic hypothesis without allowing the project to skip the simpler Compression Release proving vertical.

## Unresolved gates

W1 cannot be accepted until all of the following are closed:

- normalize remaining **gap/imbalance, standalone cycle-transform, breadth/peer, regime-conditioned and sequence/representation-learning** families into P2/archive/blocked dispositions;
- upgrade weaker source receipts when a stronger original/official method source is available;
- bind every relevant prior kill or RUL-33 block while preserving surviving measurements separately from killed constructions and without inventing stable DNR keys;
- classify any tactical-intraday residue to Radar and Terminal-only visuals to Terminal;
- complete the required independent skeptical reproduction of at least 20 sampled passports across source, formula and local implementation;
- run `python3 scripts/agentos.py validate`, all three W1 validators, `pytest tests/test_toi_w1_census.py -q`, and `git diff --check` on the exact return head.

## W3 gate

`HOLD`.

W3 Compression Release outcome testing still requires accepted W1 and accepted W2-0. No current census artifact authorizes an outcome read, model fit, parameter search, rank, gate, size, Prophet input or trade.

## Exact next action

Finish the remaining P2/archive/blocked families on this same carrier, then execute the >=20-passport reproduction review and exact W1 validation battery. W2-0 proceeds independently on PR #7094; if its exact-head packs conclude green, Sol may release and merge that records-only gate before W1 finishes. W3 remains held until both predecessors are accepted.
