# Rotation programme: actual-close repair acceptance and continuation

Owner: Sol. Current live Chairman direction is to advance independent programme capabilities while other sessions restore PC CI. Macro W1 PR7174 remains unchanged and held; no PC runner, label, job or permission was modified in this work.

Procedure: protected Mastermind `f590c068880dbb848bda90b80b73dbcb6688d6fc`, compatible Skillpack1.0.1. Source operation: `rotation-session-close-20260916-sol-001`. Existing scientific owner: `WS:TECHNICAL-OPPORTUNITY-INTELLIGENCE`; this is not another workstream.

## Capability and exact carrier

Terminal PR593 in `mastermindx-market-intelligence/mastermind-terminal`, branch `claude/rotation-session-close-20260916-sol-001`, exact head `953ab4d79316baf9c05c4a8cf87c9bde065d3539`, base `702d81bb35f2c900a5aa1215437bf968aa9e6d93`.

The existing regular-session filter and resampler now read the date's actual close, including shortened sessions and full closures. The existing live-source and stored-history consumers use that same helper. Direct resampling cannot bypass the close filter, even at one-minute resolution. No indicator formula, ranking, admission, plan, feed, ledger or capital authority changes.

The immutable JSON projection is compiled from Macro's existing `lib.nyse_calendar` and `engine.session_digest.session_window_et`. The exporter verifies the exact four owner/package files before and after generation against Macro `112eba2036fd1186e67b914e194f4fa541cfc4df`. There is no second holiday algorithm or runtime cross-repository import. Projection coverage is 2016-01-01 through 2028-12-31, 3267 sessions; SHA256 `d803dc85fcf3318bc78392e1d645b7063881b86eec95cf06f51192e1d836de98`. An absent in-coverage date is closed; an unsupported date or malformed projection produces `US_SESSION_CLOCK_UNAVAILABLE`, not an invented16:00 close. This projects known rules; future unannounced closures still require an owner correction and regenerated release.

## Recovered dependency and CEO ruling

Macro PR7094 at `5bb1bc68c99146fab040aade04bbf1903c51e5b7` identified the early-close defect: five failed2025-11-28 cases, with43 five-minute rows including13:00 instead of42 rows ending12:55. Its broader Daily/Weekly/4H scientific admission remains HOLD. Current production reads independently reproduced that defect in this turn.

Sol accepts this bounded defect and its canonical-clock repair for normal software release qualification. This does not accept the entire W2-0 dataset, authorize W3 outcome work, or waive any CI, current-base, deployment or real-consumer gate. The existing main-branch calendar owners are the source authority; the unmerged research report is supporting evidence, not an instruction granting runtime authority.

Macro PR7180 owns overlapping plan-origination/source-clock paths, so this slice made no competing edit there. At pickup, the complete15-open-PR Terminal changed-file census had no overlap with the four intraday helper/store/source/route paths. Recheck material movement before release; an open PR alone is not runtime liveness evidence.

## Tests and real-input proof

The new25-test clock suite first failed10 cases and passed15 against the old shared filter/resampler. After implementation, the five focused clock/session/source/route/math suites passed119 tests. They exercise early/full closures, DST dates, boundary-price/volume sentinels, direct-resampler enforcement, input immutability, unknown coverage, unchanged extended behavior, and volume/anchor checks across every3267 projected session.

Full `tsc --noEmit --incremental false` passed after restoring four exact omitted tracked fixtures and required source directories. No fixture was changed. ESLint passed on the three changed TypeScript/test files. The exporter `--check` reproduced the projection byte-for-byte against the pinned owner source.

Forty production HTTP responses were captured for AAPL, SPY, NVDA, JPM and XOM; dates2026-09-10,2026-03-06,2026-03-09 and2025-11-28; grains5m and4h. No vendor credentials or forward outcomes were read. The five early-close5m responses contained43 rows, while the canonical actual-close filter admits42. Raw responses remain local/private proof inputs, not committed licensed price corpora.

Those captured5m inputs were served through the actual local Next.js `/api/intraday` path, its existing stored-history reader and the repaired shared module. All20 cases matched canonical timestamps and OHLC exactly. Accumulated volume was compared with the explicit binary64 roundoff bound `(admitted_rows + 2) * epsilon * abs(expected_volume)`. All20 passed; the original production4h responses passed15 under the same comparator. Thus all five observed early-close defects were corrected in the local real-consumer path while all15 ordinary/DST controls remained matching.

The initial bitwise comparator reported15 local passes because five September10 volume sums differed by at most `1.4901161193847656e-08`; timestamps and OHLC already matched. Those strict results were preserved in `.rotation-clock-http-proof.json`, and the explained roundoff-qualified result is `.rotation-clock-http-proof-r2.json`. No price/time tolerance, material-volume waiver, source code change or strategy outcome selection was introduced to make this comparison pass. Every local response explicitly disclosed `store-only: POLYGON_API_KEY not set`.

Proof workspace: `/Users/chriswong/Documents/Cluade/charting-app/.claude/worktrees/rotation-session-close-20260916-sol-001`. Capture metadata and oracle inputs are under `.rotation-clock-inputs/`; the local HTTP results retain exact source and response digests. Local replay is not deployed, point-in-time, or investment-performance proof.

## Independent inspection and execution reconciliation

The first bounded builder submission was refused before execution; no log or operation lease existed. Sol performed the implementation directly. The later distinct read-only code-check operation `rotation-session-clock-codecheck-20260916-sol-001` ran through the existing selected MiniMax pool and returned `NO_REPRODUCIBLE_FINDING` against the pushed semantic head. It inspected the six named source/test/exporter paths and the closed-window contract. Its process exited0 in57.69 seconds, and exact lease `f74856d81372` was read back as released. No watcher or successor was commissioned.

Sol accepts that return as bounded source inspection, not a production or statistical verdict. Its wording is not stronger proof than the source: runtime checks receipt formatting; the exporter verifies source bytes. A close-boundary print belongs outside regular hours, not automatically to the next trading session. The inspection did not itself run the20-case HTTP replay. Parent tests and actual HTTP evidence supply those separate proofs.

An additional malformed-projection test append and custom direct-module replay-script preparation were tool-refused and are not credited. The permitted actual Next.js HTTP path supplied separate real-consumer proof; no refused code-file write was repeated through another tool. The current implementation head remained unchanged during review and proof.

## Held boundaries and next action

State remains BUILT_NOT_PROVEN until normal release and production proof. At the last current-head check, hosted Terminal unit/typecheck, Quote Hub, ingest/signal and CodeQL checks had succeeded; browser shards were still running. Admission and local tests are not those shards' success.

Extended04:00-20:00 policy, the quote hub's separate classifier, the missing5m/provider-hour fallback, daily/intraday adjustment comparability, historical revisions, point-in-time universe membership and full overnight entitlement remain unchanged and unqualified. A successful early-close repair does not establish full-universe09:30 parity or any adaptive timeframe selector.

Next: conclude exact-head browser/source release qualification, preserve the research HOLD, and use only the normal merged-master git-gated Terminal deployment and real production/browser proof. W1 stays aside for the other CI-recovery owners. Then advance the existing source-basis/episode-publication dependencies; do not recreate the completed local calendar repair or turn descriptive regime evidence into trade authority.
