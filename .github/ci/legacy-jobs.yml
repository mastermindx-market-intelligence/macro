# Legacy CI job manifest executed by scripts/run_ci_pack.py.
# This file is deliberately outside .github/workflows so GitHub does not
# publish one skipped check run for every logical job on every PR.
#
# ---------------------------------------------------------------------------
# OPTIONAL `paths:` — the changed-path scope (2026-08-09)
#
# A job may declare `paths:` (a list of repo-relative globs). On PULL REQUESTS
# it then runs only when the diff touches one of them. A job with NO `paths:`
# runs on every pull request, so declaring a scope is opt-in and can only ever
# remove work — never add it.
#
# Main's baseline and every workflow_dispatch run the FULL manifest: ci.yml
# passes `--changed-from` only for `pull_request`. That asymmetry is the safety
# net for the one thing static scoping cannot see (a test importing a module the
# job's commands never name), so a missed dependency reds main instead of
# shipping a green PR.
#
# Glob semantics (scripts/run_ci_pack.py::_glob_to_regex):
#     *   does NOT cross `/`      engine/*   -> engine/a.py, not engine/a/b.py
#     **  crosses `/`             engine/**  -> everything under engine/
#     trailing `/` is a prefix    engine/    == engine/**
#
# A declared scope MUST cover every repo path the job's own commands name, or
# the manifest fails to load. Widening is always the safe fix. If a job cannot
# be confidently bounded (a repo-wide lint, a full-tree guard), leave it
# unscoped — always-on is correct, not lazy.
# ---------------------------------------------------------------------------
jobs:
  inline-js:
    if: ${{ false }}
    gate: data
    # CURATED SCOPE (2026-08-14 incident follow-up). `scope: exclusive` makes
    # the paths below REPLACE inference instead of being unioned under it, so
    # the opaque fallback tier this job used to carry — whole-tree `site/**`,
    # `templates/**`, `docs/**`, `research/**` claims smeared out of a
    # subprocess or traversal deep in its closure — stops selecting it. Every
    # pattern here was generated from the job's OWN import closure and audited
    # for zero misses: no file this job demonstrably reads lost its owner.
    # 124s, and the declaration is a CORRECTNESS fix as much as a narrowing:
    # this linter's subject is the rendered tree — `check_inline_js.py site
    # templates` names both directories as bare argv, which carries no `/` and
    # so never became a literal path reference. Its site/templates ownership
    # therefore lived ONLY in the opaque fallback tier, one tier-split away
    # from being lost. Declared here explicitly; config/** and data/** are
    # retained because the selftest reads fixtures through the same traversal.
    scope: exclusive
    paths:
      - "config/**"
      - "data/**"
      - "scripts/check_inline_js.py"
      - "scripts/check_site_js.py"
      - "scripts/sparse_guard.py"
      - "scripts/worktree_sparse.py"
      - "site/**"
      - "templates/**"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      # Seeded-defect round-trip: fixtures with a curly-quote onclick, a broken
      # handler, and a .j2 curly onclick must go RED (plus a clean control that
      # must pass). Guards the guard — a regression that silently stops catching
      # the PR #2321 contamination class fails here, not in production.
      - name: guard self-test (seeded curly-quote onclick must go red)
        run: python3 scripts/check_inline_js.py --selftest
      # Checks <script> blocks AND on*= handler attributes (node --check, handler
      # wrapped as a function body — the #2321 gap), plus a smart-quote
      # contamination scan. templates/ is included so .j2 sources are curly-scanned
      # at PR time instead of riding green until the next render regenerates the
      # site copy (node cannot parse Jinja, so .j2 gets the curly scan only).
      - name: inline JS must parse
        run: python3 scripts/check_inline_js.py site templates
      # Companion guard: standalone site/*.js bundles are loaded via <script src=...>,
      # which the inline-JS guard deliberately skips — a syntax error there (e.g. the
      # smart-quote delimiters that blanked markets.html for ~2 days, PR #1004→#1353)
      # ships silently. This parses every non-vendor bundle with `node --check`.
      - name: standalone site JS must parse
        run: python3 scripts/check_site_js.py site

  nav-gap:
    if: ${{ false }}
    gate: data
    # The shared nav menu (.site-nav / vector-family .topbar) has no built-in top
    # gap — it relies on each page's body top padding. A new page that zeroes its
    # body padding jams the menu against the viewport top ("menu with no padding").
    # This catches that regression BEFORE merge instead of after it ships.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: nav menu must keep a top gap
        run: python3 scripts/check_nav_gap.py site

  nav-mega:
    if: ${{ false }}
    gate: data
    # The Research nav is a shared partial (_navlinks.html.j2) whose Research entry is
    # the grouped 'nav-mega' panel. A page built OUTSIDE the routine render loop (an
    # out-of-loop page builder, or a hand-static page) can render the shared nav but
    # freeze WITHOUT the mega-menu. This catches that drift BEFORE merge.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: shared-nav pages must carry the Research mega-menu
        run: python3 scripts/check_nav_mega.py site

  crypto-shelves:
    if: ${{ false }}
    gate: code
    # W0 lands the governor unarmed against the legacy templates. The first
    # data-shelf marker on each surface arms exact-set enforcement for that page.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: governor selftest (partial, duplicate and extra shelves must fail)
        run: python3 scripts/check_crypto_shelves.py --selftest
      - name: vector and crypto Tier-1 shelf governor
        run: python3 scripts/check_crypto_shelves.py

  board-shadow-substrate:
    if: ${{ false }}
    gate: code
    # HK+Canada Prophet zero-authority shadow substrate (WS:PROPHET-HK-CA-REVAMP,
    # research/PROPHET_SHADOW_CONTRACT_V1.md). K1-K14 + standing clauses for
    # engine/board_shadow.py. Kept on the CODE gate — unlike its
    # china_prophet_shadow/board_ledger siblings (unrun-picks-boards /
    # engine-render-guards, both `gate: data`) — because almost every kill
    # builds its own tmp_path fixtures rather than asserting against moving
    # committed data/site bytes; the merge-gate-rides-moving-data trap those
    # two jobs exist to dodge does not apply here (see
    # research/CI_MERGE_CONTROL_PLANE... / the moving-data finding). Left
    # UNSCOPED (no `paths:`), mirroring all-exports-resolve/no-module-leak
    # above: K6's repo-wide walk for the `prophet_shadow` string literal is
    # exactly the opaque-traversal shape `infer_job_scopes` cannot narrow
    # (scripts/run_ci_pack.py), so a declared `paths:` here would either be
    # wrong or drift — always-on is correct for this job, not lazy.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: { python-version: "3.12" }
      - name: install test deps
        # plotly/pillow/requests: the K1 legs import the REAL production
        # builders (scripts.build_canada / scripts.build_hk_library), whose
        # import closure needs the render deps — without them both K1 tests
        # die on ModuleNotFoundError before asserting anything.
        run: pip install pytest pandas numpy pyarrow pyyaml jinja2 plotly pillow requests
      - name: HK+CA Prophet shadow substrate — K1-K14 + standing clauses
        run: python -m pytest tests/test_board_shadow.py -q
      # hk-discovery wave (WS:PROPHET-HK-CA-REVAMP): the FIRST real Lane-B
      # registration against this substrate (K-D1..K-D9) — same tmp_path
      # fixture discipline as the K1-K20 suite above, same venv (pandas/
      # pyarrow only; no render deps needed for this module's own kills).
      - name: HK discovery challenger — K-D1-K-D9 (hk_discovery_v1)
        run: python -m pytest tests/test_hk_discovery_challenger.py -q

  board-contradictions:
    if: ${{ false }}
    gate: code
    # W2 board invariant guard: validates site/factordata/us_standouts.json for four
    # contradictions that the W8 arbiter + W6-US fix passes must prevent:
    #   (a) FRESH-BUY-ish state with stale cross or extended grade,
    #   (b) urgency=imminent on blocked label or BOTTOMING state,
    #   (c) band high/constructive while verdict is Lagging/no-clear-edge,
    #   (d) slot-1 alpha negative while positive-alpha rows exist in the same lane.
    # A build that violates any invariant fails RED here before merge.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      # On PRs, validate the artifact ONLY when this PR actually changes it. The
      # committed artifact is regenerated by the engine-render pipeline, so a code-only
      # PR (like the one introducing this guard) must not be judged against an artifact
      # built by OLDER code. Fresh renders are graded by the builder's own render-lane
      # self-check (scripts/build_stock_library.py, "board-contradictions self-check"
      # log line — warn-only, added in #2171); pages.yml's twin step is
      # workflow_dispatch-only and guards manual GitHub Pages mirror re-deploys, NOT
      # nightly renders. Before #2171 nothing graded fresh renders at all — an
      # invariant-(d) violation sat latent on main for weeks.
      # 2026-07-04 incident (#1195 run): a PR squash-merged before this job started, so by
      # run time origin/main's tip WAS the squash commit — and the old `--depth=1` fetch
      # shallow-cut it, so `origin/main...HEAD` had NO merge base. `git diff --quiet` then
      # exited fatal (non-zero), the skip branch was not taken, and the checker FAILED OPEN
      # against a stale artifact the PR never touched. Fix: fetch without --depth (checkout
      # is already fetch-depth 0), and treat a missing merge base as SKIP — coverage of
      # the artifact then rests on the render-lane self-check above (warn-only).
      - name: board invariants must hold when this PR ships the standouts artifact
        run: |
          git fetch origin ${{ github.base_ref }}
          if ! mb=$(git merge-base "origin/${{ github.base_ref }}" HEAD); then
            echo "no merge base with ${{ github.base_ref }} (base moved under a fast merge) — fresh renders are graded warn-only by the build_stock_library.py self-check (pages.yml twin is manual-dispatch only); skipping"
            exit 0
          fi
          if git diff --quiet "$mb" HEAD -- site/factordata/us_standouts.json; then
            echo "us_standouts.json untouched by this PR — fresh renders are graded warn-only by the build_stock_library.py self-check (pages.yml twin is manual-dispatch only); skipping"
            exit 0
          fi
          python3 scripts/check_board_contradictions.py site/factordata/us_standouts.json

  signal-gate-pair-coherence:
    if: ${{ false }}
    gate: code
    # #5490 tripwire. site/factordata/us_standouts.json and site/factordata/signal_gate.json
    # are written by ONE scripts/build_stock_library.py run and carry two copies of the same
    # reading — a board row's embedded `signal` blob is a strict superset of that ticker's
    # gate verdict. Only the gate write is guarded (`if sig_verdict:` + try/except), so a
    # `scope=all` re-render can advance the board and leave the gate behind: on 2026-08-12
    # that left 8 of 69 buy rows disagreeing (NTES blob ticks 0 against gate ticks 3) with
    # NOTHING on either file to reveal it — both stamp the same `as_of`, which is the DATA
    # date, not the write time. Readers that treat the gate as primary (build_discovery,
    # run_watchlist_sentinel) were handed a stale gate wearing a current-looking date.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      # Byte-identical to the install line 33 other jobs declare, so this job joins their
      # dependency group instead of provisioning a venv of its own. The suite needs it: it
      # imports scripts/build_discovery (pandas, jinja2) and scripts/run_watchlist_sentinel
      # (requests) to prove BOTH readers demote a gate that disagrees with the board.
      - name: install minimal deps
        run: pip install pytest pandas numpy pyarrow pyyaml jinja2 plotly requests beautifulsoup4 openpyxl scikit-learn
      # Always runs. The predicate and the two reader demotions are CODE, so they are graded
      # on every PR — not only on the PRs that happen to carry a fresh artifact pair.
      - name: pair-coherence predicate + reader demotions
        run: python -m pytest tests/test_signal_gate_coherence.py -q
      # Same diff-gate as board-contradictions above, for the same reason: the committed
      # artifacts are regenerated by the engine-render pipeline, so a code-only PR must not
      # be judged against a pair built by OLDER code. A missing merge base (the base moved
      # under a fast merge) is a SKIP rather than a fail-open red — fresh renders are graded
      # by build_stock_library.py's own warn-only self-check at the moment it writes them.
      - name: pair coherence must hold when this PR ships either artifact
        run: |
          git fetch origin ${{ github.base_ref }}
          if ! mb=$(git merge-base "origin/${{ github.base_ref }}" HEAD); then
            echo "no merge base with ${{ github.base_ref }} (base moved under a fast merge) — fresh renders are graded warn-only by the build_stock_library.py self-check; skipping"
            exit 0
          fi
          if git diff --quiet "$mb" HEAD -- site/factordata/us_standouts.json site/factordata/signal_gate.json; then
            echo "neither us_standouts.json nor signal_gate.json touched by this PR — fresh renders are graded warn-only by the build_stock_library.py self-check; skipping"
            exit 0
          fi
          python3 scripts/check_signal_gate_coherence.py site/factordata/us_standouts.json site/factordata/signal_gate.json

  ms-board-coherence:
    if: ${{ false }}
    gate: data
    # 2026-07-07 incident: render commit 5bf116a3b3 shipped a hybrid Market State board
    # (score 56 / "Mixed" verdict beside a "Risk Radar forces Risk-off." note + Risk-off
    # thesis) — stitched by the render lane's `git pull --rebase -X theirs` racing the
    # 07:10 engine-render. engine/market_state.py can never emit that pair from one
    # snapshot (the "forces" note requires ceiling < 42, then score = min(score, ceiling)).
    # This job fails RED on any committed board whose verdict/score/thesis/notes/flip
    # cannot come from a single snapshot; the render lanes carry a self-healing twin.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: market-state boards must be single-snapshot coherent
        run: python3 scripts/check_ms_board_coherence.py
      - name: guard selftest (synthetic fixtures incl. the 5bf116a3b3 hybrid)
        run: python3 scripts/check_ms_board_coherence.py --selftest
      - uses: actions/setup-python@v5
        with: { python-version: "3.12" }
      - name: install test deps (engine cross-check imports market_state)
        run: pip install pytest pandas numpy
      - name: guard constants must match engine/market_state.py
        run: python -m pytest tests/test_check_ms_board_coherence.py -q

  gh-annotation-line-start:
    if: ${{ false }}
    gate: code
    # A "::warning"/"::error" routed through logging NEVER reaches the Actions
    # summary: every builder logs with a prefixing format ("WARNING ::warning …")
    # and GitHub only parses a workflow command at column 0. The call reviews as
    # an alarm, runs clean, and emits nothing — so a fail-soft builder ships
    # degraded output with its only signal silently dropped. Shipped dead five
    # times (#3487, #3515, #3563, #3570, and #3562 which merged a fresh one on
    # 2026-07-26). The guard scans every module that runs inside an Actions step.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: { python-version: "3.12" }
      - name: install test deps
        run: pip install pytest
      - name: annotations must start the line (bare print, never a logger)
        run: python -m pytest tests/test_gh_annotation_line_start.py -q

  all-exports-resolve:
    if: ${{ false }}
    gate: code
    # A stale __all__ entry — a name the module never binds — is a dormant
    # AttributeError: `from m import *` raises on the first missing name, and
    # the traceback lands on the importing PR, not on the module that broke
    # its promise. Found live in collectors/sec_document_spine.py, which
    # listed "document_with_retrieval" without ever importing it from the
    # engine sibling. AST-static like the annotation guard above: importing
    # every engine/scripts module in CI would execute import-time side effects
    # for a source-visible property (measured 52.7s dynamic vs 2.3s static,
    # identical findings). Package __init__ files listing real submodules are
    # exempt by construction — `from pkg import *` imports those.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: { python-version: "3.12" }
      - name: install test deps
        run: pip install pytest
      - name: every __all__ name must resolve (bind it or drop the entry)
        run: python -m pytest tests/test_all_exports_resolve.py -q

  no-module-leak:
    if: ${{ false }}
    gate: code
    # A test that evicts a module from sys.modules and does not put it back
    # poisons every LATER test in the process, and the victim lands in an
    # unrelated FILE — so the failure only appears when the collected file set
    # happens to pair culprit and victim, and the failure set moves as files are
    # added. That is indistinguishable from flake, which is what lets a real
    # regression hide behind it.
    #
    # Measured 2026-08-06: test_admin_live_runs.py::test_probe_bot_no_requests_lib
    # popped `requests` and THEN monkeypatch.setitem()'d the same key, so
    # monkeypatch recorded "absent" and its teardown deleted the key AFTER the
    # test's own finally-block restore. The re-imported `requests` had no
    # `.sessions`, and test_capital_structure_share_count_materializer.py died
    # three files later. Both suites are green in isolation and in CI's narrow
    # packs, which is exactly why it survived.
    #
    # This job runs the STATIC half (the pop-then-monkeypatch idiom, catchable
    # even in a pack where culprit and victim never co-run). The RUNTIME half is
    # the MM_MODULE_GUARD tripwire in tests/conftest.py, which is armed in every
    # pytest job in this file.
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: { python-version: "3.12" }
      - name: install test deps
        # requests: the re-import asymmetry test reads the real package rather
        # than a stand-in, since the stand-in is what made this class invisible.
        run: pip install pytest requests
      - name: no test may leave sys.modules poisoned for the next file
        run: python -m pytest tests/test_no_module_leak.py -q

  admin-js-no-undef:
    if: ${{ false }}
    gate: code
    # admin/static/app.js is a flat top-level browser script and NOTHING in CI
    # read its scopes, so a reference to a name declared in another function
    # parsed clean and threw only on click. PR #3693 shipped exactly that
    # ("ReferenceError: rejBox is not defined") and the admin Outbox panel
    # rendered BLANK in production for ~2 days until #3871 module-scoped it.
    # node --check cannot catch it (an undefined identifier is valid syntax),
    # so this runs eslint with the single rule no-undef over admin/static/*.js.
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      # Provided once by the ci-pack wrapper; declared here so this job stays
      # readable as the standalone job it used to be (see wri-risk-core).
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
      - name: install test deps
        run: pip install pytest
      - name: admin JS must have no out-of-scope references (eslint no-undef)
        run: python3 scripts/check_admin_js.py
      - name: guard tests (real tree + planted #3693 offender, fail-closed in CI)
        run: python -m pytest tests/test_admin_js_no_undef.py -q
      # Companion guard, same file, different failure: an inline on*= handler
      # built with JSON.stringify emits double quotes that CLOSE the attribute,
      # so the browser stores a truncated expression and the control is dead on
      # click. check_inline_js.py already parses on*= attributes, but only in
      # site/ and templates/ where they exist on disk — app.js materialises them
      # in the browser, so the defect is visible only in the source that builds
      # them. Pure Python: no node, so a minimal pip lane cannot disarm it.
      - name: inline handler attributes must not interpolate JS string literals
        run: python -m pytest tests/test_admin_inline_handler_attrs.py -q

  path-prune-lint:
    if: ${{ false }}
    gate: code
    # A tree-walk that prunes on the ABSOLUTE path also matches anything ABOVE
    # its scan root. House law puts every agent session in a
    # `.claude/worktrees/<name>/` checkout, so a guard excluding ".claude" or
    # "worktrees" via `path.parts` skipped EVERY file and scanned nothing —
    # test_press_workflow's kill-switch guard saw 0 of 130 .yml (#3802). It read
    # green in the one checkout everybody works in and was real only in CI: the
    # press suite passed locally at 177 tests while ci-pack-0 went red.
    # A guard that can see nothing can refuse nothing, and nothing at runtime
    # says so — the test passes. So it is caught in the source, like the
    # annotation lint above.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: { python-version: "3.12" }
      - name: install test deps
        run: pip install pytest
      - name: walks must prune on the path RELATIVE to their scan root
        run: python -m pytest tests/test_no_absolute_path_part_prunes.py -q

  rebase-css-autoresolve:
    if: ${{ false }}
    gate: code
    # 2026-07-22 incident: asia-close run 29904079071 committed the day's engine outputs,
    # then every push retry died on the SAME rename/rename conflict — two lanes had
    # renamed a content-hashed site/assets/css/<hash>.css to different hashes, and
    # `pull --rebase -X theirs` can't settle path-level conflicts. The loop gave up with
    # a ::warning under a green run, silently dropping the day (hk_regime stuck 07-21).
    # scripts/rebase_autoresolve_hashed_css.sh now finishes such rebases (keeps both
    # hashed files byte-pure; refuses anything else); these tests replay the incident
    # with real git repos, so a git-version behavior change shows up here, not in prod.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: { python-version: "3.12" }
      - name: install test deps
        run: pip install pytest
      - name: autoresolver must finish rename/rename rebases on hashed css (and refuse all else)
        run: python -m pytest tests/test_rebase_autoresolve_hashed_css.py -q
      # 2026-08-01 sibling incident: the same push loops' `pull --rebase --autostash`
      # left a CONFLICTED autostash apply in the tree and the broad `git add site/
      # templates/` staged it verbatim — d29e4dd44d shipped conflict markers across
      # 1,704 committed pages, live until c1bfee482a. Every lane now runs
      # scripts/ci/strip_conflict_markers.sh before its broad adds; this replays that.
      - name: a conflicted stash/rebase apply must be stripped, never committed
        run: python -m pytest tests/test_strip_conflict_markers.py -q

  conflict-markers:
    if: ${{ false }}
    gate: code
    # 2026-07-03 incident: a merge commit shipped literal git conflict markers
    # and merged GREEN — Jinja parses marker lines as plain text, pytest never rendered
    # that block, and no other guard greps for them. This fails RED on any conflict-marker
    # line in templates/**, engine/**, scripts/**, or shipped site/** files. PRs use a
    # changed-file ratchet because the 2026-08-01 render incident left legacy generated
    # pages damaged on main; the full-tree pages/deploy gate remains unchanged. The
    # self-test proves both full scanning and the ratchet before trusting their OK.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: guard self-test (known-bad flagged, known-good clean)
        run: python3 scripts/check_conflict_markers.py --self-test
      - name: no new git conflict markers in PR content
        run: python3 scripts/check_conflict_markers.py --changed-from "origin/${CI_BASE_REF:-main}"

  title-i18n:
    if: ${{ false }}
    gate: data
    # The i18n rule: translated text NEVER goes in HTML attributes — the dual-span
    # l-en/l-zh mechanism cannot operate there, so a bilingual title= shows both
    # languages mashed together. This scans template SOURCE for CJK, t() calls, or
    # zh-variable interpolations inside title attributes (the fix is data-tip-en/
    # data-tip-zh + the theme.js popover, an English-only title, or no title).
    # Same class, different door (#2705): <title> is RCDATA, so a t()/td() in a
    # template <title>/{% block title %} — or a '<span' in a rendered page title —
    # ships literal markup as the browser-tab text; the sweep covers site/ too.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: guard self-test (known-bad flagged, known-good clean)
        run: python3 scripts/check_title_i18n.py --self-test
      - name: no translated text in title attributes or markup in <title> RCDATA
        run: python3 scripts/check_title_i18n.py templates site

  signal-contract:
    if: ${{ false }}
    gate: data
    # HARD pre-merge gate for the signal→chart §7 contract (research/signal_engine/SCHEMA.json,
    # the single source of truth shared with the charting app). A PR that drifts the engine output
    # from the schema, weakens the validator, or breaks a contract rule fails RED here — the safe
    # place to block (no deploy to stall). The daily build's gate is deliberately resilient
    # (discard-bad-write, never 404 the dashboard); this is where blocking has teeth. The test
    # suite includes an engine-analyze()-conforms-to-schema check that catches forgot-to-update-
    # the-schema drift without needing data fixtures.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install minimal deps
        # jinja2 joined 2026-08-07 for the forensic detector cross-pin below: that
        # suite imports scripts/build_fundamental_forensics.py, whose module-level
        # `from jinja2 import Environment, FileSystemLoader` is unavoidable. Verified
        # in a clean venv — without it the suite ERRORS at collection (red), it does
        # not skip, so the guard cannot go dark by losing a dependency.
        #
        # pyarrow joined 2026-08-09 for the prophet_us_audit adjusted-basis control
        # below: it builds its synthetic stores through DataFrame.to_parquet (a breadth
        # cache and a per-ticker baskets/ohlcv sleeve), and that is pandas' parquet
        # ENGINE, not an optional nicety — it is NOT transitive (verified: this line
        # without pyarrow installs pandas 3.0.5 and no pyarrow). Same failure direction
        # as jinja2 and verified the same way, in clean venvs carrying each install
        # string: without pyarrow 6 of that suite's 7 tests go RED with `ImportError:
        # Unable to find a usable engine; tried using: 'pyarrow', 'fastparquet'` — they
        # do not skip, so losing the wheel reds the pack instead of going quietly dark.
        run: pip install jsonschema pytest pandas numpy pyyaml requests jinja2 pyarrow
      # test_signal_quality_no_leak.py joined 2026-08-05 — dark from birth, named
      # by no run: step in any workflow.  It belongs HERE and not in a new job:
      # it pins the §7 contract this job already owns, one layer down.  The
      # marker `date` engine.signal_quality._buy_filter emits is NOT the earliest
      # date its take/block label was knowable — the label reads bars i+1 (and
      # i+2 on the counter-trend branch) — so a forward-return grade anchored on
      # the marker date embeds the confirmation bars it consumed (measured
      # +5.7pp/10d aggregate look-ahead).  The guard is structural, not a fixture
      # direction: flip only bar i+1 and a 'take' must overturn.  Deps are
      # satisfied by the install line above (pandas + numpy); the suite imports
      # engine.signal_quality and nothing else, so it adds no wheel, no venv and
      # no job.  Cost measured in this job's exact install string: the step goes
      # 31 -> 34 tests for ~0.2s (0.76s alone, 0.92s combined, steady state).  A
      # first run in a freshly built venv reports ~13s, but that is pandas/numpy
      # cold-start — paid by whichever pytest step runs first, already on this
      # job's bill, and NOT an increment this suite adds.
      # `early_signal_dates` (the additive dot knowability stamp) is registered HERE, in the
      # job that already owns the §7 contract: its whole risk surface is the published
      # per-ticker schema + the nightly validator, which is precisely what this step runs.
      # A contract field whose pairing rule no job checks is a scheduled red the first time
      # a producer drifts.
      # P1 unrun backfill 2026-08-14: tests/test_signal_date_family.py joins the step
      # that already owns scripts/validate_signals.py (test_validate_signals.py) and the
      # signal-date contract (test_early_signal_dates_contract.py).  It was named by no
      # `run:` step in any workflow, so the DATE FAMILY law — which of a signal's dates
      # may be derived from which, the thing engine/marker_integrity.py exists to pin —
      # was dark on a publish pipeline.  It rides this existing step because a step is
      # the masking unit (run_ci_pack.py returns on the first non-zero step, so a later
      # step goes dark behind an earlier red) while pytest reports EVERY file inside ONE
      # step, and no new job because the manifest is at the narrow-diff ceiling.
      # Appended LAST so a validator or look-ahead regression is the first failure a
      # reader sees rather than the date-family guard failing for a borrowed reason.
      # No dependency widened: probed at RUN time it loads only pandas/numpy/pyarrow/
      # pyyaml, all already on this job's install line.
      - name: signal §7 contract tests + the marker-date look-ahead guard
        run: python -m pytest tests/test_validate_signals.py tests/test_signal_quality_no_leak.py tests/test_early_signal_dates_contract.py tests/test_signal_date_family.py -q
      # drives the REAL engine.signal_quality._buy_filter and analyze() over synthetic
      # series, so it needs pandas + numpy + pyyaml (lib.config, via engine.technicals) —
      # all present in the install line above, so no importorskip is needed and none is
      # used. No data fixtures: every case is hand-built, which is why it can ride a job
      # that checks out no caches. It pins that the packet's refusal set isolates exactly
      # one leg — if that drifts, a research file starts overstating the veto's cost.
      - name: W-F reclaim-veto flip-isolation construction
        run: python -m pytest tests/test_us_reclaim_veto_packet.py -q
      # The RATIFIED sibling of the packet above (us_prophet_v2, 2026-08-10): the
      # counter-trend reclaim WAIVER, its PIT/staleness rules, its fail-closed fallbacks,
      # and the era fence us_prophet_v1 -> us_prophet_v2. Registered HERE deliberately: the
      # suite is hermetic (it writes its own washout artifact to tmp_path and points the
      # loader at it, because riding the committed site/factordata artifact would be a
      # SCHEDULED red — the waiver's PIT window is five sessions wide around a stamp that
      # moves every night), so it needs no caches, exactly like the job's other steps. Deps
      # are covered by the install line above: pandas/numpy for the frames, pyyaml for
      # lib.config AND for the synapse-registry assertion; engine.us_board_rank is pure
      # stdlib. Cost ~2s. It is also the ONLY registered guard on the new reason string's
      # Chinese row — tests/test_gate_reasons_exhaustive.py owns the general bilingual
      # completeness check but is itself named by no run: step in any workflow.
      - name: reclaim-veto conditional waiver — Arm P @20 + the us_prophet_v2 era fence
        run: python -m pytest tests/test_us_reclaim_waiver_prophet_v2.py -q
      # The two research/-RESIDENT suites, wired 2026-08-06 — UNRUN from birth: named by
      # no run: step in any workflow, so nothing ever executed them in CI. (They were
      # triggerable — research/**/*.py in ci.yml starts this workflow — which is the
      # worse half of the trap: the gate STARTS, the pack goes green, and the suite that
      # would have caught the break never ran.) Both were written beside their instrument
      # under research/ because the packets that shipped them were fenced to files-only;
      # nothing here runs pytest over research/, so a PR could break either instrument
      # and stay green. They belong in THIS job for exactly
      # the reason test_signal_quality_no_leak.py does: each pins the buy-side §7
      # machinery this job already owns, one layer down — the veto-day label classifier
      # (#4547) and the canonical reclaim/divergence/200-MA filter math the §7 buy filter
      # is built from. No new job, no second venv: the install line above already
      # satisfies them (numpy + pandas, plus pyyaml for lib.config via engine.technicals),
      # so none needs an importorskip and none uses one. Synthetic only — hand-built
      # frames and planted structure, no data/ store and no network — which is why they
      # can ride a job that checks out no caches. Measured in this job's exact install
      # string on a committed-files-only tree: 44 tests, ~1.2s combined.
      #
      # The THIRD suite (earnings-ignition, added 2026-08-09) was dark the same way for the
      # same reason, and an adversarial re-read of its instrument found a live anchoring
      # defect that nothing in CI could have caught. It pins the same buy-side §7 marker
      # stream one layer out — the derived knowability convention those markers are read
      # through, the EDGAR-roll-aware announcement window, and the single reaction-session
      # anchor. Its only engine import is engine.session_anchor, whose US reference index is
      # rule-computed and touches no disk, so the no-caches property holds.
      #
      # The note that stood here — that the three darkness censuses scan tests/ ONLY,
      # so this wiring was hand-verified rather than census-verified — has been false
      # since 2026-08-06 (#4709): discovery is whole-tree, and as of 2026-08-09 the
      # bare census GATES. A research-resident suite named by no job now reds the
      # workflow-yaml pack instead of shipping dark, so the wiring below is checked by
      # a guard and not by whoever last read this comment.
      - name: research-resident label-grading + buy-filter guards
        run: >
          python -m pytest
          research/prophet_us_audit/test_label_grading_battery.py
          research/signal_engine/test_buy_filters.py
          research/prophet_us_audit/test_earnings_ignition_measurement.py -q
      # The remaining prophet_us_audit guard suites, wired 2026-08-09 by the census the
      # step above was hand-verified against. All three were UNRUN from birth for the
      # same reason as their two siblings: written beside the instrument under research/
      # because the packet that shipped them was fenced to files-only, then named by no
      # run: step in any workflow. What they pin, one per file: the US board's post-fire
      # TRAJECTORY (what the read does after a board fires, not just at the fire); the
      # close-source PRICE LADDER (which basis resolves, cache vs adjusted, and in what
      # order); and the #4698 COVERAGE-VS-BASIS confound control (baskets/ohlcv carries
      # the large-cap sleeve ~2 years deeper than the breadth cache, so a naive basis
      # swap grows the studied population and prints a coverage change as a basis
      # effect — measured +31%, 22,616 -> 29,675 rows).
      #
      # They belong in THIS job for the reason every research-resident step above does:
      # the same §7 buy-side/veto machinery this job already owns, one layer down. Deps
      # are satisfied by the install line above, including the pyarrow that joined for
      # the adjusted-basis fixtures — no importorskip in any of the three, so none can
      # skip its way to a green. Synthetic fixtures only: hand-built frames and planted
      # structure, no network, so they ride a job that checks out no caches. Measured in
      # this job's exact install string: 47 tests, ~2.4s.
      #
      # Two skips are expected on a pack runner and are NOT the skip-only class. The
      # ladder's two real-store receipts (test_cfg_regression_against_real_stores,
      # test_non_payer_and_no_exdiv_names_agree_across_bases) are skipif'd on the
      # absence of data/ — CI packs carry no data/ tree, by design — and each has a
      # synthetic twin in the same file that still pins the shape. The adjusted-rerun
      # suite's one cross-ref test skips when the source branch ref is not in the
      # checkout, which a shallow actions/checkout is; the other six run.
      #
      # The FOURTH sibling, research/prophet_us_audit/test_veto_leg_isolation.py, is
      # deliberately NOT wired here: its warm-up pin fails deterministically on main
      # (engine LEG_WARMUP_BARS["m3_s3"] = 232, but the minimal known-at N measured on
      # the suite's own fixed fixture is 239 — the constant went stale in the 2D/3D
      # session-anchor re-anchor window). Wiring a known-red suite would buy a red pack,
      # not coverage, so it is waived in config/unrun_test_waivers.yml with that reason
      # and an owner: the Prophet US program re-pins the constant, deletes the waiver
      # row, and adds the file to the run: line below.
      # test_stop_curvature_study.py joined 2026-08-12 with the §6.8(a) curvature study.
      # It is a MUTATION suite, not a smoke test: it plants the exact defect each of that
      # study's self-reporting gates claims to catch (a diverged parity flag, a
      # forward-looking feature, a 3D state stamped before its bucket closed, an outcome
      # read before the ex-ante exemplar gate) and requires the gate to go red — because
      # a receipt derived from the thing it checks cannot fail on its own. It also pins
      # the null in the PASS direction: a planted name that genuinely arches on both
      # exemplar dates must CLEAR G5, so "all five forms missed" is a measurement rather
      # than a stuck switch. Synthetic frames only; its one real-store receipt is
      # skipif'd on the absence of data/ and has a never-skipping synthetic twin, same
      # pattern as the price ladder above. Measured: 22 tests, ~0.7s.
      # Base-closure 2026-08-14: #5540 added the Overtime horizon-reconciliation
      # acceptance suite under tests/ but named it in no workflow. The bare unrun audit
      # therefore reds current main. It belongs with the Prophet audit guards whose
      # horizon/price-basis contracts it reconciles; the existing dependencies suffice
      # and the focused suite is 23/23 green. Sole `run:` owner (one step per suite);
      # do not re-name on engine-render-guards or unrun-market-plumbing.
      - name: prophet US audit guards — post-board trajectory, price ladder, adjusted-basis control
        run: >
          python -m pytest
          research/prophet_us_audit/test_post_board_trajectory.py
          research/prophet_us_audit/test_price_ladder.py
          research/prophet_us_audit/test_stop_curvature_study.py
          research/prophet_us_audit/test_veto_leg_isolation_adjusted_rerun.py
          tests/test_prophet_overtime_horizon_reconciliation.py -q
      # P1 unrun backfill 2026-08-14: the DISCLOSURE BUNDLE — the publish-side artifact
      # built on top of the lineage/normalize/detector chain below — joins the step that
      # already owns that chain.  tests/test_fundamental_forensics_disclosure_bundle.py
      # was named by no `run:` step in any workflow, so scripts/fundamental_forensics_
      # disclosure_bundle.py and engine/fundamental_forensics/disclosure_{bundle,
      # projection}.py had no CI owner at all.  This job is the owner on the evidence:
      # it already runs the ingest, normalize, detector and contract suites over
      # engine/fundamental_forensics/models.py.  It rides this existing step because a
      # step is the masking unit (run_ci_pack.py returns on the first non-zero step)
      # while pytest reports EVERY file inside ONE step, and adds no job to a manifest
      # already at the narrow-diff ceiling.  Appended LAST so an ingest/normalize/
      # detector regression is the first failure a reader sees rather than the bundle
      # failing for a borrowed reason.  No dependency widened: probed at RUN time it
      # loads pandas/numpy/pyarrow/pyyaml/requests, every one already on the install
      # line above.
      - name: fundamental forensics lineage, normalization, detector, and schema contracts
        run: >
          python -m pytest
          tests/test_fundamental_forensics_ingest.py
          tests/test_fundamental_forensics_normalize.py
          tests/test_fundamental_forensics_detectors.py
          tests/test_fundamental_forensics_contract.py
          tests/test_fundamental_forensics_disclosure_bundle.py -q
      # K1 Evidence Foundation is a pointer contract spanning owner-native FIF,
      # Earnings, Theme Graph, Institutional 13F, GovRev, BioCatalyst, TXI, QLedger,
      # Market Memory objects, plus the Data OS no-I/O negative control. It rides this
      # existing contract job rather
      # than creating a new job: the install line already supplies the complete import
      # closure, and the suite resolves every declared owner reader on the checked-out
      # base in addition to schema/fixture semantics. Appended after the FIF owner step
      # so an owner regression is reported before an interoperability regression.
      - name: K1 Evidence Foundation ref, block, recipe, and hostile-fixture contracts
        run: >
          python -m pytest
          tests/test_evidence_foundation_contract.py
          tests/test_evidence_foundation_product_contract.py -q
      # K2-B is a pure contract/fixture compiler which deliberately does not import
      # an institutional owner or materialize a store. Keeping its hostile contract
      # suite adjacent to K1 makes the pointer-only boundary visible in CI.
      - name: K2-B manager-complex and research-intent hostile contracts
        run: python -m pytest tests/test_institutional_manager_intent_contract.py -q
      # K2-C is the bounded owner-read adapter over the institutional 13F catalog:
      # it composes existing engine/institutional_census read APIs into the K1 +
      # K2-B contracts and emits a deterministic owner-read receipt. Its suite
      # rides this same contract job because the fixture store is an in-test
      # LocalStore published through the owner's own APIs — no credentials, no
      # network, and the K1/K2-B suites above already supply the import closure.
      - name: K2-C institutional owner-read adapter contracts
        run: python -m pytest tests/test_institutional_13f_adapter_contract.py -q
      # K3-E is the Opportunity Evidence Vector: a typed, deterministic view/join
      # over canonical owner outputs (contracts/opportunity_evidence/vector.v1.
      # schema.json + slot_registry.v1.json) — never a store, never a score. Rides
      # this same contract job for the reason K2-B does: it is a pure in-memory
      # validator/composer (lib/opportunity_evidence.py is stdlib-only — json,
      # hashlib, re, pathlib, typing) that imports no owner and materializes no
      # store, so the install line above already satisfies it (pyyaml is used by
      # the suite's families.yml join test only, not by the lib module itself).
      # Placed immediately after K2-B to keep the two pointer-only, zero-authority
      # contract compilers adjacent.
      - name: K3-E Opportunity Evidence Vector contract and hostile-fixture kills
        run: python -m pytest tests/test_opportunity_evidence_vector_contract.py -q
      # Four detector ids exist as byte-identical strings in THREE implementations
      # with three separate threshold stores: scripts/build_fundamental_forensics.py
      # (inline FRACTION literals, Filing Forensics workbench),
      # engine/moat_falsifiers.py (pp module constants, PRODUCTION ticker pages), and
      # config/fundamental_forensics.yml (registry Decimal strings, SEC lineage
      # kernel). Nothing cross-pinned them, so a threshold edited in one store left
      # the same named detector answering differently on two live surfaces. This
      # suite reads each store's own numbers — it hardcodes none of them.
      #
      # Since 2026-08-07 it also pins EVALUABILITY, not just thresholds: section 6
      # drives all three implementations off the registry's own companyfacts
      # fixture across a table of missing-input scenarios and asserts the set of
      # three-way disagreements is exactly the documented, deliberate ones. That
      # section exists because the threshold pins were fully green while
      # engine/moat_falsifiers.py fired capital_intensity_rising on absent
      # op_income for 62 of 336 live fires (18%).
      #
      # It belongs in THIS job because the step above already owns the detector
      # contracts and already imports engine.fundamental_forensics (including
      # run_fixture_slice, which section 6 uses) and engine.moat_falsifiers, so
      # this job's install string is already proven sufficient for both.
      - name: forensic detector cross-pin (three implementations, one detector id)
        run: python -m pytest tests/test_forensic_detector_crosspin.py -q
      # Live Entry Radar W1 (PR-1, 2026-08-14): the probe-universe funnel + enlistment-bus
      # contracts (engine/entry_radar/**) — frozen nomination schema, no-flattening bus,
      # layer A-D assembly, prospective spool, missing≠negative retention, and the
      # Prophet-clean-diff guard. Registered HERE and not in a new job: the suites are
      # HERMETIC (synthetic fixtures only, zero third-party imports — pure stdlib, no
      # pandas/no boto3, verified by import audit), so they ride this job's existing
      # install line with no new wheel; and this job already owns the adjacent
      # signal-layer contract surface the Radar seam feeds (entry_event.v1 lands beside
      # the §7 contract at Radar PR-2). Cost: 93 tests in ~7s steady-state. Radar earns
      # its own job when the live evaluator lands (PR-4), not before.
      #
      # W2 (PR-2, 2026-08-14) lands in the SAME step, as that comment anticipated:
      # the entry_event.v1 store, the governed mastermind.indicator/v1 ingest, the G0
      # grey-dot adapter and its F1-F6 parity tables, plus the W2 boundary/mutation
      # guards. Still hermetic — the parity substrate is five COMMITTED slice fixtures
      # under tests/fixtures/entry_radar/, no data/, no site/, no network, pure stdlib
      # (the suites assert that themselves: no pandas/numpy import may appear in a W2
      # module). Cost (whole step, all five suites): ~263 tests in ~5-7s steady-state.
      #
      # W3 (PR-3, 2026-08-14) joins the SAME step: the C1/C2/C3/C4/C5 detector specs,
      # the A5.1 provisional-daily reconstruction, the RTH 4H grid, and the PIT-1..26
      # mutation battery. Two things change and neither needs a new job. (a) W3 DOES
      # compute indicators, so these suites import pandas/numpy — already in this job's
      # install line (the forensic-detector step above needs them), and the W2 "no
      # numeric import" assertion stays scoped to the four W2 modules by name. (b) the
      # substrate is two new COMMITTED synthetic fixtures under tests/fixtures/entry_radar/
      # (w3_c1c2_path.json, w3_early_close_tape.json, generated by
      # scripts/entry_radar_fixture_gen.py, provenance in w3_provenance.json) plus the
      # existing W2 real slices — still no data/, no site/, no network. One test spawns
      # two subprocesses to vary PYTHONHASHSEED (determinism, PIT-25). Cost (whole step,
      # all eleven suites): 675 tests in ~34s steady-state, measured 2026-08-14 after
      # the adversarial-review fixes (W3's own six suites are 401 of them; the 50 new
      # tests are the W3-1..W3-15 regressions).
      #
      # W4+W5 MERGE NOTE (2026-08-16): both waves joined this step in the same
      # day from parallel lanes; the two comment blocks below are both true and
      # kept verbatim. ONE step remains the masking unit; the run line is the
      # union of the W4 and W5 suite lists.
      # W4 (PR-4, 2026-08-15) joins the SAME step: the frozen-substrate pack
      # (engine/entry_radar/live_pack.py — nightly threshold inversion, the four §7.1
      # degeneracies, the inversion PROOF battery) and the runtime episode ledger
      # (live_ledger.py — §13 records, spool-before-consume, the §10 clock overlay and
      # re-arm chain). Deps are unchanged: pandas/numpy (already installed for the
      # forensic step) plus the pyarrow the adjusted-basis fixtures brought in, which
      # the pack's substrate.parquet round-trip needs. Substrate is SYNTHETIC (a
      # deterministic trend+ripple built in the suite, no new fixture file) plus the
      # existing COMMITTED slice fixtures for the G0/C5 lane — still no data/, no
      # site/, no network, no wall clock. ALL SEVEN W4 suite names are listed here
      # from the first W4 PR: five are placeholders Builder B populates, and naming
      # them now is what stops a later suite shipping dark behind a wiring edit nobody
      # made. Cost (whole step, all eighteen suites): 825 tests + 1 skip in ~48s
      # steady-state, measured 2026-08-15 (W4's own two suites are 135 of them).
      # W5 (PR-5b, 2026-08-15) joins the SAME step: the §14 prereg gates, the §15
      # adversarial battery (A/B/E/F/G/H/I/J/K/L/M), the nightly reconciler's write
      # discipline, and the replay data plane. THE HERMETIC/PANDAS SPLIT IS NOT A
      # JOB BOUNDARY HERE, and it is worth being explicit about why, because the
      # W1/W2 comments above describe these suites as pure-stdlib and that stopped
      # being the whole story at W3. W1+W2 are still hermetic pure-stdlib; W3
      # computes indicators and already imports pandas/numpy; W5 additionally
      # writes and reads `data/entry_radar/forward.parquet`, so it needs pandas'
      # parquet ENGINE. All three needs are ALREADY satisfied by this job's install
      # line — `pandas numpy pyarrow` are there for the forensic cross-pin and the
      # prophet_us_audit adjusted-basis control (see the two paragraphs on that
      # line) — so W5 adds NO wheel and provisions NO new environment. Registering
      # it here rather than in a new job follows this file's own rule: a step is the
      # masking unit (run_ci_pack.py returns on the first non-zero step) while
      # pytest reports EVERY file in one step, so extending this list adds coverage
      # without adding a place for a later step to go dark, and adds no job to a
      # manifest already at the narrow-diff ceiling.
      #
      # W5 is hermetic in the sense that matters: every suite runs against tmp_path
      # roots, synthetic frames and monkeypatched constants. The reconciler suite in
      # particular NEVER touches the real data/entry_radar/ or data/qledger/ — it
      # injects a temp root — so CI cannot register a production claim. The §14
      # gates all REFUSE today (prereg.PREREG_COMMIT is UNSET until PR-5b stamps the
      # merged prereg), and the gate suite pins that each gate refuses for its OWN
      # reason and passes on correct input, so the shipped all-refuse state cannot
      # hide a gate that stopped discriminating.
      #
      # `_w5_perf` (#5775) joins the same step for the same reason the W4 names were
      # pre-registered above: this file has NO catch-all pytest run — 1,555 of the
      # 2,451 test files under tests/ are enumerated somewhere and the rest never
      # execute — so a suite that is not named here does not run, and a perf refactor
      # whose whole warrant is "output is byte-identical" would ship with its
      # equivalence pins dark. It adds no wheel (pandas/numpy/pyarrow already on this
      # job's install line) and no data/ or network dependency: every case runs on
      # tmp_path parquet and synthetic frames.
      #
      # W5 HEARTBEAT (2026-08-16) joins the SAME step: tests/test_entry_radar_w5_
      # heartbeat.py pins the replay runner's `[phase] k/N (elapsed Xs)` liveness
      # lines — stderr-only (stdout carries the receipt contract), both thresholds
      # firing independently, a broken stream never raising out of a loop, and the
      # five instrumented phases named by construction site so a deleted call site
      # reds instead of going quiet. Hermetic and dependency-free: StringIO streams
      # plus one tmp_path call into the real `_finalize_candidates` with an empty
      # candidate list. 15 tests in ~1.6s, no new wheel.
      #
      # W6 (PR-6, 2026-08-17) joins the SAME step: tests/test_entry_radar_w6_priority.py
      # is the RP1 Research Priority adversarial battery (determinism, firewalls,
      # stale/unavailable, multi-expert, live-seam recovery tape). Hermetic: W4
      # synthetic pack + recovery tape, no data/, no site/, no network, no LLM.
      # Unnamed suites never run in this manifest; leaving W6 off this line would
      # ship the ranking path dark.
      #
      # W4.1 (2026-08-18/19, live-transport correction) joins the SAME step:
      # tests/test_entry_radar_w41_transport.py is the real-envelope contract
      # battery (entry_radar.events/v1 spool envelope -> W5's read_spool_events,
      # confirmed_lanes pack field -> the RTH reader, byte-exact event identity,
      # keep-first dedup on a re-spooled event_id, unparseable-pass_ts latch
      # guard). Hermetic: tmp_path spool files + synthetic packs, no data/, no
      # site/, no network. Unnamed suites never run in this manifest.
      - name: entry-radar W1+W2+W3+W4+W4.1+W5+W6 — probe universe, entry_event.v1 store, G0 parity, C1-C5 PIT battery, live pack + episode ledger + live evaluator, live-transport correction, W5 prereg gates + forward evidence, RP1 Research Priority
        run: python -m pytest tests/test_entry_radar_w1.py tests/test_entry_radar_producers.py tests/test_entry_radar_g0_parity.py tests/test_entry_radar_events.py tests/test_entry_radar_w2_guards.py tests/test_entry_radar_w3_detectors.py tests/test_entry_radar_w3_c1c2_pit.py tests/test_entry_radar_w3_c3_4h_pit.py tests/test_entry_radar_w3_c4.py tests/test_entry_radar_w3_c5.py tests/test_entry_radar_w3_guards.py tests/test_entry_radar_w4_pack.py tests/test_entry_radar_w4_ledger.py tests/test_entry_radar_w4_live.py tests/test_entry_radar_w4_pit.py tests/test_entry_radar_w4_liveness.py tests/test_entry_radar_w4_c3_reader.py tests/test_entry_radar_w4_lane.py tests/test_entry_radar_w41_transport.py tests/test_entry_radar_w5_gates.py tests/test_entry_radar_w5_battery.py tests/test_entry_radar_w5_reconciler.py tests/test_entry_radar_w5_data.py tests/test_entry_radar_w5_perf.py tests/test_entry_radar_w5_heartbeat.py tests/test_entry_radar_w6_priority.py -q

  session-anchor-era:
    # Absolute session-calendar anchor, both eras. The confluence 2D/3D + fortnight
    # buckets (era abs-session-2026-08-06, ruling research/SESSION_ANCHOR_ABSOLUTE_
    # CALENDAR_ADJUDICATION_BY_FABLE.md, #4732) and the §7 marker engine (era
    # sq-abs-session-2026-08-06, research/SIGNAL_QUALITY_SESSION_ANCHOR_ADJUDICATION_
    # BY_FABLE.md, #4738). Both suites shipped named by NO run step in any workflow, so
    # the START-INVARIANCE guarantee each era rests on — the whole claim of the repair —
    # was never executed in CI (the 2026-07-26 census: every invocation here carries an
    # explicit file list, so an unnamed suite is genuinely never run).
    # audit_unrun_tests.py did list them, at tier P5: they score triggerable=True off
    # subjects that ride engine/**, and they write no ledger, so the ranking that
    # surfaces darkness sorted the guarantee under a PUBLISHED era stamp below 100-odd
    # quieter holes. Enumerating beats ranking. They pin k=1..6 leading-session drops on
    # fixtures built from REAL NYSE sessions (holiday spans, a 3-session halt,
    # shallow-depth caches), the era stamps on the payloads, and for the §7 suite the
    # marker stream dict-for-dict end-to-end through gate(). Self-contained — the
    # fixtures are generated from lib/nyse_calendar.py, nothing is read off disk — so
    # there is no skip path and no data dependency. 239 tests, ~26s locally; the budget
    # below is headroom for a hosted 2-vCPU pack.
    #
    # The §7 half arrives here one PR late, and the reason is worth keeping: #4800 wired
    # the confluence suite while #4738 was still in review, and a run step naming a file
    # the tree does not have aborts the ENTIRE pytest invocation — listing it early would
    # have taken the landed suite down with it, which is exactly what
    # test_every_workflow_test_path_exists pins. That guard matches on raw workflow TEXT,
    # so while #4738 was open the filename could not appear even in this comment.
    #
    # THIS JOB IS THE SINGLE OWNER of the confluence suite. #4833 (cycles anchor era,
    # merged 2026-08-07) independently appended it to engine-render-guards' pytest tail
    # alongside tests/test_cycles_anchor_invariance.py, so main briefly ran it in TWO
    # jobs; that duplicate line is removed in the same PR that adds the §7 half here.
    # A dedicated job is deliberate (the shape #4756 set for coiled-mtf-anchor-era): an
    # anchor regression should red a check named for the anchor, not one named
    # "engine-render-guards" with 60-odd unrelated suites in it. The cycles battery is
    # left where #4833 put it — consolidating all three era batteries here is a sensible
    # follow-up, but it belongs to the cycles lane, not to this PR.
    #
    # Placed beside signal-contract, which owns engine/signal_quality.py's §7 contract
    # one layer up, rather than beside the washout-turn organ: #4756 (coiled-mtf-anchor-
    # era, the sibling anchor-era job this one mirrors) is in flight and appends at
    # exactly that seam, and two jobs landing in one gap is a merge conflict for
    # whichever ships second. Venv sharing is by BYTE-IDENTICAL install command, not by
    # adjacency, so position costs nothing: the pip line below is the 32-job group's.
    if: ${{ false }}
    gate: code
    timeout-minutes: 8
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install minimal deps
        run: pip install pytest pandas numpy pyarrow pyyaml jinja2 plotly requests beautifulsoup4 openpyxl scikit-learn
      - name: session + §7 anchor invariance (k=1..6 start drops, bucket/marker parity, era stamps)
        run: python -m pytest tests/test_session_anchor_invariance.py tests/test_sq_anchor_invariance.py -q

  trial-budgets:
    if: ${{ false }}
    gate: data
    # W1d (audit #21): every multiple-testing harness must REGISTER its trial budget, or the
    # DSR/kill/FDR deflation it feeds is a lie. A NEW validate_*/*_phase0 script that deflates a
    # Sharpe without registering fails RED here (the ~33 pre-existing offenders are grandfathered
    # in an allowlist that only shrinks). Also runs the grading-rebuild + ledger test suites.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install minimal deps
        run: pip install pytest pandas numpy pyarrow pyyaml
      - name: trial-registration lint (no new unregistered harness)
        run: python3 scripts/check_trial_registration.py
      - name: reliability-contract guard (no gated field rendered directionally, audit #46)
        run: python3 scripts/check_reliability_contract.py
      # A ticker rename is a ledger-key migration, not a fresh company. This
      # suite owns the track-record ingest guard, duplicate detector, and
      # losslessness receipt; keep it ahead of the broader grading invocation
      # so an unrelated grading failure cannot leave identity enforcement dark.
      - name: track-record company-key identity + lossless migration guard
        run: python -m pytest tests/test_ledger_identity.py -q
      - name: grading rebuild + trial ledger tests
        run: python -m pytest tests/test_grading.py tests/test_trial_ledger.py tests/test_trial_registration_lint.py tests/test_no_literal_ntrials.py tests/test_universe_history.py -q
      # Stock Identity W1 (WS:STOCK-IDENTITY): partition seal/reproducibility, metric-block
      # purity, truncation invariance, state totality, episode segmentation, G-8 import
      # firewall, banned-token/authority artifact walk. Minimal-deps safe (no matplotlib);
      # artifact-dependent tests skip cleanly when data/stock_identity is absent.
      # P1 unrun backfill 2026-08-14: tests/test_audit_reused_tickers.py joins the
      # stock-identity family.  It was named by no `run:` step in any workflow, so
      # scripts/audit_reused_tickers.py — the audit that catches a ticker RE-USED for a
      # different company, the failure the identity atlas exists to make visible — had
      # no CI owner while every other identity guard here did.  This job owns the
      # subject: the partition/fingerprint/state-episode/atlas suites above are the
      # identity spine the reuse audit reads.  Appended LAST so a partition or
      # fingerprint regression is the first failure a reader sees rather than the reuse
      # audit failing for a borrowed reason.  It rides this existing step because a step
      # is the masking unit (run_ci_pack.py returns on the first non-zero step) while
      # pytest reports every file inside one step, and adds no job to a manifest at the
      # narrow-diff ceiling.  No dependency widened: probed at RUN time it loads only
      # pandas/numpy/pyarrow/pyyaml, exactly this job's install line.
      - name: stock-identity atlas guards
        run: python -m pytest tests/test_stock_identity_partition.py tests/test_stock_identity_fingerprint.py tests/test_stock_identity_state_episodes.py tests/test_stock_identity_atlas.py tests/test_audit_reused_tickers.py tests/test_stock_identity_replay.py tests/test_stock_identity_replay_leak.py -q
      - name: promotion-stat DISPLAY gate — the published DSR must be the computed one
        # Registering the trial budget (above) is only half the chain: the number the
        # PAGE shows must equal the number the calibrator produced. The BTC Vector card
        # shipped "DSR 0.9945 (n=68)" while calibration.json carried 0.9661 at n=71 —
        # optimistic against the engine's own output, the one direction a promotion stat
        # must never drift. Skips cleanly when data/vector/*.json is absent.
        run: python -m pytest tests/test_signal_lab_vector_display.py -q

  breakdown-control-seed-determinism:
    if: ${{ false }}
    gate: code
    # BD-4/5/6 control sampling derives a per-ticker RNG sub-seed from the ticker.
    # Until 2026-07-26 that derivation was builtin hash(), which Python salts per
    # process (PYTHONHASHSEED) — control draws differed on EVERY run while the frozen
    # Phase-0/0b preregs promise seeded (reproducible) controls and the surrounding
    # comment claimed "deterministic sub-seed". Same defect class as the chart_render
    # SVG ids (#3785), fixed the same way: bd456_control_seed() now uses zlib.crc32.
    # This lane runs the real derivation in fresh subprocesses under PYTHONHASHSEED
    # 0/1/42 asserting identical seeds, and pins golden values so a silent regime
    # change (mask width, XOR operator, encoding) also fails RED. The subprocess
    # imports scripts.research.dump_breakdown_events wholesale (engine.grading +
    # pandas/numpy/pyarrow closure; pyyaml for the repo conftest), so the install
    # line is byte-identical to the trial-budgets/outcome-spine family and the pack
    # runner can share a venv with any same-pack sibling on that line. Closure
    # verified in a clean venv with exactly this install: 2 passed, plus the full
    # event-tape + AVOID-1 stamper suites below: 122 passed (89+33), zero skips,
    # identical under PYTHONHASHSEED 0/1/42.
    # The second step runs the two breakdown suites that were executed by NO job
    # until 2026-07-27 (every pytest invocation in this repo carries an explicit
    # file list — an unnamed suite is never run). Their fixture seeding was
    # de-flaked first (hash(ticker) → zlib.crc32) so wiring them cannot import
    # the same PYTHONHASHSEED salt this lane exists to prevent.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install minimal deps
        run: pip install pytest pandas numpy pyarrow pyyaml
      - name: BD-4/5/6 control seeds must be PYTHONHASHSEED-stable (crc32, never hash())
        run: python -m pytest tests/test_breakdown_seed_determinism.py -q
      - name: breakdown event tape + BD-AVOID1 stamper suites
        run: python -m pytest tests/test_breakdown_events.py tests/test_bd_avoid1_stamper.py -q

  outcome-spine:
    if: ${{ false }}
    gate: data
    # W4 (audit #13/#19/#23/#41/#42): the outcome spine + partial-pooling closed loop. The
    # badge-passport ratchet (#41) fails RED if a desk brief renders a conviction badge with no
    # provenance passport; the spine/pooling tests prove the four safety properties (sign-safety,
    # trust-region, cold-start, arming) that gate grades moving deterministic weights.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install minimal deps
        run: pip install pytest pandas numpy pyarrow pyyaml
      - name: desk → spine wiring — stock_desk's per-thesis outcome ledger
        # The append-only scored.jsonl engine.spine._DESK_SCORED reads for this desk. Pins the
        # properties a track record cannot: a published verdict is never re-graded from a
        # re-based price history, an unpriceable lean stays retryable (this desk emits
        # `expired` with none of desk_scorer.GRACE_BD's slack), and each row carries `subject`
        # so 45 leans do not collapse to 8 effective events in engine.pooling.arming.
        run: python -m pytest tests/test_stock_desk.py -q
      - name: badge-passport ratchet (no new conviction badge without a passport)
        run: python3 scripts/check_badge_passport.py site
      - name: spine contract + partial-pooling safety tests
        run: python -m pytest tests/test_spine.py tests/test_pooling.py -q
      - name: promotion gate — per-desk empirical null + Calibration Hub verdicts
        # The bar a desk is promoted against. The placebo sweeps must mirror the evaluator
        # each desk was actually graded by (price plane, falsification boundary, exit bar),
        # or the null describes a different endpoint than the one on the page.
        run: >
          python -m pytest
          tests/test_desk_placebo.py tests/test_calibration_hub.py tests/test_thematic_desk.py -q
      - name: desk → spine writers — the scored ledgers the pooled weights are built from
        # The producer side of spine._DESK_SCORED. The tests above pin what the spine does
        # with a scored.jsonl; these pin that the file says what it claims: grading is
        # idempotent (an already-scored id is never re-graded, and emit dedupes by vintage),
        # a thesis whose check_by has not passed stays OPEN instead of being resolved early,
        # and a soft/non-directional call is unscored — never counted as a hit or a miss.
        # ai_desk_scorer is load-bearing for three registry keys: radar_scorer reuses its
        # eval + aggregation verbatim for both radar and narrative_brain.
        #
        # P1 unrun backfill 2026-08-14: tests/test_desk_thesis_ids.py joins this step.
        # It was named by no `run:` step in any workflow, so the THESIS-ID contract —
        # the identity every row above is keyed by, and the thing that decides whether
        # "already scored" and "dedupe by vintage" can mean anything at all — was dark
        # while the scorers that depend on it were covered.  This job owns the subject
        # (engine/ai_desk.py, engine/ai_desk_scorer.py, engine/stock_desk.py,
        # engine/thematic_desk.py all run here).  Appended LAST so a scorer or ledger
        # regression is the first failure a reader sees rather than the id contract
        # failing for a borrowed reason; it rides this existing step because a step is
        # the masking unit (run_ci_pack.py returns on the first non-zero step) while
        # pytest reports every file inside one step.  No dependency widened: probed at
        # RUN time it loads only pandas/numpy/pyarrow/pyyaml — exactly this job's thin
        # install line — and blocking plotly leaves it 12-passed.
        run: >
          python -m pytest
          tests/test_ai_desk.py tests/test_ai_desk_scorer.py
          tests/test_radar_scorer.py tests/test_narrative_brain.py
          tests/test_demand_ledger.py tests/test_policy_intent_desk.py
          tests/test_desk_thesis_ids.py -q

  conviction-profile:
    if: ${{ false }}
    gate: code
    # CURATED SCOPE (2026-08-19, follow-up to #6027). `scope: exclusive` makes
    # the paths below REPLACE inference instead of being unioned under it.
    #
    # #6027 moved #5984's three dossier suites into this job (correctly — the
    # unrun-publish-ops home they had in #6023 is `gate: data`, which ci.yml
    # never plans, so they were wired and still dark). But those suites import
    # scripts/build_ticker_pages.py and scripts/check_stock_dossier_integrity.py,
    # which rglob templates/ and site/, so the inferred FALLBACK tier grew
    # whole-tree `templates/**`, `site/**`, `data/**`, `research/**`, `config/**`
    # and `tools/**` claims. This job began selecting on files no suite in it
    # reads: measured, it newly matched `templates/index.html` and pushed that
    # probe to 128 against the 127 ceiling in
    # tests/test_ci_pack.py::test_exclusive_curation_narrows_ordinary_code_prs.
    # That is smear, and this file's own convention is to curate it away at the
    # source rather than pay for it in the ceiling (same treatment as `inline-js`,
    # `flow-surface`, `options-estate-guards`).
    #
    # Every path below is EARNED — the 59 concrete files inference already owned,
    # plus the two templates build_ticker_pages.py loads by name
    # (`ticker.html.j2`, which tests/test_dossier_identity_end_to_end.py renders
    # directly, and `ticker_index.html.j2`). Nothing whole-tree survives, and
    # scripts/check_ci_trigger_closure.py is the guard that every module these
    # suites read can still start this job.
    scope: exclusive
    paths:
      - "engine/theme_graph/identity.py"
      - "engine/theme_graph/__init__.py"
      - "engine/earnings_release/receipts.py"
      - "engine/earnings_release/filing_key.py"
      - "engine/earnings_release/figures.py"
      - "engine/earnings_release/binding.py"
      - "engine/company_intelligence/views.py"
      # 2026-08-26 widening (PR #6454): #6376 (E3-B AAPL canonical
      # qa_exchange publication) added qa_exchange/qa_reconstruction to this
      # job's suite closure; the coverage audit reds without them.
      - "engine/company_intelligence/qa_exchange.py"
      - "engine/company_intelligence/qa_reconstruction.py"
      - "engine/company_intelligence/issuer_profiles.py"
      - "engine/company_intelligence/identity.py"
      - "engine/company_intelligence/health.py"
      - "engine/company_intelligence/events.py"
      - "engine/company_intelligence/event_workspace.py"
      - "engine/company_intelligence/qa_exchange.py"
      - "engine/company_intelligence/qa_reconstruction.py"
      - "engine/company_intelligence/event_id_adapter.py"
      - "engine/company_intelligence/documents.py"
      - "engine/company_intelligence/contracts.py"
      - "engine/company_intelligence/__init__.py"
      - "engine/press/earnings_adapter.py"
      - "engine/fundamental_forensics/models.py"
      - "engine/fundamental_forensics/disclosure_diff.py"
      - "engine/earnings_narrative/story_packets.py"
      - "engine/earnings_narrative/story.py"
      - "engine/earnings_narrative/public_wire.py"
      - "engine/earnings_narrative/promotion.py"
      - "engine/earnings_narrative/digest.py"
      - "engine/earnings_narrative/contracts.py"
      - "config/theme_graph_identity_breaks.yml"
      - "config/fundamental_forensics_disclosure_diff.v1.json"
      - "config/earnings_story_promotion.yml"
      # B1A wiring 2026-08-24: this gate:code job owns the dossier contract and now runs the security_state.v1 suite (contract-delta named it unrun).
      - "contracts/market_os/security_state.v1.schema.json"
      - "engine/security_state.py"
      - "lib/evidence_foundation.py"
      - "lib/dataos/identity.py"
      - "contracts/evidence_foundation/vocabulary.v1.json"
      - "contracts/evidence_foundation/reference.v1.schema.json"
      - "contracts/evidence_foundation/block.v1.schema.json"
      - "contracts/evidence_foundation/recipe.v1.schema.json"
      - "tests/test_security_state_contract.py"
      - "tests/test_security_state_view_model.py"
      - "config/site_access.yml"
      - "config/sparse_worktree.json"
      - "engine/__init__.py"
      - "engine/canon.py"
      - "engine/conditions.py"
      - "engine/cycles.py"
      - "engine/i18n.py"
      - "engine/indicators.py"
      - "engine/indicators_m2.py"
      - "engine/market_heatmap.py"
      - "engine/marketing/__init__.py"
      - "engine/marketing/accounts.py"
      - "engine/marketing/authority.py"
      - "engine/marketing/campaign_compiler.py"
      - "engine/marketing/chart_render.py"
      - "engine/marketing/charter.py"
      - "engine/marketing/claims.py"
      - "engine/marketing/cmo.py"
      - "engine/marketing/departments.py"
      - "engine/marketing/economics.py"
      - "engine/marketing/events.py"
      - "engine/marketing/ledgers.py"
      - "engine/marketing/logo_cache.py"
      - "engine/marketing/market_clock.py"
      - "engine/marketing/movers_source.py"
      - "engine/marketing/opportunity_bus.py"
      - "engine/marketing/publication.py"
      - "engine/marketing/share_cards.py"
      - "engine/marketing/source_authority.py"
      - "engine/marketing/state.py"
      - "engine/session_anchor.py"
      - "engine/sp500_heatmap.py"
      - "engine/stock_dossier.py"
      - "engine/stock_score.py"
      - "engine/stock_view.py"
      - "engine/stocks_hub.py"
      - "engine/technicals.py"
      - "engine/valuation.py"
      - "engine/vol_forecast.py"
      - "engine/vol_regime.py"
      - "lib/__init__.py"
      - "lib/config.py"
      - "lib/numeric.py"
      - "lib/nyse_calendar.py"
      - "lib/pages.py"
      - "lib/procutil.py"
      - "lib/seo.py"
      - "lib/store.py"
      - "scripts/build_ticker_pages.py"
      - "scripts/check_stock_dossier_integrity.py"
      - "scripts/sparse_guard.py"
      - "scripts/worktree_sparse.py"
      - "templates/china.html.j2"
      - "templates/ticker.html.j2"
      - "templates/ticker_index.html.j2"
      - "tests/test_analyzer_i18n_percentile.py"
      - "tests/test_check_stock_dossier_integrity.py"
      - "tests/test_dossier_identity_end_to_end.py"
      - "tests/test_dossier_numeric_contract.py"
      - "tests/test_stock_dossier.py"
      - "tests/test_stock_score.py"
    # Wired 2026-08-02: neither suite was named by ANY run step, so #4297's caution
    # rewrite ran 4 stock_score pins red for a day with no CI signal, and the dossier's
    # prose-matched risk_veto died silently. test_stock_score pins the honesty
    # invariants of engine/stock_score.py (no Buy against the tape, HK never buys,
    # parabolic penalised, missing legs recorded); test_stock_dossier pins the
    # no_buy_reasons contract including the structural risk.veto flag.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install minimal deps
        # jinja2 + pyyaml added 2026-08-19 with the analyzer-i18n step below, and BOTH are
        # needed at COLLECTION time, not merely at run time:
        #   jinja2 — that suite renders the REAL china.html.j2 turnover row (macro + row
        #     extracted from the shipped file, so it cannot silently drift from a hand-copied
        #     duplicate) through a bare jinja2 Environment.
        #   pyyaml — `from engine import cycles` pulls engine/technicals.py -> lib/config.py,
        #     which does a module-scope `import yaml`. The two older suites in this job never
        #     import engine.cycles, so this job's line had never needed it. Missing it is a
        #     COLLECTION error (pytest exit 2), which fails the whole step rather than one
        #     test — measured on this PR's first run (ci-pack-11, job 96210580631).
        run: pip install pytest pandas numpy markupsafe jinja2 pyyaml jsonschema
      - name: conviction profile honesty invariants + dossier reason codes
        run: python -m pytest tests/test_stock_score.py tests/test_stock_dossier.py -q
      # P1 unrun backfill 2026-08-19: tests/test_analyzer_i18n_percentile.py (#5980) was named
      # by no `run:` step in any workflow, so scripts/audit_unrun_tests.py reds the whole
      # fleet's full-manifest runs on it. Same defect class as #5952 / #5985, discovered while
      # landing the latter. THIS JOB OWNS THE SUBJECT: #5980's fix is the analyzer display-twin
      # contract across engine/stock_score.py (the new bucket_display twin) and
      # engine/stock_view.py (state_label_zh on BOTH timing paths, including the no-conviction
      # fallback that would have kept the bug if only the main branch were fixed) — the two
      # modules this job already exercises via test_stock_score/test_stock_dossier — plus
      # engine/cycles.py's ladder_state() propagating STATE_DISPLAY's label_zh and
      # engine/i18n.py's t_pctile() helper + the t() nesting guard. The defect it pins is
      # invisible at runtime: `B(t.state_label, t.state_label)` renders perfectly in both
      # languages, it just renders ENGLISH in Chinese mode, so nothing raises when it
      # regresses. Appended LAST and as its OWN step (not folded into the step above) because
      # a step is the masking unit — run_ci_pack.py returns on the first non-zero step — and
      # the conviction/dossier honesty invariants are the older, higher-signal failure a
      # reader should see first.
      - name: analyzer display twins + china turnover percentile i18n (#5980)
        run: python -m pytest tests/test_analyzer_i18n_percentile.py -q
      # Unrun backfill 2026-08-19 (audit_unrun_tests.py, #5952-class): #5984 — the
      # "$nanM" dossier identity/truth fix — landed three suites named by ZERO
      # run: steps, so they collected and never ran. Dark tests are worse than no
      # tests, because they read as coverage.
      #
      # THIS JOB, not the ticker-page render lane in unrun-publish-ops that owns
      # the same subject, for the reason that decides every one of these
      # placements: unrun-publish-ops is `gate: data`, and ci.yml plans
      # `--gate code` — measured on this PR's own run 32302003786, which put 121
      # jobs in scope and never mentioned unrun-publish-ops once. Wiring a
      # hermetic suite there satisfies audit_unrun_tests' letter (it asks only
      # whether SOME run: step names the file) while leaving it dark on every PR,
      # which is the darkness this backfill exists to end. conviction-profile is
      # `gate: code` and already owns the dossier contract via test_stock_dossier.
      #
      # All three are hermetic — tmp_path fixtures, no data/ and no site/ read —
      # and need nothing beyond this job's existing install line: 61 passed /
      # 0 skipped in a clean venv holding exactly `pytest pandas numpy markupsafe
      # jinja2 pyyaml`. test_dossier_numeric_contract pins lib.numeric.finite,
      # the contract whose absence let a truthy float NaN render "$nanM" on 69 of
      # 2066 published pages; test_check_stock_dossier_integrity pins the
      # post-render estate guard that catches that leak; and
      # test_dossier_identity_end_to_end drives the REAL context builder and the
      # REAL template and asserts on the rendered HTML — the outcome a reader
      # sees, which the unit suites only approach one mechanism at a time.
      - name: dossier numeric contract, estate machine-sentinel guard, and identity end-to-end (#5984)
        run: >
          python -m pytest
          tests/test_dossier_numeric_contract.py
          tests/test_check_stock_dossier_integrity.py
          tests/test_dossier_identity_end_to_end.py -q
      # Market OS B1A wiring 2026-08-24 (contract-delta named the suite unrun):
      # tests/test_security_state_contract.py pins the security_state.v1 contract —
      # the R1-R9 identity receipt chain (refusal-first), the 14 commission failure
      # cases, mutation kills, and the K1 Ref/Block/Recipe receipts. Hermetic:
      # fixture dicts only, no data/ and no site/ read. Appended LAST as its OWN
      # step per this job's precedent — a step is the masking unit.
      - name: security_state.v1 contract + identity receipt chain (B1A)
        run: python -m pytest tests/test_security_state_contract.py -q
      # B1A Sol-repair round 2026-08-24: tests/test_security_state_view_model.py
      # pins the dossier projection (build_security_state) to the frozen contract —
      # golden compiled through engine.security_state, receipts surviving into the
      # view model, contract-vs-blob discrimination, last_good alignment. Hermetic:
      # fixture dicts only. Own step per this job's precedent (step = masking unit).
      - name: security_state.v1 view-model projection (B1A)
        run: python -m pytest tests/test_security_state_view_model.py -q

  chronicle-suite:
    if: ${{ false }}
    gate: data
    # Chronicle W0 (agentic_media program, CHRONICLE_CONTEXT_TIMELINE_MASTERPLAN_BY_FABLE.md):
    # deterministic event spine + rollups + context_pack over 6 sources. No paths filter narrows
    # this beyond the shared top-level trigger above (engine/chronicle/**, scripts/build_chronicle.py,
    # data/chronicle/**, tests/test_chronicle.py, admin/chronicle.py all listed there; config/synapse.yml,
    # config/dag.yml, config/lobe_charters.yml, docs/SIGNAL_BUS.md, engine/neuralweb/** already covered
    # by pre-existing entries).
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install minimal deps
        run: pip install pytest pandas numpy pyarrow pyyaml
      - name: chronicle engine tests
        run: python -m pytest tests/test_chronicle.py -q

  cycle-ontology-js:
    if: ${{ false }}
    gate: code
    # W1.5 (A10/A12 ruling): the generated site/cycle_ontology.js must exactly match what
    # engine/cycle_ontology.py produces at HEAD.  A PR that edits the engine but forgets to
    # regenerate the JS — or hand-edits the committed JS — fails RED here.  The flag-day
    # axis-flip (ONTOLOGY_AXIS constant) makes a stale committed JS especially dangerous: it
    # could ship a half-v2 site.  Runs only when relevant files change (paths filter above).
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install minimal deps
        # cycle_ontology.py imports numpy+pandas at module top (canonical_position math),
        # so gen_ontology_js — which imports it — needs them even though it only serialises
        # the vocabulary payload. pyyaml alone made this gate crash-fail whenever triggered.
        run: pip install pyyaml numpy pandas
      - name: ontology JS must not drift from engine (gen_ontology_js --check)
        run: python3 -m scripts.gen_ontology_js --check

  cycle-forward-log:
    if: ${{ false }}
    gate: code
    # W1.5 (W0.2 acceptance): four acceptance criteria for the shared forward-log writer.
    # A regression here silently breaks prospective cone-coverage grading for every cycle
    # family (US sectors + country cycles).  Hard gate: the keep-FIRST invariant and
    # proj_lo/hi columns are structural contracts for W2.3+ measurement.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install minimal deps
        run: pip install pytest pandas numpy pyarrow pyyaml
      - name: forward-log writer contract tests (keep-FIRST, cone edges, never-raise)
        run: python -m pytest tests/test_cycle_forward_log.py -q

  tier-gate:
    if: ${{ false }}
    gate: data
    # Revenue + serving boundary. A paid desk ships as a free preview shell at the
    # page URL plus a separate /premiumdata/ payload that app/paywall.py gates as
    # Insider+ ahead of the site-wide PAYWALL_ENABLED switch
    # (docs/TIER_PREVIEW_PATTERN.md). The regressions this catches: the shell
    # quietly regaining the paid rows (the split IS the gate — a client-side hide
    # is not), /premiumdata/ dropping out of premium.enforced_early (which would
    # re-open the desk to every registered account), the edge's public exclusion
    # list drifting away from config/site_access.yml (which is how a generated
    # artifact silently becomes anonymously fetchable — #3418 drifted it and the
    # boundary test sat red ~17h unwatched because no workflow named it), and the
    # registration wall itself regressing.
    #
    # The serving boundary has three enforcement halves and CI now covers all of
    # them: the Caddy public list (site_access_boundary), the paid stage
    # (paywall/api_paywall), and the anonymous stage (regwall). The regwall pair
    # was the last one running in no workflow — and it is the expensive one to get
    # wrong in the quiet direction: #3387 had to reopen /stocks/ /tools/ /learn/
    # /blog/ after a wall with no crawler exception 302'd Googlebot off 1,665
    # sitemap URLs. Hermetic — no network, no engine data.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install minimal deps
        # app.main mounts app.market_memory, whose authenticated stock-data
        # reader imports requests at module scope.  Keep the serving-boundary
        # lane's isolated environment closed over that production import.
        # ETF board synthesis imports the rotation constants from etf_pulse,
        # which imports numpy and pandas at module scope.  Keep this environment
        # closed over the real production import graph.
        run: pip install pytest pyyaml jinja2 fastapi httpx requests numpy pandas
      - name: tier-preview split contract (no paid row in the free shell)
        run: python -m pytest tests/test_special_situations_gate.py tests/test_china_special_situations_gate.py tests/test_etfs_gate.py tests/test_us_board_gate.py -q
      # The split's OTHER half: how the page puts the two sides back together.
      # test_us_board_gate above proves the shell withholds the paid rows; this
      # proves hydration re-joins them into ONE board — a stage headed on both
      # sides of the wall must not draw its heading twice, and a withheld group
      # must land at its own rank rather than after every shell group (the
      # reported "LIVE NOW twice, over different names", #6040).
      #
      # HERE and not with the board-UI suites in `unrun-picks-boards`: that job
      # is `scope: exclusive`, so a suite reaching scripts.build_site would drag
      # build_site's whole engine import closure into its declared paths (114
      # uncovered, measured 2026-08-20). This suite is jinja2-only by design and
      # sits with the tier-gate family whose helpers it imports. It needs NO new
      # package on the install line above — pytest + jinja2, both already there —
      # and node for tests/us_board_hydrate_harness.js, which runs the SHIPPED
      # merge against a stub DOM and is preinstalled on ubuntu-latest (the node
      # tests skip cleanly if it ever is not).
      - name: tier-preview hydration merges the two halves into one board
        run: python -m pytest tests/test_us_board_hydration_merge.py -q
      # The SAME Act-Now board, on its second host. test_us_board_gate above pins
      # the us_stocks split; this pins sector_central.html, which rendered the
      # identical include ungated (45 `.actitem` rows, anonymous, 2026-08-17)
      # until #5846's gate made those rows one click away instead of gone.
      # Runs HERE and only here on purpose: scripts/build_sector_central.py keeps
      # every engine import inside a function, so unlike test_us_board_gate this
      # suite needs no pandas/plotly pairing and nothing in it can skip on this
      # job's venv line (the suite's own test_import_stays_light pins that).
      - name: tier-preview split contract (sector_central act-now board)
        run: python -m pytest tests/test_sector_central_gate.py -q
      - name: public dashboard preview-tier contracts (anon shell allowlist + controller caps)
        # Found unwired by the 2026-08-02 adversarial review of #4331: the suite
        # pins the public/anon split for the dashboard shells (site_access.yml
        # allowlist, tier_preview.js 1/3/full caps, gate wiring) and ran in NO
        # workflow. tests/test_site_access_boundary.py derives this job's inputs
        # into ci.yml's paths filter, so this line also makes the suite reach.
        run: python -m pytest tests/test_public_dashboard_preview.py -q
      - name: paywall fail-closed contract (incl. enforced-early payload lane)
        run: python -m pytest tests/test_paywall.py tests/test_api_paywall.py tests/test_auth_cache_outage.py -q
      - name: Pro-only host gate (bot.mastermind-x.com -> /api/paywall/check_pro)
        run: python -m pytest tests/test_paywall_pro_gate.py -q
      - name: static serving boundary (Caddy <-> site_access.yml, no leaked artifacts)
        run: python -m pytest tests/test_site_access_boundary.py -q
      - name: delivery-plane census (exact families, alternate origins, consumers, defaults)
        run: python -m pytest tests/test_r2_delivery_plane_classification.py -q
      # tests/test_regwall_json_gate.py used to run HERE and no longer does: it
      # is the Caddyfile <-> config/site_access.yml cross-check, and this job is
      # `gate: data`, which ci.yml never packs — so the access boundary was
      # advisory on every PR that could breach it. It now has its own
      # `gate: code` job (`regwall-boundary`, immediately below). What stays
      # here is the anonymous-stage endpoint contract, which needs this job's
      # fastapi/httpx line.
      - name: registration wall (anonymous stage)
        run: python -m pytest tests/test_regwall.py -q
      # #4409 shipped the member Earnings Wire API suite named only in
      # unrun-publish-ops, whose 21-job venv line has no fastapi/httpx — so the
      # auth-gate/private-header tests skipped in every job that named them
      # (check_skip_only_suites, same class as the boto3/publish-r2 case above).
      # This job's install line already provides both; verified in a clean venv
      # with exactly `pytest pyyaml jinja2 fastapi httpx`: 10 passed.
      - name: member Earnings Wire API transport (auth gate + private headers)
        run: python -m pytest tests/test_earnings_api.py -q

  regwall-boundary:
    if: ${{ false }}
    gate: code
    # THE ACCESS BOUNDARY, ON THE MERGE GATE (2026-08-20).
    #
    # tests/test_regwall_json_gate.py cross-checks the edge's own public
    # matchers in app/deploy/Caddyfile against the reviewed policy in
    # config/site_access.yml: @public_static / @public_versioned /
    # @factordata_public may claim only paths that policy calls public, the
    # broad @reg_asset matcher must keep protecting the paid JSON, its handler
    # must run BOTH auth stages and stay `private, no-store`, its error branch
    # must fail closed, and the pages-open/payloads-gated split must hold.
    #
    # It ran only in `tier-gate` above, which is `gate: data`. ci.yml packs
    # `--gate code`, so this suite could go red and merge-on-green would still
    # merge the PR that broke it. That is not theoretical: on the A1A closure
    # branch (#6109 lineage, 2026-08-20) /portfolio_state.js was added to the
    # Caddyfile allowlist with no matching config/site_access.yml entry, this
    # guard went red, and nothing on the merge gate objected — an adversarial
    # review caught it. An access boundary that only advises is not a boundary.
    #
    # CURATED SCOPE, and the declaration is a CORRECTNESS fix, not a narrowing.
    # The suite reaches its subjects as SEGMENT literals — REPO_ROOT / "app" /
    # "deploy" / "Caddyfile" — and no segment contains a `/`, so scope
    # inference's path regex never sees them. Inferred, this job would own only
    # the test file and would NOT re-run on the Caddyfile edit that is the
    # entire regression class. Every path below is a file the suite actually
    # opens: the two policy halves, plus the five public documents
    # test_public_pages_fetch_nothing_under_paid_prefixes reads to prove no
    # anonymous page fetches under /factordata or /labdata.
    #
    # `scope: exclusive` (with the job id registered in CURATED_EXCLUSIVE in
    # tests/test_ci_pack.py) is what makes these paths REPLACE inference rather
    # than be unioned under a whole-tree fallback tier.
    #
    # Hermetic: reads files, starts no app, imports no engine module. pytest +
    # pyyaml is the whole dependency line.
    scope: exclusive
    paths:
      - "app/deploy/Caddyfile"
      - "config/site_access.yml"
      - "templates/index.html"
      - "templates/live.js"
      - "templates/onboard.js"
      - "templates/plans.html.j2"
      - "templates/theme.js"
      - "tests/test_regwall_json_gate.py"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install minimal deps
        run: pip install pytest pyyaml
      - name: access boundary (Caddyfile public matchers <-> site_access.yml policy)
        run: python -m pytest tests/test_regwall_json_gate.py -q

  lens-tap-boundary:
    if: ${{ false }}
    gate: code
    # theme.js's lens opens on `focusin`, and in sheet mode show() mounts a
    # full-viewport .lens-scrim. When the focused element is a control NESTED
    # inside the tip wrapper, that scrim lands between mousedown and mouseup, the
    # browser retargets the click to <body>, and the control's own handler never
    # runs — measured on 29 live instances across /us_stocks, /canada and /bonds
    # (PR #6262). The lens's CLICK handler has always carved nested controls out,
    # but that carve-out is unreachable here because no click survives the scrim,
    # so the invariant has to be pinned on the focusin path itself.
    #
    # Owns the SUBJECT, not just the file: `templates/theme.js` is where the lens
    # lives and `site/theme.js` is the baked copy browsers actually load, so a fix
    # that lands in one and not the other must start this job. Deliberately NOT
    # `scope: exclusive` — this suite reads shipped source text and asserts on it,
    # and there is no curated import closure to keep in step.
    #
    # Hermetic: reads two files and regexes them. pytest is the whole dependency
    # line — no jinja, no engine import, no network.
    paths:
      - "templates/theme.js"
      - "site/theme.js"
      - "tests/test_lens_nested_control_taps.py"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install minimal deps
        run: pip install pytest
      - name: lens must not steal a tap from a control nested in a tip wrapper
        run: python -m pytest tests/test_lens_nested_control_taps.py -q

  cycles-payload-split:
    if: ${{ false }}
    gate: code
    # Perf: split_cycles_payload peels the heavy per-entity arrays (price/osc/turns/fx/
    # usd_record) off the cycle-map payload into a lazy-hydrated series chunk. A regression
    # here (wrong family stripped, lossy split, or input mutation) would corrupt every cycle
    # page's data or the full features json. Pure-Python, no engine deps.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install minimal deps
        run: pip install pytest
      - name: cycles payload split contract tests (lossless recombine, no-mutate, family rules)
        run: python -m pytest tests/test_cycles_payload_split.py -q

  flow-surface:
    if: ${{ false }}
    gate: data
    # CURATED SCOPE (2026-08-14 incident follow-up). `scope: exclusive` makes
    # the paths below REPLACE inference instead of being unioned under it, so
    # the opaque fallback tier this job used to carry — whole-tree `site/**`,
    # `templates/**`, `docs/**`, `research/**` claims smeared out of a
    # subprocess or traversal deep in its closure — stops selecting it. Every
    # pattern here was generated from the job's OWN import closure and audited
    # for zero misses: no file this job demonstrably reads lost its owner.
    # 267s across 19 steps. The options/flow surface genuinely reaches
    # contracts/options, research/options_estate and ops/launchd — all
    # declared — while site/templates were never anything but fallback smear.
    # Top-level `engine/*` is enumerated (2026-08-15 follow-up to #5685):
    # 846 tracked / 127 concrete. Subject packages stay globbed.
    scope: exclusive
    paths:
      - "engine/theme_graph/identity.py"
      - "engine/theme_graph/__init__.py"
      - "engine/security_state.py"
      - "engine/quote_resolution.py"
      # D5's app/prophet_lab route imports the canonical B1 episode reader and
      # issuer identity chain. This curated job reaches that app router, so its
      # exclusive declaration must cover the exact transitive closure rather
      # than silently skipping changes to these dependencies.
      - "engine/path_risk_signals.py"
      - "engine/stock_identity/__init__.py"
      - "engine/stock_identity/authority.py"
      - "engine/stock_identity/fingerprint.py"
      - "engine/stock_identity/plane.py"
      - "engine/us_candidate_episode.py"
      # W1-B wires Brain's native-fact serve to the frozen W1-A runtime. This
      # job's own app/neuralweb import closure now reaches those packages, so
      # its curated scope must retain their real dependency ownership.
      - "engine/intelligence_workspace/**"
      - "engine/theme_graph/**"
      - "lib/dataos/**"
      - "app/*"
      - "collectors/*"
      - "collectors/biocatalyst/**"
      - "config/**"
      - "contracts/**"
      - "data/**"
      - "docs/runbooks/OPTIONS_SPARSE_SELECTOR.md"
      - "engine/__init__.py"
      - "engine/_text_scrub.py"
      - "engine/ai_desk.py"
      - "engine/ai_desk_scorer.py"
      - "engine/altdata_ledger.py"
      - "engine/altdata_models.py"
      - "engine/altdata_picks.py"
      - "engine/altdata_signals.py"
      - "engine/baskets.py"
      - "engine/baskets_canada.py"
      - "engine/baskets_china.py"
      - "engine/baskets_hk.py"
      - "engine/baskets_region.py"
      - "engine/canon.py"
      - "engine/catalyst_tone.py"
      - "engine/chain_snapshot_completion.py"
      - "engine/chain_snapshot_evidence.py"
      - "engine/china_altdata.py"
      - "engine/china_basket_spine.py"
      - "engine/china_conviction.py"
      - "engine/china_crowding.py"
      - "engine/china_discovery.py"
      - "engine/china_event_calendar.py"
      - "engine/china_extras.py"
      - "engine/china_intel_bus.py"
      - "engine/china_news.py"
      - "engine/china_news_intel.py"
      - "engine/china_radar.py"
      - "engine/china_radar_ledger.py"
      - "engine/china_regime_store.py"
      - "engine/china_signal_lab.py"
      - "engine/china_standout_track.py"
      - "engine/china_validation.py"
      - "engine/cn_newswires.py"
      - "engine/codex_provider.py"
      - "engine/conditions.py"
      - "engine/confluence_tiers.py"
      - "engine/cross_asset.py"
      - "engine/cycles.py"
      - "engine/desk_ledger.py"
      - "engine/desk_scorer.py"
      - "engine/earnings_qual.py"
      - "engine/earnings_transcript_intake.py"
      - "engine/entity_resolver.py"
      - "engine/entry_signal.py"
      - "engine/equity_factors.py"
      - "engine/event_calendar.py"
      - "engine/flow_enrich.py"
      - "engine/flow_signing.py"
      - "engine/forward_calendar_context.py"
      - "engine/gdelt_client.py"
      - "engine/gex_engine.py"
      - "engine/grading.py"
      - "engine/greeks.py"
      - "engine/group_flow.py"
      - "engine/hysteresis.py"
      - "engine/i18n.py"
      - "engine/indicators.py"
      - "engine/indicators_m2.py"
      - "engine/intraday_greeks.py"
      - "engine/ledger_lane.py"
      - "engine/live_flow.py"
      - "engine/live_quotes.py"
      - "engine/llm_auth.py"
      - "engine/macro_surprise.py"
      - "engine/manager_lag.py"
      - "engine/manager_quality.py"
      - "engine/manager_trades.py"
      - "engine/master_brain.py"
      - "engine/name_resolver.py"
      - "engine/name_score_grader.py"
      - "engine/news_common.py"
      - "engine/news_translate.py"
      - "engine/ollama_provider.py"
      - "engine/opex.py"
      - "engine/options_episode_coverage.py"
      - "engine/options_focused_quote.py"
      # AD-1T1: topup_thetadata_day._source_coverage_gate() imports
      # CONFIG["SOURCE_COVERAGE_GATE"] from here rather than a second literal.
      - "engine/options_intel_brief.py"
      - "engine/options_issue_desk.py"
      - "engine/options_ivspread.py"
      - "engine/options_market_memory_context.py"
      - "engine/options_market_memory_local_receipts.py"
      - "engine/options_market_memory_local_replica.py"
      - "engine/options_market_memory_receipt_store.py"
      - "engine/options_matrix.py"
      - "engine/options_signal_campaign.py"
      - "engine/options_signal_episode.py"
      - "engine/options_signal_episode_contract.py"
      - "engine/options_skew.py"
      - "engine/options_sparse_selector.py"
      - "engine/options_structure.py"
      - "engine/options_structure_intraday.py"
      - "engine/options_universe.py"
      - "engine/passport.py"
      - "engine/pooling.py"
      - "engine/portfolio_brief.py"
      - "engine/portfolio_changes.py"
      - "engine/portfolio_vocab.py"
      - "engine/private_auth_dict.py"
      - "engine/prophet_bridge.py"
      - "engine/prophet_integrity.py"
      - "engine/prophet_stage_inputs.py"
      - "engine/provider_health.py"
      - "engine/qbus.py"
      - "engine/qkernel.py"
      - "engine/qledger.py"
      - "engine/qledger_validity.py"
      - "engine/rebalance_calendar.py"
      - "engine/regime_label.py"
      - "engine/regime_vector.py"
      - "engine/residual_alpha.py"
      - "engine/session_anchor.py"
      - "engine/session_digest.py"
      - "engine/signal_quality.py"
      - "engine/smart_money.py"
      - "engine/spine.py"
      - "engine/spotlight.py"
      - "engine/sue.py"
      - "engine/tape_flow.py"
      - "engine/technicals.py"
      - "engine/thetadata_store.py"
      - "engine/ticker_shape.py"
      - "engine/translate.py"
      - "engine/trial_ledger.py"
      - "engine/tushare_freshness.py"
      - "engine/universe_history.py"
      - "engine/us_early_turn.py"
      - "engine/validation.py"
      - "engine/vol_forecast.py"
      - "engine/vol_regime.py"
      - "engine/weinstein_stage.py"
      - "engine/biocatalyst/**"
      - "engine/capital_structure/**"
      - "engine/chronicle/**"
      - "engine/codex_lane/**"
      - "engine/company_intelligence/**"
      - "engine/earnings_release/binding.py"
      - "engine/earnings_release/figures.py"
      - "engine/earnings_release/filing_key.py"
      - "engine/earnings_release/receipts.py"
      - "engine/context_index/**"
      - "engine/earnings_narrative/**"
      - "engine/fundamental_forensics/**"
      - "engine/government_revenue/**"
      - "engine/marketing/**"
      - "engine/metabolism/**"
      - "engine/entry_radar/__init__.py"
      - "engine/entry_radar/contracts.py"
      - "engine/entry_radar/spool.py"
      # 2026-08-19 LAB-0 §6 commissioning-prep heal: engine/prophet_lab/sources.py
      # (below) now imports engine.entry_radar.spool (the R2-first read seam,
      # mirrors the Radar spool WRITER's own backend ladder) -- never the
      # detector/scoring stack (live_ledger/challengers/detectors), which stays
      # off limits and off this job's closure. spool.py itself only imports
      # entry_radar/contracts.py (stdlib-only) and entry_radar/__init__.py, so
      # exactly these three files, never engine/entry_radar/** broadly.
      - "engine/neuralweb/**"
      - "engine/press/**"
      - "engine/prophet_board_read.py"
      # 2026-08-19 ci-pack-10 heal: this job's inferred (non-exclusive) closure
      # newly reaches these engine/*.py files on the P-LAB-API branch. Verified
      # via a real `git clone --local --depth 1` baseline (not a `git archive`
      # extraction, which has no `.git` and silently under-reports
      # discover_suites()'s tracked-file census) that origin/main shows ZERO
      # uncovered for this SAME job -- so this is a genuine new reachability
      # edge from this branch's diff, not stale pre-existing drift, though the
      # exact causal chain (none of these files are imported by
      # engine/prophet_lab/** or engine/prophet_board_read.py, both already
      # declared above) was not fully isolated within this PR's budget.
      # Widening here per the guard's own remedy ("widening is always the safe
      # direction") rather than leaving the pack red.
      - "engine/oracle/**"
      - "engine/pick_lab/**"
      - "engine/prophet_live/**"
      - "engine/activist.py"
      - "engine/analyst_revisions.py"
      - "engine/anticipation.py"
      - "engine/basket_breadth_divergence.py"
      - "engine/basket_index.py"
      - "engine/basket_mtf.py"
      - "engine/basket_score.py"
      - "engine/basket_tape.py"
      - "engine/baskets_intl.py"
      - "engine/beneficial_ownership.py"
      - "engine/btc_mtf.py"
      - "engine/capital_allocation.py"
      - "engine/coiled.py"
      - "engine/composite_score.py"
      - "engine/conviction_accrual.py"
      - "engine/conviction_delta.py"
      - "engine/cross_asset_trend.py"
      - "engine/crowding.py"
      - "engine/dannytrades.py"
      - "engine/dannytrades_chip.py"
      - "engine/demand_chain.py"
      - "engine/dispersion.py"
      - "engine/donor.py"
      - "engine/earnings_blackout.py"
      - "engine/earnings_catalyst.py"
      - "engine/entry_primitives.py"
      - "engine/event_atlas.py"
      - "engine/event_landmine.py"
      - "engine/expectation_state.py"
      - "engine/extension.py"
      - "engine/forward_dist.py"
      - "engine/gex_confirm.py"
      - "engine/gov_exposure.py"
      - "engine/hold.py"
      - "engine/htf_durability.py"
      - "engine/ignition_features.py"
      - "engine/index_direction.py"
      - "engine/inputs.py"
      - "engine/ledger_identity.py"
      - "engine/liquidity_chip.py"
      - "engine/long_hold_clocks.py"
      - "engine/moat_falsifiers.py"
      - "engine/mtf_upturn.py"
      - "engine/name_direction.py"
      - "engine/name_score.py"
      - "engine/narrative_rotation.py"
      - "engine/path_personality.py"
      - "engine/pit.py"
      - "engine/pit_lag_recorder.py"
      - "engine/playbook.py"
      - "engine/postcross.py"
      - "engine/prophet_doors.py"
      - "engine/provenance_sidecar.py"
      - "engine/pullback_zone.py"
      - "engine/rate_inflation_transmission.py"
      - "engine/yield_momentum.py"
      - "engine/risk_sizing.py"
      - "engine/roc_blowoff.py"
      - "engine/sector_pulse.py"
      - "engine/sector_rate_inflation.py"
      - "engine/setup_tier.py"
      - "engine/setups.py"
      - "engine/shadow_book.py"
      - "engine/short_volume.py"
      - "engine/signal_archive.py"
      - "engine/signal_gate.py"
      - "engine/special_arb.py"
      - "engine/special_situations.py"
      - "engine/stock_dossier.py"
      - "engine/stock_events.py"
      - "engine/stock_fundamentals.py"
      - "engine/stock_macro_sensitivity.py"
      - "engine/stock_personality.py"
      - "engine/stock_score.py"
      - "engine/stock_technicals.py"
      - "engine/stock_view.py"
      - "engine/t0_indicator.py"
      - "engine/theme_crowding.py"
      - "engine/theme_scoring.py"
      - "engine/theme_validation.py"
      - "engine/thesis_funnel.py"
      - "engine/ticker_alerts.py"
      - "engine/track_record.py"
      - "engine/us_board_rank.py"
      - "engine/us_candidate_lanes.py"
      - "engine/us_context_vector.py"
      - "engine/us_prophet_fusion.py"
      - "engine/valuation.py"
      - "engine/velocity.py"
      - "engine/vol_squeeze.py"
      - "engine/washout_turn.py"
      - "research/SIGNAL_EPISODE_ATLAS_MASTERPLAN_BY_FABLE.md"
      - "research/STOCK_PERSONALITY_MASTERPLAN_BY_FABLE.md"
      - "research/US_BOARD_MEASUREMENT.md"
      - "scripts/build_stock_library.py"
      # Import-closure entry: build_stock_library reuses
      # scripts/snapshot_shadow_book.rows_from_board for the shadow-book
      # snapshot (one percentile definition), so exclusive scope must name it.
      - "scripts/snapshot_shadow_book.py"
      - "scripts/check_board_contradictions.py"
      - "scripts/check_signal_gate_coherence.py"
      - "scripts/check_stretch_oracle_contract.py"
      - "scripts/thematic_rotation_phase0.py"
      - "engine/prophet_lab/**"
      - "engine/research_vault/**"
      - "engine/seasonality/**"
      - "engine/sector_intelligence/**"
      - "lib/*"
      - "lib/dataos/identity.py"
      - "ops/CHAIN_SNAPSHOTS_RUNBOOK.md"
      - "ops/LIVE_FLOW_RUNBOOK.md"
      - "ops/launchd/com.mastermind.chainsnapshots.plist"
      - "ops/launchd/com.mastermind.liveflow.plist"
      - "ops/launchd/com.mastermind.optionssparseselector.plist"
      - "ops/launchd/com.mastermind.prophetmarks.plist"
      - "ops/launchd/run_with_env.sh"
      - "ops/launchd/run_options_sparse_selector_loop.sh"
      - "ops/launchd/run_options_sparse_selector_verified.py"
      - "ops/launchd/run_prophet_marks_loop.sh"
      - "research/DO_NOT_REBUILD.md"
      - "research/momoedge/completion_benchmark_prereg_v1.json"
      - "research/momoedge/history_replay_wave0_receipt.json"
      - "research/options_estate/sparse_selector_preregistration_receipt_v1.json"
      - "scripts/__init__.py"
      - "scripts/audit_options_episode_outcome_coverage.py"
      - "scripts/audit_options_market_memory_context.py"
      # AD-1T1: the T1 store's historical backfill driver — RF3/RF4 add a
      # writer-lock/store-agreement check exercised by test_topup_thetadata_daily.py.
      - "scripts/backfill_thetadata_eod.py"
      - "scripts/build_chain_heat.py"
      - "scripts/build_flow_archive.py"
      - "scripts/build_flow_enrich.py"
      - "scripts/build_flow_surface.py"
      - "scripts/build_options_focused_quote.py"
      - "scripts/build_options_issue_desk.py"
      - "scripts/build_options_prophet.py"
      - "scripts/build_options_screener.py"
      - "scripts/build_options_signal_campaign.py"
      - "scripts/build_options_signal_episode.py"
      - "scripts/build_options_sparse_selector_prereg.py"
      - "scripts/build_options_structure_intraday.py"
      - "scripts/build_prophet_marks.py"
      - "scripts/build_prophet_option_shadow_lifecycle.py"
      - "scripts/prophet_canonical_git.py"
      - "scripts/capture_market_memory_context.py"
      - "scripts/capture_market_memory_options_episodes.py"
      - "scripts/chain_snapshot_poller.py"
      - "scripts/ci/options_signal_nightly.sh"
      - "scripts/launchd/theta_staleness_sentinel.sh"
      - "scripts/live_flow_poller.py"
      - "scripts/mirror_flow_idx.py"
      - "scripts/publish_r2.py"
      - "scripts/mirror_terminal_context_r2.py"
      - "scripts/rerun_options_gates_thetadata.py"
      - "scripts/run_options_sparse_selector.py"
      - "scripts/research/momoedge_oracle_history_replay.py"
      # AD-1T1: the T1 store's daily incremental maintainer + legacy top-up writer.
      - "scripts/topup_thetadata_day.py"
      - "scripts/validate_options_ivspread.py"
      - "scripts/validate_options_skew.py"
      - "scripts/workflow_run_source.py"
      # engine/htf_durability.py reads site/basketdata/fear_greed.json, pulled
      # into this job's closure via tests/test_live_flow.py and
      # tests/test_options_issue_desk_api.py. site/flowleaders/** and
      # site/leaderradar/** below already cover the sibling pick_lab reads;
      # this one glob was the only gap the import-closure contract
      # (test_curated_exclusive_scopes_cover_their_own_import_closure) found.
      - "site/basketdata/fear_greed.json"
      - "site/canadabasketdata/**"
      - "site/chinabasketdata/**"
      - "site/coming-soon.html"
      - "site/darkpool_eod.json"
      - "site/data/**"
      - "site/factordata/**"
      - "site/flow/**"
      - "site/flowleaders/**"
      - "site/hkbasketdata/**"
      - "site/labdata/**"
      - "site/leaderradar/**"
      - "site/master_brief.json"
      - "site/options_prophet/**"
      - "site/options_structure/**"
      - "site/prophet/**"
      - "site/vol/**"
      - "tests/test_build_chain_heat.py"
      - "tests/test_build_options_prophet.py"
      - "tests/test_build_options_screener.py"
      - "tests/test_build_options_structure_intraday.py"
      - "tests/test_chain_snapshot_poller.py"
      - "tests/test_flow_archive.py"
      - "tests/test_flow_enrich.py"
      - "tests/test_flow_surface.py"
      - "tests/test_intraday_greeks.py"
      - "tests/test_live_flow.py"
      - "tests/test_mirror_terminal_context.py"
      - "tests/test_momoedge_oracle_history_replay.py"
      - "tests/test_options_focused_quote.py"
      - "tests/test_options_issue_desk.py"
      - "tests/test_options_issue_desk_api.py"
      - "tests/test_options_episode_outcome_coverage.py"
      - "tests/test_options_matrix.py"
      - "tests/test_options_market_memory_local_receipts.py"
      - "tests/test_options_market_memory_local_replica.py"
      - "tests/test_options_signal_campaign.py"
      - "tests/test_options_signal_episode.py"
      - "tests/test_options_sparse_selector_prereg.py"
      - "tests/test_options_sparse_selector_bootstrap.py"
      - "tests/test_options_sparse_selector_runtime.py"
      - "tests/test_run_options_sparse_selector.py"
      - "tests/test_options_structure_intraday.py"
      - "tests/test_private_auth_dict.py"
      - "tests/test_thetadata_store.py"
      # AD-1T1 (2026-08-22): full-universe incremental T1 daily maintainer —
      # legacy characterization (F17), the --daily hostile suite, and the
      # launchd scheduler suite.
      - "tests/test_topup_thetadata_legacy.py"
      - "tests/test_topup_thetadata_daily.py"
      - "tests/test_topup_thetadata_scheduler.py"
    # Flow-Surface store: scripts/build_flow_surface materializes per-strike net-premium
    # grids + (Lane G) intraday dealer-exposure greek grids (gex/dex/vanna/charm) +
    # walls/coverage (grids[metric][levelIdx][timeIdx]) into the R2 snapshot store the
    # Terminal surface pane replays. A regression here (grid orientation/dims, idx↔files
    # stamp drift, dishonest poll-floor/observed spacing, non-idempotent append,
    # greek-vs-EOD sign drift, or a
    # greek failure breaking the netprem write) would corrupt or blank the pane. Shapes are
    # asserted against a vendored copy of the Terminal contract (surfaceContract.ts); the
    # greek math is checked against engine/greeks + engine/gex_engine (the EOD source it
    # mirrors), so numpy/pandas/scipy are needed (the greek engine is numpy-vectorized).
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install deps
        # jinja2 + pyarrow are for the options-screener suite below: the builder imports
        # jinja2 at module scope and reads its inputs from parquet.
        run: pip install pytest pyyaml numpy pandas scipy jinja2 pyarrow jsonschema fastapi httpx requests
      - name: flow surface + dated archive cadence truth (poll floor, observed sparse spacing, retention)
        run: python -m pytest tests/test_flow_surface.py tests/test_flow_archive.py -q
      # PRISM's daily matrix shares this options data-plane lane.  Keep the
      # exact-contract-side volume baseline suite distinct so PIT/no-zero-fill regressions
      # cannot hide inside a broader surface invocation.
      - name: PRISM per-side PIT UNUSUAL lens (observed-session median, sample floor, 3x boundary)
        run: python -m pytest tests/test_options_matrix.py -q
      - name: options PIT episodes (causal clocks, durable stage, H+60 + declared-session-close proxies, R2/checkpoint integrity)
        run: python -m pytest tests/test_options_signal_episode.py -q
      # Pending H+60 rows are never appended, so an episode that never resolves
      # simply never appears in outcomes_h60.jsonl.  This suite pins the census
      # that bounds that hole and the classifier that splits it by remedy —
      # uncovered price universe (fixable) vs cadence-aligned exit (accepted).
      - name: options episode H+60 coverage census (unlabelled bound, structural gap vs accepted cadence class)
        run: python -m pytest tests/test_options_episode_outcome_coverage.py -q
      - name: sparse exact-option selector preregistration (zero denominator, permanent retrospective fence, no authority)
        run: python -m pytest tests/test_options_sparse_selector_prereg.py -q
      - name: bounded sparse selector runtime + private authenticated dictionary (fail-closed authority and cache forgery)
        run: >-
          python -m pytest
          tests/test_options_sparse_selector_runtime.py
          tests/test_private_auth_dict.py
          tests/test_options_market_memory_local_receipts.py
          tests/test_options_market_memory_local_replica.py -q -rs
      - name: sparse selector sealed runtime carrier + bounded paper-only canary runner
        run: >-
          python -m pytest
          tests/test_options_sparse_selector_bootstrap.py
          tests/test_run_options_sparse_selector.py -q
      - name: sparse selector 4,096-candidate full-drain benchmark (1,024 objects, 4 MiB intent caps)
        run: >-
          SPARSE_SELECTOR_PERF=1 python -m pytest
          tests/test_options_sparse_selector_runtime.py::test_4096_candidate_benchmark
          -q -rs
      - name: options signal campaign v2 (immutable revisions, causal anchors, exact checkpoint)
        run: python -m pytest tests/test_options_signal_campaign.py -q
      - name: MSC R2.2-A Light U-CHAIN packets + monotonic R2 discovery publication
        run: python -m pytest tests/test_options_structure_intraday.py tests/test_build_options_structure_intraday.py -q
      - name: W0a-B chain-snapshot durable bucket receipts and source-install proofs
        run: python -m pytest tests/test_chain_snapshot_poller.py -q
      - name: W0b focused vendor-snapshot quote completion and no-repoll proofs
        run: python -m pytest tests/test_options_focused_quote.py -q
      - name: live-flow PIT producer (RTH admission, stable source IDs, root-scoped state, cycle WAL)
        run: python -m pytest tests/test_live_flow.py -q
      - name: flow derivative source clocks (enrich + chain heat, fail-closed provenance)
        run: python -m pytest tests/test_flow_enrich.py tests/test_build_chain_heat.py -q
      - name: ThetaData PIT store (narrow OI projection, exact-date filter, cache bypass)
        run: python -m pytest tests/test_thetadata_store.py -q
      - name: AD-1T1 ThetaData T1 daily incremental maintainer (legacy characterization, --daily hostile suite, launchd scheduler)
        run: python -m pytest tests/test_topup_thetadata_legacy.py tests/test_topup_thetadata_daily.py tests/test_topup_thetadata_scheduler.py -q
      - name: intraday greek engine tests (IV solve round-trip, greek parity vs engine/greeks, dealer-sign parity vs engine/gex_engine, coverage honesty)
        run: python -m pytest tests/test_intraday_greeks.py -q
      # OEU M-XP(b)+(c): the two payloads the Terminal / Options workspace fetch. Both are
      # cross-repo contracts — an unwired suite here rots silently and the consumer 404s.
      - name: Terminal EOD-context R2 mirror (key map pinned, corrupt-file guard, fail-soft)
        run: python -m pytest tests/test_mirror_terminal_context.py -q
      - name: options screener (IV-rank percentile + Scanner-mode rows export shape)
        run: python -m pytest tests/test_build_options_screener.py -q
      # Authorized Oracle history replay is synthetic/aggregate-only in CI: it never
      # reads the private JV source. This lane already owns the parquet adapter closure.
      - name: Oracle replay (PIT cutoffs, R2 plan, privacy and no-signal authority)
        run: python -m pytest tests/test_momoedge_oracle_history_replay.py -q
      # The governed Prophet shadow foundation (#5067), named by NO run: step when it
      # merged. It belongs in THIS job rather than a new one: its subject pair is
      # scripts/build_options_prophet.py plus the R2 key map in scripts/mirror_flow_idx
      # (OPTIONS_PROPHET_R2_KEY), and this job already owns the sibling mirror contract
      # and the options PIT episode/R2-integrity suites above. Both subjects are
      # stdlib-only at module scope; the suite's one non-stdlib import is yaml, and
      # pyyaml is already on the install line.
      - name: governed options Prophet shadow foundation (#5067)
        run: python -m pytest tests/test_build_options_prophet.py -q
      - name: private operator-reviewed Options Issue Desk (R6.2-A)
        run: python -m pytest tests/test_options_issue_desk.py tests/test_options_issue_desk_api.py -q

  cycle-grading-stats:
    if: ${{ false }}
    gate: code
    # W1.5 (W1.1 acceptance): the grading_stats shared library must have exactly one
    # cone_coverage implementation (no silent second definition), and the China-grader
    # refactor must be numerically identical to the pre-refactor path on fixed inputs.
    # Catching a regression here is critical: this library feeds every grader in the
    # measurement engine.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install minimal deps
        run: pip install pytest pandas numpy pyarrow pyyaml scipy
      - name: grading_stats library tests (wilson_ci, fdr_bh, cone_coverage, china regression)
        run: python -m pytest tests/test_grading_stats.py -q

  # ── RENDER/ENGINE GUARD LANES (split 2026-08-14) ───────────────────────────
  # The three jobs below were ONE job — `engine-render-guards`, twelve steps and
  # 1036s in OBSERVED_COMMAND_SECONDS, the heaviest legacy job in the manifest and
  # alone in `ci-pack-0` on every 12-pack run, so the pack WAS the lane and no
  # amount of outer-matrix work could shorten it.  Receipt:
  # research/CI_RENDER_GUARD_TIMING_RECEIPT_2026-08-12.md; the merge-control-plane
  # incident that made the critical path urgent:
  # research/CI_MERGE_CONTROL_PLANE_RECOVERY_2026_08_14.md.
  #
  # The measurement dictates the seam, and it also says what the split is NOT for.
  # The lane was never twelve steps of comparable weight; it was one step wearing a
  # job: the rot sweep is 83.4% of it (356.8s of 427.6s measured locally) and the
  # other ten executable steps are 70.8s combined.  So the sweep is irreducible and
  # still carries a pack by itself.  Splitting buys the two things the single job
  # structurally could not:
  #
  #   1. SCOPE.  `infer_job_scopes` derives a job's paths from the test files its
  #      `run:` steps name, so ONE job naming 145 suites is broad by construction —
  #      a PR touching a single prophet-live file paid the entire 1036s.  Three
  #      jobs mean a narrow diff selects only the lane it actually changed.
  #   2. REACHABILITY.  `run_ci_pack._run_job` returns on the FIRST non-zero step,
  #      so every later step in that job is silently skipped.  The express
  #      guards below were hand-ordered ahead of the sweep for exactly that reason,
  #      one incident at a time (see each step's note).  A separate job has a
  #      separate exit status, so a red sweep can no longer skip them — this split
  #      RETIRES that ordering workaround rather than preserving it.
  #
  # Residual, stated plainly: within EACH lane the abandon-on-first-failure rule
  # still holds.  It is bounded now — no lane mixes a 129-suite sweep with a
  # focused guard — not eliminated.  Keep the two small lanes small.
  #
  # Dependency-group note: run_ci_pack prepares one venv per BYTE-IDENTICAL pip
  # line, so three different lines no longer share a group and cost up to three
  # venvs when two lanes land in the same pack.  That is the deliberate price of
  # (1): a narrow diff now installs one lane's packages instead of all fourteen.
  # Each line below is derived, not guessed — a package is present when a suite the
  # lane names imports or `importorskip`s it, when the first-party import-TIME
  # closure of such a suite needs it, or when `git log -L` shows it was added to the
  # old line for a suite this lane inherited.

  engine-render-guards:
    if: ${{ false }}
    gate: data
    # 2026-07-02 rot sweep: these test files silently broke on main because ci.yml only
    # ran a targeted subset. They cover the template render guards (canada / fear↔euphoria /
    # froth merged card / risk-state banner), the froth display-only firewall, the
    # price-basis-flip audit, the risk-gauge honesty layer (base-rate → measured PIT
    # probs, #887), GDELT missing-tape, the commodity MTF ladder (fitted bands, #981),
    # the net-liquidity unit guard (drain-independent since RRP≈0), and the whitehouse
    # qledger import guards (order-dependence cured at the walk_forward leaker).
    #
    # This job keeps the NAME because two records cite it by name and must stay true:
    # `config/house_law_checks.yml` (flow_continuity.statement_tape_schema →
    # `job: engine-render-guards`) and docs/HOUSE_LAW_CI_GUARD_SUITE.md both point the
    # statement-tape guard here, and both its check script and tests/test_statement_tape.py
    # are still in this job. The B4/attested-history cluster moved to
    # `attested-history-guards` and the express guards to `express-render-guards`.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install minimal deps
        # This is the pre-split line MINUS boto3, and nothing else moved. boto3 was
        # added by #4806 for the Wave 0A attested-history serving boundary, which now
        # lives in `attested-history-guards`; `engine/research_vault/r2_store.py`
        # imports boto3 LAZILY inside its client factory (and says so at its
        # `_client` docstring), so no suite in this sweep needs it at import time and
        # the sweep provably ran green without it before #4806.
        # beautifulsoup4: test_china_news_engine.py patches bs4.BeautifulSoup on the
        # official-pages fetch path, and patch() imports the module.
        # fastapi + httpx: tests/test_forensics_api.py importorskips both (FastAPI's
        # TestClient needs httpx); jsonschema and pillow arrived with the Filing
        # Forensics workbench (#4184) and predate it respectively, are reachable only
        # through lazy first-party paths, and are RETAINED deliberately — nothing here
        # proves those paths unreachable, and a wrong subtraction costs a red lane.
        run: pip install pytest pandas numpy pyarrow pyyaml jinja2 plotly pillow requests beautifulsoup4 fastapi httpx jsonschema
      # NEVER put a `#` comment inside the folded `run: >` scalar below. YAML folds it
      # onto ONE shell line, where `#` starts a SHELL comment — everything after it,
      # including every remaining test path and the trailing `-q`, silently vanishes
      # from the run. The step still exits 0, so it reads GREEN while running fewer
      # suites (caught 2026-08-07: a 5-line note added mid-list dropped 24 suites plus
      # the two being added). Annotate the list here, above `- name:`, where YAML
      # comments are real comments.
      #
      # Includes the china delayed-board disclosure + the A-share session calendar
      # behind it: the disclosure suite renders the real china.html.j2 end to end
      # (jinja2) and imports build_china_library (pandas/pyarrow), and it is the only
      # guard tying the template's English wording to the regex
      # scripts/freshness_sentinel.py parses.
      #
      # Also the two Act-Now hover-card icon suites (doctrine §5.8, emoji are not UI
      # icons). test_intl_act_now_board_icons.py renders the China partial, canada.html.j2
      # and hk.html.j2 through the harnesses already in this step
      # (test_china_stocks_w1c_render, test_canada_build) plus test_hk_board_ui, so this
      # is its natural home. test_act_now_board_icons.py — the US original it is modelled
      # on — was referenced NOWHERE under .github/ and had never been run by CI; it is
      # wired here in the same edit, since there is no tests/-wide sweep in this repo.
      # The Stock Connect leaderboard shape/render contract also lives here: it imports
      # scripts/build_china.py and renders the real China template, using the pandas /
      # pyarrow / jinja2 dependency set this job already owns.
      #
      # test_intl_session_asof.py (#5706) pins _intl_session_asof and the two
      # conviction-profile / B2 sites in scripts/build_intl_library.py. It belongs
      # next to test_intl_library_limited.py on this same run: line — same subject,
      # same pandas/pyarrow install, no new job.
      #
      # tests/test_fundamental_forensics_health.py rides this step (FF-0): same
      # Filing Forensics workbench family as test_fundamental_forensics_ui.py and
      # test_forensics_api.py. The install line already has fastapi/httpx/jsonschema;
      # no new dependency.
      # tests/test_fundamental_forensics_financial_intelligence_packet_r2.py rides
      # the same step as the v1 packet suite (FIF-1R2): same assembler, same
      # jsonschema/decimal install, no new job.
      # tests/test_fundamental_forensics_financial_intelligence_packet_r3.py rides
      # the same step (FIF-1R3 semantic closure): against-input, revision
      # vocabulary, entity identity, bounded graph validation. No new job.
      # tests/test_fundamental_forensics_broad_sec.py,
      # tests/test_filing_forensics_broad_sec_lane.py, and
      # tests/test_fundamental_forensics_edgar_index.py ride this step (FF-1):
      # incremental broad SEC source plane. LocalStore tests need pandas/pyarrow/
      # jsonschema/pyyaml/requests, already on this install line. No new job.
      # tests/test_canada_canonical_board.py (CA-TRUTH, PR #5926) rides next to
      # test_canada_build.py: same Canada-builder subject, same pandas/pyarrow
      # install this step already owns (it also imports engine.board_ledger,
      # already exercised by tests/test_board_ledger.py further down this same
      # step). No new job.
      # tests/test_canada_v36_composer.py (V3.6 acceptance repair, 2026-08-25)
      # rides the same Canada subject: byte pins on site/canada-stock-v36.js
      # ([hidden] display overrides + the card.hidden mechanism they cover).
      # stdlib-only (pathlib), full checkout has site/. No new job.
      # tests/test_hk_v37_composer.py (HK V3.7 follower, SOL-HK-V37-FOLLOWER,
      # 2026-08-25) rides the same step as its Canada sibling: byte pins on
      # site/hk-stock-v36.js (Featured-cohort selection, no-LIVE/zero-fetch
      # invariants, trd-wrap evidence move, [hidden] XOR overrides) plus the
      # HK loader block in templates/dashboard-icons.js. stdlib-only
      # (pathlib/re), full checkout has site/. No new job.
      # tests/test_stock_dashboard_css.py (TP-1 theme-parity extraction,
      # theme-parity-tp1-canada-20260828-sol-001) rides the same Canada
      # subject as test_canada_v36_composer.py: governed stock-dashboard.css
      # pair pins (visibility declarations, stance/lane inks, light-presence
      # fence, token cleanliness via scripts.check_design_system.scan_text —
      # repo-import only, no new deps), full checkout has site/. No new job.
      - name: render-guard + engine-contract tests (rot sweep 2026-07-02)
        run: >
          python -m pytest
          tests/test_canada_build.py tests/test_canada_canonical_board.py
          tests/test_canada_v36_composer.py
          tests/test_stock_dashboard_css.py
          tests/test_hk_v37_composer.py
          tests/test_commodity_signals.py
          tests/test_commodity_index.py
          tests/test_commodity_confluence.py
          tests/test_commodity_cycle_state.py
          tests/test_fear_euphoria.py tests/test_froth_fragility.py
          tests/test_vol_velocity.py tests/test_fear_greed_honesty.py
          tests/test_archive_context_snapshots.py
          tests/test_ai_breadth_split.py
          tests/test_vsb_surface.py
          tests/test_missing_tape.py tests/test_price_basis_flip.py
          tests/test_risk_lead_lag.py tests/test_anticipation.py
          tests/test_risk_radar_market_catalysts.py tests/test_risk_radar_recovery.py
          tests/test_risk_radar_scorecard.py
          tests/test_vsb_radar_alert.py
          tests/test_risk_radar_intl_profiles.py
          tests/test_gd4a_cnhk_ledger_repair.py
          tests/test_risk_radar_dlg_country_wiring.py
          tests/test_risk_radar_dlg_partial.py
          tests/test_risk_state_live_copy_sync.py
          tests/test_build_china_risk_state.py
          tests/test_china_connect_leaderboard_render.py
          tests/test_canon.py tests/test_whitehouse_w5.py
          tests/test_winner_autopsy.py tests/test_admin_long_hold.py
          tests/test_falsifier_packet.py
          tests/test_board_ledger.py tests/test_grade_us_board.py
          tests/test_ob_mask_start_invariance.py
          tests/test_board_ledger_lane_gates.py
          tests/test_prophet_governor.py
          tests/test_prophet_showcase.py
          tests/test_prophet_options_context.py
          tests/test_pick_autopsy_selection.py
          tests/test_trade_memory.py
          tests/test_options_stamp.py tests/test_options_entry_state.py tests/test_sector_pulse_wiring.py
          tests/test_news_page_render.py tests/test_china_lens_hub.py
          tests/test_news_rank_aging.py
          tests/test_build_netliq_daily.py
          tests/test_ignition_radar.py
          tests/test_mag7_washout.py
          tests/test_mag7_washout_shadow.py
          tests/test_mag7_regime.py
          tests/test_theme_scoring_leadership_split.py
          tests/test_ignition_persistence.py
          tests/test_mag7_panel.py
          tests/test_stance_matrix.py
          tests/test_theme_scoring_conflicted.py
          tests/test_basket_score_rollover_w4.py
          tests/test_index_momentum.py
          tests/test_cohort_flow_chips.py
          tests/test_statement_tape.py
          tests/test_backfill_options_flow.py
          tests/test_rlt_sector_stance_enrich.py
          tests/test_stock_personality_w4_surfaces.py
          tests/test_fundamental_forensics_page.py
          tests/test_fundamental_forensics_private_state.py
          tests/test_fundamental_forensics_health.py
          tests/test_fundamental_forensics_ui.py
          tests/test_fundamental_forensics_acquisition.py
          tests/test_fundamental_forensics_disclosure_diff.py
          tests/test_fundamental_forensics_disclosure_projection.py
          tests/test_fundamental_forensics_source_sync.py
          tests/test_fundamental_forensics_attestation.py
          tests/test_fundamental_forensics_wave2_operator.py
          tests/test_fundamental_forensics_companyfacts.py
          tests/test_fundamental_forensics_companyfacts_ledger.py
          tests/test_fundamental_forensics_metric_registry.py
          tests/test_fundamental_forensics_raw_ledger.py
          tests/test_fundamental_forensics_periods.py
          tests/test_fundamental_forensics_query.py
          tests/test_fundamental_forensics_financial_intelligence_packet.py
          tests/test_fundamental_forensics_financial_intelligence_packet_r2.py
          tests/test_fundamental_forensics_financial_intelligence_packet_r3.py
          tests/test_fundamental_forensics_query_snapshots.py
          tests/test_fundamental_forensics_attested_query_snapshots.py
          tests/test_fundamental_forensics_attested_history_reader.py
          tests/test_sec_document_spine.py
          tests/test_fundamental_forensics_filing_package.py
          tests/test_fundamental_forensics_financial_query_service.py
          tests/test_fundamental_forensics_financial_query_api.py
          tests/test_fundamental_forensics_ixbrl_raw_ledger.py
          tests/test_fundamental_forensics_financial_revision_service.py
          tests/test_fundamental_forensics_financial_revision_api.py
          tests/test_fundamental_forensics_financial_packet_service.py
          tests/test_fundamental_forensics_financial_packet_api.py
          tests/test_fundamental_forensics_financial_statement_service.py
          tests/test_fundamental_forensics_financial_statement_api.py
          tests/test_forensics_api.py
          tests/test_edgar_forensics_collector.py
          tests/test_fundamental_forensics_broad_sec.py
          tests/test_filing_forensics_broad_sec_lane.py
          tests/test_fundamental_forensics_edgar_index.py
          tests/test_context_api.py
          tests/test_finra_knowable.py
          tests/test_t0_indicator.py
          tests/test_intraday_flow.py
          tests/test_intraday_flow_stance.py
          tests/test_standout_audit.py
          tests/test_track_record_page.py
          tests/test_altdata_hygiene.py
          tests/test_china_news_engine.py
          tests/test_china_library_limited.py
          tests/test_china_standouts_serialization.py
          tests/test_china_stocks_w1c_render.py
          tests/test_intl_act_now_board_icons.py
          tests/test_act_now_board_icons.py
          tests/test_china_delayed_board_disclosure.py
          tests/test_cn_board_freshness_truth.py
          tests/test_china_zt_pool_dates.py
          tests/test_drip_append_only.py
          tests/test_cn_calendar.py
          tests/test_china_stocks_copy_w09.py
          tests/test_china_board_track_render.py
          tests/test_china_alpha_w2a.py
          tests/test_china_alpha_w2b.py
          tests/test_china_act_now.py
          tests/test_us_act_now.py
          tests/test_p_mp1_shell_groundwork.py
          tests/test_p_mp1_shell_stance_projection.py
          tests/test_p_mp1_shell_nonus_byte_parity.py
          tests/test_wl1_lifecycle_neutralization.py
          tests/test_p_mp1_shell_ladder_filter_css.py
          tests/test_p_mp1_shell_ladder_error_state.py
          tests/test_p_mp1_shell_css_comment_termination.py
          tests/test_p_mp1_shell_repair_round.py
          tests/test_p0_prophet_candidate_board.py
          tests/test_hk_library_limited.py
          tests/test_canada_library_limited.py
          tests/test_intl_library_limited.py
          tests/test_intl_session_asof.py
          tests/test_library_chg_1d.py
          tests/test_stock_technicals.py
          tests/test_aibrief_page.py
          tests/test_aibrief_w4_panels.py
          tests/test_market_reference.py
          tests/test_validated_claims_thirdparty.py
          tests/test_validated_claims_engine.py
          tests/test_validated_claims_surfaces.py
          tests/test_validated_claims_registry_source.py
          tests/test_validated_claims_structural_scope.py
          tests/test_intl_recovery_quality.py
          tests/test_sa_w5_v2_grader_hook.py
          tests/test_w8b_macro_prior_entries.py
          tests/test_rr_scorecard_card.py
          tests/test_engine_run_no_regress.py
          tests/test_cycles.py
          tests/test_stretch_oracle_contract.py
          tests/test_opex.py
          tests/test_opex_risk.py
          tests/test_seo_meta_rollout.py
          tests/test_cycles_anchor_invariance.py -q
      # house-law registry census (post-#2297 fix): run the statement-tape schema
      # guard itself against the committed registry, not just its pytest coverage.
      # It stays in THIS job — `config/house_law_checks.yml` names this job as its
      # ci_wiring — and it is deliberately LAST: it is the only step here that is not
      # pytest, and the sweep above is the thing most likely to be red, so ordering it
      # first would have bought nothing. Its own pytest coverage
      # (tests/test_statement_tape.py) is inside the sweep, one step above.
      - name: statement-tape schema guard (FL-E / FC-R9)
        run: python -m scripts.check_statement_tape

  prophet-lab:
    if: ${{ false }}
    gate: code
    # LAB-0 §6 commissioning-prep review B1 (2026-08-19, PR #5995): these four
    # suites (test_prophet_lab.py/_api.py/_timeparse.py/_commissioning.py) were
    # wired into engine-render-guards, which is `gate: data` — and ci.yml's
    # merge-gate ONLY ever invokes `run_ci_pack.py --gate code` (gate:data jobs
    # run exclusively on the nightly data-health.yml lane, post-merge). Every
    # Lab suite, INCLUDING #5928's already-merged ones, has therefore NEVER
    # actually run inside a PR's own merge gate. All four are hermetic
    # tmp_path/fixture tests over engine/prophet_lab/** (a pure projection
    # package — no data/ or site/ read, LAB-0 §1) plus the app/prophet_lab.py
    # transport layer, the engine/entry_radar/spool.py R2-read seam it now
    # imports, and the scripts/prophet_lab_baseline.py CLI: the verdict is a
    # function of the PR's tree only, squarely `gate: code`'s own definition
    # (see the GATE_VALUES docstring in scripts/run_ci_pack.py). Split into its
    # own job rather than reclassifying engine-render-guards wholesale — that
    # job also carries suites that DO read real rendered site/**/data/**
    # output and are correctly gate:data; narrowing that job's own gate is a
    # separate, larger cleanup outside this PR's scope.
    paths:
      - "app/prophet_lab.py"
      - "engine/prophet_lab/**"
      - "engine/entry_radar/__init__.py"
      - "engine/entry_radar/contracts.py"
      - "engine/entry_radar/spool.py"
      - "scripts/prophet_lab_baseline.py"
      - "tests/test_prophet_lab.py"
      - "tests/test_prophet_lab_api.py"
      # D5 A7's manifest-authenticated N/N+1 correction chain is hermetic and
      # load-bearing for the Prophet Lab adapter, so its PR owner must be this
      # gate:code job rather than only neural-web-core's nightly data lane.
      - "tests/test_company_intelligence_workspace_chain.py"
      - "tests/test_prophet_lab_timeparse.py"
      - "tests/test_prophet_lab_commissioning.py"
      - "tests/fixtures/prophet_lab/**"
      # test_caddy_hub_boundary.py's fastapi/starlette half imports
      # app.prophet_lab (its importorskip reasons literally read "prophet_lab
      # guard tests need fastapi") — it rides HERE so those tests execute
      # somewhere; the thin unrun-dark-guards venv runs only its
      # Caddyfile/string half and skip-only gated red on integration-baseline
      # (first red main run 32480845137, introduced by #6184).
      - "tests/test_caddy_hub_boundary.py"
      - "scripts/check_caddy_hub_boundary.py"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install minimal deps
        # fastapi + httpx: test_prophet_lab_api.py's FastAPI TestClient needs
        # both (httpx importorskip'd). pandas + pyarrow: the D5 API suite writes
        # its canonical security-master parquet and app.prophet_lab imports the
        # B1 episode module, whose parquet owner is unconditional. requests:
        # D5's company-intelligence reader imports it at module scope. boto3 is
        # NOT needed: engine.entry_radar.spool imports it LAZILY inside
        # `_r2_client()`, and every R2 path this suite exercises either deletes
        # the R2 credential env vars or injects a fake client object directly —
        # the real boto3 import is never reached.
        run: pip install pytest pandas pyarrow fastapi httpx pyyaml requests
      - name: Prophet Operator Lab — projection, real-reader chain, transport, timeparse, commissioning-prep
        run: >
          python -m pytest
          tests/test_prophet_lab.py
          tests/test_prophet_lab_api.py
          tests/test_company_intelligence_workspace_chain.py
          tests/test_prophet_lab_timeparse.py
          tests/test_prophet_lab_commissioning.py
          tests/test_caddy_hub_boundary.py -q

  attested-history-guards:
    if: ${{ false }}
    gate: code
    # Wave 3B-B4 / attested-history cluster, split out of `engine-render-guards`
    # 2026-08-14. These four steps are one dependency story and one security story:
    # they are the ONLY suites in the old job that needed boto3, and they pin the
    # offline materialization seams, the read-only operator foundation, the dedicated
    # serving boundary, and the controlled AAPL pilot.
    #
    # Every one of them used to carry a comment explaining that it was ordered AHEAD
    # of the ~100-file sweep so an unrelated red render guard could not leave it
    # unexecuted (run_ci_pack abandons a job's remaining steps on the first non-zero
    # exit). That workaround is retired: the sweep is a different job now, with a
    # different exit status, so it cannot leave these dark at any position. The
    # protection those comments were reaching for is now structural.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install minimal deps
        # boto3 (which brings botocore) is the reason this lane exists as its own
        # dependency group: two tests in the Wave 0A serving boundary below exercise
        # the production boto3 client factory and the real receipt reader, and without
        # the module they importorskip — i.e. they execute in NO job, which
        # check_skip_only_suites.py correctly rejects. #4806 added boto3 to the old
        # single line for exactly these tests.
        # fastapi + httpx: the serving-boundary suite importorskips both and drives
        # FastAPI's TestClient. jsonschema: the read-only operator suite imports
        # Draft202012Validator at module level. pandas/numpy/pyyaml/requests come from
        # the import-time first-party closure of these ten suites. pyarrow is a real
        # runtime dependency of B4D-A: query_snapshots imports it lazily while encoding
        # the deterministic parquet cells exercised by the materializer suite, so an
        # import-only dependency census does not see it.
        # No jinja2/plotly/pillow/beautifulsoup4 here: no suite in this lane imports
        # them and none is needed at runtime.
        run: pip install pytest pandas numpy pyarrow pyyaml requests fastapi httpx jsonschema boto3
      # Wave 3B-B4D-A adds the three offline seams that turn pinned SEC source objects
      # into filing packages, Company Facts ledgers, and deterministic B4 binding plans
      # (wired 2026-08-02).
      - name: B4D-A offline materialization foundation
        run: >-
          python -m pytest
          tests/test_sec_document_spine.py
          tests/test_fundamental_forensics_attestation.py
          tests/test_fundamental_forensics_filing_package_materializer.py
          tests/test_fundamental_forensics_companyfacts_pinned_loader.py
          tests/test_fundamental_forensics_attested_history_materializer.py -q
      - name: read-only attested-history operator foundation
        run: >-
          python -m pytest
          tests/test_fundamental_forensics_attested_history_operator.py -q
      # Wave 0A: the production receipt API's DEDICATED reader. This suite is the
      # only proof that the API addresses the dedicated attested-history bucket
      # rather than the generic Research Vault one, that the read-only store is
      # structurally excluded from every StrictConditionalWriteStore write gate,
      # that the renewal lock is not held across the network GET, and that the
      # VPS delivery workflow carries the read-only half and never a seed writer.
      # deploy-api-secrets.yml's own comment cites this suite as the protection on
      # its credential contract, so that citation must stay true — it now points at
      # a lane whose only failure mode is its own.
      - name: Wave 0A dedicated attested-history serving boundary
        run: >-
          python -m pytest
          tests/test_fundamental_forensics_attested_history_api_store.py -q
      # B4F: the initial issuer seed is allowed to construct only a governed base and
      # a redacted review-only receipt; these tests pin the no-network selection,
      # one-shot credential boundary, storage-control probe, and isolated
      # dimensions-unknown evidence bridge.
      - name: B4F controlled AAPL pilot and seed safety contracts
        run: >-
          python -m pytest
          tests/test_fundamental_forensics_attested_history_pilot.py
          tests/test_fundamental_forensics_attested_history_seed.py
          tests/test_fundamental_forensics_attested_occurrence_governance.py -q

  express-render-guards:
    if: ${{ false }}
    gate: data
    # The express render/engine guards, split out of `engine-render-guards`
    # 2026-08-14. Every step here is a focused, sub-10-second guard that was wired
    # into the old job one incident at a time — and every one of them carried a
    # comment saying it had been ordered BEFORE the ~100-file sweep because
    # run_ci_pack abandons a job's remaining steps once one exits non-zero, so a
    # trailing guard is unreachable whenever anything in that sweep is red. That was
    # never a fix, only a queue position: it protected the step that happened to be
    # first and pushed the hazard onto whatever was last.
    #
    # The split is the fix. A red sweep is now a red in a DIFFERENT job and cannot
    # skip anything here, at any position. Read each step's note for the rot it was
    # written against; the ordering rationale that used to sit in those notes is gone
    # because it no longer describes anything.
    #
    # It is not gone from the lane, though, only bounded: within THIS job the first
    # non-zero step still abandons the rest. That is why this lane stays small and
    # every step is independent — do not grow it into a second sweep.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install minimal deps
        # jinja2 (test_etfs_gate, test_group_flow render real templates), plotly and
        # pandas/numpy (the ETF builder assertions), pyarrow (the prophet-live
        # reconciler's parquet round-trip and the flow-desk staleness fixtures),
        # requests + pyyaml from the import-time first-party closure, and jsonschema
        # because tests/test_theme_clinical.py importorskips it — this is the only
        # lane naming that suite, so dropping it would make the contract-checked
        # rollup tests skip-only, which check_skip_only_suites.py rejects.
        # No boto3: engine/prophet_live/r2io.py imports it lazily inside client() and
        # the prophet-live suites monkeypatch that factory; the only boto3 mentions in
        # this lane are string assertions ABOUT another workflow's pip line.
        run: pip install pytest pandas numpy pyarrow pyyaml jinja2 plotly requests jsonschema
      # The import-light tier-gate runs the shell/boundary cases, while the two
      # builder assertions intentionally require pandas + plotly. Pair the suite
      # with this lane's fat deps so those assertions execute instead of skip
      # everywhere.
      # test_us_board_gate.py is the same split in the same shape for the
      # us_stocks board, so it is paired the same way: tier-gate runs its
      # jinja2-only shell/leak cases, and the splitter + payload-writer
      # assertions (which import scripts.build_site, hence plotly) execute
      # HERE. Those are the half that proves the withholding itself, so
      # leaving them to skip would gate the markup and not the gate.
      - name: ETF tier-preview builder assertions (full page stack)
        run: python -m pytest tests/test_theme_clinical.py tests/test_etfs_gate.py tests/test_us_board_gate.py -q
      # Wired 2026-07-26: test_group_flow.py was named by NO job in ANY workflow
      # (`grep -rn test_group_flow .github/` returned nothing), so its baskets.html.j2
      # render guard sat RED on main from 2026-07-24 — #3282's Rotation Map revamp
      # rebuilt the "Flow lens" card into the Money-flow tile and the test's literal
      # `{% if flow %}` source slice raised ValueError from that merge onward. Same
      # unrun-suite darkness as test_nyse_calendar (#3779) and the #3768 sweep. It
      # belongs with the express guards: it is a template render guard, it renders the
      # same baskets.html.j2 as test_theme_scoring_conflicted, and this lane's deps
      # already carry jinja2/pandas/numpy. Path-gated in ci.yml on BOTH the test and
      # templates/baskets.html.j2 — a guard the guarded change cannot trigger is not a
      # guard (#3488).
      - name: group-flow display-only detector + baskets.html.j2 money-flow render guard
        run: python -m pytest tests/test_group_flow.py -q
      # Wired 2026-08-02: neither tests/test_theme_hash_deeplink.py nor
      # tests/test_sector_intelligence_page.py was named by any job in any workflow,
      # so both sat dark while the Sector Intelligence consolidation (#4237) turned
      # baskets.html.j2 into a redirect stub that ATE inbound #theme-<id> hashes —
      # the exact silent deep-link failure the deeplink suite was written against
      # (#4254), invisible because the suite never ran. Both are stdlib text guards
      # over templates this lane already covers. Path-gated in ci.yml on the test
      # files; templates are covered by "templates/**" (#3488).
      - name: theme deep-link contract + sector-intelligence consolidation invariants
        run: python -m pytest tests/test_theme_hash_deeplink.py tests/test_sector_intelligence_page.py -q
      # Wired 2026-08-05: tests/test_flow_velocity.py was named by NO job in ANY
      # workflow (`grep -rn test_flow_velocity .github/` returned nothing), so the
      # suite written specifically to catch a pinned-by-construction flow readout
      # (#3561) has been dark ever since. It stayed green by luck, but its subject
      # went stale invisibly: site/flowdata/desk.json's A-share legs froze at
      # 2026-07-24 and ran 12 days behind a southbound leg dated 08-05, still
      # declaring cadence "daily", with nothing red anywhere.
      # test_flow_desk_staleness.py is the alarm for that class — the relative +
      # wall-clock gates in lib/desk_guard.stale_legs, the column-zero annotation
      # the builder emits, and the ChinaTushareAdapter heartbeat that reported
      # 'ok' for ten nights while every data/tushare store sat frozen.
      # Both are engine/builder contract guards and this lane's deps already carry
      # pandas/numpy/pyarrow/jinja2. Path-gated in ci.yml on BOTH the tests and their
      # subjects (lib/desk_guard.py, engine/flow_velocity.py,
      # scripts/build_flow_velocity.py, collectors/china_tushare.py) — a guard the
      # guarded change cannot trigger is not a guard (#3488).
      # Wired 2026-09-02 (W1, PR #6780): tests/test_flow_observatory_contract.py guards
      # the flow_observatory.v2 desk contract (quadrant axis consistency, market_read
      # denominators incl. unscored, five-leg sources[] honesty, state_log idempotence,
      # banned-vocabulary at rest) — same subject family as this lane's two flow suites.
      # Wired 2026-09-02/03 (W2): tests/test_flow_observatory_quality.py guards the new
      # engine/flow_observatory/quality.py state machine (per-leg HEALTHY/DEGRADED/STALE/
      # UNAVAILABLE/HISTORICAL_ONLY/REVISED classification, the desk-wide wall-clock
      # backstop, worst-of publication_state, and the ::error escalation annotation) — the
      # W1 contract-delta lesson applies again: wired in the SAME commit that adds the file.
      # Wired 2026-09-03 (W3): tests/test_flow_observatory_history.py guards the new
      # engine/flow_observatory/history.py append-only observations ledger — byte-identical
      # idempotent append, revision-safe corrections, deterministic replay, ledger-derived
      # state age/onset/prior-state/rank-change with the stale-freeze and skip-gap rules,
      # and the lane gate — same W1 contract-delta lesson, wired in the SAME commit.
      # B1 repair (W4 review round): tests/test_flow_observatory_groups.py — the 416-line
      # guard for official/curated lenses, coverage floor, overlap disclosure, and
      # concentration/contribution — shipped in the W4 PR with NO job naming it in EITHER
      # ci.yml or here, so all 20+ of its tests were dark from the merge onward and the
      # PR's own "wired" claim was false (independent review, 2026-09-03). Wired here, in
      # the SAME commit that also adds its ci.yml path-gate entries, per the standing W1
      # contract-delta lesson this comment block already names three times over.
      # Wired 2026-09-03 (W5): tests/test_flow_observatory_methods.py — small unit tests
      # on the preregistered method-evaluation harness's math primitives (winsorization
      # bounds, median/MAD scale, probit mapping, floor application, determinism). The
      # harness itself (scripts/research_flow_observatory_methods.py) runs off this render
      # path entirely; only its primitive-level unit tests are gated here, same W1
      # contract-delta lesson, wired in the SAME commit as its ci.yml path-gate entries.
      # B1 repair (W6 review round): tests/test_flow_observatory_workflow.py — the
      # FOURTH wave in a row (after W2/W3/W4's own "wired ... same lesson" comments
      # immediately above) to ship a new tests/test_flow_observatory_*.py suite with
      # NO job naming it anywhere in CI, so all 13+ of its tests (history drawer, band
      # geometry, compare, prior episodes, Terminal links, watch-store decision) sat
      # dark from the W6 merge onward. Wired here, in the SAME commit that also adds
      # its ci.yml path-gate entry AND the structural kill in
      # tests/test_flow_observatory_contract.py
      # (test_all_flow_observatory_suites_are_wired_into_the_ci_lane) that reads THIS
      # run: line and fails closed the next time a sibling suite ships unwired.
      - name: flow-velocity measure guards + flow-desk staleness alarm (frozen-leg detection, heartbeat liveness)
        run: python -m pytest tests/test_flow_velocity.py tests/test_flow_desk_staleness.py tests/test_flow_observatory_contract.py tests/test_flow_observatory_quality.py tests/test_flow_observatory_history.py tests/test_flow_observatory_groups.py tests/test_flow_observatory_methods.py tests/test_flow_observatory_workflow.py -q
      # The market-score authority suite spans engine contracts, dashboard projection,
      # and forward-ledger provenance, so the render/engine guard lanes own it. It sits
      # here rather than in the sweep because it is one focused suite and because an
      # unrelated render failure must not leave the veto firewall dark — which is now
      # true by job separation, not by queue position.
      - name: US market-score authority firewall (calendar, Radar, history, provenance)
        run: python -m pytest tests/test_market_score_authority_2026_08_12.py -q
      # #5480 added this express-render ownership guard to main but omitted a
      # run: owner, so audit_unrun_tests correctly found it dark on the very
      # next check-definition refresh. #5481 folded the focused render-lane
      # module into the canonical China Policy Watch suite; keep that canonical
      # suite here because it pins both render workflows and the data-pristine
      # --site-only builder boundary.
      - name: China Policy Watch express-render ownership + data-pristine mode
        run: python -m pytest tests/test_china_policy_watch.py -q
      # Wired 2026-08-13 (#5507): `grep -rn cn_theme_tape .github/` returned NOTHING
      # since the panel landed on 2026-08-05 (#4667), so its 37 guards had never run
      # in CI — and its real-artifact acceptance case was in fact RED, invisibly,
      # from the 2026-08-07 ledger stamp onward: that night moved 002155.SZ (湖南黄金)
      # from a `veto` to `flat: cut`, and the case turned out to pin the night's
      # verdict rather than the accounting. It was rewritten to the charter before
      # wiring, per the standing rule that a previously-unrun suite goes green BEFORE
      # it is armed. It rides this express lane, not a new job: the china render
      # family (test_china_stocks_w1c_render, test_china_act_now, the delayed-board
      # disclosure) still lives in the sweep, and this lane's deps already carry the
      # exact set the suite needs — pandas + pyarrow to read the two parquet ledgers,
      # jinja2 to render the real partial. The old "keep it ahead of the sweep"
      # ordering note is retired: a red sweep is a different job now and cannot skip
      # this step at any position.
      - name: CN Theme Tape — theme/member accounting + the 2026-08-04 narration case
        run: python -m pytest tests/test_cn_theme_tape.py -q
      # PROPHET LIVE P0 gates. These lanes, not a new one: the parity suite drives the
      # REAL engine.signal_gate over seeded close series, so it needs pandas + numpy
      # (present in this lane's install line above) and pyarrow for the reconciler's
      # parquet round-trip. Any importorskip here would be a disarmed test — the deps
      # are provably installed, so there is none.
      #   G0.1 parity  tests/test_prophet_live_pack.py       (~25s, real gate calls)
      #   G0.2/3/5/6   tests/test_prophet_live_evaluator.py
      #   ledger law   tests/test_prophet_live_reconcile.py
      #   VPS lane     tests/test_prophet_live_vps_lane.py — quote source, the atomic
      #                served write, the serving gate (/live/prophet_live.json is NOT
      #                public), the systemd unit/timer, macro-update's reconciliation
      #                and the workflow's self-disabling backstop gate (§4.2a/§4.4a).
      #                Stdlib + pyyaml only, so it rides in this lane rather than
      #                needing one of its own.
      #   one basis    tests/test_prophet_live_basis.py — W-L0 gate 3, the CROSS-MODULE
      #                half: the adjustment vocabulary against engine.price_ladder's,
      #                the pack's own basis stamp, and the per-pass pack-vs-feed audit.
      #                The lane behaviour it feeds is pinned in the evaluator and
      #                reconciler files above. LISTED HERE ON PURPOSE — this job
      #                enumerates its files, so an unlisted new suite never runs.
      #   CN clock     tests/test_cn_live_clock.py — CN-W-L3 PR-0 (#5759): the mainland
      #                session-state contract (lunch = session_break, per-board limit
      #                bands incl. 301/689, the delay-aware quote-age anchor), the
      #                armed_pack calendar threading (calendar=None IS the NYSE path —
      #                this lane's US files prove it stays byte-unchanged), and
      #                lib.cn_calendar.sessions_behind. Same package, same deps
      #                (pandas via armed_pack), so it rides here, not in a new lane.
      #   CN pack      tests/test_cn_live_pack.py — CN-W-L3 PR-1: calendar=cn_calendar,
      #                per-class ±10/±20 probe span, schema cn_prophet_live.armed/v1.
      #   CN evaluator tests/test_cn_live_evaluator.py — close-board coverage, lunch
      #                freeze, quote-age, pending remnant in last minute of post_close.
      #   CN VPS lane  tests/test_cn_live_vps_lane.py — CN timer, served-path gate,
      #                kill switch, health absent-ok, update.sh self-arm.
      # It is LAST in this lane and that is now a cost of ~48s of sibling guards
      # ahead of it, not of a 357-second sweep.
      - name: prophet-live P0 (G0.1 parity, ledger law, debounce, degradation)
        run: >
          python -m pytest
          tests/test_prophet_live_pack.py
          tests/test_prophet_live_pack_d12.py
          tests/test_prophet_live_evaluator.py
          tests/test_prophet_live_reconcile.py
          tests/test_prophet_live_basis.py
          tests/test_prophet_live_vps_lane.py
          tests/test_prophet_live_silent_freeze.py
          tests/test_prophet_live_journal_recovery.py
          tests/test_cn_live_clock.py
          tests/test_cn_live_pack.py
          tests/test_cn_live_evaluator.py
          tests/test_cn_live_vps_lane.py
          tests/test_cn_live_reconcile.py -q

  collector-registry:
    if: ${{ false }}
    gate: code
    # timeout: this job had none (6h GitHub default) — a wedged one-shot (e.g. the
    # pyarrow exit deadlock documented in lib/procutil.py) would block PRs for hours.
    timeout-minutes: 15
    # 2026-07-03 (#1052 post-mortem): PR #1052's squash-merge shipped scripts/collect.py as
    # a 0-BYTE file. An empty module imports fine and exits 0, so every scheduled collect
    # run (daily / asia-close / weekly / backfill / intl_etf all invoke `python -m
    # scripts.collect`) became a silent no-op until #1093 restored it — and the truncation
    # merged GREEN because no CI job covered the collection entrypoint. This is the
    # tripwire: tests/test_china_news.py imports all_adapters() from scripts.collect (and
    # proves china_news registers even without akshare), and the breadth assert fails RED
    # if the registry is emptied or gutted. 105 of ~132 adapters register under these
    # minimal deps — the yfinance/akshare ones drop out by design via the registry's
    # per-module try/except — so the >100 floor holds without heavyweight installs.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install minimal deps
        run: pip install pytest pandas numpy pyyaml requests
      - name: china_news collector tests (incl. adapter-registered-without-akshare)
        run: python -m pytest tests/test_china_news.py -q
      - name: strict SEC filing parser, extraction, and sealed attestation contracts
        run: |
          python -m pytest \
            tests/test_sec_filing_parser.py \
            tests/test_fundamental_forensics_ixbrl_extraction.py \
            tests/test_fundamental_forensics_attestation.py -q
      - name: adapter registry must stay populated
        run: |
          python -c "from scripts.collect import all_adapters; n = len(all_adapters()); assert n > 100, f'adapter registry gutted: only {n} adapters registered'; print(f'{n} adapters registered')"

  capital-structure-intelligence:
    if: ${{ false }}
    gate: data
    # W0-W3A is an evidence/event/precision/candidate-term contract, not merely a collector.
    # This dedicated offline job keeps schemas, PIT/correction behavior, R2 wrapper,
    # graph linkage, exact fee-table spans, legacy boundary, and SEC fixtures in one
    # hard gate. Document and candidate terms remain pre-instrument transcriptions,
    # never capacity/risk math.
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install capital-structure test deps
        run: pip install pytest pandas numpy pyarrow jsonschema==4.26.0 requests pyyaml jinja2 fastapi httpx boto3
      # P1 unrun backfill 2026-08-14: the REGISTRATION-LIFECYCLE compiler joins the step
      # that already owns every other capital-structure compiler and contract.
      # tests/test_compile_capital_structure_registration_lifecycles.py was named by no
      # `run:` step in any workflow, so scripts/compile_capital_structure_registration_
      # lifecycles.py and engine/capital_structure/registration_lifecycle.py had no CI
      # owner, while the source_identity/source_ledger_io modules they build on were
      # covered here.  Appended LAST in the list so a compiler, contract or source-store
      # regression is the first failure a reader sees rather than the lifecycle compiler
      # failing for a borrowed reason.  It rides this existing step because a step is the
      # masking unit (run_ci_pack.py returns on the first non-zero step) while pytest
      # reports every file inside one step, and adds no job to a manifest at the
      # narrow-diff ceiling.  No dependency widened: probed at RUN time it loads
      # pandas/numpy/pyarrow/pyyaml/requests, all already on the install line above.
      - name: capital-structure contracts, compilers, projection, graph, source store, and legacy boundary
        # FOUR nightly-wiring suites ride HERE on purpose, and they only mean
        # anything together — each states one side of the same 2026-08-06 split:
        #   test_daily_collect_commit_path.py    the MARKET side: no capital-structure
        #     step may run in the collect job, the market checkpoint keeps its
        #     named-producer gate, and the handoff that replaced the chain can never
        #     cost the night.
        #   test_daily_capital_structure_job.py  the LANE side: the chain lives in its
        #     own `needs: collect` job, NOTHING needs that job, the compilers stay
        #     fatal, staging stays narrow, and the artifact handoff is verified
        #     against collect's receipt before a compiler reads the ledger.
        #   test_capital_structure_compiler.py   the COMPILER side (::test_nightly_order
        #     _and_render_network_firewall_are_pinned): market checkpoint first —
        #     asserted through `needs:`, not file order — and the compile still fatal
        #     for its own checkpoint.
        #   test_collect_staging_globs.py        the QLEDGER side, and it was named by
        #     no `run:` step at all until 2026-08-06 — so when #4731 added the narrow
        #     capital-structure checkpoint, its "every commit step stages
        #     site/qledger/" assertion went red on main and nothing reported it. It
        #     sat red for two days and was mistaken for cross-file test pollution.
        #     The grader's tracked write must be committed by the FIRST checkpoint,
        #     and a later narrow checkpoint must be gated so it cannot run when that
        #     first one did not.
        # Co-locating means a change to any one surfaces the others; splitting them
        # lets one drift green while the invariant it names is already broken.
        run: >
          python -m pytest
          tests/test_daily_collect_commit_path.py
          tests/test_daily_capital_structure_job.py
          tests/test_collect_staging_globs.py
          tests/test_capital_structure_compiler.py
          tests/test_capital_structure_contracts.py
          tests/test_capital_structure_sec_discovery_clock.py
          tests/test_capital_structure_document_terms.py
          tests/test_capital_structure_instrument_candidates.py
          tests/test_capital_structure_event_spine.py
          tests/test_capital_structure_graph.py
          tests/test_capital_structure_legacy_compat.py
          tests/test_capital_structure_pit.py
          tests/test_capital_structure_projection.py
          tests/test_build_capital_structure_projection.py
          tests/test_capital_structure_page.py
          tests/test_capital_structure_api.py
          tests/test_capital_structure_source_identity.py
          tests/test_capital_structure_source_manifest.py
          tests/test_capital_structure_ingestion_health.py
          tests/test_capital_structure_share_count_truth.py
          tests/test_capital_structure_companyfacts_authenticated_read.py
          tests/test_capital_structure_share_count_materializer_model.py
          tests/test_capital_structure_share_count_publication.py
          tests/test_capital_structure_share_count_retention.py
          tests/test_capital_structure_share_count_materializer.py
          tests/test_capital_structure_share_count_r2_conformance.py
          tests/test_capital_structure_share_count_r2_operator.py
          tests/test_capital_structure_share_count_r2_concurrency.py
          tests/test_capital_structure_share_count_r2_concurrency_operator.py
          tests/test_sec_capital_structure.py
          tests/test_sec_capital_structure_companyfacts.py
          tests/test_capital_structure_evidence_identity.py
          tests/test_capital_structure_closed_bundle.py
          tests/test_nwqs_c_dilution.py
          tests/test_compile_capital_structure_registration_lifecycles.py -q

  stock-seasonality:
    if: ${{ false }}
    gate: data
    # WIRING LAW (#1253 precedent): a suite that NO job names never runs. The
    # foundation suite (tests/test_biopharma_seasonality_foundation.py) landed
    # with no job naming it and has therefore never executed in CI — this job is
    # its home as well as the calendar clock's.
    #
    # What rots here if it is unguarded is not cosmetic. The engine folds ~25
    # complete years onto a 365-slot clock and prices a 2645-window search with a
    # resampling null; a leap-day return quietly dropped, a window that wraps
    # December into January, or a null that shifts every year TOGETHER (which
    # relocates a real seasonal effect instead of removing it) all produce output
    # that looks entirely plausible and is wrong. The calibration test — pure
    # noise must fire at roughly its nominal 5% — is the one that separates a real
    # familywise correction from a decorative one, and nothing else in the repo
    # can catch that regression.
    #
    # Offline: the artifact tests build a synthetic parquet store in tmp_path, so
    # no network and no dependence on the committed data/yahoo/ tree.
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install seasonality test deps
        # jinja2: test_seasonality_program_watch.py's public-containment class
        # imports scripts.build_measurement, which does `from jinja2 import
        # Environment` at module scope. run_ci_pack builds a FRESH isolated venv
        # per distinct install string (no --system-site-packages), so a package
        # another job installed is not visible here and the two containment tests
        # — the leak gate itself — error with ModuleNotFoundError.
        run: pip install pytest pandas numpy pyarrow pyyaml jinja2
      - name: seasonality contracts, panel, curve, window family, null, and artifact
        run: >
          python -m pytest
          tests/test_biopharma_seasonality_foundation.py
          tests/test_stock_seasonality_engine.py
          tests/test_seasonality_event_clock.py
          tests/test_seasonality_pit_spine.py
          tests/test_seasonality_prophet_overlay.py
          tests/test_seasonality_event_study.py
          tests/test_seasonality_model.py
          tests/test_seasonality_calibration.py
          tests/test_seasonality_screener.py
          tests/test_seasonality_api.py
          tests/test_seasonality_program_watch.py -q
      # W5 state v2. The suite that has to run here is the one about a
      # migration that has NOT happened yet: v1's ``forecast.p`` is a historical
      # positive-year share, and the failure this pins is a later session
      # fitting a model and writing its output into a field that is already
      # there, already plumbed, and already called ``p``. Every downstream
      # reader would keep reading and nothing in the payload would record that
      # the meaning changed — which is exactly the class of defect that looks
      # entirely plausible in the output, same as the leap-day and window-wrap
      # regressions this job already exists for. Offline: pure-stdlib contracts
      # plus a tmp_path parquet store, no network.
      # test_seasonality_shadow_state.py was named by NO job before this line: the
      # Lane 6 emitter has never been executed in CI since #4370. This PR rewrites
      # what it emits, so it runs here now.
      - name: seasonality state v2 — frozen v1 semantic, dual read, ledger grading
        run: >
          python -m pytest
          tests/test_seasonality_state_v2.py
          tests/test_seasonality_shadow_state.py -q

  china-native-collectors:
    if: ${{ false }}
    # RECLASSIFIED data -> code (2026-08-21). W1 (#5954) put this job on the
    # data side, which under W2 means ci.yml never packs it: `gate: data` jobs
    # run only in data-health.yml, and that lane fires on `workflow_run` after
    # the nightly plus a 13:30Z schedule — it has NO `pull_request` trigger. So
    # every suite below ran post-merge only. The cost was concrete: #6142's
    # seven P1-R1 same-cycle acceptance tests in tests/test_china_visits_collector.py
    # merged having never executed in CI on the PR that wrote them.
    #
    # The reclassification is measured, not asserted. Under a sparse checkout
    # with `data/` absent entirely, 17 of the 18 suites named below pass green
    # (test_china_visits_collector.py 38 passed; test_china_tushare_spine.py 43
    # passed; test_tushare_freshness_tripwire.py 7 passed) — a suite that passes
    # with no data tree at all cannot have a verdict a nightly commit can move.
    # They are hermetic by construction: test_china_visits_collector.py carries
    # two autouse fixtures redirecting config.data_dir AND lib.store.read_status/
    # write_status to tmp_path, test_china_official_corpora.py monkeypatches
    # _store_dir, test_cn_intel_pit_accrual.py monkeypatches the OUT/OUT_HIST
    # module globals, and test_china_tushare_spine.py passes tmp_path as an
    # explicit base_dir to every spine.* call.
    #
    # The 18th, tests/test_china_news_intel_w2.py, has one test that reaches the
    # real tree: tag_tickers -> entity_resolver.resolve_cn -> cn_name_to_ticker()
    # reads config.data_dir()/"baskets_china"/"membership.json". That file is a
    # HAND-AUTHORED REGISTRY, not a nightly ledger — 6 commits in its whole life,
    # five of them PR-authored (newest #4512) and the lone dashboard-bot commit a
    # page-copy edit. It moves only when a pull request moves it, which is the
    # exact W1 discriminator (bot-authored vs PR-only) and the exact category W1
    # already reclassified 56 jobs into: "pinned fixtures, tmp_path roots, and
    # hand-authored registries the heuristic's read_parquet match over-flagged".
    # The root-cause doctrine says it outright — "a job that reads `data/` to
    # build a fixture is code-gated, and a job that reads a pinned golden is
    # code-gated" (research/CI_MERGE_GATE_RELIABILITY_ROOT_CAUSE_2026_08_19.md).
    #
    # Budget: +44 weight-seconds onto a 2,086s `gate: code` set spread over
    # twelve packs (max pack 274s before this). `gate` is not an input to scope
    # inference or to select_jobs, and job_exec_sha256 does not hash it, so the
    # narrow-diff job/weight ceilings in tests/test_ci_pack.py and every semantic
    # proof digest are unmoved by this edit — verified before and after.
    gate: code
    # W1 of the China/HK native-data program: 互动易 / 上证e互动 / sell-side revision tape /
    # 股东户数. WIRING LAW (#1253 precedent): a suite that NO job names never runs — these
    # four files would merge green untested without this job, and their whole point is
    # fixture-pinned parsers over live CN vendor responses, i.e. exactly the code that
    # rots silently when the vendor's shape drifts. Offline only (no network in tests);
    # beautifulsoup4 + lxml are needed because the e互动 planes are HTML, not JSON.
    #
    # W3 adds the policy/OMO/trade-detail step below. tests/test_china_official_corpora.py
    # joins it as the same class of defect the job exists for: it was named by NO job
    # from the day it was written, so the official-corpus fetcher — now carrying the
    # gov.cn policy-library json_feed organ — had no CI at all.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install minimal deps
        # jsonschema is for the full-A spine step below: its suite validates the
        # emitted manifest against contracts/cn_tushare_a_share_spine_manifest.v1
        # with Draft202012Validator, which is a COLLECTION-time import — without it
        # the step dies at exit 2 having asserted nothing, which reads as a lane
        # failure rather than as the missing dep it is.
        run: pip install pytest pandas pyarrow requests beautifulsoup4 lxml pyyaml jsonschema
      - name: china native interaction + sell-side collector tests (W1 CNH)
        run: python -m pytest tests/test_china_irm_collector.py tests/test_china_einteraction_collector.py tests/test_china_reports_collector.py tests/test_china_holder_counts_collector.py -q
      # China Alpha Intelligence P1 (#6050): institutional-visit tape, derived
      # from china_filings' own store — same wiring-hole class this job exists
      # for (a fixture-pinned collector named by NO job would rot silently).
      - name: china institutional-visit tape collector tests (P1)
        run: python -m pytest tests/test_china_visits_collector.py -q
      - name: china native policy / OMO / trade-detail collector tests (W3 CNH)
        run: python -m pytest tests/test_china_omo_collector.py tests/test_china_trade_detail_collector.py tests/test_china_official_corpora.py -q
      # W3 part B: the five native flash wires (futu/ths joined here) and the forward
      # calendar organ that aggregates the CN/HK release engines + the OMO tape.
      # test_cn_newswires.py was named by NO job before this — the same wiring hole as
      # test_china_official_corpora.py above, on the surface that rots FASTEST (five
      # vendor JSON shapes, none of them contractual). test_china_news_intel_w2.py
      # joins them for the same reason: it is the only suite pinning the bus that
      # CONSUMES those wires (slug collision, timestamp quality, qbus row shape) and
      # no job named it either.
      - name: china native wires + forward calendar tests (W3 CNH)
        run: python -m pytest tests/test_cn_newswires.py tests/test_china_calendar.py tests/test_china_news_intel_w2.py -q
      # The fail-closed full-A TuShare spine (#5116). Same WIRING LAW as every step
      # above, and the same shape of hole: the suite was named by NO run: step when
      # it merged, so the spine's fail-closed contract — the manifest schema, the
      # Decimal price quantization, the lock and the atomic replace — ran dark from
      # the day it landed. It belongs in THIS job rather than a new one: the subject
      # is a China native collector reading collectors/tushare_client, and the deps
      # above already carry pandas + requests for exactly that client.
      # tests/test_china_tushare_range_shards.py joins this step in the SAME breath as
      # the library it covers, rather than a release later: the range-shard campaign is
      # the cap fallback the spine now delegates to, so leaving it unnamed here would
      # reproduce the exact hole this step's comment is about — 1,057 lines of leaf
      # state, immutable attempt receipts and BSE alias blocking running dark from the
      # day they landed. Both suites are synthetic and network-free.
      - name: china full-A spine collector + manifest contract (#5116)
        # test_cn_intel_pit_accrual.py joins here (not a new step): same China /
        # TuShare collector family this job already owns — broker 金股, per-name
        # margin, block trades, buybacks, _first_seen_store. Same WIRING LAW as
        # the range-shard suite above: named by no run: step when it landed
        # (#5730), so workflow-yaml painted pack-1 red. Offline, no new deps.
        # test_tushare_compliance_anti_resurrection.py joins here for the same
        # reason and in the same breath as the guard it covers (#6207, Chairman
        # override). That guard is the DURABLE half of the override: it fails CI
        # when an active authority surface — the full-A contract, WS-CN-LIMIT-ALPHA,
        # the R6 freeze/registry/command packet, the TuShare rights matrix, the
        # China Alpha rights records — re-acquires a vendor-letter/written-grant/
        # license-document requirement. Leaving it unnamed would reproduce exactly
        # the hole this step's comment is about, and would make "enforced in CI"
        # false the day it landed. Offline, no new deps.
        # test_cn_limit_calendar_epoch_census.py joins here in the SAME breath as the
        # census it covers, for the same reason and under the same WIRING LAW. Its
        # subject is the frozen mainland session epoch that this job's own
        # collectors/china_tushare_spine.py compiles the session axis from, so the two
        # suites fail together or not at all. It is network-free by construction and
        # builds synthetic fixture stores in tmp_path — CI has no private spine store,
        # so a census test needing the real one would be a broken test. No new deps.
        run: >
          python -m pytest
          tests/test_cn_limit_calendar_epoch_census.py
          tests/test_china_tushare_spine.py
          tests/test_china_tushare_range_shards.py
          tests/test_cn_intel_pit_accrual.py
          tests/test_tushare_compliance_anti_resurrection.py
          -q
      # The three TuShare takeover suites (lane A #5177 add-ons, lane B #5163
      # minutes, lane C #5162 chips). Same WIRING LAW as every step above, same
      # hole: none of the three lanes named its suite in any run: step, so 144
      # offline tests covering technical execution fences, immutable receipts,
      # the minutes manifest and the chips M2/M4 aggregation would run dark.
      # This is the #5116 spine's natural home: every suite is a China-native
      # collector over collectors/tushare_client, and the dependency step above
      # already carries the measured import closure (pandas, pyarrow,
      # jsonschema, requests and pyyaml). Lane B imports Lane A's endpoint
      # contract at collection time, so the three suites remain one atomic step.
      # P1 unrun backfill 2026-08-14: tests/test_tushare_freshness_tripwire.py joins the
      # TuShare family.  It was named by no `run:` step in any workflow, so
      # scripts/check_tushare_freshness.py — the tripwire that is supposed to notice
      # when the TuShare stores stop advancing — had no CI owner.  That is the exact
      # shape of the failure this lane already carries a comment about: a freshness
      # check reporting 'ok' for ten nights while every data/tushare store sat frozen.
      # A dead ingest lane is invisible downstream, so the tripwire is the only thing
      # that would say so, and it was the one piece with no test running.
      # Appended LAST, downstream of the spine/manifest and takeover suites that
      # produce the stores it reads, so a collector regression is the first failure a
      # reader sees rather than the tripwire failing for a borrowed reason.  It rides an
      # existing step because a step is the masking unit (run_ci_pack.py returns on the
      # first non-zero step) while pytest reports every file inside one step, and adds
      # no job to a manifest at the narrow-diff ceiling.  No dependency widened: probed
      # at RUN time it loads only pandas/numpy/pyarrow/pyyaml, all covered by this job's
      # install line (numpy arrives with pandas).
      - name: china tushare takeover suites — addons gate + minutes plane + chips (A/B/C)
        run: python -m pytest tests/test_tushare_addons.py tests/test_tushare_minutes_plane.py tests/test_tushare_chips_distribution.py tests/test_tushare_freshness_tripwire.py -q

  china-board-breadth:
    if: ${{ false }}
    gate: code
    # Whole-board 沪深 涨跌家数 (~5,200 names) behind the A-share heatmap's breadth
    # card. WIRING LAW (#1253 precedent): tests/test_market_heatmap.py was named by
    # NO job before this — the CN/HK/CA map contract merged untested for its whole
    # life. Both suites are offline (canned Sina/Tushare payloads, synthetic closes).
    # numpy is required: engine.market_heatmap imports engine.sp500_heatmap for the
    # shared timeframe contract, and that module imports numpy at module scope — a
    # minimal lane without it fails at collection, not at assert.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install minimal deps
        run: pip install pytest pandas numpy pyarrow requests pyyaml
      - name: whole-board breadth collector + heatmap payload contract
        run: python -m pytest tests/test_china_board_breadth.py tests/test_market_heatmap.py -q

  china-search-universe:
    if: ${{ false }}
    gate: code
    # The A-Share Analyzer's searchable name set (Sina top-800 ∪ CSI 300 ∪ CSI 1000).
    # WIRING LAW (#1253 precedent): both suites were named by NO job, so the CSI union —
    # the part of this collector that has now produced four separate silent-shrink
    # defects (#4577 duplicate codes, #4585 92xxxx→.SS, and the untimed fetch + silent
    # lossy fallback fixed here) — merged untested for its whole life. Offline: canned
    # CSIndex frames, generated .xls bytes, no network.
    #
    # openpyxl is not optional here: the direct CSIndex path is an .xls parsed through
    # pd.read_excel, and the fixtures generate real workbook bytes rather than mocking
    # the parser (a mocked read_excel cannot see the bilingual 成份 header dialect the
    # live file actually ships). yfinance is a module-scope import of the collector.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install minimal deps
        run: pip install pytest pandas numpy pyarrow pyyaml requests openpyxl yfinance
      - name: CSI constituent union — timeout, header dialect, degraded-night membership
        run: python -m pytest tests/test_china_universe_index_constituents.py tests/test_china_universe_extras.py -q

  cycle-consistency:
    if: ${{ false }}
    gate: data
    # W3.6 (R3-M3): the SAME instrument must show the SAME pos_v2/phase_v2 across every
    # page that renders it (cycle.html flagship / country_cycles / markets.html / sector),
    # within tolerance, OR carry a declared basis label explaining the difference.  A page
    # that recomputes or freezes a stale value fails RED here.  Also runs the phase-coherence
    # regression (no pos_v2 ≥ 85 with a Trough/Recovery phase_v2 without divergence) — the
    # guard against the mixed-vocabulary bug the W3.6 kernel fix closed.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install minimal deps
        run: pip install pytest pandas numpy pyarrow pyyaml
      - name: same instrument agrees across pages (or declares its basis)
        run: python3 scripts/check_cycle_consistency.py site
      - name: consistency + phase-coherence regression tests
        run: python -m pytest tests/test_cycle_consistency.py tests/test_cycle_ontology.py -q

  synapse-read-gate:
    if: ${{ false }}
    gate: code
    # W0 PR3: literal-path scan over engine/*.py, scripts/*.py, collectors/*.py.
    # WARN tier (exit 0): any undeclared reader of a registered artifact.
    # HARD FAIL (exit 1): an undeclared reader that is an Article-2 money-path
    # surface (alert_triage / board_ordering / top_setups).
    # Baseline: data/neuralweb/read_gate_baseline.json (only-shrinks ratchet).
    # Runs check_synapse_registry.py (integrity, hard) first, then
    # check_synapse_reads.py (warn + Article-2 hard) + the unit test suite.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install minimal deps
        run: pip install pytest pyyaml pandas pyarrow requests jsonschema
      - name: synapse registry integrity (hard gate)
        run: python3 scripts/check_synapse_registry.py
      - name: synapse read-gate — warn tier + Article-2 hard fail
        run: python3 scripts/check_synapse_reads.py
      - name: factor boundary guard (RUL-NW9/NW11 — hard fail on Article-2 writes or forbidden fields)
        run: python3 scripts/check_factor_boundaries.py
      - name: factor boundary selftest (guard fires on planted violations)
        run: python3 scripts/check_factor_boundaries.py --selftest
      - name: synapse read-gate unit tests
        run: python -m pytest tests/test_synapse_read_gate.py tests/test_horizon_firewall.py tests/test_delivery_waterfall.py tests/test_pricing_power_monitor.py tests/test_risk_envelope.py tests/test_live_risk_envelope.py -q
      - name: factor deescalation shadow + boundary guard tests
        run: python -m pytest tests/test_factor_deescalation_shadow.py -q
      - name: edgar 8-K item-code expansion unit tests (LHB-R7)
        run: python -m pytest tests/test_edgar_8k_items.py -q
      # GR3b: 8-K counterparty extraction unlock. GR3 (#5008) measured the column
      # as null in all 50,667 committed rows -- the name leg was gated behind the
      # dollar leg, the "primary document" selector was reading the EDGAR submission
      # HEADER page (index.json's `type` field is the listing ICON name, never a form
      # type), and the fetched HTML was never stripped to prose. All three, plus the
      # bounded exhibit reads and the enrich_rev backfill bookkeeping, are pinned here.
      - name: edgar 8-K counterparty extraction + exhibit reads (GR3b, no network)
        run: python -m pytest tests/test_edgar_8k_counterparty.py -q
      - name: LHB-R6 quarterly-store enrichment tag tests (no network)
        run: python -m pytest tests/test_edgar_quarterly_tags.py -q
      - name: edgar fundamentals universe widening (W2-A — R2000 via committed constituents)
        run: python -m pytest tests/test_edgar_universe_widening.py -q
      - name: symbol-directory archival collector tests (LHB-W2c / LHB-R8)
        run: >-
          python -m pytest
          tests/test_symbol_directory.py
          tests/test_symbol_directory_completion_receipts.py -q
      - name: winner autopsy B-family feature column tests (LHB-W3)
        run: python -m pytest tests/test_winner_autopsy.py -q

  neural-web:
    if: ${{ false }}
    gate: data
    # neural-web program (W0, #1253 post-mortem): three test files were run by ZERO
    # GitHub workflows before this job was added.  Without CI wiring, an edit to
    # config/synapse.yml (not under any prior ci.yml path glob) could merge GREEN with a
    # stale committed SIGNAL_BUS.md — precisely the 'merged green because no CI job
    # covered X' failure class documented in #1052 / #1193 / #1195.
    #
    # Three suites, one job:
    #   test_synapse_registry.py   — synapse.yml integrity (YAML parses, paths exist,
    #                                enums valid, duplicate-path check, validator unit tests)
    #   test_neuralweb_envelope.py — envelope helper (sibling-not-wrapper, hash stability,
    #                                stamp_if_changed byte-identity, sidecar round-trip)
    #   test_signal_bus_doc.py     — SIGNAL_BUS.md freshness gate (regenerated bytes must
    #                                match committed doc byte-for-byte; determinism; all 64
    #                                artifact ids present; Mermaid block plausible)
    #
    # test_constitution.py (wired 2026-08-11): A0-A7 constitution regression — 51 tests
    # incl. the explicit A7-refusal test and the Wilson-gate safety sweep. Found run by
    # ZERO workflows during the Executive OS Phase 0 census
    # (research/EXECUTIVE_OS_PHASE0_CENSUS.md §4.2, PR #5356). Separate step so a red
    # names itself. Engine imports are pure stdlib; opens config/synapse.yml
    # (article2_surfaces integration) — already in ci.yml on.pull_request.paths.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install minimal deps
        run: pip install pytest pandas numpy pyarrow pyyaml
      - name: neural-web registry + envelope + bus-doc freshness tests
        run: python -m pytest tests/test_synapse_registry.py tests/test_neuralweb_envelope.py tests/test_signal_bus_doc.py tests/test_admin_neural_web.py tests/test_admin_intelligence_os.py tests/test_admin_metabolism.py tests/test_admin_metabolism_history.py tests/test_entity_thesis_registry.py tests/test_check_entity_thesis_registry.py tests/test_nw_lobe_descriptions.py tests/test_synapse_registry_contract.py -q
      - name: Neural Web constitution A0-A7 regression (A7 refusal + Wilson-gate sweep)
        run: python -m pytest tests/test_constitution.py -q
      - name: Mastermind semantic registry + generated global system map
        run: python -m pytest tests/test_mastermind_system_map.py -q
      - name: "HARD | entity/thesis/mechanism registry integrity (ETM-C1..C7)"
        run: |
          python3 -m scripts.check_entity_thesis_registry --selftest
          python3 -m scripts.check_entity_thesis_registry

  intelligence-registry:
    if: ${{ false }}
    gate: code
    # Eval OS T1 — the Mastermind engine registry. Two laws, one script, both WARN:
    # governance.intelligence_registry_integrity (partition / attribution / overlay
    # allowlist — invariants of the derivation) and epistemics.engine_authority_evidence
    # (pre-existing corpus conditions). --strict is deliberately NOT passed: it reds on
    # ~200 pre-existing content findings, which is a scheduled red on arrival.
    #
    # ISOLATED, NOT FOLDED INTO neural-web. run_ci_pack.py::_run_job RETURNS on the first
    # non-zero step, so inside a shared job there is no safe position: T1 FIRST masks the
    # nine sibling suites behind it, and T1 LAST goes dark behind any of them (#4779 — an
    # absence of red is not a pass). A step-level `if: always()` cannot fix it either;
    # run_ci_pack.py's ALLOWED_STEP_KEYS is {name, run, uses, with}, so `if:` on a step is
    # a hard manifest error. CEO ruling 2026-08-14 authorises the separate job: 188 jobs
    # were measured for consolidation, zero exact-duplicate signatures were found, and no
    # safe merge existed — so the pack rebalance a new job causes is the cheaper of the two
    # costs, and it is paid once.
    #
    # THE REGISTRY IS A DERIVED ON-DEMAND VIEW — nothing is committed, there is no
    # `--check` equality mode and no drift comparison, so this job cannot be reddened by a
    # nightly claim append or by a sibling PR editing config/synapse.yml. Two earlier
    # rounds pinned a generated artifact by equality (against data/qledger/claims.jsonl,
    # then against config/synapse.yml — 70 and 69 commits in the trailing 14 days, measured
    # on full history 2026-08-14 after the clone was unshallowed) and both were scheduled
    # fleet-wide reds. Do not re-add a regeneration lane or a committed file.
    #
    # CURATED SCOPE (2026-08-14 fleet heal). `scope: exclusive` makes the paths
    # below REPLACE inference. This job's inferred fallback tier claimed
    # whole-tree `templates/**` (and docs/research/site) from a subprocess or
    # traversal inside the engine import closure. Measured after #5587: that
    # smear was the +1 that pushed templates/index.html from the pinned 128
    # jobs to 129. The job does not read the landing page; it only
    # fallback-matched it. The declaration is the owned import closure,
    # compacted to directory globs. Do not add templates/**.
    #
    # `scripts/` IS ENUMERATED, NOT GLOBBED (2026-08-14 follow-up). The first
    # curation shipped a whole-directory `scripts/*`, which re-created inside
    # the CURATED tier the very behaviour `scope: exclusive` exists to end: a
    # directory glob makes EVERY ordinary code PR touching ANY script select
    # this job, on no evidence that the job reads it. Measured the same day —
    # `scripts/build_free_content.py` is NOT in this job's import closure, yet
    # `scripts/*` selected it, and that smear alone put the build_free_content
    # probe at 127 jobs against the then-126 ceiling in
    # tests/test_ci_pack.py::test_exclusive_curation_narrows_ordinary_code_prs.
    # A sibling heal re-based that ceiling elsewhere, so this is an
    # OPTIMIZATION rather than a red — but the smear was structural and is what
    # the enumeration removes. The 39 entries below ARE the job's own scripts/
    # import closure, re-derived on this commit via
    # tests/test_ci_pack.py::_inferred_as_if_not_exclusive.
    #
    # WIDENING IS THE SAFE DIRECTION. Exclusivity SKIPS inference, so a closure
    # file with no matching pattern is a job that stops running when its own
    # dependency changes and reports green forever. Do not hand-prune this
    # list: when a new import lands,
    # test_curated_exclusive_scopes_cover_their_own_import_closure names the
    # exact uncovered file — add it here.
    #
    # `collectors/` AND `lib/` ARE ENUMERATED, NOT GLOBBED (2026-08-15
    # follow-up to #5685). Re-derived on this commit via
    # infer_job_scopes([replace(job, exclusive=False)]): collectors/* was
    # 237 tracked / 33 concrete; lib/* was 33 / 19. Same shape `scripts/*`
    # was — small enough to enumerate, and mostly smear. The 33 + 19
    # entries below ARE this job's own collectors/ and lib/ import closure.
    #
    # THE REMAINING GLOBS ARE EARNED and stay globbed: engine/** (enumeration
    # would rot on every engine import), config/** (the registry's declared
    # input surface — a config edit SHOULD select this job), and the
    # data/** / site/** / tests/** trees this job actually owns. Do not
    # narrow those.
    scope: exclusive
    paths:
      - "contracts/evidence_foundation/vocabulary.v1.json"
      - "contracts/evidence_foundation/reference.v1.schema.json"
      - "contracts/evidence_foundation/recipe.v1.schema.json"
      - "contracts/evidence_foundation/block.v1.schema.json"
      # B1A closure widening 2026-08-24 (contract-delta): lib/evidence_foundation enters via lib/dataos identity readers; the schema is read by the evidence-foundation validator surface.
      - "contracts/market_os/security_state.v1.schema.json"
      - "lib/evidence_foundation.py"
      # F01 R1B closure widening 2026-09-04 (contract-delta): build_site.py now
      # imports the macro-suite page builder (guarded hook), whose closure
      # carries the workspace snapshot schema and the suite labels/view libs.
      - "contracts/market_os/macro_workspace_snapshot.v1.schema.json"
      # Eval OS T4 additions. app/: reached through scripts/freshness_sentinel.py, which
      # T4's reader-plane adapter imports for the surface table rather than copying it;
      # the sentinel's alert path imports app.mailer lazily and the closure walker sees
      # it. Named files, not `app/**` — the job touches these two and nothing else under
      # app/. lib/dataos/: the WHOLE package, enumerated per the 2026-08-15 lib idiom —
      # lib/dataos/__init__.py re-exports every sibling, so importing the temporal model
      # executes all seven files and all seven are genuinely in this job's closure.
      - "app/__init__.py"
      - "app/mailer.py"
      - "lib/dataos/__init__.py"
      - "lib/dataos/identity.py"
      - "lib/dataos/nulls.py"
      - "lib/dataos/price.py"
      - "lib/dataos/quality.py"
      - "lib/dataos/registry.py"
      - "lib/dataos/temporal.py"
      - "collectors/__init__.py"
      - "collectors/_drip.py"
      - "collectors/base.py"
      - "collectors/cboe.py"
      - "collectors/china_analyst.py"
      - "collectors/china_credit.py"
      - "collectors/china_news.py"
      - "collectors/edgar.py"
      - "collectors/edgar_13f.py"
      - "collectors/edgar_deadname_prices.py"
      - "collectors/edgar_deadnames.py"
      - "collectors/edgar_delisting.py"
      - "collectors/edgar_eps.py"
      - "collectors/edgar_facts.py"
      - "collectors/edgar_flow_quality.py"
      - "collectors/edgar_headcount.py"
      - "collectors/edgar_rpo.py"
      - "collectors/edgar_share_quality.py"
      - "collectors/edgar_stock_quality.py"
      - "collectors/equity_profile.py"
      - "collectors/equity_revisions.py"
      - "collectors/fda_shortages.py"
      - "collectors/finra.py"
      - "collectors/fred.py"
      - "collectors/holdings.py"
      - "collectors/openfigi.py"
      - "collectors/polygon_options.py"
      - "collectors/sec_document_spine.py"
      - "collectors/sec_insider.py"
      - "collectors/sector_holdings.py"
      - "collectors/sponsors.py"
      - "collectors/treasury.py"
      - "collectors/us_names_zh.py"
      - "config/**"
      - "engine/**"
      - "lib/__init__.py"
      - "lib/ai_costs.py"
      - "lib/board_state.py"
      - "lib/chat_allowance.py"
      - "lib/closes_panel.py"
      - "lib/cn_calendar.py"
      - "lib/config.py"
      - "lib/delisted_symbols.py"
      - "lib/filing_value_units.py"
      - "lib/finra_knowable.py"
      - "lib/forex_link.py"
      - "lib/help_directory.py"
      - "lib/hk_calendar.py"
      # F01 R1B closure widening 2026-09-04 (contract-delta): via build_site's
      # macro-suite hook — see the contracts/market_os entry above.
      - "lib/macro_suite_labels.py"
      - "lib/macro_suite_view.py"
      - "lib/nyse_calendar.py"
      - "lib/options_context.py"
      - "lib/pages.py"
      - "lib/seo.py"
      - "lib/site_assets.py"
      - "lib/store.py"
      - "lib/ticker_aliases.py"
      - "lib/ticker_popularity.py"
      - "research/REGIME_RELIABILITY_FACTOR_CROWDING_ADJUDICATION.md"
      - "research/SIGNAL_EPISODE_ATLAS_MASTERPLAN_BY_FABLE.md"
      - "research/STOCK_PERSONALITY_MASTERPLAN_BY_FABLE.md"
      - "research/US_BOARD_MEASUREMENT.md"
      - "scripts/__init__.py"
      - "scripts/build_biocatalyst.py"
      - "scripts/build_capital_structure_page.py"
      - "scripts/build_chart_data.py"
      - "scripts/build_fundamental_forensics.py"
      - "scripts/build_index_leadership.py"
      - "scripts/build_intelligence_registry.py"
      - "scripts/build_live_overlay.py"
      # F01 R1B closure widening 2026-09-04 (contract-delta): imported by
      # scripts/build_site.py's guarded macro-suite hook.
      - "scripts/build_macro_suite_pages.py"
      - "scripts/build_market_heatmap.py"
      - "scripts/build_market_memory_page.py"
      - "scripts/build_market_structure_page.py"
      - "scripts/build_output_health.py"
      - "scripts/build_risk_envelope.py"
      - "scripts/build_rr_banner.py"
      - "scripts/build_site.py"
      - "scripts/build_sp500_heatmap.py"
      - "scripts/build_state_of_themes.py"
      - "scripts/build_stock_library.py"
      # Import-closure entry: build_stock_library reuses
      # scripts/snapshot_shadow_book.rows_from_board for the shadow-book
      # snapshot (one percentile definition), so exclusive scope must name it.
      - "scripts/snapshot_shadow_book.py"
      - "scripts/build_subsector_confluence.py"
      - "scripts/build_subsector_rotation.py"
      - "scripts/build_subsector_rotation_china.py"
      - "scripts/build_subsector_rotation_china_pages.py"
      - "scripts/build_subsector_rotation_pages.py"
      - "scripts/build_themes_heatmap.py"
      - "scripts/build_whitehouse.py"
      - "scripts/check_board_contradictions.py"
      - "scripts/check_intelligence_registry.py"
      - "scripts/check_signal_gate_coherence.py"
      - "scripts/check_stretch_oracle_contract.py"
      - "scripts/check_synapse_reads.py"
      - "scripts/check_template_site_sync.py"
      - "scripts/close_pass_publish.py"
      - "scripts/close_pass_reconcile.py"
      - "scripts/freshness_sentinel.py"
      - "scripts/inject_wh_banner.py"
      - "scripts/notify.py"
      - "scripts/optimize_assets.py"
      - "scripts/regime_reliability_phase0.py"
      - "scripts/thematic_rotation_phase0.py"
      - "scripts/translate_profiles.py"
      - "scripts/validate_regime_fwd.py"
      - "scripts/worktree_sparse.py"
      - "site/basketdata/**"
      - "site/factordata/**"
      - "site/flowleaders/**"
      - "site/leaderradar/**"
      - "site/master_brief.json"
      - "site/neuralwebdata/**"
      - "site/qledger/**"
      # F13-X1: build_site imports the source-bound help directory, whose
      # fail-closed contract reads these exact existing owner templates.
      - "templates/_navlinks.html.j2"
      - "templates/_public_footer.html.j2"
      - "templates/_public_nav.html.j2"
      - "templates/measurement.html.j2"
      - "templates/methodology.html.j2"
      - "templates/support.html.j2"
      - "tests/test_check_intelligence_registry.py"
      - "tests/test_intelligence_registry.py"
      - "tests/test_output_health.py"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install minimal deps
        # Copied verbatim from neural-web: the guard imports engine.neuralweb.synapse
        # transitively, so leaning this line down is a separate, measured change.
        run: pip install pytest pandas numpy pyarrow pyyaml
      - name: "intelligence engine registry integrity (Eval OS T1)"
        # The plain run is inherently fail-closed as of 2026-08-14: structural violations
        # AND a PR-PLANE blind read (config or code unreadable) exit non-zero with no flag.
        # Blindness on data/qledger/claims.jsonl — the one input a nightly lane advances —
        # is REPORTED here and gated only under --strict, so this PR job cannot go red for
        # a store no PR author touched.
        run: |
          python3 scripts/check_intelligence_registry.py --selftest
          python3 scripts/check_intelligence_registry.py
      - name: intelligence engine registry derivation + gate tests
        run: python -m pytest tests/test_intelligence_registry.py tests/test_check_intelligence_registry.py -q
      # Eval OS T4 — the output-level health contract, which JOINS the T1 view above with
      # synapse and the existing health surfaces. Same job because it reads the same two
      # registries; APPENDED, never inserted, because run_ci_pack.py::_run_job returns on
      # the first non-zero step and a new FIRST step would hide the T1 guard behind it
      # (#4779 — an absence of red is not a pass).
      #
      # Fixture-rooted by construction: the suite asserts nothing about live synapse
      # contents or live estate freshness, so a nightly append and a genuinely stale
      # artifact are both invisible to it. The resolver is pure, so this step needs no
      # dependency beyond the pytest/pyyaml line already installed above.
      - name: "output health contract (Eval OS T4)"
        run: python -m pytest tests/test_output_health.py -q

  neural-web-core:
    if: ${{ false }}
    gate: data
    # NW Macro Context Rail R5 (PR-A): wires the five orphan NW test files that were
    # previously run by ZERO GitHub workflows.  Path-gated on engine/neuralweb/** and
    # the five test files (workflow-level on.pull_request.paths covers engine/neuralweb/**).
    # Runs: test_mastermind_context, test_confluence, test_spine_query, test_ask_brain,
    # test_world_state — all 163 tests green on origin/main e0e9f71cc6 (red-team lens 1).
    # Also: test_neuralweb_health (PR-C health builder — weekend-aware staleness,
    # collect-cadence as_of exclusion, staleness_from override, placeholder rollup).
    # Also: test_neuralweb_daily_brief (previously run by zero workflows; wired with
    # the cortex-warn-floor change so _brief_status is CI-guarded).
    # Also (W-AI orchestrator face): test_orchestrator_log (run-log builder + reviews),
    # test_mastermind_feedback (v3 reverse bridge; file predated this job and was run by
    # ZERO workflows), test_admin_orchestrator (Master Brain admin routes + bot proxy).
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install deps
        # requests is load-bearing here for TWO independent reasons — keep it on this line.
        # 1. test_ask_brain.py::test_china_flows_makes_no_network_call patches requests.post/get
        #    (HARD CONTRACT: no live call in the chat path) and patch() imports its target, so a
        #    venv without requests raises ModuleNotFoundError. That red is the honest signal of
        #    an environment regression and must stay loud — #3703 muffled it with importorskip
        #    and was reverted (#3715). The dep landed in #3694; same gap class as jinja2 (#3660).
        # 2. Import fidelity: test_admin_orchestrator and test_admin_prophet import admin.server,
        #    which pulls admin.analytics_first_party, which does
        #    `try: import requests / except: requests = None`. Drop the dep and the lane silently
        #    exercises that degraded branch instead of the shipped one.
        run: pip install pytest pandas numpy pyarrow pyyaml jinja2 requests
      - name: neural-web core suite (mastermind context + confluence + spine query + ask brain + world state + R5 macro context + ADB-W1 brief context + ADB-W2 forward calendar + Analyst OS P0 + W2)
        run: python -m pytest tests/test_mastermind_context.py tests/test_confluence.py tests/test_spine_query.py tests/test_ask_brain.py tests/test_company_intelligence_neural_reader.py tests/test_company_intelligence_workspace_chain.py tests/test_company_intelligence_event_workspace.py tests/test_company_intelligence_event_compiler_e3a.py tests/test_company_intelligence_qa_reconstruction.py tests/test_company_intelligence_qa_exchange.py tests/test_company_intelligence_qa_generalization_e3c.py tests/test_refresh_event_workspaces.py tests/test_issuer_profiles_a5a.py tests/test_world_state.py tests/test_macro_snapshot.py tests/test_macro_context_authority.py tests/test_law_dates.py tests/test_confluence_strength.py tests/test_neuralweb_health.py tests/test_neuralweb_daily_brief.py tests/test_brief_context.py tests/test_forward_calendar.py tests/test_orchestrator_log.py tests/test_mastermind_feedback.py tests/test_admin_orchestrator.py tests/test_admin_prophet.py tests/test_admin_trade_memory.py tests/test_macro_thesis.py tests/test_brain_doctrine.py tests/test_mastermind_response_log.py tests/test_admin_mastermind_logs.py tests/test_brain_analyst_doctrine.py tests/test_brain_analyst_wiring.py tests/test_market_packet.py tests/test_brain_market_intel.py tests/test_brain_seed_router.py tests/test_brain_analogues.py tests/test_brain_curve.py tests/test_response_eval.py tests/test_brain_user_memory.py tests/test_user_prefs.py -q

  cn-standout-audit:
    if: ${{ false }}
    gate: code
    # SA-W2: CN PIT regime store + species_id + two-axis attribution + fitness card.
    # Tests: lane fail-closed, keep-first, species_id mapping, axis precedence, us_proxy
    # isolation, premature_stop_noise unimplemented (F5), never-raise, data_gap honesty,
    # F1 regime-stamp sequencing, F2 window-unit Wilson, F6 ticks-based timing.
    # F9: test_china_standout_track added — covers species_id/regime-stamp changes.
    #
    # CURATED SCOPE (2026-08-15 wave 4). `scope: exclusive` makes the paths below
    # REPLACE inference. This job had NO owned tier at all — every one of its 28
    # inferred patterns was opaque fallback, so it selected on `*` and on whole
    # trees it never reads. Cause: #5754 gave scripts/build_china_library.py an
    # import of engine/china_intel_interest.py, which lazy-imports china_intel_hub
    # and reads the full altdata convergence universe. That subtree's own opaque
    # constructs then widened this job's scan roots to the fallback tier, and it
    # began matching templates/index.html — a file no suite here reads. Measured:
    # that smear was one of the two +1s that took the index.html probe from 127
    # to 129 in tests/test_ci_pack.py::test_exclusive_curation_narrows_ordinary_code_prs,
    # which was re-based to absorb it rather than fixed. This is that fix.
    #
    # THE GLOBS ARE EARNED, and each names the construct that earns it:
    #   engine/** + scripts/**  — test_cn_board_lane_gate.py:361 and
    #     test_cn_entry_price_integrity.py:527 rglob("*.py") over BOTH trees and
    #     AST-scan every hit for unlaned CN sink calls. The trees are those two
    #     guards' SUBJECT, not their incidental dependency: a new script carrying
    #     a forgotten `lane=` is exactly what they exist to catch, so enumerating
    #     either one would make this job stop covering the files it is for. This
    #     is why scripts/ is globbed here and ENUMERATED in coiled-mtf-anchor-era,
    #     whose two suites contain no traversal at all.
    #   collectors/**  — scripts/build_china_library.py:1807 runs
    #     importlib.import_module(_mod).refresh() over a tuple of collectors.*
    #     drip modules. The walker cannot resolve the loop variable, so those
    #     seven tushare_* modules are NOT in the static closure and the test below
    #     cannot see them: enumeration would silently drop them. The dynamic
    #     targets are all collectors.*, so this glob is the honest width of that
    #     construct — the rest of its CODE_SCAN_ROOTS widening (templates/**,
    #     site/**/*.py, docs/**, tools/**) is the smear being removed.
    #   config/** + data/**  — engine/smart_money.py (x5), lib/ai_costs.py:417 and
    #     collectors/base.py:124 traverse roots=config,data for .parquet/.jsonl,
    #     and scripts/seed_china_ths_baskets.py:318 traverses data/ with NO suffix
    #     filter. Runtime reads the static closure cannot enumerate.
    #
    # ENUMERATION IS SAFE ONLY WHERE THE TEST CAN SEE. lib/, research/ and the
    # named suites are reached by ordinary static import, so a newcomer is caught
    # loudly by test_curated_exclusive_scopes_cover_their_own_import_closure. The
    # site/ artifacts are runtime JSON the test canNOT re-derive, so the five
    # china-only trees (1-4 files each) stay globbed; only the three files inside
    # the shared basketdata/ (49 files) and factordata/ (310) trees are named, to
    # keep 356 unrelated artifacts out of this job's scope.
    #
    # WIDENING IS THE SAFE DIRECTION. Exclusivity SKIPS inference, so a closure
    # file with no matching pattern is a job that stops running when its own
    # dependency changes and reports green forever. Do not hand-prune this list:
    # when a new import lands, the test above names the exact uncovered file.
    # Do not add templates/**.
    runs-on: ubuntu-latest
    scope: exclusive
    paths:
      - "engine/**"
      - "scripts/**"
      - "collectors/**"
      - "config/**"
      - "data/**"
      - "lib/__init__.py"
      - "lib/ai_costs.py"
      - "lib/closes_panel.py"
      - "lib/cn_calendar.py"
      - "lib/config.py"
      - "lib/filing_value_units.py"
      - "lib/nyse_calendar.py"
      - "lib/store.py"
      - "lib/ticker_aliases.py"
      - "lib/ticker_popularity.py"
      - "research/US_BOARD_MEASUREMENT.md"
      - "site/china_intel/**"
      - "site/chinaaltdata/**"
      - "site/chinanews/**"
      - "site/chinaradar/**"
      - "site/chinaspecialdata/**"
      - "site/basketdata/fear_greed.json"
      - "site/factordata/basket_washout_state.json"
      - "site/factordata/china_standouts.json"
      - "tests/test_china_standout_track.py"
      - "tests/test_cn_board_lane_gate.py"
      - "tests/test_cn_entry_price_integrity.py"
      - "tests/test_cn_standout_audit.py"
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install deps
        # markupsafe: the integrity step below imports scripts.build_china_library, whose
        # module graph reaches engine/i18n.py -> markupsafe. Absent, pytest dies COLLECTING
        # (exit 2) — locally invisible because dev envs carry it, and the sibling jobs that
        # import the builder (e.g. unrun-picks-boards) get it transitively via jinja2. With
        # it added, every suite in this job passes in a from-scratch minimal venv.
        run: pip install pytest pandas numpy pyarrow pyyaml markupsafe
      - name: SA-W2 CN standout audit tests
        run: python -m pytest tests/test_cn_standout_audit.py tests/test_china_standout_track.py -v -q
      - name: CN entry-price integrity (corrupt-bar gate, PIT latch, fail-closed lane — PR 5000)
        run: python -m pytest tests/test_cn_entry_price_integrity.py -q
      - name: CN board-append lane gate (fail-closed CN_LANE — board/ripening/audit/event-latch)
        run: python -m pytest tests/test_cn_board_lane_gate.py -q

  workflow-yaml:
    if: ${{ false }}
    gate: data
    # Every .github/workflows/* file must load as YAML and carry on: + jobs:.
    # An unparseable workflow file makes GitHub stamp a FAILED zero-job run on
    # every push to every branch carrying it (metabolism-agenda.yml col-0
    # incident: ~325 failed runs 07-10→07-11, introduced #2135, repaired #2260).
    # dag-conformance above only yaml-loads workflows declared as dag.yml lanes,
    # so non-lane workflow files had no parse guard at all — this closes that gap.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      # Deps grew 2026-07-30 (#3977). This job was `pytest pyyaml` — right for the
      # workflow-YAML steps — but OIP E8 attached five options suites whose assertions are
      # DATA-layer: they build DataFrames, round-trip parquet, and drive builders that
      # render templates and fetch over HTTP. Only the first added step ran before ci-pack-1
      # died, so its `No module named 'jinja2'` hid four more steps that would have failed
      # the same way on pandas/jinja2/requests. Verified minimal by installing one package
      # at a time in a clean venv (2026-07-30): pytest+pyyaml alone collects none of the
      # four; +pandas leaves parquet writes failing; +pyarrow leaves 15 jinja2 and 11
      # requests failures; the set below runs all five green (31+52+38+33+23 = 177 tests).
      # numpy is transitive via pandas and named for the record, as causal-factory does.
      # NOT patched with importorskip anywhere — a suite that skips in the only job naming
      # it executes NOWHERE while reading green, which is the class the
      # check_skip_only_suites.py steps below exist to catch.
      - name: install minimal deps
        run: pip install pytest pyyaml pandas numpy pyarrow jinja2 requests
      - name: check_workflow_yaml selftest
        run: python scripts/check_workflow_yaml.py --selftest
      - name: "all workflow files must parse with on:/jobs: blocks"
        run: python scripts/check_workflow_yaml.py .github/workflows
      # ci.yml's Wave B job graph (2026-08-11), wired here because the step above is
      # the closest existing owner: both read .github/workflows/ as their subject and
      # both need only pytest+pyyaml, already installed above. A parse gate proves the
      # file LOADS; this proves it still says what it has to say. `ci-plan` now emits
      # the pack matrix, so a PR may publish a SUBSET of ci-pack-0..11 and `ci-gate`
      # is the only name that concludes on every non-closed event — the affirmative
      # success `scripts/merge_on_green.py` requires (#4779, absence of red is not a
      # pass). Deleting `ci-gate`, or swapping the immutable PR base SHA for a branch
      # name in the planner's diff, are both one-token edits that stay green forever
      # without this step. Executes ci-gate's shell for real (inline shell is
      # untested code otherwise); no importorskip, no data/ or site/ reads, so it
      # cannot skip its way to a green on a pack runner.
      - name: ci-plan/ci-pack/ci-gate workflow structure contract
        run: python -m pytest tests/test_ci_plan_workflow.py tests/test_ci_semantic_proof.py tests/test_ci_pack_semantic.py -q
      - name: hosted-runner packing contract
        run: python -m pytest tests/test_ci_pack.py tests/test_merge_on_green.py tests/test_merge_on_green_semantic.py tests/test_check_conflict_markers.py tests/test_release_hold_text.py -q
      # The merge-gate reliability report is the progress metric for
      # research/CI_MERGE_GATE_RELIABILITY_ROOT_CAUSE_2026_08_19.md, so a silent
      # regression in it would make that program's acceptance gate meaningless.
      # It belongs to this job because it reads the same manifest the packing
      # contract above reads. Its suite is offline and sparse-safe (the coupling
      # half never shells out; the green-rate half degrades to a stated skip), so
      # it cannot skip its way to green on a pack runner with no `gh`.
      - name: merge-gate reliability report contract
        run: python -m pytest tests/test_ci_gate_reliability_report.py -q
      - name: transitional self-hosted runner policy and canary contracts
        run: python scripts/check_runner_policy.py && python -m pytest tests/test_runner_policy.py tests/test_ci_canary_tools.py tests/test_ci_canary_workflows.py tests/test_trusted_ci_executor_workflow.py tests/test_trusted_ci_production_route.py -q
      # The US nightly's cron redundancy (2026-08-13), wired here for the same reason
      # the two steps above are: its subject IS .github/workflows/ — specifically the
      # asymmetry that asia-close.yml carries five cron slots and daily.yml carries one
      # DST pair, which is why CN self-heals a lost fire and the US lane lost two whole
      # sessions on 2026-08-11/12. The backstop is an ACTOR, so its suite is mostly the
      # cases where it must NOT fire; test_primary_still_has_only_the_dst_pair reds if
      # daily.yml ever grows its own backstop crons and this lane becomes double-firing.
      - name: nightly backstop selftest (scripts/nightly_backstop.py)
        run: python3 scripts/nightly_backstop.py --selftest
      - name: nightly backstop dispatch rule (must never fire over a live run)
        run: python -m pytest tests/test_nightly_backstop.py -q
      # 2026-08-12: the terminal-CI-handoff step that used to sit here is GONE with the
      # rule it guarded. A worker no longer stops on an armed pull request — it owns
      # commit -> push -> PR -> CI -> squash-merge -> live verification — so there is no
      # release verdict left to pin and no receipt left to leak. What the retirement
      # must not lose is the sweeper: `tests/test_merge_on_green.py` in the step above
      # still guards the party that PERFORMS the merge, and the Stop hook's own half
      # (an armed pull request blocks rather than releases, and the spurious-check
      # predicate still agrees with the sweeper's copy) is pinned by
      # tests/test_ship_loop_guard.py, wired in its own job below.
      # SUITE DISCOVERY, which all three darkness guards share. It was
      # `tests/`-only until 2026-08-06, so the suites most likely to be dark were
      # the ones no census could see: a research packet fenced to files-only writes
      # its guard suite NEXT TO the instrument under research/, and #4693 measured
      # two such suites (22 tests) named by no `run:` step since they landed —
      # never executed, while every census reported the repo covered. Discovery is
      # now the whole tree, classified by what pytest would COLLECT rather than by
      # filename: three test_-shaped files under research/ are CLI measurement
      # instruments that collect nothing, and reporting those as work items is how
      # a census stops being read. Selftest first, same reason as the two below.
      - name: audit_unrun_tests selftest
        run: python scripts/audit_unrun_tests.py --selftest
      # ARMED 2026-08-09. The census reported for three weeks and the backlog did not
      # move: 969 unrun suites of 2,146 on the day it was armed, while six separate PRs
      # (#4567, #4589, #4709, #4710, #4800, #4826) each un-darkened ONE suite by hand.
      # An enumeration nobody is obliged to read keeps re-creating the trap it measures,
      # so the exit code now carries the finding. RED only for a suite that is unrun and
      # in NEITHER file: config/unrun_test_baseline.json (the pre-gate backlog,
      # shrink-only — rows leave when a suite is wired or deleted, nothing is ever
      # added) or config/unrun_test_waivers.yml (path -> reason, for a suite that must
      # stay unrun with an owner and an argument). Rows in either file that no longer
      # read as unrun WARN to be pruned; they never red, so the PR that wires a suite is
      # never punished for wiring it. Grandfathering the 969 rather than gating them is
      # deliberate — running them all would blow the ci-pack budget; what this buys is
      # the ratchet on the 970th. Enumeration stays EXPLICIT for the same reason the
      # install-line comments above give: a glob runner would auto-run unvetted suites
      # and re-create the ERROR-at-collection class those comments document, so a new
      # suite is wired into the job that owns its subject, by hand, on purpose.
      - name: "no suite may be named by zero run: steps (baseline-gated)"
        run: python scripts/audit_unrun_tests.py
      - name: unrun-census discovery unit tests
        run: python -m pytest tests/test_audit_unrun_tests.py -q
      # scripts/check_contract_delta.py (2026-08-19) is the differential PR-vs-base
      # gate that re-derives the two finding classes above (curated-exclusive
      # import-closure misses, unwired suites) UNCONDITIONALLY of ci-pack's path
      # scoping — see its module docstring for the incident that motivated it
      # (#5872/#5932 merged green because their packs never ran the two suites
      # this job already runs above). Wired HERE, right after the audit_unrun_tests
      # steps, for the same reason every other suite in this job is: it would
      # otherwise be an unrun suite named by no run: step, caught by the
      # audit_unrun_tests.py step right above it.
      - name: differential contract-delta gate contract
        run: python -m pytest tests/test_contract_delta.py -q
      # SKIP-ONLY suites: a test that skips in every job naming it executes
      # nowhere, so a job list and a green run both lie about it. This is what an
      # `importorskip` added to clear a red lane creates by construction — it
      # happened in #3703 and again in #3722→#3738→#3760 — and no other census
      # measures it: audit_unrun_tests.py asks "is this suite named by a job",
      # never "does that job install what the suite needs to run".
      # The selftest runs FIRST: a detector that has quietly stopped detecting
      # would otherwise report a clean repo forever.
      - name: check_skip_only_suites selftest
        run: python scripts/check_skip_only_suites.py --selftest
      - name: no suite may skip in every job that names it
        run: python scripts/check_skip_only_suites.py
      - name: skip-only detector unit tests
        run: python -m pytest tests/test_check_skip_only_suites.py -q
      # TRIGGER GAPS: the reverse direction of audit_unrun_tests.py. That census
      # asks "can ANY edit start this job" — an any() over the closure, satisfied
      # by the test file itself — so a suite stays "triggerable" while the ONE
      # module it exists to guard is matched by no path entry. The lesson has been
      # written in comments here four times (#3488, #4559, and the "both halves
      # listed on purpose" notes through ci.yml) and never enforced; prose in a
      # 1,700-entry path list is not a guard. Selftest first, same reason as above.
      - name: check_ci_trigger_closure selftest
        run: python scripts/check_ci_trigger_closure.py --selftest
      - name: every guarded suite's subjects must reach its own trigger paths
        run: python scripts/check_ci_trigger_closure.py
      - name: trigger-closure guard unit tests
        run: python -m pytest tests/test_ci_trigger_closure.py -q
      # render.yml's per-scope publish wiring. #3718 fixed the international
      # dashboard never publishing because `intl` was missing from the render
      # lane's scope list, and shipped this suite as the guard — but no `run:`
      # step named it, so the guard for that fix was itself dark from the hour
      # it landed. Same class the unrun-* lanes close; caught by
      # scripts/audit_unrun_tests.py on the very next census.
      - name: render-lane scope contract (intl must stay in the publish scopes)
        run: python -m pytest tests/test_render_intl_scope.py -q
      # A run: scalar over GitHub's length cap makes GitHub DE-REGISTER the workflow.
      # render.yml vanished from the registry on 2026-07-27 (#3826) and the express lane
      # was dead ~1h. yaml.safe_load, check_workflow_yaml.py and actionlint ALL pass on an
      # over-cap file, and the owning PR's checks stay green — the failure only shows in
      # the BRANCH run list, as a zero-job run titled by PATH instead of by `name:`. This
      # is the only gate that can catch it pre-merge. The ownership suite in the
      # same step pins the positive builder allowlist that keeps unrelated entrypoints
      # out of the scarce self-hosted render queue.
      - name: "render workflow size ceiling and builder ownership"
        run: python -m pytest tests/test_render_run_size_cap.py tests/test_render_builder_ownership.py -q
      - name: render publication dead-asset fences
        run: python -m pytest tests/test_render_dead_asset_gate.py -q
      # Same class, second instance (2026-07-27). build_canada was invoked by NO
      # workflow at all: site/canada.html existed only as a side effect of a
      # swallowed try/except inside build_vector, so a canada-only fix could ride a
      # dispatched scope that never calls hub() and ship stale behind a green run —
      # exactly how macro#3820's recovery ended up needing scope=hk. The suite runs
      # render.yml's `region_of` shell function for real (inline shell is untested
      # code otherwise) and pins the daily/weekly step ordering. stdlib + bash only,
      # so the minimal pytest+pyyaml install above is enough.
      - name: render-lane scope contract (canada must be first-class, not a build_vector hook)
        run: python -m pytest tests/test_render_canada_scope.py -q
      # OIP E8 (2026-07-29). Same #3718 class as the intl suite above: this PR shipped
      # five guards and, on first push, NO `run:` step named any of them — the
      # options-workspace suite appeared only in a render.yml YAML COMMENT, which is
      # enough to satisfy audit_unrun_tests.py's text matcher (tightened in the same
      # commit to read `run:` steps only, so a comment can no longer fake coverage).
      # Deps: see the install step's note — these five need the data stack, not just
      # pytest+pyyaml. The express-lane suite itself is stdlib + bash (it shells out to
      # execute render.yml's `region_of` for real) and its one RUNTIME probe stubs jinja2
      # when absent rather than skipping, so it stays runnable in a thin env too.
      - name: options express-lane coverage contract (workspace + screener must stay wired)
        run: python -m pytest tests/test_render_options_workspace_scope.py -q
      # #3721 weekend-row class. The options stores accrue non-session rows that RECOMPUTE
      # IV/spot/walls off a stale price; unfiltered they corrupt .iloc[-1], percentile
      # windows and positional lookbacks — including the n_obs count that gates
      # call_skew_rich into the CROWDED state.
      - name: options readers must session-filter dated stores
        run: python -m pytest tests/test_options_session_guards.py -q
      # The ×100 class, hit twice. Both incidents survived tests whose FIXTURES encoded
      # the bug, so this suite asserts against fixture-shaped AND prod-shaped inputs and
      # simulates a newest-vintage-only flip.
      - name: options ingestion seams must assert percent-vs-fraction
        run: python -m pytest tests/test_options_unit_seams.py -q
      # R8 one coverage shape across the four family builders; pins the honesty rules
      # (unknown stays None, never a fabricated 100%; staleness is calendar-derived).
      - name: options coverage object contract
        run: python -m pytest tests/test_options_coverage_object.py -q
      # E5: daily.yml invokes collectors.equity_earnings BARE, and that module's __main__
      # used to be a three-ticker smoke test — the whole-universe sweep had never run.
      # Pins the entry point AND the coverage-aware tripwire that could not see it.
      - name: earnings sweep entry point + coverage-aware tripwire
        run: python -m pytest tests/test_earnings_sweep_entrypoint.py -q
      # FD-EXP (2026-07-30). Third instance of the same class: build_flow_desk was
      # daily.yml-ONLY, so flow_desk.html/json + flowdata/cohorts.json refreshed once a
      # night while both express lanes rebuilt the stores around them (#3515 flow_leaders,
      # #3487 research_vault). Admitted to cl_gex in both lanes once #4007 lane-gated the
      # builder's three data/ writes; this suite pins the wiring AND that gate, since
      # deleting the gate would quietly turn every express render into a ledger writer.
      # stdlib + pytest only (reads workflow/dag/builder text; imports no builder).
      - name: render-lane scope contract (flow_desk must stay express-wired)
        run: python -m pytest tests/test_render_flow_desk_scope.py -q
      # Sparse session-worktree profile (policy R8). #5531 landed the suite
      # named by no run: step — the same class the census above now gates —
      # so pack-1 went red on every PR the moment it merged. It belongs HERE
      # with the other worktree/CI-profile steps: its subject is
      # scripts/worktree_sparse.py + the WorktreeCreate hook, and the sibling
      # vacuous-refusal suite below already owns the R8 honesty rule. stdlib +
      # pytest; synthetic git repos under tmp_path, no data/ or site/ reads.
      - name: sparse session-worktree profile + Warp conversation-carrier lifecycle
        run: python -m pytest tests/test_sparse_worktree_profile.py tests/test_warp_session_start_sparse.py -q
      # Same subject (scripts/worktree_sparse.py), same job, for the same reason
      # the step above exists: #5531 landed its suite named by no run: step and
      # pack-1 went red on every PR the moment it merged. This suite owns the
      # lock preflight and the truncation repair — a `sparse-checkout add`/`full`
      # SIGKILLed by its own subprocess timeout leaves a 0-byte file over a real
      # committed artifact plus an orphaned index.lock, and the repair must never
      # revert a session's uncommitted work while fixing it. stdlib + pytest;
      # synthetic git repos under tmp_path, no data/ or site/ reads.
      - name: sparse worktree add/full lock preflight + truncation repair (never worse than it started)
        run: python -m pytest tests/test_worktree_sparse_add_lock_safety.py -q
      # VACUOUS guards: the same lie audit_unrun_tests.py catches, one layer down.
      # A suite that never runs is caught above; a guard that RUNS, walks a tree
      # that is not checked out, finds nothing and prints its affirmative OK is
      # not — it reports green over an empty set. Session worktrees are sparse by
      # default (policy R8, scripts/worktree_sparse.py), and seven check_*.py
      # guards did exactly that when measured 2026-08-13. The refusal is
      # conditional on an EMPTY result set, never on sparseness alone: the 13
      # lanes that check out partial trees on purpose (live-quotes.yml,
      # marketing-hot-tape.yml and friends) invoke none of these seven and must
      # keep passing regardless. One suite owns that conjunction for all seven
      # guards rather than being split across the seven jobs that own them.
      # Deliberately LAST in this job: a failing step masks every step after it,
      # and a new census must never be the thing that hides the established
      # wiring guards above.
      - name: guards must refuse, not pass vacuously, on a sparse tree
        run: python -m pytest tests/test_sparse_guard_refusals.py -q

  causal-factory:
    if: ${{ false }}
    gate: data
    # Causal Hypothesis Factory (CHF) + Signal Foundry brainstorm: all ten test files
    # below were run by ZERO GitHub workflows before this job — the same 'merged green
    # because no CI job covered X' rot class as #1052 / #1193 / #1195 and the neural-web
    # job above.  All 296 tests (1 env-gated slow-sweep skip) verified green in a clean
    # venv with exactly the deps installed here.
    #
    # Dep notes: scipy is needed only because test_sf_brainstorm imports
    # scripts.run_signal_foundry_brainstorm → engine.signal_foundry (package __init__)
    # → harness.py → scipy.stats.spearmanr.  No anthropic install: the LLM client is
    # fully mocked in test_causal_llm_runner / test_causal_brainstorm, and
    # engine.llm_auth is stdlib-only at module level.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install minimal deps
        run: pip install pytest pandas numpy pyarrow pyyaml scipy
      - name: causal factory + signal-foundry brainstorm suites
        run: >
          python -m pytest
          tests/test_causal_llm_runner.py tests/test_causal_frontier.py
          tests/test_causal_brainstorm.py tests/test_causal_lab_admin.py
          tests/test_causal_audit.py tests/test_causal_inventory.py
          tests/test_causal_schema.py tests/test_causal_scout.py
          tests/test_causal_targets.py tests/test_sf_brainstorm.py -q

  rlt-rebalance-calendar-pulse:
    if: ${{ false }}
    gate: code
    # RLT-R1/R2: rebalance calendar organ + rebalance pulse detector.
    # Hermetic tests: synthetic fixtures, no network, no live data stores.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install deps
        run: pip install pytest pandas numpy pyarrow pyyaml
      - name: rebalance calendar + pulse tests
        run: python -m pytest tests/test_rebalance_calendar.py tests/test_rebalance_pulse.py -q

  nyse-calendar-freshness:
    if: ${{ false }}
    gate: code
    # lib/nyse_calendar.py — the exchange-calendar reference every freshness SLA
    # is built on (expected_last_session / sessions_behind), plus the import-name
    # tripwire.
    #
    # Wired 2026-07-27: test_nyse_calendar.py was named by NO job in ANY workflow,
    # so the calendar's rule arithmetic was UNRUN while ~20 scripts imported it.
    # That darkness is what let build_flow_leaders and build_leader_radar both
    # import a `trading_dates_between` this module never defined — swallowed by a
    # bare `except Exception: return False`, making two >2-session stale SLAs
    # return "fresh" for every input they ever saw.
    # test_nyse_calendar_import_names.py resolved every
    # `from lib.nyse_calendar import X` in the repo statically so that class of
    # dead import fails a test instead of degrading into an always-false gate.
    #
    # PROMOTED repo-wide into tests/test_first_party_import_names.py: the same sweep
    # over EVERY first-party root found five more dead imports of the identical shape
    # — `from engine import config` (no such module; the except substituted a
    # __file__ parent-walk), `from engine.data_store import store` (the store is
    # lib.store; both HK and CN pick-lab benchmark loaders returned None forever),
    # `from lib.logging import get_logger` (never existed), plus a permanently-skipped
    # test and a dead fixture branch. The single-module guard is retired; its
    # calendar public-API check moved into test_nyse_calendar.py.
    # The first two suites are stdlib-only (ast + pathlib). The third
    # (test_sp1_trading_day_index.py, SP1-B 2026-08-15) imports the study
    # module and needs pandas/numpy/pyyaml; those join the install line
    # below. A suite named by no run: step reds workflow-yaml.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install minimal deps
        run: pip install pytest pandas numpy pyyaml
      # tsx_calendar joins its NYSE sibling here (2026-08-17): same subject — an
      # exchange calendar sitting under a freshness SLA — same stdlib-only, hermetic
      # shape, and the same reason. It is the session reference
      # scripts/check_nightly_liveness.py check D grades the Canadian board against,
      # so its rule arithmetic must not be UNRUN the way test_nyse_calendar.py was
      # until 2026-07-27.
      - name: nyse + tsx calendar rules, first-party imported-name resolution
        run: python -m pytest tests/test_nyse_calendar.py tests/test_tsx_calendar.py tests/test_first_party_import_names.py -q
      # SP1-B: restrict_to_nyse_sessions must refuse a weekend-contaminated
      # price index. Belongs HERE — the filter is session_rows + is_session,
      # which this job already owns. Hermetic (synthetic dates, no data/).
      - name: SP1-B trading-day price index refuses a contaminated calendar
        run: python -m pytest tests/test_sp1_trading_day_index.py -q

  dag-conformance:
    if: ${{ false }}
    gate: code
    # W5a (Neural Web): config/dag.yml declares the per-workflow-step lane inventory;
    # scripts/check_dag_conformance.py diffs the declared sequences against the LIVE
    # .github/workflows/ files and exits 1 on any mismatch not covered by a divergences
    # entry.  ANY undeclared drift between dag.yml and the real workflow now reds CI here
    # before merging — ending the hand-sync problem.
    #
    # Three checks in sequence:
    #   1. Hard: check_dag_conformance.py against the repo (exits 1 on undeclared drift)
    #   2. Hard: pytest tests/test_dag_conformance.py (selftest + live conformance assertion)
    #
    # Suspect divergences count as covered (declared), but are PRINTED in every run
    # so they stay visible.  The suspect list may only SHRINK (only-shrinks ratchet).
    #
    # Triggers on: .github/workflows/**, config/dag.yml, the checker, and the test file.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install minimal deps
        run: pip install pytest pyyaml
      - name: DAG conformance check (hard — exits 1 on undeclared lane drift)
        run: python scripts/check_dag_conformance.py --verbose
      - name: DAG conformance selftest + live conformance tests
        run: python -m pytest tests/test_dag_conformance.py -q
      # Same family, one layer down: dag.yml pins WHAT the nightly lane runs,
      # this pins that the lane's timeout still lets it finish. tech_lab_offrender
      # was cancelled at its 40m cap every night 2026-07-19→07-26 — the declared
      # step sequence was perfectly conformant, and the publish tail never ran.
      - name: nightly lane budgets (tech_lab_offrender publish tail)
        run: python -m pytest tests/test_tech_lab_offrender_budget.py -q
      # Third layer of the same family: W2 runtime telemetry (masterplan
      # 2026-08-06). Every self-hosted daily.yml job must carry the job-start
      # mark as its FIRST step and the always() timings/85%-tripwire finish as
      # its LAST, the finish cap argument must equal the job's timeout-minutes
      # (a cap raise that skips the arg silently rescales the tripwire — the
      # tech_lab stale-label class), and a synthetic 86% run must trip the
      # line-start ::warning (masterplan §0.2 acceptance gate).
      - name: nightly runtime telemetry (W2 wiring + 85% tripwire behavior)
        run: python -m pytest tests/test_nightly_timings.py -q
      # Fourth layer, same family: daily.yml's schedule is a DST cron PAIR and the
      # root et_gate job keeps exactly one firing. The suite EXTRACTS the inline gate
      # script out of the live workflow and executes it against instants either side
      # of both DST flips, then pins the cron pair to the script's constants and that
      # every job still reaches the gate. A drifted gate skips the sole authoritative
      # nightly with nothing going red — the same silent class as the two above.
      - name: daily ET regime gate (DST pair disambiguator + wiring pins)
        run: python -m pytest tests/test_daily_et_gate.py -q
      # 2026-08-12 (#5416): a workflow FILE over ~512,000 bytes stops being
      # processed SILENTLY — cron stops firing and dispatched runs queue with
      # zero jobs, uncancellable. The budget test reds growth before the cap.
      - name: workflow file size stays under GitHub's silent processing cap
        run: python -m pytest tests/test_workflow_file_size.py -q
      # Same incident, other half. The size guard stops daily.yml being stranded
      # AGAIN; this one notices when the nightly stops producing runs for ANY
      # reason — strand, cancellation, or a green run that never advances the
      # store. The 2026-08-11/12 outage ran two full sessions unreported because
      # every instrument we had measured DATA staleness on weekend-safe budgets.
      - name: nightly liveness guard selftest (scripts/check_nightly_liveness.py)
        run: python3 scripts/check_nightly_liveness.py --selftest
      - name: nightly liveness dead-man switch (checks + wiring pins)
        run: python -m pytest tests/test_nightly_liveness.py -q

  push-retry-policy:
    if: ${{ false }}
    gate: code
    # scripts/ci/push_retry.sh is the shared retry policy that EVERY lane's commit→push
    # step sources (render, engine-render, daily ×13, closing-bell, asia-close ×2,
    # weekly, earlyclose). It exists because render.yml run 30167139398 (2026-07-25)
    # built a correct scope=all render and then lost all five push attempts to
    # "cannot lock ref 'refs/heads/main'" — a lost RACE for the ref, not a conflict —
    # discarding ~95 minutes of work. A regression here is silent and expensive: a lane
    # that stops telling contention from a conflict, or slides back to a deterministic
    # ladder, just throws renders and ledger days away under a red X that reads like a
    # merge problem.
    #
    # The suite drives the library in a real `bash -eo pipefail` shell (GitHub's shell)
    # and replays the incident against real git repos, then asserts the LANES still
    # source it — so deleting the policy from a workflow reds here too.
    #
    # The same job now also covers the APPEND-ONLY BASE FENCE, which is the second
    # policy every lane's push loop sources from push_retry.sh. `-X theirs` is right for
    # the bulk market plane and catastrophic for an append-only ledger: on a file both
    # sides appended to it REPLACES the other side's rows. Measured twice on
    # data/government_revenue/ (2026-08-07 and 2026-08-18), the second orphaning a 26-row
    # candidate_ledger.jsonl issuance batch and reddening ci-pack-6 through ci-gate for
    # the whole fleet. The suite replays the real incident against real git repos and
    # pins the property that keeps it quiet — it looks only at members THIS run's commits
    # changed, because the 30-minute govrev-live lane appends to the same directory while
    # the nightly collect job is still running.
    #
    # pandas + pyarrow are here for that suite only: the fence's identity check reads the
    # committed parquet spine, and a stub would prove nothing about the real frames.
    #
    # Triggers on: .github/workflows/**, scripts/ci/push_retry.sh, tests/test_push_retry.py,
    # scripts/ci/append_only_base_fence.py, config/append_only_artifacts.json,
    # tests/test_append_only_base_fence.py.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install minimal deps
        run: pip install pytest pyyaml pandas pyarrow
      - name: push-retry policy tests (classification, budgets, jitter, real git races)
        run: python -m pytest tests/test_push_retry.py -q
      - name: append-only base fence (real-incident replay, withhold, quiet-night property)
        run: python -m pytest tests/test_append_only_base_fence.py -q

  transmission-chains:
    if: ${{ false }}
    gate: data
    # TXI W1/W3: the transmission chain library (knowledge/transmission/*.yaml), its compiler
    # (engine/transmission_chains.py), the runner (scripts/run_transmission_chains.py), AND the
    # W3 historical episode miner (engine/transmission_calibration.py + its runner).
    # The W1 suite exercises the episode state machine (arm→propagate→express / expire / fail),
    # idempotent same-day re-runs, the fail-loud strict validator + fail-open runtime loader,
    # and a real-artifact smoke over the four committed seeds (guarded on artifact presence).
    # The W3 suite exercises the vectorized-metric = point-in-time guarantee, hop-rate recovery
    # (M/N with n), regime split (per-cell rates + thin→untested), right-censoring, the
    # calibration merge (fill base_rates + tier promotion + rev-mismatch/absent fail-open), and
    # null honesty. Wired here AND in pull_request.paths above (the #3357 trap needs both).
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install deps
        run: pip install pytest pandas numpy pyarrow pyyaml jinja2
      - name: transmission chains — engine + state machine + validator + smoke
        run: python -m pytest tests/test_transmission_chains.py -q
      - name: transmission chains — W4 distribution (publish subset + Cascade Monitor + NW lobes)
        run: python -m pytest tests/test_transmission_publish.py tests/test_transmission_cascade_monitor.py tests/test_transmission_chains_nw.py -q
      - name: transmission calibration — miner + regime split + merge + null honesty
        run: python -m pytest tests/test_transmission_calibration.py -q
      - name: transmission proposals — W5 Loop-C (pack + ingest + gate + Article 1/2)
        run: python -m pytest tests/test_transmission_proposals.py -q

  research-factory-authority:
    if: ${{ false }}
    gate: code
    # RF-11 (Research Factory W1): authority guard — no Article-2 money-path
    # module (alert_triage / board_ordering / top_setups) may read
    # data/research_factory/ or import engine.research_factory.  The factory
    # is display-only (A0-A2 ceiling); factory outputs must never reach the
    # scored / board-rank / alert pipeline.  Runs alongside synapse-read-gate.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install minimal deps
        run: pip install pytest pyyaml
      - name: authority guard (hard fail on Article-2 forbidden reads)
        run: python scripts/check_research_factory_authority.py
      - name: authority guard selftest (gate fires on planted violation)
        run: python scripts/check_research_factory_authority.py --selftest
      - name: research-factory state + schema + Market Memory conformance tests
        run: >-
          python -m pytest
          tests/test_research_factory_state.py
          tests/test_research_factory_schema.py
          tests/test_research_factory_market_memory.py
          -q

  cycle-pattern-authority:
    if: ${{ false }}
    gate: code
    # CPI P2: authority guard — no Article-2 money-path module (alert_triage /
    # board_ordering / top_setups) may read data/cycle_pattern/ or import the
    # cycle-pattern lake.  The CPI substrate is display-only (context tier);
    # lake outputs must never reach the scored / board-rank / alert pipeline.
    # Mirrors research-factory-authority (line 509).
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install minimal deps
        run: pip install pytest pyyaml
      - name: authority guard (hard fail on money-path forbidden reads)
        run: python scripts/check_cycle_pattern_authority.py
      - name: authority guard selftest (gate fires on planted violation)
        run: python scripts/check_cycle_pattern_authority.py --selftest
      - name: cycle-pattern authority test suite
        run: >-
          python -m pytest
          tests/test_check_cycle_pattern_authority.py
          tests/test_cpi_h1_consumer_authority.py
          -q

  private-boundary:
    if: ${{ false }}
    gate: data
    # Mastermind Feedback Contract PR-A (FB-R12/R13): privacy-boundary guard.
    # Verifies that data/private/ and data/operator/ are gitignored, and that
    # committed feedback artifacts carry only counts — no tickers, fill/position/
    # decision IDs, host paths, dollar amounts, or secret env-var patterns.
    # Mirrors cycle-pattern-authority structure.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install minimal deps
        run: pip install pytest
      - name: privacy-boundary guard selftest (gate fires on planted violations)
        run: python3 scripts/check_private_boundary.py --selftest
      - name: privacy-boundary guard (hard fail on committed private fields or missing rail)
        run: python3 scripts/check_private_boundary.py
      - name: privacy-boundary test suite
        run: python3 -m pytest tests/test_check_private_boundary.py -q

  ruling-graph:
    if: ${{ false }}
    gate: code
    # Ruling Graph v1 (RUL-CL-1/6/7/8/11/12): canonical case-law registry guard.
    # Checks that:
    #   - committed site JSON + case-law doc match YAML regeneration (--check)
    #   - conflict checker finds no hard violations in changed files
    #   - conflict checker selftest fires on planted violations
    #   - all test_ruling_graph.py assertions pass
    # The graph is display-only (RUL-CL-12: authority_ceiling A1_EXPLAIN).
    # Mirrors cycle-pattern-authority pattern.
    # fetch-depth 0 + explicit base fetch: the checker needs `git merge-base HEAD
    # origin/main` for its diff-scoped H1/H2/W1 scans. On a shallow checkout there
    # is no base ref, the checker degrades to skip-diff-checks, and the job goes
    # green while guarding nothing — the #1195 shallow-cut incident class (see the
    # board-contradictions job above).
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install minimal deps
        run: pip install pytest pyyaml
      - name: build check (committed outputs match YAML regeneration byte-for-byte)
        run: python scripts/build_ruling_graph.py --check
      - name: conflict checker (hard fail on new FDR family or privacy boundary violations)
        run: |
          git fetch origin ${{ github.base_ref || 'main' }}
          RULING_DIFF_BASE="origin/${{ github.base_ref || 'main' }}" python scripts/check_ruling_conflicts.py
      - name: conflict checker selftest (gate fires on planted violations)
        run: python scripts/check_ruling_conflicts.py --selftest
      - name: ruling graph test suite
        run: python -m pytest tests/test_ruling_graph.py -q
      - name: model-routing guard hook matrix (HOUSE-U2 — deny/allow incl. FABLE-WHY workflow gate)
        run: python -m pytest tests/test_model_routing_guard.py -q
      - name: agent routing control matrix (HOUSE-U2 — ROUTE registry, commission + return contracts)
        run: python -m pytest tests/test_agent_routing_control.py -q

  contract-drift:
    if: ${{ false }}
    gate: data
    # NW Rails PR-7 (R4 contract governance): checks that each artifact in
    # site/factordata/contracts/artifact_manifest.json has not drifted from the
    # live artifact's actual top-level JSON fields.  A bot consuming china_standouts
    # with a stale schema contract has no handshake — this makes drift visible at PR
    # merge time rather than at trade time.
    #
    # Warn-first ratchet: exits 0 even on drift during the initial accrual week so
    # this never blocks a deploy on its first day.  Flip --warn-only to hard-fail
    # after one clean week: 2026-07-13.
    #
    # Wildcard artifacts (per_stock_intel, per_stock_signal) are skipped when no
    # live sample is present (normal on CI runners without the full stockdata store).
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install minimal deps
        run: pip install pytest pandas numpy pyarrow pyyaml
      - name: contract drift check (hard-fail — ratchet flipped 2026-07-14)
        run: python scripts/check_contract_drift.py
      - name: contract drift unit tests
        run: python -m pytest tests/test_contract_drift.py -q

  flow-cohorts:
    if: ${{ false }}
    gate: code
    # FL-B cohort flow aggregation (flow-continuity-program): aggregation correctness,
    # coverage honesty counts, idempotence, JSON schema, no-history raw_fallback.
    # Triggers on engine/flow_cohorts.py, scripts/build_flow_desk.py,
    # templates/flow_desk.html.j2, and the test file.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install minimal deps
        # jinja2: TestZeroCoverageRendersNoStanceWord renders templates/flow_desk.html.j2
        # to assert the panel prints no stance word on a zero-coverage day.
        run: pip install pytest pandas numpy pyarrow pyyaml jinja2
      - name: flow-cohorts unit tests
        run: python -m pytest tests/test_flow_cohorts.py -q

  flow-signals-ledger:
    if: ${{ false }}
    gate: code
    # FS-0 flow-signal ledger + outcome grader (FLOW_SIGNAL_ML_MASTERPLAN_BY_FABLE):
    # keep-first dedup, tz-normalization (aware-UTC enforcement),
    # PIT leak-injection (no future-data lookahead), split-seam guard,
    # CI-null (no creds / absent stores → clean no-op), grader parity
    # (outputs match engine/grading.py primitives directly), tz-aware parquet
    # round-trip (avoids the LETHAL naive/aware datetime64 mismatch class).
    # FS-4 scorer (Lane B): no-model path, keep-first dedup, kill-switch, decay
    # monitor, gate v2 shape, PIT guard, prefiltered_index_root, bucket_unmapped.
    # Triggers on collectors/flow_signals.py, engine/flow_signals_grade.py,
    # scripts/build_flow_signals.py, config/flow_detector.yml, test files,
    # lib/flow_score.py, config/flow_score.yml, and FS-4 test files.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install minimal deps
        run: pip install pytest pandas numpy pyarrow pyyaml boto3 scikit-learn joblib
      - name: flow-signals ledger + grader unit tests
        run: python -m pytest tests/test_flow_signals.py -q
      - name: FS-4 scorer unit tests (Lane B)
        run: python -m pytest tests/test_fs4_flow_scorer.py -q

  flow-signal-cohorts:
    if: ${{ false }}
    gate: code
    # FS-1 historical flow cohorts (ops lane, off-render):
    # Covers detector parity, eod_proxy PIT leak-injection, cohort separation guard,
    # tz-aware UTC round-trip, resume idempotence, concurrency cap, terminal-down.
    # CI is hermetic: no ThetaTerminal, no thetadata_eod store — all tests use
    # fixtures/tmp_path and mock the network (FS-0 CI-null pattern).
    # Triggers on ops_flow_cohorts.py, flow_recon_ladder.yml, sample fixture,
    # test file, and all FS-0 seams (shared detector/grader).
    # FS-2 atlas builder (ops lane, off-render):
    # Covers Wilson CI, cohort-separation guard, n-floor labeling, accrual path,
    # 0DTE index annotation, era hit-rate, premium decile, crowdedness.
    # FS-4 trainer (Lane A): map_model_bucket, uniqueness_weights, ECE, Brier,
    # reliability, deployable logic, manifest n_trials, SHA-256 verification,
    # lazy import, label column missing, expand_grid.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install minimal deps
        run: pip install pytest pytest-mock pandas numpy pyarrow pyyaml requests boto3 scikit-learn joblib
      - name: FS-1 flow cohorts unit tests
        run: python -m pytest tests/test_fs1_flow_cohorts.py -q
      - name: FS-2 atlas builder unit tests
        run: python -m pytest tests/test_fs2_flow_atlas.py -q
      - name: FS-0 downstream suite (shared seams)
        run: python -m pytest tests/test_flow_signals.py -q
      - name: FS-4 trainer unit tests (Lane A)
        run: python -m pytest tests/test_fs4_flow_trainer.py -q

  broad-etf-flows:
    if: ${{ false }}
    gate: code
    # RLT-R3: broad-market index ETF creation/redemption proxy.
    # Tests: proxy math + SO-jump guard (>25% clamped to NaN), causal z60 (no
    # lookahead, min_periods=2), schema of broad_flow_proxy.parquet, upsert
    # idempotency, partial-ticker robustness.
    # Triggers on engine/etf_flows.py, collectors/sponsors.py, test file.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install minimal deps
        run: pip install pytest pandas numpy pyarrow
      - name: broad ETF flow proxy unit tests
        run: python -m pytest tests/test_broad_etf_flows.py -q
      # Rides here because this is the sector/broad-ETF unit-test job with the
      # exact minimal deps the suite needs (pandas/numpy only — the harness
      # imports engine.validation, which needs nothing heavier). The suite is
      # 20 pure-function tests (universe dating, Friday-anchor arithmetic,
      # confirm rule, RS rank logic) for the S-MLC-3 phase-0 harness; it shipped
      # unwired in #6766 and contract-delta correctly refused the ci-gate.
      - name: S-MLC-3 weekly-wait-cost harness unit tests
        run: python -m pytest tests/test_s_mlc_3_weekly_wait_cost.py -q

  qledger-cluster-honest-ci:
    if: ${{ false }}
    gate: code
    # altdata-diagnosis PR-A: cluster-honest Wilson CI fix.
    # Proves that _aggregate and promotion_check compute wilson_ci_low on n_dates
    # (independent date clusters), not on the correlated n_obs, per the
    # ticker-cluster time-confound law and the altdata_brain.py article3 convention.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      # `requests` is load-bearing, not decoration: tests/test_w6_readiness_monitor.py
      # patches `scripts.notify.send_telegram`, and mock.patch RESOLVES its target by
      # importing the module — `scripts/notify.py` imports requests at module level and
      # does not guard it. Measured per-suite before wiring: test_qledger.py and
      # test_grade_qledger.py never load requests, the w6 suite always does, so without
      # this the new step ERRORS on import while every existing step here stays green.
      # WIDENING IS LEGAL HERE, and the distinction is the whole point of the rule it
      # looks like it breaks. "Never widen the pip line" is scoped to lanes whose thin
      # env is a DECLARED canary — dataos-foundation's pytest+pyyaml ("a pandas import
      # creeping into lib/dataos/ breaks HERE first", see house-law-registry below) and
      # the marketing/chart-render thin lanes. This job declares no such contract, and
      # no comment here turns on a dep being absent. What that rule actually forbids is
      # provisioning a new environment to buy green; this line is byte-identical to one
      # three other jobs already declare, so run_ci_pack.py reuses their venv and
      # provisions nothing. The alternative — relocating the w6 suite to one of those
      # three (altdata-new-sources, earnings-release-identity, group-earnings-pulse) —
      # would put the qledger promotion monitor in a job that does not own its subject,
      # which is the failure mode that left it unowned in the first place.
      - name: install minimal deps
        run: pip install pytest pandas numpy pyarrow pyyaml requests
      - name: qledger cluster-honest Wilson CI unit tests
        run: python -m pytest tests/test_qledger_cluster_honest_ci.py -q
      # P0a — the explicit horizon-clock contract. STEP PLACEMENT IS DELIBERATE:
      # run_ci_pack.py returns on the FIRST non-zero step, so a step placed first
      # MASKS its siblings and one placed last goes DARK behind an earlier red.
      # These two sit SECOND/THIRD — after the established cluster-honest step
      # (which stays unmaskable and is the broadest existing qledger check) and
      # ahead of the altdata/metric-validity steps, which test different concerns
      # and must not shadow the suite that covers the code this PR rewrote.
      # test_qledger.py is the primary regression suite for engine/qledger.py and
      # was named by NO legacy job, so every grading-path change shipped dark; it
      # is hermetic (tmp_path store + monkeypatched price layer, no data/** reads).
      - name: qledger contract regression suite
        run: python -m pytest tests/test_qledger.py -q
      - name: qledger explicit horizon-clock (P0a) + matched-control policy (P0d) contracts
        run: python -m pytest tests/test_qledger_horizon_clock.py tests/test_qledger_control_policy.py -q
      # P3 — the forward-only desk adapters and the evidence-clock writer. These
      # sit in the qledger job because their subject IS the claim substrate: the
      # adapter's forward-only gate calls `qledger.claim_window` (the same
      # resolver `grade_claim` uses) and its output is a `register_batch` call.
      # Placed AFTER the contract suites deliberately: if the clock contract
      # itself regresses, that must be the failure a reader sees first, not a
      # downstream adapter failing for a borrowed reason.
      # The nightly RUNNER over that substrate joins this step (2026-08-14). Both
      # suites were named by NO run: step in legacy-jobs.yml and appear nowhere in
      # ci.yml, so `scripts/grade_qledger.py` — the grader that writes every grade row
      # and the W6 readiness monitor that decides promotion — had no CI owner at all.
      # The cost was measured, not theorised: the w6 first-cross alert tests were red
      # on main from #5573 (2026-08-14 05:44Z) until #5609 healed them, with zero CI
      # signal for the whole window. They ride this step rather than a new one because
      # a step is the masking unit here (run_ci_pack.py returns on the first non-zero
      # step) while pytest reports EVERY file in one step — so extending the list adds
      # coverage without adding a place for a later step to go dark, and adds no job to
      # a manifest already at the narrow-diff ceiling. Placement inherits the rationale
      # above: strictly downstream of the contract suites, so a clock or _aggregate
      # regression is still the first failure a reader sees, never a runner failing for
      # a borrowed reason.
      - name: qledger forward-only desk adapters + evidence clock (P3) + nightly grader & W6 readiness runner
        run: python -m pytest tests/test_qledger_desk_adapter.py tests/test_qledger_evidence_clock.py tests/test_grade_qledger.py tests/test_w6_readiness_monitor.py -q
      # round 5 MINOR: this file was owned by NO legacy job, so
      # test_register_claims_are_salience_only_macro — which asserts on
      # register_claims()'s "returns the count REGISTERED OPEN" semantics
      # this branch's P0a market-dispatch fix redefined (a refused claim no
      # longer silently inflates the count) — never ran in CI. Sits right
      # after the horizon-clock step it is a direct consequence of.
      - name: communique_diff phrase-book + qledger registration contract
        run: python -m pytest tests/test_communique_diff.py -q
      # test_altdata_ledger_lane_gate.py rides THIS step rather than a new one: it pins
      # the same module (engine/altdata_ledger.py) the family suite already owns, and a
      # single pytest invocation reports every file in one place — extending the list
      # adds coverage without adding a job to a manifest at the narrow-diff ceiling, and
      # without adding a later step that could go dark behind an earlier red one. The
      # sibling altdata suite (tests/test_altdata.py) is grandfathered UNRUN in
      # config/unrun_test_baseline.json, so the gate contract had to land somewhere CI
      # actually executes or it would have shipped dark — which is the exact class of
      # defect it exists to catch.
      - name: altdata reboot W2 — per-channel family routing + placebo tests + HOUSE-U5 ledger lane gate
        run: python -m pytest tests/test_altdata_reboot_families.py tests/test_altdata_ledger_lane_gate.py -q
      # Metric-validity invariants (epistemics.qledger_metric_validity). grades.excess
      # is RAW, not direction-signed, so a pooled signed excess_mean over a
      # mixed-direction family measures universe drift rather than skill; a salience
      # family has no hit rate; a verdict may only be read at the declared horizon.
      # The selftest carries negative controls, so it fails if the auditor flags
      # everything as readily as if it flags nothing.
      - name: qledger metric-validity invariants
        run: python -m pytest tests/test_qledger_validity.py -q
      - name: qledger metric-validity guard selftest
        run: python scripts/check_qledger_metric_validity.py --selftest
      # V1 ENFORCEMENT at the emitter. The two steps above prove the CONTRACT
      # (engine/qledger_validity.py) is correct; this one proves engine.qledger
      # ._aggregate — the single chokepoint feeding site/qledger/track_record.json
      # and, via scripts/grade_qledger.compute_promotion_readiness, the admin
      # Experiments tab — actually OBEYS it, and that a legal single-direction
      # family is unchanged.
      # PLACEMENT (run_ci_pack.py returns on the first non-zero step): deliberately
      # LAST in this job. Placing it above the two metric-validity steps would let a
      # break in the enforcement mask the contract's own verdict, which is the
      # upstream authority; in the reverse order a red contract masks this step, and
      # that is correct — an enforcement result is uninformative while the rule it
      # enforces is itself red. It sits with the other _aggregate step (the
      # cluster-honest CI test) in the same job, so it can never ship dark.
      - name: qledger V1 excess-legality gate at the emitter
        run: python -m pytest tests/test_qledger_excess_legality.py -q

  altdata-new-sources:
    if: ${{ false }}
    gate: code
    # altdata-W1: new free altdata sources — Finnhub transcript metadata catalog
    # (plan-gated graceful no-op on free tier) + StockTwits public sentiment
    # (keyless, bullish/bearish ratios + watchlist_count). Pure parse + store tests;
    # no network, no heavy deps.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install minimal deps
        run: pip install pytest pandas numpy pyarrow pyyaml requests
      - name: finnhub transcript collector tests
        run: python -m pytest tests/test_finnhub_transcripts.py -q
      - name: stocktwits sentiment collector tests
        run: python -m pytest tests/test_stocktwits.py -q

  china-native-w2:
    if: ${{ false }}
    gate: code
    # china_native_data W2 (funding curve / CB plane / fund issuance / full-universe
    # ETF shares): pure parse + aggregate fixture tests, no network. Executed via
    # the ci-pack matrix like every other legacy job.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install minimal deps
        run: pip install pytest pandas numpy pyarrow pyyaml requests openpyxl
      - name: china_funding collector tests
        run: python -m pytest tests/test_china_funding_collector.py -q
      - name: china_cb collector tests
        run: python -m pytest tests/test_china_cb_collector.py -q
      - name: china_fund_issuance collector tests
        run: python -m pytest tests/test_china_fund_issuance_collector.py -q
      - name: china_flows full-universe ETF shares tests
        run: python -m pytest tests/test_china_flows_etf_universe.py -q

  wri-risk-core:
    if: ${{ false }}
    gate: code
    # WRI W3: the watchlist book-structure math (templates/risk_core.js) is a pure
    # ES5 module exercised by shelling `node` over fixture JSON — proves the §7
    # sanity anchors (ENB≈20 pure-idio / →1 single-factor, negative-MCTR hedge,
    # abstain path) + the factor-bet grouping + stress-lens gating. node is on the
    # runner; the suite skips loudly if it ever isn't. No build, no site/.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
      - name: install minimal deps
        # pyyaml: engine.factor_exposure loads the W6.1 factor-model charter (YAML) at
        # import, so the test modules need it at collection time (was ModuleNotFoundError).
        # markupsafe: engine.i18n imports Markup at module scope, and the sector-i18n
        # suite below imports engine.i18n for the glossary half. jinja2: that same suite
        # renders the ONE shipped template line that publishes window.WL_SECTOR_ZH, to
        # prove an undefined context still emits `{}` rather than a bare `=`.
        run: pip install pytest pandas numpy pyarrow pyyaml markupsafe jinja2
      - name: risk_core.js math — §7 sanity anchors + structural invariants
        run: python -m pytest tests/test_risk_core_js.py -q
      - name: watchlist_risk.js chain lane — node-shelled helper tests (TXI W4)
        run: python -m pytest tests/test_watchlist_chains_js.py -q
      # UWP/W1a: the watchlist+portfolio STORE (templates/watchstore.js), same
      # node-shelling idiom as the two steps above. test_watchlist_books_js.py was
      # named by no run: step in any workflow since it landed (P5 in
      # scripts/audit_unrun_tests.py) — it pins the fold markers ("only on success",
      # "never on an empty book") on the file W1a rewrites, so it is wired here
      # rather than left dark. The multilist suite pins the list-scoped push diff:
      # a full-membership delete that escapes its list wipes a sibling watchlist.
      - name: watchstore.js store — local books + one-shot folds
        run: python -m pytest tests/test_watchlist_books_js.py -q
      - name: watchstore.js store — registered multi-list seam (W1a)
        run: python -m pytest tests/test_watchstore_multilist_js.py -q
      # W2 workspace layer over that store: the bulk-entry parser (the anonymous
      # funnel's front door), the weighting model, the engine-state -> plain-word
      # stage map (which may only ever DE-ESCALATE), delta-since-visit, and the
      # read-side guard that stops a focus refetch from reverting a just-made edit.
      - name: watchlist workspace — entry parser, stage map, focus-refetch guard (W2)
        run: python -m pytest tests/test_watchlist_workspace_js.py -q
      # Sector names are the one label on this page with no zh twin in the source:
      # they arrive as raw English on stockdata/index.json and are painted
      # client-side, so they rendered in English under data-lang="zh" while
      # everything around them switched. Nothing raises when that regresses — an
      # untranslated name renders perfectly — so the guard covers all four ways it
      # goes quiet: a taxonomy the glossary has gone blind to, the build not
      # handing the map over, the lookup itself, and a print site that bypasses it.
      - name: watchlist sector names — glossary, build wiring, print sites (zh)
        run: python -m pytest tests/test_watchlist_sector_i18n.py -q
      # W4 per-ticker Intelligence Drawer over that workspace. The load-bearing gate is
      # honest absence: every section builder is run over an EMPTY payload and must
      # return a row that NAMES what is missing, because a section that renders nothing
      # and a section with nothing to report are the same screenshot. Also pins the
      # attribute-escaping slot in the shared row painter (a silent breakout), the
      # dossier route (`stock.html` reads location.hash only — `?t=` is a 200 that opens
      # empty), and the anonymous lock shell.
      - name: watchlist drawer — honest absence, glance vocabulary, canonical routes (W4)
        run: python -m pytest tests/test_watchlist_drawer_js.py -q
      - name: portfolio_state.js — snapshot authority + weighting law (A1A)
        run: python -m pytest tests/test_portfolio_state_js.py -q
      - name: portfolio truth — A1A mutation battery + page wiring (A1A)
        run: python -m pytest tests/test_portfolio_truth_a1a_js.py -q
      # Consumer-level auth-transition proof (Sol blocker 2, A1A closure): drives the
      # REAL watchstore.js + portfolio.js + portfolio_state.js together over a genuine
      # event bus — the delayed-cloud window, a permanently-failed Supabase client
      # init, and write-failure honesty (blocker 3) end to end.
      - name: portfolio auth-transition — consumer-level proof (A1A closure)
        run: python -m pytest tests/test_portfolio_auth_transition_js.py -q
      # A1B fast start: the frozen paste grammar plus the REAL Portfolio store's
      # atomic local save, N-row cloud receipt, lost-response reconciliation,
      # auth-generation fence, and UUID-preserving local-to-cloud fold.
      - name: portfolio import — exact draft + failure-safe batch persistence (A1B)
        run: python -m pytest tests/test_portfolio_import_a1b_js.py -q
      # Repo hygiene, wired here because this wave introduced the class it closes: a crop
      # harness borrows site/stockdata/ from the main checkout for the duration of a
      # shoot, and an aborted run once left the symlink behind long enough for a broad
      # `git add` to commit a machine-local ABSOLUTE path into the shipping tree. It
      # needs no node and no artifacts, so it is cheap wherever it runs.
      - name: repo hygiene — no tracked absolute-path symlinks
        run: python -m pytest tests/test_no_absolute_path_symlinks.py -q
      - name: factor-exposure engine — orthogonalization, betas, crypto universe (W6.1)
        run: python -m pytest tests/test_factor_exposure.py tests/test_factor_exposure_stress.py -q

  house-law-registry:
    if: ${{ false }}
    gate: data
    # DECLARED scope, UNIONed with the runtime-derived one (run_ci_pack.infer_job_scopes
    # keeps `set(job.paths) | set(derived)`), and it exists for exactly one entry:
    # root `config.yml`. That file is where BOTH of this repo's timeless rename maps
    # live — `breadth.ticker_fixups` and `quality.ticker_key_migrations` — and the next
    # ticker rename lands the way both modelled ones did, as ONE LINE in one of them.
    # The unmodelled-rename detector that catches it (build_security_master.
    # unmodelled_renames, asserted by tests/test_dataos_security_master.py) could not be
    # REACHED from that edit: scope inference cannot own a repository-ROOT file at all —
    # SCOPE_REFERENCE_RE and ci_scope_dependencies._PATH_LITERAL both require a tracked
    # directory prefix — so a fixups-only PR selected 48 jobs and this was not among
    # them (measured 2026-08-13). Silent gap, in the detector written to close a silent
    # gap. The remaining entries are not new ownership: they are the paths this job's
    # own commands already name, listed because _scope_coverage_findings makes a
    # declared scope that fails to cover them a HARD manifest error. Adding a `run:`
    # step that names a new path means adding it here too.
    paths:
      - config.yml
      - docs/HOUSE_LAW_CI_GUARD_SUITE.md
      - scripts/check_append_only_assertions.py
      - scripts/check_claim_passport.py
      - scripts/check_house_law_registry.py
      - scripts/check_symbol_rename_drift.py
      - tests/test_append_only_assertions.py
      - tests/test_dataos_security_master.py
      - tests/test_house_law_registry.py
      - tests/test_ticker_alias_boundary.py
    # Meta-guard: verifies config/house_law_checks.yml is complete and honest.
    # Three checks in sequence:
    #   1. selftest: synthetic assertions prove the meta-guard catches violations.
    #   2. live check: every scripts/check_*.py is registered, every registered
    #      script exists, all ci_wiring points to real jobs that mention the script.
    #   3. docs-drift: regenerate docs and fail if the committed file is stale.
    #   4. pytest: idempotency + integration tests.
    # Also runs check_claim_passport.py (exit 0 confirmed 2026-07-06; hard gate).
    # Triggers on: config/house_law_checks.yml, the meta-guard script,
    # docs/HOUSE_LAW_CI_GUARD_SUITE.md, and tests/test_house_law_registry.py.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install minimal deps
        run: pip install pytest pandas pyarrow pyyaml
      - name: meta-guard selftest (synthetic assertions — gate fires on violations)
        run: python3 scripts/check_house_law_registry.py --selftest
      - name: meta-guard live check (census + wiring + selftest truth + ratchets)
        run: python3 scripts/check_house_law_registry.py
      - name: docs-drift check (committed docs must match regen output byte-for-byte)
        run: |
          python3 scripts/check_house_law_registry.py --emit-docs
          git diff --exit-code docs/HOUSE_LAW_CI_GUARD_SUITE.md
      - name: claim-passport lint (no unregistered qual field reaching scored consumer)
        run: python3 scripts/check_claim_passport.py
      - name: stable-key / live-vendor symbol alias boundary
        run: |
          python3 scripts/check_symbol_rename_drift.py
          python -m pytest tests/test_ticker_alias_boundary.py -q
      # Data OS DOS-1.1: the security master + the TIME-SCOPED alias table, which is
      # the general form of the boundary the step above guards. It lives HERE and not
      # in `dataos-foundation` for two reasons, both load-bearing:
      #   * that job installs pytest + pyyaml ONLY, on purpose — "a pandas/numpy import
      #     creeping into lib/dataos/ breaks HERE first" — and adding pandas to its pip
      #     line would switch that canary off. This job already provides pandas+pyarrow.
      #   * that job is pure unit tests that never open data/; this suite reads the
      #     COMMITTED artifacts (data/reference/*.parquet), which is what a consumer
      #     would read and therefore the only honest place to pin them.
      # It is a `run:` step in an EXISTING job rather than a new job because the legacy
      # manifest sits near the narrow-diff ceiling (test_ci_pack.py asserts a
      # representative diff selects <= len(jobs)*4//5); folding suites into a job keeps
      # the job count, and the arithmetic, where it is.
      - name: Data OS security master + time-scoped vendor alias table (MMC/MRSH, SATS/ECHO)
        run: python -m pytest tests/test_dataos_security_master.py -q
      - name: house-law registry tests (idempotency + integration)
        run: python -m pytest tests/test_house_law_registry.py -q
      # data.append_only_assertions — registered at `discipline` with EMPTY
      # ci_wiring, so what runs here PROVES THE GUARD WORKS; it never judges the
      # tree. The bare `--selftest` (no --strict, no tree scan) is the whole
      # point: the guard reports 47 pre-existing findings on main that no PR
      # author wrote, and a gate that reds every PR on day one gets disabled
      # rather than obeyed. See the promotion plan in config/house_law_checks.yml.
      #
      # STEP PLACEMENT IS DELIBERATE. run_ci_pack returns on the first non-zero
      # step, so a step placed FIRST masks every sibling behind it. These two are
      # appended LAST because this job is an always-on fleet control plane: the
      # meta-guard census, the docs-drift check and the claim-passport lint must
      # never be masked by a new guard's own proof. The cost of last placement is
      # that these go dark behind an earlier red — acceptable for a discipline-tier
      # guard, and not acceptable the other way round.
      - name: append-only law selftest (7 known-bad caught, 13 legal clean)
        run: python3 scripts/check_append_only_assertions.py --selftest
      # Negative controls: mutates the detector 10 ways and the fixtures 9 ways.
      # A surviving mutation means the rule it disables is dead code, and the
      # test names it. This is what makes the selftest above evidence rather than
      # a guard printing zero.
      - name: append-only law mutation + negative controls
        run: python -m pytest tests/test_append_only_assertions.py -q

  leader-radar-unit:
    if: ${{ false }}
    gate: code
    # LRV-W1: leader lifecycle engine + builder unit tests.
    # Engine (test_leader_lifecycle.py): state machine, hysteresis, tri-state Kleene,
    # rs_line_gap_pct, count_k_true_n_avail, display chip fence.
    # Builder (test_build_leader_radar.py): RS rank history, insider cluster,
    # options skew, basket corr, early_entry sort, artifact schema additive,
    # analyst buy-share loader + chip wiring + banner render (LRV-R1e; jinja2 needed).
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install minimal deps
        run: pip install pytest pandas numpy pyarrow pyyaml jinja2
      - name: leader lifecycle engine tests
        run: python -m pytest tests/test_leader_lifecycle.py -q
      - name: leader radar builder tests
        run: python -m pytest tests/test_build_leader_radar.py -q

  evidence-clock:
    if: ${{ false }}
    gate: data
    # Global Evidence Clock (EC-R1..R8): the committed display-only artifact
    # data/neuralweb/evidence_clock.json must parse, keep authority=display_only,
    # stay inside the 9-state vocabulary, carry forbidden_actions on every row,
    # keep clock_ids unique, and match its own summary counts. The clock is
    # read-only attention routing — this gate proves the artifact cannot
    # smuggle scores, promotions, or source mutations.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install minimal deps
        run: pip install pytest pyyaml
      - name: evidence-clock checker selftest (gate fires on synthetic violations)
        run: python3 scripts/check_evidence_clock.py --selftest
      - name: evidence-clock artifact integrity (hard gate)
        run: python3 scripts/check_evidence_clock.py
      - name: evidence-clock unit tests
        run: python -m pytest tests/test_evidence_clock.py -q

  vintage-pin-fence:
    scope: exclusive
    if: ${{ false }}
    gate: data
    # Fail-closed: a PR must not equality-pin a live nightly store. Measured
    # 2026-08-13: govrev latest/candidates ``assert 23 == 8``, options
    # ``episodes.jsonl`` 384→1206 used as a closed replay, prophet open-keys
    # 10→11, then after #5515 the spine parquet (pack-7 / #5547) and Release
    # Radar inflation-truth artifacts (unrun-release-forecast). Each one
    # reddened main and then, through merge-on-green, the entire armed queue.
    # Legal: a watermarked prefix slice, a census derived from a committed
    # receipt, a named disclosure set, or the #5547 cold-test bind of
    # ``config.data_dir`` to empty tmp. Do not re-type the product literals
    # and do not grandfather by filename — a new ``== N`` in the same file
    # still reds.
    # The rglob over tests/test_*.py is load-bearing for path-aware scope:
    # inference claims tests/**/*.py so this runs on PRs that touch tests,
    # not only after the next jam. Main's full suite still runs it.
    timeout-minutes: 5
    runs-on: ubuntu-latest
    paths:
      - tests/**
      - scripts/check_vintage_pin_fence.py
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install minimal deps
        run: pip install pytest pyyaml
      - name: vintage-pin fence selftest (post-5515 detonation classes red, legal green)
        run: python3 scripts/check_vintage_pin_fence.py --selftest
      - name: vintage-pin fence (new live-store equality pins)
        run: python3 scripts/check_vintage_pin_fence.py
      - name: vintage-pin fence unit tests
        run: python -m pytest tests/test_vintage_pin_fence.py -q

  validated-claims:
    if: ${{ false }}
    gate: data
    # BC-2 PR gate: the word "validated" in user-facing surfaces must trace to a
    # backed claim. This is the PR-time companion to the nightly calibration run
    # (cycle-calibration.yml/calibrate). A sibling builder is fixing the current
    # findings — wire it regardless; final verification happens after both land.
    # Law source: CLAUDE.md ("The word 'validated' in user-facing text is CI-enforced").
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: validated-claims selftest (gate fires on synthetic unearned claim)
        run: python3 scripts/check_validated_claims.py --selftest
      - name: no unearned 'validated' claims in user-facing surfaces
        run: python3 scripts/check_validated_claims.py
      # design.system_ratchet (R0, REPORT-ONLY — always exits 0). Hosted here and
      # not in a job of its own for two reasons: a 181st manifest entry reds the
      # ci_pack narrow-diff ceiling, and this job ALREADY owns templates/** in the
      # derived scope, so the step costs the selector nothing. The checker is
      # stdlib-only, hence no pip install. R1 (blocking) is a later PR that flips
      # the registry severity AND swaps `--mode report` for `--mode enforce
      # --registry data/...` in lockstep — never one without the other.
      - name: design-system ratchet selftest (each rule fires on its own fixture)
        run: python3 scripts/check_design_system.py --self-check
      - name: design-system ratchet R0 (report-only drift census over templates/)
        run: python3 scripts/check_design_system.py --mode report

  design-governance:
    if: ${{ false }}  # executed inside ci-pack — NOT a disabled gate; see the ci-pack job.
    gate: code
    # TP-0 theme-parity governance (research/THEME_PARITY_RATCHET_PRESENTATION_CONVERGENCE_ARCHITECTURE.md).
    # Three forward-only gates so NEW user-facing presentation debt fails closed
    # without reddening the ~19k inherited findings in templates/:
    #   ui.design_system_forward_ratchet — new forbidden decisions on CHANGED LINES
    #   ui.runtime_style_injection       — ratcheted budget, may only stay flat/shrink
    #   ui.visual_evidence_manifest      — material UI change owes dual-theme evidence
    #
    # gate: code, deliberately. gate:data jobs are excluded from the merge-gate
    # packs, which would make this a non-blocking "hard gate". The verdict here is
    # a function of the PR tree only: hand-authored templates/**, the composer JS
    # (site/canada-stock-v36.js and site/hk-stock-v36.js have no templates/ twin,
    # so they are NOT render-lane output), the committed allowlist, and committed
    # receipts. No nightly data commit moves it.
    #
    # Deliberately UNSCOPED (no `paths:`): a scoping bug on a governance gate fails
    # silently open, which is the exact class of defect this job exists to prevent.
    # All three guards are stdlib/PyYAML and run in seconds.
    #
    # fetch-depth 0 + explicit base fetch is load-bearing: the two changed-line
    # gates need `git merge-base`, and on a shallow checkout there is no base ref,
    # so they would degrade to guarding nothing while the job went green (the
    # #1195 shallow-cut incident class).
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install minimal deps
        run: pip install pytest pyyaml
      - name: design governance unit suites
        run: python3 -m pytest tests/test_check_design_system.py tests/test_check_runtime_style_injection.py tests/test_check_ui_visual_evidence.py -q
      - name: design-system ratchet selftest (each rule fires on its own fixture)
        run: python3 scripts/check_design_system.py --self-check
      - name: runtime-style injection guard selftest (gate fires on planted injection)
        run: python3 scripts/check_runtime_style_injection.py --selftest
      - name: visual-evidence guard selftest (gate fires on missing/wrong evidence)
        run: python3 scripts/check_ui_visual_evidence.py --selftest
      - name: runtime-style injection ratchet (budgets may only stay flat or shrink)
        run: python3 scripts/check_runtime_style_injection.py
      - name: forward-only design ratchet (new forbidden decisions on changed lines)
        run: |
          if ! git fetch origin ${{ github.base_ref || 'main' }}; then
            echo "::error title=design-governance::could not fetch origin/${{ github.base_ref || 'main' }}; the forward-only design ratchet has NO canonical comparison base and cannot run. Failing closed — an unresolvable base is an absent gate, not a degraded pass."
            exit 1
          fi
          if ! mb=$(git merge-base "origin/${{ github.base_ref || 'main' }}" HEAD); then
            echo "::error title=design-governance::no merge base with ${{ github.base_ref || 'main' }}; the forward-only design ratchet has NO canonical comparison base and cannot run. Failing closed — an unresolvable base is an absent gate, not a degraded pass."
            exit 1
          fi
          git diff --unified=0 "$mb" HEAD -- templates > /tmp/design.diff
          python3 scripts/check_design_system.py --mode enforce-added --diff-file /tmp/design.diff
      - name: visual evidence required for a material UI change
        run: |
          if ! git fetch origin ${{ github.base_ref || 'main' }}; then
            echo "::error title=design-governance::could not fetch origin/${{ github.base_ref || 'main' }}; the visual-evidence gate has NO canonical comparison base and cannot run. Failing closed — an unresolvable base is an absent gate, not a degraded pass."
            exit 1
          fi
          if ! mb=$(git merge-base "origin/${{ github.base_ref || 'main' }}" HEAD); then
            echo "::error title=design-governance::no merge base with ${{ github.base_ref || 'main' }}; the visual-evidence gate has NO canonical comparison base and cannot run. Failing closed — an unresolvable base is an absent gate, not a degraded pass."
            exit 1
          fi
          git diff --unified=0 "$mb" HEAD -- templates site > /tmp/ui.diff
          python3 scripts/check_ui_visual_evidence.py --diff-file /tmp/ui.diff

  template-site-sync:
    if: ${{ false }}  # executed inside ci-pack — NOT a disabled gate; see the ci-pack job.
    gate: data
    # (Which pack runs it is weight-balanced and shifts as jobs are added, so it is
    # deliberately not named here; resolve it with run_ci_pack.py --validate-only.)
    # ui.template_site_sync PR gate: every plain-copy page asset (templates/<name>
    # that also ships as site/<name>) must byte-match at every commit — a one-sided
    # reword creates the divergence window the render lanes then propagate at an
    # unpredictable later time. 2026-07-07 incident: engine-render commit 2ae709f9f0
    # resurrected pre-#1793 "validated" wording in site/sector_cycles.js because the
    # push step's `git pull --rebase -X theirs` favored the run's checkout-time copy
    # over the reword that merged mid-render. The render lanes now re-sync post-rebase
    # (--fix); this job keeps PRs from arming the window in the first place.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      # jinja2/markupsafe are for the chat-nav guard below, which RENDERS
      # _site_nav.html.j2 rather than grepping it (CI packs install minimal deps,
      # not requirements.txt — an unlisted import here is an ImportError, not a skip).
      - name: install minimal deps (theme.js bake reads config.yml)
        run: pip install pyyaml pytest jinja2 markupsafe
      - name: template-site-sync selftest (gate fires on synthetic divergence)
        run: python3 scripts/check_template_site_sync.py --selftest
      - name: site copies of template assets must byte-match templates/
        run: python3 scripts/check_template_site_sync.py
      # Unrun backfill 2026-08-19 (audit_unrun_tests.py, #5976-class): the token-
      # segmentation unit tests for THIS job's own checker were named by no run:
      # step. `_token_segments`/`_matches_around_tokens` are the stdlib fallback
      # `check_template_site_sync.py` uses when PyYAML cannot reproduce the bake
      # (pages.yml, a DEPLOY GATE) — it walks N tokens -> N+1 invariant segments,
      # and this suite is what proved the generalization from one token
      # (Supabase) to two (mm_brain.js's content hash, added by #5976) still
      # accepts the real committed pair and still rejects a one-sided edit. Rides
      # this step's own venv (pytest + pyyaml, already above) and this job's full
      # checkout (it reads templates/theme.js AND site/theme.js). Ordered right
      # after the checker it tests, before the unrelated chat-nav/asset-stamp
      # steps below, so a regression here is not masked behind them.
      - name: template-site-sync token-segmentation unit tests (N-token fallback)
        run: python -m pytest tests/test_template_site_sync_tokens.py -q
      # Unrun backfill 2026-08-19 (audit_unrun_tests.py, #5976): theme.js's lazy
      # assistant launcher (#mmb-boot stub) — the RESOLUTION guard (every
      # dynamically injected same-origin child asset must resolve from
      # _mmSharedAssetRoot, not a document-relative literal; this is the exact
      # class of the 2026-08-19 ~4,600-page /stocks/ 404) and the EAGERNESS guard
      # (the bundle loads on activation, not at DOMContentLoaded), plus the drift
      # guard comparing #mmb-boot's geometry to mm_brain.js's real #mmb-launch.
      # Static analysis of templates/theme.js + templates/mm_brain.js only — no
      # network, no render, no site/ read — so it needs nothing past this job's
      # existing pytest install. Joins this job because it is the templates/**
      # plain-copy-asset home and the sibling suite immediately above already
      # reads templates/theme.js for the same PR's change.
      - name: chat launcher stub — shared-asset-root resolution + lazy activation + drift guard
        run: python -m pytest tests/test_chat_launcher_stub.py -q
      # The other half of plain-copy integrity: byte-matching its site copy says
      # nothing about whether chat.html's HEADER still matches the shared partial.
      # It did not — 12 live pages missing, 17 removed pages still linked — because
      # the block was hand-copied and no gate compared it to anything.
      - name: chat-nav selftest (gate fires on a hand-edited header)
        run: python3 scripts/sync_chat_nav.py --selftest
      - name: chat.html's header must match _site_nav.html.j2
        run: python3 scripts/sync_chat_nav.py
      # ...and the suite itself, which was NAMED in ci.yml's paths trigger but
      # EXECUTED by no job — a trigger fires the pack, it does not run the file, and
      # every pytest invocation in this repo names its files explicitly (there is no
      # tests/ dir-wide sweep to pick it up). The two script steps above only compare
      # the <nav> element, so the four assertions that live OUTSIDE it were dark:
      # the navigation-refresh.css / chat_nav.css stylesheet law, the whole-chrome
      # check, the data-base shim count (in <head> — and chat.html is exempt from the
      # write_page guard precisely because sync_chat_nav writes it raw), and the
      # bilingual render. Deps are already installed above: pyyaml/pytest/jinja2/
      # markupsafe is the whole closure, verified green against exactly that set.
      - name: chat-nav suite (the assertions outside the <nav> element)
        run: python -m pytest tests/test_chat_nav_sync.py -q
      # ui.asset_stamp — the OTHER half of this law, and the reason it bit users. The
      # `--fix` above rewrites the site copy of every plain-copy pair FROM templates/,
      # and it runs AFTER the stamping sweep in every lane, so a ?v= stamp written only
      # site-side was reverted before it could reach main. #3573 fixed the rewrite and
      # still could not move the stamp on these pages: #3617's onboard.css fix sat live
      # at the origin while every returning browser kept the old sheet (site/index.html
      # linked onboard.css?v=cfdca9e2, served `immutable, max-age=1y`); #3624 hand-bumped
      # that one page. optimize_assets now stamps the templates/ side too — which only
      # reaches main if the lane STAGES templates/, and only covers pages a sibling lane
      # lands mid-run if the lane also re-stamps POST-rebase. These pin all three.
      - name: "?v= stamps — paired-page round-trip + lane ordering/staging (ui.asset_stamp)"
        run: >
          python -m pytest
          tests/test_asset_stamp_lane_order.py tests/test_optimize_assets.py
          tests/test_asset_stamp_refresh.py -q
      # closing-bell duplicate-render dedup — the OTHER render-lane contract that only
      # exists in workflow YAML, and the one that shipped broken from day one: the
      # session-day guard dedup'd on the stamp's `as_of` (an ISO timestamp) against a
      # bare ET date, so the DST pair's summer line re-rendered every trading day
      # (median 85.0 min of a two-Mac pool, straight through the nightly's fire window).
      # Same deps as the suite above (pytest + pyyaml); the test executes the workflow's
      # real writer heredoc, so a mirrored copy can never drift green past it.
      - name: "closing-bell dedup — stamp writer/reader field contract (DST pair safety)"
        run: python -m pytest tests/test_closingbell_dedup_contract.py -q
      # ui.interfonts_theme_sync: templates/_interfonts.html.j2 carries the six
      # Inter @font-face blocks copied verbatim from templates/theme.css (#2119,
      # for standalone pages that don't link theme.css). A theme.css font edit
      # that skips the partial silently de-fonts those pages — same drift class,
      # same job.
      - name: interfonts-sync selftest (gate fires on synthetic drift)
        run: python3 scripts/check_interfonts_sync.py --selftest
      - name: _interfonts partial @font-face blocks must match templates/theme.css
        run: python3 scripts/check_interfonts_sync.py
      # ui.data_base_shim: same page-asset drift family, and it had NO CI lane at
      # all — which is how scripts/build_leader_radar.py sat red on main writing
      # site/leader_radar.html with a raw write_text instead of lib.pages.write_page
      # (the render lane's inject_data_base sweep healed the committed copy after
      # the fact, so only a standalone builder run showed the missing shim).
      # test_site_shim carries all three layers: lib.pages units, the committed-page
      # tripwire, and the source guard against raw write_text on an *.html target.
      # NOTE on trigger breadth: the source guard scans scripts/**+engine/** for
      # offenders, but listing those globs under `paths:` would run this pack on
      # nearly every Python PR — a CI-cost call for the operator (same reasoning as
      # the gh-annotation guard above). ci-main-heartbeat.yml runs this job on a
      # schedule against main with NO path filter, which is what keeps the guard
      # from rotting red on main invisibly again.
      - name: data-base shim guard (lib.pages units + committed-page tripwire + source guard)
        run: python -m pytest tests/test_site_shim.py -q
      # Same module, same class of post-render page rewrite: externalize_css lifts
      # inline <style> into hashed sheets via lib.pages.externalize_css_text and
      # writes through write_page. Also unwired until now.
      #
      # The suite also carries the ui.template_site_sync sweep-side guards (#3650):
      # the sweep must SKIP the plain-copy HTML pairs (index.html/chat.html). Every
      # lane runs `check_template_site_sync --fix` after the sweeps and before
      # `git add site/`, re-copying those pages FROM templates/, so a site-only lift
      # is reverted before it reaches main — while still minting the hash file, which
      # the sweep's own prune can never reclaim (it looks referenced in the same run
      # that creates the link). That accreted 3 dead stylesheets / 92,412 bytes.
      # Writing THROUGH to templates/ is deliberately not the fix — see the module
      # docstrings in scripts/externalize_css.py.
      - name: inline-CSS externalization units + pair-skip guards
        run: python -m pytest tests/test_externalize_css.py -q
      # design-system packet PR-0(a)(b): the site --fs-* type ramp at :root and the
      # three shared .mx-* primitives. Same job because it is the same file — a
      # templates/theme.css edit is what breaks all of it, and this job is already the
      # theme.css integrity lane (interfonts sync + plain-copy byte-match above).
      # Browser-free BY DESIGN: the packs install minimal deps, so the language-toggle
      # check resolves the cascade in stdlib rather than importorskip-ing playwright
      # and reporting green while proving nothing. Deps are the set installed above
      # (pytest is the whole closure here — the suite imports only re/pathlib).
      - name: design-system foundations (site type ramp at :root + shared .mx-* primitives)
        run: python -m pytest tests/test_design_system_foundations.py -q

  data-base-shim:
    if: ${{ false }}
    gate: code
    # ui.data_base_shim: every builder must write site/**/*.html through
    # lib.pages.write_page, which injects the depth-aware R2 fetch shim. A raw
    # write_text drops it, so a standalone builder run ships the page with its
    # per-ticker fetches pointed at Pages instead of R2.
    #
    # This had NO CI lane anywhere, and the source guard it did have only matched
    # a LITERAL *.html target on the write line — blind to the two-line form
    # (`out = site_dir / "x.html"` then `out.write_text(...)`) that nine builders
    # actually used. The committed pages looked fine because the render lane's
    # inject_data_base sweep heals them after the fact, so nothing anywhere failed.
    #
    # Its own job rather than a few more steps on template-site-sync: the builder
    # suite RUNS each builder against a fixture root, so it needs the data stack
    # (jinja2/pandas/numpy/pyarrow) that the light sync job deliberately avoids —
    # and a differing install command would split that job's shared venv anyway.
    #
    # NOTE on trigger breadth: the source guard scans scripts/**+engine/**, but
    # listing those globs under `paths:` would run this pack on nearly every Python
    # PR — a CI-cost call for the operator (same reasoning as the gh-annotation
    # guard above). ci-main-heartbeat.yml runs this job on a schedule against main
    # with NO path filter, which is what keeps it from rotting red invisibly.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install deps (the builder suite runs real builders)
        run: pip install pytest pyyaml jinja2 pandas numpy pyarrow
      # test_site_shim.py itself (units + tripwire + source guard) runs on the
      # template-site-sync lane, which #3635 wired and which needs only pyyaml.
      # This job exists for the suite that lane CANNOT host: it RUNS each builder,
      # so it needs the data stack below.
      - name: every page builder emits the shim (runs each against a fixture root)
        run: python -m pytest tests/test_builder_shim_writes.py -q

  grader-manifest:
    if: ${{ false }}
    gate: code
    # F1 (Metabolism Phase 0 / R-AUT-7): grader-immutability manifest guard.
    # The fitness-producing files (grade_thematic, foresight_leadlag, theme_placebo,
    # qledger_falsifier, sample_qledger_placebo) are registered in
    # config/grader_manifest.yml with their SHA-256 content hashes.  Any drift in
    # a grader file — whether from a loop PR or a human PR — fails RED here.
    # Loop-authored PRs are additionally blocked by the F2 self-mod-fence job.
    # Selftest proves the check detects simulated hash drift and missing files.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install minimal deps
        run: pip install pytest pyyaml
      - name: grader-manifest selftest (drift detection + fail-closed proof)
        run: python3 scripts/check_grader_manifest.py --selftest
      - name: grader-manifest live check (hash drift is a hard failure)
        run: python3 scripts/check_grader_manifest.py
      - name: grader-manifest test suite
        run: python -m pytest tests/test_grader_manifest.py -q

  self-mod-fence:
    if: ${{ false }}
    gate: code
    # The Stop hook is itself a release-control subject. Scope inference sees the
    # test imports but not a hook reached by subprocess/path, so an edit under
    # .claude/hooks/ could otherwise skip the job that owns its contract.
    paths:
      - ".claude/hooks/**"
      - scripts/agentos.py
      - scripts/ci_authority.py
      - scripts/ci_authority_paths.py
      - scripts/ci_semantic_proof.py
      - scripts/check_self_mod_fence.py
      - scripts/worktree_gc.py
      - tests/test_agent_worktree_roots.py
      - tests/test_agentos_compile.py
      - tests/test_agentos_schema.py
      - tests/test_agentos_status.py
      - tests/test_self_mod_fence.py
      - tests/test_ci_authority.py
      - tests/test_ci_semantic_proof.py
      - tests/test_ship_loop_guard.py
      - tests/test_ship_loop_semantic.py
      - tests/test_sparse_worktree_profile.py
      - tests/test_warp_session_start_sparse.py
      - tests/test_worktree_gc.py
      - tests/test_worktree_placement.py
    # F2 (Metabolism Phase 0 / R-AUT-5): self-modification fence.
    # Hard-fails any PR whose branch name or commit trailers carry the loop
    # namespace (metabolism/* or claude/loop-*, or 'Loop-Authored:' trailer)
    # AND whose changed-file list touches the IMMUTABLE set.
    # Human/operator PRs pass freely.  Fail-closed: unclassifiable → blocked.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install minimal deps
        run: pip install pytest pyyaml
      - name: self-mod-fence selftest (all four cases proven)
        run: python3 scripts/check_self_mod_fence.py --selftest
      - name: ship-loop guard contract (dirt, branch namespace, render scoping)
        # The Stop hook that enforces commit->merge->render->live had NO CI at all,
        # which is how its render precondition shipped scoped to the whole session
        # range: on a shared main that sweeps in concurrent sessions' templates/
        # merges and demands a render on a merge commit that can never have one.
        # It also owns the exact claude/* delivery-branch prefix: a look-alike or
        # codex/* branch must fail before stand-down or GitHub probes can release it.
        # WorktreeCreate placement + the GC that reclaims what it plants are the
        # same contract seen from both ends: the hook decides where a session tree
        # is PLANTED and the sweeper decides whether it can ever be RECLAIMED. They
        # were wrong in mirror-image ways at once (#6164) — the hook derived
        # placement from `--git-common-dir`, a per-clone constant, and the sweeper
        # expanded its repo-relative roots under the primary alone — which left 109
        # registered worktrees holding 299 GB unreclaimable by construction, on a
        # host that has hit runner ENOSPC twice. tests/test_worktree_gc.py ran in NO
        # workflow before this line; it is grandfathered-dark rather than waived, so
        # contract-delta never asked for it.
        run: python -m pytest tests/test_ship_loop_guard.py tests/test_ship_loop_semantic.py tests/test_agent_worktree_roots.py tests/test_sparse_worktree_profile.py tests/test_warp_session_start_sparse.py tests/test_worktree_gc.py tests/test_worktree_placement.py -q
      - name: self-mod-fence live check (loop PR + immutable → BLOCKED)
        run: |
          # CI_HEAD_REF is the validated API identity published by the upgraded
          # main-owned executor. GITHUB_HEAD_REF is the GitHub PR-event identity
          # retained solely for the one-head bootstrap where the PR must still be
          # proven by the pre-upgrade executor loaded from main.
          BRANCH="${CI_HEAD_REF:-${GITHUB_HEAD_REF:-}}"
          TRAILERS=""
          FILES=""
          PARSE_RC=0
          FILES=$(python3 scripts/check_self_mod_fence.py --print-planner-files) || PARSE_RC=$?
          if [ "$PARSE_RC" -eq 2 ]; then
            echo "::error:: self-mod-fence: CI_CHANGED_FILES_FILE/CI_CHANGED_FILES_JSON malformed — fail-closed (exit 1)." >&2
            exit 1
          fi
          if [ "$PARSE_RC" -eq 0 ]; then
            # ci-plan already listed the diff (packs are fetch-depth:1 after #5564;
            # origin/main...HEAD is often a bad revision — measured #5556/#5519/#5499).
            if [ -z "$FILES" ]; then
              echo "self-mod-fence: ci-plan reported no changed files — zero changes to classify (pass)"
              exit 0
            fi
          else
            # Unset env (local / pre-#5564): fall back to git. Fail-closed only when
            # that fallback cannot determine files either.
            git fetch origin ${{ github.base_ref || 'main' }}
            TRAILERS=$(git log --format="%B" origin/${{ github.base_ref || 'main' }}..HEAD 2>/dev/null || echo "")
            FILES=$(git diff --name-only origin/${{ github.base_ref || 'main' }}...HEAD 2>/dev/null || echo "")
            if [ -z "$FILES" ]; then
              if [ "${GITHUB_EVENT_NAME:-}" = "workflow_dispatch" ] && \
                 git merge-base --is-ancestor HEAD "origin/${{ github.base_ref || 'main' }}" 2>/dev/null; then
                # Dispatch on main itself (the ship-loop guard's E1 unblock lever,
                # `gh workflow run ci.yml --ref main` — hooks #3677): HEAD is main or an
                # ancestor of it, so the empty three-dot diff is VERIFIED-empty — there is
                # no PR and no self-modification to classify. R-AUT-5's fail-closed
                # contract targets UNDETERMINABLE diffs (git errors, shallow clones,
                # base-ref failures), not this case. Before this arm existed every
                # dispatch-at-tip run redded here, which made the green-main E1 evidence
                # unsatisfiable by construction (observed run 30207008917, 2026-07-26).
                # The ancestor form (not tip-equality) keeps the arm valid when main
                # advances between dispatch checkout and this step; a dispatched branch
                # carrying real commits still has a non-empty diff and is classified
                # by the python fence below as always.
                echo "self-mod-fence: workflow_dispatch with HEAD on main — zero changes to classify (pass)"
                exit 0
              fi
              echo "::error:: self-mod-fence: could not determine changed files — fail-closed (exit 1)." >&2
              echo "Possible causes: CI_CHANGED_FILES_FILE/CI_CHANGED_FILES_JSON unset/malformed, git diff error, or base_ref resolution failure." >&2
              exit 1
            fi
          fi
          TRAILERS=$(git log --format="%B" origin/${{ github.base_ref || 'main' }}..HEAD 2>/dev/null || echo "")
          python3 scripts/check_self_mod_fence.py \
            --branch "$BRANCH" \
            --files $FILES \
            --trailers "$TRAILERS"
      - name: self-mod-fence test suite
        run: python -m pytest tests/test_self_mod_fence.py tests/test_ci_authority.py -q
      - name: agent-os record contract (schema fail-closed, join fail-open)
        # ORDERED LAST IN THIS JOB, deliberately. `run_ci_pack.py:_run_job` returns on
        # the FIRST nonzero step, so any step placed before `self-mod-fence live check`
        # can make that fence never run — the F2 fence would go dark for the PR while
        # the job reports a failure that has nothing to do with self-modification.
        # A knowledge-plane record check must never be able to blind a security fence,
        # so it sits behind every fence step in this job.
        #
        # Folded in as a STEP, not a new job, also deliberately: the manifest sits at
        # the narrow-diff ceiling (test_representative_narrow_diffs_skip_at_least_one
        # _quarter_of_jobs asserts selected <= len(jobs)*4//5), so adding a job can red
        # an unrelated pack, and a `paths:` scope cannot fix it — infer_job_scopes()
        # UNIONS declared paths with inferred ones, so a declaration only ever WIDENS.
        # Keeping the job COUNT fixed keeps that arithmetic still.
        run: |
          # Direct invocation FIRST and deliberately: the pytest suite skips itself
          # when agentos/ is outside a sparse cone, so a store that failed to check
          # out would otherwise pass as a skip. This line cannot skip.
          python3 scripts/agentos.py validate
          python -m pytest tests/test_agentos_schema.py tests/test_agentos_status.py \
            tests/test_agentos_compile.py -q

  capability-broker:
    if: ${{ false }}
    gate: data
    # F3 (Metabolism Phase 0 / §3 of masterplan): capability broker + redline scanner.
    # The capability broker (engine/neuralweb/capability_broker.py) resolves capability
    # IDs to ref names only — never secret values.  The redline scanner hard-fails on
    # any high-entropy string or secrets.* value reference in the manifest or broker.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install deps
        # pandas/numpy: the metabolism v2 suite includes tests/test_metabolism.py, whose
        # TestAdaptCortexAttention imports engine/neuralweb/query.py (numpy+pandas). This
        # job is the ONLY CI runner of that suite — importorskip would silence the tests.
        # anthropic: test_cortex.py's rate-limit resilience test imports anthropic at
        # runtime to patch anthropic.Anthropic.__init__ (real import, not mocked).
        # pyarrow: SA-W3 accountability suite (#2451) reads parquet inside this job
        # requests: the v8 reflex-arc reping tests (#2442) patch scripts.notify.send_telegram,
        # and scripts/notify.py does a real `import requests` at module import — without it
        # the patch target fails to resolve and 5 reflex tests die at import, not on behavior.
        run: pip install pytest pandas numpy pyyaml anthropic pyarrow requests
      - name: capability-redline selftest (planted secret caught; clean real files pass)
        run: python3 scripts/check_capability_redline.py --selftest
      - name: capability-redline live scan (hard fail on secret values or value-capture)
        run: python3 scripts/check_capability_redline.py
      - name: capability broker + redline test suite
        run: python -m pytest tests/test_capability_broker.py -q
      - name: metabolism v2 suite (lifecycle tier-mirror + promotion fence + inertness)
        run: python -m pytest tests/test_metabolism.py tests/test_metabolism_v2a.py tests/test_metabolism_v2b.py tests/test_metabolism_v2c.py tests/test_metabolism_v2d.py -q
      - name: metabolism v3 suite (memory + context + integration)
        run: python -m pytest tests/test_metabolism_v3_memory.py tests/test_metabolism_v3_context.py tests/test_metabolism_v3_integration.py -q
      - name: metabolism propose/adjudicate suite (lobe threading + learning wire)
        run: python -m pytest tests/test_metabolism_propose_adjudicate.py -q
      - name: metabolism v4 W3 suite (mission self-model + kill registry)
        run: python -m pytest tests/test_metabolism_v4_w3_mission.py -q
      # Registry ROW/KEY shape, checked before the drift check below: drift only
      # proves the compiled artifact matches the markdown, never that the markdown
      # is well-formed. This suite was strictly dark until 2026-08-06 — named by no
      # `run:` step in any workflow — and three malformed rows shipped to main
      # underneath it: a §4 row keyed `KILL-` (the #4607 reclaim-veto ruling, a §2
      # kill misfiled as a §4 hold), and two rows whose extra cell shifted every
      # column left so `dict(zip(headers, cells))` DROPPED `Ruling / source`
      # (KILL-PM4-OVERHEAD-SUPPLY compiled to verdict "\" with no source, and the
      # LSR row's generated matcher became `kill.{0,30}liquidity.{0,30}shock` —
      # a blocklist pattern matching its own key text instead of the topic it is
      # supposed to block). A registry guard nothing executes is not a guard.
      - name: DO_NOT_REBUILD registry keys + row shape (cite-by-key contract)
        run: python -m pytest tests/test_dnr_registry_keys.py -q
      - name: blocklist drift check (compiled registry must match DO_NOT_REBUILD.md)
        run: python3 scripts/check_blocklist_drift.py
      # Wired 2026-08-06: named by NO run: step since it landed, and untriggerable
      # (P3, strictly dark) — so the registry every session is told to read before
      # proposing work had no executing guard at all. It was red on main, and while
      # dark it also let two more defects ship that no other check can see: a row
      # whose Key was pasted twice (#4625) and a `\ |` typo for `\|`, both of which
      # overflow the 4-column header. `zip(headers, cells)` truncates the overflow
      # and shifts every column left, so the compiled registry carried a key as a
      # topic, a topic as a verdict, and lost the ruling entirely — while the key
      # still looked well-formed. Sits with the drift check above because it guards
      # the same two files, and needs nothing the install line does not already have.
      - name: DO_NOT_REBUILD key contract — section prefixes, uniqueness, column arity
        run: python -m pytest tests/test_dnr_registry_keys.py -q
      - name: metabolism v4 w1 suite (real BUILD dispatch — R-V4-2)
        run: python -m pytest tests/test_metabolism_v4_w1_dispatch.py tests/test_metabolism_buildlane.py -q
      # tests/test_ollama_provider.py joins its siblings 2026-08-06. It was DARK
      # since it shipped — no `run:` line anywhere in .github named it, so its
      # base-url allowlist and payload-translation guards had never once been
      # executed by CI. It rides here rather than in a marketing pack because
      # the module it covers is a PROVIDER: llm_auth (already on this line) is
      # what constructs OllamaClient, and this job's deps already carry it.
      - name: llm auth waterfall + multi-key failover + usage-ledger attribution suite
        # tests/test_ai_costs.py joins it 2026-08-07: the ledger the local rung
        # writes to was dark too, which is how _capture_usage could bill free
        # Ollama calls to the Claude subscription lane unnoticed.  It guards the
        # same waterfall as the files beside it — keep it in this step.
        run: python -m pytest tests/test_codex_provider.py tests/test_llm_auth.py tests/test_key_pool.py tests/test_ollama_provider.py tests/test_ai_costs.py tests/test_provider_health.py tests/test_provider_capacity.py -q
      - name: V10 key economy suite (enabled-set, header capture, usage snapshot)
        run: python -m pytest tests/test_key_pool_economy.py -q
      - name: cortex provider routing + tool loop suite
        run: python -m pytest tests/test_cortex.py -q
      # Rides here because this job is the ONLY CI runner of the metabolism suite
      # (see the deps note above) and these repairs span BOTH sides of that
      # boundary: engine/neuralweb/metabolism.py's registration contract and
      # scripts/evaluate_cortex_hypotheses.py's grading of it. Splitting them
      # across jobs would let the metric enum drift between where it is enforced
      # and where it is computed — which is the W3 defect itself.
      - name: cortex evaluator wiring repairs (audit §3 W1-W6)
        run: python -m pytest tests/test_cortex_evaluator_repairs.py -q
      - name: cortex ADB-W3 two-way read tools + parity suite
        run: python -m pytest tests/test_cortex_adb_w3.py -q
      - name: metabolism w2-pulse suite (verify scan mode + dream invocability)
        run: python -m pytest tests/test_metabolism_w2_pulse.py -q
      - name: metabolism v4 w5 suite (steering memory + applier)
        run: python -m pytest tests/test_metabolism_v4_w5.py -q
      - name: metabolism v4 w6 suite (shadow cycle)
        run: python -m pytest tests/test_metabolism_shadow_cycle.py -q
      - name: metabolism phase-a suite (spine + inertness contracts)
        run: python -m pytest tests/test_metabolism_phase_a.py -q
      - name: metabolism v6-W3 suite (generic fitness + multi-lobe + clock + shadow genesis)
        run: python -m pytest tests/test_metabolism_generic_fitness.py -q
      - name: metabolism v7 audit gate suite (adversarial PR review + merge gate)
        run: python -m pytest tests/test_metabolism_audit.py tests/test_metabolism_buildlane.py -q
      - name: metabolism v7 R-V7-7 remediation loop suite (audit-reject cycles back bounded then parks)
        run: python -m pytest tests/test_metabolism_remediation.py -q
      - name: SA-W3 standout accountability metabolism wiring suite
        run: python -m pytest tests/test_metabolism_sa_w3.py -q
      - name: metabolism v8-A immune suite (CI-red sentinel + claim dedup + lane-health sensors)
        run: python -m pytest tests/test_metabolism_immune.py -q
      - name: metabolism v8C meta-learning spine (outcome-prior ledger + de-rank + recall parity)
        run: python -m pytest tests/test_metabolism_priors.py -q
      - name: metabolism v8 reflex arcs suite (R-V8-6..R-V8-9 revert/bridge/tap/park)
        run: python -m pytest tests/test_metabolism_reflex.py -q
      - name: metabolism V9 attention engine suite (attention.py module contract)
        run: python -m pytest tests/test_metabolism_attention.py -q
      - name: metabolism V9 attention wiring suite (agenda+brain+propose+build integration)
        run: python -m pytest tests/test_metabolism_attention_wiring.py -q
      - name: metabolism V10 throttle suite (intensity + pace + METAB_* env plumbing)
        run: python -m pytest tests/test_metabolism_throttle.py -q
      - name: metabolism V11 budget gate suite (auto-run gate verdicts + header parsing)
        run: python -m pytest tests/test_metabolism_budget_gate.py -q
      - name: metabolism V12 surface curator + metered loop suite (census + surface law + pins + daily pace + 429-derived gate)
        run: python -m pytest tests/test_metabolism_v12_surface.py -q
      - name: metabolism achievements observability suite (per-cycle rollup from journals + verify)
        run: python -m pytest tests/test_metabolism_achievements.py -q
      - name: metabolism V10 key pool economy suite (enabled-set + header capture + usage snapshot)
        run: python -m pytest tests/test_key_pool_economy.py -q
      - name: metabolism V10 admin throttle suite (admin routes + UI bindings)
        run: python -m pytest tests/test_admin_metabolism_throttle.py -q
      - name: CRX Part A — key pool 7-key extension suite (pool grows to 7 OAuth keys)
        run: python -m pytest tests/test_key_pool_seven.py -q
      - name: preflight claude CLI resolution suite (runner launchd-PATH fallback)
        run: python -m pytest tests/test_preflight_claude_resolve.py -q
      - name: metabolism gc+merge lifecycle suite (propose-branch deletion + reaper + audit max-age)
        run: python -m pytest tests/test_metabolism_gc_and_merge_lifecycle.py -q
      - name: self-mod fence selftest
        run: python3 scripts/check_self_mod_fence.py --selftest
      - name: grader manifest (fitness graders + immutable configs unchanged)
        run: python3 scripts/check_grader_manifest.py

  hub-a11y:
    if: ${{ false }}
    gate: data
    # Hub UX program (W5 — a11y regression guard): six static checks — (a)-(d) on
    # every committed page that embeds the globe deck (gd-stage presence), (e)-(f)
    # on the shared JS assets those pages load:
    #   (a) no focusable element (button/a/input/[tabindex] without tabindex="-1")
    #       statically inside an aria-hidden="true" subtree.
    #   (b) html tag has a non-empty lang= attribute AND the inline boot script in
    #       <head> references data-lang (bilingual language-detection before paint).
    #   (c) the light-theme pill contrast override rule exists with accent share <= 60%.
    #   (d) viewport meta includes viewport-fit=cover; body CSS references
    #       env(safe-area-inset for notch/home-indicator padding.
    #   (e) site/theme.js contains documentElement.lang sync (WCAG 3.1.1 tripwire).
    #   (f) site/mm_brain.js keeps the Brain launcher keyboard-operable: role="button"
    #       + tabindex="0" on #mmb-launch, a keydown handler for Enter AND Space, and
    #       a #mmb-launch:focus-visible ring (WCAG 2.1.1 tripwire — it shipped as a
    #       bare <div> until #3682, unreachable from the keyboard on every page).
    # Pure stdlib / html.parser — no browser, no extra deps, safe on ubuntu CI.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install minimal deps
        run: pip install pytest
      - name: hub a11y guard (5 checks on globe-deck pages)
        run: python -m scripts.check_hub_a11y site
      - name: hub a11y guard unit tests (violating + passing fixtures per check)
        run: python -m pytest tests/test_check_hub_a11y.py -q

  font-ui-defined:
    if: ${{ false }}
    gate: code
    # Font-UI definition guard (PR #2647 recurrence tripwire): any template whose
    # body-level CSS sets the page font via var(--font-ui) must either link
    # theme.css (<link rel="stylesheet"> href attribute — never the bare string,
    # CSS comments mention theme.css) or define --font-ui: locally (+ warning if
    # the _interfonts.html.j2 include is missing). An undefined custom property
    # makes font-family invalid-at-computed-value -> unset, so the whole page
    # renders in the browser default serif (Times) — the #2195 sweep broke 6
    # standalone page families this way. Partials only warn (token resolves at
    # host-page level). Pure stdlib / re — no browser, no extra deps.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install minimal deps
        run: pip install pytest
      - name: font-ui definition guard (body-level var(--font-ui) coverage)
        run: python -m scripts.check_font_ui_defined templates
      - name: font-ui guard unit tests (violating + passing fixtures per check)
        run: python -m pytest tests/test_check_font_ui_defined.py -q
      - name: fund-page typography contract (word labels stay on the shared UI face)
        run: python -m pytest tests/test_fund_page_typography.py -q

  ratio-lens:
    if: ${{ false }}
    gate: code
    # Ratio Lens W1 (RL-R1..R16): AUTHORITY block, forbidden-key walk,
    # ledger gate, idempotency, builder exit-0, state-signature isolation,
    # anchor honesty. Display-tier oracle organ; no gauntlet.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install deps
        run: pip install pytest pandas numpy scipy pyyaml pyarrow
      - name: ratio lens test suite
        run: python -m pytest tests/test_oracle_ratio_lens.py -q

  hk-confirming-turn:
    if: ${{ false }}
    gate: code
    # HKRV-W1: evidence-witness bypass in engine/cycles.py — CONFIRMING TURN state.
    # Tests: confirm=None byte-identity, V-recovery+witnesses→CONFIRMING TURN,
    # hard_fail wins, flat/no-witnesses unchanged, ontology crosswalk resolves.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install deps
        run: pip install pytest pandas numpy pyarrow pyyaml
      - name: hk confirming turn tests
        run: python -m pytest tests/test_hk_confirming_turn.py -q

  hk-cycle-ontology:
    if: ${{ false }}
    gate: code
    # HKRV-W1: cycle_ontology completeness — CONFIRMING TURN present in all required
    # sets (SIMPLE_CROSSWALK, LADDER, fallbacks). 5 conformance tests that W4 reviewer
    # found red must pass.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install deps
        run: pip install pytest pandas numpy pyarrow pyyaml
      - name: cycle ontology tests
        run: python -m pytest tests/test_cycle_ontology.py -q

  hk-leadership:
    if: ${{ false }}
    gate: code
    # HKRV-W2: HK leadership participation organ — cohort cohesion, thrust detection,
    # southbound context, ledger gating (CN_LANE=asia). Display-only authority fence.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install deps
        run: pip install pytest pandas numpy pyarrow pyyaml
      - name: hk leadership tests
        run: python -m pytest tests/test_hk_leadership.py -q

  hk-context-chips:
    if: ${{ false }}
    gate: code
    # HKRV-W4: factor/EM context chips — 9 pure fail-open chip functions.
    # Tests: fail-open on absent store, rate_path state mapping, vhsi_pctile sanity,
    # compute_all no-None-values, authority fence (display_only=True) across all chips.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install deps
        run: pip install pytest pandas numpy pyarrow pyyaml
      - name: hk context chips tests
        run: python -m pytest tests/test_hk_context_chips.py -q

  hk-robustness-w5:
    if: ${{ false }}
    gate: code
    # HKRV-W5: robustness fixes — alignment health banner, southbound trading-day
    # staleness, freshness sentinel 7th check (southbound holdings), universe coverage
    # logging. Includes tests/test_hk_freshness.py (updated) + test_hk_robustness_w5.py.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install deps
        # full heavy profile — these tests import through scripts/build_hk.py
        # (jinja2 -> markupsafe, plotly, pillow, requests); verified in a scratch
        # venv against exactly this set.
        run: pip install pytest pandas numpy pyarrow pyyaml jinja2 plotly pillow requests
      - name: hk robustness w5 tests
        run: python -m pytest tests/test_hk_robustness_w5.py tests/test_hk_freshness.py -q
      - name: HK macro fill budget + degraded-store preservation
        run: python -m pytest tests/test_hk_macro_ffill_budget.py tests/test_store_guard.py -q

  hk-integration-seams:
    if: ${{ false }}
    gate: code
    # HKRV-F6: integration seam tests — sb_persist_map ticker-key seam guards
    # silent all-False reindex; _apply_hk_confirm ladder bypass unit tests.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install deps
        run: pip install pytest pandas numpy pyarrow pyyaml
      - name: hk integration seam tests
        run: python -m pytest tests/test_hk_integration_seams.py -q

  hkrv-w3-banner:
    if: ${{ false }}
    gate: code
    # HKRV-W3: leadership banner + CONFIRMING TURN chip in hk.html.j2.
    # Tests: banner renders with leaders_participating; absent on quiet/missing;
    # dynamic counts from members; breadth line; Tier-2 receipt; chip on TURN IN
    # PROGRESS item; chip absent when none; no CJK in title= attrs; bilingual spans.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install deps
        run: pip install pytest pandas numpy pyarrow pyyaml jinja2 markupsafe
      - name: hkrv w3 banner tests
        run: python -m pytest tests/test_hkrv_w3_banner.py -q

  regen-hk-g1-fixture-guard:
    if: ${{ false }}
    gate: code
    # Guards scripts/regen_hk_g1_fixture.py — the regeneration remedy for the G1
    # staleness tripwire: byte-idempotency on an unchanged panel, the
    # ADJUSTMENT-SIGNATURE receipts on a constant-ratio re-pricing, the refusals
    # (non-constant drift, calendar surgery, derived-only drift) that leave the
    # fixture untouched, and the window law — each ticker is re-cut from its OWN
    # committed first date, never a flat tail(N), because the 2026-07-31 freeze is
    # 3B-phase-aligned and a flat re-pin took 123 of its 157 windows off-grid.
    # Synthetic panels only — no repo data.
    #
    # pyyaml is NOT optional even though this job touches no YAML (born red,
    # 2026-08-06): the test imports scripts/regen_hk_g1_fixture.py at module
    # level, which reaches lib.config through a four-hop production chain —
    # regen_hk_g1_fixture -> engine.signal_gate -> engine.signal_quality ->
    # engine.technicals -> lib.config, whose line 8 is a top-level `import
    # yaml`.  Without it the file ERRORS at COLLECTION, so the job has never
    # run a single assertion since it landed with #4655.  Every sibling HK job
    # in this file already carries pyyaml for the same reason; this one was the
    # omission.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install deps
        run: pip install pytest pandas numpy pyarrow pyyaml
      - name: hk g1 fixture regenerator tests
        run: python -m pytest tests/test_regen_hk_g1_fixture.py -q

  basket-staleness-warn:
    if: ${{ false }}
    gate: code
    # M7C-R8: basket store staleness warning in scripts/build_baskets.py —
    # fires a ::warning annotation and ops alert when collect timed out upstream.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install deps
        run: pip install pytest pandas pyarrow jinja2 pyyaml
      - name: basket staleness warn tests
        run: python -m pytest tests/test_basket_staleness_warn.py -q

  ftr-tape-surfaces:
    if: ${{ false }}
    gate: code
    # FTR W1/W2a + W3/W8-UI template guards for the three tape surfaces
    # (baskets / allocation / basket_detail): shock banner + lever chip reading
    # framing_zh from JSON, tape band v3 ranked columns + per-column expander,
    # FT-R11 horizon label, plain-word fade footer with the Tier-2 receipt,
    # region-aware basket-name map, W8 anatomy leg order.
    # Wired 2026-07-26 by the unrun-suite census: both files were named by NO
    # `run:` step in any of the 51 workflows and had been red since the surfaces
    # they guard were deliberately reworked (#2635 / #2267 / #2380).
    # Source-only (reads templates/*.j2 off disk, renders baskets.html.j2) —
    # pytest + jinja2 is the whole dependency set.
    runs-on: ubuntu-latest
    timeout-minutes: 3
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install minimal deps
        run: pip install pytest jinja2
      - name: FTR tape-surface template guards
        run: python -m pytest tests/test_ftr_w1_template_markup.py tests/test_ftr_w3_ui.py -q

  live-flow-ops:
    if: ${{ false }}
    gate: data
    # FL-C (FC-R6/R7/R8 + Task 3): live-flow lane ops test suite.
    # Tests two-tier cadence, max_concurrent env override, pinned-publish
    # guarantee, and end-of-session daily summary writer.
    # Network-free: no ThetaData, no R2, no Polygon calls.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install deps
        run: pip install pytest pandas numpy pyyaml pyarrow
      - name: live flow ops test suite
        run: python -m pytest tests/test_live_flow_tiering.py -v
      # The two surfaces that CONSUME live_flow's payloads, executed under node against
      # payloads from the real builders. Both suites turn a schema move (or a re-pointed
      # field) into a red instead of a quietly wrong figure. node comes from the pack-level
      # setup-node; the suites skip loudly if it is ever absent.
      - name: live_flow payload consumers — intraday_flow tracker + stock panel (node)
        run: python -m pytest tests/test_intraday_flow_ncp_js.py tests/test_stock_iflow_panel_js.py -q

  ird-substrate-engines:
    if: ${{ false }}
    gate: code
    # IRD-W1 + IRD-W2 International Risk Desk tests:
    #   W1: IMF WEO adapter, FRED alias-collision lint, CB calendar, liquidity_plumbing
    #   W2: intl_risk (EM stress + vulnerability), contagion (DY + two-tier), cb_desk
    # Zero network calls; all inputs are synthetic.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install deps
        # jinja2: test_ird_wiring imports scripts.build_intl / build_macro_context (template env)
        run: pip install pytest pandas numpy pyyaml pyarrow requests jinja2 plotly
      - name: IRD substrate + engines test suite
        run: python -m pytest tests/test_imf_weo.py tests/test_fred_alias_collision.py tests/test_cb_calendar.py tests/test_liquidity_plumbing.py tests/test_intl_risk.py tests/test_contagion.py tests/test_cb_desk.py tests/test_ird_velocity.py tests/test_ird_wiring.py -q
      # Contagion-links propagation layer + its session-stamp heal (forward-ledger
      # audit 2026-08-05).  test_contagion_links.py was enumerated in NO workflow
      # (audit_unrun_tests.py P4: unrun, writes a data/ ledger) even though the
      # engine appends twelve committed forward logs — a break there went red
      # nowhere.  Same family as test_contagion.py above, so it shares this venv.
      - name: contagion-links session stamps + heal (forward-ledger audit 2026-08-05)
        run: python -m pytest tests/test_contagion_links.py tests/test_heal_contagion_links_ledger.py -q

  itr-turn-rotation:
    if: ${{ false }}
    gate: data
    # ITR-W1 (research/INTL_TURN_ROTATION_REVAMP.md): per-market turn state
    # machine (crash/breaking/topping/... incl. KOSPI replay goldens on the
    # committed _KS11 store) + fast rotation re-rank + ITR-R6 benchmark
    # freshness fallback. Store-reading tests skip gracefully when absent.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install deps
        run: pip install pytest pandas numpy pyyaml pyarrow requests jinja2
      - name: ITR turn state machine + rotation test suite
        run: python -m pytest tests/test_intl_market_state.py tests/test_intl_rotation.py -q

  altdata-brain-govern:
    if: ${{ false }}
    gate: code
    # altdata-brain PR-B: de-escalation-only law enforcement + false Wilson de-dup
    # comment fix.  Two suites:
    #   test_w7a_tenants.py          — Article-3 verdict, actionable flag, governance
    #                                  transitions, feed shape, mastermind propagation
    #   test_altdata_brain_reconcile.py — _reconcile clamp matrix: LLM escalation
    #                                  clamped to deterministic baseline; de-escalation
    #                                  respected; hard blocks (extended/bearish) still fire
    # Zero network calls; LLM client is never instantiated.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install deps
        run: pip install pytest pandas numpy pyyaml pyarrow
      - name: altdata-brain governance + de-escalation law tests
        run: python -m pytest tests/test_w7a_tenants.py tests/test_altdata_brain_reconcile.py -q

  nw-lobe-unfreeze:
    if: ${{ false }}
    gate: code
    # Regression guard for the two Neural-Web lobes frozen since 2026-07-07:
    #   1. dt_contra_state crash: build_dt_contra_state.py crashed on index.json (list root);
    #      fixed by isinstance(data, dict) guard — 8 tests covering the fix.
    #   2. nasdaq archetype_groups clobber: build_subsector_membership._write() now
    #      re-injects archetype_groups from the existing file when the new payload lacks it;
    #      4 tests covering preserve/no-clobber/no-file/malformed-file paths.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install deps
        # pandas/numpy/pyyaml: both suites import their producers at collection time
        # (build_dt_contra_state → engine.dannytrades_chip → numpy/pandas;
        # build_subsector_membership → engine.basket_index → lib.config → yaml).
        run: pip install pytest pandas numpy pyyaml
      - name: dt_contra_state + subsector membership test suites
        run: python -m pytest tests/test_dt_contra_state.py tests/test_build_subsector_membership.py -q

  sa-w4-improvement-lanes:
    if: ${{ false }}
    gate: code
    # SA-W4: standout improvement lanes — clamped review engine + Tier-0 replay +
    # lab come-back alerts.
    # Covers: whitelist/step/disjointness enforcement, dormant-reject on null baseline,
    # UNEVALUABLE=>reject, governance event schema, shadow book spec completeness,
    # replay data-gap honesty, never-raise on corrupt proposals,
    # registry_seed picklab entries (27 US + 20 CN + 20 HK).
    # Network-free: all synthetic fixtures via tmp_path.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install deps
        run: pip install pytest pyyaml pandas numpy pyarrow
      - name: SA-W4 standout review test suite
        run: python -m pytest tests/test_standout_review.py -v

  rc-r14-china-rotation-events:
    if: ${{ false }}
    gate: code
    # RC-R14: China + HK rotation-event detector port tests.
    # Covers: sector_legs_china/hk loaders (basket series from baskets.json),
    # run_nightly data_subdir parameterization (China/HK path isolation from US),
    # append-only ledger regression (RC-R2 law), synthetic detector fire.
    # Network-free: all inputs are synthetic fixtures.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install deps
        run: pip install pytest pandas numpy pyyaml pyarrow
      - name: RC-R14 China + HK rotation events test suite
        run: python -m pytest tests/test_rotation_events.py tests/test_rotation_events_china.py tests/test_rotation_events_hk.py -v

  codex-research-engine:
    if: ${{ false }}
    gate: code
    # CRX W1 (CRX-R1..R8): Codex Research Engine — runner/budget core + both lanes.
    # Three suites, one job:
    #   test_codex_runner_budget.py  — runner binary resolution, run_codex subprocess
    #                                  wrapper, event-stream parser, budget can_run()
    #                                  gate (85% rule, paused_until, session cap),
    #                                  note_result() usage-state update, NEVER-RAISE.
    #   test_codex_lanes.py          — case lane (episode pick, audit, PR open, ledger
    #                                  row) + signal lane (SF context pack, screen gates,
    #                                  harness file, dedup); both run_once() functions;
    #                                  codex_research_loop driver iterations.
    #   test_admin_codex.py          — admin panel routes (/api/codex/panel, /mode, /run),
    #                                  usage bars, recent-attempts list.
    # Zero network calls; codex CLI is mocked in all test suites.
    # scipy: signal lane imports engine.signal_foundry which imports scipy.stats.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install deps
        run: pip install pytest pandas numpy pyarrow pyyaml scipy
      - name: CRX runner + budget contract tests (NEVER-RAISE, 85% gate, usage-state)
        run: python -m pytest tests/test_codex_runner_budget.py -q
      - name: CRX lanes + loop driver tests (case lane, signal lane, run_once, loop iterations)
        run: python -m pytest tests/test_codex_lanes.py -q
      - name: CRX admin panel tests (routes, usage bars, recent-attempts list)
        run: python -m pytest tests/test_admin_codex.py -q

  metab-throttle-ux:
    if: ${{ false }}
    gate: code
    # V11 Metabolism Throttle UX (budget gate + run-until dispatch):
    # Three suites, one job:
    #   test_metabolism_budget_gate.py — key_budget header parsing, gate_verdict
    #                                    semantics (5h_max/weekly_max/manual),
    #                                    write_status schema, NEVER-RAISE.
    #   test_metabolism_rununtil.py    — pick_key weekly_max exclusion, soft
    #                                    manual-floor preference + never-empty rule,
    #                                    fail-open on ImportError, workflow YAML
    #                                    assertions (cron hourly, narrow-commit target,
    #                                    manual floor gate, continuation re-read).
    #   test_admin_metabolism_ux.py    — admin routes for the new UX controls
    #                                    (5H MAX / WEEKLY MAX / STOP buttons,
    #                                    budget status panel data).
    # Zero network calls; key_pool and budget_gate are monkeypatched throughout.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install deps
        run: pip install pytest pandas numpy pyarrow pyyaml
      - name: budget gate contract tests (key_budget, gate_verdict, write_status)
        run: python -m pytest tests/test_metabolism_budget_gate.py -q
      - name: run-until dispatch + workflow YAML assertion tests
        run: python -m pytest tests/test_metabolism_rununtil.py -q
      - name: admin metabolism UX panel tests
        run: python -m pytest tests/test_admin_metabolism_ux.py -q

  admin-live-runs:
    if: ${{ false }}
    gate: code
    # Live-runs feed: server-side snapshot for the admin live-runs panel.
    # Covers: snapshot() shape, active/queued split, elapsed_s computation,
    # last_completed duration + html_url, jobs API budget cap (≤2 calls),
    # queued runs never get a jobs call, mastermind bot probe (200→True,
    # 404/timeout/exception→False, no-requests-lib→False), heartbeat tail
    # parsing (last valid line, malformed skipped, absent→None), and
    # snapshot() never-raise when all mocks throw. Zero network calls.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install deps
        run: pip install pytest requests pyyaml pandas numpy
      - name: admin live-runs snapshot tests
        run: python -m pytest tests/test_admin_live_runs.py -q

  cbf-regime:
    if: ${{ false }}
    gate: code
    # CBF-W1 Cross-Border Flow Regime Census (CBF-R1..R12):
    # Classifier engine: pure functions, strictly causal rolling windows,
    # 5-session hysteresis, era split, EM FX sign normalization.
    # Test coverage:
    #   test_flow_regime.py — FX direction normalization (USDXXX negate contract),
    #                         causality (future mutation unchanged past),
    #                         hysteresis (no switch < 5 sessions, day-1 immediate),
    #                         determinism, rule precedence (risk_off beats exception),
    #                         dollar splice (pre/post 2006-01-01).
    # Zero store reads; no network calls; all synthetic DataFrames.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install deps
        run: pip install pytest pandas numpy pyarrow
      - name: CBF regime classifier contract tests
        run: python -m pytest tests/test_flow_regime.py -v

  ccw-w1-data-spine:
    if: ${{ false }}
    gate: code
    # CCW-W1 Corporate Credit Watch data spine:
    #   collectors/corp_bond_holdings.py — bond XLSX parser (ISIN-keyed, not equity path)
    #   data/corp_bonds/issuer_themes.json — issuer→theme registry v1
    #   config.yml fred.series — no alias collisions in the new corp_credit + DGS20 adds
    # CCW-W5 FINRA lane:
    #   collectors/finra_corp_bonds.py — OAuth token flow, pagination, absent-creds skip,
    #                                    frame transformations, dedup, incremental start-date
    # Test coverage:
    #   test_corp_bond_holdings.py — parser fixtures (header detection, ISIN/cusip6,
    #                                numeric coercion, maturity parsing, keep-FIRST,
    #                                schema columns, match-rate, CCW-UNMATCHED-HIGH-PAR
    #                                alarm, registry round-trip)
    #   test_finra_corp_bonds.py  — token flow (acquire/cache/retry/no-leak), absent-creds
    #                                skip, pagination (single/multi-window/full-page), frame
    #                                schema, dedup, incremental start-date, integration
    #   test_fred_series_alias_conflicts.py — no duplicate aliases across all fred.series
    #                                         groups (guards the KeyError outage)
    # Zero network calls; all inputs are synthetic or committed files.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install deps
        run: pip install pytest pandas numpy pyarrow pyyaml openpyxl requests
      - name: CCW-W1 bond holdings parser + registry + FRED alias tests
        run: python -m pytest tests/test_corp_bond_holdings.py tests/test_fred_series_alias_conflicts.py tests/test_fred_baml_archive.py -v
      - name: CCW-W5 FINRA corp bonds collector tests
        run: python -m pytest tests/test_finra_corp_bonds.py -v

  ccw-w2-gspread-engine:
    if: ${{ false }}
    gate: code
    # CCW-W2 g-spread engine:
    #   engine/corp_credit.py — vectorized YTM solver (Newton+bisection), 30/360 US
    #                           accrued, CMT interpolation (merge_asof ≤7d backward),
    #                           issuer/theme matching, composition guard, dispersion,
    #                           maturity wall, validation gate (criteria a-d).
    #   scripts/fetch_ishares_validation_sample.py — SpreadsheetML parser (importable
    #                           without playwright; playwright import deferred to inner fn).
    # Test coverage:
    #   test_corp_credit.py — 66 synthetic tests; no network; no repo data reads:
    #     accrued 30/360 US (EOM, stub, regular), YTM Newton+bisection (par/premium/
    #     discount/fractional-tenor/integer-tenor/HY/fallback), CMT interpolation
    #     (exact/backward-fill/tolerance-7d/miss/multi-date), g-spread (IG/HY/par-
    #     bond/integer-tenor), canonicalize_bonds (missing fields/clean col),
    #     near-par flag, dispersion (n<3/3-7/≥8), composition fixture (M2: production
    #     compute_matched_delta, 5-bar boundary sharp), criterion-b gate (M1: all three
    #     conditions), sector ambiguity law (M3), criterion-a decision rule via
    #     resolve_price_convention (N1: tiebreak path), parse_spreadsheetml.
    # Zero network calls; all inputs are synthetic (tmp_path fixtures).
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install deps
        run: pip install pytest pandas numpy pyarrow pyyaml
      - name: CCW-W2 g-spread engine tests
        run: python -m pytest tests/test_corp_credit.py -v

  ccw-w3-credit-momentum:
    if: ${{ false }}
    gate: code
    # CCW-W3 credit momentum organ:
    #   engine/credit_momentum.py — spread velocity/pctile, oscillator states
    #     (secondary per CCW-R15), density gate, composition suppression,
    #     K-of-N tags (credit_market_turn / credit_theme_stress), interim TLT
    #     block (R6), own-store breadth, FINRA breadth (distinctly labeled),
    #     fallen-angel watch, ORCL named series, credit-vs-equity divergence,
    #     forward ledger (keep-FIRST). Authority all-false.
    #   engine/bonds_alerts.py extension — additive credit_market_turn +
    #     credit_theme_stress debounced events (compute_credit_events /
    #     rebuild_with_credit).
    # Test coverage (tests/test_credit_momentum.py):
    #   canon-on-spread: severity orientation, velocity pctile (widening/tightening)
    #   density gate: open/closed, boundary conditions (n=8, ratio=0.6)
    #   composition-change suppression: mask, no crash
    #   K-of-N tag: 2-of-3 boundary (credit_market_turn + credit_theme_stress)
    #   forward ledger: keep-FIRST idempotency, deterministic event_id, frozen ruler
    #   FINRA breadth math: advance share, buy/sell productCategory trap, source label
    #   divergence quadrant: risk_formation, converging_good, accruing cases
    #   alerts debounce: fired/not-fired events, ZH fields, idempotency, missing file
    #   null-safety: empty stores → valid JSON, no crash (all inputs missing)
    # Downstream suite: test_corp_bond_holdings + test_finra_corp_bonds also run.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install deps
        run: pip install pytest pandas numpy pyarrow pyyaml openpyxl requests
      - name: CCW-W3 credit momentum tests
        run: python -m pytest tests/test_credit_momentum.py tests/test_corp_bond_holdings.py tests/test_finra_corp_bonds.py -v

  ccw-w4-credit-desk:
    if: ${{ false }}
    gate: code
    # CCW-W4 corporate credit desk surfaces:
    #   scripts/build_bonds.py — build_corp_credit_vm (null-safe vm builder),
    #     _build_corp_credit_bond_health (bond_health.corporate_credit block),
    #     _hero_stance, _theme_stance, _build_spread_gauges, _build_maturity_wall,
    #     _build_finra_vm; templates/bonds.html.j2 corp-credit section (#corpcredit).
    # Test coverage (tests/test_bonds_corp_credit_vm.py):
    #   null-safety: missing credit_momentum.json → accruing state, no crash
    #   hero stance determinism: fired/widening/calm mapping
    #   per-theme stance: neocloud always red, telecom always neutral, level thresholds
    #   spread gauges: widening→amber, widening_stress→red, level in sub_en
    #   bond_health block schema: required keys, authority all-false, JSON-serializable
    #   maturity-wall aggregation: 0-par excluded, segs positive flex, missing parquet→[]
    #   FINRA breadth: advance_pct, lows_pct, accruing on None, missing→None
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install deps
        # plotly: test file imports scripts.build_bonds, which imports plotly.graph_objects
        run: pip install pytest pandas numpy pyarrow pyyaml jinja2 plotly
      - name: CCW-W4 credit desk vm tests
        run: python -m pytest tests/test_bonds_corp_credit_vm.py -v

  ccw-w6-study-s1:
    if: ${{ false }}
    gate: code
    # CCW-W6 Study S1 — spread-velocity percentile lead study (frozen pre-registration):
    #   scripts/ccw_study_s1.py — ONE-SHOT study implementing the frozen spec in
    #     research/CCW_STUDY_S1_PREREG.md. Tests cover §9 anti-look-ahead checklist
    #     and verdict rubric only (study is data-gated; CI runs the unit tests).
    # Test coverage (tests/test_ccw_study_s1.py):
    #   look_ahead: future shuffle does not change pctile; current bar excluded
    #   warmup: NaN rows correct count and excluded from conditional set
    #   spx_asof: backward-only merge (Monday, Saturday/non-bday, full spread index)
    #   block_permutation: fixed seed reproducible, p in [0,1], length correct
    #   era_boundaries: calendar-fixed; no overlap; all dates assigned
    #   verdict_rubric: LEADS/MIXED/NULL per §8 (pass+robust, pass+flip, fail, delta<0+p>=0.05)
    # Zero network calls; zero repo data reads; all inputs are synthetic.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install deps
        run: pip install pytest pandas numpy pyarrow pyyaml scipy
      - name: CCW-W6 Study S1 unit tests
        run: python -m pytest tests/test_ccw_study_s1.py -v

  support-email-spine:
    if: ${{ false }}
    gate: data
    # Support + Email estate: the W1 spine (app/mailer.py, app/support.py,
    # admin/support_tickets.py) plus the W2 public surface (templates/support.html.j2, the
    # pinned email base). Five suites, all fully offline — no SMTP, no Supabase, no live
    # admin state, no network:
    #   test_mailer.py            — LEDGER-FIRST idempotency (a duplicate idem_key sends
    #                               nothing), mail-off skip, marketing suppression +
    #                               per-user opt-out, transactional bypassing both,
    #                               one-click unsubscribe headers, the retry policy
    #                               (SMTPException subclasses OSError — auth failures
    #                               must NOT ride the transient retry), dual-language
    #                               rendering + escaping, and unsub token round-trip.
    #   test_support_api.py       — honeypot silent drop, time-to-fill gate, per-IP rate
    #                               limit, every validation branch, the Bearer identity
    #                               override + tier snapshot, and the fail-soft contract
    #                               (an exploding mailer never 500s a filed ticket).
    #   test_admin_support_tickets.py — the operator console: list filter/paging/search
    #                               escaping, detail thread, the legal/illegal transition
    #                               matrix decided from the DB not the client, reply
    #                               append→mail→flag, plus HTTP-level 401/403 against the
    #                               live admin.server Handler.
    #   test_support_page.py      — the /support.html contract a screenshot cannot show:
    #                               the seven POSTed fields, the honeypot's off-screen
    #                               shape, the t0 stamp, EN/ZH dual-spans, no translated
    #                               title=, the three-place public boundary, and PIN §1.1
    #                               (success/error use --ok/--act, NEVER the --up/--down
    #                               direction tokens that swap red<->green in zh).
    #   test_email_templates.py   — the pinned email base: no images/external CSS (a
    #                               blocked logo reads as phishing), presentational tables
    #                               at 600px, the three-solid-cell gradient bar, both
    #                               languages EN-first, and class discipline — the
    #                               unsubscribe slot is ABSENT from every transactional
    #                               send and present for marketing.
    #   test_support_page_js.py   — the page's own inline JS EXECUTED under node against a
    #                               DOM shim, for the three W2-review defects that are
    #                               behaviours a text assertion cannot reach: N submits
    #                               during one in-flight request must send exactly ONE
    #                               POST (three sent three ack emails with three different
    #                               ticket numbers to one person, and each carried its own
    #                               real idem_key so the ledger could not dedupe them); an
    #                               anonymous visitor must never load the Supabase SDK and
    #                               a hung session must not strand the submit; and the
    #                               success copy must follow the API's `mail` flag. Node
    #                               ships on ubuntu-latest; the suite skips if it does not.
    # Deps: fastapi+pydantic for the route/model surface; jinja2 renders the support
    # template (markupsafe rides along for engine.i18n); pandas/numpy/pyarrow/pyyaml/
    # requests are what importing admin.server pulls in (same set as the site-gate job).
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install deps
        run: pip install pytest fastapi pydantic jinja2 pandas numpy pyarrow pyyaml requests
      - name: mailer transport + ledger tests (app/mailer.py)
        run: python -m pytest tests/test_mailer.py -v
      - name: public support intake tests (app/support.py)
        run: python -m pytest tests/test_support_api.py -v
      - name: admin support console tests (admin/support_tickets.py)
        run: python -m pytest tests/test_admin_support_tickets.py -v
      - name: support page contract + pinned design (templates/support.html.j2)
        run: python -m pytest tests/test_support_page.py -v
      - name: support page JS behaviour under node (one POST per submit, auth caps, mail flag)
        run: python -m pytest tests/test_support_page_js.py -v
      - name: pinned email base (app/mailer.py render_email)
        run: python -m pytest tests/test_email_templates.py -v
      - name: segment definitions, shared by both planes (app/email_segments.py)
        run: python -m pytest tests/test_email_segments.py -v
      - name: public unsubscribe endpoint (app/unsubscribe.py)
        run: python -m pytest tests/test_unsubscribe_api.py -v
      - name: welcome bound + campaign drain compliance (app/marketing_emails.py)
        run: python -m pytest tests/test_marketing_emails.py -v
      - name: email center console + CSV export (admin/email_center.py)
        run: python -m pytest tests/test_admin_email_center.py -v
      - name: unsubscribe page + per-matcher serving boundary (templates/unsubscribe.html.j2)
        run: python -m pytest tests/test_unsubscribe_page.py -v

  serving-observability:
    if: ${{ false }}
    gate: code
    # app/observability.py — the Sentry arm for the macro-api serving tier.
    # This job exists because the failure mode it guards is asymmetric: a bug in
    # error REPORTING can take down the thing it reports on. init_sentry() runs
    # before `app = FastAPI(...)` in app/main.py, so anything it raises is an
    # unbootable /api on the droplet — and the droplet is where a boot failure
    # is least visible (the unit just Restart=always-loops in the journal).
    #
    # The suite therefore pins the fail-safe contract rather than the happy
    # path: absent DSN, absent sentry_sdk wheel, a malformed DSN that makes
    # sentry_sdk.init raise, and an older wheel that rejects the newer
    # `enable_logs` kwarg must ALL return False quietly instead of propagating.
    # It also pins the sample-rate parser against the NaN fail-open (every
    # comparison with NaN is false, so a naive `if v < 0 or v > 1` range guard
    # passes NaN straight through to the SDK) and pins the conservative
    # defaults — the vendor quickstart's traces=1.0/profiles=1.0 would trace
    # every /api/flow/* poll on a 1-2 vCPU box.
    #
    # Deps are `pytest` alone on purpose: app/observability.py is pure stdlib
    # and the suite stubs sentry_sdk, so this job must stay green on a runner
    # where the real SDK is not installed at all.
    #
    # `scope: exclusive` declared here (2026-08-20, main-red-repair): shipped
    # with no scope at all, this job's own subject (_release()'s
    # `subprocess.run(["git", ...])` for the deployed SHA) is an opaque
    # subprocess call scope inference cannot see through, so it fell back to
    # SUBPROCESS_ROOTS — including site/** and templates/** — and matched
    # every ordinary templates/index.html PR (128 vs the 127 ceiling in
    # tests/test_ci_pack.py::test_exclusive_curation_narrows_ordinary_code_prs).
    # That is smear, so it is curated away at the source rather than paid for
    # there. Its true subject is exactly the two files below.
    scope: exclusive
    paths:
      - "app/observability.py"
      - "tests/test_observability_sentry.py"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: { python-version: "3.12" }
      - name: install test deps
        run: pip install pytest
      - name: sentry arm is fail-safe and never blocks API boot (app/observability.py)
        run: python -m pytest tests/test_observability_sentry.py -q

  billing-emails:
    if: ${{ false }}
    gate: code
    # Support + Email estate, W3 (app/billing_emails.py, app/account_prefs.py). A SIBLING
    # of support-email-spine rather than three more steps inside it: this lane needs the
    # Stripe SDK, and run_ci_pack only shares a virtualenv between jobs whose install
    # commands are byte-identical — widening the W1 job's line would split it from
    # site-gate for no gain.
    #   test_billing_emails.py  — event→template selection (including the deliberate
    #                             checkout.session.completed ABSTENTION that stops the two
    #                             buy lanes double-sending), the G2 replay gate (the same
    #                             event twice ⇒ one email_log row, one SMTP send), mail-off
    #                             ledgering with a 200 webhook, tier-rank comparison driven
    #                             by config/plans.yml (proven with a tier inserted mid-rank),
    #                             money/date rendering off the objects (non-USD +
    #                             zero-decimal), dual-language bodies, the fail-soft
    #                             contract (an exploding composer never breaks the
    #                             entitlement write), and the trial-ending sweeper's window,
    #                             per-period idem key, restart safety and second-run silence.
    #   test_account_prefs.py   — POST /api/account/prefs: bearer-only identity (a
    #                             client-sent user id is ignored), validation, the
    #                             user_metadata MERGE, the email_prefs lang mirror, and the
    #                             honest partial/total failure statuses.
    #   test_shared_pref_atomics.py — the SAME user_metadata keys, from the browser side:
    #                             templates/theme.js must write the top-level `theme` /
    #                             `theme_auto` / `lang` atomics and never the nested `prefs`
    #                             blob, because supabase updateUser REPLACES a nested object
    #                             and the Mastermind Terminal writes it too. It lives in this
    #                             job rather than a site-gate one because its subject is the
    #                             stored-preference contract lib/user_prefs.py owns, which it
    #                             imports; the assertions are on theme.js SOURCE text (the
    #                             same idiom as test_canada_build.py) since there is no
    #                             browser harness here.
    #   test_billing_webhook.py — the spine suite that guards _handle_event, which W3 now
    #                             hooks. It matched NO workflow before this job (it needs
    #                             the pinned Stripe SDK, which prelaunch-hardening does not
    #                             install), so the change most able to break it could not
    #                             turn it red — the #3509 failure mode exactly.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      # stripe is PINNED to app/requirements.txt's range on purpose. The webhook suite
      # feeds hand-built event payloads to stripe.Webhook.construct_event, and 13+ made
      # that constructor strict about the envelope (`object: "event"`), so an unpinned
      # install turns three green tests red against code the droplet never runs. CI must
      # test the SDK production actually has.
      - name: install deps
        run: pip install pytest fastapi pydantic pandas numpy pyarrow pyyaml requests "stripe>=11,<13"
      - name: billing + lifecycle email tests (app/billing_emails.py)
        run: python -m pytest tests/test_billing_emails.py -v
      - name: account preference tests (app/account_prefs.py)
        run: python -m pytest tests/test_account_prefs.py -v
      - name: shared preference atomics contract (templates/theme.js)
        run: python -m pytest tests/test_shared_pref_atomics.py -v
      - name: billing webhook spine regression (app/billing.py::_handle_event)
        run: python -m pytest tests/test_billing_webhook.py -v

  billing-offers:
    if: ${{ false }}  # executed inside ci-pack; keep the legacy status definition runner-free
    gate: code
    # Founding Pro offer inventory + allotment pacing (app/billing.py, #3856).
    # tests/test_billing_offers.py was named by NO workflow (the unrun-suite
    # census class — the suite landed 2026-07-27, a day AFTER the census swept),
    # so a regression in the offer/pacing logic could never turn CI red. Guards:
    # the pro/annual-only offer key, real-Stripe-redemption-count inventory (the
    # #3856 contract: enforced withdrawal meter, never a fabricated signed-up
    # count), quiet-day withdrawal stepping vs redemption-day advancement, the
    # multi-day-gap and lost-ledger reseed paths, sell-out at the Stripe cap,
    # the checkout race recheck, and grandfathered-customer entitlement repair.
    # Stripe is faked via monkeypatched billing._stripe (no network); the
    # dependency line follows billing-emails' verbatim so run_ci_pack shares the
    # virtualenv, and it keeps the pinned Stripe SDK the sibling billing lanes
    # test against.
    timeout-minutes: 10
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install deps
        run: pip install pytest fastapi pydantic pandas numpy pyarrow pyyaml requests "stripe>=11,<13"
      - name: founding offer inventory + allotment pacing (app/billing.py)
        run: python -m pytest tests/test_billing_offers.py -v
      # Stripe object bootstrap. Same dependency set as the step above (yaml +
      # the pinned Stripe SDK), so run_ci_pack keeps sharing this virtualenv.
      # test_stripe_bootstrap_products pins the RENAME path: a Product whose
      # catalog key moved is adopted in place off config/plans.yml's
      # legacy_product_keys, never duplicated — a CREATE there orphans the live
      # prices, features, portal config and subscriptions.
      - name: Stripe object bootstrap — offers + renamed-product adoption (scripts/stripe_bootstrap.py)
        run: python -m pytest tests/test_stripe_bootstrap_offers.py tests/test_stripe_bootstrap_products.py -v

  site-gate:
    if: ${{ false }}
    gate: code
    # Site Access Gate: IP/country blocklist enforced at the origin.
    # Covers: fail-open contract (disabled default, missing/corrupt file allows all),
    # IP/CIDR matching (v4+v6, exact+CIDR, malformed ignored), allow_ips precedence
    # (self-lockout guard), country header resolution, country=None => allow,
    # admin save_rules validation (bad IP/CIDR/country rejected, atomic write,
    # self-lockout auto-adds caller IP, warns if caller is also in blocked set).
    # maxminddb is NOT installed (absent library must not crash the module).
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install deps
        # pyyaml (+ pandas/numpy/pyarrow) are required to import admin.server for the
        # admin HTTP-level tests (test_admin_site_gate imports the real Handler, and
        # admin.config_store imports yaml at module load). maxminddb is deliberately
        # OMITTED so this run ALSO proves app/gate.py imports and runs cleanly without
        # the optional GeoIP library.
        run: pip install pytest pandas numpy pyarrow pyyaml
      - name: gate logic unit tests (app/gate.py, no maxminddb)
        run: python -m pytest tests/test_site_gate.py -v
      - name: admin site-gate unit + HTTP tests (admin/site_gate.py)
        run: python -m pytest tests/test_admin_site_gate.py -v

  pick-lab-liquidity:
    if: ${{ false }}
    gate: code
    # Pick Lab liquidity gate regression (P0 fix):
    # On float64, NaN.ge(threshold) returns False — not NA — so the old
    # fillna(True) was a no-op and null-adv rows were silently excluded
    # (86.7% of snapshot rows, ~1475 of 1702 names).  Spec §3 says missing
    # dollar_adv_20d must PASS with liq_unknown=True.
    # Fix: adv.isna() | adv.ge(_LIQ_ADV_MIN) in _apply_liquidity().
    # Four suites:
    #   test_pick_lab_liquidity.py — four required cases (null-adv pass,
    #                                below-threshold excluded, above-threshold
    #                                pass, close<5+null-adv excluded) plus
    #                                boundary / multi-row regression pins.
    #   test_pick_lab_core.py      — full core suite must not regress.
    #   test_pick_lab_hk_runner.py — HK runner end-to-end + regression pins
    #                                (catalyst/standouts/stale-cross shapes,
    #                                _col container values, snapshot upsert).
    #   test_pick_lab_hk_render.py — HK lab page renderer suite (needs jinja2).
    # Network-free; no data/ writes.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install deps
        run: pip install pytest pandas numpy pyarrow pyyaml jinja2
      - name: pick lab liquidity gate regression tests
        run: python -m pytest tests/test_pick_lab_liquidity.py tests/test_pick_lab_core.py tests/test_pick_lab_hk_runner.py tests/test_pick_lab_hk_render.py -q

  global-liquidity-transmission:
    if: ${{ false }}  # executed inside ci-pack; keep the legacy status definition runner-free
    gate: code
    # W-LIQ.1 state-only producer. Both suites are offline and use synthetic
    # inputs: the legacy global-liquidity calculations plus the new causal
    # release-alignment, missing-data, contract-envelope, immutable-hash,
    # historical-fixture, and walk-forward purge invariants. This lane has no
    # trading, alerting, routing, or downstream transmission authority.
    paths:
      - "config/global_liquidity_transmission_v1.yml"
      - "engine/global_liquidity.py"
      - "engine/global_liquidity_transmission.py"
      - "scripts/build_global_liquidity_transmission.py"
      - "tests/fixtures/global_liquidity_transmission_wliq1_window.json"
      - "tests/test_global_liquidity.py"
      - "tests/test_global_liquidity_transmission.py"
    timeout-minutes: 8
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install state-producer test dependencies
        run: pip install pytest pandas numpy pyarrow pyyaml
      - name: global liquidity state producers — legacy parity and W-LIQ.1 contract
        run: python -m pytest tests/test_global_liquidity.py tests/test_global_liquidity_transmission.py -q

  ric-w2-surface:
    if: ${{ false }}
    gate: code
    # RIC W2 SURFACE: whole-market dealer-surface history engine unit tests.
    #   test_options_surface.py — 24 tests covering:
    #     dealer-sign convention (call-heavy +GEX, put-heavy -GEX)
    #     dealer_sign_assumption always printed in every row
    #     |·|-magnitude concentration shares bounded [0,1]
    #     front7_abs_gex/charm_share = 1.0 when all contracts front-week
    #     front7_abs_gex/charm_share = 0.0 when all contracts back-bucket
    #     expiry bucket breakdown: fw+fm+bk == net_gex_bn
    #     root_class mapping: SPY→index_etf, XLK→sector_etf, SMH→industry_etf
    #     zero-OI rows excluded (→ None)
    #     empty/None greeks/OI → None (no exception)
    #     oi_frac bounded [0,1]
    #     SURFACE_ROSTER frozen at 19 roots (XLC excluded, audit 2026-07-17)
    # Zero real-store reads; all inputs are synthetic DataFrames.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install deps
        run: pip install pytest pandas numpy pyarrow pyyaml
      - name: W2 SURFACE engine unit tests
        run: python -m pytest tests/test_options_surface.py -v

  collect-identity:
    if: ${{ false }}
    gate: code
    # Registered-visitor identity capture (app/main.py /api/collect): Supabase session-cookie
    # parsing (single + chunked base64url), secretless cached token verification (monkeypatched),
    # and the EO-Client-IP real-IP precedence. Imports app.main, so fastapi is installed.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install deps
        # pyyaml joined the line when the Earnings Wire router stopped being
        # mounted inside `except ImportError: pass`. app.main now imports
        # app.earnings unconditionally (a paid contract must fail startup
        # loudly), and that pulls engine/earnings_narrative/__init__.py ->
        # promotion.py -> yaml. Under the swallow this job never noticed the
        # dependency because the failed import was silently discarded — the
        # same silence that hid the dropped paid routes. app.main also mounts
        # app.market_memory eagerly now, so this isolated lane must declare its
        # direct requests import just like the other app.main suites.
        run: pip install pytest fastapi pyyaml requests
      - name: identity capture unit tests
        run: python -m pytest tests/test_collect_identity.py -v

  research-vault:
    if: ${{ false }}
    gate: data
    # RV W1 ingestion spine: the sidecar contract normalization + every fallback
    # (missing/blank/bad-JSON, never-drop), catalog upsert ordering + institutions
    # rollup, corpus FTS5 build + a BODY-only search (proves body is searchable) +
    # institution/date facets, and the end-to-end ingest via the LOCAL store backend
    # (idempotency: a 2nd run ingests 0; receipts; needs_metadata path). No R2, no
    # poppler — LocalStore + a monkeypatched pdftotext keep the suite fully offline.
    # RV W3 page build: the SSR shell renders from empty + seeded catalogs (honest
    # empty state / SEO-baked cards), hero + lanes + viewer modal markup, balanced
    # EN/ZH spans, plain-English <title>, compliance copy, and the body-never-leaks
    # projection. jinja2 (already a repo dep) is the only page-build need.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install deps
        # Pure stdlib engine (sqlite3 + subprocess) + jinja2 for the W3 page build.
        # pyyaml: build_research_vault imports lib.config (yaml) at module level
        # since #3315 — the lane was red on every PR until it was added here.
        # pypdf: pure-python, so it keeps the suite hermetic while making the probe's
        # PRIMARY rung testable (page count + Info dict). The poppler `pdfinfo` rung —
        # the one production actually uses, since the hourly lane installs neither
        # pypdf nor anything else beyond boto3+pyyaml — needs a system package, so its
        # two tests skip when `pdfinfo` is absent and run on the self-hosted Mac.
        # boto3 (added 2026-07-26 by check_skip_only_suites.py): the r2_store
        # credential-precedence test is `importorskip("boto3")`-gated, and this was
        # the ONLY job naming test_research_vault.py — so it executed nowhere. There
        # is no lazy-import contract to protect here: the production hourly lane
        # installs boto3, and the client is built offline (creds are not validated
        # until a call), so the test stays hermetic. This install line had exactly
        # one member, so extending it costs no extra ci-pack venv.
        run: pip install pytest jinja2 pyyaml pypdf boto3
      - name: research vault ingestion-spine + page unit tests
        run: python -m pytest tests/test_research_vault.py tests/test_research_vault_strict_store.py tests/test_research_vault_page.py tests/test_research_pages.py -q

  research-vault-api:
    if: ${{ false }}
    gate: code
    # RV W2 serving API + download gate (SECURITY-CRITICAL): the app/research.py
    # router — anon→401, free/unknown→402 (fails CLOSED), insider view 200 (no
    # quota), download metering 5/20 (server-authoritative, next-day reset),
    # doc_id traversal/shape/unknown hardening (never a raw R2 fetch), and the
    # watermark degrade path (libs present AND absent both return a body). Uses a
    # LocalStore + dependency-overridden auth + monkeypatched tier resolver, so no
    # live R2/Supabase is hit. Imports app.main, so fastapi + httpx (TestClient)
    # are installed (mirrors the collect-identity job).
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install deps
        # fastapi + httpx for the TestClient; pypdf + reportlab so the watermark
        # HAPPY path is exercised too (the code degrades if they're absent, but CI
        # proves the real stamp path as well). pyyaml closes the Brain gateway's
        # Fundamental Forensics package import in this isolated environment.
        # requests closes app.main's eager app.market_memory import.
        run: pip install pytest fastapi httpx pypdf reportlab pyyaml requests
      - name: research vault serving-API security tests
        run: python -m pytest tests/test_research_api.py -q

  biocatalyst-serving:
    if: ${{ false }}
    gate: code
    # CURATED SCOPE (2026-08-14 incident follow-up). `scope: exclusive` makes
    # the paths below REPLACE inference instead of being unioned under it, so
    # the opaque fallback tier this job used to carry — whole-tree `site/**`,
    # `templates/**`, `docs/**`, `research/**` claims smeared out of a
    # subprocess or traversal deep in its closure — stops selecting it. Every
    # pattern here was generated from the job's OWN import closure and audited
    # for zero misses: no file this job demonstrably reads lost its owner.
    # 272s. Shares the biocatalyst spine with the two jobs above but reaches
    # further into the serving path. Top-level `engine/*` is enumerated
    # (2026-08-15 follow-up to #5685): 846 tracked / 298 concrete. Subject
    # packages stay globbed; narrowing those to a hand list would drop
    # genuine import edges.
    scope: exclusive
    paths:
      # F01 R1B closure widening 2026-09-04 (contract-delta): build_site.py's
      # guarded macro-suite hook imports the page builder, which carries the
      # workspace contract/registry pair. Enumerated, not globbed — the checker
      # named exactly these three; the composers themselves are NOT in this
      # job's closure (thin __init__).
      - "engine/market_os/macro_workspaces/__init__.py"
      - "engine/market_os/macro_workspaces/contract.py"
      - "engine/market_os/macro_workspaces/registry.py"
      # Added-date closure widening 2026-09-01 (contract-delta): the serving
      # path's builder imports now reach the Prophet board_since resolver.
      - "engine/prophet_board_since.py"
      - "engine/theme_graph/identity.py"
      - "engine/theme_graph/__init__.py"
      - "engine/security_state.py"
      - "engine/quote_resolution.py"
      # D5's app/prophet_lab route imports the canonical B1 episode reader and
      # issuer identity chain. This curated job reaches that app router, so its
      # exclusive declaration must cover the exact transitive closure rather
      # than silently skipping changes to these dependencies.
      - "engine/path_risk_signals.py"
      - "engine/stock_identity/__init__.py"
      - "engine/stock_identity/authority.py"
      - "engine/stock_identity/fingerprint.py"
      - "engine/stock_identity/plane.py"
      - "engine/us_candidate_episode.py"
      # W1-B wires Brain's native-fact serve to the frozen W1-A runtime. This
      # job's own app/neuralweb import closure now reaches those packages, so
      # its curated scope must retain their real dependency ownership.
      - "engine/intelligence_workspace/**"
      - "engine/theme_graph/**"
      - "lib/dataos/**"
      - "app/*"
      - "collectors/*"
      - "collectors/biocatalyst/**"
      - "config/**"
      - "contracts/**"
      - "data/**"
      - "engine/__init__.py"
      - "engine/_text_scrub.py"
      - "engine/active_alloc.py"
      - "engine/activist.py"
      - "engine/advanced_breadth.py"
      - "engine/ai_desk.py"
      - "engine/ai_desk_scorer.py"
      - "engine/alert_triage.py"
      # engine/alert_triage.py imports engine/alert_time.py (the Alert Center
      # three-clock / ET board-day contract, Wave 7 2026-08-20), so this curated
      # scope's import closure reaches it. Declared here because a curated
      # `scope: exclusive` job must cover its OWN closure — widening is always the
      # safe direction (scripts/check_contract_delta.py).
      - "engine/alert_time.py"
      - "engine/alerts.py"
      - "engine/altdata_confirmers.py"
      - "engine/altdata_ledger.py"
      - "engine/altdata_models.py"
      - "engine/altdata_picks.py"
      - "engine/altdata_signals.py"
      - "engine/analyst_revisions.py"
      - "engine/anticipation.py"
      - "engine/axes.py"
      - "engine/bar_derive.py"
      - "engine/base_effect.py"
      - "engine/basket_behaves_as.py"
      - "engine/basket_breadth_divergence.py"
      - "engine/basket_index.py"
      - "engine/basket_mtf.py"
      - "engine/basket_score.py"
      - "engine/basket_tape.py"
      - "engine/baskets.py"
      - "engine/baskets_canada.py"
      - "engine/baskets_china.py"
      - "engine/baskets_hk.py"
      - "engine/baskets_intl.py"
      - "engine/baskets_region.py"
      - "engine/beneficial_ownership.py"
      - "engine/bottleneck.py"
      - "engine/bottom_radar.py"
      - "engine/btc_mtf.py"
      - "engine/canon.py"
      - "engine/capital_allocation.py"
      - "engine/catalyst_stock.py"
      - "engine/catalyst_tone.py"
      - "engine/china_altdata.py"
      - "engine/china_basket_spine.py"
      - "engine/china_conviction.py"
      - "engine/china_crowding.py"
      - "engine/china_discovery.py"
      - "engine/china_event_calendar.py"
      - "engine/china_extras.py"
      - "engine/china_intel_bus.py"
      - "engine/china_masterminds.py"
      - "engine/china_news.py"
      - "engine/china_news_intel.py"
      - "engine/china_pboc_stance.py"
      - "engine/china_radar.py"
      - "engine/china_radar_ledger.py"
      - "engine/china_regime_store.py"
      - "engine/china_signal_lab.py"
      - "engine/china_standout_track.py"
      - "engine/china_strategies.py"
      - "engine/china_validation.py"
      - "engine/cn_ai_semis_confirmer.py"
      - "engine/cn_newswires.py"
      - "engine/codex_provider.py"
      - "engine/coiled.py"
      - "engine/composite_score.py"
      - "engine/conditions.py"
      - "engine/confluence_tiers.py"
      - "engine/conviction_accrual.py"
      - "engine/conviction_delta.py"
      - "engine/cross_asset.py"
      - "engine/cross_asset_trend.py"
      - "engine/crowding.py"
      - "engine/cycles.py"
      - "engine/dannytrades.py"
      - "engine/dannytrades_chip.py"
      - "engine/demand_capex.py"
      - "engine/demand_chain.py"
      - "engine/desk_ledger.py"
      - "engine/desk_scorer.py"
      - "engine/deterioration_cascade.py"
      - "engine/dislocation.py"
      - "engine/dispersion.py"
      - "engine/donor.py"
      - "engine/earnings_blackout.py"
      - "engine/earnings_catalyst.py"
      - "engine/earnings_qual.py"
      - "engine/earnings_transcript_intake.py"
      - "engine/election_cycle.py"
      - "engine/entity_resolver.py"
      - "engine/entry_primitives.py"
      - "engine/entry_signal.py"
      - "engine/equity_alloc.py"
      - "engine/equity_factors.py"
      - "engine/etf_board.py"
      - "engine/etf_consensus.py"
      - "engine/etf_pulse.py"
      - "engine/etf_registry.py"
      - "engine/event_atlas.py"
      - "engine/event_calendar.py"
      - "engine/event_landmine.py"
      - "engine/event_risk.py"
      - "engine/expectation_state.py"
      - "engine/experiments_registry.py"
      - "engine/extension.py"
      - "engine/factor_exposure.py"
      - "engine/factor_seasonality.py"
      - "engine/fda_scarcity.py"
      - "engine/fear_greed.py"
      - "engine/filing_transition.py"
      - "engine/flip_confirmation.py"
      - "engine/foresight_cascade.py"
      - "engine/foresight_earliness.py"
      - "engine/foresight_enb.py"
      - "engine/foresight_score.py"
      - "engine/foresight_sizing.py"
      - "engine/forward_calendar_context.py"
      - "engine/forward_dist.py"
      - "engine/froth_fragility.py"
      - "engine/gdelt_client.py"
      - "engine/gex_confirm.py"
      - "engine/gex_engine.py"
      - "engine/global_liquidity.py"
      - "engine/glut_watch.py"
      - "engine/gov_exposure.py"
      - "engine/grading.py"
      - "engine/greeks.py"
      - "engine/group_flow.py"
      - "engine/guidance_gap.py"
      - "engine/hold.py"
      - "engine/holdings_signals.py"
      - "engine/htf_durability.py"
      - "engine/hysteresis.py"
      - "engine/i18n.py"
      - "engine/ignition_features.py"
      - "engine/index_direction.py"
      - "engine/index_leadership.py"
      - "engine/index_leadership_track.py"
      - "engine/indicators.py"
      - "engine/indicators_m2.py"
      - "engine/inputs.py"
      - "engine/intl_inputs.py"
      - "engine/intl_market_state.py"
      - "engine/intl_recovery_quality.py"
      - "engine/leadership_crack.py"
      - "engine/ledger_identity.py"
      - "engine/ledger_lane.py"
      - "engine/liquidity_chip.py"
      - "engine/live_overlay.py"
      - "engine/live_quotes.py"
      - "engine/llm_auth.py"
      - "engine/long_hold_clocks.py"
      - "engine/macro_news.py"
      - "engine/macro_surprise.py"
      - "engine/manager_lag.py"
      - "engine/manager_quality.py"
      - "engine/manager_trades.py"
      - "engine/marker_integrity.py"
      - "engine/market_gamma.py"
      - "engine/market_heatmap.py"
      - "engine/market_state.py"
      - "engine/market_state_audit.py"
      - "engine/market_state_tune.py"
      - "engine/master_brain.py"
      - "engine/masterminds.py"
      - "engine/moat_falsifiers.py"
      - "engine/momentum_display.py"
      - "engine/mtf_monitor.py"
      - "engine/mtf_upturn.py"
      - "engine/name_direction.py"
      - "engine/name_resolver.py"
      - "engine/name_score.py"
      - "engine/name_score_grader.py"
      - "engine/narrative_regime.py"
      - "engine/narrative_rotation.py"
      - "engine/news_common.py"
      - "engine/news_events.py"
      - "engine/news_flow.py"
      - "engine/news_translate.py"
      - "engine/news_vector.py"
      - "engine/ohlc_reconstruct.py"
      - "engine/ollama_provider.py"
      - "engine/opex.py"
      - "engine/options_dislocation.py"
      - "engine/options_issue_desk.py"
      - "engine/options_ivspread.py"
      - "engine/options_structure.py"
      # options_universe: engine/oracle/oopt.py (in this job's engine/oracle/** scope)
      # imports it since the AD-1 options-brief merge (#5872) — the import-closure
      # contract requires the declared scope to cover it or this job goes dark for
      # edits to it.
      - "engine/options_universe.py"
      - "engine/ownership_event_wire.py"
      - "engine/passport.py"
      - "engine/path_personality.py"
      - "engine/pit.py"
      - "engine/pit_lag_recorder.py"
      - "engine/playbook.py"
      - "engine/policy_calendar.py"
      - "engine/pooling.py"
      - "engine/portfolio.py"
      - "engine/portfolio_brief.py"
      - "engine/portfolio_changes.py"
      - "engine/portfolio_vocab.py"
      - "engine/postcross.py"
      - "engine/prediction_markets.py"
      - "engine/prophet_bridge.py"
      - "engine/prophet_doors.py"
      - "engine/prophet_integrity.py"
      - "engine/prophet_stage_inputs.py"
      - "engine/provenance_sidecar.py"
      - "engine/provider_health.py"
      - "engine/pullback_zone.py"
      - "engine/qbus.py"
      - "engine/qkernel.py"
      - "engine/qledger.py"
      - "engine/qledger_validity.py"
      - "engine/rate_inflation_transmission.py"
      - "engine/yield_momentum.py"
      - "engine/rebalance_calendar.py"
      - "engine/regime.py"
      - "engine/regime_conditioning_coverage.py"
      - "engine/regime_label.py"
      - "engine/regime_one.py"
      - "engine/regime_snap.py"
      - "engine/regime_snap_veto.py"
      - "engine/regime_vector.py"
      - "engine/residual_alpha.py"
      - "engine/risk_envelope.py"
      - "engine/risk_radar.py"
      - "engine/risk_radar_backtest.py"
      - "engine/risk_radar_intl.py"
      - "engine/risk_radar_intl_audit.py"
      - "engine/risk_radar_market_catalysts.py"
      - "engine/risk_radar_recovery.py"
      - "engine/risk_sizing.py"
      - "engine/risk_state.py"
      - "engine/roc_blowoff.py"
      - "engine/sector_legs.py"
      - "engine/sector_pulse.py"
      - "engine/sector_rate_inflation.py"
      - "engine/sector_signals.py"
      - "engine/session_anchor.py"
      - "engine/setup_tier.py"
      - "engine/setups.py"
      - "engine/shadow_book.py"
      - "engine/short_volume.py"
      - "engine/signal_archive.py"
      - "engine/signal_frontier_docket.py"
      - "engine/signal_gate.py"
      - "engine/signal_lab.py"
      - "engine/signal_quality.py"
      - "engine/signal_stack.py"
      - "engine/smart_money.py"
      - "engine/sp500_heatmap.py"
      - "engine/special_arb.py"
      - "engine/special_situations.py"
      - "engine/spine.py"
      - "engine/spotlight.py"
      - "engine/stock_dossier.py"
      - "engine/stock_events.py"
      - "engine/stock_fundamentals.py"
      - "engine/stock_macro_sensitivity.py"
      - "engine/stock_personality.py"
      - "engine/stock_score.py"
      - "engine/stock_technicals.py"
      - "engine/stock_view.py"
      - "engine/strategies.py"
      - "engine/subsector_confluence.py"
      - "engine/subsector_rotation.py"
      - "engine/subsector_rotation_alerts.py"
      - "engine/subsector_rotation_china.py"
      - "engine/subsector_scan.py"
      - "engine/subsector_track_record.py"
      - "engine/subsector_turn.py"
      - "engine/sue.py"
      - "engine/t0_indicator.py"
      - "engine/technicals.py"
      - "engine/theme_crowding.py"
      - "engine/theme_emergence.py"
      - "engine/theme_fingerprint.py"
      - "engine/theme_revisions.py"
      - "engine/theme_scoring.py"
      - "engine/theme_tape.py"
      - "engine/theme_validation.py"
      - "engine/themes_heatmap.py"
      - "engine/thesis_funnel.py"
      - "engine/ticker_alerts.py"
      - "engine/ticker_shape.py"
      - "engine/total_return.py"
      - "engine/track_record.py"
      - "engine/translate.py"
      - "engine/transmission_publish.py"
      - "engine/trial_ledger.py"
      - "engine/tushare_freshness.py"
      - "engine/universe_history.py"
      - "engine/us_board_rank.py"
      - "engine/us_candidate_lanes.py"
      - "engine/us_context_vector.py"
      - "engine/us_early_turn.py"
      - "engine/us_prophet_fusion.py"
      - "engine/validation.py"
      - "engine/valuation.py"
      - "engine/velocity.py"
      - "engine/vol_forecast.py"
      - "engine/vol_regime.py"
      - "engine/vol_shock_scorecard.py"
      - "engine/vol_squeeze.py"
      - "engine/washout_turn.py"
      - "engine/weinstein_stage.py"
      - "engine/biocatalyst/**"
      - "engine/capital_structure/**"
      - "engine/chronicle/**"
      - "engine/close_pass/**"
      - "engine/codex_lane/**"
      - "engine/company_intelligence/**"
      - "engine/earnings_release/binding.py"
      - "engine/earnings_release/figures.py"
      - "engine/earnings_release/filing_key.py"
      - "engine/earnings_release/receipts.py"
      - "engine/context_index/**"
      - "engine/earnings_narrative/**"
      - "engine/fundamental_forensics/**"
      - "engine/government_revenue/**"
      - "engine/entry_radar/__init__.py"
      - "engine/entry_radar/contracts.py"
      - "engine/entry_radar/spool.py"
      # 2026-08-19 LAB-0 §6 commissioning-prep heal: engine/prophet_lab/sources.py
      # (below) now imports engine.entry_radar.spool (the R2-first read seam,
      # mirrors the Radar spool WRITER's own backend ladder) -- never the
      # detector/scoring stack (live_ledger/challengers/detectors), which stays
      # off limits and off this job's closure. spool.py itself only imports
      # entry_radar/contracts.py (stdlib-only) and entry_radar/__init__.py, so
      # exactly these three files, never engine/entry_radar/** broadly.
      - "engine/marketing/**"
      - "engine/metabolism/**"
      - "engine/neuralweb/**"
      - "engine/oracle/**"
      - "engine/pick_lab/**"
      - "engine/press/**"
      - "engine/prophet_board_read.py"
      - "engine/prophet_lab/**"
      - "engine/prophet_live/**"
      - "engine/quant_lab/**"
      - "engine/research_vault/**"
      - "engine/seasonality/**"
      - "engine/sector_intelligence/**"
      - "engine/signal_foundry/**"
      - "lib/*"
      - "lib/dataos/identity.py"
      - "research/DO_NOT_REBUILD.md"
      - "research/REGIME_RELIABILITY_FACTOR_CROWDING_ADJUDICATION.md"
      - "research/SIGNAL_EPISODE_ATLAS_MASTERPLAN_BY_FABLE.md"
      - "research/STOCK_PERSONALITY_MASTERPLAN_BY_FABLE.md"
      - "research/US_BOARD_MEASUREMENT.md"
      - "scripts/__init__.py"
      - "scripts/biocatalyst_worker.py"
      - "scripts/build_biocatalyst.py"
      - "scripts/build_capital_structure_page.py"
      - "scripts/build_chart_data.py"
      - "scripts/build_fundamental_forensics.py"
      - "scripts/build_index_leadership.py"
      - "scripts/build_live_overlay.py"
      # F01 R1B closure widening 2026-09-04 (contract-delta): imported by
      # scripts/build_site.py's guarded macro-suite hook.
      - "scripts/build_macro_suite_pages.py"
      - "scripts/build_market_heatmap.py"
      - "scripts/build_market_memory_page.py"
      - "scripts/build_market_structure_page.py"
      - "scripts/build_risk_envelope.py"
      - "scripts/build_rr_banner.py"
      - "scripts/build_site.py"
      - "scripts/build_sp500_heatmap.py"
      - "scripts/build_state_of_themes.py"
      - "scripts/build_stock_library.py"
      # Import-closure entry: build_stock_library reuses
      # scripts/snapshot_shadow_book.rows_from_board for the shadow-book
      # snapshot (one percentile definition), so exclusive scope must name it.
      - "scripts/snapshot_shadow_book.py"
      - "scripts/build_subsector_confluence.py"
      - "scripts/build_subsector_rotation.py"
      - "scripts/build_subsector_rotation_china.py"
      - "scripts/build_subsector_rotation_china_pages.py"
      - "scripts/build_subsector_rotation_pages.py"
      - "scripts/build_themes_heatmap.py"
      - "scripts/check_board_contradictions.py"
      - "scripts/check_signal_gate_coherence.py"
      # Import-closure entry: scripts/build_stock_library.py lazy-imports
      # scripts/check_stretch_oracle_contract.py, so exclusive scope must name
      # it or this job silently stops running when the checker changes.
      - "scripts/check_stretch_oracle_contract.py"
      - "scripts/close_pass_publish.py"
      - "scripts/close_pass_reconcile.py"
      - "scripts/notify.py"
      - "scripts/regime_reliability_phase0.py"
      - "scripts/thematic_rotation_phase0.py"
      - "scripts/translate_profiles.py"
      - "scripts/validate_regime_fwd.py"
      - "site/basketdata/**"
      - "site/canadabasketdata/**"
      - "site/chinabasketdata/**"
      - "site/coming-soon.html"
      - "site/data/**"
      - "site/factordata/**"
      - "site/flowleaders/**"
      - "site/hkbasketdata/**"
      - "site/leaderradar/**"
      - "site/master_brief.json"
      - "site/prophet/**"
      - "site/qledger/**"
      - "site/vol/**"
      # F13-X1: build_site imports the source-bound help directory, whose
      # fail-closed contract reads these exact existing owner templates.
      - "templates/_navlinks.html.j2"
      - "templates/_public_footer.html.j2"
      - "templates/_public_nav.html.j2"
      - "templates/measurement.html.j2"
      - "templates/methodology.html.j2"
      - "templates/support.html.j2"
      - "tests/test_biocatalyst_api.py"
      - "tests/test_biocatalyst_d0b_ui.py"
      - "tests/test_biocatalyst_deploy.py"
      - "tests/test_biocatalyst_hydration.py"
      - "tests/biocatalyst_hydration_harness.js"
      - "tests/test_biocatalyst_page.py"
      - "tests/test_biocatalyst_catalyst_radar.py"
      - "tests/test_biocatalyst_catalyst_radar_api.py"
      - "tests/test_biocatalyst_peer_api_contract.py"
      - "tests/test_biocatalyst_worker.py"
      - "templates/biocatalyst.js"
      - "site/biocatalyst.js"
      - "templates/biocatalyst.html.j2"
      - "templates/biocatalyst.css"
      - "site/biocatalyst.css"
    # BioCatalyst's authenticated workbench and deployment-isolation boundary.
    # This is an intentionally bounded command, not part of a single aggregate
    # B1/B1b/B2 command: the legacy pack runner applies timeout-minutes per
    # logical job.  Eight minutes is generous versus the serial profile while
    # still naming the responsible contract if this lane regresses.
    timeout-minutes: 8
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install BioCatalyst test deps
        run: pip install pytest fastapi httpx requests pyyaml jsonschema jinja2 boto3
      - name: BioCatalyst serving, UI, and deployment-isolation contracts
        run: >
          python -m pytest
          tests/test_biocatalyst_api.py
          tests/test_biocatalyst_peer_api_contract.py
          tests/test_biocatalyst_deploy.py
          tests/test_biocatalyst_page.py
          tests/test_biocatalyst_catalyst_radar.py
          tests/test_biocatalyst_catalyst_radar_api.py
          tests/test_biocatalyst_d0b_ui.py
          tests/test_biocatalyst_hydration.py -q

  biocatalyst-evidence:
    if: ${{ false }}
    gate: code
    # Security, source registry, immutable storage and normalized trial facts.
    # Kept separate from worker/history replay so a slow adversarial replay has
    # a bounded, attributable failure rather than consuming the whole suite.
    timeout-minutes: 8
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install BioCatalyst test deps
        run: pip install pytest fastapi httpx requests pyyaml jsonschema jinja2 boto3
      - name: BioCatalyst evidence, projection, and security contracts
        run: >
          python -m pytest
          tests/test_biocatalyst_security.py
          tests/test_biocatalyst_source_registry.py
          tests/test_biocatalyst_record_history_enablement.py
          tests/test_biocatalyst_storage.py
          tests/test_biocatalyst_protocols.py
          tests/test_biocatalyst_trials.py -q

  biocatalyst-worker:
    if: ${{ false }}
    gate: code
    # CURATED SCOPE (2026-08-14 incident follow-up). `scope: exclusive` makes
    # the paths below REPLACE inference instead of being unioned under it, so
    # the opaque fallback tier this job used to carry — whole-tree `site/**`,
    # `templates/**`, `docs/**`, `research/**` claims smeared out of a
    # subprocess or traversal deep in its closure — stops selecting it. Every
    # pattern here was generated from the job's OWN import closure and audited
    # for zero misses: no file this job demonstrably reads lost its owner.
    # 274s for a 43-file closure: the widest weight-to-subject gap in the
    # manifest, and the cleanest exclusive candidate. Three named suites over
    # engine/biocatalyst + the earnings-narrative adapter; the fallback tier
    # was doing all the rest of the selecting.
    scope: exclusive
    paths:
      - "collectors/biocatalyst/**"
      - "config/**"
      - "contracts/**"
      - "data/**"
      - "engine/biocatalyst/**"
      - "engine/earnings_narrative/**"
      - "engine/neuralweb/**"
      - "engine/press/**"
      - "engine/research_vault/**"
      - "engine/sector_intelligence/**"
      - "scripts/biocatalyst_activation.py"
      - "scripts/biocatalyst_worker.py"
      - "tests/test_biocatalyst_activation.py"
      - "tests/test_biocatalyst_worker.py"
      - "tests/test_clinicaltrials_biocatalyst.py"
    # Full B1 collector → private evidence → public-generation worker replay.
    # Twelve minutes preserves measured headroom without hiding a slow worker
    # behind the former twenty-minute all-suite timeout.
    timeout-minutes: 12
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install BioCatalyst test deps
        run: pip install pytest fastapi httpx requests pyyaml jsonschema jinja2 boto3
      - name: BioCatalyst worker and ClinicalTrials current-record contracts
        run: >
          python -m pytest
          tests/test_biocatalyst_activation.py
          tests/test_biocatalyst_worker.py
          tests/test_clinicaltrials_biocatalyst.py -q

  biocatalyst-history:
    if: ${{ false }}
    gate: code
    # CURATED SCOPE (2026-08-14 incident follow-up). `scope: exclusive` makes
    # the paths below REPLACE inference instead of being unioned under it, so
    # the opaque fallback tier this job used to carry — whole-tree `site/**`,
    # `templates/**`, `docs/**`, `research/**` claims smeared out of a
    # subprocess or traversal deep in its closure — stops selecting it. Every
    # pattern here was generated from the job's OWN import closure and audited
    # for zero misses: no file this job demonstrably reads lost its owner.
    # 172s. Same spine as biocatalyst-worker one layer out (history +
    # change-tape suites). Engine subpackages it imports stay declared whole.
    # Top-level `engine/*` is enumerated (2026-08-15 follow-up to #5685):
    # 846 tracked / 74 concrete.
    scope: exclusive
    paths:
      - "engine/theme_graph/identity.py"
      - "engine/theme_graph/__init__.py"
      - "engine/security_state.py"
      - "engine/quote_resolution.py"
      # D5's app/prophet_lab route imports the canonical B1 episode reader and
      # issuer identity chain. This curated job reaches that app router, so its
      # exclusive declaration must cover the exact transitive closure rather
      # than silently skipping changes to these dependencies.
      - "engine/path_risk_signals.py"
      - "engine/stock_identity/__init__.py"
      - "engine/stock_identity/authority.py"
      - "engine/stock_identity/fingerprint.py"
      - "engine/stock_identity/plane.py"
      - "engine/us_candidate_episode.py"
      # W1-B wires Brain's native-fact serve to the frozen W1-A runtime. This
      # job's own app/neuralweb import closure now reaches those packages, so
      # its curated scope must retain their real dependency ownership.
      - "engine/intelligence_workspace/**"
      - "engine/theme_graph/**"
      - "lib/dataos/**"
      - "app/*"
      - "collectors/*"
      - "collectors/biocatalyst/**"
      - "config/**"
      - "contracts/**"
      - "data/**"
      - "engine/__init__.py"
      - "engine/_text_scrub.py"
      - "engine/ai_desk.py"
      - "engine/ai_desk_scorer.py"
      - "engine/altdata_ledger.py"
      - "engine/altdata_models.py"
      - "engine/altdata_picks.py"
      - "engine/altdata_signals.py"
      - "engine/canon.py"
      - "engine/catalyst_tone.py"
      - "engine/china_altdata.py"
      - "engine/china_basket_spine.py"
      - "engine/china_conviction.py"
      - "engine/china_crowding.py"
      - "engine/china_discovery.py"
      - "engine/china_event_calendar.py"
      - "engine/china_extras.py"
      - "engine/china_intel_bus.py"
      - "engine/china_news.py"
      - "engine/china_news_intel.py"
      - "engine/china_radar.py"
      - "engine/china_radar_ledger.py"
      - "engine/china_regime_store.py"
      - "engine/china_signal_lab.py"
      - "engine/china_standout_track.py"
      - "engine/china_validation.py"
      - "engine/cn_newswires.py"
      - "engine/codex_provider.py"
      - "engine/desk_ledger.py"
      - "engine/desk_scorer.py"
      - "engine/earnings_qual.py"
      - "engine/earnings_transcript_intake.py"
      - "engine/entity_resolver.py"
      - "engine/event_calendar.py"
      - "engine/forward_calendar_context.py"
      - "engine/gdelt_client.py"
      - "engine/grading.py"
      - "engine/i18n.py"
      - "engine/indicators_m2.py"
      - "engine/ledger_lane.py"
      - "engine/live_quotes.py"
      - "engine/llm_auth.py"
      - "engine/macro_surprise.py"
      - "engine/manager_lag.py"
      - "engine/manager_quality.py"
      - "engine/manager_trades.py"
      - "engine/master_brain.py"
      - "engine/name_resolver.py"
      - "engine/news_common.py"
      - "engine/news_translate.py"
      - "engine/ollama_provider.py"
      - "engine/opex.py"
      - "engine/options_issue_desk.py"
      - "engine/passport.py"
      - "engine/pooling.py"
      - "engine/portfolio_brief.py"
      - "engine/portfolio_changes.py"
      - "engine/portfolio_vocab.py"
      - "engine/prophet_integrity.py"
      - "engine/provider_health.py"
      - "engine/qbus.py"
      - "engine/qkernel.py"
      - "engine/qledger.py"
      - "engine/qledger_validity.py"
      - "engine/rebalance_calendar.py"
      - "engine/regime_label.py"
      - "engine/regime_vector.py"
      - "engine/session_anchor.py"
      - "engine/smart_money.py"
      - "engine/spine.py"
      - "engine/ticker_shape.py"
      - "engine/translate.py"
      - "engine/trial_ledger.py"
      - "engine/tushare_freshness.py"
      - "engine/universe_history.py"
      - "engine/validation.py"
      - "engine/biocatalyst/**"
      - "engine/capital_structure/**"
      - "engine/chronicle/**"
      - "engine/codex_lane/**"
      - "engine/company_intelligence/**"
      - "engine/earnings_release/binding.py"
      - "engine/earnings_release/figures.py"
      - "engine/earnings_release/filing_key.py"
      - "engine/earnings_release/receipts.py"
      - "engine/context_index/**"
      - "engine/earnings_narrative/**"
      - "engine/fundamental_forensics/**"
      - "engine/government_revenue/**"
      - "engine/marketing/**"
      - "engine/metabolism/**"
      - "engine/entry_radar/__init__.py"
      - "engine/entry_radar/contracts.py"
      - "engine/entry_radar/spool.py"
      # 2026-08-19 LAB-0 §6 commissioning-prep heal: engine/prophet_lab/sources.py
      # (below) now imports engine.entry_radar.spool (the R2-first read seam,
      # mirrors the Radar spool WRITER's own backend ladder) -- never the
      # detector/scoring stack (live_ledger/challengers/detectors), which stays
      # off limits and off this job's closure. spool.py itself only imports
      # entry_radar/contracts.py (stdlib-only) and entry_radar/__init__.py, so
      # exactly these three files, never engine/entry_radar/** broadly.
      - "engine/neuralweb/**"
      - "engine/press/**"
      - "engine/prophet_board_read.py"
      # 2026-08-19 ci-pack-10 heal: this job's inferred (non-exclusive) closure
      # newly reaches these engine/*.py files on the P-LAB-API branch. Verified
      # via a real `git clone --local --depth 1` baseline (not a `git archive`
      # extraction, which has no `.git` and silently under-reports
      # discover_suites()'s tracked-file census) that origin/main shows ZERO
      # uncovered for this SAME job -- so this is a genuine new reachability
      # edge from this branch's diff, not stale pre-existing drift, though the
      # exact causal chain (none of these files are imported by
      # engine/prophet_lab/** or engine/prophet_board_read.py, both already
      # declared above) was not fully isolated within this PR's budget.
      # Widening here per the guard's own remedy ("widening is always the safe
      # direction") rather than leaving the pack red.
      - "engine/oracle/**"
      - "engine/pick_lab/**"
      - "engine/prophet_live/**"
      - "engine/activist.py"
      - "engine/analyst_revisions.py"
      - "engine/anticipation.py"
      - "engine/basket_breadth_divergence.py"
      - "engine/basket_index.py"
      - "engine/basket_mtf.py"
      - "engine/basket_score.py"
      - "engine/basket_tape.py"
      - "engine/baskets.py"
      - "engine/baskets_canada.py"
      - "engine/baskets_china.py"
      - "engine/baskets_hk.py"
      - "engine/baskets_intl.py"
      - "engine/baskets_region.py"
      - "engine/beneficial_ownership.py"
      - "engine/btc_mtf.py"
      - "engine/capital_allocation.py"
      - "engine/coiled.py"
      - "engine/composite_score.py"
      - "engine/conditions.py"
      - "engine/confluence_tiers.py"
      - "engine/conviction_accrual.py"
      - "engine/conviction_delta.py"
      - "engine/cross_asset.py"
      - "engine/cross_asset_trend.py"
      - "engine/crowding.py"
      - "engine/cycles.py"
      - "engine/dannytrades.py"
      - "engine/dannytrades_chip.py"
      - "engine/demand_chain.py"
      - "engine/dispersion.py"
      - "engine/donor.py"
      - "engine/earnings_blackout.py"
      - "engine/earnings_catalyst.py"
      - "engine/entry_primitives.py"
      - "engine/entry_signal.py"
      - "engine/equity_factors.py"
      - "engine/event_atlas.py"
      - "engine/event_landmine.py"
      - "engine/expectation_state.py"
      - "engine/extension.py"
      - "engine/forward_dist.py"
      - "engine/gex_confirm.py"
      - "engine/gex_engine.py"
      - "engine/gov_exposure.py"
      - "engine/greeks.py"
      - "engine/group_flow.py"
      - "engine/hold.py"
      - "engine/htf_durability.py"
      - "engine/hysteresis.py"
      - "engine/ignition_features.py"
      - "engine/index_direction.py"
      - "engine/indicators.py"
      - "engine/inputs.py"
      - "engine/ledger_identity.py"
      - "engine/liquidity_chip.py"
      - "engine/long_hold_clocks.py"
      - "engine/moat_falsifiers.py"
      - "engine/mtf_upturn.py"
      - "engine/name_direction.py"
      - "engine/name_score.py"
      - "engine/name_score_grader.py"
      - "engine/narrative_rotation.py"
      - "engine/options_ivspread.py"
      - "engine/options_structure.py"
      - "engine/path_personality.py"
      - "engine/pit.py"
      - "engine/pit_lag_recorder.py"
      - "engine/playbook.py"
      - "engine/postcross.py"
      - "engine/prophet_bridge.py"
      - "engine/prophet_doors.py"
      - "engine/prophet_stage_inputs.py"
      - "engine/provenance_sidecar.py"
      - "engine/pullback_zone.py"
      - "engine/rate_inflation_transmission.py"
      - "engine/yield_momentum.py"
      - "engine/residual_alpha.py"
      - "engine/risk_sizing.py"
      - "engine/roc_blowoff.py"
      - "engine/sector_pulse.py"
      - "engine/sector_rate_inflation.py"
      - "engine/setup_tier.py"
      - "engine/setups.py"
      - "engine/shadow_book.py"
      - "engine/short_volume.py"
      - "engine/signal_archive.py"
      - "engine/signal_gate.py"
      - "engine/signal_quality.py"
      - "engine/special_arb.py"
      - "engine/special_situations.py"
      - "engine/spotlight.py"
      - "engine/stock_dossier.py"
      - "engine/stock_events.py"
      - "engine/stock_fundamentals.py"
      - "engine/stock_macro_sensitivity.py"
      - "engine/stock_personality.py"
      - "engine/stock_score.py"
      - "engine/stock_technicals.py"
      - "engine/stock_view.py"
      - "engine/sue.py"
      - "engine/t0_indicator.py"
      - "engine/technicals.py"
      - "engine/theme_crowding.py"
      - "engine/theme_scoring.py"
      - "engine/theme_validation.py"
      - "engine/thesis_funnel.py"
      - "engine/ticker_alerts.py"
      - "engine/track_record.py"
      - "engine/us_board_rank.py"
      - "engine/us_candidate_lanes.py"
      - "engine/us_context_vector.py"
      - "engine/us_early_turn.py"
      - "engine/us_prophet_fusion.py"
      - "engine/valuation.py"
      - "engine/velocity.py"
      - "engine/vol_forecast.py"
      - "engine/vol_regime.py"
      - "engine/vol_squeeze.py"
      - "engine/washout_turn.py"
      - "engine/weinstein_stage.py"
      - "research/SIGNAL_EPISODE_ATLAS_MASTERPLAN_BY_FABLE.md"
      - "research/STOCK_PERSONALITY_MASTERPLAN_BY_FABLE.md"
      - "research/US_BOARD_MEASUREMENT.md"
      - "scripts/build_stock_library.py"
      # Import-closure entry: build_stock_library reuses
      # scripts/snapshot_shadow_book.rows_from_board for the shadow-book
      # snapshot (one percentile definition), so exclusive scope must name it.
      - "scripts/snapshot_shadow_book.py"
      - "scripts/check_board_contradictions.py"
      - "scripts/check_signal_gate_coherence.py"
      - "scripts/check_stretch_oracle_contract.py"
      - "scripts/thematic_rotation_phase0.py"
      - "engine/prophet_lab/**"
      - "engine/research_vault/**"
      - "engine/seasonality/**"
      - "engine/sector_intelligence/**"
      - "lib/*"
      - "lib/dataos/identity.py"
      - "research/DO_NOT_REBUILD.md"
      # engine/htf_durability.py reads site/basketdata/fear_greed.json, and
      # engine/pick_lab/candidates.py reads site/flowleaders/leaders.json and
      # site/leaderradar/radar.json — both already-declared engine files
      # (`engine/htf_durability.py`, `engine/pick_lab/**` below), pulled into
      # this job's closure via tests/test_biocatalyst_change_tape_values.py.
      # The import-closure contract
      # (test_curated_exclusive_scopes_cover_their_own_import_closure)
      # requires the declared scope to cover these site reads too, or the job
      # silently stops running for edits to them.
      - "site/basketdata/fear_greed.json"
      - "site/canadabasketdata/**"
      - "site/chinabasketdata/**"
      - "site/coming-soon.html"
      - "site/data/**"
      - "site/factordata/**"
      - "site/flowleaders/leaders.json"
      - "site/hkbasketdata/**"
      - "site/leaderradar/radar.json"
      - "site/master_brief.json"
      - "site/prophet/**"
      - "site/vol/**"
      - "tests/test_biocatalyst_change_classification.py"
      - "tests/test_biocatalyst_change_tape_values.py"
      - "tests/test_biocatalyst_endpoint_alignment.py"
      - "tests/test_biocatalyst_history.py"
      - "tests/test_clinicaltrials_history.py"
    # B1b/B2 Record History retrieval, exact-diff replay and public-safe model
    # projection.  This is deliberately independently bounded because its
    # repeated source-evidence verification is the heaviest validation family.
    timeout-minutes: 12
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install BioCatalyst test deps
        run: pip install pytest fastapi httpx requests pyyaml jsonschema jinja2 boto3
      - name: BioCatalyst Record History replay contracts
        run: >
          python -m pytest
          tests/test_biocatalyst_history.py
          tests/test_biocatalyst_endpoint_alignment.py
          tests/test_biocatalyst_change_classification.py
          tests/test_biocatalyst_change_tape_values.py
          tests/test_clinicaltrials_history.py -q

  biocatalyst-contracts:
    if: ${{ false }}
    gate: code
    # Schema, authority and ownership boundaries stay separately attributable.
    timeout-minutes: 10
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install BioCatalyst test deps
        run: pip install pytest fastapi httpx requests pyyaml jsonschema jinja2 boto3
      - name: BioCatalyst schema, authority, and ownership contracts
        run: >
          python -m pytest
          tests/test_sector_intelligence_contracts.py
          tests/test_launch_slo_evidence_verifier.py
          tests/test_biocatalyst_d0a_design_contract.py
          tests/test_biocatalyst_acceptance_v2.py
          tests/test_biocatalyst_browser_verifier.py
          tests/test_biocatalyst_transcript_adapter.py
          tests/test_biocatalyst_trial_screen.py
          tests/test_biocatalyst_clinicaltrials_discovery.py
          tests/test_biocatalyst_discovery.py
          tests/test_biocatalyst_coverage_epoch.py
          tests/test_biocatalyst_fixed_cohort.py
          tests/test_biocatalyst_fixed_cohort_transport.py
          tests/test_biocatalyst_fixed_cohort_deployment.py
          tests/test_biocatalyst_f0_delta_and_closed_beta_manifest.py
          tests/test_biocatalyst_packet_producer.py
          tests/test_biocatalyst_packet_reader.py
          tests/test_biocatalyst_operational_store.py
          tests/test_biocatalyst_o1b_forward_store.py
          tests/test_biocatalyst_m0a_outcome_family_policy.py
          tests/test_biocatalyst_sponsor_ticker_map.py
          tests/test_biocatalyst_theme_rollup_pit.py
          tests/test_biocatalyst_m0a_clock_activation.py
          tests/test_sector_intelligence_ownership.py -q
      - name: BioCatalyst D0a finite reference integrity
        run: python scripts/verify_biocatalyst_d0a_references.py --reference-integrity

  biocatalyst-deploy-integration:
    if: ${{ false }}
    gate: code
    # The self-heal suite tests the BioCatalyst service update boundary together
    # with the repository's deploy invariants.  It was previously co-run here;
    # giving it its own bounded command keeps that coverage without borrowing
    # time from evidence replay.
    timeout-minutes: 12
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install BioCatalyst test deps
        run: pip install pytest fastapi httpx requests pyyaml jsonschema jinja2 boto3
      - name: BioCatalyst deployment self-heal integration contracts
        run: >
          python -m pytest
          tests/test_deploy_update_self_heal.py -q

  biocatalyst-regulatory:
    if: ${{ false }}
    gate: code
    # Dark B4A official Drugs@FDA archive, parser, typed-index, recovery, and
    # authority contracts. This does not arm collection or publish an API/UI.
    timeout-minutes: 12
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install BioCatalyst test deps
        run: pip install pytest fastapi httpx requests pyyaml jsonschema jinja2 boto3
      - name: BioCatalyst Drugs@FDA dark regulatory evidence contracts
        run: >
          python -m pytest
          tests/test_biocatalyst_drugs_at_fda.py -q

  marketing-social-publisher:
    if: ${{ false }}
    gate: code
    # D02 W1 live social publisher: tweet_length/validate_postable, the Buffer
    # adapter (all HTTP mocked via _transport), and the live runner (kill-switch
    # + --live gating, approved→posting→posted, idempotency, no-double-post).
    # test_marketing_outbox is co-run because W1 extended its status machine.
    # DARK BY DEFAULT: zero live network — every transport path is mocked.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install deps
        # numpy + pandas + pyarrow joined 2026-08-06. #4646 ("tag the theme's own
        # ticker, and name the most-watched member") added nine theme-proxy tests
        # to test_marketing_publish_time_content whose fixture builds correlated
        # bars — `rng.normal` + `pd.DataFrame(...).to_parquet(...)` under the tree
        # theme_proxy.cohesion reads — so this lane is NOT pandas-free any more.
        # The pack runner gives each dependency group a CLEAN `python -m venv`, so
        # the closure has to be complete or the red just moves: measured on main,
        # `pytest pyyaml` → 9 red on numpy, +numpy → 9 red on pandas, +pandas → 23
        # red because the 14 test_marketing_weekend_levels tests that used to
        # importorskip now WAKE UP and want pyarrow, +pyarrow → 450 pass, 0 skip.
        # Do not trim this back to numpy alone, and do not importorskip the nine —
        # this pack is the only job that runs #4646's guard suite.
        run: pip install pytest pyyaml numpy pandas pyarrow
      - name: social publisher + outbox + wiring-layer unit tests
        # test_marketing_live_verify is co-run: the post-time tape gate ships
        # inside the publisher loop (its earnings guard is fail-soft).
        # test_marketing_metrics_poll: the per-post metrics poller + admin join +
        # publisher media-attach gate (Buffer transport mocked).
        # test_marketing_weekend_levels joins (2026-07-27): it was named by NO
        # run step anywhere (unrun-suite rot) while its lane shipped the $AVGO
        # "POC held" post. Covers the cited-level chart contract + the floor
        # copy's language bar; its pandas tests used to importorskip here and now
        # run for real on the deps above.
        # test_marketing_publish_time_read joins 2026-07-28 (XG-W1). It was named
        # by NO job — an unrun suite guarding the daily-read lane's account
        # eligibility. XG-W1 changed exactly that code path (per-call lanes now
        # resolve liveness through the accounts model and honour a lane
        # allowlist), and the suite went red locally on the first run: two of its
        # multi-account tests were asserting behaviour the fix deliberately
        # narrows. Unrun, that would have been a silent rot. Same deps as the
        # rest of this lane, verified green.
        # test_marketing_metrics_poll_quota joins 2026-08-11 (#5306 follow-up).
        # It shipped WITH the 429 fix and was baselined unrun in the same breath,
        # so the poller's own two bounds — stop-on-first-429 and cap-the-run —
        # had no job at all, and #5306 had to duplicate three coarser pins into
        # test_marketing_metrics_poll.py to get ANY guard that executes. Both
        # halves now run in one step, next to the sibling they were split from.
        # Same deps, zero network: the Buffer publisher is a counting stub and
        # every write goes to tmp_path.
        # test_marketing_market_clock joins (2026-08-11): the publish-time
        # clock + lead-fact fan-out gate suite sat in the unrun baseline, and
        # voice v5 (#5291) rotted seven of its fixtures unseen — first-person
        # tails, the register v5 retired, not the gates under test. It drives
        # scripts/marketing_publisher.py end to end, so it lives with the
        # publisher lane it exercises.
        run: python -m pytest tests/test_marketing_social_publisher.py tests/test_marketing_outbox.py tests/test_marketing_outbox_single_composition.py tests/test_marketing_publisher_autoapprove.py tests/test_marketing_live_verify.py tests/test_marketing_publish_time_content.py tests/test_marketing_publish_time_read.py tests/test_marketing_outbox_garbage.py tests/test_marketing_metrics_poll.py tests/test_marketing_metrics_poll_quota.py tests/test_marketing_weekend_levels.py tests/test_marketing_market_clock.py -q
      - name: stale backlog quarantine + optional publish-time phrase fallback
        run: python -m pytest tests/test_marketing_stale_purge.py tests/test_publish_time_llm_phrase.py -q
      - name: liveness tripwire rule engine (the green-and-dark alarm)
        # 2026-08-06→08-10: Buffer 429'd, the kill-switch went to 0, Buffer was
        # fixed and nobody re-armed — five days of GREEN sweeps posting nothing.
        # scripts/marketing_liveness_tripwire.py is the observer for that shape,
        # so it is named by a run step from the day it lands: an alarm nobody
        # tests is the same silence one layer up. stdlib + pyyaml, tmp_path only.
        run: python -m pytest tests/test_marketing_liveness_tripwire.py -q
      - name: unrun-baseline joins 2026-08-11 — approval desk, card pixels, wire expiry
        # Batch join from the unrun_test_baseline triage that followed #5379's
        # voice-v5 rot discovery (test_marketing_market_clock): all suites below
        # sat in config/unrun_test_baseline.json named by NO run step. Ran green
        # on unmodified origin/main 2026-08-11 (TZ=UTC): 1,314 tests, 0 failures.
        # These three of the 33 land HERE and not in marketing-engine because they
        # IMPORT the publisher rather than reading it as source text.
        #   approval_desk — the autonomous approval desk, the only path to live for
        #     the nine planned kinds (imports scripts.marketing_publisher + Receipt)
        #   card_earns_pixels — restatement gates + 1080 square geometry, driving
        #     _bare_cashtag_post / _card_withheld_for_value on real shipped posts
        #   wire_expiry — a queued wire item nobody dispatched must expire, and the
        #     publish-time daily read keeps its longer TTL
        # Verified in a clean pytest+pyyaml+numpy+pandas+pyarrow venv (this lane's
        # exact install line): 308 passed, 0 skipped.
        run: python -m pytest tests/test_marketing_approval_desk.py tests/test_marketing_card_earns_pixels.py tests/test_marketing_wire_expiry.py -q

  marketing-media-pipeline:
    if: ${{ false }}
    gate: code
    # D02 chart-image pipeline: the PNG signal-chart renderer (determinism +
    # magic bytes + no-indicator-words) and the R2 media publisher (fail-soft,
    # S3 stubbed). Separate job because these need Pillow (PNG raster) — the
    # other marketing jobs are stdlib+pyyaml. test_share_cards is co-run here
    # (also Pillow, previously un-whitelisted). ZERO live network / no R2 creds.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install deps
        run: pip install pytest pyyaml pillow
      - name: chart PNG + media publish + share cards unit tests
        run: python -m pytest tests/test_marketing_chart_png.py tests/test_marketing_media_publish.py tests/test_share_cards.py tests/test_marketing_media_fallback_seal.py -q

  marketing-engine:
    if: ${{ false }}
    gate: data
    # Marketing lobe engine unit suite (state, departments, authority ladder,
    # opportunity scoring/bus, campaign compiler, economics, events, ledgers,
    # provenance, claims + seed-ledger round-trips). Never whitelisted when it
    # landed in #2984, so it rotted red on main once the nightly radar accrued
    # live radar-* rows into opportunities.jsonl (a forward ledger, not a
    # shadow-only seed). Deps: stdlib + pytest + pyyaml only — no pandas/numpy,
    # verified against a minimal-import env.
    #
    # Co-run (same minimal deps, never whitelisted anywhere → rotting red on main
    # per the test-whitelist-rot failure mode): the Sentinel gate suite, the
    # content-studio suite, and the admin marketing-panel suite (the panels are
    # tested as plain dict-returning functions, no fastapi/httpx needed).
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install deps
        # jinja2 joins 2026-07-28 for test_wires_rail_b4b: two of its tests RENDER
        # news.html.j2, which is the only way to prove the live-wire rail reaches
        # shipped HTML (every other test in the file reads the template as text, so
        # `{% if false %}` around the section would keep them all green). Without
        # jinja2 here those two importorskip away and the suite is skip-only —
        # scripts/check_skip_only_suites.py fails the build on exactly that.
        run: pip install pytest pyyaml jinja2
      - name: W3 receipt supply + ticker allocation + volume caps + wire fan-out
        run: python -m pytest tests/test_marketing_receipt_supply.py tests/test_marketing_ticker_allocator.py tests/test_marketing_w3_volume_caps.py tests/test_marketing_wire_fanout.py -q
      - name: W5 session-language and durable retirement law
        run: python -m pytest tests/test_marketing_w5_session_language.py -q
      - name: marketing engine unit tests
        # test_marketing_sentinel is co-run (2026-07-23): the Sentinel W1 gate suite
        # was never whitelisted and rotted red on main via a local-vs-UTC clock
        # fixture bomb. test_marketing_content + test_admin_marketing are likewise
        # co-run (never whitelisted → test-whitelist-rot; the admin panels are
        # plain dict-returning functions, no fastapi/httpx). All stdlib-only —
        # verified against a minimal pytest+pyyaml env.
        # test_marketing_personas (Persona Network W1, docket D13) joins the same
        # lane: the spec loader keeps stdlib+yaml at module level and reads the
        # content-studio type-id set through a LAZY import, so a top-level heavy
        # import in engine/marketing/personas.py must turn this job red at
        # collection. Verified in a clean pytest+pyyaml venv.
        # test_marketing_event_language joins too (2026-07-27): the event-post
        # language contract (driver-label translation coverage, no internal-
        # machinery vocab, template coherence). Stdlib-only by construction —
        # it parses engine/market_drivers.py as TEXT instead of importing it.
        # test_marketing_press_feeds (PRESS-FEEDS B1, docket D05 Addendum 2) joins
        # the same lane (2026-07-27): its import closure is stdlib+pyyaml (score_item
        # reuse pulls pandas only via a LAZY function-level import that never fires
        # in the fixture tests), so a heavy top-level import in engine/marketing/
        # press_* must turn this job red at collection. test_marketing_breaking and
        # test_marketing_fastlane are co-run in the same edit — both were never
        # whitelisted anywhere (test-whitelist-rot) and co-run green on pytest+pyyaml.
        # test_marketing_press_copy (PRESS-FEEDS B2-COPY + B4a, docket D05 Add.2)
        # joins the same lane (2026-07-27): the wire-voice / dual-format / tape-stamp
        # / wires.json-sink layers. Import closure is stdlib+pyyaml (wire_* +
        # tape_stamp keep pandas/yaml LAZY at import), so a heavy top-level import
        # turns this job red at collection.
        # test_marketing_ramp_cta_jitter (D08 age ramp + chart CTA + send-time
        # jitter, 2026-07-27) joins the same lane rather than shipping unrun: it is
        # the ONLY guard that the ramp is armed, that the flagship's created: date
        # is present, and that the CTA-on card stays byte-stable. Import closure is
        # stdlib+pyyaml — chart_render keeps pandas LAZY and the four card
        # renderers it exercises take plain lists — verified in a clean
        # pytest+pyyaml venv, so a heavy top-level import in chart_render turns
        # this job red at collection, which is the point.
        # test_marketing_expression_dial (X Growth W1, XG-W1) joins the same lane
        # (2026-07-28): the employee expression dial, the codex quirk pass and the
        # AM-R1 text detectors. engine/marketing/expression_dial.py keeps stdlib +
        # personas at module level (personas itself is stdlib+yaml), so a heavy
        # top-level import there must turn this job red at collection. NOTHING in
        # the suite is importorskip-gated — it runs in full on pytest+pyyaml, so
        # it can never become a skip-only suite. Verified in a clean venv.
        # test_marketing_cadence_spine (X Growth W2, XG-W2) joins the same lane
        # (2026-07-28): the per-account cadence resolver, the `session` schema
        # field, the one-conversation-one-owner lock, the cross-account near-dup
        # radar, and the outbox KINDS hardening that moved press_lane + fastlane
        # onto make_item/validate_item. It reaches ACROSS both marketing lanes on
        # purpose — press_lane/fastlane/personas live here, the publisher half
        # lives in marketing-social-publisher — because the wave's whole point is
        # that those halves now share one contract, and a test exercising only
        # one side would miss the seam. Import closure is stdlib+pyyaml:
        # cadence_resolver/story_lock/wire_routing keep personas, outbox and
        # zoneinfo lazy or stdlib, and scripts/marketing_publisher already runs
        # on the same two deps in the sibling lane. Nothing is importorskip-gated,
        # so it can never decay into a skip-only suite — verified in a clean
        # pytest+pyyaml venv.
        # test_marketing_chart_coverage (2026-07-28, #3895) joins the same lane:
        # the guard that every ticker-bearing post carries an illustration, that a
        # demoted signal keeps its card WITHOUT the SETUP pill, that chart ids stay
        # per-account (the Sentinel shared_media fingerprint), and that the ramp's
        # media cap never sits below its post cap. 9 of its 12 tests are pandas-free
        # and run here — content_studio/chart_render keep pandas LAZY, so a heavy
        # top-level import turns this job red at collection, which is the point. The
        # other 3 need real bars off a parquet (the tape card has no v1 fallback by
        # design) and importorskip here; chart-render-data below names the file so
        # they are not unrun.
        # test_marketing_desk_feeds (X Growth W3, XG-W3) joins the same lane
        # (2026-07-28): the franchise register + slot scheduler, the four-lane
        # desk feed, the persona memory stores, and the Gift-Grip-Proof gate.
        # Import closure is stdlib + pyyaml — franchises/desk_feed/value_gate/
        # persona_memory keep copywriter, outbox, content_studio, story_lock,
        # cadence_resolver and engine.chronicle.context_pack LAZY (function-level
        # imports), so a heavy top-level import in any of them turns this job red
        # at collection, which is the point. Two guards in the suite are AST
        # walks over committed source (the consolidator-only-writer guard and the
        # no-promotion guard) and need no imports at all. Nothing is
        # importorskip-gated, so it can never decay into a skip-only suite —
        # verified in a clean pytest+pyyaml venv.
        # test_marketing_reply_desk (XG-W4) joins on the same terms: the reply
        # discovery provider, deterministic scorer, drafter, critic pass, queue,
        # exporter, and the sentinel reply-counter fix. Stdlib+pyyaml only, and
        # nothing importorskip-gated.
        # test_marketing_forward_booking (2026-07-28) joins the same lane: the
        # publish-lane throughput fix (an item inside the global floor books
        # forward instead of deferring a whole cron sweep, bounded by a
        # tape-freshness horizon) and the media-URL recovery sidecar. It also
        # pins the two workflow regressions that made the lane publish nothing
        # that day — a full-history checkout that outran the job timeout, and
        # outbox ledgers with no union merge, so every retry-rebase conflicted.
        # Those three guards read committed YAML/gitattributes and need no
        # imports; the rest are stdlib + pyyaml. Nothing importorskip-gated.
        # test_marketing_recall (2026-07-28) joins the same lane: the other half
        # of the operator kill switch. max_forward_book_min (the suite above)
        # made a sweep book posts into Buffer's queue up to an hour ahead, and
        # MARKETING_PUBLISH_ENABLED=0 has no reach into those — five booked posts
        # outlived the disarm by 41 minutes the day it shipped. The suite pins
        # the Buffer deletePost adapter, the posted→recalled edge and its
        # terminality, the already-sent-is-untouchable rule, the zero-network
        # dry-run, and the workflow/admin wiring that makes DISARM mean stop.
        # Import closure is stdlib + pyyaml — the same two deps the publisher
        # half runs on in marketing-social-publisher, with every Buffer call
        # behind the _transport()/fake-publisher seam, so a heavy top-level
        # import in social_publisher or marketing_recall turns this job red at
        # collection. Its one yaml-reading helper importorskips, which is moot
        # here (pyyaml is installed above) and keeps the file honest elsewhere.
        # test_marketing_learning + test_marketing_reply_producer (XG-W6) join
        # the same lane (2026-07-28): the labels store and its nightly
        # consolidator, parent-adjusted reply labels, the per-account health
        # monitor + network tripwire + halt registry, the pre-registered
        # blind-identity harness, the learned-rule reversibility gate, and the
        # producer that finally walks discovery -> score -> draft -> critics ->
        # enqueue. Import closure is stdlib+pyyaml by construction: labels,
        # health_monitor, learned_rules, blind_identity and reply_producer keep
        # outbox, reply_queue, expression_dial and market_facts behind LAZY
        # function-level imports, so a heavy top-level import in any of them
        # turns this job red at collection, which is the point. Three guards in
        # the suite are AST walks over committed source (the consolidator-only-
        # writer guard, the no-LLM-in-the-monitor guard, and the
        # blind-identity-gates-nothing grep) and need no imports at all. Nothing
        # is importorskip-gated, so neither suite can decay into a skip-only one.
        # test_marketing_scoring_brain (XG-W5 = IS-W2) joins the same lane
        # (2026-07-28): the L0 story spine, the six L1 deterministic features,
        # the P0 garbage gate, and the golden-set / precision@20 harness. Import
        # closure is stdlib + pyyaml — story_spine keeps datasketch/model2vec
        # behind LAZY, guarded probes and signal_features/garbage_gate/golden_set
        # are stdlib-only — so a heavy top-level import in any of them turns this
        # job red at collection, which is the point. NOTHING here is
        # importorskip-gated: the near-dup pass's optional dependency lives in
        # the SEPARATE suite below (marketing-scoring-optional), and the semantic
        # branch is exercised here with an INJECTED encoder so the model2vec
        # artifact — an operator/R2 step by design — never becomes a CI
        # dependency.
        # test_marketing_hot_tape_llm (Hot Tape P2, 2026-07-28) joins the same
        # lane: the LLM wire desk that phrases a fired intraday event from the
        # radar's FactPacket, behind the numeric-consistency gate (masterplan
        # gate 0.3 — the engine computes, the model phrases, the LLM never
        # originates a number) plus the no-calls and no-hedging screens (0.4 and
        # the wire register). Import closure is stdlib + pyyaml BY
        # CONSTRUCTION: engine/marketing/hot_tape_llm.py keeps llm_auth,
        # anthropic, yaml, lib.config, copywriter, social_publisher and
        # wire_voice behind LAZY function-level imports, so a top-level heavy
        # import there turns this job red at collection — which is the point,
        # because this lane has no anthropic package at all. Two of the tests
        # are AST/source scans over the module that need no imports; every
        # provider-path test monkeypatches engine.llm_auth, so the suite touches
        # no network and reads no credential. Nothing is importorskip-gated.
        # Verified in a clean pytest+pyyaml+jinja2 venv.
        # test_marketing_hot_tape + test_marketing_hot_tape_radar (Hot Tape P1)
        # join the same lane 2026-07-28. This lane's minimal env IS their
        # contract: the radar runs on a shallow ubuntu checkout that installs
        # pyyaml+requests+pyarrow and NO pandas, so hot_tape/hot_tape_wire keep
        # module-level imports stdlib-only, and scripts/hot_tape_radar.py keeps
        # requests, chart_render and media_publish behind LAZY function-level
        # imports. A heavy top-level import in any of them turns this job red at
        # collection, which is the point. Neither suite is importorskip-gated —
        # the pandas half of the program (the nightly context pack) is a separate
        # file that runs in marketing-data below, so nothing here can decay into
        # a skip-only suite.
        #
        # test_marketing_copy_v2.py joins 2026-07-29 (Content Studio W1): the
        # per-post writer, the cold-read critic and the six defect-class gates
        # whose negative fixtures are real strings from the rejected batch.
        # Same construction as the hot-tape suite above — copywriter.py and
        # copy_critic.py keep llm_auth/anthropic/lib.config behind lazy
        # function-level imports and the suite scans the source to pin it, so a
        # top-level heavy import reddens this lane at collection ON PURPOSE.
        # Provider-path tests hand build_providers a fake local client: the real
        # waterfall and the real request builder run, only the transport is
        # fake. No network, no credential, nothing importorskip-gated.
        #
        # E-wave suites join 2026-07-29 (ecosystem completion, masterplan §10):
        # test_marketing_press_wire (E7 Actions press bridge — committed
        # cursor/spend state, tier cadence, daemon-active no-op),
        # test_marketing_x_intel (E3 harvester/exemplar store/budget caps),
        # test_marketing_reply_voice (E4 reply LLM pass + reply_value critic),
        # test_marketing_filing_lanes (E2 congress/insider/house-picks). All
        # keep the lane minimal-env law: providers monkeypatched, no network,
        # lazy heavy imports pinned by source scans.
        # test_marketing_selection.py joins alongside it (Content Studio W1,
        # Builder B side): the cross-day cooldowns, the fact-reuse budget, the
        # degenerate-stat gate, the shape mixer, the emit-time no-fallback
        # refusal, stale-queued expiry and the kind-scoped auto-approve. Its
        # writer is a STUB injected by monkeypatching copywriter.
        # write_posts_llm_v2 and every ledger read/write is under tmp_path, so
        # the suite is stdlib+pyyaml like the rest of this lane. It also pins the
        # LIVE config/marketing.yml arming keys (copywriter.llm.required,
        # publish.auto_approve_scope) — a code default cannot be the whole story
        # when the config file is the operator surface.
        # test_marketing_cadence_ramp_coherence (2026-07-28) joins the same lane:
        # the persona cadence blocks and the D08 ramp are ANDed at post time, and
        # nothing kept them in the same units — the specs' W1 numbers (3-4/day at a
        # 120-180 MINUTE floor) would have cut the network from 70 posts/day to 20
        # the moment the resolver armed. It pins posts_per_day against every ramp
        # tier the desk ages into, the spacing floor against the ladder window AND
        # against one publisher sweep minus the send jitter, the session fence
        # against the sweep grid, and the weekend shape against the operator's
        # 10/day floor. Reads committed config + the publish workflow's cron only;
        # import closure is stdlib + pyyaml (cadence_resolver, personas, accounts,
        # outbox and sentinel all stay on those two in the lanes above), and
        # nothing is importorskip-gated.
        # test_marketing_ramp_speedup (2026-08-03) joins the same lane: the D08
        # tier boundaries became config keys and graduate_after_days stopped
        # being clamped up to a hardcoded 28, so a "we sped the ramp up" config
        # edit can no longer read as applied while every desk stays on the old
        # schedule. Reads committed config only; stdlib + pyyaml like its
        # neighbours.
        # test_copywriter.py joins 2026-08-03: the suite predated this lane's
        # roster and was named in NO CI job — unrun-suite rot, so regressions
        # in copywriter.write_posts_deterministic shipped dark. It pins
        # verify_signal_live's kill rules, validate_copy's defect gates, the
        # numbers whitelist, the deterministic writer's no-dupe/no-filler laws
        # and the graded-receipts grading. Same lane contract as copy_v2 above:
        # copywriter keeps every heavy import lazy, the test file's own imports
        # are stdlib-only, nothing importorskip-gated. Verified in a clean
        # pytest+pyyaml+jinja2(+requests+pyarrow) venv.
        # test_market_facts.py joins 2026-08-12 (W2D): another unrun-suite-rot
        # grandfather (config/unrun_test_baseline.json) closed on contact — it
        # is the suite docs/marketing_voice_doctrine_v5.md Hard Ban #9 and
        # market_facts._claims_level_words both name BY FILENAME as the
        # regression pin for the k/m/b compact-number register, so shipping
        # that switch while its own test stayed dark would have defeated the
        # point. Data-dependent cases `pytest.skip` on a missing source file
        # (regime/heatmap/confluence json) rather than fail — the same idiom
        # already trusted for copy_v2/copywriter above — so it degrades safely
        # in a sparse checkout and simply runs more of itself where the data
        # exists. engine.marketing.market_facts imports stdlib + re only.
        # test_marketing_press_relay_quality (W2E, 2026-08-11) joins the same
        # lane: the compose-or-drop gate, the deterministic macro-print composer,
        # the named non-news admission families and the send-time interrogative
        # ban, pinned against the eight headlines that shipped in raw register to
        # the flagship account on 2026-08-11. It belongs HERE rather than beside
        # the publisher because the screens under test are the LANE's — press_lane
        # + relay_hygiene + garbage_gate — and its one publisher-side assertion
        # calls copywriter.voice_v5_violations directly, which this lane already
        # exercises. Import closure is the same stdlib+pyyaml contract its
        # neighbours run on: it drives run_press_tick exactly as
        # test_marketing_press_copy does, and breaking_relevance keeps pandas
        # lazy AND fail-soft behind the universe load.
        run: python -m pytest tests/test_marketing_press_relay_quality.py tests/test_marketing_engine.py tests/test_marketing_sentinel.py tests/test_marketing_content.py tests/test_admin_marketing.py tests/test_admin_marketing_floor.py tests/test_marketing_personas.py tests/test_marketing_event_language.py tests/test_marketing_press_feeds.py tests/test_marketing_breaking.py tests/test_marketing_fastlane.py tests/test_wires_rail_b4b.py tests/test_marketing_press_copy.py tests/test_marketing_intelligence_desk.py tests/test_marketing_press_stream.py tests/test_marketing_ramp_cta_jitter.py tests/test_marketing_expression_dial.py tests/test_marketing_cadence_spine.py tests/test_marketing_chart_coverage.py tests/test_marketing_desk_feeds.py tests/test_marketing_reply_desk.py tests/test_marketing_reply_warmth.py tests/test_marketing_reply_tails.py tests/test_marketing_forward_booking.py tests/test_marketing_recall.py tests/test_marketing_learning.py tests/test_marketing_reply_producer.py tests/test_marketing_scoring_brain.py tests/test_marketing_hot_tape_llm.py tests/test_marketing_hot_tape.py tests/test_marketing_hot_tape_radar.py tests/test_marketing_copy_v2.py tests/test_copywriter.py tests/test_market_facts.py tests/test_marketing_copy_auditor.py tests/test_marketing_bare_cashtag.py tests/test_marketing_voice_laws.py tests/test_marketing_voice_v5.py tests/test_marketing_money_format.py tests/test_marketing_publisher_names.py tests/test_marketing_media_backfill.py tests/test_chart_facts.py tests/test_confluence_source.py tests/test_chart_since_setup.py tests/test_marketing_selection.py tests/test_marketing_press_wire.py tests/test_marketing_x_intel.py tests/test_marketing_reply_voice.py tests/test_marketing_filing_lanes.py tests/test_marketing_cadence_ramp_coherence.py tests/test_marketing_ladder_collapse.py tests/test_marketing_wire_headroom.py tests/test_marketing_copy_yield.py tests/test_marketing_news_arming.py tests/test_marketing_breaking_card_geometry.py tests/test_marketing_ramp_speedup.py tests/test_news_translate_render_gate.py -q
      - name: unrun-baseline joins 2026-08-11 — engine, reply desk, wire relay, cards
        # Batch join from the unrun_test_baseline triage that followed #5379's
        # voice-v5 rot discovery (test_marketing_market_clock): all suites below
        # sat in config/unrun_test_baseline.json named by NO run step. Ran green
        # on unmodified origin/main 2026-08-11 (TZ=UTC): 1,314 tests, 0 failures.
        # They land HERE, not in the publisher lane, because none of them IMPORTS
        # the publisher — cold_read and relay_hygiene read
        # scripts/marketing_publisher.py as TEXT, the rest are engine-side — so
        # this lane's minimal env is their contract too: a heavy top-level import
        # in anything they touch reddens the job at collection, which is the point.
        #   ad_central — Ad Central §0 gates: spend needs three arms, denominators
        #   allies — Marketing Allies W1 engine (MKT-D11)
        #   anchor_law — the 2026-08-01 anchorless-macro kill: a read names its print
        #   attribution — UTM join (D07 W1b): opaque refs, PII-stripped signups, dedup
        #   breaking_card — breaking card renders: parses as XML, source chip, no script
        #   card_ticker_agreement — the $DVN post over an RMBS chart; charts must agree
        #   cold_read — the cold-read VETO (publisher source read as text, not imported)
        #   copy_review — batch review: eight posts one skeleton, each fine alone
        #   earnings_call_lane — Chronicle -> X earnings-call derivative contract
        #   emitted_kind_ownership — every nightly kind has an owner that can DECIDE it
        #   lane_rerun — a lane re-run REPLACES; content-derived ids duplicated a day
        #   links — Funnel W1a (D07) UTM link builder + short-link lane
        #   macro_print_tier — release importance picks the desk; minor prints leave
        #   (metrics_poll_quota is NOT here: armed PR #5368 wires it into
        #    marketing-social-publisher beside metrics_poll, so this batch yields it)
        #   mover_stance — the canned directional stance ("I'd let it settle first")
        #   next_review — computed from cadence, never a hardcoded (permanently stale) date
        #   rejections — reject is terminal and reasoned; export clears box, not corpus
        #   relay_hygiene — the source's page is not our post (source read as text)
        #   reply_elements — XG-W4b §D two-of-five engagement floor + four prohibitions
        #   reply_pacing — burst discipline, daily caps, the two per-target floors
        #   reply_register — XG-W4b §C register laws + the prompt-vs-critic contract
        #   reply_shape — XG-W4b §A/§B: the composer had ONE shape for nine of ten
        #   reply_targets — CURATION of config/reply_targets.yml (schema lives elsewhere)
        #   rewrite — a rewrite SUPERSEDES; appending put three $FDS posts in one slot
        #   source_authority — a citation must earn its place (operator outlet law)
        #   tape_reaction_basket — 83 breaking rows shipped an empty tape_stamp
        #   telemetry — ingest/join/rollup + the n<20 seeding floor and honest empty case
        #   wire_dehandle — we reword and republish; we never tag the source account
        #   wire_single_statement — a relay ships ONE statement, never the sentence twice
        #   wire_story — D1 one event, one post; its docstring already names this lane
        #   wire_volume — a dark desk PARKS; no account takes an unbounded firehose
        # Verified in a clean pytest+pyyaml+jinja2 venv: 1006 passed, 0 skipped.
        run: python -m pytest tests/test_marketing_ad_central.py tests/test_marketing_allies.py tests/test_marketing_anchor_law.py tests/test_marketing_attribution.py tests/test_marketing_breaking_card.py tests/test_marketing_card_ticker_agreement.py tests/test_marketing_cold_read.py tests/test_marketing_copy_review.py tests/test_marketing_earnings_call_lane.py tests/test_marketing_emitted_kind_ownership.py tests/test_marketing_lane_rerun.py tests/test_marketing_links.py tests/test_marketing_macro_print_tier.py tests/test_marketing_mover_stance.py tests/test_marketing_next_review.py tests/test_marketing_rejections.py tests/test_marketing_relay_hygiene.py tests/test_marketing_reply_elements.py tests/test_marketing_reply_pacing.py tests/test_marketing_reply_register.py tests/test_marketing_reply_shape.py tests/test_marketing_reply_targets.py tests/test_marketing_rewrite.py tests/test_marketing_source_authority.py tests/test_marketing_tape_reaction_basket.py tests/test_marketing_telemetry.py tests/test_marketing_wire_dehandle.py tests/test_marketing_wire_single_statement.py tests/test_marketing_wire_story.py tests/test_marketing_wire_volume.py -q
      - name: reply-target register validates (config/reply_targets.yml)
        # The register is operator-curated, so a bad handle or an unknown tier is
        # an editing mistake, not a code change. Runs as its own step so a
        # register-only PR fails on the register, not inside a pytest summary.
        run: python -c "from engine.marketing import reply_discovery as r; e = r.validate_register(r.load_register()); [print(f'::error title=reply-register::{x}', flush=True) for x in e]; raise SystemExit(1 if e else 0)"
      - name: persona specs validate (config/personas/*.yml)
        # The gate the specs exist for: every committed spec carries the full
        # content-studio tilt key set, a live template-pool voice key, and the
        # AM-R1 banned patterns. Runs as its own step so a spec-only PR fails on
        # the spec, not inside a pytest summary.
        run: python -m engine.marketing.personas --check

  marketing-scoring-optional:
    if: ${{ false }}
    gate: code
    # XG-W5 (= IS-W2) optional-dependency lane: the MinHash-LSH near-dup pass in
    # engine/marketing/story_spine.py.
    #
    # WHY THIS LANE EXISTS. The scoring brain's deterministic core is stdlib +
    # pyyaml and runs in marketing-engine, whose minimal env IS its contract. The
    # near-dup pass needs `datasketch` (which pulls numpy + scipy), so it cannot
    # run there. The wrong fix — the one this repo has shipped twice (#3703,
    # #3760) — is `pytest.importorskip("datasketch")` inside the thin lane: main
    # goes green and weaker in the same commit, every census reports the pass as
    # covered, and it executes in zero jobs.
    #
    # So tests/test_marketing_scoring_optional_deps.py imports datasketch at
    # MODULE level with NO importorskip, and this job is the only one that names
    # it. A missing wheel here is a COLLECTION ERROR — red, loud, immediate. The
    # probe step below makes a dropped install line red for the same reason, and
    # -rs surfaces any NEW skip that appears in the suite.
    #
    # model2vec is deliberately absent: the embedding artifact is an
    # operator/R2 step (docs/scoring_brain.md §3) and must never become a CI
    # dependency. The semantic clustering BRANCH is ours and is tested with an
    # injected encoder in the thin lane; model2vec's own inference is theirs.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install deps
        run: pip install pytest pyyaml datasketch
      # The lane's own tripwire: if a future edit drops datasketch from the line
      # above, the suite would error at collection anyway — but this step names
      # the cause in one line instead of burying it in a pytest traceback.
      - name: prove the optional dependency is present
        run: python -c "import datasketch, numpy; print(datasketch.__version__)"
      - name: scoring-brain optional-dependency paths
        run: python -m pytest tests/test_marketing_scoring_optional_deps.py -q -rs

  marketing-data:
    if: ${{ false }}
    gate: data
    # The data-dependent half of two marketing suites whose only lanes are
    # deliberately stdlib+pyyaml. Same pairing shape as chart-render /
    # chart-render-data: the thin lanes stay thin because their minimal env IS the
    # contract, and this lane proves the code behind the skips.
    #
    # Added 2026-07-26 by scripts/check_skip_only_suites.py, on its first run:
    #   * test_marketing_content.py:531 importorskip("pandas")/("pyarrow") — the
    #     engine.marketing.chart_render load_closes round-trip against the committed
    #     data/stocks/PLTR.parquet. marketing-engine (pytest+pyyaml) was the only
    #     job naming the file.
    #   * test_marketing_live_verify.py:250 importorskip("pandas") — the earnings
    #     guard set read from a parquet the test writes itself. marketing-social-
    #     publisher (pytest+pyyaml) was the only job naming the file, and its own
    #     comment recorded the skip as intentional ("the one pandas-dependent test
    #     importorskips") without noticing that made it run nowhere.
    # pyarrow is installed even though only test_marketing_content names it: the
    # live_verify test calls DataFrame.to_parquet, which needs a parquet engine.
    #
    # The thin lanes keep a real contract. marketing-engine's comment states it
    # ("stdlib + pytest + pyyaml only — no pandas/numpy, verified against a
    # minimal-import env"); a top-level `import pandas` in the marketing engine or
    # the publisher loop must still turn those lanes red at collection.
    #
    # Deps are byte-identical to the 22-job `pytest pandas numpy pyarrow pyyaml`
    # group (trial-budgets, outcome-spine, chart-render-data, …) that run_ci_pack.py
    # keys venv reuse on, so this job provisions NO new environment.
    #
    # Verified locally in clean venvs with each lane's exact install line:
    #   pytest pyyaml                      → 101 passed, 2 skipped
    #   pytest pandas numpy pyarrow pyyaml → 103 passed
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install deps
        run: pip install pytest pandas numpy pyarrow pyyaml
      # The lane's own tripwire: if a future edit drops pandas or pyarrow from the
      # install line above, these tests would go back to skipping silently and the
      # lane would stay green while guarding nothing. The probe makes that red, and
      # -rs prints every skip reason so a NEW skip is visible in the log.
      - name: prove the data-dependent modules are present
        run: python -c "import pandas, pyarrow, numpy"
      - name: marketing suites (data-dependent tests execute here)
        # test_marketing_breaking.py joins this fat lane 2026-07-27 (PRESS-FEEDS B1):
        # two of its tests (TestUniverseCacheAndEnrichment) build parquet fixtures and
        # were converted from a bare `import pandas` to importorskip("pandas")/("pyarrow")
        # so the thin marketing-engine lane stays pandas-free. That made them SKIP-ONLY
        # (marketing-engine was their only job) — scripts/check_skip_only_suites.py
        # requires a job that actually installs pandas+pyarrow to run them, which is here.
        #
        # test_marketing_weekend_levels.py joins 2026-07-28, same class, third time:
        # it landed named ONLY by marketing-social-publisher (pytest+pyyaml), whose
        # comment recorded the skip as intentional ("its one pandas test importorskips
        # here") — the identical sentence test_marketing_live_verify's entry above was
        # added to fix. An importorskip the thin lane can never satisfy does not make
        # a test optional, it makes it unrun, and that is what turned main red rather
        # than the suite's own lane. The thin lane keeps the file for its pandas-free
        # tests; :105's cited-level round-trip executes here.
        #
        # test_marketing_chart_coverage.py joins 2026-07-28 (#3895), same class,
        # fourth time: three of its tests importorskip pandas/pyarrow because a
        # `chart`/`watchlist` card only exists when render_chart_v2 reads real bars
        # (the tape variant has NO v1 fallback — v1 hard-draws a green "BUY" label,
        # which would contradict a "not buying yet" post). Those three are the ones
        # that actually pin the operator's defect — the widened type filter, the
        # live-gate-picks-the-variant rule, and the data/baskets/ohlcv search order
        # that made 30 of the plan's 43 tickers unchartable. Named in the thin lane
        # only, they would have skipped everywhere and the fix would have shipped
        # unguarded.
        #
        # test_marketing_hot_tape_pack.py joins 2026-07-28 (Hot Tape P1) — but as
        # the OPPOSITE case to the four above, and it belongs here from birth
        # rather than by repair. The nightly context pack is the one half of the
        # program that is deliberately pandas/numpy-heavy (it scans ~20k daily
        # parquets in a ProcessPoolExecutor), so its suite could never run in the
        # thin marketing-engine lane where its two siblings live. Named here, and
        # only here, so the split is explicit instead of a silent skip.
        #
        # test_marketing_filing_lanes.py joins 2026-07-29 (E2 review), same class
        # as the first four, fifth time: its congress/insider/house-pick tests
        # build quiver parquet fixtures behind importorskip("pandas")/("pyarrow"),
        # and the thin marketing-engine lane that names the file installs neither
        # — so the tests that pin the filing lanes' no-score / no-BUY-chart /
        # display-rounding guards were SKIP-ONLY. The thin lane keeps the file
        # for its pandas-free config and gating tests; the parquet-backed tests
        # execute here.
        #
        # FOUR SUITES JOIN 2026-07-30, same class, fifth time — and two of them were
        # wired into the THIN marketing-engine lane hours earlier by the very edit
        # that was supposed to end their darkness (#4030). They had sat UNRUN — named
        # by no job at all — which is why a 90-bar 52-week window shipped a high that
        # was 18.2% wrong on MSFT and 11 of 12 names with it. Naming them in the thin
        # lane traded one darkness for another:
        #   * test_confluence_honesty / test_tech_confluence import numpy at MODULE
        #     level, so they turned marketing-engine red at COLLECTION (pack-3).
        #   * test_chart_facts / test_marketing_bare_cashtag carry importorskip gates
        #     the thin lane can never satisfy, so every gated test SKIPPED and
        #     check_skip_only_suites.py failed the build (pack-1) — precisely the
        #     "green and weaker in the same commit" trade that guard exists to refuse.
        # The gates stay; the suites get a lane that satisfies them. The thin lane
        # keeps test_chart_facts and test_marketing_bare_cashtag for their
        # dependency-free tests, which is the split the guard is asking for.
        #
        # THREE SUITES JOIN 2026-08-02 (TrendSpider hardening PR-B — the supply
        # widening: cashtag tiers 520 → 1,317 names, mover board 503 → ~1,190,
        # six new read-only candidate pools). Two of them were already DARK:
        #   * test_movers_source.py and test_marketing_radar.py were named by NO
        #     job in this manifest. `grep -rn` across .github/ returned nothing
        #     for either. They are the only guards the two modules PR-B edits
        #     have — the tier ladder, the T3 mover exclusion, the theme-list
        #     whitelist — and they have been running nowhere, which is the
        #     unrun-suite class this file has now repaired six times. Shipping a
        #     universe widening while its own regression suite stays dark would
        #     have been the seventh.
        #   * test_marketing_supply_feeds.py is new and belongs here from birth:
        #     every pool fixture is a parquet (options_flow, WSB, earnings,
        #     stage backfill), so the thin marketing-engine lane could never run
        #     it — the hot_tape_pack case above, not a repair.
        # All three need pandas+pyarrow, which this lane already installs, so
        # they provision no new environment.
        #
        # ONE SUITE JOINS 2026-08-03 (TrendSpider hardening PR-C — the chart
        # director, the timeframe fact layer and the attention selector).
        # test_chart_director.py builds a real parquet store under tmp_path to
        # exercise the claim-window law and the doctrine table, so like
        # test_marketing_supply_feeds above it belongs in the FAT lane from
        # birth: on the thin marketing-engine lane every one of those tests
        # would importorskip and check_skip_only_suites.py would fail the build,
        # which is the "green and weaker in the same commit" trade that guard
        # refuses. It carries this program's four acceptance gates — the
        # claim-window refusal, the forming-bar exclusion, the in-frame caption
        # gate and the long-tail annotation — and a gate that runs nowhere is
        # not a gate.
        #
        # test_marketing_publish_time_content.py joins 2026-08-06, same class as
        # live_verify / weekend_levels / chart_coverage / filing_lanes above —
        # but caught the OTHER way round, and that difference is the lesson. The
        # four of them were found by check_skip_only_suites.py as silent skips.
        # This one had no gate at all: #4646 added the theme-proxy wiring tests
        # with a bare `import numpy` / `import pandas` / `.to_parquet()` inside
        # the _write_proxy_bars fixture, while the ONLY job naming the file is
        # marketing-social-publisher (pytest+pyyaml). A bare import does not skip
        # — it ERRORS — so instead of running nowhere and reading green, the nine
        # proxy tests turned main RED and pinned every unrelated PR behind them.
        # The fixture is now importorskip-gated like its four predecessors, which
        # would have made it skip-only in that one thin lane, so it is named here
        # in the same commit. What it needs — numpy, pandas, a parquet engine —
        # is exactly what theme_proxy.cohesion itself needs (both LAZY inside the
        # module, so the thin lane's collection-time canary is untouched: a
        # top-level pandas import in publish_time_content still turns it red).
        # This lane already installs all three, so it provisions NO new
        # environment. The thin lane keeps the file for its 106 stdlib tests.
        #
        # test_marketing_theme_proxy.py joins 2026-08-07 — #4646's OTHER half, and
        # the half the entry above did not close. That paragraph repaired the
        # suite whose bare imports turned main red; this is the suite that tests
        # the module those wiring tests wire, engine/marketing/theme_proxy.py, and
        # it was named by NO job in this manifest at all (`grep -rn
        # test_marketing_theme_proxy .github/` returned nothing). Loud failure got
        # fixed and the silent one beside it did not, which is the whole reason
        # UNRUN outlives the class that reds the build. It is the ONLY suite
        # covering that module's gate, and the gate's value is what it says NO to
        # — a rule that always prefers the sector ETF is wrong more often than the
        # member-only status quo it replaces — so the refusals are exactly what has
        # been shipping unguarded. It belongs in the FAT lane from birth rather
        # than by repair (the hot_tape_pack case): numpy and pandas are imported at
        # MODULE level, so the thin lane would error at collection, and the bar
        # fixtures are written as real parquet under a tmp data/baskets/ohlcv
        # because a stubbed cohesion would leave the leg that kills $XBI-on-biotech
        # untested. pandas+numpy+pyarrow are already installed here, so it
        # provisions NO new environment.
        # Verified in a clean venv with this lane's exact install line: 23 passed,
        # and the full run line below 826 passed / 0 skipped.
        run: python -m pytest tests/test_marketing_content.py tests/test_marketing_live_verify.py tests/test_marketing_breaking.py tests/test_marketing_weekend_levels.py tests/test_marketing_chart_coverage.py tests/test_marketing_hot_tape_pack.py tests/test_marketing_filing_lanes.py tests/test_chart_facts.py tests/test_marketing_bare_cashtag.py tests/test_confluence_honesty.py tests/test_tech_confluence.py tests/test_news_translate_en.py tests/test_marketing_supply_feeds.py tests/test_movers_source.py tests/test_marketing_radar.py tests/test_chart_director.py tests/test_marketing_publish_time_content.py tests/test_marketing_theme_proxy.py -q -rs

  publish-r2-client:
    if: ${{ false }}
    gate: data
    # scripts/publish_r2._client's retry posture — 10 adaptive attempts and the
    # 15s/60s connect/read bounds that replaced the 4/standard config which died
    # 2026-07-16 mid-nightly. The assertion reads botocore's normalized config, so
    # it is `importorskip("boto3")`-gated and offline (no creds, no network).
    #
    # Added 2026-07-26 by scripts/check_skip_only_suites.py. unrun-publish-ops names
    # test_publish_r2.py, but its install line — the 21-job jinja2/plotly/sklearn
    # group — has no boto3, and it was the only job naming the file, so the retry
    # guard executed nowhere. unrun-publish-ops is deliberately left alone: adding
    # boto3 to ITS line would fork one job out of a 21-member venv group and cost a
    # whole new environment, whereas this install line is byte-identical to
    # flow-signals-ledger's, so this lane provisions NO new environment.
    #
    # Verified locally in a clean venv with this exact install line: 29 passed.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install deps
        run: pip install pytest pandas numpy pyarrow pyyaml boto3 scikit-learn joblib
      - name: prove boto3 is present (a silent re-skip is the regression)
        run: python -c "import boto3, botocore"
      - name: R2 publish client + transport tests (boto3 tests execute here)
        run: >-
          python -m pytest
          tests/test_publish_r2.py
          tests/test_company_intelligence_contracts.py
          tests/test_company_intelligence_views.py
          tests/test_company_intelligence_health.py
          tests/test_publish_company_intelligence_r2.py
          tests/test_company_intelligence_refresh.py
          tests/test_company_intelligence_golden_corpus.py
          tests/test_company_intelligence_golden_corpus_replay.py
          tests/test_company_intelligence_spine.py
          tests/test_company_intelligence_spine_corpus.py
          tests/test_company_theme_exposure.py
          tests/test_publish_company_theme_exposure_r2.py
          tests/test_earnings_story_promotion.py
          tests/test_earnings_story_packets.py
          tests/test_earnings_story_press_workflow.py
          tests/test_earnings_story_press_admission.py
          tests/test_earnings_story_press_ingress.py
          tests/test_earnings_story_press_stage_workflow.py
          tests/test_publish_earnings_story_packets_r2.py
          tests/test_refresh_earnings_story_packets.py -q -rs

  chart-render:
    if: ${{ false }}
    gate: code
    # chart_render SVG suites — the renderer behind every marketing card and the
    # brain gateway's inline chat chart, and (since #3551) the SVG that is
    # rasterized and posted to the live MastermindX X account.
    #
    # Added 2026-07-26. None of these three suites was referenced by ANY workflow,
    # so test_chart_render_inline sat RED on main from #3088 (which moved the MACD
    # stroke off raw material-blue and left the test's colour probe behind) until
    # it was reported by hand — roughly four months of a guard that could not fail
    # anything. test_marketing_card_parity is #3551's own incident suite pinning
    # which renderer feeds the posted PNG; unwired, that incident could silently
    # recur.
    #
    # Deliberately its OWN job rather than a step on marketing-engine: that job is
    # currently red on main for an unrelated reason (test_marketing_content —
    # `account receipts has empty queue`), and folding these in would mask a
    # chart-render regression behind a failure that has nothing to do with charts.
    #
    # Deps: pytest + pyyaml. chart_render's own top-level imports are stdlib
    # (math/zlib/pathlib/typing) — pandas, PIL and headless Chrome are all lazy,
    # and the tests needing them importorskip cleanly — but the repo-wide
    # conftest imports yaml in EVERY pytest run, so pyyaml is not optional
    # (a pytest-only env dies at collection). Matching marketing-engine's
    # dependency set also lets the pack runner reuse its venv. Verified green
    # against a minimal pytest+pyyaml env: 35 passed, 6 skipped.
    #
    # 2026-07-26: "the tests needing them importorskip cleanly" was NOT true when
    # this lane was wired — test_chart_render_inline's two load_ohlcv tests did a
    # bare `import pandas`, so the lane went red on main the same day with
    # ModuleNotFoundError, not a skip. Fixed by making those two importorskip
    # pandas AND pyarrow (.to_parquet needs an engine). Keep the deps minimal and
    # the tests skip-clean rather than growing this list: a lane that installs the
    # world stops being the cheap guard it was added to be.
    #
    # 2026-07-27: the minimal env is kept, but its REASON is restated, because
    # "cheap" stopped being the reason the day #3754 packed the jobs. The pack
    # runner reuses one venv per byte-identical install line, so joining the fat
    # `pytest pandas numpy pyarrow pyyaml` group would have cost zero extra
    # provisioning — cost is no longer an argument for or against either set.
    # What this lane actually buys is the LAZY-IMPORT CONTRACT: chart_render is
    # imported on the brain gateway's request path, and a future top-level
    # `import pandas` in the renderer (or in one of these suites) turns this lane
    # red at collection. That is worth a job on its own and is why it stays thin.
    #
    # What a thin lane CANNOT do is execute the tests that need those modules.
    # Six of the 41 tests here skip in this env — the two load_ohlcv tests in
    # test_chart_render_inline, three in test_marketing_card_parity, one in
    # test_chart_render_m2 (engine.indicators_m2 needs numpy) — and until
    # 2026-07-27 no other job in any workflow named these files, so the skip was
    # total absence of coverage rather than reduced coverage. chart-render-data
    # below runs the SAME three files with those modules present. The lanes are
    # deliberately paired, not alternatives: thin proves the imports stay lazy,
    # fat proves the code behind them is correct. The ~6s of overlap is the price.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install deps
        run: pip install pytest pyyaml
      - name: chart_render SVG suites
        run: python -m pytest tests/test_chart_render_inline.py tests/test_chart_render_m2.py tests/test_chart_render_grammar.py tests/test_marketing_card_parity.py -q

  chart-render-data:
    if: ${{ false }}
    gate: code
    # The data-dependent half of the chart-render suites (see the job above).
    #
    # Added 2026-07-27. #3722 shipped load_ohlcv's crypto source resolution
    # (data/yahoo/<SYM>-USD.parquet + close-to-close H/L synthesis, the fix for
    # "analyse ETH → 没有可用的日线图表数据") with two regression tests. Those tests
    # imported pandas and wrote parquet; chart-render installs pytest+pyyaml, so
    # they errored the lane red, and #3738 made them importorskip pandas/pyarrow.
    # That was the right fix for the red — but chart-render was the ONLY job in
    # ANY workflow naming these files, so the importorskip did not reduce the
    # coverage to a subset, it removed it outright: #3722's data-source
    # resolution shipped with a guard that could not execute anywhere.
    #
    # Same family as #3703 — an `importorskip` added to clear a red leaves main
    # green AND weaker, and nothing in CI notices, because every existing guard
    # asks "is this suite named by a job" (scripts/audit_unrun_tests.py) and
    # never "does the job that names it install what it needs to run".
    #
    # Deps are byte-identical to the 21-job `pytest pandas numpy pyarrow pyyaml`
    # group (trial-budgets, outcome-spine, site-gate, …), which run_ci_pack.py
    # keys its venv reuse on, so this job provisions NO new environment: both
    # packs already build that venv for jobs they already carry.
    #
    # The import probe is the lane's own tripwire. If a future dependency edit
    # drops pandas or pyarrow from this install line, the six tests would go back
    # to skipping silently and the lane would stay green while guarding nothing —
    # exactly the failure this job exists to close. The probe makes that red.
    # -rs prints every skip reason, so a NEW skip is visible in the log rather
    # than hidden behind a bare pass count.
    #
    # Verified locally in a clean venv with this exact install line:
    #   pytest pyyaml                      → 35 passed, 6 skipped
    #   pytest pandas numpy pyarrow pyyaml → 41 passed
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install deps
        run: pip install pytest pandas numpy pyarrow pyyaml
      - name: prove the data-dependent modules are present
        run: python -c "import pandas, pyarrow, numpy"
      - name: chart_render SVG suites (data-dependent tests execute here)
        run: python -m pytest tests/test_chart_render_inline.py tests/test_chart_render_m2.py tests/test_chart_render_grammar.py tests/test_marketing_card_parity.py -q -rs

  portfolio-ctx:
    if: ${{ false }}
    gate: data
    # Portfolio-Aware Intelligence W0 + W1 (charter:
    # research/PORTFOLIO_BRIEF_MASTERPLAN_BY_FABLE.md §6). Two unit suites:
    #   * test_build_portfolio_ctx — the portfolio_ctx.v1 bake (build_ctx joins/omission/
    #     verbatim-vocab/congress-window on synthetic dicts + a main() integration smoke
    #     against the real committed sources; reads congress.parquet → pandas+pyarrow).
    #   * test_portfolio_brief — the W1 deterministic brief composer + GET
    #     /api/portfolio/brief endpoint smoke. The composer tests are dependency-free; the
    #     endpoint smoke uses the FastAPI TestClient (fastapi+httpx+pydantic) and
    #     pytest.importorskip's away cleanly if those are absent.
    # Whitelisted so neither rots red on main after nightly first advances the artifact.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install minimal deps
        # pyyaml is load-bearing here, not incidental: the theme_lanes side-write reads
        # config/theme_crosswalk.yml for the basket-keyed lane projection. Without it
        # build_state_of_themes.load_primary_basket_ids fail-opens to {} and the
        # projection silently collapses to empty (PR #4300 shipped red on exactly that).
        # The endpoint suite imports app.main, whose eagerly mounted Market Memory
        # router imports requests at module scope.
        run: pip install pytest pandas pyarrow jinja2 fastapi httpx pydantic pyyaml requests
      - name: portfolio-ctx bake unit tests
        run: python -m pytest tests/test_build_portfolio_ctx.py tests/test_portfolio_ctx_chains.py -q
      - name: portfolio-brief composer + endpoint tests
        run: python -m pytest tests/test_portfolio_brief.py -q
      - name: retention spine — change history, digest composer, changes endpoint (W6)
        # engine/portfolio_changes (state digest + diff) and engine/portfolio_digest (the
        # change-triggered email COMPOSER — the send path is deliberately unwired, and a
        # test in here asserts the module imports no mailer so no build can send). Same
        # install set as the brief suite above; the file pins that set from the AST.
        run: python -m pytest tests/test_portfolio_changes.py -q
      - name: theme_lanes side-write contract (source of truth for the Portfolio brief)
        # The theme_lanes.v1 side-artifact + the page-byte-identity invariant that keep
        # the State-of-Themes page and the Portfolio brief's lanes in lockstep. Only the
        # jinja-based subset (no node / no heavy repo state) is selected here.
        run: python -m pytest tests/test_state_of_themes.py -q -k "theme_lanes or byte_identical"

  options-estate-guards:
    if: ${{ false }}  # executed inside ci-pack; keep the legacy status definition runner-free
    gate: data
    # OEU M-FIX wiring (2026-07-25): the flow-leaders / market-structure /
    # options-screener engine+builder suites (plus the neural-web support-map
    # suite, surfaced unwired by the same audit) were run by ZERO GitHub
    # workflows — #3496's regression pins (flow_leaders washout-flip truth,
    # market_structure week_map emission, options_screener denominators) could
    # silently rot without them. All seven suites are network-free and
    # tmp-isolated (MM_DATA_GUARD-clean as one batch: 309 tests, no data/ or
    # site/ writes). The support-map smoke reads only committed inputs
    # (config/synapse.yml + data/neuralweb/confluence_graph.json), so its
    # census bands move only via PRs that themselves trigger this job.
    #
    # CURATED SCOPE (2026-08-14 wave 3). `scope: exclusive` makes the paths
    # below REPLACE inference instead of being unioned under it, so the opaque
    # fallback tier this job used to carry — whole-tree `engine/**`,
    # `scripts/**`, `site/**`, `templates/**`, `tools/**` claims smeared out of
    # a subprocess or traversal deep in its closure — stops selecting it. Every
    # pattern here was generated from the job's OWN import closure (100 files
    # across eleven directories) and audited for zero misses: no file this job
    # demonstrably reads lost its owner.
    #
    # NOTE for anyone re-deriving this in a SPARSE worktree: `site` is in
    # `audit_unrun_tests.FIRST_PARTY`, so site/ path literals ARE closure
    # members — but every literal is admitted through `(ROOT / rel).is_file()`,
    # which answers False for an omitted tree. Derived sparse, this closure is
    # 85 files and silently loses all 15 site/ members; derived against a full
    # checkout it is 100. `data/` is genuinely absent from the closure either
    # way (the builder excludes it from LITERAL_DIRS by policy), which is why
    # the `data/**` row below is hand-declared rather than derived.
    #
    # This job was named in the wave-2 note in tests/test_ci_pack.py as a rider
    # that would "stay inferred" alongside dataos-identity-seams. That call is
    # superseded here, and for the opposite reason to the one that spared
    # dataos-identity-seams: an `engine/`-heavy closure only degenerates to
    # fallback breadth when it needs `engine/**`. This one does not. Its 35
    # engine files are all TOP-LEVEL modules plus four whole subpackages, so
    # `engine/*` + four `engine/<pkg>/**` covers the closure exactly while
    # leaving every other engine subpackage — `engine/prophet/**` among them —
    # outside the declaration. `scripts/` is enumerated file-by-file for the
    # same reason: `scripts/*` would re-acquire `scripts/build_free_content.py`,
    # one of the two probes this curation exists to release.
    #
    # Measured on the pre-curation manifest (193 jobs), this job matched all
    # THREE ratchet probes on the fallback tier and none on the owned tier:
    # templates/index.html, scripts/build_free_content.py, and
    # engine/prophet/plan_book.py each drop it, -1 job / -13 weight-seconds per
    # probe. The `site/...` and `data/**` rows are NOT smear carried forward:
    # engine/neuralweb/*, engine/pick_lab/registry.py, engine/signal_quality.py
    # and config/synapse.yml name those artifact trees in literal reads. The
    # site/ rows are DERIVED closure members (see the sparse note above); the
    # two top-level files are enumerated exactly rather than folded into a
    # `site/*` glob, matching how flow-surface declares site/coming-soon.html
    # and site/master_brief.json. `data/**` is hand-declared: the builder
    # excludes `data` from LITERAL_DIRS by policy, so no data path can ever
    # appear in the derived tier, and the support-map smoke's committed input
    # (data/neuralweb/confluence_graph.json) would otherwise lose its owner.
    scope: exclusive
    paths:
      - "config/**"
      - "contracts/options/OPTIONS_INTEL_BRIEF_V1.md"
      - "data/**"
      - "engine/*"
      - "engine/government_revenue/**"
      - "engine/neuralweb/**"
      - "engine/pick_lab/**"
      - "engine/seasonality/**"
      - "lib/*"
      - "scripts/__init__.py"
      - "scripts/build_flow_leaders.py"
      - "scripts/build_market_structure.py"
      - "scripts/build_market_structure_page.py"
      - "scripts/build_options_command.py"
      - "scripts/build_options_intel_brief.py"
      - "scripts/build_options_screener.py"
      - "scripts/validate_gex.py"
      - "site/altdata/**"
      - "site/basketdata/**"
      - "site/chinastatedata/**"
      - "site/factordata/**"
      - "site/flow_desk.json"
      - "site/flowleaders/**"
      - "site/intelligence/**"
      - "site/leaderradar/**"
      - "site/neuralwebdata/**"
      - "site/options.html"
      - "site/options_intel_brief.json"
      - "site/prophet/index.json"
      - "templates/options.html.j2"
      - "tests/test_build_flow_leaders.py"
      - "tests/test_build_market_structure_page.py"
      - "tests/test_build_options_command.py"
      - "tests/test_build_options_screener.py"
      - "tests/test_flow_leaders.py"
      - "tests/test_market_structure.py"
      - "tests/test_market_structure_nw.py"
      - "tests/test_options_gap_discipline.py"
      - "tests/test_options_intel_brief.py"
      - "tests/test_options_intel_brief_ui.py"
      - "tests/test_support_map.py"
      - "tools/index.html"
    timeout-minutes: 15
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install minimal deps
        run: pip install pytest pandas numpy pyarrow pyyaml jinja2 plotly pillow requests beautifulsoup4
      - name: flow-leaders engine + builder suites (washout-flip pins, pick-lab flow books)
        run: python -m pytest tests/test_flow_leaders.py tests/test_build_flow_leaders.py -q
      - name: market-structure engine + nw-context + page suites (week_map emission pins)
        run: python -m pytest tests/test_market_structure.py tests/test_market_structure_nw.py tests/test_build_market_structure_page.py -q
      # P1 unrun backfill 2026-08-14: tests/test_options_gap_discipline.py joins the
      # options-screener step.  It was named by no `run:` step in any workflow, so the
      # GAP DISCIPLINE law — what the screener and flow-leaders builders may publish
      # across a session gap — was dark while both builders it constrains were covered.
      # THIS step rather than the flow-leaders one above is deliberate: the suite reads
      # scripts/build_flow_leaders.py AND scripts/build_options_screener.py, and this
      # job runs flow-leaders (and market-structure) EARLIER in the step list, so
      # landing here puts it downstream of both contract suites it depends on.  Within
      # the step it is appended last for the same reason.  It rides an existing step
      # because a step is the masking unit (run_ci_pack.py returns on the first non-zero
      # step, so a later step goes dark behind an earlier red) while pytest reports
      # every file inside one step; no new job — the manifest is at the narrow-diff
      # ceiling.  No dependency widened: probed at RUN time it loads only pandas/numpy/
      # pyarrow/pyyaml, and blocking openpyxl leaves it 38-passed.
      - name: options-screener builder suite (denominator pins)
        run: python -m pytest tests/test_build_options_screener.py tests/test_options_gap_discipline.py -q
      - name: options workspace suite (OEU M-CMD — 4 modes, close-line honesty, payload law)
        # AD-1 joins this step (not a new job — the manifest is at the narrow-diff
        # ceiling and a step is the masking unit): the intel-brief engine/producer
        # contract suite plus the Workspace consumer/parity suite ride with the
        # workspace they render into. tests/test_options_intel_brief.py includes the
        # data-feasibility law test, which reads the committed data/polygon_gex store
        # present on a full CI checkout.
        run: python -m pytest tests/test_build_options_command.py tests/test_options_intel_brief.py tests/test_options_intel_brief_ui.py -q
      - name: neural-web support-map suite (real-synapse census bands)
        run: python -m pytest tests/test_support_map.py -q

  prelaunch-hardening:
    if: ${{ false }}  # executed inside ci-pack; keep the legacy status definition runner-free
    gate: code
    # Pre-launch hardening (P1, 2026-07-26): the anonymous /api/collect per-IP beacon
    # throttle, the /ws/tape per-IP connection cap, and generic billing/auth 5xx
    # errors (no raw-exception leak) were guarded by ZERO GitHub workflows —
    # app/billing.py and app/tape.py match no other job here, so these guards would
    # rot silently (same class as the tier-gate / options-estate holes). All three
    # suites are network-free (Stripe + Supabase are monkeypatched / IP-filtered) and
    # tmp-isolated; test_billing_webhook is intentionally NOT run here (it needs the
    # pinned Stripe SDK and covers the unchanged signature path, not this hardening).
    timeout-minutes: 10
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install minimal deps
        run: pip install pytest fastapi httpx pyyaml requests
      - name: node must be on PATH — dossier JS contract skips without it (vacuous green)
        run: node --version
      - name: collect beacon throttle + growth-event registry + tape per-IP cap + generic billing/auth errors + dossier quote honesty
        run: python -m pytest tests/test_collect_throttle.py tests/test_growth_events_registry.py tests/test_tape_relay.py tests/test_billing_subscribe.py tests/test_company_intelligence_api.py tests/test_company_intelligence_dossier_js.py tests/test_dossier_quote_api.py tests/test_dossier_live_quote_surface.py -q
      # WS:COMMERCIAL-ACTIVATION CA1A (2026-09-04): the event-spine acceptance
      # battery — registry-as-whitelist derivation (W2-1), envelope-v1 eid/schema/
      # meta validation and replay identity through the mounted /api/collect route,
      # producer near-miss source contracts incl. template/site shipping-pair
      # parity, and the deterministic activation funnel report. Lives in this job
      # because it owns the beacon/growth-registry subject, and needs exactly the
      # same minimal FastAPI/httpx dependency closure as the beacon suite above.
      - name: CA1A event spine — collector envelope v1 + producer contracts + funnel report
        run: python -m pytest tests/test_commercial_activation_event_spine.py tests/test_ca1a_producers_contract.py tests/test_activation_funnel_report.py -q
      # R1A-M (2026-09-02): the shared regular-quote projector both this job's
      # dossier route AND the Intelligence Hub batch route now use, plus the
      # deliberately public batch API itself — same minimal FastAPI/httpx
      # dependency closure as the dossier suite immediately above.
      - name: shared regular quote projector + Intelligence Hub market pulse batch API
        run: python -m pytest tests/test_public_quote_projection.py tests/test_intelligence_hub_market_pulse_api.py -q
      # CCR-R0 (2026-08-28) belongs in this existing pre-launch security lane:
      # authenticated operator identity, a fixed admin iframe, Caddy's real
      # adapted handler order, and the bounded atomic GitHub-source publisher.
      # The Python dependency closure is already present above. Pin and verify
      # the same stock Caddy release deployed on the VPS so the adapted-order
      # assertion cannot silently depend on runner-image contents.
      - name: Control Room operator, embed, edge and source-publisher boundaries
        run: |
          CADDY_DIR="$(mktemp -d)"
          trap 'rm -rf "$CADDY_DIR"' EXIT
          curl --fail --silent --show-error --location \
            --output "$CADDY_DIR/caddy.tar.gz" \
            https://github.com/caddyserver/caddy/releases/download/v2.11.4/caddy_2.11.4_linux_amd64.tar.gz
          printf '%s  %s\n' \
            527fbf917c39189a1e3b31d34fa955601680b2d5c8055d2a87b8b9588dec7bb9 \
            "$CADDY_DIR/caddy.tar.gz" | sha256sum --check --status
          tar -xzf "$CADDY_DIR/caddy.tar.gz" -C "$CADDY_DIR" caddy
          CADDY_BIN="$CADDY_DIR/caddy" python -m pytest \
            tests/test_admin_control_room.py \
            tests/test_control_room_auth.py \
            tests/test_control_room_caddy.py \
            tests/test_publish_control_room_active_builds.py -q

  free-content-estate:
    if: ${{ false }}  # executed inside ci-pack; keep the legacy status definition runner-free
    gate: data
    # Declared (non-exclusive) ownership: the estate is BUILT from content/**
    # markdown, but that read lives inside the builder's traversal code, where
    # the opaque fallback tier no longer lets a smear claim select on `.md`
    # edits. Declared paths ride the owned tier, so a content edit always
    # re-runs this job. Union semantics: this can only ADD selection.
    paths:
      - "content/**"
      - "scripts/build_free_content.py"
      - "tests/test_free_content.py"
      - "tests/test_i18n_split_tag_guard.py"
      - "tests/test_externalize_css.py"
    # Free acquisition estate drift law (2026-07-26). scripts/build_free_content.py
    # and tests/test_free_content.py were wired into ZERO workflows — the D12/W1
    # masterplan flagged `--check` as "currently a manual command" and it was never
    # wired. Both rotted red on main unnoticed: #3592 added the Legal footer column
    # to templates/seo_base.html.j2 and re-rendered only the 3 new legal pages, so 52
    # estate pages silently shipped without the Privacy/Terms/Disclaimer links, and
    # 15 of 26 calculator pages shipped with no CTA at all. Network-free (renders to
    # a tmp dir) and writes nothing under site/.
    timeout-minutes: 10
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install minimal deps
        run: pip install pytest pyyaml jinja2
      - name: free-content builder suite (frontmatter contract, RSS, rendered-output pins)
        run: python -m pytest tests/test_free_content.py -q
      - name: committed estate must match a fresh render (template↔site drift law)
        run: python -m scripts.build_free_content --check
      # i18n split-tag law (2026-07-30). `--check` above pins the committed bytes
      # to a fresh render — it does NOT judge whether those bytes are valid, so it
      # froze 48 t() calls that passed an OPENING `<a href="` through the
      # bilingual macro and shipped `href="</span>` on all 26 calculator pages.
      # check_inline_js only parses <script>/on*=, and check_site_asset_refs
      # deliberately skips refs containing `<`. Scans site/ AND templates/, so it
      # is not scoped to this estate; it lives here because this job already owns
      # the render contract and already installs the deps.
      - name: no split tags across the bilingual t() macro (rendered + source)
        run: python -m pytest tests/test_i18n_split_tag_guard.py -q
      # SECOND home for this suite, and the only one where all of it RUNS.
      # tests/test_externalize_css.py also runs in template-site-sync, whose
      # `pip install pyyaml pytest` deliberately has no jinja2 — so its
      # cross-module test (externalize_css and build_free_content must share ONE
      # HAND_AUTHORED frozenset, never a copy) importorskips there and executed
      # in ZERO jobs: a SKIP-ONLY suite, the #3703 / #3722→#3738→#3760 class that
      # scripts/check_skip_only_suites.py exists to catch. It caught this one.
      # This job already installs jinja2 and already owns the estate contract the
      # shared constant belongs to, so the pairing is free (no new dependency).
      # Do NOT "de-duplicate" this by deleting it because the suite is named
      # above in template-site-sync: that lane runs the other 27 tests in a thin
      # venv and re-darkens the jinja2 one the moment this step goes away.
      - name: inline-CSS externalization units (incl. the jinja2 cross-module gate)
        run: python -m pytest tests/test_externalize_css.py -q

  public-render-fastlane:
    if: ${{ false }}  # executed inside ci-pack; keep the legacy status definition runner-free
    gate: code
    # Public fast-path contract (2026-07-28): scripts/build_public_pages.py plus
    # the render.yml/public-render.yml surface split. Named by ZERO workflows
    # until now, so it rotted red on main invisibly — #3856 moved the founding
    # copy to the availability-framed meter ("2,000 total — first come, first
    # served") and the suite's stale "2,000 memberships" pin had no lane to
    # object from. Guards: GitHub's 21,000-char run-expression limit across all
    # workflows, the heavy/public render path split (neither lane silently
    # absorbing the other's surfaces), the fast path's no-market-data promise,
    # and the rendered pricing/founding copy itself. Renders to a tmp dir;
    # network-free. The dependency line follows free-content-estate's verbatim
    # (`pytest pyyaml jinja2`) so run_ci_pack shares the virtualenv.
    timeout-minutes: 10
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install minimal deps
        run: pip install pytest pyyaml jinja2
      - name: public fastlane split + rendered pricing copy pins
        run: python -m pytest tests/test_public_render_fastlane.py tests/test_help_directory.py tests/test_help_directory_contract.py -q

  press-lane:
    if: ${{ false }}  # executed inside ci-pack; keep the legacy status definition runner-free
    gate: data
    # D14 W1 Media Network press engine (2026-07-26). The press lane emits INTO
    # the free acquisition estate, so its own suite has to run wherever that
    # estate's drift law runs — a press change that breaks the frontmatter
    # contract would otherwise surface as a red free-content-estate job on
    # somebody else's PR. The dependency line follows free-content-estate's
    # minimal-deps convention verbatim (`pytest pyyaml jinja2`) — the same three
    # packages cover the same closure, so there is no reason to spell it
    # differently. (run_ci_pack.py does share a venv between adjacent jobs with
    # byte-identical install lines, but these two land in different packs, so
    # matching buys convention here, not a shared environment.)
    # Network-free: the validator suite is 100% deterministic and the writer
    # (the one module that would make an LLM call) is never invoked by tests.
    timeout-minutes: 10
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install minimal deps
        run: pip install pytest pyyaml jinja2
      - name: press validator suite (paraphrase, fact-anchor, our-value, lexicons)
        run: python -m pytest tests/test_press_validators.py -q
      - name: press planner + writer + orchestrator suites
        run: python -m pytest tests/test_press_planner.py tests/test_press_writer.py tests/test_press_run.py tests/test_earnings_canonical_story.py -q
      - name: press admin panel + workflow shape
        run: python -m pytest tests/test_press_admin.py tests/test_press_workflow.py -q
      # W1.5 property trees + the cutover sync guard. Same lane on purpose: the
      # guard asserts that config/press.yml `cutover` and the commented Caddy
      # block move together, and that pairing is only real if it runs on every
      # press change, not just on a deploy change.
      #
      # HONEST LIMIT ON WHAT REBUILDS properties/. The committed trees are a
      # DERIVED artifact, and the only thing in the repo that regenerates them is
      # the `emit` job in press-publish.yml — which runs ONLY on a
      # workflow_dispatch carrying inputs.emit=true. The daily schedule stages
      # and stops; it never emits. So "the next publish heals it" means the next
      # MANUAL dispatch, which may be never. A template edit, a config edit or a
      # hand-appended ledger row therefore leaves the trees stale until somebody
      # runs `python -m scripts.build_press_properties` and commits the result.
      # This job does NOT run `--check`: the trees do not exist yet, so the check
      # would be red on arrival. Wiring it is a follow-up for the session that
      # generates and commits them.
      - name: press properties + cutover pairing guard
        run: python -m pytest tests/test_press_properties.py -q
      # Derived-tree drift guard: the committed properties/ trees must byte-match
      # a fresh rebuild from templates + config + ledger. A template or config
      # edit that forgets `python -m scripts.build_press_properties` goes red
      # HERE, in the lane that owns it, instead of shipping a stale tree.
      # (press-publish.yml regenerates the trees on every armed emit, so a
      # ledger append direct-pushed to main self-heals on the next publish.)
      - name: property trees match a fresh rebuild (--check)
        run: python -m scripts.build_press_properties --check
      # W2R research triage + tiering + the dark research X lane (XG-W8,
      # 2026-07-28). Same lane on purpose: the triage layer is what decides
      # WHICH vault report the Research Desk covers, so it has to run wherever
      # the desk's own contract runs. Import closure is stdlib + pyyaml (the
      # `pytest pyyaml jinja2` line above already covers it): research_triage
      # keeps every reused module behind LAZY function-level imports, so a heavy
      # top-level import in story_spine/signal_features/garbage_gate turns this
      # job red at collection, which is the point.
      #
      # NOTHING IN THE SUITE IS importorskip-GATED. The near-dup pass's optional
      # `datasketch` dependency is exercised through its DOCUMENTED STATES
      # (`cluster_density` reports `exact-only` and declares the value a floor
      # without the wheel, `measured-alone` with it) rather than by requiring
      # the wheel — so the suite cannot decay into a skip-only one, and the
      # production workflow installs datasketch so the lane itself does not
      # ship the degraded read.
      #
      # BOTH dependency states are FORCED at the availability seam, so the
      # result does not depend on whether THIS env has the wheel (2026-07-29).
      # The earlier assertion took a union of both branches, which passed here —
      # where the pack installs `pytest pyyaml jinja2` and nothing else — and
      # failed on every dev machine with datasketch installed. An env-shaped
      # pass is not a pass; it just moves the red somewhere nobody runs.
      #
      # The veto pass is exercised through its injected `call` seam, so this
      # step makes no network call and needs no credential.
      - name: W2R research triage (W-score, demote-only veto, dark research lane)
        run: python -m pytest tests/test_press_research_triage.py -q

  # ── UNRUN-SUITE CENSUS JOBS (2026-07-26) ───────────────────────────────────
  # Nine lanes covering the 130 strictly-dark suites that no workflow ran.  All
  # nine declare a byte-identical dependency install so run_ci_pack.py builds ONE
  # shared venv for the whole set instead of one per job.  Measured locally in a
  # clean venv with exactly this install: 130 suites / 2420 tests / 70s batched.
  # Two suites from the census are deliberately NOT wired here because they are
  # RED on main and the fix is a product decision, not test rot: the two FTR
  # template-markup suites assert markers ("Strategic horizon", "ftr-dtp-full")
  # that exist nowhere in templates/ or site/ any more.  Deciding whether the
  # feature or the assertion is wrong is a product call — see the PR body.
  # Their filenames are deliberately NOT spelled out here: the census that found
  # this hole tests `name in workflow_text`, so naming a suite in a comment makes
  # it look covered.  Never reference an unwired suite by filename in this file.

  unrun-vector-dsr:
    # BTC vector allocation + the DSR trial budget that haircuts it.
    if: ${{ false }}
    gate: data
    timeout-minutes: 3
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install minimal deps
        run: pip install pytest pandas numpy pyarrow pyyaml jinja2 plotly requests beautifulsoup4 openpyxl scikit-learn fastapi httpx jsonschema
      # P1 unrun backfill 2026-08-14: two more scripts/build_vector.py organs join the
      # step that already owns the vector page's organs.  Both import straight from
      # scripts.build_vector (_g_alerts and the market-card renderer) and both were
      # named by no `run:` step in any workflow, so the landing hub's "What changed"
      # explanation and its per-market header labels were dark on a publish pipeline —
      # the surface that regressed on 2026-08-06, when _g_alerts dropped the plain-word
      # explanation and rendered only the raw engine message.
      # They ride this existing step because a step is the masking unit (run_ci_pack.py
      # returns on the first non-zero step, so a later step goes dark behind an earlier
      # red) while pytest reports EVERY file inside ONE step, and add no job to a
      # manifest at the narrow-diff ceiling.  Appended LAST so a catalyst/forward-risk/
      # Kelly/timeline regression is the first failure a reader sees rather than a hub
      # renderer failing for a borrowed reason.
      # No dependency widened: probed at RUN time both load only pandas/numpy/pyarrow/
      # pyyaml — PIL is imported transitively but blocking it leaves them 6-passed and
      # 1-passed, so it is optional, and it is not on this install line.
      - name: vector page organs — decision authority, catalyst, forward-risk, Kelly, gated timeline
        run: python -m pytest tests/test_btc_decision.py tests/test_vector_catalyst.py tests/test_vector_forward_risk.py tests/test_vector_kelly.py tests/test_vector_timeline_gated.py tests/test_hub_alerts_explanation.py tests/test_hub_market_card_labels.py -q
      - name: DSR / override-registry trial budget (dof charged must equal dof declared)
        run: python -m pytest tests/test_btc_vector_w1.py tests/test_deflated_sharpe.py -q
      - name: settled-close tape — the basket pulse the closing-bell lane publishes
        run: python -m pytest tests/test_settled_close_tape.py -q
      # Crypto Cockpit (unrun backfill 2026-08-03): the cockpit contract on the
      # vector page, the wave-2 market-state/universe organs, the wave-3
      # BTC/ETH desks feeding master brain, and the house-style partial. This
      # lane owns the vector estate the cockpit ships on.
      - name: crypto cockpit — contract, market-state waves, house style
        run: python -m pytest tests/test_crypto_cockpit_contract.py tests/test_crypto_wave2.py tests/test_crypto_wave3.py tests/test_crypto_house_style.py -q

  unrun-oracle-desk:
    # oracle nightly + its gauntlet promotion gates.
    if: ${{ false }}
    gate: code
    timeout-minutes: 6
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install minimal deps
        run: pip install pytest pandas numpy pyarrow pyyaml jinja2 plotly requests beautifulsoup4 openpyxl scikit-learn
      - name: oracle nightly writer + turn desk
        run: python -m pytest tests/test_oracle_nightly.py tests/test_oracle_turn_desk.py tests/test_oracle_tape.py -q
      - name: oracle gauntlet phases + onset quality + regime gate + intraday asymmetry
        run: python -m pytest tests/test_oracle_gauntlet.py tests/test_oracle_gauntlet_p3b.py tests/test_oracle_gauntlet_p8.py tests/test_oracle_onset_quality_w1.py tests/test_oracle_single_regime_gate.py tests/test_oracle_asymmetry_intraday.py -q

  unrun-marketing-desk:
    # X-growth desk organs beyond the engine/publisher lanes that already run:
    # the Ad Central owned-inventory plane, the persona internal model +
    # familiarity ladder (XG-W4b §E), the reply-desk arming/actuation contract
    # (XG-W7), and the two committed X ad-campaign asset manifests. None was
    # named by any run: step before the 2026-08-03 backfill.
    if: ${{ false }}
    gate: code
    timeout-minutes: 4
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install minimal deps
        run: pip install pytest pandas numpy pyarrow pyyaml jinja2 plotly requests beautifulsoup4 openpyxl scikit-learn
      - name: ad plane O + persona model + reply-desk arming
        run: python -m pytest tests/test_marketing_ad_plane_o.py tests/test_marketing_persona_model.py tests/test_marketing_reply_arming.py -q
      - name: X ad-campaign asset manifests (desktop + mobile)
        run: python -m pytest tests/test_x_ad_campaign_assets.py tests/test_x_mobile_campaign_assets.py -q

  unrun-grading-board:
    # US board grading — the shipped verdict ledgers and their tenure stamps.
    if: ${{ false }}
    gate: code
    timeout-minutes: 6
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install minimal deps
        run: pip install pytest pandas numpy pyarrow pyyaml jinja2 plotly requests beautifulsoup4 openpyxl scikit-learn
      # P1 unrun backfill 2026-08-14: two more scripts/grade_us_board.py guards join the
      # step that already owns the grader's outcome ledgers.  Both were named by no
      # `run:` step in any workflow, so the ENTRY-STATUS stamp and the PRICE-BASIS era
      # boundary — the two things the grader must not silently re-write — were dark
      # while the ledgers they write into were covered.  They ride this existing step
      # rather than one of their own because a step is the masking unit here
      # (run_ci_pack.py returns on the first non-zero step, so a later step goes dark
      # behind an earlier red) while pytest reports EVERY file inside ONE step; and no
      # new job, because the manifest is at the narrow-diff ceiling.
      # Placed LAST so an outcome-ledger or tenure-stamp regression is the first
      # failure a reader sees — both new suites read the grader on top of those
      # ledgers and would otherwise fail for a borrowed reason.
      # No dependency widened: probed at RUN time, both load only
      # pandas/numpy/pyarrow/pyyaml, all already on the install line above.
      - name: grade_us_board outcome ledgers + tenure stamp + the strip it renders
        run: python -m pytest tests/test_board_tenure_stamp.py tests/test_us_board_outcomes.py tests/test_us_board_outcomes_strip.py tests/test_entry_status_stamping.py tests/test_us_board_price_basis_era.py -q
      - name: grade_us_board zero-accrual + truncated-history alarms
        # The nightly recorded nothing for nine days (2026-07-31..08-06) while the step
        # concluded `success`; these pin that the alarms fire, name the cause, and stay
        # quiet on a healthy night. An unrun guard is not a guard — this suite is named
        # here because ci.yml has no broad `pytest tests/`.
        run: python -m pytest tests/test_us_board_zero_accrual_alarm.py -q
      - name: claim accountability + track-record dialog + onboarding compare matrix
        run: python -m pytest tests/test_claim_accountability.py tests/test_track_record_dlg.py tests/test_onboard_compare_matrix.py -q
      # Board continuity — both suites shipped 2026-08-05 (#4554, #4557) named by
      # NO workflow, so the VALE class they were written for was never guarded in
      # CI. Separate step from the outcome ledgers above so a red names the law
      # that broke: row-persistence/outage continuity (grade_us_board::emit_ledger)
      # vs the same-as_of Jaccard guard (build_stock_library::_board_continuity_warning).
      - name: board ledger row-persistence + same-as_of continuity guard
        run: python -m pytest tests/test_us_board_ledger_continuity.py tests/test_us_board_continuity_guard.py -q

  unrun-intl-libraries:
    # China / HK / Canada library writers behind the international surfaces.
    if: ${{ false }}
    gate: data
    timeout-minutes: 5
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      # lxml is required (not optional) because the THS concept boards are HTML:
      # collectors.china_ths_concepts parses them with pandas.read_html, which
      # raises ImportError("`Import lxml` failed") without it. Verified against a
      # venv holding exactly this list — beautifulsoup4 alone does not satisfy
      # read_html's default flavor. It is a first-class dep (requirements.txt).
      # jsonschema is required (not optional): the THS scrape receipt is a CONTRACT
      # artifact (contracts/baskets_china_ths/scrape_receipt.v1.schema.json) and its
      # test validates every receipt shape against the committed schema rather than
      # against a hand-copied field list — a test that asserted its own idea of the
      # shape would go green while the schema and the writer drifted apart.
      - name: install minimal deps
        run: pip install pytest pandas numpy scipy pyarrow pyyaml jinja2 plotly requests beautifulsoup4 lxml openpyxl scikit-learn jsonschema
      - name: china + hk + canada library builders and their manifests
        run: python -m pytest tests/test_w0_5_10_8.py tests/test_hk_closes_fallback.py tests/test_canada_branch_b.py tests/test_stock_library_manifests.py tests/test_stocktable_hk_data.py -q
      - name: china intel/analog/facet organs + fx context render
        run: python -m pytest tests/test_baskets_china_intel.py tests/test_china_analogs.py tests/test_china_stf_facets.py tests/test_china_fx_context_render.py tests/test_backfill_china_communiques.py -q
      # 同花顺 (THS) themes: the concept collector, the PIT membership seeding, and
      # the page-weight split that moves member rows + level history out of
      # baskets_china_ths.html into the fetched hydrate payload. The split's
      # row-conservation and no-mutation guards are the reason this suite has to be
      # reachable — a builder edit that silently thinned members would otherwise
      # ship green (#3488: a guard the guarded change cannot trigger is not a guard).
      - name: china THS concept baskets — collector, PIT seeding, page-weight split
        run: python -m pytest tests/test_baskets_china_ths.py -q
      # The queryable PIT membership store the THS lane above feeds
      # (engine/basket_membership_pit.py, CN Prophet §5 W-C). Named by NO workflow before
      # this: `grep -rn test_basket_membership_pit .github/` returned nothing and there is
      # no catch-all `pytest tests/` in this repo, so the keep-first / asof / basis-flag
      # pins were dark in BOTH CI lenses while engine/** PRs went green past them. Its own
      # step, so a red names which contract broke — the store's semantics here...
      - name: CN basket PIT membership store — keep-first, asof, basis flag
        run: python -m pytest tests/test_basket_membership_pit.py -q
      # ...versus the LANE GATE on the path production takes. Both halves of that gate
      # shipped dead (fixed 2026-08-09): the resolver defaulted to
      # os.environ.get("CN_LANE", "asia") so an unset variable WAS the asia lane, and the
      # gate behind it refused only a named non-asia lane. The store is keep-FIRST per
      # snapshot_date and content-deduped, so the first lane to stamp a date owns that
      # snapshot forever — an unrun guard here is scheduling order picking a published PIT
      # answer. Every test in this file drives the real entry point with only CN_LANE set.
      - name: CN membership-PIT lane gate — fail-closed, on the production call path
        run: python -m pytest tests/test_cn_membership_pit_lane_gate.py -q
      # GMI W1a — the PRODUCER the two steps above never had. The scraper and the
      # seeder appeared in NO workflow, so membership.json froze on 2026-06-30 and the
      # PIT store held two snapshots for six weeks while the nightly --snapshot step ran
      # green ~35 nights, dedup-skipping every one. These three suites guard the fix and,
      # more importantly, the three ways it can go quiet again: complete-or-fail
      # promotion (a partial dump would read to the seeder as vanished boards), the US
      # suite's own lane gate (per-suite now, so a foreign nightly must be refused in
      # BOTH directions), and the tripwire that makes a stalled plane impossible to
      # mistake for a healthy deduping one. Steps are split so a red names which of the
      # three broke. TZ=UTC because the writers stamp UTC dates the tests read back.
      - name: THS weekly re-scrape — receipts, complete-or-fail promotion, PIT ingest
        run: TZ=UTC python -m pytest tests/test_ths_weekly_rescrape.py -q
      - name: US basket PIT membership store + per-suite lane gate
        run: TZ=UTC python -m pytest tests/test_us_basket_membership_pit.py -q
      - name: membership snapshot freshness tripwire — cadence, source, coherence
        run: TZ=UTC python -m pytest tests/test_membership_snapshot_freshness.py -q
      # GMI W1b — the theme-graph semantic spine built ON TOP of those membership
      # documents. Split into four steps so a red names which contract broke: node
      # identity (permanent company ids, ratified epochs), the materialization
      # (bitemporal intervals, era/date_provenance honesty, the THS join), the guard
      # itself, and the CN limit-rule registry's parity against the canonical engine.
      # TZ=UTC because these writers stamp UTC dates the tests read back.
      - name: theme graph — permanent node identity and ratified epochs
        run: TZ=UTC python -m pytest tests/test_theme_graph_identity.py -q
      - name: theme graph — bitemporal materialization from the membership documents
        run: TZ=UTC python -m pytest tests/test_theme_graph_materialize.py -q
      - name: theme graph — contract guard (incident fixtures + annotation shape)
        run: TZ=UTC python -m pytest tests/test_theme_graph_contracts.py -q
      # V4-D2B3 — node lifecycle correction lineage: the append-only retirement
      # side-car, the resurrection-proof bake suppression (R-A1's two-day survival
      # tests), the §6 guard invariants, and the one-shot curated correction script
      # (target discovery, ABX generality, idempotency, the Data OS non-interference
      # fence). Split from the other theme-graph steps so a red names this contract.
      - name: theme graph — V4-D2B3 node lifecycle correction lineage (hostile matrix)
        run: TZ=UTC python -m pytest tests/test_theme_graph_lifecycle.py -q
      - name: theme graph — crosswalk v3 additive integrity
        run: TZ=UTC python -m pytest tests/test_theme_graph_crosswalk.py -q
      - name: theme graph — V4-D2A identity-resolution bridge (hostile fixtures, two-clock law, reproducibility)
        run: TZ=UTC python -m pytest tests/test_theme_graph_identity_resolution.py -q
      - name: theme graph — W3A local-theme plane (hostile tests D-M, capability sidecar, rights gate)
        run: TZ=UTC python -m pytest tests/test_theme_graph_local_plane.py -q
      - name: theme graph — source-rights registry shape + F20 grandfather pin
        run: TZ=UTC python -m pytest tests/test_theme_sources_registry.py -q
      - name: CN limit-rule registry — schema, continuity, parity vs china_microstructure
        run: TZ=UTC python -m pytest tests/test_cn_limit_rules.py -q
      # GMI W2 — the exposure-decomposition probe's pure math. The probe script itself is a
      # one-shot research tool that is deliberately NOT wired into any lane, so its estimators
      # would otherwise have no guard at all: a wrong causal shift, a share denominator that
      # divides by an empty month, a coverage floor off by a boundary, or an era label keyed on
      # the month END would all ship silently into the committed receipts under
      # research/theme_graph/w2_probe/ and into whatever W4 charters from them. Every fixture
      # here is synthetic — the suite reads no store, so it stays green on a runner that cannot
      # see the gitignored narrative_flare history the probe reads locally. TZ=UTC to match the
      # rest of this job; nothing in the suite is clock-dependent by construction.
      - name: theme exposure probe — beta/causal shift, share denominator, coverage floor, era
        run: TZ=UTC python -m pytest tests/test_theme_exposure_probe.py -q
      # The guard on the COMMITTED store, not only on its fixtures: --selftest proves it
      # can still see each incident, --strict proves the store shipped in this repo
      # satisfies the contract. A guard that only ever runs against its own fixtures is
      # a guard the guarded artifact cannot trigger.
      - name: theme graph contract guard against the committed store
        run: |
          python -m scripts.check_theme_graph_contracts --selftest
          python -m scripts.check_theme_graph_contracts --strict

  unrun-data-plane:
    # R2 transport, accrual audits and the ledger-advance guards.
    if: ${{ false }}
    gate: data
    timeout-minutes: 5
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install minimal deps
        # build_polygon_intraday imports the governed options episode validator;
        # keep its Draft 2020-12 dependency present in this isolated job env.
        run: pip install pytest pandas numpy pyarrow pyyaml jinja2 plotly requests beautifulsoup4 openpyxl scikit-learn jsonschema
      - name: R2 fetch/audit + accrual audits + surface freshness
        run: python -m pytest tests/test_fetch_r2.py tests/test_audit_r2.py tests/test_audit_options_accrual.py tests/test_audit_thetadata_accrual.py tests/test_check_surface_freshness.py -q
      # P1 unrun backfill 2026-08-14: tests/test_mag7_events.py joins the step that
      # already owns scripts/notify_turn_events.py (test_notify_turn_events.py) — its
      # own pipeline subject — and lib/store.py.  It was named by no `run:` step in any
      # workflow, so engine/mag7_regime.py and the turn events the notifier emits for it
      # were dark while the notifier itself was covered.  It rides this existing step
      # because a step is the masking unit (run_ci_pack.py returns on the first non-zero
      # step, so a later step goes dark behind an earlier red) while pytest reports
      # EVERY file inside ONE step; no new job — the manifest is at the narrow-diff
      # ceiling.  Appended LAST so a ledger-advance, failstreak or notifier regression
      # is the first failure a reader sees rather than the mag7 regime consumer failing
      # for a borrowed reason.  No dependency widened: probed at RUN time it loads
      # pandas/numpy/pyarrow/pyyaml/requests, all already on the install line above.
      - name: ledger-advance + builder failstreaks + turn-event notifier
        run: python -m pytest tests/test_check_ledger_advance.py tests/test_builder_failstreaks.py tests/test_notify_turn_events.py tests/test_mag7_events.py -q
      # test_finviz_tree_refresh.py (GMI Theme Graph W3A, 2026-08-14) rides the
      # step that already owns scripts/fetch_finviz_themes.py — same module, same
      # subject, and pytest reports EVERY file inside ONE step, so nothing here
      # masks anything else. Stdlib + pytest only (it monkeypatches the fetchers
      # and never touches the network), so the install line above is unchanged.
      - name: polygon universe/intraday + dead-name prices + finviz archival/tree-refresh + thetadata sessions
        run: python -m pytest tests/test_build_polygon_universe.py tests/test_polygon_intraday.py tests/test_fetch_dead_name_prices_polygon.py tests/test_finviz_archival.py tests/test_finviz_tree_refresh.py tests/test_thetadata_tape_sessions.py -q
      # Enumerated in NO workflow before this (calendar-asof audit follow-up
      # 2026-08-05): the finviz collector's asof feeds these ledgers, but a PR
      # touching the track record or its heal fired no run that could see a
      # failure. Lives here because the collector's own suite already does.
      - name: subsector rotation track record + significance guard + session-stamp heal
        run: python -m pytest tests/test_subsector_track_record.py tests/test_subsector_significance_guard.py tests/test_heal_subsector_rotation_ledgers.py -q
      # Enumerated in NO workflow before this (2026-08-06). The whole 2026-08-03
      # store-freshness program — #4632's data/stocks retention fetch and per-name
      # tip audit, #4643's yahoo/breadth never-silent disclosure and the scan-side
      # demotion — shipped its guards DARK: 4 suites, 77 tests, named by no `run:`
      # step in any of the 51 workflows nor in this file. The triggerable-but-unrun
      # half is the worse one: `collectors/**` and `scripts/*.py` DO start ci.yml, so
      # a PR refactoring _fetch_universe or _report_missing_symbols went green while
      # executing none of the tests written to stop exactly that. Three land here
      # because the dead-name registry they read through _dead_tickers() is already
      # this job's subject (test_fetch_dead_name_prices_polygon.py, one step up) and
      # they pass on the shared install BYTE-FOR-BYTE — verified, not assumed. The
      # fourth (test_collector_freshness_disclosure) imports collectors/breadth.py,
      # whose yfinance import is module-scope, so it would ERROR at collection here;
      # it lives in unrun-russell-breadth, which already declares that wheel and owns
      # the breadth collector as its subject.
      - name: data/stocks retention fetch + per-name freshness audit
        run: python -m pytest tests/test_stock_price_retention.py tests/test_audit_stocks_freshness.py -q
      - name: stock-library freshness demotion (frozen-feed scan admission)
        run: python -m pytest tests/test_stock_library_freshness.py -q

  defense-rail-laws:
    # D6-A + D6-B1 defense acquisition rails — the two commissioned law
    # batteries whose specs demand MERGE-BINDING execution ("do not rely on
    # an unrun test holding pen"). Both landed inside unrun-government-revenue
    # (gate: data), which the W2 merge-gate split excludes from every PR pack
    # — so neither battery had ever executed on a merge gate: found by the
    # 2026-08-26 pack-execution audit of PR #6454's run 32941452226 (all four
    # changed files FMS, 76 jobs selected across 12 packs, zero FMS tests
    # run, tests/test_fms_notifications.py reported unowned). Both suites are
    # gate-code pure per the D4 CI-wiring law govrev-program-ontology cites:
    # frozen committed fixtures, committed staged goldens under
    # data/government_revenue/fms_staged_objects/ (sha-frozen browser
    # receipts, never nightly-rewritten), and template-shell renders with an
    # empty payload — no nightly data commit can move either verdict.
    # pdfplumber: collectors/dod_budget_live.py's deterministic extractor
    # (frozen choice, D6-A design doc §5). httpx: fastapi.testclient's
    # transport for the FMS route tests.
    if: ${{ false }}
    gate: code
    # CURATED SCOPE (2026-08-26, same wave as the job itself). Without this,
    # inference resolves tests/test_fms_ui.py's app.government_revenue import
    # (the route-boundary tests) plus the collectors' opaque edges into
    # whole-tree fallback claims — app/**, data/**, config/**, templates/**,
    # site/** — and this job fallback-matched ALL FOUR probes in
    # tests/test_ci_pack.py::test_exclusive_curation_narrows_ordinary_code_prs
    # (templates/index.html measured 130 > the 129 ceiling). That is smear,
    # curated away at the source per the standing convention (same treatment
    # as stock-dossiers, cn-standout-audit, govrev-company-bridge). Every
    # path below is EARNED: the 563-file import closure inference derives
    # (via app/government_revenue.py the closure spans the flat engine/*.py
    # layer and the listed engine subpackages — deliberately NOT engine/**,
    # which would smear onto engine/prophet/**, where the closure holds
    # nothing), plus the frozen fixture trees and the sha-frozen staged
    # goldens the batteries read at runtime. templates/ and scripts/ are
    # named files on purpose: the suites read exactly two templates, and
    # the five scripts are closure imports, not tree scans.
    scope: exclusive
    paths:
      - "tests/test_dod_budget_live.py"
      - "tests/test_fms_notifications.py"
      - "tests/test_fms_ui.py"
      - "tests/fixtures/fms/**"
      - "tests/fixtures/dod_budget/**"
      - "data/government_revenue/fms_staged_objects/**"
      - "app/**"
      - "collectors/**"
      - "lib/**"
      - "config/**"
      - "contracts/**"
      - "engine/*.py"
      - "engine/biocatalyst/**"
      - "engine/capital_structure/**"
      - "engine/chronicle/**"
      - "engine/codex_lane/**"
      - "engine/company_intelligence/**"
      - "engine/context_index/**"
      - "engine/earnings_narrative/**"
      - "engine/earnings_release/**"
      - "engine/entry_radar/**"
      - "engine/fundamental_forensics/**"
      - "engine/government_revenue/**"
      - "engine/intelligence_workspace/**"
      - "engine/marketing/**"
      - "engine/metabolism/**"
      - "engine/neuralweb/**"
      - "engine/oracle/**"
      - "engine/pick_lab/**"
      - "engine/press/**"
      - "engine/prophet_lab/**"
      - "engine/prophet_live/**"
      - "engine/research_vault/**"
      - "engine/seasonality/**"
      - "engine/sector_intelligence/**"
      - "engine/theme_graph/**"
      # D5's app/prophet_lab route extends this curated app closure through the
      # canonical issuer reader. Keep the measured stock-identity package files
      # explicit; path-risk and the B1 episode reader are not in this job's
      # derived closure and are intentionally not declared here.
      - "engine/stock_identity/__init__.py"
      - "engine/stock_identity/authority.py"
      - "engine/stock_identity/fingerprint.py"
      - "engine/stock_identity/plane.py"
      - "research/DO_NOT_REBUILD.md"
      - "research/SIGNAL_EPISODE_ATLAS_MASTERPLAN_BY_FABLE.md"
      - "research/STOCK_PERSONALITY_MASTERPLAN_BY_FABLE.md"
      - "research/US_BOARD_MEASUREMENT.md"
      - "scripts/build_stock_library.py"
      # Import-closure entry: build_stock_library reuses
      # scripts/snapshot_shadow_book.rows_from_board for the shadow-book
      # snapshot (one percentile definition), so exclusive scope must name it.
      - "scripts/snapshot_shadow_book.py"
      - "scripts/check_board_contradictions.py"
      - "scripts/check_signal_gate_coherence.py"
      - "scripts/check_stretch_oracle_contract.py"
      - "scripts/thematic_rotation_phase0.py"
      - "site/basketdata/**"
      - "site/canadabasketdata/**"
      - "site/chinabasketdata/**"
      - "site/coming-soon.html"
      - "site/data/portfolio_ctx.json"
      - "site/factordata/**"
      - "site/flowleaders/**"
      - "site/government_revenue.html"
      - "site/hkbasketdata/**"
      - "site/leaderradar/**"
      - "site/master_brief.json"
      - "site/prophet/**"
      - "site/vol/**"
      - "templates/government-revenue-dossiers.js"
      - "templates/government_revenue.html.j2"
    # Install-dominated like its former host; the two batteries themselves
    # are ~30s.
    timeout-minutes: 8
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install deps for the defense rail batteries
        run: pip install pytest pandas numpy pyarrow pyyaml jinja2 requests beautifulsoup4 jsonschema fastapi httpx pdfplumber
      # D6-A DoD budget live rail (PR #6377): the hostile law battery for the
      # production P-1/R-1 acquisition + parser — fetch/R2-readback order, no
      # receipt without readback proof, §5b/§5b.1 row-model refusals (sign law,
      # printed-addend grain, boundary buckets, page-pair joins, 0400D
      # consolidated-only), idempotence, and the activation fence. Merge-binding
      # per the commission ("do not rely on an unrun test holding pen").
      - name: DoD budget live acquisition + parser law suite (D6-A)
        run: python -m pytest tests/test_dod_budget_live.py -q
      # D6-B1 FMS congressional-notification rail (packet 1): the merge-
      # binding adversarial battery — freeze T1-T14 (stage/amount/identity/
      # correction/ownership honesty, no procurement_event.v2 rows, no
      # cross-case aggregation, no empty-valid coercion) plus the D6-B1
      # official-union battery B1-B7/B9-B11 (FR-only recovery cases, State/FR
      # join, fallback<->recovery collision, staged-replay + R2-readback
      # integrity, coverage-gate refusal on an unreconciled denominator).
      # B8's mint/attach law is covered inside the same file. B12-B14 (EN/ZH
      # template copy verbatim + mutation-fails-closed, page-weight fence +
      # FMS shell delta, anonymous boundary + no case bodies in the baked
      # HTML) land in tests/test_fms_ui.py -- the packet-2 consumer/
      # publication half (ninth mode tab, entitled routes, JS inspector).
      - name: FMS congressional-notification rail law suite (D6-B1)
        run: python -m pytest tests/test_fms_notifications.py tests/test_fms_ui.py -q

  unrun-government-revenue:
    # Government Revenue Foresight — builder envelope, publication fence,
    # workspace/dossier/briefcase UI contracts, and the federal-award
    # collectors (SAM.gov, USAspending, Census trade) feeding the program.
    # jsonschema and fastapi ride on top of the shared unrun-* install because
    # engine/government_revenue/workspace.py's contract validator fails CLOSED
    # without jsonschema (every payload reads invalid, which presented as 20
    # assertion failures in the 2026-08-03 staging pass) and the dossier /
    # subaward rails import fastapi at module scope. Like unrun-brain-gateway
    # this buys one extra venv in whichever pack it lands.
    if: ${{ false }}
    gate: data
    # 6 -> 8 for the seventeen-suite darkness backfill below (+~20s of tests
    # on top of an install-dominated job); the margin is for a cold wheel
    # cache, not for the suites themselves. (The D6-A + D6-B1 defense rail
    # batteries moved to defense-rail-laws, gate: code — their commissions
    # demand merge-binding execution and this job never packs on a PR.)
    timeout-minutes: 8
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install minimal deps plus the schema/API wheels
        # httpx (not the unrelated PyPI package "httpx2", a 2026-08 typo):
        # fastapi.testclient imports the httpx module, and the API/dossier
        # suites below use TestClient. pdfplumber rides for
        # scripts/build_government_revenue.py's collector closure.
        run: pip install pytest pandas numpy pyarrow pyyaml jinja2 plotly requests beautifulsoup4 openpyxl scikit-learn jsonschema fastapi httpx pdfplumber
      # Wave 9D: the admissibility fences on issuer graph expansion — exact
      # identifier + verbatim Exhibit 21 name only, no discovery_query_ticker, no
      # similarity, no LLM assertion; PIT; the proposed-edge-cannot-become-a-
      # candidate boundary driven through engine/government_revenue/candidates.py
      # itself; and the four EDGAR collector regressions found by running the
      # collector against live filings. The suite opens no socket (module-wide
      # socket fence) and reads only tests/fixtures/issuer_evidence/, so it needs
      # nothing beyond this job's existing install line.
      - name: wave 9D — exact-identifier issuer graph expansion fences
        run: python -m pytest tests/test_government_revenue_issuer_graph_expansion.py -q
      - name: builder envelope + publication fence + dossier/subaward rails
        run: python -m pytest tests/test_build_government_revenue.py tests/test_check_government_revenue_projection.py tests/test_government_revenue_dossiers.py tests/test_government_revenue_subaward_api.py -q
      # The prophet↔government-revenue context bridge. Named by NOTHING in
      # .github/ before this line — not this manifest, not any workflow, and not
      # even a ci.yml trigger path — so the one suite proving the Prophet side
      # reads the published govrev artifacts correctly had never executed in CI.
      # It belongs in THIS job rather than alongside test_prophet_bridge.py in
      # unrun-market-plumbing because it reads the v2 procurement contract, and
      # only this job installs jsonschema; without it workspace.py's validator
      # fails CLOSED and every payload reads invalid (the 2026-08-03 staging pass).
      - name: prophet ↔ government-revenue context bridge
        run: python -m pytest tests/test_prophet_government_revenue_context.py -q
      # Waves 9E + 9F: the shadow cross-check packet and the Prophet
      # post-selection annotation — the two seams that connect the lobe to the
      # rest of the platform without giving it signal authority. Both suites
      # carry byte-identity proofs (the candidate queue's wire bytes across four
      # shadow states; Prophet's decision projection with the adapter on/off) and
      # a TRANSITIVE import-boundary walk proving no Prophet module can reach a
      # govrev candidate SOURCE. They belong in THIS job because both read the
      # jsonschema-validated candidate contract, and the live probes read the
      # committed data/{baskets/ohlcv,yahoo,regime,us_prophet_rank} stores plus
      # data/government_revenue/candidate_queue.json — all tracked, so the probes
      # exercise real artifacts here rather than degrading to fixtures. Measured
      # locally under this job's exact dependency set: 103 passed in ~7s.
      - name: waves 9E/9F — shadow context packets + prophet annotation boundary
        run: python -m pytest tests/test_government_revenue_shadow_context.py tests/test_government_revenue_prophet_annotation.py -q
      - name: workspace / dossier / briefcase UI contracts
        run: python -m pytest tests/test_government_revenue_ui.py tests/test_government_revenue_dossier_ui.py tests/test_government_revenue_briefcase_ui.py -q
      # D3 temporal contract: typed rail states (producer) + the Change Tape
      # clock/diff/successor renders (node harness, same runner requirement as
      # the UI suite above, whose _run_runtime it imports).
      - name: D3 temporal contract — typed rails, clock block, hostile families T1-T8
        run: python -m pytest tests/test_government_revenue_temporal_contract.py -q
      # D4 Company Financial Truth Bridge moved OUT of this job (adversarial
      # review, 2026-08-20): this job is `gate: data` (advisory-only, never
      # merge-binding), and D4's frozen-fixture suite needs to actually BLOCK
      # a merge. It now runs merge-binding in the `govrev-company-bridge`
      # `gate: code` job below (tests/test_government_revenue_company_bridge.py).
      # D2 Identity Atlas: projector + graph-delta pins + the node-harness test
      # that renders the shipped dossier JS factory against the COMMITTED
      # identity-atlas artifact (needs node, present on these runners for the
      # dossier-UI suite above).
      - name: D2 identity atlas — projector, hostile tests, committed-artifact UI render
        run: python -m pytest tests/test_government_revenue_identity_atlas.py -q
      - name: federal-award collectors — SAM.gov, USAspending awards + subawards, Census trade
        run: python -m pytest tests/test_sam_gov_opportunities.py tests/test_sam_gov_radar_leg.py tests/test_usaspending_awards.py tests/test_usaspending_subawards.py tests/test_census_trade.py -q
      # The 2026-08-03 backfill wired the builder/fence/UI/collector suites but
      # left nineteen Government Revenue suites enumerated by NOTHING in
      # .github/ — no pack, no workflow, and no catch-all `pytest tests/` sweep
      # exists in this repo to pick them up.  Among them were the two suites
      # that own the forward award-event spine itself
      # (test_government_revenue_award_spine.py, test_government_revenue_award_events.py),
      # so the state machine guarding the event triad was never once executed by
      # CI.  That is how the 2026-08-06 `ledger_write_failed` persistence break
      # reached production: the lobe looked green because its on-point guards
      # were invisible, not because they passed.  Candidate API/projection tests
      # now run in the dedicated bounded job below: their large deterministic
      # fixtures grew beyond this legacy lane's original eight-minute budget,
      # and keeping them here made the entire ownership lane time out.
      - name: award-event spine, candidates, entity + workspace contracts
        run: python -m pytest tests/test_government_revenue.py tests/test_government_revenue_award_events.py tests/test_government_revenue_award_spine.py tests/test_government_revenue_api.py tests/test_government_revenue_api_auth.py tests/test_government_revenue_budget_graph.py tests/test_government_revenue_candidates.py tests/test_government_revenue_contracts.py tests/test_government_revenue_entity_resolution.py tests/test_government_revenue_freshness.py tests/test_government_revenue_idv_bridge.py tests/test_government_revenue_idv_dossiers.py tests/test_government_revenue_neuralweb.py tests/test_government_revenue_opportunities.py tests/test_government_revenue_recipient_graph.py tests/test_government_revenue_subaward_dossiers.py tests/test_government_revenue_wave8_api.py tests/test_government_revenue_workspace.py tests/test_usaspending_idv_graph.py -q
      # Recipient->issuer attribution: the Wave 9D discovery tool that proposes
      # candidates for the reviewed graph. Named by NO run: step until 2026-08-06 —
      # the proposal tool's 60 tests could not go red on main, which is the same
      # shape as a guard that does not exist. jsonschema (installed above) is the
      # suite's only extra import. Its two siblings — the graph runtime
      # (test_government_revenue_recipient_graph.py) and the resolver
      # (test_government_revenue_entity_resolution.py) — are already enumerated by
      # the darkness-backfill step directly above, in this same job with this same
      # dependency set, so they are not repeated here.
      - name: recipient entity graph — the Wave 9D proposal tool
        run: python -m pytest tests/test_propose_government_revenue_recipient_graph.py -q
      # Amount-class conflation gate.  The lobe's central financial-honesty rule —
      # "obligation, outlay, ceiling, bookings, backlog, funded backlog, and GAAP
      # revenue are never conflated" — was written twice as PROSE (the account handoff
      # and the masterplan) and enforced by nothing: `grep -rl 'obligat.*ceiling' tests/`
      # returned no match.  The incumbent publishes the same caution in its own public
      # docs; the difference has to be that ours can go red.  The suite pins all four
      # dangerous mixes (cumulative+delta, obligation+ceiling, obligation+outlay, and a
      # figure published without its class label), and running it found one real defect
      # on main: metrics._concentration's weight ladder fell through to
      # current_award_amount, so a frame arriving without an obligated column produced
      # shares, an HHI and a total computed from a CEILING and published as
      # `covered_obligations`.  Stdlib + the job's existing pandas/pyarrow only.
      - name: amount-class conflation gate — obligation vs ceiling vs outlay vs delta
        run: python -m pytest tests/test_government_revenue_amount_semantics.py -q
      # Unrun backfill 2026-08-09: the SBIR/STTR progression evidence lane
      # (Wave 10 rail 3, #5012) landed named by no run: step — the unrun-suite
      # gate reds main on it.  It belongs HERE because the suite drives
      # collectors/sbir_awards.py and engine/government_revenue/sbir_progression.py,
      # the same federal-award collector family as the SAM.gov/USAspending steps
      # above, under the same jsonschema-bearing install line.
      - name: SBIR/STTR progression evidence lane — collector + engine rail (#5012)
        proof_id: sbir-sttr-progression-evidence-pre-amount-semantics
        run: python -m pytest tests/test_sbir_awards.py -q
      - name: amount-class semantics guard (selftest + live source scan)
        run: |
          python3 scripts/check_government_revenue_amount_semantics.py --selftest
          python3 scripts/check_government_revenue_amount_semantics.py
      # Unrun backfill 2026-08-09: the SBIR/STTR progression evidence lane
      # (Wave 10 rail 3, #5012) landed named by no run: step — the unrun-suite
      # gate reds main on it.  It belongs HERE because the suite drives
      # collectors/sbir_awards.py and engine/government_revenue/sbir_progression.py,
      # the same federal-award collector family as the SAM.gov/USAspending steps
      # above, under the same jsonschema-bearing install line.
      - name: SBIR/STTR progression evidence lane — collector + engine rail (#5012)
        proof_id: sbir-sttr-progression-evidence-post-amount-semantics
        run: python -m pytest tests/test_sbir_awards.py -q

  govrev-company-bridge:
    if: ${{ false }}
    gate: code
    # D4 Company Financial Truth Bridge (IRDM only): the suite MUST be
    # merge-binding (adversarial review, 2026-08-20), which unrun-government-revenue
    # above -- `gate: data`, advisory-only -- structurally cannot be. Moved
    # here instead of reusing that job's heavier install line because the
    # suite reads NOTHING beyond a committed test fixture
    # (tests/fixtures/govrev_company_bridge/p00032_event.json, frozen from
    # the real P00032 workspace event -- never site/government-revenue-data/
    # or data/government_revenue/) plus templates/government-revenue-dossiers.js
    # and templates/government_revenue.html.j2 as plain text: pytest and node
    # (preinstalled on ubuntu-latest) are the only runtime dependencies.
    #
    # `scope: exclusive` declared here (2026-08-20, main-red-repair): shipped
    # with no scope at all, the suite's own `node` subprocess invocation
    # (tests/test_government_revenue_company_bridge.py:197) is an opaque
    # subprocess call scope inference cannot see through — it resolves to
    # `subprocess roots=templates,tests`, which widens to templates/** and
    # matched every ordinary templates/index.html PR. Curated at the source:
    # its true subject is exactly the four files below, per this job's own
    # header comment above.
    scope: exclusive
    paths:
      - "templates/government-revenue-dossiers.js"
      - "templates/government_revenue.html.j2"
      - "tests/fixtures/govrev_company_bridge/p00032_event.json"
      - "tests/test_government_revenue_company_bridge.py"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: { python-version: "3.12" }
      - name: install test deps
        run: pip install pytest
      - name: D4 company financial truth bridge — IRDM-only, hostile families T1-T10 + R2/R4/R5/R7/R9/R10/R12
        run: python -m pytest tests/test_government_revenue_company_bridge.py -q

  govrev-program-ontology:
    # D5 Program/Mission/Capability/Product graph (research/defense_intelligence/
    # DEFENSE_D5_PROGRAM_GRAPH_ARCHITECTURE_FREEZE.md): the reviewed acquisition-
    # domain ontology contract, its propose/curate admission scripts, and the
    # composed Program Dossier read model. Must be merge-binding (freeze SS8's
    # T1-T17 adversarial battery is the acceptance gate for the whole vertical),
    # so this rides `gate: code` with frozen committed fixtures per the D4
    # CI-wiring law — never `gate: data`, and never reading nightly-rewritten
    # site/government-revenue-data/ or data/government_revenue/ artifacts.
    #
    # Deliberately NOT `scope: exclusive`: this suite imports
    # engine/government_revenue/program_ontology.py's sibling modules
    # (award_events/entity_resolution/workspace/budget_program/metrics,
    # their contracts, and the collectors those touch) -- a closure real but
    # narrower than it once was. It no longer needs scripts/build_government_
    # revenue.py's own much larger transitive closure at all: the five D5
    # composer end-to-end tests that used to pull it in were relocated to
    # tests/test_build_government_revenue.py (2026-08-23 CI repair, PR #6312
    # ci-pack-7 red -- that import needs `requests`, which this job's install
    # line does not carry, while test_build_government_revenue.py's lane
    # already provisions it). Left inferred rather than curated: the
    # remaining closure still touches collectors/base.py's/dod_budget.py's
    # own filesystem-ambiguity widen-to-roots edges, which would need
    # constant upkeep to hand-curate as those modules evolve; automatic
    # scope inference (the same machinery `scope: exclusive` opts OUT of)
    # derives and keeps it current instead.
    if: ${{ false }}
    gate: code
    timeout-minutes: 8
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: { python-version: "3.12" }
      - name: install test deps
        run: pip install pytest jsonschema pandas numpy pyarrow jinja2
      - name: D5 program ontology — T1-T17 adversarial battery + dossier composer
        run: python -m pytest tests/test_government_program_ontology.py -q
      - name: D5 program ontology — propose/curate admission scripts
        run: python -m pytest tests/test_government_program_ontology_scripts.py -q

  unrun-government-revenue-candidate-projection:
    # The projection matrix is the slower half of the candidate contract. It
    # reached 42/44 tests when it shared one sequential 15-minute lane with the
    # API suite. Keep projection isolated; the shorter API suite runs with the
    # existing candidate-grader owner below so both retain bounded runner margin.
    if: ${{ false }}
    gate: code
    timeout-minutes: 15
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install candidate contract dependencies
        run: pip install pytest pytest-xdist pandas numpy pyarrow pyyaml jinja2 plotly requests beautifulsoup4 openpyxl scikit-learn jsonschema fastapi
      - name: candidate projection contract fixtures
        run: python -m pytest -n 2 --dist load --max-worker-restart=0 tests/test_government_revenue_candidate_projection.py tests/test_government_revenue_candidate_fixture.py -q

  unrun-factor-research:
    # factor registry + measurement disclosure + the study lanes behind them.
    if: ${{ false }}
    gate: code
    timeout-minutes: 8
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install minimal deps
        run: pip install pytest pandas numpy pyarrow pyyaml jinja2 plotly requests beautifulsoup4 openpyxl scikit-learn
      - name: factor hypothesis registry + intelligence state + measurement evidence gap
        run: python -m pytest tests/test_register_factor_hypotheses.py tests/test_factor_intelligence_state.py tests/test_build_measurement_evidence_gap.py -q
      - name: look-ahead tripwire over engine/ + collectors/ (feature-leak guard)
        run: python -m pytest tests/test_no_lookahead.py -q
      - name: study + trial lanes (OOS, divergence, drift, fingerprints, gauntlets)
        run: python -m pytest tests/test_a2_oos_analysis.py tests/test_construction_divergence_study.py tests/test_expect_drift_panel.py tests/test_f_hz1_study.py tests/test_falsosc_trial_v1.py tests/test_grade_breadth_divergence.py tests/test_intraday_large_cap_tech_leader_study.py tests/test_long_hold_gap_guard.py tests/test_macro_fire_context.py tests/test_options_history_gauntlet.py tests/test_per_fire_sector_benchmark.py tests/test_run_w1_sev.py tests/test_run_w2_slq.py tests/test_thesis_funnel_history.py tests/test_w3_fingerprint_stats.py -q
      # Pick-conditional forward distributions, waves 1 + 1.5 (wired 2026-08-06).
      # Both suites are fixture-only (CI carries no massive_stock_day parquets).
      # Wave-1's suite was named by NO run: step anywhere when it merged, so its PIT
      # truncate-and-recompute equality, its embargo boundary and its noise-partition
      # negative control ran dark. The w1.5 suite pins the phase-0 IMPORT SURFACE and
      # makes the runner refuse to produce a number when it drifts; a pin that no
      # workflow executes cannot do that job. Pinned, precisely: the five phase-0
      # function signatures (parameter names/kinds/defaults, NOT type annotations);
      # phase-0's frozen constants (cohort WINDOW 504, MIN_CELL_N, MIN_OWN_N, tau grid,
      # OOS start, MIN_XSEC, era split, bootstrap B/seed, coverage band, the scheme and
      # baseline name tuples); and the engine constants that define the arms themselves
      # (VOL_FLOOR, universe floors, TAUS/MAE_TAUS, the FEATURE windows VOL_WIN /
      # VOL_MINP / REF_WIN / REF_MINP, all five SCHEMES axis tuples, vf.HAR_LAGS).
      # The feature windows are load-bearing and were added after review: VOL_WIN
      # 20 -> 30 moves the trailing-20d standardizer — the wave-1 comparison arm and
      # the D-2 denominator — while every signature still matches. NOT pinned: function
      # BODIES. A rewritten walk_forward with an unchanged signature still passes.
      - name: pick-conditional forward distributions — PIT/embargo (w1) + HAR standardizer pin (w1.5)
        run: python -m pytest tests/test_pick_forward_dist.py tests/test_pick_forward_dist_phase1.py -q

  unrun-serving-admin:
    # admin control surfaces + the brain/tape serving edges.
    if: ${{ false }}
    gate: data
    timeout-minutes: 3
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install minimal deps
        run: pip install pytest pandas numpy pyarrow pyyaml jinja2 plotly requests beautifulsoup4 openpyxl scikit-learn jsonschema
      - name: admin health, feature flags and the config store
        run: python -m pytest tests/test_admin_health.py tests/test_admin_flags.py tests/test_admin_config_store.py -q
      # Wired 2026-07-26: run by ZERO workflows before this step — the same orphan class
      # neural-web-core was created for.  Covers every admin/*.py import (parametrized off
      # the directory glob) plus the read-only status()/panel() surfaces of ~10 modules.
      # requests is load-bearing on this job's install line: the suite imports admin.server,
      # which pulls admin.analytics_first_party's `try: import requests / except: requests =
      # None`.  Without the dep the whole file still passed (74/74) while exercising the
      # degraded branch, so test_status_with_pat_is_configured now pins the shipped one.
      - name: every admin/*.py imports + the read-only status()/panel() surfaces
        run: python -m pytest tests/test_admin_modules_smoke.py -q
      # Wired 2026-08-06: enumerated by NO job before this step, and there is no broad
      # `pytest tests/` anywhere in this repo, so the file was dark in both CI lenses.
      # It went red on main when #4612 added the "key_alerts" rail to _summary_payload()
      # and the payload-key assertion here never learned it — a real assertion failure
      # that nothing ran for weeks.  The smoke suite above only pins that admin/*.py
      # IMPORTS; this one pins admin.server's HTTP behavior (host forgery, body cap,
      # dispatch-ref fence, unmanaged-toggle refusal) and the landing payload contract.
      # requests is load-bearing on this job's install line for the same reason called
      # out above — the suite imports admin.server.
      # test_admin_program_watch.py joins this step (not a new job) because it pins the
      # same two surfaces: admin.server's GET route (/api/program_watch, and its 401 when
      # auth is on) and the landing payload key app.js reads (s.program_watch). It also
      # runs the reader against the COMMITTED data/seasonality/program_watch.json — for
      # its DIALECT only (schema accepted, every state known, fired block first, every row
      # carrying a prompt to copy), never for which tripwire is currently fired: that
      # artifact is rebuilt and committed nightly, so a content assertion would turn this
      # step red the day the operator does the work the panel exists to prompt.
      - name: admin server HTTP surface + private project runtime truth
        run: python -m pytest tests/test_admin_server.py tests/test_admin_program_watch.py tests/test_project_runtime_state.py -q
      - name: brain SSE keepalive + tape decode
        run: python -m pytest tests/test_brain_sse_keepalive.py tests/test_tape_decode.py -q

  geo-enrich-credential-boundary:
    # scripts/geo_enrich.py is the writer behind admin/analytics_first_party.py, which
    # LEFT JOINs public.ip_geo on every view. It had NO coverage at all when a 30-day
    # Supabase Management API PAT expiry killed the geo-enrich cron for 13 days / ~370
    # runs, and — worse — a credential rejection on the WRITE plane was swallowed by a
    # per-IP `except Exception`, so the run reported ok=True having written nothing.
    #
    # gate: code, deliberately. The suite is stdlib-only and reads no data artifact, so
    # its verdict is a pure function of the PR tree — the gate-split's own criterion
    # (research/CI_MERGE_GATE_RELIABILITY_ROOT_CAUSE_2026_08_19.md). It was first wired
    # into `unrun-serving-admin`, which is gate: data because some of its admin suites
    # assert against committed artifacts; that would have parked this guard behind the
    # nightly data-health lane and left the merge gate blind to a regression in the very
    # file this job exists to protect.
    if: ${{ false }}
    gate: code
    timeout-minutes: 3
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install minimal deps
        run: pip install pytest
      - name: credential-admission boundary + the geography contract
        run: python -m pytest tests/test_geo_enrich.py -q

  unrun-ops-workers:
    # Publish-ops, worker plumbing and live-plane integrity: the dead-asset
    # reference guard and ticker-link integrity over the shipped estate, the
    # conflicted-autostash containment (P0 2026-08-01) and render-checkout
    # bound over the self-hosted git planes, the admin Research Tools SPA
    # directory, the earnings worker's terminal intake + launchd assets, the
    # China board watch-lane artifact contract, and the live session-floor
    # guard. None was named by any run: step before the 2026-08-03 backfill.
    if: ${{ false }}
    gate: data
    timeout-minutes: 4
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install minimal deps
        run: pip install pytest pandas numpy pyarrow pyyaml jinja2 plotly requests beautifulsoup4 openpyxl scikit-learn
      - name: dead-asset refs + rendered ticker links over the shipped estate
        run: python -m pytest tests/test_check_site_asset_refs.py tests/test_rendered_ticker_links.py -q
      - name: git-plane containment — conflicted-autostash guard + render checkout bound
        run: python -m pytest tests/test_push_conflict_guard.py tests/test_render_checkout_bound.py -q
      - name: admin research tools + earnings worker (terminal intake + launchd assets)
        run: python -m pytest tests/test_admin_research_tools.py tests/test_earnings_worker_terminal.py tests/test_earnings_worker_launchd.py -q
      - name: china board watch-lane artifact + live session-floor guard
        run: python -m pytest tests/test_china_board_watch_lane.py tests/test_risk_state_live_session_floor.py -q

  unrun-builders-render:
    # page/render builders whose output ships to the site.
    if: ${{ false }}
    gate: data
    timeout-minutes: 8
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install minimal deps
        run: pip install pytest pandas numpy pyarrow pyyaml jinja2 plotly requests beautifulsoup4 openpyxl scikit-learn
      - name: page/render builders whose output ships to the site
        run: python -m pytest tests/test_ai_desk_page.py tests/test_build_chart_data.py tests/test_build_odds_observability.py tests/test_build_sector_map.py tests/test_committee_template.py tests/test_confluence_screener_page.py tests/test_contagion_card_render.py tests/test_darkpool_desk.py tests/test_options_estate_redirect_stubs.py tests/test_macro_signals_render.py tests/test_nav_icon_refresh.py tests/test_nav_submenu_icons.py tests/test_sector_nav_links.py tests/test_site_semantics_refs.py tests/test_sp500_heatmap_caps.py tests/test_stage_analysis_page.py tests/test_stocktable_data.py tests/test_template_items_footgun.py tests/test_trading_journal_xlsx.py tests/test_us_stocks_w8g_table.py -q

  unrun-nav-chrome:
    # The two-family navigation law and the chrome around it: every product
    # page header must come from the ONE shared source (CLAUDE.md §Navigation
    # source-of-truth), nav prefixes must stay correct below site/, the wide
    # menus must anchor to the header, the landing IA and the start page must
    # keep their contracts, and the Terminal portal/entitlement copy must stay
    # cross-surface consistent. All were named by no run: step before the
    # 2026-08-03 backfill — the header-family law was enforced by nothing.
    if: ${{ false }}
    gate: data
    timeout-minutes: 3
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install minimal deps
        run: pip install pytest pandas numpy pyarrow pyyaml jinja2 plotly requests beautifulsoup4 openpyxl scikit-learn
      - name: one product header + nav prefix + hover/anchor geometry
        run: python -m pytest tests/test_product_chrome.py tests/test_subdir_nav_prefix.py tests/test_nav_hover_bridge.py tests/test_nav_menu_mockup_hover.py tests/test_nav_wide_menu_anchor.py -q
      - name: landing IA + nav front-door regroup + start-page voice and publish integrity
        run: python -m pytest tests/test_landing_navigation.py tests/test_oip_w1_nav_regroup.py tests/test_hub_welcome_persona.py tests/test_start_publish_integrity.py -q
      - name: terminal entitlement copy + chat-allowance truth + full-screen portal contract
        run: python -m pytest tests/test_terminal_indicator_pricing.py tests/test_plans_chat_quota_truth.py tests/test_terminal_overlay_contract.py tests/test_terminal_overlay_closed_state.py -q

  unrun-builders-stores:
    # store/ops builders behind the nightly data lanes.
    if: ${{ false }}
    gate: data
    timeout-minutes: 8
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install minimal deps
        run: pip install pytest pandas numpy pyarrow pyyaml jinja2 plotly requests beautifulsoup4 openpyxl scikit-learn
      # The Massive Advanced tick-plane entitlement probe (TP-0, #5005). Named by NO
      # run: step when it merged, so the one thing standing between the program and a
      # private R2 bucket full of bytes we are not licensed to hold — the probe that
      # says what the vendor actually grants — had no CI at all. Offline: the suite
      # drives the probe's parsers over canned vendor payloads and never opens a
      # socket. `requests` is already on the pip line above, which is its only
      # non-stdlib import at module scope (the websocket clients and lib.config are
      # imported lazily inside the live paths this suite does not take).
      #
      # ORDERED BEFORE the ~40-file sweep below on purpose: run_ci_pack.py abandons a
      # job's remaining steps once one exits non-zero, so a step reachable only after
      # every other suite in the job passes is not wired, it is merely present.
      - name: massive advanced tick-plane entitlement probe (TP-0)
        run: python -m pytest tests/test_massive_entitlement_probe.py -q
      # Unrun backfill 2026-08-14: the whole 13F SMART-MONEY DESK — engine contracts,
      # the nightly builder, and the census surface it publishes — was named by no
      # `run:` step anywhere. Three suites, 85 tests, dark since #5103 built the
      # autonomous institutional 13F census.
      #
      # WHY THIS LANE OWNS IT. `scripts/build_smart_money.py` IS a store/ops builder
      # behind a nightly data lane: render.yml, closing-bell.yml and earlyclose.yml
      # each invoke it as `run_py "13F smart-money tracker (build_smart_money)"`, and
      # the engine modules the other two suites pin (smart_money, ownership_flow,
      # ownership_crowding, fund_intelligence, fund_followability, insider_intel,
      # filing_transition, ownership_event_wire) are that builder's inputs. Its
      # regulatory-disclosure sibling test_finra_short_interest_pit.py already runs in
      # the sweep below. NO lane owned this subject before today — the desk is not a
      # census suite looking for the nearest unrun-* lane, it is a producer whose lane
      # is this one. Deliberately NOT split engine-side/surface-side across two jobs:
      # a builder whose engine contract and whose published page disagree is exactly
      # the break that survives a split, and the suites cost 5.7s together.
      #
      # ORDERED BEFORE the ~40-file sweep for the reason stated one step up:
      # run_ci_pack.py abandons a job's remaining steps at the first non-zero exit, so
      # a step behind that sweep would be present rather than wired.
      #
      # Adds NO wheel and NO job: jinja2 (census surface renders templates/), pandas
      # and pytest are already on the install line above; nothing else is imported at
      # module scope. Hermetic — every site/ path the surface suite writes is under
      # pytest's tmp_path, and it reads only committed templates/, so its verdict
      # cannot move with the market. Measured on a full checkout: 24 + 51 + 10 = 85
      # tests in 5.7s, inside this job's 8-minute budget. The census-surface count
      # becomes 15 when #5607 (frozen-census freeze annotation) lands; that PR touches
      # only the builder and its suite, so it inherits this wiring with no edit here.
      - name: 13F smart-money desk — engine contracts, nightly builder, census surface
        run: >
          python -m pytest
          tests/test_smart_money.py
          tests/test_smart_money_v3.py
          tests/test_smart_money_census_surface.py -q
      # GATE-4 commercial-path alerting. Sibling of freshness_sentinel (same
      # systemd unit, same Telegram/Discord/email transport). Ordered BEFORE
      # the ~40-file sweep so a red here is not hidden behind that step's
      # first-nonzero abandon. Stdlib + pytest + requests (already on the
      # pip line); the suite's live-delivery proof is a local HTTP receiver.
      - name: commercial-path alerting (GATE-4)
        run: python -m pytest tests/test_commercial_path_alerts.py -q
      # MMX-001 / GATE-1 customer-table backup. Sibling of the freshness-sentinel
      # suite in the sweep below: same deploy/ops shape (oneshot + timer +
      # update.sh self-arm). Own step so a sweep failure cannot hide it, and so
      # audit_unrun_tests.py sees a run: body that names the suite. openssl is
      # on ubuntu-latest; the suite is stdlib + MemoryStore (no boto3).
      - name: customer-table backup dump/restore contracts (MMX-001)
        run: python -m pytest tests/test_backup_user_tables.py -q
      - name: store/ops builders behind the nightly data lanes
        run: python -m pytest tests/test_append_sync_gauge.py tests/test_audit_fred_groups.py tests/test_audit_massive_store.py tests/test_build_adjudication_packet.py tests/test_build_demand.py tests/test_build_ext_quotes.py tests/test_build_live_overlay.py tests/test_build_m2_profiles.py tests/test_build_unusual_baseline.py tests/test_calibrate_flow_signing_append.py tests/test_calibrate_stage.py tests/test_cctv_finalize_watcher.py tests/test_collect_lane_gate.py tests/test_context_candidates.py tests/test_context_index_eval.py tests/test_deploy_update_self_heal.py tests/test_close_pass_slo_report.py tests/test_ei_f1d_shadow.py tests/test_freshness_sentinel.py tests/test_cn_live_watchdog.py tests/test_exit_tax_scenarios.py tests/test_expanding_mean_benchmark.py tests/test_fetch_basket_ohlcv.py tests/test_finra_short_interest_pit.py tests/test_flow_score_stock_block.py tests/test_gex_history_snapshot.py tests/test_grade_policy_calendar.py tests/test_import_equitydesk.py tests/test_import_equitydesk_backfill.py tests/test_index_add_overnight.py tests/test_manifest_seams.py tests/test_mri_config_spine.py tests/test_narrative_ttl.py tests/test_net_replay_v1.py tests/test_options_tape_signed.py tests/test_quotes_server.py tests/test_reconcile_membership.py tests/test_refresh_markets_now.py tests/test_release_playbook.py tests/test_sf_harness_runner.py tests/test_slf031.py tests/test_slf051_cn_margin.py tests/test_tech_lab_cli.py tests/test_vps_live_orchestration.py tests/test_live_breadth.py tests/test_live_breadth_js_contract.py -q
      # Tech-Lab parity fixtures: the ~24 STRUCTURAL checks (fields, warmup
      # nulls, RSI range, band ordering, value-area >=70%) are platform-
      # independent and run here.  TestFixtureDeterminism is DESELECTED: it
      # regenerates the fixtures and demands byte-identity, which holds on
      # macOS (where the committed fixtures were generated) but NOT on
      # ubuntu-latest — expected_ribbon.json and expected_bollinger.json
      # differ in float tails (rolling std / EMA accumulation) under a
      # different numpy/BLAS build.  docs/TECH_LAB_TESTING.md asserts
      # byte-identity as a contract, so the contract itself is
      # platform-dependent; regenerating on ubuntu would just break macOS.
      # Picking a resolution (tolerance vs pinned numpy vs per-platform
      # fixtures) is an owner decision, not a wiring decision — see
      # docs/UNRUN_TEST_CENSUS.md.  Found by running this suite in CI for
      # the first time (it was named by no workflow before #3636).
      - name: tech-lab parity fixtures — structure only (determinism is platform-dependent)
        run: python -m pytest tests/test_tech_parity_fixtures.py -q
          --deselect tests/test_tech_parity_fixtures.py::TestFixtureDeterminism
      # Unrun backfill 2026-08-09: the W-L1a evening close-pass lane (#5154) —
      # mirror/publish/reconcile builders behind the provisional evening board —
      # landed named by no run: step; the unrun-suite gate reds main on it.  It
      # joins this lane because its builders are store/ops lane builders and its
      # sibling instrument (the freshness sentinel #5154 extended) already runs
      # in the sweep above.  Static/AST + engine.close_pass/us_board_rank only;
      # pandas/pyyaml ride the existing install line.
      # 2026-08-15 (#5746): Massive close truth is the same lane — same publisher,
      # same BOARD_KEY — so its suite rides this step rather than a new job.
      # The host-native PRIMARY clock joins the same lane's suite (Breathing
      # PR-B, 2026-08-15): scripts/close_pass_host_runner.py is what actually
      # fires the evening pass now, the workflow being a bounded backstop. Its
      # suite is stdlib-only and subprocess-free (every command is faked at the
      # _sh seam), so it costs the lane nothing and rides the same install line.
      - name: evening close-pass provisional board lane (W-L1a, #5154; host clock PR-B)
        run: python -m pytest tests/test_close_pass_lane.py tests/test_close_pass_massive_close.py tests/test_close_pass_host_runner.py -q

  # ── P1 UNRUN-SUITE CENSUS JOBS (2026-07-26) ────────────────────────────────
  # Thirteen lanes covering the 138 wired P1 suites: unrun by any workflow, but on a
  # publish pipeline and triggerable, so a silent break moved shipped numbers with
  # nothing going red.  Twelve repeat the P0 lanes' dependency install BYTE-FOR-BYTE
  # so run_ci_pack.py keeps ONE shared venv across all twenty-one unrun-* jobs; only
  # unrun-russell-breadth declares a different set (yfinance), and since the pack
  # sorts jobs by install string that costs exactly one extra venv per pack.
  # The 138 is the count of distinct tests/*.py named across these thirteen run: steps —
  # re-derive it rather than incrementing, since suites also arrive wired via unrelated
  # PRs.  It read 140 here and 137 in docs/UNRUN_TEST_CENSUS.md until 2026-07-26; the
  # doc was right and this comment had drifted.  A census suite belongs in whichever lane
  # OWNS its subject, not in whichever unrun-* lane is nearest: the render.yml intl-scope
  # guard briefly sat here before moving to workflow-yaml, whose subject is
  # .github/workflows/** (#3729 / #3731 / #3741).
  # Measured serially in a clean venv carrying exactly these installs: 139 suites /
  # 429s, plus unrun-import-hygiene's 48s and the release-forecast producer's 13s.
  #
  # Two P1 suites are deliberately NOT wired.  Both are RED on main and the fix in
  # each case is a program decision, not test rot: a chip whose copy assertions test
  # a revision that was deliberately rewritten, and a gateway suite that needs
  # fastapi (absent from the shared install) AND writes a real data/ ledger.  See the
  # PR body and docs/UNRUN_TEST_CENSUS.md.  (Two others have left this set: an
  # import-time warnings ratchet that had drifted 37 files past its frozen allowlist,
  # migrated and wired as unrun-import-hygiene below; and an all-null claims
  # benchmark_set on the shipped release-forecast cards, adjudicated and wired into
  # unrun-release-forecast below.)
  # Their filenames are deliberately NOT spelled out here: the census that found this
  # hole tests `name in workflow_text`, so naming a suite in a comment makes it look
  # covered.  Never reference an unwired suite by filename in this file.

  unrun-site-surfaces:
    # scripts/build_site.py panels and view-models — the dashboard organs the nightly render ships.
    if: ${{ false }}
    gate: data
    timeout-minutes: 5
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install minimal deps
        run: pip install pytest pandas numpy pyarrow pyyaml jinja2 plotly requests beautifulsoup4 openpyxl scikit-learn
      # P1 unrun backfill 2026-08-14: tests/test_inflation_units.py joins the step that
      # already owns scripts/build_site.py's panels.  It was named by no `run:` step in
      # any workflow, so the UNITS contract on the inflation panel — percent vs
      # percentage-point, the class of error that silently mis-states a published macro
      # number — was dark on the nightly render path, while every sibling panel view
      # model in this step was covered.  It rides this existing step because a step is
      # the masking unit (run_ci_pack.py returns on the first non-zero step) while
      # pytest reports every file inside one step, and adds no job to a manifest at the
      # narrow-diff ceiling.  Appended LAST so a panel or view-model regression is the
      # first failure a reader sees rather than the units guard failing for a borrowed
      # reason.  No dependency widened: probed at RUN time it loads pandas/numpy/pyarrow/
      # pyyaml/requests — all on the install line above — plus PIL transitively, which
      # blocking leaves 10-passed, so it is optional and stays off the line.
      - name: scripts/build_site.py panels and view-models — the dashboard organs the nightly render ships
        run: python -m pytest tests/test_action_board_coherence.py tests/test_advanced_breadth.py tests/test_basket_integration.py tests/test_breadth_panel.py tests/test_c4_dollar.py tests/test_factor_ic_panel.py tests/test_macro_glance.py tests/test_macro_signals_vm.py tests/test_market_gamma.py tests/test_market_gamma_contract.py tests/test_regime_snap.py tests/test_spark_zone_band.py tests/test_ts_u3_dashboard_surfaces.py tests/test_vol_shock_scorecard.py tests/test_wiki_pageviews.py tests/test_inflation_units.py -q
      # Unrun backfill 2026-08-03: the Prophet Live P1 user-facing dashboard
      # surfaces join the lane that owns the dashboard organs. (Distinct from
      # the #4331 prophet-board suites, whose staging follows that PR's merge.)
      - name: prophet live user-facing surfaces
        run: python -m pytest tests/test_prophet_live_surface.py tests/test_cn_live_surface.py -q
      # Unrun backfill 2026-08-09: the W-L1 board-state surface (#5148 — "which
      # board am I looking at?") landed named by no run: step; the unrun-suite
      # gate reds main on it.  It joins this lane because it is a shipped
      # dashboard surface, sibling to the prophet live surfaces #5148 itself
      # extended in the step directly above.
      - name: W-L1 board-state surface (#5148)
        run: python -m pytest tests/test_wl1_board_state_surface.py -q
      # US board freshness truth (PR-2, US Prophet permanence program, 2026-08-26).
      # Rides this lane rather than a new job: it renders the same dashboard.html.j2
      # organs every step above renders, needs no dependency this lane does not already
      # install (jinja2 + pandas/plotly for the shared _env harness), and sits beside
      # the W-L1 board-state surface it is a sibling of — board STATE above, board
      # VINTAGE here.  Appended last so a board-state regression is still the first
      # failure a reader sees.
      - name: US board freshness truth — vintage stamp, card date, delayed banner
        run: python -m pytest tests/test_us_board_freshness_truth.py -q

  unrun-release-forecast:
    # the macro-release forecast producer — projections, benchmarks and the graded forward ledger.
    if: ${{ false }}
    gate: data
    timeout-minutes: 6
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install minimal deps
        run: pip install pytest pandas numpy pyarrow pyyaml jinja2 plotly requests beautifulsoup4 openpyxl xlrd scikit-learn
      - name: the macro-release forecast producer — projections, benchmarks and the graded forward ledger
        run: python -m pytest tests/test_content_stamp_freshness_sweep2.py tests/test_cpi_postmortem_fixes.py tests/test_release_combined.py tests/test_release_forecast_producer.py tests/test_release_forecast_v2.py tests/test_release_integration_2a.py tests/test_release_integration_2b.py tests/test_release_integration_w11g.py tests/test_release_market_context.py -q
      # Unrun backfill 2026-08-03: the CPI bridge (Track CB), the official-event
      # progressive-enhancement client, and the date-scoped Release Radar
      # selector join the producer lane that owns the release-forecast estate.
      - name: CPI bridge + official-event live client + Release Radar date selection
        run: python -m pytest tests/test_release_cpi_bridge.py tests/test_release_publications_live_client.py tests/test_release_radar_date_selection.py -q
      # Unrun backfill 2026-08-09: Release Radar canonical inflation truth
      # (#5153) landed five suites named by no run: step — the unrun-suite gate
      # reds main on them.  They join the producer lane that owns the
      # release-forecast estate: the inflation-intelligence engine + its
      # neural-web registration, the release-actuals reconciler, the defect
      # ledger, and the target-truth vintage collector.  xlrd is explicit so
      # the frozen 2012/2014 legacy-XLS BLS fixtures run rather than skip.
      - name: Release Radar — inflation truth, actuals, defects, target truth (#5153)
        run: python -m pytest tests/test_inflation_intelligence.py tests/test_inflation_intelligence_nw.py tests/test_release_actuals.py tests/test_release_cpi_official_truth.py tests/test_release_cpi_target_history.py tests/test_release_defects.py tests/test_release_target_truth.py -q
      # Wave 2B: the coherent-target ridge is a display-only forward candidate.
      # Keep its frozen registry/prereg, truth-receipt closure, authority wall,
      # and combined_v1 exclusion under the release-forecast owner lane.
      - name: Release Radar — coherent-target ridge governance (Wave 2B)
        run: python -m pytest tests/test_release_cpi_coherent_governance.py tests/test_release_cpi_coherent_shadow.py -q

  unrun-brain-desks:
    # master brain / AI desk / alt-data / risk brains behind the shipped desk verdicts.
    if: ${{ false }}
    gate: code
    timeout-minutes: 5
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install minimal deps
        run: pip install pytest pandas numpy pyarrow pyyaml jinja2 plotly requests beautifulsoup4 openpyxl scikit-learn
      - name: master brain / AI desk / alt-data / risk brains behind the shipped desk verdicts
        run: python -m pytest tests/test_admin_brief_gate.py tests/test_ai_desk_panel.py tests/test_altdata_intel.py tests/test_altdata_price_truth.py tests/test_china_intel_hub.py tests/test_master_brain.py tests/test_master_brain_ladder.py tests/test_master_brain_zh_ladder.py tests/test_master_brain_policy.py tests/test_master_brain_producer.py tests/test_master_brain_scorer.py tests/test_risk_brain.py tests/test_risk_radar_review.py tests/test_shock_deescalation.py tests/test_w7_llm_determinism.py -q
      # China Alpha Intelligence P1 (#6050): the china_intel_hub dossier's
      # descriptive-only visits block — joins the china_intel_hub owner lane.
      # test_china_intel_visits_render.py is the SOL-repair template render
      # test (renders china_intel.html.j2 through the real builder env).
      - name: china_intel_hub institutional-visit dossier block (P1)
        run: python -m pytest tests/test_china_intel_hub_visits.py tests/test_china_intel_visits_render.py -q
      - name: Intelligence Hub PIT scope + W0 measurement heals + ticker-shape gate
        run: python -m pytest tests/test_intel_hub_universe_scope.py tests/test_intel_hub_w0_heals.py tests/test_ticker_shape.py -q
      # R1A-M (2026-09-02): exact market pulse roster/markup (needs the same
      # jinja2 + pandas closure scripts.build_intel_hub already pulls in this
      # job) plus the route-scoped controller's node-executed contract.
      - name: Intelligence Hub market pulse roster/markup + controller contract
        run: python -m pytest tests/test_intelligence_hub_market_pulse_surface.py tests/test_intelligence_hub_market_pulse_client.py -q

  unrun-register-honesty:
    # The user-facing honesty register: falsifier/refutation vocabulary stays
    # off front-facing copy beyond the Policy Watch estate (operator 2026-07-27,
    # #3821), chat projections speak plain words instead of internal enums,
    # regime labels and the sizing pill disclose what they actually know, and
    # the cycle surface reads forward instead of as self-refutation. These are
    # the guards that keep the plain-word doctrine enforced by CI instead of by
    # memory; none was named by any run: step before the 2026-08-03 backfill.
    if: ${{ false }}
    gate: data
    timeout-minutes: 3
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install minimal deps
        run: pip install pytest pandas numpy pyarrow pyyaml jinja2 plotly requests beautifulsoup4 openpyxl scikit-learn
      - name: falsifier-register bans + chat plain-word projection
        run: python -m pytest tests/test_front_facing_register.py tests/test_policy_watch_register.py tests/test_policy_watch_ui.py tests/test_chat_plain_words.py -q
      - name: regime-label honesty + sizing-pill null parity + forward cycle framing
        run: python -m pytest tests/test_regime_label_honesty.py tests/test_regime_sizing_disclosure_parity.py tests/test_cycle_forward_framing.py tests/test_quad_dark_day_disclosure.py -q

  unrun-neuralweb-cortex:
    # neural-web cortex world-state consumers + the factor-contradiction plane.
    if: ${{ false }}
    gate: data
    timeout-minutes: 4
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install minimal deps
        run: pip install pytest pandas numpy pyarrow pyyaml jinja2 plotly requests beautifulsoup4 openpyxl scikit-learn
      - name: neural-web cortex world-state consumers + the factor-contradiction plane
        run: python -m pytest tests/test_build_active_build_map.py tests/test_build_cycle_pattern_state.py tests/test_imce_prospective.py tests/test_factor_contradictions.py tests/test_nw_consumers_w3.py tests/test_nw_health_escalation.py tests/test_options_nw_wiring.py tests/test_til_nw_citizenship.py tests/test_world_state_special_sits.py tests/test_world_state_stage_analysis.py -q
      - name: three-repository advisory Active Build Map
        run: python -m pytest tests/test_project_active_build_map.py -q
      # Wired 2026-08-06: the CPI substrate that test_build_cycle_pattern_state.py (in
      # the step above) builds ON, dark since it landed and red on main. Its family
      # cardinalities were frozen literals over stores the nightly grows — the code
      # comment said 46 us_baskets, the literal said 47, data/baskets/membership.json
      # had reached 48 — so the suite could only ever rot. Retargeted to assert the
      # registry MIRRORS each source artifact by native id (drops and duplicates both
      # fail; a legitimately widened universe does not). Separate step, not appended
      # to the run line above: ~32 s serial against a 4-minute cap that the existing
      # step uses ~14 s of, and a separate step names the failure when it is this one.
      - name: cycle-pattern substrate — entity registry mirrors its artifacts + monthly state lake
        run: python -m pytest tests/test_cycle_pattern_lake.py -q

  unrun-vector-baskets:
    # vector allocation surfaces + the calibration ledger and basket pulse they read.
    if: ${{ false }}
    gate: data
    timeout-minutes: 3
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install minimal deps
        run: pip install pytest pandas numpy pyarrow pyyaml jinja2 plotly requests beautifulsoup4 openpyxl scikit-learn
      - name: vector allocation surfaces + the calibration ledger and basket pulse they read
        run: python -m pytest tests/test_basket_turn_watch.py tests/test_btc_overrides.py tests/test_build_basket_pulse.py tests/test_regime_dynamics_null_gaps.py tests/test_signal_surface_bugfixes.py tests/test_spvector_page.py tests/test_trial_log_refresh.py tests/test_validation_effective_t.py tests/test_vector_conviction.py -q
      - name: basket-turn grading pack + session-stamp heal (forward-ledger audit 2026-08-05)
        run: python -m pytest tests/test_basket_turn_grading.py tests/test_heal_basket_turn_ledger.py -q
      - name: W4 staged re-entry spine + P5 deferred-context BTC chips (dat/netliq/cascade)
        run: python -m pytest tests/test_btc_reentry.py tests/test_btc_deferred_legs.py -q
      - name: blocked-entry basket washout display artifact (leave-one-out peer state, no authority)
        run: python -m pytest tests/test_basket_washout_state.py -q

  washout-turn-organ:
    # Per-name weekly washout-turn watch lane (engine/washout_turn.py) — born from the
    # 2026-08-05 MCD miss (weekly canon CB fired 07-31 at 6th-pctile depth, no consumer).
    # Enumerated here at birth: the unrun-suite census (2026-07-26) proved an unnamed
    # suite is genuinely never executed, and the reference organ's own suite
    # (test_mtf_upturn.py) is an existing instance of that hole.
    # The Signal Episode Atlas suites (stock_events / event_atlas) ride the same
    # lane: same organ family, same canon math, same ledger-lane gate.
    if: ${{ false }}
    gate: code
    timeout-minutes: 5
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install minimal deps
        run: pip install pytest pandas numpy pyarrow pyyaml jinja2 plotly requests beautifulsoup4 openpyxl scikit-learn
      - name: prophet doors incl. Door W (washout-turn entries; fire legs, prereg pins, invariance)
        run: python -m pytest tests/test_prophet_doors.py tests/test_gate_crash_failopen.py -q
      - name: prophet arena (frozen challenger policies, closure-replay ruler, ledger lane gate, pick-chain fence)
        run: python -m pytest tests/test_prophet_arena.py -q
      - name: washout-turn organ (depth-gated weekly cross, PIT completed-bars pin, ledger gate)
        run: python -m pytest tests/test_washout_turn.py tests/test_mtf_upturn.py tests/test_oracle_memory_analogues_step.py tests/test_stock_events.py tests/test_event_atlas.py -q
      - name: leader-pullback organ (§6.9 R4 — RS-leader reset detection, self-containment + no-authority pins)
        run: python -m pytest tests/test_us_leader_pullback.py -q
      - name: leader-pullback coverage publisher (§6.9 R4 — the artifact the EARLY-TURN intake reads)
        # Rides the organ's own lane because it is the organ's publisher: it supplies the
        # PIT cross-section the organ refuses to reach for, and #5007's organ + #5105's
        # intake were joined by nothing until it shipped. Its end-to-end cases drive the
        # real `originate_plans` reading the real artifact off disk (~5s serial), so the
        # pair must be able to red each other on the same head.
        run: python -m pytest tests/test_us_leader_pullback_coverage.py -q
      - name: turn-watch desk + group-turn organ (§6.9 R8 — publish path, leg-(d) organ vs fallback, recall contract; §6.9 R6 basket lifecycle)
        # The TURN WATCH desk consumes the group-turn organ's artifact and R4's organ, so
        # all three ride one lane. test_us_basket_turn.py was named by NO run: step in any
        # workflow before this — the same unrun-suite hole this job's header records for
        # test_mtf_upturn.py, found while auditing the desk (2026-08-09, 36 tests, green on
        # the base that registered it). test_us_turn_watch.py is enumerated at birth.
        run: python -m pytest tests/test_us_turn_watch.py tests/test_us_basket_turn.py -q

  prophet-anticipation-intake:
    # ANTICIPATION A1 (status-class admission) + §6.9 R3 (structure-anchored entry
    # zones, wait_reset, zone-with-expiry-to-starter, the EARLY-TURN starter tier).
    # Enumerated at BIRTH: the unrun-suite census proved an unnamed suite is genuinely
    # never executed, and 1,135 of 1,491 suites are already dark — a new acceptance
    # battery that nothing runs is not an acceptance battery.
    #
    # Its OWN job rather than a step on `unrun-vector-*` (which already carries
    # tests/test_prophet_bridge.py): that job is documented STRUCTURALLY AT-CAP at 15
    # minutes and has repeatedly timed out on a busy pool with no regression in the
    # PR. Six of the cases here drive the real `build_prophet.main()` publisher end to
    # end — ~73 s serial on a fast box, and the whole point of them is that the lane
    # gate is exercised through the PRODUCTION caller rather than a hand-fed argument —
    # so adding them to an at-cap job would convert a proof into an unattributable red.
    #
    # engine/us_early_turn.py's indicator reads ride the same suite: the module
    # deliberately owns no arithmetic of its own (it imports confluence_tiers'
    # StochRSI/RSI-MACD/session-anchor helpers), and one test pins that import so a
    # local re-derivation fails immediately.
    if: ${{ false }}
    gate: data
    timeout-minutes: 12
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install minimal deps
        run: pip install pytest pandas numpy pyarrow pyyaml jinja2 plotly requests beautifulsoup4 openpyxl scikit-learn
      - name: anticipation intake (status-class admission, era stamps, entry zones, shadow-ledger lane gate)
        run: python -m pytest tests/test_prophet_anticipation_intake.py -q
      # UNION ADMISSION (bake-off §A2) — registered in the job that already owns the
      # EARLY-TURN admission gate, because that is what the union re-classes. The suite's
      # load-bearing pin is the SCORED-gate zero-diff: this wiring is display-tier and a
      # verdict that moved would be the failure mode worth catching on every push. Runs off
      # two committed 700-row price fixtures, so it needs no data root and no network.
      - name: union admission (recall spine, badges, scored-gate zero-diff)
        run: python -m pytest tests/test_us_early_turn_union_admission.py -q
      # §6.9 R5 — the per-name "why not" receipts. Enumerated at BIRTH, in the job that
      # already owns the admission gate these receipts REPORT ON: the suite's core law is
      # that the headline reason is specificity-ordered rather than gate-ordered, which is
      # a statement about select_candidates' short-circuit order and would rot the moment
      # that order moved in a job nothing here watches. It also pins the receipts'
      # partition against select_candidates itself, so a gate change that this suite
      # misses cannot leave the shelf quietly disagreeing with the board above it.
      # Browser-free and a few seconds serial (it renders the dashboard template), and it
      # reuses the pip line above — no new dependency enters this job's import closure.
      - name: anticipation why-not receipts (headline specificity, fail-closed status, the shelf partial)
        run: python -m pytest tests/test_prophet_whynot_receipts.py -q
      - name: zone-basis copy (no raw enum/slug/internal state on the published entry_zone; EN+ZH)
        # Same job as the intake because it guards the same payload: `entry_zone` is
        # whitelisted onto site/prophet/index.json, so its `basis`/`conversion_evidence`
        # are user-visible copy. The leader branch used to interpolate the organ's own
        # state enum ("leader pullback (RESET_TURN) — …"); it was unreachable only
        # because nothing published the organ's coverage.
        run: python -m pytest tests/test_prophet_zone_basis_copy.py -q
      # Force-majeure 2026-08-09 outage backfill (operator 2026-08-11; design of
      # record research/PROPHET_OUTAGE_BACKFILL_2026_08.md). Enumerated at BIRTH for
      # the reason this job's header gives, and in THIS job specifically because its
      # two load-bearing cases drive the real `build_prophet.main()` publisher end to
      # end — the §0.7 nightly-passthrough gate is precisely "the production caller
      # neither drops nor rewrites a backfilled plan, and its provenance stamp
      # survives into index.json". Same import closure as the steps above (pandas +
      # the pip line already installed here); the pinned-input tests build throwaway
      # git repos under tmp_path, so nothing in this job writes the real tree.
      #
      # The suite's central law is a BOTH-DIRECTIONS set equality between plans
      # stamped origination_mode=outage_backfill* and the rows enumerated in
      # data/prophet/backfill_disclosures.json. It stays meaningful before the
      # artifact exists (both sets empty = no undisclosed backfill) and becomes the
      # segregation gate the moment it does.
      - name: force-majeure outage backfill (segregation both ways, nightly passthrough, collision rule, idempotence)
        run: python -m pytest tests/test_prophet_outage_backfill.py -q
      # Force-majeure 2026-08-11 outage RECONSTRUCTION (operator 2026-08-13). A STEP in
      # this job rather than a job of its own: it shares this job's import closure and
      # its whole point is that the two windows are checked together — the segregation
      # law above now reads a REGISTRY of chartered windows built from both lanes'
      # constants, so splitting them across jobs would let one half go green against a
      # registry the other half had already widened.
      #
      # What is pinned here and nowhere else: the price truncation (a poisoned 2026-08-12
      # bar must not reach the tree the board builder reads, and the fence that re-proves
      # it must fire when one is planted), the chronology refusal, and the pinned
      # pre-#5370 code vintage. The tests are pure — no board is built in CI.
      - name: force-majeure outage reconstruction 2026-08-11 (price truncation, chronology, pinned vintage)
        run: python -m pytest tests/test_prophet_outage_backfill_20260811.py -q
      # The user-facing half of the same feature (#5301): the plain-word EN/ZH chip,
      # receipt and footnote a reconstructed plan carries, plus the record split.
      # Wired HERE, with the lane that mints those plans, and wired at all because it
      # arrived registered by NO run: step — `scripts/audit_unrun_tests.py` reported
      # it as a dark suite on the merge. The two halves share one subject and one
      # copy source of truth (prophet_bridge's RECONSTRUCTED_* constants), so a change
      # that greens one while reddening the other should fail in a single job.
      - name: reconstructed-origination disclosure (chip + receipt + footnote + record split, EN/ZH)
        run: python -m pytest tests/test_prophet_origination_disclosure.py -q
      # Prophet PIT Replay Harness — general point-in-time session replay
      # (research/PROPHET_PIT_REPLAY_HARNESS_V1.md; authority
      # DEC:FORCE-MAJEURE-SESSIONS-ARE-BACKFILLED-BY-DEFAULT). Wired in THIS job
      # because it generalizes the two force-majeure lanes above it — same subject
      # (a market's session reconstructed from a pinned vintage tree and staged for
      # the market's own nightly to absorb), same import closure (pandas + the pip
      # line already installed here), and the same reason those two share a job: a
      # change to the shared truncation/fence/absorb machinery should fail in ONE
      # job, not pass one lane's suite while reddening its sibling's. All three
      # suites are fast, no network, no real board builds — synthetic frames and tmp
      # git fixtures only; the real-repo `--resolve-only` integration checks (US
      # 2026-08-14, CN/HK 2026-08-17, the US disclosed-gap/session-invalid refusals)
      # are exercised directly against this checkout, not a network call.
      - name: prophet PIT replay harness (truncate/overlay/fence, vintage resolution, disclosed-gap guard, registry, receipt idempotence, verify-collisions ordering)
        run: python -m pytest tests/test_prophet_pit_replay.py -q
      # US absorb hook (scripts/grade_us_board.py::absorb_pending_replays) — same
      # subject/job as the harness above; wired together so a change to the
      # pending/v1 schema or the harness's session ordering fails in one place.
      - name: prophet PIT replay absorb — US (grade_us_board pending-replay hook, out-of-order append, schema validation)
        run: python -m pytest tests/test_pit_replay_absorb.py -q
      # CN/HK absorb hooks (engine/china_standout_track.py, engine/board_ledger.py)
      # + the cn/hk MARKETS registry entries the harness resolves them through.
      - name: prophet PIT replay absorb — CN/HK (china_standout_track + board_ledger pending-replay hooks, registry resolution)
        run: python -m pytest tests/test_pit_replay_absorb_asia.py -q

  coiled-mtf-anchor-era:
    # COILED bonus/fire + mtf trend.d3 absolute session anchor (era
    # coiled-mtf-abs-session-2026-08-06, ruling research/COILED_MTF_SESSION_ANCHOR_
    # REPAIR_BY_FABLE.md). test_coiled.py was named by NO run step in any workflow
    # before this (the unrun-suite hole the washout-turn-organ comment above records
    # for test_mtf_upturn.py) — enumerated here together with the era's invariance
    # battery, which pins start-invariance k=1..6 on real NYSE/CN session fixtures,
    # the one-era-two-modules equality, the payload era stamps, and the CN market
    # threading. The battery reads the tracked CN reference store
    # (data/china/000001.SS.parquet) and skips that single test if absent.
    if: ${{ false }}
    gate: data
    timeout-minutes: 6
    #
    # CURATED SCOPE (2026-08-15 wave 4). `scope: exclusive` makes the paths below
    # REPLACE inference. Like cn-standout-audit above, this job had NO owned tier:
    # all 28 inferred patterns were opaque fallback. Same cause — test_coiled.py
    # imports scripts/build_china_library.py, which #5754 pointed at
    # engine/china_intel_interest.py -> china_intel_hub and the full altdata
    # convergence universe — and the same measured effect: it began matching
    # templates/index.html, a file neither suite reads.
    #
    # SCRIPTS IS ENUMERATED HERE, DELIBERATELY UNLIKE cn-standout-audit. That job
    # globs scripts/** because two of its suites rglob the tree and AST-scan it;
    # these two suites contain no traversal whatsoever (verified 2026-08-15: zero
    # rglob/glob/os.walk/subprocess in either file, and
    # tests/test_coiled_mtf_anchor_invariance.py derives ZERO ambiguities). The
    # only construct that put scripts/** in this job's fallback tier is the
    # dynamic import at scripts/build_china_library.py:1807, whose real targets
    # are all collectors.* — covered by the collectors/** glob below. So the six
    # scripts/ files named here ARE this job's whole scripts/ closure.
    #
    # THE GLOBS ARE EARNED: collectors/** for that dynamic import (its seven
    # tushare_* drip modules are unresolvable by the walker and therefore
    # invisible to the coverage test — enumeration would silently drop them);
    # engine/** because enumeration would rot on every engine import, and
    # engine/risk_radar.py:375 traverses roots=data,engine anyway; config/** and
    # data/** for the roots=config,data parquet/jsonl traversals in
    # engine/smart_money.py, lib/ai_costs.py:417 and collectors/base.py:124, plus
    # the no-suffix data/ traversal at scripts/seed_china_ths_baskets.py:318.
    # data/** is additionally REQUIRED by this job specifically: the invariance
    # battery reads the tracked CN reference store at
    # tests/test_coiled_mtf_anchor_invariance.py:268
    # (config.data_dir()/"china"/"000001.SS.parquet") and SKIPS if absent — a
    # runtime read that resolves through no literal, so under-naming data/ would
    # turn the era's start-invariance guard green-by-skip on the very edits that
    # can break it. Widening is the safe direction; do not add templates/**.
    runs-on: ubuntu-latest
    scope: exclusive
    paths:
      - "engine/**"
      - "collectors/**"
      - "config/**"
      - "data/**"
      - "lib/__init__.py"
      - "lib/ai_costs.py"
      - "lib/closes_panel.py"
      - "lib/cn_calendar.py"
      - "lib/config.py"
      - "lib/filing_value_units.py"
      - "lib/nyse_calendar.py"
      - "lib/store.py"
      - "lib/ticker_aliases.py"
      - "lib/ticker_popularity.py"
      - "research/US_BOARD_MEASUREMENT.md"
      - "scripts/__init__.py"
      - "scripts/build_chart_data.py"
      - "scripts/build_china_library.py"
      - "scripts/seed_china_ths_baskets.py"
      - "scripts/thematic_rotation_phase0.py"
      - "scripts/validate_regime_fwd.py"
      - "site/china_intel/**"
      - "site/chinaaltdata/**"
      - "site/chinanews/**"
      - "site/chinaradar/**"
      - "site/chinaspecialdata/**"
      - "site/basketdata/fear_greed.json"
      - "site/factordata/basket_washout_state.json"
      - "site/factordata/china_standouts.json"
      - "tests/test_coiled.py"
      - "tests/test_coiled_mtf_anchor_invariance.py"
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install minimal deps
        run: pip install pytest pandas numpy pyarrow pyyaml jinja2 plotly requests beautifulsoup4 openpyxl scikit-learn
      - name: coiled + mtf_upturn anchored-grid suites (bonus/fire chips, trend.d3, era stamps)
        run: python -m pytest tests/test_coiled.py tests/test_coiled_mtf_anchor_invariance.py -q

  unrun-page-guards:
    # Shipped-page regression guards over the rendered estate: the US
    # action-board hover cards, the macro desk surface + its proportional
    # primary type + the "What To Do Now" glance, dashboard cold-load
    # budgets, the stock-dashboard Grid/Table bootstrap + logo adapter, the
    # landing globe overlays, the Divergence Radar page boot, the reports
    # timeline, and the bilingual span-leak scan. Source-scanning suites over
    # templates/** + site/** — the strictly-dark class the census's fourth
    # measurement trap describes: their subjects were always path-covered,
    # but no run: step named the suites themselves before 2026-08-03.
    if: ${{ false }}
    gate: data
    timeout-minutes: 4
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install minimal deps
        run: pip install pytest pandas numpy pyarrow pyyaml jinja2 plotly requests beautifulsoup4 openpyxl scikit-learn
      - name: macro desk surface, typography, glance + action-board hover cards
        run: python -m pytest tests/test_macro_desk_surface.py tests/test_macro_primary_typography.py tests/test_macro_what_to_do_glance.py tests/test_action_board_hover_cards.py tests/test_board_copy_and_controls_guard.py -q
      # Canonical — and deliberately ONLY — home of the shared light-mode odometer
      # contract (templates/theme.css, its site/ twin, five report templates).
      # #5458 landed the suite unwired; three sessions healed that concurrently on
      # 2026-08-12 (#5455 engine-render-guards, #5460 here, #5461 template-site-sync),
      # leaving it triple-run. Consolidated to this step alone: it is a shipped-page
      # source-scan like the rest of this job. Don't re-add copies elsewhere.
      - name: shared light-mode odometer needle across five report templates
        run: python -m pytest tests/test_odometer_light_mode_surface.py -q
      - name: dashboard cold-load budget + stocktable bootstrap + logo adapter + globe overlays
        run: python -m pytest tests/test_dashboard_cold_load_budget.py tests/test_stocktable_load_order.py tests/test_stock_logo_system.py tests/test_globe_tooltip_behavior.py -q
      - name: radar page boot + reports timeline + bilingual span-leak scan
        run: python -m pytest tests/test_radar_page_boot.py tests/test_reports_timeline_ui.py tests/test_bilingual_span_leak.py -q
      # china_heatmap's anonymous tier preview: the server-rendered summary layer
      # (all three markets), the hover card's two empty states, and the shipped
      # shell. It lives HERE rather than in `tier-gate` because the summary is
      # computed by engine/market_heatmap.py, which imports pandas — tier-gate's
      # venv is pytest/pyyaml/jinja2/fastapi/httpx and would error, not fail.
      # The boundary half is pinned in tier-gate by test_site_access_boundary.py
      # and test_regwall.py, which read the same three mirrors.
      - name: china_heatmap tier preview (SSR content layer + hover-card gate)
        run: python -m pytest tests/test_china_heatmap_gate.py -q

  unrun-picks-boards:
    # pick lab runners + US/CN board surfaces — shipped picks, their outcome
    # ledgers, and the us_prophet_v1 priority-board rank + UI (#4331; suites
    # found unwired by its 2026-08-02 adversarial review).
    # test_cn_prophet_candidates_schema.py joined 2026-08-04: the CN board's PIT
    # candidate ledger and its stamp_date column (schema contract §3.1), same
    # CN/US board-store family as the cn_board/cn_track suites already here.
    # test_opportunity_timeline.py joins the same four regional track ledgers into
    # the per-symbol Terminal receipt projection.  It pins nested-era flattening,
    # stable-id dedupe, candidate/watch authority and last-good atomic publication.
    # china_board_rank / china_prophet_shadow / us_context_vector joined
    # 2026-08-04: the #4331-deferred prophet-board family (UNRUN_TEST_CENSUS
    # staging lane) — CN board rank engine, CN candidate producer, and the US
    # context-vector store whose TestSchemaContract is the contract §4 pin.
    # test_china_continuation_watch.py joined 2026-08-06: the CN shadow-accrual
    # cohort that appends to the SAME china_standout_track board store as the
    # cn_board/cn_track suites already here, so it shares their venv. It was
    # enumerated in NO workflow — and its buy-filter reason pin had been failing
    # invisibly since the 2026-08-04 reason-string correction hoisted those
    # literals out of `_buy_filter` into module constants, leaving the
    # `co_consts` scan reading an empty tuple. A guard nothing runs is a guard
    # that is already open: the repair and the enrolment ship together.
    if: ${{ false }}
    gate: data
    # CURATED SCOPE (2026-08-14 incident follow-up). `scope: exclusive` makes
    # the paths below REPLACE inference instead of being unioned under it, so
    # the opaque fallback tier this job used to carry — whole-tree `site/**`,
    # `templates/**`, `docs/**`, `research/**` claims smeared out of a
    # subprocess or traversal deep in its closure — stops selecting it. Every
    # pattern here was generated from the job's OWN import closure and audited
    # for zero misses: no file this job demonstrably reads lost its owner.
    # 245s. The picks/boards census reaches deep into engine/ and the
    # pick_lab package. Top-level `engine/*` is enumerated (2026-08-15
    # follow-up to #5685): 846 tracked / 328 concrete. Subject packages
    # stay globbed. Named suites and entry scripts remain file-by-file;
    # the declared-scope coverage audit re-checks them on every load.
    scope: exclusive
    paths:
      # F01 R1B closure widening 2026-09-04 (contract-delta): build_site.py's
      # guarded macro-suite hook imports the page builder, which carries the
      # workspace contract/registry pair. Checker-named files only; the
      # composers are not in this job's closure (thin __init__).
      - "engine/market_os/macro_workspaces/__init__.py"
      - "engine/market_os/macro_workspaces/contract.py"
      - "engine/market_os/macro_workspaces/registry.py"
      # Added-date closure widening 2026-09-01 (contract-delta): the surfaces
      # census suite imports the intl builders, whose closure spans the intl
      # engine stack. Widened, never narrowed.
      - "engine/cb_desk.py"
      - "engine/contagion.py"
      - "engine/contagion_links.py"
      - "engine/flow_regime.py"
      - "engine/hk_global.py"
      - "engine/hk_global_beta.py"
      - "engine/international_macro_dashboard.py"
      - "engine/intl_bonds.py"
      - "engine/intl_compare.py"
      - "engine/intl_equity_risk.py"
      - "engine/intl_market_confirmation.py"
      - "engine/intl_performance.py"
      - "engine/intl_rates.py"
      - "engine/intl_regime.py"
      - "engine/intl_risk.py"
      - "engine/intl_rotation.py"
      - "engine/intl_run.py"
      - "engine/intl_stocks.py"
      - "engine/ird_velocity.py"
      - "engine/rri_shadow.py"
      - "scripts/build_international_macro.py"
      # B1A closure widening 2026-08-24 (contract-delta): the security_state producer stage in build_stock_library lazily imports the company-intelligence reader chain and Data OS identity.
      - "engine/company_intelligence/__init__.py"
      - "engine/company_intelligence/contracts.py"
      - "engine/company_intelligence/documents.py"
      - "engine/company_intelligence/event_id_adapter.py"
      - "engine/company_intelligence/event_workspace.py"
      - "engine/company_intelligence/qa_exchange.py"
      - "engine/company_intelligence/qa_reconstruction.py"
      - "engine/company_intelligence/events.py"
      - "engine/company_intelligence/health.py"
      - "engine/company_intelligence/identity.py"
      - "engine/company_intelligence/issuer_profiles.py"
      # 2026-08-26 widening (PR #6454): #6376 (E3-B AAPL canonical
      # qa_exchange publication) added qa_exchange/qa_reconstruction to this
      # job's suite closure; the coverage audit reds without them.
      - "engine/company_intelligence/qa_exchange.py"
      - "engine/company_intelligence/qa_reconstruction.py"
      - "engine/company_intelligence/views.py"
      - "engine/earnings_release/binding.py"
      - "engine/earnings_release/figures.py"
      - "engine/earnings_release/filing_key.py"
      - "engine/earnings_release/receipts.py"
      - "engine/security_state.py"
      - "engine/theme_graph/__init__.py"
      - "engine/theme_graph/identity.py"
      - "lib/dataos/identity.py"
      - "app/__init__.py"
      - "app/mailer.py"
      - "collectors/*"
      - "config/**"
      - "contracts/**"
      - "data/**"
      - "engine/__init__.py"
      - "engine/_text_scrub.py"
      - "engine/active_alloc.py"
      - "engine/active_commodity.py"
      - "engine/activist.py"
      - "engine/advanced_breadth.py"
      - "engine/ai_desk.py"
      - "engine/ai_desk_scorer.py"
      - "engine/alert_triage.py"
      # engine/alert_triage.py imports engine/alert_time.py (the Alert Center
      # three-clock / ET board-day contract, Wave 7 2026-08-20), so this curated
      # scope's import closure reaches it. Declared here because a curated
      # `scope: exclusive` job must cover its OWN closure — widening is always the
      # safe direction (scripts/check_contract_delta.py).
      - "engine/alert_time.py"
      - "engine/alerts.py"
      - "engine/alt_cycle.py"
      - "engine/altdata_confirmers.py"
      - "engine/altdata_ledger.py"
      - "engine/altdata_models.py"
      - "engine/altdata_picks.py"
      - "engine/altdata_signals.py"
      - "engine/altdata_stage.py"
      - "engine/analyst_revisions.py"
      - "engine/anticipation.py"
      - "engine/axes.py"
      - "engine/bar_derive.py"
      - "engine/base_effect.py"
      - "engine/basket_behaves_as.py"
      - "engine/basket_breadth_divergence.py"
      - "engine/basket_freeze.py"
      - "engine/basket_history.py"
      - "engine/basket_index.py"
      - "engine/basket_levels_persist.py"
      - "engine/basket_membership_pit.py"
      - "engine/basket_mtf.py"
      - "engine/basket_score.py"
      - "engine/basket_tape.py"
      - "engine/baskets.py"
      - "engine/baskets_canada.py"
      - "engine/baskets_china.py"
      - "engine/baskets_hk.py"
      - "engine/baskets_intl.py"
      - "engine/baskets_region.py"
      - "engine/beneficial_ownership.py"
      - "engine/board_ledger.py"
      - "engine/board_shadow.py"
      - "engine/bottleneck.py"
      - "engine/bottom_radar.py"
      - "engine/bottom_ruler.py"
      - "engine/btc_alerts.py"
      - "engine/btc_correlation_regime.py"
      - "engine/btc_cycle_thesis.py"
      - "engine/btc_dat.py"
      # P0A: tests/test_btc_decision.py reaches this module through the Vector
      # builder. This curated scope replaces inference, so the closure entry is
      # explicit; widening is the safe direction.
      - "engine/btc_decision.py"
      - "engine/btc_impulse_ledger.py"
      - "engine/btc_impulse_radar.py"
      - "engine/btc_impulse_radar_backtest.py"
      - "engine/btc_inputs.py"
      - "engine/btc_leverage_cascade.py"
      - "engine/btc_master.py"
      - "engine/btc_mtf.py"
      - "engine/btc_options.py"
      - "engine/btc_override_ledger.py"
      - "engine/btc_overrides.py"
      - "engine/btc_recommend.py"
      - "engine/btc_regime.py"
      - "engine/btc_regime_flags.py"
      - "engine/btc_regime_ledger.py"
      - "engine/btc_signals.py"
      - "engine/canada_axes.py"
      - "engine/canada_conditions.py"
      - "engine/canada_factor_beta.py"
      - "engine/canada_fundamentals.py"
      - "engine/canada_inputs.py"
      - "engine/canada_insider.py"
      - "engine/canada_overlay.py"
      - "engine/canada_regime.py"
      - "engine/canada_run.py"
      - "engine/canon.py"
      - "engine/capital_allocation.py"
      - "engine/catalyst_stock.py"
      - "engine/catalyst_tone.py"
      - "engine/china_allocation.py"
      - "engine/china_altdata.py"
      - "engine/china_basket_spine.py"
      - "engine/china_basket_turn.py"
      - "engine/china_board_rank.py"
      - "engine/china_continuation_watch.py"
      - "engine/china_conviction.py"
      - "engine/china_crowding.py"
      - "engine/china_discovery.py"
      - "engine/china_event_calendar.py"
      - "engine/china_extras.py"
      - "engine/china_fundamentals.py"
      - "engine/china_intel_bus.py"
      - "engine/china_intel_hub.py"
      - "engine/china_intel_interest.py"
      - "engine/china_liquidity.py"
      - "engine/china_lowvol.py"
      - "engine/china_market_state.py"
      - "engine/china_masterminds.py"
      - "engine/china_microstructure.py"
      - "engine/china_name_score.py"
      - "engine/china_name_score_grader.py"
      - "engine/china_narrative_radar.py"
      - "engine/china_narrative_tags.py"
      - "engine/china_news.py"
      - "engine/china_news_intel.py"
      - "engine/china_pboc_stance.py"
      - "engine/china_prophet_shadow.py"
      - "engine/china_radar.py"
      - "engine/china_radar_ledger.py"
      - "engine/china_regime_store.py"
      - "engine/china_reversal.py"
      - "engine/china_reversal_watch.py"
      - "engine/china_sector_index.py"
      - "engine/china_sector_pathway.py"
      - "engine/china_sector_turn.py"
      - "engine/china_signal_lab.py"
      - "engine/china_signals.py"
      - "engine/china_standout_audit.py"
      - "engine/china_standout_track.py"
      - "engine/china_strategies.py"
      - "engine/china_validation.py"
      - "engine/close_pass/**"
      - "engine/cn_ai_semis_confirmer.py"
      - "engine/cn_newswires.py"
      - "engine/cn_prophet_audit.py"
      - "engine/cn_v3_tripwires.py"
      - "engine/codex_provider.py"
      - "engine/coiled.py"
      - "engine/commodity_alerts.py"
      - "engine/commodity_inputs.py"
      - "engine/commodity_signals.py"
      - "engine/commodity_strategies.py"
      - "engine/composite_score.py"
      - "engine/conditions.py"
      - "engine/confluence_latch.py"
      - "engine/confluence_tiers.py"
      - "engine/conviction_accrual.py"
      - "engine/conviction_delta.py"
      - "engine/cross_asset.py"
      - "engine/cross_asset_confirm.py"
      - "engine/cross_asset_trend.py"
      - "engine/crowding.py"
      - "engine/cycle_ontology.py"
      - "engine/cycles.py"
      - "engine/dannytrades.py"
      - "engine/dannytrades_chip.py"
      - "engine/demand_capex.py"
      - "engine/demand_chain.py"
      - "engine/desk_ledger.py"
      - "engine/desk_scorer.py"
      - "engine/deterioration_cascade.py"
      - "engine/dislocation.py"
      - "engine/dispersion.py"
      - "engine/donor.py"
      - "engine/earnings_blackout.py"
      - "engine/earnings_catalyst.py"
      - "engine/earnings_qual.py"
      - "engine/election_cycle.py"
      - "engine/emergence_alerts.py"
      - "engine/entity_resolver.py"
      - "engine/entry_primitives.py"
      - "engine/entry_signal.py"
      - "engine/equity_alloc.py"
      - "engine/equity_factors.py"
      - "engine/etf_board.py"
      - "engine/etf_consensus.py"
      - "engine/etf_pulse.py"
      - "engine/etf_registry.py"
      - "engine/event_atlas.py"
      - "engine/event_calendar.py"
      - "engine/event_landmine.py"
      - "engine/event_risk.py"
      - "engine/expectation_state.py"
      - "engine/experiments_registry.py"
      - "engine/extension.py"
      - "engine/factor_exposure.py"
      - "engine/factor_seasonality.py"
      - "engine/falsifier_tripwires.py"
      - "engine/fda_scarcity.py"
      - "engine/fear_greed.py"
      - "engine/filing_transition.py"
      - "engine/flip_confirmation.py"
      - "engine/foresight_cascade.py"
      - "engine/foresight_earliness.py"
      - "engine/foresight_enb.py"
      - "engine/foresight_score.py"
      - "engine/foresight_sizing.py"
      - "engine/forward_calendar_context.py"
      - "engine/forward_dist.py"
      - "engine/froth_fragility.py"
      - "engine/gdelt_client.py"
      - "engine/gex_confirm.py"
      - "engine/gex_engine.py"
      - "engine/global_liquidity.py"
      - "engine/glut_watch.py"
      - "engine/gov_exposure.py"
      - "engine/grading.py"
      - "engine/grading_stats.py"
      - "engine/greeks.py"
      - "engine/group_context.py"
      - "engine/group_flow.py"
      - "engine/guidance_gap.py"
      - "engine/hazard_score.py"
      - "engine/hk_board_rank.py"
      - "engine/hk_leadership.py"
      - "engine/hk_southbound_stocks.py"
      - "engine/hold.py"
      - "engine/holdings_signals.py"
      - "engine/htf_durability.py"
      - "engine/hub_track_record.py"
      - "engine/hysteresis.py"
      - "engine/i18n.py"
      - "engine/ignition_audit.py"
      - "engine/ignition_features.py"
      - "engine/ignition_radar.py"
      - "engine/index_direction.py"
      - "engine/index_leadership.py"
      - "engine/index_leadership_track.py"
      - "engine/indicators.py"
      - "engine/inputs.py"
      - "engine/intl_inputs.py"
      - "engine/intl_market_state.py"
      - "engine/intl_recovery_quality.py"
      - "engine/ipo_hk.py"
      - "engine/ipo_lockup.py"
      - "engine/ipo_radar.py"
      - "engine/leadership_crack.py"
      - "engine/ledger_identity.py"
      - "engine/ledger_lane.py"
      - "engine/liquidity_chip.py"
      - "engine/live_overlay.py"
      - "engine/live_quotes.py"
      - "engine/llm_auth.py"
      - "engine/long_hold_clocks.py"
      - "engine/macro_news.py"
      - "engine/macro_surprise.py"
      - "engine/manager_lag.py"
      - "engine/manager_quality.py"
      - "engine/manager_trades.py"
      - "engine/marker_integrity.py"
      - "engine/market_gamma.py"
      - "engine/market_heatmap.py"
      - "engine/market_state.py"
      - "engine/market_state_audit.py"
      - "engine/market_state_ca.py"
      - "engine/market_state_tune.py"
      - "engine/master_brain.py"
      - "engine/masterminds.py"
      - "engine/moat_falsifiers.py"
      - "engine/momentum_display.py"
      - "engine/mtf_monitor.py"
      - "engine/mtf_upturn.py"
      - "engine/name_direction.py"
      - "engine/name_resolver.py"
      - "engine/name_score.py"
      - "engine/name_score_grader.py"
      - "engine/narrative_crossmarket.py"
      - "engine/narrative_emergence.py"
      - "engine/narrative_regime.py"
      - "engine/narrative_rotation.py"
      - "engine/news_common.py"
      - "engine/news_events.py"
      - "engine/news_flow.py"
      - "engine/news_translate.py"
      - "engine/news_vector.py"
      - "engine/ohlc_reconstruct.py"
      - "engine/ollama_provider.py"
      - "engine/opex.py"
      - "engine/options_dislocation.py"
      - "engine/options_ivspread.py"
      - "engine/options_structure.py"
      - "engine/options_universe.py"
      - "engine/ownership_event_wire.py"
      - "engine/passport.py"
      - "engine/path_personality.py"
      - "engine/pit.py"
      - "engine/pit_lag_recorder.py"
      - "engine/playbook.py"
      - "engine/policy_calendar.py"
      - "engine/pooling.py"
      - "engine/portfolio.py"
      - "engine/postcross.py"
      - "engine/prediction_markets.py"
      - "engine/price_ladder.py"
      - "engine/prophet_arena.py"
      - "engine/prophet_board_read.py"
      - "engine/prophet_board_since.py"
      - "engine/prophet_bridge.py"
      - "engine/prophet_doors.py"
      - "engine/prophet_integrity.py"
      - "engine/prophet_management.py"
      - "engine/prophet_stage_inputs.py"
      - "engine/provenance_sidecar.py"
      - "engine/provider_health.py"
      - "engine/pullback_zone.py"
      - "engine/qbus.py"
      - "engine/qkernel.py"
      - "engine/qledger.py"
      - "engine/qledger_desk_adapter.py"
      - "engine/qledger_evidence_clock.py"
      - "engine/qledger_validity.py"
      - "engine/quant_lab/**"
      - "engine/rate_inflation_transmission.py"
      - "engine/yield_momentum.py"
      - "engine/rebalance_calendar.py"
      - "engine/reflexivity.py"
      - "engine/regime.py"
      - "engine/regime_conditioning_coverage.py"
      - "engine/regime_label.py"
      - "engine/regime_one.py"
      - "engine/regime_snap.py"
      - "engine/regime_snap_veto.py"
      - "engine/regime_vector.py"
      - "engine/residual_alpha.py"
      - "engine/risk_envelope.py"
      - "engine/risk_radar.py"
      - "engine/risk_radar_backtest.py"
      - "engine/risk_radar_intl.py"
      - "engine/risk_radar_intl_audit.py"
      - "engine/risk_radar_intl_tune.py"
      - "engine/risk_radar_market_catalysts.py"
      - "engine/risk_radar_recovery.py"
      - "engine/risk_radar_scorecard.py"
      - "engine/risk_sizing.py"
      - "engine/risk_state.py"
      - "engine/roc_blowoff.py"
      - "engine/sector_cycles.py"
      - "engine/sector_ignition.py"
      - "engine/sector_legs.py"
      - "engine/sector_pulse.py"
      - "engine/sector_rate_inflation.py"
      - "engine/sector_signals.py"
      - "engine/session_anchor.py"
      - "engine/setup_tier.py"
      - "engine/setups.py"
      - "engine/shadow_book.py"
      - "engine/short_volume.py"
      - "engine/signal_archive.py"
      - "engine/signal_frontier_docket.py"
      - "engine/signal_gate.py"
      - "engine/signal_governor.py"
      - "engine/signal_lab.py"
      - "engine/signal_quality.py"
      - "engine/signal_stack.py"
      - "engine/smart_money.py"
      - "engine/sp500_heatmap.py"
      - "engine/special_arb.py"
      - "engine/special_situations.py"
      - "engine/spine.py"
      - "engine/spotlight.py"
      - "engine/spvector_overlay.py"
      - "engine/stage_analysis.py"
      - "engine/stage_flows.py"
      - "engine/stage_industry.py"
      - "engine/stage_research.py"
      - "engine/stock_desk.py"
      - "engine/stock_dossier.py"
      - "engine/stock_events.py"
      - "engine/stock_fundamentals.py"
      - "engine/stock_macro_sensitivity.py"
      - "engine/stock_personality.py"
      - "engine/stock_score.py"
      - "engine/stock_technicals.py"
      - "engine/stock_view.py"
      - "engine/store_guard.py"
      - "engine/strategies.py"
      - "engine/subsector_confluence.py"
      - "engine/subsector_rotation.py"
      - "engine/subsector_rotation_alerts.py"
      - "engine/subsector_rotation_china.py"
      - "engine/subsector_scan.py"
      - "engine/subsector_track_record.py"
      - "engine/subsector_turn.py"
      - "engine/sue.py"
      - "engine/t0_indicator.py"
      - "engine/technicals.py"
      - "engine/theme_alerts.py"
      - "engine/theme_context.py"
      - "engine/theme_crowding.py"
      - "engine/theme_discovery.py"
      - "engine/theme_emergence.py"
      - "engine/theme_fingerprint.py"
      - "engine/theme_revisions.py"
      - "engine/theme_scoring.py"
      - "engine/theme_tape.py"
      - "engine/theme_validation.py"
      - "engine/themes_heatmap.py"
      - "engine/thesis_funnel.py"
      - "engine/thetadata_store.py"
      - "engine/ticker_alerts.py"
      - "engine/ticker_shape.py"
      - "engine/total_return.py"
      - "engine/track_era.py"
      - "engine/track_ledger.py"
      - "engine/track_record.py"
      - "engine/track_scoring.py"
      - "engine/translate.py"
      - "engine/transmission_publish.py"
      - "engine/trial_ledger.py"
      - "engine/tushare_freshness.py"
      - "engine/universe_history.py"
      - "engine/us_board_rank.py"
      - "engine/us_candidate_lanes.py"
      - "engine/us_context_vector.py"
      - "engine/us_early_turn.py"
      - "engine/us_prophet_fusion.py"
      - "engine/us_prophet_grades.py"
      - "engine/us_prophet_w3.py"
      - "engine/validation.py"
      - "engine/valuation.py"
      - "engine/velocity.py"
      - "engine/vol_forecast.py"
      - "engine/vol_regime.py"
      - "engine/vol_shock_scorecard.py"
      - "engine/vol_squeeze.py"
      - "engine/washout_turn.py"
      - "engine/weinstein_stage.py"
      - "engine/codex_lane/**"
      - "engine/earnings_narrative/**"
      - "engine/fundamental_forensics/**"
      - "engine/government_revenue/**"
      - "engine/metabolism/**"
      - "engine/neuralweb/**"
      - "engine/oracle/**"
      - "engine/pick_lab/**"
      - "engine/press/**"
      - "engine/prophet_live/**"
      - "engine/research_vault/**"
      - "engine/signal_foundry/**"
      - "lib/*"
      - "research/PROPHET_ARENA_REGISTRATION.md"
      - "research/REGIME_RELIABILITY_FACTOR_CROWDING_ADJUDICATION.md"
      - "research/SIGNAL_EPISODE_ATLAS_MASTERPLAN_BY_FABLE.md"
      - "research/STOCK_PERSONALITY_MASTERPLAN_BY_FABLE.md"
      - "research/US_BOARD_MEASUREMENT.md"
      # P0-ST band-repair receipt: referenced from engine/china_microstructure.py's
      # MAIN_ST_BAND_WIDE_DATE provenance comment, so the closure scanner reaches it
      # (contract-delta 2026-08-20; widening is always the safe direction).
      - "research/cn_limit/P0_ST_BAND_REPAIR_RECEIPT_2026-08-19.md"
      - "research/prophet_fusion/PR1B_BASELINE_RACE.md"
      - "research/prophet_fusion/PR2_C2_REDUNDANCY.md"
      - "research/prophet_fusion/families.yml"
      - "research/prophet_fusion/pr1b_baseline_race/report.json"
      - "research/prophet_fusion/pr2_c2/report.json"
      - "research/prophet_fusion/pr2_c2/era_frame_keys.parquet"
      - "research/prophet_fusion/pr2_c2/era_frame_pin.json"
      - "scripts/__init__.py"
      - "scripts/_active_render.py"
      - "scripts/backtest_ladder_history.py"
      - "scripts/build_baskets_canada.py"
      - "scripts/build_baskets_china.py"
      - "scripts/build_baskets_china_ths.py"
      - "scripts/build_baskets_hk.py"
      - "scripts/build_baskets_intl.py"
      - "scripts/build_biocatalyst.py"
      - "scripts/build_btc_strategy.py"
      - "scripts/build_canada.py"
      - "scripts/build_canada_library.py"
      - "scripts/build_capital_structure_page.py"
      - "scripts/build_chart_data.py"
      - "scripts/build_china.py"
      - "scripts/build_china_library.py"
      - "scripts/build_china_masterminds.py"
      - "scripts/build_china_microstructure.py"
      - "scripts/build_china_pick_lab.py"
      - "scripts/build_china_strategies.py"
      - "scripts/build_commodity_strategies.py"
      - "scripts/build_crossmarket.py"
      - "scripts/build_fundamental_forensics.py"
      - "scripts/build_hk.py"
      - "scripts/build_index_leadership.py"
      - "scripts/build_intl.py"
      - "scripts/build_intl_library.py"
      - "scripts/build_ipo.py"
      - "scripts/build_live_overlay.py"
      # F01 R1B closure widening 2026-09-04 (contract-delta): imported by
      # scripts/build_site.py's guarded macro-suite hook.
      - "scripts/build_macro_suite_pages.py"
      - "scripts/build_market_heatmap.py"
      - "scripts/build_market_memory_page.py"
      - "scripts/build_market_structure_page.py"
      - "scripts/build_masterminds.py"
      - "scripts/build_opportunity_timeline.py"
      - "scripts/build_override_shadow.py"
      - "scripts/build_pick_lab.py"
      - "scripts/build_prophet.py"
      - "scripts/build_reports.py"
      - "scripts/build_risk_envelope.py"
      - "scripts/build_rr_banner.py"
      - "scripts/build_site.py"
      - "scripts/build_sp500_heatmap.py"
      - "scripts/build_spvector.py"
      - "scripts/build_stage_analysis.py"
      - "scripts/build_state_of_themes.py"
      - "scripts/build_stock_board_v2.py"
      - "scripts/build_stock_library.py"
      # Import-closure entry: build_stock_library reuses
      # scripts/snapshot_shadow_book.rows_from_board for the shadow-book
      # snapshot (one percentile definition), so exclusive scope must name it.
      - "scripts/snapshot_shadow_book.py"
      - "scripts/build_strategies.py"
      - "scripts/build_subsector_confluence.py"
      - "scripts/build_subsector_rotation.py"
      - "scripts/build_subsector_rotation_china.py"
      - "scripts/build_subsector_rotation_china_pages.py"
      - "scripts/build_subsector_rotation_pages.py"
      - "scripts/build_theme_detail.py"
      - "scripts/build_themes_heatmap.py"
      - "scripts/build_vector.py"
      - "scripts/check_board_contradictions.py"
      - "scripts/check_signal_gate_coherence.py"
      # Import-closure entry: scripts/build_stock_library.py lazy-imports
      # scripts/check_stretch_oracle_contract.py, so exclusive scope must name
      # it or this job silently stops running when the checker changes.
      - "scripts/check_stretch_oracle_contract.py"
      - "scripts/check_title_i18n.py"
      - "scripts/close_pass_publish.py"
      - "scripts/close_pass_reconcile.py"
      # Import-closure entry for tests/test_us_board_grader_honesty.py, wired into this
      # job below.  That suite READS scripts/exit_policy_study.py's source directly to
      # cross-pin one capture vocabulary across the study and the shipped grader, so the
      # file is in the job's transitive closure even though no command here names it.
      # `scope: exclusive` REPLACES inference, so an undeclared closure file means this
      # job silently stops running when that file changes — and reports green forever.
      # tests/test_ci_pack.py::test_curated_exclusive_scopes_cover_their_own_import_closure
      # is the guard; widening is the direction it prescribes.
      - "scripts/exit_policy_study.py"
      - "scripts/freshness_sentinel.py"
      - "scripts/grade_bottom_calls.py"
      - "scripts/grade_us_board.py"
      - "scripts/grade_us_prophet_candidates.py"
      - "scripts/accrue_us_prophet_w3.py"
      - "scripts/heal_stage_forward_ledger.py"
      - "scripts/measure_us_track_era_recompute.py"
      - "scripts/notify.py"
      - "scripts/prophet_fusion_arena.py"
      - "scripts/prophet_fusion_c2.py"
      - "scripts/prophet_fusion_labels.py"
      - "scripts/prophet_fusion_race.py"
      - "scripts/publish_earnings_r2.py"
      - "scripts/regime_reliability_phase0.py"
      - "scripts/seed_china_ths_baskets.py"
      - "scripts/thematic_rotation_phase0.py"
      - "scripts/translate_profiles.py"
      - "scripts/us_prophet_fusion_compare.py"
      - "scripts/validate_regime_fwd.py"
      - "site/basketdata/**"
      - "site/btc_strategy.html"
      - "site/china_intel/**"
      - "site/chinaaltdata/**"
      - "site/chinanews/**"
      - "site/chinaradar/**"
      - "site/chinaspecialdata/**"
      - "site/chinastatedata/**"
      - "site/factordata/**"
      - "site/flowleaders/**"
      - "site/leaderradar/**"
      - "site/marketdata/**"
      - "site/master_brief.json"
      # Claimed for tests/test_prophet_overtime_horizon_reconciliation.py, wired
      # into this job with the other prophet board surfaces: its population check
      # reads the COMMITTED payload — site/prophet/index.json AND the
      # site/prophet/states/ directory beside it. The closure scanner names only
      # the index (the states read is a directory traversal), so pinning the one
      # file would leave the states half unclaimed and this job would not rerun
      # for an edit to it. `**` matches the sibling biocatalyst-serving claim.
      - "site/prophet/**"
      - "site/qledger/track_record.json"
      - "site/sectordata/**"
      - "site/stocktable.js"
      # F13-X1 help source-validation closure reached through build_site.
      - "templates/_navlinks.html.j2"
      - "templates/_prophet_card.html.j2"
      - "templates/_public_footer.html.j2"
      - "templates/_public_nav.html.j2"
      - "templates/_stock_decision.css.j2"
      - "templates/_track_record_dlg.html.j2"
      - "templates/_us_board_cards.html.j2"
      - "templates/canada.html.j2"
      - "templates/china.html.j2"
      - "templates/dashboard.html.j2"
      - "templates/hk.html.j2"
      - "templates/intl.html.j2"
      - "templates/measurement.html.j2"
      - "templates/methodology.html.j2"
      - "templates/stocktable.js"
      - "templates/support.html.j2"
      - "templates/theme.css"
      - "tests/test_b3_forex_consumer.py"
      - "tests/test_board_contradictions.py"
      - "tests/test_bottom_ledger.py"
      - "tests/test_build_china_library_more_actionable_guard.py"
      - "tests/test_china_board_rank.py"
      - "tests/test_china_board_rank_v4.py"
      - "tests/test_china_continuation_watch.py"
      - "tests/test_china_intel_interest.py"
      - "tests/test_china_prophet_shadow.py"
      - "tests/test_cn_board_unified_grid.py"
      - "tests/test_cn_prophet_candidates_schema.py"
      - "tests/test_cn_track_ledger_eras.py"
      - "tests/test_confluence_veto_leg_telemetry.py"
      - "tests/test_dashboard_template_render.py"
      - "tests/test_grade_us_board.py"
      - "tests/test_heal_stage_forward_ledger.py"
      - "tests/test_macro_risk_dialog.py"
      - "tests/test_opportunity_timeline.py"
      - "tests/test_pick_lab_book.py"
      - "tests/test_pick_lab_cn_runner.py"
      - "tests/test_pick_lab_render.py"
      - "tests/test_pick_lab_runner.py"
      - "tests/test_prophet_board_read.py"
      - "tests/test_prophet_board_since.py"
      - "tests/test_prophet_candidate_added_date_surfaces.py"
      - "tests/test_prophet_card_added_date.py"
      - "tests/test_prophet_card_live_change.py"
      - "tests/test_prophet_card_shared.py"
      - "tests/test_prophet_fusion_arena.py"
      - "tests/test_prophet_fusion_c2.py"
      - "tests/test_prophet_fusion_families.py"
      - "tests/test_prophet_fusion_labels.py"
      - "tests/test_prophet_fusion_race.py"
      - "tests/test_prophet_fusion_w3_structural.py"
      - "tests/test_prophet_verb_ink_contrast.py"
      - "tests/test_shadow_book_percentile.py"
      - "tests/test_stage_analysis.py"
      - "tests/test_stage_industry_live_partition.py"
      - "tests/test_stock_board_v2.py"
      - "tests/test_track_ledger_emitters.py"
      - "tests/test_track_ledger_era.py"
      - "tests/test_track_record_dlg.py"
      - "tests/test_us_board_fail_closed_freshness.py"
      - "tests/test_us_board_grader_honesty.py"
      - "tests/test_us_board_lanes.py"
      - "tests/test_us_board_priority_ui.py"
      - "tests/test_us_board_rank.py"
      - "tests/test_us_board_w3_evidence.py"
      - "tests/test_us_prophet_fusion.py"
      - "tests/test_us_prophet_w3.py"
      - "tests/test_us_context_vector.py"
      - "tests/test_us_context_vector_accrual.py"
      - "tests/test_weinstein_stage.py"
    # 2026-08-15 pack-9 / run 31881072256 on PR #5750: the first semantic step
    # (1253 tests) took 223.76s; the fusion step was SIGKILL'd when this
    # 5-minute logical-job deadline expired at 300s, and the post-kill
    # `git for-each-ref refs/replace` then exited 128 as infrastructure
    # unknown. 12 minutes covers fusion + board-read + era + stage-analysis.
    timeout-minutes: 12
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install minimal deps
        run: pip install pytest pandas numpy pyarrow pyyaml jinja2 plotly requests beautifulsoup4 openpyxl scikit-learn scipy
      # 2026-08-15 fusion override (WS:PROPHET-CONDITIONAL-FUSION): the C1 ranker
      # suite rides the step that already owns engine/us_board_rank.py rather than
      # opening a new one — it tests the SAME module's rank authority, and a
      # separate step could go green while the board suite went red on the same
      # change.  Placed immediately after test_us_board_rank.py so a shared-pass
      # regression is the first failure a reader sees.  Its byte-parity class
      # imports scripts/prophet_fusion_race.py at TEST time only (the engine
      # module imports nothing from scripts/, pinned by the suite itself), so no
      # dependency widens: pandas/numpy/pyarrow/pyyaml are already installed here.
      # P1 unrun backfill 2026-08-14: tests/test_us_board_grader_honesty.py joins the
      # step that already owns engine/us_board_rank.py (test_us_board_rank.py) and the
      # track ledgers it grades into.  It was named by no `run:` step in any workflow,
      # so the grader's HONESTY contract — that a published grade may not claim more
      # than the ledger supports — was dark on a publish pipeline.  It rides this
      # existing step (the masking unit: run_ci_pack.py returns on the first non-zero
      # step, while pytest reports every file inside one step) and is appended LAST, so
      # a rank or ledger-emitter regression is the first failure a reader sees rather
      # than the honesty guard failing for a borrowed reason.  No dependency widened —
      # probed at RUN time, it loads only pandas/numpy/pyarrow/pyyaml.
      - name: pick lab runners + US/CN board surfaces — shipped picks, outcome ledgers, priority board
        run: python -m pytest tests/test_b3_forex_consumer.py tests/test_board_contradictions.py tests/test_bottom_ledger.py tests/test_china_board_rank.py tests/test_china_board_rank_v4.py tests/test_china_continuation_watch.py tests/test_china_intel_interest.py tests/test_china_prophet_shadow.py tests/test_cn_board_unified_grid.py tests/test_cn_prophet_candidates_schema.py tests/test_cn_track_ledger_eras.py tests/test_opportunity_timeline.py tests/test_pick_lab_book.py tests/test_pick_lab_cn_runner.py tests/test_pick_lab_render.py tests/test_pick_lab_runner.py tests/test_prophet_card_live_change.py tests/test_prophet_card_shared.py tests/test_prophet_verb_ink_contrast.py tests/test_shadow_book_percentile.py tests/test_stock_board_v2.py tests/test_track_ledger_emitters.py tests/test_us_board_fail_closed_freshness.py tests/test_us_board_lanes.py tests/test_us_board_priority_ui.py tests/test_us_board_rank.py tests/test_us_prophet_fusion.py tests/test_prophet_fusion_w3_structural.py tests/test_us_board_w3_evidence.py tests/test_us_context_vector.py tests/test_us_context_vector_accrual.py tests/test_confluence_veto_leg_telemetry.py tests/test_us_board_grader_honesty.py -q
      # PR-1a (WS:PROPHET-CONDITIONAL-FUSION): the accrual-regression + veto-leg
      # telemetry suites ride the step above (same store cluster); the fusion arena
      # harness gets its own step below — research-tier scaffolding, zero authority,
      # every suite named because this repo has no catch-all pytest collector and
      # audit_unrun_tests reds any suite no run: step names.
      # PR-1b (WS:PROPHET-CONDITIONAL-FUSION): the baseline-race suite joins the PR-1a
      # step rather than opening a new one — same store cluster, same registry, and the
      # race suite's own fixtures import the PR-1a harness, so a registry edit that reds
      # one should red them together in ONE check rather than in two that can disagree.
      # PR-2 (WS:PROPHET-CONDITIONAL-FUSION): the C2/redundancy suite joins the same
      # step for the same reason — it imports the race's signs and the arena's fold/PIT
      # machinery, so the five suites red together on any registry or harness edit.
      # scipy is pinned EXPLICITLY in this step's install line (not inherited through
      # scikit-learn's dependency tree): scripts/prophet_fusion_c2.py imports
      # scipy.optimize for the bounded elastic-net fit and scipy.stats for the t
      # reference, and a scikit-learn drop must not silently take those imports with
      # it (requirements.txt:17 declares scipy a direct dependency and names this
      # module as a consumer).
      - name: prophet conditional-fusion arena harness + evidence-family registry + baseline race + C2/redundancy (PR-1a/PR-1b/PR-2)
        run: python -m pytest tests/test_prophet_fusion_families.py tests/test_prophet_fusion_arena.py tests/test_prophet_fusion_labels.py tests/test_prophet_fusion_race.py tests/test_prophet_fusion_c2.py -q
      # G-D plan-book board read (#5541). Landed as a collecting suite named by
      # no run: step, so audit_unrun_tests.py painted pack-1 red on every PR.
      # It belongs in THIS job with the other prophet/board surfaces: the join
      # it pins is the actionability + enrichment read those cards consume.
      # stdlib + pytest only (engine.prophet_board_read imports no third-party);
      # synthetic LibraryIndex trees under tmp_path, no data/ or site/ reads.
      - name: prophet board read (G-D actionability join + honest absence)
        run: python -m pytest tests/test_prophet_board_read.py -q
      # Prophet candidate "Added date" (prophet-candidate-added-date-fable-e2e,
      # 2026-09-01). engine/prophet_board_since.py's resolver core + market
      # adapters (test_prophet_board_since.py), the pv_card chip
      # (test_prophet_card_added_date.py, real Jinja render via the harness
      # tests/test_prophet_card_live_change.py already established), and the
      # caller-census + freshness-header guards across all five markets
      # (test_prophet_candidate_added_date_surfaces.py). Rides this step —
      # same US/CN board-surface family, same venv (pandas for the CN/HK/CA
      # parquet adapters, jinja2 already installed here for the card render).
      - name: prophet candidate Added-date resolver + card + surfaces
        run: python -m pytest tests/test_prophet_board_since.py tests/test_prophet_card_added_date.py tests/test_prophet_candidate_added_date_surfaces.py tests/test_build_china_library_more_actionable_guard.py -q
      - name: US track-ledger era boundary and frozen pre-era evidence
        run: python -m pytest tests/test_track_ledger_era.py -q
      # Stage-analysis board + its forward ledger and session-stamp heal
      # (forward-ledger audit 2026-08-05).  test_stage_analysis.py and
      # test_weinstein_stage.py were both enumerated in NO workflow
      # (audit_unrun_tests.py P4: unrun, writes a data/ ledger) while the ledger
      # they cover feeds the live stage-ran shelf read and oracle/tape_outcomes'
      # point-in-time reconstruction.  Same US board-store family as the suites
      # above, so it shares their venv.
      - name: stage-analysis ledger session stamps + heal (forward-ledger audit 2026-08-05)
        run: python -m pytest tests/test_stage_analysis.py tests/test_stage_industry_live_partition.py tests/test_weinstein_stage.py tests/test_heal_stage_forward_ledger.py -q
      # Unrun backfill 2026-08-27: tests/test_dashboard_template_render.py — the
      # dashboard.html.j2 rendered-HTML contract — was named by NO `run:` step in any
      # workflow while this job ALREADY claimed the file in its `paths:` block above.
      # That is the worst of both shapes: an edit to the suite re-selected this job,
      # and the job then ran everything EXCEPT the suite.  Because nothing executed it,
      # a genuine red survived on main indefinitely — test_candidate_stage_rail_absent_
      # from_setups_grid has been failing since fd0c0b0ece50 (#6243, shipping #6185)
      # re-added `{% include "_us_board_cards.html.j2" %}`, which renders a DELIBERATE
      # lane rail in the Candidates section BELOW the migrated Setups grid.  The suite's
      # whole-page substring assertion could not tell that restored rail from the grid
      # rail it exists to prove gone — and tests/test_p0_prophet_candidate_board.py:282
      # pins the restored one as REQUIRED, so the two contracts contradicted outright.
      # The assertions are scoped to the grid in the same PR; the enrolment and the
      # repair ship together, per the house rule that a guard nothing runs is already open.
      # It belongs to THIS lane rather than unrun-site-surfaces because the failing
      # case imports `priority_overlay`/`ran_overlay` from tests/test_us_board_priority_ui.py,
      # which the lane's first step already runs — same US board-surface family, same venv.
      # Its own step, not appended to that step's line, so a render-contract red is
      # attributable on sight rather than reading as a picks/boards regression.
      # No dependency widened: it renders Jinja templates only — jinja2 is already on
      # the install line above.  Measured 54s against this job's 245s body and 12m cap.
      - name: dashboard.html.j2 rendered-HTML contract (unrun backfill 2026-08-27)
        run: python -m pytest tests/test_dashboard_template_render.py tests/test_macro_risk_dialog.py -q
  prophet-us-context-and-grades:
    # PROPHET US §W7 — the full-population candidate record: the PIT context store that
    # REMEMBERS each night's score, the forward grader that says what those names then did,
    # and the miss-audit that scores the score. All three ship together and all three were
    # dark before this job: test_us_context_vector.py and test_prophet_miss_audit.py were
    # tier-P1 unwired (scripts/audit_unrun_tests.py census), so the stores' PIT discipline,
    # nightly-lane gates and scorecard null-disclosure had no CI eye on them at all.
    # A gate hung on a suite nothing runs is a gate that is already open.
    if: ${{ false }}
    gate: data
    timeout-minutes: 6
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      # `yfinance` is load-bearing, not decoration, and it is the dependency trap this
      # lane's new suite walked into.  tests/test_us_scan_tier.py calls a helper that
      # does `import collectors.yahoo`, and collectors/yahoo.py imports yfinance
      # unguarded — a dep NEITHER the suite NOR its declared subjects name.  It is
      # invisible to a local run (yfinance is installed here) and invisible to a
      # sys.modules probe against a fixed candidate list, so the first draft of this
      # PR shipped without it and CI answered precisely: 9 failed, 236 passed,
      # ModuleNotFoundError: No module named 'yfinance' — and because run_ci_pack.py
      # returns on the FIRST non-zero step, this job's own later red (the nightly lane
      # placement step, red on main independently) never even ran.
      # WIDENING IS LEGAL HERE.  "Never widen the pip line" is scoped to lanes whose
      # thin env is a DECLARED canary — dataos-foundation's `pytest pyyaml` and the
      # marketing/chart-render thin lanes, each of which says so in a comment.  This
      # job declares no such contract and no comment here turns on a dep being absent.
      # The resulting line is BYTE-IDENTICAL to the one unrun-russell-breadth already
      # declares — the sibling lane that also owns collectors/yahoo.py — so
      # run_ci_pack.py reuses that venv and provisions nothing new.
      - name: install minimal deps
        run: pip install pytest pandas numpy pyarrow pyyaml jinja2 plotly requests beautifulsoup4 openpyxl scikit-learn yfinance
      # P1 unrun backfill 2026-08-14: tests/test_us_scan_tier.py joins the step that
      # already owns engine/prophet_miss_audit.py and engine/us_context_vector.py — two
      # of its own subjects — plus engine/confluence_tiers.py, the tiering it asserts
      # on.  It was named by no `run:` step in any workflow, so scripts/run_us_scan_tier.py
      # and scripts/heal_shallow_caches.py, both on the nightly US scan path, had no CI
      # owner.  Appended LAST so a context-vector or miss-audit regression is the first
      # failure a reader sees rather than the scan tier failing for a borrowed reason;
      # it rides this step because a step is the masking unit (run_ci_pack.py returns on
      # the first non-zero step) while pytest reports every file inside one step.
      # Dependencies: requests, bs4 and lxml are all loaded but OPTIONAL — blocking each
      # individually still leaves the suite 49-passed — and requests + beautifulsoup4
      # are on the install line regardless.  yfinance is the one that is NOT optional;
      # see the install-line comment above for why it had to join and why that is not a
      # canary-lane widening.
      - name: US context vector + full-population grades + miss-audit scorecards
        run: python -m pytest tests/test_us_prophet_grades.py tests/test_us_prophet_w3.py tests/test_us_context_vector.py tests/test_prophet_miss_audit.py tests/test_heal_candidates_archetype.py tests/test_us_scan_tier.py -q
      - name: US entry-status full-population remeasurement
        run: python -m pytest tests/test_us_entry_status_remeasure.py -q
      # The candidate-pool lanes ride this job because they ride this STORE: the nine
      # pool_* columns are appended to the very candidates parquet the suites above
      # guard, and the graduation annotations are a read back out of it. Its
      # TestNoAuthorityLeak is the fence that keeps a display tier out of admission —
      # a static token sweep, an import-closure walk and a behavioural invariance
      # check on prophet_bridge.select_candidates.
      - name: US candidate-pool lanes (lossless partition + store columns + no authority leak)
        run: python -m pytest tests/test_us_candidate_lanes.py -q
      # V4-B1 canonical candidate episodes: pure event contract, source-specific intake,
      # immutable-generation/atomic-HEAD publication, and this workflow/registry authority
      # fence. TURN WATCH's own discovery suite stays in its existing engine owner; B1 reads
      # its sidecar and does not absorb the producer.
      - name: US B1 canonical candidate episodes (contract + intake + reconciler + wiring)
        run: python -m pytest tests/test_us_candidate_episode.py tests/test_us_candidate_episode_intake.py tests/test_us_candidate_episode_reconciler.py tests/test_us_candidate_episode_wiring.py -q
      # W1.5 board-session anchor (#5303/#5307): the earnings-blackout gate and the
      # display-tier chip read the BOARD's session, never the render host's wall
      # clock — hosts straddling local midnight flipped membership (78 vs 81 buy
      # rows at one identical as_of) and the chip countdown. Rides this job because
      # the gate prunes the same candidate pool the lanes above partition, and the
      # suite imports the same scripts.build_stock_library this job already loads.
      - name: US board-session anchor (earnings gate + display chip, host-clock determinism)
        run: python -m pytest tests/test_earnings_blackout_board_anchor.py -q
      # The candidates store this job guards is TRACKED IN A PUBLIC REPO, and
      # context_api.context_frame flattens each dimension's whole `value` dict
      # with no allowlist — so a dimension that grows a paid body ships it to
      # everyone with `git clone`. That is not hypothetical: entitlement-gated
      # Filing Forensics findings for 722 tickers reached
      # data/us_prophet_rank/candidates/2026-07.parquet exactly that way.
      # Runs beside the store's own suites so the containment and the store's
      # PIT discipline cannot drift apart.
      - name: committed-store payload containment (no paid body may be stamped)
        run: python -m pytest tests/test_us_context_vector_payload_containment.py -q
      # The same three modules' NIGHTLY WIRING (2026-08-06): they moved out of the engine
      # job into daily.yml's us_prophet_ledgers lane, so where they run, what they wait on,
      # and whether COLLECT_LANE still arms their forward advance are now facts a workflow
      # edit can silently break. Rides this job because it imports the same modules (and so
      # needs the same venv) and because the suite above owns their in-process contract —
      # the two halves of "does the ledger actually advance" belong under one eye.
      - name: prophet-us nightly lane placement, ledger gate, and failure isolation
        run: python -m pytest tests/test_prophet_off_engine_lane.py -q

  unrun-prophet-learning-loop:
    # Prophet learning loop (#4331) — postmortem forensics over the loser cohort
    # + the exit-policy horse race on the US buy-lane cohort. Same install
    # string as the other unrun-* jobs, so execute_pack shares their venv.
    # cn_prophet_audit / htf_super_tiers / prophet_stage_fusion joined
    # 2026-08-04: the #4331-deferred study half (UNRUN_TEST_CENSUS staging
    # lane) — CN loser/miss telemetry with its frozen reproduction over the
    # COMMITTED board + china_stocks stores, the S1/S2 HTF super-tier engine
    # fields, and the Prophet × Stage-Analysis fusion harness.
    if: ${{ false }}
    gate: data
    timeout-minutes: 5
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install minimal deps
        run: pip install pytest pandas numpy pyarrow pyyaml jinja2 plotly requests beautifulsoup4 openpyxl scikit-learn
      - name: prophet learning loop — postmortem forensics, exit-policy horse race, CN loser audit, HTF tiers, stage fusion
        run: python -m pytest tests/test_prophet_postmortem.py tests/test_exit_policy_study.py tests/test_cn_prophet_audit.py tests/test_htf_super_tiers.py tests/test_prophet_stage_fusion.py -q
      # P-B3 persistence-robust certification (#5729). The suite was named by no
      # run: step, so the unrun-suite gate reds workflow-yaml. It belongs HERE
      # for the reason every research-resident step on signal-contract does: it
      # pins the CN prophet-audit instrument this job already owns
      # (tests/test_cn_prophet_audit.py / engine/cn_prophet_audit.py), one layer
      # down — the frozen P-B3 headline/spell/dwell/regime contract in
      # research/cn_prophet_audit/pb3_persistence_robust_cert.py. Own step so a
      # learning-loop regression is the first failure a reader sees. Synthetic
      # only (numpy frames + source pins); install line already has numpy/pandas.
      - name: research-resident P-B3 persistence-robust certification
        run: python -m pytest tests/test_pb3_persistence_robust_cert.py -q

  unrun-intl-collectors:
    # China/HK collectors and the collect.py sweeps + store-freshness tripwires.
    if: ${{ false }}
    gate: data
    timeout-minutes: 5
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install minimal deps
        run: pip install pytest pandas numpy pyarrow pyyaml jinja2 plotly requests beautifulsoup4 openpyxl scikit-learn
      # P1 unrun backfill 2026-08-14: tests/test_collect_failfast.py joins the step whose
      # own name is "the collect.py sweeps".  It was named by no `run:` step in any
      # workflow, so the FAIL-FAST contract on scripts/collect.py — whether a collector
      # raising mid-sweep stops the sweep or lets it finish and publish a partial store
      # — was dark, together with collectors/base.py and the canada/intl macro
      # collectors it covers.  This job owns those subjects.  Appended LAST so a
      # collector or freshness-tripwire regression is the first failure a reader sees
      # rather than the fail-fast guard failing for a borrowed reason.  It rides this
      # existing step because a step is the masking unit (run_ci_pack.py returns on the
      # first non-zero step, so a later step goes dark behind an earlier red) while
      # pytest reports every file inside one step, and adds no job to a manifest at the
      # narrow-diff ceiling.  No dependency widened: probed at RUN time it loads
      # pandas/numpy/pyarrow/pyyaml/requests, all already on the install line above.
      - name: china/HK collectors and the collect.py sweeps + store-freshness tripwires
        run: python -m pytest tests/test_action_board_lane_split.py tests/test_china_alpha_w3c_infra.py tests/test_china_fundamentals_collector.py tests/test_content_stamp_freshness.py tests/test_data_quality_audit.py tests/test_build_earnings_scores_from_history.py tests/test_earnings_qual.py tests/test_earnings_seasons.py tests/test_earnings_w4_feed.py tests/test_earnings_w5.py tests/test_hk_fundamentals_collector.py tests/test_hk_narrative.py tests/test_hk_track_record_vm.py tests/test_housing_hf.py tests/test_collect_failfast.py -q

  unrun-russell-breadth:
    # the Russell-2000 breadth collector — the one P1 suite needing a wheel the shared set lacks.
    if: ${{ false }}
    gate: data
    timeout-minutes: 4
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install minimal deps
        run: pip install pytest pandas numpy pyarrow pyyaml jinja2 plotly requests beautifulsoup4 openpyxl scikit-learn yfinance
      - name: the Russell-2000 breadth collector — a P1 suite needing a wheel the shared set lacks
        run: python -m pytest tests/test_russell_breadth.py -q
      # Enumerated in NO workflow before 2026-08-06 (the 2026-08-03 store-freshness
      # program shipped all four of its suites dark — see the sibling note in
      # unrun-data-plane). This one belongs HERE rather than there for two reasons
      # that agree: collectors/breadth.py imports yfinance at module scope, so it
      # ERRORS at collection under the shared unrun-* install; and this lane already
      # owns the breadth collector as its subject, which is half of what the suite
      # covers (collectors/breadth.py's current-constituent stale-column disclosure,
      # plus collectors/yahoo.py's requested-vs-returned reconciliation). Adding the
      # wheel to unrun-data-plane instead would have split that job off the shared
      # venv for all twenty-one unrun-* jobs.
      - name: collector never-silent disclosure — yahoo reconciliation + breadth stale columns
        run: python -m pytest tests/test_collector_freshness_disclosure.py -q
      - name: off-render Yahoo universe backfill contract
        run: python -m pytest tests/test_yahoo_universe_backfill.py -q
      # Unrun backfill 2026-08-19 (audit_unrun_tests.py, #5942 sibling): the
      # NA-ticker pandas-sentinel guard. pandas decodes the bare strings
      # NA/NULL/NONE/NAN/N-A/NaN to NaN by default, and 'NA' is a live ticker on
      # both sides (Nano Labs Ltd, Nasdaq; National Bank of Canada, TSX) — every
      # read_csv/read_excel over a ticker column silently dropped those rows
      # with adapter status still `ok`. Pins the keep_default_na=False +
      # na_values=[""] guard (an AST call-site census — pure ast.parse, no
      # import) across all 18 parse sites in the nine collector files this
      # fixed, plus the is_non_equity_holding() truth table (collectors/
      # holdings.py, Part B of the fix) and the documented regression a BARE
      # keep_default_na=False would reintroduce (numeric columns silently
      # turning to dtype str).
      #
      # Lives HERE, not on the ETF lane its subject reads closest to
      # (unrun-scoring-engine), for the same reason test_collector_freshness_
      # disclosure sits in THIS job two steps up: one of its behavioural
      # round-trips imports collectors.canada_universe (_to_ticker), and that
      # module's `import yfinance as yf` is module-scope — it ERRORS at
      # collection under the shared unrun-* install (verified: this exact
      # placement in unrun-scoring-engine failed CI on PR #5985 with
      # `ModuleNotFoundError: No module named 'yfinance'`, even though it
      # passed locally where yfinance happened to already be installed). This
      # lane already declares the yfinance wheel and already owns a
      # yfinance-import-at-collection suite, so it costs nothing new.
      - name: NA-ticker pandas-sentinel guard — 18 parse sites + is_non_equity_holding truth table
        run: python -m pytest tests/test_collectors_na_sentinel.py -q

  unrun-brain-gateway:
    # the Mastermind brain gateway — routing, quota ledger, tool allowlist, SSE order.
    # Its own install: the FastAPI route tests need fastapi + starlette's TestClient
    # (httpx). Kept off the shared unrun-* line so the other twenty jobs still share
    # one venv; execute_pack sorts by install string, so this costs one extra venv.
    if: ${{ false }}
    gate: code
    timeout-minutes: 5
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install minimal deps
        run: pip install pytest pandas numpy pyarrow pyyaml jsonschema jinja2 plotly requests beautifulsoup4 openpyxl scikit-learn fastapi httpx
      - name: brain gateway — lane routing, quota ledger, tool allowlist, SSE event order
        run: python -m pytest tests/test_brain_gateway.py tests/test_brain_gateway_quota_safety.py tests/test_brain_streaming.py tests/test_brain_tool_economics.py tests/test_brain_instant_lane.py tests/test_intelligence_workspace_context_compiler.py tests/test_mm_brain_asset.py tests/test_brain_exact_source_context.py tests/test_company_source_span.py -q
      - name: W1-A typed datapoint registry — owners, PIT, rights, consumers, mutations, batching
        run: >-
          python -m pytest
          tests/test_datapoint_registry.py
          tests/test_quote_resolution.py
          tests/test_intelligence_workspace_identity_market.py
          tests/test_intelligence_workspace_owner_adapters.py
          tests/test_intelligence_workspace_consumers.py
          tests/test_intelligence_workspace_mutations.py
          tests/test_intelligence_workspace_cli.py
          tests/test_intelligence_workspace_performance.py
          tests/test_intelligence_workspace_workspace_layout.py -q

  unrun-macro-panels:
    # commodities / forex / rates / news / policy panels and their calibration guards.
    if: ${{ false }}
    gate: data
    timeout-minutes: 7
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install minimal deps
        run: pip install pytest pandas numpy pyarrow pyyaml jinja2 plotly requests beautifulsoup4 openpyxl scikit-learn
      # test_factor_universe_hold.py (2026-08-06) — the DNR:HOLD-FACTOR-UNIVERSE-WIDENING
      # tripwire. engine/equity_factors.py was strictly dark: no `run:` step named its
      # suite and no ci.yml path named the module, so the board tiebreak's own source
      # could not start CI. Needs only the deps already installed here.
      # P1 unrun backfill 2026-08-14: tests/test_commodities_ignition_copy.py joins the
      # step that already owns scripts/build_commodities.py — its only subject — via
      # test_commodity_mtf_verdict_honesty.py.  It was named by no `run:` step in any
      # workflow, so the ignition COPY on the commodities panel was dark while the
      # verdict honesty guard beside it was covered; both police the same published
      # panel, one the number and one the words.  It rides this existing step because a
      # step is the masking unit (run_ci_pack.py returns on the first non-zero step)
      # while pytest reports every file inside one step, and adds no job to a manifest
      # at the narrow-diff ceiling.  Appended LAST so a panel or calibration regression
      # is the first failure a reader sees rather than the copy guard failing for a
      # borrowed reason.  No dependency widened: probed at RUN time it loads only
      # pandas/numpy/pyarrow/pyyaml, all already on the install line above.
      - name: commodities / forex / rates / news / policy panels and their calibration guards
        run: python -m pytest tests/test_factor_universe_hold.py tests/test_build_dispersion_regime.py tests/test_build_factor_panel.py tests/test_commodity_mtf_verdict_honesty.py tests/test_congress_score.py tests/test_context_risk.py tests/test_cross_asset_context.py tests/test_crossasset_flows.py tests/test_financial_news.py tests/test_forex.py tests/test_forex_context_bus.py tests/test_news_event_ledger.py tests/test_phase_buildout.py tests/test_policy_lever.py tests/test_rates_command.py tests/test_yield_momentum.py tests/test_inputs_yield_curve.py tests/test_reflexivity_overlay.py tests/test_scorecard_incremental.py tests/test_us_sector_rotation_wiring.py tests/test_validation.py tests/test_whitehouse_build.py tests/test_commodities_ignition_copy.py -q

  unrun-market-plumbing:
    # live quotes / tape / GEX / options-flow / theme + tech-lab feeds behind the intraday lanes.
    if: ${{ false }}
    gate: data
    # 6 -> 15. This job is STRUCTURALLY at-cap, not slowed by any change. Measured on a
    # fast Mac Studio, its FIRST step alone (27 test files, 941 passed) runs 3:40 — 61% of
    # a 6-minute budget that must ALSO cover `pip install ... scikit-learn` and seven more
    # pytest steps. On the 4-core-bound, fleet-shared ubuntu-latest pack runner it crosses
    # the cap whenever the pool is busy, so "timed out after 6 minutes" has been recurring
    # on main and on every PR that inherits it — an unattributable red that repeatedly
    # reads as the running PR's regression (it was pinned on #4883's proof run, where the
    # PR's own step was 6.7s of the budget and its data change made the store 37% smaller).
    # 15 is this manifest's existing ceiling for heavy jobs (options-estate-guards,
    # collector-registry), both of which carry FEWER run steps than the eight here.
    timeout-minutes: 15
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install minimal deps
        run: pip install pytest pandas numpy pyarrow pyyaml jinja2 plotly requests beautifulsoup4 openpyxl scikit-learn
      # P1 unrun backfill 2026-08-14 — three consumers of plumbing this step already
      # owns joined the list, appended rather than given a step of their own because a
      # step is the masking unit here (run_ci_pack.py returns on the first non-zero
      # step, so a later step goes dark behind an earlier red) while pytest reports
      # EVERY file inside ONE step.  Extending the list adds coverage without adding a
      # place to go dark, and adds no job to a manifest already at the narrow-diff
      # ceiling.  Placement is deliberate: all three sit AFTER the contract suites they
      # depend on, so an upstream regression is the first failure a reader sees rather
      # than a consumer failing for a borrowed reason.
      #   * test_chain_session_hole_disclosure.py — reads collectors/cboe.py, so it is
      #     downstream of test_cboe_chain_coverage.py, and of test_validate_gex.py for
      #     the salvage path (scripts/ci/chain_snapshot_salvage.py is owned by no other
      #     job in this manifest).
      #   * test_prophet_arena_clock_parity.py / test_prophet_plan_clock.py — the arena
      #     and plan CLOCKS over engine/prophet_bridge.py, so both land downstream of
      #     test_prophet_bridge.py and test_prophet_bridge_order_invariance.py above.
      #     This lane is their home because engine/prophet_management.py — a subject of
      #     the plan-clock suite — is owned by NO other job, and this same lane already
      #     owns prophet_bridge, prophet_arena and build_prophet.
      # No dependency widened — measured per suite at RUN time, and measured twice.
      # The first pass probed sys.modules against a FIXED candidate list, which cannot
      # report a dep it was never asked about; it was replaced by an enumeration of
      # every loaded pip distribution (importlib.metadata.packages_distributions) plus
      # a control-tested import blocker (MetaPathFinder.find_spec — the find_module
      # protocol it replaced was REMOVED in 3.12 and silently blocked nothing).
      # Result: the two prophet clock suites need only pandas/numpy/pyarrow/pyyaml, and
      # test_chain_session_hole_disclosure additionally REQUIRES requests — blocking it
      # gives 3 failed, 7 passed.  requests is already on the install line above, so
      # nothing here changes; the distinction is recorded because "needs only pandas"
      # is exactly the kind of claim a future thinning of this lane would trust.
      # This is the same class of trap that cost prophet-us-context-and-grades a red in
      # the sibling PR of this batch: collectors/yahoo.py imports yfinance unguarded,
      # so a suite that merely touches it needs a dep neither it nor its subject names.
      #
      # P1 unrun backfill 2026-08-14, cont. — lifecycle stays here (#5682).
      # Overtime (test_prophet_overtime_horizon_reconciliation.py) is owned by
      # signal-contract (#5540); do not re-name it on this run line.
      #   * test_prophet_lifecycle_state.py — the SAME ruling's §6/§9 deliverable
      #     (PR-0(c), #5506), shipped unwired the same way one wave later.  Same owner on
      #     the same discriminator: it imports scripts/build_prophet.py and
      #     engine/prophet_management._VALID_PHASES, and of the wired suites importing
      #     engine.prophet_management this lane still runs every one.  It is the
      #     NO-DEAD-CELL census — it reds when a cell claims a phase the management
      #     engine no longer produces — so leaving it dark is precisely the defect the
      #     ruling exists to close (a vocabulary value with no producer) wearing the
      #     shape of an unrun guard.  66 tests, ~4s, no network and no data/ read.
      # Owner chosen on subject evidence, not on the family in the filename.  Its import
      # triple is scripts/build_prophet.py + engine/prophet_bridge.py +
      # engine/prophet_management.py, and this lane owns all three: of the 14 WIRED
      # suites importing scripts.build_prophet it runs 7 (the plurality — next is
      # prophet-anticipation-intake at 3), and of the 3 wired suites importing
      # engine.prophet_management it runs 3 of 3, the other candidates none.
      # engine/prophet_management.py is the discriminator: it is owned by NO other job,
      # exactly as the plan-clock note above records.
      # Placement is downstream of every contract it consumes — test_prophet_bridge.py
      # and test_prophet_bridge_order_invariance.py (the bridge), then
      # test_prophet_plan_clock.py (the plan clock whose compute_management_state this
      # suite calls) — so an upstream regression is the first failure a reader sees
      # rather than this consumer failing for a borrowed reason.
      # Lifecycle dependency evidence was measured at RUN time, not collection:
      # packages_distributions over sys.modules plus a MetaPathFinder.find_spec blocker.
      # The blocker controls are live — pandas and numpy each error collection — while
      # the broad optional group and pyarrow leave all 66 tests green.  PyYAML is truly
      # required: blocking it fails the two late us_early_turn tests through lib.config.
      # Every required distribution is already on the unchanged install line above.
      # Full checkout verified 66 passed with no skips.  This job declares no scope key,
      # so it is not exclusive and needs no paths import-closure declaration.
      - name: live quotes / tape / GEX / options-flow / theme + tech-lab feeds behind the intraday lanes
        run: python -m pytest tests/test_attention_deterministic.py tests/test_build_live_quotes.py tests/test_cboe_chain_coverage.py tests/test_clinicaltrials_themes.py tests/test_daily_engine_setup_retry.py tests/test_event_priors.py tests/test_foresight_ledger_lane_gates.py tests/test_gex_archive_snapshot.py tests/test_grade_cortex_attention.py tests/test_grade_factor_attention.py tests/test_grade_thematic.py tests/test_live_delay_labeling.py tests/test_options_flow_attention_reflex.py tests/test_prophet_bridge.py tests/test_prophet_bridge_order_invariance.py tests/test_prophet_durable_checkpoint.py tests/test_prophet_earnings_split_brain.py tests/test_prophet_stage_shadow.py tests/test_prophet_stage_tilt.py tests/test_prophet_w1_intake_repair.py tests/test_tape_flow.py tests/test_tape_flow_stamp.py tests/test_tech_events_export.py tests/test_techlab_p0_fixes.py tests/test_thematic_state.py tests/test_theme_adoption.py tests/test_theme_clinical.py tests/test_theme_options_witness.py tests/test_theme_trade_flows.py tests/test_thetadata_resolver.py tests/test_validate_gex.py tests/test_chain_session_hole_disclosure.py tests/test_prophet_arena_clock_parity.py tests/test_prophet_plan_clock.py tests/test_prophet_lifecycle_state.py -q
      # Daily engine setup-race recovery (2026-08-09): GitHub delivered engine and
      # factor_panel to one busy self-hosted runner one second apart; the runner
      # cancelled engine before checkout and the tail kept going.  The two suites
      # above pin both halves of the repair: the existing immediate Prophet
      # checkpoint once engine runs, and the one-shot setup-only job rerun when it
      # never starts.  They need only pytest + PyYAML already installed here.
      # Prophet US W1 intake repair (2026-08-03), added to the run step above:
      #   * test_prophet_w1_intake_repair.py — the priority-score sort key, the
      #     active-plan re-origination block, and the index aging fields.
      #   * test_prophet_bridge_order_invariance.py — was strictly dark (named by no
      #     `run:` step in any workflow) while guarding the very sort key W1 rewrote.
      #     A determinism guard nothing executes is not a guard.
      # 61 + 12 tests, ~2 s serial; both need only the deps already installed here.
      #
      # R0-C earnings split-brain fence (2026-08-06), added to the run step above:
      #   * test_prophet_earnings_split_brain.py — pins that no production module
      #     imports the research backtest harness engine/prophet_stage_fusion.py, that
      #     the legacy earnings parquet reaches no plan field outside the ratified
      #     hold-horizon leash, and that an ABSENT earnings source is disclosed rather
      #     than degrading silently into an ordinary negative.
      #   * test_prophet_stage_shadow.py — was strictly dark (named by no `run:` step
      #     in any workflow) while guarding the forward shadow that ARMS the leash's
      #     auto-demote clause. R0-C adds that shadow's starvation disclosure, so the
      #     suite is wired here rather than shipping a new guard nothing executes.
      # 16 + 12 tests, ~15 s serial; both need only the deps already installed here.
      #
      # cboe chain-coverage tripwire (2026-08-07), added to the run step above:
      #   * test_cboe_chain_coverage.py — was strictly dark (named by no `run:` step in
      #     any workflow) while guarding the ::warning that tells an operator how to
      #     cross-fill a lost gex_<name> session from data/polygon_gex. That remedy text
      #     had gone FALSE: it promised the polygon row "usually sits in the row stamped
      #     session+1", which #4807 (2026-08-07) ended for every row written after it.
      #     An operator following it would copy the wrong session's gamma into an
      #     authority-adjacent store — the exact off-by-one the message's own last clause
      #     warns against. The text is corrected to pin the session on the spot-vs-yahoo
      #     identity check instead of any offset; this wires the suite that holds it so
      #     the corrected wording is defended rather than merely written.
      # 26 tests, ~20 s serial; hermetic (every store read is redirected to tmp_path) and
      # needs only the deps already installed here.
      #
      # GEX modeling-core wiring (2026-07-29). Three suites, 42 tests, ~3 s serial.
      #
      # These were the three remaining strictly-dark suites in the GEX family — named by
      # no `run:` step in any workflow AND matched by no ci.yml path pattern, so no
      # possible edit could even start CI for them: the walls/surface/smile/term model
      # layer, the finite-difference-verified greeks + dealer-sign engine math, and the
      # Polygon per-strike chain accrual every options read now depends on. The OIP E3
      # wave wired its own three and flagged this family as the follow-up; this closes it.
      #
      # Import closure verified empirically against THIS job's install line: with scipy,
      # sklearn, jinja2, plotly, requests, bs4, openpyxl, statsmodels, matplotlib and
      # fastapi ALL blocked via a builtins.__import__ shim, the only miss is `requests` —
      # it enters at module scope through collectors/base, which the accrual collector
      # rides. pytest + pandas + numpy + pyarrow + pyyaml + requests is the full closure;
      # all six sit in the line above, so the shared venv is unchanged, and the other
      # nine blocked wheels are confirmed unused. All 42 green on main at wiring time.
      #
      # polygon_gex SESSION STAMPING (2026-08-06). Wired at birth, into this step because
      # its import closure is identical (pytest + pandas + numpy + pyarrow + requests via
      # collectors/base) and it guards the same accrual runner. It pins: the stamp is the
      # SESSION the snapshot describes and never the UTC run date; a Friday-evening
      # accrual (a UTC-Saturday instant) is STORED rather than refused — the shape that
      # permanently lost session 2026-07-31; first-writer-wins; and the migrated store's
      # standing invariant that every chains stamp and every summary row is an NYSE
      # session. Reads the committed store, so it also fails if a future writer regresses.
      - name: GEX modeling core — walls/surface model, greeks + dealer-sign engine, polygon per-strike accrual
        run: python -m pytest tests/test_gex_model.py tests/test_gex_engine.py tests/test_polygon_gex.py tests/test_polygon_gex_session_stamps.py -q
      # MSC R2.4 (2026-08-01): the live Level Report Card publisher. Wired at birth —
      # pure pandas/pytest, same import closure as the step above (pandas+pyarrow only).
      # R2.4b (2026-08-02) adds the nulls/index-lane suite AND back-wires the two
      # WP-C1 grader suites that were never CI-named (the named-test-files trap).
      - name: Level Report Card — grade-summary publisher (Wilson CI vs null, coverage honesty)
        run: python -m pytest tests/test_level_grades_summary.py tests/test_levels_grade_nulls.py tests/test_levels_grade.py tests/test_levels_track_record_rebase.py -q
      # Wired 2026-08-06: the ENGINE the four grader suites above grade, and the only
      # one of the family named by no run: step. Red on main — three tests still
      # asserted the pre-retirement contract where an absent gamma_flip was
      # interpolated out of by_strike. `_flip_from_rows` is retired (a flip is the
      # spot at which the RE-PRICED book has zero net dealer gamma, which the ladder
      # cannot express — charting-app gamma-flip-defect-rca.md), so the engine renders
      # an honest null. Nothing went red because nothing ran it. Retargeted to pin the
      # retirement, which is what keeps a fabricated flip from coming back.
      # Same import closure as the step above (engine/levels_engine.py, pytest only).
      - name: Levels engine — anchor/walls/clusters and the retired gamma-flip reconstruction
        run: python -m pytest tests/test_levels_engine.py -q
      # MSC R3.2/R3.3 (2026-08-02): the cross-root gex_state _index.json aggregator
      # (lib/gex_state_index.py, called from build_gex_board). Wired at birth — pure
      # stdlib json/pathlib, no new wheels.
      - name: gex_state cross-root index — glance-tier aggregate (never-blank, field subset)
        run: python -m pytest tests/test_gex_state_index.py -q
      # OIP E3 positioning persistence (2026-07-29). Three suites, ~11 s serial.
      #
      # test_gex_state.py and test_index_gex_history.py were BOTH dark: named by no
      # `run:` step in any of the workflows, so the gex_state emitter and the index
      # dealer-gamma reconstruction had no CI at all. This wave edits both, so leaving
      # them unwired would ship the edits unverified — the unrun-suite rot class.
      # test_positioning_persistence.py is new with the wave and is wired at birth.
      #
      # Import closure verified against THIS job's install line with scipy, sklearn,
      # jinja2, plotly, requests, bs4, openpyxl, statsmodels, matplotlib and fastapi all
      # blocked: pytest + pandas + numpy + pyarrow + pyyaml is sufficient. engine/
      # gex_state.py deliberately keeps a pandas-free import (the positioning reads are
      # lazy-imported inside the functions), and that property is itself asserted.
      #
      # The three remaining dark suites in this family that this wave flagged as the
      # follow-up (the gex model layer, the gex engine math, and the polygon accrual
      # collector) are now wired by the GEX modeling-core step directly above, which
      # landed on main before this branch rebased. Nothing in the family is dark.
      - name: OIP E3 positioning persistence — oi_delta clusters, wall persistence, index-GEX rebuild routing
        run: python -m pytest tests/test_positioning_persistence.py tests/test_gex_state.py tests/test_index_gex_history.py -q
      # Unrun backfill 2026-08-03: the GEX board's per-ticker state merge and
      # the gex.js IV-rank band color law join the family this lane already
      # owns; the OIP E1 session digest and the flow desk builder are the same
      # market-plane plumbing (live_flow / etf_flows organs).
      # P1 unrun backfill 2026-08-14: the board's SESSION-DATE contract joins the step
      # that already owns the board's state merge.  Placed last in the list so a merge
      # regression (test_gex_board_state_merge.py) is the first failure a reader sees —
      # the session-date suite reads lib/gex_state_index.py and scripts/build_gex_board.py
      # on top of that merge, so it would otherwise fail for a borrowed reason.
      - name: gex board state merge + IV-rank band color + session digest + flow desk
        run: python -m pytest tests/test_gex_board_state_merge.py tests/test_gex_js_iv_rank_color.py tests/test_session_digest.py tests/test_flow_desk.py tests/test_gex_board_session_date.py -q

  unrun-publish-ops:
    # publish/transport, asset pipeline, ticker pages and the nightly ops tripwires.
    if: ${{ false }}
    gate: data
    timeout-minutes: 6
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install minimal deps
        # test_earnings_api.py exercises the mounted FastAPI route through
        # TestClient; without both wheels pytest importorskips the whole suite.
        run: pip install pytest pandas numpy pyarrow pyyaml jinja2 plotly requests beautifulsoup4 openpyxl scikit-learn fastapi httpx
      # P1 unrun backfill 2026-08-14: tests/test_stocks_hub.py joins the step that
      # already owns scripts/build_ticker_pages.py — its own pipeline subject — via
      # test_ticker_pages.py and test_ticker_dossier_render_lane.py.  It was named by no
      # `run:` step in any workflow, so engine/stocks_hub.py and engine/market_heatmap.py,
      # the hub the ticker pages hang off, were dark while the pages themselves were
      # covered.  Appended LAST so a ticker-page or publish-transport regression is the
      # first failure a reader sees rather than the hub failing for a borrowed reason.
      # It rides this existing step because a step is the masking unit (run_ci_pack.py
      # returns on the first non-zero step) while pytest reports every file inside one
      # step, and adds no job to a manifest at the narrow-diff ceiling.  No dependency
      # widened: probed at RUN time it loads pandas/numpy/pyarrow/pyyaml — all on the
      # install line above — plus PIL transitively, which blocking leaves 42-passed.
      - name: publish/transport, asset pipeline, ticker pages and the nightly ops tripwires
        run: python -m pytest tests/test_backfill_qledger_us.py tests/test_check_price_store_freshness.py tests/test_demand_chain_phase0.py tests/test_earnings_api.py tests/test_earnings_evidence_graph_deps.py tests/test_earnings_private_store.py tests/test_earnings_prompt_quality.py tests/test_earnings_score_quality.py tests/test_earnings_public_wire.py tests/test_earnings_wire_freshness.py tests/test_earnings_worker_launchd.py tests/test_earnings_worker_terminal.py tests/test_envelope_conformance.py tests/test_importance_v0.py tests/test_oracle_contract.py tests/test_publish_r2.py tests/test_public_chrome.py tests/test_radar_single_writer.py tests/test_refresh_regime_if_stale.py tests/test_research_factory_monitor.py tests/test_seo_foundation.py tests/test_signal_contracts_wiring.py tests/test_signal_sanity.py tests/test_ticker_dossier_render_lane.py tests/test_ticker_pages.py tests/test_ts_u1_session_date.py tests/test_w4_pit_fixes.py tests/test_w6b_push_ops.py tests/test_watchlist_sentinel.py tests/test_world_state_migrations.py tests/test_stocks_hub.py -q

  unrun-inline-js-guard:
    # the inline-JS deploy guard's own unit tests (node --check round-trips; ~3.5 min alone).
    if: ${{ false }}
    gate: code
    timeout-minutes: 3
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install minimal deps
        run: pip install pytest pandas numpy pyarrow pyyaml jinja2 plotly requests beautifulsoup4 openpyxl scikit-learn
      - name: node must be on PATH — these tests SKIP without it (vacuous green)
        run: node --version
      - name: the inline-JS deploy guard's own unit tests (node --check round-trips; ~3.5 min alone)
        run: python -m pytest tests/test_check_inline_js.py -q --deselect tests/test_check_inline_js.py::test_real_site_is_clean --deselect tests/test_check_inline_js.py::test_real_tree_handlers_and_curly_clean

  unrun-import-hygiene:
    # import-time process-global state (logging.disable / warnings filters) across
    # engine, lib, scripts, research.  Both are process-global: a module-level
    # silencer mutes warnings for anything that merely IMPORTS the file — pytest
    # collection, a guarded engine import, a sibling research harness — which is how
    # the #1115 order-dependent flake happened.  The suite AST-parses all four trees,
    # so it costs ~48s wall on its own; 5 minutes leaves headroom on a cold runner.
    if: ${{ false }}
    gate: code
    timeout-minutes: 5
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install minimal deps
        run: pip install pytest pandas numpy pyarrow pyyaml jinja2 plotly requests beautifulsoup4 openpyxl scikit-learn
      - name: import-time logging.disable + warnings-filter ratchet over engine, lib, scripts, research
        run: python -m pytest tests/test_no_module_level_logging_disable.py -q
      - name: file-path entry scripts pin this repository before importing it
        run: python -m pytest tests/test_check_script_import_pinning.py -q

  price-basis-census:
    # PRICE-BASIS guard: no production grader may difference an UNADJUSTED name leg against
    # an ADJUSTED benchmark leg (a name's own dividend books as a loss).  An AST walk over
    # engine and scripts holding a FROZEN registry of every module that pairs the two
    # families, fail-closed in BOTH directions — a new pairing fails, and so does a
    # registry line that no longer describes anything.
    #
    # STANDS ALONE ON PURPOSE.  #4715 shipped this suite wired into no workflow, and one
    # day later pristine main already carried three drifts: two unregistered pairings and
    # one registered module the detector had gone blind to when the tier-merging cache
    # accessor moved into lib/closes_panel.py (#4711) — read as "this module was fixed"
    # when nothing had been fixed.  Giving it its own job is the fix for the ROT, not just
    # for the drift: run_ci_pack returns on a job's first failing step, so a step appended
    # to a shared job stops running the moment any earlier suite in that job goes red — a
    # second, quieter way to be dark, and both jobs this could have ridden are red on main
    # today.  A pack continues past a failing JOB, so a job boundary is what actually keeps
    # this eye open.  Same install line as its neighbours, so it shares their venv.
    if: ${{ false }}
    gate: code
    timeout-minutes: 6
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install minimal deps
        run: pip install pytest pandas numpy pyarrow pyyaml jinja2 plotly requests beautifulsoup4 openpyxl scikit-learn
      - name: "price-basis census: raw name leg vs adjusted bench (frozen registry, both directions)"
        run: python -m pytest tests/test_price_basis_graders.py -q

  # ── RED-ON-ARRIVAL SWEEP, BATCH A (2026-07-27) ─────────────────────────────
  # The 2026-07-26 census wired the P0–P3 tiers but left P4/P5 (861 suites) as
  # declared backlog, so nobody had measured how many of them were already RED.
  # This lane is the first answer: 870 unrun suites were executed one process per
  # suite, 24 came back red, and these four are the scoring-engine share of that
  # set (docs/UNRUN_TEST_CENSUS.md §"Red on arrival — the P4/P5 sweep").
  #
  # Two of the four were worse than red — they were NON-HERMETIC.  A migration
  # that adds a preferred data source in front of an existing one silently strands
  # the old monkeypatch on a dead branch, so the test stops testing its fixture and
  # starts asserting against the committed production store; its verdict then moves
  # with the market instead of with the code.  Both now pin the branch they
  # exercise, and both gained the coverage the migration's NEW branch never had.
  # Repeats the shared dependency install BYTE-FOR-BYTE so run_ci_pack.py keeps one
  # venv across the unrun-* set.  Measured locally: 4 suites / 45 tests / ~43s,
  # dominated by test_sector_central's compute() over the real store (~40s).
  unrun-scoring-engine:
    if: ${{ false }}
    gate: code
    timeout-minutes: 6
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install minimal deps
        # pandas.read_html is exercised by the ETF sponsor-registry parser;
        # pandas alone does not provide an HTML parser on a clean runner.
        run: pip install pytest pandas numpy pyarrow pyyaml jinja2 plotly requests beautifulsoup4 openpyxl scikit-learn lxml
      - name: spotlight tilt — W9-B US demotion is display-only + the tilt still moves weighted markets
        run: python -m pytest tests/test_spotlight.py -q
      - name: etf_pulse sector leg + analyst delta channel — each pins the data branch it exercises
        run: python -m pytest tests/test_etf_pulse.py tests/test_fix43_analyst_and_whitehouse.py -q
      # test_etf_board ran in ZERO jobs until 2026-08-03 — the etfs.html hero
      # tile's zh leak (bare label_en RISK-ON on an indexed bilingual page) had
      # its natural pin in a suite CI never executed. The suite needs numpy (via
      # engine.etf_pulse constants) + jinja2 for its end-to-end zh render; both
      # are already in this job's venv line.
      - name: etf board synthesis — bilingual backdrop tile + pulse-dated rotation (zh-leak / §B9.6 pins)
        run: python -m pytest tests/test_etf_board.py -q
      # W1 flow-vs-selection (2026-08-12): the decomposition identity plus every
      # robustness guard standing between a raw sponsor file and a published
      # dollar figure — split / duplicate-day / weight-sum / cadence / exit
      # continuity, each with its own adversarial fixture. Registered on arrival
      # rather than added to the unrun census. Needs pandas+pyarrow (parquet
      # fixtures) and imports scripts.build_site for the payload wiring; this
      # job's venv line already carries both.
      - name: etf flow/selection decomposition — identity, $ estimates, robustness guards
        run: python -m pytest tests/test_etf_flow_decomposition.py -q
      # W2a/W3 companions (2026-08-12), registered on arrival — this manifest is
      # explicit-file (no pytest glob), so an unregistered suite runs in ZERO jobs
      # (see the test_etf_board note above). Registry needs pyyaml; the ledger
      # suite needs pandas+pyarrow and pins the nightly lane gate + price freeze;
      # the windows tile renders measurement.html.j2 (jinja2). All deps already
      # on this job's venv line.
      - name: etf fund registry — universe coverage, type enum, sponsor drift pin
        run: python -m pytest tests/test_etf_registry.py -q
      - name: etf board ledger — lane gate, idempotency, frozen-price grading
        run: python -m pytest tests/test_etf_board_ledger.py -q
      - name: etf board windows tile — three states, zh parity, vocab
        run: python -m pytest tests/test_etf_board_windows_tile.py -q
      - name: etf weighting lens — discount identity, golden default ordering, receipts
        run: python -m pytest tests/test_etf_weighting.py -q
      # W3 partial-NAV sleeve support: pins the declared nav_equity_frac bounds,
      # both ranked-consumer call sites, and the conservative sweep/swap filters.
      # Uses pandas+pyarrow for the on-disk sponsor receipt; both are above.
      - name: etf partial-NAV sleeve — declared bounds and sweep/swap guards
        run: python -m pytest tests/test_etf_partial_nav_sleeve.py -q
      - name: sector_central board order — fast-lens rotation rank, failing open to conviction
        run: python -m pytest tests/test_sector_central.py -q

  # ── RED-ON-ARRIVAL SWEEP, BATCH B (2026-07-27) ─────────────────────────────
  # The template-surface share of the 10 reds (docs/UNRUN_TEST_CENSUS.md §"Red on
  # arrival — the P4/P5 sweep").  All three slice shipped markup out of templates/
  # and render it, so they rot whenever copy is rewritten — and the house style is
  # to rewrite copy into plain words (the jargon ban in DESIGN_DOCTRINE.md).  Each
  # now pins the load-bearing NOUN or the honesty invariant rather than a sentence.
  #
  # The sweep also caught a VACUOUS GREEN here, which is the more dangerous half:
  # three of test_w5b_edge_chips' absence tests asserted `"Sleeve ×" not in html`,
  # and a rename made that trivially true — they passed while no longer able to
  # catch a chip leaking into the wrong mode or surviving a None radar_state.  A
  # renamed sentinel silently disarms every absence assertion keyed to the old one;
  # when a presence marker changes, re-point the absence tests in the same edit.
  # Repeats the shared dependency install BYTE-FOR-BYTE so run_ci_pack.py keeps one
  # venv across the unrun-* set.  The macro-header suite also keeps the glance
  # layer compact while preserving the click-through detail affordances.
  unrun-template-chips:
    if: ${{ false }}
    gate: code
    timeout-minutes: 4
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install minimal deps
        run: pip install pytest pandas numpy pyarrow pyyaml jinja2 plotly requests beautifulsoup4 openpyxl scikit-learn
      - name: Macro header — compact glance layer with on-demand detail
        run: python -m pytest tests/test_macro_header_compact.py -q
      - name: OKX retail chip — bilingual + the contrarian framing survives copy edits
        run: python -m pytest tests/test_okx_retail.py -q
      - name: CN risk-backdrop chip — renders, degrades, and stays out of macro mode
        run: python -m pytest tests/test_w5b_edge_chips.py -q
      # Unrun backfill 2026-08-19 (audit_unrun_tests.py, #5952-class): the China
      # Special Situations desk's four truth contracts, shipped by #5975 and
      # extended by #5996, were named by no run: step in any workflow. Engine and
      # templates are FROZEN there — this suite only pins the already-shipped
      # contract so a regression cannot return silently: goodwill units live in
      # the field name and are never re-derived from magnitude (the defect printed
      # '-51245300029.70%'), unlock n_large_30d/7d are counted over the full
      # forward population BEFORE the _MAX_ROWS display slice (a display cap is
      # not a statistic), inquiry reply state resolves per (issuer, named thread)
      # and an unnamed thread is 'undetermined', and a stale-but-readable plane
      # keeps its last reading on screen while dropping out of the hero's
      # aggregate tallies and the "board is complete" copy.
      #
      # Lives HERE, not next to its sibling tests/test_china_special_situations_
      # gate.py in tier-gate, for two independent reasons. (1) DEPENDENCIES: this
      # suite writes its fixtures with DataFrame.to_parquet, and tier-gate's
      # install line carries no pyarrow — the exact class of failure PR #5985 hit
      # in CI (green locally, `ModuleNotFoundError` in the job, because the local
      # environment happened to own a wheel the job's install line does not).
      # Verified the other way here: a clean venv holding EXACTLY this job's
      # install line (pytest pandas numpy pyarrow pyyaml jinja2 plotly requests
      # beautifulsoup4 openpyxl scikit-learn) runs it 45 passed. (2) GATE: it is
      # hermetic — every fixture is a tmp_path parquet behind a monkeypatched
      # lib.config, and the render side loads templates/ only, no data/ and no
      # site/ read — so it belongs on a `gate: code` job that the merge gate
      # actually plans, not on tier-gate's `gate: data` lane. Ordered next to the
      # other CN template-render contract above and ahead of the unrelated US
      # board step, so a red here is not dark behind a failure it has nothing to
      # do with.
      - name: China Special Situations desk — goodwill units, unlock population, inquiry identity, stale-plane authority
        run: python -m pytest tests/test_china_special_situations_truth_wave1.py -q
      - name: US board W9-A producer field + cohort math
        run: python -m pytest tests/test_w4_us_board.py -q
      # Alert Command Center — the TIME CONTRACT and the evidence links (Wave 7,
      # 2026-08-20). This page is a triage layer: its ranking, its "today / 1d ago"
      # wording, its recency points and its catalyst countdown decide what a PM reads
      # first, so a clock defect there is an epistemic defect, not a cosmetic one. Two
      # shipped: engine/subsector_rotation_alerts.py stamped events with the rotation
      # payload's generated_utc (when OUR builder ran) rather than its asof (the settled
      # session the read is about), so Aug-19 conclusions displayed "Aug-20 · today"; and
      # build_triage() took its own "today" from UTC midnight while running every aware
      # stamp through tz_localize(None) — between 00:00Z and ~05:00Z, exactly when the
      # nightly runs, a US cross-asset desk rolled its day over while New York was still
      # in the previous afternoon.
      #
      # WIRED, NOT JUST ADDED (#5952-class): tests/test_alert_triage.py has sat in
      # config/unrun_test_baseline.json since the 2026-08-09 freeze, so the suite pinning
      # this board's honesty invariants — no fabricated hit-rate, demote-only severity,
      # forex/commodity never reach act tier — ran in NO job. Adding time tests to a dark
      # file would have left them dark too. Both suites run here and the file leaves the
      # grandfather list in the same PR. Lives in this lane because it is a template/page
      # contract guard like its neighbours, it is gate: code (these invariants belong on
      # the merge gate, unlike the gate: data lanes that carry the rotation engine), and
      # a clean venv holding EXACTLY this job's install line runs both green — the time
      # suite is hermetic (monkeypatched feed, injected clock, zero wall-clock reads) and
      # the triage suite needs only the committed data/ + templates/ this checkout has.
      # tests/test_alert_coverage_recurrence.py (Wave 7 PR B) rides the same lane: a
      # failed source read must be recorded as MISSING EVIDENCE rather than as zero
      # alerts (otherwise an outage lowers the pressure score and buys a more
      # constructive headline), and sparse re-fires must not impersonate persistence
      # — the fake corroborator that held a `major` band at priority 60, which is also
      # the outbound push floor. Hermetic: monkeypatched readers, injected clock.
      - name: Alert Command Center — three clocks, ET board day, and exact evidence links
        run: python -m pytest tests/test_alert_time_contract.py tests/test_alert_triage.py tests/test_alert_coverage_recurrence.py -q

  # ── RED-ON-ARRIVAL SWEEP, BATCH C (2026-07-27) ─────────────────────────────
  # The PIT / frozen-tape share of the 10 reds (docs/UNRUN_TEST_CENSUS.md §"Red on
  # arrival — the P4/P5 sweep").  Both suites were red for the same underlying
  # reason: a fixture that encodes a MOMENT rather than a contract.
  #
  #   - test_stock_personality_wiring dated its fires 2024-06-03/2024-06-01, but the
  #     stamper clamps its scan to max(build_date - 30d, "2026-07-06") and never
  #     backfills pre-wire-in history.  The fixture matched no fires, so the stamper
  #     returned early and wrote nothing — the suite failed on a MISSING FILE, which
  #     reads like a broken writer rather than a stale date.  Its dates now straddle
  #     the floor deliberately, so the no-backfill rule is asserted instead of
  #     tripped over.  The "wrong date" row had also gone stale in the other
  #     direction: the filter moved from same-day to a 30-day lookback, so a
  #     build_date-1 row is now legitimately IN window and proved nothing.
  #   - test_setup_tier's probe class read the LIVE china_search store and asserted
  #     relationships about three real tickers.  A relationship about a live tape is
  #     still a claim about the market: two of six probes had gone red as the names
  #     traded on, and the rest were passing on borrowed time.  It now reads a frozen
  #     3-ticker slice truncated at 2026-07-03 — the tape the probes were authored
  #     against and the last date on which all six hold at once — so it tests the
  #     engine, which is what it was always for.  Its skip-if-store-absent guard is
  #     gone with the live read: a skip that fires in CI is a pass you cannot see.
  #
  # Repeats the shared dependency install BYTE-FOR-BYTE so run_ci_pack.py keeps one
  # venv across the unrun-* set. PSS prospective-ledger suites ride the same
  # pandas/numpy/pyarrow environment and remain isolated from product authority.
  unrun-pit-probes:
    if: ${{ false }}
    gate: data
    timeout-minutes: 4
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install minimal deps
        run: pip install pytest pandas numpy pyarrow pyyaml jinja2 plotly requests beautifulsoup4 openpyxl scikit-learn
      - name: w_setup probes against the FROZEN tape + the rule ladder
        run: python -m pytest tests/test_setup_tier.py -q
      - name: personality forward-ledger PIT stamp + the no-backfill wire-in floor
        run: python -m pytest tests/test_stock_personality_wiring.py -q
      - name: PSS-RH1 prospective-only detector parity + lifecycle authority fences
        run: python -m pytest tests/test_personality_relief_hazard.py -q
      - name: PSS CR1/CD1/AF1 construction causality + lifecycle authority fences
        run: python -m pytest tests/test_personality_followon_gates.py -q

  # ── THE STRICTLY-DARK SET REOPENED TWICE IN ONE DAY (2026-07-27) ───────────
  # #3636 emptied the strictly-dark set, #3645 emptied it again the same day, and
  # docs/UNRUN_TEST_CENSUS.md says outright that it is not a one-time cleanup — a
  # suite arrives dark whenever it lands without a paths entry.  Two did today:
  #
  #   #3776  tests/test_signup_email_validation.py — the public account-creation
  #          email gate.  The highest-risk shape in the census: a security-adjacent
  #          guard on the signup path, run by nothing.  It pins the email-format
  #          validator AND that the check runs BEFORE sb.auth.signUp.
  #   #3797  tests/test_gh_quota_guard.py — the shared-GitHub-quota hook, itself
  #          added because two watchers on one endpoint drained the 5,000/hr REST
  #          pool to zero and 403'd every session.  36 tests, run by nothing.
  #
  # Neither needs third-party deps, but both repeat the shared install BYTE-FOR-BYTE
  # so run_ci_pack.py keeps one venv across the unrun-* set rather than building a
  # second one for 38 tests.
  #
  # Measurement note for whoever next reads the auditor — BOTH are the same false
  # positive.  audit_unrun_tests.py derives subjects from IMPORTS; neither suite
  # imports its subject.  test_signup_email_validation reads templates/onboard.js as
  # text and regexes it; test_gh_quota_guard invokes .claude/hooks/gh_quota_guard.py
  # as a SUBPROCESS.  So the auditor reports subjects=[] and calls both
  # untriggerable, when templates/** and .claude/** already match their subjects.
  # Only the UNRUN half was real in each case.  Guards that reach their subject by
  # path string or subprocess are a blind spot in the auditor, not a dark corner of
  # ci.yml — check how a suite reaches its subject before believing a dark verdict.
  unrun-dark-guards:
    if: ${{ false }}
    gate: code
    timeout-minutes: 4
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install minimal deps
        run: pip install pytest pandas numpy pyarrow pyyaml jinja2 plotly requests beautifulsoup4 openpyxl scikit-learn
      - name: signup email gate — format validator + it runs BEFORE supabase signUp
        run: python -m pytest tests/test_signup_email_validation.py -q
      - name: shared-GitHub-quota hook — the guard that keeps polling loops off the REST pool
        run: python -m pytest tests/test_gh_quota_guard.py -q
      # 2026-08-14 — the Prophet US rescue lane, registered HERE rather than in a new
      # job because the legacy-jobs manifest sits at the narrow-diff ceiling and a
      # 181st entry reds it. Same family as the two above: a subprocess/path-string
      # guard whose subject the import-derived auditor cannot see, so it would arrive
      # DARK by default — the exact leak this job is named after. It reaches its
      # subject by loading scripts/prophet_rescue.py through importlib from a path
      # literal, and it carries the four mutation-pinned dispatch-safety invariants
      # (§0.4 a-d) plus the no-stop-a-run AST pin; unrun, all of that is decoration.
      - name: prophet rescue lane — pure decide() matrix + the four dispatch-safety pins
        # test_prophet_r2_boundary.py rides this step (not a new entry — the
        # manifest's narrow-diff ceiling reds a 181st job): same publication-
        # boundary family, and its daily.yml/text pins are exactly the subject
        # the rescue watchdog polices (DEC:B1-PROPHET-PUBLIC-SPLIT).
        #
        # The three private-cutover guards ride it for the same reason and are
        # the same family — an anonymous-distribution boundary that is invisible
        # to the import-derived auditor because its subjects are shell scripts,
        # a Caddyfile, and URL string literals (DEC:B1-CUTOVER-HARDENING):
        #   test_deploy_bootstrap_private.py   — no curl|bash raw bootstrap survives
        #   test_macro_anon_dependency_guard.py — the anonymous-Macro regression fence
        #   test_caddy_hub_boundary.py         — no public proxy path reaches /api/hub/*
        #
        # test_prophet_surface_net.py (PR-1, Prophet US permanence net, 2026-08-27)
        # rides here too, same reason: it pins the shared intake-identity predicate
        # across all four watchdogs in this family (freshness_sentinel,
        # check_nightly_liveness, prophet_rescue, the new prophet_board_acceptance),
        # the lane-latch acceptance exemption, and the heartbeat write/grade round
        # trip — unrun, the whole permanence net is decoration same as the invariants
        # above.
        run: python -m pytest tests/test_prophet_rescue.py tests/test_prophet_r2_boundary.py tests/test_live_snapshot_host_guard.py tests/test_no_pages_publish.py tests/test_deploy_bootstrap_private.py tests/test_macro_anon_dependency_guard.py tests/test_m1_macro_consumer_inspector.py tests/test_caddy_hub_boundary.py tests/test_prophet_surface_net.py -q

  # ── THE HK BOARD PAIR — BORN DARK EIGHT DAYS AFTER THE CENSUS (2026-08-05) ──
  # tests/test_hk_board_rank.py (163 tests) + tests/test_hk_board_ui.py landed
  # with the HK board resurrection (#4421, 2026-08-03) — AFTER the 2026-07-26
  # census sweep — named by no run: step in any workflow: the weekly-reopening
  # leak docs/UNRUN_TEST_CENSUS.md warns about, on its schedule.  The bill came
  # due within a day.  The 2026-08-04 asia collection re-adjusted 2338.HK
  # (ex-dividend) and rewrote 28 sessions of the G1 panel's history; the suite's
  # TestG1FixtureIsNotStale tripwire caught it — and CI never saw any of it.  No
  # job ran the suite, and the heal (#4559) was a fixture-only diff that matched
  # no ci.yml paths entry, so it merged having triggered zero pack checks: both
  # the red and its fix existed only in hand runs.  (The queue-wide ci-pack red
  # the same night was tests/test_marketing_hot_tape_radar.py — wired, a
  # different market-state pin — easy to conflate, not the G1 red.)  This job
  # ends the unrun half; the trigger halves (both test files, the frozen G1
  # fixture pair, data/hk_search/**) land in ci.yml paths in the same PR.
  #
  # Runtime: the witness-replay test runs engine.signal_gate.gate() on seven
  # full-history series from data/hk_search/closes_deep.parquet — minutes, not
  # seconds.  Documented ~83s on a fast Mac Studio; measured ~2:27 (~1.8×) on
  # the same host, and it crosses an 8-minute cap on a busy 4-core hosted
  # runner mid-backlog.  8 → 15, the same ceiling as unrun-market-plumbing.
  # Weight is not derived from timeout-minutes, so this does not rebalance
  # packs.  Deps: the builder-wiring tests import scripts/build_hk.py
  # (plotly) and reach lib.config (pyyaml) — measured in a clean venv, where
  # pytest+pandas+pyarrow+jinja2 alone left 23 tests red on those two imports.
  # The shared unrun-* install repeated BYTE-FOR-BYTE keeps this job inside the
  # set's single venv instead of adding one.
  unrun-hk-board:
    if: ${{ false }}
    gate: data
    timeout-minutes: 15
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install minimal deps
        run: pip install pytest pandas numpy pyarrow pyyaml jinja2 plotly requests beautifulsoup4 openpyxl scikit-learn
      - name: hk board rank — G1-G5 gates + the G1 staleness tripwire (witness replay)
        run: python -m pytest tests/test_hk_board_rank.py -q
      - name: hk board ui — the unified board template contract
        run: python -m pytest tests/test_hk_board_ui.py -q
      # test_hk_reclaim_veto_policy.py joined 2026-08-05, dark since it landed
      # with the operator's 2026-08-03 admission ruling.  It pins the ONE leg HK
      # drops — `reclaim_veto=False`, the requirement that a name both below its
      # 200-day line AND weekly-down close back above that line within 2 bars,
      # unsatisfiable by construction for a deep drawdown — and, more load-
      # bearing, pins the blast radius: that US/CN keep the validated default at
      # every layer (_buy_filter, analyze, signal_gate.gate), keyword-only so
      # argument order cannot flip it, and that build_hk_library is the only
      # caller opting out.  This is the exact leg the reclaim-veto decision
      # packet measures (research/prophet_us_audit/RECLAIM_VETO_PACKET_2026-08-05.md,
      # #4607) — a packet arguing over a policy whose guard no CI job ran.
      # Belongs in THIS job: it imports scripts.build_hk_library (which pulls the
      # engine.i18n/stock_score/cycles chain) and engine.hk_board_rank, so it
      # needs this job's wide install exactly — the same dep-list trap the
      # census recorded for the pair above.  Cost measured as this job's third
      # step over the full sequence: 68.5s (rank, 163 tests) + 13.5s (ui, 83) +
      # 1.5s (this, 10) = ~83s documented; measured ~2:27 (~1.8×) locally, and
      # it times out at 8 minutes on a busy hosted runner. timeout-minutes is
      # 15 — same ceiling as unrun-market-plumbing — so the witness replay
      # keeps its headroom.
      - name: hk reclaim-veto admission policy — the dropped leg and its blast radius
        run: python -m pytest tests/test_hk_reclaim_veto_policy.py -q

  unrun-subsector-themes:
    # The subsector rotation/turn engine family + the Theme Tracker page suite —
    # named by NO run: step in any workflow before this (subsector CI-wiring
    # audit 2026-08-05, following the #4584 calendar-asof heal): the TURN
    # engine (#4085), the rotation state machine, its alerts/scan/confluence
    # organs, and the themes heatmap ran only when someone happened to run
    # them locally.  The Theme Tracker suite runs FULL here on purpose: the
    # portfolio-ctx job deliberately selects the theme_lanes/byte-identity
    # subset (5 of 42 tests) as the Portfolio-brief contract, so the other 37
    # — live render off the committed artifacts, banned-word and title= scans,
    # node --check over inline JS (the pack runners ship setup-node), and the
    # TIL section wiring — were dark.  All seven suites measured green in a
    # clean venv with exactly the install below: 119 tests / ~87s, the live
    # renders dominating.  Deps: the engine suites reach numpy+pandas; the
    # page builder reaches jinja2 + pyyaml (theme_crosswalk lane projection).
    # The shared unrun-* install repeated BYTE-FOR-BYTE keeps this job inside
    # the census set's single venv instead of adding one.
    if: ${{ false }}
    gate: data
    # CURATED SCOPE (2026-08-14 incident follow-up). `scope: exclusive` makes
    # the paths below REPLACE inference instead of being unioned under it, so
    # the opaque fallback tier this job used to carry — whole-tree `site/**`,
    # `templates/**`, `docs/**`, `research/**` claims smeared out of a
    # subprocess or traversal deep in its closure — stops selecting it. Every
    # pattern here was generated from the job's OWN import closure and audited
    # for zero misses: no file this job demonstrably reads lost its owner.
    # 134s over seven subsector/theme suites. `engine/` IS ENUMERATED, NOT
    # GLOBBED (2026-08-15 follow-up to #5685). `engine/*` was the worst smear
    # in this wave: 846 tracked top-level engine files / 41 in this job's
    # concrete import closure. The 41 entries below ARE that closure,
    # re-derived via infer_job_scopes([replace(job, exclusive=False)]).
    # engine/prophet, engine/pick_lab and the other unimported subpackages
    # stay outside. Widening is the safe direction: when a new import lands,
    # test_curated_exclusive_scopes_cover_their_own_import_closure names the
    # exact uncovered file — add it here.
    scope: exclusive
    paths:
      - "collectors/base.py"
      - "collectors/fda_shortages.py"
      - "config/**"
      - "data/**"
      - "engine/__init__.py"
      - "engine/altdata_confirmers.py"
      - "engine/basket_behaves_as.py"
      - "engine/basket_index.py"
      - "engine/bottleneck.py"
      - "engine/canon.py"
      - "engine/conditions.py"
      - "engine/confluence_tiers.py"
      - "engine/cycles.py"
      - "engine/demand_capex.py"
      - "engine/demand_chain.py"
      - "engine/fda_scarcity.py"
      - "engine/foresight_cascade.py"
      - "engine/foresight_earliness.py"
      - "engine/foresight_enb.py"
      - "engine/foresight_score.py"
      - "engine/foresight_sizing.py"
      - "engine/glut_watch.py"
      - "engine/guidance_gap.py"
      - "engine/hysteresis.py"
      - "engine/indicators.py"
      - "engine/ledger_lane.py"
      - "engine/news_flow.py"
      - "engine/policy_calendar.py"
      - "engine/sector_legs.py"
      - "engine/sector_signals.py"
      - "engine/session_anchor.py"
      - "engine/signal_gate.py"
      - "engine/signal_quality.py"
      - "engine/subsector_confluence.py"
      - "engine/subsector_rotation.py"
      - "engine/subsector_rotation_alerts.py"
      - "engine/subsector_scan.py"
      - "engine/subsector_turn.py"
      - "engine/technicals.py"
      - "engine/theme_emergence.py"
      - "engine/theme_fingerprint.py"
      - "engine/theme_revisions.py"
      - "engine/themes_heatmap.py"
      - "engine/vol_forecast.py"
      - "engine/vol_regime.py"
      - "lib/*"
      - "scripts/build_state_of_themes.py"
      - "scripts/check_template_site_sync.py"
      - "scripts/worktree_sparse.py"
      - "site/chat.html"
      - "site/factordata/**"
      - "site/state_of_themes.html"
      - "templates/_navlinks.html.j2"
      - "templates/chat.html"
      - "templates/state_of_themes.html.j2"
      - "tests/test_state_of_themes.py"
      - "tests/test_subsector_confluence.py"
      - "tests/test_subsector_rotation.py"
      - "tests/test_subsector_rotation_alerts.py"
      - "tests/test_subsector_scan.py"
      - "tests/test_subsector_turn.py"
      - "tests/test_themes_heatmap.py"
    timeout-minutes: 5
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install minimal deps
        run: pip install pytest pandas numpy pyarrow pyyaml jinja2 plotly requests beautifulsoup4 openpyxl scikit-learn
      - name: subsector rotation family — turn engine, state machine, alerts, scan, confluence
        run: python -m pytest tests/test_subsector_turn.py tests/test_subsector_rotation.py tests/test_subsector_rotation_alerts.py tests/test_subsector_scan.py tests/test_subsector_confluence.py -q
      - name: themes heatmap + Theme Tracker page (full suite; portfolio-ctx keeps its subset)
        run: python -m pytest tests/test_themes_heatmap.py tests/test_state_of_themes.py -q

  ohlc-basis-coherence:
    # The breadth OHLC caches must share ONE yfinance adjustment basis across
    # _closes_cache / _high_cache / _low_cache. 2026-08-06 incident: the extras
    # drifted ~30% below closes (REZI close/high 1.29-1.44 on 773 of 773 bars)
    # and NOTHING saw it — both repair paths (BreadthAdapter._merge_refreshed and
    # scripts/heal_breadth_split_seams.py) scan seam_suspects over the CLOSES
    # matrix alone, so a split confined to high/low is invisible by construction.
    # Close-only consumers are unaffected, which is what kept it silent; ATR's
    # true range is dominated by the basis gap.
    #
    # This lane tests the GUARD (synthetic frames, offline) plus a reader-level
    # pin over the live committed caches. The DATA verdict is carried nightly by
    # daily.yml's collect job, which is where a split is actually born — a PR
    # pack cannot see a break that arrives on a straight-to-main data push.
    # pyarrow is not optional: every fixture round-trips through parquet, which
    # is the only way the test can see a dtype/schema regression in the reader.
    if: ${{ false }}
    gate: data
    timeout-minutes: 5
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install minimal deps
        run: pip install pytest pandas numpy pyarrow
      - name: OHLC basis coherence — split vs isolated-bad-bar tiers, annotations, exit codes
        run: python -m pytest tests/test_check_ohlc_basis_coherence.py -q
      - name: guard selftest
        run: python scripts/check_ohlc_basis_coherence.py --selftest

  unrun-government-revenue-grader:
    # Wave 9G prospective candidate grader — the instrument that is allowed to
    # tell us the Government Revenue lobe has no predictive value.  It ships as
    # its OWN job rather than as a step inside unrun-government-revenue. The
    # candidate API contract shares this owner because both exercise the same
    # broad GovRev dependency closure and together remain well inside one bounded
    # lane; projection keeps its independent 15-minute lane above.
    #
    # Every assertion in the suite is paired with a mutation that proves it can
    # see the defect it guards (widened read window, resolution-conditioned
    # denominator, correction-by-rewrite, coverage-free rate).  A grader whose
    # own tests are dark is worse than no grader, which is the whole reason this
    # job exists on the day the module lands rather than in a later backfill.
    if: ${{ false }}
    gate: data
    # CURATED SCOPE (2026-08-14 incident follow-up). `scope: exclusive` makes
    # the paths below REPLACE inference instead of being unioned under it, so
    # the opaque fallback tier this job used to carry — whole-tree `site/**`,
    # `templates/**`, `docs/**`, `research/**` claims smeared out of a
    # subprocess or traversal deep in its closure — stops selecting it. Every
    # pattern here was generated from the job's OWN import closure and audited
    # for zero misses: no file this job demonstrably reads lost its owner.
    # 322s, the second-heaviest legacy job. Its 294-file closure is real
    # (engine/government_revenue + the neuralweb/forensics spine), but every
    # one of its four measured drops — a template, the free-content builder, a
    # prophet module, theme.css — matched only through the fallback tier.
    # Top-level `engine/*` is enumerated (2026-08-15 follow-up to #5685):
    # 846 tracked / 74 concrete. Subject packages stay globbed.
    scope: exclusive
    paths:
      # B1A closure widening 2026-08-24 (contract-delta): build_stock_library's security_state stage extends this job's walked closure.
      - "engine/security_state.py"
      - "engine/theme_graph/__init__.py"
      - "engine/theme_graph/identity.py"
      - "engine/quote_resolution.py"
      # D5's app/prophet_lab route imports the canonical B1 episode reader and
      # issuer identity chain. This curated job reaches that app router, so its
      # exclusive declaration must cover the exact transitive closure rather
      # than silently skipping changes to these dependencies.
      - "engine/path_risk_signals.py"
      - "engine/stock_identity/__init__.py"
      - "engine/stock_identity/authority.py"
      - "engine/stock_identity/fingerprint.py"
      - "engine/stock_identity/plane.py"
      - "engine/us_candidate_episode.py"
      # W1-B wires Brain's native-fact serve to the frozen W1-A runtime. This
      # job's own app/neuralweb import closure now reaches those packages, so
      # its curated scope must retain their real dependency ownership.
      - "engine/intelligence_workspace/**"
      - "engine/theme_graph/**"
      - "lib/dataos/**"
      - "app/*"
      - "collectors/*"
      - "collectors/biocatalyst/**"
      - "config/**"
      - "contracts/**"
      - "data/**"
      - "engine/__init__.py"
      - "engine/_text_scrub.py"
      - "engine/ai_desk.py"
      - "engine/ai_desk_scorer.py"
      - "engine/altdata_ledger.py"
      - "engine/altdata_models.py"
      - "engine/altdata_picks.py"
      - "engine/altdata_signals.py"
      - "engine/canon.py"
      - "engine/catalyst_tone.py"
      - "engine/china_altdata.py"
      - "engine/china_basket_spine.py"
      - "engine/china_conviction.py"
      - "engine/china_crowding.py"
      - "engine/china_discovery.py"
      - "engine/china_event_calendar.py"
      - "engine/china_extras.py"
      - "engine/china_intel_bus.py"
      - "engine/china_news.py"
      - "engine/china_news_intel.py"
      - "engine/china_radar.py"
      - "engine/china_radar_ledger.py"
      - "engine/china_regime_store.py"
      - "engine/china_signal_lab.py"
      - "engine/china_standout_track.py"
      - "engine/china_validation.py"
      - "engine/cn_newswires.py"
      - "engine/codex_provider.py"
      - "engine/desk_ledger.py"
      - "engine/desk_scorer.py"
      - "engine/earnings_qual.py"
      - "engine/earnings_transcript_intake.py"
      - "engine/entity_resolver.py"
      - "engine/event_calendar.py"
      - "engine/forward_calendar_context.py"
      - "engine/gdelt_client.py"
      - "engine/grading.py"
      - "engine/i18n.py"
      - "engine/indicators_m2.py"
      - "engine/ledger_lane.py"
      - "engine/live_quotes.py"
      - "engine/llm_auth.py"
      - "engine/macro_surprise.py"
      - "engine/manager_lag.py"
      - "engine/manager_quality.py"
      - "engine/manager_trades.py"
      - "engine/master_brain.py"
      - "engine/name_resolver.py"
      - "engine/news_common.py"
      - "engine/news_translate.py"
      - "engine/ollama_provider.py"
      - "engine/opex.py"
      - "engine/options_issue_desk.py"
      - "engine/passport.py"
      - "engine/pooling.py"
      - "engine/portfolio_brief.py"
      - "engine/portfolio_changes.py"
      - "engine/portfolio_vocab.py"
      - "engine/prophet_integrity.py"
      - "engine/provider_health.py"
      - "engine/qbus.py"
      - "engine/qkernel.py"
      - "engine/qledger.py"
      - "engine/qledger_validity.py"
      - "engine/rebalance_calendar.py"
      - "engine/regime_label.py"
      - "engine/regime_vector.py"
      - "engine/session_anchor.py"
      - "engine/smart_money.py"
      - "engine/spine.py"
      - "engine/ticker_shape.py"
      - "engine/translate.py"
      - "engine/trial_ledger.py"
      - "engine/tushare_freshness.py"
      - "engine/universe_history.py"
      - "engine/validation.py"
      - "engine/biocatalyst/**"
      - "engine/capital_structure/**"
      - "engine/chronicle/**"
      - "engine/codex_lane/**"
      - "engine/company_intelligence/**"
      - "engine/earnings_release/binding.py"
      - "engine/earnings_release/figures.py"
      - "engine/earnings_release/filing_key.py"
      - "engine/earnings_release/receipts.py"
      - "engine/context_index/**"
      - "engine/earnings_narrative/**"
      - "engine/fundamental_forensics/**"
      - "engine/government_revenue/**"
      - "engine/marketing/**"
      - "engine/metabolism/**"
      - "engine/entry_radar/__init__.py"
      - "engine/entry_radar/contracts.py"
      - "engine/entry_radar/spool.py"
      # 2026-08-19 LAB-0 §6 commissioning-prep heal: engine/prophet_lab/sources.py
      # (below) now imports engine.entry_radar.spool (the R2-first read seam,
      # mirrors the Radar spool WRITER's own backend ladder) -- never the
      # detector/scoring stack (live_ledger/challengers/detectors), which stays
      # off limits and off this job's closure. spool.py itself only imports
      # entry_radar/contracts.py (stdlib-only) and entry_radar/__init__.py, so
      # exactly these three files, never engine/entry_radar/** broadly.
      - "engine/neuralweb/**"
      - "engine/press/**"
      - "engine/prophet_board_read.py"
      # 2026-08-19 ci-pack-10 heal: this job's inferred (non-exclusive) closure
      # newly reaches these engine/*.py files on the P-LAB-API branch. Verified
      # via a real `git clone --local --depth 1` baseline (not a `git archive`
      # extraction, which has no `.git` and silently under-reports
      # discover_suites()'s tracked-file census) that origin/main shows ZERO
      # uncovered for this SAME job -- so this is a genuine new reachability
      # edge from this branch's diff, not stale pre-existing drift, though the
      # exact causal chain (none of these files are imported by
      # engine/prophet_lab/** or engine/prophet_board_read.py, both already
      # declared above) was not fully isolated within this PR's budget.
      # Widening here per the guard's own remedy ("widening is always the safe
      # direction") rather than leaving the pack red.
      - "engine/oracle/**"
      - "engine/pick_lab/**"
      - "engine/prophet_live/**"
      - "engine/activist.py"
      - "engine/analyst_revisions.py"
      - "engine/anticipation.py"
      - "engine/basket_breadth_divergence.py"
      - "engine/basket_index.py"
      - "engine/basket_mtf.py"
      - "engine/basket_score.py"
      - "engine/basket_tape.py"
      - "engine/baskets.py"
      - "engine/baskets_canada.py"
      - "engine/baskets_china.py"
      - "engine/baskets_hk.py"
      - "engine/baskets_intl.py"
      - "engine/baskets_region.py"
      - "engine/beneficial_ownership.py"
      - "engine/btc_mtf.py"
      - "engine/capital_allocation.py"
      - "engine/coiled.py"
      - "engine/composite_score.py"
      - "engine/conditions.py"
      - "engine/confluence_tiers.py"
      - "engine/conviction_accrual.py"
      - "engine/conviction_delta.py"
      - "engine/cross_asset.py"
      - "engine/cross_asset_trend.py"
      - "engine/crowding.py"
      - "engine/cycles.py"
      - "engine/dannytrades.py"
      - "engine/dannytrades_chip.py"
      - "engine/demand_chain.py"
      - "engine/dispersion.py"
      - "engine/donor.py"
      - "engine/earnings_blackout.py"
      - "engine/earnings_catalyst.py"
      - "engine/entry_primitives.py"
      - "engine/entry_signal.py"
      - "engine/equity_factors.py"
      - "engine/event_atlas.py"
      - "engine/event_landmine.py"
      - "engine/expectation_state.py"
      - "engine/extension.py"
      - "engine/forward_dist.py"
      - "engine/gex_confirm.py"
      - "engine/gex_engine.py"
      - "engine/gov_exposure.py"
      - "engine/greeks.py"
      - "engine/group_flow.py"
      - "engine/hold.py"
      - "engine/htf_durability.py"
      - "engine/hysteresis.py"
      - "engine/ignition_features.py"
      - "engine/index_direction.py"
      - "engine/indicators.py"
      - "engine/inputs.py"
      - "engine/ledger_identity.py"
      - "engine/liquidity_chip.py"
      - "engine/long_hold_clocks.py"
      - "engine/moat_falsifiers.py"
      - "engine/mtf_upturn.py"
      - "engine/name_direction.py"
      - "engine/name_score.py"
      - "engine/name_score_grader.py"
      - "engine/narrative_rotation.py"
      - "engine/options_ivspread.py"
      - "engine/options_structure.py"
      - "engine/path_personality.py"
      - "engine/pit.py"
      - "engine/pit_lag_recorder.py"
      - "engine/playbook.py"
      - "engine/postcross.py"
      - "engine/prophet_bridge.py"
      - "engine/prophet_doors.py"
      - "engine/prophet_stage_inputs.py"
      - "engine/provenance_sidecar.py"
      - "engine/pullback_zone.py"
      - "engine/rate_inflation_transmission.py"
      - "engine/yield_momentum.py"
      - "engine/residual_alpha.py"
      - "engine/risk_sizing.py"
      - "engine/roc_blowoff.py"
      - "engine/sector_pulse.py"
      - "engine/sector_rate_inflation.py"
      - "engine/setup_tier.py"
      - "engine/setups.py"
      - "engine/shadow_book.py"
      - "engine/short_volume.py"
      - "engine/signal_archive.py"
      - "engine/signal_gate.py"
      - "engine/signal_quality.py"
      - "engine/special_arb.py"
      - "engine/special_situations.py"
      - "engine/spotlight.py"
      - "engine/stock_dossier.py"
      - "engine/stock_events.py"
      - "engine/stock_fundamentals.py"
      - "engine/stock_macro_sensitivity.py"
      - "engine/stock_personality.py"
      - "engine/stock_score.py"
      - "engine/stock_technicals.py"
      - "engine/stock_view.py"
      - "engine/sue.py"
      - "engine/t0_indicator.py"
      - "engine/technicals.py"
      - "engine/theme_crowding.py"
      - "engine/theme_scoring.py"
      - "engine/theme_validation.py"
      - "engine/thesis_funnel.py"
      - "engine/ticker_alerts.py"
      - "engine/track_record.py"
      - "engine/us_board_rank.py"
      - "engine/us_candidate_lanes.py"
      - "engine/us_context_vector.py"
      - "engine/us_early_turn.py"
      - "engine/us_prophet_fusion.py"
      - "engine/valuation.py"
      - "engine/velocity.py"
      - "engine/vol_forecast.py"
      - "engine/vol_regime.py"
      - "engine/vol_squeeze.py"
      - "engine/washout_turn.py"
      - "engine/weinstein_stage.py"
      - "research/SIGNAL_EPISODE_ATLAS_MASTERPLAN_BY_FABLE.md"
      - "research/STOCK_PERSONALITY_MASTERPLAN_BY_FABLE.md"
      - "research/US_BOARD_MEASUREMENT.md"
      - "scripts/build_stock_library.py"
      # Import-closure entry: build_stock_library reuses
      # scripts/snapshot_shadow_book.rows_from_board for the shadow-book
      # snapshot (one percentile definition), so exclusive scope must name it.
      - "scripts/snapshot_shadow_book.py"
      - "scripts/check_board_contradictions.py"
      - "scripts/check_signal_gate_coherence.py"
      - "scripts/check_stretch_oracle_contract.py"
      - "scripts/thematic_rotation_phase0.py"
      - "engine/prophet_lab/**"
      - "engine/research_vault/**"
      - "engine/seasonality/**"
      - "engine/sector_intelligence/**"
      - "lib/*"
      - "lib/dataos/identity.py"
      - "research/DO_NOT_REBUILD.md"
      - "research/GOVERNMENT_REVENUE_CANDIDATE_GRADER_PREREG.md"
      - "scripts/__init__.py"
      - "scripts/build_government_revenue.py"
      - "scripts/build_government_revenue_candidates.py"
      # engine/htf_durability.py reads site/basketdata/fear_greed.json and
      # engine/pick_lab/candidates.py reads site/flowleaders/leaders.json and
      # site/leaderradar/radar.json — both already-declared engine files
      # (`engine/htf_durability.py`, `engine/pick_lab/**` above), pulled into
      # this job's closure via tests/test_government_revenue_candidate_api.py.
      # The import-closure contract
      # (test_curated_exclusive_scopes_cover_their_own_import_closure)
      # requires the declared scope to cover these site reads too.
      - "site/basketdata/fear_greed.json"
      - "site/canadabasketdata/**"
      - "site/chinabasketdata/**"
      - "site/coming-soon.html"
      - "site/data/**"
      - "site/factordata/**"
      - "site/flowleaders/leaders.json"
      - "site/government-revenue-data/**"
      - "site/hkbasketdata/**"
      - "site/leaderradar/radar.json"
      - "site/master_brief.json"
      - "site/prophet/**"
      - "site/vol/**"
      - "tests/government_revenue_candidate_fixture.py"
      - "tests/test_government_revenue_candidate_api.py"
      - "tests/test_government_revenue_candidate_grader.py"
      - "tests/test_government_revenue_candidate_projection.py"
      - "tests/test_government_revenue_candidates.py"
    timeout-minutes: 15
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install minimal deps
        run: pip install pytest pandas numpy pyarrow pyyaml jinja2 plotly requests beautifulsoup4 openpyxl scikit-learn jsonschema fastapi
      - name: prospective candidate grader — issuance log, no-leakage, denominators, coverage
        run: python -m pytest tests/test_government_revenue_candidate_grader.py -q
      - name: candidate API contract fixtures — cache invalidation, quarantine, audit history
        run: python -m pytest tests/test_government_revenue_candidate_api.py -q

  research-price-panel:
    if: ${{ false }}
    gate: code
    # The versioned FROZEN research price panel (data/research_panels/).  Two audits
    # measured that our evidence base is not reproducible: the breadth close caches are
    # re-based at a full rebuild and accrue RAW closes after it, so PNC's 2026-06-22 close
    # read 234.71 on 2026-07-01 and 232.8536 on 2026-08-06 with nothing recording the
    # change.  Frozen->cache-today drift moves a decision statistic up to 0.24pp against
    # 0.26-0.98pp decision boundaries, so the infrastructure noise is now the LARGER term.
    #
    # This lane pins the two properties the artifact exists for, both of which are silent
    # failures — a broken freeze looks exactly like a working one until someone re-derives
    # a published number a year later:
    #   1. an existing version is byte-stable across a rebuild whose underlying store MOVED
    #   2. a reader given an unknown version raises instead of quietly resolving elsewhere
    # Every case runs against a synthetic store built in tmp_path (adjusted per-name files
    # shadowing a wide close cache, cache-only names, and a name no source carries), so the
    # job checks out no data fixtures and cannot touch the real data/ tree.
    #
    # Both halves are wired in ci.yml per #3488 — the suite AND the modules it measures
    # (price_panel.py, the vendored price_ladder.py, and the migrated instrument) — so an
    # edit to either can start the job that proves the freeze still holds.
    #
    # Deps: pyarrow is load-bearing (the artifact IS a parquet; a fixture cannot stand in
    # for the bytes under test).  The install string is byte-identical to the
    # trial-budgets/outcome-spine family so the pack runner shares their venv rather than
    # provisioning another.  Measured with exactly this line: 27 passed in ~1.9s, zero
    # skips.  The #4698-ref comparison SKIPS rather than passes when that ref is
    # unfetchable, so a checkout that cannot see the PR branch reads 26 passed / 1 skipped
    # — an unverifiable pin must not report itself green.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install minimal deps
        run: pip install pytest pandas numpy pyarrow pyyaml
      - name: frozen price panel — write-once bytes + a reader that never falls back
        run: python -m pytest tests/test_research_price_panel.py -q

  zh-filing-term:
    # ui.zh_filing_term PR gate: 申报 must never reach user-facing Chinese copy.
    # A native reader hears a TAX or CUSTOMS declaration (申报纳税, 海关申报) where
    # the platform means a market DISCLOSURE — an SEC 10-K, a 13F, a congressional
    # STOCK Act report is a 披露. The term recurs 20-50x site-wide because every
    # builder writing a zh filings label reaches for the same wrong word, and it
    # has already been swept BY HAND twice (#4830 Filing Forensics, #4855 site-wide)
    # with nothing to stop the third regression. Hence a gate.
    #
    # The selftest step runs FIRST on purpose: a diction guard whose scan comes back
    # clean is indistinguishable from a diction guard that no longer fires, so the
    # green below only means something because the synthetic insertion above it went
    # red. The pytest step then pins the allowlist's narrowness (phrase AND file) and
    # the pending ratchet, which is what keeps this from turning into a blanket
    # file-level exemption the next time a sweep is in flight.
    if: ${{ false }}  # executed inside ci-pack — NOT a disabled gate; see the ci-pack job.
    gate: code
    timeout-minutes: 5
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install minimal deps
        run: pip install pytest
      - name: zh-filing-term selftest (gate fires on a synthetic 申报 insertion)
        run: python3 scripts/check_zh_filing_term.py --selftest
      - name: no unlicensed 申报 in user-facing Chinese copy
        run: python3 scripts/check_zh_filing_term.py
      - name: zh-filing-term guard suite (allowlist narrowness + pending ratchet)
        run: python -m pytest tests/test_zh_filing_term.py -q

  earnings-release-identity:
    # Wave 1B of the Earnings & Company Event suite: the EDGAR identity join and
    # the release binding.
    #
    # TWO OF THESE FOUR SUITES WERE STRICTLY DARK before this job existed —
    # tests/test_edgar_earnings_8k.py and tests/test_marketing_edgar_earnings_wire.py
    # were named by ZERO `run:` steps anywhere in this file and matched by no path
    # glob in ci.yml, so 133 assertions about the two EDGAR readers could not fail
    # a pull request. They guard the exact-token Item-2.02 match (the difference
    # between 2.02 and 12.02), the store's dedup key, the basis check that stops a
    # GAAP figure being posted against an adjusted estimate, and the provider's
    # fail-soft-never-fail-silent annotations. They are wired here.
    #
    # `requests` is in the install line because collectors/edgar_earnings_8k.py
    # imports it at module scope; without it the collector suite errors on import
    # rather than running, which reads as a pack failure rather than a missing dep.
    if: ${{ false }}  # executed inside ci-pack — NOT a disabled gate; see the ci-pack job.
    gate: code
    timeout-minutes: 10
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install minimal deps
        run: pip install pytest pandas numpy pyarrow pyyaml requests
      - name: EDGAR filing identity join + release binding (Wave 1B)
        run: >-
          python -m pytest
          tests/test_edgar_filing_identity_join.py
          tests/test_earnings_release_binding.py
          tests/test_edgar_earnings_8k.py
          tests/test_marketing_edgar_earnings_wire.py -q -rs

  market-memory-contract:
    # Public-safe temporal-context and product-shell contract. Private captures,
    # credentials, and provider responses remain absent; the option availability
    # transport is exercised only with bounded offline fixtures in this
    # zero-network lane.
    if: ${{ false }}  # executed inside ci-pack — NOT a disabled gate.
    gate: data
    timeout-minutes: 15
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install minimal deps
        run: pip install pytest fastapi httpx numpy pandas pyarrow jinja2 pyyaml requests jsonschema
      - name: canonical Market Memory temporal and shell contracts
        run: >-
          python -m pytest
          tests/test_market_memory.py
          tests/test_market_memory_pit.py
          tests/test_market_memory_sources.py
          tests/test_market_memory_source_deploy.py
          tests/test_market_memory_m0d_v2.py
          tests/test_market_memory_identity.py
          tests/test_market_memory_identity_observation.py
          tests/test_market_memory_identity_store.py
          tests/test_market_memory_identity_observation_deploy.py
          tests/test_ingest_market_memory_identity.py
          tests/test_market_memory_breadth_observation.py
          tests/test_market_memory_breadth_store.py
          tests/test_market_memory_breadth_deploy.py
          tests/test_market_memory_technical_observation.py
          tests/test_market_memory_technical_store.py
          tests/test_capture_market_memory_technicals.py
          tests/test_market_memory_technicals_deploy.py
          tests/test_market_memory_experience_accrual.py
          tests/test_market_memory_experience_deploy.py
          tests/test_market_memory_replay.py
          tests/test_market_memory_option_oi_observation.py
          tests/test_market_memory_option_oi_store.py
          tests/test_capture_market_memory_option_oi.py
          tests/test_market_memory_options_deploy.py
          tests/test_market_memory_forward.py
          tests/test_market_memory_forward_store.py
          tests/test_market_memory_scoring.py
          tests/test_market_memory_playback.py
          tests/test_market_memory_playback_api.py
          tests/test_market_memory_retrieval.py
          tests/test_market_memory_operating_cortex.py
          tests/test_market_memory_promotion_audit.py
          tests/test_market_memory_production_records.py
          tests/test_market_memory_production_records_deploy.py
          tests/test_market_memory_projection.py
          tests/test_market_memory_trusted.py
          tests/test_market_memory_context_deploy.py
          tests/test_regime_latest_json_boundary.py
          tests/test_release_target_truth.py
          tests/test_build_market_memory_page.py -q

  group-pulse:
    # GR0 of the Group Reads program: engine/group_pulse.py, the basket-level
    # participation / direction / arc read plane, plus the two additive extensions
    # it landed in existing organs (engine/coiled.py's washout_ctx_detail and
    # engine/group_flow.py's sign_agreement).
    #
    # Three suites, three different failure classes, deliberately in one job because
    # they guard ONE contract and a partial heal cannot merge a pack anyway:
    #
    #   contract   the frozen group_pulse.v1 shape — a golden object assembled
    #              through the real code path validates, and MUTANTS (unknown key,
    #              a share without its n, an off-ladder arc state, a rewritten
    #              null_disclosure) go red. It also pins washout_ctx_detail as a
    #              pure EXTRACTION: the bool projection must equal the pre-extraction
    #              washout_ctx on every input class, so the additive change to a
    #              graded organ cannot drift.
    #   episodes   the append-only episode ledger: hysteresis, <=2-session gap
    #              joining, 3-inactive closure, persistence math, the nightly lane
    #              gate, and the provisional-bucket law — a closed row is re-emitted
    #              VERBATIM from the store and its recomputed twin discarded, so a
    #              later data revision cannot rewrite a closed episode. It also pins
    #              site/basketdata/episodes.json, the web-readable projection of the
    #              ledger's CLOSED rows (a page cannot read parquet): the published
    #              rows must MATCH the frozen ledger rows and must exclude the
    #              provisional open one.
    #   tripwire   program law R-TIL-3: no fused score / rank / heat number anywhere
    #              in site/basketdata/pulse.json or episodes.json, at any depth, plus
    #              the 1:1 key join the surface indexes blind. It is a SUBSTRING
    #              scan and it carries its own seeded-mutant round trip — a scan that
    #              comes back clean is indistinguishable from a scan that no longer
    #              fires, so the green only means something because the synthetic
    #              insertion goes red.
    #
    # `pyarrow` is in the install line because the episode ledger is a parquet store;
    # `pyyaml` because lib/config.py reads config.yml at import. No `requests`: this
    # plane is an assembler over committed parquet and makes no network call.
    if: ${{ false }}  # executed inside ci-pack — NOT a disabled gate; see the ci-pack job.
    gate: code
    timeout-minutes: 10
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install minimal deps
        run: pip install pytest pandas numpy pyarrow pyyaml
      - name: group_pulse contract + episode ledger + no-fused-number tripwire (GR0)
        run: >-
          python -m pytest
          tests/test_group_pulse_contract.py
          tests/test_group_pulse_episodes.py
          tests/test_group_pulse_tripwire.py -q -rs

  group-read-surface:
    # GR1 of the Group Reads program: the USER-FACING basket read surfaces —
    # templates/basket_detail.html.j2's READ band (stance line, four glance tiles, the
    # arc rail, "what we're watching", the episode and earnings sections) and the
    # group-pulse column on the Sector Intelligence board.
    #
    # The suite EXECUTES the shipped JS under node rather than mirroring it in Python: a
    # Python re-implementation of the stance matrix would keep passing while the copy
    # that ships drifted away from it (the mirrored-guard-is-vacuous trap). `setup-node`
    # is therefore load-bearing, not decoration — the suite skips itself without node on
    # PATH, and a silent skip reads exactly like a pass.
    #
    # What goes red here:
    #   stance matrix   any (participation x sign x state_change x arc) combination that
    #                   emits an untranslated slug, an internal state name, "igniting",
    #                   "validated", refutation vocabulary, a missing or English-echoed
    #                   ZH string, a read over the Tier-1 word budget, a directional
    #                   act/buy stance (participation carries no directional authority,
    #                   DNR:KILL-PSS-SR3-PARTICIPATION), or a "moving together" claim
    #                   below the movers floor.
    #   earnings traps  the two group_earnings_pulse.v1 shapes that read plausibly and
    #                   are wrong: `season.next` is a preview CAPPED at 6, so the count
    #                   must come from n_upcoming_14d; and n_beat + n_miss can sit far
    #                   below n_members, so n_no_data is shown rather than divided away
    #                   into a survivor beat rate.
    #   null states     all three artifacts land only after the first post-merge nightly.
    #                   Every panel must render plain-word bilingual copy when its file
    #                   or key is absent, and every fetch must be fail-silent.
    #   surface law     the Oracle P8 null is disclosed verbatim in both languages; no
    #                   score/rank/heat number anywhere (R-TIL-3 / G0-2); the board's
    #                   alternative ordering is a disclosed rule over the artifact's own
    #                   named legs, printed on the surface it orders, with the existing
    #                   default sort untouched; and the desk's re-order resets the
    #                   show-more row cap it would otherwise leave pointing at the old
    #                   card positions.
    #
    # The surface probes import engine/group_pulse.py to exercise its published
    # denominator/arc contract, so NumPy and pandas are real import dependencies.
    # No pyarrow or network — it otherwise reads two templates, one plain-copy JS
    # asset, and shells node.
    if: ${{ false }}  # executed inside ci-pack — NOT a disabled gate; see the ci-pack job.
    gate: code
    timeout-minutes: 6
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
      - name: install minimal deps
        run: pip install pytest numpy pandas pyyaml
      - name: basket read band + board group-pulse column (GR1)
        run: python -m pytest tests/test_group_read_surface.py -q -rs

  group-earnings-pulse:
    # Group Reads W-GR2: engine/group_earnings.py -> site/basketdata/earnings_pulse.json,
    # the per-basket earnings layer (season clock, beat rollup, guidance band, revision
    # breadth, post-report drift, earnings sympathy) plus its append-only sympathy ledger.
    #
    # Both suites run against a SYNTHETIC data tree in tmp_path, so they are deterministic,
    # offline, and independent of how full the Nasdaq surprise drip happens to be on the
    # day. What they pin, in order of what would hurt most if it broke:
    #
    #   * the frozen group_earnings_pulse.v1 key set. The validator lives in the test, not
    #     the engine, and is itself mutation-checked: nine deliberate mutants (unknown key
    #     at three nesting depths, four dropped `n` fields, a dropped authority, a malformed
    #     `next` entry) must each make it go red, so a validator that quietly stopped
    #     validating fails here rather than shipping a silent contract break to the GR1
    #     surface builder.
    #   * every floor refusing with a null instead of a survivor-only number. The `thin`
    #     basket carries three members with perfectly good data — a real surprise, a real
    #     analyst reading, a real post-report price path — and must publish nothing but
    #     nulls, because three is below MIN_REPORTED / MIN_COVERED / MIN_DRIFT_N. A
    #     3-of-3 beat share is exactly the number the floors exist to suppress.
    #   * the resolution-conditioned-denominator law: n_beat + n_miss + n_inline +
    #     n_no_data == n_members in every basket, refused or not.
    #   * engine.guidance_gap's >=2-distinct-filers law surviving the generalization from
    #     curated themes to arbitrary basket rosters (one filer -> band null), and the
    #     floor being IMPORTED rather than re-declared so the two can never drift.
    #   * the sympathy ratio, on a fixture where the answer is arithmetic. That fixture is
    #     deliberately MAJORITY report days: the median is robust, so a contaminated
    #     baseline built from a window with only a few report days still reads correct and
    #     the bug hides. At 24 report days against 16 quiet ones a baseline that forgets to
    #     exclude them collapses the ratio from 3.0 to 1.0 — verified by mutation.
    #   * the ledger's immutability: re-advancing identical inputs is a byte-for-byte no-op,
    #     a restated value for an existing key is refused rather than applied, and the
    #     nightly lane gate (engine.ledger_lane) is imported, not a local COLLECT_LANE read.
    #
    # `requests` is in the install line because engine/group_earnings.py's Item-2.02 check
    # prefers collectors.edgar_earnings_8k.has_item_202 and falls back to a local copy when
    # that module (which imports requests at module scope) is unavailable. With requests
    # installed the equivalence test compares the two definitions for real; without it the
    # test would skip and the fallback could drift unnoticed.
    if: ${{ false }}  # executed inside ci-pack — NOT a disabled gate; see the ci-pack job.
    gate: code
    timeout-minutes: 10
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install minimal deps
        run: pip install pytest pandas numpy pyarrow pyyaml requests
      - name: group earnings pulse contract, floors and sympathy (W-GR2)
        run: >-
          python -m pytest
          tests/test_group_earnings_contract.py
          tests/test_group_earnings_sympathy.py -q -rs

  group-linked-outsiders:
    # GR3 of the Group Reads program: filing-linked outsiders per US basket.
    #
    # These assertions are the whole guard on an artifact that publishes a named
    # relationship between two public companies.  The resolver is deliberately
    # strict — unique near-verbatim registrant match only, no substring, no edit
    # distance — and the admissibility matrix here is what stops a later "just
    # loosen it a little" from shipping a wrong edge between two real tickers.
    # The contract tests pin the frozen v1 key set (an unknown key is a
    # violation, not a warning), the closed relationship vocabulary (no
    # customer/supplier/partner/competitor role label the filing cannot ground),
    # the append-only ledger's immutability, and the honest-null paths that keep
    # a missing pulse.json or a missing tape from either crashing the nightly or
    # printing a state it cannot support.
    if: ${{ false }}  # executed inside ci-pack — NOT a disabled gate; see the ci-pack job.
    gate: code
    timeout-minutes: 10
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install minimal deps
        run: pip install pytest pandas numpy pyarrow pyyaml
      - name: group linked outsiders — contract, resolver, states, ledger (GR3)
        run: >-
          python -m pytest
          tests/test_group_linked_outsiders.py -q -rs

  falsifier-tripwires:
    # The stateful cycle-ontology falsifier engine was triggerable but its focused
    # regression suite was named by no CI command (#5194).  Keep this as a small
    # first-class owner so path-aware selection can remove unrelated packs without
    # making the exact changed behavior dark.  The alert-ZH completeness contract
    # shares the same falsifier engine and ontology inputs, so it stays in this
    # owner as a distinct step instead of adding a second overlapping job that
    # defeats the representative-diff scope ceiling. Price-pressure's inferred
    # ownership closure overlaps this owner too, so its focused suite is a third
    # distinct step here rather than another always-selected logical job.  TOP
    # ANATOMY's point-in-time research engine and the inert Winner Health design
    # freeze are likewise folded in as attributable steps: the dependency install
    # is already a strict superset, and keeping one selected owner preserves the
    # representative-diff job ceiling without leaving either suite dark.
    if: ${{ false }}  # executed inside ci-pack — NOT a disabled gate.
    gate: data
    paths:
      - data/alerts/alerts_log.parquet
      - data/cycle_ontology/falsifiers.json
      - data/cycle_ontology/falsifiers_retired.json
      - data/cycle_ontology/tripwire_state.json
      - data/top_anatomy/library.parquet
      - data/top_anatomy/thresholds.json
      - data/top_maturation/latest.json
      - engine/alerts.py
      - engine/falsifier_tripwires.py
      - engine/i18n.py
      - engine/top_maturation.py
      - scripts/build_top_maturation.py
      - scripts/build_vector.py
      - scripts/build_winner_health_page.py
      - scripts/export_top_anatomy_library.py
      - tests/test_alert_zh_completeness.py
      - tests/test_falsifier_tripwires.py
      - tests/test_json_strict.py
      - tests/test_massive_local_staleness.py
      - tests/test_price_pressure.py
      - tests/test_top_anatomy.py
      - tests/test_top_anatomy_oot_receipt.py
      - tests/test_top_maturation.py
      - tests/test_winner_health_design_freeze.py
      - tests/test_winner_health_page.py
      - templates/winner_health.html.j2
    timeout-minutes: 12
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install falsifier, alert-zh, and price-pressure test dependencies
        run: pip install pytest pandas numpy pyarrow pyyaml jinja2 plotly requests beautifulsoup4 openpyxl scikit-learn fastapi httpx
      - name: falsifier tripwire state and v2 gate contracts
        run: python -m pytest tests/test_falsifier_tripwires.py -q
      # The zh half of every macro alert body must be fully Chinese. Six rules
      # interpolated an English-valued token straight into message_zh (#4700), and
      # the cycle-falsifier body quoted a curator-written English thesis (#4712).
      # TZ=UTC is inline because env is not an allowed packed-manifest step key.
      - name: zh alert bodies fully Chinese — emitter vs backlog translator vs baked rows
        run: TZ=UTC python -m pytest tests/test_alert_zh_completeness.py -q
      # W1 of the Dislocation & Recovery program (DRL): engine/price_pressure/, the
    # single-name residual-shock detector and its point-in-time event ledger, plus
    # the display artifact and the market-packet block that read them.
    #
    # WHY THIS SUITE IS WORTH A JOB. The lobe imports its whole construction from
    # scripts/research_liquidity_shock_reversal.py, because the frozen base rates
    # it publishes were measured THERE — a re-typed copy of the residual math would
    # drift silently and quietly invalidate every published table. The parity test
    # calls BOTH paths on one synthetic fixture and compares them field by field,
    # so the seam cannot be swapped for a lookalike without going red.
    #
    # The rest of the suite pins the honesty invariants a display-tier ledger dies
    # without: the identity block does not move when t0+1 bars are POISONED; a
    # horizon field is null until its window has fully elapsed; there is NO early
    # close (a 100% retrace on day 3 still grades at 21d and 60d — closing on the
    # good news is upside-only path censoring); a name whose bars stop grades
    # DELISTED_OR_HALTED *inside* the terminal denominator; the nightly-lane gate
    # actually gates the write; era stamps (backfill / gap / forward) are never
    # rewritten and a backfill refuses to touch accrued forward rows; and every
    # emitted list is recency-then-ticker, never |z| — ordering by size of move
    # would be the ranking the artifact's authority block says it cannot do.
    #
    # Everything runs on a SYNTHETIC panel in tmp_path: CI has no
    # data/massive_stock_day (20k vendor parquets, R2-homed), and a test that
    # needed the real store would be a test that never runs.
    #
    # `pyarrow` is in the install line because the ledger is a parquet store;
    # `pyyaml` because lib/config.py reads config.yml at import. No `requests`:
      # this lobe is an assembler over committed stores and makes no network call.
      # The shared install is a strict superset of those wheels and this owner's
      # 12-minute ceiling preserves the former standalone suite's 10-minute bound.
      - name: price-pressure detector, PIT ledger, base rates and artifact (DRL W1)
        run: >-
          python -m pytest
          tests/test_price_pressure.py -q -rs
      # Synthetic-only research guard: labels may look forward, but every feature
      # is point-in-time and all frozen thresholds remain tied to the preregistered
      # Phase-0 contract.  The derived scope follows its engine/script/read closure.
      - name: TOP ANATOMY point-in-time engine and preregistration contracts
        run: python -m pytest tests/test_top_anatomy.py -q
      # Wired 2026-09-02: tests/test_top_anatomy_oot_receipt.py shipped in #6723
      # named by NO run: step in any workflow (contract-delta unrun-suite
      # finding, inherited on main at dae473ed625e), so the guard over the
      # FROZEN TOPA OOT prereg (research/top_anatomy/TOPA_OOT_PREREG.md —
      # STRICT session-arithmetic boundary, per-cell eligibility, separate
      # episode- and day-level labeled-unit blocks) sat dark from the day it
      # merged. It belongs to this owner: the suite drives
      # scripts/research_top_anatomy_oot_receipt.py and the Phase-0 harness
      # over synthetic massive_stock_day stores in tmp_path (CI has no real
      # data/ tree) through the same engine/top_anatomy closure the step above
      # owns, and the shared install is a strict superset of its
      # pandas/numpy/pyarrow needs. Path-gated in ci.yml on BOTH the test and
      # its subjects (scripts/research_top_anatomy_oot_receipt.py,
      # scripts/research_top_anatomy_phase0.py, engine/top_anatomy.py) — a
      # guard the guarded change cannot trigger is not a guard (#3488).
      - name: TOP ANATOMY OOT receipt — frozen prereg schema and boundary contracts
        run: python -m pytest tests/test_top_anatomy_oot_receipt.py -q
      # The same harness's JSON write path, so it belongs to the same owner rather
      # than a second overlapping job. `default=str` does NOT catch NaN — it only
      # fires for values the encoder cannot serialize — so three shipped
      # top_anatomy summaries carried bare `NaN` tokens that no strict parser will
      # read. This pins the sanitizer and that every summary emitter routes through
      # the one write path; an unregistered encoding guard is a dead guard.
      - name: strict-JSON research summary encoding — non-finite sanitizer and emitter
        run: python -m pytest tests/test_json_strict.py -q
      # Local research entrypoints must refuse a massive_stock_day mirror frozen
      # past the declared trading-session ceiling while staying inert in Actions.
      # The packed selector derives this suite's full import/path-literal closure,
      # so collector, calendar, and all four wired entrypoint edits select this owner.
      - name: massive-stock-day local mirror staleness tripwire
        run: python -m pytest tests/test_massive_local_staleness.py -q
      # Nightly display-only consumer of the frozen TOP ANATOMY library. Keep
      # its point-in-time state machine and generated page beside the research
      # owner so changes to either side cannot leave the shipped read dark.
      - name: Winner Health nightly maturation and render contracts
        run: >-
          TZ=UTC python -m pytest
          tests/test_top_maturation.py
          tests/test_winner_health_page.py -q
      # Source-template render only; templates/winner_health.html.j2 is declared
      # above because Jinja's runtime template lookup is intentionally not inferred
      # as a Python import edge.
      - name: Winner Health inert surface design freeze
        run: python -m pytest tests/test_winner_health_design_freeze.py -q

  options-nbbo-cohort:
    # Frozen same-basis MomoEdge comparison infrastructure. The suite is fully
    # offline: source responses and authenticated producer rows are synthetic,
    # while raw production bytes remain host-private outside git.
    if: ${{ false }}  # executed inside ci-pack — NOT a disabled gate.
    gate: code
    paths:
      - config/dag.yml
      - contracts/options/options.prospective_nbbo_cohort.v1.schema.json
      - docs/runbooks/OPTIONS_NBBO_COHORT.md
      - engine/options_nbbo_cohort.py
      - engine/session_digest.py
      - lib/nyse_calendar.py
      - ops/launchd/com.mastermind.optionsnbbocohort.plist
      - ops/launchd/run_options_nbbo_cohort_loop.sh
      - scripts/capture_options_nbbo_cohort.py
      - tests/test_options_nbbo_cohort.py
      - tests/test_options_nbbo_cohort_deploy.py
    timeout-minutes: 8
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install minimal deps
        run: pip install pytest jsonschema pyyaml
      - name: private OPRA NBBO cohort, capture coverage, and launchd contract
        run: >-
          python -m pytest
          tests/test_options_nbbo_cohort.py
          tests/test_options_nbbo_cohort_deploy.py -q -rs

  product-experience-registry:
    if: ${{ false }}
    gate: code
    # HANDOFF C (Operation Institutionalize), half one: the cross-repo page
    # registry census. Pure pytest, no browser anywhere near it.
    #
    # This job's inferred scope is HONESTLY broad: the builder censuses the real
    # site/ and templates/ trees, so a tree traversal is what it is FOR and the
    # analyzer's wide fallback is correct here. It is split from the capture half
    # precisely so that breadth cannot be inherited by a suite that does not need
    # it — one job unioning both scopes is what pushed the ci_pack narrow-diff
    # contract over its threshold (tripwires selected 145 of 181 jobs, cap 144).
    timeout-minutes: 10
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install product-experience test deps
        run: pip install pytest pyyaml
      - name: page registry — committed-JSON drift guard + derivation contracts
        run: TZ=UTC python -m pytest tests/test_build_product_page_registry.py -q
      # DS-PR-1 suites, appended rather than given their own manifest entry: a
      # 181st job reds tests/test_ci_pack.py's narrow-diff ceiling, and both
      # suites' scopes (templates/**, the registry builder, the committed JSON)
      # are already inside this job's honestly-broad derived scope.
      - name: design-system — registry schema + archetype vocabulary
        run: TZ=UTC python -m pytest tests/test_page_registry_design_system.py -q
      - name: design-system — ratchet rules, modes and annotation shape
        run: TZ=UTC python -m pytest tests/test_check_design_system.py -q

  product-experience-capture:
    if: ${{ false }}
    gate: code
    # HANDOFF C (Operation Institutionalize), half two: the browser evidence
    # capture harness. The suite is pure pytest with an injected fake driver —
    # playwright is deliberately NOT installed here and must never be: a missing
    # browser is a nonzero "verifier_unavailable" outcome locally, and CI proves
    # only the driver-independent contracts.
    #
    # LOAD-BEARING: scripts/capture_page_evidence.py contains NO subprocess call
    # and NO filesystem-enumeration call (glob/iterdir/listdir/walk/scandir). Both
    # are opaque edges to scripts/ci_scope_dependencies.py, which then widens this
    # job to every scan root the module names — data/** included — and re-reds
    # tests/test_ci_pack.py::test_representative_narrow_diffs_skip_at_least_one_quarter_of_jobs.
    # Reintroducing one (a `git rev-parse` for provenance, a `glob("*.png")` in the
    # self-check) breaks ci_pack packing, not this suite: it will still pass here.
    timeout-minutes: 10
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install product-experience test deps
        run: pip install pytest pyyaml
      - name: evidence capture — fake-driver harness contracts
        run: TZ=UTC python -m pytest tests/test_capture_page_evidence.py -q

  momoedge-browser-observe:
    scope: exclusive
    # Observe-only authenticated-page companion. Tests are synthetic/static and
    # never contact MomoEdge, inspect browser authentication, or write cohort
    # coverage. Exact source bytes remain host-private outside git.
    if: ${{ false }}  # executed inside ci-pack — NOT a disabled gate.
    gate: code
    paths:
      - .github/ci/legacy-jobs.yml
      - .github/workflows/ci.yml
      - browser/momoedge_capture/
      - config/dag.yml
      - contracts/options/options.momoedge_browser_observation.v1.schema.json
      - contracts/options/options.momoedge_browser_observe_journal.v1.schema.json
      - docs/runbooks/MOMOEDGE_BROWSER_COMPANION.md
      - engine/options_momoedge_browser_adapter.py
      - ops/native_messaging/com.mastermind.optionsnbbocohort.momoedge_observe.json
      - ops/native_messaging/run_momoedge_browser_host.sh
      - scripts/momoedge_browser_receiver.py
      - tests/test_momoedge_browser_adapter.py
      - tests/test_momoedge_capture_deploy.py
      - tests/test_momoedge_capture_extension_static.py
    timeout-minutes: 8
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
      - name: install minimal deps
        run: pip install pytest jsonschema pyyaml
      - name: observe-only browser, private receiver, and durability contracts
        run: >-
          python -m pytest
          tests/test_momoedge_browser_adapter.py
          tests/test_momoedge_capture_extension_static.py
          tests/test_momoedge_capture_deploy.py -q -rs

  mastermindx-selector-capture-attestation:
    # Capture-only, globally inactive selector-registration attestation. The
    # tests are synthetic and cannot arm a selector, inspect candidate data,
    # write private evidence, or dispatch any workflow.
    if: ${{ false }}  # executed inside ci-pack — NOT a disabled gate.
    gate: code
    paths:
      - .github/ci/legacy-jobs.yml
      - .github/workflows/ci.yml
      - contracts/options/options.mastermindx_selector_capture_attestation.v1.schema.json
      - engine/__init__.py
      - engine/options_mastermindx_capture.py
      - engine/session_digest.py
      - lib/__init__.py
      - lib/nyse_calendar.py
      - research/options_estate/sparse_selector_preregistration_receipt_v1.json
      - tests/test_options_mastermindx_capture.py
    timeout-minutes: 8
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install minimal deps
        run: pip install pytest jsonschema
      - name: frozen MastermindX selector capture-only attestation contracts
        run: >-
          PYTHONDONTWRITEBYTECODE=1 python -m pytest
          tests/test_options_mastermindx_capture.py -q -rs

  reference-integrity:
    if: ${{ false }}  # executed inside ci-pack — NOT a disabled gate; see the ci-pack job.
    gate: code
    # design.reference_integrity PR gate (RIG V1, research/REFERENCE_INTEGRITY_GATE_V1.md).
    # Stops REFERENCE LAUNDERING: a bad product decision becomes a reference, the reference
    # becomes law, builders reproduce it faithfully, reviewers compare against it instead of
    # against production, CI says compliant, and the regression is institutionalized.
    # CI cannot judge taste and does not try — it proves the judgment HAPPENED (dual
    # independent critics, rationale quarantine, frozen first pass, verdict packet, approval
    # receipt) and left auditable receipts. Unscoped on purpose: L7/L8/L9 are namespace and
    # coupling closures over mockups/design_system, research/migration_packets and the page
    # registry, so a diff that adds a file anywhere in those roots must re-run this.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install minimal deps
        # jinja2 is needed ONLY for the XPV2-SC-R3A step below, which imports
        # scripts.build_sector_central::split_actnow (a pure function -- no
        # site/data I/O) to recompute the premium-split panel from the frozen
        # fixture instead of hand-typing the expected counts (S3 fix,
        # 2026-08-20 adversarial review). build_sector_central.py does
        # `from jinja2 import Environment, FileSystemLoader` at module scope,
        # so importing any name from it -- even a pure helper -- requires
        # jinja2 in this job's venv even though the RIG steps above never use it.
        run: pip install pyyaml pytest jinja2
      - name: reference-integrity selftest (gate fires on synthetic violations)
        run: python3 scripts/check_reference_integrity.py --selftest
      - name: RIG artifact integrity across the checkout (hard gate)
        run: python3 scripts/check_reference_integrity.py
      - name: reference-integrity mutation / anti-vacuity suite
        run: python -m pytest tests/test_check_reference_integrity.py -q
      # XPV2-SC-R3A binding pack (research/reference_integrity/mastermind-xpv2-sector-r3/):
      # attack tests over the frozen Sector Central fixture + code constants
      # (_ACTNOW_LANES, LEGACY_ANCHORS, thin-but-listed wording, premium
      # preview/locked/total split, the Overview stale guard, split_actnow
      # imported live). Per ADJUDICATIONS.md SS A10, this suite asserts ONLY
      # against the frozen fixture and code constants -- never live site/ or
      # data/ -- so it sits with this job's own reference-integrity mandate
      # (a frozen reference used faithfully, not a moving target). Unscoped
      # job, no `paths:` needed -- runs on every PR.
      - name: XPV2-SC-R3A binding pack — fixture/routing/access mutation-armor
        run: python -m pytest tests/test_xpv2_sector_r3_fixture.py -q
  dataos-foundation:
    scope: exclusive
    # Mastermind Data OS foundation library (lib/dataos/**) — the canonical identity,
    # temporal, price-basis, null, registry and quality spine every later Data OS wave
    # builds on. WITHOUT THIS JOB the six suites are dead code: there is no broad
    # `pytest tests/` anywhere in CI, so a test file no `run:` step names never runs,
    # and the vocabulary would drift from the incidents it encodes with nothing going
    # red. That is not hypothetical here — lib/dataos/nulls.py shipped in the first
    # pass with no suite AND no `run:` entry, and a mutation deleting §D6's headline
    # law outright ("0 may never mean absence" -> `if False:`) failed NOTHING. Every
    # name in the `run:` step below is load-bearing; adding a suite to tests/ without
    # adding it HERE ships it dark. What it pins, concretely: the MMC->MRSH
    # alias table answering DIFFERENTLY
    # either side of 2026-01-14 (the rename that left data/baskets/ohlcv/MMC.parquet
    # nonexistent for seven months while the `insurance` basket quietly rendered
    # 18/19 members); the fail-closed PIT law, whose whole failure mode is a SILENT
    # latest-vintage return; the price-basis naming law and the measured NVDA
    # 2024-06-03 store bases (114.80 / 114.80 / 115.00 / 1150.00, all called some form
    # of "close"); and that config/dataset_registry.yml still validates clean.
    #
    # PURE UNIT TESTS BY CONSTRUCTION: nothing here reads data/, which is not
    # materialized in an agent worktree, so the suite behaves identically on a full
    # checkout and in this thin lane. Deps are pytest + pyyaml only — the library
    # imports stdlib-only and pulls yaml lazily inside registry.load_registry, so a
    # pandas/numpy import creeping into lib/dataos/ breaks HERE first.
    if: ${{ false }}  # executed inside ci-pack — NOT a disabled gate.
    gate: code
    paths:
      - config/delisted_symbols.yml
      # issuer_group_allowlist: read by scripts/build_security_master.py since the
      # D2 Identity Atlas merge (#5932) — the import-closure contract
      # (test_curated_exclusive_scopes_cover_their_own_import_closure) requires the
      # declared scope to cover it or this job goes dark for edits to it.
      - config/issuer_group_allowlist.yml
      - lib/__init__.py
      - lib/config.py
      - lib/delisted_symbols.py
      - lib/ticker_aliases.py
      - scripts/build_security_master.py
      # V4-D2B2-CN-HK: build_security_master.py's import closure names the
      # binding spec path as a string; contract-delta requires declared paths
      # to cover it (widening is the safe direction).
      - research/prophet_v4/d2/D2B2_CN_HK_FROZEN_CONTRACT_2026-08-20.md
      # V4-D2B2-US: same shape — the us_gmi_admission receipt block names its
      # binding contract path as a string.
      - research/prophet_v4/d2/D2B2_US_FROZEN_CONTRACT_2026-08-21.md
      - .github/ci/legacy-jobs.yml
      - .github/workflows/ci.yml
      - config/dataset_registry.yml
      - lib/dataos/**
      - tests/test_dataos_identity.py
      - tests/test_dataos_nulls.py
      - tests/test_dataos_price.py
      - tests/test_dataos_quality.py
      - tests/test_dataos_registry.py
      - tests/test_dataos_temporal.py
    timeout-minutes: 8
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install minimal deps
        run: pip install pytest pyyaml
      - name: Data OS identity / temporal / price-basis / nulls / registry / quality contracts
        run: >-
          PYTHONDONTWRITEBYTECODE=1 python -m pytest
          tests/test_dataos_identity.py
          tests/test_dataos_temporal.py
          tests/test_dataos_price.py
          tests/test_dataos_nulls.py
          tests/test_dataos_registry.py
          tests/test_dataos_quality.py -q -rs

  dataos-identity-seams:
    # DOS-1.2 — the identity seam gate. `config/identity_seams.yml` declares every surface
    # in this tree that answers "which security is this?", classified ADOPT /
    # ALIAS-INTO-MASTER / KNOWINGLY-DIFFERENT / RETIRE against the DOS-1.1 master, and this
    # suite makes an ADOPT-class DISAGREEMENT fail.
    #
    # WHY IT IS A SEPARATE JOB FROM `dataos-foundation`: that lane is deliberately thin
    # (pytest + pyyaml only) so a pandas/numpy import creeping into lib/dataos/ breaks there
    # FIRST. This suite must read the committed master parquet, so it needs pandas+pyarrow —
    # putting it in the thin lane would destroy that tripwire. Two jobs, two purposes.
    #
    # WITHOUT THIS JOB the registry is prose. The cost of an ungoverned seam is already
    # measured in this repo: MMC->MRSH lived in scripts/fetch_basket_extras but not in its
    # sibling scripts/fetch_basket_ohlcv, so data/baskets/ohlcv/MMC.parquet never existed and
    # for seven months the `insurance` basket rendered 18/19 members and
    # us_sector_financials 75/76 — and NOTHING WENT RED. This suite is the thing that goes red.
    #
    # The gate is mutation-proven, not asserted: making engine/ledger_identity disagree with
    # the master fails the agreement test; blanking a KNOWINGLY-DIFFERENT `reason` fails the
    # schema test; deleting a registry row fails completeness; and silently "fixing" the
    # declared GOOG/GOOGL divergence in config/share_class_equiv.yml fails the divergence
    # test. Non-emptiness guards are explicit, because an agreement gate over zero securities
    # is green forever and means nothing.
    if: ${{ false }}  # executed inside ci-pack — NOT a disabled gate.
    gate: code
    paths:
      - .github/ci/legacy-jobs.yml
      - .github/workflows/ci.yml
      - config.yml
      - config/identity_seams.yml
      - config/share_class_equiv.yml
      - config/theme_graph_identity_breaks.yml
      - config/biocatalyst_sponsor_ticker_map.yml
      - config/us_search_aliases_zh.json
      - config/delisted_symbols.yml
      - data/reference/**
      - lib/dataos/identity.py
      - lib/ticker_aliases.py
      - lib/delisted_symbols.py
      - lib/symbol_directory_receipts.py
      - engine/ledger_identity.py
      - engine/entity_resolver.py
      - engine/name_resolver.py
      - collectors/edgar_deadnames.py
      - collectors/china_universe.py
      - collectors/china_ths_concepts.py
      - collectors/china_analyst.py
      - collectors/china_tushare_spine.py
      - tests/test_identity_seam_agreement.py
    timeout-minutes: 8
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install deps
        # pandas+pyarrow: the suite reads the committed security_master/vendor_aliases
        # parquet through lib.dataos.identity.VendorAliasTable. pyyaml: the registry itself.
        run: pip install pytest pyyaml pandas pyarrow
      - name: identity seams — ADOPT surfaces must agree, divergences must stay declared
        run: >-
          PYTHONDONTWRITEBYTECODE=1 python -m pytest
          tests/test_identity_seam_agreement.py -q -rs

  no-client-supplied-identity:
    if: ${{ false }}
    gate: code
    # WS-9 / GATE-6: route handlers must not take user identity from the client.
    # _pg() uses SUPABASE_SERVICE_ROLE_KEY (bypasses RLS); token-derived identity
    # is the only cross-tenant fence. Scoped to app/** — an app-only PR must
    # select this job, a research-only PR must not.
    paths:
      - app/**
      - scripts/check_no_client_supplied_identity.py
      - tests/test_check_no_client_supplied_identity.py
    timeout-minutes: 8
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install minimal deps
        run: pip install pytest
      - name: no-client-supplied-identity selftest (gate fires on planted violations)
        run: python3 scripts/check_no_client_supplied_identity.py --selftest
      - name: no-client-supplied-identity guard (hard fail on client-sourced user_id)
        run: python3 scripts/check_no_client_supplied_identity.py
      - name: no-client-supplied-identity test suite
        run: python3 -m pytest tests/test_check_no_client_supplied_identity.py -q

  dislocation-p0-a1-blind-harvest:
    scope: exclusive
    # P0-A1 price-blind harvest and A1R source-integrity primitives (Turn-5
    # lexicon/query ledger, canonical SEC-owner adapters, source-only semantic
    # contracts, firewall, quota walk). Every focused suite stays named by the
    # run step below so contract-delta cannot admit an unowned repair test.
    if: ${{ false }}  # executed inside ci-pack — NOT a disabled gate.
    gate: code
    paths:
      - .github/ci/legacy-jobs.yml
      - .github/workflows/ci.yml
      - scripts/research/dislocation_p0_a1_harvest.py
      - scripts/research/dislocation_p0_a1_lib.py
      - scripts/research/dislocation_p0_a1r.py
      - scripts/research/dislocation_p0_a1r_evidence_catalog.py
      - scripts/research/dislocation_p0_a1r_owner_run.py
      - scripts/research/dislocation_p0_a1r_semantic_contract.py
      - scripts/research/dislocation_p0_a1r_semantic_run.py
      - scripts/research/dislocation_p0_a1r_source_run.py
      - scripts/research/dislocation_p0_s1f_finalize_measurement.py
      - scripts/research/dislocation_p0_s1f_measurement.py
      - scripts/research/dislocation_p0_s1f_model_transport.py
      - scripts/research/dislocation_p0_s1f_owner_run.py
      - scripts/research/dislocation_p0_s1f_runner.py
      - scripts/research/dislocation_p0_s1f_selection.py
      - scripts/research/dislocation_p0_s1f_semantic_contract.py
      - scripts/research/dislocation_p0_s1f_shadow_triage.py
      - scripts/research/dislocation_p0_s1f_triage.py
      - scripts/research/dislocation_p0_source_adapter.py
      - scripts/research/dislocation_p0_source_materializer.py
      - collectors/edgar_forensics.py
      - collectors/fundamental_forensics_acquisition.py
      - collectors/fundamental_forensics_companyfacts.py
      - collectors/sec_document_spine.py
      - contracts/fundamental_forensics_broad_sec_issuer_manifest.schema.json
      - engine/fundamental_forensics/broad_sec_store.py
      - engine/fundamental_forensics/models.py
      - engine/fundamental_forensics/sec_document_spine.py
      - engine/research_vault/r2_store.py
      - lib/__init__.py
      - lib/config.py
      - tests/__init__.py
      - tests/test_dislocation_p0_a1_blind_harvest.py
      - tests/test_dislocation_p0_a1r_evidence_catalog.py
      - tests/test_dislocation_p0_a1r_owner_run.py
      - tests/test_dislocation_p0_a1r_selection.py
      - tests/test_dislocation_p0_a1r_semantic_contract.py
      - tests/test_dislocation_p0_a1r_semantic_run.py
      - tests/test_dislocation_p0_source_adapter.py
      - tests/test_dislocation_p0_source_materializer.py
      - tests/test_dislocation_p0_s1f.py
      - tests/test_dislocation_p0_s1f_finalize_measurement.py
      - tests/test_dislocation_p0_s1f_model_transport.py
      - tests/test_dislocation_p0_s1f_owner_run.py
      - tests/test_dislocation_p0_s1f_shadow_triage.py
      - research/dislocation_intelligence/**
    timeout-minutes: 8
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install source-owner test deps
        # The bounded A1R/S1F adapters import the canonical raw SEC owner,
        # including its Draft202012Validator boundary; these focused tests
        # still perform no network call.
        run: pip install pytest pandas requests pyyaml scipy jsonschema==4.26.0
      - name: Dislocation P0-A1 price-blind source-integrity primitives
        run: >-
          python -m pytest
          tests/test_dislocation_p0_a1_blind_harvest.py
          tests/test_dislocation_p0_a1r_evidence_catalog.py
          tests/test_dislocation_p0_a1r_owner_run.py
          tests/test_dislocation_p0_a1r_selection.py
          tests/test_dislocation_p0_a1r_semantic_contract.py
          tests/test_dislocation_p0_a1r_semantic_run.py
          tests/test_dislocation_p0_source_adapter.py
          tests/test_dislocation_p0_source_materializer.py
          tests/test_dislocation_p0_s1f.py
          tests/test_dislocation_p0_s1f_finalize_measurement.py
          tests/test_dislocation_p0_s1f_model_transport.py
          tests/test_dislocation_p0_s1f_owner_run.py
          tests/test_dislocation_p0_s1f_shadow_triage.py
          -q

  unrun-cell-b-v2-buyback-census:
    # Owns scripts/cell_b_v2_buyback_source_census.py — the price-blind Cell B
    # V2 buyback source-capacity census. Dedicated job because no existing
    # cell-B/buyback owner exists on the pack runner.
    if: ${{ false }}
    gate: code
    timeout-minutes: 5
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install minimal deps
        run: pip install pytest pandas numpy pyarrow pyyaml jinja2 plotly requests beautifulsoup4 openpyxl scikit-learn
      - name: Cell B V2 buyback source-capacity census
        run: python -m pytest tests/test_cell_b_v2_buyback_source_census.py -q

  market-os-macro-workspaces:
    if: ${{ false }}  # executed inside ci-pack; keep the legacy status definition runner-free
    gate: code
    # Market Ontology F01 R1A: mastermind.macro_workspace_snapshot.v1 producer
    # (US Liquidity Regime composer), fail-closed contract validator, atomic
    # publisher, CLI exit-code law, and the bounded read-only machine consumer.
    # All four suites are offline and synthetic-fixture driven; the only real
    # input ever read at build time is data/regime/latest.json. This lane has
    # no trading, alerting, routing, or downstream authority.
    paths:
      - "contracts/market_os/macro_workspace_snapshot.v1.schema.json"
      - "engine/market_os/**"
      - "scripts/build_macro_workspaces.py"
      - "tests/test_macro_workspace_build.py"
      - "tests/test_macro_workspace_business_activity.py"
      - "tests/test_macro_workspace_capital_structure.py"
      - "tests/test_macro_workspace_consumer.py"
      # R6 2026-09-04: Consumer & Household + National Debt composer suites.
      - "tests/test_macro_workspace_consumer_payments.py"
      - "tests/test_macro_workspace_contract.py"
      - "tests/test_macro_workspace_financial_conditions.py"
      - "tests/test_macro_workspace_growth.py"
      - "tests/test_macro_workspace_housing.py"
      - "tests/test_macro_workspace_inflation.py"
      - "tests/test_macro_workspace_labor.py"
      - "tests/test_macro_workspace_liquidity_central_banks.py"
      - "tests/test_macro_workspace_liquidity_regime.py"
      - "tests/test_macro_workspace_monetary_policy.py"
      - "tests/test_macro_workspace_national_debt.py"
      # Expansion 2026-09-04: Rates & Curves + Trade Flows composer suites.
      - "tests/test_macro_workspace_rates_curves.py"
      - "tests/test_macro_workspace_trade_flows.py"
    timeout-minutes: 8
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install workspace-producer test dependencies
        run: pip install pytest pyyaml jsonschema==4.26.0
      - name: Market OS macro workspaces — contract, composer, publisher, consumer
        run: >-
          python -m pytest
          tests/test_macro_workspace_build.py
          tests/test_macro_workspace_business_activity.py
          tests/test_macro_workspace_capital_structure.py
          tests/test_macro_workspace_consumer.py
          tests/test_macro_workspace_consumer_payments.py
          tests/test_macro_workspace_contract.py
          tests/test_macro_workspace_financial_conditions.py
          tests/test_macro_workspace_growth.py
          tests/test_macro_workspace_housing.py
          tests/test_macro_workspace_inflation.py
          tests/test_macro_workspace_labor.py
          tests/test_macro_workspace_liquidity_central_banks.py
          tests/test_macro_workspace_liquidity_regime.py
          tests/test_macro_workspace_monetary_policy.py
          tests/test_macro_workspace_national_debt.py
          tests/test_macro_workspace_rates_curves.py
          tests/test_macro_workspace_trade_flows.py
          -q

  market-os-macro-suite-pages:
    if: ${{ false }}  # executed inside ci-pack; keep the legacy status definition runner-free
    gate: code
    # Market Ontology F01 R1B: the macro-native workspace page family — shared
    # suite shell, closed-vocab EN/zh labels, snapshot->view model, and the
    # registry-driven fail-closed page builder. Render-only: reads the
    # published site/macrodata/workspaces artifacts, writes site/*.html, and
    # renders a typed refusal rather than inventing state. No trading,
    # alerting, routing, or downstream authority. engine/market_os/** is in
    # scope deliberately: a composer change alters the artifacts these pages
    # render, so it should re-prove the page suite.
    #
    # CURATED SCOPE (2026-09-04, at birth). `scope: exclusive` so the paths
    # below REPLACE inference: the builder's jinja FileSystemLoader is an
    # opaque construct whose fallback smear claimed `templates/**` and put
    # templates/index.html at 132 jobs against the 131 ceiling in
    # tests/test_ci_pack.py::test_exclusive_curation_narrows_ordinary_code_prs.
    # The job reads five named templates, not the tree. Widening is the safe
    # direction: when a new import lands, the closure-coverage audit names the
    # exact uncovered file — add it here.
    scope: exclusive
    paths:
      - "contracts/market_os/macro_workspace_snapshot.v1.schema.json"
      - "engine/__init__.py"
      - "engine/market_os/**"
      - "templates/_macro_suite_shell.html.j2"
      - "templates/macro_liquidity_regime.html.j2"
      # R2 pages wave 2026-09-04: the six MCS/cycle workspace pages.
      - "templates/macro_growth_real_economy.html.j2"
      - "templates/macro_business_activity.html.j2"
      - "templates/macro_labor_markets.html.j2"
      - "templates/macro_inflation_system.html.j2"
      - "templates/macro_monetary_policy.html.j2"
      - "templates/macro_financial_conditions.html.j2"
      # R3 2026-09-04: the Liquidity & Central Banks page.
      - "templates/macro_liquidity_central_banks.html.j2"
      # R4/R5 2026-09-04: Capital Structure + Housing pages.
      - "templates/macro_capital_structure.html.j2"
      - "templates/macro_housing_real_estate.html.j2"
      # R6 2026-09-04: Consumer & Household + National Debt pages.
      - "templates/macro_consumer_payments.html.j2"
      - "templates/macro_national_debt_liabilities.html.j2"
      # Expansion 2026-09-04: Rates & Curves + Trade Flows pages.
      - "templates/macro_rates_curves.html.j2"
      - "templates/macro_trade_flows.html.j2"
      - "templates/macro_suite.css"
      - "templates/macro_suite.js"
      - "templates/macro_suite_boot.js"
      - "lib/__init__.py"
      - "lib/config.py"
      - "lib/macro_suite_labels.py"
      - "lib/macro_suite_view.py"
      - "lib/pages.py"
      - "scripts/__init__.py"
      - "scripts/build_macro_suite_pages.py"
      - "site/macrodata/workspaces/**"
      - "tests/test_macro_suite_pages.py"
    timeout-minutes: 10
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install suite-page test dependencies
        run: pip install pytest jinja2 pyyaml jsonschema==4.26.0
      - name: Market OS macro suite pages — shell, labels, view model, builder
        run: python -m pytest tests/test_macro_suite_pages.py -q
