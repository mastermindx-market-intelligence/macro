name: ci
# Pre-merge guards for the committed site. Runs on PRs that touch site/ so a
# blank-page regression (a malformed inline <script> that aborts the page IIFE)
# shows up as a red check BEFORE it reaches main — complementing the same guard
# that gates the Pages deploy in pages.yml.
on:
  pull_request:
    # Only proof-producing lifecycle events belong in semantic CI. A delayed
    # `closed` event is not a causally ordered cancellation message: GitHub may
    # deliver it after a later `reopened` event, and same-group pending runs are
    # replaced regardless of cancel-in-progress. Abandoned-PR cleanup is a
    # separate capability; it must never share this proof slot implicitly.
    types: [opened, synchronize, reopened]
    branches: [main]
    paths:
      # Start on every repository path. The selector, not this event filter, owns
      # cost; this catch-all is what makes a brand-new top-level directory fail
      # closed instead of bypassing CI before the ownership audit can see it.
      - "**"
      - "*"
      # Path-aware pack ownership makes broad START coverage cheap.  These roots
      # ensure an edit to any suite or first-party dependency can start the job
      # whose derived scope owns it; the pack selector then runs only that owner
      # family.  Keep the detailed entries below as executable provenance for
      # non-code artifacts and special cross-root contracts.
      - "app/**"
      - "admin/**"
      - "collectors/**"
      - "config/**"
      - "content/**"
      - "contracts/**"
      - "data/**"
      - "docs/**"
      - "engine/**"
      - "lib/**"
      - "ops/**"
      - "research/**"
      - "scripts/**"
      - "site/**"
      - "templates/**"
      - "tests/**"
      - "tools/**"
      - "worker/**"
      - "wrangler.toml"
      # repo-wide pytest conftest: session fixtures/guards run in EVERY pytest
      # job, so a conftest edit must trigger the full suite (2026-07-16 #2636
      # fixture import broke 14 minimal-lane jobs without triggering ci.yml).
      - "tests/conftest.py"
      # the data spine itself: config.yml decides which FRED series are collected and,
      # via fred.vintage_series, which survive in the ALFRED point-in-time store — that
      # key OVERRIDES collectors/fred.py:DEFAULT_VINTAGE_SERIES and fetch_vintages()
      # rewrites vintages.parquet wholesale, so a one-line deletion here silently drops
      # series from the store on the next keyed collect. That has now happened twice
      # (IC4WSA/CCSA, restored #3710; then PPIFES/ECIALLCIV/ECIWAG, #3735). Until this line,
      # config.yml matched no pattern in this list at all — a PR touching only config.yml
      # got no ci run, so no guard placed anywhere could have caught either round.
      # tests/test_mri_config_spine.py now asserts the superset invariant; this line is
      # what lets that guard fire on the edit it guards.
      - "config.yml"
      - "site/**"
      - "scripts/check_inline_js.py"
      # dead-reference guard: a page linking a file that isn't in the tree is a
      # silent 404 (pruned stylesheet #3988/#4042; the 605-page ../baskets/ typo).
      - "scripts/check_site_asset_refs.py"
      - "tests/test_check_site_asset_refs.py"
      - "scripts/check_site_js.py"
      - "scripts/check_nav_gap.py"
      - "scripts/check_nav_mega.py"
      - "scripts/check_board_contradictions.py"
      # ms-board coherence guard: a rendered Market State board must be single-snapshot
      # coherent (2026-07-07 -X theirs hybrid incident, commit 5bf116a3b3).
      - "scripts/check_ms_board_coherence.py"
      - "tests/test_check_ms_board_coherence.py"
      - "engine/market_state.py"
      # hub-a11y guard: six static a11y regressions on globe-deck pages + shared assets.
      - "scripts/check_hub_a11y.py"
      - "tests/test_check_hub_a11y.py"
      # hashed-css rebase autoresolver: every render lane's push loop leans on it to not
      # silently drop a day's engine outputs (2026-07-22 asia-close run 29904079071).
      - "scripts/rebase_autoresolve_hashed_css.sh"
      - "tests/test_rebase_autoresolve_hashed_css.py"
      # conflict-wreckage strip gate: the push loops' other failure mode — a conflicted
      # autostash apply staged wholesale (2026-08-01 d29e4dd44d, 1,704 marker pages).
      - "scripts/ci/strip_conflict_markers.sh"
      - "tests/test_strip_conflict_markers.py"
      # font-ui guard: body-level var(--font-ui) must resolve (theme.css or local def) — #2647.
      - "scripts/check_font_ui_defined.py"
      - "tests/test_check_font_ui_defined.py"
      - "tests/test_fund_page_typography.py"
      - "site/theme.js"
      # analytics identity capture — registered-visitor email attribution (server reads the
      # shared Supabase session cookie off the beacon) + read-side coalescing.
      - "app/main.py"
      - "admin/analytics_first_party.py"
      - "tests/test_collect_identity.py"
      # Pre-launch hardening guards (P1): the anonymous /api/collect per-IP throttle
      # (app/main.py, above), the /ws/tape per-IP connection cap, and generic
      # billing/auth 5xx errors (no raw-exception leak). Neither app/billing.py nor
      # app/tape.py triggers any other job, so a billing/tape-only PR must list them
      # here to reach the prelaunch-hardening guard below.
      - "app/billing.py"
      - "app/tape.py"
      - "tests/test_collect_throttle.py"
      - "tests/test_tape_relay.py"
      - "tests/test_billing_subscribe.py"
      # title-i18n guard: bilingual/CJK text must never sit in a title= attribute.
      - "scripts/check_title_i18n.py"
      # conflict-marker guard: a merge must never ship literal <<<<<<< / ======= / >>>>>>> lines.
      - "scripts/check_conflict_markers.py"
      # Crypto Cockpit governor: W0 lands warn-until-marked shelf checks; W1/W2 arm
      # exact S1-S6 / H1-H8 sets and the one-verdict law.
      - "scripts/check_crypto_shelves.py"
      # Crypto Cockpit suites (unrun backfill 2026-08-03): cockpit contract,
      # wave-2/wave-3 organs, house-style partial. Subjects covered by
      # engine/** + collectors/** + scripts/*.py entries; test halves added.
      - "tests/test_crypto_cockpit_contract.py"
      - "tests/test_crypto_wave2.py"
      - "tests/test_crypto_wave3.py"
      - "tests/test_crypto_house_style.py"
      # GH-annotation guard: a "::warning" behind logging's level prefix never parses as
      # an annotation. NOTE: this only fires when ci.yml is already triggered — making it
      # fire on EVERY python change needs "scripts/**"/"engine/**" here, which is a
      # CI-cost call for the operator (it would run the whole pack on most PRs).
      - "tests/test_gh_annotation_line_start.py"
      # __all__ guard: a listed name the module never binds is a dormant
      # AttributeError under `from m import *`. Same trigger posture as the
      # annotation guard above — an edit to the scan runs the scan; it fires
      # for source changes whenever ci.yml is already triggered.
      - "tests/test_all_exports_resolve.py"
      # sys.modules leak guard. Its RUNTIME half lives in tests/conftest.py (already
      # listed at the top of this block, which triggers the full suite), so this line
      # covers the STATIC half: an edit to the scan itself must run the scan. Same
      # both-halves reasoning as the collect-checkpoint pair below.
      - "tests/test_no_module_leak.py"
      # collect-lane qledger staging guard. Its SUBJECT — daily.yml / asia-close.yml —
      # reaches CI via ".github/workflows/**"; this line is the other direction. The
      # suite was named by no `run:` step at all until 2026-08-06, which is how #4731's
      # second checkpoint left it red on main for two days looking like test flake.
      - "tests/test_collect_staging_globs.py"
      # admin-js no-undef guard: admin/static/app.js is a flat top-level script, so a
      # name declared in one function and read from another parses clean and throws
      # only on click (#3693 blanked the Outbox panel for ~2 days; fixed in #3871).
      # The glob is listed alongside app.js (already here via the SEE W1 block below)
      # because the checker itself globs admin/static/*.js — a SECOND admin bundle
      # would otherwise be guarded by a job its own edits could never trigger.
      - "admin/static/*.js"
      - "scripts/check_admin_js.py"
      - "tests/test_admin_js_no_undef.py"
      # Same file, second failure mode: `onclick="fn(${JSON.stringify(v)})"` emits
      # double quotes that close the attribute, so the browser stores `fn(` and the
      # control is dead on click. Six sites shipped that way — the entitlements tier
      # and status chips, the support status chips, and the ticket thread's Resolve,
      # Close and Send-reply buttons (support replies could not be sent at all).
      # Listed by name so a test-only edit still triggers the lane that runs it.
      - "tests/test_admin_inline_handler_attrs.py"
      # Absolute-path prune guard (#3802): a walk excluding ".claude"/"worktrees"
      # via `path.parts` also matches the checkout's own path and scans nothing.
      # Named so a test-only edit still triggers the lane that runs it.
      - "tests/test_no_absolute_path_part_prunes.py"
      # template↔site sync guard: plain-copy page assets must byte-match their templates.
      - "scripts/check_template_site_sync.py"
      # chat.html header guard: the one product page that cannot {% include %} the
      # shared nav (it is a plain-copy pair) has its header RENDERED from
      # _site_nav.html.j2 instead. Named here, not just reached via templates/**,
      # so a PR touching only the generator still runs the job that checks it.
      - "scripts/sync_chat_nav.py"
      - "tests/test_chat_nav_sync.py"
      # ?v= stamp lane guard (ui.asset_stamp): the sweep that moves a stamp, and the
      # ordering/staging contract the lanes must keep for it to survive to main. The
      # lane YAMLs themselves already reach this via ".github/workflows/**" below.
      - "scripts/optimize_assets.py"
      - "tests/test_optimize_assets.py"
      - "tests/test_asset_stamp_lane_order.py"
      - "tests/test_asset_stamp_refresh.py"
      # closing-bell duplicate-render dedup contract. The SUBJECT (the guard + stamp
      # heredocs) already reaches CI via ".github/workflows/**"; this line is the other
      # direction, so an edit to the test alone still runs the job that executes it.
      - "tests/test_closingbell_dedup_contract.py"
      # interfonts guard: _interfonts.html.j2 @font-face blocks must match theme.css.
      - "scripts/check_interfonts_sync.py"
      # data-base shim guard: every builder must write site/**/*.html through
      # lib.pages.write_page, or the page's per-ticker fetches miss the R2 reroute
      # in a standalone run. Unwired until #3635 — which is how nine builders shipped
      # shim-less pages under a green guard (the render lane's inject_data_base
      # sweep healed the committed copies, hiding it everywhere but a direct run).
      - "lib/pages.py"
      - "tests/test_site_shim.py"
      # ...and the suite that RUNS each builder against a fixture root (#3652) —
      # the only layer that can catch a target whose ".html" is never a source literal.
      - "tests/test_builder_shim_writes.py"
      - "scripts/inject_data_base.py"
      # inline-CSS externalization units (same lib/pages page-rewrite family).
      - "tests/test_externalize_css.py"
      - "scripts/externalize_css.py"
      - "templates/**"
      # SEO meta rollout guard (D12A): template-source SEO head/desc contract.
      - "tests/test_seo_meta_rollout.py"
      - ".github/workflows/ci.yml"
      - ".github/ci/legacy-jobs.yml"
      # Research Vault (RV W1) ingestion spine: sidecar contract, corpus FTS,
      # catalog, and the idempotent never-raise ingest pipeline. (RV W3) adds the
      # SSR page builder + its render test + the page template.
      - "engine/research_vault/**"
      - "scripts/ingest_research.py"
      - "scripts/build_research_vault.py"
      - "tests/test_research_vault.py"
      - "tests/test_research_vault_strict_store.py"
      # Research Vault (RV W2) serving API + download gate: quota ledger, view
      # rate-limit, watermark, the app/research.py router + its security tests.
      - "app/research.py"
      - "app/main.py"
      - "tests/test_research_api.py"
      # Research Vault (RV W3) page + viewer: page test + template.
      - "tests/test_research_vault_page.py"
      - "templates/research_vault.html.j2"
      # Research Vault SEO landing pages (programmatic SEO): per-report page
      # generator + its templates + test.
      - "scripts/build_research_pages.py"
      - "templates/research_report.html.j2"
      - "templates/research_index.html.j2"
      - "tests/test_research_pages.py"
      # Authorized competitor-history Wave 0: strict contracts + synthetic replay/R2
      # inventory tests and an aggregate-only receipt. The private history and targeted
      # restore plan stay outside the repository; tests/** is deliberately not broad.
      - "contracts/research/momoedge_oracle_*.schema.json"
      - "contracts/research/momoedge_theta_remote_inventory_aggregate.v1.schema.json"
      - "scripts/research/momoedge_oracle_history_replay.py"
      - "tests/test_momoedge_oracle_history_replay.py"
      - "research/momoedge/history_replay_wave0_receipt.json"
      # Host-private prospective same-basis OPRA NBBO cohort. Raw capture and
      # quote bytes never enter git; this trigger owns only code/contracts/tests.
      - "contracts/options/options.prospective_nbbo_cohort.v1.schema.json"
      - "engine/options_nbbo_cohort.py"
      - "scripts/capture_options_nbbo_cohort.py"
      - "ops/launchd/com.mastermind.optionsnbbocohort.plist"
      - "ops/launchd/run_options_nbbo_cohort_loop.sh"
      - "docs/runbooks/OPTIONS_NBBO_COHORT.md"
      - "tests/test_options_nbbo_cohort.py"
      - "tests/test_options_nbbo_cohort_deploy.py"
      # Authenticated-page MomoEdge companion: observe-only extension, private
      # native receiver, immutable contracts, and synthetic security tests.
      - "browser/momoedge_capture/**"
      - "contracts/options/options.momoedge_browser_observation.v1.schema.json"
      - "contracts/options/options.momoedge_browser_observe_journal.v1.schema.json"
      - "engine/options_momoedge_browser_adapter.py"
      - "scripts/momoedge_browser_receiver.py"
      - "ops/native_messaging/com.mastermind.optionsnbbocohort.momoedge_observe.json"
      - "ops/native_messaging/run_momoedge_browser_host.sh"
      - "docs/runbooks/MOMOEDGE_BROWSER_COMPANION.md"
      - "tests/test_momoedge_browser_adapter.py"
      - "tests/test_momoedge_capture_extension_static.py"
      - "tests/test_momoedge_capture_deploy.py"
      # Signal→chart §7 contract: any change to the schema, validator, engine, or build script
      # must re-prove the contract still holds (see the signal-contract job below).
      - "research/signal_engine/SCHEMA.json"
      - "scripts/validate_signals.py"
      - "scripts/build_signal_quality.py"
      - "engine/signal_quality.py"
      - "tests/test_validate_signals.py"
      # The §7 marker-date look-ahead guard on _buy_filter (wired into the
      # signal-contract job 2026-08-05, dark before that). engine/** above
      # already triggers on the subject module; this is the test half, which
      # nothing matched — the #3488/#4559 lesson that a guard whose own edit
      # cannot start the workflow is only half wired.
      - "tests/test_signal_quality_no_leak.py"
      # Absolute session-calendar anchor invariance (job session-anchor-era): the
      # confluence buckets' era abs-session-2026-08-06 (#4732) and the §7 marker
      # engine's era sq-abs-session-2026-08-06 (#4738). Their subjects —
      # engine/session_anchor.py, engine/confluence_tiers.py, engine/signal_quality.py,
      # engine/signal_gate.py — all ride the "engine/**" trigger above, so these two
      # entries are the OTHER half: a PR editing ONLY a suite (a new fixture shape, a
      # widened k) must still start the pack that executes it. Both suites were UNRUN
      # (audit_unrun_tests.py tier P5 — named by no run step in any workflow or the
      # legacy manifest) while scoring triggerable=True off those same subjects, so
      # neither census flagged them: an era stamp shipped resting on a start-invariance
      # guarantee that CI had never once executed. The suite files matched no path entry
      # either — the #4559 "test half unreachable" shape these lines close.
      - "tests/test_session_anchor_invariance.py"
      - "tests/test_sq_anchor_invariance.py"
      # W-F reclaim-veto decision packet (2026-08-05). Both halves per #3488: the suite AND
      # the module it measures, so an edit to either can trigger the job that proves the
      # flip-isolation construction still isolates exactly one leg.
      - "research/prophet_us_audit/reclaim_veto_packet.py"
      - "tests/test_us_reclaim_veto_packet.py"
      # The RATIFIED sibling of that packet (us_prophet_v2, 2026-08-10): the counter-trend
      # reclaim waiver and its era fence. Both halves per #3488 — the suite, plus the
      # AUTHORITY it reads. engine/** already covers signal_quality/signal_gate/us_board_rank
      # and site/** does not exist as a trigger, so the prereg is named explicitly: this
      # suite pins the shipped NOTCH ("20"), and a PR that moves the operator's dial in the
      # ratification log without moving the constant must be able to start the job that
      # would catch it.
      - "tests/test_us_reclaim_waiver_prophet_v2.py"
      - "research/RECLAIM_VETO_CONDITIONAL_PREREG.md"
      # Versioned FROZEN research price panel (research-price-panel job). Both halves per
      # #3488: the suite AND every module it measures. The writer/reader own the write-once
      # and never-fall-back properties; price_ladder.py is the adjusted-first resolver they
      # reuse from #4698 (vendored byte-identical — a drifted copy is a silent basis change);
      # relay_position_standin.py is the worked migration whose pinned version the suite's
      # contract documents. An edit to any of them must be able to start the job.
      - "research/research_panels/price_panel.py"
      - "research/prophet_us_audit/price_ladder.py"
      - "research/prophet_us_audit/relay_position_standin.py"
      - "tests/test_research_price_panel.py"
      # NOT the packet's .md/.json artifacts. #4733 added them here to clear a
      # check_ci_trigger_closure.py finding against tests/test_merge_on_green.py,
      # but that suite never opens them — they are members of INCIDENT_4607_FILES,
      # file NAMES handed to a fake GitHub API to reconstruct one commit. The entries
      # encoded a dependency that does not exist and armed the full 4-pack run on
      # every edit to two decision-packet files. The fixture now carries a
      # `# ci-trigger-closure: data` marker instead.
      # research/-RESIDENT suites (2026-08-06). Same job, same lesson, one directory over.
      # The packets that introduced these were fenced to files-only, so each suite was
      # written NEXT TO its instrument under research/ instead of in tests/. Nothing in
      # this repo runs pytest over research/ — no workflow collects that tree and no
      # `run:` step named either file — so both were UNRUN from birth: green locally,
      # and unable to fail a PR that broke the very instrument they pin.
      # They were NOT untriggerable: `research/**/*.py` (added far below for the
      # import-hygiene ratchet, an unrelated concern) already starts this workflow on
      # any research/ .py edit. These four entries are the #3488 "both halves BY NAME"
      # idiom anyway — exactly as reclaim_veto_packet.py directly above is named
      # explicitly despite that same catch-all covering it — so the trigger survives if
      # the ratchet's glob is ever narrowed to its own subject. The engine/lib modules
      # these suites import reach this workflow through the engine/** and lib/**
      # subtrees below. Wired into signal-contract, which already owns this family
      # (the reclaim-veto packet directly above).
      #   * label-grading battery (#4547): the veto-day label classifier + the ran-lane
      #     numpy-bool trap the entry gauge is written against.
      #   * buy_filters: the canonical reclaim-and-hold / bearish-divergence / 200-MA
      #     bar-raiser math — the same buy-side §7 machinery
      #     tests/test_signal_quality_no_leak.py pins one layer up.
      - "research/prophet_us_audit/label_grading_battery.py"
      - "research/prophet_us_audit/test_label_grading_battery.py"
      #   * earnings-ignition cohort logic: the derived knowability convention, the
      #     EDGAR-roll-aware announcement window, and the single reaction-session anchor
      #     both the reaction and the forward cells hang from. Rides the same step.
      - "research/prophet_us_audit/earnings_ignition_measurement.py"
      - "research/prophet_us_audit/test_earnings_ignition_measurement.py"
      - "research/signal_engine/buy_filters.py"
      - "research/signal_engine/test_buy_filters.py"
      # W1d trial-budget gate: any new multiple-testing harness must register its budget.
      - "scripts/*_phase0.py"
      - "scripts/*_phase1.py"
      - "scripts/validate_*.py"
      - "scripts/check_trial_registration.py"
      - "engine/trial_ledger.py"
      - "engine/grading.py"
      - "engine/track_record.py"
      # Track-record company identity: one ledger key per company across ticker
      # renames. Name both the executable subjects and the suite explicitly so
      # a test-only or migration-tool edit starts the packed owner (#3488).
      - "engine/ledger_identity.py"
      - "scripts/audit_reused_tickers.py"
      - "scripts/migrate_track_record_keys.py"
      - "tests/test_ledger_identity.py"
      - "engine/sector_signals.py"
      - "engine/meta_label.py"
      - "tests/test_grading.py"
      - "tests/test_trial_ledger.py"
      - "tests/test_trial_registration_lint.py"
      - "tests/test_no_literal_ntrials.py"
      # membership-ledger tz guard: a tz-aware asof froze the PIT universe ledger for a
      # month (2026-06-14 → 2026-07-10, TypeError on every collect run) — the upsert
      # must keep accepting aware timestamps against the naive parquet columns.
      - "engine/universe_history.py"
      - "tests/test_universe_history.py"
      # W4 outcome spine: the shared signal-id→outcome contract + partial-pooling + badge passport.
      - "engine/spine.py"
      - "engine/pooling.py"
      - "engine/passport.py"
      - "engine/desk_scorer.py"
      # The desk that FEEDS the spine. engine/spine.py registers data/stock_desk/scored.jsonl
      # in _DESK_SCORED, but nothing wrote that file, so 45 decided outcomes never reached
      # engine.pooling and the cross-desk family was one member (ai_desk) in practice. Neither
      # the module nor its suite carried a trigger — the file that decides what a desk
      # contributes to the pooled weights could not start ci.yml at all (same class as the
      # tier-gate hole, #3509). Its suite runs in the outcome-spine job below.
      - "engine/stock_desk.py"
      - "tests/test_stock_desk.py"
      - "engine/alert_triage.py"
      # Alert Command Center TIME CONTRACT (Wave 7, 2026-08-20). The board's three clocks
      # — event / observation / system-recorded — and its America/New_York board day live
      # in engine/alert_time.py; its suite is the guard. Both need explicit entries: the
      # module is new, and tests/test_alert_triage.py sat in the unrun grandfather list
      # (config/unrun_test_baseline.json) since 2026-08-09, so the suite that pins this
      # page's ranking honesty ran nowhere. A guard the guarded change cannot trigger is
      # not a guard (#3488), so the producer whose event-time defect started this wave is
      # listed too.
      - "engine/alert_time.py"
      - "engine/subsector_rotation_alerts.py"
      - "engine/rotation_events.py"
      - "tests/test_alert_time_contract.py"
      - "tests/test_alert_triage.py"
      - "tests/test_alert_coverage_recurrence.py"
      - "engine/altdata_signals.py"
      - "scripts/check_badge_passport.py"
      - "tests/test_spine.py"
      - "tests/test_pooling.py"
      # Promotion gate (#3495/#3523): the per-desk empirical null and the Calibration Hub
      # verdicts it feeds. These files carried NO trigger — a change to the module that
      # decides whether a desk is "calibrated" could not start ci.yml at all, so its own
      # suites never ran on the PR that changed it (same class as the tier-gate hole, #3509).
      - "engine/desk_placebo.py"
      - "engine/calibration_hub.py"
      - "engine/thematic_desk.py"
      - "tests/test_desk_placebo.py"
      - "tests/test_calibration_hub.py"
      - "tests/test_thematic_desk.py"
      # The Phase-C desks that WRITE the scored ledgers spine._DESK_SCORED reads. The engine
      # modules are already covered by engine/**, but no job ran these suites at all, so a
      # silent break in any of them moved the deterministic desk-weight vector
      # (spine.adapt_desk_scorer → desk_scorer.desk_weights → pooling.arming) with zero CI
      # signal — and a test-only edit could not even start ci.yml (ci-test-whitelist-rot
      # guard). Registry coverage: ai_desk (ai_desk + ai_desk_scorer), radar + narrative_brain
      # (radar_scorer reuses ai_desk_scorer's eval verbatim), demand_chain (demand_ledger),
      # policy_intent. Run in the outcome-spine job below.
      - "tests/test_ai_desk.py"
      - "tests/test_ai_desk_scorer.py"
      - "tests/test_radar_scorer.py"
      - "tests/test_narrative_brain.py"
      - "tests/test_demand_ledger.py"
      - "tests/test_policy_intent_desk.py"
      # Honesty-register guards (unrun-suite backfill 2026-08-03): the falsifier
      # vocabulary bans, chat plain-word projection, regime-label/sizing-pill
      # disclosure parity, and forward cycle framing. Subjects live under
      # engine/** and templates/** (broad globs); the register scan also reads
      # the shipped policy intel artifact, which nothing else matches.
      - "data/policy/intel.json"
      - "tests/test_front_facing_register.py"
      - "tests/test_policy_watch_register.py"
      - "tests/test_chat_plain_words.py"
      - "tests/test_regime_label_honesty.py"
      - "tests/test_regime_sizing_disclosure_parity.py"
      - "tests/test_cycle_forward_framing.py"
      # WRI W3: watchlist book-structure math + its node-shelled unit tests. templates/**
      # already covers risk_core.js/watchlist_risk.js; list the test file so a test-only
      # edit still triggers CI (ci-test-whitelist-rot guard).
      - "tests/test_risk_core_js.py"
      - "templates/risk_core.js"
      - "templates/watchlist_risk.js"
      # TXI W4 watchlist chain lane: the node-shelled chain-lane helper tests (furthest chain
      # selection, 404-empty, rail-sentence condition). Same ci-test-whitelist-rot guard.
      - "tests/test_watchlist_chains_js.py"
      - "tests/fixtures/transmission/transmission_chains.json"
      # UWP/W1a watchlist+portfolio store: the node-shelled suites over
      # templates/watchstore.js — the local books + one-shot folds, and the registered
      # multi-list seam (list CRUD isolation, fold retarget + run-twice, the
      # list-scoped push diff, semantic invariants A-D). templates/** already covers
      # watchstore.js/watchlist.js; list the test files so a test-only edit still
      # triggers CI (ci-test-whitelist-rot guard). Both run in the wri-risk-core job.
      # test_watchlist_books_js.py had been named by NO run: step since it landed
      # (P5 in scripts/audit_unrun_tests.py) — it guards the fold markers on the very
      # file W1a rewrites, so it is wired here rather than left dark.
      - "tests/test_watchlist_books_js.py"
      - "tests/test_watchstore_multilist_js.py"
      - "templates/watchstore.js"
      # Main-side orphan healed here (landed in #5458 wired to nothing, which reddens
      # the workflow-yaml pack via scripts/audit_unrun_tests.py). templates/** and
      # site/** already cover its subjects (theme.css + five report templates); this
      # entry is the test half, so a test-only edit still triggers CI. Runs in the
      # unrun-page-guards job — its single canonical home after the 2026-08-12 triple
      # concurrent heal (#5455/#5460/#5461) was consolidated to one run: step.
      - "tests/test_odometer_light_mode_surface.py"
      # live_flow payload consumers: the node-shelled suites that execute the intraday_flow
      # tracker's and the stock panel's flow loaders against payloads from the real builders.
      # templates/** and site/** already cover the pages and engine/** the builders; list the
      # test files so a test-only edit still triggers CI (ci-test-whitelist-rot guard).
      # Run in the live-flow-ops job.
      - "tests/test_intraday_flow_ncp_js.py"
      - "tests/test_stock_iflow_panel_js.py"
      # WRI W6.1: the factor-exposure engine bakes factor_betas.json that risk_core.js
      # consumes; list its emit + tests so an engine/test-only edit triggers CI
      # (ci-test-whitelist-rot guard). Run in the wri-risk-core job below.
      - "engine/factor_exposure.py"
      - "tests/test_factor_exposure.py"
      - "tests/test_factor_exposure_stress.py"
      # audit #46 reliability-contract guard: a gated-out field must not render directionally.
      - "scripts/check_reliability_contract.py"
      - "engine/btc_leverage_cascade.py"
      - "engine/options_flow.py"
      - "templates/**"
      # W1.5 cycle-intelligence gates: ontology JS drift, forward-log writer, grading_stats.
      - "scripts/gen_ontology_js.py"
      - "engine/cycle_ontology.py"
      - "site/cycle_ontology.js"
      - "engine/cycle_forward_log.py"
      - "tests/test_cycle_forward_log.py"
      # perf: cycle-map payload splitter (core + lazy series chunk)
      - "scripts/_cycles_payload.py"
      - "tests/test_cycles_payload_split.py"
      # Flow-Surface snapshot store: per-strike net-premium grids materialized in the
      # live_flow poller loop for the Terminal surface pane (shape ↔ surfaceContract.ts).
      - "scripts/build_flow_surface.py"
      - "tests/test_flow_surface.py"
      # Source-clock/cadence truth closes the producer, archives, display derivatives,
      # static consumer, and operator documentation under the existing flow owners.
      - "scripts/build_flow_archive.py"
      - "scripts/build_flow_enrich.py"
      - "scripts/build_chain_heat.py"
      - "engine/flow_enrich.py"
      - "tests/test_flow_archive.py"
      - "tests/test_flow_enrich.py"
      - "tests/test_build_chain_heat.py"
      - "ops/LIVE_FLOW_RUNBOOK.md"
      - "ops/launchd/com.mastermind.flowenrich.plist"
      - "research/INTRADAY_FLOW_TRACKER_DESIGN.md"
      - "research/momoedge/our_data_contracts.md"
      # PRISM daily matrix: exact-contract-side PIT UNUSUAL baseline and its payload
      # validator.  List every subject plus the suite explicitly even though
      # engine/** and tests/** also start CI, so the packed-job ownership seam
      # stays executable and reviewable.
      - "engine/options_matrix.py"
      - "engine/options_structure.py"
      - "engine/thetadata_store.py"
      - "scripts/build_options_matrix.py"
      - "site/options_structure/examples/matrix.json"
      - "tests/test_options_matrix.py"
      # MSC R2.2-A Light U-CHAIN: compact per-root eligibility receipts and the
      # monotonic all-root R2 discovery commit. Keep this list exact so source,
      # schema, publisher, or either adversarial suite can start their packed job.
      - "engine/options_structure_intraday.py"
      - "scripts/build_options_structure_intraday.py"
      - "contracts/options/options.contract_eligibility.v1.schema.json"
      - "tests/test_options_structure_intraday.py"
      - "tests/test_build_options_structure_intraday.py"
      - "scripts/live_flow_poller.py"
      - "engine/live_flow.py"
      - "engine/thetadata_store.py"
      - "tests/test_live_flow.py"
      - "tests/test_thetadata_store.py"
      # W0a-B chain-snapshot producer authority: strict per-bucket receipts,
      # source installer, governed schema, adversarial suite, and ops contract.
      - "engine/chain_snapshot_completion.py"
      - "scripts/chain_snapshot_poller.py"
      - "contracts/options/chain_snapshots.bucket_completion.v1.schema.json"
      - "tests/test_chain_snapshot_poller.py"
      - "ops/CHAIN_SNAPSHOTS_RUNBOOK.md"
      - "ops/launchd/com.mastermind.chainsnapshots.plist"
      # W0b focused vendor-snapshot quote: explicit W0a-B completion proof,
      # immutable semantic attempt/receipt, private CAS, and hostile no-repoll.
      - "collectors/thetadata.py"
      - "engine/options_focused_quote.py"
      - "scripts/build_options_focused_quote.py"
      - "contracts/options/options.focused_quote_attempt.v1.schema.json"
      - "contracts/options/options.contract_eligibility.index.v1.schema.json"
      - "tests/test_options_focused_quote.py"
      - "research/options_estate/OPTIONS_FOCUSED_QUOTE_R2_RUNBOOK.md"
      # OIP PIT decision/outcome ledger: both the durable R2 source and the
      # nightly consumer/contracts must trigger the exact adversarial suite.
      - "engine/options_signal_episode.py"
      - "engine/options_episode_coverage.py"
      - "scripts/build_options_signal_episode.py"
      - "scripts/audit_options_episode_outcome_coverage.py"
      - "scripts/build_polygon_intraday.py"
      - "contracts/options/options.signal_episode.v1.schema.json"
      - "contracts/options/options.signal_episode_outcome.v1.schema.json"
      - "contracts/options/options.signal_episode_session_outcome.v1.schema.json"
      - "contracts/options/options.signal_campaign.v1.schema.json"
      - "contracts/options/polygon.intraday_price_receipt.v1.schema.json"
      - "data/options_signal_episode/campaigns.jsonl"
      - "research/options_estate/OPTIONS_SIGNAL_CAMPAIGNS_PREREG.md"
      - "ops/LIVE_FLOW_RUNBOOK.md"
      - "tests/test_options_signal_episode.py"
      - "tests/test_options_episode_outcome_coverage.py"
      - "tests/test_polygon_intraday.py"
      # Canonical campaign v2: independent immutable revisions, causal outcomes,
      # exact-prefix checkpoint, preregistration, and hostile replay tests.
      - "engine/options_signal_campaign.py"
      - "engine/options_signal_episode_contract.py"
      - "scripts/build_options_signal_campaign.py"
      - "contracts/options/options.signal_campaign.v2.schema.json"
      - "contracts/options/options.signal_campaign_outcome.v1.schema.json"
      - "contracts/options/options.signal_campaign_checkpoint.v1.schema.json"
      - "data/options_signal_campaign/**"
      - "research/options_estate/OPTIONS_SIGNAL_CAMPAIGN_V2_PREREG.md"
      - "tests/test_options_signal_campaign.py"
      # R6.2 private operator-reviewed Options Issue Desk. engine/** and
      # scripts/*.py cover the engine/builder halves; these exact entries close
      # the API, contract, and test-only trigger seams without widening app/**.
      - "app/options_issue_desk.py"
      - "contracts/options/options.issue_desk.v1.schema.json"
      - "contracts/options/options.issue_desk_proposal.v1.schema.json"
      - "contracts/options/options.issue_desk_decision.v1.schema.json"
      - "contracts/options/options.issue_receipt.v1.schema.json"
      - "tests/test_options_issue_desk.py"
      - "tests/test_options_issue_desk_api.py"
      # OEU M-XP: date-keyed surface retention + the Terminal EOD-context R2 mirror +
      # the Scanner-mode screener rows export. All three feed the Terminal/Options
      # workspace, so a silent contract change here shows up as a 404 over there.
      - "scripts/mirror_terminal_context_r2.py"
      - "tests/test_mirror_terminal_context.py"
      - "scripts/build_options_screener.py"
      - "tests/test_build_options_screener.py"
      # Lane G: intraday BS greek engine feeding the surface gex/dex/vanna/charm grids.
      - "engine/intraday_greeks.py"
      - "tests/test_intraday_greeks.py"
      - "engine/grading_stats.py"
      - "tests/test_grading_stats.py"
      # W3.6 cross-page cycle-consistency gate (R3-M3): same instrument must show the
      # same pos_v2/phase_v2 across every page (cycle/country/markets/sector) or carry a
      # declared basis label.  Also the phase-coherence regression (pos_v2 vs phase_v2).
      - "scripts/check_cycle_consistency.py"
      - "tests/test_cycle_consistency.py"
      - "tests/test_cycle_ontology.py"
      # 2026-07-02 rot sweep (engine-render-guards job): these files broke silently on
      # main because no CI job ran them. engine/** is broad on purpose — most of these
      # tests pin engine contracts (display-only firewalls, unit guards, honesty layer).
      - "engine/**"
      # W7 seasonality research-browser API surface. engine/** already covers
      # engine/seasonality/screener.py, but tests/test_seasonality_api.py imports
      # app/seasonality.py, and a PR touching ONLY that handler would otherwise
      # never start the stock-seasonality pack that executes the suite.
      - "app/seasonality.py"
      # Signal-Lab promotion-stat display gate: the BTC Vector card's DSR/Sharpe/n
      # must equal what calibrate_vector actually computed. engine/** covers the
      # module, but a test-only re-pin (or a nightly refresh of the artifacts the
      # test reads) must still retrigger the trial-budgets lane below.
      - "tests/test_signal_lab_vector_display.py"
      - "data/vector/calibration.json"
      - "data/vector/trial_log.json"
      # regime no-regress guard (2026-07-13 earlyclose↔render flip-flop fix)
      - "tests/test_engine_run_no_regress.py"
      # stretch-oracle / overextension-brake disclosure contract. Carried by
      # engine-render-guards beside test_cycles.py (same producer: engine/cycles.py).
      # engine/** covers the producer; scripts/*.py covers the checker. Name the
      # test half and the contract doc so a test-only or doc-only edit still
      # starts the owner job (#3488 both-halves).
      - "tests/test_stretch_oracle_contract.py"
      - "scripts/check_stretch_oracle_contract.py"
      - "docs/site_semantics/stretch_oracles.md"
      - "scripts/build_site.py"
      - "scripts/audit_price_basis.py"
      - "research/signal_engine/walk_forward.py"
      - "tests/test_canada_build.py"
      - "tests/test_commodity_signals.py"
      - "tests/test_commodity_index.py"
      - "engine/commodity_confluence.py"
      - "tests/test_commodity_confluence.py"
      - "engine/commodity_cycle_state.py"
      - "tests/test_commodity_cycle_state.py"
      - "tests/test_fear_euphoria.py"
      - "tests/test_froth_fragility.py"
      - "engine/vol_velocity.py"
      - "tests/test_vol_velocity.py"
      - "tests/test_fear_greed_honesty.py"
      - "tests/test_archive_context_snapshots.py"
      - "scripts/archive_context_snapshots.py"
      - "tests/test_ai_breadth_split.py"
      - "scripts/build_ai_adjacency_tag.py"
      - "engine/breadth_split.py"
      - "tests/test_vsb_surface.py"
      - "scripts/build_site.py"
      - "templates/dashboard.html.j2"
      - "tests/test_missing_tape.py"
      - "tests/test_price_basis_flip.py"
      - "tests/test_risk_lead_lag.py"
      - "tests/test_anticipation.py"
      - "tests/test_risk_radar_market_catalysts.py"
      - "tests/test_risk_radar_recovery.py"
      - "tests/test_risk_radar_scorecard.py"
      - "engine/risk_radar_scorecard.py"
      - "tests/test_rr_scorecard_card.py"
      # Market-score authority firewall: the engine/template subjects are already
      # covered by engine/** and templates/**; name the suite so a test-only edit
      # still starts the owner job.
      - "tests/test_market_score_authority_2026_08_12.py"
      # #5481 folded the China Policy Watch express-render guards into this
      # canonical suite. Name the test half explicitly beside its owner job;
      # collectors/**, engine/**, scripts/**, and templates/** cover its subjects.
      - "tests/test_china_policy_watch.py"
      # VSB W6: corr_floor_break + ai_breadth_divergence Tier-B legs + corr alert
      - "tests/test_vsb_radar_alert.py"
      - "engine/risk_radar.py"
      # intl 7-market radar profiles (KR/JP/TW/IN/AU/GB/EZ) + frozen cn/hk/ca guard
      - "tests/test_risk_radar_intl_profiles.py"
      - "engine/risk_radar_intl.py"
      - "engine/intl_run.py"
      - "engine/alerts.py"
      - "templates/_risk_radar_card.html.j2"
      - "templates/_risk_radar_card.css.j2"
      - "tests/test_canon.py"
      - "tests/test_whitehouse_w5.py"
      # Winner Autopsy Lab: every case file under research/winners/cases/ must parse
      # against winner_case.v1 (engine/winner_autopsy.py is already covered by engine/**).
      - "research/winners/cases/**"
      - "scripts/research/build_winner_autopsy.py"
      - "admin/long_hold.py"
      - "tests/test_winner_autopsy.py"
      - "tests/test_admin_long_hold.py"
      # IGN WB: Ignition Radar — risk-ON mirror of the Risk Radar.
      # engine/ignition_radar.py + ignition_audit.py US arm + template card.
      - "engine/ignition_radar.py"
      - "engine/ignition_audit.py"
      - "tests/test_ignition_radar.py"
      # M7C-R2/R3: Mag-7 Regime Context Organ (engine/mag7_regime.py).
      - "engine/mag7_regime.py"
      - "tests/test_mag7_regime.py"
      # MWR-W0/W1: Mag-7 washout re-entry gate + shadow lane (background-only).
      - "engine/mag7_washout.py"
      - "engine/mag7_washout_shadow.py"
      - "tests/test_mag7_washout.py"
      - "tests/test_mag7_washout_shadow.py"
      - "tests/test_ignition_persistence.py"
      # Mag 7 panel: template partial + build_site loader.
      - "templates/_mag7_panel.html.j2"
      - "templates/_mag7_panel.css.j2"
      - "tests/test_mag7_panel.py"
      # MLC-W2b stance matrix + conflicted shelf: builder, theme_scoring demotion, templates.
      - "scripts/build_stance_matrix.py"
      - "engine/theme_scoring.py"
      - "templates/baskets.html.j2"
      - "templates/baskets_desk.js"
      - "templates/allocation.html.j2"
      - "tests/test_stance_matrix.py"
      - "tests/test_theme_scoring_conflicted.py"
      # MLC-W4: histogram-fade leg in rollover_risk + cooling demotion.
      - "engine/basket_score.py"
      - "tests/test_basket_score_rollover_w4.py"
      # FL-D FC-R5: cohort flow chips on turn surfaces + cohort_flow.v1 forward ledger.
      - "engine/basket_turn_watch.py"
      - "templates/basket_detail.html.j2"
      - "tests/test_cohort_flow_chips.py"
      # FTR tape-surface guards (unrun-suite census, 2026-07-26): both suites read
      # templates/{baskets,allocation,basket_detail}.html.j2 — all three are already
      # listed above — but the test files need explicit paths so a test-only re-pin
      # still re-runs the ftr-tape-surfaces job that carries them.  Both files sat
      # RED and unrun for ~2 weeks with no signal: three deliberate surface changes
      # each moved a marker the suites pinned as a bare literal (#2635 ported the
      # FT-R11 horizon copy off Law-2 jargon, #2267 deleted the flat #ftr-dtp-full
      # container that interleaved the expansion, #2380 made the basket-name map
      # region-aware) and no `run:` step in any of the 51 workflows named either
      # file, so nothing went red.  Both halves listed on purpose (#3488).
      - "tests/test_ftr_w1_template_markup.py"
      - "tests/test_ftr_w3_ui.py"
      # group_flow display-only detector + its baskets.html.j2 money-flow render
      # guard (unrun-suite census, 2026-07-26).  Same defect as the FTR pair above:
      # `grep -rn test_group_flow .github/` returned nothing, so when #3282's
      # Rotation Map revamp rebuilt the "Flow lens" card into the Money-flow tile
      # (2026-07-24) the suite's literal `{% if flow %}` source slice started raising
      # ValueError and stayed red on main unseen.  Now carried by
      # express-render-guards (it rode engine-render-guards until that lane was
      # split three ways on 2026-08-14).
      # The template half of the trigger is already covered twice — "templates/**"
      # and an explicit "templates/baskets.html.j2" above — so only the test file
      # needs a new entry here; both halves do trigger the job (#3488).
      - "tests/test_group_flow.py"
      # #theme-<id> deep-link contract + Sector Intelligence consolidation invariants
      # (unrun-suite wiring, 2026-08-02): both suites read baskets/sector_central
      # templates (covered by "templates/**") and pin engine/alert_triage.py's themes
      # page (listed above); the test files need explicit entries so a test-only
      # re-pin still re-runs the engine-render-guards job that carries them (#3488).
      - "tests/test_theme_hash_deeplink.py"
      - "tests/test_sector_intelligence_page.py"
      # CPI net-liquidity artifact (data infrastructure): builder + contract tests
      # must re-run together so a canon-formula or stale-guard regression fails RED.
      - "scripts/build_netliq_daily.py"
      - "tests/test_build_netliq_daily.py"
      # collector-registry tripwire (#1052 post-mortem): scripts/collect.py shipped as a
      # 0-byte file and merged green because nothing here covered the collection entrypoint.
      # lib/** is included because the registry import chain runs through lib.config/lib.store.
      - "scripts/collect.py"
      - "collectors/**"
      - "lib/**"
      # Canonical Market Memory contract and product-shell trigger halves.
      - "scripts/__init__.py"
      - "engine/__init__.py"
      - "engine/neuralweb/__init__.py"
      - "app/market_memory.py"
      - "contracts/market_memory/as_known_at.v1.schema.json"
      - "engine/neuralweb/market_memory.py"
      - "engine/neuralweb/market_memory_pit.py"
      - "engine/neuralweb/market_memory_sources.py"
      - "engine/neuralweb/market_memory_source_kernel.py"
      - "engine/neuralweb/market_memory_sources_spy.py"
      - "engine/neuralweb/market_memory_identity.py"
      - "engine/neuralweb/market_memory_identity_observation.py"
      - "engine/neuralweb/market_memory_identity_store.py"
      - "engine/neuralweb/market_memory_actual_output_store.py"
      - "engine/neuralweb/market_memory_breadth_observation.py"
      - "engine/neuralweb/market_memory_technical_observation.py"
      - "engine/neuralweb/market_memory_technical_store.py"
      - "engine/neuralweb/market_memory_experience_accrual.py"
      - "engine/neuralweb/market_memory_replay.py"
      - "engine/neuralweb/market_memory_option_oi_observation.py"
      - "engine/neuralweb/market_memory_option_oi_store.py"
      - "engine/neuralweb/market_memory_forward.py"
      - "engine/neuralweb/market_memory_forward_store.py"
      - "engine/neuralweb/market_memory_scoring.py"
      - "engine/neuralweb/market_memory_playback.py"
      - "engine/neuralweb/market_memory_retrieval.py"
      - "engine/neuralweb/market_memory_operating_cortex.py"
      - "engine/neuralweb/market_memory_promotion_audit.py"
      - "engine/neuralweb/market_memory_production_records.py"
      - "engine/neuralweb/market_memory_projection.py"
      - "engine/neuralweb/market_memory_trusted.py"
      - "contracts/market_memory/source_artifact_receipt.v1.schema.json"
      - "contracts/market_memory/macro_regime_snapshot.v1.schema.json"
      - "contracts/market_memory/macro_regime_feature_object.v1.schema.json"
      - "contracts/market_memory/trusted_capture_receipt.v1.schema.json"
      - "contracts/market_memory/spy_listing_object.v1.schema.json"
      - "contracts/market_memory/spy_listing_observation.v1.schema.json"
      - "contracts/market_memory/identity_observation_prepared.v1.schema.json"
      - "contracts/market_memory/identity_observation_capture_receipt.v1.schema.json"
      - "contracts/market_memory/identity_observation_store_receipts.v1.schema.json"
      - "contracts/market_memory/breadth_source_observation.v1.schema.json"
      - "contracts/market_memory/breadth_factors_snapshot.v1.schema.json"
      - "contracts/market_memory/breadth_actual_output_capture_receipt.v1.schema.json"
      - "contracts/market_memory/breadth_actual_output_store.v1.schema.json"
      - "contracts/market_memory/spy_daily_price_source_observation.v1.schema.json"
      - "contracts/market_memory/spy_raw_close_ratio_snapshot.v1.schema.json"
      - "contracts/market_memory/technicals_actual_output_capture_receipt.v1.schema.json"
      - "contracts/market_memory/technicals_actual_output_store.v1.schema.json"
      - "contracts/market_memory/spy_experience_registration.v1.schema.json"
      - "contracts/market_memory/spy_experience_opportunity.v1.schema.json"
      - "contracts/market_memory/spy_experience_outcome_revision.v1.schema.json"
      - "contracts/market_memory/spy_experience_population_receipt.v1.schema.json"
      - "contracts/market_memory/event_time_uncertainty.v1.schema.json"
      - "contracts/market_memory/market_session_window.v1.schema.json"
      - "contracts/market_memory/sensitivity_replay_plan.v1.schema.json"
      - "contracts/market_memory/option_oi_probe_receipt.v1.schema.json"
      - "contracts/market_memory/spy_option_oi_source_observation.v1.schema.json"
      - "contracts/market_memory/option_oi_capture_receipt.v1.schema.json"
      - "contracts/market_memory/option_oi_store.v1.schema.json"
      - "contracts/market_memory/state_snapshot.v1.schema.json"
      - "contracts/market_memory/trial_registration.v1.schema.json"
      - "contracts/market_memory/forecast_record.v1.schema.json"
      - "contracts/market_memory/outcome_record.v1.schema.json"
      - "contracts/market_memory/baseline_forecast_bundle.v1.schema.json"
      - "contracts/market_memory/event_score_record.v1.schema.json"
      - "contracts/market_memory/operational_playback_catalog.v1.schema.json"
      - "contracts/market_memory/retrieval_registration.v1.schema.json"
      - "contracts/market_memory/episodic_retrieval_record.v1.schema.json"
      - "contracts/market_memory/operating_cortex_registration.v1.schema.json"
      - "contracts/market_memory/operating_cortex_packet.v1.schema.json"
      - "contracts/market_memory/feature_promotion_audit.v1.schema.json"
      - "contracts/market_memory/options_signal_episode_production_record.v1.schema.json"
      - "contracts/symbol_directory/symbol_directory_completion_receipt.v1.schema.json"
      - "config/market_memory_canary.v1.json"
      - "config/market_memory_technical_price_basis.v1.json"
      - "config/market_memory_option_oi_source.v1.json"
      - "config/market_memory_spy_experience_registration.v1.json"
      - "config/market_memory_spy_experience_registration.v2.json"
      - "scripts/collect_release_target_vintages.py"
      - "scripts/collect_release_cpi_official_truth.py"
      - "scripts/build_release_cpi_truth_parity.py"
      - "scripts/capture_market_memory_context.py"
      - "scripts/initialize_market_memory_w1a.py"
      - "scripts/ingest_market_memory_sources.py"
      - "scripts/ingest_market_memory_sources_spy.py"
      - "scripts/capture_market_memory_technicals_v2.py"
      - "scripts/accrue_market_memory_spy_experience_v2.py"
      - "scripts/project_market_memory_context.py"
      - "scripts/audit_options_market_memory_context.py"
      - "scripts/ingest_market_memory_identity.py"
      - "scripts/capture_market_memory_breadth.py"
      - "scripts/capture_market_memory_technicals.py"
      - "scripts/accrue_market_memory_spy_experience.py"
      - "scripts/capture_market_memory_option_oi.py"
      - "scripts/capture_market_memory_options_episodes.py"
      - "data/options_signal_episode/episodes.jsonl"
      - "engine/run.py"
      - "data/regime/latest.json"
      - "app/deploy/macro-market-memory-source.service"
      - "app/deploy/macro-market-memory-source.timer"
      - "app/deploy/macro-market-memory-source-spy-rest.service"
      - "app/deploy/macro-market-memory-source-spy-rest.timer"
      - "app/deploy/macro-market-memory-technicals-v2.service"
      - "app/deploy/macro-market-memory-technicals-v2.timer"
      - "app/deploy/macro-market-memory-experience-v2.service"
      - "app/deploy/macro-market-memory-experience-v2.timer"
      - "app/deploy/macro-market-memory-context.service"
      - "app/deploy/macro-market-memory-context.timer"
      - "app/deploy/macro-market-memory-options-context-audit.service"
      - "app/deploy/macro-market-memory-options-context-audit.timer"
      - "app/deploy/macro-market-memory-identity.service"
      - "app/deploy/macro-market-memory-identity.timer"
      - "app/deploy/macro-market-memory-breadth.service"
      - "app/deploy/macro-market-memory-breadth.timer"
      - "app/deploy/macro-market-memory-technicals.service"
      - "app/deploy/macro-market-memory-technicals.timer"
      - "app/deploy/macro-market-memory-experience.service"
      - "app/deploy/macro-market-memory-experience.timer"
      - "app/deploy/market-memory-spy-rest-prereqs.sh"
      - "app/deploy/market-memory-options-prereqs.sh"
      - "app/deploy/market-memory-options-unit-boundary.sh"
      - "app/deploy/market-memory-options-runtime-fence.sh"
      - "app/deploy/market-memory-options-dropin-migration.sh"
      - "app/deploy/codex-runtime-setup.sh"
      - "app/deploy/macro-market-memory-options.service"
      - "app/deploy/macro-market-memory-options.timer"
      - "app/deploy/macro-market-memory-production-records.service"
      - "app/deploy/macro-market-memory-production-records.timer"
      - "app/deploy/README.md"
      - "docs/ops/market-memory-option-oi-canary.md"
      - "app/deploy/api-setup.sh"
      - "app/deploy/update.sh"
      - "tests/test_market_memory.py"
      - "tests/test_market_memory_pit.py"
      - "tests/test_market_memory_sources.py"
      - "tests/test_market_memory_source_deploy.py"
      - "tests/test_market_memory_m0d_v2.py"
      - "tests/test_market_memory_identity.py"
      - "tests/test_market_memory_identity_observation.py"
      - "tests/test_market_memory_identity_store.py"
      - "tests/test_market_memory_identity_observation_deploy.py"
      - "tests/test_ingest_market_memory_identity.py"
      - "tests/test_market_memory_breadth_observation.py"
      - "tests/test_market_memory_breadth_store.py"
      - "tests/test_market_memory_breadth_deploy.py"
      - "tests/test_market_memory_technical_observation.py"
      - "tests/test_market_memory_technical_store.py"
      - "tests/test_capture_market_memory_technicals.py"
      - "tests/test_market_memory_technicals_deploy.py"
      - "tests/test_market_memory_experience_accrual.py"
      - "tests/test_market_memory_experience_deploy.py"
      - "tests/test_market_memory_replay.py"
      - "tests/test_market_memory_option_oi_observation.py"
      - "tests/test_market_memory_option_oi_store.py"
      - "tests/test_capture_market_memory_option_oi.py"
      - "tests/test_market_memory_options_deploy.py"
      - "tests/test_market_memory_forward.py"
      - "tests/test_market_memory_forward_store.py"
      - "tests/test_market_memory_scoring.py"
      - "tests/test_market_memory_playback.py"
      - "tests/test_market_memory_playback_api.py"
      - "tests/test_market_memory_retrieval.py"
      - "tests/test_market_memory_operating_cortex.py"
      - "tests/test_market_memory_promotion_audit.py"
      - "tests/test_market_memory_production_records.py"
      - "tests/test_market_memory_production_records_deploy.py"
      - "tests/test_market_memory_projection.py"
      - "tests/test_market_memory_trusted.py"
      - "tests/test_market_memory_context_deploy.py"
      - "tests/test_regime_latest_json_boundary.py"
      - "tests/test_release_target_truth.py"
      - "research/GLOBAL_MARKET_INTELLIGENCE_MASTERPLAN_BY_FABLE.md"
      - "config/synapse.yml"
      - "docs/SIGNAL_BUS.md"
      - "tests/test_release_cpi_official_truth.py"
      - "tests/test_release_cpi_target_history.py"
      - "tests/fixtures/release_cpi_truth/**"
      - "engine/release_cpi_official_truth.py"
      - "engine/release_cpi_target_history.py"
      - "data/release_forecast/cpi_truth/**"
      - "data/release_forecast/official_actual_defects.json"
      - "data/fred_vintage/release_targets/**"
      - "tests/test_build_market_memory_page.py"
      - "data/breadth/breadth.parquet"
      - "data/breadth/constituents.parquet"
      - "lib/nyse_calendar.py"
      - "research/licenses/MASSIVE_ENTITLEMENT_RECORD.md"
      - "research/KONSEKI_CLEAN_ROOM_MARKET_MEMORY_AND_COGNITIVE_ARCHITECTURE_FOR_FABLE_2026-08-08.md"
      - "tests/test_china_news.py"
      # Wave 3B-B2: strict offline XBRL/iXBRL parsing and its receipt-bound
      # immutable extraction artifact.  Test/fixture-only changes must reach the
      # same gate as collector and engine edits.
      - "tests/test_sec_filing_parser.py"
      - "tests/test_fundamental_forensics_ixbrl_extraction.py"
      - "tests/fixtures/sec_filings/**"
      # Capital Structure Intelligence W0-W1: evidence contracts, offline compiler,
      # PIT graph, and source-store/SEC fixtures must never exist as an unrun suite.
      # collectors/** and scripts/collect.py cover collector edits; these explicit
      # paths cover contract-, compiler-, fixture-, and test-only changes too.
      - "engine/capital_structure/**"
      - "app/capital_structure.py"
      - "app/requirements.txt"
      - "scripts/compile_capital_structure_events.py"
      - "scripts/compile_capital_structure_document_terms.py"
      - "scripts/compile_capital_structure_covenant_terms.py"
      - "scripts/materialize_capital_structure_share_counts.py"
      - "scripts/retain_capital_structure_share_counts.py"
      - "scripts/build_capital_structure_projection.py"
      - "scripts/build_capital_structure_page.py"
      - "contracts/capital_structure_*.schema.json"
      - "docs/CAPITAL_STRUCTURE_INTELLIGENCE_CONTRACT.md"
      - "templates/capital_structure.html.j2"
      # The two nightly-wiring guards ride in the capital-structure pack. They pin
      # the 2026-08-06 split from both sides: the chain left the collect job for its
      # own `needs: collect` job that nothing needs in turn, and the source ledger
      # crosses that job boundary as a receipt-verified artifact because the market
      # checkpoint deliberately never commits it. Their SUBJECTS — daily.yml,
      # config/dag.yml — already reach CI via ".github/workflows/**" and the
      # config/dag.yml entry; these lines cover the test-only direction, so an edit
      # that only re-pins a guard still runs the job that carries it.
      # NOTE the second name does NOT match the "tests/test_capital_structure_*.py"
      # glob further down — it is test_DAILY_capital_structure_job.py — so it has to
      # be listed by name or it goes dark on a test-only edit.
      - "tests/test_daily_collect_commit_path.py"
      - "tests/test_daily_capital_structure_job.py"
      - "templates/capital_structure_boot.js"
      - "templates/capital_structure.css"
      - "templates/capital_structure.js"
      - "tests/test_capital_structure_*.py"
      - "tests/test_build_capital_structure_projection.py"
      - "tests/test_sec_capital_structure.py"
      - "tests/test_sec_capital_structure_companyfacts.py"
      - "tests/test_nwqs_c_dilution.py"
      - "tests/fixtures/capital_structure/**"
      # LHB-W2b 8-K item expansion: test-only edits must still trigger the job
      - "tests/test_edgar_8k_items.py"
      # W2-A fundamentals universe widening: test-only edits must still trigger the job
      # (collectors/** already covers the collector itself)
      - "tests/test_edgar_universe_widening.py"
      # LHB-W3 A1 falsifier packet: engine + builder + admin block + test re-run together.
      - "engine/falsifier_packet.py"
      - "scripts/research/build_falsifier_packets.py"
      - "tests/test_falsifier_packet.py"
      # Stateful cycle-ontology tripwires: both committed state inputs, the engine,
      # and the formerly dark focused suite must start their scoped owner.
      - "data/cycle_ontology/falsifiers.json"
      - "data/cycle_ontology/tripwire_state.json"
      - "engine/falsifier_tripwires.py"
      - "tests/test_falsifier_tripwires.py"
      # china_news engine contract (theme gate, flash filter, bilingual synthesis
      # labels — doctrine Law 2): engine + test re-run together.
      - "engine/china_news.py"
      - "tests/test_china_news_engine.py"
      # LHB-W2c symbol-directory archival: collector + test must re-run together.
      - "tests/test_symbol_directory.py"
      - "tests/test_symbol_directory_completion_receipts.py"
      # W1 CNH interaction & sell-side collectors: collectors/** already triggers the
      # job, but a test-only edit (a re-pinned live fixture) must re-run it too.
      - "tests/test_china_irm_collector.py"
      - "tests/test_china_einteraction_collector.py"
      - "tests/test_china_reports_collector.py"
      - "tests/test_china_holder_counts_collector.py"
      # Post-P-B2 China Intelligence PIT hist (wired into china-native-collectors).
      # collectors/** already covers the five subject modules; the suite itself
      # must be named so a test-only re-pin still starts the pack.
      - "tests/test_cn_intel_pit_accrual.py"
      # W3 CNH policy/OMO/trade-detail collectors. Same wiring law: collectors/**
      # already triggers the job, but a test-only edit (a re-pinned live fixture) must
      # re-run it too. test_china_official_corpora.py was named by NO job before this —
      # the corpus fetcher's whole surface merged untested for its entire life, which
      # is exactly the #1253 shape, and W3 adds a gov.cn json_feed organ to it.
      - "tests/test_china_omo_collector.py"
      - "tests/test_china_trade_detail_collector.py"
      - "tests/test_china_official_corpora.py"
      - "tests/fixtures/china_omo/**"
      - "tests/fixtures/china_trade_detail/**"
      - "tests/fixtures/china_official_corpora/**"
      # Full-A TuShare spine (#5116), wired into china-native-collectors. The
      # collector and its suite are already reachable through collectors/** and
      # tests/**; the MANIFEST SCHEMA is not, and it is the one subject the suite
      # actually opens (Draft202012Validator over the emitted manifest). Without
      # this entry a PR that edits only the schema — the exact edit that can break
      # the spine's fail-closed contract — never starts the job that would catch it.
      - "contracts/cn_tushare_a_share_spine_manifest.v1.schema.json"
      - "research/TUSHARE_WIRING_TAKEOVER_2026-08-09.md"
      - "contracts/cn_tushare_minutes_manifest.v1.schema.json"
      # Government Revenue Foresight (unrun-suite backfill 2026-08-03): the
      # builder/fence/UI suites and the federal-award collector suites, plus
      # the one subject file no broad glob matches — engine/government_revenue/**
      # and collectors/** are covered, app/ is enumerated per-file.
      - "app/government_revenue.py"
      # Wave 9D issuer graph expansion. The suite's subject modules are already
      # reachable through engine/** and collectors/**, so what has to be named
      # here is the suite itself and its committed EDGAR fixtures — the fixtures
      # ARE the subject for four of its regression tests (index-headers parsing,
      # character references, spacer cells, run-together cells), and an edit to a
      # fixture alone must be able to start the job that reads it.
      - "tests/test_government_revenue_issuer_graph_expansion.py"
      - "tests/fixtures/issuer_evidence/**"
      - "tests/test_build_government_revenue.py"
      - "tests/test_check_government_revenue_projection.py"
      - "tests/test_government_revenue_dossiers.py"
      - "tests/test_government_revenue_subaward_api.py"
      - "tests/test_government_revenue_ui.py"
      - "tests/test_government_revenue_dossier_ui.py"
      - "tests/test_government_revenue_briefcase_ui.py"
      # D3 temporal contract suite (wired by the same PR that added it).
      - "tests/test_government_revenue_temporal_contract.py"
      # D4 Company Financial Truth Bridge suite (wired by the same PR that added
      # it). Its subject files (templates/government-revenue-dossiers.js,
      # templates/government_revenue.html.j2) already ride "templates/**" above;
      # this line is the suite itself, per the contract-delta law.
      - "tests/test_government_revenue_company_bridge.py"
      # The prophet↔govrev context bridge, wired into unrun-government-revenue by
      # the same PR that added this line. It was named by nothing in .github/ at
      # all, so it was inert AND untriggerable — the strictly-dark shape.
      - "tests/test_prophet_government_revenue_context.py"
      # Waves 9E/9F — shadow cross-check packets and the Prophet post-selection
      # annotation boundary. Both suites are named here because their subjects
      # reach OUTSIDE engine/government_revenue/**: the 9F import-boundary test
      # walks every engine/prophet*.py transitively, and the wired seam lives in
      # engine/prophet_bridge.py, so an edit to a Prophet module must be able to
      # start the job that proves the boundary still holds.
      - "tests/test_government_revenue_shadow_context.py"
      - "tests/test_government_revenue_prophet_annotation.py"
      - "engine/prophet_bridge.py"
      - "tests/test_sam_gov_opportunities.py"
      # quota-aware radar-leg collector (2026-08-05 SAM key integration): the
      # batch/rotation/429-stop/merge disciplines that fit the shared ~10/day key.
      - "tests/test_sam_gov_radar_leg.py"
      - "tests/test_usaspending_awards.py"
      - "tests/test_usaspending_subawards.py"
      - "tests/test_census_trade.py"
      # Darkness backfill 2026-08-06: these nineteen suites were named by NO
      # pack, NO workflow, and no catch-all sweep, so editing one could not even
      # fire ci.yml.  They include the forward award-event spine's own state
      # machine — the surface whose persistence break shipped to production on
      # 2026-08-06 precisely because nothing ran its guards.
      - "tests/test_government_revenue.py"
      - "tests/test_government_revenue_award_events.py"
      - "tests/test_government_revenue_award_spine.py"
      - "tests/test_government_revenue_api.py"
      - "tests/test_government_revenue_budget_graph.py"
      - "tests/test_government_revenue_candidate_api.py"
      - "tests/test_government_revenue_candidate_projection.py"
      - "tests/test_government_revenue_candidates.py"
      - "tests/test_government_revenue_contracts.py"
      - "tests/test_government_revenue_entity_resolution.py"
      - "tests/test_government_revenue_freshness.py"
      - "tests/test_government_revenue_idv_bridge.py"
      - "tests/test_government_revenue_idv_dossiers.py"
      - "tests/test_government_revenue_neuralweb.py"
      - "tests/test_government_revenue_opportunities.py"
      - "tests/test_government_revenue_recipient_graph.py"
      - "tests/test_government_revenue_subaward_dossiers.py"
      - "tests/test_government_revenue_wave8_api.py"
      - "tests/test_government_revenue_workspace.py"
      - "tests/test_usaspending_idv_graph.py"
      # Recipient->issuer attribution (job: unrun-government-revenue, wired
      # 2026-08-06). engine/** already covers entity_resolution.py and
      # scripts/*.py the proposal tool, but both halves are listed on purpose:
      # a test-only edit, or a re-pinned EX-21 fixture, must re-run the suite
      # that measures it. The fixtures are two VERBATIM SEC exhibits pinned by
      # sha256 — swapping one for a synthetic is exactly the edit that has to
      # retrigger. (test_government_revenue_recipient_graph.py and
      # test_government_revenue_entity_resolution.py are already named in the
      # darkness-backfill list directly above.)
      - "scripts/propose_government_revenue_recipient_graph.py"
      - "tests/test_propose_government_revenue_recipient_graph.py"
      - "tests/fixtures/govrev_issuer_evidence/**"
      # Amount-class conflation gate (job: unrun-government-revenue, wired 2026-08-07).
      # The suite's subjects are the taxonomy (engine/**), the collector's canonical
      # number-column declarations it DERIVES coverage from (collectors/**), and the
      # rendered surfaces (templates/**) — all already reachable.  What is NOT reachable
      # through any broad glob is the guard script itself: there is no "scripts/**"
      # entry in this filter, so an edit to the reader would ship without ever running
      # the suite that measures it.  Both halves are named on purpose.
      - "scripts/check_government_revenue_amount_semantics.py"
      - "tests/test_government_revenue_amount_semantics.py"
      # W3 CNH part B: the five native flash wires + the forward calendar organ.
      # engine/** is NOT a blanket trigger here, so each of these must be named or a
      # parser/aggregator edit merges with the suite unrun. test_cn_newswires.py was
      # itself named by no job until W3 — the #1253 shape on the fastest-rotting
      # surface in the tree (five vendor JSON payloads, zero of them contractual).
      - "engine/cn_newswires.py"
      - "engine/china_calendar.py"
      - "engine/china_event_calendar.py"
      - "engine/hk_event_calendar.py"
      - "scripts/build_china_calendar.py"
      - "tests/test_cn_newswires.py"
      - "tests/test_china_calendar.py"
      - "tests/fixtures/cn_newswires/**"
      # china_news_intel is the CONSUMER of those wires (slug collision, timestamp
      # quality, the qbus rows) and its W2 migration suite was named by NO job — the
      # #1253 shape again. A wire_sources change or a _timestamp_quality edit could
      # merge with the only suite that pins them unrun.
      - "engine/china_news_intel.py"
      - "tests/test_china_news_intel_w2.py"
      # Whole-board 涨跌家数: the heatmap card quotes a ~5,200-name count, and the
      # engine gates the row against the map's own session. Both suites were
      # previously named by NO job (#1253 wiring law) — test_market_heatmap.py
      # included — so a regression in either would merge green.
      - "collectors/china_board_breadth.py"
      - "tests/test_china_board_breadth.py"
      - "engine/market_heatmap.py"
      - "scripts/build_market_heatmap.py"
      - "tests/test_market_heatmap.py"
      # china_heatmap's anonymous tier preview (SEO_SUPERCHARGE W2). The subject
      # files above already trigger, but the guard itself must too — a PR that
      # only weakens the test would otherwise reach main unchecked.
      - "tests/test_china_heatmap_gate.py"
      # LHB-W3 winner-autopsy B-family feature columns: engine + builder + test re-run together.
      - "tests/test_winner_autopsy.py"
      # sector_pulse wiring tripwire (#1193 post-mortem): PR #1170 injected sector_pulse
      # into build_stock_library.py; an unrelated refactor 90 min later silently deleted
      # the block and merged green because nothing here covered the builder. The wiring
      # test pins producer (AST) + template consumer, so the builder must trigger CI.
      - "scripts/build_stock_library.py"
      - "tests/test_sector_pulse_wiring.py"
      # Options Alpha W1.3/W-C/W-OVC stamp + entry-state guards: engine files are
      # covered by engine/**, but the test files need explicit paths so a test-only
      # change (e.g. the 2026-07-23 W-OVC fixture re-anchor) still re-runs the
      # whitelist step that carries them.
      - "tests/test_options_stamp.py"
      - "tests/test_options_entry_state.py"
      # China Lens / Intel Hub W0.2: builder + test must re-run together so a
      # headline crash or schema drift fails RED before merging.
      - "scripts/build_intel_hub.py"
      - "tests/test_china_lens_hub.py"
      # china.html.j2 Prophet-board render guards: the W1-C three-shelf + anv2 board test,
      # the W0.9 standout-header copy test, and the board-track-record test render against
      # templates/china.html.j2 (already in templates/**), but the test-only files need
      # explicit paths so a test-only re-pin still re-runs the rot-sweep step that carries them.
      - "tests/test_china_stocks_w1c_render.py"
      - "tests/test_china_stocks_copy_w09.py"
      - "tests/test_china_board_track_render.py"
      # china-alpha W2 narrative confluence (#1118): W2-A narrative heat + name tags
      # (engine/china_narrative_tags.py), W2-B builder wiring + ledger schema
      # (engine/china_standout_track.py) + the china.html.j2 render group.  Every
      # source side is already covered — engine/**, templates/**, lib/** — so these
      # two entries exist for the same reason as the three above: a test-only re-pin
      # must still re-run the rot-sweep step that carries them.  Wired 2026-07-26
      # after both files sat red and unrun (no ci.yml job named either one).
      - "tests/test_china_alpha_w2a.py"
      - "tests/test_china_alpha_w2b.py"
      # china_act_now assembler contract tests (W8-R3/R7 organ rider): engine file is
      # covered by engine/**, but a test-only re-pin must still re-run the rot-sweep
      # step that carries it (2026-07-24 born-red organ-chip re-pin).
      - "tests/test_china_act_now.py"
      # W4 stock-personality surface suite (re-pinned 2026-07-25 after PR #3256's Prophet
      # card removed the dashboard chips): template/engine/script sides are covered by
      # their globs; the test file needs an explicit path so a test-only re-pin still
      # re-runs the rot-sweep step that carries it.
      - "tests/test_stock_personality_w4_surfaces.py"
      # Earnings & Company Event suite, Wave 1B: the canonical EDGAR filing key
      # (cik, accession) and the Exhibit 99.1 release binding. Both readers and
      # both new modules are named, plus the fixtures — tests/** is deliberately
      # NOT a catch-all here, and BOTH EDGAR reader suites were measured
      # strictly dark (named by zero `run:` steps, matched by no glob) before
      # this block and the earnings-release-identity job were added together.
      - "engine/earnings_release/**"
      - "collectors/edgar_earnings_8k.py"
      - "engine/marketing/edgar_earnings_wire.py"
      - "tests/fixtures/earnings_release/**"
      - "tests/test_edgar_filing_identity_join.py"
      - "tests/test_earnings_release_binding.py"
      - "tests/test_edgar_earnings_8k.py"
      - "tests/test_marketing_edgar_earnings_wire.py"
      # The release binding normalizes through the disclosure-diff source-text
      # engine, so a change there must also start the Wave 1B pack; the glob
      # below already covers it.
      #
      # Filing Forensics v1: immutable SEC lineage kernel + display-only workbench.
      # Keep every contract, fixture, page, collector, and context test wired even
      # when a PR changes only its config/schema/test side.
      - "engine/fundamental_forensics/**"
      - "app/forensics.py"
      - "app/main.py"
      - "collectors/edgar_forensics.py"
      - "collectors/fundamental_forensics_acquisition.py"
      - "collectors/fundamental_forensics_companyfacts.py"
      - "collectors/sec_document_spine.py"
      - "scripts/build_fundamental_forensics.py"
      - "scripts/build_fundamental_forensics_disclosures.py"
      - "scripts/run_fundamental_forensics_wave2.py"
      - "scripts/run_fundamental_forensics_attested_history.py"
      # Disclosure-projection bundle (2026-08-08): the SEC acquire/project/sync lane
      # moved off daily.yml's engine job, and the engine now restores this bundle as its
      # hard gate. engine/** and config/fundamental_forensics/** already cover the module
      # and the pinned target list; the CLI, the new lane workflow, and its pin suite need
      # their own entries so an edit to any of them re-runs these contracts.
      - "scripts/fundamental_forensics_disclosure_bundle.py"
      - "engine/fundamental_forensics/disclosure_bundle.py"
      - "config/fundamental_forensics/wave2_targets.v1.json"
      - ".github/workflows/filing-forensics-sec.yml"
      - "tests/test_filing_forensics_sec_lane.py"
      - "scripts/run_fundamental_forensics_broad_sec.py"
      - ".github/workflows/filing-forensics-broad-sec.yml"
      - "tests/test_filing_forensics_broad_sec_lane.py"
      - "contracts/fundamental_forensics_broad_sec_run.schema.json"
      - "contracts/fundamental_forensics_broad_sec_issuer_manifest.schema.json"
      # B4F is a controlled, one-shot AAPL bootstrap. Its runner and workflow
      # are both production-control surfaces: a test-only edit is covered by
      # the broad suite glob below, while an edit to the runner/workflow itself
      # must also start the pack that executes its no-network safety contracts.
      - "scripts/seed_fundamental_forensics_attested_history.py"
      - ".github/workflows/attested-history-aapl-seed.yml"
      - "requirements/attested-history-macos-arm64-py312.lock"
      - "config/fundamental_forensics.yml"
      - "config/fundamental_forensics/**"
      - "config/fundamental_forensics_disclosure_diff.v1.json"
      - "contracts/fundamental_forensics_run.schema.json"
      - "contracts/fundamental_forensics_health.schema.json"
      - "contracts/fundamental_forensics_attested_history_operator.schema.json"
      - "contracts/fundamental_forensics_attested_history_preflight_receipt.schema.json"
      - "contracts/financial_intelligence_packet.schema.json"
      - "scripts/build_financial_intelligence_packet.py"
      - ".github/workflows/attested-history-operator.yml"
      - "templates/fundamental_forensics.html.j2"
      - "templates/fundamental_forensics.css"
      - "templates/fundamental_forensics.js"
      - "tests/fixtures/fundamental_forensics/**"
      - "tests/test_fundamental_forensics_*.py"
      # Cross-pin for the four detector ids that exist in THREE implementations:
      # scripts/build_fundamental_forensics.py (inline fraction literals),
      # engine/moat_falsifiers.py (pp module constants, ships to production ticker
      # pages), and config/fundamental_forensics.yml (registry data). All three
      # subjects already ride triggers above ("engine/**",
      # "scripts/build_fundamental_forensics.py", "config/fundamental_forensics.yml"),
      # but the suite name does NOT match the broad
      # "tests/test_fundamental_forensics_*.py" glob, so a test-only edit to the
      # cross-pin itself needs this explicit entry to re-run it.
      - "tests/test_forensic_detector_crosspin.py"
      # Wave 3B-B4A/B4B/B4D-A/B4F: the broad fundamental-forensics glob also
      # matches these suites, but the explicit names document the sealed-history
      # reader, bounded serving, pinned-source materializer, one-shot seed, and
      # dimensions-unknown evidence-bridge contracts. They also protect
      # test-only re-pins if the broad glob is ever narrowed.
      - "tests/test_fundamental_forensics_attested_query_snapshots.py"
      - "tests/test_fundamental_forensics_attested_history_reader.py"
      - "tests/test_fundamental_forensics_filing_package_materializer.py"
      - "tests/test_fundamental_forensics_companyfacts_pinned_loader.py"
      - "tests/test_fundamental_forensics_attested_history_materializer.py"
      - "tests/test_fundamental_forensics_attested_history_operator.py"
      - "tests/test_fundamental_forensics_attested_history_api_store.py"
      - "tests/test_fundamental_forensics_attested_history_pilot.py"
      - "tests/test_fundamental_forensics_attested_history_seed.py"
      - "tests/test_fundamental_forensics_attested_occurrence_governance.py"
      - "tests/test_fundamental_forensics_health.py"
      - "tests/test_sec_document_spine.py"
      - "tests/test_forensics_api.py"
      - "tests/test_edgar_forensics_collector.py"
      - "tests/test_context_api.py"
      # BioCatalyst B1/B1b/B2: ClinicalTrials.gov evidence lineage, normalized
      # trial facts, exact Record History replay, authenticated read API, and its rendered
      # workbench. Keep the source registry, contracts, public reader, deployment
      # isolation, and every test half together: current-only source facts must not
      # silently gain signal authority, and a test-only security re-pin must still
      # start the pack that executes it.
      - "engine/biocatalyst/**"
      - "engine/sector_intelligence/contracts.py"
      - "engine/sector_intelligence/launch_slo_verifier.py"
      - "collectors/biocatalyst/**"
      - "app/biocatalyst.py"
      - "app/main.py"
      - "app/requirements.txt"
      - "scripts/biocatalyst_worker.py"
      - "scripts/biocatalyst_regulatory_worker.py"
      - "scripts/build_biocatalyst.py"
      - "scripts/build_site.py"
      - "config/biocatalyst_sources.yml"
      - "config/biocatalyst_outcomes.yml"
      - "config/biocatalyst_launch_slo_manifest.yml"
      # Closed-beta source manifest: tests/test_biocatalyst_f0_delta_and_closed_beta_manifest.py
      # reads it by a __file__-rooted path join, so an edit to the manifest alone must be
      # able to start the pack that runs its guard (ops.ci_trigger_closure, #4693).
      - "config/biocatalyst_closed_beta_source_manifest.yml"
      # BC-D0a: finite design-reference corpus and non-authorizing product
      # acceptance contract. These paths must reach the same contracts pack even
      # when an edit changes only a PNG/hash/spec/test, otherwise a reference can
      # drift while its integrity verifier never runs.
      - "config/biocatalyst_product_acceptance.yml"
      - "config/sector_intelligence_ownership.yml"
      - "config/site_access.yml"
      - "contracts/biocatalyst/**"
      - "data/biocatalyst/fixtures/**"
      - "mockups/refs/biocatalyst/**"
      - "scripts/verify_biocatalyst_d0a_references.py"
      - "research/BIOCATALYST_D0A_IA_STATE_CONTENT_CONTRACT.md"
      # Same idiom as the line above: tests/test_biocatalyst_acceptance_v2.py reads this
      # adjudication by path literal, so an edit to it alone must start the contracts pack.
      - "research/BIOCATALYST_D0A_DESIGN_ADJUDICATION_2026-08-06.md"
      # The operator ruling tests/test_biocatalyst_fixed_cohort_deployment.py reads by
      # path literal. Without it a PR touching only the ruling never starts
      # biocatalyst-contracts, the job that runs the suite (#3488 shape).
      - "research/BIOCATALYST_OPERATOR_RULING_2026-08-07.md"
      - "templates/biocatalyst.*"
      - "templates/_navlinks.html.j2"
      - "templates/_site_nav.html.j2"
      - "templates/plans.html.j2"
      - "app/deploy/macro-api.service"
      - "app/deploy/macro-biocatalyst.service"
      - "app/deploy/macro-biocatalyst.timer"
      - "app/deploy/biocatalyst-*.sh"
      - "app/deploy/biocatalyst-*.py"
      - "app/deploy/biocatalyst-requirements.txt"
      - "app/deploy/update.sh"
      - "docs/biocatalyst_operations_runbook.md"
      - "docs/BIOCATALYST_LAUNCH_SLO_OFFLINE_VERIFIER.md"
      - "tests/test_biocatalyst_*.py"
      - "tests/test_clinicaltrials_biocatalyst.py"
      - "tests/test_clinicaltrials_history.py"
      - "tests/test_sector_intelligence_contracts.py"
      - "tests/test_sector_intelligence_ownership.py"
      - "tests/test_launch_slo_evidence_verifier.py"
      - "tests/test_deploy_update_self_heal.py"
      # W5a DAG conformance: any change to a workflow, the dag.yml, the checker, or
      # the test file must re-prove the conformance check still holds.
      - ".github/workflows/**"
      - "config/dag.yml"
      - "scripts/check_dag_conformance.py"
      - "tests/test_dag_conformance.py"
      # Nightly lane budgets, run in the same dag-conformance job. Its subject is
      # .github/workflows/daily.yml (already covered above); this entry is the other
      # half — a PR editing only the budget test must reach its own guard.
      - "tests/test_tech_lab_offrender_budget.py"
      # W2 nightly runtime telemetry, run in the same dag-conformance job. daily.yml
      # itself is covered by ".github/workflows/**" above; these entries are the
      # other half — a PR editing only the timings module, its finish wrapper, or
      # the test must still reach the wiring + tripwire guard.
      - "scripts/nightly_timings.py"
      - "scripts/nightly_timings_report.py"
      - "scripts/ci/nightly_timings_finish.sh"
      - "tests/test_nightly_timings.py"
      # W-L0 daily ET regime gate, run in the same dag-conformance job. The gate's
      # subject is .github/workflows/daily.yml (already covered above); this entry is
      # the other half — a PR editing only the gate's test must reach its own guard.
      - "tests/test_daily_et_gate.py"
      # 2026-08-12 (#5416) workflow size cap: the guard test must reach its own
      # suite when only the test itself is edited (same one-sided-trigger trap
      # as the et_gate entry above; workflow files are already path-covered).
      - "tests/test_workflow_file_size.py"
      # 2026-08-13 US nightly cron redundancy (the CN five-slot idiom, ported). The
      # actor and its suite must reach the workflow-yaml job when either is edited
      # alone; the workflow it schedules is already covered by ".github/workflows/**".
      - "scripts/nightly_backstop.py"
      - "tests/test_nightly_backstop.py"
      # 2026-08-13 nightly liveness dead-man switch — the other half of the same
      # incident. The guard and its suite must reach the dag-conformance job when
      # either is edited alone; the workflow it schedules is already path-covered
      # by ".github/workflows/**" above.
      - "scripts/check_nightly_liveness.py"
      - "tests/test_nightly_liveness.py"
      # TXI W1 transmission chains: the chain library, the compiler/engine, the runner, and
      # the test file must all trigger the chain suite so a bad edit reds the PR (the #3357
      # vacuous-green trap needs BOTH the paths entry here AND the job below).
      - "knowledge/transmission/**"
      - "engine/transmission_chains.py"
      - "scripts/run_transmission_chains.py"
      - "tests/test_transmission_chains.py"
      # TXI W4 distribution: the site-publication adapter, the Cascade Monitor page + its
      # render entry, and the two W4 test files must all trigger the chain suite too
      # (#3357 vacuous-green trap — paths here AND the run command in the job below).
      - "engine/transmission_publish.py"
      - "scripts/build_transmission.py"
      - "scripts/build_site.py"
      - "scripts/build_portfolio_ctx.py"
      - "templates/transmission.html.j2"
      - "engine/neuralweb/world_state.py"
      - "engine/neuralweb/mastermind_context.py"
      - "tests/test_transmission_publish.py"
      - "tests/test_transmission_cascade_monitor.py"
      - "tests/test_transmission_chains_nw.py"
      - "tests/fixtures/transmission/**"
      # TXI W3 transmission calibration: the historical episode miner, its runner, and its test
      # file must all trigger the calibration suite (same #3357 both-halves rule). The miner
      # imports the shared vectorized evaluator from engine/transmission_chains.py (already above).
      - "engine/transmission_calibration.py"
      - "scripts/run_transmission_calibration.py"
      - "tests/test_transmission_calibration.py"
      # TXI W5 Loop-C closure: the proposal pack generator, the ingest, the gated runner, its
      # config, and the test file must all trigger the chain suite (same #3357 both-halves
      # rule — paths here AND the run command in the job below). The ingest resolves proposals
      # through engine/transmission_chains.py (already above).
      - "scripts/propose_transmission_chains.py"
      - "scripts/ingest_transmission_chains.py"
      - "scripts/run_transmission_proposals.py"
      - "config/transmission_proposals.yml"
      - "tests/test_transmission_proposals.py"
      # workflow-yaml guard: every workflow file must stay loadable (metabolism-agenda
      # col-0 incident #2135→#2260 — unparseable file = failed run on EVERY push).
      - "scripts/check_workflow_yaml.py"
      # Wave B/C self-hosted canary policy and receipts. Workflow files are
      # reached by the catch-all above; these are the other half of the guard.
      - ".github/runner-policy.yml"
      - "scripts/check_runner_policy.py"
      - "scripts/resolve_ci_canary_ref.py"
      - "scripts/select_ci_canary_packs.py"
      - "scripts/monitor_ci_host_resources.py"
      - "scripts/capture_ci_canary_receipt.py"
      - "scripts/compare_ci_canary_receipts.py"
      - "ops/runner-host/**"
      - "tests/test_runner_policy.py"
      - "tests/test_ci_canary_tools.py"
      - "tests/test_ci_canary_workflows.py"
      # Wave B job-graph guard (2026-08-11), same two-halves law as the blocks around
      # it. The SUBJECT is this very file — the ci-plan/ci-pack/ci-gate wiring below —
      # which the "**" catch-all at the top of this list already reaches; this entry
      # is the OTHER half, so an edit to the guard's own source can start the job that
      # runs it. What goes dark without it: deleting `ci-gate` (every no-work PR then
      # reads unproven and never merges, #4779) or swapping the immutable PR base SHA
      # for a branch name in the planner's diff. Both stay green on their own.
      - "tests/test_ci_plan_workflow.py"
      # push-retry policy: the shared retry loop every lane's commit→push step sources.
      # A silent regression here throws away whole renders/ledger days (render.yml run
      # 30167139398, 2026-07-25 — ~95 min discarded on a lost ref race). Both entries
      # needed: .github/workflows/** covers the lanes, these cover the policy itself.
      - "scripts/ci/push_retry.sh"
      - "tests/test_push_retry.py"
      # Append-only base fence: the second policy that loop sources. `-X theirs` on a
      # file both sides appended to REPLACES the other side's rows — measured twice on
      # data/government_revenue/ (2026-08-07, 2026-08-18), the second orphaning a 26-row
      # candidate_ledger.jsonl issuance batch and reddening ci-pack-6 through ci-gate for
      # the whole fleet. Same two-halves law: .github/workflows/** covers the lanes that
      # call it, these cover the fence and its registry.
      - "scripts/ci/append_only_base_fence.py"
      - "config/append_only_artifacts.json"
      - "tests/test_append_only_base_fence.py"
      # Publish-ops / worker / live-plane guards (unrun backfill 2026-08-03):
      # dead-asset refs, ticker links, autostash containment, checkout bound,
      # admin research tools, earnings worker, china board watch lane, live
      # session floor. ops/** is the earnings worker's launchd estate — the
      # one subject tree no glob matched; other subjects ride existing
      # entries (scripts/*, admin/**, site/**, .github/workflows/**).
      - "ops/**"
      - "tests/test_rendered_ticker_links.py"
      - "tests/test_push_conflict_guard.py"
      - "tests/test_render_checkout_bound.py"
      - "tests/test_admin_research_tools.py"
      - "tests/test_earnings_worker_terminal.py"
      - "tests/test_earnings_worker_launchd.py"
      # test_earnings_worker_launchd.py pins the plist's EARNINGS_LLM_MODEL to
      # this file's openai_compat.model, because the plist env var OVERRIDES it
      # and a drift 404s every local call while the run completes on the paid
      # fallback (R0-A, 2026-08-06). ops/** starts the pack for the plist side;
      # without this line an edit to the CONFIG side could not start the pack
      # that runs the guard over it — the #3488 shape.
      - "config/earnings_qual.yml"
      # The two guards written after the 2026-08-02 ingest outage, same #3488
      # shape: the lanes they protect are workflow YAML and a scripts/ module, so
      # without these lines a test-only edit could not start the pack that runs
      # them. test_earnings_evidence_graph_deps.py derives the pyyaml requirement
      # from the live import chain; test_earnings_wire_freshness.py pins the
      # published-vs-upstream lag tripwire.
      - "tests/test_earnings_evidence_graph_deps.py"
      - "tests/test_earnings_wire_freshness.py"
      # The prompt/scorer-quality guards (2026-08-14). The prompt now lives in
      # engine/earnings_qual.py and the worker re-exports it, so an edit to
      # either side — or to the config bound they are gated against — must start
      # the pack that runs them.
      - "tests/test_earnings_prompt_quality.py"
      - "tests/test_earnings_score_quality.py"
      - "scripts/audit_earnings_score_quality.py"
      - "tools/earnings_worker/prompts.py"
      - "config/earnings_qual.yml"
      - "scripts/audit_earnings_wire_freshness.py"
      - "tests/test_china_board_watch_lane.py"
      - "tests/test_risk_state_live_session_floor.py"
      # Hosted-runner packing contract: changes to the orchestrator or its
      # fail-closed tests must exercise the workflow-yaml legacy job below.
      - "scripts/run_ci_pack.py"
      - "tests/test_ci_pack.py"
      # Same contract for the MERGE orchestrator, and the same #3488 shape: the
      # workflow-yaml job above runs tests/test_merge_on_green.py, but neither the
      # sweeper nor its tests could start it — a PR editing the thing that decides
      # every merge in this repo published no pack check at all, so the sweeper
      # reported it "unproven" and would never merge it (measured on #4968, which
      # had ZERO runs on its branch). A guard whose own change cannot trigger it is
      # a guard that reviews as covered and runs never.
      - "scripts/merge_on_green.py"
      - "tests/test_merge_on_green.py"
      # SKIP-ONLY suite guard (job: workflow-yaml). scripts/*.py already reaches
      # the checker; these two are the other halves. requirements.txt matters
      # because a `-r requirements.txt` install line is how several jobs declare
      # what they provide — dropping a package there can strand a skip gate, and
      # the trigger has to reach the guard for it to say so.
      - "tests/test_check_skip_only_suites.py"
      - "requirements.txt"
      # TRIGGER-CLOSURE guard (job: workflow-yaml). scripts/*.py already reaches
      # the checker; this is its other half, per the same two-halves law the guard
      # enforces on everything else.
      - "tests/test_ci_trigger_closure.py"
      # VINTAGE-PIN fence (job: vintage-pin-fence). scripts/** and tests/**
      # already start CI; these two name the pair the job runs so a rename of
      # either still reaches the lane that proves it.
      - "scripts/check_vintage_pin_fence.py"
      - "tests/test_vintage_pin_fence.py"
      # UNRUN-CENSUS discovery (job: workflow-yaml). scripts/*.py reaches
      # audit_unrun_tests.py itself; this is the test half. The census owns suite
      # discovery for all THREE darkness guards, so a regression here re-blinds
      # every one of them at once — which is what tests/ scoping did until #4693.
      - "tests/test_audit_unrun_tests.py"
      # The census's two EXCEPTION files, added 2026-08-09 when the bare run was
      # armed as a gate. These are not decoration: the whole point of a waiver is
      # that it is reviewed, and a PR that adds a row to either file while starting
      # no job would be exactly the "exception nobody checked" this gate exists to
      # prevent. check_ci_trigger_closure.py reds without them (the suite opens both
      # by path), which is the guard closing its own loop.
      - "config/unrun_test_baseline.json"
      - "config/unrun_test_waivers.yml"
      # Same shape, different ratchet: sealed-runtime entry scripts that must
      # not carry a module-load repo pin (T2b). A PR that adds a row while
      # starting no job would be the exception nobody checked.
      - "config/script_import_pin_sealed_runtime_waivers.yml"
      # ── the 31 subjects that guard found unreachable on its first run ──────
      # Each line is a file some suite in the packed legacy jobs actually reads
      # (import, whole-string path literal, or a __file__-rooted path join) and
      # that NO entry above matched — so a PR touching only that file merged
      # without ever running the suite that measures it. That is the #3488 shape
      # exactly, 31 times, none of it visible to audit_unrun_tests.py because
      # every one of those suites is reachable through its own test file.
      # These are cheap to carry: config/, contracts/, research/ pre-registrations
      # and unit files change rarely, so they widen the trigger surface barely at
      # all. Do not delete a line here without deleting the read that needs it —
      # scripts/check_ci_trigger_closure.py will fail the PR.
      - "admin/deploy/admin.service"
      - "admin/static/styles.css"
      - "app/deploy/codex-runtime-setup.sh"
      - "app/deploy/macro-live-fast.service"
      - "app/deploy/marketing-press-feeds.service"
      - "app/deploy/marketing-reply-desk.service"
      - "app/tape_symbols.py"
      - "config/causal_priors.yml"
      - "config/fable_mode_core.md"
      - "config/metabolism_schedule.yml"
      - "config/reflexes.yml"
      - "config/trade_flow_codes.yml"
      - "config/ux_simplicity_rules.yml"
      # engine/government_revenue/workspace.py loads four of these schemas at
      # RUNTIME (procurement_event.v2, procurement_workspace.v2,
      # entity_coverage.v1, recipient_resolution_coverage.v1) and its validator
      # fails CLOSED without them, so a schema edit that flips every payload to
      # invalid must be able to start the job that validates against it. Only 3 of
      # the 17 files here were named before, and check_ci_trigger_closure.py could
      # not catch the rest: it resolves what a TEST FILE reads (depth 1), and these
      # are read one layer deeper, inside the engine module the test imports. The
      # subtree glob closes the whole directory instead of re-deriving the list by
      # hand on every contract added. The explicit entries below are kept
      # deliberately — this list's own header says not to delete a line without
      # deleting the read that needs it.
      - "contracts/government_revenue/**"
      - "contracts/government_revenue/government_revenue_candidate.v1.schema.json"
      - "contracts/government_revenue/government_revenue_candidate_issuance_corrections.v1.schema.json"
      - "contracts/government_revenue/government_revenue_candidate_queue.v1.schema.json"
      - "contracts/government_revenue/government_revenue_dossiers.v1.schema.json"
      # Subjects of the nineteen suites this PR un-darkens. They were not
      # triggers before because no job ran the suites that read them, so
      # check_ci_trigger_closure.py had nothing to close over. Wiring the suites
      # is what makes these reachable — and the closure guard failed until they
      # were named, which is the guard doing its job.
      - "contracts/government_revenue/government_procurement_event.v2.schema.json"
      - "contracts/government_revenue/government_procurement_workspace.v2.schema.json"
      - "contracts/government_revenue/government_subaward_dossiers.v1.schema.json"
      - "config/government_revenue/candidate_historical_suppressions.v1.json"
      - "config/government_revenue/candidate_issuance_corrections.v1.json"
      - "config/government_revenue/idv_discovery.v1.json"
      - "contracts/marketing_publication_receipt.schema.json"
      - "research/DISLOCATION_VALIDATION.md"
      - "research/MARKETING_REPLY_DOCTRINE_BY_FABLE.md"
      - "research/OPTIONS_HISTORY_GAUNTLET_E1.md"
      - "research/PSS_RH1_RELIEF_HAZARD_PROSPECTIVE_CHARTER.md"
      - "research/US_BOARD_MEASUREMENT.md"
      - "research/VECTOR_IMPULSE_PREDICTION.md"
      - "research/WINNER_AUTOPSY_MASTERPLAN_BY_FABLE.md"
      - "research/agentic_media/X_GROWTH_UNIFIED_OPERATION_BY_FABLE.md"
      - "research/cycle_masterplan/PREREGISTRATION.md"
      - "research/cycle_masterplan/W42_HAZARD_VERDICT.md"
      - "research/entry_timing/WAVE2_REPORT.md"
      - "research/fable_exit/05_ENTITY_THESIS_MECHANISM_REGISTRY_HANDOFF.md"
      - "research/release_playbook/results/playbook_v1.json"
      - "tools/earnings_worker/run_worker.py"
      # render.yml scope contract (job: workflow-yaml). .github/workflows/**
      # already reaches render.yml itself; this entry is so a test-only edit
      # still starts CI, per the two-halves law.
      - "tests/test_render_intl_scope.py"
      - "tests/test_render_run_size_cap.py"
      - "tests/test_render_builder_ownership.py"
      - "tests/test_render_dead_asset_gate.py"
      - "tests/test_render_canada_scope.py"
      # OIP E8 options integrity suites (job: workflow-yaml). engine/**, scripts/** and
      # .github/workflows/** already reach their subjects; these entries are so a
      # TEST-ONLY edit still starts CI, per the two-halves law.
      - "tests/test_render_options_workspace_scope.py"
      - "tests/test_options_session_guards.py"
      - "tests/test_options_unit_seams.py"
      - "tests/test_options_coverage_object.py"
      - "tests/test_earnings_sweep_entrypoint.py"
      # ...and the two shared libs they guard, so a change to either re-runs them.
      - "lib/options_units.py"
      - "lib/options_coverage.py"
      - "lib/nyse_calendar.py"
      - "collectors/equity_earnings.py"
      - "scripts/audit_earnings_freshness.py"
      # Sparse session-worktree profile (job: workflow-yaml, policy R8).
      # scripts/**, config/** and .claude/** already reach the actor, config and
      # WorktreeCreate hook; this is the test half so a test-only edit still
      # starts the job that runs it.
      - "tests/test_sparse_worktree_profile.py"
      - "tests/test_worktree_sparse_add_lock_safety.py"
      # W0 PR3 synapse read-gate: any change to engine, scripts, collectors, the registry,
      # the gate script, the test, or the baseline must re-run the read-gate + Article-2 check.
      - "config/synapse.yml"
      - "scripts/check_synapse_reads.py"
      - "data/neuralweb/read_gate_baseline.json"
      - "tests/test_synapse_read_gate.py"
      - "tests/test_horizon_firewall.py"
      # LHB-W3 A2 pricing-power pilot: engine + builder + test must all trigger the gate.
      - "engine/pricing_power_monitor.py"
      - "scripts/research/build_pricing_power_monitor.py"
      - "tests/test_pricing_power_monitor.py"
      # neural-web program (W0): synapse.yml registry, gen_signal_bus_doc generator,
      # committed SIGNAL_BUS.md, and the three test files must all trigger the gate so a
      # config edit that produces a stale doc — or an envelope refactor that breaks
      # conformance — fails RED here before merging.  research/neuralweb/** is included
      # because the census_w0.json evidence file lives there and documents the program.
      - "scripts/gen_signal_bus_doc.py"
      - "docs/SIGNAL_BUS.md"
      - "engine/neuralweb/**"
      - "research/neuralweb/**"
      - "tests/test_signal_bus_doc.py"
      - "tests/test_synapse_registry.py"
      - "tests/test_neuralweb_envelope.py"
      # Chronicle W0 (agentic_media program): market-context timeline spine. engine/chronicle/**,
      # the builder script, the committed seed store, the admin inspector, and the test file must
      # all trigger the chronicle-suite job below — config/synapse.yml, config/dag.yml,
      # config/lobe_charters.yml, docs/SIGNAL_BUS.md, and engine/neuralweb/** (mastermind_context.py
      # registration) are already covered above.
      - "engine/chronicle/**"
      - "scripts/build_chronicle.py"
      - "data/chronicle/**"
      - "tests/test_chronicle.py"
      - "admin/chronicle.py"
      # RIC W4 EVW: event-window engine + forward ledger + ex-ante read (P3 RIC program).
      # engine/** already covers engine/event_window.py; test file + research doc +
      # config/lobe_charters.yml + config/metabolism_anomaly.yml need explicit paths.
      - "engine/event_window.py"
      - "tests/test_event_window.py"
      - "scripts/build_event_windows.py"
      - "research/DEALER_LOAD_EXTREME_LANE_II_PREREG.md"
      - "config/lobe_charters.yml"
      - "config/metabolism_anomaly.yml"
      # RLT-R1/R2: rebalance calendar organ + rebalance pulse detector.
      - "engine/rebalance_calendar.py"
      - "engine/rebalance_pulse.py"
      - "scripts/build_rebalance_pulse.py"
      - "tests/test_rebalance_calendar.py"
      - "tests/test_rebalance_pulse.py"
      # NW Macro Context Rail R5 (PR-A): five orphan NW test files now wired via neural-web-core job.
      - "tests/test_mastermind_context.py"
      - "tests/test_confluence.py"
      - "tests/test_spine_query.py"
      - "tests/test_ask_brain.py"
      - "tests/test_company_intelligence_neural_reader.py"
      - "tests/test_company_intelligence_event_workspace.py"
      - "tests/test_company_intelligence_qa_reconstruction.py"
      - "tests/test_company_intelligence_qa_exchange.py"
      - "tests/test_world_state.py"
      # PR-C health builder tests (weekend-aware staleness, cadence exclusion, staleness_from).
      - "tests/test_neuralweb_health.py"
      - "tests/test_neuralweb_daily_brief.py"
      # CMX W4: technician doctrine library tests (neural-web-core job).
      - "tests/test_brain_doctrine.py"
      # Analyst OS P0 (Mastermind superintelligence masterplan): analyst doctrine
      # library + gateway wiring + Live Market State Packet + market-intel tools.
      # engine/neuralweb/** covers the code; these cover test-only edits.
      - "tests/test_brain_analyst_doctrine.py"
      - "tests/test_brain_analyst_wiring.py"
      - "tests/test_market_packet.py"
      - "tests/test_brain_market_intel.py"
      - "tests/test_brain_seed_router.py"
      # Analyst OS W2: analogue engine + curve-detail tool + offline eval harness.
      - "tests/test_brain_analogues.py"
      - "tests/test_brain_curve.py"
      - "tests/test_response_eval.py"
      # Analyst OS W3: per-user memory tools + prefs plumbing.
      - "tests/test_brain_user_memory.py"
      - "tests/test_user_prefs.py"
      # Analyst OS W4: news-translate suites were wired into the marketing pack
      # (previously run by ZERO jobs); test-only edits must trigger a run too.
      - "tests/test_news_translate_render_gate.py"
      - "tests/test_news_translate_en.py"
      # Analyst OS W5 (latency wave): true token streaming, tool-loop economics,
      # instant-fact lane + latency instrumentation. Bench script has a parser test.
      - "tests/test_brain_streaming.py"
      - "tests/test_brain_tool_economics.py"
      - "tests/test_brain_instant_lane.py"
      - "scripts/brain_latency_bench.py"
      # The two cortex suites gate their own jobs ('cortex provider routing + tool loop'
      # and 'cortex ADB-W3 two-way read tools + parity') but were never listed here, so a
      # TEST-ONLY edit to either file triggered no ci.yml run at all and the suite it
      # changed never executed. engine/neuralweb/** covers edits to the code under test;
      # these two lines cover edits to the tests themselves.
      - "tests/test_cortex.py"
      - "tests/test_cortex_adb_w3.py"
      - "tests/test_admin_neural_web.py"
      - "tests/test_admin_metabolism.py"
      - "tests/test_admin_metabolism_history.py"
      - "admin/metabolism_panel.py"
      - "admin/metabolism_history.py"
      - "admin/github_api.py"
      # W-AI orchestrator face: run-log builder, v3 feedback bridge, admin page/proxy.
      # engine/neuralweb/** already triggers; the admin modules and test files must too.
      - "admin/orchestrator_chat.py"
      - "admin/mastermind_proxy.py"
      - "admin/neural_web.py"
      - "tests/test_orchestrator_log.py"
      - "tests/test_mastermind_feedback.py"
      - "tests/test_admin_orchestrator.py"
      - "tests/test_admin_prophet.py"
      - "tests/test_admin_trade_memory.py"
      # Macro Thesis Ledger — sibling of Trade Memory in the same admin area
      # (thesis-grain, committed JSONL, zero authority). Both halves listed: the
      # test file AND every module it guards, plus the committed seed ledger,
      # which is the suite's own input and can drift on its own.
      - "tests/test_macro_thesis.py"
      - "engine/macro_thesis.py"
      - "admin/macro_thesis.py"
      - "data/macro_thesis/ledger.jsonl"
      - "admin/prophet.py"
      - "admin/trade_memory.py"
      - "engine/neuralweb/trade_memory.py"
      - "engine/neuralweb/trade_memory_store.py"
      - "scripts/run_trade_memory.py"
      - "scripts/run_prophet_pick_autopsies.py"
      - "scripts/deploy/0008_trade_memory.sql"
      - "tests/test_trade_memory.py"
      # Mastermind AI response log (both surfaces → R2 → admin eval view).
      - "admin/mastermind_logs.py"
      - "tests/test_mastermind_response_log.py"
      - "tests/test_admin_mastermind_logs.py"
      - "tests/test_macro_snapshot.py"
      - "tests/test_macro_context_authority.py"
      - "tests/test_law_dates.py"
      # ADB-W1: NW context packet composer (brief_context.py + test)
      - "tests/test_brief_context.py"
      # ADB-W2: forward-calendar state block (forward_calendar_context.py + test)
      - "engine/forward_calendar_context.py"
      - "tests/test_forward_calendar.py"
      # Wave B2 ETM registry: builder, integrity gate, config, and both test files must
      # re-run together so a schema drift or authority violation fails RED before merging.
      # engine/neuralweb/** above already covers engine/neuralweb/entity_thesis_mechanism_registry.py.
      - "scripts/build_entity_thesis_mechanism_registry.py"
      - "scripts/check_entity_thesis_registry.py"
      - "config/entity_thesis_mechanism_registry.yml"
      - "tests/test_entity_thesis_registry.py"
      - "tests/test_check_entity_thesis_registry.py"
      - "data/neuralweb/entity_thesis_mechanism_registry.json"
      # NW Rails PR-7 R4 contract governance: schema drift check + test suite.
      - "scripts/check_contract_drift.py"
      - "scripts/export_signal_contracts.py"
      - "site/factordata/contracts/artifact_manifest.json"
      - "tests/test_contract_drift.py"
      # Portfolio-Aware Intelligence W0/W1: portfolio_ctx.v1 bake + brief composer +
      # theme_lanes side-write + tests (see the portfolio-ctx job). Whitelisted so the
      # bake's unit suite, the composer suite, and the theme_lanes.v1 side-write guard
      # (page-byte-identity invariant) all run on any change.
      - "scripts/build_portfolio_ctx.py"
      - "tests/test_build_portfolio_ctx.py"
      - "tests/test_portfolio_ctx_chains.py"   # TXI W4 per-ticker chains merge
      - "scripts/build_state_of_themes.py"
      - "tests/test_state_of_themes.py"
      - "engine/portfolio_brief.py"
      - "tests/test_portfolio_brief.py"
      # unrun-subsector-themes — subsector rotation/turn engines + the Theme
      # Tracker page suite (subsector CI-wiring audit 2026-08-05). The engine
      # subjects are covered by engine/**, and the page builder + its suite are
      # whitelisted just above; only the test halves below were missing, so a
      # test-only edit could not start this workflow.
      - "tests/test_subsector_turn.py"
      - "tests/test_subsector_rotation.py"
      - "tests/test_subsector_rotation_alerts.py"
      - "tests/test_subsector_scan.py"
      - "tests/test_subsector_confluence.py"
      - "tests/test_themes_heatmap.py"
      # Research Factory W1 (RF-11): authority guard, ledger schemas, state machine.
      - "scripts/research_factory*.py"
      - "scripts/*research_factory*.py"
      - "data/research_factory/**"
      - "tests/test_research_factory*.py"
      # W6A Market Memory candidate conformance: keep the exact adapter/schema,
      # hostile suite, and owner-contract amendment explicit despite broad globs.
      - "engine/research_factory/adapter_market_memory.py"
      - "engine/research_factory/schema.py"
      - "tests/test_research_factory_market_memory.py"
      - "research/RESEARCH_FACTORY_MASTERPLAN_BY_FABLE.md"
      - "research/KONSEKI_CLEAN_ROOM_MARKET_MEMORY_AND_COGNITIVE_ARCHITECTURE_FOR_FABLE_2026-08-08.md"
      - "config/synapse.yml"
      # CPI P2 authority guard: no money-path module may read cycle-pattern lake.
      - "scripts/check_cycle_pattern_authority.py"
      - "config/cycle_pattern/**"
      - "tests/test_check_cycle_pattern_authority.py"
      # CPI-H1 consumer-authority heal (2026-08-21): the registry-vocabulary half
      # of cycle-pattern-authority (scan_registry_vocabulary(), additive to the
      # literal-path scan above) reads these directly. Named explicitly here for
      # provenance; the job's own scope is auto-inferred (already correctly
      # includes these — verified via run_ci_pack.py --changed-from) and this job
      # deliberately stays unscoped in .github/ci/legacy-jobs.yml because its
      # literal-path scan is a full-tree guard (see that file's own house-law
      # comment on when NOT to declare a curated `paths:`).
      - "data/cycle_pattern/truths.jsonl"
      - "engine/cycle_pattern/consumer_authority.py"
      - "engine/cycle_pattern/truths.py"
      - "tests/test_cpi_h1_consumer_authority.py"
      # House-law registry meta-guard: any change to the registry, the meta-guard
      # script, the generated docs, or the test suite must re-prove the registry
      # is complete and consistent.
      - "config/house_law_checks.yml"
      - "scripts/check_house_law_registry.py"
      - "scripts/check_append_only_assertions.py"
      - "docs/HOUSE_LAW_CI_GUARD_SUITE.md"
      - "docs/**"
      - "tests/test_house_law_registry.py"
      - "tests/test_append_only_assertions.py"
      # validated-claims guard: wire as PR gate so unearned 'validated' claims
      # are caught before merge (not just in the nightly calibration run).
      - "scripts/check_validated_claims.py"
      - "data/regime/validated_claims_allowlist.json"
      # The third-party-sink exemption's own suite (#3770). Named so an edit to
      # the test ALONE still triggers the lane that runs it.
      - "tests/test_validated_claims_thirdparty.py"
      # The engine-source scan's own suite (#3765 → #3790 latency). engine/** already
      # triggers the lane for copy changes; name the suite so a test-only edit does too.
      - "tests/test_validated_claims_engine.py"
      # zh diction gate (ui.zh_filing_term): 申报 must never reach user-facing
      # Chinese copy — it reads as a TAX/CUSTOMS declaration, not a 披露. Name the
      # guard, its allowlist and its suite so an edit to ANY one of the three
      # re-proves the gate, not just an edit to the copy it polices.
      - "scripts/check_zh_filing_term.py"
      - "config/zh_filing_term_allowlist.json"
      - "tests/test_zh_filing_term.py"
      # The `surfaces`-enforcement + zh-token suite (2026-07-29 dislocation-panel audit):
      # a phrase justified for one page must not license another, and 经验证/经过验证 are
      # gated. Named so a test-only edit still triggers the lane that runs it.
      - "tests/test_validated_claims_surfaces.py"
      # The intl backdrop copy that tripped BC-2 on 2026-07-27. engine/** already covers
      # the module; list the suite so a test-only edit still triggers CI.
      - "tests/test_intl_recovery_quality.py"
      # Mastermind Feedback Contract PR-A (FB-R12/R13): privacy-boundary guard.
      # Any change to the guard script, its tests, committed governance/feedback
      # artifacts, .gitignore, or operator_grading must re-prove the rail holds.
      - "scripts/check_private_boundary.py"
      - "tests/test_check_private_boundary.py"
      - "data/governance/*.json"
      - "site/mastermind/*.json"
      - ".gitignore"
      - "engine/operator_grading.py"
      - "research/MASTERMIND_FEEDBACK_CONTRACT_PROGRAM.md"
      # Ruling Graph v1: canonical case-law store, conflict checker, tests, generated docs.
      - "config/ruling_graph.yml"
      - "scripts/build_ruling_graph.py"
      - "scripts/check_ruling_conflicts.py"
      - "tests/test_ruling_graph.py"
      - "docs/NEURAL_WEB_CASE_LAW.md"
      - "site/neuralwebdata/ruling_graph.json"
      - "data/neuralweb/ruling_conflicts_allowlist.json"
      # Metabolism Phase 0 (F1/F2/F3): grader manifest, self-mod fence, capability broker.
      # Any change to these files re-proves all three cage fences.
      # NOTE: the full IMMUTABLE set (R-AUT-4) must be covered here so the self-mod-fence
      # CI job actually fires for every protected path.  The four paths below were missing
      # from the original filter, creating a fail-open window for 40% of the immutable set
      # (.claude/hooks/**, config/metabolism_budget.yml, settings.json, and the masterplan).
      - ".claude/**"
      - "config/grader_manifest.yml"
      - "config/capability_manifest.yml"
      - "config/metabolism_budget.yml"
      - "scripts/check_grader_manifest.py"
      - "scripts/check_self_mod_fence.py"
      - "scripts/check_capability_redline.py"
      - "engine/neuralweb/capability_broker.py"
      - "tests/test_grader_manifest.py"
      - "tests/test_self_mod_fence.py"
      - "tests/test_capability_broker.py"
      - ".github/workflows/metabolism-heartbeat.yml"
      - "scripts/metabolism_guard.py"
      - "settings.json"
      - "research/AUTONOMIC_LOOP_MASTERPLAN_BY_FABLE.md"
      # V4-W3 additions: mission self-model, kill registry, compiler, drift check
      - "config/nw_mission.yml"
      - "config/compiled_kill_registry.yml"
      - "engine/metabolism/mission.py"
      - "scripts/compile_loop_blocklists.py"
      - "scripts/check_blocklist_drift.py"
      - "tests/test_metabolism_v4_w3_mission.py"
      - "research/DO_NOT_REBUILD.md"
      # Registry key/row-shape guard (wired 2026-08-06 — it had never run in CI;
      # DO_NOT_REBUILD.md and the compiler above already trigger this job, so this
      # entry only covers edits to the guard itself).
      - "tests/test_dnr_registry_keys.py"
      # FL-C live-flow ops (FC-R6/R7/R8 + Task 3): two-tier cadence, pinned publish,
      # daily summary writer, baselines extension.  Pure-code changes; no network.
      - "scripts/live_flow_poller.py"
      - "scripts/build_live_flow_baselines.py"
      - "tests/test_live_flow_tiering.py"
      # Causal Hypothesis Factory (CHF) + Signal Foundry brainstorm: ten test files wired
      # via the causal-factory job, plus every non-neuralweb module surface they import.
      # engine/neuralweb/** above already covers the causal_* engine modules and cortex.
      - "tests/test_causal_*.py"
      - "tests/test_sf_brainstorm.py"
      - "scripts/run_causal_brainstorm.py"
      - "scripts/causal_brainstorm_pack.py"
      - "scripts/causal_ingest_brainstorm.py"
      - "scripts/causal_factory_handoff.py"
      - "scripts/run_signal_foundry_brainstorm.py"
      - "admin/causal_lab.py"
      - "admin/paths.py"
      - "engine/signal_foundry/**"
      - "engine/trial_ledger.py"
      - "engine/validation.py"
      - "engine/codex_provider.py"
      - "engine/llm_auth.py"
      - "engine/neuralweb/key_pool.py"
      # Multi-key OAuth failover surface: the auth waterfall, the key pool,
      # the dispatcher, and the build lane must re-prove failover on change.
      - "tests/test_llm_auth.py"
      - "tests/test_codex_provider.py"
      - "tests/test_key_pool.py"
      - "tests/test_key_pool_economy.py"
      # The local rung of that same waterfall + the usage ledger it writes to.
      # Untriggered until 2026-08-07: a PR touching only lib/ai_costs.py or the
      # ollama provider scheduled no ci.yml at all, so the mis-attribution that
      # billed free local calls to the Claude subscription lane had no gate.
      - "engine/ollama_provider.py"
      - "tests/test_ollama_provider.py"
      - "lib/ai_costs.py"
      - "tests/test_ai_costs.py"
      - "scripts/metabolism_build.py"
      - "scripts/metabolism_dispatch.py"
      # V7 PR Audit gate (R-V7-1..6): audit engine, CLI, merge gate, workflow, test.
      - "engine/metabolism/audit.py"
      - "scripts/metabolism_audit.py"
      - "scripts/metabolism_merge.py"
      - "tests/test_metabolism_audit.py"
      - ".github/workflows/metabolism-audit.yml"
      # R-V7-7 audit-reject remediation loop: remediation helpers in build +
      # insight_bus new kind + budget key + test file.
      - "tests/test_metabolism_remediation.py"
      - "engine/metabolism/insight_bus.py"
      - "config/metabolism_budget.yml"
      # SA-W3 standout accountability metabolism wiring: charter upgrades, budget caps,
      # roles, rubric W-4, standout_auditor organ, insight_bus audit_due kind, shadow probe.
      - "tests/test_metabolism_sa_w3.py"
      - "tests/test_pick_autopsy_selection.py"
      - "engine/metabolism/standout_auditor.py"
      - "config/lobe_charters.yml"
      - "config/metabolism_roles.yml"
      - "config/adjudication_rubrics.yml"
      # R-V8-A immune lane: CI-red sentinel + lane-health sensors + claim dedup.
      - "engine/metabolism/immune.py"
      - "scripts/metabolism_immune.py"
      - "config/metabolism_immune.yml"
      - "config/metabolism_anomaly.yml"
      - "tests/test_metabolism_immune.py"
      - ".github/workflows/metabolism-immune.yml"
      # R-V8-10/11/12 meta-learning spine: durable outcome-prior ledger, de-rank,
      # recall parity.  dream.py + agenda.py + budget keys + test file.
      - "tests/test_metabolism_priors.py"
      - "engine/metabolism/dream.py"
      - "engine/metabolism/agenda.py"
      # R-V8-6..R-V8-9 reflex arcs: revert scanner, falsifier→ladder bridge,
      # tap escalation, construction parking, test file.
      - "tests/test_metabolism_reflex.py"
      - "scripts/metabolism_revert.py"
      - "scripts/metabolism_verify.py"
      - "scripts/metabolism_digest.py"
      - "engine/metabolism/verify.py"
      # M7C-R8 basket staleness warning: build_baskets.py is the producer; a change
      # to it alone (without touching ci.yml or engine/**) must still retrigger the
      # basket-staleness-warn test job — belt-and-suspenders, per reviewer nit.
      - "scripts/build_baskets.py"
      - "tests/test_basket_staleness_warn.py"
      # altdata hygiene sweep (PR-C): collector rot + signal-construction fixes.
      - "collectors/quiver.py"
      - "engine/altdata.py"
      - "engine/altdata_models.py"
      - "tests/test_altdata_hygiene.py"
      # FL-B cohort flow aggregation (flow-continuity-program):
      # engine module, desk builder, template, and test file.
      - "engine/flow_cohorts.py"
      - "scripts/build_flow_desk.py"
      - "templates/flow_desk.html.j2"
      - "tests/test_flow_cohorts.py"
      # Capital-flow velocity desk (site/flowdata/desk.json) + its staleness alarm.
      # Wired 2026-08-05: test_flow_velocity.py was matched by no pattern here and
      # named by no job, so both halves of the #3488 shape were open — the suite
      # never ran AND no edit to its subject could start ci.yml. Listed together
      # so the desk that froze for 12 days (A-share legs pinned at 2026-07-24
      # behind a dark upstream Tushare plane) can never lose its guard again.
      # lib/desk_guard.py is the shared staleness/thinning logic; china_tushare is
      # the collector whose utcnow()-stamped heartbeat masked the outage.
      - "engine/flow_velocity.py"
      - "scripts/build_flow_velocity.py"
      - "lib/desk_guard.py"
      - "collectors/china_tushare.py"
      - "tests/test_flow_velocity.py"
      - "tests/test_flow_desk_staleness.py"
      # Flow Observatory V2 (W1, PR #6780): the v2 contract package and its suite —
      # listed with their subjects so the guard and the guarded change trigger together.
      - "engine/flow_observatory/**"
      - "tests/test_flow_observatory_contract.py"
      # Flow Observatory V2 W2 (source quality + fail-visible publication): quality.py is
      # already covered by the engine/flow_observatory/** glob above; its test file is
      # listed explicitly per the W1 contract-delta lesson (wire the new file + its path
      # gate in the SAME commit).
      - "tests/test_flow_observatory_quality.py"
      # Flow Observatory V2 W3 (append-only observations ledger + revision-safe
      # corrections + replay): history.py is already covered by the
      # engine/flow_observatory/** glob above; its test file is listed explicitly per the
      # same W1 contract-delta lesson (wire the new file + its path gate in the SAME
      # commit — data/flow_observatory/observations.parquet itself is NOT a path gate
      # subject, per house law "data files are not CI path subjects").
      - "tests/test_flow_observatory_history.py"
      # Flow Observatory V2 W4 (official/curated lenses, coverage floor, overlap,
      # concentration): groups.py is already covered by the engine/flow_observatory/**
      # glob above; its test file and the new collectors/china_sectors.py collector
      # (the official lens' membership source) are listed explicitly — B1 repair, same
      # W1 contract-delta lesson (wire the new file + its path gate in the SAME commit;
      # the W4 landing PR omitted BOTH this entry and the ci/legacy-jobs.yml run: line,
      # leaving the whole suite dark). data/china_sectors/** is NOT a path-gate subject
      # (house law "data files are not CI path subjects") — code files only.
      - "collectors/china_sectors.py"
      - "tests/test_flow_observatory_groups.py"
      # Flow Observatory V2 W5 (preregistered descriptive method evaluation + threshold
      # calibration harness, research/flow_observatory/W5_PREREG.md): pure-read,
      # off-render-path research script + its unit-test guard, wired here in the SAME
      # commit per the standing W1 contract-delta lesson. Never imported by engine/ or
      # any builder — reports/flow_observatory_w5_methods.{md,json} are NOT path-gate
      # subjects (house law "data files are not CI path subjects" extends to committed
      # report artifacts derived from data).
      - "scripts/research_flow_observatory_methods.py"
      - "tests/test_flow_observatory_methods.py"
      # Flow Observatory V2 W6 (per-group history drawer, compare, prior episodes,
      # Terminal links, watch-store decision, research/flow_observatory/W6_SPEC.md):
      # workflow.py is already covered by the engine/flow_observatory/** glob above;
      # its test file is listed explicitly per the same W1 contract-delta lesson —
      # B1 repair (W6 review round): this entry was MISSING from the W6 landing PR
      # (same defect class as W2/W3/W4/W5's own comments above), wired here in the
      # SAME commit as the .github/ci/legacy-jobs.yml run: line.
      - "tests/test_flow_observatory_workflow.py"
      # TOP ANATOMY OOT receipt harness (contract-delta unrun-suite finding,
      # wired 2026-09-02): the suite shipped in #6723 with no run: owner and sat
      # dark on main. Now carried by falsifier-tripwires; the receipt entrypoint,
      # the Phase-0 harness it drives, and the frozen engine closure are listed
      # WITH the test file so a subject-only edit starts ci.yml and a test-only
      # re-pin still re-runs the owner that carries it (#3488 both halves).
      - "scripts/research_top_anatomy_oot_receipt.py"
      - "scripts/research_top_anatomy_phase0.py"
      - "engine/top_anatomy.py"
      - "tests/test_top_anatomy_oot_receipt.py"
      # FS-0 flow-signal ledger + outcome grader (FLOW_SIGNAL_ML_MASTERPLAN_BY_FABLE):
      # collector, grader engine, build script, detector config, and test file.
      - "collectors/flow_signals.py"
      - "engine/flow_signals_grade.py"
      - "scripts/build_flow_signals.py"
      - "config/flow_detector.yml"
      - "tests/test_flow_signals.py"
      # FS-1 historical flow cohorts (ops lane, off-render):
      # ops entrypoint, ladder config, sample fixture, and test file.
      - "scripts/ops_flow_cohorts.py"
      - "config/flow_recon_ladder.yml"
      - "tests/fixtures/_sample_V_2026-07-02.json"
      - "tests/test_fs1_flow_cohorts.py"
      # FS-2 atlas builder (ops lane, off-render):
      # atlas entrypoint, field guide, and test file.
      - "scripts/ops_flow_atlas.py"
      - "research/FLOW_SIGNAL_FIELD_GUIDE.md"
      - "tests/test_fs2_flow_atlas.py"
      # FS-4 flow-score trainer + nightly scorer + gate v2:
      # lib module, trainer ops script, config, scorer tests, and trainer tests.
      - "lib/flow_score.py"
      - "config/flow_score.yml"
      - "scripts/ops_train_flow_score.py"
      - "tests/test_fs4_flow_scorer.py"
      - "tests/test_fs4_flow_trainer.py"
      # FL-A EOD flow backfill (FC-R4/R11): backfill script, build_options_flow staleness
      # warning, and the test suite that pins idempotence + schema-variant handling.
      - "scripts/backfill_options_flow.py"
      - "scripts/build_options_flow.py"
      - "tests/test_backfill_options_flow.py"
      # LRV-W1 leader lifecycle + builder (LRV-R1/R2/R3/R4/R5)
      - "engine/leader_lifecycle.py"
      - "scripts/build_leader_radar.py"
      - "tests/test_leader_lifecycle.py"
      - "tests/test_build_leader_radar.py"
      # OEU M-PRO display-tier options context: the shared loader/vocabulary, the two
      # Prophet-side consumers, and the fence suite. A change to any of them must
      # retrigger the fence tests even when nothing else in this filter moved.
      - "lib/options_context.py"
      - "engine/prophet_bridge.py"
      - "scripts/build_prophet.py"
      - "tests/test_prophet_options_context.py"
      # PROPHET LIVE P0 (research/PROPHET_LIVE_INTRADAY_SIGNALS_MASTERPLAN_BY_FABLE.md).
      # G0.1 is a PARITY gate: the published interval must reproduce signal_gate's
      # verdict, so an edit to the gate, the cascade, the arming probe, the intraday
      # state machine OR the workflow that runs it has to retrigger the parity suite.
      # Without these lines a PR touching only these files matches nothing in this
      # filter, ci.yml never starts, and the guard cannot fire on the change it guards.
      - "engine/prophet_live/**"
      - "engine/signal_gate.py"
      - "engine/confluence_tiers.py"
      - "scripts/build_prophet_live_pack.py"
      # Unrun backfill 2026-08-03: the P1 user-facing surface suite's own half.
      - "tests/test_prophet_live_surface.py"
      - "scripts/prophet_live_evaluator.py"
      - "scripts/reconcile_prophet_live.py"
      - ".github/workflows/prophet-live.yml"
      - "tests/test_prophet_live_pack.py"
      - "tests/test_prophet_live_evaluator.py"
      - "tests/test_prophet_live_reconcile.py"
      # SILENT-FREEZE guard (2026-08 incident). The lane froze for 27 days because
      # the producer, the status surface and the external dead-man each reported
      # success independently. The guard therefore spans all three owners, so an
      # edit to ANY of them must retrigger it — a guard that does not run on the
      # file it guards fires on somebody else's PR instead (the #3488 failure mode).
      - "tests/test_prophet_live_silent_freeze.py"
      - "scripts/check_vps_live_health.py"
      - "app/main.py"
      # PIT RECOVERY of the spool the 2026-08 outage never published. These rows
      # enter forward.parquet, so the guard must retrigger on the producer, the
      # pending reader and the ledger writer alike.
      - "scripts/prophet_live_journal_recovery.py"
      - "tests/test_prophet_live_journal_recovery.py"
      - "tests/fixtures/prophet_live/import_probe.py"
      # PROPHET LIVE VPS lane (§4.2a/§4.4a). The unit files, the reconciler that
      # installs them and the provisioner that arms them are the subjects of
      # test_prophet_live_vps_lane.py — a PR touching only a unit file must retrigger
      # the guard that reads it, or the guard fires on everyone else's PR instead
      # (the #3488 failure mode).
      - "app/deploy/macro-live-prophet.service"
      - "app/deploy/macro-live-prophet.timer"
      - "app/deploy/live-setup.sh"
      - "tests/test_prophet_live_vps_lane.py"
      # CLOSE-PASS MIRROR lane (W-L1a, #5154). Same #3488 shape as the prophet
      # units above: test_close_pass_lane.py parses these two unit files, so a
      # PR touching only a unit file must retrigger the suite that reads it.
      - "app/deploy/macro-live-closepass.service"
      - "app/deploy/macro-live-closepass.timer"
      # CLOSE-PASS HOST CLOCK (Breathing PR-B, 2026-08-15). The evening pass's
      # PRIMARY clock is now launchd on the Mac Studio, not the close-pass.yml
      # cron — measured 27 min of schedule drift plus a 95 min queue wait on
      # 2026-08-14, publishing ~19:20 ET against a 16:15 ET target. Neither the
      # runner nor its host wiring lives under a directory any other entry
      # covers (ops/launchd/** is named by nothing here), so without these lines
      # a PR editing ONLY the clock would run no guard at all — the #3488 shape
      # again, on the lane that decides when the board ships. Both halves, as
      # always: the subjects AND their suites.
      - "scripts/close_pass_host_runner.py"
      - "scripts/install_closepass_launchd.sh"
      - "ops/launchd/com.macro.closepass.plist"
      - "tests/test_close_pass_host_runner.py"
      - "tests/test_close_pass_lane.py"
      # altdata-W1: Finnhub transcript catalog + StockTwits sentiment collectors.
      - "collectors/finnhub_transcripts.py"
      - "collectors/stocktwits.py"
      - "tests/test_finnhub_transcripts.py"
      - "tests/test_stocktwits.py"
      # china_native_data W2: collector fixture suites (test-only edits must retrigger)
      - "tests/test_china_funding_collector.py"
      - "tests/test_china_cb_collector.py"
      - "tests/test_china_fund_issuance_collector.py"
      - "tests/test_china_flows_etf_universe.py"
      # IRD-W1 International Risk Desk substrate: IMF WEO adapter, CB calendar,
      # FRED alias-collision lint, and liquidity_plumbing swap-lines Phase-5.
      - "collectors/imf_weo.py"
      - "data/intl_risk/cb_calendar.yml"
      - "config/synapse.yml"
      - "engine/neuralweb/liquidity_plumbing.py"
      - "tests/test_imf_weo.py"
      - "tests/test_fred_alias_collision.py"
      - "tests/test_cb_calendar.py"
      - "tests/test_liquidity_plumbing.py"
      # RLT-R3 broad-market index ETF creation/redemption proxy (belt-and-suspenders):
      # a future PR touching engine/etf_flows.py or collectors/sponsors.py alone
      # (without ci.yml) must still retrigger the broad-etf-flows job.
      - "engine/etf_flows.py"
      - "collectors/sponsors.py"
      - "tests/test_broad_etf_flows.py"
      # T0 beta turn indicator (display-only): engine module + build wiring + test suite.
      - "engine/t0_indicator.py"
      - "tests/test_t0_indicator.py"
      # IFT-V2 stance engine (dealer_context + stance in engine/intraday_flow.py):
      # engine file already covered by engine/**, but test files need explicit paths
      # so a test-only change still re-runs the guard.
      - "engine/intraday_flow.py"
      - "scripts/build_intraday_flow.py"
      - "tests/test_intraday_flow.py"
      - "tests/test_intraday_flow_stance.py"
      # SA-W1 standout accountability: attribution organ + fitness card.
      # Any change to the engine module or tests must re-prove the attribution
      # taxonomy, effective-n math, keep-first sidecar, and never-raise contract.
      - "engine/standout_audit.py"
      - "tests/test_standout_audit.py"
      # Prophet W1 track record page: cohort rollup, accruing/data-gap paths,
      # never-raise contract (SA-R16).
      - "scripts/build_track_record_page.py"
      - "tests/test_track_record_page.py"
      # altdata-brain de-escalation law + false Wilson de-dup comment fix (PR-B):
      # any change to the brain module or either test file must re-prove the law holds.
      - "engine/altdata_brain.py"
      - "tests/test_w7a_tenants.py"
      - "tests/test_altdata_brain_reconcile.py"
      # NW lobe unfreeze (fix/nw-unfreeze-dtcontra-nasdaq): dt_contra_state list-root
      # crash fix + nasdaq archetype_groups preserve-through-rebuild fix.
      - "scripts/build_dt_contra_state.py"
      - "tests/test_dt_contra_state.py"
      - "scripts/build_subsector_membership.py"
      - "tests/test_build_subsector_membership.py"
      - "data/baskets_nasdaq/membership.json"
      # LHB-R6 quarterly-store enrichment: receivables, inventory, payables,
      # contract_liabilities added to statements_quarterly.parquet (2026-07-12).
      # collectors/** already covers edgar_facts.py; backfill script and test
      # need explicit paths so a test-only or script-only change still re-runs.
      - "scripts/backfill_edgar_quarterly.py"
      - "tests/test_edgar_quarterly_tags.py"
      # ITR-W1: per-market turn state machine + fast rotation re-rank
      - "engine/intl_market_state.py"
      - "engine/intl_rotation.py"
      - "tests/test_intl_market_state.py"
      - "tests/test_intl_rotation.py"
      # IRD-W2 engines: EM stress, contagion/DY spillover, CB desk
      - "engine/intl_risk.py"
      - "engine/contagion.py"
      - "engine/cb_desk.py"
      - "tests/test_intl_risk.py"
      - "tests/test_contagion.py"
      - "tests/test_cb_desk.py"
      # IRD-W2 integrations: velocity, inversion, smile decomp, build wiring, world_state
      - "engine/ird_velocity.py"
      - "engine/intl_bonds.py"
      - "engine/intl_rates.py"
      - "engine/forex_dollar.py"
      - "engine/neuralweb/world_state.py"
      - "scripts/build_intl.py"
      - "scripts/build_bonds.py"
      - "tests/test_ird_velocity.py"
      - "tests/test_ird_wiring.py"
      # IRD-W4 weather-station intl_risk card (builder lives in build_macro_context)
      - "scripts/build_macro_context.py"
      - "templates/macro_context.html.j2"
      # CRX W1 — Codex Research Engine: runner/budget core + both lane scripts + loop driver
      # + key-pool 7-key extension + admin panel + workflow wiring.  Any change to these
      # surfaces re-proves the CRX contract tests before merging.
      - "engine/codex_lane/**"
      - "scripts/codex_case_lane.py"
      - "scripts/codex_signal_lane.py"
      - "scripts/codex_research_loop.py"
      - "config/codex_lane.yml"
      - "admin/codex_panel.py"
      - "tests/test_key_pool_seven.py"
      - "tests/test_codex_runner_budget.py"
      - "tests/test_codex_lanes.py"
      - "tests/test_admin_codex.py"
      - ".github/workflows/codex-research.yml"
      # V11 Metabolism Throttle UX: budget gate engine, dispatch balancer,
      # workflow machinery, admin UX panel.  Any change to these surfaces
      # re-proves the auto-run contract tests before merging.
      - "engine/metabolism/budget_gate.py"
      - "tests/test_metabolism_budget_gate.py"
      - "tests/test_metabolism_rununtil.py"
      - "tests/test_admin_metabolism_ux.py"
      - ".github/workflows/metabolism-cycle.yml"
      - ".github/workflows/key-pool-probe.yml"
      # V12 Surface Curator + Metered Loop: site-surface census, surface law
      # screens, operator pins, daily pace rung, 429-derived budget readings.
      - "engine/metabolism/surface_map.py"
      - "scripts/build_surface_map.py"
      - "config/metabolism_surface_rules.yml"
      - "tests/test_metabolism_v12_surface.py"
      # achievements observability: per-cycle rollup from journals + verify + PR list.
      - "tests/test_metabolism_achievements.py"
      - ".github/workflows/metabolism-dream.yml"
      # admin live-runs feed: server-side snapshot for the live-runs panel.
      - "admin/live_runs.py"
      - "tests/test_admin_live_runs.py"
      # CBF-W1 regime census: classifier engine + test suite.
      # Any change to the flow regime engine or test file re-proves the contract.
      - "engine/flow_regime.py"
      - "tests/test_flow_regime.py"
      # CCW-W1 corporate credit watch data spine: bond holdings collector, issuer
      # registry, FRED config adds. Any change to these files re-proves the contracts.
      - "collectors/corp_bond_holdings.py"
      - "data/corp_bonds/issuer_themes.json"
      - "tests/test_corp_bond_holdings.py"
      - "tests/test_fred_series_alias_conflicts.py"
      - "tests/test_fred_baml_archive.py"
      - "scripts/snapshot_fred_archive.py"
      # CCW-W2 g-spread engine: YTM solver, CMT interpolation, accrued interest,
      # composition guard, dispersion, maturity wall, validation gate.
      - "engine/corp_credit.py"
      - "tests/test_corp_credit.py"
      - "scripts/fetch_ishares_validation_sample.py"
      # Support + Email estate (SEE W1): the mail transport/ledger, the public ticket
      # intake, the operator console, and the schema they all address. The #3509 lesson
      # is why the SOURCE files are listed and not just the tests — a job whose paths
      # cannot be reached by the change that breaks it is a guard that cannot fail.
      - "app/mailer.py"
      - "app/support.py"
      - "admin/support_tickets.py"
      - "scripts/deploy/0007_support_email.sql"
      # The suite's HTTP-level auth/CSRF test drives the real admin.server Handler and the
      # console tab lives in app.js — routes whose own files could not trigger this job.
      - "admin/server.py"
      - "admin/static/app.js"
      # The public intake's REAL body cap and its unspoofable rate-limit key are Caddy
      # config, not app code; a change there changes what the suite's assumptions rest on.
      - "app/deploy/Caddyfile"
      - "tests/test_mailer.py"
      - "tests/test_support_api.py"
      - "tests/test_admin_support_tickets.py"
      # Support + Email estate (SEE W3): the Stripe-webhook-driven billing emails, the
      # trial-ending lifecycle sweeper, and POST /api/account/prefs. Same #3509 lesson —
      # the SOURCES are listed, not only the tests. config/plans.yml is here because the
      # money, the plan names AND the tier ordering in every one of those emails are read
      # from it, so a catalog edit is exactly the change that can break the suite.
      - "app/billing_emails.py"
      - "app/account_prefs.py"
      - "config/plans.yml"
      - "tests/test_billing_emails.py"
      - "tests/test_account_prefs.py"
      - "tests/test_billing_webhook.py"
      # SEE W2: the public /support.html surface, its builder, and the pinned email base.
      # templates/** and site/** already reach this workflow, but the #3509 lesson is that
      # a guard has to be reachable from the file that BREAKS it by name, not by a broad
      # wildcard somebody may narrow later.
      - "templates/support.html.j2"
      - "site/support.html"
      - "tests/test_support_page.py"
      - "tests/test_email_templates.py"
      # The page's JS run for real under node (the W2 review's B1/B2/B3 are behaviours a
      # text assertion cannot reach), plus the DOM shim it runs against.
      - "tests/test_support_page_js.py"
      - "tests/fixtures/support_dom_shim.js"
      # The design pin the page and the email base are ported FROM: a change to the pin
      # without a matching change to the implementation is exactly the drift to catch.
      - "mockups/support_email/**"
      # X-growth desk suites (unrun backfill 2026-08-03): ad plane O, persona
      # model, reply arming, and the two committed ad-campaign manifests. The
      # mockups trees are the campaigns' source of truth and matched no glob;
      # engine/marketing/** and the scripts entries cover the rest.
      - "mockups/x-ads-2026-07/**"
      - "mockups/x-mobile-ads-2026-07/**"
      - "tests/test_marketing_ad_plane_o.py"
      - "tests/test_marketing_persona_model.py"
      - "tests/test_marketing_reply_arming.py"
      - "tests/test_x_ad_campaign_assets.py"
      - "tests/test_x_mobile_campaign_assets.py"
      # Support + Email estate (SEE W4): the marketing foundation. Same #3509 lesson —
      # every SOURCE by name, not only the tests. app/email_segments.py is listed first
      # because it is the shared one: BOTH the admin console's SQL and the sweeper's
      # Python read their membership rules from it, so an edit there can break the
      # compliance gate on either plane, and neither plane's own file is touched.
      - "app/email_segments.py"
      - "app/unsubscribe.py"
      - "app/marketing_emails.py"
      - "admin/email_center.py"
      - "templates/unsubscribe.html.j2"
      - "site/unsubscribe.html"
      - "tests/test_email_segments.py"
      - "tests/test_unsubscribe_api.py"
      - "tests/test_marketing_emails.py"
      - "tests/test_admin_email_center.py"
      - "tests/test_unsubscribe_page.py"
      # The secret-delivery lane is an INPUT to app/marketing_emails.py, not merely ops:
      # MAIL_WELCOME_AFTER is an ISO datetime and the workflow's `_add` helper strips
      # spaces, so a legal `2026-07-26 12:00:00` arrived unparseable — which the module
      # treats exactly like unset, i.e. the welcome leg silently sends nothing.
      # test_marketing_emails.py asserts the `_addv` call, so the edit that breaks it has
      # to reach the guard by name (#3509).
      - ".github/workflows/deploy-api-secrets.yml"
      # W2 entry points (funnel, not nav) + the page's writer.
      - "templates/index.html"
      - "site/index.html"
      - "templates/plans.html.j2"
      - "lib/pages.py"
      - "lib/seo.py"
      # Site Access Gate: gate logic engine, admin backend, test suites.
      # Any change to app/gate.py, admin/site_gate.py, or either test file must
      # re-prove the fail-open + self-lockout + IP/CIDR + country contracts.
      - "app/gate.py"
      - "admin/site_gate.py"
      - "tests/test_site_gate.py"
      - "tests/test_admin_site_gate.py"
      # Pick Lab liquidity gate: candidates module + liquidity regression test.
      # Any change to candidates.py or the liquidity test file re-proves the
      # NaN.ge() regression fix (null-adv rows must pass with liq_unknown=True).
      - "engine/pick_lab/candidates.py"
      - "tests/test_pick_lab_liquidity.py"
      # RIC W3 OPEXRISK: opex_risk engine + event_calendar enrichment + tests.
      # Any change to these surfaces re-proves the n_hot/n_applicable math,
      # availability-normalization, keep-FIRST idempotency, and lane gate.
      - "engine/opex_risk.py"
      - "engine/event_calendar.py"
      - "scripts/build_vol_regime.py"
      - "tests/test_opex_risk.py"
      - "data/opex_windows/.gitkeep"
      # D02 W1 live social publisher: backend-agnostic publisher + Buffer adapter,
      # the live runner, and the outbox status machine it drives (posting state).
      - "engine/marketing/social_publisher.py"
      - "engine/marketing/outbox.py"
      - "scripts/marketing_publisher.py"
      - "tests/test_marketing_social_publisher.py"
      - "tests/test_marketing_outbox.py"
      # W1 wiring layer: auto-approve gate + writeless dry-run report + admin panel.
      - "tests/test_marketing_publisher_autoapprove.py"
      # Weekend levels lane (2026-07-27): previously unrun; carries the
      # cited-level chart contract + floor-copy language bar.
      - "tests/test_marketing_weekend_levels.py"
      # Post-time tape gate: never post yesterday's read against today's tape.
      - "tests/test_marketing_live_verify.py"
      # Publish-time mover/theme generation: honest live tape posts, scoped
      # auto-approve. engine/marketing/publish_time_content.py is covered by the
      # engine/marketing/** entry below.
      - "tests/test_marketing_publish_time_content.py"
      # The daily-read half of the same lane. Was named by no job at all until
      # XG-W1 wired it into marketing-social-publisher; it guards which accounts
      # the per-call read lane may draw, which is exactly what XG-W1 narrowed.
      - "tests/test_marketing_publish_time_read.py"
      # Publish-time clock + lead-fact fan-out gates (2026-08-11): unrun until
      # voice v5 rotted its fixtures unseen; runs in marketing-social-publisher.
      - "tests/test_marketing_market_clock.py"
      # Marketing lobe engine unit suite (see the marketing-engine job). Whitelisted
      # 2026-07-23 after it rotted red on main: opportunities.jsonl is a forward
      # ledger the nightly radar fills with live radar-* rows, but the seed round-trip
      # test asserted shadow-only. Triggers on the engine tree + the committed ledgers.
      # test_marketing_sentinel co-whitelisted 2026-07-23: it too rotted red on main
      # (fixture aged dates with local date.today() while receipts_context ages
      # against the UTC date — red every evening 5pm–midnight PDT).
      - "engine/marketing/**"
      - "tests/test_marketing_engine.py"
      - "tests/test_marketing_sentinel.py"
      # Content Studio W1 + E-waves (2026-07-29): writer/critic/selection +
      # press-wire bridge + intel loop + reply voice + filing lanes. Scripts
      # and workflows listed so a script-only edit still reaches the lane.
      - "tests/test_marketing_copy_v2.py"
      - "tests/test_marketing_voice_laws.py"
      - "tests/test_marketing_publisher_names.py"
      - "tests/test_marketing_media_backfill.py"
      - ".github/workflows/marketing-earnings-wire.yml"
      - "tests/test_chart_facts.py"
      - "tests/test_confluence_source.py"
      - "tests/test_confluence_honesty.py"
      - "tests/test_tech_confluence.py"
      - "tests/test_chart_since_setup.py"
      - "tests/test_marketing_selection.py"
      - "tests/test_marketing_press_wire.py"
      - "tests/test_marketing_x_intel.py"
      - "tests/test_marketing_reply_voice.py"
      - "tests/test_marketing_filing_lanes.py"
      - "tests/test_copywriter.py"
      - "tests/test_marketing_copy_review.py"
      - "scripts/marketing_copy_dryrun.py"
      - "scripts/marketing_press_wire.py"
      - "scripts/x_intel_harvest.py"

      # Unrun-baseline batch join 2026-08-11: 33 marketing suites that sat in
      # config/unrun_test_baseline.json named by NO run step, wired into their
      # owning jobs in the same PR (30 -> marketing-engine, 3 -> marketing-social-
      # publisher). test_marketing_metrics_poll_quota.py was part of the triage but
      # is yielded to armed PR #5368, which wires it into marketing-social-publisher
      # and adds its own paths entry.
      # Listed by name so a TEST-ONLY edit still reaches its own guard —
      # the subject code is already covered by engine/marketing/** and
      # scripts/marketing_publisher.py above. test_marketing_copy_review.py is part
      # of the batch but was already listed in the Content Studio block above, so it
      # is not repeated here.
      # -> marketing-engine
      - "tests/test_marketing_ad_central.py"
      - "tests/test_marketing_allies.py"
      - "tests/test_marketing_anchor_law.py"
      - "tests/test_marketing_attribution.py"
      - "tests/test_marketing_breaking_card.py"
      - "tests/test_marketing_card_ticker_agreement.py"
      - "tests/test_marketing_cold_read.py"
      - "tests/test_marketing_earnings_call_lane.py"
      - "tests/test_marketing_emitted_kind_ownership.py"
      - "tests/test_marketing_lane_rerun.py"
      - "tests/test_marketing_links.py"
      - "tests/test_marketing_macro_print_tier.py"
      - "tests/test_marketing_mover_stance.py"
      - "tests/test_marketing_next_review.py"
      - "tests/test_marketing_rejections.py"
      - "tests/test_marketing_relay_hygiene.py"
      - "tests/test_marketing_reply_elements.py"
      - "tests/test_marketing_reply_pacing.py"
      - "tests/test_marketing_reply_register.py"
      - "tests/test_marketing_reply_shape.py"
      - "tests/test_marketing_reply_targets.py"
      - "tests/test_marketing_rewrite.py"
      - "tests/test_marketing_source_authority.py"
      - "tests/test_marketing_tape_reaction_basket.py"
      - "tests/test_marketing_telemetry.py"
      - "tests/test_marketing_wire_dehandle.py"
      - "tests/test_marketing_wire_single_statement.py"
      - "tests/test_marketing_wire_story.py"
      - "tests/test_marketing_wire_volume.py"
      # -> marketing-social-publisher (these three import the publisher itself)
      - "tests/test_marketing_approval_desk.py"
      - "tests/test_marketing_card_earns_pixels.py"
      - "tests/test_marketing_wire_expiry.py"

      # D08 age ramp + chart-CTA + send-time jitter (2026-07-27). Listed by name
      # so a TEST-ONLY edit reaches its own guard: the subject code is already
      # covered by engine/marketing/** + scripts/marketing_publisher.py above,
      # but this suite also pins live config/marketing.yml values (ramp armed,
      # CTA off, jitter 7) that nothing else asserts.
      - "tests/test_marketing_ramp_cta_jitter.py"
      # Event-post language contract (2026-07-27 incident): driver-label
      # translation coverage + template coherence. The subject module halves
      # (engine/marketing/market_facts.py, copywriter.py, content_studio.py)
      # are already matched by engine/marketing/** above; market_drivers.py is
      # listed so adding a driver without a translation trips the census.
      - "tests/test_marketing_event_language.py"
      - "engine/market_drivers.py"
      # tests/test_market_facts.py joins 2026-08-12 (W2D): another
      # unrun-suite-rot grandfather closed on contact, now run in
      # marketing-engine. The subject code is already covered by
      # engine/marketing/** above; listed by name so a TEST-ONLY edit (a new
      # market_facts producer pin, say) still reaches its own guard.
      - "tests/test_market_facts.py"
      - "data/marketing/opportunities.jsonl"
      - "data/marketing/claims.jsonl"
      # content-studio + admin marketing-panel suites — co-run in the
      # marketing-engine job (added 2026-07-23; never whitelisted anywhere →
      # test-whitelist-rot). Trigger on the admin panel + config that drive them.
      - "tests/test_marketing_content.py"
      - "tests/test_admin_marketing.py"
      - "admin/marketing.py"
      - "config/marketing.yml"
      # Marketing floor / model desk / X lanes (2026-07-29): the operator's view
      # inside the factory — per-station yield with every loss named, model
      # routing + the OAuth key-pool balancer, and the E-wave lanes' live/dark
      # state. Listed so a PR touching only the panel or only its suite still
      # reaches the marketing-engine lane.
      - "tests/test_admin_marketing_floor.py"
      - "admin/marketing_floor.py"
      # Content Studio W1 selection layer (LLM-first masterplan §0, 2026-07-29):
      # cross-day cooldowns, the fact-reuse budget, the degenerate-stat gate,
      # the shape mixer, the emit-time no-fallback refusal, stale-queued expiry
      # and the kind-scoped auto-approve. engine/marketing/** and
      # config/marketing.yml above already reach its subjects; the suite is
      # listed so a PR touching ONLY the tests still reaches the lane, and the
      # dry-run workflow is listed because the suite's live-config assertions
      # are the only thing pinning that lane's arming keys.
      - "tests/test_marketing_selection.py"
      # Batch auditor (operator 2026-07-30): the only gate that reads a whole
      # day at once, so it is the only one that can see cross-post monotony.
      - "tests/test_marketing_copy_auditor.py"
      - "engine/marketing/copy_auditor.py"
      # Bare-cashtag law (operator 2026-07-30): a post that names tickers
      # ships a picture. The kind-keyed media gate structurally could not
      # see theme_list/mover, which is what was auto-posting naked.
      - "tests/test_marketing_bare_cashtag.py"
      - "tests/test_marketing_voice_laws.py"
      - "tests/test_marketing_publisher_names.py"
      - "tests/test_marketing_media_backfill.py"
      - ".github/workflows/marketing-earnings-wire.yml"
      - "tests/test_chart_facts.py"
      - "tests/test_confluence_source.py"
      - "tests/test_confluence_honesty.py"
      - "tests/test_tech_confluence.py"
      - "tests/test_chart_since_setup.py"
      - ".github/workflows/marketing-copy-dryrun.yml"
      # chart coverage (#3895, 2026-07-28): every ticker-bearing post carries an
      # illustration; the live gate picks the card VARIANT rather than vetoing it;
      # chart ids stay per-account; the ramp media cap never sits below the post
      # cap. Runs in BOTH marketing-engine (9 pandas-free tests) and
      # chart-render-data (the 3 that need real bars). engine/marketing/** and
      # config/marketing.yml already reach its subjects — this entry is so a
      # test-only edit reaches its own guard.
      - "tests/test_marketing_chart_coverage.py"
      # Publish-lane throughput + media recovery (2026-07-28). Its subjects are
      # scripts/marketing_publisher.py, scripts/marketing_media_backfill.py, the
      # two marketing workflow files and .gitattributes — none of which the
      # engine/marketing/** globs above reach, so without these entries a change
      # to any of them (or to the suite itself) would never meet its own guard.
      - "tests/test_marketing_forward_booking.py"
      - "scripts/marketing_media_backfill.py"
      - ".github/workflows/marketing-publish.yml"
      - ".github/workflows/marketing-media-backfill.yml"
      - ".gitattributes"
      # Kill-switch recall (2026-07-28) — the other half of forward booking
      # above. Same reasoning for listing the runner by name: scripts/ is not
      # reached by the engine/marketing/** globs, so without this entry an edit
      # to the recall runner would never meet its own guard. The suite also
      # asserts on .github/workflows/marketing-publish.yml (already listed
      # above) and admin/marketing.py (listed further down).
      - "scripts/marketing_recall.py"
      - "tests/test_marketing_recall.py"
      # Persona Network W1 (docket D13): the persona spec layer + its roster panel,
      # co-run in the marketing-engine job (same stdlib+pyyaml closure). The SPECS
      # are listed, not only the code — a PR that edits ONLY config/personas/*.yml
      # is exactly the change `python -m engine.marketing.personas --check` exists
      # to catch, and without this entry it would never reach the guard.
      # engine/marketing/** already covers engine/marketing/personas.py;
      # admin/server.py + admin/static/app.js are already listed above (SEE W1).
      - "config/personas/**"
      - "admin/personas.py"
      - "tests/test_marketing_personas.py"
      # X Growth W1 (XG-W1): the employee expression dial + codex quirk pass.
      # engine/marketing/expression_dial.py is already matched by
      # engine/marketing/** above; the suite is listed so a PR that edits ONLY a
      # codex (config/personas/**, already listed) or only this test still
      # reaches the lane that runs it.
      - "tests/test_marketing_expression_dial.py"
      # X Growth W2 (XG-W2): the per-account cadence resolver, the persona
      # `session` territory clock, the one-owner story lock, the cross-account
      # near-dup radar, and the outbox KINDS hardening. Its subject modules are
      # all matched by engine/marketing/** + scripts/marketing_publisher.py +
      # config/marketing.yml + config/personas/** above; the suite is listed by
      # name so a TEST-ONLY edit still reaches the lane that runs it.
      - "tests/test_marketing_cadence_spine.py"
      # The cadence spec <-> D08 ramp coherence guard (2026-07-28). The two are
      # ANDed at post time, so a persona `cadence:` block below its ramp tier is a
      # silent network-wide throttle. Its subjects are config/personas/** and
      # config/marketing.yml (both listed above) plus .github/workflows/
      # marketing-publish.yml, which is NOT otherwise matched by anything in this
      # list — the suite derives the sweep grid from that cron, so a cron edit has
      # to reach the lane too. Both are named so a workflow-only or test-only edit
      # still runs the guard.
      - "tests/test_marketing_cadence_ramp_coherence.py"
      - ".github/workflows/marketing-publish.yml"
      # X Growth W4a/W4b/W4c (2026-08-02): the nightly ladder collapses to ONE
      # day, the per-account rung count is sized to that desk's D08 ramp cap, a
      # format the tier forbids gets zero allocation, and the cooldown drop
      # counter reaches the plan artifact. Subject module is
      # engine/marketing/content_studio.py (matched by engine/marketing/**
      # above); the suite is named so a TEST-ONLY edit still reaches the lane
      # that runs it — and so it is not a guard that never executes.
      - "tests/test_marketing_ladder_collapse.py"
      # The other four W4 suites, added 2026-08-02 for the same reason and in the
      # same breath: W4d press-wire headroom (press_lane/hot_tape/wire_routing),
      # W4e copy-lane yield (copywriter), W4f the mastermind_news arming
      # (config/marketing.yml), W4g the breaking-card geometry (chart_render/
      # breaking_summary). All four shipped their guards WITHOUT a lane entry —
      # they ran green on the author's laptop and would never have executed here.
      - "tests/test_marketing_wire_headroom.py"
      - "tests/test_marketing_copy_yield.py"
      - "tests/test_marketing_news_arming.py"
      - "tests/test_marketing_breaking_card_geometry.py"
      # The D08 age-ramp relaxation (operator 2026-08-03): the two tier
      # boundaries became config keys (`sentinel.ramp.weeks_1_2_days` /
      # `weeks_3_4_days`) and `graduate_after_days` stopped being clamped up to a
      # hardcoded 28. Subject modules are engine/marketing/sentinel.py +
      # config/marketing.yml (both matched above); the suite is named so a
      # TEST-ONLY edit still reaches the lane that runs it.
      - "tests/test_marketing_ramp_speedup.py"
      # X Growth W3 (XG-W3): the franchise register + slot scheduler, the
      # four-lane desk feed, the persona memory stores (promises/phrases/
      # relations) and the Gift-Grip-Proof value gate. Its subject modules are
      # all matched by engine/marketing/** + config/marketing.yml +
      # config/personas/** above; the suite is listed by name so a TEST-ONLY
      # edit still reaches the lane that runs it.
      - "tests/test_marketing_desk_feeds.py"
      # X Growth W4 (XG-W4): the reply desk — discovery provider, scorer,
      # drafter, critics, queue, exporter, and the sentinel reply-counter fix.
      # The engine modules are matched by engine/marketing/** above and
      # config/marketing.yml + admin/marketing.py are already listed; these three
      # are named so a PR that edits ONLY the operator-curated author register,
      # ONLY the twitterapi.io sub-budget, or ONLY the suite still reaches the
      # marketing-engine lane that runs the register validator and the tests.
      - "config/reply_targets.yml"
      - "config/press_sources.yml"
      - "tests/test_marketing_reply_desk.py"
      # The reply REGISTER suites — the warmth openers (2026-08-01) and the
      # doorway tails (same day). Both drive engine/marketing/reply_drafter.py,
      # which the engine/marketing/** glob already reaches, so these two entries
      # exist for the suite-only edit: without them a PR that changes ONLY the
      # register assertions never reaches the marketing-engine lane that runs
      # them (test-whitelist-rot). test_marketing_reply_warmth.py shipped
      # unwhitelisted and unrun in CI on 2026-08-01 and is named here now.
      - "tests/test_marketing_reply_warmth.py"
      - "tests/test_marketing_reply_tails.py"
      # X Growth W6 (XG-W6): the ONE feedback loop (labels store + weekly
      # scorecard), parent-adjusted reply labels, the per-account health monitor
      # + network tripwire + halt registry, the pre-registered blind-identity
      # eval, the learned-rule version log, and the reply-desk PRODUCER. The
      # engine modules are matched by engine/marketing/** above and
      # config/marketing.yml + admin/marketing.py are already listed; these are
      # named so a PR that edits ONLY the nightly step, ONLY the prereg document,
      # or ONLY a suite still reaches the marketing-engine lane. The prereg doc
      # is listed because a test asserts the doc and blind_identity.PREREG agree
      # on every number — a prereg that can drift from its own doc is not one.
      - "scripts/marketing_learning_nightly.py"
      - "scripts/marketing_fastlane_daemon.py"
      - "tests/test_marketing_intelligence_desk.py"
      - "docs/blind_identity_eval_prereg.md"
      - "tests/test_marketing_learning.py"
      - "tests/test_marketing_reply_producer.py"
      # The shared fixture both XG-W6 suites import. Listed by name for the same
      # reason the suites are: an edit to ONLY this helper changes what both
      # suites assert, and without the entry it would never reach the lane that
      # runs them (test-whitelist-rot).
      - "tests/_xgw6_helpers.py"
      # X Growth W5 (XG-W5 = IS-W2): the scoring brain — L0 story spine, the six
      # L1 deterministic features, the P0 garbage gate, and the golden-set /
      # precision@20 harness. The engine modules are matched by
      # engine/marketing/** and config/marketing.yml above; the two suites and
      # the CLI are named so a TEST-ONLY or CLI-ONLY edit still reaches the lanes
      # that run them. The suites are DELIBERATELY SPLIT: the deterministic core
      # runs in the thin marketing-engine lane (pytest+pyyaml), and the
      # optional-dependency near-dup pass runs in marketing-scoring-optional,
      # which installs datasketch — that suite imports it at MODULE level with no
      # importorskip, so a missing wheel is red there rather than a silent skip.
      - "tests/test_marketing_scoring_brain.py"
      - "tests/test_marketing_scoring_optional_deps.py"
      - "scripts/marketing_golden_set.py"
      - "docs/scoring_brain.md"
      # Hot Tape P2 (2026-07-28): the LLM wire desk + its golden FactPackets +
      # the PR-evidence dry run. engine/marketing/hot_tape_llm.py is ALREADY
      # covered by the engine/marketing/** glob above, so it is deliberately not
      # repeated here; these three are named because a fixture-only, suite-only
      # or runner-only edit would otherwise never reach the marketing-engine
      # lane that runs the suite (test-whitelist-rot). The fixture is the
      # subject of most of the suite — an edit to a golden packet changes what
      # every gate is asserted against.
      - "tests/test_marketing_hot_tape_llm.py"
      - "tests/fixtures/hot_tape_golden_packets.json"
      - "scripts/hot_tape_llm_dryrun.py"
      # Content Studio W1 (2026-07-29): the per-post LLM writer, the cold-read
      # critic and the display-rounding law. engine/marketing/copywriter.py,
      # copy_critic.py and market_facts.py are already matched by the
      # engine/marketing/** glob above, so only the suite and the dry-run
      # runner are named here — a suite-only or runner-only edit would
      # otherwise never reach the marketing-engine lane (test-whitelist-rot,
      # the same failure mode the four entries above exist for).
      - "tests/test_marketing_copy_v2.py"
      - "tests/test_marketing_voice_laws.py"
      - "tests/test_marketing_publisher_names.py"
      - "tests/test_marketing_media_backfill.py"
      - ".github/workflows/marketing-earnings-wire.yml"
      - "tests/test_chart_facts.py"
      - "tests/test_confluence_source.py"
      - "tests/test_confluence_honesty.py"
      - "tests/test_tech_confluence.py"
      - "tests/test_chart_since_setup.py"
      - "scripts/marketing_copy_dryrun.py"
      # D02 per-post metrics poller + chart-image (PNG) pipeline. The poller
      # reads back Buffer analytics; media_publish uploads chart PNGs to R2;
      # content_studio renders the PNG at plan-build time. engine/marketing/**
      # covers the engine changes; the poller script + its tests are explicit.
      - "scripts/marketing_metrics_poll.py"
      - "tests/test_marketing_metrics_poll.py"
      # Both halves on purpose: the quota suite was wired 2026-08-11 and an edit
      # to a test that no path selects is a test nobody can prove they changed.
      - "tests/test_marketing_metrics_poll_quota.py"
      - "tests/test_marketing_chart_png.py"
      - "tests/test_marketing_media_publish.py"
      - "tests/test_share_cards.py"
      # The BUY-marker seal across the same pipeline (E-wave review, 2026-07-29):
      # a markerless card must stay markerless through the Chrome-less legacy
      # raster, which is the PNG X actually receives. Named here for the same
      # test-whitelist-rot reason as its three siblings above; import closure is
      # pytest + pyyaml + pillow (verified with pandas/numpy/pyarrow blocked).
      - "tests/test_marketing_media_fallback_seal.py"
      # chart_render SVG suites (jobs: chart-render, chart-render-data). Added
      # 2026-07-26; never whitelisted anywhere → test-whitelist-rot. Found via
      # test_chart_render_inline sitting RED on main since #3088, which moved the
      # MACD stroke off raw material-blue and left the test's colour probe behind.
      # engine/marketing/** already triggers on renderer changes; these entries
      # make a test-only edit reach its own guard.
      # 2026-07-27: the two lanes run the same three files, thin (pytest+pyyaml,
      # pins the lazy-import contract) and fat (pandas/numpy/pyarrow, the only
      # place the six data-dependent tests actually execute). Both are reachable
      # from these paths already — engine/marketing/** covers chart_render.py and
      # engine/** covers indicators_m2.py — so this list needs no new entry.
      - "tests/test_chart_render_inline.py"
      - "tests/test_chart_render_m2.py"
      # TrendSpider-hardening PR-A: the annotation-grammar suite runs in the
      # SAME paired lanes (thin proves the imports stay lazy, fat executes the
      # three loader/profile tests). Its committed SVG samples are inputs to it,
      # so a fixture-only edit must reach the lanes too — a stale golden that no
      # job re-renders is the dead-suite trap one layer down.
      - "tests/test_chart_render_grammar.py"
      - "tests/fixtures/chart_render/**"
      - "scripts/dev_render_chart_grammar_pngs.py"
      - "tests/test_marketing_card_parity.py"
      # Hot Tape P1 — the intraday attention radar (jobs: marketing-engine for the
      # two thin suites, marketing-data for the pandas-heavy nightly pack suite).
      # engine/marketing/** already covers the three engine modules and .github/**
      # covers the workflow, so these entries are the OTHER half: a PR that edits
      # ONLY a suite, ONLY the radar script, or ONLY the operator's tuning surface
      # must still reach the lanes that run them. A suite that ships dark is the
      # unrun-suite rot class, and this program adds three of them at once.
      - "tests/test_marketing_hot_tape.py"
      - "tests/test_marketing_hot_tape_pack.py"
      - "tests/test_marketing_hot_tape_radar.py"
      - "scripts/hot_tape_radar.py"
      - "config/hot_tape.yml"
      # TrendSpider hardening PR-B — the supply widening (job: marketing-data).
      # Same OTHER-half reason as the block above: engine/marketing/** and
      # config/marketing.yml already reach the subjects, so these three entries
      # exist so a PR editing ONLY a suite still reaches the lane that runs it.
      # test_movers_source and test_marketing_radar were named by no path filter
      # AND no job before this PR — they are the only regression guards the tier
      # ladder and the mover board have, and they ran nowhere.
      - "tests/test_marketing_supply_feeds.py"
      - "tests/test_movers_source.py"
      - "tests/test_marketing_radar.py"
      # TrendSpider hardening PR-C — the chart director + timeframe facts +
      # attention selector (job: marketing-data). Same OTHER-half reason: a PR
      # editing ONLY this suite must still reach the lane that runs it, because
      # this file carries the program's four acceptance gates (claim-window
      # refusal, forming-bar exclusion, in-frame caption gate, long-tail
      # annotation) and a gate that runs nowhere is not a gate. The committed
      # proof crops are inputs to the review, not to the suite, and are listed
      # so a crop-only refresh still triggers the renderer lanes.
      - "tests/test_chart_director.py"
      - "mockups/refs/trendspider-hardening/**"
      # tier-gate serving/revenue boundary (job: tier-gate). Found 2026-07-25
      # shipping #3488: NONE of the boundary's enforcement files were listed, so a
      # PR that changes ONLY the site-access boundary (regwall, Caddy public list,
      # site_access.yml, paywall) never triggered ci.yml — the drift tests the
      # boundary docs point at only ever fired incidentally, on unrelated PRs.
      # List every subject + test file the tier-gate job runs (app/main.py is
      # already listed above), so the exact PRs that move the boundary are gated.
      # A guard the guarded change cannot trigger is not a guard: that is the #3418
      # lesson ("red ~17h unwatched because no workflow named it") one layer up —
      # #3474 wired the test into a job, nothing wired the job to its inputs.
      # The TEST files below were the other half of the same hole (a PR editing only
      # a boundary test ran nothing either). tests/test_site_access_boundary.py now
      # pins the whole list by parsing this job's own `run:` steps — keep in sync.
      - "app/regwall.py"
      - "app/paywall.py"
      - "app/deploy/Caddyfile"
      - "config/site_access.yml"
      - "tests/test_site_access_boundary.py"
      - "tests/test_regwall.py"
      - "tests/test_regwall_json_gate.py"
      - "tests/test_paywall.py"
      - "tests/test_api_paywall.py"
      - "tests/test_auth_cache_outage.py"
      - "tests/test_paywall_pro_gate.py"
      - "tests/test_special_situations_gate.py"
      - "tests/test_china_special_situations_gate.py"
      # etfs.html joined the tier-gate job in SEO Supercharge W2. Its subjects
      # (templates/**, scripts/build_site.py, engine/**) already trigger above;
      # this line is the half that test_site_access_boundary derives from the
      # job's own `run:` steps, so a PR editing only this suite still runs it.
      - "tests/test_etfs_gate.py"
      - "tests/test_public_dashboard_preview.py"
      # us_stocks.html joined the tier-gate job with the board split (#5840) and
      # its four adjacent panels. Same half as the two lines above: templates/**
      # and scripts/build_site.py already trigger this job, so this line exists so
      # a PR editing ONLY the suite still runs it — a guard the guarded change
      # cannot trigger is not a guard.
      - "tests/test_us_board_gate.py"
      # test_etf_board joined the unrun-scoring-engine job 2026-08-03 (it ran in
      # zero jobs before that; its subjects engine/** + templates/** already
      # trigger above — this line covers a PR editing only the suite itself).
      - "tests/test_etf_board.py"
      # OEU M-FIX wiring (2026-07-25): flow-leaders / market-structure /
      # options-screener suites + the neural-web support-map suite — previously
      # run by ZERO workflows (see the options-estate-guards job). Engine
      # sources listed here; engine/neuralweb/**, engine/pick_lab/candidates.py,
      # config/synapse.yml and templates/** already trigger above.
      # confluence_graph.json feeds the support-map coverage census band.
      - "engine/flow_leaders.py"
      - "engine/market_structure_context.py"
      - "engine/systematic_flows.py"
      - "engine/pick_lab/registry.py"
      - "scripts/build_flow_leaders.py"
      - "scripts/build_market_structure.py"
      - "scripts/build_market_structure_page.py"
      - "scripts/build_options_screener.py"
      - "tests/test_flow_leaders.py"
      - "tests/test_build_flow_leaders.py"
      - "tests/test_market_structure.py"
      - "tests/test_market_structure_nw.py"
      - "tests/test_build_market_structure_page.py"
      - "tests/test_build_options_screener.py"
      - "tests/test_support_map.py"
      - "data/neuralweb/confluence_graph.json"
      # OEU M-CMD (2026-07-26): the Options workspace builder + its suite. A guard
      # the guarded change cannot trigger is not a guard — list the subject files
      # so a PR touching ONLY the workspace reaches options-estate-guards.
      # (templates/** and lib/** already trigger above.)
      - "scripts/build_options_command.py"
      - "tests/test_build_options_command.py"
      # Free acquisition estate (2026-07-26): the builder, its content sources and
      # its templates had NO workflow at all — `--check` and test_free_content.py
      # were manual-only, so the estate rotted 52 pages behind seo_base.html.j2
      # (the #3592 Legal footer column never reached them) with main staying green.
      - "scripts/build_free_content.py"
      - "tests/test_free_content.py"
      # i18n split-tag guard (2026-07-30). Its SUBJECTS — site/** and templates/**
      # — already trigger above, so the guarded change always reaches it; this
      # line only covers a PR that edits the guard itself.
      - "tests/test_i18n_split_tag_guard.py"
      - "content/seo/**"
      - "templates/seo_base.html.j2"
      - "templates/seo_article.html.j2"
      - "templates/seo_calculator_base.html.j2"
      - "templates/seo_blog_index.html.j2"
      - "templates/seo_learn_index.html.j2"
      - "templates/seo_tools_index.html.j2"
      - "templates/calculators/**"
      # D14 W1 Media Network press engine (2026-07-26): the lane that WRITES
      # into the free estate. Listed alongside the estate's own paths because a
      # press change is exactly the change that can break the estate contract —
      # a guard the guarded change cannot trigger is not a guard, and the press
      # suite lives in the `press-lane` job in .github/ci/legacy-jobs.yml.
      - "engine/press/**"
      - "engine/earnings_narrative/**"
      - "scripts/build_earnings_story_packet.py"
      - "scripts/run_press.py"
      - "config/press.yml"
      - "admin/press.py"
      - "tests/test_press_*.py"
      - "tests/test_earnings_canonical_story.py"
      - ".github/workflows/press-publish.yml"
      # D14 W2R / X Growth XG-W8 (2026-07-28): the research triage layer that
      # decides WHICH vault report the Research Desk covers, its demote-only LLM
      # veto pass, the cold-start volume ramp, and the dark Mastermind Research
      # X lane. engine/press/** + config/press.yml + tests/test_press_*.py above
      # already reach the engine, the config and the suite; the runner, the
      # workflow and the runbook are named so a PR that edits ONLY one of them
      # still reaches the press-lane job that runs the guard. The runbook is
      # listed because a test asserts it names every arming lever — a runbook
      # that can drift from the levers it documents is not one.
      - "scripts/run_research_triage.py"
      - ".github/workflows/research-triage.yml"
      - "docs/research_triage.md"
      # The vault catalog snapshot is the triage layer's INPUT, and the research
      # desks plan from it. A hourly ingest commit can change what the planner
      # sees without touching a line of code.
      - "data/research_vault/catalog.json"
      - "data/press/research_triage.jsonl"
      # D14 W1.5 press properties (2026-07-27): the per-publication static
      # trees, their templates, and the builder. Same reasoning as above — the
      # cutover pairing guard and the property determinism checks have to be
      # reachable by the changes they guard, which now includes a
      # templates-only or a properties-only diff.
      - "scripts/build_press_properties.py"
      - "templates/press/**"
      - "properties/**"
      - "content/press/**"
      # The ledger is the property trees' SOURCE, so a commit that appends to it
      # can put the committed trees out of date on its own. Without this line
      # the derived artifact and its input could move in separate PRs and only
      # the derived one would be checked.
      - "data/press/published.jsonl"
      # ── UNRUN-SUITE CENSUS (2026-07-26) ──────────────────────────────────────
      # Measured when the census was scoped to tests/; it now covers the whole tree
      # (#4693, 2026-08-06), so re-run scripts/audit_unrun_tests.py for live counts
      # rather than reading the historical figures below as current.
      # 1135 of 1491 tests/test_*.py suites were named by NO `run:` step in ANY of
      # the 51 workflows, and there is no broad `pytest tests/` anywhere — every
      # invocation carries an explicit file list, so an unnamed suite is genuinely
      # never executed.  #3595 fixed a six-suite instance of this hole; the block
      # below closes the STRICTLY DARK subset: suites where neither the test file
      # nor the module under test was matched by any pattern here, so no possible
      # edit could even START ci.yml.  Both halves are listed on purpose — wiring
      # a suite into a job while leaving its subject untriggerable rebuilds the
      # #3488 shape ("a guard the guarded change cannot trigger is not a guard").
      # Ranked by blast radius: everything reachable from the nightly/render
      # pipelines first (build_vector, grade_us_board, oracle_nightly, the R2
      # transport), since a silent break there moves shipped numbers with no
      # signal at all.  Suites run in the unrun-* jobs below.
      # unrun-vector-dsr — BTC vector allocation + the DSR trial budget that haircuts it
      - "tests/test_btc_vector_w1.py"
      - "tests/test_deflated_sharpe.py"
      - "tests/test_settled_close_tape.py"
      - "tests/test_vector_catalyst.py"
      - "tests/test_vector_forward_risk.py"
      - "tests/test_vector_kelly.py"
      - "tests/test_vector_timeline_gated.py"
      - "scripts/build_basket_pulse.py"
      - "scripts/build_vector.py"
      - "scripts/calibrate_vector.py"
      # unrun-oracle-desk — oracle nightly + its gauntlet promotion gates
      - "tests/test_oracle_asymmetry_intraday.py"
      - "tests/test_oracle_gauntlet.py"
      - "tests/test_oracle_gauntlet_p3b.py"
      - "tests/test_oracle_gauntlet_p8.py"
      - "tests/test_oracle_nightly.py"
      - "tests/test_oracle_onset_quality_w1.py"
      - "tests/test_oracle_single_regime_gate.py"
      - "tests/test_oracle_tape.py"
      - "tests/test_oracle_turn_desk.py"
      - "scripts/oracle_asymmetry_intraday.py"
      - "scripts/oracle_gauntlet_p3.py"
      - "scripts/oracle_gauntlet_p8.py"
      - "scripts/oracle_nightly.py"
      - "scripts/oracle_onset_quality_w1.py"
      - "scripts/oracle_reversion_screen.py"
      - "scripts/oracle_review_inbox.py"
      - "scripts/oracle_tape.py"
      # unrun-grading-board — US board grading — the shipped verdict ledgers and their tenure stamps
      - "tests/test_board_tenure_stamp.py"
      - "tests/test_claim_accountability.py"
      - "tests/test_onboard_compare_matrix.py"
      - "tests/test_track_record_dlg.py"
      # Board continuity, both shipped 2026-08-05 and named by NO workflow until
      # now (#4554 row-persistence/outage continuity, #4557 same-as_of Jaccard).
      # Subject halves are already triggerable, so only the test halves were
      # missing: scripts/grade_us_board.py is listed below, and
      # scripts/build_stock_library.py (which owns _board_continuity_warning) is
      # listed with the setup-tier block above — do not re-add either here.
      - "tests/test_us_board_continuity_guard.py"
      - "tests/test_us_board_ledger_continuity.py"
      - "tests/test_us_board_outcomes.py"
      - "tests/test_us_board_outcomes_strip.py"
      - "tests/test_us_board_zero_accrual_alarm.py"
      - "scripts/audit_claim_accountability.py"
      - "scripts/grade_us_board.py"
      # unrun-intl-libraries — China / HK / Canada library writers behind the international surfaces
      - "tests/test_backfill_china_communiques.py"
      - "tests/test_baskets_china_intel.py"
      - "tests/test_baskets_china_ths.py"
      - "tests/test_canada_branch_b.py"
      - "tests/test_china_analogs.py"
      - "tests/test_china_connect_leaderboard_render.py"
      - "tests/test_china_fx_context_render.py"
      - "tests/test_china_stf_facets.py"
      - "tests/test_hk_closes_fallback.py"
      - "tests/test_stock_library_manifests.py"
      - "tests/test_stocktable_hk_data.py"
      - "tests/test_w0_5_10_8.py"
      - "scripts/backfill_china_communiques.py"
      - "scripts/build_baskets_china.py"
      - "scripts/build_baskets_china_ths.py"
      - "scripts/build_canada_library.py"
      - "scripts/build_china_analogs.py"
      - "scripts/build_china_library.py"
      - "scripts/build_hk_library.py"
      # unrun-hk-board — the HK board resurrection pair (#4421), born dark AFTER
      # the census sweep: the hk_prophet_v2 rank/lane engine (G1-G5 gates) and
      # the unified board UI contract.  engine/** and templates/** already cover
      # the code halves; these are the test halves PLUS the frozen G1 pair (the
      # panel-replay fixture and the board artifact it excludes against) and the
      # committed close panel the staleness tripwire replays.  The fixture
      # entries are the #4559 lesson: that heal was a fixture-only diff that
      # matched NOTHING in this list, so the PR re-pinning the G1 measurement
      # merged having triggered zero pack checks.  The panel glob closes the
      # other input: a hand rewrite of the tripwire's replay grid must start
      # the workflow (the nightly asia collection pushes to main and never
      # opens a PR, so the glob costs nothing on the daily bake).
      - "tests/test_hk_board_rank.py"
      - "tests/test_hk_board_ui.py"
      - "tests/fixtures/hk_board_2026_07_31.json"
      - "tests/fixtures/hk_standouts_2026_07_31.json"
      - "data/hk_search/**"
      # The regenerator's own suite (job: regen-hk-g1-fixture-guard).  Its subject
      # scripts/regen_hk_g1_fixture.py rides the "scripts/*.py" catch-all below,
      # but tests/** is deliberately NOT broad, so without this line a test-only
      # edit could not start the packs that run it — the #3488 shape ("a guard the
      # guarded change cannot trigger is not a guard"), and the same darkness the
      # two entries above were added to close.
      - "tests/test_regen_hk_g1_fixture.py"
      # The HK reclaim-veto admission policy (operator ruling 2026-08-03),
      # joined to unrun-hk-board 2026-08-05 — dark since it landed. Its two
      # subjects reach this list already (engine/** for signal_quality /
      # signal_gate / hk_board_rank, scripts/**/*.py for build_hk_library's
      # HK_RECLAIM_VETO); the test half matched nothing, so an edit narrowing
      # the pin itself could not start the workflow that runs it.
      - "tests/test_hk_reclaim_veto_policy.py"
      # nyse-calendar-freshness — the exchange-calendar reference under every
      # freshness SLA. lib/** above already triggers on lib/nyse_calendar.py, but
      # a test-only edit must reach its own guard too (#3488: "a guard the guarded
      # change cannot trigger is not a guard"). test_nyse_calendar.py was named by
      # NO job in ANY workflow while ~20 scripts imported the module — the darkness
      # that let two builders import a `trading_dates_between` it never defined and
      # swallow the ImportError into a permanently-false stale gate.
      - "tests/test_nyse_calendar.py"
      # tsx-calendar — the Canadian session reference under check D's board
      # freshness budget. Same #3488 reason as its NYSE sibling above: lib/** already
      # triggers on lib/tsx_calendar.py, but a test-only edit must reach its own guard.
      - "tests/test_tsx_calendar.py"
      # first-party-import-names — the single-module sweep in
      # test_nyse_calendar_import_names.py was promoted repo-wide here. It resolves
      # EVERY `from lib|engine|scripts|collectors|app|admin|tests|tools|worker import
      # NAME` against the target's real definitions, which found five more dead
      # imports of the same shape (engine.config, engine.data_store, lib.logging,
      # _dna_class, _TAPE_PATH). Stdlib-only (ast + pathlib) so it stays in the
      # minimal-deps lane. A test-only edit must reach its own guard.
      - "tests/test_first_party_import_names.py"
      # unrun-data-plane — R2 transport, accrual audits and the ledger-advance guards
      - "tests/test_audit_options_accrual.py"
      - "tests/test_audit_r2.py"
      # The 2026-08-03 store-freshness program's four suites (added 2026-08-06).
      # Their SUBJECTS were already triggerable — collectors/** and scripts/*.py
      # below both match — so only the test-file half of the census was missing:
      # editing a suite to weaken it started no run that could see it do so.
      - "tests/test_audit_stocks_freshness.py"
      - "tests/test_collector_freshness_disclosure.py"
      - "tests/test_stock_library_freshness.py"
      - "tests/test_stock_price_retention.py"
      - "tests/test_audit_thetadata_accrual.py"
      - "tests/test_build_polygon_universe.py"
      - "tests/test_builder_failstreaks.py"
      - "tests/test_check_ledger_advance.py"
      - "tests/test_check_surface_freshness.py"
      - "tests/test_fetch_dead_name_prices_polygon.py"
      - "tests/test_fetch_r2.py"
      - "tests/test_finviz_archival.py"
      # The refresh contract's own suite (W3A, 2026-08-14). Its SUBJECT
      # (scripts/fetch_finviz_themes.py, below) already triggers, but a test-only
      # edit that weakened an interlock assertion would otherwise fire no run —
      # the exact half-registration this list exists to close.
      - "tests/test_finviz_tree_refresh.py"
      - "tests/test_notify_turn_events.py"
      - "tests/test_polygon_intraday.py"
      - "tests/test_thetadata_tape_sessions.py"
      - "scripts/audit_options_accrual.py"
      - "scripts/audit_r2.py"
      - "scripts/audit_thetadata_accrual.py"
      - "scripts/build_polygon_intraday.py"
      - "scripts/build_polygon_universe.py"
      - "scripts/calibrate_thetadata_tape_sessions.py"
      - "scripts/check_builder_failstreaks.py"
      - "scripts/check_ledger_advance.py"
      - "scripts/check_surface_freshness.py"
      - "scripts/fetch_finviz_themes.py"
      - "scripts/fetch_r2.py"
      - "scripts/notify_turn_events.py"
      - "scripts/research/fetch_dead_name_prices_polygon.py"
      # subsector-rotation session-stamp hygiene (calendar-asof audit 2026-08-05):
      # the finviz collector's `asof` is the clock these ledgers were stamped from,
      # but the track record, its significance guard and the heal were enumerated in
      # NO workflow — a PR touching them fired no run that could see a failure.
      - "tests/test_heal_subsector_rotation_ledgers.py"
      - "tests/test_subsector_significance_guard.py"
      - "tests/test_subsector_track_record.py"
      - "engine/subsector_track_record.py"
      - "scripts/heal_subsector_rotation_ledgers.py"
      # unrun-factor-research — factor registry + measurement disclosure + the study lanes behind them
      - "tests/test_a2_oos_analysis.py"
      - "tests/test_build_measurement_evidence_gap.py"
      - "tests/test_construction_divergence_study.py"
      - "tests/test_expect_drift_panel.py"
      - "tests/test_f_hz1_study.py"
      - "tests/test_factor_intelligence_state.py"
      - "tests/test_falsosc_trial_v1.py"
      - "tests/test_grade_breadth_divergence.py"
      - "tests/test_intraday_large_cap_tech_leader_study.py"
      - "tests/test_long_hold_gap_guard.py"
      - "tests/test_macro_fire_context.py"
      - "tests/test_no_lookahead.py"
      - "tests/test_options_history_gauntlet.py"
      - "tests/test_per_fire_sector_benchmark.py"
      # Pick-conditional forward distributions (advanced-quant-methods w1 + w1.5,
      # wired 2026-08-06). BORN DARK: wave-1 shipped tests/test_pick_forward_dist.py
      # named by no `run:` step in any of the 51 workflows, so its PIT
      # truncate-and-recompute equality, its embargo boundary and its
      # noise-partition negative control have never executed in CI. The w1.5 suite
      # would have inherited exactly that — and its whole job is an IMPORT-SURFACE
      # PIN that refuses the study when phase-0's function SIGNATURES (parameter
      # names/kinds/defaults) or the frozen CONSTANTS defining both arms drift:
      # phase-0's cohort window/floors/taus/OOS-start/bootstrap seed, and the engine's
      # VOL_FLOOR, universe floors, feature windows (VOL_WIN/VOL_MINP/REF_WIN/REF_MINP)
      # and all five scheme axis tuples. Function BODIES are not pinned. That guard is
      # worth nothing if it never runs. Both halves listed: the suites AND their
      # subjects (engine/pick_forward_dist.py, engine/vol_forecast.py, the two study
      # scripts), so no possible edit leaves the guard untriggerable.
      - "tests/test_pick_forward_dist.py"
      - "tests/test_pick_forward_dist_phase1.py"
      - "engine/pick_forward_dist.py"
      - "engine/vol_forecast.py"
      - "scripts/pick_forward_dist_phase0.py"
      - "scripts/pick_forward_dist_phase1_har.py"
      - "tests/test_register_factor_hypotheses.py"
      - "tests/test_run_w1_sev.py"
      - "tests/test_run_w2_slq.py"
      - "tests/test_thesis_funnel_history.py"
      - "tests/test_w3_fingerprint_stats.py"
      - "scripts/build_factor_intelligence_state.py"
      - "scripts/build_measurement.py"
      - "scripts/grade_breadth_divergence.py"
      - "scripts/register_factor_hypotheses.py"
      - "scripts/research/a2_oos_analysis.py"
      - "scripts/research/build_expect_drift_panel.py"
      - "scripts/research/build_macro_fire_context.py"
      - "scripts/research/build_per_fire_sector_benchmark.py"
      - "scripts/research/build_thesis_funnel_history.py"
      - "scripts/research/build_thesis_funnel_snapshot.py"
      - "scripts/research/f_hz1_study.py"
      - "scripts/research/intraday_large_cap_tech_leader_study.py"
      - "scripts/research/long_hold_label_panel.py"
      - "scripts/research/options_history_gauntlet.py"
      - "scripts/research/run_w1_sev.py"
      - "scripts/research/run_w2_slq.py"
      - "scripts/research/run_w3_census_fingerprints.py"
      - "scripts/run_falsosc_trial_v1.py"
      - "scripts/study_construction_divergence.py"
      # unrun-serving-admin — admin control surfaces + the brain/tape serving edges
      # Project runtime truth: durable topology + strict private schema + one shared
      # collector used by both the CLI and authenticated admin route.  Keep every
      # half on this lane so a schema-only or topology-only drift cannot ship dark.
      - "config/production_topology.yml"
      - "contracts/runtime/mastermind.runtime_state.v1.schema.json"
      - "docs/PRODUCTION_RUNTIME_TRUTH.md"
      - "lib/project_runtime_state.py"
      - "scripts/build_project_runtime_state.py"
      - "tests/test_project_runtime_state.py"
      - "tests/test_admin_config_store.py"
      - "tests/test_admin_flags.py"
      - "tests/test_admin_health.py"
      # admin.server's HTTP surface + the landing summary payload contract. Listed by
      # name so a test-only edit still reaches the lane; the SOURCES it guards
      # (admin/server.py, admin/key_alerts.py) are already covered by the `admin/*.py`
      # glob below. Untriggered AND unrun until 2026-08-06: #4612 added the key_alerts
      # rail to _summary_payload() and did schedule this workflow via admin/*.py, but no
      # step ran the suite, so the stale payload-key assertion went red on main unseen.
      - "tests/test_admin_server.py"
      - "tests/test_brain_sse_keepalive.py"
      - "tests/test_tape_decode.py"
      - "admin/config_store.py"
      - "admin/flags.py"
      - "admin/health.py"
      - "app/brain_runs.py"
      - "app/tape_decode.py"
      # admin/*.py smoke.  Its parametrization is DERIVED from the admin/*.py glob, so a
      # new admin module silently adds an import case — and the three file-by-file entries
      # above cover 3 of admin/'s 46 modules, so that new module could not start this
      # workflow.  Same shape as the unrun-import-hygiene ratchet below: a directory-wide
      # scanner needs a directory-wide trigger, or the guarded change never reaches the
      # guard.  `admin/*.py` is top-level-only under GitHub's filter syntax, which is
      # exactly the non-recursive glob the suite itself walks.
      - "tests/test_admin_modules_smoke.py"
      - "admin/*.py"
      # unrun-builders-render — page/render builders whose output ships to the site
      - "tests/test_ai_desk_page.py"
      - "tests/test_build_chart_data.py"
      - "tests/test_build_odds_observability.py"
      - "tests/test_build_sector_map.py"
      - "tests/test_committee_template.py"
      - "tests/test_confluence_screener_page.py"
      - "tests/test_contagion_card_render.py"
      # Dark-pool desk (backfill 2026-08-06). The subject rides the "scripts/*.py"
      # catch-all below, so an edit to the builder DID start this workflow — it just
      # ran nothing that touched the builder, which is the worse half: a green ci.yml
      # read as proof. tests/** is deliberately not broad, so the test half matched no
      # pattern at all. #4704 is the measured cost: the suite overwrote three tracked
      # artifacts on every run (site/darkpool.html, site/darkpool_eod.json,
      # data/darkpool/context/latest.json). conftest.py's repo-wide MM_DATA_GUARD
      # tripwire detects exactly that class and forces exit 1 — but it never ran here,
      # so only a local invocation could ever find it. Both halves listed per the
      # census convention ("a guard the guarded change cannot trigger is not a guard").
      - "tests/test_darkpool_desk.py"
      - "tests/test_options_estate_redirect_stubs.py"
      - "tests/test_macro_signals_render.py"
      # product-nav-icons.css is the icon authority test_nav_submenu_icons.py now
      # checks the nav partial against, so its own guard must not stay dark.
      - "tests/test_nav_icon_refresh.py"
      - "tests/test_nav_submenu_icons.py"
      - "tests/test_sector_nav_links.py"
      - "tests/test_site_semantics_refs.py"
      - "tests/test_sp500_heatmap_caps.py"
      - "tests/test_stage_analysis_page.py"
      - "tests/test_stocktable_data.py"
      - "tests/test_template_items_footgun.py"
      - "tests/test_trading_journal_xlsx.py"
      - "tests/test_us_stocks_w8g_table.py"
      # Nav-chrome guards (unrun-suite backfill 2026-08-03): the one-header
      # law, nav prefix/hover/anchor geometry, landing IA, start-page voice +
      # publish integrity, and the Terminal portal/entitlement contracts.
      # Subjects all live under templates/**, site/**, and named scripts/*
      # entries already present; only the test halves were missing.
      - "tests/test_product_chrome.py"
      - "tests/test_subdir_nav_prefix.py"
      - "tests/test_nav_hover_bridge.py"
      - "tests/test_nav_menu_mockup_hover.py"
      - "tests/test_nav_wide_menu_anchor.py"
      - "tests/test_landing_navigation.py"
      - "tests/test_oip_w1_nav_regroup.py"
      - "tests/test_hub_welcome_persona.py"
      - "tests/test_start_publish_integrity.py"
      - "tests/test_terminal_indicator_pricing.py"
      - "tests/test_terminal_overlay_contract.py"
      # The portal's CASCADE guard: substring assertions cannot see that a
      # descendant re-declaring an inherited property un-hides itself through
      # the closed root. Its subject is templates/terminal_overlay.js, already
      # covered by "templates/**" below — this half wires the test itself.
      - "tests/test_terminal_overlay_closed_state.py"
      - "scripts/build_ai_desk_page.py"
      - "scripts/build_chart_data.py"
      - "scripts/build_confluence_screener.py"
      - "scripts/build_darkpool_desk.py"
      - "scripts/build_odds.py"
      - "scripts/build_sector_map.py"
      - "scripts/build_sp500_heatmap.py"
      - "scripts/build_stage_analysis_page.py"
      # TOP ANATOMY W1 (Winner Health): the page builder's twin, same reason as
      # its stage-analysis sibling directly above — provenance for a builder that
      # renders from a committed artifact in three lanes (daily, render,
      # engine-render). Coverage registration, never an exemption: this key is
      # `on.pull_request.paths` and ci.yml has no `paths-ignore` at all.
      - "scripts/build_winner_health_page.py"
      # unrun-builders-stores — store/ops builders behind the nightly data lanes
      - "tests/test_append_sync_gauge.py"
      - "tests/test_audit_fred_groups.py"
      - "tests/test_audit_massive_store.py"
      - "tests/test_build_adjudication_packet.py"
      - "tests/test_build_demand.py"
      - "tests/test_build_ext_quotes.py"
      - "tests/test_build_live_overlay.py"
      - "tests/test_build_m2_profiles.py"
      - "tests/test_build_unusual_baseline.py"
      - "tests/test_calibrate_flow_signing_append.py"
      - "tests/test_calibrate_stage.py"
      - "tests/test_cctv_finalize_watcher.py"
      - "tests/test_collect_lane_gate.py"
      - "tests/test_context_candidates.py"
      - "tests/test_context_index_eval.py"
      - "tests/test_deploy_update_self_heal.py"
      # ...and the SUBJECT that suite guards (#3509 lesson: listing only the test
      # leaves the guarded change unable to trigger its own guard). update.sh
      # decides which pushed commits restart macro-api; a module the API imports
      # but its restart regex omits deploys to the VPS and never goes live, because
      # sys.modules pins the old object — engine/research_vault/* sat in exactly
      # that hole until #3684, shipping dead download caps with main green. Without
      # this line a PR editing ONLY the restart regex still runs nothing.
      - "app/deploy/update.sh"
      # test_deploy_update_self_heal recomputes the API's load-time import closure
      # from the AST of app/*.py, so a new module-level engine/lib import in ANY
      # router must reach it. hub.py was the last one matching no other job here
      # (brain_runs.py and tape_decode.py are already listed above).
      - "app/hub.py"
      # W1 freshness sentinel (the dead-man switch) and BOTH its subjects. The
      # units and the script are what a change would edit; without them a PR
      # retuning a freshness budget or a systemd cap would run the guard on
      # nothing (#3509 lesson — a guard the guarded change cannot trigger is
      # not a guard). app/deploy/update.sh + config/site_access.yml already
      # appear elsewhere in this list and carry the arming/boundary halves.
      - "tests/test_freshness_sentinel.py"
      - "scripts/freshness_sentinel.py"
      - "app/deploy/macro-sentinel.service"
      - "app/deploy/macro-sentinel.timer"
      # MMX-001 customer-table backup — same #3509 lesson as the sentinel:
      # listing only the suite leaves a unit/script-only edit unable to start
      # the job that owns it. update.sh already appears above.
      - "tests/test_backup_user_tables.py"
      - "scripts/backup_user_tables.py"
      - "app/deploy/macro-user-backup.service"
      - "app/deploy/macro-user-backup.timer"
      # PR-C — the W-L1 acceptance record built on top of that sentinel. It
      # reads the sentinel's own state file, so a change to either side of that
      # contract must be able to START this workflow; same #3509 lesson as the
      # four lines above.
      - "tests/test_close_pass_slo_report.py"
      - "scripts/close_pass_slo_report.py"
      # The china half of that dead-man switch: the board-lag disclosure the
      # sentinel's china surface anchors on, and the A-share session calendar the
      # engine computes `delayed` from. Both halves listed for the same reason as
      # above — a PR retuning the CN holiday table or rewording the banner must be
      # able to START this workflow, or the contract test tying the template's
      # English wording to the sentinel's regex guards nothing. china.html.j2 and
      # the two builders already match via templates/** and scripts/**.
      - "tests/test_china_delayed_board_disclosure.py"
      - "tests/test_cn_calendar.py"
      - "lib/cn_calendar.py"
      - "tests/test_ei_f1d_shadow.py"
      - "tests/test_exit_tax_scenarios.py"
      - "tests/test_expanding_mean_benchmark.py"
      - "tests/test_fetch_basket_ohlcv.py"
      - "tests/test_finra_short_interest_pit.py"
      - "tests/test_flow_score_stock_block.py"
      - "tests/test_gex_history_snapshot.py"
      - "tests/test_grade_policy_calendar.py"
      - "tests/test_import_equitydesk.py"
      - "tests/test_import_equitydesk_backfill.py"
      - "tests/test_index_add_overnight.py"
      - "tests/test_manifest_seams.py"
      - "tests/test_mri_config_spine.py"
      - "tests/test_narrative_ttl.py"
      - "tests/test_net_replay_v1.py"
      - "tests/test_options_tape_signed.py"
      - "tests/test_quotes_server.py"
      - "tests/test_reconcile_membership.py"
      - "tests/test_refresh_markets_now.py"
      - "tests/test_release_playbook.py"
      - "tests/test_sf_harness_runner.py"
      - "tests/test_slf031.py"
      - "tests/test_slf051_cn_margin.py"
      - "tests/test_tech_lab_cli.py"
      - "tests/test_tech_parity_fixtures.py"
      - "tests/test_vps_live_orchestration.py"
      - "scripts/_narrative_ttl.py"
      - "scripts/analyze_index_add_overnight.py"
      - "scripts/append_sync_gauge.py"
      - "scripts/audit_common.py"
      - "scripts/audit_fred_groups.py"
      - "scripts/audit_massive_store.py"
      - "scripts/build_adjudication_packet.py"
      - "scripts/build_context_candidates.py"
      - "scripts/build_demand.py"
      - "scripts/build_ext_quotes.py"
      - "scripts/build_live_overlay.py"
      - "scripts/build_m2_profiles.py"
      - "scripts/build_masterminds.py"
      - "scripts/build_narrative_tolerance_report.py"
      - "scripts/build_options_hub_nightly.py"
      - "scripts/build_tech_parity_fixtures.py"
      - "scripts/build_unusual_baseline.py"
      - "scripts/calibrate_flow_signing.py"
      - "scripts/calibrate_stage_vs_equitydesk.py"
      - "scripts/cctv_finalize_watcher.py"
      - "scripts/check_vps_live_health.py"
      - "scripts/context_index_eval.py"
      - "scripts/ei_shadow_review.py"
      - "scripts/fetch_basket_ohlcv.py"
      - "scripts/grade_policy_calendar.py"
      - "scripts/import_equitydesk_backfill.py"
      - "scripts/import_equitydesk_full.py"
      - "scripts/migrate_narrative_ttl.py"
      - "scripts/options_tape_signed_pilot.py"
      - "scripts/quotes_server.py"
      - "scripts/reconcile_membership.py"
      - "scripts/refresh_markets_now.py"
      - "scripts/run_signal_foundry_harness.py"
      - "scripts/slf031_edgar_pilot.py"
      - "scripts/tech_lab_cli.py"
      - "scripts/vps_live_orchestrator.py"
      - "scripts/watch_release_publications.py"
      # ── P1 unrun-suite census (2026-07-26) ─────────────────────────────────
      # Both halves, per #3488: the test files AND the subject modules nothing else
      # glob-matches. A guard the guarded change cannot trigger is not a guard.
      # unrun-site-surfaces
      - "tests/test_action_board_coherence.py"
      - "tests/test_advanced_breadth.py"
      - "tests/test_basket_integration.py"
      - "tests/test_breadth_panel.py"
      - "tests/test_c4_dollar.py"
      - "tests/test_factor_ic_panel.py"
      - "tests/test_macro_glance.py"
      - "tests/test_macro_signals_vm.py"
      - "tests/test_market_gamma.py"
      - "tests/test_market_gamma_contract.py"
      - "tests/test_regime_snap.py"
      - "tests/test_spark_zone_band.py"
      - "tests/test_ts_u3_dashboard_surfaces.py"
      - "tests/test_wiki_pageviews.py"
      - "scripts/c4_cnh_basis.py"
      - "scripts/c4_reer_value.py"
      # Page-guard suites (unrun-suite backfill 2026-08-03): rendered-estate
      # regression guards — macro desk/typography/glance, action-board hover
      # cards, cold-load budgets, stocktable bootstrap, logo adapter, globe
      # overlays, radar boot, reports timeline, bilingual span scan. Their
      # subjects were always covered by templates/** + site/**; only the test
      # halves were missing.
      - "tests/test_macro_desk_surface.py"
      - "tests/test_macro_primary_typography.py"
      - "tests/test_macro_what_to_do_glance.py"
      - "tests/test_action_board_hover_cards.py"
      - "tests/test_dashboard_cold_load_budget.py"
      - "tests/test_stocktable_load_order.py"
      - "tests/test_stock_logo_system.py"
      - "tests/test_globe_tooltip_behavior.py"
      - "tests/test_radar_page_boot.py"
      - "tests/test_reports_timeline_ui.py"
      - "tests/test_bilingual_span_leak.py"
      # unrun-release-forecast
      - "tests/test_content_stamp_freshness_sweep2.py"
      - "tests/test_cpi_postmortem_fixes.py"
      - "tests/test_release_combined.py"
      - "tests/test_release_forecast_producer.py"
      - "tests/test_release_forecast_v2.py"
      - "tests/test_release_integration_2a.py"
      - "tests/test_release_integration_2b.py"
      - "tests/test_release_integration_w11g.py"
      - "tests/test_release_market_context.py"
      - "scripts/build_release_forecast.py"
      - "scripts/check_hazard_model_freshness.py"
      # Unrun backfill 2026-08-03: CPI bridge, official-event live client,
      # Release Radar date selection — the release estate's missing test halves.
      - "tests/test_release_cpi_bridge.py"
      - "tests/test_release_publications_live_client.py"
      - "tests/test_release_radar_date_selection.py"
      - "tests/test_inflation_intelligence.py"
      - "tests/test_inflation_intelligence_nw.py"
      - "tests/test_inflation_units.py"
      - "tests/test_release_actuals.py"
      - "tests/test_release_defects.py"
      - "tests/test_release_target_truth.py"
      - "tests/test_release_cpi_official_truth.py"
      - "tests/test_release_cpi_target_history.py"
      - "tests/test_release_cpi_coherent_governance.py"
      - "tests/test_release_cpi_coherent_shadow.py"
      - "tests/fixtures/release_cpi_truth/**"
      - "engine/inflation_intelligence.py"
      - "engine/neuralweb/ask_brain.py"
      - "engine/neuralweb/cortex.py"
      - "engine/neuralweb/mastermind_context.py"
      - "engine/neuralweb/world_state.py"
      - "engine/release_actuals.py"
      - "engine/release_defects.py"
      - "engine/release_target_truth.py"
      - "engine/release_cpi_official_truth.py"
      - "engine/release_cpi_target_history.py"
      - "engine/release_cpi_coherent_shadow.py"
      - "scripts/build_inflation_intelligence.py"
      - "scripts/collect_release_target_vintages.py"
      - "scripts/collect_release_cpi_official_truth.py"
      - "scripts/build_release_cpi_truth_parity.py"
      - "config/release_forecast_model_registry.yml"
      - "research/release_forecast/PREREG_COHERENT_RIDGE_V1.md"
      - "data/release_forecast/cpi_truth/**"
      - "data/release_forecast/official_actual_defects.json"
      - "data/fred_vintage/release_targets/**"
      - "scripts/reconcile_release_actuals.py"
      # unrun-brain-desks
      - "tests/test_admin_brief_gate.py"
      - "tests/test_ai_desk_panel.py"
      - "tests/test_altdata_intel.py"
      - "tests/test_altdata_price_truth.py"
      - "tests/test_china_intel_hub.py"
      - "tests/test_master_brain.py"
      - "tests/test_master_brain_policy.py"
      - "tests/test_master_brain_producer.py"
      - "tests/test_master_brain_scorer.py"
      - "tests/test_risk_brain.py"
      - "tests/test_risk_radar_review.py"
      - "tests/test_shock_deescalation.py"
      - "tests/test_w7_llm_determinism.py"
      # unrun-neuralweb-cortex
      - "tests/test_build_active_build_map.py"
      - "tests/test_build_cycle_pattern_state.py"
      - "tests/test_factor_contradictions.py"
      - "tests/test_nw_consumers_w3.py"
      - "tests/test_nw_health_escalation.py"
      - "tests/test_options_nw_wiring.py"
      - "tests/test_til_nw_citizenship.py"
      - "tests/test_world_state_special_sits.py"
      - "tests/test_world_state_stage_analysis.py"
      - "scripts/build_active_build_map.py"
      - "scripts/build_cycle_pattern_state.py"
      - "scripts/build_factor_panel.py"
      - "scripts/build_sector_central.py"
      - "scripts/check_nw_health_escalation.py"
      - "scripts/check_synapse_registry.py"
      # unrun-vector-baskets
      - "tests/test_basket_turn_grading.py"
      - "tests/test_basket_turn_watch.py"
      - "tests/test_btc_deferred_legs.py"
      - "tests/test_btc_overrides.py"
      - "tests/test_btc_reentry.py"
      - "tests/test_build_basket_pulse.py"
      - "tests/test_heal_basket_turn_ledger.py"
      - "tests/test_signal_surface_bugfixes.py"
      - "tests/test_spvector_page.py"
      - "tests/test_trial_log_refresh.py"
      - "tests/test_validation_effective_t.py"
      - "tests/test_vector_conviction.py"
      # Forward-ledger calendar-asof audit 2026-08-05 (#4568 pattern): three
      # clock-stamped ledger writers converted to data-plane session stamps.
      # test_contagion_links.py and test_stage_analysis.py were enumerated in NO
      # workflow before this (audit_unrun_tests.py P4 — unrun, writes a data/
      # ledger), so an edit to either engine produced no check that could see a
      # failure; test_weinstein_stage.py is the classifier the stage ledger reads
      # and was dark for the same reason.  The engines themselves are already
      # reached by the broad "engine/**" trigger.
      - "tests/test_contagion_links.py"
      - "tests/test_heal_contagion_links_ledger.py"
      - "tests/test_stage_analysis.py"
      - "tests/test_heal_stage_forward_ledger.py"
      - "tests/test_weinstein_stage.py"
      # coiled/mtf_upturn absolute session anchor (era coiled-mtf-abs-session-
      # 2026-08-06, job coiled-mtf-anchor-era): the engines are reached by the
      # broad "engine/**" trigger; these entries are the OTHER half — a PR
      # editing ONLY a suite must still start the pack that executes it
      # (test_coiled.py was previously named by no run step AND no path).
      - "tests/test_coiled.py"
      - "tests/test_coiled_mtf_anchor_invariance.py"
      - "scripts/heal_contagion_links_ledger.py"
      - "scripts/heal_stage_forward_ledger.py"
      # The nightly stage builder invokes the heal above; it is the only lane
      # permitted to write this ledger (.gitignore:574), so an edit to it must
      # start the pack that executes the stage suites.
      - "scripts/build_stage_analysis.py"
      # basket-turn session-stamp hygiene (forward-ledger audit 2026-08-05): the
      # W9 grading pack's engines + heal were enumerated in NO workflow before
      # this — a PR touching them fired no run that could see a failure.
      - "engine/basket_turn_cohort.py"
      - "engine/tape_disagreement.py"
      - "scripts/build_basket_turn_grading.py"
      - "scripts/heal_basket_turn_ledger.py"
      # unrun-picks-boards
      - "tests/test_b3_forex_consumer.py"
      - "tests/test_board_contradictions.py"
      - "tests/test_bottom_ledger.py"
      - "tests/test_china_board_rank.py"
      - "tests/test_china_continuation_watch.py"
      - "tests/test_china_prophet_shadow.py"
      - "tests/test_cn_board_unified_grid.py"
      - "tests/test_cn_prophet_candidates_schema.py"
      - "tests/test_cn_track_ledger_eras.py"
      - "tests/test_pick_lab_book.py"
      - "tests/test_pick_lab_cn_runner.py"
      - "tests/test_pick_lab_render.py"
      - "tests/test_pick_lab_runner.py"
      - "tests/test_prophet_card_shared.py"
      - "tests/test_prophet_board_read.py"
      - "tests/test_prophet_verb_ink_contrast.py"
      - "tests/test_stock_board_v2.py"
      - "tests/test_track_ledger_emitters.py"
      - "tests/test_us_board_fail_closed_freshness.py"
      - "tests/test_us_board_lanes.py"
      - "tests/test_us_board_priority_ui.py"
      - "tests/test_us_board_rank.py"
      - "tests/test_us_board_w3_evidence.py"
      - "tests/test_us_context_vector.py"
      - "tests/test_us_context_vector_payload_containment.py"
      - "scripts/build_china_pick_lab.py"
      - "scripts/build_pick_lab.py"
      - "scripts/build_stock_board_v2.py"
      - "scripts/grade_bottom_calls.py"
      - "engine/us_board_rank.py"
      # The CN candidate store is the SUBJECT of test_cn_prophet_candidates_schema.py,
      # so a nightly append (or a hand rewrite like the 2026-08-04 stamp_date rename)
      # that reintroduced the pre-rename column must be able to start this workflow.
      # The producer engine/china_prophet_shadow.py is already covered by engine/**.
      - "data/china_prophet_rank/**"
      # prophet-us-context-and-grades (PROPHET US §W7)
      - "tests/test_us_prophet_grades.py"
      - "tests/test_us_context_vector.py"
      - "tests/test_us_context_vector_payload_containment.py"
      - "tests/test_prophet_miss_audit.py"
      - "scripts/grade_us_prophet_candidates.py"
      # Archetype coverage pipeline (2026-08): the candidates-stamp heal script and
      # the suite pinning fac-less classification + store freshness + fill-null-only
      # heal semantics. The store subjects (engine/stock_fundamentals.py,
      # data/archetypes/) ride the engine/** and data/us_prophet_rank/** entries.
      - "scripts/heal_candidates_archetype.py"
      - "tests/test_heal_candidates_archetype.py"
      - "data/archetypes/**"
      # The nightly-wiring half of the same pack job (2026-08-06): the four modules moved
      # off the engine job's critical path into daily.yml's us_prophet_ledgers lane. Its
      # subjects (.github/workflows/daily.yml, config/dag.yml) already reach CI via
      # ".github/workflows/**" and the config/dag.yml entry above; this line covers the
      # test-only direction, so a re-pin still re-runs the job that carries it.
      - "tests/test_prophet_off_engine_lane.py"
      - "scripts/emit_prophet_doors.py"
      - "scripts/grade_prophet_doors.py"
      - "scripts/run_prophet_miss_audit.py"
      # The US candidate + grade stores are the SUBJECT of that suite (PIT keep-first,
      # monthly-part isolation, the frozen grade key), so a nightly append or a hand
      # rewrite of either store must be able to start this workflow — the same reason
      # data/china_prophet_rank/** is listed above. The producers engine/us_context_vector.py
      # and engine/us_prophet_grades.py are already covered by engine/**.
      - "data/us_prophet_rank/**"
      # unrun-prophet-learning-loop
      - "tests/test_prophet_postmortem.py"
      - "tests/test_exit_policy_study.py"
      - "tests/test_cn_prophet_audit.py"
      - "tests/test_htf_super_tiers.py"
      - "tests/test_prophet_stage_fusion.py"
      # P-B3 persistence-robust certification (#5729): the suite and its
      # research-resident instrument. research/** already starts this workflow;
      # these lines are the executable provenance so a test-only or instrument-
      # only edit still names the job that runs them.
      - "tests/test_pb3_persistence_robust_cert.py"
      - "research/cn_prophet_audit/pb3_persistence_robust_cert.py"
      - "engine/postmortem.py"
      - "scripts/prophet_postmortem.py"
      - "scripts/exit_policy_study.py"
      # The exit-policy calibration is pinned to a FROZEN input slice (2026-08-06):
      # recomputing from the live price caches only agreed with the shipped ledger while
      # one nightly run wrote both, and collect re-adjusts historical closes in place, so
      # no date pin can restore like-for-like. The slice and its regenerator are inputs to
      # the suite's claims exactly as the module under test is — an edit to either must be
      # able to start the job that checks them, or the pin is a guard its own change
      # cannot trigger (#3488 shape).
      - "tests/fixtures/exit_policy_vintage/**"
      - "scripts/build_exit_policy_vintage_fixture.py"
      # And the ARTIFACT the suite asserts about. #4827 rewrote this report and touched
      # nothing else, so the pack that checks it never started: the PR merged green having
      # never run its own guard, and main went red an hour later when #4763 pinned the
      # guard to a slice the freshly-rewritten report no longer matched. A committed
      # artifact under a committed-artifact guard is an input to the suite's claims exactly
      # as the module under test is (#3488 shape, same as the slice above).
      - "reports/exit-policy-horserace.md"
      # The COMMITTED CN board store + china_stocks price panel are the input grid
      # of test_cn_prophet_audit.py's frozen reproduction (REPRO_ASOF-truncated),
      # so a hand rewrite of either store — a re-adjustment sweep, an era edit —
      # must be able to start this workflow and hit the re-freeze protocol.
      # Engine subjects (cn_prophet_audit, confluence_tiers, signal_gate,
      # prophet_stage_fusion) ride the broad engine/** entry above.
      - "data/china_standout_track/**"
      - "data/china_stocks/**"
      # unrun-intl-collectors
      - "tests/test_action_board_lane_split.py"
      - "tests/test_china_alpha_w3c_infra.py"
      - "tests/test_china_fundamentals_collector.py"
      - "tests/test_content_stamp_freshness.py"
      - "tests/test_data_quality_audit.py"
      - "tests/test_build_earnings_scores_from_history.py"
      - "tests/test_earnings_qual.py"
      # The scorer's config carries BEHAVIOR, not just tuning: openai_compat's
      # per-rung max_chars is what keeps the local 4,096-token endpoint from
      # being handed a 24,000-char prompt (it answers 200-with-prose, not an
      # error, and every transcript falls through to a metered provider).
      # test_earnings_qual.py pins that bound, so an edit to the number alone
      # must be able to start the job that measures it.
      - "config/earnings_qual.yml"
      - "tests/test_earnings_seasons.py"
      - "tests/test_earnings_w5.py"
      - "tests/test_hk_fundamentals_collector.py"
      - "tests/test_hk_narrative.py"
      - "tests/test_hk_track_record_vm.py"
      - "tests/test_housing_hf.py"
      - "scripts/audit_earnings_freshness.py"
      - "scripts/build_earnings_scores_from_history.py"
      - "scripts/audit_macro.py"
      - "scripts/audit_prices.py"
      - "scripts/audit_universe.py"
      - "scripts/build_canada.py"
      - "scripts/build_china.py"
      - "scripts/build_hk.py"
      - "scripts/collect_nyfed_cmdi.py"
      - "scripts/collect_payems_vintages.py"
      - "scripts/collect_redfin_hf.py"
      - "scripts/collect_zori.py"
      - "scripts/fetch_earnings_scores.py"
      - "scripts/publish_earnings_r2.py"
      # Company Intelligence is an immutable R2 projection over the earnings
      # history and Terminal transcript marker. Its focused contract and
      # refresh tests run in the publish-r2-client pack; list every source and
      # test half so a workflow-only/test-only edit cannot evade that proof.
      #
      # The product has three additional release surfaces: the bounded API +
      # ticker dossier, the deterministic earnings wire, and the protected
      # receipt-bound context projection consumed by Brain/Prophet. Name their
      # subjects explicitly even where broad engine/templates globs also match.
      # Their focused tests run in unrun-publish-ops and the Prophet pack below.
      - "app/company_intelligence.py"
      - "app/earnings.py"
      - "tests/test_company_intelligence_api.py"
      - "tests/test_company_intelligence_dossier_js.py"
      - "site/assets/js/company-intelligence-dossier.js"
      - "templates/ticker.html.j2"
      - "engine/earnings_narrative/public_wire.py"
      - "engine/earnings_narrative/context_packets.py"
      - "engine/earnings_narrative/private_publication.py"
      - "engine/neuralweb/earnings_context_reader.py"
      - "engine/prophet_bridge.py"
      - "scripts/build_earnings_public_wire.py"
      - "scripts/publish_earnings_private_store.py"
      - "templates/earnings_wire/**"
      - "tests/test_earnings_public_wire.py"
      - "tests/test_earnings_api.py"
      - "tests/test_earnings_private_store.py"
      - "tests/test_prophet_bridge.py"
      - "engine/company_intelligence/**"
      # Company Intelligence golden corpus (Earnings/Company Event Suite R0-D): the
      # frozen benchmark Wave 1 is graded against. BOTH halves are listed on purpose —
      # the suites so a test-only edit re-runs them, AND the corpus itself (manifest +
      # fixtures) so an edit to the benchmark runs its own guard rather than shipping a
      # manifest that claims coverage it no longer has. The builder is already reachable
      # through the scripts/**/*.py entry below.
      - "research/company_intelligence/**"
      - "tests/fixtures/company_intelligence/**"
      - "tests/test_company_intelligence_golden_corpus.py"
      - "tests/test_company_intelligence_golden_corpus_replay.py"
      # Wave 1A event/document/span spine. The engine modules are already covered
      # by engine/company_intelligence/** above; these two lines exist so a
      # test-only edit still re-runs the suites that grade the spine against the
      # corpus, which tests/** does not do — it is not a catch-all in this repo.
      - "tests/test_company_intelligence_spine.py"
      - "tests/test_company_intelligence_spine_corpus.py"
      - "engine/company_theme_exposure/**"
      - "engine/earnings_transcript_intake.py"
      - "scripts/build_company_intelligence.py"
      - "scripts/publish_company_intelligence_r2.py"
      - "scripts/refresh_company_intelligence.py"
      - "scripts/build_company_theme_exposure.py"
      - "scripts/publish_company_theme_exposure_r2.py"
      - "data/baskets/membership.json"
      - "config/theme_crosswalk.yml"
      - "data/neuralweb/theme_state.json"
      - ".github/workflows/company-intelligence.yml"
      - "tests/test_company_intelligence_contracts.py"
      - "tests/test_company_intelligence_views.py"
      - "tests/test_company_intelligence_health.py"
      - "tests/test_publish_company_intelligence_r2.py"
      - "tests/test_company_intelligence_refresh.py"
      - "tests/test_company_theme_exposure.py"
      - "tests/test_publish_company_theme_exposure_r2.py"
      # Governed earnings story packets are an immutable R2 projection over the
      # exact earnings-evidence root.  Keep every policy, refresh, publisher and
      # focused-test path on the publish-r2-client lane so a test-only or
      # transport-only edit cannot silently bypass its provenance replay.
      - "config/earnings_story_promotion.yml"
      - "scripts/build_earnings_evidence_graph.py"
      - "scripts/publish_earnings_evidence_graph_r2.py"
      - "scripts/refresh_earnings_story_packets.py"
      - "scripts/publish_earnings_story_packets_r2.py"
      - "scripts/stage_earnings_story_press.py"
      - ".github/workflows/earnings-story-press-stage.yml"
      - "tests/test_earnings_story_promotion.py"
      - "tests/test_earnings_story_packets.py"
      - "tests/test_earnings_story_press_workflow.py"
      - "tests/test_earnings_story_press_admission.py"
      - "tests/test_earnings_story_press_ingress.py"
      - "tests/test_earnings_story_press_stage_workflow.py"
      - "tests/test_publish_earnings_story_packets_r2.py"
      - "tests/test_refresh_earnings_story_packets.py"
      # unrun-russell-breadth
      - "tests/test_russell_breadth.py"
      # unrun-brain-gateway
      - "tests/test_brain_gateway.py"
      - "tests/test_brain_gateway_quota_safety.py"
      # unrun-macro-panels
      # engine/equity_factors.py was STRICTLY DARK (2026-08-06): named by no `run:` step
      # and by no path here, so an edit to the module carrying `composite_rank` — the
      # board TIEBREAK read by build_stock_library.py / build_site.py — could not even
      # START ci.yml. Both halves listed on purpose (a guard the guarded change cannot
      # trigger is not a guard): the tripwire suite AND the two files it reads.
      - "tests/test_factor_universe_hold.py"
      - "engine/equity_factors.py"
      - "research/DO_NOT_REBUILD.md"
      - "research/FACTOR_UNIVERSE_WIDENING_CHARTER.md"
      - ".gitignore"
      - "tests/test_build_dispersion_regime.py"
      - "tests/test_build_factor_panel.py"
      - "tests/test_commodity_mtf_verdict_honesty.py"
      - "tests/test_congress_score.py"
      - "tests/test_context_risk.py"
      - "tests/test_cross_asset_context.py"
      - "tests/test_crossasset_flows.py"
      - "tests/test_financial_news.py"
      - "tests/test_forex.py"
      - "tests/test_forex_context_bus.py"
      - "tests/test_news_event_ledger.py"
      - "tests/test_phase_buildout.py"
      - "tests/test_policy_lever.py"
      - "tests/test_rates_command.py"
      - "tests/test_reflexivity_overlay.py"
      - "tests/test_scorecard_incremental.py"
      - "tests/test_us_sector_rotation_wiring.py"
      - "tests/test_validation.py"
      - "tests/test_whitehouse_build.py"
      - "scripts/build_commodities.py"
      - "scripts/build_congress.py"
      - "scripts/build_context_risk.py"
      - "scripts/build_crossasset.py"
      - "scripts/build_dispersion_regime.py"
      - "scripts/build_forex.py"
      - "scripts/build_macro_snapshot.py"
      - "scripts/build_news.py"
      - "scripts/build_policy_lever.py"
      - "scripts/build_rates_command.py"
      - "scripts/build_reflexivity_overlay.py"
      - "scripts/build_us_sector_rotation.py"
      - "scripts/build_whitehouse.py"
      - "scripts/calibrate_commodities.py"
      - "scripts/calibrate_forex.py"
      - "scripts/factor_ic_scorecard.py"
      # Cortex attention grader (W3, 2026-08-14). The script was reachable by NO path
      # pattern and named by NO `run:` step — strictly dark, which is how a direction=0
      # coercion and a substring-matching falsifier router survived in the nightly
      # A2 earn-in path. Both halves are wired here: the script so a grader-only edit
      # starts the pack, and the suite below so a suite-only edit does too.
      - "scripts/grade_cortex_attention.py"
      # Factor attention grader (sibling of cortex #5693). Named so a grader-only
      # edit starts the pack; the suite half is in the unrun-market-plumbing block.
      - "scripts/grade_factor_attention.py"
      - "scripts/grade_news_events.py"
      - "scripts/inject_wh_banner.py"
      # unrun-market-plumbing
      - "tests/test_attention_deterministic.py"
      - "tests/test_build_live_quotes.py"
      - "tests/test_clinicaltrials_themes.py"
      - "tests/test_event_priors.py"
      - "tests/test_foresight_ledger_lane_gates.py"
      - "tests/test_gex_archive_snapshot.py"
      - "tests/test_grade_cortex_attention.py"
      - "tests/test_grade_factor_attention.py"
      - "tests/test_grade_thematic.py"
      - "tests/test_live_delay_labeling.py"
      - "tests/test_options_flow_attention_reflex.py"
      - "tests/test_prophet_bridge.py"
      # Prophet US W1 (2026-08-03). The intake-repair suite and the order-invariance
      # suite both guard engine/prophet_bridge.py's sort key + origination path, which
      # the patterns above already reach — these two lines are the OTHER half, so a
      # suite-only edit (a new tie case, a new legacy-artifact shape) can start the
      # pack that executes it. test_prophet_bridge_order_invariance.py was strictly
      # dark before this: no `run:` step and no path pattern named it.
      - "tests/test_prophet_bridge_order_invariance.py"
      # Prophet anticipation has its own manifest lane. Keep suite-only isolation
      # repairs reachable through this workflow's outer path filter.
      - "tests/test_prophet_anticipation_intake.py"
      # R0-C earnings split-brain fence (2026-08-06). Both suites guard
      # engine/prophet_bridge.py, engine/prophet_stage_shadow.py and the new
      # engine/prophet_stage_inputs.py, all of which the broad "engine/**" entry above
      # already reaches — these lines are the OTHER half, so a suite-only edit can start
      # the pack that executes them. test_prophet_stage_shadow.py was strictly dark
      # before this (no `run:` step, no path pattern) while guarding the forward shadow
      # that arms the hold-leash's auto-demote clause.
      - "tests/test_prophet_earnings_split_brain.py"
      - "tests/test_prophet_stage_shadow.py"
      - "tests/test_prophet_stage_tilt.py"
      - "tests/test_prophet_w1_intake_repair.py"
      - "tests/test_tape_flow.py"
      - "tests/test_tape_flow_stamp.py"
      - "tests/test_tech_events_export.py"
      - "tests/test_techlab_p0_fixes.py"
      - "tests/test_thematic_state.py"
      - "tests/test_theme_adoption.py"
      - "tests/test_theme_clinical.py"
      - "tests/test_theme_options_witness.py"
      - "tests/test_theme_trade_flows.py"
      - "tests/test_thetadata_resolver.py"
      - "tests/test_validate_gex.py"
      # GEX modeling-core wiring (2026-07-29), into unrun-market-plumbing. The subject
      # modules are already covered — engine/** reaches the walls/surface model layer
      # and the greeks + engine math, collectors/** reaches the accrual collector, and
      # the scripts/*.py catch-all reaches the accrual runner — so these three entries
      # are the OTHER half: a suite-only edit (a new wall-shape case, a chain-parse
      # re-pin) must still be able to START ci.yml. All three suites existed before
      # this wiring and were reachable from no path pattern at all.
      - "tests/test_gex_engine.py"
      - "tests/test_gex_model.py"
      - "tests/test_polygon_gex.py"
      # polygon_gex session stamping (2026-08-06), same two-halves law: the subject
      # modules ride the scripts/*.py catch-all, so these entries are the OTHER half —
      # a suite-only or migration-only edit must still be able to START ci.yml.
      - "tests/test_polygon_gex_session_stamps.py"
      - "scripts/migrate_polygon_gex_session_stamps.py"
      # OIP E3 positioning persistence (2026-07-29), wired into unrun-market-plumbing.
      # engine/** already covers engine/positioning_persistence.py, engine/gex_state.py
      # and engine/market_gamma.py, and scripts/build_index_gex_history.py rides the
      # scripts/*.py catch-all — so these three entries are the OTHER half: a suite-only
      # edit (a re-pin of the back-compat key snapshot, a new truth-table case) must
      # still be able to START ci.yml. Both suites below existed BEFORE this wave and
      # were reachable from no path pattern at all.
      - "tests/test_positioning_persistence.py"
      - "tests/test_gex_state.py"
      - "tests/test_index_gex_history.py"
      # Unrun backfill 2026-08-03, same two-halves law: the GEX board state
      # merge, the gex.js IV-rank color guard, the session digest, and the
      # flow desk. Subjects under scripts/*.py + site/** + engine/**.
      - "tests/test_gex_board_state_merge.py"
      - "tests/test_gex_js_iv_rank_color.py"
      - "tests/test_session_digest.py"
      - "tests/test_flow_desk.py"
      # MSC R2.4 level-grade scorecards (2026-08-01), wired into unrun-market-plumbing.
      # scripts/*.py is already path-covered; this entry is the OTHER half so a
      # suite-only edit still starts ci.yml (two-halves law). R2.4b (2026-08-02)
      # adds the nulls/index-lane suite + the two back-wired WP-C1 grader suites.
      - "tests/test_level_grades_summary.py"
      - "tests/test_levels_grade_nulls.py"
      - "tests/test_levels_grade.py"
      - "tests/test_levels_track_record_rebase.py"
      # MSC R3.2/R3.3 gex_state index aggregator (2026-08-02), wired into
      # unrun-market-plumbing. lib/*.py + scripts/*.py are path-covered; this is the
      # OTHER half so a suite-only edit still starts ci.yml (two-halves law).
      - "tests/test_gex_state_index.py"
      # The launchd lane that produces data/index_gex_history off the render path. A
      # plist/runner-only edit changes the artifact's cadence and its push tail, so it
      # must reach the suite that pins the resolver routing and the R2 registration.
      - "ops/launchd/com.macro.indexgexhistory.plist"
      - "ops/launchd/run_index_gex_history.sh"
      - "scripts/backtest_event_priors.py"
      - "scripts/build_attention_deterministic.py"
      - "scripts/build_discovery_confluence.py"
      - "scripts/build_foresight.py"
      - "scripts/build_gex_board.py"
      - "scripts/build_live_quotes.py"
      - "scripts/build_options_flow_attention.py"
      - "scripts/build_special_situations.py"
      - "scripts/build_tape_flow.py"
      - "scripts/build_tape_flow_daily.py"
      - "scripts/build_tech_lab_data.py"
      - "scripts/build_thematic_state.py"
      - "scripts/grade_thematic.py"
      - "scripts/stamp_options_state.py"
      # unrun-publish-ops
      - "tests/test_backfill_qledger_us.py"
      - "tests/test_check_price_store_freshness.py"
      - "tests/test_demand_chain_phase0.py"
      - "tests/test_envelope_conformance.py"
      - "tests/test_importance_v0.py"
      - "tests/test_oracle_contract.py"
      - "tests/test_publish_r2.py"
      - "tests/test_radar_single_writer.py"
      - "tests/test_refresh_regime_if_stale.py"
      - "tests/test_seo_foundation.py"
      - "tests/test_signal_contracts_wiring.py"
      - "tests/test_signal_sanity.py"
      - "tests/test_earnings_worker_launchd.py"
      - "tests/test_earnings_worker_terminal.py"
      - "tests/test_ticker_dossier_render_lane.py"
      - "tests/test_ticker_pages.py"
      - "tests/test_ts_u1_session_date.py"
      - "tests/test_w4_pit_fixes.py"
      - "tests/test_w6b_push_ops.py"
      - "tests/test_watchlist_sentinel.py"
      - "tests/test_world_state_migrations.py"
      - "scripts/backfill_qledger_us.py"
      - "scripts/build_impulse.py"
      - "ops/bootstrap_earnings_worker.sh"
      - "ops/launchd/com.mastermind.earnings-worker.plist"
      - "ops/launchd/run_earnings_worker.sh"
      - "scripts/build_intelligence.py"
      - "scripts/build_radar_plus.py"
      - "scripts/build_sector_cycles.py"
      - "scripts/build_ticker_pages.py"
      - "scripts/check_price_store_freshness.py"
      - "scripts/healthcheck.py"
      - "scripts/notify.py"
      - "scripts/publish_r2.py"
      - "scripts/refresh_regime_if_stale.py"
      - "scripts/report_importance_duel.py"
      - "scripts/run_watchlist_sentinel.py"
      - "scripts/shadow_importance_v0.py"
      - "scripts/shadow_importance_v0_pit.py"
      - "scripts/signal_sanity.py"
      # unrun-inline-js-guard
      - "tests/test_check_inline_js.py"
      # unrun-dead-reference-guard
      - "tests/test_check_site_asset_refs.py"
      # unrun-import-hygiene — the ratchet AST-scans engine/, lib/, scripts/ and
      # research/ for import-time process-global state.  engine/** and lib/** are
      # already broad catch-alls above; scripts/ and research/ were only covered
      # file-by-file (443/968 and 1/87 .py files), so a new module-level silencer
      # in any of the rest could not start this workflow.  Both spellings per tree:
      # `dir/*.py` is top-level only under GitHub's filter syntax, `dir/**/*.py`
      # nested.  Scoped to .py because that is exactly what the ratchet reads.
      - "tests/test_no_module_level_logging_disable.py"
      - "scripts/*.py"
      - "scripts/**/*.py"
      - "research/*.py"
      - "research/**/*.py"
      # ── RED-on-arrival sweep, batch A: scoring engine (2026-07-27) ──────────
      # The first pass that measured how many of the 870 remaining unrun suites are
      # actually RED (24 of 870; see docs/UNRUN_TEST_CENSUS.md §"Red on arrival —
      # the P4/P5 sweep").  These four were red on main and are fixed in this PR.
      # Subjects all live under engine/**, already a broad catch-all above, so only
      # the test halves need entries here — tests/** is deliberately NOT broad.
      # unrun-scoring-engine
      - "tests/test_etf_pulse.py"
      - "tests/test_fix43_analyst_and_whitehouse.py"
      - "tests/test_sector_central.py"
      - "tests/test_spotlight.py"
      # ── W-A US bottoming-watch lane (2026-08-05) ────────────────────────────
      # The suite shipped DARK with #4599: `tests/test_us_act_now.py` was in no
      # pack, so it never ran once — while its CN sibling test_china_act_now.py
      # had been enumerated all along. It was found red on committed data (the
      # receipt asserted gold_miners was on the lane; the cycle engine graduated
      # it Trough→Recovery and the assertion silently became false). Now runs in
      # engine-render-guards beside the CN sibling. Subject engine/us_act_now.py
      # rides the engine/** catch-all above; tests/** is deliberately NOT broad,
      # so the test half needs this entry or a test-only edit stays dark.
      # engine-render-guards
      - "tests/test_us_act_now.py"
      # ── Breakdown event tape (BD Phase-0/0b) + AVOID-1 stamper (2026-07-27) ──
      # Subjects (scripts/research/dump_breakdown_events.py, bd_avoid1_stamper.py,
      # engine/grading.py) ride the scripts/** + engine/** catch-alls above, so
      # only the test halves need entries. All three run in the
      # breakdown-control-seed-determinism lane. #3792 wired the determinism file
      # into that lane without a path entry here, so a test-only edit could not
      # start the packs — same gap for the two event-tape suites it left unwired.
      # breakdown-control-seed-determinism
      - "tests/test_breakdown_seed_determinism.py"
      - "tests/test_breakdown_events.py"
      - "tests/test_bd_avoid1_stamper.py"
      # ── RED-on-arrival sweep, batch B: template surfaces (2026-07-27) ───────
      # Chip/copy suites that slice shipped markup out of templates/ and render it.
      # Their subjects are templates/** and scripts/*.py, both broad catch-alls
      # above, so again only the test halves need entries here.
      # unrun-template-chips
      - "tests/test_macro_header_compact.py"
      - "tests/test_okx_retail.py"
      - "tests/test_w4_us_board.py"
      - "tests/test_w5b_edge_chips.py"
      # ── RED-on-arrival sweep, batch C: PIT + frozen tape (2026-07-27) ───────
      # Subjects are engine/setup_tier.py and scripts/build_stock_library.py, both
      # already covered by the engine/** and scripts/*.py catch-alls above.  The
      # probe fixture is listed too: it IS the input under test, so re-freezing the
      # tape must retrigger the suite that reads it.
      # unrun-pit-probes
      - "tests/test_setup_tier.py"
      - "tests/test_stock_personality_wiring.py"
      - "tests/test_personality_relief_hazard.py"
      - "tests/test_personality_followon_gates.py"
      - "tests/fixtures/setup_tier_probe_closes.parquet"
      # ── The strictly-dark set reopened TWICE in one day (2026-07-27) ────────
      # #3776 landed the public signup email gate and #3797 the shared-quota hook,
      # each with its regression suite named by no workflow — arriving unrun the
      # way the census keeps predicting. Both reach their subject WITHOUT importing
      # it (one reads templates/onboard.js as text, one runs the hook as a
      # subprocess), so audit_unrun_tests.py — which derives subjects from imports —
      # reports subjects=[] and calls both UNTRIGGERABLE. That half is a false
      # positive: templates/** and .claude/** above already match their subjects.
      # The unrun half was real in both cases. Every half is pinned explicitly here
      # so neither a guard nor the file it inspects can drift out of coverage.
      # unrun-dark-guards
      - "tests/test_signup_email_validation.py"
      - "templates/onboard.js"
      - "tests/test_gh_quota_guard.py"
      - ".claude/hooks/gh_quota_guard.py"
      # Prophet US rescue lane (2026-08-14), run in the same unrun-dark-guards job.
      # Both halves are listed for the same reason as the pair above: the suite
      # reaches its subject through importlib from a path literal, so the
      # import-derived auditor reports subjects=[] and would call it untriggerable.
      - "tests/test_prophet_rescue.py"
      - "scripts/prophet_rescue.py"
      # PR-1 Prophet US permanence net (2026-08-27), same unrun-dark-guards family:
      # the new post-publish acceptance alarm (imported via importlib from
      # daily.yml's engine job, the same path-literal shape as the rescue lane
      # above) and the cross-cutting suite that pins the shared intake-identity
      # predicate across freshness_sentinel/check_nightly_liveness/prophet_rescue/
      # this script, the lane-latch acceptance exemption, and the heartbeat
      # write/grade round trip (see check_surface_freshness.py's own pair above
      # for the precedent this follows).
      - "scripts/prophet_board_acceptance.py"
      - "tests/test_prophet_surface_net.py"
      # CCR-R0 Control Room. These four suites run together in prelaunch-hardening:
      # operator-only identity, fixed-origin admin embed, real adapted Caddy order,
      # and the bounded atomic GitHub-source publisher. List every owned subject
      # explicitly so a source-only or test-only change reaches its own guard.
      # The six existing app/admin/Caddy subjects are already named elsewhere
      # in this trigger list; only the new publisher and suite paths belong here.
      - "scripts/build_project_active_build_map.py"
      - "scripts/publish_control_room_active_builds.py"
      - "tests/test_admin_control_room.py"
      - "tests/test_control_room_auth.py"
      - "tests/test_control_room_caddy.py"
      - "tests/test_publish_control_room_active_builds.py"
      # ── Public-render fastlane (2026-07-28) ─────────────────────────────────
      # test_public_render_fastlane pins the heavy/public render split and the
      # rendered pricing copy; it was named by no lane and rotted red when #3856
      # reframed the founding copy. Its subjects mostly ride catch-alls above
      # (scripts/*.py covers the builder, templates/** the plans template,
      # .github/workflows/** the split lanes, and config/plans.yml is listed for
      # the billing lanes) — only the fast-path shell script and the test half
      # need their own entries.
      # public-render-fastlane
      - "tests/test_public_render_fastlane.py"
      - "scripts/ci/public_render.sh"
      # ── Founding offer inventory + pacing (2026-07-28) ──────────────────────
      # test_billing_offers pins app/billing.py's offer/pacing logic (the #3856
      # enforced-withdrawal meter). The suite landed a day after the unrun-suite
      # census and was named by no lane. Both subjects already reach this
      # workflow by name (app/billing.py via the prelaunch-hardening cluster,
      # config/plans.yml via the billing-emails cluster) — only the test half
      # needs an entry.
      # billing-offers
      - "tests/test_billing_offers.py"
      # ── RED-on-arrival sweep, batch D: the P4/P5 re-drain (2026-08-06) ──────
      # A fresh census re-ran all 959 currently-unrun suites one process each. 951
      # pass; 8 are genuinely red on main, and these three are the cheap unclaimed
      # ones. Every subject rides a catch-all above — engine/** for levels_engine
      # and cycle_pattern, research/DO_NOT_REBUILD.md + scripts/*.py for the kill
      # registry and its compiler — so only the test halves need entries, and
      # tests/** is deliberately NOT broad.
      #
      # The registry pair is why the test half matters most here: the DNR guard's
      # subject is a MARKDOWN FILE that agents append to, and an append is precisely
      # the edit that must run it. That half was already listed (the compiled-registry
      # cluster above); this adds the guard's own half so a fix to the GUARD is
      # covered too — the "both halves on purpose" rule this list keeps re-learning.
      # capability-broker
      - "tests/test_dnr_registry_keys.py"
      # unrun-market-plumbing
      - "tests/test_levels_engine.py"
      # unrun-neuralweb-cortex
      - "tests/test_cycle_pattern_lake.py"
      # ── Stripe object bootstrap (tier rename, Phase 2) ──────────────────────
      # scripts/stripe_bootstrap.py reaches this workflow by NO other path, and
      # its renamed-product adoption is the step that decides whether a live
      # Product is reused or duplicated. Both halves listed by name (#3509).
      - "scripts/stripe_bootstrap.py"
      - "tests/test_stripe_bootstrap_offers.py"
      - "tests/test_stripe_bootstrap_products.py"
      # ── Breadth OHLC basis coherence (2026-08-06 split-basis incident) ──────
      # BOTH halves plus BOTH repair paths listed on purpose. The guard's whole
      # subject is that _high/_low can drift onto a different yfinance
      # adjustment basis than _closes while every existing scanner looks only at
      # the closes matrix — so an edit to either writer is exactly the edit that
      # must start this lane. collectors/breadth.py owns _merge_refreshed's
      # extras graft (the `t in w.columns` filter that can heal closes and leave
      # the extras poisoned); heal_breadth_split_seams.py rewrites the extras
      # only for closes-flagged tickers. Neither reaches this workflow by any
      # other pattern, and a trigger gap here is the same shape as the hole the
      # guard exists to close (check_ci_trigger_closure).
      - "scripts/check_ohlc_basis_coherence.py"
      - "tests/test_check_ohlc_basis_coherence.py"
      - "collectors/breadth.py"
      - "scripts/heal_breadth_split_seams.py"
      # ── Government Revenue Wave 9G prospective grader (2026-08-06) ──────────
      # Job: unrun-government-revenue-grader (.github/ci/legacy-jobs.yml, tail).
      # Deliberately a separate block from the Government Revenue cluster near
      # the top of this list: PR #4776 is concurrently editing that cluster, and
      # a grader whose own tests cannot start CI is exactly the darkness that PR
      # is fixing. engine/** already reaches the module itself; the test half and
      # the PREREGISTRATION are named because tests/** and research/** are not
      # catch-alls here. The prereg entry is load-bearing, not decorative — the
      # suite reads that document and fails if the registered horizons, kill
      # condition, or expiry drift from the code, so an edit to the registration
      # must re-run the guard that binds it.
      - "tests/test_government_revenue_candidate_grader.py"
      - "research/GOVERNMENT_REVENUE_CANDIDATE_GRADER_PREREG.md"
      # ── Trigger-gap heal (2026-08-07) ───────────────────────────────────────
      # Four suites already RUN by a pack read a document or config by path, with
      # no on.pull_request.paths entry reaching it — so an edit to exactly the
      # thing each guard binds ships without the guard running, and
      # check_ci_trigger_closure.py reds test_main_has_no_trigger_gap for EVERY
      # open PR until it is listed. Same load-bearing-prereg reasoning as the
      # block directly above; these are the fixes the guard itself prints.
      #   research/PROPHET_DOORS_PREREG.md            <- tests/test_prophet_doors.py:1400
      #   research/PROPHET_ARENA_REGISTRATION.md      <- tests/test_prophet_arena.py:1042
      #   research/SIGNAL_EPISODE_ATLAS_MASTERPLAN_BY_FABLE.md
      #                                               <- tests/test_event_atlas.py:360
      #   config/biocatalyst_outcome_family_policy.yml
      #                        <- tests/test_biocatalyst_m0a_outcome_family_policy.py:19
      - "research/PROPHET_DOORS_PREREG.md"
      - "research/PROPHET_ARENA_REGISTRATION.md"
      - "research/SIGNAL_EPISODE_ATLAS_MASTERPLAN_BY_FABLE.md"
      - "config/biocatalyst_outcome_family_policy.yml"
      # ── Price-basis census (2026-08-07) ─────────────────────────────────────
      # Job: price-basis-census (.github/ci/legacy-jobs.yml). Same two-halves law
      # as the blocks above: every module the census SCANS rides a catch-all
      # already (engine/** and scripts/*.py are exactly the two trees it walks),
      # so this entry is the OTHER half — the suite carries a frozen registry as
      # its own source, and editing that registry is the single most likely way
      # to make it wrong. A registry edit that cannot START ci.yml is the darkness
      # this whole job exists to end.
      - "tests/test_price_basis_graders.py"
      # ── Mastermind Data OS foundation (2026-08-12) ──────────────────────────
      # Job: dataos-foundation (.github/ci/legacy-jobs.yml). BOTH halves listed on
      # purpose, same law as every block above: the five suites AND the modules
      # they measure. lib/dataos/** rides the lib/** catch-all and
      # config/dataset_registry.yml rides config/**, but a catch-all is provenance
      # nobody can read — these entries are the executable record of what this job
      # binds, so the next person to narrow a catch-all can see what they would be
      # switching off. The registry file is the load-bearing one: it is the SUBJECT
      # of tests/test_dataos_registry.py (the committed registry must validate
      # clean and must not claim a store that does not exist), so a registry edit
      # that cannot START ci.yml is exactly the darkness the closure guard exists
      # to end.
      - "lib/dataos/**"
      - "config/dataset_registry.yml"
      - "tests/test_dataos_identity.py"
      - "tests/test_dataos_temporal.py"
      - "tests/test_dataos_price.py"
      - "tests/test_dataos_nulls.py"
      - "tests/test_dataos_registry.py"
      - "tests/test_dataos_quality.py"
      # Job: dataos-identity-seams (.github/ci/legacy-jobs.yml). DOS-1.2. Both halves
      # again: the suite AND the surfaces it measures. config/** and lib/** catch-alls
      # would cover most of these, but a catch-all is provenance by accident — an edit
      # to a surface must demonstrably be able to start the guard that reads it.
      - "config/identity_seams.yml"
      - "config/share_class_equiv.yml"
      - "tests/test_identity_seam_agreement.py"
      - "data/reference/**"
      # ── Data OS security master + vendor aliases (DOS-1.1, 2026-08-13) ──────
      # Job: house-law-registry (.github/ci/legacy-jobs.yml) — folded into that job
      # rather than dataos-foundation, which installs pytest+pyyaml only so that a
      # pandas import creeping into lib/dataos/ reds THERE first. Both halves again:
      # the suite, the producer it imports, and the ARTIFACTS it reads. The artifacts
      # are the load-bearing entry — the suite's whole subject is what those two
      # parquets answer on either side of 2026-01-14 and 2026-06-24, so a hand-edit or
      # a regenerated artifact that could not START ci.yml would be the darkness the
      # closure guard exists to end. data/** is NOT a catch-all in this filter.
      - "tests/test_dataos_security_master.py"
      - "scripts/build_security_master.py"
      - "data/reference/**"
      # Job: dislocation-p0-a1-blind-harvest (.github/ci/legacy-jobs.yml).
      # Two-halves: every source-only suite AND the primitives they import.
      - "tests/test_dislocation_p0_a1_blind_harvest.py"
      - "tests/test_dislocation_p0_a1r_*.py"
      - "tests/test_dislocation_p0_source_*.py"
      - "tests/test_dislocation_p0_s1f*.py"
      - "scripts/research/dislocation_p0_a1_harvest.py"
      - "scripts/research/dislocation_p0_a1_lib.py"
      - "scripts/research/dislocation_p0_a1r*.py"
      - "scripts/research/dislocation_p0_source_*.py"
      - "scripts/research/dislocation_p0_s1f*.py"
      - "collectors/edgar_forensics.py"
      - "collectors/sec_document_spine.py"
      - "engine/fundamental_forensics/models.py"
      - "engine/fundamental_forensics/sec_document_spine.py"
      - "lib/__init__.py"
      - "lib/config.py"
      - "research/dislocation_intelligence/**"
  workflow_dispatch:
    inputs:
      expected_sha:
        description: Exact main SHA the semantic baseline request observed
        required: false
        type: string

permissions:
  contents: read

concurrency:
  # A newer commit on the same PR makes every older result obsolete.  Cancelling
  # it here prevents rapid agent pushes from leaving full packed runs in queue.
  # Manual full-CI dispatches are also deduplicated per ref; several ship-loop
  # sessions may request the same main proof at once, but only the newest matters
  # — how that dedup survives WITHOUT killing a running proof is the 2026-08-09
  # block below.
  #
  # Proof-producing PR events share the candidate's PR-number slot, so a newer
  # synchronize/reopen can replace obsolete proof for that candidate. `closed`
  # is absent from the trigger above and therefore cannot occupy, replace, or
  # cancel this slot when lifecycle delivery is out of order.
  group: ci-${{ github.event.pull_request.number || github.ref }}
  # cancel-in-progress became EVENT-CONDITIONAL 2026-08-09, after the flat `true`
  # livelocked main's proof path fleet-wide (12 merge-blocked + 56 cap-deferred
  # pull requests; sweep run 31311549150 reported "0 main commit(s) classified …
  # main proof: NO clean name at unknown-sha, undated").
  #
  # THE LIVELOCK. This workflow has no `push` trigger, so main is proven ONLY by
  # a workflow_dispatch. Every main-ref dispatch lands in `ci-refs/heads/main`,
  # and with a flat `true` each new one KILLED the proof already running: run
  # 31309720615 (11:00:26Z) was cancelled at 11:44:49Z — 44 minutes in, near the
  # end of a 30-34 minute-per-pack run — by dispatch 31311537537, which was
  # itself cancelled four minutes later by 31311693575. No proof ever concludes,
  # so merge_on_green's base-inherited-red refresh stays closed, so more sessions
  # pin and re-fire the documented `gh workflow run ci.yml --ref main` lever, so
  # the next proof dies too. The recovery lever WAS the disease.
  #
  # The fix keeps both behaviours because they belong to different events:
  #   pull_request  -> true.  A newer proof-producing event obsoletes the older
  #                    packs; cancelling them is pure savings. Closed events do
  #                    not trigger this workflow and never enter the group.
  #   workflow_dispatch -> false.  A dispatch is an ORDER FOR EVIDENCE. Evidence
  #                    that is 44 minutes old is nearly evidence; discarding it
  #                    for an identical run that starts from zero is never right.
  # Dedup is NOT lost: GitHub allows one PENDING run per group and replaces it
  # regardless of this flag, so N racing dispatches still collapse to "the one
  # running + the newest queued". Only the kill is gone. Progress is therefore
  # guaranteed — the same argument that flipped integration-baseline.yml to
  # `false` on 2026-08-07, and the same shape as the sweeper's own anti-stampede
  # bound (`MAIN_BASELINE_MIN_INTERVAL_MINUTES`, scripts/merge_on_green.py).
  #
  # Note the group expression partitions by event type: a pull_request run always
  # has `pull_request.number`, a dispatch never does, so `ci-refs/heads/main`
  # holds dispatches ONLY. Every run in a given group evaluates this expression
  # to the same value, which is why the fix does not depend on whether GitHub
  # reads the flag from the arriving run or the in-progress one.
  cancel-in-progress: ${{ github.event_name != 'workflow_dispatch' }}

jobs:
  # PLAN ONCE, EXECUTE MANY, ADJUDICATE ONCE. `ci-plan` is the sole selection and
  # partition authority. Its v2 artifact binds the exact tested tree, PR subject
  # head, tested base, changed-file digest, logical jobs, semantic proof IDs, and
  # execution-spec digests. Packs consume that artifact verbatim and emit raw
  # bounded observations; they never recompute the proof universe or declare
  # causality. `ci-gate` reconciles complete accounting into the one stable
  # `ci.semantic_evidence.v1` verdict. Pack indices remain transport only.
  #
  # This is a proof-semantics boundary, not the CI SPEED wave. Pack count,
  # weighting, runner fleet, dependency caching, and existing scope selection are
  # deliberately unchanged here.
  ci-plan:
    name: ci-plan
    runs-on: ubuntu-latest
    # Planning loads one manifest and diffs one base; 30 minutes is a hang bound,
    # not a budget. `ci-pack` keeps its own 180.
    timeout-minutes: 30
    # NOTHING UNBOUNDED MAY BE AN OUTPUT (2026-08-14, run 31775693780). A job
    # output becomes an `env:` string in the consuming job, and execve caps a
    # single one at 131,072 bytes on Linux; the retired `changed_files` output
    # measured 350,264 bytes on PR #5578 and killed all twelve packs at launch
    # with "Argument list too long" before one test ran. Its replacement is the
    # 64-character digest below, which is also inside the hashed plan payload —
    # so the list travels as the `ci-changed-files` artifact while its identity
    # still rides this trusted channel and `--expect-plan-sha` still pins it.
    outputs:
      matrix: ${{ steps.plan.outputs.matrix }}
      has_work: ${{ steps.plan.outputs.has_work }}
      plan_sha: ${{ steps.plan.outputs.plan_sha }}
      reason: ${{ steps.plan.outputs.reason }}
      changed_files_sha256: ${{ steps.plan.outputs.changed_files_sha256 }}
      changed_files_count: ${{ steps.plan.outputs.changed_files_count }}
      plan_artifact_name: ${{ steps.identity.outputs.plan_artifact_name }}
      tested_tree_sha: ${{ steps.identity.outputs.tested_tree_sha }}
      subject_head_sha: ${{ steps.identity.outputs.subject_head_sha }}
      tested_base_sha: ${{ steps.identity.outputs.tested_base_sha }}
      semantic_role: ${{ steps.identity.outputs.semantic_role }}
    steps:
      # NO sparse-clear step and NO setup-node here, unlike `ci-pack` below. Both of
      # those exist for conditions this job cannot meet: the sparse-clear guards a
      # SHARED self-hosted workspace, and this job is hosted-only (one `runs-on`, no
      # self-hosted branch), so it gets a fresh VM and can never inherit a sibling's
      # `core.sparseCheckout`; and setup-node exists for the legacy jobs' Node steps,
      # which planning never executes. Adding either would be cargo cult.
      - uses: actions/checkout@v4
        with:
          # Semantic evidence binds the exact tree this runner executes. A PR
          # run proves GitHub's synthetic merge commit, not a later API-resolved
          # branch tip, so make the event SHA explicit.
          ref: ${{ github.sha }}
          # `fetch-depth: 0` is REQUIRED, not an optimisation. The planner derives
          # the tested base from parent 1 of this exact synthetic merge commit, and
          # a shallow clone may not contain that commit — the diff would fail, the planner would widen to the full
          # suite by law, and the plan would be meaningless on every PR. The
          # blob:none filter keeps full history cheap. Packs do NOT repeat this
          # fetch: they download the `ci-changed-files` artifact and
          # shallow-checkout.
          filter: blob:none
          fetch-depth: 0
          # W3 working-tree containment. The planner's mechanical census reads
          # source/config/test bytes outside these four generated-heavy trees;
          # omitted tracked-path existence is supplied separately by the exact
          # tested-tree inventory below. Unknown/new top-level trees remain
          # included, so profile drift widens cost rather than narrowing proof.
          sparse-checkout: |
            /*
            !/data/
            !/site/
            !/mockups/
            !/verify_shots/
          sparse-checkout-cone-mode: false
      - uses: actions/setup-python@v5
        with:
          # PINNED to an exact patch release, not the floating "3.12".
          #
          # engine/capital_structure/document_terms.py seals its parser behind
          # _PARSER_V1_1_0_RUNTIME_ALLOWLIST, whose key includes sys.version_info
          # AND the SHA-256 of specific stdlib source files. A CPython patch bump
          # therefore produces a fingerprint the allowlist does not carry, and the
          # parser fails closed with "document-term parser runtime fingerprint is
          # not released" — 22 tests in tests/test_capital_structure_document_terms.py
          # at once, on every PR whose pack happens to include
          # capital-structure-intelligence.
          #
          # That is exactly what happened on 2026-08-19: the hosted tool cache moved
          # 3.12.13 -> 3.12.14, `python-version: "3.12"` resolved to
          # /opt/hostedtoolcache/Python/3.12.14/x64, and ci-pack-8 went red on two
          # independent PR heads (#5737 entry-radar mockups, #5903 prophet-fusion
          # test pins) with an identical failure that neither PR's files could
          # possibly cause.
          #
          # The self-hosted lanes never broke, because they run Homebrew CPython
          # 3.12.13 — which IS released. So the floating pin was making hosted CI
          # test a runtime PRODUCTION DOES NOT USE, and the seal correctly refused
          # it. Pinning restores CI/production parity; it does not weaken the seal.
          #
          # To move this pin, first add the target release to the allowlist in
          # engine/capital_structure/document_terms.py (a review act owned by the
          # Capital Structure Intelligence lane, with provenance recorded the way
          # the existing 3.12.13 entry records its actions/python-versions archive
          # SHA-256), THEN bump here. tests/test_ci_pack.py enforces that order.
          python-version: "3.12.13"
      - id: identity
        name: bind the exact tested tree and base
        shell: bash
        env:
          EVENT_NAME: ${{ github.event_name }}
          EVENT_HEAD_SHA: ${{ github.event.pull_request.head.sha || github.sha }}
          EXPECTED_DISPATCH_SHA: ${{ inputs.expected_sha || '' }}
        run: |
          set -euo pipefail
          tested_tree_sha=$(git rev-parse HEAD)
          if [ "$tested_tree_sha" != "$GITHUB_SHA" ]; then
            echo "::error title=ci-tested-tree::checkout $tested_tree_sha does not match event SHA $GITHUB_SHA"
            exit 1
          fi
          if [ "$EVENT_NAME" = "workflow_dispatch" ] && [ -n "$EXPECTED_DISPATCH_SHA" ]; then
            if ! [[ "$EXPECTED_DISPATCH_SHA" =~ ^[0-9a-f]{40}$ ]] || [ "$GITHUB_SHA" != "$EXPECTED_DISPATCH_SHA" ]; then
              echo "::error title=ci-tested-tree::dispatch expected $EXPECTED_DISPATCH_SHA but main resolved to $GITHUB_SHA"
              exit 1
            fi
          fi
          if [ "$EVENT_NAME" = "pull_request" ]; then
            read -r merge_sha tested_base_sha subject_head_sha extra < <(git rev-list --parents -n 1 "$GITHUB_SHA")
            if [ "$merge_sha" != "$GITHUB_SHA" ] || [ -z "${tested_base_sha:-}" ] || [ -z "${subject_head_sha:-}" ] || [ -n "${extra:-}" ]; then
              echo "::error title=ci-tested-base::PR event SHA is not an exact two-parent merge commit"
              exit 1
            fi
            if [ "$subject_head_sha" != "$EVENT_HEAD_SHA" ]; then
              echo "::error title=ci-tested-base::merge parent 2 $subject_head_sha does not match event head $EVENT_HEAD_SHA"
              exit 1
            fi
            semantic_role=pr_head
          else
            subject_head_sha=$GITHUB_SHA
            tested_base_sha=$GITHUB_SHA
            semantic_role=main
          fi
          {
            echo "tested_tree_sha=$tested_tree_sha"
            echo "subject_head_sha=$subject_head_sha"
            echo "tested_base_sha=$tested_base_sha"
            echo "semantic_role=$semantic_role"
            echo "plan_artifact_name=ci-semantic-plan-${GITHUB_RUN_ID}-${GITHUB_RUN_ATTEMPT}"
          } >> "$GITHUB_OUTPUT"
      - name: install isolated pack runner
        run: |
          python -m venv "$RUNNER_TEMP/ci-plan-runner"
          "$RUNNER_TEMP/ci-plan-runner/bin/pip" install pyyaml
      - name: bind exact tested-tree path inventory
        run: >-
          "$RUNNER_TEMP/ci-plan-runner/bin/python" scripts/ci_scope_dependencies.py
          --write-tracked-paths "$RUNNER_TEMP/ci-tracked-paths/tracked-paths.v1"
          --tested-tree-sha "${{ steps.identity.outputs.tested_tree_sha }}"
      # Same `env`-built, UNQUOTED shell-argument idiom as the pack step below, and
      # for the same two reasons: a folded `>-` scalar tolerates no `#` and no
      # varying indent, and an empty `CI_SCOPE_ARG` must word-split away to nothing
      # so main's baseline and every workflow_dispatch stay full-suite.
      #
      # The authoritative tested base is parent 1 of the exact synthetic merge
      # commit checked out above. The event's base SHA is trigger provenance and may
      # be stale by runner pickup; a branch name is mutable and is never substituted.
      # Parent 2 must equal the signed event head before this value is published.
      - id: plan
        name: plan the CI packs once
        env:
          # Scope mode governs WHICH jobs are eligible; matrix mode governs only
          # which pack indices are EMITTED. They are deliberately separate switches.
          # Both are active unless the matching repository variable is exactly
          # `off` (2026-08-13). A leftover `shadow` from the first deployment
          # would otherwise keep copying every main-red onto every pull request
          # — a nightly data pin in one legacy job failed all twelve packs on
          # every armed head. `off` is each one's emergency full-suite kill
          # switch. Main's workflow_dispatch still omits --changed-from and
          # therefore still audits the complete manifest.
          CI_SCOPE_MODE: ${{ vars.CI_SCOPE_MODE == 'off' && 'off' || 'active' }}
          CI_DYNAMIC_MATRIX_MODE: ${{ vars.CI_DYNAMIC_MATRIX_MODE == 'off' && 'off' || 'active' }}
          CI_SCOPE_ARG: ${{ github.event_name == 'pull_request' && format('--changed-from {0}', steps.identity.outputs.tested_base_sha) || '' }}
          TESTED_TREE_SHA: ${{ steps.identity.outputs.tested_tree_sha }}
          SUBJECT_HEAD_SHA: ${{ steps.identity.outputs.subject_head_sha }}
          TESTED_BASE_SHA: ${{ steps.identity.outputs.tested_base_sha }}
          SEMANTIC_ROLE: ${{ steps.identity.outputs.semantic_role }}
        run: >-
          "$RUNNER_TEMP/ci-plan-runner/bin/python" scripts/run_ci_pack.py
          --workflow .github/ci/legacy-jobs.yml
          --gate code
          --pack-count 12
          --plan-only
          --github-output "$GITHUB_OUTPUT"
          --emit-plan-json "$RUNNER_TEMP/ci-semantic-plan/ci-plan.json"
          --emit-changed-files "$RUNNER_TEMP/ci-changed-files/changed-files.json"
          --tracked-paths-file "$RUNNER_TEMP/ci-tracked-paths/tracked-paths.v1"
          --workflow-run-id "$GITHUB_RUN_ID"
          --workflow-name ci
          --event "$GITHUB_EVENT_NAME"
          --role "$SEMANTIC_ROLE"
          --tested-tree-sha "$TESTED_TREE_SHA"
          --subject-head-sha "$SUBJECT_HEAD_SHA"
          --base-sha "$TESTED_BASE_SHA"
          $CI_SCOPE_ARG
      # THE CHANGED-FILE LIST'S TRANSPORT (2026-08-14, run 31775693780). An
      # artifact rather than a job output, because a job output becomes an `env:`
      # string in the consuming job and execve caps a single one at 131,072
      # bytes; this list is unbounded by nature — 350,264 bytes on PR #5578,
      # which killed all twelve packs at launch before one test ran.
      #
      # UNCONDITIONAL, deliberately. `run_ci_pack.py` writes the file on BOTH
      # exits of the plan step: the resolved list on success, the token `null` on
      # the planner-fallback path where it swallows its exception and exits 0.
      # Packs launch only when this whole job succeeded, so "a pack is running"
      # implies "this artifact exists" — which is what lets the pack-side
      # download be a hard failure instead of a silent widen.
      #
      # `if-no-files-found: error` is the fail-closed half: a missing file must
      # red the planner, not hand twelve packs an empty download and a scope
      # nobody chose. 14 days outlives any armed PR's useful life.
      - name: publish the changed-file list
        uses: actions/upload-artifact@v4
        with:
          name: ci-changed-files
          path: ${{ runner.temp }}/ci-changed-files/changed-files.json
          if-no-files-found: error
          retention-days: 14
      - name: publish the authoritative semantic plan
        if: steps.plan.outputs.plan_sha != ''
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        with:
          name: ${{ steps.identity.outputs.plan_artifact_name }}
          path: ${{ runner.temp }}/ci-semantic-plan/ci-plan.json
          if-no-files-found: error
          retention-days: 14
  # P3B-B: the expensive same-repository PR execution is defined by protected
  # main, not by candidate-authored workflow bytes. This call accepts no identity
  # inputs or inherited secrets; the called workflow independently admits the
  # exact PR merge ref, derives the immutable candidate/base/plan, and caps the
  # isolated PC pool at three slots. Forks never enter this job.
  trusted-ci:
    name: trusted-ci
    needs: ci-plan
    if: needs.ci-plan.outputs.has_work == 'true' && github.event.pull_request.head.repo.full_name == github.repository
    uses: mastermindx-market-intelligence/macro/.github/workflows/trusted-ci-executor.yml@main
    permissions:
      contents: read
      pull-requests: read
  ci-pack:
    needs: [ci-plan, trusted-ci]
    # The `has_work` clause is the Wave B addition, and it is safe only because
    # `has_work: false` is an AFFIRMATIVE planner proof that no legacy job is
    # selected. Every uncertainty widens instead: a planner exception, an unknown
    # top-level path, a global invalidator, or a failed git diff all emit all twelve
    # indices with `has_work: true`. And a no-work PR is still PROVEN, because
    # `ci-gate` below publishes a real success on that path — the merge-on-green
    # sweeper requires an affirmative pass, since absence of red is not a pass
    # (#4779), so a PR whose only packs were skipped would otherwise read unproven
    # and never merge.
    if: >-
      always() && needs.ci-plan.result == 'success' &&
      needs.ci-plan.outputs.has_work == 'true' &&
      (github.event.pull_request.head.repo.full_name != github.repository ||
      needs.trusted-ci.result == 'success')
    # The logical jobs in .github/ci/legacy-jobs.yml remain the reviewable
    # source of truth, but GitHub sees only this matrix. Twelve pack runners
    # execute every legacy run step against a clean working tree, sharing one
    # checkout and reusing only identical dependency sets instead of
    # provisioning more than one hundred separate VMs/check records.
    #
    # Four, up from two (operator 2026-07-28): two packs put ~35 minutes
    # between a PR opening and a verdict, and the operator + sessions merge in
    # 0-8 — the whole merge-outruns-CI class lives in that gap. The greedy
    # partition balances 4x619 weight against 2x1238 (run_ci_pack.py
    # --validate-only), so wall-clock to green roughly halves; GitHub-hosted
    # pickup is 2-3 s per job, so two more runners cost queue nothing. These
    # Twelve, up from four (operator 2026-08-10): paid Enterprise capacity now
    # starts the whole hosted matrix within seconds, while four full packs still
    # took 40-55 minutes. Twelve keeps every legacy job gated and cuts the
    # measured balanced critical-path weight roughly in half; the remaining
    # floor is one deliberately indivisible render-guard lane. These names are
    # the intended required-checks set (ci-pack-0..11) for any future branch
    # protection; renaming or renumbering them must update that setting too.
    #
    # AMENDED Wave B (2026-08-11), because the last two sentences are no longer the
    # whole truth. The matrix is now the plan's, so a PR may legitimately publish a
    # SUBSET of ci-pack-0..11 — a required-checks rule naming all twelve would hang
    # forever on the packs the plan never launched. `ci-gate` (bottom of this file)
    # is the stable aggregate name for any future branch protection; the per-pack
    # names remain the useful diagnostics and are still what main's full-suite
    # workflow_dispatch baseline publishes, all twelve of them, unchanged. Nothing
    # about the twelve-way PARTITION moved: `--pack-count 12` is still passed here
    # and in `ci-plan`, and the same greedy balance produces the same packs.
    name: ci-pack-${{ matrix.pack }}
    # EVERY event runs on the hosted pool. The self-hosted detour is retired.
    #
    # History, because the reversal is the point: main's proof was moved to
    # `["self-hosted","render-linux"]` earlier on 2026-08-09 because it sat `queued`
    # 30+ minutes behind 133 queued runs and never started, while `pc-render-1..4`
    # idled. That starved run blocked the whole fleet — `merge_on_green.main_proof`
    # answers "is main green on ci-pack-N" from the newest CONCLUDED ci.yml run on
    # main, so with no fresh proof the base-inherited-red refresh cannot fire and every
    # armed pull request that inherited a since-healed red stays blocked.
    #
    # That fix was correct for its constraint, and the constraint is now GONE. The
    # repository moved to the MastermindX enterprise organization on 2026-08-09 and its
    # paid Enterprise plan now carries GitHub's 500-standard-hosted-job ceiling instead
    # of the 20-job Free ceiling. The measured backlog that caused the starvation was
    # ~195 hosted jobs, so it now fits inside one wave; observed four-pack jobs begin
    # within the same second. There is no longer anything to escape.
    #
    # Routing back is strictly better than leaving it:
    #   * `render-linux` is FOUR runners shared with `render.yml`, `engine-render.yml`
    #     and, at the time, `merge-on-green.yml`. Main's four packs took the entire
    #     pool, starving both renders and the sweeper. The packs and sweeper now use
    #     hosted runners; all four self-hosted slots are back with render workloads.
    #   * The `max-parallel: 2` that bounded that damage is deleted with it — on hosted
    #     runners the hosted packs run concurrently again. With paid Enterprise
    #     capacity, twelve balanced packs reduce the measured 40-55 minute
    #     four-pack tail without competing with render workers.
    #   * One `runs-on` for every event removes the event-dependent routing entirely,
    #     so main's baseline and a pull request now prove the packs the SAME way.
    #
    # NOT the macstudio pool, unchanged: that is the render/nightly lane and is
    # routinely saturated (CLAUDE.md — render budget is law).
    runs-on: ubuntu-latest
    timeout-minutes: 180
    strategy:
      # Sibling packs must FINISH so their proofs survive a single red. A
      # pull_request fail-fast (2026-08-13) cancelled the other eleven the
      # moment one pack died; ci-gate then went red on `cancelled`/`failure`,
      # the sweeper had nothing to merge, and a heal had to rerun every pack
      # from zero. execute_pack already continues after a red INSIDE a pack
      # so one run reports every legacy failure; the matrix must do the same
      # BETWEEN packs. A real red remains present in the raw fragment and the
      # reconciled semantic evidence still blocks merge. workflow_dispatch was already
      # fail-fast:false. Do not add max-parallel (#5465).
      #
      # Literal boolean, not an expression. `fail-fast: "${{ ... }}"` evaluates
      # to the strings "true"/"false"; some Actions parsers treat any non-empty
      # string as true.
      fail-fast: false
      # NO `max-parallel`. It existed only to stop main's four packs from taking all
      # four `render-linux` runners and starving `merge-on-green.yml`, then co-resident
      # on that pool. With both control/proof lanes hosted there is no shared pool to
      # protect, and throttling would only make main's proof twice as slow (~26 min
      # unthrottled vs ~50 min at 2). Do not reintroduce one without a shared-pool
      # reason; the hosted ceiling is an ACCOUNT-wide limit that this key cannot help.
      #
      # The literal `pack: [0..11]` list that stood here until Wave B (2026-08-11) is
      # now the PLAN's, emitted by `ci-plan` as {"include": [{"pack": N}, ...]} over
      # the non-empty pack indices. `matrix.pack` keeps exactly the meaning the
      # literal gave it — the index handed to `--pack-index` below — and the
      # partition arithmetic is unchanged because `--pack-count 12` is still passed
      # here and in `ci-plan`. Under `CI_DYNAMIC_MATRIX_MODE=active` the plan emits
      # only the non-empty pack indices; `shadow`/`off` still launch all twelve.
      matrix: ${{ fromJSON(needs.ci-plan.outputs.matrix) }}
    steps:
      # A self-hosted runner REUSES its workspace between jobs, and a sibling job that
      # checked out sparsely leaves `core.sparseCheckout=true` behind in the SAME
      # directory. `actions/checkout@v4` does not clear that when its own
      # `sparse-checkout` input is absent, so the next job silently gets a partial tree.
      #
      # Measured 2026-08-09, the first main baseline on this pool: checkout, setup-python
      # and setup-node all reported SUCCESS and the pack died 2-3 minutes later on
      # `scripts/run_ci_pack.py: [Errno 2] No such file or directory`. The sweeper
      # (#5095) had moved to these same runners and checks out exactly three paths for
      # speed, so `scripts/` was simply not materialised. A green "Set up job" plus a
      # missing source file is the signature of an inherited sparse checkout, not of a
      # broken runner or a bad ref.
      #
      # DO NOT DELETE THE `config --unset-all` LINE BECAUSE CHECKOUT "ALREADY DISABLES
      # SPARSE". It does — run 31304933108 shows checkout@v4 running
      # `git sparse-checkout disable` itself — and the workspace was partial anyway.
      # `disable` clears the WORKTREE-scoped flag; checkout then unsets
      # `extensions.worktreeConfig`, and that makes the repo-scoped `core.sparseCheckout`
      # visible, and effective, again. The config unset is the half that ends the outage.
      # The `disable` in front of it is what re-materialises the missing paths for a job
      # whose own checkout is skipped or conditional (render.yml) — keep both.
      #
      # Hosted runners get a fresh VM per job and cannot hit this, hence the guard.
      - name: clear any sparse checkout a sibling job left in this shared workspace
        if: runner.environment != 'github-hosted'
        shell: bash
        run: |
          if [ -d "${{ github.workspace }}/.git" ]; then
            git -C "${{ github.workspace }}" sparse-checkout disable || echo "::warning title=sparse-clear::sparse-checkout disable failed — this workspace may still hold a partial tree"
            # `--unset-all` exits 5 when there is nothing to unset: that is the normal
            # clean case and must never fail the job. Exit 0 means this workspace really
            # WAS poisoned, which is worth one line in the run summary.
            rc=0
            git -C "${{ github.workspace }}" config --unset-all core.sparseCheckout || rc=$?
            if [ "$rc" = 0 ]; then
              echo "::notice title=sparse-clear::cleared an inherited core.sparseCheckout in this shared workspace"
            elif [ "$rc" != 5 ]; then
              echo "::warning title=sparse-clear::core.sparseCheckout unset exited $rc — this workspace may still be sparse"
            fi
          fi
      - uses: actions/checkout@v4
        if: github.event.pull_request.head.repo.full_name != github.repository
        with:
          ref: ${{ github.sha }}
          # Packs consume ci-plan's changed-file list and only need the current
          # tree. fetch-depth: 0 on all twelve packs at once was the 56-minute
          # wall-clock: measured PR #5550 / run 31729769728, ci-pack-1 spent
          # 31 minutes in "Checking out the ref" (18:22:54Z–18:54:04Z) while
          # ci-plan, fetching the same history alone, finished in 7 minutes.
          # A missed diff here would fail-safe-widen every PR; the
          # `ci-changed-files` artifact downloaded below is the substitute, and an
          # unreadable or malformed handle still widens on the unpinned path.
          # (It rode a job output into this step's `env:` until 2026-08-14, when
          # 350,264 bytes of paths met execve's 131,072-byte MAX_ARG_STRLEN and
          # killed all twelve packs at launch — run 31775693780.)
          filter: blob:none
          fetch-depth: 1
      - uses: actions/setup-python@v5
        if: github.event.pull_request.head.repo.full_name != github.repository
        with:
          # PINNED to an exact patch release, not the floating "3.12".
          #
          # engine/capital_structure/document_terms.py seals its parser behind
          # _PARSER_V1_1_0_RUNTIME_ALLOWLIST, whose key includes sys.version_info
          # AND the SHA-256 of specific stdlib source files. A CPython patch bump
          # therefore produces a fingerprint the allowlist does not carry, and the
          # parser fails closed with "document-term parser runtime fingerprint is
          # not released" — 22 tests in tests/test_capital_structure_document_terms.py
          # at once, on every PR whose pack happens to include
          # capital-structure-intelligence.
          #
          # That is exactly what happened on 2026-08-19: the hosted tool cache moved
          # 3.12.13 -> 3.12.14, `python-version: "3.12"` resolved to
          # /opt/hostedtoolcache/Python/3.12.14/x64, and ci-pack-8 went red on two
          # independent PR heads (#5737 entry-radar mockups, #5903 prophet-fusion
          # test pins) with an identical failure that neither PR's files could
          # possibly cause.
          #
          # The self-hosted lanes never broke, because they run Homebrew CPython
          # 3.12.13 — which IS released. So the floating pin was making hosted CI
          # test a runtime PRODUCTION DOES NOT USE, and the seal correctly refused
          # it. Pinning restores CI/production parity; it does not weaken the seal.
          #
          # To move this pin, first add the target release to the allowlist in
          # engine/capital_structure/document_terms.py (a review act owned by the
          # Capital Structure Intelligence lane, with provenance recorded the way
          # the existing 3.12.13 entry records its actions/python-versions archive
          # SHA-256), THEN bump here. tests/test_ci_pack.py enforces that order.
          python-version: "3.12.13"
      - uses: actions/setup-node@v4
        if: github.event.pull_request.head.repo.full_name != github.repository
        with:
          node-version: "20"
      - name: install isolated pack runner
        if: github.event.pull_request.head.repo.full_name != github.repository
        run: |
          python -m venv "$RUNNER_TEMP/ci-pack-runner"
          "$RUNNER_TEMP/ci-pack-runner/bin/pip" install pyyaml
      - name: download the authoritative semantic plan
        if: github.event.pull_request.head.repo.full_name != github.repository && needs.ci-plan.outputs.plan_sha != ''
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093
        with:
          name: ${{ needs.ci-plan.outputs.plan_artifact_name }}
          path: ${{ runner.temp }}/ci-semantic-plan
      # THE CHANGED-FILE LIST ARRIVES AS A FILE (2026-08-14, run 31775693780).
      # ci-plan uploads it; this step downloads it and the next exports only the
      # PATH. Both halves of that sentence are the repair: the list is what the
      # legacy guards scope themselves to (conflict-markers, self-mod-fence,
      # run_ci_pack's own selection), and it is also the one value that grows
      # with the PR — 350,264 bytes on PR #5578 against execve's 131,072-byte
      # per-string cap, which is why every pack died before its first test.
      #
      # A FAILED DOWNLOAD FAILS THIS JOB, deliberately. ci-plan uploads the list
      # unconditionally and packs launch only when ci-plan succeeded, so a pack
      # running without this artifact is not a widen to reason about — it is
      # evidence that the control plane is broken. And a pack that somehow got
      # here with the wrong bytes still cannot run: `changed_files_sha256` is
      # inside the hashed plan payload, so it would recompute a different plan
      # sha and `--expect-plan-sha` refuses below.
      - name: download the planner's changed-file list
        if: github.event.pull_request.head.repo.full_name != github.repository
        uses: actions/download-artifact@v4
        with:
          name: ci-changed-files
          path: ${{ runner.temp }}/ci-changed-files
      - name: publish the changed-file handle
        if: github.event.pull_request.head.repo.full_name != github.repository
        run: echo "CI_CHANGED_FILES_FILE=$RUNNER_TEMP/ci-changed-files/changed-files.json" >> "$GITHUB_ENV"
      # Pack index is transport only. Each runner consumes the one hashed plan,
      # verifies the exact tree/head/base identities, and emits raw semantic facts.
      # Causality is computed later by ci-gate; a pack never labels itself inherited.
      - name: validate and run legacy CI pack
        if: github.event.pull_request.head.repo.full_name != github.repository && needs.ci-plan.outputs.plan_sha != ''
        id: execute_semantic_pack
        env:
          CI_BASE_REF: ${{ github.base_ref || 'main' }}
          CI_HEAD_REF: ${{ github.head_ref || github.ref_name }}
          EXPECTED_PLAN_SHA: ${{ needs.ci-plan.outputs.plan_sha }}
          TESTED_TREE_SHA: ${{ needs.ci-plan.outputs.tested_tree_sha }}
          SUBJECT_HEAD_SHA: ${{ needs.ci-plan.outputs.subject_head_sha }}
          TESTED_BASE_SHA: ${{ needs.ci-plan.outputs.tested_base_sha }}
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p "$RUNNER_TEMP/ci-semantic-fragments"
          "$RUNNER_TEMP/ci-pack-runner/bin/python" scripts/run_ci_pack.py \
            --workflow .github/ci/legacy-jobs.yml \
            --gate code \
            --plan-json "$RUNNER_TEMP/ci-semantic-plan/ci-plan.json" \
            --changed-files-file "$CI_CHANGED_FILES_FILE" \
            --expect-plan-sha "$EXPECTED_PLAN_SHA" \
            --expect-tested-tree-sha "$TESTED_TREE_SHA" \
            --expect-subject-head-sha "$SUBJECT_HEAD_SHA" \
            --expect-base-sha "$TESTED_BASE_SHA" \
            --pack-index ${{ matrix.pack }} \
            --pack-count 12 \
            --base-replay-budget-seconds 900 \
            --emit-semantic-fragment "$RUNNER_TEMP/ci-semantic-fragments/pack-${{ matrix.pack }}.json" \
            --execute
      # Planner uncertainty still widens to the full suite. It cannot mint
      # semantic authority because no exact plan exists, so ci-gate will block
      # even if these compatibility packs happen to pass.
      - name: fail-safe full suite when no authoritative plan was produced
        if: github.event.pull_request.head.repo.full_name != github.repository && needs.ci-plan.outputs.plan_sha == ''
        env:
          CI_BASE_REF: ${{ github.base_ref || 'main' }}
          CI_HEAD_REF: ${{ github.head_ref || github.ref_name }}
        run: >-
          "$RUNNER_TEMP/ci-pack-runner/bin/python" scripts/run_ci_pack.py
          --workflow .github/ci/legacy-jobs.yml
          --gate code
          --pack-index ${{ matrix.pack }}
          --pack-count 12
          --execute
      # The called main workflow already ran this exact pack on the PC pool. These
      # two hosted steps are deliberately tiny: they preserve the existing
      # ci-pack-N check and artifact contract while refusing any mismatch between
      # the hosted authoritative plan and the independently main-derived plan.
      - name: relay the trusted pack fragment under the existing check contract
        if: github.event.pull_request.head.repo.full_name == github.repository
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093
        with:
          name: trusted-ci-fragment-${{ matrix.pack }}
          path: ${{ runner.temp }}/trusted-ci-fragment
      - name: bind trusted execution to the hosted authoritative plan
        if: github.event.pull_request.head.repo.full_name == github.repository
        env:
          HOSTED_PLAN_SHA: ${{ needs.ci-plan.outputs.plan_sha }}
          TRUSTED_PLAN_SHA: ${{ needs.trusted-ci.outputs.plan_sha }}
          TRUSTED_FRAGMENT: ${{ runner.temp }}/trusted-ci-fragment/trusted-fragment.json
          RELAY_FRAGMENT: ${{ runner.temp }}/ci-semantic-fragments/pack-${{ matrix.pack }}.json
        shell: bash
        run: |
          set -euo pipefail
          python3 - "$TRUSTED_FRAGMENT" "$RELAY_FRAGMENT" "$HOSTED_PLAN_SHA" "$TRUSTED_PLAN_SHA" <<'PY'
          import json
          import shutil
          import sys
          from pathlib import Path

          source = Path(sys.argv[1])
          destination = Path(sys.argv[2])
          hosted_plan_sha = sys.argv[3]
          trusted_plan_sha = sys.argv[4]
          if not hosted_plan_sha or hosted_plan_sha != trusted_plan_sha:
              raise SystemExit(
                  "hosted/trusted plan mismatch: "
                  f"hosted={hosted_plan_sha!r} trusted={trusted_plan_sha!r}"
              )
          fragment = json.loads(source.read_text(encoding="utf-8"))
          if fragment.get("plan_sha256") != hosted_plan_sha:
              raise SystemExit("trusted fragment is not bound to the authoritative plan")
          destination.parent.mkdir(parents=True, exist_ok=True)
          shutil.copyfile(source, destination)
          PY
      - name: publish this pack's raw semantic fragment
        if: always() && needs.ci-plan.outputs.plan_sha != ''
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        with:
          name: ci-semantic-pack-${{ github.run_id }}-${{ matrix.pack }}
          path: ${{ runner.temp }}/ci-semantic-fragments/pack-${{ matrix.pack }}.json
          if-no-files-found: warn
          overwrite: true
          retention-days: 14
  # DIFFERENTIAL merge-train contract gate (2026-08-19). ci-pack's own path
  # scoping is exactly the blind spot this job exists to cover: a curated
  # `scope: exclusive` job's import closure, or the set of pytest suites no
  # workflow wires, are both CI-CONTRACT facts that a normal path-scoped pack
  # never re-derives unless the edit happens to land inside that job's own
  # declared paths (tests/test_ci_pack.py, scripts/audit_unrun_tests.py). Two
  # PRs (#5872, #5932) each widened a curated job's import closure without
  # widening its declared paths; neither PR's pack ran
  # test_curated_exclusive_scopes_cover_their_own_import_closure, both merged
  # green, and main went red 5+ hours later with 21 armed PRs baseline-blocked
  # behind it (research the incident under WS/PROPHET or grep press-wire for
  # 2026-08-19 ~12:00Z). This job re-derives both finding classes on every PR,
  # unconditionally of path scope, and reds ONLY when the PR's own tree
  # introduces a finding that the PR's base did not already carry.
  #
  # DIFFERENTIAL, NEVER ABSOLUTE, on purpose — see
  # scripts/check_contract_delta.py's module docstring for the full argument.
  # An absolute validator would re-jam the fleet at PR level the moment main
  # itself carries an inherited finding (main law: "Validator in an ALWAYS-ON
  # job = fleet control plane"), which is exactly the failure mode ci-gate's
  # own base-inherited-red carve-outs exist to avoid for every OTHER check.
  contract-delta:
    # PR events only. A push/workflow_dispatch main-proof run has no
    # `pull_request.base.sha` to diff against and must never be asked to run
    # this job — see ci-gate's handling of this job's `result` below, which
    # treats `skipped` as OK precisely because non-PR events skip it here.
    if: github.event_name == 'pull_request'
    name: contract-delta
    runs-on: ubuntu-latest
    # 25 -> 45 (2026-08-19). PR #6013's contract-delta job was CANCELLED at
    # exactly 25 minutes — checkout/setup/fetch all green, only the gate step
    # itself killed mid-run — because scripts/check_contract_delta.py's two
    # whole-repo censuses (head + base) ran serially and this hosted 2-core
    # runner is slower than the machine they were timed on. The script itself
    # now overlaps those two censuses (concurrent head/base, see its module
    # docstring), which is the real fix; this is belt-and-suspenders margin.
    # Safe to keep generous: this job is off the critical path entirely — the
    # ci-pack matrix it runs alongside already takes ~30 min.
    timeout-minutes: 45
    steps:
      # Checkout recipe copied verbatim from integration-baseline.yml's
      # `baseline` job (2026-08-13 diet) — same reasoning applies here: this
      # job parses .github/ci/legacy-jobs.yml, walks workflow definitions, and
      # runs static/manifest analysis only. It opens nothing under site/ or
      # data/. Non-cone with negation, deliberately: it states the EXCLUSION,
      # so a new top-level directory is included by default and cannot
      # silently fall out of this job's view the way a cone allowlist would.
      - uses: actions/checkout@v4
        with:
          filter: blob:none
          fetch-depth: 1
          sparse-checkout: |
            /*
            !/site/
            !/data/
          sparse-checkout-cone-mode: false
      - uses: actions/setup-python@v5
        with:
          # PINNED to the same exact patch release as ci-plan/ci-pack above —
          # see their comment for why a floating "3.12" is unsafe on this
          # hosted pool (2026-08-19, ci-pack-8 red on two unrelated PR heads
          # when the tool cache silently moved 3.12.13 -> 3.12.14).
          python-version: "3.12.13"
      - name: install control-plane dependencies
        # Static/manifest analysis only, same posture as integration-baseline.yml —
        # neither scripts/run_ci_pack.py's inference nor
        # scripts/audit_unrun_tests.py's census imports the product data stack.
        run: pip install pytest pyyaml
      - name: fetch the PR's base commit
        # check_contract_delta.py materializes the base tree itself (a detached
        # `git worktree add`), which needs the base commit's object present
        # locally — the fetch-depth: 1 checkout above only guarantees the head.
        run: git fetch --depth=1 origin ${{ github.event.pull_request.base.sha }}
      - name: differential contract-delta gate
        run: >-
          python scripts/check_contract_delta.py
          --base ${{ github.event.pull_request.base.sha }}
  # ONE STABLE NAME FOR "CI SAID YES" (Wave B, 2026-08-11). Every other check name
  # in this workflow is now conditional: `ci-pack-N` exists only when the plan
  # selected pack N, and the whole matrix is skipped on a proven no-work PR. That
  # is fine for diagnostics and fatal for anything that has to ADJUDICATE, because
  # the merge-on-green sweeper (scripts/merge_on_green.py) requires an affirmative
  # success on the head — absence of red is not a pass (#4779) — so a head whose
  # packs were all skipped would read `unproven` and never merge. `ci-gate` is the
  # one name that publishes a real conclusion on every workflow event, and it is
  # the name to point any future branch-protection rule at.
  ci-gate:
    # `always()` is what makes this job publish even when `ci-pack` was skipped or
    # failed — WITHOUT it the aggregate would vanish exactly when it is needed.
    if: always()
    name: ci-gate
    needs: [ci-plan, ci-pack, contract-delta]
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: check out the one shared semantic-proof law
        uses: actions/checkout@v4
        with:
          ref: ${{ github.sha }}
          filter: blob:none
          fetch-depth: 1
          sparse-checkout: |
            scripts/ci_semantic_proof.py
          sparse-checkout-cone-mode: false
      - name: download the authoritative semantic plan
        if: needs.ci-plan.outputs.plan_sha != ''
        continue-on-error: true
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093
        with:
          name: ${{ needs.ci-plan.outputs.plan_artifact_name }}
          path: ${{ runner.temp }}/ci-semantic-plan
      - name: download every raw semantic pack fragment
        if: needs.ci-plan.outputs.plan_sha != '' && needs.ci-plan.outputs.has_work == 'true'
        continue-on-error: true
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093
        with:
          pattern: ci-semantic-pack-${{ github.run_id }}-*
          path: ${{ runner.temp }}/ci-semantic-fragments
          merge-multiple: true
      # This is the one causal decision. Pack conclusions are scheduling and
      # transport facts only; complete plan accounting, exact-base comparison,
      # and missing/malformed refusal all live in ci_semantic_proof.py.
      - id: semantic_reconcile
        name: reconcile complete semantic evidence
        continue-on-error: true
        env:
          PLAN_RESULT: ${{ needs.ci-plan.result }}
          PLAN_REASON: ${{ needs.ci-plan.outputs.reason }}
          FALLBACK_RUN_ID: ${{ github.run_id }}
          FALLBACK_EVENT: ${{ github.event_name }}
          FALLBACK_ROLE: ${{ needs.ci-plan.outputs.semantic_role }}
          FALLBACK_TESTED_TREE: ${{ needs.ci-plan.outputs.tested_tree_sha }}
          FALLBACK_SUBJECT_HEAD: ${{ needs.ci-plan.outputs.subject_head_sha }}
          FALLBACK_BASE: ${{ needs.ci-plan.outputs.tested_base_sha }}
        run: |
          set -u
          mkdir -p "$RUNNER_TEMP/ci-semantic-fragments" "$RUNNER_TEMP/ci-semantic-final"
          fallback_args=()
          if [ -n "$FALLBACK_ROLE" ] && [ -n "$FALLBACK_TESTED_TREE" ] && [ -n "$FALLBACK_SUBJECT_HEAD" ] && [ -n "$FALLBACK_BASE" ]; then
            fallback_args=(
              --fallback-workflow-run-id "$FALLBACK_RUN_ID"
              --fallback-workflow ci
              --fallback-event "$FALLBACK_EVENT"
              --fallback-role "$FALLBACK_ROLE"
              --fallback-tested-tree-sha "$FALLBACK_TESTED_TREE"
              --fallback-subject-head-sha "$FALLBACK_SUBJECT_HEAD"
              --fallback-base-sha "$FALLBACK_BASE"
            )
          fi
          python3 scripts/ci_semantic_proof.py reconcile \
            --plan "$RUNNER_TEMP/ci-semantic-plan/ci-plan.json" \
            --fragments-dir "$RUNNER_TEMP/ci-semantic-fragments" \
            --planner-outcome "$PLAN_RESULT" \
            --planner-detail "$PLAN_REASON" \
            --output "$RUNNER_TEMP/ci-semantic-final/ci-semantic-evidence.json" \
            "${fallback_args[@]}"
      # Published on success AND failure. The fixed run-scoped name plus
      # overwrite prevents reruns from leaving two apparently authoritative
      # artifacts; consumers reject duplicates anyway.
      - name: publish final semantic evidence
        if: always()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        with:
          name: ci-semantic-evidence-${{ github.run_id }}
          path: ${{ runner.temp }}/ci-semantic-final/ci-semantic-evidence.json
          if-no-files-found: error
          overwrite: true
          retention-days: 14
      - name: summarize semantic verdict
        if: always()
        env:
          EVIDENCE: ${{ runner.temp }}/ci-semantic-final/ci-semantic-evidence.json
        run: |
          python3 - <<'PY'
          import json
          import os
          from pathlib import Path

          path = Path(os.environ["EVIDENCE"])
          if not path.is_file():
              print("Semantic evidence was not materialized.")
              raise SystemExit(0)
          doc = json.loads(path.read_text(encoding="utf-8"))
          counts = {}
          for job in doc.get("jobs", []):
              for step in job.get("steps", []):
                  key = step.get("classification", "unknown")
                  counts[key] = counts.get(key, 0) + 1
          summary = os.environ.get("GITHUB_STEP_SUMMARY")
          text = (
              "## CI semantic proof\n\n"
              f"- status: `{doc.get('status', 'failure')}`\n"
              f"- evidence: `{doc.get('evidence_sha256', 'missing')}`\n"
              f"- classifications: `{json.dumps(counts, sort_keys=True)}`\n"
              f"- infrastructure findings: `{len(doc.get('infrastructure', []))}`\n"
          )
          if summary:
              with open(summary, "a", encoding="utf-8") as handle:
                  handle.write(text)
          print(text)
          PY
      - name: enforce semantic verdict
        if: always()
        env:
          RECONCILE_OUTCOME: ${{ steps.semantic_reconcile.outcome }}
        run: |
          if [ "$RECONCILE_OUTCOME" != "success" ]; then
            echo "::error title=ci-gate::semantic proof is blocking ($RECONCILE_OUTCOME)"
            exit 1
          fi
          echo "::notice title=ci-gate::complete semantic proof is clear"
      # `contract-delta` is `if: github.event_name == 'pull_request'` (see its own
      # job above), so a push/workflow_dispatch main-proof run always sees
      # `skipped` here — that must NOT fail ci-gate, or every main baseline would
      # go red the moment this job was added. Matches PLAN_RESULT's own idiom
      # above: read the upstream job's `needs.<job>.result` into an env var and
      # branch on it in a plain shell step, rather than folding it into the
      # semantic-proof reconciler, which has no notion of this job at all.
      - name: enforce contract-delta verdict
        if: always()
        env:
          CONTRACT_DELTA_RESULT: ${{ needs.contract-delta.result }}
        run: |
          if [ "$CONTRACT_DELTA_RESULT" = "failure" ]; then
            echo "::error title=ci-gate::contract-delta found a NEW CI-contract defect this PR introduces ($CONTRACT_DELTA_RESULT) — see the contract-delta job's own log for the job/suite and the exact fix"
            exit 1
          fi
          echo "::notice title=ci-gate::contract-delta clear (result=$CONTRACT_DELTA_RESULT)"
