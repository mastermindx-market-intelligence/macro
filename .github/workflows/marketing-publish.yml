name: marketing-publish
# ─────────────────────────────────────────────────────────────────────────────
# D02 W1 — scheduled LIVE social publisher. DARK BY DEFAULT.
#
# Runs on [self-hosted, macstudio-light] since 2026-08-03 — NOT ubuntu-latest
# (that line was stale) and NOT `macstudio`: the light lane is how this stays OFF
# the nightly render path, and the runs-on block below documents the swap.
# It posts APPROVED, DUE outbox items to X via Buffer using
# scripts.marketing_publisher.
#
# WHY IT IS SAFE / DARK: the runner self-downgrades to DRY-RUN unless the global
# kill-switch is on (engine.marketing.sentinel.publish_enabled() reads env
# MARKETING_PUBLISH_ENABLED ∈ {1,true,yes}). The kill-switch is a repo VARIABLE
# (vars.MARKETING_PUBLISH_ENABLED) — the single source of truth; `1` = armed,
# `0`/absent = dark. With it off the run posts NOTHING and the ledger does not
# change, so the commit-back step is a clean no-op. `--live` is passed
# unconditionally BECAUSE that downgrade guard makes it harmless until the
# operator opts in (scripts/marketing_publisher.py: `live = bool(args.live) and
# kill_on`).
#
# HOW TO ARM IT (operator — one click in the admin Publisher panel; the panel
# writes the variable for you, see docs/marketing_publisher_runbook.md §6):
#   1. Repo secret    BUFFER_TOKEN              = a Buffer personal API token
#      (the panel's token paste-box sets this via `gh secret set BUFFER_TOKEN`)
#   2. Repo VARIABLE  MARKETING_PUBLISH_ENABLED = 1   (the kill-switch; the panel
#      ARM toggle sets it — `0` or absent to disarm). This is a VARIABLE, not a
#      secret; any lingering old SECRET of the same name is IGNORED by this run.
#   3. config/marketing.yml  publish.channels.flagship = the Buffer channel id
#      (discover it: BUFFER_TOKEN=... python -m scripts.marketing_publisher --list-channels)
# Set MARKETING_PUBLISH_ENABLED to 0 (or the panel DISARM) at any time to
# instantly revert every path to dry-run — that is the kill switch / rollback.
# ─────────────────────────────────────────────────────────────────────────────
on:
  schedule:
    # 30-minute Pacific signal ladder — 28 rungs, S1=4:00 AM … S28=5:30 PM PT
    # (cadence masterplan §5; operator re-spec 2026-07-27, widened from 19 rungs
    # at 45 min on 2026-07-28, #3904). Sweep every 30 min across the 00:00–23:30
    # UTC window that covers that grid in BOTH PDT and PST — 30 sweeps/day, EVERY
    # day. The window spans 00:00,00:30 and 01:00..23:30 because the last rung
    # lands at 00:30Z in PDT and 01:30Z in PST.
    #
    # A sweep is NO LONGER capped at one post (#3913). It used to be: an item
    # inside the 10-min global floor deferred to the next sweep, which made the
    # CRON GRID the unit of volume — 30 sweeps/day meant 30 posts/day
    # network-wide however many the desks generated, and every dropped sweep cut
    # into that. publish.max_forward_book_min now books such an item at the
    # moment the floor clears (Buffer customScheduled) instead of deferring, so
    # the spacing is unchanged and the floor is the real governor: ~6 posts/hr,
    # bounded by a tape-freshness horizon. The humanizing jitter still scatters
    # off the round minute. Breaking/news fire via event-driven dispatch
    # (workflow_dispatch below), unlimited and off the cron.
    #
    # The ~22:30 UTC nightly (daily.yml) also commits the outbox ledger; the
    # sweeps overlapping it use a retry-rebase push loop, and the ledgers carry
    # merge=union (.gitattributes) so a concurrent append resolves instead of
    # conflicting the loop to death. The checkout is SHALLOW — see the step.
    - cron: "0,30 0,1,11-23 * * *"
  workflow_dispatch:          # manual runs (still dark unless the secret is set)
    inputs:
      post_now_item:
        description: >-
          BREAKING DISPATCH — outbox item id(s), comma-separated. Restricts the
          run to those items, approves them regardless of publish.auto_approve,
          and sends them immediately (ignoring their ladder slot). Every safety
          gate still runs. Leave blank for a normal sweep. The admin Outbox
          "Post now" button fills this in.
        required: false
        default: ""
      skip_jitter:
        description: >-
          Skip the humanizing jitter sleep. Automatic for a post_now_item
          dispatch — a breaking post should not wait out a random delay.
        required: false
        type: boolean
        default: false
      recall_pending:
        description: >-
          KILL-SWITCH RECALL — cancel every post already booked at Buffer that
          has not sent yet, instead of publishing. This is the other half of
          MARKETING_PUBLISH_ENABLED=0: the variable stops NEW sends, but
          publish.max_forward_book_min hands posts to Buffer up to an hour
          ahead and those fire on Buffer's schedule regardless (2026-07-28:
          five booked posts survived the disarm by 41 minutes). The admin
          Publisher panel's DISARM toggle sets this automatically. Runs
          scripts.marketing_recall, which is deliberately NOT gated on the arm
          variable — recall must work while the publisher is off.
        required: false
        type: boolean
        default: false

permissions:
  contents: write             # commit changed outbox ledgers back to main

concurrency:
  # Never let two publisher runs overlap (double-post guard is also in the
  # runner, but non-overlapping runs keep the ledger writes serialized too).
  group: marketing-publish
  cancel-in-progress: false

jobs:
  publish:
    # SELF-HOSTED (operator ruling 2026-08-03, autonomous mandate). This ran on
    # ubuntu-latest to stay off the render path, and that was right until the
    # HOSTED pool became the outage: on 2026-08-03 a manually dispatched sweep
    # sat QUEUED 27 minutes while the account posted nothing, the scheduled
    # sweeps had already skipped 11:00/11:30/12:00/12:30/13:00, and the last
    # real sweep was 10:30Z. Five consecutive days ended with no posts. The mac
    # pool was idle the whole time.
    #
    # `macstudio-light` — NOT `macstudio`: the light lane exists precisely so a
    # short, time-critical job never queues behind the heavy render pool, which
    # is the same reason commodity-sentinel uses it. Publishing is API calls plus
    # an occasional card raster; it is light work with a hard deadline, which is
    # exactly that lane's purpose.
    runs-on: [self-hosted, macstudio-light]
    # 25m is plenty NOW THAT THE CHECKOUT IS SHALLOW (see fetch-depth below).
    # It was not before: on 2026-07-28 the full-history checkout ran the entire
    # 25m and was CANCELLED mid-clone, so `run publisher` never executed and the
    # desk network published nothing all day. Raising the cap is the wrong lever
    # — the clone grows nightly, so any fixed wall is a future outage. Keep the
    # checkout shallow and this budget stays generous.
    timeout-minutes: 25
    env:
      BUFFER_TOKEN: ${{ secrets.BUFFER_TOKEN }}
      # Kill-switch: a repo VARIABLE (single source of truth), NOT a secret. The
      # admin Publisher panel's ARM/DISARM toggle writes it. `1` = armed; `0` or
      # absent = dark. No secrets.* fallback on purpose — a lingering old secret
      # of this name must NOT keep the publisher live after the toggle is off.
      MARKETING_PUBLISH_ENABLED: ${{ vars.MARKETING_PUBLISH_ENABLED }}
    steps:
      - name: checkout (shallow + write token)
        # SHALLOW ON PURPOSE (2026-07-28). This was fetch-depth: 0 and the clone
        # of this heavy-churn repo grew past the 25m job timeout — the 12:58Z
        # sweep spent 12:58:08→13:23:07 inside `checkout` and was cancelled with
        # every later step skipped, so the publisher never ran and NOTHING went
        # out that day. Nothing here needs history: the runner reads the outbox
        # ledgers at HEAD, and the commit step below commits one row on top of
        # main and pushes it. The push loop deepens to 20 so `pull --rebase`
        # always has a merge base even when main advanced mid-run.
        uses: actions/checkout@v4
        with:
          fetch-depth: 1
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: python 3.12 (Homebrew venv - self-hosted runner)
        # NOT actions/setup-python: on this Studio a setup-python 3.12 exec dies
        # with SIGKILL (known trap), and a self-hosted runner has no hosted
        # tool-cache to fall back on. Same venv-per-runner pattern the
        # commodity-sentinel light lane already uses, so the two lanes cannot
        # drift apart on interpreter setup.
        run: |
          VENV="$HOME/.cache/mm-venv-${RUNNER_NAME:-default}"
          [ -x "$VENV/bin/python" ] || /opt/homebrew/bin/python3.12 -m venv "$VENV"
          echo "$VENV/bin" >> "$GITHUB_PATH"
          "$VENV/bin/python" -m pip install --quiet --upgrade pip

      - name: install deps
        # pyyaml: config/marketing.yml load. pandas+pyarrow: the tape gate's
        # earnings-day guard reads data/earnings/earnings.parquet (fail-soft —
        # the guard degrades open if the import or file is missing), and pyarrow
        # is also chart_render's no-pandas parquet reader for the mover card's
        # daily bars.
        #
        # pillow + boto3 JOIN 2026-07-31, with the R2 block on the publisher step
        # below. THIS LANE NOW RENDERS ITS OWN CARDS AT PUBLISH TIME:
        # engine/marketing/publish_time_content builds a watchlist card for every
        # theme_list and a tape chart for every mover, because a mover/theme post
        # with no picture is quarantined outright by the bare-cashtag law
        # (scripts/marketing_publisher._TICKER_ROLLUP_KINDS) and the lane was
        # therefore structurally dead. Rendering is only half the job:
        #   * pillow — LOGO COMPOSITING, not rasterisation. Both cards embed the
        #     company icon as a data: URI via chart_render.resolve_color_logo →
        #     logo_cache.color_logo_datauri, which on a cache MISS fetches the
        #     CDN png and hands it to logo_cache._downscale — a PIL call that
        #     returns None the moment `from PIL import Image` fails, taking the
        #     logo with it. The colour cache is the thin one: 57 `*_color.png`
        #     are committed against 1,204 `*_white.png`, so on a shallow checkout
        #     most tickers this lane picks MISS and need the fetch+downscale
        #     path. Without pillow the card still renders and still posts — it
        #     just ships with blank avatar chips, which is a silent quality
        #     regression rather than an outage.
        #     (An earlier version of this comment claimed pillow backstops
        #     media_publish.publish_card's `legacy_png` PIL raster when Chrome is
        #     missing. THAT WIRE DOES NOT EXIST HERE: publish_time_content's
        #     _resolve_card calls publish_card(svg, chart_id=…, as_of=…, root=…)
        #     and never passes `legacy_png`, so a Chrome-less host produces b""
        #     and the candidate is dropped for `no-media-url` no matter what PIL
        #     is installed. Chrome itself is not pip-installable and is not
        #     installed here — ubuntu-latest ships Google Chrome in the runner
        #     image, and chart_render.find_chrome picks it up off PATH.)
        #   * boto3 — publish_chart_png needs an S3 client, and without one the
        #     upload fail-softs to None on every card, which under the new law
        #     means every candidate is dropped rather than shipped bare. This one
        #     IS an outage dependency: no boto3, no post.
        run: pip install --quiet pyyaml pandas pyarrow pillow boto3

      - name: humanizing jitter
        # Spread posts off perfectly round cron times (Actions cron is already
        # best-effort ~0-15m late; this adds a little scatter so the account never
        # posts on the exact same wall-clock minute). Capped at 2m (was 5m, was
        # 10m): now that the ladder runs 30 sweeps/day, a big per-sweep sleep is
        # wasted runner time AND the 10-min global floor already paces posting, so
        # 2m is plenty of de-sync. Well clear of the 25m job timeout.
        #
        # SKIPPED on a breaking dispatch (masterplan §6): a "post now" click must
        # not sit out a random sleep. Any post_now_item implies skip_jitter.
        #
        # The event_name guard is deliberate belt-and-braces: on a `schedule` run
        # the `inputs` context is empty, and rather than depend on how GitHub
        # coerces a missing property against '' (it compares as equal, so the
        # bare form also works), this makes the scheduled lane provably identical
        # to before this input existed. The scheduled lane is not a place to be
        # clever about expression semantics we cannot run locally.
        #
        # Also SKIPPED on a recall dispatch, for the same reason and then some:
        # a recall is racing posts that are already scheduled — the 2026-07-28
        # incident had the first booked post firing 5 minutes after the operator
        # disarmed — so sleeping up to 2 minutes before cancelling them would
        # spend a quarter of the entire window on a de-sync nobody needs.
        if: >-
          ${{ !(github.event_name == 'workflow_dispatch'
                && (inputs.skip_jitter || inputs.recall_pending
                    || inputs.post_now_item != '')) }}
        run: sleep $((RANDOM % 120))

      - name: fetch full-universe live quotes (tape gate input)
        # AFTER the jitter sleep so the snapshot is as fresh as possible at post
        # time. The live-data orphan branch carries a ~2k-symbol quotes.json
        # refreshed every 5 min in RTH (15-min-delayed prices). The publisher's
        # tape gate (engine/marketing/live_verify.py) uses it to re-verify every
        # item against TODAY's tape before posting — the content plan was
        # written off yesterday's close. Fail-soft: without the snapshot the
        # gate falls back to site/live/quotes.json + the sp500 heatmap, and
        # holds any signal it cannot verify.
        run: |
          if git fetch --depth 1 origin live-data && \
             git show FETCH_HEAD:quotes.json > data/marketing/live_quotes_snapshot.json 2>/dev/null; then
            echo "live quotes snapshot: $(wc -c < data/marketing/live_quotes_snapshot.json) bytes"
          else
            echo "::warning::live-data quotes unavailable — tape gate falls back to site/live artifacts"
            rm -f data/marketing/live_quotes_snapshot.json
          fi

      - name: recall booked-but-unsent posts (kill-switch recall)
        # THE OTHER HALF OF THE KILL SWITCH. Runs INSTEAD of the publisher when
        # dispatched with recall_pending — see the input's description and
        # scripts/marketing_recall.py's module docstring for the incident.
        #
        # Not gated on MARKETING_PUBLISH_ENABLED on purpose: the operator has
        # just set that variable to 0, and a recall that only worked while the
        # publisher was armed would be inoperable exactly when it is needed. The
        # runner's own safety is the candidate rule — it will only cancel a post
        # whose booked send time is still in the FUTURE, and only moves the
        # ledger on a confirmed backend delete.
        #
        # Exits 1 if any delete failed, which turns this job red on purpose:
        # a failed recall means posts the operator asked to stop are still going
        # out, and that must not go green.
        if: ${{ github.event_name == 'workflow_dispatch' && inputs.recall_pending }}
        run: python -m scripts.marketing_recall --recall-pending --live

      - name: run publisher (dark until the kill-switch secret is set)
        # Skipped on a recall dispatch — that run is here to STOP posts, not to
        # send more. (With the variable already 0 the runner would dry-run
        # anyway; this makes the intent explicit rather than incidental.)
        if: ${{ !(github.event_name == 'workflow_dispatch' && inputs.recall_pending) }}
        env:
          # Breaking dispatch: empty on every scheduled sweep, so the normal
          # ladder run is byte-identical to before this input existed.
          POST_NOW_ITEM: ${{ inputs.post_now_item }}

          # ── COLD READ: THE LOCAL MODEL THAT READS A POST AS A STRANGER ─────
          # engine/marketing/cold_read.py. Needs BOTH of these to run at all,
          # and config/marketing.yml `cold_read.action` is what decides whether
          # a flag does anything (it ships as "shadow" — logs, blocks nothing).
          #
          # Unconfigured repo variables resolve to "" and the rung is dropped,
          # so these lines are inert until OLLAMA_BASE_URL points at the box.
          # That state is NOT silent: the publisher prints a
          # `::warning title=marketing-cold-read-dark` when the gate is enabled
          # and no model answered for any relayed item, because "zero flags"
          # from a dark gate reads exactly like "the copy was clean".
          OLLAMA_BASE_URL: ${{ vars.OLLAMA_BASE_URL }}
          MARKETING_LLM_ENABLED: "1"

          # ── R2: WHERE THE PUBLISH-TIME CARD BECOMES A PICTURE ──────────────
          # Same arming contract, and the same failure mode, as the press wire
          # (marketing-press-wire.yml): publish_card writes the SVG and the PNG
          # locally, then publish_chart_png needs a client (boto3, added to the
          # install line above) AND these credentials to produce the public https
          # URL Buffer fetches — a repo path is not something X can load.
          #
          # Absent either, the upload fail-softs to None. Before 2026-07-31 that
          # was silent and the post shipped text-only; now the lane refuses to
          # enqueue an unhosted card at all (chartless DEFER), so an unconfigured
          # secret here means the mover/theme lane generates NOTHING and says so
          # with a ::warning per sweep rather than posting a naked ticker list.
          # Unconfigured repo secrets resolve to "" and are treated as absent, so
          # these lines are inert until the secrets land.
          R2_ENDPOINT: ${{ secrets.R2_ENDPOINT }}
          R2_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
          R2_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
          R2_BUCKET: ${{ secrets.R2_BUCKET }}
        run: |
          # --live is safe here: with MARKETING_PUBLISH_ENABLED unset the runner
          # runs DRY-RUN, posts nothing, and writes no status transitions.
          if [ -n "${POST_NOW_ITEM:-}" ]; then
            echo "breaking dispatch — restricting this run to: $POST_NOW_ITEM"
            python -m scripts.marketing_publisher --live --post-now "$POST_NOW_ITEM"
          else
            python -m scripts.marketing_publisher --live
          fi

      - name: poll post metrics (dark until BUFFER_TOKEN is set)
        # ONCE A DAY, NOT ONCE A SWEEP (2026-08-03). This ran on all ~30 sweeps
        # against ~54 targets — ~1,600 Buffer calls a day for telemetry, on the
        # SAME token and the SAME 24h quota the publisher posts with. On
        # 2026-08-03 it exhausted the allowance (Retry-After: 20696) and the
        # next sweep's approved, due, human-audited post was refused with
        # `rate_limited=1`. Five consecutive days had ended with nothing posted.
        #
        # "ONCE A DAY" MEANT TWICE A DAY (2026-08-11). The hour check alone
        # admits BOTH the 13:00Z and the 13:30Z sweep — `date -u +%H` reads "13"
        # for the whole hour — so this fired twice daily and spent 2x the cap.
        # Run 31453875632 (2026-08-11 02:56Z) is the receipt: a live post attempt
        # came back `429 retry after 37768s`, and 02:56Z + 37768s = 13:25Z — the
        # exact minute the 2026-08-10 13:25Z poll's calls roll out of Buffer's
        # 24h window. Buffer was naming, to the second, whose calls were in the
        # way of the post. The gate is now hour==13 AND minute<25: sweeps land at
        # :00 and :30 plus up to ~15 min of Actions queue drift, so `<25` admits
        # the 13:00Z sweep and only it.
        #
        # A BREAKING post_now DISPATCH NEVER POLLS. That click is an operator
        # trying to get ONE post out NOW; spending its run on telemetry first is
        # this same failure class in miniature. Every other manual dispatch still
        # polls, so the console is never more than a click from current.
        #
        # NEITHER DOES A RECALL DISPATCH (2026-08-11 follow-up). A recall is the
        # kill-switch: the operator is cancelling posts ALREADY BOOKED at Buffer,
        # and every one of those deletes is a call on this same rolling 24h
        # allowance. Telemetry standing in front of an emergency stop is the
        # worst version of the failure class this whole comment is about — the
        # post that cannot be pulled is worse than the post that cannot be sent.
        # Carried as a step-level `if`, identical to the publisher step's, so the
        # step never starts rather than bailing inside the shell.
        #
        # Metrics refresh ~daily, so 30 polls a day of the same post bought
        # nothing and cost the product. The 13:00Z slot is after the US open so
        # the prior session's posts have had time to accrue numbers. The script
        # ALSO stops on the first 429 and caps its own run (8 calls) — belt and
        # braces, because a workflow condition is one edit away from being
        # loosened by someone who does not know what it cost.
        #
        # The gate is a CLOCK CHECK, not `github.event.schedule`: this workflow
        # declares ONE cron expression covering every slot, so that context value
        # is the same literal on all 30 sweeps and could never single out an hour.
        #
        # Reads back per-post analytics (impressions/likes/reposts/… + the x.com
        # permalink) for items posted in the last 7 days and appends them to
        # data/marketing/post_metrics.jsonl. Fully independent of the
        # kill-switch: with BUFFER_TOKEN unset it prints one line and exits 0
        # (no network, no write) — so it is harmless while the publisher is dark.
        if: ${{ !(github.event_name == 'workflow_dispatch' && inputs.recall_pending) }}
        env:
          # Same input the publisher step reads: empty on every scheduled sweep,
          # non-empty only on a breaking dispatch.
          POST_NOW_ITEM: ${{ inputs.post_now_item }}
        run: |
          H=$(date -u +%H)
          M=$(date -u +%M)
          if [ -n "${POST_NOW_ITEM:-}" ]; then
            echo "metrics poll: skipped — breaking post_now dispatch."
            echo "The post gets this run's allowance; telemetry can wait."
            exit 0
          fi
          if [ "${{ github.event_name }}" = "schedule" ]; then
            if [ "$H" != "13" ] || [ "$M" -ge 25 ]; then
              echo "metrics poll: skipped — once-daily at the 13:00Z sweep, now ${H}:${M}Z."
              echo "The publisher shares this token; telemetry does not outbid posting."
              exit 0
            fi
          fi
          python -m scripts.marketing_metrics_poll

      - name: commit changed outbox ledgers (no-op when nothing changed)
        # Only persist ledger state when ARMED. While dark the dry-run writes a
        # throwaway activity row we must NOT commit to main — else every dark run
        # would spam main with no-op commits. The kill-switch is now a repo
        # VARIABLE whose disarmed value is the literal "0" (non-empty), so this
        # guard tests `== '1'` (armed), NOT `!= ''` — otherwise a disarmed "0"
        # would wrongly commit the dry-run's audit row.
        #
        # A RECALL DISPATCH IS THE EXCEPTION, and it has to be. The operator
        # flips the variable to 0 and the recall runs moments later, so the
        # armed test is false by construction — yet that run writes REAL ledger
        # state (posted→recalled transitions plus the publications retraction
        # rows). Without this second clause every recall would cancel the posts
        # at Buffer and then throw away the record of having done so: main would
        # still show them `posted`, the next fold would disagree with reality,
        # and the audit trail for a kill-switch event would be empty.
        if: >-
          ${{ always()
              && (env.MARKETING_PUBLISH_ENABLED == '1'
                  || (github.event_name == 'workflow_dispatch' && inputs.recall_pending)) }}
        run: |
          set -uo pipefail
          . "${GITHUB_WORKSPACE:-.}/scripts/ci/push_retry.sh"
          PUSH_LABEL="marketing-publish"
          # workflow_dispatch takes any ref — never publish HEAD to main from one.
          if ! push_on_main_ok; then exit 0; fi
          git config user.name "dashboard-bot"
          git config user.email "actions@users.noreply.github.com"

          # Stage ONLY the marketing ledgers — never touch other paths. A dry-run
          # (dark mode) still appends a publisher_dry_run activity row, which is a
          # legitimate audit trail; a live run additionally advances statuses AND
          # appends posted receipts to publications.jsonl (the Channels page
          # reader). post_metrics.jsonl (one level up from outbox/) is the metrics
          # poller's append-only ledger — staged alongside so a fetched analytics
          # row lands on main with the same commit. Absent (poller dark) → no-op.
          git add data/marketing/outbox 2>/dev/null || true
          git add data/marketing/publications.jsonl 2>/dev/null || true
          git add data/marketing/post_metrics.jsonl 2>/dev/null || true

          if git diff --cached --quiet; then
            echo "marketing-publish: no ledger changes to commit"
            exit 0
          fi

          git commit -m "marketing-publish: outbox run $(date -u +%FT%H:%MZ) [skip ci]"

          # Retry-rebase push loop to tolerate races with the nightly/other lanes
          # (mirrors seo-director.yml push mechanics). --autostash parks nothing
          # here since everything is already committed above.
          for i in 1 2 3 4 5; do
            # --depth 20: the checkout is shallow, so a bare fetch would leave
            # `pull --rebase` without a merge base once main advances. 20 covers
            # any realistic mid-run drift; the ledgers themselves carry
            # merge=union (.gitattributes) so a concurrent nightly commit to the
            # same JSONL resolves instead of conflicting.
            git fetch --depth 20 origin main || true
            # Drop any untracked file that shadows an origin/main path (a mid-run
            # commit could TRACK a path we wrote as an untracked side-effect,
            # which would make the rebase checkout refuse). Everything we publish
            # is already committed, so such a path is a throwaway local write.
            comm -12 <(git ls-files --others --exclude-standard | LC_ALL=C sort) \
                     <(git ls-tree -r --name-only origin/main | LC_ALL=C sort) \
              | while IFS= read -r f; do rm -f -- "$f" || true; done
            if git pull --rebase --autostash origin main && push_autostash_ok && git push origin HEAD:main; then
              echo "marketing-publish: pushed ledger on attempt $i"
              exit 0
            fi
            git rebase --abort 2>/dev/null || true
            echo "marketing-publish: push attempt $i lost a race; re-syncing"
            sleep $((i * 7))
          done

          echo "::warning::marketing-publish: could not push ledger after 5 attempts (non-fatal)"
          exit 0

      - name: liveness tripwire
        # THE OBSERVER FOR "NOTHING CAME OUT". 2026-08-06 Buffer's plan locked
        # (429s); 08-08 MARKETING_PUBLISH_ENABLED went to 0, correctly, while the
        # backend refused; Buffer was fixed and NOBODY RE-ARMED. For five days
        # this workflow ran on schedule, concluded GREEN on every sweep, and
        # posted nothing — because a dry-run sweep is a successful sweep and no
        # check anywhere asked "when did we last actually post?".
        #
        # scripts/marketing_liveness_tripwire.py asks it: armed + silent deep in
        # the posting window is an ::error and a red sweep; disarmed longer than
        # a day is a ::warning every sweep plus one ::error at 13:00Z, so a
        # kill-switch left off has to be re-decided out loud once a day instead
        # of quietly becoming the new default.
        #
        # `always()` IS LOAD-BEARING: a sweep that died in `run publisher` is
        # precisely the sweep whose silence needs reporting, and this step is
        # last — AFTER the ledger commit — so a red tripwire can never cost the
        # ledger its push. The step only reads two files and prints; it posts
        # nothing, approves nothing and writes nothing.
        #
        # Skipped on a recall dispatch, like the publisher step: that run exists
        # to STOP posts, so "no posts went out" is its success condition.
        if: ${{ always() && !(github.event_name == 'workflow_dispatch' && inputs.recall_pending) }}
        env:
          # Same repo VARIABLE as the job env above, restated here so the one
          # input this alarm reads is visible at the step that reads it.
          MARKETING_PUBLISH_ENABLED: ${{ vars.MARKETING_PUBLISH_ENABLED }}
        run: python -m scripts.marketing_liveness_tripwire
