name: daily
# Embed the fired cron (or event) so watchdogs can tell a real slot from an
# et_gate no-op without a second jobs API call. 2026-08-14/15: the EST-guard
# survivor concluded success and read as "the nightly ran".
run-name: daily ${{ github.event.schedule || github.event_name }}
on:
  schedule:
    # Build B — the sole authoritative / ledger-advancing build (two-build design 2026-07).
    # Anchor: 18:30 ET YEAR-ROUND — ~30 min after the ~6pm-ET FINRA CNMSshvol post (the
    # file's window spans ~5-7pm ET; a 6:00pm-ET trigger races it — safe-earliest is
    # ~18:15 ET). Was "0 2 * * *" (10pm ET nominal, ~1-3am actual with dispatch lag) —
    # moved 3.5h earlier so authoritative data + ledger advance land the same evening.
    # The 16:05-ET closing-bell.yml lane (Build A) ships a full provisional site ~17:55 ET
    # beforehand. 7-day cadence retained (weekend runs do accrual/self-heals, unchanged
    # behavior from the old 02:00 slot). China/HK remain split to asia-close.yml (08:30 UTC).
    #
    # DST: GitHub cron has no timezone, so a fixed 18:30-ET anchor needs a DST PAIR (the
    # closing-bell.yml idiom). Both lines fire every day and the et_gate job below keeps
    # exactly ONE of them. et_gate derives the America/New_York UTC offset from stdlib
    # zoneinfo — NO hand-maintained DST table to re-edit twice a year — and proceeds only
    # when the fired cron (github.event.schedule) matches the regime: EDT (UTC-4) →
    # "30 22 * * *", EST (UTC-5) → "30 23 * * *". The test is REGIME-match, not a
    # wall-clock window: a real firing that sat queued behind a long run still proceeds
    # hours late, where a window check would silently skip the authoritative nightly.
    # The off-regime firing costs one ~15-second ubuntu job, because every downstream job
    # takes `needs: [et_gate, ...]` and gates on `needs.et_gate.outputs.run != 'false'`.
    # That comparison is deliberately fail-OPEN: a broken or errored gate makes BOTH
    # firings run (each cron has its own concurrency group, so they cannot supersede
    # each other — see the concurrency block). A gate outage can double-run a night
    # but can never silently zero-run one. The off-regime firing is a ~15s ubuntu
    # no-op in its OWN group; it must never share a group with the real slot.
    # Known accepted edge: a firing whose START is delayed ACROSS the 02:00-ET flip itself
    # resolves to the NEW regime — worst case one skipped or one doubled run on flip
    # weekend, backstopped by the next evening's run and by workflow_dispatch.
    # Filed by research/BREATHING_PLATFORM_MASTERPLAN_BY_FABLE.md §4 W-L0; fixed 2026-08-08.
    - cron: "30 22 * * *"  # 18:30 ET while America/New_York is on EDT (UTC-4, Mar→Nov)
    - cron: "30 23 * * *"  # 18:30 ET while America/New_York is on EST (UTC-5, Nov→Mar)
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  # Daily stays off the shared "pipeline" lane (a 2-3h backfill once starved it).
  # Push steps rebase-retry, so overlap with a batch job is race-safe.
  #
  # THE 2026-08-14/15 KILL. Both DST crons used to share one group. GitHub still
  # cancels a PENDING run in a group even when cancel-in-progress is false
  # (fences.yml 2026-08-09 measured the same pending-supersede). EDT run
  # 31848262472 sat queued from 22:52Z and was CANCELLED ("superseded") at ~23:45Z
  # when EST-guard 31851452961 entered the group; the survivor then skipped every
  # real job (wrong slot). Event-conditional cancel-in-progress (the fences.yml
  # fix) cannot save a queued slot — only distinct groups can. Each cron gets its
  # own group; workflow_dispatch stays on pipeline-daily-3 so manuals serialize
  # with each other, never with a cron. cancel-in-progress stays false so a
  # second fire of the SAME slot cannot kill a running bake.
  #
  # File-size cliff (~512,000 bytes, silent strand): tests/test_workflow_file_size.py.
  # #5422 renamed pipeline-daily → pipeline-daily-3 on a now-refuted zombie-group
  # theory; kept to avoid churn. Varying kill delays are a poll cycle — grep
  # session transcripts for the run id before concluding "no canceller exists".
  group: ${{ github.event_name == 'schedule' && format('pipeline-daily-3-cron-{0}', github.event.schedule) || 'pipeline-daily-3' }}
  cancel-in-progress: false

jobs:
  # ── ET regime gate ────────────────────────────────────────────────────────
  # Root disambiguator for the DST cron pair above (see the schedule comment).
  # ubuntu-hosted + checkout-free on purpose: the no-op firing must cost seconds,
  # and a self-hosted job with timeout-minutes would owe the W2 timings wiring
  # (tests/test_nightly_timings.py). tests/test_daily_et_gate.py extracts and
  # EXECUTES this exact inline script against fixed instants around the DST
  # flips, and pins the cron constants to the schedule block — edit either side
  # only in lockstep.
  et_gate:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    outputs:
      run: ${{ steps.gate.outputs.run }}
    steps:
      - name: ET regime gate (DST pair disambiguator)
        id: gate
        env:
          FIRED_CRON: ${{ github.event.schedule }}
          EVENT_NAME: ${{ github.event_name }}
        run: |
          python3 - <<'PY'
          import datetime
          import os
          from zoneinfo import ZoneInfo

          EDT_CRON = "30 22 * * *"  # 18:30 ET while America/New_York is UTC-4 (EDT)
          EST_CRON = "30 23 * * *"  # 18:30 ET while America/New_York is UTC-5 (EST)

          now_env = os.environ.get("ET_GATE_NOW_UTC")  # test injection only
          now = (datetime.datetime.fromisoformat(now_env) if now_env
                 else datetime.datetime.now(datetime.timezone.utc))
          if now.tzinfo is None:
              now = now.replace(tzinfo=datetime.timezone.utc)
          offset = now.astimezone(ZoneInfo("America/New_York")).utcoffset()
          # Any offset other than EDT's -4h resolves to the EST cron: with broken
          # tzdata the nightly still runs exactly once (possibly an hour late),
          # never zero times.
          intended = EDT_CRON if offset == datetime.timedelta(hours=-4) else EST_CRON
          fired = os.environ.get("FIRED_CRON", "")
          event = os.environ.get("EVENT_NAME", "")
          verdict = "true" if (event != "schedule" or fired == intended) else "false"
          # Hours, not the raw timedelta: str(timedelta(hours=-4)) renders
          # "-1 day, 20:00:00", which is the wrong thing to read at 3am.
          off_h = offset.total_seconds() / 3600
          print(f"::notice title=daily ET gate::event={event or '-'} fired={fired or '-'} "
                f"intended={intended} ny_utc_offset={off_h:+.0f}h -> run={verdict}", flush=True)
          with open(os.environ["GITHUB_OUTPUT"], "a", encoding="utf-8") as fh:
              fh.write(f"run={verdict}\n")
          PY

  # collect (a.k.a. collect-core since the 2026-07-14 split): the main adapter
  # sweep + in-collect graders/audits, committing as early as possible so the
  # engine job (needs: collect) starts ~30-75m sooner. The slow post-adapter
  # accrual band — ThetaData tape-flow, TIL organs, shadow-importance, qledger
  # backfill — runs in the sibling collect_tail job below. Engine never read
  # those outputs same-night (≤1-run-stale contract, same as factor_series;
  # verified 2026-07-14: flow_leaders/flow_desk/options_screener are absent-safe
  # display legs, stamp_options_state is PIT-anchored, grade_qledger grades only
  # claims matured ≥5d, TIL lobes carry 2160h freshness SLAs).
  collect:
    needs: [et_gate]
    # always() is load-bearing here: it is what makes the ET gate FAIL-OPEN. Without
    # it an errored et_gate job would skip collect via needs-status and silently kill
    # the whole nightly; with it, only an explicit run=false stands the pipeline down.
    if: always() && needs.et_gate.outputs.run != 'false'
    runs-on: [self-hosted, macstudio]
    # was 100 — the collectors step's real baseline has grown to ~60-67m (the old "~35m"
    # budget), and on 2026-07-11 it spiked to 98.5m (EDGAR 8-K velocity ~18m, OpenFIGI
    # ~10m, shadow-importance ~14m on a Friday close). The job was CANCELLED at exactly
    # 100m while still ahead of the data checkpoint (then one step named "commit data";
    # since 2026-08-06 the market half is "commit market data"), so the whole night was
    # thrown away and engine built the dashboards from the previous day's committed
    # store (us_standouts stuck on 07-09). Same silent-timeout class as the asia-close
    # freeze (#2193) and the engine job's own 70→120 bump below. 150m = spike ×1.5.
    # 2026-07-14 split: the 98.5m spike basis included shadow-importance (~14m), which
    # moved to collect_tail — 150m is now ≥1.7× the residual spike and is deliberately
    # kept: an undersized cap costs a whole night's collections (bitten twice).
    # 2026-07-17: bitten a THIRD time — the first 18:30-ET (22:30 UTC) Build B run's
    # collect ran 150.4m and was cancelled AT the data checkpoint, seconds from
    # landing ~148m of freshly-pulled collections (run 29542087837; downstream engine/
    # publish all green but the day's collector data never committed). Runtime grew
    # ~115m→~148m after the schedule moved 02:00 UTC → 22:30 UTC — suspected early-
    # evening source retry loops (under forensics). 185m buys survival at the observed
    # pace + commit headroom while the growth is root-caused.
    # 2026-08-05: 185 nearly bit a FOURTH time. When the M1 host went down (ENOSPC) the
    # macstudio label failed over to a COLD spare (mac-builder-5: fresh venv, no warm
    # GHA/R2 caches) and the collectors step took 172m — a 13-minute margin under the
    # cap, on a run that had already lost three nights. The fleet can always fail over
    # to a cold runner, so the cap must clear the COLD pace, not the warm one: 240m is
    # ~1.4x the observed 172m. The job still exits as soon as its steps finish; a
    # larger cap costs nothing on a healthy night and buys the commit on a slow one.
    timeout-minutes: 240
    outputs:
      government_revenue_projection_needed: ${{ steps.pushdata.outputs.published }}
      # ── CAPITAL-STRUCTURE HANDOFF CONTRACT (2026-08-06 job split) ──────────
      # The capital-structure chain moved to its own `capital_structure` job. Its
      # INPUT — the source ledger `run collectors` (sec_capital_structure) just
      # wrote — is deliberately NOT committed by this job (the market checkpoint
      # unstages it; see the carve-out below), so it cannot travel to that job
      # through git. It travels as an artifact, and these two outputs are the
      # receipt that proves the artifact the other job downloaded is the ledger
      # THIS job produced.
      #
      # Without the receipt the split has a silent-freeze failure mode: a missing
      # or mis-pathed download leaves the other job's fresh `actions/checkout`
      # holding the LAST COMMITTED ledger, the compilers reproduce yesterday
      # byte-for-byte, "no capital-structure changes to commit" prints, and the
      # lane is frozen forever while every step stays green. Measured on this
      # tree: 1,258 rows are committed, the 2026-08-06 run carried 1,447 — the
      # 189-row difference is exactly what would be lost, every night.
      capital_structure_ledger_sha256: ${{ steps.cs_handoff_stamp.outputs.sha256 }}
      capital_structure_ledger_rows: ${{ steps.cs_handoff_stamp.outputs.rows }}
    steps:
      # W2 runtime telemetry (research/NIGHTLY_RESILIENCE_AND_LIVE_TRANSITION_MASTERPLAN_2026-08-06.md
      # §A2): stamp the job's wall-clock start BEFORE checkout (checkout alone has run 3s→14.5m on
      # this tree), so the timings ledger measures the same clock the job cap kills. The matching
      # LAST step (scripts/ci/nightly_timings_finish.sh, if: always()) appends the per-band row to
      # data/ops/nightly_timings/<job>.jsonl and trips a ::warning above 85% of timeout-minutes —
      # every cap in this file was historically raised only AFTER nights died at it; this makes the
      # creep visible BEFORE a kill night. Same two steps on every self-hosted job here
      # (tests/test_nightly_timings.py pins the wiring + cap args).
      - name: timings — job start mark (W2)
        run: date +%s > "${RUNNER_TEMP}/nightly-timings-${GITHUB_RUN_ID}-${GITHUB_JOB}-start"
      - uses: actions/checkout@v4
      - name: python 3.12 (Homebrew venv — self-hosted runner)
        run: |
          VENV="$HOME/.cache/mm-venv-${RUNNER_NAME:-default}"
          [ -x "$VENV/bin/python" ] || /opt/homebrew/bin/python3.12 -m venv "$VENV"
          echo "$VENV/bin" >> "$GITHUB_PATH"
          "$VENV/bin/python" -m pip install --quiet --upgrade pip
      - run: pip install -r requirements.txt
      - name: restore breadth close cache
        uses: actions/cache@v4
        with:
          path: data/breadth/_closes_cache.parquet
          key: breadth-closes-${{ github.run_id }}
          restore-keys: breadth-closes-
      - name: restore smallcap (S&P 600) breadth close cache
        uses: actions/cache@v4
        with:
          path: data/smallcap_breadth/_closes_cache.parquet
          key: smallcap-closes-${{ github.run_id }}
          restore-keys: smallcap-closes-
      - name: restore midcap (S&P 400) breadth close cache
        uses: actions/cache@v4
        with:
          path: data/midcap_breadth/_closes_cache.parquet
          key: midcap-closes-${{ github.run_id }}
          restore-keys: midcap-closes-
      - name: restore Russell 2000 breadth close cache
        uses: actions/cache@v4
        with:
          path: data/russell_breadth/_closes_cache.parquet
          key: russell-closes-${{ github.run_id }}
          restore-keys: russell-closes-
      - name: restore china breadth close cache
        uses: actions/cache@v4
        with:
          path: data/china_breadth/_closes_cache.parquet
          key: china-breadth-closes-${{ github.run_id }}
          restore-keys: china-breadth-closes-
      - name: restore hk breadth close cache
        uses: actions/cache@v4
        with:
          path: data/hk_breadth/_closes_cache.parquet
          key: hk-breadth-closes-${{ github.run_id }}
          restore-keys: hk-breadth-closes-
      - name: restore china per-name OHLC store (signal-engine deep store; nightly backfill grows it)
        # Per-name daily OHLC (collectors/china_stock_prices.py → data/china_stocks/<T>.parquet).
        # data/china_stocks is git-TRACKED and written by asia-close (sole owner). daily.yml's
        # collect job excludes the asia group (--exclude-group asia) so it never advances this
        # store. Removing restore-keys prevents daily from overlaying a stale GHA cache on top
        # of the fresh data asia-close committed — that overlay + `-X theirs` on the rebase-push
        # was the root cause of the 5-day lag regression (CN-SYS W5a fix).
        uses: actions/cache@v4
        with:
          path: data/china_stocks
          key: china-stocks-ohlc-${{ github.run_id }}
      - name: restore hk per-name OHLC store (signal-engine deep store; nightly backfill grows it)
        uses: actions/cache@v4
        with:
          path: data/hk_stocks
          key: hk-stocks-ohlc-${{ github.run_id }}
          restore-keys: hk-stocks-ohlc-
      - name: restore intraday (4H) cache
        # Polygon hourly bars: regenerable + daily-changing, so cached cross-job (like the
        # breadth caches) instead of committed, to keep the repo from bloating. The collect
        # job populates it; the engine job restores it to emit site/intraday.
        uses: actions/cache@v4
        with:
          path: data/intraday
          key: intraday-${{ github.run_id }}
          restore-keys: intraday-
      - name: restore data-quality revision baselines
        # The end-of-collect audit (scripts/audit_macro) needs the PRIOR run's macro values to
        # detect backwards-revisions. The baselines are a daily-changing full copy of the macro
        # series — pure machine state — so they are cached cross-run (like the breadth caches),
        # not committed, to keep the repo from bloating. Absent on the first run => the audit just
        # seeds them (no detection that day) and revision-checking is active from the next run.
        uses: actions/cache@v4
        with:
          path: data/quality/baselines
          key: quality-baselines-${{ github.run_id }}
          restore-keys: quality-baselines-
      - name: restore attention store from R2 (deep history is git-ignored)
        # data/attention/*.parquet (SLF-048 Wikipedia pageviews, 2015-07→) moved to the
        # R2 data plane 2026-07-06 — a fresh checkout holds NONE of them. Restore FIRST
        # so the wiki_pageviews collector's ~120d fetch upserts ON TOP of deep history
        # (lib.store.upsert keeps disk-only rows) instead of rebuilding a shallow window.
        # continue-on-error: on restore failure the collector still produces a usable
        # ~120d store (the display chip needs ~95d) — but the publish-back step after
        # "commit market data" is GATED on this step's outcome, so a shallow rebuild can never
        # clobber the deep store on R2 (publish_r2's bytes floor is the second fence).
        id: attention_restore
        continue-on-error: true
        env:
          R2_ENDPOINT: ${{ secrets.R2_ENDPOINT }}
          R2_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
          R2_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
          R2_BUCKET: ${{ secrets.R2_BUCKET }}
        run: python -m scripts.fetch_r2 --dirs attention
      - name: restore massive_stock_day store from R2 (whole-market daily store; incremental upserts on top)
        # data/massive_stock_day (~617 MB, ~20k per-ticker parquets) is R2-CANONICAL: a
        # checkout holds only the two committed JSON sidecars. Restore FIRST so
        # collectors/massive_stock_day.py run_incremental() upserts the new trading days
        # ON TOP of the full store instead of tripping its store-absent fence (that fence
        # exists because fetching into a throwaway tree would mark days processed in the
        # COMMITTED resume state and lose them permanently).
        # continue-on-error: the publish-back step after the checkpoints is GATED on this
        # step's outcome, so a failed or partial restore can never clobber the deep store
        # on R2 — fetch_r2 exits 1 on any download failure, and publish_r2's
        # _DATA_DIR_MIN_FILES floor is the second fence. On failure the adapter blocks
        # loudly (::warning) and the night self-heals tomorrow.
        # Incident: the store froze 2026-07-04→07-29 (run_status no_creds ×21) with the
        # secrets never wired into this job at all — see the collector step below.
        id: massive_restore
        continue-on-error: true
        env:
          R2_ENDPOINT: ${{ secrets.R2_ENDPOINT }}
          R2_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
          R2_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
          R2_BUCKET: ${{ secrets.R2_BUCKET }}
        run: python -m scripts.fetch_r2 --dirs massive_stock_day --workers 24
      - name: timings band — collectors (W2)
        run: python3 scripts/nightly_timings.py mark --band collectors || echo "::warning::timings band mark failed (non-fatal)"
      - name: run collectors (graceful degradation — never fails the build on one source)
        # id is load-bearing: BOTH checkpoints ("commit market data" and "commit
        # capital-structure data") gate on THIS step's outcome by name.
        # It is the sole bulk producer of the tree that commit stages.
        id: collectors
        env:
          FRED_API_KEY: ${{ secrets.FRED_API_KEY }}
          FINRA_API_CLIENT_ID: ${{ secrets.FINRA_API_CLIENT_ID }}      # CCW-W5: FINRA breadth/sentiment/capped-vol (collectors/finra_corp_bonds.py); absent -> skip-clean, non-fatal
          FINRA_API_CLIENT_SECRET: ${{ secrets.FINRA_API_CLIENT_SECRET }}  # CCW-W5: see FINRA_API_CLIENT_ID
          FINNHUB_KEY: ${{ secrets.FINNHUB_KEY }}
          POLYGON_API_KEY: ${{ secrets.POLYGON_API_KEY }}   # massive.com options-OI accrual (GEX foundation)
          MASSIVE_API_KEY: ${{ secrets.MASSIVE_API_KEY }}   # accepted alias (same key, prior .env name)
          # Massive S3 flat-file entitlement — gates the whole-market daily store's
          # incremental (collectors/massive_stock_day.py). These four were only ever set
          # on the ENGINE job's builder step; their absence HERE is what froze the store
          # 2026-07-04→07-29: run_incremental() returned {"blocked": "no_creds"} every
          # night, the adapter raised, and graceful degradation swallowed it under a
          # green run (21 consecutive breaker increments in data/run_status.json).
          MASSIVE_S3_ENDPOINT: ${{ secrets.MASSIVE_S3_ENDPOINT }}
          MASSIVE_S3_ACCESS_KEY_ID: ${{ secrets.MASSIVE_S3_ACCESS_KEY_ID }}
          MASSIVE_S3_SECRET_ACCESS_KEY: ${{ secrets.MASSIVE_S3_SECRET_ACCESS_KEY }}
          MASSIVE_S3_BUCKET: ${{ secrets.MASSIVE_S3_BUCKET }}
          QUIVER_API_KEY: ${{ secrets.QUIVER_API_KEY }}     # Quiver alt-data suite (collectors/quiver.py); absent -> 'blocked', non-fatal
          GRANTS_GOV_API_KEY: ${{ secrets.GRANTS_GOV_API_KEY }}  # Simpler Grants.gov pre-award FOA flow (collectors/grants_gov.py); free Login.gov key; absent -> 'blocked', non-fatal
          FDA_API_KEY: ${{ secrets.FDA_API_KEY }}           # openFDA higher rate limit (collectors/openfda.py); OPTIONAL — keyless works at 1k/day
          HF_TOKEN: ${{ secrets.HF_TOKEN }}                 # Hugging Face higher rate limit (collectors/huggingface.py); OPTIONAL — anon tier suffices
          FINNHUB_API_KEY: ${{ secrets.FINNHUB_API_KEY }}   # Finnhub analyst/insider-MSPR/earnings trio (collectors/finnhub_altdata.py); free tier; FINNHUB_KEY also accepted
          SAM_API_KEY: ${{ secrets.SAM_API_KEY }}           # SAM.gov pre-award solicitations (collectors/sam_gov.py); free key (entity reg); absent -> 'blocked', non-fatal
          GITHUB_TOKEN: ${{ github.token }}                 # GitHub star velocity (collectors/github_repos.py); the auto-injected workflow token reads public-repo metadata at 1000/hr — no new secret needed
          TUSHARE_TOKEN: ${{ secrets.TUSHARE_TOKEN }}       # gated CN moneyflow/chips/margin plane (collectors/tushare_*.py); silently no-ops absent — was frozen 2026-06-21→ (CHINA_ENGINE_REASSESSMENT)
          AMPLIFY_FIREBASE_KEY: ${{ secrets.AMPLIFY_FIREBASE_KEY }}  # Amplify public Firestore holdings feed (collectors/etf_holdings.py); absent -> amplify funds skip, non-fatal (was a source literal — CXI tripwire 2026-07-18)
          EASTMONEY_WEB_TOKEN: ${{ secrets.EASTMONEY_WEB_TOKEN }}    # Eastmoney legacy-datacenter web token (collectors/china_property.py cgb); asia-lane collector — here for manual/full runs
          EASTMONEY_UT_TOKEN: ${{ secrets.EASTMONEY_UT_TOKEN }}      # Eastmoney push2ex ut token (collectors/china_flows.py limit_breadth); asia-lane collector — here for manual/full runs
          R2_ENDPOINT: ${{ secrets.R2_ENDPOINT }}                    # content-addressed SEC filing evidence store
          R2_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
          R2_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
          R2_BUCKET: ${{ secrets.R2_BUCKET }}
          # Activation gate for the authenticated Company Facts head. This has no
          # fallback: provision the repository secret before merging this lane.
          CAPITAL_STRUCTURE_COMPANYFACTS_HEAD_HMAC_KEY: ${{ secrets.CAPITAL_STRUCTURE_COMPANYFACTS_HEAD_HMAC_KEY }}
          # Optional dedicated guard bucket; absent uses R2_CAPITAL_STRUCTURE_BUCKET,
          # then the existing shared R2_BUCKET with the same R2 credentials.
          COMPANYFACTS_HEAD_GUARD_BUCKET: ${{ secrets.COMPANYFACTS_HEAD_GUARD_BUCKET }}
          # Optional dedicated account/bucket. Each credential falls back to the
          # shared R2_* value in source_store.py, so same-account deployments need
          # only the dedicated bucket secret while isolated accounts can set all four.
          R2_CAPITAL_STRUCTURE_ENDPOINT: ${{ secrets.R2_CAPITAL_STRUCTURE_ENDPOINT }}
          R2_CAPITAL_STRUCTURE_ACCESS_KEY_ID: ${{ secrets.R2_CAPITAL_STRUCTURE_ACCESS_KEY_ID }}
          R2_CAPITAL_STRUCTURE_SECRET_ACCESS_KEY: ${{ secrets.R2_CAPITAL_STRUCTURE_SECRET_ACCESS_KEY }}
          R2_CAPITAL_STRUCTURE_BUCKET: ${{ secrets.R2_CAPITAL_STRUCTURE_BUCKET }}
          R2_RESEARCH_ENDPOINT: ${{ secrets.R2_RESEARCH_ENDPOINT }}
          R2_RESEARCH_ACCESS_KEY_ID: ${{ secrets.R2_RESEARCH_ACCESS_KEY_ID }}
          R2_RESEARCH_SECRET_ACCESS_KEY: ${{ secrets.R2_RESEARCH_SECRET_ACCESS_KEY }}
          R2_RESEARCH_BUCKET: ${{ secrets.R2_RESEARCH_BUCKET }}
          COLLECT_LANE: nightly  # lane sentinel: forward-ledger graders (grade_breadth_divergence,
                                 # grade_policy_calendar) check COLLECT_LANE==nightly before writing;
                                 # asia-close/weekly/intl_etf leave it unset → no-op in those lanes.
        run: |
          if [ -n "$FRED_API_KEY" ]; then echo "::notice::FRED_API_KEY present — using official API"; else echo "::warning::FRED_API_KEY ABSENT — check repo Settings > Secrets and variables > Actions > Repository secrets"; fi
          # --exclude-group asia: China/HK collectors run in their own asia-close shard
          # (after the Asia cash close), not here — keeps this US-evening run lean.
          # --skip-shadow-importance: the ~13-14m shadow passes run standalone in
          # collect_tail (2026-07-14 split) — off the engine job's critical path.
          # sec_capital_structure_companyfacts is serially registered immediately
          # after sec_capital_structure in scripts.collect's shared SEC host group.
          # It can therefore consume only same-run verified complete-submission
          # anchors, while retaining raw Company Facts JSON separately and without
          # invoking any share-count consumer. Immutable Parquet generations and
          # chained receipts are installed/read back before its tiny current pointer
          # advances; any failure leaves the prior authenticated generation selected.
          # sam_gov_opportunities is excluded because government-revenue-live.yml
          # is its sole serialized ledger owner. Two workflows must never merge and
          # overwrite the same revision/document Parquets concurrently.
          python -m scripts.collect --exclude-group asia --exclude sam_gov_opportunities --skip-shadow-importance
      - name: refresh Finviz themes snapshot (subsector-rotation feed; non-fatal)
        # The only network leg for the themes map + Subsector Rotation. Refreshes
        # data/themes_heatmap/perf_snapshot.json (stamped with asof) so the rotation
        # read is fresh-daily and its forward track-record accrues a real daily
        # history. Keyless, can be rate-limited from CI IPs — strictly non-fatal:
        # on failure the build keeps the last committed snapshot.
        run: |
          python scripts/fetch_finviz_themes.py \
            || echo "::warning::Finviz themes fetch failed (non-fatal) — keeping committed snapshot"
      - name: collect ZORI national rent index (MRI shelter leg; non-fatal)
        # Fetches Zillow's public ZORI CSV (keyless) and writes data/zori/national.parquet.
        # data/zori/national.parquet is git-tracked (un-ignored by .gitignore negation);
        # metro-level parquet files under data/zori/ remain untracked (size).
        # collect_zori.py raises RuntimeError on HTTP failure — shell-level guard mandatory.
        run: |
          python scripts/collect_zori.py \
            || echo "::warning::ZORI collection failed (non-fatal) — shelter leg will use cached parquet"
      - name: backfill yahoo price coverage for the hub ledger universe (bounded, non-fatal)
        # collectors/yahoo.py fetches the CONFIG universe; the Intelligence Hub grades
        # the SIGNAL LEDGER, and every hub grader prices names from
        # data/yahoo/<T>.parquet and nothing else (engine/desk_grader.py:31,
        # engine/trajectory.py:11 — the breadth cache is split-UNADJUSTED and poisons
        # forward returns). Measured 2026-08-08: 603 of 7,711 ledger tickers had a
        # parquet, desk_grader's 20d coverage was 6.2%, and 7 of that night's 30
        # Command names could not be priced at all. This step closes that from the
        # other side, ~400 names a night (~16 nights for the addressable backlog).
        #
        # HERE, in the collect tail, for two reasons: it writes into data/yahoo, so it
        # must land before the market checkpoint below to be committed at all; and it is
        # in the COLLECT job, which is a different job from the render — nothing on the
        # 67-min render path waits on it. Bounded twice (--cap tickers and --budget-s
        # wall clock, measured 181 ms/symbol => ~102s at cap 400) plus a step timeout,
        # so a slow vendor night cannot eat the collect job's remaining budget.
        #
        # Strictly non-fatal and NOT in the market-checkpoint gate below: the backfill
        # is purely additive (new parquets for names nobody had), so a night it gets
        # nothing is a night with no new files, not a torn tree. The script exits 0 by
        # design; the `||` guard covers an import/interpreter failure it cannot catch.
        timeout-minutes: 12
        run: |
          python -m scripts.backfill_yahoo_universe --cap 400 --budget-s 480 \
            || echo "::warning::yahoo universe backfill failed (non-fatal) — hub price coverage does not advance tonight"
      - name: audit massive_stock_day store (content tripwire — loud, never blocks the night)
        # This lane runs --exclude-group asia, and scripts/collect.py skips the WHOLE
        # run_quality_audits gate on any partial run (--only/--group/--exclude-group) —
        # so the store's content audit has never executed nightly. The 2026-07 freeze
        # ran 18 sessions with zero audit coverage behind that hole. Hence its own step.
        # The module bare-prints ::error/::warning annotations and exits 1 on a content
        # fail; continue-on-error keeps the night's commit safe while the annotations
        # plus the engine job's strict audit_r2 anchor carry the alarm. Writes
        # data/quality/massive_store_audit.json, picked up by "commit market data" below.
        #
        # always() (2026-08-05): continue-on-error stops this step from failing the job,
        # but it does NOT stop an EARLIER failure from skipping it — a bare step inherits
        # success(). So on every night a veto fired (08-02 → 08-05) the store's own
        # tripwire was dark, and the freeze surfaced only 6 days later as the engine job's
        # R2 staleness anchor. The alarm must be loudest exactly on the nights the night
        # is lost. It stays continue-on-error, so this cannot red the collect job.
        if: always()
        continue-on-error: true
        env:
          COLLECT_LANE: nightly   # load-bearing: the skip path only annotates on this lane
                                  # (elsewhere an absent store is normal). Without it the
                                  # "restore failed -> store did not advance" warning — the
                                  # single loudest signal here — would never be emitted.
        run: python -m scripts.audit_massive_store
      - name: audit stocks store freshness (per-name stale-tip tripwire — loud, never blocks the night)
        # Own step for the same reason as "audit massive_stock_day store" above: this
        # lane runs --exclude-group asia, so scripts/collect.py skips the WHOLE
        # run_quality_audits gate as a partial run, leaving this audit zero nightly
        # coverage without its own step. 2026-08-03 incident: a name that exits every
        # sector SPDR's top-20 froze its data/stocks parquet forever — invisible to
        # audit_prices (interior gaps only) and check_price_store_freshness (SPY in
        # data/yahoo only). The module bare-prints a ::warning annotation and always
        # exits 0 for data findings (flags-only law, never gates the night); the ||
        # echo below only guards an unexpected crash. Writes
        # data/quality/stocks_freshness_audit.json, picked up by "commit market data" below.
        #
        # always() (2026-08-06): the SAME hole its sibling step above closed one day
        # earlier, left open one step below it. continue-on-error stops this step from
        # failing the job but does NOT stop an EARLIER failure from skipping it — a
        # bare step inherits success(). Measured on run 31056495943 (08-05 night):
        # `run collectors` succeeded at step 17, `compile capital-structure direct
        # document terms` failed at step 21, and this audit was SKIPPED at step 27 —
        # so on the four consecutive nights that veto fired (08-02 -> 08-05) the
        # per-name stale-tip tripwire was dark, which is precisely how the data/stocks
        # freeze this audit exists to catch (QCOM/HOOD 21d, MRVL 18d, CVNA/HON 14d,
        # SATS 43d) went another four nights unannounced. The alarm must be loudest
        # exactly on the nights the night is lost. It stays continue-on-error, so this
        # cannot red the collect job.
        if: always()
        continue-on-error: true
        run: |
          python -m scripts.audit_stocks_freshness \
            || echo "::warning::audit_stocks_freshness crashed (non-fatal — per-name freshness disclosure absent this run)"
      - name: audit reused tickers (zombie-print / identity-swap tripwire — loud, never blocks the night)
        # Own step for the same partial-run reason as the freshness audit above.
        # 2026-08-06 ECHO identity swap: Echo Global Logistics (taken private 2021)
        # had its ticker recycled by EchoStar (SATS->ECHO rename, eff. 2026-06-24) and
        # the store's bare-string fetch minted a fresh, continuous, internally
        # consistent EchoStar history under the dead name's key — invisible to every
        # gap/freshness/basis check. Dead-registry cross-reference + NASDAQ symbol-
        # directory presence; per-name ::warning for un-acked zombies, flags-only.
        # Writes data/quality/reused_tickers_audit.json, picked up by "commit market data".
        continue-on-error: true
        run: |
          python -m scripts.audit_reused_tickers \
            || echo "::warning::audit_reused_tickers crashed (non-fatal — reused-ticker disclosure absent this run)"
      - name: breadth OHLC basis coherence (split-basis tripwire — loud, never blocks the night)
        # 2026-08-06 incident. The breadth namespaces keep _closes_cache beside the
        # ADDITIVE _high/_low/_volume extras, and yfinance auto_adjust back-adjusts
        # ONLY the downloaded window — so a split/large dividend re-bases the fresh
        # 1mo window while the cached history keeps the old basis. Both repair paths
        # (BreadthAdapter._merge_refreshed, scripts/heal_breadth_split_seams.py) scan
        # seam_suspects over the CLOSES matrix ALONE and touch the extras only as a
        # side effect of a closes-flagged ticker — so a basis split confined to
        # high/low is invisible to every scanner in the tree. Close-only consumers
        # (breadth ratios, 200DMA, NH-NL) are unaffected, which is what makes it
        # silent; ATR's TR = max(H-L, |H-C_prev|, |L-C_prev|) is dominated by the
        # basis gap and blows up for build_pick_lab / calibrate_bottom_radar /
        # residual_momentum_phase0.
        #
        # HERE and not only in a PR pack because this is where a split is BORN: the
        # nightly collection pushes straight to main, so no PR gate can ever see the
        # commit that introduces one. This step runs AFTER the collectors and BEFORE
        # the market checkpoint, so the night's own writes are what gets judged.
        #
        # always() + continue-on-error for the same reason as the two audits above:
        # the alarm must be loudest exactly on the nights an earlier step failed, and
        # a data finding must never discard hours of collected bytes. The module
        # bare-prints ::error/::warning (a logger-prefixed annotation is dropped by
        # GitHub) and exits 3 on a split; the teeth are that annotation plus
        # data/quality/ohlc_basis_coherence.json, picked up by "commit market data"
        # below. Isolated bad bars (yfinance ex-dividend rounding — 17 such bars
        # across the three US panels at 2026-08-06) annotate as ::warning and never
        # gate, so this cannot become a scheduled red.
        if: always()
        continue-on-error: true
        run: |
          python -m scripts.check_ohlc_basis_coherence \
            || echo "::warning::check_ohlc_basis_coherence exited non-zero (see the ::error annotations above; heal with scripts/heal_breadth_split_seams.py)"
      # V4-D2B1 live-refresh seam (§7): the Data OS security-master issuer axis has no
      # scheduled producer of its own (reference.security_master stays frequency:
      # on_demand) — this is the ONE place it gets a nightly chance to advance.
      # Fail-closed inside the builder itself (scripts/build_security_master.py
      # run_nightly_refresh): refuses non-fatally on a missing/unreadable identity
      # input, is a byte-stable no-op when nothing advanced, and only re-stamps
      # generated_at when inputs genuinely changed — a source failure here can never
      # produce a falsely fresh identity generation. Always exits 0; the house
      # `|| echo "::warning..."` is belt-and-suspenders for a hard crash outside that
      # function (e.g. an import error), never load-bearing for the refresh logic
      # itself.
      - name: security master — issuer axis nightly refresh (V4-D2B1)
        continue-on-error: true
        run: |
          python3 scripts/build_security_master.py --nightly \
            || echo "::warning::build_security_master.py --nightly exited non-zero (unexpected — the function itself always returns 0; see the traceback above)"
      # Marked AFTER the coherence tripwire above so the tripwire's runtime is not
      # billed to the market-commit-push band it is measuring.
      - name: timings band — market-commit-push (W2)
        run: python3 scripts/nightly_timings.py mark --band market-commit-push || echo "::warning::timings band mark failed (non-fatal)"
      # ── MARKET CHECKPOINT (1 of 2) ──────────────────────────────────────────
      # TWO-CHECKPOINT SPLIT (2026-08-06). This job used to land the whole night in
      # ONE commit that sat AFTER the five capital-structure steps, so a
      # capital-structure defect could still cost the market plane its publish
      # window: the 2026-08-05 named-steps gate stopped a CS failure DISCARDING
      # the collection, but ~3h of collected bytes still lived only on the runner
      # for the whole length of the CS chain, one job-cap cancel or runner death
      # away from the next job's actions/checkout deleting them. Six consecutive
      # nights (2026-08-01 →) were lost through that window to four unrelated
      # defects (#4534 duck-typing, runner ENOSPC, #4600 ledger identity, #4640
      # SEC grammar) and data/stocks froze at 2026-07-31.
      #
      # So the checkpoint is now split in two, by SCOPE, in publication order:
      #   1. THIS step + "push market data": data/ and site/qledger/, EXCLUDING
      #      the capital-structure paths. It runs before a single CS step, so no
      #      capital-structure outcome can reach it — not by failing, not by
      #      hanging, not by burning the job's remaining budget.
      #   2. "commit capital-structure data" + its push, after the CS chain.
      # A partial capital-structure generation is still never committed: it is
      # carved out of THIS commit unconditionally, and its own checkpoint below
      # only runs when the whole chain returned success.
      - name: commit market data (local checkpoint — no network ops in this step)
        # NAMED-STEPS GATE (2026-08-05). This step used to carry no `if:` at all, so it
        # inherited an implicit success() and ANY earlier red discarded the night's
        # market collection — 08-02, 08-03 (collectors duck-typing, #4560), 08-04 (host
        # disk), 08-05 (capital-structure ManifestIdentityError). Four nights lost to
        # three causes, none of which had anything to do with whether the PRICE data on
        # disk was sound. The 2026-08-06 split above removes the last of those three
        # from this step's reach entirely; the gate stays because the gate is what
        # keeps a SKIPPED or half-run producer from being checkpointed.
        #
        # A bare always() is NOT the fix and is forbidden here: on 2026-08-04 an
        # always() commit step outran its SKIPPED normalizers and committed raw
        # un-normalized pages sitewide (P0). A skipped producer must block exactly like
        # a failed one, which is why this gates on `.outcome == 'success'` — outcome is
        # the raw step result, so 'skipped' and 'cancelled' both block, and unlike
        # `.conclusion` it is not laundered by a continue-on-error.
        #
        # STAGED PATH -> PRODUCING STEP  (the mapping this gate is derived from; extend
        # it when you add a step that WRITES into the staged tree):
        #
        #   data/**                    -> `run collectors`  (id: collectors)  GATES
        #     the whole market plane: data/stocks, data/baskets/ohlcv, breadth close
        #     caches, macro series, ~228 adapters. The only bulk producer here.
        #   site/qledger/track_record.json
        #                              -> `run collectors`  (grade_qledger)   GATES
        #   data/capital_structure/**  -> NOT STAGED HERE. `run collectors`
        #     (sec_capital_structure) rewrites source_manifest.jsonl inside the broad
        #     `data/` add, so it is unstaged again below and belongs to the SECOND
        #     checkpoint, after its compilers have accepted it.
        #   data/themes_heatmap/perf_snapshot.json
        #                              -> `refresh Finviz themes`   does NOT gate
        #   data/zori/national.parquet -> `collect ZORI`             does NOT gate
        #     Both are self-guarded (`|| echo ::warning::`) and purely additive: a miss
        #     re-commits the previous snapshot, which is a no-op diff, not a torn tree.
        #   data/yahoo/<T>.parquet (NEW names only) + data/yahoo_backfill_state.json
        #                              -> `backfill yahoo price coverage`  does NOT gate
        #     Same reason: bounded, self-guarded and additive — it only ever CREATES a
        #     parquet for a ledger ticker that had none, so a night it gets nothing
        #     stages nothing. It must never gate the market plane, which is collected
        #     by `run collectors` above and has no dependency on it.
        #   data/quality/massive_store_audit.json
        #                              -> `audit massive_stock_day store`  does NOT gate
        #     A tripwire REPORTS on the tree; it does not produce it. Gating on an alarm
        #     would let the alarm delete the evidence.
        #
        # NOT in the gate, deliberately:
        #   * the whole capital-structure chain — it has not RUN yet. Since the
        #     2026-08-06 split every one of those steps sits BELOW this checkpoint, so
        #     `steps.cs_*.outcome` here would be the empty string on every night. That
        #     is the point of the split: the market plane is published before the CS
        #     lane can fail, hang, or eat the job's remaining budget. Its paths are
        #     unstaged below and ride their own checkpoint.
        #   * collect_tail — a SEPARATE JOB with its own commit; nothing it produces is
        #     staged here (site/basketdata moved with it in the 2026-07-14 split), and
        #     cross-job step outcomes are not referenceable in an `if:` anyway.
        if: always() && steps.collectors.outcome == 'success'
        id: commitdata
        run: |
          echo "committed=false" >> "$GITHUB_OUTPUT"
          git config user.name "dashboard-bot"
          git config user.email "actions@users.noreply.github.com"
          # site/qledger: the qledger grader (scripts/collect.py -> grade_qledger)
          # writes the TRACKED site/qledger/track_record.json (un-ignored by #1139).
          # Left unstaged it blocks `git pull --rebase` ("unstaged changes") and the
          # whole data push silently dies after 5 attempts — 07-03/07-04 collections
          # were lost exactly this way (the sentinel staging-gap pattern). The
          # --autostash on the rebase below is the durable guard: it survives ANY
          # stray tracked write collect leaves outside data/+site/qledger/ (e.g.
          # reports/*.md graders), not just the qledger paths we happen to stage here.
          # (site/basketdata moved with the TIL steps to collect_tail's commit —
          # 2026-07-14 split; nothing left in this job writes site/basketdata.)
          git add data/ site/qledger/
          # W0b (2026-07-08 stale-HK incident): US collect job must NOT commit asia-owned
          # data that it never produced. The GHA cache restore steps above may lay stale
          # data/hk_stocks (restore-keys: hk-stocks-ohlc-) and breadth caches on top of
          # the fresh files asia-close already committed.  After the broad `git add data/`
          # above, unstage every asia-close-owned path so -X theirs on the rebase-push
          # cannot clobber asia's fresh commit with this job's stale copy.
          # Derived from asia-close.yml: collect --group asia = adapters starting with
          # `china` or `hk` (scripts/collect.py group_members), so excludes cover all
          # data/china*, data/hk*, and data/hkma (adapter key `hkma` starts with `hk`).
          git reset -q -- data/hk data/hk_* data/hkma data/china data/china_* 2>/dev/null || true
          # Capital-structure SCOPE CARVE-OUT — same idiom as the asia unstage above.
          # UNCONDITIONAL since the 2026-08-06 split: none of the five capital-structure
          # steps has run yet at this point, so there is no outcome to test and nothing
          # here could be a complete generation. `run collectors`
          # (sec_capital_structure) has already rewritten the TRACKED
          # source_manifest.jsonl, which the broad `git add data/` above swept in — a
          # source ledger no compiler has accepted. Drop it; the second checkpoint
          # commits it once the whole chain has validated it.
          #
          # `git reset` ONLY — never `git checkout`. The pre-2026-08-06 carve-out fired
          # AFTER a failed chain and restored the clean committed ledger (#4600's
          # self-heal). Here the chain has not run, and that freshly-collected ledger is
          # its INPUT: reverting it would feed the spine yesterday's manifests every
          # single night. Unstage, do not revert.
          git reset -q -- data/capital_structure site/capital-structure-data 2>/dev/null || true
          if git diff --cached --quiet; then echo "no market data changes to commit"; exit 0; fi
          git commit -m "data: daily collection $(date -u +%F)"
          echo "committed=true" >> "$GITHUB_OUTPUT"
      - name: salvage unrecoverable chain snapshots (collectors red — market commit skipped)
        # THE HALF THE NAMED-STEPS GATE ABOVE CANNOT COVER (2026-08-06 postmortem).
        # That gate is right for the bulk plane and must not be loosened, and the
        # 2026-08-06 checkpoint SPLIT already removed the capital-structure chain's
        # ability to veto it. What neither closes is a red `run collectors` itself:
        # the market commit is still gated on `collectors == 'success'`, and that
        # costs the ONE thing in data/ that no later run can rebuild.
        # data/cboe/{putcall,gex*}.parquet are one-row-per-session snapshots of a
        # delayed-quotes endpoint that serves only the LIVE book. Every other store
        # re-fetches its history, so a skipped commit costs them nothing; these twelve
        # are deleted forever by the next run's clean checkout.
        #
        # Measured, not hypothetical — 2026-08-03 (job 91848095334): gex and all ten
        # symbols RECOVERED on the post-cooldown sweep (`cboe_gex -> ok (11 rows, last
        # 2026-08-03)`), then `run collectors` died on `AttributeError:
        # 'NYGamingAdapter' object has no attribute 'fetch_result_status'` — a bug in
        # an unrelated adapter's duck-typing — and every one of those rows was gone the
        # next evening. The board regime panel, conditions P/C and the gex_flip_cross
        # alert have been computing across that hole since.
        #
        # This is NOT the forbidden bare always() commit: the 08-04 P0 was a BULK tree
        # committed ahead of its SKIPPED normalizers. Nothing is downstream of these
        # files — the adapters write the final artifact directly — and the selector
        # (scripts/ci/chain_snapshot_salvage.py) stages only the declared chain family,
        # each file individually re-read and required to tip on a real NYSE session, so
        # a parquet torn by a mid-write crash is dropped rather than published.
        # Runs only when the market commit did NOT, so the two can never race.
        if: always() && steps.collectors.outcome != 'success'
        run: |
          git config user.name "dashboard-bot"
          git config user.email "actions@users.noreply.github.com"
          SAFE=$(python -m scripts.ci.chain_snapshot_salvage . 2>/dev/null) || SAFE=""
          if [ -z "$SAFE" ]; then
            echo "no salvageable chain snapshots on disk"; exit 0
          fi
          echo "$SAFE" | while IFS= read -r f; do [ -n "$f" ] && git add -- "$f"; done
          if git diff --cached --quiet; then
            echo "chain snapshots already committed — nothing to salvage"; exit 0
          fi
          SAVED=$(git diff --cached --name-only | tr '\n' ' ')
          git commit -m "data: salvage unrecoverable cboe chain snapshots $(date -u +%F)" || exit 0
          # Bare line-start print: a logger/indent would push "::" off column 0 and
          # GitHub would drop the annotation (tests/test_gh_annotation_line_start.py).
          printf '::warning title=chain-snapshot-salvage::%s\n' "collect was red so the night's data commit was skipped; salvaged the unbackfillable delayed-chain snapshots only: $SAVED"
          . "${GITHUB_WORKSPACE:-.}/scripts/ci/push_retry.sh"
          PUSH_ALARM=420
          push_retry_init "salvaged chain snapshots"
          while push_attempt; do
            perl -e 'alarm 120; exec @ARGV or die' -- git fetch origin main || true
            if perl -e 'alarm 420; exec @ARGV or die' -- git pull --rebase --autostash -X theirs origin main && push_autostash_ok; then
              if push_do; then echo "pushed salvaged chain snapshots"; push_won; exit 0; fi
            fi
            push_abort_rebase
            push_backoff
          done
          push_lost
          printf '::warning title=chain-snapshot-salvage::%s\n' "could not push the salvaged chain snapshots after $PUSH_ATTEMPT attempts ($PUSH_STOP) — they exist only on this runner and the next checkout will delete them"
      # COMMIT/PUSH SPLIT (2026-07-17, run 29542087837 postmortem): the local commit above
      # is atomic and integrity-complete (fully-staged tree, asia paths reset out, empty-
      # guarded) — everything below is pure network publish. It lives in a SEPARATE
      # always()-guarded step so a job-timeout cancel during the push can never again
      # discard an already-committed night of collections (a hung git push ate 57m and
      # the 150m cap killed the step mid-publish; the commit existed only on the runner).
      # Every git network op is alarm-bounded (macOS runners have no GNU timeout;
      # perl alarm survives execve — SIGALRM kills a hung op, the loop retries).
      - name: push market data (survives a job-cap cancel; alarm-bounded git ops)
        id: pushdata
        if: always() && steps.commitdata.outputs.committed == 'true'
        run: |
          echo "published=false" >> "$GITHUB_OUTPUT"
          . "${GITHUB_WORKSPACE:-.}/scripts/ci/push_retry.sh"
          # best-effort push: losing a race with a concurrent push must NOT fail the
          # pipeline (the data is collected; the next run re-syncs). Retry rebase+push,
          # preferring our freshly-collected data on any conflict.
          #
          # The capital-structure paths carved out of the commit above are DIRTY here,
          # and --autostash parks them across the rebase. On a clean re-apply the
          # compilers below read the ledger the collectors just wrote — the normal
          # night. On a conflicted re-apply push_autostash_ok resets hard and retries,
          # so the compilers read origin/main's committed ledger instead. Both are
          # sound inputs: the second is exactly #4600's restore-a-clean-ledger self-heal.
          PUSH_ALARM=420
          push_retry_init "collected market data"
          while push_attempt; do
            # pre-sync collision sweep (render.yml / #2252): a mid-run commit on origin/main can
            # TRACK a path this job wrote as an UNTRACKED side-effect — the rebase checkout then
            # refuses ("untracked working tree files would be overwritten") and --autostash can't
            # clear it (it parks tracked modifications only), so every retry dies identically.
            # Everything this step publishes is already committed above, so a path untracked HERE
            # but tracked on origin/main is a throwaway local write — origin/main is authoritative.
            # Gitignored files and untracked paths origin/main doesn't track never enter the
            # intersection (runner-local stores and caches are untouched).
            # (Asia paths unstaged above: when newly tracked on main, deleting the local stale
            # copy lets the rebase materialize asia-close's fresh commit — the W0b intent.)
            perl -e 'alarm 120; exec @ARGV or die' -- git fetch origin main || true
            comm -12 <(git ls-files --others --exclude-standard | LC_ALL=C sort) \
                     <(git ls-tree -r --name-only origin/main | LC_ALL=C sort) \
              | while IFS= read -r f; do rm -f -- "$f" || true; done
            # Append-only base freshness (2026-08-18 lost update, DSC-OVERLAPPING-
            # DAILY-COLLECT-JOBS-LOSE-APPEND-ONLY-ROWS): two collect jobs that OVERLAP
            # each build data/government_revenue/ from the base each checked out, and
            # `-X theirs` below then REPLACES the earlier run's appended rows with this
            # run's instead of unioning them. Runs after the fetch and before the rebase
            # because that is the only point where origin/main's version is known and
            # this run's commit is still withholdable. No-op on an ordinary night.
            # Non-zero ONLY when the fence proved a loss and then could not withhold it;
            # publishing that tree is the corruption the fence exists to stop, so skip the
            # attempt rather than fail open. Every other fence outcome returns 0.
            if ! push_append_only_fence origin/main; then push_backoff; continue; fi
            if perl -e 'alarm 420; exec @ARGV or die' -- git pull --rebase --autostash -X theirs origin main && push_autostash_ok; then
              if push_do; then
                echo "pushed market data on attempt $PUSH_ATTEMPT"
                echo "published=true" >> "$GITHUB_OUTPUT"
                push_won
                exit 0
              fi
            fi
            push_abort_rebase
            push_backoff
          done
          push_lost
          echo "::warning::could not push collected market data after $PUSH_ATTEMPT attempts ($PUSH_STOP) (non-fatal)"
      # Last-ditch publish if the job was CANCELLED mid-push (cap timeout): one bounded
      # attempt inside the post-cancel grace window. The commit already exists locally;
      # losing it costs the whole night's collections (2026-07-16 incident).
      - name: salvage push (job cancelled mid-publish — one bounded attempt)
        if: cancelled() && steps.commitdata.outputs.committed == 'true'
        run: |
          . "${GITHUB_WORKSPACE:-.}/scripts/ci/push_retry.sh"
          perl -e 'alarm 90; exec @ARGV or die' -- git fetch origin main || true
          # Same append-only base fence as the main push loop above; a cancel-window
          # salvage must not be the hole that publishes a stale-base ledger.
          # A failed withhold ends the salvage: this step is a best-effort grace-window
          # publish, and publishing a tree that drops evidence is worse than not salvaging.
          push_append_only_fence origin/main || {
            echo "::error title=append-only-base-fence::withhold failed — abandoning the salvage push rather than publishing a tree that drops evidence"
            exit 0
          }
          { perl -e 'alarm 180; exec @ARGV or die' -- git pull --rebase --autostash -X theirs origin main && push_autostash_ok; } || true
          perl -e 'alarm 120; exec @ARGV or die' -- git push \
            && echo "salvage push landed the collections" \
            || echo "::warning::salvage push failed — tonight's collections stay uncommitted"
      # ── CAPITAL-STRUCTURE HANDOFF (the chain itself moved out 2026-08-06) ───
      # The five capital-structure steps, their diagnostics capture, and their
      # commit/push checkpoint used to run HERE. They now run in the
      # `capital_structure` job below (needs: collect). WHY: the lane is fatal by
      # design — an all-or-nothing SEC publication protocol whose compilers must
      # never be given continue-on-error — and a fatal-by-design chain inside the
      # one job the whole nightly hangs off redded the run on three consecutive
      # nights for three unrelated causes (#4600 08-04, #4640 08-05, #4740 08-06).
      #
      # WHAT HAS TO CROSS THE JOB BOUNDARY, AND WHY IT CANNOT CROSS VIA GIT:
      # `run collectors` (sec_capital_structure) APPENDS tonight's rows to the
      # TRACKED source_manifest.jsonl, and the market checkpoint above unstages
      # exactly that path — a source ledger no compiler has accepted must not be
      # committed, and that carve-out is what preserves #4600's self-heal. So the
      # freshly-appended rows exist ONLY in this runner's workspace. A fresh
      # `actions/checkout` one job over would silently restore the last COMMITTED
      # ledger instead, the compilers would reproduce yesterday byte-for-byte,
      # "no capital-structure changes to commit" would print, and the lane would
      # freeze forever while every step stayed green. Measured 2026-08-06: 1,258
      # rows committed, 1,447 in the run — 189 rows a night lost, their retained
      # R2 bytes orphaned. The receipt below is what makes that unfalsifiable.
      #
      # BOTH steps are continue-on-error, deliberately: the market plane is
      # already committed AND pushed, and the entire point of the split is that
      # nothing about the capital-structure lane may red this job. A failed stamp
      # or upload is NOT silently absorbed — it fails CLOSED one job over, where
      # `verify the capital-structure handoff` refuses to compile a ledger it
      # cannot prove is tonight's.
      - name: stamp the capital-structure handoff receipt (no network ops in this step)
        id: cs_handoff_stamp
        continue-on-error: true
        run: |
          ledger="data/capital_structure/source_manifest.jsonl"
          if [ ! -f "$ledger" ]; then
            echo "::warning::no $ledger to hand off — the capital_structure job will fail closed rather than compile the committed ledger"
            exit 0
          fi
          # Stamped HERE, after "push market data", deliberately: that step's rebase
          # runs --autostash, and on a CONFLICTED re-apply push_autostash_ok resets
          # hard, leaving origin/main's committed ledger in the tree. That is still a
          # sound input (#4600's restore-a-clean-ledger self-heal) but it is a
          # DIFFERENT ledger, so the receipt must describe whatever actually survived
          # rather than what the collectors wrote 20 minutes earlier.
          if command -v shasum >/dev/null 2>&1; then
            sha="$(shasum -a 256 "$ledger" | cut -d' ' -f1)"
          else
            sha="$(sha256sum "$ledger" | cut -d' ' -f1)"
          fi
          # JSONL, newline-terminated by contract (decode_source_ledger rejects a
          # partial final line), so `wc -l` IS the row count.
          rows="$(wc -l < "$ledger" | tr -d ' ')"
          echo "sha256=$sha" >> "$GITHUB_OUTPUT"
          echo "rows=$rows" >> "$GITHUB_OUTPUT"
          echo "capital-structure handoff stamped: $rows rows, sha256 $sha"
      - name: hand the capital-structure workspace to its own job
        continue-on-error: true
        # actions/upload-artifact v4.6.2 — the pin already used by the diagnostics
        # capture that moved to the capital_structure job.
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        with:
          # RUN-scoped, deliberately NOT attempt-scoped. "Re-run failed jobs"
          # increments github.run_attempt for the whole run, so an attempt-scoped
          # name would make the capital_structure job impossible to re-run on its
          # own: attempt 2 would look for an artifact only attempt 1 ever
          # uploaded, and the download would fail every time. That is the single
          # most likely thing an operator will want to do with this lane — it is
          # the one that failed three nights running.
          #
          # `overwrite` covers the other direction: re-running the WHOLE workflow
          # re-runs collect, which must be able to replace its own upload rather
          # than fail on the duplicate name. The receipt stays consistent either
          # way, because needs.collect.outputs and the artifact both come from
          # whichever collect execution is current.
          name: capital-structure-workspace-${{ github.run_id }}
          overwrite: true
          # The gitignored subtrees are excluded BY NAME, not left to luck: a retained
          # local source store would otherwise turn a 4 MB handoff into hundreds of MB.
          # CAPITAL_STRUCTURE_LOCAL_STORE is unset in this workflow and all 1,447
          # manifests in the 2026-08-06 run were retained on R2 (1,258 r2_shared, 189
          # r2_research, zero local-store), so these are empty today — the excludes
          # keep them empty by CONTRACT rather than by accident.
          path: |
            data/capital_structure
            !data/capital_structure/source_objects
            !data/capital_structure/share_counts
            !data/capital_structure/*.tmp.parquet
          # An empty handoff is a real defect, not a quiet no-op: fail the upload so
          # the receipt is absent and the other job refuses to compile.
          if-no-files-found: error
          # Machine state in flight between two jobs of one run — never an archive.
          retention-days: 1
      - name: timings band — r2-publish (W2)
        run: python3 scripts/nightly_timings.py mark --band r2-publish || echo "::warning::timings band mark failed (non-fatal)"
      - name: publish attention store back to R2 (gated on the restore)
        # The day's upserted increments ride back to R2 so tomorrow's restore (and the
        # engine job's render-side restore) reads a CURRENT deep store — a frozen R2
        # copy would slowly starve the chip's trailing-5d window. Gated on the restore
        # step's outcome: if the restore failed, the local tree is a shallow ~120d
        # rebuild whose file count matches the deep store, and publishing it would
        # clobber 11y of history. Gaps self-heal: the collector's ~120d fetch window
        # re-covers any missed days on the next successful cycle. Runs AFTER "commit
        # market data" so the commit is never delayed by the upload. Non-fatal.
        # always(): see the massive_stock_day publish below — an `if:` carrying no status
        # function gets an implicit success(), so ANY earlier red in this ~2h job silently
        # skips the publish. The restore gate is the real fence and it is preserved.
        if: always() && steps.attention_restore.outcome == 'success'
        env:
          R2_ENDPOINT: ${{ secrets.R2_ENDPOINT }}
          R2_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
          R2_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
          R2_BUCKET: ${{ secrets.R2_BUCKET }}
        run: python -m scripts.publish_r2 --dirs attention || echo "::warning::attention R2 publish failed (non-fatal — R2 store goes stale, chip self-heals within the 120d fetch window)"
      - name: publish massive_stock_day store back to R2 (gated on the restore)
        # The incremental's delta rides back to the canonical copy (content-hash skip, so
        # only tonight's new/changed parquets upload). The manifest object is put LAST and
        # unconditionally on a full publish — that Last-Modified is exactly what the
        # engine job's audit_r2 anchor keys on, so this step is what keeps that anchor
        # green. Gated on massive_restore for the same reason as attention above: a
        # shallow or partial local tree must never overwrite the deep store, and here the
        # collector's store-absent fence means such a tree would not even have advanced.
        # Runs after "commit market data" so the upload never delays the commit. Non-fatal.
        #
        # always() (2026-08-05, run 30960328285): an `if:` containing NO status-check
        # function gets an implicit success(), so this step was skipped by ANY earlier
        # red in the ~3h collect job. It has therefore NEVER run: added 2026-08-01
        # (#4013), eligible on exactly three nights, and skipped all three — 08-02/08-03
        # on the collectors duck-typing crash (#4560), 08-05 on the capital-structure
        # ManifestIdentityError (#4600). That last night the collector had already spent
        # 1,898s advancing the store to latest=2026-08-03; the skip discarded all of it
        # and left the R2 anchor stuck at 2026-07-30T03:08Z / content 2026-07-28.
        #
        # The deliberate veto set (tests/test_daily_collect_commit_path.py) exists to
        # stop a PARTIAL capital-structure generation from being COMMITTED. That is a
        # git-checkpoint law and it stands. It is not a reason to also throw away an
        # unrelated R2 store's completed advance: publish_r2 is upsert-only, never
        # prunes remote keys, and PUTs the anchor manifest last and only after every
        # file lands. The restore gate below remains the real fence against clobbering
        # the deep store with a shallow tree.
        if: always() && steps.massive_restore.outcome == 'success'
        env:
          R2_ENDPOINT: ${{ secrets.R2_ENDPOINT }}
          R2_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
          R2_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
          R2_BUCKET: ${{ secrets.R2_BUCKET }}
        run: python -m scripts.publish_r2 --dirs massive_stock_day || echo "::warning::massive_stock_day R2 publish failed (non-fatal — R2 copy goes stale; the engine job's strict audit_r2 anchor goes red within 26h)"
      # W2: per-band timings row + >85%-of-cap tripwire. always() so a job-cap cancel
      # still records the night in the grace window (elapsed ≈ cap → the warning fires).
      - name: timings ledger + 85% budget tripwire (W2)
        if: always()
        run: bash scripts/ci/nightly_timings_finish.sh 240

  # ── CAPITAL-STRUCTURE LANE (its own job since 2026-08-06) ─────────────────
  # WHY ITS OWN JOB. This is an all-or-nothing SEC publication protocol. Its
  # compilers are FATAL BY DESIGN and must never be given continue-on-error, a
  # quarantine, or a fail-soft: a partial generation must not be committed, and
  # #4578 tried continue-on-error on the spine and was correctly reverted. That
  # contract is right for this lane and catastrophic where the lane used to sit —
  # inside `collect`, the one job the whole nightly hangs off. Three DISTINCT
  # capital-structure defects redded the entire nightly on three consecutive
  # nights. Every fix was correct; the PLACEMENT was the defect:
  #   2026-08-04  #4600  pyarrow schema unification broke manifest identity
  #   2026-08-05  #4640  the SEC-HEADER opener grammar EDGAR actually emits
  #   2026-08-06  #4740  observation-lineage bytes
  #
  # BLAST RADIUS, before -> after (stated exactly, not rounded up):
  #   BEFORE: `collect` is needs:-ed by government_revenue_projection,
  #     collect_tail, engine, factor_series and active_build_map — and
  #     transitively by factor_panel (under factor_series) and by everything
  #     under engine: oracle_offrender, tech_lab_offrender, standout_audit_us,
  #     us_scan_tier, stock_briefs, cortex, causal, publish, ledger_heartbeat.
  #     Those siblings all carry `if: always()`, which DNR:KILL-NIGHTLY-HARD-GATE
  #     requires, so a red `collect` did NOT skip them. What a capital-structure
  #     defect DID cost is still three things:
  #       1. the nightly RUN's conclusion — red, three nights running;
  #       2. government_revenue_projection SKIPPED — its `if:` names a collect
  #          OUTPUT and carries no status function, so GitHub ANDs an implicit
  #          success() onto it and a red collect skips it;
  #       3. every `needs: collect` job WAITING OUT the chain before it starts —
  #          20.8 min on 2026-08-06 (run 31067383446) before it failed, in front
  #          of an engine job that is chronically at its cap.
  #   AFTER: NOTHING `needs:` this job. A capital-structure failure reds exactly
  #     this job and reaches nothing else. Pinned from both directions by
  #     tests/test_daily_capital_structure_job.py.
  #
  # THIS IS NOT the pattern DNR:KILL-NIGHTLY-HARD-GATE forbids. That kill is
  # about hard-gating the DASHBOARD jobs (skipping engine/publish when collect
  # cancels), where partial output beats shipping nothing. Here the default
  # `needs:` success semantics are the SAME fence the steps already had — a bare
  # step inherits an implicit success(), so any earlier red in `collect` skipped
  # them — and they are load-bearing: without a verified handoff there is no
  # ledger to compile, and compiling the stale committed one is precisely the
  # silent-freeze mode the receipt exists to prevent. No dashboard job is gated
  # on this one at all, so nothing here can fail-dark the site.
  capital_structure:
    # ET gate (see the schedule comment): reached TRANSITIVELY, not by wiring.
    # This lane carries no `if:` at all — the implicit success() on `needs: collect`
    # is the verified-handoff fence, pinned by tests/test_daily_capital_structure_job.py
    # (DNR:KILL-NIGHTLY-HARD-GATE) — so an off-regime firing, which skips `collect`,
    # skips this job too by needs-status. Wiring et_gate in directly would both break
    # that fence and invent a failure mode: a gate ERROR fails OPEN into collect, and
    # this job would then be the one lane that skipped the night. Do not add an
    # always() here — it would sever the skip propagation this depends on.
    needs: collect
    runs-on: [self-hosted, macstudio]
    # Measured 2026-08-06 (run 31067383446, `collect`): event spine 3s;
    # document-terms compiler 20.8m before it failed on #4740 — a full successful
    # pass reads every new complete submission back from R2, so it is longer, not
    # shorter. Add checkout of the ~14k-file tree (observed 3s..14.5m, see the
    # engine job's timeout comment), venv+pip, and the var-gated share-count
    # materializer's own 15m30s process ceiling if it is ever switched on. 90m is
    # ~3x the observed chain with cold-runner headroom — the macstudio label can
    # fail over to a cold spare, which is what made collect's collectors step take
    # 172m on 2026-08-05. Nothing waits on this job, so a generous cap costs
    # nothing on a healthy night and buys the commit on a slow one.
    timeout-minutes: 90
    steps:
      # W2 runtime telemetry (research/NIGHTLY_RESILIENCE_AND_LIVE_TRANSITION_MASTERPLAN_2026-08-06.md
      # §A2): stamp the job's wall-clock start BEFORE checkout, same two steps as every other
      # self-hosted job here (tests/test_nightly_timings.py pins the wiring + cap args).
      - name: timings — job start mark (W2)
        run: date +%s > "${RUNNER_TEMP}/nightly-timings-${GITHUB_RUN_ID}-${GITHUB_JOB}-start"
      - uses: actions/checkout@v4
        with:
          ref: main
      # ref: main + an explicit pull, the us_scan_tier / us_prophet_ledgers idiom:
      # the tree must already contain the market commit `collect` just pushed.
      - run: git pull origin main
      - name: python 3.12 (Homebrew venv — self-hosted runner)
        run: |
          VENV="$HOME/.cache/mm-venv-${RUNNER_NAME:-default}"
          [ -x "$VENV/bin/python" ] || /opt/homebrew/bin/python3.12 -m venv "$VENV"
          echo "$VENV/bin" >> "$GITHUB_PATH"
          "$VENV/bin/python" -m pip install --quiet --upgrade pip
      - run: pip install -r requirements.txt
      # ── THE HANDOFF ─────────────────────────────────────────────────────────
      # Downloaded AFTER the checkout and the pull, never before: both would
      # overwrite the ledger with the committed copy, which is exactly the
      # silent-freeze mode below.
      - name: download the capital-structure workspace handed off by collect
        # actions/download-artifact v4.3.0 — the v4 series that pairs with the
        # v4.6.2 upload pin in `collect`. Fail-closed by default: a missing
        # artifact fails this step rather than leaving the checkout's committed
        # ledger in place.
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093
        with:
          # Must match collect's RUN-scoped name exactly — see the upload step for
          # why github.run_attempt is deliberately absent from both.
          name: capital-structure-workspace-${{ github.run_id }}
          path: data/capital_structure
      - name: verify the capital-structure handoff (fail closed — never compile a stale ledger)
        # THE guard of this split. `collect` writes tonight's manifest rows into a
        # TRACKED file that its market checkpoint deliberately unstages, so those
        # rows reach this job only through the artifact above. If the artifact were
        # missing, mis-pathed, or landed under the checkout instead of over it, the
        # compilers would silently read the LAST COMMITTED ledger, reproduce
        # yesterday byte-for-byte, commit nothing, and freeze the lane while every
        # step in this job stayed green. Comparing the bytes against the receipt
        # `collect` stamped is what makes that impossible rather than unlikely.
        #
        # It is also the producer this job's checkpoint gates on (`cs_handoff`),
        # standing where `steps.collectors.outcome` stood before the split.
        id: cs_handoff
        env:
          EXPECTED_SHA: ${{ needs.collect.outputs.capital_structure_ledger_sha256 }}
          EXPECTED_ROWS: ${{ needs.collect.outputs.capital_structure_ledger_rows }}
        run: |
          set -euo pipefail
          ledger="data/capital_structure/source_manifest.jsonl"
          if [ -z "${EXPECTED_SHA:-}" ]; then
            echo "::error title=capital-structure-handoff::collect published no ledger receipt. Its stamp and upload steps are continue-on-error by design (they must never red the market plane), so their failure surfaces HERE. Refusing to compile: without the receipt this job cannot tell tonight's ledger from the committed one."
            exit 1
          fi
          if [ ! -f "$ledger" ]; then
            echo "::error title=capital-structure-handoff::$ledger is absent after the artifact download."
            exit 1
          fi
          if command -v shasum >/dev/null 2>&1; then
            actual_sha="$(shasum -a 256 "$ledger" | cut -d' ' -f1)"
          else
            actual_sha="$(sha256sum "$ledger" | cut -d' ' -f1)"
          fi
          actual_rows="$(wc -l < "$ledger" | tr -d ' ')"
          if [ "$actual_sha" != "$EXPECTED_SHA" ]; then
            echo "::error title=capital-structure-handoff::ledger mismatch — collect stamped ${EXPECTED_ROWS} rows / ${EXPECTED_SHA}, this tree holds ${actual_rows} rows / ${actual_sha}. The likeliest cause is that the artifact did not land ON TOP of the checkout, leaving the last COMMITTED ledger in place; compiling that would reproduce yesterday byte-for-byte, commit nothing, and freeze the lane green."
            exit 1
          fi
          echo "capital-structure handoff verified: $actual_rows rows, sha256 $actual_sha"
      - name: timings band — capital-structure (W2)
        run: python3 scripts/nightly_timings.py mark --band capital-structure || echo "::warning::timings band mark failed (non-fatal)"
      - name: materialize authenticated capital-structure share counts (context-only)
        # PRE-PRODUCTION GATE: leave false until publication protocol v2 and the
        # bounded full-generation retention lane pass their release audits. A
        # skipped step receives no R2/HMAC secrets and performs no write.
        if: ${{ vars.CAPITAL_STRUCTURE_SHARE_COUNT_PUBLICATION_ENABLED == 'true' }}
        # Re-authenticates the selected Company Facts receipt/head and reads exact
        # retained bytes through their manifest-bound R2 namespace. A separate
        # signed share-count head makes this an all-or-nothing publication lane;
        # missing trust, source mismatch, or partial progress blocks the checkpoint.
        # This step observes direct SEC facts only. It cannot select a current share
        # basis, calculate fully diluted supply, score risk, or influence Prophet.
        env:
          R2_ENDPOINT: ${{ secrets.R2_ENDPOINT }}
          R2_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
          R2_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
          R2_BUCKET: ${{ secrets.R2_BUCKET }}
          R2_CAPITAL_STRUCTURE_ENDPOINT: ${{ secrets.R2_CAPITAL_STRUCTURE_ENDPOINT }}
          R2_CAPITAL_STRUCTURE_ACCESS_KEY_ID: ${{ secrets.R2_CAPITAL_STRUCTURE_ACCESS_KEY_ID }}
          R2_CAPITAL_STRUCTURE_SECRET_ACCESS_KEY: ${{ secrets.R2_CAPITAL_STRUCTURE_SECRET_ACCESS_KEY }}
          R2_CAPITAL_STRUCTURE_BUCKET: ${{ secrets.R2_CAPITAL_STRUCTURE_BUCKET }}
          R2_RESEARCH_ENDPOINT: ${{ secrets.R2_RESEARCH_ENDPOINT }}
          R2_RESEARCH_ACCESS_KEY_ID: ${{ secrets.R2_RESEARCH_ACCESS_KEY_ID }}
          R2_RESEARCH_SECRET_ACCESS_KEY: ${{ secrets.R2_RESEARCH_SECRET_ACCESS_KEY }}
          R2_RESEARCH_BUCKET: ${{ secrets.R2_RESEARCH_BUCKET }}
          CAPITAL_STRUCTURE_COMPANYFACTS_HEAD_HMAC_KEY: ${{ secrets.CAPITAL_STRUCTURE_COMPANYFACTS_HEAD_HMAC_KEY }}
          COMPANYFACTS_HEAD_GUARD_BUCKET: ${{ secrets.COMPANYFACTS_HEAD_GUARD_BUCKET }}
          CAPITAL_STRUCTURE_SHARE_COUNT_HEAD_HMAC_KEY: ${{ secrets.CAPITAL_STRUCTURE_SHARE_COUNT_HEAD_HMAC_KEY }}
          SHARE_COUNT_HEAD_GUARD_BUCKET: ${{ secrets.SHARE_COUNT_HEAD_GUARD_BUCKET }}
          SHARE_COUNT_HEAD_GUARD_ACCOUNT_ID: ${{ secrets.SHARE_COUNT_HEAD_GUARD_ACCOUNT_ID }}
          # Migration-only switches. Both default false and the engine rejects
          # a run with both true before trust/client construction. V3 can only
          # install its same-selection fence; V4 can only structurally migrate
          # one clean v2/v3 head. Neither switch enables native publication.
          CAPITAL_STRUCTURE_SHARE_COUNT_HEAD_V3_MIGRATION_ENABLED: ${{ vars.CAPITAL_STRUCTURE_SHARE_COUNT_HEAD_V3_MIGRATION_ENABLED || 'false' }}
          CAPITAL_STRUCTURE_SHARE_COUNT_HEAD_V4_MIGRATION_ENABLED: ${{ vars.CAPITAL_STRUCTURE_SHARE_COUNT_HEAD_V4_MIGRATION_ENABLED || 'false' }}
        run: |
          # The in-process bridge has one 15-minute monotonic deadline.  R2
          # strict reads cannot accept that remaining budget, so Linux runners
          # add a process-level GNU timeout with 30 seconds for the bridge to
          # return its fail-closed result before TERM, then a 30-second KILL
          # backstop for a blocking SDK call.  The Mac Studio has no GNU
          # timeout; Perl alarm replaces itself with Python and supplies the
          # equivalent 15m30s hard process ceiling there.
          if command -v timeout >/dev/null 2>&1; then
            timeout --signal=TERM --kill-after=30s 15m30s python -m scripts.materialize_capital_structure_share_counts
          else
            perl -e 'alarm 930; exec @ARGV or die' -- python -m scripts.materialize_capital_structure_share_counts
          fi
      - name: retain unselected share-count v2 generations (pre-production)
        # HARD-DISABLED PRE-PRODUCTION SHELL. This step receives no R2 or HMAC
        # secrets. Even if both visibility gates are flipped accidentally, the
        # command fails before credential lookup or a remote call. Release needs
        # provider-proven conditional delete, a shared publisher/retention fence,
        # and a verifier-only capability that cannot write the publication head.
        # A third variable separately requests apply mode; one retention-variable
        # flip can never request deletion.
        if: ${{ vars.CAPITAL_STRUCTURE_SHARE_COUNT_PUBLICATION_ENABLED == 'true' && vars.CAPITAL_STRUCTURE_SHARE_COUNT_RETENTION_ENABLED == 'true' }}
        env:
          CAPITAL_STRUCTURE_SHARE_COUNT_RETENTION_ENABLED: 'true'
          CAPITAL_STRUCTURE_SHARE_COUNT_RETENTION_APPLY_ENABLED: ${{ vars.CAPITAL_STRUCTURE_SHARE_COUNT_RETENTION_APPLY_ENABLED }}
        run: |
          retention_args=()
          if [ "$CAPITAL_STRUCTURE_SHARE_COUNT_RETENTION_APPLY_ENABLED" = "true" ]; then
            retention_args+=(--apply)
          fi
          if command -v timeout >/dev/null 2>&1; then
            timeout --signal=TERM --kill-after=30s 15m30s python -m scripts.retain_capital_structure_share_counts "${retention_args[@]}"
          else
            perl -e 'alarm 930; exec @ARGV or die' -- python -m scripts.retain_capital_structure_share_counts "${retention_args[@]}"
          fi
      - name: compile capital-structure event spine (offline, context-only)
        # id: gates "commit capital-structure data" below — this lane's OWN checkpoint.
        # It cannot reach the market checkpoint at all any more: that checkpoint lives
        # in the `collect` job, which had to CONCLUDE before this job was eligible to
        # start (2026-08-06 job split).
        id: cs_spine
        # AFTER collection — today's verified SEC manifests arrived through the
        # handoff verified above — and BEFORE the capital-structure commit, so
        # immutable event versions/edges ride the same checkpoint as the source
        # ledger they were compiled from.
        # This compiler is network-free by contract and has no Prophet/rank authority.
        # Any source-bundle, ledger, or generation-receipt integrity failure blocks
        # the checkpoint; a partial capital-structure generation is never committed.
        run: python -m scripts.compile_capital_structure_events
      - name: compile capital-structure direct document terms (SEC-network-free, context-only)
        id: cs_terms
        # The fee-table compiler reads only source-manifest-addressed complete
        # submissions through their exact R2 namespace. The collector's source-store
        # environment is step-local, so wire it again here rather than pretending a
        # manifest object key is independently resolvable. It transcribes direct SEC
        # cells only: no aggregate capacity, instrument, risk, probability, or Prophet
        # claim may originate in this step. A missing/mismatched source aborts instead
        # of committing a partial term ledger.
        env:
          R2_ENDPOINT: ${{ secrets.R2_ENDPOINT }}
          R2_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
          R2_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
          R2_BUCKET: ${{ secrets.R2_BUCKET }}
          R2_CAPITAL_STRUCTURE_ENDPOINT: ${{ secrets.R2_CAPITAL_STRUCTURE_ENDPOINT }}
          R2_CAPITAL_STRUCTURE_ACCESS_KEY_ID: ${{ secrets.R2_CAPITAL_STRUCTURE_ACCESS_KEY_ID }}
          R2_CAPITAL_STRUCTURE_SECRET_ACCESS_KEY: ${{ secrets.R2_CAPITAL_STRUCTURE_SECRET_ACCESS_KEY }}
          R2_CAPITAL_STRUCTURE_BUCKET: ${{ secrets.R2_CAPITAL_STRUCTURE_BUCKET }}
          R2_RESEARCH_ENDPOINT: ${{ secrets.R2_RESEARCH_ENDPOINT }}
          R2_RESEARCH_ACCESS_KEY_ID: ${{ secrets.R2_RESEARCH_ACCESS_KEY_ID }}
          R2_RESEARCH_SECRET_ACCESS_KEY: ${{ secrets.R2_RESEARCH_SECRET_ACCESS_KEY }}
          R2_RESEARCH_BUCKET: ${{ secrets.R2_RESEARCH_BUCKET }}
        run: python -m scripts.compile_capital_structure_document_terms
      - name: gate capital-structure ingestion health (fail closed)
        id: cs_health
        # After both compilers and before projection so the public artifact can
        # consume one generation-bound discovery→retained→compiled horizon
        # verdict. A selected queue with zero durable verified evidence must red
        # THIS job — collect stays green so the market plane is not frozen. No
        # continue-on-error, no `|| echo`: that is how 2026-08-08→08-14 PutObject
        # AccessDenied nights stayed green while source_manifest froze at 1,258.
        run: python -m scripts.check_capital_structure_health
      - name: build capital-structure projection (offline, context-only)
        id: cs_projection
        # Reads the verified telemetry-last event generation and the exactly
        # generation-bound health horizon produced immediately above. It publishes
        # one observed-filing-state payload to a canonical artifact and byte-identical
        # public twin; terms, capacity, runway, scores, probabilities, and Prophet
        # authority stay absent.
        run: python -m scripts.build_capital_structure_projection
      # Diagnostics only — changes NO behavior. When the compiler above fails, the
      # ledger it rejected is the one artifact that identifies WHY, and today it is
      # unrecoverable: the failure skips every later step, so the post-collect tree
      # is never committed, and the next job's actions/checkout deletes it. That is
      # exactly how the 2026-08-05 evidence was lost (run 30960328285:
      # ManifestIdentityError on source ledger row 0, tree wiped before anyone could
      # copy it, and the drift CANNOT be reproduced locally — without the R2 secrets
      # every document retrieval defers with "content-addressed source store
      # unavailable", so no manifest is ever appended and the ledger never moves).
      # Placed AFTER every capital-structure step, not after the first one: `if:
      # failure()` only sees failures that already happened, so sitting directly
      # under the event spine made it fire ONLY for that step. Run 30997579632
      # proved it — the spine passed, the document-terms compiler two steps later
      # failed, and this capture was SKIPPED. From here it catches whichever of
      # the five steps aborts (a later step is skipped after an earlier failure,
      # but an `if: failure()` step still runs).
      # BOTH encodings are listed on purpose: #4600 moves this ledger from
      # source_manifest.parquet to source_manifest.jsonl (that is the fix for the
      # drift above). Naming only one would make this capture silently upload
      # nothing the moment the other lands — `if-no-files-found: warn` would not
      # even go red about it. Keep both names until the parquet copy is gone.
      # actions/upload-artifact v4.6.2
      - name: capture the rejected capital-structure ledger (diagnostics; failure only)
        if: failure()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        with:
          name: capital-structure-ledger-${{ github.run_id }}-${{ github.run_attempt }}
          path: |
            data/capital_structure/source_manifest.jsonl
            data/capital_structure/source_manifest.parquet
            data/capital_structure/telemetry.json
            data/capital_structure/ingestion_run.json
            data/capital_structure/health.json
          if-no-files-found: warn
          retention-days: 14
      # ── THE CAPITAL-STRUCTURE CHECKPOINT ────────────────────────────────────
      # The market plane was committed AND pushed by the `collect` job, which had
      # to conclude before this job started. Nothing here can cost it. This
      # checkpoint publishes ONLY the capital-structure generation,
      # all-or-nothing.
      #
      # WHY ITS OWN COMMIT RATHER THAN A WIDER ONE UPSTREAM: this lane is an
      # all-or-nothing SEC publication protocol whose generation is coherent only
      # if the entire chain ran. `run collectors` (sec_capital_structure) rewrites
      # the TRACKED source_manifest.jsonl long before the spine validates it, so
      # the tree legitimately holds a source ledger no compiler has accepted yet.
      # `collect`'s market checkpoint UNSTAGES those paths (never `git checkout`s
      # them — that ledger is these compilers' INPUT, which is also why it has to
      # reach this job as an artifact), and this step commits them only after
      # every link returned success.
      #
      # On a failure this step simply never runs, so the rejected ledger is never
      # committed and dies with the runner's workspace — which IS #4600's
      # self-heal: the next night's actions/checkout restores a clean ledger from
      # git. What it can no longer do is take the night's market data with it, or
      # any other job: nothing `needs:` this one.
      #
      # Deliberately NOT continue-on-error, and never to be given it: the compile
      # steps must stay fatal for THIS checkpoint (that was tried in #4578 and
      # correctly reverted). Fatal for their own commit, invisible to everything
      # else in the nightly.
      - name: commit capital-structure data (local checkpoint — no network ops in this step)
        # Gated on the NAMED producers of the paths it stages, compared with
        # `.outcome == 'success'` so a SKIPPED link blocks exactly like a failed one
        # and a future continue-on-error cannot launder a failure into a commit
        # (`.conclusion` would). always() keeps it decoupled from JOB status for the
        # same reason the market checkpoint is: an unrelated red elsewhere in this
        # job must not discard a capital-structure generation that did compile.
        #
        # cs_handoff replaces the old `steps.collectors.outcome` term. `collectors`
        # runs in `collect` and cross-job step outcomes are not referenceable — but
        # the substitution is not a weakening, it is the same producer named where
        # it actually lands: cs_handoff is the step that puts the collector's ledger
        # in THIS tree, and it fails closed unless the bytes match the receipt
        # `collect` stamped. Dropping the term instead would let this checkpoint
        # commit a stale committed ledger as if it were tonight's generation.
        #
        # The two var-gated share-count steps are deliberately NOT in this gate:
        # they are designed to be SKIPPED (vars off), so `== 'success'` on them would
        # deadlock this checkpoint forever. They write only under
        # data/capital_structure/, which this step stages wholesale.
        if: >-
          always()
          && steps.cs_handoff.outcome == 'success'
          && steps.cs_spine.outcome == 'success'
          && steps.cs_terms.outcome == 'success'
          && steps.cs_projection.outcome == 'success'
          && steps.cs_health.outcome == 'success'
        id: commitcs
        run: |
          echo "committed=false" >> "$GITHUB_OUTPUT"
          git config user.name "dashboard-bot"
          git config user.email "actions@users.noreply.github.com"
          # SCOPE: exactly the capital-structure generation and its byte-identical
          # public twin — nothing else. The two move together or the twin law breaks.
          # A broad `git add` here would re-open the coupling this split removed:
          # market paths must never ride a checkpoint the CS chain can veto.
          git add data/capital_structure site/capital-structure-data
          if git diff --cached --quiet; then echo "no capital-structure changes to commit"; exit 0; fi
          git commit -m "capital-structure: nightly generation $(date -u +%F)"
          echo "committed=true" >> "$GITHUB_OUTPUT"
      # COMMIT/PUSH SPLIT, same law as the market checkpoint (2026-07-17, run
      # 29542087837 postmortem): the local commit above is atomic and integrity-
      # complete; everything below is pure network publish in its OWN always()-guarded
      # step, so a job-cap cancel during the push cannot discard an already-committed
      # generation. Every git network op is alarm-bounded (macOS runners have no GNU
      # timeout; perl alarm survives execve — SIGALRM kills a hung op, the loop retries).
      - name: push capital-structure data (alarm-bounded git ops)
        id: pushcs
        if: always() && steps.commitcs.outputs.committed == 'true'
        run: |
          . "${GITHUB_WORKSPACE:-.}/scripts/ci/push_retry.sh"
          # Same best-effort policy as the market push: losing a race for refs/heads/main
          # must not red the job. (Pre-split this loop doubled as a second chance at a
          # lost market push, because both commits sat in one workspace. It does not any
          # more — that commit lives in `collect`'s workspace, and `collect`'s own
          # salvage push is what covers it. This loop publishes the generation only.)
          PUSH_ALARM=420
          push_retry_init "capital-structure generation"
          while push_attempt; do
            # pre-sync collision sweep — see the market push above for the full rationale
            # (a mid-run commit on origin/main tracking a path this job wrote untracked).
            perl -e 'alarm 120; exec @ARGV or die' -- git fetch origin main || true
            comm -12 <(git ls-files --others --exclude-standard | LC_ALL=C sort) \
                     <(git ls-tree -r --name-only origin/main | LC_ALL=C sort) \
              | while IFS= read -r f; do rm -f -- "$f" || true; done
            # Whole-generation append-only fence (DEC:CS-V2-WHOLE-GENERATION-APPEND-ONLY-FENCE).
            # Two overlapping capital_structure jobs can each compile from the ledger they
            # checked out; `-X theirs` below then REPLACES origin/main's coherent CS
            # generation. Runs after fetch and before rebase so a proven drop of
            # source_manifest.jsonl withholds data/capital_structure and
            # site/capital-structure-data together — never one file, never merge=union.
            # Collect cannot host this fence: collect unstages those paths.
            if ! push_append_only_fence origin/main; then push_backoff; continue; fi
            if perl -e 'alarm 420; exec @ARGV or die' -- git pull --rebase --autostash -X theirs origin main && push_autostash_ok; then
              if push_do; then
                echo "pushed capital-structure generation on attempt $PUSH_ATTEMPT"
                push_won
                exit 0
              fi
            fi
            push_abort_rebase
            push_backoff
          done
          push_lost
          echo "::warning::could not push the capital-structure generation after $PUSH_ATTEMPT attempts ($PUSH_STOP) (non-fatal — the market data is already published; the next night recompiles)"
      # W2: per-band timings row + >85%-of-cap tripwire. always() so a job-cap cancel
      # still records the night in the grace window (elapsed ≈ cap → the warning fires).
      - name: timings ledger + 85% budget tripwire (W2)
        if: always()
        run: bash scripts/ci/nightly_timings_finish.sh 90

  government_revenue_projection:
    # A GITHUB_TOKEN push intentionally cannot recursively launch a push workflow.
    # Call the serialized owner synchronously instead, so the engine checkout and
    # its Neural Web/Prophet readers cannot race the prior procurement generation.
    # ET gate (see the schedule comment): reached TRANSITIVELY, like capital_structure.
    # This `if:` carries no status function, so the implicit success() on `needs: collect`
    # already skips this lane whenever collect skips — which is exactly what an
    # off-regime firing does. `needs: collect` stays a SCALAR: tests/test_dag_conformance.py
    # pins the shape. Do not add always() here — it would sever the skip propagation.
    needs: collect
    if: needs.collect.outputs.government_revenue_projection_needed == 'true'
    permissions:
      contents: write
    uses: ./.github/workflows/government-revenue-live.yml
    with:
      projection_only: true

  # collect_tail: the slow post-adapter accrual band, split out of `collect`
  # (2026-07-14) so the engine job starts as soon as the adapter sweep commits
  # (~30-75m earlier; the 'run collectors' step was 89-91m of a 94m median job,
  # with tape-flow budgeted up to 75m more on backlog nights).
  # needs: collect → this job's checkout+pull reads TONIGHT's committed stores
  # (qbus for shadow-importance, github stars for the W9 join, altdata theses
  # for the qledger backfill). The engine job does NOT wait on this job: every
  # same-night reader of these outputs tolerates ≤1-run-stale data — the same
  # contract as the factor_series sibling (flow_leaders/flow_desk/
  # options_screener are absent-safe display legs, stamp_options_state is
  # PIT-anchored to historical fire dates, grade_qledger grades only claims
  # matured ≥5d, TIL lobes carry 2160h freshness SLAs).
  # if: always() — accrual is idempotent and must not stop because a collector
  # crashed core (tape-flow pulls from ThetaData, TIL organs from external APIs;
  # a failed core just means tonight's pass re-scores yesterday's stores).
  # Runner budget: PINNED to theta-m1 (mac-builder-1/2/3, the M1 host) since
  # 2026-07-26 — the ThetaData Terminal (:25503, launchd com.macro.theta-terminal)
  # and the 60GB thetadata_eod store are machine-local on the M1 after the
  # 2026-07-25 M2→M1 data-plane migration, while the wider `macstudio` label
  # spans TWO machines (M1 builders 1/2/3 + M2 builders 4/5). Un-pinned, this
  # job landed on an M2 runner ~2/5 nights and every tape_flow step self-skipped
  # (probe-only Terminal law, #3644 — CI must never launch a second Terminal;
  # the single-session license kicks the M1 session). Contention (2026-07-26
  # audit): the other heavy jobs stay 5-slot-eligible and 4 slots remain free
  # while this job holds one M1 slot; the only new exposure is THIS job
  # queueing when long jobs happen to occupy all 3 M1 slots — acceptable for a
  # state-resumable accrual job whose commit has a full night of runway. The
  # macstudio-light lane (own runner) is unaffected.
  # Forward-ledger law: untouched — the nightly graders stay inside
  # scripts.collect (collect core, COLLECT_LANE=nightly); this job only accrues
  # stores + registers idempotent claims, and its commit lands hours before the
  # next day's collect.
  collect_tail:
    needs: [et_gate, collect]
    if: always() && needs.et_gate.outputs.run != 'false'
    # 2026-08-17 UNPIN (Prophet outage postmortem, research/PROPHET_OUTAGE_2026_08_17_POSTMORTEM.md):
    # this job was pinned `theta-m1` (mac-builder-1/2/3, the M1 host) while that
    # host existed. The M1 runners were deregistered without the label moving, so
    # from ~2026-08-15 this job could NEVER be scheduled — and a job queued on a
    # label with no live runner holds the whole run open until GitHub's 24h
    # queued-job kill, which holds this run's per-cron concurrency group, which
    # pends the NEXT night's real slot: the nightly cascade that froze every
    # Prophet board 08-14→08-17. macstudio always has live runners, and every
    # store-coupled step below is probe-guarded by design: tape_flow degrades to
    # warning + self-skip (never a second Terminal, #3644), and the W11 witness
    # carries the keep-last-real law for store-less landings. RE-PIN RULE: when
    # the M1 host returns (see .github/runner-policy.yml `m1-theta` canary), move
    # this back to the store-bearing label in the same PR that graduates the
    # canary — until then a self-skipped tape lane is the designed degraded mode,
    # a 24h-hostage nightly is not.
    runs-on: [self-hosted, macstudio]
    # Budget arithmetic (re-cut 2026-08-02 after the 5-night starvation cancel,
    # 07-29→08-02: shadow-importance outgrew its "~13-14m" estimate to 80-120m+
    # and the job died at the old 150m cap with every later step — earnings
    # sweep, qledger, CCW, "commit tail data" — SKIPPED): tape-flow budgets
    # 25+30+20=75m (hard-stopped in-script) + census ≤8m (--max-requests 450)
    # + W11 options witness ~5m (real-path on the store-bearing M1 — measured
    # 4m35s on mac-builder-1, 2026-07-26) + other TIL organs ~seconds
    # + earnings sweep ~4m + shadow-importance now STEP-BOUNDED at 60m+50m
    # (see the band's own comment) + qledger backfill + CCW + commit ≈ ~160m
    # realistic worst (observed pre-scorer band runs ~33m, not its 88m budget
    # sum). 200m clears that with headroom (engine job precedent, same cap);
    # the step bounds — not this cap — are what guarantee the commit runs, and
    # `if: always()` on "commit tail data" is the last-resort grace-window
    # backstop if a freak night stacks every budget at once.
    timeout-minutes: 200
    steps:
      - name: timings — job start mark (W2)
        run: date +%s > "${RUNNER_TEMP}/nightly-timings-${GITHUB_RUN_ID}-${GITHUB_JOB}-start"
      - uses: actions/checkout@v4
        with:
          ref: main
      - run: git pull origin main
      - name: python 3.12 (Homebrew venv — self-hosted runner)
        run: |
          VENV="$HOME/.cache/mm-venv-${RUNNER_NAME:-default}"
          [ -x "$VENV/bin/python" ] || /opt/homebrew/bin/python3.12 -m venv "$VENV"
          echo "$VENV/bin" >> "$GITHUB_PATH"
          "$VENV/bin/python" -m pip install --quiet --upgrade pip
      - run: pip install -r requirements.txt
      - name: ThetaData Terminal — health-check (probe-only, non-fatal)
        # The ThetaData Terminal v3 is a local Java daemon the tape_flow steps need.
        # Since the 2026-07-25 M2→M1 data-plane migration it is launchd-managed on the
        # M1 (com.macro.theta-terminal keepalive), and the ThetaData license allows ONE
        # upstream-connected Terminal per account — the `macstudio` runner label now
        # spans TWO machines, so this step must never launch its own Terminal: a second
        # instance on the other host kicks the M1 session off the account (server-side
        # MDDS/FPSS kick-out) and breaks the store writer lanes. Probe-only:
        #   - :25503 up (job landed on the Terminal host) → OK.
        #   - down (job landed on a non-Terminal macstudio host) → warning; tape_flow
        #     steps self-skip via their reachable() gate — non-fatal, and the
        #     round-robin cursor accrues the skipped roots on subsequent nights.
        run: |
          HEALTH_URL="http://127.0.0.1:25503/v3/option/list/symbols"
          if curl -sf --connect-timeout 3 "$HEALTH_URL" >/dev/null 2>&1; then
            echo "ThetaData Terminal already running — OK"
          else
            echo "::warning::ThetaData Terminal not reachable on this runner host — tape_flow steps will self-skip (Terminal is launchd-managed on the M1 since 2026-07-25; CI must never launch one)"
          fi
      - name: timings band — tape-flow (W2)
        run: python3 scripts/nightly_timings.py mark --band tape-flow || echo "::warning::timings band mark failed (non-fatal)"
      - name: T2a tape-flow features — forward accrual (collect-tail lane, non-fatal)
        # Pulls prior-trading-day trade+NBBO tape for the full gex_symbols() universe
        # (~360 roots) via wildcard-bulk ThetaData endpoint (2 requests/root-day).
        # Aggregates inline to signed-flow features; discards raw tape for single names.
        # Requires ThetaData Terminal running on the self-hosted runner (port 25503).
        # Budget: 25 min default (hard-coded in script; overridable via --budget-minutes).
        #   At 6-concurrent: ~13 min (T2A_THROUGHPUT_PROBE.md §6a) — all 375 roots.
        #   At 2-concurrent (backfill alive): ~30 min → 25 min budget covers ~180 roots/night;
        #   round-robin cursor in state ensures remaining roots accrue on subsequent nights.
        # Non-fatal: terminal absent or any error logs a warning and does not fail the build.
        # run_status.json registration: sources["tape_flow_forward"] (P0.7 pattern).
        env:
          # 3s default connect timeout false-fails under concurrent bulk load
          # (measured 2026-07-11: 352/375 false 'not reachable' at 3s; 20s → 375/375).
          THETA_CONNECT_TIMEOUT: "20"
        run: |
          python -m scripts.build_tape_flow_daily --mode forward \
            || echo "::warning::T2a tape-flow forward accrual failed (non-fatal)"
      - name: T2a tape-flow features — episode-window backfill (non-fatal, budgeted)
        # Tier-S episode windows 2022→, ±15d, ETF anchors only (≈11 nodes post-2022).
        # Runs AFTER the forward step so the daily row always lands first.
        # Hard-stop at 30 min (budget_deadline): state is persisted after every
        # completed root-day; the next nightly run resumes from remaining work.
        # Budget: 30 min — sized so episodes(30) + etf-history(20) + forward(~13)
        # + shadow-importance (~13-14m) + census (≤8m) stay well under this job's
        # 150-min timeout, ensuring "commit tail data" is always reached before
        # GitHub Actions cancels the job.
        # Outputs (data/tape_flow/daily/*.parquet + data/tape_flow/_state.json)
        # are covered by the "commit tail data" step's `git add data/` below.
        # Non-fatal: terminal absent or any error logs a warning and continues.
        env:
          THETA_CONNECT_TIMEOUT: "20"   # see forward step note
        run: |
          python -m scripts.build_tape_flow_daily --mode episodes --budget-minutes 30 \
            || echo "::warning::T2a tape-flow episodes backfill failed (non-fatal)"
      - name: T2a tape-flow features — ETF history backfill (non-fatal, budgeted)
        # 20 ETF anchors, full history 2017→. R6 priority 3 — runs last, after forward
        # and episodes, so earlier steps are never delayed by the large backlog.
        # Hard-stop at 20 min (budget_deadline): state resumes each night until done.
        # Budget: 20 min — see episodes comment above for the joint ceiling rationale.
        # Outputs covered by `git add data/` in the commit step below (same as above).
        # Non-fatal: terminal absent or any error logs a warning and continues.
        env:
          THETA_CONNECT_TIMEOUT: "20"   # see forward step note
        run: |
          python -m scripts.build_tape_flow_daily --mode etf-history --budget-minutes 20 \
            || echo "::warning::T2a tape-flow ETF-history backfill failed (non-fatal)"
      - name: timings band — til-organs (W2)
        run: python3 scripts/nightly_timings.py mark --band til-organs || echo "::warning::timings band mark failed (non-fatal)"
      # ── TIL collect-lane organs (W8/W9/W10/W11) ───────────────────────────
      # Moved here from the collect job (2026-07-14 split); placement and order
      # follow the declared dag.yml collect_tail lane. W7 (dol_certs) stays
      # unwired by design (accrual gate) — its divergence remains declared.
      # Measured 2026-07-12 (Mac Studio, warm venv): W9-W11 6.6s + 0.5s + 0.5s
      # + 1.4s ≈ 9s total; W8 keyless/invalid-key no-op 0.8s + 0.6s; W8 keyed
      # runs hard-capped (see step comment) — no material claim on the budget.
      #
      # STEP CAPS ON THE TIL/MLC/CCW BAND (thetadata wedge postmortem 2026-08-06):
      # every step below was `python -m … || echo "::warning::…"` with a measured
      # budget in its comment and NO duration bound. `||` bounds the EXIT CODE, never
      # the DURATION: a step that hangs never exits, so the `||` never fires and the
      # step silently eats the job's whole 200m cap, taking every step behind it down
      # as SKIPPED — the exact starvation shape the shadow-importance band below was
      # capped for (07-29→08-02), including the "commit tail data" step, so even
      # completed work evaporates on the next clean checkout.
      # This is not hypothetical for W11: on 2026-08-06 the thetadata_eod store moved
      # to an external volume (/Volumes/STORAGE), and a launchd-context process that
      # opens a file there parks forever in open() on the macOS removable-volume
      # access gate — uninterruptible, no timeout, no error. The M1's backfill lane
      # accrued nothing for 3h in exactly that state. W11 reads that same store on
      # that same host, so an unbounded W11 is one permission drift away from a
      # 200m nightly.
      # Caps are sized ~3-4x the measured budget: generous enough that a slow night
      # never trips them, tight enough that a HANG is truncated loudly instead of
      # silently. As the band below says: if a step starts hitting its bound
      # nightly, fix ITS runtime — do not silently raise the bound.
      # `continue-on-error: true` is REQUIRED alongside each cap and is not
      # decoration: when the runner kills a timed-out step it kills the shell, so the
      # `|| echo` never runs and the step is marked FAILED. Without the pair, adding a
      # cap would convert these non-fatal steps into job-killers — strictly worse than
      # the bug being fixed. Same pairing law as the shadow-importance band below.
      - name: TIL W8 — Census HS-code import-flow ingest (non-fatal)
        # Cap: --max-requests 450 already self-bounds to ~7-8m; 15m is the backstop
        # for a hang in a single request (the cap is on requests, not on wall time).
        timeout-minutes: 15
        continue-on-error: true
        # Requires the CENSUS_API_KEY repo secret (API is NOT keyless — verified
        # 2026-07-09; keyless/invalid key 302s and the run no-ops honest-null in
        # ~1s, exit 0). --max-requests 450 hard-bounds every run to ~7-8 min
        # (measured ~0.5s/request + 0.5s pacing) AND honors the documented
        # 500 req/day/key API limit; the ~2,900-request 2018-01 first backfill
        # self-chunks across ~7 nights via data/trade_flows/census_state.json
        # (state-resume covered in tests/test_census_trade.py::TestRequestCap).
        # Writes data/trade_flows/imports_monthly.parquet (git-stored via
        # `git add data/` below).
        env:
          CENSUS_API_KEY: ${{ secrets.CENSUS_API_KEY }}
        run: |
          python -m collectors.census_trade --max-requests 450 \
            || echo "::warning::census_trade ingest failed (non-fatal)"
      - name: TIL W8 — theme trade-flow rollup (non-fatal)
        # Cap: measured 0.6s — 5m is a pure hang backstop.
        timeout-minutes: 5
        continue-on-error: true
        # Rolls the parquet above into per-theme YoY/accel/confirmation reads
        # (measured 0.6s on the honest-null path). Emits honest null with a
        # fresh as_of when the parquet is absent — keeps both W8 lobes' as_of
        # current on committee.html Web Health while data accrues. Writes
        # data/neuralweb/theme_trade_flows.json + site/basketdata/trade_flows.json
        # (staged via `git add data/ ... site/basketdata/` below).
        run: |
          python -m engine.theme_trade_flows \
            || echo "::warning::theme_trade_flows rollup failed (non-fatal)"
      - name: TIL W10 — ClinicalTrials.gov theme pipeline ingest (non-fatal)
        # Cap: measured 6.6s; 10m covers a slow CT.gov night and bounds a stalled fetch.
        timeout-minutes: 10
        continue-on-error: true
        # Keyless CT.gov v2 full fetch (9 modalities, ~2.5k rows, measured 6.6s).
        # Writes data/clinical_trials/modality_monthly.parquet (git-stored via
        # `git add data/` below). Per-modality isolation; always exits 0.
        run: |
          python -m collectors.clinicaltrials_themes \
            || echo "::warning::clinicaltrials_themes ingest failed (non-fatal)"
      - name: TIL W10 — theme clinical rollup (non-fatal)
        # Cap: measured 0.5s — 5m is a pure hang backstop.
        timeout-minutes: 5
        continue-on-error: true
        # Pure in-memory aggregation of the parquet above (measured 0.5s).
        # Produces data/neuralweb/theme_clinical.json + site/basketdata/clinical_pipeline.json.
        run: |
          python -m engine.theme_clinical \
            || echo "::warning::theme_clinical rollup failed (non-fatal)"
      - name: TIL W11 — options-tape crowding witness (non-fatal)
        # Cap: measured 4m35s on mac-builder-1 (2026-07-26) → 15m (~3x) bounds the
        # real compute path. THIS is the step the band comment above is about: it
        # reads the thetadata_eod store on the M1, and that store now lives on an
        # external volume whose open() can park forever under a launchd context
        # (2026-08-06). Unbounded, that hang would consume the job's 200m cap and
        # SKIP the ~10 steps behind it, including "commit tail data".
        timeout-minutes: 15
        continue-on-error: true
        # Since the theta-m1 pin (2026-07-26) this job runs on the store-bearing
        # M1 host: the canonical resolver finds the thetadata_eod store via the
        # ops-wt path (engine/thetadata_store.py), so this step takes the REAL
        # compute path nightly — measured 4m35s on mac-builder-1 (2026-07-26),
        # budgeted in the job's arithmetic above. Two-writer coexistence with
        # the launchd lane (ops/launchd/com.macro.theme-options-witness.plist,
        # also on the M1 post-migration) is by design: both writers score the
        # SAME store, so last-writer-wins is content-stable; the keep-last-real
        # law (engine keep-last-real: a store-LESS run must not overwrite real
        # legs newer than 6d, and discloses a dead real lane past 6d via the
        # honest null) still guards any future store-less landing — e.g. if the
        # theta-m1 label set or the store path ever drifts.
        run: |
          python -m engine.theme_options_witness \
            || echo "::warning::theme_options_witness failed (non-fatal)"
      - name: TIL W9 — discovery-confluence join (non-fatal)
        # Cap: measured 1.4s — 5m is a pure hang backstop.
        timeout-minutes: 5
        continue-on-error: true
        # Two-source join (measured 1.4s): EDGAR phrase velocity over the
        # git-tracked data/edgar/{bottleneck,emergence}_hits.parquet (dripped by
        # the engine job) × GitHub star velocity over data/github/repo_stars.parquet
        # (collected by tonight's collect core and read here from its committed
        # push, so the leg still reads today's stars).
        # Produces site/basketdata/{phrase_velocity,github_adoption}.json +
        # data/neuralweb/discovery_confluence.json.
        run: |
          python -m scripts.build_discovery_confluence \
            || echo "::warning::discovery confluence join failed (non-fatal)"
      - name: MLC-W5 — US earnings calendar sweep (non-fatal)
        # Cap: measured ~3.5m/night (66 calendar calls ~155s + surprise drip ~60s)
        # → 12m bounds a stalled Nasdaq call without tripping on a slow night.
        timeout-minutes: 12
        continue-on-error: true
        # Root-cause fix (MLC postmortem 07-14): collectors/equity_earnings.py was never
        # wired into the nightly pipeline — earnings.parquet was permanently empty/missing,
        # making the W1.5 blackout gate and the W5 display chips always stale-fail-open.
        # Sweeps Nasdaq's public calendar for the next 66 weekdays (~one quarter), writing
        # data/earnings/earnings.parquet (ticker-indexed: next_date, next_time, eps_forecast,
        # surprises_json, surprises_as_of, as_of). Bot-wall-aware: if the first calendar call
        # is blocked (Akamai 403), the existing cache is kept untouched and the step exits 0.
        # MUST run in collect_tail, NOT collect (house law: new accrual steps go in collect_tail).
        # PLACEMENT (starvation incident 07-29→08-02): this step and its tripwire used to sit
        # AFTER the shadow-importance band, whose two unbudgeted full-store scorers grew from
        # the "~13-14m combined" estimate to 80-120m+ and blew the job's 150m cap five nights
        # running — every step behind them (this sweep, qledger, CCW, "commit tail data") was
        # SKIPPED, freezing the store at the pre-#3979 two-stamp shape. Cheap bounded accrual
        # runs BEFORE unbudgeted scorers; keep it that way.
        # KEEP THIS LINE BARE. No-args means "sweep the whole universe" (breadth union +
        # Hot Tape liquid names); passing --tickers collapses the universe to that subset
        # and every other row keeps a frozen as_of. That is exactly the 2026-06-19 freeze:
        # the collector's __main__ used to fall through to a 3-name demo default, so for six
        # weeks the nightly refreshed only AAPL/NVDA/JPM while 1361 of 1364 rows sat at
        # as_of 2026-06-19 — and the tripwire below read the store-level max() as_of and
        # reported sla_ok the whole time.
        # Pinned by tests/test_earnings_w5.py::TestNightlyEntryPointSweepsWholeUniverse.
        # Wall time (measured 2026-07-29): calendar sweep 66 calls ≈ 155s (round-trip
        # dominates, not the 0.25s pacing sleep) + surprise drip capped at max_new=120
        # names ≈ 60s → ~3.5m per night, well within the job budget.
        # Writes data/earnings/earnings.parquet — covered by `git add data/` below.
        run: |
          python -m collectors.equity_earnings \
            || echo "::warning::equity_earnings sweep failed (non-fatal)"
      - name: MLC-W5 — earnings freshness tripwire (non-fatal)
        # Cap: single-parquet read + audit (seconds) — 5m is a pure hang backstop.
        timeout-minutes: 5
        continue-on-error: true
        # Reads the just-written data/earnings/earnings.parquet and checks that
        # the store's as_of is within the expected SLA (2 trading days).
        # Writes data/quality/earnings_freshness_audit.json for observability.
        # Emits ::warning:: when stale so CI surfaces the gap without failing the build.
        run: |
          python -m scripts.audit_earnings_freshness \
            || echo "::warning::audit_earnings_freshness crashed (non-fatal)"
      - name: timings band — shadow-scorers (W2)
        run: python3 scripts/nightly_timings.py mark --band shadow-scorers || echo "::warning::timings band mark failed (non-fatal)"
      # ── importance_v0 SHADOW lanes (W3 + W4) ──────────────────────────────
      # Moved out of scripts.collect (2026-07-14 split; collect core passes
      # --skip-shadow-importance). Both passes score the FULL US+CN qbus store
      # from tonight's collect-core commit. Claims register with stable idempotent
      # ids; grade_qledger (inside collect core's scripts.collect) grades only
      # claims matured ≥5d, so registering one job later never changes grading
      # arithmetic — the old "register BEFORE the grader" ordering was never
      # load-bearing same-night. Non-fatal either way.
      # STEP BUDGETS (starvation incident 07-29→08-02): the split-day "~13-14m
      # combined" estimate rotted silently as the qbus store accrued — measured
      # 40m+40m on the last green night (07-27) and 61m (W3) + 56m-and-killed
      # (W4) on 08-01, blowing the job's 150m cap five nights running and
      # SKIPPING every later step, including "commit tail data" (so even
      # completed work evaporated on the next night's clean checkout). The
      # timeout-minutes + continue-on-error pair bounds each scorer so the tail
      # band and the commit ALWAYS get their turn: a scorer that outgrows its
      # budget is truncated loudly (step timeout annotation) instead of silently
      # cancelling the whole job. Claims are idempotent, so a truncated pass
      # simply re-registers on a later night. Bounds sized to observed need
      # (W3 61m worst → 60m; W4 40m green → 50m) inside the job's 200m cap; if
      # a scorer starts hitting its bound nightly, fix ITS runtime — do not
      # silently raise the bound (that is how 13-14m became 120m unnoticed).
      - name: importance_v0 shadow tape — W3 scorer (non-fatal)
        timeout-minutes: 60
        continue-on-error: true
        run: |
          python -m scripts.shadow_importance_v0 \
            || echo "::warning::shadow_importance_v0 failed (non-fatal)"
      - name: importance_v0 shadow tape — W4 PIT-correct scorer (non-fatal)
        timeout-minutes: 50
        continue-on-error: true
        run: |
          python -m scripts.shadow_importance_v0_pit \
            || echo "::warning::shadow_importance_v0_pit failed (non-fatal)"
      - name: timings band — qledger-ccw (W2)
        run: python3 scripts/nightly_timings.py mark --band qledger-ccw || echo "::warning::timings band mark failed (non-fatal)"
      - name: qledger backfill (altdata + radar + policy → claims.jsonl)
        # ALTDATA_REBOOT W2 (2026-07-12): routes new altdata theses to per-channel
        # claim families with pre-registered horizon rulers; emits 2 placebo claims
        # per real altdata claim. Also registers new radar edge_snapshots and policy
        # theses as qledger claims. Idempotent — stable claim_ids prevent duplicates.
        # Reads data/altdata/theses.jsonl from tonight's collect-core commit; runs
        # BEFORE "commit tail data" so claims.jsonl is committed in the same push.
        # Non-fatal.
        run: |
          set +e
          python -m scripts.backfill_qledger_us \
            > "$RUNNER_TEMP/backfill_qledger_us.log" 2>&1; rc=$?
          cat "$RUNNER_TEMP/backfill_qledger_us.log"
          if [ "$rc" -ne 0 ]; then
            echo "::warning title=backfill_qledger_us::rc=$rc (non-fatal)"
          fi
          exit 0
      - name: CCW W2 — corporate credit g-spread engine (non-fatal)
        # Cap: measured 3.8s locally (2026-07-14); the step's own comment says
        # "revisit if it nears ~2m", so 10m bounds a hang well clear of that line.
        timeout-minutes: 10
        continue-on-error: true
        # Reads tonight's holdings from collect-core's commit (data/corp_bonds/holdings/*)
        # plus data/fred/DGS*.parquet (CMT curve pillars) and
        # data/corp_bonds/issuer_themes.json (issuer registry from W1).
        # Writes data/corp_bonds/series/*.parquet (6 files: issuer_daily, theme_daily,
        # sector_daily, market_daily, maturity_wall, bond_panel_latest) plus
        # data/corp_bonds/latest.json and data/corp_bonds/validation.json.
        # All outputs are covered by `git add data/` in the "commit tail data" step below.
        # Wall time measured locally 2026-07-14: 3.8s ([timing] corp_credit.snapshot,
        # single holdings date, ~10.2k bonds, incl. the convention re-solve pass).
        # Full-history recompute scales ~linearly in dates; revisit if it nears ~2m.
        # Non-fatal: a missing holdings store or solver failure logs a warning only.
        run: |
          python -m engine.corp_credit \
            || echo "::warning::corp_credit engine failed (non-fatal)"
      - name: CCW W3 — credit momentum organ (non-fatal)
        # Runs AFTER corp_credit so theme/market series are fresh.
        # Reads: archive-merged FRED spreads (hy_oas/ig_oas 1996→, ladder ~3y),
        #   Moody's DBAA/DAAA, FINRA breadth/sentiment (W5), yahoo ETF prices,
        #   corp_credit series output, holdings snapshots (own-store breadth).
        # Writes: data/corp_bonds/credit_momentum.json + forward_log.jsonl.
        # Spread velocity-percentile PRIMARY; oscillator crosses SECONDARY (CCW-R15).
        # Display-tier; authority all-false. Non-fatal (CCW-R16).
        run: |
          python -m engine.credit_momentum \
            || echo "::warning::credit_momentum engine failed (non-fatal)"
      - name: timings band — commit (W2)
        run: python3 scripts/nightly_timings.py mark --band commit || echo "::warning::timings band mark failed (non-fatal)"
      - name: commit tail data
        # if: always() — last-resort backstop for the job-timeout cancel class
        # (07-29→08-02: five nights of completed accrual evaporated because this
        # step was SKIPPED on cancel and the next night's clean checkout discarded
        # the uncommitted writes). GitHub gives cancelled jobs a grace window in
        # which always() steps run — NOT guaranteed to finish (the engine job's
        # 07-24 postmortem saw the commit land but a later upload die), which is
        # why the scorer step bounds above are the primary defense; this is
        # defense in depth. Every earlier step is non-fatal by design, so the
        # only new path always() adds is the post-cancel one. A partial night's
        # stores are safe to commit: every lane here is state-resumable and
        # claims are idempotent.
        if: always()
        run: |
          . "${GITHUB_WORKSPACE:-.}/scripts/ci/push_retry.sh"
          git config user.name "dashboard-bot"
          git config user.email "actions@users.noreply.github.com"
          # Everything this job writes is under data/ (tape_flow, trade_flows,
          # clinical_trials, neuralweb, qledger claims, run_status) or
          # site/basketdata/ (TIL site projections). No cache-restore steps run
          # in this job, so the W0b asia unstage sweep from the collect job is
          # not needed here — a fresh checkout+pull can only differ where THIS
          # job's steps wrote. (The adapter sweep + qledger grader never run in
          # this job, so site/qledger/track_record.json is never touched here;
          # tests/test_collect_staging_globs.py keys on the grader-bearing
          # module's dotted name, which this comment deliberately avoids.)
          # data/run_status.json is co-written by the concurrent engine job
          # (flow_signals registration) — whole-file -X theirs means one side's
          # sources[] update can lose a race and go 1-run stale. Display-only
          # and self-healing (healthcheck's hard-fail keys last_run/
          # circuit_breaker are written solely by collect-core's adapter sweep,
          # present in every job's base) — accepted at the 07-14 split review.
          git add data/ site/basketdata/
          if git diff --cached --quiet; then echo "no tail data changes to commit"; exit 0; fi
          git commit -m "data: collect-tail accrual $(date -u +%F)"
          # best-effort push: same 5-retry rebase loop as the collect job's
          # "commit market data" (see the pre-sync collision-sweep rationale there —
          # render.yml / #2252). -X theirs prefers THIS job's freshly-accrued
          # data on any conflict; exhausting the retry budget is non-fatal (the
          # stores are state-resumable and re-accrue next night).
          PUSH_ALARM=420
          push_retry_init "tail data"
          while push_attempt; do
            perl -e 'alarm 120; exec @ARGV or die' -- git fetch origin main || true
            comm -12 <(git ls-files --others --exclude-standard | LC_ALL=C sort) \
                     <(git ls-tree -r --name-only origin/main | LC_ALL=C sort) \
              | while IFS= read -r f; do rm -f -- "$f" || true; done
            if perl -e 'alarm 420; exec @ARGV or die' -- git pull --rebase --autostash -X theirs origin main && push_autostash_ok; then
              if push_do; then echo "pushed tail data on attempt $PUSH_ATTEMPT"; push_won; exit 0; fi
            fi
            push_abort_rebase
            push_backoff
          done
          push_lost
          echo "::warning::could not push collect-tail data after $PUSH_ATTEMPT attempts ($PUSH_STOP) (non-fatal)"
      - name: timings ledger + 85% budget tripwire (W2)
        if: always()
        run: bash scripts/ci/nightly_timings_finish.sh 200

  engine:
    needs: [et_gate, collect, government_revenue_projection]
    if: always() && needs.et_gate.outputs.run != 'false'            # a scraper failure must still produce a dashboard from cached data
    runs-on: [self-hosted, macstudio]
    timeout-minutes: 300    # was 240 — the 2026-08-07 night died AT the raised 240 cap too (22:00:48→02:05:48, killed in the final commit/push band ~2 min from done: last step group logged 02:03:28, so the workload is now ~242m and the site commit never landed → THIRD consecutive stale-site night and the frozen /prophet surface found 2026-08-08). 300 = the observed ~242m + ~58m headroom on the same cold-spare law as before. NOT a wedge: step groups tick seconds apart through the tail band, this is workload size. build_site is NO LONGER the creep lever (back to ~24m on 08-07 after the earlier 61.7m peak — do not inherit that label); the next profile must find where the other ~80m vs the 135-160m era went before any further cap raise. W4 PROFILE 2026-08-08 (that directive, executed same-day; evidence = jobs-API steps[] for the five 08-05→08-08 engine executions — read steps[].conclusion, NOT the run log: after a cancel the if:always() cleanup steps keep ticking seconds apart and mimic an almost-done tail): the '~2 min from done / NOT a wedge / workload is now ~242m' reading above is OVERTURNED. The 08-07 cancelled step was 'Prophet nightly' — 23:07:57→02:00:56 = 173m on mac-builder-1, a step the very next execution measured at 24 SECONDS (run 31226002132, same asof 2026-08-07, same first-unfrozen standouts board, mac-builder-2) — and builders-parallel + tail-desks were SKIPPED (~53m of real work at green pace), so the night was killed ~53m short, not 2 min. Its 02:04 partial commit contains ZERO site/prophet|data/prophet|data/prophet_arena writes: build_prophet WEDGED pre-first-write and logged nothing, because the step buffered stdout to RUNNER_TEMP and cat it only on completion — fixed at the step itself (#4980: 15m step timeout + continue-on-error + PYTHONUNBUFFERED streaming, the arming-pass belt-and-braces). TRUE steady-state workload measured green 2026-08-08: 126.3m — BELOW the 135-160m era (build_site back at 23.9m/24.9m after the frozen-slice fix; prophet-us doors/grades/miss-audit moved to us_prophet_ledgers). There is NO unattributed ~80m. The ~205m kills decompose (jobs API, runs 31056495943 mac-builder-5 + 31067383446 mac-builder-1): engine-core 72-73m (the build_site peak) + Intelligence Hub 15.6-17.5m + then-in-engine prophet-us tail modules + commit-band variance (19.4m vs 6.2m green). The ONE band still creeping: 'restore, acquire, project, and snapshot Filing Forensics SEC sources (hard gate)' — 7.4m → 11.4m → 15.2m → 23.3m → 23.8m across those five successive executions (fixed 12-ticker list; the growth is the run-clock-hashed receipts growing the immutable source store, not the SEC download, which is flat ~128MB) — trim in flight 2026-08-08 as #4986 (acquire/project/sync → filing-forensics-sec.yml 02:30Z off-render lane, engine keeps a constant-cost restore; expect restores-preamble ~26m→~3m in engine.jsonl once merged). AMENDED DOCTRINE: no future raise without (1) the jobs-API step timeline of the killed job and (2) data/ops/nightly_timings/engine.jsonl bands (live since 08-08) — this comment has now carried a wrong workload narrative twice, both times written from the run log alone; with prophet belted and SEC off-render the honest warm floor is ~105m, so 300 is pure cold-spare headroom and the next cap move on a healthy fortnight is DOWN, not up. Prior: was 200 — the 2026-08-05 AND 08-06 nights were BOTH hard-cancelled at ~205m (cap + grace) on different hosts (mac-builder-5, then mac-builder-1), before the pages artifact landed → publish's "No artifacts named github-pages", two consecutive stale-site nights, and the 6-day Prophet-pick gap found 2026-08-06. Two hosts rules out one sick machine; this is workload creep plus the cold-spare law the collect job's 185→240 bump above already states: the macstudio label can fail over to a cold runner (fresh venv, no warm GHA/R2 caches), so the cap must clear the COLD pace, not the warm one. 240 = the observed ~205m kill point + 35m headroom; the job exits when its steps finish, so a larger cap costs nothing on a healthy night. The real fix is the workload trim — the cap raise buys the nights back while that lands. Prior: was 150 — 5 of the 8 nightlies 2026-07-16→07-23 got CANCELLED at exactly the 150m cap (the greens finished 133m/147m/148.6m — success had become a coin flip). Measured step timings (07-21 green vs 07-24 cancelled jobs): checkout 3s→14.5m (huge variance on the 14k-file tree), R2 restores + signal/ledger preamble ~10–20m, "run regime engine + build dashboard + daily brief" 74–84m (inside it: build_site ~33m, build_news ~26–29m incl. GDELT 429-backoff stalls, share_cards ~7m), parallel regional/desk builders 31–33m normal with one >61m blowout (07-23 night, cut mid-step), tail band (NW/LLM desks → hub) ~35m. Normal total 135–160m; worst observed combination ≈190m. The timeout-cancel grace window does NOT reliably save the deploy: 07-24's "commit engine outputs" (if: always) ran post-cancel but the pages-artifact upload still failed → publish red; 07-23 was hard-killed before commit → stale site. So the cap must cover the whole job: 200m clears the worst observed night with headroom. Next lever if creep resumes: build_site — W3 re-measure 2026-08-06 (never inherit step-cost labels): build_news is NO LONGER the lever; the GDELT circuit breaker (#3442, 2026-07-24) already collapsed it from ~26–29m to a measured 4.3–5.9m/night (07-27 green 5.9m, 08-03 4.3m, 08-05 4.7m, 08-06 5.4m — ~2m of that is the nightly one-time 429 ladder that arms the breaker). The band that IS creeping is build_site: ~33m (07-21 measurement) → 35.7m (07-27) → 42.0m (08-03) → 61.7m/57.8m (08-05/08-06) — +25m in two weeks, the single largest step in the job. Profile build_site's page loop before touching anything else. Prior history: was 120 (crept 61m→116m→125m, breached 07-13/07-14); was 70 (cancelled at special-situations pre-commit; fixed by parallelising the ~22 regional/desk builders — serial band ~75m → longest cluster, special-situations' ~28m EDGAR sweep). pages.yml deploys the COMMITTED main tree, so any cancel before "commit engine outputs" leaves the live site serving a stale dashboard until a later run lands.
    env:
      COLLECT_LANE: nightly   # job-level sentinel: inherited by every step; ensures all
                              # forward-ledger writers in this job (build_track_record,
                              # build_flip_confirmation, oracle_nightly, demand_ledger,
                              # build_confluence_strength, archive_signals,
                              # archive_context_snapshots, build_thematic_state,
                              # grade_thematic) can advance their ledgers.
    steps:
      - name: timings — job start mark (W2)
        run: date +%s > "${RUNNER_TEMP}/nightly-timings-${GITHUB_RUN_ID}-${GITHUB_JOB}-start"
      - uses: actions/checkout@v4
        with:
          ref: main
      - run: git pull origin main
      - name: python 3.12 (Homebrew venv — self-hosted runner)
        run: |
          VENV="$HOME/.cache/mm-venv-${RUNNER_NAME:-default}"
          [ -x "$VENV/bin/python" ] || /opt/homebrew/bin/python3.12 -m venv "$VENV"
          echo "$VENV/bin" >> "$GITHUB_PATH"
          "$VENV/bin/python" -m pip install --quiet --upgrade pip
      - run: pip install -r requirements.txt
      - name: timings band — restores-preamble (W2)
        run: python3 scripts/nightly_timings.py mark --band restores-preamble || echo "::warning::timings band mark failed (non-fatal)"
      - name: restore Filing Forensics disclosure projections (hard gate)
        # Acquire/project/sync moved to .github/workflows/filing-forensics-sec.yml
        # (02:30 UTC) on 2026-08-08 after the inline step grew 7.4m -> 23.8m across the
        # 08-05..08-08 engine executions with the growing immutable SEC source store
        # (measurements in the lane file). This step now restores ONLY the 12 private
        # disclosure projections the workbench attaches: one bounded R2 pointer GET, one
        # bounded bundle GET, verified local writes — constant cost as the store grows.
        #
        # Gate contract UNCHANGED: any restore or validation failure fails this job
        # before the broad build, so entitled disclosure coverage is never silently
        # replaced by an empty/shallow state. A red SEC lane leaves the pointer at the
        # last COMPLETE bundle, so the render is stale-but-complete (warn beyond 3 days,
        # refuse beyond 21) instead of losing the night to one SEC outage. Never renders
        # the public shell or publishes private browser state.
        env:
          R2_RESEARCH_ENDPOINT: ${{ secrets.R2_RESEARCH_ENDPOINT }}
          R2_RESEARCH_ACCESS_KEY_ID: ${{ secrets.R2_RESEARCH_ACCESS_KEY_ID }}
          R2_RESEARCH_SECRET_ACCESS_KEY: ${{ secrets.R2_RESEARCH_SECRET_ACCESS_KEY }}
          R2_RESEARCH_BUCKET: ${{ secrets.R2_RESEARCH_BUCKET }}
        run: |
          set -euo pipefail
          now="$(python -c 'from datetime import datetime, timezone; print(datetime.now(timezone.utc).replace(microsecond=0).isoformat().replace("+00:00", "Z"))')"
          python -m scripts.fundamental_forensics_disclosure_bundle \
            --restore --targets-file config/fundamental_forensics/wave2_targets.v1.json --now "$now"
      - name: restore attention store from R2 (wiki chip reads it at render)
        # build_site.build_attention_data reads data/attention/*.parquet — git-ignored
        # since 2026-07-06, R2-hosted, kept current by the collect job's publish-back.
        # Non-fatal: an absent store means the attention chip is silently absent for
        # one render (build_attention_data degrades to None); nothing else reads it.
        env:
          R2_ENDPOINT: ${{ secrets.R2_ENDPOINT }}
          R2_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
          R2_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
          R2_BUCKET: ${{ secrets.R2_BUCKET }}
        run: python -m scripts.fetch_r2 --dirs attention || echo "::warning::attention restore failed (non-fatal — attention chip absent/stale this render)"
      - name: restore odds desk OHLCV store from R2 (engine hops runners; store is runner-local)
        # data/odds_ohlcv/*.parquet is gitignored + R2-backed (published back by this job's
        # R2 publish step) but was never restored, so every runner hop cold-started the
        # store and build_odds re-fetched all 119 tickers at period="max" nightly — and a
        # single-night yfinance flake dropped that ticker from site/oddsdata/catalog.json
        # entirely (AMAT 2026-07-21). With the store restored, build_odds takes its 1mo
        # upsert path (adjustment-basis guard: tests/test_upsert_basis_guard.py), so a
        # flaked ticker degrades to one-day-stale-but-present instead of absent. Non-fatal:
        # on restore failure build_odds cold-backfills exactly as before. Publish-back
        # needs no gate on this step (unlike attention): a cold rebuild is a COMPLETE
        # deep store, and publish_r2 never prunes R2 keys absent locally.
        env:
          R2_ENDPOINT: ${{ secrets.R2_ENDPOINT }}
          R2_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
          R2_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
          R2_BUCKET: ${{ secrets.R2_BUCKET }}
        run: python -m scripts.fetch_r2 --dirs odds_ohlcv || echo "::warning::odds_ohlcv R2 restore failed (non-fatal — build_odds cold-backfills)"
      - name: restore subsector OHLC sidecars from R2 (index_leadership reads them)
        # build_index_leadership (engine step below) computes per-group closes from
        # site/subsectorohlc{,_nasdaq,_russell}/*.json — gitignored + R2-hosted (published
        # back at the end of this job), so a fresh checkout holds NONE of them, compute()
        # found zero tabs, and the builder skipped silently EVERY nightly: index_leadership
        # .json + the data/index_leadership/snapshots.jsonl forward ledger froze at their
        # 2026-06-30 seed for 18 nightlies (experiments audit 2026-08-03). The sidecar
        # producer (build_subsector_confluence main/main_index — only the heavy entrypoints
        # emit them) runs in the parallel band AFTER the engine step, so restore LAST
        # NIGHT's generation here — the same generation as the committed
        # subsector_confluence*.json boards it is read against (the builder is a decoupled
        # post-pass over committed artifacts by design, and stays ordered BEFORE its
        # consumer build_stock_board_v2). Non-fatal: on restore failure the builder now
        # skips LOUDLY (::warning from build(), ::error + rc!=0 from its nightly main).
        env:
          R2_ENDPOINT: ${{ secrets.R2_ENDPOINT }}
          R2_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
          R2_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
          R2_BUCKET: ${{ secrets.R2_BUCKET }}
        run: python -m scripts.fetch_r2 --dirs subsectorohlc,subsectorohlc_nasdaq,subsectorohlc_russell || echo "::warning::subsectorohlc R2 restore failed (non-fatal — index_leadership skips loudly and keeps the last committed board)"
      # restore-keys REMOVED from all five breadth caches below — the SAME defect the
      # china per-name OHLC store 25 lines down already fixed (CN-SYS W5a).
      #
      # A prefix restore-key matches the newest cache under that prefix from ANY prior run,
      # so this job laid a stale parquet on top of the fresh one `collect` had committed
      # hours earlier the same night. Measured 2026-08-06: collect wrote
      # data/breadth/_closes_cache.parquet through the 08-05 close (348 rows x 510 names,
      # commit 361136284f2); the engine job 14 hours later committed it back at 2026-07-31
      # (345 x 509, commit 2dfebf35dbd).
      #
      # WHY THIS BIT BREADTH AND NOT china*/hk*: the commit steps in this file already
      # unstage every asia-close-owned path after `git add data/` (see the W0b 2026-07-08
      # stale-HK incident notes in the collect and engine commit steps), so a stale
      # cache-restore there can never reach a commit. data/breadth is US-owned, is NOT in
      # that unstage list, and so had no such protection.
      #
      # That one regression froze the entire US board. _closes() ends at 07-31, so
      # engine/residual_alpha.py:277 stamps as_of = R.index.max() = 2026-07-31, alpha.json
      # carries it, build_stock_library.py:4267 copies it into us_standouts.json, and the
      # snapshotter — which keys on as_of — saw the same date every night and appended
      # nothing: data/us_board_ledger/snapshots.jsonl dead-ended at 2026-07-31 for six days
      # while the board kept publishing picks ranked on 07-31 factors beside current-day
      # entry zones (the operator's ~$6 gap on FCX). build_stock_library's own
      # _board_continuity_warning had been printing "22 builds all claiming
      # as_of=2026-07-31" throughout, but it is display-only and gates nothing.
      #
      # Dropping the prefix restore-key makes a tracked panel use the fresh copy already in
      # the git checkout instead of overlaying it with an older run's cache. The Russell panel
      # is different: its close cache is gitignored, so engine MUST restore the exact same-run
      # key that collect saved. Keep that producer/consumer key byte-identical; the old
      # `russell-closes-engine-*` key was a guaranteed cold-runner miss and cut ~1,220 names
      # from the keep-FIRST name-score stamp (2026-08-10..14 incident).
      - name: restore breadth close cache (stock search library needs constituent closes)
        uses: actions/cache@v4
        with:
          path: data/breadth/_closes_cache.parquet
          key: breadth-closes-engine-${{ github.run_id }}
      - name: restore smallcap (S&P 600) breadth close cache (stock library needs constituent closes)
        uses: actions/cache@v4
        with:
          path: data/smallcap_breadth/_closes_cache.parquet
          key: smallcap-closes-engine-${{ github.run_id }}
      - name: restore midcap (S&P 400) breadth close cache (stock library needs constituent closes)
        uses: actions/cache@v4
        with:
          path: data/midcap_breadth/_closes_cache.parquet
          key: midcap-closes-engine-${{ github.run_id }}
      - name: restore Russell 2000 breadth close cache (stock library needs constituent closes)
        uses: actions/cache/restore@v4
        with:
          path: data/russell_breadth/_closes_cache.parquet
          key: russell-closes-${{ github.run_id }}
      - name: restore hk breadth close cache (hk stock search library needs constituent closes)
        uses: actions/cache@v4
        with:
          path: data/hk_breadth/_closes_cache.parquet
          key: hk-breadth-closes-engine-${{ github.run_id }}
      - name: restore china per-name OHLC store (build_china_library overlays it for US parity)
        # restore-keys intentionally removed: data/china_stocks is git-tracked and owned by
        # asia-close. Restoring a stale GHA cache here was causing the 5-day lag regression
        # (the engine job's git add data/ + -X theirs rebase-push overwrote the fresh
        # asia-close data with cache-restored stale parquets). CN-SYS W5a fix.
        uses: actions/cache@v4
        with:
          path: data/china_stocks
          key: china-stocks-ohlc-engine-${{ github.run_id }}
      - name: restore hk per-name OHLC store (build_hk_library overlays it for US parity)
        uses: actions/cache@v4
        with:
          path: data/hk_stocks
          key: hk-stocks-ohlc-engine-${{ github.run_id }}
          restore-keys: hk-stocks-ohlc-
      - name: restore intraday (4H) cache (build_chart_data emits site/intraday from it)
        uses: actions/cache@v4
        with:
          path: data/intraday
          key: intraday-engine-${{ github.run_id }}
          restore-keys: intraday-
      - name: price-store freshness gate (last SPY bar vs expected NYSE session; keyless self-heal)
        # 2026-07-07 incident: collect fetched Monday's bars but its non-fatal push died 5x
        # (pre-#1823 unstaged-changes rebase bug) and went GREEN with a ::warning:: — this job
        # then fresh-checked-out a store frozen at 07-02 and persisted a faithful-but-stale
        # RISK_OFF as the canonical market_state, which the live feed carried all session.
        # Every committed store froze in the SAME dead push, so only the exchange calendar
        # (lib/nyse_calendar — zero data dependencies) can see the missing session. --heal
        # re-pulls the yahoo group (keyless, ~16s) so the regime engine below reads real bars
        # and "commit engine outputs" carries them to main. continue-on-error: a red gate must
        # not kill the cached-data dashboard (this job's if:always() philosophy) — it shows RED
        # on the run page while engine.market_state.persist stamps the snapshot freshness.stale.
        continue-on-error: true
        run: python -m scripts.check_price_store_freshness --heal
      - name: MTF buy-filter signals + §7 contract gate (build_signal_quality → validate_signals)
        # Compute the per-ticker chart markers (site/signals/<T>.json) + the brain leaf
        # (data/signal_archive/mtf_signals_latest.json), THEN validate both against
        # research/signal_engine/SCHEMA.json — the §7 signal→chart contract, the single source of
        # truth shared verbatim with the charting app. Runs BEFORE the regime engine so a clean,
        # freshly written leaf is what engine.run loads into latest["mtf_signals"].
        #
        # ABORT-THE-WRITE, NOT-THE-DEPLOY: a contract drift/malformed write must NOT ship, but —
        # like every other builder here — must NOT 404 the dashboard either. So on a validation
        # failure we QUARANTINE the bad write (restore the last-good committed leaf; drop the
        # untracked per-ticker files so the chart safe-degrades to no-markers) and surface a loud
        # red ::error:: annotation + run-summary, then exit 0 so the deploy continues. The bad
        # write never reaches main (leaf reverted, per-ticker files gone) and the violation is
        # recorded in the committed data/quality/signals_audit.json. The HARD red gate for
        # contract-breaking PRs lives in ci.yml (signal-contract job) where blocking is safe.
        # Compute is cheap (reads the committed data/stocks/*.parquet deep-history panel).
        run: |
          set +e
          echo "::group::build + validate MTF signals"
          python -m scripts.build_signal_quality > "$RUNNER_TEMP/sigq.log" 2>&1; bq=$?
          cat "$RUNNER_TEMP/sigq.log"
          if [ "$bq" -ne 0 ]; then
            echo "::error title=build_signal_quality failed (rc=$bq)::signal compute failed — leaf left at last-good"
          fi
          python -m scripts.validate_signals > "$RUNNER_TEMP/sigv.log" 2>&1; vrc=$?
          cat "$RUNNER_TEMP/sigv.log"
          echo "::endgroup::"
          if [ "$vrc" -ne 0 ]; then
            # Discard the drifted/malformed write so neither the commit-to-main nor the Pages
            # artifact can ship it. Leaf AND per-ticker files are tracked on main (per-ticker
            # tracked since 2026-06-28 via the render lane) -> restore last-good committed
            # copies; git clean sweeps only untracked strays (e.g. a new ticker's first write).
            # NEVER `rm` the tracked per-ticker files here: the 226 staged deletions ride the
            # broad `git add site/` into the engine commit and modify/delete-conflict with the
            # intraday render lane's signal commits — the rebase-push then fails every retry
            # and the WHOLE nightly engine commit (regime update + forward ledgers) is dropped
            # (2026-07-12 incident: pick-lab ledgers lost).
            git checkout -- data/signal_archive/mtf_signals_latest.json 2>/dev/null || true
            git checkout -- site/signals/ 2>/dev/null || true
            git clean -fq -- site/signals/ 2>/dev/null || true
            echo "::error title=signal §7 contract gate FAILED::malformed/drifted signal write discarded (leaf + per-ticker files restored to last-good committed copies); see data/quality/signals_audit.json"
            { echo "### ❌ signal §7 contract gate failed — bad write discarded";
              echo '<details><summary>validator output</summary>'; echo; echo '```';
              tail -n 40 "$RUNNER_TEMP/sigv.log"; echo '```'; echo '</details>'; } >> "$GITHUB_STEP_SUMMARY"
          fi
          exit 0
      - name: build signal track-record ledger (w0-stageB)
        # Incremental + cheap (reads site/signals/ + data/stocks/ — both already
        # written by the step above). Runs AFTER build_signal_quality so both
        # site/signals/*.json and mtf_signals_latest.json are fresh. Resilient:
        # any error is logged to stderr and the step exits 0 (never aborts deploy).
        # data/signal_archive/track_record.parquet is git-tracked (un-ignored,
        # w0-stageB) and is staged by the "commit engine outputs" step's
        # `git add data/` below.
        run: |
          echo "::group::build_track_record"
          python -m scripts.build_track_record 2>&1 | tee "$RUNNER_TEMP/tr.log"
          echo "::endgroup::"
          python -m scripts.audit_sizes || echo "::warning::track_record size tripwire fired �� check data/quality/sizes_audit.json"
          exit 0
      - name: export cross-repo signal contracts (golden_signals + artifact_manifest)
        # Writes site/factordata/contracts/golden_signals.json + artifact_manifest.json --
        # the oracle the Terminal's golden_gate.py + artifact_conformance.py consume as a
        # conformance check (audit #7 #9). Runs after build_signal_quality (engine.canon is
        # confirmed working) + after the china/hk OHLC cache restores. Window is FIXED
        # (2015-2024) so output is deterministic and the step is cheap (~5s). Resilient.
        run: |
          python -m scripts.export_signal_contracts \
            || echo "::warning::export_signal_contracts failed (non-fatal) -- keeping last committed contract"
      - name: S&P 500 heatmap real-cap reference (weekly Polygon refresh; committed)
        # (Re)builds data/sp500_heatmap/reference.parquet (~503 rows: ticker → shares +
        # SIC) so build_site's S&P heatmap sizes tiles by REAL market cap (shares × latest
        # close) on EVERY lane. Since site/stockdata left git for R2, the render /
        # engine-render / earlyclose lanes run on fresh checkouts with no cap source and
        # size_basis silently degraded to the ETF weight_proxy. The parquet is small and
        # git-tracked — `git add data/` in "commit engine outputs" below lands it, so all
        # other lanes read real caps at checkout. Runs BEFORE build_site so tonight's
        # payload already sizes by market cap. The script self-gates to a weekly Polygon
        # sweep via the cache's embedded asof stamp (shares move slowly; ~1-2 min when it
        # does sweep). The 2026-08-06 run wedged here for 3h36m until the engine job was
        # cancelled, so none of the downstream build/Prophet steps were scheduled. The
        # 15m cap is deliberately >7x the legitimate refresh and, with continue-on-error,
        # converts another provider/client wedge into a stale-cache fallback instead of a
        # skipped nightly. A failed refresh leaves the previous cache in place and
        # build_sp500_heatmap falls back to site/stockdata caps, then to the last committed
        # marketcap payload's sizes.
        timeout-minutes: 15
        continue-on-error: true
        env:
          POLYGON_API_KEY: ${{ secrets.POLYGON_API_KEY }}
          MASSIVE_API_KEY: ${{ secrets.MASSIVE_API_KEY }}
        run: |
          python -m scripts.build_sp500_heatmap --refresh-caps-only || echo "::warning title=sp500 heatmap cap refresh::rc!=0 (non-fatal — previous cache/fallbacks remain)"
      - name: timings band — engine-core (W2)
        run: python3 scripts/nightly_timings.py mark --band engine-core || echo "::warning::timings band mark failed (non-fatal)"
      - name: run regime engine + build dashboard + daily brief (resilient)
        # Self-diagnosing + deploy-resilient: each builder runs isolated; a failure
        # is surfaced as a ::error:: annotation (readable on the run page + via the
        # public check-runs API) AND appended to the run Summary with its traceback,
        # but NEVER aborts the job — so the site still deploys (via the always()
        # artifact upload) and one broken builder can't 404 the whole dashboard.
        env:
          # catalyst_tone (Tier-A FOMC/dislocation digest) runs INSIDE engine.run, so it
          # needs the DeepSeek key here too — not only on the master_brain step below.
          # No-op (degrades to neutral) when the secret is absent.
          DEEPSEEK_API_KEY: ${{ secrets.DEEPSEEK_API_KEY }}
          # W-L1 confirmation receipt: build_site GETs the evening board
          # (live_flow/us_board_provisional.json) to grade tonight's board against it
          # in the same build that renders it. READ ONLY here — the render publishes
          # nothing; the artifact is written by the reconcile step below, which is the
          # key's single writer. r2io.get_json falls back to the public base without
          # these, but an authenticated GET is not edge-cached, and a stale evening
          # board would silently cost the receipt (the session check refuses it).
          R2_ENDPOINT: ${{ secrets.R2_ENDPOINT }}
          R2_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
          R2_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
          # NWS-01 (#2658) moved build_news INTO this step (pre-build_site) from the
          # parallel cluster below — the financial_news polygon/finnhub legs need the
          # provider keys HERE now, or the keyed nightly itself would ship a dark feed
          # (and after 36h the never-darken guard would lapse into permanent darkness).
          POLYGON_API_KEY: ${{ secrets.POLYGON_API_KEY }}
          MASSIVE_API_KEY: ${{ secrets.MASSIVE_API_KEY }}
          FINNHUB_KEY: ${{ secrets.FINNHUB_KEY }}
          # Live intraday layer: set repo VARIABLE LIVE_QUOTES_WORKER_URL (Settings ->
          # Secrets and variables -> Actions -> Variables) to your deployed Cloudflare
          # quotes-worker URL to turn ON browser live prices — build_site bakes it into
          # site/live_config.js. Empty -> live layer stays dormant (no-op). No code edit needed.
          LIVE_QUOTES_WORKER_URL: ${{ vars.LIVE_QUOTES_WORKER_URL }}
        # Body extracted verbatim to scripts/ci/daily_engine_regime_dashboard.sh (512KB processing-cap diet 2026-08-12 — tests/test_workflow_file_size.py).
        run: bash scripts/ci/daily_engine_regime_dashboard.sh
      - name: publish the provisional → nightly confirmation delta (R2 artifact)
        # W-L1. Runs HERE, after the build step, because that is the first moment
        # site/factordata/us_standouts.json is tonight's board of record — the same
        # document build_site just graded in-process for the render. Same function
        # (engine.close_pass.reconcile.confirmation_receipt), same two immutable
        # inputs, so the rendered receipt and this artifact cannot disagree about a
        # night. Was a workflow_run job on close-pass.yml until 2026-08-10, where it
        # could only ever fire AFTER the render it was supposed to feed.
        # LEDGER LAW: reads two JSON documents, writes one R2 object, touches no
        # data/ path — the nightly remains the sole writer of record.
        continue-on-error: true
        env:
          R2_ENDPOINT: ${{ secrets.R2_ENDPOINT }}
          R2_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
          R2_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
        run: python -m scripts.close_pass_reconcile

      - name: publish Filing Forensics private state
        # Publish only after the full workbench build returned. The publisher validates
        # gzip/schema, writes the existing PRIVATE Research Vault bucket, reads it back,
        # and byte-verifies it. Failure is loud but retains the last-good private object;
        # no premium state is ever staged by the broad site/data commit below.
        continue-on-error: true
        env:
          R2_ENDPOINT: ${{ secrets.R2_ENDPOINT }}
          R2_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
          R2_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
          R2_RESEARCH_ENDPOINT: ${{ secrets.R2_RESEARCH_ENDPOINT }}
          R2_RESEARCH_ACCESS_KEY_ID: ${{ secrets.R2_RESEARCH_ACCESS_KEY_ID }}
          R2_RESEARCH_SECRET_ACCESS_KEY: ${{ secrets.R2_RESEARCH_SECRET_ACCESS_KEY }}
          R2_RESEARCH_BUCKET: ${{ secrets.R2_RESEARCH_BUCKET }}
        run: python -m scripts.build_fundamental_forensics --publish-only
      - name: backfill regime stamps (nw-macro-rail R5)
        # Calls engine.qledger.backfill_regime_stamps() — stamps regime columns onto qledger
        # rows that have as_of within regime_vector.parquet coverage.  Near-term yield ≈ 2 rows
        # (coverage starts 2026-07-01).  Non-fatal: if the function does not yet exist in
        # engine.qledger the step exits 0 with a warning.  Runs AFTER engine.run (regime
        # artifacts are fresh) and BEFORE build_spine_index (index reads the stamped rows).
        run: |
          set +e
          python -m scripts.backfill_regime_stamps > "$RUNNER_TEMP/backfill_regime_stamps.log" 2>&1; rc=$?
          cat "$RUNNER_TEMP/backfill_regime_stamps.log"
          if [ "$rc" -ne 0 ]; then echo "::warning title=backfill_regime_stamps::rc=$rc (non-fatal)"; fi
          exit 0  # PR-2 fix: missing exit 0 caused [ "$rc" -ne 0 ] to exit 1 when rc=0, skipping all downstream kernel steps
      - name: Sector-flip confirmation lens (Policy-Shock W1-C)
        # Detects violent defensive/offense spread flips (|z| >= 2.5 vs 252d), appends the
        # keep-first event ledger at data/flip_confirmation/events.jsonl, registers one
        # CRAWL_BOUNDED (gradeable) qledger claim per CONFIRMED/FADED event at horizon_d=21
        # on the priceable XLP-vs-XLK proxy pair (chassis grades at 5d + 21d automatically;
        # MIXED events abstain). Emits site/flip_confirmation_data.json for the market-state card.
        # AFTER engine.run (market_drivers_log.parquet must be updated for absorption_delta)
        # and AFTER backfill_regime_stamps (qledger consistency). Non-fatal.
        continue-on-error: true
        run: |
          set +e
          python -m scripts.build_flip_confirmation > "$RUNNER_TEMP/flip_confirmation.log" 2>&1; rc=$?
          cat "$RUNNER_TEMP/flip_confirmation.log"
          if [ "$rc" -ne 0 ]; then echo "::warning title=build_flip_confirmation::rc=$rc (non-fatal)"; fi
          exit 0
      - name: Leader-pullback coverage (§6.9 R4 publisher — MUST precede build_prophet)
        # Publishes site/anticipationdata/us_leader_pullback.json: the engine.us_leader_pullback
        # organ's per-name state over the graded universe, evaluated on a run-wide PIT
        # cross-sectional RS percentile (the organ refuses to reach for one itself, so a
        # single-name call can never leak a cross-section).
        #
        # ORDER IS LOAD-BEARING. engine.us_early_turn resolves the EARLY-TURN starter tier's
        # LEADER licence from this file at ORIGINATION time. #5007 shipped the organ and #5105
        # shipped the intake, but nothing published the artifact — so the leader half read {}
        # on every production run and was structurally incapable of admitting anything, while
        # the washout half (us_basket_turn) worked. Scheduled after build_site (the price
        # store is settled) and immediately before the Prophet band.
        #
        # DISPLAY TIER. Advances NO ledger and writes no data/ path. A degraded input
        # publishes fewer names with their reasons printed; a RUN-WIDE data failure publishes
        # nothing and leaves the previous artifact in place, because a board of nulls and a
        # broken price store look identical from downstream. Measured 2026-08-09: ~39s over
        # 680 graded names (budget 600s, self-reported and ::warning past it).
        timeout-minutes: 12
        continue-on-error: true
        run: |
          set +e
          echo "::group::build_leader_pullback_coverage (EARLY-TURN leader licence)"
          python -m scripts.build_leader_pullback_coverage > "$RUNNER_TEMP/leader_pullback_coverage.log" 2>&1; rc=$?
          cat "$RUNNER_TEMP/leader_pullback_coverage.log"
          echo "::endgroup::"
          if [ "$rc" -ne 0 ]; then echo "::warning title=build_leader_pullback_coverage::rc=$rc (non-fatal — the intake keeps yesterday's coverage until tomorrow's run)"; fi
          exit 0
      - name: timings band — prophet (W2)
        run: python3 scripts/nightly_timings.py mark --band prophet || echo "::warning::timings band mark failed (non-fatal)"
      - name: Prophet nightly (plan refresh + ledger advancement; R2 after checkpoint)
        id: prophet_nightly
        # AFTER build_site: reads site/factordata/us_standouts.json (just written).
        # Originates new prophet.trade_plan/v1 envelopes, runs the management engine
        # for every active plan, advances the forward outcome ledger (nightly is the
        # SOLE advancer — idempotent per plan ID). R2 publication is deliberately
        # NOT performed here: only the post-checkpoint step may mirror the exact
        # committed site/prophet/index.json to key "prophet/index.json".
        # A successful run is checkpointed to main by the immediately-following
        # step. The later broad "commit engine outputs" explicitly restores and
        # excludes Prophet paths, so this narrow checkpoint is their sole git publisher
        # and never waits behind the regional-builder barrier.
        # continue-on-error: true — a prophet failure must never abort the nightly deploy.
        # R2 credentials are intentionally absent from this generation step. They
        # exist only on the post-checkpoint publisher below.
        # Wall-time: SECONDS measured (24s on the green 2026-08-08 run, <2m every other
        # observed night) — but on 2026-08-07 this step WEDGED pre-first-write for 173m
        # on mac-builder-1 (run 31210097197: 23:07:57→02:00:56, cancelled AT the job cap,
        # ZERO site/prophet|data/prophet|data/prophet_arena writes in its 02:04 commit)
        # and, because stdout was buffered into RUNNER_TEMP and cat only on completion,
        # the cancelled step left NO log at all. Hence belt-and-braces like the arming
        # pass below: timeout-minutes bounds a wedge (15m ≥ 7× the worst legit night),
        # continue-on-error keeps the bound non-fatal, and output now STREAMS
        # (PYTHONUNBUFFERED, no temp-file buffer) so a killed step shows exactly
        # where it died.
        timeout-minutes: 15
        continue-on-error: true
        env:
          PYTHONUNBUFFERED: "1"
        run: bash scripts/ci/daily_engine_prophet_nightly.sh
      - name: prophet-board-acceptance (post-publish alarm, never a gate)
        id: prophet_board_acceptance
        # Same job, same runner, same bytes as the "Prophet nightly" step just
        # above — placed immediately after it and BEFORE the checkpoint commit
        # below so there is no cross-job pull race and no window where a later
        # reader sees something this check did not. ALARM, never a GATE: see
        # scripts/prophet_board_acceptance.py's module docstring for the full
        # honest-scope statement (a no-op on a night this job never reaches; dead
        # if the run is cancelled before it — that class belongs to
        # scripts/check_nightly_liveness.py and scripts/prophet_rescue.py, not
        # this step). continue-on-error keeps a failure here from blocking the
        # checkpoint/commit/publish steps that follow, exactly like every other
        # Prophet step in this job — a failure still reaches the job's overall
        # conclusion via the step's own nonzero exit plus the ::error annotation,
        # which is what pages through GitHub's own check-run surface.
        continue-on-error: true
        timeout-minutes: 5
        env:
          PYTHONUNBUFFERED: "1"
        run: |
          python3 -m scripts.prophet_board_acceptance --run-id "${GITHUB_RUN_ID}"
      - name: checkpoint Prophet outputs to main (durable before engine tail)
        id: prophet_checkpoint
        if: steps.prophet_nightly.outputs.succeeded == 'true'
        timeout-minutes: 12
        continue-on-error: true
        env:
          PROPHET_DELTA_MANIFEST: ${{ steps.prophet_nightly.outputs.delta_manifest }}
        # The engine worktree is intentionally very dirty here: build_site and
        # the pre-Prophet builders have produced thousands of not-yet-normalized
        # files. Publish the proven per-file delta from a clean temporary
        # worktree instead. Correction/provenance ledgers never cross this
        # boundary, deletions are forbidden, and any same-path change on main
        # aborts the checkpoint rather than being conflict-resolved away.
        run: bash scripts/ci/daily_engine_prophet_checkpoint.sh
      - name: publish Prophet public health receipt to R2
        if: steps.prophet_checkpoint.outputs.r2_ready == 'true'
        timeout-minutes: 5
        continue-on-error: true
        env:
          PROPHET_CHECKPOINT_SHA: ${{ steps.prophet_checkpoint.outputs.checkpoint_sha }}
          PROPHET_INDEX_SHA256: ${{ steps.prophet_checkpoint.outputs.index_sha256 }}
          R2_ENDPOINT: ${{ secrets.R2_ENDPOINT }}
          R2_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
          R2_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
          R2_BUCKET: ${{ secrets.R2_BUCKET }}
        # Reconstruct from the accepted Git commit, verify its content hash, then
        # re-check current main immediately before upload — same checkpoint-proof
        # machinery as before. What changed is the PAYLOAD (DEC:B1-PROPHET-PUBLIC-
        # SPLIT): the full plan book is premium/private and must never reach public
        # R2 again, so this publisher builds and puts ONLY the minimal public health
        # projection (prophet/health.json). It no longer owns the forbidden-key
        # tombstone — that self-heal runs unconditionally in the dedicated
        # "enforce Prophet R2 index tombstone" step below, on every nightly wake
        # regardless of whether this step ran (r2_ready gates ONLY on an index
        # content change, so an unchanged-index night must not skip the tombstone).
        run: |
          set -euo pipefail
          R2_INDEX_FILE="$(mktemp "${RUNNER_TEMP}/prophet-r2-index.XXXXXX.json")"
          cleanup_prophet_r2() { rm -f -- "$R2_INDEX_FILE"; }
          trap cleanup_prophet_r2 EXIT

          verify_prophet_checkpoint_current() {
            git fetch origin +refs/heads/main:refs/remotes/origin/main \
              || return 1
            git merge-base --is-ancestor "$PROPHET_CHECKPOINT_SHA" origin/main \
              || return 1
            git diff --quiet "$PROPHET_CHECKPOINT_SHA" origin/main -- \
              data/prophet \
              data/prophet_arena \
              site/prophet
          }

          if ! verify_prophet_checkpoint_current; then
            echo "::warning title=Prophet R2 publish superseded::checkpoint $PROPHET_CHECKPOINT_SHA is not the current origin/main Prophet projection; preserving the newer R2/main authority"
            exit 0
          fi
          git show "${PROPHET_CHECKPOINT_SHA}:site/prophet/index.json" > "$R2_INDEX_FILE"
          ACTUAL_SHA256="$(python3 -c 'import hashlib,sys; print(hashlib.sha256(open(sys.argv[1], "rb").read()).hexdigest())' "$R2_INDEX_FILE")"
          if [ "$ACTUAL_SHA256" != "$PROPHET_INDEX_SHA256" ]; then
            echo "::error title=Prophet R2 publish hash mismatch::commit bytes do not match the checkpoint output; refusing R2 upload"
            exit 1
          fi
          # Close the fetch/hash window as far as the object store permits. R2
          # has no compare-and-swap against a Git ref, so a second main proof is
          # the last action before the single-object upload.
          if ! verify_prophet_checkpoint_current; then
            echo "::warning title=Prophet R2 publish superseded::origin/main Prophet state advanced during verification; preserving the newer publication"
            exit 0
          fi
          python3 - "$R2_INDEX_FILE" <<'PY'
          import json
          import os
          import subprocess
          import sys
          from datetime import datetime, timezone
          from pathlib import Path

          from scripts.build_prophet import (
              R2_HEALTH_KEY,
              _r2_client,
              build_public_health_projection,
              guarded_put_object,
          )

          local_path = Path(sys.argv[1])
          client = _r2_client()
          if client is None:
              print(
                  "::warning title=Prophet R2 credentials unavailable::"
                  "guarded Git checkpoint landed; R2 remained unchanged",
                  flush=True,
              )
              raise SystemExit(0)
          from botocore.exceptions import ClientError

          bucket = os.environ.get("R2_BUCKET") or "mastermindx"
          expected_sha = os.environ["PROPHET_INDEX_SHA256"]
          checkpoint_sha = os.environ["PROPHET_CHECKPOINT_SHA"]
          published_at = datetime.now(timezone.utc).strftime("%Y-%m-%dT%H:%M:%SZ")

          index = json.loads(local_path.read_text(encoding="utf-8"))
          health_payload = build_public_health_projection(
              index,
              checkpoint_sha=checkpoint_sha,
              index_sha256=expected_sha,
              published_at=published_at,
          )
          health_bytes = json.dumps(
              health_payload, allow_nan=False, default=str, indent=2,
          ).encode("utf-8")

          # Snapshot the destination generation first. The conditional put below
          # makes any concurrent R2 writer win rather than be overwritten.
          try:
              current = client.head_object(Bucket=bucket, Key=R2_HEALTH_KEY)
          except ClientError as exc:
              response = exc.response or {}
              status = (response.get("ResponseMetadata") or {}).get("HTTPStatusCode")
              code = str((response.get("Error") or {}).get("Code") or "")
              if status == 404 or code in {"404", "NoSuchKey", "NotFound"}:
                  condition = {"IfNoneMatch": "*"}
              else:
                  print(
                      "::error title=Prophet R2 preflight failed::"
                      "could not read the current object generation; upload withheld",
                      flush=True,
                  )
                  raise SystemExit(1) from exc
          else:
              etag = str(current.get("ETag") or "")
              metadata = current.get("Metadata") or {}
              if (
                  metadata.get("git-checkpoint") == checkpoint_sha
                  and metadata.get("sha256") == expected_sha
              ):
                  print(
                      "::notice title=Prophet R2 already current::"
                      "the guarded checkpoint hash is already published",
                      flush=True,
                  )
                  raise SystemExit(0)
              if not etag:
                  print(
                      "::error title=Prophet R2 preflight failed::"
                      "current object supplied no ETag for a conditional update",
                      flush=True,
                  )
                  raise SystemExit(1)
              condition = {"IfMatch": etag}

          # Re-prove Git after the R2 generation snapshot. If main or R2 moves
          # after its respective proof, the conditional put rejects the race.
          git_checks = (
              ["git", "fetch", "origin", "+refs/heads/main:refs/remotes/origin/main"],
              ["git", "merge-base", "--is-ancestor", checkpoint_sha, "origin/main"],
              [
                  "git", "diff", "--quiet", checkpoint_sha, "origin/main", "--",
                  "data/prophet", "data/prophet_arena", "site/prophet",
              ],
          )
          if any(subprocess.run(cmd, check=False).returncode for cmd in git_checks):
              print(
                  "::warning title=Prophet R2 publish superseded::"
                  "current main changed during R2 preflight; upload withheld",
                  flush=True,
              )
              raise SystemExit(0)

          try:
              guarded_put_object(
                  client,
                  bucket=bucket,
                  key=R2_HEALTH_KEY,
                  Body=health_bytes,
                  ContentType="application/json",
                  Metadata={
                      "git-checkpoint": checkpoint_sha,
                      "sha256": expected_sha,
                  },
                  **condition,
              )
          except ClientError as exc:
              status = ((exc.response or {}).get("ResponseMetadata") or {}).get(
                  "HTTPStatusCode"
              )
              if status == 412:
                  print(
                      "::warning title=Prophet R2 publish superseded::"
                      "the R2 object advanced concurrently; preserving that writer",
                      flush=True,
                  )
                  raise SystemExit(0) from exc
              print(
                  "::error title=Prophet R2 upload failed::"
                  "guarded Git checkpoint landed but its verified health receipt did not reach R2",
                  flush=True,
              )
              raise SystemExit(1) from exc
          print(
              "::notice title=Prophet R2 published::"
              "uploaded the public health receipt for the guarded Git checkpoint",
              flush=True,
          )
          PY
      - name: enforce Prophet R2 index tombstone (unconditional)
        if: ${{ !cancelled() }}
        timeout-minutes: 5
        continue-on-error: true
        env:
          R2_ENDPOINT: ${{ secrets.R2_ENDPOINT }}
          R2_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
          R2_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
          R2_BUCKET: ${{ secrets.R2_BUCKET }}
        # Reviewer finding (§8b): the previous tombstone lived inside the
        # publisher step above, which is gated `r2_ready == 'true'` — and
        # r2_ready is set ONLY when the checkpointed index content actually
        # changed that night. On an unchanged-index night the publisher step
        # is skipped entirely, so NO tombstone ran and a reappeared
        # prophet/index.json on public R2 (DEC:B1-PROPHET-PUBLIC-SPLIT
        # forbids it — the full plan book must never be public) would sit
        # undetected until the next content-changing night. This step is
        # decoupled from that gate: it runs on every nightly wake the R2
        # creds exist, independent of whether the health publish ran, and is
        # the sole tombstone owner (the publisher step no longer enforces
        # it). `!cancelled()` runs this even if an earlier step in the job
        # failed, so a red upstream step never suppresses the self-heal.
        run: |
          python3 - <<'PY'
          import os

          from scripts.build_prophet import _r2_client

          # Forbidden public key literal (DEC:B1-PROPHET-PUBLIC-SPLIT), named
          # here rather than imported — the tombstone check/delete never
          # needs a live index, only the key it must never let stand.
          FORBIDDEN_INDEX_KEY = "prophet/index.json"

          client = _r2_client()
          if client is None:
              print(
                  "::notice title=Prophet R2 tombstone skipped::"
                  "R2 credentials unavailable; nothing to enforce",
                  flush=True,
              )
              raise SystemExit(0)
          from botocore.exceptions import ClientError

          bucket = os.environ.get("R2_BUCKET") or "mastermindx"

          try:
              client.head_object(Bucket=bucket, Key=FORBIDDEN_INDEX_KEY)
          except ClientError as exc:
              response = exc.response or {}
              status = (response.get("ResponseMetadata") or {}).get("HTTPStatusCode")
              code = str((response.get("Error") or {}).get("Code") or "")
              if status == 404 or code in {"404", "NoSuchKey", "NotFound"}:
                  print(
                      "::notice title=Prophet R2 tombstone::"
                      f"{FORBIDDEN_INDEX_KEY} already absent",
                      flush=True,
                  )
                  raise SystemExit(0)
              print(
                  "::warning title=Prophet R2 tombstone check failed::"
                  f"could not verify {FORBIDDEN_INDEX_KEY} absence: {exc}",
                  flush=True,
              )
              raise SystemExit(0)

          try:
              client.delete_object(Bucket=bucket, Key=FORBIDDEN_INDEX_KEY)
          except Exception as exc:  # noqa: BLE001 — deletion failure is non-fatal
              print(
                  "::warning title=Prophet R2 tombstone delete failed::"
                  f"forbidden public object {FORBIDDEN_INDEX_KEY} could not be removed: {exc}",
                  flush=True,
              )
              raise SystemExit(0)
          print(
              "::notice title=Prophet R2 tombstone::"
              f"removed forbidden public object {FORBIDDEN_INDEX_KEY}",
              flush=True,
          )
          PY
      - name: restore accepted Prophet source for downstream derivations
        id: prophet_accepted_source
        if: always()
        timeout-minutes: 3
        continue-on-error: true
        # Stage-shadow and later observational lanes must never read the dirty
        # local build after its checkpoint was refused. Rehydrate every source
        # they consume from current accepted main instead. This deliberately
        # permits a safe no-op night: previously accepted plans may still mature
        # and grade even when this run originated no new plan.
        run: |
          set -euo pipefail
          EVENT_REF="${GITHUB_REF:-}"
          SOURCE_BRANCH="$(git symbolic-ref --short -q HEAD || true)"
          if [ "$EVENT_REF" != "refs/heads/main" ] \
            || [ "$SOURCE_BRANCH" != "main" ]; then
            echo "::warning title=Prophet downstream source withheld::off-main event/checkout cannot advance Prophet-derived ledgers"
            exit 0
          fi
          git fetch origin +refs/heads/main:refs/remotes/origin/main
          ACCEPTED_PROPHET_SHA="$(git rev-parse origin/main)"
          git checkout "$ACCEPTED_PROPHET_SHA" -- \
            site/prophet \
            data/prophet \
            data/prophet_arena \
            data/prophet_stage_shadow
          # checkout does not remove generated additions absent from the accepted
          # tree. Plans/states/arena are closed builder-owned roots, so remove
          # only those additions; data/prophet correction ledgers are never
          # cleaned and were restored from accepted main above.
          git clean -fd -- \
            site/prophet \
            data/prophet/origination_receipts \
            data/prophet_arena \
            data/prophet_stage_shadow
          RESTORE_MISMATCH=false
          if ! git diff --cached --quiet "$ACCEPTED_PROPHET_SHA" -- \
              site/prophet data/prophet data/prophet_arena data/prophet_stage_shadow \
            || ! git diff --quiet -- \
              site/prophet data/prophet data/prophet_arena data/prophet_stage_shadow \
            || [ -n "$(git ls-files --others --exclude-standard -- data/prophet)" ]; then
            RESTORE_MISMATCH=true
          fi
          # Keep accepted bytes in the worktree for downstream readers, but do
          # not leave them staged against this job's older checkout-time HEAD.
          # This reset must follow verification: a checkpoint can legitimately
          # add plans/receipts/Arena ledgers that do not exist in HEAD, and an
          # earlier reset makes those accepted paths look deleted/untracked.
          git reset -q -- \
            site/prophet \
            data/prophet \
            data/prophet_arena \
            data/prophet_stage_shadow
          if [ "$RESTORE_MISMATCH" = "true" ]; then
            echo "::error title=Prophet downstream source mismatch::engine worktree could not be made byte-equivalent to accepted main; derived ledgers withheld"
            exit 1
          fi
          echo "ready=true" >> "$GITHUB_OUTPUT"
          echo "accepted_sha=$ACCEPTED_PROPHET_SHA" >> "$GITHUB_OUTPUT"
      - name: Prophet × Stage forward-shadow (tag actual entries + nightly grade advance)
        id: prophet_stage_shadow
        if: steps.prophet_accepted_source.outputs.ready == 'true'
        # AFTER the accepted-source restore: reads site/prophet/{index.json,plans/*.json}
        # + data/prophet/ledger.jsonl from current origin/main. That is this run's
        # checkpoint when it landed, or the prior/newer accepted projection when the
        # local build was refused/superseded. Tags every Prophet entry with its PIT
        # stage-at-entry + last-EC (idempotent,
        # PIT-fixed at entry), advances matured grades (nightly is the SOLE advancer — COLLECT_LANE=nightly
        # gate), writes data/prophet_stage_shadow/summary.json (display-only). This is the DEFINITIVE
        # on-Prophet test the fusion backtest left open (research/PROPHET_STAGE_FUSION_PREREG.md §6; the
        # win-rate-gate construction itself is KILLED — this NEVER gates/ranks/alters Prophet, forward
        # accrual only, first 126d cohort ~Dec 2026). Raw PIT tags, append-only
        # revisions and the effective summary are tracked as one closed output set;
        # the immediately-following narrow checkpoint is their sole publisher.
        # Seconds; non-fatal to the wider engine job.
        continue-on-error: true
        env:
          COLLECT_LANE: nightly
        run: |
          set +e
          STAGE_BASELINE="${RUNNER_TEMP}/prophet-stage-${GITHUB_RUN_ID}.before.json"
          STAGE_DELTA="${RUNNER_TEMP}/prophet-stage-${GITHUB_RUN_ID}.delta.tsv"
          export STAGE_BASELINE STAGE_DELTA
          python3 - <<'PY'
          import hashlib
          import json
          import os
          from pathlib import Path

          root = Path(os.environ["GITHUB_WORKSPACE"])
          owned = (
              "data/prophet_stage_shadow/ledger.jsonl",
              "data/prophet_stage_shadow/revisions.jsonl",
              "data/prophet_stage_shadow/summary.json",
          )

          def fingerprint(path: Path) -> str:
              if path.is_symlink():
                  raise RuntimeError(f"refusing symlinked Stage-shadow output: {path}")
              return (
                  f"{path.stat().st_mode & 0o7777:04o}:"
                  f"{hashlib.sha256(path.read_bytes()).hexdigest()}"
              )

          before = {
              rel: fingerprint(root / rel)
              for rel in owned
              if (root / rel).is_file()
          }
          Path(os.environ["STAGE_BASELINE"]).write_text(
              json.dumps(before, sort_keys=True), encoding="utf-8"
          )
          PY
          baseline_rc=$?
          if [ "$baseline_rc" -ne 0 ]; then
            echo "succeeded=false" >> "$GITHUB_OUTPUT"
            echo "::warning title=Prophet Stage manifest::could not fingerprint the accepted Stage-shadow baseline (rc=$baseline_rc); accrual checkpoint withheld"
            exit 0
          fi
          echo "::group::build_prophet_stage_shadow (tag + grade + summarize)"
          python -m scripts.build_prophet_stage_shadow > "$RUNNER_TEMP/prophet_stage_shadow.log" 2>&1; rc=$?
          cat "$RUNNER_TEMP/prophet_stage_shadow.log"
          echo "::endgroup::"
          if [ "$rc" -ne 0 ]; then
            echo "succeeded=false" >> "$GITHUB_OUTPUT"
            echo "::warning title=build_prophet_stage_shadow::rc=$rc (non-fatal; accrual checkpoint withheld)"
            exit 0
          fi
          python3 - <<'PY'
          import hashlib
          import json
          import os
          import sys
          from pathlib import Path

          from engine import prophet_stage_shadow as shadow

          root = Path(os.environ["GITHUB_WORKSPACE"])
          data_root = root / "data"
          site_root = root / "site"
          owned = (
              "data/prophet_stage_shadow/ledger.jsonl",
              "data/prophet_stage_shadow/revisions.jsonl",
              "data/prophet_stage_shadow/summary.json",
          )

          def fingerprint(path: Path) -> str:
              if path.is_symlink():
                  raise RuntimeError(f"refusing symlinked Stage-shadow output: {path}")
              return (
                  f"{path.stat().st_mode & 0o7777:04o}:"
                  f"{hashlib.sha256(path.read_bytes()).hexdigest()}"
              )

          before = json.loads(
              Path(os.environ["STAGE_BASELINE"]).read_text(encoding="utf-8")
          )
          after = {
              rel: fingerprint(root / rel)
              for rel in owned
              if (root / rel).is_file()
          }
          deleted = sorted(set(before) - set(after))
          if deleted:
              print(
                  "::error title=Prophet Stage output deletion::"
                  + ", ".join(deleted)
                  + " — append-only checkpoint refuses deletions",
                  flush=True,
              )
              raise SystemExit(2)

          summary_file = root / "data/prophet_stage_shadow/summary.json"
          if not summary_file.is_file():
              print("::error title=Prophet Stage summary missing::no effective projection was emitted", flush=True)
              raise SystemExit(3)
          summary = json.loads(summary_file.read_text(encoding="utf-8"))
          projection = shadow._project_shadow_ledger(data_root)
          expected_entries = shadow.collect_prophet_entries(site_root, data_root)
          missing_ids, unexpected_ids = shadow.projection_membership_gaps(
              expected_entries, projection
          )
          receipt = summary.get("integrity_projection") or {}
          if (
              summary.get("schema") != shadow.SUMMARY_SCHEMA
              or projection.authority_error is not None
              or receipt.get("authority_error") is not None
              or receipt.get("raw_rows") != projection.raw_count
              or receipt.get("revision_rows") != projection.revision_count
              or receipt.get("effective_rows") != len(projection.rows)
              or summary.get("n_entries") != len(projection.rows)
              # Lossless cohort contract: collect_prophet_entries already applies
              # current canonical quarantine. Every remaining expected plan must
              # have exactly one effective shadow row, and no orphan row may survive.
              or missing_ids
              or unexpected_ids
          ):
              print(
                  "::error title=Prophet Stage projection mismatch::"
                  "raw ledger, append-only revisions and summary do not form one "
                  "validated effective projection; checkpoint withheld",
                  flush=True,
              )
              raise SystemExit(4)

          changed = sorted(path for path, value in after.items() if before.get(path) != value)
          with Path(os.environ["STAGE_DELTA"]).open("w", encoding="utf-8") as handle:
              for rel in changed:
                  handle.write(f"{rel}\t{before.get(rel, 'MISSING')}\t{after[rel]}\n")
          PY
          manifest_rc=$?
          if [ "$manifest_rc" -eq 0 ]; then
            echo "succeeded=true" >> "$GITHUB_OUTPUT"
            echo "delta_manifest=$STAGE_DELTA" >> "$GITHUB_OUTPUT"
          else
            echo "succeeded=false" >> "$GITHUB_OUTPUT"
            echo "::warning title=Prophet Stage manifest::accrual returned 0 but its effective projection could not be proven (rc=$manifest_rc); checkpoint withheld"
          fi
          exit 0
      - name: checkpoint Prophet Stage-shadow accrual to main
        id: prophet_stage_checkpoint
        if: steps.prophet_stage_shadow.outputs.succeeded == 'true'
        timeout-minutes: 10
        continue-on-error: true
        env:
          STAGE_DELTA_MANIFEST: ${{ steps.prophet_stage_shadow.outputs.delta_manifest }}
          ACCEPTED_PROPHET_SHA: ${{ steps.prophet_accepted_source.outputs.accepted_sha }}
        # Raw PIT evidence, correction/grade revisions, and their summary move as
        # one hash-verified set from a clean worktree. A newer Prophet authority
        # projection or same-path Stage writer wins; there is no conflict override.
        run: |
          set -euo pipefail
          . "${GITHUB_WORKSPACE:-.}/scripts/ci/push_retry.sh"
          git config user.name "dashboard-bot"
          git config user.email "actions@users.noreply.github.com"

          EVENT_REF="${GITHUB_REF:-}"
          SOURCE_BRANCH="$(git -C "$GITHUB_WORKSPACE" symbolic-ref --short -q HEAD || true)"
          if [ "$EVENT_REF" != "refs/heads/main" ] \
            || [ "$SOURCE_BRANCH" != "main" ] \
            || [ -z "${ACCEPTED_PROPHET_SHA:-}" ]; then
            echo "::error title=Prophet Stage checkpoint source rejected::only an accepted main Prophet projection may advance the Stage-shadow ledger"
            exit 1
          fi
          if [ ! -f "${STAGE_DELTA_MANIFEST:-}" ]; then
            echo "::error title=Prophet Stage manifest missing::no readable closed-output delta was supplied"
            exit 1
          fi
          if [ ! -s "$STAGE_DELTA_MANIFEST" ]; then
            echo "Prophet Stage checkpoint: no accrual changes"
            exit 0
          fi

          git -C "$GITHUB_WORKSPACE" fetch origin \
            +refs/heads/main:refs/remotes/origin/main
          PROPHET_AUTHORITY_PATHS=(data/prophet data/prophet_arena site/prophet)
          if ! git -C "$GITHUB_WORKSPACE" merge-base --is-ancestor \
              "$ACCEPTED_PROPHET_SHA" origin/main \
            || ! git -C "$GITHUB_WORKSPACE" diff --quiet \
              "$ACCEPTED_PROPHET_SHA" origin/main -- \
              "${PROPHET_AUTHORITY_PATHS[@]}" \
            || ! git -C "$GITHUB_WORKSPACE" diff --quiet \
              "$ACCEPTED_PROPHET_SHA" origin/main -- data/prophet_stage_shadow; then
            echo "::error title=Prophet Stage checkpoint superseded::Prophet authority or Stage-shadow history advanced after the accepted source snapshot; preserving main"
            exit 1
          fi

          STAGE_CHECKPOINT_ROOT="$(mktemp -d "${RUNNER_TEMP}/prophet-stage-checkpoint.XXXXXX")"
          STAGE_CHECKPOINT_DIR="${STAGE_CHECKPOINT_ROOT}/tree"
          STAGE_CHECKPOINT_BRANCH="_prophet_stage_checkpoint_${GITHUB_RUN_ID}_${GITHUB_RUN_ATTEMPT:-1}"
          cleanup_stage_checkpoint() {
            cd "$GITHUB_WORKSPACE"
            git worktree remove --force "$STAGE_CHECKPOINT_DIR" 2>/dev/null || true
            git branch -D "$STAGE_CHECKPOINT_BRANCH" 2>/dev/null || true
            rmdir "$STAGE_CHECKPOINT_ROOT" 2>/dev/null || true
          }
          trap cleanup_stage_checkpoint EXIT
          git worktree add -b "$STAGE_CHECKPOINT_BRANCH" \
            "$STAGE_CHECKPOINT_DIR" origin/main
          cd "$STAGE_CHECKPOINT_DIR"

          while IFS=$'\t' read -r rel before_sha after_sha; do
            [ -n "$rel" ] || continue
            case "$rel" in
              data/prophet_stage_shadow/ledger.jsonl|\
              data/prophet_stage_shadow/revisions.jsonl|\
              data/prophet_stage_shadow/summary.json) ;;
              *)
                echo "::error title=Prophet Stage path rejected::$rel is outside the closed Stage-shadow output set"
                exit 1
                ;;
            esac
            if [ ! -f "$GITHUB_WORKSPACE/$rel" ] || [ -L "$GITHUB_WORKSPACE/$rel" ]; then
              echo "::error title=Prophet Stage output rejected::$rel is missing or symlinked; deletions are forbidden"
              exit 1
            fi
            current_after="$(python3 -c 'import hashlib,os,sys; p=sys.argv[1]; print(f"{os.stat(p).st_mode & 0o7777:04o}:"+hashlib.sha256(open(p,"rb").read()).hexdigest())' "$GITHUB_WORKSPACE/$rel")"
            if [ "$current_after" != "$after_sha" ]; then
              echo "::error title=Prophet Stage source changed::$rel changed after its validated projection; checkpoint withheld"
              exit 1
            fi
            if [ -L "$STAGE_CHECKPOINT_DIR/$rel" ]; then
              echo "::error title=Prophet Stage main symlink rejected::$rel"
              exit 1
            elif [ -f "$STAGE_CHECKPOINT_DIR/$rel" ]; then
              main_before="$(python3 -c 'import hashlib,os,sys; p=sys.argv[1]; print(f"{os.stat(p).st_mode & 0o7777:04o}:"+hashlib.sha256(open(p,"rb").read()).hexdigest())' "$STAGE_CHECKPOINT_DIR/$rel")"
            else
              main_before="MISSING"
            fi
            if [ "$main_before" != "$before_sha" ]; then
              echo "::error title=Prophet Stage same-path race::origin/main changed $rel; preserving its append-only history"
              exit 1
            fi
            mkdir -p "$(dirname "$rel")"
            cp -p "$GITHUB_WORKSPACE/$rel" "$rel"
            git add -- "$rel"
          done < "$STAGE_DELTA_MANIFEST"

          if git diff --cached --quiet; then
            echo "Prophet Stage checkpoint: no staged changes"
            exit 0
          fi
          push_staged_clean || exit 1
          git commit -m "prophet-stage: durable nightly accrual $(date -u +%F)"
          STAGE_CHECKPOINT_PARENT="$(git rev-parse HEAD^)"
          PUSH_ALARM=120
          PUSH_BUDGET_SECS=420
          PUSH_MAX_ATTEMPTS=12
          PUSH_MAIN_BRANCHES="$STAGE_CHECKPOINT_BRANCH"
          push_retry_init "Prophet Stage-shadow durable checkpoint"
          push_on_main_ok || exit 0
          while push_attempt; do
            if ! push_fetch_main_for_rebase; then
              push_abort_rebase
              push_backoff
              continue
            fi
            if ! git merge-base --is-ancestor "$ACCEPTED_PROPHET_SHA" origin/main \
              || ! git diff --quiet "$ACCEPTED_PROPHET_SHA" origin/main -- \
                "${PROPHET_AUTHORITY_PATHS[@]}" \
              || ! git diff --quiet "$ACCEPTED_PROPHET_SHA" origin/main -- \
                data/prophet_stage_shadow; then
              echo "::error title=Prophet Stage authority race::Prophet inputs or Stage-shadow history advanced during publish; preserving main"
              exit 1
            fi
            same_path=""
            while IFS=$'\t' read -r rel _ _; do
              [ -n "$rel" ] || continue
              if ! git diff --quiet "$STAGE_CHECKPOINT_PARENT" origin/main -- "$rel"; then
                same_path="${same_path}${same_path:+, }${rel}"
              fi
            done < "$STAGE_DELTA_MANIFEST"
            if [ -n "$same_path" ]; then
              echo "::error title=Prophet Stage same-path race::origin/main advanced $same_path; no conflict override is allowed"
              exit 1
            fi
            if perl -e 'alarm 180; exec @ARGV or die' -- git rebase origin/main; then
              STAGE_CHECKPOINT_PARENT="$(git rev-parse HEAD^)"
              if push_do origin HEAD:main; then
                echo "pushed Prophet Stage-shadow checkpoint on attempt $PUSH_ATTEMPT"
                push_won
                exit 0
              fi
            fi
            push_abort_rebase
            push_backoff
          done
          push_lost
          echo "::error title=Prophet Stage checkpoint NOT pushed::$PUSH_ATTEMPT attempts failed ($PUSH_STOP); the broad engine commit is fenced from these unaccepted bytes"
          exit 1
      - name: Prophet Live arming pass (provisional-close trigger pack -> R2)
        # AFTER build_site (same close stores the board was built from). Probes the
        # SAME close-only admission gate (engine/signal_gate.py) with candidate
        # provisional closes and publishes per-name trigger/fade levels to R2 key
        # live_flow/prophet_live_armed.json, so the */5 prophet-live.yml lane can
        # compare a delayed price to a level instead of re-deriving a signal
        # (research/PROPHET_LIVE_INTRADAY_SIGNALS_MASTERPLAN_BY_FABLE.md §4.1).
        #
        # WRITES NO data/ PATH — the pack is runtime-tier and lives only in R2.
        # The local --out copy goes to RUNNER_TEMP so it can feed the reconciler
        # step below without a network round trip; nothing is committed here.
        #
        # BUDGET: one gate call is ~250 ms over the ~1,740-name universe, so the
        # pass pays a full centre census plus a priority-ordered probe phase and an
        # edge-verification phase, ALL THREE bounded by one wall-clock budget
        # (config.yml prophet_live.max_seconds) plus max_probe. Whatever the budget
        # cuts is disclosed in meta.skipped, never re-labelled dormant. The measured
        # build_seconds and per-phase timings print on every run — watch them here.
        #
        # timeout-minutes AND continue-on-error together: the step timeout bounds a
        # wedge, continue-on-error keeps the bound non-fatal. Without the step cap a
        # hung pass eats the ENGINE JOB's 200m cap (worst observed 190m), and that
        # cap does not fail the job — it skips the commit step, silently losing the
        # night's engine outputs. Belt (in-script deadline) and braces (this).
        timeout-minutes: 9
        continue-on-error: true
        env:
          R2_ENDPOINT: ${{ secrets.R2_ENDPOINT }}
          R2_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
          R2_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
          R2_BUCKET: ${{ secrets.R2_BUCKET }}
          # The box is 4-core-bound (asia-close.yml pins the same knob for the same
          # reason). The stock library's default would take os.cpu_count() capped at
          # 8 and oversubscribe against whatever else the engine job is running.
          STOCK_LIB_WORKERS: "4"
        run: |
          set +e
          echo "::group::build_prophet_live_pack (arm the provisional-close triggers)"
          python -m scripts.build_prophet_live_pack --publish \
            --out "$RUNNER_TEMP/prophet_live_armed.json" > "$RUNNER_TEMP/prophet_live_pack.log" 2>&1; rc=$?
          cat "$RUNNER_TEMP/prophet_live_pack.log"
          echo "::endgroup::"
          if [ "$rc" -ne 0 ]; then echo "::warning title=build_prophet_live_pack::rc=$rc (non-fatal — the intraday lane darks on a missing pack)"; fi
          exit 0
      - name: Prophet Live reconciler (intraday event spool -> forward ledger)
        # AFTER the arming pass, which is what supplies tonight's verdict set. VINTAGE
        # IS ENFORCED, not assumed: `confirmed` is written only from a verdict whose
        # basis IS the row's own session — tonight's pack when its as_of matches, else
        # the gate re-run on the close series truncated through that session — and once
        # written it is never revised by a later night.
        #
        # SOLE WRITER of data/prophet_live/forward.parquet (G0.2 / RUL-P10). The
        # */5 lane writes R2 only; this step lists the day's event objects, joins
        # confirmed? + same-day close + the next-session close fill (mirroring
        # engine.grading.fill_index, the grade_us_board convention), and union-merges
        # on (date, ticker, kind) so a rebake or a double run is idempotent and can
        # only ever ADD information. The parquet rides the "commit engine outputs"
        # git add below (data/ is already covered — no extra add needed).
        #
        # MEASURE-DARK: nothing reads this ledger. It exists so the §6 promotion
        # gauntlet has an evidence base to be pre-registered on. Seconds; non-fatal,
        # with a step cap for the same reason as the pack step above (a replay over a
        # long open-row backlog is the one path that could stretch).
        timeout-minutes: 3
        continue-on-error: true
        env:
          R2_ENDPOINT: ${{ secrets.R2_ENDPOINT }}
          R2_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
          R2_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
          R2_BUCKET: ${{ secrets.R2_BUCKET }}
          STOCK_LIB_WORKERS: "4"
        run: |
          set +e
          echo "::group::reconcile_prophet_live (accrue today's provisional crosses)"
          python -m scripts.reconcile_prophet_live --nightly \
            --pack "$RUNNER_TEMP/prophet_live_armed.json" > "$RUNNER_TEMP/prophet_live_reconcile.log" 2>&1; rc=$?
          cat "$RUNNER_TEMP/prophet_live_reconcile.log"
          echo "::endgroup::"
          if [ "$rc" -ne 0 ]; then echo "::warning title=reconcile_prophet_live::rc=$rc (non-fatal — the R2 spool is durable, tomorrow picks the day back up)"; fi
          exit 0
      - name: TURN WATCH desk (early-surfacing deck — display tier)
        # ANTICIPATION §6.9 R8. Rebuilds the capped public
        # site/turn_watch/turn_watch.json deck and writes the private, uncapped B1 intake
        # sidecar at data/us_prophet_rank/episode_inputs/turn_watch/<session>.json from the
        # same in-memory rows. The existing engine broad-data commit owns both artifacts.
        # Neither artifact is a ledger or authority surface, so a failed run costs a day's
        # refresh and the next engine run replaces the prior session's public deck.
        #
        # PATH IS LOAD-BEARING — site/turn_watch/, never site/prophet/. The "commit engine
        # outputs" step below restores site/prophet from HEAD (`git checkout HEAD --
        # site/prophet` + scoped clean/reset, PROPHET_AUTHORITY_PATHS) before staging, so a
        # deck written under that root is reverted on a SUCCESSFUL night too and the site
        # would serve the seed forever. site/turn_watch/ rides that step's broad
        # `git add data/ site/ reports/` and is touched by none of the Prophet restores.
        #
        # DISPLAY TIER, ZERO SCORED AUTHORITY — nothing here ranks, gates or sizes a graded
        # position. Measured 2026-08-08: ~135s over 702 graded names; the cap below is ~4x
        # that so a slow night annotates rather than silently eating the budget. Runs after
        # the Prophet steps because the deck's "what the slow tier says now" column is most
        # useful read beside the board those steps just refreshed.
        timeout-minutes: 12
        continue-on-error: true
        run: |
          set +e
          echo "::group::build_turn_watch (early-surfacing deck)"
          python -m scripts.build_turn_watch > "$RUNNER_TEMP/turn_watch.log" 2>&1; rc=$?
          cat "$RUNNER_TEMP/turn_watch.log"
          echo "::endgroup::"
          if [ "$rc" -ne 0 ]; then echo "::warning title=build_turn_watch::rc=$rc (non-fatal — the deck keeps yesterday's artifact until tomorrow's run)"; fi
          exit 0
      - name: timings band — builders-parallel (W2)
        run: python3 scripts/nightly_timings.py mark --band builders-parallel || echo "::warning::timings band mark failed (non-fatal)"
      - name: regional + desk builders (parallelised — independent clusters, barrier before the hub)
        # The ~22 builders between build_site and the landing hub split into mutually
        # INDEPENDENT clusters (each internally ordered by its own data deps). They used
        # to run strictly serially (~75 min of wall-clock); here each cluster runs in the
        # background and we barrier (`wait`) before build_vector, which reads every
        # cluster's latest.json. That collapses the band to ~its longest single cluster
        # (special-situations' EDGAR sweep, ~28 min) instead of the serial sum. Every
        # builder stays individually resilient via brun(): a non-zero exit is annotated
        # (::error::) and appended to the run summary with its traceback, but NEVER aborts
        # the job — so the site still deploys and one broken builder can't 404 the
        # dashboard. Each builder owns its OWN scratch files ($ART/<slug>.{log,rc,sec}) so
        # the parallel subshells never write the same file; output is replayed GROUPED + in
        # a stable order AFTER the barrier (never interleaved) and per-builder wall-times go
        # to the step summary. Builders read build_site's output (spine, above) + the
        # collect job's data; the only cross-builder reads are kept INSIDE one cluster
        # (crossasset/transmission after commodity/forex/bonds;
        # policy_watch after policy_intent). special_situations stays IN this job (not a
        # sibling job like factor_series) because it writes a PAGE + hub card that must be
        # in THIS run's deploy artifact — it is event-driven, so a one-run lag is wrong.
        env:
          DEEPSEEK_API_KEY: ${{ secrets.DEEPSEEK_API_KEY }}
          POLYGON_API_KEY: ${{ secrets.POLYGON_API_KEY }}
          MASSIVE_API_KEY: ${{ secrets.MASSIVE_API_KEY }}
          FINNHUB_KEY: ${{ secrets.FINNHUB_KEY }}
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          CLAUDE_CODE_OAUTH_TOKEN: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
          CLAUDE_CODE_OAUTH_TOKEN_1: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_1 }}
          CLAUDE_CODE_OAUTH_TOKEN_2: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_2 }}
          CLAUDE_CODE_OAUTH_TOKEN_3: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_3 }}
          CLAUDE_CODE_OAUTH_TOKEN_4: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_4 }}
          CLAUDE_CODE_OAUTH_TOKEN_5: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_5 }}
          CLAUDE_CODE_OAUTH_TOKEN_6: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_6 }}
          CLAUDE_CODE_OAUTH_TOKEN_7: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_7 }}
          METAB_KEYS_ENABLED: ${{ vars.METAB_KEYS_ENABLED }}
          MASSIVE_S3_ENDPOINT: ${{ secrets.MASSIVE_S3_ENDPOINT }}
          MASSIVE_S3_ACCESS_KEY_ID: ${{ secrets.MASSIVE_S3_ACCESS_KEY_ID }}
          MASSIVE_S3_SECRET_ACCESS_KEY: ${{ secrets.MASSIVE_S3_SECRET_ACCESS_KEY }}
          MASSIVE_S3_BUCKET: ${{ secrets.MASSIVE_S3_BUCKET }}
          # SGA (Stage Analysis): earnings-call scores arrive via R2 from the standalone
          # Qwen worker (SGA-R6, producer-only — research/STAGE_ANALYSIS_MASTERPLAN.md).
          # fetch_earnings_scores inside cl_stage no-ops gracefully when these are absent.
          R2_ENDPOINT: ${{ secrets.R2_ENDPOINT }}
          R2_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
          R2_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
          R2_BUCKET: ${{ secrets.R2_BUCKET }}
          COLLECT_LANE: nightly  # lane sentinel: basket_turn_watch.stamp_ledger + other engine
                                 # nightly-only ledger writers check COLLECT_LANE==nightly before
                                 # writing; render/earlyclose/asia-close lanes leave it unset → no-op.
          # FTR W10: build_baskets.py calls notify_turn_events at the end of main().
          # Without these env vars the hook hits dark mode (no webhook → exit 0).
          DISCORD_WEBHOOK_WATCHLIST: ${{ secrets.DISCORD_WEBHOOK_WATCHLIST }}
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
        # Body extracted verbatim to scripts/ci/daily_engine_regional_desk_builders.sh (512KB processing-cap diet 2026-08-12 — tests/test_workflow_file_size.py).
        run: bash scripts/ci/daily_engine_regional_desk_builders.sh
      # Runs AFTER the band that contains `build_baskets --snapshot`, so it grades THIS
      # run's stamp rather than last night's. The plane it watches can die with every
      # signal green: both snapshot writers are content-deduped, so "membership has not
      # changed" and "the writer has been unwired for a month" leave the identical trace
      # on disk (nothing) — which is how the THS PIT store sat at 2 snapshots for six
      # weeks while its nightly step ran green ~35 nights running. Advisory (exit 0):
      # a cold membership plane must not red a nightly that landed every other store.
      - name: membership snapshot freshness tripwire (advisory)
        run: python3 scripts/check_membership_snapshot_freshness.py
      - name: builder fail-streak escalation (consecutive-night freeze detector)
        # brun() (above) BY DESIGN never aborts on a broken builder — but that makes a
        # builder failing MANY CONSECUTIVE nightlies look identical to a one-off at a
        # glance (2026-07-16/17: build_cycle aborted two nights running, cycle.html
        # silently froze at the 07-13 render while the run stayed green — PR #2732 fixed
        # the builder; this step fixes the visibility gap). Reads the band's per-builder
        # scratch rc files ($RUNNER_TEMP/band/<slug>.rc), advances a per-builder streak
        # ledger (data/ci/builder_failstreaks.json — covered by 'git add data/' in
        # 'commit engine outputs' below), and at streak >=2 escalates loudly: a distinct
        # ::error:: per frozen page + step-summary section + one W6b ops alert
        # (engine/alert_triage.push_ops_alert, dispatch-always — same spine as the
        # nw_health streak escalation, PR #2621 precedent). --lane nightly gates the
        # ledger WRITE (nightly = sole advancer of THIS ledger); asia-close.yml runs the
        # ported step as its own write lane on its own ledger (--lane asia ->
        # data/ci/builder_failstreaks_asia.json — it is the sole nightly for the CN/HK
        # pages); any other port (closing-bell) stays report-only — the script binds
        # each write lane to exactly one ledger path. rc==0 resets a streak; a >4-day
        # gap (nightly itself dead) starts a fresh streak. Resilient: always exits 0 —
        # never blocks the deploy, never changes brun()'s never-abort semantics.
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
        run: python -m scripts.check_builder_failstreaks --art-dir "$RUNNER_TEMP/band" --lane nightly || echo "::warning::builder fail-streak check failed (non-fatal)"
      - name: timings band — tail-desks (W2)
        run: python3 scripts/nightly_timings.py mark --band tail-desks || echo "::warning::timings band mark failed (non-fatal)"
      - name: AD-1 — Daily EOD Options Intelligence Brief (build_options_intel_brief)
        # AD-1 (WS:ADVANCED-DATA-OPTIONS, contracts/options/OPTIONS_INTEL_BRIEF_V1.md):
        # deterministic intel_brief_heuristic/v1.2 over the newest lawful settled
        # S→next-session chain pair; writes site/options_intel_brief.json atomically
        # with a semantic no-op on unchanged receipts. Sits AFTER the band wait
        # barrier (chains/summaries/gex/prophet display artifacts settled) and
        # immediately BEFORE build_options_command, its only consumer — the same
        # cross-phase reasoning as M-CMD below. Never wired into closing-bell: the
        # directional OI leg cannot lawfully settle at 16:05 ET (source-clock law).
        # Additive/non-fatal: on failure the last committed artifact stands.
        run: python -m scripts.build_options_intel_brief || echo "::warning::build_options_intel_brief non-fatal failure"
      - name: OEU M-CMD — the Options workspace (build_options_command)
        # OEU M-CMD: options.html, the canonical options surface (4 modes). It is a
        # pure re-serialisation of stores four OTHER builders write, and those deps
        # straddle both phases — build_market_structure and build_flow_leaders are
        # serial-spine steps above, while build_flow_desk and build_options_screener
        # live inside cl_gex in the parallel band. Cross-phase deps ⇒ this MUST be a
        # serial step AFTER the band `wait` barrier (same reasoning as
        # build_sector_central below). O(seconds): JSON reads only, no engine work.
        # Additive — the builder catches its own render failures and exits 0, so the
        # last committed options.html stands and the deploy is never broken.
        run: python -m scripts.build_options_command || echo "::warning::build_options_command non-fatal failure"
      - name: OIP E1 — session digest, the Terminal→EOD bridge (build_session_digest)
        # OIP E1: digests the intraday options plane's R2 archives
        # (live_flow/surface/{ROOT}/{DATE}/ per-minute stamps + the dated tide/dte archives)
        # into one settled per-root session record — data only, ZERO UI.
        # Writes data/options_session/{DATE}/{ROOT}.json + site/session/{ROOT}.json +
        # data/options_session/ledger.parquet (one row per date×root). All three ride the
        # existing `git add data/ site/` in "commit engine outputs" below — no new commit path.
        # MUST be a serial step AFTER the band `wait` barrier: it reads
        # site/options_structure/gex_state/{ROOT}.json for the EOD flip/wall levels, and
        # build_gex_board writes those inside the cl_gex cluster of the parallel band — the
        # same cross-phase reasoning that puts build_options_command immediately above.
        # Ledger + all data/ writes are lane-gated on COLLECT_LANE=nightly (job-level env);
        # dedup on (date, root) keeps FIRST, so a replay adds sessions and never rewrites one,
        # and a dated record is only replaced by a read covering MORE minutes.
        #
        # COST, measured at the CONFIGURED 120s cadence (196 stamps x ~600 strikes x 3 roots):
        #   602 reads, 4.06 MB, 1.13-1.20s wall clock on a local replay (3 runs).
        # Each per-stamp file is the session TRUNCATED to that stamp, so fetching them all is
        # quadratic in bytes — 124.8 MB per root, 374 MB a night. The day is therefore read from
        # the NEWEST frame alone (it already carries the full session), and only the per-stamp
        # `spot` needs a request each, taken as a ~256-byte Range read of the frame head. That
        # is the 92x difference between 4.06 MB and 374 MB. R2 round-trips put the nightly
        # estimate at ~15-40s, inside the ~1-2 min budgeted for this join (OIP §7).
        #
        # TAIL RISK IS BOUNDED BOTH WAYS: the engine job was CANCELLED at its 200-minute cap in
        # 5 of 8 recent nightlies, so this step may not run long. `timeout-minutes: 8` is the
        # hard stop; the builder's own BUDGET_SECONDS=180 wall-clock budget fires first and
        # writes honest partial records (the shortfall printed in each record's coverage block)
        # plus a ::warning, which a killed step could not do.
        # Non-fatal: absent archives/creds degrade to printed nulls and the builder exits 0.
        # continue-on-error pairs with the cap (added 2026-08-06): the note above is right that
        # "a killed step could not" emit the ::warning — but it also could not exit 0, and an
        # UNPAIRED timeout-minutes marks the step FAILED, which fails the whole engine job and
        # SKIPS every step behind it including "commit engine outputs". BUDGET_SECONDS=180 only
        # fires if the builder reaches its own budget check; a hang below it (socket, or an
        # open() parked on the removable-volume access gate — see the collect_tail band note)
        # never does. This step declares itself non-fatal, so it must be non-fatal on the
        # timeout path too.
        timeout-minutes: 8
        continue-on-error: true
        env:
          R2_ENDPOINT: ${{ secrets.R2_ENDPOINT }}
          R2_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
          R2_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
          R2_BUCKET: ${{ secrets.R2_BUCKET }}
        run: python -m scripts.build_session_digest || echo "::warning::build_session_digest non-fatal failure"
      - name: OIP PIT — durable episodes + H+60 and declared-session-close proxy accrual
        id: options_signal_episode
        # Consumes only the date-keyed live_flow/events/{DATE}.jsonl raw stage,
        # never capped/overwritten feed_current. The monotonic checkpoint and three
        # committed JSONL ledgers advance only in COLLECT_LANE=nightly. H+60 v1 stays
        # frozen; EOD/1d/3d/5d/10d close rows accrue separately from the same exact
        # Polygon receipt. Session outcomes and coarse H+60 measurements remain
        # explicitly training-ineligible; all authority is false.
        # Serial after session digest while the restored intraday cache is present.
        # This is the sole nightly advancer and its independent inputs remain valid
        # when an unrelated earlier renderer fails.
        if: always()
        timeout-minutes: 10
        continue-on-error: true
        env:
          R2_ENDPOINT: ${{ secrets.R2_ENDPOINT }}
          R2_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
          R2_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
          R2_BUCKET: ${{ secrets.R2_BUCKET }}
        run: python -m scripts.build_options_signal_episode
      - name: OIP campaign v2 — exact-contract revision and anchored-outcome accrual
        id: options_signal_campaign
        # Consumes only the immutable episode/outcome prefixes produced above. It
        # owns a separate revision/outcome/checkpoint namespace, preserves the legacy
        # v1 cohort bytes, and grants no selector, training, or trading authority.
        if: always() && steps.options_signal_episode.outcome == 'success'
        timeout-minutes: 10
        continue-on-error: true
        run: python -m scripts.build_options_signal_campaign
      - name: OIP PIT — checkpoint the five durable episode ledgers
        id: options_signal_episode_publish
        if: always() && steps.options_signal_episode.outcome == 'success'
        timeout-minutes: 10
        continue-on-error: true
        # This checkpoint is deliberately immediate and success-only. The broad
        # engine commit is hours later and may lose its push race or hit the job
        # cap; these append-only PIT rows cannot be reconstructed after the raw
        # dated event stage ages out. Publish only the four builder-owned files.
        # The metadata replay overlays their object IDs on current origin/main in
        # a temporary index, so it never rebases, checks out, stashes, cleans, or
        # otherwise touches the thousands of unrelated dirty render outputs.
        # The extracted helper keeps the five-file candidate unreachable until the
        # metadata replay is accepted, and keeps this workflow below GitHub's cap.
        run: bash scripts/ci/options_signal_nightly.sh publish-episode
      - name: OIP campaign v2 — checkpoint the exact three canonical ledgers
        id: options_signal_campaign_publish
        if: >-
          always() &&
          steps.options_signal_episode_publish.outcome == 'success' &&
          steps.options_signal_campaign.outcome == 'success'
        timeout-minutes: 10
        continue-on-error: true
        # Campaign publication is interruption-safe and exact-path: the helper
        # never advances HEAD before origin/main accepts all three canonical files.
        run: bash scripts/ci/options_signal_nightly.sh publish-campaign
      - name: XSR W1 — US fast-sector rotation lens (build_us_sector_rotation)
        # XSR W1 (XSR-R2): display-tier fast rotation score for the 11 SPDR sector ETFs and
        # named US thematic baskets. Ported from China #2634. Reads sector_cycles
        # forward_log.parquet (written by build_sector_cycles in the parallel band above) so
        # it MUST be a serial step AFTER the band wait barrier.  Writes
        # data/us_sector_rotation/latest.json, which build_sector_central reads immediately
        # after to re-order the conviction board by fast-rotation rank.
        # Additive (always exits 0 — never breaks the deploy).
        run: python -m scripts.build_us_sector_rotation || echo "::warning::build_us_sector_rotation non-fatal failure"
      - name: US sector central intelligence (build_sector_central)
        # The US Sector Central hub fuses the cycle spine + baskets + narrative rotation + heatmap.
        # It reads baskets.json (cl_baskets) + sector_cycles_data.js (cl_misc → sectorcyc) + the
        # heatmap json (build_site spine) — those deps live in DIFFERENT parallel clusters, so this
        # MUST be a serial step AFTER the band `wait` barrier (the US analog of china_sector_central,
        # which could run inside cl_china only because both its deps lived in that one cluster).
        # Additive (the builder returns 0 on any failure), so it can never break the deploy.
        run: python -m scripts.build_sector_central
      - name: Portfolio-Aware context artifact (build_portfolio_ctx)
        # Portfolio-Aware Intelligence W1 (charter:
        # research/PORTFOLIO_BRIEF_MASTERPLAN_BY_FABLE.md §2). Bakes the full-universe
        # per-ticker context blob site/data/portfolio_ctx.json that the mastermind-terminal
        # Portfolio page joins against per-user off the render path. RE-EXPRESSES existing
        # nightly reads (no new signal); reads committed source artifacts ONLY (never advances
        # a ledger). MUST be a serial step AFTER the band `wait` barrier and after ALL its
        # sources: build_site (screener/us_standouts/insider/subsector/state_of_themes +
        # theme_lanes.json), build_alt_data (by_ticker earnings clock), build_smart_money (13F),
        # build_congress (congress.parquet) — all in the serial spine above — plus
        # build_sector_central (immediately above, sector conviction/rotation). ~2s wall
        # time on the full ~4.2k universe; output rides the existing `git add site/` commit.
        # Additive — never breaks the deploy.
        run: python -m scripts.build_portfolio_ctx || echo "::warning::build_portfolio_ctx failed (non-fatal — last committed portfolio_ctx.json stands)"
      - name: MLC W2b stance matrix (build_stance_matrix)
        # MLC-W2b coherence layer: per-basket stance matrix collecting sector/theme/confluence/
        # allocation/m7c verdicts mapped to a signed tier scale (-2..+2). Reads
        # baskets.json (build_baskets), sector_central.json (build_sector_central above),
        # basket_confluence.json, allocation.json, and mag7_regime latest.json.
        # Output site/mlcdata/stance_matrix.json rides the existing `git add data/ site/` commit.
        # Additive (always exits 0 — never breaks the deploy).
        run: python -m scripts.build_stance_matrix || echo "::warning::build_stance_matrix non-fatal failure"
      - name: FTR W9 grading pack (basket_turn.v1 cohort claims + tape_disagreement.v1)
        # basket_turn.v1: one qledger claim per IGNITION-day cohort (equal-weight basket EW vs
        # SPY, 21d horizon, expected-NULL forward meter per FT-R9). Reads
        # data/basket_turn/ledger.jsonl (written by basket_turn_watch in cl_baskets → build_baskets).
        # tape_disagreement.v1: disagreement-day records (basket IGNITION while slow reco not in
        # {enter,accumulate}), outcome tracking with explicit right-censoring at +10/+21 sessions.
        # Both families gated on COLLECT_LANE=nightly. Keep-first idempotent. Exit-0-always.
        # Runs AFTER the parallel band (cl_baskets writes the source ledger).
        continue-on-error: true
        env:
          COLLECT_LANE: nightly
        run: |
          set +e
          python -m scripts.build_basket_turn_grading > "$RUNNER_TEMP/basket_turn_grading.log" 2>&1; rc=$?
          cat "$RUNNER_TEMP/basket_turn_grading.log"
          if [ "$rc" -ne 0 ]; then echo "::warning title=build_basket_turn_grading::rc=$rc (non-fatal)"; fi
          exit 0
      - name: Polygon reference-universe cache (GICS sector + market cap for heatmap manifest)
        # Builds data/polygon_universe/reference.parquet — ticker → GICS sector + USD market cap.
        # Consumed by the charting-app's ingest/build_universe.py when it rebuilds the
        # heatmap manifest (~8.7k rows); enriches US names with real sector + cap sizing.
        # Refreshes NIGHTLY, judged from the parquet's asof column (never mtime — CI
        # checkouts reset mtime, which froze the cache at its 2026-07-09 first fill).
        # ~2 min of rate-limited fetches for ~500 names; same-day re-runs exit
        # immediately. Checkpoint is same-day resume state only (gitignored).
        # Non-fatal: a failed run leaves the previous cache in place (charting-app degrades
        # gracefully to OTHER/no-mcap for names not in cache). Output committed via
        # 'git add data/' in 'commit engine outputs' below.
        continue-on-error: true
        env:
          POLYGON_API_KEY: ${{ secrets.POLYGON_API_KEY }}
          MASSIVE_API_KEY: ${{ secrets.MASSIVE_API_KEY }}
        run: |
          python -m scripts.build_polygon_universe || echo "::warning::build_polygon_universe failed (non-fatal — previous cache remains)"
      - name: flow_idx R2 mirror (heatmap flow layer freshness)
        # Mirror site/flow/index.json → R2 key live_flow/flow_idx.json so the live
        # options-flow heatmap layer can read a fresh manifest without a full site deploy.
        # Runs AFTER the parallel band (cl_gex → build_options_flow produced the file).
        # Also mirrors the Terminal EOD-context artifacts (OEU M-XP b): site/darkpool_eod.json
        # → darkpool/eod.json and site/vol/regime.json → vol/regime.json, for the Terminal's
        # Dark Pool mini-panel + Structure strip (lane T-E).
        # Graceful: skips silently when R2 creds are absent. Never fails the build.
        continue-on-error: true
        env:
          R2_ENDPOINT: ${{ secrets.R2_ENDPOINT }}
          R2_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
          R2_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
          R2_BUCKET: ${{ secrets.R2_BUCKET }}
        run: |
          python -m scripts.mirror_flow_idx || echo "::warning::flow_idx R2 mirror failed (non-fatal)"
          python -m scripts.mirror_flow_idx --leaders || echo "::warning::flow leaders R2 mirror failed (non-fatal)"
          python -m scripts.mirror_flow_idx --radar || echo "::warning::leader radar R2 mirror failed (non-fatal)"
          python -m scripts.mirror_flow_idx --options-prophet --strict || echo "::warning::options prophet R2 mirror failed (non-fatal)"
          python -m scripts.mirror_terminal_context_r2 || echo "::warning::terminal-context R2 mirror failed (non-fatal)"
      - name: flow-signal ledger harvest + outcome grader (FS-0)
        # FS-0 (FLOW_SIGNAL_ML_MASTERPLAN_BY_FABLE §5): append-only per-event
        # flow ledger (collectors/flow_signals.py) + outcome grader
        # (engine/flow_signals_grade.py). Runs AFTER the flow_idx mirror steps
        # so that R2 archive blobs from the live_flow poller are available.
        # US engine job, R2-creds-gated, no-ops without creds. (There is no
        # us_scope gate in this job — step is unconditional but gracefully
        # degrades when R2_ENDPOINT/R2_ACCESS_KEY_ID/R2_SECRET_ACCESS_KEY are
        # absent.) Nightly is the sole advancer of this ledger (forward-ledger
        # law): intraday lanes must not write to data/flow_signals/. Output
        # covered by 'git add data/' in the 'commit engine outputs' step below.
        # Graceful: absent R2 creds → clean no-op (harvest returns 0). Never
        # fails the nightly.
        continue-on-error: true
        env:
          R2_ENDPOINT: ${{ secrets.R2_ENDPOINT }}
          R2_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
          R2_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
          R2_BUCKET: ${{ secrets.R2_BUCKET }}
        run: |
          python -m scripts.build_flow_signals || echo "::warning::build_flow_signals failed (non-fatal — keeping last committed ledger)"
      - name: build canada dashboard (S&P/TSX)
        # FIRST-CLASS STEP (2026-07-27) — build_canada used to run ONLY as a swallowed
        # try/except inside build_vector (a workaround from PR #81, whose PAT lacked
        # `workflow` scope). Two defects came with that: the failure went through
        # log.error, whose "%(levelname)s " prefix pushes "::" off the line start so
        # GitHub DROPPED the annotation and a dead builder shipped a stale canada.html
        # behind a green nightly; and the builder had no workflow of its own to be
        # scoped or timed by. Both found shipping macro#3820 (HK/Canada rebuild).
        # ORDERING IS LOAD-BEARING: this MUST stay above the build_vector step below —
        # the landing hub's _canada_state() reads the data/canada_regime/latest.json
        # written here (and the hub card is presence-gated on site/canada.html).
        # Resilient like every other builder in this job: build_canada never raises, a
        # non-zero rc is annotated + summarised here, and the step always exits 0 so one
        # broken dashboard can never abort the nightly.
        # SKIP_CANADA_HOOK=1 goes to $GITHUB_ENV so build_vector's surviving fallback
        # hook no-ops for the rest of this job — set UNCONDITIONALLY (even on failure):
        # build_canada is a pure function of the committed store, so a retry would burn
        # ~10 min of a 150m-capped nightly to fail identically. The annotation is the
        # recovery signal, not a second attempt.
        run: |
          set +e
          python -m scripts.build_canada > "$RUNNER_TEMP/canada.log" 2>&1; rc=$?
          cat "$RUNNER_TEMP/canada.log"
          echo "SKIP_CANADA_HOOK=1" >> "$GITHUB_ENV"
          if [ "$rc" -ne 0 ]; then
            echo "::error title=canada dashboard (build_canada) failed (rc=$rc)::$(tail -n 2 "$RUNNER_TEMP/canada.log" | tr '\n' ' ' | tail -c 600)"
            { echo "### ❌ canada dashboard (build_canada) failed (rc=$rc)"; echo '<details><summary>traceback</summary>'; echo; echo '```'; tail -n 60 "$RUNNER_TEMP/canada.log"; echo '```'; echo '</details>'; } >> "$GITHUB_STEP_SUMMARY"
          fi
          exit 0
      - name: build bitcoin vector + landing hub
        # installs the landing hub at index.html (rendered from stored engine
        # state, so ordering vs build_site no longer matters — build_site writes
        # the macro dashboard straight to macro.html). Self-sufficient (recomputes
        # signals); returns 0 even if the crypto engine errors, so it can never
        # break the macro site.
        run: python -m scripts.build_vector
      - name: build crypto intelligence cockpit
        # FIRST-CLASS W2 STEP — load-bearing order: build_vector emits the E0
        # BTC authority contract consumed by H6, then this class-level hub reads
        # it alongside the additive universe and market-state engines.
        run: python -m scripts.build_crypto
      - name: master brain scorer (durability loop; deterministic, resilient)
        # Grades the brain's own past-due cross-asset leans in data/master_brain/theses.jsonl
        # against realized closes (reuses engine.desk_scorer) -> data/master_brain/
        # {scored.jsonl,track_record.json}. Runs BEFORE the master_brain generator so today's
        # brief reflects the freshest track record. Deterministic, no API key, never aborts.
        run: |
          set +e
          python -m engine.master_brain_scorer > "$RUNNER_TEMP/mb_score.log" 2>&1; rc=$?
          cat "$RUNNER_TEMP/mb_score.log"
          if [ "$rc" -ne 0 ]; then echo "::warning title=master_brain_scorer::rc=$rc (non-fatal)"; fi
          exit 0
      - name: master brain (multi-lens LLM synthesis; default-off, resilient)
        # Runs LAST among the builders — it reads EVERY dashboard's latest.json
        # (macro/china/hk/commodity/forex + BTC) plus the catalyst digest, and writes
        # one brief per configured lens (master_brain.lenses, default macro+china+btc):
        #   site/master_brief.json (US dashboard) · site/china_brief.json (china.html)
        #   · site/btc_brief.json (vector.html) — each client-fetched by its AI brief
        # panel. NO-OP unless config master_brain.enabled is true AND at least one
        # rung of the ladder below can serve. Self-sufficient; never aborts the deploy.
        env:
          # PROVIDER LADDER (config.yml master_brain.provider_order):
          # codex → oauth pool → ANTHROPIC_API_KEY → DeepSeek. This step used to pass
          # DEEPSEEK_API_KEY and nothing else, so DeepSeek was not merely first — it was
          # the ONLY rung that existed at runtime, and the day its balance ran out the
          # brief stopped rendering entirely. Codex needs no secret (it reads the
          # runner's attached ~/.codex login), but the Claude rungs are invisible
          # without these, so a ladder that omits them silently collapses back to
          # DeepSeek-only. Mirrors the Narrative Brain step below.
          CLAUDE_CODE_OAUTH_TOKEN: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
          CLAUDE_CODE_OAUTH_TOKEN_1: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_1 }}
          CLAUDE_CODE_OAUTH_TOKEN_2: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_2 }}
          CLAUDE_CODE_OAUTH_TOKEN_3: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_3 }}
          CLAUDE_CODE_OAUTH_TOKEN_4: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_4 }}
          CLAUDE_CODE_OAUTH_TOKEN_5: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_5 }}
          CLAUDE_CODE_OAUTH_TOKEN_6: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_6 }}
          CLAUDE_CODE_OAUTH_TOKEN_7: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_7 }}
          METAB_KEYS_ENABLED: ${{ vars.METAB_KEYS_ENABLED }}
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          # Metered floor, deliberately LAST — the only pay-per-use rung.
          DEEPSEEK_API_KEY: ${{ secrets.DEEPSEEK_API_KEY }}
        run: |
          set +e
          python -m engine.master_brain > "$RUNNER_TEMP/mb.log" 2>&1; rc=$?
          cat "$RUNNER_TEMP/mb.log"
          if [ "$rc" -ne 0 ]; then echo "::warning title=master_brain::rc=$rc (non-fatal)"; fi
          exit 0
      - name: AI desk scorer (durability loop; deterministic, resilient)
        # Grades past-due theses in data/ai_desk/theses.jsonl against realized closes,
        # appends data/ai_desk/scored.jsonl, and writes data/ai_desk/track_record.json
        # which the desk step below reads back to calibrate conviction. Deterministic
        # (no API key); runs BEFORE the desk so today's note sees the freshest record.
        run: |
          set +e
          python -m engine.ai_desk_scorer > "$RUNNER_TEMP/aidesk_score.log" 2>&1; rc=$?
          cat "$RUNNER_TEMP/aidesk_score.log"
          if [ "$rc" -ne 0 ]; then echo "::warning title=ai_desk_scorer::rc=$rc (non-fatal)"; fi
          exit 0
      - name: Demand-chain ledger (Demand Desk Phase 2; deterministic, resilient)
        # Appends falsifiable demand-chain divergence theses (data/demand_chain/theses.jsonl)
        # and scores past-due ones vs SPY into data/demand_chain/{scored.jsonl,track_record.json},
        # which the Demand Context panel reads back as its accountability line. Rule-derived
        # (no API key) — turns the "customer capex ahead of consensus" read into a scored
        # forward record. Reuses the ai_desk_scorer predicate evaluators. Never aborts.
        run: |
          set +e
          python -m engine.demand_ledger > "$RUNNER_TEMP/demand_ledger.log" 2>&1; rc=$?
          cat "$RUNNER_TEMP/demand_ledger.log"
          if [ "$rc" -ne 0 ]; then echo "::warning title=demand_ledger::rc=$rc (non-fatal)"; fi
          exit 0
      - name: Demand-chain Phase-0 validation (forward placebo test; resilient)
        # Reads the demand_chain ledger and, once enough theses are DECIDED, tests the
        # hit-rate vs a coin flip (exact binomial) → data/demand_chain/phase0.json.
        # Honest by design: the signal is NOT historically back-testable (annual cadence,
        # no PIT consensus history), so the forward ledger is the only path; verdict stays
        # PENDING until ≥20 decided. Deterministic; never aborts.
        run: |
          set +e
          python -m scripts.demand_chain_phase0 > "$RUNNER_TEMP/demand_phase0.log" 2>&1; rc=$?
          cat "$RUNNER_TEMP/demand_phase0.log"
          if [ "$rc" -ne 0 ]; then echo "::warning title=demand_chain_phase0::rc=$rc (non-fatal)"; fi
          exit 0
      - name: Demand Desk page (build_demand)
        # market-wide view of the L2 demand layer (site/demand.html): the ranked
        # ahead-of-consensus / at-risk names across all chains + chain signals + the
        # scored-ledger track record + Phase-0 verdict. Runs AFTER the ledger/phase0
        # steps (fresh track_record/phase0) and build_site (rpo dripped). Resilient.
        run: python -m scripts.build_demand
      - name: AI desk (accountable analyst layer; context-only, resilient)
        # Runs right after master_brain (shares the keyed LLM step family). Reads the
        # TRUST-GRADED detectors — site/basketdata/flow.json (from build_baskets) +
        # data/regime/regime_snap.json (when Engine-2 is present) — and writes a
        # FALSIFIABLE desk note (data/regime/ai_desk.json + site/ai_desk.json) while
        # appending data/ai_desk/theses.jsonl for the Phase-C scorer to grade. NO-OP
        # unless config ai_desk.enabled is true AND at least one rung of the ladder
        # below can serve.
        # Context-only (never scored/sized); self-sufficient; never aborts the deploy.
        env:
          # Same ladder as the master brain step above (config.yml ai_desk.provider_order):
          # codex → oauth pool → ANTHROPIC_API_KEY → DeepSeek. ai_desk calls the brain
          # through master_brain._call_model, so it shares the waterfall and needs the
          # same secrets; without them the desk note silently reverts to DeepSeek-only.
          CLAUDE_CODE_OAUTH_TOKEN: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
          CLAUDE_CODE_OAUTH_TOKEN_1: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_1 }}
          CLAUDE_CODE_OAUTH_TOKEN_2: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_2 }}
          CLAUDE_CODE_OAUTH_TOKEN_3: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_3 }}
          CLAUDE_CODE_OAUTH_TOKEN_4: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_4 }}
          CLAUDE_CODE_OAUTH_TOKEN_5: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_5 }}
          CLAUDE_CODE_OAUTH_TOKEN_6: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_6 }}
          CLAUDE_CODE_OAUTH_TOKEN_7: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_7 }}
          METAB_KEYS_ENABLED: ${{ vars.METAB_KEYS_ENABLED }}
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          # Metered floor, deliberately LAST — the only pay-per-use rung.
          DEEPSEEK_API_KEY: ${{ secrets.DEEPSEEK_API_KEY }}
        run: |
          set +e
          python -m engine.ai_desk > "$RUNNER_TEMP/aidesk.log" 2>&1; rc=$?
          cat "$RUNNER_TEMP/aidesk.log"
          if [ "$rc" -ne 0 ]; then echo "::warning title=ai_desk::rc=$rc (non-fatal)"; fi
          exit 0
      - name: Narrative Brain (Claude radar-reasoning desk; context-only, resilient)
        # Reads the deterministic radar (site/basketdata/radar.json from build_baskets) as an
        # evidence pack and writes a FALSIFIABLE durability + rotation read (site/basketdata/
        # narrative_brain.json) + appends data/narrative_brain/theses.jsonl. Claude via the
        # user's Claude-Code OAuth token (Bearer + oauth beta header), ANTHROPIC_API_KEY fallback.
        # NO-OP unless narrative_brain.enabled AND a token is set. Context-only; never aborts.
        env:
          CLAUDE_CODE_OAUTH_TOKEN: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
          CLAUDE_CODE_OAUTH_TOKEN_1: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_1 }}
          CLAUDE_CODE_OAUTH_TOKEN_2: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_2 }}
          CLAUDE_CODE_OAUTH_TOKEN_3: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_3 }}
          CLAUDE_CODE_OAUTH_TOKEN_4: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_4 }}
          CLAUDE_CODE_OAUTH_TOKEN_5: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_5 }}
          CLAUDE_CODE_OAUTH_TOKEN_6: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_6 }}
          CLAUDE_CODE_OAUTH_TOKEN_7: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_7 }}
          METAB_KEYS_ENABLED: ${{ vars.METAB_KEYS_ENABLED }}
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          # Metered last resort: the OAuth pool can be fully rate-limited at
          # nightly time (all six keys 429'd 2026-08-03) and ANTHROPIC_API_KEY
          # is not a configured repo secret — without DeepSeek this lane has no
          # working rung and the ledger never accrues.
          DEEPSEEK_API_KEY: ${{ secrets.DEEPSEEK_API_KEY }}
        run: |
          set +e
          python -m engine.narrative_brain > "$RUNNER_TEMP/nbrain.log" 2>&1; rc=$?
          cat "$RUNNER_TEMP/nbrain.log"
          if [ "$rc" -ne 0 ]; then echo "::warning title=narrative_brain::rc=$rc (non-fatal)"; fi
          exit 0
      - name: Risk Brain (Claude/Opus daily drawdown-risk officer; context-only, resilient)
        # Reads the engine's own deterministic risk stack (data/regime/latest.json.risk_state,
        # site/riskdata/mtf_monitor.json, GEX, baskets, narrative rotation, standouts) and writes
        # a FALSIFIABLE risk read + a CODE-clamped de-risk DIRECTIVE (site/riskdata/risk_brain.json)
        # + appends data/risk_brain/theses.jsonl. Runs AFTER build_site/build_baskets so its evidence
        # exists. Claude via the OAuth token / ANTHROPIC_API_KEY fallback. NO-OP unless
        # risk_brain.enabled AND a token is set. Context-only; never aborts.
        env:
          CLAUDE_CODE_OAUTH_TOKEN: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
          CLAUDE_CODE_OAUTH_TOKEN_1: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_1 }}
          CLAUDE_CODE_OAUTH_TOKEN_2: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_2 }}
          CLAUDE_CODE_OAUTH_TOKEN_3: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_3 }}
          CLAUDE_CODE_OAUTH_TOKEN_4: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_4 }}
          CLAUDE_CODE_OAUTH_TOKEN_5: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_5 }}
          CLAUDE_CODE_OAUTH_TOKEN_6: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_6 }}
          CLAUDE_CODE_OAUTH_TOKEN_7: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_7 }}
          METAB_KEYS_ENABLED: ${{ vars.METAB_KEYS_ENABLED }}
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          # Metered last resort — same rationale as the Narrative Brain step.
          DEEPSEEK_API_KEY: ${{ secrets.DEEPSEEK_API_KEY }}
        run: |
          set +e
          python -m engine.risk_brain > "$RUNNER_TEMP/rbrain.log" 2>&1; rc=$?
          cat "$RUNNER_TEMP/rbrain.log"
          if [ "$rc" -ne 0 ]; then echo "::warning title=risk_brain::rc=$rc (non-fatal)"; fi
          exit 0
      - name: Risk Radar self-correction (Opus reviews graded mistakes -> bounded recalibration)
        # Reads the radar's realized forward-outcome scorecard (data/risk_radar/forward_log.jsonl),
        # lets Opus propose calibration nudges to cut false positives + catch misses, CLAMPS them and
        # applies ONLY if a do-no-harm backtest confirms F1 improves (full + 2020) without breaking the
        # evidence gate. Writes data/risk_radar/calibration.json + review_log.jsonl. NO-OP unless
        # risk_radar_review.enabled AND a token is set AND >=30 calls graded. Never aborts.
        env:
          CLAUDE_CODE_OAUTH_TOKEN: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
          CLAUDE_CODE_OAUTH_TOKEN_1: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_1 }}
          CLAUDE_CODE_OAUTH_TOKEN_2: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_2 }}
          CLAUDE_CODE_OAUTH_TOKEN_3: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_3 }}
          CLAUDE_CODE_OAUTH_TOKEN_4: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_4 }}
          CLAUDE_CODE_OAUTH_TOKEN_5: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_5 }}
          CLAUDE_CODE_OAUTH_TOKEN_6: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_6 }}
          CLAUDE_CODE_OAUTH_TOKEN_7: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_7 }}
          METAB_KEYS_ENABLED: ${{ vars.METAB_KEYS_ENABLED }}
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: |
          set +e
          python -m engine.risk_radar_review > "$RUNNER_TEMP/rrev.log" 2>&1; rc=$?
          cat "$RUNNER_TEMP/rrev.log"
          if [ "$rc" -ne 0 ]; then echo "::warning title=risk_radar_review::rc=$rc (non-fatal)"; fi
          exit 0
      - name: Radar + Brain ledger scorer (grade the falsifiable theses vs SPY; context-only)
        # Grades data/{radar,narrative_brain}/theses.jsonl once each check_by passes (reuses
        # engine.ai_desk_scorer) -> track_record.json + a site copy. Idempotent; context-only
        # (never feeds a score); needs no key. Runs after the brain so fresh theses are logged.
        run: |
          set +e
          python -m engine.radar_scorer > "$RUNNER_TEMP/radar_score.log" 2>&1; rc=$?
          cat "$RUNNER_TEMP/radar_score.log"
          if [ "$rc" -ne 0 ]; then echo "::warning title=radar_scorer::rc=$rc (non-fatal)"; fi
          exit 0
      - name: US Buy Board ledger (snapshot today + retro-grade matured boards vs SPY/sector)
        # Appends today's committed us_standouts.json to data/us_board_ledger/snapshots.jsonl,
        # then reconstructs every past board from git history + snapshots and grades every
        # matured row (5/10/21d, next-bar entry) vs SPY and the name's sector ETF -> writes
        # data/us_board_ledger/retro_grades.parquet + site/factordata/us_board_track.json.
        # Context-only (never feeds a score); the ledger accrues forward from here. The commit
        # step below (git add data/ site/) lands both artifacts. Idempotent per as_of. Resilient.
        #
        # WAS `|| true`, which cannot fail AND cannot report — ran-clean-no-op, crashed, and
        # worked-fine all concluded `success` identically. That is how the 2026-07-31..08-06
        # zero-accrual outage stayed invisible for nine days (the step was in fact innocent:
        # the breadth close caches were frozen, so nothing could mature). NON-FATAL IS KEPT
        # DELIBERATELY — this step sits deep in the engine job with NON-always() steps after
        # it whose ledgers the nightly is also the sole forward advancer of, so a raise here
        # would silently freeze several unrelated forward ledgers for the night. Same blast
        # radius as before (the deploy still cannot go red), strictly more information — the
        # rc is named in the Actions summary, and the module now annotates a zero-accrual
        # night and a history-truncated checkout on its own. Same idiom as risk_radar_review.
        run: |
          set +e
          python -m scripts.grade_us_board --nightly; rc=$?
          if [ "$rc" -ne 0 ]; then echo "::warning title=grade_us_board::rc=$rc (non-fatal)"; fi
          exit 0
      - name: ETF consensus board ledger (snapshot tonight's board + grade matured windows vs SPY)
        # ETF page masterplan §3 W3. Recomputes the etfs.html consensus board from the
        # committed holdings snapshots (engine.holdings_signals -> engine.etf_consensus,
        # no network calls), appends its top-20 to data/etf_board_ledger/snapshots.jsonl
        # keep-FIRST per (as_of, ticker), then grades every matured board row at 5/21/63
        # sessions (next-bar fill, adjusted-first ladder, vs SPY) into retro_grades.parquet
        # and projects site/factordata/etf_board_track.json for the Calibration Lab's
        # forward-windows panel. The commit step below (git add data/ site/) lands all
        # three. Idempotent per board date: a re-run appends zero rows and leaves the
        # fire log byte-identical.
        #
        # Both data/ writes are gated on COLLECT_LANE=nightly (job-level env above) —
        # nightly is the sole advancer of forward ledgers, and this step is the only
        # writer of that ledger anywhere in the repo.
        #
        # NON-FATAL by the same reasoning as grade_us_board directly above: this step
        # sits deep in the engine job with non-always() steps after it whose ledgers the
        # nightly is also the sole advancer of, so a raise here would silently freeze
        # several unrelated forward ledgers for the night. First-run tolerance matters
        # too — the ledger does not exist until this runs once. `set +e` + a named rc
        # keeps the failure visible in the Actions summary instead of `|| true`'s
        # ran-clean/crashed/no-op indistinguishability; the module prints its own
        # zero-accrual annotation on a night that logs nothing.
        timeout-minutes: 15
        run: |
          set +e
          python -m scripts.grade_etf_board --nightly; rc=$?
          if [ "$rc" -ne 0 ]; then echo "::warning title=grade_etf_board::rc=$rc (non-fatal)"; fi
          exit 0
      - name: Opportunity timeline (durable Prophet candidate receipts for Terminal)
        # Pure projection over the four committed track_ledger/v1 artifacts.  It runs
        # after grade_us_board so tonight's US episode marks are present; CN/HK are
        # refreshed by asia-close.yml before this nightly checkout.  The builder
        # flattens every historical definition but never promotes a board candidate
        # into a Prophet plan or Oracle signal.  Its atomic/fail-closed write keeps the
        # last complete timeline if any required source is missing or malformed, and
        # the broad site/ commit below publishes the resulting artifact.
        run: python -m scripts.build_opportunity_timeline || echo "::warning::opportunity timeline kept its last complete artifact (non-fatal)"
      # NOTE (2026-08-06): the Prophet US program's four post-board modules used to sit
      # HERE — emit_prophet_doors, grade_prophet_doors, grade_us_prophet_candidates and
      # run_prophet_miss_audit. They now run in the `us_prophet_ledgers` job (below,
      # needs: [engine, us_scan_tier]) over the tree THIS job commits. Nothing in that
      # program gates the dashboard deploy, and on a night this job hits its 200m cap
      # they used to complete and then be DISCARDED with the skipped commit (run
      # 31056495943: the doors + miss-audit succeeded at 04:48Z, "commit engine outputs"
      # never ran, the whole night's Prophet accrual was lost). See that job's comment.
      - name: Bottom Ledger (accrue bottom-class flags + mature at H=60; policy-free instrument)
        # Bottom Ledger P1: the policy-free bottom-calling learning instrument (design PR #3182).
        # Sole forward advancer of this ledger (nightly-only). Accrues board buy-lane +
        # washout WAIT + Prophet-plan flags from snapshots.jsonl (runs AFTER grade_us_board so
        # today's snapshot is present), matures rows at H=60 trading days, and emits
        # site/factordata/us_bottom_ledger.json. Display-tier; no exits/stops/authority. Both
        # the rows store (data/bottom_ledger/rows.parquet) and the site/ display artifact are
        # landed by the "commit engine outputs" step below (git add data/ site/) — like every
        # sibling forward ledger, the committed store is what lets cohorts accrue night to
        # night. Idempotent per as_of. Resilient.
        run: python -m scripts.grade_bottom_calls --nightly || true
      - name: Options-state stamp on board ledger (Options Alpha W1.3; context-only)
        # Stamps the freshly-graded + accumulated retro_grades.parquet with the eight nullable
        # opt_* options-state columns (PIT: only positioning data with as-of ≤ each fire's as_of).
        # Sources pinned per ruling A6 (polygon_gex summary + chains). NEVER touches grading
        # columns/logic (Setup-Species Stage B owns those; A9) — nullable schema-union only,
        # mirroring the regime-stamp pattern. Write path data/us_board_ledger/ is already covered
        # by the "commit engine outputs" step's `git add data/` below. Idempotent. Resilient.
        run: python -m scripts.stamp_options_state || true
      - name: Options entry-quality gate (Options Alpha W1.3; pre-registered, scored=false)
        # Reduces the horizon ledger to canonical buy-lane fires, then recomputes the registered
        # options-entry diagnostics in data/options_entry/gate.json (ledger primitives only, A10).
        # Descriptive maturity requires ≥30 fires, ≥30 dates in each bucket, and ≥30 shared
        # dates. Existing observations cannot earn authority after the methodology amendment:
        # date-cluster inference plus a frozen sequential-look plan and fresh cohort are still
        # required. scored=false / weight=0 remain hard-frozen. Cheap and resilient.
        run: python -m scripts.validate_options_entry || true
      - name: Calibration Hub (unified self-improvement observability; display-only)
        # Consolidates every Phase-C desk's track_record + the Trial Ledger into one
        # surface (data/calibration/summary.json + site/calibration.html). Runs after the
        # deterministic scorers so it reads fresh track records; never feeds a score.
        run: python -m scripts.build_calibration || true
      - name: build macro snapshot (nw-macro-rail R5)
        # Writes data/macro_snapshots/latest.json + ledger.parquet + transitions.jsonl from
        # the day's regime, transmission, forex, bonds, commodity, china, hk, canada, and
        # dispersion sources.  Fail-open per source; missing sources produce gap notes rather
        # than failures.  Must run BEFORE build_spine_index so the stamp-join picks up today's
        # macro_context_id.  Same-asof replace semantics — safe to re-run.
        run: |
          set +e
          python -m scripts.build_macro_snapshot > "$RUNNER_TEMP/macro_snapshot.log" 2>&1; rc=$?
          cat "$RUNNER_TEMP/macro_snapshot.log"
          if [ "$rc" -ne 0 ]; then echo "::warning title=build_macro_snapshot::rc=$rc (non-fatal)"; fi
          exit 0
      - name: build macro context page (nw-macro-rail R5)
        # Renders site/macro_context.html (Macro Weather Station) from world_state +
        # macro_snapshots/latest.json + transitions.jsonl.  Fail-open: all three sources
        # are read gracefully; missing files produce empty-state sections.  Also writes
        # hub contract data/macro_context/latest.json.  Non-fatal.
        run: |
          set +e
          python -m scripts.build_macro_context > "$RUNNER_TEMP/macro_context.log" 2>&1; rc=$?
          cat "$RUNNER_TEMP/macro_context.log"
          if [ "$rc" -ne 0 ]; then echo "::warning title=build_macro_context::rc=$rc (non-fatal)"; fi
          exit 0
      - name: build spine index (neural-web W2)
        # Read-side union index over all graded ledgers: spine + track_record + HK/CA/CN
        # boards + qledger claims⋈grades (read-only) + graded cycle rows. Writes
        # data/neuralweb/spine_index.parquet + envelope sidecar. AFTER grade_us_board
        # (all graders incl. qledger grading in collect have run); BEFORE radar_plus block.
        # Exit 0 on partial gaps; 1 only on write failure. Resilient wrapper.
        # if: always() — PR-2 fix: backfill_regime_stamps had a shell bug that caused it to
        # exit 1 silently, skipping all downstream kernel steps; add guard so these run
        # even if an upstream optional step fails.
        if: always()
        run: |
          set +e
          python -m scripts.build_spine_index > "$RUNNER_TEMP/spine_index.log" 2>&1; rc=$?
          cat "$RUNNER_TEMP/spine_index.log"
          # ::error:: (not ::warning::) so a spine build failure is LOUD — it stays
          # non-fatal (exit 0, last-committed-brains philosophy) but must never go
          # silent again: a frozen spine cascades to the whole NW layer (kernel /
          # confluence / cortex / half-life) rebuilding from stale data. (2026-07-07
          # freeze went unnoticed 11 days behind a ::warning::.)
          if [ "$rc" -ne 0 ]; then echo "::error title=build_spine_index::rc=$rc — SPINE FROZEN (non-fatal); downstream neural-web rebuilds from stale spine_index until fixed"; fi
          exit 0
      - name: build reliability kernel estimates (neural-web W3)
        # Reliability kernel estimates: (engine × regime × horizon) cells over the spine
        # index. Writes data/neuralweb/kernel_estimates.parquet + envelope sidecar.
        # Display-first: zero behavior-changing consumers; the quarterly FDR batch (PR2)
        # is the only path to behavior. Input: spine_index.parquet (only dependency).
        # Resilient wrapper: exits 0 on empty/thin data; 1 only on write failure.
        if: always()
        run: |
          set +e
          python -m scripts.build_kernel_estimates > "$RUNNER_TEMP/kernel_estimates.log" 2>&1; rc=$?
          cat "$RUNNER_TEMP/kernel_estimates.log"
          if [ "$rc" -ne 0 ]; then echo "::warning title=build_kernel_estimates::rc=$rc (non-fatal)"; fi
          exit 0
      - name: build covariance spine (R-ORTH PR-3)
        # R-ORTH PR-3: covariance spine MVP (RUL-ORTH-1..11). Measures structural
        # concentration across rates PCA, factor correlations, dispersion regime,
        # and NW engine firing independence. Display-only; four independently-guarded
        # blocks; always exits 0. Writes data/neuralweb/covariance_spine.json +
        # site/neuralwebdata/covariance_spine.json + history parquet.
        if: always()
        run: |
          set +e
          python -m scripts.build_covariance_spine > "$RUNNER_TEMP/covariance_spine.log" 2>&1; rc=$?
          cat "$RUNNER_TEMP/covariance_spine.log"
          if [ "$rc" -ne 0 ]; then echo "::warning title=build_covariance_spine::rc=$rc (non-fatal)"; fi
          exit 0
      - name: build kernel diagnostics (neural-web W3)
        # W3 PR3: decay curves (kernel_families.json) + lagging-signal detector
        # (lagging_signals.json). Both are display-only diagnostics; zero behavior-changing
        # consumers. Runs AFTER build_kernel_estimates (kernel_families reads its parquet).
        # Resilient wrapper: each sub-step fail-open; exits 0 unless both steps fail.
        if: always()
        run: |
          set +e
          python -m scripts.build_kernel_diagnostics > "$RUNNER_TEMP/kernel_diagnostics.log" 2>&1; rc=$?
          cat "$RUNNER_TEMP/kernel_diagnostics.log"
          if [ "$rc" -ne 0 ]; then echo "::warning title=build_kernel_diagnostics::rc=$rc (non-fatal)"; fi
          exit 0
      - name: build kernel half-lives (Signal Commons W2 — display-only)
        # W2: per-family holding-horizon half-life estimates.  Staleness half-life
        # declared UNMEASURED (fill_offset uniformly=1 in all replay parquets).
        # Reads kernel_estimates.parquet + spine_index.parquet; writes
        # data/neuralweb/half_life.json.  OFF RENDER PATH (build_site.py reads
        # the resulting JSON for committee chip display only).
        # SENTINEL LAW: always writes half_life.json (status=fit_ran or fit_failed)
        # so a crashed job is distinguishable from a successful all-null result.
        # Expected honest outcome: all families null (track_record IC rises;
        # radar/us_board have <3 graded horizons).  Resilient wrapper (exit 0).
        if: always()
        run: |
          set +e
          python -m scripts.build_kernel_half_lives > "$RUNNER_TEMP/kernel_half_lives.log" 2>&1; rc=$?
          cat "$RUNNER_TEMP/kernel_half_lives.log"
          if [ "$rc" -ne 0 ]; then echo "::warning title=build_kernel_half_lives::rc=$rc (non-fatal)"; fi
          exit 0
      - name: Confluence Graph (Neural Web W4 — display-only typed graph)
        # Builds data/neuralweb/confluence_graph.json — typed nodes (engine/sector/regime/
        # thesis/episode) and five edge families (feeds/stable/leads/contradicts/confirms).
        # Contradiction detector covers 6 typed pairs; all edges display_only=True.
        # HARD LAW: confluence never gates, never ranks, never raises a priority.
        # Gaps (missing oracle files — gitignored) logged as warnings, never failures.
        run: |
          set +e
          python -m scripts.build_confluence_graph > "$RUNNER_TEMP/confluence_graph.log" 2>&1; rc=$?
          cat "$RUNNER_TEMP/confluence_graph.log"
          if [ "$rc" -ne 0 ]; then echo "::warning title=build_confluence_graph::rc=$rc (non-fatal)"; fi
          exit 0
      - name: Confluence Strength + Sequence + Discovery (Neural Web W4 "the eye" — R-V4-6/7/8)
        # Runs three deterministic display-only steps after build_confluence_graph:
        # 1. confluence_strength  → data/neuralweb/confluence_strength.json (R-V4-6)
        #    Per-(subject,as_of,direction,horizon) confirming engine count with redundancy
        #    discount from covariance_spine dominant_overlap_cluster (partial credit).
        #    regime_bucket stamped from world_state for context; assert_no_authority checked.
        # 2. confluence_sequence  → data/neuralweb/confluence_tape.jsonl (PIT tape)
        #                         + data/neuralweb/confluence_sequence.json (R-V4-7)
        #    Nightly-sole-advancer tape append; idempotent per as_of.
        #    Per-subject: persistence_streak, strength_slope (Theil-Sen), state machine.
        # 3. confluence_discovery → data/neuralweb/confluence_candidates.jsonl (R-V4-8)
        #    Robust-stats z-scan (median/MAD, 100-draw circular-shift null).
        #    Honest-null: young tape rows carry state=accruing, z=null (never fabricated).
        # HARD LAW: all outputs display_only=True; no LLM; never gates/ranks/raises.
        # rotation-state x cycle-position pairing is DON'T-TEST (DO_NOT_REBUILD).
        # Budget target: < 30s on synthetic data. Fail-open: exceptions log warning + skip.
        run: |
          set +e
          python -m scripts.build_confluence_strength > "$RUNNER_TEMP/confluence_strength.log" 2>&1; rc=$?
          cat "$RUNNER_TEMP/confluence_strength.log"
          if [ "$rc" -ne 0 ]; then echo "::warning title=build_confluence_strength::rc=$rc (non-fatal)"; fi
          exit 0
      - name: Alt-Data Brain (Claude-Opus convergence analyst; actionable + falsifiable)
        # Reads the deterministic weighted-convergence substrate + influence graph (written by
        # build_alt_data above) as an evidence pack and writes an ACTIONABLE scored read
        # (action/conviction/direction + a falsifiable thesis graded vs SPY) -> site/altdata/
        # brain.json + data/altdata/brain_theses.jsonl, then re-runs build_alt_data to re-emit
        # mastermind.json + re-render alt_data.html with the fresh read. Claude via the user's
        # Claude-Code OAuth token (Bearer + oauth beta), ANTHROPIC_API_KEY fallback — the SAME
        # auth as the Narrative Brain step (the build_alt_data step above only carries the
        # DeepSeek key, so the Opus/Haiku calls must run HERE). NO-OP unless altdata_brain.enabled
        # AND a token is set. A hard CODE clamp blocks ACCUMULATE on an extended name; never aborts.
        env:
          CLAUDE_CODE_OAUTH_TOKEN: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
          CLAUDE_CODE_OAUTH_TOKEN_1: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_1 }}
          CLAUDE_CODE_OAUTH_TOKEN_2: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_2 }}
          CLAUDE_CODE_OAUTH_TOKEN_3: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_3 }}
          CLAUDE_CODE_OAUTH_TOKEN_4: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_4 }}
          CLAUDE_CODE_OAUTH_TOKEN_5: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_5 }}
          CLAUDE_CODE_OAUTH_TOKEN_6: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_6 }}
          CLAUDE_CODE_OAUTH_TOKEN_7: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_7 }}
          METAB_KEYS_ENABLED: ${{ vars.METAB_KEYS_ENABLED }}
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          # Metered last resort — same rationale as the Narrative Brain step.
          # Without it this gated re-run OVERWRITES a DeepSeek-served brain.json
          # from the build_alt_data step with a degraded one (2026-08-03).
          DEEPSEEK_API_KEY: ${{ secrets.DEEPSEEK_API_KEY }}
        run: |
          set +e
          python -m engine.altdata_brain > "$RUNNER_TEMP/altbrain.log" 2>&1; rc=$?
          cat "$RUNNER_TEMP/altbrain.log"
          # re-emit the Mastermind feed + re-render the page with the fresh Opus read
          python -m scripts.build_alt_data > "$RUNNER_TEMP/altbrain_render.log" 2>&1
          tail -3 "$RUNNER_TEMP/altbrain_render.log"
          if [ "$rc" -ne 0 ]; then echo "::warning title=altdata_brain::rc=$rc (non-fatal)"; fi
          exit 0
      - name: radar upgrade (build_radar_plus + IC)
        # ADDITIVE Phase-2 radar: enrich site/basketdata/radar.json with edge_score +
        # cross-source confirmation legs (alt/flows/options) + regime + decay (no edits to
        # the radar kernel), emit site/basketdata/radar_ticker.json (per-name divergence),
        # accrue the edge-snapshot IC ledger, then re-render radar.html with the mounts.
        # Runs after build_baskets (radar.json) + build_alt_data + the Alt-Data Brain, and
        # BEFORE build_intelligence (which absorbs radar_ticker.json). Resilient.
        run: |
          set +e
          python -m scripts.build_radar_plus > "$RUNNER_TEMP/radarplus.log" 2>&1; tail -4 "$RUNNER_TEMP/radarplus.log"
          python -m scripts.build_radar_ic   > "$RUNNER_TEMP/radaric.log"   2>&1; tail -3 "$RUNNER_TEMP/radaric.log"
          python -m scripts.build_radar_page  > "$RUNNER_TEMP/radarpage.log" 2>&1; tail -1 "$RUNNER_TEMP/radarpage.log"
          exit 0
      - name: unified intelligence bundle (build_intelligence)
        # AFTER build_news (news/by_ticker.json) + build_alt_data + the Alt-Data Brain re-emit
        # (altdata/mastermind.json) + build_radar_plus (radar_ticker.json): merges all per-ticker
        # facets — news flow + alt-data signal + radar divergence + factor buy-board — into ONE
        # site/intelligence/by_ticker.json (v2) the Mastermind bot pulls. Facts side by side,
        # never pre-blended. No keys needed; pure JSON merge; never aborts.
        run: python -m scripts.build_intelligence || echo "intelligence skipped (additive)"
      - name: brain briefing tunnel (build_briefing)
        # AFTER build_intelligence: reduces the per-ticker bundle to a RANKED, brain-facing
        # triage — site/intelligence/briefing.json — that the Mastermind brain (Claude Opus)
        # pulls once per cycle: priority queue (confidence × strength), the demand-vs-supply
        # divergence subset, and a regime/catalyst macro frame. Pure; resilient; never aborts.
        run: python -m scripts.build_briefing || echo "briefing skipped (additive)"
      - name: Intelligence Hub (build_intel_hub)
        # AFTER build_intelligence + build_briefing + build_policy_watch + build_radar_plus:
        # fuses ALL FIVE desks (news · alt-data · radar · buy-board · policy) into one central
        # command — site/intel_hub/hub.json + site/intelligence_hub.html — with cross-source
        # confirmation, composite conviction (docked by an unanswered falsifier), 2nd/3rd-order
        # flags (stealth_accumulation / early_edge / crowded_top / …), and the sector heat.
        # The US analogue of the China intel hub; pure, resilient, never aborts.
        run: python -m scripts.build_intel_hub || echo "intel hub skipped (additive)"
      - name: AI Daily Brief page (build_aibrief)
        # consolidated page (site/aibrief.html) surfacing the three client-fetched AI
        # briefs (master/china/btc) as toggle tabs. Pure render — order-independent;
        # resilient (returns 0). Replaces the retired deterministic daily_brief.
        run: python -m scripts.build_aibrief
      - name: AM Edition producer (build_am_edition)
        # Deterministic since-prior-close artifact (site/am_edition.json) with
        # per-source clocks. Reads committed artifacts only; never breaks the run.
        run: python -m scripts.build_am_edition
      - name: Mastermind snapshot page (build_mastermind)
        # static page (site/mastermind.html) that renders a daily snapshot of the LOCAL
        # Mastermind AI paper-trading desk. Pure chrome — the data (site/mastermind/
        # mastermind_snapshot.json) is pushed out-of-band twice a day by the local
        # Mastermind server (the cloud build can't reach localhost); mastermind.js fetches
        # it client-side and degrades to "snapshot pending" when absent. Order-independent;
        # resilient (returns 0).
        run: python -m scripts.build_mastermind
      - name: AI Desk page (build_ai_desk_page)
        # static page (site/ai_desk.html) surfacing the accountable analyst desk
        # (site/ai_desk.json: falsifiable leans + dissent + falsifier + track record +
        # the analyst panel). Pure render — order-independent; resilient (returns 0).
        run: python -m scripts.build_ai_desk_page
      - name: rebuild stock-search libraries (resilient — guarantees the analyzers ship)
        # build_china/build_hk/build_canada (and build_intl via a build_vector hook)
        # render their dashboards but SWALLOW errors (return 0), so a failure there
        # silently 404s the per-stock search. This rebuilds the search JSON
        # (site/chinastockdata, site/hkstockdata, site/canadastockdata,
        # site/intlstockdata) directly from the committed caches — independent of the
        # dashboard render — and surfaces any failure as an annotation without
        # aborting the deploy. Idempotent.
        run: |
          set +e
          libdir() {
            case "$1" in
              scripts.build_china_library) echo site/chinastockdata;;
              scripts.build_hk_library) echo site/hkstockdata;;
              scripts.build_canada_library) echo site/canadastockdata;;
              scripts.build_intl_library) echo site/intlstockdata;;
            esac
          }
          for mod in scripts.build_canada_library scripts.build_intl_library; do
            idx="$(libdir "$mod")/index.json"
            # Skip a market whose dashboard step already built its library FRESH this run
            # (index.json newer than the job-start marker). This rebuild is a FAILURE-PATH
            # safety net — on the success path the same heavy analyze() over the whole
            # universe already ran, so rebuilding is pure duplicate work. A dashboard that
            # swallowed an error leaves an older (pulled) index.json => marker is newer =>
            # we still rebuild and the library ships.
            if [ -f "$RUNNER_TEMP/engine_libs_marker" ] && [ "$idx" -nt "$RUNNER_TEMP/engine_libs_marker" ]; then
              echo "::notice::$mod — already built fresh this run ($idx); skipping redundant rebuild"
              continue
            fi
            echo "::group::$mod"; python -m "$mod" > "$RUNNER_TEMP/lib.log" 2>&1; rc=$?; cat "$RUNNER_TEMP/lib.log"; echo "::endgroup::"
            if [ "$rc" -ne 0 ]; then
              echo "::error title=$mod failed (rc=$rc)::$(tail -n 2 "$RUNNER_TEMP/lib.log" | tr '\n' ' ' | tail -c 600)"
              { echo "### ❌ $mod failed (rc=$rc)"; echo '```'; tail -n 40 "$RUNNER_TEMP/lib.log"; echo '```'; } >> "$GITHUB_STEP_SUMMARY"
            fi
          done
          exit 0
      - name: watchlist buy-zone sentinel (B6 — fail-soft; skips when creds absent)
        # B6: reads operator Supabase watchlist (two-table schema: watchlists + watchlist_symbols),
        # diffs vs yesterday's per-ticker state, emits buy-zone-enter alerts to
        # data/alerts/watchlist_alerts.jsonl + Discord.
        # Fail-soft: missing SUPABASE_SERVICE_KEY or SUPABASE_OPERATOR_USER_ID -> exit 0.
        # SUPABASE_OPERATOR_USER_ID is REQUIRED (multiple accounts exist; absent -> skip silently).
        # Same-day idempotent: second run carries forward existing state files.
        # OPS: add SUPABASE_SERVICE_KEY + SUPABASE_OPERATOR_USER_ID repo secrets to enable.
        # Optional: add DISCORD_WEBHOOK_WATCHLIST for a dedicated channel;
        #   falls back to DISCORD_WEBHOOK_URL when absent.
        env:
          SUPABASE_SERVICE_KEY: ${{ secrets.SUPABASE_SERVICE_KEY }}
          SUPABASE_OPERATOR_USER_ID: ${{ secrets.SUPABASE_OPERATOR_USER_ID }}
          DISCORD_WEBHOOK_WATCHLIST: ${{ secrets.DISCORD_WEBHOOK_WATCHLIST }}
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
        run: python -m scripts.run_watchlist_sentinel || echo "::warning::watchlist sentinel failed (non-fatal)"
      - name: notify (telegram/discord; skips when secrets absent)
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
        run: python -m scripts.notify
      - name: push priority alerts (W6b — ops-tier push spine ENABLED; operator Article-2 event 2026-07-04)
        # Neural Web W6b: push high-priority alerts above `alert_push.threshold` (default 60)
        # through Telegram/Discord with a dedup window (`alert_push.window_hours`).
        # Gate: `alert_push.enabled: true` in config.yml (W6b operator decision — see comment
        # block there). The three migrating senders (basket_freeze, signal_sanity, healthcheck)
        # already push raw Telegram today with NO dedup; routing them through push_ops_alert()
        # reduces operator noise strictly (dedup window + per-lane ledgers suppress repeat fires).
        # The operator can disable at any time by setting alert_push.enabled: false.
        # Dedup stores: data/alert_triage/push_sent.jsonl (priority lane) +
        #   data/alert_triage/push_sent_basket_freeze.jsonl (single-writer: basket_freeze lane)
        #   data/alert_triage/push_sent_signal_sanity.jsonl (single-writer: signal_sanity lane)
        #   data/alert_triage/push_sent_healthcheck.jsonl   (single-writer: heartbeat lane)
        # Resilient: always exits 0 — a dispatch error never blocks the daily build.
        # Signal-sentinel migrations (vector/commodity) are phase 3 — not in this step.
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
        run: python -m scripts.push_alerts || echo "::warning::push_alerts failed (non-fatal)"
      - name: archive signal layer (append-only point-in-time model outputs; resilient)
        # LAST data-producing step: append today's latest.json / bond_health.json /
        # regime_snap.json / forex + commodity reads to data/signal_archive/<label>.parquet
        # (keep-first per as-of date). A clean, queryable, point-in-time record of what
        # each model SAID each day — distinct from regime_history.parquet, which is
        # RECOMPUTED from scratch every run (so it carries data revisions + today's code
        # backwards = lookahead). Lossless via a snapshot_json column. Display/research-
        # only, never read back into a score; the script always exits 0.
        run: python -m scripts.archive_signals || true
      - name: archive context snapshots (context PIT history — non-fatal)
        # Appends flattened context-regime state (quad, liquidity, cycle_tag,
        # transition_state, regime_confidence, market_state verdict/score,
        # vol_regime verdict, fear-greed composite) to
        # data/signal_archive/context_daily.parquet (keep-FIRST per date).
        # Fixes P-D5-3 'no PIT context history' class going forward.
        # Every source is absent-safe; always exits 0.
        run: python -m scripts.archive_context_snapshots || true
      - name: CPI net-liquidity artifact (data infrastructure — non-fatal)
        # Deterministic full rebuild of data/macro/fed_net_liquidity.parquet (+ the
        # fed_net_liquidity_meta.json sidecar) from the three component parquets the
        # collect job refreshed tonight (data/fred/WALCL + RRPONTSYD, data/treasury/tga)
        # via engine.canon.load_net_liquidity_components — the ONE netliq formula
        # (audit #28): WALCL_bn − RRP_bn − TGA_bn, billions. Stale-guard truncates the
        # frame at min(component last real obs)+10bd so a dead component collector can
        # never silently ffill a drain flat for weeks. Context-tier ONLY — registers
        # no trial (FT-3 suspended by cycle_truth_ft2_credit_hazard_null_v1; see the
        # fed-net-liquidity notes in config/synapse.yml). Output covered by
        # 'git add data/' in 'commit engine outputs' below.
        run: python -m scripts.build_netliq_daily || echo "::warning::build_netliq_daily failed (non-fatal — last committed artifact stands)"
      - name: Neural Web liquidity-plumbing lobe (shadow-tier, non-fatal)
        # Pure aggregation, no network calls. Runs AFTER build_netliq_daily so
        # data/macro/fed_net_liquidity.parquet is fresh. Reads regime/latest.json,
        # treasury/{net_issuance,tga}.parquet, treasury_auctions/auctions.parquet.
        # Emits data/neuralweb/liquidity_plumbing.json (schema neuralweb.liquidity_plumbing.v1).
        # Shadow-tier, display/de-escalation only authority — no score raise.
        # Fail-open: missing source → gaps[] + null block, always exits 0.
        # Output covered by 'git add data/' in 'commit engine outputs' below.
        run: python -m scripts.build_liquidity_plumbing || echo "::warning::build_liquidity_plumbing failed (non-fatal — last committed artifact stands)"
      - name: Price Pressure lobe (DRL W1, display-tier, non-fatal)
        # Single-name residual shocks: the Dislocation & Recovery lobe's nightly
        # increment. Pure aggregation over committed stores, NO network calls.
        # Rebuilds the wide panel in memory (~45-60s measured, no cache is
        # committed anywhere), harvests the self-healing catch-up window, appends
        # idempotently on (ticker,date,side), re-grades every open row, and emits
        # data/price_pressure/{events.parquet,latest.json} (schema price_pressure.v1).
        # The LEDGER write is gated on COLLECT_LANE=nightly (house law: nightly is
        # the sole advancer of forward ledgers); the display artifact rebuilds in
        # any lane. Display tier, every authority boolean literal false — the
        # underlying construction is closed as alpha
        # (DNR:KILL-LIQUIDITY-SHOCK-REVERSAL-CLASSIFIER) and this lobe shows that
        # measured record as context. A missing or stale bar store prints a
        # workflow annotation and exits 0 WITHOUT touching artifacts, so a short
        # run can never overwrite a good ledger. The frozen base rates
        # (data/price_pressure/base_rates.json) are produced by a manual local
        # backfill only and are never refrozen here.
        # Outputs covered by 'git add data/' in 'commit engine outputs' below —
        # EXCEPT events.parquet, which is gitignored and R2-canonical (see below).
        #
        # THREE R2 legs live inside this one step, all non-fatal, in order:
        #
        # 1. BAR-STORE COLD-RESTORE. data/massive_stock_day is restored, advanced and
        #    published by the COLLECT job — in the COLLECT job's own workspace. Workspace
        #    state cannot cross a job boundary, so when this job lands on a runner whose
        #    checkout has never held that store (a cold spare), the store is simply
        #    absent and the builder's staleness gate correctly refuses: run 31440972065
        #    (2026-08-10) landed on actions-runner-2 and warned
        #    "price-pressure-store-stale ... artifacts left untouched" — a whole night
        #    of the lobe lost to a scheduling accident. The existence probe below is a
        #    `find -print -quit`, not a `ls *.parquet` glob: this store is ~20k files and
        #    the glob would expand ~800 KB of argv. Warm runner = one stat-ish walk that
        #    stops at the first hit, ~0 cost, no R2 call at all. Cold runner = a full
        #    ~617 MB / ~20k-object download (fetch_r2's delta is per FILE — an absent
        #    file downloads whole; only an already-current file is skipped by md5/ETag).
        # 2. LEDGER RESTORE. data/price_pressure/events.parquet is R2-canonical and
        #    gitignored (2026-08-11): the advance + the §10.1 completion pass rewrite
        #    all ~10.8 MB every successful night, which is ~4 GB/yr of binary git churn.
        #    Restore it BEFORE the builder so the nightly extends the 35k-row history
        #    instead of re-forking it. The three JSON sidecars stay git-tracked, and
        #    engine.price_pressure.ledger.restore_status refuses to advance when the
        #    restored parquet is absent or older than tracked latest.json's asof — so a
        #    failed restore costs a night, never the ledger.
        # 3. PUBLISH-BACK, GATED. Mirrors the massive_stock_day pair in the collect job:
        #    the upload runs ONLY when the restore leg exited 0 AND the builder exited 0.
        #    "restore failed" must mean "do not publish this tree over the deep store"
        #    (fetch_r2 exits 1 on a zero-object list or any failed download), and a red
        #    builder means the local tree is whatever the last step left behind. publish_r2's
        #    _DATA_DIR_MIN_FILES_OVERRIDE (3) + _DATA_DIR_MIN_BYTES (4 MB) floors are the
        #    second fence — a sidecars-only checkout is ~73 KB and gets refused there.
        #    Not anchored in the audit_r2 --strict list on purpose: that anchor keys on
        #    the manifest's Last-Modified, which would go red on every legitimate
        #    refuse-to-run night (the exact posture leg 1 exists to survive), and the
        #    builder's content-date freshness gate is the sharper tripwire anyway.
        timeout-minutes: 20
        env:
          R2_ENDPOINT: ${{ secrets.R2_ENDPOINT }}
          R2_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
          R2_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
          R2_BUCKET: ${{ secrets.R2_BUCKET }}
        run: |
          if [ -z "$(find data/massive_stock_day -maxdepth 1 -name '*.parquet' -print -quit 2>/dev/null)" ]; then
            echo "price_pressure: bar store has no parquet bars locally (cold runner) — restoring from R2"
            python -m scripts.fetch_r2 --dirs massive_stock_day --workers 24 || echo "::warning::massive_stock_day restore failed (non-fatal — the builder's staleness gate refuses and leaves artifacts untouched)"
          else
            echo "price_pressure: bar store present locally — no R2 restore needed"
          fi
          PP_RESTORE=ok
          python -m scripts.fetch_r2 --dirs price_pressure || { PP_RESTORE=failed; echo "::warning::price_pressure ledger restore failed (non-fatal — the builder's freshness gate refuses and leaves artifacts untouched)"; }
          PP_BUILD=ok
          python -m scripts.build_price_pressure || { PP_BUILD=failed; echo "::warning::build_price_pressure failed (non-fatal — last committed artifact stands)"; }
          if [ "$PP_RESTORE" = ok ] && [ "$PP_BUILD" = ok ]; then
            python -m scripts.publish_r2 --dirs price_pressure || echo "::warning::price_pressure R2 publish failed (non-fatal — R2 ledger goes stale; tomorrow's restore+freshness gate refuses rather than re-forking)"
          else
            echo "::warning::price_pressure R2 publish SKIPPED (restore=$PP_RESTORE build=$PP_BUILD) — a failed restore or build must never overwrite the canonical ledger"
          fi
      - name: Entry Radar nightly reconcile (W5 forward evidence)
        # The SOLE durable writer of data/entry_radar/** (W5 prereg §8,
        # research/live_entry_radar/W5_FORWARD_EVIDENCE_PREREG.md). The W4
        # intraday lane spools events and writes nothing durable; this step is
        # the single advancer, and it asserts that itself: --nightly calls
        # engine.ledger_lane.nightly_advance_enabled() BEFORE any read and exits
        # 2 having written NOTHING when the gate is down. The gate PASSES here
        # (COLLECT_LANE=nightly is set at job level), so exit 2 in this lane would
        # mean the job env changed underneath the law — which is exactly when the
        # step should go red.
        #
        # WHY continue-on-error IS NOT SET. The script itself exits 0 on missing
        # inputs, which is the normal state today: W4 does not exist, so there is
        # no event spool, and the correct output is the ledger state
        # WAITING_FOR_LIVE_SOURCE with a session stamp. It never manufactures
        # observations from nightly artifacts and never backfills. The only
        # non-zero exit is the lane refusal above, and that must not be swallowed:
        # a lane error that produced a partial artifact is indistinguishable from
        # a healthy pass to every staleness instrument we own.
        #
        # Outputs (data/entry_radar/{ledger_state.json,forward.parquet,
        # qledger_registration_log.jsonl}) are covered by the broad `git add data/`
        # in 'commit engine outputs' below — verified: that step runs `git add
        # data/ site/ reports/`, and none of the three paths is gitignored.
        # qledger claims (§17, live-forward rows only) land in data/qledger/,
        # already committed by the same step.
        timeout-minutes: 10
        run: python3 scripts/reconcile_entry_radar.py --nightly --verbose
      - name: Release Radar coherent-target vintage stores (truth substrate, non-fatal)
        # Full ALFRED output_type=2 matrices let the target engine select current
        # and prior levels from ONE release-date vintage.  They are additive to
        # the broad output_type=4 PIT feature store and never replace it.
        env:
          FRED_API_KEY: ${{ secrets.FRED_API_KEY }}
        timeout-minutes: 20
        run: python -m scripts.collect_release_target_vintages || echo "::warning::release target vintage collection failed (non-fatal — completion authority remains withheld until a full cohort succeeds)"
      - name: Release Radar official CPI Table 1 receipts (truth substrate, non-fatal)
        # Collects the frozen preregistered BLS Supplemental Table 1 panel into
        # immutable content-addressed source documents and source-bound receipts.
        # The batch is resumable and validates retained bytes before reuse; a
        # missing or drifting official document leaves the prior corpus standing.
        timeout-minutes: 20
        run: python -m scripts.collect_release_cpi_official_truth || echo "::warning::official CPI Table 1 collection failed (non-fatal — completion authority remains withheld if publication began)"
      - name: Release Radar coherent CPI target parity (truth substrate, non-fatal)
        # Reconstructs headline/core CPI targets from one ALFRED release vintage
        # and publishes a completion receipt only after the preregistered BLS
        # parity panel passes exactly.  This is a shadow truth gate: it does not
        # alter champion coefficients, combined weights, or promotion authority.
        timeout-minutes: 10
        run: python -m scripts.build_release_cpi_truth_parity || echo "::warning::coherent CPI truth parity failed (non-fatal — no new completion boundary published)"
      - name: Release Radar official-actual reconciliation (truth substrate, non-fatal)
        # Converts verified BLS/BEA/DOL publication facts from the VPS live
        # sidecar into keep-first, source-hashed actual receipts.  No forecast is
        # originated here; the following producer prefers these receipts when grading.
        run: python -m scripts.reconcile_release_actuals || echo "::warning::official release actual reconciliation failed (non-fatal — same-vintage fallback remains)"
      - name: MRI release forecast producer (display-only, non-fatal)
        # MRI PR-C: nightly projection for CPI (headline + core) and NFP.
        # Emits data/release_forecast/latest.json (schema release_forecast.v2),
        # site/macrodata/release_forecast.json (display copy),
        # data/release_forecast/forward_ledger.jsonl (append-only),
        # data/release_forecast/scoreboard.json (forward accrual only).
        # Runs AFTER collect (needs fresh ALFRED vintages for release-day capture).
        # display_only=True; all authority booleans False (MRI-R7). Fail-open.
        # Outputs covered by 'git add data/' and 'git add site/' below.
        run: python -m scripts.build_release_forecast || echo "::warning::build_release_forecast failed (non-fatal — last committed artifact stands)"
      - name: Inflation intelligence lobe (context-only, non-fatal)
        if: always()
        # Separates the latest released CPI state, the next BLS-print forecast,
        # and current-month proxy pressure.  It is a deterministic adapter over
        # persisted inputs: display_only=true, authority=false, and no Prophet,
        # ranking, gating, sizing, or trading consumer.
        run: python -m scripts.build_inflation_intelligence || echo "::warning::build_inflation_intelligence failed (non-fatal — last committed artifact stands)"
      - name: forward path board (RIC W7 P5, display-only, non-fatal)
        if: always()   # deterministic join over committed artifacts — build whatever exists
        # RIC W7 P5: deterministic Forward Path board (rates_command.v1). Deliberately
        # placed AFTER build_release_forecast AND after the market clusters
        # (bonds/transmission/commodities) so every input it joins — fed_path, curve
        # regime, breakevens, CPI/NFP projections, oil, market_state — comes from the
        # SAME nightly (a self-consistent join beats a same-night render; macro.html
        # picks the committed artifact up on the next build_site pass, the standard
        # transmission-context T+1 pattern). display_only=True; authority False.
        # Fail-open; forward_log.jsonl append is COLLECT_LANE=nightly gated, keep-FIRST.
        # Outputs covered by 'git add data/' below.
        run: python -m scripts.build_rates_command || echo "::warning::build_rates_command failed (non-fatal — last committed artifact stands)"
      - name: signal sanity (correctness tripwire — warn mode)
        if: always()   # validate whatever DID build, even if an earlier optional builder failed
        # The CORRECTNESS dead-man's switch over the per-ticker boards BOTH books select from
        # (coverage floor / score-column degeneracy / content-freeze / staleness / drift — see
        # engine/signal_sanity.py). Liveness (healthcheck) catches "pipeline stopped"; this catches
        # "pipeline ran and published WRONG numbers". WARN mode: it annotates, writes
        # reports/signal-sanity.md, updates the rolling baseline, and pings Telegram/Discord on a
        # trip, but never blocks the build (|| true). Opt-in deploy-gating (skip the commit when it
        # trips) is a deliberate follow-up — keep the daily commit unconditional for now.
        run: python -m scripts.signal_sanity || true
      - name: White House Watch — re-render page + inject site-wide alert banner
        # PAGE-ONLY here: re-render site/wh_banner.json + site/whitehouse.html from the
        # already-committed ledger (no feed poll, no Opus call, no ledger/state writes) — the
        # hourly `whitehouse-sentinel` workflow is the SOLE brain-caller / ledger-writer, so
        # the two workflows can never race to append-then-clobber an alert. Then inject the
        # alert-ticker <script> into EVERY generated page (depth-aware, idempotent) so the
        # banner shows site-wide. MUST be the last page-mutating step so no later builder
        # strips the tag. Resilient: rc 10 = "changed" (the desk's success signal, not an
        # error); each script leaves prior output in place on any real failure.
        run: |
          rc=0; python -m scripts.build_whitehouse --page-only || rc=$?; [ "$rc" -eq 0 ] || [ "$rc" -eq 10 ] || echo "whitehouse desk skipped (additive, rc=$rc)"
          python -m scripts.inject_wh_banner || echo "banner inject skipped (additive)"
          # data-base fetch shim (site-wide, <head>): reroutes heavy per-ticker fetches to R2
          # when window.DATA_BASE is set; no-op while empty. Last page-mutating pass so no
          # later builder strips the tag. See scripts/inject_data_base.py.
          python -m scripts.inject_data_base || echo "data-base shim inject skipped (additive)"
          python -m scripts.externalize_css || echo "css externalize skipped (additive)"
          # cache-bust (?v=<hash>) + defer local js/css so the shared bundle caches
          # immutable and stops re-revalidating on every nav. See scripts/optimize_assets.py.
          # ::warning::, not a bare echo — this step is the only thing that moves a ?v=
          # stamp, and the edge serves versioned assets `immutable, max-age=1y`, so a failure
          # swallowed into a green run pins returning browsers to the old bytes for a year
          # (#3617/#3624). See render.yml for the full rationale.
          python -m scripts.optimize_assets || echo "::warning::asset optimize FAILED — ?v= stamps may be stale"
      - name: fetch oracle panels from R2 (PR-C1 sponsorship shim)
        if: always()
        # PR-C1 (RUL-7): oracle panel_s.parquet + panel_m.parquet are built off-render
        # on the Mac (scripts/oracle_nightly.py + publish_oracle_panels.py) and published
        # to R2 under the oracle/ prefix.  Downloaded here for TWO consumers below —
        # this fetch MUST precede oracle_nightly (order pinned by
        # tests/test_oracle_nightly.py::test_daily_fetch_oracle_panels_precedes_oracle_nightly):
        #   1. oracle_nightly (next step): personality (3b) and the turn-desk lane
        #      (Steps 15-19) read data/oracle/panel_s.parquet and skip on their
        #      missing-file sentinels when it is absent.  When this fetch ran AFTER
        #      oracle_nightly, turn_desk skipped and Steps 16-19 lost the night while
        #      the panel landed ~3s later (run 30960328285, 2026-08-05).  Note
        #      episodes_s.parquet is NOT fetched (not published to R2) — turn_desk
        #      still needs it from the runner workspace (oracle_offrender builds it).
        #   2. build_bottom_sensors (further below): populates sponsorship_state for
        #      the ~1,489+ sector-mapped tickers.
        # Safe at this position: the fetch has no local inputs (R2 creds only), and
        # neither oracle_nightly nor build_world_state writes any of the three
        # fetched files (panel_s/panel_m/manifest).
        # Absent-object-safe: if R2 objects are missing the consumers degrade to
        # 'unavailable'/sentinel-skip — this step NEVER crashes the build (always exits 0).
        # No-op when R2 creds are absent (non-prod builds, forks).
        env:
          R2_ENDPOINT: ${{ secrets.R2_ENDPOINT }}
          R2_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
          R2_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
          R2_BUCKET: ${{ secrets.R2_BUCKET }}
        run: python -m scripts.fetch_oracle_panels || echo "::warning::fetch_oracle_panels failed (non-fatal — sponsorship_state will be unavailable)"
      - name: oracle nightly (state + alerts + directive + ledger + banner)
        if: always()
        # Oracle O4 live-wiring: reads committed panel/episode/graph artifacts (heavy
        # re-builds run off-render via scripts/build_oracle_panel.py etc.), emits:
        #   data/oracle/oracle_alerts.jsonl      alert event log (git add data/ covers)
        #   data/oracle/alerts_state.json        diff-state for next run
        #   data/oracle/rotation_directive.json  Mastermind context
        #   data/oracle/memory_base_rates.json   base rates from gauntlet results
        #   data/oracle/forward_ledger.jsonl     PIT keep-FIRST episode ledger
        #   site/basketdata/oracle_state.json    the bus payload (git add site/ covers)
        # Sentinel-gap check: all outputs land under data/ or site/ which are in the
        # "git add data/ site/ reports/" below — NO new explicit git add needed.
        # panel_s/panel_m availability: fetched from R2 by the step directly above.
        # --skip-panel --skip-graph --skip-episodes: heavy rebuilds run separately off-render.
        # --skip-timemachine: the TM feed export needs the panel+episode parquets, which are
        #   built off-render (build_oracle_panel forbids render-path use); the dedicated
        #   `oracle_offrender` job below rebuilds AND commits site/oracledata/ nightly.
        #   Without this flag the export FAILED (rc=1) every render on the absent parquets.
        # Non-fatal: missing panel degrades gracefully; later builds use cached state.
        run: python -m scripts.oracle_nightly --skip-panel --skip-graph --skip-episodes --skip-timemachine || echo "::warning::oracle_nightly failed (non-fatal — state and alerts degrade gracefully)"
      - name: build world state (neural-web W1)
        if: always()
        # Neural Web N1: compose data/neuralweb/world_state.json from all fresh
        # stores (market_state/latest.json, regime/latest.json, breadth.parquet,
        # oracle_state.json, run_status.json, alerts_triage.json).  All inputs
        # are ready at this point (engine step + oracle_nightly have both run).
        # Resilient: fail-open with printed gaps; a failed write keeps the last
        # committed world_state and never blocks the commit step.
        # git add data/ site/ in commit engine outputs covers data/neuralweb/ automatically.
        # engine-render.yml (express lane) also runs this step — its regime/market_state
        # recompute would otherwise drift ahead of the committed world_state copies.
        run: python -m scripts.build_world_state || echo "::warning::build_world_state failed (non-fatal) — keeping last committed world_state"
      - name: build bottom-sensor envelope (NW Rails B0)
        if: always()
        # Display-only (no ranking/gating/money-path) — assembles the bottom-sensor
        # envelope for the US board (labels_v2 decision table, Amendment 1 Lane B0).
        # Reads site/factordata/signal_gate.json + us_standouts.json (written by
        # build_site above) and optional price/earnings/oracle stores.
        # Writes data/neuralweb/bottom_sensors.parquet + site/neuralwebdata/bottom_sensors.json.
        # Both outputs are covered by 'git add data/ site/' in 'commit engine outputs' below.
        # Benchmark: 2.29s on the full US universe (~1722 names, 2026-07-06).
        # Fail-soft: missing optional inputs degrade gracefully; never blocks the commit.
        run: python -m scripts.build_bottom_sensors || echo "::warning::build_bottom_sensors failed (non-fatal - keeping last committed bottom sensors)"
      - name: mature bottom sensors shadow (Amendment-3 forward audit — display-only)
        if: always()
        # PR-2 wiring: grades elapsable decline_geometry/underwater_state descriptor snapshots
        # against realized forward returns (Amendment 3 structural shadow, RUL-28).
        # Runs AFTER build_bottom_sensors so today's snapshot is available.
        # Output: site/shadow/bottom_sensors_geometry_audit.json (display-only; covered by
        # 'git add site/' in 'commit engine outputs' below). Benchmark: 0.6s (2026-07-09).
        # Fail-soft: empty until ~1-3 months of snapshots mature; always exits 0.
        run: python -m scripts.mature_bottom_sensors_shadow || echo "::warning::mature_bottom_sensors_shadow failed (non-fatal)"
      - name: mature shadow book (stock-score forward audit; see config/dag.yml)
        if: always()
        run: python -m scripts.mature_shadow_book || echo "::warning::mature_shadow_book failed (non-fatal)"
      - name: LT-4 thesis funnel history append (PR-A2 — nightly longitudinal store)
        if: always()
        # LT-4 thesis funnel longitudinal history — append-only per (ticker, snapshot_date).
        # Runs build_thesis_funnel_snapshot with --write-history (sole-advancer law: this flag
        # is ONLY passed from this step). Reads data/edgar/{statements,fundamentals_panel,
        # statements_quarterly}.parquet (committed by collect job). Writes
        # data/research/thesis_funnel_history.parquet (keep-FIRST per ticker+date; same-date
        # re-runs are idempotent no-ops). Also writes the snapshot parquet + manifest as a
        # side-effect. Display-only; no scored_path_surfaces; FIREWALL LH-R1.
        # Output covered by 'git add data/' in 'commit engine outputs' below.
        # Benchmark: ~15s (per LT4_FUNNEL_SHADOW_REPORT.md); well under 30s threshold.
        run: python -m scripts.research.build_thesis_funnel_snapshot --write-history || echo "::warning::build_thesis_funnel_history failed (non-fatal — keeping last committed history)"
      - name: Winner Autopsy nightly watch + panel (long-hold top-down department)
        if: always()
        # Winner Autopsy Lab (WA-R1..R10). Default mode computes current breakaway watch
        # states + winner_autopsy_panel.json (admin Long-Hold Lobe surface). --write-history
        # appends keep-FIRST rows (ticker, snapshot_date) to the watch history parquet
        # (sole-advancer law: this flag is ONLY passed here; --smoke+--write-history hard-exits).
        # Reads data/yahoo/ ETFs + data/massive_stock_day/ names when present; degrades to a
        # coverage-honest no-op (exit 0) when name prices are absent on the runner. The full
        # historical episode census (winner_episodes.parquet) is built off-render via --backfill
        # on the Mac host. Writes data/research/{breakaway_watch.parquet,breakaway_watch_history
        # .parquet,winner_autopsy_panel.json,winner_autopsy_manifest.json}. Display-only; no
        # scored_path_surfaces; FIREWALL WA-R5. Output covered by 'git add data/' below.
        run: python -m scripts.research.build_winner_autopsy --write-history || echo "::warning::build_winner_autopsy failed (non-fatal — keeping last committed winner-autopsy artifacts)"
      - name: entity/thesis/mechanism registry (neural-web — display-only crosswalk)
        if: always()
        # Wave B2 ETM crosswalk: builds data/neuralweb/entity_thesis_mechanism_registry.json
        # from existing local artifacts (species, candidates, governance, reflexivity, config).
        # Display-only; no authority to gate, rank, size, or originate signals (ETM-R1..R8).
        # Output covered by 'git add data/ site/' in 'commit engine outputs' below.
        run: |
          set +e
          python -m scripts.build_entity_thesis_mechanism_registry > "$RUNNER_TEMP/etm_registry.log" 2>&1; rc=$?
          cat "$RUNNER_TEMP/etm_registry.log"
          if [ "$rc" -ne 0 ]; then echo "::warning title=build_entity_thesis_mechanism_registry::rc=$rc (non-fatal)"; fi
          exit 0
      - name: build mechanism pathways (NW core-cognition W1 — MPC v1)
        if: always()
        # MPC v1 (RUL-CC-1..13): deterministic display-only pathway compiler.
        # Reads emitted artifacts only: data/regime/latest.json (market_drivers),
        # data/transmission/latest.json (chains), data/neuralweb/factor_intelligence_state.json.
        # Writes: data/neuralweb/mechanism_pathways.json, mechanism_pathways_history.jsonl,
        # site/neuralwebdata/mechanism_pathways.json.
        # All outputs covered by 'git add data/ site/' in 'commit engine outputs' below.
        # Fail-soft: stale trigger or insufficient coverage → no_pathway artifact (never raises).
        run: |
          set +e
          python -m scripts.build_mechanism_pathways > "$RUNNER_TEMP/mechanism_pathways.log" 2>&1; rc=$?
          cat "$RUNNER_TEMP/mechanism_pathways.log"
          if [ "$rc" -ne 0 ]; then echo "::warning title=build_mechanism_pathways::rc=$rc (non-fatal)"; fi
          exit 0
      - name: build thematic state (TIL W0 — display/context only)
        if: always()
        # TIL W0 (PR-A): composition organ — reads foresight_cascade, baskets, radar/radar_enriched,
        # narrative_emergence, subsector_rotation, divergence_log. Adapter-cheap; no network calls.
        # Runs AFTER build_foresight + build_baskets (foresight_cascade.json and baskets.json must
        # be fresh). Outputs: data/neuralweb/theme_state.json, site/neuralwebdata/theme_state.json,
        # data/neuralweb/theme_phase_history.jsonl (append-only PIT tape, nightly sole advancer).
        # Authority: is_context_only=true, all promotion flags false. Always exits 0.
        # Outputs covered by 'git add data/ site/' in 'commit engine outputs' below.
        run: python -m scripts.build_thematic_state || echo "::warning::build_thematic_state failed (non-fatal — last committed artifact stands)"
      - name: grade thematic (TIL W6 — grading pack)
        if: always()
        # TIL W6 (PR-H) grading pack — AFTER build_thematic_state.
        # Stage 1: append attention rows → data/foresight/earliness_log.jsonl (sole nightly
        # advancer of the log), then crowd-arrival lead-lag grader → data/foresight/earliness_grades.json.
        # Stage 2: placebo tape → data/foresight/theme_placebo_tape.jsonl (append-only).
        # Stage 3: qledger falsifier auto-evaluation → data/qledger/falsifier_evaluations.jsonl (append-only).
        # Authority: is_context_only=true, all promotion flags false. No LLM calls. Always exits 0.
        # Outputs covered by 'git add data/ site/' in 'commit engine outputs' below.
        run: python -m scripts.grade_thematic || echo "::warning::grade_thematic failed (non-fatal — last committed artifacts stand)"
      - name: Chronicle — market context timeline spine
        if: always()
        continue-on-error: true
        # if: always() is load-bearing, NOT decoration (M12): this step sits deep
        # in the job's tail, and ~19 upstream steps here can still hard-fail
        # (build_aibrief, build_mastermind, build_sector_central, notify, ...).
        # Once any of them fails, GitHub SKIPS every later step lacking always()
        # — observed on 3 of the 5 scheduled nights 2026-07-21→07-25 (runs
        # 29877186502 / 29966266057 / 30053251096, where e.g. "White House Watch"
        # was skipped while its always() neighbours ran). continue-on-error does
        # NOT cover this: it protects a step that RUNS and fails, not one that is
        # never reached. Without always() the state_log — a forward-only capture
        # that is unrecoverable for any night it misses — would advance on
        # roughly 1 night in 5.
        # M10 fix: moved from standout_audit_us (which needs: engine, i.e. runs
        # AFTER this whole job finishes) into engine, ahead of "build mastermind
        # context" below — the chronicle lobe was permanently serving the
        # PREVIOUS night's store because it built after the very context that
        # was supposed to read it fresh. This placement also gives the build a
        # COLLECT_LANE=nightly sentinel (job-level env above) — required by the
        # B2 lane gate on the state_log write path. Measured cost 0.396s;
        # negligible against the 200-minute cap.
        run: |
          set +e
          python -m scripts.build_chronicle; rc=$?
          if [ "$rc" -ne 0 ]; then
            echo "::warning title=chronicle_governor::rc=$rc (chronicle events/state_log/rollups may be stale)"
          fi
          exit 0
      - name: timings band — commit-publish (W2)
        run: python3 scripts/nightly_timings.py mark --band commit-publish || echo "::warning::timings band mark failed (non-fatal)"
      - name: commit chronicle artifacts
        if: always()
        # always() for the same reason as the build step above — and it matters
        # more here: a build that ran but whose commit was skipped leaves the
        # appended state_log row on the runner's disk only, so the capture is
        # lost exactly as if the build had never run.
        # Scoped to data/chronicle only (not folded into "commit engine
        # outputs" below): an early, dedicated commit+push means the chronicle
        # store survives even if the rest of this job later times out (this
        # job has a documented cancellation history — see its timeout-minutes
        # comment above). Own files — no other job writes these paths.
        # METADATA-ONLY PUSH — this step must NEVER run `git pull` (P0 2026-08-01,
        # d29e4dd44d / #4167). It is the one push in this job that runs while the
        # whole night's render sits DIRTY in the working tree: on 2026-08-01 its
        # `pull --rebase --autostash` parked ~5.7k uncommitted files, the re-apply
        # conflicted against three sibling cache-rotation merges (#4151/#4155/#4158,
        # 1,895 pages each), git left `Updated upstream / Stashed changes` blocks in
        # 1,707 pages AND STILL EXITED 0 — and "commit engine outputs" below swept
        # them into main via its broad `git add`. push_metadata_replay_commit builds
        # the rebased chronicle commit in a temporary index (data/chronicle has a
        # single writer, so a true replay conflict is a genuine anomaly) and pushes
        # the object id; the dirty render tree is never touched. Local main stays on
        # the original chronicle commit — the engine push loop below rebases it out
        # as an already-applied patch, exactly as the old porcelain flow did.
        run: |
          . "${GITHUB_WORKSPACE:-.}/scripts/ci/push_retry.sh"
          git config user.name "dashboard-bot"
          git config user.email "actions@users.noreply.github.com"
          git add data/chronicle 2>/dev/null || true
          if git diff --cached --quiet; then echo "no chronicle changes"; exit 0; fi
          CHRONICLE_PARENT=$(git rev-parse HEAD)
          git commit -m "chronicle: nightly event spine + state_log + rollups $(date -u +%F)"
          CHRONICLE_COMMIT=$(git rev-parse HEAD)
          CHRONICLE_MESSAGE=$(git log -1 --format=%B "$CHRONICLE_COMMIT")
          PUSH_ALARM=420
          push_retry_init "chronicle artifacts"
          while push_attempt; do
            perl -e 'alarm 120; exec @ARGV or die' -- git fetch origin main || true
            if CHRONICLE_PUBLISH=$(push_metadata_replay_commit "$CHRONICLE_PARENT" origin/main \
                "$CHRONICLE_COMMIT" "$CHRONICLE_MESSAGE" "${RUNNER_TEMP:-/tmp}/chronicle-replay.idx"); then
              # A replay whose tree equals origin/main means the chronicle content
              # already landed (e.g. a prior attempt's push worked but its alarm
              # misread it) — pushing it would create an empty duplicate commit.
              if [ "$(git rev-parse "$CHRONICLE_PUBLISH^{tree}")" = "$(git rev-parse origin/main^{tree})" ]; then
                echo "chronicle content already on origin/main — nothing to push"
                push_won; exit 0
              fi
              if push_do origin "$CHRONICLE_PUBLISH:refs/heads/main"; then
                echo "pushed chronicle artifacts on attempt $PUSH_ATTEMPT (metadata replay — dirty render tree untouched)"
                push_won; exit 0
              fi
            else
              # data/chronicle is single-writer: a replay conflict means another
              # writer touched it on main — anomalous, surface it while retrying.
              echo "::warning title=chronicle replay conflict::newer main changed data/chronicle differently (single-writer contract broken?) — retrying"
              PUSH_FAIL_CLASS="rebase-conflict"
            fi
            push_backoff
          done
          push_lost
          echo "::warning::could not push chronicle artifacts after $PUSH_ATTEMPT attempts ($PUSH_STOP) (non-fatal — the local chronicle commit still rides the engine push loop below)"
      - name: build mastermind context (NW bridge)
        if: always()
        # NW → Mastermind bridge W1: compile data/neuralweb/mastermind_context.json and
        # site/neuralwebdata/mastermind_context.json from committed NW outputs.
        # Must run AFTER build world state so world_state.json is fresh.
        # Resilient: fail-open; kept last committed context on error; never blocks the commit.
        # Both outputs covered by 'git add data/ site/' in 'commit engine outputs' below.
        run: python -m scripts.build_nw_mastermind_context || echo "::warning::build_nw_mastermind_context failed (non-fatal) — keeping last committed mastermind_context"
      - name: build mastermind feedback summary (reverse bridge)
        if: always()
        # Mastermind → NW reverse bridge W2 (FB-R7/R8/R14): reads site/mastermind/nw_feedback.json
        # (pushed by Mastermind twice daily) and writes data/governance/mastermind_feedback_summary.json.
        # Fail-open: absent/stale source writes state:"absent"/"stale" artifact (never fabricated zeros).
        # Output covered by 'git add data/' in 'commit engine outputs' below.
        run: python -m scripts.build_mastermind_feedback_summary || echo "::warning::build_mastermind_feedback_summary failed (non-fatal) — keeping last committed summary"
      - name: build neuralweb health (PR-C — NW lobe-status surface)
        if: always()
        # PR-C two-phase health: engine job builds lobe records with cortex_source='previous_run'
        # (last committed cortex memo). Cortex job runs --refresh-cortex after cortex deliberation.
        # Fail-open: per-lobe exceptions produce status='unknown'; missing synapse.yml exits 0.
        # Both outputs covered by 'git add data/ site/' in 'commit engine outputs' below.
        run: python -m scripts.build_neuralweb_health || echo "::warning::build_neuralweb_health failed (non-fatal) — keeping last committed health.json"
      - name: check nw health escalation (M3 — persistent degradation detector)
        if: always()
        # M3 escalation organ: reads health.json + its own run-date-keyed ledger
        # data/neuralweb/nw_health_run_history.jsonl (NOT daily_brief_history —
        # that is as_of-keyed and collapses when upstream data freezes as_of).
        # When overall_status==degraded or daily-cadence lobes are missing/stale
        # beyond 1.5x SLA for >=3 consecutive nightly runs, dispatches an ops alert via
        # the nw_health W6b lane (data/alert_triage/push_sent_nw_health.jsonl).
        # cortex_source=previous_run is correct here: day-2 detection still holds.
        # Fail-open: any failure exits 0, never blocks the pipeline.
        # Ledger covered by 'git add data/' in 'commit engine outputs' below.
        # TRANSPORT SECRETS REQUIRED (2026-07-10 fix): transports are step-scoped
        # in this workflow, and this step shipped without them — send_telegram/
        # send_discord skip silently when the env vars are absent, push_ops_alert
        # returns False, and the nw_health ledger can NEVER be written even when
        # the >=3-night streak is met. Same env block as the notify/push-alerts
        # steps above.
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
        run: python -m scripts.check_nw_health_escalation || echo "::warning::check_nw_health_escalation failed (non-fatal)"
      - name: build neuralweb daily brief (PR-D — NW daily brief surface, engine phase)
        if: always()
        # PR-D two-phase brief: engine job writes phase='engine' after health.json is ready.
        # Cortex job runs --finalize after --refresh-cortex to write phase='final' + history row.
        # Fail-open: any missing input → brief with explicit gap notes, exit 0.
        # Both outputs covered by 'git add data/ site/' in 'commit engine outputs' below.
        run: python -m scripts.build_neuralweb_brief || echo "::warning::build_neuralweb_brief failed (non-fatal) — keeping last committed daily_brief.json"
      - name: build attention_deterministic (PR-4 M2 — deterministic operator-attention items)
        if: always()
        # PR-4 M2: zero-LLM deterministic attention items from world_state + health + daily_brief.
        # Five pre-registered rules: contradiction tension, lobe SLA breach, regime indeterminate,
        # evidence clock overdue, cortex degraded. Fail-open: any missing input → empty-items artifact.
        # Writes data/neuralweb/attention_deterministic.json + site/neuralwebdata/ mirror.
        # Both outputs covered by 'git add data/ site/' in 'commit engine outputs' below.
        run: python -m scripts.build_attention_deterministic || echo "::warning::build_attention_deterministic failed (non-fatal) — keeping last committed attention_deterministic.json"
      - name: research_factory_monitor (W6 — paper monitor + nightly ledger)
        if: always()
        # Research Factory W6 (RF-8/RF-9/RF-10): paper monitor over all 'paper',
        # 'awaiting_data', and 'deferred' candidates.  Appends to
        # data/research_factory/paper_monitor.jsonl (keep-first per candidate_id+as_of),
        # refreshes data/research_factory/track/<id>.json for the experiments-registry
        # overlay, and appends a health.v1 row to data/research_factory/health.jsonl.
        # Emits mechanical paper→human_review and deferred→human_review transitions
        # (RF-5 A2 attention-routing) to data/research_factory/transitions.jsonl.
        # Forward-ledger law (RF-8): --write is nightly-only; intraday invocations
        # must use --dry-run.  All data/research_factory/ writes are covered by
        # 'git add data/' in 'commit engine outputs' below.
        # Non-fatal: a failed monitor never kills the nightly build.
        run: python -m scripts.research_factory_monitor --write || echo "::warning::research_factory_monitor failed (non-fatal)"
      - name: build_evidence_clock (Global Evidence Clock v1)
        if: always()
        # Global Evidence Clock (EC-R1..R8): read-only display-only aggregator.
        # Reads every review-clock source (experiments, qledger, research_factory,
        # trial_ledger, governance, rule_experiments, cycle_pattern, species, freshness)
        # and writes data/neuralweb/evidence_clock.json.
        # No LLM, no mutations, always exits 0. Covered by 'git add data/' below.
        run: python -m scripts.build_evidence_clock || echo "::warning::evidence clock build failed"
      - name: options_flow_attention reflex trigger (W-D — nightly EOD fold)
        if: always()
        # Neural Web W-D (RO-8): direction-free reflex that fires when an on-board
        # name shows a vol>OI magnitude burst (z-score ≥ 2.0 on fresh_contracts /
        # premium_mn) or is within 1% of a GEX wall/flip.  Reads EOD parquets from
        # data/options_flow/ + data/polygon_gex/ (written by the collect job) and
        # board membership from site/factordata/us_standouts*.json (written by
        # build_site above — both artifacts are present by this step).
        # Appends to data/reflexes/options_flow_attention/firings.jsonl.
        # Covered by 'git add data/' in 'commit engine outputs' below.
        # Fail-soft: dormant until flow summaries accumulate ≥5 rows of history;
        # never kills the nightly.
        run: python -m scripts.build_options_flow_attention || echo "::warning::build_options_flow_attention failed (non-fatal — reflex dormant this run)"
      - name: commit engine outputs
        if: always()   # commit the freshly-built pages even if a LATER optional builder times out or is cancelled. pages.yml deploys the COMMITTED main tree, so a skipped commit = stale live site. The builders are individually resilient (each leaves the prior page in place on its own error), so partial output is never broken — committing what built beats shipping nothing.
        run: bash scripts/ci/daily_engine_commit_outputs.sh
      - name: assemble machine-consumable feeds (site/feeds -> R2)
        if: always()
        # Handoff H4 (2026-07-02 incident): risk_radar snapshot, froth/dislocation
        # logs, group_flow meta, RRG rotation, event calendar, intl spillover,
        # per-sector breadth — one flat dir whose _manifest.json bulk consumers
        # (Mastermind bot) sync against. Never fatal; missing sources are skipped.
        run: python -m scripts.build_feeds || echo "::warning::build_feeds failed (non-fatal)"
      - name: publish heavy per-ticker stores to R2 (additive; no-op without creds)
        if: always()
        env:
          R2_ENDPOINT: ${{ secrets.R2_ENDPOINT }}
          R2_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
          R2_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
          R2_BUCKET: ${{ secrets.R2_BUCKET }}
        # US-evening shard owns the US/intl/canada/subsector stores; CN/HK go via asia-close.yml.
        # Content-hash skip -> only deltas upload. Still committed to git for now (Pages fallback);
        # the git de-bloat + Pages slim-down is the follow-up increment once the client reads R2.
        # massive_stock_day moved to the COLLECT job (2026-07-29) — it is restored, advanced and
        # published there. An engine checkout never holds that store, so listing it here only ever
        # produced a nightly publish_r2 refusal warning (under the _DATA_DIR_MIN_FILES floor).
        run: python -m scripts.publish_r2 --dirs ohlc,intlohlc,canadaohlc,subsectorohlc,subsectorohlc_russell,subsectorohlc_nasdaq,stockdata,intlstockdata,canadastockdata,intraday,feeds,stock_personality,oddsmatrix,odds_ohlcv,seasonalitydata/entities || echo "::warning::R2 publish failed (non-fatal)"
      - name: verify R2 data plane freshness (loud tripwire — the publish above is non-fatal)
        # The publish step swallows every failure (incl. the exit-0 no-op when the R2_*
        # secrets are absent/expired), so the live site would serve stale per-ticker data
        # under a green run. HEAD the PUBLIC anchors instead: if stockdata/ohlc weren't
        # touched within 26h (was 2h — too strict; content-unchanged anchors on no-delta
        # weekend/holiday runs keep the prior day's Last-Modified and falsely tripped, e.g.
        # the 2026-07-13 Sunday run at 18.8h. 26h matches scripts/healthcheck.py), this run's
        # publish did not land -> red run + step summary.
        # Deploy is unaffected (artifact steps below are if:always()); the scheduled
        # heartbeat (scripts/healthcheck.py) independently re-probes with a 26h limit.
        # massive_stock_day joined the strict anchors 2026-07-29: its manifest is re-put by
        # the COLLECT job's publish earlier in this same run, so a dead feed, a failed
        # restore or a skipped publish turns this step red within 26h instead of freezing
        # the store silently for weeks (07-04→07-29). Safe to anchor only because that
        # manifest now carries coverage.max_missing_run_weekdays_recent — audit_r2's
        # coverage probe keys the hole test on that trailing-window figure, so only a NEW
        # tip-adjacent gap reds this step; a large full-history run (a stale/mid-backfill
        # state snapshot, or a remainder the capped incremental is still chipping) stays
        # reported without failing the build.
        if: always()
        run: |
          if ! python -m scripts.audit_r2 --strict --anchors stockdata,ohlc,massive_stock_day --max-age-hours 26; then
            echo "### ❌ R2 data plane STALE/DARK — publish_r2 failed or was skipped (expired R2_* secrets?); the live site keeps serving OLD per-ticker data. See scripts/audit_r2.py." >> "$GITHUB_STEP_SUMMARY"
            exit 1
          fi
      - name: OIP PIT — fail closed after unrelated rendering completes
        if: always()
        env:
          OIP_EPISODE_BUILD_OUTCOME: ${{ steps.options_signal_episode.outcome }}
          OIP_EPISODE_PUBLISH_OUTCOME: ${{ steps.options_signal_episode_publish.outcome }}
          OIP_CAMPAIGN_BUILD_OUTCOME: ${{ steps.options_signal_campaign.outcome }}
          OIP_CAMPAIGN_PUBLISH_OUTCOME: ${{ steps.options_signal_campaign_publish.outcome }}
        # continue-on-error above lets unrelated rendering finish; this terminal gate
        # makes any missing/failed build or exact-path publication fail the engine job.
        run: bash scripts/ci/options_signal_nightly.sh assert-integrity
      - name: timings ledger + 85% budget tripwire (W2)
        if: always()   # DELIBERATELY AHEAD of the two delivery steps below — this is the ONLY job where the finish step is not last, and the exception is load-bearing. `if: always()` steps DO run after a timeout-cancel, but only inside the runner's ~5-minute force-termination grace, and this job's tail is the most expensive always() tail in the workflow. Evidence, run 31067383446 (2026-08-06): engine started 10:05:45Z, completed 13:30:46Z = 205.0m = the 200m cap + exactly 5m of grace; steps 117-121 (commit engine outputs → feeds → R2 publish → verify → strip) all ran post-cancel, step 122 "upload pages artifact" burned what was left and ended `failure`, and step 123 — this one — WAS NEVER SCHEDULED AT ALL (the GH jobs API step list jumps straight from 122 to the Post steps). Result: data/ops/nightly_timings/engine.jsonl carried ONE row, source `backfill-gh-api`, while all 15 sibling jobs had native rows — the 85% tripwire was structurally dark for the ONE job whose creep caused the Jul31-Aug6 outage, and dark on exactly the nights it existed to pre-announce. Ordering it here costs a healthy night nothing (both steps still run to completion) and on a cancel night trades an artifact upload that is already failing for the telemetry that explains WHY the night died. The row + the ::warning land in this step's first second; only its bounded commit/push can be cut short. Pinned by tests/test_nightly_timings.py::test_finish_step_is_not_stranded_behind_the_pages_artifact_upload — do not move it back below the upload.
        run: bash scripts/ci/nightly_timings_finish.sh 300

  # The factor-portfolio RETURN SERIES (factors-page chart) is a ~22-min month-end
  # walk (compute_factors x ~38 month-ends x 2 universes) — it was the real engine
  # bottleneck that pushed the job past its cap on congested runners. It only moves
  # at month-ends, build_site reads the COMMITTED factor_series.json
  # (degrade-never-raise), and the deploy uses the engine artifact — so it runs here
  # ISOLATED, in parallel with engine, can never gate/slow the daily deploy, and its
  # refresh lands on main for the next run (<=1 run stale = fine for a ~3y month-end
  # chart whose fundamental ranks refresh ~yearly). `publish` needs ONLY engine, so a
  # slow/failed factor_series can never block the dashboard going live.
  factor_series:
    needs: [et_gate, collect]
    if: always() && needs.et_gate.outputs.run != 'false'
    runs-on: [self-hosted, macstudio]
    timeout-minutes: 45
    steps:
      - name: timings — job start mark (W2)
        run: date +%s > "${RUNNER_TEMP}/nightly-timings-${GITHUB_RUN_ID}-${GITHUB_JOB}-start"
      - uses: actions/checkout@v4
        with:
          ref: main
      - run: git pull origin main
      - name: python 3.12 (Homebrew venv — self-hosted runner)
        run: |
          VENV="$HOME/.cache/mm-venv-${RUNNER_NAME:-default}"
          [ -x "$VENV/bin/python" ] || /opt/homebrew/bin/python3.12 -m venv "$VENV"
          echo "$VENV/bin" >> "$GITHUB_PATH"
          "$VENV/bin/python" -m pip install --quiet --upgrade pip
      - run: pip install -r requirements.txt
      # build_factor_series._closes() reads these breadth close caches (broad =
      # breadth+smallcap+midcap, narrow = breadth). Restore-only — the collect job
      # produces/saves them.
      - name: restore breadth (S&P 500) close cache
        uses: actions/cache/restore@v4
        with:
          path: data/breadth/_closes_cache.parquet
          key: breadth-closes-fseries-${{ github.run_id }}
          restore-keys: breadth-closes-
      - name: restore smallcap (S&P 600) close cache
        uses: actions/cache/restore@v4
        with:
          path: data/smallcap_breadth/_closes_cache.parquet
          key: smallcap-closes-fseries-${{ github.run_id }}
          restore-keys: smallcap-closes-
      - name: restore midcap (S&P 400) close cache
        uses: actions/cache/restore@v4
        with:
          path: data/midcap_breadth/_closes_cache.parquet
          key: midcap-closes-fseries-${{ github.run_id }}
          restore-keys: midcap-closes-
      - name: factor return series (build_factor_series)
        run: python -m scripts.build_factor_series || echo "factor series skipped (additive)"
      - name: commit factor series
        run: |
          . "${GITHUB_WORKSPACE:-.}/scripts/ci/push_retry.sh"
          git config user.name "dashboard-bot"
          git config user.email "actions@users.noreply.github.com"
          git add site/factordata/factor_series.json
          if git diff --cached --quiet; then echo "no factor-series changes"; exit 0; fi
          git commit -m "factor-series: month-end walk $(date -u +%F)"
          # best-effort push (different file from engine's outputs → rebases clean)
          PUSH_ALARM=420
          push_retry_init "factor series"
          while push_attempt; do
            # pre-sync collision sweep (render.yml / #2252): a mid-run commit on origin/main can
            # TRACK a path this job wrote as an UNTRACKED side-effect — the rebase checkout then
            # refuses ("untracked working tree files would be overwritten") and --autostash can't
            # clear it (it parks tracked modifications only), so every retry dies identically.
            # Everything this step publishes is already committed above, so a path untracked HERE
            # but tracked on origin/main is a throwaway local write — origin/main is authoritative.
            # Gitignored files and untracked paths origin/main doesn't track never enter the
            # intersection (runner-local stores and caches are untouched).
            perl -e 'alarm 120; exec @ARGV or die' -- git fetch origin main || true
            comm -12 <(git ls-files --others --exclude-standard | LC_ALL=C sort) \
                     <(git ls-tree -r --name-only origin/main | LC_ALL=C sort) \
              | while IFS= read -r f; do rm -f -- "$f" || true; done
            if perl -e 'alarm 420; exec @ARGV or die' -- git pull --rebase --autostash -X theirs origin main && push_autostash_ok; then
              if push_do; then echo "pushed factor series on attempt $PUSH_ATTEMPT"; push_won; exit 0; fi
            fi
            push_abort_rebase
            push_backoff
          done
          push_lost
          echo "::warning::could not push factor series after $PUSH_ATTEMPT attempts ($PUSH_STOP) (non-fatal — daily reads the last committed json)"
      - name: timings ledger + 85% budget tripwire (W2)
        if: always()
        run: bash scripts/ci/nightly_timings_finish.sh 45

  # Factor Intelligence panel — incremental nightly append (RULING-C).
  #
  # Runs AFTER factor_series (needs factor_series.json for style_regime L/S leader
  # input from chart_data.spread). Writes data/factordata/panel/ which is GITIGNORED
  # — no commit step, no sentinel git-add changes required. Fail-soft (additive):
  # a missing/bad panel NEVER blocks the dashboard going live.
  #
  # Incremental mode: only dates missing from the existing panel are built.
  # One-shot deep backfill (--start 2020-01-01, ~25 min) runs SEPARATELY via
  # `python -m scripts.build_factor_panel --backfill` — NOT in this nightly loop.
  # See §RULING-C in research/FACTOR_INTELLIGENCE_MASTERPLAN_BY_FABLE.md.
  factor_panel:
    needs: [et_gate, factor_series]
    if: always() && needs.et_gate.outputs.run != 'false'
    runs-on: [self-hosted, macstudio]
    timeout-minutes: 20
    steps:
      - name: timings — job start mark (W2)
        run: date +%s > "${RUNNER_TEMP}/nightly-timings-${GITHUB_RUN_ID}-${GITHUB_JOB}-start"
      - uses: actions/checkout@v4
        with:
          ref: main
      - run: git pull origin main
      - name: python 3.12 (Homebrew venv — self-hosted runner)
        run: |
          VENV="$HOME/.cache/mm-venv-${RUNNER_NAME:-default}"
          [ -x "$VENV/bin/python" ] || /opt/homebrew/bin/python3.12 -m venv "$VENV"
          echo "$VENV/bin" >> "$GITHUB_PATH"
          "$VENV/bin/python" -m pip install --quiet --upgrade pip
      - run: pip install -r requirements.txt
      - name: restore breadth (S&P 500) close cache
        uses: actions/cache/restore@v4
        with:
          path: data/breadth/_closes_cache.parquet
          key: breadth-closes-fseries-${{ github.run_id }}
          restore-keys: breadth-closes-
      # FIX-8 (RULING-C nightly bounds): pin to 10 trading days back so a cold
      # runner never silently rebuilds a year. The one-shot deep backfill
      # (--start 2020-01-01, ~25 min) MUST be run once manually before the panel
      # has usable history depth. See build_factor_panel.py module docstring.
      - name: factor panel (incremental — bounded 10-day window)
        run: |
          START=$(python -c "import pandas as pd; print((pd.Timestamp.today() - pd.tseries.offsets.BDay(10)).date())")
          python -m scripts.build_factor_panel --start "$START" || echo "factor panel skipped (additive)"
      # P1-D: Pair G borrowed_strength detector. Runs AFTER factor_panel step (needs
      # today's panel partition). Fail-soft: a dormant detector emits no records and
      # logs a gap note; it never kills the nightly. Output is data/neuralweb/
      # factor_contradictions.jsonl — explicitly gitignored (R2-candidate; single-copy
      # runner-local pending R2 sync); NOT gitignored implicitly via default untracked
      # behavior. The sentinel commit step does not stage it.
      - name: factor contradictions detector (Pair G borrowed_strength — P1-D)
        run: |
          set +e
          python -m engine.neuralweb.factor_contradictions > "$RUNNER_TEMP/factor_contradictions.log" 2>&1; rc=$?
          cat "$RUNNER_TEMP/factor_contradictions.log"
          if [ "$rc" -ne 0 ]; then echo "::warning title=factor_contradictions::rc=$rc (non-fatal — dormant behavior expected until panel backfill)"; fi
          exit 0
      # RUL-NW1: factor_panel job is the sole advancer of factor-namespace artifacts.
      # Builds factor_intelligence_state.json + site mirror + history digest +
      # fire_coordinates. Fail-open: always exits 0.
      - name: build factor intelligence state
        run: |
          python -m scripts.build_factor_intelligence_state || echo "::warning::build_factor_intelligence_state failed (non-fatal — fail-open)"
      # RUL-NW1: narrow allowlisted commit — stages ONLY these six paths.
      # Never adds anything else (no git add data/).
      # Firings staged here (not in cortex job) to fix the cross-job hole where
      # factor_attention firings are written in THIS job's tree but the cortex
      # job's commit step runs in a different tree where they never exist.
      - name: commit factor intelligence outputs
        run: |
          . "${GITHUB_WORKSPACE:-.}/scripts/ci/push_retry.sh"
          git config user.name "dashboard-bot"
          git config user.email "actions@users.noreply.github.com"
          git add data/neuralweb/factor_intelligence_state.json
          git add site/neuralwebdata/factor_intelligence_state.json
          git add data/factordata/factor_state_history.jsonl
          git add data/factordata/fire_coordinates.jsonl
          git add data/neuralweb/factor_contradictions.jsonl || true  # dormant until panel backfill ≥60d; absent = expected
          git add data/reflexes/factor_attention/firings.jsonl || true  # absent until first factor_attention firing
          git add site/factordata/dna_class.json
          if git diff --cached --quiet; then echo "no factor intelligence changes"; exit 0; fi
          git commit -m "factor-intelligence: nightly state artifact + pool $(date -u +%F)"
          # best-effort push (own files -> rebases clean against engine/factor_series)
          PUSH_ALARM=420
          push_retry_init "factor intelligence"
          while push_attempt; do
            # pre-sync collision sweep (render.yml / #2252): a mid-run commit on origin/main can
            # TRACK a path this job wrote as an UNTRACKED side-effect — the rebase checkout then
            # refuses ("untracked working tree files would be overwritten") and --autostash can't
            # clear it (it parks tracked modifications only), so every retry dies identically.
            # Everything this step publishes is already committed above, so a path untracked HERE
            # but tracked on origin/main is a throwaway local write — origin/main is authoritative.
            # Gitignored files and untracked paths origin/main doesn't track never enter the
            # intersection (runner-local stores and caches are untouched).
            perl -e 'alarm 120; exec @ARGV or die' -- git fetch origin main || true
            comm -12 <(git ls-files --others --exclude-standard | LC_ALL=C sort) \
                     <(git ls-tree -r --name-only origin/main | LC_ALL=C sort) \
              | while IFS= read -r f; do rm -f -- "$f" || true; done
            if perl -e 'alarm 420; exec @ARGV or die' -- git pull --rebase --autostash -X theirs origin main && push_autostash_ok; then
              if push_do; then echo "pushed factor intelligence outputs on attempt $PUSH_ATTEMPT"; push_won; exit 0; fi
            fi
            push_abort_rebase
            push_backoff
          done
          push_lost
          echo "::warning::could not push factor intelligence after $PUSH_ATTEMPT attempts ($PUSH_STOP) (non-fatal)"
      - name: timings ledger + 85% budget tripwire (W2)
        if: always()
        run: bash scripts/ci/nightly_timings_finish.sh 20

  # ── Oracle Rotation Time Machine feed (OFF-RENDER) ─────────────────────────
  # The Time Machine (site/subsector_rotation.html) reads site/oracledata/tm_*.json.
  # That feed is rebuilt by build_oracle_timemachine, which needs the oracle PANEL +
  # EPISODE parquets. build_oracle_panel is explicitly FORBIDDEN on the render path
  # (needs the heavy massive_stock_day store; a missing PIT parquet hard-crashes), so
  # the render `engine` job skips the export (--skip-timemachine) and this dedicated
  # off-render job owns the rebuild + commit. Every other input (PIT membership,
  # constituents, yahoo, themes_tree, baskets) is committed; only massive_stock_day
  # is fetched from R2 (engine publishes it earlier this run).
  # needs: engine → today's massive_stock_day is in R2 before we fetch. Off the render
  # critical path: `publish` needs only engine, so a slow/failed rebuild here can never
  # block go-live; the feed lands on main for the next deploy (<=1-run stale, same
  # contract as stock_briefs). Before this job existed the export FAILED (rc=1) every
  # render and the committed feed sat frozen at its last manual rebuild.
  oracle_offrender:
    needs: [et_gate, engine]
    if: always() && needs.et_gate.outputs.run != 'false'
    runs-on: [self-hosted, macstudio]
    timeout-minutes: 40
    steps:
      - name: timings — job start mark (W2)
        run: date +%s > "${RUNNER_TEMP}/nightly-timings-${GITHUB_RUN_ID}-${GITHUB_JOB}-start"
      - uses: actions/checkout@v4
        with:
          ref: main
      - run: git pull origin main
      - name: python 3.12 (Homebrew venv — self-hosted runner)
        run: |
          VENV="$HOME/.cache/mm-venv-${RUNNER_NAME:-default}"
          [ -x "$VENV/bin/python" ] || /opt/homebrew/bin/python3.12 -m venv "$VENV"
          echo "$VENV/bin" >> "$GITHUB_PATH"
          "$VENV/bin/python" -m pip install --quiet --upgrade pip
      - run: pip install -r requirements.txt
      - name: fetch massive_stock_day store from R2 (the one heavy oracle input not committed)
        if: always()
        env:
          R2_ENDPOINT: ${{ secrets.R2_ENDPOINT }}
          R2_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
          R2_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
          R2_BUCKET: ${{ secrets.R2_BUCKET }}
        # No-op (exit 0) when creds absent; panel build then uses whatever store is on the
        # runner and the freshness guard below flags any resulting staleness.
        run: python -m scripts.fetch_r2 --dirs massive_stock_day || echo "::warning::massive_stock_day R2 fetch failed (non-fatal — panel build may use stale/partial store)"
      - name: rebuild oracle panels + episodes + Time Machine feed
        # Strict order: build_oracle_episodes reads panel_{tier}.parquet; build_oracle_timemachine
        # reads both panels + episodes. Panel/episode failures are warned; the TM export failing
        # is a loud ::error:: (feed won't refresh). The freshness guard is the final tripwire.
        run: |
          set +e
          python -m scripts.build_oracle_panel --tier all;    rc1=$?
          python -m scripts.build_oracle_episodes --tier all; rc2=$?
          python -m scripts.build_oracle_timemachine;         rc3=$?
          if [ "$rc1" -ne 0 ]; then echo "::warning title=oracle_panel::rc=$rc1 (panels may be stale)"; fi
          if [ "$rc2" -ne 0 ]; then echo "::warning title=oracle_episodes::rc=$rc2 (episode overlay may be stale)"; fi
          if [ "$rc3" -ne 0 ]; then echo "::error title=oracle_timemachine::export rc=$rc3 — Time Machine feed NOT refreshed"; fi
          exit 0
      - name: rebuild oracle ratio lens feed (RL-R13 — off-render, non-fatal)
        env:
          COLLECT_LANE: nightly
        run: python -m scripts.build_oracle_ratio_lens || echo "::warning::ratio_lens build failed (non-fatal — surface keeps last artifact)"
      - name: rebuild washout counter-read feed (RC-R11 — off-render, non-fatal)
        env:
          COLLECT_LANE: nightly
        run: python -m scripts.build_oracle_washout_counterread || echo "::warning::washout_counterread build failed (non-fatal — surface keeps last artifact)"
      - name: publish oracle panels to R2 (refreshes render-path sponsorship inputs)
        if: always()
        env:
          R2_ENDPOINT: ${{ secrets.R2_ENDPOINT }}
          R2_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
          R2_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
          R2_BUCKET: ${{ secrets.R2_BUCKET }}
        run: python -m scripts.publish_oracle_panels || echo "::warning::publish_oracle_panels failed (non-fatal — sponsorship keeps last R2 panels)"
      - name: Time Machine freshness tripwire (loud — the feed must advance daily)
        # Reads the just-built manifest; date_to older than 4 calendar days means the store
        # fetch or panel build produced stale data. Loud, never silent-rot; never blocks.
        run: |
          python - <<'PY' || echo "::warning::TM freshness guard errored (non-fatal)"
          import json, datetime, pathlib, sys
          mf = pathlib.Path("site/oracledata/tm_manifest.json")
          if not mf.exists():
              print("::error::tm_manifest.json missing — Time Machine feed not built"); sys.exit(0)
          m = json.loads(mf.read_text())
          today = datetime.date.today()
          for tier, info in (m.get("tiers") or {}).items():
              dt = info.get("date_to")
              if not dt:
                  print(f"::warning::TM tier {tier}: date_to is null"); continue
              age = (today - datetime.date.fromisoformat(dt)).days
              line = f"tier {tier}: date_to={dt} ({age}d old)"
              print(f"::error::TM STALE — {line}" if age > 4 else f"TM fresh — {line}")
          PY
      - name: commit oracle Time Machine feed + ratio lens
        run: |
          . "${GITHUB_WORKSPACE:-.}/scripts/ci/push_retry.sh"
          git config user.name "dashboard-bot"
          git config user.email "actions@users.noreply.github.com"
          git add site/oracledata/ data/oracle/ratio_lens_ledger.jsonl
          # washout_counterread_ledger.jsonl is only written on days the signal FIRES
          # (build_oracle_washout_counterread.py show=True); on non-firing days it is absent
          # and a hard `git add` exits 128, aborting this whole step (this failed the nightly
          # on 2026-07-13 and 2026-07-14). The commit is gated below by `git diff --cached
          # --quiet`, so tolerating the file's absence is safe.
          git add data/oracle/washout_counterread_ledger.jsonl 2>/dev/null || true
          if git diff --cached --quiet; then echo "no oracle Time Machine or ratio lens changes"; exit 0; fi
          git commit -m "oracle-tm: nightly Time Machine feed + ratio lens refresh $(date -u +%F)"
          # best-effort push (own files -> rebases clean; no other job writes site/oracledata/)
          PUSH_ALARM=420
          push_retry_init "oracle Time Machine feed"
          while push_attempt; do
            # pre-sync collision sweep (render.yml / #2252): a mid-run commit on origin/main can
            # TRACK a path this job wrote as an UNTRACKED side-effect — the rebase checkout then
            # refuses ("untracked working tree files would be overwritten") and --autostash can't
            # clear it (it parks tracked modifications only), so every retry dies identically.
            # Everything this step publishes is already committed above, so a path untracked HERE
            # but tracked on origin/main is a throwaway local write — origin/main is authoritative.
            # Gitignored files and untracked paths origin/main doesn't track never enter the
            # intersection (runner-local stores and caches are untouched).
            perl -e 'alarm 120; exec @ARGV or die' -- git fetch origin main || true
            comm -12 <(git ls-files --others --exclude-standard | LC_ALL=C sort) \
                     <(git ls-tree -r --name-only origin/main | LC_ALL=C sort) \
              | while IFS= read -r f; do rm -f -- "$f" || true; done
            if perl -e 'alarm 420; exec @ARGV or die' -- git pull --rebase --autostash -X theirs origin main && push_autostash_ok; then
              if push_do; then echo "pushed oracle Time Machine feed on attempt $PUSH_ATTEMPT"; push_won; exit 0; fi
            fi
            push_abort_rebase
            push_backoff
          done
          push_lost
          echo "::warning::could not push oracle Time Machine feed after $PUSH_ATTEMPT attempts ($PUSH_STOP) (non-fatal)"
      - name: timings ledger + 85% budget tripwire (W2)
        if: always()
        run: bash scripts/ci/nightly_timings_finish.sh 40

  # ── TLT-R2: Technical Lab data refresh (off-render) ─────────────────────
  # build_tech_lab_data.py is heavy (231 tickers × 195 signals, ~57 min) and
  # writes ONLY site/factordata/tech_screener.json + tech_lab.json (+ the
  # optional site/factordata/tech_events/ subtree when that sibling PR lands).
  # build_tech_confluence.py (multi-signal combo miner, 654.4s ≈ 11 min measured
  # end-to-end on the full universe — the "~8 min" this comment used to claim is
  # stale) runs after it → site/factordata/tech_confluence.json.
  # Kept off the main render path so it never adds to the ~67min render budget.
  # Needs: engine (data/stocks/ parquets, site/factordata/us_standouts.json).
  tech_lab_offrender:
    needs: [et_gate, engine]
    if: always() && needs.et_gate.outputs.run != 'false'
    runs-on: [self-hosted, macstudio]
    timeout-minutes: 120    # was 40 — EVERY nightly 2026-07-19→07-26 was CANCELLED at exactly the 40m cap, mid-way through the first heavy step, so steps 2-5 (incl. the confluence-screener page render AND the commit) were skipped and NOTHING published for 8 days. Cause: #3013 (merged 2026-07-19T01:40, ~4.5h before #3061 added the screener render here) expanded the single-pass build from 64 signals to 195 on a 231-ticker universe — a ~3x workload increase with no re-budget. Measured from the 07-26 job log: 50 tickers/12m28s then 50/11m50s ≈ 14.6s/ticker → 231 tickers ≈ 56m for build_tech_lab_data alone. Whole-job worst case: checkout up to ~15m (the 14k-file tree, see the engine job's note) + 57m lab data + 11m combo miner + ~1m screener page + ~2m m2_profiles + up to ~10m commit/push-retry band ≈ 96m. 120 clears that with headroom. The combo-miner figure is measured end-to-end, not inherited and not extrapolated: this job's own comment claimed "~8 min", and a full 2026-07-27 run on the runner host logged `panel: 323.4s` + `mine_long: 187.7s` + `mine_short: 140.9s` + `artifact: 1.7s` = **654.4s ≈ 11m**. Note mine_short is ~25% CHEAPER than mine_long (58 vs 59 legs but far fewer surviving combos) — an earlier symmetry estimate put the total at ~13m, so even the arithmetic shortcut was wrong. A stale step-cost label is precisely what produced this outage; re-measure, never inherit, and never extrapolate one half from the other. NOTE the per-step `set +e … exit 0` wrappers make a *script* error non-fatal but do NOTHING for a job timeout: the cap hard-cancels the running step, skips every later step, and the `::warning` alarm the step would have printed never fires — the job just reports "cancelled", which reads like this repo's known-benign cancellations. That is why an 8-day publish outage went unnoticed. If the signal count or universe grows again, re-measure and re-budget HERE.
    steps:
      - name: timings — job start mark (W2)
        run: date +%s > "${RUNNER_TEMP}/nightly-timings-${GITHUB_RUN_ID}-${GITHUB_JOB}-start"
      - uses: actions/checkout@v4
        with:
          ref: main
      - run: git pull origin main
      - name: python 3.12 (Homebrew venv — self-hosted runner)
        run: |
          VENV="$HOME/.cache/mm-venv-${RUNNER_NAME:-default}"
          [ -x "$VENV/bin/python" ] || /opt/homebrew/bin/python3.12 -m venv "$VENV"
          echo "$VENV/bin" >> "$GITHUB_PATH"
          "$VENV/bin/python" -m pip install --quiet --upgrade pip
      - run: pip install -r requirements.txt
      - name: timings band — lab-data (W2)
        run: python3 scripts/nightly_timings.py mark --band lab-data || echo "::warning::timings band mark failed (non-fatal)"
      - name: rebuild Technical Lab screener + lab data (195-signal, ~231-ticker, ~57 min)
        run: |
          set +e
          python -m scripts.build_tech_lab_data; rc=$?
          if [ "$rc" -ne 0 ]; then
            echo "::warning title=tech_lab_data::build_tech_lab_data rc=$rc (artifacts may be stale)"
          fi
          exit 0
      - name: timings band — combo-miner (W2)
        run: python3 scripts/nightly_timings.py mark --band combo-miner || echo "::warning::timings band mark failed (non-fatal)"
      - name: rebuild Technical Lab combo miner (tech_confluence, ~8 min)
        run: |
          set +e
          python scripts/build_tech_confluence.py; rc=$?
          if [ "$rc" -ne 0 ]; then
            echo "::warning title=tech_confluence::build_tech_confluence rc=$rc (combo artifact may be stale)"
          fi
          exit 0
      - name: timings band — pages-m2 (W2)
        run: python3 scripts/nightly_timings.py mark --band pages-m2 || echo "::warning::timings band mark failed (non-fatal)"
      - name: render free confluence screener page (+ share card)
        run: |
          set +e
          python -m scripts.build_confluence_screener; rc=$?
          if [ "$rc" -ne 0 ]; then
            echo "::warning title=confluence_screener::build_confluence_screener rc=$rc (page may be stale)"
          fi
          exit 0
      - name: build M2 VWAP/volume-profile level cache (~1-2 min)
        run: |
          set +e
          python scripts/build_m2_profiles.py; rc=$?
          if [ "$rc" -ne 0 ]; then
            echo "::warning title=m2_profiles::build_m2_profiles rc=$rc (M2 artifact may be stale)"
          fi
          exit 0
      - name: timings band — commit (W2)
        run: python3 scripts/nightly_timings.py mark --band commit || echo "::warning::timings band mark failed (non-fatal)"
      # DELIBERATELY NOT `if: always()`, unlike the engine and ledger_heartbeat
      # commit steps. Those salvage completed work when the job is cancelled;
      # here that would be a downgrade. build_tech_lab_data writes its payloads
      # with a bare `json.dump` into an open handle at the very END of the ~57min
      # pass (scripts/build_tech_lab_data.py:735-820) — no temp-file-and-rename —
      # so a cancel landing inside that narrow write window leaves a TRUNCATED
      # tech_screener.json / tech_lab.json on disk, and an always() commit would
      # publish it to the live site. Committing nothing beats committing corrupt
      # JSON. Make the writes atomic first if you want always() here.
      - name: commit Technical Lab artifacts
        run: |
          . "${GITHUB_WORKSPACE:-.}/scripts/ci/push_retry.sh"
          git config user.name "dashboard-bot"
          git config user.email "actions@users.noreply.github.com"
          git add site/factordata/tech_screener.json site/factordata/tech_lab.json
          if [ -f site/factordata/tech_confluence.json ]; then
            git add site/factordata/tech_confluence.json
          fi
          if [ -f site/factordata/m2_profiles.json ]; then
            git add site/factordata/m2_profiles.json
          fi
          # tech_events is written by a sibling PR (TLT-W3); pick it up once present.
          if [ -d site/factordata/tech_events ]; then
            git add site/factordata/tech_events
          fi
          if [ -f site/confluence_screener.html ]; then
            git add site/confluence_screener.html
          fi
          if [ -f site/premiumdata/confluence_screener.json ]; then
            git add site/premiumdata/confluence_screener.json
          fi
          if [ -f site/og/confluence_screener.png ]; then
            git add site/og/confluence_screener.png
          fi
          if git diff --cached --quiet; then echo "no tech-lab artifact changes"; exit 0; fi
          git commit -m "tech-lab: nightly 64-signal screener + lab refresh $(date -u +%F)"
          # best-effort rebase-push (own files → clean rebases; no other job writes these paths)
          PUSH_ALARM=420
          push_retry_init "tech-lab artifacts"
          while push_attempt; do
            # pre-sync collision sweep (render.yml / #2252): a mid-run commit on origin/main can
            # TRACK a path this job wrote as an UNTRACKED side-effect — the rebase checkout then
            # refuses ("untracked working tree files would be overwritten") and --autostash can't
            # clear it (it parks tracked modifications only), so every retry dies identically.
            # Everything this step publishes is already committed above, so a path untracked HERE
            # but tracked on origin/main is a throwaway local write — origin/main is authoritative.
            # Gitignored files and untracked paths origin/main doesn't track never enter the
            # intersection (runner-local stores and caches are untouched).
            perl -e 'alarm 120; exec @ARGV or die' -- git fetch origin main || true
            comm -12 <(git ls-files --others --exclude-standard | LC_ALL=C sort) \
                     <(git ls-tree -r --name-only origin/main | LC_ALL=C sort) \
              | while IFS= read -r f; do rm -f -- "$f" || true; done
            if perl -e 'alarm 420; exec @ARGV or die' -- git pull --rebase --autostash -X theirs origin main && push_autostash_ok; then
              if push_do; then echo "pushed tech-lab artifacts on attempt $PUSH_ATTEMPT"; push_won; exit 0; fi
            fi
            push_abort_rebase
            push_backoff
          done
          push_lost
          echo "::warning::could not push tech-lab artifacts after $PUSH_ATTEMPT attempts ($PUSH_STOP) (non-fatal)"
      - name: timings ledger + 85% budget tripwire (W2)
        if: always()
        run: bash scripts/ci/nightly_timings_finish.sh 120

  # SA-W1 — US standout attribution organ (off-render, ~40 min budget)
  # Reads the committed retro_grades.parquet produced by the engine job, runs
  # deterministic two-axis attribution, writes evidence packs, stratified scoreboard,
  # coverage monitor, missed-mover census, upstream concordance, and the US fitness
  # card. Never touches asia-owned paths. Must NOT gate publish.
  # Pattern: oracle_offrender / tech_lab_offrender (needs: engine, if: always()).
  standout_audit_us:
    needs: [et_gate, engine]
    if: always() && needs.et_gate.outputs.run != 'false'
    runs-on: [self-hosted, macstudio]
    timeout-minutes: 40
    steps:
      - name: timings — job start mark (W2)
        run: date +%s > "${RUNNER_TEMP}/nightly-timings-${GITHUB_RUN_ID}-${GITHUB_JOB}-start"
      - uses: actions/checkout@v4
        with:
          ref: main
      - run: git pull origin main
      - name: python 3.12 (Homebrew venv — self-hosted runner)
        run: |
          VENV="$HOME/.cache/mm-venv-${RUNNER_NAME:-default}"
          [ -x "$VENV/bin/python" ] || /opt/homebrew/bin/python3.12 -m venv "$VENV"
          echo "$VENV/bin" >> "$GITHUB_PATH"
          "$VENV/bin/python" -m pip install --quiet --upgrade pip
      - run: pip install -r requirements.txt
      - name: SA-W1 — US standout attribution + fitness card
        run: |
          set +e
          python -m engine.standout_audit --us; rc=$?
          if [ "$rc" -ne 0 ]; then
            echo "::warning title=standout_audit_us::rc=$rc (attribution artifacts may be stale)"
          fi
          exit 0
      - name: Prophet Trade Memory — accrue matured pick autopsies
        # Observation/reflection only: this explicitly arms the old metabolism
        # guard for this one process, but the writer remains research-only and
        # cannot rank, size, gate, suppress, or escalate a live signal.
        env:
          AUTONOMY_PAUSED: "false"
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          CLAUDE_CODE_OAUTH_TOKEN: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
          CLAUDE_CODE_OAUTH_TOKEN_1: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_1 }}
          CLAUDE_CODE_OAUTH_TOKEN_2: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_2 }}
          CLAUDE_CODE_OAUTH_TOKEN_3: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_3 }}
          CLAUDE_CODE_OAUTH_TOKEN_4: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_4 }}
          CLAUDE_CODE_OAUTH_TOKEN_5: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_5 }}
          CLAUDE_CODE_OAUTH_TOKEN_6: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_6 }}
          CLAUDE_CODE_OAUTH_TOKEN_7: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_7 }}
          DEEPSEEK_API_KEY: ${{ secrets.DEEPSEEK_API_KEY }}
        run: |
          set +e
          python -m scripts.run_prophet_pick_autopsies --market us; rc=$?
          if [ "$rc" -ne 0 ]; then
            echo "::warning title=prophet_trade_memory::pick autopsy lane failed (rc=$rc)"
          fi
          exit 0
      - name: Prophet Trade Memory — review private operator episodes
        # Personal facts/autopsies remain in owner-scoped Supabase rows. The
        # runner prints counts only and commits no private artifact.
        env:
          SUPABASE_SERVICE_KEY: ${{ secrets.SUPABASE_SERVICE_KEY }}
          SUPABASE_OPERATOR_USER_ID: ${{ secrets.SUPABASE_OPERATOR_USER_ID }}
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          CLAUDE_CODE_OAUTH_TOKEN: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
          CLAUDE_CODE_OAUTH_TOKEN_1: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_1 }}
          CLAUDE_CODE_OAUTH_TOKEN_2: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_2 }}
          CLAUDE_CODE_OAUTH_TOKEN_3: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_3 }}
          CLAUDE_CODE_OAUTH_TOKEN_4: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_4 }}
          CLAUDE_CODE_OAUTH_TOKEN_5: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_5 }}
          CLAUDE_CODE_OAUTH_TOKEN_6: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_6 }}
          CLAUDE_CODE_OAUTH_TOKEN_7: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_7 }}
          DEEPSEEK_API_KEY: ${{ secrets.DEEPSEEK_API_KEY }}
        run: |
          set +e
          python -m scripts.run_trade_memory --cap 8; rc=$?
          if [ "$rc" -ne 0 ]; then
            echo "::warning title=prophet_trade_memory::private episode lane failed (rc=$rc)"
          fi
          exit 0
      - name: Prophet W1 — US track record page
        run: |
          set +e
          python -m scripts.build_track_record_page; rc=$?
          if [ "$rc" -ne 0 ]; then
            echo "::warning title=build_track_record_page::rc=$rc (track record page may be stale)"
          fi
          exit 0
      - name: Prophet W2 — cross-market governor
        run: |
          set +e
          python -m engine.neuralweb.prophet_governor; rc=$?
          if [ "$rc" -ne 0 ]; then
            echo "::warning title=prophet_governor::rc=$rc (prophet_status/suggestions may be stale)"
          fi
          exit 0
      - name: restore massive_stock_day store from R2 (hot-tape pack wide-tail source)
        # engine/marketing/hot_tape_pack.py scans this store as the ~20k-name WIDE TAIL,
        # unioned freshest-source-per-ticker with the curated data/baskets/ohlcv +
        # data/stocks trees. This job's checkout never held it, so the nightly pack has
        # been quietly covering only the ~3k curated names — the pack degrades by design
        # (a name present only in the massive store just drops out), which is exactly why
        # nothing ever reported it. Read-only here: no publish-back, and the marketing
        # step is deliberately NOT gated on this — a failed restore costs coverage, never
        # the job. `needs: engine` (which needs collect) puts this after the collect job's
        # publish, so the restore picks up tonight's tip.
        continue-on-error: true
        env:
          R2_ENDPOINT: ${{ secrets.R2_ENDPOINT }}
          R2_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
          R2_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
          R2_BUCKET: ${{ secrets.R2_BUCKET }}
        run: python -m scripts.fetch_r2 --dirs massive_stock_day --workers 24
      - name: Marketing — NW lobe governor
        continue-on-error: true
        env:
          # Arms the nightly LLM copywriter lane (persona posts). Double-guarded
          # (config copywriter.llm.enabled AND this env var); any LLM failure or
          # per-post validation failure falls back to the deterministic templates.
          MARKETING_LLM_ENABLED: "1"
          # …and the credentials that arming actually needs. WITHOUT THESE THE
          # FLAG ABOVE IS A LIE: lib.config.secret() reads env only, so
          # llm_auth.build_providers() returned [] on every nightly,
          # write_posts_llm hit `if not providers: return None`, and EVERY post
          # silently fell back to the deterministic templates. The whole voice
          # layer — personas, copy laws, the "sound like a real person" prompt —
          # had never once run in production, which is what made the flagship
          # account read machine-written (2026-07-26 incident). copywriter.py now
          # also emits a ::warning:: when armed-but-credential-less, so this
          # cannot fail silently again.
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          CLAUDE_CODE_OAUTH_TOKEN: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
          CLAUDE_CODE_OAUTH_TOKEN_1: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_1 }}
          CLAUDE_CODE_OAUTH_TOKEN_2: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_2 }}
          CLAUDE_CODE_OAUTH_TOKEN_3: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_3 }}
          CLAUDE_CODE_OAUTH_TOKEN_4: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_4 }}
          CLAUDE_CODE_OAUTH_TOKEN_5: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_5 }}
          CLAUDE_CODE_OAUTH_TOKEN_6: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_6 }}
          CLAUDE_CODE_OAUTH_TOKEN_7: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_7 }}
          DEEPSEEK_API_KEY: ${{ secrets.DEEPSEEK_API_KEY }}
          METAB_KEYS_ENABLED: ${{ vars.METAB_KEYS_ENABLED }}
          # Emits the current D1 content-plan items into the outbox queue
          # (status=queued). Posting stays gated by approval + MARKETING_PUBLISH_ENABLED.
          MARKETING_OUTBOX_ENABLED: "1"
          # D02 chart-image pipeline: content_studio uploads the rendered PNG to the
          # public R2 data plane so the post can attach a chart (Buffer hosts no
          # uploads; X rejects SVG). Same creds the oracle/data lanes use — absent
          # them media_publish no-ops and the post degrades to text-only.
          R2_ENDPOINT: ${{ secrets.R2_ENDPOINT }}
          R2_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
          R2_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
          R2_BUCKET: ${{ secrets.R2_BUCKET }}
        run: |
          set +e
          python -m scripts.build_marketing; rc=$?
          if [ "$rc" -ne 0 ]; then
            echo "::warning title=marketing_governor::rc=$rc (marketing_state/lobe may be stale)"
          fi
          exit 0
      # Ad Central Plane O: fold the browser's ad_exposure beacons into the arena
      # ledgers (research/AD_CENTRAL_MASTERPLAN.md §2). Nightly is the SOLE advancer
      # of a forward ledger, so this is the only writer — an intraday lane may
      # compute with --dry-run and must never write.
      #
      # The Management API PAT pair, same one scripts/geo_enrich.py uses. WITHOUT
      # THEM THIS IS A NO-OP, not a failure: the script prints "skipping" and exits
      # 0, so provisioning the secret is what turns the lane on. (daily.yml carries
      # SUPABASE_SERVICE_KEY for other steps — that is a DIFFERENT credential and
      # does not reach the Management API's SQL endpoint.)
      - name: ad central — ingest split-test exposures
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
          SUPABASE_PROJECT_REF: ${{ secrets.SUPABASE_PROJECT_REF }}
        run: |
          set +e
          python -m scripts.ad_ingest_run --days 7; rc=$?
          if [ "$rc" -ne 0 ]; then
            echo "::warning title=ad_ingest::rc=$rc (arena ledgers may not have advanced)"
          fi
          exit 0
      # Persona memory consolidation (XG-W3). Emission-time counters — phrase
      # fatigue, open promises, relationship touches — accumulate all day in the
      # GITIGNORED host spool under data/marketing/personas_host/ (zero
      # tracked-repo writes intraday, the same posture as the outbox's
      # items-host.jsonl). Nightly is the SOLE advancer of the tracked ledgers
      # under data/marketing/personas/, so this is the only writer — charter §2
      # amendment 13. It is idempotent: records dedup on a stable key, because a
      # retried nightly that double-counted would silently TIGHTEN every
      # per-quirk frequency cap those counters bound.
      #
      # A host with no spool is a no-op that exits 0 and prints a ::notice, not a
      # failure — most runners never emit anything.
      - name: marketing — consolidate persona memory
        env:
          MARKETING_PERSONA_MEMORY_ENABLED: "1"
        run: |
          set +e
          python -m engine.marketing.persona_memory; rc=$?
          if [ "$rc" -ne 0 ]; then
            echo "::warning title=persona_memory::rc=$rc (persona ledgers may not have advanced)"
          fi
          exit 0
      # XG-W6 learning loop. TWO ordered actions in one step, and the order is
      # load-bearing: labels.consolidate advances the tracked label ledger +
      # scorecard from the LIVE metrics poll (data/marketing/post_metrics.jsonl,
      # written by marketing-publish.yml), the reply desk's host store, and the
      # gitignored intraday spool; health_monitor.run then READS that store to
      # grade every account and run the network tripwire. Evaluating first would
      # grade yesterday's corpus and call it today's health.
      #
      # Nightly is the SOLE advancer of data/marketing/learning/ (charter §2
      # amendment 13). The host spool (data/marketing/learning_host/) is
      # gitignored and must never appear in a git add.
      #
      # A tripwire trip writes ONE HALT ROW PER IMPLICATED ACCOUNT — a halted
      # desk stops on both rails (publisher + reply export) while the other six
      # keep running. Only an operator clears a halt, from the admin health
      # panel. With no telemetry this is a clean no-op that exits 0.
      - name: marketing — labels + account health (XG-W6)
        env:
          MARKETING_LEARNING_ENABLED: "1"
        run: |
          set +e
          python -m scripts.marketing_learning_nightly; rc=$?
          if [ "$rc" -ne 0 ]; then
            echo "::warning title=marketing_learning::rc=$rc (label ledger / health report may not have advanced)"
          fi
          exit 0
      # XG-W6 learning artifacts get their OWN commit, not a ride on the
      # standout-audit subject line. These are marketing-desk safety and
      # measurement records — labels.jsonl, scorecard.json, health.json,
      # halts.json and the learned-rule version log — and a HALT landing under
      # "standout-audit-us: nightly US attribution" is a commit message that
      # actively misleads whoever goes looking for when a desk went dark.
      #
      # halts.json is also written by the operator's admin clear, which commits
      # it separately; last writer wins and both are recorded in the registry's
      # own log. The host spool (data/marketing/learning_host/) is gitignored
      # and must never appear here.
      - name: commit marketing learning artifacts (XG-W6)
        run: |
          . "${GITHUB_WORKSPACE:-.}/scripts/ci/push_retry.sh"
          git config user.name "dashboard-bot"
          git config user.email "actions@users.noreply.github.com"
          git add data/marketing/learning 2>/dev/null || true
          if git diff --cached --quiet; then echo "no marketing learning changes"; exit 0; fi
          HALTED=$(python3 -c "import json,pathlib;p=pathlib.Path('data/marketing/learning/halts.json');d=json.loads(p.read_text()) if p.exists() else {};print(','.join(sorted(a for a,r in (d.get('accounts') or {}).items() if isinstance(r,dict) and r.get('state')=='halted')) or 'none')" 2>/dev/null || echo "unknown")
          # METADATA-ONLY PUSH (P0 d29e4dd44d/#4167, same shape as the chronicle
          # step): this push runs while the SIBLING data/marketing/ paths that
          # "commit standout audit US artifacts" broad-adds NEXT are still dirty
          # — a `pull --rebase --autostash` here could conflict its re-apply,
          # exit 0, and hand that step a marker-polluted tree. Replay in a temp
          # index and push the object id; the dirty tree is never touched.
          LEARNING_PARENT=$(git rev-parse HEAD)
          git commit -m "marketing-learning: labels + scorecard + desk health $(date -u +%F) (halted: ${HALTED})"
          LEARNING_COMMIT=$(git rev-parse HEAD)
          LEARNING_MESSAGE=$(git log -1 --format=%B "$LEARNING_COMMIT")
          PUSH_ALARM=420
          push_retry_init "marketing learning artifacts"
          while push_attempt; do
            perl -e 'alarm 120; exec @ARGV or die' -- git fetch origin main || true
            if LEARNING_PUBLISH=$(push_metadata_replay_commit "$LEARNING_PARENT" origin/main \
                "$LEARNING_COMMIT" "$LEARNING_MESSAGE" "${RUNNER_TEMP:-/tmp}/learning-replay.idx"); then
              if [ "$(git rev-parse "$LEARNING_PUBLISH^{tree}")" = "$(git rev-parse origin/main^{tree})" ]; then
                echo "marketing learning content already on origin/main — nothing to push"
                push_won; exit 0
              fi
              if push_do origin "$LEARNING_PUBLISH:refs/heads/main"; then
                echo "pushed marketing learning artifacts on attempt $PUSH_ATTEMPT (metadata replay — dirty marketing tree untouched)"
                push_won; exit 0
              fi
            else
              PUSH_FAIL_CLASS="rebase-conflict"
            fi
            push_backoff
          done
          push_lost
          echo "::warning title=marketing_learning::could not push learning artifacts (they advance again tomorrow)"
          exit 0
      - name: commit standout audit US artifacts
        # Body extracted verbatim to scripts/ci/daily_standout_audit_us_commit.sh (512KB processing-cap diet 2026-08-12 — tests/test_workflow_file_size.py).
        run: bash scripts/ci/daily_standout_audit_us_commit.sh
      - name: timings ledger + 85% budget tripwire (W2)
        if: always()
        run: bash scripts/ci/nightly_timings_finish.sh 40

  # PROPHET US §4.5 SCAN TIER — "see everything, admit selectively".
  # Operator-ratified 2026-08-05 after the receipts that FNV/FSM/EXK/AG/SBSW were
  # structurally INVISIBLE: not merely un-admitted, but absent from every frame
  # the engine looks at (the curated universe is the S&P 1500 + Russell + curated
  # extras; those five are in none of them).
  #
  # WHY ITS OWN JOB rather than a step in `engine`. The pass needs
  # data/massive_stock_day (617 MB, ~20k parquets, R2-canonical) which the engine
  # job does NOT restore — daily.yml's three restores live in `collect`,
  # `oracle_offrender` and `standout_audit_us`. The engine job already runs
  # 135-190 min against a 200-min cap, so adding a 617 MB restore plus ~13 min of
  # gating there is precisely the budget-buster the charter forbids. `needs:
  # engine` puts this after the engine's regime/basket artifacts are committed
  # (the context stamp reads them) and after collect's R2 publish, so the restore
  # picks up tonight's tip.
  #
  # MEASURED (this Mac Studio, 2026-08-04, 3,980-name floored set): store census
  # over all 20,476 files 30 s; close panel ~10 s; signal_gate ~120 ms/name;
  # context_frame 74 ms/name => ~13 min. Timeout 60 covers the restore too.
  #
  # ZERO AUTHORITY. Board admission, gates, scores, ranks, sizing and plan intake
  # are UNCHANGED — the curated universe remains the graded population. Nothing
  # written here is read for any decision.
  us_scan_tier:
    needs: [et_gate, engine]
    if: always() && needs.et_gate.outputs.run != 'false'
    runs-on: [self-hosted, macstudio]
    timeout-minutes: 60
    steps:
      - name: timings — job start mark (W2)
        run: date +%s > "${RUNNER_TEMP}/nightly-timings-${GITHUB_RUN_ID}-${GITHUB_JOB}-start"
      - uses: actions/checkout@v4
        with:
          ref: main
      - run: git pull origin main
      - name: python 3.12 (Homebrew venv — self-hosted runner)
        run: |
          VENV="$HOME/.cache/mm-venv-${RUNNER_NAME:-default}"
          [ -x "$VENV/bin/python" ] || /opt/homebrew/bin/python3.12 -m venv "$VENV"
          echo "$VENV/bin" >> "$GITHUB_PATH"
          "$VENV/bin/python" -m pip install --quiet --upgrade pip
      - run: pip install -r requirements.txt
      - name: heal shallow curated caches from deeper LOCAL history
        # §4.5 deliverable 2, re-measured: the collector already fetches from
        # 2014-01-01 unconditionally (scripts/fetch_basket_ohlcv.py), so there is
        # no API backfill to run. The real defect is that build_stock_library's
        # universe() serves some names from data/yahoo and never falls back to
        # data/baskets/ohlcv — so GOLD carried 23 bars while 3,113 sat one
        # directory over. This splices the deeper LOCAL history in: prepend-only,
        # idempotent, no network. Runs BEFORE the scan pass so tonight's gate
        # verdicts already see the healed depth. Resilient.
        continue-on-error: true
        run: python -m scripts.heal_shallow_caches --nightly || true
      - name: restore massive_stock_day store from R2 (the scan tier's price plane)
        # Read-only here: no publish-back. fetch_r2's content-hash skip means a
        # warm runner tree costs one LIST and zero GETs. A failed restore is NOT
        # fatal — the scan runner emits a line-start ::warning and writes a
        # disclosed null, which is the honest artifact for a night with no store.
        continue-on-error: true
        env:
          R2_ENDPOINT: ${{ secrets.R2_ENDPOINT }}
          R2_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
          R2_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
          R2_BUCKET: ${{ secrets.R2_BUCKET }}
        run: python -m scripts.fetch_r2 --dirs massive_stock_day --workers 24
      - name: US scan tier — stamp the widened seeing set + runner audit
        # Writes data/prophet_scan_tier/{latest.json,forward_log.jsonl} and appends
        # tier="scan" rows to the SAME data/us_prophet_rank/candidates monthly part
        # the engine job wrote earlier tonight. Keep-first on
        # (stamp_date, ticker, board_definition) makes the curated row win on any
        # overlap, so a name can never be double-counted across tiers.
        # COLLECT_LANE=nightly arms the context-vector stamp's lane gate.
        env:
          COLLECT_LANE: nightly
        run: python -m scripts.run_us_scan_tier --nightly || true
      - name: commit scan-tier artifacts
        run: |
          . "${GITHUB_WORKSPACE:-.}/scripts/ci/push_retry.sh"
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/prophet_scan_tier 2>/dev/null || true
          git add data/us_prophet_rank/candidates 2>/dev/null || true
          # Healed caches (prepend-only splices from local donors).
          git add data/yahoo 2>/dev/null || true
          git add data/stocks 2>/dev/null || true
          if git diff --cached --quiet; then echo "no scan-tier changes"; exit 0; fi
          push_staged_heal data/ || exit 1
          if git diff --cached --quiet; then echo "no scan-tier changes after conflict heal"; exit 0; fi
          git commit -m "us-scan-tier: widened seeing set + shallow-cache heal $(date -u +%F)"
          PUSH_ALARM=420
          push_retry_init "US scan tier artifacts"
          while push_attempt; do
            perl -e 'alarm 120; exec @ARGV or die' -- git fetch origin main || true
            comm -12 <(git ls-files --others --exclude-standard | LC_ALL=C sort) \
                     <(git ls-tree -r --name-only origin/main | LC_ALL=C sort) \
              | while IFS= read -r f; do rm -f -- "$f" || true; done
            if perl -e 'alarm 420; exec @ARGV or die' -- git pull --rebase --autostash -X theirs origin main && push_autostash_ok; then
              if push_do; then echo "pushed US scan tier artifacts on attempt $PUSH_ATTEMPT"; push_won; exit 0; fi
            fi
            push_abort_rebase
            push_backoff
          done
          push_lost
          echo "::warning::could not push US scan tier artifacts after $PUSH_ATTEMPT attempts ($PUSH_STOP) (non-fatal)"
      - name: timings ledger + 85% budget tripwire (W2)
        if: always()
        run: bash scripts/ci/nightly_timings_finish.sh 60

  # Prophet US trend-intelligence — the program's four post-board nightly modules, moved
  # OFF the engine job's critical path 2026-08-06 (sibling of us_scan_tier, same shape).
  #
  # WHY. The engine job is chronically at its 200m cap (205m CANCELLED 08-03 run
  # 30862763261; 205m CANCELLED 08-06 run 31056495943) and a cancel skips "commit engine
  # outputs", so the night's accrual is COMPUTED AND THEN THROWN AWAY: on 08-06 the doors
  # emitter finished 04:45:27Z and the miss-audit 04:48:11Z, both green, and the job was
  # cancelled at 05:37:19Z with the commit step never started — data/prophet_doors,
  # data/prophet_miss_audit and data/us_prophet_rank all still show 2026-08-05 as their
  # last advance. Nothing in this program gates the dashboard: `publish` needs ONLY
  # engine, so this job's conclusion cannot delay or red go-live, and a Prophet module
  # failure now reds THIS job instead of hiding behind `|| true` inside the deploy lane.
  # This is not the whole timeout fix — measured, these four steps are ~5.4m of a ~186m
  # engine step budget (emit_prophet_doors 2.62m/2.70m and run_prophet_miss_audit
  # 2.62m/2.72m on runs 31056495943/30960328285; grade_prophet_doors 1-5s;
  # grade_us_prophet_candidates 1.8s for a steady-state night / 8.8s for a 5-night
  # catch-up, measured over the live 2,932-row candidates store). The workflow's own
  # timeout comment still names build_news as the next lever, and it belongs to its owner.
  #
  # needs: [engine, us_scan_tier] — every input is read from the COMMITTED tree, and both
  # of those jobs are what commit it:
  #   emit_prophet_doors            <- data/{breadth,midcap_breadth,smallcap_breadth}/
  #                                    _closes_cache.parquet + site/marketdata/
  #                                    subsector_rotation.json (engine: "commit engine
  #                                    outputs", git add data/ site/)
  #   grade_prophet_doors           <- data/prophet_doors/flags.jsonl (written by the step
  #                                    above, same job) + data/yahoo/SPY.parquet
  #   grade_us_prophet_candidates   <- data/us_prophet_rank/candidates/YYYY-MM.parquet,
  #                                    stamped by build_stock_library inside build_site
  #                                    (engine) and appended to by run_us_scan_tier
  #                                    (us_scan_tier); both commit it before this job runs
  #   run_prophet_miss_audit        <- data/name_score/us_calls.parquet + site/prophet/
  #                                    plans/*.json + site/factordata/us_standouts.json
  #                                    (engine), the grade store above, and data/
  #                                    prophet_scan_tier/latest.json (us_scan_tier)
  # Sequencing after us_scan_tier is deliberate on two counts: it upgrades the miss-audit's
  # scan-tier read from <=1-run-stale to same-night, and it keeps the only two jobs that
  # write under data/us_prophet_rank/ from racing each other's push.
  # if: always() — accrual must not stop because the engine hit its cap; that is the whole
  # point of the move. Every module is nightly-only and idempotent, so a partial night is
  # safe (doors dedupe 21 sessions/ticker, grades freeze on their key, the miss-audit is
  # idempotent on price_through).
  # env COLLECT_LANE=nightly is LOAD-BEARING, not decoration: emit_prophet_doors,
  # reconcile_us_candidate_episodes, grade_prophet_doors, grade_us_prophet_candidates,
  # and accrue_us_prophet_w3 all gate their forward advance on
  # engine.ledger_lane.nightly_advance_enabled() (COLLECT_LANE/US_LANE == "nightly") on top
  # of --nightly. Without it these writers would run green and write NOTHING — pinned by
  # tests/test_prophet_off_engine_lane.py and tests/test_us_candidate_episode_wiring.py.
  us_prophet_ledgers:
    needs: [et_gate, engine, us_scan_tier]
    if: always() && needs.et_gate.outputs.run != 'false'
    runs-on: [self-hosted, macstudio]
    # Budget: ~5.5m of module time (measured above) + checkout/venv/pip. Checkout of the
    # 14k-file tree has been observed at 3s..14.5m (see the engine job's timeout comment),
    # so the cap is sized like the us_scan_tier sibling rather than to the module cost.
    timeout-minutes: 60
    env:
      COLLECT_LANE: nightly   # job-level sentinel — see the note above; the forward-ledger
                              # advance gate for five of the six writer steps below.
    steps:
      - name: timings — job start mark (W2)
        run: date +%s > "${RUNNER_TEMP}/nightly-timings-${GITHUB_RUN_ID}-${GITHUB_JOB}-start"
      - uses: actions/checkout@v4
        with:
          ref: main
      - run: git pull origin main
      - name: python 3.12 (Homebrew venv — self-hosted runner)
        run: |
          VENV="$HOME/.cache/mm-venv-${RUNNER_NAME:-default}"
          [ -x "$VENV/bin/python" ] || /opt/homebrew/bin/python3.12 -m venv "$VENV"
          echo "$VENV/bin" >> "$GITHUB_PATH"
          "$VENV/bin/python" -m pip install --quiet --upgrade pip
      - run: pip install -r requirements.txt
      - name: Prophet doors — accrue Door T (theme-relay) + Door R (re-arm) flags (shadow, zero authority)
        # Prophet W3 shadow doors (research/PROPHET_DOORS_PREREG.md, frozen before first accrual).
        # Evaluates both doors on the committed caches and appends to data/prophet_doors/flags.jsonl.
        # Sole forward advancer of this ledger (nightly-only: --nightly AND COLLECT_LANE=nightly).
        # PROSPECTIVE ONLY — no backfill. ZERO AUTHORITY: no board membership, no plan
        # origination, no rank/gate/size, no site/ artifact. The commit step below (git add
        # data/) lands the store. ~2.6 min. Idempotent per as_of. The old `|| true` is gone:
        # the module already degrades internally to a disclosed null, so a non-zero exit is a
        # real crash and must be visible — and it can no longer cost anything but this job.
        run: python -m scripts.emit_prophet_doors --nightly
      - name: Prophet B1 — reconcile canonical candidate episodes (immutable generation + atomic HEAD)
        # V4-B1 is the sole durable candidate-episode writer. It reads the engine-owned
        # full TURN WATCH sidecar plus the committed candidate/Doors/Radar inputs, validates
        # one complete immutable generation, and makes it visible with one atomic HEAD.json
        # replacement. The command itself refuses durable work without BOTH --nightly and
        # COLLECT_LANE=nightly. No continue-on-error: corrupt prior truth, an invalid source,
        # or a failed publication is a real red. This step is schedule-only: a manual daily
        # dispatch must skip it, while `always()` preserves visibility after an earlier
        # scheduled sibling fails. Manual report/replay modes remain read-only.
        # Runs after Door emission and before all forward graders/W3 so those remain
        # independent measurement consumers rather than episode authorities.
        if: always() && github.event_name == 'schedule'
        run: python -m scripts.reconcile_us_candidate_episodes --nightly
      - name: Prophet doors — grade matured flags at H=10/21 vs SPY (policy-free marks)
        # Grades data/prophet_doors/flags.jsonl into grades.jsonl via engine.grading.forward_metrics
        # (next-bar fill, positional session horizons, excess vs SPY) — the SAME ruler grade_us_board
        # uses, reused not forked. Policy-free: fixed-horizon marks only, no stops/exits. Graded rows
        # are frozen (one-grader law). Runs after the emitter so tonight's flags are present.
        # OUTCOMES ARE NOT TO BE READ before the prereg §4 trigger. Idempotent.
        # if: always() — accrual is per-module; a crashed sibling must not silently stop this
        # ledger. This is NOT continue-on-error: a failure here still reds the job.
        if: always()
        run: python -m scripts.grade_prophet_doors --nightly
      - name: Prophet US full-population grades (every stamped name, not just the picks)
        # Prophet US trend-intelligence W7 (operator order 2026-08-05: "we should be
        # remembering the score that we give picks … so that we can later assess how robust
        # and correct our scoring system is"). The US Context Vector store already stamps
        # every analyzed name nightly WITH its itemized us_prophet_v1 score legs; this grades
        # every one of those rows across H=10/21/42/63 sessions excess-vs-SPY, so the graded
        # record grows by thousands of rows a night instead of ~12. Runs AFTER the engine job's
        # build_stock_library (which stamps tonight's rows) and run_us_scan_tier (which appends
        # the scan-tier rows) — both COMMIT the candidates store, which is what this reads —
        # and BEFORE the miss-audit (whose priority_score_scorecard block reads this store).
        # Same ruler as grade_us_board / grade_prophet_doors (engine.grading.forward_metrics;
        # next-bar fill, positional session horizons) — reused, not forked, and pinned
        # mark-for-mark by tests/test_us_prophet_grades.py. Sole forward advancer of
        # data/us_prophet_rank/grades/ (nightly-only), idempotent: a graded
        # (stamp_date, ticker, board_definition, horizon) is frozen, so a re-run appends
        # nothing. Daily parts under the run's month, so a closed part is never rewritten.
        # ZERO authority: no rank/gate/size/board/plan consumer, ever.
        if: always()
        run: python -m scripts.grade_us_prophet_candidates --nightly
      - name: Prophet W3 paired-race ledger (measurement plumbing, ZERO authority)
        # WS:PROPHET-CONDITIONAL-FUSION PR-3C. Accrues the already-produced C1 +
        # v2-shadow paired observations, the one shared grader, and the PR-3B
        # outcome-blind structural receipt into data/us_prophet_rank/w3/. Runs AFTER
        # grade_us_prophet_candidates so tonight's H=10 marks are present, and BEFORE
        # the ledger commit. No rank/gate/featured/plan authority. No comparative
        # statistic. No second scorer or grader. No Pages backfill.
        # Nightly-only (COLLECT_LANE=nightly). Idempotent keep-first; conflicting
        # rewrite of a frozen key fails closed.
        if: always()
        run: python -m scripts.accrue_us_prophet_w3 --nightly
      - name: Prophet miss-audit (why don't the winners surface? ops telemetry, ZERO authority)
        # Prophet US trend-intelligence W0 (masterplan
        # research/PROPHET_US_TREND_INTELLIGENCE_MASTERPLAN_BY_FABLE.md): the nightly
        # measurement the program grades itself against — per-runner dominant-excluder
        # attribution, the trailing-63-session eligibility window, sighting->plan conversion,
        # and theme-representation counts. Reads the committed close caches + the board/plan
        # artifacts through the PRODUCTION tier paths (confluence_tiers.tier_stream/cascade,
        # signal_gate); computes nothing the board reads back. Its scan-tier coverage block
        # reads data/prophet_scan_tier/latest.json — written by the us_scan_tier job this one
        # now waits on, so the read is same-night instead of one run stale (an absent artifact
        # is still a disclosed null, never an exception). Sole forward advancer of
        # data/prophet_miss_audit/forward_log.jsonl (nightly-only), idempotent on
        # price_through. ~2.6 min on ~1,500 names. ZERO authority: no rank/gate/size/
        # membership consumer, ever (tests/test_prophet_miss_audit.py pins that).
        if: always()
        run: python -m scripts.run_prophet_miss_audit --nightly
      - name: commit prophet ledger artifacts
        # if: always() — the accrual above is per-module and idempotent, so whatever DID
        # advance must land even when a sibling module crashed. This is the defect the move
        # exists to fix: an uncommitted ledger row is a lost night (the next job's
        # actions/checkout deletes the runner tree), and the engine job's cancel-skipped
        # commit is exactly how 2026-08-06 was lost. Committing what advanced beats
        # discarding the night; the job's own conclusion still carries the failure.
        if: always()
        run: |
          . "${GITHUB_WORKSPACE:-.}/scripts/ci/push_retry.sh"
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          # Every path below is written ONLY by the six steps above (no other lane writes
          # them), so the add list is exact rather than a broad `git add data/`: this job
          # must never carry another lane's stray runner-tree bytes to main.
          git add data/prophet_doors 2>/dev/null || true
          git add data/us_prophet_rank/episodes 2>/dev/null || true
          git add data/us_prophet_rank/grades 2>/dev/null || true
          git add data/us_prophet_rank/w3 2>/dev/null || true
          git add data/prophet_miss_audit 2>/dev/null || true
          if git diff --cached --quiet; then echo "no prophet ledger changes"; exit 0; fi
          push_staged_heal data/ || exit 1
          if git diff --cached --quiet; then echo "no prophet ledger changes after conflict heal"; exit 0; fi
          git commit -m "prophet-us: nightly ledger advance $(date -u +%F)"
          PUSH_ALARM=420
          push_retry_init "prophet ledger artifacts"
          while push_attempt; do
            perl -e 'alarm 120; exec @ARGV or die' -- git fetch origin main || true
            comm -12 <(git ls-files --others --exclude-standard | LC_ALL=C sort) \
                     <(git ls-tree -r --name-only origin/main | LC_ALL=C sort) \
              | while IFS= read -r f; do rm -f -- "$f" || true; done
            if perl -e 'alarm 420; exec @ARGV or die' -- git pull --rebase --autostash -X theirs origin main && push_autostash_ok; then
              if push_do; then echo "pushed prophet ledger artifacts on attempt $PUSH_ATTEMPT"; push_won; exit 0; fi
            fi
            push_abort_rebase
            push_backoff
          done
          push_lost
          echo "::warning::could not push prophet ledger artifacts after $PUSH_ATTEMPT attempts ($PUSH_STOP) (non-fatal)"
      - name: timings ledger + 85% budget tripwire (W2)
        if: always()
        run: bash scripts/ci/nightly_timings_finish.sh 60

  # AI stock-research briefs = ~30 DeepSeek calls (~20 min) — the heaviest single
  # step in the build, and what pushed the engine job past its timeout. build_site
  # now only writes the target ticker list (data/catalyst/brief_targets.json); the
  # LLM calls run HERE, isolated and in parallel with `publish`. The stock page
  # fetches site/stockbrief/<T>.json client-side, so the briefs land on main for
  # the next run (<=1-run stale is fine) and the LLM never gates the daily deploy.
  # `publish` needs ONLY engine, so a slow/failed briefs job can't block go-live.
  stock_briefs:
    needs: [et_gate, engine]
    if: always() && needs.et_gate.outputs.run != 'false'
    runs-on: [self-hosted, macstudio]
    timeout-minutes: 45
    env:
      DEEPSEEK_API_KEY: ${{ secrets.DEEPSEEK_API_KEY }}
    steps:
      - name: timings — job start mark (W2)
        run: date +%s > "${RUNNER_TEMP}/nightly-timings-${GITHUB_RUN_ID}-${GITHUB_JOB}-start"
      - uses: actions/checkout@v4
        with:
          ref: main
      - run: git pull origin main
      - name: python 3.12 (Homebrew venv — self-hosted runner)
        run: |
          VENV="$HOME/.cache/mm-venv-${RUNNER_NAME:-default}"
          [ -x "$VENV/bin/python" ] || /opt/homebrew/bin/python3.12 -m venv "$VENV"
          echo "$VENV/bin" >> "$GITHUB_PATH"
          "$VENV/bin/python" -m pip install --quiet --upgrade pip
      - run: pip install -r requirements.txt
      # the per-stock technical-signal leg reads constituent closes; restore-only.
      - name: restore breadth (S&P 500) close cache
        uses: actions/cache/restore@v4
        with:
          path: data/breadth/_closes_cache.parquet
          key: breadth-closes-briefs-${{ github.run_id }}
          restore-keys: breadth-closes-
      - name: restore smallcap (S&P 600) close cache
        uses: actions/cache/restore@v4
        with:
          path: data/smallcap_breadth/_closes_cache.parquet
          key: smallcap-closes-briefs-${{ github.run_id }}
          restore-keys: smallcap-closes-
      - name: restore midcap (S&P 400) close cache
        uses: actions/cache/restore@v4
        with:
          path: data/midcap_breadth/_closes_cache.parquet
          key: midcap-closes-briefs-${{ github.run_id }}
          restore-keys: midcap-closes-
      - name: precompute AI stock briefs (build_stock_briefs)
        run: python -m scripts.build_stock_briefs || echo "stock briefs skipped (additive)"
      - name: commit stock briefs
        run: |
          . "${GITHUB_WORKSPACE:-.}/scripts/ci/push_retry.sh"
          git config user.name "dashboard-bot"
          git config user.email "actions@users.noreply.github.com"
          # site/stockbrief is gitignored for local-dev cleanliness but must be TRACKED
          # on main so the committed-tree deploy ships it (-f overrides the ignore).
          git add -f site/stockbrief 2>/dev/null || true
          git add data/catalyst/stockbrief_cache 2>/dev/null || true
          # stock_desk (T9): the accountable-lean notes + the graded track record are written
          # by build_stock_briefs in THIS job (after the engine job's force-add of site/stockdata
          # already ran), so they must be force-added HERE or they never land on main → the
          # aidesk_lean.js panel 404s. The ledger (data/stock_desk, NOT gitignored) must also be
          # committed or the track record can never accumulate past-due leans. (Same gitignore/
          # force-add trap as the per-market stock libraries.)
          git add data/stock_desk 2>/dev/null || true
          # Eval OS E1: stock_desk's qledger registration (P3 anchor-at-registration,
          # #6598) writes claims.jsonl + evidence_clock_start/stock_desk.json under
          # data/qledger IN THIS JOB — the only lane that runs this producer. Without
          # this add, an accepted claim and the write-once evidence clock live only in
          # the runner checkout and the next job's clean checkout destroys them.
          # data/qledger's one committed-file fence (the CONTROL clock, #5970) is a
          # .gitignore rule, so a plain add here can never re-commit it.
          git add data/qledger 2>/dev/null || true
          if git diff --cached --quiet; then echo "no stock-brief changes"; exit 0; fi
          git commit -m "stock-briefs: AI research briefs + accountable leans $(date -u +%F)"
          # best-effort push (own files → rebases clean against engine/factor_series)
          PUSH_ALARM=420
          push_retry_init "stock briefs"
          while push_attempt; do
            # pre-sync collision sweep (render.yml / #2252): a mid-run commit on origin/main can
            # TRACK a path this job wrote as an UNTRACKED side-effect — the rebase checkout then
            # refuses ("untracked working tree files would be overwritten") and --autostash can't
            # clear it (it parks tracked modifications only), so every retry dies identically.
            # Everything this step publishes is already committed above, so a path untracked HERE
            # but tracked on origin/main is a throwaway local write — origin/main is authoritative.
            # Gitignored files and untracked paths origin/main doesn't track never enter the
            # intersection (runner-local stores and caches are untouched).
            perl -e 'alarm 120; exec @ARGV or die' -- git fetch origin main || true
            comm -12 <(git ls-files --others --exclude-standard | LC_ALL=C sort) \
                     <(git ls-tree -r --name-only origin/main | LC_ALL=C sort) \
              | while IFS= read -r f; do rm -f -- "$f" || true; done
            if perl -e 'alarm 420; exec @ARGV or die' -- git pull --rebase --autostash -X theirs origin main && push_autostash_ok; then
              if push_do; then echo "pushed stock briefs on attempt $PUSH_ATTEMPT"; push_won; exit 0; fi
            fi
            push_abort_rebase
            push_backoff
          done
          push_lost
          echo "::warning::could not push stock briefs after $PUSH_ATTEMPT attempts ($PUSH_STOP) (non-fatal — the stock page falls back to the last committed briefs)"
      - name: sync stock_briefs stockdata writes to R2 (post-flip freshness)
        if: always()
        env:
          R2_ENDPOINT: ${{ secrets.R2_ENDPOINT }}
          R2_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
          R2_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
          R2_BUCKET: ${{ secrets.R2_BUCKET }}
        # stock_briefs writes stockdata/ai_desk_notes.json + ai_desk_track.json AFTER the engine
        # job's R2 upload, so sync them here or the shim fetches a stale copy from R2.
        # --no-manifest: this checkout holds ONLY the two git-committed stockdata files (the
        # heavy store is R2-only), so a manifest built here would clobber the engine job's
        # full ~5000-name one — and bulk consumers prune against that list.
        run: python -m scripts.publish_r2 --dirs stockdata --no-manifest || echo "::warning::R2 stockdata sync failed (non-fatal)"
      - name: timings ledger + 85% budget tripwire (W2)
        if: always()
        run: bash scripts/ci/nightly_timings_finish.sh 45

  # ── Neural Web W7b: cortex deliberation job ──────────────────────────────
  # Runs after the engine job; own checkout + own commit lane.
  # A slow or failed cortex can never gate the deploy (publish needs: engine, not cortex).
  # Shadow probation: A2 refused today — attention queue and memo are shadow-tier.
  cortex:
    needs: [et_gate, engine]
    if: always() && needs.et_gate.outputs.run != 'false'
    runs-on: [self-hosted, macstudio]
    timeout-minutes: 30
    env:
      CLAUDE_CODE_OAUTH_TOKEN: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
      CLAUDE_CODE_OAUTH_TOKEN_1: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_1 }}
      CLAUDE_CODE_OAUTH_TOKEN_2: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_2 }}
      CLAUDE_CODE_OAUTH_TOKEN_3: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_3 }}
      CLAUDE_CODE_OAUTH_TOKEN_4: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_4 }}
      CLAUDE_CODE_OAUTH_TOKEN_5: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_5 }}
      CLAUDE_CODE_OAUTH_TOKEN_6: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_6 }}
      CLAUDE_CODE_OAUTH_TOKEN_7: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_7 }}
      METAB_KEYS_ENABLED: ${{ vars.METAB_KEYS_ENABLED }}
      ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
      # Dedicated metered API key for the cortex job — decouples deliberation from
      # the shared OAuth session quota (root cause of 429 rate_limit_error runs).
      # OPERATOR ACTION: create a GitHub secret CORTEX_ANTHROPIC_API_KEY with a
      # metered Anthropic API key.  Absent/empty = transparent fallback to the
      # shared ANTHROPIC_API_KEY above; nothing breaks if the secret is not set.
      CORTEX_ANTHROPIC_API_KEY: ${{ secrets.CORTEX_ANTHROPIC_API_KEY }}
    steps:
      - name: timings — job start mark (W2)
        run: date +%s > "${RUNNER_TEMP}/nightly-timings-${GITHUB_RUN_ID}-${GITHUB_JOB}-start"
      - uses: actions/checkout@v4
        with:
          ref: main
      - run: git pull origin main
      - name: python 3.12 (Homebrew venv — self-hosted runner)
        run: |
          VENV="$HOME/.cache/mm-venv-${RUNNER_NAME:-default}"
          [ -x "$VENV/bin/python" ] || /opt/homebrew/bin/python3.12 -m venv "$VENV"
          echo "$VENV/bin" >> "$GITHUB_PATH"
          "$VENV/bin/python" -m pip install --quiet --upgrade pip
      - run: pip install -r requirements.txt
      # Factor Intelligence P2 — registration persistence fix. Operator hypothesis
      # registrations MUST be written in the tree that commits machine_registry.jsonl
      # (this job): the factor_ops.yml dispatch actions wrote to a different
      # workflow's checkout and were wiped by the next fresh checkout (git clean)
      # before any commit step could see them — same cross-job visibility class as
      # the factor_attention firings hole (PR #1583). The W27 h1-h3 and W28 h4-h5
      # dispatch registrations were both lost this way. Idempotent (skips
      # already-registered hypothesis text); --defer-on-budget exits 0 when the
      # metabolism weekly budget (3/ISO-week) can't absorb a batch, so the step
      # retries nightly until a week with budget. Runs BEFORE cortex deliberation
      # so operator batches claim budget ahead of cortex-proposed hypotheses.
      - name: build context risk lens (nw-context-intelligence W3, R-CI7, display-only)
        # Personality risk lens: board/universe archetype exposure profile x regime matrices.
        # All inputs git-tracked (us_standouts, stock_personality, regime_timeline).
        # Outputs: data/neuralweb/context_risk.json + site/neuralwebdata/context_risk.json.
        # Covered by the narrow git add block in 'commit cortex outputs' below. Always exits 0.
        run: |
          set +e
          python -m scripts.build_context_risk > "$RUNNER_TEMP/context_risk.log" 2>&1; rc=$?
          cat "$RUNNER_TEMP/context_risk.log"
          if [ "$rc" -ne 0 ]; then echo "::warning title=build_context_risk::rc=$rc (non-fatal)"; fi
          exit 0
      - name: register factor hypotheses (P2 — idempotent, budget-deferred)
        run: |
          set +e
          python -m scripts.register_factor_hypotheses --only h1,h2,h3 --defer-on-budget > "$RUNNER_TEMP/factor_hyp_reg.log" 2>&1; rc1=$?
          python -m scripts.register_factor_hypotheses --only h4,h5 --defer-on-budget >> "$RUNNER_TEMP/factor_hyp_reg.log" 2>&1; rc2=$?
          cat "$RUNNER_TEMP/factor_hyp_reg.log"
          if [ "$rc1" -ne 0 ] || [ "$rc2" -ne 0 ]; then
            echo "::warning title=register_factor_hypotheses::rc=($rc1,$rc2) (non-fatal)"
          fi
          exit 0
      - name: build context candidates (W4 — nightly context scanner, CORTEX JOB)
        # R-CI5: three FROZEN v1 template families (T1/T2/T3) with declared budget.
        # Refuses to run without budget row (GovernorRefusal); budget is idempotently
        # registered on first run.  Output is display/context tier only (R-CI6).
        run: |
          set +e
          python -m scripts.build_context_candidates > "$RUNNER_TEMP/context_candidates.log" 2>&1; rc=$?
          cat "$RUNNER_TEMP/context_candidates.log"
          if [ "$rc" -ne 0 ]; then echo "::warning title=context_candidates::rc=$rc (non-fatal — last committed context_candidates.jsonl remains valid)"; fi
          exit 0
      - name: cortex deliberation (Opus-class tool loop — attention queue, memo, hypothesis metabolism)
        run: |
          set +e
          python -m engine.neuralweb.cortex > "$RUNNER_TEMP/cortex.log" 2>&1; rc=$?
          cat "$RUNNER_TEMP/cortex.log"
          if [ "$rc" -ne 0 ]; then echo "::warning title=cortex::rc=$rc (non-fatal — last committed outputs remain valid)"; fi
          exit 0
      - name: grade cortex attention + A2 earn-in (W7b PR2)
        run: |
          set +e
          python -m scripts.grade_cortex_attention > "$RUNNER_TEMP/cortex_grade.log" 2>&1; rc=$?
          cat "$RUNNER_TEMP/cortex_grade.log"
          if [ "$rc" -ne 0 ]; then echo "::warning title=cortex_grade::rc=$rc (non-fatal)"; fi
          exit 0
      # P1-D: grade factor_attention reflex firings. Parallel grader to grade_cortex_attention;
      # owns data/reflexes/factor_attention/grades.jsonl + probation.json (NOT cortex paths).
      - name: grade factor attention + A2 earn-in (P1-D)
        run: |
          set +e
          python -m scripts.grade_factor_attention > "$RUNNER_TEMP/factor_attention_grade.log" 2>&1; rc=$?
          cat "$RUNNER_TEMP/factor_attention_grade.log"
          if [ "$rc" -ne 0 ]; then echo "::warning title=factor_attention_grade::rc=$rc (non-fatal)"; fi
          exit 0
      - name: evaluate due cortex hypotheses (W7b PR2)
        run: |
          set +e
          python -m scripts.evaluate_cortex_hypotheses > "$RUNNER_TEMP/cortex_eval.log" 2>&1; rc=$?
          cat "$RUNNER_TEMP/cortex_eval.log"
          if [ "$rc" -ne 0 ]; then echo "::warning title=cortex_eval::rc=$rc (non-fatal)"; fi
          exit 0
      - name: refresh neuralweb health — cortex section (PR-C)
        if: always()
        run: |
          set +e
          python -m scripts.build_neuralweb_health --refresh-cortex > "$RUNNER_TEMP/nw_health_refresh.log" 2>&1; rc=$?
          cat "$RUNNER_TEMP/nw_health_refresh.log"
          if [ "$rc" -ne 0 ]; then echo "::warning title=nw_health_refresh::rc=$rc (non-fatal — keeping previous health.json)"; fi
          exit 0
      - name: build neuralweb daily brief — finalize (PR-D, cortex phase)
        if: always()
        # PR-D cortex phase: rebuild brief with phase='final' using post-refresh-cortex
        # health.json, rewrite both copies, and upsert compact history row keyed by as_of.
        # Fail-open: any missing input → brief with explicit gap notes, exit 0.
        run: |
          set +e
          python -m scripts.build_neuralweb_brief --finalize > "$RUNNER_TEMP/nw_brief_finalize.log" 2>&1; rc=$?
          cat "$RUNNER_TEMP/nw_brief_finalize.log"
          if [ "$rc" -ne 0 ]; then echo "::warning title=nw_brief_finalize::rc=$rc (non-fatal — keeping previous daily_brief.json)"; fi
          exit 0
      - name: record orchestrator run log (W-AI)
        if: always()
        # W-AI: one deterministic run-log entry per pipeline run (+ every-N-runs review) composed
        # from health.json + daily_brief.json + mastermind_feedback_summary.json — runs AFTER the
        # brief finalize so it records the run's final state. Fail-open, exit 0.
        run: |
          set +e
          python -m scripts.build_orchestrator_log --workflow daily > "$RUNNER_TEMP/orchestrator_log.log" 2>&1; rc=$?
          cat "$RUNNER_TEMP/orchestrator_log.log"
          if [ "$rc" -ne 0 ]; then echo "::warning title=orchestrator_log::rc=$rc (non-fatal)"; fi
          exit 0
      - name: commit cortex outputs
        # Body extracted verbatim to scripts/ci/daily_cortex_commit.sh (512KB processing-cap diet 2026-08-12 — tests/test_workflow_file_size.py).
        run: bash scripts/ci/daily_cortex_commit.sh
      - name: timings ledger + 85% budget tripwire (W2)
        if: always()
        run: bash scripts/ci/nightly_timings_finish.sh 30

  # ──────────────────────────────────────────────────────────────────────────
  # CHF W3 — Causal Hypothesis Factory: nightly drift + weekly bounded battery
  # CHF-R11: runs AFTER engine (needs: engine, if: always(), timeout-minutes: 45)
  # Never writes asia-owned paths. Never touches Article-2 surfaces. Never reads
  # kernel cells. Entry-quality target family gated on REPLAY_BOARDED_PATH var.
  # ──────────────────────────────────────────────────────────────────────────
  causal:
    needs: [et_gate, engine]
    if: always() && needs.et_gate.outputs.run != 'false'
    runs-on: [self-hosted, macstudio]
    timeout-minutes: 45
    env:
      REPLAY_BOARDED_PATH: ${{ vars.REPLAY_BOARDED_PATH }}
    steps:
      - name: timings — job start mark (W2)
        run: date +%s > "${RUNNER_TEMP}/nightly-timings-${GITHUB_RUN_ID}-${GITHUB_JOB}-start"
      - uses: actions/checkout@v4
        with:
          ref: main
      - run: git pull origin main
      - name: python 3.12 (Homebrew venv — self-hosted runner)
        run: |
          VENV="$HOME/.cache/mm-venv-${RUNNER_NAME:-default}"
          [ -x "$VENV/bin/python" ] || /opt/homebrew/bin/python3.12 -m venv "$VENV"
          echo "$VENV/bin" >> "$GITHUB_PATH"
          "$VENV/bin/python" -m pip install --quiet --upgrade pip
      - run: pip install -r requirements.txt
      - name: build causal feature inventory (CHF W1 refresh, nightly)
        # Refreshes config/causal_priors.yml kill-mask mirror and
        # data/neuralweb/causal_feature_inventory.json. Non-fatal (exits 0).
        run: |
          set +e
          python -m scripts.build_causal_inventory > "$RUNNER_TEMP/causal_inventory.log" 2>&1; rc=$?
          cat "$RUNNER_TEMP/causal_inventory.log"
          if [ "$rc" -ne 0 ]; then echo "::warning title=build_causal_inventory::rc=$rc (non-fatal)"; fi
          exit 0
      - name: build causal frontier + surprise queue + lab state (CHF W3 nightly, CHF-R8)
        # Drift-only step: no batteries, no TrialLedger writes, no LLM.
        # Produces: causal_frontier.json, causal_surprise_queue.jsonl,
        #           causal_lab_state.json (data/ + site/ copies). Non-fatal.
        run: |
          set +e
          python -m scripts.build_causal_frontier > "$RUNNER_TEMP/causal_frontier.log" 2>&1; rc=$?
          cat "$RUNNER_TEMP/causal_frontier.log"
          if [ "$rc" -ne 0 ]; then echo "::warning title=build_causal_frontier::rc=$rc (non-fatal)"; fi
          exit 0
      - name: build causal confluence audit (CHF W6 nightly, CHF-R10)
        # Anti-mirage auditor: deterministic rule application over committed
        # artifacts. Zero R-ORTH recomputation (RUL-ORTH-9); zero LLM content.
        # Reads: causal_feature_inventory.json, confluence_graph.json,
        #        covariance_spine.json, causal_priors.yml, causal_mechanisms.jsonl.
        # Outputs: data/neuralweb/causal_confluence_audit.json. Non-fatal.
        run: |
          set +e
          python -m scripts.build_causal_confluence_audit > "$RUNNER_TEMP/causal_audit.log" 2>&1; rc=$?
          cat "$RUNNER_TEMP/causal_audit.log"
          if [ "$rc" -ne 0 ]; then echo "::warning title=build_causal_confluence_audit::rc=$rc (non-fatal)"; fi
          exit 0
      - name: run causal battery (CHF W3 weekly, CHF-R8, Tuesday gate, K<=40)
        # Weekly bounded battery over top-K unexplored/accruing frontier cells.
        # Tuesday-gated (date +%u == 2): no-op on other days.
        # CHF-R5 estimator suite: HAC, cross-ticker permutation, lag placebo,
        # time-shift placebo (DT-R14), circular block bootstrap, era split (DT-R16).
        # Non-fatal (exits 0 even if battery fails).
        run: |
          set +e
          if [ "$(date +%u)" = "2" ]; then
            python -m scripts.build_causal_edges --weekly > "$RUNNER_TEMP/causal_edges.log" 2>&1; rc=$?
            cat "$RUNNER_TEMP/causal_edges.log"
            if [ "$rc" -ne 0 ]; then echo "::warning title=build_causal_edges::rc=$rc (non-fatal)"; fi
          else
            echo "causal battery: skipping (not Tuesday, date +%u=$(date +%u))"
          fi
          exit 0
      - name: run causal LLM brainstorm (CHF W5 weekly, Tuesday gate, auto_loop gate)
        # Weekly LLM brainstorm step (CHF-R8): runs AFTER build_causal_edges.
        # Tuesday-gated (date +%u == 2): the full run happens Tuesdays only.
        # The runner checks auto_loop in config/causal_llm.yml; if false,
        # degrades to pack-only mode (operator-paste workflow; no LLM calls).
        # Identity: CLAUDE_CODE_OAUTH_TOKEN ONLY (operator ruling 2026-07-09,
        # overriding the W-AUTO service-key line for CHF — masterplan §9).
        # Non-Tuesday nights run --status-only: refresh causal_llm_lane.json
        # from config truth (no pack, no LLM) so the lane artifact never
        # misreports the auto_loop state between weekly runs (stale-lane fix
        # 2026-07-11: the #2061 arming was invisible in the artifact for days).
        # Non-fatal (exits 0 even if LLM chain fails — pack-only degradation is expected).
        env:
          CLAUDE_CODE_OAUTH_TOKEN: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
          CLAUDE_CODE_OAUTH_TOKEN_1: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_1 }}
          CLAUDE_CODE_OAUTH_TOKEN_2: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_2 }}
          CLAUDE_CODE_OAUTH_TOKEN_3: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_3 }}
          CLAUDE_CODE_OAUTH_TOKEN_4: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_4 }}
          CLAUDE_CODE_OAUTH_TOKEN_5: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_5 }}
          CLAUDE_CODE_OAUTH_TOKEN_6: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_6 }}
          CLAUDE_CODE_OAUTH_TOKEN_7: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_7 }}
          METAB_KEYS_ENABLED: ${{ vars.METAB_KEYS_ENABLED }}
        run: |
          set +e
          if [ "$(date +%u)" = "2" ]; then
            python -m scripts.run_causal_brainstorm --trigger scheduled > "$RUNNER_TEMP/causal_brainstorm.log" 2>&1; rc=$?
            cat "$RUNNER_TEMP/causal_brainstorm.log"
            if [ "$rc" -ne 0 ]; then echo "::warning title=run_causal_brainstorm::rc=$rc (non-fatal)"; fi
          else
            echo "causal LLM brainstorm: skipping full run (not Tuesday, date +%u=$(date +%u)); refreshing lane status"
            python -m scripts.run_causal_brainstorm --status-only > "$RUNNER_TEMP/causal_lane_status.log" 2>&1; rc=$?
            cat "$RUNNER_TEMP/causal_lane_status.log"
            if [ "$rc" -ne 0 ]; then echo "::warning title=run_causal_brainstorm_status::rc=$rc (non-fatal)"; fi
          fi
          exit 0
      - name: commit causal outputs (CHF-R11 narrow git-add)
        run: |
          . "${GITHUB_WORKSPACE:-.}/scripts/ci/push_retry.sh"
          git config user.name "dashboard-bot"
          git config user.email "actions@users.noreply.github.com"
          # CHF-R11 narrow git-add block (pre-written in masterplan)
          git add data/neuralweb/causal_feature_inventory.json 2>/dev/null || true
          git add data/neuralweb/causal_edges.jsonl data/neuralweb/causal_nulls.jsonl 2>/dev/null || true
          git add data/neuralweb/causal_mechanisms.jsonl 2>/dev/null || true
          git add data/neuralweb/causal_frontier.json data/neuralweb/causal_surprise_queue.jsonl 2>/dev/null || true
          git add data/neuralweb/causal_confluence_audit.json 2>/dev/null || true
          git add data/neuralweb/causal_lab_state.json site/neuralwebdata/causal_lab_state.json 2>/dev/null || true
          git add data/neuralweb/causal_brainstorm_runs.jsonl data/neuralweb/causal_llm_lane.json 2>/dev/null || true
          git add data/trial_ledger.jsonl 2>/dev/null || true
          if git diff --cached --quiet; then echo "no causal output changes"; exit 0; fi
          git commit -m "causal: frontier drift + lab state $(date -u +%F)"
          # Retry-rebase push loop with --autostash (stranded-data law #1823)
          PUSH_ALARM=420
          push_retry_init "causal outputs"
          while push_attempt; do
            # pre-sync collision sweep (render.yml / #2252): a mid-run commit on origin/main can
            # TRACK a path this job wrote as an UNTRACKED side-effect — the rebase checkout then
            # refuses ("untracked working tree files would be overwritten") and --autostash can't
            # clear it (it parks tracked modifications only), so every retry dies identically.
            # Everything this step publishes is already committed above, so a path untracked HERE
            # but tracked on origin/main is a throwaway local write — origin/main is authoritative.
            # Gitignored files and untracked paths origin/main doesn't track never enter the
            # intersection (runner-local stores and caches are untouched).
            perl -e 'alarm 120; exec @ARGV or die' -- git fetch origin main || true
            comm -12 <(git ls-files --others --exclude-standard | LC_ALL=C sort) \
                     <(git ls-tree -r --name-only origin/main | LC_ALL=C sort) \
              | while IFS= read -r f; do rm -f -- "$f" || true; done
            if perl -e 'alarm 420; exec @ARGV or die' -- git pull --rebase --autostash origin main && push_autostash_ok; then
              if push_do; then echo "pushed causal outputs on attempt $PUSH_ATTEMPT"; push_won; exit 0; fi
            fi
            push_abort_rebase
            push_backoff
          done
          push_lost
          echo "::warning::could not push causal outputs after $PUSH_ATTEMPT attempts ($PUSH_STOP) (non-fatal — downstream reads last committed)"

      - name: signal foundry forward accrual (SF-R4, nightly-ledger law)
        # Accrues realized (feature, target) rows for every registered Foundry spec
        # where today > registered_at (forward evidence only — SF-R4).
        # Non-fatal: exits 0 regardless of internal errors.
        run: |
          python -m scripts.signal_foundry_accrue > "$RUNNER_TEMP/sf_accrue.log" 2>&1; rc=$?
          cat "$RUNNER_TEMP/sf_accrue.log"
          if [ "$rc" -ne 0 ]; then
            echo "::warning title=signal_foundry_accrue::rc=$rc (non-fatal)"
          fi
          exit 0

      - name: commit signal foundry forward outputs (SF-R10 narrow git-add)
        run: |
          . "${GITHUB_WORKSPACE:-.}/scripts/ci/push_retry.sh"
          git config user.name "dashboard-bot"
          git config user.email "actions@users.noreply.github.com"
          # SF-R10 narrow git-add: only data/signal_foundry/forward and lane_status
          git add data/signal_foundry/forward 2>/dev/null || true
          git add data/signal_foundry/lane_status.json 2>/dev/null || true
          if git diff --cached --quiet; then echo "no signal foundry forward changes"; exit 0; fi
          git commit -m "signal-foundry: forward accrual $(date -u +%F)"
          # Retry-rebase push loop with --autostash (stranded-data law #1823)
          PUSH_ALARM=420
          push_retry_init "sf forward outputs"
          while push_attempt; do
            # pre-sync collision sweep (render.yml / #2252): a mid-run commit on origin/main can
            # TRACK a path this job wrote as an UNTRACKED side-effect — the rebase checkout then
            # refuses ("untracked working tree files would be overwritten") and --autostash can't
            # clear it (it parks tracked modifications only), so every retry dies identically.
            # Everything this step publishes is already committed above, so a path untracked HERE
            # but tracked on origin/main is a throwaway local write — origin/main is authoritative.
            # Gitignored files and untracked paths origin/main doesn't track never enter the
            # intersection (runner-local stores and caches are untouched).
            perl -e 'alarm 120; exec @ARGV or die' -- git fetch origin main || true
            comm -12 <(git ls-files --others --exclude-standard | LC_ALL=C sort) \
                     <(git ls-tree -r --name-only origin/main | LC_ALL=C sort) \
              | while IFS= read -r f; do rm -f -- "$f" || true; done
            if perl -e 'alarm 420; exec @ARGV or die' -- git pull --rebase --autostash origin main && push_autostash_ok; then
              if push_do; then echo "pushed sf forward outputs on attempt $PUSH_ATTEMPT"; push_won; exit 0; fi
            fi
            push_abort_rebase
            push_backoff
          done
          push_lost
          echo "::warning::could not push sf forward outputs after $PUSH_ATTEMPT attempts ($PUSH_STOP) (non-fatal)"
          exit 0
      - name: timings ledger + 85% budget tripwire (W2)
        if: always()
        run: bash scripts/ci/nightly_timings_finish.sh 45

  # Light advisory job: generates docs/ACTIVE_BUILD_MAP.md and
  # data/governance/active_builds.json — a map of open PRs, file collisions,
  # and recent merges for multi-agent session air-traffic control.
  # ADVISORY ONLY: fail-open, no merge gating, exit 0 even if gh errors.
  active_build_map:
    needs: [et_gate, collect]
    if: always() && needs.et_gate.outputs.run != 'false'
    runs-on: [self-hosted, macstudio]
    timeout-minutes: 10
    steps:
      - name: timings — job start mark (W2)
        run: date +%s > "${RUNNER_TEMP}/nightly-timings-${GITHUB_RUN_ID}-${GITHUB_JOB}-start"
      - uses: actions/checkout@v4
        with:
          ref: main
      - run: git pull origin main
      - name: python 3.12 (Homebrew venv — self-hosted runner)
        run: |
          VENV="$HOME/.cache/mm-venv-${RUNNER_NAME:-default}"
          [ -x "$VENV/bin/python" ] || /opt/homebrew/bin/python3.12 -m venv "$VENV"
          echo "$VENV/bin" >> "$GITHUB_PATH"
      - name: generate active build map
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          python -m scripts.build_active_build_map || echo "::warning::active_build_map generation failed (non-fatal — fail-open)"
      - name: generate agent OS state
        # ORDERED AFTER the active build map, which is its PR-state input: the join is
        # only as fresh as active_builds.json, so regenerating first would publish a
        # CEO view built on yesterday's PR state.
        #
        # This is the ONLY regenerator (DEC:AGENTOS-NIGHTLY-IS-THE-ONLY-REGENERATOR).
        # There is deliberately no CI drift guard forcing record-touching PRs to commit
        # a regenerated copy — that would put two independent record edits into conflict
        # on a shared generated file, which is the write pattern invariant I2 exists to
        # prevent.  One writer; staleness is printed in the artifact, never hidden.
        # No network: PR state comes only from the file written by the step above.
        run: |
          python3 scripts/agentos.py status || echo "::warning::agent_os_state generation failed (non-fatal — fail-open)"
      - name: commit active build map outputs
        run: |
          . "${GITHUB_WORKSPACE:-.}/scripts/ci/push_retry.sh"
          git config user.name "dashboard-bot"
          git config user.email "actions@users.noreply.github.com"
          git add docs/ACTIVE_BUILD_MAP.md data/governance/active_builds.json \
                  docs/AGENT_OS_STATE.md data/governance/agent_os_state.json
          if git diff --cached --quiet; then echo "no active build map changes"; exit 0; fi
          git commit -m "docs: active build map $(date -u +%F)"
          PUSH_ALARM=420
          push_retry_init "active build map"
          while push_attempt; do
            # pre-sync collision sweep (render.yml / #2252): a mid-run commit on origin/main can
            # TRACK a path this job wrote as an UNTRACKED side-effect — the rebase checkout then
            # refuses ("untracked working tree files would be overwritten") and --autostash can't
            # clear it (it parks tracked modifications only), so every retry dies identically.
            # Everything this step publishes is already committed above, so a path untracked HERE
            # but tracked on origin/main is a throwaway local write — origin/main is authoritative.
            # Gitignored files and untracked paths origin/main doesn't track never enter the
            # intersection (runner-local stores and caches are untouched).
            perl -e 'alarm 120; exec @ARGV or die' -- git fetch origin main || true
            comm -12 <(git ls-files --others --exclude-standard | LC_ALL=C sort) \
                     <(git ls-tree -r --name-only origin/main | LC_ALL=C sort) \
              | while IFS= read -r f; do rm -f -- "$f" || true; done
            if perl -e 'alarm 420; exec @ARGV or die' -- git pull --rebase --autostash -X theirs origin main && push_autostash_ok; then
              if push_do; then echo "pushed active build map on attempt $PUSH_ATTEMPT"; push_won; exit 0; fi
            fi
            push_abort_rebase
            push_backoff
          done
          push_lost
          echo "::warning::could not push active build map after $PUSH_ATTEMPT attempts ($PUSH_STOP) (non-fatal)"
      - name: timings ledger + 85% budget tripwire (W2)
        if: always()
        run: bash scripts/ci/nightly_timings_finish.sh 10

  # The `publish` GitHub Pages deploy job (needs: [et_gate, engine], deploy-pages@v4)
  # was RETIRED pre-private-cutover (DEC:B1-MACRO-PRIVATE-CUTOVER) — the Pages mirror
  # served the premium book/premiumdata anonymously and must not keep re-publishing
  # once the repo goes private. The VPS/origin deploy (the site's real serving path)
  # is untouched; only the Pages producer went away.

  # CSP-W6: forward-ledger heartbeat — runs after the engine job so we know whether
  # the site committed fresh pages for today (render_happened detection).
  # if: always() so this detection step runs even on partial nights — it is a
  # pure detector, never a gate (CSP-R1 forbids fail-dark).
  # daily.yml also touched by other open lanes today (merge-order conflicts
  # expected — keep this diff minimal and rebased on fresh main).
  ledger_heartbeat:
    needs: [et_gate, engine]
    if: always() && needs.et_gate.outputs.run != 'false'
    runs-on: [self-hosted, macstudio]
    timeout-minutes: 5
    steps:
      - name: timings — job start mark (W2)
        run: date +%s > "${RUNNER_TEMP}/nightly-timings-${GITHUB_RUN_ID}-${GITHUB_JOB}-start"
      - uses: actions/checkout@v4
        with:
          ref: main
      - run: git pull origin main
      - name: python 3.12 (Homebrew venv — self-hosted runner)
        run: |
          VENV="$HOME/.cache/mm-venv-${RUNNER_NAME:-default}"
          [ -x "$VENV/bin/python" ] || /opt/homebrew/bin/python3.12 -m venv "$VENV"
          echo "$VENV/bin" >> "$GITHUB_PATH"
          "$VENV/bin/python" -m pip install --quiet --upgrade pip
      - run: pip install -r requirements.txt
      - name: check forward-ledger advance (CSP-W6 heartbeat)
        # Detection only — never gates or fails the deploy (CSP-R1).
        # --render-happened: the engine job may have committed even on a partial
        # night (its commit step runs if: always()). The Pages `publish` deploy job
        # this comment used to also wait on was retired pre-private-cutover
        # (DEC:B1-MACRO-PRIVATE-CUTOVER); the VPS/origin deploy is the site's real
        # serving path and is untouched.
        # The flag is always passed unconditionally here — run_check() is itself
        # trading-day-aware (skips weekends/holidays) and idempotent per date.
        run: |
          python -m scripts.check_ledger_advance --render-happened || echo "::warning::check_ledger_advance failed (non-fatal — CSP-R1 fail-open)"
      - name: commit ledger heartbeat state
        if: always()
        run: |
          . "${GITHUB_WORKSPACE:-.}/scripts/ci/push_retry.sh"
          git config user.name "dashboard-bot"
          git config user.email "actions@users.noreply.github.com"
          git add data/ci/ledger_heartbeat_state.json 2>/dev/null || true
          if git diff --cached --quiet; then echo "no heartbeat state change"; exit 0; fi
          git commit -m "ops: ledger heartbeat state $(date -u +%F)"
          PUSH_ALARM=120
          PUSH_MAX_ATTEMPTS=6      # a one-line ops state file, non-fatal — a shorter budget than the render lanes is right, but 3 was not enough to outlast main's commit rate
          push_retry_init "ledger heartbeat state"
          while push_attempt; do
            perl -e 'alarm 60; exec @ARGV or die' -- git fetch origin main || true
            if perl -e 'alarm 120; exec @ARGV or die' -- git pull --rebase --autostash -X theirs origin main && push_autostash_ok; then
              if push_do; then echo "heartbeat state pushed on attempt $PUSH_ATTEMPT"; push_won; exit 0; fi
            fi
            push_abort_rebase
            push_backoff
          done
          push_lost
          echo "::warning::could not push heartbeat state after $PUSH_ATTEMPT attempts ($PUSH_STOP) (non-fatal)"
      - name: timings ledger + 85% budget tripwire (W2)
        if: always()
        run: bash scripts/ci/nightly_timings_finish.sh 5
