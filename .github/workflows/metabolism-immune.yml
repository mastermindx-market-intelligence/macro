name: metabolism-immune
# Metabolism Phase V8-A — IMMUNE lane (R-V8-1..R-V8-5).
#
# Runs every 2h.  Three responsibilities:
#   (A) Main-red sentinel: classifies red REQUIRED checks, runs deterministic
#       heal recipes in a fresh worktree, opens DRAFT heal PRs, claims work.
#   (B) Lane-health sensors: dead cron, queue saturation, offline runners,
#       key-pool degradation — each fires once per day per condition.
#   (C) Auto-merge: DEFERRED (R-V8-3 amended 2026-07-12).  Returns as R-V8-3b
#       (future wave).  This wave opens DRAFT PRs only; operator review required.
#
# NOTE: NO job-level AUTONOMY_PAUSED gate here — sensing and insight emission
# must always run.  The git-push/PR-open path is gated IN-SCRIPT on _is_paused()
# (R-V8-1 amended 2026-07-12), mirroring metabolism_build.py's is_paused-first
# pattern.  ci_status writes and insight emission run regardless of pause state.
#
# KILL SWITCH (R-AUT-2):
#   gh variable set AUTONOMY_PAUSED --body true    # pause heal push/PR (sensing always runs)
#   gh variable set AUTONOMY_PAUSED --body false   # arm heal push/PR (operator-only)

on:
  schedule:
    - cron: "15 */2 * * *"
  workflow_dispatch:
    inputs:
      dry_run:
        description: "Dry run (describe actions, no real changes)"
        type: boolean
        default: false

permissions:
  contents: write       # push heal branches + commit claim rows
  pull-requests: write  # open DRAFT heal PRs + auto-merge
  checks: read          # read check-runs for the main-red sentinel (_get_required_red_checks)
  actions: read         # read workflow runs for the ci-main-heartbeat.yml SHA + lane-health sensors

concurrency:
  group: metabolism-immune
  cancel-in-progress: false

jobs:
  immune:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          # Checkout always uses the ambient GITHUB_TOKEN — sensing (the main-red
          # sentinel) must not depend on METABOLISM_MERGE_PAT being alive, and the
          # checkout token has no bearing on whether a later PR self-triggers CI
          # (that is purely a function of the actor that OPENS the PR via gh pr
          # create, scoped separately below).
          token: ${{ secrets.GITHUB_TOKEN }}

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: install deps
        run: pip install --quiet pyyaml

      - name: run immune lane
        env:
          AUTONOMY_PAUSED: ${{ vars.AUTONOMY_PAUSED }}
          # Sensing (the main-red sentinel) runs entirely on the ambient
          # GITHUB_TOKEN — it must never depend on METABOLISM_MERGE_PAT being
          # alive.  METABOLISM_MERGE_PAT is passed through SEPARATELY below and
          # is used by scripts/metabolism_immune.py in exactly ONE place: the
          # 'gh pr create' call that opens the DRAFT heal PR (see
          # _heal_pr_create_env), because a PR opened by GITHUB_TOKEN does not
          # trigger pull_request CI (GitHub's anti-recursion rule) while a PR
          # opened by a separate no-workflows-scope PAT does.
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          METABOLISM_MERGE_PAT: ${{ secrets.METABOLISM_MERGE_PAT }}
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          RUNNER_TEMP: ${{ runner.temp }}
          DRY_RUN: ${{ github.event.inputs.dry_run || 'false' }}
        run: |
          set -euo pipefail

          DRY_FLAG=""
          if [ "${DRY_RUN:-false}" = "true" ]; then
            DRY_FLAG="--dry-run"
            echo "Running in DRY-RUN mode"
          fi

          python -m scripts.metabolism_immune $DRY_FLAG

      - name: commit ci_status.json (R-V8-5)
        if: always()
        env:
          # GITHUB_TOKEN only (no GH_TOKEN fallback to METABOLISM_MERGE_PAT).
          # NOTE the fix here is the CHECKOUT step's token: above, not this
          # GH_TOKEN — this step's `git pull --rebase` / `git push` calls
          # (scripts/ci/push_retry.sh) authenticate via the credentials
          # `actions/checkout` persisted (http.extraheader), and that script
          # references neither GH_TOKEN nor GITHUB_TOKEN at all; GH_TOKEN
          # here only matters if this step's script ever shells out to `gh`.
          # The old METABOLISM_MERGE_PAT-first checkout token meant a
          # dead-but-present PAT (see the checkout step above) silently
          # 403'd every push made under those persisted credentials — this
          # step's push included — while the failure was swallowed into a
          # non-fatal ::warning below.  ci_status.json stayed frozen at
          # 2026-08-09T08:13:13+00:00 even after the sentinel itself could
          # see again.  Same "green is indistinguishable from blindness"
          # defect this PR exists to remove, one step down: a lane that can
          # sense but cannot publish is still silently useless to
          # anomaly_monitor's ci_red_streak.  Fixing the checkout token
          # alone would already have fixed this push; changing GH_TOKEN here
          # too is consistency, not the actual mechanism — do not revert
          # the checkout token while trusting this line to still cover it.
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          . "${GITHUB_WORKSPACE:-.}/scripts/ci/push_retry.sh"
          PUSH_LABEL="metabolism-immune ci_status"
          # workflow_dispatch takes any ref — never publish HEAD to main from one.
          if ! push_on_main_ok; then exit 0; fi
          # If ci_status.json was written, commit it to main so anomaly_monitor can read it.
          STATUS_FILE="data/metabolism/ci_status.json"
          if [ ! -f "$STATUS_FILE" ]; then
            echo "ci_status.json not written — skipping commit"
            exit 0
          fi

          git config user.email "metabolism-immune[bot]@users.noreply.github.com"
          git config user.name "metabolism-immune[bot]"

          # Also stage any immune journal markers written this run
          git add "$STATUS_FILE" "data/metabolism/immune/" 2>/dev/null || true

          if git diff --cached --quiet; then
            echo "no changes to commit"
            exit 0
          fi

          # Commit ONCE before the retry loop — mirrors heartbeat.yml / dream.yml idiom.
          # git commit inside the &&-chain would exit 1 on iteration 2+ (nothing staged),
          # killing the chain silently while the workflow stays green (F1 fix).
          COMMIT_MSG="chore(immune): update ci_status + immune journals [skip ci]"
          git commit -m "$COMMIT_MSG"
          # pre-sync collision sweep (render.yml / #2252): a mid-run commit on origin/main can
          # TRACK a path this job wrote as an UNTRACKED side-effect — the rebase checkout then
          # refuses ("untracked working tree files would be overwritten") and --autostash can't
          # clear it (it parks tracked modifications only), so every retry dies identically.
          # Everything this step publishes is already committed above, so a path untracked HERE
          # but tracked on origin/main is a throwaway local write — origin/main is authoritative.
          git fetch origin main || true
          comm -12 <(git ls-files --others --exclude-standard | LC_ALL=C sort) \
                   <(git ls-tree -r --name-only origin/main | LC_ALL=C sort) \
            | while IFS= read -r f; do rm -f -- "$f" || true; done
          for _i in 1 2 3; do
            if git pull --rebase --autostash origin main && push_autostash_ok && git push origin HEAD:main; then
              echo "ci_status committed on attempt $_i"
              exit 0
            fi
            git rebase --abort 2>/dev/null || true
            echo "push attempt $_i lost a race; re-syncing"
            sleep $((_i * 7))
          done
          echo "::warning::could not push ci_status.json after 3 attempts (non-fatal)"

      - name: notify on failure
        if: failure()
        env:
          TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          RUN_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
        run: |
          [ -n "$TOKEN" ] || exit 0
          curl -s -X POST "https://api.telegram.org/bot${TOKEN}/sendMessage" \
            -d chat_id="$CHAT_ID" \
            -d text="[Metabolism] metabolism-immune run failed: ${RUN_URL}" || true
