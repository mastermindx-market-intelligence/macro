name: weekly
on:
  schedule:
    - cron: "0 14 * * 6"    # Saturday 14:00 UTC — deep-dive report
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  # Serialized batch lane shared with backfill / special-sits-backfill. daily has its own
  # "pipeline-daily" lane, so weekly never blocks (and is never blocked by) the daily refresh.
  group: pipeline-batch
  cancel-in-progress: false

jobs:
  weekly-report:
    runs-on: [self-hosted, macstudio]
    timeout-minutes: 300   # was 45 -> 120 -> 300. EVERY archived run (10/10, 2026-06-13..07-11) died at the cap:
                           # a timeout kill concludes "cancelled" not "failed", so GitHub never notified (#2193 class —
                           # same as asia-close's 8-day silent death). Measured 07-11 (run 29156750557): at the 120m kill
                           # the chain was mid-`build china` with ~15 sub-steps never reached (all of HK, commodities,
                           # bonds, transmission, vector, aibrief + library rebuild + commit). Measured segments:
                           # collectors+dead-name+IC ~18m, recalibrate 26m, build_site 25m, china fundamentals 29m,
                           # calibrate china 10m; asia-close measured build_china alone at 36m on the same congested
                           # 2-core runners; canada is now its own ~10m step (was a build_vector hook) and build_vector
                           # still builds intl via a hook — full chain ≈ 250-270m (unchanged: the canada work moved
                           # out of build_vector, it was not added).
                           # 300m restores the whole chain with headroom. Future outgrowth is no longer silent: the
                           # "stamp weekly lane alive" step below + scripts.healthcheck check_weekly_lane (heartbeat.yml)
                           # alert when the committed stamp exceeds 7.5 days.
    steps:
      - uses: actions/checkout@v4
      - name: python 3.12 (Homebrew venv - self-hosted runner)
        run: |
          VENV="$HOME/.cache/mm-venv-${RUNNER_NAME:-default}"
          [ -x "$VENV/bin/python" ] || /opt/homebrew/bin/python3.12 -m venv "$VENV"
          echo "$VENV/bin" >> "$GITHUB_PATH"
          "$VENV/bin/python" -m pip install --quiet --upgrade pip
      - run: pip install -r requirements.txt
      - name: restore attention store from R2 (wiki chip reads it at render)
        # data/attention/*.parquet are git-ignored (R2 data plane; deep history 2015-07→,
        # kept current by daily.yml's collect job). Non-fatal: absent store = the
        # attention chip is silently absent this render (build_attention_data -> None).
        env:
          R2_ENDPOINT: ${{ secrets.R2_ENDPOINT }}
          R2_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
          R2_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
          R2_BUCKET: ${{ secrets.R2_BUCKET }}
        run: python -m scripts.fetch_r2 --dirs attention || echo "::warning::attention restore failed (non-fatal — attention chip absent/stale this render)"
      # The library build (build_site) needs every S&P 1500 constituent close
      # matrix present. Weekly does not re-collect them, so restore from the
      # cache the daily run saved (caches are gitignored, not committed).
      - name: restore breadth (S&P 500) close cache
        uses: actions/cache@v4
        with:
          path: data/breadth/_closes_cache.parquet
          key: breadth-closes-weekly-${{ github.run_id }}
          restore-keys: breadth-closes-
      - name: restore smallcap (S&P 600) close cache
        uses: actions/cache@v4
        with:
          path: data/smallcap_breadth/_closes_cache.parquet
          key: smallcap-closes-weekly-${{ github.run_id }}
          restore-keys: smallcap-closes-
      - name: restore midcap (S&P 400) close cache
        uses: actions/cache@v4
        with:
          path: data/midcap_breadth/_closes_cache.parquet
          key: midcap-closes-weekly-${{ github.run_id }}
          restore-keys: midcap-closes-
      # 2026-07-25: this step was missing — the weekly deep-dive stamped a 1,704-name
      # us_calls universe between 2,966-name nightly stamps (keep-FIRST PIT: permanent).
      # Every lane that restores the S&P trio for the library must restore Russell too.
      - name: restore Russell 2000 breadth close cache
        uses: actions/cache@v4
        with:
          path: data/russell_breadth/_closes_cache.parquet
          key: russell-closes-weekly-${{ github.run_id }}
          restore-keys: russell-closes-
      # 2026-08-08 (901282ec209): these two were missing — build_hk/build_china
      # here regenerate the regime history from whatever cache the persistent
      # self-hosted workspace happened to hold. A weeks-stale leftover ran the
      # HK inflation basket dry, the axis went NaN for 9 sessions, and the
      # shipped hk_regime_timeline.json contradicted the committed parquet
      # (landing-hub crash). Restore asia-close's same-morning saves. Keys are
      # prefixed weekly- so this job's post-run save can never shadow an
      # asia-close save under its own restore prefix.
      - name: restore hk breadth close cache
        uses: actions/cache@v4
        with:
          path: data/hk_breadth/_closes_cache.parquet
          key: weekly-hk-breadth-closes-${{ github.run_id }}
          restore-keys: hk-breadth-closes-
      - name: restore china breadth close cache
        uses: actions/cache@v4
        with:
          path: data/china_breadth/_closes_cache.parquet
          key: weekly-china-breadth-closes-${{ github.run_id }}
          restore-keys: china-breadth-closes-
      - name: weekly collect + recalibrate + build (resilient, self-diagnosing)
        # Same hardening as daily: each step runs isolated; a failure becomes a
        # ::error:: annotation (readable on the run page + via the public check-runs
        # API) + a traceback in the run Summary, but NEVER aborts the job — so one
        # broken calibration/builder can't kill the whole weekly deploy. A failed
        # calibration just leaves last week's (stale) calibration in place.
        env:
          FRED_API_KEY: ${{ secrets.FRED_API_KEY }}
          # see daily.yml: turns ON browser live prices when the repo var is set.
          LIVE_QUOTES_WORKER_URL: ${{ vars.LIVE_QUOTES_WORKER_URL }}
        run: |
          set +e
          run_py() {
            local label="$1"; shift
            echo "::group::$label"
            python -m "$@" > "$RUNNER_TEMP/step.log" 2>&1; local rc=$?
            cat "$RUNNER_TEMP/step.log"
            echo "::endgroup::"
            if [ "$rc" -ne 0 ]; then
              echo "::error title=$label failed (rc=$rc)::$(tail -n 2 "$RUNNER_TEMP/step.log" | tr '\n' ' ' | tail -c 600)"
              { echo "### ❌ $label failed (rc=$rc)"; echo '<details><summary>traceback</summary>'; echo; echo '```'; tail -n 60 "$RUNNER_TEMP/step.log"; echo '```'; echo '</details>'; } >> "$GITHUB_STEP_SUMMARY"
            fi
          }
          run_py "weekly collectors" scripts.collect --only cot,sentiment_naaim,sentiment_aaii,fundamentals,stock_fundamentals
          # Phase-1+ survivorship de-bias, CIK long tail: lift the dead names that
          # company_tickers.json/Polygon can't reach (acquired/renamed → dropped from the
          # current ticker map) via the windowed EDGAR full-text crawl (efts.sec.gov +
          # data.sec.gov submissions confirm). Resolve-only + drip-capped (~200 tickers
          # /run, SEC-paced); resilient (efts 5xx retry next run). Runs FIRST so newly
          # resolved CIKs feed the companyfacts pull below in the same run. Honest,
          # partial yield by design — see data/edgar/_dead_name_coverage.json fts_funnel.
          run_py "dead-name CIK crawl (EDGAR full-text)" scripts.build_dead_name_fundamentals --resolve-only --fts-max 200
          # Phase-1B survivorship de-bias: resolve dead S&P names -> CIK (needs
          # www.sec.gov/Polygon, reachable from CI; cached) + drip companyfacts into
          # data/edgar/dead_name_panel.parquet stamped with the true filed date.
          # Resilient (run_py tolerates SEC hiccups); ~200 filers/run accrue coverage.
          run_py "dead-name fundamentals (de-bias)" scripts.build_dead_name_fundamentals --max-new 200
          # Phase-1A dead-name PRICES: recover delisted daily closes (Stooq/Polygon,
          # reachable from CI; yfinance ~0) so the merged panel can compute dead-name
          # forward returns. Resilient + drip-capped; nothing depends on it.
          run_py "dead-name prices (de-bias)" scripts.build_dead_name_prices --max-new 150
          # Phase-1A+ bankruptcy tail: classify each dead CIK acquisition/bankruptcy/
          # unknown from its 8-K Item 1.03 (data.sec.gov, reachable everywhere) and
          # impute a −100% terminal close for flagged bankruptcies that have a price
          # anchor, so the leak-free backtest realizes the wipe instead of a survivorship
          # gap. Resilient + drip-capped; softens the price-coverage residual-bias caveat.
          run_py "dead-name bankruptcy imputation (8-K 1.03)" scripts.build_dead_name_delisting --max-new 200
          # De-biased factor IC scorecard over the survivor+dead MERGED universe ->
          # data/edgar/ic_scorecard_debiased.json. Self-gating (equals the survivor
          # scorecard until dead-name prices accrue); never clobbers ic_scorecard.json.
          run_py "de-biased IC scorecard" scripts.factor_ic_scorecard --debiased --horizon 63
          # Phase-4 breadth honesty: leak-free PIT IC test of the new fundamental-trend
          # legs -> BH-FDR + VIF prune + inverse-corr shrinkage -> data/strategies/
          # signal_factory.json. Ledger-logged (idempotent — dedups by config).
          run_py "signal factory (breadth honesty)" scripts.signal_factory
          run_py "recalibrate (macro)" scripts.recalibrate
          run_py "regime engine (engine.run)" engine.run
          run_py "S&P allocation vector (build_spvector)" scripts.build_spvector
          run_py "policy-lever card (build_policy_lever)" scripts.build_policy_lever
          run_py "macro dashboard + US stocks (build_site)" scripts.build_site
          # descriptive A-share fundamentals (akshare, keyless) — refresh high-value
          # names + widen the universe ~150/run; the China build reads the cache.
          run_py "china fundamentals (subset)" collectors.china_fundamentals
          run_py "china fundamentals (widen)" collectors.china_fundamentals --all --limit 150
          run_py "calibrate china" scripts.calibrate_china
          run_py "build china" scripts.build_china
          run_py "hk fundamentals" collectors.hk_fundamentals
          run_py "calibrate hk" scripts.calibrate_hk
          run_py "build hk" scripts.build_hk
          run_py "calibrate commodities" scripts.calibrate_commodities
          run_py "research commodity conviction" scripts.research_commodity_conviction
          run_py "build commodities" scripts.build_commodities
          run_py "calibrate bonds" scripts.calibrate_bonds
          run_py "build bonds" scripts.build_bonds
          run_py "calibrate rate/inflation transmission" scripts.calibrate_rate_inflation
          run_py "build transmission" scripts.build_transmission
          # TXI W3: historical episode miner + regime-conditional hop calibration. WEEKLY
          # cadence (heavy full-history scan, off the render path — masterplan §W3). Runs
          # AFTER build_transmission (reads the same transmission/regime latest.json + the
          # parquet store). Writes data/transmission/chain_calibration.json; the nightly
          # transmission_chains read picks it up to fill hop base_rates + promote the display
          # tier. Display-only, additive, no-ops gracefully on absent history. ~1s wall.
          run_py "calibrate transmission chains (TXI W3 episode miner)" scripts.run_transmission_calibration
          # TXI W5 — Loop-C closure: autonomous transmission-chain proposals via the EXISTING
          # CHF brainstorm lane (NOT a second LLM loop). WEEKLY cadence. GATED behind BOTH
          # config/causal_llm.yml auto_loop (CHF-R8) AND config/transmission_proposals.yml
          # enabled (ships FALSE) — the runner no-ops (writes nothing, exit 0) unless both are
          # on, so wiring this step here can never auto-activate it. When on: emits the proposal
          # PACK to the CHF lane's drop location + ingests chain-shaped replies into
          # hypothesis-tier knowledge/transmission/proposed/. Display-only; W1 compiles + W3
          # backtests proposals next cycle; human PR promotes out of proposed/. Non-fatal.
          run_py "transmission-chain proposals (TXI W5 Loop-C, gated)" scripts.run_transmission_proposals --trigger scheduled
          run_py "forward path board (build_rates_command)" scripts.build_rates_command
          run_py "calibrate vector" scripts.calibrate_vector
          run_py "validate gex (forward-RV gate)" scripts.validate_gex
          # Canada / S&P-TSX — FIRST-CLASS step (2026-07-27). Was a swallowed try/except
          # inside build_vector, so its failures never reached the Actions summary (the
          # hook logged through log.error, whose level prefix pushes "::" off the line
          # start) and it had no scope/timing of its own. MUST stay above build_vector:
          # the landing hub's _canada_state() reads the data/canada_regime/latest.json
          # written here. SKIP_CANADA_HOOK=1 makes build_vector's surviving fallback hook
          # a no-op for the rest of this step so the ~10m builder does not run twice in a
          # chain that already measures ~250-270m against a 300m cap.
          run_py "build canada" scripts.build_canada
          export SKIP_CANADA_HOOK=1
          run_py "build vector + landing hub" scripts.build_vector
          run_py "AI brief page (build_aibrief)" scripts.build_aibrief
          exit 0
      - name: rebuild stock-search libraries (resilient — guarantees the analyzers ship)
        # build_china/build_hk/build_canada (and build_intl via a build_vector hook)
        # swallow errors; rebuild the per-stock search JSON (site/chinastockdata,
        # site/hkstockdata, site/canadastockdata, site/intlstockdata) directly from
        # committed caches so the A-Share / HK / TSX / intl analyzers always ship.
        # Idempotent; non-fatal.
        run: |
          set +e
          for mod in scripts.build_china_library scripts.build_hk_library scripts.build_canada_library scripts.build_intl_library; do
            echo "::group::$mod"; python -m "$mod" > "$RUNNER_TEMP/lib.log" 2>&1; rc=$?; cat "$RUNNER_TEMP/lib.log"; echo "::endgroup::"
            if [ "$rc" -ne 0 ]; then
              echo "::error title=$mod failed (rc=$rc)::$(tail -n 2 "$RUNNER_TEMP/lib.log" | tr '\n' ' ' | tail -c 600)"
              { echo "### ❌ $mod failed (rc=$rc)"; echo '```'; tail -n 40 "$RUNNER_TEMP/lib.log"; echo '```'; } >> "$GITHUB_STEP_SUMMARY"
            fi
          done
          exit 0
      - name: stamp weekly lane alive (dead-man's switch)
        # #2193-class guard: a timeout kill concludes "cancelled" (not "failed"), so GitHub
        # sends no notification — this lane was dead EVERY Saturday 2026-06-13..07-11 at the
        # old 120m cap before anyone noticed. This stamp is only written when the whole build
        # chain above actually completed (no `if: always()` — a cancelled run must NOT stamp),
        # and it rides to main via the commit step below. scripts.healthcheck check_weekly_lane
        # (heartbeat.yml, weekdays 14:30 UTC) FAILS + pings Telegram/Discord when the committed
        # stamp exceeds 7.5 days — catching a timeout kill, a dead runner, and a 5x-lost push
        # race alike (healthy worst case reads ~6 days on Friday; a missed Saturday reads ~9
        # days by the Monday check).
        run: |
          printf '{"last_run": "%s", "run_id": "%s", "workflow": "weekly"}\n' \
            "$(date -u +%FT%T+00:00)" "${GITHUB_RUN_ID:-local}" > data/weekly_status.json
      # W1 repair (2026-08-26 experiments audit §3): the BH-FDR consumer the cortex
      # evaluator has promised since W7b PR2 and that did not exist — H5's "queued for
      # FDR" pass sat in the experiments panel with nothing to read it.
      #
      # It rides WEEKLY, not with the nightly evaluator that feeds it. daily.yml is
      # ~250 bytes under the 487,000-byte budget that guards GitHub's silent
      # ~512,000-byte processing cap (#5416: over it, cron stops firing and dispatched
      # runs strand queued with zero jobs, uncancellable) — a quarter-scoped batch is
      # not worth spending that headroom on. Cadence is not correctness here: the batch
      # groups by QUARTER, is idempotent, and appends a governance event only when the
      # BH decision changes, so a weekly re-run of the same quarter is a no-op.
      - name: quarterly cortex FDR batch (W1 — promotes nothing; records the BH account)
        run: |
          python -m scripts.quarterly_cortex_fdr || echo "::warning title=cortex_fdr::batch failed (non-fatal)"
      - name: inject data-base shim (site-wide; a re-render would strip it otherwise)
        run: |
          python -m scripts.inject_data_base || echo "data-base shim inject skipped (additive)"
          python -m scripts.externalize_css || echo "css externalize skipped (additive)"
          # ::warning::, not a bare echo — this step is the only thing that moves a ?v=
          # stamp, and the edge serves versioned assets `immutable, max-age=1y`, so a failure
          # swallowed into a green run pins returning browsers to the old bytes for a year
          # (#3617/#3624). See render.yml for the full rationale.
          python -m scripts.optimize_assets || echo "::warning::asset optimize FAILED — ?v= stamps may be stale"
          # template↔site re-sync (ui.template_site_sync): externalize_css above lifts the
          # plain-copy landing/chat pages' inline <style> to hashed files, diverging
          # site/index.html + site/chat.html from their templates. This lane's push loop is
          # minimal (no post-rebase --fix like render.yml/asia-close.yml), so heal here —
          # BEFORE `git add site/` below — so weekly never commits/ships the divergence.
          python -m scripts.bake_landing_preview --fix || true
          python -m scripts.check_template_site_sync --fix || true
      - name: commit weekly outputs
        run: |
          . "${GITHUB_WORKSPACE:-.}/scripts/ci/push_retry.sh"
          git config user.name "dashboard-bot"
          git config user.email "actions@users.noreply.github.com"
          # templates/ too, and ONLY for the plain-copy HTML pairs (index.html, chat.html):
          # optimize_assets re-stamps their templates/ side because the --fix above rewrites
          # their site copy FROM templates/ — a stamp staged site-only is reverted before it
          # reaches main (#3617's fix never reached one returning browser), and staging site/
          # alone would land the pair diverged and red the publish gate. Nothing else in the
          # build path writes templates/. See render.yml.
          # refuse stash/rebase wreckage (2026-08-01 d29e4dd44d: a conflicted
          # autostash apply was staged wholesale — 1,704 pages shipped markers)
          bash "${GITHUB_WORKSPACE:-.}/scripts/ci/strip_conflict_markers.sh"
          git add data/ reports/ site/
          # separate + tolerant: `git add site/ templates/` exits 128 when a pathspec
          # matches nothing, and this step runs under `-eo pipefail` — a missing
          # templates/ would abort the commit and lose the whole render rather than
          # just the stamp. Staging site/ keeps its hard-fail semantics.
          git add templates/ 2>/dev/null || true
          # W0b sibling fix (#1963, 2026-07-08 stale-HK incident): asia-close.yml runs
          # daily at 08:30 UTC INCLUDING Saturdays and is the sole authoritative writer
          # of all china*/hk*/hkma data stores. Weekly runs at 14:00 UTC — AFTER the
          # Saturday asia-close has already committed fresh HK/China data. This job runs
          # build_china/build_hk/calibrate_china/calibrate_hk (which write to
          # data/china_regime/, data/hk_regime/ etc.), but a checkout-time stale copy of
          # data/china_stocks/, data/hk_stocks/, data/china_breadth/, data/hk_breadth/
          # etc. can also be staged by the broad `git add data/` above. Combined with
          # `-X theirs` on the rebase-push, this would clobber asia-close's fresh commit
          # with the checkout's stale snapshot — the same bug class as the 2026-07-08
          # daily.yml incident (fixed in #1963).
          # Guard: unstage every asia-close-owned path after the broad add so `-X theirs`
          # is harmless for the asia data plane. Path derivation matches #1963: asia group
          # = adapters starting with `china` or `hk` (scripts/collect.py group_members);
          # data/hkma adapter key `hkma` also starts with `hk`.
          git reset -q -- data/hk data/hk_* data/hkma data/china data/china_* 2>/dev/null || true
          # W0b carve-out: re-add the weekly-OWNED outputs the reset above just
          # unstaged. These are produced by THIS job in THIS run — calibrate_china /
          # calibrate_hk ladder records and the china/hk fundamentals collector caches —
          # not checkout-stale copies of asia-close's market stores. weekly.yml is their
          # ONLY producer (asia-close runs build_china/build_hk but never calibrate_* or
          # the fundamentals collectors), so the #1963 stale-clobber rationale does not
          # apply and `-X theirs` can never overwrite fresher asia-close data through
          # them. Without this carve-out they freeze FOREVER, not "until next Saturday"
          # as the original W0b comment claimed (observed: hk ladder frozen since its
          # 06-13 initial commit, both fundamentals parquets since 06-18, and the china
          # ladder had to be hand-seeded in #2483 to unblock the A-share record table).
          # If a calibrate/collector sub-step failed this run, its file is unchanged in
          # the working tree and this add stages nothing — last good state persists.
          # 2026-08-08 (901282ec209): regime_history.parquet joins the carve-out. The
          # engines REGENERATE it this run and the site timeline JSONs (staged via
          # `git add site/`) are built FROM that regeneration — discarding the parquet
          # while shipping the JSON committed a timeline that contradicted its own
          # store in one tree. Same THIS-run freshness argument as the ladder records;
          # engine/store_guard.py refuses the write when the recompute is degraded, so
          # what gets staged here is never worse than the checkout state.
          git add -- data/china_regime/ladder_calibration.json \
                     data/hk_regime/ladder_calibration.json \
                     data/china_regime/regime_history.parquet \
                     data/hk_regime/regime_history.parquet \
                     data/china_fundamentals/fundamentals.parquet \
                     data/hk_fundamentals/fundamentals.parquet 2>/dev/null || true
          # Stock-search libraries are gitignored — force-add so the committed-tree
          # deploy ships their index/detail JSON. See daily.yml.
          if git diff --cached --quiet; then echo "no weekly changes to commit"; exit 0; fi
          # Fail-closed conflict gate (P0 2026-08-01, d29e4dd44d / #4167): same
          # broad-add + autostash-pull + always()-artifact shape as the daily
          # engine job. Heal display paths from HEAD, die on ledger paths —
          # push_staged_heal also sweeps offenders so the always() Pages
          # artifact can never ship marker bytes.
          push_staged_heal data/ reports/ site/ templates/ || exit 1
          if git diff --cached --quiet; then echo "no weekly changes after conflict heal"; exit 0; fi
          git commit -m "weekly: deep-dive $(date -u +%F)"
          # best-effort push: losing a race with a concurrent push must NOT fail the
          # pipeline (the deploy below uses the uploaded artifact). Retry rebase+push,
          # preferring our freshly-built outputs on any conflict.
          # --autostash: parity with daily.yml (#1823 fix); prevents an "unstaged changes"
          # rebase halt when builders leave stray tracked writes outside staged paths
          # (e.g. reports/*.md graders, site/qledger/) — without it, the push dies on
          # every attempt and the weekly commit is silently discarded.
          PUSH_BUDGET_SECS=600   # the weekly data day exists only on this runner (job timeout 300 min — ample headroom)
          PUSH_MAX_ATTEMPTS=20   # let the 600s DEADLINE stop this loop, not the attempt count: 10 contention
                                 # retries burn only ~2 min, so the raised budget above would never bind
          push_retry_init "weekly outputs"
          while push_attempt; do
            # pre-sync collision sweep (render.yml / #2252): a mid-run commit on origin/main can
            # TRACK a path this job wrote as an UNTRACKED side-effect — the rebase checkout then
            # refuses ("untracked working tree files would be overwritten") and --autostash can't
            # clear it (it parks tracked modifications only), so every retry dies identically.
            # Everything this step publishes is already committed above, so a path untracked HERE
            # but tracked on origin/main is a throwaway local write — origin/main is authoritative.
            # Gitignored files and untracked paths origin/main doesn't track never enter the
            # intersection (runner-local stores and caches are untouched).
            # (Asia paths unstaged above: when newly tracked on main, deleting the local stale
            # copy lets the rebase materialize asia-close's fresh commit — the W0b intent.)
            git fetch origin main || true
            comm -12 <(git ls-files --others --exclude-standard | LC_ALL=C sort) \
                     <(git ls-tree -r --name-only origin/main | LC_ALL=C sort) \
              | while IFS= read -r f; do rm -f -- "$f" || true; done
            # rename/rename on content-hashed site/assets/css/* is deterministic across
            # retries (path conflict; -X theirs can't settle it — 2026-07-22 asia-close
            # run 29904079071 dropped a day's outputs this way). The autoresolver keeps
            # both sides' hashed files (next externalize_css prunes the orphan) and
            # finishes the rebase; anything else is left for the abort+retry below.
            if { git pull --rebase --autostash -X theirs origin main || bash scripts/rebase_autoresolve_hashed_css.sh; } && push_autostash_ok; then
              if push_do; then echo "pushed weekly outputs on attempt $PUSH_ATTEMPT"; push_won; exit 0; fi
            fi
            push_abort_rebase
            push_backoff
          done
          push_lost
          echo "::error title=weekly outputs NOT pushed::$PUSH_ATTEMPT rebase/push attempts failed ($PUSH_STOP) — the weekly data/ outputs exist only on this runner and are lost at the next checkout (no Pages-artifact fallback anymore — the VPS/origin deploy only gets this render once it lands on main). Check the per-attempt [class] tags above: repeated [contention] means main is simply busier than this budget, repeated [rebase-conflict] means a NEW deterministic conflict class — see scripts/rebase_autoresolve_hashed_css.sh."
          echo "### ❌ weekly outputs NOT pushed after $PUSH_ATTEMPT attempts — weekly data day at risk (no Pages-artifact fallback anymore)" >> "$GITHUB_STEP_SUMMARY"

  # The `publish` GitHub Pages deploy job (needs: weekly-report, deploy-pages@v4) was
  # RETIRED pre-private-cutover (DEC:B1-MACRO-PRIVATE-CUTOVER) — the Pages mirror
  # served the premium book/premiumdata anonymously and must not keep re-publishing
  # once the repo goes private. The VPS/origin deploy (the site's real serving path)
  # is untouched; only the Pages producer went away.
