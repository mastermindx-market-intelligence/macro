name: metabolism-build
# Metabolism Phase V2-B — BUILD lane (write-serialization, R-V2-5).  INERT.
#
# Builds EVERY docket riding the selected propose branch — the base docket
# (<cycle>.json, til) plus per-lobe dockets (<cycle>-<lobe>.json, R-V6-5
# multi-lobe propose).  For each ADJUDICATE-granted proposal in each docket:
#   (a) CLAIMS its target_files → data/metabolism/claims.jsonl + regen ACTIVE_BUILD_MAP
#   (b) SKIPS if target_files already claimed this cycle (collision → later cycle)
#   (c) Creates a git worktree on metabolism/build-<lobe>-<cycle> off FRESH origin/main
#   (d) Dispatches a headless Sonnet build session (OAuth key from dispatcher)
#       — IN THIS PR the dispatch is a DOCUMENTED STUB: lane wiring ships here;
#         the live LLM call fires only when armed (AUTONOMY_PAUSED=false + operator grant)
#   (e) Opens a DRAFT PR (never merges)
#   (f) if:always() GCs the worktree (metabolism_gc)
#
# KILL SWITCH (R-AUT-2):
#   gh variable set AUTONOMY_PAUSED --body true    # pause (default-safe)
#   gh variable set AUTONOMY_PAUSED --body false   # arm (operator-only)
# Absent OR anything other than the exact string 'false' → clean no-op.
#
# INERTNESS GUARANTEE:
#   - Double-gated: `if: vars.AUTONOMY_PAUSED != 'true'` AND shell re-check.
#   - Opens DRAFT PRs only — the whole job no-ops when paused.
#   - NO scored-surface writes; any scored-surface change requires T2 metabolism_live flag.
#   - NO autonomous merge (that lives exclusively in metabolism-merge.yml).

on:
  # FIX-1 (BLOCKER-1): auto-chain BUILD after adjudicate (10:15).  45 10 * * * = 10:45 UTC.
  # When running on schedule, cycle_id input is empty; the discovery step below derives
  # the most-recently-adjudicated propose branch exactly as adjudicate.yml does.
  # metabolism_build.py is idempotent via journal claims, so re-running on an already-built
  # cycle is a safe no-op.
  schedule:
    - cron: "45 10 * * *"
  workflow_dispatch:
    inputs:
      cycle_id:
        description: "Cycle id whose docket to build (leave blank on schedule — auto-discovered)"
        type: string
        required: false
        default: ""
      docket_branch:
        description: "Branch carrying the docket (default: metabolism/propose-<cycle_id>)"
        type: string
        default: ""
      force_run:
        description: "Force even when AUTONOMY_PAUSED=true (ops verification only)"
        type: boolean
        default: false

permissions:
  contents: write        # commit claims.jsonl + worktree branches
  pull-requests: write   # open DRAFT PRs

concurrency:
  # Schedule runs land in the same group as each other (single active slot); dispatch
  # is per-cycle so manual re-runs never queue-cancel an in-flight scheduled run.
  group: metabolism-build-${{ github.event_name == 'schedule' && 'scheduled' || github.event.inputs.cycle_id }}
  cancel-in-progress: false

jobs:
  build:
    # KILL SWITCH — absent variable → treated as paused (default-safe).
    if: vars.AUTONOMY_PAUSED != 'true'
    runs-on: [self-hosted, macstudio]
    timeout-minutes: 60

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: python 3.12 (Homebrew venv - self-hosted runner)
        run: |
          VENV="$HOME/.cache/mm-venv-${RUNNER_NAME:-default}"
          [ -x "$VENV/bin/python" ] || /opt/homebrew/bin/python3.12 -m venv "$VENV"
          echo "$VENV/bin" >> "$GITHUB_PATH"
          "$VENV/bin/python" -m pip install --quiet --upgrade pip

      - name: install deps
        run: pip install --quiet pyyaml anthropic

      - name: BUILD (claim + worktree + dispatch-stub + draft PR + GC)
        if: always()
        env:
          AUTONOMY_PAUSED: ${{ vars.AUTONOMY_PAUSED }}
          METAB_INTENSITY: ${{ vars.METAB_INTENSITY }}
          METAB_KEYS_ENABLED: ${{ vars.METAB_KEYS_ENABLED }}
          FORCE_RUN: ${{ github.event.inputs.force_run }}
          CYCLE_ID: ${{ github.event.inputs.cycle_id }}
          DOCKET_BRANCH_INPUT: ${{ github.event.inputs.docket_branch }}
          # OAuth pool (capability_ids only — dispatcher reads env-var NAMES)
          # Keys can be added one at a time; absent secrets resolve to empty env
          # = "not present" — presence-based discovery handles partial sets with
          # no code change required (pool comment in engine/neuralweb/key_pool.py).
          CLAUDE_CODE_OAUTH_TOKEN_1: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_1 }}
          CLAUDE_CODE_OAUTH_TOKEN_2: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_2 }}
          CLAUDE_CODE_OAUTH_TOKEN_3: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_3 }}
          CLAUDE_CODE_OAUTH_TOKEN_4: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_4 }}
          CLAUDE_CODE_OAUTH_TOKEN_5: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_5 }}
          CLAUDE_CODE_OAUTH_TOKEN_6: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_6 }}
          CLAUDE_CODE_OAUTH_TOKEN_7: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_7 }}
          # Fallback single-key (V2-A compat)
          CLAUDE_CODE_OAUTH_TOKEN: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
          AI_COSTS_SHARD: "${{ github.workflow }}-${{ github.run_id }}-${{ github.run_attempt }}"
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          # FIX-5 (MINOR-11): prefer METABOLISM_MERGE_PAT for draft PR creation so the
          # resulting PR triggers pull_request CI workflows.  GITHUB_TOKEN-created PRs are
          # exempt from the anti-recursion rule ONLY for the creator, not for CI checks —
          # so GITHUB_TOKEN PRs never get required status checks, which is fatal once
          # required checks are enforced on main.  A PAT with no-workflows scope fixes
          # this while keeping the loop unable to edit CI files.
          GH_TOKEN: ${{ secrets.METABOLISM_MERGE_PAT || secrets.GITHUB_TOKEN }}
          GITHUB_RUN_ID: ${{ github.run_id }}
          GITHUB_WORKFLOW: ${{ github.workflow }}
          GITHUB_SHA: ${{ github.sha }}
          GITHUB_ACTOR: ${{ github.actor }}
        run: |
          set -euo pipefail
          . "${GITHUB_WORKSPACE:-.}/scripts/ci/push_retry.sh"

          # Double-gate: runner-side AUTONOMY_PAUSED re-check (defends against race).
          AP="$(printf '%s' "${AUTONOMY_PAUSED:-}" | tr '[:upper:]' '[:lower:]')"
          FR="$(printf '%s' "${FORCE_RUN:-false}" | tr '[:upper:]' '[:lower:]')"
          if [ "$AP" != "false" ] && [ "$FR" != "true" ]; then
            echo "AUTONOMY_PAUSED not 'false' (=${AUTONOMY_PAUSED:-<unset>}) — clean no-op."
            exit 0
          fi

          # FIX-1: When running on schedule (cycle_id input is empty), discover the propose
          # branch to build.  The build script is idempotent via journal claims, so
          # re-running on an already-built cycle is a safe no-op.
          # R-V9-9: the pick is attention-aware — within the newest cycle date, dockets
          # for FOCUS lobes dispatch first (rank_cycle_ids falls back to the pre-V9
          # lexicographic-last pick on any error, so this can never block the lane).
          if [ -z "${CYCLE_ID:-}" ]; then
            CYCLE_ID="$(git ls-remote --heads origin 'refs/heads/metabolism/propose-*' \
              | awk '{print $2}' | sed 's|refs/heads/metabolism/propose-||' \
              | python -c 'import sys; sys.path.insert(0, "."); from engine.metabolism.attention import rank_cycle_ids; ids = [line.strip() for line in sys.stdin if line.strip()]; ranked = rank_cycle_ids(ids); print(ranked[0] if ranked else "")')"
            if [ -z "$CYCLE_ID" ]; then
              echo "no open propose branch found — clean no-op (schedule)."
              exit 0
            fi
            echo "schedule: discovered cycle_id=$CYCLE_ID (attention-ranked)"
          fi

          BRANCH="${DOCKET_BRANCH_INPUT:-metabolism/propose-$CYCLE_ID}"
          echo "cycle_id=$CYCLE_ID docket_branch=$BRANCH"

          # Check out the docket branch
          git fetch origin "$BRANCH" || echo "branch $BRANCH not on origin — using current ref"
          git checkout "$BRANCH" 2>/dev/null || git checkout -B "$BRANCH" "origin/$BRANCH" 2>/dev/null || true

          # R-V6-5 multi-lobe PROPOSE puts ALL of a cycle's dockets on ONE branch:
          # the base docket ($CYCLE_ID.json, til) plus per-lobe dockets
          # ($CYCLE_ID-<lobe>.json).  Enumerate every one (#2481 primitive) — the
          # branch name carries only the base id, so building $CYCLE_ID.json alone
          # silently strands ADJUDICATE-authorized per-lobe proposals at BUILD.
          # Fallback: a propose branch cut before the primitive merged lacks
          # --list-dockets on its checkout — fall back to the base docket
          # (pre-fan-out behavior) instead of stranding the whole branch.
          _DOCKETS="$(python -m scripts.metabolism_adjudicate --list-dockets --cycle-id "$CYCLE_ID")" \
            || _DOCKETS="data/metabolism/dockets/$CYCLE_ID.json"
          if [ -z "$_DOCKETS" ]; then
            echo "no dockets for $CYCLE_ID on this ref — clean no-op."
            exit 0
          fi
          echo "dockets for $CYCLE_ID: $(echo "$_DOCKETS" | tr '\n' ' ')"

          git config user.name "dashboard-bot"
          git config user.email "actions@users.noreply.github.com"

          # Run BUILD lane per docket.  Per-docket cycle id = docket filename stem
          # (journal, claims, budget, and governance rows are keyed on it by
          # PROPOSE/ADJUDICATE).  Per-docket failure isolation: one bad docket
          # never strands its siblings; journal/claims writes from every docket
          # are committed together below (durability, #2295).
          _built_ok=0
          while IFS= read -r _DOCKET; do
            [ -n "$_DOCKET" ] && [ -f "$_DOCKET" ] || continue
            _DCID="$(basename "$_DOCKET" .json)"
            echo "--- build docket cycle_id=$_DCID file=$_DOCKET ---"
            python -m scripts.metabolism_build \
              --cycle-id "$_DCID" \
              --docket-file "$_DOCKET" \
              || { echo "build lane failed for $_DCID — skip docket"; continue; }
            _built_ok=$((_built_ok + 1))
          done <<< "$_DOCKETS"
          echo "build lane done for $_built_ok docket(s) on $CYCLE_ID"

          # Commit any new claims.jsonl rows + ACTIVE_BUILD_MAP updates.
          # FIX-4: key_ledger.jsonl included — key pool durability (no prior workflow committed it).
          # R-V5-1/2 durability (#2295 review F1): the journal carries the
          # _failed_attempts retry counter + running/failed markers — without
          # committing it, every fresh checkout resets the counter and the
          # park/re-attempt mechanism is dead code.
          git add data/ai_costs/usage.d data/metabolism/claims.jsonl data/metabolism/key_ledger.jsonl data/metabolism/journal docs/ACTIVE_BUILD_MAP.md 2>/dev/null || true
          if ! git diff --cached --quiet; then
            git commit -m "metabolism: BUILD claims + ACTIVE_BUILD_MAP $CYCLE_ID ($_built_ok docket(s)) [Phase V2-B]" \
                       -m "Loop-Authored: build run=${GITHUB_RUN_ID:-local}"
            # pre-sync collision sweep (render.yml / #2252): a mid-run commit on origin/$BRANCH can
            # TRACK a path this job wrote as an UNTRACKED side-effect — the rebase checkout then
            # refuses ("untracked working tree files would be overwritten") and --autostash can't
            # clear it (it parks tracked modifications only), so every retry dies identically.
            # Everything this step publishes is already committed above, so a path untracked HERE
            # but tracked on origin/$BRANCH is a throwaway local write — origin/$BRANCH is authoritative.
            # Gitignored files and untracked paths origin/$BRANCH doesn't track never enter the
            # intersection (runner-local stores and caches are untouched).
            git fetch origin "$BRANCH" || true
            comm -12 <(git ls-files --others --exclude-standard | LC_ALL=C sort) \
                     <(git ls-tree -r --name-only "origin/$BRANCH" 2>/dev/null | LC_ALL=C sort) \
              | while IFS= read -r f; do rm -f -- "$f" || true; done
            { git pull --rebase --autostash origin "$BRANCH" 2>/dev/null && push_autostash_ok; } || true
            git push origin HEAD:"$BRANCH"
          else
            echo "no claim changes staged — no commit needed."
          fi

      - name: GC orphan worktrees (if:always)
        if: always()
        run: |
          python -m scripts.metabolism_gc || echo "GC completed (any errors above are non-fatal)"

      - name: narrow-commit journal to main (achievements observability)
        # The BUILD step above commits journal entries to the propose branch only;
        # achievements observability reads journals from main.  Push journal dir to main
        # so build outcomes are visible without waiting for a propose-branch merge.
        # FIX-1 (BLOCKER): save journals BEFORE checkout so the branch-switch does not
        # revert the working tree and lose them; restore from $RUNNER_TEMP after switching
        # to main.  Rebase-tolerant; [skip ci].  Fail-soft: non-fatal push race.
        if: always()
        continue-on-error: true
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -uo pipefail
          . "${GITHUB_WORKSPACE:-.}/scripts/ci/push_retry.sh"
          git config user.name "dashboard-bot" || true
          git config user.email "actions@users.noreply.github.com" || true
          # (a) Save journals BEFORE any branch switch.
          _JSAVE="$RUNNER_TEMP/journal-save-build"
          rm -rf "$_JSAVE" || true
          mkdir -p "$_JSAVE" || true
          if [ -d data/metabolism/journal ]; then
            cp -f data/metabolism/journal/*.json "$_JSAVE/" 2>/dev/null || true
          fi
          # (b) Switch to a fresh main (never bare 'main' — avoid clobbering local ref).
          git fetch origin main || true
          git checkout -B _journal_main origin/main 2>/dev/null || git checkout -B _journal_main origin/main || true
          # (b2) The re-anchor above is the ONLY thing that makes this step safe to
          # dispatch over a propose branch, and it is suffixed `|| true` — a failed
          # checkout leaves HEAD on metabolism/propose-<id> and the push below would
          # then land the whole unreviewed proposal on main. Naming _journal_main as
          # the sole acceptable publish target makes that failure fail closed.
          PUSH_LABEL="metabolism-build journal"
          PUSH_MAIN_BRANCHES="_journal_main"
          if ! push_on_main_ok; then exit 0; fi
          # (c) Restore saved journals.
          mkdir -p data/metabolism/journal || true
          cp -f "$_JSAVE"/*.json data/metabolism/journal/ 2>/dev/null || true
          # (d) Commit + push (tolerate no-change).
          git add data/ai_costs/usage.d data/metabolism/journal/ 2>/dev/null || true
          if git diff --cached --quiet; then
            echo "no journal changes to push to main — skip."
            exit 0
          fi
          git commit -m "metabolism: journal build outcomes snapshot [skip ci]" \
                     -m "Loop-Authored: build run=${GITHUB_RUN_ID:-local}" || true
          for _i in 1 2 3; do
            git fetch origin main || true
            comm -12 <(git ls-files --others --exclude-standard | LC_ALL=C sort) \
                     <(git ls-tree -r --name-only origin/main | LC_ALL=C sort) \
              | while IFS= read -r f; do rm -f -- "$f" || true; done
            if git pull --rebase --autostash origin main && push_autostash_ok && git push origin HEAD:main; then
              echo "journal pushed to main on attempt $_i"
              exit 0
            fi
            git rebase --abort 2>/dev/null || true
            echo "journal push attempt $_i lost race; re-syncing"
            sleep $((_i * 5))
          done
          echo "::warning::could not push journal to main after 3 attempts (non-fatal)"
          exit 0

      - name: notify on failure
        if: failure()
        env:
          TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          RUN_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
        run: |
          [ -n "$TOKEN" ] || exit 0
          curl -s -X POST "https://api.telegram.org/bot${TOKEN}/sendMessage" \
            -d chat_id="$CHAT_ID" \
            -d text="[Metabolism] metabolism-build run failed: ${RUN_URL}" || true
