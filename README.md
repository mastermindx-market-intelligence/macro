# China Risk Radar PR #7029 render proof transport

Disposable proof-only branch. It downloads an immutable, tree-bound candidate archive, executes the real China producer on Render, rejects silent no-op/fatal output, and serves only the resulting proof surface. It is not an implementation carrier and contains no product source changes.
