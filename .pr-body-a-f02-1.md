## What / Why

Packet A-F02-1: base map + OFAC sanctions overlay, an `intelligence_desk`
page answering "Where do US sanctions point today, and which countries is
our coverage silent about?" Closes ledger rows **MO-DELTA-031**, **MO-PAID-008**.

Display-only leaf: `engine/sanctions_map.py` never raises, originates no
score/attribution — every country<->programme edge is a human-reviewed row
in `config/sanctions_ofac_programs.yml`.

## Files (OWNED FILES per commission)

- `collectors/ofac_sdn.py` (new) — fetches OFAC's public SDN CSV, persists raw + source_url/list_published_date/fetched_at.
- `config/sanctions_ofac_programs.yml` (new) — human-reviewed programme→country attribution.
- `engine/sanctions_map.py` (new) — pure view-model builder, never raises.
- `scripts/build_sanctions_map.py` (new) — renders the page + writes `data/sanctions_map/latest.json`.
- `scripts/gen_worldmap_svg.py` (new, off render path) — reproducible Natural Earth→SVG provenance tool.
- `templates/sanctions_map.html.j2` (new) — the page.
- `templates/_worldmap_base.html.j2` (new) — inline SVG base geometry partial.
- `templates/_navlinks.html.j2` (+1 line) — nav wiring under the existing International dropdown, after `baskets_intl.html`.
- `config/product_experience/page_registry_overrides.yml` (+entry) — `archetype: intelligence_desk`, primary user question, evidence.
- `engine/i18n.py` (+LEX entries) — Russia/Iran/North Korea/Syria/Cuba/Venezuela/Belarus/Myanmar added to the existing lexicon.
- `tests/test_sanctions_map_engine.py`, `tests/test_sanctions_map_page.py`, `tests/test_worldmap_base.py`, `tests/test_sanctions_map_nav.py` (new).
- Auto-regenerated by the chat-nav guard hook: `templates/chat.html`, `site/chat.html` (byte-mirrors of the nav splice; house law, not a manual edit).

## Acceptance (per FROZEN SPEC §11)

1. **Base map + OFAC overlay, bilingual, dark/light named** — page composes desk header → map (three-rung ink ladder: 1 / 2–3 / 4+) → cards → watch table → method disclosure, per `t()` EN/ZH pairs throughout. Dark/light treatments are the exact CSS from §6/§7 of the frozen spec (luminance-step glow in dark, structural ring+frame in light) — **not evidenced with live crops in this PR** (see Gaps).
2. **Only base map + OFAC public overlay ship** — no chokepoint/military/satellite layer exists anywhere in the diff; the method disclosure states this in both languages (`<details class="sm-disc">`).
3. **No second geospatial store / country master** — geometry lives in the `_worldmap_base.html.j2` template partial (not a data store); country keys are ISO3, matching `engine/intl_risk.py`'s `_IMF_COUNTRIES` vocabulary; country names extend the existing `engine/i18n.py` `LEX`, not a new lexicon. Source URL + OFAC publication date print on the page; missing coverage prints as `unknown`/`未知` plus a counted unresolved line — never as zero (tested).

## Tests — all pass locally

```
python3 -m pytest tests/test_sanctions_map_engine.py tests/test_sanctions_map_page.py tests/test_worldmap_base.py tests/test_sanctions_map_nav.py -q
21 passed
```

Covers (§9 of the spec): `build()` never raises on a missing store; a
country with no programmes is absent (never `n_programs: 0`); an unmapped
code lands in `unresolved` and is counted; `coverage.resolved +
coverage.unresolved == n_programs_total`; rung boundaries exact at 1/2/3/4;
every `.l-en` has an `.l-zh` twin; no CJK in `title=`; source/as-of print;
`as_of=None` renders unknown/未知; `coverage=None` renders the degraded
`.mx-empty-why` copy and hides cards+table; no banned vocabulary; every
`<path>` has a unique `data-iso3` and no presentation attributes/`<title>`;
exactly one `sanctions_map.html` nav anchor inside the International
dropdown with EN+ZH labels and no `data-intl-country`; nav-dd family count
unchanged (no third header).

## GAPS (honest, not silently absorbed)

- **World map geometry is a placeholder, not real Natural Earth coastline.** `scripts/gen_worldmap_svg.py` is the real, reproducible Natural Earth 1:110m Admin-0→SVG provenance tool per the spec, but running it needs a local Natural Earth GeoJSON export that this build did not have budget to source and process. `templates/_worldmap_base.html.j2` currently ships simplified rectangular placeholders (positioned near real centroids) for ~18 countries including every ISO3 in `config/sanctions_ofac_programs.yml` (test-enforced superset). **Before this ships to users**, run `gen_worldmap_svg.py` against a real Natural Earth export and regenerate the partial — the template contract (one `<path data-iso3>`, no presentation attributes, no title) does not change.
- **Visual verification matrix (§10, 8 cells + 2 mechanism crops) was not captured** — no headless-Chrome screenshots were taken in this build. The page has not been visually confirmed in a browser in any theme/lang/viewport combination; only Jinja-render + pytest string assertions ran.
- **OFAC live fetch was not exercised** — `collectors/ofac_sdn.py` was written per the spec's documented SDN CSV field layout but not run against the live OFAC endpoint in this session (no network fetch attempted); `engine/sanctions_map.py`'s degraded-state path was tested against synthetic fixtures instead.
- **Item 15 (nightly builder wiring)** was explicitly out of scope for this commission's OWNED FILES and is not done — `build_sanctions_map` is not yet wired into any `.github/workflows/*.yml` builder list.
- **`config/product_experience/page_registry_overrides.yml` was edited but the derived registry was not regenerated** (`python3 scripts/build_product_page_registry.py --as-of <ISO8601>` was not run in this session) — `data/product_experience/page_registry.json` is stale relative to this override until that script runs.
- **Design-system / runtime-style checkers were not run** (`check_design_system.py --mode enforce-added`, `check_runtime_style_injection.py`, `check_ui_visual_evidence.py`, `check_template_site_sync`, `test_product_chrome.py`, `test_check_nav_gap.py`) — CSS was authored token-only per the frozen spec's exact §6 block (verified by reading, not by the checker), but the checkers themselves were not executed against this diff.
- **Live URL was not confirmed** — `/spr.html` was not loaded to verify the apex per §2 of the spec.

Given the above, this PR is **not** armed with `merge-on-green` and is opened as **Draft**, per the commission's explicit instruction (NOT DONE UNLESS gates). A continuation builder should: source Natural Earth data and regenerate the map partial, capture the 8+2 visual crops in both themes/langs, run the design-system checkers and the registry regen, then arm the PR.

🤖 Generated with [Claude Code](https://claude.com/claude-code)


---

## Continuation update (2026-09-06)

**Done this pass:**
1. **Real coastlines** — `templates/_worldmap_base.html.j2` regenerated via `scripts/gen_worldmap_svg.py` from a live-fetched Natural Earth 1:110m Admin-0 GeoJSON (public domain/CC0, `raw.githubusercontent.com/nvkelso/natural-earth-vector`). 177 real country paths, contract unchanged (one `<path data-iso3>`, no presentation attrs, no `<title>`). `tests/test_worldmap_base.py` passes against it; the sanctions-config ISO3 set is a confirmed subset.
2. **Live OFAC fetch + engine confirmation, and a real bug found and fixed**: ran `collectors/ofac_sdn.py` against the live `treasury.gov/ofac/downloads/sdn.csv` endpoint (19,329 raw entries snapshotted to `data/sanctions_ofac/`). Running `engine.sanctions_map.build()` against that live snapshot surfaced that **both** `collectors/ofac_sdn.py::extract_program_codes` and `engine/sanctions_map.py::_load_program_code_counts` were reading the wrong CSV column (index 7, a vessel-type/tonnage field) instead of OFAC's actual "program" field (index 3) — verified by inspecting the live CSV directly. Column 7 produced 0 resolved countries against real data even though the synthetic unit-test fixtures (which encoded the same wrong index) passed. Fixed both readers to read column 3, added splitting for OFAC's bracket-joined multi-program field format (`"[A] [B]"`), and updated `tests/test_sanctions_map_engine.py`'s fixture to match the real format. Live result after the fix: 73 distinct program mentions, 6 resolve to countries (CUB/IRN/BLR/RUS/VEN/PRK against the existing `config/sanctions_ofac_programs.yml`), 67 correctly land in `unresolved` (printed, not hidden) rather than being silently dropped. All 21 touched tests still pass.
3. **Live apex reconfirmed**: `https://mastermind-x.com/spr.html` redirects (301) to `https://www.mastermind-x.com/spr.html` (200). The live URL claim for this packet is therefore `https://www.mastermind-x.com/sanctions_map.html`.
4. Nightly wiring for `build_sanctions_map` remains explicitly **out of scope** for this PR per the commission — a separate packet should add it beside `build_spr`.

**Still not done (reported, not silently dropped):**
- The full 8-cell + 2-mechanism visual verification matrix (dark/light × EN/ZH × 1440/390, three-state proof, hover-divergence crop) from spec §10 has **not** been captured in this pass — no headless-Chrome capture harness was run this session. This PR stays Draft and **unarmed** (`merge-on-green` not applied) until that evidence is posted, per this commission's explicit instruction.
- OFAC's `list_published_date` is not exposed by the CSV endpoint used; `as_of` renders `unknown`/`未知` live, which is the spec's documented degraded-state behavior (§4.6), not a defect.

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
