{% macro help(en, zh='') -%}
<span class="help">?<span class="tip"><span class="l-en">{{ en }}</span><span class="l-zh">{{ zh if zh else en }}</span></span></span>
{%- endmacro %}
{% macro t(en, zh='') -%}
<span class="l-en">{{ en }}</span><span class="l-zh">{{ zh if zh else en }}</span>
{%- endmacro %}
<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<title>{{ s.name }} ({{ s.fund }}) — cycle & holdings</title>
<script>try{var t=localStorage.getItem('theme');document.documentElement.setAttribute('data-theme',t||'dark');var l=localStorage.getItem('lang')||(function(){try{var L=navigator.languages||[navigator.language||navigator.userLanguage||''],i;for(i=0;i<L.length;i++)if((L[i]||'').toLowerCase().slice(0,2)==='zh')return'zh';if(/Shanghai|Hong_Kong|Macau|Urumqi|Chongqing|Harbin|Kashgar|Chungking/.test(Intl.DateTimeFormat().resolvedOptions().timeZone||''))return'zh';}catch(e){}return'';})();if(l)document.documentElement.setAttribute('data-lang',l);}catch(e){}</script>
<link rel="stylesheet" href="../theme.css">
<style>
  * { box-sizing:border-box; }
  body { background:var(--bg); color:var(--text); font:14px/1.5 -apple-system,
         "Segoe UI",Roboto,Helvetica,sans-serif; margin:0; padding:18px; }
  @media (min-width:1100px){ body{ padding:26px max(40px,calc((100vw - 1200px)/2)); } }
  .early { margin:6px 0; padding:8px 12px; border-radius:8px;
           background:color-mix(in srgb, var(--g-cold) 14%, var(--panel2));
           border-left:3px solid var(--g-cold); font-size:13px; }
  .macro-caution { margin:6px 0; padding:8px 12px; border-radius:8px;
           background:color-mix(in srgb, var(--down) 10%, var(--panel2));
           border-left:3px solid var(--down); font-size:13px; }
  .shock-deesc-banner { margin:10px 0; padding:10px 14px; border-radius:8px;
           background:color-mix(in srgb, var(--warn) 15%, var(--panel2));
           border-left:4px solid var(--warn); font-size:13px; font-weight:600; }
  .agewhen { margin:6px 0; padding:7px 11px; border-radius:8px; background:var(--panel2);
             border-left:3px solid var(--link); font-size:13px; color:var(--text); }
  .agechip { font-size:11px; font-weight:700; padding:2px 7px; border-radius:9px;
             background:var(--panel); color:var(--muted); border:1px solid var(--line);
             white-space:nowrap; }
  .eqbadge { display:inline-flex; align-items:center; gap:5px; padding:3px 10px;
             border-radius:11px; font-size:12.5px; font-weight:700; white-space:nowrap;
             border:1px solid var(--line); }
  .eqbadge.sm { font-size:11px; padding:2px 7px; border-radius:9px; }
  .eqbadge .eqg { font-weight:500; opacity:.8; }
  .eqbadge.eq-up { background:color-mix(in srgb,var(--up) 16%,transparent); color:var(--ink-up, var(--up));
                   border-color:color-mix(in srgb,var(--up) 40%,transparent); }
  .eqbadge.eq-down { background:color-mix(in srgb,var(--down) 16%,transparent); color:var(--ink-down, var(--down));
                     border-color:color-mix(in srgb,var(--down) 40%,transparent); }
  .eqbadge.eq-flat { background:var(--panel2); color:var(--muted); }
  .alpha-entry { font-size:11px; font-weight:700; padding:2px 8px; border-radius:9px;
                 text-transform:capitalize; border:1px solid var(--line); white-space:nowrap; }
  .ae-pullback { background:color-mix(in srgb,var(--up) 16%,transparent); color:var(--ink-up, var(--up));
                 border-color:color-mix(in srgb,var(--up) 40%,transparent); }
  .ae-extended { background:color-mix(in srgb,var(--warn) 22%,transparent); color:var(--text);
                 border-color:color-mix(in srgb,var(--warn) 55%,transparent); }
  .ae-intact { background:color-mix(in srgb,var(--link) 14%,transparent); color:var(--ink-link, var(--link)); }
  .ae-laggard,.ae-neutral { background:var(--panel); color:var(--muted); }
  /* entry call bar + urgency colors */
  .entrybar { display:flex; gap:10px; align-items:flex-start; margin:10px 0 8px;
              padding:9px 12px; border-radius:9px; background:var(--panel2);
              border-left:4px solid var(--muted); font-size:14px; }
  .entrytag { font-weight:800; font-size:12px; letter-spacing:.04em; white-space:nowrap;
              padding:3px 9px; border-radius:7px; }
  .urgpill { font-weight:700; font-size:11px; padding:2px 8px; border-radius:9px; }
  .urg-now .entrytag, .urgpill.urg-now { background:var(--up); color:#fff; }
  .urg-now { border-left-color:var(--up); }
  .urg-imminent .entrytag, .urgpill.urg-imminent { background:var(--g-cold); color:#fff; }
  .urg-imminent { border-left-color:var(--g-cold); }
  .urg-soon .entrytag, .urgpill.urg-soon { background:color-mix(in srgb,var(--g-cold) 30%,var(--panel)); color:var(--text); }
  .urg-soon { border-left-color:var(--g-cold); }
  .urg-hold .entrytag, .urgpill.urg-hold { background:var(--panel); color:var(--muted); border:1px solid var(--line); }
  .urg-hold { border-left-color:var(--muted); }
  .urg-later .entrytag, .urgpill.urg-later { background:var(--panel); color:var(--muted); border:1px solid var(--line); }
  .urg-later { border-left-color:var(--muted); }
  .urg-caution .entrytag, .urgpill.urg-caution { background:var(--warn); color:#1a1a1a; }
  .urg-caution { border-left-color:var(--warn); }
  .urg-exit .entrytag, .urgpill.urg-exit { background:var(--down); color:#fff; }
  .urg-exit { border-left-color:var(--down); }
  .urg-avoid .entrytag, .urgpill.urg-avoid { background:color-mix(in srgb,var(--down) 30%,var(--panel)); color:var(--text); }
  .urg-avoid { border-left-color:var(--down); }
  /* glance-level two-timeframe synthesis sentence, sits right under the verdict */
  .synthesis { margin:8px 0 4px; padding:9px 13px; border-radius:9px; font-size:13.5px; line-height:1.5;
               background:color-mix(in srgb, var(--link) 9%, var(--panel2)); border-left:3px solid var(--link); }
  /* context-indicator chip: factual extreme, NOT an alarm — neutral so it can't compete with the verdict */
  .sigchip.ctx { border-color:var(--line); }
  .sigchip.ctx .sc-v, .sigchip.ctx .sc-s { color:var(--muted); }
  .sigstrip-label { font-size:11px; color:var(--muted); text-transform:uppercase; letter-spacing:.05em; margin:8px 0 4px; }
  .points { margin:6px 0; padding-left:18px; } .points li { margin:2px 0; }
  .points.sm { font-size:12.5px; }
  .cyclebox { margin:8px 0; padding:9px 12px; border-radius:9px; background:var(--panel2);
              font-size:13px; line-height:1.6; }
  a { text-decoration:none; }
  h1 { font-size:20px; margin:0; }
  h2 { font-size:14px; margin:0 0 10px; color:var(--muted); text-transform:uppercase;
       letter-spacing:.06em; }
  .panel { background:var(--panel); border:1px solid var(--line); border-radius:10px;
           padding:14px 16px; margin-bottom:14px; }
  .topline { display:flex; flex-wrap:wrap; align-items:center; gap:12px; }
  .muted { color:var(--muted); }
  .state { display:inline-block; padding:2px 11px; border-radius:12px; font-weight:700;
           font-size:12.5px; }
  .card { background:var(--panel2); border:1px solid var(--line); border-radius:10px;
          padding:12px 14px; margin:10px 0; }
  .help { display:inline-flex; align-items:center; justify-content:center; width:15px;
          height:15px; border-radius:50%; border:1px solid var(--muted); color:var(--muted);
          font-size:10px; font-weight:600; cursor:help; position:relative;
          vertical-align:middle; margin-left:5px; }
  .help .tip { display:none; position:absolute; top:140%; left:50%;
               transform:translateX(-50%); width:300px; background:var(--panel);
               border:1px solid var(--line); padding:9px 11px; border-radius:8px; z-index:60;
               font-size:12px; line-height:1.45; color:var(--text); font-weight:400;
               box-shadow:var(--popover-shadow); pointer-events:auto; cursor:auto;
               max-height:min(60vh,420px); overflow-y:auto; overscroll-behavior:contain; }
  /* invisible hover pad across the icon→tip gap — keeps :hover alive mid-travel (on .help, not .tip: the tip is a scroll container and clips its pseudos) */
  .help:hover::before { content:''; position:absolute; top:100%; left:-30px; right:-30px; height:10px; }
  .help:hover .tip { display:block; }
  table { border-collapse:collapse; width:100%; font-size:13px; }
  th,td { text-align:left; padding:4px 8px; border-bottom:1px solid var(--line); }
  th { color:var(--muted); font-weight:500; }
  .tv { margin-top:8px; overflow:hidden; }
  details summary { cursor:pointer; color:var(--ink-link, var(--link)); font-size:12.5px; margin-top:6px; }
</style>
</head>
<body>
{# NOTE: sector pages render under site/sectors/, one level deep — every nav link
   MUST be ../-prefixed (like ../theme.css above), or it 404s as /sectors/<page>.
   The set line below is what delivers that: _site_nav/_navlinks read nav_prefix from
   the including context. #4228's 12-variants→1 conversion dropped it here (alone among
   the four subdir templates), which broke all ~85 nav links on the 11 sector pages and
   wedged render.yml's dead-reference guard (run 30766686540, 2026-08-02). #}
{% set nav_prefix = '../' %}
{% include "_site_nav.html.j2" %}
{# ---- Policy-Shock de-escalation banner (PS-R3: display de-escalation only — no reordering, no sizing) ---- #}
{% if shock_state is defined and shock_state and shock_state.get('active') %}
<div class="shock-deesc-banner">⚠ <span class="l-en">SHOCK — ranks computed on pre-shock data; discount until {{ shock_state.get('expires', '') }}.</span><span class="l-zh">冲击 — 排名依据冲击前数据计算；有效期至 {{ shock_state.get('expires', '') }}，届前请酌情折扣。</span></div>
{% endif %}
  <div class="panel">
    <div class="topline">
      <h1>{{ td(s.name) }} <span class="muted">({{ s.fund }})</span></h1>
      <span class="state st-{{ s.ladder.state | replace(" ", "_") }}">{{ td(s.ladder.label) }}</span>
      {% if s.ladder.eq_badge %}<span class="eqbadge eq-{{ s.ladder.eq_dir }}">{{ t('Entry','入场') }} {{ s.ladder.eq_badge }} <span class="eqg">{{ td(s.ladder.eq_grade) }}</span></span>{{ help(s.ladder.eq_tip, s.ladder.eq_tip_zh) }}{% endif %}
      <span style="flex:1"></span>
      <span class="af-wrap">
        <button class="af-toggle" id="af-btn" type="button" aria-label="Toggle alert feed" aria-expanded="false" aria-controls="af-feed">🔔 <span>{{ t('Alerts','警报') }}</span>{% if alerts and alerts.n_recent %}<span class="af-badge">{{ alerts.n_recent }}</span>{% endif %} <span class="af-caret">▾</span></button>
        <div class="af-scrim" id="af-scrim"></div>
        <div class="af" id="af-feed" role="dialog" aria-label="Alerts" aria-hidden="true">
          <div class="af-bar"><span class="af-bar-t">🔔 {{ t('Alerts','警报') }}</span>
            <button class="af-x" id="af-close" type="button" aria-label="Close">✕</button></div>
          <div class="af-body">
            {% if alerts and alerts.pinned %}{% set p = alerts.pinned %}
            <div class="af-pin dir-{{ p.dir }}">
              <div class="af-pin-h"><span class="af-pin-tag">{{ t('Current signal','当前信号') }}</span>
                <span class="af-pin-title">{{ t(p.headline|e, p.headline_zh|e) }}</span></div>
              <p class="af-pin-d">{{ t(p.detail|e, p.detail_zh|e) }}</p>
            </div>{% endif %}
            {% if alerts and alerts.timeline %}
            <div class="af-filters">
              {% set flz = {'All':'全部','Signal':'信号','Trend':'趋势','Momentum':'动量','vs Market':'相对强弱','Cycle':'周期'} %}
              {% for f,lab in [('all','All'),('signal','Signal'),('trend','Trend'),('momentum','Momentum'),('strength','vs Market'),('cycle','Cycle')] %}
              <button class="aff {{ 'active' if f=='all' else '' }}" type="button" data-f="{{ f }}">{{ t(lab, flz[lab]) }}</button>{% endfor %}
            </div>
            {% for day in alerts.timeline %}
            <div class="af-day">
              <div class="af-date">{{ t(day.daylabel, day.daylabel_zh) }}</div>
              {% for ev in day.events %}
              <details class="af-item{{ ' pinned' if ev.pinned else '' }}" data-f="{{ ev.filter }}">
                <summary>
                  <span class="af-node dir-{{ ev.dir }}"></span>
                  <span class="af-chip f-{{ ev.filter }}">{{ t(ev.label, ev.label_zh) }}</span>
                  <span class="af-head">{{ t(ev.headline|e, ev.headline_zh|e) }}</span>
                </summary>
                <div class="af-detail">{{ t(ev.detail|e, ev.detail_zh|e) }}
                  {% if ev.edge %}<div class="af-edge"><b>{{ t('Edge:','优势：') }}</b> {{ t(ev.edge|e, ev.edge_zh|e) }}</div>{% endif %}
                  {% if ev.forward %}<div class="af-fwd">{{ t(ev.forward|e, ev.forward_zh|e) }}</div>{% endif %}
                </div>
              </details>
              {% endfor %}
            </div>
            {% endfor %}
            {% else %}
            <div class="af-empty">{{ t('No signal changes in the recent window — quiet on every tracked signal.','近期无信号变化 — 所有跟踪信号均平静。') }}</div>
            {% endif %}
          </div>
        </div>
      </span>
    </div>
    {% set e = s.ladder.entry %}
    {# ---- bold VERDICT: the action verb + tag + call, read in a glance ---- #}
    <div class="verdict urg-{{ e.urgency }}">
      <span class="v-act">{{ td(s.ladder.action) }}</span>
      <span class="v-tag">{{ td(e.tag) }}</span>
      <span class="v-text">{{ t(e.text, e.text_zh) }}</span>
    </div>
    {# ---- glance-level SYNTHESIS: reconciles daily-vs-weekly so the context chips below
       can't read as a contradiction (emitted by the engine on the weekly-cross rescue) ---- #}
    {% if s.ladder.synthesis_line %}
    <p class="synthesis">🧭 {{ t(s.ladder.synthesis_line, s.ladder.synthesis_line_zh) }}</p>
    {% endif %}
    {# ---- at-a-glance signal-chip strip — CONTEXT only (already weighed into the verdict) ---- #}
    {% set tech = s.tech or {} %}{% set d = (s.mtf or {}).get('D') or {} %}
    <div class="sigstrip-label">{{ t('Indicators · context — already weighed into the verdict above', '指标 · 参考 — 已计入上方研判') }}</div>
    <div class="sigstrip">
      {% set ab = tech.above200 %}
      <div class="sigchip {{ 'up' if ab else 'down' }}">
        <span class="sc-k">{{ t('Trend','趋势') }}</span>
        <span class="sc-v">{{ '▲' if ab else '▼' }} 200d</span>
        <span class="sc-s">{% if tech.pct_vs_200dma is not none %}{{ "%+.1f"|format(tech.pct_vs_200dma) }}%{% endif %}</span>
      </div>
      {% set mpos = d.macd_pos %}
      <div class="sigchip {{ 'up' if mpos else 'down' }}">
        <span class="sc-k">{{ t('Momentum','动量') }}</span>
        <span class="sc-v">MACD {{ '＋' if mpos else '－' }}{% if d.macd_cross_up %} ↑{% elif d.macd_cross_dn %} ↓{% endif %}</span>
        <span class="sc-s">{{ t('daily','日线') }}</span>
      </div>
      {% set r = tech.rsi14 %}
      <div class="sigchip ctx">
        <span class="sc-k">RSI</span>
        <span class="sc-v">{{ r|round(0)|int if r is not none else '—' }}</span>
        <span class="sc-s">{{ t('high','偏高') if (r is not none and r>=70) else (t('low','偏低') if (r is not none and r<=30) else t('mid','中性')) }}</span>
      </div>
      {% set sv = d.stoch %}
      <div class="sigchip ctx">
        <span class="sc-k">Stoch</span>
        <span class="sc-v">{{ sv|round(0)|int if sv is not none else '—' }}</span>
        <span class="sc-s">{{ t('Washout','超卖') }}</span>
      </div>
      {% set dd = s.cycle.dc_day %}{% set band = s.cycle.dc_band %}
      <div class="sigchip ctx">
        <span class="sc-k">{{ t('Cycle','周期') }}</span>
        <span class="sc-v">{{ t('Day','第') }} {{ dd }}</span>
        <span class="sc-s">{% if band %}{{ t('of','/') }} {{ band[0] }}–{{ band[1] }}{% endif %}</span>
      </div>
    </div>
    {# ---- rate & inflation transmission overlay (display-only macro context) ---- #}
    {% if s.rate_infl and (s.rate_infl.rate.measured or s.rate_infl.inflation.measured) %}
    {% set ri = s.rate_infl %}{% set _ric = {'neg':'var(--down)','pos':'var(--up)','muted':'var(--muted)'} %}
    <div class="ri-strip" style="margin:10px 0 0;display:flex;flex-wrap:wrap;align-items:center;gap:8px;font-size:12px">
      <span class="muted" style="font-weight:700">{{ t('Rate & inflation','利率与通胀') }}
        {{ help("Per-channel rate / inflation headwind or tailwind this sector faces RIGHT NOW — the MEASURED cross-asset transmission coefficient (split-half forward IC) times the current driver state, split into a rate channel (real-rate speed, curve, policy pricing) and an inflation channel (breakevens, core-PCE gap, re-acceleration, expectations). DISPLAY-ONLY: it is never part of the heat or cycle score — the foundation's scored-leg gate found no rate/inflation leg robust enough to time returns. A 'regime-dependent' tag means the sector's link to that channel is only context-conditional (CONTEXT) on a forward-IC basis, so no robust headwind/tailwind is claimed.", "该板块当前在利率／通胀两个通道上面临的逆风或顺风 —— 实测的跨资产传导系数（分半前瞻IC）乘以当前驱动状态，分为利率通道（实际利率速度、曲线、政策定价）与通胀通道（盈亏平衡、核心PCE缺口、再加速、预期）。仅供展示：绝不计入热度或周期分数 —— 基础层的可计分腿检验未发现足够稳健以择时回报的利率/通胀腿。“依赖状态”标签表示在前瞻IC基础上该板块与该通道的关联仅在特定市场状态下成立（CONTEXT），因此不主张稳健的逆风/顺风。") }}
      </span>
      {% for ch in [ri.rate, ri.inflation] %}
      <span style="border:1px solid var(--line);border-radius:6px;padding:2px 8px;font-weight:600;color:{{ _ric[ch.tone] }}">{{ ch.arrow }} {{ t(ch.chip.en, ch.chip.zh) }}{% if ch.drivers %}<span class="muted" style="font-weight:400;font-size:11px"> · {{ t(ch.drivers[0].label.en, ch.drivers[0].label.zh) }} IC {{ '%+.2f'|format(ch.drivers[0].ic) }}</span>{% endif %}</span>
      {% endfor %}
    </div>
    <p class="muted" style="margin:4px 0 0;font-size:11px">{{ t(ri.summary.en, ri.summary.zh) }}</p>
    {% endif %}
    {# ---- dollar-desk lean → DISPLAY-ONLY context chip (W3-C4b, dollar-sensitive sectors) ---- #}
    {% if s.dollar_ctx %}{% set dc = s.dollar_ctx %}
    {% set _dct = {'headwind':'var(--down)','tailwind':'var(--up)','neutral':'var(--muted)'} %}
    <div class="ri-strip" style="margin:8px 0 0;display:flex;flex-wrap:wrap;align-items:center;gap:8px;font-size:12px">
      <span class="muted" style="font-weight:700">{{ t('Dollar','美元') }}
        {{ help("The forex Dollar Desk's MEASURED broad-dollar lean (real-rate regime + trend stack + liquidity + Fed path) as CONTEXT for this dollar-sensitive sector. A strong dollar is a mechanical headwind for multinational / commodity / export cyclicals (cheaper USD-priced commodities + FX-translation drag on overseas revenue); a soft dollar is a tailwind. DISPLAY-ONLY and UNVALIDATED FOR SIZING — it never enters the heat, cycle, or basket score. The forex 60-trial family found no per-pair dollar conviction that clears the DSR door (INTL-43); the direction shown is the MECHANISM, not a measured return coefficient.", "外汇美元台的实测宽美元倾向（实际利率状态+趋势栈+流动性+美联储路径），作为该美元敏感板块的参考背景。强美元对跨国/大宗商品/出口周期股构成机械性逆风（以美元计价的大宗商品更便宜+海外收入折算拖累）；弱美元为顺风。仅供展示、不用于仓位——绝不计入热度、周期或篮子分数。外汇60试验家族未发现任何可过DSR门的单对美元信念（INTL-43）；所示方向为机制，而非实测回报系数。") }}
      </span>
      <span style="border:1px solid var(--line);border-radius:6px;padding:2px 8px;font-weight:600;color:{{ _dct[dc.tone] }}">{{ dc.arrow }} {{ t(dc.chip_en, dc.chip_zh) }}<span class="muted" style="font-weight:400;font-size:11px"> · {{ t(dc.lean_en, dc.lean_zh) }}</span></span>
    </div>
    <p class="muted" style="margin:4px 0 0;font-size:11px">{{ t(dc.note_en, dc.note_zh) }} — {{ t('context only, not sized', '仅供参考，不用于仓位') }}</p>
    {% endif %}
    {% if s.ladder.age_line %}<p class="agewhen">{{ t(s.ladder.age_line, s.ladder.age_line_zh) }}</p>{% endif %}
    {% if s.ladder.early_note %}<p class="early">{{ t(s.ladder.early_note, s.ladder.early_note_zh) }}</p>{% endif %}
    {% if s.ladder.macro_line %}<p class="macro-caution">⚠ {{ t(s.ladder.macro_line, s.ladder.macro_line_zh) }}</p>{% endif %}
    <ul class="points">
      {% for p, pz in zip(s.ladder.points, s.ladder.points_zh) %}<li>{{ t(p, pz) }}</li>{% endfor %}
    </ul>
    {% set cp = s.ladder.cycle_plain %}
    <div class="cyclebox">
      <div><b>📅 {{ t(cp.daily_line, cp.daily_line_zh) }}</b> — {{ t(cp.daily_phase, cp.daily_phase_zh) }}
        {{ help("The DAILY cycle is the short rhythm — equities tend to bottom every ~36-42 trading days. 'Day N' counts trading days since the last daily low. Past ~42 (overdue) means a fresh low could come any day.", "日线周期是短节奏 — 股市通常每约 36-42 个交易日筑底一次。“第 N 天”指自上次日线低点以来的交易日数。超过约 42 天（已逾期）意味着新低随时可能出现。") }}</div>
      {% if cp.weekly_line %}<div><b>🗓 {{ t(cp.weekly_line, cp.weekly_line_zh) }}</b> — {{ t(cp.weekly_phase, cp.weekly_phase_zh) }}
        {{ help("The WEEKLY (investor) cycle is the bigger rhythm — ~16-26 weeks, containing several daily cycles. It sets the tide the daily cycles swim in; early weeks = more upside room, late weeks = treat rallies cautiously.", "周线（投资者）周期是更大的节奏 — 约 16-26 周，包含数个日线周期。它决定了日线周期所处的大势；早期周次=上行空间更大，晚期周次=对反弹保持谨慎。") }}</div>{% endif %}
      {% if cp.translation %}<div class="muted">{{ t(cp.translation, cp.translation_zh) }}</div>{% endif %}
    </div>
    {% if s.cycle.failed_cycle %}<p class="neg" style="margin:6px 0 0">⚠ {{ t('Failed cycle: price is below the low that started this cycle — historically the larger trend is rolling over in ~80% of such cases.', '周期失败：价格已跌破启动本周期的低点 — 历史上约 80% 的此类情形中，更大的趋势正在转向。') }}</p>{% endif %}
    <details style="margin-top:8px"><summary>{{ t('full reasoning', '完整推理') }}</summary>
      <p class="muted" style="margin:6px 0">{{ t(s.ladder.why, s.ladder.why_zh) }} {{ t('Next:', '下一步：') }} {{ t(s.ladder.next, s.ladder.next_zh) }}</p>
    </details>
  </div>

  {% if s.confluence and s.confluence.ok %}
  {% set c = s.confluence %}
  <div class="panel" id="confluence">
    <h2>{{ t('Entry setup — the 3-day view', '入场配置 — 3 日视角') }}
      {{ help("The same buy/sell read that drives the Sector setups board on the main dashboard, checked against the 200-day trend. A sector whose momentum is already stretched is never a fresh buy. The base rate is what this state actually went on to do over the next 63 days, against SPY. A risk and timing read, not a forecast.", "和主看板「板块买卖配置」用的是同一套买卖读数，以 200 日趋势把关。动量已经拉得很开的板块，绝不算新买点。基础概率是这个状态在随后 63 天里对 SPY 的实际表现。这是风险和择时的参考，不是预测。") }}
    </h2>
    <p style="margin:6px 0">
      <span class="state" style="border:1px solid {{ c.color }};color:{{ c.color }};padding:2px 9px;border-radius:7px;font-weight:700">{{ t(c.label, c.label_zh) }}</span>
      <span class="muted" style="margin-left:8px">{{ t(c.conviction_label, c.conviction_label_zh) }}{% if c.days_to_cross and c.state == 'SETUP_BUY' %} · ~{{ c.days_to_cross }}d {{ t('to cross', '后交叉') }}{% endif %}</span>
    </p>
    <p style="margin:4px 0">{{ t(c.action, c.action_zh) }}</p>
    <p class="muted" style="font-size:12.5px;margin:6px 0 0">
      {{ t('3-day signal', '3 日信号') }}: {{ t(c.signal_en, c.signal_zh) }} ·
      RSI {{ c.rsi_3d | round | int if c.rsi_3d is not none else "—" }} · Stoch {{ c.stoch_3d | round | int if c.stoch_3d is not none else "—" }} ·
      {{ "✓" if c.above200 else "✗" }}200d {{ "✓" if c.above50 else "✗" }}50d
      {% if c.base_rate %}· {{ t('measured 63d', '实测 63 日') }}:
        {% if c.side == 'tactical' and c.base_rate.abs63 is defined %}{{ "%+.1f" | format(c.base_rate.abs63) }}% {{ t('absolute', '绝对') }} ({{ c.base_rate.abs_hit }}% {{ t('up', '上涨') }}) · {{ "%+.1f" | format(c.base_rate.exc63) }}% {{ t('vs SPY', '对 SPY') }}{% else %}{{ "%+.1f" | format(c.base_rate.exc63) }}% {{ t('vs SPY', '对 SPY') }} ({{ c.base_rate.hit }}% {{ t('up', '上涨') }}){% endif %}{% if c.base_rate.n %}, n={{ c.base_rate.n }}{% endif %}{% endif %}
    </p>
  </div>
  {% endif %}

  <div class="panel">
    <h2>{{ t('Momentum across timeframes', '跨周期动量') }}
      {{ help("Daily, 3-day and weekly views of the same ETF. Weekly is the tide, 3-day the wave, daily the ripple — signals are strongest when all three agree. The gauges run from washed-out to overbought; the bars show which way momentum is heading and roughly how many days until it turns.", "同一只 ETF 的日线、3 日和周线视角。周线是潮汐，3 日是波浪，日线是涟漪 — 三者一致时信号最强。表盘从跌透到超买；柱状图显示动量往哪个方向走，以及大概还有几天转向。") }}
    </h2>
    <div data-mtf='{{ s.mtf_json }}'></div>
  </div>

  <div class="panel">
    <h2>{{ t('Chart', '图表') }}</h2>
    <div class="tv">
      <div class="tradingview-widget-container">
        <div id="tv_main"></div>
        <script src="https://s3.tradingview.com/tv.js"></script>
        <script>new TradingView.widget({"container_id":"tv_main","symbol":"{{ s.fund }}","interval":"D","theme":(localStorage.getItem('theme')==='light'?'light':'dark'),"style":"1","height":440,"width":"100%","hide_side_toolbar":true,"allow_symbol_change":true,"studies":["RSI@tv-basicstudies","MACD@tv-basicstudies"]});</script>
      </div>
    </div>
  </div>

  {% if s.accumulation %}
  <div class="panel" id="accumulation">
    <h2>{{ t('Accumulation watch — weight change beyond price', '增持监测 — 超越价格的权重变动') }}
      {{ help("Each top-10 holding's weight change since the prior snapshot, split into the part explained by the stock's own price move and a residual ('active' = weight beyond price). A large positive residual = real buying flowed in beyond market drift. HONEST CAVEAT: this is a PASSIVE index fund, so the residual is index reconstitution / float-weight flow (forced buying), NOT discretionary manager conviction. ✅ = accumulation that also has a forming technical buy setup. Thresholds uncalibrated until more snapshots accumulate.", "每只前十大持仓自上一快照以来的权重变动，被拆分为“由个股自身价格变动解释的部分”与“残差”（“主动”=超越价格的权重）。较大的正残差=除市场漂移外确有真实买盘流入。诚实提示：这是被动指数基金，故残差代表指数再平衡／流通权重调整带来的资金流（强制买入），并非主动经理的信念。✅=增持同时出现正在形成的技术买入形态。在积累更多快照前阈值未校准。") }}
    </h2>
    <table>
      <tr>
        <th class="l">{{ t('stock', '个股') }}</th>
        <th>{{ t('weight', '权重') }}</th>
        <th>{{ t('raw Δ', '原始Δ') }}</th>
        <th>{{ t('active Δ', '主动Δ') }}</th>
        <th>{{ t('active %', '主动%') }}</th>
        <th class="l">{{ t('technical', '技术面') }}</th>
      </tr>
      {% for a in s.accumulation %}
      <tr>
        <td class="l"><a href="../stock.html#{{ a.ticker }}"><b>{{ a.ticker }}</b></a> <span class="muted">{{ a.name }}</span></td>
        <td class="muted">{{ "%.2f" | format(a.w1) }}%</td>
        <td class="{{ 'pos' if a.raw_change >= 0 else 'neg' }}">{{ "%+.2f" | format(a.raw_change) }}pp</td>
        <td class="{{ 'pos' if a.active_change >= 0 else 'neg' }}"><b>{{ "%+.2f" | format(a.active_change) }}pp</b></td>
        <td class="{{ 'pos' if (a.active_pct or 0) >= 0 else 'neg' }}">{% if a.active_pct is not none %}{{ "%+.0f" | format(a.active_pct) }}%{% else %}—{% endif %}</td>
        <td class="l">{% if a.ladder %}<span class="state st-{{ a.ladder.state | replace(' ', '_') }}">{{ td(a.ladder.label) }}</span>{% if a.confirmed %} ✅{% endif %}{% else %}<span class="muted">—</span>{% endif %}{% if a.vol and a.vol.surging %} <span title="volume {{ a.vol.ratio }}× recent avg">📊</span>{% endif %}</td>
      </tr>
      {% endfor %}
    </table>
    <p class="muted" style="font-size:12px;margin-top:8px">{{ t('Window: prior snapshot → latest. “Active Δ” strips out price action; on this passive ETF it reflects index reconstitution / float-weight flow, not a manager’s discretion.', '窗口：上一快照 → 最新快照。“主动Δ”已剔除价格变动；在该被动 ETF 上反映的是指数再平衡／流通权重调整带来的资金流，而非经理人的主动决策。') }}</p>
  </div>
  {% endif %}

  {% if s.alpha_leaders %}
  <div class="panel" id="alpha-leaders">
    <h2>{{ t('Alpha leaders — strongest stock-specific momentum in the sector', '阿尔法领跑 — 板块内最强个股动量') }}
      {{ help("Each stock's return with its MARKET and SECTOR beta removed — sector-neutral residual momentum over a 12-1-month window. It ranks the names pulling ahead of their OWN sector on stock-specific strength, not just riding the sector tide. 'idio ann' is the annualized idiosyncratic (beta-stripped) return; the entry tag flags a leader that just spiked ('extended' = short-term reversal risk) vs one on a recent dip ('pullback' = constructive entry). Phase-0 validated as a MODEST, regime-decayed edge — context, not a buy list.", "每只股票剔除其市场与板块贝塔后的收益 —— 12-1 个月的板块中性残差动量。它排出在个股特定强度上领先于自身板块、而非仅随板块上涨的股票。“年化残差”为年化的特异（剔除贝塔）收益；入场标签标记刚急涨的领跑股（“extended”=短期反转风险）与近期回调者（“pullback”=较佳入场）。第 0 阶段验证为温和、随周期衰减的优势 —— 仅供参考，而非买入清单。") }}
    </h2>
    <table>
      <tr>
        <th class="l">{{ t('stock','个股') }}</th>
        <th>{{ t('alpha z','阿尔法 z') }}</th>
        <th>{{ t('sector rank','板块排名') }}</th>
        <th>{{ t('idio ann','年化残差') }}</th>
        <th>{{ t('1-mo','近一月') }}</th>
        <th class="l">{{ t('entry','入场') }}</th>
      </tr>
      {% for a in s.alpha_leaders %}
      <tr>
        <td class="l"><a href="../stock.html#{{ a.ticker }}"><b>{{ a.ticker }}</b></a> <span class="muted">{{ a.name }}</span></td>
        <td><b class="pos">{% if a.alpha is not none %}{{ "%+.2f"|format(a.alpha) }}{% else %}—{% endif %}</b></td>
        <td class="muted">{{ a.sector_rank }}/{{ a.sector_n }}</td>
        <td class="{{ 'pos' if (a.resid_ann or 0) >= 0 else 'neg' }}">{% if a.resid_ann is not none %}{{ "%+.0f"|format(a.resid_ann) }}%{% else %}—{% endif %}</td>
        <td class="{{ 'pos' if (a.rev_1m or 0) >= 0 else 'neg' }}">{% if a.rev_1m is not none %}{{ "%+.1f"|format(a.rev_1m) }}%{% else %}—{% endif %}</td>
        <td class="l">{% if a.entry %}{% set _aez = {'pullback':'回调买点','extended':'急涨偏热','intact':'趋势完好','laggard':'落后','neutral':'中性'} %}<span class="alpha-entry ae-{{ a.entry }}">{{ t(a.entry, _aez.get(a.entry, a.entry)) }}</span>{% endif %}</td>
      </tr>
      {% endfor %}
    </table>
    <p class="muted" style="font-size:12px;margin-top:8px">{{ t('Sector-neutral residual momentum — a modest, regime-decayed edge measured over 1962–2026. Stock-selection context within the sector, not a standalone buy list.', '板块中性残差动量 —— 基于 1962–2026 测得的温和、随周期衰减的优势。用于板块内选股的参考，而非独立买入清单。') }}</p>
  </div>
  {% endif %}

  <div class="panel">
    <h2>{{ t('Top 10 holdings — each analyzed on its own cycle', '前 10 大持仓 — 各自按其周期分析') }}
      {{ help("Every holding gets the same treatment as the ETF: daily-cycle position, multi-timeframe momentum, and a ladder state. The states carry measured base rates (table at the bottom). Weight = % of the ETF this stock represents — the top names drive the fund.", "每只持仓都与 ETF 同样处理：日线周期位置、跨周期动量及阶梯状态。各状态附有实测基准胜率（见底部表格）。权重=该股票占 ETF 的百分比 — 前几大成分股主导基金走势。") }}
    </h2>
    {% for h in s.holdings %}
    <div class="card">
      <div class="topline">
        <a href="../stock.html#{{ h.ticker }}"><b style="font-size:15px">{{ h.ticker }}</b></a>
        <span class="muted">{{ h.name }}</span>
        <span class="muted">{{ h.weight_pct }}%{{ t(' of fund', ' 占基金') }}</span>
        <span class="state st-{{ h.ladder.state | replace(" ", "_") }}">{{ td(h.ladder.label) }}</span>
        <span class="urgpill urg-{{ h.ladder.entry.urgency }}">{{ td(h.ladder.entry.tag) }}{% if h.ladder.entry.days_hi and h.ladder.entry.urgency == 'imminent' %} ~{{ h.ladder.entry.days_hi }}d{% endif %}</span>
        {% if h.ladder.age_short %}<span class="agechip" title="when this signal last crossed">⏱ {{ t(h.ladder.age_short, h.ladder.age_short_zh) }}</span>{% endif %}
        {% if h.ladder.eq_badge %}<span class="eqbadge sm eq-{{ h.ladder.eq_dir }}" title="{{ h.ladder.eq_tip }}">{{ h.ladder.eq_badge }}</span>{% endif %}
        <span style="flex:1"></span>
        <span class="muted" style="font-size:12px">{{ t('daily cycle day', '日线周期第') }} {{ h.cycle.dc_day }}/~{{ h.cycle.dc_band[1] }}{{ t('', ' 天') }}</span>
      </div>
      <ul class="points sm">
        {% for p, pz in zip(h.ladder.points[:3], h.ladder.points_zh[:3]) %}<li>{{ t(p, pz) }}</li>{% endfor %}
      </ul>
      {% if h.ladder.early_note %}<p class="early">{{ t(h.ladder.early_note, h.ladder.early_note_zh) }}</p>{% endif %}
      {% if h.ladder.macro_line %}<p class="macro-caution">⚠ {{ t(h.ladder.macro_line, h.ladder.macro_line_zh) }}</p>{% endif %}
      {% if h.fundamentals and (h.fundamentals.fwd_pe or h.fundamentals.rev_growth) %}
      <p class="muted" style="font-size:12px; margin:4px 0">{% if h.fundamentals.fwd_pe %}{{ t('fwd P/E', '预期市盈率') }} <b>{{ h.fundamentals.fwd_pe | round(1) }}</b>{% endif %}{% if h.fundamentals.rev_growth %} · {{ t('rev growth', '营收增长') }} <b>{{ (h.fundamentals.rev_growth * 100) | round(1) }}%</b>{% endif %}{% if h.fundamentals.profit_margin %} · {{ t('margin', '利润率') }} <b>{{ (h.fundamentals.profit_margin * 100) | round(0) }}%</b>{% endif %} {{ help("Best-effort weekly valuation snapshot from free sources — LOW CONFIDENCE, rough context only.", "来自免费数据源的尽力而为的每周估值快照 — 低可信度，仅供粗略参考。") }}</p>
      {% endif %}
      <div data-mtf='{{ h.mtf_json }}'></div>
      <details style="margin-top:6px"><summary>{{ t('full reasoning', '完整推理') }}</summary>
        <p class="muted" style="margin:5px 0">{{ t(h.ladder.why, h.ladder.why_zh) }} {{ t('Next:', '下一步：') }} {{ t(h.ladder.next, h.ladder.next_zh) }}</p>
      </details>
    </div>
    {% endfor %}
  </div>

  <div class="panel">
    <h2>{{ t('How much to trust these states — measured, not promised', '这些状态有多可信 — 实测得出，而非承诺') }}
      {{ help("Every trading day since 2000, across sector ETFs and large stocks, we computed the same ladder state from data available at the time and measured what price did over the next month. These are those base rates. They are tendencies with real spread around them — position sizing beats certainty.", "自 2000 年以来的每个交易日，针对各行业 ETF 与大型股票，我们用当时可得的数据计算出相同的阶梯状态，并测量此后一个月的价格表现。以下即为这些基准胜率。它们是带有实际离散度的倾向 — 仓位管理胜过确定性。") }}
    </h2>
    {% if calibration %}
    <table>
      <tr><th>{{ t('state', '状态') }}</th><th>{{ t('samples', '样本数') }}</th><th>{{ t('up next month', '下月上涨') }}</th><th>{{ t('avg next month', '下月均值') }}</th></tr>
      {% for st in ladder_order %}{% if calibration.get(st) %}
      <tr>
        <td><span class="state st-{{ st | replace(" ", "_") }}">{{ td(state_display[st].label) }} · {{ td(state_display[st].action) }}</span></td>
        <td>{{ calibration[st].n }}</td>
        <td>{{ calibration[st].hit_pct }}%</td>
        <td class="{{ 'pos' if calibration[st].avg_fwd_pct > 0 else 'neg' }}">{{ "%+.2f" | format(calibration[st].avg_fwd_pct) }}%</td>
      </tr>
      {% endif %}{% endfor %}
    </table>
    <p style="margin:10px 0 0"><b>{{ t('Read the table before trusting any state.', '相信任何状态之前，先读这张表。') }}</b>
      <span class="muted"><span class="l-en">What it shows: stocks drift up over a month from EVERY state
      (that's the market's long upward bias), the spread between states is small, and the
      "scary" states measured slightly BETTER forward returns than the buy states —
      markets mean-revert, and waiting for full confirmation costs part of the bounce.
      So use the ladder for <b>structure</b>: where you are in the cycle, what would
      confirm a turn, and exactly where you're wrong (the invalidation price) — and use
      position size, not certainty, to express conviction. It is not a return predictor,
      and the table proves we checked.</span><span class="l-zh">它说明了什么：从每一种状态出发，股票在一个月内平均都会上行
      （这是市场长期的向上偏向），各状态之间的差距很小，而那些“吓人”的状态实测的远期收益
      反而略优于买入状态 — 市场会均值回归，等待完全确认会损失一部分反弹。
      所以请把阶梯用于<b>结构判断</b>：你处于周期的哪个位置、什么信号能确认转向，
      以及你在哪里就错了（失效价位）— 然后用仓位大小、而非确定性来表达信念。
      它不是收益预测器，这张表正证明我们核查过了。</span></span></p>
    {% if calibration.get("BOTTOM WATCH +early-bull") and calibration.get("BOTTOM WATCH no-early") %}
    {% set ew = calibration["BOTTOM WATCH +early-bull"] %}{% set nw = calibration["BOTTOM WATCH no-early"] %}
    <p class="early" style="margin-top:10px">⚡ <span class="l-en"><b>Do the early (pre-emptive) signals actually help?</b>
      We measured it the same way. When a "nearing a low" reading ALSO had an early bullish signal
      (momentum diverging, turning up, or popping off the low end), the next month was up
      <b>{{ ew.hit_pct }}%</b> of the time (avg {{ "%+.2f" | format(ew.avg_fwd_pct) }}%, n={{ ew.n }}) —
      versus <b>{{ nw.hit_pct }}%</b> (avg {{ "%+.2f" | format(nw.avg_fwd_pct) }}%, n={{ nw.n }}) without one.
      So on average the early signals did <b>not</b> beat simply waiting — they fire sooner and catch the
      occasional sharp V-bottom, but at a higher false-alarm rate. Use them to know <i>when to pay
      attention</i>, then still require confirmation before committing. That's the honest tradeoff,
      measured rather than assumed.</span><span class="l-zh"><b>这些提前（预判）信号真的有用吗？</b>
      我们用同样的方法做了测量。当一次“接近低点”的读数同时伴有提前的看多信号
      （动量出现背离、开始转向，或从低位跳起）时，此后一个月上涨的概率为
      <b>{{ ew.hit_pct }}%</b>（均值 {{ "%+.2f" | format(ew.avg_fwd_pct) }}%，n={{ ew.n }}）—
      而没有此信号时为 <b>{{ nw.hit_pct }}%</b>（均值 {{ "%+.2f" | format(nw.avg_fwd_pct) }}%，n={{ nw.n }}）。
      所以平均而言，提前信号并<b>未</b>胜过单纯等待 — 它们触发更早，能抓住偶尔出现的尖锐 V 型底，
      但误报率也更高。用它们来判断<i>何时该留意</i>，然后在投入之前仍需要确认。
      这就是诚实的取舍 — 实测得出，而非臆断。</span></p>
    {% endif %}
    {% else %}<p class="muted">{{ t('Calibration pending — regenerates with the weekly run.', '校准待定 — 将随每周运行重新生成。') }}</p>{% endif %}
    <p class="muted" style="font-size:12px"><span class="l-en">Cycle methodology after graddhy.com / thefinancialtap.com: equity daily cycles
       typically run 36-42 trading days trough-to-trough; timing bands catch roughly 70% of lows, so ~3 in 10 arrive
       early or late — that's why confirmation (swing low, 10-day average) gates every buy state. Charts by TradingView.</span><span class="l-zh">周期方法论参考 graddhy.com / thefinancialtap.com：股票日线周期
       通常为 36-42 个交易日（谷底到谷底）；时间窗能捕捉约 70% 的低点，故约十分之三会提前或滞后出现 —
       这正是为何每个买入状态都以确认（摆动低点、10 日均线）为前提。图表由 TradingView 提供。</span></p>
  </div>

<script>
/* alert feed: floating popover (grows out of the 🔔 button, overlays the page)
   + filter chips */
(function(){
  var btn=document.getElementById('af-btn'), feed=document.getElementById('af-feed'),
      scrim=document.getElementById('af-scrim'), x=document.getElementById('af-close');
  function place(){
    // phone (≤560) is a CSS bottom-sheet — clear any desktop inline pinning
    if(window.matchMedia('(max-width: 560px)').matches){
      feed.style.left=feed.style.top=feed.style.right=feed.style.transformOrigin=''; return;
    }
    var b=btn.getBoundingClientRect(), pw=feed.offsetWidth||470, m=10;
    var left=Math.min(b.right-pw, window.innerWidth-m-pw);
    if(left<m) left=m;
    feed.style.left=left+'px'; feed.style.right='auto';
    feed.style.top=(b.bottom+9)+'px';
    feed.style.transformOrigin=Math.max(14,Math.min(pw-14,(b.left+b.right)/2-left))+'px top';
  }
  function set(open){
    feed.classList.toggle('af-open',open);
    if(scrim) scrim.classList.toggle('af-open',open);
    btn.setAttribute('aria-expanded',open?'true':'false');
    feed.setAttribute('aria-hidden',open?'false':'true');
  }
  function open(){ place(); set(true); document.addEventListener('keydown',onKey);
    window.addEventListener('scroll',close); window.addEventListener('resize',close); }
  function close(){ set(false); document.removeEventListener('keydown',onKey);
    window.removeEventListener('scroll',close); window.removeEventListener('resize',close); }
  function onKey(e){ if(e.key==='Escape'||e.key==='Esc') close(); }
  if(btn&&feed){
    btn.addEventListener('click',function(){ feed.classList.contains('af-open')?close():open(); });
    if(x) x.addEventListener('click',close);
    if(scrim) scrim.addEventListener('click',close);
  }
  var fil=document.querySelectorAll('.aff'), items=document.querySelectorAll('.af-item');
  fil.forEach(function(b){b.addEventListener('click',function(){
    fil.forEach(function(x){x.classList.remove('active')}); b.classList.add('active');
    var f=b.dataset.f;
    items.forEach(function(it){ it.hidden=!(f==='all'||it.dataset.f===f); });
    document.querySelectorAll('.af-day').forEach(function(dd){
      var any=[].slice.call(dd.querySelectorAll('.af-item')).some(function(i){return !i.hidden});
      dd.style.display=any?'':'none';
    });
  });});
})();
</script>
<script src="../mtf.js"></script>
<script src="../theme.js"></script>
<footer class="site-footer">
  <span class="made">© 2026 Mastermind Inc</span>
</footer>
</body>
</html>
