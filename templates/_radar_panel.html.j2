{# Divergence Radar compact panel (display-only) — drop-in embed for baskets.html and any page.
   Reads basketdata/radar.json + radar_enriched.json + radar_news.json via radar_panel.js.
   Self-contained: hides itself when JSON is absent. CSS injected by JS (rp- prefix). #}
<section id="divergence-radar" class="panel pad" style="margin-top:10px"></section>
<script src="radar_panel.js"></script>
<script>renderDivergenceRadar({ base: "basketdata/" });</script>
