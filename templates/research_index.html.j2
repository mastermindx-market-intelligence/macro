{%- macro t(en, zh='') -%}
<span class="l-en">{{ en|safe }}</span><span class="l-zh">{{ (zh if zh else en)|safe }}</span>
{%- endmacro -%}
{% set nav_prefix = '../' %}
<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<title>Institutional Research Archive — {{ total }} desk reports | MastermindX Research Vault</title>
<meta name="description" content="A browsable archive of institutional desk research on MastermindX — buy-side and sell-side reports from {{ groups|length }} institutions, indexed by desk. Read summaries; full PDFs on Pro.">
<link rel="canonical" href="{{ canonical }}">
<meta property="og:type" content="website">
<meta property="og:site_name" content="MastermindX">
<meta property="og:title" content="Institutional Research Archive — MastermindX">
<meta property="og:url" content="{{ canonical }}">
<meta property="og:image" content="{{ site_base }}/apple-touch-icon.png">
<script>try{var th=localStorage.getItem('theme');document.documentElement.setAttribute('data-theme',th||'dark');var l=localStorage.getItem('lang');if(l)document.documentElement.setAttribute('data-lang',l);}catch(e){}</script>
<link rel="icon" href="../favicon.svg" type="image/svg+xml">
<link rel="apple-touch-icon" href="../apple-touch-icon.png">
<link rel="stylesheet" href="../theme.css">
<style>
:root{ --num: ui-monospace, "SF Mono", "Cascadia Code", Menlo, Consolas, monospace;
  --rv:#4f8ff7; --rv-2:#38bdf8; --rv-ink:#9cc2ff; --rv-dim:rgba(79,143,247,.13);
  --rx-bg:rgba(16,22,30,.55); --rx-bd:rgba(255,255,255,.12); }
html[data-theme="light"]{ --rv:#285fff; --rv-2:#0f8fd8; --rv-ink:#1b49c8; --rv-dim:rgba(40,95,255,.10);
  --rx-bg:rgba(255,255,255,.82); --rx-bd:rgba(0,0,0,.06); }
html:not([data-lang="zh"]) .l-zh{ display:none; } html[data-lang="zh"] .l-en{ display:none; }
html,body{ background:var(--bg); }
body{ padding-top:22px; color:var(--text); min-height:100vh; }
.rx-wrap{ max-width:960px; margin:0 auto; padding:26px 22px 96px; }
.rx-eyebrow{ font-family:var(--num); font-size:10.5px; font-weight:700; letter-spacing:.16em; text-transform:uppercase; color:var(--rv-ink); }
html[data-theme="light"] .rx-eyebrow{ color:var(--rv); }
.rx-wrap h1{ font-weight:800; font-size:clamp(26px,3.6vw,38px); line-height:1.1; letter-spacing:-.02em; color:var(--text); margin:8px 0 8px; }
.rx-sub{ font-size:14.5px; color:var(--muted); max-width:60ch; margin:0 0 8px; }
.rx-cta{ display:inline-flex; align-items:center; gap:7px; margin-top:14px; padding:10px 18px; border-radius:11px; font-weight:700; font-size:13.5px;
  color:#fff; text-decoration:none; background:linear-gradient(120deg,var(--rv),color-mix(in srgb,var(--rv) 74%,var(--rv-2))); }
.rx-grp{ margin-top:30px; }
.rx-grp h2{ font-size:13px; font-weight:800; letter-spacing:.03em; text-transform:uppercase; color:var(--muted); margin:0 0 12px;
  padding-bottom:8px; border-bottom:1px solid color-mix(in srgb,var(--text) 8%,transparent); }
.rx-grp ul{ list-style:none; margin:0; padding:0; display:grid; grid-template-columns:repeat(auto-fill,minmax(280px,1fr)); gap:9px; }
.rx-grp a{ display:block; padding:12px 15px; border-radius:11px; text-decoration:none; color:var(--text);
  background:var(--rx-bg); border:1px solid var(--rx-bd); transition:.15s; }
.rx-grp a:hover{ border-color:color-mix(in srgb,var(--rv) 40%,var(--rx-bd)); transform:translateY(-1px); }
/* date over title. Both are <span>, so without display:block they render on one
   line and the date collides with the title ("Jul 25, 2026EM Asia FX…") — and the
   .ti margin-top is silently inert on an inline box. */
.rx-grp .d{ display:block; font-family:var(--num); font-size:10.5px; line-height:1.4; letter-spacing:.01em;
  color:var(--muted); font-variant-numeric:tabular-nums; }
.rx-grp .ti{ display:block; font-size:13.5px; font-weight:600; margin-top:4px; line-height:1.35; }
.rx-foot{ margin-top:40px; padding-top:20px; border-top:1px solid color-mix(in srgb,var(--text) 8%,transparent); font-size:12px; color:var(--muted); }
@media (prefers-reduced-motion:reduce){ .rx-grp a:hover{ transform:none; } }
</style>
</head>
<body>
{% include "_site_nav.html.j2" %}
<main class="rx-wrap">
  <span class="rx-eyebrow">{{ t('MastermindX Research Vault', 'MastermindX 研报库') }}</span>
  <h1>{{ t('Institutional research archive', '机构研报存档') }}</h1>
  <p class="rx-sub">{{ t('Every desk report in the vault — buy-side and sell-side institutional research, indexed by institution. Read the summaries here; the full PDFs are on Pro.', '库内全部机构研报 —— 买方与卖方研究，按机构索引。此处可读摘要，PDF 全文为 Pro 专享。') }}</p>
  <a class="rx-cta" href="../research_vault.html">{{ t('Open the Research Vault', '进入研报库') }} →</a>

  {% for g in groups %}
  <section class="rx-grp">
    <h2>{{ g.inst }} <span style="font-family:var(--num);color:var(--rv-ink);font-weight:700">· {{ g.reports|length }}</span></h2>
    <ul>
      {% for r in g.reports %}
      <li><a href="{{ r.slug }}.html">{% if r.date_disp %}<span class="d">{{ r.date_disp }}</span>{% endif %}<span class="ti">{{ r.title }}</span></a></li>
      {% endfor %}
    </ul>
  </section>
  {% endfor %}

  <p class="rx-foot"><span class="l-en"><b>Not investment advice.</b> Third-party institutional research, hosted for reference and education.</span><span class="l-zh"><b>非投资建议。</b>第三方机构研究，仅供参考与学习。</span></p>
</main>
<script src="../theme.js"></script>
</body>
</html>
