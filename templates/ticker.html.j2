{%- macro t(en, zh='') -%}
<span class="l-en">{{ en }}</span><span class="l-zh">{{ zh if zh else en }}</span>
{%- endmacro -%}
{% set nav_prefix = '../' %}
{% set terminal_url = 'https://app.mastermind-x.com/terminal?sym=' ~ ticker ~ '&from=macro' %}
{% set intelligence_upgrade_url = 'https://app.mastermind-x.com/terminal?sym=' ~ ticker ~ '&pane=transcripts&from=company-dossier&utm_source=company_dossier&utm_medium=organic_research&utm_campaign=company_intelligence_upgrade' %}
<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<title>{{ ticker }} — {{ name }}: signals, options &amp; factor profile | MastermindX</title>
{% if stale %}<meta name="robots" content="noindex">{% endif %}
<meta name="description" content="{{ meta_desc }}">
<link rel="canonical" href="{{ canonical_url }}">
{# Machine-readable identity assertion, audited by scripts/check_stock_dossier_integrity.py
   against the canonical rendered universe. The guard reads THESE rather than scraping
   the page, because the dossier's own "Financials" section heading makes prose
   unreadable as a sector claim. mm:desc-provenance is the auditable linkage for the
   third-party business description; absent means no description is published. #}
{% if identity %}<meta name="mm:ticker" content="{{ identity.ticker }}">
<meta name="mm:issuer" content="{{ identity.issuer }}">
<meta name="mm:sector" content="{{ identity.sector }}">
{% if identity.sector_conflict %}<meta name="mm:sector-conflict" content="{{ identity.sector_conflict }}">
{% endif %}{% if identity.desc_provenance %}<meta name="mm:desc-provenance" content="{{ identity.desc_provenance }}">
{% endif %}{% endif %}<meta property="og:title" content="{{ ticker }} — {{ name }}: signals &amp; factor profile">
<meta property="og:description" content="{{ meta_desc }}">
<meta property="og:url" content="{{ canonical_url }}">
<meta property="og:type" content="website">
<meta property="og:site_name" content="MastermindX">
{% if og_image_url %}<meta property="og:image" content="{{ og_image_url }}">
<meta property="og:image:width" content="1200">
<meta property="og:image:height" content="630">
<meta name="twitter:card" content="summary_large_image">
<meta name="twitter:image" content="{{ og_image_url }}">{% endif %}
<link rel="dns-prefetch" href="https://app.mastermind-x.com">
<link rel="preconnect" href="https://app.mastermind-x.com" crossorigin>
<script type="application/ld+json">{{ jsonld_str | safe }}</script>
<script data-dbase src="../data_base.js"></script>
<script>try{var th=localStorage.getItem('theme');document.documentElement.setAttribute('data-theme',th||'dark');var l=localStorage.getItem('lang')||(function(){try{var L=navigator.languages||[navigator.language||navigator.userLanguage||''],i;for(i=0;i<L.length;i++)if((L[i]||'').toLowerCase().slice(0,2)==='zh')return'zh';if(/Shanghai|Hong_Kong|Macau|Urumqi|Chongqing|Harbin|Kashgar|Chungking/.test(Intl.DateTimeFormat().resolvedOptions().timeZone||''))return'zh';}catch(e){}return'';})();if(l)document.documentElement.setAttribute('data-lang',l);document.documentElement.classList.add('has-js');}catch(e){}</script>
<link rel="stylesheet" href="../theme.css">
<script defer src="../theme.js"></script>
<script defer src="../assets/js/company-intelligence-dossier.js?v=20260820a"></script>
<script defer src="../assets/js/dossier-live-quote.js?v=20260827a"></script>
<style>
/* ══ dossier v6 ═══════════════════════════════════════════════════════════
   One voice: Inter (--font-ui) everywhere; numbers are tabular Inter, never
   monospace. The page is tinted by the engine stance (--stance): aurora,
   hero ribbon and ladder all derive from the same verdict. */
*{box-sizing:border-box;}
:root{--stance:var(--muted);
      --hair:color-mix(in srgb,var(--line) 60%,transparent);
      --v6-radius:18px;}
body.st-pos{--stance:var(--up);}
body.st-warn{--stance:var(--warn);}
body.st-neg{--stance:var(--down);}
html body{font:400 13.5px/1.55 var(--font-ui);background:var(--bg);color:var(--text);margin:0;padding:24px 0 0;-webkit-font-smoothing:antialiased;}  /* 24px top gap = check_nav_gap contract */
html,body{overflow-x:clip;}  /* safety net — offenders are also fixed at the source */
a{text-decoration:none;color:inherit;}
.num{font-variant-numeric:tabular-nums;letter-spacing:-.01em;}
.pos{color:var(--ink-up, var(--up));}.neg{color:var(--ink-down, var(--down));}.wrn{color:var(--ink-warn, var(--warn));}.mut{color:var(--muted);}
.sm{font-size:12px;}

/* aurora — stance-tinted room light (plain gradients: no filter cost) */
.aurora{position:fixed;inset:0;z-index:0;pointer-events:none;overflow:hidden;}
.aurora::before{content:"";position:absolute;width:1100px;height:780px;top:-260px;left:-180px;
  background:radial-gradient(closest-side,color-mix(in srgb,var(--stance) 9%,transparent) 0%,transparent 78%);}
.aurora::after{content:"";position:absolute;width:820px;height:640px;bottom:-160px;right:-140px;
  background:radial-gradient(closest-side,color-mix(in srgb,var(--info) 6%,transparent) 0%,transparent 78%);}

/* sticky jump strip */
.strip{position:fixed;top:0;left:0;right:0;z-index:220;display:flex;align-items:center;gap:12px;height:48px;overflow:hidden;
  padding:0 22px;background:color-mix(in srgb,var(--bg) 86%,transparent);
  backdrop-filter:blur(18px) saturate(150%);-webkit-backdrop-filter:blur(18px) saturate(150%);
  border-bottom:1px solid var(--hair);transform:translateY(-100%);transition:transform .3s cubic-bezier(.22,.9,.26,1.02);}
.strip.vis{transform:translateY(0);}
.strip .stk{font-size:14.5px;font-weight:800;}
.strip .stp{font-size:13.5px;font-weight:650;}
.schip{font-size:11px;font-weight:700;padding:3px 10px;border-radius:999px;white-space:nowrap;
  color:var(--stance);background:color-mix(in srgb,var(--stance) 12%,transparent);
  border:1px solid color-mix(in srgb,var(--stance) 30%,transparent);}
.strip nav{display:flex;gap:2px;margin-left:6px;overflow-x:auto;scrollbar-width:none;flex:1;}
.strip nav::-webkit-scrollbar{display:none;}
.strip nav a{font-size:12px;color:var(--muted);padding:5px 10px;border-radius:8px;white-space:nowrap;}
.strip nav a:hover{color:var(--text);background:var(--gbtn-bg);}
.strip nav a.is-on{color:var(--text);background:color-mix(in srgb,var(--link) 14%,transparent);}
@media(max-width:760px){.strip nav{display:none;}.strip .sname{display:none;}}
@media(max-width:480px){.strip .schip{display:none;}}

/* layout */
.page-wrap{position:relative;z-index:1;max-width:1240px;margin:0 auto;padding:0 26px 60px;}
.crumb{font-size:11.5px;font-weight:550;color:var(--muted);padding:18px 2px 0;letter-spacing:.02em;}
.crumb a:hover{color:var(--ink-link, var(--link));}
.crumb b{color:var(--text);font-weight:600;}

/* module chrome */
.mod{background:var(--glass-bg);border:1px solid var(--glass-brd);border-radius:var(--v6-radius);
  box-shadow:var(--glass-shadow);backdrop-filter:blur(22px) saturate(150%);-webkit-backdrop-filter:blur(22px) saturate(150%);
  padding:20px 24px 22px;min-width:0;}
.mod-hd{display:flex;align-items:baseline;gap:10px;margin:0 0 16px;padding-bottom:12px;border-bottom:1px solid var(--hair);}
.mod-hd h2{display:flex;align-items:center;gap:10px;margin:0;font-size:15.5px;font-weight:650;letter-spacing:-.01em;}
.mod-hd h2::before{content:"";width:3px;height:15px;border-radius:2px;flex-shrink:0;
  background:linear-gradient(180deg,var(--link),color-mix(in srgb,var(--link) 30%,transparent));}
.mod-hd .hint{margin-left:auto;font-size:11.5px;color:var(--muted);font-weight:500;white-space:nowrap;display:flex;align-items:center;gap:8px;}
.mod-ft{margin-top:14px;padding-top:10px;border-top:1px solid var(--hair);font-size:11.5px;color:var(--muted);line-height:1.5;}

/* reveal-on-scroll (html.js gated; reduced-motion opts out; no-JS shows all) */
html.has-js .rv{opacity:0;transform:translateY(16px);}
html.has-js .rv.in{opacity:1;transform:none;transition:opacity .55s ease,transform .55s cubic-bezier(.22,.9,.26,1.02);transition-delay:var(--d,0ms);}
@media(prefers-reduced-motion:reduce){html.has-js .rv{opacity:1;transform:none;transition:none;}}

/* hero */
.hero{position:relative;margin-top:14px;padding:26px 30px 0;overflow:hidden;}
.hero::before{content:"";position:absolute;left:0;top:0;bottom:0;width:3px;
  background:linear-gradient(180deg,var(--stance),color-mix(in srgb,var(--stance) 12%,transparent));}
.hero-grid{display:grid;grid-template-columns:minmax(0,1fr) auto;gap:16px 48px;align-items:start;}
.eyebrow{font-size:10.5px;font-weight:650;letter-spacing:.09em;text-transform:uppercase;color:var(--muted);}
.hero h1{display:flex;align-items:baseline;flex-wrap:wrap;gap:6px 16px;margin:6px 0 0;
  font-size:clamp(38px,4.8vw,54px);font-weight:900;letter-spacing:-.04em;line-height:.95;}
.hero h1 a:hover{color:var(--ink-link, var(--link));}
.hero h1 .co{font-size:15px;font-weight:550;letter-spacing:0;color:var(--muted);}
.hero-desc{margin:12px 0 0;max-width:66ch;font-size:13px;line-height:1.6;color:var(--muted);}
.chips{display:flex;flex-wrap:wrap;gap:6px;margin:14px 0 0;}
.chip6{display:inline-flex;align-items:center;gap:5px;font-size:11px;font-weight:600;padding:3px 10px;
  border-radius:999px;background:var(--gbtn-bg);border:1px solid var(--gbtn-brd);color:var(--muted);}
.px-block{text-align:right;min-width:230px;}
.px-big{font-size:clamp(32px,3.6vw,44px);font-weight:750;letter-spacing:-.025em;line-height:1;}
.px-chg{display:inline-flex;align-items:center;gap:6px;margin-top:8px;font-size:13.5px;font-weight:650;}
.px-chg .pill6{padding:2px 9px;border-radius:8px;}
.px-chg.pos .pill6{background:color-mix(in srgb,var(--up) 13%,transparent);border:1px solid color-mix(in srgb,var(--up) 30%,transparent);}
.px-chg.neg .pill6{background:color-mix(in srgb,var(--down) 13%,transparent);border:1px solid color-mix(in srgb,var(--down) 30%,transparent);}
/* The live pip: one dot, used beside the price AND on the stance stamp so the
   two read as the same "this is current" signal. The glow is a box-shadow
   pulse rather than an opacity blink — the dot never disappears, so a glance
   that lands mid-cycle still sees it. Base state carries the glow, which is
   what reduced-motion falls back to. */
/* --ok, not --up: a live pip is a STATUS read, and the standing zh colour law
   (红涨绿跌) names "live/feed dots" in the list that must NOT flip. Painting it
   from --up made it turn red in Chinese, where red means "rising" — a
   directional claim from an indicator that only ever meant "this is current".
   --ok is the non-flipping status green and is theme-aware in both modes. */
.live-dot{width:6px;height:6px;border-radius:50%;background:var(--ok);display:inline-block;
  box-shadow:0 0 7px 1px color-mix(in srgb,var(--ok) 60%,transparent);animation:v6lp 2.6s ease-in-out infinite;}
@keyframes v6lp{
  0%,100%{opacity:1;box-shadow:0 0 7px 1px color-mix(in srgb,var(--ok) 62%,transparent);}
  50%{opacity:.62;box-shadow:0 0 2px 0 color-mix(in srgb,var(--ok) 14%,transparent);}}
/* stance stamp — replaces the old "Engine read · <date>" line */
.livestamp{display:inline-flex;align-items:center;gap:6px;font-size:11px;font-weight:700;
  letter-spacing:.06em;text-transform:uppercase;color:var(--ink-ok, var(--ok));white-space:nowrap;}
[data-lang="zh"] .livestamp{letter-spacing:0;text-transform:none;}
/* Quote-state stamp. The pip and its label are painted from the QUOTE's own
   measured freshness, never from the build date — a nightly page used to print
   a pulsing green "Live" beside a price that was a day old. The green pulse is
   therefore reserved for `live`: a measured realtime row inside an open
   regular session. Every weaker state is a still, muted pip, so "current"
   can never be implied by decoration alone. */
.dq-stamp[data-dq-state]{color:var(--muted);}
.dq-stamp[data-dq-state] .live-dot{background:var(--muted);box-shadow:none;animation:none;}
.dq-stamp[data-dq-state="delayed"]{color:var(--warn);}
.dq-stamp[data-dq-state="delayed"] .live-dot{background:var(--warn);box-shadow:none;animation:none;}
.dq-stamp[data-dq-state="live"]{color:var(--ink-ok,var(--ok));}
.dq-stamp[data-dq-state="live"] .live-dot{background:var(--ok);
  box-shadow:0 0 7px 1px color-mix(in srgb,var(--ok) 60%,transparent);animation:v6lp 2.6s ease-in-out infinite;}
.wk52{margin-top:16px;}
.wk52 .lbls{display:flex;justify-content:space-between;gap:14px;font-size:10.5px;color:var(--muted);margin-bottom:5px;}
.wk52 .lbls b{color:var(--text);font-weight:650;}
.wk52 .track{position:relative;height:4px;border-radius:3px;background:var(--line);}
.wk52 .fill{position:absolute;top:0;bottom:0;left:0;border-radius:3px;
  background:linear-gradient(90deg,color-mix(in srgb,var(--link) 40%,transparent),var(--link));}
.wk52 .dotp{position:absolute;top:50%;transform:translate(-50%,-50%);
  width:10px;height:10px;border-radius:50%;background:var(--text);box-shadow:0 0 0 3px color-mix(in srgb,var(--link) 35%,transparent);}
.stance-row{display:flex;align-items:center;gap:14px;flex-wrap:wrap;margin:22px -30px 0;padding:13px 30px;
  border-top:1px solid var(--hair);background:color-mix(in srgb,var(--stance) 5%,transparent);}
.stance-big{display:inline-flex;align-items:center;gap:8px;font-size:13px;font-weight:750;padding:6px 14px;border-radius:10px;
  color:var(--stance);background:color-mix(in srgb,var(--stance) 13%,transparent);
  border:1px solid color-mix(in srgb,var(--stance) 32%,transparent);}
.stance-line{flex:1;min-width:260px;font-size:12.5px;color:var(--muted);line-height:1.5;}
.asof{font-size:11px;color:var(--muted);white-space:nowrap;}
@media(prefers-reduced-motion:no-preference){
  html.has-js .hi{opacity:0;transform:translateY(12px);animation:v6hin .6s cubic-bezier(.22,.9,.26,1.02) forwards;}
  html.has-js .hi2{animation-delay:.08s;}html.has-js .hi3{animation-delay:.16s;}html.has-js .hi4{animation-delay:.24s;}
  @keyframes v6hin{to{opacity:1;transform:none;}}
}

/* Company Intelligence — dynamic context plane, never a trading signal. */
.ci-mod{position:relative;margin-top:20px;padding:0;overflow:hidden;
  background:
    radial-gradient(900px 360px at 7% -34%,color-mix(in srgb,var(--link) 19%,transparent),transparent 58%),
    radial-gradient(560px 320px at 95% 8%,color-mix(in srgb,var(--info) 11%,transparent),transparent 66%),
    var(--glass-bg);}
.ci-mod::before{content:"";position:absolute;inset:0 0 auto;height:1px;pointer-events:none;
  background:linear-gradient(90deg,transparent,var(--link),color-mix(in srgb,var(--info) 65%,transparent),transparent);}
.ci-top{position:relative;display:flex;align-items:flex-start;justify-content:space-between;gap:24px;padding:22px 26px 18px;border-bottom:1px solid var(--hair);}
.ci-kicker{display:flex;align-items:center;gap:8px;margin-bottom:5px;font-size:10px;font-weight:780;letter-spacing:.12em;text-transform:uppercase;color:var(--ink-link, var(--link));}
.ci-orbit{position:relative;width:8px;height:8px;border-radius:50%;background:var(--link);box-shadow:0 0 0 4px color-mix(in srgb,var(--link) 11%,transparent);}
.ci-orbit::after{content:"";position:absolute;inset:-5px;border:1px solid color-mix(in srgb,var(--link) 36%,transparent);border-radius:50%;animation:ci-pulse 2.8s ease-out infinite;}
@keyframes ci-pulse{0%{transform:scale(.65);opacity:.8}70%,100%{transform:scale(1.55);opacity:0}}
.ci-top h2{margin:0;font-size:clamp(21px,2vw,27px);line-height:1.06;letter-spacing:-.035em;font-weight:780;}
.ci-top p{max-width:67ch;margin:8px 0 0;color:var(--muted);font-size:12.5px;line-height:1.55;}
.ci-mod{--ci-ok:#37c89a;--ci-caution:#e3a34b;}
[data-theme="light"] .ci-mod{--ci-ok:#126b51;--ci-caution:#8a5600;}
.ci-state{display:inline-flex;align-items:center;gap:7px;flex:0 0 auto;max-width:240px;padding:5px 10px;border-radius:999px;border:1px solid var(--gbtn-brd);background:var(--gbtn-bg);color:var(--muted);font-size:10.5px;font-weight:700;line-height:1.35;}
.ci-state::before{content:"";width:6px;height:6px;border-radius:50%;background:currentColor;}
.ci-state.ready{color:var(--ci-ok);border-color:color-mix(in srgb,var(--ci-ok) 32%,transparent);background:color-mix(in srgb,var(--ci-ok) 9%,transparent);}
.ci-state.partial{color:var(--ci-caution);border-color:color-mix(in srgb,var(--ci-caution) 34%,transparent);background:color-mix(in srgb,var(--ci-caution) 10%,transparent);}
.ci-state.unavailable{color:var(--muted);}
.ci-loading[hidden],.ci-empty[hidden],#ci-content[hidden]{display:none!important;}
.ci-loading{display:grid;grid-template-columns:minmax(0,1fr) minmax(260px,.48fr);gap:18px;min-height:330px;padding:24px 26px 25px;}
.ci-skel{height:16px;border-radius:7px;background:linear-gradient(100deg,var(--gbtn-bg) 20%,color-mix(in srgb,var(--text) 7%,transparent) 48%,var(--gbtn-bg) 76%);background-size:220% 100%;animation:ci-shimmer 1.45s linear infinite;}
.ci-skel.big{height:86px}.ci-skel.med{width:72%;margin-top:12px}.ci-skel.sm{width:43%;margin-top:12px}
@keyframes ci-shimmer{to{background-position:-220% 0}}
.ci-empty{padding:25px 26px 27px;}
.ci-empty h3{margin:0 0 6px;font-size:16px}.ci-empty p{max-width:72ch;margin:0 0 15px;color:var(--muted);}
.ci-grid{display:grid;grid-template-columns:minmax(0,1.55fr) minmax(270px,.62fr);}
.ci-story{min-width:0;padding:24px 26px 22px;border-right:1px solid var(--hair);}
.ci-period{display:flex;align-items:center;gap:8px;flex-wrap:wrap;margin-bottom:12px;color:var(--muted);font-size:11px;font-weight:600;letter-spacing:.025em;}
.ci-period strong{color:var(--text);font-size:11.5px;}
.ci-period .ci-source-lang{display:inline-flex;padding:2px 7px;border:1px solid var(--hair);border-radius:999px;font-size:9.5px;letter-spacing:.04em;text-transform:uppercase;}
.ci-summary{max-width:840px;margin:0;font-size:clamp(18px,2.2vw,25px);line-height:1.28;letter-spacing:-.025em;font-weight:665;}
.ci-summary.is-empty{color:var(--muted);font-weight:560;}
.ci-lenses{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:10px;margin-top:20px;}
.ci-lens{position:relative;padding:14px 15px 15px;border:1px solid var(--hair);border-radius:13px;background:color-mix(in srgb,var(--panel2) 62%,transparent);min-width:0;}
.ci-lens::before{content:"";position:absolute;left:0;top:13px;bottom:13px;width:2px;border-radius:2px;background:var(--muted);}
.ci-lens.strength::before{background:var(--up)}.ci-lens.pressure::before{background:var(--warn)}
.ci-lens-label{display:flex;align-items:center;gap:7px;margin:0 0 7px;font-size:10px;font-weight:780;letter-spacing:.08em;text-transform:uppercase;color:var(--muted);}
.ci-lens p{margin:0;color:var(--text);font-size:12.5px;line-height:1.55;}
.ci-tags{display:flex;flex-wrap:wrap;gap:6px;margin-top:14px;}
.ci-tag{padding:3px 8px;border:1px solid color-mix(in srgb,var(--link) 18%,var(--hair));border-radius:999px;background:color-mix(in srgb,var(--link) 6%,transparent);color:var(--muted);font-size:10px;font-weight:620;}
.ci-side{display:flex;flex-direction:column;min-width:0;padding:22px 22px 20px;background:color-mix(in srgb,var(--panel2) 38%,transparent);}
.ci-side-label{margin:0 0 10px;font-size:10px;font-weight:780;letter-spacing:.09em;text-transform:uppercase;color:var(--muted);}
.ci-metrics{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:8px;}
.ci-metric{min-height:76px;padding:10px 11px;border:1px solid var(--hair);border-radius:12px;background:color-mix(in srgb,var(--panel) 70%,transparent);}
.ci-metric span{display:block;color:var(--muted);font-size:10.5px;line-height:1.3;}
.ci-metric strong{display:block;margin-top:5px;font-size:19px;line-height:1;font-weight:760;font-variant-numeric:tabular-nums;}
.ci-metric small{display:block;margin-top:5px;color:var(--muted);font-size:10.5px;font-variant-numeric:tabular-nums;}
.ci-metric small.up{color:var(--ink-up, var(--up))}.ci-metric small.down{color:var(--ink-down, var(--down))}
.ci-metric em{display:block;margin-top:5px;color:var(--muted);font:650 9px/1.2 var(--font-ui);font-style:normal;letter-spacing:.04em;text-transform:uppercase;}
.ci-next{margin-top:15px;padding-top:14px;border-top:1px solid var(--hair);}
.ci-next p{margin:0;color:var(--muted);font-size:11.5px;line-height:1.55;}
.ci-actions{display:flex;align-items:center;gap:7px;flex-wrap:wrap;margin-top:auto;padding-top:16px;}
.ci-actions .gbtn{justify-content:center;}
/* Company Intelligence action row — house .gbtn motion, nothing bespoke.
   The previous treatment defined its own ::after, which has the same
   specificity as .gbtn::after and lands later in the cascade, so it REPLACED
   the house sheen sweep with a static washed overlay at z-index:-1. It also
   left .gbtn:hover free to overwrite background and color, so hovering the
   primary swapped its gradient for grey glass and its dark ink for near-white.
   Both are gone: the buttons now take .gbtn / .gbtn-cta wholesale — the same
   sweep, lift and breathing halo as every other CTA on the site — and these
   rules only size them and lift the sweep to a strength that is actually
   visible against a tinted surface (the shared 13% white reads as nothing). */
.ci-upgrade{min-height:36px;padding:9px 15px;font-weight:770;color:#fff;border-color:rgba(255,255,255,.22);
  background:linear-gradient(112deg,#3b82f6,#6366f1 54%,#7c5cff);}
/* .gbtn:hover repaints background AND color, and it out-specifies a plain
   class — so a tinted button MUST restate both or it turns into grey glass
   with near-white ink on hover (which is what it used to do). */
.ci-upgrade:hover,.ci-upgrade:focus-visible{color:#fff;border-color:rgba(255,255,255,.4);
  background:linear-gradient(112deg,#4a8dff,#7076f5 54%,#8b6dff);}
/* Same house sweep, two inks: the secondaries sweep in the text colour so it
   reads in both themes, the tinted CTA sweeps in white because its surface is
   saturated blue in both. The shared 13% white is invisible on either. */
.ci-actions .gbtn:not(.ci-upgrade)::after{
  background:linear-gradient(105deg,transparent 38%,color-mix(in srgb,var(--text) 26%,transparent) 50%,transparent 62%);}
.ci-upgrade::after{background:linear-gradient(105deg,transparent 36%,rgba(255,255,255,.5) 50%,transparent 64%);}
.ci-history{display:flex;align-items:center;gap:10px;min-width:0;padding:13px 18px 14px;border-top:1px solid var(--hair);background:color-mix(in srgb,var(--bg) 14%,transparent);}
.ci-history-label{flex:0 0 auto;color:var(--muted);font-size:10px;font-weight:760;letter-spacing:.08em;text-transform:uppercase;}
.ci-history-tabs{display:flex;gap:6px;min-width:0;overflow-x:auto;scrollbar-width:none;}
.ci-history-tabs::-webkit-scrollbar{display:none;}
.ci-history-tab{appearance:none;min-height:34px;border:1px solid var(--hair);border-radius:9px;background:var(--gbtn-bg);color:var(--muted);padding:7px 10px;font:650 11px/1.2 var(--font-ui);white-space:nowrap;cursor:pointer;}
.ci-history-tab:hover,.ci-history-tab:focus-visible{color:var(--text);border-color:color-mix(in srgb,var(--link) 42%,var(--hair));outline:none;}
.ci-history-tab[aria-pressed="true"]{color:var(--ink-link, var(--link));background:color-mix(in srgb,var(--link) 10%,transparent);border-color:color-mix(in srgb,var(--link) 32%,transparent);}
.ci-receipt{margin-left:auto;flex:0 0 auto;color:var(--muted);font-size:10px;white-space:nowrap;}
.ci-foot{display:flex;align-items:center;gap:10px;flex-wrap:wrap;padding:10px 22px 12px;border-top:1px solid var(--hair);color:var(--muted);font-size:11px;line-height:1.5;}
.ci-foot b{color:var(--text);font-weight:650;}.ci-foot .ci-note{margin-left:auto;text-align:right;}
.ci-sr{position:absolute!important;width:1px!important;height:1px!important;padding:0!important;margin:-1px!important;overflow:hidden!important;clip:rect(0,0,0,0)!important;white-space:nowrap!important;border:0!important;}
[data-lang="zh"] .ci-kicker,[data-lang="zh"] .ci-lens-label,[data-lang="zh"] .ci-side-label,[data-lang="zh"] .ci-history-label,[data-lang="zh"] .ci-source-lang,[data-lang="zh"] .ci-metric em{letter-spacing:0;text-transform:none;}
/* Reduced motion: name the pseudo-elements explicitly — a bare element
   selector does not reach ::after. .live-dot keeps its resting glow, so
   stopping the pulse leaves a visible dot rather than a blank slot. */
@media(prefers-reduced-motion:reduce){.ci-orbit::after,.ci-skel,.live-dot{animation:none;}
  .fcard .fb[data-v]::after,.fcard .fb[data-v]:hover::after{transition:none;transform:translateX(-50%);}}
@media(max-width:900px){.ci-grid{grid-template-columns:1fr}.ci-story{border-right:0;border-bottom:1px solid var(--hair)}.ci-side{display:grid;grid-template-columns:minmax(0,1fr) minmax(220px,.65fr);gap:18px}.ci-actions{grid-column:1/-1;padding-top:0}.ci-next{margin-top:0;padding-top:0;border-top:0;border-left:1px solid var(--hair);padding-left:18px}}
@media(max-width:640px){.ci-top{padding:18px 16px 15px;gap:12px}.ci-top p{font-size:12px}.ci-state{max-width:152px;padding:5px 8px}.ci-loading{grid-template-columns:1fr;min-height:560px;padding:19px 16px}.ci-story{padding:19px 16px 17px}.ci-lenses{grid-template-columns:1fr}.ci-side{display:flex;padding:17px 16px}.ci-next{margin-top:14px;padding:13px 0 0;border-left:0;border-top:1px solid var(--hair)}.ci-actions{padding-top:15px}.ci-actions .ci-upgrade{width:100%;order:-1}.ci-history{align-items:flex-start;flex-wrap:wrap;padding:12px 16px}.ci-history-tabs{order:3;flex-basis:100%;padding-right:18px}.ci-receipt{margin-left:auto}.ci-foot{padding:10px 16px}.ci-foot .ci-note{margin-left:0;text-align:left}.ci-summary{font-size:18px}}
/* v2 glance row styles — Reported/Guidance/Watch/Coverage */
.ci-v2-rows{margin-top:12px;}
.ci-v2-section{font-size:10px;font-weight:780;letter-spacing:.09em;text-transform:uppercase;color:var(--muted);margin:14px 0 4px;padding-bottom:3px;border-bottom:1px solid var(--hair);}
.ci-v2-section:first-child{margin-top:0;}
.ci-v2-row{display:grid;grid-template-columns:minmax(0,1.1fr) minmax(0,1.4fr);gap:4px 12px;padding:5px 0;border-bottom:1px solid var(--hair);font-size:12.5px;align-items:baseline;}
.ci-v2-key{color:var(--muted);min-width:0;}
.ci-v2-val{font-weight:650;text-align:right;min-width:0;overflow-wrap:anywhere;}
/* In v2 mode: hide v1-specific composition areas via root data attribute */
#company-update[data-ci-mode="v2"] .ci-summary,
#company-update[data-ci-mode="v2"] .ci-lenses,
#company-update[data-ci-mode="v2"] .ci-next,
#company-update[data-ci-mode="v2"] #ci-history,
#company-update[data-ci-mode="v2"] #ci-tags,
#company-update[data-ci-mode="v2"] #ci-title,
#company-update[data-ci-mode="v2"] .ci-top > div > p,
#company-update[data-ci-mode="v2"] #ci-metrics-label,
#company-update[data-ci-mode="v2"] #ci-metrics{display:none!important;}
#company-update[data-ci-mode="v2"] #ci-v2-host{display:block;}
#company-update:not([data-ci-mode="v2"]) #ci-v2-host{display:none!important;}
#company-update[data-ci-mode="unavailable"] #ci-title,
#company-update[data-ci-mode="unavailable"] .ci-top > div > p{display:none!important;}
@media(max-width:390px){.ci-v2-row{grid-template-columns:minmax(0,1fr);}
  .ci-v2-val{text-align:left;font-weight:600;}}

/* chart module */
.chart-mod{margin-top:20px;padding:0;overflow:hidden;}
.chart-mod .mod-hd{padding:16px 24px 12px;margin:0;border-bottom:1px solid var(--hair);}
.chart-frame{position:relative;height:clamp(360px,42vw,520px);background:color-mix(in srgb,#0b0d11 40%,transparent);}
[data-theme="light"] .chart-frame{background:color-mix(in srgb,var(--panel2) 55%,transparent);}
.chart-frame iframe{position:absolute;inset:0;width:100%;height:100%;border:0;opacity:0;transition:opacity .45s ease;}
.chart-frame.loaded iframe{opacity:1;}
.chart-skel{position:absolute;inset:0;display:flex;align-items:center;justify-content:center;color:var(--muted);font-size:12px;overflow:hidden;}
.chart-skel::before{content:"";position:absolute;inset:0;
  background:linear-gradient(100deg,transparent 30%,color-mix(in srgb,var(--text) 4%,transparent) 50%,transparent 70%);
  transform:translateX(-100%);animation:v6sk 1.6s ease-in-out infinite;}
@media(prefers-reduced-motion:reduce){.chart-skel::before{animation:none;}}
@keyframes v6sk{to{transform:translateX(100%);}}
.chart-frame.loaded .chart-skel,.chart-frame.failed .chart-skel{display:none;}
.chart-fallback{position:absolute;inset:0;display:none;flex-direction:column;align-items:center;justify-content:center;gap:12px;color:var(--muted);font-size:13px;text-align:center;padding:20px;}
.chart-frame.failed .chart-fallback{display:flex;}
.chart-ft{display:flex;align-items:center;gap:14px;flex-wrap:wrap;padding:10px 24px 14px;border-top:1px solid var(--hair);}
.powered{margin-left:auto;display:inline-flex;align-items:center;gap:7px;font-size:11.5px;font-weight:650;color:var(--muted);}
.powered:hover{color:var(--ink-link, var(--link));}
.powered .bolt{width:16px;height:16px;border-radius:5px;display:inline-flex;align-items:center;justify-content:center;
  background:linear-gradient(135deg,var(--link),var(--info));color:#0f1115;font-size:10px;font-weight:900;}

{%- if security_state %}
{# The Decision Spine ships its own CSS only on the pages that render it: a
   listing whose blob carries no security_state block must stay byte-for-byte
   what it was before this section existed. #}
/* ══ SECURITY STATE — the Decision Spine ═══════════════════════════════════
   Six separate reads on one listing, each carrying its own coverage state.
   The composition decisions, written down because they are choices:

   · ONE panel holding six cells, not six floating panels. The job is "one
     coherent answer you can audit"; six independent cards read as six
     independent widgets. The cell is the .ci-lens idiom (panel2 tint, hairline,
     left rail) widened to a three-column grid — house idiom extended, not a
     second card system. Nesting depth stays 2.
   · The LEFT RAIL is the signature, and its SHAPE is the state class, not just
     its hue: solid = covered · dashed = interrupted (stale/partial/corrected) ·
     hairline = absent (unavailable/not covered/not applicable/rights blocked) ·
     split = contradicted. Hue is never the only channel, so the grid survives
     zh, light mode and colour blindness. The chip dot repeats the same four
     shapes at glyph scale, and the tally ticks below the grid repeat them
     again — the strip is literally the grid's legend.
   · Absence is GREY. Only a contradiction between sources earns --act. An
     unavailable owner painted red would be a finding nobody made.
   · Ink marks epistemic status, never content: a covered card's answer prints
     in --text, a degraded card's headline prints in its state ink.
   · No composite. No total row, no score, no average, anywhere.               */
.ss-t-ok  {--ss-c:var(--ok);  --ss-ink:var(--ink-ok,  var(--ok));}
.ss-t-warn{--ss-c:var(--warn);--ss-ink:var(--ink-warn,var(--warn));}
.ss-t-act {--ss-c:var(--act); --ss-ink:var(--ink-act, var(--act));}
.ss-t-off {--ss-c:var(--muted);--ss-ink:var(--muted);}
.ss-tone-ink{color:var(--ss-ink);}

.ss-mod{margin-top:20px;}
.ss-sub{margin:-4px 0 15px;max-width:76ch;font-size:12.5px;line-height:1.55;color:var(--muted);}
.ss-grid{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:12px;}
.ss-cell{position:relative;display:flex;flex-direction:column;min-width:0;
  padding:14px 15px 12px 17px;border:1px solid var(--hair);border-radius:13px;
  background:color-mix(in srgb,var(--panel2) 62%,transparent);}
.ss-cell::before{content:"";position:absolute;left:0;top:14px;bottom:14px;width:3px;border-radius:3px;}
.ss-r-solid::before {background:var(--ss-c);}
.ss-r-dash::before  {background:repeating-linear-gradient(180deg,var(--ss-c) 0 11px,transparent 11px 17px);}
.ss-r-hollow::before{left:1px;width:1px;background:color-mix(in srgb,var(--ss-c) 85%,transparent);}
.ss-r-split::before {background:linear-gradient(180deg,var(--warn) 0 50%,var(--act) 50% 100%);}

.ss-axis{display:flex;align-items:center;gap:9px;margin:0 0 9px;min-width:0;
  font-size:10px;font-weight:780;letter-spacing:.08em;text-transform:uppercase;color:var(--muted);}
.ss-axis>span:first-child{min-width:0;overflow-wrap:anywhere;}
.ss-chip{display:inline-flex;align-items:center;gap:5px;flex:0 0 auto;margin-left:auto;
  padding:2px 8px;border-radius:999px;font-size:9.5px;font-weight:720;letter-spacing:.01em;text-transform:none;
  color:var(--ss-ink);background:color-mix(in srgb,var(--ss-c) 12%,transparent);
  border:1px solid color-mix(in srgb,var(--ss-c) 30%,transparent);}
/* the chip dot is the rail vocabulary at glyph scale */
.ss-chip::before{content:"";flex:0 0 auto;width:5px;height:5px;border-radius:50%;background:var(--ss-c);}
.ss-r-dash   .ss-chip::before{width:7px;height:2px;border-radius:1px;}
.ss-r-hollow .ss-chip::before{background:transparent;box-shadow:inset 0 0 0 1px var(--ss-c);}
.ss-r-split  .ss-chip::before{border-radius:1px;background:linear-gradient(90deg,var(--warn) 0 50%,var(--act) 50% 100%);}

.ss-head{margin:0;font-size:14.5px;font-weight:700;line-height:1.32;letter-spacing:-.012em;
  color:var(--text);overflow-wrap:anywhere;}
.ss-cell:not(.is-ok) .ss-head{color:var(--ss-ink);}
.ss-body{margin:7px 0 0;font-size:12.5px;line-height:1.55;color:var(--text);overflow-wrap:anywhere;}
.ss-why{margin:6px 0 0;font-size:11.5px;line-height:1.5;color:var(--muted);overflow-wrap:anywhere;}
.ss-note{margin:10px 0 0;padding-top:10px;border-top:1px solid var(--hair);
  font-size:11.5px;line-height:1.5;color:var(--muted);min-width:0;}
.ss-note b{display:block;margin-bottom:4px;font-size:9.5px;font-weight:780;letter-spacing:.08em;
  text-transform:uppercase;color:var(--muted);}
.ss-note .v{color:var(--text);font-weight:600;overflow-wrap:anywhere;}
.ss-list{list-style:none;margin:0;padding:0;}
.ss-list li{position:relative;padding:2px 0 2px 13px;color:var(--text);overflow-wrap:anywhere;}
.ss-list li::before{content:"";position:absolute;left:1px;top:10px;width:5px;height:1px;background:var(--muted);}
.ss-more{display:block;margin-top:4px;}

.ss-foot{display:flex;align-items:center;gap:8px 12px;flex-wrap:wrap;width:100%;margin-top:auto;
  padding:11px 0 0;border:0;border-top:1px solid var(--hair);background:none;text-align:left;
  appearance:none;cursor:pointer;font:inherit;color:inherit;transition:color .16s ease;}
.ss-foot .ss-clock{font-size:10.5px;line-height:1.4;color:var(--muted);font-variant-numeric:tabular-nums;
  min-width:0;overflow-wrap:anywhere;}
.ss-foot .ss-go{margin-left:auto;flex:0 0 auto;font-size:10.5px;font-weight:700;color:var(--muted);white-space:nowrap;}
.ss-foot .ss-go::after{content:" \2192";}
.ss-foot:hover .ss-go,.ss-foot:focus-visible .ss-go{color:var(--ink-link, var(--link));}
.ss-foot:focus-visible{outline:2px solid color-mix(in srgb,var(--link) 72%,transparent);outline-offset:3px;border-radius:4px;}

/* the one flag: identity that is not proven, or a read that could not be built.
   It names what failed AND what still works — never a bare degraded panel. */
.ss-flag{display:block;margin:0 0 14px;padding:11px 14px;border-radius:11px;
  border:1px solid color-mix(in srgb,var(--ss-c) 26%,var(--hair));border-left:3px solid var(--ss-c);
  background:color-mix(in srgb,var(--ss-c) 7%,transparent);font-size:12px;line-height:1.55;color:var(--text);}
.ss-flag b{display:block;margin-bottom:3px;font-size:11.5px;font-weight:750;color:var(--ss-ink);}

/* evidence strip — the grid's legend and its one panel-level as-of */
.ss-ev{display:flex;align-items:center;gap:10px 16px;flex-wrap:wrap;margin-top:16px;padding-top:13px;
  border-top:1px solid var(--hair);}
.ss-ev-label{flex:0 0 auto;font-size:10px;font-weight:780;letter-spacing:.09em;text-transform:uppercase;color:var(--muted);}
.ss-tally{display:flex;align-items:center;gap:6px 14px;flex-wrap:wrap;min-width:0;font-size:11.5px;color:var(--muted);}
.ss-tally>span{display:inline-flex;align-items:center;gap:6px;min-width:0;}
.ss-tally b{color:var(--text);font-weight:700;font-variant-numeric:tabular-nums;}
.ss-tick{flex:0 0 auto;width:3px;height:12px;border-radius:2px;background:var(--ss-c);}
.ss-tick.ss-r-dash  {background:repeating-linear-gradient(180deg,var(--ss-c) 0 3px,transparent 3px 6px);}
.ss-tick.ss-r-hollow{width:1px;background:color-mix(in srgb,var(--ss-c) 85%,transparent);}
.ss-tick.ss-r-split {background:linear-gradient(180deg,var(--warn) 0 50%,var(--act) 50% 100%);}
.ss-ev .gbtn{margin-left:auto;flex:0 0 auto;}

/* hero chip — additive, one element, jumps to the section it summarises */
.ss-hchip{display:inline-flex;align-items:center;gap:6px;flex:0 0 auto;padding:4px 11px;border-radius:999px;
  font-size:10.5px;font-weight:700;line-height:1.35;white-space:nowrap;color:var(--ss-ink);
  background:color-mix(in srgb,var(--ss-c) 11%,transparent);
  border:1px solid color-mix(in srgb,var(--ss-c) 28%,transparent);transition:border-color .16s ease;}
.ss-hchip::before{content:"";flex:0 0 auto;width:5px;height:5px;border-radius:50%;background:var(--ss-c);}
.ss-hchip:hover{border-color:color-mix(in srgb,var(--ss-c) 54%,transparent);}
.ss-hchip:focus-visible{outline:2px solid color-mix(in srgb,var(--link) 72%,transparent);outline-offset:2px;}

/* drilldown internals — reuse .dpanel/.dkv/.dnotes; mono is for the machine
   identifiers only (owner object ids, recipe id, gate codes), never for words */
.ss-dp{margin:0 0 8px;font-size:13px;line-height:1.6;}
.ss-dp:last-child{margin-bottom:0;}
.ss-dp.mut{font-size:12.5px;color:var(--muted);}
.ss-id{font-family:var(--font-mono);font-size:11px;letter-spacing:0;overflow-wrap:anywhere;}
.ss-gate{padding:9px 0;border-bottom:1px solid var(--hair);}
.ss-gate:last-child{border-bottom:0;}
.ss-gate .g{font-size:12.5px;font-weight:650;line-height:1.45;}
.ss-gate .c{display:block;margin-top:3px;color:var(--muted);}
.ss-gate .w{display:block;margin-top:4px;font-size:11.5px;line-height:1.5;color:var(--muted);}
.ss-gate .w em{font-style:normal;font-weight:650;color:var(--text);}
.ss-receipt{margin-top:8px;font-size:11.5px;line-height:1.55;color:var(--muted);}

/* ── the provenance register ──────────────────────────────────────────────
   Two kinds of line live in this panel: values quoted from an owner, and
   values worked out here from those quotes. Quoting is the majority and the
   default, so the QUOTED lines carry no mark at all and the worked-out ones
   carry this tag — mark the exception, not the rule, or the mark stops
   meaning anything. It is a tag, not a badge: hairline, muted, no hue, so it
   never competes with the state rail for attention.                        */
.ss-prov{display:inline-flex;align-items:center;flex:0 0 auto;margin-left:8px;padding:1px 6px;
  border:1px solid var(--hair);border-radius:5px;vertical-align:middle;
  font-size:9px;font-weight:700;letter-spacing:.06em;text-transform:uppercase;
  line-height:1.6;white-space:nowrap;color:var(--muted);}
.dpanel h3 .ss-prov{position:relative;top:-1px;}

/* estimated timing — an estimate that could be mistaken for a diary entry is
   the failure mode this element exists to prevent, so the qualifier sits on
   its own line under the window rather than in a tooltip. */
.ss-est{display:block;margin-top:2px;font-size:10.5px;line-height:1.45;color:var(--ink-warn,var(--warn));}
.ss-when{font-variant-numeric:tabular-nums;}

/* nested owner reads inside one leg (opportunity context) */
.ss-subs{display:flex;flex-direction:column;gap:4px;}
.ss-sub-r{display:flex;align-items:baseline;gap:8px;min-width:0;}
.ss-sub-r>.n{min-width:0;overflow-wrap:anywhere;color:var(--text);}
.ss-sub-r>.s{margin-left:auto;flex:0 0 auto;font-size:10.5px;font-weight:650;color:var(--ss-ink);}
.ss-sub-r>.s::before{content:"";display:inline-block;width:5px;height:5px;margin-right:5px;
  border-radius:50%;background:var(--ss-c);vertical-align:middle;}
.ss-sub-r.ss-r-hollow>.s::before{background:transparent;box-shadow:inset 0 0 0 1px var(--ss-c);}
.ss-sub-r.ss-r-dash>.s::before{width:7px;height:2px;border-radius:1px;}

/* identity receipt rows — R1…R9, one block each, in the .ss-gate family */
.ss-chk{padding:11px 0;border-bottom:1px solid var(--hair);}
.ss-chk:last-child{border-bottom:0;}
.ss-chk-h{display:flex;align-items:baseline;gap:9px;flex-wrap:wrap;}
.ss-chk-h .r{font-family:var(--font-mono);font-size:11.5px;font-weight:700;color:var(--text);}
.ss-chk-h .v{font-size:10.5px;font-weight:700;letter-spacing:.05em;text-transform:uppercase;color:var(--ss-ink);}
.ss-chk-h .v::before{content:"";display:inline-block;width:5px;height:5px;margin-right:5px;
  border-radius:50%;background:var(--ss-c);vertical-align:middle;}
.ss-chk-h .c{margin-left:auto;color:var(--muted);}
.ss-chk-d{margin:4px 0 0;font-size:12px;line-height:1.5;color:var(--text);overflow-wrap:anywhere;}
.ss-chk-s{margin:4px 0 0;font-size:10.5px;line-height:1.5;color:var(--muted);overflow-wrap:anywhere;}
.ss-chk-v{display:grid;grid-template-columns:auto minmax(0,1fr);gap:2px 10px;margin-top:6px;}
.ss-chk-v dt,.ss-chk-v dd{margin:0;font-family:var(--font-mono);font-size:10.5px;line-height:1.55;
  overflow-wrap:anywhere;}
.ss-chk-v dt{color:var(--muted);}
.ss-chk-v dd{color:var(--text);}

[data-lang="zh"] :is(.ss-axis,.ss-ev-label,.ss-note b,.ss-prov,.ss-chk-h .v){letter-spacing:0;text-transform:none;}

/* Light is designed, not translated: a 10% mix of a hue with `transparent`
   sits on white glass and disappears, so light tints mix with #fff instead,
   and the hairline rail is given more ink to stay ≥3:1 on white. */
[data-theme="light"] .ss-cell{background:var(--panel2);}
[data-theme="light"] .ss-chip{background:color-mix(in srgb,var(--ss-c) 13%,#fff);
  border-color:color-mix(in srgb,var(--ss-c) 42%,transparent);}
[data-theme="light"] .ss-flag{background:color-mix(in srgb,var(--ss-c) 9%,#fff);}
[data-theme="light"] .ss-hchip{background:color-mix(in srgb,var(--ss-c) 12%,#fff);
  border-color:color-mix(in srgb,var(--ss-c) 40%,transparent);}
[data-theme="light"] :is(.ss-r-hollow::before,.ss-tick.ss-r-hollow){background:var(--ss-c);}

@media(max-width:1023px){.ss-grid{grid-template-columns:repeat(2,minmax(0,1fr));}}
@media(max-width:640px){
  .ss-mod{padding:18px 16px 18px;}
  .ss-grid{grid-template-columns:minmax(0,1fr);gap:10px;}
  .ss-cell{padding:13px 14px 11px 16px;}
  .ss-sub{margin:-2px 0 14px;font-size:12px;}
  .ss-foot{padding-top:14px;padding-bottom:2px;min-height:40px;}
  .ss-ev .gbtn{width:100%;justify-content:center;margin-left:0;min-height:40px;}
}
{%- endif %}

/* two-column zone */
.cols{display:grid;grid-template-columns:minmax(0,1fr) 322px;gap:20px;margin-top:20px;align-items:start;}
.main-col,.rail-col{display:flex;flex-direction:column;gap:20px;min-width:0;}

/* ladder */
.lrow{position:relative;display:grid;grid-template-columns:84px 18px minmax(0,1fr) auto;gap:0 10px;align-items:center;
  padding:9px 6px;border-radius:12px;transition:background .18s;}
.lrow:hover{background:color-mix(in srgb,var(--panel2) 55%,transparent);}
.lrow .lpx{font-size:13.5px;font-weight:700;text-align:right;}
.lrow .lnode{position:relative;display:flex;align-items:center;justify-content:center;height:100%;}
.lrow .lnode::before{content:"";position:absolute;top:-10px;bottom:-10px;width:2px;background:var(--hair);}
.lrow:first-child .lnode::before{top:50%;}
.lrow.llast .lnode::before{bottom:50%;}
.ldot{position:relative;z-index:1;width:9px;height:9px;border-radius:50%;background:var(--muted);box-shadow:0 0 0 3px var(--panel);}
.ldot.wall{background:transparent;border:2px solid var(--muted);width:8px;height:8px;}
.ldot.chase{background:var(--warn);}
.ldot.buy{background:var(--up);}
.ldot.stop{background:var(--down);}
.lrow .lk{font-size:12.5px;font-weight:600;line-height:1.25;}
.lrow .lk small{display:block;font-size:11px;font-weight:500;color:var(--muted);margin-top:1px;}
.lrow .ld{font-size:11.5px;font-weight:650;color:var(--muted);}
.lrow.now{background:color-mix(in srgb,var(--stance) 8%,var(--panel2));
  border:1px solid color-mix(in srgb,var(--stance) 26%,transparent);box-shadow:0 8px 22px -12px rgba(3,7,18,.8);
  margin:3px -2px;padding:11px 8px;}
.lrow.now .lpx{font-size:15px;color:var(--text);}
.lrow.now .ldot{background:var(--stance);box-shadow:0 0 0 3px var(--panel),0 0 10px color-mix(in srgb,var(--stance) 60%,transparent);}
.lband{position:relative;margin:2px 0 2px 102px;padding:7px 12px;border-radius:10px;
  background:color-mix(in srgb,var(--up) 8%,transparent);border:1px dashed color-mix(in srgb,var(--up) 35%,transparent);
  font-size:11.5px;color:var(--ink-up, var(--up));font-weight:600;line-height:1.4;}

/* key-value rows */
.kv6 .r6{display:flex;justify-content:space-between;align-items:baseline;gap:12px;padding:7px 0;border-bottom:1px solid var(--hair);font-size:12.5px;}
.kv6 .r6:last-child{border-bottom:0;}
.kv6 .k6{color:var(--muted);}
.kv6 .v6{font-weight:650;text-align:right;}

/* technicals */
.trow{display:flex;align-items:center;gap:12px;padding:8px 0;border-bottom:1px solid var(--hair);font-size:12.5px;flex-wrap:wrap;}
.trow:last-child{border-bottom:0;}
.trow .tk{color:var(--muted);flex:0 0 130px;}
.trow .tv6{font-weight:650;}
.minitrack{position:relative;flex:1;max-width:150px;min-width:90px;height:5px;border-radius:3px;background:var(--line);}
.minitrack .zone{position:absolute;top:0;bottom:0;left:30%;right:30%;background:color-mix(in srgb,var(--muted) 22%,transparent);border-radius:3px;}
.minitrack .mk{position:absolute;top:50%;transform:translate(-50%,-50%);width:2.5px;height:13px;border-radius:2px;background:var(--warn);}
.okx{display:inline-flex;align-items:center;gap:5px;font-size:11px;font-weight:700;padding:2px 9px;border-radius:999px;}
.okx.y{color:var(--ink-up, var(--up));background:color-mix(in srgb,var(--up) 12%,transparent);}
.okx.n{color:var(--ink-down, var(--down));background:color-mix(in srgb,var(--down) 12%,transparent);}
.okx.w{color:var(--ink-warn, var(--warn));background:color-mix(in srgb,var(--warn) 12%,transparent);}
.okx.q{color:var(--muted);background:color-mix(in srgb,var(--muted) 12%,transparent);}
.sigchips{display:flex;flex-wrap:wrap;gap:6px;margin-bottom:12px;}

/* earnings */
.earn-top{display:grid;grid-template-columns:auto minmax(0,1fr);gap:20px;align-items:center;}
/* Countdown ring. "DAYS TO GO" is 10 uppercase characters — it does not fit
   inside a 78px inner disc, and used to sit on the ring stroke. The ring now
   holds the number alone (which can therefore be bigger) and the label sits
   under it, outside the circle. */
.cring-wrap{display:flex;flex-direction:column;align-items:center;gap:9px;flex-shrink:0;}
.cring-lbl{font-size:10px;font-weight:680;letter-spacing:.07em;text-transform:uppercase;color:var(--muted);white-space:nowrap;}
[data-lang="zh"] .cring-lbl{letter-spacing:0;text-transform:none;font-size:11px;}
.cring{position:relative;width:92px;height:92px;border-radius:50%;display:flex;align-items:center;justify-content:center;flex-shrink:0;
  background:conic-gradient(var(--link) calc(var(--p)*1%),color-mix(in srgb,var(--line) 60%,transparent) 0);}
.cring::before{content:"";position:absolute;inset:7px;border-radius:50%;background:var(--panel);}
.cring .cd{position:relative;text-align:center;line-height:1.05;}
.cring .cd b{font-size:31px;font-weight:800;letter-spacing:-.03em;}
/* Surprise bars. The old fixed 78px height was smaller than the column it
   contained (a 56px bar plus two label lines is ~89px), and because the row
   bottom-aligns, the overflow went UPWARD — the tallest bar drew straight over
   the post-earnings-drift line above it. No fixed height: the row grows to fit
   its tallest bar, and bottom-alignment still puts every bar on one baseline. */
.sbars{display:flex;align-items:flex-end;gap:10px;margin-top:15px;}
.sbar{flex:1;max-width:64px;display:flex;flex-direction:column;align-items:center;gap:5px;}
.sbar i{width:100%;max-width:44px;border-radius:5px 5px 2px 2px;background:linear-gradient(180deg,var(--up),color-mix(in srgb,var(--up) 45%,transparent));}
.sbar i.m{background:linear-gradient(180deg,var(--down),color-mix(in srgb,var(--down) 45%,transparent));}
.sbar b{font-size:11px;font-weight:700;}
.sbar span{font-size:10px;color:var(--muted);}

/* tables */
.vt-wrap{overflow-x:auto;-webkit-overflow-scrolling:touch;margin:0 -2px;padding:0 2px;}
.vt{width:100%;border-collapse:collapse;font-size:12.5px;}
.vt th{font-size:10.5px;font-weight:650;color:var(--muted);text-transform:uppercase;letter-spacing:.06em;text-align:left;padding:2px 8px 8px;border-bottom:1px solid var(--hair);}
.vt td{padding:8px;border-bottom:1px solid var(--hair);}
.vt tr:last-child td{border-bottom:0;}
.vt td:nth-child(n+2){text-align:right;}
.vt td.b{font-weight:700;}
.cheapbar{display:inline-flex;align-items:center;gap:8px;justify-content:flex-end;}
.cheapbar .cb{width:74px;height:5px;border-radius:3px;background:var(--line);overflow:hidden;}
.cheapbar .cb i{display:block;height:100%;border-radius:3px;}

/* financials */
/* Financial history cards. Six per-bar value labels at 8.5px never fit a
   third-width card: they overlapped each other AND punched up through the
   card's own eyebrow, because .fbars was 70px tall while the column inside it
   (label + 50px bar + year) needed ~79px and the row bottom-aligns. The card
   now leads with the figure that actually carries the story — the latest year,
   at a readable size — and the bars became the shape of the trend. Per-year
   numbers are still there, one hover away, which is where six of them belong. */
.fin3{display:grid;grid-template-columns:repeat(3,1fr);gap:12px;}
.fcard{background:color-mix(in srgb,var(--panel2) 55%,transparent);border:1px solid var(--hair);border-radius:13px;padding:13px 14px;min-width:0;}
.fcard .fhd{display:flex;align-items:baseline;justify-content:space-between;gap:9px;min-width:0;}
.fcard .ft{min-width:0;font-size:10.5px;font-weight:650;color:var(--muted);text-transform:uppercase;letter-spacing:.06em;
  overflow:hidden;text-overflow:ellipsis;white-space:nowrap;}
[data-lang="zh"] .fcard .ft{letter-spacing:0;text-transform:none;font-size:11.5px;}
.fcard .flast{flex:0 0 auto;display:inline-flex;align-items:baseline;gap:5px;}
.fcard .flast b{font-size:15.5px;font-weight:750;letter-spacing:-.02em;font-variant-numeric:tabular-nums;}
.fcard .flast em{font-style:normal;font-size:10px;color:var(--muted);font-variant-numeric:tabular-nums;}
.fcard .fbars{display:flex;align-items:flex-end;gap:5px;height:54px;margin-top:13px;}
.fcard .fb{position:relative;flex:1;min-width:0;display:flex;align-items:flex-end;}
.fcard .fb i{display:block;width:100%;border-radius:4px 4px 2px 2px;background:linear-gradient(180deg,var(--link),color-mix(in srgb,var(--link) 35%,transparent));}
.fcard .fb i.dn6{background:linear-gradient(180deg,var(--down),color-mix(in srgb,var(--down) 35%,transparent));}
.fcard .fb[data-v]::after{content:attr(data-y) " · " attr(data-v);position:absolute;left:50%;bottom:calc(100% + 7px);
  transform:translateX(-50%) translateY(3px);padding:3px 8px;border-radius:8px;
  background:var(--panel);border:1px solid var(--gbtn-brd);box-shadow:0 6px 18px -10px rgba(3,7,18,.7);
  font:650 10.5px/1 var(--font-ui);color:var(--text);white-space:nowrap;font-variant-numeric:tabular-nums;
  opacity:0;pointer-events:none;z-index:3;transition:opacity .16s ease,transform .16s ease;}
.fcard .fb[data-v]:hover::after{opacity:1;transform:translateX(-50%) translateY(0);}
.fcard .fspan{display:flex;justify-content:space-between;gap:8px;margin-top:8px;font-size:10px;color:var(--muted);font-variant-numeric:tabular-nums;}
.fcard .fcagr{margin-top:9px;padding-top:9px;border-top:1px solid var(--hair);font-size:11px;color:var(--muted);}
.fcard .fcagr b{color:var(--text);}
.mrow{display:flex;align-items:center;gap:10px;padding:5px 0;font-size:12px;}
.mrow .mk{color:var(--muted);flex:0 0 130px;}
.mrow .mtr{flex:1;height:5px;border-radius:3px;background:var(--line);overflow:hidden;}
.mrow .mtr i{display:block;height:100%;border-radius:3px;background:linear-gradient(90deg,color-mix(in srgb,var(--link) 50%,transparent),var(--link));}
.mrow .mv{width:52px;text-align:right;font-weight:650;font-size:12px;}
.hchips{display:flex;flex-wrap:wrap;gap:8px;margin-top:14px;}
.pio{display:inline-flex;align-items:center;gap:2.5px;padding:5px 10px;border-radius:9px;background:var(--gbtn-bg);border:1px solid var(--gbtn-brd);}
.pio b{font-size:11px;margin-right:5px;}
.pio i{width:9px;height:5px;border-radius:1.5px;background:var(--line);}
.pio i.on6{background:var(--up);}
.kv2{display:grid;grid-template-columns:1fr 1fr;gap:4px 26px;margin-top:14px;}
.kv2 .r6{display:flex;justify-content:space-between;align-items:baseline;gap:12px;padding:6px 0;border-bottom:1px solid var(--hair);font-size:12.5px;}
.kv2 .k6{color:var(--muted);}
.kv2 .v6{font-weight:650;text-align:right;}
.kv2 h4{grid-column:1/-1;margin:6px 0 2px;font-size:11px;font-weight:650;color:var(--muted);text-transform:uppercase;letter-spacing:.06em;}

/* why moving */
.why .wr{display:flex;gap:12px;align-items:flex-start;padding:8px 10px;border-radius:11px;font-size:12.5px;margin:2px -10px;}
.why .wr.wr-on{background:color-mix(in srgb,var(--up) 6%,transparent);}
.why .wd{width:8px;height:8px;border-radius:50%;background:var(--line);flex-shrink:0;margin-top:5px;}
.why .wr.wr-on .wd{background:var(--up);box-shadow:0 0 8px color-mix(in srgb,var(--up) 55%,transparent);}
.why .wc{flex:0 0 90px;color:var(--muted);font-size:11.5px;padding-top:1px;}
.why .wl{flex:1;line-height:1.5;min-width:200px;}

/* options tiles */
.otiles{display:grid;grid-template-columns:repeat(3,1fr);gap:10px;}
.otile{background:color-mix(in srgb,var(--panel2) 55%,transparent);border:1px solid var(--hair);border-radius:12px;padding:11px 13px;min-width:0;}
.otile .ok6{font-size:10.5px;font-weight:650;color:var(--muted);text-transform:uppercase;letter-spacing:.05em;}
.otile .ov{font-size:16.5px;font-weight:720;margin-top:4px;}
.otile .ob{position:relative;height:4px;border-radius:3px;background:var(--line);margin-top:8px;overflow:hidden;}
.otile .ob i{position:absolute;top:0;bottom:0;left:0;background:linear-gradient(90deg,color-mix(in srgb,var(--warn) 50%,transparent),var(--warn));border-radius:3px;}

/* holders */
.gchip{display:inline-flex;align-items:center;justify-content:center;width:22px;height:22px;border-radius:7px;font-size:11px;font-weight:800;background:var(--panel2);color:var(--muted);}
.gchip.A{background:color-mix(in srgb,var(--up) 16%,transparent);color:var(--ink-up, var(--up));}
.gchip.B{background:color-mix(in srgb,var(--warn) 16%,transparent);color:var(--ink-warn, var(--warn));}

/* timeline */
.tl{position:relative;padding-left:20px;}
.tl::before{content:"";position:absolute;left:5px;top:8px;bottom:8px;width:2px;background:var(--hair);}
.tev{position:relative;padding:7px 0 7px 6px;}
.tev::before{content:"";position:absolute;left:-19px;top:13px;width:8px;height:8px;border-radius:50%;background:var(--muted);box-shadow:0 0 0 3px var(--panel);}
.tev.up::before{background:var(--up);}
.tev.down::before{background:var(--down);}
.tev.caution::before{background:var(--warn);}
.tev .td{font-size:11px;color:var(--muted);font-weight:600;}
.tev .th{font-size:12.5px;font-weight:600;margin-top:1px;}
.tev .tx{font-size:11.5px;color:var(--muted);margin-top:1px;line-height:1.45;}
.pinned6{background:color-mix(in srgb,var(--warn) 7%,transparent);border:1px solid color-mix(in srgb,var(--warn) 26%,transparent);
  border-radius:12px;padding:11px 14px;margin-bottom:12px;font-size:12.5px;}
.pinned6 .ph{display:block;font-weight:700;}
.pinned6 .pd{color:var(--muted);font-size:11.5px;}

/* style DNA + watch items */
.dna .dr{display:flex;align-items:center;gap:12px;padding:6px 0;font-size:12.5px;}
.dna .dk{flex:0 0 118px;color:var(--muted);}
.dna .dtr{position:relative;flex:1;height:6px;border-radius:4px;background:var(--line);}
.dna .dtr::before{content:"";position:absolute;left:50%;top:-3px;bottom:-3px;width:1.5px;background:color-mix(in srgb,var(--muted) 45%,transparent);}
.dna .dtr i{position:absolute;top:0;bottom:0;border-radius:4px;}
.dna .dtr i.p{left:50%;background:linear-gradient(90deg,color-mix(in srgb,var(--up) 45%,transparent),var(--up));}
.dna .dtr i.n{right:50%;background:linear-gradient(270deg,color-mix(in srgb,var(--down) 45%,transparent),var(--down));}
.dna .dw{flex:0 0 96px;text-align:right;font-size:11.5px;font-weight:650;}
.watch{list-style:none;padding:0;margin:8px 0 0;}
.watch li{margin:5px 0;font-size:12.5px;color:var(--muted);line-height:1.5;padding-left:18px;position:relative;}
.watch li::before{content:"⚠";position:absolute;left:0;font-size:11px;color:var(--ink-warn, var(--warn));}
.subhd{font-size:11px;font-weight:650;color:var(--muted);text-transform:uppercase;letter-spacing:.06em;margin:14px 0 6px;}

/* seasonality */
.seas{display:grid;grid-template-columns:repeat(6,1fr);gap:5px;}
.seas .sc{aspect-ratio:1.25/1;border-radius:9px;display:flex;flex-direction:column;align-items:center;justify-content:center;gap:1px;
  background:var(--panel2);font-size:11px;font-weight:700;}
.seas .sc span{font-size:9px;font-weight:600;color:var(--muted);}
.seas .sc.cur{outline:2px solid var(--link);outline-offset:1px;}

/* news / themes / peers */
.nrow{padding:9px 0;border-bottom:1px solid var(--hair);font-size:12.5px;line-height:1.45;}
.nrow:last-child{border-bottom:0;}
.nrow a{font-weight:550;}
.nrow a:hover{color:var(--ink-link, var(--link));}
.nrow .nm{display:block;font-size:11px;color:var(--muted);margin-top:2px;}
.throw{display:flex;align-items:center;gap:10px;padding:8px 0;border-bottom:1px solid var(--hair);font-size:12.5px;flex-wrap:wrap;}
.throw:last-child{border-bottom:0;}
.throw a{font-weight:650;color:var(--ink-link, var(--link));}
.throw a:hover{text-decoration:underline;}
.throw .tm{flex:1;color:var(--muted);font-size:11.5px;min-width:120px;}
.peers{display:grid;grid-template-columns:repeat(2,1fr);gap:9px;}
.pcard{background:color-mix(in srgb,var(--panel2) 55%,transparent);border:1px solid var(--hair);border-radius:12px;padding:10px 12px;min-width:0;
  transition:transform .16s,border-color .16s,background .16s;}
.pcard:hover{transform:translateY(-2px);border-color:color-mix(in srgb,var(--link) 40%,var(--hair));background:color-mix(in srgb,var(--panel2) 80%,transparent);}
.pcard b{font-size:13.5px;font-weight:750;color:var(--ink-link, var(--link));}
.pcard span{display:block;font-size:11px;color:var(--muted);white-space:nowrap;overflow:hidden;text-overflow:ellipsis;}
.pcard small{display:block;font-size:10.5px;color:var(--muted);margin-top:3px;}
/* Peers come from factors.json, a WIDER universe than the pages we render, so a
   real same-sector name can have no dossier to open. It keeps its card — the
   name and the market cap are the point of the grid — but must not advertise a
   click it cannot honour: no link colour, no hover lift, no pointer. */
.pcard-off{cursor:default;}
.pcard-off:hover{transform:none;border-color:var(--hair);background:color-mix(in srgb,var(--panel2) 55%,transparent);}
.pcard-off b{color:var(--text);}

/* rail CTA + band */
.railcta{background:linear-gradient(150deg,color-mix(in srgb,var(--link) 14%,var(--panel)),color-mix(in srgb,var(--info) 7%,var(--panel)));
  border:1px solid color-mix(in srgb,var(--link) 30%,transparent);}
.railcta h3{margin:0 0 6px;font-size:14.5px;font-weight:750;}
.railcta p{margin:0 0 14px;font-size:12px;color:var(--muted);line-height:1.55;}
.cta-band{margin-top:26px;padding:30px 34px;display:flex;align-items:center;gap:26px;flex-wrap:wrap;
  background:linear-gradient(120deg,color-mix(in srgb,var(--link) 12%,var(--panel)) 0%,var(--glass-bg) 60%);
  border:1px solid color-mix(in srgb,var(--link) 26%,var(--glass-brd));}
.cta-band .cc{flex:1;min-width:250px;}
.cta-band h2{margin:0 0 6px;font-size:19px;font-weight:800;letter-spacing:-.02em;}
.cta-band p{margin:0;font-size:12.5px;color:var(--muted);line-height:1.55;}
.brief-list{margin:4px 0 0;padding-left:18px;}
.brief-list li{margin:3px 0;font-size:12.5px;line-height:1.5;}

/* footer */
.site-footer6{margin-top:34px;padding-top:18px;border-top:1px solid var(--hair);color:var(--muted);font-size:11.5px;line-height:1.7;}
.site-footer6 a{color:var(--ink-link, var(--link));}
.site-footer6 a:hover{text-decoration:underline;}


/* ── popup dashboards (dsr-dlg — macro.html mx5 mechanics) ── */
html.dsr-dlg-lock{overflow:hidden;}
html.dsr-dlg-lock .strip{visibility:hidden;}
html.dsr-dlg-lock body{overflow:hidden;position:fixed;width:100%;}
.dsr-dlg{display:none;position:fixed;inset:0;z-index:300;align-items:center;justify-content:center;}
.dsr-dlg.open{display:flex;}
.dsr-dlg-backdrop{position:absolute;inset:0;background:rgba(3,6,10,.58);backdrop-filter:blur(6px);-webkit-backdrop-filter:blur(6px);}
.dsr-dlg-panel{position:relative;z-index:1;max-width:880px;width:calc(100% - 48px);max-height:84vh;
  background:color-mix(in srgb,var(--panel) 94%,transparent);border:1px solid var(--glass-brd);border-radius:var(--v6-radius);
  backdrop-filter:blur(28px) saturate(1.4);-webkit-backdrop-filter:blur(28px) saturate(1.4);
  box-shadow:0 40px 120px rgba(0,0,0,.72),0 2px 0 rgba(255,255,255,.06) inset;overflow-y:auto;display:flex;flex-direction:column;}
.dsr-dlg-header{display:flex;align-items:center;justify-content:space-between;gap:12px;padding:16px 22px 13px;position:sticky;top:0;z-index:2;
  background:color-mix(in srgb,var(--panel) 96%,transparent);backdrop-filter:blur(20px);-webkit-backdrop-filter:blur(20px);border-bottom:1px solid var(--hair);}
.dsr-dlg-title{font-size:15px;font-weight:700;letter-spacing:-.01em;display:flex;align-items:center;gap:10px;}
.dsr-dlg-title::before{content:"";width:3px;height:15px;border-radius:2px;background:linear-gradient(180deg,var(--stance),color-mix(in srgb,var(--stance) 25%,transparent));}
.dsr-dlg-close{display:flex;align-items:center;justify-content:center;width:32px;height:32px;border-radius:50%;
  background:var(--gbtn-bg);border:1px solid var(--gbtn-brd);cursor:pointer;color:var(--muted);font-size:17px;line-height:1;font-family:inherit;flex-shrink:0;transition:background .15s,color .15s;}
.dsr-dlg-close:hover{background:var(--gbtn-bg-hover);color:var(--text);}
.dsr-dlg-body{padding:18px 22px 24px;flex:1;overflow-y:auto;overflow-x:hidden;}
.dpanel{margin:0 0 20px;}
.dpanel:last-child{margin-bottom:0;}
.dpanel h3{margin:0 0 10px;font-size:12px;font-weight:650;color:var(--muted);text-transform:uppercase;letter-spacing:.07em;}
.dpanel .dfoot{margin-top:8px;font-size:11.5px;color:var(--muted);line-height:1.5;}
/* macro PR 6920 round-4 review MAJOR-3 fix: round-3's `.dfoot .c` rule set
   only `color:var(--muted)`, identical to the `.dpanel .dfoot` color it
   inherits already — a no-op, so the code sat inline with the sentence,
   indistinguishable from the prose (each refusal/disclosure line also ran
   on into the next via " · ", a run-on paragraph). Each line now gets its
   own row (`<br>` in the template between items) and the code is a real
   bordered pill — same family as `.ci-tag`/`.ss-chip`/`.schip` elsewhere in
   this file, using the shared pill-radius token (`--r-pill`, same fallback
   convention `templates/reference.html.j2` already uses), a `--hair`
   border and a `--muted`-tinted fill, never a new palette or component. */
.dfoot-row{display:inline-block;}
.dfoot .c{display:inline-block;margin-left:6px;padding:1px 6px;border-radius:var(--r-pill,999px);
  border:1px solid var(--hair);background:color-mix(in srgb,var(--muted) 12%,transparent);
  color:var(--muted);font-size:9.5px;letter-spacing:.02em;}
.dcards{display:grid;grid-template-columns:repeat(auto-fit,minmax(150px,1fr));gap:10px;}
.dcard{background:color-mix(in srgb,var(--panel2) 55%,transparent);border:1px solid var(--hair);border-radius:12px;padding:11px 13px;}
.dcard .dk{font-size:10.5px;font-weight:650;color:var(--muted);text-transform:uppercase;letter-spacing:.05em;}
.dcard .dv{font-size:16px;font-weight:750;margin-top:4px;}
.dkv .r6d{display:flex;justify-content:space-between;align-items:baseline;gap:14px;padding:7px 0;border-bottom:1px solid var(--hair);font-size:12.5px;}
.dkv .r6d:last-child{border-bottom:0;}
.dkv .r6d .k{color:var(--muted);}
.dkv .r6d .vv{font-weight:650;text-align:right;}
.dtable{width:100%;border-collapse:collapse;font-size:12.5px;}
.dtable th{font-size:10.5px;font-weight:650;color:var(--muted);text-transform:uppercase;letter-spacing:.05em;text-align:right;padding:2px 8px 7px;border-bottom:1px solid var(--hair);white-space:nowrap;}
.dtable th:first-child{text-align:left;}
.dtable td{padding:7px 8px;border-bottom:1px solid var(--hair);text-align:right;font-variant-numeric:tabular-nums;}
.dtable td:first-child{text-align:left;color:var(--muted);}
.dtable tr:last-child td{border-bottom:0;}
.dtable-wrap{overflow-x:auto;margin:0 -4px;padding:0 4px;}
.dnotes{list-style:none;margin:0;padding:0;}
.dnotes li{font-size:12.5px;line-height:1.55;padding:5px 0 5px 16px;position:relative;}
.dnotes li::before{content:"·";position:absolute;left:2px;font-weight:900;}
@media(prefers-reduced-motion:no-preference){
  .dsr-dlg-backdrop{opacity:0;transition:opacity 240ms ease-out;}
  .dsr-dlg.dsr-in .dsr-dlg-backdrop{opacity:1;}
  .dsr-dlg-panel{opacity:0;transform:translateY(14px) scale(.965);transition:opacity 320ms cubic-bezier(.22,.9,.26,1.02),transform 320ms cubic-bezier(.22,.9,.26,1.02);}
  .dsr-dlg.dsr-in .dsr-dlg-panel{opacity:1;transform:translateY(0) scale(1);}
  .dsr-dlg.dsr-closing .dsr-dlg-backdrop,.dsr-dlg.dsr-closing .dsr-dlg-panel{transition-duration:180ms;}
  .dsr-dlg-body > .dpanel{opacity:0;transform:translateY(10px);transition:opacity 300ms ease-out,transform 300ms cubic-bezier(.22,.9,.26,1.02);}
  .dsr-dlg.dsr-in .dsr-dlg-body > .dpanel{opacity:1;transform:translateY(0);}
  .dsr-dlg.dsr-in .dsr-dlg-body > .dpanel:nth-child(1){transition-delay:60ms;}
  .dsr-dlg.dsr-in .dsr-dlg-body > .dpanel:nth-child(2){transition-delay:100ms;}
  .dsr-dlg.dsr-in .dsr-dlg-body > .dpanel:nth-child(n+3){transition-delay:140ms;}
  .dsr-closing .dsr-dlg-body > .dpanel{transition-delay:0ms!important;}
}
@media(max-width:700px){
  .dsr-dlg{align-items:flex-end;}
  .dsr-dlg-panel{width:calc(100% - 20px);max-width:none;max-height:88vh;border-radius:var(--v6-radius) var(--v6-radius) 0 0;margin:0 10px;}
  .dsr-dlg-close{width:42px;height:42px;font-size:21px;}
  .dsr-dlg-body{padding:15px 15px 24px;}
}

/* ── responsive (media queries AFTER base rules, always) ── */
@media(max-width:980px){.cols{grid-template-columns:1fr;}}
@media(max-width:860px){.hero-grid{grid-template-columns:1fr;gap:12px;}.px-block{text-align:left;}}
@media(max-width:760px){.chart-mod .mod-hd{flex-wrap:wrap;}}
@media(max-width:700px){
  .mod-hd{flex-wrap:wrap;row-gap:8px;}
  .mod-hd .hint{white-space:normal;flex-wrap:wrap;justify-content:flex-end;row-gap:6px;}
}
@media(max-width:480px){
  .why .wr{flex-wrap:wrap;}
  .why .wc{flex-basis:calc(100% - 20px);padding-top:0;}
  .why .wl{min-width:0;}
}
@media(max-width:640px){
  .page-wrap{padding:0 14px 44px;}
  .mod{padding:16px 15px 17px;border-radius:15px;}
  .hero{padding:20px 18px 0;}
  .stance-row{margin:18px -18px 0;padding:12px 18px;}
  .chart-mod .mod-hd{padding:13px 15px 10px;}
  .chart-ft{padding:9px 15px 12px;}
  .fin3{grid-template-columns:1fr;}
  .earn-top{grid-template-columns:1fr;}
  .kv2{grid-template-columns:1fr;}
  .otiles{grid-template-columns:repeat(2,1fr);}
}
</style>
</head>
<body class="st-{{ (hero.stance_class if hero and hero.stance_class else 'neu') }}">
<div class="aurora" aria-hidden="true"></div>
{% include "_site_nav.html.j2" %}
<main>

<!-- sticky jump strip (revealed after the hero scrolls past) -->
<div class="strip" id="v6strip" aria-hidden="true">
  <span class="stk">{{ ticker }}</span>
  <span class="sname mut sm">{{ name }}</span>
  {# dq-px, NOT nb-px: the dossier price is owned by dossier-live-quote.js and
     must not also be a target of the shared live.js poller. Two owners writing
     one node is a race whose loser is whichever fetch returns second. #}
  {% if hero and hero.price %}<span class="stp num">$<span class="dq-px" data-dq-sym="{{ ticker }}">{{ hero.price }}</span></span>{% endif %}
  {% if hero %}<span class="schip">{{ t(hero.stance_en, hero.stance_zh) }}</span>{% endif %}
  <nav id="v6jumps">
    <a href="#company-update">{{ t('Company', '公司动态') }}</a>
    <a href="#chart">{{ t('Chart', '图表') }}</a>
    {% if ladder %}<a href="#levels">{{ t('Levels', '点位') }}</a>{% endif %}
    {% if technicals %}<a href="#technicals">{{ t('Technicals', '技术面') }}</a>{% endif %}
    {% if earnings %}<a href="#earnings">{{ t('Earnings', '财报') }}</a>{% endif %}
    {% if valuation %}<a href="#valuation">{{ t('Valuation', '估值') }}</a>{% endif %}
    {% if financials %}<a href="#financials">{{ t('Financials', '财务') }}</a>{% endif %}
    {# META-CEO B r4 MAJOR-1 (amended to match the panel, with one ruled
       asymmetry). Chip AND section for: reported (the loaded/success status),
       no_filings, not_loaded, identity_mismatch, no_maturity_facts.
       unresolved: the section's no-record sentence WITHOUT a chip (ruled
       asymmetry — an identity gap has nothing pending to jump to).
       not_applicable: neither chip nor section. Tests pin all seven. #}
    {% if debt_maturity and debt_maturity.status not in ('not_applicable', 'unresolved') %}<a href="#debt-maturity">{{ t('Debt', '债务') }}</a>{% endif %}
    {% if options %}<a href="#options">{{ t('Options', '期权') }}</a>{% endif %}
    {% if ownership %}<a href="#ownership">{{ t('Holders', '持仓') }}</a>{% endif %}
    {% if news %}<a href="#news">{{ t('News', '新闻') }}</a>{% endif %}
  </nav>
  <a class="gbtn gbtn-sm" href="{{ terminal_url }}" rel="noopener noreferrer" target="_blank">{{ t('Terminal', '终端') }} ↗</a>
</div>

<div class="page-wrap">
{% set deep_ids = [] %}
{% if deep %}{% for d in deep.dialogs %}{% set _ = deep_ids.append(d.id) %}{% endfor %}{% endif %}

<p class="crumb">
  <a href="index.html">{{ t('Stocks', '股票') }}</a> <span class="mut">/</span>
  {% if hero and hero.sector %}<span class="mut">{{ hero.sector }}</span> <span class="mut">/</span>{% endif %}
  <b>{{ ticker }}</b>
</p>

<!-- ═ HERO ═ -->
<header class="mod hero" id="top">
  <div class="hero-grid">
    <div>
      <div class="eyebrow hi">{{ name }}{% if hero and hero.sector %} · {{ hero.sector }}{% endif %}</div>
      <h1 class="hi hi2"><a href="{{ terminal_url }}" rel="noopener noreferrer" target="_blank">{{ ticker }}</a>
        <span class="co">{{ t('US listed · USD', '美股 · 美元') }}</span></h1>
      {% if hero and hero.desc_en %}
      <p class="hero-desc hi hi3">{{ t(hero.desc_en, hero.desc_zh) }}</p>
      {% endif %}
      {% if hero %}
      {# Chip order runs from the fact every reader wants first (size) to the one
         that only some readers act on (the factor archetype), which is why the
         archetype chip is last. #}
      <div class="chips hi hi3">
        {% if stats and stats.mktcap %}<span class="chip6">{{ t(hero.mktcap_label_en or 'Market cap', hero.mktcap_label_zh or '市值') }} · {{ stats.mktcap }}</span>
        {% elif hero.mktcap_label_en %}<span class="chip6">{{ t(hero.mktcap_label_en, hero.mktcap_label_zh) }}</span>{% endif %}
        {% if hero.sector %}<span class="chip6">{{ hero.sector }}</span>{% endif %}
        {% if hero.index_chips %}{% for c in hero.index_chips %}<span class="chip6">{{ t(c.en, c.zh) }}</span>{% endfor %}{% endif %}
        {% if hero.arch_label_en %}<span class="chip6">{{ t(hero.arch_label_en, hero.arch_label_zh) }}</span>{% endif %}
      </div>
      {% endif %}
    </div>
    <div class="px-block hi hi2">
      <div class="px-big num">
        {%- if hero and hero.price -%}
        $<span class="dq-px" data-dq-sym="{{ ticker }}">{{ hero.price }}</span>
        {%- else -%}—{%- endif -%}
      </div>
      {# The day move repaints WITH the price from one quote. It used to be
         static HTML beside a live-looking price, so a repaint could leave a
         current price next to yesterday's move — the two must move together
         or not at all. The pip that used to sit here was decorative (it keyed
         off the BUILD's date); freshness is now stated once, on the stamp. #}
      {% if hero and hero.chg %}
      <div class="px-chg {{ 'pos' if hero.chg.pos else 'neg' }}" data-dq-chg>
        <span class="pill6 num"><span data-dq-abs>{{ hero.chg.abs }}</span> · <span data-dq-pct>{{ hero.chg.pct }}</span></span>
      </div>
      {% endif %}
      {% if hero and hero.range52 %}
      <div class="wk52">
        <div class="lbls"><span>{{ t('52w low', '52周低') }} <b class="num">{{ hero.range52.lo }}</b></span><span>{{ t('high', '高') }} <b class="num">{{ hero.range52.hi }}</b></span></div>
        {# The bar repaints with the price. Left baked it would place the NEW
           price at the OLD point on the scale — the one element whose whole job
           is "where in the range are we", answering it wrong. #}
        <div class="track" data-dq-range{% if hero.range52.lo_raw is not none %} data-dq-lo="{{ hero.range52.lo_raw }}" data-dq-hi="{{ hero.range52.hi_raw }}"{% endif %}><div class="fill" data-dq-fill style="width:{{ hero.range52.pos_pct }}%;"></div><div class="dotp" data-dq-dot style="left:{{ hero.range52.pos_pct }}%;"></div></div>
      </div>
      {% endif %}
    </div>
  </div>
  {% if hero %}
  <div class="stance-row hi hi4">
    <span class="stance-big">{{ t(hero.stance_en, hero.stance_zh) }}</span>
    {% if hero.inv_en %}
    <span class="stance-line"><span class="l-en">{{ hero.inv_en }}</span><span class="l-zh">{{ hero.inv_zh }}</span></span>
    {% endif %}
    {# The stamp says whether this read is current, not which machine produced
       it — "Engine read" named the plumbing. Fresh reads carry the same live
       pip as the price above; a stale one still prints its date, because that
       is the case where the date is the whole point. #}
    {%- if security_state %}
    {# Dominant degradation, additive: one chip that jumps to the section it
       summarises. It prints on a clean read too — "checked and current" and
       "never checked" must not look the same from the hero. #}
    <a class="ss-hchip ss-t-{{ security_state.degradation.tone }}" href="#security-state">{{ t(security_state.degradation.en, security_state.degradation.zh) }}</a>
    {%- endif %}
    {% if stale %}<span class="asof wrn">{{ t('May be stale', '可能过期') }} · {{ freshness }}</span>
    {# This said "Live" unconditionally, decided by the BUILD's own date: a page
       rendered last night printed a pulsing green "Live" next to a baked price
       that was a full session old. It now ships in the honest `baked` state —
       naming the date of the bytes actually served — and is upgraded ONLY by a
       measured quote, by dossier-live-quote.js. With JS off it stays truthful. #}
    {% else %}<span class="livestamp dq-stamp" data-dq-stamp data-dq-state="baked"><span class="live-dot"></span>{{ t('As of ' ~ freshness, '截至 ' ~ freshness) }}</span>{% endif %}
  </div>
  {% endif %}
</header>

<!-- ═ COMPANY INTELLIGENCE — verified dynamic context, context-only authority ═ -->
<section class="mod ci-mod rv" id="company-update" data-company-intelligence data-ticker="{{ ticker }}" aria-labelledby="ci-title">
  <div class="ci-top">
    <div>
      <div class="ci-kicker"><span class="ci-orbit" aria-hidden="true"></span>{{ t('Company intelligence', '公司情报') }}</div>
      <h2 id="ci-title">{{ t('The latest call, in context', '把最新财报放回历史脉络') }}</h2>
      <p>{{ t('A synthesized event record across management commentary and recorded metrics. Read as context; verify exact wording in the transcript before relying.', '综合管理层表述与记录指标形成事件档案。仅供参考；使用前请在电话会原文中核对准确措辞。') }}</p>
    </div>
    <span class="ci-state" id="ci-state" aria-live="polite">{{ t('Checking latest record', '正在检查最新记录') }}</span>
  </div>

  <div class="ci-loading" id="ci-loading" aria-hidden="true" hidden>
    <div><div class="ci-skel big"></div><div class="ci-skel med"></div><div class="ci-skel sm"></div></div>
    <div><div class="ci-skel big"></div><div class="ci-skel med"></div></div>
  </div>

  <div class="ci-empty" id="ci-empty">
    <h3 id="ci-empty-title">{{ t('Live company record', '实时公司记录') }}</h3>
    <p id="ci-empty-copy">{{ t('JavaScript loads the source-bounded event record here. Open Terminal if the live module does not start.', 'JavaScript 会在此加载受来源约束的事件记录。如实时模块未启动，请打开终端。') }}</p>
    <a class="gbtn gbtn-cta ci-upgrade" id="ci-terminal-upgrade-empty" href="{{ intelligence_upgrade_url }}" rel="noopener noreferrer" target="_blank">{{ t('Continue with full transcript history', '继续查看完整电话会历史') }} ↗</a>
  </div>

  <div id="ci-content" hidden>
    <div class="ci-grid">
      <div class="ci-story">
        <div class="ci-period" id="ci-period"></div>
        <div id="ci-v2-host" hidden></div>
        <h3 class="ci-summary" id="ci-summary"></h3>
        <div class="ci-lenses">
          <article class="ci-lens strength">
            <div class="ci-lens-label">{{ t('Positive context', '积极背景') }}</div>
            <p id="ci-strength"></p>
          </article>
          <article class="ci-lens pressure">
            <div class="ci-lens-label">{{ t('Pressure context', '压力背景') }}</div>
            <p id="ci-pressure"></p>
          </article>
        </div>
        <div class="ci-tags" id="ci-tags" aria-labelledby="ci-title"></div>
      </div>
      <aside class="ci-side" aria-labelledby="ci-metrics-label">
        <div>
          <div class="ci-side-label" id="ci-metrics-label">{{ t('Recorded metrics', '记录指标') }}</div>
          <div class="ci-metrics" id="ci-metrics"></div>
        </div>
        <div class="ci-next">
          <div class="ci-side-label">{{ t('What to inspect next', '下一步核查什么') }}</div>
          <p id="ci-next-copy"></p>
        </div>
        <div class="ci-actions">
          <a class="gbtn gbtn-cta ci-upgrade" id="ci-terminal-upgrade" href="{{ intelligence_upgrade_url }}" rel="noopener noreferrer" target="_blank">{{ t('Continue with full transcript history', '继续查看完整电话会历史') }} ↗</a>
          <a class="gbtn" id="ci-transcript" href="{{ terminal_url }}&pane=transcripts" rel="noopener noreferrer" target="_blank">{{ t('Open transcript', '打开电话会原文') }} ↗</a>
          <a class="gbtn" id="ci-earnings-record" href="earnings/">{{ t('Browse earnings records', '浏览财报记录') }}</a>
        </div>
      </aside>
    </div>
    <div class="ci-history" id="ci-history" hidden>
      <span class="ci-history-label" id="ci-history-label">{{ t('History mode', '历史模式') }}</span>
      <div class="ci-history-tabs" id="ci-history-tabs" role="toolbar" aria-labelledby="ci-history-label"></div>
      <span class="ci-receipt" id="ci-receipt"></span>
      <span class="ci-sr" id="ci-announcer" aria-live="polite"></span>
    </div>
    <div class="ci-foot">
      <span><b>{{ t('Context only', '仅供参考') }}</b> · {{ t('not a signal or recommendation', '不构成信号或建议') }}</span>
      <span class="ci-note" id="ci-foot-note">{{ t('Narrative claims may await line-level citation; verify in the transcript.', '叙述性内容可能仍待逐行引用，请在电话会原文中核对。') }}</span>
    </div>
  </div>
</section>

<!-- ═ CHART — full content width; Mastermind Terminal embed ═ -->
<section class="mod chart-mod rv" id="chart">
  <div class="mod-hd">
    <h2>{{ t('Price chart', '价格图表') }}</h2>
    <div class="hint">
      <span>{{ t('daily · interactive', '日线 · 可交互') }}</span>
      <a class="gbtn gbtn-sm" href="{{ terminal_url }}" rel="noopener noreferrer" target="_blank">{{ t('Open in Terminal', '打开终端') }} ↗</a>
    </div>
  </div>
  <div class="chart-frame" id="chart-frame">
    <div class="chart-skel">{{ t('Loading chart…', '图表加载中…') }}</div>
    <iframe id="chart-embed" title="{{ ticker }} interactive price chart"
      loading="eager" allowtransparency="true" referrerpolicy="strict-origin-when-cross-origin"
      data-embed="https://app.mastermind-x.com/embed/chart?symbol={{ ticker }}&transparent=1&range=1Y"></iframe>
    <div class="chart-fallback">
      <span>{{ t('The interactive chart could not load here.', '互动图表暂时无法在此加载。') }}</span>
      <a class="gbtn" href="{{ terminal_url }}" rel="noopener noreferrer" target="_blank">{{ t('Open the live chart in Terminal', '在终端中打开实时图表') }} ↗</a>
    </div>
    <noscript><div class="chart-fallback" style="display:flex;position:static;height:100%;">
      <a class="gbtn" href="{{ terminal_url }}" rel="noopener noreferrer" target="_blank">{{ t('Open the live chart in Terminal', '在终端中打开实时图表') }} ↗</a>
    </div></noscript>
  </div>
  <div class="chart-ft">
    <span class="sm mut">{{ t('Candles, volume and moving averages — pan and zoom inside the chart.', '蜡烛图、成交量与均线 — 图内可缩放拖动。') }}</span>
    <a class="powered" href="{{ terminal_url }}" rel="noopener noreferrer" target="_blank"><span class="bolt">M</span>{{ t('Mastermind Terminal — live & interactive', 'Mastermind 终端 — 实时互动') }} ↗</a>
  </div>
</section>

{%- if security_state %}{% set ss = security_state %}

<!-- ═ SECURITY STATE — six separate reads, each carrying its own coverage ═ -->
<section class="mod ss-mod rv" id="security-state" aria-labelledby="ss-title">
  <div class="mod-hd">
    <h2 id="ss-title">{{ t('Security state', '证券状态') }}</h2>
    <span class="hint">{{ t('six reads', '六项读数') }}</span>
  </div>
  <p class="ss-sub">{{ t('Six separate reads on this listing — what each one rests on, and how old it is. Use it to check the rest of the page. Not a buy or sell signal, and never blended into a score.', '对该证券的六项独立读数 — 每项的依据来源与时效。可用于核对本页其余内容。不构成买卖信号，也不会合并为任何评分。') }}</p>

  {# One flag, at most. A read that could not be built outranks an identity
     problem; both name what still works, so the reader never has to guess how
     far the failure reaches. #}
  {% if ss.degradation.failed %}
  {# The last-good citation is gated on a read that actually succeeded: a
     recorded fallback whose own compile failed is not something to fall back
     on, and naming it would promise the reader a version that never existed. #}
  <p class="ss-flag ss-t-act"><b>{{ t('This read could not be built', '本次读数无法生成') }}</b>{{ t('The six reads below could not be compiled from their sources this run. The price, chart and company sections above are unaffected.', '本次运行未能从来源生成下方六项读数。上方的价格、图表与公司板块不受影响。') }}{% if ss.last_good.ok %} {{ t('Last complete read:', '上次完整读数：') }} {{ ss.last_good.at }}{% if ss.last_good.deg and ss.last_good.deg.code != 'NONE' %} — {{ t(ss.last_good.deg.en|lower, ss.last_good.deg.zh) }}{% endif %}.{% endif %}</p>
  {% elif not ss.identity.ok %}
  <p class="ss-flag ss-t-{{ ss.identity.tone }}"><b>{{ t(ss.identity.en, ss.identity.zh) }}</b>{{ t(ss.identity.why_en, ss.identity.why_zh) }}</p>
  {% endif %}

  <div class="ss-grid">
    {% for a in ss.axes %}
    <article class="ss-cell ss-t-{{ a.tone }} ss-r-{{ a.rail }}{{ ' is-ok' if a.ok }}">
      <h3 class="ss-axis"><span>{{ t(a.title_en, a.title_zh) }}</span><span class="ss-chip">{{ t(a.cov_en, a.cov_zh) }}</span></h3>
      <p class="ss-head">{{ t(a.headline.en, a.headline.zh) }}</p>
      {% if a.clause %}<p class="ss-body">{{ t(a.clause.en, a.clause.zh) }}</p>{% endif %}
      {% if a.why %}<p class="{{ 'ss-why' if a.clause else 'ss-body' }}">{{ t(a.why.en, a.why.zh) }}</p>{% endif %}

      {# The strongest unresolved fact sits ahead of every secondary block in
         DOM order, so the mobile column needs no reordering to satisfy it. #}
      {# Direction is a second quoted field, not a second opinion — one short
         row, and only when the page can say it in both languages. #}
      {% if a.direction %}
      <div class="ss-note"><b>{{ t('Direction', '方向') }}</b><span class="v">{{ t(a.direction.en, a.direction.zh) }}</span></div>
      {% endif %}
      {% if a.subreads %}
      <div class="ss-note"><b>{{ t('Reads behind this', '构成这项的读数') }}</b>
        <div class="ss-subs">{% for s in a.subreads %}<span class="ss-sub-r ss-t-{{ s.tone }} ss-r-{{ s.rail }}"><span class="n">{{ t(s.en, s.zh) }}</span><span class="s">{{ t(s.cov_en, s.cov_zh) }}</span></span>{% endfor %}</div>
      </div>
      {% endif %}
      {% if a.unresolved %}
      <div class="ss-note"><b>{{ t('Biggest open question', '最大未决问题') }}<span class="ss-prov">{{ t('worked out here', '本页推算') }}</span></b><span class="v">{{ t(a.unresolved.en, a.unresolved.zh) }}</span></div>
      {% endif %}
      {% if a.gates %}
      <div class="ss-note"><b>{{ t('Checks not cleared', '未通过的检查') }}</b>
        <ul class="ss-list">{% for g in a.gates[:2] %}<li>{{ t(g.en, g.zh) }}</li>{% endfor %}</ul>
        {% if a.gates|length > 2 %}<span class="ss-more">+{{ a.gates|length - 2 }} {{ t('more, with codes, in receipts', '项（含代码）见凭证') }}</span>{% endif %}
      </div>
      {% endif %}
      {% if a.observables %}
      <div class="ss-note"><b>{{ t('Next observable', '下一个可观测点') }}</b>
        <ul class="ss-list">{% for o in a.observables[:3] %}<li>{{ t(o.en, o.zh) }}{% if o.when %} <span class="mut ss-when">· {{ o.when }}</span>{% endif %}{% if o.est %}<span class="ss-est">{{ t('Estimated window — not an announced date', '预计窗口 — 并非官方公布日期') }}</span>{% endif %}</li>{% endfor %}</ul>
      </div>
      {% endif %}
      {% if a.warnings %}
      <div class="ss-note"><b>{{ t('Noted while building', '生成过程中的提示') }}</b>
        <ul class="ss-list">{% for w in a.warnings[:2] %}<li>{{ t(w.en, w.zh) }}</li>{% endfor %}</ul>
      </div>
      {% endif %}

      {# One drill target per card, and it is the whole receipt line: a bigger
         hit area than a pill, no six-times-repeated button, and the clock sits
         inside the control that explains where the clock came from. #}
      <button type="button" class="ss-foot" onclick="dsrOpenDlg('dlg-{{ a.dlg }}')">
        {% if a.asof %}<span class="ss-clock">{{ t('as of', '截至') }} {{ a.asof }}</span>{% endif %}
        <span class="ss-go">{{ t('Receipts', '凭证') }}<span class="ci-sr"> — {{ t(a.title_en, a.title_zh) }}</span></span>
      </button>
    </article>
    {% endfor %}
  </div>

  <div class="ss-ev">
    <span class="ss-ev-label">{{ t('Evidence & receipts', '证据与凭证') }}</span>
    <span class="ss-tally">
      {% for b in ss.tally %}<span class="ss-t-{{ b.tone }}"><i class="ss-tick ss-r-{{ b.rail }}" aria-hidden="true"></i><b>{{ b.n }}</b> {{ t(b.en|lower, b.zh) }}</span>{% endfor %}
      <span class="ss-prov">{{ t('counted here', '本页统计') }}</span>
    </span>
    {% if ss.frontier_at %}<span class="ss-clock">{{ t('sources through', '来源截至') }} {{ ss.frontier_at }}</span>{% endif %}
    <button type="button" class="gbtn gbtn-sm" onclick="dsrOpenDlg('dlg-ss-evidence')">{{ t('Open evidence', '查看证据') }}</button>
  </div>
</section>
{%- endif %}

<!-- ═ MAIN + RAIL (rail begins under the chart) ═ -->
<div class="cols">
  <div class="main-col">

  {% if technicals %}
  <section class="mod rv" id="technicals">
    <div class="mod-hd"><h2>{{ t('Technicals', '技术面') }}</h2><span class="hint">{{ t('daily bars', '日线数据') }} · {{ freshness }} {% if 'technicals' in deep_ids %}<button class="gbtn gbtn-sm" onclick="dsrOpenDlg('dlg-technicals')">{{ t('Full detail', '完整数据') }}</button>{% endif %}</span></div>
    {% if technicals.active_signals %}
    {%- set shortsigs = [] -%}
    {%- for sig in technicals.active_signals -%}
      {%- if sig|length <= 42 and shortsigs|length < 4 -%}{%- set _ = shortsigs.append(sig) -%}{%- endif -%}
    {%- endfor -%}
    {% if shortsigs %}<div class="sigchips">{% for sig in shortsigs %}<span class="okx q">{{ sig }}</span>{% endfor %}</div>{% endif %}
    {% endif %}
    {% if technicals.rsi %}
    {%- set rsiv = technicals.rsi|float(50) -%}
    <div class="trow">
      <span class="tk">{{ t('Momentum (RSI 14)', '动量（RSI 14）') }}</span>
      <span class="tv6 num">{{ technicals.rsi }}</span>
      <div class="minitrack"><div class="zone"></div><div class="mk" style="left:{{ '%.0f'|format([[rsiv, 100]|min, 0]|max) }}%;"></div></div>
      {% if technicals.rsi_zone_en %}<span class="okx {{ 'n' if technicals.rsi_zone_en == 'Overbought' else ('y' if technicals.rsi_zone_en == 'Oversold' else 'q') }}">{{ t(technicals.rsi_zone_en, technicals.rsi_zone_zh) }}</span>{% endif %}
    </div>
    {% endif %}
    {% if technicals.ma50_en %}
    <div class="trow">
      <span class="tk">{{ t('vs 50-day average', '相对50日均线') }}</span>
      <span class="tv6 num {{ 'pos' if technicals.above50 else 'neg' }}">{{ technicals.ma50_en }} {{ t('above' if technicals.above50 else 'below', '上方' if technicals.above50 else '下方') }}</span>
      <span class="okx {{ 'y' if technicals.above50 else 'n' }}">{{ t('Uptrend intact' if technicals.above50 else 'Below trend', '趋势完好' if technicals.above50 else '跌破趋势') }}</span>
    </div>
    {% endif %}
    {% if technicals.ma200_en %}
    <div class="trow">
      <span class="tk">{{ t('vs 200-day average', '相对200日均线') }}</span>
      <span class="tv6 num {{ 'pos' if technicals.above200 else 'neg' }}">{{ technicals.ma200_en }} {{ t('above' if technicals.above200 else 'below', '上方' if technicals.above200 else '下方') }}</span>
      <span class="okx {{ 'y' if technicals.above200 else 'n' }}">{{ t('Long trend up' if technicals.above200 else 'Long trend down', '长期向上' if technicals.above200 else '长期向下') }}</span>
    </div>
    {% endif %}
    {% if technicals.adx %}
    <div class="trow">
      <span class="tk">{{ t('Trend strength (ADX)', '趋势强度（ADX）') }}</span>
      <span class="tv6 num">{{ technicals.adx }}</span>
      {% if technicals.adx_word_en %}<span class="mut sm">{{ t(technicals.adx_word_en, technicals.adx_word_zh) }}</span>{% endif %}
    </div>
    {% endif %}
    {% if technicals.hv_pctile %}
    <div class="trow">
      <span class="tk">{{ t('Volatility', '波动率') }}</span>
      <span class="tv6 num">{% if technicals.hv_pctile_num %}<span class="l-en">{{ technicals.hv_pctile_num }}th percentile</span><span class="l-zh">第{{ technicals.hv_pctile_num }}百分位</span>{% else %}{{ technicals.hv_pctile }}{% endif %}</span>
    </div>
    {% endif %}
    {% if technicals.squeeze_en %}
    <div class="trow">
      <span class="tk">{{ t('Volatility squeeze', '波动压缩') }}</span>
      <span class="okx w">{{ t(technicals.squeeze_en, technicals.squeeze_zh) }}</span>
    </div>
    {% endif %}
    <div class="mod-ft">{{ t('These readings feed the stance at the top — one source per number, refreshed nightly.', '以上读数构成页首的引擎立场 — 每个数字只有一个来源，每晚更新。') }}</div>
  </section>
  {% endif %}

  {% if earnings %}
  <section class="mod rv" id="earnings">
    <div class="mod-hd"><h2>{{ t('Earnings', '财务业绩') }}</h2>
      <span class="hint">{% if earnings.next_date %}{{ t('next report', '下次财报') }} {{ earnings.next_date }} {% endif %}{% if 'earnings' in deep_ids %}<button class="gbtn gbtn-sm" onclick="dsrOpenDlg('dlg-earnings')">{{ t('Full detail', '完整数据') }}</button>{% endif %}</span></div>
    <div class="earn-top">
      {% if earnings.days_to_next is not none and earnings.days_to_next >= 0 %}
      {%- set ep = 100 - ([[earnings.days_to_next, 45]|min, 0]|max / 45 * 100) -%}
      <div class="cring-wrap">
        <div class="cring" style="--p:{{ '%.0f'|format(ep) }};"><div class="cd"><b class="num">{{ earnings.days_to_next }}</b></div></div>
        <span class="cring-lbl">{{ t('days to go', '天后公布') }}</span>
      </div>
      {% endif %}
      <div>
        {% if earnings.last_date and (earnings.days_to_next is none or earnings.days_to_next < 0) %}
        <p class="sm mut" style="margin:0 0 6px;">{{ t('Last reported', '上次报告') }} {{ earnings.last_date }}</p>
        {% endif %}
        {% if earnings.rev_en %}<p class="sm" style="margin:0 0 4px;">{{ t(earnings.rev_en, earnings.rev_zh) }}</p>{% endif %}
        {% if earnings.sue_en %}<p class="sm" style="margin:0 0 4px;">{{ t(earnings.sue_en, earnings.sue_zh) }}</p>{% endif %}
        {% if earnings.pead_en %}<p class="sm mut" style="margin:0;">{{ t(earnings.pead_en, earnings.pead_zh) }}</p>{% endif %}
        {% if earnings.surprises %}
        {%- set smax = namespace(v=0.5) -%}
        {%- for s in earnings.surprises -%}
          {%- set mag = s.surprise_pct|float(0)|abs -%}
          {%- if mag > smax.v %}{% set smax.v = mag %}{% endif -%}
        {%- endfor -%}
        <div class="sbars">
          {% for s in earnings.surprises %}
          {%- set sp = s.surprise_pct|float(0) -%}
          <div class="sbar">
            <i class="{{ 'm' if sp < 0 else '' }}" style="height:{{ '%.0f'|format([sp|abs / smax.v * 56, 4]|max) }}px;"></i>
            <b class="{{ 'pos' if sp > 0 else 'neg' }} num">{{ '+' if sp > 0 else '' }}{{ '%.1f'|format(sp) }}%</b>
            <span>{{ s.qtr }}</span>
          </div>
          {% endfor %}
        </div>
        <p class="sm mut" style="margin:6px 0 0;">{{ t('EPS surprise vs consensus, last quarters', '每股收益相对预期的超预期幅度（近几个季度）') }}{% if earnings.avg_surprise_pct %} · {{ t('average', '平均') }} {{ earnings.avg_surprise_pct }}{% endif %}</p>
        {% endif %}
      </div>
    </div>
  </section>
  {% endif %}

  {% if valuation %}
  <section class="mod rv" id="valuation">
    <div class="mod-hd"><h2>{{ t('Valuation', '估值') }}</h2>
      <span class="hint">{% if gauges and gauges.valuation %}<span class="okx {{ 'y' if (gauges.valuation.pct|float(0)) > 55 else ('n' if (gauges.valuation.pct|float(0)) < 30 else 'w') }}" style="font-size:10.5px;">{{ t(gauges.valuation.verdict_en, gauges.valuation.verdict_zh) }}</span> {% endif %}{% if 'stats' in deep_ids %}<button class="gbtn gbtn-sm" onclick="dsrOpenDlg('dlg-stats')">{{ t('Full detail', '完整数据') }}</button>{% endif %}</span>
    </div>
    <div class="vt-wrap"><table class="vt">
      <thead><tr>
        <th>{{ t('Multiple', '指标') }}</th><th>{{ ticker }}</th>
        <th>{{ t('Sector median', '行业中位') }}</th><th>{{ t('Cheaper than', '相对便宜') }}</th>
      </tr></thead>
      <tbody>
      {% for row in valuation %}
      <tr>
        <td class="mut">{{ t(row.label_en, row.label_zh) }}</td>
        <td class="b num">{{ row.value }}</td>
        <td class="mut num">{{ row.sector_med if row.sector_med else '—' }}</td>
        <td>
          {% if row.cheap is not none %}
          <span class="cheapbar"><span class="cb"><i style="width:{{ row.cheap }}%;background:{% if row.cheap >= 55 %}var(--up){% elif row.cheap <= 30 %}var(--down){% else %}var(--warn){% endif %};"></i></span><span class="mut num sm">{{ '%.0f'|format(row.cheap) }}%</span></span>
          {% else %}—{% endif %}
        </td>
      </tr>
      {% endfor %}
      </tbody>
    </table></div>
    <div class="mod-ft">{{ t('"Cheaper than" = share of sector peers trading richer on that multiple.', '「相对便宜」= 行业中该指标估值更高的同行占比。') }}</div>
  </section>
  {% endif %}

  {% if financials %}
  <section class="mod rv" id="financials">
    <div class="mod-hd"><h2>{{ t('Financials', '财务数据') }}</h2><span class="hint">{{ t('fiscal years', '财年') }} {% if 'financials' in deep_ids %}<button class="gbtn gbtn-sm" onclick="dsrOpenDlg('dlg-financials')">{{ t('Full detail', '完整数据') }}</button>{% endif %}</span></div>
    {%- set fyears = financials.years -%}
    {%- set n_years = fyears | length -%}
    <div class="fin3">
      {% if financials.revenue and financials.revenue | select('ne', '') | list %}
      <div class="fcard">
        {%- set rev_max = namespace(v=1) -%}
        {%- set rev_last = namespace(v='', y='') -%}
        {%- for i in range(n_years) -%}
          {%- set v = financials.revenue[i] if i < financials.revenue|length else '' -%}
          {%- if v -%}
            {%- set vn = v|replace('$','')|replace('B','')|replace('M','')|replace('T','')|float(0) -%}
            {%- if vn > rev_max.v -%}{%- set rev_max.v = vn -%}{%- endif -%}
            {%- set rev_last.v = v -%}{%- set rev_last.y = fyears[i] -%}
          {%- endif -%}
        {%- endfor -%}
        <div class="fhd"><span class="ft">{{ t('Revenue', '营业收入') }}</span>
          {% if rev_last.v %}<span class="flast"><b class="num">{{ rev_last.v }}</b><em>{{ rev_last.y }}</em></span>{% endif %}</div>
        <div class="fbars">
          {% for i in range(n_years) %}
          {%- set rv = financials.revenue[i] if i < financials.revenue|length else '' -%}
          {%- set rv_n = rv|replace('$','')|replace('B','')|replace('M','')|replace('T','')|float(0) if rv else 0 -%}
          <div class="fb"{% if rv %} data-y="{{ fyears[i] }}" data-v="{{ rv }}"{% endif %}><i style="height:{{ [((rv_n / rev_max.v) * 50)|int, 2]|max }}px;"></i></div>
          {% endfor %}
        </div>
        {% if n_years %}<div class="fspan"><span>{{ fyears[0] }}</span><span>{{ fyears[n_years - 1] }}</span></div>{% endif %}
        {% if financials.rev_cagr %}<div class="fcagr">{{ t('5-yr growth', '5年复合增速') }} <b>{{ financials.rev_cagr }}</b></div>{% endif %}
      </div>
      {% endif %}
      {% if financials.eps and financials.eps | select('ne', '') | list %}
      <div class="fcard">
        {%- set eps_max = namespace(v=0.01) -%}
        {%- set eps_last = namespace(v='', y='') -%}
        {%- for i in range(n_years) -%}
          {%- set v = financials.eps[i] if i < financials.eps|length else '' -%}
          {%- if v -%}
            {%- set vn = v|float(0)|abs -%}
            {%- if vn > eps_max.v -%}{%- set eps_max.v = vn -%}{%- endif -%}
            {%- set eps_last.v = v -%}{%- set eps_last.y = fyears[i] -%}
          {%- endif -%}
        {%- endfor -%}
        <div class="fhd"><span class="ft">{{ t('Earnings per share', '每股收益') }}</span>
          {% if eps_last.v %}<span class="flast"><b class="num">${{ eps_last.v }}</b><em>{{ eps_last.y }}</em></span>{% endif %}</div>
        <div class="fbars">
          {% for i in range(n_years) %}
          {%- set ev = financials.eps[i] if i < financials.eps|length else '' -%}
          {%- set ev_n = ev|float(0) if ev else 0 -%}
          <div class="fb"{% if ev %} data-y="{{ fyears[i] }}" data-v="${{ ev }}"{% endif %}><i class="{{ 'dn6' if ev_n < 0 else '' }}" style="height:{{ [(ev_n|abs / eps_max.v * 50)|int, 2]|max }}px;"></i></div>
          {% endfor %}
        </div>
        {% if n_years %}<div class="fspan"><span>{{ fyears[0] }}</span><span>{{ fyears[n_years - 1] }}</span></div>{% endif %}
        {% if financials.eps_cagr %}<div class="fcagr">{{ t('5-yr growth', '5年复合增速') }} <b>{{ financials.eps_cagr }}</b></div>{% endif %}
      </div>
      {% endif %}
      {% if financials.fcf and financials.fcf | select('ne', '') | list %}
      <div class="fcard">
        {%- set fcf_max = namespace(v=0.01) -%}
        {%- set fcf_last = namespace(v='', y='') -%}
        {%- for i in range(n_years) -%}
          {%- set v = financials.fcf[i] if i < financials.fcf|length else '' -%}
          {%- if v -%}
            {%- set vn = v|replace('$','')|replace('B','')|replace('M','')|float(0)|abs -%}
            {%- if vn > fcf_max.v -%}{%- set fcf_max.v = vn -%}{%- endif -%}
            {%- set fcf_last.v = v -%}{%- set fcf_last.y = fyears[i] -%}
          {%- endif -%}
        {%- endfor -%}
        <div class="fhd"><span class="ft">{{ t('Free cash flow', '自由现金流') }}</span>
          {% if fcf_last.v %}<span class="flast"><b class="num">{{ fcf_last.v }}</b><em>{{ fcf_last.y }}</em></span>{% endif %}</div>
        <div class="fbars">
          {% for i in range(n_years) %}
          {%- set fv = financials.fcf[i] if i < financials.fcf|length else '' -%}
          {%- set fv_n = fv|replace('$','')|replace('B','')|replace('M','')|float(0) if fv else 0 -%}
          <div class="fb"{% if fv %} data-y="{{ fyears[i] }}" data-v="{{ fv }}"{% endif %}><i class="{{ 'dn6' if fv_n < 0 else '' }}" style="height:{{ [(fv_n|abs / fcf_max.v * 50)|int, 2]|max }}px;"></i></div>
          {% endfor %}
        </div>
        {% if n_years %}<div class="fspan"><span>{{ fyears[0] }}</span><span>{{ fyears[n_years - 1] }}</span></div>{% endif %}
      </div>
      {% endif %}
    </div>
    {% if financials.margins %}
    <div style="margin-top:16px;">
      {% for k, lbl_en, lbl_zh in [('gross_margin','Gross margin','毛利率'),('op_margin','Operating margin','营业利润率'),('net_margin','Net margin','净利率'),('fcf_margin','FCF margin','自由现金流利润率')] %}
      {%- set mv = financials.margins[k] -%}
      {%- if mv is not none -%}
      <div class="mrow">
        <span class="mk">{{ t(lbl_en, lbl_zh) }}</span>
        <div class="mtr"><i style="width:{{ [mv|float(0)|abs, 100]|min }}%;"></i></div>
        <span class="mv num">{{ '%.1f'|format(mv|float(0)) }}%</span>
      </div>
      {%- endif -%}
      {% endfor %}
    </div>
    {% endif %}
    <div class="hchips">
      {% if financials.piotroski is not none %}
      {%- set pio_of = financials.piotroski_of or 9 -%}
      <span class="pio"><b>{{ t('Balance-sheet quality', '财务体质') }} {{ financials.piotroski }}/{{ pio_of }}</b>{% for i in range(pio_of) %}<i class="{{ 'on6' if i < financials.piotroski else '' }}"></i>{% endfor %}</span>
      {% endif %}
      {% if financials.altman_zone %}
      <span class="okx {{ 'y' if financials.altman_zone == 'safe' else ('w' if financials.altman_zone == 'grey' else 'n') }}">{{ t('Bankruptcy risk: ' ~ financials.altman_zone ~ ' zone', '破产风险：' ~ ('安全区' if financials.altman_zone == 'safe' else ('灰色区' if financials.altman_zone == 'grey' else '警戒区'))) }}</span>
      {% endif %}
      {% if financials.roe is not none %}<span class="okx {{ 'y' if financials.roe|float(0) > 15 else 'q' }}">{{ t('Return on equity', '股本回报率') }} {{ '%.0f'|format(financials.roe|float(0)) }}%</span>{% endif %}
      {% if financials.aq_headline_en %}<span class="okx w">{{ t(financials.aq_headline_en, financials.aq_headline_zh) }}</span>{% endif %}
    </div>
    {% if financials.leverage_rows or financials.cap_rows %}
    <div class="kv2">
      {% if financials.leverage_rows %}
      <h4>{{ t('Leverage', '杠杆') }}</h4>
      {% for row in financials.leverage_rows %}<div class="r6"><span class="k6">{{ t(row.label_en, row.label_zh) }}</span><span class="v6 num">{{ row.value }}</span></div>{% endfor %}
      {% endif %}
      {% if financials.cap_rows %}
      <h4>{{ t('Capital allocation', '资本配置') }}</h4>
      {% for row in financials.cap_rows %}<div class="r6"><span class="k6">{{ t(row.label_en, row.label_zh) }}</span><span class="v6 num">{{ row.value }}</span></div>{% endfor %}
      {% endif %}
    </div>
    {% endif %}
  </section>
  {% endif %}
  {% include "_debt_maturity.html.j2" %}

  {% if why_moving %}
  <section class="mod rv why" id="why">
    <div class="mod-hd"><h2>{{ t('Why is it moving', '为何波动') }}</h2><span class="hint">{{ t('6 checks · nightly', '六项检查 · 每晚') }}</span></div>
    {% for row in why_moving %}
    <div class="wr {{ 'wr-on' if row.state == 'active' else '' }}">
      <div class="wd"></div>
      <span class="wc">{{ t(row.category_en, row.category_zh) }}</span>
      <span class="wl">{{ t(row.line_en, row.line_zh) }}</span>
    </div>
    {% endfor %}
  </section>
  {% endif %}

  {% if options %}
  <section class="mod rv" id="options">
    <div class="mod-hd"><h2>{{ t('Options positioning', '期权持仓') }}</h2><span class="hint">{{ t('as of last close', '截至上一收盘') }} {% if 'options' in deep_ids %}<button class="gbtn gbtn-sm" onclick="dsrOpenDlg('dlg-options')">{{ t('Full detail', '完整数据') }}</button>{% endif %}</span></div>
    {% if options.regime_en %}<p style="margin:0 0 14px;font-size:12.5px;line-height:1.6;">{{ t(options.regime_en, options.regime_zh) }}</p>{% endif %}
    <div class="otiles">
      {% if options.em_daily_pct %}<div class="otile"><div class="ok6">{{ t('Expected daily move', '预期日波动') }}</div><div class="ov num">±{{ options.em_daily_pct }}%</div></div>{% endif %}
      {% if options.em_weekly_pct %}<div class="otile"><div class="ok6">{{ t('Expected weekly move', '预期周波动') }}</div><div class="ov num">±{{ options.em_weekly_pct }}%</div></div>{% endif %}
      {% if options.iv_rank_pct %}<div class="otile"><div class="ok6">{{ t('IV rank', 'IV历史分位') }}</div><div class="ov num">{{ options.iv_rank_pct }}%</div><div class="ob"><i style="width:{{ [options.iv_rank_pct|float(0), 100]|min }}%;"></i></div></div>{% endif %}
      {% if not ladder and options.call_wall %}<div class="otile"><div class="ok6">{{ t('Call wall', '看涨期权墙') }}</div><div class="ov num">${{ options.call_wall }}</div></div>{% endif %}
      {% if not ladder and options.put_wall %}<div class="otile"><div class="ok6">{{ t('Put wall', '看跌期权墙') }}</div><div class="ov num">${{ options.put_wall }}</div></div>{% endif %}
      {% if options.gamma_flip %}<div class="otile"><div class="ok6">{{ t('Gamma flip', '伽玛翻转位') }}</div><div class="ov num">${{ options.gamma_flip }}</div></div>{% endif %}
      {% if options.pc_ratio %}<div class="otile"><div class="ok6">{{ t('Put/Call ratio', '认沽认购比') }}</div><div class="ov num">{{ options.pc_ratio }}</div></div>{% endif %}
      {% if options.max_pain %}<div class="otile"><div class="ok6">{{ t('Max pain', '最大痛点价') }}</div><div class="ov num">${{ options.max_pain }}</div></div>{% endif %}
      {% if options.rr_25d %}<div class="otile"><div class="ok6">{{ t('25d risk reversal', '25d风险逆转') }}</div><div class="ov num {{ 'neg' if options.rr_25d and '-' in options.rr_25d else '' }}">{{ options.rr_25d }}</div></div>{% endif %}
    </div>
    {% if options.flow_en %}
    <p class="sm" style="margin:12px 0 0;"><span class="mut">{{ t('Flow:', '资金流向：') }}</span> {{ t(options.flow_en, options.flow_zh) }}</p>
    {% endif %}
    {% if options.skew_tone %}
    <div class="hchips" style="margin-top:10px;">
      <span class="okx {{ 'n' if options.skew_tone == 'fear' else ('y' if options.skew_tone == 'greed' else 'q') }}">{{ t('Skew: ' ~ options.skew_tone, '偏斜：' ~ ('避险' if options.skew_tone == 'fear' else ('贪婪' if options.skew_tone == 'greed' else options.skew_tone))) }}</span>
    </div>
    {% endif %}
    {% if ladder and (options.call_wall or options.put_wall) %}
    <div class="mod-ft">{{ t('Call and put walls are plotted on the Trade levels ladder →', '看涨/看跌期权墙已标注在「交易点位」阶梯上 →') }}</div>
    {% endif %}
  </section>
  {% endif %}

  {% if ownership %}
  <section class="mod rv" id="ownership">
    <div class="mod-hd"><h2>{{ t('Ownership & smart money', '持仓与聪明钱') }}</h2><span class="hint">13F {% if 'ownership' in deep_ids %}<button class="gbtn gbtn-sm" onclick="dsrOpenDlg('dlg-ownership')">{{ t('Full detail', '完整数据') }}</button>{% endif %}</span></div>
    {% if ownership.insider_en %}
    <p style="margin:0 0 10px;font-size:12.5px;"><span class="mut">{{ t('Insider activity:', '内部人动向：') }}</span> {{ t(ownership.insider_en, ownership.insider_zh) }}</p>
    {% endif %}
    {% if ownership.hhi_en %}<p class="sm mut" style="margin:0 0 10px;">{{ t(ownership.hhi_en, ownership.hhi_zh) }}</p>{% endif %}
    {% if ownership.holders %}
    <div class="vt-wrap"><table class="vt">
      <thead><tr>
        <th>{{ t('Fund', '基金') }}</th><th>{{ t('Grade', '评级') }}</th>
        <th>{{ t('Latest move', '最新动作') }}</th><th>{{ t('% of book', '组合占比') }}</th><th>{{ t('As of', '截至') }}</th>
      </tr></thead>
      <tbody>
      {% for h in ownership.holders %}
      <tr>
        <td>{{ h.fund_name }}</td>
        <td><span class="gchip {{ h.fund_grade }}">{{ h.fund_grade }}</span></td>
        <td class="{{ 'pos' if h.action_en in ('New position','Adding','Buying') else ('neg' if h.action_en in ('Exited','Selling','Reducing') else 'mut') }}">{{ t(h.action_en, h.action_zh) }}</td>
        <td class="mut num">{{ h.shares_pct }}</td>
        <td class="mut sm num">{{ h.period_end }}</td>
      </tr>
      {% endfor %}
      </tbody>
    </table></div>
    {% endif %}
  </section>
  {% endif %}

  {% if signal_history %}
  <section class="mod rv" id="history">
    <div class="mod-hd"><h2>{{ t('Signal history', '信号历史') }}</h2><span class="hint">{{ t('engine events', '引擎事件') }}</span></div>
    {% if signal_history.pinned %}
    {%- set pn = signal_history.pinned -%}
    <div class="pinned6">
      <span class="ph {{ 'pos' if pn.dir == 'up' else ('neg' if pn.dir == 'down' else 'wrn') }}">{{ t(pn.headline, pn.headline_zh) if pn.headline_zh else pn.headline }}</span>
      {% if pn.detail %}<span class="pd">{{ t(pn.detail, pn.detail_zh) if pn.detail_zh else pn.detail }}</span>{% endif %}
    </div>
    {% endif %}
    {% if signal_history.events %}
    <div class="tl">
      {% for ev in signal_history.events %}
      <div class="tev {{ ev.dir or 'neutral' }}">
        <div class="td num">{{ ev.date }}</div>
        <div class="th">{{ t(ev.headline, ev.headline_zh) if ev.headline_zh else ev.headline }}</div>
        {% if ev.detail %}<div class="tx">{{ t(ev.detail, ev.detail_zh) if ev.detail_zh else ev.detail }}</div>{% endif %}
      </div>
      {% endfor %}
    </div>
    {% if signal_history.n_total > 15 %}
    <div class="mod-ft">{{ t('Showing the last 15 of', '显示最近15条，共') }} {{ signal_history.n_total }} {{ t('signals', '条信号') }}</div>
    {% endif %}
    {% endif %}
  </section>
  {% endif %}

  {% if profile_extras or (hero and hero.desc_en) or factors %}
  <section class="mod rv" id="profile">
    <div class="mod-hd"><h2>{{ t('Company profile', '公司档案') }}</h2></div>
    {% if hero and hero.desc_en %}
    <p style="margin:0;font-size:13px;line-height:1.65;">{{ t(hero.desc_en, hero.desc_zh) }}</p>
    {% endif %}
    {% if profile_extras and profile_extras.demand_chain_en %}
    <p class="sm mut" style="margin:10px 0 0;">{{ t('Demand chain:', '需求链：') }} {{ t(profile_extras.demand_chain_en, profile_extras.demand_chain_zh if profile_extras.demand_chain_zh else profile_extras.demand_chain_en) }}</p>
    {% endif %}
    {% if factors and factors.rows %}
    <div class="subhd">{{ t('Style DNA — how it trades vs the market', '风格基因 — 相对市场的交易特征') }}</div>
    <div class="dna">
      {% for row in factors.rows %}
      {%- set zmag = [row.z|abs * 30, 48]|min -%}
      <div class="dr">
        <span class="dk">{{ t(row.label_en, row.label_zh) }}</span>
        <div class="dtr"><i class="{{ 'p' if row.positive else 'n' }}" style="width:{{ '%.0f'|format(zmag) }}%;"></i></div>
        <span class="dw {{ 'pos' if row.positive else 'neg' }}">{{ t('Above market' if row.positive else 'Below market', '高于市场' if row.positive else '低于市场') }}</span>
      </div>
      {% endfor %}
    </div>
    {% endif %}
    {% if profile_extras and profile_extras.active_falsifiers %}
    <div class="subhd">{{ t('Watch items — what could change the story', '关注项 — 可能改变故事的因素') }}</div>
    <ul class="watch">
      {% for f in profile_extras.active_falsifiers %}<li>{{ f }}</li>{% endfor %}
    </ul>
    {% endif %}
  </section>
  {% endif %}

  {% if brief %}
  <section class="mod rv" id="brief">
    <div class="mod-hd"><h2>{{ t('AI research brief', 'AI研究简报') }}</h2>
      {% if brief.as_of %}<span class="hint">{{ t('generated', '生成于') }} {{ brief.as_of }}</span>{% endif %}</div>
    {% if brief.summary %}<p class="l-en" style="margin:0 0 8px;font-size:13px;line-height:1.6;">{{ brief.summary }}</p>{% endif %}
    {% if brief.zh_summary %}<p class="l-zh" style="margin:0 0 8px;font-size:13px;line-height:1.6;">{{ brief.zh_summary }}</p>{% endif %}
    {% if brief.drivers %}
    <div class="subhd">{{ t('Key drivers', '主要驱动') }}</div>
    <ul class="brief-list l-en">{% for d in brief.drivers %}<li>{{ d }}</li>{% endfor %}</ul>
    {% endif %}
    {% if brief.risks %}
    <div class="subhd">{{ t('Key risks', '主要风险') }}</div>
    <ul class="brief-list l-en">{% for r in brief.risks %}<li>{{ r }}</li>{% endfor %}</ul>
    {% endif %}
    {% if brief.catalysts %}
    <div class="subhd">{{ t('Upcoming catalysts', '近期催化剂') }}</div>
    <ul class="brief-list l-en">{% for c in brief.catalysts %}<li>{{ c }}</li>{% endfor %}</ul>
    {% endif %}
    {% if brief.disclaimer %}<div class="mod-ft">{{ brief.disclaimer }}</div>{% endif %}
  </section>
  {% endif %}

  {% if news %}
  <section class="mod rv" id="news">
    <div class="mod-hd"><h2>{{ t('Recent news', '最新动态') }}</h2></div>
    {% for h in news %}
    <div class="nrow">
      <a href="{{ h.url }}" rel="noopener noreferrer" target="_blank">{{ h.title }}</a>
      <span class="nm">{% if h.source %}{{ h.source }}{% endif %}{% if h.published %} · {{ h.published }}{% endif %}</span>
    </div>
    {% endfor %}
  </section>
  {% endif %}
  </div>

  <!-- ═ RAIL — starts under the chart ═ -->
  <aside class="rail-col">

  {% if ladder %}
  <section class="mod rv" id="levels" style="--d:60ms;">
    <div class="mod-hd"><h2>{{ t('Trade levels', '交易点位') }}</h2><span class="hint">{{ t('engine · nightly', '引擎 · 每晚') }}</span></div>
    <div>
      {% for item in ladder.levels %}
      {% if item.kind == 'band' %}
      <div class="lband">{{ t('Buy zone', '买入区') }} {{ item.price }} — {{ t('where patience gets paid', '耐心者的进场区') }}</div>
      {% else %}
      <div class="lrow {{ 'now' if item.cls == 'now' else '' }}{{ ' llast' if loop.last else '' }}">
        <span class="lpx num {{ 'neg' if item.cls in ('stop',) else ('mut' if item.cls == 'wall' else ('wrn' if item.cls == 'chase' else ('pos' if item.cls == 'buy' else ''))) }}">{{ item.price }}</span>
        <span class="lnode"><span class="ldot {{ item.cls if item.cls != 'now' else '' }}"></span></span>
        <span class="lk">{{ t(item.label_en, item.label_zh) }}{% if item.note_en %}<small>{{ t(item.note_en, item.note_zh) }}</small>{% endif %}</span>
        <span class="ld num">{% if item.cls == 'now' %}<span class="live-dot"></span>{% else %}{{ item.dist }}{% endif %}</span>
      </div>
      {% endif %}
      {% endfor %}
    </div>
    <div class="mod-ft">{{ t('Reference levels from the nightly engine — research context, not advice.', '来自每晚引擎的参考点位 — 仅供研究参考，非投资建议。') }}</div>
  </section>
  {% endif %}

  {% if stats %}
  <section class="mod rv kv6" id="facts" style="--d:120ms;">
    <div class="mod-hd"><h2>{{ t('Key facts', '关键数据') }}</h2><span class="hint">{% if 'stats' in deep_ids %}<button class="gbtn gbtn-sm" onclick="dsrOpenDlg('dlg-stats')">{{ t('Full detail', '完整数据') }}</button>{% endif %}</span></div>
    {% if stats.mktcap %}<div class="r6"><span class="k6">{{ t('Market cap', '市值') }}</span><span class="v6 num">{{ stats.mktcap }}</span></div>{% endif %}
    {% if stats.trailing_pe or stats.forward_pe %}<div class="r6"><span class="k6">{{ t('P/E · forward', '市盈率 · 预测') }}</span><span class="v6 num">{{ stats.trailing_pe or '—' }} · {{ stats.forward_pe or '—' }}</span></div>{% endif %}
    {% if stats.eps %}<div class="r6"><span class="k6">{{ t('EPS (trailing)', '每股收益') }}</span><span class="v6 num">{{ stats.eps }}</span></div>{% endif %}
    {% if stats.beta %}<div class="r6"><span class="k6">{{ t('Beta vs market', '贝塔') }}</span><span class="v6 num">{{ stats.beta }}{% if stats.beta_en %} — {{ t(stats.beta_en, stats.beta_zh) }}{% endif %}</span></div>{% endif %}
    {% if stats.div_yield %}<div class="r6"><span class="k6">{{ t('Dividend yield', '股息率') }}</span><span class="v6 num">{{ stats.div_yield }}</span></div>{% endif %}
    {% if stats.short_pct_float %}<div class="r6"><span class="k6">{{ t('Short interest', '空头占比') }}</span><span class="v6 num">{{ stats.short_pct_float }}</span></div>{% endif %}
    {% if stats.volume_en %}<div class="r6"><span class="k6">{{ t('Volume', '成交量') }}</span><span class="v6 num">{{ t(stats.volume_en, stats.volume_zh) }}</span></div>{% endif %}
    {% if stats.hv_pctile %}<div class="r6"><span class="k6">{{ t('Volatility rank', '波动率分位') }}</span><span class="v6 num">{% if stats.hv_pctile_num %}<span class="l-en">{{ stats.hv_pctile_num }}th pctile</span><span class="l-zh">第{{ stats.hv_pctile_num }}百分位</span>{% else %}{{ stats.hv_pctile }}{% endif %}</span></div>{% endif %}
    {% if stats.next_earnings %}<div class="r6"><span class="k6">{{ t('Next earnings', '下次财报') }}</span><span class="v6 num">{{ stats.next_earnings }}</span></div>{% endif %}
    {% if stats.range_52w_en %}<div class="r6"><span class="k6">{{ t('52-week range', '52周区间') }}</span><span class="v6">{{ t(stats.range_52w_en, stats.range_52w_zh) }}</span></div>{% endif %}
  </section>
  {% endif %}

  {% if seasonality %}
  <section class="mod rv" id="seasonality" style="--d:160ms;">
    <div class="mod-hd"><h2>{{ t('Seasonality', '季节性规律') }}</h2><span class="hint">{{ t(seasonality.computed_window_en or '', seasonality.computed_window_zh or '') }} {% if 'seasonality' in deep_ids %}<button class="gbtn gbtn-sm" onclick="dsrOpenDlg('dlg-seasonality')">{{ t('Full detail', '完整数据') }}</button>{% endif %}</span></div>
    {% if seasonality.this_label %}
    <p class="sm" style="margin:0 0 10px;line-height:1.5;"><span class="mut">{{ t('This month:', '本月：') }}</span> {{ t(seasonality.this_label, seasonality.this_label_zh) }}</p>
    {% endif %}
    {% if seasonality.next_label %}
    <p class="sm mut" style="margin:0 0 10px;line-height:1.5;">{{ t('Next month:', '下月：') }} {{ t(seasonality.next_label, seasonality.next_label_zh) }}</p>
    {% endif %}
    {% if seasonality.monthly %}
    {%- set cur_month_idx = freshness[5:7] | int(1) - 1 -%}
    <div class="seas">
      {% for m in seasonality.monthly %}
      {%- set wr = m.win_rate | float(0) -%}
      <div class="sc {{ 'cur' if loop.index0 == cur_month_idx else '' }}"
           style="background:{% if wr >= 70 %}color-mix(in srgb,var(--up) {{ ((wr-50)*2)|int }}%,var(--panel2)){% elif wr < 40 %}color-mix(in srgb,var(--down) {{ ((50-wr)*2)|int }}%,var(--panel2)){% else %}var(--panel2){% endif %};">
        {% if m.win_rate is not none %}<span class="num" style="font-size:11px;color:var(--text);font-weight:700;">{{ '%.0f'|format(wr) }}%</span>{% else %}—{% endif %}
        <span>{{ m.month }}</span>
      </div>
      {% endfor %}
    </div>
    <div class="mod-ft">{{ t('Share of years the month closed higher.', '该月份收涨的年份占比。') }}</div>
    {% endif %}
  </section>
  {% endif %}

  {% if themes %}
  <section class="mod rv" id="themes" style="--d:200ms;">
    <div class="mod-hd"><h2>{{ t('Themes & baskets', '主题与篮子') }}</h2></div>
    {% if themes.baskets %}
    {% for b in themes.baskets %}
    {#- The name links ONLY when basket/<id>.html actually shipped. A basket is
        curated by hand into membership.json but its page is built only once 3+
        members carry price history (engine/baskets.py), so a new sleeve is
        linkable-looking and unbuilt for at least a night — silver_miners was
        curated 2026-08-05 and 404'd from HL and SSRM, which failed the dead-ref
        guard on every render for ~28h. Unlinked it still reads: the name and the
        charter sentence below it are the substance. `b.linked` is set for every
        row by _build_themes, so an unset value here means an old context dict,
        not an unbuilt page — hence `is defined` rather than a bare falsy test. -#}
    <div class="throw">
      {% if b.linked is not defined or b.linked %}<a href="../basket/{{ b.id }}.html">{{ t(b.name_en, b.name_zh) }}</a>{% else %}<b>{{ t(b.name_en, b.name_zh) }}</b>{% endif %}
      <span class="tm">{{ b.theme if b.theme else b.rationale }}</span>
      {% if b.band_en %}<span class="okx q" style="font-size:10px;">{{ t(b.band_en, b.band_zh) }}</span>{% endif %}
    </div>
    {% endfor %}
    {% endif %}
    {% if themes.basket_alloc %}
    <p class="sm mut" style="margin:10px 0 0;">{{ t('Primary basket:', '主要篮子：') }} {{ t(themes.basket_alloc.name_en, themes.basket_alloc.name_zh) }} — {{ t(themes.basket_alloc.label_en, themes.basket_alloc.label_zh if themes.basket_alloc.label_zh else themes.basket_alloc.label_en) }}</p>
    {% endif %}
    {% if themes.sector_pulse %}
    <p class="sm mut" style="margin:6px 0 0;">{{ t('Sector theme:', '行业主题：') }} {{ t(themes.sector_pulse.theme_name, themes.sector_pulse.theme_name_zh) }} <span class="okx {{ 'y' if themes.sector_pulse.heat in ('hot','heating') else ('n' if themes.sector_pulse.heat in ('cold','cooling') else 'q') }}" style="font-size:10px;">{{ themes.sector_pulse.heat }}</span></p>
    {% endif %}
  </section>
  {% endif %}

  {% if peers %}
  <section class="mod rv" id="peers" style="--d:240ms;">
    <div class="mod-hd"><h2>{{ t('Peers', '同业比较') }}</h2><span class="hint">{{ t('by size', '按市值') }}</span></div>
    <div class="peers">
      {% for p in peers %}
      {% if p.href %}<a class="pcard" href="{{ p.href }}">{% else %}<div class="pcard pcard-off">{% endif %}
        <b>{{ p.ticker }}</b>
        <span>{{ p.name }}</span>
        {% if p.mktcap %}<small class="num">{{ p.mktcap }}</small>{% endif %}
      {% if p.href %}</a>{% else %}</div>{% endif %}
      {% endfor %}
    </div>
  </section>
  {% endif %}

  <section class="mod railcta rv" style="--d:280ms;">
    <h3>{{ t('This page, live', '这一页，实时版') }}</h3>
    <p>{{ t('The Terminal runs these signals intraday — live chart, options flow and alerts for ' ~ ticker ~ '.', '终端全天实时运行这些信号 — ' ~ ticker ~ ' 的实时图表、期权资金流与提醒。') }}</p>
    <a class="gbtn gbtn-cta" href="{{ terminal_url }}" rel="noopener noreferrer" target="_blank" style="width:100%;">{{ t('Open ' ~ ticker ~ ' in Terminal', '在终端中打开 ' ~ ticker) }} ↗</a>
  </section>
  </aside>
</div>

<!-- ═ CTA band ═ -->
<section class="mod cta-band rv">
  <div class="cc">
    <h2>{{ t('The whole US universe — S&P 1500 and Russell 2000 — read nightly.', '覆盖标普1500与罗素2000全部成分股 — 每晚更新解读。') }}</h2>
    <p>{{ t('Signals, options positioning, smart-money moves and factor DNA — the same engine that wrote this page covers 2,700+ US stocks.', '信号、期权持仓、聪明钱动向与因子基因 — 撰写本页的引擎覆盖2700多只美股。') }}</p>
  </div>
  <div style="display:flex;gap:10px;flex-wrap:wrap;">
    <a class="gbtn gbtn-cta" href="index.html" style="padding:10px 20px;font-size:13.5px;">{{ t('Browse all stocks', '浏览所有股票') }}</a>
    <a class="gbtn" href="{{ terminal_url }}" rel="noopener noreferrer" target="_blank" style="padding:10px 20px;font-size:13.5px;">{{ t('Open Terminal', '打开终端') }}</a>
  </div>
</section>

<footer class="site-footer6">
  <p>{{ t('Research context only — not investment advice. Past patterns do not guarantee future results.', '仅供研究参考，不构成投资建议。历史规律不代表未来表现。') }}</p>
  <p>{{ t('Data through', '数据截至') }} {{ freshness }} · {{ t('regenerated nightly', '每晚更新') }} · {{ t('Generated', '生成于') }} {{ generated_utc }} · <a href="index.html">{{ t('All coverage', '所有股票') }}</a> · <a href="../stock.html">{{ t('Analyzer', '分析器') }}</a></p>
</footer>

</div><!-- /page-wrap -->

{%- if security_state %}{% set ss = security_state %}
{# ═ SECURITY STATE drilldowns ═ These live at the dialog layer, not inside the
   section: .mod carries backdrop-filter and .rv carries a transform, and either
   one makes itself the containing block for position:fixed descendants — a
   dialog nested in the section would be trapped inside the card. They reuse the
   page's dsrOpenDlg mechanics wholesale (focus trap, Escape, scroll lock, and
   the ≤640px bottom-sheet geometry the drawer requirement asks for). #}
{% for a in ss.axes %}
<div class="dsr-dlg" id="dlg-{{ a.dlg }}" role="dialog" aria-labelledby="dlg-{{ a.dlg }}-title">
  <div class="dsr-dlg-backdrop"></div>
  <div class="dsr-dlg-panel">
    <div class="dsr-dlg-header">
      <span class="dsr-dlg-title" id="dlg-{{ a.dlg }}-title">{{ t(a.title_en, a.title_zh) }}</span>
      <button class="dsr-dlg-close" onclick="dsrCloseDlg()" aria-label="Close">✕</button>
    </div>
    <div class="dsr-dlg-body">
      <section class="dpanel">
        <h3>{{ t('What this read says', '这项读数说了什么') }}</h3>
        <p class="ss-dp">{{ t(a.headline.en, a.headline.zh) }}</p>
        {% if a.clause %}<p class="ss-dp mut">{{ t(a.clause.en, a.clause.zh) }}</p>{% endif %}
        {% if a.why %}<p class="ss-dp mut">{{ t(a.why.en, a.why.zh) }}</p>{% endif %}
        {% if a.derived %}<div class="dfoot">{{ t('This sentence is our plain wording of the values in this panel. The values themselves are quoted.', '此表述为本页对本面板数值的通俗措辞；数值本身为原样引用。') }}</div>{% endif %}
      </section>

      <section class="dpanel">
        <h3>{{ t('Where it comes from', '来源与时间') }}</h3>
        <div class="dkv">
          <div class="r6d"><span class="k">{{ t('Field status', '字段状态') }}</span><span class="vv ss-t-{{ a.tone }} ss-tone-ink">{{ t(a.cov_en, a.cov_zh) }}</span></div>
          {% if a.owner %}<div class="r6d"><span class="k">{{ t('Canonical owner', '权威来源方') }}</span><span class="vv">{{ a.owner }}</span></div>{% endif %}
          {% if a.owner_object %}<div class="r6d"><span class="k">{{ t('Owner object', '来源对象') }}</span><span class="vv ss-id">{{ a.owner_object }}</span></div>{% endif %}
          {% if a.source %}<div class="r6d"><span class="k">{{ t('Source', '原始来源') }}</span><span class="vv">{{ a.source }}</span></div>{% endif %}
          {% if a.generation_id %}<div class="r6d"><span class="k">{{ t('Generation', '生成批次') }}</span><span class="vv ss-id">{{ a.generation_id }}</span></div>{% endif %}
          {% for c in a.clocks %}<div class="r6d"><span class="k">{{ t(c.en, c.zh) }}</span><span class="vv num">{{ c.v }}</span></div>{% endfor %}
          <div class="r6d"><span class="k">{{ t('Correction state', '更正状态') }}</span><span class="vv">{{ a.correction if a.correction else t('None recorded', '无记录') }}</span></div>
        </div>
      </section>

      {# The quoted values the card's sentence rests on. The sentence above is
         this page's wording; these are the source's own fields, unchanged. #}
      {% if a.fields %}
      <section class="dpanel">
        <h3>{{ t('Fields read, exactly as recorded', '按原样读取的字段') }}</h3>
        <dl class="ss-chk-v">{% for f in a.fields %}<dt>{{ f.k }}</dt><dd>{{ f.v if f.v else '—' }}</dd>{% endfor %}</dl>
        {% if a.quoted_summary %}<p class="ss-receipt">{{ t('Recorded as', '记录原文') }}: <span class="ss-id">{{ t(a.quoted_summary.en, a.quoted_summary.zh) }}</span></p>{% endif %}
      </section>
      {% endif %}

      {% if a.subreads %}
      <section class="dpanel">
        <h3>{{ t('Reads behind this', '构成这项的读数') }}</h3>
        <div class="dkv">
          {% for s in a.subreads %}<div class="r6d"><span class="k">{{ t(s.en, s.zh) }}</span><span class="vv ss-t-{{ s.tone }} ss-tone-ink">{{ t(s.cov_en, s.cov_zh) }}</span></div>{% endfor %}
        </div>
        {% for s in a.subreads %}{% if s.reason_en or s.reason_zh %}<p class="ss-receipt">{{ t(s.en, s.zh) }} — {{ t(s.reason_en, s.reason_zh) }}</p>{% endif %}{% endfor %}
        <div class="dfoot">{{ t('A read that is not available is left out. No rank, score or stand-in value is put in its place.', '不可用的读数即留空，不会以任何排名、评分或替代数值填补。') }}</div>
      </section>
      {% endif %}

      {% if a.unresolved %}
      <section class="dpanel">
        <h3>{{ t('Biggest open question', '最大未决问题') }}</h3>
        <p class="ss-dp">{{ t(a.unresolved.en, a.unresolved.zh) }}</p>
      </section>
      {% endif %}

      {% if a.gates %}
      <section class="dpanel">
        <h3>{{ t('Checks not cleared', '未通过的检查') }}</h3>
        {% for g in a.gates %}
        <div class="ss-gate">
          <span class="g">{{ t(g.en, g.zh) }}<span class="c ss-id">{{ g.code }}</span></span>
          <span class="w"><em>{{ t('Clears when', '通过条件') }}:</em> {{ t(g.clear_en, g.clear_zh) }}</span>
        </div>
        {% endfor %}
        <div class="dfoot">{{ t('A check that did not clear is shown as not cleared. It is never rounded up to a pass or replaced with a neutral value.', '未通过的检查即按未通过呈现，不会被计为通过，也不会以中性数值替代。') }}</div>
      </section>
      {% endif %}

      {% if a.observables %}
      <section class="dpanel">
        <h3>{{ t('What to watch next', '接下来关注什么') }}{% if a.observables|selectattr('est')|list %}<span class="ss-prov">{{ t('worked out here', '本页推算') }}</span>{% endif %}</h3>
        {% for o in a.observables %}
        <div class="ss-gate">
          <span class="g">{{ t(o.en, o.zh) }}{% if o.when %}<span class="c ss-when">{{ o.when }}</span>{% endif %}</span>
          {% if o.est %}<span class="ss-est">{{ t('Estimated window — not an announced date', '预计窗口 — 并非官方公布日期') }}</span>{% endif %}
          {% if o.basis %}<span class="w"><em>{{ t('Worked out from', '推算依据') }}:</em> {{ o.basis }}</span>{% endif %}
        </div>
        {% endfor %}
      </section>
      {% endif %}

      {% if a.warnings %}
      <section class="dpanel">
        <h3>{{ t('Noted while building', '生成过程中的提示') }}</h3>
        {% for w in a.warnings %}
        <div class="ss-gate"><span class="g">{{ t(w.en, w.zh) }}{% if w.code %}<span class="c ss-id">{{ w.code }}</span>{% endif %}</span></div>
        {% endfor %}
      </section>
      {% endif %}

      {% if a.refs %}
      <section class="dpanel">
        <h3>{{ t('Supported claims', '受支持的依据') }}</h3>
        <ul class="dnotes">{% for r in a.refs %}<li class="ss-id">{{ r }}</li>{% endfor %}</ul>
      </section>
      {% endif %}

      <section class="dpanel">
        <h3>{{ t('Can you act on this', '是否可据此行动') }}</h3>
        <p class="ss-dp">{{ t(a.actionable.en, a.actionable.zh) }}</p>
      </section>
      {# Provenance, in two registers. The panel used to claim it calculated
         nothing — it does: it counts, it picks, it estimates a window. Saying
         so, and marking which lines those are, is worth more than a promise
         the panel could not keep. #}
      <div class="dfoot">{{ t("Most lines here are quoted — copied as recorded from the source named beside them. Lines tagged “worked out here” are produced by Mastermind from those quoted values under a fixed rule, never by judgement. The plain wording of a source's own status is ours; the status itself is the source's, and a missing source stays missing.", '本面板多数内容为引用 — 按记录原样复制自旁注来源。标注「本页推算」的内容由 Mastermind 依据这些引用值按固定规则得出，不含主观判断。来源状态的通俗措辞由本页给出，状态本身来自来源；缺失的来源仍按缺失呈现。') }}</div>
    </div>
  </div>
</div>
{% endfor %}

<div class="dsr-dlg" id="dlg-ss-evidence" role="dialog" aria-labelledby="dlg-ss-evidence-title">
  <div class="dsr-dlg-backdrop"></div>
  <div class="dsr-dlg-panel">
    <div class="dsr-dlg-header">
      <span class="dsr-dlg-title" id="dlg-ss-evidence-title">{{ t('Evidence & receipts', '证据与凭证') }}</span>
      <button class="dsr-dlg-close" onclick="dsrCloseDlg()" aria-label="Close">✕</button>
    </div>
    <div class="dsr-dlg-body">
      <section class="dpanel">
        <h3>{{ t('Coverage of the six reads', '六项读数的覆盖情况') }}<span class="ss-prov">{{ t('counted here', '本页统计') }}</span></h3>
        <div class="dkv">
          {% for b in ss.tally %}<div class="r6d"><span class="k ss-t-{{ b.tone }} ss-tone-ink">{{ t(b.en, b.zh) }}</span><span class="vv num">{{ b.n }} / {{ ss.axes|length }}</span></div>{% endfor %}
        </div>
      </section>

      {# Two different questions, so two different rows. "Current" counts only
         reads that came back current; "not in the way" also counts a read that
         does not apply to this listing or sits outside its coverage. Folding
         them into one number is how a read that does not apply ends up looking
         like a read that is fine. #}
      {% if ss.coverage.req_total is not none or ss.coverage.opt_total is not none %}
      <section class="dpanel">
        <h3>{{ t('Required and optional reads', '必需与可选读数') }}</h3>
        <div class="dkv">
          {% if ss.coverage.req_total is not none %}
          <div class="r6d"><span class="k">{{ t('Required reads, current', '必需读数：当前') }}</span><span class="vv num">{{ ss.coverage.req_avail if ss.coverage.req_avail is not none else '—' }} / {{ ss.coverage.req_total }}</span></div>
          {% if ss.coverage.req_nonblock is not none %}<div class="r6d"><span class="k">{{ t('Required reads, not in the way', '必需读数：不构成阻碍') }}</span><span class="vv num">{{ ss.coverage.req_nonblock }} / {{ ss.coverage.req_total }}</span></div>{% endif %}
          {% endif %}
          {% if ss.coverage.opt_total is not none %}
          <div class="r6d"><span class="k">{{ t('Optional reads, current', '可选读数：当前') }}</span><span class="vv num">{{ ss.coverage.opt_avail if ss.coverage.opt_avail is not none else '—' }} / {{ ss.coverage.opt_total }}</span></div>
          {% if ss.coverage.opt_nonblock is not none %}<div class="r6d"><span class="k">{{ t('Optional reads, not in the way', '可选读数：不构成阻碍') }}</span><span class="vv num">{{ ss.coverage.opt_nonblock }} / {{ ss.coverage.opt_total }}</span></div>{% endif %}
          {% endif %}
        </div>
        <div class="dfoot">{{ t('"Current" counts only reads that came back current. "Not in the way" also counts a read that does not apply to this listing, or sits outside its coverage — which is not the same as current. These counts include the evidence read, which has its own panel below rather than a card above.', '「当前」仅统计返回为当前的读数；「不构成阻碍」还包括不适用于该证券或不在覆盖范围内的读数 — 与「当前」并不相同。以上计数包含证据读数，该读数在下方独立面板中呈现，而非上方卡片。') }}</div>
      </section>
      {% endif %}

      {% if ss.gates %}
      <section class="dpanel">
        <h3>{{ t('Every check that did not clear', '所有未通过的检查') }}</h3>
        {% for g in ss.gates %}
        <div class="ss-gate">
          <span class="g">{{ t(g.en, g.zh) }}<span class="c ss-id">{{ g.code }}</span></span>
          <span class="w"><em>{{ t('Clears when', '通过条件') }}:</em> {{ t(g.clear_en, g.clear_zh) }}</span>
        </div>
        {% endfor %}
      </section>
      {% endif %}

      {% if ss.evidence.refs %}
      <section class="dpanel">
        <h3>{{ t('Supported claims', '受支持的依据') }}</h3>
        <ul class="dnotes">{% for r in ss.evidence.refs %}<li class="ss-id">{{ r }}</li>{% endfor %}</ul>
      </section>
      {% endif %}

      <section class="dpanel">
        <h3>{{ t('How this was put together', '生成过程') }}</h3>
        <div class="dkv">
          {% if ss.evidence.recipe_id %}<div class="r6d"><span class="k">{{ t('Recipe', '配方') }}</span><span class="vv ss-id">{{ ss.evidence.recipe_id }}</span></div>{% endif %}
          {% if ss.version %}<div class="r6d"><span class="k">{{ t('Schema', '数据结构') }}</span><span class="vv ss-id">{{ ss.version }}</span></div>{% endif %}
          {% if ss.listing_key %}<div class="r6d"><span class="k">{{ t('Listing', '上市代码') }}</span><span class="vv ss-id">{{ ss.listing_key }}</span></div>{% endif %}
          {% if ss.security_id %}<div class="r6d"><span class="k">{{ t('Security', '证券标识') }}</span><span class="vv ss-id">{{ ss.security_id }}</span></div>{% endif %}
          {% if ss.issuer_id %}<div class="r6d"><span class="k">{{ t('Issuer', '发行人标识') }}</span><span class="vv ss-id">{{ ss.issuer_id }}</span></div>{% endif %}
          {% if ss.content_sha256 %}<div class="r6d"><span class="k">{{ t('Content fingerprint', '内容指纹') }}</span><span class="vv ss-id">{{ ss.content_sha256 }}</span></div>{% endif %}
          {% if ss.market_at %}<div class="r6d"><span class="k">{{ t('Market data through', '行情数据截至') }}</span><span class="vv num">{{ ss.market_at }}</span></div>{% endif %}
          {% if ss.frontier_at %}<div class="r6d"><span class="k">{{ t('Sources through', '来源截至') }}</span><span class="vv num">{{ ss.frontier_at }}</span></div>{% endif %}
          {% if ss.compiled_at %}<div class="r6d"><span class="k">{{ t('Compiled', '编排时间') }}</span><span class="vv num">{{ ss.compiled_at }}</span></div>{% endif %}
        </div>
        {# identity_proof PROVEN is a quiet receipt line, never a banner. #}
        <p class="ss-receipt">{{ t(ss.identity.en, ss.identity.zh) }}{% if ss.identity.legs %} · {{ ss.identity.legs|length }} {{ t('source checks', '项来源核对') }}{% endif %}{% if ss.identity.equalities %} · {{ ss.identity.equalities|length }} {{ t('matches', '项一致') }}{% endif %}{% if ss.identity.why_en %} — {{ t(ss.identity.why_en, ss.identity.why_zh) }}{% endif %}</p>
      </section>

      {# The evidence read's own receipt: which records were asked for, which
         were used, and what happened to the rest. The zeros print too. #}
      {% if ss.evidence.compile_state or ss.evidence.counts %}
      <section class="dpanel">
        <h3>{{ t('What went into the evidence', '证据的构成') }}<span class="ss-prov">{{ t('counted here', '本页统计') }}</span></h3>
        <div class="dkv">
          {% if ss.evidence.compile_state %}<div class="r6d"><span class="k">{{ t('Evidence compiled', '证据编排结果') }}</span><span class="vv ss-t-{{ ss.evidence.compile_state.tone }} ss-tone-ink">{{ t(ss.evidence.compile_state.en, ss.evidence.compile_state.zh) }}</span></div>{% endif %}
          {% for c in ss.evidence.counts %}<div class="r6d"><span class="k">{{ t(c.en, c.zh) }}</span><span class="vv num">{{ c.v }}</span></div>{% endfor %}
          {% if ss.evidence.compiled_at %}<div class="r6d"><span class="k">{{ t('Compiled', '编排时间') }}</span><span class="vv num">{{ ss.evidence.compiled_at }}</span></div>{% endif %}
        </div>
        {% if ss.evidence.conflicts %}<ul class="dnotes">{% for c in ss.evidence.conflicts %}<li>{{ c }}</li>{% endfor %}</ul>{% endif %}
      </section>
      {% endif %}

      {% if ss.identity.legs or ss.identity.refusals or ss.identity.disclosures %}
      <section class="dpanel">
        <h3>{{ t('Identity checks', '身份核对') }}</h3>
        {% for lg in ss.identity.legs %}
        <div class="ss-chk ss-t-{{ lg.tone }}">
          <div class="ss-chk-h">
            <span class="r">{{ lg.check }}</span>
            <span class="v">{{ t(lg.result_en, lg.result_zh) }}</span>
            {% if lg.code %}<span class="c ss-id">{{ lg.code }}</span>{% endif %}
          </div>
          {% if lg.desc_en %}<p class="ss-chk-d">{{ t(lg.desc_en, lg.desc_zh) }}</p>{% endif %}
          {% if lg.artifact_en %}<p class="ss-chk-s">{{ t(lg.artifact_en, lg.artifact_zh) }}</p>{% endif %}
          {% if lg.reader_en %}<p class="ss-chk-s">{{ t('read by', '读取方') }} {{ t(lg.reader_en, lg.reader_zh) }}</p>{% endif %}
          {% if lg.reads %}<dl class="ss-chk-v">{% for v in lg.reads %}<dt>{% if v.label_en %}{{ t(v.label_en, v.label_zh) }}{% else %}<span class="ss-id">{{ v.k }}</span>{% endif %}</dt><dd>{% if v.v_en %}{% if v.is_id %}<span class="ss-id">{{ t(v.v_en, v.v_zh) }}</span>{% else %}{{ t(v.v_en, v.v_zh) }}{% endif %}{% else %}—{% endif %}</dd>{% endfor %}</dl>{% endif %}
        </div>
        {% endfor %}
        {% if ss.identity.equalities %}<ul class="dnotes">{% for e in ss.identity.equalities %}<li>{% if e.label_en %}{{ t(e.label_en, e.label_zh) }}{% else %}<span class="ss-id">{{ e.check }}</span>{% endif %}: {% if e.left_is_id %}<span class="ss-id">{{ t(e.left_en, e.left_zh) }}</span>{% else %}{{ t(e.left_en, e.left_zh) }}{% endif %} = {% if e.right_is_id %}<span class="ss-id">{{ t(e.right_en, e.right_zh) }}</span>{% else %}{{ t(e.right_en, e.right_zh) }}{% endif %} — {{ t(e.verdict_en, e.verdict_zh) }}</li>{% endfor %}</ul>{% endif %}
        {% if ss.identity.refusals %}<div class="dfoot">{{ t('Held back', '暂不呈现') }}: {% for r in ss.identity.refusals %}<span class="dfoot-row">{{ t(r.en, r.zh) }} <span class="c ss-id">{{ r.code }}</span></span>{% if not loop.last %}<br>{% endif %}{% endfor %}</div>{% endif %}
        {% if ss.identity.disclosures %}<div class="dfoot">{% for d in ss.identity.disclosures %}<span class="dfoot-row">{{ t(d.en, d.zh) }}{% if d.code %} <span class="c ss-id">{{ d.code }}</span>{% endif %}</span>{% if not loop.last %}<br>{% endif %}{% endfor %}</div>{% endif %}
      </section>
      {% endif %}

      {% if ss.last_good.ok %}
      <section class="dpanel">
        <h3>{{ t('Last complete read', '上次完整读数') }}</h3>
        <div class="dkv">
          <div class="r6d"><span class="k">{{ t('Built', '生成时间') }}</span><span class="vv num">{{ ss.last_good.at }}</span></div>
          {% if ss.last_good.deg %}<div class="r6d"><span class="k">{{ t('Its own coverage', '其自身覆盖情况') }}</span><span class="vv ss-t-{{ ss.last_good.deg.tone }} ss-tone-ink">{{ t(ss.last_good.deg.en, ss.last_good.deg.zh) }}</span></div>{% endif %}
          {% if ss.last_good.sha %}<div class="r6d"><span class="k">{{ t('Content fingerprint', '内容指纹') }}</span><span class="vv ss-id">{{ ss.last_good.sha }}</span></div>{% endif %}
        </div>
        {% if ss.last_good.reason %}<p class="ss-receipt">{{ t('Why this earlier read is named', '为何列出这一较早读数') }}: {{ t(ss.last_good.reason.en, ss.last_good.reason.zh) }}</p>{% endif %}
      </section>
      {% endif %}

      <div class="dfoot">{{ t("This panel is a receipt. Anything untagged is quoted from the owner named beside it. The tagged lines — the counts, the pick of the biggest open question, and the estimated window — are worked out here from those quotes under a fixed rule. Nothing is estimated to cover a gap, and no missing source is replaced by a neutral value.", '本面板为凭证。未加标注的内容均引用自旁注来源方；标注项 — 各项计数、最大未决问题的选取，以及预计窗口 — 由本页依据上述引用按固定规则得出。缺失的来源不会被推算填补，也不会以中性数值替代。') }}</div>
    </div>
  </div>
</div>
{%- endif %}

{% if deep %}
{% for d in deep.dialogs %}
<div class="dsr-dlg" id="dlg-{{ d.id }}" role="dialog" aria-labelledby="dlg-{{ d.id }}-title">
  <div class="dsr-dlg-backdrop"></div>
  <div class="dsr-dlg-panel">
    <div class="dsr-dlg-header">
      <span class="dsr-dlg-title" id="dlg-{{ d.id }}-title">{{ t(d.title_en, d.title_zh) }}</span>
      <button class="dsr-dlg-close" onclick="dsrCloseDlg()" aria-label="Close">✕</button>
    </div>
    <div class="dsr-dlg-body">
      {% for pnl in d.panels %}
      <section class="dpanel">
        {% if pnl.title_en %}<h3>{{ t(pnl.title_en, pnl.title_zh) }}</h3>{% endif %}
        {% if pnl.kind == 'cards' %}
        <div class="dcards">
          {% for c in pnl.cards %}
          <div class="dcard"><div class="dk">{{ t(c.k_en, c.k_zh) }}</div><div class="dv num {{ c.cls or '' }}">{{ c.v }}</div></div>
          {% endfor %}
        </div>
        {% elif pnl.kind == 'kv' %}
        <div class="dkv">
          {% for r in pnl.rows %}
          <div class="r6d"><span class="k">{{ t(r.k_en, r.k_zh) }}</span>
            <span class="vv num {{ r.cls or '' }}">{{ r.v }}{% if r.v_en %}{{ t(r.v_en, r.v_zh) }}{% endif %}</span></div>
          {% endfor %}
        </div>
        {% elif pnl.kind == 'table' %}
        <div class="dtable-wrap"><table class="dtable">
          <thead><tr>{% for h in pnl.head %}<th>{{ t(h[0], h[1]) }}</th>{% endfor %}</tr></thead>
          <tbody>
          {% for row in pnl.rows %}
          <tr>{% for cell in row %}<td>{% if cell is mapping %}{{ t(cell.en, cell.zh) }}{% else %}{{ cell }}{% endif %}</td>{% endfor %}</tr>
          {% endfor %}
          </tbody>
        </table></div>
        {% elif pnl.kind == 'notes' %}
        <ul class="dnotes">
          {% for ln in pnl.lines %}<li class="{{ ln.cls or '' }}">{{ t(ln.en, ln.zh) }}</li>{% endfor %}
        </ul>
        {% endif %}
        {% if pnl.foot_en %}<div class="dfoot">{{ t(pnl.foot_en, pnl.foot_zh) }}</div>{% endif %}
      </section>
      {% endfor %}
      <div class="dfoot">{{ t('Research context only — refreshed nightly. Not investment advice.', '仅供研究参考 — 每晚更新，不构成投资建议。') }}</div>
    </div>
  </div>
</div>
{% endfor %}
{% endif %}

</main>

<script>
(function(){
  /* Terminal chart embed: src carries current theme + lang; retint on toggle. */
  var frame = document.getElementById('chart-frame');
  var ifr = document.getElementById('chart-embed');
  if (frame && ifr) {
    var loaded = false;
    function src(){
      var d = document.documentElement;
      var u = ifr.getAttribute('data-embed');
      if ((d.getAttribute('data-theme') || 'dark') === 'light') u += '&theme=light';
      if (d.getAttribute('data-lang') === 'zh') u += '&lang=zh';
      return u;
    }
    ifr.addEventListener('load', function(){ if (ifr.getAttribute('src')) { loaded = true; frame.classList.add('loaded'); frame.classList.remove('failed'); } });
    ifr.setAttribute('src', src());
    // 'failed' catches hard failures only (origin unreachable / frame blocked) —
    // a served page always fires load; /embed/chart renders its own empty states.
    setTimeout(function(){ if (!loaded) frame.classList.add('failed'); }, 8000);
    new MutationObserver(function(){
      var next = src();
      if (ifr.getAttribute('src') !== next) { loaded = false; frame.classList.remove('loaded'); ifr.setAttribute('src', next); }
    }).observe(document.documentElement, {attributes: true, attributeFilter: ['data-theme', 'data-lang']});
  }

  /* Reveal-on-scroll */
  var rvs = document.querySelectorAll('.rv');
  if ('IntersectionObserver' in window) {
    var io = new IntersectionObserver(function(es){
      es.forEach(function(e){ if (e.isIntersecting) { e.target.classList.add('in'); io.unobserve(e.target); } });
    }, {rootMargin: '0px 0px -8% 0px', threshold: 0.06});
    rvs.forEach(function(el){ io.observe(el); });
  } else {
    rvs.forEach(function(el){ el.classList.add('in'); });
  }

  /* Sticky strip after hero + scroll-spy */
  var hero = document.getElementById('top'), strip = document.getElementById('v6strip');
  if (hero && strip && 'IntersectionObserver' in window) {
    new IntersectionObserver(function(es){
      strip.classList.toggle('vis', !es[0].isIntersecting);
      strip.setAttribute('aria-hidden', es[0].isIntersecting ? 'true' : 'false');
    }, {rootMargin: '-56px 0px 0px 0px'}).observe(hero);
    var links = {};
    document.querySelectorAll('#v6jumps a').forEach(function(a){ links[a.getAttribute('href').slice(1)] = a; });
    var spy = new IntersectionObserver(function(es){
      es.forEach(function(e){
        if (e.isIntersecting) {
          Object.keys(links).forEach(function(k){ links[k].classList.remove('is-on'); });
          var l = links[e.target.id]; if (l) l.classList.add('is-on');
        }
      });
    }, {rootMargin: '-30% 0px -60% 0px'});
    Object.keys(links).forEach(function(id){ var el = document.getElementById(id); if (el) spy.observe(el); });
  }
})();
</script>


<script>
/* dsrOpenDlg — the macro.html mx5 dialog system (focus trap, scroll lock,
   double-rAF entrance, animated close, Escape + backdrop, #dlg- deep links). */
(function(){
  var _active=null,_trigger=null,_scrollY=0;
  function _reduced(){return !!(window.matchMedia&&window.matchMedia('(prefers-reduced-motion:reduce)').matches);}
  function _focusables(d){return d.querySelectorAll('button,[href],input,select,textarea,[tabindex]:not([tabindex="-1"])');}
  function _trap(d,e){
    if(e.key!=='Tab')return;
    var f=_focusables(d);if(!f.length)return;
    var first=f[0],last=f[f.length-1];
    if(e.shiftKey){if(document.activeElement===first){e.preventDefault();last.focus();}}
    else{if(document.activeElement===last){e.preventDefault();first.focus();}}
  }
  function _teardown(d){
    d.classList.remove('open','dsr-in','dsr-closing');
    d.removeAttribute('aria-modal');
    if(d._trapH){d.removeEventListener('keydown',d._trapH);delete d._trapH;}
    var bd=d.querySelector('.dsr-dlg-backdrop');
    if(bd&&bd._h){bd.removeEventListener('click',bd._h);delete bd._h;}
    if(d._timer){clearTimeout(d._timer);delete d._timer;}
  }
  function _release(){
    document.documentElement.classList.remove('dsr-dlg-lock');
    document.body.style.top='';
    window.scrollTo(0,_scrollY);
  }
  window.dsrOpenDlg=function(id){
    if(_active){_teardown(_active);_active=null;}
    var d=document.getElementById(id);if(!d)return;
    _trigger=document.activeElement;_active=d;
    d.classList.remove('dsr-closing');
    d.classList.add('open');d.setAttribute('aria-modal','true');
    if(!document.documentElement.classList.contains('dsr-dlg-lock')){
      _scrollY=window.pageYOffset||document.documentElement.scrollTop||0;
      document.body.style.top=(-_scrollY)+'px';
      document.documentElement.classList.add('dsr-dlg-lock');
    }
    if(_reduced()){d.classList.add('dsr-in');}
    else{requestAnimationFrame(function(){requestAnimationFrame(function(){if(_active===d)d.classList.add('dsr-in');});});}
    var f=_focusables(d);if(f.length)f[0].focus();
    d._trapH=function(e){_trap(d,e);};d.addEventListener('keydown',d._trapH);
    var bd=d.querySelector('.dsr-dlg-backdrop');
    if(bd){bd._h=function(){window.dsrCloseDlg();};bd.addEventListener('click',bd._h);}
  };
  window.dsrCloseDlg=function(){
    if(!_active)return;
    var d=_active;_active=null;_release();
    var trig=_trigger;_trigger=null;
    var refocus=function(){if(trig&&typeof trig.focus==='function')trig.focus();};
    if(_reduced()){_teardown(d);refocus();return;}
    d.classList.add('dsr-closing');d.classList.remove('dsr-in');
    var done=function(){_teardown(d);refocus();};
    d._timer=setTimeout(done,340);
  };
  document.addEventListener('keydown',function(e){
    if(e.key==='Escape'&&_active){e.stopImmediatePropagation();window.dsrCloseDlg();}
  },true);
  function _hash(){var h=(location.hash||'').slice(1);if(h&&h.indexOf('dlg-')===0)window.dsrOpenDlg(h);}
  if(document.readyState==='loading'){document.addEventListener('DOMContentLoaded',_hash);}else{_hash();}
})();
</script>

</body>
</html>
