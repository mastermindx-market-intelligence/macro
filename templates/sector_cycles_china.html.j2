<!DOCTYPE html>
<html lang="en" class="cyc-page sc-page sc-china">
{# China Sector Cycle Intelligence — the A-share sibling of sector_cycles.html.
   REUSES the same design system (cycle.css), SVG engine (mm_charts.js) and orchestration
   (sector_cycles.js) — but every line is a Shenwan (申万) L1 sector index or a curated
   A-share thematic basket, and the forward layer is the evidence-gated, honest stack from
   engine/china_sector_cycles + engine/china_sector_pathway. Data + narratives are emitted by
   scripts/build_china_sector_cycles.py as sector_cycles_china_data.js; this template owns the
   page shell + the EXPERIMENTAL / honest-forecast provenance frame. #}
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<script>try{var t=localStorage.getItem('theme');document.documentElement.setAttribute('data-theme',t||'dark');var l=localStorage.getItem('lang')||(function(){try{var L=navigator.languages||[navigator.language||navigator.userLanguage||''],i;for(i=0;i<L.length;i++)if((L[i]||'').toLowerCase().slice(0,2)==='zh')return'zh';if(/Shanghai|Hong_Kong|Macau|Urumqi|Chongqing|Harbin|Kashgar|Chungking/.test(Intl.DateTimeFormat().resolvedOptions().timeZone||''))return'zh';}catch(e){}return'';})();if(l)document.documentElement.setAttribute('data-lang',l);}catch(e){}</script>
<title>China Sector Cycle Intelligence — Mastermind</title>
{% set seo_title = "China Sector Cycles — Mastermind" %}{% set seo_desc = "Every China A-share sector placed on its cycle clock — early, late, and rotating leadership, updated after each Asia close." %}{% set seo_path = "sector_cycles_china.html" %}
{% include "_seo_head.html.j2" %}
<link rel="stylesheet" href="theme.css">
<link rel="stylesheet" href="cycle.css?v=4">
<link rel="stylesheet" href="sector_cycles.css">
{# Preload the China cycle critical path (parallel download); prefetch the after-first-paint chunks.
   optimize_assets stamps ?v=<hash> on these hrefs; sector_cycles.js vUrl() reuses them. #}
<link rel="preload" href="sector_cycles_china_data.js" as="script">
<link rel="preload" href="mm_charts.js" as="script">
<link rel="preload" href="sector_cycles.js" as="script">
<link rel="prefetch" href="sector_cycles_china_series_data.js" as="script">
<link rel="prefetch" href="sector_cycles_china_narr_data.js" as="script">
<link rel="prefetch" href="sector_cycles_china_dna_data.js" as="script">
<link rel="stylesheet" href="macro-desk.css?v=e46e15d5">
</head>
<body class="macro-desk page-cycle">
{% include "_site_nav.html.j2" %}

<div class="cyc-wrap">
  <header class="cyc-hd">
    <div class="ht">
      <div class="cyc-kicker"><span class="hb"></span> <span class="l-en">China Sector Cycle Intelligence</span><span class="l-zh">中国板块周期情报</span> <span class="sc-flag">🇨🇳</span></div>
      <h1 class="cyc-title"><span class="l-en">Where every China sector stands</span><span class="l-zh">每个中国板块所处的位置</span></h1>
      <p class="cyc-sub"><span class="l-en">All 31 Shenwan (申万) L1 sectors and the curated A-share thematic baskets on one cycle clock —
        real price (rebased &amp; log, so every line shares an axis) with a one-tap <b>cycle-position</b> view. Peaks &amp; troughs are
        auto-detected from the deep tape (back to 1999), each is graded by where it sits, and the forward read is the
        <b>honest stack</b>: where we are now, the typical next-turn rhythm, and — for the GS-style sectors — evidence-gated
        conditional odds.</span><span class="l-zh">全部 31 个申万一级行业与精选 A 股主题篮子同处一张周期时钟——真实价格（再基准化并取对数，使每条线共用同一坐标轴），
        可一键切换为<b>周期位置</b>视图。波峰与波谷由深度行情（追溯至 1999 年）自动识别，每个板块按其所处阶段评级；前瞻部分采用<b>诚实分层</b>：
        当前位置、典型的下一拐点节奏，以及（针对 GS 式行业）经证据门槛的条件概率。</span></p>
    </div>
    <div class="cyc-ctrls">
      <span class="cyc-asof"><span class="l-en">As of</span><span class="l-zh">截至</span> <span id="sc-asof">…</span></span>
    </div>
  </header>

  {# ---- EXPERIMENTAL / honest-forecast provenance frame (Phase-0) ---- #}
  <div class="sc-provenance" role="note">
    <span class="sc-prov-chip">⚗︎ <span class="l-en">EXPERIMENTAL</span><span class="l-zh">实验性</span></span>
    <p class="sc-prov-text"><span class="l-en"><b>How the forward read works here.</b> House Phase-0 testing
      (<i>research/CHINA_SECTOR_PATHWAY_PHASE0.md</i>) found that <b>no single China driver clears a strict significance bar</b>
      on monthly sector returns, and the A-share trend-gate edge does not hold — so this page makes <b>no point-forecast claim of alpha</b>.
      What it shows, layered by how much the evidence supports it: <b>(1)</b> where each sector sits on the washout↔euphoria axis (the
      one stable signature); <b>(2)</b> a next-turn projection from the sector's own median half-cycle — a <i>rhythm</i> estimate, not a
      backtested forecast; <b>(3)</b> for Banks · Consumption · Real Estate · Auto, an empirical <b>conditional probability</b> with a
      confidence interval ("when the setup looked like today, forward returns were positive X% of the time vs a Y% base rate"). Conditioning, not a buy signal.
      Every dated call is logged so its accuracy can be measured over time.</span><span class="l-zh"><b>本页前瞻读数的工作方式。</b>
      内部 Phase-0 检验（<i>research/CHINA_SECTOR_PATHWAY_PHASE0.md</i>）显示，<b>没有任何单一中国驱动因子能通过严格显著性门槛</b>，
      A 股趋势门控的边际在此也不成立——因此本页<b>不做点位预测、不宣称超额收益</b>。按证据支持度分层展示：<b>(1)</b> 每个板块在
      超卖↔过热轴上的位置（唯一稳定的特征）；<b>(2)</b> 基于板块自身中位半周期的下一拐点推演——这是<i>节奏</i>估计，并非回测预测；
      <b>(3)</b> 针对银行·消费·地产·汽车，给出带置信区间的经验<b>条件概率</b>（“当配置与今天相似时，未来收益为正的历史频率约为 X%，基准为 Y%”）。
      属于条件化背景，而非买卖信号。每个带日期的判断都会被记录，以便长期衡量其准确度。</span></p>
  </div>

  <div class="sc-tabs" id="sc-tabs" role="tablist">
    <button class="sc-tab on" data-fam="sectors" role="tab">🧭 <span class="l-en">Shenwan Sectors</span><span class="l-zh">申万行业</span> <i id="sc-tab-n-sec"></i></button>
    <button class="sc-tab" data-fam="baskets" role="tab">🧺 <span class="l-en">Thematic Baskets</span><span class="l-zh">主题篮子</span> <i id="sc-tab-n-bsk"></i></button>
  </div>

  <section class="cyc-stage">
    <div class="cyc-hero">
      <div class="cyc-hero-hd">
        <h2 id="sc-hero-title"><span class="l-en">Sector rotation overlay</span><span class="l-zh">板块轮动叠加</span></h2>
        <div class="cyc-hero-tools">
          <div class="sc-modes" id="sc-modes" aria-label="Chart mode">
            <button class="sc-mbtn" data-mode="price" aria-pressed="false"><span class="l-en">Price</span><span class="l-zh">价格</span></button>
            <button class="sc-mbtn on" data-mode="osc" aria-pressed="true"><span class="l-en">Cycle position</span><span class="l-zh">周期位置</span></button>
          </div>
          <div class="sc-mode-cap"><span class="l-en">Low = washed-out · High = stretched</span><span class="l-zh">低 = 超卖 · 高 = 拉伸</span></div>
          <div class="sc-advanced" id="sc-advanced">
            <button type="button" class="sc-adv-toggle" id="sc-adv-toggle" aria-expanded="false" aria-controls="sc-advbody"><span class="l-en">Chart options</span><span class="l-zh">图表选项</span></button>
            <div class="sc-adv-body" id="sc-advbody">
              <div class="sc-scale disabled" id="sc-scale" hidden aria-label="Price scale">
                <button class="sc-sbtn on" data-scale="log" aria-pressed="true"><span class="l-en">Log</span><span class="l-zh">对数</span></button>
                <button class="sc-sbtn" data-scale="linear" aria-pressed="false"><span class="l-en">Linear</span><span class="l-zh">线性</span></button>
              </div>
              <div class="cyc-legend" id="sc-legend" aria-label="Legend">
                <span><i class="lc" style="background:color-mix(in srgb,var(--down) 42%,transparent)"></i> <span class="l-en">washed-out</span><span class="l-zh">超卖</span></span>
                <span><i class="lc" style="background:color-mix(in srgb,var(--muted) 32%,transparent)"></i> <span class="l-en">mid-cycle</span><span class="l-zh">中段</span></span>
                <span><i class="lc" style="background:color-mix(in srgb,var(--warn) 46%,transparent)"></i> <span class="l-en">stretched</span><span class="l-zh">拉伸</span></span>
              </div>
              <div class="cyc-zoom" id="sc-zoom" aria-label="Zoom"></div>
            </div>
          </div>
        </div>
      </div>
      <div class="cyc-groups" id="sc-groups" aria-label="Phase filter"></div>
      <div class="cyc-chips" id="sc-chips" aria-label="Sector toggles"></div>
      <div class="sc-baskets" id="sc-baskets" aria-label="Thematic baskets"></div>
      <span id="sc-sr-status" class="sc-sr-status" role="status" aria-live="polite"></span>
      <div class="sc-chartwrap">
        <div id="sc-chart" data-h="600" role="img" aria-label="China sector cycle clock — each line a sector's 0 to 100 cycle position over time. Tap a line for its story."></div>
        <div class="sc-chart-hint" id="sc-chart-hint" hidden></div>
      </div>
    </div>

    <aside class="cyc-detail" id="sc-detail">
      <div class="cyc-handle" id="sc-handle" aria-hidden="true"></div>
      <div class="cyc-panels">
        <div class="cyc-panel show" id="sc-panel-default"></div>
        <div class="cyc-panel" id="sc-panel-focus"></div>
      </div>
    </aside>
  </section>

  <div class="cyc-cards-hd">
    <h2><span class="l-en">Sector scorecards</span><span class="l-zh">板块记分卡</span></h2>
    <span class="sub"><span class="l-en">Tap any card — or its line on the chart — to focus it</span><span class="l-zh">点按任意卡片（或图中其曲线）以聚焦</span></span>
  </div>
  <div class="cyc-cards" id="sc-cards"></div>
</div>

<script src="sector_cycles_china_data.js"></script>
<script src="mm_charts.js"></script>
<script>window.SC_EXTRA_DATA=['sector_cycles_china_narr_data.js','sector_cycles_china_dna_data.js'];window.SC_SERIES_DATA='sector_cycles_china_series_data.js';</script>
<script src="sector_cycles.js"></script>
<script>try{document.getElementById('sc-asof').textContent=(window.SECTOR_CYCLES&&window.SECTOR_CYCLES.meta||{}).asOf||'';}catch(e){}</script>
<script src="theme.js"></script>
<!-- RC-R14: rotation-event chips on basket cards (display-only context) -->
<style>
  .rc-cn-chip { display:inline-block; font-size:10.5px; font-weight:600; padding:2px 7px; border-radius:6px;
    border:1px solid color-mix(in srgb,var(--link) 35%,transparent);
    background:color-mix(in srgb,var(--link) 8%,transparent); color:var(--ink-link, var(--link));
    margin-top:5px; text-decoration:none; cursor:pointer; white-space:nowrap; }
  .rc-cn-chip:hover { background:color-mix(in srgb,var(--link) 16%,transparent); }
</style>
<script>
(function(){
'use strict';
function isZh(){return document.documentElement.getAttribute('data-lang')==='zh';}
// Inject rotation-event handoff chips onto matching basket cards.
// basket cards in sector_cycles have data-id = "b-" + basket_cn_key (e.g. "b-cn_semis").
function injectChipsCn(payload){
  if(!payload||!payload.active||!payload.active.length) return;
  // Map leg key → event(s) it participates in
  var byLeg={};
  payload.active.forEach(function(ev){
    [ev.from_leg,ev.to_leg].forEach(function(leg){
      if(!leg) return;
      if(!byLeg[leg.key]) byLeg[leg.key]=[];
      byLeg[leg.key].push(ev);
    });
  });
  Object.keys(byLeg).forEach(function(legKey){
    var card=document.querySelector('[data-id="b-'+legKey+'"]');
    if(!card) return;
    var ev=byLeg[legKey][0];
    var role=ev.from_leg.key===legKey?'from':'to';
    var label=role==='from'
      ?(isZh()?'⟲ 轮出 → '+(ev.to_leg.name_zh||ev.to_leg.name_en)+' 第'+ev.day_n+'天'
                :'⟲ out→'+(ev.to_leg.name_en)+' day '+ev.day_n)
      :(isZh()?'⟲ 轮入自 '+(ev.from_leg.name_zh||ev.from_leg.name_en)+' 第'+ev.day_n+'天'
                :'⟲ in from '+(ev.from_leg.name_en)+' day '+ev.day_n);
    var prev=card.querySelector('.rc-cn-chip');
    if(prev) prev.remove();
    var chip=document.createElement('a');
    chip.className='rc-cn-chip';
    chip.href='sector_central_china.html#rc-events-cn';
    chip.textContent=label;
    card.appendChild(chip);
  });
}
// Cards are rendered after sector_cycles.js mounts. Poll once after a brief delay
// then re-run on lang/theme change.
function tryInject(){
  fetch('marketdata/rotation_events_china.json',{cache:'no-cache'})
    .then(function(r){return r.ok?r.json():null;}).catch(function(){return null;})
    .then(function(data){if(data) injectChipsCn(data);});
}
// Wait for cards to be DOM-present before first inject
var _injTries=0;
function waitAndInject(){
  var cards=document.querySelectorAll('#sc-cards .cyc-card[data-id^="b-"]');
  if(cards.length>0){tryInject();return;}
  if(_injTries++<20) setTimeout(waitAndInject,300);
}
waitAndInject();
var _obs=new MutationObserver(function(){tryInject();});
_obs.observe(document.documentElement,{attributes:true,attributeFilter:['data-lang']});
})();
</script>
</body>
</html>
