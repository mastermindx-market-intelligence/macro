{% macro t(en, zh='') -%}<span class="l-en">{{ en }}</span><span class="l-zh">{{ zh if zh else en }}</span>{%- endmacro %}
<!doctype html>
<html lang="en">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover">
{% include "_interfonts.html.j2" %}
<script>try{var th=localStorage.getItem('theme');document.documentElement.setAttribute('data-theme',th||'dark');var l=localStorage.getItem('lang')||(function(){try{var L=navigator.languages||[navigator.language||''];for(var i=0;i<L.length;i++)if((L[i]||'').toLowerCase().slice(0,2)==='zh')return'zh'}catch(e){}return''})();if(l)document.documentElement.setAttribute('data-lang',l)}catch(e){}</script><link rel="stylesheet" href="theme.css">
<link rel="stylesheet" href="government-revenue-parity.css">
<title>Government Revenue Foresight — Mastermind</title>
{% set seo_title = "Government Revenue Foresight — Mastermind" %}
{% set seo_desc = "Federal award changes, official opportunities and derived expiry watches mapped to public companies with evidence." %}
{% set seo_path = "government_revenue.html" %}
{% include "_seo_head.html.j2" %}
<style>
:root{
  --gr-accent:#62dbe8;--gr-blue:#88a5ff;--gr-good:#72d6aa;--gr-warn:#e6b65e;--gr-bad:#ee7783;
  --gr-ink:color-mix(in srgb,var(--text) 88%,#fff 12%);--gr-muted:color-mix(in srgb,var(--text) 57%,var(--muted));
  --gr-faint:color-mix(in srgb,var(--text) 36%,var(--muted));--gr-line:color-mix(in srgb,var(--text) 10%,transparent);
  --gr-line2:color-mix(in srgb,var(--text) 16%,transparent);--gr-surface:color-mix(in srgb,var(--panel) 96%,transparent);
  --gr-raised:color-mix(in srgb,var(--panel2) 83%,var(--panel));--gr-font:Inter,-apple-system,BlinkMacSystemFont,'SF Pro Text',system-ui,sans-serif;
  --gr-mono:'SFMono-Regular',Consolas,'Liberation Mono',monospace;--gr-shadow:0 34px 90px -52px rgba(0,0,0,.86),0 9px 28px -23px rgba(0,0,0,.76);
}
html[data-theme="light"]{--gr-accent:#05758c;--gr-blue:#365dc5;--gr-good:#147852;--gr-warn:#9e6108;--gr-bad:#b72f45;--gr-line:rgba(21,37,55,.09);--gr-line2:rgba(21,37,55,.16);--gr-surface:#fff;--gr-raised:#f4f6f8;--gr-shadow:0 34px 80px -55px rgba(28,48,76,.36),0 8px 28px -25px rgba(28,48,76,.25)}
*{box-sizing:border-box}html{scroll-behavior:smooth}html:not([data-lang="zh"]) .l-zh{display:none}html[data-lang="zh"] .l-en{display:none}
body{margin:0;padding:18px 0 90px;background:var(--bg);color:var(--text);font:13px/1.45 var(--gr-font);font-variant-numeric:tabular-nums;-webkit-font-smoothing:antialiased}
html[data-lang="zh"] body{font-family:"PingFang SC","Hiragino Sans GB","Microsoft YaHei",Inter,sans-serif}
button,input,select{font:inherit}button{color:inherit}a{color:var(--ink-link, var(--link));text-decoration:none}.gr-wrap{width:min(1580px,calc(100% - 42px));margin:0 auto}.skip-link{position:fixed;z-index:1000;left:18px;top:10px;transform:translateY(-160%);padding:9px 12px;border:1px solid var(--gr-accent);border-radius:8px;background:var(--panel);color:var(--text);font-weight:750}.skip-link:focus{transform:none}
.gr-header{padding:33px 3px 19px}.gr-kicker{display:flex;align-items:center;gap:9px;color:var(--gr-accent);font-size:9px;font-weight:800;letter-spacing:.17em;text-transform:uppercase}.gr-kicker:before{content:"";width:22px;height:1px;background:currentColor}.gr-title-row{display:flex;justify-content:space-between;align-items:flex-end;gap:28px;margin-top:13px}.gr-title h1{margin:0;font-size:clamp(32px,4.1vw,59px)!important;line-height:.98;letter-spacing:-.052em;font-weight:790}.gr-title p{max-width:720px;margin:11px 0 0;color:var(--gr-muted);font-size:13px}.gr-cut{flex:none;min-width:205px;padding-left:17px;border-left:1px solid var(--gr-line2);color:var(--gr-muted);font-size:9px}.gr-cut strong{display:block;margin-bottom:3px;color:var(--text);font-size:11px}.gr-cut time{font-family:var(--gr-mono);font-size:8.5px}
.gr-pulse{display:flex;align-items:stretch;border-top:1px solid var(--gr-line);border-bottom:1px solid var(--gr-line);min-height:61px;margin-top:21px}.pulse-lead{display:flex;align-items:center;gap:11px;min-width:238px;padding:11px 20px 11px 0}.pulse-orbit{position:relative;width:30px;height:30px;border:1px solid color-mix(in srgb,var(--gr-accent) 45%,var(--gr-line));border-radius:50%;flex:none}.pulse-orbit:before{content:"";position:absolute;width:6px;height:6px;left:11px;top:11px;border-radius:50%;background:var(--gr-accent);box-shadow:0 0 0 5px color-mix(in srgb,var(--gr-accent) 11%,transparent),0 0 22px color-mix(in srgb,var(--gr-accent) 44%,transparent)}.pulse-lead strong{display:block;font-size:10.5px}.pulse-lead span{font-size:9px;color:var(--gr-muted)}.pulse-stat{min-width:130px;padding:10px 19px;border-left:1px solid var(--gr-line);display:flex;flex-direction:column;justify-content:center}.pulse-stat b{font-size:16px;letter-spacing:-.025em}.pulse-stat span{color:var(--gr-muted);font-size:8.5px}.pulse-spacer{flex:1}.pulse-policy{display:flex;align-items:center;max-width:310px;padding:10px 0 10px 18px;border-left:1px solid var(--gr-line);font-size:9px;color:var(--gr-muted)}
.company-filmstrip{display:grid;grid-template-columns:minmax(210px,auto) minmax(0,1fr);align-items:center;gap:15px;margin:13px 0 16px;padding:9px 11px 9px 13px;border:1px solid var(--gr-line);border-radius:12px;background:linear-gradient(100deg,color-mix(in srgb,var(--gr-accent) 6%,transparent),transparent 46%),var(--gr-surface);overflow:hidden}.company-filmstrip-copy{min-width:0}.company-filmstrip-kicker{display:block;color:var(--gr-accent);font-size:9px;font-weight:800;letter-spacing:.13em;text-transform:uppercase}.company-filmstrip-title{display:block;margin-top:2px;color:var(--text);font-size:10px;font-weight:760}.company-filmstrip-caption{display:block;margin-top:2px;color:var(--gr-muted);font-size:9px;line-height:1.35}.company-filmstrip-legend{display:flex;flex-wrap:wrap;gap:6px;margin-top:6px;color:var(--gr-faint);font-size:9px}.company-filmstrip-key{display:inline-flex;align-items:center;gap:4px;white-space:nowrap}.company-filmstrip-key i,.company-ticker-state:before{content:"";width:5px;height:5px;border-radius:50%;background:currentColor;box-shadow:0 0 0 3px color-mix(in srgb,currentColor 10%,transparent)}.company-filmstrip-key.linked{color:var(--gr-good)}.company-filmstrip-key.pending{color:var(--gr-warn)}.company-filmstrip-track{display:flex;align-items:stretch;gap:6px;min-width:0;max-width:100%;overflow-x:auto;overscroll-behavior-inline:contain;scroll-snap-type:inline proximity;scrollbar-width:thin;padding:2px 1px 5px}.company-filmstrip-track::-webkit-scrollbar{height:5px}.company-filmstrip-track::-webkit-scrollbar-thumb{background:var(--gr-line2);border-radius:999px}.company-filmstrip-item{flex:0 0 auto;scroll-snap-align:start}.company-ticker{display:grid;grid-template-columns:auto 1fr;gap:7px;align-items:center;min-width:112px;min-height:43px;padding:7px 9px;border:1px solid var(--gr-line);border-radius:8px;background:color-mix(in srgb,var(--gr-raised) 64%,transparent);text-align:left;cursor:pointer;transition:border-color .16s ease,background .16s ease}.company-ticker:hover{border-color:color-mix(in srgb,var(--gr-accent) 45%,var(--gr-line));background:color-mix(in srgb,var(--gr-accent) 8%,var(--gr-raised))}.company-ticker:focus-visible{outline:2px solid var(--gr-accent);outline-offset:2px}.company-ticker-symbol{color:var(--text);font:760 10px var(--gr-mono);letter-spacing:.02em}.company-ticker-state{display:flex;align-items:center;gap:4px;margin-top:2px;color:var(--gr-faint);font-size:9px;line-height:1.15;white-space:nowrap}.company-ticker.state-candidate .company-ticker-state{color:var(--gr-good)}.company-ticker.state-mapping .company-ticker-state{color:var(--gr-warn)}
.company-ticker.state-verified .company-ticker-state{color:var(--gr-blue)}.company-ticker.state-locked .company-ticker-state{color:var(--gr-accent)}
.gr-shell{min-height:694px;border:1px solid var(--gr-line2);border-radius:18px;background:var(--gr-surface);box-shadow:var(--gr-shadow);overflow:hidden}.shell-top{min-height:55px;display:flex;align-items:center;justify-content:space-between;gap:18px;padding:8px 12px 8px 17px;border-bottom:1px solid var(--gr-line)}.mode-tabs{display:flex;align-items:center;gap:4px}.mode-tab{position:relative;border:0;border-radius:8px;background:transparent;padding:9px 11px;cursor:pointer;color:var(--gr-muted);font-size:10px;font-weight:720}.mode-tab:after{content:"";position:absolute;left:10px;right:10px;bottom:-9px;height:2px;background:transparent}.mode-tab[aria-selected="true"]{color:var(--text);background:var(--gr-raised)}.mode-tab[aria-selected="true"]:after{background:var(--gr-accent)}.mode-count{margin-left:5px;color:var(--gr-faint);font:700 8px var(--gr-mono)}.shell-tools{display:flex;align-items:center;gap:9px}.health{display:flex;align-items:center;gap:7px;color:var(--gr-muted);font-size:9px}.health i{width:6px;height:6px;border-radius:50%;background:var(--gr-good)}.health.warn i{background:var(--gr-warn)}.health.bad i{background:var(--gr-bad)}.tool-btn{display:inline-flex;align-items:center;justify-content:center;gap:6px;min-height:32px;border:1px solid var(--gr-line2);border-radius:8px;background:var(--gr-raised);padding:6px 10px;color:var(--gr-muted);font-size:9px;font-weight:700;cursor:pointer}.tool-btn:hover{border-color:color-mix(in srgb,var(--gr-accent) 48%,var(--gr-line));color:var(--text)}.workspace-degraded{display:flex;align-items:center;justify-content:space-between;gap:14px;padding:8px 12px 8px 16px;border-bottom:1px solid color-mix(in srgb,var(--gr-warn) 36%,var(--gr-line));background:linear-gradient(90deg,color-mix(in srgb,var(--gr-warn) 9%,transparent),transparent 72%);color:var(--gr-muted);font-size:9px}.workspace-degraded[hidden]{display:none}.workspace-degraded[data-state="loading"]{border-bottom-color:color-mix(in srgb,var(--gr-blue) 35%,var(--gr-line));background:linear-gradient(90deg,color-mix(in srgb,var(--gr-blue) 8%,transparent),transparent 72%)}.workspace-degraded[data-state="locked"]{border-bottom-color:color-mix(in srgb,var(--gr-accent) 38%,var(--gr-line));background:linear-gradient(90deg,color-mix(in srgb,var(--gr-accent) 9%,transparent),transparent 72%)}.workspace-degraded-copy{display:flex;min-width:0;align-items:center;gap:8px;line-height:1.4}.workspace-degraded-copy i{width:7px;height:7px;flex:none;border-radius:50%;background:var(--gr-warn);box-shadow:0 0 0 4px color-mix(in srgb,var(--gr-warn) 12%,transparent)}.workspace-degraded[data-state="loading"] .workspace-degraded-copy i{background:var(--gr-blue)}.workspace-degraded[data-state="locked"] .workspace-degraded-copy i{background:var(--gr-accent)}.workspace-degraded .tool-btn{min-height:27px;flex:none;padding:4px 8px}.workspace-degraded .tool-btn[hidden]{display:none}a.tool-btn{text-decoration:none}.mobile-only{display:none}
.shell-body{display:grid;grid-template-columns:216px minmax(520px,.95fr) minmax(410px,.75fr);min-height:638px}.filter-pane,.queue-pane,.inspector-pane{min-width:0}.filter-pane{border-right:1px solid var(--gr-line);background:color-mix(in srgb,var(--gr-raised) 35%,transparent)}.queue-pane{border-right:1px solid var(--gr-line)}.pane-head{min-height:53px;padding:13px 15px;border-bottom:1px solid var(--gr-line)}.pane-head strong{display:block;font-size:10px}.pane-head span{display:block;margin-top:2px;color:var(--gr-muted);font-size:8.5px}.filter-body{padding:14px}.filter-group{padding:0 0 17px}.filter-group+.filter-group{padding-top:15px;border-top:1px solid var(--gr-line)}.filter-label{display:flex;align-items:center;justify-content:space-between;margin-bottom:8px;color:var(--gr-muted);font-size:8px;font-weight:800;letter-spacing:.12em;text-transform:uppercase}.filter-label kbd{font:7.5px var(--gr-mono);padding:2px 4px;border:1px solid var(--gr-line2);border-radius:4px;background:var(--gr-surface)}.field{width:100%;border:1px solid var(--gr-line2);border-radius:8px;background:var(--gr-surface);color:var(--text);padding:8px 9px;outline:0;font-size:10px}.field:focus{border-color:var(--gr-accent);box-shadow:0 0 0 3px color-mix(in srgb,var(--gr-accent) 10%,transparent)}select.field{appearance:auto}.filter-stack{display:grid;gap:4px}.filter-choice{width:100%;border:0;border-radius:7px;background:transparent;padding:7px 8px;text-align:left;cursor:pointer;color:var(--gr-muted);font-size:9.5px}.filter-choice:hover{background:var(--gr-raised);color:var(--text)}.filter-choice.on{background:color-mix(in srgb,var(--gr-accent) 10%,var(--gr-raised));color:var(--gr-accent);font-weight:720}.filter-choice b{float:right;color:var(--gr-faint);font:700 8px var(--gr-mono)}.filter-note{font-size:8.5px;color:var(--gr-muted);line-height:1.5}.mini-health{margin-top:7px;display:grid;grid-template-columns:7px 1fr;gap:7px;align-items:start}.mini-health i{width:6px;height:6px;margin-top:4px;border-radius:50%;background:var(--gr-good)}.mini-health.warn i{background:var(--gr-warn)}.mini-health.bad i{background:var(--gr-bad)}
.queue-head{display:flex;align-items:center;justify-content:space-between;gap:12px;min-height:53px;padding:10px 14px;border-bottom:1px solid var(--gr-line)}.queue-title strong{display:block;font-size:10.5px}.queue-title span{display:block;color:var(--gr-muted);font-size:8.5px;margin-top:2px}.queue-sort{font-size:8px;color:var(--gr-faint);text-transform:uppercase;letter-spacing:.09em}.queue-list{height:585px;overflow:auto;scrollbar-gutter:stable}.queue-row{position:relative;width:100%;min-height:99px;display:grid;grid-template-columns:5px minmax(0,1fr);gap:12px;padding:13px 15px 12px 12px;border:0;border-bottom:1px solid var(--gr-line);background:transparent;text-align:left;cursor:pointer}.queue-row:hover{background:color-mix(in srgb,var(--gr-accent) 4%,transparent)}.queue-row.on{background:color-mix(in srgb,var(--gr-accent) 7%,transparent)}.queue-row.on:after{content:"";position:absolute;right:0;top:0;bottom:0;width:2px;background:var(--gr-accent)}.queue-rail{width:3px;height:100%;min-height:54px;border-radius:4px;background:var(--gr-line2)}.queue-rail.official{background:var(--gr-accent)}.queue-rail.derived{background:var(--gr-warn)}.queue-top{display:flex;align-items:center;justify-content:space-between;gap:10px}.truth-set{display:flex;align-items:center;gap:5px;min-width:0}.truth{display:inline-flex;align-items:center;gap:4px;max-width:180px;padding:2px 6px;border:1px solid var(--gr-line2);border-radius:5px;color:var(--gr-muted);font-size:7.5px;font-weight:760;letter-spacing:.06em;text-transform:uppercase;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.truth:before{content:"";width:4px;height:4px;border-radius:50%;background:currentColor;flex:none}.truth.official{color:var(--gr-accent)}.truth.derived{color:var(--gr-warn)}.truth.reviewed{color:var(--gr-good)}.queue-time{color:var(--gr-faint);font:8px var(--gr-mono);white-space:nowrap}.queue-main{margin-top:7px;font-size:11px;line-height:1.35;font-weight:680;display:-webkit-box;-webkit-box-orient:vertical;-webkit-line-clamp:2;overflow:hidden}.queue-sub{margin-top:5px;color:var(--gr-muted);font-size:8.8px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.queue-foot{display:flex;align-items:center;gap:7px;margin-top:7px;color:var(--gr-faint);font-size:8px}.queue-foot .ticker-token{color:var(--gr-blue);font:750 8px var(--gr-mono)}
.empty-state{display:grid;place-items:center;min-height:250px;padding:30px;text-align:center;color:var(--gr-muted)}.empty-mark{width:37px;height:37px;border:1px solid var(--gr-line2);border-radius:50%;display:grid;place-items:center;margin:0 auto 12px;color:var(--gr-accent);font-size:18px}.empty-state strong{display:block;color:var(--text);font-size:11px}.empty-state p{max-width:36ch;margin:6px auto 0;font-size:9px;line-height:1.5}
.inspector-pane{height:638px;overflow:auto}.inspector-empty{height:100%;display:grid;place-items:center;padding:30px;text-align:center;color:var(--gr-muted)}.inspector-empty strong{display:block;color:var(--text);font-size:12px}.inspector-empty p{max-width:38ch;font-size:9px}.inspector{padding-bottom:22px}.inspect-hero{padding:17px 18px 15px;border-bottom:1px solid var(--gr-line);background:color-mix(in srgb,var(--gr-raised) 37%,transparent)}.inspect-kicker{display:flex;justify-content:space-between;gap:10px;align-items:center}.inspect-type{color:var(--gr-accent);font-size:8px;font-weight:800;letter-spacing:.12em;text-transform:uppercase}.inspect-id{font:8px var(--gr-mono);color:var(--gr-faint);max-width:175px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.inspect-hero h2{font-size:18px;line-height:1.18;letter-spacing:-.025em;margin:10px 0 7px}.inspect-meta{color:var(--gr-muted);font-size:9px}.inspect-truth{display:flex;flex-wrap:wrap;gap:5px;margin-top:11px}.inspect-actions{display:flex;gap:6px;margin-top:13px}.inspect-actions .tool-btn{background:var(--gr-surface)}.inspect-section{padding:13px 18px;border-bottom:1px solid var(--gr-line)}.inspect-section:last-child{border-bottom:0}.inspect-label{margin-bottom:7px;color:var(--gr-faint);font-size:7.5px;font-weight:800;letter-spacing:.13em;text-transform:uppercase}.inspect-value{color:var(--gr-ink);font-size:10px;line-height:1.55}.inspect-value strong{color:var(--text)}.stance{display:flex;gap:10px;align-items:flex-start;padding:10px 11px;border-left:2px solid var(--gr-warn);background:color-mix(in srgb,var(--gr-warn) 6%,transparent)}.stance b{color:var(--gr-warn);font-size:9.5px;white-space:nowrap}.stance span{color:var(--gr-muted);font-size:9px;line-height:1.45}.diff-list{display:grid;gap:5px}.diff-row{display:grid;grid-template-columns:minmax(90px,.55fr) 1fr;gap:10px;padding:6px 8px;background:var(--gr-raised);border-radius:6px;font-size:8.5px}.diff-row b{color:var(--gr-muted);font-weight:600}.facts-grid{display:grid;grid-template-columns:1fr 1fr;border-top:1px solid var(--gr-line);border-left:1px solid var(--gr-line)}.fact-cell{min-height:54px;padding:8px 9px;border-right:1px solid var(--gr-line);border-bottom:1px solid var(--gr-line)}.fact-cell span{display:block;color:var(--gr-faint);font-size:7.5px;text-transform:uppercase;letter-spacing:.06em}.fact-cell b{display:block;margin-top:4px;font-size:9px;font-weight:650;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}
.fact-cell.moved{box-shadow:inset 2px 0 0 var(--fc-dir)}
.fact-cell.up{--fc-dir:var(--ink-up,var(--up))}
.fact-cell.down{--fc-dir:var(--ink-down,var(--down))}
.fact-cell.flat{--fc-dir:var(--gr-faint)}
.fact-cell i{display:block;margin-top:3px;font-style:normal;font-size:8px;font-weight:600;color:var(--fc-dir,var(--gr-muted));white-space:nowrap;overflow:hidden;text-overflow:ellipsis}
.fact-cell i u{font-family:var(--gr-mono);font-weight:650;text-decoration:none;letter-spacing:-.01em}
.fact-cell.fact-more b{color:var(--gr-muted);font-weight:600}
.transmission-list{display:grid;gap:6px}.transmission{padding:8px 9px;border:1px solid var(--gr-line);border-radius:7px}.transmission b{color:var(--gr-blue);font:750 9px var(--gr-mono)}.transmission span{display:block;margin-top:3px;color:var(--gr-muted);font-size:8.5px}.limit-copy{color:var(--gr-muted);font-size:8.5px;line-height:1.5}.source-link{display:flex;align-items:center;justify-content:space-between;gap:12px;margin-top:7px;padding:8px 9px;border:1px solid var(--gr-line);border-radius:7px;color:var(--text);font-size:8.5px}.source-link:hover{border-color:var(--gr-accent)}.source-link span{color:var(--gr-accent)}.crosslinks{display:flex;flex-wrap:wrap;gap:6px}.crosslink{border:1px solid var(--gr-line2);border-radius:999px;background:transparent;padding:5px 8px;cursor:pointer;color:var(--gr-muted);font-size:8px}.crosslink:hover{color:var(--text);border-color:var(--gr-accent)}
.gr-footer{display:flex;justify-content:space-between;gap:20px;padding:13px 3px;color:var(--gr-muted);font-size:8.5px}.gr-footer a{font-weight:700}.gr-footer .scope{text-align:right}
.candidate-empty{display:grid;place-items:center;min-height:280px;padding:31px 27px;text-align:center;background:radial-gradient(circle at 50% 0,color-mix(in srgb,var(--gr-accent) 12%,transparent),transparent 56%);color:var(--gr-muted)}.candidate-empty .empty-mark{margin-bottom:11px}.candidate-empty strong{display:block;color:var(--text);font-size:12px}.candidate-empty p{max-width:35ch;margin:7px auto 0;font-size:9px;line-height:1.55}.candidate-empty-meta{display:flex;justify-content:center;flex-wrap:wrap;gap:5px;margin-top:13px}.candidate-empty-meta span{padding:4px 7px;border:1px solid var(--gr-line);border-radius:999px;background:var(--gr-raised);color:var(--gr-faint);font:700 7.5px var(--gr-mono)}.candidate-row{position:relative;width:100%;display:grid;grid-template-columns:4px minmax(0,1fr);gap:12px;padding:14px 15px 13px 12px;border:0;border-bottom:1px solid var(--gr-line);background:linear-gradient(105deg,color-mix(in srgb,var(--gr-accent) 4%,transparent),transparent 66%);text-align:left;cursor:pointer}.candidate-row:hover{background:linear-gradient(105deg,color-mix(in srgb,var(--gr-accent) 9%,transparent),transparent 72%)}.candidate-row.on{background:linear-gradient(105deg,color-mix(in srgb,var(--gr-accent) 13%,transparent),transparent 76%)}.candidate-row.on:after{content:"";position:absolute;right:0;top:0;bottom:0;width:2px;background:var(--gr-accent)}.candidate-rail{width:3px;min-height:69px;border-radius:4px;background:var(--gr-good)}.candidate-top,.candidate-foot{display:flex;align-items:center;justify-content:space-between;gap:8px}.candidate-state{display:inline-flex;align-items:center;gap:5px;min-width:0;color:var(--gr-good);font-size:7.4px;font-weight:800;letter-spacing:.08em;text-transform:uppercase}.candidate-state:before{content:"";width:4px;height:4px;border-radius:50%;background:currentColor}.candidate-time{color:var(--gr-faint);font:8px var(--gr-mono);white-space:nowrap}.candidate-name{margin-top:8px;color:var(--text);font-size:12px;font-weight:760;letter-spacing:-.015em}.candidate-name code{margin-right:5px;color:var(--gr-blue);font:750 9px var(--gr-mono)}.candidate-change{display:block;margin-top:5px;color:var(--gr-muted);font-size:8.8px;line-height:1.45}.candidate-foot{justify-content:flex-start;flex-wrap:wrap;margin-top:9px}.candidate-foot span{color:var(--gr-faint);font-size:7.6px}.candidate-foot .candidate-channel{color:var(--gr-accent);font-weight:720}.candidate-crosschecks{display:flex;flex-wrap:wrap;gap:4px}.candidate-crosscheck{padding:3px 5px;border:1px solid var(--gr-line);border-radius:4px;color:var(--gr-faint);font-size:6.8px}.candidate-crosscheck.ok{border-color:color-mix(in srgb,var(--gr-good) 45%,var(--gr-line));color:var(--gr-good)}.candidate-crosscheck.mixed{border-color:color-mix(in srgb,var(--gr-warn) 45%,var(--gr-line));color:var(--gr-warn)}.candidate-proof{display:grid;gap:6px;padding:10px;border-left:2px solid var(--gr-good);background:color-mix(in srgb,var(--gr-good) 6%,transparent)}.candidate-proof strong{font-size:9px;color:var(--gr-good)}.candidate-proof span,.candidate-crosscheck-grid span{color:var(--gr-muted);font-size:8.3px;line-height:1.5}.candidate-crosscheck-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:6px}.candidate-crosscheck-grid div{display:grid;gap:3px;padding:8px;border:1px solid var(--gr-line);border-radius:7px;background:var(--gr-raised)}.candidate-crosscheck-grid b{font-size:7.4px;letter-spacing:.06em;text-transform:uppercase}.candidate-crosscheck-grid .ok b{color:var(--gr-good)}.candidate-crosscheck-grid .mixed b{color:var(--gr-warn)}.candidate-crosscheck-grid .pending b{color:var(--gr-faint)}
button:focus-visible,input:focus-visible,select:focus-visible,a:focus-visible{outline:2px solid var(--gr-accent);outline-offset:2px}
@media(max-width:1250px){.shell-body{grid-template-columns:196px minmax(470px,1fr) minmax(365px,.78fr)}.pulse-policy{display:none}}
@media(max-width:980px){body{padding-bottom:112px}.gr-wrap{width:min(100% - 24px,760px)}.gr-title-row{display:block}.gr-cut{margin-top:16px;padding:9px 0 0;border-left:0;border-top:1px solid var(--gr-line)}.gr-pulse{overflow:auto;scroll-snap-type:x proximity}.pulse-lead,.pulse-stat{scroll-snap-align:start}.pulse-lead{min-width:210px}.pulse-stat{min-width:120px}.gr-shell{min-height:650px}.shell-body,.gr-shell.dossier-open .shell-body{display:block;min-height:590px}.filter-pane,.inspector-pane,.gr-shell.dossier-open .inspector-pane{display:none;height:auto}.queue-pane{border-right:0}.queue-list{height:auto;max-height:none;min-height:530px}.mobile-only{display:inline-flex}.mobile-summary{position:fixed;z-index:50;display:flex;align-items:center;justify-content:space-between;gap:12px;left:12px;right:82px;bottom:12px;min-height:62px;padding:9px 10px 9px 13px;border:1px solid var(--gr-line2);border-radius:13px;background:color-mix(in srgb,var(--panel) 94%,transparent);box-shadow:0 18px 55px -25px rgba(0,0,0,.85);backdrop-filter:blur(18px)}.mobile-summary-copy{min-width:0}.mobile-summary-copy b,.mobile-summary-copy span{display:block;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.mobile-summary-copy b{font-size:10px}.mobile-summary-copy span{margin-top:3px;color:var(--gr-muted);font-size:8px}.mobile-sheet{display:block!important;position:fixed;z-index:992;left:8px;right:8px;bottom:8px;top:auto;max-height:min(82vh,760px);height:auto;border:1px solid var(--gr-line2);border-radius:16px;background:var(--panel);box-shadow:0 -30px 90px -42px rgba(0,0,0,.92);overflow:auto}.mobile-sheet.filter-pane{border-right:1px solid var(--gr-line2);height:auto}.mobile-sheet.inspector-pane,.gr-shell.dossier-open .mobile-sheet.inspector-pane{height:min(82vh,760px);display:block}.shell-top{overflow-x:auto}.mode-tab{white-space:nowrap}}
@media(max-width:620px){.gr-header{padding-top:25px}.gr-title h1{font-size:35px!important}.gr-title p{font-size:11.5px}.gr-pulse{margin-top:16px}.pulse-stat{min-width:105px;padding-left:13px;padding-right:13px}.pulse-stat b{font-size:14px}.company-filmstrip{display:block;margin-top:10px}.company-filmstrip-track{margin-top:8px}.shell-top{padding-left:8px;gap:8px}.mode-tab{padding-left:8px;padding-right:8px}.health{display:none}.gr-footer{display:block}.gr-footer .scope{text-align:left;margin-top:5px}.queue-row{padding-right:12px}.candidate-crosscheck-grid{grid-template-columns:1fr}.inspect-hero h2{font-size:16px}}
@media(prefers-reduced-motion:no-preference){.queue-row,.mode-tab,.tool-btn,.filter-choice{transition:background .15s ease,color .15s ease,border-color .15s ease}.pulse-orbit:after{content:"";position:absolute;inset:-7px;border:1px solid color-mix(in srgb,var(--gr-accent) 16%,transparent);border-radius:50%;animation:pulse-ring 3.1s ease-out infinite}@keyframes pulse-ring{0%,60%{transform:scale(.8);opacity:0}70%{opacity:1}100%{transform:scale(1.35);opacity:0}}}
@media(prefers-reduced-motion:reduce){html{scroll-behavior:auto}*{animation-duration:.01ms!important;animation-iteration-count:1!important;transition-duration:.01ms!important}}
.queue-rail.observed{background:var(--gr-blue)}.truth.observed{color:var(--gr-blue)}
.truth.conflict{color:var(--gr-bad)}.truth.historic{color:var(--gr-muted)}
.truth.late{color:var(--gr-warn)}.truth.correction{color:var(--gr-blue)}
#companyBridge>.inspect-label{margin-top:12px}#companyBridge>.inspect-label:first-child{margin-top:0}
</style></head>
<body>
<a class="skip-link" href="#gov-workspace">{{ t('Skip to procurement intelligence','跳至政府采购情报') }}</a>
<div class="gr-wrap">
{% include "_site_nav.html.j2" %}
<header class="gr-header">
<div class="gr-kicker">{{ t('Vertical intelligence · procurement','垂直情报 · 政府采购') }}</div>
<div class="gr-title-row">
<div class="gr-title"><h1>{{ t('Government Revenue','政府收入') }} <span style="color:var(--gr-accent)">{{ t('Foresight','前瞻') }}</span></h1><p>{{ t('See the contract change, then trace the public-company exposure. Watch the evidence—do not trade the headline alone.','先看合同变化，再追踪上市公司暴露。观察证据，不要只凭标题交易。') }}</p></div>
<div class="gr-cut"><strong>{{ t('Evidence cut','证据截点') }} · {{ as_of or '—' }}</strong>{{ t('Last assembled','最后汇总') }} <time id="knownAt" data-iso="{{ known_at or '' }}">{{ known_at or '—' }}</time></div>
</div>
<div class="gr-pulse" aria-label="Procurement pulse summary">
<div class="pulse-lead"><span class="pulse-orbit" aria-hidden="true"></span><div><strong id="pulseStance">{{ t('Watch the procurement tape','观察采购脉搏') }}</strong><span id="pulseStatus">{{ t('Reading official receipts','正在读取官方凭证') }}</span></div></div>
<div class="pulse-stat"><b id="statChanges">—</b><span>{{ t('governed changes','受治理变化') }}</span></div>
<div class="pulse-stat"><b id="statOpps">—</b><span>{{ t('active opportunities','活跃机会') }}</span></div>
<div class="pulse-stat"><b id="statDeadlines">—</b><span>{{ t('closing in 30 days','30 天内截止') }}</span></div>
<div class="pulse-stat"><b id="statExposure">—</b><span>{{ t('mapped exposure','已映射暴露') }}</span></div>
<div class="pulse-spacer"></div>
<div class="pulse-policy">{{ t('Official procurement facts may enrich research. Derived links cannot rank, size or trigger a trade.','官方采购事实可丰富研究。推导关联不得排序、调整仓位或触发交易。') }}</div>
</div>
</header>
<section class="company-filmstrip" aria-labelledby="companyFilmstripTitle">
<div class="company-filmstrip-copy">
<span class="company-filmstrip-kicker">{{ t('Company coverage','公司覆盖') }}</span>
<strong class="company-filmstrip-title" id="companyFilmstripTitle">{{ t('Follow the issuer file','追踪公司档案') }}</strong>
<span class="company-filmstrip-caption">{{ t('Linked records open Candidate Radar; every other name opens its company file.','已关联记录打开候选雷达；其他公司打开其公司档案。') }}</span>
<div class="company-filmstrip-legend"><span class="company-filmstrip-key linked"><i aria-hidden="true"></i>{{ t('Evidence linked','证据已关联') }}</span><span class="company-filmstrip-key pending"><i aria-hidden="true"></i>{{ t('Link pending','关联待核') }}</span><span class="company-filmstrip-key"><i aria-hidden="true"></i>{{ t('Company file','公司档案') }}</span></div>
</div>
<div class="company-filmstrip-track" id="companyFilmstrip" role="list"></div>
</section>
<main class="gr-shell" id="gov-workspace">
<div class="shell-top">
<div class="mode-tabs" role="tablist" aria-label="Procurement workspace mode">
<button class="mode-tab" type="button" role="tab" aria-selected="false" tabindex="-1" data-mode="candidates">{{ t('Candidate Radar','候选雷达') }}<span class="mode-count" id="countCandidates">—</span></button>
<button class="mode-tab" type="button" role="tab" aria-selected="true" tabindex="0" data-mode="changes">{{ t('Changes','变化') }}<span class="mode-count" id="countChanges">—</span></button>
<button class="mode-tab" type="button" role="tab" aria-selected="false" tabindex="-1" data-mode="awards">{{ t('Award tape','授标脉搏') }}<span class="mode-count" id="countAwards">—</span></button>
<button class="mode-tab" type="button" role="tab" aria-selected="false" tabindex="-1" data-mode="opportunities">{{ t('Opportunities','机会') }}<span class="mode-count" id="countOpps">—</span></button>
<button class="mode-tab" type="button" role="tab" aria-selected="false" tabindex="-1" data-mode="recompetes">{{ t('Recompete watch','再竞争观察') }}<span class="mode-count" id="countRecompetes">—</span></button>
<button class="mode-tab" type="button" role="tab" aria-selected="false" tabindex="-1" data-mode="budget">{{ t('Budget & programs','预算与项目') }}<span class="mode-count" id="countBudget">—</span></button>
<button class="mode-tab" type="button" role="tab" aria-selected="false" tabindex="-1" data-mode="programs">{{ t('Program Dossier','项目档案') }}<span class="mode-count" id="countPrograms">—</span></button>
<button class="mode-tab" type="button" role="tab" aria-selected="false" tabindex="-1" data-mode="companies">{{ t('Companies','公司') }}<span class="mode-count" id="countCompanies">—</span></button>
      <button class="mode-tab" type="button" role="tab" aria-selected="false" tabindex="-1" data-mode="fms">{{ t('FMS Congressional Notifications','FMS 国会通知') }}<span class="mode-count" id="countFms">—</span></button>
</div>
<div class="shell-tools">
<div class="health" id="sourceHealth"><i aria-hidden="true"></i><span id="healthCopy">{{ t('Checking sources','检查数据源') }}</span></div>
<button id="companyMode" type="button" hidden aria-hidden="true" tabindex="-1">{{ t('Companies','公司') }}</button>
<button class="tool-btn mobile-only" type="button" id="filterOpen" aria-controls="filterPane" aria-expanded="false">{{ t('Filters','筛选') }}</button>
</div>
</div>
<div class="workspace-degraded" id="workspaceDegraded" role="status" aria-live="polite" hidden>
<div class="workspace-degraded-copy"><i aria-hidden="true"></i><span id="workspaceDegradedCopy">{{ t('Showing the compact evidence cut while the complete workspace loads.','完整工作区加载期间显示精简证据截点。') }}</span></div>
<a class="tool-btn" id="workspaceUnlock" href="plans.html" hidden>{{ t('View membership plans','查看会员方案') }}</a>
<button class="tool-btn" type="button" id="workspaceRetry" hidden>{{ t('Retry full workspace','重试完整工作区') }}</button>
</div>
<div class="shell-body">
<aside class="filter-pane" id="filterPane" aria-label="Procurement filters">
<div class="pane-head"><strong>{{ t('Narrow the tape','缩小脉搏范围') }}</strong><span>{{ t('Every filter updates this evidence cut','所有筛选均基于当前证据截点') }}</span></div>
<div class="filter-body">
<div class="filter-group"><div class="filter-label"><span>{{ t('Search','搜索') }}</span><kbd>/</kbd></div><input class="field" id="deskSearch" type="search" autocomplete="off" placeholder="Notice, agency or ticker" aria-label="Search notice, agency or ticker"></div>
<div class="filter-group"><div class="filter-label">{{ t('Truth layer','证据层') }}</div><div class="filter-stack" id="truthFilters"><button class="filter-choice on" type="button" data-filter="all">{{ t('All evidence','全部证据') }}<b id="filterAllCount">—</b></button><button class="filter-choice" type="button" data-filter="official">{{ t('Official facts','官方事实') }}</button><button class="filter-choice" type="button" data-filter="linked">{{ t('Issuer-linked','已关联公司') }}</button><button class="filter-choice" type="button" data-filter="defense">{{ t('Defense only','仅国防') }}</button></div></div>
<div class="filter-group"><label class="filter-label" for="agencyFilter">{{ t('Agency','机构') }}</label><select class="field" id="agencyFilter"><option value="">All agencies</option></select></div>
<div class="filter-group"><label class="filter-label" for="tickerFilter">{{ t('Public company','上市公司') }}</label><select class="field" id="tickerFilter"><option value="">All companies</option></select></div>
<div class="filter-group briefcase" id="researchBriefcase">
<div class="filter-label"><span>{{ t('Research briefcase','研究公文包') }}</span><span>{{ t('this device','本设备') }}</span></div>
<select class="field" id="savedViewSelect" aria-label="Saved research views"><option value="">{{ t('Current unsaved view','当前未保存视图') }}</option></select>
<div class="briefcase-row"><input class="field" id="savedViewName" maxlength="60" autocomplete="off" placeholder="Name this view" aria-label="Name this saved view"><button class="tool-btn" type="button" id="saveView">{{ t('Save','保存') }}</button></div>
<div class="briefcase-actions"><button class="tool-btn" type="button" id="deleteView" disabled>{{ t('Delete','删除') }}</button><button class="tool-btn" type="button" id="localInbox">{{ t('Inbox','收件箱') }}<span class="briefcase-count" id="localInboxCount">0</span></button></div>
<select class="field" id="alertType" aria-label="Local alert type"><option value="opportunity">{{ t('Opportunity change','机会变化') }}</option><option value="award_change">{{ t('Award / action change','授标 / 行动变化') }}</option><option value="recompete">{{ t('Derived expiry watch','推导到期观察') }}</option></select>
<button class="tool-btn" type="button" id="toggleLocalAlert" disabled>{{ t('Enable local alert','启用本地提醒') }}</button>
<div class="briefcase-actions"><button class="tool-btn" type="button" id="exportViewJson">{{ t('Export JSON','导出 JSON') }}</button><button class="tool-btn" type="button" id="exportViewCsv">{{ t('Export CSV','导出 CSV') }}</button></div>
<div class="briefcase-status" id="briefcaseStatus" role="status" aria-live="polite">{{ t('Saved views and alerts stay in this browser. Alerts are checked only when you open or refresh this page.','保存的视图和提醒仅保留在此浏览器中。提醒仅在打开或刷新此页面时检查。') }}</div>
</div>
<div class="filter-group"><div class="filter-label">{{ t('Source health','数据源状态') }}</div><div id="healthDetail" class="filter-note"></div></div>
</div>
</aside>
<section class="queue-pane" aria-labelledby="queueHeading">
<div class="queue-head"><div class="queue-title"><strong id="queueHeading">{{ t('Decision queue','决策队列') }}</strong><span id="queueSummary">{{ t('Latest material changes first','最新重要变化优先') }}</span></div><span class="queue-sort" id="queueSort">{{ t('newest first','最新优先') }}</span></div>
<div class="queue-list" id="queueList" role="listbox" aria-label="Procurement evidence queue" tabindex="0" aria-live="polite"></div>
</section>
<aside class="inspector-pane" id="inspectorPane" aria-label="Investor inspector">
<div id="inspector" class="inspector-empty"><div><span class="empty-mark" aria-hidden="true">⌁</span><strong>{{ t('Select a change','选择一项变化') }}</strong><p>{{ t('Open a row to trace what changed, why it may matter and the official receipt.','打开一行，追踪变化、潜在影响及官方凭证。') }}</p></div></div>
</aside>
</div>
</main>
<footer class="gr-footer"><div>{{ t('Official sources','官方来源') }} · <a href="https://sam.gov/content/opportunities" target="_blank" rel="noopener">SAM.gov</a> · <a href="https://www.usaspending.gov/" target="_blank" rel="noopener">USAspending.gov</a></div><div class="scope">{{ t('Research context only · not company guidance, GAAP backlog or a trade signal','仅作研究情境 · 非公司指引、GAAP 积压或交易信号') }}</div></footer>
</div>
<div class="mobile-summary" id="mobileSummary" hidden><div class="mobile-summary-copy"><b id="mobileTitle">{{ t('Select a change','选择一项变化') }}</b><span id="mobileMeta">{{ t('Official evidence and investor context','官方证据与投资者情境') }}</span></div><button class="tool-btn" type="button" id="inspectorOpen" aria-controls="inspectorPane" aria-expanded="false">{{ t('Inspect','查看') }}</button></div>
<div class="drawer-backdrop" id="drawerBackdrop" hidden></div>
<aside class="evidence-drawer" id="evidenceDrawer" role="dialog" aria-modal="true" aria-labelledby="drawerTitle" hidden><div class="drawer-head"><strong id="drawerTitle">{{ t('Evidence receipts','证据凭证') }}</strong><button class="tool-btn" type="button" id="drawerClose">{{ t('Close','关闭') }} · Esc</button></div><div class="drawer-body" id="drawerBody"></div></aside>
<script id="gov-data" type="application/json">{{ payload_json|replace('</', '<\/')|safe }}</script><script data-sync src="government-revenue-dossiers.js"></script><script data-sync src="government-revenue-candidate-radar.js"></script><script data-sync src="government-revenue-briefcase.js"></script><script data-sync src="government-revenue-briefcase-ui.js"></script><script>
(function(){
  'use strict';
  var DATA={};
  try{DATA=JSON.parse((document.getElementById('gov-data')||{}).textContent||'{}')}catch(e){DATA={companies:[],load_error:true}}
  var COMPANIES=arr(DATA.companies).filter(obj),OPPINT=obj(DATA.opportunity_intelligence)?DATA.opportunity_intelligence:{},OPPS=arr(OPPINT.opportunities).filter(obj),EVENTS=arr(OPPINT.events).filter(obj),WORKSPACE=obj(DATA.procurement_workspace)?DATA.procurement_workspace:{},WORKSPACE_EVENTS=arr(WORKSPACE.events).filter(obj),HAS_WORKSPACE=/^government_procurement_workspace\.v[12]$/.test(text(WORKSPACE.schema_version));
  var state={mode:'changes',truth:'all',q:'',agency:'',ticker:'',selected:'',drawer:false};
  var rowsByMode={candidates:[],changes:[],awards:[],opportunities:[],recompetes:[],budget:[],programs:[],companies:[],fms:[]},visible=[],lastFocus=null,briefcaseController=null,briefcaseWorkspaceReady=false,budgetStatus='loading',programsStatus='loading',candidateStatus='loading',fmsStatus='loading',candidateMeta={total:0,mapping_backlog_total:0,mapping_backlog_tickers:[],mapping_backlog_states:{},exact_candidate_availability:'unavailable',content_id:null},pendingSelection='',missingCandidateSelection='';
  function $(id){return document.getElementById(id)}
  function obj(x){return !!x&&typeof x==='object'&&!Array.isArray(x)}
  function arr(x){return Array.isArray(x)?x:[]}
  function zh(){return document.documentElement.getAttribute('data-lang')==='zh'}
  function tr(en,cn){return zh()?cn:en}
  function esc(x){return String(x==null?'':x).replace(/[&<>"']/g,function(c){return {'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]})}
  function n(x){if(x==null||x===''||typeof x==='boolean')return null;var v=Number(x);return Number.isFinite(v)?v:null}
  function text(x,fb){return x==null||x===''?(fb==null?'—':fb):String(x)}
  function agencyName(value){if(value==null||value==='')return '';if(typeof value==='string'){var s=value.trim();if(!s)return '';if(s.charAt(0)==='{'||s.charAt(0)==='['||s.indexOf('[object Object]')===0)return tr('Unspecified agency','未指定机构');return s}if(obj(value)){var keys=['department_name','subagency_name','office_name','name','subagency'];for(var i=0;i<keys.length;i++){var nested=value[keys[i]];if(nested==null||nested==='')continue;var label=agencyName(nested);if(label&&label!==tr('Unspecified agency','未指定机构'))return label}return ''}return ''}function agencySubName(block){if(!obj(block))return '';var sub=agencyName(block.subagency_name||block.subagency||'');var primary=agencyName(block);return (sub&&sub!==primary)?sub:''}function label(o,keys,fb){for(var i=0;i<keys.length;i++)if(o&&o[keys[i]]!=null&&o[keys[i]]!=='')return o[keys[i]];return fb==null?'—':fb}
  function money(x){x=n(x);if(x==null)return'—';var a=Math.abs(x),s=x<0?'−':'';if(a>=1e12)return s+'$'+(a/1e12).toFixed(1)+'T';if(a>=1e9)return s+'$'+(a/1e9).toFixed(1)+'B';if(a>=1e6)return s+'$'+(a/1e6).toFixed(1)+'M';if(a>=1e3)return s+'$'+(a/1e3).toFixed(1)+'K';return s+'$'+Math.round(a).toLocaleString()}
  function pct(x){x=n(x);return x==null?'—':(x>0?'+':'')+x.toFixed(1)+'%'}
  function date(x){if(!x)return'—';var d=new Date(x);if(isNaN(d.getTime()))return String(x).slice(0,10);try{return new Intl.DateTimeFormat(zh()?'zh-CN':'en-CA',{year:'numeric',month:'short',day:'2-digit',timeZone:'UTC'}).format(d)}catch(e){return String(x).slice(0,10)}}
  function ago(x){if(!x)return'';var d=new Date(x),days=Math.floor((Date.now()-d.getTime())/864e5);if(!Number.isFinite(days))return date(x);if(days<0)return tr('in ','还有 ')+Math.abs(days)+tr('d',' 天');if(days===0)return tr('today','今天');if(days===1)return tr('1d ago','1 天前');if(days<31)return days+tr('d ago',' 天前');return date(x)}
  function safeUrl(x){try{var u=new URL(String(x||''),location.href);return u.protocol==='https:'?u.href:''}catch(e){return''}}
  function safeHref(x){x=String(x||'');if(/^https:\/\//.test(x))return x;if(/^[a-zA-Z0-9_./?=&%#-]+$/.test(x)&&!x.startsWith('//'))return x;return''}
  function compact(x){if(x==null||x==='')return'—';if(typeof x==='string'||typeof x==='number'||typeof x==='boolean')return String(x);try{var s=JSON.stringify(x);return s.length>170?s.slice(0,167)+'…':s}catch(e){return text(x)}}
  function normalizedStatus(x){x=String(x||'').trim().toLowerCase();return x||null}
  function statusRank(status){if(/fail|error|blocked/.test(status||''))return 6;if(status==='stale')return 5;if(/partial|degrad/.test(status||''))return 4;if(/unavailable|unverified|future/.test(status||''))return 3;return status==='ok'?0:2}
  function statusWorse(a,b){return statusRank(b)>statusRank(a)?b:a}
  function sourceFreshness(source,nowMs){source=obj(source)?source:{};var base=normalizedStatus(source.status),stamp=source.observed_at||source.last_successful_observed_at||source.known_at||source.captured_at||null,stampMs=stamp?Date.parse(stamp):NaN,slaMinutes=n(source.freshness_sla_minutes),slaDays=n(source.freshness_sla_days),sla=slaMinutes!=null&&slaMinutes>0?slaMinutes:(slaDays!=null&&slaDays>0?slaDays*1440:null),age=Number.isFinite(stampMs)?Math.max(0,Math.floor((nowMs-stampMs)/60000)):null,aged=null,status=base||null;if(!status&&!stamp&&!sla)return null;if(sla!=null&&age!=null){if(age>sla*2)aged='stale';else if(age>sla)aged='partial';if(aged)status=statusWorse(status||'ok',aged)}return{status:status||'unverified',observed_at:stamp||null,age_minutes:age,freshness_sla_minutes:sla,aged_status:aged}}
  function effectiveFreshnessState(data,workspace,nowMs){data=obj(data)?data:{};workspace=obj(workspace)?workspace:{};var root=obj(data.freshness)?data.freshness:{},wf=obj(workspace.freshness)?workspace.freshness:{},sources=[],primary=normalizedStatus(wf.status)||normalizedStatus(root.status),awardEvents=sourceFreshness(wf.award_events||root.award_events,nowMs)||{status:'unavailable',observed_at:null,age_minutes:null,freshness_sla_minutes:null,aged_status:null};if(primary)sources.push({status:primary,observed_at:null,age_minutes:null,freshness_sla_minutes:null,aged_status:null});[wf.opportunities,wf.recompetes,root.aggregate,root.award_detail,root.actions].forEach(function(source){var assessed=sourceFreshness(source,nowMs);if(assessed)sources.push(assessed)});var status=null;sources.forEach(function(source){status=status==null?source.status:statusWorse(status,source.status)});var clocked=sources.filter(function(source){return source.age_minutes!=null}).sort(function(a,b){return b.age_minutes-a.age_minutes})[0]||null;return{status:status||'unavailable',award_events:awardEvents.status,award_event_detail:awardEvents,observed_at:clocked&&clocked.observed_at||null,age_minutes:clocked&&clocked.age_minutes!=null?clocked.age_minutes:null,freshness_sla_minutes:clocked&&clocked.freshness_sla_minutes!=null?clocked.freshness_sla_minutes:null,aged:!!(clocked&&clocked.aged_status),sources:sources}}
  function workspaceExpectedCount(workspace){workspace=obj(workspace)?workspace:{};var declared=n(workspace.total);if(declared!=null)return Math.max(0,Math.floor(declared));var visible=n((workspace.coverage||{}).events_visible);return visible!=null?Math.max(0,Math.floor(visible)):arr(workspace.events).filter(obj).length}
  function workspaceNeedsHydration(workspace){workspace=obj(workspace)?workspace:{};return !!workspace.next_cursor||arr(workspace.events).filter(obj).length<workspaceExpectedCount(workspace)}
  function workspaceIsComplete(workspace){workspace=workspace===undefined?WORKSPACE:workspace;workspace=obj(workspace)?workspace:{};return !workspace.next_cursor&&arr(workspace.events).filter(obj).length>0&&arr(workspace.events).filter(obj).length>=workspaceExpectedCount(workspace)}
  // D3 §3d: typed failure_state is authoritative when present; a missing rail
  // block fails CLOSED; pre-v3 artifacts keep the status-string read (spec §2).
  function opportunityRailUnavailable(){var wf=obj(WORKSPACE.freshness)?WORKSPACE.freshness:{},root=obj(DATA.freshness)?DATA.freshness:{},oi=obj(OPPINT.freshness)?OPPINT.freshness:{},block=obj(wf.opportunities)?wf.opportunities:null;if(HAS_WORKSPACE&&!block)return true;if(block&&Object.prototype.hasOwnProperty.call(block,'failure_state'))return block.failure_state!=null;var status=normalizedStatus((block||{}).status)||normalizedStatus((obj(root.opportunities)?root.opportunities:{}).status)||normalizedStatus(oi.status);return status==='unavailable'||status==='error'||status==='failed'}
  // D3 §3d: budget's typed `failure_state` (workspace.py `_budget_rail`).
  function budgetFailureState(){var wf=obj(WORKSPACE.freshness)?WORKSPACE.freshness:{},root=obj(DATA.freshness)?DATA.freshness:{},block=obj(wf.budget)?wf.budget:(obj(root.budget)?root.budget:null),fs=block&&block.failure_state;return fs==='projection_missing'||fs==='source_unavailable'?fs:null}
  function fallbackBudgetStatus(){var fs=budgetFailureState();return fs==='projection_missing'?'projection_missing':'unavailable'}
  // freshness.fms fallback applies ONLY with no module (mirrors budget above).
  function fmsFailureState(){var wf=obj(WORKSPACE.freshness)?WORKSPACE.freshness:{},root=obj(DATA.freshness)?DATA.freshness:{},block=obj(wf.fms)?wf.fms:(obj(root.fms)?root.fms:null),fs=block&&block.failure_state;return fs==='projection_missing'||fs==='source_unavailable'?fs:null}
  function fallbackFmsStatus(){var fs=fmsFailureState();return fs==='projection_missing'?'projection_missing':'unavailable'}
  function workspaceBundleMatches(shell,full){var expected=obj(shell)&&typeof shell.bundle_id==='string'?shell.bundle_id:'',actual=obj(full)&&typeof full.bundle_id==='string'?full.bundle_id:'',schema=obj(shell)?text(shell.schema_version):'',fullSchema=obj(full)?text(full.schema_version):'',prefix=schema==='government_procurement_workspace.v2'?'grw2':'grw1';return schema===fullSchema&&new RegExp('^'+prefix+'-[a-f0-9]{24}$').test(expected)&&expected===actual}
  function ageCopy(minutes){if(minutes==null)return'';if(minutes<60)return minutes+'m';if(minutes<1440)return (minutes/60).toFixed(minutes%60?1:0)+'h';return (minutes/1440).toFixed((minutes%1440)?1:0)+'d'}
  function opportunityCurrentStateCopy(o){o=obj(o)?o:{};if(o.current_state_verified===true)return tr('Current state re-observed','当前状态已重新观察');if(o.current_state_verified===false)return tr('Last observed — recheck needed','最后观测 — 需要复核');if(o.current_state)return String(o.current_state).replace(/_/g,' ');return tr('State not verified','状态未验证')}
  function workspaceCoverageCopy(){var coverage=obj(WORKSPACE.coverage)?WORKSPACE.coverage:{},visible=n(coverage.events_visible),available=n(coverage.events_available_before_cap),cap=n(coverage.event_cap),scope=text(coverage.facet_scope,tr('visible bounded workspace events','可见的受限工作区事件')),truncated=coverage.events_truncated===true||n(coverage.events_truncated)>0;if(!truncated&&!(available!=null&&visible!=null&&available>visible))return'';return tr('Workspace exposes ','工作区展示 ')+text(visible,0)+tr(' of ',' / ')+text(available,visible||0)+tr(' governed events',' 条受治理事件')+(cap!=null?tr(' (cap ','（上限 ')+cap+')':'')+tr('. Facets apply only to ','。筛选仅适用于 ')+scope+'.'}
  function company(ticker){return COMPANIES.find(function(c){return String(c.ticker||'')===String(ticker||'')})}
  function tickerRailState(ticker){if(rowsByMode.candidates.some(function(row){return arr(row.tickers).indexOf(ticker)>-1}))return'candidate';if((candidateMeta.mapping_backlog_states||{})[ticker]==='partial_identifier_coverage')return'verified';if(arr(candidateMeta.mapping_backlog_tickers).indexOf(ticker)>-1)return'mapping';if(candidateStatus==='locked'&&!workspaceIsComplete())return'locked';if(candidateStatus==='loading'||candidateStatus==='unavailable'||candidateStatus==='locked')return'unavailable';return'coverage'}
  function tickerRailCopy(stateName){return stateName==='candidate'?tr('Evidence linked','证据已关联'):stateName==='verified'?tr('Issuer path verified','发行人路径已核验'):stateName==='mapping'?tr('Link pending','关联待核'):stateName==='locked'?tr('Members only','会员可见'):stateName==='unavailable'?tr('Link status unavailable','关联状态不可用'):tr('Company file','公司档案')}
  function renderTickerFilmstrip(){var rail=$('companyFilmstrip');if(!rail)return;rail.innerHTML=COMPANIES.map(function(c){var ticker=text(c.ticker),stateName=tickerRailState(ticker),name=text(c.name,ticker),label=ticker+' · '+name+' · '+tickerRailCopy(stateName)+'. '+(stateName==='candidate'?tr('Open Candidate Radar','打开候选雷达'):tr('Open company file','打开公司档案'));return'<div class="company-filmstrip-item" role="listitem"><button type="button" class="company-ticker state-'+esc(stateName)+'" data-filmstrip-ticker="'+esc(ticker)+'" aria-label="'+esc(label)+'"><span class="company-ticker-symbol">'+esc(ticker)+'</span><span class="company-ticker-state">'+esc(tickerRailCopy(stateName))+'</span></button></div>'}).join('');rail.querySelectorAll('[data-filmstrip-ticker]').forEach(function(button){button.addEventListener('click',function(){openTickerFilmstrip(button.dataset.filmstripTicker)})})}
  function openTickerFilmstrip(ticker){ticker=text(ticker,'');if(!ticker)return null;var stateName=tickerRailState(ticker),target;state.q='';state.agency='';state.truth='all';$('deskSearch').value='';$('agencyFilter').value='';document.querySelectorAll('.filter-choice').forEach(function(choice){choice.classList.toggle('on',choice.dataset.filter==='all')});state.ticker=ticker;$('tickerFilter').value=ticker;state.mode=stateName==='candidate'?'candidates':'companies';activateMode(false);target=arr(rowsByMode[state.mode]).find(function(row){return arr(row.tickers).indexOf(ticker)>-1});if(target)selectRow(target.id,true);else syncUrl();return{mode:state.mode,ticker:state.ticker,selected:state.selected}}
  function opp(id){return OPPS.find(function(o){return String(o.notice_id||'')===String(id||'')})}
  function metric(c,key){return n(((c||{}).metrics||{})[key])}
  function isOfficial(r){return r.truth==='official'}
  function truthChip(kind,copy){return '<span class="truth '+esc(kind)+'">'+esc(copy)+'</span>'}
  function truthLabel(r){if(r.truthCopy)return r.truthCopy;if(r.kind==='company')return tr('Discovery coverage','发现范围覆盖');if(r.kind==='candidate')return tr('Receipt-bound event','与凭证绑定的事件');if(r.kind==='award_change'&&r.truth==='official')return tr('Official USAspending change','USAspending 官方变化');if(r.kind==='award_change'&&r.truth==='observed')return tr('Observed USAspending revision','观测到的 USAspending 修订');if(r.truth==='official')return tr('Official fact','官方事实');if(r.truth==='observed')return tr('Observed document revision','观测到的文件修订');if(r.truth==='reviewed')return tr('Reviewed map','已核验映射');return r.kind==='recompete'?tr('Derived watch','推导观察'):tr('Derived context','推导情境')}
  function reviewedImpact(r){return arr((r.workspaceEvent||{}).listed_company_impacts).filter(obj).some(function(x){return x.relation_semantic==='reviewed'})}
  function secondaryTruth(r){var o=r.workspaceEvent&&r.opportunity||{};if(r.kind==='candidate'&&r.exactIssuer)return truthChip('reviewed',tr('Exact issuer path','精确发行人路径'));if(r.kind==='company')return truthChip('observed',tr('Mapping needed','需要映射'));if(r.workspaceEvent&&r.kind==='recompete')return truthChip('official',tr('Official POP end','官方履约结束日'));if(r.workspaceEvent&&r.kind==='opportunity'&&o.current_state_verified===false)return truthChip('observed',tr('Last observed state','最后观测状态'));if(r.linked&&reviewedImpact(r))return truthChip('reviewed',tr('Reviewed issuer link','已核验发行人关联'));if(r.linked&&(r.kind==='opportunity'||r.workspaceEvent))return truthChip('derived',tr('Derived issuer link','推导公司关联'));if(r.truth==='reviewed')return truthChip('reviewed',tr('Reviewed identity','已核验身份'));return''}
  // D3 §3c: additive chip keyed only on the producer's change.is_correction.
  function correctionChip(r){return(r.kind==='award_change'&&r.workspaceEvent&&(r.workspaceEvent.change||{}).is_correction===true)?truthChip('correction',tr('Correction','更正')):''}
  // D3 §3a: late rows read BOTH clocks; non-late keep single ago(known_at).
  function queueTimeCopy(r){return r.isLate?tr('Took effect ','生效 ')+date(r.effectiveAt)+' · '+tr('found ','获知 ')+(ago(r.knownAt)||'—'):ago(r.date)}
  function workspaceTitle(e){return zh()&&e.title_zh?e.title_zh:(e.title_original||e.title_zh||tr('Procurement event','采购事件'))}
  function workspaceSummary(e){var c=e.change||{};return zh()?(c.what_changed_zh||c.what_changed_en):(c.what_changed_en||c.what_changed_zh)||tr('Open the governed event contract','打开受治理的事件合约')}
  function candidateTickers(o){return arr((o||{}).company_candidates).filter(obj).map(function(c){return c.ticker}).filter(Boolean)}
  function candidateCopy(o){var t=candidateTickers(o);return t.length?t.join(' · '):''}
  function defenseAgency(value){return /(?:defen[sc]e|air force|department of the army|department of the navy|marine corps|space force|national security agency)/i.test(text(value))}
  function changedCopy(fields){fields=arr(fields);if(!fields.length)return tr('New official record','新官方记录');var map={response_deadline:tr('Response date moved','响应日期变更'),notice_type:tr('Notice type changed','公告类型变更'),resource_links:tr('Documents changed','文件发生变化'),status:tr('Status changed','状态变化'),title:tr('Title changed','标题变化'),description:tr('Scope text changed','范围文本变化')};return fields.slice(0,3).map(function(k){return map[k]||String(k).replace(/_/g,' ')}).join(' · ')}
  function buildRows(){
    var changes=[];
    if(HAS_WORKSPACE){
      changes=WORKSPACE_EVENTS.map(function(e){var impacts=arr(e.listed_company_impacts).filter(obj),ticks=impacts.map(function(x){return x.ticker}).filter(Boolean),agencyBlock=e.agency||{},agency=agencyName(agencyBlock)||tr('Unspecified agency','未指定机构'),agencySub=agencySubName(agencyBlock),kind=e.kind==='recompete'?'recompete':e.kind==='award_change'?'award_change':'opportunity',source=text((e.evidence||{}).source_class,''),truth=kind==='recompete'?'derived':source==='observed_source_revision'?'observed':'official',c=company(e.primary_ticker||ticks[0]),truthCopy=source==='observed_source_revision'?(kind==='award_change'?tr('Observed USAspending revision','观测到的 USAspending 修订'):tr('Observed document revision','观测到的文件修订')):'';return{id:'ws:'+text(e.event_id,e.record_id),kind:kind,truth:truth,truthCopy:truthCopy,linked:!!ticks.length,defense:defenseAgency([agency,agencyBlock.subagency||agencyBlock.subagency_name].join(' '))||!!(c&&arr(c.tags).indexOf('defense')>-1),tickers:ticks,agency:agency,agencySub:agencySub,date:(e.change||{}).known_at||(e.change||{}).effective_at,
      // D3 §3a: keyed ONLY on the producer's is_late_discovery flag.
      isLate:kind==='award_change'&&(e.award_change||{}).is_late_discovery===true,
      effectiveAt:(e.change||{}).effective_at||null,knownAt:(e.change||{}).known_at||null,
      title:workspaceTitle(e),subtitle:workspaceSummary(e),workspaceEvent:e,opportunity:e.opportunity||{},award:e.award_change||{},watch:e.recompete||{},company:c}});
      rowsByMode.changes=changes;
      rowsByMode.awards=changes.filter(function(r){return r.kind==='award_change'});
      rowsByMode.opportunities=changes.filter(function(r){return r.kind==='opportunity'});
      rowsByMode.recompetes=changes.filter(function(r){return r.kind==='recompete'});
    }else{
      EVENTS.forEach(function(e){var o=opp(e.notice_id)||{},ticks=candidateTickers(o);changes.push({id:'event:'+text(e.event_id,e.notice_id),kind:'event',truth:'official',linked:!!ticks.length,defense:!!o.defense_relevant,tickers:ticks,agency:agencyName(o.agency),date:e.known_at||e.effective_at||o.posted_at,title:e.title||o.title||tr('Opportunity update','机会更新'),subtitle:changedCopy(e.changed_fields),event:e,opportunity:o})});
      COMPANIES.forEach(function(c){arr(c.recent_actions).filter(obj).forEach(function(a,i){var fallback=text(label(a,['award_id'],'award'))+'-'+text(label(a,['modification_number','action_date'],i));changes.push({id:'action:'+text(c.ticker)+'-'+text(label(a,['action_id','id'],fallback)),kind:'action',truth:'official',linked:true,defense:arr(c.tags).indexOf('defense')>-1,tickers:[c.ticker],agency:agencyName(label(a,['awarding_agency','agency'],'')),date:label(a,['action_date','effective_at','known_at'],''),title:label(a,['description','action_type_description'],tr('Contract action','合同变更')),subtitle:tr('Official obligation change ','官方义务额变化 ')+money(label(a,['federal_action_obligation','obligation_delta','amount'],null)),company:c,action:a})});arr(c.catalyst_facts).filter(obj).filter(function(f){return !/recompete_window|large_contract_action/.test(String(f.kind||''))}).forEach(function(f,i){changes.push({id:'fact:'+text(c.ticker)+'-'+i,kind:'fact',truth:'derived',linked:true,defense:arr(c.tags).indexOf('defense')>-1,tickers:[c.ticker],agency:'',date:f.effective_at||f.known_at||DATA.known_at,title:f.headline||f.title||tr('Derived investor context','推导投资者情境'),subtitle:f.detail||tr('Rule-derived from official award data','由官方授标数据规则推导'),company:c,fact:f})})});
      changes.sort(function(a,b){return String(b.date||'').localeCompare(String(a.date||''))});rowsByMode.changes=changes;
      rowsByMode.awards=changes.filter(function(r){return r.kind==='action'});
      rowsByMode.opportunities=OPPS.map(function(o){var ticks=candidateTickers(o);return{id:'opp:'+text(o.notice_id),kind:'opportunity',truth:'official',linked:!!ticks.length,defense:!!o.defense_relevant,tickers:ticks,agency:agencyName(o.agency),date:o.known_at||o.posted_at,title:o.title||tr('Untitled opportunity','未命名机会'),subtitle:[o.notice_type,o.status,o.days_to_response!=null?(o.days_to_response+tr('d to response',' 天后截止')):''].filter(Boolean).join(' · '),opportunity:o}});
      var recomp=[];COMPANIES.forEach(function(c){arr(c.recompete_candidates).filter(obj).forEach(function(w,i){recomp.push({id:'recomp:'+text(c.ticker)+'-'+text(label(w,['award_id','piid','id'],i)),kind:'recompete',truth:'derived',linked:true,defense:arr(c.tags).indexOf('defense')>-1,tickers:[c.ticker],agency:agencyName(label(w,['agency','awarding_agency'],'')),date:label(w,['potential_end_date','end_date'],''),title:label(w,['description'],tr('Period-of-performance expiry','履约期到期')),subtitle:tr('Derived expiry watch · not an official recompete date','推导到期观察 · 非官方再竞争日期'),company:c,watch:w})})});recomp.sort(function(a,b){return String(a.date||'9999').localeCompare(String(b.date||'9999'))});rowsByMode.recompetes=recomp;
    }
    rowsByMode.companies=COMPANIES.map(function(c){var v=metric(c,'award_velocity_yoy_pct');return{id:'company:'+text(c.ticker),kind:'company',truth:'observed',truthCopy:tr('Discovery coverage','发现范围覆盖'),linked:false,coverage:true,defense:arr(c.tags).indexOf('defense')>-1,tickers:[c.ticker],agency:'',date:DATA.known_at,title:c.name||c.ticker,subtitle:text(c.ticker)+' · '+tr('award flow ','授标流 ')+pct(v)+' · '+tr('TTM obligations ','TTM 义务额 ')+money(metric(c,'ttm_obligations')),company:c}})
  }
  function modeLabel(mode){return{candidates:tr('Candidate Radar','候选雷达'),changes:tr('Decision queue','决策队列'),awards:tr('Official award tape','官方授标脉搏'),opportunities:tr('Covered opportunities','覆盖机会'),recompetes:tr('Recompete watch','再竞争观察'),budget:tr('Budget & programs','预算与项目'),programs:tr('Program Dossier','项目档案'),companies:tr('Company coverage','公司覆盖'),fms:tr('FMS Congressional Notifications','FMS 国会通知')}[mode]||mode}
  function emptyCopy(){if(state.mode==='candidates'){if(candidateStatus==='loading')return[tr('Checking exact issuer links','正在核验精确发行人关联'),tr('No ticker is named until the candidate ledger verifies its receipts and issuer path.','在候选账本核验凭证及发行人路径前，不会显示任何股票代码。')];if(candidateStatus==='locked')return[tr('Candidate Radar is locked','候选雷达已锁定'),tr('The exact-linked candidate ledger is part of a membership. Company coverage stays open here and is not issuer proof.','精确关联候选账本包含在会员权益中。公司覆盖在此保持开放，且不构成发行人证明。')];if(candidateStatus==='unavailable')return[tr('Candidate Radar unavailable','候选雷达暂不可用'),tr('The candidate ledger could not be verified. Company coverage remains separate and is not issuer proof.','候选账本无法完成核验。公司覆盖保持独立，且不构成发行人证明。')];if(candidateMeta.exact_candidate_availability==='unavailable')return[tr('Exact change detection unavailable','精确变化检测不可用'),tr('Issuer-path coverage can still be reviewed, but the receipt-bound award-change rail is not active in this evidence cut. No candidate or signal was inferred.','仍可审查发行人路径覆盖，但当前证据截点的凭证绑定授标变化链路未激活。未推断候选或信号。')];if(candidateMeta.exact_candidate_availability==='quarantined_historical_issuance')return[tr('Historical issuance quarantined','历史发布已隔离'),tr('The eight affected rows remain in the immutable audit ledger but are excluded from candidate, Prophet, ranking, sizing, gating, and signal surfaces.','受影响的 8 条记录仍保留在不可变审计账本中，但已从候选、Prophet、排名、仓位、门控与信号界面排除。')];if(candidateMeta.exact_candidate_availability==='withheld_historical')return[tr('Historical exact rows withheld','历史精确记录已暂缓发布'),tr('Reviewed pre-boundary exact rows were not issued or backfilled into the candidate ledger. No candidate or signal was inferred.','经审查的边界前精确记录未写入或回填候选账本。未推断候选或信号。')];return[tr('No exact-linked changes yet','尚无精确关联的变化'),tr('The receipt-bound change rail is active, but no eligible issuer-linked event exists in this evidence cut. Watch — don’t chase.','凭证绑定变化链路已激活，但当前证据截点没有符合条件的发行人关联事件。观察，不要追逐。')]};if(state.mode==='awards')return[tr('Award tape is warming','授标脉搏正在建立'),tr('No receipt-bound USAspending change is eligible in this evidence cut. Baseline rows are never backfilled as new events.','当前证据截点中没有符合条件、与凭证绑定的 USAspending 变化。基线行绝不会回填为新事件。')];if(state.mode==='opportunities'){if(opportunityRailUnavailable())return[tr('SOURCE_UNAVAILABLE','SOURCE_UNAVAILABLE'),tr('The SAM.gov opportunity rail has no observation. This is not a valid empty bid week. Use the Change Tape.','SAM.gov 机会链路没有观测。这不是有效的空投标周。请使用变化脉搏。')];return[tr('No opportunities in this cut','当前截点无机会'),tr('No covered opportunity remains after your filters. No rows were invented.','筛选后没有剩余的覆盖机会。系统未生成虚构记录。')];}if(state.mode==='recompetes')return[tr('No expiry watches','无到期观察'),tr('No official period-of-performance end date supports a derived watch in this view.','当前视图中没有官方履约结束日期支持推导观察。')];if(state.mode==='budget')return[budgetStatus==='loading'?tr('Loading budget request graph','正在加载预算请求图谱'):budgetStatus==='projection_missing'?tr('PROJECTION_MISSING','PROJECTION_MISSING'):tr('Budget program evidence unavailable','预算项目证据不可用'),budgetStatus==='loading'?tr('Verifying the precomputed P-1/R-1 evidence graph before showing any program row.','在显示任何项目行前，正在核验预计算的 P-1/R-1 证据图谱。'):budgetStatus==='projection_missing'?tr('DoD Comptroller P-1 and R-1 PDFs are public. Mastermind has no request graph on this desk, so this is not an empty budget year. Use the Change Tape.','国防部主计长 P-1 与 R-1 PDF 为公开文件。本台没有请求图谱，因此这不是空预算年。请使用变化脉搏。'):tr('No P-1/R-1 request graph is active in this evidence cut. No budget row, funding conclusion or issuer link was inferred.','当前证据截点没有激活 P-1/R-1 请求图谱。未推断任何预算行、资金结论或发行人关联。')];if(state.mode==='programs')return[programsStatus==='loading'?tr('Verifying the program ontology','正在核验项目本体'):programsStatus==='locked'?tr('Program Dossier is locked','项目档案已锁定'):programsStatus==='ontology_unavailable'?tr('Program ontology unavailable','项目本体数据不可用'):programsStatus==='empty'?tr('No current program on file','当前无在案项目'):tr('Program Dossier unavailable','项目档案不可用'),programsStatus==='loading'?tr('Verifying the reviewed program/platform ontology before showing any program row.','在显示任何项目行前，正在核验已复核的项目/平台本体。'):programsStatus==='locked'?tr('The reviewed Program/Platform Dossier is part of a membership. No program row is shown until the membership is verified.','已复核的项目/平台档案包含在会员权益中。会员身份核验前不会显示任何项目行。'):programsStatus==='ontology_unavailable'?tr('No certified program ontology is active in this evidence cut. No program row or attribution was inferred.','当前证据截点没有已认证的项目本体处于活动状态。未推断任何项目行或归属。'):programsStatus==='empty'?tr('A certified program ontology is active, but no current program identity exists in it yet.','已认证的项目本体处于活动状态，但其中尚无当前项目身份。'):tr('The program ontology could not be verified. No program row was inferred.','项目本体无法完成核验。未推断任何项目行。')];if(state.mode==='companies')return[tr('No company match','无匹配公司'),tr('Try another ticker or clear the current filters.','尝试其他股票代码或清除筛选。')];if(state.mode==='fms')return[fmsStatus==='loading'?tr('Verifying the FMS coverage manifest','正在核验 FMS 覆盖清单'):fmsStatus==='projection_missing'?tr('PROJECTION_MISSING','PROJECTION_MISSING'):tr('FMS notifications unavailable','FMS 通知暂不可用'),fmsStatus==='loading'?tr('Verifying the official-union coverage manifest before showing any case.','在显示任何案例前，正在核验官方联合覆盖清单。'):fmsStatus==='projection_missing'?tr('The FMS congressional-notification acquisition lane has not published a case graph on this desk yet, so this is not an empty notification month. Use the Change Tape.','FMS 国会通知采集链路尚未在本台发布案例图谱，因此这不是空通知月份。请使用变化脉搏。'):tr('No official-union coverage manifest is active in this evidence cut. No case was inferred.','当前证据截点没有生效的官方联合覆盖清单。未推断任何案例。')];return[tr('No qualifying changes','无符合条件的变化'),tr('The evidence cut is quiet or this source is still warming up.','当前证据截点较安静，或数据源仍在建立。')]}
  function renderMissingCandidate(id){var el=$('inspector');dossierUI.invalidate();atlasUI.invalidate();budgetUI.invalidate();bridgeUI.invalidate();el.className='inspector-empty';el.innerHTML='<div><span class="empty-mark" aria-hidden="true">○</span><strong>'+esc(tr('Candidate no longer available','候选已不可用'))+'</strong><p>'+esc(tr('This linked candidate is not present in the current verified ledger. No substitute was selected.','此链接候选不在当前已核验账本中。系统未选择其他候选替代。'))+'</p><span class="candidate-time">'+esc(id)+'</span></div>';$('mobileSummary').hidden=true}
  function applyFilters(){var q=state.q.toLowerCase(),rows=arr(rowsByMode[state.mode]);visible=rows.filter(function(r){if(state.truth==='official'&&!isOfficial(r))return false;if(state.truth==='linked'&&!r.linked)return false;if(state.truth==='defense'&&!r.defense)return false;if(state.agency&&r.agency!==state.agency)return false;if(state.ticker&&arr(r.tickers).indexOf(state.ticker)<0)return false;if(q){var hay=[r.title,r.subtitle,r.agency].concat(r.tickers).join(' ').toLowerCase();if(hay.indexOf(q)<0)return false}return true});renderQueue();syncCounts();var verifiedCandidateState=candidateStatus==='ok'||candidateStatus==='empty',requestedMissing=pendingSelection&&state.mode==='candidates'&&verifiedCandidateState&&!rows.some(function(r){return r.id===pendingSelection}),waitingForCandidate=pendingSelection&&state.selected===pendingSelection&&state.mode==='candidates'&&candidateStatus==='loading';if(requestedMissing){var missingId=pendingSelection;pendingSelection='';missingCandidateSelection=missingId;state.selected='';renderMissingCandidate(missingId);return}if(missingCandidateSelection&&state.mode==='candidates'&&verifiedCandidateState){if(rows.some(function(r){return r.id===missingCandidateSelection})){state.selected=missingCandidateSelection;missingCandidateSelection=''}else{renderMissingCandidate(missingCandidateSelection);return}}if(state.selected&&!visible.some(function(r){return r.id===state.selected})&&!waitingForCandidate)state.selected='';var selected=visible.find(function(r){return r.id===state.selected})||(!waitingForCandidate?visible[0]:null);if(selected)selectRow(selected.id,false);else renderInspector(null)}
  function candidateQueueState(r){var state=text((r.candidate||{}).candidate_state,'');return{detected:tr('Research now','立即研究'),awaiting_crosscheck:tr('Checking other evidence','核对其他证据'),active:tr('Research now','立即研究'),matured:tr('Review outcome','复核结果'),superseded:tr('Newer evidence available','已有更新证据'),withdrawn:tr('Record withdrawn','记录已撤回'),blocked:tr('Research paused','研究暂停')}[state]||tr('Research queue','研究队列')}
  function candidateDirection(r){var direction=text((r.candidate||{}).transmission_direction,'');return{possible_positive:tr('Possible upside channel','可能的上行通道'),possible_negative:tr('Possible downside channel','可能的下行通道'),mixed:tr('Mixed statement channel','混合财报通道'),unknown:tr('Channel still open','传导路径仍待明确')}[direction]||tr('Channel still open','传导路径仍待明确')}
  function candidateQueueRow(r,i){var checks=candidateUI&&typeof candidateUI.crosschecks==='function'?candidateUI.crosschecks(r.candidate||{},true):'',selected=r.id===state.selected||(!state.selected&&i===0);return '<button type="button" class="queue-row candidate-row'+(r.id===state.selected?' on':'')+'" role="option" aria-selected="'+(r.id===state.selected?'true':'false')+'" tabindex="'+(selected?'0':'-1')+'" data-row="'+esc(r.id)+'"><span class="candidate-rail" aria-hidden="true"></span><span><span class="candidate-top"><span class="candidate-state">'+esc(candidateQueueState(r))+'</span><span class="candidate-time">'+esc(ago(r.date))+'</span></span><span class="candidate-name"><code>'+esc((r.tickers||[])[0]||'')+'</code>'+esc(r.title.replace(/^[-.A-Z0-9]+ · /,''))+'</span><span class="candidate-change">'+esc(r.subtitle||tr('Receipt-bound procurement change','与凭证绑定的采购变化'))+'</span><span class="candidate-foot"><span class="candidate-channel">'+esc(candidateDirection(r))+'</span><span>'+esc(tr('Exact issuer path','精确发行人路径'))+'</span><span class="candidate-crosschecks">'+checks+'</span></span></span></button>'}
  function renderQueue(){var box=$('queueList'),empty=emptyCopy(),isBudget=state.mode==='budget',isCandidates=state.mode==='candidates';$('queueHeading').textContent=modeLabel(state.mode);$('queueSummary').textContent=isCandidates?(candidateStatus==='loading'?tr('Verifying the candidate ledger','正在核验候选账本'):candidateStatus==='locked'?tr('Candidate ledger locked','候选账本已锁定'):candidateStatus==='unavailable'?tr('Candidate ledger unavailable','候选账本暂不可用'):visible.length+tr(' exact-linked research candidates',' 个精确关联研究候选')):isBudget?(budgetStatus==='projection_missing'?tr('PROJECTION_MISSING','PROJECTION_MISSING'):budgetStatus==='loading'?tr('Budget graph verifying','预算图谱正在核验'):visible.length+tr(' source-native programs in this request evidence cut',' 个来源原生项目位于当前请求证据截点')):(state.mode==='opportunities'&&opportunityRailUnavailable()?tr('SOURCE_UNAVAILABLE','SOURCE_UNAVAILABLE'):visible.length+tr(' covered records in this evidence cut',' 条覆盖记录位于当前证据截点'));$('queueSort').textContent=isCandidates?tr('newest known evidence','最新获知证据'):isBudget?tr('source-native program order','来源原生项目顺序'):HAS_WORKSPACE&&state.mode!=='companies'?tr('governed display order','受治理的展示顺序'):state.mode==='recompetes'?tr('nearest end first','最近到期优先'):tr('newest first','最新优先');if(!visible.length){var meta=isCandidates?(candidateStatus==='locked'?'<div class="candidate-empty-meta"><a class="tool-btn" href="plans.html">'+esc(tr('View membership plans','查看会员方案'))+'</a></div>':candidateStatus==='unavailable'?'<div class="candidate-empty-meta"><button class="tool-btn" type="button" id="candidateRadarRetry">'+esc(tr('Retry candidate ledger','重试候选账本'))+'</button></div>':'<div class="candidate-empty-meta"><span>'+esc(text(candidateMeta.total,0))+tr(' exact candidates',' 个精确候选')+'</span><span>'+esc(COMPANIES.length)+tr(' coverage dossiers',' 个覆盖档案')+'</span><span>'+esc(text(candidateMeta.mapping_backlog_total,0))+tr(' mapping workbench',' 个映射工作台项')+'</span></div>'):(state.mode==='programs'&&programsStatus==='locked'?'<div class="candidate-empty-meta"><a class="tool-btn" href="plans.html">'+esc(tr('View membership plans','查看会员方案'))+'</a></div>':'');box.innerHTML='<div class="'+(isCandidates?'candidate-empty':'empty-state')+'"><div><span class="empty-mark" aria-hidden="true">○</span><strong>'+esc(empty[0])+'</strong><p>'+esc(empty[1])+'</p>'+meta+'</div></div>';var retryRadar=$('candidateRadarRetry');if(retryRadar)retryRadar.addEventListener('click',function(){candidateUI.load()});return}box.innerHTML=isCandidates?visible.map(candidateQueueRow).join(''):visible.map(function(r,i){var ticks=arr(r.tickers),secondary=secondaryTruth(r),lateChip=r.isLate?truthChip('late',tr('Late discovery','延迟发现')):'';return '<button type="button" class="queue-row'+(r.id===state.selected?' on':'')+'" role="option" aria-selected="'+(r.id===state.selected?'true':'false')+'" tabindex="'+(r.id===state.selected||(!state.selected&&i===0)?'0':'-1')+'" data-row="'+esc(r.id)+'"><span class="queue-rail '+esc(r.truth)+'" aria-hidden="true"></span><span><span class="queue-top"><span class="truth-set">'+truthChip(r.truth,r.truthCopy||truthLabel(r))+secondary+lateChip+correctionChip(r)+'</span><span class="queue-time">'+esc(queueTimeCopy(r))+'</span></span><span class="queue-main">'+esc(r.title)+'</span><span class="queue-sub">'+esc(r.subtitle||tr('Open the evidence inspector','打开证据查看器'))+'</span><span class="queue-foot">'+(ticks.length?'<span class="ticker-token">'+esc(ticks.join(' · '))+'</span>':'')+(r.agency?'<span>'+esc(r.agency)+'</span>':'')+(r.agencySub?'<span>'+esc(r.agencySub)+'</span>':'')+'</span></span></button>'}).join('');box.querySelectorAll('.queue-row,.candidate-row').forEach(function(el){el.addEventListener('click',function(){selectRow(el.dataset.row,true)});el.addEventListener('dblclick',openMobileInspector)})}
  function officialSource(r){if(r.workspaceEvent){var receipt=arr((r.workspaceEvent.evidence||{}).receipts).find(function(x){return safeUrl(x.url)});return receipt?safeUrl(receipt.url):safeUrl((r.opportunity||{}).sam_url)}if(r.opportunity)return safeUrl(r.opportunity.source_url);if(r.watch)return safeUrl(r.watch.source_url);if(r.action)return safeUrl(r.action.source_url||r.action.usaspending_url);var c=r.company||{},p=arr(c.source_receipts||c.provenance).find(function(x){return safeUrl(x.source_url)});return p?safeUrl(p.source_url):''}
  function authorityLimit(){return tr('This evidence can inform research, but it cannot rank a company, change conviction, size a position or trigger a trade.','该证据可用于研究，但不得为公司排序、改变信心、调整仓位或触发交易。')}
  function observationLimit(r){var o=r.workspaceEvent&&r.opportunity||{};if(r.workspaceEvent&&r.kind==='opportunity'&&o.current_state_verified===false){var reason=o.current_state_reason?String(o.current_state_reason).replace(/_/g,' '):tr('verification window elapsed','验证窗口已过期'),horizon=o.observation_horizon_at?tr(' as of ','，截至 ')+date(o.observation_horizon_at):'';return tr('The opportunity is retained as last observed rather than verified current','该机会保留为最后观测状态，而非已验证的当前状态')+horizon+'. '+reason+'.'}return''}
  function inspectIdentity(r){
    if(r.workspaceEvent){
      var e=r.workspaceEvent,a=e.agency||{},o=e.opportunity||{},w=e.recompete||{},award=e.award_change||{};
      if(r.kind==='award_change')return{type:tr('Official award / action change','官方授标 / 行动变化'),id:award.piid||award.generated_award_id||award.award_key||award.action_id||e.record_id||e.event_id,title:r.title,meta:[award.recipient_name,a.department_name||a.name,a.subagency_name||a.subagency].filter(Boolean).join(' · ')};
      return{type:r.kind==='recompete'?tr('Derived expiry watch','推导到期观察'):tr('Federal opportunity change','联邦机会变化'),id:o.solicitation_number||o.notice_id||w.piid||e.record_id||e.event_id,title:r.title,meta:[a.department_name||a.name,a.subagency_name||a.subagency,a.office_name].filter(Boolean).join(' · ')}
    }
    if(r.kind==='opportunity'||r.kind==='event'){var o=r.opportunity||{};return{type:tr('Federal opportunity','联邦机会'),id:o.solicitation_number||o.notice_id||(r.event||{}).notice_id,title:r.title,meta:[o.agency,o.office].filter(Boolean).join(' · ')}}
    if(r.kind==='recompete')return{type:tr('Derived expiry watch','推导到期观察'),id:label(r.watch,['award_id','piid','id'],(r.company||{}).ticker),title:r.title,meta:[(r.company||{}).ticker,(r.company||{}).name,r.agency].filter(Boolean).join(' · ')};
    if(r.kind==='action')return{type:tr('Official contract action','官方合同变更'),id:label(r.action,['modification_number','award_id','id'],r.company.ticker),title:r.title,meta:[r.company.ticker,r.company.name,r.agency].filter(Boolean).join(' · ')};
    if(r.kind==='company')return{type:tr('Company coverage','公司覆盖'),id:r.company.ticker,title:r.company.name,meta:tr('Discovery-scope history · mapping needed','发现范围历史 · 需要映射')};
    return{type:tr('Derived investor context','推导投资者情境'),id:r.company&&r.company.ticker,title:r.title,meta:r.company&&r.company.name}
  }
  function changedSection(r){if(r.workspaceEvent)return workspaceSummary(r.workspaceEvent);if(r.kind==='event')return changedCopy(r.event.changed_fields);if(r.kind==='action')return tr('The government recorded an obligation change of ','政府记录了一笔义务额变化 ')+money(label(r.action,['federal_action_obligation','obligation_delta','amount'],null))+'.';if(r.kind==='opportunity')return tr('An official notice is active with ','官方公告处于活跃状态，距离响应截止 ')+(r.opportunity.days_to_response!=null?r.opportunity.days_to_response+tr(' days to respond.',' 天。'):tr('no reported response countdown.','暂无报告的响应倒计时。'));if(r.kind==='recompete')return tr('The reported period of performance ends on ','报告的履约期结束于 ')+date(label(r.watch,['potential_end_date','end_date'],''))+'.';if(r.kind==='company')return tr('This dossier consolidates official awards, actions and bounded exposure metrics for the mapped recipient.','该档案汇总了映射收款方的官方授标、变更及有限暴露指标。');return r.subtitle}
  function whySection(r){if(r.workspaceEvent){var impact=arr(r.workspaceEvent.listed_company_impacts)[0];if(impact)return(zh()?(impact.why_it_matters_zh||impact.why_it_matters_en):(impact.why_it_matters_en||impact.why_it_matters_zh))||tr('A reviewed recipient or ownership path links this procurement fact to a listed issuer; economic transmission still requires research.','经核验的收款方或所有权路径将该采购事实关联至上市发行人；经济传导仍需研究。');if(r.kind==='award_change')return tr('This is a receipt-bound USAspending award or action change. It is closer to funded economic activity than a press-release ceiling, but no defensible public-company transmission is resolved yet.','这是与凭证绑定的 USAspending 授标或行动变化。它比新闻稿合同上限更接近已拨款经济活动，但尚未建立可靠的上市公司传导。');return tr('This covered procurement change is decision-relevant, but no defensible listed-company transmission is resolved yet.','该覆盖范围内的采购变化具有研究价值，但尚未建立可靠的上市公司传导。')}if(r.kind==='opportunity')return r.linked?tr('The requirement overlaps with one or more issuer capability or award-history fields. That makes it worth research, not a bidder forecast.','该需求与一个或多个发行人的能力或授标历史字段重合。值得研究，但不是投标预测。'):tr('The notice may move a defense program or supplier set, but no defensible public-company link is present yet.','该公告可能影响国防项目或供应商群，但目前尚无可靠的上市公司关联。');if(r.kind==='recompete')return tr('An approaching contract end can create renewal, displacement or revenue-timing risk. The end date is official; the recompete interpretation is derived.','临近合同结束可能产生续签、替代或收入时点风险。结束日期为官方信息；再竞争解读为推导。');if(r.kind==='action')return tr('Funded obligation changes are closer to economic transmission than press-release ceiling values, but still are not reported revenue.','已拨付义务额变化比新闻稿中的合同上限更接近经济传导，但仍不等于报告收入。');if(r.kind==='company')return tr('Use the dossier to separate official funded activity from unpriced capacity and company-reported backlog.','使用该档案区分官方已拨付活动、未定价余量和公司报告的积压。');if(r.kind==='fact'&&/velocity/i.test(text((r.fact||{}).kind)))return tr('A change in obligation flow can lead reported government revenue, but reporting lag and award mix can distort the read.','义务额流量变化可能领先政府收入报告，但报告延迟和授标结构可能扭曲判断。');if(r.kind==='fact'&&/concentration/i.test(text((r.fact||{}).kind)))return tr('Customer concentration can amplify both program upside and budget or recompete risk.','客户集中度会同时放大项目上行空间以及预算或再竞争风险。');return text(r.subtitle)}
  function stanceSection(r){if(r.workspaceEvent){var impact=arr(r.workspaceEvent.listed_company_impacts)[0]||{},map={watch_dont_chase:tr('Watch — do not chase','观察，不要追逐'),stand_aside:tr('Stand aside','暂不行动'),get_ready:tr('Get ready','提前准备'),research_next:tr('Research next','继续研究')},title=map[impact.stance]||(r.kind==='award_change'&&!r.linked?tr('Resolve the recipient','解析收款方'):tr('Watch with evidence','以证据观察')),next=zh()?(impact.watch_next_zh||impact.watch_next_en):(impact.watch_next_en||impact.watch_next_zh);if(r.kind==='award_change'&&!r.linked)next=tr('Keep the official change visible, but withhold issuer impact until a reviewed recipient or ownership path exists.','保留官方变化的可见性，但在经核验的收款方或所有权路径建立前，不赋予发行人影响。');return[title,next||tr('Keep this context bounded until the next official receipt changes the case.','在下一份官方凭证改变判断前，保持有限情境观察。')]}if(r.kind==='recompete')return[tr('Get ready','提前准备'),tr('Watch the end window and wait for an official solicitation or modification before acting.','观察结束窗口，等待官方招标或合同变更后再行动。')];if(r.kind==='opportunity'&&r.linked)return[tr('Research next','继续研究'),tr('Open the notice and verify capability, vehicle and incumbent evidence. Do not treat the link as bidder odds.','打开公告并核实能力、合同工具及现任承包商证据。不要将关联视为投标概率。')];if(r.kind==='action')return[tr('Check the delta','核对变化'),tr('Compare the obligation change with prior actions and the company reporting calendar.','将义务额变化与历史变更及公司报告日历进行比较。')];if(r.kind==='company')return[tr('Watch the tape','观察脉搏'),tr('Use award velocity as a research lead, then confirm the change in official actions and filings.','将授标速度作为研究线索，然后在官方变更和文件中确认。')];return[tr('Watch — do not chase','观察，不要追逐'),tr('Keep the fact in context until an issuer link and economic transmission path are evidenced.','在发行人关联和经济传导路径得到证据支持前，仅作情境观察。')]}
  function diffRows(r){var fields=[];if(r.workspaceEvent)fields=arr((r.workspaceEvent.change||{}).changed_fields);else if(r.kind==='event')fields=arr(r.event.changed_fields);if(!fields.length)return'<div class="limit-copy">'+esc(r.workspaceEvent&&r.kind==='recompete'?tr('This case entered a deterministic watch window; no source field revision is being claimed.','该案例进入确定性观察窗口；系统不声称源字段发生修订。'):tr('No prior visible version is available for a field-by-field comparison.','没有可见的先前版本用于逐字段比较。'))+'</div>';return'<div class="diff-list">'+fields.slice(0,8).map(function(f){if(obj(f)){var semantic=f.semantic==='observed_document_revision'?tr('Observed document revision','观测到的文件修订'):tr('Official field','官方字段');return'<div class="diff-row"><b>'+esc(text(f.field).replace(/_/g,' '))+'<br><small>'+esc(semantic)+'</small></b><span>'+esc(compact(f.before))+' → '+esc(compact(f.after))+'</span></div>'}return'<div class="diff-row"><b>'+esc(String(f).replace(/_/g,' '))+'</b><span>'+esc(tr('Changed in this official revision','在本次官方修订中发生变化'))+'</span></div>'}).join('')+'</div>'}
  function factCell(labelCopy,value){return'<div class="fact-cell"><span>'+esc(labelCopy)+'</span><b>'+esc(value==null||value===''?'—':value)+'</b></div>'}
  // D3 §3b: fact-cell + sub-label line via the existing `<i>` slot.
  function clockCell(labelCopy,value,subCopy){return'<div class="fact-cell"><span>'+esc(labelCopy)+'</span><b>'+esc(value==null||value===''?'—':value)+'</b><i>'+esc(subCopy)+'</i></div>'}
  // D3 §3b: four clock rows always, incl. the NAMED-NULL source-publication
  // clock -- source_published_at is never read or substituted (spec §1, T3).
  function clocksSection(r){if(!r.workspaceEvent||r.kind!=='award_change')return'';var e=r.workspaceEvent,c=e.change||{},award=e.award_change||{},cut=text(WORKSPACE.as_of,text(DATA.as_of,''));var cells=clockCell(tr('Took effect','生效'),date(c.effective_at),tr('Official action date','官方行动日期'))+clockCell(tr('First known to Mastermind','Mastermind 首次获知'),date(c.known_at),tr('Our knowledge clock — when the collector verified the record, not when the source published it.','我们的知识时钟——采集器核验该记录的时间，而非来源发布时间。'))+clockCell(tr('Source publication time','来源发布时间'),tr('Not asserted','未认定'),tr('USAspending does not expose a per-revision publication time. Nothing is substituted for it.','USAspending 未提供逐修订的发布时间。系统不会以其他时间替代。'))+clockCell(tr('Evidence cut','证据截点'),date(cut),tr('Packet generated ','数据包生成于 ')+date(WORKSPACE.generated_at||DATA.generated_at));var verdict=(obj(award)&&award.is_late_discovery===true)?'<div class="limit-copy" style="margin-top:7px">'+esc(discoveryTiming(c,award))+'</div>':'';return'<section class="inspect-section"><div class="inspect-label">'+esc(tr('Clocks','时间线'))+'</div><div class="facts-grid">'+cells+'</div>'+verdict+'</section>'}
  // D3 §3c: shown only when the producer names prior_source_identity.
  function successorLine(r){if(!r.workspaceEvent)return'';var prior=text((r.workspaceEvent.award_change||{}).prior_source_identity,'');if(!prior)return'';var short=String(prior).slice(0,12);return'<div class="limit-copy" style="margin-top:7px">'+esc(tr('Succeeds a prior recorded source state (','承接先前记录的来源状态（')+short+tr('). Corrections append; earlier receipts stay on record.','）。更正以追加方式记录；先前凭证保留在案。'))+'</div>'}
  function signedMoney(v){var x=n(v);return x==null?'—':(x>0?'+':'')+money(x)}
  // Direction is DERIVED ONLY from a signed delta_* amount the payload already carries.
  // No delta -> no directional claim. Colours route through --ink-up/--ink-down so
  // html[data-lang="zh"] flips them for 红涨绿跌; never --gr-good/--gr-bad (those do not flip).
  function moveWords(field,dir){var m={current_award_amount:[tr('Award value raised','授标金额调高'),tr('Award value lowered','授标金额调低')],potential_award_amount:[tr('Ceiling raised','上限调高'),tr('Ceiling lowered','上限调低')],total_obligated_amount:[tr('More money obligated','已承诺义务增加'),tr('Less money obligated','已承诺义务减少')]}[field]||[tr('Raised','上调'),tr('Lowered','下调')];return dir>0?m[0]:m[1]}
  function deltaIndex(items){var d={};arr(items).forEach(function(x){var c=String((x&&(x.label_code||x.semantic))||'');if(c.indexOf('delta_')===0&&n(x.value)!=null)d[c.slice(6)]=n(x.value)});return d}
  function agreedDirection(deltas){var k=Object.keys(deltas||{}),sign=0;for(var i=0;i<k.length;i++){var v=deltas[k[i]],s=v>0?1:v<0?-1:0;if(!s)continue;if(!sign)sign=s;else if(sign!==s)return 0}return sign}
  // dupWords: when a lone delta drives both, the headline keeps the words and
  // the cell keeps the figure (word-budget law) — one job per element.
  function movementCell(labelCopy,value,field,delta,withFigure,dupWords){if(delta==null)return factCell(labelCopy,value);var cls=delta>0?'up':delta<0?'down':'flat',mark=delta>0?'▲':delta<0?'▼':'·',words=delta===0?tr('No net change','无净变化'):moveWords(field,delta);if(withFigure&&words===dupWords)words='';var fig=(delta===0||!withFigure)?'':'<u>'+esc(signedMoney(delta))+'</u>';return'<div class="fact-cell moved '+cls+'"><span>'+esc(labelCopy)+'</span><b>'+esc(value==null||value===''?'—':value)+'</b><i>'+esc(mark+(words?' '+words:''))+(fig?' '+fig:'')+'</i></div>'}
  function factMore(k){return'<div class="fact-cell fact-more"><span>'+esc(tr('Not shown here','此处未显示'))+'</span><b>'+esc(tr('+'+k+' more reported fields','另有 '+k+' 项报告字段'))+'</b></div>'}
  function eventTypeCopy(code,dir){var c=String(code||''),swing={award_value_changed:[tr('Award values raised','授标金额调高'),tr('Award values lowered','授标金额调低'),tr('Award values changed','授标金额变化')],ceiling_changed:[tr('Ceiling raised','上限调高'),tr('Ceiling lowered','上限调低'),tr('Ceiling changed','上限变化')],current_value_changed:[tr('Award value raised','授标金额调高'),tr('Award value lowered','授标金额调低'),tr('Award value changed','授标金额变化')],reported_obligation_balance_changed:[tr('More money obligated','已承诺义务增加'),tr('Less money obligated','已承诺义务减少'),tr('Obligated balance changed','已承诺义务变化')]}[c];if(swing)return dir>0?swing[0]:dir<0?swing[1]:swing[2];return{obligation:tr('Money obligated','新增承诺义务'),deobligation:tr('Obligation reduced','承诺义务减少'),award_discovered_late:tr('Award found late','延迟发现的授标'),period_extended:tr('Contract extended','合同延长'),period_shortened:tr('Contract shortened','合同缩短')}[c]||(c?c.replace(/_/g,' '):'—')}
  function amountText(x){return(String(x.currency||'USD')==='USD'?(String((x.label_code||x.semantic)||'').indexOf('delta_')===0?signedMoney(x.value):money(x.value)):compact(x.value)+' '+text(x.currency,''))+(x.is_lower_bound?tr(' minimum',' 最低值'):'')}
  function semanticLabel(x){var map={period_of_performance_end:tr('Official POP end','官方履约结束日'),response_deadline:tr('Response deadline','响应截止'),posted_at:tr('Posted','发布日期'),updated_at:tr('Updated','更新日期'),action_date:tr('Action date','行动日期'),official_action_date:tr('Official action date','官方行动日期'),effective_at:tr('Took effect','生效日期'),known_at:tr('We first saw it','我们首次获知'),start_date:tr('Contract starts','合同开始'),end_date:tr('Contract ends','合同结束'),reported_obligations:tr('Reported obligations','报告义务额'),opportunity_value:tr('Opportunity value','机会金额'),current_award_value:tr('Current award value','当前授标价值'),potential_award_value:tr('Potential award value','潜在授标价值'),current_award_amount:tr('Award value now','当前授标金额'),potential_award_amount:tr('Ceiling (all options)','上限（含选择权）'),total_obligated_amount:tr('Money obligated so far','累计已承诺义务'),delta_current_award_amount:tr('Change in award value','授标金额变化'),delta_potential_award_amount:tr('Change in ceiling','上限变化'),delta_total_obligated_amount:tr('Change in money obligated','已承诺义务变化'),federal_action_obligation:tr('Action obligation','行动义务额'),official_pop_end:tr('Official POP end','官方履约结束日'),obligated:tr('Obligated','已承诺义务')};return map[x]||String(x||tr('Reported field','报告字段')).replace(/_/g,' ')}
  // Discovery timing reads the event's OWN dual clock, never is_late_discovery
  // alone: the flag is measured only on a FIRST observation (award_events.py
  // _is_late_discovery) and false means "not a first discovery", NOT "seen
  // live". Both branches state something true (D3 spec §1).
  var LATE_DISCOVERY_DAYS=45; // mirrors engine DEFAULT_LATE_DISCOVERY_DAYS (award_events.py:42)
  function discoveryLagDays(change){var c=obj(change)?change:{},eff=c.effective_at,known=c.known_at;if(!eff||!known)return null;var ed=new Date(eff),kd=new Date(known);if(isNaN(ed.getTime())||isNaN(kd.getTime()))return null;return Math.round((kd.getTime()-ed.getTime())/86400000)}
  function discoveryTiming(change,award){var lag=discoveryLagDays(change);
    // No usable clock pair: say so. The engine's own fail-closed late verdict still outranks
    // a missing gap, and an unmeasurable gap is never evidence of a live observation.
    if(lag==null)return (obj(award)&&award.is_late_discovery)?tr('Late discovery','延迟发现'):tr('Timing unconfirmed','时点未确认');
    if(lag>LATE_DISCOVERY_DAYS)return tr('Late discovery · '+lag+' days','延迟发现 · '+lag+' 天');
    return tr('Observed in live window','在实时窗口内观测')}
  function factGrid(r){
    var o=r.opportunity||{},w=r.watch||{},a=r.action||{},c=r.company||{};
    if(r.workspaceEvent){var e=r.workspaceEvent,out='',workspaceOpportunity=e.opportunity||{},award=e.award_change||{},evDeltas=deltaIndex(e.amounts),evTypeCopy=null;if(r.kind==='award_change'){evTypeCopy=eventTypeCopy(award.event_type,agreedDirection(evDeltas));out+=factCell(tr('Change type','变化类型'),evTypeCopy);out+=factCell(tr('Recipient','收款方'),text(award.recipient_name));out+=factCell(tr('Award / PIID','授标 / PIID'),text(award.piid||award.generated_award_id||award.award_key));out+=factCell(tr('Action identity','行动身份'),text(award.action_id));out+=factCell(tr('Source rail','来源链路'),text(award.source_rail).replace(/_/g,' '));out+=factCell(tr('Discovery timing','发现时点'),discoveryTiming(e.change,award))}var FACT_CAP=6,evDates=arr(e.dates);evDates.slice(0,FACT_CAP).forEach(function(x){out+=factCell(semanticLabel(x.label_code||x.semantic),date(x.value))});if(evDates.length>FACT_CAP)out+=factMore(evDates.length-FACT_CAP);
      /* delta_X folds into X's cell (number + movement in one place); a delta
         with no base amount keeps its own cell — nothing drops uncounted. */
      var evAmounts=arr(e.amounts),baseCodes={},ordered=[];evAmounts.forEach(function(x){var c=String((x.label_code||x.semantic)||'');if(c.indexOf('delta_')!==0)baseCodes[c]=1});evAmounts.forEach(function(x){var c=String((x.label_code||x.semantic)||'');if(c.indexOf('delta_')!==0||!baseCodes[c.slice(6)])ordered.push(x)});
      /* Only a LONE delta lets the headline replace a cell's words; with 2+
         moving numbers every moved cell keeps its own words in both locales. */
      var dupGuard=Object.keys(evDeltas).length===1?evTypeCopy:null;
      ordered.slice(0,FACT_CAP).forEach(function(x){var c=String((x.label_code||x.semantic)||''),isDelta=c.indexOf('delta_')===0,field=isDelta?c.slice(6):c,dv=isDelta?n(x.value):(Object.prototype.hasOwnProperty.call(evDeltas,c)?evDeltas[c]:null);out+=movementCell(semanticLabel(c),amountText(x),field,dv,!isDelta,dupGuard)});if(ordered.length>FACT_CAP)out+=factMore(ordered.length-FACT_CAP);if(r.kind==='recompete')out+=factCell(tr('Watch classification','观察分类'),tr('Derived expiry watch','推导到期观察'));if(r.kind==='opportunity'){out+=factCell(tr('Current-state check','当前状态检查'),opportunityCurrentStateCopy(workspaceOpportunity));if(workspaceOpportunity.observation_age_minutes!=null)out+=factCell(tr('Observation age','观测时效'),ageCopy(n(workspaceOpportunity.observation_age_minutes)));if(workspaceOpportunity.observation_basis)out+=factCell(tr('Observation basis','观测依据'),String(workspaceOpportunity.observation_basis).replace(/_/g,' '))}return out||factCell(tr('Reported fields','报告字段'),tr('Unavailable','不可用'))}
    if(r.kind==='opportunity'||r.kind==='event')return factCell(tr('Posted','发布日期'),date(o.posted_at))+factCell(tr('Response due','响应截止'),date(o.response_deadline))+factCell(tr('Notice type','公告类型'),text(o.notice_type))+factCell(tr('Status','状态'),text(o.status||o.state))+factCell('NAICS',text(o.naics_code||arr(o.naics_codes).join(' · ')))+factCell('PSC',text(o.psc_code||arr(o.psc_codes).join(' · ')));
    if(r.kind==='recompete')return factCell(tr('Potential end','潜在结束'),date(label(w,['potential_end_date','end_date'],'')))+factCell(tr('Days to end','距离结束'),text(w.days_to_end))+factCell(tr('Observed value','观测价值'),money(label(w,['total_obligated','value'],null)))+factCell(tr('Classification','分类'),tr('Derived window','推导窗口'));
    if(r.kind==='action')return factCell(tr('Action date','变更日期'),date(label(a,['action_date','effective_at'],'')))+factCell(tr('Obligation delta','义务额变化'),money(label(a,['federal_action_obligation','obligation_delta','amount'],null)))+factCell(tr('We first saw it','我们首次获知'),date(a.known_at))+factCell(tr('Company','公司'),text(c.ticker));
    return factCell(tr('TTM obligations','TTM 义务额'),money(metric(c,'ttm_obligations')))+factCell(tr('Award flow YoY','授标流同比'),pct(metric(c,'award_velocity_yoy_pct')))+factCell(tr('Observed capacity','观测余量'),money(metric(c,'funded_capacity_observed')))+factCell(tr('Net award action flow (90d)','净授标行动流（90天）'),money(metric(c,'net_award_action_flow_90d')))
  }
  function transmissions(r){var out=[];if(r.workspaceEvent)arr(r.workspaceEvent.listed_company_impacts).filter(obj).forEach(function(c){var why=zh()?(c.why_it_matters_zh||c.why_it_matters_en):(c.why_it_matters_en||c.why_it_matters_zh),mat=c.materiality||{},relation=c.relation_semantic==='reviewed'?tr('Reviewed issuer link','已核验发行人关联'):tr('Derived issuer link','推导公司关联'),detail=relation+' · '+text(c.confidence,tr('unresolved','未确定'))+(mat.band?' · '+tr('materiality ','重要性 ')+mat.band:'')+'. '+text(why,'')+(c.label_limit?' '+c.label_limit:'');out.push('<div class="transmission"><b>'+esc(text(c.ticker))+' · '+esc(text(c.company_name))+'</b><span>'+esc(detail)+'</span></div>')});else if(r.opportunity)arr(r.opportunity.company_candidates).filter(obj).forEach(function(c){out.push('<div class="transmission"><b>'+esc(text(c.ticker))+' · '+esc(text(c.name))+'</b><span>'+esc(tr('Derived exposure candidate · ','推导暴露候选 · ')+text(c.confidence_state,tr('unresolved','未确定'))+'. '+tr('Not bidder odds or a revenue estimate.','非投标概率或收入估算。'))+'</span></div>')});else if(r.company)out.push('<div class="transmission"><b>'+esc(text(r.company.ticker))+' · '+esc(text(r.company.name))+'</b><span>'+esc(tr('Recipient identity is curated; funding transmission still depends on award scope, timing and accounting.','收款方身份经人工整理；资金传导仍取决于授标范围、时点和会计处理。'))+'</span></div>');return out.join('')||'<div class="limit-copy">'+esc(r.kind==='award_change'?tr('Official procurement change; public-company transmission remains unresolved until a reviewed recipient or ownership path exists.','官方采购变化；在经核验的收款方或所有权路径建立前，上市公司传导仍未解析。'):tr('No defensible public-company transmission link is available yet.','目前尚无可靠的上市公司传导关联。'))+'</div>'}
  function sourceLinks(r){if(r.workspaceEvent){var receipts=arr((r.workspaceEvent.evidence||{}).receipts),html=receipts.map(function(x){var url=safeUrl(x.url);return url?'<a class="source-link" href="'+esc(url)+'" target="_blank" rel="noopener"><b>'+esc(text(x.publisher,tr('Open source receipt','打开来源凭证')))+' · '+esc(text(x.record_id,r.workspaceEvent.record_id))+'</b><span>↗</span></a>':''}).join('');return html||'<div class="limit-copy">'+esc(tr('This governed event has no public record URL. Review its retained receipt metadata.','该受治理事件没有公开记录链接。请查看其保留的凭证元数据。'))+'</div>'}var url=officialSource(r),html='';if(url)html+='<a class="source-link" href="'+esc(url)+'" target="_blank" rel="noopener"><b>'+esc(r.opportunity?tr('Open official SAM.gov record','打开 SAM.gov 官方记录'):tr('Open official source','打开官方来源'))+'</b><span>↗</span></a>';return html||'<div class="limit-copy">'+esc(tr('The compact artifact has no record-level public URL for this row. Use the dataset provenance receipt.','该精简数据未包含此行的记录级公开链接。请使用数据集出处凭证。'))+'</div>'}
  function crossLinks(r){var ticks=arr(r.tickers),html=ticks.map(function(tk){return'<button type="button" class="crosslink" data-company="'+esc(tk)+'">'+esc(tk)+' · '+esc(tr('company dossier','公司档案'))+'</button>'}).join('');if(r.workspaceEvent)arr(r.workspaceEvent.listed_company_impacts).forEach(function(impact){arr(impact.cross_desk_links).forEach(function(link){var href=safeHref(link.href);if(href&&link.available!==false)html+='<a class="crosslink" href="'+esc(href)+'">'+esc((zh()?link.label_zh:link.label_en)||link.surface_id||tr('Related desk','相关工作台'))+' ↗</a>'})});if(r.opportunity&&r.kind==='event')html+='<button type="button" class="crosslink" data-opportunity="'+esc(r.opportunity.notice_id)+'">'+esc(tr('Full opportunity','完整机会'))+'</button>';return html||'<span class="limit-copy">'+esc(tr('No related dossier is available in this evidence cut.','当前证据截点无相关档案。'))+'</span>'}
  function currentFilterState(){return{mode:state.mode,truth:state.truth,q:state.q,agency:state.agency,ticker:state.ticker}}
  function applyBriefcaseFilters(f){f=obj(f)?f:{};state.mode=f.mode||'changes';state.truth=f.truth||'all';state.q=f.q||'';state.agency=f.agency||'';state.ticker=f.ticker||'';$('deskSearch').value=state.q;$('agencyFilter').value=state.agency;$('tickerFilter').value=state.ticker;$('truthFilters').querySelectorAll('.filter-choice').forEach(function(x){x.classList.toggle('on',x.dataset.filter===state.truth)});activateMode(true)}
  function openEvidenceDrawer(payload){payload=obj(payload)?payload:{};lastFocus=payload.focus&&payload.focus.focus?payload.focus:document.activeElement;$('drawerTitle').textContent=text(payload.title,tr('Evidence receipts','证据凭证'));$('drawerBody').innerHTML=text(payload.html,'');$('drawerBackdrop').hidden=false;$('evidenceDrawer').hidden=false;state.drawer=true;$('drawerClose').focus()}
  try{if(typeof window.mountGovernmentRevenueBriefcaseUI==='function')briefcaseController=window.mountGovernmentRevenueBriefcaseUI({get:$,tr:tr,esc:esc,text:text,getFilters:currentFilterState,applyFilters:applyBriefcaseFilters,getWorkspace:function(){return WORKSPACE},isWorkspaceReady:function(){return briefcaseWorkspaceReady},openDrawer:openEvidenceDrawer})}catch(e){briefcaseController=null}
  var dossierUI=typeof window.createGovernmentRevenueDossier==='function'?window.createGovernmentRevenueDossier({obj:obj,arr:arr,esc:esc,text:text,n:n,money:money,date:date,tr:tr,safeUrl:safeUrl,factCell:factCell,host:function(){return $('dossierBook')},selected:function(){return state.selected},shellMode:function(open){$('gov-workspace').classList.toggle('dossier-open',!!open)},openEvidenceDrawer:openEvidenceDrawer}):{loadCompany:function(){},invalidate:function(){}};
  function setMobileSummary(title,meta){$('mobileSummary').hidden=false;$('mobileTitle').textContent=text(title);$('mobileMeta').textContent=text(meta)}
  // D3 §3d: the REAL module's HTTP-receipt status is authoritative; the typed
  // freshness.budget fallback applies ONLY with no module (spec §0 gate 5).
  var budgetUI=typeof window.createGovernmentRevenueBudget==='function'?window.createGovernmentRevenueBudget({obj:obj,arr:arr,esc:esc,text:text,n:n,money:money,date:date,tr:tr,safeUrl:safeUrl,host:function(){return $('inspector')},isSelected:function(id){return state.selected===id},openEvidenceDrawer:openEvidenceDrawer,copyLink:copyLink,setMobile:setMobileSummary,onRows:function(rows,meta){rowsByMode.budget=arr(rows).filter(obj);budgetStatus=text((meta||{}).status,'unavailable');syncCounts();populateFilters();if(state.mode==='budget'){state.selected='';applyFilters()}}}):(function(){budgetStatus=fallbackBudgetStatus();return{load:function(){return Promise.resolve([])},refresh:function(){return[]},render:function(){},invalidate:function(){},state:function(){return budgetStatus}}})();
  var fmsUI=typeof window.createGovernmentRevenueFms==='function'?window.createGovernmentRevenueFms({obj:obj,arr:arr,esc:esc,text:text,n:n,money:money,date:date,tr:tr,safeUrl:safeUrl,host:function(){return $('inspector')},isSelected:function(id){return state.selected===id},openEvidenceDrawer:openEvidenceDrawer,copyLink:copyLink,setMobile:setMobileSummary,onRows:function(rows,meta){rowsByMode.fms=arr(rows).filter(obj);fmsStatus=text((meta||{}).status,'unavailable');syncCounts();populateFilters();if(state.mode==='fms'){state.selected='';applyFilters()}}}):(function(){fmsStatus=fallbackFmsStatus();return{load:function(){return Promise.resolve([])},refresh:function(){return[]},render:function(){},invalidate:function(){},state:function(){return fmsStatus}}})();
  var programsUI=typeof window.createGovernmentRevenueProgramDossier==='function'?window.createGovernmentRevenueProgramDossier({obj:obj,arr:arr,esc:esc,text:text,n:n,money:money,date:date,tr:tr,safeUrl:safeUrl,host:function(){return $('inspector')},isSelected:function(id){return state.selected===id},openEvidenceDrawer:openEvidenceDrawer,copyLink:copyLink,setMobile:setMobileSummary,onRows:function(rows,meta){rowsByMode.programs=arr(rows).filter(obj);programsStatus=text((meta||{}).status,'unavailable');syncCounts();populateFilters();if(state.mode==='programs'){state.selected='';applyFilters()}}}):{load:function(){programsStatus='unavailable';return Promise.resolve()},refresh:function(){return[]},render:function(){},invalidate:function(){},state:function(){return programsStatus}};
  var candidateUI=typeof window.createGovernmentRevenueCandidateRadar==='function'?window.createGovernmentRevenueCandidateRadar({obj:obj,arr:arr,esc:esc,text:text,n:n,money:money,date:date,tr:tr,safeUrl:safeUrl,host:function(){return $('inspector')},openEvidenceDrawer:openEvidenceDrawer,copyLink:copyLink,setMobile:setMobileSummary,onRows:function(rows,meta){var requested=pendingSelection||state.selected,fresh=obj((meta||{}).freshness)?meta.freshness:{};rowsByMode.candidates=arr(rows).filter(obj);candidateStatus=text((meta||{}).status,'unavailable');candidateMeta={total:n((meta||{}).total)||0,mapping_backlog_total:n((meta||{}).mapping_backlog_total)||0,mapping_backlog_tickers:Array.isArray((meta||{}).mapping_backlog_tickers)?meta.mapping_backlog_tickers.slice():[],mapping_backlog_states:obj((meta||{}).mapping_backlog_states)?meta.mapping_backlog_states:{},exact_candidate_availability:text(fresh.exact_candidate_availability,'unavailable'),content_id:text((meta||{}).content_id)||null};renderTickerFilmstrip();syncCounts();populateFilters();if(state.mode==='candidates'){state.selected=requested;applyFilters();freshness()}}}):{load:function(){return Promise.resolve([])},refresh:function(){return[]},render:function(){},invalidate:function(){},state:function(){return 'unavailable'},crosschecks:function(){return ''}};
  var atlasUI=typeof window.createGovernmentRevenueIdentityAtlas==='function'?window.createGovernmentRevenueIdentityAtlas({obj:obj,arr:arr,esc:esc,text:text,n:n,date:date,tr:tr,zh:zh,safeUrl:safeUrl,host:function(){return $('identityAtlas')}}):{loadCompany:function(){},invalidate:function(){},state:function(){return'unavailable'}};
  function identityAtlasSection(){return'<section class="inspect-section"><div class="inspect-label">'+esc(tr('Identity Atlas','身份图谱'))+'</div><div class="identity-atlas" id="identityAtlas"><div class="dossier-status" aria-busy="true"><span><b>'+esc(tr('Tracing the identity path','正在追踪身份路径'))+'</b><span>'+esc(tr('Loading the reviewed recipient, entity and ownership record…','正在加载已核验的收款方、实体与所有权记录…'))+'</span></span><span class="dossier-count">•••</span></div></div></section>'}
  // D4: IRDM-only Company Financial Truth Bridge -- factory hides its own host for any other ticker.
  var bridgeUI=typeof window.createGovernmentRevenueCompanyBridge==='function'?window.createGovernmentRevenueCompanyBridge({obj:obj,arr:arr,esc:esc,text:text,n:n,date:date,tr:tr,safeUrl:safeUrl,factCell:factCell,host:function(){return $('companyBridge')},workspaceEvents:function(){return WORKSPACE_EVENTS},workspaceComplete:function(){return workspaceIsComplete()}}):{loadCompany:function(){},invalidate:function(){}};
  function companyBridgeSection(){return'<section class="inspect-section company-bridge" id="companyBridge" hidden></section>'}
  function dossierBookSection(){return'<section class="inspect-section"><div class="inspect-label">'+esc(tr('Award history','授标历史'))+'</div><div class="dossier-book" id="dossierBook"><div class="dossier-status"><span><b>'+esc(tr('Opening official award book','正在打开官方授标档案'))+'</b><span>'+esc(tr('Loading bounded official records…','正在加载受限官方记录…'))+'</span></span><span class="dossier-count">•••</span></div></div></section>'}
  function renderInspector(r){var el=$('inspector');if(!r||r.kind!=='company'){dossierUI.invalidate();atlasUI.invalidate();bridgeUI.invalidate()}if(!r||r.kind!=='budget_program')budgetUI.invalidate();if(!r||r.kind!=='program_dossier')programsUI.invalidate();if(!r||r.kind!=='fms_case')fmsUI.invalidate();if(!r){el.className='inspector-empty';el.innerHTML='<div><span class="empty-mark" aria-hidden="true">⌁</span><strong>'+esc(tr('No record selected','未选择记录'))+'</strong><p>'+esc(tr('Choose a row from the decision queue.','请从决策队列中选择一行。'))+'</p></div>';$('mobileSummary').hidden=true;return}if(r.kind==='budget_program'){budgetUI.render(r);return}if(r.kind==='program_dossier'){programsUI.render(r);return}if(r.kind==='fms_case'){fmsUI.render(r);return}var ident=inspectIdentity(r),stance=stanceSection(r),source=officialSource(r),sourceCopy=r.truth==='observed'?tr('Source receipt ↗','来源凭证 ↗'):tr('Official source ↗','官方来源 ↗');el.className='inspector';el.innerHTML='<div class="inspect-hero"><div class="inspect-kicker"><span class="inspect-type">'+esc(ident.type)+'</span><span class="inspect-id">'+esc(ident.id)+'</span></div><h2>'+esc(ident.title)+'</h2><div class="inspect-meta">'+esc(ident.meta||tr('Evidence-linked research record','证据关联研究记录'))+'</div><div class="inspect-truth">'+truthChip(r.truth,truthLabel(r))+secondaryTruth(r)+correctionChip(r)+'</div><div class="inspect-actions"><button class="tool-btn" type="button" id="evidenceOpen">'+esc(tr('View receipts','查看凭证'))+'</button><button class="tool-btn" type="button" id="copyLink">'+esc(tr('Copy link','复制链接'))+'</button>'+(source?'<a class="tool-btn" href="'+esc(source)+'" target="_blank" rel="noopener">'+esc(sourceCopy)+'</a>':'')+'</div></div>'+
      '<section class="inspect-section"><div class="inspect-label">'+esc(tr('What changed','发生了什么变化'))+'</div><div class="inspect-value"><strong>'+esc(changedSection(r))+'</strong></div></section>'+
      '<section class="inspect-section"><div class="inspect-label">'+esc(tr('Why it matters','为何重要'))+'</div><div class="inspect-value">'+esc(whySection(r))+'</div></section>'+
      '<section class="inspect-section"><div class="inspect-label">'+esc(tr('Research stance','研究动作'))+'</div><div class="stance"><b>'+esc(stance[0])+'</b><span>'+esc(stance[1])+'</span></div></section>'+
      '<section class="inspect-section"><div class="inspect-label">'+esc(tr('Revision diff','修订差异'))+'</div>'+diffRows(r)+successorLine(r)+'</section>'+
      clocksSection(r)+
      '<section class="inspect-section"><div class="inspect-label">'+esc(tr('Dates & amounts','日期与金额'))+'</div><div class="facts-grid">'+factGrid(r)+'</div></section>'+
      '<section class="inspect-section"><div class="inspect-label">'+esc(tr('Company transmission','公司传导'))+'</div><div class="transmission-list">'+transmissions(r)+'</div></section>'+
      '<section class="inspect-section"><div class="inspect-label">'+esc(tr('Evidence & limits','证据与限制'))+'</div><div class="limit-copy">'+esc(authorityLimit())+'</div>'+(observationLimit(r)?'<div class="limit-copy" style="margin-top:7px">'+esc(observationLimit(r))+'</div>':'')+sourceLinks(r)+'</section>'+
      (r.kind==='company'?identityAtlasSection()+dossierBookSection():'')+
      '<section class="inspect-section"><div class="inspect-label">'+esc(tr('Cross-links','交叉链接'))+'</div><div class="crosslinks">'+crossLinks(r)+'</div></section>';
    $('evidenceOpen').addEventListener('click',function(){openDrawer(r)});$('copyLink').addEventListener('click',copyLink);el.querySelectorAll('[data-company]').forEach(function(b){b.addEventListener('click',function(){state.mode='companies';activateMode(false);var target=rowsByMode.companies.find(function(x){return x.company.ticker===b.dataset.company});if(target)selectRow(target.id,true)})});el.querySelectorAll('[data-opportunity]').forEach(function(b){b.addEventListener('click',function(){state.mode='opportunities';activateMode(false);var target=rowsByMode.opportunities.find(function(x){return x.opportunity.notice_id===b.dataset.opportunity});if(target)selectRow(target.id,true)})});if(r.kind==='company'){var atlasTicker=text((r.company||{}).ticker,'');dossierUI.loadCompany(atlasTicker);atlasUI.loadCompany(atlasTicker)}setMobileSummary(ident.title,ident.type+' · '+(r.tickers||[]).join(' '))
  }
  function renderCompanyCoverageInspector(r){
    var el=$('inspector'),c=r.company||{},ticker=text(c.ticker),name=text(c.name,ticker),verified=tickerRailState(ticker)==='verified';
    var meta=verified?tr('Discovery-scope history · issuer path verified','发现范围历史 · 发行人路径已核验'):tr('Discovery-scope history · mapping needed','发现范围历史 · 需要映射');
    var status=verified?tr('Issuer path verified','发行人路径已核验'):tr('Issuer mapping needed','需要发行人映射');
    var proof=verified?tr('An exact, reviewed recipient-to-issuer path exists for covered identifiers. No current eligible receipt-bound award change exists, so this is not a candidate or signal. Discovery history remains partial.','已为覆盖标识符建立精确、已审核的收款方至发行人路径。当前没有符合条件的凭证绑定授标变化，因此不构成候选或信号。发现历史仍不完整。'):tr('This is discovery-scope award history. It does not establish a recipient-to-issuer path or name an investable candidate.','这是发现范围内的授标历史。它不构成收款方至发行人的路径，也不会指名可投资候选。');
    var stance=verified?tr('Monitor exact changes','监测精确变化'):tr('Mapping needed','需要映射');
    var stanceCopy=verified?tr('Keep the verified path ready for the event engine; wait for a fresh eligible award-change receipt before emitting a research candidate.','保持已核验路径供事件引擎使用；在发出研究候选前，等待新的合格授标变化凭证。'):tr('Use this coverage history as a research lead; wait for an exact recipient-to-issuer path before naming a candidate.','将此覆盖历史作为研究线索；在建立精确收款方至发行人路径前，不要指名候选。');
    budgetUI.invalidate();el.className='inspector';el.innerHTML='<div class="inspect-hero"><div class="inspect-kicker"><span class="inspect-type">'+esc(tr('Company coverage','公司覆盖'))+'</span><span class="inspect-id">'+esc(ticker)+'</span></div><h2>'+esc(name)+'</h2><div class="inspect-meta">'+esc(meta)+'</div><div class="inspect-truth">'+truthChip('observed',tr('Discovery coverage','发现范围覆盖'))+truthChip(verified?'reviewed':'observed',status)+'</div><div class="inspect-actions"><button class="tool-btn" type="button" id="coverageEvidenceOpen">'+esc(tr('View receipts','查看凭证'))+'</button><button class="tool-btn" type="button" id="coverageCopyLink">'+esc(tr('Copy link','复制链接'))+'</button></div></div><section class="inspect-section"><div class="inspect-label">'+esc(tr('Coverage status','覆盖状态'))+'</div><div class="candidate-proof"><strong>'+esc(status)+'</strong><span>'+esc(proof)+'</span></div></section><section class="inspect-section"><div class="inspect-label">'+esc(tr('Visible history','可见历史'))+'</div><div class="facts-grid">'+factCell(tr('TTM obligations','TTM 义务额'),money(metric(c,'ttm_obligations')))+factCell(tr('Award flow YoY','授标流同比'),pct(metric(c,'award_velocity_yoy_pct')))+factCell(tr('Observed capacity','观测余量'),money(metric(c,'funded_capacity_observed')))+factCell(tr('Net award action flow (90d)','净授标行动流（90天）'),money(metric(c,'net_award_action_flow_90d')))+'</div></section><section class="inspect-section"><div class="inspect-label">'+esc(tr('Research stance','研究动作'))+'</div><div class="stance"><b>'+esc(stance)+'</b><span>'+esc(stanceCopy)+'</span></div></section>'+identityAtlasSection()+dossierBookSection()+(ticker==='IRDM'?companyBridgeSection():'')+'<section class="inspect-section"><div class="inspect-label">'+esc(tr('Evidence & limits','证据与限制'))+'</div><div class="limit-copy">'+esc(tr('Coverage context only. It cannot rank a company, change conviction, size a position or trigger a trade.','仅作覆盖情境。不得为公司排序、改变信心、调整仓位或触发交易。'))+'</div>'+sourceLinks(r)+'</section>';
    $('coverageEvidenceOpen').addEventListener('click',function(){openDrawer(r)});$('coverageCopyLink').addEventListener('click',copyLink);dossierUI.loadCompany(ticker);atlasUI.loadCompany(ticker);bridgeUI.loadCompany(ticker);setMobileSummary(name,meta)
  }
  function selectRow(id,writeUrl){state.selected=id;pendingSelection='';missingCandidateSelection='';document.querySelectorAll('.queue-row').forEach(function(el){var on=el.dataset.row===id;el.classList.toggle('on',on);el.setAttribute('aria-selected',on?'true':'false');el.tabIndex=on?0:-1});var r=visible.find(function(x){return x.id===id})||arr(rowsByMode[state.mode]).find(function(x){return x.id===id});if(r&&r.kind==='candidate'){dossierUI.invalidate();atlasUI.invalidate();budgetUI.invalidate();bridgeUI.invalidate();candidateUI.render(r)}else if(r&&r.kind==='company')renderCompanyCoverageInspector(r);else renderInspector(r||null);if(writeUrl)syncUrl()}
  function syncCounts(){$('countCandidates').textContent=candidateMeta.total;$('countChanges').textContent=rowsByMode.changes.length;$('countAwards').textContent=rowsByMode.awards.length;$('countOpps').textContent=rowsByMode.opportunities.length;$('countRecompetes').textContent=rowsByMode.recompetes.length;$('countBudget').textContent=rowsByMode.budget.length;$('countPrograms').textContent=rowsByMode.programs.length;$('countCompanies').textContent=rowsByMode.companies.length;$('countFms').textContent=rowsByMode.fms.length;$('filterAllCount').textContent=arr(rowsByMode[state.mode]).length}
  function syncUrl(){try{var u=new URL(location.href);u.searchParams.set('mode',state.mode);if(state.selected)u.searchParams.set('item',state.selected);else u.searchParams.delete('item');if(state.ticker)u.searchParams.set('ticker',state.ticker);else u.searchParams.delete('ticker');history.replaceState(null,'',u.pathname+u.search+u.hash)}catch(e){}}
  function copyLink(button){syncUrl();var value=location.href,target=button&&button.textContent!=null?button:$('copyLink');if(navigator.clipboard&&navigator.clipboard.writeText)navigator.clipboard.writeText(value).then(function(){if(target)target.textContent=tr('Copied','已复制')},function(){window.prompt(tr('Copy this link','复制此链接'),value)});else window.prompt(tr('Copy this link','复制此链接'),value)}
  function activateMode(writeUrl,preserveSelection){document.querySelectorAll('.mode-tab').forEach(function(t){var on=t.dataset.mode===state.mode;t.setAttribute('aria-selected',on?'true':'false');t.tabIndex=on?0:-1});$('companyMode').setAttribute('aria-pressed',state.mode==='companies'?'true':'false');if(!preserveSelection){state.selected='';pendingSelection='';missingCandidateSelection=''}applyFilters();freshness();if(writeUrl)syncUrl()}
  function populateFilters(){var agencies=Array.from(new Set([].concat(rowsByMode.changes,rowsByMode.opportunities,rowsByMode.recompetes,rowsByMode.budget,rowsByMode.programs,rowsByMode.fms).map(function(r){return agencyName(r.agency)}).filter(Boolean))).sort();$('agencyFilter').innerHTML='<option value="">'+esc(tr('All agencies','全部机构'))+'</option>'+agencies.map(function(a){return'<option value="'+esc(a)+'">'+esc(a)+'</option>'}).join('');$('tickerFilter').innerHTML='<option value="">'+esc(tr('All companies','全部公司'))+'</option>'+COMPANIES.slice().sort(function(a,b){return String(a.ticker).localeCompare(String(b.ticker))}).map(function(c){return'<option value="'+esc(c.ticker)+'">'+esc(c.ticker+' · '+c.name)+'</option>'}).join('')}
  function freshness(){var assessment=effectiveFreshnessState(DATA,WORKSPACE,Date.now()),status=assessment.status,awardStatus=assessment.award_events,awardMode=state.mode==='awards',budgetMode=state.mode==='budget';if(awardMode&&awardStatus!=='ok')status=awardStatus;if(budgetMode&&budgetStatus!=='ok')status=budgetStatus;var cls=/fail|error|blocked/.test(status)?'bad':/stale|partial|degrad|unverified|unavailable|future|loading|projection_missing/.test(status)?'warn':'',copy=budgetMode?(budgetStatus==='loading'?tr('Budget graph verifying','预算图谱正在核验'):budgetStatus==='ok'?tr('Budget request evidence connected','预算请求证据已连接'):budgetStatus==='projection_missing'?tr('PROJECTION_MISSING','PROJECTION_MISSING'):tr('Budget request rail unavailable','预算请求链路不可用')):(state.mode==='opportunities'&&opportunityRailUnavailable()?tr('SOURCE_UNAVAILABLE','SOURCE_UNAVAILABLE'):awardMode&&awardStatus!=='ok'?(cls==='bad'?tr('Award tape unavailable','授标脉搏不可用'):tr('Award tape warming or qualified','授标脉搏正在建立或覆盖受限')):cls==='bad'?tr('Procurement sources unavailable','采购数据源不可用'):cls==='warn'?tr('Partial or stale coverage','覆盖部分或过期'):tr('Official sources connected','官方数据源已连接')),clock=budgetMode&&budgetStatus!=='ok'?tr('No budget source observation timestamp is available.','没有可用的预算来源观测时间。'):assessment.observed_at?(assessment.aged?tr('Oldest required source observed ','最早所需来源观测于 '):tr('Latest covered source observed ','最新覆盖来源观测于 '))+date(assessment.observed_at):tr('No source observation timestamp is available.','没有可用的数据源观测时间。'),age=budgetMode&&budgetStatus!=='ok'?'':assessment.age_minutes!=null?tr('Age ','时效 ')+ageCopy(assessment.age_minutes)+(assessment.freshness_sla_minutes!=null?tr(' · SLA ',' · SLA ')+ageCopy(assessment.freshness_sla_minutes):''):'',capNote=workspaceCoverageCopy(),railNote=budgetMode?tr('Budget request rail: ','预算请求链路：')+text(budgetStatus,'unavailable'):awardMode?tr('Award-event rail: ','授标事件链路：')+text(awardStatus,'unavailable'):'';$('sourceHealth').className='health '+cls;$('healthCopy').textContent=copy;$('pulseStatus').textContent=copy;$('pulseStance').textContent=cls==='bad'?tr('Use last-good evidence only','仅使用最近可用证据'):cls==='warn'?tr('Watch with qualified coverage','在有限覆盖下观察'):tr('Watch the procurement tape','观察采购脉搏');$('healthDetail').innerHTML='<div class="mini-health '+cls+'"><i aria-hidden="true"></i><span><strong>'+esc(copy)+'</strong><br>'+esc(clock)+(age?'<br>'+esc(age):'')+(railNote?'<br>'+esc(railNote):'')+'<br>'+esc(tr('SAM scope: configured defense and technology acquisition universe.','SAM 范围：已配置的国防与技术采购领域。'))+(capNote?'<br>'+esc(capNote):'')+'</span></div>';return cls}
  function headline(){var market=obj(DATA.market)?DATA.market:{},om=obj(OPPINT.market)?OPPINT.market:{},wc=obj(WORKSPACE.coverage)?WORKSPACE.coverage:{},covered=wc.events_visible!=null?wc.events_visible:(WORKSPACE.total!=null?WORKSPACE.total:WORKSPACE_EVENTS.length);$('statChanges').textContent=text(HAS_WORKSPACE?covered:om.amendments_7d,0);$('statOpps').textContent=text(HAS_WORKSPACE?wc.open_opportunities:(om.active_opportunities!=null?om.active_opportunities:market.active_opportunities),0);$('statDeadlines').textContent=text(om.deadlines_30d,0);$('statExposure').textContent=money(market.ttm_obligations);var clock=$('knownAt'),iso=clock.dataset.iso;if(iso)clock.textContent=date(iso);freshness()}
  function receiptRows(r){
    var out=[],src=officialSource(r),o=r.opportunity||{},e=r.event||{};
    if(r.workspaceEvent){
      var w=r.workspaceEvent,ev=w.evidence||{},change=w.change||{},award=w.award_change||{},impacts=arr(w.listed_company_impacts).filter(obj),allReviewed=impacts.length&&impacts.every(function(x){return x.relation_semantic==='reviewed'});
      arr(ev.receipts).forEach(function(x){
        var body=r.kind==='award_change'?tr('Receipt-bound USAspending award or action observation. The source identity and both visible clocks are retained.','与凭证绑定的 USAspending 授标或行动观测。来源身份及两个可见时钟均被保留。'):ev.source_class==='observed_source_revision'?tr('Observed document bytes changed. This is distinct from an official amendment.','观测到文件字节发生变化。这与官方修订不同。'):r.kind==='recompete'?tr('The official record supports the period-of-performance end date; the expiry watch remains derived.','官方记录支持履约结束日；到期观察仍为推导。'):tr('Government-published fields retained in the governed event contract.','受治理事件合约中保留的政府发布字段。');
        var code='ref_id: '+text(x.ref_id)+'\neffective_at: '+text(x.effective_at)+'\nknown_at: '+text(x.known_at)+'\nretrieved_at: '+text(x.retrieved_at)+'\nsha256: '+text(x.content_sha256);
        if(r.kind==='award_change')code+='\naward_key: '+text(award.award_key)+'\naction_id: '+text(award.action_id)+'\nsource_rail: '+text(award.source_rail)+'\nsource_identity: '+compact(award.source_identity);
        out.push({kind:x.publisher||tr('Source receipt','来源凭证'),title:text(x.record_id,w.record_id),body:body,code:code,url:safeUrl(x.url)})
      });
      out.push({kind:tr('Governed event','受治理事件'),title:workspaceSummary(w),body:tr('Exact visible field changes are carried by the backend contract, not reconstructed in the browser.','精确的可见字段变化由后端合约提供，而非在浏览器中重建。'),code:'event_id: '+text(w.event_id)+'\nrecord_id: '+text(w.record_id)+'\nchange_type: '+text(change.type)+'\nchanged_fields: '+compact(change.changed_fields)});
      arr(ev.derivations).forEach(function(x){out.push({kind:tr('Deterministic derivation','确定性推导'),title:text(x.classification||x.method).replace(/_/g,' '),body:text(x.detail)||tr('A governed rule created context; it did not create an official fact or forecast.','受治理规则生成情境；它并未生成官方事实或预测。'),code:'ref_id: '+text(x.ref_id)+'\nformula_version: '+text(x.formula_version)+'\nbasis_refs: '+arr(x.basis_refs).join(', ')})});
      if(impacts.length)out.push({kind:allReviewed?tr('Reviewed issuer links','已核验发行人关联'):tr('Derived issuer links','推导公司关联'),title:impacts.map(function(x){return x.ticker}).filter(Boolean).join(' · '),body:allReviewed?tr('Recipient and ownership paths were reviewed against governed evidence. This is still not reported revenue or an investment ranking.','收款方与所有权路径已根据受治理证据核验。它仍不等于报告收入或投资排名。'):tr('Rule-based recipient or capability links. Not bidder odds, revenue estimates or investment rankings.','基于规则的收款方或能力关联。非投标概率、收入估算或投资排名。'),code:impacts.map(function(x){return text(x.ticker)+': '+text(x.relation_semantic)+' · '+text(x.match_method)+' · '+text(x.confidence)}).join('\n')});
      out.push({kind:tr('Authority & limits','权限与限制'),title:tr('Display priority is not investment rank','展示优先级并非投资排名'),body:arr(ev.limitations).concat(arr(WORKSPACE.limitations)).join(' · '),code:'formula_version: '+text((w.display_priority||{}).formula_version)+'\nis_investment_rank: false\ncan_rank: '+text((w.authority||{}).can_rank)+'\ncan_size: '+text((w.authority||{}).can_size)+'\nconflicts: '+compact(ev.conflicts)});
      return out
    }
    if(src)out.push({kind:tr('Official source','官方来源'),title:r.title,body:r.kind==='recompete'?tr('The source supports the period-of-performance end date, not a guaranteed recompete.','该来源支持履约期结束日期，不代表确定的再竞争。'):tr('Open the source record to verify the current government-published fields.','打开源记录以核实当前政府发布字段。'),code:src,url:src});
    if(o.notice_id)out.push({kind:tr('Record identity','记录身份'),title:o.solicitation_number||o.notice_id,body:tr('Point-in-time opportunity revision retained by MastermindX.','MastermindX 保留的时点机会修订。'),code:'notice_id: '+o.notice_id+'\nrevision_id: '+text(o.revision_id)+'\nknown_at: '+text(o.known_at)});
    if(e.event_id)out.push({kind:tr('Revision event','修订事件'),title:changedCopy(e.changed_fields),body:tr('Field names are a deterministic diff between visible official versions.','字段名称是可见官方版本之间的确定性差异。'),code:'event_id: '+e.event_id+'\nchanged: '+arr(e.changed_fields).join(', ')});
    if(o.company_candidates&&o.company_candidates.length)out.push({kind:tr('Derived issuer link','推导公司关联'),title:candidateCopy(o),body:tr('Rule-based overlap using notice text, capabilities and award history. Not bidder odds, an award forecast or a revenue estimate.','基于公告文本、能力和授标历史的规则重合。非投标概率、授标预测或收入估算。'),code:arr(o.company_candidates).map(function(c){return c.ticker+': '+arr(c.match_reasons).map(function(x){return x.kind}).join(', ')}).join('\n')});
    var c=r.company||{};arr(c.source_receipts||c.provenance).slice(0,2).forEach(function(p){out.push({kind:tr('Dataset provenance','数据集出处'),title:p.publisher||p.dataset,body:arr(p.limitations).join(' · '),code:'known_at: '+text(p.known_at)+'\neffective_through: '+text(p.effective_through),url:safeUrl(p.source_url)})});
    return out
  }
  function openDrawer(r){var rows=receiptRows(r),html=rows.map(function(x){return'<article class="receipt"><div class="receipt-kind">'+esc(x.kind)+'</div><h3>'+esc(x.title)+'</h3><p>'+esc(x.body)+'</p><div class="receipt-code">'+esc(x.code)+'</div>'+(x.url?'<a class="source-link" href="'+esc(x.url)+'" target="_blank" rel="noopener"><b>'+esc(tr('Open receipt','打开凭证'))+'</b><span>↗</span></a>':'')+'</article>'}).join('')||'<div class="empty-state"><div><strong>'+esc(tr('No public receipt in compact artifact','精简数据中无公开凭证'))+'</strong><p>'+esc(tr('The row remains bounded by its dataset provenance and authority limits.','该行仍受数据集出处和权限限制约束。'))+'</p></div></div>';openEvidenceDrawer({title:tr('Evidence receipts','证据凭证'),html:html,focus:document.activeElement})}
  function closeDrawer(){if(!state.drawer)return;$('drawerBackdrop').hidden=true;$('evidenceDrawer').hidden=true;$('drawerTitle').textContent=tr('Evidence receipts','证据凭证');state.drawer=false;if(lastFocus&&lastFocus.focus)lastFocus.focus()}
  function trapDrawerFocus(e){if(!state.drawer||e.key!=='Tab')return;var nodes=Array.prototype.slice.call($('evidenceDrawer').querySelectorAll('a[href],button:not([disabled]),input:not([disabled]),select:not([disabled]),[tabindex]:not([tabindex="-1"])'));if(!nodes.length)return;var first=nodes[0],last=nodes[nodes.length-1];if(e.shiftKey&&document.activeElement===first){e.preventDefault();last.focus()}else if(!e.shiftKey&&document.activeElement===last){e.preventDefault();first.focus()}}
  function openMobileInspector(){if(window.innerWidth>980)return;$('inspectorPane').classList.add('mobile-sheet');$('inspectorOpen').setAttribute('aria-expanded','true');$('drawerBackdrop').hidden=false}
  function closeSheets(){if($('filterPane').classList.contains('mobile-sheet')){$('filterPane').classList.remove('mobile-sheet');$('filterOpen').setAttribute('aria-expanded','false')}if($('inspectorPane').classList.contains('mobile-sheet')){$('inspectorPane').classList.remove('mobile-sheet');$('inspectorOpen').setAttribute('aria-expanded','false')}if(!state.drawer)$('drawerBackdrop').hidden=true}
  function rowKeys(e){if(!/^(ArrowDown|ArrowUp|Home|End|Enter)$/.test(e.key))return;var els=Array.prototype.slice.call($('queueList').querySelectorAll('.queue-row'));if(!els.length)return;var i=els.indexOf(document.activeElement),next=i;if(e.key==='Home')next=0;else if(e.key==='End')next=els.length-1;else if(e.key==='ArrowDown')next=Math.min(els.length-1,Math.max(0,i+1));else if(e.key==='ArrowUp')next=Math.max(0,i<0?0:i-1);else if(e.key==='Enter'){if(i>=0)selectRow(els[i].dataset.row,true);if(window.innerWidth<=980)openMobileInspector();return}e.preventDefault();els[next].focus();selectRow(els[next].dataset.row,true)}
  function bind(){document.querySelectorAll('.mode-tab').forEach(function(tab){tab.addEventListener('click',function(){state.mode=tab.dataset.mode;activateMode(true)});tab.addEventListener('keydown',function(e){if(!/^(ArrowRight|ArrowLeft|Home|End)$/.test(e.key))return;var tabs=Array.prototype.slice.call(document.querySelectorAll('.mode-tab')),i=tabs.indexOf(tab),next=e.key==='Home'?0:e.key==='End'?tabs.length-1:(i+(e.key==='ArrowRight'?1:-1)+tabs.length)%tabs.length;e.preventDefault();state.mode=tabs[next].dataset.mode;activateMode(true);tabs[next].focus()})});$('companyMode').addEventListener('click',function(){state.mode='companies';activateMode(true)});$('deskSearch').addEventListener('input',function(){state.q=this.value.trim();applyFilters()});$('deskSearch').addEventListener('keydown',function(e){if(e.key==='Escape'){this.value='';state.q='';applyFilters();this.blur()}});$('truthFilters').addEventListener('click',function(e){var b=e.target.closest('[data-filter]');if(!b)return;state.truth=b.dataset.filter;this.querySelectorAll('.filter-choice').forEach(function(x){x.classList.toggle('on',x===b)});applyFilters()});$('agencyFilter').addEventListener('change',function(){state.agency=this.value;applyFilters()});$('tickerFilter').addEventListener('change',function(){state.ticker=this.value;applyFilters();syncUrl()});$('queueList').addEventListener('keydown',rowKeys);$('drawerClose').addEventListener('click',closeDrawer);$('evidenceDrawer').addEventListener('keydown',trapDrawerFocus);$('drawerBackdrop').addEventListener('click',function(){closeDrawer();closeSheets()});$('filterOpen').addEventListener('click',function(){$('filterPane').classList.add('mobile-sheet');this.setAttribute('aria-expanded','true');$('drawerBackdrop').hidden=false;$('deskSearch').focus()});$('inspectorOpen').addEventListener('click',openMobileInspector);document.addEventListener('keydown',function(e){var tag=(e.target&&e.target.tagName||'').toLowerCase();if(e.key==='/'&&!/input|textarea|select/.test(tag)&&!(e.target&&e.target.isContentEditable)){e.preventDefault();$('deskSearch').focus()}if(e.key==='Escape'){closeDrawer();closeSheets()}});document.addEventListener('langchange',function(){boot(false);if(bannerShown)workspaceBanner(bannerShown.state,bannerShown.reason)})}
  function bindBriefcase(){if(briefcaseController){briefcaseController.bind();return}['savedViewSelect','saveView','deleteView','toggleLocalAlert','localInbox','exportViewJson','exportViewCsv'].forEach(function(id){$(id).disabled=true})}
  function bindHydration(){var retry=$('workspaceRetry');if(retry)retry.addEventListener('click',function(){hydrateWorkspace(true)})}
  function whenAuthSettled(){return new Promise(function(resolve){var done=false;function go(){if(done)return;done=true;window.__govrevAuthSettled=true;resolve()}if(window.MDXAuth&&typeof window.MDXAuth.onChange==='function'){window.MDXAuth.onChange(function(){go()});return}if(typeof window.addEventListener==='function')window.addEventListener('mdx-auth',go)})}
  function bindAuthReload(){whenAuthSettled().then(function(){budgetUI.load();programsUI.load();candidateUI.load();fmsUI.load()});if(typeof window.addEventListener==='function')window.addEventListener('mdx-auth',function(){window.__govrevAuthSettled=true;budgetUI.load();programsUI.load();candidateUI.load();fmsUI.load()})}
  function restoreUrl(){try{var p=new URLSearchParams(location.search),m=p.get('mode'),id=p.get('item'),ticker=p.get('ticker');if(['candidates','changes','awards','opportunities','recompetes','budget','programs','companies','fms'].indexOf(m)>-1)state.mode=m;if(id){state.selected=id;pendingSelection=id}if(ticker&&COMPANIES.some(function(c){return c.ticker===ticker})){state.ticker=ticker;$('tickerFilter').value=ticker}}catch(e){}}
  function boot(first){buildRows();renderTickerFilmstrip();populateFilters();restoreUrl();headline();syncCounts();activateMode(false,true);briefcaseWorkspaceReady=HAS_WORKSPACE&&!workspaceNeedsHydration(WORKSPACE)&&!WORKSPACE.next_cursor;if(workspaceIsComplete())workspaceBanner(null);if(briefcaseController)briefcaseController.render();if(first){bind();bindHydration();bindAuthReload();bindBriefcase();budgetUI.load();programsUI.load();candidateUI.load();fmsUI.load()}else{budgetUI.refresh();programsUI.refresh();candidateUI.refresh();fmsUI.refresh()}if(briefcaseWorkspaceReady&&briefcaseController)briefcaseController.reconcile(false)}
  var bannerShown=null;
  function workspaceBanner(state,reason){bannerShown=state?{state:state,reason:reason}:null;var banner=$('workspaceDegraded'),copy=$('workspaceDegradedCopy'),retry=$('workspaceRetry'),unlock=$('workspaceUnlock');if(!banner||!copy||!retry)return;if(!state||((reason==='loading'||reason==='unavailable')&&workspaceIsComplete())){banner.hidden=true;retry.hidden=true;retry.disabled=false;if(unlock)unlock.hidden=true;delete banner.dataset.state;bannerShown=null;return}var visible=WORKSPACE_EVENTS.length,summary=tr('Showing the first ','仅显示前 ')+visible+tr(' governed records while the complete workspace is unavailable.',' 条受治理记录；完整工作区暂不可用。');if(reason==='loading')summary=tr('Loading the complete governed workspace…','正在加载完整受治理工作区…');if(reason==='bundle_mismatch')summary=tr('Showing the first ','仅显示前 ')+visible+tr(' governed records because the full workspace belongs to a different evidence cut.',' 条受治理记录，因为完整工作区属于不同的证据截点。');if(reason==='invalid')summary=tr('Showing the first ','仅显示前 ')+visible+tr(' governed records because the full workspace did not pass integrity checks.',' 条受治理记录，因为完整工作区未通过完整性检查。');if(reason==='locked')summary=tr('Showing the first ','仅显示前 ')+visible+tr(' governed records. The complete workspace is part of a membership.',' 条受治理记录。完整工作区包含在会员权益中。');banner.hidden=false;banner.dataset.state=state;copy.textContent=summary;if(unlock)unlock.hidden=reason!=='locked';retry.hidden=reason==='loading'||reason==='locked';retry.disabled=reason==='loading'}
  function hydrationFailure(code){var error=new Error(code);error.code=code;return error}
  function hydrateWorkspace(force){
    var needs=workspaceNeedsHydration(WORKSPACE)||!!WORKSPACE.next_cursor;
    if(!needs&&!force){if(workspaceIsComplete())workspaceBanner(null);return Promise.resolve(false)}
    if(!HAS_WORKSPACE||typeof fetch!=='function'){workspaceBanner('degraded','unavailable');return Promise.resolve(false)}
    if(workspaceIsComplete()&&!force){workspaceBanner(null);return Promise.resolve(false)}
    workspaceBanner('loading','loading');
    // D4: this hydration completion handler's own `applyFilters()` call below
    // (after WORKSPACE_EVENTS is replaced with the full set) re-runs
    // selectRow() for the CURRENT state.selected -- so a company row selected
    // before hydration finishes self-heals bridgeUI.loadCompany(ticker) a
    // second time here with the complete event set, with no polling loop of
    // its own. govFactHtml() additionally distinguishes "not yet hydrated"
    // (workspaceIsComplete()===false -> loading copy) from "hydrated and the
    // event is genuinely absent" (-> the unavailable copy) so the pre-heal
    // render is never mistaken for a permanent verdict.
    return fetch('government-revenue-data/workspace.json',{credentials:'same-origin'}).then(function(response){if(!response.ok)throw hydrationFailure(response.status===401||response.status===403?'locked':'unavailable');return response.json()}).then(function(full){if(!obj(full)||!/^government_procurement_workspace\.v[12]$/.test(text(full.schema_version))||!Array.isArray(full.events))throw hydrationFailure('invalid');if(!workspaceBundleMatches(WORKSPACE,full))throw hydrationFailure('bundle_mismatch');var fullEvents=arr(full.events).filter(obj),fullExpected=workspaceExpectedCount(full);if(fullEvents.length<WORKSPACE_EVENTS.length||fullEvents.length<fullExpected)throw hydrationFailure('invalid');var requested=state.selected;if(state.mode!=='candidates'){try{requested=new URLSearchParams(location.search).get('item')||state.selected}catch(e){requested=state.selected}}WORKSPACE=full;WORKSPACE_EVENTS=fullEvents;HAS_WORKSPACE=true;workspaceBanner(null);buildRows();populateFilters();$('agencyFilter').value=state.agency;$('tickerFilter').value=state.ticker;state.selected=requested;headline();syncCounts();applyFilters();briefcaseWorkspaceReady=true;if(briefcaseController)briefcaseController.reconcile(true);try{document.dispatchEvent(new CustomEvent('governmentworkspacehydrated',{detail:{events:WORKSPACE_EVENTS.length,bundle_id:WORKSPACE.bundle_id}}))}catch(e){}return true}).catch(function(error){briefcaseWorkspaceReady=false;var code=error&&error.code||'unavailable';workspaceBanner(code==='locked'?'locked':'degraded',code);return false})
  }
  window.__governmentRevenueRuntime={effectiveFreshnessState:effectiveFreshnessState,workspaceBundleMatches:workspaceBundleMatches,workspaceNeedsHydration:workspaceNeedsHydration,workspaceIsComplete:workspaceIsComplete,agencyName:agencyName,tickerRailState:tickerRailState,openTickerFilmstrip:openTickerFilmstrip,currentSelection:function(){return{mode:state.mode,ticker:state.ticker,selected:state.selected,pending:pendingSelection}},workspaceRows:function(){return rowsByMode.changes.map(function(r){return{kind:r.kind,truth:r.truth,linked:r.linked,defense:r.defense,title:r.title}})},agencyNames:function(){return rowsByMode.changes.map(function(r){return r.agency})},agencyOptions:function(){var el=$('agencyFilter');return el?String(el.innerHTML||''):''}};
  window.__hydrateGovernmentWorkspace=hydrateWorkspace;
  boot(true);
  hydrateWorkspace(false);
})();
</script><script src="theme.js"></script></body>
</html>
