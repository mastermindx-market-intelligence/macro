/* Runtime-generated on the VPS from /etc/macro-api.env.
   Empty in source and local builds: stock-logos.js keeps its monogram fallback. */
window.MMX_LOGO_DEV_TOKEN = window.MMX_LOGO_DEV_TOKEN || "";
