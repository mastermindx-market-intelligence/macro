.venv/
.review_venv/
__pycache__/
*.pyc
.pytest_cache/
# Local build caches keyed by tool (e.g. scripts/make_launch_card.py caches the
# Google Fonts woff2 blobs it inlines). Re-downloadable; never a delivery path.
.cache/
.DS_Store
.env
.env.*
!.env.example
*.pem
*.p12
*.pfx
id_rsa*
id_ed25519*
# Operator analytics self-exclusion hide-list — holds personal emails / home location.
# Kept OUT of git (the .example.json template documents the schema). The admin service
# reads it via ANALYTICS_EXCLUSIONS; recommended location is /var/lib/macro-admin/.
admin/analytics_exclusions.json
# Brain guest-access knob — operator-tunable anonymous free Fast lane (on/off + daily cap).
# Kept OUT of git (the .example.json template documents the schema) so toggling never touches
# git / needs a deploy. Read via BRAIN_GUEST_CFG (default admin/brain_guest_access.json).
admin/brain_guest_access.json
# VPS deploy artifact: Caddy's served root is an atomic rsync mirror of site/,
# living OUTSIDE the git work-tree so `git reset --hard` can't expose a partial
# file (app/deploy/update.sh + Caddyfile). Box-only; never committed.
site.served/
# retired 2026-07-05: Pages has no custom domain and workflow-type Pages deploys
# ignore CNAME files anyway — listed here so an in-flight render on pre-removal
# code can't re-commit it (git add skips ignored files).
site/CNAME
site/_tmp/
# short-link lane (engine/marketing/links.py, D07 W1a): orphaned atomic-write
# temps possible only after a hard crash mid-write; pages themselves ARE committed.
site/go/**/.tmp_*
# no trailing slash: a trailing-slash pattern matches DIRECTORIES ONLY, so a
# SYMLINK at this path is not ignored — which is how a machine-local absolute-path
# symlink reached a commit (a crop harness links the nightly artifacts in from the
# main checkout, an aborted run left it behind, and a broad `git add` swept it up).
site/stockdata
site/ohlc/
site/stockbrief/
site/chinastockdata/
site/chinamechanicsdata/
site/hkstockdata/
site/canadastockdata/
site/intlstockdata/
site/chinaohlc/
site/canadaohlc/
site/intlohlc/
site/hkohlc/
site/intraday/
# machine-consumable contract plane (scripts/build_feeds.py) — R2-only by design,
# regenerated every daily run; consumers read the R2 _manifest.json, never git
site/feeds/
# live intraday overlay — runtime artifact written by scripts/build_live_overlay (intraday
# cron) and by the offline test path; never committed (the cron force-pushes / re-emits it).
site/live/
# NOTE: site/research/ is deliberately NOT ignored. #3507 ignored it as "a runtime
# plane like site/live/", but the estate is 87 TRACKED files (#3501/#3512) that reach
# the edge by being committed. Ignoring a tracked tree is the worst of both: `git add
# site/` still stages edits to existing pages and silently skips every NEW report page,
# freezing the estate while research-ingest keeps growing catalog.json. If this ever
# does need ignoring, render.yml must force-add it (as it does site/stockbrief).
# Polygon hourly bars for the 4H chart timeframe — regenerable + daily-changing, so
# cached cross-job in CI (actions/cache) rather than committed; site/intraday is the
# force-tracked, shipped artifact.
data/intraday/
# D14 press staging (scripts/run_press.py --staging) — per-run drafts + validator
# reports. Run artifacts, not repo content: press-publish.yml uploads them as a
# workflow artifact and --emit consumes them, and its commit step never stages
# this path. Committing them would put unpublished LLM drafts on main. The
# LEDGER (data/press/published.jsonl) is the opposite — append-only, committed,
# and the only durable record of what shipped.
data/press/staging/*.json
# Financial-news fetch cache (engine/financial_news) — transient 12h fetch cache,
# regenerated every build. NOT committed: a keyless local cache must never shadow
# CI's keyed (Polygon/Finnhub) fetch within the TTL window.
data/financial_news/
# GDELT cross-process throttle stamp (runtime-only; written by engine/gdelt_client._throttle)
data/gdelt/
# GR3b: 8-K primary-doc + material-contract-exhibit text cache (collectors/edgar_8k).
# Transient fetch cache that makes the counterparty backfill resumable; the extraction
# RESULT is committed in material_8k_events.parquet, the raw filing text never is (it is
# hundreds of MB of public EDGAR prose that re-fetches on demand).
data/edgar/_8k_doc_cache/
data/russell_breadth/_closes_cache.parquet
data/china_breadth/_closes_cache.parquet
data/hk_breadth/_closes_cache.parquet
data/canada_breadth/_closes_cache.parquet

# qledger control-evidence clock starts are WRITE-ONCE registrar receipts written
# on the nightly checkout at a real registration (engine/qledger.py C3.1/C3.4,
# record_control_clock_start). They live on the runner's disk, NEVER in git — a
# committed clock file is the retrospective-stamping vector that
# tests/test_qledger_control_policy.py::test_t9_no_clock_file_is_ever_committed
# forbids. 2026-08-19: the regime-update lane's add-sweep committed
# demand_chain.json (13750ecd1789) and redded qledger-cluster-honest-ci on every
# merge-ref fleet-wide; this rule keeps the sweep from ever re-committing one.
data/qledger/control_evidence_clock_start/
# TOP ANATOMY W1: the Winner Health trailing panel — ~80 MB of repaired bars,
# rebuilt from massive_stock_day on every run and keyed on each source file's
# (mtime, size). Pure derived speed state: deleting it costs one cold rebuild and
# nothing else, so it never belongs in the tree. The ARTIFACTS it feeds
# (data/top_maturation/latest.json, data/top_maturation_log/*.jsonl) ARE tracked.
data/top_maturation/_panel_cache/
# R5: factor panel monthly partitions (off-render-path nightly artifact; R2-only by design,
# regenerated every daily run; consumers join via (ticker,date) against the replay parquet).
data/factordata/panel/
data/stock_personality/panel/
# hk_shorts: positions.parquet + turnover.parquet + coverage.json are GIT-tracked
# (NOT R2) because they are not regenerable without re-scraping and are critical
# evidence for the H2a phase-0 test.  Only the _closes_cache-equivalent rebuild
# artifacts are excluded (none exist for this store).
# NOTE: data/hk_shorts/ is intentionally NOT listed here — all files are committed.
# hk_placements (H-PLC, W1c): events.parquet + coverage.json are GIT-tracked (NOT R2)
# — tiny append-only event store (~1.5k rows/yr), evidence for the post-placement
# drift accrual. data/hk_placements/ intentionally NOT listed here.
# data-quality audit revision baselines: a daily-changing full copy of the macro series
# (machine state for backwards-revision detection), cached cross-run in CI rather than
# committed. The audit JSON reports under data/quality/*.json ARE committed (trend artifact).
data/quality/baselines/
# Orphaned atomic-write temps under the committed data plane (same class as
# site/go/**/.tmp_* above). Every writer here publishes through a unique temp in
# the target's own directory and unlinks it on failure, so one can only survive a
# hard kill mid-write — and the nightly's `git add data/` would then commit it
# forever. The real artifacts never end in .tmp.
data/**/*.tmp
# w2051 housing HF: runtime data stores (R2/data-plane pattern, not git-committed)
data/redfin_hf/
data/zori/*
!data/zori/national.parquet
!data/zori/_fetched.json
# Leading-leg validation caches (regenerated by scripts/validate_*.py): the EDGAR 13D/13G
# quarterly backfill (~7MB) + the yfinance price panels (~14MB). Only the small gate JSONs
# + reports are committed; these heavy caches are never.
data/beneficial_ownership/_backfill/
data/index_reconstitution/prices.parquet
data/index_reconstitution/prices_attempted.json
data/index_reconstitution/ohlc.parquet
data/index_reconstitution/ohlc_attempted.json
# IPO Radar ticker→CIK map cache (regenerable from SEC company_tickers.json; ~170KB)
data/ipo/ticker_cik.json
# FINRA short-interest VINTAGE PANEL (~120MB, 206 settlements x ~20k rows, 2018->now).
# Fully regenerable in ~20 min from FINRA's public API — `python -m
# scripts.backfill_finra_short_interest` — so the panel itself stays Mac-local while its
# vintage-stamped coverage JSON IS committed as the reviewable receipt.
# CONTRAST: data/ibkr_borrow/daily/ is deliberately NOT listed here. Those captures are
# snapshot-only with no backfill path anywhere, so they are committed; losing them loses
# the accrual permanently.
data/finra/short_interest_panel.parquet
# Derived insider-panel concat (40MB) — regenerated by build_factors_page from the
# committed data/sec_insider/panel/ per-quarter cache; never commit the rebuild.

# LLM digest cache (regenerable; never commit — would ship a stale-floor digest)
data/catalyst/digest_cache/
data/catalyst/stockbrief_cache/

# master_brain same-day LLM reply cache (engine/master_brain.py _mb_reply_cache_*):
# prompt hash changes daily with state, so entries are dead weight after one day.
# Written & read only inside the nightly ENGINE job (one machine); engine-render
# deliberately skips the LLM brains — no cross-machine reader. 410 of the 411
# once-committed files were pytest junk swept in pre-#3097 (hermetic-cache
# fixture); untracked 2026-07-20 so the blanket `git add data/` stops re-growing it.
data/master_brain/reply_cache/

# Event-calendar fetch caches (regenerable; keyless TreasuryDirect + keyed FRED
# release/dates). Date-stamped & ephemeral — never commit.
data/macro/news_cache/
data/macro/auction_cache/
data/macro/release_cache/

# China macro-news build-time flash cache (engine/china_news.py — Eastmoney
# 全球财经快讯). Date-stamped & ephemeral — never commit. (The CCTV 新闻联播
# policy-tone series data/china_news/cctv_tone.parquet IS versioned.)
data/china_news/flash_cache/
# CCTV 新闻联播 raw archive shards (W4 backfill). Projected ≈43MB total — under
# the 150MB commit threshold, but shards are gitignored because the long backfill
# scrape outlives this PR. A single follow-up commit will land all shards once the
# scrape completes. The derived tone history (cctv_tone_history.parquet) IS versioned.
data/china_news/cctv_archive/*.parquet

# News-vector GDELT fetch cache (regenerable, date-stamped). The append-only PIT
# event store data/news_vector/events.parquet is NOT ignored — it must persist &
# accrue forward vintages (like data/prediction_markets/snapshots.parquet).
data/news_vector/fetch_cache/

# Large local-only research/build caches (regenerable; kept out of the deploy repo
# so the daily CI `git add data/` never re-commits & grows them). Not on origin/main.
data/breadth/_closes_deep.parquet
data/breadth/_closes_delisted.parquet
data/breadth/_closes_delisted_1500.parquet
data/breadth/sp500_pit_membership.parquet
data/china_search/closes_deep.parquet
data/sec_insider/insider_panel.parquet
# deep-history close panel for the deep factor IC scorecard (offline backfill via
# scripts.sue_deep_phase0; the small deep ic_scorecard.json IS committed, the 54MB panel is not).
data/edgar/sue_deep_closes.parquet

# PR-5 (L1 short-side): BD Phase-0 event tape — Mac-local, regenerable from massive_stock_day.
# Summary JSON (breakdown_events_summary.json) IS committed (RUL-P10 path b).
data/research/breakdown_events.parquet

# TOPA phase-0 (top-anatomy): panel/feature caches + episode tape — Mac-local,
# regenerable from massive_stock_day + price-ladder rungs (family top_anatomy_p0).
# The small vintage-stamped summary JSON (top_anatomy_p0_summary.json) IS committed.
data/research/top_anatomy_p0/

# Admin dashboard: local-only secrets / GA4 service-account keys (never commit)
admin/.env
admin/secrets/
admin/*service-account*.json
admin/*-sa.json

# dev-only fast-render view-model cache (scripts/render_macro_fast.py)
data/_dev_macro_vm.pkl
data/_dev_china_vm.pkl

# Signal track-record log — un-ignored (w0-stageB #1139): maturation-fill semantics
# make row-level JSONL append-only awkward; parquet append-only is repo practice.
# Size tripwire in scripts/audit_sizes.py guards against runaway growth.
# data/signal_archive/track_record.parquet  <-- intentionally tracked

# Signal track-record audit scratch output (reader writes this; git-ignored)
research/signal_engine/_track_record_audit.md
data/edgar/company_tickers.json
data/fundamental_forensics/raw/
data/fundamental_forensics/archive/
data/fundamental_forensics/private/
# Per-run SEC observation log — cron history, deliberately outside the
# restorable raw/archive source trees (DNR:LAW-RUN-CLOCK-IN-CONTENT-IDENTITY, R6)
data/fundamental_forensics/observations/

# Special Situations: fetched filing-text cache + LLM summary cache (regenerable)
data/special_situations/doc_cache/
data/special_situations/digest_cache/
# Capital Structure Intelligence: original SEC bytes live in R2 or an explicit
# local evidence store. Compact manifests/derived ledgers remain tracked.
data/capital_structure/source_objects/
# Interrupted collector/compiler staging is never an artifact. The compiler
# stages outside data and promotes telemetry last; the collector uses these
# same-directory parquet temps for one-file atomic replacement.
data/capital_structure/*.tmp.parquet
.capital-structure-compile-*/
# raw digest capture / reverse-engineering evidence (private; digest_db.parquet is the committed artifact)
research/_recon_raw/

# live intraday overlay output (regenerated each intraday run; not a committed artifact)
site/live/

# options-flow transient caches (re-downloadable; keep out of git)
# Options Alpha §A8: data/massive_flat/ and data/massive_flatfiles/ are dead stray dirs; do not write to them.
data/massive_flat/
data/massive_flatfiles/
data/options_flow/_*.parquet
# heavy per-ticker stores — served from Cloudflare R2 (scripts/publish_r2.py), not committed/deployed
site/subsectorohlc/
site/subsectorohlc_china/
site/subsectorohlc_russell/
site/subsectorohlc_nasdaq/

# A/B risk-gate per-day divergence dump (~3MB, 13k+ rows) — the committed summary
# (calibration/ab_risk_gate.json) carries the stats + the event-window replay rows.
calibration/ab_risk_gate_divergence.jsonl

# Expanded HSCI universe deep OHLCV store (R2-only, ~65 MB initial and growing).
# Per-ticker parquets served from Cloudflare R2 via scripts/publish_r2 --dirs hk_stocks_ext.
# Committed files in the same dir (_universe.json, _checkpoint.json) are NOT ignored.
data/hk_stocks_ext/*.parquet

# SLF-048 Wikipedia attention pageview store (R2-only since 2026-07-06).
# Deep history 2015-07→ (~30 MB, rewrites whole per-ticker parquets daily — repo
# bloat if tracked). Nightly loop: fetch_r2 restore -> wiki_pageviews upsert ->
# publish_r2 --dirs attention. Recover locally: python -m scripts.fetch_r2 --dirs attention
data/attention/*.parquet

# Per-ticker Google Trends attention parquets (R2-class local store; not tracked).
data/google_trends/

# Price Pressure (DRL W1) event ledger — R2-canonical since 2026-08-11.
# ~10.8 MB REWRITTEN WHOLE every successful night (the advance re-grades every open
# row, and the §10.1 completion pass rewrites stamps), so tracking it costs ~4 GB/yr
# of binary git churn. Nightly loop mirrors attention: fetch_r2 restore ->
# build_price_pressure -> outcome-gated publish_r2 (daily.yml, price-pressure step).
# Recover locally: python -m scripts.fetch_r2 --dirs price_pressure
# EXACT path on purpose — the sidecars in the same dir stay TRACKED:
#   latest.json              (display artifact AND the tracked truth the builder's
#                             freshness gate compares the restored parquet against)
#   base_rates.json          (frozen backfill measurement)
#   completion_receipts.jsonl (immutable §10.1 stamp receipts)
data/price_pressure/events.parquet

# Massive.com whole-market daily OHLCV derived store (R2-only; Setup-Species §7 W0.6a).
# Per-ticker parquets published via scripts/publish_r2 --dirs massive_stock_day.
# Committed: data/massive_stock_day/_manifest.json (freshness anchor for audit_r2)
#            data/massive_stock_day/_backfill_state.json (resume state)
# ~12,400 tickers × ~350 rows = ~240 MB total — must NOT be committed to git.
data/massive_stock_day/*.parquet
# Mixed-case vendor identities use a hex-keyed subdirectory so default APFS cannot
# fold them into the legacy uppercase filename (lib.massive_ticker).
data/massive_stock_day/__case_v1/*.parquet
# Options Alpha §A4/A8: raw Massive.com options day-agg cache.
# Local cache + R2-published via publish_r2 --dirs massive_options_day.
# NEVER committed to git; content is re-downloadable from massive.com entitlement.
# See research/OPTIONS_ALPHA_MASTERPLAN.md §3 rulings A4 and A8.
data/massive_options_day/
# ThetaData Phase-A T1 backfill store (rulings A4/A8 + §7.2 of LIVE_ORDER_FLOW_BRAINSTORM).
# ~30–80 GB raw EOD chains + OI + Greeks; published to R2 via publish_r2 --dirs thetadata_eod.
# NEVER committed to git; re-downloadable from ThetaData entitlement.
# Committed (git-tracked): _backfill_state.json (resume state) + _manifest.json (R2 anchor).
data/thetadata_eod/eod/
data/thetadata_eod/oi/
data/thetadata_eod/greeks/
# Negation: keep the committed state + manifest files tracked even though the data subdirs above are excluded
!data/thetadata_eod/_backfill_state.json
!data/thetadata_eod/_manifest.json
# AD-1T1 §B/F15: the crash-safe advisory writer flock — local coordination
# state only, never committed and never uploaded to R2 (see publish_r2._uploadable).
data/thetadata_eod/_writer.lock
# (NOT R2) — not regenerable without re-scraping; critical H2a phase-0 evidence.
# data/hk_shorts/ is intentionally NOT listed here; all files are committed.
# ---- Slice G store decisions (2026-07-03) ------------------------------------
# CIRO short-position panel: data/ciro_short/ is GIT-tracked (not R2).
# ~114 report-dates x ~2,500 TSX rows = ~285k rows = ~14MB -- inside git threshold.
# positions.parquet and coverage.json are intentionally committed so the CSPR
# history (2021-10->) accrues in the repo. data/ciro_short/ is NOT gitignored.
#
# Canada insider -- new append-only store vs legacy blob:
#   data/canada_insider/transactions.parquet -- GIT-tracked (new; accumulates history)
#   data/canada_insider/insider.parquet      -- GIT-tracked (legacy; kept 1 month for
#                                              migration safety, phase out 2026-08-03)
# Both are small (<5MB), non-regenerable, git-tracked. Not gitignored.

# ThetaData T2a signed-flow features: data/tape_flow/daily/ is GIT-tracked (NOT R2).
# Storage math: ~400 roots × 252 sessions/yr × 3y × ~80 bytes/row (parquet compressed)
# ≈ 24 MB total — well under the ~30 MB git threshold.
# Per-name parquets are append-only feature stores (aggregate-then-discard; no raw rows).
# Revisit if universe exceeds 800 names or backfill extends to 12y (would reach ~193 MB → R2).
# data/tape_flow/daily/ is intentionally NOT gitignored; all per-root parquets are committed.
#
# data/tape_flow/raw/ contains FULL tape bytes (ETF anchors + episode windows) — R2-plane only.
# Only the manifest (_manifest.json) is committed; raw parquets are NEVER committed.
data/tape_flow/raw/*.parquet
# state file is tiny/git-tracked (resumable backfill):
!data/tape_flow/_state.json
!data/tape_flow/raw/_manifest.json

# ObsidianBrain vault (own git repo; view over memory/ + research/, lives inside this folder)
ObsidianBrain/

# Oracle rotation panel parquets (O0 — built off-render on Mac, published to R2).
# manifest.json MAY be committed in a later PR once it stabilises; for now it is
# also excluded to avoid stale snapshots in git.
# Mirror pattern: data/massive_stock_day/*.parquet above.
data/oracle/*.parquet
# RUL-P10 explicit entries: panel_s/m must never ride a blanket 'git add data/'
# (45 MB total — R2 only, served via scripts/publish_oracle_panels.py).
# Already covered by the glob above; explicit lines guard against glob removal.
data/oracle/panel_s.parquet
data/oracle/panel_m.parquet
data/oracle/manifest.json
# Oracle graph outputs (O1) — heavy JSON artifacts; published to R2 per D5
data/oracle/graph_s.json
data/oracle/graph_m.json
# Oracle P3 gauntlet outputs — results MD is committed; p3_results.json is ALSO committed
# (as of PR-A1 feat/oracle-onset-baselines): it carries the adjudicated episode baselines
# required by the sentinel decay monitor. The negation below overrides the gitignore.
data/oracle/gauntlet/p3_results.json
!data/oracle/gauntlet/p3_results.json
# p3_trial_ledger.json IS committed: it is the registration record and must be in git.
# The negation below overrides the gitignore for that specific file only.
data/oracle/gauntlet/p3_trial_ledger.json
!data/oracle/gauntlet/p3_trial_ledger.json
# Oracle P8 gauntlet outputs — JSON artifacts gitignored; results MD is committed
data/oracle/gauntlet/p8_results.json
data/oracle/gauntlet/p8_trial_ledger.json
# Oracle B1 personality output — heavy local artifact, mirrors panel_*.parquet pattern
data/oracle/personality.json
# Oracle Research Factory (W-B1/B3) — governance + ledger files ARE committed.
# registry.jsonl is the compound spec registry (small governance data).
# trial_ledger.jsonl and live_ledger.jsonl are append-only result ledgers.
# promotion_queue.json is the Fable-adjudicated promotion candidate list.
!data/oracle/compounds/registry.jsonl
!data/oracle/compounds/trial_ledger.jsonl
!data/oracle/compounds/live_ledger.jsonl
!data/oracle/promotion_queue.json
data/oracle/memory_active_analogues.json
# P9 Hypothesis Inbox — append-only governance data, committed per constitution.
# hypothesis_state.json is also governance data (tracks graded ids + sentinel line count).
!data/oracle/hypothesis_inbox.jsonl
!data/oracle/hypothesis_state.json

# Research Factory W1 (RF-8): broad exclude for bulk/replay artifacts; explicit
# negations for the eight durable git-tracked ledgers, directories, and the allowlist.
# W5 additions: track/ (RF-9 per-candidate clock skeletons) and requeue.jsonl (RF-10 audit trail).
data/research_factory/*
!data/research_factory/candidates.jsonl
!data/research_factory/transitions.jsonl
!data/research_factory/challenges/
!data/research_factory/review/
!data/research_factory/health.jsonl
!data/research_factory/paper_monitor.jsonl
!data/research_factory/authority_allowlist.json
!data/research_factory/track/
!data/research_factory/requeue.jsonl

# Live options-flow poller runtime artifacts (regenerated each session)
# live_flow_baselines/baselines.json is git-TRACKED (small, ~50 roots × 4 fields)
data/live_flow_state/
data/live_flow_out/

# U-CHAIN chain-snapshot lane output (~0.4-1 GB/day forward; never committed).
# No trailing slash: the dedicated M1 deploy clone carries this path as a
# symlink to the preserved physical authority in flow-ops-wt.
data/chain_snapshots
data/options_structure_intraday_r2/

# W0.2 OHLC store (reproducible from collector + manifest; not committed)
data/yahoo_ohlc/
research/oracle_asymmetry/W2_member_trades.csv

# NW Rails R1 (PR-1, RUL-P10) — Mac-local per-fire parquet files.
# The small summary JSONs at data/rule_experiments/results/*_summary.json ARE committed
# (git-committed, single-writer: scripts/run_rule_replay.py).
# The large per-fire parquet files are Mac-local only (never staged by nightly blanket
# git add data/).
data/rule_experiments/results/*_perfire.parquet

# NW Rails R1 (PR-1, RUL-P10 retroactive) — replay parquet files are Mac-local/server-local
# only (not gitignored previously; made explicit per RUL-P10 to prevent accidental staging
# by the nightly blanket git add data/).
data/replay/*.parquet
# NW Rails PR-8 / RUL-P10 (path a): operator action ledger.
# Admin server is the single intraday writer; MUST NOT be staged by the nightly
# blanket `git add data/` sweep. Off-band backup rides the admin host's path.
data/operator/
# Mastermind Feedback Contract PR-A (FB-R12): reserved private plane for any
# future Mastermind raw-feedback mirror.  MUST NOT be staged by the nightly
# blanket `git add data/` sweep.  No writer exists in v1 — the rail is
# pre-built so the path is safe before any data ever lands here.
data/private/

# R6.2 Options Issue Desk append-only operator records. Production defaults to
# /var/lib/macro-api/options_issue_desk, outside the repository; this narrow
# containment wall also protects the registered local-development path from a
# broad git add. These rows can contain private research positions and reviews.
/runtime-private/options_issue_desk/

# Paid TuShare add-on response partitions are private, non-redistributable pilot
# data. Never stage them through the nightly blanket `git add data/` sweep.
# Same rule for the minute plane (Lane B) and chip-distribution store (Lane C).
data/tushare_addons/
data/tushare_minutes/
data/china_chips_distribution/

# SLF-001 SEC FTD collector (feat/slf001-sec-ftd-collector-phase0).
# panel.parquet is 184MB / 22.4M rows — R2 data-plane law: must NOT enter git.
# Mirrors pattern: data/massive_stock_day/*.parquet above.
# The store dir (data/sec_ftd/) itself is a real tracked dir; parquets are excluded.
# _manifest.json (freshness anchor, if created) is kept via negation.
data/sec_ftd/*.parquet

# chain-heat smoke output (--no-publish writes to CWD = repo root; must not enter git).
# Documented smoke command in ops/launchd/com.macro.chainheat.plist and module docstring.
chain_heat_current.json
!data/sec_ftd/_manifest.json

# W5-B Options Tape Signed Pilot (feat/w5b-options-tape-pilot).
# Per-ticker parquets are R2-plane only (projected 10-50 GB at 500 names x full backfill).
# Mirrors pattern: data/massive_stock_day/*.parquet above.
# _backfill_state.json is NOT git-tracked (it lives under data/ which is gitignored).
# On a fresh checkout the state file is absent; the build detects this and starts from scratch,
# pulling the full date range for any underlyings not yet in completed_dates.
# The *.parquet ignore already permits the JSON (only *.parquet is blocked), so
# git-adding the state file is possible if persistence across checkouts is needed;
# currently it is not added — nightly CI always runs on the host machine, not a fresh checkout.
data/options_tape_signed/*.parquet

# D2 comment-letter release phase-0: EDGAR quarter indexes + computed event returns.
# These are generated artifacts (reproducible from EDGAR fetch) — never staged.
# The throwaway ledger is also worktree-local, not the canonical tracked ledger.
data/comment_letter_events/
data/trial_ledger_d2_local.jsonl

# A10 block-tape archiver (feat/f501-cn-block-tape-archiver).
# ~12MB, 38 parquets — R2 data-plane law: must NOT enter git.
# Yearly mrtj_YYYY.parquet + mrmx_YYYY.parquet + sw_l1_map.parquet +
# sw_l1_constituents.parquet are Mac-local/server-local only.
data/china_block_tape/

# TIL W7 DOL foreign-labor-certification store (PR-W7).
# Quarterly xlsx files are 50-80MB (LCA) / 1-30MB (PERM) — never committed.
# Compact normalized store (certs.parquet) is runner-local only; not in git.
# Env override: DOL_CERTS_STORE=/path/to/certs.parquet
# The data velocity artifact (hiring_velocity.json) is also runner-local.
# Only the site projection (site/basketdata/hiring_intent.json) is git-tracked.
data/dol_certs/

# TIL W9 EDGAR phrase-velocity runner-local parquet store.
# Env override: EDGAR_PHRASE_VELOCITY_STORE (DIRECTORY semantics — DOL_CERTS_STORE convention).
# Only the site projections (site/basketdata/phrase_velocity.json,
# site/basketdata/github_adoption.json, data/neuralweb/discovery_confluence.json) are git-tracked.
data/edgar_phrase_velocity/

# Odds Desk (research/ODDS_DESK.md) — heavy per-ticker stores, R2-plane only.
# site/oddsmatrix/<T>.json factor matrices are served from Cloudflare R2 via
# scripts/publish_r2 --dirs oddsmatrix, never committed/deployed. The small
# site/oddsdata/*.json artifacts (catalog.json, factor_match.json) stay git-tracked
# like other sitedata — intentionally NOT listed here.
site/oddsmatrix/
# data/odds_ohlcv/<T>.parquet OHLCV backfill store (yfinance deep history,
# auto-adjusted) — R2-backed via publish_r2 --dirs odds_ohlcv; runner-local
# between runs. Mirrors pattern: data/massive_stock_day/*.parquet above.
# The negation keeps the small _meta.json manifest tracked.
data/odds_ohlcv/*.parquet
!data/odds_ohlcv/_meta.json

# Stock seasonality calendar clock — heavy per-symbol year panels, R2-plane only.
# site/seasonalitydata/entities/<SYM>.json carries up to 25 complete-year
# cumulative paths at 365 slots, twice (raw + market-neutral), so a covered
# universe of ~290 names is ~28 MB rewritten every trading night. Served from
# Cloudflare R2 via `scripts/publish_r2 --dirs seasonalitydata/entities`, exactly
# like site/oddsmatrix/ above; the page fetches
# (window.DATA_BASE||'') + 'seasonalitydata/entities/<SYM>.json'.
# The small site/seasonalitydata/{index,methodology}.json stay git-tracked like
# other sitedata — intentionally NOT listed here.
# The negation keeps the DEFAULT symbol committed so the page has an honest
# server-rendered first paint (and something indexable) without R2.
site/seasonalitydata/entities/*.json
!site/seasonalitydata/entities/SPY.json

# FS-4 flow-score model artifacts (GITIGNORED per A4 — versioned joblib artifacts
# never committed; served from Cloudflare R2 and local models_dir in config/flow_score.yml).
# data/flow_signals/scores.parquet IS committed (like grades.parquet — covered by
# the nightly `git add data/` commit glob; see .github/workflows/daily.yml).
data/flow_signals/models/
# CXI-1: derived context index DB (never committed)
.context-index/

# SGA W4 — earnings-call qualitative scores (rulings SGA-R5/R6).
# Produced OFF the render path by the Windows-PC Qwen worker (and cloud-fallback
# on the Mac), transported via R2 (scripts/publish_earnings_r2.py /
# fetch_earnings_scores.py), joined into Stage Analysis at nightly time. NEVER
# git-committed — the worker is producer-only (SGA-R6).
# TRAP (masterplan §7): data/earnings/ is the CALENDAR store (committed parquet)
# and must NOT be ignored — only data/earnings_calls/ is the R2-transported lane.
data/earnings_calls/
# SGA W5: backfill_analysis.parquet is large (gitignored); equitydesk_overview.parquet
# is the COMMITTED yardstick (negation rule keeps it tracked).
data/earnings_calls/backfill_analysis.parquet
# data/stage_analysis/ is NOT ignored globally; only the large derived backfill artifact:
# (equitydesk_overview.parquet is small enough to commit and is the calibration yardstick)
# No negation needed — data/stage_analysis/ is not in a broader ignore pattern above.

# D01 Marketing fast lane — daemon-local intraday state (never committed).
# Nightly is the sole advancer of forward ledgers; the daemon is an intraday lane.
data/marketing/fastlane_seen.jsonl
data/marketing/fastlane_quarantine.jsonl
data/marketing/fastlane_heartbeat.txt
# The outbox itself IS committed since W1 (#3288 nightly emit + marketing-publish
# ledger commit-back both `git add data/marketing/outbox` — a blanket ignore here
# silently no-ops both and the queue evaporates on the next runner clean).
# Only the transient advisory lock stays local.
data/marketing/outbox/.lock
data/options_signal_episode/checkpoint.json.lock
data/options_signal_episode/.checkpoint.json.*.tmp
# XG-W2 daemon spool. The fast lanes (press, earnings) now emit through the
# canonical outbox path, and they run inside scripts/marketing_fastlane_daemon.py
# on the VPS — whose checkout is refreshed by a 3-minute `git pull`. Appending to
# the TRACKED items.jsonl there would dirty the tree and conflict that pull, so
# daemon-side emissions go to this gitignored sibling instead (outbox.enqueue
# spool=True). Local guards read the UNION of both files, so the dedup and
# near-dup radars still see every emission this host made.
# LOUDNESS: this pattern is items-host-SCOPED. It must NOT swallow items.jsonl —
# verify with `git check-ignore data/marketing/outbox/items-host.jsonl` (ignored)
# vs `.../items.jsonl` (NOT ignored) after any edit here.
data/marketing/outbox/items-host.jsonl
# XG-W3 persona-memory host spool. Same posture, same reason: emission-time
# counters (phrase fatigue, open promises, relationship touches) accumulate
# intraday on whichever host emitted, and the nightly consolidator
# (engine/marketing/persona_memory.consolidate) is the SOLE advancer of the
# tracked ledgers under data/marketing/personas/ — the nightly-sole-advancer
# law, charter §2 amendment 13. Readers see the union of both trees, so a cap
# still counts today's un-consolidated posts.
# LOUDNESS: this pattern is personas_HOST-scoped. It must NOT swallow the
# tracked ledgers — verify with
#   git check-ignore data/marketing/personas_host/cici/phrases.jsonl   (ignored)
#   git check-ignore data/marketing/personas/cici/phrases.jsonl        (NOT ignored)
# after any edit here.
data/marketing/personas_host/
# XG-W6 learning host spool. Third instance of the same posture: the reply
# producer banks draft-time label observations (parent engagement, post age,
# thread saturation — readings that are UNRECOVERABLE once the thread moves on)
# at tick cadence on whichever host produced, and the nightly consolidator
# (engine/marketing/labels.consolidate) is the SOLE advancer of the tracked
# ledgers under data/marketing/learning/. Readers see the union of both trees.
# LOUDNESS: this pattern is learning_HOST-scoped. It must NOT swallow the
# tracked ledgers — verify with
#   git check-ignore data/marketing/learning_host/labels.jsonl  (ignored)
#   git check-ignore data/marketing/learning/labels.jsonl       (NOT ignored)
# after any edit here.
data/marketing/learning_host/
# D02 chart-image pipeline: chart *PNGs* are R2-DELIVERED BY DESIGN. The PNG's sole
# delivery plane is its public R2 URL (media_url on the item); the local file is a
# throwaway render input, never a delivery artifact. The recursive `git add
# data/marketing/outbox` in daily.yml + marketing-publish.yml would otherwise commit
# ~0.6–1.8 MB of binaries per night forever. So ignore ONLY the PNGs — the .svg
# chart snapshots STAY COMMITTED (the admin console previews render from SVG).
# LOUDNESS (see #3326 postmortem: a stale ignore once silently no-oped a needed
# git add): this pattern is deliberately PNG-scoped — it must NOT swallow .svg or
# the JSON queue. Verify with `git check-ignore data/marketing/outbox/media/X/Y.png`
# (ignored) vs `.../Y.svg` (NOT ignored) after any edit here.
data/marketing/outbox/media/**/*.png
# HOT-TAPE intraday renders (2026-07-30). The rule above deliberately keeps
# chart SVGs committed because the admin console previews render from them --
# and that was correct when a night produced ~29 of them. scripts/hot_tape_radar.py
# then armed the P1 radar, which renders a card for EVERY candidate it evaluates
# (before it knows whether the post will ship) on every intraday sweep: 8,068
# hottape-*.svg landed in ONE day, all of them tracked, 420 MB in the media tree.
# That is what has been printing "too many unreachable loose objects" on every
# git command here.
#
# Same argument the PNG rule already makes: the delivery plane is the public R2
# URL on the item, the local file is a throwaway render input. The admin console
# previews the NIGHTLY chart-NNN.svg snapshots, which stay committed -- this
# pattern is deliberately hottape-scoped and must NOT swallow those. Verify with
#   git check-ignore data/marketing/outbox/media/X/hottape-a-b-0000Z.svg (ignored)
#   git check-ignore data/marketing/outbox/media/X/chart-001.svg          (NOT ignored)
# after any edit here.
data/marketing/outbox/media/**/hottape-*.svg
# Publish-workflow-local fetch of the live-data branch quotes (tape gate input);
# ephemeral per-run, never committed.
data/marketing/live_quotes_snapshot.json
# D05 breaking-feed local-only poller state (seen ledger + ETag state);
# the nightly blanket "git add data/" must never stage these.
data/marketing/breaking/
# PRESS-FEEDS B1 (D05 Addendum 2) press-lane local-only poller state — provider
# cursors, conditional-GET ETags, twitterapi.io spend accounting, flagship
# counter, corroboration window, seen-ledger. The poller writes ONLY here and
# makes ZERO git writes; the nightly blanket "git add data/" must never stage it.
data/marketing/press/

# SGA (Stage Analysis v2): raw EquityDesk backfill seeds (~55MB incl. the 34MB
# earnings_calls.parquet) violate the <1MB-in-git rule — one-time competitor backfill
# kept LOCAL + archived at ~/Documents/Cluade/equitydesk_backfill/. Engines calibrate/
# regenerate from them locally; on CI/deploy they are absent and every consumer
# fails-open to the committed sub-1.3MB display JSON (data/stage_analysis/*.json).
data/stage_analysis/backfill/
# Forward ledger: nightly is the SOLE advancer (house law) — never commit from a worktree.
data/stage_analysis/forward_ledger.jsonl

# Prophet × Stage forward-shadow raw ledger + correction/grade revisions are tracked.
# Nightly remains the sole grade advancer; a narrow guarded workflow checkpoint is the
# sole publisher. Tracking is required because Actions checkout cleans ignored files,
# which previously erased the forward history at the start of every run.

# Mastermind AI response log — ingested corpus of every user-facing answer (Macro
# brain + Terminal copilot) + the operator's eval sidecar. Holds USER QUESTIONS AND
# ANSWERS: admin-local ONLY, never committed. The admin panel ingests it from R2 (the
# data plane) on demand via admin/mastermind_logs.refresh(); nothing in git reads it.
data/mastermind/response_log.jsonl
data/mastermind/response_eval.jsonl
# Weekly answer-quality eval summary (scripts/run_brain_eval.py, W2 harness). Derived
# from the corpus above, so it inherits the same rule — and it carries LLM-judge scores,
# which are INTERNAL QA TELEMETRY ONLY and must never reach a public artifact or a
# committed file the site could read. The archive copy lives on R2 under
# mastermind_response_evals/summary/<ISO-year>-W<week>.json (private data plane).
data/mastermind/eval_summary_latest.json
data/mastermind/*.json.tmp

# Authenticated share-count publication is an R2 data plane. The local generation,
# receipt, pointer, pending, recovery, and lease files are crash-recovery mirrors for
# the current runner only. The nightly workflow broadly stages data/, so this rule is
# load-bearing: cumulative ledgers must never become Git objects or public site data.
data/capital_structure/share_counts/

# Universal 13F evidence is an immutable R2/warehouse data plane, not a Git data
# plane.  Keep only bounded public summaries and small provenance receipts under
# data/institutional_13f/public and data/institutional_13f/receipts.  Raw SEC
# packages, accession documents, SQLite work files, and Parquet generations can
# reach tens of millions of rows and must never be swept up by broad `git add data`.
data/institutional_13f/*
!data/institutional_13f/public/
!data/institutional_13f/public/*.json
!data/institutional_13f/receipts/
!data/institutional_13f/receipts/*.json

# Earnings Wire member continuations and exact-evidence context belong to the
# private Research Vault object store.  The builder now refuses repository-local
# private output, and this exact legacy path is ignored as a second containment
# wall so an old local render can never be swept into the public Git history.
/site/premiumdata/earnings/

# Agent worktree roots. Parallel Claude/Codex/Cursor/Grok/Warp sessions create
# linked git worktrees under these paths; they are checkouts, never content, and
# must never be committed.
# `.claude/worktrees/` has historically been excluded per-clone via .git/info/exclude,
# which every fresh clone and every new session has to rediscover — and the codex root
# had no rule at all, so it surfaced as an untracked directory in every session's
# `git status` and tripped the ship-loop guard on work belonging to another session.
# 2026-08-11: the rule above shipped covering only two of the four roots, so
# `.claire/worktrees/` (3 checkouts) and `.codex/worktrees/` (55) still surfaced as
# untracked directories in every session's `git status` — the exact symptom the note
# above says it was fixing. 2026-08-18: `.cursor/worktrees/` and `.grok/worktrees/`
# were already session-root markers in scripts/worktree_sparse.py but missing from
# this triad, the same silent-miss class. 2026-08-22: `.warp/worktrees/` is the
# Warp/Oz mint root — Warp has no SessionStart event, so sessions mint there via
# `.warp/hooks/session_start_sparse.py` and the project skill. The same set is
# duplicated in `config/worktree_gc.json` and `ship_loop_guard.AGENT_WORKTREE_ROOTS`;
# `tests/test_agent_worktree_roots.py` pins all three in step, so extend them together.
.claude/worktrees/
.claire/worktrees/
.codex/worktrees/
.codex-worktrees/
.cursor/worktrees/
.grok/worktrees/
.warp/worktrees/

# Private paid-vendor A-share spine. The collector defaults outside the repo;
# this containment wall prevents an explicitly configured legacy store from
# being swept into broad data/ staging or public Git history.
/data/china_tushare_spine/

# Page-evidence screenshots are LOCAL observation, not repository content. One P0
# sweep is ~360 full-page PNGs (state matrix x 30 pages) at ~0.5 MB each, and they
# are re-derivable from the committed manifest at any time. The manifest and the
# smell report carry each shot's sha256, so the evidence stays citable without the
# bytes ever entering Git. See docs/product_experience/PAGE_EVIDENCE_HARNESS.md.
data/product_experience/evidence/

# Agent OS CEO-brief check-in marker — a local pointer to "when did I last look",
# not organizational state.  Machine-local by construction; never committed.
data/governance/.ceo_brief_last

# qledger write-once control clocks (C3.1) — REGISTRAR-LOCAL state, never
# committed: a clock a lane can commit is a clock a lane can re-stamp, and
# tests/test_qledger_control_policy.py::test_t9 reds any tracked entry. The
# 2026-08-19 nightly's blanket `git add data/` committed demand_chain.json and
# redded the merge gate fleet-wide; ignoring the directory makes that
# impossible at the plumbing instead of merely detected.
data/qledger/control_evidence_clock_start/

# E3-A eval shards. Bounded proof lives in the eval receipt, not the global ledger.
/data/ai_costs/usage.d/e3a-pr6245-*.jsonl
/research/earnings_intelligence/e3/gold/provider_health_*.jsonl
