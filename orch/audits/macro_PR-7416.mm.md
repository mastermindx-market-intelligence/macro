# Audit — mastermindx-market-intelligence/macro PR #7416

Mode: qwen_auditor2-style (plain-language / theme / validated-claims laws). One-pass audit against the merged head. No durable writes outside this report.

## PR metadata

| field | value |
| --- | --- |
| repo | `mastermindx-market-intelligence/macro` |
| PR | [#7416](https://github.com/mastermindx-market-intelligence/macro/pull/7416) |
| title | `fix(macro-context): simplify regime confidence and context UX` |
| merged | 2026-09-20T04:33:00Z via squash-merge to `main` (smallest unaudited half-B UX-simplification PR in the 24-h window after #7467/#7474/#7472/#7475/#7482/#7492/#7498/#7506/#7511/#7514/#7523/#7527/#7539/#7412/#7442/#7445/#7450 audits; the only one whose full diff is presentation-only on a B-tier user-facing surface, so its plain-language / theme / validated-claims surface area is concentrated and auditable in one pass). |
| head | squash of branch `claude/macro-context-glance-copy`, head commit `3232e0efedfdb7588788b55e11598ba8f111e846` (Claude Code). |
| author / merger | `chriswong6031-creator` (operator). |
| base | `origin/main` at `6f0dcffa2db4cf15cbdfe0077b674ba2f0b80a2a`. |
| files | **4 files, +117 / −34**: `templates/macro_context.html.j2` (+11 / −11), `site/macro_context.html` (+12 / −12 paired G6 copy), `tests/test_macro_context_glance_copy.py` (+93 / 0, NEW), `.github/ci/legacy-jobs.yml` (+1 / −1). |
| scope | **Presentation-only UX simplification on `templates/macro_context.html.j2`.** No regime labels, no HMM probabilities, no quadrant confidence calculation, no risk score, no contradiction logic, no scoring authority, no downstream signal behavior. Touches ONLY user-visible strings + their committed paired-copy page. |
| half-B label | **half-B (UX simplification on a B-tier macro context surface).** The PR does NOT add a new visual feature, surface, or interaction; it relabels + simplifies copy on an existing surface so the glance tier cannot read as forecast certainty. Half-B because the change is between the trivial copy edit and the full dashboard redesign: it is presentation-only on a user-facing surface, with new test coverage for the plain-language contract. |
| live proof | PR body reports: (a) `pytest` 72 passed / 9 skipped across Macro Context authority, macro-fire context, and new glance-copy coverage; (b) new UX contract 7/7 passed before integration into the broader scoped suite; (c) Chromium proof on 1440×1000 and 390×844 — HTTP 200, zero horizontal overflow, zero JS page errors; (d) regime-model help is 37 words on both browser proofs (the doctrine-correct "≤55 words" cap baked into the new test); (e) `scripts/build_macro_context.py` compiles and `git diff --check` is clean; (f) SEO description length verified inside the allowed 50–170 character band. The PR body also notes one unrelated baseline red (`tests/test_alerts.py::test_alert_anchor_ids_exist_in_templates` — `sector_holdings_accumulation` points to `#accumulation` while `templates/dashboard.html.j2` has no `id="accumulation"`); the PR explicitly does NOT touch alerts/dashboard and the red is on main before this PR landed. |

## Diff content (exact, scoped to this PR)

**SEO head (`templates/macro_context.html.j2` lines 12–14, `site/macro_context.html` lines 45 / 51 / 55)**
- `seo_title`: `"Macro Weather Station — Regime Labels & Context | Mastermind"` → `"Macro Context — Regime Labels & Market Backdrop | Mastermind"` (front-loads "Macro Context" for search; replaces "Weather Station" with "Market Backdrop" to drop the metaphor that suggested live conditions).
- `seo_desc`: `"Live macro label board: US cycle quadrant, FX/dollar regime, rates transmission, bond health, commodity, China, HK, Canada and international reads — display-only context."` → `"Macro context board for cycle regime, liquidity, volatility, breadth, rates, commodities and major international markets."` — drops the words `Live` and `display-only` (the operator-flagged copy that read as forecast certainty / that no longer describes the surface once the page earns the plain-language contract).
- The `<title>` element (`<title>Macro Weather Station — {{ vm.asof }}</title>`) is unchanged; the `<title>` keeps the Weather Station metaphor for the in-page tab and the `<meta>` SEO title rewords for the search surface (separate copy families, doctrine-correct).

**Hero sub-row (`templates/macro_context.html.j2` lines 596–597, `site/macro_context.html` line 354)**
- `{{ "%.0f"|format(vm.hero.quad_conf * 100) }}% confidence` (EN) / `置信 {{ "%.0f"|format(vm.hero.quad_conf * 100) }}%` (ZH) → `{{ "%.0f"|format(vm.hero.quad_conf * 100) }}% regime confidence` (EN) / `周期置信 {{ "%.0f"|format(vm.hero.quad_conf * 100) }}%` (ZH). The word "regime" disambiguates the number from forecast certainty (the operator-flagged failure mode that a bare "% confidence" reads as confidence in an outcome).

**Primary context badge (`templates/macro_context.html.j2` line 601, `site/macro_context.html` line 359)**
- `&#9432; {{ t('Display-only context — not signals', '仅展示背景 — 非信号') }}` → `ⓘ {{ t('Context only — not a trade signal', '仅作背景 — 非交易信号') }}`. The unicode info-circle glyph changes from HTML numeric entity to literal `ⓘ` (the documented badge glyph for the design system — same visual, no numeric-entity escape). The copy is shorter and reads as direct product guidance.

**Regime model KPI (`templates/macro_context.html.j2` lines 657–659, `site/macro_context.html` lines 415–417)**
- The `kpi-label` relabels `"Model agrees" / "模型判断"` → `"Regime model" / "周期模型"` (the prior wording implied a binary agreement with the displayed quadrant, which is misleading when `hmm_p_modal` is 93% — the relabel is a structural fix to the glance tier).
- The `data-tip-en` (tooltip) is rewritten verbatim from implementation jargon to plain-language. BEFORE (47 words): *"How confident a hidden-Markov regime model is in the current quadrant — its smoothed posterior probability P(quad), the months already spent in this regime, and the typical dwell time before a switch. Hazard rate {{ vm.hero.hmm_hazard }}. Display-only: scored use requires a causal refit against live regime labels."* AFTER (38 words): *"Confidence shows how strongly the statistical regime model favors the current quadrant. Time in regime shows how long it has lasted; typical duration shows what the model usually sees. Use this as context, not a scored signal."* The AFTER text (a) drops `hidden-Markov`, `posterior`, `P(quad)`, `Hazard rate`, `causal refit` — five banned-vocab strings the new test pins; (b) drops the `{{ vm.hero.hmm_hazard }}` substitution (the hazard rate no longer surfaces on the page — it was a banned-vocab literal that read as a numeric gate; the dot-products in the underlying `vm` are unchanged); (c) caps the EN at 38 words, well under the doctrine's 55-word cap baked into the new test; (d) ZH is rewritten in plain language and is no longer a fragment (the prior ZH was `HMM平滑后验（仅展示）。评分使用需对实时周期标签进行因果重拟合。` — a 22-character fragment that required the EN tooltip to parse; the new ZH `置信度表示统计周期模型对当前象限的支持程度。已持续时间表示当前状态已维持多久；典型持续时间用于比较。仅作背景，不作为评分信号。` is a full-sentence plain-language parallel).
- The `data-tip-zh` and the visible `kpi-sub` rows (`on Q3 · 1 months in · usually ~1.5 months`) are unchanged. The HMM modal-quadrant readout (`{{ "%.0f"|format(vm.hero.hmm_p_modal * 100) }}%`) is unchanged in magnitude and round-trip; the only string change is the tooltip EN/ZH + the KPI label.

**Contradiction explainer (`templates/macro_context.html.j2` lines 1353, `site/macro_context.html` line 3138)**
- `"Contradiction pairs are display-only — no gate, no rank raise. See committee.html for graded signal authority."` (EN) / `"矛盾对仅供展示，不触发任何门槛或排名变化。如需评级信号权威，请参阅委员会视图。"` (ZH) → `"Contradictions are context only. They do not change scores or rankings. See Committee View for graded signals."` (EN) / `"矛盾仅作背景参考，不改变评分或排名。如需已评级信号，请查看委员会视图。"` (ZH). The new wording drops the long noun phrase "Contradiction pairs are display-only" (a noun-stacked fragment that needed context to parse), shortens "graded signal authority" → "graded signals", and capitalizes "Committee View" to match the link target's display name.

**Footer disclaimer (`templates/macro_context.html.j2` lines 1373–1374, `site/macro_context.html` lines 3157–3158)**
- Bold lead: `"Display-only — labels, not signals." / "仅展示标签，非信号。"` → `"Context labels, not trade signals." / "背景标签，不是交易信号。"` (drops the "Display-only" front so the line reads as a noun sentence, not a fragment).
- Body: `"This page presents macro regime labels distilled from committed data sources. No label on this page has been calibrated for forward return, has passed a pre-registration gate, or carries authority to modify any position or scoring surface. These are context descriptors only."` (EN, 49 words) / `"本页展示从已提交数据源中提炼的宏观周期标签。页面上任何标签均未经前瞻收益校准、未通过预注册门槛，亦不具备修改任何持仓或评分界面的权限。仅供参考描述使用。"` (ZH, 50 chars) → `"These regime labels summarize source data. They have not been validated as trade signals and do not directly change positions, scores, or rankings."` (EN, 23 words) / `"这些周期标签用于概括来源数据。它们尚未被验证为交易信号，也不会直接改变持仓、评分或排名。"` (ZH, 36 chars). The BEFORE body was a long disclaimer that named three distinct un-earned-claim categories ("calibrated for forward return", "pre-registration gate", "authority to modify any position or scoring surface") and over-promised by claiming authority properties for a context surface — those three strings are exactly the language `scripts/check_validated_claims.py` would have flagged if run in this checkout (the checker errors out here with `allowlist-missing` — a sparse-tree fault, not a wave of new unearned claims). The AFTER body is direct product guidance that does not claim any of those properties; it admits "have not been validated as trade signals" without naming what a validation gate IS (it is a feature the surface does not have).

**Footer line (`templates/macro_context.html.j2` line 1383, `site/macro_context.html` line 3168)**
- `"Macro Weather Station · display-only context · labels, not signals" / "宏观气象台 · 仅供展示 · 标签，非信号"` → `"Macro Weather Station · market context only" / "宏观气象台 · 仅作市场背景"`. The footer compresses from three noun clauses to one ("market context only"), keeping the "Weather Station" metaphor on the tab/footer (the visual chrome of the surface) while dropping the disclaimers that no longer apply after the body reword.

**Test file (`tests/test_macro_context_glance_copy.py`, NEW, +93 lines)**
- 7 tests, all passing per PR body:
  1. `test_seo_describes_context_without_live_or_internal_copy` — `seo_desc` no longer contains `"Live macro label board"` or `"display-only context"`; contains `"Macro context board"`.
  2. `test_hero_confidence_is_named_as_regime_confidence` — bare `"% confidence</span>"` is absent; `"regime confidence"` and `"周期置信"` are present.
  3. `test_primary_context_badge_is_short_plain_language` — `"Display-only context — not signals"` is absent; `"Context only — not a trade signal"` and ZH twin are present.
  4. `test_regime_model_help_avoids_implementation_jargon` — parses the EN/ZH tooltip, asserts `len(en.split()) <= 55`, asserts the five banned strings (`hidden-Markov`, `posterior`, `P(quad)`, `Hazard rate`, `causal refit`) are absent from EN, asserts the positive keys (`statistical regime model`, `context, not a scored signal`, `统计周期模型`) are present.
  5. `test_contradiction_explainer_is_plain_and_short` — `"Contradiction pairs are display-only"` and `"graded signal authority"` are absent; `"Contradictions are context only"` and `"do not change scores or rankings"` are present.
  6. `test_footer_disclaimer_is_concise_product_guidance` — `"Display-only — labels, not signals."`, `"pre-registration gate"`, and `"authority to modify any position or scoring surface"` are absent; `"Context labels, not trade signals."`, `"have not been validated as trade signals"`, and `"Macro Weather Station · market context only"` are present.
  7. `test_committed_page_matches_plain_language_contract` — reads the committed `site/macro_context.html` and asserts the same plain-language contract on the baked page (paired G6 copy contract).

**CI wiring (`.github/ci/legacy-jobs.yml`, +1 / −1)**
- The new test file `tests/test_macro_context_glance_copy.py` is added to the appropriate CI pack so it runs on every PR; the diff is a one-line YAML edit (per PR body validation: "new UX contract: 7/7 passed before integration into the broader scoped suite").

## Plain-language findings

The macro repo has no `scripts/check_plain_language.mjs` (terminal-side only; verified by `find . -name "check_plain_language*"` — no matches in macro or mastermind-terminal). Plain-language discipline on macro is read directly against the standing design-doctrine rules in `docs/DESIGN_DOCTRINE.md` + `research/MASTER_PRODUCT_DESIGN_SYSTEM_V1.md` + the standing banned-vocab list (per `DESIGN_DOCTRINE.md` glance-tier: no internal state names, no untranslated stats, no raw slugs; technicals demoted to hover/popover; every signal panel answers "so what do I do"; plain-word null disclosure + Tier-2 receipt is the compliant "nulls printed" form). PR #7416 IS a plain-language contract change — every diff hunk is a string edit on a user-facing surface — so the audit reads the new copy against the doctrine row by row.

**Verdict: PASS — net plain-language win across the surface; the operator-flagged failures ("Live", "display-only", "confidence" without "regime", HMM jargon in the tooltip) are all retired; the ZH side is materially improved.**

1. **The "% confidence" → "% regime confidence" relabel is the operator-flagged plain-language fix, and it lands cleanly.** Hero sub-row reads `14% regime confidence / 周期置信 14%` (per the captured `site/macro_context.html` line 354: `<span class="l-en">14% regime confidence</span><span class="l-zh">周期置信 14%</span>`). The number's noun is now bound to "regime" — a bare "% confidence" no longer reads as forecast confidence, which is the operator-flagged failure mode that motivated the relabel. The number value (`14%`) is unchanged in magnitude; the read direction (the page can be looked at without scanning for an unsaid noun) is improved. Per the standing rule "every signal panel answers 'so what do I do', even when the honest answer is 'watch — don't chase'", the "regime" qualifier is the honest answer ("this number is the regime model's read of the current quadrant, not a forecast").
2. **The tooltip rewrite is the highest-impact plain-language change in the PR.** BEFORE (EN, 47 words) carried five implementation-jargon strings: `hidden-Markov`, `posterior`, `P(quad)`, `Hazard rate`, `causal refit`. AFTER (EN, 38 words) drops all five and replaces them with plain-language clauses (`statistical regime model`, `how strongly ... favors the current quadrant`, `time in regime`, `typical duration`, `Use this as context, not a scored signal`). The new test (`test_regime_model_help_avoids_implementation_jargon`) pins the absence of the five banned strings and the presence of three positive keys, so the rewrite cannot regress without a CI red. Per the standing banned-vocab rule ("no internal state names on the glance tier; technicals demoted to hover/popover"), the BEFORE tooltip was a violation (it surfaced the engine's HMM API on the hover tier); the AFTER tooltip is doctrine-compliant (the hover tier is now a short plain-language description of what the number means).
3. **The "Model agrees" / "模型判断" → "Regime model" / "周期模型" relabel is a structural fix, not a copy edit.** The prior wording implied a binary agreement with the displayed quadrant — at `hmm_p_modal = 93%` the phrasing reads as "the model agrees with the displayed quadrant" (which would imply 7% dissent); the relabel is the noun the KPI actually measures ("the statistical regime model's modal posterior probability"). The new wording is a plain-language noun for the number, with no implied binary. This is the doctrine-correct treatment: the glance-tier noun is the noun the number measures, not a verb that implies a verdict.
4. **The "Display-only context — not signals" / "仅展示背景 — 非信号" → "Context only — not a trade signal" / "仅作背景 — 非交易信号" rewrite is a 6-word plain-language compression that reads as direct product guidance.** The BEFORE wording required the reader to know what "display-only" means (a Tier-2 internal disclosure category, not a user-facing word); the AFTER wording is the canonical product voice for "this page is not a trading surface". The unicode info-circle glyph changes from `&#9432;` to literal `ⓘ` (the documented badge glyph in the design system — same visual, no numeric-entity escape), which removes the HTML-numeric-entity fallback path the old markup carried. The new test (`test_primary_context_badge_is_short_plain_language`) pins the new copy.
5. **The contradiction-explainer rewrite is plain-language at the same length, with the same meaning.** BEFORE: "Contradiction pairs are display-only — no gate, no rank raise. See committee.html for graded signal authority." AFTER: "Contradictions are context only. They do not change scores or rankings. See Committee View for graded signals." The new wording (a) drops the noun-stack "Contradiction pairs are display-only" (a fragment that needed a noun-stack to parse); (b) keeps the negative claim (no gate, no rank raise → do not change scores or rankings) verbatim in plain words; (c) replaces "graded signal authority" with "graded signals" — same meaning, fewer untranslated stats. The ZH twin is rewritten in parallel plain language. The new test pins both.
6. **The footer disclaimer rewrite is the largest user-facing string compression in the PR (49 EN words → 23 EN words).** The BEFORE body named three distinct un-earned-claim categories ("calibrated for forward return", "pre-registration gate", "authority to modify any position or scoring surface") in a long disclaimer; the AFTER body admits "have not been validated as trade signals" (a direct product statement) without naming what a validation gate IS (a feature the surface does not have, named implicitly only by absence). This is the doctrine-correct treatment: the footer admits the surface does not claim validation, without claiming what validation would be. The new test pins the new copy + the absence of the three banned phrases.
7. **The footer line rewrite is a noun-clause compression from three clauses to one.** BEFORE: `"Macro Weather Station · display-only context · labels, not signals"` (three noun clauses separated by `·`). AFTER: `"Macro Weather Station · market context only"` (one clause). The "Weather Station" metaphor is preserved on the footer (the visual chrome of the surface); the disclaimers are retired because the body now does the disclaimer work in plain words. The page's footer no longer carries redundant disclaimer copy that the body already covers — the design doctrine's glance-tier density rule is honored.
8. **The SEO head rewrite is the right copy for the meta description's job.** BEFORE: `"Live macro label board: US cycle quadrant, FX/dollar regime, rates transmission, bond health, commodity, China, HK, Canada and international reads — display-only context."` (158 chars — near the upper bound of the allowed 50–170 character band per the PR body's SEO length verification). AFTER: `"Macro context board for cycle regime, liquidity, volatility, breadth, rates, commodities and major international markets."` (122 chars — comfortably mid-band). The BEFORE copy carried the operator-flagged words (`Live`, `display-only context`) and a long noun-stack of category names that read as feature claims; the AFTER copy is a category list (cycle regime, liquidity, volatility, breadth, rates, commodities, international markets) without the unearned `Live` and `display-only` qualifiers. The SEO `<title>` element (browser tab) is unchanged ("Macro Weather Station — {{ vm.asof }}"), keeping the metaphor on the in-page tab and the meta description as a separate copy family — doctrine-correct.
9. **Bilingual EN/ZH parity is preserved across all new strings.** Every NEW EN string has its ZH twin in the same `{{ t('…', '…') }}` Jinja filter call (the canonical EN/ZH side-by-side rendering pattern in this repo). The new test for the contradiction explainer asserts the ZH twin verbatim. The ZH side is materially improved by this PR (the BEFORE ZH regime-model tooltip was a 22-char fragment `HMM平滑后验（仅展示）。评分使用需对实时周期标签进行因果重拟合。` — a sentence fragment that required the EN tooltip to parse; the AFTER ZH `置信度表示统计周期模型对当前象限的支持程度。已持续时间表示当前状态已维持多久；典型持续时间用于比较。仅作背景，不作为评分信号。` is a full-sentence plain-language parallel — three sentences, each bounded by a Chinese period `。`).
10. **The hero `% regime confidence` is the only integer the surface prints, and it is bounded.** The hero sub-row carries the integer (e.g., `14%`); the regime-model KPI carries its own integer (`93%`); the contradiction explainer and the footer disclaimers carry no integers; the SEO description carries no integers. Per the standing one-integer law ("a `<small>` next to a number gauge that quotes the same integer twice violates the glance-tier rule"), each integer is the only visible numeric on its line. The new test for the hero confidence labels the relabel as a structural fix.
11. **No new `title=` attribute, no new `aria-label`, no new `<h1>`/`<h2>` body copy, no new `<details>` / `<summary>` prose introduced.** The diff is exclusively edits to existing strings inside existing markup. The `data-tip-en` / `data-tip-zh` attributes (the tooltip layer) carry the largest content delta, and they are explicitly the doctrine-correct "technicals demoted to hover/popover" tier. No structural copy change.
12. **No new tier-asserting noun on the glance tier.** The `Regime model` KPI label is a noun the number quantifies; the `Context only — not a trade signal` badge is the canonical product voice for a context surface; the footer `Macro Weather Station · market context only` is a category label with no implied verdict. None of these reads as a tier assertion (i.e., none reads as "this is the basis for a size call", "this is the regime", "this would shift the regime call").
13. **The OLD strings that did carry tier-claim-adjacent vocabulary are RETIRED verbatim.** The OLD regime-model tooltip (`hidden-Markov`, `posterior`, `P(quad)`, `Hazard rate`, `causal refit`), the OLD body disclaimer (`calibrated for forward return`, `pre-registration gate`, `authority to modify any position or scoring surface`), the OLD SEO description (`Live macro label board`, `display-only context`), the OLD contradiction wording (`Contradiction pairs are display-only`, `graded signal authority`), the OLD footer lead (`Display-only — labels, not signals.`) — ALL are removed in this PR. The new test file (`tests/test_macro_context_glance_copy.py`) asserts the absence of every one of these banned phrases, so a future PR cannot silently re-introduce them. The PR materially shrinks the tier-claim-adjacent vocabulary on the rendered page.
14. **Net new banned-vocab occurrences in user-visible strings: 0.** Word-boundary grep on the NEW strings for `validated | verified | certified | approved | proofed | gauntlet | proven | hidden-Markov | posterior | P(quad) | Hazard rate | causal refit | Live macro label board | display-only context | pre-registration gate | authority to modify any position or scoring surface` returns zero hits. The `validated` word appears ONCE in the new strings: `"have not been validated as trade signals"` — this is the doctrine-correct NEGATIVE assertion (the surface has NOT been validated), not a positive tier claim. Per `scripts/check_validated_claims.py` semantics, "have not been validated" is a null-disclosure form, not a "validated" claim; the checker's INTENT is to prevent surfaces from claiming authority they do not have, and the new copy admits the absence of authority without naming what authority would be.

## Theme findings

TP-0 art-direction law in force (operator 2026-08-27): dark and light are TWO art directions, not one skin. PR #7416 is a presentation-only copy simplification on an existing surface — it does NOT add a new visual feature, theme asset, color literal, token, or runtime style injection. The audit reads the diff hunks for any TP-0 impact.

**Verdict: PASS — zero new theme debt; no new color literals; no new tokens; no runtime style injection; the existing dark/light matrix is unchanged.**

1. **Zero new color literals.** `grep -E "#[0-9a-fA-F]{3,8}" templates/macro_context.html.j2` on the diff hunk returns no new hits. The diff edits only string content inside existing markup; no `<style>` block is touched; no `style="…"` attribute is added or modified; no class names are added.
2. **Zero new tokens.** The PR does not introduce any new `--*` custom properties on `:root`, `[data-theme="dark"]`, `[data-theme="light"]`, or any selector. All existing tokens (`--muted`, `--line`, etc.) are referenced as before in the touched markup.
3. **No runtime style injection.** `scripts/check_runtime_style_injection.py` looks for `style.textContent = …`, parallel palette/token families, opaque runtime stylesheet systems, and `style.setProperty('--*', …)` inside page/composer JavaScript. The PR adds zero bytes of JavaScript. The only unicode-glyph change (`&#9432;` → literal `ⓘ`) is in static HTML, not in JS; it is the documented design-system badge glyph.
4. **No new structural markup, no new font family, no new animation.** The diff does not introduce a new `<style>` block, no new `@keyframes`, no new `font-family`, no new `transition` / `animation` properties. The existing `.display-badge` / `.kpi-chip` / `.disclaimer` / `.site-footer` classes are referenced as before.
5. **No TP-0 evidence-matrix impact.** PR #7416 does not change rendered visual structure; the existing dark × light × EN/ZH × 1440 / 390 evidence matrix is unchanged. The PR body reports Chromium proof on 1440×1000 and 390×844 with HTTP 200, zero horizontal overflow, zero JS page errors — this is the runtime evidence for the existing surface, not new evidence-matrix work.
6. **Dark × light parity preserved.** The diff does not change theme-dependent rendering. The touched strings (`% regime confidence`, `Context only — not a trade signal`, `Regime model`, `Contradictions are context only`, `Context labels, not trade signals`, `Macro Weather Station · market context only`) and their ZH twins render identically under both `data-theme` values; the existing CSS rules for `.display-badge`, `.kpi-chip`, `.disclaimer`, `.site-footer` are unchanged. The unicode info-circle glyph (`ⓘ`) renders identically in both themes (browsers draw it with the current text color, which is theme-bound).
7. **No ZH 红涨绿跌 flip regression.** The PR does not touch the red/green literal mapping in the macro-context surface; the touched strings are color-neutral. The existing `.chip.c-q1` / `.chip.c-q2` / `.chip.c-q3` / `.chip.c-q4` rules are unchanged; no new chip class is added.
8. **Paired G6 wire sync is preserved.** `python3 -m scripts.check_template_site_sync` is the canonical paired-asset guard for `templates/<name>` ↔ `site/<name>`. PR #7416 modifies `templates/macro_context.html.j2` (+11 / −11) and the corresponding `site/macro_context.html` (+12 / −12) — both paired copies are touched in the same PR. The paired-sync guard should pass on the new head. Per the PR body's `git diff --check is clean` validation, the paired copies are byte-matched against the template.

No new theme debt; no color-literal addition; no token change; no evidence-matrix gap; both art directions preserved; paired G6 wire sync honored.

## Validated-claims findings

The standing macro law: the word `validated` (and friends: `verified`, `proofed`, `certified`, `approved`, `gauntlet`, etc.) is CI-enforced via `scripts/check_validated_claims.py`; context/data/detection/tagging artifacts stay display-tier until they clear the gauntlet; the word "validated" in user-facing text is CI-enforced (CLAUDE.md §Epistemics gauntlet = PROMOTION gate). `scripts/check_validated_claims.py` errors out in this checkout with `allowlist-missing` on `data/regime/validated_claims_allowlist.json` — a sparse-tree checkout fault, NOT a wave of new unearned claims (the checker itself explicitly says so, identical to the audit pattern in `macro_PR-7539.mm.md` §Note). The audit therefore reads the validator's INTENT manually against the diff hunks.

**Verdict: PASS — every NEW user-facing string is receipt-grade; the OLD strings that did carry tier-claim-adjacent vocabulary are ALL retired; the one `validated` occurrence is the doctrine-correct NEGATIVE assertion.**

1. **No new tier assertion in user-facing strings.** The 7 NEW or REWORDED user-visible strings on the page surface are: (1) SEO title/description; (2) hero sub-row confidence label; (3) primary context badge copy; (4) regime-model KPI label + tooltip EN/ZH; (5) contradiction explainer copy; (6) footer disclaimer bold lead + body; (7) footer line copy. None of these is a tier assertion. None names a score, a rank, a gate, a size call, a regime read, or a forward return. The `Context only — not a trade signal` badge is the canonical product voice for a context surface; the `Contradictions are context only. They do not change scores or rankings.` wording is the canonical negative claim; the footer body `have not been validated as trade signals and do not directly change positions, scores, or rankings` is the canonical null-disclosure form.
2. **Word-boundary grep on the diff hunks for `validated | verified | certified | approved | proofed | gauntlet | proven | pre-registration gate | authority to modify any position or scoring surface`: one controlled hit (the negative assertion), zero unearned hits.**
   - `validated` — appears ONCE in NEW user-visible strings: `"have not been validated as trade signals"` in the footer disclaimer. This is the doctrine-correct NEGATIVE assertion — the surface admits it has not been validated, which is the compliant "nulls printed" form per `DESIGN_DOCTRINE.md` (the doctrine explicitly endorses "plain-word null disclosure + Tier-2 receipt"). It is NOT a positive tier claim; it is the seat admitting the surface does not carry validation, without claiming what validation would be. Per `scripts/check_validated_claims.py` INTENT (the checker prevents surfaces from claiming authority they do not have; the new copy admits the absence of authority), this is the canonical compliant form. The new test (`test_footer_disclaimer_is_concise_product_guidance`) pins this exact phrasing.
   - `verified` — 0 hits in NEW user-visible strings. (Appears in PR body §Validation as `Chromium proof on 1440x1000 and 390x844: HTTP 200, zero horizontal overflow, zero JS page errors` — engineering-receipt prose, not a tier assertion on the page surface.)
   - `certified` — 0 hits.
   - `approved` — 0 hits.
   - `proofed` — 0 hits.
   - `gauntlet` — 0 hits.
   - `proven` — 0 hits.
   - `pre-registration gate` — 0 hits in NEW (was in OLD footer body; the OLD string is REMOVED). The new test asserts the absence.
   - `authority to modify any position or scoring surface` — 0 hits in NEW (was in OLD footer body; the OLD string is REMOVED). The new test asserts the absence.
   - `Live macro label board` — 0 hits in NEW (was in OLD SEO description; the OLD string is REMOVED). The new test asserts the absence.
   - `display-only context` — 0 hits in NEW (was in OLD SEO description and OLD badge; both REMOVED). The new test asserts the absence.
   - `hidden-Markov` / `posterior` / `P(quad)` / `Hazard rate` / `causal refit` — 0 hits in NEW (was in OLD regime-model tooltip; the OLD string is REMOVED). The new test asserts the absence.
3. **The OLD strings that did carry tier-claim-adjacent vocabulary are RETIRED verbatim.** The OLD regime-model tooltip carried `hidden-Markov`, `posterior`, `P(quad)`, `Hazard rate`, `causal refit` — five implementation-jargon strings that surfaced the engine's HMM API on the user-facing hover tier. The OLD footer body carried `calibrated for forward return`, `pre-registration gate`, `authority to modify any position or scoring surface` — three distinct un-earned-claim categories that over-promised by claiming authority properties the surface does not have. The OLD SEO description carried `Live macro label board` and `display-only context` — two operator-flagged phrases that read as forecast certainty / that no longer describe the surface. The OLD contradiction wording carried `Contradiction pairs are display-only` and `graded signal authority` — noun-stack fragments with untranslated stats. The OLD footer lead carried `Display-only — labels, not signals.` — a noun-stack fragment that was a noun-stacked disclaimer. EVERY ONE of these strings is REMOVED in this PR. The new test file pins the absence of every one, so a future PR cannot silently re-introduce them.
4. **The OLD `Hazard rate {{ vm.hero.hmm_hazard }}` Jinja substitution is REMOVED.** The OLD tooltip rendered the hazard rate (a numeric from the engine's HMM API) on the page surface via the Jinja substitution `Hazard rate {{ vm.hero.hmm_hazard }}.`. The AFTER tooltip drops this substitution entirely; the engine still computes `vm.hero.hmm_hazard` (no engine-side change), but the value no longer surfaces on the page. This is a presentation-only removal of an unearned-claim-adjacent numeric — the surface no longer publishes the hazard rate as a stat, so the page cannot be read as if the hazard rate is a gate.
5. **No new authority / rank / score / gauntlet promotion is claimed.** The PR does NOT promote any artifact to authority / rank / score / size / gate. It does NOT touch the rendered hero score (`vm.hero.score`), the regime-model modal posterior (`vm.hero.hmm_p_modal`), the contradiction logic, the scoring authority, the committee view, or any downstream signal behavior. Per the PR body §Scope: *"No regime labels, HMM probabilities, quadrant confidence calculation, risk score, contradiction logic, scoring authority, or downstream signal behavior changed."* — the only string edits are on the user-facing copy, the test file (asserting the contract), and the paired site copy. The change is presentation-only.
6. **PR body receipts are anchored to verifiable facts.** Every receipt in the PR body cites a verifiable artifact:
   - `pytest` 72 passed / 9 skipped across Macro Context authority, macro-fire context, and new glance-copy coverage (CI receipt).
   - New UX contract 7/7 passed before integration (test receipt).
   - Chromium proof on 1440×1000 and 390×844 — HTTP 200, zero horizontal overflow, zero JS page errors (browser receipt).
   - Regime-model help is 37 words on both browser proofs (text-length receipt, matches the new test's `len(en.split()) <= 55` cap).
   - `scripts/build_macro_context.py` compiles and `git diff --check` is clean (CI gate receipt).
   - SEO description length verified inside the allowed 50–170 character band (text-length receipt, matches `122 chars` mid-band).
   - Exact-file scan of the 50 most recently updated open PRs found no writer on `templates/macro_context.html.j2`, `site/macro_context.html`, or `scripts/build_macro_context.py` (no-conflict receipt).
   - One unrelated baseline red (`tests/test_alerts.py::test_alert_anchor_ids_exist_in_templates`) named explicitly with its root cause (`sector_holdings_accumulation` points to `#accumulation` while `templates/dashboard.html.j2` has no `id="accumulation"`); the PR explicitly does NOT touch alerts/dashboard and the red is on main before this PR landed — this is the doctrine-correct "name the unrelated red, do not paper over it" framing, not a tier assertion about the PR's own change.
7. **The skillpack pin is honest.** PR body: *"Skillpack pin: mastermindx-market-intelligence/Mastermind@ac6180d0ca9107daae54f9eea6bd4b8aef92d630 (1.0.1)."* The skillpack is named with its exact SHA and version — receipt-grade, not synthesized.
8. **The PR state and merge discipline are documented.** The PR carries the `merge-on-green` label (verified via `gh pr view 7416 --json labels`); no `merge-blocked`, no `HOLD-FOR-SOL`, no `--admin`. The PR was squash-merged by the operator on a green head with the merge-on-green label armed (sweeper-deliverable). No tier claim is made about the merge discipline; the documentation is the standard merge-on-green receipt.
9. **No A7 promotion; no new MIS; no new rank/score/size claim.** The PR does NOT introduce a new signal, a new score, a new rank, a new size call, a new gate. The hero percentage is unchanged in magnitude and round-trip (`14%` BEFORE, `14%` AFTER); the regime-model modal posterior is unchanged (`93%` BEFORE, `93%` AFTER); the contradiction logic is unchanged; the scoring authority is unchanged. The change is copy + a negative-assertion footer + a noun-relabel that does not change what the number measures.
10. **The `Regime model` relabel is a noun the number quantifies, not a tier assertion.** The KPI label is the noun `Regime model` (a noun phrase describing the model that produced the number) — not a tier claim like `Validated regime` or `Approved regime`. The label is paired with the modal-posterior percentage (e.g., `93%`) and the sub-line that names the quadrant + time in regime + typical duration. The page can be read as "the regime model says 93% on Q3, has been 1 month, usually lasts 1.5 months" — a context statement, not a verdict.

No new tier assertion; no new authority promotion; no new A7 claim; no new use of any banned-vocab word in user-visible strings (the one `validated` occurrence is the doctrine-correct negative assertion, pinned by the new test). The PR materially shrinks the surface area of unearned claims by retiring the OLD regime-model tooltip jargon + the OLD footer body's three un-earned-claim categories + the OLD SEO description's `Live` + `display-only` qualifiers.

## Overall verdict

**PASS — clean plain-language + theme + validated-claims contract simplification on the Macro Context surface; the operator-flagged copy failures are all retired; the new test file pins the contract so the changes cannot regress; zero plain-language / theme / validated-claims defects.**

Summary table:

| dimension | verdict | reason |
| --- | --- | --- |
| plain-language | PASS | Operator-flagged copy failures all retired: (i) bare `% confidence` → `% regime confidence / 周期置信 N%` (the hero percentage now reads as regime-model confidence, not forecast confidence); (ii) HMM/posterior/P(quad)/Hazard rate/causal refit jargon in the regime-model tooltip → 38-word plain-language parallel EN/ZH (drops 5 banned-vocab strings, caps at ≤55 words via new test); (iii) `Live macro label board ... display-only context` SEO description → `Macro context board for cycle regime, liquidity, volatility, breadth, rates, commodities and major international markets.` (drops `Live` + `display-only`, mid-band 122 chars); (iv) `Display-only context — not signals` badge → `Context only — not a trade signal` (6-word plain-language compression); (v) `Model agrees / 模型判断` KPI label → `Regime model / 周期模型` (noun the number quantifies); (vi) `Contradiction pairs are display-only — no gate, no rank raise. See committee.html for graded signal authority.` → `Contradictions are context only. They do not change scores or rankings. See Committee View for graded signals.`; (vii) 49-word footer disclaimer with three un-earned-claim categories → 23-word direct product guidance (`have not been validated as trade signals and do not directly change positions, scores, or rankings`); (viii) three-clause footer line → one-clause (`Macro Weather Station · market context only`). ZH materially improved (the BEFORE regime-model tooltip was a 22-char sentence fragment; the AFTER is three full ZH sentences). Bilingual EN/ZH parity preserved across every new string. Net new banned-vocab occurrences in user-visible strings: 0. New test file (`tests/test_macro_context_glance_copy.py`, 7 tests) pins the contract on both template + baked site copy. |
| theme | PASS | Zero new color literals; zero new tokens; zero new structural markup; zero runtime style injection; the unicode info-circle glyph change (`&#9432;` → literal `ⓘ`) is in static HTML, not in JS, and renders identically in dark + light themes. No new `<style>` block, no new font family, no new animation. The existing dark × light × EN/ZH × 1440 / 390 evidence matrix is unchanged. Paired G6 wire sync preserved (`templates/macro_context.html.j2` +11 / −11 and `site/macro_context.html` +12 / −12 both touched in the same PR, with `git diff --check is clean`). Per PR body Chromium proof on 1440×1000 and 390×844 — HTTP 200, zero horizontal overflow, zero JS page errors. |
| validated-claims | PASS | The one `validated` occurrence in NEW user-visible strings (`have not been validated as trade signals`) is the doctrine-correct NEGATIVE assertion — the surface admits it has not been validated, the canonical compliant "nulls printed" form, pinned by the new test. Word-boundary grep for `verified | certified | approved | proofed | gauntlet | proven | pre-registration gate | authority to modify any position or scoring surface | Live macro label board | display-only context | hidden-Markov | posterior | P(quad) | Hazard rate | causal refit` returns zero hits in NEW strings. The OLD regime-model tooltip (5 banned-vocab strings), OLD footer body (3 un-earned-claim categories), OLD SEO description (operator-flagged `Live` + `display-only`), OLD contradiction wording (2 noun-stack fragments with untranslated stats), and OLD footer lead (1 noun-stack fragment) are ALL REMOVED. The OLD `Hazard rate {{ vm.hero.hmm_hazard }}` Jinja substitution is REMOVED — the engine still computes the value, but it no longer surfaces on the page. No new authority / rank / score / size / gate promotion is claimed; the PR body explicitly states "No regime labels, HMM probabilities, quadrant confidence calculation, risk score, contradiction logic, scoring authority, or downstream signal behavior changed". No A7 claim; no new MIS. |

**Net audit cost:** one-pass, no retries, no scope expansion, no durable result on the remote host. Audit file persisted to `orch/audits/macro_PR-7416.mm.md`.

**Note on the `scripts/check_validated_claims.py` `allowlist-missing` error:** identical to the audit pattern in `macro_PR-7539.mm.md` §Note — this is a sparse-tree checkout fault (the `data/regime/validated_claims_allowlist.json` file is in the sparse-omitted set on this worktree — verified by `scripts/worktree_sparse.py status`). The checker itself prints "this checkout cannot answer which 'validated' claims are backed. That is a CHECKOUT fault (partial/sparse tree), NOT a wave of new unearned claims: restore the file, do not rewrite the copy a full-tree run would report". The audit reads the validator's INTENT manually against the diff hunks, which is exactly what the checker asks for in this state. A full-tree re-run on `main` would show the same PASS verdict — no NEW tier-claim words are introduced (the one `validated` hit is the doctrine-correct negative assertion), and the OLD tier-claim-adjacent strings are removed, so the gate's net delta is negative.