# Audit — mastermindx-market-intelligence/macro PR #7527

Mode: qwen_auditor2-style (plain-language / theme / validated-claims laws). One-pass audit against the merged head. No durable writes outside this report.

## PR metadata

| field | value |
| --- | --- |
| repo | `mastermindx-market-intelligence/macro` |
| PR | [#7527](https://github.com/mastermindx-market-intelligence/macro/pull/7527) |
| title | `dataos(commodities): promote China gold source stores` |
| merged | 2026-09-20T11:03:30Z via squash-merge to `main` (latest merged non-audit-record macro PR in the 24-h window at audit time). |
| head | squash of branch (sole commit `95d7ae528df8f4b20977df6188c915b8d4fa1f47`, "Sol Integration Proof" <sol-integration-proof@mastermind-x.invalid>, authored 2026-09-20T10:56:36Z); merge commit `b199aa0694db7cd8af323785d1972fd60e19a19f`. PR body supplies no separate base / head pin; diff base is current `origin/main` at audit time. |
| files | **2 files, +25 / −23.** `config/dataset_registry.yml` (+18 / −18) and `tests/test_dataos_registry.py` (+7 / −5). Zero bytes in `templates/`, `site/`, `mockups/`, `engine/`, `scripts/`, `app/`, `admin/`. |
| scope | **Internal Data OS state promotion + matching regression test rewrite; zero user-facing surface changes.** Both touched files live under the registry/test lanes — no Jinja, no rendered HTML, no CSS, no JS, no J2, no engine logic, no builder code. |
| half-B label | **half-B (Data OS L1 commodity source promotion).** The PR flips two `commodity.gold.*` source-plane rows from `status: PROPOSED` → `status: PRODUCED` after their first protected-main source accrual + an exact-main source→engine→rendered-panel audit. This is the canonical "L1 source contract graduation" tier, NOT a code build, NOT a UI release, and NOT a promotion to a higher-tier (display/authority/rank) capability. The provider/license metadata (TuShare compliance under `DEC:CNLI-TUSHARE-COMPLIANCE-IS-CHAIRMAN-VERIFIED-PRIVATE`, Massive capability manifest wiring, `data/regime` positive control) is deliberately NOT modified — only the L1 status flag and the human-readable `notes`/`updated` are. |
| owner | Sol Integration Proof bot lane (commit author marker `sol-integration-proof@mastermind-x.invalid`). Single squash commit; no human PR-author label. The bounded applier `python -m scripts.promote_china_gold_dataos --apply` is the named writer (PR body); no producer/storage/consumer authority changes were claimed. |
| live proof | PR body reports `python3 -m pytest -q tests/test_dataos_registry.py tests/test_china_gold_premium_engine_contract.py` → **81 passed**. Receipts in body: protected-main backfill run `35498734408` completed for `only=gold_china_basis`; `data/gold_china_basis/sge_au9999.parquet` 246 rows (2025-09-15 → 2026-09-18); `data/gold_china_basis/xaucny_spot.parquet` 148 rows (2025-10-06 → 2026-09-18); aligned close observations 142; four independent official SGE reference closes matched exactly (2026-09-15 928.17 / 2026-09-16 936.75 / 2026-09-17 934.81 / 2026-09-18 947.09 RMB/g); exact-main worktree acceptance: `status=available_fresh`, `headline_method=close_proxy`, `headline_premium=-0.0817727402%`, `render_consistency=true`, `machine_projection_consistency=true`, `close_proxy_dataos_promotion_ready=true`, `promotion_blockers=[]`; bound artifact sha256s `a54a6275ee55412833d909a74a32961525cfb54a51f58c0649d500f70f772c4a` (SGE) and `d135aecf1d6924e500e1d6db9c4248d369973a3a67bf542c1f8b3122838d47b1` (XAU/CNY). The `scripts/check_plain_language.mjs` referenced in the task prompt is **terminal-side only** and does NOT exist in `mastermindx-market-intelligence/macro` (verified — `ls scripts/check_plain_language*` → no matches); plain-language discipline on macro is read directly against the standing design-doctrine rules (see §Plain-language findings below). |

## Diff content (exact, scoped to this PR)

**Registry promotion (1 file, +18 / −18)**

`config/dataset_registry.yml`:
- Header `updated` flipped `2026-09-19` → `2026-09-20` (1 line).
- Section banner comment rewritten from "PROPOSED until natural nightly proof" to "PRODUCED after live source proof"; the producer-existence rationale is preserved and the "promotion requires real store + production quality receipt" sentence is dropped (because the precondition has now been met).
- `dataset_id: commodity.gold.sge_au9999.close` row: `status: PROPOSED` → `status: PRODUCED` (1 line). `notes` block rewritten from "PROPOSED pending first natural nightly acceptance. TuShare compliance is Chairman-verified-private/SATISFIED under DEC:CNLI-TUSHARE-COMPLIANCE-IS-CHAIRMAN-VERIFIED-PRIVATE; private agreement material is deliberately not a repository input. Promotion to PRODUCED requires the real store to exist and the production quality receipt to bind source -> engine -> rendered Gold panel." → "PRODUCED 2026-09-20 after protected-main source accrual plus an exact-main quality receipt bound the real parquet artifact, selected 2026-09-18 observation, machine projection and rendered Gold panel. TuShare compliance remains Chairman-verified-private/SATISFIED under DEC:CNLI-TUSHARE-COMPLIANCE-IS-CHAIRMAN-VERIFIED-PRIVATE; private agreement material is deliberately not a repository input." (`status:` and the `notes:` literal are the only lines touched on this row — every other field, the schema spec, the code_consumers, the temporal_profile, the conflict_policy, and the provider/license fingerprint all stay byte-identical.)
- `dataset_id: commodity.gold.xaucny.close_ref` row: `status: PROPOSED` → `status: PRODUCED` (1 line). `notes` block rewritten symmetrically: "PROPOSED pending first natural nightly acceptance and the exact-endpoint technical entitlement receipt already wired into the incumbent Massive capability manifest. The operator-level licensing record remains research/licenses/MASSIVE_ENTITLEMENT_RECORD.md; dataset-specific vendor conditions, if any, remain authoritative. Promotion to PRODUCED requires the real store plus the production source->engine->render quality receipt." → "PRODUCED 2026-09-20 after protected-main source accrual plus an exact-main quality receipt bound the real parquet artifact, selected 2026-09-18 observation, machine projection and rendered Gold panel. The exact-endpoint technical entitlement remains wired into the incumbent Massive capability manifest. The operator-level licensing record remains research/licenses/MASSIVE_ENTITLEMENT_RECORD.md; dataset-specific vendor conditions, if any, remain authoritative."

Net diff: 2 dataset rows promoted L1, 1 header date bump, 2 notes rewritten, 2 banner-comment sentences rewritten. Every `code_consumers`, every `producer`, every `storage`, every `temporal_profile`, every `conflict_policy`, every `layer`, every `owner` is unchanged.

**Regression test update (1 file, +7 / −5)**

`tests/test_dataos_registry.py`:
- Test name `test_china_gold_source_contracts_are_proposed_until_natural_nightly_acceptance` → `test_china_gold_source_contracts_are_produced_after_live_acceptance` (1 line).
- Test docstring updated from "The source-plane ids may be durable before their first live store, but not PRODUCED. This lets closeout promote the exact same ids after a natural nightly without making today's registry lie about artifacts that have not landed yet." → "The stable source-plane ids are PRODUCED only after real stores + live path proof. The 2026-09-20 protected-main backfill landed both parquet stores, and the exact-main source -> engine -> render/machine audit made the bounded Data OS promotion eligible before these statuses changed."
- Single assertion flipped: `assert item.status is DatasetStatus.PROPOSED` → `assert item.status is DatasetStatus.PRODUCED`.
- One new assertion appended: `assert "PRODUCED 2026-09-20" in item.notes` (anchors the gate to the literal date so a future regression that reverts `status:` without updating the note string fails the suite).
- Every other assertion in the test (`owner == "macro-dashboard"`, `producer == "collectors/china_gold_basis.py::ChinaGoldBasisAdapter"`, `storage.startswith("data/gold_china_basis/")`, `temporal_profile == SNAPSHOT_SERIES`, `conflict_policy == PRIMARY_ONLY`, `code_consumers == ("engine/china_gold_premium.py",)`) is unchanged.

Net diff: 1 test renamed, 1 docstring rewritten, 1 assertion direction flipped, 1 new assertion appended.

## Plain-language findings

The macro repo has no `scripts/check_plain_language.mjs` (terminal-side only; verified `ls scripts/check_plain_language*` → no matches). Plain-language discipline on macro is read directly against the standing design-doctrine rules — but the doctrine applies to USER-FACING strings, and PR #7527 carries zero user-facing bytes. There is no `templates/`, no `site/`, no Jinja copy, no rendered HTML, no HTML attribute surfaced to a reader.

**Verdict: PASS (N/A — no user-facing surface in this PR).**

1. **No templates/ touched.** `grep -E "^\+\+\+ b/templates/" <diff>` returns zero matches — the 2-file diff contains no `templates/_*.html.j2`, no `templates/_*.css.j2`, no `templates/*.js`. The plain-language page can't fire on a PR that ships no Jinja.
2. **No site/ touched.** `grep -E "^\+\+\+ b/site/" <diff>` returns zero matches — the paired-site G6 wire-sync cycle (which would carry the same user-facing copy) does not apply.
3. **No user-facing HTML attribute added.** The two touched strings in `config/dataset_registry.yml` are YAML `status:` enum values (`PRODUCED`) and `notes:` scalar block-literals read by no template (verified — `grep -rE "commodity\.gold\.(sge_au9999|xaucny)" templates/ site/` returns zero matches in the user-facing tree; the dataset_id values are wired into the engine layer via `engine/china_gold_premium.py` only). The test docstring and test name are reviewer-facing, not reader-facing.
4. **PR body language is precise engineering prose, not user copy.** Words used are internal contract nouns (Data OS, L1, PROPOSED → PRODUCED, protected-main backfill, exact-main acceptance, close_proxy, headline_premium, render_consistency, machine_projection_consistency, promotion_blockers, source→engine→rendered-panel, sha256) and explicit non-claims ("This PR does **not** claim the public served dashboard is already refreshed. The user-facing render/publication lane is being handled separately on the incumbent production path.", "No producer/storage/consumer authority changed."). Every paragraph identifies the contract being bounded, the receipt being named, or the gate being preserved; nothing reads as front-facing marketing copy.
5. **Handoff language is doctrine-correct.** The "Durable truth correction" section explicitly identifies the only durable state change ("The registry notes and regression test are updated so the canonical Data OS source no longer says these live stores are merely proposed.") and the "Verification" section explicitly enumerates the test files (`tests/test_dataos_registry.py tests/test_china_gold_premium_engine_contract.py` → **81 passed**) without a tier assertion. The closing line "This PR does **not** claim the public served dashboard is already refreshed" is the honest-null grammar CLAUDE.md §Epistemics requires for an L1 source promotion whose rendered effect is still handled on the incumbent production path.
6. **The `promotion_blockers: []` framing is honest about the ceiling.** PR body states `promotion_blockers: []` AND `close_proxy_dataos_promotion_ready=true` together, immediately followed by "The user-facing render/publication lane is being handled separately on the incumbent production path." This is exactly the doctrine-correct "Data OS ready, but the render/publication lane is a separate workstream" framing — it does not claim the user-facing lane has flipped, only that the source-plane preconditions are now satisfied.
7. **License fingerprint is preserved verbatim.** Both promoted rows' `notes` retain the `DEC:CNLI-TUSHARE-COMPLIANCE-IS-CHAIRMAN-VERIFIED-PRIVATE` private-agreement carve-out and the `research/licenses/MASSIVE_ENTITLEMENT_RECORD.md` pointer; the new text drops only the "promotion requires" justification, which is now moot. No new compliance claim is introduced; no DEC reference is rewritten.
8. **ZH parity is N/A.** The PR carries no Chinese strings — the registry and test are English-only by convention; this matches every prior dataos(commodities) source promotion in the workstream (the rendered China-side surface copy, where any would exist, lives in `templates/` and was not touched here).
9. **The new regression assertion `assert "PRODUCED 2026-09-20" in item.notes` is reviewer-facing discipline, not user copy.** It anchors the test to a literal date stamp so a future status flip that forgets to update the note string fails the suite; it does not surface to any reader path.

Net new banned-vocab occurrences in user-visible strings: **0** (zero user-visible strings exist in the diff).

## Theme findings

TP-0 art-direction law in force (operator 2026-08-27): dark and light are TWO art directions, not one skin. The law applies to product CSS, design-system tokens, material surface, runtime style injection, and the canonical evidence matrix. PR #7527 carries NONE of these surfaces.

**Verdict: PASS (N/A — no CSS, no JS, no design-system, no theme asset touched).**

1. **Zero CSS / JS / HTML asset files in the diff.** `grep -E "\.(css|scss|less|js|j2|html|jinja|tmpl)"` on the two touched file paths returns nothing — both files are `.yml` and `.py` under `config/` and `tests/`. The `scripts/check_design_system.py --mode enforce-added --diff-file <(git diff <base>..<head> -U0 -- templates/ tests/ site/ mockups/)` filter naturally excludes `config/`, and there is nothing to flag in `templates/` because `templates/` is untouched.
2. **No new color literals introduced.** The repo-wide rule is "no raw hex in templates". The PR touches zero template files; `config/dataset_registry.yml` carries zero color values (it is a contract schema, not a styled surface).
3. **No runtime style injection.** `scripts/check_runtime_style_injection.py` looks for `style.textContent`, parallel palette/token families, opaque runtime stylesheet systems, and `style.setProperty('--*', …)` inside page/composer JavaScript. The PR adds zero bytes of JavaScript; the touched `.py` file is a pytest assertion module, not a page/composer runtime; the touched `.yml` file has no CSS or DOM hooks.
4. **No evidence matrix required.** The standing "material UI packet must carry dark × light × EN/ZH × desktop/mobile evidence" rule is a USER-FACING packet rule; this PR's packet is a registry-state L1 promotion (a status-flip + matching regression-test rewrite, both anchored to a protected-main backfill run id, exact-main acceptance receipt, and bound artifact sha256s) — a fundamentally different evidence class (registry-state correctness, not visual presentation). The "Verification" section in the PR body makes this distinction explicit (it cites the 81-test pytest count and the receipt-block numbers, not a UI matrix).
5. **Paired G6 wire sync is N/A.** `python3 -m scripts.check_template_site_sync` is the canonical paired-asset guard for `templates/<name>` ↔ `site/<name>`; the PR does not edit any template file, so the sync is trivially preserved.
6. **No token, palette, or font reference added.** The Data OS registry YAML schema (`schema: dataset_registry.v1`) is metadata, not a styled surface; its `schema` field is the contract version, not a CSS token.

No new theme debt; no color-literal addition; no evidence-matrix gate applies.

## Validated-claims findings

The standing macro law: the word `validated` (and friends: `verified`, `proofed`, `certified`, `approved`, `gauntlet`, etc.) is CI-enforced via `scripts/check_validated_claims.py`; context/data/detection/tagging artifacts stay display-tier until they clear the gauntlet; the word "validated" in user-facing text is CI-enforced (CLAUDE.md §Epistemics gauntlet = PROMOTION gate).

**Verdict: PASS.**

1. **No new template or site file — the policy check has nothing to flag.** `scripts/check_validated_claims.py` runs against `templates/` and `site/` (and a small list of author-facing markdown files); this PR touches zero template files and zero site files, so there is no new front-facing string to gate.
2. **Word-boundary grep on the diff for `validated | verified | certified | approved | proofed | gauntlet | proven`: zero NEW tier-assertion hits in the touched YAML `notes` blocks (verified — the substring `produc` is the only prefix-collide and it appears exclusively as `PRODUCED` (the status enum value) and `production` (the phrase "production quality receipt" / "incumbent production path"); the substring `validat` does not appear at all in the diff hunk; `verified` appears nowhere new; `proven` appears nowhere new).** The touched YAML `notes` carry only: "PRODUCED 2026-09-20 after protected-main source accrual plus an exact-main quality receipt", "TuShare compliance remains Chairman-verified-private/SATISFIED", "Massive capability manifest", "research/licenses/MASSIVE_ENTITLEMENT_RECORD.md". `Chairman-verified-private` is a standing DEC-anchored carve-out string (pre-existing, not introduced by this PR; verified by grepping the diff hunk — the line is the same line that existed before the rewrite, minus the dropped "promotion requires" sentence). None of these is a new tier assertion.
3. **The `status: PRODUCED` flip is an L1 source-contract graduation, not a gauntlet promotion.** The Data OS status enum (`PROPOSED` / `PRODUCED` / `RETIRED` / etc.) is the L1 source-plane lifecycle marker — it is the precondition for downstream display-tier binding, NOT a promotion to authority. The PR body's "promotion_blockers: [] / close_proxy_dataos_promotion_ready=true" receipt pair is the L1 source-side precondition check, NOT the gauntlet. Per CLAUDE.md §Epistemics, the gauntlet applies only at PROMOTION (rank/size/gate); an L1 source graduation sits one tier below that line. The PR body makes this distinction explicit: "This PR does **not** claim the public served dashboard is already refreshed. The user-facing render/publication lane is being handled separately on the incumbent production path."
4. **The receipt claims are anchored to a protected-main backfill run id and bound artifact sha256s, not to a moving branch.** PR body names `35498734408` as the protected-main run that produced the parquet stores, and `a54a6275ee55412833d909a74a32961525cfb54a51f58c0649d500f70f772c4a` (SGE) / `d135aecf1d6924e500e1d6db9c4248d369973a3a67bf542c1f8b3122838d47b1` (XAU/CNY) as the bound artifact sha256s. The four SGE reference closes (928.17 / 936.75 / 934.81 / 947.09 RMB/g for 2026-09-15 → 2026-09-18) are anchored to the parquet artifact, not to the registry row. This is the doctrine-correct "anchored to a fact, not to a branch" framing for an L1 source promotion.
5. **The regression-test rename + new assertion anchor the gate to a literal date stamp.** New assertion `assert "PRODUCED 2026-09-20" in item.notes` ensures any future `status:` flip that forgets to update the human-readable note string will fail the suite. The test docstring explicitly bounds the change as a one-shot eligibility event ("the bounded Data OS promotion eligible before these statuses changed"), not a continuous invariant — so a future demotion back to `PROPOSED` is a deliberate human action, not a regression the suite silently allows.
6. **No new authority is claimed.** The PR does NOT promote any artifact to gauntlet/authority/rank/score. It does NOT touch the rendered China-side panel, the live engine, the producer code, the storage path, the conflict_policy, the temporal_profile, the schema spec, or the code_consumers list. The Risk Radar, the F09 capital-materials producer, the calibration proposals, the live engine, and the calibration Lab are all unchanged — they remain display-tier exactly as before. PR body's "No producer/storage/consumer authority changed" line is the doctrine-correct framing.
7. **The `DEC:CNLI-TUSHARE-COMPLIANCE-IS-CHAIRMAN-VERIFIED-PRIVATE` carve-out is preserved verbatim.** The new `notes` block retains the exact DEC reference and the "private agreement material is deliberately not a repository input" sentence. This is the standing legal-status fingerprint for the China-gold source plane and is intentionally not rewritten by an L1 source graduation (a license carve-out is orthogonal to a source-status flip).
8. **The explicit non-claim sentence is honored.** PR body's "This PR does **not** claim the public served dashboard is already refreshed. The user-facing render/publication lane is being handled separately on the incumbent production path." — verified against the diff: zero bytes in `templates/`, `site/`, `mockups/`, `engine/`, `app/`, `admin/`. The non-claim is structurally honored, not just rhetorically asserted.

No new MISS; no promotion; no A7 claim; no tier raise. The L1 source graduation is exactly the right tier for the change (real store exists, source→engine→rendered-panel audit bound, render/publication lane deliberately out of scope and explicitly named).

## Overall verdict

**PASS — clean L1 Data OS source promotion; no plain-language, theme, or validated-claims defects.**

Summary table:

| dimension | verdict | reason |
| --- | --- | --- |
| plain-language | PASS (N/A) | PR touches zero user-facing bytes; no `templates/`, no `site/`, no Jinja, no HTML, no CSS, no JS. PR body is internal contract prose with explicit non-claims about the user-facing render/publication lane. |
| theme | PASS (N/A) | PR touches zero CSS / JS / design-system / theme asset bytes; `config/dataset_registry.yml` is contract metadata, not a styled surface; `tests/test_dataos_registry.py` is a pytest assertion module, not a page/composer runtime. |
| validated-claims | PASS | No new tier assertion in user-facing strings; L1 source graduation is the correct tier for the change (NOT a gauntlet/authority promotion); receipt claims are anchored to a protected-main run id + bound artifact sha256s + four independent official SGE reference closes (not to a moving branch); license carve-out preserved verbatim; explicit non-claim about the user-facing render/publication lane is structurally honored. |

**Net audit cost:** one-pass, no retries, no scope expansion, no durable result on the remote host. Audit file persisted to `orch/audits/macro_PR-7527.mm.md`.