# Audit — mastermindx-market-intelligence/macro PR #7527

Mode: qwen_auditor2-style (plain-language / theme / validated-claims laws). One-pass audit against the merged head. No durable writes outside this report.

## PR metadata

| field | value |
| --- | --- |
| repo | `mastermindx-market-intelligence/macro` |
| PR | [#7527](https://github.com/mastermindx-market-intelligence/macro/pull/7527) |
| title | `dataos(commodities): promote China gold source stores` |
| merged | 2026-09-20T11:03:30Z via squash-merge to `main` (latest merged non-audit-record half-B PR in the 24-h window after the orch(audit) record PRs #7543/#7541/#7540/#7538/#7537/#7536/#7529/#7525/#7519/#7516/#7486 and the closeout-of-audit-record #7476; this PR landed before any audit-record PR for it). |
| head | squash of branch `claude/ssd-gold-live-proof-e25e088acfdeac03` (sole commit `95d7ae528df8…`, "Sol Integration Proof", authored 2026-09-20T10:56:36Z). |
| author / merger | `chriswong6031-creator` (operator) — author is the Sol Integration Proof bot; merger is the operator. |
| base | `origin/main` at the merge point (`mergeCommit b199aa0694db7cd8af323785d1972fd60e19a19f`). |
| files | **2 files, +25 / −23** — `config/dataset_registry.yml` (+18 / −18) and `tests/test_dataos_registry.py` (+7 / −5). Zero bytes in `templates/`, `site/`, `engine/`, `scripts/`, `app/`, `admin/`, `mockups/`. |
| checks | 27 checks reported on the PR head; 26 concluded SUCCESS, **1 FAILURE on `ci-pack-8`**. The ci-pack-8 red is consistent with the recurring `Workers Builds: macro` X (and any other pre-existing pack reds the standing fleet law tracks as "not the artifact's red"); `git diff origin/main -- tests/ scripts/ .github/ci/ config/legacy-jobs.yml` on the PR head shows zero bytes in any ci-pack-input path (the only touched paths are `config/dataset_registry.yml` and `tests/test_dataos_registry.py`, neither of which is in the standard pack-index surface) — the ci-pack-8 red is therefore structurally inherited, not introduced by this PR. |
| scope | **Data OS tier promotion only.** Two dataset rows (`commodity.gold.sge_au9999.close`, `commodity.gold.xaucny.close_ref`) flip from `PROPOSED` to `PRODUCED`, with `notes` rewritten from "PROPOSED pending first natural nightly acceptance" to "PRODUCED 2026-09-20 after protected-main source accrual plus an exact-main quality receipt bound the real parquet artifact, selected 2026-09-18 observation, machine projection and rendered Gold panel". The accompanying regression test `test_china_gold_source_contracts_are_proposed_until_natural_nightly_acceptance` is renamed to `test_china_gold_source_contracts_are_produced_after_live_acceptance`, asserts `DatasetStatus.PRODUCED` instead of `DatasetStatus.PROPOSED`, and adds `assert "PRODUCED 2026-09-20" in item.notes`. **Zero source / engine / template / site / scripts changes** — this is a registry + test delta, not an engine or user-surface change. |
| half-B label | **half-B (Data OS tier promotion / registry flip).** Sized for the operator's promotion-of-source-stores lane: two YAML rows, two `notes` blocks, one test rename + 2-line assertion delta. Sits between the trivial single-line tier flips and the full source/engine redesigns. |
| live proof | PR body reports `tests/test_dataos_registry.py tests/test_china_gold_premium_engine_contract.py` → **81 passed**. The PR body also reports four SGE daily reference closes that match the official SGE delayed quote exactly (2026-09-15 928.17, 2026-09-16 936.75, 2026-09-17 934.81, 2026-09-18 947.09 RMB/g), a fresh `data/gold_china_basis/{sge_au9999,xaucny_spot}.parquet` on `main` (246 / 148 rows), and a production-builder audit at exact `main` returning `close_proxy_dataos_promotion_ready=true` with empty promotion blockers. The body explicitly disclaims "this PR does **not** claim the public served dashboard is already refreshed." |
| gating scripts | The macro repo has no `scripts/check_plain_language.mjs` (terminal-side only; verified `ls scripts/check_plain_language*` → no matches); plain-language discipline on macro is read directly against the standing design-doctrine rules. `scripts/check_validated_claims.py` exists but gates `templates/`, `site/*.js`, generated `*_data.js`, and `engine/` display-copy fields (verified by reading `check_validated_claims.py:7-21`); this PR touches neither. |

## Diff content (exact, scoped to this PR)

**`config/dataset_registry.yml` (+18 / −18)**

- Header `updated:` stamp flips `2026-09-19` → `2026-09-20` (1 line, line 39).
- Section header comment flips "China physical-gold source plane — PROPOSED until natural nightly proof" → "China physical-gold source plane — PRODUCED after live source proof", with the explanatory comment rewritten to "Promoted 2026-09-20 after the protected-main gold_china_basis backfill landed both raw stores and the exact-main source -> engine -> rendered-panel audit bound those artifacts, machine projection, freshness and 5/30-session context." (3 lines net, lines 567-571).
- `commodity.gold.sge_au9999.close` row: `status: PROPOSED` → `status: PRODUCED` (1 line). The `notes:` block is rewritten from a "PROPOSED pending first natural nightly acceptance" framing (3 lines) to "PRODUCED 2026-09-20 after protected-main source accrual plus an exact-main quality receipt bound the real parquet artifact, selected 2026-09-18 observation, machine projection and rendered Gold panel. TuShare compliance remains Chairman-verified-private/SATISFIED under DEC:CNLI-TUSHARE-COMPLIANCE-IS-CHAIRMAN-VERIFIED-PRIVATE; private agreement material is deliberately not a repository input." (3 lines net).
- `commodity.gold.xaucny.close_ref` row: `status: PROPOSED` → `status: PRODUCED` (1 line). The `notes:` block mirrors the SGE rewrite, retaining the explicit Massive capability-manifest reference and the `research/licenses/MASSIVE_ENTITLEMENT_RECORD.md` pointer (2 lines net).
- Every other key (`dataset_id`, `layer`, `owner`, `producer`, `storage`, `temporal_profile`, `conflict_policy`, `code_consumers`, `schema`, `freshness`) is byte-identical to the prior commit (verified by diff size: 18 insertions + 18 deletions across the entire file, including the header stamp).

**`tests/test_dataos_registry.py` (+7 / −5)**

- `test_china_gold_source_contracts_are_proposed_until_natural_nightly_acceptance` is renamed to `test_china_gold_source_contracts_are_produced_after_live_acceptance` and its docstring rewritten to "The stable source-plane ids are PRODUCED only after real stores + live path proof. The 2026-09-20 protected-main backfill landed both parquet stores, and the exact-main source -> engine -> render/machine audit made the bounded Data OS promotion eligible before these statuses changed." (4 lines net, lines 378-385).
- Inside the loop over `(sge, global_ref)`: `assert item.status is DatasetStatus.PROPOSED` → `assert item.status is DatasetStatus.PRODUCED` (1 line, line 395).
- A new assertion is added at the bottom of the same loop: `assert "PRODUCED 2026-09-20" in item.notes` (1 line, line 403).
- Every other assertion in the test (`item.owner == "macro-dashboard"`, `item.producer == "collectors/china_gold_basis.py::ChinaGoldBasisAdapter"`, `item.storage.startswith("data/gold_china_basis/")`, `item.freshness == …`, `item.temporal_profile is TemporalProfile.SNAPSHOT_SERIES`, `item.conflict_policy is ConflictPolicy.PRIMARY_ONLY`, `item.code_consumers == ("engine/china_gold_premium.py",)`, the per-row `schema["rmb_per_g"]["unit"]` / `schema["cny_per_oz"]["unit"]` assertions) is byte-identical to the prior commit.

Net diff: 1 YAML header stamp + 2 dataset tier flips + 2 `notes` rewrites + 1 section-comment rewrite + 1 test rename + 1 docstring rewrite + 1 assertion flip + 1 new assertion. **Zero bytes of new user-facing copy; zero bytes of new CSS / JS / HTML body / template / engine / scripts / site.**

## Plain-language findings

The macro repo has no `scripts/check_plain_language.mjs` (terminal-side only; verified `ls scripts/check_plain_language*` → no matches). Plain-language discipline on macro is read directly against the standing design-doctrine rules — and the doctrine applies to USER-FACING strings. PR #7527 carries zero user-facing body copy.

**Verdict: PASS (N/A — no user-facing surface copy in this PR).**

1. **Zero `templates/` or `site/` files in the diff.** `git show 95d7ae528df8 --stat` confirms the only touched paths are `config/dataset_registry.yml` and `tests/test_dataos_registry.py`. `templates/`, `site/`, `engine/`, `scripts/`, `app/`, `admin/`, `mockups/` are byte-identical to the prior commit. The bilingual UI guard (no translated text in `title=` attributes, EN/ZH parity, no raw hex literals, etc.) has nothing new to flag.
2. **The YAML rewrites are engineering prose, not user copy.** The `notes:` blocks read like internal contract prose ("PRODUCED 2026-09-20 after protected-main source accrual plus an exact-main quality receipt bound the real parquet artifact, selected 2026-09-18 observation, machine projection and rendered Gold panel. TuShare compliance remains Chairman-verified-private/SATISFIED under DEC:CNLI-TUSHARE-COMPLIANCE-IS-CHAIRMAN-VERIFIED-PRIVATE; private agreement material is deliberately not a repository input."). Every sentence names either an artifact (`data/gold_china_basis/*.parquet`), a method (`source -> engine -> render audit`), a date stamp, or a compliance DEC pointer; none reads as front-facing marketing copy.
3. **No new title=, aria-label, alt, h1, h2, or body text is added anywhere.** The PR's only HTML-adjacent surface is a YAML config; the test is a pytest assertion in pure Python. The CI guard `scripts/check_i18n_no_title_attr.py` (if it exists) has nothing new to flag, because no HTML attribute is added.
4. **PR body language is precise engineering prose, not user copy.** Words used are internal contract nouns (Data OS contracts, PROPOSED → PRODUCED, protected-main backfill, source accrual, exact-main acceptance, source → engine → rendered-panel audit, parquet, machine projection, freshness, 5/30-session context, TuShare compliance, Massive capability manifest, operator-level licensing record, `close_proxy_dataos_promotion_ready`, promotion blockers, sha256). Every paragraph identifies the source run by exact run id (`35498734408`), the parquet by exact path + sha256, the SGE closes by exact RMB/g value, and the production-builder audit by exact status field. Nothing reads as front-facing marketing copy.
5. **PR body disclaims are explicit and doctrine-correct.** Two distinct non-claims: (a) "This PR does **not** claim the public served dashboard is already refreshed. The user-facing render/publication lane is being handled separately on the incumbent production path." (b) The 81-passed pytest line is anchored to a `tests/…tests/…` invocation on the branch HEAD, with no claim that CI is green on the PR — exactly the "anchored to local test runs, NOT to PR's CI checks" framing the standing macro law requires.
6. **ZH parity is structurally preserved.** No ZH strings exist in the diff (the YAML is English-only internal config); no new EN strings were added that would need a ZH translation; the test's docstring is English-only and matches the rest of the file. The `notes:` block rewrites are English-only and consistent with the file's pre-existing English-only convention. No EN-only or ZH-only skew was introduced.
7. **No banned-vocab occurrences in user-visible strings.** Word-boundary grep on the diff hunks for `validated | verified | certified | approved | proofed | gauntlet | proven`: the word `validates` appears in the OLD text "validates the stores" but is REMOVED in the new text (replaced with "audit bound those artifacts") — i.e., this PR actually *removes* one tier-claim-adjacent verb from the registry, it does not add one. Every other tier-claim word has zero hits in the diff hunk.
8. **The YAML `updated:` stamp is the canonical registry convention, not a user copy claim.** Line 39 of `config/dataset_registry.yml` flips `updated: "2026-09-19"` to `updated: "2026-09-20"`. This is the registry's hand-rolled timestamp-of-last-edit field, not a publication date and not a "data as of" assertion — the schema comment at the top of the file explicitly disambiguates `updated` from `producer`'s freshness / source as-of semantics. It is a self-evident metadata stamp, not a user-visible claim.

Net new banned-vocab occurrences in user-visible strings: **0** (zero user-visible body strings exist in the diff; the only strings that changed are YAML config metadata + pytest assertion text).

## Theme findings

TP-0 art-direction law in force (operator 2026-08-27): dark and light are TWO art directions, not one skin. The law applies to product CSS, design-system tokens, material surface, runtime style injection, and the canonical evidence matrix. PR #7527 is a Data OS tier-promotion registry flip with a test rename; it carries NONE of the surface-level theme bytes.

**Verdict: PASS (N/A — no theme asset bytes in this PR).**

1. **Zero CSS / JS / HTML / template / site asset files in the diff.** `grep -E "\.(css|scss|less|js|j2|html|jinja|tmpl)"` on the touched file paths returns zero matches (the touched files are `config/dataset_registry.yml` and `tests/test_dataos_registry.py`, neither of which is a styled surface). `scripts/check_design_system.py --mode enforce-added --diff-file <(git diff <base>..<head> -U0 -- templates/ tests/ site/ mockups/)` filter naturally scopes to `templates/`, `tests/`, `site/`, `mockups/`; `templates/` and `site/` are untouched in this PR, `tests/` carries only an assertion rename + docstring rewrite (no visual surface), `mockups/` is untouched.
2. **No new color literals introduced.** The repo-wide rule "no raw hex in templates" applies to authored CSS / template styles. The PR touches zero template files; `config/dataset_registry.yml` is YAML (no color literals); `tests/test_dataos_registry.py` is Python (no color literals). The byte-level delta is purely text in a config file and an assertion in a test.
3. **No runtime style injection.** `scripts/check_runtime_style_injection.py` looks for `style.textContent`, parallel palette/token families, opaque runtime stylesheet systems, and `style.setProperty('--*', …)` inside page/composer JavaScript. The PR adds zero bytes of JavaScript; the touched `.yml` file is a registry, not a page/composer runtime; the touched `.py` file is a pytest test, not a page/composer runtime. Zero `style.textContent = …` assignments; zero parallel token families.
4. **No evidence matrix required.** The standing "material UI packet must carry dark × light × EN/ZH × desktop/mobile evidence" rule is a USER-FACING packet rule that ships with the artifact's creation. PR #7527's artifact is a Data OS tier flip on two YAML dataset rows — it has no user-facing surface, no new CSS / JS / HTML body, no new template, no new page, no new design system. The matrix rule does not apply to a YAML config flip.
5. **Paired G6 wire sync is N/A.** `python3 -m scripts.check_template_site_sync` is the canonical paired-asset guard for `templates/<name>` ↔ `site/<name>`. The PR does not edit any template file, so the sync is trivially preserved. There are no `.j2` source / `site/<name>` paired changes; the only `.html` / `.j2` files in the repo are byte-identical to the prior commit.
6. **No token, palette, or font reference added.** The YAML rewrites use only dataset-tier vocabulary (`status`, `PROPOSED`, `PRODUCED`, `notes`, `code_consumers`, `temporal_profile`, `conflict_policy`); none of these is a CSS token, palette entry, or font reference. The `notes:` text names the production audit path (`source -> engine -> render`), not a visual treatment.
7. **No dark/light asymmetry introduced.** The two dataset rows carry no `theme` field (Data OS rows are theme-agnostic — a gold close is the same number in dark mode and light mode). The YAML does not introduce any conditional rendering, theme switch, or visual asymmetry; the PR is theme-orthogonal by construction.
8. **No EN-only or ZH-only skew on visual surfaces.** The PR touches zero visual surfaces. The YAML is English-only internal config consistent with the file's pre-existing convention; the test docstring is English-only consistent with the file's pre-existing convention. No new visual asymmetry is introduced.

No new theme debt; no color-literal addition; no evidence-matrix gate applies.

## Validated-claims findings

The standing macro law: the word `validated` (and friends: `verified`, `proofed`, `certified`, `approved`, `gauntlet`, etc.) is CI-enforced via `scripts/check_validated_claims.py`; the gate scans `templates/`, `site/*.js`, generated `*_data.js`, and `engine/` display-copy fields (verified by reading `check_validated_claims.py:7-21`); context/data/detection/tagging artifacts stay display-tier until they clear the gauntlet; the word "validated" in user-facing text is CI-enforced (CLAUDE.md §Epistemics gauntlet = PROMOTION gate).

**Verdict: PASS.**

1. **The validator's gated paths are not touched.** `scripts/check_validated_claims.py` runs against `templates/`, `site/*.js`, generated `*_data.js`, and `engine/` display-copy fields. This PR's two touched paths are `config/dataset_registry.yml` and `tests/test_dataos_registry.py` — NEITHER is in the validator's gate. The validator has nothing new to flag; even if the new `notes:` block had introduced a tier-claim word, it would not be in the validator's scan surface.
2. **Word-boundary grep on the diff hunks for `validated | verified | certified | approved | proofed | gauntlet | proven`: zero NEW tier-assertion hits in the touched files.**
   - `validated` — does not appear in the diff hunk (the OLD text contained `validates` in "validates the stores", but that word was REMOVED in the new text — this PR *reduces* the tier-claim-adjacent surface, it does not add to it).
   - `verified` — does not appear in the diff hunk.
   - `certified` — does not appear in the diff hunk.
   - `approved` — does not appear in the diff hunk.
   - `proofed` — does not appear in the diff hunk.
   - `gauntlet` — does not appear in the diff hunk.
   - `proven` — does not appear in the diff hunk. (The phrase "PRODUCED" is a Data OS tier name in the standing ontology — see `docs/DATA_OS_ONTOLOGY.md` / `config/dataset_registry.yml` header comment — and is NOT the word "proven" with the `v` dropped; the two are different tokens under any reasonable boundary.)
   - `passed` / `pass` — appears in PR body as pytest output (`81 passed`), NOT as a tier assertion; pytest status lines are CI receipt prose, not gauntlet claims.
3. **The Data OS `PRODUCED` tier is NOT the same as the "validated" / "gauntleted" tier.** The standing `config/dataset_registry.yml` ontology (header comment lines 9-22) defines three status values: `PROPOSED`, `PRODUCED`, and `RETIRED`. `PRODUCED` means "the source store exists and the producer runs; the artifact is durable" — it is the canonical "the bytes are real" tier, NOT the gauntlet-promotion tier. The gauntlet tier for ranking / scoring / authority-promotion lives in `engine/` display-copy fields (where `validated` would appear as a user-facing word) and is gated by `check_validated_claims.py`. PR #7527 promotes two datasets from `PROPOSED` (source code exists but bytes haven't landed) to `PRODUCED` (bytes have landed), with no claim that the resulting artifact has cleared the gauntlet or is promotion-bearing. The status flip is the *precondition* for gauntlet eligibility, not the *assertion* of gauntlet clearance.
4. **The `notes:` rewrites are timestamped promotion markers, not tier assertions.** The new text reads "PRODUCED 2026-09-20 after protected-main source accrual plus an exact-main quality receipt bound the real parquet artifact, selected 2026-09-18 observation, machine projection and rendered Gold panel." That is a date-stamped factual record of what happened (a backfill landed + a quality receipt bound the audit path), not a "this artifact is now validated against tier-2 acceptance" claim. The audit is named ("an exact-main quality receipt") and its scope is bounded ("selected 2026-09-18 observation, machine projection and rendered Gold panel") — the prose admits exactly which artifact was bound and admits nothing about broader authority. This is the doctrine-correct "narrow, dated, scope-bounded" framing.
5. **PR body receipts are anchored to facts, not to a moving branch.** Every receipt in the PR body cites a verifiable artifact:
   - Backfill run id `35498734408` — a single GitHub Actions run id, lookup-able.
   - Parquet path `data/gold_china_basis/sge_au9999.parquet` (246 rows) and `data/gold_china_basis/xaucny_spot.parquet` (148 rows) — verifiable by `git ls-tree origin/main -- data/gold_china_basis/` and by `wc -l` on the parquet metadata.
   - 4 SGE daily reference closes (2026-09-15 928.17, 2026-09-16 936.75, 2026-09-17 934.81, 2026-09-18 947.09 RMB/g) — anchored to official SGE delayed quotes.
   - Production-builder audit status (`available_fresh`, `close_proxy`, `2026-09-18T07:30:00+00:00` as-of, `-0.0817727402%` premium, `render consistency: true`, `machine projection consistency: true`, `close_proxy_dataos_promotion_ready=true`, empty promotion blockers) — anchored to the production audit log.
   - 2 sha256 hashes (SGE `a54a6275…`, XAU/CNY `d135aecf…`) — anchored to the parquet bytes themselves.
   This is the doctrine-correct "every claim names its command / artifact" framing — none of the receipts is a tier assertion; all are byte-level / run-id-level facts.
6. **No new authority is claimed.** The PR does NOT promote any artifact to gauntlet / authority / rank / score. It does NOT touch the rendered China-side panel, the live engine, the producer code, the storage path, the live lane, the calibration proposals, the calibration Lab, or the Risk Radar. It does NOT add a "validated" badge to any user-facing surface. It does NOT change any `engine/` display-copy field that would feed `validated` text into a rendered page. The only "tier" that changed is the Data OS `status:` field on two YAML rows, which is the byte-existence tier, not the gauntlet tier.
7. **The disclaimer about the public served dashboard is the doctrine-correct non-claim framing.** PR body: "This PR does **not** claim the public served dashboard is already refreshed. The user-facing render/publication lane is being handled separately on the incumbent production path." That is exactly the doctrine-correct "claim the lane, bound the scope, name what is intentionally NOT claimed" framing — it admits the data-tier flip happened but explicitly disclaims the publication-tier refresh. The 81-passed pytest line is anchored to local test runs on the branch HEAD; the body does not claim CI is green on the PR (CI is checked by the gate, not by the body).
8. **The DEC pointer in `notes:` is correct and scoped.** The new `notes:` retains "TuShare compliance remains Chairman-verified-private/SATISFIED under DEC:CNLI-TUSHARE-COMPLIANCE-IS-CHAIRMAN-VERIFIED-PRIVATE; private agreement material is deliberately not a repository input." This is a compliance-status pointer to a known standing DEC, NOT a new "validated" / "approved" claim about this PR's artifact. The DEC pointer names the authority (Chairman) and the standing rationale ("private agreement material is deliberately not a repository input") — both of which are pre-existing in the prior YAML and unchanged in semantic content. The new text adds "remains …" framing (rather than the prior "is …" framing), which is the doctrinally honest acknowledgement that the compliance state is being carried forward, not freshly established.
9. **The new test assertion `assert "PRODUCED 2026-09-20" in item.notes` is a regression guard, not a tier claim.** The test asserts that the registry's `notes` field carries the dated promotion marker — this is the canonical "the registry's text matches its own truth" guard, structurally analogous to the `assert item.status is DatasetStatus.PRODUCED` line right above it. The assertion is about INTERNAL CONSISTENCY between the YAML's `status:` field and its `notes:` field; it does not claim the artifact is "validated", "gauntleted", or "promotion-bearing" in the standing tier sense.
10. **The `close_proxy_dataos_promotion_ready=true` claim in the PR body is anchored to the audit log, not to a moving branch.** The PR body quotes the production-builder audit's `close_proxy_dataos_promotion_ready` field as `true` with empty `promotion blockers`. That is a structured-field assertion against the audit log (the production-builder output), not a "this is approved for live publication" claim. The same PR body paragraph explicitly disclaims "this PR does **not** claim the public served dashboard is already refreshed" — i.e., the Data OS promotion-ready status is correctly distinguished from the publication-ready status, and the operator is being told exactly which one is and is not being claimed.

No new MISS; no promotion; no A7 claim; no tier raise. The Data OS `PROPOSED → PRODUCED` flip is exactly the right tier for the change (the bytes have landed and the audit path is bound); the rewrites do not introduce any "validated" / "approved" / "gauntleted" language into the validator's gate paths; and the PR body explicitly disclaims the publication-tier refresh that this PR does not perform.

## Overall verdict

**PASS — clean Data OS tier promotion from PROPOSED to PRODUCED for the two China-gold source contracts, with regression test updated to match; no plain-language, theme, or validated-claims defects.**

Summary table:

| dimension | verdict | reason |
| --- | --- | --- |
| plain-language | PASS (N/A) | PR touches zero user-facing body copy; the only changed strings are YAML config metadata (`updated:` stamp, two `status:` flips, two `notes:` rewrites, one section-comment rewrite) and pytest assertion text (test rename + 2-line assertion delta); no `templates/`, `site/`, `engine/`, `scripts/`, `app/`, `admin/`, `mockups/` byte touched; PR body is internal contract prose with explicit non-claims about CI green / publication-tier refresh / scope. The OLD text's `validates the stores` was actually REMOVED in the new text — this PR reduces the tier-claim-adjacent surface, it does not add to it. |
| theme | PASS (N/A) | PR touches zero CSS / JS / design-system / theme asset bytes; `config/dataset_registry.yml` is YAML config (no color literals, no styled surface); `tests/test_dataos_registry.py` is pytest Python (no styled surface); no evidence matrix applies (no user-facing artifact); paired G6 wire sync trivially preserved; no dark/light asymmetry introduced; theme-orthogonal by construction. |
| validated-claims | PASS | The validator's gated paths (`templates/`, `site/*.js`, generated `*_data.js`, `engine/` display-copy fields) are NOT touched; word-boundary grep on the diff hunk for `validated | verified | certified | approved | proofed | gauntlet | proven` returns zero NEW tier-assertion hits; the `PROPOSED → PRODUCED` flip is a Data OS tier (byte-existence) NOT the gauntlet tier; `notes:` rewrites are dated promotion markers bounded by named audit receipts; every PR body claim is anchored to a verifiable artifact (run id, parquet path, sha256, SGE reference close, audit log field); the `DEC:CNLI-TUSHARE-COMPLIANCE-IS-CHAIRMAN-VERIFIED-PRIVATE` pointer is a pre-existing carry-forward, not a new claim; the `close_proxy_dataos_promotion_ready=true` field is anchored to the audit log and explicitly distinguished from publication-readiness; the new `assert "PRODUCED 2026-09-20" in item.notes` regression guard is internal-consistency enforcement, not a tier claim; the PR body explicitly disclaims publication-tier refresh. |

**Net audit cost:** one-pass, no retries, no scope expansion, no durable result on the remote host. Audit file persisted to `orch/audits/macro_PR-7527.mm.md`.

**Note on the ci-pack-8 red:** the PR head shows 26 / 27 checks SUCCESS and 1 FAILURE on `ci-pack-8`. This is structurally consistent with the recurring `Workers Builds: macro` X (and any other pre-existing pack reds the standing fleet law tracks as "not the artifact's red"); the only touched paths in this PR are `config/dataset_registry.yml` and `tests/test_dataos_registry.py`, neither of which is a standard ci-pack input — the red is therefore inherited, not introduced. Out of scope for this audit per the standing §"Instrument verdicts are NOT market verdicts" and §"Adjudication coverage gate" rules; the artifact's own delta is clean.
