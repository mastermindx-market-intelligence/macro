# Audit — mastermindx-market-intelligence/macro PR #7492

Mode: qwen_auditor2-style (plain-language / theme / validated-claims laws). One-pass audit against the merged head. No durable writes outside this report.

## PR metadata

| field | value |
| --- | --- |
| repo | `mastermindx-market-intelligence/macro` |
| PR | [#7492](https://github.com/mastermindx-market-intelligence/macro/pull/7492) |
| title | `feat(risk): audit recorded probabilities against matched outcomes` |
| merged | 2026-09-20T07:13:03Z via squash-merge to `main` (labelled `merge-on-green`; most-recent merged half-A/risk PR in the 24-h window at audit time, after the orch/audit-record PRs #7537/#7536/#7529/#7525/#7519/#7516/#7486/#7476/#7470/#7468/#7460/#7454 and the already-audited MO-B / MO-A PRs #7527, #7523, #7514, #7511, #7498, #7482, #7472, #7469, #7467, #7465, #7458, #7451). |
| head | squash of branch `claude/risk-radar-forecast-evidence-20260920` (sole commit `bf06db19d10bd85ec3ad32b83723b27c3b546d4c`). PR body cites exact head `bf06db19d10bd85ec3ad32b83723b27c3b546d4c`. |
| author / merger | PR author is Sol session (`claude/risk-radar-forecast-evidence-20260920`, recorded as `model: sol` in the agentos handoff); merger is `mastermidx4`. |
| base | `origin/main` at `83746deb2f3f4ce2de6ef4e683d66e9337087e38` (predecessor of the live main when the PR was opened). |
| files | **21 files, +1570 / −0.** Two engine/test files (engine + 2 test files) + 1 agentos handoff + 2 research markdown + 4 evidence artefacts under `research/grey_deer/evidence/probability-audit-20260920/` + 1 evidence pack of 8 PNGs + 1 `dom-proof.json` + 1 `manifest.json` + 1 `smells.json` under `mockups/evidence/risk-radar-forecast-evidence-20260920/parent-live/`. Zero bytes in `templates/`, `site/`, `app/`, `admin/`. |
| scope | **Additive US-only probability diagnostic on the existing `risk_radar_scorecard` producer.** The producer's existing fields (`monitoring`, `windows`, etc.) are byte-equal to the parent build (verified — `legacy_fields_unchanged: true`, ledger SHA-256 unchanged at `612c4ecb5cfe1d794b42953ca0d0c8c9d5f43a294e7b03e60d10f74e5fc468a6`). A new key `probability_audit` is added under the `us` market entry, carrying horizons h5/h10/h21 with eligible/excluded counts, mean forecast, observed ≥5% SPY pullback rate, Brier loss, paired same-row baseline comparison, fixed reliability bins, and explicit `publication_timing_verified: false` / `current_model_validated: false` / `status: descriptive_only` flags. NO new template, NO new site file, NO new JS/CSS, NO new risk model, NO probability tuning, NO scheduler, NO ledger writer, NO promotion gate. PR body explicit non-claims: "Display-only; never a forecast or promotion gate." |
| half-A label | **half-A (risk side, US-only, additive on existing owner).** The PR extends `engine/risk_radar_scorecard.py` which is the canonical half-A risk producer (per the workstream WS:GREY-DEER-RISK-INTELLIGENCE tracked in `agentos/handoffs/`). The workstream handoff (`agentos/handoffs/GREY-DEER-RISK-INTELLIGENCE-2026-09-20-PROBABILITY-EVIDENCE.md`) cites `workstream: WS:GREY-DEER-RISK-INTELLIGENCE`, `session: claude/risk-radar-forecast-evidence-20260920`, `model: sol`, `ended_because: blocked` (render lane blocked; diagnostic/reader path only). The PR body itself is "real research" framing (no half-A/half-B tag), but the workstream taxonomy is half-A risk research. |
| pre-reg | Protocol-only commit `9659c1b7b41efc6c54e32c3cd79ba91c060e287b` precedes outcome inspection (PR body §Real research result). Base `9a481ef520e397853c9f5a8edd120cfff528ce16`. Pre-reg doc: `research/grey_deer/RISK_RADAR_PROBABILITY_AUDIT_PREREG_2026-09-20.md` (+44 lines). |
| live proof | PR body reports `python3 -m pytest tests/test_risk_radar_scorecard.py tests/test_rr_scorecard_card.py tests/test_risk_radar_audit.py -q` → **88 passed**. Eight public browser cells (en/zh × dark/light × 1440/390) for the parent release PR7482 are preserved as evidence under `mockups/evidence/risk-radar-forecast-evidence-20260920/parent-live/`. Live JS SHA-256: `47a6eb04c763621586810accfc35777113ccaa982be3a520716f97f989428abd`. The PR explicitly states the new diagnostic is **not deployed in Risk Radar UI** ("the intended compact probability-table UI creation was platform-blocked before execution and confirmed absent. It was not retried, renamed or inlined. This PR therefore claims the diagnostic/real research/reader path only, NOT a new visible probability table or production deployment."). The `scripts/check_plain_language.mjs` referenced in the task prompt is **terminal-side only** and does NOT exist in `mastermindx-market-intelligence/macro` (verified — `ls scripts/check_plain_language*` → no matches); plain-language discipline on macro is read directly against the standing design-doctrine rules (see §Plain-language findings below). |

## Diff content (exact, scoped to this PR)

**Engine producer (1 file, +128 / −0)**

`engine/risk_radar_scorecard.py`: two `**({"probability_audit": probability_audit(...) ...} if market == "us" else {})` injected keys added inside `_market_entry(...)`'s two return-dict sites (one for empty-row path, one for full path). Both are conditional on `market == "us"` — no-op for non-US markets. New module-level helpers (`_probability_number`, `_probability_date`, `_probability_clock`), one new public function `probability_audit(rows, today=None)`, one new constant `_PROBABILITY_BINS = ((0., .1), (.1, .2), (.2, .4), (.4, .6), (.6, 1.))`, one new constant `_PROBABILITY_TARGET = ">=5% SPY pullback (empirical 2006-2026; rises with intensity + conjunction)"`. The new function returns a `us_issued_probability_audit.v1` dict with: `definition`, `as_of`, `target`, `input_rows`, `min_display_n`, `horizons` (h5/h10/h21 each carrying `n`, `excluded_n`, `excluded`, `from`, `through`, `events`, `both_outcomes_present`, `mean_forecast`, `observed_rate`, `brier`, `paired_n`, `missing_baseline_n`, `paired_model_brier`, `paired_base_brier`, `paired_brier_delta`, `bins`), `status: "descriptive_only"`, `sample_unit: "overlapping_daily_forecast"`, `publication_timing_verified: false`, `current_model_validated: false`, `note_en`, `note_zh`. NO change to existing `monitoring`, `windows`, or any other prior field; the legacy dict shape is preserved byte-equal (verified by the PR's own `legacy_fields_unchanged: true` claim and the tests' `test_probability_oracle_and_no_input_mutation` which asserts the input rows are not mutated and the existing scorecard fields stay equal).

**Test additions (2 files, +165 / −0)**

`tests/test_risk_radar_scorecard.py`: +145 lines of new tests covering the probability audit. Key assertions (verified by reading the test bodies in the diff): `test_probability_oracle_and_no_input_mutation` asserts (n=5, mean_forecast=.2, observed_rate=0., brier=.04) on h21 and (paired_n=5, paired_model_brier=.04, paired_base_brier=.01, paired_brier_delta=+.03) — the `+0.03` paired delta in the SYNTHETIC oracle is the SIGNATURE of an audit that does NOT silently flip sign under synthesis; `test_probability_small_sample_is_not_zero` ([0,1,4] parametrize) asserts sub-`min_display_n=5` rows return `None` not `0.0` for `mean_forecast`, `observed_rate`, `brier`, `paired_brier_delta` — this is the doctrine-correct "nulls printed, not zero" grammar (CLAUDE.md §Epistemics); `test_probability_rejects_invalid_numbers_per_horizon` parametrize over `[None, True, False, "0.2", -.1, 1.1, NaN, Inf, 10**400]` asserts only the bad-`h5` row is excluded (h21 stays n=5) — exclusion is per-horizon, not whole-row; `test_probability_matched_baseline_never_uses_different_rows` asserts `paired_n=5` and `missing_baseline_n=1` when one row's `base_h21=None` but its `h21=.9` is valid — the model-row is not erased, only the paired comparison is gated.

`tests/test_rr_scorecard_card.py`: +20 lines asserting the existing Market State reader (`scorecard_card.py`) preserves the new `probability_audit` block and the legacy nulls pass through.

**Agentos handoff (1 file, +77 / −0)**

`agentos/handoffs/GREY-DEER-RISK-INTELLIGENCE-2026-09-20-PROBABILITY-EVIDENCE.md`: YAML frontmatter + a 9-line tail paragraph. Frontmatter declares `workstream: WS:GREY-DEER-RISK-INTELLIGENCE`, `session: claude/risk-radar-forecast-evidence-20260920`, `model: sol`, `ended_because: blocked` (blocked = UI creation, not the diagnostic). The `verified:` block lists four `verified:` claims each with a literal `command:` and `result:`. The `unverified:` block lists three explicit `unverified:` items including "the new probability diagnostic is deployed and visible in Risk Radar" and "the forward sample validates today's model or its downside discrimination" — both explicitly named `unverified:` with `what_would_verify:` chains. The `unresolved:` block names three honest gaps. The `do_not_redo:` block names four DNR items. The `danger_areas:` block names three. The `prs: [7467, 7482]` block links this PR to its predecessor PRs. Cold-stranger test passes (every `verified:` claim has a literal `command:` invocation).

**Pre-reg + results research docs (2 files, +108 / −0)**

`research/grey_deer/RISK_RADAR_PROBABILITY_AUDIT_PREREG_2026-09-20.md` (+44 lines): the frozen descriptive protocol — fixed evaluation (target = recorded ≥5% SPY close-relative pullback within h5/h10/h21; only recorded `drawdown_prob[horizon]`, `base_[horizon]`, `graded.hit[horizon].dd5`; no recomputation; no substituting today's calibration); explicit "no p-values, no confidence intervals, no independent-N claim, no significance, no calibration PASS, no trading permission from this audit"; fixed bins `[0,.1) … [.6,1]`; existing `min-n=5` disclosure floor (not a validation threshold); null below floor, never zero; daily overlapping forecasts are NOT independent episodes. Method references cited (Gneiting & Raftery 2007; scikit-learn calibration docs). Authority line: "no new probability, tuning, rank/size/gate/execute authority or GD-5 promotion is permitted."

`research/grey_deer/RISK_RADAR_PROBABILITY_AUDIT_RESULTS_2026-09-20.md` (+64 lines): the real-ledger result doc — finding stated explicitly as "the forward record cannot yet establish downside discrimination"; ledger SHA-256 `612c4ecb5…`; 51 recorded forecast dates 2026-06-23 → 2026-09-17; 33 eligible graded dates; 18 ungraded; means 2.87%/7.70%/15.55% across h5/h10/h21; paired comparisons use 29 dates (4 early rows lack h5/h10 baselines); explicit non-claim "Lower historical Brier loss against the recorded base is NOT validated downside skill: this sample has no positive events and daily windows overlap." The doc matches the `result.json` numbers exactly.

**Evidence artefacts (4 files, +494 / −0)**

`research/grey_deer/evidence/probability-audit-20260920/`:
- `agentos.log` (+262 lines): the running log of the audit session, including phantom-artifact warnings and a clear `agentos: validate` pass. (Note: the phantom-artifact warnings are inherited cross-repo `agentos` registry drift, NOT introduced by this PR — verified the same warning shape exists on prior audit PRs.)
- `discrimination.json` (+22 lines): null-discrimination verdict under the zero-event sample — the audit correctly refuses to claim discrimination.
- `result.json` (+207 lines): full machine-readable audit result. Per horizon: `n: 33, excluded_n: 18, excluded: {"ungraded": 18}, from: "2026-06-23", through: "2026-08-18", events: 0, both_outcomes_present: false, paired_n: 29 (h5/h10) or 33 (h21), paired_brier_delta: -0.000492 / -0.001617 / -0.007184`. Bins are populated correctly: all 33 h5 rows in [0,.1); 29 h10 rows in [0,.1) + 4 in [.1,.2); 33 h21 rows in [.1,.2). The `events: 0` and `both_outcomes_present: false` flags are the canonical signal that no discrimination claim follows.
- `tests.log` (+3 lines): the 88-passed receipt line.

**Browser evidence pack — PR7482 parent release (11 files, +599 / −0)**

`mockups/evidence/risk-radar-forecast-evidence-20260920/parent-live/`:
- 8 PNGs: `703ca1f65df9a898.png` (1440 dark en), `c88f0d8d5341ff6c.png` (1440 light en), `bd3a2d58a4b2b294.png` (1440 dark zh), `4f9e1d32a61b4164.png` (1440 light zh), `2ceb85b81a425c2f.png` (390 dark en), `41d5b52065f44244.png` (390 light en), `68c6450c92e95edb.png` (390 dark zh), `8ffc973e1cfab0c9.png` (390 light zh). All 8 cells are captured with sha256 values bound to the byte-rolled PNGs.
- `dom-proof.json` (+118 lines): 8 `observations` entries each with `width`, `theme`, `lang`, `http_status: 200`, `inside_radar: true`, `hidden_at_rest: true`, `hint_count: 1`, `hint_inside_existing_button: true`, `hint: "Fragile internals内部脆弱"`, `old_panel: false`, `page_overflow: false`. The 8 cells are symmetric across en/zh × dark/light × 390/1440.
- `manifest.json` (+399 lines): the page-evidence manifest from `scripts/capture_page_evidence.py` (module sha256 `8d753e28…`, version `1.3.0`). Captures `page_id: "macro.html"`, `selection.mode: "explicit_routes"`, `totals: {pages: 1, states_attempted: 8, states_captured: 8}`. The `gaps:` array names `requires authenticated session; not automatable without approved fixtures` for free/essential/pro access tiers and `state not synthesizable against static output` for loading/empty/stale/error page_states — the gap acknowledgement is the doctrine-correct honest-null grammar.
- `smells.json` (+81 lines): page-evidence smell report (no banned-vocab occurrences, no raw-slug hits, no TODO placeholder hits).

**Total surface change**: zero bytes in any rendered file. The diagnostic is wired into the existing `_market_entry(...)` US branch only; the parent module's existing fields are byte-equal (`legacy_fields_unchanged: true` is a recorded receipt, not an assertion). Zero `templates/` bytes, zero `site/` bytes, zero `app/` or `admin/` bytes, zero `engine/` bytes outside the one producer file, zero test bytes outside the two test files.

## Plain-language findings

The macro repo has no `scripts/check_plain_language.mjs` (terminal-side only; verified `ls scripts/check_plain_language*` → no matches). Plain-language discipline on macro is read directly against the standing design-doctrine rules — but the doctrine applies to USER-FACING strings, and PR #7492 carries zero user-facing bytes. There is no `templates/`, no `site/`, no Jinja copy, no rendered HTML body delta, no HTML attribute added.

**Verdict: PASS (N/A — zero user-facing surface in this PR; the explicit PR-body claim "This PR therefore claims the diagnostic/real research/reader path only, NOT a new visible probability table or production deployment" is doctrine-correct plain-language disclosure).**

1. **No templates/ touched.** `grep -E "^\+\+\+ b/templates/" <diff>` returns zero matches — the 21-file diff contains no `templates/_*.html.j2`, no `templates/_*.css.j2`, no `templates/*.js`, no `templates/_risk_probability_audit.html.j2` (PR body §Scope: "templates/_risk_probability_audit.html.j2 write was blocked before execution; original path confirmed absent. No retry or alternate implementation."). The plain-language page can't fire on a PR that ships no Jinja.
2. **No site/ touched.** `grep -E "^\+\+\+ b/site/" <diff>` returns zero matches — the paired-site G6 wire-sync cycle does not apply. No body HTML byte changes anywhere on disk that a reader could hit.
3. **The only English + Chinese strings added are in `engine/risk_radar_scorecard.py`'s returned `note_en` and `note_zh` keys**, both of which are producer-output strings carried in the diagnostic dict and intended for downstream rendering — but the renderer that would surface them is NOT in this PR (PR body §Scope: "the renderer lane remains explicitly blocked until its platform gate changes"). The strings themselves read as the doctrine-correct honest-null grammar:
   - `note_en: "Historical issued odds, not a test of today's model. Daily windows overlap; publication timing is unverified."`
   - `note_zh: "历史发布概率，并非当前模型验证。每日窗口重叠；首发时点未经核实。"`
   Both are short, plain, factual, and explicitly non-promotional. The ZH string is a faithful translation of the EN string ("Historical issued probability, not a current-model validation. Daily windows overlap; first-publication timing unverified."). No internal state name, no study slug, no raw stat in either string.
4. **PR body language is precise engineering/research prose, not user copy.** Words used are internal contract nouns (issued-probability diagnostic, display-only, descriptive_only, overlap sample unit, publication_timing_verified, current_model_validated, paired same-row recorded base, fixed bins, eligible/excluded counts, frozen descriptive protocol, ledger SHA-256, prereg commit, immutable source, additive block, byte-equal, hostiled/missing-data tests, matched-denominator oracle, discrimination against parent, real-ledger diagnostic) and explicit non-claims ("no new probability, tuning, rank/size/gate/execute authority or GD-5 promotion is permitted", "Display-only; never a forecast or promotion gate", "Lower historical Brier loss against the recorded base is NOT validated downside skill", "the forward record cannot yet establish downside discrimination", "no p-values, no confidence intervals, no independent-N claim, no significance, no calibration PASS, no trading permission from this audit"). Every paragraph identifies the contract being bounded, the receipt being named, or the doctrine being preserved.
5. **Handoff language is doctrine-correct.** The agentos handoff's `ended_because: blocked` (UI creation), `verified:` block with literal `command:`/`result:` chains, `unverified:` block with three explicit `what_would_verify:` chains, `unresolved:` block with three honest gaps, `do_not_redo:` block with four DNR items, `danger_areas:` block with three. This is the cold-stranger test passing — every `verified:` claim names its command.
6. **The 33-eligible / 18-ungraded split is honest.** The audit does NOT impute the 18 ungraded dates, does NOT call all 51 rows independent episodes, and does NOT promote the paired_brier_delta (which is negative on all three horizons) as "skill". The PR body §Real research result: "no calibration/weight/probability/trading-authority change follows." That is the doctrine-correct honest framing for a paired-Brier-delta that COULD be overread as "model beats baseline" but where the sample has zero events and the windows overlap.
7. **The PR7482 parent-release evidence carries the existing user-facing copy, not new copy.** The 8 PNGs are captures of the page PR7482 already released; the DOM proof enumerates the existing button hint `"Fragile internals内部脆弱"` (which is bilingual EN+ZH and reads as the design-doctrine-correct glance-tier phrasing — fragments are not explained, jargon is paired with a Chinese rendering, no internal state name leaked). The PR does NOT add or rewrite this hint; it preserves it as the parent artifact's evidence pack.
8. **ZH parity is preserved for the only two bilingual strings introduced** (`note_en` + `note_zh` in the producer return dict). Both are short, factual, doctrinally-aligned. ZH is a faithful translation of EN, not a literal character-for-character mapping. No ZH-only or EN-only skew.

Net new banned-vocab occurrences in user-visible strings: **0** (zero user-visible strings exist in the diff; the only EN/ZH strings added are inside the producer's return dict and only land in user-visible surfaces after the deliberately-blocked renderer gate opens).

## Theme findings

TP-0 art-direction law in force (operator 2026-08-27): dark and light are TWO art directions, not one skin. The law applies to product CSS, design-system tokens, material surface, runtime style injection, and the canonical evidence matrix. PR #7492 carries NONE of these surfaces — it is an additive data-layer diagnostic on an existing producer, with all visual surfaces unchanged.

**Verdict: PASS (N/A — no CSS, no JS, no design-system, no theme asset touched; the parent-release evidence pack covers the existing art direction's cells).**

1. **Zero CSS / JS / HTML asset files in the diff.** `grep -E "\.(css|scss|less|js|j2|html|jinja|tmpl)"` on the 21 touched file paths returns only `engine/risk_radar_scorecard.py` (Python, not CSS/JS/HTML), `tests/test_risk_radar_scorecard.py` (Python test, not CSS/JS/HTML), and `tests/test_rr_scorecard_card.py` (Python test, not CSS/JS/HTML). The remaining 17 files are `.md` (3 docs), `.json` (4 evidence artefacts), `.png` (8 browser captures), `.txt`/`.log` (1 log). The `scripts/check_design_system.py --mode enforce-added --diff-file <(git diff <base>..<head> -U0 -- templates/ tests/ site/ mockups/)` filter naturally excludes `engine/`, `research/`, `agentos/`; and the only `mockups/` files are evidence JSONs/PNGs (no new design assets).
2. **No new color literals introduced.** The repo-wide rule is "no raw hex in templates". The PR touches zero template files; `engine/risk_radar_scorecard.py`'s new lines are pure-Python numeric/dict/string manipulation (no CSS, no HTML, no color literals even in the returned dict). The 8 PNGs are raster outputs that re-capture the parent-release art direction; their colors are inherited from the existing `theme.css` and are not authored in this PR.
3. **No runtime style injection.** `scripts/check_runtime_style_injection.py` looks for `style.textContent`, parallel palette/token families, opaque runtime stylesheet systems, and `style.setProperty('--*', …)` inside page/composer JavaScript. The PR adds zero bytes of JavaScript; the touched `.py` files are server-side producer code, not page/composer runtimes; the touched `.json` files are evidence fixtures, not page/composer runtimes; the touched `.png` files are raster outputs.
4. **No evidence matrix required for the new diagnostic itself.** The standing "material UI packet must carry dark × light × EN/ZH × desktop/mobile evidence" rule is a USER-FACING packet rule that ships with the artifact's creation. The new artifact is the `probability_audit` dict inside `risk_radar_scorecard.py`'s US branch — and that dict is NOT a user-facing surface in this PR (PR body §Scope: "the intended compact probability-table UI creation was platform-blocked before execution and confirmed absent"). The PR's own evidence pack under `research/grey_deer/evidence/probability-audit-20260920/` is the correct evidence class for a frozen-protocol diagnostic: `result.json` (machine-readable), `discrimination.json` (null-discrimination verdict under zero events), `tests.log` (88-passed receipt), `agentos.log` (session log). That evidence class is fundamentally different from the visual-presentation evidence class the matrix rule governs.
5. **The 8-cell browser evidence pack covers the parent-release art direction.** The PR inherits the parent release PR7482's evidence under `mockups/evidence/risk-radar-forecast-evidence-20260920/parent-live/`: 8 PNGs × en/zh × dark/light × 1440/390 × macro.html. The DOM proof shows `hint_count: 1`, `hint_inside_existing_button: true`, `page_overflow: false`, `old_panel: false` on all 8 cells — the existing button hint `"Fragile internals内部脆弱"` is inside the existing button on every cell. This is the symmetric cell coverage the matrix rule requires for the parent release; the new diagnostic inherits it without re-minting.
6. **No token, palette, or font reference added.** The PR does not introduce a new color token, does not modify `theme.css`, does not modify `navigation-refresh.css`, does not modify any token file. The new `engine/risk_radar_scorecard.py` code references no token, palette, or font — it only reads/writes Python dicts of probabilities and Brier losses.
7. **Paired G6 wire sync is N/A.** `python3 -m scripts.check_template_site_sync` is the canonical paired-asset guard for `templates/<name>` ↔ `site/<name>`. The PR does not edit any template file, so the sync is trivially preserved. The `templates/_risk_probability_audit.html.j2` path that PR body §Unresolved names as "write was blocked before execution; original path confirmed absent" is also a non-existent path on main (PR body confirms it), so no paired-asset violation is even possible.
8. **Dark/light symmetry on the inherited evidence pack is preserved.** Receipt structure (verified in `mockups/.../parent-live/manifest.json`): each locale has 4 PNGs covering dark/light × 1440/390; each theme has 4 PNGs covering en/zh × 1440/390; each viewport has 4 PNGs covering en/zh × dark/light. All 8 sha256s are unique; no cell is silently dropped.

No new theme debt; no color-literal addition; no evidence-matrix gate applies to the additive data-layer diagnostic (the parent release's matrix is preserved unchanged).

## Validated-claims findings

The standing macro law: the word `validated` (and friends: `verified`, `proofed`, `certified`, `approved`, `gauntlet`, etc.) is CI-enforced via `scripts/check_validated_claims.py`; context/data/detection/tagging artifacts stay display-tier until they clear the gauntlet; the word "validated" in user-facing text is CI-enforced (CLAUDE.md §Epistemics gauntlet = PROMOTION gate). PR #7492 is a display-tier diagnostic — exactly the kind of artifact §Epistemics says stays display-tier until it clears the gauntlet.

**Verdict: PASS — the audit is exemplary in its non-promotion discipline; `current_model_validated: false` is a literal flag in the producer's return dict, the danger-areas block names the false-skill risk, and the unverified-claims block explicitly bounds the conclusions.**

1. **No new template or site file — the policy check has nothing to flag.** `scripts/check_validated_claims.py` runs against `templates/` and `site/` (and a small list of author-facing markdown files); this PR touches zero template files and zero site files, so there is no new front-facing string to gate.
2. **Word-boundary grep on the diff for `validated | verified | proofed | certified | approved | gauntlet | proven`: every occurrence is the EXPLICIT NON-CLAIM form, no NEW tier-assertion hits** (verified across all 21 touched files in the diff):
   - `validated` — appears as `current_model_validated: false` (engine.py, 2 sites: empty-row path and full path; result.json §audit field; `note_zh`/`note_en` context). Every occurrence is `False`, asserting the OPPOSITE of validation. This is the doctrine-correct enforcement of "validated" in machine-readable form: the engine refuses to call itself validated.
   - `validation` — appears in `tests/test_risk_radar_scorecard.py` as `audit["current_model_validated"] is False` (test asserts `False`); `agentos/handoffs/GREY-DEER-RISK-INTELLIGENCE-2026-09-20-PROBABILITY-EVIDENCE.md` `unverified:` block: "the forward sample validates today's model or its downside discrimination" — this is in the `unverified:` block, explicitly named as NOT verified; PR body §Real research result: "no calibration/weight/probability/trading-authority change follows"; PR body §Capability: "No … current-model validation claim"; pre-reg doc §Fixed evaluation: "This is not out-of-sample validation"; pre-reg doc §Acceptance: "Research result is historical/descriptive, not proof of current-model validity". Every occurrence is the explicit non-claim.
   - `verified` — appears in the handoff's `verified:` block (with literal `command:`/`result:` chains for four claims — protocol-before-outcome, 88-passed test, ledger-hash-unchanged, PR7482-publicly-served) and `unverified:` block (three claims explicitly named unverified). The `verified:` block is the agentos handoff schema (every claim names its command), not a tier-assertion.
   - `certified` — does not appear in the diff hunk.
   - `approved` — does not appear in the diff hunk.
   - `proof` — appears as `proofed`, `proof`, `proof-of-publication` contexts, all in the explicit non-claim form ("A receipt order is NOT proof of publication before the first outcome bar", "the diagnostic is not proof of current-model validity"). No tier assertion.
   - `gauntlet` — does not appear in the diff hunk. (The PR explicitly declines to invoke the gauntlet: "display only, not model promotion", "no GD-5 promotion is permitted".)
   - `proven` — does not appear in the diff hunk.
   - `pass` / `passed` — appears as pytest output (`88 passed in 9.69s`, in `tests.log`), NOT as a tier assertion; pytest status lines are CI receipt prose, not gauntlet claims.
3. **No NEW promotion occurred.** The PR is an additive US-only diagnostic on an existing producer; it does NOT promote any artifact to gauntlet/authority/rank/score/size/gate/execute. The pre-reg doc §Capability and boundaries: "No new probability, tuning, rank/size/gate/execute authority or GD-5 promotion is permitted." The producer's return dict explicitly asserts `status: "descriptive_only"`, `sample_unit: "overlapping_daily_forecast"`, `current_model_validated: false`, `publication_timing_verified: false`. The result doc §Finding: "the forward record cannot yet establish downside discrimination." The discrimination.json explicitly states null-discrimination. PR body §Real research result: "no calibration/weight/probability/trading-authority change follows."
4. **The data-half landing is NOT promoted to authority.** The audit's central numeric result is a paired_brier_delta of `-0.000492` (h5), `-0.001617` (h10), `-0.007184` (h21) — negative in all three horizons, which would naively read as "model Brier loss is lower than baseline Brier loss". The PR refuses to promote this. Pre-reg doc §Fixed evaluation: "Daily overlapping forecasts are not independent episodes: no p-values, confidence intervals, independent-N claim, significance, calibration PASS or trading permission from this audit." PR body §Real research result: "Lower historical Brier loss against the recorded base is NOT validated downside skill: this sample has no positive events and daily windows overlap." Danger-areas block: "No positive target events means lower Brier loss can come merely from forecasting less risk; it is not validated skill." This is precisely the doctrine-correct honest-null framing for a fact that COULD be overread as "model beats baseline" but where the sample is structurally unsuited to that claim.
5. **The 18 ungraded dates are honestly nulled.** `result.json` per horizon shows `excluded_n: 18, excluded: {"ungraded": 18}, n: 33`. The audit does NOT impute the missing grades, does NOT call the 51 rows independent episodes, does NOT call the 33 eligible rows an "independent sample". The pre-reg §Fixed evaluation: "Deduplicate exact identical dates once; exclude all conflicting duplicates. Never pick a winner." The result doc: "33 have usable recorded grades; 18 have no recorded grade. This audit neither imputes those outcomes nor assumes all 18 are immature."
6. **The 4 missing h5/h10 baselines are honestly nulled.** `result.json` shows `paired_n: 29, missing_baseline_n: 4` for h5 and h10 (paired_n: 33, missing_baseline_n: 0 for h21). The audit does NOT erase the model rows (`n: 33` stays); it only gates the PAIRED comparison. The test `test_probability_matched_baseline_never_uses_different_rows` enforces this contract: missing baseline excludes the row from the paired block but not from the model's own brier. PR body §Real research result: "Four early short-horizon baselines are absent, so h5/h10 comparisons use 29 matched dates, not 33."
7. **The PR body explicitly enumerates what this PR is NOT.** PR body §Scope and remaining gates: "the intended compact probability-table UI creation was platform-blocked before execution and confirmed absent. It was not retried, renamed or inlined. This PR therefore claims the diagnostic/real research/reader path only, NOT a new visible probability table or production deployment. No templates or generated site data are changed." PR body §Capability: "Per h5/h10/h21: … No imputation of old baselines, boolean coercion, selection among conflicting duplicate dates, or current-model validation claim." PR body §Verification: "Four deliberate faults were rejected: mismatched baseline denominators, invented missing baselines, wrong-horizon outcomes, and a false validation claim." This is the doctrine-correct explicit non-claim grammar.
8. **The agentos handoff's `do_not_redo:` block binds.** `do_not_redo:` names four items: PR7467/PR7482 are accepted at their documented proof level (no re-litigation); do not alter scores/probabilities/risk-policy permission/ledgers/GD-3 natural-event proof; PR6989/PR7236/PR6685 retain their own holds and writers; never fill missing historical baselines from today's calibration. These DNR items are explicit, source-anchored, and test-enforceable.
9. **The agentos handoff's `danger_areas:` block is the explicit risk acknowledgement.** Three danger areas named: "No positive target events means lower Brier loss can come merely from forecasting less risk" (the structural-underreading risk the audit deliberately refuses); "Logged-before-graded does not prove publication before the first outcome bar or homogeneous model identity" (the receipt-order-is-not-proof disclosure); "The concurrent unified dashboard hero is preserved; do not overwrite the whole rendered page with this branch's older artifact" (the cross-PR overwrite risk). Each is named explicitly with its remedy.

No new MISS; no promotion; no A7 claim; no tier raise; the audit is exemplary in its non-promotion discipline.

## Overall verdict

| gate | result |
| --- | --- |
| plain-language (read directly; macro has no `check_plain_language.mjs`) | **PASS — N/A** (zero user-facing bytes in the diff; no templates/, no site/, no Jinja copy, no HTML, no CSS, no JS, no rendered attribute; PR body uses precise engineering/research contract nouns with explicit non-claims and explicit lists of what this PR is NOT; the only EN/ZH strings introduced are producer-output `note_en`/`note_zh` carried in the diagnostic dict and explicitly NOT rendered in this PR per the deliberately-blocked renderer lane; the handoff `ended_because: blocked` and `verified:`/`unverified:`/`unresolved:`/`do_not_redo:`/`danger_areas:` blocks are doctrine-correct cold-stranger-test grammar) |
| theme (`check_design_system.py --mode enforce-added` + CSS audit + evidence matrix) | **PASS — N/A** (zero CSS/JS/HTML asset files in the diff; no color-literal addition; no runtime style injection; no paired G6 sync needed; no evidence matrix required for an additive data-layer diagnostic on an existing producer that deliberately ships no new UI; the parent-release PR7482's 8-cell browser evidence pack is preserved unchanged under `mockups/evidence/risk-radar-forecast-evidence-20260920/parent-live/`) |
| validated-claims (`check_validated_claims.py`) | **PASS** (zero front-facing string to gate — the PR does not edit templates/ or site/; every word-boundary `validated`/`validation`/`verified`/`proof` occurrence in the diff is the EXPLICIT NON-CLAIM form: `current_model_validated: false` is a literal flag in the producer's return dict, the handoff `unverified:` block explicitly names three unverified claims with `what_would_verify:` chains, the pre-reg §Fixed evaluation explicitly forbids "p-values, confidence intervals, independent-N claim, significance, calibration PASS, trading permission from this audit", and the PR body §Capability §Verification §Real research result §Scope and remaining gates enumerate what this PR is NOT four times; the audit is exemplary in its non-promotion discipline) |
| display-tier discipline (CLAUDE.md §Epistemics: context/data/detection/tagging stays display-tier; gauntlet = PROMOTION gate, NOT a build gate) | **PASS** (the `probability_audit` dict is display-tier: `status: "descriptive_only"`, `current_model_validated: false`, `publication_timing_verified: false`, `sample_unit: "overlapping_daily_forecast"`; the result.json discrimination verdict is null under the zero-event sample; no gauntlet invocation; no A7 claim; the artifact is the canonical "honest-null display-tier diagnostic" — it is published to the scorecard, not promoted to authority) |
| pre-reg integrity | **PASS** (protocol-only commit `9659c1b7b41efc6c54e32c3cd79ba91c060e287b` precedes outcome inspection per PR body §Real research result; pre-reg doc §Authority/§Operation/§Capability and boundaries/§Fixed evaluation/§Acceptance/§Method references is a complete frozen descriptive protocol with explicit non-claims; per-horizon eligible/excluded counts, mean forecast, observed rate, paired baseline comparison, fixed bins, and explicit "no independent-N claim / no significance / no trading permission" language; immutable source `9a481ef520e397853c9f5a8edd120cfff528ce16`; ledger SHA-256 `612c4ecb5cfe1d794b42953ca0d0c8c9d5f43a294e7b03e60d10f74e5fc468a6` is unchanged by this PR) |
| legacy-preservation contract | **PASS** (`legacy_fields_unchanged: true` is a recorded receipt; test `test_probability_oracle_and_no_input_mutation` asserts input rows are not mutated by the audit and the existing scorecard fields stay byte-equal; ledger SHA-256 before/after this PR is identical at `612c4ecb5…`; the producer's existing `monitoring`, `windows`, and every other prior field are unchanged; `tests/test_rr_scorecard_card.py` proves the existing Market State reader preserves the new diagnostic block and the legacy nulls pass through) |
| rejection of deliberate faults (PR body §Verification) | **PASS** (four deliberate faults were rejected: mismatched baseline denominators, invented missing baselines, wrong-horizon outcomes, and a false validation claim. PR body cites the test log: `88 passed in 9.69s`. The deliberate-fault ledger entry is the doctrine-correct gate-sensitivity proof) |
| independent review | N/A (the agentos handoff `model: sol` records this as a Sol-principal direct-execution bounded diagnostic; no child START, no source-writer transfer, no alternate publishing system — `Direct execution was PRINCIPAL_JUDGMENT / LOWER_TOTAL_OVERHEAD`. The handoff `verified:` block names the four claims with literal `command:` chains; `unverified:` names three claims explicitly unverified; `unresolved:` names three honest gaps. The review model is principal-self-verification with full receipts) |
| handoff / workstream continuity | **PASS** (`agentos/handoffs/GREY-DEER-RISK-INTELLIGENCE-2026-09-20-PROBABILITY-EVIDENCE.md` declares the workstream `WS:GREY-DEER-RISK-INTELLIGENCE`, the session `claude/risk-radar-forecast-evidence-20260920`, the model `sol`, the predecessor PRs `[7467, 7482]`, the `do_not_redo:` and `danger_areas:` blocks; cold-stranger test passes; no second store, no local mirror; the PR is registered against the workstream registry that already exists) |
| honest-null / display-tier grammar | **PASS** (the 18 ungraded dates are nulled in `excluded: {"ungraded": 18}`, not imputed; the 4 missing h5/h10 baselines are nulled in `paired_n: 29, missing_baseline_n: 4`, not silently replaced by today's calibration; the `events: 0` and `both_outcomes_present: false` flags are explicit; the `mean_forecast`, `observed_rate`, `brier`, `paired_brier_delta` fields are `None` (not `0.0`) below the `min_display_n=5` floor per `test_probability_small_sample_is_not_zero`; the `note_en` and `note_zh` strings name the overlap-window and timing-unverified limits explicitly) |

**Overall: PASS.** This is a half-A (risk) additive US-only probability diagnostic on the existing `risk_radar_scorecard` producer. The PR adds a single new `probability_audit` key under the US market entry (no-op for non-US markets) that returns a `us_issued_probability_audit.v1` dict with explicit display-tier non-promotion flags (`status: descriptive_only`, `current_model_validated: false`, `publication_timing_verified: false`, `sample_unit: overlapping_daily_forecast`). The pre-reg protocol-only commit `9659c1b7` precedes outcome inspection; the immutable source is `9a481ef5`; the ledger SHA-256 `612c4ecb5…` is unchanged by this PR. 51 recorded forecast dates, 33 eligible graded, 18 ungraded (correctly nulled, not imputed). Paired baseline comparisons use 29 matched dates on h5/h10 (4 early rows lack h5/h10 baselines) and 33 on h21 (no missing baselines). The paired_brier_delta is negative on all three horizons (-0.000492 / -0.001617 / -0.007184), which the audit EXPLICITLY REFUSES to promote to "model beats baseline" or "validated skill" because the sample has zero positive events and the daily windows overlap. Zero new debt across plain-language, theme, validated-claims, display-tier discipline, pre-reg integrity, legacy-preservation, rejection-of-deliberate-faults, independent review, handoff continuity, or honest-null grammar. The macro repo's `scripts/check_plain_language.mjs` referenced in the task prompt does not exist in this repo — plain-language discipline on macro is read directly against the standing design-doctrine rules, which apply to USER-FACING strings, and this PR ships zero user-facing bytes (the producer-output `note_en`/`note_zh` are deliberately not rendered in this PR per the explicitly-blocked renderer lane).
