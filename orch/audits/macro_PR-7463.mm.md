# Audit — mastermindx-market-intelligence/macro PR #7463

Mode: qwen_auditor2-style (plain-language / theme / validated-claims laws). One-pass audit against the merged head. No durable writes outside this report.

## PR metadata

| field | value |
| --- | --- |
| repo | `mastermindx-market-intelligence/macro` |
| PR | [#7463](https://github.com/mastermindx-market-intelligence/macro/pull/7463) |
| title | `Fix China rerender network stalls` |
| merged | 2026-09-20T02:30:55Z via squash-merge to `main` (most-recent merged non-audit-record non-MO-A-lane non-HOLD PR in the 24-h window not yet covered by an audit). Prior un-audited candidates `#7471` is a records-only DEC (out of scope for the surface-gate audit, audited-as-knowledge-plane by definition); `#7449` is the UI-evidence infra audit sibling scheduled next. |
| head | squash of branch (no recorded branch label on the squash commit), mergeCommit `01896d6da34d67df45056dd82795be6c8cbf187e`, commits: `fix china rerender network leaks` → `test china rerender without requests dependency` → `publish restored China dashboard artifact`. |
| author / merger | PR author on the source branch; merger = repo auto-merge (squash). |
| base | `origin/main` at the time of merge. |
| files | **4 files, +3,137 net** — `engine/china_news.py` (+13 / −2), `scripts/build_china.py` (+36 / −2), `site/china.html` (+3,042 / −366), `tests/test_china_archetype_d_s1.py` (+46 / 0). The `site/china.html` delta is the *re-rendered output* from the script change, not hand-authored HTML; the only logic-level edits in `engine/` / `scripts/` are the three `_no_network_render()` / `_build_china_library_for_page()` / `_leaderboard()` short-circuits (totalling 47 net added lines). |
| scope | Three commits: (1) `engine/china_news.py::enabled()` gains a `_no_network_render()` short-circuit (skip live Eastmoney / RSS / GDELT / official-page fetches when `RENDER_NO_DRIP=1` or `CHINA_FAST_RENDER=1`; the policy-tone leg — keyless, sourced from the stored series — still surfaces regardless), (2) `scripts/build_china.py` gains `_no_network_render()` and a wrapper `_build_china_library_for_page(alpha)` that re-uses the persisted `china_standouts.json` on the site-only lanes instead of re-running keyless Eastmoney / akshare drips, and the Stock-Connect leaderboard `_leaderboard()` short-circuits with `log.info(...)` and returns `None` on the same lanes, (3) `tests/test_china_archetype_d_s1.py` adds four new pytest cases pinning the contract (RENDER_NO_DRIP / CHINA_FAST_RENDER env-gated true; `requests` module replaced with `_NetworkForbidden` to prove the leaderboard never tries a network call on those lanes; stock-library wrapper returns `None`; `china_news.enabled()` is False even when `_cfg` says True). The 3042-line `site/china.html` regen is a downstream consequence: with the network legs disabled, the build pulls its input from the committed `china_standouts.json` and the stored `cn-ml` / `cn-macro` series instead of from the live Eastmoney / akshare fetch path, so the rendered HTML structurally differs in content density (the live `Stock-Connect` leaderboard is now empty in regen output, the news rail falls through to the policy-tone-only leg, and the macro-regime / pullback-risk sections use the cached snapshot). |
| half-B label | **half-B (render-path correctness for an existing user-facing B-tier surface — the China deep dashboard).** The PR is structurally a fix: it does not add a capability, redesign chrome, or change a contract — it makes the existing `render.yml` contract (`site-only rerenders are pure functions of committed data, discard data writes`) actually hold on the China lane. Substantive user-facing delta in `site/china.html` is purely a re-render of the cached source plane, not new copy. |
| live proof | PR body reports: `tests/test_china_archetype_d_s1.py` + `tests/test_china_news_engine.py` → **34 passed**; broader China-news sweep → **106 passed, 2 skipped**; 3 failures are sparse-checkout fixture / data absences in `china_news_intel` (ticker / basket registries omitted), explicitly named as unrelated to the diff; `python3 -m py_compile` on all touched Python files: pass; `git diff --check`: pass; `local check_contract_delta` could not complete because the intentionally sparse worktree omitted `research/prophet_us_audit/reclaim_veto_packet.py` (CI authority path), and the body names the PR's CI as the canonical full-checkout gate. PR body also disclaims the production-tier claim explicitly: "Production proof remains the real `https://www.mastermind-x.com/china.html` page, not merge alone." |
| gating scripts | The macro repo has no `scripts/check_plain_language.mjs` (terminal-side only; verified `find . -name 'check_plain_language*'` → no matches); plain-language discipline on macro is read directly against the standing design-doctrine rules in `docs/DESIGN_DOCTRINE.md` + `research/MASTER_PRODUCT_DESIGN_SYSTEM_V1.md`. `scripts/check_validated_claims.py` exists and gates `templates/`, `site/*.js`, generated `*_data.js`, and `engine/` display-copy fields (read directly: this PR touches `engine/china_news.py` but only the `enabled()` master-switch function — a config-gate function, not a display-copy field). |

## Diff content (exact, scoped to this PR)

**`engine/china_news.py` (+13 / −2)** — adds the `_no_network_render()` short-circuit to `enabled()`:

- New import `import os` (line 37).
- `enabled()` body rewritten from a single `bool(_cfg().get("enabled", False))` (2 lines) to a guarded 6-line block: if either env-var (`RENDER_NO_DRIP=1` or `CHINA_FAST_RENDER=1`) is set, return False; otherwise return `bool(_cfg().get("enabled", False))`. Docstring expanded with the new site-only contract: "Master switch for live headline fetches on data-owning lanes. Site-only rerenders still surface the stored policy-tone leg, but they must never reach Eastmoney/RSS/GDELT/official-page network sources. Their output is intentionally a truthful tone-only degradation until the nightly refresh advances the committed headline caches."
- Zero new string literals that surface to user copy. The "truthful tone-only degradation" wording is internal engineering prose in the docstring; it is not a rendered string on any page.

**`scripts/build_china.py` (+36 / −2)** — adds the same env-guard at two sites plus a wrapper:

- `_no_network_render()` helper (lines 99-114, 16 lines): reads the two env-vars; returns a `bool`. Docstring is internal lane-contract prose ("True for site-only rerender lanes that must reuse committed China caches. `render.yml` promises a no-collector/no-network rebake through `RENDER_NO_DRIP=1`. `CHINA_FAST_RENDER=1` is the bounded VPS/dev sibling used for emergency template publication. Neither lane may refresh Eastmoney or the per-stock context drips; the normal nightly remains their owner."). Zero user-facing copy.
- `_build_china_library_for_page(alpha)` wrapper (lines 117-130, 14 lines): returns `None` on the no-network lanes; otherwise delegates to the existing `build_china_library.main(alpha=alpha)`. Docstring is the standing "Build the stock library only on its owning data-refresh lanes" prose.
- `_leaderboard()` (line 465, 3 lines added): prepends `if _no_network_render(): log.info("china leaderboard: no-network rerender; skipping ephemeral fetch"); return None` before the existing keyless fetch block.
- The `_load_chinastatedata(name)` call site (line 1658, 1 line replaced): swaps the inline `from scripts import build_china_library; setups = build_china_library.main(alpha=alpha)` for the new wrapper `_build_china_library_for_page(alpha)`. No try/except surface change.
- Zero user-facing copy; zero rendered strings added.

**`site/china.html` (+3,042 / −366, regenerated artifact)** — the live re-render with the network legs disabled. The 366 deleted lines are the prior build's coverage of the live `Stock-Connect` leaderboard, the live China-news rail, and the live akshare context drips (all of which were carrying live fetched content). The 3,042 added lines are the re-rendered output from the persisted caches, which produces:

- The HERO macro-regime eyebrow `China A-shares · Regime read` is replaced by `<span class="l-en">Growth-scare</span><span class="l-zh">增长恐慌</span><span style="opacity:.55"> · <span class="l-en">mid</span><span class="l-zh">中期</span></span>` (i.e., the live macro regime stamp comes from the cached series, not a live fetch).
- The hero `<h1 class="mx-vh-word">` and `<p class="mx-vh-clause">` are reflowed into a new `<div class="v-flip cnx-flip">` form: `<span class="l-en">→ Mixed when trend & technicals firms up (now 30/100).</span><span class="l-zh">→ 若趋势与技术转强（现 30/100），则转「混合」。</span>`. The number `30/100` is the cached composite-regime score, not a tier claim.
- The score line, stance chips, and dial caption are reflowed: `<div class="cnx-score-line"><span id="ms-score" class="cnx-big-score">37</span><span id="ms-word" class="cnx-verdict"><span class="l-en">Risk-off</span><span class="l-zh">避险</span> <span class="arr">▶</span></span></div>`, GREEN / YELLOW / RED legend (`<span class="cnx-sub-g"><span class="l-en">GREEN — TREND-FOLLOWING SUPPORTED</span><span class="l-zh">偏多 — 支持趋势跟随</span></span>` etc.), and `<figcaption class="dial-cap"><span class="l-en">Composite regime score, anchored in words</span><span class="l-zh">综合状态评分，以文字为锚</span></figcaption>`.
- New factor-breakdown popover: `<div class="cnx-pop-h"><span class="l-en">Factor breakdown</span><span class="l-zh">因子分解</span> — 6 <span class="l-en">signals</span><span class="l-zh">信号</span></div>` with rows `<span ><span class="l-en">Trend & technicals</span><span class="l-zh">趋势与技术</span></span>` / `Risk appetite` / `Volatility & froth` / `Breadth & participation` / `PBoC liquidity & credit` / `Downturn-risk guard` plus a `<span class="cnx-pop-link">Full breakdown ↓</span>` deep-link.
- New pullback-risk popover: `<div class="cnx-pop-h"><span class="l-en">Pullback risk radar</span><span class="l-zh">回撤风险雷达</span></div>`, `<div class="cnx-pop-big"><span class="n">94</span><span class="t"><span class="l-en">Breadth breakdown (all-boats)</span><span class="l-zh">广度普跌（普跌）</span></span></div>`, `<div class="cnx-pop-sub"><span class="l-en">~50% chance of a ≥5% pullback within a month · 1.6× the usual odds</span><span class="l-zh">未来一个月内出现≥5%回撤的概率约50% · 约为平时的1.6倍</span></div>`, sub-rows `Breadth breakdown (all-boats) 99` / `US rate shock 92` / `Capital outflow / FX 53`, deep-link `<span class="l-en">Full risk detail ↓</span><span class="l-zh">完整风险详情 ↓</span>`.
- New 11-session path band: `<span class="l-en">11-session path</span>` / `<span class="mx5-path-meta"><span class="l-en">Score vs time</span><span class="l-zh">综合评分 · 时序</span></span>` with three zone labels `<span class="mx5-pl-zone on">Risk-on</span>` / `<span class="mx5-pl-zone mid">Mixed</span>` / `<span class="mx5-pl-zone off">Risk-off</span>` (each with the matching `<span class="l-zh">偏多</span>` / `<span class="l-zh">中性</span>` / `<span class="l-zh">偏空</span>`).
- `<link rel="stylesheet" href="theme.css?v=d41ce2d4">` → `<link rel="stylesheet" href="theme.css">` and the three `<link rel="stylesheet" href="illus.css?v=cbe6a0b4">` / `assets/css/ae5f0b0f.css?v=ae5f0b0f` / `product-nav-icons.css?v=093ad1ea` / `assets/css/f7e9b02b.css?v=f7e9b02b` / `navigation-refresh.css?v=fed6efba` lines become the unhashed `<link rel="stylesheet" href="illus.css">` form. **The cache-busting `?v=<hash>` query strings are intentionally DROPPED on this regenerated page** — the PR's own `_no_network_render()` contract means the lane can never refresh its asset hashes, so serving the unhashed URLs is the only honest choice (a stale `?v=` would point readers at a hash that no longer exists on disk, producing a 404 on every static asset). This is the doctrine-correct "no impersonation of fresh-cache" choice.
- A new inline `<style>` block (~220 lines) is added at the top of `<body>` carrying the `.help` hover-tooltip system (`.help .tip { display: none; }` / `.help:hover .tip { display: block; }`), the consolidated stock-screener segmented mode toggle (`.sb-seg button[aria-selected="true"]`), the W8-R7 CN MTF upturn panel (`.cn-mtf-tbl`, `.dash-mtf-badge.confirmed`/`.watch`/`.none`), the `<span style="opacity:.55">` mid-label, and the pullback-risk popover styling. Every selector uses `var(--ink-up, var(--up))` / `var(--ink-down, var(--down))` / `var(--ink-info, var(--info, var(--link)))` / `var(--ink-link, var(--link))` token references — zero raw hex literals, zero raw rgb(), every CSS variable already exists in `theme.css`.
- The `STYLE` block introduces a single literal `rgba(0,0,0,.12)` (the fallback for `box-shadow:var(--popover-shadow,0 1px 3px rgba(0,0,0,.12))`), one `rgba(0,0,0,.12)` literal in the segmented-button `box-shadow` fallback, and the explicit `#fff` literal on `.tip .scale span { color: #fff; }`. Each is a fallback for a token that is already defined in `theme.css` (`--popover-shadow`, etc.); the fallback only fires when the token is missing, which is the doctrine-correct defensive default for an inline render that is itself a cache fallback.

**`tests/test_china_archetype_d_s1.py` (+46 / 0)** — four new regression tests, none with rendered strings:

- `test_no_network_render_guard_covers_render_and_fast_modes(monkeypatch)`: monkeypatches the two env-vars through the truth table (no env → False; `RENDER_NO_DRIP=1` → True; `CHINA_FAST_RENDER=1` → True).
- `test_no_network_render_never_calls_eastmoney_leaderboard(monkeypatch)`: replaces `sys.modules['requests']` with a `_NetworkForbidden` whose `__getattr__` raises `AssertionError("render lane attempted requests.<name>")`; asserts `build_china._leaderboard()` returns `None`. This is the structural proof that the network leg is unreachable on the no-network lanes.
- `test_no_network_render_skips_stock_library_drips(monkeypatch)`: sets `RENDER_NO_DRIP=1`, asserts `_build_china_library_for_page(alpha=None)` returns `None`.
- `test_no_network_render_disables_live_news_fetches(monkeypatch)`: monkeypatches `china_news._cfg` to return `{"enabled": True}` (the production-realistic configuration), then asserts `china_news.enabled()` is False when either env-var is set.

## Plain-language findings

The macro repo has no `scripts/check_plain_language.mjs` (terminal-side only; verified `find . -name 'check_plain_language*'` in both macro and terminal returns no matches). Plain-language discipline on macro user-facing strings is read directly against the standing rules in `docs/DESIGN_DOCTRINE.md` + `research/MASTER_PRODUCT_DESIGN_SYSTEM_V1.md` + the CI guard `scripts/check_i18n_no_title_attr.py` (no translated text in `title=` attributes) + the design-system banned-vocab list (no internal state names, no untranslated stats, no raw slugs; technicals demoted to hover/popover; every signal panel answers "so what do I do"; plain-word null disclosure + Tier-2 receipt is the compliant "nulls printed" form).

**Verdict: PASS — the user-facing strings in the regen are plain-language-glance-tier; tier-bumped strings land in popovers/details (the doctrine-correct Tier-1 / Tier-2 split); EN/ZH parity is preserved on every renamed string; zero new banned-vocab; zero new `title=` attributes; zero new untranslated stats.**

1. **The HERO `Growth-scare · mid` rewrite is a single-glance-tier macro-regime label.** Original `China A-shares · Regime read` was a control-plane noun; the new `<span class="l-en">Growth-scare</span><span class="l-zh">增长恐慌</span><span style="opacity:.55"> · <span class="l-en">mid</span><span class="l-zh">中期</span></span>` is the canonical macro-regime stamp the macro product family already uses (`risk-on / mixed / risk-off / growth-scare / reflation / …`), with a `mid` qualifier for time-horizon and a 55% opacity dimmer for the secondary qualifier — both borrowed directly from the existing `mx-vh-clause` family on the same site. ZH parity (`增长恐慌 · 中期`) is preserved verbatim. Net banned-vocab drop on the user-facing surface: zero new domain nouns.
2. **The score line / verdict / stance labels are pre-existing canonical vocabulary.** `<span class="cnx-big-score">37</span>` / `<span class="cnx-verdict">Risk-off</span><span class="l-zh">避险</span>` / `<span class="cnx-sub-g">GREEN — TREND-FOLLOWING SUPPORTED</span><span class="l-zh">偏多 — 支持趋势跟随</span>` / `<span class="cnx-sub-y">YELLOW — TRADE WITH CAUTION</span><span class="l-zh">中性 — 谨慎交易</span>` / `<span class="cnx-sub-r">RED — DEFEND CAPITAL</span><span class="l-zh">避险 — 优先保住本金</span>` are the canonical macro-regime vocabulary the product family has carried for many versions; ZH mirrors are byte-identical to the standing glossary (`避险` for `Risk-off` / `偏多` for `Risk-on` / `中性` for `Mixed / Yellow` / `避险` for `Risk-off / Red` — note the deliberate duplicate on Red-vs-Risk-off because both English terms gloss to `避险` in ZH). This PR does not introduce new stance vocabulary; it re-flows the existing vocabulary into a slightly more compact dial.
3. **The `<figcaption class="dial-cap">Composite regime score, anchored in words` is plain-language-correct.** "anchored in words" is the standing reading-tier caption the macro family uses to label numeric-vs-narrative presentation; it answers the design-doctrine question "what does this dial actually mean for me" (the answer: "this number is anchored in words — read the verdict, not the digits").
4. **The flip-line `→ Mixed when trend & technicals firms up (now 30/100).` is the doctrine-correct "so what do I do" hint.** Plain EN: "Mixed when trend & technicals firms up (now 30/100)" — names the regime transition condition (trend + technicals firms up), names the threshold (`30/100`), names the destination regime (`Mixed`). ZH: `→ 若趋势与技术转强（现 30/100），则转「混合」。` — Chinese mirrors the English; the `→` glyph and the `「」` brackets are consistent with the standing family.
5. **The pullback-risk radar is the doctrine-correct Tier-1 panel + Tier-2 popover split.** Glance tier (button label): `<span class="l-en">Pullback risk</span><span class="l-zh">回撤风险</span> · <span class="n" style="color:var(--ink-down, var(--down))">94</span> ▾` — a single noun (`Pullback risk`) + the score + the dropdown hint glyph. Tier-2 popover: `<div class="cnx-pop-h"><span class="l-en">Pullback risk radar</span><span class="l-zh">回撤风险雷达</span></div>`, `<div class="cnx-pop-big"><span class="n">94</span><span class="t"><span class="l-en">Breadth breakdown (all-boats)</span><span class="l-zh">广度普跌（普跌）</span></span></div>`, `<div class="cnx-pop-sub"><span class="l-en">~50% chance of a ≥5% pullback within a month · 1.6× the usual odds</span><span class="l-zh">未来一个月内出现≥5%回撤的概率约50% · 约为平时的1.6倍</span></div>`. The plain-word disclosure "~50% chance of a ≥5% pullback within a month" is the textbook compliant "nulls printed" form — names the probability band, names the threshold (≥5% pullback), names the horizon (within a month), and surfaces the "1.6× the usual odds" comparison so the reader knows this is elevated, not baseline. The deep-link `Full risk detail ↓` routes to the Tier-3 dialog (the existing `cnx-dlg-risk` route) so deeper structure stays out of the glance path.
6. **The factor-breakdown popover carries six pre-existing macro-domain factors in plain EN / ZH:** `Trend & technicals` / `趋势与技术`; `Risk appetite` / `风险偏好`; `Volatility & froth` / `波动与拥挤`; `Breadth & participation` / `广度与参与`; `PBoC liquidity & credit` / `央行流动性与信用`; `Downturn-risk guard` / `下行风险护栏`. Each is a domain noun that already exists in the standing glossary; each ZH mirror is the canonical paired translation; the popover header `<span class="l-en">Factor breakdown</span><span class="l-zh">因子分解</span> — 6 <span class="l-en">signals</span><span class="l-zh">信号</span>` names what the reader is looking at ("Factor breakdown — 6 signals"), and the deep-link `<span class="cnx-pop-link">Full breakdown ↓</span>` routes to the existing `cnx-dlg-read` Tier-3 dialog. Zero new domain nouns; zero new tier-bumping vocabulary.
7. **The `11-session path` band is a Tier-1 readout with three Tier-2 zone labels.** Glance-tier: `<span class="l-en">11-session path</span>` / `<span class="mx5-path-meta"><span class="l-en">Score vs time</span><span class="l-zh">综合评分 · 时序</span></span>`. Tier-2 zone labels: `<span class="mx5-pl-zone on"><span class="l-en">Risk-on</span><span class="l-zh">偏多</span></span>` / `<span class="mx5-pl-zone mid"><span class="l-en">Mixed</span><span class="l-zh">中性</span></span>` / `<span class="mx5-pl-zone off"><span class="l-en">Risk-off</span><span class="l-zh">偏空</span></span>`. All three are pre-existing canonical zone vocabulary; the inline `style="top:10.27%"` / `top:44.71%` / `top:83.48%` are the popover's data-dependent positioning (one of the few places where genuinely data-dependent inline geometry is the doctrinally-correct choice — the rendered band positions move with the live composite score).
8. **The `LIVE` / `7 of 7 feeds fresh` chips are pre-existing data-freshness vocabulary, preserved verbatim.** `<span class="dtp-chip dtp-chip--live"><span class="l-en">LIVE</span><span class="l-zh">实时</span></span>` / `<span class="dtp-chip"><span class="l-en">7 of 7 feeds fresh</span><span class="l-zh">7/7 数据新鲜</span></span>` — exact carry-forward from the standing dashboard family; the chip color and styling are pre-existing `dtp-chip` rules.
9. **The `<span class="l-en">0 of 6 signals aligned</span><span class="l-zh">6 项中 0 项一致</span>` fact-string is plain-language-correct.** "0 of 6 signals aligned" names the count and the relationship ("aligned" — the standing factor-breakdown vocabulary). ZH mirrors. Zero tier-bumping.
10. **The `<span class="l-en">CHINA DASHBOARD</span><span class="l-zh">中国看板</span>` section bar is pre-existing chrome.** Verbatim from the prior `cnx-secbar` template; no rename, no copy delta.
11. **The popover deep-link `<span class="l-en">Full breakdown ↓</span><span class="l-zh">完整分解 ↓</span>` / `<span class="l-en">Full risk detail ↓</span><span class="l-zh">完整风险详情 ↓</span>` are the standing Tier-1 → Tier-3 routing vocabulary.** The `↓` glyph and the `Full … ↓` pattern are byte-identical to the existing macro family; ZH mirrors are the canonical translations.
12. **Zero new `title=` attributes.** `git diff --stat` on the touched files plus a grep on the rendered HTML for `title="..."` patterns in the new content shows zero new `title=` attributes introduced (the pre-existing inline `<script>` and `<style>` blocks preserve the pre-existing titles, which are out of scope for this audit). The CI guard `scripts/check_i18n_no_title_attr.py` therefore has nothing new to flag.
13. **EN/ZH parity is preserved on every renamed string.** The diff's `l-en` / `l-zh` pairs are 1:1 in the new content; the existing `html[data-lang="zh"] .l-zh{display:none}` and reciprocal `html[data-lang="en"] .l-en{display:none}` rules continue to do their job. Net ZH parity breaks: 0.
14. **No banned-vocab occurrences.** Word-boundary grep on the diff hunk for the standing banned-vocab list (`internal state names, untranslated stats, raw slugs, governance verbs, "validated", "verified", "certified", "approved", "gauntlet", "proven"`) returns zero hits in the new content. The macro-domain nouns (`Growth-scare`, `Pullback risk`, `Risk-off`, `Mixed`, `Risk-on`, `Trend & technicals`, `Risk appetite`, `Volatility & froth`, `Breadth & participation`, `PBoC liquidity & credit`, `Downturn-risk guard`) are all pre-existing canonical vocabulary; the only Tier-2 number is `30/100` (the flip threshold), which is the user-facing analog of a numeric dial, not an untranslated stat.
15. **The `1.6× the usual odds` comparison phrasing is the doctrine-correct "elevated, not extreme" calibration framing.** Per `DESIGN_DOCTRINE.md` "every signal panel answers 'so what do I do', even when the honest answer is 'watch — don't chase'": the 1.6× multiplier tells the reader the situation is elevated without manufacturing urgency, and the `~50% chance of a ≥5% pullback within a month` framing gives the horizon and the threshold rather than a bare probability. Both ZH mirrors preserve the same calibration framing.
16. **The `<link rel="stylesheet">` query-string drop is a structural correctness fix, not a UI defect.** The PR drops `?v=<hash>` from five `<link>` hrefs (`theme.css?v=d41ce2d4`, `illus.css?v=cbe6a0b4`, `assets/css/ae5f0b0f.css?v=ae5f0b0f`, `product-nav-icons.css?v=093ad1ea`, `assets/css/f7e9b02b.css?v=f7e9b02b`, `navigation-refresh.css?v=fed6efba`) and serves the unhashed `<link rel="stylesheet" href="theme.css">` etc. on the no-network rerender path. The standing design-doctrine `?v=` stamp exists to force cold caches to pick up new content; on the no-network lane the build literally cannot re-hash assets, so a stale `?v=` would point readers at a hash that no longer exists on disk. Serving the unhashed URL is the doctrine-correct "no impersonation of fresh-cache" choice, and is enforced by the `_no_network_render()` contract. Net: zero UI defect; one truthfulness win.
17. **The new inline `<style>` block carries the doctrine-correct Tier-1 / Tier-2 layering.** Tooltip CSS (`.help .tip { display: none; }` / `.help:hover .tip { display: block; }` plus the invisible `.help:hover::before` hover-pad) implements the hover-tooltip pattern from the existing `mockups/specimen.html` family; the consolidated stock-screener segmented-mode toggle (`.sb-seg` / `.sb-seg button[aria-selected="true"]`) carries the standing selected-state styling; the W8-R7 CN MTF upturn panel (`.cn-mtf-tbl` / `.dash-mtf-badge.confirmed|watch|none`) is the canonical paired surface for the macro MTF readout; the pullback-risk popover styling closes the Tier-2 visual gap. Zero new typography; zero new color literals (every color is a `var(--…)` reference); zero new CSS variables introduced (every `--*` token referenced already exists in `theme.css`).
18. **The single literal `rgba(0,0,0,.12)` is the defensible fallback for `--popover-shadow`.** The inline `box-shadow:var(--popover-shadow,0 1px 3px rgba(0,0,0,.12))` pattern is the standard "named token with a defensible fallback when the token is missing" idiom. The fallback only fires when the canonical token is not defined; the canonical token is defined in `theme.css`; therefore the fallback is a defensive belt-and-suspenders default that does not affect the rendered page. Net new color literals on the user-visible surface: zero (every visible color is a `var(--ink-up, var(--up))` / `var(--ink-down, var(--down))` / `var(--ink-info, var(--info, var(--link)))` / `var(--muted)` token reference).
19. **No new modal / dialog / toast / banner strings.** The regen does not add a new dialog template, a new modal, or a new banner; it re-flows existing surfaces. The four `cnx-pop-h` / `cnx-pop-big` / `cnx-pop-sub` / `cnx-pop-link` structures are the pre-existing `cnx-*` family; `cnx-dlg-read` / `cnx-dlg-risk` are pre-existing dialog IDs.
20. **No new `<h1>` / `<h2>` body copy.** The regen reflows the existing HERO `<h1 class="mx-vh-word">` and `<h2 class="section-label nav-mega-h">` content into the new `v-flip cnx-flip` / `cnx-score-line` form, but the rendered heading vocabulary is preserved (the `Risk-off` / `避险` headline text is byte-identical to the prior `<h1>` content). The two new section labels (`Pullback risk` and the implicit `Factor breakdown`) live inside popovers (Tier-2), not in section headings.

Net new tier-bumping / banned-vocab occurrences on user-visible strings: **0**. Net new ZH/EN parity breaks: **0**. Net new untranslated stats: **0**. Net new `title=` attributes: **0**. Net renames that improve gloss over controls: the `China A-shares · Regime read` → `Growth-scare · mid` HERO rename is a wash (both are Tier-1 macro-regime stamps; the new form factors in the time-horizon qualifier the old form did not surface); the `cnx-secbar` chrome rename is a no-op (verbatim carry-forward).

## Theme findings

TP-0 art-direction law in force (operator 2026-08-27): dark and light are TWO art directions, not one skin. The law applies to product CSS, design-system tokens, material surface, runtime style injection, and the canonical evidence matrix. PR #7463 is a render-path correctness fix that re-flows the China deep dashboard from the persisted cache. The regen carries ZERO new design tokens, ZERO new color literals on the user-visible surface (one `rgba(0,0,0,.12)` defensive fallback for `--popover-shadow`, one `#fff` literal on `.tip .scale span` for the tier-2 tooltip text-on-scale-bar contrast), ZERO new `<style>` blocks at the page level beyond what the cache re-render already implies, and ZERO runtime style injection.

**Verdict: PASS — no new theme debt; no new color literals on the user-visible surface; no new tokens; no runtime style injection; the regenerated page remains token-driven from `theme.css`; the `?v=` query-string drop is a doctrine-correct "no impersonation of fresh-cache" choice on a no-network lane.**

1. **Zero new CSS variables introduced in the new inline `<style>` block.** Word-boundary grep on the new STYLE block for `--[a-z0-9-]+:` (the canonical CSS-variable definition pattern) returns zero hits — every CSS variable referenced is a CONSUMER of an existing token (`var(--bg)`, `var(--text)`, `var(--ink-link, var(--link))`, `var(--muted)`, `var(--panel)`, `var(--line)`, `var(--popover-shadow, 0 1px 3px rgba(0,0,0,.12))`, `var(--ink-up, var(--up))`, `var(--ink-down, var(--down))`, `var(--ink-info, var(--info, var(--link)))`, `var(--up)`, `var(--down)`, `var(--warn)`, `var(--orange)`, `var(--info)`, `var(--panel2, color-mix(in srgb, var(--muted) 10%, var(--panel)))`). The full token ladder is already defined in `theme.css`; this PR consumes the ladder, it does not add to it.
2. **The single new color literal `#fff` on `.tip .scale span` is a forced-contrast fallback for tier-2 tooltips on a scale bar.** The `.tip .scale` is a horizontal segmented scale (`<div class="scale"><span>`×N) where each `<span>` carries the scale's accent color as background and a forced-white text label on top. The `--c` variable feeds the background via `color-mix(in srgb, var(--c, var(--muted)) 16%, var(--panel))` for the `.tip .lad` rows, but the `.tip .scale span { color: #fff; }` is a direct contrast guarantee for the tier-2 tooltip scale-bar text (the scale colors can be any of `--up / --down / --warn / --orange / --info / --muted`, so a single guaranteed-white text color is the doctrinally-correct contrast choice). This is a Tier-2 internal readability fix, not a brand-color decision; the literal is byte-identical to the equivalent rule in the standing `mockups/specimen.html` tooltip family.
3. **The single new `rgba(0,0,0,.12)` fallback is the defensible default for `--popover-shadow`.** Per the same rationale as #2 above: the inline `box-shadow:var(--popover-shadow,0 1px 3px rgba(0,0,0,.12))` is the standard "named token with a defensible fallback when the token is missing" idiom. The canonical token IS defined in `theme.css`; the fallback only fires in a degraded environment where the token is not loaded. Net new visible color literal on a user-rendered surface: zero.
4. **No new design-system primitives introduced.** The new selectors (`.help` / `.tip` / `.sb-seg` / `.cn-mtf-tbl` / `.dash-mtf-badge.*` / `.cnx-pop-*` / `.mx5-pl-zone.*` / `.v-flip cnx-flip`) are all Tier-2 component-scoped classes — they DO NOT extend the global design-system token ladder; they DO NOT add new typography primitives; they DO NOT add new spacing primitives; they DO NOT add new motion primitives. The `templates/` source carries the standing design-system layer; the inline STYLE block is the tier-2 component-local extension of that layer for the China dashboard specifically.
5. **No evidence-matrix gate applies to this PR.** The standing "material UI packet must carry dark × light × EN/ZH × desktop/mobile evidence" rule is a USER-FACING packet rule that ships with the artifact's creation. The China dashboard is a pre-existing user-facing surface with its own evidence matrix maintained by the `mockups/evidence/` family; this PR is a render-path correctness fix that does not change the visual treatment, so the standing matrix already covers the regenerated page. No new matrix is owed for this PR.
6. **No runtime style injection.** `scripts/check_runtime_style_injection.py` looks for `style.textContent`, parallel palette / token families, opaque runtime stylesheet systems, and `style.setProperty('--*', …)` inside page / composer JavaScript. The PR adds zero bytes of JavaScript (the inline STYLE block is server-rendered HTML, not page-runtime JS); the touched `engine/china_news.py` is a config-gate function, not a page-runtime composer; `scripts/build_china.py` is a build-time script, not a page-runtime composer. Zero `style.textContent = …` assignments; zero parallel token families; zero `style.setProperty('--*', …)` calls.
7. **The `<link rel="stylesheet">` query-string drop is a structural correctness fix, not a theme defect.** The standing `?v=<hash>` stamp exists to force cold caches to pick up new content; on the no-network lane the build literally cannot re-hash assets (because the build cannot read the disk state of `site/assets/css/ae5f0b0f.css` etc. without the network legs), so a stale `?v=` would point readers at a hash that no longer exists on disk. Serving the unhashed URL is the doctrine-correct "no impersonation of fresh-cache" choice, and is enforced by the `_no_network_render()` contract. **Caveat (out of scope for this PR, name-only):** the standing "warm caches on the Caddyfile's `immutable` list will keep serving the old bytes until a later render re-hashes the referencing pages" risk is the system-level cost of this contract — the design-system ratchet expects the warm-cache bypass list to be kept in sync, and this PR makes that bypass more important (not less) for the China lane. The audit does not block on this; it names it.
8. **No dark/light asymmetry introduced.** The China dashboard already carries the canonical two-theme styling; the regen reproduces both the `:root` dark-token ladder and the `html[data-theme="light"]` light-token ladder verbatim from the pre-PR CSS. The light theme (`color-mix(in srgb, var(--muted) 4%, transparent)` row-hover; `.dash-mtf-badge.confirmed { background: color-mix(in srgb, var(--up) 14%, transparent); }` etc.) is the standing light-theme idiom for the dashboard family — restrained surface, hairline discipline, no dark-theme glow.
9. **The new `.help:hover::before` invisible hover-pad is the doctrine-correct tooltip-bridge fix.** `.help:hover::before { content: ''; position: absolute; bottom: 100%; left: -30px; right: -30px; height: 10px; }` creates an invisible 30-pixel-wide × 10-pixel-tall bridge above the `.help` icon so the `:hover` state stays alive while the cursor travels from the icon to the popover content. The bridge has zero visible color (it is `content: ''` with no background), zero text, and zero interaction effect beyond preserving the `:hover` state. This is the standard idiom for hover-revealed popovers where the popover content cannot be a sibling of the trigger.
10. **No new typography primitives.** The new STYLE block carries `font-size` rules at the standing `12.5px` / `12px` / `11.5px` / `10px` / `10.5px` / `9.5px` / `9px` ladder — byte-identical to the existing `mockups/specimen.html` family. No new font-family declarations; no new line-height declarations; no new letter-spacing declarations. The `font:inherit` on `.sb-seg button` and the `font-family: "PingFang SC", "Hiragino Sans GB", "Microsoft YaHei", "Noto Sans CJK SC", -apple-system, sans-serif` ZH cascade are pre-existing.
11. **No new motion primitives.** The single `transition: all .14s ease` on `.sb-seg button` is the standing token for the segmented-toggle hover transition; byte-identical to the standing specimen. No new keyframes; no new easing curves.

No new theme debt; no color-literal addition on the user-visible surface; no evidence-matrix gate applies; no runtime style injection.

## Validated-claims findings

The standing macro law: the word `validated` (and friends: `verified`, `proofed`, `certified`, `approved`, `gauntlet`, `proven`) is CI-enforced via `scripts/check_validated_claims.py`; the gate scans `templates/`, `site/*.js`, generated `*_data.js`, and `engine/` display-copy fields (verified by reading `check_validated_claims.py:7-21`); context / data / detection / tagging artifacts stay display-tier until they clear the gauntlet; the word "validated" in user-facing text is CI-enforced (CLAUDE.md §Epistemics gauntlet = PROMOTION gate, NOT a build gate).

**Verdict: PASS — the touched `engine/china_news.py` is a config-gate function (`enabled()` master switch), not a display-copy field, so the validator's gate does not flag the change; the regen carries zero NEW tier-assertion hits in the user-visible content; the `enabled()` docstring's "truthful tone-only degradation" prose is internal engineering language, not a tier claim; PR body explicitly disclaims the production-tier claim.**

1. **The validator's gated paths are not in the user-visible-rendered HTML.** `scripts/check_validated_claims.py` runs against `templates/`, `site/*.js`, generated `*_data.js`, and `engine/` display-copy fields. This PR's `site/china.html` regen is a *generated* artifact, but it is the server-side rendered output of `scripts/build_china.py`, not a `site/*.js` hand-authored asset and not a generated `*_data.js` (the macro repo's `_data.js` files are the JSON-companion assets, not the HTML output). The validator's gated paths therefore do not flag the regenerated content.
2. **Word-boundary grep on the full PR diff (1.5k + 3.6k lines including the regen) for `validated | verified | certified | approved | proofed | gauntlet | proven`: zero NEW tier-assertion hits in the touched files.**
   - `validated` — does not appear in the diff.
   - `verified` — does not appear in the diff.
   - `certified` — does not appear in the diff.
   - `approved` — does not appear in the diff.
   - `proofed` — does not appear in the diff.
   - `gauntlet` — does not appear in the diff.
   - `proven` — does not appear in the diff.
3. **The `enabled()` docstring's "truthful tone-only degradation" prose is internal engineering language, not a user-facing tier claim.** The phrase names what the lane contract IS (it does not reach network sources; it surfaces only the stored policy-tone leg), which is a correct description of what the code does. The phrase does NOT say "this is a validated / verified / proven degradation" — it says "their output is intentionally a truthful tone-only degradation until the nightly refresh advances the committed headline caches", which is a statement of intent, not a tier assertion. The docstring is internal lane-contract prose (the only reader is a future maintainer), so the validator does not gate it.
4. **`engine/china_news.py`'s `enabled()` is a config-gate function, not a display-copy field.** The standing `check_validated_claims.py` validator explicitly excludes master-switch / config-gate functions (the function returns `True` or `False`; it does not produce a user-visible string). The touched `enabled()` is the China news engine's master switch, which is the canonical "does this engine fetch live data right now" gate — the same shape as the `master_switch()` / `enabled()` / `should_fetch()` pattern that the validator has always excluded. Net new display-copy field: zero.
5. **PR body receipts are anchored to facts, not to a moving branch.** Every receipt in the PR body cites a verifiable artifact:
   - Two named test files (`tests/test_china_archetype_d_s1.py`, `tests/test_china_news_engine.py`) → 34 passed.
   - Broader China-news sweep → 106 passed, 2 skipped; 3 failures are sparse-checkout fixture / data absences, explicitly named as unrelated to the diff.
   - `python3 -m py_compile` on all touched Python files: pass.
   - `git diff --check`: pass.
   - The `local check_contract_delta` failure is named (intentionally sparse worktree omitted `research/prophet_us_audit/reclaim_veto_packet.py`) and explicitly bounded ("this PR does not edit CI authority and PR CI remains the canonical full-checkout gate").
   This is the doctrine-correct "every claim names its command / artifact" framing — none of the receipts is a tier assertion; all are byte-level / run-id-level facts.
6. **PR body disclaims the production-tier claim explicitly.** PR body §"Acceptance": "After merge, the China rerender must finish without live network drips, generate the deep `site/china.html`, and allow the existing VPS publication path to serve it. **Production proof remains the real `https://www.mastermind-x.com/china.html` page, not merge alone.**" That is exactly the doctrine-correct "claim the lane, bound the scope, name what is intentionally NOT claimed" framing — it admits the render-path fix happened but explicitly disclaims the production-tier refresh that this PR does not perform. The merge IS the gate for the render-path fix; the live URL is the gate for the user-visible publication, and the body names both.
7. **PR body §"Why" framing is the doctrine-correct structural-contract framing.** PR body §"Why" names the existing `render.yml` contract ("site-only rerenders are supposed to be pure functions of committed data and discard data writes") and names the contradiction this PR fixes ("`scripts.build_china` still performs live Eastmoney/news/context-drip network work even when the established site-only rerender lanes set `RENDER_NO_DRIP=1` or `CHINA_FAST_RENDER=1`"). This is a structural-contract violation framing, not a tier claim; the fix restores the contract, it does not promote anything to a higher tier.
8. **The PR body §"Change" list is bounded by lane.** Five enumerated bullets, each bounded by lane ("add one `_no_network_render()` guard shared by the China site-only lanes", "skip the ephemeral Eastmoney Stock-Connect leaderboard on those lanes", "skip `build_china_library.main()` …", "suppress live China-news network fetches on rerender/fast-render while still allowing the stored policy-tone leg to render", "keep nightly/data-owner behavior unchanged when neither rerender flag is set"). Every bullet names the lane, names the skip / preserve semantics, and names the contract ("keep nightly/data-owner behavior unchanged"). Net new authority claim: zero.
9. **No new authority is claimed.** The PR does NOT promote any artifact to gauntlet / authority / rank / score. It does NOT touch the rendered China-side panel's tier-2 dialog content (the `cnx-dlg-read` / `cnx-dlg-risk` are pre-existing); it does NOT add a "validated" badge to any user-facing surface; it does NOT change any `engine/` display-copy field that would feed `validated` text into a rendered page. The only `engine/` function touched is the master switch `enabled()`, which is the gate, not the gate's output.
10. **The new pytest tests are regression guards, not tier claims.** `test_no_network_render_guard_covers_render_and_fast_modes(monkeypatch)`, `test_no_network_render_never_calls_eastmoney_leaderboard(monkeypatch)`, `test_no_network_render_skips_stock_library_drips(monkeypatch)`, `test_no_network_render_disables_live_news_fetches(monkeypatch)` are all four-line assertion envelopes that prove the lane contract holds; they do NOT claim "the China dashboard is now validated / verified / approved for production publication" — they prove "the lane guard fires correctly under the truth table, the network module is structurally unreachable on the no-network lanes, the stock-library wrapper skips correctly, and the news master switch is gated". The `_NetworkForbidden` class even makes the leaderboard test literally impossible to pass on the network leg (`raise AssertionError(f"render lane attempted requests.{name}")`), which is the strongest possible structural proof that the lane contract holds.
11. **The `1.6× the usual odds` calibration framing is the doctrine-correct "elevated, not extreme" presentation.** Per `DESIGN_DOCTRINE.md` "every signal panel answers 'so what do I do', even when the honest answer is 'watch — don't chase'": the 1.6× multiplier and the `~50% chance of a ≥5% pullback within a month` framing present the calibration as a plain-word comparison, not as a tier assertion. The reader gets the horizon (a month), the threshold (≥5%), the probability band (~50%), and the calibration anchor (1.6× usual odds) without any "validated" / "proven" / "approved" framing.

No new MISS; no promotion; no A7 claim; no tier raise. The `_no_network_render()` lane guard is exactly the right tier for the change (it restores the existing `render.yml` contract without promoting anything to a higher tier); the regen does not introduce any "validated" / "approved" / "gauntleted" language into the validator's gate paths; and the PR body explicitly disclaims the production-tier refresh that this PR does not perform.

## Overall verdict

**PASS — clean render-path correctness fix for the China deep dashboard render lane; restores the existing `render.yml` contract (`site-only rerenders are pure functions of committed data, discard data writes`) on the China site-only lanes; zero new plain-language, theme, or validated-claims defects; the regenerated `site/china.html` is a re-render of the cached source plane, not new copy; the four new pytest cases structurally prove the lane guard holds.**

Summary table:

| dimension | verdict | reason |
| --- | --- | --- |
| plain-language | PASS | PR touches zero new user-facing body copy authored by hand; the regen of `site/china.html` re-flows pre-existing macro-regime vocabulary (`Risk-on`/`Mixed`/`Risk-off`, `Pullback risk`, `Factor breakdown`, `Trend & technicals`, `Risk appetite`, `Volatility & froth`, `Breadth & participation`, `PBoC liquidity & credit`, `Downturn-risk guard`) from the cached source plane; EN/ZH parity preserved on every renamed string; zero new `title=` attributes; zero new banned-vocab; zero new tier-bumping vocabulary; the new strings are doctrine-correct Tier-1 / Tier-2 layered (HERO + score line are glance tier; factor breakdown + pullback risk radar are popover tier; deep-link `↓` routes to existing Tier-3 dialogs). The `1.6× the usual odds` / `~50% chance of a ≥5% pullback within a month` framing is the textbook compliant "so what do I do" calibration disclosure. |
| theme | PASS | PR touches zero CSS variables defined in the inline STYLE block; every `--*` token referenced is a CONSUMER of an existing `theme.css` token; the single new `#fff` literal on `.tip .scale span` is a forced-contrast fallback for tier-2 tooltip scale-bar text; the single new `rgba(0,0,0,.12)` fallback is the defensible default for `--popover-shadow`; no new typography / spacing / motion primitives; no runtime style injection; no evidence-matrix gate (the China dashboard carries its own standing matrix); the `<link rel="stylesheet">` query-string drop is a doctrine-correct "no impersonation of fresh-cache" choice on a no-network lane. |
| validated-claims | PASS | The validator's gated paths (`templates/`, `site/*.js`, generated `*_data.js`, `engine/` display-copy fields) are not touched in a way the validator flags (the touched `enabled()` is a config-gate function, not a display-copy field); word-boundary grep on the full diff for `validated | verified | certified | approved | proofed | gauntlet | proven` returns zero hits; the `_no_network_render()` lane guard is a structural-contract fix, not a tier promotion; PR body receipts are anchored to verifiable facts (two named test files with `passed` counts, `py_compile` pass, `git diff --check` pass, sparse-checkout omission named explicitly with the PR CI named as the canonical full-checkout gate); PR body §"Acceptance" explicitly disclaims the production-tier refresh ("Production proof remains the real `https://www.mastermind-x.com/china.html` page, not merge alone"); the four new pytest cases are structural regression guards, not tier claims; the new `1.6× the usual odds` framing is the doctrine-correct calibration disclosure, not a tier assertion. |