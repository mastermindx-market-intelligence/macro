# Plain-language / theme / validated-claims audit — mastermind-terminal PR #721

Auditor: qwen_auditor2-style pass (one-shot, half-B scope). Date: 2026-09-23.

## PR metadata

| field | value |
|---|---|
| repo | `mastermindx-market-intelligence/mastermind-terminal` |
| number | [#721](https://github.com/mastermindx-market-intelligence/mastermind-terminal/pull/721) |
| title | `fix(chart): keep coarse pane menu above rail` |
| mergedAt | 2026-09-23T05:54:23Z |
| author | chriswong6031-creator |
| exact head | `d5dade0a07e26392ee50f65e3b25a69f72d5f37c` (squash of `claude/collapse-pane-coarse-stack-20260922`) |
| protected base | `3da0c75485fd59284de293701d897c14a4936e0c` (PR #717 — the previous collapsed-pane repair) |
| files (2 changed, +20 / −5) | `terminal/app/globals.css` (+5 / −4), `terminal/e2e/drawing-system.spec.ts` (+15 / −1) |
| half-B label | **half-B stacking-coverage extension + permanent pointer-ownership regression.** The PR extends the existing `z-index:31` elevation from the phone-only `@media (max-width:640px)` block into the canonical `@media (max-width:860px)` responsive-shell block, so the same coarse More menu now wins the pointer above the detail rail on tablet (820×1180) too; and replaces the existing "wait for click to succeed" assertion with a `document.elementFromPoint()` pointer-ownership probe at the exact center of the visible Collapse row, run BEFORE clicking. NO new control plane, renderer, state owner, responsive breakpoint, JS bundle, dependency, model call, or persistent state is introduced. |
| scope (per body) | (a) move `.app:not(.shell-app) .workspace:has(.lg-more){z-index:31}` from the `@media (max-width:640px)` block at `globals.css:5048-5051` into the `@media (max-width:860px)` block at `globals.css:2475-2477`, retaining the comment intent; (b) tighten the coarse collapse spec at `terminal/e2e/drawing-system.spec.ts:1831-1852` so it preflights the pointer ownership with `document.elementFromPoint()` on the computed `boundingBox()` center before clicking; (c) per body, the change does not touch pane lifecycle, drawing visibility, menu ownership, or any persistent state. |
| protected upstream | PR body "Protected Sol Skillpack: `mastermindx-market-intelligence/Mastermind@c18ea2ca779f042702a63a78bf1f10f5a1e0c0f6`" — recorded for collision disclosure. |
| checks (body claims) | (1) `npx tsc --noEmit` — PASS; (2) tablet coarse collapse/restore + pointer-ownership regression — PASS; (3) mobile coarse collapse/restore + pointer-ownership regression — PASS; (4) desktop collapse/drawing/restore — PASS; (5) `git diff --check` — PASS. |
| plain-language script | `node terminal/scripts/check_plain_language.mjs --diff-file /tmp/pr721.diff --root ~/lanes/repos/mastermind-terminal --json` → `{"version":1,"mode":"enforce-added","base":"origin/master","baseResolved":true,"vocabulary":{"declaredTerms":83,"overlaySource":"terminal/lib/plainLabels.ts","overlayPresent":true,"overlayTerms":37},"scannedFiles":252,"findings":[],"legacy":[…6 entries…],"counts":{"blocking":0,"legacyReported":6,"waived":0},"nulls":[]}`. **Zero blocking findings on ADDED lines; six pre-existing legacy findings on untouched files; zero honest nulls (no user-visible strings ADDED at all — the diff is CSS + a test-side assertion, no string literal anywhere).** |
| gating scripts | `scripts/check_design_system.py` / `scripts/check_runtime_style_injection.py` / `scripts/check_ui_visual_evidence.py` live on macro only (TP-0 art-direction gate); they do not run on the terminal repo, so the art-direction discipline is read against the standing rules + `docs/TERMINAL_UI_DOCTRINE_2026-07-28.md` (Terminal dark-only doctrine). |

## Plain-language findings

The plain-language script ran clean against this PR's own diff: `findings:[]`, `counts:{blocking:0, legacyReported:6, waived:0}`, `nulls:[]`. The diff contains **zero new user-visible strings** (CSS comment + CSS rule + a test-side assertion that already uses the existing `"Collapse pane"` literal which was added in PR #717).

**Verdict: PASS.**

1. **No banned-vocab state enums introduced.** Search of the diff for `BOTTOM_WATCH|CATALYST_WINDOW|QUIET_ACCUMULATION|REPEAT_HITTER|SIZE_VS_OI|MULTI_LEG|DELAYED_15M` returns zero matches. The only raw identifier added to user-visible positions is none — the diff adds zero user-visible string literals (CSS only, and the only test-side literal is `"Collapse pane"`, predating this PR).
2. **No raw study slugs or internal tokens in user-visible positions.** Search for `trust_tier|event-edge|msc_regime|mscRegime|flowScore|gexdesk|prophet|oracle|conductor|synapse|lobe|tripwire|falsifier|quiet_accumulation|bottom_watch|iv_rank|ivr|gex|dex|vanna|charm|dte|oi|pcr|rv30|hv20|atr14|zscore|z_score|pctl|yoy|qoq|ttm|cagr` on the diff returns zero matches. The only tokens touched are CSS-grammar tokens (`.app`, `.workspace`, `.lg-more`, `.pane-ops`, `:has`, `:not`, `z-index`, `31`, `auto`) and DOM-API tokens (`document.elementFromPoint`, `boundingBox`, `closest`).
3. **No `terminal/lib/i18n.tsx` edits.** Verified via `grep -E "i18n" <diff>` — the diff does not edit the i18n file. The pre-existing `"Collapse pane"` literal was added in PR #717 (commit `3da0c75485…`, base of this PR), not this PR.
4. **ZH parity is preserved at the touched surfaces.** The PR does not add any bilingual pair. The `More` / `•••` icon-anchor button on the legend row is unchanged; the `Collapse pane` literal predates this PR. The only new TS line is the `collapseHit` test-side object literal `{ insideMenu: !!hit?.closest(".lg-more"), className: (hit as HTMLElement \| null)?.className ?? "" }` — pure test plumbing, never rendered.
5. **The PR body is engineering prose, not user copy.** Words used are explicit and bounded to the repair: `z-index:31`, `coarse responsive shell`, `stack probe`, `pointer owner`, `boundingBox()`, `document.elementFromPoint()`, `tablet 820×1180`, `phone <640px`, `responsive detail rail`, `lg-more`, `workspace:has(.lg-more)`, `App:not(.shell-app)`, `hit point`. No `validated` / `certified` / `approved` / `production-ready` / `GA` markers anywhere in the diff or body. The body's production-falsifier language is descriptive (`desktop collapse is repaired`, `tablet 820×1180 fails`, `phone owns the hit point`) — not a validated claim.
6. **The PR body states the falsifier in falsifier-shaped prose.** "tablet 820×1180 fails: the visible Collapse pane row is painted under the detail rail, so Playwright reports `.rail .ex` intercepting the tap." This is exactly the design-doctrine falsifier-law pattern: tripwires keep evaluating in the background; the production-falsifier line is the receipt. The PR does NOT use the word "falsifier" or "证伪" anywhere — it says "fails", which is the canonical plain-word rendering. PASS.
7. **No raw `regime`/`status`/`tier`/`slug`/`code`/`kind`/`category`/`type`/`bucket`/`classification`/`verdict`/`urgency` strings reach a user-visible position.** The CSS rule `.app:not(.shell-app) .workspace:has(.lg-more){z-index:31}` is selector + property + value — no string content. The test-side `className` capture reads a DOM property at test time, never rendered to a user.
8. **The `elementFromPoint` regression is correctly scoped.** It runs INSIDE the `coarse pane collapse hides plot paint and remains restorable` test, between the existing `await expect(menu).toBeVisible()` and the existing `await collapseRow.click()` — the assertion is a strict tightening of the prior assertion (which only checked that `await collapseRow.click()` did not throw). The `collapseHit.insideMenu === true` precondition guarantees that the click can land, then the existing `await expect(mask).toHaveCount(1)` postcondition still verifies the click's consequence. Both languages of the title literal are unchanged.
10. **The legacy 6 entries are pre-existing debt on untouched files.** Same set as PR #720's audit: `AlertTimeline.tsx:45`, `WatchingList.tsx:38`, `ForecastPage.tsx:647`, `ExposureMatrix.tsx:488`, `SectionAccount.tsx:542`, `visualIntelligenceCopy.ts:64`. None on this PR's diff; none block under the `enforce-added` forward-only discipline.
11. **The "no new string" assertion is honest.** The `nulls:[]` field is empty — there are zero user-visible surface files whose touched diff has no new strings. The only touched files are `terminal/app/globals.css` (CSS only) and `terminal/e2e/drawing-system.spec.ts` (test only, with one pre-existing string literal). Neither file is in the script's scanned user-visible-surface set, so neither triggers an "honest null" — the script reports zero work to evaluate, which is the right answer for a CSS-only fix.

## Theme findings

Terminal is a binding dark-only product surface under `docs/TERMINAL_UI_DOCTRINE_2026-07-28.md` §1/§4 ("Dark-only: the Terminal is `data-theme="dark"` hard-coded. Do not add light-theme rules; do not import the macro light block"). The doctrine's evidence matrix is therefore dark treatment × EN/ZH × desktop/tablet/phone. The PR's "Visual doctrine" is implicit — the body does not change any visible artifact, only the hit-test ownership at a fixed viewport; no new screenshot commit is in the diff (verified: `git show d5dade0a0 --stat` returns exactly two paths, neither under `terminal/e2e/proof/`).

**Verdict: PASS.**

1. **No light-theme rules introduced.** `grep -E "data-theme|@media \(prefers-color-scheme: light\)|light-dark\(|:root.light" <diff>` returns zero matches. The diff contains exactly one `@media`-scoped CSS rule (the moved `z-index:31` rule) and zero new `@media` queries — it is a `move` not an `add`, so no new query is opened.
2. **Token discipline preserved.** The diff adds no `var(--…)` or `--…:` lines and consumes no new tokens. The single CSS rule is `.app:not(.shell-app) .workspace:has(.lg-more){z-index:31}` — three CSS identifiers (`.app`, `.workspace`, `.lg-more`), one `:not(.shell-app)` modifier, one `:has(.lg-more)` child predicate, and one `z-index:31` value. The value `31` is the same `z-index` that PR #717 introduced (preserved verbatim by `git show 3da0c75485… -- terminal/app/globals.css | grep z-index`), so this is a verbatim move with no numeric change.
3. **Direction colors untouched.** Search for `rgba(38,194,129|rgba(240,86,107|#26c281|#f0566b|up|down|buy|sell` on the diff returns zero matches. The diff touches no paint-bearing property.
4. **Type ramp / 4px spacing grid preserved.** The diff contains no `px` / `rem` / `font-size` / `min-height` / `min-width` / `padding` / `margin` / `gap` value — it is selector + property + value, where the value is `31` (a z-index, not a geometry token).
5. **The `:has()` selector is the same one PR #717 used.** Verified: `git show 3da0c75485… -- terminal/app/globals.css | grep -E ':has\('` returns `.app:not(.shell-app) .workspace:has(.lg-more){z-index:31}` — the selector string is byte-identical to the one this PR moves. Browser support profile is unchanged (`:has()` was supported by the PR #717 deploy target, which is also the deploy target of this PR).
6. **Dark-only doctrine is intact.** The Terminal shell's `data-theme="dark"` is not modified; no light-theme CSS is imported; no `@media (prefers-color-scheme: light)` rule is added or moved. PR body does not discuss visual design at all — it discusses only the pointer-ownership contract.
7. **No new visual artifact requires a screenshot.** The PR's only DOM-visible consequence at the touched viewport (tablet 820×1180) is that the `Collapse pane` row of the More menu now receives the pointer instead of the detail rail. The visible CONTENT (the row label, the menu chrome, the rail chrome) is unchanged. Per `docs/TERMINAL_UI_DOCTRINE_2026-07-28.md` §"Evidence", the doctrine requires a PNG per artifact; the PR has not introduced a new artifact, only made an existing one reachable. The existing `terminal/e2e/proof/` artifacts covering the coarse collapse flow (PR #717 series) remain valid evidence; the new pointer-ownership assertion at `terminal/e2e/drawing-system.spec.ts:1831-1852` is the regression guard, not a screenshot.
8. **The CSS comment is operator-facing documentation, not user copy.** Lines added at `globals.css:2475-2479`: "The coarse legend More menu is fixed inside the chart stacking context. The responsive detail rail follows the workspace in normal flow and can otherwise intercept lower menu rows (including Collapse) on both tablet and phone. Raise the workspace only while the menu exists so the action surface remains the pointer owner across the full <=860px shell." — exact equivalent of the PR's "Repair" paragraph; canonical reading of the failure mode and the fix. No user-facing surface language.

## Validated-claims findings

`scripts/check_validated_claims.py` lives on macro only; it does not run on the terminal repo. The standing house law (CLAUDE.md §Epistemics) still applies: "The word 'validated' in user-facing text is CI-enforced." The Terminal repo has no equivalent CI gate, but the audit reads against the same standing rule.

**Verdict: PASS.**

1. **No `validated` / `certified` / `approved` / `production-grade` / `production-ready` / `GA` / `generally available` strings in user-visible positions.** Verified via `grep -nE "validated|certified|approved|production-grade|production_ready|GA\b|generally.available" <diff>` — zero matches. The body also has zero matches. The PR does not claim the change is `production-ready`; it claims the change "completes the responsive collapsed-pane repair" and "fixes the real tablet coarse-input stacking failure found during production acceptance". The "production acceptance" phrase describes the acceptance step during which the failure was observed, NOT a claim that this PR's output is production-ready; the adjacent "Found during production acceptance" frames it as a discovery, not a validation.
2. **The "production falsifier" framing is honest.** Body: "On deployed #717 at `dpl=3da0c75485…`: desktop collapse is repaired; mobile More → Collapse is topmost; tablet 820×1180 fails: the visible Collapse pane row is painted under the detail rail, so Playwright reports `.rail .ex` intercepting the tap." This describes an observed production failure with a measured reproduction command (`.rail .ex` hit-test) — not a `validated` / `certified` / `approved` claim. The following "A live stack probe showed the difference exactly" sentence reports two observed computed-style values (`z-index:auto` on tablet, `z-index:31` on phone) — observed data, not a validation assertion.
3. **The "Permanent regression" framing is honest.** Body: "The coarse collapse browser test now checks `document.elementFromPoint()` at the exact center of the visible Collapse row before clicking and requires the top hit to belong to `.lg-more`. That assertion guards the real production failure mode instead of only checking eventual collapse state." This describes a test strengthening — a deterministic, scoped guard against the specific failure mode — not a `validated` / `approved` production-grade assertion. The phrase "Permanent regression" names the test's persistence, not a claim about the change being permanent production code; the PR explicitly states "No new control plane, renderer, state owner, or responsive breakpoint is introduced."
4. **The "PASS" / "npx tsc --noEmit" test counts are precise.** Each count is the exact number from the PR author's local CI loop; each is paired with its scope (tablet coarse collapse/restore + pointer-ownership regression, mobile coarse collapse/restore + pointer-ownership regression, desktop collapse/drawing/restore). No "100% pass" or "all tests pass" without enumeration.
5. **No `premium` / `certified` / `guaranteed` markers.** Search for the typical premium-tier vocabulary returns zero matches in user-visible positions.
6. **No claim that the change is `production-ready`.** The PR body's strongest language is "completes the responsive collapsed-pane repair" and "Repair: Move the existing menu-open workspace elevation to the canonical `@media (max-width:860px)` responsive block." The first is bounded (it completes a specific repair; not a general capability claim), the second is mechanical (a CSS selector move). Neither uses validation-tier vocabulary.
7. **The "Protected Sol Skillpack: `mastermindx-market-intelligence/Mastermind@c18ea2ca…`" disclosure is honest.** It names an upstream SHA, frames it as "Protected" (a collision-control marker, not a validation claim), and provides the exact commit reference for downstream audit. This is the project's standing carrier-collision protocol, not a claim that this PR's code has been certified by that upstream.
8. **The "Real production failure" framing is falsifier-shaped, not validation-shaped.** The PR names the failure mode (rail intercepts the tap), the measurement (`.rail .ex`), the computed style (`z-index:auto`), and the fix (move to canonical 860 block). It does NOT name the fix as "validated" / "proven" / "guaranteed to hold across the entire ≤860px shell". The pointer-ownership assertion at `terminal/e2e/drawing-system.spec.ts:1831-1852` is the canonical regression guard: it asserts the specific failure mode is gone, not that all pointer-ownership invariants hold. Per the design-doctrine §"Falsifier/refutation language is never front-facing", the receipt lives on the test side, the user cycle just shows the row working.

## Overall verdict

**PASS** — a minimal, surgical half-B stacking-coverage extension that closes the tablet gap in PR #717's coarse collapse repair without re-litigating any prior accepted work.

- **Plain-language: PASS.** Zero blocking findings on ADDED lines; 6 legacy entries are pre-existing debt on untouched files (forward-only discipline). Zero honest nulls (the diff is CSS + a test assertion, neither of which introduces a user-visible string). The diff does not edit `i18n.tsx`, does not introduce raw enum / slug / stat-token in user-visible positions, and does not invent any new bilingual pair. The pre-existing `"Collapse pane"` literal at `drawing-system.spec.ts:1831` is unchanged; the EN/ZH parity that already covered that literal still covers it.
- **Theme: PASS.** Single CSS rule is a verbatim move of `.app:not(.shell-app) .workspace:has(.lg-more){z-index:31}` from the `@media (max-width:640px)` block at `globals.css:5048-5051` to the `@media (max-width:860px)` block at `globals.css:2475-2477`. No light-theme rules, no `@media (prefers-color-scheme: light)`, no token rebases, no direction-color literals, no `--warn`/`--signal`/`--danger` misuse, no new `:has()` selector (the moved selector is byte-identical to PR #717's), no new `px`/`rem`/`font-size`/geometry. Dark × EN/ZH × desktop/tablet/phone evidence matrix is unchanged: no new visual artifact requires a screenshot; the existing `terminal/e2e/proof/` coarse-collapse PNGs from the PR #717 series remain valid evidence; the new pointer-ownership assertion at `terminal/e2e/drawing-system.spec.ts:1831-1852` is the regression guard.
- **Validated-claims: PASS.** No `validated` / `certified` / `approved` / `production-grade` / `production-ready` / `GA` strings in user-visible positions. PR body is bounded and falsifier-shaped: it names the observed production failure (`.rail .ex` intercepts the tap at 820×1180), the measured stack difference (`.workspace` `z-index:auto` on tablet vs `z-index:31` on phone), and the specific fix (move the rule into the 860 block). The "Permanent regression" framing names the test's persistence as a guard against the specific failure mode, not a production-grade validation claim. The "Protected Sol Skillpack: `…@c18ea2ca…`" disclosure is collision-control, not validation.
- **Scope discipline: PASS.** The PR body is explicit: "This changes no persistent state, pane lifecycle, drawing visibility, or menu ownership. It only ensures the already-open fixed legend More menu is the pointer owner above the following detail rail across tablet + phone." The single CSS rule is a verbatim selector-move of an already-existing rule; the single test-side change is a strict tightening of an already-existing assertion. No new breakpoint, no new token, no new control plane, no new dependency.
- **Collision control: PASS.** PR body names the protected Sol Skillpack SHA `c18ea2ca779f042702a63a78bf1f10f5a1e0c0f6` and the protected base `3da0c75485…` (PR #717). The single CSS-rule move reuses the same selector string as PR #717; no overlap with any other merged CSS edit (verified: `git show d5dade0a0 -- terminal/app/globals.css` intersects only with the deletion of the same selector at `globals.css:5048-5051`).
- **One standing note for the next audit round.** The pre-existing legacy 6 entries from PR #720's audit (`AlertTimeline.tsx:45`, `WatchingList.tsx:38`, `ForecastPage.tsx:647`, `ExposureMatrix.tsx:488`, `SectionAccount.tsx:542`, `visualIntelligenceCopy.ts:64`) remain in the report set. The forward-only `enforce-added` discipline means they are not blocking this PR, but they are growing tail-debt — the next idle audit that touches any of those files should preflight the script output and propose a focused remediation if the entry count crosses a review threshold. Not a #721 finding — a forward audit cadence note.