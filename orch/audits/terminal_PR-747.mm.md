# Audit — mastermindx-market-intelligence/mastermind-terminal PR #747

Mode: qwen_auditor2-style (plain-language / theme / validated-claims laws). One-pass audit against the merged head. No durable writes outside this report.

## PR metadata

| field | value |
| --- | --- |
| repo | `mastermindx-market-intelligence/mastermind-terminal` |
| PR | [#747](https://github.com/mastermindx-market-intelligence/mastermind-terminal/pull/747) |
| title | `[MO-B] fix: mobile marker-tooltip tap flake in e2e (settle on the chart's pane accounting, not a still marker field)` |
| merged | 2026-09-25T06:14:32Z via squash-merge. Half-B (MO-B) e2e flake repair — most-recent half-B merged PR in the 24-h window. |
| head | squash commit from branch `claude/<task>` carrying the e2e fix (single commit, single file). |
| labels | `merge-on-green` (sweeper-armed at merge). |
| files | **1 path: `terminal/e2e/marker-tooltip.spec.ts`** (e2e spec only — zero production / API / component / CSS / i18n changes). Diff: one new `markerField(page)` helper (+52 / refactored), tighter readiness gate inside `settledMarkers`, tighter `prev` key reset. |
| half-B label | **half-B (MO-B F11-10b e2e flake repair).** The flake is on the mobile chart page's marker tooltip — `Terminal e2e mobile (shard)` retries `e2e/marker-tooltip.spec.ts:506/:488` and the tooltip element is rendered but empty (no `data-marker-at`, no text) because the hit-test missed. The fix is on the e2e side: read the chart's *pane accounting* (CSS custom properties `--subpanes` / `--drawing-host-height` + lightweight-charts table sub-pane row count + fills-vs-wrapper) in the same evaluate as the marker field so a read can never straddle a relayout, and require `declared === drawn && fills` before settling. **No product / API / UI / theme / i18n / data contract change.** |
| scope | (a) Add a `markerField(page)` helper that, in ONE `evaluate`, reads both the marker list and the chart's pane accounting: `--subpanes` and `--drawing-host-height` from `.chart-body`, the count of `tr` rows under `.tv-lightweight-charts table` whose `<canvas>` actually paints (`drawn = canvas-rows − 2` for the price + time axes), and a `fills` flag (`|table.height − wrap.height| ≤ 1`). (b) Refactor `markers(page)` to delegate to `markerField(page).list` so every caller reads the same snapshot. (c) Tighten `settledMarkers`: bind readiness to `subpanes === "0" && declared === drawn && fills && list.length > 0 && key === prev`, AND reset `prev` whenever readiness fails so a read over a pending relayout cannot count as the first of two identical reads. (d) Update the `expect.poll` failure message to name the new invariant. |
| out-of-scope | No change to the chart product code, the layout state machine, the subpane `--subpanes` React state, the ResizeObserver, the MobileSheet overlay, the marker hit-test, the gesture threshold, or any CSS. No new test hook, no new production DOM contract, no new user-visible string, no new screenshot. The fix is test-only — product behavior is unchanged. |
| collision control | The diff is on `terminal/e2e/marker-tooltip.spec.ts` only. The PR body documents that this is settling on the chart's pane accounting (`--subpanes`, `--drawing-host-height` set by `ChartPanel`/`TerminalShell` per #688), not on a still marker field — there is no parallel layout-settlement helper this PR collides with, and no other in-flight PR is recorded as editing this same spec line. The `expect.poll` timeout is unchanged at 25 s with the same `[150, 200, 300, 400, 500]` interval ramp. |
| checks | The PR body §"Verification" is bounded: existing local CI was not re-fired for this flake-repair squash; the change is test-only, zero product lines touched, and the `merge-on-green` arm is what carried it across. |
| plain-language script | `node terminal/scripts/check_plain_language.mjs --json` ran on the post-merge tree: `{"version":1,"mode":"enforce-added","base":"origin/master","baseResolved":true,"vocabulary":{"declaredTerms":83,"overlaySource":"terminal/lib/plainLabels.ts","overlayPresent":true,"overlayTerms":37},"scannedFiles":252,"findings":[],"legacy":[…6 entries…],"counts":{"blocking":0,"legacyReported":6,"waived":0},"nulls":[]}`. **Zero blocking findings on ADDED lines; six pre-existing legacy findings on untouched files; zero nulls** — the script's default full-census mode (no `--diff-file`) confirmed that no user-visible string added by this PR fails the vocabulary check. The diff contains no string literal that the script flags. |
| gating scripts | `scripts/check_design_system.py` / `scripts/check_runtime_style_injection.py` / `scripts/check_ui_visual_evidence.py` live on macro only (TP-0 art-direction gate); they do not run on the terminal repo. The Terminal is dark-only under `docs/TERMINAL_UI_DOCTRINE_2026-07-28.md`; this PR adds zero CSS, so the art-direction discipline is read against the standing rule "no light-theme CSS, no `@media (prefers-color-scheme: light)`, no `light-dark()`, no `:root.light`, no color token rebases". |

## Plain-language findings

The plain-language script ran clean against the post-merge tree: `findings:[]` (zero blocking on ADDED lines), `legacy:6` (six pre-existing on untouched files), `counts:{blocking:0, legacyReported:6, waived:0}`, `nulls:[]` (no file in the diff produced a null). The diff adds zero string literals that the script scans.

**Verdict: PASS.**

1. **No banned-vocab state enums introduced.** Search of the diff for `BOTTOM_WATCH|CATALYST_WINDOW|QUIET_ACCUMULATION|REPEAT_HITTER|SIZE_VS_OI|MULTI_LEG|DELAYED_15M` returns zero matches. The diff adds no user-visible copy at all. The `expect.poll` failure message reads "the requested zero-subpane marker geometry should stop moving, with the chart filling a container sized for its panes, before it is measured" — it is a developer-facing test failure hint, not user copy, and contains no banned token.
2. **No raw study slugs or internal tokens in user-visible positions.** Search for `trust_tier|event-edge|msc_regime|mscRegime|flowScore|gexdesk|prophet|oracle|conductor|synapse|lobe|tripwire|falsifier|quiet_accumulation|bottom_watch|iv_rank|ivr|gex|dex|vanna|charm|dte|oi|pcr|rv30|hv20|atr14|zscore|z_score|pctl|yoy|qoq|ttm|cagr` on the diff returns zero matches. The diff uses CSS selectors `[data-sig-layer]`, `.chart-body`, `.tv-lightweight-charts table` and `tr` queries — these are test-only DOM reads, never user-visible.
3. **No `regime` / `status` / `tier` / `slug` / `code` / `kind` / `category` / `type` / `bucket` / `classification` / `verdict` / `urgency` in a user-visible position.** Verified via `grep -nE "\b(regime|status|tier|slug|code|kind|category|type|bucket|classification|verdict|urgency)\b" <diff>` — the only matches are CSS property reads (`getPropertyValue("--subpanes")`, `getPropertyValue("--drawing-host-height")`) and the CSS selector `.tv-lightweight-charts table` — none rendered to a user.
4. **No i18n string touched.** Verified via `grep -E "i18n|t\(|labelKey" <diff>` — zero matches. The diff does not edit `terminal/lib/i18n.tsx`; no `t(...)` call, no `labelKey` reference, no ZH/EN string pair is introduced, removed, or modified. ZH parity cannot regress because no bilingual string is touched.
5. **No string literal reaches a user-visible position.** Every string in the diff is either (a) a CSS selector (`[data-sig-layer]`, `.chart-body`, `.tv-lightweight-charts table`), (b) a CSS custom-property name (`--subpanes`, `--drawing-host-height`), (c) the `expect.poll` failure message (developer-facing, Playwright's `message:` field, never rendered to a user), or (d) a comment explaining the invariant ("bind readiness to the actual parent layout contract…", "the chart drawing exactly the sub-panes the parent has accounted for and filling its wrapper…", "a read taken over a pending relayout never counts as the first of the two"). None of these are user copy.
6. **The PR body is engineering prose, not user copy.** Words used are explicit MO-B F11-10b e2e vocabulary: `MAS-234 shape`, `still marker field`, `pane accounting`, `declared === drawn`, `chart fills container`, `pending relayout`, `sub-pane count`, `drawing-host-height`, `ResizeObserver + rAF`, `saturated runner`, `committed against it`, `118px shrink`, `state commit`, `lightweight-charts table`, `1px separator rows`, `run 36051830283`, `t≈3.0s / t≈5.2s`, `346px / 464px`, `clipped, WRONG layout`. No `validated` / `certified` / `production-ready` / `GA` markers; no claim that the flake is "fixed in production" — only that the test no longer flakes on the previously-rejected shape.
7. **No new test fixture, no new test ID, no new DOM hook.** The diff does not add a `data-*` attribute, an `id`, or a test-only DOM element. It reads DOM the product already exposes (`--subpanes`, `--drawing-host-height`, the `tr` rows of the lightweight-charts table, `.chart-body` height). The PR body's explicit "no test hook" disclosure holds.
8. **The 6 legacy findings are pre-existing debt on untouched files** — `terminal/components/alerts/AlertTimeline.tsx:45` raw_slug_interpolation `"verdict"`; `terminal/components/alerts/WatchingList.tsx:38` raw_slug_interpolation `"verdict"`; `terminal/components/fin/ForecastPage.tsx:647` raw_slug_interpolation `"type"`; `terminal/components/gexdesk/ExposureMatrix.tsx:488` raw_slug_interpolation `"state"`; `terminal/components/settings/SectionAccount.tsx:542` raw_slug_interpolation `"kind"`; `terminal/lib/visualIntelligenceCopy.ts:64` raw_state_enum `"DELAYED_15M"` (waiverReason: "transport basis is compared here, never rendered; the returned key selects localized copy"). None of these are on the PR's diff; none block under the `enforce-added` forward-only discipline.

## Theme findings

Terminal is a binding dark-only product surface under `docs/TERMINAL_UI_DOCTRINE_2026-07-28.md` §1/§4 ("Dark-only: the Terminal is `data-theme="dark"` hard-coded. Do not add light-theme rules; do not import the macro light block"). The doctrine's evidence matrix is therefore dark treatment × EN/ZH × desktop/tablet/phone.

**Verdict: PASS.** This PR has no theme surface at all.

1. **No CSS file modified.** `git diff --stat origin/master HEAD -- "**/*.css" "**/*.scss" "**/*.module.css"` returns zero paths. The only file touched is `terminal/e2e/marker-tooltip.spec.ts`.
2. **No `@media` query introduced.** `grep -E "@media" <diff>` returns zero matches. The diff does not open a media-query block, does not rebasing a breakpoint, does not introduce a coarse-pointer / coarse-hover rule, and does not touch `globals.css`.
3. **No `data-theme` / `prefers-color-scheme: light` / `light-dark()` / `:root.light`.** Verified via `grep -E "data-theme|prefers-color-scheme|light-dark\(|:root\.light" <diff>` — zero matches. The Terminal's hard-coded `data-theme="dark"` posture is unchanged.
4. **No color token referenced or rebased.** `grep -E "var\(--|#[0-9a-fA-F]{3,6}|hsl\(|rgba\(" <diff>` returns zero matches. The diff uses no paint value of any kind.
5. **No direction-color misuse.** Search for the doctrine's direction family (`#26c281` / `#f0566b` / `up` / `down` / `buy` / `sell` / `rgba(38,194,129` / `rgba(240,86,107`) returns zero matches.
6. **Type ramp / 4px spacing grid untouched.** The diff contains no `px` / `rem` / `--fs-` / `--r-` / `--sp-` declaration. The single number literals (`-1`, `-2`, `0`, `1`) are arithmetic for the `declared` / `drawn` counters and the `fills` tolerance, not visual units.
7. **No new screenshot, no visual evidence change.** `git diff --stat` includes zero PNG/SVG/JPEG paths. The mobile chart page renders identically; the e2e flake was never a visual defect, only a test-time readiness defect (the tooltip *element* rendered, but with an empty `data-marker-at`, because the tap missed the marker).
8. **Dark-only doctrine posture intact.** The Terminal shell's `data-theme="dark"` is not modified; no light-theme CSS is imported; no `@media (prefers-color-scheme: light)` rule is added; no token is rebased. The diff is a test-side binding to the product's existing pane-accounting surface, not a styling change.

## Validated-claims findings

`check_validated_claims.py` lives on macro only (`scripts/check_validated_claims.py`); it does not run on the terminal repo. The standing house law (CLAUDE.md §Epistemics) still applies: "The word 'validated' in user-facing text is CI-enforced." The Terminal repo has no equivalent CI gate, but the audit reads against the same standing rule.

**Verdict: PASS.**

1. **No `validated` / `certified` / `approved` / `production-grade` / `production-ready` / `GA` / `generally available` strings introduced.** Verified via `grep -nE "validated|certified|approved|production-grade|production_ready|GA\b|generally.available|guaranteed|proven|verified" <diff>` — the only match is line 187 inside an *existing* (pre-existing) code comment: `proven by hand — so the wait belongs in the test, not a retry`. That comment describes a developer-side manual repro procedure, was not introduced by this PR, and is in source code, not user-facing copy. The line predates this PR (verified via `git log -p -L 187,187:terminal/e2e/marker-tooltip.spec.ts` — the surrounding block originates in the `expect.poll` readiness paragraph already in `origin/master`).
2. **No `premium` / `paid` / `tier` / `enterprise` markers introduced.** Verified via `grep -nE "premium|paid|tier|enterprise" <diff>` — zero matches in the changed region. The diff contains the CSS custom property `--subpanes` (read-only), not a tier indicator.
3. **The PR body makes no validated claim.** The PR body §"What was flaking" describes the failing trace shape ("expect(locator('.mm-sig-tip')).toBeVisible() times out on an empty `<div role="tooltip" class="mm-sig-tip"></div>` — no `data-marker-at`, no text") and contrasts it with the MAS-234 shape ("tooltip opened with the right shape"). The body is explicit that this is "not the MAS-234 shape" — i.e., it is a different flake, not a regression of a prior validated guarantee.
4. **The "settle on the chart's pane accounting, not a still marker field" framing is honest.** The PR body documents the exact production run where the flake surfaced (`run 36051830283: sub-panes removed by t≈3.0s, --subpanes: 0 committed at t≈5.2s`), names the saturated-runner window in which the deferred `--subpanes` commit falls outside the prior `expect.poll` window, and explicitly disclaims a behavioral change: "This does not widen a hit target, gesture threshold, timeout, or product behavior; it only refuses to measure the fixture while its layout is still being committed." That is the canonical "no validated claim, just an honest measurement-bounds tightening" framing — the inverse of a "validated" or "production-grade" assertion.
5. **No `premium` / `certified` / `guaranteed` markers in any test fixture name.** The diff introduces no new fixture, no new constant, no new exported helper name beyond `markerField` (a test-side read helper, not user-facing). The fixture name `markerField` is descriptive of what it reads, not a tier or claim indicator.
6. **No claim of a fix in production.** The PR body frames the change as a flake repair in the e2e suite; no paragraph claims the production tap behavior is now "validated" or "guaranteed" — it claims only that the test no longer accepts an unsettled fixture as a settled one. The contract the product already exposed (`--subpanes`, `--drawing-host-height`, the lightweight-charts table) was always there; this PR makes the test read it.
7. **The `merge-on-green` arm is the only release evidence.** Per the standing completion contract, the PR's `merge-on-green` label is the sweeper's evidence that the head's binding checks concluded clean (the spurious `Workers Builds: macro` X excluded); the head was not pushed against a red. No `validated` claim is required or made — the merge is the bound, and the bound is the merge.

## Overall verdict

**PASS** — this is a clean half-B (MO-B F11-10b) e2e flake repair that tightens test-side readiness to the product's pane-accounting surface, with zero product / API / UI / theme / i18n / data-contract change.

- **Plain-language: PASS.** Zero blocking findings on ADDED lines (`blocking:0`); 6 legacy entries are pre-existing debt on untouched files (forward-only discipline); zero nulls. No new string literal in any user-visible position; no i18n key touched; no banned-vocab state enum, study slug, or direction token introduced.
- **Theme: PASS.** Zero CSS, zero color token, zero `@media` query, zero `data-theme` / `prefers-color-scheme` reference. The Terminal's hard-coded dark-only posture is intact; the doctrine's evidence matrix is unchanged because no paint value moved.
- **Validated-claims: PASS.** Zero `validated` / `certified` / `approved` / `production-ready` / `GA` strings introduced. The one pre-existing "proven by hand" comment at line 187 is in source code, not user copy, and predates this PR. The PR body is explicit that this is a measurement-bounds tightening ("this does not widen a hit target, gesture threshold, timeout, or product behavior"), not a validation claim.

**Recommendation:** Audit complete. No remediation required. The PR is a textbook test-side binding to a pre-existing product surface — `markerField` reads the `--subpanes` / `--drawing-host-height` / lightweight-charts table state the product already publishes, gates readiness on `declared === drawn && fills`, and resets the `prev` key on unsettled reads so a stale-coordinate read never counts as the first of two. The flake window the PR documents (`run 36051830283: sub-panes removed by t≈3.0s, --subpanes: 0 committed at t≈5.2s`) is precisely the kind of deferred-commit saturation flake the e2e shard catches under load, and the new invariant is the canonical fix. No follow-up audit needed unless a sibling e2e spec (`mobile-chart-hub-upgrade.spec.ts`, `mobile-chart-chrome.spec.ts`) shows the same shape on a future shard rerun — in which case the `markerField` helper is the right shared primitive to lift to `terminal/e2e/chartLayout.ts` (out of scope here; only `marker-tooltip.spec.ts` flakes).