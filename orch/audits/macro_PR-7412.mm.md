# Audit — mastermindx-market-intelligence/macro PR #7412

Mode: qwen_auditor2-style (plain-language / theme / validated-claims laws). One-pass audit against the merged head. No durable writes outside this report.

## PR metadata

| field | value |
| --- | --- |
| repo | `mastermindx-market-intelligence/macro` |
| PR | [#7412](https://github.com/mastermindx-market-intelligence/macro/pull/7412) |
| title | `fix(basket-detail): simplify technical UX and score disclosures` |
| merged | 2026-09-20T06:56:41Z via squash-merge to `main` (the substantive `fix(basket-detail)` half-B in the 24-h window after the orch(audit) record PRs and the closeout #7476; this PR landed between the prior `fix(basket-detail): preserve score state…` and the later `fix(basket-detail): render snapshot details…` siblings, all un-audited until this report). |
| head | squash of `e8695877fb83b71db450ce498d8a15147da328fc` (4 squashed commits: `fix(basket-detail): simplify technical UX and disclosures` `5f7a17011c` → `ci: own basket detail UX regression suite` `1abbce4c5a` → `fix(basket-detail): use governed radius token` `e03fbb9121` → `test(basket-detail): add visual evidence receipt` `76a99df43e`; co-authored by `Sol Integration Proof`). |
| author / merger | `chriswong6031-creator` (operator, META-CEO A seat). |
| base | `origin/main` at the pre-merge base. |
| files | **144 files, +4,312 / −2,807** dominated by regenerated `site/basket/`, `site/basket_china/`, `site/basket_hk/`, `site/basket_canada/`, `site/basket_intl/` plain copies (+27/−23 each, ~121 files), `templates/basket_detail.html.j2` (+19/−37, the rewritten footer + the new `TIER_LABELS`/`tierBadgeHtml` helper + the dropped `display-only` chips + the new `<details>` disclosure + the simplified macroAbsent footnote + the simplified Potential disc + the rewritten fast-watch copy), `site/assets/css/fb966e77.css` (NEW, 267 lines, replaces `ff321a97.css`), `tests/test_basket_detail_glance_copy.py` (NEW, 129 lines, the 8-test plain-language regression suite for the 121 committed pages), `mockups/evidence/basket-detail-ux/` (NEW: `EVIDENCE.yml` + `manifest.json` + `smells.json` + 16 PNG cells), `.github/ci/legacy-jobs.yml` (1 line — adds `tests/test_basket_detail_glance_copy.py` to the FTR tape-surface pack). |
| half-B label | **half-B (Basket Detail UX wave, simplify-and-disclose round).** Sized between the trivial CSS rebases and the full engine redesigns: one Jinja template edit + one new fingerprinted CSS sheet + one new 129-line test file + one new evidence matrix + a 121-page plain-copy refresh that fans out from the same template re-render. |
| scope | **User-facing UX simplification of the 121 committed Basket Detail pages (US + China + Hong Kong + Canada + International outputs).** Replace visible `T1/T2/T3/T4` entry-code badges with reader-language labels (`confirmed setup / early setup / forming / first sign`); drop the visible `display-only` chips from the `Same narrative elsewhere` and `Timing & risk` headings; rewrite the Fast-turn Watch helper from `T1/T2 jargon` to `early-turn sign`; shorten the long Potential methodology paragraph; move W8 `Score Anatomy` mechanics behind a collapsed `<details>` disclosure (`How this score is built / 评分如何构成`); rewrite the footer to concise research/not-investment-advice guidance; rephrase the macroAbsent footnote to remove internal jargon (`W8b pre-registration (FT-R7)` → plain clause). Ranking weights and tier weights are unchanged (`TIER_W={T1:0.9,T2:1.0,T3:0.6,T4:0.4}` survives intact). No basket score formula, recommendation ordering, tier weights, cycle gates, Conviction Profile inputs, market-pulse logic, or signal authority changed. |
| checks | PR body reports 148 tests passing: FTR W1/W3 UI, group-read, stock-personality, product chrome, subdir nav, and the new Basket Detail glance-copy coverage. New UX guard (`tests/test_basket_detail_glance_copy.py`) verifies all 121 committed pages AND the CSS SHA-256 filename contract (the `…\?v=\1` fingerprinted wire). Chromium proof: US desktop 1440x1000, US mobile 390x844, China desktop 1440x1000 — HTTP 200, zero horizontal overflow, zero JS page errors. `mockups/evidence/basket-detail-ux/manifest.json` carries the full 8-cell matrix (dark × light × en × zh × 1440 / 390) PLUS a `score-focus` forced state (the `<summary>` of the new disclosure opened) for each cell, totaling 16 PNG cells. Visible US tier proof reads `confirmed setup`, never raw `T1`. `git diff --check` clean; `scripts/build_theme_detail.py` compiles. |
| gating scripts | `scripts/check_plain_language.mjs` — DOES NOT EXIST in macro (terminal-side only). Plain-language discipline on macro is read against the standing design-doctrine rules AND the new `tests/test_basket_detail_glance_copy.py` 8-test regression suite the PR itself ships. `scripts/check_validated_claims.py` exists and runs (no `allowlist-missing` fault on this checkout). `scripts/check_design_system.py` and `scripts/check_runtime_style_injection.py` exist (TP-0 art-direction gate). |

## Diff content (scoped to this PR)

**`templates/basket_detail.html.j2` (+19 / −37)**

- NEW CSS block at lines 140–148 (5 rules for `.ftr-anatomy-disclosure` + `> summary` + open/closed chevron + focus-visible ring).
- Footer rewritten (lines 327–329): `'Read-only analytics, not investment advice. Theme scores organize what to study; they are descriptive, not a validated buy list. Member ratings reuse the stock dashboard Conviction Profile and trust tier. Treat them as a focus list, never an order.' / '只读分析，非投资建议。主题评分用于整理研究重点；它是描述性读数，不是已验证买入清单。成分股评分复用股票看板的信念档案与信任层级。请把它当作关注清单，绝非下单指令。'` → `'Research dashboard, not investment advice. Theme and member scores help prioritize what to study; they are not a buy list or an order. / 研究看板，非投资建议。主题与成分股评分用于安排研究优先级，不是买入清单或下单指令。'`. Note: the OLD string contained the word `validated` in a user-visible position (`not a validated buy list / 不是已验证买入清单`); the NEW string drops BOTH occurrences of the banned-vocab word. This is a validated-claims WIN.
- NEW `TIER_LABELS` map (lines 359–364): `T1:['confirmed setup','已确认机会']`, `T2:['early setup','早期机会']`, `T3:['forming','形成中']`, `T4:['first sign','初现迹象']`. The keys `T1/T2/T3/T4` survive only as engine-internal anchors; the user-visible badge text is the plain-word label.
- NEW `tierBadgeHtml(tier,provisional)` helper (lines 366–371) replaces the OLD inline tier badge. The helper:
  - Returns `''` if no tier.
  - Looks up the bilingual label from `TIER_LABELS`.
  - Builds a `data-tip-en` / `data-tip-zh` hover popover describing the tier ("The entry read is active. Check the wider setup and your risk plan before acting." / "入场判断已生效。操作前请结合整体布局和自己的风险计划。" for T1, etc.).
  - Falls back to `['entry setup','入场机会']` for an unknown tier.
  - Emits `<span class="tier ${esc(tier)}" data-tip-en="Technical tier T1: ..." data-tip-zh="技术层级 T1：...">${L(lab[0],lab[1])}</span>` — the technical tier code IS still in the DOM (for engine-side sorting and CSS class targeting) but is now ONLY in `class=` and the `data-tip-*` hover popover, NOT as the badge's visible text.
- `display-only` chip removed from the `Same narrative elsewhere` heading (line 573): `+L('display-only','仅展示')} ⓘ</span></h2>` → `+</h2>`. The internal `display-only` framing is no longer published as a heading badge.
- `display-only` chip removed from the `Timing & risk textures` heading, AND the heading renamed: `'Timing &amp; risk textures','择时与风险纹理'}` + `<span class="chip">${L('display-only','仅展示')}</span>` → `'Timing &amp; risk','择时与风险'}` with no chip. The OLD "textures" framing is gone too — the heading is now plain noun.
- Fast-turn Watch helper copy rewritten (line 1158): `'A fresh T1/T2 signal shows up here instead of being quietly dropped. The blocker column says why it is still not a buy.' / '新出现的 T1/T2 信号会显示在这里，不会被悄悄丢掉。"阻断原因"一列说明它为什么还算不上买点。'` → `'A fresh early-turn sign appears here instead of being hidden. The blocker column explains why it is still not a buy. / 新的早期转折迹象会显示在这里，而不是被隐藏。"阻断原因"一列说明它为什么还算不上买点。'`. The `T1/T2` jargon is replaced with `early-turn sign / 早期转折迹象`.
- Fast-turn Watch tier cell now uses the new helper (line 1160): `<span class="tier ${esc(x.tier)}">${esc(x.tier)}${x.provisional?'<span class="prov">*</span>':''}</span>` → `${tierBadgeHtml(x.tier,!!x.provisional)}`. Visible tier code `T1` replaced with `confirmed setup` (and ZH twin).
- Holdings Potential tooltip simplified (line 1336): `'Potential is buy-readiness, not signal speed: slow-cycle state, room to run, survivability and entry timing. A fresh T1/T2 can coexist with a low score.'` → `'Potential combines cycle state, room to run, survivability and entry timing. A fast setup can appear before the slow cycle confirms.'`. The `T1/T2` jargon is gone, and the explanation is shorter and plainer.
- Holdings Potential disc paragraph simplified (line 1338): the OLD 86-word paragraph (`'Potential ranks buy readiness, not the speed of a turn: it blends slow-cycle state, room to run, survivability and entry timing. A fresh T1/T2 can therefore coexist with a low Potential when the slow cycle has not confirmed. The default order surfaces fresh T1–T4 confluence first, then blends verdict quality, Potential and entry timing while pushing cycle-blocked names down (T3* is provisional and may repaint). ⚠ means the cycle blocks a buy.'`) is replaced with a 32-word plain clause: `'Potential combines cycle state, room to run, survivability and entry timing. The default order puts fresh entry setups first, then weighs verdict quality and Potential; cycle-blocked names move down. ⚠ means the cycle blocks a buy. / 潜力综合周期状态、上涨空间、生存质量和入场时机。默认排序优先显示新的入场机会，再结合结论质量与潜力；被周期阻断的个股会下调。⚠ 表示周期阻止买入。'`. The OLD `T1/T2/T3/T4`/`confluence`/`repaint` jargon is GONE; the test `test_potential_explanation_is_short_and_non_engineering` enforces ≤48 words AND forbids `T1/T2/T3/T4/confluence/repaint` substrings in the new explanation.
- `Score Anatomy / 评分解剖` heading renamed to `Score details / 评分详情` (line 1429): `'+L('Score Anatomy','评分解剖')+'` → `'+L('Score details','评分详情')+'`. The OLD internal vocabulary is replaced with a plain noun.
- MacroAbsent footnote rewritten (line 1468): `'Macro absent: score renormalises over '+nAvailLegs+' available legs. Adding a macro prior changes the calibrated score — requires W8b pre-registration (FT-R7). / 宏观缺失：评分在现有腿上重新归一化。添加宏观先验会改变校准评分 — 需W8b预注册（FT-R7）。'` → `'Macro context is absent, so the score is rebalanced across '+nAvailLegs+' available inputs. Adding macro context would change the calibrated score, so it stays excluded until separately validated. / 宏观背景缺失，因此评分会在现有 '+nAvailLegs+' 个输入之间重新平衡。加入宏观背景会改变校准评分，因此在单独验证前保持排除。'`. The OLD `W8b pre-registration (FT-R7)` engineering jargon is gone (a plain-language WIN), but the NEW string introduces the word `validated` in an affirmative forward-looking frame — see Validated-claims findings below.
- W8 `Score Anatomy` panel wrapped in a `<details>` disclosure (line 1647): `'<h2><span class="idx">W8</span>'+L('Score Anatomy','评分解剖')+' <span class="chip" style="font-size:10px;padding:1px 6px">'+L('display-only','仅展示')+'</span></h2>'+anatomyPanelHtml(th2)` → `'<details class="ftr-anatomy-disclosure"><summary>'+L('How this score is built','评分如何构成')+'</summary>'+anatomyPanelHtml(th2)+'</details>'`. The OLD heading (`<span class="idx">W8</span>Score Anatomy` + `display-only` chip) is replaced with a collapsed `<details>` whose summary reads `How this score is built / 评分如何构成`. The W8 code-prefix (`<span class="idx">W8</span>`) is REMOVED from the heading; the `display-only` chip is REMOVED; the `Score Anatomy / 评分解剖` Chinese-English pair is REMOVED. The technical score mechanics now live BEHIND the disclosure (visible only on click or screen-reader/keyboard open), per the doctrine's "technicals demoted to hover/popover/detail pages" rule.
- `tierBadge` inner function (line 1233 → 1253) simplified to delegate: `const tierBadge=(c)=>{const s=c&&c.signal; if(!s||!s.tier) return ''; const tipEn={...}...; return ` <span class="tier ${s.tier}" data-tip-en="${tipEn}" data-tip-zh="${tipZh}">${s.tier}${s.provisional?'<span class="prov">*</span>':''}</span>`;};` → `const tierBadge=(c)=>{const sg=c&&c.signal; if(!sg||!sg.tier) return ''; return ' '+tierBadgeHtml(sg.tier,!!sg.provisional);};`. The inline `{T1: 'T1 CONFIRMED...', T2: 'T2 PRIME...', T3: 'T3 APPROACHING...', T4: 'T4 FIRST SPARK...'}` map is REMOVED from the `tierBadge` body and replaced with the shared `tierBadgeHtml` helper.

**`site/assets/css/fb966e77.css` (NEW, 267 lines — replaces `ff321a97.css`)**

- The CSS lives in a NEW fingerprinted sheet `fb966e77.css` (sha256[:8] = `fb966e77`); all 121 generated basket pages are re-pointed to this fingerprint (`href="../assets/css/fb966e77.css?v=fb966e77"`); the test `test_generated_pages_share_the_new_fingerprinted_stylesheet` enforces `len(refs) == 1` and sha256[:8] == fingerprint match.
- NEW `.ftr-anatomy-disclosure` block at lines 132–137 of the new sheet: container `margin:8px 0 2px;`; `> summary` with `cursor:pointer; list-style:none; width:max-content; max-width:100%; color:var(--link); font-size:12px; font-weight:700; padding:5px 8px; border-radius:var(--r-sm);`; `::-webkit-details-marker {display:none;}`; `::after {content:'  ›'; color:var(--muted);}`; `[open] > summary::after {content:'  ⌄';}`; `> summary:focus-visible {outline:2px solid var(--link); outline-offset:2px;}`.
- Token substitution: every color reference in the new block is `var(--token)` (`var(--link)`, `var(--muted)`, `var(--r-sm)`) — zero raw hex literals; the radii use the governed radius token.
- No `html[data-theme="light"]` overrides for the new block — the disclosure is intentionally theme-agnostic and inherits the page's `var(--link)` and `var(--muted)` semantics for whichever theme is active. The new component's geometry (chevron `›`/`⌄`, padding, focus ring, summary hover) is shared across themes; only the resolved semantic color of `var(--link)` differs between dark and light. This is the doctrine-correct treatment for a control whose identity is "command surface", not material — the visual identity stays "press to expand", and the link ink tone picks up the theme.
- Existing rules for `.ftr-anatomy-card`, `.ftr-anatomy-hd`, `.ftr-ah-score`, `.ftr-leg-*`, `.ftr-anatomy-ft`, `.ftr-crowd-note` (lines 111–130) are UNCHANGED — they apply when the disclosure is OPEN. The new block only governs the closed summary affordance.
- The rest of the 267-line sheet (lines 138–266) is the existing basket detail CSS, carried over from the previous sheet. Word-boundary grep for `#[0-9a-f]{6}|#[0-9a-f]{3}\b` over the FULL sheet returns a handful of raw hex literals in pre-existing rules (e.g., `:root {--gr:#8f8ae0;}` line 152, `html[data-theme="light"] {--gr:#5a51bd;}` line 153, `.gpr-tile` background `#f3f6fb` line 173, `.tier.T4 {background:#3c4a8c;}` line 66, plus the various `rgba(...)` shadow + fill literals) — none of these are NEW to this PR; they all carried over from the prior fingerprinted sheet and are within the pre-existing TP-0 budget for non-token-bound surface tokens.

**`tests/test_basket_detail_glance_copy.py` (NEW, 129 lines)**

8 tests that lock the plain-language contract on the template AND all 121 generated pages:

1. `test_primary_section_names_are_reader_language` — asserts `Timing &amp; risk textures` NOT in src; `${L('Timing &amp; risk','择时与风险')}` IS; `Fast tape is display-only` NOT in src; `Fast moves are context` IS; `Same narrative elsewhere','其他市场的同一叙事')} <span class=\"chip\"` NOT in src.
2. `test_entry_tier_codes_are_demoted_to_tooltip_detail` — asserts `const TIER_LABELS=` IS; `confirmed setup`, `early setup`, `forming`, `first sign` ARE; helper carries `Technical tier` substring; visible badge does NOT contain `>${s.tier}`; `tierBadgeHtml(sg.tier` IS called from the `tierBadge` shim; `T1 CONFIRMED`, `T2 PRIME`, `T3 APPROACHING`, `T4 FIRST SPARK` NOT in src (the OLD uppercase tier labels are dead).
3. `test_fast_turn_watch_uses_plain_stage_words` — asserts `A fresh T1/T2 signal shows up here` NOT in src; `A fresh early-turn sign appears here` IS; `tierBadgeHtml(x.tier` IS called in the watch table; `>${esc(x.tier)}` NOT in watch (visible tier code is gone).
4. `test_potential_explanation_is_short_and_non_engineering` — extracts the plain Potential explanation, asserts `len(words) <= 48` AND forbids `T1`, `T2`, `T3`, `T4`, `confluence`, `repaint` substrings in the explanation.
5. `test_score_mechanics_are_progressive_disclosure` — asserts `Score Anatomy / 评分解剖` NOT in src; `<details class="ftr-anatomy-disclosure">` IS; `How this score is built / 评分如何构成` ARE; `<span class="idx">W8</span>` NOT in src; `W8b pre-registration`, `FT-R7` NOT in src.
6. `test_footer_is_concise_product_guidance` — asserts `Conviction Profile and trust tier` NOT in src; `validated buy list` NOT in src; `Research dashboard, not investment advice` IS.
7. `test_all_committed_basket_pages_match_the_glance_contract` — iterates every page under `site/basket/`, `site/basket_china/`, `site/basket_hk/`, `site/basket_canada/`, `site/basket_intl/` (asserts `len(pages) >= 100`) and forbids `Timing &amp; risk textures`, `Fast tape is display-only`, `A fresh T1/T2 signal shows up here`, `Potential ranks buy readiness, not the speed of a turn:`, `Score Anatomy`, `评分解剖`, `<span class="idx">W8</span>`, `Conviction Profile and trust tier`; asserts `How this score is built` and `Research dashboard, not investment advice` ARE present.
8. `test_generated_pages_share_the_new_fingerprinted_stylesheet` — asserts every page references `../assets/css/<8-hex>.css?v=<8-hex>` with a consistent digest, that the CSS file at that path exists, that its sha256[:8] matches the digest, and that it carries `.ftr-anatomy-disclosure > summary`.

This is the doctrine-correct enforcement shape: a 129-line plain-language regression suite that BOTH locks the template source AND sweeps every committed output page. Without it, a re-render of the template could regress these strings; with it, any PR that re-introduces a banned phrase across ≥121 pages fails CI on the first page it touches.

**`mockups/evidence/basket-detail-ux/` (NEW evidence matrix)**

- `EVIDENCE.yml` (`schema: mastermind.page_evidence_receipt.v1`, `changed_paths: templates/basket_detail.html.j2`, `manifest: mockups/evidence/basket-detail-ux/manifest.json`).
- `manifest.json` (458 lines): per-cell records for the 16 cells (8 base cells `theme(dark|light) × lang(en|zh) × viewport(1440|390)` × `basket_ai_semiconductors.html` reference page, PLUS 8 `score-focus` forced-state cells on the same matrix where the `<summary>` of the new `<details>` is opened before the shot). Each cell records `file`, `sha256`, `viewport_width/height`, `theme`, `locale`, `applied_locale`, `applied_theme`, `applied_force_state`, `bytes`, `height`. The `axes` block records `force_states: [{name: "score-focus", kind: "focus", spec: "score-focus:focus(.ftr-anatomy-disclosure > summary)", value: ".ftr-anatomy-disclosure > summary"}]`.
- `smells.json` (81 lines): per-cell smell metrics (heading counts, long paragraphs, raw slug hits, todo placeholders, horizontal overflow, console errors, payload bytes).
- 16 PNGs: 8 base + 8 `score-focus` cells.
- The PR body cites Chromium proof for `US desktop 1440x1000`, `US mobile 390x844`, `China desktop 1440x1000` (HTTP 200, zero horizontal overflow, zero JS page errors), and the `score-focus` state is forced on the `<summary>` of the new disclosure.

**`site/basket*/** (~121 files, ~+27 / −23 each)** — plain-copy refresh from the same template re-render. Sha256 deltas only.

## Plain-language findings

The macro repo has no `scripts/check_plain_language.mjs` (terminal-side only). Plain-language discipline on macro is read directly against the standing design-doctrine rules AND the new `tests/test_basket_detail_glance_copy.py` 8-test regression suite the PR itself ships (a stricter, project-specific gate that runs on every CI pass). PR #7412 carries substantial CHANGES to user-facing copy in `templates/basket_detail.html.j2` and a NEW 267-line CSS sheet that introduces the new `.ftr-anatomy-disclosure` block. Every string is checked below.

**Verdict: PASS.**

1. **The new entry-tier reader labels are doctrine-correct plain word.** The 4 tier→word pairs are bilingual side-by-side, and each is a short plain clause, not a metric name or technical noun: `T1→confirmed setup / 已确认机会`, `T2→early setup / 早期机会`, `T3→forming / 形成中`, `T4→first sign / 初现迹象`. None carries a raw value, integer, or "higher/lower than N%" framing on the badge tier — the technical tier code IS still in the DOM (in `class=` and the `data-tip-*` hover popover) but is now ONLY in those engineering anchors, NOT as the badge's visible text. The one-integer law is honoured (the badge owns the word; the hover owns the technical anchor).
2. **The technical tier code is preserved in the right altitude.** The new `tierBadgeHtml` helper builds a `<span class="tier T1" data-tip-en="Technical tier T1: …" data-tip-zh="技术层级 T1：…">${L('confirmed setup','已确认机会')}</span>` — the `class="tier T1"` anchor remains so engine-side CSS (`.tier.T1`, `.tier.T2`, `.tier.T3`, `.tier.T4`) and the tier-class drive (the four fill colors) still works, AND the hover popover still names the technical tier for the user who wants it (per the doctrine's "technicals demoted to hover/popover/detail pages" rule). This is the doctrine-correct progressive disclosure: glance tier = the plain word, hover tier = the technical anchor, sort/rank tier = the engine-side code that never reaches the visible badge.
3. **No internal metric names on the glance tier.** The OLD `T1 CONFIRMED: the entry read is active…`, `T2 PRIME: an early, higher-conviction entry window…`, `T3 APPROACHING: an early entry read that is not confirmed yet…`, `T4 FIRST SPARK: the earliest and least-certain entry read…` (and ZH twins `T1 CONFIRMED（已确认）…`, `T2 PRIME（最佳入场）…`, `T3 APPROACHING（临近）…`, `T4 FIRST SPARK（初现）…`) are REMOVED in this PR — the test `test_entry_tier_codes_are_demoted_to_tooltip_detail` asserts `T1 CONFIRMED`, `T2 PRIME`, `T3 APPROACHING`, `T4 FIRST SPARK` are NOT in src. The new popover phrasing (`The entry read is active. Check the wider setup and your risk plan before acting.` / `入场判断已生效。操作前请结合整体布局和自己的风险计划。`) is plain word — no `T1 CONFIRMED`-style all-caps tier-name prefix.
4. **No banned-vocab occurrences in NEW user-visible strings (other than the single flagged "until separately validated" finding below).** Word-boundary grep on the diff hunks for `validated | verified | certified | approved | proofed | gauntlet | proven` returns:
   - `validated` — 1 hit in NEW user-visible strings: `'…so it stays excluded until separately validated. / …在单独验证前保持排除。'` (the macroAbsent footnote). This is the **flagged** occurrence; see Validated-claims findings #2 below.
   - `verified` — 0 hits in NEW user-visible strings.
   - `certified` — 0 hits.
   - `approved` — 0 hits.
   - `proofed` — 0 hits.
   - `gauntlet` — 0 hits.
   - `proven` — 0 hits.
5. **The OLD strings that DID carry tier-claim-adjacent vocabulary are REMOVED.** The OLD footer (`'…they are descriptive, not a validated buy list. Member ratings reuse the stock dashboard Conviction Profile and trust tier. Treat them as a focus list, never an order.' / '…它是描述性读数，不是已验证买入清单。成分股评分复用股票看板的信念档案与信任层级。'`) is replaced with the concise `'Research dashboard, not investment advice. Theme and member scores help prioritize what to study; they are not a buy list or an order.'` — both occurrences of `validated` AND `Conviction Profile and trust tier` AND `validated buy list` AND `已验证买入清单` are REMOVED. The test `test_footer_is_concise_product_guidance` asserts `Conviction Profile and trust tier` and `validated buy list` are NOT in src. This is a validated-claims WIN in the footer.
6. **The OLD internal-jargon strings are REMOVED.** The OLD macroAbsent footnote (`'…requires W8b pre-registration (FT-R7). / …需W8b预注册（FT-R7）。'`) is rewritten — the engineering-vocabulary `W8b pre-registration` and `FT-R7` are GONE (the test `test_score_mechanics_are_progressive_disclosure` asserts both are NOT in src). The OLD `Score Anatomy / 评分解剖` heading is renamed to `Score details / 评分详情` AND wrapped in a `<details>` disclosure. The OLD `<span class="idx">W8</span>` heading prefix is REMOVED (the test asserts `<span class="idx">W8</span>` NOT in src). The OLD `Timing & risk textures / 择时与风险纹理` heading is renamed to `Timing & risk / 择时与风险` AND its `display-only` chip is REMOVED. The OLD `Same narrative elsewhere … <span class="chip" title="…">display-only ⓘ</span>` heading drops the `display-only` chip. The OLD fast-turn Watch copy (`'A fresh T1/T2 signal shows up here…' / '新出现的 T1/T2 信号会显示在这里…'`) is rewritten to `'A fresh early-turn sign appears here…' / '新的早期转折迹象会显示在这里…'`. The OLD long Potential paragraph (86 words, with `T1/T2/T3/T4 confluence`, `T3* is provisional and may repaint`) is replaced with a 32-word plain clause (the test enforces `len(words) <= 48` AND forbids `T1/T2/T3/T4/confluence/repaint` substrings). The OLD `'Fast tape is display-only; 5/20/60d trend, breadth and the slow-cycle gate still govern the recommendation.'` is rewritten to `'Fast moves are context; the slower trend, breadth and cycle still drive the recommendation. / 快速波动只作背景；较慢的趋势、广度和周期仍决定建议。'` — the `display-only` self-labeling is GONE; the `5/20/60d` raw-window framing is simplified to `the slower trend`; `still govern the recommendation` → `still drive the recommendation`. This PR materially reduces the surface area of jargon AND tier-claim-adjacent vocabulary in the rendered page.
7. **Null disclosure is doctrine-compliant.** The macroAbsent footnote is the only NEW user-visible string that names an absence — `Macro context is absent, so the score is rebalanced across X available inputs. / 宏观背景缺失，因此评分会在现有 X 个输入之间重新平衡。` It describes the state of the inputs (rebalancing across available inputs), not "we have validated X and only X" — the doctrine-correct "quiet, not refuted" framing for an absent lane. (The second clause, `until separately validated`, is the validated-claims concern flagged separately.)
8. **Bilingual EN/ZH parity is preserved.** Every NEW EN string has its ZH twin in the adjacent `L(en, zh)` call. No EN-only or ZH-only skew on the new strings; no `<span class="l-en">` without an adjacent `<span class="l-zh">` and vice versa (verified by grepping the new tier labels, the new helper function, and the new macroAbsent footnote — every `L('English','中文')` call carries both).
9. **No translated text in `title=` attributes (CI-guarded rule).** The diff has no `title="…"` attributes added or modified. (The pre-existing `title="contested — faster descriptive textures disagree with the validated label: ${…}"` on line 1101 is unchanged from prior commit — pre-existing tier-claim-adjacent vocabulary, NOT new in this PR.) Compliant.
10. **No raw hex literals in the NEW CSS block.** Word-boundary grep for `#[0-9a-f]{6}|#[0-9a-f]{3}\b` over lines 132–137 of the new `fb966e77.css` returns zero hits. Every color reference in the new disclosure block is `var(--link)`, `var(--muted)`, or `var(--r-sm)` — token-driven. The new design-system checker `scripts/check_design_system.py --mode enforce-added` should pass.
11. **The new `<details>` disclosure is theme-agnostic by design.** The new block carries no `html[data-theme="light"]` overrides; it relies on `var(--link)`, `var(--muted)`, and `var(--r-sm)` for its color and radius, all of which are theme-token-driven. The geometry (chevron `›` / `⌄`, padding, focus ring, summary hover) is shared across themes; only the resolved token color of `var(--link)` differs between dark and light. This is the doctrine-correct treatment for a control whose identity is "press to expand", not material — the visual identity stays "command surface" and the link ink tone picks up the theme. The PR body itself acknowledges the change by noting "score detail starts collapsed and opens on demand in browser proof" — the disclosure's progressive-disclosure behaviour is the doctrine-correct way to gate a tier-claim-adjacent surface (the W8 score mechanics) behind user intent.

Net new banned-vocab occurrences in user-visible strings (other than the flagged single `until separately validated`): **0**. Net new tier-claim-adjacent verb occurrences: **0** (the OLD `T1 CONFIRMED`, `T2 PRIME`, `T3 APPROACHING`, `T4 FIRST SPARK`, `display-only`, `Conviction Profile and trust tier`, `validated buy list`, `W8b pre-registration`, `FT-R7`, `confluence`, `may repaint`, `the slow-cycle gate still govern the recommendation` are ALL removed or simplified). Plain-language discipline is honored end-to-end except for the single flagged validated-claims finding in #2 of the next section.

## Theme findings

TP-0 art-direction law in force (operator 2026-08-27): dark and light are TWO art directions, not one skin. The new CSS block added to `site/assets/css/fb966e77.css` (lines 132–137) is the canonical case to apply TP-0 to. The PR also replaces the previous basket CSS sheet with a new fingerprinted one (`ff321a97.css` → `fb966e77.css`).

**Verdict: PASS.**

1. **The new disclosure block is theme-token-driven, no token-substitution-only artifact.** The new `.ftr-anatomy-disclosure` block does NOT just swap `--link` for an alternate `--link-light` token; it composes a single block whose color references are `var(--link)` (the semantic link color, which is already token-driven in both themes) and `var(--muted)` (the muted ink tone, also token-driven). The chevron glyphs (`›` and `⌄`) and the focus ring use the same tokens; the geometry (margin, padding, border-radius, font-size, font-weight) is shared across themes. The dark and light treatments of `var(--link)` and `var(--muted)` are managed by the upstream theme token system, not by per-component overrides. This is the doctrine-correct "command surface" treatment — the visual identity stays "press to expand", and the link ink tone picks up the theme.
2. **No theme-asymmetry in the SAME component's geometry.** Both themes use the same `display: <details-native>` for the container, the same `cursor:pointer; list-style:none; padding:5px 8px; border-radius:var(--r-sm);` for the summary, the same `›` / `⌄` chevron transition for the open/closed indicator, and the same `outline:2px solid var(--link); outline-offset:2px;` for the focus ring. Only the resolved semantic color of `var(--link)` differs between dark and light — geometry and behavior are shared, as TP-0 mandates ("dark and light share information architecture, component semantics, spacing/type scales, state meanings, user actions, data contracts, ordering/density law and interaction behavior").
3. **No raw hex literals in the new block.** Word-boundary grep for `#[0-9a-f]{6}|#[0-9a-f]{3}\b` over the new block returns zero hits. Every color reference is `var(--token)` or `var(--r-sm)` for the radius. The design-system checker `scripts/check_design_system.py --mode enforce-added` should pass on the new block.
4. **No raw hex literals in NEW template HTML (other than pre-existing `LABEL_COLOR`/`RECO_COLOR`/`COMP_COLOR` JS color maps).** The new `TIER_LABELS`/`tierBadgeHtml` JS helpers do not introduce any new color literal — they emit `<span class="tier ${esc(tier)}">` whose fill is owned by `.tier.T1`/`.T2`/`.T3`/`.T4` CSS rules (lines 61–67 of the new sheet), all of which use `var(--fill-up, var(--up))` or `var(--fill-info, var(--info))` token references (the `var()` chain means the resolved color is theme-token-driven). The NEW macroAbsent footnote uses `<span style="font-size:11px;color:var(--muted)">` — token-driven, not raw hex.
5. **Scoping is correct.** The new block is a CLASS selector (`.ftr-anatomy-disclosure`), scoped to a single component (the Score Anatomy card) which is itself scoped to the Basket Detail page. The new block does not leak to the macro route, the prophet routes, or any other page — only Basket Detail pages opt in via the `<details class="ftr-anatomy-disclosure">` element.
6. **No runtime style injection.** `scripts/check_runtime_style_injection.py` looks for `style.textContent`, parallel palette/token families, opaque runtime stylesheet systems, and `style.setProperty('--*', …)` inside page/composer JavaScript. The PR adds zero bytes of runtime JavaScript style injection; the touched `.j2` file does NOT add any new inline `style=` attribute (the only pre-existing inline styles — `style="margin-bottom:9px"`, `style="margin-top:5px"`, `style="margin-top:4px"`, `style="color:…"` — are unchanged); the touched `.css` file adds governed rules. Zero `style.textContent = …` assignments; zero parallel token families.
7. **Paired G6 wire sync is preserved.** `python3 -m scripts.check_template_site_sync` is the canonical paired-asset guard for `templates/<name>` ↔ `site/<name>`. PR #7412 modifies `templates/basket_detail.html.j2` (56 net lines changed), and the corresponding `site/basket/`, `site/basket_china/`, `site/basket_hk/`, `site/basket_canada/`, `site/basket_intl/` plain copies (~121 files, ~+27/−23 each) are regenerated in the same PR. The test `test_all_committed_basket_pages_match_the_glance_contract` enforces that every committed page matches the new glance contract — it iterates the full set and forbids the OLD banned phrases. The pair is consistent.
8. **The fingerprinted CSS filename contract is enforced by CI.** The test `test_generated_pages_share_the_new_fingerprinted_stylesheet` enforces:
   - Every page references `../assets/css/<8-hex>.css?v=<8-hex>`.
   - The set of referenced fingerprints has length 1 (no page references a different CSS).
   - The CSS file at `site/assets/css/<8-hex>.css` exists.
   - Its sha256[:8] matches the fingerprint.
   - The CSS file carries `.ftr-anatomy-disclosure > summary` (the new block must be in the fingerprint).
   This is the doctrine-correct wire-sync guard: the fingerprinted CSS lives at the same path on every page, and the sha256 of the file matches the path. A page referencing an old `ff321a97.css` fingerprint would fail this test.
9. **The evidence matrix is COMPLETE per TP-0 (dark × light × EN/ZH × 1440 / 390 = 8 base cells + 8 `score-focus` forced-state cells = 16 PNGs).** `mockups/evidence/basket-detail-ux/` carries:
   - 8 base full-page captures (`basket_ai_semiconductors.html` × `theme(dark|light) × lang(en|zh) × viewport(1440|390)`).
   - 8 `score-focus` forced-state captures on the same matrix (the `<summary>` of the new disclosure is opened before the shot, per `axes.force_states[0].spec = "score-focus:focus(.ftr-anatomy-disclosure > summary)"`).
   - `EVIDENCE.yml` (schema `mastermind.page_evidence_receipt.v1`).
   - `manifest.json` (per-cell records: `file`, `sha256`, `viewport_width/height`, `theme`, `locale`, `applied_*`, `bytes`, `height`).
   - `smells.json` (per-cell smell metrics: heading counts, long paragraphs, raw slug hits, todo placeholders, horizontal overflow, console errors, payload bytes).
   - 16 PNGs total.
   The full 8-cell matrix is present for the THEME that changed (dark × light × EN/ZH × 1440 / 390); the `score-focus` state proves the new disclosure opens correctly on every cell. The evidence is captured against the actual rendered page, not a stub.
10. **No dark/light asymmetry in the NEW component's behavior.** Both themes use the same `<details>` native open/closed semantics; the chevron `›` / `⌄` transition is governed by `[open] > summary::after {content:'  ⌄';}` — the same selector, same content, same color (`var(--muted)`). The focus-visible ring is the same `outline:2px solid var(--link);` in both themes; only the resolved token color differs.

No new theme debt; no color-literal addition in the new CSS block; no evidence-matrix gap; no dark/light asymmetry in the new disclosure component.

## Validated-claims findings

The standing macro law: the word `validated` (and friends: `verified`, `proofed`, `certified`, `approved`, `gauntlet`, etc.) is CI-enforced via `scripts/check_validated_claims.py`; the gate scans `templates/`, `site/*.js`, generated `*_data.js`, and `engine/` display-copy fields; context/data/detection/tagging artifacts stay display-tier until they clear the gauntlet; the word "validated" in user-facing text is CI-enforced (CLAUDE.md §Epistemics gauntlet = PROMOTION gate). The validator's regex is `validated|已验证|经验证|经过验证` (case-insensitive); its negation pattern covers `cannot ... validated`, `无...验证`, `非...验证`, `未...验证`, `未经验证`, `不...已验证` — honest disclaimers that must never be blocked.

**Verdict: PARTIAL — one new affirmative `validated` occurrence in user-visible text (a forward-looking `until separately validated` clause that is NOT covered by the validator's negation pattern and is NOT in the new `tests/test_basket_detail_glance_copy.py` test). The PR otherwise REDUCES the surface area of unearned claims by retiring four prior `validated`/`已验证` occurrences in user-visible text and removing the OLD `Conviction Profile and trust tier` tier-label framing.**

1. **The validator's gated paths are touched, and the gate would fire on the NEW occurrence.** `scripts/check_validated_claims.py` runs against `templates/`, `site/*.js`, generated `*_data.js`, and `engine/` display-copy fields. PR #7412 touches `templates/basket_detail.html.j2` (in-gate), regenerates `site/basket*/**` (~121 in-gate plain copies), and adds the new `site/assets/css/fb966e77.css` (in-gate as it is referenced from `templates/basket_detail.html.j2`). Word-boundary grep on the diff hunks for `validated`:
   - `validated` — 1 hit in NEW user-visible strings (the macroAbsent footnote: `'…so it stays excluded until separately validated.'`). The validator's regex `r"validated|已验证|经验证|经过验证"` (case-insensitive, line 168 of `scripts/check_validated_claims.py`) WOULD catch this as an affirmative claim — the negation pattern on line 21 covers `cannot ... validated`, `无...验证`, `非...验证`, `未...验证`, `未经验证`, `不...已验证`, but NOT `until ... validated` (the phrase is a forward-looking claim, not a disclaimer). The phrase implies "validations are a real category of state-change and macro-context is awaiting one" — which is a tier-claim of authority ("the rest of the score has cleared validation, this one hasn't"). On this PR's PR-head checkout, the validator would exit 1 with a finding pointing at line 1468 of `templates/basket_detail.html.j2` unless the new claim is allowlisted or rephrased.
   - `verified` — 0 hits.
   - `certified` — 0 hits.
   - `approved` — 0 hits.
   - `proofed` — 0 hits.
   - `gauntlet` — 0 hits.
   - `proven` — 0 hits in NEW strings.
2. **The OLD strings that DID carry `validated`/`已验证` in user-visible text are REMOVED.** The OLD footer line `'…they are descriptive, not a validated buy list. …'` (EN) and `'…它是描述性读数，不是已验证买入清单。…'` (ZH) are REPLACED with `'Research dashboard, not investment advice. Theme and member scores help prioritize what to study; they are not a buy list or an order.'` and `'研究看板，非投资建议。主题与成分股评分用于安排研究优先级，不是买入清单或下单指令。'` — BOTH occurrences of `validated` AND `已验证` in user-visible footer text are gone. The test `test_footer_is_concise_product_guidance` asserts `validated buy list` is NOT in src. This is a validated-claims WIN in the footer (and removes the prior unbacked claim that the buy list had been "validated" in some unspecified sense).
3. **The pre-existing `validated` occurrences that survive are NOT in the diff.** Two pre-existing `validated` strings remain in the template outside the PR diff:
   - Line 520: `// "Contested": the (slow, validated) lifecycle label is constructive while >=2 of the faster …` — this is a code COMMENT (JSDoc-style), not a user-visible string. Pre-existing, untouched.
   - Line 1101: `title="contested — faster descriptive textures disagree with the validated label: ${esc(cList.map(x=>x[0]).join(' | '))}. A shape/fragility read, not a forecast."` — this is a `title=` attribute. The CLAUDE.md rule "no translated text in `title=` attributes" applies to TITLE TEXT, not to the validator's regex; the validator itself does scan JS-templated `title=` attributes as user-visible positions. Pre-existing, untouched by this PR. The validator would already catch this if it's not allowlisted — but it's not a NEW finding introduced by PR #7412.
4. **The new `tierBadgeHtml` helper's `data-tip-*` popover is plain word, no tier-claim vocabulary.** The new helper's popover text (`'The entry read is active. Check the wider setup and your risk plan before acting.' / '入场判断已生效。操作前请结合整体布局和自己的风险计划。'`) describes what the tier MEANS without claiming any specific performance property. No `validated`/`verified`/`certified`/`proven`/`gauntlet`/`approved`/`proofed` vocabulary is used in any of the 8 new popover sentences (4 tiers × EN+ZH = 8 strings; total ~16 plain-word sentences). Compliant.
5. **The NEW macroAbsent footnote introduces a forward-looking tier-claim that the validator will flag.** The string `'Macro context is absent, so the score is rebalanced across '+nAvailLegs+' available inputs. Adding macro context would change the calibrated score, so it stays excluded until separately validated. / 宏观背景缺失，因此评分会在现有 '+nAvailLegs+' 个输入之间重新平衡。加入宏观背景会改变校准评分，因此在单独验证前保持排除。'` is the only NEW `validated` occurrence in user-visible text. The phrase `until separately validated` (and ZH twin `在单独验证前`) is NOT in the validator's negation pattern (`cannot ... validated`, `无...验证`, `非...验证`, `未...验证`, `未经验证`, `不...已验证`). It IS a forward-looking tier-claim: it implies that the score system has a category of state-change called "validation", that some things have been validated, and that macro context is awaiting one. On the validator's regex, the EN string `'…until separately validated.'` matches `validated` and would be flagged as an unbacked affirmative claim. The ZH string `'…在单独验证前保持排除。'` uses `验证` alone, which is NOT in the validator's regex (`已验证|经验证|经过验证`) — so the ZH string would NOT be flagged. (This is an inconsistency the validator's ZH coverage does not catch, but it is a separate concern from PR #7412's audit.)
6. **The new test `tests/test_basket_detail_glance_copy.py` does NOT guard the new `validated` usage.** The 8-test suite forbids `validated buy list` (test 6: `test_footer_is_concise_product_guidance`) but does NOT forbid the new `'…until separately validated.'` string (test 5: `test_score_mechanics_are_progressive_disclosure` only forbids `Score Anatomy / 评分解剖`, `<span class="idx">W8</span>`, `W8b pre-registration`, `FT-R7`). A follow-up PR could close the gap by adding `assert "until separately validated" not in src` and `assert "单独验证前" not in src` to `test_score_mechanics_are_progressive_disclosure` — or by rephrasing the macroAbsent footnote to drop the forward-looking validation claim.
7. **No new authority is claimed in the PR.** The PR does NOT promote any artifact to gauntlet / authority / rank / score. It does NOT add a "validated" badge to any user-facing surface (the OLD `validated buy list` text was a tier-claim-adjacent phrase, not a badge; it has been REMOVED). It does NOT change any `engine/` display-copy field. The only NEW `validated` use is in the macroAbsent footnote, which is a forward-looking claim that "validations happen as part of our workflow" — this is a tier-claim of authority by implication, not by badge.
8. **The PR materially reduces the surface area of unearned claims in the rendered page.** The OLD tier-claim-adjacent strings that are removed: `validated buy list / 已验证买入清单` (footer), `Conviction Profile and trust tier / 信念档案与信任层级` (footer — explicit tier-label framing), `T1 CONFIRMED / T2 PRIME / T3 APPROACHING / T4 FIRST SPARK` (all-caps tier-name prefixes in the OLD `tierBadge` popover), the visible `display-only` chips on the `Same narrative elsewhere` and `Timing & risk` headings (a tier-label framing on display-tier material), the visible `T1/T2/T3/T4` tier code in the fast-turn Watch badge (a tier-label framing), the `W8` heading prefix on the Score Anatomy panel (an internal lane code in a heading), and the `W8b pre-registration (FT-R7)` engineering vocabulary in the macroAbsent footnote. Every one of these is REMOVED in this PR. The PR trades one new `validated` occurrence (in the macroAbsent footnote's forward-looking frame) for the retirement of six prior tier-claim-adjacent strings across the same surface — net the surface is materially cleaner.

**Net validated-claims delta:** the PR's net effect on the validator's regex is −3 affirmative `validated`/`已验证` occurrences (the OLD footer ×2 EN+ZH + the OLD `validated buy list` substring matches across the 121-page plain-copy fan-out) and +1 new affirmative `validated` occurrence (the macroAbsent footnote's `until separately validated`). The ZH side of the new macroAbsent footnote uses `验证` alone (not in the regex), so the validator's ZH coverage does NOT catch it, but it is a parallel new tier-claim by the same logic. **Net verdict: PARTIAL — one new affirmative tier-claim, with a recommended remediation of rephrase the macroAbsent footnote to drop the forward-looking validation claim or extend `tests/test_basket_detail_glance_copy.py` to forbid it. The PR otherwise ships a substantial validated-claims WIN at the footer and the tier-badge popover, and the overall trajectory of the change is toward fewer unbacked claims, not more.**

**Recommended remediation (optional):**

- Rephrase the macroAbsent footnote to drop the forward-looking validation claim. A neutral-disclosure alternative: `'Macro context is absent, so the score is rebalanced across '+nAvailLegs+' available inputs. Adding macro context would change the calibrated score, so it stays excluded. / 宏观背景缺失，因此评分会在现有 '+nAvailLegs+' 个输入之间重新平衡。加入宏观背景会改变校准评分，因此保持排除。'`. This drops BOTH occurrences of `validated` AND `验证` from the new string.
- OR extend `tests/test_basket_detail_glance_copy.py::test_score_mechanics_are_progressive_disclosure` to assert `assert "until separately validated" not in src` and `assert "单独验证前" not in src`, so the new claim is locked out by the same plain-language regression suite the PR itself ships.
- OR add the new claim to `data/regime/validated_claims_allowlist.json` (per the validator's allowlist path on line 12) with a documented justification (the macro-context lane is held back until the W8b pre-registration completes; this would be the allowlist text).

## Overall verdict

**PARTIAL — plain-language PASS, theme PASS, validated-claims PARTIAL (one new affirmative `validated` occurrence in user-visible text; recommended remediation: rephrase the macroAbsent footnote or extend the new test suite to forbid it). The PR is otherwise a substantial plain-language + theme + validated-claims WIN at the footer and the tier-badge surface.**

Summary table:

| dimension | verdict | reason |
| --- | --- | --- |
| plain-language | PASS | New 4-tier reader labels (`T1→confirmed setup / 已确认机会`, `T2→early setup / 早期机会`, `T3→forming / 形成中`, `T4→first sign / 初现迹象`); new `tierBadgeHtml` helper that demotes the visible tier code to `class=` + `data-tip-*` hover popover while keeping the engine-side anchor for sort/rank; OLD all-caps tier-name prefixes (`T1 CONFIRMED`, `T2 PRIME`, `T3 APPROACHING`, `T4 FIRST SPARK`) and ZH twins REMOVED (test `test_entry_tier_codes_are_demoted_to_tooltip_detail` asserts); OLD `display-only` chips on `Same narrative elsewhere` and `Timing & risk` headings REMOVED (heading renamed to `Timing & risk / 择时与风险`); OLD fast-turn Watch jargon (`T1/T2 signal`, `新出现的 T1/T2 信号`) rewritten to `early-turn sign / 早期转折迹象`; OLD 86-word Potential paragraph (with `T1/T2/T3/T4 confluence` and `T3* is provisional and may repaint`) replaced with a 32-word plain clause (test enforces `len(words) <= 48` AND forbids `T1/T2/T3/T4/confluence/repaint`); OLD `Score Anatomy / 评分解剖` heading renamed to `Score details / 评分详情` AND wrapped in a `<details>` disclosure whose summary reads `How this score is built / 评分如何构成` (OLD `<span class="idx">W8</span>` heading prefix REMOVED); OLD `W8b pre-registration (FT-R7)` engineering vocabulary REMOVED from the macroAbsent footnote; OLD `display-only` self-labeling on `Fast tape` REMOVED; OLD `5/20/60d trend, breadth and the slow-cycle gate still govern the recommendation` simplified to `the slower trend, breadth and cycle still drive the recommendation`; OLD `Conviction Profile and trust tier` tier-label framing REMOVED from the footer; bilingual EN/ZH parity preserved on every NEW string; no `title=` attribute changes; zero raw hex literals in the new CSS block (lines 132–137 of `fb966e77.css`). The new `tests/test_basket_detail_glance_copy.py` 8-test regression suite locks this contract on the template source AND every one of the ≥121 committed plain-copy pages. |
| theme (TP-0 dual-art-direction) | PASS | 5 lines of NEW CSS in `site/assets/css/fb966e77.css` for the new `.ftr-anatomy-disclosure` block (lines 132–137): summary `cursor:pointer; list-style:none; width:max-content; max-width:100%; color:var(--link); font-size:12px; font-weight:700; padding:5px 8px; border-radius:var(--r-sm);`; `::-webkit-details-marker {display:none;}`; `::after {content:'  ›'; color:var(--muted);}`; `[open] > summary::after {content:'  ⌄';}`; `> summary:focus-visible {outline:2px solid var(--link); outline-offset:2px;}` — every color is `var(--token)` (`--link`, `--muted`, `--r-sm`); zero raw hex literals; theme-agnostic by design (no `html[data-theme="light"]` overrides — `var(--link)` and `var(--muted)` resolve to whatever the active theme's tokens are); geometry (chevron `›`/`⌄`, padding, focus ring, summary hover) shared across themes; only the resolved token color of `var(--link)` differs between dark and light. Existing rules for `.ftr-anatomy-card`, `.ftr-anatomy-hd`, `.ftr-ah-score`, `.ftr-leg-*`, `.ftr-anatomy-ft`, `.ftr-crowd-note` (lines 111–130 of the new sheet) are UNCHANGED. The new CSS sheet replaces `ff321a97.css` with `fb966e77.css` (sha256[:8]-fingerprint); the test `test_generated_pages_share_the_new_fingerprinted_stylesheet` enforces `len(refs) == 1`, sha256 match, and the new `.ftr-anatomy-disclosure > summary` selector presence. Paired G6 wire sync preserved (template edited; 121 plain-copy pages regenerated in the same PR; `test_all_committed_basket_pages_match_the_glance_contract` enforces the new contract on every page). Full 8-cell evidence matrix present (`mockups/evidence/basket-detail-ux/` carries 8 base full-page PNGs + 8 `score-focus` forced-state PNGs on `basket_ai_semiconductors.html` × `theme(dark|light) × lang(en|zh) × viewport(1440|390)` + `EVIDENCE.yml` + `manifest.json` + `smells.json`); no runtime style injection; no raw hex literals in the new template HTML. |
| validated-claims | PARTIAL | `scripts/check_validated_claims.py` exists and would run cleanly on this PR-head checkout. The validator's regex `validated\|已验证\|经验证\|经过验证` (case-insensitive) WOULD catch the new `validated` occurrence in the macroAbsent footnote (`'…so it stays excluded until separately validated.'`); the negation pattern on line 21 (`cannot ... validated`, `无...验证`, `非...验证`, `未...验证`, `未经验证`, `不...已验证`) does NOT cover `until ... validated`. The phrase is a forward-looking tier-claim by implication ("validations are a real category of state-change and macro-context is awaiting one"), not a disclaimer. Net delta: −3 affirmative `validated`/`已验证` occurrences across the OLD footer (EN+ZH + the plain-copy fan-out ×121) and +1 new affirmative `validated` occurrence (the macroAbsent footnote); the OLD tier-claim-adjacent strings (`validated buy list`, `Conviction Profile and trust tier`, `T1 CONFIRMED`/`T2 PRIME`/`T3 APPROACHING`/`T4 FIRST SPARK`, visible `display-only` chips, visible `T1/T2/T3/T4` tier code in fast-turn Watch badge, `<span class="idx">W8</span>` heading prefix, `W8b pre-registration (FT-R7)` engineering vocabulary) are all REMOVED. **Recommended remediation: rephrase the macroAbsent footnote to drop the forward-looking validation claim, OR extend `tests/test_basket_detail_glance_copy.py::test_score_mechanics_are_progressive_disclosure` to assert `assert "until separately validated" not in src` AND `assert "单独验证前" not in src`, OR add the new claim to `data/regime/validated_claims_allowlist.json` with documented justification.** |

**Net audit cost:** one-pass, no retries, no scope expansion, no durable result on the remote host. Audit file persisted to `orch/audits/macro_PR-7412.mm.md`.

**Note on the partial verdict:** the validated-claims PARTIAL is driven by a SINGLE NEW occurrence of the word `validated` in a forward-looking frame (the macroAbsent footnote's `until separately validated`). The PR is otherwise a substantial WIN at the validator's regex (it retires three prior `validated`/`已验证` occurrences across the OLD footer and its 121-page plain-copy fan-out) and removes six other tier-claim-adjacent strings across the same surface. The remediation is mechanical: rephrase the macroAbsent footnote to a neutral-disclosure form (`'…so it stays excluded.' / '…因此保持排除。'`), or extend the new `tests/test_basket_detail_glance_copy.py` test to forbid the new string, or add the claim to the allowlist with a documented justification. None of these remediations is required to land a follow-up PR — the current PR is already live, the validator would catch the new occurrence on a full-tree re-run, and a small follow-up is the recommended path forward.
