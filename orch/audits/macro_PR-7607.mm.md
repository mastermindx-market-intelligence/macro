# PR audit — mastermindx-market-intelligence/macro#7607

**Auditor:** meta-ceo-b-2026-09-08 (one-pass remote useful-idle, no retries, no scope expansion)
**Audit timestamp:** 2026-09-21
**Repo / PR:** mastermindx-market-intelligence/macro #7607
**PR title:** Publish China selective synthesis and unify reason Lens
**Merged at:** 2026-09-21T11:35:45Z (most recent merged PR in macro within the 24 h audit window that carries a real user-facing template + theme-lens surface — see §1 scope note)
**Head sha:** `claude/publish-china-synthesis-20260921` (DRAFT armed with `merge-on-green`; PR body declares the rebake capture sha `0ed6cbafc813b2e569d2048d2a09884aef0896f379175fe4985d73457828a8c0` for the published `site/china.html` content)
**Merge commit:** `8eaf4eda03c92fceba8760694c0c34e013e75055` (per `gh pr view --json mergeCommit`)
**Author:** chriswong6031-creator (seat-authored; PR carries an in-PR "What changed ↓" disclosure of the design law controlling the selective synthesis, plus 8/8 PASS browser matrix in the body)
**Repo root verified:** `/Users/chriswong/lanes/repos/macro` on a `claude/...` worktree (clean), `origin/main` reachable; merge confirmed in `origin/main` log.

> **Scope note (§1):** the chronologically most-recent merges inside the 24 h window are #7608 (research/risk, 11:23:11Z), #7607 (China synthesis + Lens, 11:35:45Z), #7606 / #7602 / #7598 (all `orch(audit)` ledger entries — zero audit-engaging surface), and #7603 / #7599 / #7585 / #7602 (already audited at `macro_PR-7603.mm.md`, `macro_PR-7599.mm.md`, `macro_PR-7585.mm.md`, `macro_PR-7602.mm.md`). Among the **un-audited** merges with a real user-facing template + theme/lens surface, **#7607** is the only one: (a) it touches the canonical China template (`templates/china.html.j2`, +4/−20), the rendered copy (`site/china.html`, +2562/−282), and a China-specific test (`tests/test_china_archetype_d_s1.py`, +9/−7); (b) it carries the full plain-language surface for the China synthesis (≈70 new bilingual EN/ZH strings on the synthesis + the consolidated reason Lens); (c) it carries a real theme-relevant change — the consolidation of the China-local `cnx-lens` tooltip plane onto the shared `data-tip-*` Lens owner in `templates/theme.js` (the very interaction defect the mobile browser run caught); (d) the other un-audited candidates (#7608 caution-persistence research, #7589 basket-detail focus fix, #7587 orch(audit) ledger, #7586 risk research, #7577 sector-subtheme plan, #7576 commodities wire, #7573 prophet-live receipts, #7571 design ratchet, #7570 / #7568 / #7566 / #7565 / #7564 / #7561 / #7560 / #7558 / #7556 / #7555) carry zero template/engine surface that the audit mandate requires. The seat selects #7607.

---

## 1. PR metadata

| field | value |
|---|---|
| repo | mastermindx-market-intelligence/macro |
| number | 7607 |
| title | Publish China selective synthesis and unify reason Lens |
| merged_at | 2026-09-21T11:35:45Z |
| head (body-disclosed) | `claude/publish-china-synthesis-20260921` (DRAFT, `merge-on-green` armed) |
| merge_commit | `8eaf4eda03c92fceba8760694c0c34e013e75055` |
| base | `main` (rebased forward to `c48e15193a29fd5eff94e963d0d0c4d6f3104867` per the PR body; protected main has since advanced with **zero movement** on owned China template/test/build/news/site paths or shared `theme.js`/theme CSS at the latest comparison) |
| branch | `claude/publish-china-synthesis-20260921` |
| changed files | 3 (`gh pr view 7607 --json files`) — `site/china.html` (+2562/−282), `templates/china.html.j2` (+4/−20), `tests/test_china_archetype_d_s1.py` (+9/−7) |
| additions / deletions | 2,575 / 309 (per `gh pr view --json additions,deletions`); ≈2,560 of those are the rebaked `site/china.html` carry-through of the already-accepted #7485 synthesis + the consolidated reason-Lens HTML; the rest are the template patch (4 lines) + the test rename/strengthening (16 lines) |
| files of interest | `templates/china.html.j2` (+4/−20 — removes 3 lines of `.cnx-lens::after`/`.cnx-tip-open` tooltip CSS and the `cnxToggleLens`/`cnxCloseLenses` handlers, removes `onclick="cnxToggleLens(this,event)"` from the lens button and `window.cnxToggleLens`/`cnxCloseLenses` exposure), `site/china.html` (+2562/−282 — rebake), `tests/test_china_archetype_d_s1.py` (+9/−7 — `test_synthesized_reason_receipts_are_keyboard_and_tap_reachable` renamed to `test_synthesized_reason_receipts_use_the_shared_lens_plane`, asserts `data-tip-en`/`data-tip-zh` attributes present + `cnxToggleLens`/`cnxCloseLenses`/`cnx-tip-open` absent + `document.querySelector('.lens-pop.open')` in the Escape handler) |
| labels | `merge-on-green` (sweeper armed; cleared by squash-merge) |
| scope collision | none — body declares zero collision with `engine/*`, the China-deep ROW 4 cards/dialogs, the China playbook selective synthesis (already accepted at #7485), the shared `data-tip-*` Lens owner in `templates/theme.js`, or the global outside-click/Escape handlers |

**Nature of change (China-page half-B payload — selective synthesis publication + lens-system consolidation):** the PR has two intertwined deliverables, both of which the body declares together:

1. **Publish the already-accepted selective synthesis from #7485 onto the committed China page.** The selective synthesis (the `cnx-playbook-context` layer, the Plain/What Changed/action/freshness reading, the Macro News identity with `What changed ↓`, the deep ROW 4 three-column racks) is the canonical evolution path per **#7534 is controlling: selective synthesis is the default evolution path** (body-cited). The retired `L1-4 · Why — four drivers` production shell from the rejected six-block Archetype-D plan is **absent** — the PR does not revive it. The published page is content-addressed: SHA-256 `0ed6cbafc813b2e569d2048d2a09884aef0896f379175fe4985d73457828a8c0`, 440,039 bytes.

2. **Unify the China reason Lens onto the shared `data-tip-*` Lens owner.** The China template had grown a duplicate `cnxToggleLens(this,event)` / `cnxCloseLenses()` tooltip plane (`templates/china.html.j2` lines 1057–1060 in the diff) that competed with the site-wide Lens system in `templates/theme.js` (which already handles hover, focus, touch sheets, outside-close, and Escape). On mobile the two capture handlers fought; the real-browser matrix (8/8 PASS across 1440/390 × dark/light × EN/ZH) caught the old mobile failure before this fix. The PR routes every `cnx-lens` button (which carries `data-tip-en`/`data-tip-zh` attributes on the visible button) through the shared `data-tip-*` owner, deletes the China-local handler, and routes the Escape key through `.lens-pop.open` (the shared Lens's class) instead of `.cnx-lens.cnx-tip-open`. China-local dialog/card keyboard behavior (`cnxOpenDlg`, `cnxCloseDlg`, `cnxTogglePop`, `cnxClosePops`, `cnxToggleHm`) is preserved.

**Body signals — already-run gates (PR-quoted, NOT re-run in this audit per one-pass constraint):**
- `python3 -m pytest -q tests/test_china_archetype_d_s1.py -k china` → **24 passed** (body-quoted).
- `git diff --check`: PASS.
- Real generated-page browser matrix: **8/8 PASS** across 1440/390 × dark/light × EN/ZH; all eight cells: HTTP 200, zero page errors, zero failed requests, zero horizontal overflow; all eight: 12 cards, 13 dialogs, 3 deep three-column racks, ROW 4 present, synthesis present, retired Archetype-D shell absent; reason receipt opens/closes through the shared Lens on desktop **and mobile**; Enter activates the existing What To Do dialog in all cells.
- Rebake content-hash bound: `site/china.html` SHA-256 `0ed6cbaf…d73457828a8c0`, 440,039 bytes.

**Scope boundary (body-disclosed):** the PR is bounded to the China page + the China reason Lens + the China-specific test. Does **not** touch the shared `theme.js` data-tip owner (the China page now *uses* it but does not modify it), the Neural Web grade spine, the daily `RECURRING_BRIEFS_ENABLE` policy, the China-data ingestion, or any other page's lens handler.

**Cross-reference (body-disclosed, not re-run):** #7534 ("selective synthesis is the default evolution path") is the design law controlling this PR; #7485 carried the selective synthesis that this PR ships; the body explicitly forbids re-opening the retired Archetype-D shell from a wholesale China design wipe (which would require substantial end-to-end superiority, not mere cleaner/newer/fewer).

---

## 2. Plain-language findings

**Source for plain-language law:** the macro repo does not ship a standalone `check_plain_language.mjs` (that check is terminal-repo-only); the macro equivalent is the **visible-string audit** mandated by `CLAUDE.md` §"Design (user-first law)" — `docs/DESIGN_DOCTRINE.md` + the `frontend-design` skill — read against the Tier-1 obligations (state + plain-word stance under hard word budgets, no internal state names, no untranslated stats, no raw slugs, bilingual EN/ZH parity on every visible string, plain-word null disclosure) and the Tier-2 obligations (Tier-2 receipts for nulls, no falsifier vocabulary front-facing, plain-word cadence call-outs).

**Scope check — does this PR carry plain-language surface?** Yes — the rebaked `site/china.html` adds ≈70 new bilingual EN/ZH strings (the selective synthesis + the consolidated reason-Lens receipts); the template patch is a 4-line add that removes the duplicate `cnx-lens::after` tooltip CSS and replaces `onclick="cnxToggleLens(this,event)"` with the bare `data-tip-en`/`data-tip-zh` attributes that the shared Lens reads.

**Plain-language walkthrough (selected visible strings from the diff):**

| source | string (EN/ZH side-by-side) | audience | plain-language read |
|---|---|---|---|
| page H1 (carried over) | `'China — selective synthesis' / '中国 — 精选综合'` | reader | bilingual plain heading; "selective synthesis" / "精选综合" is the canonical label for the already-accepted reading surface. ✓ |
| playbook context | `'Playbook posture: Neutral.' / '策略姿态：中性。'` | reader | plain one-clause stance + state word; no acronym; the colon matches the Chinese full-width convention. ✓ |
| pullback-risk label | `'Pullback risk' / '回撤风险'` | reader | plain two-word heading; established glossary term. ✓ |
| pullback-risk subhead | `'~35% chance of a ≥5% pullback within a month · 1.1× the usual odds' / '未来一个月内出现≥5%回撤的概率约35% · 约为平时的1.1倍'` | reader | plain bilingual one-sentence statement with an explicit time horizon ("within a month" / "未来一个月内"), a percentile (35%), and a multiplier (1.1×). ZH uses the standard `回撤` (pullback) idiom and `约为平时` (about the usual). ✓ |
| pullback-risk bigsub | `'Breadth breakdown (all-boats)' / '广度普跌（普跌）'` | reader | plain bilingual label; "all-boats" is the operator's gloss on "broad market decline" and ZH mirrors with the parenthetical `普跌`. ✓ |
| pullback-risk odds row | `'≥5% dip odds' / '≥5%回撤概率'` + value `35%` | reader | plain heading + plain odds copy; the `≥5%` unit is preserved in both languages. ✓ |
| pullback-risk foot | `'Trim chasing; favour good entries over extended leaders.' / '减少追高；择优入场而非追逐已延展的龙头。'` | reader | plain two-clause action copy in plain words; no jargon; ZH mirrors with `减少追高；择优入场而非追逐已延展的龙头` — the standard operator-facing idiom. ✓ |
| pullback-risk alert pill | `'CAUTION' / '谨慎'` (and `'警戒'` in the rationale-receipts block) | reader | plain state badge in capital-case Latin + standard Mandarin state word; both are state-semantic tokens (the standing convention for state badges in both themes). ✓ |
| macro-news heading | `'Macro News' / '宏观新闻'` | reader | plain two-word heading. ✓ |
| macro-news "what changed" | `'What changed ↓' / '最近变化 ↓'` | reader | plain bilingual link label; the `↓` glyph is the standard open-dialog affordance. ✓ |
| macro-news empty state | `'No material macro changes surfaced this session.' / '本会话暂无重大宏观变化。'` | reader | plain Tier-2 null disclosure — the honest "nulls printed" form. The body explicitly forbids falsifier vocabulary front-facing (operator 2026-07-27, #3821), and "no material changes" is exactly the calm, plain-word acknowledgement form that satisfies Tier-2 without "falsifier fired / thesis refuted". ✓ |
| macro-news empty-state "why" | `'News and alert sources are quiet or have not refreshed yet.' / '新闻与警报源当前安静，或尚未刷新。'` | reader | plain Tier-2 reason chip — names the source class (news + alert sources) and the cause ("quiet or have not refreshed yet") in plain words. ✓ |
| model-headline row | `'Model headline' / '模型原始标题'` + `'Risk-off — stress is elevated; defend capital first.' / '避险 — 压力升高；优先防守。'` | reader | plain heading + plain stance copy; "Risk-off" is the operator's standing glossary label and is glossed inline ("stress is elevated; defend capital first"); ZH uses `避险` (the standard Mandarin risk-off term). ✓ |
| pullback-risk intensity | `'Intensity' / '强度'` + `81/100` | reader | plain one-word heading + a bounded integer on a 0–100 scale; the unit is preserved. ✓ |
| pullback-risk sizing | `'×0.90 of normal' / '常规的 0.90 倍'` + `'suggested size' / '建议仓位'` | reader | plain sizing copy with the explicit `×0.90 of normal` multiplier; ZH mirrors with `常规的 0.90 倍` (the standard Mandarin multiplier idiom). ✓ |
| pullback-risk horizon | `'within 21 days' / '21 日内'` | reader | plain bilingual time-horizon copy; the `21 日内` is the standard Mandarin horizon idiom (not "21 days later" / "21 天后" which would be wrong tense). ✓ |
| pullback-risk vs-normal | `'vs normal' / '对比常态'` + `'30%'` baseline | reader | plain bilingual comparison copy. ✓ |
| pullback-risk narrative | `'Pullback odds 50% → 35% (peaked 26 days ago).' / '回撤概率 50% → 35%（26 日前见顶）。'` | reader | plain bilingual narrative; the `→` is the standard transition glyph and ZH uses full-width `（）` parentheses. ✓ |
| pullback-risk "do" | `'Do' / '应对'` + `'Trim chasing; favour good entries over extended leaders.' / '减少追高；择优入场而非追逐已延展的龙头。'` | reader | plain action block; "Do" / "应对" is the standard action-row label. ✓ |
| pullback-risk track-record | `'🧾 Track record' / '🧾 往绩'` | reader | plain bilingual row label with the standing receipt-glyph `🧾`; ZH uses `往绩` (the standard Mandarin "track record"). ✓ |
| track-record null | `'Past-year hit rate: too few graded alerts yet (daily readings — consecutive days overlap) | Watch/caution: too few graded | Recovery: too few graded' / '过去一年命中率：暂无足够评分记录（按日读数——连续日期相互重叠） | 观察/谨慎：暂无足够评分 | 修复信号：暂无足够评分'` | reader | plain Tier-2 null disclosure: every cell is "too few graded" (the operator's standard null-disclosure idiom, not "unvalidated" or "refuted" or "falsifier fired"); ZH mirrors each clause; the em-dash and pipe separators are consistent with the standing macro copy conventions. ✓ |
| track-record cadence | `'Grading calls as they mature' / '评分随信号成熟展开'` + `'about 21 trading days' / '约21个交易日'` + `'no new entries lately' / '近期无新增记录'` | reader | plain bilingual cadence call-out: names the cadence ("as they mature" / "随信号成熟展开"), the duration ("about 21 trading days" / "约21个交易日"), and the current state ("no new entries lately" / "近期无新增记录"). ✓ |
| capital-flow row | `'Capital outflow / FX' / '资本外流／汇率'` + value `22` | reader | plain bilingual row label; the full-width slash `／` matches the ZH half-width rule. ✓ |
| reason-receipt aria-label | `'Why this read / 为什么'` | screen reader | plain bilingual aria-label; this is the SAME pattern the macro dashboard hero uses for locked-driver receipts (audit #7585 MINOR-1), and the seat records the cross-PR consistency as a +. ✓ |
| reason-receipt tip #1 (Growth-scare) | `'This is historical regime context from the page's stored record. It does not by itself confirm an entry and this display does not claim a fresh recalibration.' / '这是本页已存记录中的历史周期背景。它本身不确认入场，本展示也不声称已进行新的重新校准。'` | reader | plain three-clause Tier-1 disclosure: (a) names the data class ("historical regime context from the page's stored record"), (b) explicitly **disclaims** authority ("It does not by itself confirm an entry"), (c) explicitly **disclaims** freshness ("this display does not claim a fresh recalibration"). This is the OPPOSITE of a "validated" claim — the receipt actively declines to claim recalibration, in plain words. ✓ |
| reason-receipt tip #2 (Monetary tightening) | `'3 inputs — all point the same way. Counted once, as a single monetary-conditions read.' / '3项输入——方向一致，合并为一次货币条件读数。'` | reader | plain bilingual Tier-1 disclosure: names the input count ("3 inputs" / "3项输入"), the directional agreement ("all point the same way" / "方向一致"), and the aggregation ("Counted once, as a single monetary-conditions read" / "合并为一次货币条件读数"). No acronym; the em-dash is the standard. ✓ |
| theme.js shared Lens (carried over, not in diff) | (the global `data-tip-*` owner already handles hover, focus, touch, outside-close, Escape) | screen reader + reader | the consolidation moves China into the **same** Lens system the rest of the site uses, so the screen-reader semantics, the outside-click dismissal, and the Escape behavior are identical across pages. The PR does **not** introduce a third lens plane. ✓ |
| China-local dialog/card (carried over) | (`cnxOpenDlg`, `cnxCloseDlg`, `cnxTogglePop`, `cnxClosePops`, `cnxToggleHm`) | reader + keyboard | unchanged by the diff; the outside-click handler still calls `cnxClosePops()` (China-local popovers), the Escape handler now exits early if `.lens-pop.open` is present (the shared Lens's class). No Chinese-only or English-only copy leaked. ✓ |

**Bilingual parity check:** every new visible string in the diff carries a `<span class="l-en">` / `<span class="l-zh">` pair. The aria-label `'Why this read / 为什么'` is bilingual in a single attribute value, mirroring the hero's locked-driver pattern. The Track-record null and the cadence row both have ZH mirrors with the standing Mandarin idioms (暂无足够评分 / 随信号成熟展开 / 约21个交易日 / 近期无新增记录).

**Plain-language obligation check:**
- **No internal state names reach the user.** Searched the diff for `payload.`, `ms_history`, `event_strip`, `state_reason`, `radar.cycle`, `cycle.sector_bias`, `vm['breadth_split']`, `_zh` suffix in user copy, raw slugs (`cpi`, `pmi`, `nfp`, `fomc`, `ms-red`, etc.) — zero leaks. ✓
- **No untranslated stats.** Every integer (`81`, `35%`, `22`, `50% → 35%`, `0.90`, `30%`, `26 days`, `21 trading days`, `3 inputs`) carries its ZH mirror's unit (百分号 `%`, 日, 个交易日, 项输入). The arrow `→` is preserved on both sides. ✓
- **No falsifier vocabulary.** Searched the diff for `falsifier`, `refuted`, `invalidated`, `thesis-refuted`, `证伪`, `证伪已触发`, `已失效`, `已驳回` — zero matches. The reason-receipts explicitly **disclaim** authority ("It does not by itself confirm an entry and this display does not claim a fresh recalibration") in plain words, which is exactly the operator's 2026-07-27 #3821 rule: "tripwires keep evaluating in the background; user cycle surfaces show projection windows… quiet 'read being updated' chips… and 'what we're watching' conditions — never 'falsifier fired / thesis refuted / 证伪'". ✓
- **No raw slugs in copy.** Searched the diff for `| capitalize`, `{{ var }}` in visible copy, `{{ face.en }}` outside its `<span class="l-en">` wrapper, etc. — zero leaks. ✓
- **Plain-word null disclosure.** The Track-record block ("too few graded alerts yet" / "暂无足够评分记录") and the Macro News empty-state ("No material macro changes surfaced this session" / "本会话暂无重大宏观变化") are the Tier-2 plain-word form. ✓
- **No `title=` translations.** Searched the diff for `title="…"` with translated text — zero matches. The shared Lens uses `aria-label` + `data-tip-*` for screen readers + hover tooltips, not `title=`. ✓

**Plain-language compliance verdict:** **PASS.** Every new visible string is plain English and idiomatic Mandarin; the bilingual parity holds across all 70+ additions; the reason receipts explicitly **disclaim** authority in plain words (the opposite of a "validated" claim); the Track-record and Macro News empty-state are the Tier-2 null-disclosure form; the `Why this read / 为什么` aria-label matches the hero's locked-driver pattern (cross-PR consistency +); no falsifier vocabulary reaches the user; no internal state names, untranslated stats, or raw slugs leak.

---

## 3. Theme findings

**Source for theme law:** `CLAUDE.md` §"Theme art direction — required (dark and light are TWO art directions, not one skin; TP-0 2026-08-27)" + the project-local gates `scripts/check_design_system.py --mode enforce-added` and `scripts/check_runtime_style_injection.py`. Theme law covers token-only swaps fail light, runtime stylesheet injection prohibition, reduced-motion discipline, light-mode material treatment, and per-packet evidence matrix.

**Scope check — does this PR carry theme-relevant changes?** Yes — the PR's primary user-visible change is a **theme-system consolidation**: it removes the China-local `cnx-lens` tooltip plane (3 lines of CSS: `.cnx-lens::after` / `html[data-lang="zh"] .cnx-lens::after` / `.cnx-lens.cnx-tip-open::after` + 9 lines of JS: `cnxCloseLenses` + `cnxToggleLens` + the `onclick` + the `window.*` exposure + the Escape branch + the outside-click branch) and routes every `cnx-lens` button (which carries `data-tip-en`/`data-tip-zh` attributes on the visible button) through the shared `data-tip-*` Lens owner in `templates/theme.js`. This is exactly the kind of TP-0 consolidation the law exists to enforce — one Lens system, not N competing copies per page.

**Theme compliance walkthrough:**

1. **TP-0 dual art direction.** The PR does not introduce a new theme-specific selector block (no `[data-theme="dark"]`, no `.theme-light`, no `@media (prefers-color-scheme: dark)`). Every material-color decision in the rebaked `site/china.html` inline CSS delegates to a token (`var(--bg)`, `var(--text)`, `var(--muted)`, `var(--panel)`, `var(--panel2)`, `var(--line)`, `var(--popover-shadow)`, `var(--card-shadow)`, `var(--ink-link, var(--link))`, `var(--ink-up, var(--up))`, `var(--ink-warn, var(--warn))`, `var(--ink-info, var(--info, var(--link)))`, `var(--up)`, `var(--down)`, `var(--warn)`, `var(--info)`, `var(--orange)`, `var(--aib2-acc)`, `var(--aib2-mut)`, `var(--r-ctl)`, `var(--r-pill)`, etc.) or to a `color-mix(in srgb, var(--c) P%, transparent)` token-true expression. The only color literal in the diff is `box-shadow:0 4px 16px rgba(0,0,0,.18)` on the `.nb-cau-pop` popover — a single black drop-shadow with `0.18` opacity, the standard "soft drop shadow" convention; this is **not** a theme-art-direction color (it is a single shadow literal on a popover whose background uses `var(--panel)` and whose border uses `color-mix(in srgb,var(--warn) 40%,var(--line))`); the rest of the popover stack is fully token-true. The other non-token literal is `#fff` for `.tip .scale span` text on a filled scale bar — a single white text color on a flex-bar of `flex:1` cells where the background is set by a token-derived `--c` (`--up`/`--down`/`--warn`/`--orange`/`--info`/`--muted`) via `color-mix(in srgb, var(--c) 16%, var(--panel))`. This is a contrast guarantee on a token-tinted bar (white text on a saturated tint), not a parallel palette. ✓

2. **Evidence matrix (TP-0 evidence obligation).** The PR body declares an **8/8 PASS** real-browser matrix across 1440 × 390 × dark × light × EN × ZH for `site/china.html` (HTTP 200, zero page errors, zero failed requests, zero horizontal overflow, 12 cards + 13 dialogs + 3 deep three-column racks + ROW 4 + synthesis + zero retired Archetype-D shell). This is the standard TP-0 evidence obligation for a user-facing page change, with **all eight** cells PASS — the seat records the matrix as content-bound by the rebake sha `0ed6cbaf…d73457828a8c0` and the browser-run receipt in the body. ✓

3. **Runtime stylesheet injection.** The diff adds ZERO new `.js` files. The inline `<script>` that ships in `site/china.html` is the **existing** China-local script (carried over from the previous rebake), not a new injection. The PR's JS-side change is the **deletion** of `cnxToggleLens(this,event)` / `cnxCloseLenses()` and the `window.cnxToggleLens` / `window.cnxCloseLenses` exposure — i.e., the PR is a *removal* of competing runtime behavior, not an addition. The page does ship an inline `<script>` for the theme/locale bootstrap (carried over from the previous rebake), but the script only sets `data-theme` / `data-lang` attributes and reads `localStorage` — it does **not** call `style.textContent = …`, does **not** inject a `<style>` element, does **not** define a parallel palette/token family, and does **not** author visible material decisions. The PR's structural change is the **opposite** of a runtime-stylesheet-injection violation: it deletes the China-local competing tooltip plane and folds China into the shared `data-tip-*` owner. **Project-local gate (run on local checkout for awareness; CI re-runs on the head):** `python3 scripts/check_runtime_style_injection.py` → `runtime style injection guard OK (195 .js files scanned, 44 injecting, 89 total hits — all within frozen allowances)`. The PR adds zero `.js` files, so the count is unchanged by construction. ✓

4. **Reduced-motion discipline.** The diff adds `@media (prefers-reduced-motion: reduce) { .nb-cau-btn { animation:none; } }` on the China-local CAUTION-pulse button (which carries the `@keyframes nb-cau-pulse` 2.5s opacity pulse) — i.e., the new attention pulse is paired with the reduced-motion override. ✓ The page does **not** autoplay video, embed external fonts beyond the body font stack (carried over: `-apple-system, "Segoe UI", Roboto, Helvetica, sans-serif` on EN, `"PingFang SC", "Hiragino Sans GB", "Microsoft YaHei", "Noto Sans CJK SC"` on ZH), or fetch third-party assets at runtime. ✓

5. **Paired plain-copy asset parity.** The PR does not touch `templates/_public_nav.html.j2` / `site/glossary.html` (the anonymous-nav chrome) or `templates/_site_nav.html.j2` / paired nav HTML (the authenticated chrome). The China page is an authenticated-shell page (it uses `_site_nav.html.j2`), so the standing `scripts/check_template_site_sync.py` CI guard does not apply to this PR's specific files — the PR is scoped to a single page's rebake and its own template, and the standing chrome parity is unaffected. ✓

6. **Stylesheet `?v=` re-stamp behavior.** The diff removes `?v=62b49cc0` from `theme.css`, `?v=cbe6a0b4` from `illus.css`, and the asset-css re-stamps (carried over `?v=…` suffixes are stripped). This is **intentional** and matches the standing render-lane behavior: per `CLAUDE.md` §"Render lane + paired plain-copy", `render.yml` re-stamps the `?v=` suffixes after a merge via the Caddyfile's `immutable` list mechanism (`theme.css`, `live.js`, `theme.css`, `illus.css`, etc.). The PR explicitly notes the rebake is the new content; the re-stamp happens on the next render. The seat does not flag this as a finding — it is the documented lane behavior. ✓

**Theme-system consolidation sub-finding (positive):** the PR is **structurally** a TP-0 win — it removes a competing tooltip plane and folds China into the shared `data-tip-*` Lens owner. The mobile browser run explicitly caught the old failure mode (the global Lens capture handler and the China-local handler were competing on mobile); consolidating onto the shared owner fixes it at the source rather than adding a mobile-only workaround. This is exactly the consolidation TP-0 mandates, and the seat records it as a +. The Escape handler now exits early if `.lens-pop.open` is present (the shared Lens's class), and the outside-click handler closes only `cnxClosePops()` (China-local popovers), leaving the shared Lens to handle its own dismissal via `theme.js`. The China-local dialog/card keyboard behavior (`cnxOpenDlg`, `cnxCloseDlg`, `cnxTogglePop`, `cnxClosePops`, `cnxToggleHm`) is preserved verbatim. ✓

**Adjacent standing rule (GH annotation line-start, CLAUDE.md §House laws):** the PR does not introduce a new `::warning` / `::error` / `::notice` GitHub annotation. The script removals (`cnxToggleLens`, `cnxCloseLenses`, the `window.*` exposures) are deletion-only — no new annotation surface is introduced. ✓

**Theme compliance verdict:** **PASS.** The PR is structurally a TP-0 consolidation: it deletes the China-local `cnx-lens` tooltip plane (CSS + JS + `onclick` + `window.*` exposure + the Escape branch + the outside-click branch) and routes every `cnx-lens` button through the shared `data-tip-*` Lens owner in `templates/theme.js`. Every material decision in the rebaked `site/china.html` delegates to a token or a `color-mix(in srgb, var(--c) P%, …)` token-true expression; the only non-token literals are a single black drop-shadow (`rgba(0,0,0,.18)`) on a token-background popover and a single white text color (`#fff`) on a token-tinted flex bar — neither is a parallel palette. The 8/8 PASS browser matrix is content-bound by the rebake sha. The new `@keyframes nb-cau-pulse` attention pulse is paired with the `@media (prefers-reduced-motion: reduce)` override. The runtime-stylesheet-injection prohibition is honored by **construction** (the PR is a *removal* of competing runtime behavior, not an addition; zero new `.js` files).

---

## 4. Validated-claims findings

**Source for validated-claims law:** `scripts/check_validated_claims.py` (BC-2, PREREGISTRATION.md §4, D2 §4.3). The doctrine: every "validated" / "已验证" / "经验证" / "经过验证" claim in a user-facing surface must trace to (a) a justified entry in `data/regime/validated_claims_allowlist.json` whose `surfaces` list includes the claiming file, OR (b) an artifact JSON whose top-level `validated == true`. Negated/hedged uses are ignored. Third-party research quotations are skipped by `_THIRD_PARTY_PAGES`. Engine source copy is also scanned (claims gated at the source, not at nightly-render).

**Search for new "validated"-class claims introduced by this PR (full 3,380-line `+` side of the diff on the user-facing surface):**

- `templates/china.html.j2` diff (+4/−20): the 4-line add is the bare `data-tip-en` / `data-tip-zh` attributes on the lens button (no copy), and the 20-line removal is the local tooltip CSS + the local handler + the `window.*` exposure + the Escape branch + the outside-click branch (no copy). Scanned line-by-line for the token class `/validated|已验证|经验证|经过验证/`. **Zero matches.** ✓
- `site/china.html` diff (+2562/−282): the rebake is a content-addressed SHA-256 (`0ed6cbaf…d73457828a8c0`) carry-through of the already-accepted #7485 synthesis + the consolidated Lens HTML. Scanned the entire `+` side (the new rebaked HTML) for the token class — **zero matches.** ✓
- `tests/test_china_archetype_d_s1.py` diff (+9/−7): the test is renamed from `test_synthesized_reason_receipts_are_keyboard_and_tap_reachable` to `test_synthesized_reason_receipts_use_the_shared_lens_plane` and asserts `data-tip-en="{{ face.tip_en | e }}"` / `data-tip-zh="{{ face.tip_zh | e }}"` are present + `cnxToggleLens` / `cnxCloseLenses` / `cnx-tip-open` are absent + `document.querySelector('.lens-pop.open')` is present in the Escape handler. **Zero matches.** ✓

**Reason-receipt Tier-1 explicit disclaimers (positive sub-finding):** the two new reason-receipt tooltips in the diff **actively disclaim** authority in plain words, which is the **opposite** of a "validated" claim:

- "It does not by itself confirm an entry and this display does not claim a fresh recalibration." / "它本身不确认入场，本展示也不声称已进行新的重新校准。"
- "Counted once, as a single monetary-conditions read." / "合并为一次货币条件读数。" (this one is descriptive, not a claim-class token, but it pairs the input count with the aggregation step — i.e., it shows the reader exactly what the page is doing rather than asserting that the result is "validated".)

This is the **calibrated** Tier-1 form the operator's 2026-07-27 #3821 rule mandates: "tripwires keep evaluating in the background; user cycle surfaces show projection windows ('windows, not certainties — re-drawn nightly'), quiet 'read being updated' chips, and 'what we're watching' conditions — never 'falsifier fired / thesis refuted / 证伪'". The receipt text follows that pattern exactly. ✓

**Track-record Tier-2 null disclosure (positive sub-finding):** the Track-record block's null disclosure ("Past-year hit rate: too few graded alerts yet" / "过去一年命中率：暂无足够评分记录") is the Tier-2 plain-word null form — it discloses the null state in plain words without "unvalidated" / "not validated" / "无验证" / "未验证" framing. The standing BC-2 negation/hedge skip (`no validated …`, `not a validated …`, `unvalidated`, `not … validated`, `un-validated`, "hasn't/doesn't/won't/… validated", `cannot … validated`, `无...验证`, `非...验证`, `未...验证`, `未经验证`, `不...已验证`) covers "too few graded" cleanly — the phrase is not in the negation set but is also not in the affirmative "validated" set, so it is neither a claim nor a negation. ✓

**Project-local validated-claims gate (run on local checkout for awareness; CI re-runs on the head):**
- `python3 scripts/check_validated_claims.py 2>&1 | grep -E "china\.html"` → **no rows.** The pre-existing 38 UNEARNED findings the gate emits are all in `templates/_macro_suite_shell.html.j2`, `templates/canada.html.j2`, `templates/hk.html.j2`, the `macro_<topic>.html.j2` family, `mm_brain.js`, `macro_suite.js`, their rendered `site/` siblings, and `engine/market_os/macro_workspaces/consumer.py` — **none of which this PR touches.** ✓
- `python3 scripts/check_validated_claims.py 2>&1 | wc -l` → 38 (i.e., the count is unchanged from the pre-PR baseline — the PR adds zero new exposure). ✓

**Validated-claims compliance verdict:** **PASS.** Zero new "validated"-class claims in any of the 70+ new visible strings; zero claim-class tokens in the template patch, the rebaked `site/china.html`, or the renamed test; the two new reason-receipt tooltips **actively disclaim** authority ("does not confirm an entry", "does not claim a fresh recalibration") in plain words — exactly the Tier-1 calibrated form the operator's 2026-07-27 #3821 rule mandates; the Track-record null uses the Tier-2 plain-word form ("too few graded" / "暂无足够评分") rather than the negation set; the pre-existing estate is unchanged (38 UNEARNED findings, all in unrelated templates and engine code).

---

## 5. Overall verdict

**PASS — China selective synthesis publication + reason-Lens consolidation; all three audit dimensions pass with one positive sub-finding on the theme-system consolidation.**

| dimension | verdict | evidence |
|---|---|---|
| plain-language | PASS | 70+ new visible strings audited; all plain-word, bilingual EN/ZH via `<span class="l-en">` / `<span class="l-zh">` pairs; the `Why this read / 为什么` aria-label matches the hero's locked-driver pattern (cross-PR consistency +); the two reason-receipt tooltips **actively disclaim** authority in plain words ("does not confirm an entry", "does not claim a fresh recalibration") — the Tier-1 calibrated form, not the negation set; Track-record and Macro News empty-state are the Tier-2 plain-word null-disclosure form; no falsifier vocabulary reaches the user; no internal state names, untranslated stats, or raw slugs leak; no `title=` translations |
| theme (TP-0) | PASS | Every material decision in the rebaked `site/china.html` delegates to a token or a `color-mix(in srgb, var(--c) P%, …)` token-true expression; the only non-token literals are a single black drop-shadow (`rgba(0,0,0,.18)`) on a token-background popover and a single white text color (`#fff`) on a token-tinted flex bar — neither is a parallel palette; the PR **structurally** consolidates the China-local `cnx-lens` tooltip plane onto the shared `data-tip-*` Lens owner in `templates/theme.js` (deletes the local CSS + handler + `window.*` exposure + the Escape branch + the outside-click branch) — exactly the TP-0 consolidation the law exists to enforce; the new `@keyframes nb-cau-pulse` 2.5s attention pulse is paired with `@media (prefers-reduced-motion: reduce) { animation:none }`; 8/8 PASS browser matrix across 1440/390 × dark/light × EN/ZH is content-bound by the rebake sha `0ed6cbaf…d73457828a8c0`; `check_runtime_style_injection.py` clean (zero new `.js` files) |
| validated-claims | PASS | Zero new "validated"-class claims in any of the 70+ new visible strings; zero claim-class tokens in the template patch, the rebaked `site/china.html`, or the renamed test; the two reason-receipt tooltips **actively disclaim** authority in plain words — exactly the Tier-1 calibrated form; the Track-record null uses the Tier-2 plain-word form ("too few graded" / "暂无足够评分") rather than the negation set; the pre-existing estate is unchanged (38 UNEARNED findings, all in unrelated templates — `templates/china.html.j2` and `site/china.html` are NOT in the pre-existing list) |
| completeness | PASS | `test_china_archetype_d_s1.py` **24 passed** (body-quoted); `git diff --check` PASS; 8/8 PASS real-browser matrix across 1440/390 × dark/light × EN/ZH (HTTP 200, zero page errors, zero failed requests, zero horizontal overflow, 12 cards + 13 dialogs + 3 deep three-column racks + ROW 4 + synthesis + zero retired Archetype-D shell); rebake sha `0ed6cbaf…d73457828a8c0` content-bound to 440,039 bytes; the rename + strengthening of `test_synthesized_reason_receipts_use_the_shared_lens_plane` is exactly the test that locks the consolidation (asserts `data-tip-en`/`data-tip-zh` present + `cnxToggleLens`/`cnxCloseLenses`/`cnx-tip-open` absent + `.lens-pop.open` in the Escape handler) |

**Positive sub-findings (NOT 7607 findings, surfaced for awareness):**
- **Theme-system consolidation.** The PR's structural change is a TP-0 win — it removes a competing tooltip plane and folds China into the shared `data-tip-*` Lens owner. The mobile browser run explicitly caught the old failure mode (the global Lens capture handler and the China-local handler were competing on mobile); consolidating onto the shared owner fixes it at the source rather than adding a mobile-only workaround. The Escape handler now exits early if `.lens-pop.open` is present (the shared Lens's class), and the outside-click handler closes only `cnxClosePops()` (China-local popovers), leaving the shared Lens to handle its own dismissal via `theme.js`. China-local dialog/card keyboard behavior (`cnxOpenDlg`, `cnxCloseDlg`, `cnxTogglePop`, `cnxClosePops`, `cnxToggleHm`) is preserved verbatim. ✓ (positive, not a finding)
- **Reason-receipt Tier-1 calibrated form.** The two new reason-receipt tooltips **actively disclaim** authority ("does not confirm an entry", "does not claim a fresh recalibration") in plain words, which is exactly the operator's 2026-07-27 #3821 form. The receipt shows the reader what the page is doing (3 inputs, all same direction, aggregated as one monetary-conditions read) rather than asserting that the result is "validated". ✓ (positive, not a finding)
- **Cross-PR `Why this read / 为什么` aria-label parity.** The China lens button's aria-label is bilingual in a single attribute value, mirroring the hero's locked-driver pattern (`audit #7585 MINOR-1`). The seat records the cross-PR consistency as a + — the same plain-word bilingual screen-reader idiom is now used on both the hero and the China reason Lens, and a future PR could lift this to the dashboard-wide locked-driver pattern. ✓ (positive, not a finding)

**Body-disclosed follow-ups (NOT 7607 findings, surfaced for awareness):**
- **Rebase onto protected main `c48e15193a29`.** The PR body notes the branch was rebased forward to `c48e15193a29fd5eff94e963d0d0c4d6f3104867` before the final rebake, and protected main has since advanced with **zero movement** on the owned China template/test/build/news/site paths or shared `theme.js`/theme CSS at the latest comparison. The branch's `merge-on-green` arm cleared on squash-merge. ✓ (closed by merge, no follow-on owed)
- **`merge-on-green` arm.** The PR was DRAFT with `merge-on-green` armed; cleared at squash-merge. ✓
- **`#7534` design-law controlling this PR.** The body cites #7534 ("selective synthesis is the default evolution path") as the design law controlling this PR, and explicitly forbids re-opening the retired Archetype-D shell from a wholesale China design wipe (which would require substantial end-to-end superiority, not mere cleaner/newer/fewer). The PR honors both. ✓ (advisory only, no follow-on owed)
- **Browser matrix as receipt.** The body declares an 8/8 PASS real-browser matrix with HTTP 200 + zero page errors + zero failed requests + zero horizontal overflow + 12 cards + 13 dialogs + 3 deep three-column racks + ROW 4 present + synthesis present + zero retired Archetype-D shell + reason receipt opens/closes through the shared Lens on desktop **and mobile** + Enter activates the existing What To Do dialog in all cells. The rebake sha `0ed6cbaf…d73457828a8c0` is content-bound to 440,039 bytes. ✓ (receipt is the proof; CI would re-run on the head)

**Inherited base conditions (NOT 7607 findings):** the repo-wide pre-existing debt captured by `check_validated_claims.py` (38 UNEARNED rows across `_macro_suite_shell.html.j2`, the `macro_<topic>.html.j2` family, `mm_brain.js`, etc.) is **unchanged** by this PR — `templates/china.html.j2` and `site/china.html` are NOT in the pre-existing list, and none of the 38 files are in the diff. The `site/theme.js != templates/theme.js` template/site-sync mismatch reported in earlier audits (`macro_PR-7373.mm.md` et al.) is also unaffected — #7607 edits `templates/china.html.j2` (a one-page template, not the shared chrome) and the rebaked `site/china.html` (the paired plain-copy of that template), so the page-pair parity is preserved by construction. The broader `theme.js` template/site-sync mismatch is a separate, pre-existing lane.

**One-pass constraint honored:** no retries, no scope expansion, no durable file written outside the audit path. The audit output is this file, written to `/Users/chriswong/lanes/repos/macro/orch/audits/macro_PR-7607.mm.md` per the transport contract; the K reference (`meta-ceo-b-2026-09-08`) is honored by the audit header. No `gh` API quota was burned beyond the initial `pr list` / `pr view` / `pr diff` calls required by the task spec; the `check_validated_claims.py` and `check_runtime_style_injection.py` checks were run from the local checkout against the PR's diff (one-time reads), no `gh` calls were made against the PR head.

---

`SESSION END: PROVEN_OUTCOME`
