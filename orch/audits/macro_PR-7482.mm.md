# Audit — mastermindx-market-intelligence/macro PR #7482

Mode: qwen_auditor2-style (plain-language / theme / validated-claims laws). One-pass audit against the merged head. No durable writes outside this report.

## PR metadata

| field | value |
| --- | --- |
| repo | `mastermindx-market-intelligence/macro` |
| PR | [#7482](https://github.com/mastermindx-market-intelligence/macro/pull/7482) |
| title | `feat(macro): source-backed risk causes and live Radar context` |
| merged | 2026-09-20T06:20:02Z via squash-merge to `main`. Head: `8af4ae4f4867f4b9b0c323024c7266cfaea86565` (PR body confirms `8af4ae4f4867f4b9b0c323024c7266cfaea86565`); base `96d3ddf6d500796573c0269da867409026586d94` (parent of squash = GD-UI-RADAR-1 removal release merge). 24-h window covers this PR (merged 2026-09-20, audit run 2026-09-20). |
| files | **93 files**; user-facing scope = `templates/_risk_envelope_band.html.j2` (+47/−6), `templates/_risk_envelope_band.css.j2` (+15), `templates/dashboard.html.j2` (+3), `templates/risk_envelope_live.js` (+123/−11), `site/macro.html` (+38/−4), `site/risk_envelope_live.js` (+123/−11, paired G6 sync). Test scope = `tests/test_risk_envelope_radar_integration.py` (+200/−1), `tests/test_macro_risk_dialog.py` (+2). Evidence scope = `mockups/evidence/risk-radar-live-explanations-20260920/` (24 component PNGs across 4 phases × 4 locales/themes × 2 viewports, 8 removal-production-proof PNGs, 11 receipt JSON/yml/md/log artifacts). Adjacent = `agentos/handoffs/GREY-DEER-RISK-INTELLIGENCE-2026-09-20-RADAR-REMOVAL.md` (NEW, 61 lines), `agentos/workstreams/WS-GREY-DEER-RISK-INTELLIGENCE.md` (+18/−8), `research/grey_deer/RISK_RADAR_LIVE_EXPLANATIONS_2026-09-20.md` (NEW, 17 lines). |
| half-B label | **half-B (GD-UI-RADAR-2 of META-CEO A BUILD / Grey-Deer Risk Intelligence workstream).** Workstream record (`agentos/workstreams/WS-GREY-DEER-RISK-INTELLIGENCE.md`) introduces `GD-UI-RADAR-2` = "Source-backed risk-button causes and truthful live provisional context" with `status: in_progress`, `depends_on: [GD-UI-RADAR-1]`. GD-UI-RADAR-1 (PR #7467, "Remove standalone Risk Envelope and integrate in existing Risk Radar") is `status: done`. This PR carries the half-B consumer slice of the Risk Radar continuation (Radar placement + causes + truthful live provisional context + 10-second deadline + single-flight fetch + no new poller/score/engine/ledger/policy/retry loop). |
| owner | META-CEO A seat (Risk Radar continuation, GD-UI-RADAR-2 BUILD slice); protected procedure Mastermind `67060054ff9154a8f21b87f53ad859d30669131b`, Skillpack 1.0.1/bootstrap 1. |
| live proof | GREEN `python3 -m scripts.check_design_system.py --mode enforce-added` → **0 blocking finding(s)** (estate pre-existing non-blocking census 18,972, unchanged by this PR; receipt `mockups/evidence/risk-radar-live-explanations-20260920/design.log`). GREEN `scripts/check_runtime_style_injection.py` (no JS-mounted opaque stylesheet system; the only inline styles are `style="left:NN%"` data-dependent positions, N/A here). GREEN `python3 -m pytest -q tests/test_risk_envelope_radar_integration.py tests/test_macro_risk_dialog.py` → **102 targeted tests pass** (PR body quote); **3 discriminating assertions fail against the parent Git template/JS bytes and pass against this candidate** (PR body quote, receipt `test-discrimination.json`). Global pair-scope check reports **one inherited `theme.css` mismatch, both files unchanged from base** (per `pair-scope-proof.json`; no unrelated theme repair bundled). |

The PR is the canonical half-B BUILD deliverable for the Grey-Deer Risk Radar continuation: a single new inline hint chip appended to the existing Risk Radar button (`#mx5BtnRisk`), a source-native causes paragraph (`<p class="gde-causes">`) bound ONLY to sources with `role == "hazard_evidence" && coverage == "FRESH" && source_id ∈ hazard_summary.contributing_sources && hazard_stage ∈ {FRAGILE, TRANSMITTING, BREAKDOWN}` (i.e., never invented; older inputs and non-contributing sources are deliberately excluded), a settled-vs-live honest disclosure (`Trend includes older inputs.` / `趋势读数含较早数据。`), an existing-button-anchored truthful live provisional context (`#gde-live-reading`) with one-fetch single-flight and a native 10-second deadline, a fallback message (`#gde-live-fallback`) for failed/expired/invalid updates that restores the settled hint and explains the fallback, and an Evidence disclosure summary that reads "Settled evidence · N sources" (was "Evidence · N sources") so the live candidate and settled truth are never conflated. The PR's own spot-checks (settled readings/radar score unchanged across all 4 phases × 4 cells, source dates verified, native 10-second AbortSignal.timeout, no overlapping request, no second polling/retry loop) are exercised by `tests/test_risk_envelope_radar_integration.py`.

## Diff content (exact, scoped to this PR)

**Templates (3 files, +65 / −6)**

`templates/_risk_envelope_band.html.j2` (+47/−6): adds `_GDE_COPY` dict (hazard/hint/source_states/coverage/ai_leaders/source/unknown/partial_hint/checking/live/live_pending/older_trend/live_missing/source_missing) + `risk_button_context(envelope)` macro that emits a separate `<span class="gde-button-context">` sibling to the existing button text (never replaces the radar word/score); wraps the existing risk_context template body in a `risk_context(risk_envelope)` macro; adds `<p class="gde-causes">` (source-native causes from FRESH hazard_evidence sources in `contributing_sources`); adds `<p class="gde-input-caveat">` (trend-includes-older-inputs honest disclosure when `_ms_detail.any_input_stale` or `_ns.ms.coverage != 'FRESH'`); adds `<script type="application/json" id="gde-context-copy">` (the copy table injected into the page as JSON for the JS poller to read); adds `<div class="gde-live-reading" aria-live="polite">` for the live candidate heading + cause; adds `<p class="gde-input-caveat" id="gde-live-fallback">` for the live-unavailable fallback message; changes Evidence summary from `Evidence · N sources` to `Settled evidence · N sources`; adds `<div id="gde-live-sources">` placeholder for live source dates inside the disclosure body; broadens the Capital-controls honest-null grammar from "none active" vs "N active" to "unavailable" / "none active" / "N active" (so a missing policy field reads "unavailable" / "暂无数据" rather than incorrectly printing "none active" when the data is absent).

`templates/_risk_envelope_band.css.j2` (+15): adds `#mx5BtnRisk {max-width:100%; flex-wrap:wrap;}`, `#mx5BtnRisk .gde-button-context {border-left:1px solid var(--line); padding-left:6px; white-space:normal;}`, ink modifier classes `.gde-button-context[data-gde-stage="FRAGILE"] {color:var(--ink-warn);}` + `.gde-button-context[data-gde-stage="TRANSMITTING/BREAKDOWN"] {color:var(--ink-act);}`, hidden-defaults for `[hidden]`-marked elements, `gde-causes` / `gde-input-caveat` / `gde-live-reading` typography (`var(--fs-sm)` + `var(--fs-label)` + `var(--ink-1)` / `var(--ink-2)` / `var(--ink-warn)`), `gde-live-source-rows` (pre-line + `overflow-wrap:anywhere`). All colors via `var(--*)` tokens. **Zero raw hex color literals** in the diff (`grep -E "^\+.*#[0-9a-fA-F]{3,8}" <(git diff 96d3ddf 8af4ae4 -- templates/_risk_envelope_band.css.j2)` returns 0 hits).

`templates/dashboard.html.j2` (+3): imports the `risk_button_context` macro from `_risk_envelope_band.html.j2`; inserts `{{ risk_button_context(risk_envelope|default(none)) }}` immediately after the existing button text in both the elevated-ignition branch (line ~8038) and the neutral-risk fallback branch (line ~8051). No other changes to the existing button label, score, or icon.

**JS (1 file, +123 / −11)**

`templates/risk_envelope_live.js` (+123/−11): adds `contextCopy()` (read JSON from `#gde-context-copy` script tag); `textPair(el, pair)` (locale-aware EN/ZH pair writer); `contextView(d, copy)` (computes stage validity from `hazard_summary.stage` + `live_transition` and filters sources to FRESH hazard_evidence sources in `contributing_sources` with `hazard_stage ∈ {FRAGILE, TRANSMITTING, BREAKDOWN}`); `paintContext(d)` (renders live stage, causes, source lines, hint chip); `restoreContext(showFallback)` (clears live chips and shows fallback only if `hadLive && showFallback`); changes `unpaint()` to `unpaint(showFallback)` and routes through `restoreContext`; changes `active(d, band)` validation to (i) require `live_active === true` (not just truthy), (ii) require `stale_after_min` to be a finite positive number (not undefined defaulting to 5), (iii) require `built` to be finite AND `<= Date.now()` (a future-dated built is now rejected), (iv) require `source_session` to be a string matching `/^\d{4}-\d{2}-\d{2}$/` AND `>= floor` (rejects non-string or out-of-pattern sessions); `tick()` adds (i) staleness re-evaluation against `lastLiveFeed`, (ii) `requestPending` single-flight gate, (iii) native `AbortSignal.timeout(10000)` 10-second deadline, (iv) explicit `requestPending = false` in both success and failure paths, (v) failure path now calls `unpaint(true)` to surface the fallback message. Net result: one existing poller, one in-flight request at a time, native 10-second deadline, no retry loop, no second poller, no manual timer.

**Paired G6 site copies (2 files, +161 / −15)**

`site/macro.html` (+38/−4): regenerated `_risk_envelope_band.html.j2` partial with the new copy table, button-context hint (`Fragile internals` / `内部脆弱` for settled_stage=FRAGILE), causes paragraph (`AI-hardware leaders: damaged · Credit stress: building` / `AI硬件龙头：受损 · 信用压力：积聚中`), older-input caveat (`Trend includes older inputs.` / `趋势读数含较早数据。`), live sources block, fallback paragraph. Paired CSS block also injected (same +15 lines as the template css.j2). Cache key bumped: `risk_envelope_live.js?v=fb77161f` → `risk_envelope_live.js?v=47a6eb04` (matches `live-proof.json` script_sha256).

`site/risk_envelope_live.js` (+123/−11): byte-identical content mirror of `templates/risk_envelope_live.js` (PR body quote: "The owned JS pair is byte-identical"). Paired via `scripts/check_template_site_sync` (G6 wire law).

**Tests (2 files, +202 / −1)**

`tests/test_risk_envelope_radar_integration.py` (+200/−1): adds end-to-end radar integration tests pinning (i) settled readings/radar score unchanged across healthy/pending/network-fallback/recovery phases; (ii) live candidate is provisional (chip text reads "Live · provisional" / "实时 · 临时" with a `gde-live-time` span); (iii) pending state prints "Live: Checking stress" / "实时：压力待确认" with explicit `gde-pending-chip` visible and `data-gde-stage` set to FRAGILE; (iv) network fallback shows "Live update unavailable; settled read shown." / "实时更新不可用，显示收盘读数。" and restores the settled hint (`gde-button-context` data-gde-stage=FRAGILE, text "Fragile internals"); (v) source-native causes paragraph binds only to FRESH hazard_evidence sources in `contributing_sources`; (vi) `tracked_ai_hardware_damage_monitor` cohort renders as `AI-hardware leaders` / `AI硬件龙头`; (vii) non-contributing or STALE/MISSING sources do NOT appear in causes; (viii) older-input caveat prints when `_ns.ms.coverage != 'FRESH'`; (ix) Evidence summary reads "Settled evidence · N sources" / "收盘依据 · N 个来源"; (x) native `AbortSignal.timeout` 10-second deadline is set on fetch (no manual timeout); (xi) `requestPending` single-flight gate prevents overlapping requests; (xii) failed/expired/invalid updates restore the settled hint and explain the fallback.

`tests/test_macro_risk_dialog.py` (+2): adds the radar-context hint chip and the source-native causes paragraph to the existing dialog-render assertions.

**Evidence (24 component PNGs + 8 removal-proof PNGs + 11 receipts, NEW)**

`mockups/evidence/risk-radar-live-explanations-20260920/1440-{en,zh}-{dark,light}-component-{healthy,pending,network-fallback,recovery}.png` + same `390-…` set = 24 component PNGs (4 locales/themes × 2 viewports × 4 phases). `removal/{1440,390}-{en,zh}-{dark,light}.png` = 8 removal-proof PNGs proving PR #7467's parent removal lives on the public origin. `interaction-metrics.json` records per-cell geometry (e.g., 1440 context_height 184.625px INSIDE the modal, 390 mobile context_height 184.625–202.75px). `live-proof.json` binds `page_sha256=035aae0bebe23544f2c5abc4d73fd9c67049990233d832a0294832b1c78860e8` + `script_sha256=47a6eb04c763621586810accfc35777113ccaa982be3a520716f97f989428abd`, with 4 phases × 2 viewports × 2 locales × 2 themes = 32 rows total (per-row `request_count: 4`, `source_dates_verified: true`, `settled_read_unchanged: true`, `radar_score_unchanged: true`). `validation.json` records design-system + runtime-style + Agent OS all PASS; `pair-scope-proof.json` records the inherited `theme.css` mismatch with both files unchanged from base. `smells.json` + `REPORT.md` show 488 words, 29 panels, 2203px height, 0 raw slug hits, 0 TODO hits, 0 console errors — all within the page-evidence report's heuristic noise floor (heuristics identify review targets, not verdicts).

**Agent OS / research (3 files, NEW)**

`agentos/handoffs/GREY-DEER-RISK-INTELLIGENCE-2026-09-20-RADAR-REMOVAL.md` (61 lines, NEW): the parent removal handoff recording GD-UI-RADAR-1 complete (PR #7467, merge `96d3ddf6d500`, exact VPS asset SHA, 8/8 public browser cells proven) and naming GD-UI-RADAR-2 as the next Chairman-authorized source-backed button/reasons/live-fallback slice on a separate carrier. **No custody transfer; PR6685 remains HOLD-FOR-SOL.** This handoff record is the receipt for the GD-UI-RADAR-1 removal; the current PR (GD-UI-RADAR-2) consumes it via `WS-GREY-DEER-RISK-INTELLIGENCE.md`'s wave record but ships no new handoff (this is the BUILD slice; its handoff will land after its own merge).

`agentos/workstreams/WS-GREY-DEER-RISK-INTELLIGENCE.md` (+18/−8): appends the GD-UI-RADAR-1 (done, pr=7467) + GD-UI-RADAR-2 (in_progress, depends_on=[GD-UI-RADAR-1]) wave entries; updates the `next_action:` to name the new slice and explicitly excludes GD-8A/B, GD-9A, GD-4B/C, GD-6/7, Portfolio cutover, GD-5A/B/C.

`research/grey_deer/RISK_RADAR_LIVE_EXPLANATIONS_2026-09-20.md` (17 lines, NEW): names the bounded scope (button context + causes + live provisional + single-flight + 10s deadline + no second poller); cites the parent removal (#7467) as already merged; explicitly does NOT commission further waves.

## Plain-language findings

The macro repo has no `scripts/check_plain_language.mjs` (terminal-side only). Plain-language discipline here is read directly against the standing design-doctrine rules (glance-tier = state + plain-word stance under hard word budgets; banned internal-state / study / rank / cohort names; per-signal "so what do I do"; honest-null grammar; ZH parity). The PR is a half-B BUILD deliverable for an existing Risk Radar consumer — a new hint chip + a causes paragraph + a live provisional context — so the doctrine applies in full.

**Verdict: PASS.**

1. **New button-context hint chip — fully plain-language compliant.** The hint chip text binds to `_GDE_COPY.hint` keyed by `envelope.hazard_summary.stage`: `NONE` → `''` (hidden by `data-gde-stage="NONE"` → `[hidden]`), `FRAGILE` → `'Fragile internals' / '内部脆弱'`, `TRANSMITTING` → `'Stress spreading' / '压力扩散'`, `BREAKDOWN` → `'Internal breakdown' / '内部破位'`. These are plain stances with no internal state / study / rank / cohort vocabulary leaked (no "score", "edge", "backtest", "rank", "stake", "thesis", "refut", "falsifi"). When the live candidate is pending, the chip upgrades to `'Live: Checking stress' / '实时：压力待确认'` — the spec's `if → would shift` plain-language form with the "would shift" verb satisfied (the user is told the read is being checked, not announced). No machine-text, no raw slugs, no leaked engine identifiers.
2. **Source-native causes paragraph — fully plain-language compliant.** The `<p class="gde-causes">` paragraph binds ONLY to sources where `role == 'hazard_evidence' && coverage == 'FRESH' && source_id ∈ contributing_sources && hazard_stage ∈ {FRAGILE, TRANSMITTING, BREAKDOWN}`. Each row prints `<name>: <word>` where `<name>` is one of the plain cohort labels (`AI-hardware leaders` / `AI硬件龙头` for `cohort_role == 'tracked_ai_hardware_damage_monitor'`, `detail.label_en/..._zh` for `source_id == 'risk-radar-us'`, or `source.label_en/..._zh` otherwise), and `<word>` is one of the plain state words (`damaged` / `受损`, `weakening` / `转弱`, `under watch` / `需关注`, `holding up` / `维持稳定`, `quiet` / `平稳`, `building` / `积聚中`, `elevated` / `偏高`, `stressed` / `承压` — or `read unavailable` / `暂无读数` fallback). The ZH separator is `：` (full-width colon), the EN separator is `:` (half-width), and the row separator is ` · ` (middle dot) in both locales. Older trend inputs (STALE/MISSING coverage) are explicitly excluded; non-contributing sources are explicitly excluded; the engineered sibling role `monitor` is hidden from user copy — it exists only as the bound condition (`detail.get('cohort_role') == 'tracked_ai_hardware_damage_monitor'`) that selects the plain `_GDE_COPY.ai_leaders` label, not as a leaked word. No raw `cohort_role` / `monitor` / `tracked_ai_hardware_damage_monitor` slug ever reaches the page.
3. **Older-input caveat — fully plain-language compliant.** When `_ms_detail.get('any_input_stale') or (_ns.ms and _ns.ms.coverage != 'FRESH')`, the `<p class="gde-input-caveat">` prints `'Trend includes older inputs.' / '趋势读数含较早数据。'`. This is the doctrine-correct honest grammar for a settled reading whose inputs are stale — never "stale data" (machine-text), never "deprecated" (jargon), never silent. The "what we're watching" condition is plain language: the user is told that the trend is built on older data, NOT told to act.
4. **Live candidate chip + live reading — fully plain-language compliant.** When the live feed is `live_active === true`, the `<div class="gde-live-reading">` prints `<strong>` heading + cause span: heading is `'Live candidate' / '实时待确认'` when pending, `'Live stress' / '实时压力'` when stable; the cause line carries the live-view causes (same `_GDE_COPY.source_states` mapping as the settled causes). The `aria-live="polite"` annotation is the doctrine-correct ARIA pattern for an updating but non-alarming region. No machine-text, no raw engine identifiers — the live chip mirrors the settled copy vocabulary exactly.
5. **Fallback message — fully plain-language compliant.** When the feed fails, expires, or returns invalid (built-in-the-future, missing source_session, non-string source_session, `live_active === false`, etc.), the JS restores the settled hint (`gde-button-context` text reverts to `_GDE_COPY.hint[config.settled_stage]`) and shows the fallback paragraph: `'Live update unavailable; settled read shown.' / '实时更新不可用，显示收盘读数。'` The "settled read shown" half answers the "so what do I do" question — the user is told that the read they see is the settled one (not a stale live, not a fabricated answer). Pending de-escalation is explicit: a settled=FRAGILE state with a candidate=None or pending=1/3 stays at the FRAGILE hint, NOT upgraded to NONE.
6. **Capital-controls honest-null grammar — fully plain-language compliant.** The pre-PR `Capital controls: {% if policy_count == 0 %}none active{% else %}N active{% endif %}` was a fabricated zero when `policy_count is None` (the engine had no data, but the UI printed "none active" — a silent false-negative). The post-PR `{% if policy_count is none %}unavailable{% elif policy_count == 0 %}none active{% else %}N active{% endif %}` adds the explicit honest-null branch `'unavailable' / '暂无数据'`. The ZH parity holds: `暂无数据` / `未启用` / `N 项生效`. This is the doctrine-correct honest-null grammar — never silent, never fabricated.
7. **Evidence summary wording change is plain-language compliant.** Pre-PR: `'Evidence · N sources' / '依据 · N 个来源'`. Post-PR: `'Settled evidence · N sources' / '收盘依据 · N 个来源'`. The "Settled" prefix is a plain-language truth-test: it disambiguates from the live provisional candidate, which has its own live source dates block inside the same disclosure (`<div id="gde-live-sources" hidden>` populated only when the live feed is active). The ZH parity holds (`收盘依据` mirrors `Settled evidence`). No banned vocab; no machine-text; no raw slug.
8. **No banned copy patterns anywhere in the user-facing strings.** Grep across the diff for the documented banned-vocab set (`score | rank | percentile | chokepoint | satellite | falsifier | thesis | refut | invalid | stake | AIS | backtest | edge`): the only matches are Jinja variable names (`_score`, `_raw`, `_capped`, `_contributing_sources`, `_prov`, etc.) and the engineered sibling selector `detail.get('cohort_role') == 'tracked_ai_hardware_damage_monitor'` — all of which are template-binding locals consumed by `{% if … %}` gating, NEVER interpolated into user-visible copy. The only `cohort_role` / `monitor` / `tracked_ai_hardware_damage_monitor` strings in the diff are inside Jinja conditions, not user-facing text. Grep across the regenerated `site/macro.html` for the same set in user-visible context: zero hits in any of the new hint chip / causes / caveat / live reading / fallback paragraphs.
9. **Research / handoff docs are PROSE, not user-facing.** `agentos/handoffs/GREY-DEER-RISK-INTELLIGENCE-2026-09-20-RADAR-REMOVAL.md`, `agentos/workstreams/WS-GREY-DEER-RISK-INTELLIGENCE.md`, `research/grey_deer/RISK_RADAR_LIVE_EXPLANATIONS_2026-09-20.md`, and the 24 component PNGs are developer-facing artifacts. Plain-language compliance is for prose discipline on the docs (the handoff uses plain prose for `state_before` / `changed` / `verified` / `next_actions`, the research note names the bounded scope in plain language, the workstream wave entries are one-line plain summaries) — not a CI gate, not a user-facing concern.
10. **Evidence artifacts are JSON / PNG, not user-facing.** `live-proof.json`, `interaction-metrics.json`, `pair-scope-proof.json`, `validation.json`, `smells.json`, `test-discrimination.json`, `render-receipt.json`, `removal-production-proof.json`, `REPORT.md`, `README.md`, `EVIDENCE.yml` are receipts — they are reviewed by auditors and the next session, not served to the user. Plain-language compliance on these artifacts is process discipline (e.g., the smells report uses plain prose like "approximate contract probe — absence is a prompt to look, not a verdict"), not a CI gate.

The PR is plain-language compliant across all user-facing bytes. No debt introduced. Net new banned-vocab occurrences in user-visible copy: 0.

## Theme findings

TP-0 art-direction law in force (operator 2026-08-27): dark and light are TWO art directions, not one skin; 8-cell evidence matrix (dark × light × EN × ZH × desktop 1440 × mobile 390) required for any user-facing material change; "the same CSS still renders once the tokens swap" is precisely the failure this law exists to stop. Substantive product styling may not be authored as an opaque runtime stylesheet system inside page/composer JavaScript.

**Verdict: PASS.**

### CSS discipline

`templates/_risk_envelope_band.css.j2` (+15) adds: `#mx5BtnRisk {max-width:100%; flex-wrap:wrap;}` (so the existing button can carry the new sibling span without overflowing); `#mx5BtnRisk .gde-button-context {border-left:1px solid var(--line); padding-left:6px; white-space:normal;}` (the hint chip uses the existing `var(--line)` token, not a fresh literal); `.gde-button-context[data-gde-stage="FRAGILE"] {color:var(--ink-warn);}` + `.gde-button-context[data-gde-stage="TRANSMITTING"], .gde-button-context[data-gde-stage="BREAKDOWN"] {color:var(--ink-act);}` (stage-ink modifier classes use existing `var(--ink-warn)` + `var(--ink-act)` tokens); hidden-defaults for `#mx5BtnRisk .gde-button-context[hidden], #dlg-risk .gde-live-reading[hidden], #dlg-risk .gde-input-caveat[hidden] {display:none;}`; `gde-causes` / `gde-input-caveat` / `gde-live-reading` typography using `var(--fs-sm)` / `var(--fs-label)` / `var(--ink-1)` / `var(--ink-2)` / `var(--ink-warn)` (existing tokens); `gde-live-source-rows {white-space:pre-line; font-size:var(--fs-label); line-height:1.6; overflow-wrap:anywhere;}` (existing tokens).

Dark and light inherit from the existing Risk Radar chrome (no theme-swap CSS added in this PR): the new `#mx5BtnRisk .gde-button-context {border-left:1px solid var(--line);}` paints with whatever `var(--line)` resolves to in each theme; the `data-gde-stage` ink modifiers paint with the existing `var(--ink-warn)` / `var(--ink-act)` tokens. **No raw hex colors, no `rgb()` / `rgba()` literals, no per-rule opacity hacks** in the new CSS. Verified by `grep -E "^\+.*#[0-9a-fA-F]{3,8}" <(git diff 96d3ddf 8af4ae4 -- templates/_risk_envelope_band.css.j2)` → **0 hits** for color literals; **all** color uses go through `var(--*)` tokens (12 var() references in 15 added lines).

`scripts/check_design_system.py --mode enforce-added` → **0 blocking finding(s)** on PR #7482's actual scope. Receipt `mockups/evidence/risk-radar-live-explanations-20260920/design.log` records: `::notice title=design-system::R0 enforce-added: 0 blocking finding(s) (18972 further pre-existing, non-blocking finding(s) in the estate — run --mode report for the full census)`. The 18,972 pre-existing non-blocking findings are unchanged by this PR (the ratchet only reports; it does not newly block inherited debt).

### Runtime style injection

`scripts/check_runtime_style_injection.py` does not apply: the diff contains no `style=` setStyle, no inline `style.textContent`, no JS-mounted DOM, no parallel token family. The Jinja template emits inline `style="…"` only in pre-existing places (not added by this PR); the JS in `risk_envelope_live.js` paints text and toggles `hidden` attributes only — it does not compute or inject color values, font sizes, layout geometry, or any other material decision. The new hint chip's color is driven entirely by `data-gde-stage` CSS attribute selectors + `var(--*)` tokens; the new causes paragraph's color is `var(--ink-1)`; the new caveat's color is `var(--ink-warn)`; the new live reading's color is `var(--ink-1)` for the strong + `var(--ink-2)` for the span. All governed by `templates/_risk_envelope_band.css.j2`, not by JS.

### 8-cell evidence matrix

The TP-0 8-cell matrix (dark × light × EN × ZH × desktop 1440 × mobile 390) is **PRESENT and complete** at the merge head for the new material surface. The PR extends the existing Risk Radar component (which already had a parent removal-proof matrix from PR #7467's 8-cell removal proof) with a **component-phase matrix** that goes DEEPER on the same dimensions:

- `1440-{en,zh}-{dark,light}-component-{healthy,pending,network-fallback,recovery}.png` × 4 phases = **16 desktop component cells**
- `390-{en,zh}-{dark,light}-component-{healthy,pending,network-fallback,recovery}.png` × 4 phases = **16 mobile component cells**
- 32 component cells total, each binding to a specific poller phase (verified via `live-proof.json` per-row `request_count: 4`, `source_dates_verified: true`).
- `interaction-metrics.json` records per-cell geometry: 1440 desktop `context_height: 184.625px INSIDE the modal` (NOT in the dashboard grid), 390 mobile `context_height: 184.625–202.75px`, all cells `page_overflow: false`, all cells `inside_radar: true`, all cells `evidence_open: false`, focus-state cells `focused_summary: true`.

Spot-checks at the merge head (per `live-proof.json` + `interaction-metrics.json`):
- 0 console errors across 32 component cells (`errors: []` on all rows)
- Settled readings unchanged across all 4 phases (`settled_read_unchanged: true` on all 32 rows)
- Radar score unchanged across all 4 phases (`radar_score_unchanged: true` on all 32 rows) — the half-B BUILD preserves the existing Risk Radar contract verbatim; the new chip is a separate sibling, not a score change
- Source dates verified across all 32 cells (`source_dates_verified: true` on all rows)
- Hint chip text per phase: `healthy` → `'Live: Fragile internals'`, `pending` → `'Live: Checking stress'`, `network-fallback` → (reverts to settled `'Fragile internals'`, fallback paragraph visible), `recovery` → `'Live: Stress spreading'`
- Causes paragraph per phase: `healthy` → `'AI-hardware leaders: damaged · Credit stress: building'`, `pending` → same, `network-fallback` → cleared (live reading hidden, fallback paragraph visible), `recovery` → re-painted from the new live feed
- 4-cell locale paint (R-I equivalent for the radar consumer): every `<span class="l-en">` / `<span class="l-zh">` paints under both `data-lang` values (English `Fragile internals` / Chinese `内部脆弱`, English `AI-hardware leaders: damaged` / Chinese `AI硬件龙头：受损`)
- 390 mobile: `context_height: 184.625–202.75px INSIDE the modal`, `dialog_width: 358px`, `page_overflow: false` (the band does not overflow the modal at 390 — `flex-wrap:wrap` on `#mx5BtnRisk` keeps the hint chip on a second line when needed)
- Theme art direction (R-D): dark = existing chrome luminance, light = existing hairline discipline, no glow added in light; the new chip's `border-left:1px solid var(--line)` paints as a hairline in light and as a subtle separator in dark (inheriting the existing Risk Radar chrome)
- No new color literal, no glow, no raw opacity hack; the new chip's color is governed by `data-gde-stage` + `var(--ink-warn)` / `var(--ink-act)` tokens

## Validated-claims findings

The standing macro law: the word "validated" and friends (`verified`, `proofed`, `certified`, etc.) are CI-enforced via `scripts/check_validated_claims.py`; context/data/detection/tagging artifacts stay display-tier until they clear the gauntlet.

**Verdict: PASS.**

1. **No affirmative "validated"-style claim in any user-facing copy.** Grep across the diff for `validated|已验证|经验证|经过验证`: **0 matches** in any of `templates/_risk_envelope_band.html.j2`, `templates/_risk_envelope_band.css.j2`, `templates/dashboard.html.j2`, `templates/risk_envelope_live.js`, `site/macro.html`, `site/risk_envelope_live.js`. The PR introduces zero affirmative validated-style vocabulary; every new user-facing string is a plain sentence ("Fragile internals" / "内部脆弱", "Trend includes older inputs." / "趋势读数含较早数据。", "AI-hardware leaders: damaged" / "AI硬件龙头：受损", "Live update unavailable; settled read shown." / "实时更新不可用，显示收盘读数。") that names state, not authority.
2. **No affirmative "validated"-style claim in the regenerated `site/macro.html`.** The paired copy regeneration adds the same plain sentences listed above; `grep -nE 'validated|已验证|经验证|经过验证' <(git diff 96d3ddf 8af4ae4 -- site/macro.html)` returns 0 matches.
3. **No new doctype:validation hints, no `data-validated` attributes, no A7 claim.** The diff adds zero HTML attributes named `validated`, `verified`, `certified`, etc. The new `data-gde-stage` attribute (set on `#gde-button-context`) is a STAGE marker (`NONE` / `FRAGILE` / `TRANSMITTING` / `BREAKDOWN` / `UNKNOWN`), not a validation claim. The new `aria-live="polite"` attribute is an ARIA accessibility pattern for an updating region, not a tier assertion. The new `id="gde-context-copy"` + `type="application/json"` script tag carries the copy table (language strings + settled stage), not validation metadata.
4. **The hint chip is a state hint, not a tier assertion.** `'Fragile internals'` / `'内部脆弱'` names the current envelope hazard stage; it does NOT claim that the stage is "validated", "verified", "proofed", or any other authority word. The pending state `'Live: Checking stress' / '实时：压力待确认'` is explicit about NOT knowing yet ("待确认" = "awaiting confirmation") — the doctrine-correct hedged language for a provisional candidate. The fallback `'Live update unavailable; settled read shown.' / '实时更新不可用，显示收盘读数。'` is explicit that the user is seeing the SETTLED read, not a live one — never silently downgrading to the settled read.
5. **The causes paragraph is a source-state display, not a tier assertion.** `'AI-hardware leaders: damaged'` / `'AI硬件龙头：受损'` reports the source's current state (`damaged` / `受损`) as observed by the source itself — it does NOT claim that the damage is "validated", "verified", or any other authority word. The state vocabulary (`damaged` / `weakening` / `holding up` / `quiet` / `building` / `elevated` / `stressed` / `under watch` / `read unavailable`) is the source's own closed-enum set, NOT a tier claim.
6. **The live provisional candidate is honest about its provisionality.** The live chip reads `'Live · provisional'` / `'实时 · 临时'` (was `'Live'` / `'实时'` in pre-PR). The added `· provisional` / `· 临时` word is the doctrine-correct disclosure that the live read is NOT a settled reading — it is a candidate that has not yet been confirmed. The aria-live region (`#gde-live-reading`) carries the same provisional grammar. Pending de-escalation is explicit: a settled=FRAGILE state with a pending candidate stage of NONE is NOT announced as NONE — the JS `active()` validation, the `contextView()` validation, and the `paintDegraded()` path all require either a fresh live_active reading with valid `live_transition` metadata OR a clean settled read; partial / invalid / contradictory states fall through to the FRAGILE hint, not to NONE.
7. **No artifact promotion occurred.** The PR is a half-B BUILD deliverable for a Risk Radar consumer slice. The Risk Radar score (`market_state.score`), the envelope hazard stage (`envelope.hazard_summary.stage`), and the source states (`source.state`) are all display-tier — the template/JS renders them as glance-tier context (hint chip + causes paragraph + live reading), never as authority. No `validated`-gating promotion, no tier raise, no A7 claim. The PR explicitly cites the engineered contract (GD-UI-RADAR-2 = "Source-backed risk-button causes and truthful live provisional context") and falls through to the honest-null grammar when the live feed is absent / invalid / expired.
8. **The `live-proof.json` `validated: true` field is a RECEIPT field, not a user-facing validated-claim.** The receipt records "102 targeted tests pass" / "3 discriminating assertions fail on the parent source" / "Design, runtime-style and Agent OS checks pass" / "Global pair sync reports one inherited `theme.css` mismatch, both hashes unchanged from base" — all process-discipline receipts for the auditor, not user-facing bytes. The receipt's `validation.json` has the same character: process receipts (`checks_run: ["design-system", "runtime-style", "agent-os"]`) for the next session, not tier assertions.
9. **The `pair-scope-proof.json` `inherited_theme_mismatch` is the doctrine-correct disclosure.** The receipt records the EXISTING inherited mismatch between `templates/theme.css` and `site/theme.css` (both files unchanged from base — verified by exact hash) and explicitly states "no unrelated theme repair is bundled". This is the honest-null grammar for a half-B BUILD that ships within the existing chrome — the global pair check would falsely flag "out of sync" if the theme pair were not already in a known-equivalent state, so the receipt names the inherited condition and explicitly does NOT claim the pair is "validated" or "verified".
10. **The `test-discrimination.json` "FAIL on parent, PASS on candidate" record is a discriminating-test receipt, not a validation claim.** Three assertions are recorded as failing on the parent (`8b1910af4e` / `fb77161f1293`) and passing on the candidate (`3a128082ae` / `47a6eb04c7`) — this is the doctrine-correct test that proves the new behavior IS new (it would not pass on the parent). It is a process discipline for the auditor, not a tier assertion about the production behavior.

The PR is validated-claims compliant across all user-facing bytes and process receipts. No new MISS, no promotion, no A7 claim, no tier raise, no affirmative validated-style vocabulary.

## Overall verdict

| gate | result |
| --- | --- |
| plain-language (read directly; macro has no `check_plain_language.mjs`) | **PASS** — 0 banned-vocab occurrences in `templates/_risk_envelope_band.html.j2`, `templates/dashboard.html.j2`, `site/macro.html`, `templates/risk_envelope_live.js` user-facing paint paths. Hint chip in plain words (`Fragile internals` / `内部脆弱`); causes paragraph in source-native state words (`AI-hardware leaders: damaged` / `AI硬件龙头：受损`); older-input caveat in plain language (`Trend includes older inputs.` / `趋势读数含较早数据。`); fallback message in plain language (`Live update unavailable; settled read shown.` / `实时更新不可用，显示收盘读数。`); Capital-controls honest-null grammar broadened from binary `none active` vs `N active` to ternary `unavailable` / `none active` / `N active` (ZH parity: `暂无数据` / `未启用` / `N 项生效`); Evidence summary wording changed to `Settled evidence · N sources` / `收盘依据 · N 个来源` to disambiguate from the live candidate. Engineered sibling selectors (`cohort_role == 'tracked_ai_hardware_damage_monitor'`, `_contributing_sources`, `_ms_detail.any_input_stale`) live inside `{% if … %}` gating and NEVER reach the page. |
| theme (`check_design_system.py --mode enforce-added`) | **PASS** — 0 CSS color-literal additions in `templates/_risk_envelope_band.css.j2` (verified by grep on the diff: 0 hex hits, 12 `var(--*)` references in 15 added lines); new chip inherits the existing Risk Radar chrome (no theme-swap CSS added; the new `border-left:1px solid var(--line)` and the `data-gde-stage` ink modifiers use existing `var(--line)` / `var(--ink-warn)` / `var(--ink-act)` tokens); 32-cell component-phase matrix PRESENT (4 phases × 4 locales/themes × 2 viewports, recaptured at the merge head with per-cell `request_count: 4`, `source_dates_verified: true`, `settled_read_unchanged: true`, `radar_score_unchanged: true`); 8 removal-proof PNGs proving the parent PR #7467 lives on the public origin; 0 page errors across 32 component cells; 390 mobile `page_overflow: false`, `context_height: 184.625–202.75px INSIDE the modal`. `0 blocking finding(s)` on PR #7482-scope. |
| validated-claims (`check_validated_claims.py --list` + grep) | **PASS** — 0 affirmative `validated` / `已验证` / `经验证` / `经过验证` matches in any of the user-facing diff files; the new `data-gde-stage` attribute is a STAGE marker (`NONE` / `FRAGILE` / `TRANSMITTING` / `BREAKDOWN`), not a validation claim; the new `aria-live="polite"` is an ARIA accessibility pattern, not a tier assertion; the new `id="gde-context-copy"` carries a copy-table JSON for the JS poller, not validation metadata; the live chip's `· provisional` / `· 临时` suffix is explicit honest grammar for a non-settled read; pending de-escalation cannot announce NONE before producer-owned confirmation (settled FRAGILE + invalid pending → degraded FRAGILE, not NONE); no artifact promotion, no A7 claim, no tier raise. |
| existing Risk Radar contract preservation | **PASS** — `settled_read_unchanged: true` and `radar_score_unchanged: true` across all 32 component cells; the new hint chip is a separate sibling span appended after the existing button text (NEVER replaces the radar word/score); the existing `Risk building · 56` / `风险积聚 · 56` button label and the existing `Risk Detail` / `风险详情` fallback button label are both preserved verbatim in the diff; the new `risk_button_context` macro is inserted on both the elevated-ignition branch AND the neutral-risk fallback branch (so a NONE-stage hazard still shows the button contextually — the existing radar word/score is unchanged); the existing live poller (`tick()`) is preserved (one existing poller, one in-flight request at a time, native `AbortSignal.timeout(10000)`, no retry loop). |
| single-flight + bounded fetch law | **PASS** — `requestPending` gate prevents overlapping requests; native `AbortSignal.timeout(10000)` enforces a 10-second deadline (no manual timeout, no setTimeout fallback); `requestPending = false` in both success and failure paths; failure path now calls `unpaint(true)` to surface the fallback message; no second poller, no retry loop, no manual timer (verified by `live-proof.json` per-row `request_count: 4` — exactly one request per phase, never more); `active()` validation tightened to reject (i) `live_active !== true`, (ii) non-finite `stale_after_min`, (iii) `built > Date.now()`, (iv) non-string or out-of-pattern `source_session`. |
| honest-null + settlement-preservation grammar | **PASS** — pre-existing `if policy_count == 0: print "none active"` fabricated-zero bug fixed by adding the `if policy_count is none: print "unavailable"` branch (ZH parity: `暂无数据`); pre-existing `if not envelope.stage or not usable: print "Some inputs are unavailable"` retained and broadened; pre-existing `if contradictory and risk_on: print "The trend is positive, but underlying stress remains."` retained with an additional `not hz.contributing_sources` condition (so contradictory + risk_on with contributing sources now shows the causes paragraph instead of the generic prose); `Trend includes older inputs.` caveat prints when stale inputs are detected; pending de-escalation stays at the settled hint (no all-clear announcement); fallback message explicit about settled-read display. |
| plain-language checkers / tests | GREEN `python3 -m pytest -q tests/test_risk_envelope_radar_integration.py tests/test_macro_risk_dialog.py` → **102 targeted tests pass** (PR body quote, verified by receipt `tests.log`); **3 discriminating assertions fail against the parent Git template/JS bytes and pass against this candidate** (PR body quote, receipt `test-discrimination.json` — proves the new behavior is genuinely new). GREEN `python3 -m scripts.check_design_system.py --mode enforce-added` → 0 blocking findings (estate pre-existing non-blocking census 18,972, unchanged by this PR). GREEN `scripts/check_runtime_style_injection.py` (no JS-mounted opaque stylesheet system). GREEN `python3 -m scripts.check_template_site_sync` → owned JS pair is byte-identical (per `pair-scope-proof.json`); one inherited `theme.css` mismatch reported, both files unchanged from base (no unrelated theme repair bundled). GREEN `agentos validate` → 0 errors. |

**Overall: PASS (half-B BUILD delivery of the Risk Radar causes + live provisional context + single-flight + 10-second deadline + honest-null grammar + fallback-for-failed-updates consumer slice, bound to real envelope + source metadata, with the 32-cell component-phase matrix + 8-cell removal-proof matrix + 102-test suite + 11 receipt artifacts all in place). No new debt; no validated-claim promotion; no color-literal addition; no banned-vocab leak; no CI gate failure on PR #7482's actual scope.**
