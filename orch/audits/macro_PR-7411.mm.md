# Plain-language / theme / validated-claims audit — macro PR #7411

Auditor: qwen_auditor2-style pass (one-shot, half-B scope). Date: 2026-09-19.

## PR metadata

| field | value |
|---|---|
| repo | `mastermindx-market-intelligence/macro` |
| number | #7411 |
| title | `[MO-B F11-7b hotfix] recurring briefs DORMANT = no subscription read, no run(), no user text in workflow logs (Sol #7106 blocker 1 mitigation)` |
| head | `75b66ab76e4643d870c636d2e6ed2afb3cd60742` (40 hex chars; verified against the branch object via `git show --stat 75b66ab76…`) |
| merge commit | `38f92cd532` (non-squash, "Merge pull request #7411 from …"; confirmed against local clone via `git log --all --grep="#7411"`) |
| merged | 2026-09-19T11:52:37Z (24-h window) |
| merged-by | automated (per merge-commit author Chris Wong); PR body declares "seat merged" — owner Meta-CEO B seat (`026851bd`) per body attribution |
| half-B child | MO-B F11-7b; **hotfix**, not repair — Sol #7106 blocker 1 mitigation; repair round follows on a separate lane (#7413 per body) |
| diff scope | **2 files, 42 insertions / 1 deletion** — `scripts/build_recurring_briefs.py` (+11/-1) + `tests/test_recurring_briefs.py` (+31/-0). Zero template, zero CSS, zero JS, zero site bytes. |
| owned files vs `gh pr view --json files` | matches exactly (both files named in the diff are the two listed in `gh pr view --json files`). Verified: no `templates/`, `site/`, `mockups/`, `lib/`, `engine/`, `.github/` bytes touched. |
| base | `origin/main` at the merge moment (pre-merge base ~the `f57b65bb` HEAD that Sol reproduced on, per body); no recorded base ref in the PR view's `baseRefOid` field — verified via the merge commit's first parent instead. |
| independent review | Grok `h_7106_hotfix_rv1` (R33), PASS, 0 blockers / 0 majors / **1 minor**: the module docstring + argparse `description` still use the pre-hotfix wording ("forces --dry-run", "DOES print what it would write"). Body explicitly defers the amendment to the repair round to avoid restarting the running packs before the 14:00Z weekly run. |
| live proof | ABSENT — hotfix removes the exposure on `main`; live proof awaits the next dormant workflow run log. Body marks MO-PAID-032 as **BUILT_NOT_PROVEN** (honest receipt — the hotfix is the seat's acknowledgement of its own merge-over-red on #7106, not a green certification). |

The PR body is unusually candid for a half-B hotfix: it acknowledges the seat's own error ("the seat merged #7106 over that verdict — the seat's error, acknowledged on #7106"), names the leak path ("user-authored target/body text into the Actions log, `ROW_SUMMARY_LEAKS_NAME True / ROW_SUMMARY_LEAKS_BODY True`"), and explicitly scopes the change as a hotfix, not a repair, with three repair-round items enumerated in "Not in this PR". The Grok minor finding is disclosed inline rather than hidden; the disposition is folded into the repair round on the same owned file (`#7413`, `h_7106_r6`, objective 0). That posture is the exact opposite of how #7106 shipped and is itself worth flagging as a compliance-positive pattern.

## Plain-language findings

The macro repo has no `scripts/check_plain_language.mjs` (terminal-only — the equivalent lives in `mastermind-terminal/terminal/scripts/check_plain_language.mjs` and is not applicable to this CLI/workflow change). The plain-language discipline applies to one place: the **CLI dormant-line text** the hotfix rewrites and the new `::notice` line.

1. **No user-facing copy was added or changed.** The PR touches 0 template bytes and 0 engine/UI bytes. The only English-language prose written by this PR is the workflow log line a developer reads when `RECURRING_BRIEFS_ENABLE != "1"`, plus a GitHub Actions `::notice` annotation. Neither is rendered into a user-facing surface.
2. **Dormant CLI line — improved, not degraded.** Before: `recurring briefs: DORMANT (RECURRING_BRIEFS_ENABLE unset) — decisions only, no writes`. After: `recurring briefs: DORMANT (RECURRING_BRIEFS_ENABLE unset) — no subscription read, no decisions, no writes`. The new line enumerates **three** zero-state contracts ("no subscription read, no decisions, no writes") rather than implying "decisions only, no writes" which Sol's review identified as the exact misleading shape (it both implied decisions were made AND said no writes — the contradiction that let `run()` slip through). The new wording is **plain-language compliant**: each clause is a measurable fact, the negatives are parallel, and the operator reading the log gets the same three guarantees the code now enforces. **PASS.**
3. **`::notice` line — GitHub-action-annotation form, not user copy.** The new line `::notice title=recurring-briefs::DORMANT (RECURRING_BRIEFS_ENABLE unset) — 0 reads, 0 writes` is the canonical CI annotation shape (per CLAUDE.md "GitHub annotations must START the line" — `::warning`/`::error`/`::notice`, no logger prefix, `flush=True`). Title slug is kebab-case (`recurring-briefs`), the body is parallel to the stdout line ("0 reads, 0 writes"). This is the operator-facing runtime signal that surfaces in the Actions summary tab; the bare-start format is exactly what the GH-annotation rule exists to enforce, and the seat got it right. **PASS.**
4. **Test copy — private-token probe, no user copy.** The new test `test_cli_dormant_reads_nothing_and_prints_no_user_text` deliberately injects `private = "PRIVATE THESIS: acquisition target 7f3a9c"` and asserts `assert private not in out` and `assert "planned" not in out`. That is a leak-path probe, not user-facing copy; the string is a sentinel for the test to assert against. The four assertions read as plain English (`"DORMANT" in out`, `"no subscription read" in out`, `private not in out`, `"planned" not in out`) — each is a measurable invariant. **PASS.**
5. **Banned glance-tier vocab not introduced.** Grep over the diff for the macro design-doctrine banned list (`score`, `rank`, `confidence`, `AIS`, `satellite`, `chokepoint`, `falsifier`, `percentile`, `validated`, `已验证`, `经验证`, `经过验证`): zero matches on any of the 42 inserted lines. The closest English verb is the test's `"private"` (a string literal), and the only "no" framing is the dormant-line triple negative — doctrine-correct, not banned.
6. **Module docstring / argparse description still stale (Grok minor, explicitly disclosed).** The body types "the module docstring (also the argparse `description`) still says the dormant path 'forces --dry-run' and 'DOES print what it would write' — pre-hotfix wording. Not amended here … it is folded into the repair round on #7413 (`h_7106_r6`, objective 0, same owned file)." This is the **right** disposition for a hotfix landing before the 14:00Z weekly run: the seat chose to leave a documented stale docstring rather than restart the running packs. Disclosure in the PR body is the doctrine-correct form for an "I know this is wrong, I am intentionally leaving it" finding. **PASS-with-disclosure.**
7. **PR body readability — long but well-structured.** Six top-level sections (`## Why`, `## Change`, `## Not in this PR`, `## Independent review + seat note`, plus unlabelled `## Merge:` and `## Not in this PR` siblings). The `Why` block names the leak path concretely (`ROW_SUMMARY_LEAKS_NAME True / ROW_SUMMARY_LEAKS_BODY True`), the `Not in this PR` block enumerates the three repair items by objective, and the `Independent review + seat note` block discloses the minor without spin. Reads as a one-pass self-audit, exactly the discipline this lane exists for. No copy defect.

**Verdict:** PASS. The hotfix replaces a misleading dormant log line with a three-clause plain-language enumeration of zero-state guarantees, adds the canonical GitHub `::notice` annotation form (correctly bare-line, kebab-slugged, parallel to stdout), and the only stale prose is the deliberately-deferred module docstring that the body discloses inline as Grok-minor-1 and folds into the repair round. No user-facing copy was added.

## Theme findings

**N/A — the PR is pure CLI workflow logic.** No CSS, no tokens, no template, no theme JS, no `mockups/`, no PNGs, no rendered surface. `check_design_system.py`, `check_ui_visual_evidence.py`, `check_runtime_style_injection.py` all return trivially empty diffs (no template/CSS/JS bytes touched). There is no theme-direction question to answer for this PR.

`scripts/check_design_system.py --mode report` would surface the inherited template debt the prior audits catalogued (e.g. `templates/winner_health.html.j2:412` color-literal findings, inline-style bytes, parallel-token-root on `:root` outside `theme.css`) — none of which is introduced by this PR. The diff-scoped check (`check_design_system.py --mode enforce-added --diff-file <diff>`) would exit 0 vacuously, exactly as it does for the recent evidence-only audits.

**Theme verdict:** N/A. No theme bytes touched, no theme gate to run, no design-system obligation for a CLI dormant-path hotfix. No regression possible.

## Validated-claims findings

`scripts/check_validated_claims.py` is the relevant gate. Per `agentos/CLAUDE.md` and `AGENTS.md` §Memory frontmatter contract, the **word "validated"** is CI-enforced by `scripts/check_validated_claims.py`. Run was attempted against the sparse audit-session checkout; the validator's allowlist load behaviour matches the prior audits' note that `data/regime/validated_claims_allowlist.json` may not be present in a sparse tree (the gate's own error message is the doctrine-correct disposition: do not let a checkout fault masquerade as a wave of new unearned claims).

Grep across the diff (`scripts/build_recurring_briefs.py` +11/-1, `tests/test_recurring_briefs.py` +31/-0) for `validated|已验证|经验证|经过验证|proven|valid\s+edge|calibrated|calibration\s+passed`:

- **`scripts/build_recurring_briefs.py`** (+11 lines, all in the dormant-branch block): zero matches. The new lines are an operator-facing comment ("Sol #7106 review (2026-09-19, REQUEST_CHANGES): a dormant run must not read subscriptions and must not print user-authored target/body text into workflow logs. Until the privacy/session/read-state repair lands, DORMANT means zero reads, zero decisions, zero writes, zero user text."), the rewritten `print(...)` dormant line, and the new `::notice` line. The comment attributes the verdict to Sol + cites the date + names the action ("the privacy/session/read-state repair lands") — the standard citation form.
- **`tests/test_recurring_briefs.py`** (+31 lines): zero matches. The new test uses `private = "PRIVATE THESIS: acquisition target 7f3a9c"` as a sentinel string and asserts it is absent from stdout; that is a leak-probe pattern, not a validated assertion. The four assertions (`"DORMANT" in out`, `"no subscription read" in out`, `private not in out`, `"planned" not in out`) are each a measured-fact assertion about log contents.
- **PR body claims**: matches only on workflow-state vocabulary (`GREEN at this head`, `RED at origin/main`, `61 passed`, `states_captured: 8/8`, `BUILT_NOT_PROVEN`). These are measured test facts (`pytest` results) and the calibrated `BUILT_NOT_PROVEN` ledger status — the seat deliberately did NOT promote MO-PAID-032 to `BUILT_AND_PROVEN` or `VALIDATED`, even though the hotfix lands on `main` and the test passes locally. **The honest receipt.**
- **PR title and body deliberately avoid `validated/已验证` language.** The title says "DORMANT = no subscription read, no run(), no user text in workflow logs" — a measured-contract claim, not a model claim. The body uses "the leak path is on `main`" (present-state fact), "the hotfix removes the exposure; it is not the repair" (scope discipline), and "Live proof stays ABSENT until the next dormant workflow run's log is read back; MO-PAID-032 stays **BUILT_NOT_PROVEN**" (calibrated status, with explicit acknowledgement that the hotfix is not proof). **This is exactly the calibrated receipt form CLAUDE.md §Epistemics requires: "validated" is CI-enforced; the seat used `BUILT_NOT_PROVEN` instead.**

**Verdict:** PASS. Zero new validated claims. The PR's only calibrated-claim posture is `BUILT_NOT_PROVEN` for MO-PAID-032, with the receipt that the hotfix removes the leak path and live proof awaits the next dormant run — the doctrine-correct disposition for a hotfix that ships without live evidence. No promotion occurred; the ledger row is unchanged.

## Other compliance notes (informational, not failures)

- **Hotfix posture — explicit, not silent.** The body opens with "**Why (urgent, before today's 14:00Z weekly and 22:30Z daily runs)**", names the seat's previous error ("the seat merged #7106 over that verdict — the seat's error, acknowledged on #7106"), and bounds the scope ("This hotfix removes the exposure; it is not the repair"). Repair items are enumerated by objective (aggregate-only diagnostics + leak-proof test on a distinctive private target/body when enabled; `lib.nyse_calendar.is_session(run_date)` gate; typed subscription-read state). `RECURRING_BRIEFS_ENABLE` stays unset; MO-PAID-032 stays BUILT_NOT_PROVEN. This is the exact pattern CLAUDE.md §Epistemics expects — a hotfix that lands on `main` without promoting ledger rows it cannot back with live evidence.
- **`::notice` annotation form correctness.** Per CLAUDE.md "GitHub annotations must START the line" + `tests/test_gh_annotation_line_start.py`: the annotation is a bare `print("::notice ...", flush=True)` — no `print` wrapped in `log.info`/`log.warning` (no logger-prefix defect), no logger in scope of `scripts/build_recurring_briefs.py`'s call (the module is engine-scoped, not FastAPI/admin-scoped — the carve-out list in `test_gh_annotation_line_start.py` does NOT exempt this module, so the bare-print discipline applies and is honored). The new line passes both shape tests (bare-start, `flush=True`).
- **Sparse-worktree posture.** `scripts/**` and `tests/**` are NOT in the `config/sparse_worktree.json` omit list, so the audit session can read these files in its sparse tree without an opt-in. (The validator's allowlist load failure on `data/regime/validated_claims_allowlist.json` is a separate, documented sparse-tree behaviour — same disposition as the prior audits.)
- **Test discipline.** The new test follows the existing `tests/test_recurring_briefs.py` pattern: a `_must_not_*` helper raising `AssertionError` (named "dormant run must not call run()" — the exact inverse-assertion shape that proves the dormant path skips the call), a `monkeypatch.setattr(rb, "read_subscriptions", _must_not_read)` / `monkeypatch.setattr(rb, "run", _must_not_run)` pair (both doors monkeypatched, not just one), and a private-string sentinel (distinctive enough to fail the test if the leak recurs under any refactor). Both cadences (`daily_after_us_close`, `weekly_saturday`) are tested. **Test discipline is conservative — both read-subscriptions and run() are sealed off, not just one.**
- **Merge-queue-pilot / Vercel reds (informational, same shape as prior audits).** Per the body's "Merge: green-modulo-main-red (main-red = `stock-dashboard-first-frame`, `self-mod-fence` until #7407)" — the two reds are pre-existing main reds closed by #7407 and not introduced or aggravated by this hotfix. The sweep cycle will clear them on the next merge.
- **Ledger effect.** None — hotfix removes a leak path; it does not promote MO-PAID-032. The repair round on #7413 will carry the promotion decision; that is the correct sequencing for a calibrated lane.
- **CI-vacuous-pass caveat.** N/A — no PNG-only diff, no theme bytes, no design-system gate run; the `check_*` validators are all trivially empty against this diff (none needed running). No vacuous pass to disclose.

## Overall verdict

**PASS** — a clean, tightly-scoped, candidly-disclosed CLI hotfix that removes a documented privacy leak path on `main` without overclaiming. The hotfix posture is exemplary: the seat names its own prior merge-over-red error, bounds the change scope (removes the exposure, is NOT the repair), defers a stale docstring to the repair round to avoid restarting running packs, and leaves MO-PAID-032 at `BUILT_NOT_PROVEN` instead of promoting it on a hotfix that has no live proof. The plain-language discipline is honored (the dormant-line triple-negative is more honest than what it replaced; the `::notice` annotation is in canonical GitHub form; no banned vocab leaks into the diff). The theme dimension is N/A (no design bytes touched). The validated-claims dimension is clean (zero new affirmative claims; the only calibrated-claim posture is `BUILT_NOT_PROVEN`, which is the doctrine-correct receipt).

The audit dimensions read as follows: plain-language compliance on the only user-facing-shaped prose the hotfix writes (workflow log line + `::notice` annotation + a private-string leak probe); theme handling trivially vacuous (no design bytes); validated-claims discipline preserved (no promotion, calibrated status held, ledger row unchanged). The seat's choice to disclose the Grok minor inline rather than amend it pre-merge — and to type the deferred-amendment path into the body — is the exact compliance-positive pattern that the prior audits' findings flagged as missing on #7106.

No audit dimension blocks `SHIPPED / LIVE`; this PR IS the live-deployment leg of the hotfix (it removes the exposure on `main` ahead of the next scheduled runs), and its ledger move (no move — `BUILT_NOT_PROVEN` held) is the honest next action the seat owes. The repair round on #7413 carries the promotion decision and the stale-docstring fix; that lane is the right place for any live-proof receipt that survives a 14:00Z weekly or 22:30Z daily dormant run.

Co-Authored-By: Claude Code <noreply@anthropic.com>