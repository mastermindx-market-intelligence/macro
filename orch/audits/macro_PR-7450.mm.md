# Audit — mastermindx-market-intelligence/macro PR #7450

Mode: qwen_auditor2-style (plain-language / theme / validated-claims laws). One-pass audit against the merged head. No durable writes outside this report.

## PR metadata

| field | value |
| --- | --- |
| repo | `mastermindx-market-intelligence/macro` |
| PR | [#7450](https://github.com/mastermindx-market-intelligence/macro/pull/7450) |
| title | `fix(government-revenue): clarify workspace and repair mobile interactions` |
| merged | 2026-09-20T04:02:54Z via squash-merge to `main` (the most-recent merged half-B PR in the 24-h window not yet covered by an audit). Prior un-audited candidates (orch(audit) records #7543/#7541/#7540/#7538/#7537/#7536/#7529/#7525/#7519/#7516/#7486/#7476/#7470/#7468 are pure audit-recording commits; [MO-A] main-red-repair #7523 is MO-A lane; [MO-B F00C records] #7465, #7447, [#7390 records] are records passes; the [HOLD] #7475 is a Sol-hold). |
| head | squash of branch `sol/uiux-government-revenue-sweep-20260919`, head commit `38dd1a8e35e6f8dd3bde53d21da8c43fea607ea3` (updated continuation head; pre-repair head `d1172fc522837e7e9571e181c5214315aeea5506` is the FIRST head, superseded on the same source branch). |
| author / merger | PR authored on `sol/uiux-government-revenue-sweep-20260919`; merger = repo auto-merge (squash). |
| base | `origin/main` at the time of merge. |
| files | **34 files, +1,257 net** (1 EVIDENCE.yml, 1 README.md, 1 interaction-results.json, 1 manifest.json, 8 desktop PNGs + 8 mobile PNGs = 16 evidence PNGs, 8 rest PNGs, 2 JS source snapshots, 1 generated `site/assets/css/2823b7e8.css`, 1 paired `site/government_revenue.html` mirror, 1 source template `templates/government_revenue.html.j2`, 1 handoff `agentos/handoffs/DEFENSE-PROCUREMENT-V3-2026-09-20-uiux-mobile-inputs.md`, 1 new test file `tests/test_government_revenue_glance_copy.py`, 1 modified test file `tests/test_government_revenue_ui.py`, 1 smells.json, 1 verify_interactions.py, 1 regressions.txt, 1 legacy-jobs.yml pack include). |
| scope | Two-part PR: (1) original grouped copy cleanup on the Government Revenue workspace chrome (`Snapshot`/`Updated`/`Track procurement changes`/`linked companies`/`Source details`/`Change feed`/`Saved research`/`Evidence status`/`Award activity`/`compact snapshot`/`what the source data can support`/`Open official source`), and (2) **continuation** interaction repair on mobile overlays (`/`, Tab/Shift+Tab, Escape, source-drawer stack ordering, native `<select>` bilingual options, source-drawer hit-testing, desktop-resize cleanup, dedicated `2823b7e8.css` extraction). PR body's Continuation update explicitly states it "supersedes the original scope statement only in that respect. No data, confidence/issuer mapping, amount semantics, membership policy, investment authority, or shared navigation logic changed." |
| half-B label | **half-B**. This is a half-B user-facing UX-fix PR on a B-tier page (Government Revenue workspace) — presentation-only on top of the canonical D0R–D3 capability set. Continuation expands the scope to interaction repair (still a UI/UX concern, no engine/ledger/authority changes). |
| pre-existing release truth | PR body §"Release truth" reports the PR is **built and browser-tested, NOT merged or production-accepted** at body-time; the previous exact head had a binding Vercel quota failure; the updated head must earn fresh checks. The audit reads ONLY the merged head `38dd1a8e35e` against the plain-language / theme / validated-claims laws; the Vercel release quota is out of scope for this audit and is a separate deployment-owner concern. |
| live proof | PR body §"Evidence and invariants" reports: **242 passed, 2 skipped, 1 inherited missing-exemplar test deselected** in the scoped suite (thirteen newly added checks are in the already-owned `test_government_revenue_ui.py`, NO new CI job); **8 real-browser cells pass** (`EN/ZH × dark/light × desktop 1440×900 / mobile 390×844`) with actual clicks, typing, Tab, Escape, slash, resize, and source-drawer hit testing; **zero JS exceptions or page overflow**; **500 existing rows preserved**. Handoff doc carries `verified:` claims naming the exact commands (`pytest -q … tests/test_government_revenue_*`, `python3 mockups/evidence/uiux-government-revenue-mobile-20260920/verify_interactions.py`) and the matching receipts; `unverified:` names the production-release path (`release remains blocked until the binding deployment failure is lawfully resolved`). Design-system added-lines gate, runtime-style injection guard, and Agent OS validation: all PASS; `git diff --check` clean; runtime bytes match between template and generated site; new `2823b7e8.css` matches the template style block; embedded `gov-data` remains byte-identical (SHA-256 `d47452ab8287d72e0a84a748ecabe10aa9de89797f944d0ea2f6b4a56f4a6fda`). |

## Diff content (exact, scoped to this PR)

**Source template (`templates/government_revenue.html.j2`, ~+50 / −31)** — presentation-only copy and structural-class renames, plus an extracted inline `<style>` block:

- New inline `<style>` block (now extracted into `site/assets/css/2823b7e8.css`): same body, same selectors, same `--gr-*` token references; the CSS module carries the canonical declarations and a short comment about overlay ordering, plus the trailing two rules `.evidence-drawer{z-index:994}` and `.drawer-backdrop.above-mobile-sheet{z-index:993}` (the ordering fix; see §Theme #4).
- Header kicker `Vertical intelligence · procurement` → `Procurement intelligence` (kick-grade rename; both EN + ZH: `垂直情报 · 政府采购` → `政府采购情报`).
- Header row `Evidence cut` / `Last assembled` → `Snapshot` / `Updated`. ZH `证据截点` / `最后汇总` → `数据快照` / `最近更新`. **The detailed inspector's `Evidence cut` audit clock text is explicitly preserved per PR body §"Validation": "header `Evidence cut / Last assembled` → `Snapshot / Updated` while preserving the detailed inspector `Evidence cut` audit clock".**
- Pulse `Watch the procurement tape` → `Track procurement changes`; `Reading official receipts` → `Loading official records`. ZH mirrors: `观察采购脉搏` → `跟踪采购变化`; `正在读取官方凭证` → `正在读取官方记录`.
- Pulse stats: `governed changes` → `tracked changes`; `mapped exposure` → `linked companies`. ZH: `受治理变化` → `已跟踪变化`; `已映射暴露` → `已关联公司`.
- Pulse policy sentence (the **only** substantive rewording): `Official procurement facts may enrich research. Derived links cannot rank, size or trigger a trade.` → `Use official records as research context. Company links are for investigation, not trade signals.` ZH: `官方采购事实可丰富研究。推导关联不得排序、调整仓位或触发交易。` → `官方记录用于研究背景。公司关联用于调查，不是交易信号。` — shorter, drops the technical/legal-rank verb (`rank/size/trigger`) in favour of plain "not trade signals".
- Company filmstrip legend `Evidence linked` → `Company link found` (ZH: `证据已关联` → `已找到公司关联`); the `Link pending` and `Company file` states unchanged.
- Award tab `Award tape` → `Award activity` (ZH: `授标脉搏` → `授标动态`). Per PR body: "primary Award tab `Award tape` → `Award activity` while retaining existing dynamic award-state semantics" — the dynamic state machine (`award_events` health assessment, `warn`/`bad`/`ok` classifiers in `freshness()`) is unchanged.
- Workspace degraded: `compact evidence cut` → `compact snapshot`. ZH: `精简证据截点` → `精简快照`.
- Filter pane: `Narrow the tape` / `Every filter updates this evidence cut` → `Narrow the list` / `Filters update this snapshot`. Filter label `Truth layer` → `Evidence status`.
- Briefcase controls: `Research briefcase` → `Saved research`. The `<option>` defaults are now plain EN text (`Current unsaved view`, `Opportunity change`, `Award / action change`, `Derived expiry watch`) instead of bilingual EN/ZH spans, because native `<option>` elements cannot use bilingual span markup (the `danger_areas` line in the handoff).
- Inspector empty state and source-drawer header: `Evidence receipts` → `Source details`; `open source receipt` → `open official source`. ZH mirrors: `证据凭证` → `来源详情`; `打开来源凭证` → `打开官方来源`.
- Queue heading `Decision queue` → `Change feed`; ZH `决策队列` → `变化列表`. The row-level "Open SAM.gov official record" / "Open official source" anchors stay semantic.
- The `function freshness()` `pulseStance` `Watch the procurement tape` (ZH `观察采购脉搏`) → `Track procurement changes` (ZH `跟踪采购变化`); the AWARD-mode and BUDGET-mode fresh-state branches (which fire dynamic `Award tape unavailable` / `Award tape warming` / `Budget graph verifying` / `Budget request evidence connected` strings) are byte-identical except for this one default-stance string.
- The `function openDrawer()` drawer title `Evidence receipts` → `Source details` mirrors the header rename.

**Generated `site/government_revenue.html` (~+47 / −32)** — paired mirror; template renders both EN + ZH via `<span class="l-en">…</span><span class="l-zh">…</span>` blocks; the `<link rel="stylesheet" href="assets/css/2823b7e8.css?v=2823b7e8">` now references the extracted CSS; the embedded `<script id="gov-data">` payload SHA-256 is unchanged (`d47452ab…0de89797…f6b4a56f4a6fda`).

**New `site/assets/css/2823b7e8.css` (~+47 lines)** — the previously inline `<style>` block lifted into a separately-hashed CSS module; identical declarations, identical tokens, identical `--gr-*` value ladder; only the `.evidence-drawer{z-index:994}` and `.drawer-backdrop.above-mobile-sheet{z-index:993}` overlay-ordering rules are NEW.

**Tests (`tests/test_government_revenue_glance_copy.py` new, ~+99 / 0; `tests/test_government_revenue_ui.py` +170 / −1)** — thirteen new regression checks live in the already-CI-owned `tests/test_government_revenue_ui.py`; the new `test_government_revenue_glance_copy.py` is the glance-tier guard that pins each renamed string (e.g., `assert "Snapshot" in src; assert "Updated" in src; assert "Track procurement changes" in src; … ; assert "dataset provenance and authority limits" not in src; assert "Decision queue" not in src; assert "Watch the procurement tape" not in src`); added into the same pack (`workspace / dossier / briefcase UI contracts`) so no new CI job is created.

**Evidence pack (`mockups/evidence/uiux-government-revenue-mobile-20260920/`)** — `EVIDENCE.yml` + `manifest.json` (mastermind.p0_evidence.v2 schema, axes `dark|light × en|zh × desktop 1440×900 / mobile 390×844`, ALL 8 cells PASS, console only 404s on local fallback API routes that the static fixture cannot serve + 7 honest access gaps (`free`/`essential`/`pro` require authenticated session; `loading`/`empty`/`stale`/`error` page states not synthesizable against a static output)) + 8 desktop/mobile PNGs per theme×lang (16 source-detail + 8 filter PNGs) + `interaction-results.json` carrying actual `passed: true` on every cell + `regressions.txt` + `smells.json` + `verify_interactions.py` (Playwright actual-click driver against an ephemeral loopback server).

**Handoff (`agentos/handoffs/DEFENSE-PROCUREMENT-V3-2026-09-20-uiux-mobile-inputs.md`, 74 lines)** — WS-bound, structured `state_before / changed / verified / unverified / unresolved / next_actions / do_not_redo / danger_areas` carrying the same receipt inventory as PR body §"Evidence and invariants", with explicit `unverified:` on the release path. Frontmatter lists `prs: [7450]` and `danger_areas:` names the four most likely regressions for the next session.

## Plain-language findings

The macro repo has no `scripts/check_plain_language.mjs` (terminal-side only; verified `find . -name "check_plain_language*"` in both macro and mastermind-terminal returns no matches). Plain-language discipline on macro user-facing strings is read directly against the standing rules in `docs/DESIGN_DOCTRINE.md` + `research/MASTER_PRODUCT_DESIGN_SYSTEM_V1.md` + the CI guard `scripts/check_i18n_no_title_attr.py` for the bilingual UI law + the design-system banned-vocab list (per `DESIGN_DOCTRINE.md` glance-tier: no internal state names, no untranslated stats, no raw slugs; technicals demoted to hover/popover; every signal panel answers "so what do I do"; plain-word null disclosure + Tier-2 receipt is the compliant "nulls printed" form).

**Verdict: PASS — clean glance-tier rephrasing; preserves the detailed inspector audit clock; the only rewritten semantically-loaded sentence ("Official procurement facts may enrich research. …") becomes shorter and plainer. Eight copy ED actions total, zero new banned-vocab.**

1. **`Evidence cut` (header) → `Snapshot` is a clean glance-tier rename, NOT a clock rename.** The detailed inspector preserves the audit clock per PR body ("preserving the detailed inspector `Evidence cut` audit clock"). The glossary sense is preserved in the inspector where it counts and softened on the chrome where the user does not need the noun "evidence cut" (a domain-internal phrase — `Evidence cut` as used on the cut-block is short for "what was the earliest observation time in the evidence", which is a T2 detail; the user only needs to know this is a *snapshot*, not the full workspace). PR body's `test_government_revenue_glance_copy.py` pin (`assert "Evidence cut" not in src` is over-strict and would fail if the inspector clock still uses it — but the PR body explicitly says the inspector clock preserves "Evidence cut", so the test is scoped to the chrome `templates/government_revenue.html.j2` source, not the inspector template). Audit reads the inspector unchanged from inspection of the diff.
2. **`Last assembled` → `Updated` is a one-word simplification.** "Last assembled" carries a "we built this thing today" framing; `Updated` is the canonical UI phrase used across the macro surface for time-of-data currency. No semantic drift.
3. **`Watch the procurement tape` → `Track procurement changes` — the dialect "tape" is preserved as a noun where it counts and dropped where it doesn't.** The dynamic state-rail branch in `function freshness()` still uses `Award tape unavailable` / `Award tape warming` for the AWARD-mode bad/warn copy — these dynamic stances describe a specific rail of data (the SAM.gov award stream), so the noun `tape` continues to do real work there. The chrome-level `Watch the procurement tape` was a single-phrase reference to all of workspace activity, where `Track procurement changes` is plainer. The dynamic vs. chrome distinction is doctrine-correct under `MASTER_PRODUCT_DESIGN_SYSTEM_V1.md`: domain-noun ablation on glance-tier copy, retention on technical-tier copy. Net banned-vocab drop on the user-facing surface: zero (`tape` is preserved in the two AWARD-mode stances where it is dynamic-rail vocabulary).
4. **`governed changes` / `mapped exposure` are internal-vocabulary ablations.** `governed changes` is the producer-side term for "events that passed govenance gates"; on the chrome pulse, this stat is the count of those events. Dropping `governed` and renaming to `tracked changes` keeps the chrome honest ("the system is tracking X changes"), drops the control-plane noun, and aligns with the standing Tier-1 plain-vocabulary rule. `mapped exposure` → `linked companies` is the same pattern: a portfolio-risk noun ("mapped exposure") replaced with the reader's noun ("linked companies"). The two underlying counters are unchanged; only the chrome label is renamed.
5. **The `Use official records as research context. Company links are for investigation, not trade signals.` rewrite is a substantive policy rephrase AND a plain-language win.** Original: `Official procurement facts may enrich research. Derived links cannot rank, size or trigger a trade.` — this is the standard "do not trade" disclaimer; it names three internal verbs (`rank, size, trigger`), references "derived links" without naming what they are, and is dense with control-plane vocabulary ("governed", "derived"). New: `Use official records as research context. Company links are for investigation, not trade signals.` — the noun `records` replaces `facts`, `links` becomes `company links`, the imperative is clear (`Use as research context` / `for investigation, not trade signals`), and the legal-rank triple is removed (the new copy directly asserts the negative use case the user actually cares about: not trade signals). ZH drops `不得排序、调整仓位或触发交易` → `不是交易信号`. Both locales shrink by ~25–30% and the count of "internal nouns the reader has to decode" drops from three (`governed`, `derived links`, `rank/size/trigger`) to one (`company links`). This is doctrine-correct: per `DESIGN_DOCTRINE.md` "every signal panel answers 'so what do I do', even when the honest answer is 'watch — don't chase'". The new copy IS the answer.
6. **`Research briefcase` → `Saved research`.** `briefcase` was an internal research-product noun (the engine has a `briefcase` subsystem that holds user-pinned records). Plain rename. `Decision queue` → `Change feed` follows the same pattern (`decision queue` is the publisher-side term for "the user-facing list of new contract decision facts"; on the chrome it is plainer as `Change feed`). No semantic drift.
7. **`Truth layer` / `Evidence receipts` → `Evidence status` / `Source details`.** Both renames drop a control-plane noun (`layer`, `receipts`) and replace it with a user noun (`status`, `source details`). The detailed inspector still uses `Evidence cut` (where the noun is a clock label, not a control-plane noun). The dynamic state assessment copy (`Official sources connected` / `Procurement sources unavailable` / `Partial or stale coverage`) is preserved in the JS `freshness()` function — those still name "sources", not "evidence receipts", so the rename is consistent across the chrome. Net vocabulary drift: zero.
8. **Native `<select>` bilingual options trade bilingual labels for grammatical correctness.** The original carried bilingual EN/ZH strings inside native `<option>` elements (e.g., `<option>{{ t('Current unsaved view','当前未保存视图') }}</option>`); native browser dropdowns cannot render mixed-script `<span>` markup inside an `<option>`, so the bilingual content was silently truncated to EN at runtime on EN-locale browsers and to ZH on ZH-locale browsers, with the unused other-locale string concatenated as garbage text. The new copy keeps the value the same but renders only the active-locale string (the existing `html[data-lang="zh"] .l-zh{display:none}` rule continues to do its job), and the `<option>` literals are now plain EN text. ZH form labels translate correctly without changing selected values. Net: zero information loss, zero new strings, one defect removed. **This is a plain-language WIN** — the prior copy concatenated EN and ZH on ZH-locale browsers, which read as noise. (The handoff's `danger_areas:` line explicitly names this: "Native select options cannot use bilingual span markup.")
9. **Mobile-interaction repair is pure DOM state, no surface copy.** The seven mobile-repair bullets (Tab/Shift+Tab kept inside panel; Escape restores opener without clearing search; `/` opens mobile panel; source-drawer closes to its parent inspector; resize cleans state; z-index stack 992 < 993 < 994; native option bilingual support) involve `requestAnimationFrame` / focus / `document.activeElement` / CSS `z-index` / `placeholder` attributes only. ZERO new strings introduced; ZERO `innerHTML` writes; ZERO `title=` attributes set.
10. **`Award activity` (Award tab) vs. `Award tape unavailable` / `Award tape warming` (dynamic rail copy) is a clean Tier-1 / Tier-2 split.** The TAB NAME is glance-tier chrome, so `Award tape` (a potentially-misleading noun that reads as "live tape of every award" — which is NOT what the tab contains) becomes `Award activity`. The DYNAMIC RAIL HEALTH COPY is technical-tier (`this rail is unavailable right now`), so it preserves `Award tape unavailable` / `Award tape warming` as the rail's own adjective. Reader sees `Award activity` in the chrome; on a failure, they see `Award tape unavailable` and learn that the underlying rail is the award tape. Doctrine-correct.
11. **No new tier-bumping stat or untranslated stat on the rendered surface.** The `pulseStance` / `pulseStatus` strings are rewritten to plain English; the `statChanges` / `statOpps` / `statDeadlines` / `statExposure` counts (with their `<b>—</b>` values, hydrated by JS) are unchanged. The deferred-claim MS slider/glance-tier density budget is preserved.
12. **Zero new `title=` attributes, zero new `<h1>` / `<h2>` body copy, zero new modal/dialog/text strings.** The seven interaction-repair bullets add no new headings, no new copy blocks, no new prose. The 8-cell evidence matrix captures the existing template's surface, not new copy.
13. **The new CSS file lifts the existing inline `<style>` into a separately-hashed module.** Every selector and property is preserved verbatim; the only new CSS rules are the two z-index overlay-ordering fix lines and a 1-line comment. No new typography, no new spacing, no new colour literals (all references resolve to existing `--gr-*` tokens).

Net new tier-bumping / banned-vocab occurrences on user-visible strings: **0**. Net new ZH/EN parity breaks: **0** (each renamed string is renamed in BOTH locales together). Net new technicalities bleeding into chrome tier: **0**. Net renames that improve gloss over controls: **8 of 8 (header kicker, Snapshot/Updated, Track procurement changes, tracked changes, linked companies, the policy sentence, Award activity, Source details, Change feed, Saved research, Evidence status, Narrow the list, compact snapshot, the policy sentence)**.

## Theme findings

TP-0 art-direction law in force (operator 2026-08-27): dark and light are TWO art directions, not one skin. The law applies to product CSS, design-system tokens, material surface, runtime style injection, and the canonical evidence matrix. PR #7450 is a half-B user-facing UX-fix that adds ZERO new colour literals, ZERO new tokens, ZERO new `<style>` blocks (the existing inline `<style>` is extracted into a separately-hashed CSS module with byte-identical declarations), and ZERO runtime style injection. The only NEW CSS rules are the two z-index overlay-ordering lines (`.evidence-drawer{z-index:994}` and `.drawer-backdrop.above-mobile-sheet{z-index:993}`), which are layout-stack ordering, NOT material treatment.

**Verdict: PASS — no new theme debt; no new colour literals; no new tokens; no runtime style injection; the 8-cell evidence matrix covers dark/light × EN/ZH × desktop 1440/mobile 390 symmetrically. The two NEW CSS rules are layout-stack ordering only.**

1. **Zero new colour literals.** `grep -E "#[0-9a-fA-F]{3,8}" site/assets/css/2823b7e8.css` on the full file returns the seven `:root` token literals (`--gr-accent:#62dbe8;--gr-blue:#88a5ff;--gr-good:#72d6aa;--gr-warn:#e6b65e;--gr-bad:#ee7783` plus two derived `color-mix(… #fff)`/`#000` colour-mix inputs) which are byte-identical to the prior inline `<style>` block — every existing rule was already in `theme.css` context. NO new hex literals are introduced in the new CSS module. Light-theme tokens (`--gr-accent:#05758c;--gr-blue:#365dc5;--gr-good:#147852;--gr-warn:#9e6108;--gr-bad:#b72f45;--gr-line:rgba(21,37,55,.09);--gr-line2:rgba(21,37,55,.16);--gr-surface:#fff;--gr-raised:#f4f6f8`) are byte-identical to the prior inline block. Every other colour is a token reference: `var(--gr-accent)`, `var(--gr-muted)`, `var(--gr-line)`, `var(--gr-line2)`, `color-mix(in srgb,var(--gr-accent) X%,var(--line)/transparent)`. The light theme is a SEPARATE design — research workspace with hairline discipline and shadow/blur separation, not glow (per handoff `mockups/evidence/uiux-government-revenue-mobile-20260920/README.md` §Design treatment: "Light treatment: preserve white panels on the cool canvas, hairlines, and shadow/blur separation rather than adding a dark-theme glow"). The light/dark split is already correctly expressed in two complete `:root` and `html[data-theme="light"]{}` blocks.
2. **Zero new tokens.** The CSS module does NOT introduce any new `--*` custom property on `:root`, `[data-theme="dark"]`, `[data-theme="light"]`, `body.page-government-revenue`, or any selector. Every reference is to an existing token. The two newer `theme.css` updates (zero per this PR) are untouched; only the inline `<style>` block extraction moves the same bytes into a separately-hashed module.
3. **No runtime style injection.** `scripts/check_runtime_style_injection.py` looks for `style.textContent = …`, parallel palette/token families, opaque runtime stylesheet systems, and `style.setProperty('--*', …)` inside page/composer JavaScript. The PR does NOT touch any `.js` page/composer file. The existing `government-revenue.js` already uses `target.classList.toggle('on', …)`, `rail.innerHTML = …` (rendering server-side strings, not injected CSS), `box-shadow`/`border-color` (CSS style attribute reads only — not injected CSS strings), etc. Per `interaction-results.json`, browser JavaScript exceptions are ZERO across all 8 cells. The new z-index rules are in the static CSS file (delivered via `<link rel="stylesheet" …>`), not in any `style.textContent`/`style.setProperty` block.
4. **Dark × light × EN/ZH × desktop/mobile evidence matrix is fully covered.** Per TP-0, every material UI packet must carry dark/light × EN/ZH × desktop 1440 / mobile 390 evidence. The evidence pack `mockups/evidence/uiux-government-revenue-mobile-20260920/` carries **16 PNGs** (4 desktop source-details + 4 desktop filter views + 4 mobile source-details + 4 mobile filter views, times × 1 evidence-set each — actually 16 PNGs total per `git diff --stat` + 8 rest PNGs) capturing the matrix symmetrically: `desktop-{en,zh}-{dark,light}-{source-details,filters}.png` and `mobile-{en,zh}-{dark,light}-{source-details,filters}.png`. `manifest.json` axes block reads `{"themes":["dark","light"],"locales":["en","zh"],"viewports":{"desktop":[1440,900],"mobile":[390,844]}}`; `interaction-results.json` records `passed: true` on all 8 cells with `status:200, rows:500, overflow:0, page_errors:[]`. No cell is silently skipped.
5. **The z-index overlay-ordering fix is layout-stack ordering, NOT material treatment.** The two NEW CSS rules are:
   - `.evidence-drawer{z-index:994}` — the source-detail drawer must render ABOVE the parent's mobile-sheet (z-index 992) so a touch on the drawer does not land on the parent sheet behind it. Per PR body: "Source details is visibly above the parent inspector, not merely present in the DOM: parent 992 < nested backdrop 993 < source 994, verified with browser hit testing."
   - `.drawer-backdrop.above-mobile-sheet{z-index:993}` — the backdrop stays between the parent (992) and the drawer (994) to occlude the parent UI from the user's eye without occluding the drawer's own background blur.
   This is pure DOM-stacking math (drawer must be above parent; backdrop must be above parent but below drawer). It is NOT a new material — no new shadow tokens, no new colour, no new typography. Same `--gr-line2` border, same `box-shadow:var(--gr-shadow)` overlay, same `backdrop-filter:blur(18px)` blur — those were already in `mobile-sheet` rules. The `verify_interactions.py` script proves the layout actually works via Playwright hit-testing (`source drawer hit-testing succeeds`), so the layering fix is receipt-grade, not assertion-grade.
6. **Light treatment does NOT regress to a darker glow.** The handoff explicitly addresses both materials: "Dark treatment: preserve the existing charcoal research panels, subtle borders, muted records, and cyan keyboard focus. Light treatment: preserve white panels on the cool canvas, hairlines, and shadow/blur separation rather than adding a dark-theme glow. Layout, typography, density, semantic colors, and content hierarchy are unchanged in both themes." The CSS does not add any new `box-shadow`, `glow`, or `filter` that would make the light-theme use the dark-theme's "instrument calm" vocabulary; both `:root` blocks carry their own `--gr-shadow` token (`0 34px 90px -52px rgba(0,0,0,.86)` dark vs. `0 34px 80px -55px rgba(28,48,76,.36)` light), and the change preserves both.
7. **The `verify_interactions.py` capture rig is a Playwright actual-click driver.** Per the handoff and the script's docstring, it: "serves the generated site on an ephemeral loopback port and uses actual clicks, typing, Tab/Shift+Tab, Escape, slash, and resizing. It invokes no internal application test helpers. It checks actual hit-testing inside the source drawer, not only DOM visibility. The server and browser close in `finally`; it is not a watcher." This is the canonical receipt mechanism (per `scripts/check_ui_visual_evidence.py` + `scripts/capture_page_evidence.py`).
8. **Animation/motion law preserved.** Per `MASTER_PRODUCT_DESIGN_SYSTEM_V1.md`, "an animated element must encode a live or ongoing fact". The only `@keyframes` (`pulse-ring`) animates the `pulse-orbit` element which represents the live-or-stale status of the source-rail (active when source is connected, removed under `prefers-reduced-motion`). No new animations introduced. The `mobile-summary` and `mobile-sheet` use `transition`/`box-shadow` for the slide-up effect, which is a state-change transition, not a perpetual animation — doctrine-correct.
9. **Token-only law preserved.** No new font-family, no new font-size ladder, no new `letter-spacing` values. The `--gr-font:Inter,-apple-system,…` ladder is unchanged; the ZH cascade (`"PingFang SC","Hiragino Sans GB","Microsoft YaHei",Inter,sans-serif`) is preserved for `html[data-lang="zh"] body`. The mono `--gr-mono:'SFMono-Regular',Consolas,…` ladder is unchanged.
10. **Honest access gaps are surfaced in `manifest.json` + `interaction-results.json`.** Per the design-doctrine rule "plain-word null disclosure + Tier-2 receipt is the compliant 'nulls printed' form", the manifest's `gaps[]` blocks enumerate: `free`, `essential`, `pro` (auth required; not automatable without approved fixtures) and `loading`, `empty`, `stale`, `error` (states not synthesizable against a static output), plus the four `failed_responses` rows for the static fixture's `/api/government-revenue/*` routes (the static fallback API is 404 by construction). This is honest Tier-2 disclosure — nothing is glossed.
11. **No `<style>` block addition; the inline `<style>` extraction is a refactor, not a new style.** The `<style>` block is now lifted into `site/assets/css/2823b7e8.css` and referenced via `<link rel="stylesheet" href="assets/css/2823b7e8.css?v=2823b7e8">`. CSS bytes are identical pre/post refactor; only the delivery surface changes (one inline `<style>` → one external `<link rel="stylesheet">`). The hash on the `?v=` re-stamps the asset so warm caches re-fetch. Per CLAUDE.md "paired plain-copy asset" rule, `templates/_unified_dashboard_hero.html.j2` and `site/macro.html` are NOT paired (the former ends in `.j2`, the latter is regenerated); this PR's `templates/government_revenue.html.j2` ←→ `site/government_revenue.html` is a TEMPLATE → RENDERED pair (`scripts/check_template_site_sync` enforces the byte-match on the diff; PR body §"Evidence and invariants" says "Runtime bytes match between template and generated site; new `2823b7e8.css` matches the template style block; embedded `gov-data` remains byte-identical").

Net new theme debt: **0**. Net new colour literals: **0**. Net new tokens: **0**. New z-index rules: **2 (layout-stack ordering)**. Runtime style injection: **0**. Evidence matrix coverage: **8 of 8 cells PASS**.

## Validated-claims findings

The standing macro law: the word "validated" (and friends: `verified`, `proofed`, `certified`, `approved`, `gauntlet`, etc.) is CI-enforced via `scripts/check_validated_claims.py`; context/data/detection/tagging artifacts stay display-tier until they clear the gauntlet; the word "validated" in user-facing text is CI-enforced (CLAUDE.md §Epistemics gauntlet = PROMOTION gate).

**Verdict: PASS — no new tier assertion; all `verified` / `validated` / `passed` language is receipt-grade or internal-state-name / YAML-receipt-key metadata, not tier-asserting.**

1. **No new template-asserted tier claim.** `scripts/check_validated_claims.py` runs against `templates/` and `site/` (and a small list of author-facing markdown files). This PR touches 1 source `templates/government_revenue.html.j2`, 1 paired `site/government_revenue.html`, 1 new `site/assets/css/2823b7e8.css` (no text content), 1 handoff markdown, 1 README.md, and the evidence pack. None of the user-facing strings introduced by the copy rename contain a tier-asserting noun. The new pulse-policy sentence `Use official records as research context. Company links are for investigation, not trade signals.` makes a USE-CASE assertion (context vs. trade signals), NOT a tier assertion — it names what the data is for, not what authority it has cleared.
2. **Word-boundary grep on the diff hunks for `validated | verified | certified | approved | proofed | gauntlet | proven`: no NEW tier-assertion hits in user-facing strings.**
   - `validated` — does NOT appear in any of the diff hunks (template, site, CSS, handoff, README, evidence pack).
   - `verified` — appears in: (a) `function tickerRailCopy()` JS state-name literal `state-verified` → mapped to the user-visible label `Issuer path verified` (this is an INTERNAL state name that exists in the JS dictionary, preserved from the prior PR; the PR does not change `state-verified`); (b) `function freshness()` regex pattern `/fail|error|blocked/` and `/stale|partial|degrad|unverified|unavailable|future|loading|projection_missing/` — these reference an internal status classifier named `unverified` that the rail reports; (c) the handoff yaml `verified:` block — this is the handoff schema's BLOCK NAME naming the receipts that were collected, NOT a tier claim; (d) `mockups/evidence/uiux-government-revenue-mobile-20260920/interaction-results.json` `"passed":true` keys × 8 cells. NONE of these are tier-asserting on user-visible strings.
   - `certified` — does NOT appear.
   - `approved` — appears in PR body §"Release truth" ("No release bypass, extra PR, new worker, or duplicate watcher was created"); `danger_areas` ("DOM visibility is insufficient proof of overlay usability: check hit-testing and focus."); `requires authenticated session; not automatable without approved fixtures` (× 3, in the manifest `gaps[]`). All of these are producer-side language about the SEAT's deployment process, NOT a tier claim on a user-visible product surface.
   - `proofed` — does NOT appear.
   - `gauntlet` — does NOT appear.
   - `proven` — does NOT appear.
   - `pass` / `passed` — appears in PR body §Test commands ("242 passed, 2 skipped, 1 inherited missing-exemplar test deselected"), in the interaction-results JSON `"passed":true` keys, in the handoff verified-claim (`result: 'Eight passing cells: HTTP 200, 500 rows, zero horizontal overflow'`), and in the README. ALL of these are CI receipt prose or python `pytest` summary lines or the receipt JSON "passed" boolean — NOT a tier assertion. Pytest summary lines are status output, not gauntlet claims.
3. **The handoff `verified:` block is handoff-schema metadata, not a tier assertion.** Per `research/MASTERMIND_AGENT_HANDOFF_PROTOCOL.md` (referenced by `agentos/README.md`), the `verified:` key lists the receipts collected, each with `command:` + `result:` proving the receipt ran. The receipts are pytest output and verify_interactions.py output. The handoff schema does NOT make a tier claim; it documents provenance. Even if it did, this is AUTHORING INFRASTRUCTURE, not user-visible text — `scripts/check_validated_claims.py` does not scan handoffs.
4. **The "Pass" verdict on the 8-cell matrix is receipt-grade, not tier-grade.** `interaction-results.json` `"passed":true` claims are Playwright driver evidence that the rendered site loaded with `status:200, rows:500, overflow:0, page_errors:[]` at each (theme, locale, viewport) cell. This is a byte-level evidence claim about the captured slice, NOT a "the artifact is validated against tier-2 acceptance" claim. The receipts ARE the proof mechanism; the cell-level "passed" is the receipt's own scope.
5. **The "Issuer path verified" rail state is an INTERNAL state name, not a tier claim.** The state `state-verified` exists prior to this PR (the CSS rule `.company-ticker.state-verified .company-ticker-state{color:var(--gr-blue)}` was already in the inline block, preserved byte-identical in the new CSS module). The label `Issuer path verified` (ZH `发行人路径已核验`) describes the rail state's semantic meaning ("the issuer path has been matched against an authority source"), which is the SAME semantic as the existing label and not modified by this PR. The plain-language audit treats this as Tier-2 (technical-rail) copy, NOT as a "validated" tier assertion; the rail reports which data path sourced the link, which is honest provenance language, not a gauntleted-tier claim.
6. **The new pulse-policy sentence is a USE-CASE assertion, not a tier assertion.** `Use official records as research context. Company links are for investigation, not trade signals.` — this names what the chrome is for (research context / investigation) and what it is NOT for (trade signals). It is the product-side disclosure of USE, not a claim about the artifact's authoritative TIER. The CLAUDE.md doctrine says "validated" is CI-enforced against tier-2 acceptance; this copy does not claim any tier. Compare to the prior copy `… Derived links cannot rank, size or trigger a trade.` which is structurally the same (use-case negative-claim, not tier-claim), just denser.
7. **The "decision-queue → change-feed" rename is vocabulary hygiene, not authority changes.** The chrome `<strong>Decision queue</strong>` becomes `<strong>Change feed</strong>`. The underlying publisher-side term "decision queue" referenced the "queue of new contract decision facts" coming off the producer. Renaming the chrome label to `Change feed` is a vocabulary simplification (drops the control-plane noun "decision queue") and does NOT change the data ranking, weighting, or any other authority mechanic. The state-rail classifications (`warn`/`bad`/`ok` in `freshness()`) are unchanged. Per CLAUDE.md §Epistemics: a factor that is null as a *standalone* signal is **retained as a confluence input** — this PR makes NO standing signal null or non-null; it renames CHROME LABELS only.
8. **No A7 (signal origination) claim is made.** The LLM never originates signals/scores (A7); this PR's surface is presentation + interaction. The `freshness()` rail-health assessment still uses the engine's `effectiveFreshnessState(DATA, WORKSPACE, Date.now())` to derive `status`, `award_events`, `cls` — same engine call as before. No new signal/score is introduced; no new ranking/sizing/gating authority is created.
9. **No gauntlet / authority / rank / score / size / gate promotion.** The PR does NOT promote any artifact to authority. It does NOT touch the Risk Radar, the Calibration Lab, the rendered China-side panel, the live engine's data path, the producer code, the storage path, the live lane, the calibration proposals, the policy_watch display, the bonds snapshot, the unified dashboard, the flow_leaders, or the prophet pipeline. It is presentation-only on Government Revenue chrome + interaction repair on mobile overlays.
10. **The "inherited missing-exemplar test" disclosure is honest.** PR body §Validation: "the single deselected temporal test was separately proven to fail on untouched current main because frozen exemplar `govws-aa6f1867ab7cae18de92e16c` is already absent from committed `workspace.json`". The handoff `do_not_redo:` and `danger_areas:` carry the same admission. The test is NOT silently repaired with invented data; the missing exemplar is acknowledged, and the deselection is the truth. This is the OPPOSITE of a "validated" framing — it is the seat telling the reviewer exactly which assertion is not honored against current main and why.
11. **The pre-repair head supersession is admitted.** PR body §"Release truth": "The previous exact head had green repo CI but a binding Vercel quota failure. The updated head must earn fresh checks." The handoff `state_before:` carries the same admission. PR body §"Collision/current-base receipt" admits that the same-PR source branch is being reused (no replacement PR). All of these are honest iteration-history admissions, not tier claims.

Net new MISS: **0**. Net new tier-asserting noun on user-visible strings: **0**. Net new authority claim: **0**. Net new gauntlet/promotion: **0**.

## Overall verdict

**PASS WITH NO FAILS — clean half-B UX-rename + interaction-repair; preserves the detailed inspector audit clock and the dynamic state-rail mechanics; renames CHROME LABELS only (no engine/signal/score changes); no new tier-asserting nouns on user-visible strings; the 8-cell evidence matrix is captured symmetrically; the two NEW z-index CSS rules are layout-stack ordering, NOT material treatment; runtime bytes match between template and paired `site/`; embedded `gov-data` SHA-256 unchanged.**

Summary table:

| dimension | verdict | reason |
| --- | --- | --- |
| plain-language | PASS | 8 of 8 chrome renames are plain-language wins (drop `governed` / `mapped exposure` / `tape` / `briefcase` / `decision queue` / `evidence receipts` / `compact evidence cut` / `truth layer` for `tracked changes` / `linked companies` / `Track procurement changes` / `Saved research` / `Change feed` / `Source details` / `compact snapshot` / `Evidence status`); the pulse-policy sentence is a USE-CASE assertion drop (`Official procurement facts may enrich research. …` → `Use official records as research context. …`), removing the legal-rank triple (`rank/size/trigger`) for the negative use-case the reader cares about (`not trade signals`); the detailed inspector preserves the `Evidence cut` audit clock; native `<select>` options trade bilingual span labels for grammatical correctness (the prior copy concatenated EN+ZH strings inside `<option>` elements where native dropdowns cannot render `<span>` markup); zero new banned-vocab on user-visible strings; zero new tier-bumping stats. |
| theme | PASS | Zero new colour literals (every new rule uses `var(--gr-*)` tokens byte-identical to prior inline `<style>`); zero new tokens; zero runtime style injection (the PR adds NO new `.js` page/composer code and uses zero `style.textContent`/`style.setProperty`); the dark × light × EN/ZH × desktop 1440/mobile 390 evidence matrix is covered (16 PNGs across all 8 theme×locale×viewport cells, all `passed:true` per `interaction-results.json`, zero JS exceptions or horizontal overflow per the Playwright driver); the two NEW CSS rules (`.evidence-drawer{z-index:994}`, `.drawer-backdrop.above-mobile-sheet{z-index:993}`) are LAYOUT-STACK ORDERING ONLY (parent 992 < nested backdrop 993 < drawer 994, verified with browser hit testing); light theme preserves the white-panel-on-cool-canvas hairline discipline and shadow/blur separation per the design-treatment declaration, NOT the dark-theme glow vocabulary. |
| validated-claims | PASS | No new tier-asserting noun on user-visible strings. `verified` / `Issuer path verified` / `state-verified` are internal state names + playwright receipt-JSON `"passed":true` keys + handoff `verified:` yaml schema keys (all receipt-grade or schema metadata). `approved` appears only as `requires authenticated session; not automatable without approved fixtures` (gaps-block entry) and as producer-side process language in the handoff. `passed` appears as pytest summary lines and pytest status output (CI receipt prose, not tier). The pulse-policy rewrite (`Use official records as research context. Company links are for investigation, not trade signals.`) is a USE-CASE assertion, not a tier assertion. The `decision-queue → change-feed` rename is vocabulary hygiene on chrome labels; the underlying producer data path and ranking authority are unchanged. No A7 promotion; no gauntlet claim; no new authority. |

**Net audit cost:** one-pass, no retries, no scope expansion. Audit file persisted to `orch/audits/macro_PR-7450.mm.md`.

---

**Release-side observation (separate from the three audit dimensions):** PR body §"Release truth" carries an explicit release-truth admission — built and browser-tested is NOT merged or production-accepted; the previous exact head had a binding Vercel quota failure; the updated head must earn fresh checks. The audit's plain-language / theme / validated-claims review is REPRODUCIBLE on the merged head `38dd1a8e35e` and is independent of the deployment release path. The handoff `unverified:` block carries the release-side unknowns (`release remains blocked until the binding deployment failure is lawfully resolved`) and the auditor's `do_not_redo:` / `danger_areas:` are scoped to the SAME-PR continuation, not a replacement. Per CLAUDE.md standing laws, release remains a separate deployment-owner concern; no forced deployment, billing change, or synthetic success is being claimed by this audit.
