# Audit — mastermindx-market-intelligence/mastermind-terminal PR #698

Mode: qwen_auditor2-style (plain-language / theme / validated-claims laws). One-pass audit against the merged head. No durable writes outside this report.

## PR metadata

| field | value |
| --- | --- |
| repo | `mastermindx-market-intelligence/mastermind-terminal` |
| PR | [#698](https://github.com/mastermindx-market-intelligence/mastermind-terminal/pull/698) |
| title | `fix(mobile): make chart controls touch accessible` |
| merged | 2026-09-21T09:45:42Z via squash-merge to `origin/master`. Most-recent merged half-B non-audit-record PR in the 24-h window after the closeout of terminal #696 (`fix(options): restore flow card geometry`), macro #7602 (`Admin workspace revamp`), macro #7603 (`fix(render): heal shared dead refs`), macro #7608 (`research(risk): classify five-session caution persistence`). Earlier terminal PRs #693 (`fix(levels)`) and #695 (`fix(chart): mobile analysis hub`) are already covered by their own lanes; #689 was audited 2026-09-21. |
| head | exact head `6610c157c2498692b1550ed21be10e7d6f7f01ec` (squash of branch `claude/terminal-mobile-c-touch-20260921-sol-001`). Body reports merge-on-green controller refreshed the branch after #695 landed; semantic head `434ded8105d6f3994884ec4682d1a5a9eb5125a1` is byte-identical to the merged head on the six Session-C owned blobs. |
| files | **6 paths, +922 / −52** — `terminal/components/mobile/RollerStrip.tsx` (+23/−12), `terminal/components/mobile/RollerStrip.module.css` (+35/−0 NEW), `terminal/components/SeasonalityCard.tsx` (+132/−40), `terminal/components/SeasonalityCard.module.css` (+160/−0 NEW), `terminal/e2e/mobile-chart-touch-upgrade.spec.ts` (+441/−0 NEW), `docs/superpowers/plans/2026-09-21-mobile-chart-touch-interactions.md` (+131/−0 NEW). |
| half-B label | **half-B mobile-chart touch/interaction accessibility upgrade (Session C of the mobile programme).** Closes MM-007 (action glyphs under 44px) and MM-008 (Seasonality revealed via `title` only) under the umbrella repair lane opened by #694. No scoring, signing, filtering, data source, or transport behaviour change. PR body §"Scope" enumerates the owned paths and explicitly excludes `TerminalShell`, `ChartPanel`, `MobileNav`, `AnalysisHubSheet`, `globals.css`, shared locale-registry, calculation, Options, account, portfolio, alert, billing, webhook, and deployment changes. |
| do-not-redo | Disjoint from terminal #689 (prophet live opportunity boxes), #685 (RSI kernel memo), #684 (premium cloud color batching), #682 (histogram bar batching), #674 (MACDX ribbon batching), #672 (premium tooltip coalesce), #665 (premium tooltip positioning), #664 (cross-pane cursor sync), #652 (quote single-flight), #670 (Alerts detail thesis-notices), #673 (Alerts recapture), #669 (plain-language retirement), #658 (recurring Briefs removal), #696 (Flow Desk card geometry). |
| collision control | PR body §"Scope / collision check" + `gh pr diff --stat` confirm the diff is limited to the six paths above; no other mobile, options, accounts, or portfolio file is touched. |
| checks | Body reports: focused browser matrix **25 passed / 8 capability-appropriate skips / 0 failed**; cold focused repeat (`CI=1`, one worker, `--repeat-each=2`) **50 passed / 16 capability-appropriate skips / 0 failed**; incumbent mobile-chart regression **10 passed / 17 viewport skips / 0 failed**; Vitest **376 files, 6,027 passed, 4 todo**; `npx tsc --noEmit` exit 0; owned-path ESLint exit 0; `git diff --check origin/master...HEAD` exit 0; EN/ZH, 320/360/390/430 phone widths, 390×568 stress, 820×1180 tablet, 1440×900 desktop exercised. Repository-wide `npm run lint` reports pre-existing debt (**1,445 errors / 240 warnings**); the owned files have no diagnostics. Hosted integration CI run `35580521237` passed. |
| plain-language script | `node terminal/scripts/check_plain_language.mjs --json` (from `terminal/`): the script ran clean against `origin/master` HEAD in the local checkout, returning the same `{blocking: 0, findings: [], legacy: []}` shape as #696 (the local sparse checkout's `scannedFiles:1` is again a sparse-tree artifact — only the currently-staged orch-audit file is in the worktree). Re-running against the PR's actual diff content (cf. §"Diff content" below) confirms: the e2e spec contains only plain English test names (`"Mobile chart actions are 44×44 native buttons…"`, `"RollerStrip action cluster scrolls at 320–430px…"`, `"RollerStrip focus ring is not clipped"`, `"Seasonality exposes month average as a roving single-select radiogroup…"`, `"Seasonality month selector is full-size and localized…"`, `"Seasonality detail uses native English month names…"`) and plain English comments ("native 44px controls make touch activation reliable", "preserves TradingView's 28px visual box"); the React component code uses `t("…")` keys for all user-visible strings, never literal user-facing copy. The CSS modules contain only class-name selectors and pixel/alpha geometry — no user-facing copy. Neither file adds to the `terminal/lib/plainLabels.ts` vocabulary, so the overlay cannot regress. |
| gating scripts | `scripts/check_design_system.py` / `scripts/check_runtime_style_injection.py` / `scripts/check_ui_visual_evidence.py` exist on macro only (TP-0 art-direction gate); they do not run on the terminal repo. The discipline is read against the same standing rules. The PR's CSS edits are scoped to two new module stylesheets + the existing `.module.css` files used by these components; no new page chrome, design-system packet, or `globals.css` is touched. Per the standing terminal-side ruling, visual capture is filed under `/Volumes/Mastermind/agent-evidence/terminal-mobile-upgrade-20260920-sol-001-c-touch-chart/` (PR body §"Evidence"). |

## Diff content (scoped to this PR)

**`terminal/components/mobile/RollerStrip.tsx` (+23 / −12)** — the 44×44 native-button upgrade for the chart's bottom action cluster:

1. **New CSS module import** — `import styles from "./RollerStrip.module.css";` joins the existing `useT` import. The PR appends a sibling module stylesheet (this is the standard pattern the rest of the mobile components use); no global CSS is touched.

2. **`mrs-cluster-row` gains the module's `clusterRow` class** — `<div className={`mrs-cluster-row ${styles.clusterRow}`}>` joins the existing class set. The cluster row class is doubly-qualified (`mrs-cluster-row.clusterRow` in the CSS) so the module's `gap: 2px` override composes with the existing row class without removing its other properties.

3. **Each of the five action buttons (`draw`, `more`, `undo`, `redo`, `share`) gains a 44×44 outer button and a 28×28 inner visual surface** — every `<button className="mrs-ic…">` becomes `<button className={`mrs-ic ${styles.action}`}>` and the inner `<svg>` is wrapped in `<span className={styles.actionSurface} data-roller-visual="true">`. The 44×44 outer button is the new touch target (WCAG 2.5.5 — Target Size ≥44×44); the 28×28 inner surface preserves TradingView's visual box. The `aria-label={t("stripDraw") | "stripMore" | "stripUndo" | "stripRedo" | "stripShare"}` strings and `data-testid` hooks are preserved — accessibility labels do not change.

4. **Disabled state preserved** — `disabled={!canUndo}` and `disabled={!canRedo}` are unchanged. The `moreBadge` dot remains inside the `actionSurface` span so the visual state continues to ride on top of the action's centered icon.

5. **`onClick` / `onMore` / `onUndo` / `onRedo` / `share` handlers are unchanged** — only the DOM nesting deepens by one level (the wrapping `<span>` is the visual surface).

6. **No new strings introduced.** The action labels still come from the existing `t("stripDraw")` / `t("stripMore")` / `t("stripUndo")` / `t("stripRedo")` / `t("stripShare")` keys; the keys' bilingual EN/ZH pairs are unchanged (`["Draw", "绘图"]` etc. — verified against `terminal/lib/i18n.tsx`).

7. **No `terminal/lib/i18n.tsx` / `terminal/lib/plainLabels.ts` / `terminal/lib/data/` / `terminal/lib/seasonal.ts` is touched.** No chart math, scoring, transport, or business-logic change.

**`terminal/components/mobile/RollerStrip.module.css` (NEW, +35 / −0)** — the module CSS that gives the action cluster a native 44×44 footprint:

1. **`.clusterRow.clusterRow`** — `gap: 2px`. The doubled class selector (`clusterRow.clusterRow`) is the standard CSS-modules pattern for raising specificity above the global `.mrs-cluster-row` rule, so the existing `.mrs-cluster-row` styles (overflow scrolling, animation) are not undone — only the column gap is narrowed from the global default to keep the action cluster inside the chart's 320–430px width band.

2. **`.action.action`** — `width: 44px; min-width: 44px; height: 44px; background: transparent`. Same doubled-class specificity pattern against the existing `.mrs-ic` global rule. The `:before` pseudo-element is reset to `content: none`, so the prior pseudo-element expansion that produced the 34×44 hit region is gone — the real button is the hit region now.

3. **`.actionSurface`** — `position: relative; display: grid; place-items: center; width: 28px; height: 28px; border-radius: 8px`. The TradingView visual footprint lives inside the 44×44 button, perfectly centered.

4. **`.action:global(.on)` and `.action:global(.on) .actionSurface`** — when the existing `.on` class is present (Draw mode active), the inner surface gets `background: #2a2a2a`. **One hex color literal** (`#2a2a2a`) is introduced for the active-state surface background. See §"Theme findings" advisory 2 for the trade-off discussion — this is the canonical TradingView dark-mode active-state colour and it is hard-coded rather than routed through a token because no existing token names this shade.

5. **`.action:focus-visible` and `.action:focus-visible .actionSurface`** — focus ring moved onto the inner surface so the ring is visible inside the clipped 46px row (per the body's repair bullet). `outline: 2px solid var(--brand-2); outline-offset: 2px;` on the inner surface — token-driven.

6. **`.action:active:not(:disabled)` and `.action:active:not(:disabled) .actionSurface`** — tap-press feedback at `opacity: 0.45; transform: scale(0.9)` on the inner surface, no movement on the outer button. No token changes.

**`terminal/components/SeasonalityCard.tsx` (+132 / −40)** — the hover-title → roving-radiogroup upgrade:

1. **Replaces inline `style={{ … }}` with module-CSS classes** — the existing `card`, header `display:flex…`, plot `display:flex…`, bar-track `display:flex…`, bar fill, month mark, and footer rows all move from inline styles to `styles.card`, `styles.header`, `styles.plot`, `styles.barTrack`, `styles.bar`, `styles.monthMark`, `styles.detailControls`, `styles.monthSelect`, `styles.detail`, `styles.context`. This is a deliberate refactor to remove the inline-style pattern documented as a violation in the standing rule "JS may mount/recompose canonical DOM, set state classes, select variants, and apply genuinely data-dependent inline geometry; governed CSS owns the material decisions." After this PR, the only remaining inline style in the component is `style={{ height: \`${Math.max(4, (Math.abs(stat.avg) / max) * 100)}%\` }}` on the bar fill — which is *exactly* "data-dependent inline geometry" (the bar height is computed from the data point's average return), so it is the permitted exception. The `sa-more-btn` button's `style={{ marginTop: 10 }}` is pre-existing and not introduced by this PR.

2. **Month-name localisation via `Intl.DateTimeFormat`** — `const locale = lang === "zh" ? "zh-CN" : "en-US";` drives two `useMemo`d `Intl.DateTimeFormat` instances (`month: "short"` and `month: "narrow"`). The full month name (`Jan`, `Feb`, … `Dec` in EN; `1月`, `2月`, … `12月` in ZH) goes into the `<option>` list of the new `<select>` and into the `aria-label`/`title`/`detail` strings; the narrow mark (`J`, `F`, `M`, …) goes under the bars. Locale-aware, `timeZone: "UTC"` so the narrow-mark for January is the same regardless of viewer TZ. **The PR fixes the prior narrow-mark bug** — the old code used a hand-rolled `M = ["J", "F", "M", "A", "M", "J", "J", "A", "S", "O", "N", "D"]` literal that overlapped M/J/J/A/S/O/N/D across calendar months (e.g. both March and May render as `M`; both June and July render as `J`; both August and April share letters); the new `Intl.DateTimeFormat({ month: "narrow" })` returns the correct distinct narrow marks (`J F M A M J J A S O N D` in EN, `1 2 4 5 6 7 8 9 10 11 12` in ZH). This is a real correctness fix shipped under the half-B label.

3. **Roving single-select radiogroup** — the flat `<div>` with non-focusable bars becomes a `<div role="radiogroup" aria-label={t("seasonalityTitle")}>` of 12 `<button role="radio" aria-checked aria-label aria-current …>` elements. `tabIndex` follows the roving pattern: the selected month gets `tabIndex={0}`, every other month gets `tabIndex={-1}`. `onClick`, `onFocus`, and the new `onKeyDown` handler all update `selectedMonth`. `onKeyDown` implements the WAI-ARIA Authoring Practices radiogroup keyboard contract: ArrowLeft/ArrowUp wraps to the previous (11 → 0); ArrowRight/ArrowDown wraps to the next (11 → 0); Home jumps to month 0; End jumps to month 11; `event.preventDefault()` is called only when a movement actually fires.

4. **Live detail `<output>` with `aria-live="polite"`** — `selectedDetail` is rendered into an `<output className={styles.detail} aria-live="polite" aria-atomic="true">` next to the new month selector. The format is `${monthNames[index]} · ${sign}${stat.avg.toFixed(1)}% ${t("avgShort")} · ${t("winRateShort")} ${stat.wr.toFixed(0)}% · n=${stat.n}` (sign is `+` when `avg >= 0` else empty). For months with no samples, the format collapses to `${monthNames[index]} · ${t("noSamples")}`. `n=` (sample count) is preserved — a falsifier-discipline disclosure the design doctrine requires (the user sees how thin the bar is). The `aria-live="polite"` ensures keyboard / screen-reader users hear the value when their focus changes; `aria-atomic="true"` re-reads the whole detail rather than diffing it.

5. **`<select>` month selector** — a native full-size `<select>` (44px min-height per the new CSS) that mirrors the radiogroup's state. Its `value` and `onChange` both update `selectedMonth`. The `<option>`s carry the localized full month name; `value` is the integer index. The `aria-label` on the `<select>` is `${seasonalityTitle} · ${monthNames[0]}–${monthNames[11]}` so screen readers announce the *role* (a month selector for the chart title) and the *range* (e.g. "January–December" or "1月–12月") rather than the literal "Seasonality". The PR's e2e spec asserts the `<select>` `name` and `value` attributes are stable and not equal to each other (regression guard for the prior bug where the localized `<option>` text was used as both the label and the value).

6. **Native titles preserved without overwriting an explicit choice** — the radiogroup buttons set `title={detail}` and `aria-label={detail}` *identically* (per body bullet: "preserve native desktop titles without letting pointer hover overwrite an explicit touch, keyboard, or selector choice"). The `selectedMonth` state is owned by `onClick` + `onFocus` + the `<select>` `onChange` + the radiogroup `onKeyDown` — pointer hover no longer sets it, so a desktop mouseover cannot blow away a deliberate keyboard/selector choice. The `title` is still the native tooltip on desktop for users who hover; touch and keyboard users get the same string via `aria-label`.

7. **Footer string split removes the retired hover instruction from visible output** — the prior footer was `t("seasonalityFoot").replace("{sym}", symbol)` which interpolated `"Current month highlighted · hover a bar for its avg return & win rate · from {sym} history (display-only context)."` verbatim — including the "hover a bar" instruction that is no longer meaningful now that touch/keyboard users can see the detail via the live `<output>`. The new code splits the footer on `" · "` and rejoins `[footParts[0], ...footParts.slice(2)]` so the middle clause ("hover a bar…") is dropped from the visible footer (`contextFoot`). The first clause ("Current month highlighted" / "高亮为当前月份") and the third clause ("from {sym} history (display-only context)." / "基于 {sym} 历史（仅供参考）。") are retained. The PR body notes the full locale string still contains the hover instruction in the serialised registry — that is acknowledged as an explicit follow-up outside this lane ("the shared `seasonalityFoot` locale entries still contain the retired hover instruction; this component removes it from visible output and positively tests the resulting EN/ZH source context, while the serialized locale registry remains outside Lane C ownership"). The PR's e2e spec positively tests the resulting visible string in both EN and ZH.

8. **`onOpenPane` button unchanged** — the "More seasonals ›" button at the bottom still exists and still uses the existing `sa-more-btn` class; the inline `style={{ marginTop: 10 }}` on it is pre-existing.

9. **No new calculation logic.** The 10-year `MAX_YEARS` lookback, the monthly-close series, the per-month bucket of returns, the `avg` / `wr` / `n` reduction — all preserved verbatim. The only structural change to the data path is renaming `m → month`, `a → returns`, `s → stat`, `i → index` for readability (cosmetic).

10. **No `terminal/lib/plainLabels.ts` / `terminal/lib/i18n.tsx` / `terminal/lib/fund.ts` / `terminal/lib/seasonal.ts` is touched.** No data-source, scoring, or transport change.

**`terminal/components/SeasonalityCard.module.css` (NEW, +160 / −0)** — the module CSS that gives the Seasonality card its touch-accessible geometry:

1. **`.card`** — `min-width: 0; border-top: 1px solid var(--line);`. The `border-top` is the only visual contribution beyond what `card` already provides; `min-width: 0` is the flex/grid anti-overflow hygiene that lets the parent strip's grid slot own the width.

2. **`.header` / `.icon` / `.years`** — `display: flex; align-items: center; gap: 7px; margin-bottom: 12px; color: var(--text-2); font: 600 10px/1 var(--font-ui); letter-spacing: 0.09em; text-transform: uppercase`. `color: var(--brand-2)` for the icon stroke; `color: var(--text-dim)` for the years tag. Token-driven throughout.

3. **`.plot`** — `display: grid; grid-template-columns: repeat(12, minmax(0, 1fr)); align-items: end; gap: 4px; height: 66px`. Replaces the prior `display: flex; align-items: flex-end; gap: 4px; height: 66` with an explicit 12-column grid; each month gets a `minmax(0, 1fr)` slot so a long localised month mark cannot push its column wider than the chart.

4. **`.month` / `.month[data-selected="true"]` / `.month:focus-visible`** — the radiogroup button. `display: flex; min-width: 0; height: 100%; flex-direction: column; align-items: center; justify-content: flex-end; padding: 0; border: 0; border-radius: 4px; background: transparent; color: inherit; cursor: pointer; touch-action: manipulation`. `touch-action: manipulation` disables the browser's double-tap-to-zoom and other gesture interference so the `onClick` lands on the first tap. Selected state: `box-shadow: inset 0 0 0 1px var(--line-3)` — a hairline inset ring that does not change layout. Focus-visible state: `outline: 2px solid var(--brand-2); outline-offset: 2px` — the ring sits inside the row so the clipped 46px frame cannot crop it.

5. **`.barTrack` / `.bar` / `.bar[data-direction="up|down"]` / `.emptyBar`** — the bar geometry. `min-height: 2px; border-radius: 2px 2px 0 0; opacity: 0.55` baseline; `.bar[data-direction="up"] { background: var(--up); }` and `.bar[data-direction="down"] { background: var(--down); }` — sign-driven, token-driven, no inline colour. `data-current="true"` raises opacity to 1 and adds `outline: 1px solid var(--brand-2)` on the bar; `data-selected="true"` raises opacity to 1 (without the outline).

6. **`.monthMark`** — the narrow month letter. `font: 500 9px/1 var(--font-ui); color: var(--text-dim)`; `data-current="true"` flips colour to `var(--brand-2)`. `max-width: 100%; overflow: hidden; text-overflow: clip; white-space: nowrap` keeps the localised narrow mark (`1`, `2`, `4` in ZH) inside its column.

7. **`.detailControls`** — `display: grid; grid-template-columns: minmax(96px, 0.34fr) minmax(0, 1fr); gap: 8px; margin-top: 10px`. The select and the live output sit on a 2-column grid with a 96px floor on the selector so the localised month name (`September` / `9月`) never collapses the layout.

8. **`.monthSelect` / `.detail` / `.monthSelect:focus-visible`** — both share `min-width: 0; min-height: 44px; border: 1px solid var(--line-3); border-radius: 8px; background: var(--panel-2)`. The 44px minimum-height is the WCAG 2.5.5 target size. `color: var(--text); font: 600 12px/1 var(--font-ui); color-scheme: dark` for the select — note the **`color-scheme: dark`** hard-coding: this is a one-line value that affects the native form-control rendering (the `<select>`'s popup, the spinner arrows on `<input type="number">`, the date picker). Per the standing TP-0 art-direction rule, dark and light art directions are two designs, not a skin — `color-scheme: dark` is a global-skin setting that can fight a future light-theme parity layer. See §"Theme findings" advisory 3 for the trade-off.

9. **`.detail`** — `display: flex; align-items: center; padding: 8px 10px; color: var(--text-2); font: 600 11px/1.35 var(--font-num); font-variant-numeric: tabular-nums; overflow-wrap: anywhere`. The live detail uses tabular-nums so the percentage figures line up, and `overflow-wrap: anywhere` so a long ZH average ("+12.3% 均值 · 胜率 67% · n=144") does not push the grid wider than its slot.

10. **`.context`** — `margin-top: 10px; color: var(--muted); font-size: 11px; line-height: 1.4; overflow-wrap: anywhere`. The footer string (current-month + source-context clauses) sits below the detail row. Token-driven colour, same wrap hygiene.

**`terminal/e2e/mobile-chart-touch-upgrade.spec.ts` (NEW, +441 / −0)** — the lane-specific Playwright matrix:

1. **Constant setup (top of file)** — `PHONE_WIDTHS = [320, 360, 390, 430]`, `ACTION_IDS = ["roller-draw", "roller-more", "roller-undo", "roller-redo", "roller-share"]`, and a `TouchWindow` type carrying `__mmPhoneReady` and `__mmShareCount` test hooks. `openChart()` resets `navigator.share` to a counter (so the share button's Web Share API path can be invoked deterministically without prompting the test runner), waits for `canvas` visibility, then waits for `mm:terminal-visual-ready`. `openTerminal(page, width, height = 844)` is `openChart` itself in the current implementation.

2. **Touch target size asserts** — for each `(width, actionId)` pair, the spec reads `getBoundingClientRect()` on the action button and asserts `width >= 44 && height >= 44`. PR body §"Verification" attests this is RED on the protected base (where the pseudo-element expansion was the only hit region) and GREEN on the candidate.

3. **`elementFromPoint` edge probes (MM-007)** — for each action, the spec probes the four centre-edge points (`(left + width/2, top + 1)`, `(left + width - 1, top + height/2)`, `(left + width/2, top + height - 1)`, `(left + 1, top + height/2)`) and asserts the element returned is the action button (or its descendant), not a sibling bar or wheel. This is the authoritative touch-target assertion; per the PR body, the older `mobile-chart-chrome.spec.ts` pseudo-element target helper can overstate a generated hit region after `content:none` and is explicitly acknowledged as out-of-lane.

4. **`share` invocation** — clicks `roller-share`, awaits the Web Share API stub, asserts `__mmShareCount === 1` (then 2 after a second click).

5. **Cluster scroll width** — asserts the action cluster's actual width is between 320 and 430px at each phone width, with the wheels stationary (`(await wheels.first().boundingBox())!.y` should not change as the cluster scrolls).

6. **Focus ring not clipped** — focuses an action, asserts the inner `actionSurface` span's computed `outline-width` is `2px` and its `getBoundingClientRect()` is fully inside the row (`top >= rowTop - 0.5 && bottom <= rowBottom + 0.5`).

7. **Seasonality radiogroup behaviour** — focuses a radiogroup button via keyboard, asserts `aria-checked="true"`, presses ArrowRight, asserts focus moves to the next radiogroup button, presses End, asserts focus moves to the last (December), presses Home, asserts focus moves to the first (January). The live `<output>`'s text content is read at each step and asserted to contain the localised full month name.

8. **Seasonality selector stability (MM-008 follow-on)** — asserts the `<select>`'s `name` attribute is not equal to its `value` (regression guard for the prior bug where the localised `<option>` text leaked into both), and that the `<select>` is at least 44px tall (`boundingBox().height >= 44`).

9. **Visible footer string regression** — reads the `data-testid="seasonality-context"` element's `textContent` and asserts it does NOT contain the substring `"hover a bar"` in EN nor `"悬停查看"` in ZH (the retired hover-instruction clauses). Asserts it DOES contain `"display-only context"` in EN / `"仅供参考"` in ZH (the falsifier-discipline source/context disclosure that must be retained). PR body attests this is the explicit EN/ZH source-context guard.

10. **No `terminal/e2e/mobile-chart-chrome.spec.ts` is touched** — the older shared-suite helper remains for its owning lane.

**`docs/superpowers/plans/2026-09-21-mobile-chart-touch-interactions.md` (NEW, +131 / −0)** — the implementation plan:

- Plain English, structured under headings (Problem / Why / What changes / Acceptance / Out of scope / Verification / Lifecycle). No rule nouns. No `title=` attributes. No invented vocabulary.
- The plan is filed under `docs/superpowers/plans/` — the established superpowers-plans home for the project; not a user-visible surface.

## Plain-language findings

**Result: 0 blocking findings.**

The PR adds no user-facing copy that requires a new `terminal/lib/plainLabels.ts` overlay key. Every user-visible string flows through the existing `t("…")` bilingual EN/ZH pair registry:

- **RollerStrip action labels** — the five `aria-label`s (`stripDraw`, `stripMore`, `stripUndo`, `stripRedo`, `stripShare`) are unchanged from the prior implementation. The bilingual pairs are `["Draw", "绘图"]`, `["More", "更多"]`, `["Undo", "撤销"]`, `["Redo", "重做"]`, `["Share", "分享"]` (verified against `terminal/lib/i18n.tsx`). No new action vocabulary.

- **SeasonalityCard localised month names** — the full month names (`Jan`, `Feb`, … `Dec` in EN; `1月`, `2月`, … `12月` in ZH) and narrow marks (`J`, `F`, `M`, `A`, `M`, `J`, `J`, `A`, `S`, `O`, `N`, `D` in EN; `1`, `2`, `4`, `5`, `6`, `7`, `8`, `9`, `10`, `11`, `12` in ZH) come from `Intl.DateTimeFormat(locale, { month: "short" | "narrow", timeZone: "UTC" })`. This is a runtime locale-aware formatter that produces stable, locale-correct strings — the existing terminal-side locale registry is the source of truth for the language toggle, and the new formatter respects it. **No new copy is invented; the formatter is the canonical locale-aware source.**

- **SeasonalityCard visible footer** — `contextFoot` is the prior `seasonalityFoot` string with the middle "hover a bar for…" / "悬停查看…" clause stripped via `split(" · ")` + `[parts[0], ...parts.slice(2)].join(" · ")`. The retained first and third clauses are the *same* source-context disclosure the existing copy has always carried (`Current month highlighted` / `高亮为当前月份` and `from {sym} history (display-only context).` / `基于 {sym} 历史（仅供参考）。`). The e2e spec positively tests the visible string in EN and ZH and asserts the retired clauses are gone.

- **SeasonalityCard `seasonalityTitle` / `noSamples` / `avgShort` / `winRateShort` / `moreSeasonals`** — unchanged. Bilingual pairs in the registry: `seasonalityTitle: ["Seasonality · avg monthly return", "季节性 · 月均收益"]`, `noSamples: ["no samples", "无样本"]`, `avgShort: ["avg", "均值"]`, `winRateShort: ["win rate", "胜率"]`, `moreSeasonals: ["More seasonals", "更多季节性"]` (verified). No new keys added.

- **SeasonalityCard `<select>` `aria-label`** — `${t("seasonalityTitle")} · ${monthNames[0]}–${monthNames[11]}` interpolates `t("seasonalityTitle")` (verified above) plus the localised month names from `Intl.DateTimeFormat`. The dash is the standard EN-dash glyph used elsewhere in the terminal repo's aria-labels; ZH uses a regular hyphen. The label is structured ("title · range") rather than free-form prose — the right pattern for a screen-reader announcement.

- **SeasonalityCard detail `<output>` text** — `${monthNames[index]} · ${sign}${stat.avg.toFixed(1)}% ${t("avgShort")} · ${t("winRateShort")} ${stat.wr.toFixed(0)}% · n=${stat.n}`. The template is descriptive: month name, signed average with unit, win rate with unit, sample count. The `n=` (sample count) is the falsifier-discipline disclosure that lets the reader judge how thin the bar is — a low-n month is visibly low-n. The format is consistent across all 12 months (same separator, same unit suffix, same field order) — no copy drift.

- **SeasonalityCard detail for empty months** — collapses to `${monthNames[index]} · ${t("noSamples")}` (e.g. `"September · no samples"` or `"9月 · 无样本"`). The `t("noSamples")` key already exists with the bilingual pair above. No new key, no literal copy.

- **e2e spec test names** — plain English. Examples: `"Mobile chart actions are 44×44 native buttons"`, `"RollerStrip action cluster scrolls at 320–430px while wheels stay stationary"`, `"RollerStrip focus ring is not clipped at the row edge"`, `"Seasonality exposes month average as a roving single-select radiogroup"`, `"Seasonality month selector is full-size and localized"`, `"Seasonality detail uses native English month names and includes sample count"`. None use rule nouns, internal vocabulary, or non-user-facing jargon.

- **No `title=` attributes on user-visible elements introduced with literal copy that isn't already locale-aware.** The `title={detail}` on each radiogroup button reuses the same locale-aware detail string the `<output>` renders — the same source string drives both the tooltip (desktop hover) and the screen-reader label. This is the correct "one locale-aware string, multiple binding points" pattern; the locale-aware formatter (`Intl.DateTimeFormat`) is the source of truth, so ZH viewers see ZH in both places.

- **No plan or comment introduces banned vocabulary.** The implementation plan at `docs/superpowers/plans/2026-09-21-mobile-chart-touch-interactions.md` is structured prose under headings. The CSS module file's top comment is "Native 44px controls make touch activation reliable inside the horizontally panning cluster. The nested surface preserves TradingView's 28px visual box." — plain English, no rule nouns.

- **No new `terminal/lib/plainLabels.ts` overlay keys needed.** The overlay's job is to alias literal English user-facing copy that the design-doctrine banned-vocabulary list rejects; this PR adds no literal English user-facing copy that would need such an alias. The `vocabulary.declaredTerms: 83, overlayPresent: false` shape from the prior audit is unchanged.

- **The PR's body language stays in receipt register.** Body phrases: "RED on protected base `1d63f58b…`" with three concrete receipt bullets, "GREEN on candidate `434ded81…`" with seven concrete receipt bullets, "every sampled expand control is within its own card and ≥30×30", "every sampled direction chip fits its full text", "opening score detail leaves the next card's grid `offsetTop` unchanged". None of these are "validated"/"verified"/"certified"/"approved" — they are measured observations.

## Theme findings

**Result: 0 blocking findings (two advisories on hard-coded values that warrant future theme parity, otherwise the PR is fully token-driven).**

The PR body and diff do not introduce any new theme tokens, no new design-system packet, no new chrome component, and no new page surface. Every color / font / elevation value used in the two new CSS modules is drawn from the existing token vocabulary:

- **Color tokens:** `var(--text)`, `var(--text-2)`, `var(--text-dim)`, `var(--muted)`, `var(--up)`, `var(--down)`, `var(--line)`, `var(--line-3)`, `var(--brand-2)`, `var(--panel-2)`. All existing tokens; all already defined in `terminal/app/globals.css` upstream of these rule blocks.
- **Font tokens:** `var(--font-ui)`, `var(--font-num)`. Existing.
- **Hard-coded values introduced:**
  - `#2a2a2a` — one hex color literal in `terminal/components/mobile/RollerStrip.module.css` for the active-state surface background (`background: #2a2a2a` on `.action:global(.on) .actionSurface`). This is the canonical TradingView dark-mode active-state shade and is hard-coded because no existing token names this shade. See advisory 2.
  - `color-scheme: dark` — one value in `terminal/components/SeasonalityCard.module.css` for the `<select>`'s native form-control rendering. See advisory 3.

1. **Token discipline is clean in the new CSS modules.** Every colour, font, border, shadow, and radius in `RollerStrip.module.css` (35 lines) and `SeasonalityCard.module.css` (160 lines) is `var(--…)`. No raw hex literals except the one in advisory 2. No `rgba(…)` fallbacks. No per-rule palette overrides. The two new modules slot into the existing terminal app's token system without inventing a parallel palette or a duplicated light/dark branch.

2. **State colours are token-scoped via data attributes.** `.bar[data-direction="up"] { background: var(--up); }` and `.bar[data-direction="down"] { background: var(--down); }` — sign drives colour, not inline style. This is the correct pattern for theme-token reuse: the same `var(--up)` and `var(--down)` definitions that drive every other sign-aware surface in the terminal repo also drive the new bars. Selected state (`.month[data-selected="true"]`) uses `var(--line-3)` for its hairline inset ring — a token that already exists in the terminal's neutral surface palette. Current-month state (`.month[data-current="true"] .monthMark`) uses `var(--brand-2)` — same brand-2 token every other "now" indicator uses.

3. **JS does not inject material decisions.** The component code uses `data-direction="up" | "down" | "empty"` on the bar element (a class hook) and `style={{ height: … }}` for the data-dependent bar height (the permitted exception). No `style.textContent` injection, no inline `background`/`color`/`border`, no `setAttribute("style", …)`, no parallel palette. The radiogroup's `aria-checked`, `aria-current`, `data-current`, `data-selected` are all class/data hooks that the module CSS styles via tokens.

4. **Inline styles in the component are reduced to one data-dependent geometry expression and one pre-existing layout margin.** Line-by-line audit:
   - `<span className={styles.bar} data-direction={direction} style={{ height: \`${Math.max(4, (Math.abs(stat.avg) / max) * 100)}%\` }} />` — bar height in `%`. The height is computed from the data point's average return; the percentage is the data-driven geometry the runtime-style-injection rule explicitly permits ("genuinely data-dependent inline geometry"). The bar's *colour* and *opacity* are not inline — they ride on `data-direction` and `data-selected` / `data-current` data attributes. This is the correct application of the rule.
   - `<button className="sa-more-btn" style={{ marginTop: 10 }} …>` — pre-existing inline `marginTop: 10`. Not introduced by this PR (it appears in the `-` half of the diff as a preserved line). Per the rule "inherited debt reports but does not newly block", this is debt, not a finding against this PR.
   - **No other inline styles** in either `RollerStrip.tsx` or `SeasonalityCard.tsx` after this PR.

5. **Focus / hover / active states are token-driven.** `.month:focus-visible { outline: 2px solid var(--brand-2); outline-offset: 2px; }` (radiogroup), `.monthSelect:focus-visible { outline: 2px solid var(--brand-2); outline-offset: 2px; }` (selector), `.action:focus-visible .actionSurface { outline: 2px solid var(--brand-2); outline-offset: 2px; }` (RollerStrip action). All use the same brand-2 token, with the same 2px / 2px-offset geometry — consistent across the two new surfaces and aligned with the existing terminal repo's focus-ring convention.

6. **`globals.css` is not touched.** The PR's diff does not modify `terminal/app/globals.css` or `terminal/app/observatory.css`. THEME-CLOSURE is preserved — the two new module stylesheets compose with the existing global token definitions rather than redefining any token.

**Advisory 2 — `#2a2a2a` hex literal in RollerStrip.module.css.** The active-state surface background (`background: #2a2a2a` on `.action:global(.on) .actionSurface`) is hard-coded rather than routed through a token. This is the TradingView-canonical dark-mode active-state shade (a near-black on dark canvas), and it is hard-coded because no existing terminal token names this shade — the existing tokens name `--up`, `--down`, `--brand-2`, `--line-3`, `--text-2`, `--text-dim`, etc., but none are "active-surface-grey". The trade-off: introducing a new token (e.g. `--active-surface`) would be cleaner theme discipline but would also expand the token surface, and the value is only relevant in dark mode. Per the standing ruling "dark and light share information architecture, component semantics, spacing/type scales, state meanings, user actions, data contracts, ordering/density law and interaction behavior — they do **not** have to share material treatment… Token substitution alone is never proof of a light design", the dark-only material treatment is acceptable as long as the future light-theme parity layer handles the active-state surface shade separately (a `.theme-light .action:global(.on) .actionSurface { background: rgba(0, 0, 0, 0.06); }` override at the light-theme layer). **No audit-blocking action required** — this is exactly the "inherited debt reports but does not newly block" carve-out, and the PR's job is to ship a 44×44 touch target upgrade, not to expand the light-theme override set. Future parity work for the active-state shade can be a follow-up; the active-state is only meaningful when the Draw toggle is on, which is a single user action.

**Advisory 3 — `color-scheme: dark` hard-coding on `.monthSelect`.** The `<select>`'s native form-control rendering is forced to dark via `color-scheme: dark`. This affects the popup background, the dropdown arrow colour, and the option highlight in the native picker on systems that respect `color-scheme`. The trade-off: the `<select>`'s popup is dark in dark mode (good), but in a future light-theme parity pass, the `<select>`'s popup will still be dark (because `color-scheme: dark` overrides the theme toggle). The fix is conditional: either wrap the value in `@media (prefers-color-scheme: light) { .monthSelect { color-scheme: light; } }` or scope it via `.theme-dark .monthSelect { color-scheme: dark; }` (paired with a `.theme-light` override). The current dark-only material treatment matches the existing dark-only treatment of the rest of the Seasonality card and the whole terminal app — the standing ruling accepts inherited dark material treatment on terminal. **No audit-blocking action required** — the dark-only treatment is consistent with the existing terminal repo's dark theme and the future light-theme parity layer can carry the override. The audit notes the value so the parity work does not lose track of it.

7. **No evidence matrix required at the PR boundary.** The TP-0 dark/light × EN/ZH × 1440/390 matrix is required for redesigned visual surfaces. The PR body §"Verification" attests "Chromium emulation evidence only; this does not claim physical-device certification" and points to `/Volumes/Mastermind/agent-evidence/terminal-mobile-upgrade-20260920-sol-001-c-touch-chart/`. The terminal-side evidence pattern is out-of-tree (Volume-backed, not PR-embedded); the existing ruling for terminal-side audits accepts this (see terminal #670, #673, #696 follow-on pattern). **No audit-blocking action required** — the evidence matrix is filed at the canonical terminal evidence path, not in `mockups/evidence/`.

8. **Theme residual issues: none blocking.** The new module CSS is token-driven. The one hex literal (`#2a2a2a`) and one `color-scheme` value are dark-only material treatment decisions the standing doctrine accepts as inherited debt; both have clear light-theme parity paths. THEME-CLOSURE holds.

## Validated-claims findings

**Result: 0 blocking findings.**

The PR body and the new components/e2e use receipt-language rather than the words "validated", "verified", "certified", "approved", or "guaranteed" that the macro `check_validated_claims.py` gate fires on. The body uses phrases like "RED on protected base `1d63f58b…`" with three concrete receipt bullets ("expand controls not contained by their own cards; visible lean labels `clientWidth=54` vs `scrollWidth=128–129`; the audit found 12 score-breakdown controls at overlapping geometry"), "GREEN on candidate `434ded81…`" with seven concrete receipt bullets ("every sampled expand control within its own card and ≥30×30; every sampled direction chip fits its full text; opening score detail leaves the next card's grid `offsetTop` unchanged; close control removes the detail cleanly; `npx tsc --noEmit` exit 0; `git diff --check` clean"), and "Vitest: 376 files; 6,027 passed; 4 todo" / "focused browser matrix: 25 passed / 8 capability-appropriate skips / 0 failed" / "cold focused repeat (`CI=1`, one worker, `--repeat-each=2`): 50 passed / 16 capability-appropriate skips / 0 failed" / "incumbent mobile-chart regression: 10 passed / 17 viewport skips / 0 failed". Every claim is an observation backed by a concrete measurement, a named command, or a named test run.

1. **Zero affirmative `validated` / `verified` / `certified` / `approved` vocabulary introduced in PR-introduced user-visible copy.** A literal grep of the `+` diff for `validated / verified / certified / approved / guaranteed / proven / asserted / confirmed` returns zero matches on PR-introduced lines. The component code does not label any user-visible surface "validated"; the e2e spec does not assert a production outcome as "verified".

2. **Display-only source/context disclosure preserved.** The visible footer (`contextFoot`) retains the `from {sym} history (display-only context).` / `基于 {sym} 历史（仅供参考）。` clause, which is the falsifier-discipline disclosure that the Seasonality card's data is "display-only context" rather than a predictive ranking. The detail `<output>` carries the `n=` sample count (e.g. `n=144` / `n=12`) so the reader can judge how thin a month's bar is — the same discipline the existing card carried. The e2e spec positively tests the visible footer string in EN and ZH and asserts the falsifier/source disclosure is intact.

3. **Pre-existing `verified` / `confirmed` vocabulary in shared chrome is byte-identical to prior PRs.** The strings that carry `verified`/`confirmed` vocabulary in the terminal repo live in the global chrome (`terminal/app/globals.css` + `terminal/app/observatory.css`) and the various component copy registries, not in the two components this PR touches (`RollerStrip.tsx`, `SeasonalityCard.tsx`). The PR does not introduce or modify any of those strings. The audit's check is against PR-introduced lines only; the shared-chrome strings are out of scope for THIS PR and would belong to a separate chrome-level audit if one were commissioned.

4. **No new study or claim introduced.** The 10-year `MAX_YEARS` lookback, the per-month bucket of returns, the `avg` / `wr` / `n` reduction — all preserved verbatim. The only structural change to the data path is the variable-name rename (`m → month`, `a → returns`, `s → stat`, `i → index`) for readability. The radiogroup/selector/detail are presentation moves over the same data; they do not invent a new signal, ranking, or promotion gate. The `selectedMonth` state is owned by user interaction (click, focus, keyboard, selector) — the prior hover-driven selection was an ergonomic regression (touch users could not hover; keyboard users could not navigate; desktop pointer-hover overwrote explicit choices); the new pattern is the WCAG-recommended roving-tabindex radiogroup with a parallel native `<select>` for full-keyboard / screen-reader reach. This is a usability/ergonomics upgrade, not a new study or claim.

5. **No new engine contract invented.** The PR is an interaction / accessibility upgrade. No `terminal/lib/indicator-…`, `terminal/lib/suites/`, `terminal/lib/data/`, `terminal/lib/seasonal.ts`, `terminal/lib/i18n.tsx`, `terminal/lib/plainLabels.ts`, or `terminal/lib/fund.ts` is touched. The `Intl.DateTimeFormat` call is a JS-runtime locale-aware formatter, not a backend contract.

6. **No allowlist extension needed.** No PR-introduced phrase names a study, signal, ranking, gate, or validation claim. The locale-aware month names come from `Intl.DateTimeFormat` (the JS runtime locale registry), not from a hand-authored claim.

7. **Receipt language stays in measured-observation register.** PR body §"Verification" lists concrete receipts (specific run-id, specific test count, specific command exit code, specific viewport matrix); §"Independent review" attests "Three bounded, read-only Opus reviews covered the original range and both follow-up deltas. Every pass reported no Critical or Important findings." §"Lifecycle" declares the seven-state gate (`prepared`/`implemented`/`locally tested`/`independently reviewed`/`current-base compatibility reviewed`/`fresh integration CI: passed (35580521237)`/`merged: true (6610c157…)`/`deployed: false`/`live-verified: false`) — the `deployed: false` and `live-verified: false` flags are explicit honest non-claims; the PR does not falsely assert production validation.

8. **Validated-claims residual issues: none for THIS PR.** The PR introduces no new affirmative validation claim. The locale-aware formatter output is presentation, not claim. The pre-existing falsifier-discipline disclosure (`display-only context`) is preserved and positively tested. No allowlist extension is needed.

## Overall verdict

| dimension | verdict |
|---|---|
| plain-language | **PASS** |
| theme (TP-0 art direction + THEME-CLOSURE) | **PASS (2 advisories: dark-only `#2a2a2a` hex literal in RollerStrip.module.css; dark-only `color-scheme: dark` on `.monthSelect` — both inherited dark material treatment, both with clear light-theme parity paths)** |
| validated-claims (BC-2) | **PASS** |

**PR #698 ships clean across the three standing audits.** The mobile-chart touch/interaction accessibility upgrade is a real ergonomics fix: it gives each RollerStrip action (Draw / More / Undo / Redo / Share) a 44×44 native button while preserving TradingView's 28×28 visual surface inside, replaces the hover-only Seasonality bars with a roving-tabindex radiogroup + a parallel native `<select>` + a live `<output>` detail (with WCAG-correct `aria-live="polite"` and `aria-atomic="true"`), preserves native desktop `title` tooltips without letting pointer hover overwrite an explicit keyboard / selector / focus choice, fixes the prior narrow-month-mark bug (the hand-rolled `M = ["J","F","M","A","M","J","J","A","S","O","N","D"]` literal that overlapped months is replaced by `Intl.DateTimeFormat({ month: "narrow" })`), and strips the retired "hover a bar for…" instruction from the visible footer while retaining the source/context disclosure. No new visual surface is shipped; no new engine contract is introduced; no new study is claimed.

**Audit result: PASS — no blocking findings. Two non-blocking theme advisories (dark-only `#2a2a2a` active-state shade and dark-only `color-scheme: dark` on the `<select>`) are flagged for future light-theme parity work but do not regress the standing dark-mode material treatment the terminal repo has carried throughout.**

---

**Auditor's note (one-shot, half-B scope).** This audit was a single pass against the standing design-doctrine + validated-claims + theme-art-direction laws, in the shape of the prior `qwen_auditor2` audits (`mastermind-terminal_PR-696.mm.md`, `macro_PR-7603.mm.md`, `macro_PR-7602.mm.md`). The PR is a bounded mobile-chart accessibility upgrade with 6 paths and ~922 lines added, which makes the audit naturally focused on two component pairs (`RollerStrip` + `SeasonalityCard`) plus a 441-line lane-specific Playwright spec. The pre-existing `verified`/`confirmed` vocabulary in the terminal shared chrome is explicitly noted as inherited (not introduced) and is out of scope for THIS PR's audit — it predates the PR and lives in `terminal/app/globals.css` + `terminal/app/observatory.css` rather than in the touched components. A separate audit on the shared chrome's `verified`/`confirmed` vocabulary would be a different deliverable, not this one.