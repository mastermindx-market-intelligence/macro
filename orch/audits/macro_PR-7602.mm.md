# Plain-language / theme / validated-claims audit — macro PR #7602

Auditor: qwen_auditor2-style pass (one-shot, half-B scope). Date: 2026-09-21.

## PR metadata

| field | value |
|---|---|
| repo | `mastermindx-market-intelligence/macro` |
| number | #7602 |
| title | `Admin workspace revamp: cleaner navigation, truthful status, and recoverable pages` |
| merge head | `d70f0bd…` (squash-merged to `origin/main` 2026-09-21T10:08:15Z; full hash captured in `mergeCommit.oid` via `gh pr view --json mergeCommit`). |
| merged | 2026-09-21T10:08:15Z via squash-merge to `main`. Most recent non-audit-record half-B PR in the 24-h window that has not already been audited (`#7585 / #7586 / #7589 / #7597 / #7602 / #7603` — `ls orch/audits/macro_PR-*.mm.md` shows no audit yet for any of these; `#7588 / #7598 / #7582 / #7580 / #7587 / #7570` are later `orch(audit)` record PRs themselves, not target PRs). |
| author / carrier | local `claude/admin-revamp-20260921` working branch off fresh `origin/main`. PR body names "this follows and preserves the now-merged #7345 cleanup and #7251 rejected-cache recovery" — i.e. the PR is a downstream of two merged cleanups and is consistent with the standing "do not replace engine owners" clause in CLAUDE.md §House laws. |
| base | fresh `origin/main` at PR-body merge base (the local worktree was opened from current main, not from an occupied sibling). |
| files | **13 paths, +1126 / −96** — `admin/README.md` (+11), `admin/static/app.js` (+205 / −90), `admin/static/styles.css` (+81), `research/ADMIN_PANEL_REVAMP_2026-09-21.md` (+74, ADDED), `research/evidence/admin-revamp-20260921/{browser-receipt.json,desktop-overview.png,mobile-navigation.png,mobile-overview.png,source-receipt.json}` (+469 binaries), `scripts/qa/admin_workspace_smoke.py` (+158, ADDED), `tests/test_admin_navigation_ux.py` (+123 / −2), `tests/test_admin_program_watch.py` (+1 / −1), `tests/test_admin_research_tools.py` (+4 / −3). |
| half-B label | **half-B (admin-shell revamp — navigation, overview rebuild, recoverable error path, browser-only smoke; no engine rewrite, no engine-owner change, no auth/CSRF/Caddy boundary change, no live deployment mutation, no specialist-route deletion).** PR body explicitly enumerates what it does not change: "No write retry/abort and no server-auth/CSRF change … No publishing, customer, payment, messaging, access or deployment action was exercised as a test. No Vercel or deployment-path change." |
| scope | (a) reduce visible everyday links from 54 to 13 while keeping all 54 routes searchable and addressable; (b) rebuild Overview around truthful pipeline/service/resource/cost status and four daily-work links, removing the unsupported Analytics "Live" claim, the false "running normally" service wording, and the duplicated dispatch buttons; (c) introduce `#/page/<id>` bookmarkable URLs, reload/back behavior, unknown-route recovery, retryable async rendering, GET/body deadlines, last-good snapshot retention, and fail-closed session verification; (d) native keyboard controls; mobile drawer inert/focus and one-time wiring; responsive overview/topbar; (e) a loopback-only browser smoke script that blocks non-GET/HEAD requests; (f) committed screenshot evidence + `research/ADMIN_PANEL_REVAMP_2026-09-21.md` audit/remediation record. |
| durable owner | none added. The PR is **a UI shell revamp of the existing admin console**, not a new control plane; the engine owners (`engine/health`, `engine/services`, `engine/cost`, etc.) are preserved and remain the source of `SUMMARY.*` fields. The research doc explicitly notes "rather than … creating another control plane". |
| collision / scope note | The PR does NOT introduce a new control plane (`agentos/`, `control_plane/`, `strategic_state.yml`, etc. are untouched). It does not touch `templates/`, `site/`, `site/assets/css/`, or any user-facing page. The `admin/` tree is internal/operator-only. |
| checks | PR body reports: 15 focused navigation/cache/JS tests passed; 63 program-watch/research/local-route rechecks passed; 17 browser checks passed across 54 route failure paths; zero unhandled browser errors and zero writes attempted (loopback guard); desktop 1440×960 + mobile 390×844 screenshots reviewed; `node --check admin/static/app.js` + `git diff --check` PASS; **full admin suite: 1394 passed in 145.49 s**. The first broad invocation was interrupted at 100% by a stalled git-status guard; its zero-byte lock was inspected for live owners before removal; the rerun above exited cleanly. |
| gating scripts | `scripts/check_plain_language.mjs` — **DOES NOT EXIST in macro** (terminal-side only; `find . -name check_plain*.mjs -not -path '*/node_modules/*'` returns no matches). Plain-language discipline on macro is read against the standing design-doctrine rules (`docs/DESIGN_DOCTRINE.md`, "Glance tier = state + plain-word stance under hard word budgets; technicals demoted to hover/popover/detail pages; banned vocab: internal state/study names, untranslated stats, raw slugs"). `scripts/check_validated_claims.py` exists but its `SCAN_GLOBS` (line 73) covers `templates/`, `site/`, and `site/prophet/` — **NOT `admin/`**. So the operator-facing admin copy this PR introduces is read manually against the standing rule, not scanned by the CI gate. `scripts/check_design_system.py`, `scripts/check_runtime_style_injection.py`, `scripts/check_ui_visual_evidence.py` exist but are scoped to design-system surfaces and are correctly not invoked on a `admin/`-tree change. |

## Diff content (scoped to this PR)

The PR is bounded to the existing admin console's shell — navigation, overview, error/retry, drawer focus, browser-only smoke — plus its tests, evidence and audit record. The 13 touched files are summarized below at the level of detail the three audits need.

**`admin/static/app.js` (+205 / −90, MODIFIED) — the bulk of the change**

Navigation inventory (`NAV_GROUPS`):
- Was 11 groups / 54 items all expanded by default, with the comment block at the old top of `NAV_GROUPS` carrying the operator's 2026-07-29 verbatim rationale for the original 19-item Marketing split. That verbatim rationale is removed (the new block carries a one-line replacement: "One inventory, organized by operator task. Specialist tools stay searchable and keep their route IDs; they are not deleted or presented as everyday work."). This is a comment-only loss; no behavioral claim is dropped, and the operator's original 2026-07-29 rationale is preserved in the git history (`git log -p admin/static/app.js`).
- Now 6 groups / 54 items: **3 primary groups (`Workspace`, `Customers`, `Publishing`, 13 items total)** with `primary: true` rendered as flat nav; **3 specialist groups (`Intelligence & research`, `Publishing tools`, `Platform & settings`, 41 items total)** rendered as native `<details>` disclosures that show their `<span class="nav-count">` and auto-expand when the current page lives inside them. Every route ID is preserved — the regression test `tests/test_admin_navigation_ux.py` enumerates all 54 expected IDs and asserts total count, primary count, and uniqueness.
- Search: `nav-label` now matches `label + id-with-spaces + group-label.toLowerCase()` — i.e. typing "ai cost" hits "AI Cost", "cost", and the "Platform & settings" group. The new `searching` state preserves each `<details>` element's `open` value before search started so collapsing a search restores the previous open state — a small correctness gain that prevents "search → close → all collapsed" surprise.

Routing:
- New `go(id)` adds: hash-based `#/page/<encoded-id>` URLs (pushState on navigation, replaceState on first load); explicit guard against unknown routes (calls `adminPageFailure("overview", new Error("This admin page does not exist."), () => go("overview"))`); wraps `RENDER[id]()` in a `runAdminRender(id, render)` boundary that catches async errors and renders the recovery screen instead of leaving a dead panel.
- New `route()` wraps each branch in `try/catch`, uses the regex `location.hash.match(/^#\/page\/([^/]+)$/)` for the new address scheme, and throws on unknown hashes so malformed URLs are recoverable instead of silently dropping back to overview.
- New `adminPageFailure(id, error, retry)` renders a `<section class="card admin-page-error" role="alert">` with `<h2>This page could not be loaded</h2>`, an honest subhead, two buttons ("Try again" → `clearApiCache(); retry()`, "Open overview" → `go("overview")`), and a `<details class="tech-details">` foldout for the technical detail. The `Try again` button is the operator recovery path; the `Open overview` button is the never-stranded path.

Read discipline:
- New `readAdminResponse(path, opts)` is a wrapper around `fetch()` + `readJson()` that applies a 30 s `AbortController` timeout **only** when the method is GET and no caller-supplied `signal` is present. The PR body and the inline comment ("Bound reads, never auto-retry or abort an operator write: a timed-out write may already have executed") explicitly forbid applying the timeout to non-GET requests. This is the correct fail-closed behavior — a timed-out POST may have reached the server, and silently aborting it could double-bill a customer. The body-parsing step is inside the deadline so a slow body can't outlive the read window.
- `api(...)` is updated to use the wrapper. The 401 → `clearApiCache()` + `showLogin()` path is preserved; cache TTLs are unchanged.

Session verification (PR body claim: "fail-closed session verification"):
- The PR adds the inline comment "Validate the actual session response and remain behind login when verification fails" — see the `research/ADMIN_PANEL_REVAMP_2026-09-21.md` repair table row "Failed session transport fabricates an authenticated local state". This is an in-repo note, not a copy line; the actual code change lives in `tests/test_admin_navigation_ux.py` (regression tests assert that a startup that returns `auth_enabled:false, authenticated:true` does NOT bypass login). The session-verification behavior itself is the preserved #7251 cleanup; this PR adds the regression that pins it.

Last-good snapshot retention:
- `refresh()` is reordered so `SUMMARY` is replaced ONLY after the new summary validates (the comment in the audit record says "Validate the next summary before replacing the previous snapshot"). This is a defensive re-order, not a new feature — and it is the right altitude: an operator who has been looking at a stable Overview should not see it blank-out because a single transient refresh failed.

Header + overview rebuild (`renderHeader()` + `RENDER.overview`):
- `renderHeader()` now reads `healthKnown` + `serviceKnown` from `adminOverviewModel(SUMMARY)`. The "Healthy" badge requires **both** checks to be known and healthy; otherwise the badge is "Attention" or a state that reflects partial knowns. This kills the previously false "Healthy" header state when service checks were actually unavailable.
- `RENDER.overview` now builds a single `<section class="admin-home">` with four named regions: header (title + subtitle + Refresh button), `admin-status-grid` (4 metric cards: Data pipeline, Background services, Server resources, Estimated AI cost), `admin-attention` (operational notices list, with a fallback "No issues reported by the available pipeline and service checks" row when nothing is wrong), and `admin-actions` (Daily work: Customer support / Publishing queue / Users & access / Builds & releases) plus a collapsed `<details class="admin-secondary">` holding the dated Research alerts & program watches.
- **Status truth:**
  - Data pipeline: `!healthKnown ? "Unavailable" : hh.healthy ? "Healthy" : "Needs attention"`. Detail: `${ok}/${total} sources · last run ${fmtAge(hh.age_hours)} ago` when known, else `"No current source report"`. Tone: `warn` when `!healthKnown || !hh.healthy`, else `ok`.
  - Background services: `!sv.available || typeof sv.healthy !== "boolean" ? "Unavailable" : sv.healthy ? "Running normally" : "Needs attention"`. Detail: `${ok}/${total} services running` when counts are known, else `"Live service checks unavailable"`. Tone: `""` when unknown, `ok` when healthy, `bad` when unhealthy.
  - Server resources: `sys.available && Number.isFinite(mem.used_pct) ? "${mem.used_pct}% memory" : "Unavailable"`. Detail: `${disk.used_pct}% disk used` or `"Host metrics are not available in this environment"`. No tone.
  - Estimated AI cost: `Number.isFinite(c.monthly_usd) ? "${fmtUSD(c.monthly_usd)} /mo" : "Unavailable"`. Detail: `Number.isFinite(c.effective_daily_usd) ? "${fmtUSD(cost.effective_daily_usd)} /day · estimate, not billed spend" : "No current cost estimate"`. No tone.
- All four metrics explicitly state "Unavailable" / "Needs attention" / "No current source report" / "Host metrics are not available" / "estimate, not billed spend" instead of inventing green checks. This is the design-doctrine "nulls printed, not hidden" stance applied to the operator's own console. The cost card explicitly says "estimate, not billed spend" — that copy is the operator-facing version of "this is a derived number, not an invoice".
- The Refresh button: `clearApiCache(); await refresh(); if (CURRENT === "overview") await RENDER.overview();` with `try/catch` showing the `toast("Status could not be refreshed. The previous snapshot is still shown.", true)` — i.e. the last-good-snapshot retention is observable to the operator as "the previous snapshot is still shown".
- Daily work links are real `<button>` elements with `data-admin-go` and the four pre-existing nav icons (`support_tickets`, `marketing_outbox || content`, `users`, `deploy`). The "Deploy actions are unavailable. Run history remains in Build & Deploy" sub appears only when `!meta.has_token` — i.e. an honest disclosure that the link goes somewhere that is itself honest about not having write access.
- The Research alerts & program watches section is wrapped in a `<details>` with `<summary>Research alerts & program watches<span class="sub">${alerts === null ? "Status unavailable" : `${alerts} alert records`} · not site incidents</summary>` — the `not site incidents` text is the explicit operator-facing truth that historical research records do not become live operational incidents. The body says "These are research records, separate from site operations. Check each record's date before acting." — this is the right altitude for the operator.

Mobile drawer + keyboard:
- New `setSidebarOpen(open)` adds `mobile = matchMedia("(max-width: 900px)").matches`; on mobile, sets `sidebar.inert = !open` and `aria-hidden`; on close, returns focus to `#sidebarToggle`; on open, focuses `#navSearch`. The `wireSidebarDrawer()` function adds a once-only flag (`ADMIN_DRAWER_WIRED`) so repeated `boot()` calls don't multiply event listeners, and adds Tab-cycle focus containment inside the open drawer.
- These are exactly the touch-accessibility / focus / inert / keyboard patterns the operator asked for in the original audit. The PR does NOT add a new framework — it uses native `<button>`, `<details>`, `inert`, and `aria-current="page"`.

Browser smoke (`scripts/qa/admin_workspace_smoke.py`, +158 NEW):
- Loopback-only (`urlparse(base).hostname in {"localhost", "127.0.0.1", "::1"}`), refuses non-loopback. `parser.error(...)` fails closed.
- Route guard (`guard(route)`): blocks all non-`GET`/`HEAD` requests (`request.method not in {"GET", "HEAD"}` → `route.abort()`), records each blocked write in `receipt["blocked_writes"]`, and blocks all cross-origin requests. The smoke therefore proves the UI recovery paths WITHOUT exercising any write path against any backend. This is exactly the "no production integration proof" boundary the PR body states.
- 17 assertions covering: 13 visible primary links + all 54 routes retained, research section starts collapsed, search reaches collapsed specialist pages, native keyboard activation updates URL, reload retains selected page, browser Back returns to overview, malformed route is recoverable, retry recovers failed page without browser reload, mobile has no horizontal overflow, closed mobile nav is inert, opening mobile nav focuses search, closing nav returns focus and makes it inert. The 54-route failure sweep is the smoke's main surface coverage proof.
- Three committed screenshots (`desktop-overview.png`, `mobile-overview.png`, `mobile-navigation.png`) at 1440×960 / 390×844. The `visual_scope` field on `browser-receipt.json` records `"existing dark English admin, desktop 1440x960 and mobile 390x844"`.

Tests:
- `tests/test_admin_navigation_ux.py` (+123 / −2): adds navigation UX tests pinning the 13/54 split, primary counts, route uniqueness, and the keyboard activation path.
- `tests/test_admin_program_watch.py` (+1 / −1) and `tests/test_admin_research_tools.py` (+4 / −3): stale copy/navigation assertion repairs called out in the PR body's verification section.

Evidence record (`research/ADMIN_PANEL_REVAMP_2026-09-21.md`, +74 NEW):
- Audit-and-remediation plan with a findings/repairs table (16 rows), explicit "what this PR does NOT change" sections, and a "remaining work, in order" list that explicitly enumerates (1) authenticated workflow audit, (2) publishing consolidation, (3) intelligence consolidation, (4) performance and stale-success races, (5) truth/maturity policy, (6) theme/language and deeper accessibility. The fact that the PR documents its own remaining-work backlog at length is itself a truth posture — the operator sees the full extent of the unfinished admin remediation program, not a "we're done" claim.

## Plain-language findings

The admin console is **operator-facing**, not customer-facing. Standing plain-language discipline (`docs/DESIGN_DOCTRINE.md`, "Glance tier = state + plain-word stance under hard word budgets; technicals demoted to hover/popover/detail pages; plain-word null disclosure") applies to operator copy as well, with the relevant word budget being operator-glance rather than customer-glance. The PR introduces the following operator-facing copy strings (the complete set is in `RENDER.overview`, `renderHeader`, `adminPageFailure`, `adminOverviewMetric`, and the search/nav elements):

**Eyebrow / header:**
- `"Operations workspace"` — eyebrow for the Overview page header. Concrete, glance-tier; says what page you're on without inventing a metaphor. PASS.
- `"Admin overview"` — page `<h1>`. Concrete. PASS.
- `"Check the system, handle customer work, and review publishing."` — subtitle. Plain-word operator action verbs. PASS.
- `"Refresh status"` / `"Refreshing…"` — Refresh button states. The `…` is the standard progress punctuation, not an over-claimed ellipsis. PASS.

**Metric labels (the four admin cards):**
- `"Data pipeline"` / `"Background services"` / `"Server resources"` / `"Estimated AI cost"`. All four are domain-correct plain words — no jargon, no marketing-style verbs ("supercharge", "unleash"), no invented category names. PASS.

**Metric values (the truthful-status rebuild):**
- `"Healthy"` / `"Needs attention"` / `"Unavailable"` (pipeline) — three-state enum with no false-green. The previous hard-coded "Live" and "running normally" copy is gone. PASS.
- `"Running normally"` / `"Needs attention"` / `"Unavailable"` (services) — same pattern. The "running normally" copy is preserved as a truth-conditional — it now ONLY renders when `sv.healthy === true`. PASS.
- `"${mem.used_pct}% memory"` / `"Unavailable"` (server) — concrete when known, honest when not. PASS.
- `"${fmtUSD(cost.monthly_usd)} /mo"` / `"Unavailable"` (cost) — and the detail `"${fmtUSD(cost.effective_daily_usd)} /day · estimate, not billed spend"` explicitly disclaims the billed-spend framing. This is the **exact** design-doctrine "nulls printed, not hidden" + "even when the honest answer is 'watch — don't chase'" stance, applied to operator money. PASS.

**Metric detail (sub):**
- `"${ok}/${total} sources · last run ${fmtAge(hh.age_hours)} ago"` / `"No current source report"` — the second one is the explicit null disclosure. PASS.
- `"${ok}/${total} services running"` / `"Live service checks unavailable"` — same. PASS.
- `"${disk.used_pct}% disk used"` / `"Host metrics are not available in this environment"` — the second one names the environment (this is the operator's own machine) rather than the abstract "unavailable". PASS.
- `"${fmtUSD(cost.effective_daily_usd)} /day · estimate, not billed spend"` / `"No current cost estimate"` — see above. PASS.

**Operational attention section:**
- `<h2>Operational attention</h2>` — concrete, plain. PASS.
- The notice copy is generated from `notices` with three possible titles: `"Pipeline status unavailable"`, `"Data pipeline needs attention"`, `"Background services need attention"`. Each title is a concrete operational state, no marketing verbs. PASS.
- Fallback: `"No issues reported by the available pipeline and service checks."` — note the qualifier "available": when service checks are unavailable, the operator sees this line **alongside** the explicit "Service monitoring is unavailable; this is not a complete system health check." note below. This is the right null-disclosure pattern. PASS.

**Coverage notes (the `!serviceKnown` and `!meta.has_token` paths):**
- `"Service monitoring is unavailable; this is not a complete system health check."` — names the partial-knowns truth rather than hiding it. PASS.
- `"Deploy actions are unavailable. Run history remains in Build & Deploy."` — the second clause tells the operator where to find what is still available. PASS.

**Daily work section:**
- `<h2>Daily work</h2>` / `"Open the queue or control you need."` — concrete, operator action verbs. PASS.
- Four work-link titles: `"Customer support"` / `"Publishing queue"` / `"Users & access"` / `"Builds & releases"`. Each is paired with a one-line sub: `"Review tickets and reply to customers."` / `"Review content before it is published."` / `"Find accounts and manage customer access."` / `"Inspect runs before triggering a deployment."`. Each sub is a verb-first operator action sentence with a specific target. The third one's verb "Find accounts and manage customer access" is the most honest possible description of the Users page (which is a customer-facing account-search/management surface, not a "Customer Success Hub" or "Account Intelligence" — the kind of invented name the design doctrine bans). PASS.

**Research alerts section:**
- `<summary>Research alerts & program watches<span class="sub">${alerts === null ? "Status unavailable" : `${alerts} alert records`} · not site incidents</summary>` — the explicit `not site incidents` qualifier is the plain-language truth posture this entire PR is built around. PASS.
- Body: `"These are research records, separate from site operations. Check each record's date before acting."` + `"Review experiments"` button. Three plain-word operator instructions: it is research, it is separate, check the date. PASS.

**Recovery screen (`adminPageFailure`):**
- `<h2>This page could not be loaded</h2>` / `"Check your connection, then try again. You can still open another admin page."` — concrete, operator-action verbs ("check", "try again", "open another"). The second clause is the never-stranded guarantee. PASS.
- Buttons: `"Try again"` / `"Open overview"`. Concrete. PASS.
- `<details class="tech-details"><summary>Technical details</summary>${esc(error.message || "Page unavailable")}</details>` — the technical detail is demoted to a foldout, exactly the "technicals demoted to hover/popover/detail pages" design-doctrine pattern. The fallback `"Page unavailable"` is plain English, not a stack trace. PASS.

**Sidebar search:**
- `"Find an admin page…"` placeholder — concrete. PASS.
- `"No matching pages. Try another name."` empty state — second clause is an operator action (try another), not a passive "no results". PASS.

**Nav specialist disclosure:**
- Specialist groups render as `<details>` with the group label as `<summary>`, plus `<span class="nav-count">${g.items.length}</span>` (e.g. "14", "18", "9"). The count is honest arithmetic, not a marketing-style "10+ tools" or "everything you need". PASS.

**Toast (catch on Refresh failure):**
- `"Status could not be refreshed. The previous snapshot is still shown."` — concrete, names the recovery. PASS.

**Header copy (`renderHeader`):**
- The "Healthy" / "Attention" badge now requires both `healthKnown && serviceKnown` before showing "Healthy" (the previous behavior showed "Healthy" even when service checks were unavailable). This is a copy-level truth improvement: the badge now matches the design-doctrine "Healthy" / "Needs attention" / "Unavailable" three-state pattern. PASS.

**Specialist nav labels (`NAV_GROUPS` items):**
- All 54 route IDs and labels are preserved verbatim from the previous inventory. The only changes are (a) grouping under 6 buckets instead of 11, (b) the "Marketing · …" prefixes are replaced by either moving under "Publishing tools" or — in the four primary cases — renaming "Floor" → "Publishing overview" (which is the operator-facing plain-word for what Floor actually shows: a publishing overview), "Marketing Outbox" → "Outbox" (parent context makes the prefix redundant), "Marketing Content" → "Content Studio" (the page already calls itself Content Studio in its title), "Marketing Publish" → "Publisher" (same). Each rename is a content-rooted plain-word shortening, not a marketing-flavor injection. The other 50 labels are unchanged. PASS.

**Plain-language residual issues:**

1. **The header `<title>` and the topbar title** (`setTopbarTitle(TAB_LABELS[id] || id)`): unchanged behavior; uses the nav label as the topbar title. PASS.
2. **`"This admin page address is not recognized."`** (malformed-route error) — concrete, plain. PASS.
3. **`"This admin page does not exist."`** (unknown-route error) — concrete, plain. PASS.
4. **`"The admin server took too long to respond. Try again."`** (read timeout) — concrete, names the cause, names the operator action. This is the operator-facing version of the design-doctrine "watch — don't chase" stance: "the server is slow; try again later; don't click 30 times". PASS.

**Plain-language verdict: PASS.** The PR introduces a substantial block of new operator-facing copy (Overview rebuild, recovery screen, search empty state, refresh toast, sidebar specialist disclosures, mobile drawer focus states). Every new string is at the operator-glance tier, uses concrete verbs over invented jargon, names partial-knowns explicitly ("estimate, not billed spend"; "not a complete system health check"; "not site incidents"), demotes technical detail to foldouts, and removes the previously false-positive copy ("Live", "running normally" without `sv.healthy === true`, "Healthy" badge without service checks). No new internal state/study name leaks to operator copy. No untranslated statistic is shown (the admin is English-only by standing product policy, and the PR explicitly disclaims light/ZH parity — see Theme findings). No banned design-doctrine vocabulary appears. The PR body and the research record are both operator-facing and use concrete falsifiable prose throughout.

## Theme findings

The TP-0 standing rule (`docs/DESIGN_DOCTRINE.md`, "dark and light are TWO art directions, not one skin … Token substitution alone is never proof of a light design") binds UI packets that claim light-mode or dual-theme. PR #7602 explicitly **does not claim light-mode or Chinese-language parity**. The `research/ADMIN_PANEL_REVAMP_2026-09-21.md` doc, the PR body, and the `visual_scope` field on `browser-receipt.json` all state the same thing: `"existing dark English admin, desktop 1440x960 and mobile 390x844"`. The "Remaining work" section of the research doc lists "Theme/language and deeper accessibility" as item (6), explicitly OUT of this batch's scope.

**Summary: PASS (scope-bounded — dark/English only, dual-theme is an inherited product-wide gap, not this PR's claim).**

**Specific theme deltas worth recording:**

1. **No new palette, no new token family, no new component primitive**: the 81 lines added to `admin/static/styles.css` use only existing tokens (`var(--border)`, `var(--accent)`, `var(--muted)`, `var(--text)`, `var(--surface)`, `var(--surface2)`, `var(--ok)`, `var(--warn)`, `var(--bad)`, `var(--r-sm)`, `var(--r-md)`, `var(--dur)`, `var(--ease)`). The PR body's claim "Reuse the admin console's existing Observatory tokens, materials and native disclosure components. No new palette, external UI framework, generated-data contract or engine rewrite" is verified by the CSS diff: every color, radius, and motion value is an existing token reference. PASS.
2. **The new components use the existing `admin-*` / `nav-*` class names** scoped to the admin tree (not bleeding into `templates/` or `site/`). The `admin-status-grid`, `admin-metric`, `admin-attention`, `admin-work-grid`, `admin-work-link`, `admin-secondary`, `admin-page-error`, `nav-search`, `nav-specialist` are all admin-scoped. The topbar CSS reset (`.topbar { display: grid … }`) is a scoped topbar change to fix the "legacy CSS leaks into the new header" finding; it does NOT touch the global `header` rule that affects other pages. PASS.
3. **No runtime style injection**: the JS diff uses class-name-driven CSS (`class="admin-metric admin-tone-warn"`), template-string HTML with proper escaping (`esc(label)`, `esc(value)`, `esc(error.message)`), and inline `style` only where the existing code already used inline `style` (e.g. `style="margin-left:8px"` is preserved only on the old buttons that are removed; the new buttons have no inline style). PASS.
4. **Native disclosure (`<details>`/`<summary>`) is used** instead of a JS-driven accordion. The CSS provides custom `::-webkit-details-marker` removal and a `::before` chevron that rotates `[open]` — same pattern the existing `.admin-secondary > summary::before { content: "+"; } → "[open] { content: "−" }` already uses elsewhere in the admin CSS. The chevron uses `transform: rotate(90deg)` which honors `prefers-reduced-motion: reduce` (the media query at the bottom of the new CSS disables the transition). PASS.
5. **Responsive composition**: three breakpoints (`max-width:1200px`, `max-width:600px`, `max-width:360px`) reduce the status grid from 4 → 2 → 1 columns; the topbar becomes a 2-column grid with `grid-column: 1 / -1` for the metadata row on mobile; the work-grid collapses to one column. The mobile screenshots are at 390×844 (iPhone 14 Pro viewport). PASS.
6. **Reduced motion**: `@media (prefers-reduced-motion:reduce) { .admin-home *, .nav-specialist > summary::before { animation:none !important; transition:none !important; } }` — same pattern the existing admin CSS uses elsewhere. PASS.
7. **No light-mode claim**: the CSS has no `[data-theme="light"]` or `.light-mode` selectors; the new tokens are all light-agnostic (they reference theme variables, so when/if the admin gets a light theme it will inherit). The PR explicitly disclaims light-mode parity and lists it as remaining work item (6). PASS.
8. **No bilingual (EN/ZH) claim**: no `<span class="l-en">` / `<span class="l-zh">` patterns; no `data-zh` attributes; no localized enum labels in the nav. The admin is English-only by standing product policy (the design doctrine's "Bilingual (EN/ZH) UI" rule applies to `templates/` and `site/`, not to `admin/` — the operator console has never been localized). PASS.
9. **Evidence matrix**: the PR commits a `desktop-overview.png` and a `mobile-overview.png` and a `mobile-navigation.png` (the smoke script takes them at 1440×960 and 390×844 with `reduced_motion="reduce"`). The dark × EN × desktop 1440 / mobile 390 evidence matrix for the rebuilt shell IS satisfied. The matrix's light and ZH axes are correctly NOT claimed (the standing TP-0 evidence matrix is for packets that DO claim dual-theme / bilingual; this PR's PR body and research doc both disclaim those claims). PASS.

**Theme residual issues: none.** The PR is scoped to dark/English by its own admission, reuses existing tokens, uses native disclosure primitives, and commits the dark × EN × desktop/mobile evidence receipts it claims. The standing TP-0 light/ZH evidence matrix is correctly not invoked on this packet — it will be invoked on whichever future packet actually claims dual-theme parity for the admin console.

**Theme verdict: PASS (scope-bounded — dark/English only; dual-theme parity is correctly OUT of scope).**

## Validated-claims findings

The standing rule "The word 'validated' in user-facing text is CI-enforced (`scripts/check_validated_claims.py`)" binds every user-facing claim. PR #7602 introduces no new user-facing copy using the banned word family `validated / confirmed / proven / fired / refuted / thesis`. The `check_validated_claims.py` `SCAN_GLOBS` (line 73 of the script) does NOT cover `admin/`, so the CI gate does not scan this PR's operator-facing copy — but the same plain-language discipline that informs the gate was applied manually above (Plain-language findings).

**Summary: PASS.** No banned word family appears in operator-facing copy. The previously false-positive claims (the hard-coded `Live` Analytics tile, the unconditional `running normally` service line, the always-Healthy header) are removed. The new copy uses three-state truth enums (`Healthy / Needs attention / Unavailable`; `Running normally / Needs attention / Unavailable`; `${ok}/${total} … ago` vs `No current source report`) and explicit null disclosure (`not a complete system health check`; `not site incidents`; `estimate, not billed spend`).

**Specific validated-claims deltas worth recording:**

1. **The Analytics "Live" tile is removed.** Old: `${card("Analytics", `<div class="big" style="color:var(--ok);font-size:18px">Live</div><div class="sub">${m.integrations && m.integrations.umami ? "connected" : "tracking enabled"}</div>`)}`. New: no Analytics tile on Overview at all (Analytics is reachable via the "Customers" primary group). This eliminates an UNSUPPORTED "Live" claim that the previous Overview carried. PASS.
2. **The unconditional "running normally" wording is replaced with a truth-conditional.** Old: `sv.available ? `… ${sv.ok_count}/${sv.total}</div><div class="sub">running normally</div>`` (always renders "running normally" when `sv.available` is true, regardless of `sv.healthy`). New: `serviceValue = !sv.available || typeof sv.healthy !== "boolean" ? "Unavailable" : sv.healthy ? "Running normally" : "Needs attention"`. This is the same fix the audit doc lists as the "Failed services still sound healthy" repair row. PASS.
3. **The "Healthy" header badge requires both checks known.** Old: `const allOk = hh.healthy && (!sv.available || sv.healthy);` — false-green when `sv.available` is false. New: `const allOk = healthKnown && hh.healthy && serviceKnown && sv.healthy;` where `healthKnown = typeof hh.healthy === "boolean" && Number.isFinite(hh.age_hours) && hh.age_hours >= 0` and `serviceKnown = sv.available === true && typeof sv.healthy === "boolean"`. PASS.
4. **The hard-coded "Features" tile** (`enabled features count`) **and "Experiments" tile** (`ready experiments + next in Nd`) **are removed from Overview.** The PR's research doc lists these as the "Global experiment badge distracts from operations" and "Global counters" repairs. The Experiments data is preserved on its own page (linked via the research disclosure section's `"Review experiments"` button). This eliminates two more UNSUPPORTED "tracked" / "ready" claims that were sitting on every operator's Overview. PASS.
5. **The cost copy disclaims billed-spend framing.** Old: `Est. AI cost` with `monthly_usd` and `effective_daily_usd`. New: same two numbers, with the detail `"${fmtUSD(cost.effective_daily_usd)} /day · estimate, not billed spend"`. The `· estimate, not billed spend` is the explicit operator-facing disclaimer that this is a derived number, not an invoice. PASS.
6. **No "validated" / "confirmed" / "proven" / "fired" / "refuted" / "thesis" word appears in operator copy.** Grep of `/tmp/pr7602.diff` for the banned set:
   - `validated` appears only in the research-doc repair table as a verb inside the repair column ("Validate the actual session response", "Validate the next summary") — these are operator-facing process notes (do X, then Y), not customer-facing claims, and they sit in a `research/` audit document, not in `templates/` or `site/`. The standing rule is about user-facing text; the research doc is operator-internal.
   - `confirmed` / `proven` / `fired` / `refuted` — zero occurrences.
   - `thesis` — zero occurrences in operator copy. The string `macro_thesis` appears as a nav-route ID (unchanged from the previous inventory), not as a copy word. PASS.
7. **The recovery-screen copy is truth-conditional.** Old behavior: a failed `RENDER[id]()` left a dead panel with no recovery. New: the `<section class="card admin-page-error" role="alert">` shows the technical detail inside a `<details class="tech-details">` foldout, not in the main heading. The operator's first read is "This page could not be loaded" + "Check your connection, then try again" + two recovery buttons — i.e. the failure is named, the operator action is named, and the technical detail is demoted. The error message fallback `"Page unavailable"` is plain English, not a raw exception string. PASS.
8. **The Refresh-status toast is honest about partial refresh.** `toast("Status could not be refreshed. The previous snapshot is still shown.", true)` — names both the failure (could not be refreshed) and the recovery (previous snapshot is still shown). This is the operator-facing version of the design-doctrine "nulls printed, not hidden" stance applied to UI state. PASS.
9. **The "remaining work" section is itself a truth posture.** The research doc enumerates six categories of remaining work, the first of which is "verify the read contract, freshness, empty/error state and operator value for every specialist page" — i.e. the PR explicitly says "we have not audited every specialist page in this batch, that is the next batch". An operator reading this doc sees the full extent of the unfinished admin remediation program, not a "we're done with the admin" claim. PASS.

**Validated-claims residual issues: none.** The PR removes more unsupported claims than it introduces. The new copy is at the operator-glance tier with concrete verbs, three-state truth enums, and explicit null disclosure. No banned word family appears. The "remaining work" enumeration in the audit doc is itself a truth posture.

**Validated-claims verdict: PASS.**

## Overall verdict

**PASS.** PR #7602 is a half-B admin-shell revamp that rebuilds the navigation (54 → 13 everyday + 41 specialist), rewrites Overview to replace three previously false-positive claims ("Live" Analytics tile, unconditional "running normally" services line, always-Healthy header) with three-state truth enums + explicit null disclosure, adds bookmarkable `#/page/<id>` URLs and reload/back behavior, introduces a recoverable error path with `clearApiCache(); retry()` + "Open overview", wraps reads in a 30 s deadline (GET-only, no write auto-retry/abort), and ships a loopback-only browser smoke that blocks all non-GET/HEAD requests. The PR explicitly disclaims light-mode / Chinese-language parity (standing inherited product-wide gaps) and lists them as remaining work. The 1394-test admin suite passes; the smoke proves 17 browser assertions including all 54 route failure paths with zero writes attempted; the dark × EN × desktop 1440 × mobile 390 evidence matrix is satisfied (light × ZH × dual-theme is correctly not claimed on this packet). Operator-facing copy uses concrete verbs, three-state truth enums, and explicit null disclosure throughout; technical detail is demoted to `<details class="tech-details">` foldouts. No banned word family (`validated / confirmed / proven / fired / refuted / thesis`) appears in operator copy. No new palette, no new token family, no new component primitive — only existing admin tokens (`var(--border / --accent / --muted / --text / --surface / --surface2 / --ok / --warn / --bad / --r-sm / --r-md / --dur / --ease)`). No engine owner is changed. No auth / CSRF / Caddy boundary is changed. No production customer / payment / publishing / messaging / access / deployment mutation is exercised as a test. No second control plane is introduced.

The PR is a textbook half-B packet: a single bounded admin-shell revamp, scope-limited to the existing admin console's chrome, with falsifiable verification and explicit out-of-scope enumeration in both the PR body and the committed `research/ADMIN_PANEL_REVAMP_2026-09-21.md` doc.
