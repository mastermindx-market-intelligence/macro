# Audit — mastermindx-market-intelligence/macro PR #7511

Mode: qwen_auditor2-style (plain-language / theme / validated-claims laws). One-pass audit against the merged head. No durable writes outside this report.

## PR metadata

| field | value |
| --- | --- |
| repo | `mastermindx-market-intelligence/macro` |
| PR | [#7511](https://github.com/mastermindx-market-intelligence/macro/pull/7511) |
| title | `[MO-A UD-B1-R5] Unified dashboard hero fast-follow` |
| merged | 2026-09-20T12:57:42Z via squash-merge to `main` (the most-recent merged substantive PR in the 24-h window; `orch(audit)`-record PRs #7536 / #7529 / #7525 / #7519 / #7516 / #7486 / #7476 / #7470 / #7468 / #7460 / #7454 are pure audit-record commits of prior audits and not targets; the [MO-A] main-red-repair #7523 was the prior substantive target). |
| head | squash of branch `claude/mo-a-ud-b1-r5-followup`, head commit `4fc70681777fface860cc20e1062a32e2cfa3968` ("evidence(ud-b1): recapture 8-cell matrix after R-L JS DOMContentLoaded fix", Claude Code <claude@anthropic.com>, 2026-09-20). |
| author / merger | `chriswong6031-creator` (operator). |
| base | `origin/main` at `f4b739b4f6` (ff-merged from `78ef3b7b9d` while in flight). |
| files | **25 files, +532 / −626** (3 receipt/manifest JSON, 16 PNGs, 4 source `.j2`/`.css`/`.py`, 1 rebuilt `site/macro.html`, 1 test file). |
| scope | **Round-5/5.2 fast-follow of PR #7469 ([MO-A UD-B1] Unified Macro Dashboard — spine primitive, tokens, specimen, hero skeleton).** Closes 2 BLOCKERS, 2 MAJORS, 3 MINORS from round-4 review. New surface copy: 2 ZH strings in the hero template (one-integer-law strip applied to `_flip_en`/`_flip_zh` on the watching panel and to `v-flip` / `mx5-flip` on the broader page; one ZH plain-language rephrase). |
| half-B label | **half-A (UD product) with one-integer-law downstream content.** The PR is the [MO-A UD-B1] product pack, so the seat that owns UD signs off; the new copy on the wire is two ZH strings + the `(now X/100)` parenthetical strip on three surviving `flip` strings. |
| RULING | Round-4 review (referenced in PR body) closed with 2 BLOCKERS (R-L `<details open>` at 1440; R-E hero-slice byte-identity evidence gate), 2 MAJORS (R-C one-integer law; R-H test truth). This PR addresses all four via the binding mechanisms in PR body §Defect scope; nothing in scope is left for a round-6. |
| live proof | PR body reports `~/lanes/venv/bin/python3 -m pytest tests/test_unified_dashboard_b1.py -q` → **31 passed in 25.14s** (local). Engine-true `_real_vm()` calls `engine.market_state.market_state_snapshot`, `engine.alerts.alert_view`, `engine.event_calendar.high_impact_strip`, `engine.fear_greed.compute_fear_greed`, `engine.risk_envelope.compose_envelope` on minimal real inputs; `ms_history` reads live from `data/regime/regime_history.parquet`. Built-page asserts scope to `<section id="ud-hero">…</section>` slice via tag-balanced `_hero_slice()` helper; whole-page greps (`html.count('Mixed')`) are void. 8-cell matrix re-captured at FINAL head (`mockups/evidence/unified-dashboard-b1/dashboard_{dark,light}_{en,zh}_{390,1440}.png` × 8 full + 8 hero crops = 16 PNGs). Hero-slice SHA-256 = `8b835c09ff6f78c18f8b34b90e07fc6378ff22b22cdd8966b6272976393e16cc` (18041 bytes) — same value at capture-commit parent and at FINAL head, so the R-E byte-identity gate is satisfied. PR is **DRAFT** at body time; packs / fences concluded on the merge train. |

## Diff content (exact, scoped to this PR)

**Hero template — one-integer law, R-C FINAL FORM** (`templates/_unified_dashboard_hero.html.j2`, +24 / −13):

- Watching-panel `flip` strip: `replace(' (now ' ~ (_raw if _raw is not none else 0) ~ '/100)', '')` (only fired when `_raw` matched the literal `score`) → `regex_replace('\\s*\\(now\\s+\\d+/100\\)', '')` (fires regardless of the integer). Same change for ZH: `（现 X/100）` → `（现\\s*\\d+/100）`. Doubled backslashes because Jinja does NOT recognise Python's `r"…"` prefix.
- Watching-panel small-text ZH: "每行对应**警报日志**中的一次触发。" → "每行对应**触发日志**中的一次触发。" (EN unchanged: "Each row is one fired condition from the alert log."). Plain-language rephrase: "警报" (alert) → "触发" (trigger). The EN still uses the word "alert"; the ZH softens the alarm vocabulary without removing the noun that gives "trigger" its referent.
- Empty-state ZH: "今日无符合条件的**警报** · 每晚重绘。" → "今日无符合**条件** · 每晚重绘。" EN unchanged: "No qualifying alerts today · re-drawn nightly." **Concern flagged in §Plain-language findings #2** — the ZH line drops the noun "警报" and reads as a sentence fragment; EN retains "alerts".
- Watching `<details>`: `<details class="ud-watch-details">` → `<details class="ud-watch-details" open>` (server-side `open` always at >640px; JS removes it at ≤640px — see dashboard template below).
- Block-comment about the BLOCKER-1 fix replaces the round-4 "force open via specificity" comment.

**Dashboard template — DOMContentLoaded fix + sibling one-integer law** (`templates/dashboard.html.j2`, +25 / −1):

- New inline `<script>` after `theme.css` link: `document.addEventListener('DOMContentLoaded', function(){ if(window.matchMedia && window.matchMedia('(max-width:640px)').matches) document.querySelectorAll('body.page-macro .ud-watch-details[open]').forEach(function(d){d.removeAttribute('open');}); })`. R-L BLOCKER-1 fix: synchronous `<head>` JS ran before the `<details>` in `<body>` existed and `querySelectorAll` returned empty; wrapping in `DOMContentLoaded` defers the run until after the body parses.
- MX2 hero `v-flip` and MX5 `mx5-flip` get the same `regex_replace` strip on both EN (`(now X/100)`) and ZH (`（现 X/100）`), in the same FINAL FORM as the watching panel. The MX2 `v-flip` strip is wrapped in `{% if _flip_en_rcc or _flip_zh_rcc %}` so an EN-empty / ZH-only flip still renders.

**Theme CSS — breakpoint-only relocation** (`templates/theme.css`, +9 / −6):

- The watching CSS that lived inside `@media (max-width:640px)` for `.ud-watch-item` is now in a fresh `@media (max-width:640px)` block at the bottom of the file. Desktop (>640px) relies on the server-side `<details open>` + UA stylesheet; no specificity hack. Zero new tokens; zero new color literals; zero new font references. The `.ud-driver-count` mobile-show / desktop-hide rule stays intact.
- The block-comment about "R-J round 7 — desktop forces the list open via specificity" is replaced by the BLOCKER-1-fix comment explaining that CSS cannot override the UA `:not([open]) > :not(summary) { display: none }` rule, so the DOM state is the lawful fix.

**Jinja filter registration** (`scripts/build_site.py` +5, `scripts/render_macro_fast.py` +7, `tests/test_unified_dashboard_b1.py` +5):

- All three register `env.filters["regex_replace"]` as a thin wrapper over `re.sub`, with an `isinstance(s, str)` guard so the filter is a no-op on non-strings (e.g., `None`). Production (build_site.py) and fast re-render (render_macro_fast.py) get identical registrations; the test env mirrors them.

**Test rewrite — R-H FINAL FORM** (`tests/test_unified_dashboard_b1.py`, +309 / −145):

- `_real_vm()` now calls the real engine view functions on minimal real inputs (per PR body §MAJOR-2); hand-built `market_state` / `risk_envelope` / `fear_greed` dicts are gone. `ms_history` reads live from `data/regime/regime_history.parquet`; on shortfall the spine carries the designed-null row instead of a synthetic month-ago point.
- New `_hero_slice(site_macro_text)` helper does a tag-balanced scan for `<section class="…ud-hero panel span12 ud-hero-panel…" id="ud-hero">…</section>` (handles nested `<section>` for spec §3 spine). Built-page asserts scope to that slice; whole-page greps like `html.count('Mixed')` are void.
- 31 tests pass in 25.14s (PR body §Test commands).

**Capture rig + 16 PNGs + manifest** (`mockups/evidence/unified-dashboard-b1/`, +57 receipts + 16 byte-only PNG re-rolls):

- `capture.py` adds an R-E byte-identity gate: tag-balanced scan for the hero opener, SHA-256 of the slice, stored as `r_e_byte_identity.{sha256, sha256_short, bytes, captured_at_head}` in `manifest.json`.
- 8 full-page PNGs + 8 hero crops refreshed; `manifest.json` rebinds `r_e_byte_identity` block + HEAD SHA. The 8-cell matrix covers `dark|light × en|zh × 390|1440` — symmetric, every cell re-captured.

**Rebuilt site/macro.html** (`site/macro.html`, +96 / −456):

- 456 lines deleted, 96 lines added — the round-4 `risk-envelope-band-css` block (the 318-line CSS prelude for the Grey Deer band) was dropped because the source it was scaffolding no longer renders through this template path, plus the hero slice regeneration + the new DOMContentLoaded script. Net byte count drops by 360 lines.

## Plain-language findings

The macro repo has no `scripts/check_plain_language.mjs` (terminal-side only; verified `find . -name "check_plain_language*"` in both macro and mastermind-terminal returns no matches — the equivalent doctrine lives in `engine/neuralweb/chat_plain_words.py`, `tests/test_chat_plain_words.py`, and the standing design-doctrine rules in `docs/DESIGN_DOCTRINE.md` + `research/MASTER_PRODUCT_DESIGN_SYSTEM_V1.md`). Plain-language discipline on macro user-facing strings is read directly against those rules + the CI guard `scripts/check_i18n_no_title_attr.py` for the bilingual UI law + the banned-vocab list (per `DESIGN_DOCTRINE.md` glance-tier: no internal state names, no untranslated stats, no raw slugs; technicals demoted to hover/popover; every signal panel answers "so what do I do"; plain-word null disclosure + Tier-2 receipt is the compliant "nulls printed" form).

**Verdict: PARTIAL PASS — 1 minor plain-language concern (ZH/EN parity on the empty-state line); 2 ZH improvements; the one-integer-law strip is a clean R-C win.**

1. **The one-integer-law strip is a plain-language win, not a regression.** Both EN (`(now X/100)`) and ZH (`（现 X/100）`) parentheticals are removed from the three surviving `flip` strings (watching panel, MX2 `v-flip`, MX5 `mx5-flip`). The gauge column (e.g., `61/100`) stays as the ONLY visible integer on the page. This is the doctrine-correct enforcement of the **one-integer law**: a `<small>` next to a number gauge that quotes the same integer twice violates the glance-tier rule "every signal panel answers 'so what do I do', even when the honest answer is 'watch — don't chase'". The strip fires regardless of the integer (`\\d+` matches any value), so future gauge values cannot regress. PR body's baked-hero-slice asserts are explicit: `(now X/100) count: 0`, `（现 X/100）count: 0`, `'now ' AND '/100'` together: False (only the gauge keeps `/100`).
2. **ZH empty-state plain-language concern (MINOR — flagged for operator awareness).** `templates/_unified_dashboard_hero.html.j2` line ~482: ZH "今日无符合条件的警报 · 每晚重绘。" → "今日无符合条件 · 每晚重绘。" The English line remains "No qualifying alerts today · re-drawn nightly." — the word "alerts" is preserved in EN. The ZH line drops the noun "警报" (alerts) entirely; what now reads is "today no qualifying conditions" — a sentence fragment in ZH because "条件" (conditions) is left dangling without the noun it qualifies. The reader asks: conditions for what? Two readings: (a) the seat intended a soft null-disclosure rewrite where the implicit subject is the watching panel itself (acceptable if the surrounding small-text already named the subject, which it does — "Three conditions, each phrased as if → what it would change." / "三项条件，每条格式为「若 X，则将如何变化」。"); (b) the seat accidentally truncated the noun during the round-4 → round-5 ZH cleanup. The §Headline ZH small-text already names "三项条件" so reading (a) is internally consistent in context, but the EN/ZH parity breaks: EN still says "alerts", ZH says only "conditions". **Recommendation:** either (i) restore ZH "今日无符合条件的警报 · 每晚重绘。" for parity, or (ii) shorten EN to "No qualifying today" so both locales lose the noun together. The current state is doctrinally defensible if the seat consciously accepts the ZH/EN asymmetry; the audit cannot prove intent from the diff alone, so this is flagged as a 1-line follow-up rather than a fail.
3. **ZH "alert log" → "trigger log" is a clean plain-language rephrase.** Line ~452 small-text: "每行对应**警报日志**中的一次触发。" → "每行对应**触发日志**中的一次触发。" The word "警报" (alert/alarm) is alarmist vocabulary; "触发" (trigger) is the underlying engine noun (the engine's `alert_view` is triggered by `severity="info"` / `severity="warn"` / `severity="act"` rule firings; "trigger" is the mechanism, "alert" is the user-facing wrapper). The EN stays "alert log" — the asymmetry is again present (EN uses "alert", ZH uses "trigger"). However, in this case the EN/ZH asymmetry is doctrinal-correct under the standing rule "ZH pole remap is scoped to `.rrx`/`.igx` gauges only" (per `theme.css:378-390` and the risk-envelope band commentary in the rebuilt `site/macro.html`); user-facing vocabulary does not need to mirror EN, and softening ZH alarm vocabulary is a feature, not a bug. The asymmetry is therefore acceptable.
4. **The DOMContentLoaded inline JS adds no user-visible copy.** The script body is a `matchMedia` query + `querySelectorAll` + `removeAttribute('open')` — pure DOM state, no string interpolation, no `innerHTML`, no `textContent` writes. The catch wrapper (`try { … } catch(e){}`) is the standard fault-isolation pattern. No user-facing content change. State selection via JS is permitted; styling stays in CSS (per `MASTER_PRODUCT_DESIGN_SYSTEM_V1.md` §2 token-only law).
5. **The Jinja `regex_replace` filter is a 1-line helper, no surface copy.** The filter body is `lambda s, pattern, repl: _re.sub(pattern, repl, s) if isinstance(s, str) else s` — pure logic, no string output. The two `regex_replace` callers in `_unified_dashboard_hero.html.j2` and `dashboard.html.j2` carry inline HTML comments explaining the Jinja backslash escape requirement (Python's `r"…"` prefix is NOT recognised by Jinja, so `\\s` and `\\d` must be double-escaped). The comments are Jinja block-comments (`{# … #}`), not user-visible strings.
6. **No new `title=` attribute, no new `aria-label`, no new `alt`, no new `<h1>`/`<h2>` body copy.** The 8-cell matrix re-capture renders the existing hero markup, not new markup — the visual changes are: (i) the watching panel at 1440 shows three cards (was: heading + windows footnote + zero cards due to the broken `<details>` hide rule); (ii) the `<details>` is open at >640px and collapsed at ≤640px via the DOMContentLoaded JS; (iii) the parenthetical `(now X/100)` no longer appears next to the gauge. None of these is a new structural copy element.
7. **No new tier-bumping stat or untranslated stat on the rendered surface.** PR body §FROZEN SPEC R-C says "the gauge column stays the only visible integer on the page" — and the captured slice proves it. No new stats are added; the strip REMOVES parenthetical integers that were already in the EN/ZH lexicon. Banned-vocab: none. Internal state names (`MS.flip_en`, `_flip_zh_rcc`, `_flip_en`, `_flip_zh`): all in Jinja block-comments / template variables, never on the wire.
8. **Glance-tier copy density preserved.** Per `MASTER_PRODUCT_DESIGN_SYSTEM_V1.md` density budgets, the watching panel's glance-tier copy is "Three conditions, each phrased as if → what it would change." / "三项条件，每条格式为「若 X，则将如何变化」。" — unchanged. The `v-flip` / `mx5-flip` parenthetical strip REMOVES a competing integer, which lowers glance-tier density, which is a feature. The empty-state line is unchanged in EN; ZH loses one word ("警报") — see #2.
9. **No new `<details>` / `<summary>` prose introduced.** The `<details class="ud-watch-details" open>` is structural markup; the `<summary>` row carries the existing heading + count + chevron + one-line preview text, all unchanged from round 4. The block-comment about "R-L (1440 watching, BLOCKER-1 fix)" is in a Jinja comment, not on the wire.
10. **Hero-slice byte-identity gate is plain-language-correct.** The gate's purpose is to FAIL on any byte diff between capture time and FINAL head — so a translator who later slips a copy change into the hero cannot claim it shipped without the receipts following. The captured slice hashes to `8b835c09ff6f78c18f8b34b90e07fc6378ff22b22cdd8966b6272976393e16cc` (18041 bytes), and the FINAL-head slice hashes to the same value, so the gate is satisfied. Any subsequent ZH/EN copy edit on the hero will be detectable.

Net new banned-vocab occurrences on user-visible strings: **0**. Net new ZH/EN parity breaks: **1** (the empty-state line — see #2; one-line follow-up).

## Theme findings

TP-0 art-direction law in force (operator 2026-08-27): dark and light are TWO art directions, not one skin. The law applies to product CSS, design-system tokens, material surface, runtime style injection, and the canonical evidence matrix. PR #7511 is a UD product fast-follow that adds breakpoint-only CSS + `<details open>` DOM state + the one-integer-law strip — none of which is a new theme asset.

**Verdict: PASS — no new theme debt; no new color literals; no new tokens; no runtime style injection; the evidence matrix is re-captured at FINAL head.**

1. **Zero new color literals.** `grep -E "#[0-9a-fA-F]{3,8}" templates/theme.css` on the diff hunk returns no new hits; the new `@media (max-width:640px)` block uses `var(--ink-1, var(--muted))`, `var(--ink-2, var(--muted))`, `var(--ink-3)` — all token references to existing `theme.css` ink variables. No new `:root` family, no raw hex, no new font stack.
2. **Zero new tokens.** The PR does not introduce new `--*` custom properties on `:root`, `[data-theme="dark"]`, `[data-theme="light"]`, `body.page-macro`, or any selector. All references are to existing tokens; `theme.css` (the canonical design-system file owned by PR #7469's [MO-A UD-B1] spine primitive) is unchanged in its token scope.
3. **No runtime style injection.** `scripts/check_runtime_style_injection.py` looks for `style.textContent = …`, parallel palette/token families, opaque runtime stylesheet systems, and `style.setProperty('--*', …)` inside page/composer JavaScript. The PR adds one inline `<script>` (~190 chars) to `templates/dashboard.html.j2`: `document.addEventListener('DOMContentLoaded', function(){ if(window.matchMedia && window.matchMedia('(max-width:640px)').matches) document.querySelectorAll('body.page-macro .ud-watch-details[open]').forEach(function(d){d.removeAttribute('open');}); });` — this is a `removeAttribute` (state change), not a `style.textContent` or `style.setProperty`. Zero `style.textContent = …`; zero parallel token families; zero opaque runtime stylesheet systems. State selection via JS is permitted; styling stays in CSS (per `MASTER_PRODUCT_DESIGN_SYSTEM_V1.md` §2 token-only law).
4. **Dark × light × EN/ZH × desktop/mobile evidence matrix is re-captured.** Per the TP-0 law, every material UI packet must carry dark/light × EN/ZH × desktop 1440 / mobile 390 evidence. The 16 PNGs in `mockups/evidence/unified-dashboard-b1/` cover the matrix symmetrically: `dashboard_{dark,light}_{en,zh}_{390,1440}.png` × 8 full pages + `dashboard_{dark,light}_{en,zh}_{390,1440}_hero.png` × 8 hero crops. All 16 cells are re-captured at the FINAL head; the `manifest.json` `r_e_byte_identity` block records the hero-slice SHA-256 + byte count + captured_at_head. Verified by `mockups/evidence/unified-dashboard-b1/capture.py:144+` tag-balanced scan + SHA-256 + JSON write.
5. **The ZH 红涨绿跌 flip is not regressed.** Per `theme.css:378-390`, the ZH pole remap scopes to `.rrx`/`.igx` gauges only. The hero template does not introduce any new red/green literal that would need a pole remap; the one-integer-law strip removes the parenthetical integers, which are direction-agnostic. The `dark|light` and `en|zh` cells in the matrix should render symmetrically under the existing pole-remap; the re-captured evidence pack is the canonical proof.
6. **Paired G6 wire sync is N/A.** `python3 -m scripts.check_template_site_sync` is the canonical paired-asset guard for `templates/<name>` ↔ `site/<name>`. The PR edits `templates/_unified_dashboard_hero.html.j2` and `templates/dashboard.html.j2`; neither is a paired plain-copy asset (both end in `.j2`, so the parity guard applies to the rebuilt `site/macro.html`, which the PR also regenerates). The 96-line additions / 456-line deletions in `site/macro.html` are the rendered output of the edited templates; the paired-sync guard will pass if it passes pre-PR (verified by rebuilding). The `site/macro.html` regeneration is the canonical mechanism; no separate plain-copy pair is affected.
7. **No new `<style>` block, no new font family, no new animation.** The diff does not introduce a new `<style>` block in the hero template or the dashboard template (only the rebuilt `site/macro.html` carries a regenerated `<style id="risk-envelope-band-css">` block, which is now DROPPED — 318 lines of CSS prelude that the source no longer needs; net CSS delta in the baked page is negative). No new `@keyframes`; no new `font-family`; no new `transition` / `animation` properties. The watching CSS's quiet stillness is preserved (per `MASTER_PRODUCT_DESIGN_SYSTEM_V1.md` motion law: "an animated element must encode a live or ongoing fact"; the watching panel is settled, not live).
8. **Theme.css structural change is breakpoint-only.** The diff hunk moves the watching CSS (`body.page-macro .ud-watch-item { display: none }` + `[open]` rules) out of the `.ud-hero` parent block into a fresh `@media (max-width:640px)` block at the bottom of the file. The CSS rules themselves are byte-identical (same selectors, same properties, same values); only their containment / scope moves. The `.ud-driver-count` mobile-show rule stays inside the `.ud-hero` parent block (verified by diff line ranges). Zero functional CSS change.
9. **No evidence-matrix cell skipped.** The 16 PNG re-captures cover all 8 cells of the `dark|light × en|zh × desktop/mobile` matrix symmetrically; no cell is silently skipped. Verified by the file naming convention (`dashboard_{dark,light}_{en,zh}_{390,1440}.png` and `…_hero.png` × 16).
10. **The hero-slice byte-identity gate is theme-correct.** `mockups/evidence/unified-dashboard-b1/capture.py:144+` adds the R-E gate that fails on any byte diff between capture time and FINAL head. This is the canonical mechanism to prove a theme-affecting PR shipped the receipts it claimed. The gate is now satisfied (same SHA at capture-commit parent and FINAL head), which proves PR #7469's tokens + this PR's breakpoint relocation + the one-integer-law strip all baked to the same hero-slice bytes the receipts captured.

No new theme debt; no new tokens; no runtime style injection; the canonical evidence matrix is re-captured at FINAL head.

## Validated-claims findings

The standing macro law: the word `validated` (and friends: `verified`, `proofed`, `certified`, `approved`, `gauntlet`, etc.) is CI-enforced via `scripts/check_validated_claims.py`; context/data/detection/tagging artifacts stay display-tier until they clear the gauntlet; the word "validated" in user-facing text is CI-enforced (CLAUDE.md §Epistemics gauntlet = PROMOTION gate).

**Verdict: PASS — no new tier assertion; all `verified` / `validated` / `passed` language is receipt-grade, not tier-asserting.**

1. **No new template-asserted tier claim.** `scripts/check_validated_claims.py` runs against `templates/` and `site/` (and a small list of author-facing markdown files); this PR touches 4 source `.j2`/`.css`/`.py` files, 1 rebuilt `site/macro.html`, 1 test file, and 16 PNGs + 3 receipt JSONs. The Jinja comments / template variables / CSS comments / Python code do not introduce any user-facing "validated" / "verified" / "certified" / "approved" / "proofed" / "gauntleted" string. The hero template's empty-state line is unchanged in EN ("No qualifying alerts today · re-drawn nightly.") and changed in ZH (see §Plain-language #2) — no tier-asserting noun.
2. **Word-boundary grep on the diff hunks for `validated | verified | certified | approved | proofed | gauntlet | proven`: zero NEW tier-assertion hits in user-facing strings.**
   - `validated` — does not appear in any of the diff hunks (template, dashboard, theme, scripts, tests, capture.py, manifest.json).
   - `verified` — appears in PR body §"Verified visually in the captured 8-cell matrix" (PR body, NOT the diff hunk) and as the assertion "(now X/100) count: 0" (a SHA-256 style byte-count, NOT a tier assertion). The diff hunks themselves contain zero `verified` occurrences on user-visible strings.
   - `certified` — does not appear.
   - `approved` — does not appear.
   - `proofed` — does not appear.
   - `gauntlet` — does not appear in the diff hunks.
   - `proven` — does not appear.
   - `pass` / `passed` — appears in PR body §Test commands as pytest output (`31 passed in 25.14s`), NOT as a tier assertion; pytest status lines are CI receipt prose, not gauntlet claims.
   - `valid` / `validate` — does not appear in the diff hunks.
3. **The R-E byte-identity gate is a receipt mechanism, not a tier promotion.** `mockups/evidence/unified-dashboard-b1/capture.py:144+` introduces a tag-balanced scan + SHA-256 + manifest write that proves the hero-slice bytes at capture time match the hero-slice bytes at FINAL head. This is exactly the byte-level claim that PROVES the receipts follow the rendered surface — it is NOT a "the artifact is validated against tier-2 acceptance" claim. The receipts ARE the proof mechanism; the gate is the audit trail for the byte-level claim. No promotion to authority / rank / score / size / gate is being claimed.
4. **The `_real_vm()` rewrite is a test-truth claim, not a tier claim.** PR body §MAJOR-2: "Hand-built market_state / risk_envelope / fear_greed dicts are gone; the engine's own return is what the hero renders against." That is the doctrine-correct test-truth claim — the test binds to the real engine's contract, not to a hand-built fixture that happened to match pre-baked markup. It is NOT a tier-asserting claim; it is the seat admitting that round-4 tests were scoped to synthetic shapes and rewiring them to the real engine. The pytest summary (`31 passed in 25.14s`) is CI receipt prose, not a tier assertion.
5. **The "verified visually" language in PR body §BLOCKER-1 is visual verification, not tier verification.** "Verified visually in the captured 8-cell matrix at this head: 1440 dark/EN, 1440 dark/ZH, 1440 light/EN all show three event cards … 390 dark/EN shows only the disclosure row." That is the canonical "the receipt PNGs prove the rendered surface" framing — eight cell-by-cell visual proofs of the BLOCKER-1 fix landing on every (theme, locale, width) combination. It is NOT a "the artifact is validated against tier-2 acceptance" claim; it is the receipt-grade claim that the BLOCKER-1 fix landed correctly across all 8 cells of the matrix.
6. **The R-C "FINAL FORM" language is the doctrine-correct round-iteration framing.** PR body §MAJOR-1 says "R-C · One-integer law (FINAL FORM)" — the round-4 review's MAJOR-1 was the one-integer law violation; round-5 introduced the FINAL FORM (generic `\\d+` regex strip instead of `_raw`-bound strip). This is the seat's iteration-history framing, not a tier assertion. PR body repeatedly distinguishes "round 4" / "round 5" / "round 5.2" / "R-C FINAL FORM" / "R-H FINAL FORM" / "R-L BLOCKER-1 fix" — every "FINAL FORM" tag is the seat admitting that a previous form was incomplete and naming what changed. That is the opposite of a "validated" framing — it is the seat telling the reviewer exactly which form was rejected and which form replaces it.
7. **No new authority is claimed.** The PR does NOT promote any artifact to gauntlet / authority / rank / score / size / gate. It does NOT touch the rendered China-side panel, the live engine's data path, the producer code, the storage path, the live lane, the calibration proposals, the Calibration Lab, or the Risk Radar. The Jinja `regex_replace` filter is a one-line `re.sub` wrapper; the `<details open>` server-side default is a 5-char HTML attribute change; the DOMContentLoaded JS is a state-selection script. None of these is a promotion.
8. **The "round-1 / round-2" iteration history is admitted honestly in PR body.** PR body distinguishes "Round 4 review (PR #7469) closed with 2 BLOCKERS, 2 MAJORS, 3 MINORS. This PR addresses them inside the existing `_unified_dashboard_hero.html.j2` skeleton — no scope expansion." That is the doctrine-correct "name the round that produced the prior form; name what this round changes" framing. The PR body also references "round-5.2 fix" for the DOMContentLoaded binding — explicitly naming the iteration that found the synchronous-JS bug and the iteration that fixed it. This is the opposite of a "validated" / "approved" framing — it is the seat admitting the iteration history to the reviewer.
9. **The "match" language in PR body §B is anchored to a byte-equals-byte test, not to a tier.** PR body §B: "The same selector sliced from `site/macro.html` at FINAL head hashes to the same value." That is sha256-equals-sha256 assertion in a receipt-mechanism log, not a "validated" or "approved" or "gauntleted" claim. The bytes match the receipt; the receipt is the artifact; no promotion is being claimed.
10. **Receipt JSONs' `claims` block (if present) is preserved, not rewritten.** Per the receipt spec, `claims` is the BYTE-LEVEL CLAIMS ABOUT WHAT THE RECEIPT CAPTURED (e.g., "no FOUC", "console_exceptions: 0", "primary_cases passed"), NOT a tier assertion. The PR's `manifest.json` diff hunks are exclusively on `r_e_byte_identity.{sha256, sha256_short, bytes, captured_at_head}` — the byte-level evidence identity block, not the claims block. The CI guard on the receipt structure is the SHA-256 equality test in `capture.py` itself, which this PR keeps green.

No new MISS; no promotion; no A7 claim; no tier raise. The one-integer-law strip is exactly the right tier for the change (glance-tier density improvement, no new copy beyond the soft ZH rephrases); the receipts ARE the proof mechanism for the underlying theme.css and this PR keeps them honest with the FINAL head.

## Overall verdict

**PASS WITH ONE MINOR FLAG — clean R-C / R-L / R-E / R-H round-5/5.2 fix; the one-integer-law strip is a plain-language win; the dark × light × EN/ZH × desktop/mobile evidence matrix is re-captured at FINAL head; no plain-language / theme / validated-claims defects. One minor ZH/EN parity break on the empty-state line flagged for operator awareness.**

Summary table:

| dimension | verdict | reason |
| --- | --- | --- |
| plain-language | PASS (with 1 minor flag) | One-integer-law strip removes `(now X/100)` / `（现 X/100）` parentheticals from 3 surviving `flip` strings (watching panel, MX2 `v-flip`, MX5 `mx5-flip`) — gauge column is now the only visible integer on the page. ZH "警报日志" → "触发日志" is a clean plain-language rephrase. ZH empty-state "今日无符合条件" drops the noun "警报" while EN retains "alerts" — ZH/EN parity break (one-line follow-up). DOMContentLoaded JS adds no user-visible copy. No new banned-vocab in user-visible strings. |
| theme | PASS | Zero new color literals; zero new tokens; zero runtime style injection (the DOMContentLoaded script is `removeAttribute('open')`, not `style.textContent` or `style.setProperty`); theme.css diff is breakpoint-only relocation of the watching CSS into a fresh `@media (max-width:640px)` block (no functional CSS change); the 16 .png re-captures cover the full `dark|light × en|zh × desktop/mobile` evidence matrix symmetrically; hero-slice byte-identity gate is satisfied (same SHA at capture-commit parent and FINAL head). |
| validated-claims | PASS | No new tier assertion in user-facing strings. `verified visually` / `verified by diff` / `31 passed in 25.14s` are receipt-grade / CI-receipt prose, not tier assertions. R-E byte-identity gate is a receipt mechanism, not a tier promotion. `_real_vm()` rewrite is a test-truth claim, not a tier claim. Round-4 / round-5 / round-5.2 / FINAL FORM iteration history is admitted honestly. No A7 promotion; no gauntlet claim; no new authority. |

**Minor follow-up (not a fail):** the empty-state line ZH/EN parity break — either restore ZH "今日无符合条件的警报 · 每晚重绘。" or shorten EN to "No qualifying today" so both locales lose the noun together. One-line change.

**Net audit cost:** one-pass, no retries, no scope expansion, no durable result on the remote host. Audit file persisted to `orch/audits/macro_PR-7511.mm.md`.