# Audit — mastermindx-market-intelligence/macro PR #7467

Mode: qwen_auditor2-style (plain-language / theme / validated-claims laws). One-pass audit against the merged head. No durable writes outside this report.

## PR metadata

| field | value |
| --- | --- |
| repo | `mastermindx-market-intelligence/macro` |
| PR | [#7467](https://github.com/mastermindx-market-intelligence/macro/pull/7467) |
| title | `fix(macro): move market internals into Risk Radar` |
| merged | 2026-09-20T04:17:03Z via squash-merge to `main` (commit `96d3ddf6d5`). 24-h window covers this PR. |
| head | squash `96d3ddf6d500796573c0269da867409026586d94` (squash of branch `claude/risk-radar-integration-20260919`; PR body lists frozen head `56c537915eb33ae3e53eecdb5a3c0b1c031aff7e`; the squash collapsed them onto main). |
| base | parent of squash `1f98b7010e` (data nightly capital_structure 2026-09-20 — auto-advance). |
| files | **58 files**, +2209 / −745 (matches PR body claim). Touched: `templates/dashboard.html.j2` (−7/+5 net: removes standalone envelope include at line 8503, adds the same include inside `#dlg-risk` dialog at line 12562), `templates/_risk_envelope_band.html.j2` (+48/−244 — already-merely compact Risk Radar context, was page panel), `templates/_risk_envelope_band.css.j2` (+49/−308 — was a full-page band, now a compact inset), `templates/risk_envelope_live.js` (+23 — adds `relocateLegacyEnvelope()` idempotent compatibility bridge for previously rendered/cached pages), `site/macro.html` (+202/−184 — paired G6 wire sync of the relocated include), `site/risk_envelope_live.js` (+23 — paired JS sync), `mockups/evidence/risk-radar-integration-20260919/` (NEW: 25 PNGs + `manifest.json` 1085 lines + `interaction-metrics.json` 242 lines + `partial-render-receipt.json` + `EVIDENCE.yml` + `README.md` 26 lines + `REPORT.md` 22 lines + `smells.json` 86 lines + `button-proof.json` + `button_proof.py` + `capture_adapter.py` + `refresh_page.py` — 36-cell evidence matrix), `tests/test_risk_envelope_radar_integration.py` (+129, NEW), `research/grey_deer/RISK_RADAR_INTEGRATION_2026-09-19.md` (+55, NEW), `agentos/decisions/DEC-RISK-ENVELOPE-IN-RADAR-NOT-DASHBOARD-PANEL.md` (+36, NEW), `agentos/workstreams/WS-GREY-DEER-RISK-INTELLIGENCE.md` (+1, NEW DEC reference), `.github/ci/legacy-jobs.yml` (+8/−1 — registers the new test in `tests/__init__.py`-based contracts). |
| half-B label | **half-B (Risk Radar integration B-half of Grey Deer Risk Intelligence).** Chairman-directed removal of the second container's viewport cost (`DEC:RISK-ENVELOPE-IN-RADAR-NOT-DASHBOARD-PANEL`, `decided_by: chairman-chris 2026-09-19`, `reversibility: easy`). The accepted settled/live engine, distinct Trend/Stress/Policy semantics, promotion holds, source clocks and authority booleans are unchanged — only the GD-2 standalone-band placement and density are superseded. Bridge preserves the legacy id (`#risk-envelope-band`) and live-hook identities so the accepted live consumer keeps its identity on previously rendered/cached pages. |
| owner | GREY-DEER-RISK-INTELLIGENCE seat (WS:GREY-DEER-RISK-INTELLIGENCE; Skillpack pin 1.0.1/bootstrap 1). |
| live proof | `python3 scripts/check_validated_claims.py` → exit 0 (no NEW MISS in the new files; existing `site/macro_*` "validated" strings are unchanged and live in scope-out sibling pages). `python3 scripts/check_design_system.py --mode enforce-added --diff-file <(git diff 96d3ddf6d5^..96d3ddf6d5 -U0 -- templates/ tests/ site/ mockups/evidence/risk-radar-integration-20260919/manifest.json mockups/evidence/risk-radar-integration-20260919/EVIDENCE.yml)` → **`::notice title=design-system::R0 enforce-added: 0 blocking finding(s)`** (estate pre-existing non-blocking census: 18,972, unchanged). `python3 -m pytest tests/test_risk_envelope_radar_integration.py` — PR body reports **72 targeted UI/live-consumer tests passed**; the two bridge tests ride through `tests/test_macro_risk_dialog.py` per `legacy-jobs.yml` line 12409. |
| evidence matrix | **36 cells** captured against `site/macro.html` at frozen head `56c53791`: 12 rest cells (the canonical `capture_page_evidence` driver, dark/light × EN/ZH × desktop 1440 / tablet 820 / mobile 390, anonymous) + 24 forced `radar_open` / `radar_focus` cells via `capture_adapter.py` (real focus + Enter to open native Evidence, Escape closes). All 24 forced cells: `inside_radar: true`, `page_overflow: false`, `evidence_open` toggled by keyboard. Desktop context height 164.6875 px; standalone dashboard block is absent. `interaction-metrics.json` is the per-cell numeric receipt. `manifest.json` records the 24 console errors (all 404 on `/live/risk_envelope.json`, `release_publications.json`, `api/brain/me`, `/ws/tape` — the local HTTP server returns 404 for any `live/*` path because no collectors were run; the `page-level` rendering succeeded). |

The PR is the canonical half-B BUILD deliverable for Grey Deer Risk Intelligence's dashboard integration: a single Jinja include (`_risk_envelope_band.html.j2`) moved from being a first-level dashboard container to a compact inset INSIDE the existing Risk Radar dialog (`#dlg-risk .mx5-dlg-body`), with the same engine contract (`schema: mastermind.risk_envelope/v1`) preserved, an idempotent compatibility bridge for legacy cached pages, and the canonical 36-cell evidence matrix + per-cell interaction receipt committed.

## Diff content (exact, scoped to this PR)

**Templates (4 files, +119 / −545)**

`templates/_risk_envelope_band.html.j2` (+48/−244, **net −196**): the SAME include is now a compact Risk-Radar-context panel — `<section class="riskdlg-context" id="risk-envelope-band">` containing (i) `<h3>` with `<span class="l-en">Market internals</span><span class="l-zh">市场内部</span>`, (ii) a coherence stamp `Signals diverge` / `Signals align` / `Incomplete picture` (bilingual, no raw state codes), (iii) two reading cells `Trend` / `Stress` each with `Trend: Risk-on ▲` / `Stress: No stress flagged` bilingual labels, (iv) a designed-null disagreement clause `Some inputs are unavailable; this is not an all-clear.` / `部分数据缺失，不代表风险已解除。` printed only when `_stage is none` or `_ms.get('usable')` is false, (v) a settled stamp `Settled <source_session>` and live chip `Live · provisional <live_time>`, (vi) an expandable `<details>` Evidence table with `Source / Its own read / As of / Freshness` headers and `This session` / `Older than this session` / `Did not publish` coverage cells, (vii) a contradictions list `Where they disagree` with sentence-form prose `… reads X while … reads Y.` (no machine-text states), (viii) a machine receipt line `definition_id · bundle <id> · coverage <state> · coherence <state> (scope <scope>) · stage <stage> (<reason>) · … · engine/risk_envelope.py` (developer-facing receipt, hidden until the live-overlay hook fills it), (ix) the capital-controls footer `Capital controls: N active. Separate from the market readings.` / `资金限制：N 项生效。与市场判读独立。` (always present; explicit "Separate from" line is the doctrine-correct separation contract per `DEC:RISK-STATE-HAZARD-POLICY-SEPARATION`).

`templates/_risk_envelope_band.css.j2` (+49/−308, **net −259**): now a compact inset CSS — `#dlg-risk .riskdlg-context` with `margin-top:12px; padding:12px 16px; border:1px solid var(--line); border-radius:var(--r-card); background:var(--panel2)`. All colors via `var(--*)` tokens (no raw hex). The light theme ships with `html[data-theme="light"] #dlg-risk .riskdlg-context { background:color-mix(in srgb,var(--panel2) 60%,var(--panel)); box-shadow:none; }` — a token-mixed panel, not a token-swapped skin (CLAUDE.md §Theme art direction). The legacy band rails and large spacing are explicitly suppressed: `#dlg-risk .gde-head {border:0; padding:0; margin:0;}` and `#dlg-risk .gde-read {border:0; padding:0; margin:0;}` so the moved node does not impose its old page-band structure inside the dialog.

`templates/dashboard.html.j2` (−7/+5): the only structural change. Line 8503 — the standalone include `{% if mode != 'stocks' %}{% include "_risk_envelope_band.html.j2" %}{% endif %}` — is REMOVED. Line 12562 — INSIDE `#dlg-risk` dialog — the same include is ADDED with comment `{# Same engine, same Risk Radar entry point; no standalone envelope panel. #}`. The `<script src="risk_envelope_live.js"></script>` line 17779 comment is updated to point at the dialog instead of the removed page panel.

`templates/risk_envelope_live.js` (+23): adds `relocateLegacyEnvelope()` — idempotent compatibility bridge. On every tick: `if (!band || band.closest("#dlg-risk")) return;` (already-relocated or absent → no-op). Otherwise if `#dlg-risk .mx5-dlg-body` exists, the legacy `<section id="risk-envelope-band">` is detached from its old parent and re-attached INSIDE a freshly-created `<details class="mx-disc riskdlg-envelope-legacy">` placed before `.riskdlg-drivers`. Summary text is plain `Market internals · trend and stress` / `市场内部 · 趋势与压力`. If the dialog body is absent (cached page where Risk Radar has not yet mounted), `band.hidden = true; band.style.display = "none";` — suppressed, never duplicated. No feed consumer is cloned (the function moves the SAME node including all live-hook identities).

**Paired G6 site copies (2 files, +225 / −184)**

`site/macro.html` (+202/−184): the same remove-from-page / add-inside-`#dlg-risk` structural move; the rendered `_risk_envelope_band.html.j2` output keeps its bilingual labels and machine-receipt strings.

`site/risk_envelope_live.js` (+23): paired JS sync of the same `relocateLegacyEnvelope()` bridge.

**Evidence matrix (NEW, 17 files + 25 PNGs)**

`mockups/evidence/risk-radar-integration-20260919/manifest.json` (1085 lines): axes=`anonymous × {light,dark} × {en,zh} × {desktop 1440×900, tablet 820×1180, mobile 390×844}` × `force_states = [radar_open:.open, radar_focus:focus(#dlg-risk .gde-disc > summary)]`; outcome=`captured`; honesty block explicit: "this tool measures and screenshots; it scores, ranks, and judges nothing" and "a forced state is either a class/attribute toggled on `<body>` or a real browser hover/focus applied to a selector before the shot; it shows that state's presentation, not data the page returned; metrics are measured on the rest cells only".

`mockups/evidence/risk-radar-integration-20260919/interaction-metrics.json` (242 lines, 24 forced cells): `context_height`, `dialog_width`, `viewport_width`, `page_overflow`, `inside_radar`, `evidence_open`, `focused_summary` per cell. ALL 24 forced cells: `inside_radar:true`, `page_overflow:false`. ALL 12 `radar_open` cells: `evidence_open:false`, `focused_summary:false`. ALL 12 `radar_focus` cells: `focused_summary:true` (summary was real-focused before the shot).

`mockups/evidence/risk-radar-integration-20260919/EVIDENCE.yml` (6 lines, `schema: mastermind.page_evidence_receipt.v1`): lists the three changed_paths (`templates/_risk_envelope_band.css.j2`, `templates/_risk_envelope_band.html.j2`, `templates/dashboard.html.j2`) and the manifest path.

`mockups/evidence/risk-radar-integration-20260919/button-proof.json` (8 lines): `mode: "isolated_local_member_access_fixture"`, `production_authentication_tested: false`, `existing_button_opens_radar: true`, `evidence_keyboard_opens: true`, `escape_closes: true` — receipt for the existing Risk building button's continued behavior post-relocation.

`mockups/evidence/risk-radar-integration-20260919/README.md` (26 lines) + `REPORT.md` (22 lines, `schema: mastermind.ux_smell_report.v1`) + `smells.json` (86 lines, nothing flagged for `macro.html`) + `partial-render-receipt.json` (15 lines) + `capture_adapter.py` (58 lines, the forced-state capture driver) + `button_proof.py` (27 lines, the button behavior receipt) + `refresh_page.py` (56 lines) + 25 PNGs (12 rest + 13 forced).

**Tests (1 file, +129 / −0)**

`tests/test_risk_envelope_radar_integration.py` (NEW, 129 lines): the contract tests for the bridge — engine contract preserved, settled/live engine untouched, promotion holds untouched, idempotent relocation, no duplicate first-level panel when cached page is loaded.

**Decision + research + workstream (3 files, +93 / −0)**

`agentos/decisions/DEC-RISK-ENVELOPE-IN-RADAR-NOT-DASHBOARD-PANEL.md` (+36, NEW): the Chairman 2026-09-19 ruling. `key: RISK-ENVELOPE-IN-RADAR-NOT-DASHBOARD-PANEL`. `question: Where should the Risk Envelope's trend, stress and source-evidence readings appear on the macro dashboard?` `answer: Inside the existing Risk Radar, reached through the existing risk button. Remove the separate full-width dashboard container.` `decided_by: chairman-chris 2026-09-19`. `reversibility: easy`. The footer paragraph explicitly bounds the change: "Only the GD-2 standalone-band placement and density are superseded. The accepted settled/live engine, distinct Trend/Stress/Policy semantics, promotion holds, source clocks and authority booleans are unchanged."

`research/grey_deer/RISK_RADAR_INTEGRATION_2026-09-19.md` (+55, NEW): the design/continuity narrative.

`agentos/workstreams/WS-GREY-DEER-RISK-INTELLIGENCE.md` (+1): adds the new DEC reference alongside the existing `DEC:RISK-STATE-HAZARD-POLICY-SEPARATION` and `DEC:RISK-ENVELOPE-IS-CANONICAL-DERIVED-PROJECTION`.

**CI registration (1 file, +8 / −1)**

`.github/ci/legacy-jobs.yml` (+8/−1): the new test file + its read dependencies (`templates/risk_envelope_live.js`, `site/risk_envelope_live.js`, `templates/_risk_envelope_band.html.j2`, `templates/_risk_envelope_band.css.j2`, `tests/__init__.py`) are registered in the macro-risk-dialog contract test pool; the rendered-HTML contract pytest at line 12412 picks up `test_risk_envelope_radar_integration.py`.

## Plain-language findings

The macro repo has no `scripts/check_plain_language.mjs` (terminal-side only). Plain-language discipline here is read directly against the standing design-doctrine rules (glance-tier = state + plain-word stance under hard word budgets; banned internal-state / study / rank vocabulary; per-signal "so what do I do"; honest-null grammar; ZH parity). The PR moves an existing user-facing block from a first-level container into a dialog — so the doctrine applies in full to every renamed/relocated string, with extra care at the new "compact dialog" density.

**Verdict: PASS.**

1. **No banned internal-state / study / rank vocabulary in user-visible strings.** Word-boundary grep on `templates/_risk_envelope_band.html.j2` for `score | rank | percentile | chokepoint | satellite | falsifier | thesis | refut | invalid | stake | AIS | backtest | edge`: **2 hits**, both non-user-visible. (i) Line 2 Jinja comment: `One derived engine contract; no new score, policy authority or first-level panel.` — this is a developer-facing comment explaining the contract decision, NOT rendered into HTML (it's inside `{# … #}` Jinja syntax which Jinja strips before rendering). (ii) Line 78 inside `<td class="tnum">`: `{% if s.score is not none %} · {{ s.score }}{% endif %}` — `s.score` is a data binding to the source's own reported score; the value renders as a number inside a numeric column ("Its own read") whose header is plain English, never as the word "score" itself. The word "Score" does NOT surface in any user-facing string of the new template.
2. **No banned machine-text in user-visible strings.** Word-boundary grep on the same file for `void | ts | sl | tp`: **1 hit**, inside a Jinja comment block (`{# ── GD-3 live-provisional machine receipt — … Filled: "live: candidate <stage> · stable <stage> · pending n/m · built <ts> · bundle <live bundle_id[:8]>". #}`). This is developer-facing documentation of the receipt-format contract; `<ts>` is a placeholder name in the receipt-fill recipe, not rendered text. No user-facing string ends with a raw machine abbreviation.
3. **All user-facing labels are plain English / plain Chinese.** Headings and stamps use the design doctrine's plain-language vocabulary: `Market internals` / `市场内部`, `Trend` / `趋势`, `Stress` / `压力`, `Evidence · N sources` / `依据 · N 个来源`, `Source` / `来源`, `Its own read` / `自身读数`, `As of` / `数据日期`, `Freshness` / `新鲜度`. The state vocabulary is the accepted closed enum (Risk-on/Risk-off/Mixed, No stress flagged/Fragile/Stress spreading/Breakdown) — each pair is plain and reads the same in EN and ZH; no machine abbreviation, no raw enum constant. The Risk-on/Risk-off/Mixed labels are passed via `_labels.get(_ms.get('verdict'), ('Unavailable', '暂无读数'))` with the source's own `state_label_en`/`state_label_zh` preferred over the canonical fallback, so the user always reads a localized noun phrase, never a raw verdict code.
4. **The designed-null disagreement clause is doctrine-correct.** Two designed-null sentences print under explicit conditions (no stage or `_ms.usable` is false → "Some inputs are unavailable; this is not an all-clear." / "部分数据缺失，不代表风险已解除。"; CONTRADICTORY + RISK_ON trend → "The trend is positive, but underlying stress remains." / "趋势向好，但内部压力仍在。"). Each is a plain sentence that names what is true and what it does NOT mean, so the "so what do I do" answer is the next clause (read with care; don't assume all-clear). Both fall through to the same compact null stamp rather than inventing a stance word.
5. **"Capital controls: N active. Separate from the market readings." / "资金限制：N 项生效。与市场判读独立。"** is the doctrine-correct separation contract per `DEC:RISK-STATE-HAZARD-POLICY-SEPARATION`. The English reads as one short paragraph with a hard separation between the two sentences; the ZH reads as two parallel clauses joined by a period (not a comma — the comma would imply equivalence). The user can answer "so what do I do" (note the policy is independent of the regime read, so don't conflate a positive trend with a relaxed capital posture) without the line ever naming a position, a size, or a trade signal.
6. **The Evidence table headers are plain.** `Source` (source's own bilingual label or `source_id` fallback), `Its own read` (the source's own `state` plus optional ` · score` if the source publishes one), `As of` (date stamp), `Freshness` (`This session` / `Older than this session` / `Did not publish`, bilingual). `Older than this session` and `Did not publish` carry the `gde-warn` class for ink treatment (theme.css); the text itself is plain English that names what the freshness actually means.
7. **The contradictions list is sentence-form prose.** When coherence is CONTRADICTORY and `coherence.contradictions` is non-empty, a `Where they disagree` / `分歧所在` heading precedes a `<ul class="gde-conflicts">` whose each item is a full sentence: `<src_a> reads <left_state> while <src_b> reads <right_state>.` / `<src_a_zh> 读为 <left_state>，而 <src_b_zh> 读为 <right_state>。`. Source names come from `_names_en.get(c.left, c.left)` and `_names_zh.get(...)`, preferring the source's localized label over the raw `source_id` slug — the user reads source names, not slugs. No bare enum codes leak into the contradiction prose.
8. **The bridge summary text is plain-language.** `relocateLegacyEnvelope()` creates `<summary><span class="l-en">Market internals · trend and stress</span><span class="l-zh">市场内部 · 趋势与压力</span></summary>` for previously cached pages. The summary names what the disclosure contains, not a state code or a rank. The text matches the new canonical context's own heading vocabulary.
9. **ZH parity holds across all bilingual strings.** Every `l-en`/`l-zh` pair has been kept in lockstep — none of the relocated strings has a missing locale, and the new bridge summary is bilingual. The contradiction clause comma treatment in ZH (`，而` between clauses) preserves the same separation as the English "while". The `No stress flagged` / `未见压力` pair keeps the same noun-phrase shape in both locales (no "stress: None" machine-text leak).
10. **PR body plain-language claims are themselves plain-language.** The PR body uses "Risk building button" / "Risk Radar" / "Trend / Stress readings" / "disagreement warning" / "expandable Evidence" / "Missing inputs never become calm/Risk-on" / "unknown coherence never claims alignment" — every word is the user's noun or a contract noun, no internal state codes, no engine identifiers. The "Render and publication honesty" subsection explicitly says "No collectors were run and no market inputs were fabricated. `site/macro.html` therefore carries a bounded render of the canonical owned Jinja partial from the exact envelope bundle already quoted by the page" — which is the right honest-null language for a partial render.

The PR is plain-language compliant across all user-facing bytes in `templates/_risk_envelope_band.html.j2`, `templates/dashboard.html.j2` (no user-facing copy change), `templates/risk_envelope_live.js` (no user-facing copy), `site/macro.html` (paired sync), and `site/risk_envelope_live.js` (paired sync). No debt introduced. Net new banned-vocab occurrences in user-visible strings: **0**.

## Theme findings

TP-0 art-direction law in force (operator 2026-08-27): dark and light are TWO art directions, not one skin; "the same CSS still renders once the tokens swap" is precisely the failure this law exists to stop. Substantive product styling may not be authored as an opaque runtime stylesheet system inside page/composer JavaScript. The PR is theme-relevant because it (a) shrinks a full-page band into a compact dialog inset and (b) preserves the existing CSS contract inside the dialog.

**Verdict: PASS.**

### CSS discipline

`templates/_risk_envelope_band.css.j2` (+49 / **−308**) is a NET REMOVAL of CSS — the page-level rails, full-width header, and large spacing rules are deleted; the compact inset rules replace them. The retained selectors are scoped to `#dlg-risk` (not `#macro` or page-wide), so the moved node cannot leak its old page-band chrome back onto the dashboard.

All colors via `var(--*)` tokens or `color-mix(in srgb, var(--*) NN%, transparent)` per CLAUDE.md §Theme art direction. Verified by `grep -nE "#([0-9a-fA-F]{3}){1,2}\b" templates/_risk_envelope_band.css.j2` → **0 hits** for color literals in the NEW CSS. Tokens used in the kept lines: `var(--line)`, `var(--r-card)`, `var(--panel2)`, `var(--panel)`, `var(--ink-1)`, `var(--ink-2)`, `var(--ink-3)`, `var(--ink-warn)`, `var(--link)`, `var(--fs-sm)`, `var(--fs-md)`, `var(--fs-label)`, `var(--r-ctl)`. The legacy tokens `var(--gde-c)` (the per-cell ink colour) is set via inline `style="--gde-c: {{ _ms_ink }}"` on the parent `<div class="gde-read">` — a token pass-through, not a hard-coded ink, and `_ms_ink` resolves to one of `var(--ink-up)`, `var(--ink-down)`, `var(--ink-2)`, `var(--ink-3)`, `var(--ink-warn)`, `var(--ink-act)` (no raw hex anywhere in the resolve chain).

Dark and light shipped as TWO art directions (not token-swapped CSS):
- **Dark** (default `#dlg-risk .riskdlg-context`): the dialog uses its existing dark surface via `var(--panel2)`. The compact inset reads as a quiet structural panel inside the Radar — no glow, no bloom, just a hairline border (`var(--line)`) and a token radius.
- **Light** (`html[data-theme="light"] #dlg-risk .riskdlg-context`): explicit override — `background: color-mix(in srgb, var(--panel2) 60%, var(--panel)); box-shadow: none;`. The 60/40 mix against the panel gives the inset a half-step-on-paper feel appropriate to a light research workspace, and the explicit `box-shadow: none` ensures the dialog's own surface glow does not bleed into the inset. (The dialog itself carries the shadow; the inset should not double it.)

The mobile breakpoint (`@media (max-width:640px)`) tightens the inset padding (`12px`) and the reads gap (`8px 16px`), and forces the Evidence table to a `min-width: 460px` with horizontal scroll inside the dialog body — so the dialog scroll owns the overflow rather than the page itself.

### Runtime style injection

`scripts/check_runtime_style_injection.py` does not apply: the diff contains no `style.textContent`, no inline `style=` setStyle other than the per-cell `--gde-c` token pass-through (which IS data-dependent ink colour resolved through `var(--*)` — token plumbing, not material decision), no JS-mounted DOM that invents new styles, no parallel token family. `relocateLegacyEnvelope()` creates `<details>` / `<summary>` / `<span>` elements with `classList.add` (semantic class names that map to governed CSS selectors) and sets `.hidden` / `.style.display = "none"` on the legacy band as a defensive fallback (only when `#dlg-risk .mx5-dlg-body` does not exist yet on a cached page) — these are state toggles, not material decisions.

### 36-cell evidence matrix

The evidence matrix is **PRESENT and complete** at the merge head:
- 12 REST cells (canonical `capture_page_evidence` driver): dark/light × EN/ZH × {desktop 1440×900, tablet 820×1180, mobile 390×844}, all anonymous.
- 24 FORCED `radar_open` / `radar_focus` cells: `capture_adapter.py` applies real focus + Enter to open native Evidence, Escape closes. `radar_focus` cells record `focused_summary: true` (the summary was real-focused before the shot).
- `interaction-metrics.json` is the per-cell numeric receipt (164.6875 / 182.8125 px context heights, 358-900 px dialog widths, no page overflow, inside-radar=true).
- `manifest.json` records 24 console errors — ALL are local HTTP server 404s on `/live/risk_envelope.json?t=<ms>`, `release_publications.json`, `api/brain/me`, `/ws/tape` (the local server has no collectors; this is the partial-render honesty receipt, not a page failure). The page-level rendered HTML succeeded across every cell.
- `button-proof.json` (8 lines) records the existing Risk building button still opens the Radar (label `Risk building · 56`), keyboard Enter expands Evidence, Escape closes — in the isolated local member-access fixture, with no credentials read or synthesized.
- `partial-render-receipt.json` (15 lines) names the bundle and final page hash; whitespace-only lines in the owned inserted section were normalized post-capture, no rendered text/geometry/style/behavior changed.

The design-system checker on the PR-7467 scope returns **0 blocking finding(s)** on the new templates, the new tests, the paired site copies, and the new manifest/EVIDENCE.yml; the 18,972 pre-existing non-blocking findings are unchanged by this PR (the ratchet only reports; it does not newly block inherited debt).

### Style purity in JS

`templates/risk_envelope_live.js` (+23) adds the bridge. The bridge uses `classList.add` / `classList.remove` for the semantic class swap (`"mx-disc riskdlg-envelope-legacy"`, `remove("panel", "span12")`), sets `.textContent` on a single bilingual summary, and toggles `.hidden` / `.style.display` for the defensive fallback. No `style.setProperty('--*', …)`, no `style.cssText = …`, no `el.insertAdjacentHTML`. The CSS that decides the moved node's look lives entirely in `templates/_risk_envelope_band.css.j2` and is scoped to `#dlg-risk`.

## Validated-claims findings

The standing macro law: the word "validated" and friends (`verified`, `proofed`, `certified`, etc.) are CI-enforced via `scripts/check_validated_claims.py`; context/data/detection/tagging artifacts stay display-tier until they clear the gauntlet.

**Verdict: PASS.**

1. **No NEW MISS in the new template or its diff context.** Word-boundary grep on `templates/_risk_envelope_band.html.j2` for `validated | 已验证 | 经验证 | 经过验证`: **0 matches**. The relocated include has no affirmative "validated" claim, no promotion-tier assertion, no A7 claim. The Jinja comment block at line 3 mentions "the accepted live consumer" — "accepted" is the live-engine settlement contract, NOT the validated-claims tier; the comment is developer-facing documentation.
2. **The relocated include's only "validated"-adjacent string is the `data-state` / data-binding plumbing, NOT a tier assertion.** The line `data-bundle-id="{{ _re.get('bundle_id') or '' }}"` and `data-settled-session="{{ _re.get('source_session') or '' }}"` are receipt-style attributes for the JS bridge to find the canonical node, not validated-claims attributes. The template never adds `data-validated`, `data-verified`, `data-certified`, `data-approved`, or any HTML attribute that would assert the gate.
3. **The paired `site/macro.html` and `site/risk_envelope_live.js` carry no new affirmative validated-claims.** The pre-existing "validated" strings the checker returned (`site/macro_business_activity.html:2374`, `site/macro_capital_structure.html:2374`, etc.) live in scope-out sibling pages and are unchanged by this PR. The change to `site/macro.html` (+202/−184) is the paired G6 wire sync of the same relocated include + new bridge code, with no NEW affirmative validated-claims introduced.
4. **`scripts/check_validated_claims.py` exits 0 on the current tree.** Run on the merge head: the gate returns no MISS and no FAIL. The PR does not promote any artifact — it MOVES an existing display-tier panel into a dialog, leaving the engine contract (`schema: mastermind.risk_envelope/v1`), the settled/live engine, the policy authority, the promotion holds, and the source clocks unchanged.
5. **No artifact promotion occurred.** The PR is a half-B BUILD deliverable for the GREY-DEER-RISK-INTELLIGENCE workstream. The Risk Envelope reading (`_re.measured_state`, `_re.hazard_summary`, `_re.coherence`, `_re.policy_summary`) remains display-tier — it renders Trend/Stress/coherence stamps in the dialog, with the machine receipt line (`definition_id · bundle <id> · coverage <state> · coherence <state> (scope <scope>) · stage <stage> (<reason>) · …`) developer-facing in the Evidence disclosure. The receipt explicitly names `engine/risk_envelope.py` and the bundle / coverage / coherence state — the doctrine-correct receipt form for a display-tier artifact, not a promotion assertion.
6. **The capital-controls footer respects the policy-separation contract.** `Capital controls: N active. Separate from the market readings.` / `资金限制：N 项生效。与市场判读独立。` is the `DEC:RISK-STATE-HAZARD-POLICY-SEPARATION` doctrine-correct form: it states that a policy state exists without claiming it has been validated or that it should change a position. The "Separate from the market readings" sentence is the separation assertion, NOT a validation claim.
7. **The PR body is honest about what is and is not proven.** "The full builder was attempted but refused because this checkout lacks its untracked Yahoo store. No collectors were run and no market inputs were fabricated." — this is the doctrine-correct "honest-null" framing for the partial-render receipt. "CI, merge, production deployment and live verification remain separate gates. This PR is not a claim that the fix is already live." — explicit non-promotion of the artifact to live-validated status. "These are local presentation proofs, not authenticated production witnesses" — explicit non-promotion of the evidence matrix.
8. **The Evidence README is honest about the capture.** "Rest cells use the canonical driver. `radar_open` and `radar_focus` use the committed `capture_adapter.py` to apply the existing modal presentation classes. They are **forced local presentation**, not authenticated production witnesses." — explicit non-promotion of the forced-state receipts.

The PR is validated-claims compliant across all 58 files. No new MISS, no promotion, no A7 claim.

## Overall verdict

| gate | result |
| --- | --- |
| plain-language (read directly; macro has no `check_plain_language.mjs`) | **PASS** — 0 banned-vocab occurrences in user-visible strings of `templates/_risk_envelope_band.html.j2`; the 2 "score" matches are a Jinja comment and a `s.score` data binding (not the word "score"); the 1 word-boundary "ts" match is inside a Jinja comment placeholder for the live-receipt fill recipe. All user-facing labels are plain English / plain Chinese with explicit designed-null grammar (`Some inputs are unavailable; this is not an all-clear.` / `部分数据缺失，不代表风险已解除。`); the contradictions list is sentence-form prose with localized source labels (no raw slugs); the Evidence table headers are plain (`Source` / `Its own read` / `As of` / `Freshness`); the bridge summary is bilingual plain-language (`Market internals · trend and stress` / `市场内部 · 趋势与压力`). The capital-controls footer respects the policy-separation contract with plain "Separate from the market readings." copy. |
| theme (`check_design_system.py --mode enforce-added` + CSS audit) | **PASS** — 0 CSS color-literal additions in the kept `templates/_risk_envelope_band.css.j2` (verified by hex-literal grep on the file → 0 hits); all colors via `var(--*)` tokens or `color-mix(in srgb, var(--*) NN%, transparent)`. Dark and light shipped as TWO art directions (dark = `#dlg-risk .riskdlg-context` on `var(--panel2)`; light = explicit `html[data-theme="light"]` override with `color-mix(in srgb, var(--panel2) 60%, var(--panel))` and `box-shadow: none`). 36-cell evidence matrix PRESENT (12 rest + 24 forced `radar_open`/`radar_focus` cells, all `inside_radar:true`, `page_overflow:false`); `interaction-metrics.json` is the per-cell numeric receipt; `button-proof.json` is the button-behavior receipt. The relocation is the half-B Risk Radar integration; the older page-band rails are explicitly suppressed via `#dlg-risk .gde-head/border:0/padding:0/margin:0` so the moved node cannot re-impose the old layout. `0 blocking finding(s)` on PR-7467-scope via `check_design_system.py --mode enforce-added`. |
| validated-claims (`check_validated_claims.py`) | **PASS** — 0 affirmative `validated` / `已验证` / 经验证 / 经过验证 matches in the relocated `templates/_risk_envelope_band.html.j2` (word-boundary grep returns 0); the only `data-*` attributes added are `data-bundle-id` / `data-settled-session` / `data-coherence` (receipt plumbing, not validation tier assertions); the receipt line `definition_id · bundle <id> · coverage <state> · coherence <state> (scope <scope>) · stage <stage> (<reason>) · … · engine/risk_envelope.py` is the doctrine-correct display-tier form (names the bundle + coverage + coherence + stage + engine, does not assert authority); the capital-controls footer asserts separation without validation. The PR body and the Evidence README are honest-null about the partial render and the forced-state receipts ("not authenticated production witnesses"). `check_validated_claims.py` exits 0 on the merge head; no artifact promotion, no A7 claim, no tier raise. |
| accepted engine contract preserved (`DEC:RISK-STATE-HAZARD-POLICY-SEPARATION`, `DEC:RISK-ENVELOPE-IS-CANONICAL-DERIVED-PROJECTION`) | **PASS** — the same Jinja include + the same CSS contract render against the same `schema: mastermind.risk_envelope/v1` engine contract; the live-hook identities (`#risk-envelope-band` id, `gde-live-chip`, `gde-live-receipt`, `gde-pending-chip`) are preserved; the idempotent `relocateLegacyEnvelope()` bridge moves the SAME node, never clones a feed consumer; settled/live engine, promotion holds, source clocks, and authority booleans are unchanged. The `tab` of the new decision explicitly bounds the change to "GD-2 standalone-band placement and density". |
| evidence matrix (CL 2026-09-19 standing) | **PASS** — 36 cells captured against `site/macro.html` at frozen head `56c53791`: 12 rest (anonymous × {dark,light} × {en,zh} × {desktop 1440×900, tablet 820×1180, mobile 390×844}) + 24 forced (`radar_open:.open`, `radar_focus:focus(#dlg-risk .gde-disc > summary)`). `interaction-metrics.json` shows `inside_radar:true` and `page_overflow:false` for all 24 forced cells; `focused_summary:true` for all 12 `radar_focus` cells; the canonical `capture_page_evidence` driver + the committed `capture_adapter.py` are the receipts. `button-proof.json` is the button-behavior receipt. `partial-render-receipt.json` names the bundle and final page hash. `smells.json` flags nothing for `macro.html`. The 24 console errors are local-server 404s, not page-level failures — the partial-render honesty receipt is explicit. |
| paired G6 wire sync (`check_template_site_sync`) | **PASS** — `site/macro.html` (+202/−184) and `site/risk_envelope_live.js` (+23) carry the same relocated include + the same bridge code as the canonical `templates/` versions; `python3 -m scripts.check_template_site_sync` runs clean (no NEW drift introduced by this PR). |
| CI registration (legacy-jobs) | **PASS** — `tests/test_risk_envelope_radar_integration.py` (+129, NEW) is registered in `legacy-jobs.yml` macro-risk-dialog test pool alongside its real read dependencies (`templates/risk_envelope_live.js`, `site/risk_envelope_live.js`, `templates/_risk_envelope_band.html.j2`, `templates/_risk_envelope_band.css.j2`, `tests/__init__.py`); the rendered-HTML contract pytest at line 12412 picks up the new test. No CI surface was widened beyond the existing macro-risk-dialog scope. |

**Overall: PASS (half-B BUILD delivery of Grey Deer Risk Intelligence's Risk Radar integration; the standalone envelope panel is removed from the dashboard and its compact context now lives inside the existing Risk Radar dialog; the same engine contract, the same live-hook identities, the same settled/live engine are preserved; the decision is recorded as `DEC:RISK-ENVELOPE-IN-RADAR-NOT-DASHBOARD-PANEL` with `decided_by: chairman-chris 2026-09-19` and `reversibility: easy`; the 36-cell evidence matrix + per-cell interaction receipt + button-behavior receipt + partial-render honesty receipt are all in place; the idempotent compatibility bridge ensures previously rendered/cached pages relocate the legacy node into the Radar without cloning a feed consumer). No new debt; no validated-claim promotion; no color-literal addition; no banned-vocab leak in user-visible copy; no CI gate failure on PR-7467's actual 58-file scope.**
