# Audit — mastermindx-market-intelligence/macro PR #7539

Mode: qwen_auditor2-style (plain-language / theme / validated-claims laws). One-pass audit against the merged head. No durable writes outside this report.

## PR metadata

| field | value |
| --- | --- |
| repo | `mastermindx-market-intelligence/macro` |
| PR | [#7539](https://github.com/mastermindx-market-intelligence/macro/pull/7539) |
| title | `[MO-A UD-B2-W1] Fold vol-weather chips into Risk isle per R3 (rows 21+35)` |
| merged | 2026-09-20T19:53:20Z via squash-merge to `main`. Merge commit `3f1a1e0d9ef1f0ac79c91b2c27ff55e533df3abe`. **Most-recent merged half-B PR in the 24-h window at audit time** (the only newer merge is the orch(audit) record PR #7549, which is NOT a half-B subject). Prior-older half-B merges (#7511, #7523, #7527, #7514, #7506, #7498, #7492, #7482, #7474, #7472) already on disk under `orch/audits/macro_PR-<N>.mm.md`. |
| branch | `claude/mo-a-ud-b2-w1-volweather-fold` |
| merge base | `c22864f80967e77e6e42cd926ef00f11f7197d06` |
| FINAL head (capture-last) | `e901ae422cd6ffb8ec251472a38fd16596c7fea8` |
| tokenization+rebind head | `cb4ed1c0ea12d6973c20dc56906287685ecf8fc7` (ancestor of FINAL head, the tree the 8-cell matrix was taken against) |
| captures commit | `9bb52598e451129ec8fac4f58c030dc8c4df1fcf` (round-4 capture, slice sha256 `346b194c2cb8`) |
| Round-5 rebind head | `0e0be24511f986d60bbe8c5431135cef95dbc3f9` |
| author / merger | `chriswong6031-creator` (operator); author on the per-commit trail is `claude` (Claude Code); merger is the operator (Sol Integration Proof bot path) |
| base | `origin/main` at the merge point |
| files (measured `git diff --stat`) | **163 files changed, 6536 insertions(+), 12027 deletions(-)**. `gh pr view --json files` returns 100 paths (API-capped); the measured git list is 163. The 63 paths the API omits are the remainder of `site/basket{,_china,_hk,_canada,_intl}/`, `site/macro.html`, `site/theme.css`, `site/research_screener.html`, `templates/dashboard.html.j2`, `templates/theme.css`, and the three W1 test files. Round-4's 28-file inventory + Round-5's 128-file rebind table + Round-6's 19-file tokenization table together cover the cumulative surface. |
| Round-by-round commits | 12 commits total: `a85b884ab8` (R1 fold), `818d0ca9c1` (R2 plain-word + mobile-wrap), `153f9917e5` (R2 site regen), `0cdc8557fb` (R3 capture), `e2e2a80bab` (R3 capture-last), `761f74eb30` (R4 host swap to `.mx5-sc-left`), `ea6ad8ffef` (R4 stub-removal), `9bb52598e4` (R4 capture-last), `0e0be24511` (R5 theme-rebind closure), `cb4ed1c0ea` (R6 tokenization), `e901ae422c` (R6 capture-last, FINAL head). |
| scope | **Template + theme + site-regen + 3 new test files + evidence matrix.** New Jinja2 macro `sx_vw_strip(scope_class)` (75 lines) inside `templates/dashboard.html.j2`, mounted inside a new `.mx5-sc-vw` host that lives in `.mx5-sc-left` (the mx5 scorecard's left column); the legacy `#sx-risk-v2` container is collapsed to `display:none!important` on the macro route (with the `.mx4-grid` carve-out). Theme receives 155 lines of `.sx-vw-strip / .sx-vw-row / .sx-vw-eyebrow / .sx-vw-name / .sx-vw-mid / .sx-vw-tier` rules scoped to `body.page-macro .mx5-sc-vw` with `[data-theme='light']` overrides + a `@media(max-width:560px)` breakpoint. `site/macro.html` regenerated from the templates (-11665 +4332 = -7333 net bytes — the fold replaces a sibling section with a sub-row). `site/theme.css` regenerated. 121 generated basket pages + 1 screener page receive theme.css stamp `62b49cc0` (was `665c4b34` → `4c26bb2c` → `62b49cc0` across rounds). Three new test files (`test_ud_b2_w1_vw_fold.py` +478, `test_ud_b2_w1_red_first_proof.py` +214, plus `test_vsb_surface.py` +214/-28) wire the 8-cell evidence matrix (16 PNGs + `manifest.json` + `EVIDENCE.yml` + `p0_evidence.json` + `capture.py`). `.github/ci/legacy-jobs.yml`, `.github/workflows/ci.yml`, `.github/workflows/ci-main-heartbeat.yml` wire the new test suites beside `test_vsb_surface.py`. |
| half-B label | **half-B (MO-A UD-B2-W1 — vol-weather fold into the visible risk surface).** This is the W1 packet of UD-B2: a template-side fold that moves an existing sibling strip into the mx5 scorecard's left column, demoting internal metric names to disclosure tier and surfacing only plain-word tier reads (`calm / breeze / gust / storm`) on glance. Engine binding: `vm['vol_weather']` unchanged — no new keys, no engine edits, no score/signal invention. |
| live proof | PR body reports the local ratchet stack is clean: `check_design_system.py --mode enforce-added` returns 0 blocking findings on the diff (with 18,961 pre-existing estate-wide findings reported but not gated); `check_runtime_style_injection.py` OK on 195 .js files (44 injecting, 89 total hits — all within frozen allowances); `check_ui_visual_evidence.py` OK; `check_design_system.py --self-check` OK (8 rule fixtures + clean fixture + traversal); `check_template_site_sync` OK on 99 pairs. Test suites: `test_check_design_system + test_check_runtime_style_injection + test_check_ui_visual_evidence + test_freshness_chips_language_invariant` → 179 passed in 5.74s. `test_vsb_surface + test_ud_b2_w1_vw_fold + test_ud_b2_w1_red_first_proof` → 46 passed, 1 skipped in 37.60s. `test_research_screener::test_fresh_bake_matches_committed_site_html + test_stock_dashboard_first_frame::test_committed_browser_receipts_are_self_binding_fixture_proof + test_basket_detail_glance_copy::test_generated_pages_share_the_new_fingerprinted_stylesheet + test_stock_dashboard_first_frame::test_visual_manifest_preserves_history_and_appends_bounded_repair` → 5 passed in 1.47s. R-E byte-identity gate: `.mx5-sc-left` slice sha256 `346b194c2cb8` (23397 bytes) at capture-time head `cb4ed1c0ea` and at FINAL head `e901ae422c` — byte-identical. R-W1-E capture-last: `git diff e901ae422c..HEAD -- templates/ mockups/evidence/unified-dashboard-b2w1/` = EMPTY. |
| CI checks (prior head `761f74eb30`, 2026-09-20T16:34:54Z) | FAIL on `contract-delta` (2 unwired suites — fixed on FINAL head), `ci-gate` (follows contract-delta), `ci-pack-3`, `ci-pack-4`, `ci-pack-9`, `Vercel` (rate-limit, infrastructure), `ci-authority/codex/merge-queue-pilot`. PASS on `ci-pack-0/1/2/5/6/7/8/10/11`, `ci-plan`, `fence-pack`, `ci-authority`, `capability-broker`, `grader-manifest`, `self-mod-fence`, `Vercel Preview Comments`. SKIPPING: `trusted-ci`, `fork-capability-broker-unused`, `fork-grader-manifest-unused`, `fork-self-mod-fence-unused`. CI run 35527769872 failed ONLY on `ci-pack-8`'s design-governance forward-only ratchet (new forbidden decisions on changed lines) + derivative `ci-gate`; round-6 tokenization head `cb4ed1c0ea` addresses all four ratchet findings and the local re-run reports 0 blocking findings on the lines NEW to this diff. The PR body explicitly states "CI: not claimed green — checks at this new head are pending / in flight" and the PR is DRAFT (never ready, never merge, no merge-on-green, no labels). The macro-side Vercel check fails on the same `Deployment rate limited — retry in 24 hours` reason (pre-existing repo-wide quota state, not introduced by this PR). |
| gating scripts | The macro repo has no `scripts/check_plain_language.mjs` (terminal-side only; verified `ls scripts/check_plain_language*` → no matches); plain-language discipline on macro is read directly against the standing design-doctrine rules. `scripts/check_validated_claims.py` exists but gates `templates/`, `site/*.js`, generated `*_data.js`, and `engine/` display-copy fields (verified by reading `check_validated_claims.py:7-21`); this PR touches `templates/dashboard.html.j2` and `templates/theme.css` — both in scope for the gate, and the local run is implicit-clean (no `::error title=validated-claims::…` in the PR body transcript). |

## Diff content (exact, scoped to the user-facing surface)

**`templates/dashboard.html.j2` (+231 / −116) — new macro + dead-code removal**

- **New Jinja2 macro `sx_vw_strip(scope_class)`** (75 lines at line 214-end-of-file region). Header comment names the seat ruling (R3 rows 21+35 from `research/UNIFIED_DASHBOARD_DISPOSITION.md`), the plain-word glance tier (R-W1-B), the engine-true binding (`vm['vol_weather']` is the unchanged contract; tier word comes from `vm['vol_weather'].chips[].band`, no template-side re-classification from percentiles), and the one-integer law (no raw values, no `higher/lower than N%` integers on glance — the hero already owns N/100).
- **Plain-word name map `_vw_plain_name`** (8 chips × 2 langs): `vix_level: ('Volatility level', '波动率水平')`; `vix_velocity: ('5-day volatility change', '5 日波动率变化')`; `term_slope: ('Near-term vs far-term outlook', '近期与远期展望')`; `vvix_vix: ('How much traders expect next', '交易员对未来的预期')`; `vix1d: ('Overnight price swings', '隔夜价格波动')`; `cor1m: ('How much stocks move together', '股票同步程度')`; `cor3m: ('3-month implied correlation', '3个月隐含相关性')`; `dspx: ('Stock-by-stock swings', '个股走势分散度')`. The PR body explicitly states the `cor1m` string is the engine's `plain_normal` (`engine/vol_velocity.py:601` "How much stocks move together is within the normal range.") and the `cor3m` parenthetical is from the engine's `name_en`/`name_zh` (`engine/vol_velocity.py:609` "COR3M (3-month implied correlation)").
- **Tier-word map `_vw_band_word`** (6 bands × 2 langs): `calm: ('calm', '平静')`; `quiet/low/normal: ('breeze', '微风')`; `elevated: ('gust', '疾风')`; `extreme: ('storm', '风暴')`. All bands have EN + ZH twins; no EN-only skew.
- **Eyebrow** (`.sx-vw-eyebrow`): `<span class="l-en">Volatility weather</span><span class="l-zh">波动率天气</span>` — plain-word, no internal metric name on glance.
- **Young/missing-freshness read** (`.sx-vw-read` for `young|missing` chips): `<span class="l-en">Still collecting — check back soon</span><span class="l-zh">仍在积累数据，稍后再看</span>` — plain-word, no raw value, no pctile phrase.
- **Tier column** (`.sx-vw-tier`): for `young|missing`, renders `—` (em-dash, not a numeric placeholder); otherwise the tier word (`calm / breeze / gust / storm`) and ZH twin.
- **Mount site**: inside a new wrapper `<div class="mx5-sc-vw" data-sx-vw-host="mx5-sc-left">` inside `.mx5-sc-left`, AFTER the dial + scar chips row.
- **Dead-code removal**: 41 stale `body.page-macro #sx-risk-v2 .sx-vw-*` comma-targets in `templates/theme.css` (PR body §5.Minors); the `.mx5-sc-vw` host alone owns the styling. Old `#dlg-sentiment .vsb-vw-row` rule retired (R1/R3).
- **Hide-rule restoration**: `body.page-macro #sx-risk-v2, body.page-macro.mx4-grid #sx-risk-v2{display:none!important;}` and `body.page-macro #sx-risk-v2 .sxg-face, body.page-macro.mx4-grid #sx-risk-v2 .sxg-face{display:none!important;}` — `#sx-risk-v2` is now an empty hidden stub (no leftover `Risk` / `风险` + Credit Stress stub face under the scorecard). Round-4 host swap: the strip moved from inside `#sx-risk-v2` to inside `.mx5-sc-left` per R-W1-A-AMENDED.

**`templates/theme.css` (+155 / −0) — vol-weather strip styling**

- **Dark treatment** (TP-0 command-center): strip bg `color-mix(in srgb, var(--panel2) 56%, transparent)`; strip border-top `color-mix(in srgb, var(--line) 70%, transparent)`; row bg `color-mix(in srgb, var(--panel) 88%, transparent)`; row inset hairline `box-shadow: inset 0 1px 0 rgba(255,255,255,.02)`; eyebrow color `var(--isle-color, var(--ink-3))`. Glyph colors are class-driven (`sx-vw-row--{calm|quiet|low|normal|elevated|extreme|young}` → `--up / --info / --info / --info / --warn / --down / --muted`) — no inline `style="--sg-wx-c:..."`.
- **Light treatment** (TP-0 research workspace): strip bg `color-mix(in srgb, var(--panel2) 30%, var(--panel) 70%)`; strip border-top `color-mix(in srgb, var(--line) 80%, transparent)`; row bg `color-mix(in srgb, var(--panel) 100%, transparent)`; row box-shadow `0 1px 2px rgba(15,23,42,.04)` (shadow instead of glow). Glyph colors unchanged — same semantic meaning in both themes.
- **Border-radius literals removed** in round-6: `border-radius: 0 0 inherit inherit` → `border-radius: inherit` + `border-top-left-radius: 0; border-top-right-radius: 0` (R6 finding 3); `border-radius: 8px` → `border-radius: var(--r-ctl,8px)` (R6 finding 4). Specimen `--r-ctl:8px` lives at the root. Zero raw radius literals added.
- **Color/radius literals removed from template**: `.sg-pos-mark` rule moved from `templates/dashboard.html.j2` into `templates/theme.css` (R6 finding 1); `--isle-color` opener restored to merge-base byte form `style="--isle-color:var(--isle-risk)"` (R6 finding 2 — the ratchet had been eating past the missing semicolon). Zero new color literals in template code.
- **Mobile breakpoint** (`@media(max-width:560px)`): row name wraps (`overflow-wrap:anywhere` on `.sx-vw-read`; row name keeps ellipsis only as a fallback).

**`mockups/evidence/unified-dashboard-b2w1/manifest.json` (+130) — 8-cell evidence**

- Head/captured_at_head: `cb4ed1c0ea12d6973c20dc56906287685ecf8fc7`.
- Selector: `.mx5-sc-left` (slice from `'<div class="mx5-sc-left">'` to matching close).
- sha256: `346b194c2cb82100aac7d26efe27c99f2bc3d6629f84db6ffa37cd2053a23358`.
- 8 cells: theme `dark|light` × lang `en|zh` × viewport `1440|390`. Strip widths: **354** (1440) / **327** (390); strip heights: 391 / 371; rows: 8. eyebrow_en/eyebrow_zh: `"Volatility weather" / "波动率天气"`.
- 4 additional cells (round-6): `risk_isle_hover_en_dark_1440`, `risk_isle_hover_en_light_1440`, `risk_isle_focus_en_dark_1440`, `risk_isle_focus_en_light_1440` — bind the design-governance visual-evidence gate's hover/focus kinds (`#mx5BtnRisk` focus cell + `.mx5-sc-left` hover cell). Canonical `mastermind.p0_evidence.v2` schema.

**`mockups/evidence/unified-dashboard-b2w1/p0_evidence.json` (+275) — canonical evidence**

- Schema: `mastermind.p0_evidence.v2`. Cells bind the transitive `.page-macro` hover/focus kinds.

**Other touched paths (all measured)**

- `templates/dashboard.html.j2` (+117 / −116 in round 1-4, +155 / 0 in round 6 — total +231 / −116). R6 tokenization head net adds the `sg-pos-mark` → `theme.css` move and the `--isle-color` opener restoration.
- `templates/theme.css` (+155 / −0). The dead `body.page-macro #sx-risk-v2 .sx-vw-*` comma-targets removal lands in theme.css.
- `site/macro.html` (+4332 / −11665 = −7333 net): regenerated from templates; the fold replaces the sibling section with a sub-row inside `.mx5-sc-left`.
- `site/theme.css` (+155 / −0): byte-identical to regenerated `templates/theme.css` after `check_template_site_sync --fix`.
- `site/research_screener.html` (+1 / −1): theme.css stamp `4c26bb2c` → `62b49cc0` (round-5 + round-6 rebind closure).
- `site/basket{,/china,/hk,/canada,_intl}/*.html` (121 pages, ±2 each): theme.css stamp `62b49cc0` + basket fingerprint `f2599f8e` (split: US/CA/Intl on `fb966e77`, China/HK on `f2599f8e`).
- `mockups/evidence/prophet-p0b-zero-fouc/{mobile-layout.json,mobile-layout-canada.json,manifest.json}` (±6): receipts pin `site/theme.css` at `62b49cc0`. 16 historical PNGs + 3 owner-empty CA light PNGs re-rolled by `scripts/verify_stock_dashboard_mobile_layout.cjs` rig (one Playwright run at a time).
- `tests/test_ud_b2_w1_vw_fold.py` (+478): 8 new tests + RED-first helpers. Asserts strip is present once inside `.mx5-sc-left`, chips present inside risk isle, strip lives below dial + scar chips in mx5 scorecard; `_vw_host_slice` slices `<div class="mx5-sc-vw">`; distinct `cor1m/cor3m` plain-word labels.
- `tests/test_ud_b2_w1_red_first_proof.py` (+214): `TestUDB2W1FoldClaimsFailOnPreFold` class with 4 OPPOSITE-direction tests (asserts fold IS true on pre-fold main — guards against mis-identifying parent). Helpers wrapped by `pytest.raises` on pre-fold HTML.
- `tests/test_vsb_surface.py` (+214 / −28): host comments; still slices `.mx5-sc-vw`; `test_vol_weather_plain_text_and_pctile_phrase` slices `.mx5-sc-vw` host.
- `.github/ci/legacy-jobs.yml`, `.github/workflows/ci.yml`, `.github/workflows/ci-main-heartbeat.yml` (each +2 / −0): wire the two new test suites beside `test_vsb_surface.py` (resolves the `contract-delta` failure on prior head `761f74eb30`).

Net new user-facing strings: **16** (8 chip names × 2 langs) + **6** tier-word pairs + **2** eyebrow strings + **2** "still collecting" strings + **2** em-dash placeholders = 28 user-facing string pairs (each EN + ZH twin). All 28 derive from or are consistent with the engine's `vol_weather` chip contract — none invents a new metric, score, or signal.

## Plain-language findings

The macro repo has no `scripts/check_plain_language.mjs` (terminal-side only; verified `ls scripts/check_plain_language*` → no matches). Plain-language discipline on macro is read directly against the standing design-doctrine rules — internal metric names demote to disclosure tier; glance tier carries tier words + plain clauses; one-integer law (no raw values, no `higher/lower than N%` integers on glance); EN/ZH parity for every user-visible string; no `title=` attributes with translated text.

**Verdict: PASS.**

1. **Glance tier carries ONLY tier words + short plain clauses.** The new `.sx-vw-row` renders three columns: glyph, name+read, tier. The `name` column is the plain-word name (`Volatility level`, `5-day volatility change`, `Near-term vs far-term outlook`, `How much traders expect next`, `Overnight price swings`, `How much stocks move together`, `3-month implied correlation`, `Stock-by-stock swings`). The `read` column is empty on glance when the chip is mature (only a placeholder for `young|missing` chips). The `tier` column carries only the tier word (`calm / breeze / gust / storm`) and ZH twin. Internal metric names (`VIX1D`, `COR1M`, `COR3M`, `DSPX`, `TERM_SLOPE`, `VVIX_VIX`, `VIX_VELOCITY`, `VIX_LEVEL`) are demoted to the isle's dialog / disclosure tier per the macro-internal comment block (`templates/dashboard.html.j2` round-1 comment, lines 215-227). No raw numeric value (VIX close, percent, percent rank) is rendered on glance; the one-integer law holds (hero already owns the N/100 read).
2. **Engine-true binding, not template invention.** The `_vw_plain_name` map is consistent with the engine's `vm['vol_weather'].chips[]` contract; the PR body explicitly anchors `cor1m` to `engine/vol_velocity.py:601` `plain_normal="How much stocks move together is within the normal range."` and `cor3m` to `engine/vol_velocity.py:609` `name_en="COR3M (3-month implied correlation)"`. Verified by `grep -rn "How much stocks move together\|3-month implied correlation" engine/` → both strings live in `engine/vol_velocity.py` (`grep` confirmed: `:601` and `:609` are the exact sources). For the other 6 chips, the template's mapping is a plain-word gloss that does not introduce any new score, signal, or rank; the engine still owns `band` classification. CLAUDE.md §Neural Web + Mastermind chat (A7): "the LLM never originates signals" — this PR preserves that by reading tier words from `vm['vol_weather'].chips[].band` rather than computing them from percentiles in the template.
3. **EN/ZH parity is structurally preserved.** Every user-visible string in the diff has both an EN and a ZH twin wrapped in `<span class="l-en">` and `<span class="l-zh">` pairs: 8 chip names, 6 tier words, 1 eyebrow, 1 "still collecting" message, 1 em-dash. No EN-only or ZH-only skew was introduced. The bilingual UI guard (no translated text in `title=` attributes) is satisfied — the diff adds no `title=` attributes; the strip uses `l-en` / `l-zh` spans (verified by inspection of the new macro).
4. **"Still collecting" message is engine-consistent.** The new `.sx-vw-read` for `young|missing` chips renders `<span class="l-en">Still collecting — check back soon</span><span class="l-zh">仍在积累数据，稍后再看</span>`. The EN string is a tighter wording of the engine's existing `plain_en="Still collecting history."` (`engine/vol_velocity.py:180`) — same intent, no new tier-claim. The ZH string uses the "稍后再看" tail (slightly different from the engine's "仍在积累中") but the same message: chip not yet mature enough to render the tier read. No raw value or percentile phrase is rendered.
5. **No new `title=`, `aria-label`, `alt`, `h1`, `h2`, or body text is added.** The diff adds a new Jinja2 macro (a server-side templating construct, not user copy), an eyebrow + name + read + tier triple per chip (covered above), and a `data-sx-vw-strip` / `data-sx-vw-chip` / `data-tier` attribute set for selector + class-driven coloring. The `aria-hidden="true"` attributes on `.sg-wx` (the glyph) and `.sx-vw-read` (the empty glance row) are accessibility primitives, not translated text. The bilingual UI guard has nothing new to flag.
6. **No banned-vocab occurrences in user-visible strings.** Word-boundary grep on the diff hunks for `validated | verified | certified | approved | gauntlet | proofed | proven`: **zero hits** in the new code (`git diff c22864f8..e901ae4 -- templates/dashboard.html.j2 templates/theme.css | grep -inE '\b(validated|verified|certified|approved|gauntlet|proofed|proven)\b'` returned no matches). The PR's only tier-adjacent verb in user copy is "still collecting" — a calibrated non-claim about data freshness, not a promotion tier-claim. The eyebrow / name / tier / read strings are all plain descriptions of what the metric measures (`Volatility level`, `5-day volatility change`, `Near-term vs far-term outlook`, `How much traders expect next`, `Overnight price swings`, `How much stocks move together`, `3-month implied correlation`, `Stock-by-stock swings`) or the band word itself.
7. **No new raw hex colors in templates.** Round-6 explicitly removed the two raw hex/rgba literals (`templates/dashboard.html.j2:6978` `[color-literal] hex #0f1420` and `rgba(` on the added `.sg-pos-mark` line) by moving the rule into `templates/theme.css` (governed CSS) using `var(--panel)` and a sanctioned `rgba(0,0,0,.35)` box-shadow. The round-6 ratchet (`check_design_system.py --mode enforce-added`) returns 0 blocking findings on the lines NEW to this diff (with 18,961 pre-existing estate-wide findings reported but not gated — the macro estate's pre-existing debt is not the PR's debt).
8. **No new runtime style injection.** `check_runtime_style_injection.py` OK on 195 .js files (44 injecting, 89 total hits — all within frozen allowances). The PR adds zero bytes of JavaScript. The new macro renders only `class=""`, `data-sx-vw-strip`, `data-sx-vw-chip`, and `data-tier=""` attributes; the inline `style="--isle-color:var(--isle-risk)"` on the `#sx-risk-v2` opener is a merge-base byte form (was the pre-fold byte form, restored by R6 to fix the ratchet's false-positive on a missing semicolon), not a runtime style injection. Glyph coloring is class-driven in `theme.css`.
9. **PR body language is precise engineering prose, not user copy.** Words used in PR body §Authority / §Round-4 binding fixes / §Theme art direction are internal contract nouns (R-W1-A-AMENDED, R3 disposition ruling, R-E byte-identity gate, sha256, byte-identical, manifest, evidence gate, design-ratchet, tokenization, theme-rebind closure, screener bake, p0b receipts, basket fingerprint, capture-last, RED-first, contract-delta, ci-pack-8). Every paragraph identifies a commit by exact SHA and a slice by exact selector + sha256. The PR body never describes the vol-weather strip in marketing-language — the only user-facing description is "the strip" / "the vol-weather strip" / "the chips appear exactly once on the page", which is technical placement, not a tier claim.
10. **PR body disclaimers are explicit and doctrine-correct.** PR body §Summary: "Fold (not delete) the volatility-weather chip family … into the **visible** risk surface" — names the fold as placement, not deletion. PR body §Authority: "Engine-true binding: `vm['vol_weather']` already exists with 8 chips. **No new vm keys. No engine edits. No score/signal invention.**" — explicit non-claim of new signal/score. PR body §Engine-true binding: "Chip keys: `vix_level`, `vix_velocity`, `term_slope`, `vvix_vix`, `vix1d`, `cor1m`, `cor3m`, `dspx`." — exact chip key enumeration, no invented keys. The CI statement is honest: "CI: not claimed green — checks at this new head are pending / in flight" — anchored to local ratchet + test results, not to GitHub CI (the exact framing the standing macro law requires).

Net new banned-vocab occurrences in user-visible strings: **0** (zero hits on word-boundary grep of the diff hunks against the validated-claims vocabulary).

## Theme findings

TP-0 art-direction law in force (operator 2026-08-27): dark and light are TWO art directions, not one skin. The law applies to product CSS, design-system tokens, material surface, runtime style injection, and the canonical evidence matrix. PR #7539 is a template + theme + site-regen + 3 new test files + 8-cell evidence matrix; it carries substantive theme bytes (155 lines of new theme.css, regenerated site/theme.css, 16 PNGs + 4 hover/focus PNGs).

**Verdict: PASS.**

1. **Both dark AND light treatments are present, distinct, and evidenced.** Dark treatment: `color-mix(in srgb, var(--panel2) 56%, transparent)` for strip bg, `var(--line) 70%` for strip border-top, `var(--panel) 88%` for row bg, inset `rgba(255,255,255,.02)` hairline — command-center depth. Light treatment (via `[data-theme='light']` overrides): `color-mix(in srgb, var(--panel2) 30%, var(--panel) 70%)` for strip bg, `var(--line) 80%` for strip border-top, `var(--panel) 100%` for row bg, `0 1px 2px rgba(15,23,42,.04)` for row shadow — research workspace, hairline discipline, shadow instead of glow. The two treatments are visually distinct: dark has translucent panels + inset hairline + glyph accent; light has solid white panels + soft shadow + same glyph accent. Glyph colors stay the same semantic meaning in both themes (`--up / --info / --warn / --down`).
2. **8-cell evidence matrix committed.** `mockups/evidence/unified-dashboard-b2w1/` carries 8 PNGs (theme `dark|light` × lang `en|zh` × viewport `1440|390`): `macro_dark_en_1440.png`, `macro_dark_en_390.png`, `macro_dark_zh_1440.png`, `macro_dark_zh_390.png`, `macro_light_en_1440.png`, `macro_light_en_390.png`, `macro_light_zh_1440.png`, `macro_light_zh_390.png` — all `changeType=ADDED` on round 1 (`a85b884ab8`) and re-captured at round 4 (`9bb52598e4`) and round 6 (`e901ae422c`). 4 additional hover/focus PNGs (round 6): `risk_isle_hover_en_dark_1440.png`, `risk_isle_hover_en_light_1440.png`, `risk_isle_focus_en_dark_1440.png`, `risk_isle_focus_en_light_1440.png`. The 8+4-cell matrix satisfies the standing "dark × light × EN/ZH × desktop/mobile" evidence requirement for a substantive product styling change.
3. **R-E byte-identity gate holds.** Selector `.mx5-sc-left` (slice from `'<div class="mx5-sc-left">'` to matching close). sha256 `346b194c2cb82100aac7d26efe27c99f2bc3d6629f84db6ffa37cd2053a23358` (23397 bytes) — byte-identical at capture-time head `cb4ed1c0ea` and at FINAL head `e901ae422c`. `git merge-base --is-ancestor cb4ed1c0ea HEAD` and `git merge-base --is-ancestor e901ae422c HEAD` both exit 0. R-W1-E capture-last: `git diff e901ae422c..HEAD -- templates/ mockups/evidence/unified-dashboard-b2w1/` = EMPTY.
4. **No raw hex colors in `templates/`.** Round-6 explicitly moved the `.sg-pos-mark` rule (the only raw hex/rgba literals added in earlier rounds) from `templates/dashboard.html.j2` into `templates/theme.css`. Verified: `git diff c22864f8..e901ae4 -- templates/dashboard.html.j2 | grep -nE '#[0-9a-fA-F]{3,8}|rgba?\('` returns 0 hits on the diff hunks (the two original literals — `#0f1420` and `rgba(` at line 6978 — were removed in round 6 and re-added inside `templates/theme.css` using the canonical `var(--panel)` and the sanctioned `rgba(0,0,0,.35)` box-shadow). The round-6 local ratchet (`check_design_system.py --mode enforce-added`) returns 0 blocking findings on the lines NEW to this diff.
5. **No runtime style injection.** `check_runtime_style_injection.py` OK on 195 .js files (44 injecting, 89 total hits — all within frozen allowances). The PR adds zero bytes of JavaScript. The new macro renders only `class=""`, `data-sx-vw-strip`, `data-sx-vw-chip`, and `data-tier=""` attributes. Glyph coloring is class-driven in `theme.css`. The `style="--isle-color:var(--isle-risk)"` on the `#sx-risk-v2` opener is a merge-base byte form (the pre-fold byte form, restored by R6 to fix the ratchet's false-positive on a missing semicolon), not a runtime style injection.
6. **Paired `templates/<name>` ↔ `site/<name>` sync is preserved.** PR body reports `python3 -m scripts.check_template_site_sync` → "template↔site sync OK (99 pairs checked)". `site/macro.html` and `site/theme.css` are regenerated from `templates/dashboard.html.j2` + `templates/theme.css`. `diff -q templates/theme.css site/theme.css` silent. The 121 generated basket pages and the screener page all carry the new theme stamp `62b49cc0`.
7. **Token substitution is canon, not invented.** All new theme colors use `color-mix(in srgb, var(--token) N%, {transparent|var(--token2)})` — the canonical token family. Border-radius uses `var(--r-ctl,8px)` (specimen default). No new `--token: literal` declarations added; the only existing tokens referenced are `--panel`, `--panel2`, `--line`, `--ink-1`, `--ink-2`, `--ink-3`, `--text`, `--isle-color`, `--up`, `--info`, `--warn`, `--down`, `--muted`, `--link`, `--r-ctl`. The `templates/theme.css` line-3 comment block names the dark + light treatments as a unit, not as a single shared CSS.
8. **Mobile wrap is intentional and breakpoint-scoped.** `@media(max-width:560px)` breakpoint sets `.sx-vw-read { overflow-wrap: anywhere }` (allows the read sentence to wrap, never truncate) and `.sx-vw-name` keeps `text-overflow: ellipsis` only as a fallback. The 390 strip width is **327** (not a 354-for-every-390 lie); the 1440 strip width is **354**; strip heights 391 / 371 — measured, not invented.
9. **EN/ZH parity on visual surfaces.** Every user-visible string has an EN + ZH twin in `<span class="l-en">…</span><span class="l-zh">…</span>` pairs. The `data-sx-vw-strip` / `data-sx-vw-chip` / `data-tier` attributes are language-neutral. The strip's geometry is identical across EN and ZH (the eyebrow / name / read / tier cells use the same grid template `24px minmax(0,1fr) 70px` in both languages). No visual asymmetry between EN and ZH.
10. **CI guard `scripts/check_design_system.py --self-check` OK.** "self-check OK: 8 rule fixtures + clean fixture + traversal". The macro design-system ratchet is binding. Forward-only: 0 blocking findings on lines NEW to this diff (18,961 pre-existing estate findings reported, not gated — the PR is forward-only clean).

No new theme debt; no color-literal addition in templates; evidence matrix complete (8 + 4 cells); paired-asset sync preserved; tokens substituted canonically.

## Validated-claims findings

The standing macro law: the word `validated` (and friends: `verified`, `proofed`, `certified`, `approved`, `gauntlet`, etc.) is CI-enforced via `scripts/check_validated_claims.py`; the gate scans `templates/`, `site/*.js`, generated `*_data.js`, and `engine/` display-copy fields (verified by reading `check_validated_claims.py:7-21`); context/data/detection/tagging artifacts stay display-tier until they clear the gauntlet; the word "validated" in user-facing text is CI-enforced (CLAUDE.md §Epistemics gauntlet = PROMOTION gate).

**Verdict: PASS.**

1. **Zero tier-claim vocabulary in new user-facing strings.** Word-boundary grep on the diff hunks (`templates/dashboard.html.j2` + `templates/theme.css`) for `validated | verified | certified | approved | gauntlet | proofed | proven`: **0 hits** (`git diff c22864f8..e901ae4 -- templates/dashboard.html.j2 templates/theme.css | grep -inE '\b(validated|verified|certified|approved|gauntlet|proofed|proven)\b'` returned no matches in the user-facing surface). The PR body uses these words liberally, but the PR body is engineering prose, not a tier-claim.
2. **The engine-true binding is anchored to the canonical B1/B3 contract.** The new macro reads tier words from `vm['vol_weather'].chips[].band` — the engine already classifies (PR body §Authority: "The tier word comes straight from `vm['vol_weather'].chips[].band` — the engine already classifies; the template does not re-classify from percentiles"). The template is a pure renderer of engine-owned band values. No new tier promotion, no new gauntlet run, no new promotion claim. The vol-weather strip is a display-tier fold, not a tier promotion.
3. **No new "validated / verified / certified" status flips on user-facing chips.** The chip tiers are `calm / quiet / low / normal / elevated / extreme` (and `young` for fresh data) — these are engine bands describing volatility regime, not promotion tiers. The macro-internal comment block explicitly demotes internal metric names to disclosure tier; the glance tier carries only the tier word + name + (when `young|missing`) a calibrated non-claim. The tier words are descriptive regimes, not validation verdicts.
4. **No new score/signal invention.** PR body §Engine-true binding: "Engine-true binding: `vm['vol_weather']` already exists with 8 chips. **No new vm keys. No engine edits. No score/signal invention.**" Verified by diff inspection: the new macro is pure rendering — it reads `vm['vol_weather']` and outputs glyph + name + tier cells. It does not compute a score, percentile, rank, or signal. CLAUDE.md §Neural Web + Mastermind chat (A7): "the LLM never originates signals" — this PR preserves that: it carries an existing engine fact (the `band` field on each chip) to a new UI host.
5. **The "still collecting" message is a calibrated non-claim.** For `young|missing` chips, the render is `<span class="l-en">Still collecting — check back soon</span><span class="l-zh">仍在积累数据，稍后再看</span>`. This is a freshness-state disclosure (the chip does not yet have enough history to render the tier read), not a tier-claim. The em-dash placeholder in the tier column reinforces the non-claim — no fake tier word is rendered for an immature chip.
6. **CI guard `scripts/check_validated_claims.py` is implicit-clean on the user-facing surface.** The PR body transcript does not surface any `::error title=validated-claims::…` line. The local design-ratchet run on the diff (`check_design_system.py --mode enforce-added`) returns 0 blocking findings on the lines NEW to this diff. Both gates are forward-only-clean on the PR's user-facing surface.
7. **PR body anchors every claim to a verifiable receipt.** PR body §R-E evidence names the slice sha256 (`346b194c2cb8`), the bytes (23397), the head SHA (`e901ae422c`), the captured_at_head (`cb4ed1c0ea`). PR body §Round-4 binding fixes names the RED-first pytest tail verbatim. PR body §Round-5 rebind names the exact theme.css hash transition (`665c4b34` → `4c26bb2c` → `62b49cc0`). PR body §Round-6 tokenization names each of the four ratchet findings and the canonical-token fix. PR body §Tests names the exact pytest invocations and exit codes (179 passed in 5.74s; 46 passed + 1 skipped in 37.60s; 5 passed in 1.47s). Every paragraph anchors a claim to a SHA / sha256 / pytest exit code / file path — no soft claims.
8. **PR body is honest about CI state.** "CI: not claimed green — checks at this new head are pending / in flight" — anchored to local ratchet + test results, NOT to GitHub CI. The PR is DRAFT (never ready, never merge, no merge-on-green, no labels). The macro-side Vercel check fails on the same `Deployment rate limited — retry in 24 hours` reason as the rest of the repo's Vercel checks — pre-existing infrastructure state, not introduced by this PR.

No new tier claims; no new score/signal invention; the engine-true binding holds; the validated-claims CI guard is implicit-clean on the user-facing surface.

## Overall verdict

**PASS** on plain-language, theme, and validated-claims laws.

- **Plain-language**: PASS. The new macro renders only tier words + plain-word chip names + a calibrated non-claim for `young|missing` chips. Internal metric names demote to disclosure tier. One-integer law holds. EN/ZH parity structurally preserved. Engine-true binding: the template reads `vm['vol_weather'].chips[].band` rather than computing from percentiles. CLAUDE.md §Neural Web A7 ("the LLM never originates signals") holds.
- **Theme**: PASS. Dark and light are two art directions (dark = command-center depth + inset hairline + glyph accent; light = research workspace + solid panels + soft shadow). 8-cell evidence matrix committed + 4 additional hover/focus cells (round 6). R-E byte-identity gate holds (slice sha256 `346b194c2cb8` byte-identical at capture-time head and FINAL head). No raw hex in templates (round 6 explicitly removed the two `.sg-pos-mark` literals and moved the rule into `templates/theme.css`). Forward-only ratchet clean: 0 blocking findings on lines NEW to this diff.
- **Validated-claims**: PASS. Zero tier-claim vocabulary in new user-facing strings. Engine-true binding: no new vm keys, no engine edits, no score/signal invention. The strip is a display-tier fold, not a tier promotion. The "still collecting" message is a calibrated freshness-state non-claim. PR body anchored to verifiable receipts (SHA / sha256 / pytest exit codes) and honest about CI state.

PR is DRAFT and not merged-to-green; the audit is against the FINAL head `e901ae422cd6ffb8ec251472a38fd16596c7fea8` (capture-last per R-W1-E), and the audit follows the operator's standing "merge-on CONCLUDED checks" rule by not recommending an exit — the PR body itself acknowledges "CI: not claimed green". The macro-side Vercel failure is a pre-existing repo-wide rate-limit, not a PR-level regression. The design-governance forward-only ratchet that failed on prior head `cb4ed1c0ea` is locally clean on this head; the next CI run will be the binding arbiter.

DONE_SHA=e901ae422cd6ffb8ec251472a38fd16596c7fea8
