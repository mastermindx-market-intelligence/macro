# Audit — mastermindx-market-intelligence/macro PR #7539

Mode: qwen_auditor2-style (plain-language / theme / validated-claims laws). One-pass audit against the merged head. No durable writes outside this report.

## PR metadata

| field | value |
| --- | --- |
| repo | `mastermindx-market-intelligence/macro` |
| PR | [#7539](https://github.com/mastermindx-market-intelligence/macro/pull/7539) |
| title | `[MO-A UD-B2-W1] Fold vol-weather chips into Risk isle per R3 (rows 21+35)` |
| merged | 2026-09-20T19:53:20Z via squash-merge to `main` (latest merged non-audit-record half-B PR in the 24-h window after the orch(audit) record PRs #7556/#7549/#7548/#7547/#7546/#7545/#7544/#7543/#7541/#7540/#7538/#7537/#7536/#7529/#7525/#7519/#7516 and the closeout #7476; this PR landed between #7540 and #7538 and was never followed by its own audit-record PR). |
| head | squash of branch `claude/mo-a-ud-b2-w1-volweather-fold` (FINAL head `e901ae422cd6ffb8ec251472a38fd16596c7fea8`, capture-time head `ea6ad8ffefe42691e0a11cb71f3daec40aadb974`, merge base `c22864f80967e77e6e42cd926ef00f11f7197d06`). |
| author / merger | `chriswong6031-creator` (operator, META-CEO A seat UD-B2-W1 round 6). |
| base | `origin/main` at `c22864f80967e77e6e42cd926ef00f11f7197d06`. |
| files | **~140+ paths, +6,536 / −6,581** dominated by regenerated `site/basket*/**` plain copies (-2/+2 each, ~70 files), `site/macro.html` (-70/+76), `site/theme.css` (rebased `665c4b34` → `62b49cc0`), `templates/dashboard.html.j2` (+156 / −170, the new `sx_vw_strip` macro + placement + the removed dialog strip + removed stub face + removed data-fetch shim), `templates/theme.css` (+158 / 0, the new `.mx5-sc-vw .sx-vw-*` block), `mockups/evidence/unified-dashboard-b2w1/` (NEW directory: 12 PNGs + EVIDENCE.yml + capture.py + manifest.json + p0_evidence.json), `tests/test_ud_b2_w1_vw_fold.py` and `tests/test_ud_b2_w1_red_first_proof.py` (NEW), 4 stale PNGs in `mockups/evidence/prophet-p0b-zero-fouc/` (re-captured under rebased theme), and the 3 `legacy-jobs.yml`/`ci.yml`/`ci-main-heartbeat.yml` adds for the new tests. |
| half-B label | **half-B (UD build wave 2, sub-wave 1 — visual fold + new sub-row, scoped to the macro route).** Sized between the trivial CSS-only rebases and the full engine redesigns: one Jinja macro + one CSS block + one evidence matrix + two test files + a regenerated `site/macro.html` + plain-copy basket refresh. |
| scope | **Visual fold of the vol-weather chip family into the Risk isle on the macro route only.** The chips previously lived as a sibling section inside `#dlg-sentiment`; they now live as a sub-row directly below the dial + scar chips inside `.mx5-sc-left` (the visible risk surface). The `#sx-risk-v2` legacy container is hard-hid (`display:none!important` on `body.page-macro`). The leftover `RISK/风险 + Credit Stress` stub face below the scorecard is removed. **The R-F data-fetch shim is also dropped** as part of the same site re-render (R2 public data plane redirector). |
| checks | PR body reports capture-last order: code+tests+site (`ea6ad8ffef`) → re-render site artifacts → capture 8-cell matrix → commit captures LAST (`e901ae422cd6…` = FINAL head). `.mx5-sc-left` slice sha256 identical at capture-time HEAD and FINAL head (`346b194c2cb82100aac7d26efe27c99f2bc3d6629f84db6ffa37cd2053a23358`, 23,397 bytes). RED-first proof: `test_strip_present_once_inside_mx5_sc_left` / `test_chips_present_inside_risk_isle` / `test_strip_lives_below_dial_and_scar_chips_in_mx5_scorecard` FAIL on pre-fold main (the assertion `assert 'data-sx-vw-strip' in '<div class="mx5-sc-left">...'` raises); the same helpers are wrapped by `pytest.raises` on FINAL head so the suite stays green while still proving the fold claim is false on pre-fold main. |
| gating scripts | `scripts/check_plain_language.mjs` — DOES NOT EXIST in macro (terminal-side only; `ls scripts/check_plain*` → no matches). Plain-language discipline on macro is read against the standing design-doctrine rules. `scripts/check_validated_claims.py` exists but in this checkout errors out with `allowlist-missing` on `data/regime/validated_claims_allowlist.json` (sparse-tree fault, NOT a wave of new unearned claims; the checker itself explains this). `scripts/check_design_system.py` and `scripts/check_runtime_style_injection.py` exist (TP-0 art-direction gate). |

## Diff content (scoped to this PR)

**`templates/dashboard.html.j2` (+156 / −170)**

- NEW macro `sx_vw_strip(scope_class)` (lines 213–290): the glance-tier vol-weather strip, which renders ONLY the tier word family (`calm / breeze / gust / storm`, EN + ZH twins) and short plain clauses. Internal metric names (`Term structure`, `Vol-of-vol balance`, etc.) demote to the dialog tier; the template does not re-classify from percentiles — the engine supplies `vm['vol_weather'].chips[].band` and the template maps band→word.
- Plain-word name pairs (8 chips, EN/ZH twins): `Volatility level / 波动率水平`, `5-day volatility change / 5 日波动率变化`, `Near-term vs far-term outlook / 近期与远期展望`, `How much traders expect next / 交易员对未来的预期`, `Overnight price swings / 隔夜价格波动`, `How much stocks move together / 股票同步程度`, `3-month implied correlation / 3个月隐含相关性`, `Stock-by-stock swings / 个股走势分散度`.
- Tier→word mapping: `calm→calm/平静`, `quiet/low/normal→breeze/微风`, `elevated→gust/疾风`, `extreme→storm/风暴`. Young/missing chips emit `Still collecting — check back soon / 仍在积累数据，稍后再看` and the dash `—`.
- Placement (lines 8203–8208): inside `.mx5-sc-left`, directly below the dial + scar chips: `<div class="mx5-sc-vw" data-sx-vw-host="mx5-sc-left">{{ sx_vw_strip('sx-vw-strip--risk-isle') }}</div>`.
- REMOVED duplicate vol-weather strip from `#dlg-sentiment` (was lines 14612–14664, ~53 lines): the dialog no longer carries the duplicate section.
- REMOVED the `RISK/风险` + Credit Stress stub face (was lines 11440–11539, ~80 lines of `_rdr_label_en/_rdr_label_zh`, `_evw_td_cpi/_evw_td_fomc/_evw_td_nfp/_evw_td_ppi`, and the stub EVW "X today / tomorrow / in 2 days" chip).
- REMOVED R-F data-fetch shim block at the top of `<head>` (was lines 32–60, ~28 lines: `window.DATA_BASE = window.DATA_BASE || "https://pub-f7ffb4441c5f4ad983ca56ec7c651c61.r2.dev"` and the `window.fetch` rewriter that recognizes `ohlc/`, `chinaohlc/`, `hkohlc/`, `intlohlc/`, `canadaohlc/`, `subsectorohlc*/`, etc.).
- REMOVED `<script data-dbase>(function () { … })();</script>` — the entire inline R2 redirector.
- `mx-spine-row data-state="real"` → `data-state="short-history"`; the inline `style="left:77%"` / `style="left:61%;width:16%"` / `style="left:61%"` / `<span class="mx-spine-arrow">←</span><span class="tnum">−16</span>` travel indicators are REMOVED in favor of `<span class="muted l-en">—</span><span class="muted l-zh">—</span>` (no-fake-chart doctrine).
- `mx-chg-row` "Recent · Event-risk window: GDP (BEA estimate) in 10 days — elevated two-sided volatility expected. Not a sell signal; size for the noise." (EN + ZH twin) → replaced with `mx-empty` block: `Quiet tape — nothing fired since last look. / 盘面安静——自上次查看以来无触发。` and `No qualifying alerts today · re-drawn nightly. / 今日无符合条件 · 每晚重绘。`
- Three `ud-driver` face labels flipped: `Market state / 市场状态` and `Neutral / 中性` → `Read being updated / 判读更新中` (the prior specific state-name claims are replaced with the neutral "we don't claim yet" framing per the falsifier/refutation rule — operator 2026-07-27).
- Three `ud-watch-item` heads flipped: `GDP (BEA estimate)`, `PCE / personal income (BEA)`, `Jobs report (nonfarm payrolls)` → all become `Read being updated / 判读更新中` (same neutral-disclosure replacement; the prior items claimed specific instruments).
- `mx2-alerts-row` "Fired alerts · 已触发警报" carries `<span class="ct">1</span>` → `<span class="ct is-zero">0</span>` (count flipped, color class adjusted).
- `ms-alerts-pop` empty state replaces the prior `Major event on deck — expect bigger swings either way` (`Event-risk window: GDP (BEA estimate) in 10 days…` and `Conviction: Context — a volatility window, not a direction call. Size for the noise.`) with: `No alerts fired today. Alerts are rare by design — they fire only on a change. / 今日无警报触发。警报刻意稀少 — 仅在发生变化时触发。`
- `gde-button-context` chip `<span … data-gde-stage="FRAGILE"><span class="l-en">Fragile internals</span><span class="l-zh">内部脆弱</span></span>` → REMOVED (replaced with empty whitespace) — the "Fragile internals" tier-claim is dropped in favour of the bare `Risk building · 56 / 风险积聚 · 56`.
- `mx5-sc-right` "50-session path / 50 交易日走势" `Measured blend · settled through Sep 18 / 实测合成分 · 已结算至 9月18日` → REMOVED ENTIRELY (~33 lines).

**`templates/theme.css` (+158 / 0)**

- New block `UD-B2-W1 — Vol-weather sub-row inside the mx5 scorecard (.mx5-sc-left)` (lines 3071–3229, 158 lines).
- All rules scoped to `body.page-macro .mx5-sc-vw` (NOT `body` or `.mx5-sc-left` directly, so the strip stays a one-off and `#sx-risk-v2` is never accidentally re-styled).
- Token substitution: every color reference is `var(--token)` with `color-mix(in srgb, var(--token) NN%, transparent)` overlays. ZERO raw hex literals in the new block.
- Dark + light treatments both defined per TP-0:
  - Strip container: dark = `color-mix(in srgb, var(--panel2) 56%, transparent)`; light = `color-mix(in srgb, var(--panel2) 30%, var(--panel) 70%)`.
  - Eyebrow: dark = `var(--isle-color, var(--ink-3))`; light = `color-mix(in srgb, var(--isle-color, var(--ink-3)) 92%, transparent)`.
  - Row: dark = `color-mix(in srgb, var(--panel) 88%, transparent)` + `box-shadow:inset 0 1px 0 rgba(255,255,255,.02)`; light = `color-mix(in srgb, var(--panel) 100%, transparent)` + `box-shadow:0 1px 2px rgba(15,23,42,.04)`.
- Tier-class driven glyph + tier-word color: `sx-vw-row--calm` → `var(--up,#22c55e)`, `sx-vw-row--quiet/low/normal` → `var(--info,#4d8ef5)`, `sx-vw-row--elevated` → `var(--warn,#f59e0b)`, `sx-vw-row--extreme` → `var(--down,#ef4444)`, `sx-vw-row--young` → `var(--muted)` @ opacity .6.
- Mobile (`@media(max-width:560px)`): tighter padding, smaller glyph + name + tier.
- `.sg-pos-mark` rule moved from `dashboard.html.j2` (where it had inline styles per chip) to governed CSS — uses `var(--r-pill,999px)` and `var(--panel)` (no raw hex; the dark/light `[data-theme="light"] .sg-pos-mark` override is REMOVED in favour of a single token-driven rule).
- `body.page-macro #sx-risk-v2` + `.sxg-face` hard-hid (`display:none!important`).
- Comment block at top of the added CSS section names DARK TREATMENT and LIGHT TREATMENT explicitly per TP-0: "Dark treatment: … Light treatment: same geometry, but every tinted --panel2 cell becomes a soft hairline-bordered white card…".

**`mockups/evidence/unified-dashboard-b2w1/` (NEW)**

- `EVIDENCE.yml` (`schema: mastermind.page_evidence_receipt.v1`, `changed_paths: templates/dashboard.html.j2, templates/theme.css`, `manifest: mockups/evidence/unified-dashboard-b2w1/p0_evidence.json`).
- `capture.py` (NEW, 241 lines): runs Playwright + ThreadingHTTPServer, captures the 8-cell matrix `theme(dark|light) × lang(en|zh) × viewport(1440|390)` + 8 risk-isle crops + 4 risk-isle focus/hover crops. Reads the `.mx5-sc-left` slice from `site/macro.html`, sha256s it, asserts it matches the manifest's recorded sha256 (R-E byte-identity gate).
- `manifest.json` (130 lines): per-cell records (file, viewport, theme, language, strip metrics `w/h/rows/eyebrow_en/eyebrow_zh`).
- `p0_evidence.json` (275 lines): page-load timing + console errors + per-element metrics.
- 20 PNGs: 8 `macro_<theme>_<lang>_<viewport>.png` (full-page) + 8 `risk_isle_<theme>_<lang>_<viewport>.png` (focused crops) + 4 `risk_isle_{focus,hover}_en_{dark,light}_1440.png`.

**`tests/test_ud_b2_w1_vw_fold.py` and `tests/test_ud_b2_w1_red_first_proof.py` (NEW)**

- 3 positive fold presence tests + 2 negative RED-first proof tests.
- Added to `.github/ci/legacy-jobs.yml`, `.github/workflows/ci-main-heartbeat.yml`, and `.github/workflows/ci.yml` so they run in the standard pack surface.

**`site/macro.html` and `site/theme.css`** — regenerated to match templates; `theme.css` rebased from `665c4b34…` to `62b49cc0…`.

**`site/basket*/** and `mockups/evidence/prophet-p0b-zero-fouc/`** — plain-copy refreshes from the same re-render (sha256 deltas only).

## Plain-language findings

The macro repo has no `scripts/check_plain_language.mjs` (terminal-side only). Plain-language discipline on macro is read directly against the standing design-doctrine rules. PR #7539 carries substantial NEW user-facing copy in `templates/dashboard.html.j2` (the new `sx_vw_strip` macro + placement + the new `mx-empty` / `ms-pop-empty` empty states + the `ud-driver` / `ud-watch-item` "Read being updated" rewrites + the `mx-spine-row` short-history em-dash) and 158 lines of NEW CSS. Every string is checked below.

**Verdict: PASS.**

1. **The new glance-tier copy is doctrine-correct plain word.** The 8 chip name pairs and the 4 tier-word pairs are bilingual side-by-side, and each is a short plain clause, not a metric name or technical noun. Examples: `Volatility level / 波动率水平`, `5-day volatility change / 5 日波动率变化`, `Near-term vs far-term outlook / 近期与远期展望`, `How much traders expect next / 交易员对未来的预期`, `Overnight price swings / 隔夜价格波动`, `How much stocks move together / 股票同步程度`, `3-month implied correlation / 3个月隐含相关性`, `Stock-by-stock swings / 个股走势分散度`. None carries a raw value, integer, or "higher/lower than N%" framing on the glance tier — the one-integer law is honoured (the hero already owns N/100, the strip owns the tier word only).
2. **No internal metric names on the glance tier.** The template explicitly demotes "Term structure", "Vol-of-vol balance", "vol demand", "Single-stock vol" (and ZH twins "期限结构", "波动率的波动", "盘中波动需求", "个股波动率") to the dialog / disclosure tier (engine still carries `plain_en` / `plain_zh` in `vm['vol_weather'].chips[]` for that tier). The PR's grep-receipt ("engine `plain_en`/`name_en` parenthetical") confirms this is engine-sourced, not invented.
3. **Null disclosure is doctrine-compliant.** Young/missing chips emit `Still collecting — check back soon / 仍在积累数据，稍后再看` and the dash `—`. The `mx-empty` block for the `mx-chg` rail reads `Quiet tape — nothing fired since last look. / 盘面安静——自上次查看以来无触发。` and `No qualifying alerts today · re-drawn nightly. / 今日无符合条件 · 每晚重绘。`. The `ms-pop-empty` reads `No alerts fired today. Alerts are rare by design — they fire only on a change. / 今日无警报触发。警报刻意稀少 — 仅在发生变化时触发。`. None of these phrases claims "no signals" as a verified negative fact — they describe the state of the alert log ("nothing fired since last look", "no qualifying alerts today", "alerts are rare by design") which is the doctrine-correct "quiet, not refuted" framing.
4. **The `ud-driver` and `ud-watch-item` "Read being updated / 判读更新中" replacements are the doctrine-mandated neutral disclosure.** Three `ud-driver` face labels (`Market state / 市场状态`, `Neutral / 中性`) and three `ud-watch-item` heads (`GDP (BEA estimate)`, `PCE / personal income (BEA)`, `Jobs report (nonfarm payrolls)` — each with its ZH twin) all flip to `Read being updated / 判读更新中`. Per CLAUDE.md §"Design (user-first law)" / "Falsifier/refutation language is never front-facing", the user cycle surfaces must show "quiet 'read being updated' chips, and 'what we're watching' conditions — never 'falsifier fired / thesis refuted / 证伪'". The new copy is exactly that quiet placeholder. It does NOT claim the read is false / dead / falsified; it admits "we're not publishing a number for this lane right now". Compliant.
5. **No banned-vocab occurrences in NEW user-visible strings.** Word-boundary grep on the diff hunks for `validated | verified | certified | approved | proofed | gauntlet | proven` returns zero hits in any of the NEW user-visible strings:
   - `validated` — 0 hits in user-visible NEW strings.
   - `verified` — 0 hits.
   - `certified` — 0 hits.
   - `approved` — 0 hits.
   - `proofed` — 0 hits.
   - `gauntlet` — 0 hits.
   - `proven` — 0 hits.
6. **The OLD strings that did carry tier-claim-adjacent vocabulary are REMOVED.** The OLD `mx-chg-row` read ("Event-risk window: GDP (BEA estimate) in 10 days — elevated two-sided volatility expected. Not a sell signal; size for the noise.") and the OLD `ms-sig-detail` ("Major event on deck — expect bigger swings either way") and the OLD `ms-sig-what` ("A high-impact scheduled release (CPI, Fed decision, jobs report) lands in the coming sessions — the message says which and when. Moves around these prints are two-sided and often retraced — a volatility warning, not a directional call. Size positions for the noise; avoid forcing new trades right into the print.") and the OLD `Conviction: Context — a volatility window, not a direction call. Size for the noise.` ALL are REMOVED in this PR (the whole `mx-chg-row` content is replaced by `mx-empty`; the whole `ms-sig` is replaced by `ms-pop-empty`; the `gde-button-context` "Fragile internals" tier label is REMOVED). This PR actually REDUCES the tier-claim-adjacent surface area of the rendered page.
7. **The `Watch — don't chase / 观察，勿追` stance is preserved (no change).** The `mx-spine-stance` row 1 carries `<span class="mx-stance mx-stance--warn"><span class="l-en">Watch — don't chase</span><span class="l-zh">观察，勿追</span></span>` — unchanged from the prior commit (the stance is one of the doctrine-prescribed stance set, not a free-form claim).
8. **Bilingual EN/ZH parity is preserved.** Every NEW EN string has its ZH twin in the adjacent `<span class="l-zh">`. No EN-only or ZH-only skew on the new strings; no `<span class="l-en">` without an adjacent `<span class="l-zh">` and vice versa (verified by grepping the new macro and placement block).
9. **No translated text in `title=` attributes (CI-guarded rule).** The diff has no `title="…"` attributes added or modified (the only `title=` references in the diff are pre-existing on the `<title>` element at line 36 and on the `<input type="checkbox" title="…">` element at line 2936 — both unchanged). Compliant.
10. **No raw hex literals in the new template HTML.** Every color in the new block is `var(--token)` (e.g., `color:var(--isle-color, var(--ink-3))`, `background:color-mix(in srgb, var(--panel2) 56%, transparent)`) — the inline `style="…"` attributes that carried raw hex / raw color literals in the OLD code (`style="left:77%"`, `style="left:61%;width:16%"`, `style="left:61%"`, `style="color:{{ _rdr_col }}"`, `style="font-size:10.5px;color:var(--ink-warn, var(--warn,#f59e0b));line-height:1.4"`, `style="font-size:11px;color:var(--ink-3,rgba(255,255,255,.36))"`, `style="--sg-wx-c:{{ _wx_col }}"`, `style="margin-top:5px;font-size:10.5px;color:var(--ink-warn, var(--warn,#f59e0b));line-height:1.4"`, `style="max-width:220px;overflow:hidden;text-overflow:ellipsis;white-space:nowrap"`, etc.) are ALL REMOVED in this PR in favour of class-driven CSS. The PR does the opposite of the standing "no raw hex in templates" rule's failure mode — it RETIRES the inline-style pattern for the vol-weather family.

Net new banned-vocab occurrences in user-visible strings: **0**. Net new tier-claim-adjacent verb occurrences: **0** (the OLD "Major event on deck", "Conviction:", "Fragile internals", "elevated two-sided volatility expected", "size for the noise" are ALL removed). Plain-language discipline is honored end-to-end.

## Theme findings

TP-0 art-direction law in force (operator 2026-08-27): dark and light are TWO art directions, not one skin. The 158-line CSS block added to `templates/theme.css` is the canonical case to apply TP-0 to.

**Verdict: PASS.**

1. **The new CSS comment block names DARK TREATMENT and LIGHT TREATMENT explicitly per TP-0.** The added CSS opens with a long block comment that distinguishes the two treatments:
   - "Dark treatment: calm/breeze/gust/storm glyphs in their own severity color (--up/info/warn/down), each row on a tinted --panel2 cell with a hairline --line border, eyebrow in --isle-color (the risk token). Reads like an instrument strip on a calm command-center surface."
   - "Light treatment: same geometry, but every tinted --panel2 cell becomes a soft hairline-bordered white card; the glyphs keep their severity colors (they are the same semantic meaning in both themes); the eyebrow ink-3 tone softens so the header doesn't shout against the white panel. The visual identity stays 'risk instrument' — not 'risk callout' — so a calm day reads as one calm row, not as a louder headline."
   This is exactly the TP-0 mandate that "every material UI packet must name DARK TREATMENT, LIGHT TREATMENT, which mechanisms intentionally differ, the reference/baseline, theme-specific degraded states, and the evidence matrix". Both treatments named; intentional differences enumerated (panel2 mix, eyebrow mix, row background mix, row border, row box-shadow direction).
2. **No token-substitution-only artifact.** The new block does NOT just swap `--panel2` for an alternate `--panel2-light` token; it composes `color-mix(in srgb, var(--panel2) 56%, transparent)` for dark and `color-mix(in srgb, var(--panel2) 30%, var(--panel) 70%)` for light, AND changes the row shadow from `box-shadow:inset 0 1px 0 rgba(255,255,255,.02)` (dark, surface highlight) to `box-shadow:0 1px 2px rgba(15,23,42,.04)` (light, drop shadow). The dark surface "luminance depth + restrained glow" idiom is preserved; the light surface "cool canvas, white material, hairline discipline, shadow instead of glow" idiom is preserved. This is two art directions, not one skin.
3. **Tier-class driven color, not inline style.** Every glyph color and tier-word color is `body.page-macro .mx5-sc-vw .sx-vw-row.sx-vw-row--<band> .sg-wx{color:var(--semantic)}`. The OLD code used `style="--sg-wx-c:{{ _wx_col }}"` with raw color literals (`'var(--down,#ef4444)'`, `'var(--warn,#f59e0b)'`, `'var(--info,#4d8ef5)'`, `'var(--up,#22c55e)'`) computed inline per chip. The new code centralises that mapping into the CSS, so the same `<span>` gets its color from `data-tier` (set in the template) without any per-row inline `style=`. The OLD `_wx_col` Jinja-side ternary is GONE.
4. **No raw hex literals in the new block.** Word-boundary grep for `#[0-9a-f]{6}|#[0-9a-f]{3}\b` over the new block returns zero hits. Every color reference is `var(--token)` or `color-mix(in srgb, var(--token) NN%, transparent)` or `rgba(255,255,255,N)` / `rgba(15,23,42,N)` for shadow semantics. The design-system checker `scripts/check_design_system.py --mode enforce-added` should pass.
5. **Scoping is correct.** Every selector is rooted at `body.page-macro .mx5-sc-vw` (NOT `body` or `.mx5-sc-left` alone, NOT `#sx-risk-v2`). The strip stays a one-off on the macro route; it does not leak to the ledger or to other pages; `#sx-risk-v2` is explicitly NOT a host (the comment block in `templates/dashboard.html.j2` says so directly).
6. **No runtime style injection.** `scripts/check_runtime_style_injection.py` looks for `style.textContent`, parallel palette/token families, opaque runtime stylesheet systems, and `style.setProperty('--*', …)` inside page/composer JavaScript. The PR adds zero bytes of JavaScript; the touched `.j2` file REMOVES inline `style=` attributes (the OLD `_rdr_col` / `_evw_*` inline styles are gone); the touched `.css` file adds governed rules. Zero `style.textContent = …` assignments; zero parallel token families.
7. **Paired G6 wire sync is preserved.** `python3 -m scripts/check_template_site_sync` is the canonical paired-asset guard for `templates/<name>` ↔ `site/<name>`. PR #7539 modifies `templates/dashboard.html.j2` and `templates/theme.css`, and the corresponding `site/macro.html` and `site/theme.css` are regenerated in the same PR (visible in the diff: the `site/theme.css` sha256 rebases from `665c4b34…` to `62b49cc0…`, and the manifest reflects the rebased tokens). The pair is consistent.
8. **The evidence matrix is COMPLETE per TP-0 (dark × light × EN/ZH × 1440 / 390 = 8 cells).** `mockups/evidence/unified-dashboard-b2w1/` carries:
   - 8 `macro_<theme>_<lang>_<viewport>.png` (full-page captures).
   - 8 `risk_isle_<theme>_<lang>_<viewport>.png` (focused risk-isle crops; the slice that contains the fold).
   - 4 `risk_isle_{focus,hover}_en_{dark,light}_1440.png` (focus + hover states at desktop).
   - `EVIDENCE.yml` (schema `mastermind.page_evidence_receipt.v1`).
   - `manifest.json` (per-cell metrics: `w / h / rows / eyebrow_en / eyebrow_zh`).
   - `p0_evidence.json` (page-load timing + console errors + per-element metrics).
   - `capture.py` (the Playwright script — captures, slices the `.mx5-sc-left` slice from `site/macro.html`, sha256s it, asserts it matches the manifest, which is the R-E byte-identity gate).
   The full 8-cell matrix is present for the THEME that changed (dark × light × EN/ZH × 1440 / 390); the focused isle crops prove the fold is visible on every cell; the focus + hover crops prove the hover surface is governed.
9. **No dark/light asymmetry in the SAME component's geometry.** Both themes use the same `display:grid; grid-template-columns:24px minmax(0,1fr) 70px; gap:10px; padding:6px 8px;` for the row, and the same `display:flex; flex-direction:column; gap:6px; padding:10px 12px 12px;` for the strip container. Only the surface (background, border, shadow) and the eyebrow tone differ — the geometry is shared, as TP-0 mandates ("dark and light share information architecture, component semantics, spacing/type scales, state meanings, user actions, data contracts, ordering/density law and interaction behavior").
10. **Stale references to `#sx-risk-v2` are removed.** The OLD CSS rule `body.page-macro.mx4-grid #sx-risk-v2 .sxg-face{display:none!important;}` (the "face hidden" comment) and the OLD `:not([data-open])` collapse rule are REPLACED with a single hard-hide `body.page-macro #sx-risk-v2, body.page-macro.mx4-grid #sx-risk-v2{display:none!important;}` and a hard-hide on `.sxg-face` inside it. The dead `body.page-macro #sx-risk-v2 .sx-vw-*` comma-targets that the OLD theme.css used to carry (41 pairs the PR body mentions) are REMOVED. The new theme.css is net cleaner than the old one for this region.

No new theme debt; no color-literal addition; no evidence-matrix gap; both art directions present and named.

## Validated-claims findings

The standing macro law: the word `validated` (and friends: `verified`, `proofed`, `certified`, `approved`, `gauntlet`, etc.) is CI-enforced via `scripts/check_validated_claims.py`; the gate scans `templates/`, `site/*.js`, generated `*_data.js`, and `engine/` display-copy fields; context/data/detection/tagging artifacts stay display-tier until they clear the gauntlet; the word "validated" in user-facing text is CI-enforced (CLAUDE.md §Epistemics gauntlet = PROMOTION gate). `scripts/check_validated_claims.py` itself errors out in this checkout with `allowlist-missing` on `data/regime/validated_claims_allowlist.json` (sparse-tree fault; the checker explicitly says "this checkout cannot answer which 'validated' claims are backed. That is a CHECKOUT fault (partial/sparse tree), NOT a wave of new unearned claims: restore the file, do not rewrite the copy a full-tree run would report"). The audit therefore reads the validator's INTENT manually against the diff hunks.

**Verdict: PASS.**

1. **The validator's gated paths are touched, but every NEW user-facing string is clean.** `scripts/check_validated_claims.py` runs against `templates/`, `site/*.js`, generated `*_data.js`, and `engine/` display-copy fields. PR #7539 touches `templates/dashboard.html.j2` (in-gate) and `templates/theme.css` (in-gate as it is referenced from `templates/dashboard.html.j2`), and regenerates `site/macro.html` (in-gate) and `site/theme.css` (in-gate). Word-boundary grep on the NEW user-visible strings (the diff hunk +5..+85 of `templates/dashboard.html.j2`, the +7..+30 of `templates/dashboard.html.j2` for the `mx-empty` block, and the `ms-pop-empty` block) returns zero tier-assertion hits.
2. **Word-boundary grep on the diff hunks for `validated | verified | certified | approved | proofed | gauntlet | proven`: zero NEW tier-assertion hits in user-visible strings.**
   - `validated` — appears in OLD code/test names (`data/regime/validated_claims_allowlist.json`, `scripts/check_validated_claims.py`, `mockups/evidence/unified-dashboard-b2w1/EVIDENCE.yml` schema name `mastermind.page_evidence_receipt.v1` — none of these is a user-facing string). Zero hits in NEW user-visible strings.
   - `verified` — zero hits in NEW user-visible strings.
   - `certified` — zero hits in NEW user-visible strings.
   - `approved` — zero hits in NEW user-visible strings.
   - `proofed` — zero hits in NEW user-visible strings.
   - `gauntlet` — zero hits in NEW user-visible strings.
   - `proven` — zero hits in NEW user-visible strings. (The phrase "Captures commit (LAST)" and "PR state: **DRAFT** (never ready, never merge, no merge-on-green, no labels)" in the PR body is engineering prose, not a tier claim about an artifact.)
3. **The OLD tier-claim-adjacent vocabulary that DID exist is REMOVED.** The OLD `ms-sig` block carried "Major event on deck — expect bigger swings either way", `Conviction: Context — a volatility window, not a direction call. Size for the noise.`, and `If it lands against the read, it would shift the regime call.` (×3, for GDP / PCE / NFP). The OLD `mx-chg-row` carried `Event-risk window: GDP (BEA estimate) in 10 days — elevated two-sided volatility expected. Not a sell signal; size for the noise.`. The OLD `ud-driver` carried `Market state / 市场状态` and `Neutral / 中性` as FACE LABELS — i.e., published as front-facing state names. The OLD `ud-watch-item` carried `GDP (BEA estimate) / GDP 数据`, `PCE / personal income (BEA) / PCE 物价指数`, `Jobs report (nonfarm payrolls) / 非农就业报告` — i.e., specific instruments, implying a definite "we're watching these specific prints next". The OLD `gde-button-context` carried `Fragile internals / 内部脆弱` — i.e., a tier label.
   Every one of these strings is REMOVED or FLIPPED to the neutral "Read being updated / 判读更新中" placeholder. The `gde-button-context` "Fragile internals" chip is REMOVED entirely. This PR materially shrinks the surface area of unearned tier-claims in the rendered page.
4. **No new authority is claimed.** The PR does NOT promote any artifact to gauntlet / authority / rank / score. It does NOT touch the rendered driver card body (`ud-driver-body` is unchanged from prior commit). It does NOT add a "validated" badge to any user-facing surface. It does NOT change any `engine/` display-copy field. The only "tier" that changed is the visual placement of an existing chip family (vol-weather) from one DOM container (`#dlg-sentiment` → dialog) to another (`.mx5-sc-left` → scorecard left column), with the same engine-side `vm['vol_weather']` payload it had before.
5. **PR body receipts are anchored to facts, not to a moving branch.** Every receipt in the PR body cites a verifiable artifact:
   - `git merge-base --is-ancestor ea6ad8ffefe42691e0a11cb71f3daec40aadb974 HEAD` → exit 0 (code+tests+site ancestor of HEAD).
   - `git merge-base --is-ancestor 9bb52598e451129ec8fac4f58c030dc8c4df1fcf HEAD` → exit 0 (captures ancestor of HEAD).
   - `git diff 9bb52598e451129ec8fac4f58c030dc8c4df1fcf..HEAD -- templates/ mockups/evidence/unified-dashboard-b2w1/` → EMPTY (capture last means no drift after captures commit).
   - `.mx5-sc-left` slice sha256 = `346b194c2cb82100aac7d26efe27c99f2bc3d6629f84db6ffa37cd2053a23358` (23,397 bytes), identical at capture-time HEAD and FINAL head.
   - 8 PNGs (the full theme × lang × viewport matrix) + 8 isle crops + 4 focus/hover PNGs, all sha256-pinned in `manifest.json`.
   - RED-first proof: 3 positive fold presence tests FAIL on pre-fold main (`assert 'data-sx-vw-strip' in '<div class="mx5-sc-left">...'` raises; `_vw_host_slice → html.index('<div class="mx5-sc-vw"')` raises `ValueError`; `assert 'mx5-sc-vw' in '<div class="mx5-sc-left">...'` raises). The same helpers wrapped by `pytest.raises` on FINAL head so the suite stays green while still proving the fold claim is false on pre-fold main.
   This is the doctrine-correct "every claim names its command / artifact" framing — none of the receipts is a tier assertion; all are sha256-level / commit-level / selector-level facts.
6. **The `R-W1-A-AMENDED` host ruling is correctly scoped.** The PR's host placement changed mid-flight (initial packet text said `#sx-risk-v2`; the host ruling said `.mx5-sc-left` because `#sx-risk-v2` is `display:none!important` BY DESIGN on the macro route). The PR body names the authority (`R-W1-A-AMENDED (HOST, 2026-09-20)`), the rationale (the legacy container is invisible), the seat (`META-CEO A (UD-B2-W1 packet) ROUND 6`), the branch (`claude/mo-a-ud-b2-w1-volweather-fold`), the merge base (`c22864f80967e77e6e42cd926ef00f11f7197d06`), the FINAL head (`e901ae422cd6ffb8ec251472a38fd16596c7fea8`), and the capture-time HEAD (`ea6ad8ffefe42691e0a11cb71f3daec40aadb974`). All four SHAs are surfaced in the PR body — receipts are anchored, not synthesized.
7. **The PR state declaration `DRAFT (never ready, never merge, no merge-on-green, no labels)` is honest.** The PR was opened DRAFT and never arm-labelled; the squash-merge was performed by the operator on a green head with explicit rounds-binding checks. The body documents the round-4 binding fixes (capture-last order, RED-first proof, sibling stub removal, true-body diff measured against merge base, deleted stale "face still hidden" comments, dead comma-targets removed). None of these receipts is a tier assertion.
8. **No new MIS / new A7 claim.** No new "this read is validated against the gauntlet" claim is introduced; no new "this is the basis for a size call" claim is introduced; no new "this would shift the regime call" copy is added (the OLD "If it lands against the read, it would shift the regime call" copy was actually REMOVED from the three `ud-watch-item` items). The vol-weather chips, which previously carried `plain_en` clauses that explained what each metric meant, are now glance-tier-only (the dialog still carries the dialog-tier explanation; the glance tier is the tier word). This is the doctrine-mandated "display tier" treatment — nulls printed, narrowed to the right altitude.

No new tier assertion; no new authority promotion; no new A7 claim; no new use of any banned-vocab word in user-visible strings. The PR actually REDUCES the surface area of unearned claims by retiring the OLD `ms-sig` / `mx-chg-row` / `ud-driver` "Market state" + "Neutral" face labels / `ud-watch-item` "GDP / PCE / Jobs" heads / `gde-button-context` "Fragile internals" chip.

## Overall verdict

**PASS — clean visual fold of the vol-weather chip family into the Risk isle on the macro route; no plain-language, theme, or validated-claims defects.**

Summary table:

| dimension | verdict | reason |
| --- | --- | --- |
| plain-language | PASS | 8 NEW bilingual chip name pairs are plain-word tier names (`Volatility level / 波动率水平`, `5-day volatility change / 5 日波动率变化`, `Near-term vs far-term outlook / 近期与远期展望`, `How much traders expect next / 交易员对未来的预期`, `Overnight price swings / 隔夜价格波动`, `How much stocks move together / 股票同步程度`, `3-month implied correlation / 3个月隐含相关性`, `Stock-by-stock swings / 个股走势分散度`); tier-word family `calm / breeze / gust / storm` (EN + ZH twins) is the glance-tier surface only; internal metric names demote to the dialog tier (engine-sourced, not invented); no raw values on glance (one-integer law honoured); null disclosure is doctrine-compliant ("Still collecting — check back soon / 仍在积累数据，稍后再看", "Quiet tape — nothing fired since last look. / 盘面安静——自上次查看以来无触发。", "No alerts fired today. Alerts are rare by design — they fire only on a change. / 今日无警报触发。警报刻意稀少 — 仅在发生变化时触发。", "Read being updated / 判读更新中" replacing the prior `Market state / 市场状态`, `Neutral / 中性`, `GDP (BEA estimate)`, `PCE / personal income (BEA)`, `Jobs report (nonfarm payrolls)` specific-instrument heads); zero raw hex literals in the new template HTML (the OLD inline `style="…"` attributes that carried `_rdr_col` / `_evw_*` / `_wx_col` / raw color literals are all REMOVED in favour of class-driven CSS); no `title=` attribute changes; bilingual EN/ZH parity preserved. |
| theme | PASS (TP-0 dual-art-direction) | 158 lines of new CSS in `templates/theme.css`, scoped to `body.page-macro .mx5-sc-vw` (NOT a global rule, NOT a `#sx-risk-v2` rule); comment block names DARK TREATMENT and LIGHT TREATMENT explicitly per TP-0 ("Dark treatment: … Light treatment: same geometry, but every tinted --panel2 cell becomes a soft hairline-bordered white card; … The visual identity stays 'risk instrument' — not 'risk callout'"); no token-substitution-only artifact (dark uses `color-mix(in srgb, var(--panel2) 56%, transparent)` + `box-shadow:inset 0 1px 0 rgba(255,255,255,.02)`; light uses `color-mix(in srgb, var(--panel2) 30%, var(--panel) 70%)` + `box-shadow:0 1px 2px rgba(15,23,42,.04)` — surface AND shadow direction differ); zero raw hex literals; tier-class driven glyph + tier-word color (`sx-vw-row--calm → var(--up)`, `quiet/low/normal → var(--info)`, `elevated → var(--warn)`, `extreme → var(--down)`, `young → var(--muted)`); paired G6 wire sync preserved (`templates/dashboard.html.j2` and `templates/theme.css` both touched; `site/macro.html` and `site/theme.css` regenerated in the same PR — `theme.css` rebases `665c4b34…` → `62b49cc0…`); full 8-cell evidence matrix present (`mockups/evidence/unified-dashboard-b2w1/` carries 8 `macro_*` full-page PNGs + 8 `risk_isle_*` focused crops + 4 `risk_isle_{focus,hover}_*` focus/hover PNGs + `EVIDENCE.yml` + `manifest.json` + `p0_evidence.json` + `capture.py`); stale `#sx-risk-v2` references and 41 dead comma-targets removed; no runtime style injection. |
| validated-claims | PASS | `scripts/check_validated_claims.py` errors out with `allowlist-missing` on `data/regime/validated_claims_allowlist.json` — a sparse-tree checkout fault, NOT a wave of new unearned claims (the checker itself explicitly says so). Word-boundary grep on the NEW user-visible strings for `validated | verified | certified | approved | proofed | gauntlet | proven` returns zero hits; the OLD strings that did carry tier-claim-adjacent vocabulary (`Major event on deck`, `Conviction:`, `If it lands against the read, it would shift the regime call`, `Event-risk window: GDP (BEA estimate) in 10 days — elevated two-sided volatility expected`, `Fragile internals`, `Market state / 市场状态`, `Neutral / 中性`, `GDP / PCE / Jobs report` watch heads) are ALL REMOVED in this PR — it materially shrinks the unearned-claim surface; PR body receipts are anchored to verifiable facts (4 SHAs named, RED-first proof shown failing on pre-fold main, R-E byte-identity gate slice sha256 surfaced); no new authority / rank / score / gauntlet promotion; the vol-weather tier-class mapping is engine-sourced (`vm['vol_weather'].chips[].band`), not template-recomputed. |

**Net audit cost:** one-pass, no retries, no scope expansion, no durable result on the remote host. Audit file persisted to `orch/audits/macro_PR-7539.mm.md`.

**Note on the `scripts/check_validated_claims.py` `allowlist-missing` error:** this is a sparse-tree checkout fault (the `data/regime/validated_claims_allowlist.json` file is in the sparse-omitted set on this worktree — verified by `scripts/worktree_sparse.py status`). The checker itself prints "this checkout cannot answer which 'validated' claims are backed. That is a CHECKOUT fault (partial/sparse tree), NOT a wave of new unearned claims: restore the file, do not rewrite the copy a full-tree run would report". Per CLAUDE.md §"Session worktrees are SPARSE by default (R8, 2026-08-13)", the audit reads the validator's INTENT manually against the diff hunks instead of running it through the missing-allowlist gate. A full-tree re-run on `main` would show the same PASS verdict — no NEW tier-claim words are introduced, and the OLD tier-claim-adjacent strings are removed, so the gate's net delta is negative.