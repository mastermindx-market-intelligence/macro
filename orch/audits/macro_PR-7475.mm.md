# Audit — mastermindx-market-intelligence/macro PR #7475

Mode: qwen_auditor2-style (plain-language / theme / validated-claims laws). One-pass audit against the merged head. No durable writes outside this report.

## PR metadata

| field | value |
| --- | --- |
| repo | `mastermindx-market-intelligence/macro` |
| PR | [#7475](https://github.com/mastermindx-market-intelligence/macro/pull/7475) |
| title | `[RESEARCH][HOLD] Freeze prospective US Prophet fast-cycle × hidden-fragility test` |
| merged | 2026-09-20T07:11:57Z via squash-merge to `main` (most-recent merged non-audit-record PR in the 24-h window that has not yet been audited; the more-recent merged PRs #7539 / #7527 / #7523 / #7514 / #7511 / #7506 are all already audited — see `orch/audits/macro_PR-{7539,7527,7523,7514,7511,7506}.mm.md`). |
| head | squash of branch `sol/prophet-phase22-fast-cycle-regime-prereg-20260919-sol-001`, head commit `cc03ea8c353a05c81e7823559b617be59fa037ae` (Sol carrier; protected Skillpack 1.0.1). |
| author / merger | `chriswong6031-creator` (operator). |
| base | `origin/main` at `d0a2c00a8f806dbfbd8f39c4de0b5f4fa967cd3b` (PR body §"Exact branch head before PR"; the PR body also records a fresh isolated `4f1cfc00c2f3a5a855ac9f9ed071fd44ef7d4a32` no-write composition → integrated tree `d16fcb8247ec2c98fabee72a638e50d44b9357ca` → local review-only commit `a82639d558dd14f23685449089090e1fc74fabb5`). |
| files | **3 files, +518 / −0** — all NEW. `research/prophet_v4/US_PROPHET_PHASE22_FAST_CYCLE_REGIME_PROSPECTIVE_PREREG_2026-09-19.md` (+238, structured prereg prose), `research/prophet_v4/us_prophet_phase22_fast_cycle_regime_prereg_v1.json` (+162, machine-checkable prereg assertions), `agentos/discoveries/DSC-PROPHET-PHASE21-TWO-DEFECT-MECHANISM-AND-PHASE22-PROSPECTIVE-TEST.md` (+118, structured discovery record with `claim`/`falsifier`/`so_what`/`verified_at`/`verified_by`/`scope`/`confidence: probable` frontmatter). Zero files modified; zero byte deletions. |
| scope | **[RESEARCH][HOLD] — frozen prospective experiment, zero production effect.** PR body §"Outcome" is explicit: "**Draft / HOLD. Records only. No TrialLedger registration, outcome read, detector change, B4 rule, ranking, sizing, trade, or production effect.**" This is a pre-registration lock: it freezes the population, the conditioning event, the question, the verdict floors, and the start-boundary gates so that the same construction can be replayed prospectively later without re-fitting a threshold on the already-used V3 forensic evidence. The 209 V3 development episodes remain visible only as motivation; no live filter is claimed. |
| half-B label | **non-product / research HOLD** (Sol carrier, research lane; not MO-A, not MO-B, not UD, not Prophet product code). The PR is owned by the Sol research authority per the carrier name; the half-B label is N/A because no production code or design surface is touched. The standing taxonomy that places research locks at "display-tier until they clear the gauntlet" applies verbatim (CLAUDE.md §Epistemics). |
| live proof | N/A — explicit HOLD with `Draft` framing in the PR body. The PR body's verification section records `git diff --check` PASS, machine prereg assertions PASS, `python3 scripts/agentos.py validate` → **0 errors / 87 inherited warnings** on the original PR tree and **0 errors** on the fresh current-main no-write composition. |
| hold posture | Frozen start-boundary — the experiment cannot START until ALL of the following are true (PR body §"Start boundary / holds"): (1) LER-C1 private evidence transport accepted, (2) LER-C3 proves one real RTH event through private spool → existing `forward.parquet` → existing QLedger, (3) same-cut C4 context durably bound to the immutable C2 event without changing event identity, (4) this exact study registered in the existing TrialLedger before any target outcome read, (5) a prospective experiment epoch recorded. **The TrialLedger is intentionally untouched** because open Tactical R1-B PR #7274 currently owns `data/trial_ledger.jsonl`. The separate pre-outcome nominal-rates study #7418 is explicitly NOT modified. |

## Diff content (exact, scoped to this PR)

**DSC discovery record (NEW, +118 lines)** — `agentos/discoveries/DSC-PROPHET-PHASE21-TWO-DEFECT-MECHANISM-AND-PHASE22-PROSPECTIVE-TEST.md`. Frontmatter: `key: PROPHET-PHASE21-TWO-DEFECT-MECHANISM-AND-PHASE22-PROSPECTIVE-TEST`, `claim` (two distinct mechanisms hypothesis: remaining opportunity at entry + hidden fragility during the hold), `falsifier` (re-read Phase-21 synthesis at SHA-256 `37b5ac78bd93c6d8ebc9b7effb260eb00c0fb77ef8c3259ecd29c231bd124ed6`; falsified if Phase-21 does not preserve the two mechanisms separately, OR C4 d2.turn is not a same-cut stratification-only observation on the primary C2 population), `so_what` (do not fit another threshold on the 209 V3 development episodes; preserve the frozen Phase-22 prospective C2 × C4 test; retain all future false starts and losses; keep rates/breadth as prespecified context interactions rather than promoting them into a Prophet gate), `kind: architecture`, `verified_at: 2026-09-19`, `verified_by` (three receipts: Phase-21 SHA-256; `engine/entry_radar/challengers.py` C2/C4 source at `macro f95b2cc5807fd850c2378b4ad6e938d0e735d2a0`; Phase-22 prereg carrier `sol/prophet-phase22-fast-cycle-regime-prereg-20260919`), `scope` (`macro`, `WS:PROPHET-US-V4-RECOVERY`, `WS:LIVE-ENTRY-RADAR`, `WS:EVAL-OS-MEASUREMENT-LAW`, `research/prophet_v4/`), `confidence: probable`. Body restates the mechanism distinction and explicitly labels Phase-21 development evidence as "outcome-contaminated … does not validate a live filter" — the discovery is a hypothesis, not a tier-2 acceptance.

**Prospective prereg (NEW, +238 lines)** — `research/prophet_v4/US_PROPHET_PHASE22_FAST_CYCLE_REGIME_PROSPECTIVE_PREREG_2026-09-19.md`. Sections: Population (every future LIVE_FORWARD primary `C2_1D_TURN@1` event after the eventual prospective epoch; all false starts, failures, losses and missing context retained), Fast-cycle conditioner (existing stratification-only `C4_MTF_TURN@1.d2.turn` at the exact C2 decision cut; C4 remains structurally unable to fire and gets no directional claim), Q1 (D2_TURN_NOW vs source-qualified D2_NOT_TURNING_NOW; existing W5 H10 `excess_net`; mean within decision date then decision-week block bootstrap, 5,000 resamples, seed 20260919), Q2 prespecified interaction (hidden fragility = prior-session 10y TIPS real yield rising over five completed sessions AND RSP underperforming SPY over the prior five; relief/broadening = real yield non-rising AND RSP not underperforming SPY; same-session closes forbidden as decision inputs; mixed/missing stay diagnostic), Verdict floors (≥30 episodes per arm, ≥12 distinct decision months per arm, effective-N ≥8 per arm — all reused from W5). The "Why this is not another signal" section makes the existing-owner composition explicit: Radar C2 is the existing 1D-turn event; Radar C4 is same-cut 2D/3D context; W5/QLedger is the existing outcome owner; B3 is maturity owner; future B4 is Entry Availability owner. **No new detector, forward store, event ledger, qledger, scheduler, regime scorecard, or Prophet decision plane is created.** The "Start boundary / holds" section enumerates the five required preconditions verbatim. The "Verification" section records the verification receipts.

**Machine prereg JSON (NEW, +162 lines)** — `research/prophet_v4/us_prophet_phase22_fast_cycle_regime_prereg_v1.json`. JSON-schema-enforced prereg assertions, machine-checkable (PR body §"Fresh isolated verification: machine prereg assertions: PASS" against both the original PR tree and the fresh current-main no-write composition). Zero tier-asserting strings in the file body (verified by grep).

## Plain-language findings

The macro repo has no `scripts/check_plain_language.mjs` (terminal-side only; verified `ls scripts/check_plain_language*` → no matches). Plain-language discipline on macro is read directly against the standing design-doctrine rules — but the doctrine applies to USER-FACING strings, and PR #7475 carries zero user-facing bytes. There is no `templates/`, no `site/`, no Jinja copy, no rendered HTML, no HTML attribute surfaced to a reader. All three new files live under `research/prophet_v4/` and `agentos/discoveries/` — internal knowledge-plane artifacts with a dedicated schema (`agentos/schema/discovery.md`).

**Verdict: PASS (N/A — no user-facing surface in this PR).**

1. **No templates/ touched.** `grep -E "^\+\+\+ b/templates/" <diff>` returns zero matches — the 3-file diff contains no `templates/_*.html.j2`, no `templates/_*.css.j2`, no `templates/*.js`. The plain-language gate can't fire on a PR that ships no Jinja.
2. **No site/ touched.** `grep -E "^\+\+\+ b/site/" <diff>` returns zero matches — the paired-site G6 wire-sync cycle (which would carry the same user-facing copy) does not apply. The regenerated `site/china.html` from PR #7463 (separate PR, already audited-equivalent) is not touched here.
3. **No user-facing HTML attribute added.** The DSC frontmatter (`verified_at`, `verified_by`) and the JSON prereg's `assertions[]` block are schema-level metadata read by the `agentos validate` CLI and the prereg checker — neither is rendered to a reader.
4. **PR body language is precise research prose, not user copy.** Words used are explicit prereg / research / methodology nouns ("outcome-blind", "outcome-contaminated", "development-visible", "frozen", "stratification-only", "prespecified", "primary C2 population", "same-cut", "verdict floors", "block bootstrap, 5,000 resamples, seed 20260919", "TrialLedger", "QLedger", "B3 maturity owner", "B4 Entry Availability owner") and explicit non-claims ("This experiment cannot start until …", "The TrialLedger is intentionally untouched here", "Do not infer: This PR does not claim 2D timing is validated, rates/breadth are a live gate, the clean prospective path exists yet, or the original 50.8% headline is repaired", "No new detector, forward store, event ledger, qledger, scheduler, regime scorecard or Prophet decision plane is created").
5. **DSC frontmatter is schema-correct, not tier-asserting.** `kind: architecture`, `confidence: probable` (not "validated" / "proven" / "gauntleted"), `verified_at` is a timestamp on the discovery document, `verified_by` lists three receipt shas (Phase-21 synthesis SHA-256; `engine/entry_radar/challengers.py` C2/C4 source SHA; Phase-22 prereg carrier branch name). These are the canonical `agentos/schema/discovery.md` schema fields, NOT tier-2 acceptance claims. The DSC body restates: "This does not validate a live filter."
6. **ZH parity is N/A.** The PR carries no Chinese strings — the DSC and prereg artifacts are English-only by convention; this matches every prior research HOLD in the Prophet V4 workstream (e.g., `US_PROPHET_PHASE21_FAST_SLOW_REGIME_SYNTHESIS.md`).
7. **The "outcome-contaminated" framing is honest-null grammar.** DSC body labels Phase-21 development evidence as "outcome-contaminated … does not validate a live filter" and the PR body repeats "Do not infer: … the original 50.8% headline is repaired." This is exactly the standing CLAUDE.md §Epistemics honest-null grammar — the discovery names what would have to be true for the hypothesis to clear the gauntlet without claiming it has cleared.
8. **The "Why this is not another signal" section is doctrine-correct.** PR body names the existing owners of every primitive the prereg would consume (Radar C2, Radar C4, W5/QLedger, B3 maturity, future B4 Entry Availability) and explicitly denies introducing any new plane. The "Do not infer" list names the four specific claims this PR does NOT make. This is the seat-bound evidence discipline the standing rules require for any locked prospective construction.
9. **The 5,000-resample block-bootstrap seed is recorded verbatim.** PR body §Q1: "5,000 resamples, seed 20260919" — the seed is part of the prereg so a future reproduction cannot drift on initialization. This is the "anchored to a fact, not to a branch" framing that the prior F00C records-pass audits (e.g., `macro_PR-7514.mm.md` §Validated-claims #5) require for quantities that move.
10. **No new banned-vocab in user-visible strings: 0** (zero user-visible strings exist in the diff).

## Theme findings

TP-0 art-direction law in force (operator 2026-08-27): dark and light are TWO art directions, not one skin. The law applies to product CSS, design-system tokens, material surface, runtime style injection, and the canonical evidence matrix. PR #7475 carries NONE of these surfaces.

**Verdict: PASS (N/A — no CSS, no JS, no HTML, no design-system, no theme asset touched).**

1. **Zero CSS / JS / HTML asset files in the diff.** `grep -E "\.(css|scss|less|js|j2|html|jinja|tmpl)"` on the three touched file paths returns nothing — all three files are `.md` and `.json` inside `research/prophet_v4/` and `agentos/discoveries/`. The `scripts/check_design_system.py --mode enforce-added --diff-file <(git diff <base>..<head> -U0 -- templates/ tests/ site/ mockups/)` filter naturally excludes `research/` and `agentos/`, and there is nothing to flag in `templates/` because `templates/` is untouched.
2. **No new color literals introduced.** The repo-wide rule is "no raw hex in templates". The PR touches zero template files; the DSC and prereg are not styled surfaces.
3. **No runtime style injection.** `scripts/check_runtime_style_injection.py` looks for `style.textContent`, parallel palette/token families, opaque runtime stylesheet systems, and `style.setProperty('--*', …)` inside page/composer JavaScript. The PR adds zero bytes of JavaScript; the three touched files contain no `style` references at all.
4. **No evidence matrix required.** The standing "material UI packet must carry dark × light × EN/ZH × desktop/mobile evidence" rule is a USER-FACING packet rule; this PR's packet is a frozen research HOLD (population + conditioner + question + verdict floors + start-boundary gates + JSON prereg assertions), which is a fundamentally different evidence class (prereg integrity, not visual presentation).
5. **Paired G6 wire sync is N/A.** `python3 -m scripts.check_template_site_sync` is the canonical paired-asset guard for `templates/<name>` ↔ `site/<name>`; the PR does not edit any template file, so the sync is trivially preserved.
6. **No font / animation / `<style>` block change.** The PR does not touch the theme.css file, the navigation-refresh.css file, or any inline-style block. The PR body's "do not change event identity" requirement explicitly preserves C2 event identity, which is the engine-side analog of the CSS rule "do not change artifact identity".

No new theme debt; no color-literal addition; no evidence-matrix gate applies.

## Validated-claims findings

The standing macro law: the word `validated` (and friends: `verified`, `proofed`, `certified`, `approved`, `gauntlet`, etc.) is CI-enforced via `scripts/check_validated_claims.py`; context/data/detection/tagging artifacts stay display-tier until they clear the gauntlet; the word "validated" in user-facing text is CI-enforced (CLAUDE.md §Epistemics gauntlet = PROMOTION gate).

**Verdict: PASS.**

1. **No new template or site file — the policy check has nothing to flag.** `scripts/check_validated_claims.py` runs against `templates/` and `site/` (and a small list of author-facing markdown files); this PR touches zero template files and zero site files, so there is no new front-facing string to gate.
2. **Word-boundary grep on the three new files for `validated | verified | certified | approved | proofed | gauntlet | proven`: zero TIER-ASSERTING hits.**
   - `validated` — does not appear in any of the three touched files.
   - `verified` — appears only as `verified_at: 2026-09-19` and `verified_by:` schema metadata in the DSC frontmatter (the `agentos/discoveries/` schema's required timestamp + receipt fields). These are schema fields, NOT tier assertions. The DSC body uses "falsified if …", "do not fit another threshold …", "does not validate a live filter" — the latter is a NEGATIVE claim naming what is NOT true, not a tier-positive claim. No tier assertion.
   - `certified` — does not appear.
   - `approved` — appears twice in the prereg prose as the qualifying clause on a future event ("owner-approved secure transport/read model", "a future owner-approved first-available/vintage source could supersede this null"). Neither instance is a tier assertion; both name a future receipt condition, not a current-state claim. The phrasing is the standing "owner-approved" gate pattern, not a tier-1/2/3 promotion.
   - `proofed` — does not appear.
   - `gauntlet` — does not appear in any of the three touched files.
   - `proven` — appears once in the prereg prose as a future-state requirement: "LER-C3 has proven one real RTH event through private spool → existing `forward.parquet` → existing QLedger." This is a START-BOUNDARY REQUIREMENT (the experiment cannot start until LER-C3 has done this), not a claim that it has happened. The present-tense "has proven" is the standard prereg "the gate fires when this condition is satisfied" idiom; the receipt itself is NOT produced by this PR.
   - `pass` / `passed` — appears in the PR body §"Verification" as `git diff --check`: PASS and `machine prereg assertions: PASS` and `scripts/agentos.py validate` → **0 errors / 87 inherited warnings**. These are CI-receipt prose lines, not tier assertions.
3. **The DSC is hypothesis-tier, not authority-tier.** DSC frontmatter carries `confidence: probable` (not "validated" / "verified" / "gauntleted" / "promoted"). The `so_what` clause names the discipline the discovery requires: "Do not fit another threshold on the 209 V3 development episodes … keep rates/breadth as prespecified context interactions rather than promoting them into a Prophet gate." This is the seat explicitly refusing to promote the discovery into authority — the exact opposite of a tier-2 acceptance claim.
4. **No NEW promotion occurred.** The PR is a frozen prospective prereg + DSC; it does NOT promote any artifact to gauntlet/authority/rank/score. The Radar C2, Radar C4, W5/QLedger, B3 maturity, and future B4 Entry Availability owners are all UNCHANGED — they remain display-tier exactly as before. The PR body's "Why this is not another signal" section names the existing-owner composition explicitly. The "Start boundary / holds" section names the five required preconditions verbatim, and the body states "The TrialLedger is intentionally untouched here" — the receipt mechanism for a future promotion is explicitly NOT minted.
5. **The "outcome-contaminated" framing names what would have to change to clear the gauntlet.** DSC body: "Phase-21 development evidence is explicitly contaminated by known outcomes and is retained only as hypothesis motivation." PR body: "The prospectively frozen construction reuses: Radar C2 as the existing 1D-turn event; Radar C4 as same-cut 2D/3D context; W5/QLedger as the existing outcome owner; B3 as maturity owner; future B4 as Entry Availability owner." This is the seat explicitly demarcating what is hypothesis (Phase-21) and what is the prospective construction (Phase-22), and refusing to bridge them without the prereg. The bridge is gated on LER-C1/C3/C4 — none of which are produced by this PR.
6. **The PR body's "Do not infer" list is the doctrine-correct non-claim block.** Four explicit non-claims: (a) "2D timing is validated", (b) "rates/breadth are a live gate", (c) "the clean prospective path exists yet", (d) "the original 50.8% headline is repaired". Item (a) is a NEGATIVE tier claim ("not validated") and is the seat explicitly naming what would otherwise be inferred from the file's existence. Item (b) is the seat refusing to promote a context interaction into a Prophet gate. Item (c) is the honest-null grammar for the still-needed LER-C1/C3/C4 receipts. Item (d) is the seat binding itself to the prior failed headline. This is the opposite of a "validated" / "approved" / "gauntleted" framing — it is the seat telling the reviewer exactly which inferences are NOT licensed by the frozen construction.
7. **The prereg `assertions[]` block is machine-checkable, not tier-asserting.** The JSON schema enumerates the structural checks the prereg will be measured against (population, conditioner, Q1/Q2 construction, verdict floors, start-boundary gates). No assertion contains "validated" / "approved" / "proven" — every assertion is a structural inequality or set-membership check (`bootstrap_resamples == 5000`, `seed == 20260919`, `episodes_per_arm >= 30`, etc.). These are the prereg integrity checks the gauntlet would later evaluate against, not the gauntlet's verdict.
8. **No A7 claim.** A7 (the LLM never originates signals/scores) is preserved: the DSC is a hypothesis about Phase-21 mechanisms, not a signal/score minted by an LLM. The prereg is a frozen construction that will be replayed against future LIVE_FORWARD primary events, with the outcome owned by W5/QLedger (deterministic) and the verdict owned by the prespecified block-bootstrap. The LLM role is bounded to the discovery's framing + the prereg's prose, neither of which originates a signal/score.
9. **The Sol carrier name is the audit trail.** PR branch `sol/prophet-phase22-fast-cycle-regime-prereg-20260919-sol-001` is the Sol research carrier. Sol is the AI CEO per the standing `config/authority_map.yml` and CLAUDE.md §"Authority model — restated, never duplicated". A research lock on the Sol carrier is a Sol-owned research lock — the audit trail for the prereg authorship is the branch name itself, and the protected Skillpack 1.0.1 bootstrap protects the carrier's process from drift.
10. **Receipt metadata is preserved, not rewritten.** The `agentos.py validate` result (0 errors / 87 inherited warnings) is preserved verbatim from PR body §"Verification" — the 87 inherited warnings are NOT waived and NOT declared green; they are inherited from the canonical macro knowledge plane and are reported as-is. The machine prereg assertions PASS result is recorded in the PR body and re-verified on a fresh current-main no-write composition (PR body §"Fresh current-main no-write composition against `4f1cfc00c2f3a5a855ac9f9ed071fd44ef7d4a32`"). The receipt trail is intact.

No new MISS; no promotion; no A7 claim; no tier raise. The PR is a records-only research HOLD that freezes a prospective construction without minting any authority on the current main.

## Overall verdict

| gate | result |
| --- | --- |
| plain-language (read directly; macro has no `check_plain_language.mjs`) | **PASS — N/A** (zero user-facing bytes in the diff; no templates/, no site/, no Jinja copy, no HTML, no CSS, no JS; PR body uses precise research / methodology / prereg contract nouns with explicit non-claims; the four-item "Do not infer" list and the "outcome-contaminated" framing are doctrine-correct honest-null grammar; the DSC `confidence: probable` is the schema-correct non-tier rating) |
| theme (`check_design_system.py --mode enforce-added` + CSS audit + evidence matrix) | **PASS — N/A** (zero CSS/JS/HTML asset files in the diff; no color-literal addition; no runtime style injection; no paired G6 sync needed; no evidence matrix required for a frozen research HOLD on a non-user-facing artifact) |
| validated-claims (`check_validated_claims.py`) | **PASS** (zero front-facing string to gate — the PR does not edit templates/ or site/; the `verified_at` / `verified_by` DSC fields are agentos schema fields, not tier assertions; the `owner-approved` and `LER-C3 has proven` phrasings are future-state gate requirements, not current-state tier claims; zero NEW word-boundary `validated` / `verified` / `certified` / `approved` / `proofed` / `gauntleted` / `proven` hits in the diff hunks as tier-positive claims; PR body's four-item "Do not infer" list is the seat explicitly refusing to license any tier-2 inference from this PR's existence; no promotion, no A7 claim, no tier raise) |
| research HOLD posture (Sol carrier + frozen construction + start-boundary gates) | **PASS** (the PR is explicitly labeled `[RESEARCH][HOLD]` in the title and "Draft / HOLD" in the body; the five start-boundary gates are named verbatim; the TrialLedger is intentionally untouched because PR #7274 currently owns it; the separate pre-outcome nominal-rates study #7418 is explicitly NOT modified; no production effect, no detector change, no B4 rule, no ranking, no sizing, no trade) |
| prereg integrity (machine + author verification) | **PASS** (PR body §"Verification": `git diff --check` PASS; machine prereg assertions PASS; `scripts/agentos.py validate` → 0 errors / 87 inherited warnings on the original PR tree and 0 errors on the fresh current-main no-write composition against `4f1cfc00c2f3a5a855ac9f9ed071fd44ef7d4a32`; integrated tree `d16fcb8247ec2c98fabee72a638e50d44b9357ca`; review-only commit `a82639d558dd14f23685449089090e1fc74fabb5`) |
| handoff / workstream continuity | **PASS** (DSC scope names `WS:PROPHET-US-V4-RECOVERY`, `WS:LIVE-ENTRY-RADAR`, `WS:EVAL-OS-MEASUREMENT-LAW`; PR body references the phase-21 synthesis SHA-256 `37b5ac78bd93c6d8ebc9b7effb260eb00c0fb77ef8c3259ecd29c231bd124ed6` as the falsifier receipt and `engine/entry_radar/challengers.py` C2/C4 source at `macro f95b2cc5807fd850c2378b4ad6e938d0e735d2a0` as the verified_by receipt; PR body explicitly preserves PR #7418 (separate study) and PR #7274 (TrialLedger owner); Sol carrier `sol/prophet-phase22-fast-cycle-regime-prereg-20260919-sol-001` is the audit trail for the prereg authorship) |

**Overall: PASS.** This is a `[RESEARCH][HOLD]` records-only freeze of a prospective US Prophet fast-cycle × hidden-fragility test on the Sol carrier. The PR adds three files (DSC discovery record + prose prereg + machine-checkable JSON prereg) and modifies zero bytes anywhere else. The DSC `claim` enumerates two distinct mechanisms (remaining opportunity at entry + hidden fragility during the hold) that the prospective construction is built to test; the DSC `falsifier` names the exact re-read condition that would falsify the discovery (Phase-21 synthesis SHA-256 `37b5ac78bd93c6d8ebc9b7effb260eb00c0fb77ef8c3259ecd29c231bd124ed6` AND current `engine/entry_radar/challengers.py` C2/C4 source). The DSC `so_what` clause explicitly forbids fitting another threshold on the 209 V3 development episodes — the very class of error the standing CLAUDE.md §Epistemics rules guard against. The prereg freezes population, conditioner, Q1/Q2 construction, verdict floors (≥30 episodes per arm, ≥12 distinct decision months per arm, effective-N ≥8 per arm, all reused from W5), 5,000-resample block-bootstrap with seed 20260919, and the five start-boundary gates (LER-C1/C3/C4 + TrialLedger registration + prospective epoch record). The PR body §"Why this is not another signal" names the existing-owner composition verbatim (Radar C2 / Radar C4 / W5/QLedger / B3 / future B4) and the four-item "Do not infer" list explicitly denies the four most likely misreadings. Zero new debt across plain-language, theme, validated-claims, research HOLD posture, prereg integrity, or handoff continuity. The macro repo's `scripts/check_plain_language.mjs` referenced in the task prompt does not exist in this repo — plain-language discipline on macro is read directly against the standing design-doctrine rules, which apply to USER-FACING strings, and this PR ships zero user-facing bytes.

**Net audit cost:** one-pass, no retries, no scope expansion, no durable result on the remote host. Audit file persisted to `orch/audits/macro_PR-7475.mm.md`.
