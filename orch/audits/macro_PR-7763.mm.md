# PR audit — mastermindx-market-intelligence/macro#7763

**Auditor:** meta-ceo-b-2026-09-08 (one-pass remote useful-idle, no retries, no scope expansion)
**Audit timestamp:** 2026-09-23
**Repo / PR:** mastermindx-market-intelligence/macro #7763
**PR title:** [MO-A3] A-F03-W2-5b: Payoff lab consumer — "What a structure pays" fold on the index cards (options.html) + R2→emit wiring on the render path
**Merged at:** 2026-09-23T08:46:03Z (most recent merged PR in macro within the 24 h audit window — see §1 scope note)
**Head sha (final ratified, body-disclosed):** `e78d3b2df0fd7da2f5fdddaf848558548a6bea41` (Meta-CEO A seat ratification #3; supersedes `af9cabcd3` and `604dd571`)
**Merge commit:** `e78d3b2df0fd7da2f5fdddaf848558548a6bea41` (per `gh pr view --json mergeCommit`)
**Author:** chriswong6031-creator (Meta-CEO A seat)
**Repo root verified:** `/Users/chriswong/lanes/repos/macro` on the operator's local checkout, `origin/main` reachable; merge confirmed in `origin/main` log.

> **Scope note (§1):** the chronologically most-recent merges inside the 24 h window are #7768 (admin publisher records), #7771 (UK policy desk), #7770 (skew ledger backfill), #7764 (markets.css cachebust), #7763 (Payoff lab consumer), #7761 (markets mobile cycle-stage), #7760 (ci gate), #7759 (payoff lab producer), #7758 (sector-intel publish), #7756 (skew parity audit), #7755 (retire Intelligence Hub entry), #7752 (skew lane docs), #7751 (B4 prereg), #7748/#7746/#7745/#7744/#7743/#7741/#7740/#7737/#7736/#7735/#7733/#7732/#7730 (orch(audit) ledger entries + skew cutover + market-memory fix + skew accrual + docs). Among the **un-audited** substantive PRs carrying a real user-facing template + plain-language surface, **#7763** is the only half-B payload that (a) touches the canonical Options template (`templates/options.html.j2`, +75/−0) and the index-card builder (`scripts/build_options_command.py`, +483/−5); (b) carries ≈25 new bilingual EN/ZH strings on the "What a structure pays" fold (the four structure names + what-it-pays one-liners + cost/pays/risk row copy + verdict strings + null disclosure + footer); (c) carries a real theme-relevant change — the new `.oew-lab-*` fold CSS with a documented TP-0 light-theme override (`html[data-theme="light"] .oew-lab-wall{width:2px}` and `outline:none` on the breakeven dot); (d) ships a 12-cell evidence matrix (8 rest + 4 fold-open) across 1440/390 × EN/ZH × dark/light — the standard TP-0 receipt for a user-facing fold. The seat selects #7763.

---

## 1. PR metadata

| field | value |
|---|---|
| repo | mastermindx-market-intelligence/macro |
| number | 7763 |
| title | [MO-A3] A-F03-W2-5b: Payoff lab consumer — "What a structure pays" fold on the index cards (options.html) + R2→emit wiring on the render path |
| merged_at | 2026-09-23T08:46:03Z |
| head (body-disclosed) | `e78d3b2df0fd7da2f5fdddaf848558548a6bea41` (ratification #3; supersedes `af9cabcd3` and `604dd571`); round 5/N with the W2-5a DEC record LEAVING the diff via STEP 0 |
| merge_commit | `e78d3b2df0fd7da2f5fdddaf848558548a6bea41` |
| base | `main` (Meta-CEO A seat merged main a second time to mint a `pull_request` run after #7771 and #7763 both appended a `gate: code` job to `.github/ci/legacy-jobs.yml`; YAML parses, `run_ci_pack.py --validate-only` → 224 legacy jobs clean) |
| branch | `claude/mo-a-3-a-f03-w2-5b-payoff-lab-consumer` |
| changed files | 23 (`gh pr view 7763 --json files --jq '.files|length'` = 23; body declares the W2-5a DEC file LEAVES the diff at `git diff --stat origin/main...HEAD -- agentos/decisions/DEC-F03-W2-5-PAYOFF-LAB-CHARTERED-AFTER-C0-FREEZE.md` = empty) |
| additions / deletions | 2,560 / 6 (per `gh pr view --json additions,deletions`); of which the user-facing surface is `templates/options.html.j2` (+75) and `scripts/build_options_command.py` (+483/−5); the rest is the new CI gating job (.github/ci/legacy-jobs.yml +21), the engine-render R2 restore step (.github/workflows/engine-render.yml +14/−1), the config/dag.yml slugs (+4), the DEC record (+114), the agentos handoff (+142), the design-system research note (+121), and 12 evidence PNGs + 2 manifest.json + 1 EVIDENCE.yml + the test file (+1,040) |
| files of interest | `templates/options.html.j2` (+75 — `.oew-lab*` CSS fold + the `<details class="oew-aib-detail oew-lab">` markup with track / legend / verdict / 4-row table / footer); `scripts/build_options_command.py` (+483/−5 — `load_payoff_lab` + `build_payoff_lab` + the four canonical structures + the `_PAYOFF_LAB_*` constants + the rr25 strike-copy helper + the wall-bracket verdict + the `payoff_lab` context threading); `tests/test_options_payoff_lab_consumer.py` (+1,040 — 21 hermetic tests, all green on the head); `.github/workflows/engine-render.yml` (+14/−1 — R2 restore step + `brun options_payoff_lab … --emit` line in the `cl_gex()` band, max step run length = 19,633 chars vs the 20,500 guard); `.github/ci/legacy-jobs.yml` (+21 — new `options-payoff-lab-consumer` job, `if: ${{ false }}`, `gate: code`, ubuntu-latest, runs `pytest tests/test_options_payoff_lab_consumer.py`); `config/dag.yml` (+4 — `build_options_payoff_lab` declared in the `cl_gex` band + added to engine-render's ORDER for `TestBrunOrderVisibility`); `agentos/decisions/DEC-F03-W2-5B-PAYOFF-LAB-CONSUMER-IS-A-CARD-FOLD.md` (+114); `agentos/handoffs/WS-MARKET-OS-2026-09-23-f03-w2-5b-payoff-lab-consumer-r5.md` (+142 — round-5 handoff, agentos-validate 0 errors) |
| labels | `merge-on-green` (cleared at squash-merge); DRAFT until Meta-CEO A seat ratified |
| scope collision | none — body declares zero collision with `engine/*`, the Options workspace outer shell, the W2-5a producer (#7759), the daily Options scope template, the shared `theme.js`, or any other page's tooltip plane |

**Nature of change (Options-page half-B payload — payoff-lab consumer fold on the index cards):** the PR has three intertwined deliverables, all of which the body declares together:

1. **The "What a structure pays" fold.** A single `<details>`/`<summary>` inside each of the four index cards on `templates/options.html.j2` (SPY/QQQ/IWM/SPX), guarded by `{% if lab %}` so SPX (no chain) and DIA (not a card) render no fold. The fold carries (a) a wall-bracket track with walls/flip/spot/breakeven dots and a four-clause verdict, (b) a four-row plain-named table (Straddle, Upside for downside, Downside hedge, Upside play) with cost/pays/risk per-share, and (c) a one-line footer naming the expiry + the data source (ThetaData). The fold is the **consumer** half of packet A-F03-W2-5b; the producer is the store-host accrual/emit at PR #7759 (W2-5a, MERGED at `148d1bfec8`).
2. **R2→emit wiring on the render path.** `.github/workflows/engine-render.yml` gains (a) a new `restore options_payoff_lab from R2 (W2-5a store-host producer)` step after the existing `restore options_skew ledger from R2`, modeled on the same R2 env shape (non-fatal `::warning title=options-payoff-lab-hydrate::` on miss), and (b) a new `brun options_payoff_lab "index payoff lab (build_options_payoff_lab)" scripts.build_options_payoff_lab --emit` line in the `cl_gex()` band. `config/dag.yml` declares the new slug and the ORDER string for `TestBrunOrderVisibility`.
3. **The half-B evidence ledger.** A 12-cell matrix (8 rest + 4 fold-open × 1440/390 × EN/ZH × dark/light) under `mockups/evidence/a-f03-w2-5b-payoff-lab/`, with both manifests carrying `target.resolved_sha_or_none: 26685973134bda141325acbca159202f6564b7c4` (the parent merge commit, per the Body's Parent-SHA contract — the commit that ADDS a manifest cannot contain its own sha).

**Body signals — already-run gates (PR-quoted, NOT re-run in this audit per one-pass constraint):**
- `python -m pytest tests/test_options_payoff_lab_consumer.py -q` → **21 passed in 1.80s** (15 round-3 + 6 round-4 new tests, all green).
- `python -m pytest tests/test_build_options_command.py tests/test_render_options_workspace_scope.py -q -k "options"` → **152 passed in 4.43s** (pre-existing Options tests unaffected).
- `python3 scripts/check_design_system.py --mode enforce-added --diff-file <(git diff --unified=0 origin/main HEAD -- templates site)` → **R0 enforce-added: 0 blocking finding(s)** (estate pre-existing, non-blocking: 25,316).
- `python3 scripts/check_runtime_style_injection.py` → `runtime style injection guard OK (197 .js files scanned, 44 injecting, 89 total hits — all within frozen allowances)`.
- `git diff --unified=0 origin/main HEAD -- templates site | python3 scripts/check_ui_visual_evidence.py --diff-file -` → **exit 0**.
- `python3 scripts/check_contract_delta.py --base origin/main` → **0 introduced, 1 inherited** (the inherited line is `tests/test_render_dead_ref_targets.py` already unwired on base — pre-existing).
- `python3 scripts/run_ci_pack.py --workflow .github/ci/legacy-jobs.yml --pack-index 0 --pack-count 12 --validate-only` → **224 legacy jobs validated**.
- `python3 scripts/agentos.py validate` → **0 errors, 75 warnings** (1207 records: 69 workstreams, 341 decisions, 302 discoveries, 495 handoffs).
- `git diff --check`: PASS.
- Real-browser matrix: **12/12 PASS** across 1440/390 × EN/ZH × dark/light for the fold; 8 rest cells + 4 fold-open cells, each manifest re-captured against the round-5 CSS; HTTP 200, zero page errors, zero failed requests, zero horizontal overflow in all 12 cells; all 12 cells: the fold opens/closes on tap (mobile) and click (desktop); the track renders with walls/flip/spot/breakeven dots in correct geometry; the four-row table reads with `class="v mono"` on every value span; the verdict text matches the four-clause truth table.
- `grep -nE 'height: *4px' templates/options.html.j2 | grep -c oew-lab-track` = **1** (the round-5 fix: track height 4px, was 14px).
- `grep -nE 'max_gain.*or.*max_loss|max_loss.*or.*max_gain' scripts/build_options_command.py` = line **1029**: `built = payoff.get("max_gain") is not None or payoff.get("max_loss") is not None` (the round-5 fix: BUILT fires on max_loss alone, not max_gain alone).
- `git diff --stat origin/main...HEAD -- agentos/decisions/DEC-F03-W2-5-PAYOFF-LAB-CHARTERED-AFTER-C0-FREEZE.md` = **empty** (the W2-5a DEC LEAVES this PR's diff via STEP 0).

**Scope boundary (body-disclosed):** the PR is bounded to the Options workspace template + the Options command builder + the engine-render `cl_gex` band + the config DAG + the new CI gating job + the producer's evidence ledger. Does **not** touch `engine/*`, the producer (`scripts/build_options_payoff_lab` lives on the store host, not in this PR — body declares the producer unchanged; the consumer imports its `site/options_payoff_lab/latest.json` artifact), the shared `theme.js`, any other page's tooltip plane, or any `data/`/`site/` plain-copy asset.

**Cross-reference (body-disclosed, not re-run):** #7759 (W2-5a producer, MERGED on main at `148d1bfec8`) is the source of the `site/options_payoff_lab/latest.json` artifact the consumer reads; the body explicitly notes the consumer waits on the producer's live artifact and the fold renders `ledger_only` until the first accrual lands. The DEC citation chain `DEC:F03-W2-5B-…-CONSUMER-IS-A-CARD-FOLD` → `DEC:F03-W2-5-PAYOFF-LAB-CHARTERED-AFTER-C0-FREEZE` (on main, NOT in this PR's diff) is honored.

---

## 2. Plain-language findings

**Source for plain-language law:** the macro repo does not ship a standalone `check_plain_language.mjs` (that check is terminal-repo-only); the macro equivalent is the **visible-string audit** mandated by `CLAUDE.md` §"Design (user-first law)" — `docs/DESIGN_DOCTRINE.md` + the `frontend-design` skill — read against the Tier-1 obligations (state + plain-word stance under hard word budgets, no internal state names, no untranslated stats, no raw slugs, bilingual EN/ZH parity on every visible string, plain-word null disclosure) and the Tier-2 obligations (Tier-2 receipts for nulls, no falsifier vocabulary front-facing, plain-word cadence call-outs).

**Scope check — does this PR carry plain-language surface?** Yes — the PR adds ≈25 new bilingual EN/ZH strings on the consumer fold:

- Four structure names (EN/ZH): `Straddle / 跨式`, `Upside for downside / 以下行换上行`, `Downside hedge / 下行保护`, `Upside play / 上行押注`.
- Four "what-it-pays" one-liners (EN/ZH): `pays if the index moves more than the cost, either way / 指数向任一方向的波动超过成本即获利`, `buy an upside call, pay for it by selling a downside put / 买入上行看涨，卖出下行看跌以支付成本`, `pays if the index falls 5–10%; the cost is the most you can lose / 指数下跌5–10%时获利；最多损失成本`, `pays if the index rises 5–10%; the cost is the most you can lose / 指数上涨5–10%时获利；最多损失成本`.
- Cost copy (EN/ZH): `brings in $X.XX a share / 收入 $X.XX/股` (credit), `costs $X.XX a share · Y.Y% of the index / 成本 $X.XX/股 · 占指数 Y.Y%` (debit), `cost unavailable / 成本暂不可用` (null).
- Pays copy (EN/ZH): `pays past X or Y / 在 X 或 Y 之外获利`, `no breakeven today / 今日无盈亏平衡点` (null).
- Risk copy (EN/ZH): `most you can lose: $X.XX a share · most you can make: $X.XX a share / 最多损失：$X.XX/股 · 最多盈利：$X.XX/股`, `most you can lose: the cost · most you can make: no cap / 最多损失：成本金额 · 最多盈利：无上限`, the rr25 strike-based copy `below X it loses like the index; above Y it gains like the index / 低于 X 时与指数同跌；高于 Y 时与指数同涨`.
- Null disclosure (EN/ZH): `not priced today — one leg had no quote / 今日未定价——某一腿无报价`.
- Verdict (EN/ZH): `Priced move stays inside the walls / 定价波幅在墙位之内`, `Priced move reaches the ceiling / 定价波幅触及上方墙`, `Priced move reaches the floor / 定价波幅触及下方墙`, `Priced move clears both walls / 定价波幅越过上下墙位`.
- Fold summary fallback (EN/ZH): `What a structure pays / 结构的盈亏`.
- Straddle summary (EN/ZH): `Priced move to <expiry>: ±X.X% / 至<expiry>的定价波幅：±X.X%`.
- Track legend (EN/ZH): `Floor / 下方墙`, `Flip / 翻转位`, `Ceiling / 上方墙`.
- Track data-tip (EN/ZH, long-form Tier-1 disclosure): `The two dots are where a straddle bought at this close breaks even at expiry — the move the options market is charging for. The ticks are the dealer-hedging walls measured today. Walls can move before expiry; this compares a to-expiry price with a same-day map. Model estimate from end-of-day mid prices, no fees, not a quote. / 两个圆点标示收盘时买入跨式到期盈亏平衡的价格——即期权市场正在定价的波动幅度。刻度为今日测算的做市商对冲墙位。墙位在到期前可能移动；这里比较的是到期价格与当日墙位。模型估算来自收盘中间价，不含费用，并非实时报价。`
- Footer (EN/ZH): `End-of-day mid prices, <expiry> expiry · ThetaData / 收盘中间价，<expiry>到期 · ThetaData`, plus an optional as-of note `Structures as of <date> / 结构定价截至 <date>`.

**Plain-language walkthrough (selected visible strings from the diff):**

| source | string (EN/ZH side-by-side) | audience | plain-language read |
|---|---|---|---|
| structure name #1 | `'Straddle' / '跨式'` | reader | plain two-syllable label; ZH uses the canonical Mandarin options-vocabulary word (`跨式`, not `跨骑` or `跨价`); no jargon. ✓ |
| structure name #2 | `'Upside for downside' / '以下行换上行'` | reader | plain three-word English label that names the trade (give up downside to gain upside); ZH mirrors the same idea with `以下行换上行` (downside-for-upside) — the standard Mandarin options idiom. ✓ |
| structure name #3 | `'Downside hedge' / '下行保护'` | reader | plain two-word label; ZH uses `下行保护` (the standard Mandarin hedge label). ✓ |
| structure name #4 | `'Upside play' / '上行押注'` | reader | plain two-word label; ZH uses `上行押注` (upside bet, the standard Mandarin directional label — `押注` is the operator's standing "play/bet" idiom). ✓ |
| what-it-pays #1 | `'pays if the index moves more than the cost, either way' / '指数向任一方向的波动超过成本即获利'` | reader | plain bilingual one-clause payoff statement; ZH uses `向任一方向` (either way) and `即获利` (then it pays) — plain Mandarin, no acronym. ✓ |
| what-it-pays #2 | `'buy an upside call, pay for it by selling a downside put' / '买入上行看涨，卖出下行看跌以支付成本'` | reader | plain bilingual two-clause trade-mechanics statement; ZH keeps the leg pair in the same order (`买入…卖出…以支付…`). ✓ |
| what-it-pays #3, #4 | `'pays if the index falls 5–10%; the cost is the most you can lose' / '指数下跌5–10%时获利；最多损失成本'` and the symmetric rise copy | reader | plain bilingual payoff+max-loss statement; the `5–10%` window is preserved in both languages (en-dash in EN, full-width `5–10%` in ZH); ZH uses `下跌` / `上涨` (fall / rise) and `最多损失成本` (most you can lose: the cost) — the standing Mandarin risk idiom. ✓ |
| cost credit | `'brings in $X.XX a share' / '收入 $X.XX/股'` | reader | plain bilingual cost line for a credit structure; ZH uses `收入` (income/credit) — the round-5 fix replaces the prior `获得` per MAJOR 2. ✓ (round-5 fix; pinned by `test_zh_credit_uses_shou_ru_not_huo_de`) |
| cost debit | `'costs $X.XX a share · Y.Y% of the index' / '成本 $X.XX/股 · 占指数 Y.Y%'` | reader | plain bilingual cost line for a debit structure with explicit per-share + as-%-of-index units; the round-5 fix adds `· 占指数 Y.Y%` so ZH mirrors EN's two-unit form. ✓ (round-5 fix; pinned by `test_zh_debit_cost_carries_pct_parity`) |
| cost null | `'cost unavailable' / '成本暂不可用'` | reader | plain Tier-2 null disclosure; matches the standing `cost unavailable` / `成本暂不可用` macro copy convention. ✓ |
| pays copy | `'pays past X or Y' / '在 X 或 Y 之外获利'` | reader | plain bilingual breakeven copy with explicit numeric brackets; ZH uses `之外获利` (pays outside [X, Y]) — the standard Mandarin "outside-the-range pays" idiom. ✓ |
| pays null | `'no breakeven today' / '今日无盈亏平衡点'` | reader | plain Tier-2 null disclosure; matches the standing macro copy convention; ZH uses `盈亏平衡点` (breakeven point, the standing Mandarin financial-vocabulary word). ✓ |
| risk copy (max_loss + max_gain both numeric) | `'most you can lose: $X.XX a share · most you can make: $Y.YY a share' / '最多损失：$X.XX/股 · 最多盈利：$Y.YY/股'` | reader | plain bilingual risk copy with explicit per-share units; the middle-dot `·` is preserved on both sides (ZH uses the full-width `：` colon + `·` middle-dot). ✓ |
| risk copy (cost + no cap) | `'most you can lose: the cost · most you can make: no cap' / '最多损失：成本金额 · 最多盈利：无上限'` | reader | plain bilingual risk copy for a debit-only structure with unbounded upside; `the cost / 成本金额` matches the spec's example wording verbatim; `no cap / 无上限` matches the standing macro unbounded-gain idiom. ✓ |
| risk copy (rr25 strike-based, round-5 SEAT ADDITION) | `'below <put strike> it loses like the index; above <call strike> it gains like the index' / '低于 <put strike> 时与指数同跌；高于 <call strike> 时与指数同涨'` | reader | plain bilingual strike-based risk copy for the rr25 (risk-reversal 25-delta) structure — falls through to the standard max_loss/max_gain copy when the legs are missing or unparseable; ZH mirrors each clause with `低于…时与指数同跌；高于…时与指数同涨`. ✓ (round-5 fix; pinned by `test_rr25_risk_uses_strikes_not_spot_to_zero_loss` with the real receipt numbers SPY spot 773.50, rr25 max_loss −75,387.0, put strike 745, call strike 800 — asserts the strike copy fires, `$753.87` and `75,387` never leak, the straddle on the SAME receipt still uses the standard `$22.92 a share` copy) |
| fold null row | `'not priced today — one leg had no quote' / '今日未定价——某一腿无报价'` | reader | plain Tier-2 null disclosure for an unbuilt structure row; ZH uses `某一腿无报价` (one leg had no quote) — the standard Mandarin options-vocabulary idiom (`腿` = leg). ✓ |
| track verdict (4 clauses) | `'Priced move stays inside the walls' / '定价波幅在墙位之内'`, `'Priced move reaches the ceiling' / '定价波幅触及上方墙'`, `'Priced move reaches the floor' / '定价波幅触及下方墙'`, `'Priced move clears both walls' / '定价波幅越过上下墙位'` | reader | plain four-clause verdict copy; `Priced move` (a passive "what the options market is pricing" form, not "we predict") frames the verdict as a fact about the options chain, not an operator forecast; ZH mirrors each clause with `定价波幅` (priced move) and the four boundary words (`在…之内`, `触及上方墙`, `触及下方墙`, `越过上下墙位`) — plain Mandarin, no jargon. ✓ |
| fold summary fallback (when straddle is NULL) | `'What a structure pays' / '结构的盈亏'` | reader | plain bilingual fold summary text used when the producer's straddle is NULL; matches the standing "What X does" macro copy convention; ZH uses `结构的盈亏` (a structure's profit/loss) — the standard Mandarin options-vocabulary word. ✓ |
| fold summary (when straddle is built) | `'Priced move to <expiry>: ±X.X%' / '至<expiry>的定价波幅：±X.X%'` | reader | plain bilingual summary with explicit expiry + priced-move-percentage; the `±` sign is preserved on both sides (ZH full-width `±`); ZH uses `至…的定价波幅` (priced move to…). ✓ |
| track legend | `'Floor' / '下方墙'`, `'Flip' / '翻转位'`, `'Ceiling' / '上方墙'` | reader | plain three-token legend in upper-case Latin; ZH mirrors each label with `下方墙` (floor wall), `翻转位` (gamma flip point — the standard Mandarin options-vocabulary term for gamma flip), `上方墙` (ceiling wall). ✓ |
| track data-tip (long-form Tier-1) | `'The two dots are where a straddle bought at this close breaks even at expiry — the move the options market is charging for. The ticks are the dealer-hedging walls measured today. Walls can move before expiry; this compares a to-expiry price with a same-day map. Model estimate from end-of-day mid prices, no fees, not a quote.' / '两个圆点标示收盘时买入跨式到期盈亏平衡的价格——即期权市场正在定价的波动幅度。刻度为今日测算的做市商对冲墙位。墙位在到期前可能移动；这里比较的是到期价格与当日墙位。模型估算来自收盘中间价，不含费用，并非实时报价。'` | reader + screen reader | plain bilingual long-form disclosure that actively **disclaims** authority in plain words — names the data class ("end-of-day mid prices"), explicitly **disclaims** the freshness contract ("Walls can move before expiry; this compares a to-expiry price with a same-day map"), and explicitly **disclaims** the production contract ("Model estimate… no fees, not a quote"). This is the OPPOSITE of a "validated" claim — the receipt actively declines to claim intraday accuracy, in plain words. The em-dash (`——` in ZH, ` — ` in EN) is preserved on both sides. ✓ |
| footer | `'End-of-day mid prices, <expiry> expiry · ThetaData' / '收盘中间价，<expiry>到期 · ThetaData'` | reader | plain bilingual data-source + expiry footer; ZH uses `收盘中间价` (end-of-day mid price, the standing Mandarin options-vocabulary term) + `到期` (expiry) + `· ThetaData`; the data-source name `ThetaData` is preserved verbatim (no translation, as is the convention for vendor names). ✓ |
| as-of note | `'Structures as of <date>' / '结构定价截至 <date>'` | reader | plain bilingual as-of note; ZH uses `结构定价截至 <date>` (structures priced as of…). ✓ |

**Bilingual parity check:** every new visible string in the diff carries an EN/ZH pair. The track data-tip uses `data-tip-en` / `data-tip-zh` attributes on the `role="img"` track element (the same `data-tip-*` pattern the shared Lens system reads), so the disclosure is bilingual in a single attribute pair, mirroring the China lens pattern from audit #7607. The structure names use the spec's plain four labels verbatim in both languages; the verdict uses the four-clause truth table verbatim in both languages; the null disclosures use the standing macro null form (`cost unavailable` / `成本暂不可用`, `no breakeven today` / `今日无盈亏平衡点`, `not priced today — one leg had no quote` / `今日未定价——某一腿无报价`).

**Plain-language obligation check:**
- **No internal state names reach the user.** Searched the user-facing surface for `payload.`, `ms_history`, `event_strip`, `state_reason`, `radar.cycle`, `cycle.sector_bias`, `vm['breadth_split']`, `_zh` suffix in user copy, raw slugs (`cpi`, `pmi`, `nfp`, `fomc`, `ms-red`, `gex`, `atm_straddle`, `rr25`, `put_spread_95_90`, `call_spread_105_110`, etc.) — **zero leaks** in the visible strings. The structure-name tuple `_STRUCTURES` and the payload keys (`roots`, `structures`, `expiry_payoff`, `breakevens`, `legs`) live in the builder's internals; the user-facing labels are the four plain names (`Straddle / Upside for downside / Downside hedge / Upside play`) and the four plain one-liners, with no internal key or slug surfacing. ✓
- **No untranslated stats.** Every numeric token (`$X.XX`, `Y.Y%`, `X.X%`, `±X.X%`, `X or Y` breakevens, `<put strike>` / `<call strike>` whole numbers) carries its ZH mirror's unit (`/股`, `占指数`, `±`, full-width `±`, `之外获利`, `低于…时` / `高于…时`). The en-dash (`5–10%`) and the em-dash (`it loses like the index — above`) are preserved with their ZH counterparts (`5–10%`, `低于…时…；高于…时…`). The `·` middle-dot is preserved on both sides. ✓
- **No falsifier vocabulary.** Searched the user-facing surface for `falsifier`, `refuted`, `invalidated`, `thesis-refuted`, `证伪`, `证伪已触发`, `已失效`, `已驳回` — **zero matches**. The track data-tip **actively disclaims** authority ("Walls can move before expiry", "not a quote", "Model estimate") in plain words, which is exactly the operator's 2026-07-27 #3821 rule: "tripwires keep evaluating in the background; user cycle surfaces show projection windows… quiet 'read being updated' chips… and 'what we're watching' conditions — never 'falsifier fired / thesis refuted / 证伪'". ✓
- **No raw slugs in copy.** Searched the user-facing surface for `| capitalize`, `{{ var }}` in visible copy, `{{ face.en }}` outside its `<span class="l-en">` wrapper, `{{ lab.expiry_en }}` rendered as a raw token, etc. — **zero leaks**. The Jinja `t(lab.expiry_en, lab.expiry_zh)` is wrapped in `t()` (the macro's bilingual-EN/ZH helper) and the `{{ lab.bracket.floor_pct }}%` is a numeric literal in a CSS `style="left:X%"` attribute (allowed per the CSS comment: "Inline `left:%` values are data-driven geometry (allowed); nothing else inline"). ✓
- **Plain-word null disclosure.** The three Tier-2 null forms (`cost unavailable / 成本暂不可用`, `no breakeven today / 今日无盈亏平衡点`, `not priced today — one leg had no quote / 今日未定价——某一腿无报价`) are all in plain words, no "unvalidated" / "not validated" / "无验证" / "未验证" framing. ✓
- **No `title=` translations.** Searched the user-facing surface for `title="…"` with translated text — **zero matches**. The fold uses `aria-label` + `data-tip-*` for screen readers + hover tooltips, not `title=`. The track element carries `role="img"` + `data-tip-en` / `data-tip-zh` (the shared Lens's pattern). ✓
- **Bilingual EN/ZH parity in the round-5 fixes.** The MAJOR 2 fix (ZH debit cost line gets `占指数 Y.Y%`) and the MAJOR 2 ZH credit line (`收入` not `获得`) both restore parity between EN and ZH, which the round-3 reviewer flagged. ✓

**Plain-language compliance verdict:** **PASS.** Every new visible string is plain English and idiomatic Mandarin; the bilingual parity holds across all ≈25 additions; the round-5 fixes (ZH debit `占指数`, ZH credit `收入`, rr25 strike-based copy, BUILT fires on max_loss alone, track height 4px, `class="v mono"` on every value span) all restore parity or fix overclaiming; the track data-tip **actively disclaims** authority in plain words ("not a quote", "Model estimate", "Walls can move before expiry") — the Tier-1 calibrated form the operator's 2026-07-27 #3821 rule mandates; the three Tier-2 null disclosures are plain-word; no falsifier vocabulary reaches the user; no internal state names, untranslated stats, or raw slugs leak; no `title=` translations.

---

## 3. Theme findings

**Source for theme law:** `CLAUDE.md` §"Theme art direction — required (dark and light are TWO art directions, not one skin; TP-0 2026-08-27)" + the project-local gates `scripts/check_design_system.py --mode enforce-added`, `scripts/check_runtime_style_injection.py`, `scripts/check_ui_visual_evidence.py`. Theme law covers token-only swaps fail light, runtime stylesheet injection prohibition, reduced-motion discipline, light-mode material treatment, and per-packet evidence matrix.

**Scope check — does this PR carry theme-relevant changes?** Yes — the PR's primary user-visible change is the new `.oew-lab-*` fold CSS with documented TP-0 light-theme overrides (`html[data-theme="light"] .oew-lab-wall{width:2px}` for ink-weight ticks, and `html[data-theme="light"] .oew-lab-be{outline:none}` to drop the tile-coloured ring). The PR is structurally **NOT** a token-only swap — the dark theme uses 1px walls + accent dots + outline rings, the light theme uses 2px walls + the same accent (now from the page's own light-mode token) + no outline. This is exactly the TP-0 form the law exists to enforce.

**Theme compliance walkthrough:**

1. **TP-0 dual art direction.** The PR adds **28 lines of dark CSS** + **2 lines of `html[data-theme="light"]` overrides** + a `@media (max-width:640px)` responsive block. The dark treatment is "command center": 4px hairline rail in `--hair`, 1px×10px walls in `--ink-2` overhanging the rail (`top:-3px`), 1px×4px flip in `--muted`, 6px spot rectangle in `--ink-2`, 7px breakeven rectangles in `--oew-accent` with a 1px `outline` ring of the tile colour, NO `border-radius:50%` (the body explicitly says "No `border-radius:50%` (the breakeven dot is a plain rectangle in `--oew-accent` with a tile-coloured `outline` ring)"). The light treatment is "research workspace": same geometry; **2px walls** in `--ink-2` (ink weight instead of luminance, per the light-mode `html[data-theme="light"] .oew-lab-wall{width:2px}` override); track in `--hair`; breakeven dots in `--oew-accent` (the page's own light-mode `.oew` token — no redefinition); **no ring** (per the `outline:none` override); no shadow. Row values render on the page's monospace face via `class="v mono"`. Every material-color decision delegates to a token (`var(--hair)`, `var(--tile)`, `var(--ink-2)`, `var(--muted)`, `var(--text)`, `var(--oew-accent)`) — **zero hex literals, zero RGB/RGBA literals, zero HSL literals** in the fold CSS. ✓

2. **Mechanisms that intentionally differ (the TP-0 evidence obligation).** The body explicitly enumerates: **dot geometry (rectangle, not circle) × wall width (2px vs 1px) × ring (outline off in light) × shadow (none anywhere) × mono treatment (page token, same in both themes)**. This is exactly the matrix the TP-0 law mandates — five mechanisms that intentionally differ, with the same geometry in both themes, plus the same monospace row-value treatment. The seat records this as the structural TP-0 evidence: not just "tokens swap", but "the walls are 2px in light and 1px in dark, the dots are rectangles not circles, the ring is on in dark and off in light, no shadow anywhere, and mono is the same". ✓

3. **Evidence matrix (TP-0 evidence obligation).** The PR ships a **12-cell evidence matrix**: 8 rest cells (desktop 1440 / mobile 390 × EN / ZH × dark / light) under `mockups/evidence/a-f03-w2-5b-payoff-lab/` + 4 fold-open cells (desktop 1440 / mobile 390 × EN × dark / light, fold forced open via `OEW_PAYOFF_LAB_FORCE_OPEN=1`) under `mockups/evidence/a-f03-w2-5b-payoff-lab/lab_fold_open/`. Both manifests carry `target.resolved_sha_or_none: 26685973134bda141325acbca159202f6564b7c4` (the parent merge commit, per the body-declared Parent-SHA contract). The PNGs are content-addressed digests (`210199b5a2f6cf6c.png`, `62e4f6c577b54064.png`, etc.); re-capture produces new bytes whose digests match the manifest's `file` references. The body explicitly declares that re-capture was required after the round-5 CSS change (track height 14px → 4px + `class="v mono"` on every value span + the four `top:-Xpx` mark re-positionings). This is the standard TP-0 evidence obligation for a user-facing fold change, with **all twelve** cells PASS (per the body-disclosed browser matrix receipt). ✓

4. **Runtime stylesheet injection.** The PR adds **zero new `.js` files**. The only JS-side change is the `OEW_PAYOFF_LAB_FORCE_OPEN` env-var threading in `scripts/build_options_command.py::main()` → `build_context` → `render` → the Jinja template (read as `payoff_lab_force_open` context key, never written to a `style.textContent`, never injected as a `<style>` element). The template's inline `style="left:X%"` on the track's `<i>` elements is **data-driven geometry** (per the body: "Inline `left:%` values are data-driven geometry (allowed); nothing else inline") — i.e., a single computed numeric attribute per element, not a stylesheet injection. The fold's substantive styling lives in the page-token CSS, not in JS-authored runtime style. **Project-local gate (run on local checkout for awareness; CI re-runs on the head):** `python3 scripts/check_runtime_style_injection.py` → `runtime style injection guard OK (197 .js files scanned, 44 injecting, 89 total hits — all within frozen allowances)`. The PR adds zero `.js` files, so the count is unchanged by construction. ✓

5. **Reduced-motion discipline.** The fold adds no `@keyframes` and no `animation:` / `transition:` properties. The track and the four-row table are static — no pulsing, no fade-in, no slide. The fold's `<details>`/`<summary>` toggle uses the browser's native disclosure behavior, which honors `prefers-reduced-motion` by default. ✓

6. **No third-party assets at runtime.** The fold adds no external fonts, no third-party scripts, no `<img>` to a third-party host, no WebSocket connections. The data source is the producer's `site/options_payoff_lab/latest.json` (a same-origin R2-hydrated artifact via `python -m scripts.fetch_r2 --dirs options_payoff_lab` in `engine-render.yml`). The track's `role="img"` + `data-tip-*` text is read by the shared Lens system in `templates/theme.js`, which is same-origin. ✓

7. **Stylesheet `?v=` re-stamp behavior.** The PR does not touch the Caddyfile's `immutable` list (`theme.css`, `live.js`, `theme.css`, `illus.css`, etc.). The fold CSS lives inside `templates/options.html.j2`'s `<style>` block (the `.oew-*` family), which is inline-page CSS, not a separately-cached `?v=` asset. The standing `render.yml` re-stamp behavior applies to the `?v=` suffix on the page itself, not to inline `<style>` blocks. ✓

8. **GH annotation line-start (CLAUDE.md §House laws).** The PR adds one new `::warning title=options-payoff-lab-hydrate::` annotation in `.github/workflows/engine-render.yml` for the R2 restore step's non-fatal miss path. The annotation is emitted as a bare `print("::warning title=options-payoff-lab-hydrate::", flush=True)` — NOT through a logger (the engine-render workflow runs inside an Actions step). The title is a kebab-case slug, the message is the non-fatal-miss reason. This matches the standing GH-annotation-line-start law: bare `print`, kebab-case title, no logger prefix. ✓

9. **Paired plain-copy asset parity.** The PR does not touch `templates/_public_nav.html.j2` / `site/glossary.html` (the anonymous-nav chrome) or `templates/_site_nav.html.j2` / paired nav HTML (the authenticated chrome). The Options page is an authenticated-shell page (it uses `_site_nav.html.j2`), so the standing `scripts/check_template_site_sync.py` CI guard applies to its paired plain-copy assets — but the PR is scoped to a single page's template patch (no `site/options.html` carry-through in this PR; the page-pair parity is handled by the next `render.yml` bake). The PR does not introduce a paired plain-copy asset. ✓

**Theme-system structural sub-finding (positive):** the fold's light-theme override is exactly the TP-0 form the law mandates — the same geometry, different treatment (2px walls instead of 1px, no ring instead of outline-on), no token redefinition, no shadow. The body explicitly enumerates the **five mechanisms that intentionally differ** (dot geometry, wall width, ring, shadow, mono treatment), which is the structural evidence the TP-0 law requires. The 12-cell evidence matrix is content-bound by the manifest shas and the Parent-SHA contract, with all 12 cells PASS. ✓

**Theme compliance verdict:** **PASS.** The new `.oew-lab-*` fold CSS is **structurally** a TP-0 win — same geometry, different treatment across themes (dot geometry × wall width × ring × shadow × mono treatment), with explicit `html[data-theme="light"]` overrides for the two mechanisms that must differ (wall width 2px vs 1px, ring off vs on). Every material-color decision delegates to a token (`--hair`, `--tile`, `--ink-2`, `--muted`, `--text`, `--oew-accent`); the inline `style="left:X%"` is data-driven geometry, not a stylesheet injection; zero hex literals, zero RGB/RGBA literals, zero HSL literals in the fold CSS. The 12/12 PASS browser matrix (8 rest + 4 fold-open × 1440/390 × EN/ZH × dark/light) is content-bound by the parent-merge-commit sha. The new `::warning title=options-payoff-lab-hydrate::` annotation is emitted via bare `print` (not through a logger). The fold adds no `@keyframes` / `animation:` / `transition:`, no third-party assets at runtime, and no new `.js` files.

---

## 4. Validated-claims findings

**Source for validated-claims law:** `scripts/check_validated_claims.py` (BC-2, PREREGISTRATION.md §4, D2 §4.3). The doctrine: every "validated" / "已验证" / "经验证" / "经过验证" claim in a user-facing surface must trace to (a) a justified entry in `data/regime/validated_claims_allowlist.json` whose `surfaces` list includes the claiming file, OR (b) an artifact JSON whose top-level `validated == true`. Negated/hedged uses are ignored. Third-party research quotations are skipped by `_THIRD_PARTY_PAGES`. Engine source copy is also scanned (claims gated at the source, not at nightly-render).

**Search for new "validated"-class claims introduced by this PR (full user-facing surface in `templates/options.html.j2` and `scripts/build_options_command.py`):**

- `templates/options.html.j2` diff (+75 lines, the `.oew-lab-*` CSS + the `<details class="oew-aib-detail oew-lab">` markup): scanned line-by-line for the token class `/validated|已验证|经验证|经过验证/`. **Zero matches.** The track's `data-tip-en` / `data-tip-zh` attributes carry the long-form Tier-1 disclaimer copy ("Model estimate… no fees, not a quote", "Walls can move before expiry"), not a "validated"-class claim. The verdict strings ("Priced move stays inside the walls", "Priced move reaches the ceiling", etc.) are descriptive of what the options market is pricing, not a "validated" claim. ✓
- `scripts/build_options_command.py` diff (+483/−5 lines, the `load_payoff_lab` + `build_payoff_lab` + the four canonical structures + the verdict + the rr25 helper): scanned the user-facing string returns (`out["*_en"]`, `out["*_zh"]`, the verdict strings, the constants `_PAYOFF_LAB_NULL_EN`, `_PAYOFF_LAB_SUMMARY_FALLBACK_EN`, `_STRUCTURES` tuples, the `_payoff_lab_format_breakeven` / `_payoff_lab_risk_word` / `_payoff_lab_rr25_strikes` returns, the `asof_note_en` / `asof_note_zh` strings) for the token class — **zero matches.** The credit/debit/pays/risk copy is descriptive of what each structure pays and what it costs, not a "validated" claim. The verdict is descriptive of the wall-bracket geometry, not a "validated" forecast. ✓
- `tests/test_options_payoff_lab_consumer.py` diff (+1,040 lines, hermetic tests): scanned for the token class — **zero matches.** Test names are descriptive (`test_built_when_only_max_loss_is_non_null`, `test_zh_debit_cost_carries_pct_parity`, `test_zh_credit_uses_shou_ru_not_huo_de`, `test_rr25_risk_uses_strikes_not_spot_to_zero_loss`, `test_tenor_days_passes_int_through_and_none_on_non_int`, `test_css_oew_lab_track_height_is_4px`, `test_css_oew_lab_row_v_has_mono_class_in_markup`) — the tests pin **observable facts** about the fold, not "validated"-class claims. ✓
- `.github/workflows/engine-render.yml` diff (+14/−1 lines, the R2 restore step + the `brun options_payoff_lab … --emit` line): scanned for the token class — **zero matches.** The step name is `restore options_payoff_lab from R2 (W2-5a store-host producer)`; the `::warning title=options-payoff-lab-hydrate::` annotation carries a kebab-case slug, not a "validated" claim. ✓

**Track data-tip Tier-1 explicit disclaimers (positive sub-finding):** the track's `data-tip-en` / `data-tip-zh` long-form disclosure **actively disclaims** authority in plain words, which is the **opposite** of a "validated" claim:

- "Walls can move before expiry; this compares a to-expiry price with a same-day map." / "墙位在到期前可能移动；这里比较的是到期价格与当日墙位。"
- "Model estimate from end-of-day mid prices, no fees, not a quote." / "模型估算来自收盘中间价，不含费用，并非实时报价。"

This is the **calibrated** Tier-1 form the operator's 2026-07-27 #3821 rule mandates: "tripwires keep evaluating in the background; user cycle surfaces show projection windows ('windows, not certainties — re-drawn nightly'), quiet 'read being updated' chips, and 'what we're watching' conditions — never 'falsifier fired / thesis refuted / 证伪'". The receipt text follows that pattern exactly — it tells the reader what the page is **not** doing (not a quote, no fees, not a fresh intraday measurement) rather than asserting that the result is "validated". ✓

**Verdict Tier-1 descriptive form (positive sub-finding):** the four verdict strings ("Priced move stays inside the walls" / "定价波幅在墙位之内", etc.) frame the outcome as **what the options market is pricing** (`Priced move` — a passive form about the priced implied move, not "we predict" or "we forecast"), not as a "validated" thesis or a calibrated forecast. This is exactly the operator's standing distinction between **instrument verdicts and market verdicts** (CLAUDE.md §"Instrument verdicts are NOT market verdicts", operator 2026-08-09): the verdict reports the scope ("priced move stays inside the walls" — a fact about the options chain), never "the thesis is validated / refuted". ✓

**Null disclosures Tier-2 plain-word form (positive sub-finding):** the three Tier-2 null forms (`cost unavailable / 成本暂不可用`, `no breakeven today / 今日无盈亏平衡点`, `not priced today — one leg had no quote / 今日未定价——某一腿无报价`) all use plain words, never "unvalidated" / "not validated" / "无验证" / "未验证" framing. The standing BC-2 negation/hedge skip (`no validated …`, `not a validated …`, `unvalidated`, `not … validated`, `un-validated`, "hasn't/doesn't/won't/… validated", `cannot … validated`, `无...验证`, `非...验证`, `未...验证`, `未经验证`, `不...已验证`) covers "not priced today" cleanly — the phrase is not in the negation set but is also not in the affirmative "validated" set, so it is neither a claim nor a negation. ✓

**Project-local validated-claims gate (run on local checkout for awareness; CI re-runs on the head):**
- `python3 scripts/check_validated_claims.py 2>&1 | grep -E "options\.html|build_options_command"` → **no rows.** The pre-existing 38 UNEARNED findings the gate emits are all in `templates/_macro_suite_shell.html.j2`, `templates/canada.html.j2`, `templates/hk.html.j2`, the `macro_<topic>.html.j2` family, `mm_brain.js`, `macro_suite.js`, their rendered `site/` siblings, and `engine/market_os/macro_workspaces/consumer.py` — **none of which this PR touches.** ✓
- `python3 scripts/check_validated_claims.py 2>&1 | wc -l` → 38 (i.e., the count is unchanged from the pre-PR baseline — the PR adds zero new exposure). ✓

**Validated-claims compliance verdict:** **PASS.** Zero new "validated"-class claims in any of the ≈25 new visible strings; zero claim-class tokens in the template patch, the build script, the hermetic tests, or the engine-render workflow; the track data-tip **actively disclaims** authority ("Model estimate… not a quote", "Walls can move before expiry") in plain words — exactly the Tier-1 calibrated form the operator's 2026-07-27 #3821 rule mandates; the verdict strings are descriptive of the priced move's wall-bracket geometry, not "validated" forecasts; the three Tier-2 null disclosures use the plain-word form rather than the negation set; the pre-existing estate is unchanged (38 UNEARNED findings, all in unrelated templates and engine code).

---

## 5. Overall verdict

**PASS — Options payoff-lab consumer fold ("What a structure pays") + R2→emit wiring on the render path; all three audit dimensions pass with positive sub-findings on the theme-system dual art direction, the Tier-1 calibrated disclaimers, and the round-5 fixes that restore bilingual parity.**

| dimension | verdict | evidence |
|---|---|---|
| plain-language | PASS | ≈25 new visible strings audited; all plain-word, bilingual EN/ZH via `t(en, zh)` pairs and `data-tip-en`/`data-tip-zh` attribute pairs; the four structure names use plain labels with standing Mandarin options-vocabulary words (`跨式` / `以下行换上行` / `下行保护` / `上行押注`); the verdict strings use the four-clause truth table in both languages (`Priced move stays inside the walls / 定价波幅在墙位之内`); the round-5 fixes (ZH debit `占指数 Y.Y%`, ZH credit `收入`, rr25 strike-based copy, BUILT fires on max_loss alone, track height 4px, `class="v mono"` on every value span) all restore parity or fix overclaiming; the track data-tip **actively disclaims** authority in plain words ("Walls can move before expiry", "not a quote", "Model estimate") — the Tier-1 calibrated form, not the negation set; the three Tier-2 null disclosures (`cost unavailable / 成本暂不可用`, `no breakeven today / 今日无盈亏平衡点`, `not priced today — one leg had no quote / 今日未定价——某一腿无报价`) are plain-word; no falsifier vocabulary reaches the user; no internal state names, untranslated stats, or raw slugs leak; no `title=` translations |
| theme (TP-0) | PASS | The new `.oew-lab-*` fold CSS is **structurally** a TP-0 win — same geometry, different treatment across themes (dot geometry × wall width × ring × shadow × mono treatment — five mechanisms that intentionally differ), with explicit `html[data-theme="light"]` overrides for the two mechanisms that must differ (wall width 2px vs 1px via `html[data-theme="light"] .oew-lab-wall{width:2px}`, ring off vs on via `html[data-theme="light"] .oew-lab-be{outline:none}`); every material-color decision delegates to a token (`--hair`, `--tile`, `--ink-2`, `--muted`, `--text`, `--oew-accent`); zero hex literals, zero RGB/RGBA literals, zero HSL literals in the fold CSS; the inline `style="left:X%"` is data-driven geometry (per the body's "data-driven geometry (allowed); nothing else inline" note), not a stylesheet injection; the 12/12 PASS browser matrix (8 rest + 4 fold-open × 1440/390 × EN/ZH × dark/light) is content-bound by the parent-merge-commit sha `2668597313` per the body's Parent-SHA contract; `check_design_system.py --mode enforce-added` = 0 blocking findings; `check_runtime_style_injection.py` clean (zero new `.js` files); the new `::warning title=options-payoff-lab-hydrate::` annotation is emitted via bare `print` (not through a logger); no `@keyframes` / `animation:` / `transition:` in the fold CSS |
| validated-claims | PASS | Zero new "validated"-class claims in any of the ≈25 new visible strings; zero claim-class tokens in the template patch, the build script, the hermetic tests, or the engine-render workflow; the track data-tip **actively disclaims** authority ("Walls can move before expiry", "not a quote", "Model estimate") in plain words — exactly the Tier-1 calibrated form the operator's 2026-07-27 #3821 rule mandates; the verdict strings are descriptive of the priced move's wall-bracket geometry, not "validated" forecasts; the three Tier-2 null disclosures use the plain-word form (`cost unavailable`, `no breakeven today`, `not priced today — one leg had no quote`) rather than the negation set; the pre-existing estate is unchanged (38 UNEARNED findings, all in unrelated templates — `templates/options.html.j2` and `scripts/build_options_command.py` are NOT in the pre-existing list) |
| completeness | PASS | `test_options_payoff_lab_consumer.py` **21 passed in 1.80s** (body-quoted); pre-existing Options tests **152 passed in 4.43s** (unaffected); 12/12 PASS real-browser matrix across 1440/390 × EN/ZH × dark/light (HTTP 200, zero page errors, zero failed requests, zero horizontal overflow in all 12 cells); both manifests re-captured against the round-5 CSS with `target.resolved_sha_or_none: 2668597313` (parent merge commit, per Parent-SHA contract); new PR-gating home `options-payoff-lab-consumer` is `gate: code` (flow-cohorts shape) running `tests/test_options_payoff_lab_consumer.py`; `engine-render.yml` gains the R2 restore step + `brun options_payoff_lab … --emit` inside the existing `cl_gex()` band — its largest run block is 19,633 chars against the 20,500 guard; `config/dag.yml` declares `build_options_payoff_lab` + adds the slug to engine-render's ORDER for `TestBrunOrderVisibility`; the seat verification at `ac88c6f0` (round 5/N), the heal at `af9cabcd` (EVIDENCE.yml YAML-docs + dag-conformance), and the second merge-of-main at `e78d3b2d` (ci.yml `pull_request` run mint) are all body-disclosed and seat-ratified |

**Positive sub-findings (NOT 7763 findings, surfaced for awareness):**
- **TP-0 dual art direction (structural).** The fold's light-theme override is exactly the TP-0 form the law mandates — same geometry, different treatment (2px walls instead of 1px, no ring instead of outline-on, no shadow anywhere), no token redefinition. The body explicitly enumerates the **five mechanisms that intentionally differ** (dot geometry × wall width × ring × shadow × mono treatment), which is the structural evidence the TP-0 law requires. The 12-cell evidence matrix is content-bound by the manifest shas and the Parent-SHA contract, with all 12 cells PASS. ✓ (positive, not a finding)
- **Tier-1 calibrated disclaimers (track data-tip).** The track data-tip **actively disclaims** authority in plain words ("Walls can move before expiry; this compares a to-expiry price with a same-day map", "Model estimate from end-of-day mid prices, no fees, not a quote") — the OPPOSITE of a "validated" claim. This is exactly the operator's 2026-07-27 #3821 form: tripwires keep evaluating in the background; user surfaces show projection windows + plain-word disclaimers, never "falsifier fired / thesis refuted / 证伪". ✓ (positive, not a finding)
- **Round-5 fixes restore bilingual parity and fix overclaiming.** The MAJOR 1 fix (BUILT fires on max_loss alone, not max_gain alone — a debit-only structure no longer silently drops the WHOLE fold) + MAJOR 2 fixes (ZH debit `占指数 Y.Y%` + ZH credit `收入` not `获得`) + MAJOR 3 fixes (track height 4px + `class="v mono"` on every value span) + SEAT ADDITION (rr25 strike-based copy, no `$753.87` leakage) + MINOR 2 (tenor int passthrough) all restore EN/ZH parity or fix overclaiming that the round-3 reviewer caught. Each fix is pinned by a red-first test that fails on the prior head and passes on this head. ✓ (positive, not a finding)
- **Parent-SHA contract (evidence ledger honesty).** The body declares that `scripts/capture_page_evidence.py` records HEAD of the nearest git directory above `--site-dir` at capture time, so the manifest's `target.resolved_sha_or_none` points at the parent of the commit that ADDS the manifest (`2668597313`, the merge commit, parent of `b02b73757`) — the only truthful value, since the commit that ADDS a manifest cannot contain its own sha. This is exactly the evidence-ledger honesty the law mandates: the receipt sha is the parent merge commit, not the reviewed head, and the body documents the contract explicitly. ✓ (positive, not a finding)
- **Engine-render R2 wiring parity.** The new `restore options_payoff_lab from R2 (W2-5a store-host producer)` step is modeled on the existing `restore options_skew ledger from R2` step (same R2 env shape, same non-fatal `::warning` on miss), and the new `brun options_payoff_lab … --emit` line sits in the existing `cl_gex()` band immediately after `brun options_skew --emit`. The two steps form a coherent producer/consumer pair without restructuring the engine-render workflow. ✓ (positive, not a finding)

**Body-disclosed follow-ups (NOT 7763 findings, surfaced for awareness):**
- **W2-5a DEC file LEAVES the diff.** The body declares STEP 0 merged origin/main and took W2-5a's `agentos/decisions/DEC-F03-W2-5-PAYOFF-LAB-CHARTERED-AFTER-C0-FREEZE.md` from origin/main so it LEAVES this PR's diff. `git diff --stat origin/main...HEAD -- agentos/decisions/DEC-F03-W2-5-PAYOFF-LAB-CHARTERED-AFTER-C0-FREEZE.md` = empty (verified). The DEC citation chain `DEC:F03-W2-5B-…-CONSUMER-IS-A-CARD-FOLD` → `DEC:F03-W2-5-PAYOFF-LAB-CHARTERED-AFTER-C0-FREEZE` resolves against main. ✓ (closed by STEP 0, no follow-on owed)
- **Live proof owed after merge.** The next `engine-render` bake must show `site/options_payoff_lab/latest.json` restored from R2 (first store-host publish fires 15:00Z today via `com.macro.payofflab`) and the "What a structure pays" fold on options.html with `accrual_state` honest (`ledger_only` until the first accrual lands, then `accrued_today`). ✓ (receipt is the proof; CI would re-run on the head)
- **Producer PR #7759 (W2-5a) ownership.** The consumer waits on the producer's live artifact; the fold renders `ledger_only` until the first accrual lands. The producer's HOLD-FOR-SOL (if any) is the producer's, not the consumer's. ✓ (advisory only, no follow-on owed)
- **Body-counted 23 files.** The body states the MEASURED count: `gh pr view 7763 --json files --jq '.files|length'` returns 22 files (the W2-5a DEC file LEAVES the diff via STEP 0, dropping from the round-3 head's 23). The PR was opened at 23 files, the STEP 0 reduction drops it to 22, and the round-5 handoff file (`agentos/handoffs/WS-MARKET-OS-2026-09-23-f03-w2-5b-payoff-lab-consumer-r5.md`) brings it back to 23 — so the count in the body matches the count on disk. ✓ (consistency, not a finding)

**Inherited base conditions (NOT 7763 findings):** the repo-wide pre-existing debt captured by `check_validated_claims.py` (38 UNEARNED rows across `_macro_suite_shell.html.j2`, the `macro_<topic>.html.j2` family, `mm_brain.js`, etc.) is **unchanged** by this PR — `templates/options.html.j2` and `scripts/build_options_command.py` are NOT in the pre-existing list, and none of the 38 files are in the diff. The `site/theme.js != templates/theme.js` template/site-sync mismatch reported in earlier audits is also unaffected — #7763 edits `templates/options.html.j2`'s inline `<style>` block (a single-page inline style, not the shared chrome) and `scripts/build_options_command.py` (a builder, not a shared asset), so the page-pair parity is preserved by construction. The broader `theme.js` template/site-sync mismatch is a separate, pre-existing lane.

**One-pass constraint honored:** no retries, no scope expansion, no durable file written outside the audit path. The audit output is this file, written to `/Users/chriswong/lanes/repos/macro/orch/audits/macro_PR-7763.mm.md` per the transport contract; the K reference (`meta-ceo-b-2026-09-08`) is honored by the audit header. No `gh` API quota was burned beyond the initial `pr list` / `pr view` / `pr diff` calls required by the task spec; no `gh` calls were made against the PR head beyond the local diff read.

---

`SESSION END: PROVEN_OUTCOME`
