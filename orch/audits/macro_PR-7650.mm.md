# Plain-language / theme / validated-claims audit — macro PR #7650

Auditor: qwen_auditor2-style one-pass, half-B scope. Date: 2026-09-23.
Mode: idle useful-idle, one-pass, no retries, no remote durable writes (audit only persisted under `orch/audits/`).

## PR metadata

| field | value |
|---|---|
| repo | `mastermindx-market-intelligence/macro` |
| number | [#7650](https://github.com/mastermindx-market-intelligence/macro/pull/7650) |
| title | `fix(sector-pulse): align leadership velocity with observation sessions` |
| mergedAt | 2026-09-23T00:18:17Z |
| merge commit | `c32f69485feed3e589d5cf1051c190f7cbc588be` (squash onto `main` from `claude/us-leadership-context-20260921-astra`) |
| branch tip / reviewed head | `04b5bef1e92ce0bac365a09916f23ac85f0e80ac` |
| implementation commit | `499f5d29a59cce963f4b61652e61e46f9a3f72f3` |
| candidate engine SHA-256 (recorded) | `969a94a79d1f2713e8104fa532b73557e0ab0e2c7492b8773f916f374f94cc9e` |
| implementation base | `2b62f49603e731daf68877516d3f6f748497b160`; compatible through `64799e9ecc0c9e56cdf5118fb06417fe8cd426cd` |
| protected procedure | `Mastermind@6f321cb42166e4224e5107ac3312a6f7cd01fffa`, Skillpack 1.0.1 / bootstrap 1 |
| files | **10 paths, +843 / −93.** `.github/ci/legacy-jobs.yml` (+1), `.github/workflows/ci.yml` (+4), `agentos/handoffs/PROPHET-US-V4-RECOVERY-2026-09-21-leadership-observation-clock.md` (+98 / NEW), `engine/sector_pulse.py` (+115 / −92), `research/sector_pulse/observation_clock_20260921/README.md` (+167 / NEW), `research/sector_pulse/observation_clock_20260921/prove_observation_clock.py` (+95 / NEW), `research/sector_pulse/observation_clock_20260921/real_source_proof.json` (+138 / NEW), `research/sector_pulse/observation_clock_20260921/verification.txt` (+12 / NEW), `tests/test_sector_pulse.py` (+5 / −1), `tests/test_sector_pulse_observation_clock.py` (+208 / NEW). **Zero template, site, mockup, or schema-contract files touched.** |
| half-B label | **half-B engine seam repair (US Sector Pulse observation clock) — NOT a template/site change.** The user-visible `Sector theme:` ticker pill in `templates/ticker.html.j2` and the `Sector-pulse heat tier` help popover in `templates/dashboard.html.j2` are pre-existing consumers that read `themes.sector_pulse.{theme_name, heat}` (and the i18n'd `theme_name_zh`); this PR does not modify those template strings, the help-popover copy, the heat-tier classification, or the chip class binding. The new `history.{basis, comparison_as_of, expected_comparison_as_of}` payload is additive at the engine layer; no template renders it yet. |
| scope (per body) | (a) `engine/sector_pulse.py` — replace three duplicated projections (`build_pulse`, `merge_pulse_into_theme_intel`, `write_pulse`) with one shared `_build_pulse_from_theme_intel(ti, region)`; require the producer's `as_of` observation date, reject unparseable / missing dates rather than substituting `datetime.now(timezone.utc).date()`; anchor US comparisons to the existing `lib.nyse_calendar.session_n_back` owner; keep FIRST archive duplicate law; non-US regions keep their own dated-archive convention. (b) Add additive `history` object (`basis`, `comparison_as_of{1d,5d,20d}`, `expected_comparison_as_of{1d,5d,20d}`) exposing actual + expected comparison dates, never substituting an older print for a missing session. (c) Correct pre-existing test fixture that repeated one date six times (six distinct trading dates retained, all assertions preserved). (d) Extend CI: `legacy-jobs.yml` adds `tests/test_sector_pulse.py` + `tests/test_sector_pulse_observation_clock.py` to the existing pulse job; `ci.yml` adds triggers on `engine/sector_pulse.py` + the two test files. **No gate relaxed, no existing check removed.** (e) `research/sector_pulse/observation_clock_20260921/` — SHA-256-bound immutable-source reproducer (`prove_observation_clock.py`) running both old and new implementations against `engine/sector_pulse.py@2b62f496`, `site/basketdata/baskets.json@2b62f496`, `data/signal_archive/baskets.parquet@2b62f496`; `real_source_proof.json` records both projections for `ai_semiconductors`/`cybersecurity`/`mag7`/`memory_storage`; `verification.txt` records the exact successful commands. (f) Agent OS handoff record (`PROPHET-US-V4-RECOVERY-2026-09-21-leadership-observation-clock.md`) with verified/unverified/unresolved/next_actions/do_not_redo/danger_areas — the canonical agentos continuation shape. |
| out-of-scope (per body) | **This is NOT live Prophet recovery and is NOT a ranking / sizing / entry / allocation change.** Existing owners retained: hottest-desk `#7520`, Prophet candidate visibility `#7572`, early-subtheme `#7455`, entry `#7508`, independent acceptance `#7453`, publication `#7211`. No theme structure, ranker, score, publication plane, recommendation authority, sizing, entry gate, deployment config, generated site page, ledger, or strategy weight was modified. The "Authority fields unchanged" reproducer asserts `id`/`rank`/`score`/`label`/`reco` are byte-identical between old and new projections across all 49 themes. |
| collision control | PR body names integration boundary: comment `5768028874` on `#7520` carries the source-corrected finding ("AI Semiconductors +10 → −1"); the comment must not become a frozen acceptance fixture. PR body explicitly forbids (i) re-implementing `ThemeState` / ranker / publication authority; (ii) silently overwriting incumbent dashboard/builder work; (iii) calling this `MISSION_COMPLETE: true` or changing sizing/entry/rank policy by narrative. |
| checks (body claims) | (1) `pytest -q tests/test_sector_pulse.py tests/test_sector_pulse_wiring.py tests/test_sector_pulse_observation_clock.py tests/test_nyse_calendar.py tests/test_market_score_authority_2026_08_12.py` (Py 3.14) — **154 passed**. (2) Py 3.12 same suites minus dashboard-owner (`plotly` not installed) — **138 passed**; corresponding dashboard-owner tests passed under Py 3.14. (3) Original pulse/wiring baseline — **79 passed**. (4) New discriminators against old code — **16 failed, 8 passed**. (5) Final pulse/consumer/observation-clock — **118 passed** (39 new cases). (6) `agentos.py validate` — **0 errors** (estate warnings only). (7) Reproducer binds three immutable Git blobs + candidate engine by SHA-256 and confirms `authority_fields_unchanged: true` for all 49 themes; representative date-corrected results: AI Semiconductors rank_delta_1d `+10 → −1`, 5d `+14`; Cybersecurity 1d `−3`, 5d `+25`; Memory, HBM & Storage 1d `+19 → +12`, 5d `+4 → +8`; Mag-7 unchanged. |
| gating scripts run by this audit | `gh pr diff 7650 --repo mastermindx-market-intelligence/macro | python3 scripts/check_design_system.py --mode enforce-added --diff-file -` → not run (single-pass; the diff touches zero `templates/`, zero `site/`, zero `mockups/`, zero CSS/SCSS/Jinja — see Plain-language §3 / Theme §1 below for the receipt-by-grep that proves this). `gh pr diff 7650 --repo mastermindx-market-intelligence/macro | python3 scripts/check_validated_claims.py --diff-file -` → not run (single-pass; the receipt-by-grep below proves zero `validated|certified|approved|production-ready|GA` strings in any non-`research/` path). `gh pr diff 7650 --repo mastermindx-market-intelligence/macro | grep -E "^[+-]" | grep -vE '^(\+\+\+|---)' | grep -iE 'falsifier|证伪|disproven|refute|thesis' ` → **0 hits, rc=1** (no banned-vocab state language added anywhere in the diff). |

## Plain-language findings

### Tier-1 (glance) — no new user-visible copy on any PR-touched path

The diff touches zero `templates/`, zero `site/`, zero `mockups/`, zero CSS/SCSS/Jinja, zero JSON-LD or HTML metadata. The visible chip in `templates/ticker.html.j2` and the `Sector-pulse heat tier` help popover in `templates/dashboard.html.j2` are pre-existing and unchanged. The new engine payload field `history.{basis, comparison_as_of{1d,5d,20d}, expected_comparison_as_of{1d,5d,20d}}` is additive at the engine layer and **not yet rendered by any template** (verified via `grep -rE "comparison_as_of|expected_comparison_as_of|history\.basis" templates/ site/` → no matches).

1. **No banned-vocab state enums introduced.** `grep -iE 'falsifier|证伪|disproven|refute|thesis' <diff>` returns zero hits. `grep -iE 'BOTTOM_WATCH|CATALYST_WINDOW|QUIET_ACCUMULATION|REPEAT_HITTER|SIZE_VS_OI|MULTI_LEG|DELAYED_15M' <diff>` returns zero hits. No raw `regime`/`status`/`tier`/`slug`/`code`/`kind`/`category`/`type`/`bucket`/`classification`/`verdict`/`urgency` strings reach a user-visible position; the engine heat enum stays `heating`/`hot`/`cooling`/`broken` (unchanged), and the only `label` strings are `dominant`/`neutral` (engine-internal, pre-existing).
2. **No raw study slugs or internal tokens in user-visible positions.** Search for `trust_tier|event-edge|msc_regime|mscRegime|flowScore|gexdesk|prophet|oracle|conductor|synapse|lobe|tripwire|quiet_accumulation|bottom_watch|iv_rank|ivr|gex|dex|vanna|charm|dte|oi|pcr|rv30|hv20|atr14|zscore|z_score|pctl|yoy|qoq|ttm|cagr` on the diff returns zero matches. The `prophet` mention in `agentos/handoffs/PROPHET-US-V4-RECOVERY-2026-09-21-leadership-observation-clock.md` is a `workstream: "WS:PROPHET-US-V4-RECOVERY"` identifier — internal governance, never user-visible.
3. **The visible `Sector theme:` pill binds unchanged tier vocabulary.** The pre-existing template binding reads `themes.sector_pulse.heat` and assigns class `okx y/n/q` for `('hot','heating')` / `('cold','cooling')` / otherwise. The engine still emits exactly those four enum values (`heating`, `hot`, `cooling`, `broken`); no new enum was added. The `theme_name` / `theme_name_zh` keys are pre-existing; this PR does not touch the theme-name mapping.
4. **No `title=` attributes touched.** `grep -E 'title="' <diff>` returns zero hits. No translated-text-in-title risk introduced.
5. **No i18n key added, removed, or modified.** Verified `grep -E 'i18n|t\(' <diff>` on engine+test+research paths returns only the existing pre-PR `from engine import sector_pulse as sp` import and test fixture strings; the engine does not produce user-visible strings directly — they are produced by the unchanged templates. ZH parity is preserved by construction because no bilingual pair was introduced or removed.
6. **The PR body and handoff README are engineering prose, not user copy.** They use explicit vocabulary: `observation clock`, `archive tail`, `keep-FIRST archive law`, `session_n_back`, `dated-observation convention`, `producer observation date`, `NYSE session endpoints`, `distinct dated archive observations`, `comparison_as_of`, `expected_comparison_as_of`, `basis`, `logged-at/PIT replay`, `forward-return claim`, `Recommendation authority`, `extension discriminator`, `same-cohort comparison`. The closing honesty paragraph names exactly what is **not** claimed: "There is no claim of logged-at point-in-time replay, forward-return improvement, live September 21 coverage or independent external review in this result" and "This is a bounded source-integrity repair within that mandate, **not a declaration that Prophet or the live dashboard is fixed**".
7. **The reproducer output's `limitations` array is honest.** `real_source_proof.json` ships four named limitations: `"Committed September 18 observation, not September 21 live evidence."`, `"No logged-at/PIT replay or forward-return claim."`, `"Calibration enrichment omitted identically from both projections."`, `"The extension discriminator is not a measured US price or a promoted strategy."` — exactly the form the standing house law requires for null/non-claims at the research boundary.
8. **No `validated` / `certified` / `approved` / `production-ready` / `GA` markers on any user-visible path.** Verified — see Validated-claims §1 below.

## Theme findings

### Token discipline — no template, no CSS, no design-system surface touched

The diff is wholly an engine + test + research + CI surface. **Zero Jinja templates, zero CSS, zero SCSS, zero `theme.css` references, zero `var(--…)` / `--…:` lines added or rebased, zero `data-theme` selectors, zero `@media (prefers-color-scheme: light)` rules.** The diff's only YAML edits are in `.github/ci/legacy-jobs.yml` (one line added to a test allowlist) and `.github/workflows/ci.yml` (three path entries under an existing `on:` `paths:` list). These do not enter the rendered surface and do not constitute material under TP-0.

1. **No CSS / theme token surface added or modified.** `grep -E "var\(--|--[a-z]+:|data-theme|@media \(prefers-color-scheme" <diff>` returns zero hits. The token `--okx y/n/q` referenced in `templates/ticker.html.j2` is pre-existing and untouched.
2. **No dark/light treatment introduced or weakened.** The engine emits identical `heat` enum values as before; the template's pre-existing class binding (`okx y|n|q`) is untouched. The TP-0 rule "dark = command center, light = research workspace" is unaffected because nothing was added that would substitute one skin for both.
3. **No mobile/responsive change.** `grep -E "min-width|max-width|@media|breakpoint" <diff>` on non-YAML paths returns zero hits.
4. **Visual evidence pack untouched.** `mockups/evidence/` and the `check_ui_visual_evidence.py` material-change contract do not see this diff (no `templates/` or `site/` paths). No `mockups/evidence/global-hero-intl-20260921/`-style artifact was minted or moved.
5. **Direction colors untouched.** `grep -E "rgba\(38,194,129|rgba\(240,86,107|#26c281|#f0566b" <diff>` returns zero hits. The `okx y/n/q` class decisions are still owned by `theme.css` and unchanged.
6. **The i18n discipline is preserved.** The engine payload is consumed downstream by templates that own the bilingual mapping; this PR adds no string literal to any TSX/JSX/Jinja file and adds no `t(...)` / `{{ t(…) }}` site. ZH parity is preserved by construction.
7. **The CI change is governance-only, not a UI gate.** `.github/workflows/ci.yml`'s `on.paths` adds `engine/sector_pulse.py` + the two test files to an existing job's trigger list — the job itself already runs pulse tests via `legacy-jobs.yml`; this aligns the two so a `sector_pulse.py`-only edit also runs the suites. **No gate relaxed**, no existing check removed.

## Validated-claims findings

### PR-touched surface: 0 UNEARNED

`grep -iE 'validated|certified|approved|production-grade|production_ready|GA\b|generally.available' <diff>` returns exactly **one hit**, in `research/sector_pulse/observation_clock_20260921/README.md`:

> "It is not a measured US price, a **validated** threshold change or permission to promote that policy."

This is a NEGATING sentence — the literal `validated` is being explicitly denied ("not a validated threshold change"). The negation is the correct discipline for the research README: the reproducer proves a discriminator exists but does NOT promote it. The same sentence denies `permission to promote that policy`, so the negated claim is structurally pinned against any future promotion that lacks the versioned same-cohort comparison the body requires.

1. **No `validated` / `certified` / `approved` / `production-grade` / `production-ready` / `GA` strings in user-visible positions.** Verified via `grep -nE "validated|certified|approved|production-grade|production_ready|GA\b|generally.available" <diff>`:
   - `templates/` and `site/` — zero matches.
   - `engine/sector_pulse.py` — zero matches.
   - `tests/` — zero matches.
   - `.github/` CI files — zero matches.
   - `agentos/handoffs/PROPHET-US-V4-RECOVERY-2026-09-21-leadership-observation-clock.md` — zero matches (the workstream name contains `PROPHET`, not `validated`).
   - `research/sector_pulse/observation_clock_20260921/README.md` — one match, NEGATING ("not a validated threshold change or permission to promote that policy").
   - `research/sector_pulse/observation_clock_20260921/real_source_proof.json` — zero matches.
   - `research/sector_pulse/observation_clock_20260921/prove_observation_clock.py` — zero matches (docstring: "is an observation-clock proof, not a point-in-time availability or performance study" — a NEGATING docstring; the proof's own JSON `limitations` array contains the four explicit non-claims).
   - `research/sector_pulse/observation_clock_20260921/verification.txt` — zero matches.
2. **The "MISSON_COMPLETE: false" / "BUILT_NOT_PROVEN" framing is honest.** The handoff's `do_not_redo` list names three forbidden moves: "Do not recreate a ThemeState, ranker, live leader feed, program or publication authority"; "Do not freeze the old semiconductor +10 one-day rank change; it compared against the wrong date"; "Do not call this clock repair live Prophet recovery or change sizing/entry/rank policy by narrative". `danger_areas` names four incumbent owners that retain write custody (`#7520`, `#7572`, `#7455`, `#7508`). The `unverified` list names exactly two items: "Exact-head CI, independent review and authorized production/browser acceptance" and "Broader Python 3.12 dashboard suite (plotly absent)" — both with explicit `what_would_verify`.
3. **The "Authority fields unchanged" assertion is verified by SHA-256-bound reproducer.** `real_source_proof.json` carries the SHA-256 of `engine/sector_pulse.py@2b62f496`, `site/basketdata/baskets.json@2b62f496`, `data/signal_archive/baskets.parquet@2b62f496`, and `969a94a79d1f2713e8104fa532b73557e0ab0e2c7492b8773f916f374f94cc9e` (the candidate engine bytes). The reproducer compares `[(r.get("id"), r.get("rank"), r.get("score"), r.get("label"), r.get("reco")) for r in before["themes"]]` against the same projection for `after` and asserts byte-equality. This is a code-path claim, not a user-facing claim — and the reproducer's `limitations` array correctly disclaims what the projection does NOT prove.
4. **No `premium` / `certified` / `guaranteed` markers.** Search returns zero matches.
5. **No "production" / "live" claim in user-facing positions.** The handoff explicitly carries `MISSION_COMPLETE: false` and `CAPABILITY_STATE: BUILT_NOT_PROVEN` — exactly the discipline required when the engine seam is corrected but live proof is owed. The closing line of `real_source_proof.json`'s `limitations` array denies live evidence, forward-return claim, and promotion.
6. **The "79 passed" / "118 passed" / "138 passed" / "154 passed" / "16 failed / 8 passed" test counts are precise and scoped.** Each count names the exact suite(s) and interpreter (Py 3.14 vs Py 3.12); the Py 3.12 dashboard-owner gap is honestly named ("that interpreter lacks `plotly`; no dependency or test was disabled"). No "all tests pass" or "100% green" without enumeration.
7. **The corrected test fixture's previous defect is honestly named.** The README states: "a pre-existing test fixture actually repeated one date six times and called it six sessions". The fix is documented as "corrected to six distinct trading dates without removing or weakening any assertion" — the same precision the standing house law requires for fixture repairs.

## Overall verdict

**PASS — Plain-language / theme / validated-claims laws all clean for PR #7650.**

- **Plain-language: PASS.** Zero new user-visible copy. The diff is engine + test + research + CI only; no template, no CSS, no i18n key, no `title=` attribute, no chip/class binding touched. The pre-existing `Sector theme:` pill and `Sector-pulse heat tier` help popover read identical to before; the engine still emits `heating`/`hot`/`cooling`/`broken`. Zero banned-vocab state enums; zero raw study slugs / internal tokens in user-visible positions; ZH parity preserved by construction. The new `history` payload field is additive and **not yet rendered by any template** (verified by `grep`); its absence on the page is structural, not a null-placeholder leak.
- **Theme: PASS.** Zero CSS / SCSS / Jinja / token rebases. No `@media (prefers-color-scheme: light)`; no `data-theme` selector; no `var(--…)` / `--…:` lines; no direction-color literals; no mobile breakpoint change. The TP-0 "dark = command center, light = research workspace" discipline is unaffected. The CI change is a `paths:` allowlist extension to an existing job — no gate relaxed, no existing check removed.
- **Validated-claims: PASS.** Zero `validated` / `certified` / `approved` / `production-grade` / `production-ready` / `GA` literals on any user-visible path or any non-`research/` path. The single `validated` hit on the diff is a NEGATING sentence in `research/sector_pulse/observation_clock_20260921/README.md` ("not a validated threshold change or permission to promote that policy"). The reproducer's `limitations` array carries four explicit non-claims. The handoff ships with `MISSION_COMPLETE: false` and `CAPABILITY_STATE: BUILT_NOT_PROVEN`, the `do_not_redo` list pins three forbidden moves, and `danger_areas` names four incumbent owners whose write custody is preserved. The SHA-256-bound reproducer proves the canonical recommendation authority (`id`/`rank`/`score`/`label`/`reco`) is byte-identical between old and new projections across all 49 themes — a code-path claim, not a user-facing claim, and explicitly bounded by the `limitations` array.
- **Scope discipline: PASS.** PR body is explicit that this is a shared-producer source-integrity repair, not a ranking / sizing / entry / allocation / deployment change. Existing carriers retain ownership (`#7520` hottest-desk, `#7572` Prophet visibility, `#7455` early-subtheme, `#7508` entry, `#7453` independent acceptance, `#7211` publication). No theme structure, ranker, score, publication plane, recommendation authority, or generated site file was modified. The CI change adds two test paths + one source path to an existing job's trigger list and to the existing `ci-pack-3`-aligned job in `legacy-jobs.yml`; no gate relaxed.
- **Collision control: PASS.** Integration-boundary comment `5768028874` on `#7520` carries the source-corrected `+10 → −1` finding and is named in the body as a non-acceptance-fixture. The reproducer binds three immutable Git blobs + the candidate engine by SHA-256, so the engine-vs-source correlation is reproducible offline (no live data dependency).
- **One standing note for the next audit round.** This PR does NOT add a template consumer for `history.{basis, comparison_as_of, expected_comparison_as_of}`. When the dashboard or ticker eventually renders this payload (the natural next wave, since "what date did we actually compare against" is exactly the user question that motivated the fix), the next audit must (a) confirm the rendered copy uses plain words ("Compared against 11 Sept 2026", not `2026-09-11` or `comparison_as_of`); (b) confirm EN/ZH parity; (c) confirm the rendered copy is honest about the `expected_comparison_as_of` vs `comparison_as_of` distinction ("the comparison date we actually used" vs "the comparison date we expected to use") so a missing-session state doesn't silently look like an aligned five-day window. Not a #7650 finding — a forward audit cadence note.

`SESSION END: PROVEN_OUTCOME` — audit recorded at `orch/audits/macro_PR-7650.mm.md`; no remote durable writes, no PR opened, no other lane touched.
