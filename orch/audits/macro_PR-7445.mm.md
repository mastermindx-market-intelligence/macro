# Audit — mastermindx-market-intelligence/macro PR #7445

Mode: qwen_auditor2-style (plain-language / theme / validated-claims laws). One-pass audit against the merged head. No durable writes outside this report.

## PR metadata

| field | value |
| --- | --- |
| repo | `mastermindx-market-intelligence/macro` |
| PR | [#7445](https://github.com/mastermindx-market-intelligence/macro/pull/7445) |
| title | `fix(policy-watch): simplify confidence and track-record UX` |
| merged | 2026-09-20T04:39:58Z via squash-merge to `main`. This is the most-recent unaudited **half-B** user-facing UX-fix PR in the 24-h window. Prior un-audited candidates that are NOT half-B UX: orch(audit) records #7543/#7541/#7540/#7538/#7537/#7536/#7529/#7525/#7519/#7516/#7486/#7476/#7470/#7468 are pure audit-recording commits (NOT a half-B subject); [MO-A] main-red-repair #7523 is MO-A lane (NOT half-B); [MO-B F00C records] #7465 / #7447 / #7390 are records passes (NOT half-B UX); the [HOLD] #7475 is a Sol-hold research freeze (records-only, NOT user-facing UX); #7471 is a single DEC record (NOT half-B UX); #7467 (already audited); #7463 Fix China rerender is a build-pipeline fix (NOT half-B UX); #7424 / #7422 / #7428 / #7429 / #7432 / #7433 / #7434 / #7435 / #7439 / #7440 / #7444 / #7446 / #7454 / #7460 / #7468 / #7470 / #7476 / #7486 / #7516 / #7519 / #7525 / #7529 / #7536 / #7537 / #7538 / #7540 / #7541 / #7543 / #7544 / #7545 are audit-recording commits. |
| head | squash of branch `sol/uiux-policy-watch-sweep-20260919`, head ref `eb9e419623e8bfaa848e339a1ab07be13551f61f`, merge commit `ad00e63eb8130d40bae28f5a964f6d54db2c5348`. |
| author / merger | PR authored on `sol/uiux-policy-watch-sweep-20260919`; merger = repo auto-merge (squash). |
| base | `origin/main` at the time of merge. |
| files | **4 files, +119 / −33 net** (1 `.github/ci/legacy-jobs.yml` pack include adding `tests/test_policy_watch_glance_copy.py`; 1 paired `site/policy_watch.html` mirror; 1 source template `templates/policy_watch.html.j2`; 1 new glance-copy test file `tests/test_policy_watch_glance_copy.py`). |
| scope | Three-part presentation cleanup: (1) **confidence labels** — replace ambiguous bare `High / Medium / Low confidence` chips with explicit `Analysis confidence: high / medium / low` wording; (2) **review-date label** — change `Review by` → `Next review`; (3) **track-record exclusion wording** — translate `void` ledger outcomes from internal `Not scored` language to `Excluded` / `Excluded from track record` without changing the underlying `void` state, and (4) **featured-row call IDs** — remove internal `Pxx` call IDs from the six featured track-record rows on the chrome while retaining them in the expanded `See all calls` audit ledger for traceability. **All underlying data, JSON, status codes, conviction value, featured-call selection logic, and result classification are unchanged.** PR body §"Scope / non-goals" declares the PR presentation-only. |
| half-B label | **half-B**. This is a half-B user-facing UX-fix PR on the Policy Watch chrome (a B-tier user-facing page). It is a **vocabulary + label rename** PR: zero engine/ledger/authority changes, zero new CSS/JS, zero new tokens, zero new stat strings, zero new tier claims. The new `result_copy(call)` Jinja macro is the only logic addition and is presentation-only (string substitution, no state change). |
| pre-existing release truth | PR body §"Validation" reports 121 passed / 4 deselected in the scoped suite (4 deselected FOMC-history assertions were separately proven to fail on untouched `origin/main`); focused post-rebase check 16/16 passed; Chromium proof at 1440×1000 and 390×844: HTTP 200, zero horizontal overflow, zero JS page errors; browser proof: 6 featured calls contain zero `.pw-call-id` elements; expanded audit ledger retains 44 IDs; `scripts/build_policy_watch.py` compiles; `git diff --check` clean. The PR body's "Existing baseline issues observed" section is honest about the 4 pre-existing FOMC-history assertion failures and the data-write side effect on `data/policy_lifecycle/events.jsonl` (restored to HEAD, not in PR). |
| live proof | Template/site byte-sync verified (`scripts/check_template_site_sync` exit 0, 99 pairs OK). Runtime style injection guard exit 0 (195 .js files scanned, 44 injecting, 89 hits, all within frozen allowances). The PR does not touch `.js` files. Validated-claims guard (`scripts/check_validated_claims.py`) returns 38 PRE-EXISTING violations across `templates/_macro_suite_shell.html.j2`, `templates/canada.html.j2`, `templates/hk.html.j2`, 14× macro suite pages, `templates/macro_suite.js`, `templates/mm_brain.js`, `site/macro_suite.js`, `site/mm_brain.js`, 14× site suite pages, and `engine/market_os/macro_workspaces/consumer.py:106`. **None** of these 38 violations are in `templates/policy_watch.html.j2` or `site/policy_watch.html`. The PR introduces zero new validated-claims violations on the touched files. Title-i18n guard (`scripts/check_title_i18n.py`) reports a pre-existing violation in `templates/am_edition.html.j2:13` (`<title>{{ t('Morning Edition', '早间版') }} — {{ as_of }}</title>`); the policy_watch `<title>` is unchanged from its prior state and is not touched by this PR. |

## Diff content (exact, scoped to this PR)

**`templates/policy_watch.html.j2` (+16 / −7)** — three text renames and one new presentation-only Jinja macro:

- `confidence_tag(value)` macro: `High confidence` → `Analysis confidence: high`; `Medium confidence` → `Analysis confidence: medium`; `Low confidence` → `Analysis confidence: low`. ZH: `较高把握` → `分析把握：较高`; `中等把握` → `分析把握：中等`; `较低把握` → `分析把握：较低`. The macro logic (the `s.startswith('high/medium/low')` dispatch) is unchanged.
- `lean_tag(value)` macro (status badge): `void` status → `<span class="pw-badge neutral">{{ t('Excluded','不纳入记录') }}</span>` (was `Not scored / 不计分`). The status value `void` and the badge CSS class `neutral` are unchanged.
- **NEW** `result_copy(call)` macro (5 lines): a presentation-only Jinja macro that emits the call's bilingual result copy, replacing the internal `Not scored:` prefix with `Excluded from track record:` when `call.status == 'void'`. Pure string substitution (`en|replace('Not scored:', 'Excluded from track record:')`, `zh|replace('不计分：', '不纳入记录：')`); no state change, no semantic drift.
- Featured-card meta-row: `{{ t('Review by','复核日期') }}` → `{{ t('Next review','下次复核') }}`. The `{{ view.check_by }}` date literal is unchanged.
- Featured-card call rendering: `<span class="pw-call-topic">{{ bl(topic[0],topic[1]) }}</span> <span class="pw-call-id">{{ call.id }}</span>` → `<span class="pw-call-topic">{{ bl(topic[0],topic[1]) }}</span>`. The `<span class="pw-call-id">` is **removed from featured rows**.
- Featured-card result rendering: `Result: {{ bl(call.result_en,call.result_zh) }}` → `Result: {{ result_copy(call) }}` (uses the new macro).
- Featured-card stats: `not scored` (lowercase stat label) → `excluded`. ZH: `不计分` → `不纳入记录`.
- The full-row `data-policy-id="full-P43"` rendering keeps `<span class="pw-call-id">P43</span>` and `<span class="pw-call-id">P44</span>` (and the other 42 call IDs) byte-identical, with only the result-text macro substituted to `result_copy(call)`. The expanded audit ledger retains all 44 call IDs; only the 6 featured chrome rows lose the visible ID badge.

**`site/policy_watch.html` (+25 / −25)** — paired mirror; every renamed string is renamed in BOTH locales together. The 6 featured `pw-call-id` chips are removed; the 2 expanded-ledger `pw-call-id` chips (P43, P44 — the `void` rows) are preserved on the page (because the audit ledger row retains the `data-policy-id="full-Pxx"` and the `pw-call-id` chip). The diff shows `pw-call-id` surviving in the expanded `data-policy-id="full-P43"` / `data-policy-id="full-P44"` article lines (the `+` versions still carry `<span class="pw-call-id">P43</span>` and `<span class="pw-call-id">P44</span>`).

**`tests/test_policy_watch_glance_copy.py` (NEW, +77 / 0)** — single regression test asserting:
- `High confidence` not in template (chromium-pin: bare `High confidence` chip is gone)
- `Medium confidence` not in template
- `Low confidence` not in template
- `Analysis confidence: high` IS in template
- `Analysis confidence: medium` IS in template
- `Analysis confidence: low` IS in template
- `分析把握：较高` IS in template (ZH parity check)

The test file is the **glance-tier guard** for the three confidence labels. It pins the rename in BOTH locales and prevents regression to the bare `High/Medium/Low confidence` chips that the PR explicitly removes.

**`.github/ci/legacy-jobs.yml` (+1 / −1)** — pack include line adds `tests/test_policy_watch_glance_copy.py` to the existing falsifier-register + policy-watch + chat plain-word projection pack run; no new CI job is created.

## Plain-language findings

The macro repo has no `scripts/check_plain_language.mjs` (terminal-side only; verified `find . -name "check_plain_language*"` returns no matches in either repo). Plain-language discipline on macro user-facing strings is read directly against `docs/DESIGN_DOCTRINE.md` + `research/MASTER_PRODUCT_DESIGN_SYSTEM_V1.md` + the CI guard `scripts/check_i18n_no_title_attr.py` for the bilingual UI law + the design-system banned-vocab list (per `DESIGN_DOCTRINE.md` glance-tier: no internal state names, no untranslated stats, no raw slugs; technicals demoted to hover/popover; every signal panel answers "so what do I do"; plain-word null disclosure + Tier-2 receipt is the compliant "nulls printed" form).

**Verdict: PASS — clean glance-tier rephrasing; the new wording names the subject (`Analysis confidence`), clarifies the date's purpose (`Next review`), and translates the internal `void` state into reader-language (`Excluded` / `Excluded from track record`). Zero new banned-vocab, zero new tier-bumping stats, zero new technicalities bleeding into chrome tier.**

1. **`High / Medium / Low confidence` → `Analysis confidence: high / medium / low` is a clear glance-tier WIN.** The bare `High confidence` chip could read as a forecast conviction ("we are 90% sure the call is right") rather than the analysis confidence that it actually measures — the producer's `conviction` field is the analyst's confidence in the reading, not a calibrated probability of the call being correct. Adding the subject (`Analysis confidence:`) names WHAT is high/medium/low — closing the false-precision door that a bare `High confidence` chip left open. ZH: `分析把握` literally is "analysis confidence" (`分析` = analysis, `把握` = confidence/grasp), so the ZH rename is the same doctrinal fix. The underlying `conviction` value, the dispatch logic (`s.startswith('high/medium/low')`), and the badge CSS class are all unchanged. **Zero new banned-vocab** — the rename does not introduce any tier-2 vocabulary on the chrome; it names the existing metric.
2. **`Review by <date>` → `Next review <date>` is a one-phrase plain-language WIN.** "Review by 2026-10-23" reads as "we will have completed the review BY 2026-10-23" (a deadline for the producer), which is the producer-side framing, not the user-side framing. "Next review 2026-10-23" reads correctly as "the next time we will re-examine this view is 2026-10-23" — the user-side framing. ZH `复核日期` → `下次复核` carries the same fix (`复核日期` = "review-date" / generic noun; `下次复核` = "next-review" / future-action). The date literal is unchanged; only the surrounding phrase changes. No semantic drift: per PR body §"Scope / non-goals" the `view.check_by` data field is untouched.
3. **`Not scored` → `Excluded` / `Excluded from track record` is a plain-language WIN for the reader.** `Not scored` reads as "the result wasn't calculated" — a passive framing that does not say whether the call counts. `Excluded` and `Excluded from track record` actively tell the reader "this call does not appear in the track record because of X". The full result-text variant `Not scored: <reason>` becomes `Excluded from track record: <reason>` — the prefix tells the reader the consequence (excluded from track record) and the suffix keeps the existing reason text byte-identical (the `eSLR reform in November 2025` / `ceasefire premise broke` / `entered the track record` reasons are unchanged). ZH: `不计分` → `不纳入记录` — same fix. The `void` ledger state and the `neutral` CSS badge class are unchanged; this is a presentation-layer translation only.
4. **Removing `<span class="pw-call-id">Pxx</span>` from the 6 featured track-record rows is chrome hygiene, NOT data loss.** The featured chrome rows previously carried the internal `Pxx` identifier inline (`<span class="pw-call-topic">Rates</span> <span class="pw-call-id">P40</span>`); the new featured chrome renders only the topic (`<span class="pw-call-topic">Rates</span>`). The `data-policy-id="{{ call.id }}"` attribute on the `<article>` is preserved byte-identical (it still carries `P40` etc. as the data attribute), so any audit or downstream hook that read the call ID via `data-policy-id` keeps working. The expanded audit ledger (`See all calls` `<details>`) retains all 44 visible `pw-call-id` chips. The PR body §"Validation" reports: "browser proof: 6 featured calls contain zero `.pw-call-id` elements; expanded audit ledger retains 44 IDs". Net: chrome loses 6 internal IDs, audit ledger gains nothing new, `data-policy-id` attribute is unchanged. **This is exactly the Tier-1/Tier-2 split the design doctrine prescribes** — the chrome (glance-tier) does not display internal IDs; the audit ledger (technical-tier) does.
5. **The new `result_copy(call)` macro is presentation-only.** 5 lines: `{% set en = call.result_en or '' %}; {% set zh = call.result_zh or '' %}; {% if call.status == 'void' %}{{ bl(en|replace('Not scored:', 'Excluded from track record:'), zh|replace('不计分：', '不纳入记录：')) }}{% else %}{{ bl(en, zh) }}{% endif %}`. Pure Jinja string substitution; the `void` status check is the same boolean already used by `status_tag(value)` to render the `Excluded` badge. No new logic; the macro just moves the `void`-specific rewrite into one place. The pre-PR code rendered the result copy inline via `{{ bl(call.result_en, call.result_zh) }}` for ALL statuses; the post-PR code routes through `result_copy(call)` which substitutes the prefix only when `status == 'void'`. **Zero new state machine code; zero new ledger mechanics.**
6. **Zero new tier-bumping stats or untranslated stats on the rendered surface.** The four featured-call stats (`<strong>{{ counts.open }}</strong> open`, `right`, `wrong`, `excluded`) are unchanged in structure; only the `not scored` stat label becomes `excluded`. The conviction value, the lean classification, the status code, the result copy, and the source attribution are all preserved.
7. **Native `t()` bilingual UI law preserved.** All four renamed strings are wrapped in `{{ t(en, zh) }}` Jinja macro invocations: `t('Analysis confidence: high','分析把握：较高')`, `t('Next review','下次复核')`, `t('Excluded','不纳入记录')`, `t('excluded','不纳入记录')`. The ZH strings are short, well-formed, and not concatenated into `<option>` elements (which would not render correctly — see #7450 audit item 8 for the standing native-`<option>` rule; this PR does not touch any `<option>` elements).
8. **No new `title=` attributes, no new `<h1>` / `<h2>` body copy, no new modal/dialog text strings.** The rename touches existing text nodes inside `<span class="pw-meta">`, `<span class="pw-call-topic">`, and `<span class="pw-stat">` wrappers — not headings, not title attributes, not modals. The `<title>{{ t('Policy Watch — what moves the tape', ...) }}</title>` of the page is not touched by this PR (and the title-i18n pre-existing violation in `templates/am_edition.html.j2:13` is a separate concern).
9. **No new copy bleeding into chrome tier.** The featured-card meta-row `Next review <date> · Analysis confidence: <level>` reads at glance-tier — subject named (the review's purpose, the confidence's source), value readable, no jargon, no internal verb. The featured-call row `Rates: <text> · Result: <reason> · Source` reads at glance-tier — `Result` is the standard chrome label, `Source` is the standard chrome link, both unchanged. The status badge `Excluded` reads at glance-tier — the badge's full meaning is "this call is excluded from the track record" (per the result-text prefix), and the badge itself reads as the consequence.
10. **Zero new `<style>` blocks, zero new CSS rules, zero new tokens.** The PR touches 4 files: 1 CI YAML line, 1 template (text renames + 5-line macro), 1 paired site mirror (text renames), 1 new test file. No CSS or `.js` files are touched. The PR body's "Scope / non-goals" is explicit: "No prediction status, confidence value, policy data, result code, featured-call selection, market-view logic, FOMC state machine, scoring, or source authority changed."

Net new tier-bumping / banned-vocab occurrences on user-visible strings: **0**. Net new ZH/EN parity breaks: **0** (every renamed string is renamed in BOTH locales together). Net new technicalities bleeding into chrome tier: **0**. Net renames that improve gloss over controls: **3 of 3** (`Analysis confidence:` subject naming, `Next review` date-purpose naming, `Excluded from track record` consequence naming). Net new ledger-mechanics code: **0** (the new `result_copy(call)` macro is string substitution only).

## Theme findings

TP-0 art-direction law in force (operator 2026-08-27): dark and light are TWO art directions, not one skin. The law applies to product CSS, design-system tokens, material surface, runtime style injection, and the canonical evidence matrix. PR #7445 is a half-B user-facing UX-fix that touches **ZERO `.css` files, ZERO `.js` files, ZERO theme tokens, ZERO colour literals, ZERO runtime style injection**. The diff is text renames + 5-line Jinja macro + 1 new test file + 1 CI YAML line.

**Verdict: PASS — no new theme debt; no new colour literals; no new tokens; no new CSS; no new JS; no runtime style injection; runtime bytes match between template and paired `site/`.**

1. **Zero new colour literals.** The PR touches no `.css` files. The four text renames use existing CSS classes (`pw-meta`, `pw-badge neutral`, `pw-chip`, `pw-call-topic`, `pw-call-id`, `pw-stat`) which are pre-existing classes on the policy_watch stylesheet. No new colour, no new background, no new border treatment is introduced.
2. **Zero new tokens.** The PR does not edit `theme.css` or any CSS file. The CSS classes referenced by the renamed strings (`pw-meta`, `pw-badge neutral`, `pw-chip`, `pw-call-topic`, `pw-call-id`, `pw-stat`) already exist and their colour, padding, border, and typography treatments are byte-identical pre/post merge.
3. **No runtime style injection.** `scripts/check_runtime_style_injection.py` passes globally with "195 .js files scanned, 44 injecting, 89 total hits — all within frozen allowances". The PR does NOT touch any `.js` page/composer file. The 44 injecting files are pre-existing pages with stateful style mutations; none are policy_watch page/composer files. The PR's surface is purely Jinja template text + paired `site/` mirror text + 1 new test file.
4. **No new animation / motion.** The PR introduces no `@keyframes`, no new `transition`, no new `transform`. The four renamed text nodes render with the same existing CSS rules and the same existing motion behaviour.
5. **No new typography.** The PR does not introduce any new font-family, font-size, letter-spacing, line-height, or weight. The renamed strings inherit the existing `pw-meta`, `pw-call-topic`, `pw-stat`, `pw-chip`, `pw-badge neutral` typography ladders.
6. **No new dark/light art-direction debt.** The PR does not introduce any dark-theme or light-theme treatment. The text renames are language-agnostic (same string in EN and ZH, same CSS class); both themes render the new copy identically because the CSS classes are theme-agnostic (the dark/light split is owned by `theme.css` and the existing CSS variables). Per `MASTER_PRODUCT_DESIGN_SYSTEM_V1.md`, "dark and light share information architecture, component semantics, spacing/type scales, state meanings, user actions, data contracts, ordering/density law and interaction behavior" — the rename is a copy rename that respects this law (same IA, same component semantics, same state meaning: `void` → `Excluded`, same data contract, same ordering).
7. **No 8-cell evidence matrix required.** Per TP-0, "every material UI packet must carry dark/light × EN/ZH × desktop 1440 / mobile 390 evidence". The PR is a TEXT RENAME on existing chrome — it is NOT a material UI packet (no new material, no new layout, no new component, no new design treatment). The pre-existing policy_watch 8-cell matrix (built and captured by the prior PR #7391 fix(flow-leaders)/#7388/#7346 batch) remains valid because the rendered output's layout, typography, colour, and responsive behaviour are byte-identical pre/post merge — only the visible string content changes. Per `MASTER_PRODUCT_DESIGN_SYSTEM_V1.md` §"Material UI packet" definition: a packet is a NEW layout/component/visual treatment — a copy rename on an existing rendered surface is below the packet threshold and inherits the prior matrix's coverage. The PR body's "Chromium proof at 1440×1000 and 390×844: HTTP 200, zero horizontal overflow, zero JS page errors" is the receipt for the existing matrix still holding.
8. **Animation/motion law preserved.** The PR does not introduce any new animation, transition, or perpetual motion. The four renamed strings inherit the existing CSS classes' static styling.
9. **Token-only law preserved.** No new font-family, no new font-size ladder, no new `letter-spacing` values. The existing CSS classes carry the canonical typography and the rename preserves it.
10. **No `<style>` block addition.** The PR touches no `.css` files and no inline `<style>` blocks. The template's existing inline `<style>` (if any) is not touched.
11. **The `pw-call-id` class CSS rules are unchanged.** The class still exists in the stylesheet (because the expanded audit ledger rows still use it); only the chrome uses it in fewer places. No CSS rule is orphaned; no CSS rule is dead. The CSS file does not need to be edited.

Net new theme debt: **0**. Net new colour literals: **0**. Net new tokens: **0**. New CSS rules: **0**. New animations: **0**. New typography: **0**. Runtime style injection: **0**. Evidence matrix requirement: **inherits pre-existing 8-cell coverage (no new material introduced)**.

## Validated-claims findings

The standing macro law: the word "validated" (and friends: `verified`, `proofed`, `certified`, `approved`, `gauntlet`, etc.) is CI-enforced via `scripts/check_validated_claims.py`; context/data/detection/tagging artifacts stay display-tier until they clear the gauntlet; the word "validated" in user-facing text is CI-enforced (CLAUDE.md §Epistemics gauntlet = PROMOTION gate). The macro repo currently has **38 pre-existing validated-claims violations** that exist on `origin/main` and are NOT introduced by this PR; the audit enumerates them for completeness but does not flag them as PR defects.

**Verdict: PASS — the PR introduces ZERO new tier-asserting nouns on user-visible strings. All `verified` / `validated` mentions in the touched files are pre-existing receipt-grade / lineage-grade copy unchanged by this PR.**

1. **No new tier-asserting noun introduced by the PR diff.** The diff hunks contain zero `validated`, `verified`, `certified`, `approved`, `proofed`, `gauntlet`, `proven` strings. The four renamed strings (`Analysis confidence:`, `Next review`, `Excluded`, `Excluded from track record`) name the SUBJECT (what is being measured) and the CONSEQUENCE (excluded from track record), NOT a tier-assertion about the artifact's authoritative clearance.
2. **Pre-existing `verified` / `Unproven market views` strings on the touched files are UNCHANGED.** `grep -E "validated|verified|certified|approved|gauntlet|proven" templates/policy_watch.html.j2 site/policy_watch.html` returns 5 hits, ALL pre-existing:
   - `templates/policy_watch.html.j2` line `<p class="pw-mini-copy">{{ t('Last verified','最近核实') }} <span class="l-en">{{ verified_en }}</span>...</p>` — pre-existing chrome copy describing the build-clock ("Last verified <date>").
   - `templates/policy_watch.html.j2` line `<p class="pw-build-note">{{ t('Unproven market views—not trade instructions.','这些观点尚未得到验证，不是交易指令。') }}</p>` — pre-existing chrome copy explicitly negating tier assertion ("Unproven market views").
   - `templates/policy_watch.html.j2` line `{% for lever in intel.administration.verified_levers %}` × 2 — pre-existing Jinja loop over a producer-side data field named `verified_levers` (the producer's classification of which levers are verified vs. unverified). The producer field name is unchanged.
   - `templates/policy_watch.html.j2` line `<p class="pw-build-note">{{ t('Last verified','最近核实') }} <span class="l-en">{{ verified_en }}</span><span class="l-zh">{{ verified_zh }}</span> · {{ t('Built','构建于') }} {{ generated_utc }} · {{ t('For information only—not investment advice.','仅供参考，不构成投资建议。') }}</p>` — pre-existing build-clock copy describing the verification timestamp.
   - `site/policy_watch.html` (paired mirror) — same 4 occurrences as the template.
   **NONE of these 5 strings are modified by PR #7445.** The audit verifies each is byte-identical in the `git diff` output (none of the diff hunks touch these lines).
3. **`Analysis confidence: high/medium/low` is a CONFIDENCE LABEL, not a tier assertion.** Per CLAUDE.md §Epistemics: "validated" is CI-enforced against tier-2 acceptance; this copy names the analysis's confidence, not the artifact's authoritative TIER. The underlying `conviction` value is the analyst's confidence in the reading (a display-tier signal), and the rename clarifies WHAT is being measured — adding the subject closes the false-precision door. Compare to a tier assertion like "validated against tier-2 acceptance" (which would be CI-enforced as a `validated:` claim) — `Analysis confidence: high` makes no such claim.
4. **`Excluded from track record: <reason>` is a CONSEQUENCE statement, not a tier assertion.** The copy says: this call does not appear in the track record because of X. It does not say: this call is excluded because it has been judged insufficiently validated. The `void` state is a ledger-mechanics outcome (the call didn't have a clean run, the premise broke, etc.), not an authority-tier judgment. Per CLAUDE.md §Epistemics: a factor that is null as a *standalone* signal is **retained as a confluence input** (it may confirm other signals when they align) — the `void` outcome does not gate or rank the call; it merely drops it from the track record (denominator of the right/wrong ratio).
5. **`Next review` is a TIME label, not a tier assertion.** It names the next time the view will be re-examined. No tier, no authority, no gauntlet claim.
6. **Removing the `<span class="pw-call-id">Pxx</span>` from featured chrome is data preservation, not authority change.** The `data-policy-id="{{ call.id }}"` attribute is unchanged (still carries the call ID for any audit/downstream hook). The expanded audit ledger retains the visible ID badge. Per CLAUDE.md §"A7 (signal origination)": the LLM never originates signals/scores; this PR's surface is presentation + label hygiene. The underlying `P40`/`P41`/`P43`/`P44`/`P6`/`P16` ledger entries, their result codes, their conviction values, and their source attributions are all unchanged.
7. **No gauntlet / authority / rank / score / size / gate promotion.** The PR does NOT promote any artifact to authority. It does NOT touch the Risk Radar, the Calibration Lab, the rendered China-side panel, the live engine's data path, the producer code, the storage path, the live lane, the calibration proposals, the unified dashboard, the flow_leaders, the prophet pipeline, or the macro_context page. It is presentation-only on Policy Watch chrome + label hygiene on featured track-record rows.
8. **The 38 PRE-EXISTING validated-claims violations are out of scope for this PR.** They exist on `origin/main` and are NOT introduced by PR #7445 (verified by reading the diff hunks — the PR touches only `policy_watch.html.j2`, `policy_watch.html`, `legacy-jobs.yml`, and the new test file; NONE of these are in the 38 violation list). The audit enumerates them for completeness:
   - `templates/_macro_suite_shell.html.j2:17` — pre-existing `one validated` phrasing (phrase matches allowlist but `surfaces` not extended).
   - `templates/_macro_suite_shell.html.j2:798` — pre-existing `mq-lineage-note` "validated this artifact against the closed schema".
   - `templates/canada.html.j2:2309` — pre-existing `validated owner board` Jinja comment.
   - `templates/hk.html.j2:3528,3655` — pre-existing `validated identities` / `validated owner` Jinja comments.
   - 14× `templates/macro_*.html.j2:6-8` — pre-existing `validated <snapshot>` comment.
   - `templates/macro_suite.js:4`, `templates/mm_brain.js:3629` — pre-existing `hash-validated snapshot` / `already validated server-side` JS comments.
   - `site/macro_suite.js:4`, `site/mm_brain.js:3629` — paired mirrors.
   - 14× `site/macro_*.html:1748-3654` — paired `mq-lineage-note` mirrors.
   - `engine/market_os/macro_workspaces/consumer.py:106` — pre-existing `[detail] snapshot validated against mastermind.macro_workspace_snapshot.v1`.
   These are a separate concern for the macro suite page owners (per `scripts/check_validated_claims.py` allowlist extension workflow). The audit does not flag them as PR #7445 defects.
9. **The title-i18n pre-existing violation is also out of scope.** `templates/am_edition.html.j2:13` has `t('Morning Edition', '早间版')` in a `<title>` element — pre-existing on `origin/main`. PR #7445 does not touch `am_edition.html.j2` or any `<title>` element. The policy_watch `<title>` is unchanged.
10. **The "121 passed / 4 deselected" disclosure is honest.** PR body §"Existing baseline issues observed" reports: "Four FOMC-history assertions in `tests/test_policy_watch_ui.py` currently fail on untouched main because the committed FOMC ledger has advanced beyond their July-29 expectations / record-selection assumptions. This PR does not change the builder, FOMC data, or those tests." The PR does NOT silently repair the 4 failures with invented data; the deselections are the truth. The "Policy Watch test suite also mutates `data/policy_lifecycle/events.jsonl`, which trips the repo data-write guard after otherwise-green assertions. The side effect was restored to HEAD and is not in this PR" admission is the OPPOSITE of a "validated" framing — it is the seat telling the reviewer exactly which assertions are not honored against current main and why.
11. **No A7 (signal origination) claim is made.** The LLM never originates signals/scores (A7); this PR's surface is presentation + label hygiene. The underlying producer-side `verified_levers` data field, the conviction value, the featured-call selection logic, the lean classification, the status code, and the result classification are all unchanged.

Net new MISS: **0**. Net new tier-asserting noun on user-visible strings: **0**. Net new authority claim: **0**. Net new gauntlet/promotion: **0**. Pre-existing baseline violations: **38 validated-claims + 1 title-i18n** — all in files NOT touched by this PR.

## Overall verdict

**PASS WITH NO FAILS — clean half-B chrome label-rename + featured-row call-ID hygiene on Policy Watch; presentation-only with no engine/signal/score changes; zero new tier-asserting nouns on user-visible strings; zero new CSS / JS / tokens / colour literals / runtime style injection; pre-existing validated-claims and title-i18n baseline violations are in untouched files and are NOT PR #7445 defects; runtime bytes match between template and paired `site/` (`scripts/check_template_site_sync` exit 0); runtime style injection guard exit 0; the new glance-copy test file pins the rename in both locales.**

Summary table:

| dimension | verdict | reason |
| --- | --- | --- |
| plain-language | PASS | 3 of 3 chrome renames are plain-language wins: `High/Medium/Low confidence` → `Analysis confidence: high/medium/low` names the subject (closing the false-precision door where bare `High confidence` could read as forecast conviction); `Review by <date>` → `Next review <date>` clarifies the date's purpose (user-side future action, not producer-side deadline); `Not scored` → `Excluded` / `Excluded from track record` translates the internal `void` state into reader-language (the consequence: this call does not count). The 6 featured-row `pw-call-id` chips are removed (chrome hygiene — internal IDs belong in audit ledger, not glance-tier chrome); the `data-policy-id` attribute is preserved byte-identical for any downstream hook; the expanded audit ledger retains all 44 visible IDs. The new `result_copy(call)` Jinja macro is 5 lines of pure string substitution (`Not scored:` → `Excluded from track record:` for `void` rows) — no state machine change, no ledger mechanics. Zero new banned-vocab, zero new tier-bumping stats, zero new technicalities bleeding into chrome tier. |
| theme | PASS | Zero new CSS / JS / tokens / colour literals / runtime style injection. The PR touches no `.css` files, no `.js` files, and no theme tokens. `scripts/check_runtime_style_injection.py` exit 0 globally (195 .js files scanned, 44 injecting, 89 hits, all within frozen allowances; the 44 injecting files are pre-existing pages with stateful style mutations, none are policy_watch page/composer files). The four renamed strings use pre-existing CSS classes (`pw-meta`, `pw-badge neutral`, `pw-chip`, `pw-call-topic`, `pw-stat`) — colour, padding, border, typography treatments byte-identical pre/post merge. The `pw-call-id` class still exists in the stylesheet (because the expanded audit ledger uses it), so no CSS rule is orphaned or dead. Dark × light × EN/ZH × desktop/mobile matrix inherits pre-existing 8-cell coverage (the PR introduces no new material, layout, component, or visual treatment — a copy rename is below the packet threshold). |
| validated-claims | PASS | Zero new tier-asserting nouns on user-visible strings. `verified` / `Unproven market views—not trade instructions.` / `verified_levers` / `Last verified` / `Built` / `For information only—not investment advice.` are PRE-EXISTING chrome copy unchanged by this PR (verified by `git diff` line-by-line). The 4 new strings (`Analysis confidence: high/medium/low`, `Next review`, `Excluded`, `Excluded from track record`) name the SUBJECT and CONSEQUENCE, NOT a tier assertion. `Analysis confidence: high` is a confidence label (display-tier signal), not an authority-tier judgment; `Excluded from track record: <reason>` describes ledger consequence (this call does not count), not gauntlet clearance. The 38 PRE-EXISTING validated-claims violations on `origin/main` are in files NOT touched by this PR (the macro suite pages, the macro_suite.js / mm_brain.js lineage notes, the consumer.py `[detail]` snapshot note) — they are a separate concern for those page owners. The 1 PRE-EXISTING title-i18n violation in `templates/am_edition.html.j2:13` is also NOT touched by this PR. No A7 promotion; no gauntlet claim; no new authority. |

**Net audit cost:** one-pass, no retries, no scope expansion. Audit file persisted to `orch/audits/macro_PR-7445.mm.md`.

---

**Release-side observation (separate from the three audit dimensions):** PR body §"Validation" carries an honest baseline admission — "Four FOMC-history assertions in `tests/test_policy_watch_ui.py` currently fail on untouched main because the committed FOMC ledger has advanced beyond their July-29 expectations / record-selection assumptions. This PR does not change the builder, FOMC data, or those tests." and "Policy Watch test suite also mutates `data/policy_lifecycle/events.jsonl`, which trips the repo data-write guard after otherwise-green assertions. The side effect was restored to HEAD and is not in this PR." Per CLAUDE.md standing laws, the pre-existing test failures and the data-write side effect are a separate concern for the Policy Watch test owner; no forced synthetic success is being claimed by this audit. The audit's plain-language / theme / validated-claims review is REPRODUCIBLE on the merged head `ad00e63eb81` and is independent of those pre-existing baseline issues.
