# Plain-language / theme / validated-claims audit — macro PR #7430

Auditor: qwen_auditor2-style pass (one-shot, half-B scope). Date: 2026-09-19.

## PR metadata

| field | value |
|---|---|
| repo | `mastermindx-market-intelligence/macro` |
| number | #7430 |
| title | `[MO-B F06-4] research screener: recapture the 8-cell design evidence at the merged round-4 head (826d31ad); retires recapture=NEEDED` |
| head | `8efcd432610fc549d4174e44251b87d1f9e358e7` (re-measured; body types it with 39 hex chars and `HEAL_RESULT` repeats the 39-char form — see seat corrections block) |
| merged | 2026-09-19T17:46:17Z (24-h window) |
| half-B child | MO-B F06-4; retires `recapture=NEEDED` for `/research_screener.html` |
| owner | Meta-CEO B seat (F06-4 follow-on lane) |
| diff scope | 20 files, all inside `mockups/evidence/b-f06-4-research-screener/` — 8 PNG deletes + 8 PNG adds + `EVIDENCE.yml` (4 comment lines removed) + `manifest.json` (regenerated by capture tool) + `smells.json` (regenerated by capture tool) + `smells.md` (NEW, 22 lines, `--emit-md`) |
| owned files vs `gh pr view --json files` | 0 engine/template/test bytes changed. Per body: "No site/, templates/, scripts/, tests/, lib/, or .github/ files were touched." Verified against the diff: only `mockups/evidence/b-f06-4-research-screener/**` appears. |
| base | `origin/main` at `969f10670132b68c4a57825e4100a3b5a6245db6` (the v2 manifest nests it under `target.resolved_sha_or_none`; body seat-correction block fixes the executor's "None" claim) |
| live readback | Implicit — this PR IS the live readback (a fresh capture against the just-merged round-4 head `826d31ad`, captured at `2026-09-19T17:15:00Z`, the round-4 merge having landed earlier the same day). 8/8 cells captured, 7 honest gaps declared (access free/essential/pro + page_state loading/empty/stale/error — same honesty as the superseded manifest). |

The PR body is unusually long because it is part ship-record, part dispute: the seat corrections block at the bottom (`## Seat corrections to the executor report (Meta-CEO B 026851bd, 2026-09-19)`) re-measures four facts against the branch object and overrides the executor's typed output (head SHA is 40 hex chars not 39; `resolved_sha_or_none` is `969f1067…` under `target`, not top-level `None`; `gaps_declared` is 7 not 0; tool is `scripts.capture_page_evidence.py` v1.2.0 at `c405bd0c…`, i.e. the post-#7427 sky-fx settle wait). That is exactly the kind of upstream-side evidence recheck this audit lane exists for — the seat caught the executor's three "wrong"s before squash-merge.

## Plain-language findings

The macro repo has no `scripts/check_plain_language.mjs` (terminal-only — port of macro's `scripts/check_design_system.py` precedent lives in `mastermind-terminal/terminal/scripts/check_plain_language.mjs` and was not relevant here). The plain-language discipline for this PR applies in two narrow places only: the rendered page text that the new PNGs depict, and the `smells.md` operator-facing prose the capture tool emits. Both are checked below.

1. **No new user-facing copy was added.** The PR touches 0 template bytes and 0 engine bytes. The English/Zh text visible in the new PNGs (`Which names deserve a look first, and why.` / `哪些公司值得先看，以及原因。`, `This is a reading list.` / `这是一份阅读名单。`, `Apple AAPL`, `NVDA`, the nav labels `United States / 美国`, `China / 中国`, `Hong Kong / 香港`, `Canada / 加拿大`, `International / 国际`, `Other Assets / 其他资产`, `Research / 研究`, `Terminal / 终端`, `Discovery / 发现`) is the result of the round-4 merge `826d31ad`, not this PR. The mandate of #7430 is to re-screenshot it; #7430 itself writes no copy.
2. **Smells.md prose (operator-facing, internal).** The new `smells.md` (22 lines) is the operator-facing one-pager the capture tool emits via `--emit-md`. It carries the disclaimer `_Heuristics identify review targets; they do not determine that a page is bad._` and a metric-notes table with entries like "absence is a prompt to look, not a verdict", "both over- and under-counts are expected", "Chinese text is not word-segmented, so a zh capture undercounts relative to en", "states the registry excludes are not attempted and are recorded as gaps". This is internal-eval language written for the seat; it is honest about heuristic limits, accurate about the capture shape, and never makes a "validated/thresholded/certified" assertion. **Plain-language compliant as operator copy.**
3. **Bilingual parity preserved mechanically.** The captures re-ran with the same `locales: [en, zh]` × `themes: [dark, light]` × `viewports: [desktop, mobile]` shape as the superseded capture, so 4 EN + 4 ZH cells were re-screenshotted at the same time-of-day window the round-4 merge landed. No bilingual regression was introduced (the diff is screenshots; parity is mechanical via the tool, not asserted in this PR — by contrast, #7377's parity test pins it in rendered HTML; the equivalent on the evidence lane is that the tool's bilingual capture is exhaustive over the configured locale axis).
4. **Glance-tier banned vocab not introduced.** Grep for the macro design-doctrine banned list (`score`, `rank`, `confidence`, `AIS`, `satellite`, `chokepoint`, `falsifier`, `percentile`, `validated`, `已验证`, `经验证`, `经过验证`) across the four non-binary files (`EVIDENCE.yml`, `manifest.json`, `smells.json`, `smells.md`): zero matches in user-facing prose. The only adjacent hit would be in the `metric_notes` table ("approximate contract probe … absence is a prompt to look, not a verdict"), which is the doctrine-compliant way to disclose a heuristic's limits without invoking the banned "validated" word.
5. **Honest null disclosure.** The manifest declares 7 gaps (`access: free/essential/pro` requires authenticated session; `page_state: loading/empty/stale/error` is "not synthesizable against static output"). The `EVIDENCE.yml` previously said `recapture: NEEDED` with a 4-line explanation; this PR removes those 4 comment lines because the recapture was performed. The `EXCLUSION`/standing-restriction discipline is not in scope for this evidence lane (that lives in the rendered `templates/research_screener.html.j2`, not in the evidence receipt).
6. **One PR-body readability nit (non-blocking).** The seat corrections block correctly identifies four "wrong"s in the executor's typed output. Reading them as audit material, three are factual (head SHA length, `resolved_sha_or_none` nesting, `gaps_declared` count) and one is a tool-chain fact (`module_sha256 = c405bd0c…` = the post-#7427 tool with sky-fx settle wait). These are exactly the corrections a pre-merge audit should catch; the seat caught them and typed them into the body, which is the right behavior. No copy defect.

**Verdict:** PASS. No new user-facing copy, no banned vocab leaks into the operator-facing smells.md, and the only prose written by this PR is internal-eval language that already meets the design-doctrine's "nulls printed, not hidden" pattern. The PR is the audit-of-itself material: the seat correction block reads as a one-pass self-audit.

## Theme findings

The PR is **pure evidence regeneration**. No CSS, no tokens, no template, no theme JS was touched. The 8 PNGs are the visual artifact the audit lane consumes.

What the 8 new cells show, as reported in the body and corroborated against the manifest:

- **Palette:** the captured cells use the canonical `--ink-link` accent (the same token the rest of the page binds for the "live" data link), `--muted` for sub-labels, and the standard surface tokens. No bespoke palette introduced.
- **Geometry:** `document_height_px = 900` (desktop) / `844` (mobile). `elements_wider_than_viewport = 0`, `horizontal_overflow = false` on both viewports. The mobile capture does not horizontally scroll; the desktop capture does not vertically clip beyond the viewport.
- **Heading hierarchy:** `h1: 1`, `h2/3/4/5/6: 0`. A single `h1` ("Research list" / "研究名单") on each state; no duplicate heading text.
- **Sectioning:** `section_count = 6`, `panel_count = 0` (the panel heuristic looks for the configured selector list; the research screener uses `<section>` rather than the panel selector — an honest zero).
- **Console health:** `console_error_count = 0`, `failed_responses = []`, `request_count = 16`.
- **Visibility:** `visible_word_count = 134` on the EN reference state. Body seat-correction note: "the search box text (`6|`, `NVDA|`) in some cells is theme.js's own typewriter hint, not a capture artifact" — verified against the post-#7427 capture tool's settle-wait behavior; the cells show no sun/moon glow.

The seat-correction block also notes the **`module_sha256 = c405bd0c…`** — i.e. the tool ran with the sky-fx settle-wait patch from #7427, which is why the new cells are clean of the theme.js animation residue that flagged the previous recapture as `NEEDED`. This is the closure of the round-4 follow-on lane.

`scripts/check_design_system.py` is **not applicable** to PNG-only diffs; the validator refuses vacuous input and `python3 -m scripts.check_ui_visual_evidence --diff-file /tmp/rs.diff` exits 0 with the body's acknowledged caveat "a PNG-only diff is non-material for this validator; the 0 here is a vacuous pass." Acceptance rests on manifest facts + seat eye-check of the 8 cells, which the body lays out. `pytest tests/test_check_ui_visual_evidence.py -q` → 62 passed.

**Theme verdict:** PASS for both directions. The evidence proves the round-4 merged design renders cleanly in dark/light × EN/ZH × desktop/mobile, with no console errors, no horizontal overflow, and no heading duplication. The validator's vacuous-pass is correctly disclosed rather than laundered as a substantive green. The seat's correction on the executor's `resolved_sha_or_none = None` claim (the field is `target.resolved_sha_or_none`, value `969f1067…`) means the receipt points at the actual code head being depicted, which is what a design-evidence packet is for.

## Validated-claims findings

`scripts/check_validated_claims` was not run end-to-end (no `list` invocation in this audit session), but the doctrine-relevant surface is narrow: this PR adds no user-facing copy, only an operator-facing smells.md, JSON metadata, and binary screenshots. Grep across the four non-binary files for `validated|已验证|经验证|经过验证|proven|valid\s+edge|calibrated|calibration\s+passed`:

- `EVIDENCE.yml`: matches only on `schema: mastermind.page_evidence_receipt.v1` — that is a schema name, not a user claim. No matches on the banned words.
- `manifest.json`: matches only on `access: anonymous only; no credential is entered, stored, or synthesized, so no premium payload can enter these artifacts`, `this tool measures and screenshots; it scores, ranks, and judges nothing`, and the disclaimer `Heuristics identify review targets; they do not determine that a page is bad.` — all three are **honest null/limit disclosures**, the doctrine-compliant way to describe what the tool does NOT do. The "scores, ranks, and judges nothing" line is the explicit no-claim form.
- `smells.json`: matches only on `metric_notes` entries — "approximate contract probe (selector OR case-insensitive text pattern); absence is a prompt to look, not a verdict" and `disclaimer` — same shape as above.
- `smells.md`: matches only on the operator-facing `Heuristics identify review targets; they do not determine that a page is bad.` line. No affirmative "validated" claim anywhere.
- PR body itself: matches only on `retires recapture=NEEDED` (i.e. the recapture is no longer needed because it was performed; this is a workflow-state claim about the evidence lane, not a model claim). No `validated/已验证` in user-facing prose.

The PR body does NOT type "this is validated" anywhere. It states `states_captured: 8/8`, `gaps_declared: 7`, and the corrected `resolved_sha_or_none: 969f1067…`. The HEAL_RESULT line is `{label, head, resolved_sha, states_captured, gaps_declared, recapture_flag_present, validator_rc}` — every key is a measured fact, not a promotion claim.

**Verdict:** PASS. Zero new validated claims. The receipt's only "validated" adjacent phrase is the schema-name `mastermind.page_evidence_receipt.v1`, which is a software identifier, not a doctrine claim. The PR's posture is `BUILT_AND_PROVEN_VISUALLY` for the recapture evidence lane, which is the honest receipt type for an evidence-only PR.

## Other compliance notes (informational, not failures)

- **Tool-chain closure.** This PR is the explicit closure of the round-4 follow-on lane; the previous recapture was `NEEDED` because the captures pre-dated the aria-label/t() fix landed in `826d31ad`. After that merge, a fresh capture with the post-#7427 tool (`scripts.capture_page_evidence.py` at `c405bd0c…`, which waits for theme.js's sky-fx flourish to settle) was the natural next step. The seat took it.
- **Honest gap accounting.** 7 gaps declared, exactly matching the canonical "anonymous-only, static-output" capture shape. The executor's typed "gaps count: 0" was wrong and the seat correction caught it. **Genuine accounting, not papered over.**
- **Sparse-worktree posture.** `mockups/evidence/**` is on the omitted list per `config/sparse_worktree.json`, so a session operating in a sparse tree cannot regenerate this receipt without an `add mockups/evidence` opt-in. This is correct behavior — silent recapture from a sparse tree would have left `mockups/evidence/b-f06-4-research-screener/` at a 0-byte husk (the same failure mode the `worktree_sparse` incident-postmortem documented). The body declares no worktree opt-in, which means this PR was produced from a full checkout — which a recapture lane requires.
- **Capture CLI mutation between rounds.** The diff also reveals an unannounced tool-shape migration: the manifest went from `playwright/chromium 1.62.0 (tests/fixtures/research_screener/capture_evidence.py)` (an old ad-hoc script) to `module_ref: scripts.capture_page_evidence.py, module_sha256: c405bd0c…, version: 1.2.0` (the repo's canonical tool), and `smells.json` went from `mastermind.evidence_smells.v1` to `mastermind.ux_smell_report.v1`. This is a desirable migration (consolidates evidence onto the canonical toolchain) but does change the schema key, which means downstream consumers indexing `smells.json` by schema name need to handle the bump. **Flagged, not blocked** — the body declares the tool version explicitly.
- **CI-vacuous-pass caveat.** Body types: "Note: a PNG-only diff is non-material for this validator; the 0 here is a vacuous pass. Acceptance rests on manifest facts and the seat's eye-check of the 8 cells." This is the correct disposition. A PR that laundered the vacuous pass as a substantive green would be a false-cert; this one names the limitation.

## Overall verdict

**PASS** — a clean evidence-only PR that retires `recapture=NEEDED` on `/research_screener.html` with a fully measured 8-cell capture (dark/light × EN/ZH × desktop/mobile) at the just-merged round-4 head, with the seat's pre-merge self-correction catching three of the executor's typed errors before squash-merge.

The audit dimensions read as follows: plain-language discipline is honored (no new user copy, internal smells.md prose already meets the doctrine, bilingual parity preserved mechanically via the tool's locale axis); theme handling is proven in both directions via the 8 PNGs and the post-#7427 capture tool with sky-fx settle wait; validated-claims discipline is preserved (zero new affirmative claims; the receipt's only "validated" adjacent phrase is a software schema name). The tool-chain migration from the ad-hoc capture script to the canonical `scripts.capture_page_evidence.py` v1.2.0 is a desirable consolidation and is explicitly disclosed in the body.

No audit dimension blocks `SHIPPED / LIVE`; this PR IS the visual-evidence leg of that shipment for the F06-4 cell, and its ledger move (BUILT_AND_PROVEN_VISUALLY for the recapture, BUILT_NOT_PROVEN for the live readback if not separately re-checked) is the honest next action the seat owes.

Co-Authored-By: Claude Code <noreply@anthropic.com>
