# Plain-language / theme / validated-claims audit — macro PR #7554

Auditor: qwen_auditor2-style pass (one-shot, half-B scope). Date: 2026-09-21.

## PR metadata

| field | value |
|---|---|
| repo | `mastermindx-market-intelligence/macro` |
| number | #7554 |
| title | `[MO-A UD-B2-W2] Spine-scale bindings: HK/CN → market_state.score, Bonds/Commodities stay designed-null` |
| merged | 2026-09-21T05:12:49Z via squash-merge to `main`. Selected as a recent B-wave (UD-B2-W2) half-B PR in the 24-h window that has not already been audited: cross-referencing `gh pr list --state merged --search "merged:>2026-09-20T14:00:00Z"` against `git ls-tree origin/main -- orch/audits/` shows the prior 24-h merges #7607/#7608/#7603/#7602/#7599/#7589/#7585/#7573 already carry audits (`macro_PR-7607.mm.md`, `macro_PR-7608.mm.md`, `macro_PR-7603.mm.md`, `macro_PR-7602.mm.md`, `macro_PR-7599.mm.md`, `macro_PR-7589.mm.md`, `macro_PR-7585.mm.md`, `macro_PR-7573.mm.md`). #7554 is the most-recent user-facing B-wave spine-binding merge in the 24-h window — it binds HK + China A-shares rows on the unified macro dashboard's one-scale five-markets regime spine to ratified `market_state.score` engine outputs, ships a fresh DEC record (`DEC-SPINE-SCALE-BINDINGS`), persists two new snapshot artifacts (`data/hk_market_state/latest.json`, `data/china_market_state/latest.json`), and intentionally leaves the Bonds + Commodities rows as designed-null. |
| files | **23 paths** total. Source paths touched: `templates/_unified_dashboard_hero.html.j2` (+199 / −10, HK + CN row rewire from designed-null to bound + new `.mx-spine-caveat` style block), `engine/market_state.py` (+64 / −39, `persist()` + `load_persisted()` + `_store_path()` gain a `market_key` parameter; the NYSE freshness stamp becomes US-only), `scripts/build_hk.py` (+17, HK profile persist call), `scripts/build_china.py` (+17, CN profile persist call), `scripts/build_site.py` (+52, new `_persisted_ms_view('hk'/'cn')` ingest helper), `site/macro.html` (+10585 / −3265, regenerated by `python -m scripts.build_site`), `.github/ci/legacy-jobs.yml` (+1, `tests/test_unified_dashboard_b2w2.py` wired into the gate), `tests/test_unified_dashboard_b2w2.py` (+948, NEW, 26 `test_*` functions). New artifacts: `agentos/decisions/DEC-SPINE-SCALE-BINDINGS.md` (+108), `data/hk_market_state/latest.json` (+1, 31-key persist output), `data/china_market_state/latest.json` (+1, 31-key persist output), `mockups/evidence/unified-dashboard-b2w2/manifest.json` (+186), `mockups/evidence/unified-dashboard-b2w2/EVIDENCE.yml` (+4), `mockups/evidence/unified-dashboard-b2w2/spine_evidence.json` (+188), 8 PNG cells (2 themes × 2 locales × 2 viewports). Updated: `research/UNIFIED_DASHBOARD_DISPOSITION.md` (+4 / −4, rows 19b/19c flipped BLOCKED_DATA → BOUND with the ratified source named; rows 19d/19e stay BLOCKED_DATA with the R-W2-3 product-state rationale). |
| half-B label | **half-B B-wave spine-binding (UD-B2-W2 / DEC-SPINE-SCALE-BINDINGS).** Body is explicit: "bind the HK row to HK_PROFILE market_state.score — the same blender that produces the US subject row, with identical leg weights 0.24/0.18/0.16/0.16/0.14/0.12 and verdict cuts 60/42… bind the CN row to CN_PROFILE market_state.score (same blender, same weights, same cuts)… Government bonds + Commodities rows STAY DESIGNED-NULL — this is the ratified product state, not a deferral." |
| scope | (a) Bind HK row to `vm["hk_market_state"]` (HK_PROFILE blender output persisted to `data/hk_market_state/latest.json`); (b) bind CN row to `vm["cn_market_state"]` (CN_PROFILE blender output persisted to `data/china_market_state/latest.json`); (c) leave Bonds + Commodities rows as designed-null with slugs UNCHANGED; (d) carry the engine's own caveat stamp (`caveat_en` / `caveat_zh`) onto the row's disclosure — binding the score without the caveat is the named failure mode; (e) replace `color-mix(in srgb, …)` on the changed caveat rules with `var(--ink-2)` / `var(--ink-3)` (R-W2-16); (f) move the as-of date out of the row's name cell into the `.mx-spine-caveat` disclosure (R-W2-14); (g) replace the prior `aria-label="engine caveat"` with the actual disclosure sentence as `aria-label` (R-W2-15); (h) ship an 8-cell dark/light × EN/ZH × 1440/390 visual evidence matrix under `mockups/evidence/unified-dashboard-b2w2/`; (i) add 26 unit tests in `tests/test_unified_dashboard_b2w2.py`; (j) record the DEC as a 12-line `agentos/decisions/DEC-SPINE-SCALE-BINDINGS.md` so the binding is auditable outside the code. |
| durable owner | None new. The existing engine (`engine/market_state.py`, `engine/market_state_hk.py`, `engine/market_state_cn.py`) gains a per-market `market_key` parameter on `_store_path` / `persist` / `load_persisted`; the HK + CN persist calls live in their existing build scripts (`scripts/build_hk.py`, `scripts/build_china.py`); the macro site ingest helper lives in `scripts/build_site.py` under the existing `_persisted_ms_view` name. The US `data/market_state/latest.json` is never overwritten by a non-US persist. |
| checks | Body reports: **`scripts/check_design_system.py --mode enforce-added`** returns `0 blocking finding(s)` against the template diff; 26 new tests in `tests/test_unified_dashboard_b2w2.py` pass; 7 tests in `tests/test_market_state_persist_freshness.py` (pre-existing, no-regress) pass; `git diff --check` passes; **`python3 scripts/agentos.py validate`** returns 0 errors. The 8-cell PNG matrix (theme × locale × viewport) is committed with sha256 receipts. The `scripts/check_validated_claims.py --selftest` and `scripts/check_runtime_style_injection.py --selftest` both PASS in this worktree (re-confirmed for THIS audit). |
| evidence matrix | Body cites the 8-cell dark/light × EN/ZH × 1440/390 visual matrix committed to `mockups/evidence/unified-dashboard-b2w2/`. The TP-0 standing requirement is "both themes judged as designs (hierarchy, material depth, semantic color, responsive composition, EN/ZH parity)"; the body attests all 8 cells captured with sha256 + byte receipts. The capture head `d9a57dd944…` slice_sha256 pair-matches the final head `ff5aae17f…` slice (`d1a914a628b7fe99…`, 9997 bytes). The body's spec-conformance list (`R-W2-11 … R-W2-16`) names each round-3 ruling that the diff satisfies. |
| gating scripts | `scripts/check_plain_language.mjs` — **DOES NOT EXIST** in macro (terminal-side only); macroside discipline is read against the design-doctrine banned-glance vocabulary + the standing `l-en`/`l-zh` bilingual-pair discipline enforced by `tests/test_bilingual_span_leak.py` + the design-doctrine stance-word budget. `scripts/check_validated_claims.py` — exists; re-ran the selftest PASS in this worktree; PR-introduced copy carries no new affirmative `validated` / `已验证` / `经验证` / `经过验证` vocabulary and the engine caveat copy is allowlisted engine copy (caveat_en/caveat_zh on a market_state display surface). `scripts/check_design_system.py` — re-ran with `--mode enforce-added` against the template diff and returns `0 blocking finding(s)`; the new `.mx-spine-caveat` block is token-scoped (`var(--line)`, `var(--r-ctl,8px)`, `var(--fs-micro)`, `var(--ink-2)`, `var(--ink-3)`) and replaces prior `color-mix(in srgb, …)` rules per R-W2-16. `scripts/check_runtime_style_injection.py` — re-ran the selftest PASS in this worktree; PR-introduced inline `style="…"` attributes are exclusively `left:X%` / `width:X%` rail-marker positioning (genuinely data-dependent inline geometry per the standing rule; JS-style material authoring is absent). |

## Diff content (scoped to this audit)

The PR's user-facing source-of-truth is the template edit; the engine and build-script edits are the data plumbing that lets the template read the snapshot. The PR's user-facing surfaces are summarised below at the level the three audits need.

### `templates/_unified_dashboard_hero.html.j2` (+199 / −10)

**New style block (lines 198–216).** A scoped `<style>` block declares `.mx-spine-caveat` and its light/dark override. The block's preamble is explicit: "UD-B2-W2: spine caveat disclosure — engine-verbatim lighter-evidence note. Scope-narrowed to this hero template… Token-scoped only: every value is a `var()` reference, no hex / rgba / colour-function / radius literal / inline custom-property literal." The body keeps the rule in this template (not `templates/theme.css`) to keep the THEME-CLOSURE re-bind sweep out of this PR's scope. The rule:
```css
.mx-spine-caveat{
  margin-top:6px;
  padding: 4px 8px;
  border-left: 2px solid var(--line);
  border-radius: var(--r-ctl,8px);
  font-size: var(--fs-micro);
  line-height: 1.45;
  color: var(--ink-2);
}
html[data-theme="light"] .mx-spine-caveat{ color: var(--ink-3); }
```
All values are `var()` references. The dark default is `var(--ink-2)` (the dark command-center surface); the light override is `var(--ink-3)`. The block does **not** author a hardcoded colour, an rgba, or a colour-function — `color-mix(in srgb, …)` is fully gone from the changed rules per R-W2-16. The `border-radius: var(--r-ctl,8px)` carries a fallback literal that the body's DEVIATIONS list names as a MINOR-2 review-fix left in place (the rule is unique to this template's `.mx-spine-caveat` class, never re-bound by THEME-CLOSURE, and reworking it would force a full `python -m scripts.build_site` re-render for a non-colour-function miss).

**HK row rewire (lines 429–578).** The four `data-null="1"` BLOCKED_DATA rows (HK / China A / Gov bonds / Commodities) are partially opened: HK and China A become bound, Gov bonds + Commodities remain `data-null="1"`. The HK row reads `vm["hk_market_state"]` (set by `scripts/build_site.py:_persisted_ms_view('hk')`) and computes:
- **State.** Three-state classification: `_hk_today` (today's score) and `_hk_hist_score` (the 22-row-back score from `_hk.get('ms_history')`):
  - both present → `_hk_state = 'real'` (full marker + connector + travel)
  - only today → `_hk_state = 'short-history'` (marker, no travel)
  - neither → `_hk_state = 'null'` (designed-null, the original `data-blocked-feed="hk_market_state"` treatment)
- **Verdict-derived stance.** The verdict string drives `_hk_do_mod` + `_hk_do_en` + `_hk_do_zh`:
  - `RISK_ON` → `_hk_do_en = 'Watch — don't chase'` / `_hk_do_zh = '观察，勿追'` (warn modifier)
  - `RISK_OFF` → `_hk_do_en = 'Stand aside'` / `_hk_do_zh = '暂时观望'` (muted modifier)
  - `MIXED` (default) → `_hk_do_en = 'Get ready'` / `_hk_do_zh = '准备行动'` (warn modifier)
- **Rail geometry.** The marker (`mx-spine-mark`), connector (`mx-spine-conn`), and prior (`mx-spine-prev`) get inline `style="left:X%;width:Y%"` (data-dependent positioning — the only legitimate inline-style pattern).
- **Travel cell.** Only when `_hk_state == 'real'` is the travel cell populated; otherwise it carries the original `—` / `—` muted em-dashes. The arrow glyph is `→` (positive), `←` (negative), `·` (flat).
- **Caveat disclosure.** When `_hk_today is not none and _hk.get('caveat_en')`:
  - As-of + engine caveat are concatenated into a single disclosure sentence — `"As of " ~ _hk_asof ~ ". " ~ _hk_cav_en` (EN) / `"截至 " ~ _hk_asof ~ "。" ~ _hk_cav_zh` (ZH)
  - The disclosure lives in `.mx-spine-caveat` (token-scoped per the new style block)
  - **The disclosure sentence is the `aria-label`** on each bilingual span (R-W2-15): `<span class="l-en" aria-label="{{ _hk_disc_en }}">{{ _hk_disc_en }}</span>` — the test asserts the `aria-label="<full disclosure sentence>"` attribute itself, not merely the visible caveat text
  - The disclosure's `data-caveat-en` / `data-caveat-zh` attributes are also stamped onto the row for downstream consumers
- **Name cell.** Per R-W2-14, the name cell carries the market name + bilingual spans ONLY — no `as of` / `截至` date inside the name cell (the date lives in the disclosure). The em-dash `<small>—</small>` is reserved for the `_hk_today is none` (designed-null) case.

**CN row rewire (lines 597–671).** Identical geometry to HK, reading `vm["cn_market_state"]`. Three-state classification, the same three stance strings, the same data-blocked-feed rename (`hk_market_state` → `cn_market_state`), the same disclosure-as-aria-label pattern, the same `.mx-spine-caveat` styling. The CN row's `data-stance` derives the same way (`protect` / `watch` / `stand-aside`).

**Bonds + Commodities rows (lines 678–692).** UNCHANGED in shape — `data-null="1"` keeps the designed-null treatment; the `data-blocked-feed="gov_bonds_regime"` / `data-blocked-feed="commodities_regime"` slugs stay UNCHANGED (so they remain honest as not-yet-existing contracts). The DEC-SPINE-SCALE-BINDINGS rationale names the rejection: bond health is a stress inverse of the economy/credit/plumbing, not a bonds-market risk-on read; bond_compass duration lean's polarity is opposite of the spine unless inverted (which would originate a new signal); the spec mockup already designed the Commodities row as null (UNIFIED_DASHBOARD_SPEC.md §3:128–129, §7:248); the complex is internally mixed (gold vs copper) and one marker cannot be both "precious bid = risk-off" and "complex uptrend = risk-on." This is the **ratified product state, not a deferral** — the dashed rail + "Read being updated" chip stays as the honest null form.

### `engine/market_state.py` (+64 / −39)

Three functions gain a `market_key: str | None = None` parameter; the NYSE freshness stamp becomes US-only:
- `_store_path(root=None, market_key: str | None = None)` — per-market snapshot path. `market_key=None` / `"us"` preserves the canonical US path `data/market_state/latest.json`. `"hk"` → `data/hk_market_state/latest.json`; `"cn"` → `data/china_market_state/latest.json`; any other key uses `<key>_market_state/latest.json`.
- `persist(snap, root=None, now=None, market_key=None)` — per-market write with the same no-regress guard that the US path has carried since the 2026-07-07 stale-regime incident. The NYSE freshness stamp is gated to `not market_key or market_key == "us"` — HK/CN use their own session calendar, and the macro spine reads the engine caveat stamp on the row instead.
- `load_persisted(root=None, market_key=None)` — per-market read.

The no-regress guard travels with the engine for HK + CN: a snapshot whose `asof` is NEWER than the incoming one refuses to overwrite. Equal-asof always overwrites (same-session recomputes are routine).

### `scripts/build_hk.py` (+17) + `scripts/build_china.py` (+17)

Each build script gains a persist call next to the existing `_ms.market_state_snapshot(...)` line:
```python
try:
    import os as _osenv_p  # noqa: PLC0415
    if _osenv_p.environ.get("HK_FAST_RENDER") or _osenv_p.environ.get("CHINA_FAST_RENDER"):
        pass                  # dev re-render: read-only
    else:
        _ms.persist(vm.get("market_state"), market_key="hk" | "cn")
except Exception as _p_e:  # noqa: BLE001 — additive, never fatal
    log.warning("… market_state persist failed (%s); skipping", _p_e)
```
Fast-render dev rerenders skip the write (parity with the score-log append). Off the heavy render path: a single `json.dump` beside existing parquet writes.

### `scripts/build_site.py` (+52)

The macro vm ingest helper is named `_persisted_ms_view` (NOT `_intl_ms_view` — the body's DEVIATIONS list names this: the name does not reopen the R-W2-2 `intl_market_state` trap that the prior wave hit). It calls `load_persisted(market_key='hk' | 'cn')`, returns the snapshot dict, and `scripts/build_site.py` writes `vm["hk_market_state"]` / `vm["cn_market_state"]` for the template to read. The US ingest path is unchanged.

### `data/hk_market_state/latest.json` + `data/china_market_state/latest.json` (NEW, 31 keys each)

Both are the `persist(snap, market_key=)` output for HK / CN: schema `market_state.v1`, `asof: 2026-09-18`, score 37, all 31 keys (`schema, asof, score, raw_score, score_source, capped, score_ceiling, score_caps, score_gap, radar, verdict, color, label_*, posture_*, headline_*, components, mtf, overrides, flip_*, alerts_count, market, caveat_*, is_display_only, input_vintages, stale_inputs, degraded_components`). `"market"` sits **before** the caveats (matches `engine/market_state.py:1017-1019`). Compact JSON, `ensure_ascii=False, default=str`. The HK snapshot's `caveat_en` is: "Display-only and lighter than the US read: HK has no leading Risk Radar, no VIX term structure (VHSI stands in), and the downturn gauges are uncalibrated. HK is heavily globally driven — US/EM beta and the HKD peg. Context, not a forecast." The CN snapshot's `caveat_en` is: "Display-only and lighter than the US read: China has no leading Risk Radar, no VIX term structure (QVIX + margin froth stand in), no HY credit spread, and the downturn gauges are uncalibrated. It reads what has already turned — context, not a forecast."

### `tests/test_unified_dashboard_b2w2.py` (+948, NEW)

26 `test_*` functions covering the persist round-trip, the rename (`hk_market_state` / `cn_market_state`), the slug-rationale rejection (`intl_market_state` / `hk_regime` / `china_a_regime` must not reappear), the score_log row requirement (`>=22` rows for real travel, shorter → designed-null travel with a real today marker), the verdict-derived stance strings, the `.mx-spine-caveat` disclosure content, the aria-label-as-disclosure-sentence attribute (not merely `caveat_en in row`), the colour-function replacement (`color-mix(in srgb, …)` absent from the template), the as-of-in-name-cell absence (R-W2-14), the slug-stability invariant (`gov_bonds_regime` / `commodities_regime` UNCHANGED), and the 8-cell evidence manifest.

### `agentos/decisions/DEC-SPINE-SCALE-BINDINGS.md` (NEW, 108 lines)

A full DEC record (question / answer / rationale / alternatives / evidence / affects / confidence / reversibility / decided_by / decided_at) naming the HK / CN binding as composition of existing published measurements (HK_PROFILE + CN_PROFILE both publish 0–100 with the same blender and weights, and they already disclose they are lighter). The DEC names the rejection of four alternatives (different 0-100 affine; quad-to-100 mapping; bond health/compass inverted; commodity index inverted) and the rationale for the designed-null Bonds + Commodities rows.

### `research/UNIFIED_DASHBOARD_DISPOSITION.md` (+4 / −4)

Rows 19b (HK) and 19c (CN A-shares) flip BLOCKED_DATA → BOUND with the ratified source named (`HK_PROFILE` / `CN_PROFILE` `market_state.score` persisted by `build_hk` / `build_china`). Rows 19d (Gov bonds) and 19e (Commodities) STAY BLOCKED_DATA with the R-W2-3 product-state rationale added (no commensurable scalar; spec designed Commodities as null; slugs stay UNCHANGED).

## Plain-language findings

The standing plain-language discipline on macro is read against `docs/DESIGN_DOCTRINE.md` ("Glance tier = state + plain-word stance under hard word budgets; technicals demoted to hover/popover/detail pages; every signal panel answers 'so what do I do', even when the honest answer is 'watch — don't chase'") and the standing `l-en`/`l-zh` bilingual-pair discipline enforced by `tests/test_bilingual_span_leak.py` + the design-doctrine stance-word budget.

**Headline: PASS on plain-language. The PR is a plain-language IMPROVEMENT as well as a binding-correctness fix.**

Specific findings:

1. **Bilingual pair balance is exact: 90 `l-en`, 90 `l-zh` in `templates/_unified_dashboard_hero.html.j2`.** A literal grep of the committed template returns 90 `<span class="l-en">` and 90 `<span class="l-zh">` — perfectly paired. The HK row's stance pair (`'Watch — don't chase'` / `'观察，勿追'`), the CN row's stance pair (identical), the HK + CN disclosure sentence pairs (`aria-label` EN + ZH on each `<span>`), and the original name-cell pair all share the same `l-en`/`l-zh` discipline the rest of the template carries.

2. **No banned study slugs / banned glance vocabulary introduced by THIS PR.** Grep over the PR-introduced template diff for the macroside banned-glance list (`falsifier / refuted / 证伪 / tier: / trust_tier / msc_regime / mscRegime / gexdesk / prophet / oracle / conductor / synapse / lobe / tripwire / vm['leadership'] / percentile rank / z-score / zscore`) returns **zero matches** in the PR-introduced lines. The new stance strings (`Watch — don't chase`, `Stand aside`, `Get ready`) are plain imperative forms with plain ZH translations (`观察，勿追`, `暂时观望`, `准备行动`) — no jargon, no banned slugs.

3. **Three plain-word stances, all under the design-doctrine word budget.** The verdict-band fallback uses exactly three strings — `Watch — don't chase` (3 EN words), `Stand aside` (2 EN words), `Get ready` (2 EN words) — every one of them is a short imperative-or-declarative English sentence with a plain ZH translation that fits the glance tier. None embeds a hidden promise. None over-claims. None is a falsifier/refuted/`证伪` form.

4. **Disclosure sentence travels with the score on every bound row (engine caveat stamp).** Per the DEC, "binding the score onto the rail WITHOUT the caveat is the named failure mode." The template now emits the engine's own `caveat_en` / `caveat_zh` as a disclosure sentence on every bound row:
   - HK EN: `"As of 2026-09-18. Display-only and lighter than the US read: HK has no leading Risk Radar, no VIX term structure (VHSI stands in), and the downturn gauges are uncalibrated. HK is heavily globally driven — US/EM beta and the HKD peg. Context, not a forecast."`
   - CN EN: `"As of 2026-09-18. Display-only and lighter than the US read: China has no leading Risk Radar, no VIX term structure (QVIX + margin froth stand in), no HY credit spread, and the downturn gauges are uncalibrated. It reads what has already turned — context, not a forecast."`

   Each is a plain noun phrase + qualifier; each explicitly stamps "Display-only and lighter" + "Context, not a forecast" — the design-doctrine-permitted "windows, not certainties" form. **No false "validated" / "已验证" claim is attached to a number that is explicitly stamped lighter.**

5. **`aria-label` carries the disclosure sentence, not a generic hook (R-W2-15).** Two new aria-label attributes appear on the HK + CN disclosure spans (lines 567 and 655 in the post-PR template): `<span class="l-en" aria-label="{{ _hk_disc_en }}">{{ _hk_disc_en }}</span><span class="l-zh" aria-label="{{ _hk_disc_zh }}">{{ _hk_disc_zh }}</span>`. The pre-fix code (per the body's DEVIATIONS list) had `aria-label="engine caveat"` — a generic hook that does not actually convey the disclosure to screen-readers. The PR replaces this with the **actual disclosure sentence** as `aria-label`. The test (`test_spine_slice_aria_labels_are_plain_caveat_sentences`) now requires the `aria-label="<full disclosure sentence>"` attribute itself, not merely visible caveat text in the row. This is an accessibility + plain-language WIN.

6. **No `title="…"` attributes introduced in PR-introduced code.** A literal grep of the PR-introduced template lines for `title="…"` returns **zero matches**. The disclosure + aria-label discipline is the standing macro pattern (no `title=` translations), and this PR preserves it.

7. **Name cells no longer print the date (R-W2-14).** Pre-fix the row's name cell could carry an as-of prefix; the PR moves the date into the `.mx-spine-caveat` disclosure and the name cell prints only the market name + bilingual spans (`Hong Kong` / `香港`, `China A-shares` / `A股`). The em-dash `<small>—</small>` is reserved for the `_hk_today is none` (designed-null) case, where the row is honest about the null. **The date no longer competes with the stance for the glance-tier word budget.**

8. **The original 4 BLOCKED_DATA rows stay honest as designed-null.** The Gov bonds + Commodities rows keep `data-null="1"`, `data-blocked-feed="gov_bonds_regime"` / `data-blocked-feed="commodities_regime"` UNCHANGED slugs, and the dashed rail + "Read being updated" chip treatment. Per the DEC's R-W2-3 product-state rationale, this is "ratified product state, not a deferral" — the rows name what they are (no contract) and do not over-claim. **A row that names its own null is more honest than a row that invents a number.**

9. **Bonds + Commodities slug rename discipline is enforced.** The DEC-SPINE-SCALE-BINDINGS rejects the slug renames `gov_bonds_regime → <something>` and `commodities_regime → <something>` precisely because renaming them would mint a "we have a contract" signal where there is none. The slugs stay honest as not-yet-existing contracts. The HK + CN slug renames (`hk_regime → hk_market_state`, `china_a_regime → cn_market_state`) are different — they rename a wrong wire (the quad's cycle-location wire is wrong for a risk-on rail) to the right wire (the 0-100 blender output). **Renames only happen when the prior wire would lie; staying-still is the honest default.**

10. **Plain-language residual issues: none.** The PR is a binding-correctness + plain-language + theme-art-direction + validated-claims repair. The meaningful plain-language changes are: (a) three plain-word stance strings (verdict-band fallback) replacing the prior hardcoded "designed-null" stance; (b) the engine caveat stamp traveling onto the row as a disclosure sentence; (c) the disclosure sentence becoming the `aria-label`; (d) the date moving out of the name cell into the disclosure; (e) Bonds + Commodities rows staying honestly null. All are improvements.

**Plain-language verdict: PASS.**

## Theme findings

The TP-0 standing rule ("dark and light share information architecture, component semantics, spacing/type scales, state meanings, user actions, data contracts, ordering/density law and interaction behavior — they do **not** have to share material treatment. Dark = command center; light = research workspace. Token substitution alone is never proof of a light design…") and the runtime-style-injection rule ("JS may mount/recompose canonical DOM, set state classes, select variants, and apply genuinely data-dependent inline geometry; governed CSS owns the material decisions") bind this PR.

**Headline: PASS on theme. THEME-CLOSURE preserved. The PR replaces `color-mix(in srgb, …)` with token-scoped values per R-W2-16, adds a token-scoped dark/light-aware `.mx-spine-caveat` rule, and confines the new style to the hero template (not `templates/theme.css`).**

Specific findings:

1. **Token discipline is clean in the PR-introduced CSS.** The new `.mx-spine-caveat` block uses only `var()` references: `var(--line)` (border-left rail), `var(--r-ctl,8px)` (radius with fallback), `var(--fs-micro)` (font-size), `var(--ink-2)` (dark default). The light override is `html[data-theme="light"] .mx-spine-caveat{ color: var(--ink-3); }` — also token-scoped. No hex literals. No rgba. No `color-mix(in srgb, …)`. No `oklch` / `lab` / `lch` / `hwb`. The new rule passes `scripts/check_design_system.py --mode enforce-added` with **0 blocking findings** (re-confirmed in this worktree).

2. **`color-mix(in srgb, …)` on the changed caveat rules is fully gone (R-W2-16).** The body's pre-PR state carried `color-mix(in srgb, …)` on the caveat rule (the original `aria-label="engine caveat"` block). The PR replaces this with `var(--ink-2)` / `var(--ink-3)`. `scripts/check_design_system.py` `FUNC_COLOR_RE` is `r"\b(?:rgba?|hsla?)\s*\("` — it does **not** match `color-mix`. The body's DEVIATIONS list names this as a linter gap; compliance here is to the LAW (no colour-function on changed lines), not the linter's blind spot. The grep `git grep color-mix -- templates/_unified_dashboard_hero.html.j2` is empty after this PR (per the body).

3. **Dark / light art direction via tokens, not re-implementation per page.** The `.mx-spine-caveat` block declares its dark default (`var(--ink-2)`) and its light override (`html[data-theme="light"] .mx-spine-caveat{ color: var(--ink-3); }`). The two ink tokens are the standing dark/light pair — dark uses the deeper ink (ink-2) for the command-center surface; light uses the softer ink (ink-3) for the research-workspace surface. The rule is **dark/light-aware as a single class** via tokens, not re-implemented per row. This is the correct TP-0 pattern — dark and light share information architecture + component semantics; the material treatment differs only in token routing.

4. **Inline `style="..."` attributes are data-dependent geometry only.** A count of inline `style="..."` in the PR-introduced lines: **6** rail-marker positioning attributes — `<div class="mx-spine-prev" style="left:X%">`, `<div class="mx-spine-conn" style="left:X%;width:Y%">`, `<div class="mx-spine-mark" style="left:X%">` × 2 rows (HK + CN) × 3 elements each. **All 6 are `left:X%` / `width:Y%` positioning — no colour, no rgba, no hex, no border, no padding.** This is the standing-rule-permitted form ("genuinely data-dependent inline geometry") — the rail marker position is the score value (0–100), and the connector width is the score delta. The JS-style material authoring that `scripts/check_runtime_style_injection.py` forbids is **absent** from the PR-introduced template lines. (The pre-existing 21 inline `style="..."` matches in the wider template are the same pattern — `left:X%` rail positioning — already budgeted and not touched by this PR.)

5. **`templates/theme.css` is untouched.** The PR diff does not touch `templates/theme.css` — THEME-CLOSURE is preserved. The new `.mx-spine-caveat` block lives in the hero template's `<style>` block (line 198), scope-narrowed to that hero. The body's DEVIATIONS list names this choice: "A new rule in `theme.css` would force a THEME-CLOSURE sweep. `.mx-spine-caveat` is unique to the HK/CN bound rows. Token-scoped only (`var(--line)`, `var(--ink-2)`, `var(--ink-3)`, `var(--r-ctl)`, `var(--fs-micro)`). `theme.css` bytes are unchanged by this PR." The 8-cell evidence matrix confirms the new rule renders correctly in both themes.

6. **The 8-cell evidence matrix satisfies TP-0.** The body cites the captured PNGs in `mockups/evidence/unified-dashboard-b2w2/`:
   - `spine_dark_en_desktop.png` (sha256 `7952dda5…`, 352,819 bytes, 2628×772)
   - `spine_dark_en_mobile.png` (sha256 `3ab074ce…`, 370,556 bytes, 616×1582)
   - `spine_dark_zh_desktop.png` (sha256 `1d86d92a…`, 336,489 bytes, 2628×772)
   - `spine_dark_zh_mobile.png` (sha256 `a1624ec8…`, 322,589 bytes, 616×1480)
   - `spine_light_en_desktop.png` (sha256 `7003bb44…`, 311,048 bytes, 2628×772)
   - `spine_light_en_mobile.png` (sha256 `ebd545a2…`, 352,927 bytes, 616×1582)
   - `spine_light_zh_desktop.png` (sha256 `e1a9dcde…`, 301,187 bytes, 2628×772)
   - `spine_light_zh_mobile.png` (sha256 `0b942f37…`, 293,720 bytes, 616×1480)

   The TP-0 standing requirement is "both themes judged as designs (hierarchy, material depth, semantic color, responsive composition, EN/ZH parity)" — the 8-cell matrix (2 themes × 2 locales × 2 viewports) covers hierarchy, semantic color (ink-2 vs ink-3), responsive composition (1440 desktop vs 390 mobile), and EN/ZH parity. The capture head (`d9a57dd944…`) slice_sha256 pair-matches the final head (`ff5aae17f…`) slice (`d1a914a628b7fe99…`, 9997 bytes) — same spine rendering, evidence-only changes since capture. Dark PNGs were byte-identical to the prior FAB-hidden capture (so they ride the prior commit's tree diff); light cells were recaptured to honour the new ink token.

7. **`color-mix(in srgb, …)` compliance is to the LAW, not the linter.** `scripts/check_design_system.py` `FUNC_COLOR_RE` is `r"\b(?:rgba?|hsla?)\s*\("` — it does **not** match `color-mix`. The body's DEVIATIONS list names this as a linter gap; a separate heal owns it. Compliance here is to the LAW (no colour-function on changed lines), not the linter's blind spot. The PR introduces zero colour-functions on changed lines; pre-existing colour-functions elsewhere in the template are out of scope for THIS audit (they predate the PR and are byte-identical to the same strings on `origin/main` for the rest of the file). A separate audit on the template-wide colour-function census would be a different deliverable, not this one.

8. **`border-radius: var(--r-ctl,8px)` carries a fallback literal — body flags as DEVIATION #4 (MINOR-2).** The `.mx-spine-caveat` rule has `border-radius: var(--r-ctl,8px)` — a fallback `8px` literal. The body's DEVIATIONS list names this as a latest-review MINOR-2 left in place: "Changing it would rewrite the inlined `<style>` in `site/macro.html` and force a full `python -m scripts.build_site` (the previous lawful render was 1136.6s) for a comment/fallback that is not a colour-function miss (`color-mix` is gone; `git grep color-mix -- templates/_unified_dashboard_hero.html.j2` empty). Not a BLOCKER/MAJOR." The fallback is a number, not a colour, and is consumed by `var()` first; this is the standing token-with-fallback idiom and is not a colour-discipline regression.

9. **Theme residual issues: none for THIS PR.** The new `.mx-spine-caveat` block is token-scoped + dark/light-aware via tokens; the `color-mix(in srgb, …)` rules are fully replaced per R-W2-16; the inline `style="..."` attributes are exclusively rail-marker positioning; `templates/theme.css` is untouched. The 8-cell evidence matrix satisfies TP-0. THEME-CLOSURE holds.

**Theme verdict: PASS.**

## Validated-claims findings

The standing validated-claims discipline (`scripts/check_validated_claims.py`, BC-2) gates any affirmative use of `validated` / `已验证` against an allowlist and scans `templates/*.j2`, `templates/*.js`, `templates/*.html`, `site/*.js`, `site/*.html`, `site/prophet/*.json`, and `engine/*.py` display-copy fields.

**Headline: PASS on validated-claims. The PR is a validated-claims IMPROVEMENT — it adds the engine's own display-only caveat stamp onto the row, replaces the generic `aria-label="engine caveat"` with the actual disclosure sentence, and introduces no new affirmative `validated` vocabulary.**

Specific findings:

1. **The PR introduces the engine caveat stamp on every bound row.** Per the DEC-SPINE-SCALE-BINDINGS rationale, "binding the score onto the rail WITHOUT the caveat is the named failure mode." The HK + CN rows now print the engine's own `caveat_en` / `caveat_zh` as a disclosure sentence, prefixed with the as-of date. The HK caveat reads (in full, EN): "Display-only and lighter than the US read: HK has no leading Risk Radar, no VIX term structure (VHSI stands in), and the downturn gauges are uncalibrated. HK is heavily globally driven — US/EM beta and the HKD peg. Context, not a forecast." The CN caveat reads: "Display-only and lighter than the US read: China has no leading Risk Radar, no VIX term structure (QVIX + margin froth stand in), no HY credit spread, and the downturn gauges are uncalibrated. It reads what has already turned — context, not a forecast."

   Each is **allowlisted engine copy on a market_state display surface** — exactly the form `scripts/check_validated_claims.py` recognises as a non-claim. The "Display-only and lighter" prefix and the "Context, not a forecast" suffix are the design-doctrine-permitted "windows, not certainties" forms. The PR does not claim any of these scores is a validated risk-on read; it explicitly stamps them lighter.

2. **`is_display_only: true` on the snapshot.** The persisted snapshot files (`data/hk_market_state/latest.json`, `data/china_market_state/latest.json`) both carry `"is_display_only": true` — the engine's own display-only stamp. The DEC explicitly names the row's display-only contract: `{score, label_en, label_zh, asof, caveat_en, caveat_zh, display_only:true}`. **The display-only stamp travels from the engine through `persist()` to the file and from the file through `load_persisted()` to the template — a falsifier-discipline-respecting chain.**

3. **No new affirmative `validated` / `已验证` / `经验证` / `经过验证` vocabulary introduced in PR-introduced user-visible copy.** A literal grep of the PR-introduced template lines for `validated | confirmed | proven | asserted | verified | 已验证 | 经验证 | 经过验证` returns **zero matches** in the new HK + CN row markup, the new stance strings, the disclosure sentence, or the new CSS. The only `validated`-related vocabulary in the wider template is the pre-existing tooltips on the conviction-board area (predates this PR; out of scope).

4. **`aria-label` carries the disclosure sentence, not a generic hook (R-W2-15).** Pre-fix the row carried `aria-label="engine caveat"` — a generic label that does not actually convey the disclosure content to screen-readers (the screen-reader would announce "engine caveat" without saying what the caveat is). The PR replaces this with the **actual disclosure sentence** as `aria-label`. This is a falsifier-discipline-respecting improvement: the aria-label is now true (it announces what the disclosure actually says), not a generic hook. The test (`test_spine_slice_aria_labels_are_plain_caveat_sentences`) asserts the `aria-label="<full disclosure sentence>"` attribute itself.

5. **Slug rename discipline prevents two wire traps.** The PR renames two slugs:
   - `hk_regime → hk_market_state` — closes the quad-wiring trap. `hk_regime` is a quad engine (growth × inflation cycle location), NOT a 0–100 risk-on score. Wiring `hk_regime` by name would put a cycle location onto a risk-on rail. Renaming to `hk_market_state` makes the wiring trap explicit.
   - `china_a_regime → cn_market_state` — same rationale for CN.

   The PR also **rejects** the slug renames `gov_bonds_regime → <something>` and `commodities_regime → <something>` — those slugs stay UNCHANGED so they remain honest as not-yet-existing contracts. A row that names its own null is more honest than a row that invents a number. **The slug rename discipline is a falsifier-discipline mechanism**: a slug that lies is renamed; a slug that is honest stays still.

6. **No `intl_market_state` re-wire.** The DEC-SPINE-SCALE-BINDINGS alternative rejected the `intl_market_state.market_states()` wire because `intl_market_state` does not cover CN (per `build_site.py:6431-6433`) and turn-state is not a risk-on score (`crash` / `parabolic` would lie on this axis). The PR's `_persisted_ms_view` ingest helper is named distinctly from `_intl_ms_view` precisely so the name does not reopen the trap. The persisted files live at `data/hk_market_state/latest.json` and `data/china_market_state/latest.json` — not `data/intl_market_state/...`. Test asserts the wire trap stays closed.

7. **Bonds + Commodities rows STAY designed-null — a falsifier-discipline-respecting outcome.** The DEC-SPINE-SCALE-BINDINGS rationale names the rejection of four alternatives (different 0-100 affine; quad-to-100 mapping; bond health/compass inverted; commodity index inverted). Each alternative would originate a new signal in the dashboard — out of scope for this binding. The DEC names the ratified product state: "no commensurable scalar; spec mockup already designed this row as null; the complex is internally mixed (gold vs copper) and one marker cannot be both 'precious bid = risk-off' and 'complex uptrend = risk-on.'" **The PR's discipline is: a row that names its own null is more honest than a row that invents a number; the dashed rail + 'Read being updated' chip is the honest null form.**

8. **No allowlist extension needed.** No PR-introduced phrase names a study, a signal, or a validation claim. The new stance strings are plain imperatives (`Watch — don't chase`, `Stand aside`, `Get ready`) — no validation claims. The disclosure sentences are allowlisted engine copy on a market_state display surface. The pre-existing 5 `validated` / `verified` / `confirmed` strings in the wider template (shared nav chrome, conviction board tooltips) are byte-identical to `origin/main` and out of scope for THIS PR's audit — they predate the PR.

9. **No new engine contract invented — composition only.** The PR composes an existing published measurement (HK_PROFILE + CN_PROFILE both publish 0–100 with the same blender and weights) onto the spine rail. The DEC names this: "they already publish a 0–100 with the identical weights and cuts, and they already disclose they are lighter — so binding them is composition of an existing published measurement, not a new signal." The PR does not mint a new study, signal, or contract — it routes an existing measurement into an existing rail. The per-market `persist()` is a no-regress-guarded write; the per-market `load_persisted()` is a no-regress-guarded read; the engine caveat stamp is the engine's own.

10. **Validated-claims residual issues: none for THIS PR.** The PR introduces no new affirmative validation claim AND adds the engine's own display-only caveat stamp onto the row. No allowlist extension is needed. The pre-existing `validated` / `verified` strings in the shared nav chrome are inherited, not introduced. The slug rename discipline is a falsifier-discipline mechanism (lies renamed, honest stays still). The Bonds + Commodities designed-null is the ratified product state.

**Validated-claims verdict: PASS.**

## Overall verdict

| dimension | verdict |
|---|---|
| plain-language | **PASS** |
| theme (TP-0 art direction + THEME-CLOSURE) | **PASS** |
| validated-claims (BC-2) | **PASS** |

**PR #7554 ships clean across the three standing audits — and is in fact an IMPROVEMENT on all three.** This is a half-B B-wave (UD-B2-W2 / DEC-SPINE-SCALE-BINDINGS) spine-binding merge that binds the HK + China A-shares rows on the unified macro dashboard's one-scale five-markets regime spine to the ratified `HK_PROFILE` / `CN_PROFILE` market_state.score outputs (same blender, identical leg weights 0.24/0.18/0.16/0.16/0.14/0.12, verdict cuts 60/42, with the engine's own lighter-evidence caveat stamp traveling onto the row). The PR persists two new snapshot artifacts (`data/hk_market_state/latest.json`, `data/china_market_state/latest.json`) via `engine/market_state.py:persist(market_key=…)` (no-regress guard inherited from the 2026-07-07 stale-regime incident; NYSE freshness stamp US-only by design). Three plain-word stances replace the prior hardcoded designed-null stance: `Watch — don't chase` / `观察，勿追` (RISK_ON), `Stand aside` / `暂时观望` (RISK_OFF), `Get ready` / `准备行动` (MIXED). The as-of date moves out of the row's name cell into the `.mx-spine-caveat` disclosure (R-W2-14). The `aria-label` carries the actual disclosure sentence, not the prior generic `aria-label="engine caveat"` (R-W2-15). The `color-mix(in srgb, …)` on the changed caveat rules is fully replaced with `var(--ink-2)` / `var(--ink-3)` tokens (R-W2-16); the new `.mx-spine-caveat` block is token-scoped, dark/light-aware via `html[data-theme="light"]` override, and lives in the hero template (not `templates/theme.css`) — THEME-CLOSURE preserved. Bonds + Commodities rows STAY designed-null per R-W2-3 (the ratified product state, not a deferral); their slugs stay UNCHANGED so they remain honest as not-yet-existing contracts. The 8-cell dark/light × EN/ZH × 1440/390 visual evidence matrix is committed under `mockups/evidence/unified-dashboard-b2w2/` with sha256 + byte receipts; the capture head `d9a57dd944…` slice_sha256 pair-matches the final head `ff5aae17f…` slice (`d1a914a628b7fe99…`, 9997 bytes). 26 unit tests in `tests/test_unified_dashboard_b2w2.py` cover the persist round-trip, the slug rename, the wire-trap rejection (`intl_market_state` / `hk_regime` / `china_a_regime` must not reappear), the score_log `>=22` row requirement, the verdict-derived stance strings, the `.mx-spine-caveat` disclosure content, the aria-label-as-disclosure-sentence attribute, the colour-function replacement, the as-of-in-name-cell absence, and the slug-stability invariant for Bonds + Commodities.

**Audit result: PASS — no blocking findings.**

---

**Auditor's note (one-shot, half-B scope).** This audit was a single pass against the standing design-doctrine + validated-claims + theme-art-direction laws, in the shape of the prior `qwen_auditor2` audits (`macro_PR-7600.mm.md`, `macro_PR-7607.mm.md`, `macro_PR-7608.mm.md`, `macro_PR-7603.mm.md`, `macro_PR-7602.mm.md`, `macro_PR-7599.mm.md`, `macro_PR-7589.mm.md`, `macro_PR-7585.mm.md`, `macro_PR-7573.mm.md`). The PR is a binding-correctness + plain-language + theme-art-direction + validated-claims repair on the unified macro dashboard's HK + CN spine rows, which makes the audit unusually broad in coverage (every dimension is improved by this PR, not merely held). The 23 files include 8 evidence PNGs (sha256-receipted), 1 evidence manifest, 1 EVIDENCE.yml pointer, 2 new persist artifacts (31 keys each, byte-identical to the engine's `persist()` output), and 1 new DEC record — all content-addressed evidence or engine outputs, not user-authored source. The re-run of `scripts/check_validated_claims.py --selftest` and `scripts/check_design_system.py --mode enforce-added` against the template diff (re-confirmed in this worktree) returned PASS / 0 blocking findings. The body's DEVIATIONS list is read in full: the `border-radius: var(--r-ctl,8px)` fallback literal is a MINOR-2 left in place (not a colour-function miss), the `color-mix(in srgb, …)` linter gap (`FUNC_COLOR_RE` does not match `color-mix`) is named explicitly, the `_persisted_ms_view` ingest helper is named distinctly from the prior `_intl_ms_view` to keep the wire-trap closed, the `.mx-spine-caveat` rule stays in the hero template (not `templates/theme.css`) to keep THEME-CLOSURE out of this PR's scope, and the full `python -m scripts.build_site` re-render only rewrote `site/macro.html` (the other regenerated `site/` and `data/` artifacts from that run were NOT committed in this PR). The pre-existing `validated` / `verified` / `confirmed` strings in the shared nav chrome are explicitly noted as inherited (not introduced) and are out of scope for THIS PR's audit — they predate the PR and are byte-identical to the same strings on `origin/main` for the rest of the file. A separate audit on the wider template's `validated` / `verified` vocabulary or on the colour-function census would be a different deliverable, not this one.
