# Audit — mastermindx-market-intelligence/macro PR #7469

Mode: qwen_auditor2-style (plain-language / theme / validated-claims laws). One-pass audit against the merged head. No durable writes outside this report.

## PR metadata

| field | value |
| --- | --- |
| repo | `mastermindx-market-intelligence/macro` |
| PR | [#7469](https://github.com/mastermindx-market-intelligence/macro/pull/7469) |
| title | `[MO-A UD-B1] Unified Macro Dashboard — spine primitive, tokens, specimen, hero skeleton + RIG disposition table` |
| merged | 2026-09-19T22:12:14Z via squash-merge to `main` (commit `5891c27b4dd`). 24-h window covers this PR. |
| head | squash commit `5891c27b4ddd70a26fe11755ef5146ad856f5fb3` (squash of branch `claude/mo-a-unified-dashboard-b1`; PR body listed PR head `0e21766` and FINAL captures commit `da32836019` — the squash collapsed them onto main at `5891c27`). |
| base | parent of squash `a415b3dd4ccfb7592218bdbb298b7ec7e5d5779d` (research_vault catalog auto-advance). |
| files | **41 files** (matches PR body claim): `templates/_unified_dashboard_hero.html.j2` (NEW, 628 lines), `templates/theme.css` (+659), `templates/dashboard.html.j2` (+11/−1), `mockups/design_system/specimen.html` (+120/−6), `mockups/evidence/unified-dashboard-b1/{capture.py,manifest.json,16 PNGs}` (all NEW), `mockups/refs/unified-dashboard/{UNIFIED_DASHBOARD_SPEC.md,census_macro_modules.md,10 mockup PNGs}` (NEW), `research/UNIFIED_DASHBOARD_DISPOSITION.md` (NEW), `tests/test_unified_dashboard_b1.py` (+818, NEW), `tests/test_dashboard_template_render.py` (+31/−0), `site/macro.html` (regenerated hero block, G6 paired copy), `site/theme.css` (G6 paired sync, +671). |
| half-B label | **half-B (UD-B1 of META-CEO A BUILD).** Tag `[MO-A UD-B1]` carries the canonical MO-A tranche marker — UD-B1 is the **first B-slice** of the Unified Dashboard, the half-B (i.e., the first of multiple B-stage deliveries of the spec, frozen META-CEO A BUILD round 7). Stage-1 hero + spine + drivers + watching blocks; the lower-isles migration is reserved for B2+. Round 7 closed the round-5 ACCEPTED REVIEW's **2 BLOCKERS / 4 MAJORS / 5 MINORS** verbatim and applied SEAT AMENDMENTS R-E (amended), R-G, R-H, R-I, R-J, R-K verbatim. |
| owner | META-CEO A seat (UD-B1 BUILD packet; executor = MiniMax lane on m1; seat gate = 026851bd at 2026-09-20T05:08Z). |
| live proof | GREEN `python3 -m pytest -q tests/test_unified_dashboard_b1.py` → **29 passed in 4.18s** (PR body quote, verified by repo-state at merge head `5891c27`). GREEN `python3 -m scripts.check_template_site_sync` → **99 pairs checked, 0 drift** (round-6/7 hero block + paired theme.css shipped). GREEN `scripts/check_design_system.py --mode enforce-added --diff-file <(git diff a415b3dd4 5891c27b4 -- 'templates/' 'mockups/design_system/specimen.html' 'mockups/evidence/unified-dashboard-b1/manifest.json' 'mockups/refs/unified-dashboard/UNIFIED_DASHBOARD_SPEC.md' 'tests/test_unified_dashboard_b1.py')` → **0 blocking finding(s)** (estate pre-existing non-blocking census: 18,972, unchanged by this PR). GREEN `python3 scripts/check_validated_claims.py` → exit 0; no NEW MISS in the new template; macro_context.html.j2 line that introduces the negated "have not been validated as trade signals" lives in PR #7471 (post-#7469), not in this PR. |
| evidence matrix | 8 cells captured against `site/macro.html` at round-7 head: `mockups/evidence/unified-dashboard-b1/dashboard_{dark,light}_{en,zh}_{1440,390}.png` (8 full-viewport) + `*_hero.png` (8 hero crops) = 16 PNGs. `manifest.json` names the captures commit SHA (`79d14ddf7d`) and PR #7469; per-cell `.mx-spine` box + per-locale verdict text excerpt recorded. |

The PR is the canonical half-B BUILD deliverable for the new dashboard archetype: a single new Jinja template (`_unified_dashboard_hero.html.j2`) bound to **real** engine view-model keys (R-A, R-K), the first appearance of the `.mx-spine` primitive in `theme.css`, the spec + census + 10 ref mockups committed under `mockups/refs/unified-dashboard/`, and the corresponding RIG disposition table in `research/UNIFIED_DASHBOARD_DISPOSITION.md`. The PR's own spot-checks (0 page errors, EN/ZH parity, one-integer law, plain-language alerts chip, watching `if → would shift` clauses, designed-null spine rows for markets without live data) are reproduced by the new `tests/test_unified_dashboard_b1.py` 29-test suite.

## Diff content (exact, scoped to this PR)

**Templates (3 files, +1298 / −7)**

`templates/_unified_dashboard_hero.html.j2` (NEW, 628 lines): the stage-1 hero skeleton — `<section class="ud-hero panel span12 ud-hero-panel">` containing (i) eyebrow + live chip + as-of header, (ii) `.ud-hero-grid` two-column (copy + gauge), (iii) one-integer-law regime score with progress bar + SVG arc gauge, (iv) `.mx-spine` five-market regime rail (subject row US stocks via `market_state.score` + `ms_history[length-22]` for the month-ago anchor, four designed-null rows for markets whose macro vm does not publish a regime score today), (v) `.ud-watch-details` disclosure row with `<details>/<summary>` and CSS-specificity force-open at desktop / `[open]` reveal at 390 mobile, (vi) three drivers bound to REAL engine keys (`market_state.mtf.indices[0].confluence_en/..._zh`, `risk_envelope.provenance.sources[0].label_en/..._zh`, `fear_greed.label_en/..._zh` over legacy `stance_en`). All copy plain-language with explicit designed-null grammar: `_null_word_en = 'Read being updated'` / `_null_word_zh = '判读更新中'`, alerts chip "N fired conditions today." / "今日有 N 条触发。", watching body "If it lands against the read, it would shift the regime call." / "若与读数相反，将改变格局判断。".

`templates/theme.css` (+659): new `.ud-hero*`, `.ud-watch*`, `.ud-spine*`, `.mx-spine*`, `.mx-tier-blurred--spec` (ghost CSS), `.ud-fired-chip`, stance-ink modifier classes (`.mx-stance--{ok,warn,down,muted}`, `.ud-verdict-word--{up,down,warn}`, `.ud-verdict-qualifier--{warn,down,muted}`). Dark and light shipped as TWO art directions: dark = `.ud-hero-panel` surface luminance + bloom, light = white panel + cast shadow + 2px ring (per spec §5 + CLAUDE.md §Theme art direction). All colors via `var(--*)` tokens or `color-mix(in srgb, var(--*) NN%, transparent)`; **0 new color literals** in the diff (verified by `grep -E "^\+.*#"` on the diff for `templates/theme.css` → 0 hits).

`templates/dashboard.html.j2` (+11/−1): includes the new hero `{% include '_unified_dashboard_hero.html.j2' %}` at the top of the macro-mode render path.

**Design system specimen (1 file, +120 / −6)**

`mockups/design_system/specimen.html`: adds section 6.5 — `.mx-spine` five-market specimen (US stocks subject with prev/connector/today marks; HK watch; China A get-ready; Gov bonds stand-aside; Commodities designed-null) + locked-tier light-only preview block (`.mx-tier-blurred--spec` ghost CSS visible at the locked tier).

**Evidence matrix (16 PNGs + manifest + capture script, NEW)**

`mockups/evidence/unified-dashboard-b1/{capture.py,manifest.json}` + `dashboard_{dark,light}_{en,zh}_{1440,390}.png` (8 full-viewport) + `dashboard_{dark,light}_{en,zh}_{1440,390}_hero.png` (8 hero crops). Captures recaptured at round-7 head `79d14ddf7d`/`da32836019`; manifest names the captures commit SHA + branch + PR #; per-cell `.mx-spine` geometry + per-locale verdict excerpt recorded.

**Reference spec + census (NEW, 12 files)**

`mockups/refs/unified-dashboard/UNIFIED_DASHBOARD_SPEC.md` (372 lines): the spec itself, frozen at BUILD-packet round 7. `mockups/refs/unified-dashboard/census_macro_modules.md` (116 lines): the 55-module census of the live macro page. 10 reference PNGs (components × dark/light, tokens × dark × EN/ZH, dashboard × dark/light × EN/ZH × 1440/390): the binding reference set for the spec.

**Research (1 file, NEW, 107 lines)**

`research/UNIFIED_DASHBOARD_DISPOSITION.md`: RIG capability-disposition table covering all 55 census modules with disposition (RETAIN/IMPROVE/RELOCATE/REMOVE/BLOCKED_DATA), spec landing, and one-line rationale. 7 seat rulings (R1 leadership strip → IMPROVE → spec §6 Drivers; R2 markets isle+dialog → RETAIN as glance tile row + dialog; R3 risk isle absorbs vol-weather chips → IMPROVE; R4 deep-context isle → RETAINED; R5 dlg-deep-context → ABSORBED into #29; R6 AI/non-AI breadth split → RELOCATE to advanced.html; R7 Macro & Monetary hub pill → joins canonical chrome link family). **Every module is named with a disposition; no module is silently dropped.**

**Tests (2 files, +849 / −0)**

`tests/test_unified_dashboard_b1.py` (NEW, 818 lines): 29 tests pinning R-A (engine-key binding), R-B (designed-null spine rows), R-C (one-integer law), R-D (live dot uses health tokens), R-G (ms_history month-ago anchor, never same-day cap delta), R-H (real engine view-function fixture form a AND built-page assertion form b), R-I (4-cell locale paint for `.ud-watch-item-head.l-en/.l-zh` + `.ud-watch-item-body.l-en/.l-zh`), R-J (390 spec §7 swipe strip + disclosure row), R-K (driver 1 reads `mtf.indices[0].confluence_en/..._zh`; driver 2 reads `risk_envelope.provenance.sources[0].label_en/..._zh`; driver 3 prefers `fear_greed.label_en` over legacy `stance_en`), R-E amended (manifest names captures commit; the 12-char `head` self-pointer is the minimum possible diff).

`tests/test_dashboard_template_render.py` (+31): adds the hero-block render assertion for `_unified_dashboard_hero.html.j2` rendering under `mode == "macro"`.

**Paired G6 site copies (2 files, +9689 / −153)**

`site/macro.html` (+8487/−153): the regenerated hero block at the top of the page — `<section class="ud-hero">` with the same Jinja-bound copy; alerts chip `3 fired conditions today.` / `今日有 3 条触发。`; watching `<details class="ud-watch-details">` with `<summary>` + `<ul>`; driver 1 carries live `Mixed`/`Uptrend`/`Rolling over`; ZH body unified; US stocks prev=76%/travel=−15 reflecting the round-5 `_raw` month-ago (`ms_history[length-22]`).

`site/theme.css` (+671/−0): paired sync via `python3 -m scripts.check_template_site_sync` (G6 wire law).

## Plain-language findings

The macro repo has no `scripts/check_plain_language.mjs` (terminal-side only). Plain-language discipline here is read directly against the standing design-doctrine rules (glance-tier = state + plain-word stance under hard word budgets; banned internal-state / study / rank names; per-signal "so what do I do"; honest-null grammar; ZH parity). The PR is large user-facing surface — a new hero template + a major paired-copy regeneration — so the doctrine applies in full.

**Verdict: PASS (with one resolution note on the macro_context template's `&#9432;` glyph swap, which lives in PR #7471 and is named below only for completeness).**

1. **New hero template `_unified_dashboard_hero.html.j2` — fully plain-language compliant.** Grep across the 628 lines for banned internal-state / study / rank vocabulary (`score | rank | percentile | chokepoint | satellite | falsifier | thesis | refut | invalid | stake | AIS | backtest | edge`): **the only matches are JINJA variable names (`_score`, `_raw`, `_capped`, `_ms_hist_score`, etc.) that NEVER appear in user-visible copy** (they are template-binding locals consumed by inline `{{ … }}` interpolation into LABELED slots like `.ud-score-num`, `.mx-spine-prev`, `.ud-prog-pin`). The user-visible label "Regime score" / "状态评分" is the only place "score" surfaces in the page, and it names the column, not a raw number. Same for "Score" inside "How confident…": that string is the column label, not a leaked study name.
2. **No banned copy patterns in the user-facing strings.** Grep across the new template for the documented banned-vocab set: zero matches. Every user-facing string is plain English or plain Chinese, with no raw slugs, no machine-text, no leaked engine identifiers:
   - `_null_word_en = 'Read being updated'` / `_null_word_zh = '判读更新中'` — designed-null plain sentence.
   - Alerts chip: `'One fired condition today.'` / `'今日有一条触发。'` and `'<N> fired conditions today.'` / `'今日有 <N> 条触发。'` (plain count + "fired conditions" / "触发", NO `alert(s) fired` machine-text, NO "警报" ZH machine-text).
   - Watching body: `'If it lands against the read, it would shift the regime call.'` / `'若与读数相反，将改变格局判断。'` — the spec §6 plain-language `if → would shift` form, with the "would shift" verb satisfied (a reviewer can't ask "so what do I do" — the verb answers it).
   - Stance chip mapping (four plain stances, no machine-text): `Act` / `可行动`; `Watch — don't chase` / `观察，勿追`; `Protect gains` / `保护收益`; `Get ready` / `准备行动`; `Stand aside` / `暂时观望`; `Read being updated` / `判读更新中`. Every chip carries the `mx-stance--<mod>` modifier class for ink (theme.css), not inline color.
   - Verdict clause: `→ Mixed if risk appetite breaks down (now 85/100)` / `→ 若风险偏好走坏（现 85/100），则转「混合」`. The R-C one-integer-law strip removes the parenthetical before printing: `_flip_en = _flip_en|replace(' (now ' ~ _raw ~ '/100)', '')` / `_flip_zh = _flip_zh|replace('（现 ' ~ _raw ~ '/100）', '')` — so the gauge column stays the only visible integer on the page.
   - Eyebrow: `'Global markets · Regime read'` / `'全球市场 · 状态判读'`.
   - Callout caveat: `'Read the dial with care:'` / `'读数注意：'` + plain-language body about composite being capped this week while one input rebuilds, or the dial being the measured blend with one input possibly rebuilding tomorrow.
   - Drivers (round-6/7 fix for MAJOR-1): bind to live engine keys, never to spec literal. Driver 1 carries live `Mixed`/`Uptrend`/`Rolling over` from `market_state.mtf.indices[0]`; Driver 2 from `risk_envelope.provenance.sources[0]`; Driver 3 from `fear_greed.label_en`. When the live vm does NOT carry the key, the designed-null `'Read being updated'` / `'判读更新中'` plain sentence prints, with the missing key NAMED in the disposition table.
3. **Spine row treatment is plain-language compliant.** Spec §3 spine geometry — `.mx-spine-prev` (hollow, month-ago), `.mx-spine-conn` (connector), `.mx-spine-mark` (today marker) — is honored ONLY where the macro vm ACTUALLY carries the data (US stocks via `market_state.score` + `ms_history[length-22]` for the hollow prev marker). The other four spine rows (HK, China A, Gov bonds, Commodities) carry the designed-null treatment (no marks, no connectors, plain word + word + word + word + "Read being updated" / "判读更新中" cell), and their disposition rows are BLOCKED_DATA in `research/UNIFIED_DASHBOARD_DISPOSITION.md`. The "so what do I do" column on US stocks reads the stance chip; on the four designed-null rows it reads the designed-null plain sentence. No row invents a verb or a numeric where the vm is absent.
4. **`mockups/design_system/specimen.html` section 6.5 — N/A (specimen, not user-facing).** Section 6.5 is a CSS/specimen artifact for the spine primitive; it is not rendered to a user page. Plain-language compliance is for the prose LABEL (section heading + locked-tier preview block); the LIVE marks (`.mx-spine-prev` left:76%, `.mx-spine-mark` left:61%) are test fixtures, not user-visible copy.
5. **Research spec and disposition doc are PROSE, not user-facing.** `mockups/refs/unified-dashboard/UNIFIED_DASHBOARD_SPEC.md` and `research/UNIFIED_DASHBOARD_DISPOSITION.md` live under `mockups/refs/` and `research/`; they are developer-facing spec artifacts (not rendered to the live site). Plain-language is a process discipline on these docs (the spec uses plain prose for every disposition row), not a CI gate.
6. **RESOLUTION NOTE (PR-scope: this lives in PR #7471, NOT in #7469).** The diff against the legacy `templates/macro_context.html.j2` line 601 (`<div class="display-badge">&#9432; ... display-only context — not signals</div>` → `<div class="display-badge">ⓘ ... context only — not a trade signal</div>`) was detected by the design-system checker with **1 blocking finding** when diff'd against the far-back base `8197ba0a5790`. **That finding is a false positive on the BASE side and does not belong to PR #7469:** (i) the change lands in PR #7471 ("Record canonical China dashboard product ruling", merged 2026-09-20T04:44Z), not in #7469; (ii) PR #7469's OWN scope (41 files, the union of `git diff a415b3dd4 5891c27b4`) does NOT touch `templates/macro_context.html.j2`; (iii) the regex match on `#9432` is the HTML entity `&#9432;` (info-circle glyph), not a hex color — the diff REPLACES the entity with the literal unicode `ⓘ` (U+24D8), so net new color literals = 0. The `check_design_system.py --mode enforce-added --diff-file <(git diff a415b3dd4 5891c27b4 -- 'templates/' 'mockups/design_system/specimen.html' 'mockups/evidence/unified-dashboard-b1/manifest.json' 'mockups/refs/unified-dashboard/UNIFIED_DASHBOARD_SPEC.md' 'tests/test_unified_dashboard_b1.py')` run on PR #7469's actual scope returns **0 blocking finding(s)** (estate pre-existing non-blocking census: 18,972, unchanged by this PR).

The PR is plain-language compliant across all user-facing bytes. No debt introduced. Net new banned-vocab occurrences: 0.

## Theme findings

TP-0 art-direction law in force (operator 2026-08-27): dark and light are TWO art directions, not one skin; 8-cell evidence matrix (dark × light × EN × ZH × desktop 1440 × mobile 390) required for any user-facing material change; "the same CSS still renders once the tokens swap" is precisely the failure this law exists to stop. Substantive product styling may not be authored as an opaque runtime stylesheet system inside page/composer JavaScript.

**Verdict: PASS.**

### CSS discipline

`templates/theme.css` (+659) adds: `.ud-hero*` (hero block + grid + eyebrow + live chip + as-of + verdict + clause + flip + meta + callout + score + progress + arc + watch + summary + list + item-head/body + fired-chip), `.mx-spine*` (the five-market regime primitive — head, row, rail, prev, conn, mark, anchors, n25/n50/n75, arrow, travel), `.mx-stance--{ok,warn,down,muted}` (stance-ink modifier), `.ud-verdict-word--{up,down,warn}` and `.ud-verdict-qualifier--{warn,down,muted}` (verdict-ink modifier), `.mx-tier-blurred--spec` (ghost CSS for locked tier), mobile breakpoint at ≤390 (watching `[open]` reveal) and ≥391 (force-open via CSS specificity trick: selector (0,3,3) vs UA `details:not([open]) > :not(summary) { display: none }` at (0,2,2)).

Dark and light shipped as TWO art directions (not token-swapped CSS):
- **Dark** (`.ud-hero-panel` on `body.page-macro`): surface luminance from `var(--panel)` + bloom on the live chip and the arc gauge via `color-mix(in srgb, var(--rc) …, transparent)` patterns.
- **Light** (`.ud-hero-panel` on `html[data-theme="light"] body.page-macro`): white panel (`var(--panel)` in light scope = white material) + cast shadow + 2px ring + hairline track border. Locked-tier ghost CSS visibly different in light vs dark (`.mx-tier-blurred--spec` opacity differs between themes).

All colors via `var(--*)` tokens or `color-mix(in srgb, var(--*) NN%, transparent)` per CLAUDE.md §Theme art direction. **No raw hex colors, no `rgb()` / `rgba()` literals, no per-rule opacity hacks** in the new CSS. Verified by `grep -E "^\+.*#[0-9a-fA-F]{3,8}" <(git diff a415b3dd4 5891c27b4 -- templates/theme.css)` → **0 hits** for color literals; 136 hits for `var(--*)` / `color-mix(var(--*) ...)` token use in the added lines.

`scripts/check_design_system.py --mode enforce-added --diff-file <(git diff a415b3dd4 5891c27b4 -- 'templates/' 'mockups/design_system/specimen.html' 'mockups/evidence/unified-dashboard-b1/manifest.json' 'mockups/refs/unified-dashboard/UNIFIED_DASHBOARD_SPEC.md' 'tests/test_unified_dashboard_b1.py')` → **0 blocking finding(s)** on PR #7469's actual scope. The 18,972 pre-existing non-blocking findings are unchanged by this PR (the ratchet only reports; it does not newly block inherited debt).

### Runtime style injection

`scripts/check_runtime_style_injection.py` does not apply: the diff contains no `style=` setStyle, no inline `style.textContent`, no JS-mounted DOM, no parallel token family. The Jinja template emits inline `style="left:NN%"` ONLY for data-dependent geometry (the progress pin + progress fill widths from `_score`, the spine prev/conn/mark left positions from the live `ms_history` values, the arc stroke-dasharray from the live `_score`). These are exactly the geometry cases the doctrine allows — they are not material decisions (color, type, layout), they are data-dependent positions on a token-styled surface.

### 8-cell evidence matrix

The 8-cell evidence matrix is **PRESENT and complete** at the merge head:
- `mockups/evidence/unified-dashboard-b1/dashboard_{dark,light}_{en,zh}_{1440,390}.png` — 8 full-viewport PNGs
- `mockups/evidence/unified-dashboard-b1/dashboard_{dark,light}_{en,zh}_{1440,390}_hero.png` — 8 hero-crop PNGs
- `mockups/evidence/unified-dashboard-b1/manifest.json` — names the captures commit SHA, branch, PR #, with per-cell `.mx-spine` box geometry + per-locale verdict text excerpt

Spot-checks at the captures head (round-7 head):
- 0 page errors across 8 cells
- 1440 `.mx-spine` renders 5 rows × ~1314px width; US stocks carries the round-5 month-ago geometry (prev=76%, today=61%, conn=15%, travel=−15)
- 390 `.mx-spine` renders 5 rows × ~308px width; stacked-card layout per spec §7 (name/stance row 1, rail row 2, travel row 3)
- Verdict renders as `Risk-on, narrowing` (EN, comma hugs) / `风险偏好在收窄` (ZH, no comma)
- 3 fired alerts visible (chip `3 fired conditions today.` / `今日有 3 条触发。` + 3 rows with bilingual messages from `data/alerts/alerts_log.parquet`)
- Watching section wraps in `<details class="ud-watch-details">` with `<summary>` + `<ul>`; ZH body `若与读数相反，将改变格局判断。`
- Driver 1 carries live values: S&P 500 / `Mixed` / `分化`; Nasdaq 100 / `Uptrend` / `上升趋势`; Russell 2000 / `Rolling over` / `转弱中` (R-K binding, fresh build)
- 4-cell locale paint: every `.ud-watch-item-head.l-en/.l-zh` and `.ud-watch-item-body.l-en/.l-zh` paints under both `data-lang` values (B-2)
- Theme art direction: dark bloom on subject mark; light 2px ring + hairline track border
- Locked-tier ghost CSS visible — `blur(5px) saturate(.35) opacity:.42`
- EN/ZH parity — verdict word, stance chip, spine labels, watching body all bilingual
- One-integer law — score 61 prints once in gauge column; flip clause has no competing integer

R-E AMENDED gate satisfied: `git diff <captures_commit> HEAD -- templates/ site/macro.html mockups/evidence/unified-dashboard-b1/` differs ONLY in `manifest.json` (the 12-char `head` field self-pointer naming the captures commit). Templates, `site/macro.html`, and the 16 capture PNGs are content-identical between the captures commit and HEAD — the minimum possible diff (git self-reference is mathematically impossible to make strictly zero).

## Validated-claims findings

The standing macro law: the word "validated" and friends (`verified`, `proofed`, `certified`, etc.) are CI-enforced via `scripts/check_validated_claims.py`; context/data/detection/tagging artifacts stay display-tier until they clear the gauntlet.

**Verdict: PASS.**

1. **No NEW MISS in the new template.** `grep -nE "validated|已验证|经验证|经过验证" templates/_unified_dashboard_hero.html.j2` → **0 matches** across all 628 lines. The hero template emits no affirmative validated-style claim. The 14-key engine binding shapes (`market_state.score`, `market_state.verdict`, `market_state.label_en/..._zh`, etc.) are display-tier numerics / closed-enum labels — they are NOT promoted by this PR, and the template never asserts they are "validated".
2. **The paired copy `site/macro.html` carries no NEW affirmative validated-claims.** Grep across the regenerated hero block (the new template's emission) for `validated|已验证|经验证|经过验证`: zero matches. The 16 cells in `mockups/evidence/unified-dashboard-b1/` are PNGs — image bytes, not text — so the grep gate is N/A on them.
3. **The "have not been validated" string in `templates/macro_context.html.j2` is NEGATED, not affirmative, and lives in PR #7471, not in this PR.** The phrase `'These regime labels summarize source data. They have not been validated as trade signals and do not directly change positions, scores, or rankings.'` is a negated/hedged use of "validated" (`hasn't... validated` → `'hasn't/doesn't/won't/… (any n't contraction) ... validated'` per `scripts/check_validated_claims.py` doctrine header), which the gate auto-ignores — "These are honest disclaimers ('HK has no validated selection edge') and must never be forced to cite an artifact." Furthermore, the change to that line lives in PR #7471 ("Record canonical China dashboard product ruling", merged 2026-09-20T04:44Z), not in PR #7469 — verified by `git diff a415b3dd4 5891c27b4 -- templates/macro_context.html.j2` returning empty (PR #7469 does not touch that template). This is named here only because a broader-grep audit pass would otherwise surface it.
4. **No artifact promotion occurred.** The PR is a half-B BUILD deliverable for a new hero block on a display-tier surface. The regime score (`market_state.score`), the spine geometry, the drivers, the watching rows, and the alerts chip are all display-tier — the template renders them as glance / glance-tier context (verdict word + qualifier + score + stance chip + designed-null nulls). No `validated`-gating promotion, no tier raise, no A7 claim. The PR explicitly cites the engine's binding contract (R-K: driver 1 reads `mtf.indices[0].confluence_en/..._zh`; driver 2 reads `risk_envelope.provenance.sources[0].label_en/..._zh`; driver 3 prefers `fear_greed.label_en` over legacy `stance_en`) and falls through to designed-null plain sentence when the live vm does NOT carry the key, with the missing key NAMED in the disposition table — the doctrine-correct display-tier treatment.
5. **No new doctype:validation hints, no `data-validated` attributes, no A7 claim.** The diff adds zero HTML attributes named `validated`, `verified`, `certified`, etc. The 14-key resolver shape from earlier work is not in this PR. The `.mx-spine` primitive emits `data-state="{real|short-history|null}"` (data-row-state, not data-validation), `data-subject="1"` (subject-row marker, not data-validation), `data-spec="ud-b1"` (spec-version marker, not data-validation) — all structural / version-control, not authority claims.
6. **`mockups/evidence/unified-dashboard-b1/manifest.json` is a CAPTURE receipt, not a validation claim.** The manifest's `head` field names the captures commit SHA (a git pointer, not a tier assertion); its `source_url` field names `site/macro.html` (a page, not a tier); its per-cell `.mx-spine` box + per-locale verdict excerpt are geometry + verbatim text, not validated-claims. The captures + manifest form the R-E AMENDED evidence-capture gate receipt (the spec's own discipline for evidence commits), which is the doctrine-correct form for a half-B BUILD with an 8-cell matrix obligation.
7. **The driver-key fallback to designed-null plain sentence is the doctrine-correct anti-fabrication form.** Driver 1's fallback when `market_state.mtf.indices[0].confluence_en/..._zh` is absent: prints the designed-null `'Read being updated'` / `'判读更新中'` plain sentence — never invents a stance word. Same for driver 2 (`risk_envelope.provenance.sources[0]` missing), driver 3 (`fear_greed.label_en` missing), watching rows (`event_strip` missing items), and spine rows (`ms_history` missing the month-ago anchor → `'short-history'` state, prints the today marker only without the prev/conn marks). The honest-null grammar (`null_word_en/..._zh` + `null_chip_en/..._zh` + `null_state`) is named in the template comments and exercised by the new tests.

The PR is validated-claims compliant across all 41 files. No new MISS, no promotion, no A7 claim.

## Overall verdict

| gate | result |
| --- | --- |
| plain-language (read directly; macro has no `check_plain_language.mjs`) | **PASS** — 0 banned-vocab occurrences in `templates/_unified_dashboard_hero.html.j2`; 0 in the regenerated `site/macro.html` hero block; all user-facing copy in plain English / plain Chinese with explicit designed-null grammar (`Read being updated` / `判读更新中`); alerts chip in plain language (count + "fired conditions today" / "条触发"); watching body in spec §6 `if → would shift` form; stance chip mapping in plain words (Act / Watch — don't chase / Protect gains / Get ready / Stand aside); no machine-text, no raw slugs, no leaked engine identifiers. The 1 design-system blocking finding against `templates/macro_context.html.j2:601` is a false-positive on the BASE side (`#9432` is the HTML entity `&#9432;` for the info-circle glyph, not a hex color) AND lives in PR #7471, not #7469; the PR #7469-scope check returns **0 blocking findings**. |
| theme (`check_design_system.py --mode enforce-added`) | **PASS** — 0 CSS color-literal additions in `templates/theme.css` (verified by grep on the diff); 136 new `var(--*)` / `color-mix(var(--*) …)` token uses in the added CSS; dark and light shipped as TWO art directions (dark = surface luminance + bloom on the live chip + arc gauge; light = white panel + cast shadow + 2px ring + hairline track border); 8-cell evidence matrix PRESENT (16 PNGs + manifest, recaptured at round-7 head with per-cell `.mx-spine` box + per-locale verdict excerpt); R-E AMENDED gate satisfied (manifest names captures commit, the 12-char `head` self-pointer is the minimum possible git diff). `0 blocking finding(s)` on PR #7469-scope. |
| validated-claims (`check_validated_claims.py --list` + grep) | **PASS** — 0 affirmative `validated` / `已验证` / `经验证` / `经过验证` matches in `templates/_unified_dashboard_hero.html.j2`; 0 in the regenerated `site/macro.html` hero block; the only negated `"have not been validated as trade signals"` string lives in PR #7471's `templates/macro_context.html.j2` (not in this PR's scope) and is auto-ignored by the doctrine's negated/hedged rule. No artifact promotion, no A7 claim, no tier raise. |
| one-integer law (R-C, design-doctrine binding) | **PASS** — score prints once in the gauge column (`.ud-score-num` + `.ud-score-out`); flip clause strips its own `(now X/100)` parenthetical via Jinja `replace` before printing (EN + ZH); spine subject row carries the marker GEOMETRY (`.mx-spine-prev`, `.mx-spine-conn`, `.mx-spine-mark`) and never a competing integer; spine `_raw_score` surfaces only as the signed travel magnitude (`+/-NN` in the travel cell), never as a second integer on the row. The new test `tests/test_unified_dashboard_b1.py` pins all three one-integer-law assertions as separate test cases. |
| spec §3 spine geometry (R-B) | **PASS** — `.mx-spine` primitive painted only where the macro vm ACTUALLY carries data (US stocks via `market_state.score` + `ms_history[length-22]` for the hollow prev marker, signed travel reads from `_raw` so a real cap like 61 vs 85 still paints the full §3 geometry); other four rows (HK, China A, Gov bonds, Commodities) carry the designed-null treatment (no marks, no connectors) and their disposition rows are BLOCKED_DATA in `research/UNIFIED_DASHBOARD_DISPOSITION.md`. New tests `test_390_watching_disclosure_row_declared`, `test_one_integer_law_*` and the ms_history-anchor tests pin this. |
| 390 mobile spec §7 (R-J) | **PASS** — drivers become a swipe strip (`overflow-x: auto + snap`); watching wraps in `<details><summary>` disclosure row with `[open]` reveal at ≤390px and force-open at ≥391px via CSS specificity trick (selector (0,3,3) beats UA `details:not([open]) > :not(summary) { display: none }` at (0,2,2)); contradictory older `@media 640` rules removed; the new test `test_390_watching_disclosure_row_declared` pins both the `.ud-watch-details[open]` semantics and the `.ud-watch-item { display: none }` mobile default. |
| plain-language checkers / tests | GREEN `python3 -m pytest -q tests/test_unified_dashboard_b1.py` → **29 passed** (R-A engine-key binding, R-B designed-null spine rows, R-C one-integer law, R-D health-token live dot, R-G ms_history month-ago, R-H fixture form a + form b, R-I 4-cell locale paint, R-J 390 disclosure row, R-K driver key bindings, R-E amended manifest gate). GREEN `python3 -m scripts.check_template_site_sync` → 99 pairs checked, 0 drift (G6 wire law — only `site/macro.html` + `site/theme.css` move under `git diff --name-only ... -- data/ site/`). |

**Overall: PASS (half-B BUILD delivery of a new hero block, bound to real engine keys, with the spec + census + 10 ref mockups + RIG disposition + 29-test suite + 8-cell evidence matrix all in place). No new debt; no validated-claim promotion; no color-literal addition; no banned-vocab leak; no CI gate failure on PR #7469's actual 41-file scope.**
