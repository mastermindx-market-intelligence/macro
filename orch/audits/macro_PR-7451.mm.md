# Audit — mastermindx-market-intelligence/macro PR #7451

Mode: qwen_auditor2-style (plain-language / theme / validated-claims laws). One-pass audit against the merged head. No durable writes outside this report.

## PR metadata

| field | value |
| --- | --- |
| repo | `mastermindx-market-intelligence/macro` |
| PR | [#7451](https://github.com/mastermindx-market-intelligence/macro/pull/7451) |
| title | `[MO-B F09 W8-1] Cash runway on the debt-maturity ladder: one annual filing's cash, operating cash and equipment spend → months of runway + next-12-month cover (MO-PAID-059 / MO-DELTA-018 second slice)` |
| branch head | `dfaae20dfbb02c3d766f7492dcfa8bbd437e9f47` (round 2) on `claude/mo-b-w8-1-cash-runway-2026-09-19` |
| round-1 head | `fdc4c3c43b8e461abac08b23fbc63913776d1b73` (preserved below round-2 head; no force-push, no merge of `origin/main`) |
| mergedAt | 2026-09-20T00:40:06Z (24-h window — most recent half-B build PR in window) |
| files | 10 changed, 1204 insertions, 14 deletions (round-2 `git diff --stat` shape: `engine/cash_runway.py +352`, `scripts/build_debt_maturity.py +22/-12`, `scripts/build_stock_library.py +61`, `scripts/build_ticker_pages.py +1`, `templates/_debt_maturity.html.j2 +63/-2`, `templates/theme.css +12`, `tests/fixtures/cash_runway/aapl_cash_trimmed.json +119`, `tests/fixtures/cash_runway/synthetic_burn.json +53`, `tests/test_cash_runway.py +511`, `.github/ci/legacy-jobs.yml +10`) |
| merge commit | `dfaae20dfbb02c3d766f7492dcfa8bbd437e9f47` |
| base | fresh `origin/main` at `03f297b630` (per body) |
| half-B label | "half-B" = MO-B / data-side half. The PR is a **NEW BUILD**: a new `engine.cash_runway` pure module (XBRL companyfacts → free cash flow → months-of-runway + next-12-month cover %), wired into the existing debt-maturity ladder panel as a sibling `.dmw-runway` block, with a Jinja template fragment, a theme.css rule, a producer resolver, a CI-only ticker-page context pass, and a RED-first pytest surface (3 fixtures, 511 lines). |
| owned files vs `gh pr view --json files` | matches exactly — the 10 listed files are the only 10 files in the diff. No `site/`, `mockups/`, `data/`, `docs/`, `lib/` bytes touched. |
| jumped link gate | body is explicit: "the `_dm_status` jump-link gate around line 3435 is unchanged (the jump link is the ladder's)" — round-2 review MAJOR stayed addressed by NOT editing the ladder's jump-link. |
| red proof | `python3 -m pytest -q tests/test_cash_runway.py tests/test_debt_maturity.py` at round-1 head `fdc4c3c4` (without the round-2 widening) → `10 failed, 90 passed, 1 skipped in 10.71s`; each failure names a round-2 MAJOR (KeyError annual_burn_usd, runway_display enum, attributed sentence, composed sentence, runway inside section, $90M not $1.2B, ladder stale phrase, cash_runway in template context, source ordering). |
| green proof | `python3 -m pytest -q tests/test_cash_runway.py tests/test_debt_maturity.py` at round-2 head → `100 passed, 1 skipped in 12.64s`. The single skip is a pre-existing skip in `test_debt_maturity` (not introduced by this PR). |
| render proof | three EN+ZH sentences per the round-2 ruling: (a) AAPL self-funding + 323% cover, attributed to "In the year ending 2025-09-27 (10-K)"; (b) synthetic burn $50.0M / $2.5M monthly / 20 months, attributed to "2024-12-31 (10-K)"; (c) not_loaded "Cash runway not loaded yet / 现金跑道尚未加载". No raw float leakage (`0.0025`, `20.0 months` not in HTML); no English inside the ZH sentence. |
| fixture provenance | `aapl_cash_trimmed.json` carries a `_provenance` object with three SEC `data.sec.gov/api/xbrl/companyconcept/CIK0000320193/...` URLs, `fetched_at_utc: 2026-09-19T22:44:26Z`, `trimmed_by: cash_runway fixture builder`. `synthetic_burn.json` is flagged `_synthetic: true` with CIK `0000099999` (a CIK never used by a real filer). |

The PR body is one of the more disciplined two-round build shapes in the recent queue. Round 1 (preserved head `fdc4c3c4`) established the module + producer + template + 290-line test surface. Round 2 (`dfaae20d` on top of round 1 — no rewrite, no force-push, no merge of `origin/main`) widens by exactly one context line (`"cash_runway": (blob or {}).get("cash_runway")` in `build_ticker_pages.py`), adds the new fields (`annual_burn_usd`, `annual_burn_display`, `monthly_burn_display`, `more_than_10_years` enum), the attribution-aware "In the year ending YYYY-MM-DD (10-K)" prefix, the `most recent annual filing` stale suffix, and the 211 lines of test widening that pin each of those as a separate assertion. Deviations are explicit and small: synthetic CIK `0000099999` (r1 contract said `0000009999`; file kept), and the `_dm_status` jump-link gate left untouched per the standing ruling. Open questions: none.

## Diff content (exact, per direction)

`engine/cash_runway.py` (+352, ADDED) — pure functions over SEC XBRL companyfacts. Closed-enum module:
- Three XBRL tags hard-coded as a frozen tuple: cash (`CashAndCashEquivalentsAtCarryingValue`), operating cash flow (`NetCashProvidedByUsedInOperatingActivities`), equipment spend (`PaymentsToAcquirePropertyPlantAndEquipment`).
- Identity is by CIK only — `_canonical_cik()` requires digit-only ≤ 10-char non-zero; the `cik` field embedded inside the companyfacts payload is cross-checked against the caller-supplied `cik`, mismatch → `identity_mismatch`.
- Annual filings only (`10-K` / `10-K/A` / `20-F` / `40-F`); FY periods only (`fp == "FY"`); dedup on `(accn, end)`; winner = newest by `(filed desc, end desc)`.
- Cash fact must match the winner on BOTH `accn` AND `end`; mismatch → `no_cash_facts` with per-tag `drop_reasons` (`unit_conflict` / `unit_not_usd` / `period_mismatch` / `absent`).
- Free cash flow = OCF − capex. When FCF < 0 → annual burn, monthly burn, months of runway. `runway_display` is a closed enum token (`self_funding` / `months` / `more_than_10_years`), never a user-facing English phrase — the template composes the EN/ZH sentence from the token + the numeric `runway_months`.
- `near_term_cover_pct` = `round(100 * cash / y1_bucket_usd)` when ladder y1 is reported and > 0; otherwise null.
- `_STALE_DAYS = 550`; `period.stale` flag = `(as_of − period.end).days > 550`.
- Display helper `_usd_dollars()` produces `$X.XB / $X.XM / $X.XK / $X` only — never a raw float in user copy.

`scripts/build_debt_maturity.py` (+22/-12, MODIFIED) — combines the ladder's six debt-maturity tags with the runway's three. `_RUNWAY_TAGS` tuple is appended to `_TAGS` → `_ALL_TAGS`. The "bounded" comment is updated from "six" to "nine". The cache writer `slim` loop, the standalone fetch loop, and the `n_clean_responses == len(_TAGS)` gate are all widened to `_ALL_TAGS` so the partial-cycle "confirmed_no_filings" gate continues to require every tag's response (200 or 404), not just at least one — the bug the round-2 review MAJOR-2 cited (one 404 plus five 429s fabricating `confirmed_no_filings`) stays closed at nine tags.

`scripts/build_stock_library.py` (+61, MODIFIED) — new `_resolve_cash_runway()` resolver that mirrors `_resolve_debt_maturity()`'s taxonomy: `not_applicable` (crypto / ETF-macro / `-USD`), `unresolved` (no CIK), `not_loaded` (CIK resolved, no cache), `confirmed_no_filings` (cache wrote a positive empty result), and the engine's own statuses. Wired into the stockdata JSON via `rec["cash_runway"] = _resolve_cash_runway(ticker, sector, _dt.date.today(), rec["debt_maturity"])`. The exception path emits a `::warning title=stock-library cash-runway producer fault::<ticker> cash-runway lookup raised <class>: <msg> -- degrading to not_loaded, never not_applicable` annotation (bare `print(..., flush=True)` — the GitHub-annotation-must-start-the-line contract is honored, and the exception NEVER widens to `not_applicable`, so an ETF-crypto-style structural exemption cannot leak from a producer fault).

`scripts/build_ticker_pages.py` (+1, MODIFIED) — exactly one context line: `"cash_runway": (blob or {}).get("cash_runway")`. The `_dm_status` jump-link gate around line 3435 is intentionally untouched per the standing ruling.

`templates/_debt_maturity.html.j2` (+63/-2, MODIFIED) — adds a sibling `<div class="dmw-runway" data-panel="cash_runway.v1">` block inside the existing `<section id="debt-maturity">`, BEFORE the `.mod-ft` footer (asserted by `test_runway_sits_inside_section_before_footer` and `test_runway_source_sits_inside_section_before_footer`). Six render branches by status:
- `reported` + `runway_display == 'self_funding'` → "no burn to measure" + optional 12-month cover sentence.
- `reported` + `runway_display == 'more_than_10_years'` → "Cash on hand: $X.XB / $X.XM" + annual burn + "more than 10 years" + optional cover sentence.
- `reported` + `runway_display == 'months'` → "Cash on hand" + monthly burn + "about N months" (with `_n` formatted as `%.1f` when runway < 12, otherwise `%d` rounded) + optional cover sentence.
- `no_cash_facts`, `no_filings`, `not_loaded`, `unresolved`, `identity_mismatch` → bilingual `.dmw-null` / `.dmw-nullwhy` transparent-null prose.

Every `reported` branch prepends `In the year ending YYYY-MM-DD (10-K) / 截至 YYYY-MM-DD 的财年（10-K）` (EN/ZH), and appends `· most recent annual filing / · 最近一期年度文件` when `period.stale` is true.

`templates/theme.css` (+12, MODIFIED) — a single new rule `.dmw-runway{margin-top:28px;padding-top:22px;border-top:1px solid color-mix(in srgb,var(--hair) 60%,transparent);}` plus the light-theme override `[data-theme="light"] .dmw-runway{border-top-color:var(--hair);}`. The block reuses the ladder's `.dmw-lede` / `.dmw-null` / `.dmw-nullwhy` tokens and the same `mod-ft` footer. The CSS prose comment is explicit: "Reuses the ladder's ... tokens ... Dark/light treatment is inherited from the ladder panel above (same section, same #debt-maturity / #cash-runway parent). No new colours — same tokens as the ladder rules above."

`tests/fixtures/cash_runway/aapl_cash_trimmed.json` (+119, ADDED) — three FY 10-K periods from AAPL's `0000320193-25-000079` accession (FY2025/FY2024/FY2023 filings), trimmed to the three runway tags. `_provenance` block names the three SEC companyconcept URLs.

`tests/fixtures/cash_runway/synthetic_burn.json` (+53, ADDED) — `_synthetic: true`, CIK `0000099999` (never a real filer), one FY 10-K with FCF = $10M − $40M = −$30M, cash $50M → 20 months runway.

`tests/test_cash_runway.py` (+511, ADDED) — three test classes (`TestExtractCashRunway`, `TestCashRunwayRender`, `TestCashRunwayWiring`) covering: import path, FCF math, runway enum token, period-attribution, "more than 10 years" uses annual burn not cash (M3), stale phrase (M5), runway sits inside section before footer (M2), `cash_runway` reaches the template context, and source ordering pins the .dmw-runway block before .mod-ft inside </section>.

`.github/ci/legacy-jobs.yml` (+10, MODIFIED) — appends `engine/cash_runway.py` to four curated `engine/*.py` glob lists (steps that already import the existing `engine/debt_maturity.py` so the import closure reaches the new module without a new job) and appends `tests/test_cash_runway.py` to the same step that runs `tests/test_debt_maturity.py`. The comment on the test-list step names the packet: "tests/test_cash_runway.py (packet B-F09-3 second slice / W8-1, contract-delta 2026-09-19) rides this step for the same reason as test_debt_maturity.py".

## Plain-language findings

The macro repo has no `scripts/check_plain_language.mjs` equivalent — that gate is terminal-side. The macro-side nearest equivalents are `scripts/check_validated_claims.py` (front-facing vocabulary) and the design-checker family. Plain-language discipline here reduces to: did this PR introduce any new user-visible raw slug / untranslated string / English-only leak? Did the new bilingual copy honor the design doctrine (l-en / l-zh parity, attribution-aware verbs, banned glance-tier vocab)?

**Verdict: PASS — the new copy is bilingual, attribution-aware, and free of banned glance-tier vocabulary.**

PR footprint = 1 user-facing template file (`templates/_debt_maturity.html.j2`); 1 theme file (`templates/theme.css`, prose-only); the rest are engine / scripts / tests / fixtures / CI YAML.

1. **Bilingual parity on every user-facing sentence.** Every new `<p class="dmw-lede">` / `<p class="dmw-null">` / `<p class="dmw-nullwhy">` string is wrapped in the project's `t(en, zh)` helper and is rendered as sibling `<span class="l-en">` / `<span class="l-zh">` blocks (the existing ladder uses the same pattern). The 13 new user-facing sentences added by this PR:
   - "In the year ending YYYY-MM-DD (10-K) it brought in more cash than it spent, including equipment — no burn to measure." / "截至 YYYY-MM-DD 的财年（10-K）其现金流入多于支出（包括设备支出），暂无可衡量的消耗。"
   - "In the year ending YYYY-MM-DD (10-K) cash on hand covers N% of the debt coming due in the next 12 months." / "截至 YYYY-MM-DD 的财年（10-K）现金可覆盖未来12个月内到期债务的 N%。"
   - "Cash on hand: $X. In the year ending YYYY-MM-DD (10-K) it spent $X more than it brought in after buying equipment — at that pace the cash lasts more than 10 years." / ZH mirror.
   - "Cash on hand: $X. In the year ending YYYY-MM-DD (10-K) it spent $X more per month than it brought in after buying equipment — at that pace the cash lasts about N months." / ZH mirror.
   - "Its filings are available, but they do not include all three facts needed to measure cash burn — cash on hand, operating cash flow, and equipment spend." / ZH mirror.
   - "Not every company reports all three of these facts. Nothing is being estimated in their place." / ZH mirror.
   - "No SEC filings available for this listing." / "该证券暂无可用的 SEC 文件。"
   - "This panel only reads filings an issuer makes with the SEC. None were found for this one, so there is nothing to show yet." / ZH mirror.
   - "Cash runway not loaded yet." / "现金跑道尚未加载。"
   - "This panel is still catching up to the SEC filings for this listing. Check back soon — nothing is being estimated in its place." / ZH mirror.
   - "We do not have an SEC filing record for this listing." / "我们目前没有该证券的 SEC 备案记录。"
   - "This panel only reads filings an issuer makes with the SEC, and this listing has not been matched to one." / ZH mirror.
   - "We could not confirm which company filed for this listing, so the cash runway is not shown." / ZH mirror.
   - "This panel only shows figures once the identity of the filer is verified. Nothing is being guessed in its place." / ZH mirror.
   - "most recent annual filing" / "最近一期年度文件" (stale suffix).
   EN and ZH pair every sentence. There is no English-only string and no ZH-only string in the user-visible copy.
2. **Attribution-aware prefix replaces the old "Last year" / "去年" anonymous relative time.** The pre-existing ladder copy used "Last year it brought in more cash..." / "去年 ..."; the new runway copy is forced through `_year_en = 'In the year ending ' ~ _end ~ ' (' ~ _form ~ ')'` and the matching ZH so every numeric claim is anchored to a concrete period (e.g. "In the year ending 2025-09-27 (10-K) it brought in more cash than it spent, including equipment — no burn to measure"). The test `test_render_aapl_self_funding_attributed` pins this with `assert "Last year" not in en` and `assert "去年" not in zh` — the old anonymous-relative-time wording is explicitly disallowed going forward. This is the correct plain-language move (a specific date + form type is more readable than "last year" once a reader is comparing two consecutive annual filings).
3. **No raw float or raw-period in visible copy.** The Jinja template composes every numeric string from closed-enum `runway_display` token + the engine's pre-formatted `_usd_dollars()` display helper. The test `test_render_synthetic_burn_composed_months` pins this with `assert "0.0025" not in html` and `assert "20.0 months" not in html` — so a raw float like `$0.0025B` (the raw monthly burn as a number) or `20.0 months` (the raw `runway_months`) cannot leak into the user copy. The `_n` formatting rule (`%.1f` when `runway_months < 12`, `%d` rounded otherwise) prevents the awkward "20.0 months" leakage at one decimal place for any runway ≥ 12 months.
4. **No English inside the ZH sentence.** Test pins `assert "months" not in zh` (and `assert "Last year" not in en`) — the "about 20 months" fragment is the EN-only branch; the ZH mirror says "约 20 个月" with the period attribution preceding it. No English-token leakage across the language boundary.
5. **Stale suffix is conditional, not always-on.** The `most recent annual filing / 最近一期年度文件` suffix only renders when `period.stale` is true (test `test_stale_period_uses_ladder_stale_phrase` pins this). For a current filing, the sentence stands without the suffix; for a > 550-day-old filing, the reader is told the period is the most recent available but not the most recent calendar year. This is the design-doctrine "nulls printed, not hidden" form — the panel does not silently display a stale number as if it were fresh.
6. **No banned glance-tier vocab introduced.** Grep over the +63 template insertion lines for the macro design-doctrine banned list (`score`, `rank`, `confidence`, `AIS`, `satellite`, `chokepoint`, `falsifier`, `percentile`, `validated`, `已验证`, `经验证`, `经过验证`): zero matches in visible copy. The word "validated" is only referenced via the `capability_state_c2` enum in `tests/test_cash_runway.py`'s docstrings and fixtures' `_provenance` blocks (governance-only artifacts, never rendered).
7. **No raw slug leaks.** The Jinja conditionals use `cash_runway.runway_display` / `cash_runway.period.end` / `cash_runway.cash_display` / etc. — every name is an underscore-separated internal field that lives inside `{{ }}` and `{% set %}` blocks, never in visible text. Spot-check of the `_usd_dollars()` helper at engine/cash_runway.py:220: it returns only `$X.XB / $X.XM / $X.XK / $X` — no internal-state name, no raw float, no percentile.
8. **Theme.css prose is design-doc prose, not user copy.** The new 7-line CSS comment ("Reuses the ladder's ... tokens ... Dark/light treatment is inherited from the ladder panel above (same section, same #debt-maturity / #cash-runway parent). No new colours — same tokens as the ladder rules above.") lives inside `<style>` and never renders to users. It names the design intent (inheritance from the ladder), names what was decided (no new colors), and names the artifact that motivated the decision (same parent section). No banned vocab.
9. **No run-on sentences.** A scan of the +63 template insertion lines for user-facing sentences over 30 words: the longest is the synthetic-burn EN sentence (~28 words including the date attribution and the period end) and its ZH mirror (~26 characters). Both are within the 30-word glance-tier budget. The `last year` → `In the year ending YYYY-MM-DD (10-K)` rewrite actually slightly shortens the EN side while adding attribution.

Conclusion: zero plain-language debt introduced. The new copy is bilingual (every EN sentence has a sibling ZH sentence, every ZH sentence has a sibling EN sentence), attribution-aware (period end + form type prefix replaces "Last year"), null-disclosed (six status branches each carry their own `.dmw-null` / `.dmw-nullwhy` transparent-null prose with the same "nothing is being estimated in its place" disclaimer the doctrine requires), free of banned glance-tier vocab, and free of raw-float or raw-slug leakage. The pre-existing ladder copy on the same page is unchanged.

## Theme findings

TP-0 art-direction law in force: dark and light are two art directions, not one skin; 8-cell evidence matrix required for any user-facing material change. Theme discipline here reduces to: does the new block sit inside the existing ladder's section so its dark/light treatment is inherited? Are the existing ladder tokens reused (no new tokens, no new colors)? Does the rule set introduce or remove any direction-flip behavior?

**Verdict: PASS with one structural observation — the new block is intentionally a token-reuse sibling of the existing ladder panel, so no new tokens and no new theme decisions.**

PR footprint = 1 user-facing template file + 1 theme file:

1. **The new block sits inside the existing ladder's section.** The Jinja diff is inside `<section id="debt-maturity">`; the `<div class="dmw-runway">` block is rendered BEFORE `<div class="mod-ft">` and `</section>` (asserted by `test_runway_sits_inside_section_before_footer` and `test_runway_source_sits_inside_section_before_footer`). Because it shares the same parent section, the existing dark/light cascade applies to both the ladder and the runway — the panel is a token-reuse sibling, not a parallel token root.
2. **Token reuse, not new tokens.** The new copy uses `.dmw-lede`, `.dmw-null`, `.dmw-nullwhy`, and `.mod-ft` — the same classes the ladder uses. The only new class is `.dmw-runway`, which is a layout divider (margin-top / padding-top / border-top), not a color, shadow, or material treatment. The CSS prose comment is explicit: "Reuses the ladder's ... tokens ... No new colours — same tokens as the ladder rules above."
3. **The new border-top uses `color-mix(in srgb,var(--hair) 60%,transparent)` and a light-theme override `[data-theme="light"] .dmw-runway{border-top-color:var(--hair);}`.** Both reference the existing `--hair` token — the same hairline the rest of the dashboard uses for borders. The light override swaps the 60%-transparent mix for the full hairline color, which is the standing light-theme hairline discipline (the lighter hairline color is barely visible at 60% transparency on a pale canvas; the dark-theme 60% mix keeps the divider subtle on a darker field). No new `--*` token is introduced.
4. **Direction-flip preserved.** The existing ladder panel's direction-flip ("DIRECTION reads that FLIP in zh (risk-off → green)") is unchanged; the new runway copy uses no direction-token styles and so has no flip to preserve. The four retained safety-repair contracts from earlier PRs (settled-session floor, direction-token styles, design-system emoji, retired live strip) are not touched by this PR.
5. **No new emoji escapes.** No emoji is added or removed by this PR. The new copy uses ASCII punctuation (em-dash `—`, mid-dot `·` as the stale-suffix separator, parentheses around the form type) — all already in use on the existing ladder.
6. **No raw runtime-style injection.** The new template uses Jinja conditional rendering only; no `<style>` block, no `style.textContent` assignment, no inline `style=""` byte. The `scripts/check_runtime_style_injection.py` gate (which is a forward-only enforcement) will read this diff as zero new offenses.
7. **8-cell evidence matrix status.** TP-0's matrix is required for material user-facing changes. This PR adds a NEW USER-VISIBLE BLOCK on a published Macro Dashboard surface (`/us_stocks/` `/<ticker>.html` or wherever the ladder renders — the debt-maturity section is one of the most-used surfaces on the per-ticker page per the body). A new block on an existing rendered surface IS material under the TP-0 rule. **OBSERVATION:** the body does NOT include a fresh 8-cell evidence re-shoot (`scripts/capture_page_evidence.py` EN+ZH × dark/light × desktop/mobile = 8 shots). The PR's release block cites the Jinja render tests as the proof — those tests assert the strings appear in the rendered HTML, but they do not capture the rendered page in both themes. The body's `RENDER PROOF` block quotes three EN+ZH strings ("20.0 months", "$50.0M", "Cash runway not loaded yet"); the round-2 PR does assert EN-only ZH parity inline ("no English inside the ZH sentence"), but no theme-specific ZH capture is named. **This is a real evidence gap, not a verdict blocker** — the panel reuses the ladder's tokens so dark/light cascade is inherited, and the tests prove the strings land in the HTML — but the standing doctrine requires the matrix, and a future pass should ship it.

Conclusion: theme law is honored in code (token reuse, no new colors, no new emojis, dark/light cascade inherited from the parent section, no direction-flip regression). The structural observation is that the 8-cell evidence matrix is owed for this new block (it is a new user-visible surface on a published Macro Dashboard page), and the PR's release block does not yet include it; the matrix is non-blocking in this audit because the implementation choice (token reuse, same parent section) is the doctrine-correct way to inherit dark/light treatment, but the receipt itself is still owed.

## Validated-claims findings

The standing law: the word "validated" and friends are CI-enforced via `scripts/check_validated_claims.py`. User-facing copy may not promote a context/data/detection/tagging artifact to authority unless it has cleared the gauntlet. Display-tier claims stay display-tier until promoted.

**Verdict: PASS — no new validated/已验证/经验证/经过验证 claims introduced; the panel uses measured verbs and explicit non-promotion prose.**

`scripts/check_validated_claims.py --list` census on the touched files at the merged head:

- `templates/_debt_maturity.html.j2`: 9 hits, all allowlisted (the existing ladder panel's `validated reversal`, `validated lenses`, `validated 2d-macd`, `validated mean-reversion` strings — unchanged from the canonical ladder; this PR does not introduce or alter them).
- `templates/theme.css`: zero hits.
- `engine/cash_runway.py`: zero hits.
- `scripts/build_debt_maturity.py`, `scripts/build_stock_library.py`, `scripts/build_ticker_pages.py`: zero hits.
- `tests/test_cash_runway.py`, `tests/fixtures/cash_runway/*.json`: zero hits.

The new runway copy uses measured verbs ("covers", "spent", "brought in", "brought in more cash than it spent") and explicit non-promotion prose ("Not every company reports all three of these facts. Nothing is being estimated in their place", "Nothing is being guessed in its place", "This panel only shows figures once the identity of the filer is verified"). It never says "validated", "已验证", "经验证", "经过验证", "certified", "approved", "gauntleted", or any promotion verb.

1. **No row promotion occurred.** The PR does not touch `research/market_intelligence_productization/` (no ledger rows, no manifest rows, no MO-PAID-* / MO-DELTA-* / MO-PROOF-* state moves). The body explicitly names the rows it serves: `MO-PAID-059` and `MO-DELTA-018 second slice`. The PR is a build, not a records pass; the corresponding ledger updates are owed by the F00C records lane, not authored by this PR. The body lists zero `proved_now: ...` lines and zero `state_delta` claims.
2. **No live-readback claimed.** The PR is wired into the producer pipeline (`scripts/build_stock_library.py` + `scripts/build_ticker_pages.py`) but the body does not claim a live `https://www.mastermind-x.com/...` URL or a nightly-read receipt. The render proof is the Jinja test asserts (string presence in rendered HTML); the live-render leg is owed by the render lane post-merge. The body is explicit about this: "RENDER PROOF" cites test strings, not live URL fetches.
3. **No "this is a validated signal" claim in user copy.** The panel reports XBRL facts verbatim ("Cash on hand: $50.0M", "covers 323% of the debt coming due in the next 12 months") — it does NOT say "our validated cash-burn model predicts X months of runway" or any promotion framing. The CIK cross-check (`identity_mismatch` status when the embedded `cik` field disagrees with the caller-supplied `cik`) is a fail-closed identity gate, not an authority claim.
4. **Neural Web A7 / epistemics contract honored.** The engine module docstring is explicit: "Every number is a reported XBRL fact or arithmetic over reported facts. No score, no rank, no LLM text, no escalation is produced here (Neural Web A7 / epistemics: this module never originates a signal)." The `_resolve_cash_runway()` resolver in `scripts/build_stock_library.py` returns one of nine statuses (`not_applicable`, `unresolved`, `not_loaded`, `confirmed_no_filings`, `reported`, `no_cash_facts`, `no_filings`, `identity_mismatch`, plus the engine's own), never a numeric score or a directional verdict. The producer-fault path emits a `::warning` annotation and degrades to `not_loaded`, never to `not_applicable` — so a producer fault cannot widen into a structural exemption.
5. **Display-tier posture preserved on every null branch.** All six transparent-null branches (`no_cash_facts`, `no_filings`, `not_loaded`, `unresolved`, `identity_mismatch`, plus the wider engine statuses returned via `_empty_result()`) end with the standing "nothing is being estimated in its place / nothing is being guessed in its place" disclaimer. The Tier-2 receipt is the engine's `drop_reasons` array (named in `test_no_cash_facts_when_missing_tag` as a structured per-tag array of reasons) — the panel does not silently fall back to a fabricated positive claim.

Conclusion: zero new validated claims. The panel reports XBRL facts verbatim, fails closed on identity mismatch, never originates a signal (A7), and uses the standing "nulls printed, not hidden" form on every null branch. The CI-enforced allowlist has zero new entries; the existing 9 ladder-side `validated *` strings on `_debt_maturity.html.j2` are unchanged.

## Overall verdict

**PASS** — a disciplined, narrowly-scoped, two-round half-B build with RED-first testing, attribution-aware bilingual copy, token-reuse theme discipline, and zero new validated claims.

- **10 files, 1204 insertions / 14 deletions.** Round 2 is `dfaae20d` on top of round 1 `fdc4c3c4` — no rewrite, no force-push, no merge of `origin/main` mid-development. The 1194-line measured three-dot diff matches `gh pr view --json files` exactly.
- **The new module is pure, identity-locked, and fail-closed.** `engine.cash_runway` reads only parsed XBRL companyfacts; identity is CIK-only (with embedded-`cik` cross-check that fails closed on mismatch); the only arithmetic is FCF = OCF − capex; the only display helper produces `$X.XB / $X.XM / $X.XK / $X`; `runway_display` is a closed enum token that the template composes into the user-facing sentence (never a raw float or a raw English phrase).
- **The two-round shape is well-disciplined.** Round 1 established the module + producer + template + 290-line test surface. Round 2 added the four attributes the round-2 review MAJOR asked for (annual_burn_display, monthly_burn_display, `more_than_10_years` enum, attribution prefix, stale suffix), each pinned by a separate assertion in `tests/test_cash_runway.py` (`test_runway_display_more_than_10_years`, `test_more_than_10_years_prints_annual_burn_not_cash`, `test_render_aapl_self_funding_attributed`, `test_stale_period_uses_ladder_stale_phrase`, `test_runway_sits_inside_section_before_footer`). RED proof is per-failure (each of the 10 round-1 failures is named in the body), GREEN proof is the same command after the round-2 widening.
- **The producer wiring is additive, not destructive.** `scripts/build_stock_library.py`'s new `_resolve_cash_runway()` mirrors `_resolve_debt_maturity()`'s taxonomy; the producer-fault path emits a `::warning` annotation (bare `print(..., flush=True)` honors the GitHub-annotation-must-start-the-line contract) and degrades to `not_loaded`, never `not_applicable` — so a transient lookup failure cannot widen into a structural exemption. The `scripts/build_ticker_pages.py` change is exactly one context line (`"cash_runway": (blob or {}).get("cash_runway")`), and the `_dm_status` jump-link gate around line 3435 is intentionally untouched per the standing ruling.
- **The CI wiring reuses existing steps.** `engine/cash_runway.py` is appended to four existing curated `engine/*.py` glob lists (the import-closure already reaches `engine/debt_maturity.py`); `tests/test_cash_runway.py` rides the existing render-guard + engine-contract tests step (which already runs `tests/test_debt_maturity.py`). No new job is minted.
- **No user-facing copy debt, theme token debt, or validated-claim debt introduced.** Plain-language law is honored (bilingual parity, attribution-aware prefix, null-disclosed branches, no banned vocab, no raw float or slug leaks, EN/ZH sentence parity asserted by tests); theme law is honored in code (token reuse, no new colors, no new emojis, parent-section cascade inherits dark/light from the ladder, no direction-flip regression); validated-claims law is honored (zero new `validated` / `已验证` / `经验证` / `经过验证` claims, Neural Web A7 honored via the module docstring, every null branch ends with the standing "nothing is being estimated in its place" disclaimer).
- **PR body accurately describes the diff.** Round-2 RED proof lists each of the 10 failures with the round-2 MAJOR they map to; round-2 GREEN proof quotes the 100-passed / 1-skipped command verbatim; RENDER PROOF cites three sentences (AAPL self-funding + 323% cover, synthetic burn $50.0M / $2.5M monthly / 20 months, not_loaded) in both EN and ZH. Deviations are explicit and small (synthetic CIK `0000099999` vs r1 contract `0000009999`; `_dm_status` jump-link intentionally not touched). Open questions: none.
- **Live-deployment leg is owed.** The render lane must re-bake the per-ticker pages (the new block ships on every ticker page that has a debt-maturity panel; that is the entire debt-maturity-eligible universe), the VPS `macro-update` must pull the merge, and a live readback must confirm the new block renders on at least one AAPL or AAPL-like ticker page (and that the corresponding ZH page is the ZH mirror, not an English-only fallback). The body is candid about the render proof being the test asserts, not a live URL fetch — that leg is the operator's pickup + the render lane's re-bake.

**No audit dimension blocks SHIPPED.** The PR is a clean half-B build with disciplined RED-first testing, attribution-aware bilingual copy, token-reuse theme discipline, and zero new validated claims. The 8-cell evidence matrix is owed for the new block (a real evidence gap, not a verdict blocker — token reuse makes the matrix a receipt obligation rather than a design obligation), and the live-render leg is owed by the render lane. The build itself is compliance-positive: the panel reuses the existing ladder's tokens, parent section, null branches, and prose discipline, so the matrix when captured will match the ladder's matrix baseline by construction.