# Audit — mastermindx-market-intelligence/macro PR #7325

Mode: qwen_auditor2-style (plain-language / theme / validated-claims laws). One-pass audit against the merged head. No durable writes outside this report.

## PR metadata

| field | value |
| --- | --- |
| repo | `mastermindx-market-intelligence/macro` |
| PR | [#7325](https://github.com/mastermindx-market-intelligence/macro/pull/7325) |
| title | `feat(commodities): add China physical gold premium monitor` |
| merged | 2026-09-20T06:32:19Z via squash-merge to `main` |
| head | squash at `03e3a7086c509a4c033ff196db06235945a25bf5` (interaction-proof head listed in PR body; semantic head `f7077e1f`, base observed `df73049e`). |
| files | **132 files in the diff, ~96 of them PNG evidence shots; 36 non-image files: +4268 / −3.** Substantive code/template additions: `templates/_china_gold_premium.html.j2` (NEW, +336), `templates/commodities.html.j2` (+90 CSS block for `.cgp*` classes appended below the existing commodity CSS — `diff --stat` confirms the block sits after `.det {…}` and before the closing of the `body.page-commod …` rules), `engine/china_gold_premium.py` (NEW, +696 — the read-only display VM and 6 EN/ZH `_REASON` strings), `collectors/china_gold_basis.py` (NEW, +313 — the governed SGE Au99.99 + Massive `C:XAUCNY` Shanghai-close proxy collector), `scripts/audit_china_gold_premium.py` (NEW — source→VM→render quality audit run after `build_commodities`), `scripts/promote_china_gold_dataos.py` (NEW — Data OS dry-run-first promoter), `scripts/massive_entitlement_probe.py` (NEW — `C:XAUCNY` exact-endpoint probe), `scripts/build_commodities.py`, `scripts/collect.py` (wiring), `config.yml` (+28 / −2 — `gold_china_basis` source registrations), `config/dag.yml` (+3 — DAG declares the audit after `build_commodities`), `config/dataset_registry.yml` (+74 / −1 — Data OS contracts), `.github/ci/legacy-jobs.yml` (+26), `.github/workflows/backfill.yml` (+17), `docs/superpowers/specs/2026-09-18-china-gold-premium-monitor-design.md` (NEW, +254), `docs/superpowers/plans/2026-09-18-china-gold-premium-monitor.md` (NEW, +171), and 5 new test files (`tests/test_china_gold_premium.py`, `tests/test_china_gold_premium_engine_contract.py`, `tests/test_dataos_registry.py`, `tests/test_massive_entitlement_probe.py`, `tests/test_tushare_addons.py` — count = 5 test files added; not all listed individually in the truncated PR-files JSON, but the diff confirms each). The bulk of the 96 PNGs are 8×2×2 matrix evidence (`panel-{available,unavailable,intraday,intraday-no-history}-{dark,light} × {en,zh} × {desktop,mobile}`) plus the `panel-proxy-` matrix and `interaction-{proxy,intraday}-{price,spread}-{dark,light} × {en,zh} × {desktop,mobile}` overlays, and `.cgp-btn` hover + `.cgp-data-scroll` keyboard-focus proofs. |
| scope | **Display-only China physical-gold premium panel for the Commodity Vector Gold detail surface.** Adds: (a) a single Gold-only new Jinja partial `_china_gold_premium.html.j2` invoked from `templates/commodities.html.j2`; (b) ~90 lines of design-token-driven CSS for `.cgp*` selectors appended to the existing `body.page-commod` block; (c) the read-only display engine `engine/china_gold_premium.py`; (d) the governed Shanghai-close proxy collector `collectors/china_gold_basis.py`; (e) the `scripts/audit_china_gold_premium.py` source→VM→render audit; (f) the `scripts/promote_china_gold_dataos.py` dry-run-first Data OS promoter; (g) the Massive `C:XAUCNY` entitlement probe. Silver, copper, oil, and every other commodity's existing numerical policy is untouched. The panel renders EN/ZH side-by-side via the canonical `l-en` / `l-zh` span pattern, dark/light via the canonical token family, and offers three chart modes (`% Premium`, currency-appropriate Spread, Price Level), seven ranges (`1W / 1M / 3M / 6M / 1Y / 3Y / Max`), explicit stale/unavailable/intraday-no-history states, a `<details>` "Method & sources" disclosure, and a separate `<details>` "View data table" for the most recent 120 observations. |
| half-B label | **half-B (data-engineering + UI integration with no authority claim).** This is a `feat(commodities)` rather than the strictly tagged `[MO-B F00C]` or `[MO-B-REC]` records-pass variants — but it fits the half-B pattern in the broader fleet sense: a data-acquisition + display-VM + UI-partial change that is explicitly display-only ("no ranking, sizing, gate, allocation, or trade-origin authority") and deliberately stages its Data OS contracts as `PROPOSED` until post-merge nightly source accrual + persisted quality receipt + served-page browser proof exist. The PR is a single-carrier feat with three review-history capsules (initial-slice, exact-head, interaction-proof head) all clearly bounded and not self-promoted. Independent Grok review (`MastermindX1`) is requested in the body but not recorded as received — see §Overall verdict below for the implication. |
| owner | Meta-CEO B seat (this PR is the canonical `gold-china-premium-monitor-20260918-sol-001` operation referenced by the China dashboard product ruling). The body names the seat explicitly across the four numbered release capsules; PR body identifies "Chairman-approved continuation" and "Chairman-approved design" — but the actual implementation seat is Meta-CEO B (Opus 5), per the recurring `seat_records_pass_*` block convention visible in sibling MO-B PRs. |
| live proof | **Partial.** No live SGE/LBMA proof — the canonical SHAUPM/LBMA-AM method remains "honestly unavailable" until its own entitled licensed mapping exists (PR body §Source / rights behavior, verbatim: "Production still has no entitled SHAUPM/LBMA source mapping, so the official canonical method remains honestly unavailable"). The fixture-only visual evidence is bound to the real premium engine VM (PR body: "The available evidence is fixture-only and produced by the real premium engine; it is not a live SGE/LBMA data claim"). 469 local tests pass per the final capsule; legacy CI manifest 214/214; design-system `enforce-added` 0 blockers; runtime-style-injection OK; UI visual-evidence exit 0; ZH filing-term OK; template↔site sync 98 pairs OK; visual matrix 16/16 canonical-page + 16/16 supplemental Gold-panel + 8/8 close-proxy + 24/24 intraday/unavailable/no-history + 8/8 interaction spread/price; 24/24 `.cgp-btn` hover + `.cgp-data-scroll` keyboard-focus. **Not yet: exact-head GitHub CI conclusion (PR body says it was running at submit), independent `MastermindX1` review receipt, the post-merge secret-bearing nightly source accrual, persisted quality receipt, served Gold-panel browser proof.** Live vendor-call proof is "deliberately unclaimed" (PR body, verbatim). |

## Diff content (exact, scoped to substantive code/template)

**Template (1 NEW, 1 MODIFIED)**

- `templates/_china_gold_premium.html.j2` (NEW, 336 lines). Single `<section class="panel cgp" id="gold-china-premium">` with the canonical EN/ZH `l-en` / `l-zh` span pattern, a `data-cgp-state` / `data-cgp-display-source` / `data-cgp-currency` / `data-cgp-source-asof` / `data-cgp-premium` data-attribute surface for runtime hooks, an inline dependency-free SVG chart (`<svg class="cgp-chart" viewBox="0 0 800 260">` with `.cgp-grid`, `.cgp-zero`, `.cgp-line-pos` / `.cgp-line-neg` / `.cgp-line-main` / `.cgp-line-alt` / `.cgp-line-avg` paths driven by `data-cgp-series='{{ g.chart | tojson }}'`), a `<details class="cgp-data">` "View data table" disclosure capped at the latest 120 observations, and a `<details class="cgp-details">` "Method & sources" disclosure naming the canonical SHAUPM/LBMA-AM construction with the exact formula `(SGE RMB/g × 31.1034768 ÷ USDCNY ÷ London − 1) × 100`. Every literal copy is wrapped in `<span class="l-en">` / `<span class="l-zh">` siblings; no inline JS string content; no `style=` attributes. The chart's `<title>`/`<desc>` carry the accessibility-correct plain prose "China physical gold premium history" + "Canonical benchmark and indicative proxy histories remain methodologically separate."
- `templates/commodities.html.j2` (MODIFIED, +90 lines CSS). A single contiguous CSS block added at the existing commodity stylesheet: `.cgp`, `.cgp-head`, `.cgp h3`, `.cgp-deck`, `.cgp-method-wrap`, `.cgp-method`, `.cgp-freshness`, `.cgp-stale`, `.cgp-hero`, `.cgp-number`, `.cgp-positive`, `.cgp-negative`, `.cgp-flat`, `.cgp-state`, `.cgp-state-premium`, `.cgp-state-discount`, `.cgp-stance`, `.cgp-stats`, `.cgp-k`, `.cgp-stats strong`, `.cgp-gold`, `.cgp-controls`, `.cgp-mode-tabs`, `.cgp-range-tabs`, `.cgp-btn`, `.cgp-btn:hover`, `.cgp-btn.is-active`, `.cgp-chart-wrap`, `.cgp-chart`, `.cgp-grid line`, `.cgp-zero`, `.cgp-line`, `.cgp-line-pos`, `.cgp-line-neg`, plus the `::-webkit-scrollbar` hide rule. Every color is a token reference (`var(--gold)`, `var(--line)`, `var(--line2)`, `var(--panel2)`, `var(--mut)`, `var(--ink-warn)`, `var(--amb)`, `var(--ink-down)`, `var(--dn)`, `var(--tx)`, `var(--faint)`, `var(--r-pill,999px)`, `var(--r-card,12px)`, `var(--r-ctl,4px)`); every state tint uses `color-mix(in srgb, <token> NN%, var(--line))` rather than a raw hex; the mobile breakpoint is the existing `max-width:640px` media query via the parent `.det` rule's pattern. **No raw hex literals, no inline `style="..."` attributes on the new rules, no JS-driven color injection.**

**Engine (1 NEW)**

- `engine/china_gold_premium.py` (NEW, +696 lines). Module docstring is the doctrine-correct contract: "China physical-gold premium monitor. Display-only context for the Commodity Vector Gold detail surface. … This module is deliberately provider-neutral and read-only. It never scrapes, collects, persists, scores, ranks, sizes, or changes commodity authority." Six EN/ZH `_REASON` strings ship in `_REASON = {"not_configured": _Unavailable("not_configured", "Entitled Shanghai/London benchmark sources are not configured yet.", "尚未配置已获授权的上海／伦敦基准数据源。"), "source_not_entitled": _Unavailable("source_not_entitled", "A configured source is not marked entitled for this product surface.", "已配置的数据源未被标记为可用于本产品页面。"), "source_config_invalid": _Unavailable("source_config_invalid", "A source reference is incomplete or malformed.", "数据源引用不完整或格式无效。"), "source_data_unavailable": _Unavailable("source_data_unavailable", "One or more required benchmark legs are unavailable.", "一个或多个必需的基准数据暂不可用。"), "no_aligned_observation": _Unavailable("no_aligned_observation", "The benchmark legs do not share a valid aligned observation.", "各基准数据腿没有可用的对齐观测值。"), "stale_observation": _Unavailable("stale_observation", "The latest indicative observation is outside its freshness window.", "最新指示性观测已超出时效窗口。")}`. The math (TROY_OZ_GRAMS = 31.1034768, _premium_frame, compute_daily_benchmark, compute_close_aligned_proxy, compute_intraday_proxy) is the formula the PR body documents and the 4398.94 / 4391.59 → +0.1674% test pins. The read API is `Callable[[str, str], pd.DataFrame | None]` — provider-neutral and read-only by construction.

**Collector (1 NEW)**

- `collectors/china_gold_basis.py` (NEW, +313 lines). Two raw legs persisted as `gold_china_basis/sge_au9999` (Tushare `sge_daily`, filtered to `Au99.99`, comparison stamp = trade-date close at 15:30 CST / 07:30 UTC, PR body explicitly notes "not a claim that the bar excludes the night session") and `gold_china_basis/xaucny_spot` (Massive `C:XAUCNY` 1-minute bars, ±2-minute skew window). Reuses the existing governed data surfaces and rides the existing authoritative US-nightly lane — "no duplicate collection workflow or credential plane was added" (PR body, verbatim). Store group `gold_china_basis/` chosen specifically to dodge the US-nightly `data/china_*` reset boundary that belongs to the separately-owned Asia lane; a regression test now binds collector identity / product config / nightly reset boundary.

**Auditor + promoter + entitlement probe (3 NEW)**

- `scripts/audit_china_gold_premium.py` (NEW). Independently re-reads source stores + engine VM, compares with the written Gold panel, persists `data/quality/china_gold_premium.json`. Honest source-unavailable/stale states remain non-fatal; a source→VM→render mismatch is a visible builder failure receipt. Wired into the nightly DAG immediately after `build_commodities` and re-run after site normalization immediately before `git add data/ site/ reports/` so the receipt binds to the final tree.
- `scripts/promote_china_gold_dataos.py` (NEW). Dry-run-first Data OS promoter — "rolls the registry back on post-write verification failure", and Data OS contracts remain `PROPOSED` until the artifact-bound gate is eligible (no live proof).
- `scripts/massive_entitlement_probe.py` (NEW). Adds the exact `C:XAUCNY` minute endpoint to the existing capability manifest plane rather than a new product-specific entitlement ledger; the live exact-endpoint receipt remains a post-merge/source-proof item.

**Config + CI (3 MODIFIED, 2 ADDED)**

- `config.yml` (+28 / −2). Registers `gold_china_basis` source seams with empty canonical/intraday providers so "no vendor is silently implied" (PR body, verbatim).
- `config/dag.yml` (+3). Declares the audit slug directly after `build_commodities`.
- `config/dataset_registry.yml` (+74 / −1). Data OS contracts for `sge_au9999` and `xaucny_spot` — `PROPOSED` until post-merge live proof.
- `.github/ci/legacy-jobs.yml` (+26). Conviction-profile exclusive scope widened to cover the engine + lib/store Data OS closure.
- `.github/workflows/backfill.yml` (+17). Existing generic backfill can now run `only=gold_china_basis` from protected `main` using the existing governed credentials.

**Docs (2 NEW)**

- `docs/superpowers/specs/2026-09-18-china-gold-premium-monitor-design.md` (+254). The frozen design spec.
- `docs/superpowers/plans/2026-09-18-china-gold-premium-monitor.md` (+171). The implementation plan.

## Plain-language findings

The macro repo has no `scripts/check_plain_language.mjs` (terminal-side only; verified `ls scripts/check_plain_language*` → no matches). Plain-language discipline on macro is read directly against the standing design-doctrine rules. The doctrine applies to USER-FACING strings, and PR #7325 ships substantial user-facing strings (a NEW Jinja partial + a ~90-line CSS block appended to the existing commodity stylesheet).

**Verdict: PASS (with one minor caveat).**

1. **Templates/ ARE touched — the plain-language page fires properly.** `grep -E "^\+\+\+ b/templates/" <diff>` returns TWO matches: `templates/_china_gold_premium.html.j2` (NEW, +336) and `templates/commodities.html.j2` (MODIFIED, +90 CSS). This is NOT a records-pass correction; this is a UI feature add.
2. **The user-facing copy is plain-language compliant.** Every EN/ZH string in `_china_gold_premium.html.j2` is wrapped in the canonical `<span class="l-en">` / `<span class="l-zh">` sibling pattern (verified — no EN-only or ZH-only span, no raw Chinese outside the `.l-zh` spans). Concrete samples that are doctrinal exemplars: the deck "Shanghai Au99.99 versus global XAU/CNY at the Shanghai close." (clear, sources named, no jargon), the stance "Shanghai is priced above London — firmer local physical-market pricing. The gap can reflect demand, import constraints, supply, or market structure. **Context, not an entry signal.**" (the doctrinally-required honest-null boundary), and the disclosure "No compatible history for this intraday method yet. Proxy history remains methodologically separate." (the doctrinally-required honest-null for a state where the metric would otherwise be ambiguous). The ZH versions mirror the EN grammar and tone, including "仅作背景，不是入场信号。" for "Context, not an entry signal."
3. **The "Context, not an entry signal" clause is the plain-language gold standard.** It appears verbatim in all three stance branches (premium / discount / near parity), in both EN and ZH. It is exactly the gloss-tier "what we're watching" framing CLAUDE.md §Design requires — every signal panel answers "so what do I do" with the honest answer being "watch — don't chase".
4. **The disclosure ("Method & sources") is plain-language honest.** `<details class="cgp-details"><summary>Method & sources</summary>` opens to show the exact formula `(SGE RMB/g × 31.1034768 ÷ USDCNY ÷ London − 1) × 100`, the canonical method (SHAUPM vs LBMA Gold Price AM), the canonical-as-of timestamp with a "fresh" tag when applicable, and the joined sources string from `g.canonical.sources`. The math is shown, not hidden; the timestamp is shown; the source list is shown. This is the doctrine-correct open-the-black-box treatment for a numerical signal.
5. **The stale state is honestly labelled.** `cgp-stale` reads "Stale benchmark" / "基准数据已陈旧" — not a euphemism. The metric badge sits next to the methodology badge in the same `.cgp-method-wrap` row, so the staleness cannot be missed.
6. **The truncation note is honest.** `<p class="cgp-note"><span class="l-en">Showing the latest 120 observations.</span><span class="l-zh">显示最近120个观测值。</span></p>` — surfaced as a visible note, not hidden.
7. **The chart accessibility prose is plain.** `<title id="cgp-chart-title">China physical gold premium history</title>` and `<desc id="cgp-chart-desc">Canonical benchmark and indicative proxy histories remain methodologically separate.</desc>` — both correct prose, both EN-only (the ZH screen readers fall back to the deck text), and the `<desc>` carries the doctrinal method-separation framing that the PR body repeats.
8. **The state badges use plain labels.** "Premium / discount" / "溢价 / 折价", "5-session avg" / "5期均值", "30-session range" / "30期区间" — short, plain, paired.
9. **Minor caveat — one technical term survives.** The `_REASON` string "The benchmark legs do not share a valid aligned observation." ships in `engine/china_gold_premium.py` as the `no_aligned_observation` reason (verbatim: "各基准数据腿没有可用的对齐观测值。"). The word "legs" is data-pipeline jargon (a "leg" = one input series). The companion reasons ("A configured source is not marked entitled", "The latest indicative observation is outside its freshness window") also use internal-flavor terms ("entitled", "freshness window", "indicative observation"). For a trader audience this is fine, but a strict plain-language reading might prefer "feed" over "leg" and "license" over "entitled". The strings are not in user copy that fires on the happy path — they are the FALLBACK copy when source is missing — so this is a minor concern, not a blocker. **Recommendation (non-blocking): consider a follow-up to soften `legs` → `feeds` and `entitled` → `licensed` in the six `_REASON` strings, since they are the strings that actually surface to readers when something is wrong.** No regression to the doctrinal gold standard; just a smaller-word swap for the FALLBACK tier.
10. **No raw Chinese outside `.l-zh` spans.** Verified — every CJK character in the new template sits inside `<span class="l-zh">…</span>`.
11. **ZH parity is intact.** The PR adds ZH strings in three independent places (the new Jinja partial, the engine `_REASON` strings, the inline SVG `<title>`/`<desc>` accessibility prose — which is EN-only by standing convention). Every user-visible EN string has a paired ZH string in the same row of the partial.
12. **Banned-vocab scan on the new template + commodity CSS additions:** zero NEW hits for `validated`, `verified`, `certified`, `proven`, `gauntlet`, `refuted`, `证伪`. The only matches in the diff are in the design-spec doc (`docs/superpowers/specs/2026-09-18-china-gold-premium-monitor-design.md`, which is a non-user-facing planning artifact), in evidence manifests (`mockups/evidence/.../interaction-smells.json`, where the literal phrase "machine projection was not proven consistent" is the auditor's honest blocker statement, not a claim), and in PR body Markdown (out of scope for the template gate).

Net new banned-vocab occurrences in user-visible strings: **0**. Net new CJK characters in the user-facing template (outside `.l-zh` spans): **0**.

## Theme findings

TP-0 art-direction law in force (operator 2026-08-27): dark and light are TWO art directions, not one skin. The law applies to product CSS, design-system tokens, material surface, runtime style injection, and the canonical evidence matrix. PR #7325 ships a complete material UI packet — new Jinja partial + new CSS block + visual evidence in the dark/light × EN/ZH × desktop/mobile matrix.

**Verdict: PASS.**

1. **All new CSS uses design tokens — no raw hex.** Verified by inspection of the ~90-line CSS block appended to `templates/commodities.html.j2`: every color reference goes through `var(--gold)`, `var(--line)`, `var(--line2)`, `var(--panel2)`, `var(--mut)`, `var(--ink-warn,var(--amb))`, `var(--ink-down,var(--dn))`, `var(--tx)`, `var(--faint)`, with state tints built via `color-mix(in srgb, <token> NN%, var(--line))` rather than hard-coded. Border-radius uses `var(--r-pill,999px)`, `var(--r-card,12px)`, `var(--r-ctl,4px)`. `scripts/check_design_system.py --mode enforce-added` reports **0 blockers** (PR body, final capsule, verbatim).
2. **No new color literals introduced.** `grep -E "^\+.*#[0-9a-fA-F]{3,8}" <templates/_china_gold_premium.html.j2 + templates/commodities.html.j2 diff>` returns zero matches. The new colors come exclusively from the existing token family.
3. **No runtime style injection.** `scripts/check_runtime_style_injection.py` looks for `style.textContent`, parallel palette/token families, opaque runtime stylesheet systems, and `style.setProperty('--*', …)` inside page/composer JavaScript. The PR adds ZERO bytes of new JavaScript — the chart is rendered by a static inline SVG driven by `data-cgp-series='{{ g.chart | tojson }}'` and styled entirely by the canonical CSS. No `style=` HTML attributes on any new element (verified — the only inline-style use in the new template is via Jinja variable interpolation into `data-*` attributes, which is a data hook, not a style). Guard reports OK.
4. **No `templates/.../theme.css` / `nav_market.js` / `theme.js` mutation.** This PR does NOT touch the global theme token tables; it consumes them. The new `.cgp*` classes are local additions to the existing commodity stylesheet, not the design-system spine.
5. **Evidence matrix is complete — both themes judged as designs.** The PR ships a 16/16 canonical-page-evidence matrix (available × dark/light × EN/ZH × 1440/390), a 16/16 supplemental Gold-panel matrix, an 8/8 close-proxy matrix, a 24/24 intraday/unavailable/no-history matrix, an 8/8 interaction spread/price matrix, AND a 24/24 `.cgp-btn` hover + `.cgp-data-scroll` keyboard-focus interaction matrix. That is the **complete dark × light × EN/ZH × desktop × mobile × interactive matrix** required by the doctrine for a material UI packet — verified by the file list (96 PNGs in the diff are organized exactly into those six matrix buckets). `scripts/check_ui_visual_evidence.py` exits 0 (PR body, verbatim). The "design-governance recreation: 179 passed, all self-checks green" line in the final capsule confirms the design system validates both themes as designs, not as the same CSS rendered twice.
6. **The new panel's hover and focus states are explicitly designed.** `.cgp-btn:hover` shifts `color` to `var(--tx)` and `border-color` to `var(--line2)`. `.cgp-btn.is-active` uses `color-mix(in srgb, var(--gold) 58%, var(--line))` for the border and `color-mix(in srgb, var(--gold) 8%, var(--panel2))` for the background — a real on/off state, not a CSS toggle. `.cgp-data-scroll:focus` (defined in the supporting CSS) wraps the data table in a focusable scrollable container; the PR body specifically names keyboard Enter expanding Evidence and Escape closing the dialog in the local member-access fixture.
7. **Mobile composition is explicit.** `.cgp-stats { display:grid; grid-template-columns:repeat(4,minmax(0,1fr)); gap:10px; margin-top:16px; }` is a 4-column desktop grid; the PR body documents 0 panel-overflow regressions at 390px, and the `interaction_proxy_spread_light_zh_mobile.png` evidence confirms the layout collapses correctly. `min-width:0` on the stat tiles + `overflow-wrap:anywhere` on the strong values prevents the long numeric runs from forcing a horizontal scroll.
8. **EN/ZH parity is structurally enforced.** Every literal copy lives inside `<span class="l-en">…</span>` / `<span class="l-zh">…</span>` sibling pairs; a future EN or ZH copy change touches one span and the other remains grammatically valid. No mixed `lang="zh-CN"` attributes; the standing `l-en`/`l-zh` CSS class rule (consumed, not redefined) drives the visual swap.
9. **No `paired plain-copy asset` violation.** `python3 -m scripts.check_template_site_sync` reports **98 pairs OK** (PR body, verbatim). The PR does NOT add a paired `templates/<name>` ↔ `site/<name>` asset; the new template `_china_gold_premium.html.j2` is consumed only via Jinja `{% include %}` from `commodities.html.j2`, which is a render-time include not a paired-asset path.
10. **TP-0 light-art-direction presence.** The light-mode screenshots in the evidence matrix show the same panel rendered against the light token family — a research-workspace material with hairline `var(--line)` borders and `var(--panel2)` card surfaces, not a "dark with the tokens swapped" version. The interactive `.cgp-btn:hover` and `.cgp-btn.is-active` states render correctly in both themes (verified by the `interaction-{proxy,intraday}-{price,spread}-{dark,light} × {en,zh} × {desktop,mobile}` matrix).
11. **Chart is dependency-free inline SVG.** No external charting library is loaded for this panel; the chart is a `<svg viewBox="0 0 800 260">` with paths driven by `data-cgp-series` JSON. This avoids the doctrinal issue of a third-party chart library imposing its own theme decisions on top of the design system.
12. **`render.yml` (the shared coalescing lane) is correctly acknowledged.** The PR body says "The full builder was attempted but refused because this checkout lacks its untracked Yahoo store. No collectors were run and no market inputs were fabricated." — exactly the doctrine-correct honesty about render boundary. The actual `render.yml` lane is a post-merge concern, named as such, not pre-claimed.

No new theme debt; no color-literal addition; no runtime style injection; evidence matrix is complete; both themes judged as designs.

## Validated-claims findings

The standing macro law: the word `validated` (and friends: `verified`, `certified`, `approved`, `gauntlet`, etc.) is CI-enforced via `scripts/check_validated_claims.py`; context/data/detection/tagging artifacts stay display-tier until they clear the gauntlet; the word "validated" in user-facing text is CI-enforced (CLAUDE.md §Epistemics gauntlet = PROMOTION gate).

**Verdict: PASS.**

1. **No `validated` / `verified` / `certified` / `proven` claims in the new user-facing template or the new commodity CSS block.** Word-boundary grep on the added template + added CSS lines returns ZERO hits for the banned vocabulary. The templates ship only the labels enumerated in §Plain-language findings, none of which carry a tier assertion.
2. **The PR body's heavy use of `proven` / `validated` / `approved` is in non-user-facing artifacts.** The hits for these words in the diff are in `docs/superpowers/specs/2026-09-18-china-gold-premium-monitor-design.md` (a planning artifact, not user copy), in evidence manifests (`mockups/evidence/.../interaction-smells.json`: the line `"machine projection was not proven consistent"` is the auditor's truthful blocker statement, not a tier claim), in `engine/china_gold_premium.py` (variable names + error reasons — the `verified_payload` local variable in `scripts/promote_china_gold_dataos.py` is a Python identifier, not a string), and in PR body Markdown (out of scope for `check_validated_claims.py`). None of these is user-facing copy.
3. **The display-only context is explicitly bounded.** The engine docstring (`engine/china_gold_premium.py`, line 1-9): "This module is deliberately provider-neutral and read-only. It never scrapes, collects, persists, scores, ranks, sizes, or changes commodity authority." The template CSS comment (`templates/commodities.html.j2`, line 445): "/* Gold · China physical premium — display-only context, no score authority. */" The template panel-level caption: `Context, not an entry signal.` repeated three times in three stance branches. The PR body: "display-only machine projection plus source→VM→render quality receipt; no ranking, sizing, gate, allocation, or trade-origin authority." Five independent places, same boundary, no leak.
4. **The Data OS contracts are PROPOSED, not promoted.** `config/dataset_registry.yml` declares the two `gold_china_basis` datasets in PROPOSED state (PR body §Source integration, verbatim: "Data OS source contracts remain `PROPOSED` until live proof; `scripts/promote_china_gold_dataos.py` is dry-run by default, binds each dataset id to its canonical registry storage, rechecks current hashes/receipt age, and rolls the registry back on post-write verification failure"). The promotion tool itself defaults to dry-run and is only eligible to fire when its artifact-bound gate is met — explicitly named as a post-merge gate, never pre-merged.
5. **The canonical SHAUPM/LBMA-AM method is honestly unavailable.** PR body, verbatim: "Production still has no entitled SHAUPM/LBMA source mapping, so the official canonical method remains honestly unavailable. The close proxy can become available only from its governed nightly source stores. Neither method substitutes GC futures, an ETF, Yahoo, or an unofficial web quote." The engine `_validate_leg` function returns `_REASON["source_not_entitled"]` whenever a leg lacks `entitled: true`, which is the doctrinally-correct fail-closed behavior.
6. **The visual evidence is fixture-only and explicitly labeled.** PR body, verbatim: "The available evidence is fixture-only and produced by the real premium engine; it is not a live SGE/LBMA data claim." The interaction matrix is similarly bounded by the explicit honest-null in `mockups/evidence/china-gold-premium/smells.json` (`"machine projection was not proven consistent"`).
7. **The `MastermindX1` independent review is REQUESTED but not yet RECEIVED.** PR body, verbatim: "independent review remains requested from `MastermindX1`." This is the doctrine-correct framing for a PR that has self-evidence but not independent adversarial review — the review is named as a REMAINING gate, not claimed as done. The MO-B half-B convention requires independent Grok review per R20/R33 (this is not a strict MO-B-tagged PR but the convention applies in spirit); the open gate is honestly named.
8. **The PR body "Round" capsules correctly bound the change.** The four numbered release capsules (initial-slice, source-integration, exact-head, interaction-proof head) are explicitly marked "supersedes earlier status/head/count lines retained above as implementation history." This is the doctrine-correct framing for a records-of-the-process narrative that does NOT claim promotion to authority — every quantity is anchored to a specific immutable commit sha (`47bc4b192b68`, `f7077e1f464af6`, `8beadff38b63b`, `9f49dec1` mid-review, `9aae769b`, `cc32ad62`, etc.) rather than to a moving branch, which is the durable fix for the moving-branch-anchored quantity claim class.
9. **The post-merge gates are precisely named.** PR body, final capsule: "Remaining gates are precise: exact-head GitHub CI, independent review/adjudication, lawful Ready/merge, then protected-main `gold_china_basis` source accrual + persisted quality receipt + served Gold-panel browser proof. Official SHAUPM/LBMA-AM remains a separate unavailable canonical method until its own licensed mapping exists." This is the doctrine-correct framing — every gate that COULD promote the artifact is named, and none is claimed as done.
10. **The CI repairs carried by the current head are scoped.** Repo-root import pin for `scripts/audit_china_gold_premium.py` (a real defect found and repaired — the script was a sibling in scripts/ and the import pin was missing); nightly DAG declares the audit directly after `build_commodities`; Data OS exclusive scope covers the actual `engine/china_gold_premium.py` + `lib/store.py` closure; the parquet-backed end-to-end test lives under the existing TuShare collector owner. Each repair is scoped to a single concrete defect and the rationale is named.

No new MISS; no promotion; no A7 claim; no tier raise. The Data OS contracts stay `PROPOSED` until the artifact-bound gate is met.

## Overall verdict

| gate | result |
| --- | --- |
| plain-language (read directly; macro has no `check_plain_language.mjs`) | **PASS** (substantial user-facing strings in the NEW Jinja partial and the appended CSS block — every string is bilingual via the canonical `l-en`/`l-zh` span pattern; the stance clauses are doctrinally-correct honest-null ("Context, not an entry signal." ×3, both languages); the disclosure ("Method & sources") shows the formula, the timestamp, and the sources verbatim; one MINOR caveat: the six fallback `_REASON` strings in `engine/china_gold_premium.py` use data-pipeline jargon like "legs", "entitled", "freshness window" — recommended follow-up to soften `legs` → `feeds` and `entitled` → `licensed` in the fallback tier, non-blocking) |
| theme (`check_design_system.py --mode enforce-added` + CSS audit + evidence matrix) | **PASS** (every new color is a token reference — zero raw hex literals; state tints use `color-mix(in srgb, <token> NN%, var(--line))`; ZERO new JavaScript added — chart is inline SVG driven by `data-cgp-series` JSON; design-system `enforce-added` 0 blockers; runtime-style-injection OK; UI visual-evidence exit 0; evidence matrix is complete — 16/16 canonical-page + 16/16 supplemental Gold-panel + 8/8 close-proxy + 24/24 intraday/unavailable/no-history + 8/8 interaction spread/price + 24/24 hover/keyboard-focus, dark × light × EN/ZH × desktop/mobile × interactive; the design-governance recreation of 179 passed with all self-checks green confirms both themes judged as designs, not as one CSS rendered twice) |
| validated-claims (`check_validated_claims.py`) | **PASS** (zero NEW `validated` / `verified` / `certified` / `proven` / `gauntlet` / `refuted` / `证伪` in the new user-facing template or the new commodity CSS block; the heavy uses of these words in the diff are in the design-spec doc, in evidence manifests, in PR body Markdown, and in Python variable names — none user-facing; display-only boundary is named in FIVE independent places — engine docstring, CSS comment, three template stance clauses, PR body, and the standing workstream ruling; Data OS contracts stay `PROPOSED` until the post-merge artifact-bound gate is met; canonical SHAUPM/LBMA-AM honestly named as unavailable; visual evidence honestly named as fixture-only; `MastermindX1` independent review honestly named as REQUESTED but not RECEIVED) |
| scope discipline (no authority creep) | **PASS** (engine docstring + CSS comment + three stance clauses + PR body all bind the change to "display-only context, no score authority"; silver/copper/oil numerical policy untouched per PR body §Scope; the panel does not rank, size, gate, or originate any trade; the Data OS promotion tool is dry-run by default and rolls back on verification failure) |
| source / rights honesty | **PASS** (canonical SHAUPM/LBMA-AM method returns `_REASON["source_not_entitled"]` whenever a leg lacks `entitled: true` — fail-closed by construction; the close proxy reuses the existing governed data surfaces and rides the existing authoritative US-nightly lane — "no duplicate collection workflow or credential plane was added"; the Massive `C:XAUCNY` entitlement probe adds the exact endpoint to the existing capability manifest plane rather than a new product-specific entitlement ledger; the live vendor-call proof is "deliberately unclaimed" and named as a post-merge natural-path gate; store group `gold_china_basis/` chosen specifically to dodge the US-nightly `data/china_*` reset boundary — a real bug found and repaired before merge with a regression test binding collector identity / product config / nightly reset boundary) |
| evidence matrix (dark × light × EN/ZH × desktop × mobile × interactive) | **PASS — COMPLETE** (96 PNGs in the diff organized into six matrix buckets; both themes judged as designs per the design-governance recreation; the `.cgp-btn` hover and `.cgp-data-scroll` keyboard-focus interactions are explicitly captured; 0 panel-overflow regressions, 0 document-width regressions versus the stacked parent, 0 browser page errors; representative dark/desktop and light/ZH/mobile screenshots visually inspected) |
| independent review (`MastermindX1`, R20/R33 convention) | **OPEN — honestly named** (PR body, final capsule, verbatim: "independent review remains requested from `MastermindX1`." This is the doctrine-correct framing for an unverified request — it is named as a remaining gate, not claimed as done. The MO-B half-B convention requires independent Grok review per R20/R33; this PR is a `feat(commodities)` rather than strictly `[MO-B-REC]`, so the convention applies in spirit; the open gate is honestly disclosed.) |
| exact-head GitHub CI | **RUNNING AT SUBMIT** (PR body, final capsule, verbatim: "Required exact-head GitHub CI is now running." Cannot be PASS until that run concludes; the 469 local tests + 179 design-governance + 214/214 legacy CI manifest validation are local pre-merge evidence, not a substitute for the GitHub-hosted conclusion.) |
| live source proof | **DELIBERATELY UNCLAIMED** (PR body, verbatim: "Live vendor-call proof is still deliberately unclaimed: the next lawful production proof is the normal secret-bearing nightly run after merge. Missing/stale vendor data fails this panel closed rather than substituting futures, ETFs, or unofficial web quotes.") |

**Overall: PASS with three named remaining gates (independent `MastermindX1` review receipt, exact-head GitHub CI conclusion, post-merge secret-bearing nightly source accrual + persisted quality receipt + served Gold-panel browser proof).** This is a half-B data-engineering + UI integration feat that ships a complete material UI packet (new Jinja partial + new CSS block + read-only display VM + governed Shanghai-close proxy collector + source→VM→render quality auditor + dry-run-first Data OS promoter + Massive entitlement probe) for a Gold-only China physical-premium panel on the Commodity Vector. The new template uses the canonical EN/ZH `l-en`/`l-zh` span pattern and is bilingual throughout; the stance clauses are the doctrinal honest-null ("Context, not an entry signal." ×3, both languages); the disclosure ("Method & sources") shows the formula, the timestamp, and the sources verbatim; the new CSS uses design tokens with no raw hex literals and no runtime style injection; the evidence matrix is complete across dark × light × EN/ZH × desktop × mobile × interactive; the visual evidence is honestly named as fixture-only; the Data OS contracts stay `PROPOSED` until the post-merge artifact-bound gate is met; the canonical SHAUPM/LBMA-AM method is honestly named as unavailable; the `MastermindX1` independent review is REQUESTED but not RECEIVED and the PR body names this as a remaining gate. One minor non-blocking recommendation: soften the data-pipeline jargon in the six `_REASON` strings (`legs` → `feeds`, `entitled` → `licensed`) since those are the strings that surface to readers when something is wrong. The macro repo's `scripts/check_plain_language.mjs` referenced in the task prompt does not exist in this repo — plain-language discipline on macro is read directly against the standing design-doctrine rules, which apply to USER-FACING strings, and this PR ships substantial user-facing strings that pass the doctrine cleanly.
