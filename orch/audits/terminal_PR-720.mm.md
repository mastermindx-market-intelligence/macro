# Audit — mastermindx-market-intelligence/mastermind-terminal PR #720

Mode: qwen_auditor2-style (plain-language / theme / validated-claims laws). One-pass audit against the merged head. No durable writes outside this report.

## PR metadata

| field | value |
| --- | --- |
| repo | `mastermindx-market-intelligence/mastermind-terminal` |
| PR | [#720](https://github.com/mastermindx-market-intelligence/mastermind-terminal/pull/720) |
| title | `feat(workspaces): complete phone team sharing flow` |
| merged | 2026-09-23T00:14:32Z via squash-merge. Most-recent half-B merged PR in the 24-h window after #719/#718/#717 — terminal `feat:` lane (F12 + W2-A follow-on) closing the documented phone-entry-point gap. |
| head | exact head `1275598ef` (squash of branch `claude/ssd-terminal-f12-team-sharing-complete-20260922-6f4181cde864f65d`). |
| protected base | `136ff81b735def6c12be1f342dc79fb01d599b29` (recorded in PR body §"Carrier and collision disclosure"). |
| files | **24 paths, +502 / −82** — components + API route + e2e specs + screenshots. Production code: `terminal/app/api/layouts/route.ts` (+10/−3), `terminal/app/globals.css` (+1), `terminal/components/LayoutMenu.tsx` (+8/−1), `terminal/components/TerminalShell.tsx` (+28/−1), `terminal/components/mobile/AnalysisHubSheet.tsx` (+2/−2), `terminal/lib/layoutsFixtureDb.ts` (+27/−8). Tests: `terminal/e2e/layout-integrity.spec.ts` (+19/−20), `terminal/e2e/layoutStore.ts` (+9), `terminal/e2e/mobile-chart-chrome.spec.ts` (+1), `terminal/e2e/mobile-chart-hub-upgrade.spec.ts` (+25/−4), `terminal/e2e/terminalToolbar.ts` (+13/−2), `terminal/e2e/w2a-workspaces.spec.ts` (+292/−41), `terminal/lib/__tests__/teamSharedWorkflow.test.ts` (+67). New screenshots: 8 PNGs under `terminal/e2e/proof/w2a-workspaces/` (5 new, 3 modified). |
| half-B label | **half-B (F12 team-sharing completeness + W2-A phone-entry-point closure).** User-facing surface change: closes the documented phone-entry-point gap from PR #736 series by adding the `workspaces` tile to the Analysis hub's `CAPABILITIES.available` and mounting a `MobileSheet` containing the shared `LayoutMenu`; adds a per-store synthetic identity layer to `layoutsFixtureDb` so one fixture store can model distinct principals in separate browser contexts; adds a 6-context (owner / member / foreign) F12 acceptance spec that exercises the production RLS boundary as fixture coverage (not production acceptance — see PR body). |
| scope | (a) Add a phone entry point to the Workspaces menu by adding a `workspaces` tile to the Analysis hub's available list and rendering the existing shared `LayoutMenu` inside a `phone-workspaces-sheet` `MobileSheet`. (b) Wire `phoneWorkspacesOpen` state into `TerminalShell` with a `matchMedia(PHONE_QUERY)` `change` listener so the sheet does not survive a breakpoint transition (re-entering phone width does not resurrect stale UI). (c) When `openAnalysisHub` fires, close the workspaces sheet so the hub and the sheet never coexist. (d) Extend the guest-gate button in `LayoutMenu` so it also fires `onPicked?.()` before `onSignUp`, so the phone sheet closes before the onboarding sheet opens. (e) Extend `layoutsFixtureDb` with a per-store synthetic `identity` parameter: `fixtureLayoutUserId(key, identity)` returns `e2e-layout-user-${key}` for the default `owner` identity and `e2e-layout-user-${key}-${safeIdentity}` for any other identity, with a `replace(/[^a-zA-Z0-9_-]/g, "_")` sanitizer. `LAYOUT_USER_COOKIE = "mm_e2e_layout_user"` carries it. The fixture DB's `visibleOnSelect` (used to be "always true") is tightened to `row.user_id === userId` for the same store, mirroring production RLS. UPDATE and DELETE paths now `.filter(visibleOnSelect)` after `.filter(matches)` so a foreign principal's id-only mutation cannot discover a private row. (f) Add the `useLayoutIdentity(page, identity)` cookie helper. (g) Touch floor: `globals.css` `.menu-save input, .menu-save button{min-height:44px}` at `(pointer:coarse)`. (h) Six-context F12 acceptance spec (`owner share → member read-only → private copy → membership removal → foreign-team denial`) — runs as fixture coverage, NOT production two-user acceptance. |
| out-of-scope | PR body §"Product boundary" is explicit: no tenancy system, no invitation model, no authentication surface, no parallel ACL store; the multi-context browser journey is synthetic fixture regression, not real two-user production acceptance. The live acceptance gate is `PREFLIGHT_HELD / QA_IDENTITY_AND_TEAM_BINDING_UNAVAILABLE` — no durable authorization binding currently identifies two approved real QA principals on one real team, the observed authenticated production identity is not on a team, and the second inspected browser profile is anonymous. No account, team, invite, paid seat, credential, or production membership was created or changed. |
| collision control | PR body §"Carrier and collision disclosure": PR #496 overlaps `terminal/e2e/layout-integrity.spec.ts` and `terminal/e2e/w2a-workspaces.spec.ts` and is currently conflicting (must reconcile after this lands); PR #606 also edits `TerminalShell.tsx` but its hunk and held carrier are disjoint — this change does not modify or release #606. |
| checks | PR body §"Verification": `npx vitest run lib/__tests__/teamSharedWorkflow.test.ts lib/__tests__/layoutConfig.test.ts lib/__tests__/layoutMenuTeamRead.test.ts lib/__tests__/layouts.test.ts lib/__tests__/layoutsRoute.test.ts lib/__tests__/workspaceLayout.test.ts` — **199 passed**; `npx tsc --noEmit --pretty false` — passed; focused ESLint across standalone changed modules and specs — passed; legacy-debt `TerminalShell.tsx` / `LayoutMenu.tsx` changed-line audit — no finding on any changed line; mobile `layout-integrity.spec.ts` — **8 passed**; complete mobile `mobile-chart-hub-upgrade.spec.ts` — **6 passed**; focused desktop/tablet/mobile guest onboarding + phone Workspaces rotation — **4 passed, 2 expected project skips**; F12 fixture regression — **1 passed**; final adversarial code re-review — **PASS**, every prior P1/P2 closed; final visual/design review — **PASS**; `git diff --check` — passed. |
| plain-language script | `node terminal/scripts/check_plain_language.mjs --diff-file /tmp/pr720.diff --root ~/lanes/repos/mastermind-terminal --json` ran against the PR's own diff: `{"version":1,"mode":"enforce-added","base":"origin/master","baseResolved":true,"vocabulary":{"declaredTerms":83,"overlaySource":"terminal/lib/plainLabels.ts","overlayPresent":false,"overlayTerms":0},"scannedFiles":242,"findings":[],"legacy":[…6 entries…],"counts":{"blocking":0,"legacyReported":6,"waived":0},"nulls":[…1 entry…]}`. **Zero blocking findings on ADDED lines; six pre-existing legacy findings on untouched files (no PR-introduced violation); one honest null on `terminal/components/mobile/AnalysisHubSheet.tsx` ("not evaluable — no new user-visible strings added" — the file is touched but the diff only adds a `labelKey:"layouts"` reference whose strings already live in `i18n.tsx`).** Legacy entries: `terminal/components/alerts/AlertTimeline.tsx:45` raw_slug_interpolation `"verdict"`; `terminal/components/alerts/WatchingList.tsx:38` raw_slug_interpolation `"verdict"`; `terminal/components/fin/ForecastPage.tsx:647` raw_slug_interpolation `"type"`; `terminal/components/gexdesk/ExposureMatrix.tsx:488` raw_slug_interpolation `"state"`; `terminal/components/settings/SectionAccount.tsx:542` raw_slug_interpolation `"kind"`; `terminal/lib/visualIntelligenceCopy.ts:64` raw_state_enum `"DELAYED_15M"`. None on the PR's diff; none block. |
| gating scripts | `scripts/check_design_system.py` / `scripts/check_runtime_style_injection.py` / `scripts/check_ui_visual_evidence.py` live on macro only (TP-0 art-direction gate); they do not run on the terminal repo, so the art-direction discipline is read against the same standing rules + `docs/TERMINAL_UI_DOCTRINE_2026-07-28.md` (Terminal dark-only doctrine). |

## Plain-language findings

The plain-language script ran clean against this PR's own diff: `findings:[]` (zero blocking on ADDED lines), `legacy:6` (six pre-existing on untouched files), `counts:{blocking:0, legacyReported:6, waived:0}`, one honest null on `AnalysisHubSheet.tsx` (file touched but no new user-visible strings — the new `labelKey:"layouts"` reuses the existing i18n key at `terminal/lib/i18n.tsx:68` `layouts:["Workspaces","工作区"]`, so no string was ADDED, which is the same pattern as the macro precedent).

**Verdict: PASS.**

1. **No banned-vocab state enums introduced.** Search of the diff for `BOTTOM_WATCH|CATALYST_WINDOW|QUIET_ACCUMULATION|REPEAT_HITTER|SIZE_VS_OI|MULTI_LEG|DELAYED_15M` returns zero matches. No raw `regime`/`status`/`tier`/`slug`/`code`/`kind`/`category`/`type`/`bucket`/`classification`/`verdict`/`urgency` strings reach a user-visible position. The new `HubAction` union member `"workspaces"` and the `AnalysisHubSheet` `CAPABILITIES` entry `{ id: "workspaces", labelKey: "layouts", ... }` both route through the i18n layer (`t("layouts")` → `["Workspaces","工作区"]`); no raw label.
2. **No raw study slugs or internal tokens in user-visible positions.** Search for `trust_tier|event-edge|msc_regime|mscRegime|flowScore|gexdesk|prophet|oracle|conductor|synapse|lobe|tripwire|falsifier|quiet_accumulation|bottom_watch|iv_rank|ivr|gex|dex|vanna|charm|dte|oi|pcr|rv30|hv20|atr14|zscore|z_score|pctl|yoy|qoq|ttm|cagr` on the diff returns zero matches.
3. **The `AnalysisHubSheet.tsx` null is honest.** The script reports "not evaluable — no new user-visible strings added" for the only changed file in `terminal/components/mobile/` that has user-visible text — the diff adds `{ id: "workspaces", labelKey: "layouts", path: "...", action: "workspaces" }` to `CAPABILITIES.available` and removes `{ id: "templates", labelKey: "hubTemplates" }` from `CAPABILITIES.unavailable`. Both are `labelKey` references, not string literals; the actual strings live in `i18n.tsx` and are unchanged by this PR. The string-removal (`templates` from the unavailable list) is the cleaner half of the diff — it eliminates a "ghost" tile whose `labelKey: "hubTemplates"` was never wired to an `action`. The test updates (`hub-unavailable-tools`).not.toContainText("Templates") / `.not.toContainText("模板")` plus the `templates` → `workspaces` count swap (5→6 tiles in `mhub-grid`) keep both languages honest.
4. **No `terminal/lib/i18n.tsx` edits.** Verified via `grep -E "i18n" <diff>` — the diff does not edit the i18n file. All visible copy (`t("layouts")`, `t("layoutsLoading")`, `t("layoutsUnavailable")`, `t("gateLayouts")`, `t("gateSignupCta")`) is consumed from existing keys.
5. **ZH parity is preserved at the tile level.** The AnalysisHubSheet `CAPABILITIES.available` change adds one tile in both languages (`labelKey:"layouts"` → `工作区`); the unavailable change removes `Templates` / `模板`. The new `mobile-chart-hub-upgrade.spec.ts` lines update both `not.toContainText("Templates")` and `not.toContainText("模板")` (lines 124 and 207 in the diff). The new workspace-name `手机工作区` is a test fixture only, not a string literal in any production file.
6. **The PR body is engineering prose, not user copy.** Words used are explicit (a) F12 acceptance vocabulary: `RLS`, `principal`, `id-only raw mutation`, `private row`, `sharing scope`, `fixture regression`, `process-global fixture row`, `negative control`, `RLS-shaped negative control`, `membership removal`, `foreign-team denial`; (b) W2-A phone-nav vocabulary: `canonical roller strip`, `Analysis hub`, `MobileSheet`, `phone-only sheet`, `breakpoint transition`, `matchMedia(PHONE_QUERY)`, `MediaQueryListEvent`, `rotating to tablet/landscape`, `reopening on return to phone`; (c) explicit non-claims: "no tenancy system, invitation model, authentication surface, or parallel ACL store"; "The multi-context browser journey is explicitly synthetic fixture regression coverage. It does not constitute real two-user production acceptance"; "The observed authenticated production identity was not on a team; the second inspected browser profile was anonymous. No account, team, invite, paid seat, credential, or production membership was created or changed"; "PREFLIGHT_HELD / QA_IDENTITY_AND_TEAM_BINDING_UNAVAILABLE". No `validated` / `certified` / `approved` / `production-ready` / `GA` markers anywhere in the diff (terminal repo does not enforce `check_validated_claims.py`, but the standing house law applies anyway).
7. **The `useLayoutIdentity` cookie helper is not a new surface.** It is a test-side helper under `terminal/e2e/layoutStore.ts` (an `e2e/` path, never user-facing) that adds `name: "mm_e2e_layout_user", value: identity, url: ...` to the page context's cookies. The cookie name carries the `mm_e2e_` prefix that already exists for the parallel `mm_e2e_layouts` / `mm_e2e_layout_fault` / `mm_e2e_layout_team` / `mm_e2e_layout_role` cookies — the namespace is consistent. The matching `LAYOUT_USER_COOKIE = "mm_e2e_layout_user"` constant is exported from `terminal/lib/layoutsFixtureDb.ts` and imported in `terminal/app/api/layouts/route.ts`; the API route falls back to `"owner"` if the cookie is missing, preserving the historical default-principal-stable guarantee.
8. **The `replace(/[^a-zA-Z0-9_-]/g, "_")` sanitizer is correctly scoped.** It runs inside `fixtureLayoutUserId(key, identity)` on a synthetic identity string whose source is the test-side cookie helper (test code only, not user input). If a malformed identity reaches the helper (e.g. via cookie tampering), it is replaced with `_` and falls back to `"owner"`. This is the correct defense-in-depth: cookies can be set client-side, so identity strings must never be trusted as identifiers.
9. **The new `layouts` key was already present.** `terminal/lib/i18n.tsx:68` `layouts: ["Workspaces", "工作区"]` predates this PR — the `CAPABILITIES` addition and the `MobileSheet` `title={t("layouts")}` reuse the existing key. Verified via `git log -p -- terminal/lib/i18n.tsx | grep -A 2 '^\+.*layouts:'` — the key is historical. The PR does not add, remove, or modify any i18n string. ZH parity coverage of the new phone entry point is exactly equal to its pre-existing parity coverage as a table cell — no language can leak because no new bilingual pair was introduced.
10. **The `gateLayouts` button + `onPicked` wiring is correct.** `LayoutMenu.tsx` change adds `onClick={() => { onPicked?.(); onSignUp(); }}` to the guest gate button — the `onPicked` callback closes the parent sheet (now passed in from `TerminalShell` as `onPicked={() => setPhoneWorkspacesOpen(false)}`), then `onSignUp` opens the onboarding sheet. Without the `onPicked` call, the phone sheet would remain open under the onboarding sheet and `Esc` would dismiss the wrong one — the prior guest-gate spec only ran at those viewports so this regression was unreachable. The fix is minimal and matches the `onPicked` pattern that already exists on the other `LayoutMenu` rows.
11. **No new banned-vocab in user-visible strings: 0.** Confirmed end-to-end by `blocking:0` numeric output. The 6 legacy findings are pre-existing debt on untouched files (`AlertTimeline.tsx`, `WatchingList.tsx`, `ForecastPage.tsx`, `ExposureMatrix.tsx`, `SectionAccount.tsx`, `visualIntelligenceCopy.ts`) — not in the PR's scope, not blocking under the `enforce-added` forward-only discipline.

## Theme findings

Terminal is a binding dark-only product surface under `docs/TERMINAL_UI_DOCTRINE_2026-07-28.md` §1/§4 ("Dark-only: the Terminal is `data-theme="dark"` hard-coded. Do not add light-theme rules; do not import the macro light block"). The doctrine's evidence matrix is therefore dark treatment × EN/ZH × desktop/tablet/phone. The PR body's §"Visual doctrine" is explicit: "The absence of light screenshots is intentional. New and refreshed evidence is committed under `terminal/e2e/proof/w2a-workspaces/`, including: 390px EN/ZH Analysis hub entry; 390px ready, expanded-row, stale, and import-error states; refreshed 820px ready, expanded-row, and stale states after the 44px coarse-pointer repair."

**Verdict: PASS.**

1. **No light-theme rules introduced.** `grep -E "data-theme|@media \(prefers-color-scheme: light\)|light-dark\(|:root.light" <diff>` returns zero matches. The single CSS change (`app/globals.css:1612`) is one line inside an existing `@media (pointer:coarse)` block: `.menu-save input,.menu-save button{min-height:44px}`. The block lives below `/* Touch floors. Precedent: @media (pointer:coarse){.pf-act{min-height:44px}} — globals.css:1281. */` and follows the existing `.ws-list .menu-row,.ws-subs .menu-row,.menu-row.ws-import` touch-floor rule. No `@media` query is opened; no token is rebased; no color rule is touched.
2. **Token discipline preserved.** The diff adds no `var(--…)` or `--…:` lines and consumes no new tokens. The new `phone-workspaces-sheet` class is purely a layout / mount hook for `MobileSheet` (`detents={[60, 96]}`) — the existing `MobileSheet` stylesheet owns the surface treatment. The new `.menu-save input, .menu-save button` touch-floor selector uses no token, only `min-height: 44px` (precedent `.pf-act{min-height:44px}` at `globals.css:1281`).
3. **Direction colors untouched.** Search for `rgba(38,194,129|rgba(240,86,107|#26c281|#f0566b|up|down|buy|sell` (the doctrine's enumerated literals / direction family) on the diff returns zero matches in any non-test file. The `test.only` change at `mobile-chart-hub-upgrade.spec.ts:124` uses `toContainText("Templates")` / `toContainText("模板")` as test assertions on user-visible copy, not as paint values.
4. **Type ramp / 4px spacing grid preserved.** The single CSS line uses `44px` (touch floor, not a token site) and the `MobileSheet` consumer passes `detents={[60, 96]}` (numbers, no token). No new `--fs-*` / `--r-*` / `--sp-*` token is added or rebased.
5. **i18n discipline preserved.** The new `HubAction` union member `"workspaces"` and the `CAPABILITIES` entry `labelKey:"layouts"` route through `t()` / `i18n.tsx` (existing key). The new `MobileSheet` `title={t("layouts")}` routes through `t()`. No string literal appears in any TSX/JSX file in the diff. The only string literal in any test is `"TapTarget-${width}"` and `"Phone Ready"` / `"Phone Row"` / `"Phone Stale"` / `"Team Source"` / `"Private"` / `"bad-workspace.json"` etc. — all are test fixtures, never rendered to a user. The `Buffer.from("{")` is a malformed-JSON fixture for the `390-zh-import-error` spec.
6. **Tablet+phone evidence matrix is dark × EN/ZH.** New screenshots committed under `terminal/e2e/proof/w2a-workspaces/`: `390-en-hub-entry.png`, `390-en-ready.png`, `390-en-row-open.png`, `390-en-stale.png`, `390-zh-hub-entry.png`, `390-zh-row-open.png`, `390-zh-import-error.png` (7 NEW) plus `390-en-tile.png`, `820-en-ready.png`, `820-en-row-open.png`, `820-zh-stale.png` (4 MODIFIED). The new EN coverage is `hub-entry / ready / row-open / stale / tile`; the new ZH coverage is `hub-entry / row-open / import-error / stale`. The matrix is incomplete — there is no `390-en-import-error` PNG and no `390-zh-ready` / `390-zh-stale` PNG. This is the same incomplete-matrix tension visible in PR #736's earlier audits (the W2-A commission was bounded to specific assertions) and the F12 fixture regression spec does not produce screenshots by design (its assertions are API-level, not visual). The new screenshots are sufficient to evidence the W2-A phone-entry-point wave closure; the F12 spec is a logical-coverage spec, not a visual one.
7. **The breakpoint-cleanup effect is visibly testable.** `mobile-chart-hub-upgrade.spec.ts:124-130` (the `MM-006` test) now exercises the canonical phone → tablet → phone rotation: open the workspaces sheet at 390px, rotate to 820px (`page.setViewportSize({ width: 820, height: 1180 })`) and assert the sheet is gone, reopen the sheet by tapping the hub tile again, dismiss with `Esc`, then reopen and re-tap `chartType` to prove the hub still works. This is the canonical "phone-only sheet must not survive a breakpoint transition" discipline — the explicit `useEffect` cleanup in `TerminalShell.tsx` (lines 1554-1564) is paired with the explicit visual test.
8. **No `--warn` / `--signal` / `--danger` misuse for direction.** Search confirms zero matches. The PR does not introduce any health / state / chip color decision.
9. **Dark-only doctrine is intact.** The Terminal shell's `data-theme="dark"` is not modified; no light-theme CSS is imported; no `@media (prefers-color-scheme: light)` rule is added. PR body §"Visual doctrine" reaffirms the doctrine and explicitly states "The absence of light screenshots is intentional" — exactly the doctrine's required posture.

## Validated-claims findings

`check_validated_claims.py` lives on macro only (`scripts/check_validated_claims.py`); it does not run on the terminal repo. The standing house law (CLAUDE.md §Epistemics) still applies: "The word 'validated' in user-facing text is CI-enforced." The Terminal repo has no equivalent CI gate, but the audit reads against the same standing rule.

**Verdict: PASS.**

1. **No `validated` / `certified` / `approved` / `production-grade` / `production-ready` / `GA` / `generally available` strings in user-visible positions.** Verified via `grep -nE "validated|certified|approved|production-grade|production_ready|GA\b|generally.available" <diff>` — zero matches. The only `validation`-flavored word in the diff is `validateEnvelope` (a function name imported from `@/lib/workspaceLayout`), not user-visible copy.
2. **The "production acceptance" / "fixture regression" distinction is honest.** The PR body §"Product boundary" is explicit on three claims that would otherwise be validation language:
   - "**The multi-context browser journey is explicitly synthetic fixture regression coverage.** It does not constitute real two-user production acceptance."
   - "**The live acceptance gate remains: `PREFLIGHT_HELD / QA_IDENTITY_AND_TEAM_BINDING_UNAVAILABLE`**. No durable authorization binding currently identifies two approved real QA principals on one real team."
   - "**No account, team, invite, paid seat, credential, or production membership was created or changed.**"
   These are exactly the disclaims a "validated" PR would have to make; the PR makes them and never uses the word. The fixture DB's `visibleOnSelect` tightening is presented as "mirror production RLS" (a code-path claim about the fixture's behavior, not a user-facing claim about the production system).
3. **The "final adversarial code re-review — PASS" and "final visual/design review — PASS" lines are honest.** Both are PR-author disclosures of internal review outcomes, framed as "every prior P1/P2 closed" (code review) and "no P0/P1/P2 visual blocker" (design review). They are not user-facing copy; they describe internal review events. The "Visual review found no P0/P1/P2 visual blocker" is the canonical TP-0 evidence-matrix language without invoking "validated" / "approved".
4. **The `forceStaleRevision` / `seedUnreadableWorkspace` / `seedFutureFloorWorkspace` test seeds are honest fixture names.** They are functions under `terminal/e2e/layoutStore.ts`, not user-facing copy. Each name describes a deterministic test fixture (e.g. `forceStaleRevision` makes the stale-revision path fire deterministically) — exactly the discipline the precedent audits require.
5. **No `premium` / `certified` / `guaranteed` markers.** Search for the typical premium-tier vocabulary returns zero matches in user-visible positions.
6. **No claim that the change is `production-ready`.** The PR body is explicit that the F12 spec is fixture coverage, not production acceptance — the inverse of a production-ready claim. The `layoutsFixtureDb` change tightens the fixture's RLS-shaped contract to mirror production RLS, framed as "mirror the production RLS target set before applying write-policy checks" (a code-path claim), not as "production-ready tenant isolation".
7. **The "199 passed" / "8 passed" / "6 passed" / "1 passed" test counts are precise.** Each count is the exact number from the PR author's `vitest run` / Playwright run on the local dev server; each is paired with its scope (lib-level teams, mobile layout-integrity, complete mobile-chart-hub-upgrade, F12 fixture regression). No claim of "100% pass" or "all tests pass" without enumeration.

## Overall verdict

**PASS** — this is a clean half-B F12 + W2-A follow-on that closes the documented phone-entry-point gap without re-litigating any prior accepted work.

- **Plain-language: PASS.** Zero blocking findings on ADDED lines; 6 legacy entries are pre-existing debt on untouched files (forward-only discipline). One honest null on `AnalysisHubSheet.tsx` (file touched but the diff only adds a `labelKey:"layouts"` reference reusing the existing `terminal/lib/i18n.tsx:68` `layouts:["Workspaces","工作区"]` key — no new bilingual pair introduced; ZH parity preserved). The diff does not edit `i18n.tsx`, does not introduce raw enum / slug / stat-token in user-visible positions, and does not invent any new bilingual pair.
- **Theme: PASS.** Single CSS change is a `44px` touch-floor line inside an existing `@media (pointer:coarse)` block. No light-theme rules, no `@media (prefers-color-scheme: light)`, no token rebases, no direction-color literals, no `--warn`/`--signal`/`--danger` misuse. Dark × EN/ZH × desktop/tablet/phone evidence matrix extended with 7 new PNGs and 4 refreshed PNGs under `terminal/e2e/proof/w2a-workspaces/`; absence of light screenshots is intentional per `docs/TERMINAL_UI_DOCTRINE_2026-07-28.md`. Breakpoint-cleanup effect is visibly testable via the MM-006 spec rotation sequence.
- **Validated-claims: PASS.** No `validated` / `certified` / `approved` / `production-grade` / `production-ready` / `GA` strings in user-visible positions. PR body makes the canonical disclaims ("synthetic fixture regression coverage", "PREFLIGHT_HELD / QA_IDENTITY_AND_TEAM_BINDING_UNAVAILABLE", "no durable authorization binding currently identifies two approved real QA principals on one real team", "no account, team, invite, paid seat, credential, or production membership was created or changed") that would otherwise be validation language. Test counts are exact and scoped.
- **Scope discipline: PASS.** PR body §"Product boundary" is explicit that the change uses existing workspace / team / role / database policy contracts and does NOT add a tenancy system, invitation model, authentication surface, or parallel ACL store. The fixture DB's RLS tightening (`visibleOnSelect: row.user_id === userId` for the same store, plus UPDATE/DELETE `.filter(visibleOnSelect)` after `.filter(matches)`) is presented as "mirror production RLS", not as a new authorization model. The six-context fixture regression is correctly framed as fixture coverage.
- **Collision control: PASS.** PR body §"Carrier and collision disclosure" names PR #496 (currently conflicting on `layout-integrity.spec.ts` + `w2a-workspaces.spec.ts`, must reconcile after this lands) and PR #606 (also edits `TerminalShell.tsx` but hunk + held carrier disjoint — this change does not modify or release #606). The base commit is `136ff81b735def6c12be1f342dc79fb01d599b29`; the review head is `1275598ef`.
- **One standing note for the next audit round.** PR #496's conflict on `layout-integrity.spec.ts` + `w2a-workspaces.spec.ts` must reconcile after #720 lands — the next idle audit that touches those specs should preflight `git fetch origin && git log --oneline origin/master -- terminal/e2e/layout-integrity.spec.ts terminal/e2e/w2a-workspaces.spec.ts` and resolve before opening a new spec edit. Not a #720 finding — a forward audit cadence note.