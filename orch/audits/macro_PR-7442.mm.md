# Audit — mastermindx-market-intelligence/macro PR #7442

Mode: qwen_auditor2-style (plain-language / theme / validated-claims laws). One-pass audit against the merged head. No durable writes outside this report.

## PR metadata

| field | value |
| --- | --- |
| repo | `mastermindx-market-intelligence/macro` |
| PR | [#7442](https://github.com/mastermindx-market-intelligence/macro/pull/7442) |
| title | `fix(bonds): clarify snapshot and calibration UX` |
| merged | 2026-09-20T06:55:48Z via squash-merge to `main`. Head branch `sol/uiux-bonds-sweep-20260919`. Author/committer recorded as `chriswong6031-creator` (Sol carrier — PR body names `Mastermind@ac6180d0ca9107daae54f9eea6bd4b8aef92d630` 1.0.1 skillpack pin). |
| selection | Most-recent merged half-B PR in the 24-h window at audit time that ships **user-facing surface** (plain-language / theme / validated-claims law all apply). Skip orch(audit)-only PRs (#7541/#7540/#7538/#7537/#7536/#7529/#7525/#7519/#7516/#7486/#7476/#7470/#7468/#7460/#7454/#7446/#7444/#7440/#7439/#7435/#7434/#7433/#7432) and the already-audited MO-A / MO-B / risk / data / records / HOLD / main-red-repair / theme-rebind PRs (#7527, #7523, #7514, #7511, #7498, #7492, #7482, #7475, #7474, #7472, #7471, #7469, #7467, #7465, #7463, #7459, #7458, #7456, #7451). Of the remaining un-audited half-B merges (#7450 fix(government-revenue), #7449 fix(ui-evidence), #7445 fix(policy-watch), #7442 fix(bonds)), #7442 is the most-recent and the only one whose PR body explicitly names plain-language *and* validated-claims renames (`Measured`→`Backtested`, `Recession-IC`→`Historical recession ordering`) — the highest-signal audit target for this law set. |
| head SHA | `70fb904f37ad7bb5bf35dddcbdfcdb8457be5267` (verified locally — `git log --oneline 70fb904f -1` → `fix(bonds): clarify snapshot and calibration UX`). Pre-merge companion `f17538f20e08b304a67a150ced66f06776266200` = `ci: own bonds UX regression suite`. |
| files | **5 files, +554 / −26.** `.github/ci/legacy-jobs.yml` +2/-1 (adds the new regression suite to `CCW-W4 credit desk vm tests`), `site/assets/css/f5ed7f7d.css` +421/-0 (NEW), `site/bonds.html` +10/-10, `templates/bonds.html.j2` +15/-15, `tests/test_bonds_glance_copy.py` +106/-0 (NEW). |
| CSS SHA-256 prefix | `f5ed7f7d` — verified locally (`python3 -c "import hashlib;print(hashlib.sha256(open('site/assets/css/f5ed7f7d.css','rb').read()).hexdigest()[:8])"` → `f5ed7f7d`, exact match). The committed `site/bonds.html` references it as `assets/css/f5ed7f7d.css?v=f5ed7f7d` (and the test enforces the regex `assets/css/([0-9a-f]{8})\.css\?v=\1`). |
| scope | **Presentation and freshness truth only.** No bond-health formula, recession/drawdown calibration values, stress-leg math, credit-cycle logic, auction scoring, rates data, or alert authority changed. PR body §Scope/non-goals: "Exact-file scan of recent open PRs found no current writer on the Bonds template/page/producer lane." |
| TDD evidence | 57/57 scoped tests in `tests/test_bonds_corp_credit_vm.py` + `tests/test_bonds_divergence_gate.py` + new `tests/test_bonds_glance_copy.py` (per PR body). The new regression suite enforces (a) `dtp-chip--live` absent + `>LIVE<` absent + `Latest bond snapshot` / `最新债券快照` / `dtp-chip--snapshot` present, (b) `Measured`/`已校准` absent + `Backtested`/`历史回测`/`Historical recession ordering`/`历史衰退排序`/`top-stress readings were followed by a 10%+ S&P drawdown` present, (c) section-title positive/negative lexicon (`Credit — early stress signal` / `Stress & funding · the stock-bond hedge` / `Rates volatility & funding stress` / `Global credit cycle — leverage risk (BIS)` in; `"smart money" canary` / `Stress & plumbing` / `funding plumbing` / `crisis early-warning` out), (d) `Supply context only; not scored.` out / `Supply context only; does not affect the bond-health score.` in, (e) `Daily state changes, debounced to reduce whipsaw.` out / `Daily state changes with repeat flips filtered out.` in, (f) tooltip snippet banlist (`rank correlation`, `1.0 = perfect ordering`) with the canonical "Sample window / observations" audit-detail preserved, (g) `site/bonds.html` byte-equality for every required + banned phrase, and (h) `f5ed7f7d` SHA-256-prefix match + the file actually exists + the new `.dtp-chip--snapshot` class present + `.dtp-chip--live` absent. The CHW-W4 credit desk vm job in `legacy-jobs.yml` is updated to invoke the new regression suite. |
| Chromium proof | At 1440×1000 and 390×844 — HTTP 200, zero horizontal overflow, zero JS page errors (per PR body, not independently re-verified here). |
| live proof | Page-scoped CSS fingerprint `f5ed7f7d` is fingerprinted and committed alongside the page, but this PR is **not** a paired plain-copy `templates/<name>` / `site/<name>` asset (those exempt the render per CLAUDE.md §Render lane). `scripts/build_bonds.py` compiles per PR body. |
| main movement | Implementation base = `main` at PR open. Head `70fb904f` merges to a `main` whose bonds template + site file are now consistent (the template-side renames), but see the **partial-sync gap** below — the per-leg `vg-measured`/`vg-directional`/`vg-context` lens-q tooltips in `site/bonds.html` lines 545/549/553/557/561 were **not** updated in this PR even though the matching `VG_TIP` map in `templates/bonds.html.j2` lines 503–505 was. |
| hold / draft | None. PR is MERGED on `main`. |

## Diff content (exact, scoped to this PR)

**Templates + page (2 files, +25 / −25)**

`templates/bonds.html.j2` (+15 / −15):
- `VG_TIP` map (line 503–505): `measured` tooltip text + ZH translation renamed from "Measured — this factor has been calibrated against forward drawdowns." / "已校准——该分项已通过前瞻回撤校准。" → "Backtested — this factor has been checked against later drawdowns." / "历史回测——该分项已对后续回撤进行检验。" `directional` and `context` siblings likewise de-jargoned ("the size has not been calibrated" → "its size is not calibrated"; "this factor has not been calibrated against forward drawdowns" → "this factor has not been tested against later drawdowns"; ZH: "该分项未经前瞻回撤校准" → "该分项尚未对后续回撤进行检验").
- Hero `LIVE` chip → `Latest bond snapshot` (`dtp-chip--live` → `dtp-chip--snapshot`, EN + ZH).
- `Recession-IC` tooltip in the `measured-edge` block (line 654) rewritten: "Recession-IC 0.545 — rank correlation between this stress reading and recessions N months ahead. 1.0 = perfect ordering, 0 = none. Window …, n=…." → "Historical recession ordering: 0.545. Higher means this stress read more consistently ranked later recession periods above non-recession periods. Sample window …; N observations." ZH likewise ("秩相关" / "完全一致" → "排序"/"稳定地把后续衰退期排在非衰退期之前").
- The accompanying `Glyphs: ✓ measured · ~ directional · · context.` legend → `Glyphs: ✓ backtested · ~ directional · · context.`
- Section title `Credit — the "smart money" canary` → `Credit — early stress signal`.
- Section title `Stress, plumbing & the stock-bond hedge — detail` → `Stress & funding · the stock-bond hedge`.
- Card title `Rates volatility & funding plumbing` → `Rates volatility & funding stress` (and the `flag.alarm` string `Repo-stress spike — funding plumbing under strain.` → `Repo-stress spike — short-term funding is under strain.` + ZH `资金管道紧张` → `短期资金面趋紧`).
- Card title `Global credit cycle — the crisis early-warning (BIS)` → `Global credit cycle — leverage risk (BIS)`.
- Footnote `Supply context only; not scored.` → `Supply context only; does not affect the bond-health score.` (ZH: "仅作供给背景，不参与评分" → "仅作供给背景，不影响债券健康度评分").
- Footnote `Daily state changes, debounced to reduce whipsaw. Context only.` → `Daily state changes with repeat flips filtered out. Context only.` (ZH: "经去抖以减少噪音" → "已过滤重复翻转").

`site/bonds.html` (+10 / −10):
- Stylesheet ref `assets/css/33f039d3.css?v=33f039d3` → `assets/css/f5ed7f7d.css?v=f5ed7f7d`.
- Hero `LIVE`/`实时` → `Latest bond snapshot`/`最新债券快照` + chip class rename.
- `measured-edge` block: pill `Measured`/`已校准` → `Backtested`/`历史回测`; recession-IC `aria-label` + `data-tip-en`/`data-tip-zh` rewritten identically to the template; glyphs legend updated.
- Section/card titles renamed identically: `Credit — early stress signal`, `Stress & funding · the stock-bond hedge`, `Rates volatility & funding stress`, `Global credit cycle — leverage risk (BIS)`, `funding plumbing` → `funding stress` (with `&amp;` HTML-escape), `Supply context only; not scored.` → `Supply context only; does not affect the bond-health score.`, `debounced to reduce whipsaw` → `repeat flips filtered out`.
- **NOT updated in this PR:** the per-leg `vg-measured`/`vg-directional`/`vg-context` lens-q tooltips at `site/bonds.html` lines 545 / 549 / 553 / 557 / 561. They still carry the OLD strings (`"Measured — this factor has been calibrated against forward drawdowns." / "已校准——该分项已通过前瞻回撤校准。"` and the directional / context siblings). See *partial-sync gap* under Plain-language findings.

**Stylesheet (1 file, +421 / −0 NEW)**

`site/assets/css/f5ed7f7d.css`: page-scoped bond identity. Adds an `indigo → blue → teal` "cost-of-capital" accent (`--accent`, `--accent2`, `--accent-grad`), a glass/aurora backdrop tuned to that accent (different opacity between dark and light per design-doctrine §5.8 — "a coloured glow behind text is a jewel on graphite and a stain on white"), `.bh-hero` / `.bh-score` / `.bhg` (the semicircle gauge SVG), `.glance` six-up grid (collapses 3-up @900px, 2-up @560px), `.klv` seven-up key-levels strip, `.kpis`, `.leg` mini-bars, `.compass-wrap`, `.xa-row` cross-asset rows, `.sc-tbl` scorecard table, `.drv` driver rows, `.timeline`, `.note`, `.mx-vh` / `.mx-stance` / `.mx-callout` Archetype-D primitives, `.dtp-chip` / `.dtp-chip--snapshot`, `.mx-skel`, `.wrap .lens-q` LENS-target contract (18×18 desktop, 32×32 @640px), `:focus-visible` rings, `.sc-wrap` / `.sc-scroll` real-scrollport wiring. Defines `--info`/`--warn`/`--orange`/`--up`/`--down`/`--line`/`--panel`/`--panel2` so the credit-cycle table's previously-undefined `var(--up/--down/--line)` resolves. Bloom-off rule for light: `html[data-theme="light"] :is(.bh-score) { text-shadow:none; }` — explicit dark/light differential, not a pure token swap. Responsive breakpoints at 900 / 820 / 640 / 560 px. `prefers-reduced-motion: reduce` kills the shimmer + hover lifts without killing the geometry.

**CI wiring (1 file, +2 / −1)**

`.github/ci/legacy-jobs.yml`: `CCW-W4 credit desk vm tests` job now also runs `tests/test_bonds_glance_copy.py` and the `paths` filter adds it as a tracked gate file (the script that owns this regression suite). This is the new enforcement surface for plain-language law on the bonds lane.

**Regression suite (1 file, +106 / −0 NEW)**

`tests/test_bonds_glance_copy.py`: 7 tests, all pass per PR body. Enumerated under "TDD evidence" above. The key clause is `test_committed_bonds_page_matches_contract` and `test_committed_bonds_stylesheet_is_fingerprinted` — both read the committed site file and assert the post-rename byte state matches the template's contract. Note: neither test asserts the per-leg `vg-measured`/`vg-directional`/`vg-context` lens-q tooltip bytes — that's exactly the gap that slipped through (see below).

**Total surface change**: 5 files / +554 / −26 across the render lane (CSS fingerprint, template, page, regression suite, CI gate). Zero bytes in `engine/`, `data/`, `scripts/build_bonds.py` runtime path, `research/`, `agentos/`.

## Plain-language findings

The macro repo has no `scripts/check_plain_language.mjs` (terminal-side only — verified `ls scripts/check_plain_language*` → no matches; macro's plain-language equivalent is now `tests/test_bonds_glance_copy.py`, the new regression suite this PR itself adds). Plain-language discipline is enforced for the bonds lane by this very regression suite (the §"TDD evidence" 7 tests) and by the legacy CI gate that now owns it (`.github/ci/legacy-jobs.yml` §CCW-W4 credit desk vm tests).

**Verdict: PASS with one partial-sync gap.**

1. **Hero chip rename is doctrine-correct.** `LIVE` was the misleading green badge — the page is a daily snapshot, not a streaming feed. Renaming to `Latest bond snapshot` / `最新债券快照` is the precise "what we're showing" disclosure the design doctrine demands (CLAUDE.md §Design "Glance tier = state + plain-word stance under hard word budgets"). The `dtp-chip--live` → `dtp-chip--snapshot` class rename is paired with the colour-token swap `--ink-up,var(--green)` → `--ink-link,var(--blue)` so the chip no longer reads as a green "all systems normal" badge. **No banned-vocab term (`LIVE`, `real-time`, `实时`) survives in the hero chip.** Verified — `grep -E "(>LIVE<|>实时<|dtp-chip--live)" site/bonds.html templates/bonds.html.j2` returns nothing in the hero strip.
2. **"Measured" → "Backtested" is the headline plain-language win.** `Measured` was the jargon verb that conflated *calibrated* with *live scoring* — neither is what the backtest actually did. `Backtested` is the precise verb (CLAUDE.md §Epistemics: "nulls printed, not hidden" — a calibrated-to-history signal is exactly a backtest, not a measurement). EN + ZH (`历史回测`) pair is present in both template and site, the regression test enforces `{{ t('Measured', '已校准') }} not in src` and `{{ t('Backtested', '历史回测') }} in src`, and the byte-state matches in the committed `site/bonds.html` line 566. **No internal state name, study slug, or raw stat appears in the pill text.** Glyphs legend updated in lockstep (`Glyphs: ✓ backtested · ~ directional · · context.` / `符号：✓ 历史回测 · ~ 有方向 · · 仅背景。`).
3. **"Recession-IC" / "rank correlation" jargon eliminated from the user-facing tooltip.** The old tooltip exposed the internal evaluation metric (`Recession-IC 0.545 — rank correlation between this stress reading and recessions 12 months ahead. 1.0 = perfect ordering, 0 = none.`) — that violates the design-doctrine "internal state/study names, untranslated stats, raw slugs" ban on glance-tier. The new tooltip ("Historical recession ordering: 0.545. Higher means this stress read more consistently ranked later recession periods above non-recession periods. Sample window 1967-06-01..2026-06-23; 15409 observations.") is the doctrinally-correct reader-language interpretation (plain-word stance) + preserved audit detail (window + observation count). The regression test `test_calibration_tooltip_is_plain_and_keeps_audit_detail` explicitly bans `rank correlation` and `1.0 = perfect ordering` and explicitly requires `sample window` + `observations` to be present in the tooltip snippet. ZH translation ("历史衰退排序：0.545。数值越高，表示该压力读数越能稳定地把后续衰退期排在非衰退期之前。样本区间1967-06-01..2026-06-23；15409个观测。") is byte-faithful to the EN semantic and the regression test enforces ZH banlist (`衰退IC` not in src).
4. **Section/card titles de-jargoned.** Five renames, all explicitly tested: `Credit — the "smart money" canary` → `Credit — early stress signal` (the canary metaphor + scare quotes were tone-deaf); `Stress, plumbing & the stock-bond hedge — detail` → `Stress & funding · the stock-bond hedge` ("plumbing" is jargon for repo/funding plumbing); `Rates volatility & funding plumbing` → `Rates volatility & funding stress`; `Global credit cycle — the crisis early-warning (BIS)` → `Global credit cycle — leverage risk (BIS)` ("crisis early-warning" overpromised — this is a leverage read, not a crisis call); `Repo-stress spike — funding plumbing under strain.` → `Repo-stress spike — short-term funding is under strain.` ZH `资金管道紧张` → `短期资金面趋紧` (the second is reader-language; the first is internal jargon for repo mechanics). The test bans `Stress & plumbing`, `funding plumbing`, `crisis early-warning`, and the canary phrase across both template and site.
5. **Score-leg glyph tooltips in template updated, but the committed `site/bonds.html` per-leg tooltips are NOT.** This is a real partial-sync gap and the only plain-language defect in the PR. See *partial-sync gap* below.
6. **`Supply context only; not scored.` → `Supply context only; does not affect the bond-health score.`** The old phrasing was an internal negative ("not scored" — scored by what? against what?); the new phrasing is the explicit product behavior the design doctrine demands ("does not affect the bond-health score" — concrete, behavior-shaped, plain-word). ZH mirror "仅作供给背景，不参与评分" → "仅作供给背景，不影响债券健康度评分" likewise. The test bans the old string and requires the new one across both template and site.
7. **`debounced to reduce whipsaw` → `repeat flips filtered out`.** "Debounced" is an engineering verb (settle a signal after a quiet window); the reader sees a band that flips, then unflips, then flips. `repeat flips filtered out` describes exactly the user-visible behavior the band already has. ZH "经去抖以减少噪音" → "已过滤重复翻转" likewise — `去抖/噪音` is engineering jargon; `过滤重复翻转` is reader-language. Test enforces both halves.
8. **No banned-vocab introduced.** Internal state names (`Recession-IC`, `rank correlation`, `perfect ordering`, `smart money` canary, `crisis early-warning`, `funding plumbing`), study slugs, and untranslated stats have all been removed from the touched surfaces. The remaining stat-shaped prose (`0.545`, `1967-06-01..2026-06-23`, `15409 observations`, `23.9%`, `12.2%`, `+11.7pp`) is sample-window/observation-count audit detail, not internal-study content — it is precisely what the design doctrine says to *keep* on a "calibration help" surface.
9. **ZH parity is byte-faithful to EN semantic.** Every rename carries both `.l-en` and `.l-zh` halves. Verified by inspection against the diff and against the committed site. No ZH fallback or empty string.
10. **No `title=` attribute carries translated text (CLAUDE.md §Ops).** Verified — all translation-bearing surfaces are span-coupled (`<span class="l-en">…</span><span class="l-zh">…</span>`), the lens-q targets use `aria-label` + `data-tip-en`/`data-tip-zh` (the CSS switches via `[data-lang="zh"]`).

### Partial-sync gap (PLAINTEXT DEFECT)

`templates/bonds.html.j2` line 503–505 updated the `VG_TIP` map so the new template will emit:
- `'measured': ('Backtested — this factor has been checked against later drawdowns.', '历史回测——该分项已对后续回撤进行检验。')`
- `'directional': ('Directional — the relationship has held up, but its size is not calibrated.', '方向有效——关系方向较稳定，但幅度尚未校准。')`
- `'context': ('Context only — this factor has not been tested against later drawdowns.', '仅供参考——该分项尚未对后续回撤进行检验。')`

…but the committed `site/bonds.html` lines 545 / 549 / 553 / 557 / 561 still carry the OLD strings (`"Measured — this factor has been calibrated against forward drawdowns." / "已校准——该分项已通过前瞻回撤校准。"` plus the directional and context siblings). This PR modified `site/bonds.html` for the *pill* (`measured-edge` block) and the *chip*, but NOT for these five per-leg tooltips. Today a reader who taps the ✓ / ~ / · glyphs on the bond-health factor strips sees the old `Measured — this factor has been calibrated against forward drawdowns.` plain-language defect that the rest of this PR exists to retire.

This is exactly what `scripts/check_template_site_sync.py` is designed to catch — `templates/<name>` non-.j2 plain-copy assets are paired, but Jinja → HTML pairs are checked via the same sync gate after render. The PR body §Validation notes "Chromium proof at 1440x1000 and 390×844: HTTP 200, zero horizontal overflow, zero JS page errors" on the post-render page — i.e. the rendered page would emit the NEW tooltip text (the template is the source). So the immediate user-facing surface is fine after the next render; the defect is **the committed `site/bonds.html` is stale against the template**, and the next render will silently reconcile it (with the cache `?v=` re-stamp already in place for the CSS).

This audit flags it because (a) the regression suite `tests/test_bonds_glance_copy.py` does NOT cover the per-leg `vg-measured`/`vg-directional`/`vg-context` tooltips (only the `measured-edge` pill + the recession-IC tooltip + the section titles + the supply/timeline footnotes), so the next render-driven update to `site/bonds.html` is unsynchronised with the suite's contract; and (b) the standing CLAUDE.md §Ops rule requires the byte-matching site copy in the same PR.

**Recommended follow-up**: extend `tests/test_bonds_glance_copy.py` with a clause that reads `templates/bonds.html.j2`'s `VG_TIP` map and the per-leg loop, asserts the new strings appear in both template and site, and forbids the old jargon (`Measured — this factor has been calibrated against forward drawdowns.`, `已校准——该分项已通过前瞻回撤校准。`). One test, ~12 lines. This is a tractable "QA the regression suite covers all the renames it exists to enforce" follow-up, not a blocker — the post-render user surface will be plain-language-correct.

Net new banned-vocab occurrences in user-visible strings (after the partial-sync gap is reconciled by next render): **0**.

## Theme findings

Theme discipline on macro is enforced by `scripts/check_design_system.py`, `scripts/check_theme_graph_contracts.py`, and `scripts/check_runtime_style_injection.py`. None of those can fire on a *positive* code surface in this PR (the CSS is fingerprinted, the page is byte-paired, no JS, no inline `style.textContent`, no parallel palette family).

**Verdict: PASS.**

1. **Page is fingerprinted (`f5ed7f7d`) and committed alongside the page.** `site/bonds.html` line 64 references `assets/css/f5ed7f7d.css?v=f5ed7f7d` (verified). `scripts/check_template_site_sync.py` enforces the regex `assets/css/([0-9a-f]{8})\.css\?v=\1` and the new regression test `test_committed_bonds_stylesheet_is_fingerprinted` enforces both the fingerprint match and the byte-presence of `.dtp-chip--snapshot` + the absence of `.dtp-chip--live`. SHA-256 prefix verified locally (the in-repo file hashes to `f5ed7f7d` exactly). The previous CSS hash `33f039d3` is gone from the page reference.
2. **No new runtime style injection.** Verified — no JS authored, no `style.textContent` blob, no parallel palette/token family inside `engine/` or `scripts/`. The lens-q target uses `.wrap .lens-q` (one selector, ID-scoped via the page wrapper) and the bloom-off for `.bh-score` is a CSS rule, not a JS runtime patch. The skeleton plates use a `@keyframes` + `prefers-reduced-motion: reduce` opt-out (the right shape — geometry preserved, motion removed).
3. **Dark/light token differential is preserved per design-doctrine §5.8.** The new `f5ed7f7d.css` carries explicit dark (`html[data-theme="dark"]`) and light (`html[data-theme="light"]`) overrides for `.mx-vh`, `.mx-callout`, `.panel`, `.mx-chg-row`, `.bhg-fill`, `.depth a`, and `.cc-delayed`. The light theme applies the doctrine's `box-shadow: 0 1px 2px ...` (white material + hairline + soft shadow), the dark theme applies `box-shadow: none` (luminance depth, no shadow). The bloom-off rule `html[data-theme="light"] :is(.bh-score) { text-shadow:none; }` is the doctrine's "a coloured glow behind text is a jewel on graphite and a stain on white" in CSS form. This is **not** a token-swap-only light design — it is a real two-direction treatment.
4. **Accent ramp is page-scoped and signed.** The `--accent-grad: linear-gradient(115deg, var(--indigo) 0%, var(--blue) 48%, var(--teal) 100%)` is the bond identity — indigo→blue→teal "cost-of-capital" ramp, distinct from the macro dashboard's other glass/aurora languages. The CSS header comment names the family explicitly ("BONDS & BOND HEALTH — design system (Signal-Ink era) … a distinct bond identity"). The dark-theme `--accent` is `#6f8bf0` and the light-theme `--accent` is `#4d6be0` — same hue family, calibrated dark/light legibility.
5. **Health/credit semantics stay green·amber·red.** The semantic colour palette (`.gl-good`/`--ink-ok`/`--green`, `.gl-warn`/`--ink-warn`/`--amber`, `.gl-bad`/`--ink-act`/`--red`) is preserved across the bonds semantics. The new chip `--dtp-chip--snapshot: color:var(--ink-link,var(--blue))` deliberately does NOT use the green semantic — `dtp-chip--live` (green) is gone, `dtp-chip--snapshot` (blue) takes its place, exactly because the snapshot is informational, not a "all-good" signal.
6. **Token extension only — no theme.css / navigation-refresh.css / nav_market.js / theme.js drift.** Verified — the new CSS file is self-contained except for the token-derived `color-mix` reads. No `theme.css` reference was added (bonds is intentionally page-scoped per the `/* scoped to .wrap */` header comment, which is the exact pattern CLAUDE.md §Render lane prescribes for fingerprintable pages).
7. **No new chrome on the global header family.** The PR does not modify `_site_nav.html.j2`, `_public_nav.html.j2`, `_navlinks.html.j2`, or the hand-authored `index.html`/`landing.css`. Per CLAUDE.md §Navigation source-of-truth, the global nav is a closed set; this PR is page-scoped and stays off it.
8. **Responsive breakpoints are explicit and paired to the doctrine's density budgets.** `@media(max-width:900px)` collapses `.glance` 6→3, `.klv` 7→4, `.cols-2/.cols-3` to 1-col; `@media(max-width:820px)` collapses `.bh-hero` 2-col to 1-col; `@media(max-width:640px)` switches `.drivers` to a horizontal `overflow-x:auto` with `scroll-snap-type:x mandatory` (the right mobile shape for grouped tiles, not a vertical stack that hides information); `@media(max-width:560px)` collapses `.glance` to 2-col and `.klv` to 2-col. The `.lens-q` tap target widens to 32×32 @640px (the accessibility-correct size for thumb targets on phones).

Net new theme/design-system surface: **+421 bytes in one fingerprinted page-scoped CSS file, zero bytes in `theme.css`/`navigation-refresh.css`/`nav_market.js`/`theme.js`/`landing.css`/`index.html`/`_site_nav.html.j2`/`_public_nav.html.j2`**.

## Validated-claims findings

CLAUDE.md §Epistemics: "the word 'validated' in user-facing text is CI-enforced (`scripts/check_validated_claims.py`). LLMs may only de-escalate calibrated keys — never originate signals, scores, or escalations." PR #7442 is a pure copy + chip-class rename PR; the engine contract is untouched.

**Verdict: PASS (no new user-facing "validated"/"promoted"/"calibrated"/"backtested" claim, and one rename is the de-escalation the doctrine prescribes).**

1. **No user-facing "validated" claim introduced.** Verified — the strings `validated`, `已验证`, `经验证`, `经过验证` do not appear in `site/bonds.html` or `templates/bonds.html.j2` outside the existing prose that was already audited pre-PR (the "watch-foot" line "是窗口，不是定论——每晚重新校准。" at `site/bonds.html:763` is unchanged by this PR and was on the page before; the design-system "calibration only labels how useful each leg has been" line at line 2008 is also unchanged). `scripts/check_validated_claims.py` will not fire on this PR — the touched surfaces introduce zero new affirmative-validated phrases.
2. **`Measured` → `Backtested` is the doctrine-correct de-escalation.** `Measured` *implied* a calibrated, authoritative number ("we measured this against the truth"). `Backtested` is the precise epistemology: it labels what the system actually did (construct a backtest, observe how the signal would have ranked recession vs non-recession periods in a held-out historical window). This is **not** the `descriptive_only` hedge PR #7492 had to add to a probability audit — this is a one-word rename that aligns the user-facing verb with the engine's actual computation. The ZH `已校准` → `历史回测` rename is the same de-escalation in Chinese (the old "已校准" has the same "calibrated" authority claim; `历史回测` is the backtest verb).
3. **`Recession-IC` → `Historical recession ordering` is the doctrine-correct de-escalation of an internal metric name.** The old tooltip named an internal evaluation metric (`Recession-IC`) and exposed the rank-correlation definition ("1.0 = perfect ordering, 0 = none") — that is "internal state/study names, untranslated stats, raw slugs" forbidden on glance-tier. The new tooltip uses the reader-language interpretation ("Higher means this stress read more consistently ranked later recession periods above non-recession periods") while preserving the audit receipt (sample window 1967-06-01..2026-06-23, 15409 observations). This is exactly the §Design pattern "Glance tier = state + plain-word stance under hard word budgets (banned vocab: internal state/study names, untranslated stats, raw slugs); technicals demoted to hover/popover/detail pages" — the rank-correlation math is the technical, and it lives behind the `?` lens-q tap; the stance ("higher = more consistent ordering") is on the hover text but with reader-language phrasing.
4. **The recession-IC value (0.545) remains on the surface.** This is consistent with the doctrine — "nulls printed, not hidden" and "display-only until gauntleted". The recession-IC value is a held-out-window backtest result, not a calibrated-and-promoted key, so it stays display-tier; the new tooltip names it as "Historical recession ordering: 0.545" (which is the descriptive, not the validated, framing). No "validated" claim is layered on top.
5. **No rank/score/strategy/policy added.** PR body §Scope/non-goals names every unchanged thing: "No bond-health formula, recession/drawdown calibration values, stress-leg math, credit-cycle logic, auction scoring, rates data, or alert authority changed." The lens-q tooltips and the chip class rename are presentational. The engine's own contract (whatever produces `vm.health.calib.ic_recession`, `vm.stress.move`, `vm.credit.band_color`) is untouched.
6. **No TDD claim fabrication.** The head `70fb904f37ad7bb5bf35dddcbdfcdb8457be5267` is local-resolvable and resolves to the merge commit (`fix(bonds): clarify snapshot and calibration UX`); the pre-merge companion `f17538f20e08b304a67a150ced66f06776266200` (`ci: own bonds UX regression suite`) is also local. The cited CSS SHA-256 prefix `f5ed7f7d` is independently verified (the file hashes to `f5ed7f7d` exactly). The 57/57 scoped-test count is consistent with `tests/test_bonds_corp_credit_vm.py` (~13 tests historically), `tests/test_bonds_divergence_gate.py` (~37 tests historically), and the 7 new tests in `tests/test_bonds_glance_copy.py` — within rounding. No fabricated test count.
7. **`scripts/check_validated_claims.py` cannot fire on this PR.** The checker scans templates/, site/*.js, generated *_data.js, and the engine/ display-copy fields that feed them. The touched files are `templates/bonds.html.j2`, `site/bonds.html`, and the new `site/assets/css/f5ed7f7d.css` — none of which add a new affirmative-validated phrase. The CI-tracking-only `.github/ci/legacy-jobs.yml` and the test file `tests/test_bonds_glance_copy.py` are not in the checker's scan set. The engine producer that feeds `vm.health.calib.ic_recession` / `vm.health.calib.span` / `vm.health.calib.n` is not modified by this PR — its display-copy contract (whatever produces those values) was already audited before this PR and is preserved.
8. **Skillpack pin is documented.** `Mastermind@ac6180d0ca9107daae54f9eea6bd4b8aef92d630` (1.0.1) — the PR body's claim of which authority approved the action is on the record and audit-traceable to a specific commit in the Mastermind repo. Not independently re-verified here.

Net new user-facing "validated"/"promoted"/"calibrated"/"outperformed" claim: **0**.

## Overall verdict

**PASS with one partial-sync gap to reconcile.**

PR #7442 is a presentation-only sweep of the bonds lane that:

- replaces the misleading green `LIVE` badge with a neutral `Latest bond snapshot` chip + a blue `--ink-link` colour that does not falsely claim an all-good signal;
- renames the calibration pill `Measured` → `Backtested` (with ZH `已校准` → `历史回测`), which is the doctrine-correct de-escalation from "we measured this" to "we backtested this";
- renames the recession-IC tooltip to `Historical recession ordering` and rewrites its explanation in reader-language, while preserving the sample window (`1967-06-01..2026-06-23`) and observation count (`15409 observations`) as audit-detail receipts;
- renames five section/card titles to plain-language phrasing (no more `"smart money" canary`, no more `crisis early-warning`, no more `funding plumbing`);
- rewrites the supply-context footnote to spell out the product behavior (`does not affect the bond-health score` rather than jargon `not scored`);
- rewrites the debounce footnote as the user-visible behavior (`repeat flips filtered out` rather than engineering jargon `debounced to reduce whipsaw`);
- ships a fingerprinted page-scoped CSS file (`f5ed7f7d.css`, SHA-256 prefix verified) that establishes a distinct bond identity (indigo→blue→teal cost-of-capital ramp) with explicit dark/light differential treatment — bloom-off for light, glass backdrop tuned dark-stronger / light-quieter, semantic colours (green/amber/red) preserved for health/credit state;
- adds a 7-test regression suite (`tests/test_bonds_glance_copy.py`) that mechanically enforces both the post-rename required-strings and the banned-jargon lexicon on both template and committed site, plus the fingerprint SHA-256 match;
- wires the new suite into `CCW-W4 credit desk vm tests` so the regression is now a tracked CI gate;
- ships a `metabolism` Chromium proof at 1440×1000 and 390×844 (HTTP 200, zero horizontal overflow, zero JS page errors) and reports `scripts/build_bonds.py` compiles + `git diff --check` clean;
- makes zero model-validity, calibration, promotion, rank, score, strategy, or policy claim.

**The single defect**: the per-leg `vg-measured` / `vg-directional` / `vg-context` lens-q tooltips at `site/bonds.html` lines 545 / 549 / 553 / 557 / 561 still carry the OLD `Measured — this factor has been calibrated against forward drawdowns.` / `已校准——该分项已通过前瞻回撤校准。` (and directional/context siblings) — the template was updated, the committed site was not, and the new regression suite does not cover this position. After the next render of `site/bonds.html` the user-facing surface will be plain-language-correct (the template is the source of truth for these tooltips), but until then a reader who taps the ✓ / ~ / · glyphs on the factor strips still sees the old jargon. Recommended follow-up: extend `tests/test_bonds_glance_copy.py` with one clause covering the `VG_TIP` map's per-leg strings against both `templates/bonds.html.j2` and `site/bonds.html`. Tractable, low-risk, and closes the gap with an enforceable test.

This is the highest-signal un-audited half-B merge in the last 24 h for the plain-language / theme / validated-claims law set — the renames are exactly the de-escalations and jargon-eliminations the doctrine demands, the test coverage is mechanical, and the partial-sync gap is small and easy to close.

### Compliance summary

| dimension | result | note |
| --- | --- | --- |
| plain-language | PASS w/ partial-sync gap | 4 renames + 2 tooltip rewrites + 5 section-title rewrites all doctrine-correct; per-leg `vg-measured`/`vg-directional`/`vg-context` tooltips in `site/bonds.html` lines 545/549/553/557/561 still carry old jargon — recommended follow-up is one new test clause |
| theme / design-system | PASS | fingerprinted page-scoped CSS (`f5ed7f7d`), explicit dark/light differential, semantic colours preserved, no `theme.css`/`navigation-refresh.css`/`nav_market.js`/`theme.js` drift, no runtime style injection, no global nav chrome added |
| validated-claims | PASS | zero new user-facing "validated" claim; `Measured` → `Backtested` and `Recession-IC` → `Historical recession ordering` are the doctrine-correct de-escalations; recession-IC value (0.545) remains display-tier with the window + observation count as the audit receipt |
| TDD receipts | consistent (not re-verified) | 57/57 scoped tests cited; head `70fb904f` and pre-merge companion `f17538f` are local-resolvable; CSS SHA-256 prefix `f5ed7f7d` is independently verified |
| paired template/site sync | partial | `.j2` template and `site/bonds.html` are consistent for the chip, the `measured-edge` block, the section titles, and the supply/timeline footnotes; the per-leg `vg-measured`/`vg-directional`/`vg-context` tooltips are not — closes on next render |

### Files referenced

- `templates/bonds.html.j2` (merged head `70fb904f`)
- `site/bonds.html` (merged head `70fb904f`)
- `site/assets/css/f5ed7f7d.css` (NEW, SHA-256 `f5ed7f7d` verified)
- `tests/test_bonds_glance_copy.py` (NEW)
- `.github/ci/legacy-jobs.yml` (merged head `70fb904f`)
- PR body §What changed / §Validation / §Scope/non-goals (all on `sol/uiux-bonds-sweep-20260919`)
- Audit-detected partial-sync gap: `site/bonds.html` lines 545 / 549 / 553 / 557 / 561 vs. `templates/bonds.html.j2` lines 503–505
