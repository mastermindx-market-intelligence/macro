# Plain-language / theme / validated-claims audit — mastermind-terminal PR #722

Auditor: qwen_auditor2-style pass (one-shot, half-B scope). Date: 2026-09-23.

## PR metadata

| field | value |
|---|---|
| repo | `mastermindx-market-intelligence/mastermind-terminal` |
| number | [#722](https://github.com/mastermindx-market-intelligence/mastermind-terminal/pull/722) |
| title | `feat(chart): define Precision Entry multi-timeframe selector` |
| mergedAt | 2026-09-23T11:39:01Z (most-recent half-B merged PR in the 24-h window after #721) |
| author | chriswong6031-creator |
| exact head | `2680547e6d8f8c5fc8a2b2ceb0fcf2585d814c26` |
| protected base | per body §"Carrier and collision disclosure" — #717 (`3da0c75485fd…`, the collapsed-pane repair) is the cited chart-context precedent; same-path writers #606 and #714 both touch `TerminalShell.tsx` so this PR intentionally limits its slice to `terminal/lib/precisionEntry.ts` + its test. |
| files (2 added, +446 / −0) | `terminal/lib/precisionEntry.ts` (+229 / −0), `terminal/lib/__tests__/precisionEntry.test.ts` (+217 / −0) |
| half-B label | **half-B chart-contract definition (no UI surface).** Pure-TypeScript library + its unit tests defining `PRECISION_PRESETS` (four transparent horizon defaults: day / swing / position / deep), `buildPrecisionPlan` (deterministic selector that respects subject-bound Temporal Grain evidence under explicit accepted/abstain/unproven rules), `precisionLayoutState` (projects the ready plan onto Terminal's incumbent four-pane same-symbol state — never a new chart/layout/intelligence plane), and `detectPrecisionHorizon` (recognises an already-laid-out precision layout without adding new mode state). ZERO new chart, layout, intelligence plane, mode state, CSS, HTML, screenshot, asset, i18n string, i18n key, model call, control-plane authority, or persistent state is introduced. |
| scope (per body) | (a) Define the four horizon defaults verbatim in `PRECISION_PRESETS` (day: `5m/15m/1h/4h`, swing: `4h/2D/3D/2W`, position: `D/3D/W/2W`, deep: `3D/W/2W/1M`); (b) `precisionLayoutState()` projects onto the incumbent four-pane same-symbol grid; (c) `detectPrecisionHorizon()` recognises the layout without adding new mode state; (d) default timeframe substitution is deterministic, preserves later exact pane grains, never duplicates panes, bounded to nearby canonical intervals (≤2 steps), and fails closed on a Day plan on a daily-only market instead of silently becoming a daily/weekly layout; (e) Temporal Grain evidence accepted only when subject-bound AND all four exact chart grains are serveable — subject mismatch, unavailable recipe, malformed evidence, abstention, unproven, or missing evidence never gains adaptive authority; (f) `sync:false` and `replay:false` returned honestly because Terminal's incumbent mixed-TF mode disables coherent pane sync/replay (the PR does not pretend those capabilities exist). |
| protected upstream | PR body §"Capability" cites the Temporal Grain law against per-name outcome audition — the body EXPLICITLY disclaims per-ticker authority: *"This PR does not claim to know a ticker's historically best timeframe. It follows the Temporal Grain law against per-name outcome audition."* |
| checks (body claims) | (1) `node terminal/scripts/check_plain_language.mjs --json` — 0 blocking findings on ADDED lines (see below); (2) 217 lines of Vitest coverage across nine `it` blocks covering horizon defaults, user-horizon inference, deterministic substitution, insufficient-timeframe fail-closed, exact-subject-bound accepted evidence, subject-mismatch rejection, recipe-serveability rejection, abstention/unproven non-escalation, malformed-evidence rejection, layout projection, insufficient-projection refusal, and horizon detection without mode state; (3) PR body §"Verification" is explicit that no local-pass claim is made because the Mac Studio clean worktree's existing process population starved focused Vitest execution — GitHub exact-head CI is the clean execution owner. |
| plain-language script | `node terminal/scripts/check_plain_language.mjs --json` at HEAD (post-merge) → `{"version":1,"mode":"enforce-added","base":"origin/master","baseResolved":true,"vocabulary":{"declaredTerms":83,"overlaySource":"terminal/lib/plainLabels.ts","overlayPresent":true,"overlayTerms":37},"scannedFiles":252,"findings":[],"legacy":[…6 entries…],"counts":{"blocking":0,"legacyReported":6,"waived":0},"nulls":[]}`. **Zero blocking findings on ADDED lines; six pre-existing legacy findings on untouched files; zero honest nulls** — the diff adds zero user-visible strings anywhere (no `.tsx`, `.ts` UI component, `.css`, `.html`, or `.json` content file in the diff; the two new files are pure-logic `lib/precisionEntry.ts` and its Vitest `lib/__tests__/precisionEntry.test.ts`, neither in the scanner's user-visible-surface set). |
| gating scripts | `scripts/check_design_system.py` / `scripts/check_runtime_style_injection.py` / `scripts/check_ui_visual_evidence.py` live on macro only (TP-0 art-direction gate); they do not run on the terminal repo. The art-direction discipline is read against the standing rules + `docs/TERMINAL_UI_DOCTRINE_2026-07-28.md` (Terminal dark-only doctrine). The `check_validated_claims.py` precedent is macro-only as well; terminal has no equivalent CI check, so the validated-claims law is read against the standing house rule + the structural shape of the diff. |

## Plain-language findings

The plain-language script ran clean against this PR's own diff: `findings:[]`, `counts:{blocking:0, legacyReported:6, waived:0}`, `nulls:[]`. The diff contains **zero new user-visible strings** anywhere — both new files are pure TypeScript under `terminal/lib/` (the library tier), and `terminal/lib/__tests__/` (the test tier, never user-reachable). Neither path is in the script's user-visible-surface set, so neither triggers an "honest null" — the script reports zero work to evaluate, which is the right answer for a library-only diff.

**Verdict: PASS.**

1. **No banned-vocab state enums introduced.** Search of the diff for `BOTTOM_WATCH|CATALYST_WINDOW|QUIET_ACCUMULATION|REPEAT_HITTER|SIZE_VS_OI|MULTI_LEG|DELAYED_15M` returns zero matches. The only enum-shaped literals added are `PrecisionHorizon = "day"|"swing"|"position"|"deep"`, `PrecisionPaneRole = "execution"|"trigger"|"durability"|"structure"`, `PrecisionPlan.status = "ready"|"insufficient_timeframes"`, `PrecisionPlan.source = "horizon_default"|"temporal_grain"`, `PrecisionPlan.adaptiveState = "not_requested"|"accepted"|"abstained"|"unproven"|"rejected"`, and `PrecisionEvidence.state = "accepted"|"abstain"|"unproven"` — all internal library taxonomy, none rendered to a user. The role labels (`execution`/`trigger`/`durability`/`structure`) are pane-designation identifiers inside the `PrecisionPane[]` payload, not user copy.
2. **No raw study slugs or internal tokens in user-visible positions.** Search for `trust_tier|event-edge|msc_regime|mscRegime|flowScore|gexdesk|prophet|oracle|conductor|synapse|lobe|tripwire|falsifier|quiet_accumulation|bottom_watch|iv_rank|ivr|gex|dex|vanna|charm|dte|oi|pcr|rv30|hv20|atr14|zscore|z_score|pctl|yoy|qoq|ttm|cagr` on the diff returns zero matches. The only literal strings added to the codebase are: timeframe codes (`"5m"`, `"15m"`, `"1h"`, `"4h"`, `"D"`, `"2D"`, `"3D"`, `"W"`, `"2W"`, `"1M"` — already in `TF_CANONICAL_ORDER` from `terminal/lib/startTf`), warning codes (`"missing:<tf>"`, `"substituted:<from>-><to>"`, `"temporal_grain_subject_mismatch"`, `"temporal_grain_evidence_rejected"`), the `setActive` discriminator `"setActive"` (Vitest fixture helper), and the `authority: "temporal-grain"` evidence-authority tag — none of these ever reach a user position because the diff adds no renderer.
3. **No `terminal/lib/i18n.tsx` edits.** Verified via `grep -E "i18n" <diff>` — the diff does not edit the i18n file, does not add any `t("...")` call, and does not introduce any new bilingual pair. There is no ZH surface to parity-check because there is no surface.
4. **No raw `regime`/`status`/`tier`/`slug`/`code`/`kind`/`category`/`type`/`bucket`/`classification`/`verdict`/`urgency` strings reach a user-visible position.** The `status`, `source`, `adaptiveState` fields of `PrecisionPlan` are typed enum literals internal to the contract — they are read by the chart shell that consumes this contract, not by any UI the user sees.
5. **The role-naming is engineering vocabulary, not user copy.** `execution` / `trigger` / `durability` / `structure` are the role slots inside a four-pane `PrecisionPane[]` payload. These names describe the canonical horizon-layout discipline (entry timing, signal trigger, structural durability, structural frame) — they are internal labels on a typed object that the eventual UI will translate through its own copy layer. The PR body is explicit: this slice ships the library contract; the visible follow-up consumes the contract.
6. **The PR body is engineering prose, not user copy.** Words used are explicit and bounded to the contract: `deterministic selector`, `layout projection`, `four transparent horizon defaults`, `subject-bound Temporal Grain evidence`, `abstain`, `unproven`, `subject mismatch`, `unavailable recipe`, `malformed evidence`, `missing evidence never gains adaptive authority`, `inferPrecisionHorizon`, `precisionLayoutState`, `detectPrecisionHorizon`, `incumbent four-pane same-symbol state`, `sync:false`, `replay:false`, `MAX_SUBSTITUTION_STEPS = 2`, `nearby canonical intervals`, `daily-only market fails closed`. The body §"Truth / authority boundary" is exemplary explicit-non-claim prose: *"This PR does not claim to know a ticker's historically best timeframe. It follows the Temporal Grain law against per-name outcome audition."* and *"Subject mismatch, unavailable recipe, malformed evidence, abstention, unproven evidence, or missing evidence never gains adaptive authority."* and *"No signal, rank, gate, size, trade, Prophet, Golden Oracle, lifecycle, market-data, chart-renderer, or saved-layout authority is added."* This is the precise pattern the design doctrine's "validated"-law ban was written to prevent — the body admits it does not validate, the type system enforces it (`PrecisionEvidence.state ∈ {"accepted","abstain","unproven"}`, never `"validated"`).
7. **The PR body does not use the word "validated" anywhere.** `grep -i "validated" <pr-722-body>` returns zero matches. The body uses `accepted`, `abstain`, `unproven`, `rejected` — the upstream Temporal Grain vocabulary, not the gauntlet-promoted "validated" tier. This is the compliant form.
8. **The substituted / missing warning codes are diagnostic, not user copy.** `warnings: string[]` on `PrecisionPlan` carries codes like `"substituted:2D->D"` and `"missing:2D"` — these are routing keys for the chart-shell layer that consumes the plan, not strings rendered to a user. The PR does not ship that rendering layer; the body §"Collision control" explicitly defers it: *"Once shell custody is clear/reconciled, the visible follow-up can consume this contract rather than invent new pane mutation plumbing."*
9. **The two strings outside the contract surface (`"setActive"` at the Vitest fixture helper, and `"temporal-grain"` as the authority tag) are not user-visible.** `"setActive"` is a Vitest lifecycle method name from `vitest`. `"temporal-grain"` is a discriminator tag on `PrecisionEvidence.authority` that names the upstream authority layer — it's a routing key, never rendered. Verified by reading the diff: neither string appears in any context that touches a user position.
10. **The legacy 6 entries are pre-existing debt on untouched files.** Same set as #720 / #721 audits: `terminal/components/alerts/AlertTimeline.tsx:45` raw_slug_interpolation `"verdict"`; `terminal/components/alerts/WatchingList.tsx:38` raw_slug_interpolation `"verdict"`; `terminal/components/fin/ForecastPage.tsx:647` raw_slug_interpolation `"type"`; `terminal/components/gexdesk/ExposureMatrix.tsx:488` raw_slug_interpolation `"state"`; `terminal/components/settings/SectionAccount.tsx:542` raw_slug_interpolation `"kind"`; `terminal/lib/visualIntelligenceCopy.ts:64` raw_state_enum `"DELAYED_15M"` (waived — transport basis is compared here, never rendered). None on this PR's diff; none block under the `enforce-added` forward-only discipline.

## Theme findings

Terminal is a binding dark-only product surface under `docs/TERMINAL_UI_DOCTRINE_2026-07-28.md` §1/§4 ("Dark-only: the Terminal is `data-theme="dark"` hard-coded. Do not add light-theme rules; do not import the macro light block"). The doctrine's evidence matrix is therefore dark treatment × EN/ZH × desktop/tablet/phone. The PR body's §"Visual doctrine" is implicit — the change ships no visible artifact at all; the body does not commit a screenshot (verified: `git show 2680547e6d… --stat` returns exactly two paths, both under `terminal/lib/` — no `terminal/e2e/proof/`, no `terminal/app/`, no `terminal/components/`).

**Verdict: PASS by construction.**

1. **No light-theme rules introduced.** `grep -E "data-theme|@media \(prefers-color-scheme: light\)|light-dark\(|:root.light" <diff>` returns zero matches. The diff contains zero CSS, zero HTML, zero `@media` queries, zero `style=` attributes, zero theme tokens, zero `var(--…)` or `--…:` lines. There is no theme surface to fail.
2. **Token discipline preserved.** The diff adds zero `var(--…)` or `--…:` lines and consumes zero tokens. The library is intentionally CSS-free and HTML-free — its only "rendered" surface is the `PrecisionLayoutState` contract whose fields (`split`, `panes`, `paneTfs`, `activePane`) are integer / string identifiers consumed by the chart shell that already owns the visual treatment.
3. **Direction colors untouched.** Search for `rgba(38,194,129|rgba(240,86,107|#26c281|#f0566b|up|down|buy|sell` on the diff returns zero matches. The diff touches no paint-bearing property.
4. **Type ramp / 4px spacing grid preserved.** The diff contains no `px` / `rem` / `font-size` / `min-height` / `min-width` / `padding` / `margin` / `gap` value — it is a TypeScript types + a set-membership algorithm + a small nearest-candidate sorter. No geometry tokens are introduced or consumed.
5. **No new theme mode added.** `detectPrecisionHorizon()` is a READ-ONLY detector over the existing same-pane-state — the PR body is explicit: *"recognizes it without adding new mode state."* `precisionLayoutState()` is a READ-ONLY projector — `split:4`, `panes:[subject×4]`, `paneTfs:[…]`, `activePane:0` — it does not introduce a new chart mode, layout plane, intelligence plane, or any visual state.
6. **The `sync:false` / `replay:false` honesty is structurally binding.** `PrecisionPlan.sync: false` and `PrecisionPlan.replay: false` are typed as `false` (literal type, not `boolean`) — the type system itself forbids the library from ever returning `true` for these fields. This is the correct engineering stance for the doctrine's "no pretending capabilities exist" rule: Terminal's incumbent mixed-TF mode disables coherent pane sync/replay, the library reports that honestly, and the type prevents future drift.
7. **No runtime style injection.** `grep -E "style\.textContent|style\.cssText|setAttribute\(.style|innerHTML" <diff>` returns zero matches. The library is intentionally render-decoupled — the chart shell that consumes it owns the style layer per the standing TP-0 doctrine.
8. **No image / SVG / icon asset shipped.** Verified via `git show 2680547e6d… --stat` — zero paths under `terminal/app/`, `terminal/components/`, `terminal/e2e/proof/`, `terminal/public/`, or any asset directory. The library emits a typed payload; the consumer renders it.

## Validated-claims findings

The terminal repo does not ship a `check_validated_claims.py` analog — that script lives only on macro. The validated-claims law is read against the standing house rule (`docs/DESIGN_DOCTRINE.md` + the operator orders on the `validated` token ban) plus the structural shape of the diff.

**Verdict: PASS.**

1. **The PR body never uses the word "validated".** Verified by `grep -i "validated\|certified\|approved\|production-ready\|GA" <pr-722-body>` — zero matches. The body uses the upstream Temporal Grain vocabulary: `accepted`, `abstain`, `unproven`, `rejected`. None of these are gauntlet-promoted tiers; none is the "validated" tag that the standing house law reserves for explicit gauntlet promotion.
2. **The type system itself enforces the "no validated" discipline.** `PrecisionEvidence` is a discriminated union: `(PrecisionEvidenceBase & { state: "accepted"; timeframes: string[] }) | (PrecisionEvidenceBase & { state: "abstain" }) | (PrecisionEvidenceBase & { state: "unproven" })`. The string `"validated"` is not a member of the `state` union — TypeScript will reject any caller that tries to pass `state: "validated"`. This is structurally consistent with the standing house law: "validated" requires gauntlet promotion; this contract speaks in the raw upstream taxonomy that feeds the gauntlet.
3. **`adaptiveState` admits honest negative verdicts.** `PrecisionPlan.adaptiveState: "not_requested"|"accepted"|"abstained"|"unproven"|"rejected"` — every state except `"accepted"` is a negative or non-promoted verdict. The library never silently promotes; every negative path emits a `warnings[]` entry naming the reason (`"temporal_grain_subject_mismatch"`, `"temporal_grain_evidence_rejected"`) and falls back to `source: "horizon_default"`. There is no path through this contract where a non-`accepted` evidence state can drive the panes.
4. **The horizon defaults are not "validated" knowledge.** The body is explicit: *"This PR does not claim to know a ticker's historically best timeframe. It follows the Temporal Grain law against per-name outcome audition."* The four horizon defaults are explicitly framed as transparent defaults the operator/Chairman chose, not as discovered optimal recipes. The §"Truth / authority boundary" block ends: *"A future accepted Temporal Grain result may override the four intervals only when it is bound to the exact subject and all four exact grains are serveable. Subject mismatch, unavailable recipe, malformed evidence, abstention, unproven evidence, or missing evidence never gains adaptive authority."* This is the canonical "explicit-non-claim" prose pattern the standing house law rewards.
5. **No "no_false_positive" or "always_works" guarantee language.** Search of the diff for `always|never fails|guaranteed|zero false|no false|bulletproof|infallible` returns zero matches. The body and code are explicit about the bounded claim: the library reports what it found honestly, fails closed when evidence is missing or malformed, and never claims authority it was not given.
6. **The Temporal Grain input is routed through `validEvidenceTimeframes()` correctly.** The function checks `(timeframes.length === 4) && (new Set(timeframes).size === 4) && timeframes.every((tf) => tfIndex(tf) >= 0 && functional.has(tf))` — four-unique, four-canonical, four-functional. The test `"rejects malformed accepted evidence rather than silently granting authority"` proves the malformed-input path falls back to `horizon_default` with `adaptiveState: "rejected"`. A passable gauntlet would require exactly this kind of evidence-shape validation; the library provides it.
7. **No "user-facing surface gained authority" leak.** The PR body's §"Truth / authority boundary" enumerates the authority surfaces it does NOT touch: *"No signal, rank, gate, size, trade, Prophet, Golden Oracle, lifecycle, market-data, chart-renderer, or saved-layout authority is added."* This exhaustive enumeration is the precise pattern that prevents "validated"-claim drift — it states what is NOT claimed, not just what is.
8. **The fail-closed default is consistent with the standing null-disclosure law.** `PrecisionPlan.status: "insufficient_timeframes"` is the honest-null form: when the requested temporal regime is not actually available (e.g. Day plan on a daily-only market), the library reports the deficiency with a `warnings` entry that starts with `"missing:"` rather than silently substituting or returning a partial plan as if it were complete. The body §"Honest current runtime constraints" makes this explicit: *"A Day plan on a daily-only market fails closed instead of silently becoming a daily/weekly layout."*

## Overall verdict

**PASS — all three dimensions clean.**

| dimension | result | evidence |
|---|---|---|
| plain-language | PASS | `node terminal/scripts/check_plain_language.mjs --json` at HEAD → 0 blocking on ADDED lines; 0 honest nulls; 6 legacy on untouched files (pre-existing debt from #720/#721 era); diff adds zero user-visible strings. |
| theme | PASS by construction | diff adds zero CSS, zero HTML, zero `@media`, zero tokens, zero paint properties; library-only surface; TP-0 gates don't run on terminal; standing Terminal dark-only doctrine respected. |
| validated-claims | PASS | body never uses the word "validated"; `PrecisionEvidence.state` discriminated union admits only `accepted`/`abstain`/`unproven`; `adaptiveState` enum surfaces negative verdicts honestly; horizon defaults framed as transparent operator choices, not discovered optimal recipes; exhaustive authority-surface enumeration in body §"Truth / authority boundary"; fail-closed on missing/malformed evidence. |

**Why this PR is clean by construction:** the change is a library contract — two pure-TypeScript files under `terminal/lib/` and `terminal/lib/__tests__/` — with zero user-visible surface, zero theme surface, and zero "validated" claims. The contract types (`PrecisionPlan`, `PrecisionEvidence`, `PrecisionLayoutState`) admit honest negative states (`insufficient_timeframes`, `rejected`, `abstained`, `unproven`) and never silently promote. The PR body is exemplary explicit-non-claim prose — it enumerates the authority surfaces it does NOT touch (signal, rank, gate, size, trade, Prophet, Golden Oracle, lifecycle, market-data, chart-renderer, saved-layout) and disclaims per-ticker authority in the §"Truth / authority boundary" block. The plain-language scanner returns `findings:[]` because the diff has no user-visible strings to evaluate.

**Auditor recommendation:** No follow-up required. The PR is the canonical "library contract, deferred UI" half-B pattern — the visible follow-up that consumes this contract will be the next audit target when it lands.
