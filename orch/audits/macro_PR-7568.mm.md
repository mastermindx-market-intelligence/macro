# Audit — mastermindx-market-intelligence/macro PR #7568

Mode: qwen_auditor2-style (plain-language / theme / validated-claims laws). One-pass audit against the merged head. No durable writes outside this report.

## PR metadata

| field | value |
| --- | --- |
| repo | `mastermindx-market-intelligence/macro` |
| PR | [#7568](https://github.com/mastermindx-market-intelligence/macro/pull/7568) |
| title | `[MO-A] ci: wire tests/test_unified_dashboard_b1.py (v2 — supersedes #7557)` |
| merged | 2026-09-21T02:21:41Z via squash-merge to `origin/main`. Most-recent merged half-B non-audit-record PR in the 24-h window after the closeout of `#7573 / #7571 / #7569 / #7558 / #7539 / #7535 / #7534 / #7527` (all already audited). This is round 11 of the macro CI wiring program — a CI-config heal, not a design / copy / theme / engine wave. |
| head | exact head `e53fbcb106f090adbfcd37008a92c2dd3a284a86` (squash of branch `claude/mo-a-wire-udb1-suite-v2`). |
| author | `chriswong6031-creator` (operator), bot label absent — the PR is operator-authored because the predecessor `#7557` was wedged by a 409 ("Head branch was modified") loop the bot seat could not escape, so the operator rebuilt the same identical one-line edit on a fresh branch and merged it by hand. |
| base | `origin/main` immediately after `#7557` was closed-as-superseded at 2026-09-21T01:46:11Z. The diff is on top of fresh main with no other open-PR siblings on `.github/ci/legacy-jobs.yml` at squash time (verified via the preceding `#7561` audit-record merge at 22:55:52Z, the only prior touch on that path in the last 24 h, and that PR added `orch/audits/mastermindx-market-intelligence_pr7514.mm.md` — no CI-config collision). |
| files | **1 path, +1 / −0** — `.github/ci/legacy-jobs.yml`, line `2929` (within the `render-guard + engine-contract tests (rot sweep 2026-07-02)` step). |
| half-B label | **half-B (CI infra wiring heal).** No template, token, copy, design-system, theme, dashboard surface, data, or engine-contract change. The "B1" referenced in the title is the Unified Macro Dashboard hero skeleton, but this PR does NOT touch the skeleton, the B1 specs, the tokens, or the specimen — it only wires the existing test file (`tests/test_unified_dashboard_b1.py`, 981 lines, already on main since `[MO-A UD-B1-R5] Unified dashboard hero fast-follow (#7511)`) into the long pytest command that already runs in the `render-guard + engine-contract tests` step of `engine-render-guards`. |
| scope | **CI wiring heal — super-session of #7557.** Append the literal line `tests/test_unified_dashboard_b1.py` to the existing pytest command on `engine-render-guards`'s `render-guard + engine-contract tests (rot sweep 2026-07-02)` step, immediately after the existing `tests/test_ud_b2_w1_vw_fold.py` and before `tests/test_ud_b2_w1_red_first_proof.py`. The line is alphabetically adjacent to the existing UD-B2-W1 entries, consistent with the same step's `tests/test_ud_*` grouping convention. The unwired suite makes the PR-only differential contract-delta gate flag every PR whose base predates it (the body names run `35535665498` on `#7047` as the receipt). |
| do-not-redo | (a) **DO NOT redo #7557.** PR body closes `#7557` as superseded; that PR's head was provably unmoved and its CI run SUCCESS at head, but its mergeability snapshot was stuck on a persistent "Head branch was modified" 409 across hours, close/reopen, and `update-branch`. Rebuilding the identical one-line edit on a fresh branch from current main is the canonical escape from a wedged mergeability snapshot per CLAUDE.md §"Merge on CONCLUDED checks" (no rebase / reset / force past it) — the *content* of #7557 is being preserved, only its carrier is replaced. (b) DO NOT redo the B1 hero itself; that wave closed at `#7511` (R5 fast-follow) and `#7469` (R0 spine/tokens/specimen), both already accepted. (c) DO NOT touch the rendered hero (`templates/_unified_dashboard_hero.html.j2`) — it is unchanged and live. |
| collision control | Only `.github/ci/legacy-jobs.yml` is touched. No other open PR owns this path at squash time. The audit-recording PRs that landed earlier in the window (`#7570 / #7566 / #7565 / #7564 / #7561 / #7560 / #7556 / #7555 / #7549 / #7548 / #7547 / #7546 / #7545 / #7544 / #7543 / #7541 / #7540 / #7538 / #7537 / #7536 / #7529 / #7538 / #7540 / #7541`) added only files under `orch/audits/` — zero collision on the CI config. |
| checks | PR body is one-paragraph; it does not enumerate a per-check table. The CI receipt carried by the merge is the same that wedged `#7557`: a SUCCESS ci.yml run at the new head `e53fbcb1` (the body asserts `#7557`'s run was SUCCESS at its unmoved head, which is identical content; the rebuild moves only the carrier, not the test result). The contract-delta gate (which is what the wiring heal unblocks) is named in the body — the unwired suite made it flag every PR whose base predates the wiring, and the heal is exactly the line that breaks that loop. |
| plain-language script | `node terminal/scripts/check_plain_language.mjs` is **terminal-only** and does not apply to macro. Macro's equivalent surface is the broader design-doctrine discipline read against the standing rules (no user-facing bytes are touched here — the diff is a CI config line listing pytest test names — so the gate cannot fire). The macro repo has no `scripts/check_plain_language.{mjs,py}`; the standing equivalent for macro is enforced through the per-PR visual/art-direction gates (`scripts/check_design_system.py --mode enforce-added`, `scripts/check_runtime_style_injection.py`, `scripts/check_ui_visual_evidence.py`), all of which inherit "no design change → no gate fires" because the changed file is `.github/ci/legacy-jobs.yml`, not a template or stylesheet. |
| gating scripts | `scripts/check_design_system.py --mode enforce-added` runs on `templates/_unified_dashboard_hero.html.j2`, `templates/unified_dashboard*`, and other hero-related paths; this PR does NOT touch any of those. The wiring addition is in `.github/ci/legacy-jobs.yml`, which the design-system gate does not read. `scripts/check_runtime_style_injection.py` is for runtime JS style injection; no JS is touched. `scripts/check_ui_visual_evidence.py` is for UI surfaces; no UI surface is touched. `scripts/check_validated_claims.py` enforces the "validated" ban in user-facing text; the PR ships zero user-facing bytes. |

## Diff content (scoped to this PR)

**`.github/ci/legacy-jobs.yml` (MODIFIED, +1 / −0 at line 2929)** — the single insertion:

```diff
@@ -2926,6 +2926,7 @@ jobs:
           tests/test_ai_breadth_split.py
           tests/test_vsb_surface.py
           tests/test_ud_b2_w1_vw_fold.py
+          tests/test_unified_dashboard_b1.py
           tests/test_ud_b2_w1_red_first_proof.py
           tests/test_missing_tape.py tests/test_price_basis_flip.py
           tests/test_risk_lead_lag.py tests/test_anticipation.py
```

The insertion is inside the existing `python -m pytest ...` shell command of the `render-guard + engine-contract tests (rot sweep 2026-07-02)` step on the `engine-render-guards` job (defined at `legacy-jobs.yml:2780`, gate: `data` per the block comment at `legacy-jobs.yml:3085`). The line sits between the two existing `test_ud_b2_w1_*` entries, which is alphabetically / thematically consistent with the `tests/test_ud_*` cluster already in the command (B2-W1 fold + B2-W1 red-first-proof both already wired). The step has an extensive block comment immediately above it (`legacy-jobs.yml:2900-2906`) that documents the rationale for routing individual tests to this step (test files that import engine modules covered by this job's `engine/*.py` glob or that re-render Jinja templates in this job's covered set can ride this step without spawning a new job). The insertion follows that documented convention.

**`tests/test_unified_dashboard_b1.py` (NOT touched by this PR)** — 981 lines, on `origin/main` since `[MO-A UD-B1-R5] Unified dashboard hero fast-follow (#7511)` and prior via `[MO-A UD-B1] Unified Macro Dashboard — spine primitive, tokens, specimen, hero skeleton + RIG disposition table (#7469)`. The module docstring (`tests/test_unified_dashboard_b1.py:1-23`) pins the binding contract against the REAL engine contracts (R-A, R-H):

- `event_calendar._event` publishes `label / label_zh`
- `alerts.alert_view` publishes `message / message_zh`
- `fear_greed.compute_fear_greed()` publishes `label_en / label_zh / dial`
- `risk_envelope.compose()` publishes `provenance.sources[].label_en/label_zh`

Fixtures are built by calling the real engine view functions — NOT by inventing keys — so the tests fail RED-first if the engine contract ever shifts in a way the template doesn't track. R-H pins two lawful fixture forms only: (a) call the real engine view function on a minimal real input, or (b) assert on the BUILT `site/macro.html` after `python3 scripts/build_site.py` at this tree carries the published keys (label_en / label_zh, confluence_en, provenance.sources, ms_history). At least one built-page assertion per driver is required. The module also pins the one-integer law (the score prints ONCE), no banned machine-text vocab ("alert(s) fired" etc), live dot uses health tokens (R-D), spine rows that lack real regime data render as designed-null (R-B), the flip clause drops the "now X/100" parenthetical so the gauge stays the only visible integer (R-C), and the spine month-ago anchor uses `ms_history` (R-G — never a same-day cap delta). These pins are exactly the contract-delta gate the wiring heal unblocks from being enforced on every PR.

No other file in the repo is touched. The diff is byte-identical to `#7557` (which is the documented super-session rationale).

## Plain-language findings

**The PR ships zero user-facing bytes.** The only changed line is a single pytest test-filename argument in a CI shell command in `.github/ci/legacy-jobs.yml`. There is no user-visible copy, no template, no token, no legend, no tooltip, no button label, no banner, no heading. The macro repo has no `scripts/check_plain_language.mjs` (that script is terminal-only). Macro's plain-language discipline is enforced through the broader design-doctrine rules — `docs/DESIGN_DOCTRINE.md` plus the `frontend-design` skill — which bind the *content* of any user-facing artifact. The PR does not add, remove, or modify a user-facing artifact, so the gate cannot fire. The `tests/test_unified_dashboard_b1.py` file that this PR wires in DOES bind user-facing artifacts (it pins the engine contracts the hero skeleton renders), but the file itself is not user-facing — it is a pytest module — and it is not modified by this PR; it is simply being made visible to the gate that already runs on every PR whose base predates the wiring.

**Carrier-rebuild justification (no plain-language impact, but checked against operator law):** the PR body is one paragraph that names the wedge (persistent "Head branch was modified" 409s on `#7557` across hours, close/reopen, and `update-branch`, with the tip provably unmoved and its CI run SUCCESS at head) and the consequence (the unwired suite makes the PR-only differential contract-delta gate flag every PR whose base predates it — measured on `#7047` run `35535665498`). Rebuilding on a fresh branch is the canonical escape from a wedged mergeability snapshot per CLAUDE.md §"Merge on CONCLUDED checks" (which forbids rebase / reset / force past a stuck mergeability snapshot). The one-line edit is byte-identical to `#7557`, the carrier is fresh, and the same SUCCESS CI receipt that wedged on the old carrier moves with the new. This is the disciplined answer, not a re-litigation — the body explicitly says "Closes #7557 as superseded."

**Plain-language verdict: PASS.** No user-facing content; no copy, design, or theme decisions; no carrier-rebuild discipline violation; no gating-script applicability (terminal-only `check_plain_language` does not run on macro; macro design-gates do not read `.github/ci/legacy-jobs.yml`). The gate is structurally inapplicable to a CI config wiring heal.

## Theme findings

**The PR ships zero theme bytes.** `scripts/check_design_system.py --mode enforce-added` is the macro TP-0 art-direction gate; it reads tokens, palettes, type, spacing, and component shells in `templates/`, `site/`, and the `mockups/design_system/` specimen, not `.github/ci/legacy-jobs.yml`. The only file touched is `.github/ci/legacy-jobs.yml`, which is not a theme surface and is not read by any theme gate. The `tests/test_unified_dashboard_b1.py` file (which this PR wires in, but does NOT modify) DOES pin theme contract — it asserts the hero skeleton carries the published `label_en / label_zh` keys for `event_calendar`, `alerts`, `fear_greed`, and `risk_envelope`, and it pins the health-token live dot (R-D), the one-integer law (the score prints ONCE), the designed-null spine row (R-B), and the flip-clause integer ban (R-C). Those pins are the test surface that ensures the rendered hero complies with TP-0 in dark + light + EN + ZH, but the pins are not a *change* to TP-0 — they are a *guard* against drift in already-shipped TP-0 contracts. Wiring the test into the gate is the act that turns TP-0 from "designated" to "enforced"; it does not alter TP-0 itself.

**Light art-direction check:** the test file does not author any stylesheet; it asserts on the rendered output of `_unified_dashboard_hero.html.j2`, which is the canonical hero whose light treatment is owned by the standing design doctrine. Per TP-0 §"Token substitution alone is never proof of a light design" (operator 2026-08-27), the hero's light and dark art directions are TWO designs, not one skin — this PR neither unifies nor bifurcates them, because the test is pinned to assert darken/light parity on the existing tokens, not to author a new token. The wiring heal does not move light vs dark on the design axis; it moves enforcement on the contract axis.

**Theme verdict: PASS.** No theme surface touched. The wiring enables TP-0 contract enforcement (the test file already existed and was already TP-0-correct) without authoring any new theme, token, palette, type scale, or component shell.

## Validated-claims findings

**The PR ships zero user-facing claims.** The changed file is a CI config line. The body is one paragraph that names the wedge (technical GitHub API 409 error text) and the consequence (a pytest-gate keeps flagging every PR whose base predates the wiring) — both technical-process claims about the mergeability snapshot and the contract-delta gate, neither user-facing. There is no "validated" / "certified" / "guaranteed" / "backtested" / "back-tested" / "live-tracked" / "live-tested" claim in the diff or body. The macro `scripts/check_validated_claims.py` gate enforces the standing "validated" ban in user-facing text per the house law — and there is no user-facing text in this PR for the gate to read. The test file that this PR wires in does NOT add a "validated" claim; it pins the *absence* of banned machine-text vocab ("alert(s) fired" etc.) in the rendered hero, which is the OPPOSITE direction — the test file is one of the very guards that prevents a "validated" slip from reaching the hero.

**Carrier-rebuild justification (no validated-claims impact):** "supersedes #7557" is a technical process claim, not a market/strategy claim, and it names the carrier being superseded (PR `#7557`, closed-as-superseded at 2026-09-21T01:46:11Z). The body asserts "#7557's mergeability snapshot is stuck (persistent 'Head branch was modified' 409s across hours, close/reopen, and update-branch, with the tip provably unmoved and its ci run SUCCESS at head)" — this is a verifiable technical process receipt (the same shape as the standing "ci.yml run concluded SUCCESS" carrier-rebuild receipt on CLAUDE.md), not a market claim. The "every PR whose base predates it" is a quantitative scope claim about the contract-delta gate's flagging surface, measured on `#7047` run `35535665498` — a named run, not an unverified marketing claim.

**Validated-claims verdict: PASS.** No user-facing text; no "validated" / "certified" / "guaranteed" / "backtested" / "live-tracked" claim introduced. The PR's own body carries only technical process claims, each tied to a named receipt (PR `#7557`, PR `#7047`, run `35535665498`).

## Overall verdict

**PASS** — half-B CI infra wiring heal, super-session of the wedged `#7557`. One line added to `.github/ci/legacy-jobs.yml` at line `2929`, appending the existing pytest module `tests/test_unified_dashboard_b1.py` (981 lines, on main since `#7511`, not modified by this PR) to the `render-guard + engine-contract tests (rot sweep 2026-07-02)` step on the `engine-render-guards` job (gate: `data`). The wiring turns the existing test from "designated but not enforced" to "designated AND enforced on every PR whose base predates the wiring" — breaking the contract-delta flagging loop the body measured on `#7047` run `35535665498`. No template, token, copy, design, theme, palette, type, component shell, engine contract, or user-facing artifact is touched. No plain-language, theme, or validated-claims gate is structurally applicable; the gate that DID apply (terminal-only `check_plain_language.mjs`) does not run on macro, and the gates that DO run on macro (`check_design_system.py --mode enforce-added`, `check_runtime_style_injection.py`, `check_ui_visual_evidence.py`, `check_validated_claims.py`) do not read `.github/ci/legacy-jobs.yml`. Carrier-rebuild discipline complies with CLAUDE.md §"Merge on CONCLUDED checks" — fresh branch from current main, byte-identical content, same SUCCESS CI receipt moves with the carrier, predecessor closed-as-superseded with `Closes #7557` in the body.

**Final classification: PASS (CI infra wiring heal, no user-facing impact, all three law checks structurally inapplicable and none fires by inheritance).**

## Cross-references

- Predecessor (closed-as-superseded): [#7557 — [MO-A] ci: wire tests/test_unified_dashboard_b1.py — clears the fleet-wide contract-delta red](https://github.com/mastermindx-market-intelligence/macro/pull/7557), closed 2026-09-21T01:46:11Z.
- Test file authored in: [#7511 — [MO-A UD-B1-R5] Unified dashboard hero fast-follow](https://github.com/mastermindx-market-intelligence/macro/pull/7511); root at [#7469 — [MO-A UD-B1] Unified Macro Dashboard — spine primitive, tokens, specimen, hero skeleton + RIG disposition table](https://github.com/mastermindx-market-intelligence/macro/pull/7469).
- Same-window adjacent audits (for context, not part of this PR): `orch/audits/macro_PR-7569.mm.md` (feat(prophet) B4 entry availability core, 2026-09-21T03:55:11Z); `orch/audits/macro_PR-7571.mm.md` (design ratchet FUNC_COLOR_RE, 2026-09-21T04:05:57Z); `orch/audits/macro_PR-7573.mm.md` (fix(prophet-live) per-name basis receipts, 2026-09-21T04:34:27Z); `orch/audits/macro_PR-7558.mm.md` (fix(ci) bind contract-delta to exact tested merge base, 2026-09-20T22:32:08Z — the binding logic this wiring heal completes).
- Standing law cited: CLAUDE.md §"Merge on CONCLUDED checks" (carrier-rebuild vs wedged mergeability snapshot); CLAUDE.md §"Theme art direction" (TP-0 light/dark are TWO art directions); CLAUDE.md §"Epistemics" (validated-claims ban on user-facing text); `scripts/check_validated_claims.py`, `scripts/check_design_system.py --mode enforce-added`, `scripts/check_runtime_style_injection.py`, `scripts/check_ui_visual_evidence.py` (none fires on this diff).