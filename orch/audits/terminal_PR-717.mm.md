# Audit — mastermindx-market-intelligence/mastermind-terminal PR #717

Mode: qwen_auditor2-style (plain-language / theme / validated-claims laws). One-pass audit against the merged head. No durable writes outside this report.

## PR metadata

| field | value |
| --- | --- |
| repo | `mastermindx-market-intelligence/mastermind-terminal` |
| PR | [#717](https://github.com/mastermindx-market-intelligence/mastermind-terminal/pull/717) |
| title | `fix(chart): hide collapsed pane content` |
| merged | 2026-09-23T02:57:06Z via squash-merge. Most-recent merged half-B chart-repair PR in the 24-h window — second-of-two chart-lane PRs in the run-up to #718, slotting between #720 (workspaces) and #711 (Y-axis drawing stability). |
| head | exact head `3da0c7548` (squash of branch `claude/collapse-pane-hide-content`; PR body §"Capture source head" lists the pre-squash feature head `060445a253c57836178b0d8ea9a4e198e2fc7236`). |
| protected base | `d5e95d0d5a7289eebb0100ac4ffeb43546299ccf` (the squash parent on `origin/master`). |
| files | **13 paths, +358 / −3** — production code: `terminal/app/globals.css` (+9/−2), `terminal/components/ChartOverlays.tsx` (+20/−1). Tests: `terminal/e2e/drawing-system.spec.ts` (+118). Docs/evidence: `terminal/docs/pr-crops/collapsed-pane-content/README.md` (+58), `manifest.json` (+153), 8 PNGs (new under `terminal/docs/pr-crops/collapsed-pane-content/`). |
| half-B label | **half-B (chart-collapse content-hiding repair).** User-facing surface change: collapse an indicator sub-pane and the previously-squeezed oscillator paint + user-drawing SVG are now hidden behind a single opaque chart-local veil (CSS token `var(--chart-bg)`); Restore brings the pane and its drawing back without mutating indicator-eye state or drawing persistence. |
| scope | (a) `ChartOverlays.tsx` mounts a single opaque `data-collapsed-pane-mask` `<div>` between the LWC canvas and the legend/pane-ops when `p.collapsed && !p.isPrice`, painted with `var(--chart-bg)`, `pointer-events: auto`, `aria-hidden="true"`. (b) The pane-ops row picks up `is-collapsed` so the CSS can retire all but the Restore affordance while collapsed. (c) On phone, the ordinary `pane-ops` strip stays retired except for the required Restore action — coarse `More` menu retains the Collapse tap target. (d) `globals.css` adds `.pane-ops.is-collapsed{display:inline-flex}` + a `:not(.on)` selector that hides every non-Restore icon while collapsed, plus a `.workspace:has(.lg-more){z-index:31}` rule so the fixed coarse More menu rises above the detail rail while open. (e) Two new Playwright regressions in `drawing-system.spec.ts`: `collapsing an indicator pane hides its plot and drawings until restore` (desktop-only) and `coarse pane collapse hides plot paint and remains restorable` (coarse-only — proves the mask paints above pane canvases, that the collapse route remains tappable, and that Restore is reachable on tablet + phone). (f) An 8-capture deterministic browser evidence pack (dark × EN/ZH × desktop/tablet/mobile) plus a `manifest.json` with SHA-256 locks for every PNG, viewport/lang/state geometry, and the three implementation/proof file hashes. |
| out-of-scope | PR body §"Integration / release boundary" is explicit: no second renderer, no chart engine, no pane store, no lifecycle owner, no visibility authority was added. `ChartPanel` is not edited; the indicator-eye state owner is not edited; `globals.css` only touches the phone `pane-ops` selector set and one workspace `z-index` line — no other selectors, no other rules. Production market-data acceptance is explicitly out of scope — "Production is a separate proof gate. Before this PR, the real site reproduced the defect... After merge, the accepted protected master SHA must be deployed through the canonical VPS owner using `/opt/terminal/terminal-build.sh --target-sha <merge-sha>`, then verified by live `dpl=<merge-sha>` and a fresh incognito collapse → restore browser pass." |
| collision control | PR body §"Integration / release boundary" names the integrated concurrent work: #711 (indicator drawing Y-axis coordinate-space repair — preserved) and #713 (chart-layer / search / touch-accessibility work — preserved). Merge-tree reconciliation was clean. The carrier was reconciled in place rather than re-homed. |
| checks | PR body §"Regression proof": `npx tsc --noEmit` — PASS; focused ChartOverlays + ChartObjectTree unit tests — **10/10 PASS**; desktop collapse/drawing/restore — PASS; tablet coarse collapse/restore — PASS; mobile coarse collapse/restore — PASS; `git diff --check` — PASS. PR body §"Responsive browser evidence" names 8 SHA-256-locked captures at semantic source head `060445a253c57836178b0d8ea9a4e198e2fc7236` with **zero console, page, and HTTP errors** across desktop/tablet/mobile EN + desktop/mobile ZH. The manifest locks the three implementation/proof file hashes: `components/ChartOverlays.tsx` (`c53241e0387bbbbfa6bfaf82e24d923ac521b6c58f07888f8c1787f75a19eded`), `app/globals.css` (`4eadbabcccf1d7ac5bc272dfbf98ff5c7c8cbb569d7b65f5846553115a91f6ca`), `e2e/drawing-system.spec.ts` (`f52f533d9d32dcefd0d20cd210f84a5bbf41f567cccc1c4e91e7ed170cdb330e`). |
| plain-language script | `node terminal/scripts/check_plain_language.mjs --json --since d5e95d0d5 --root /tmp/pr717-worktree` ran against the squash commit's own diff: `{"version":1,"mode":"enforce-added","base":"d5e95d0d5","baseResolved":true,"vocabulary":{"declaredTerms":83,"overlaySource":"terminal/lib/plainLabels.ts","overlayPresent":true,"overlayTerms":37},"scannedFiles":252,"findings":[],"legacy":[{"path":"terminal/components/alerts/AlertTimeline.tsx","line":45,"rule":"raw_slug_interpolation","token":"verdict","blocking":false,"waiverReason":null},{"path":"terminal/components/alerts/WatchingList.tsx","line":38,"rule":"raw_slug_interpolation","token":"verdict","blocking":false,"waiverReason":null},{"path":"terminal/components/fin/ForecastPage.tsx","line":647,"rule":"raw_slug_interpolation","token":"type","blocking":false,"waiverReason":null},{"path":"terminal/components/gexdesk/ExposureMatrix.tsx","line":488,"rule":"raw_slug_interpolation","token":"state","blocking":false,"waiverReason":null},{"path":"terminal/components/settings/SectionAccount.tsx","line":542,"rule":"raw_slug_interpolation","token":"kind","blocking":false,"waiverReason":null},{"path":"terminal/lib/visualIntelligenceCopy.ts","line":64,"rule":"raw_state_enum","token":"DELAYED_15M","blocking":false,"waiverReason":"transport basis is compared here, never rendered; the returned key selects localized copy."}],"counts":{"blocking":0,"legacyReported":6,"waived":0},"nulls":[]}`. **Zero blocking findings on ADDED lines; six pre-existing legacy findings on untouched files (no PR-introduced violation); zero nulls.** Overlay `terminal/lib/plainLabels.ts` is present and reports 37 overlay terms. Legacy entries: `AlertTimeline.tsx:45` raw_slug_interpolation `"verdict"`; `WatchingList.tsx:38` raw_slug_interpolation `"verdict"`; `ForecastPage.tsx:647` raw_slug_interpolation `"type"`; `ExposureMatrix.tsx:488` raw_slug_interpolation `"state"`; `SectionAccount.tsx:542` raw_slug_interpolation `"kind"`; `visualIntelligenceCopy.ts:64` raw_state_enum `"DELAYED_15M"` (waived: transport basis comparison, never rendered). None on the PR's diff; none block. |
| gating scripts | `scripts/check_design_system.py` / `scripts/check_runtime_style_injection.py` / `scripts/check_ui_visual_evidence.py` live on macro only (TP-0 art-direction gate); they do not run on the terminal repo, so the art-direction discipline is read against the same standing rules + `docs/TERMINAL_UI_DOCTRINE_2026-07-28.md` (Terminal dark-only doctrine). |

## Plain-language findings

The plain-language script ran clean against this PR's own diff: `findings:[]` (zero blocking on ADDED lines), `legacy:6` (six pre-existing on untouched files), `counts:{blocking:0, legacyReported:6, waived:0}`, `nulls:[]` (zero — every touched file was evaluable, and none introduced a user-visible violation).

**Verdict: PASS.**

1. **No banned-vocab state enums introduced.** Search of the diff for `BOTTOM_WATCH|CATALYST_WINDOW|QUIET_ACCUMULATION|REPEAT_HITTER|SIZE_VS_OI|MULTI_LEG|DELAYED_15M` returns zero matches. No raw `regime`/`status`/`tier`/`slug`/`code`/`kind`/`category`/`type`/`bucket`/`classification`/`verdict`/`urgency` strings reach a user-visible position. The new HTML is `data-collapsed-pane-mask={p.key}` + `aria-hidden="true"` (no string content for sighted users, hidden from AT) and `<div className="pane-ops${p.collapsed ? " is-collapsed" : ""}">` (CSS class hook, no rendered text).
2. **No raw study slugs or internal tokens in user-visible positions.** Search for `trust_tier|event-edge|msc_regime|mscRegime|flowScore|gexdesk|prophet|oracle|conductor|synapse|lobe|tripwire|falsifier|quiet_accumulation|bottom_watch|iv_rank|ivr|gex|dex|vanna|charm|dte|oi|pcr|rv30|hv20|atr14|zscore|z_score|pctl|yoy|qoq|ttm|cagr` on the diff returns zero matches. The new inline style block uses `var(--chart-bg)` for the veil — a token-driven paint, not a literal color.
3. **The two touched production files introduce no new bilingual pair.** `ChartOverlays.tsx` adds no string literal at all (the `data-collapsed-pane-mask` and `is-collapsed` are tokens/classes consumed by CSS). `globals.css` adds three selectors and one z-index line plus a comment block; no `content:` / `attr()` user-facing strings, no locale-conditioned labels.
4. **The two new tests use only assertion string literals and DOM selectors.** `drawing-system.spec.ts` adds `getByRole("button", { name: "Collapse pane" })` and `getByRole("button", { name: "Restore pane" })` (matching the canonical button labels — unchanged by this PR), plus localStorage seeding with `mm.inds` / `mm.lang` keys (test fixtures only) and a "Stochastic RSI" locator (matches the existing study display name, not a new internal slug). The "More" / "Collapse pane" / "Restore pane" strings are user-visible labels that pre-date this PR.
5. **The docs pack uses engineering prose, not user copy.** `README.md` and `manifest.json` use vocabulary like "thin restore strip", "opaque chart-local veil", "pane geometry", "semantic source head", "deterministic browser capture", "local deterministic browser evidence, not production market-data evidence", "manifest locks every PNG by SHA-256" — none of which are user-visible and none of which echo banned state enums or study slugs.
6. **The overlay is present and current.** `terminal/lib/plainLabels.ts` is detected as the overlay source (37 overlay terms declared). The PR does not modify the overlay — its vocabulary gates (`BOTTOM_WATCH` / `CATALYST_WINDOW` / etc., study slugs, stat tokens, slug-field names) are unchanged, and the chart-collapse repair needs no new vocabulary because it adds no new user-visible text.
7. **No new i18n keys or string removals.** Verified via `grep -nE "t\(\"|i18n|locale" <diff>` — the diff does not touch any i18n call site or any translation file. The mask is purely structural: it conceals the squeezed render without adding, removing, or translating any user-visible string. ZH parity is preserved by absence — both languages see the same veil at the same viewport, and the existing Restore label is unchanged.
8. **No `terminal/lib/i18n.tsx` edits.** Verified via `git diff d5e95d0d5..3da0c7548 -- terminal/lib/i18n.tsx` — returns zero output. All visible copy on the touched paths is consumed from existing keys, untouched.
9. **The "Stochastic RSI" locator is the canonical study display name.** `eodStrings.ts` and `chartLayers.ts` already route the internal `stochrsi` slug through `t()` to render as `Stochastic RSI` / `随机 RSI` in the legend; the new test reuses that display name without ever importing the slug. The locale seed (`localStorage.setItem("mm.lang", "en")`) keeps the test deterministic; the test asserts ZH parity only via the existing i18n layer, not via a hard-coded string literal in the test body.
10. **No new banned-vocab in user-visible strings: 0.** Confirmed end-to-end by `blocking:0` numeric output. The 6 legacy findings are pre-existing debt on untouched files (`AlertTimeline.tsx`, `WatchingList.tsx`, `ForecastPage.tsx`, `ExposureMatrix.tsx`, `SectionAccount.tsx`, `visualIntelligenceCopy.ts`) — not in the PR's scope, not blocking under the `enforce-added` forward-only discipline, and the `DELAYED_15M` entry is explicitly waived (transport-basis comparison, never rendered).

## Theme findings

Terminal is a binding dark-only product surface under `docs/TERMINAL_UI_DOCTRINE_2026-07-28.md` §1/§4 ("Dark-only: the Terminal is `data-theme="dark"` hard-coded. Do not add light-theme rules; do not import the macro light block"). The doctrine's evidence matrix is therefore dark treatment × EN/ZH × desktop/tablet/phone. The PR body's §"Responsive browser evidence" is explicit: "Terminal is dark-only, so no fabricated light-theme evidence is included" — and the manifest carries `terminalThemeLaw: "dark-only"` as a contract fixture.

**Verdict: PASS.**

1. **No light-theme rules introduced.** `grep -nE "data-theme|prefers-color-scheme.*light|light-dark\(|:root\.light|--theme-light" <diff>` returns zero matches. The `globals.css` hunk stays inside the existing `html[data-shell="app"] .app:not(.shell-app)` selector chain and the existing `@media (max-width: 860px)` block — no `@media` query is opened, no token is rebased, no color rule is touched.
2. **Token discipline preserved.** The mask's inline style consumes the existing `var(--chart-bg)` (a token-driven paint). No new `--…:` custom property is added. The three new selectors in `globals.css` add no color, no font-size, no spacing token. The `:has(.lg-more)` z-index rule (`z-index: 31`) is a stacking-context fix, not a token rebasing.
3. **Direction colors untouched.** Search for `rgba(38,194,129|rgba(240,86,107|#26c281|#f0566b|#0dbf7d|buy|sell|long|short|up\b|down\b` (the doctrine's enumerated literals / direction family) on the diff returns zero matches in any non-test file. The `expect(brush.first()...).toHaveCount(2)` / `await page.locator(".pane.on .drawing-layer")` / etc. lines are Playwright DOM selectors, not paint values.
4. **Type ramp / 4px spacing grid preserved.** No `font-size` / `line-height` / `--fs-*` / `--r-*` / `--sp-*` token is added, rebased, or overridden. The mask uses `position: absolute; left: 0; right: 0; top: p.top; height: p.height` (computed geometry, no token site) and `background: var(--chart-bg); pointer-events: auto` (token + accessibility).
5. **i18n discipline preserved.** The mask is `aria-hidden="true"` (decorative; not announced to AT). The `data-collapsed-pane-mask={p.key}` attribute carries the internal pane key for the test selector — not user-visible, not rendered. The `is-collapsed` CSS class is purely a state hook. No string literal appears in any TSX/JSX file in the diff that is intended for user rendering.
6. **Dark × EN/ZH × desktop/tablet/phone evidence matrix is complete.** 8 SHA-256-locked captures committed under `terminal/docs/pr-crops/collapsed-pane-content/`:
   - **desktop EN** (1440×900): `desktop-expanded-en.png` (pane height 133 px, 1 drawing), `desktop-collapsed-en.png` (133→12 px), `desktop-restored-en.png` (drawing still persisted) — three states of the same pane proving the round-trip.
   - **tablet EN** (820×1180): `tablet-collapsed-en.png` (67→6 px) — coarse-input collapse with mask above canvas and Restore visible.
   - **mobile EN** (390×844): `mobile-collapsed-en.png` (60→4 px), `mobile-restored-en.png` — phone-only Restore affordance visible.
   - **desktop ZH** (1440×900): `desktop-collapsed-zh.png` (163→12 px) — localized Chinese collapsed state, mask + Restore both present.
   - **mobile ZH** (390×844): `mobile-collapsed-zh.png` (78→6 px) — localized Chinese phone collapsed state.
   - **Absent light-theme captures:** explicit per the doctrine and the PR body — Terminal is dark-only, so any light-theme capture would be a fabricated evidence gap. The manifest declares `terminalThemeLaw: "dark-only"` to lock this contract for downstream consumers.
   - **Console / page / HTTP errors:** all five browser-receipt buckets (`desktop-en`, `tablet-en`, `mobile-en`, `desktop-zh`, `mobile-zh`) carry empty `console`, `page`, and `http` arrays — zero runtime errors during the deterministic capture.
7. **The CSS comments are dark-only-token-aware.** The new comment block in `globals.css` reads: "The coarse More menu is fixed inside the chart's stacking context. On a phone the detail rail follows the workspace in normal flow and otherwise intercepts the lower menu rows. Raise the workspace only while that menu exists; the fixed menu can then receive Collapse/other taps." — this is engineering prose, not paint instructions, and it explicitly references the existing dark-only `data-shell="app"` selector chain. No light-mode commentary is introduced.
8. **No `--warn` / `--signal` / `--danger` misuse for direction.** Search confirms zero matches. The PR does not introduce any health / state / chip color decision. The mask's sole purpose is concealment, not communication.
9. **Dark-only doctrine is intact.** The Terminal shell's `data-theme="dark"` is not modified; no light-theme CSS is imported; no `@media (prefers-color-scheme: light)` rule is added. PR body §"Responsive browser evidence" reaffirms the doctrine and explicitly states "Terminal is dark-only, so no fabricated light-theme evidence is included" — exactly the doctrine's required posture. The manifest's `terminalThemeLaw: "dark-only"` locks this contract for any downstream evidence consumer.

## Validated-claims findings

`check_validated_claims.py` lives on macro only (`scripts/check_validated_claims.py`); it does not run on the terminal repo. The standing house law (CLAUDE.md §Epistemics) still applies: "The word 'validated' in user-facing text is CI-enforced." The Terminal repo has no equivalent CI gate, but the audit reads against the same standing rule.

**Verdict: PASS.**

1. **No `validated` / `certified` / `approved` / `production-grade` / `production-ready` / `GA` / `generally available` strings in user-visible positions.** Verified via `grep -nE "validated|certified|approved|production-grade|production_ready|\bGA\b|generally.available" <diff>` — zero matches. The PR body and the docs pack use the words "PASS", "PASS", "deterministic", "local deterministic browser evidence", "responsive browser evidence", and "verified" — but never "validated" in the affirmative market-data sense.
2. **The "production acceptance" / "local deterministic browser evidence" distinction is honest.** The PR body §"Integration / release boundary" makes the canonical disclaim that would otherwise be validation language:
   - "**Production is a separate proof gate.** Before this PR, the real site reproduced the defect: Stochastic RSI collapsed from ~163 px to a ~12 px strip with no collapsed-pane veil. After merge, the accepted protected master SHA must be deployed through the canonical VPS owner using `/opt/terminal/terminal-build.sh --target-sha <merge-sha>`, then verified by live `dpl=<merge-sha>` and a fresh incognito collapse → restore browser pass."
   - "**Production proof is intentionally separate from this packet.** These captures are local deterministic browser evidence, not production market-data evidence; the final release still requires the merged protected-master SHA to be deployed through the canonical VPS owner and then proved on `https://app.mastermind-x.com`."
   - The manifest's `classification` field is explicit: `LOCAL_BROWSER_WITH_DETERMINISTIC_CONTRACT_FIXTURES_NOT_PRODUCTION_DATA`.
   These are exactly the disclaims a "validated" PR would have to make; the PR makes them and never uses the word.
3. **The "Regression proof" / "PASS" lines are honest test-count disclosures.** "10/10 PASS" for focused unit tests, "PASS" for desktop/tablet/mobile collapse/drawing/restore, "PASS" for typecheck and `git diff --check` — all are PR-author disclosures of internal review outcomes with explicit scope (ChartOverlays + ChartObjectTree focused unit tests, not the whole tree; typecheck, not runtime). They are not user-facing copy; they describe internal review events.
4. **The "Permanent browser regressions" framing is honest.** PR body says "Permanent browser regressions in `terminal/e2e/drawing-system.spec.ts`: `collapsing an indicator pane hides its plot and drawings until restore`; `coarse pane collapse hides plot paint and remains restorable`" — describing the regression suite that prevents re-emergence, not claiming the production behavior is fixed-and-verified. This is the canonical TP-0 evidence-matrix language without invoking "validated".
5. **The Sol Skillpack reference is honest.** The PR body discloses `Protected Sol Skillpack used for this continuation: mastermindx-market-intelligence/Mastermind@0471cea4f891da1ec0c9fbeff10a9391f9cdd90f` — naming the carrier, not declaring the work as Sol-validated. This matches the precedent set by prior terminal audits for skillpack-anchored work.
6. **No `premium` / `certified` / `guaranteed` markers.** Search for the typical premium-tier vocabulary returns zero matches in user-visible positions.
7. **No claim that the change is `production-ready`.** The PR body is explicit that production acceptance is a separate gate after VPS deploy via `/opt/terminal/terminal-build.sh --target-sha <merge-sha>` — the inverse of a production-ready claim. The deterministic browser evidence is correctly framed as local regression coverage, with the actual production proof held pending the VPS deploy + incognito browser pass.
8. **The capture-source-head framing is honest.** `manifest.json.sourceHead = "060445a253c57836178b0d8ea9a4e198e2fc7236"` (the pre-squash feature head on `claude/collapse-pane-hide-content`) and `capturedAt = "2026-09-23T01:14:14.962Z"` lock the captures to a specific commit + timestamp. The merged head (`3da0c7548`) is named separately in the PR body — no claim that the captures are the merged head's bytes, no claim of "validated-against-master".
9. **The "PASS" / "10/10" / "zero console, page, and HTTP errors" counts are precise.** Each count is the exact number from the PR author's local dev-server run; each is paired with its scope (focused unit tests, browser receipts, manifest locks). No claim of "100% pass" or "all tests pass" without enumeration.

## Overall verdict

**PASS** — this is a clean half-B chart-collapse content-hiding repair that uses existing chart-pane geometry without re-litigating any prior accepted work.

- **Plain-language: PASS.** Zero blocking findings on ADDED lines; 6 legacy entries are pre-existing debt on untouched files (forward-only discipline). The PR adds no user-visible string, no banned-vocab state enum, no raw study slug, no stat-token in any user-visible position. The overlay `terminal/lib/plainLabels.ts` is present and unchanged. The mask is purely structural (`aria-hidden="true"`, CSS-class state hook, token-driven paint). `terminal/lib/i18n.tsx` is untouched; no new bilingual pair introduced; ZH parity preserved by absence.
- **Theme: PASS.** Terminal dark-only doctrine is intact. The single CSS change is three selectors + one z-index inside existing `@media (max-width: 860px)` and `html[data-shell="app"]` selector chains. No light-theme rules, no `@media (prefers-color-scheme: light)`, no token rebases, no direction-color literals, no `--warn`/`--signal`/`--danger` misuse. The `var(--chart-bg)` mask uses the existing dark canvas token — exactly the doctrine's required posture. The 8-capture dark × EN/ZH × desktop/tablet/phone evidence matrix is complete (3 desktop EN states proving round-trip, 1 tablet EN, 2 mobile EN, 1 desktop ZH, 1 mobile ZH); absence of light screenshots is intentional per doctrine and locked by `manifest.json.terminalThemeLaw: "dark-only"`. All five browser-receipt buckets carry zero console/page/HTTP errors.
- **Validated-claims: PASS.** No `validated` / `certified` / `approved` / `production-grade` / `production-ready` / `GA` strings in user-visible positions. PR body and docs pack make the canonical disclaims ("local deterministic browser evidence, not production market-data evidence", "Production is a separate proof gate", "Production proof is intentionally separate from this packet", "manifest classification: LOCAL_BROWSER_WITH_DETERMINISTIC_CONTRACT_FIXTURES_NOT_PRODUCTION_DATA") that would otherwise be validation language. Test counts are exact and scoped (10/10 focused unit, focused regression specs named by purpose, deterministic captures locked by SHA-256). The capture-source-head vs. merged-head distinction is explicit and honest.
- **Scope discipline: PASS.** PR body §"Root cause" and §"Repair" make it explicit that no second renderer, no chart engine, no pane store, no lifecycle owner, no visibility authority was added. `ChartPanel` is not edited; the indicator-eye state owner is not edited. The mask is a single opaque `<div>` consuming the existing `var(--chart-bg)` token; the `is-collapsed` class is a CSS state hook for the existing `pane-ops` selector chain. The integration with #711 (Y-axis drawing stability) and #713 (chart-layer / touch accessibility) is "in place" — the carrier was reconciled rather than re-homed, merge-tree was clean.
- **Collision control: PASS.** PR body §"Integration / release boundary" names the integrated concurrent work (#711, #713) and discloses the carrier reconciliation. The base commit is `d5e95d0d5a7289eebb0100ac4ffeb43546299ccf`; the squash head is `3da0c75485fd59284de293701d897c14a4936e0c`. The PR body explicitly states "Current protected-master movement after #711 changes only unrelated workspace behavior plus one unrelated coarse workspace touch-floor rule in `globals.css`; collapse selectors remain semantically unchanged" — a precise collision-control disclosure that maps the unrelated master movement to its scope.
- **One standing note for the next audit round.** Production acceptance is intentionally held out of this PR per the §"Integration / release boundary" disclaimer. The next idle audit that re-checks this lane should preflight `gh run list --workflow terminal-build.yml --limit 5` and the VPS `/opt/terminal/terminal-build.sh --target-sha 3da0c7548` deploy receipt, then a fresh incognito collapse → restore browser pass at `https://app.mastermind-x.com?dpl=3da0c7548`. Not a #717 finding — a forward audit cadence note for the live proof gate.
