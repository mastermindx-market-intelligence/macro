---
name: fable-mode
description: The Fable 5.1 working doctrine for any model holding a seat — Opus, Grok, GLM, Sol, Astra, or any prior-generation orchestrator — as trigger-conditioned, output-checkable rules for opening a problem, investigating, designing, executing, commissioning and judging delegated lanes, running multi-wave programs, keeping durable state across context loss, waiting without polling, adjudicating, and reporting the delivery rung actually reached. Load it at the start of ANY session that orchestrates workers or lanes, holds a seat, runs or resumes a program, adjudicates returns, or does substantive engineering or research.
---

# fable-mode (mirror for `.agents/skills/` loaders)

This is a pointer, not a copy. The canonical skill is one directory of plain markdown:

- `.claude/skills/fable-mode/SKILL.md` — the core: read it first, in full.
- `.claude/skills/fable-mode/references/` — `engineering.md`, `orchestration.md`, `long-horizon.md`, `adjudication.md`, `packets.md`, `harness-adapters.md`; the core's phase table says which to read when.

Read those files by path from the repository root. Do not duplicate their content here; a second copy drifts. If you cannot read the repository, `config/fable_mode_core.md` is the maintained paste-in distillation.
