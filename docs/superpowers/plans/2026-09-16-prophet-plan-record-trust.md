# Prophet plan-record trust implementation plan

## Goal and approved product direction
Chairman approved the September 16 assessment and directed Sol to complete the consolidation. Candidates remain the discovery population; existing plans become tracking/history records in the same workspace. Do not delete or blend populations, rewrite historical episodes, or invent a ranker or availability engine.

## Authority and source
- Operation: prophet-plan-trust-20260916-sol-001.
- Owner: Sol, present Chairman-authorized session; no delegated worker or Executive dispatch claimed.
- Carrier: native Git worktree on Studio, branch claude/prophet-plan-trust-20260916-sol-001.
- Worktree: /Users/chriswong/Documents/Cluade/macro-main/.claude/worktrees/prophet-plan-trust-20260916-sol-001.
- Base: 4f37209719d2ca5caf9fd3a37aca29fa363f351c.
- Protected procedure: Mastermind 5ee11ab1e993616f3568cfca4069cb21fa61fd8f, Skillpack 1.0.1/bootstrap 1.
- Existing owners: WS:PROPHET-US-V4-RECOVERY for product projection; WS:PROPHET-US-ENTRY-TIMING for Prophet engines; WS:PROPHET-US-AVAILABILITY for current inputs/publication.
- Direct-work reason: PRINCIPAL_JUDGMENT. This repair defines the boundary between historical records and current action without assuming unfinished B3/B4 authority. Reassess routine follow-on implementation for an eligible admitted worker.

## Spec: bounded first capability
A user opening a tracked plan must never read its old entry signal as a present BUY, WAIT, HOLD, or new trigger. It must remain possible to recognize the stock, inspect the model lifecycle, and see original setup geography and creation date. A model lifecycle is not brokerage-position ownership.

Implement an opt-in record_only mode in the EXISTING shared card macro, used only by the US plan partial. Reuse the existing neutral ink declaration without changing shared CSS bytes. Record mode has its own data-record-only marker and Tracking only label; it must not use the semantic data-noread marker. Missing-read behavior on ordinary callers remains separate and unchanged.

Record mode suppresses action-colored chips, featured/triage glow, new-entry trigger/live chip mounts, and score-as-current-priority. Preserve the original score in an escaped tooltip as original priority, not a current rank. The footer says Original zone (or Original zone unavailable) and labels the creation date. Do not replace missing geography with a future-confirmation promise.

The US plan partial gets company name and sector from its own typed available board_read fields before a current-shortlist fallback. It never expands cand_map, changes the candidate population, or reads another plan's identity. Escape dynamic text because the builder environment has autoescape disabled. The stock link and plan id remain unchanged.

Keep current source, screen, and creation clocks distinct. Last screen, when shown, comes only from the attached board_read.as_of, not the render clock. It is not a claim that the price or management evaluation is current. Missing values remain explicit/absent rather than becoming zero, WAIT or a fresh date. Preserve raw plans, zones, scores, episode ids, corrections and forward ledger bytes.

## Visual contract
Reference: the shipped shared Prophet card. DARK TREATMENT: existing calm neutral ink on dark panel; no bullish glow for a historical record. LIGHT TREATMENT: existing cool canvas/white material, neutral ink and hairline; no bullish filled chip. Shared type scale, grid geometry, focus behavior and wrapping remain. Record purpose is plain text, not color alone. No new stylesheet or palette. Evidence: dark/light x English/Chinese x 1440/390, with live-model, invalidated, resolved, missing-read and missing-identity records. Non-US and candidate calls without record_only must remain byte-identical.

## Scope
Modify templates/_prophet_card.html.j2 and templates/_us_prophet_plan_cards.html.j2. Extend tests/test_p_mp1_shell_stance_projection.py (existing code-gated owner) for actual shared macro + actual plan partial. Use existing parity/repair/contract suites. Documentation and narrowly bound evidence accompany the same carrier.

Leave scripts/build_site.py, templates/dashboard.html.j2, entry engines, collectors, publication workflows, ranking, entitlements and historical data untouched in this first source slice. A platform-blocked composite inspection involving builder/spark-wiring/live-process metadata is not retried via another tool. Spark enrichment and whole-workspace navigation remain later parent dependencies, not silently dropped requirements.

## Implementation order
1. Reconcile native worktree creation and prove clean base. Result: creation process 23064 exit 0, branch/base match, tracked status clean.
2. Run the existing stance/shared-card baseline before editing. Record actual result.
3. Add RED regressions through the real Jinja partial and macro. At minimum: historical buy_now plus newer blocked; invalidated and resolved original buys; no-read plus original wait; new/featured/trigger inputs on a record; original-zone label; labelled creation date; attached name/sector outside candidate list; malicious text escaping; missing data; non-record parity.
4. Run only the new tests and record intended assertion failures, not collection errors.
5. Implement the opt-in neutral historical projection and wire the US partial. Do not change us_stance_projection's legacy consumer contract or create a current-entry function.
6. Re-run new tests and existing stance/shared-card/live-change/non-US parity/repair suites. Attribute baseline failures and missing sparse artifacts honestly. No global data guard bypass or weakening existing assertions.
7. Reproduce motivating records through exact source; verify all supplied stale action values cannot create an action-colored record. Check raw input immutability. Run negative mutations for record-mode opt-out, original-zone relabel leak, and current-candidate identity dependence.
8. Capture actual isolated browser evidence against the real template and styles. Synthetic fixtures are not production proof. Preserve no external network/provider calls during fixture browser runs.
9. Run relevant source/AgentOS contracts, inspect exact diff, obtain independent exact-head review through an eligible approved route, then publish this same branch as a draft pending required checks/review. Do not manufacture a worker acknowledgment or review.
10. Release only on actual concluded binding checks, appropriate review and explicit Sol release. Use the ordinary publication owner; no duplicate nightly, cancellation, admin bypass or generation fabrication. Production acceptance requires a real browser and a subsequent ordinary update.

## Parent continuation and non-goals
This is the FIRST trust capability, not completion of consolidation. Next is Candidate-led navigation with Tracked setups and History, preserving separate counts/identities, and actual joins to associated episodes. Then integrate existing #7180/#7187/#7200 source/publication repairs and prove the served page and paid payload agree. Those incumbent branches stay with their writers. DNR:KILL-PROPHET-POP-MERGE remains effective: presentation integration is not data-population merger.

No trade recommendation, alpha improvement, win-rate claim, pricing refresh, provider session, background job, or live acceptance is established by source tests. Stop on a real source collision, platform denial, unresolved effect, or missing required authority; keep every held requirement explicit. Context rotation is continuation, not completion.

## Continuation checkpoint
Last verified effect: native worktree creation completed at the exact base with clean tracked status. No source edits, tests passed, worker dispatch or production changes are claimed by this initial plan. Preserve completed forensic diagnosis; do not repeat the 400-row census or broad open-PR archaeology absent a material invalidator. Record each material result in this same plan/AgentOS handoff and exact source evidence.


## Material checkpoint: browser count defect discovered and bounded repair
The first record projection reached 144 passing owning/adjacent tests, with one unchanged baseline China-template hash failure and no skipped tests after site materialization. The source includes no new trading authority. Original process 52611 lost its transport handle: same-carrier reconciliation found the process absent, error-copy alignment present, site assets present and no own index lock. Its original exit code is not invented; completed effects were not repeated.

The actual Chromium 151.0.7922.34 parser then reproduced a second record-trust defect: 400 real records plus ten newer-plan links became 420 cards / 400 unique ids. With ONLY those nested newer fields null, the same rows became exactly 400 cards. Receipt: docs/pr-crops/prophet-plan-record-trust-20260916/nested-link-before.json. This invalidates any explanation that assigns all pagination discrepancies solely to publication drift. It does not establish the exact cause of the older screenshot's 422 count.

Preserve both destinations by making opt-in record roots semantic articles with an accessible full-card stock link and a separate higher-layer newer-record link. Ordinary candidate/non-US roots remain their exact anchor markup. This is not a new navigation controller: native anchors own navigation and existing ids/count/filter consumers remain. Add a RED structural HTML test to the existing suite, then repeat the real-browser id/count probe and click-target checks. No hiding duplicates, dropping newer links, changing plan data or recomputing totals to conceal malformed DOM.

The error-copy owning suite tests/test_p_mp1_shell_ladder_error_state.py is also in scope: the approved no-false-freshness copy supersedes its old unconditional-current string pin; all error/loading/Retry semantics remain tested. Broader source publication, independent review, current-head CI and production proof are still outstanding.

# Latest checkpoint — publication gate recovered

Current procedure: Mastermind@4537f066775c73d305f82acf0643701f01f5e53c. Original source custody and operation are unchanged. The inherited China checksum was reproduced RED and traced to the already-merged #7054 template; only the strict expectation was updated, with an exact accepted-commit byte comparison. The expanded matrix is now 193/193 GREEN; eight prior browser cases and four mutations remain bound to unchanged product-template hashes. No waiver or skipped assertion was used.

Current-main 27ec910d04fc81692cf9640347ac31fd3de40170 has no movement on owned source/tests. Its only selected dependency change is the unrelated macro risk-dialog rank-colour repair in dashboard.html.j2. Source publication, independent review, hosted checks and production remain distinct pending gates. The GitHub plugin exposes reviewer requests but no confirmed independent receiver is assigned. Plugin discovery returned no Mastermind entry; the inspected canonical native CEO client is queue-only, not proof of worker execution. No raw provider or alternate admission path was used.

The native source and fixtures are unchanged by this checkpoint except for the justified checksum expectation. Exact prepublication proof is docs/pr-crops/prophet-plan-record-trust-20260916/release-verification.json. Prior history below remains evidence at its stated time, not the current release verdict.


# R2 checkpoint — newer-record navigation repaired on the existing lifecycle owner

Protected procedure is now `Mastermind@eec5324c5205e8bad206512e0a936898e50b2408` (Skillpack 1.0.1/bootstrap1). The protected movement from the prior pin is workbench-only and the required Skillpack bytes are unchanged. Same operation, Studio worktree, branch and PR #7237 remain authoritative.

Root cause: the newer-record anchor used a correct native fragment, but an active `?life=resolved` filter kept the target episode hidden. The prior component proof intercepted the click to inspect its href and therefore did not exercise the page's native filter + fragment journey. A real Chromium reproduction established the defect before source repair.

TDD repair: two code-gated Node executions failed on the published parent because the stable panel had no delegated newer-record handler (process 95825, two intended failures). The existing page lifecycle script now delegates from `#us-standouts`, resolves only an already-loaded same-grid target, selects its published `data-life` through the existing `setLife`, preserves unrelated query parameters and leaves native fragment navigation unprevented. Modified clicks, missing/withheld targets and malformed destinations remain untouched. Process 96821 then passed all six lifecycle-owner tests.

Fresh verification: the complete nine-suite matrix passed 199/199 in process 8553; `git diff --check` was clean. The real-template browser verifier passed eight visual cases plus two lifecycle journeys: (1) a target hidden beyond the first show-more page becomes visible and receives the native fragment, and (2) keyboard activation reaches a target inserted after the entitled grid replacement. Both preserve `foo=keep`; no external request or production effect occurs. Browser receipt SHA-256 is `9f235d19d3ae601e45a0b957f9763bc93b034b496ec9f854cc2e79281d87d26d`.

This is still `BUILT_NOT_PROVEN`. Semantic head `ca1f0a0f30782276a9f6260b6e31501f21d1d59c` was published by normal fast-forward to Draft/HOLD PR #7237 and read back from both remote branch and PR. Contract delta against current main `12b655150582d9be39bfd2779b33338d154c565f` completed with `0 introduced / 0 inherited` (process 19182, exit 0). Obtain independent exact-head review and concluded binding checks, then release only through expected-head merge and ordinary production proof. Do not widen this PR into the full Candidate/Tracked/History composition while this trust slice is under review.

# R3 checkpoint — default pager aligned to the live tracked population

The downloadable R2 handoff was stale when this continuation reconciled the same native carrier: PR #7237 and its remote branch had already advanced normally from `71c66811a60a388f7323a606380d868f9b2b91a3` through semantic navigation head `ca1f0a0f30782276a9f6260b6e31501f21d1d59c` to records head `5638b23ed1aa8ae3c8ce2ad51d5acd0be022cf69`. No live process still owned the worktree. The uncommitted follow-on edits were preserved and reviewed rather than reset or duplicated.

A separate acceptance defect remained after R2: the default plan grid hid `resolved` history through lifecycle CSS, but the shared show-more pager still counted and paged every DOM child. On the real 400-record input this produced the false label `Showing 15 of 400` even though 134 resolved records were outside the default live board. On the interleaved 22-record regression fixture, the first page exposed only 12 unresolved records instead of 15 because four resolved rows consumed pager slots.

TDD evidence: process 98291 reproduced the intended RED (`default visible unresolved=12`; one failure, one companion assertion passed). The narrow repair adds `data-showmore-exclude-life="resolved"` to both the static and entitled-hydration plan-grid paths. The existing shared pager removes only that declared lifecycle from its private paging population, leaves every record in the DOM, and marks excluded records `sm-hidden` so the existing lifecycle-filter CSS can reveal them. Grids without the opt-in attribute retain the prior child-unit paging path. Process 4280 passed the two focused regressions; process 5749 passed the complete nine-suite matrix, 218/218.

Actual Chromium 151 proof against the real template/input now preserves all 400 unique record IDs and reports exactly 266 default/live records plus 134 resolved-history records. Initial state is `Showing 15 of 266`; Show all reveals all 266 unresolved records and no resolved record; the Resolved filter reveals all 134 history records. The prior two newer-record navigation journeys and all eight dark/light × EN/ZH × desktop/mobile visual cases remain green. The capture seam was also made deterministic after repeated runs exposed hover/focus/glyph-raster timing churn: process 38971 executed the full verifier twice and produced byte-identical PNG and receipt hashes on both runs.

Protected procedure is now `Mastermind@eee809949ee79c0ea1ef922ce74c110b1bb1dddf` (Skillpack 1.0.1/bootstrap1). Current protected Macro main at reconciliation is `5269a38f383d466f03cf1d7599d12de70118acad`; its only movement in an owned file is #7228's risk-dialog rank-momentum styling at distant dashboard hunks, not either plan-grid seam. That bounded comparison is evidence of non-overlap, not current-head CI or release acceptance.

Capability remains `BUILT_NOT_PROVEN`. The pager repair does not delete history, change ranking/data/entitlements, fetch withheld rows, create a second current board, or claim authenticated production proof. PR #7237 remains Draft/HOLD. Independent review is still `NOT_ASSIGNED`; exact-descendant hosted CI/security, conflict-free current-main composition, expected-head release, authenticated production browser proof, and the subsequent ordinary scheduled update remain mandatory gates. No merge or automatic release is authorized by this checkpoint.
