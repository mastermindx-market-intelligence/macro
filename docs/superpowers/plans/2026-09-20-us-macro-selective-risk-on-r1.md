# US Macro Selective Risk-On R1 Implementation Plan

> For agentic workers: REQUIRED SUB-SKILL: use superpowers:subagent-driven-development or superpowers:executing-plans and execute task-by-task.

**Goal:** Make the US Macro Market State distinguish broad versus selective risk-on without changing the underlying score or verdict.

**Architecture:** Add one pure engine-owned display-copy projection from the already-resolved breadth component. Thread it through the settled snapshot and live publisher, then make the existing dashboard and live patcher prefer the projected copy while retaining old verdict maps as fallback.

**Tech Stack:** Python, Jinja2, vanilla JS, pytest, Node test harness.

**Spec:** docs/superpowers/specs/2026-09-20-us-macro-selective-risk-on-design.md

## Global Constraints
- No score, weight, band, Risk Radar, Prophet or sector-ranking authority changes.
- Reuse the existing breadth component only.
- Missing or malformed breadth fails to unverified.
- Preserve EN/ZH parity and static/live session-floor behavior.
- Preserve template/site JS byte parity.
- Do not modify held PR #6685 or #7060 carriers.

## Review Focus
- RISK_ON with breadth=0 must not say breadth agrees or imply broad buying.
- RISK_ON with missing breadth must not silently classify broad.
- Debounced live MIXED/RISK_OFF must not inherit RISK_ON selective copy.
- Behind-the-render feeds must still make zero DOM changes.
- Existing consumers inspecting raw verdict/label must still receive unchanged canonical fields.
---

### Task 1: Engine-owned participation copy

**Files:**
- Modify engine/market_state.py
- Create tests/test_market_state_participation_copy.py

**Interfaces:**
- Produces market_state_display_copy(verdict, components) -> dict
- Snapshot adds participation and display_copy
- Does not alter verdict, score, raw_score, label_en or label_zh

- [ ] Write failing tests for narrow, uneven, broad, unverified, and MIXED/RISK_OFF preservation.
- [ ] Run the new test and verify RED because the helper or fields do not exist.
- [ ] Implement the smallest pure helper and wire it into market_state_snapshot.
- [ ] Run the new test and relevant Market State tests GREEN.
- [ ] Commit.
### Task 2: Static and live copy parity

**Files:**
- Modify scripts/build_risk_state.py
- Modify templates/dashboard.html.j2
- Modify templates/risk_state_live.js
- Modify paired site/risk_state_live.js
- Modify tests/test_risk_state_live_session_floor.py
- Modify tests/test_risk_state_live_copy_sync.py

**Interfaces:**
- Consumes market_state_display_copy
- Live display adds label/headline/subline/action plus participation fields.
- Dashboard and live JS prefer projected copy, then fall back to legacy verdict maps.

- [ ] Write failing tests proving payload copy overrides generic Risk-on wording and debounce uses displayed verdict copy.
- [ ] Run targeted tests and verify RED.
- [ ] Wire publisher, template and patcher; sync paired JS.
- [ ] Run live-floor, copy, coherence and glance tests GREEN.
- [ ] Commit.
### Task 3: Rendered-path proof

**Files:**
- Generated by canonical builder: site/macro.html
- Evidence only through existing tests and commands; no new control plane.

- [ ] Run the canonical targeted render path for macro.html using current committed data.
- [ ] Assert the rendered page keeps score 61/RISK_ON but contains Selective risk-on and no Risk-on sentence claiming breadth lines up.
- [ ] Run design/template sync and Market State coherence guards.
- [ ] Run the focused regression pack from the clean baseline plus new tests.
- [ ] Commit governed generated output only if repository convention and build require it.
- [ ] Push branch, open PR, own CI through conclusion, merge only on concluded green, then verify the live page.
