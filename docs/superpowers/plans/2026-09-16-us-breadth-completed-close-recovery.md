# US breadth completed-close response repair

Mission: make the existing three US breadth collectors reject/repair missing completed-session prices instead of treating a dated, volume-only row as a successful current refresh. User authorization is the current Chairman request to fully recover US Prophet. This is a path-disjoint upstream dependency of existing WS:PROPHET-US-AVAILABILITY; #7187 remains with its incumbent source writer.

Source pin: macro 55c82c1249f3927f81d7df40b1fbd96eea7ba4c5. Procedure: Mastermind 0fe8074ff953b2ced9025ed40f0f66019c759967. Direct execution reason: PRINCIPAL_JUDGMENT — the new failure crosses response validity, existing retry behavior and publication integrity; no independent worker is admitted on this source.

Evidence: collector job 104621037120 in run 35041133038 reported breadth/smallcap/midcap success through Sep-14; collection commit 6a930b613598 contains Sep-15 close/high rows with zero values while volume has 502/604/400. Engine job 104658157677 consequently rebuilds Sep-14 alpha/board. A three-symbol same-provider probe later returned real Sep-15 adjusted closes. Original provider cause remains unproven; successful-response validation is the confirmed missing software boundary.

Design: use the existing NYSE expected_last_session once per fetch, only for breadth/smallcap_breadth/midcap_breadth. Validate actual finite positive closes for that date, not index.max or volume. Reuse the existing 80% coverage policy and configured per-batch retry/backoff budget. Incomplete responses retry inside that loop; do not append their fields before validation. Retain best available batch within the same call if retries exhaust, then require 80% of requested universe current before any cache writes. Validate again after seam repair. Never invent values, mix adjusted with raw quotes, substitute a provider or restamp dates. Other calendars and the Russell partial-cache path are unchanged.

Implementation order: prove observed malformed-response shape red; implement pure coverage check and wire the existing fetch/retry boundary; prove failure preserves all old cache bytes and valid retry binds matching extras; register the new hermetic suite under gate:code in the existing CI manifest; run seam and Russell regression suites plus current-source live read into an isolated directory. Then independent review, exact-head checks, merge, and real producer/publication/browser proof. No production collector rerun over the active natural nightly; no trade/ranking/eligibility changes.
