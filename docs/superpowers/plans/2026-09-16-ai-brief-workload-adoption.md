# VPS/site fabric — AI Brief consumer adoption W1

Status: source candidate; BUILT_NOT_PROVEN; no production activation.
Parent: WS:EXECUTIVE-CAPACITY-FABRIC.
Consumer operation: vps-site-aibrief-adoption-20260916-sol-001.
Source branch: claude/vps-aibrief-policy-adoption-stack-20260916-sol.
Dependency: Macro #7179 at 889174901bdaabba44a8f60f3179ff7bd32c8061.
Protected procedure: Mastermind 7642aea155d2817219135b24246b55c1d7611c66;
Skillpack 1.0.1 / bootstrap1. Current Chairman continuation supplies intent.

## Outcome and boundary

The existing AI Brief producer must honor the shared workload decision through
client construction, fallback, reply-cache reuse, refresh intervals, synthesis,
translation and publication. An opted-in denial must produce a truthful degraded
artifact, not spend developer subscriptions, revive a prior reply, silently move
a pinned custom endpoint, or replay an ambiguous request. The real consumer is
engine/master_brain.py -> existing data/site JSONs -> existing Brief template.

This does not implement global capacity reservation, authenticated fleet policy
publication, native service admission, credential enrollment, quota telemetry or
new provider entitlement. No new router, cache store, scheduler or lifecycle.
The shared policy remains owned by Provider Control; Executive/Capacity own
admission/reservation. Fable retains incumbent principal integration and review.

## Implemented source journey

1. Validate the original trusted config through the existing decide_workload
   before legacy translation inserts defaults. Empty/invalid/native profiles and
   custom endpoints return typed refusals before credentials or custom clients.
2. Build eligible providers through existing llm_auth. Policy refusals propagate
   out of its existing waterfall; they are not provider failures or retry grants.
   Recheck before each actual invocation. A profiled SDK TypeError is ambiguous,
   never permission to resend without seed or switch provider.
3. Reuse the existing reply-cache directory. Profiled hashes bind the policy
   receipt and configured model selectors; legacy hashes/text remain unchanged.
   A closed profiled envelope retains actual served-model and provider identity.
   Wrong receipts/origins are misses, never authority. No account-generation claim.
4. Reject observed policy changes during a call/rewrite; do not publish or cache
   the obsolete return. A request already sent cannot be undone by this check.
5. Existing refresh intervals retain a prior artifact only when current policy
   and model identity still match. Recheck after translation before publication.
   A refusal clears model prose and translations while preserving input dates.
6. Existing producer writes the same payload to data/regime and site. The existing
   template shows a bounded English/Chinese notice, not internal refusal codes.

Default/unprofiled operation is deliberately unchanged. This source does not
activate a profile on the VPS and does not qualify arbitrary custom endpoints.
Content fingerprints are not signed freshness, entitlement, atomic revocation,
fleet synchronization or quota reservation. Marketing descriptor caches and the
Mastermind native waterfall remain separately owned adoption debt.

## Test and review evidence

- Consumer RED: 15 failed after correcting the test fixture and opting exact
  tmp-root cache tests into the existing hermetic-cache exception set.
- Cache origin/receipt RED: 2 failed; publisher/interval RED: 5 failed.
- Within-waterfall retirement, SDK replay and typed-refusal propagation RED:
  3 failed. These prove actual forbidden extra invocations, not string coverage.
- Composed provider + Brief suites: 419 passed in 16.29 seconds under
  MM_DATA_GUARD=1 and an outside-repository operation-local pytest temp root.
- The existing code-gated unrun-brain-desks job already executes the augmented
  test_master_brain_ladder.py. No new workflow or test-execution owner.
- Real source consumer test: run -> fixture data/site payload equality -> actual
  aibrief.html.j2 renders the returned content. All provider boundaries synthetic.
- Browser proof is NOT claimed. A bounded loopback-only browser-capture request
  was blocked by the tool safety check before execution. The Chromium-path probe
  is only installation evidence, not a successful browser or provider canary.
- Current-base contract-delta, source publication and independent review require
  their own completed receipts; local tests do not substitute for those gates.

## Custody, release, continuation

Keep the new PR DRAFT/HOLD-FOR-SOL, no merge-on-green, no native auto-merge. It is
stacked on #7179; do not merge it into the parent's source branch. Parent acceptance
and normal current-main composition/retargeting must precede release. Sol retains
this consumer writer. Direct implementation was LOWER_TOTAL_OVERHEAD on a frozen
consumer boundary; no native worker dispatch or lifecycle state was fabricated.


## Composed-source validation receipt

Full differential contract-delta against Macro main
52380b870218b61865fb56458500cac00ce4932b completed with exit 0:
zero introduced / zero inherited findings (268.44 seconds). The script's own
reported base matches the captured revision. No gate or scope was weakened.
Compile checks and git diff --check pass. Agent OS: 1118 records, zero errors,
97 warnings. The composed provider/Brief regression remains 419 passing tests.
No runtime, credential, provider, deployment or production proof is implied.


## Same-writer composition repair

The first W1 head 9069f63fcef117c34ca80f39223a238912213ceb composed cleanly with
main 52380b870218b61865fb56458500cac00ce4932b (tree caa4300a6c81f1b6cd6bb320da99425c1474003a).
The #7024 composition probe exposed two W1 source-position conflicts: adjacent
stdlib imports and both consumers inserting immediately after translation.
The writer moved only its own insertion points and preserves the regime writer.
The finalization recheck now also has an explicit persist=False regression.
Fresh complete provider/Brief regression: 420 passed in 15.81 seconds.

The CI-manifest collision between #7024 and the W0 dependency also exists when
composing #7024 with parent 889174901 alone. It is not created by W1 and remains
with the incumbent integration owners; this branch does not edit that manifest.
Current-head composition and full dependency recheck remain separate receipts.


## R2 source checkpoint — first-call repair; timeout contract still RED

Current procedure: protected Mastermind 0fe8074ff953b2ced9025ed40f0f66019c759967,
Skillpack 1.0.1. Same W1 operation, writer, branch and PR #7185. CI infrastructure
is explicitly owned by another session and was not repaired or reconfigured here.

The same native file tool now accepted the previously blocked strict-signature
regression after clean-source readback at d931d179. The corrected regression was
RED (1 failed, 38 deselected), then the minimal producer repair made all 39 ladder
tests PASS: omit the unsupported seed keyword up front for profiled calls only.
Unprofiled compatibility and ambiguous-TypeError no-replay handling are unchanged.
An initial test signature wrongly required temperature; it was corrected BEFORE
product code changed, and the strict regression was rerun RED.

Actual SDK 0.125.0 and 1.6.0 each accepted the AST-extracted source callback with
one in-memory mock-transport request and returned sdk-contract-ok. Adding seed
back was rejected before any transport request. A TypeError injected AFTER an
SDK mock response produced WORKLOAD_PROVIDER_EFFECT_UNKNOWN with no repeat call.
No real HTTP, model, account or credential was used. Both temporary environments
were removed. The first 1.x proof harness used the wrong httpx import; it was
corrected to the installed SDK's httpx2 family, not by aliasing global modules.

A second SDK mismatch is now test-defended but UNFIXED: _client_tuning_kwargs
constructs httpx.Timeout while SDK 1.x requires its own transport type. The actual
1.6.0 constructor rejects that object with TypeError BEFORE the mock transport.
SDK 0.125.0 accepts it. This is a compatibility witness, not VPS version evidence.

The timeout regression in tests/test_master_brain_ladder.py is deliberately NOT skipped or
weakened. Its RED is exact: installed-SDK Timeout expected, unrelated httpx.Timeout
observed. The attempted engine/llm_auth.py repair was blocked before execution;
readback shows zero diff in that module. Do not route the denied edit through a
worker, alternate tool, another branch or a credential change.

The complete unchanged 15-file regression command from the W1 handoff now gives
**421 passed, 1 failed, exit 1 (12.85 seconds)**. The only failure is
 test_client_tuning_uses_the_installed_sdk_timeout_type.
This supersedes 420-PASS as the current candidate verdict. The working candidate
is PARTIAL / NOT RELEASEABLE, even though the first-call seed repair is proven.

Exact next repair, once the native edit is permitted: consume the installed
anthropic.Timeout re-export for configured timeouts, preserving existing fallback
and absent-key behavior. No process-global httpx alias, dependency upgrade,
model change, speculative request replay or new transport owner. Re-run the full
15-file group and actual supported SDK construction/callback witnesses; only then
request a new exact-head non-author review. Parent #7179 release stays separate.

Official SDK migration reference (transport compatibility, not account authority):
https://github.com/anthropics/anthropic-sdk-python/blob/main/MIGRATION.md
The fixture-tested SDK versions above are explicit; no claim about an installed
VPS version, eligible account, global reservation, deployment or savings is made.


## R2 browser fixture and current release ceiling

The same native process tool now completed a bounded isolated Chromium capture.
The real _render_policy_brief / aibrief.html.j2 was served through intercepted
fixture.invalid requests using existing local assets. The actual producer's
explicit empty provider order generated the refusal before any model call.
English/dark and Chinese/light each passed visible-notice and no-raw-code checks
at desktop 1440x1000 and mobile 390x844. Four PNGs and a receipt are retained at:
/Volumes/Mastermind/agent-evidence/vps-aibrief-refusal-r2-20260916-sol
Receipt SHA256: 91ff01bf7e49bd9b5628762a71fe18b30db0fa7771abbad36851a61c9132b38b.

This supersedes only the earlier browser-tool-unavailable observation. It does
not reopen the separately blocked timeout source edit or prove production use.
Missing fixture assets live_config.js and neuralweb/cortex_memo.json are explicit.
Visual inspection of the English mobile capture found the existing floating AI
control partly overlapping the notice at one scroll position; global layout
acceptance is NOT claimed. Browser contexts and the owned browser were closed.

Current compilation and diff checks pass. Agent OS validation reports 0 errors,
98 warnings. The full test verdict remains 421 PASS / 1 FAIL, not full green.
This is a durable partial-source checkpoint. Do not spend an independent review
allocation on release approval until the known timeout regression is repaired.


The timeout regression initially landed in the shared helper's nightly-only test
file. It is now in the existing Brief consumer test file, already executed by the
unrun-brain-desks code gate. The failing contract is preserved, not skipped;
no CI manifest, workflow, runner or service was changed. tests/test_llm_auth.py
is byte-restored to the original W1 base because its sole new test was moved.

Final test placement recheck: the same complete 15-file group reports 421 passed,
1 failed in 11.96 seconds. The failing timeout test now resides in the existing
code-gated Brief file. This is the current checkpoint verdict, not full green.


## R3 — SDK timeout repaired; complete consumer conformance

Procedure pin: protected Mastermind a78b8fe23d8e1ed129880ac47e97ebe96afa8aea,
Skillpack 1.0.1 with same-pin companions. Source parent 2db4499a, same writer,
branch and #7185. This section supersedes the R2 current-RED verdict above.

Fresh RED: the committed timeout test failed exactly on an unrelated
httpx.Timeout object (1 failed, 39 deselected). The same native process carrier
then accepted the narrow repair: import Timeout from the installed Anthropic SDK,
retain the existing httpx fallback for absent/stubbed SDK and the final scalar
fallback, and preserve omitted-key behavior. No global module alias, model or
production dependency change. Official primary-source migration guidance confirms
SDK-owned Timeout is portable across its httpx-to-httpx2 transport transition:
https://github.com/anthropics/anthropic-sdk-python/blob/main/MIGRATION.md

Full existing 15-file provider/Brief command: 422 passed, exit 0, 10.89 seconds.
No prior RED was skipped, weakened or excluded. No CI workflow, runner, service,
pack configuration or queue/cancellation action was changed. The native initial
combined file-inspection call was blocked; it had no source effect. Independent
source metadata, the existing regression and then the same-carrier repair were
used; no alternate writer, worktree, device or delegated edit was substituted.

Actual-SDK conformance was expanded beyond the R2 extracted callback: the REAL
provider builder, SDK constructors/messages, Brief producer, parsed result,
fixture data/site JSON and existing page template now run as one tested path.

Nine scenarios PASS in BOTH SDK 0.125.0 and 1.6.0 (18 executions, not extra unit
coverage): first Anthropic result; DeepSeek-only result and thinking override;
authoritative auth refusal fallback; authoritative quota refusal fallback; empty
eligible-provider refusal before construction; post-response TypeError with no
replay/fallback; policy revision during execution with discarded output; cached
actual-model provenance plus changed-model regeneration; Chinese translation via
the same qualified builder. All actual producer returns match both fixture JSON
outputs, and the real aibrief template consumes the result. Native subscription
discovery stays zero. SDK transport factories inject ONLY native mock transports;
constructors/messages are real. DNS/socket functions are denied and counted:
zero real network attempts in either run. Environment is scrubbed before launch.

Replay bundle (source-byte hashes, full driver, exact dependency lists, receipts):
/Volumes/Mastermind/agent-evidence/vps-aibrief-sdk-r3-20260916-sol
Manifest SHA256: d1f78eff6b7dfd9f1611677a53a327bec4183362ab7886d88f084e936d99a64e
Driver SHA256: 01ef9215d9e6f0b76857a5e3fbe489c61de904f6eb499ff8e1156f3748f300c8
To replay, create an isolated Python3.12 environment with the matching
packages-<version>.txt and invoke replay.py <exact-source-tree> <new-receipt.json>
with a scrubbed environment and disposable HOME/TMPDIR. The driver refuses a
source tree containing .env. Never run it on the live VPS or enroll credentials.
Both operation-owned SDK virtual environments were removed after verification.

Source capability is BUILT_NOT_PROVEN; whole VPS fabric cutover remains PARTIAL.
No real provider, current VPS SDK version, account eligibility, shared reservation,
production publication, live policy activation or Claude-saving claim is inferred.
Next: current-base source-contract check, immutable publication and fresh independent
exact-head review; preserve existing parent-first release and real VPS proof gates.

R3 full differential source-contract check against observed main
11485597cc53b3137346084aae4623cceed28a3f completed: exit 0, zero introduced and
zero inherited findings, 269.68 seconds. This validates the candidate dependency
contract; it is not the separate runner/pack recovery programme. The observed
main added no intervening changes on the policy, builder or Brief source surface.

Read-only public live baseline, 2026-09-16T08:37:06Z (no cookies or credentials):
- bot.mastermind-x.com/health: HTTP200, status ok, source
  e61f2951136bdc03a7ec2f5f12f960af26656a4c; scope scheduled_portfolio_reasoning;
  backend waterfall, primary claude_oauth_fallback, codex_available false,
  shared_reasoning_available true, reasoning_policy_ok true, scheduled_runtime_ok true.
- www.mastermind-x.com/api/health: HTTP200, status ok, imported process commit
  4ec24e0f47 and checkout 11485597cc. This proves different reported generations,
  not by itself an outage or a reason to restart the service.
- www.mastermind-x.com/master_brief.json: HTTP401. No alternate identity/access
  route was attempted; actual published Brief content remains unverified here.
The existing source explicitly exposes /health for monitoring. No provider call
or service operation was invoked. This baseline confirms the scheduled Portfolio
path still advertises Claude OAuth fallback; it does not measure actual spend or
prove that every other Codex path is unavailable. Keep production activation held
until its separate consumer, account-eligibility and admission owners qualify it.
