<!-- AUTO-GENERATED — do not edit by hand. Source: config/house_law_checks.yml. Regen: `python3 scripts/check_house_law_registry.py --emit-docs` -->

# House Law CI Guard Suite

This document is the single reference for every house law, whether and how it is mechanically enforced, which CI job runs it, and what its known blind spots are. It is generated from `config/house_law_checks.yml` by `scripts/check_house_law_registry.py --emit-docs`. **Do not edit this file directly** — edit the YAML and regenerate.

The registry exists so that no one needs to guess: which guard covers this law, does it run on PRs or only in nightly, is a finding fatal or just a warning, what does it miss. Everything is in one place.


## How to add a new guard

When you create `scripts/check_new_boundary.py`, you must complete these steps before
the PR can merge (the meta-guard blocks unregistered check_*.py files):

1. **Write the script** — follow the existing guard style: explicit exit codes, a brief
   docstring, and a `--selftest` flag if the check can self-verify.

2. **Add a registry entry** in `config/house_law_checks.yml` using the frozen schema
   (`schema: house_law_checks.v1`). Fill in every required field:
   - `law_id` — unique, kebab/snake slug like `domain.thing`
   - `summary` — one sentence explaining what is enforced and why it matters
   - `source_ref` — list of paths or doc references
   - `severity` — one of the vocab values in the table below
   - `check_script` — path to your script (e.g. `scripts/check_new_boundary.py`)
   - `ci_wiring` — list of `{workflow, job, lane}` entries (required unless severity is
     `manual`, `discipline`, or `known_spurious`)
   - `selftest` — `true` if the script contains a `--selftest` flag, `false` otherwise
   - `selftest_exempt_reason` — required when `check_script` is non-null and `selftest: false`
   - `owner_program` — which program owns this law
   - `known_limits` — list any static-analysis blind spots honestly

3. **Wire the CI job** — add a step to the appropriate `ci_wiring` workflow/job that calls
   your script. Match the style of neighboring guard steps (same runner, checkout, pip
   install pattern). If you need a new logical CI job, append it to
   `.github/ci/legacy-jobs.yml` without reordering or reformatting existing jobs.

4. **Run the meta-guard locally** to verify:
   ```
   python3 scripts/check_house_law_registry.py --selftest
   python3 scripts/check_house_law_registry.py
   ```

5. **Severity downgrades or entry removals** require an operator/Fable ruling recorded
   in the PR. You cannot simply remove or downgrade an entry without that ruling.


## Severity vocabulary

| Severity | Meaning |
|---|---|
| `hard` | Any finding causes CI to exit 1 — the PR cannot merge. |
| `hard_for_protected_paths` | Protected modules (Article-2 money-path: alert_triage / board_ordering / top_setups) fail CI on any finding. Other modules emit a warning but do not block the merge. |
| `warn` | Visible warning printed to the CI log. Does not block the merge. Every warn entry must carry a ratchet date (when it flips to hard) or a permanent reason in notes. |
| `manual` | Run-by-hand tool — not wired into CI. Requires a manual reason (e.g. API key needed, or pre-deploy check). Document when to run it in the notes field. |
| `discipline` | Law exists and is enforced, but not mechanically checkable by a static script. Enforcement is by architecture, code review, and program rulings. |
| `known_spurious` | A known external false-red that must NOT block merges. Document the source and why it cannot be fixed. |

## Hard laws

| Law ID | Summary | Enforced by | CI wiring | Selftest | Allowlist | Ratchet | Known limits |
|---|---|---|---|---|---|---|---|
| `auth.no_client_supplied_identity` | Route handlers must not derive user_id (or equivalent identity) from the request body, query string, or path. _pg() uses SUPABASE_SERVICE_ROLE_KEY, which bypasses RLS, so token-derived identity is the only thing preventing cross-tenant access. Law source: MASTERMIND_SECURITY_AUTH_BILLING_AUDIT.md §2.1 (WS-9 / GATE-6). | `scripts/check_no_client_supplied_identity.py` | `ci.yml/no-client-supplied-identity` (pr_ci) | yes | — | — | AST over app/** FastAPI/Starlette handlers only. Operator admin tools are a different trust boundary and are not scanned.; Dynamic key construction (body.get("user_" + "id"), getattr with a non-literal) is invisible.; Pydantic models defined in another module are not joined; only same-file body models are checked for identity fields.; A Depends() wrapper that itself reads a client field is a known miss — the parameter then looks injected. |
| `ci.vintage_pin_fence` | Tests must not equality-pin a live nightly store (govrev latest/candidates, options episodes.jsonl, prophet plans, spine predictions.parquet, the Release Radar tree under data/release_forecast including cpi_truth, data/fred_vintage/release_targets, vector and commodity alert jsonl, alerts_log.parquet, tripwire latch state). A hand-typed ``== N`` / ``<= N`` against a store the nightly advances reddens main and then, through merge-on-green, every armed PR. Legal: a watermarked prefix slice, a census derived from a committed receipt, a named disclosure set, or the #5547 cold-test pattern (bind config.data_dir to empty tmp, or pass explicit root=). Shrink-only baseline: fingerprints leave when the pin is gone; do not add a row to ship a new pin. | `scripts/check_vintage_pin_fence.py` | `ci.yml/vintage-pin-fence` (pr_ci) | yes | — | — | The live scan is a targeted ratchet over tests that read the named stores; it is not a general AST linter of every ``== N`` in the tree.; A store must be added to LIVE_STORES in the script before a new nightly-advanced path is covered.; Default-root readers are a closed set (convergence_tier, measured_ic). A helper that wraps those calls is a known miss unless the test body also names the store or calls the reader directly.; Bare config.data_dir() joins and pd.read_parquet loaders taint only when the suffix matches a named LIVE_STORES path; an unnamed sibling under data/ is invisible until it is added to LIVE_STORES. |
| `contracts.artifact_drift` | Each artifact in site/factordata/contracts/artifact_manifest.json must not drift from the live artifact's actual top-level JSON fields. Schema drift between producer and consumer is invisible at trade time without this gate. | `scripts/check_contract_drift.py` | `ci.yml/contract-drift` (pr_ci) | no | — | — | Wildcard artifacts (per_stock_intel, per_stock_signal) are skipped when no live sample is present — normal on CI runners. |
| `contracts.board_contradictions` | The standout board must not show loud labels that contradict its own quiet fields: FRESH on stale/extended cross, urgency=imminent on blocked label, green band on Lagging verdict, and a board that is not in its own declared sort order — under us_prophet_v1 that means stage buckets in order, score non-increasing within a bucket, and no blocked-stage row above a live one; on legacy boards it stays the alpha-desc-within-lane check. | `scripts/check_board_contradictions.py` | `ci.yml/board-contradictions` (pr_ci) | no | — | — | PR gate skips when the artifact is not touched by the PR. Fresh renders are graded warn-only by the builder's render-lane self-check (scripts/build_stock_library.py, "board-contradictions self-check" log line, added in PR 2171); the pages.yml twin is workflow_dispatch-only and guards manual GitHub Pages mirror re-deploys, not nightly renders. |
| `contracts.reliability_contract` | A field that has been gated out (reliability state < threshold) must not render directionally in any template or site page. Audit #46. | `scripts/check_reliability_contract.py` | `ci.yml/trial-budgets` (pr_ci) | no | — | — | Literal-path scan; does not catch dynamically computed field references. |
| `contracts.signal_gate_pair_coherence` | us_standouts.json and signal_gate.json are written by ONE build_stock_library run and carry two copies of the same reading, so every board row with a non-empty `signal` blob must equal its gate verdict on every key the gate entry carries. Only the gate write is guarded (`if sig_verdict:`), so a scope=all re-render can advance the board and leave the gate behind — and both files stamp the same `as_of` (the DATA date), so nothing on either artifact reveals the skew. | `scripts/check_signal_gate_coherence.py` | `ci.yml/signal-gate-pair-coherence` (pr_ci) | no | — | — | PR gate skips when neither artifact is touched by the PR. Fresh renders are graded warn-only by the builder's own self-check (scripts/build_stock_library.py, "::warning title=signal-gate-coherence"), which fires at the moment the skew is created.; Absent artifacts print n/a and exit 0 — sparse agent checkouts and fresh clones carry no site/ tree, and an absent pair is not a contradiction.; A differing emit.pair_id alone is not a violation; it only names which side is stale once the CONTENT already disagrees. |
| `cs.ingestion_health_gate` | Capital Structure selected filings with zero durable verified evidence must fail the isolated capital_structure job. Compiler telemetry.as_of is generation time, not source freshness. A proven empty/already-known queue is the only permitted non-failing zero-progress result. Collect stays green so the market plane is not frozen. | `scripts/check_capital_structure_health.py` | `daily.yml/capital_structure` (scheduled) | no | — | — | The gate reads collector receipts after the CS compilers; it cannot repair a dedicated-R2 AccessDenied ACL, only fail closed or fall back at collect time via build_source_store(require_writable=True). |
| `cycles.cross_page_consistency` | The same instrument must show the same pos_v2/phase_v2 across every page that renders it (cycle / country_cycles / markets / sector), within tolerance, or carry a declared basis label. | `scripts/check_cycle_consistency.py` | `ci.yml/cycle-consistency` (pr_ci) | no | — | — | Tolerance is set by the checker; basis labels must be machine-readable. |
| `cycles.ontology_js_sync` | The committed site/cycle_ontology.js must exactly match what engine/cycle_ontology.py produces at HEAD. A stale committed JS is especially dangerous around the ONTOLOGY_AXIS flag-day axis-flip. | — | `ci.yml/cycle-ontology-js` (pr_ci) | no | — | — | Enforced by `python3 -m scripts.gen_ontology_js --check` (diff mode); does not validate semantic correctness of the ontology itself. |
| `design.reference_integrity` | No reference laundering: a proposed design reference may not become law until the product/taste judgment happened, in the right order, independently, and left receipts. No implicit deletion — every production capability receives an explicit disposition, a data-motivated REMOVE is a mis-filed BLOCKED_DATA, a WORSE verdict on a critical user task and an unwarranted "stronger" authority claim are review-blocking until adjudicated, and receipts-or-no-canon: only a reference carrying a valid approval receipt may be cited as compliant by a registry row or as canonical by a migration packet. | `scripts/check_reference_integrity.py` | `legacy-jobs.yml/reference-integrity` (pr_ci) | yes | — | — | CI cannot judge taste and does not try. The gate proves the judgment HAPPENED (dual independent critics, rationale quarantine, frozen first pass, design-authority verdict packet, approval receipt) and left auditable receipts — never that the design is good.; The data-motivated-REMOVE rule (blocked-data-as-removal) is a LEXICAL heuristic over the rationale text. Deliberately no allowlist: by RIG §1 a removal caused by data insufficiency is definitionally a mis-filed BLOCKED_DATA, so a match is enforcement, and a rationale that cannot be told apart from a data excuse is not yet a documented product decision.; Sparse agent worktrees under-enumerate L7/L9 locally — mockups/design_system and data/ are excluded from most session checkouts, so those roots emit a ::notice skip instead of a finding. CI checks out the full tree, where both rules are armed; a present-but-missing registry file is a hard error.; Status-scaled by design: draft/in_review artifact sets must only be internally valid (mid-flight is legal), and a revise/rejected set keeps its upheld findings without failing CI — that record IS the artifact. Full L1-L6 arms at status approved and under --evaluate. |
| `epistemics.badge_passport` | Every conviction badge rendered in a desk brief must carry a provenance passport. A badge without a passport is an unearned claim. | `scripts/check_badge_passport.py` | `ci.yml/outcome-spine` (pr_ci) | no | inline | — | Allowlist (LEGACY_NO_PASSPORT) is fully drained — no new items allowed. |
| `epistemics.claim_passport` | Every qual-ladder field that reaches a scored consumer must have a registered claim passport entry in config/qual_ladder.yml. Grandfathered debt entries (state=DISPLAY) must be cleared once the §3 gate passes. | `scripts/check_claim_passport.py` | `ci.yml/house-law-registry` (pr_ci) | no | config/qual_ladder.yml | — | 15 grandfathered DEBT entries (altdata.signal_score, policy.dir) are DISPLAY-state (gated); must be cleared once §3 gate passes.; ADVISORY unregistered fields (31+) are printed but do not fail the check. |
| `epistemics.no_literal_ntrials` | No new code may call deflated_sharpe with a caller-asserted literal n_trials instead of ledger=; literal trial counts make budget tracking impossible. | `tests/test_no_literal_ntrials.py` | `ci.yml/trial-budgets` (pr_ci) | no | inline | — | Allowlist (LEGACY_LITERAL_NTRIALS) only shrinks. |
| `epistemics.trial_registration` | Every multiple-testing harness (validate_* / *_phase0 scripts) must register its trial budget in the trial ledger. An unregistered harness makes DSR/kill/FDR deflation calculations dishonest. | `scripts/check_trial_registration.py` | `ci.yml/trial-budgets` (pr_ci) | no | inline | — | Allowlist (LEGACY_UNREGISTERED) only shrinks; new scripts must register.; Scans script names by file pattern; does not parse script contents. |
| `epistemics.validated_claims` | Every affirmative use of the word "validated" (EN + ZH "已验证") in user-facing surfaces must trace to a stored, leak-free, pre-registered artifact or an allowlisted justification. Negated/hedged uses are ignored. Law source: CLAUDE.md ("The word 'validated' in user-facing text is CI-enforced"). | `scripts/check_validated_claims.py` | `cycle-calibration.yml/calibrate` (scheduled), `ci.yml/validated-claims` (pr_ci) | yes | data/regime/validated_claims_allowlist.json | — | Scans templates/, site/*.js, generated *_data.js only; does not scan research/*.md (frozen Fable ruling 2026-07-06 — see epistemics.research_docs_not_scanned).; Verbatim third-party research text is skipped by construction (_THIRD_PARTY_PAGES) — the syndicated-note sinks of the three research-vault render targets, site/research/<slug>.html, site/research/index.html, and the SSR-baked catalog in site/research_vault.html. Reported speech from a named external author is not a platform claim and has no artifact to cite, so the alternative was a per-quote allowlist entry (a false "claim of record") every time a note used the word in ordinary English. Scoped to the third-party SINKS, never the path — platform copy on the same pages (the .rr-gate paywall block, the footer, the crawl-hub chrome) is still gated, and templates/ gets no exemption at all. Pinned by tests/test_validated_claims_thirdparty.py. |
| `flow_continuity.statement_tape_schema` | Statement-tape schema guard (FL-E / FC-R9).  Every non-comment row of data/statement_tape/registry.jsonl must satisfy the binding schema (date, speaker, venue, quote, stance_tag enum, added_by, ISO added_at), and PS-R1 is enforced at the key level: any field name implying prediction or scoring (signal/forecast/prediction/probability/edge) is a hard failure — the tape records what was SAID, never a signal. | `scripts/check_statement_tape.py` | `ci.yml/engine-render-guards` (pr_ci) | no | — | — | Registry is operator/session-appended only (no automated collector per the FL-E scope ruling); the guard validates schema and key-name discipline, not the factual accuracy of quotes. |
| `git.no_conflict_markers` | No committed file may contain literal git conflict markers (<<<<<<< / ======= / >>>>>>>). A merge once shipped markers inside templates/dashboard.html.j2; Jinja treats them as plain text, so they rendered visibly. Selftest proves the guard fires before it is trusted. | `scripts/check_conflict_markers.py` | `ci.yml/conflict-markers` (pr_ci) | yes | — | — | Scans templates/, engine/, scripts/, site/; does not cover binary or gitignored files. |
| `governance.model_routing` | Agent/Task spawns must name an explicit model or use a pinned agent type (builder/reviewer). Fable runs only via orchestrator+FABLE-WHY (spawns) or a script-level FABLE-WHY line (workflow stages — operator re-enable 2026-07-18 'fable ultracode'). Workflow scripts whose agent() calls carry no model:/agentType: routing are denied. Law source: CLAUDE.md §Model routing, enforcement via PreToolUse hook. | `.claude/hooks/model_routing_guard.py` | `settings.json/PreToolUse` (hook) | no | — | — | Fail-open by design (hook errors do not block the tool call).; Only fires at tool-use time; does not audit committed workflow YAML files. |
| `governance.private_boundary` | data/private/ and data/operator/ must stay gitignored, and committed feedback artifacts (data/governance/*.json[l], site/mastermind/*.json) may carry only counts — no tickers, fill/position/decision IDs, host paths, dollar amounts, or secret env-var patterns (FB-R12/FB-R13). | `scripts/check_private_boundary.py` | `ci.yml/private-boundary` (pr_ci) | yes | inline | — | site/mastermind/mastermind_snapshot.json is exempt from the private-key, dollar-amount, and secret-env scans (FB-R1 paper book publishes tickers by design) but not from host-path scans. |
| `identity.stable_key_vendor_alias_boundary` | Stable universe, page, and ledger keys must not fork when a live listing moves to a new vendor symbol. Every current universe key that is absent from the latest listing directory must resolve through lib.ticker_aliases to one currently listed vendor symbol; alias targets must be unique and must not also appear as a second universe member for the same company. | `scripts/check_symbol_rename_drift.py` | `ci.yml/house-law-registry` (pr_ci) | no | — | — | The hard gate evaluates the latest tracked listing directory and current universe snapshots; it does not rewrite historical filing or membership symbols, which retain the identifiers known at their event time.; Stale open point-in-time membership intervals are reported as warnings because closing them requires separate dated membership evidence.; Class-share spellings use the repository's bounded dot/dash/base-symbol tolerance and do not constitute a general company-identity mapper. |
| `meta.house_law_registry` | The house-law registry (config/house_law_checks.yml) must be internally consistent and cover every scripts/check_*.py file in the repo. An unregistered check_*.py blocks merges. | `scripts/check_house_law_registry.py` | `ci.yml/house-law-registry` (pr_ci) | yes | — | — | Census only auto-covers scripts/check_*.py — guard-shaped pytest files (e.g. tests/test_no_literal_ntrials.py) and harness hooks are voluntary entries; adding them is required but not mechanically enforced by census.; Wiring check is textual (job steps mention the script basename), not execution-order-aware. |
| `metabolism.blocklist_drift` | One kill-registry, compiled everywhere (R-V4-5).  research/DO_NOT_REBUILD.md is the single source of standing kills; scripts/compile_loop_blocklists.py deterministically compiles it to config/compiled_kill_registry.yml plus the generated block of config/signal_foundry_blocklist.yml.  This check recompiles and byte-diffs BOTH committed artifacts, and hard-fails on table rows outside DO_NOT_REBUILD.md sections 1-4 (the compiler parses only 1-4; a row elsewhere silently never reaches enforcement — MRI-R38 CPI row, found live).  Drift heals with `--fix`; prevention is the PostToolUse hook .claude/hooks/blocklist_regen_guard.py, which auto-regens on any harness Edit/Write to DO_NOT_REBUILD.md in the editing session's own tree. | `scripts/check_blocklist_drift.py` | `ci.yml/capability-broker` (pr_ci), `settings.json/PostToolUse` (hook) | no | — | — | The compiler's topic→pattern heuristic (3 significant words) provides baseline mechanical coverage of killed topics; hand-curated loop blocklist entries remain authoritative for phrasings it cannot match.; The auto-regen hook fires only on harness Edit/Write tool calls; shell appends (cat >>) bypass it — the CI byte-diff remains the backstop, and the immune blocklist-drift recipe remains the heal path for main.; A misplaced row is detected but not auto-fixable (--fix and the immune heal_cmd cannot move a row between sections); the immune lane will bounce off it bounded-then-park per R-V7-7. |
| `metabolism.capability_redline` | Secret VALUES must NEVER land in a tracked git artifact.  The capability manifest (config/capability_manifest.yml) and broker (engine/neuralweb/capability_broker.py) are scanned for high-entropy strings (40-char hex, 20+ base64) and value-capture patterns (secrets.* references, os.environ[...] assignments to tracked literals).  Any finding is a hard CI failure.  This is the single unforgivable defect in the Metabolism (§3 of masterplan / THE REDLINE). | `scripts/check_capability_redline.py` | `ci.yml/capability-broker` (pr_ci) | yes | — | — | Entropy scan has false-positive risk on long base64-looking strings in comments; known-safe names are stripped before the scan.; Does not scan the runtime environment (os.environ values at execution time) — that is outside the scope of a static git-artifact scan.; Does not catch secrets stored in GitHub Actions env-var YAML steps (those are scanned by the workflow YAML paths in F2/ci.yml). |
| `metabolism.grader_immutability` | The fitness-producing grader files (grade_thematic, foresight_leadlag, theme_placebo, qledger_falsifier, sample_qledger_placebo) are registered in config/grader_manifest.yml with SHA-256 hashes.  Any hash drift — whether from a loop PR or a human PR — is a hard CI failure.  This is the highest-leverage fence in the Metabolism: it prevents the loop from rewriting its own fitness scoreboard (R-AUT-7). | `scripts/check_grader_manifest.py` | `ci.yml/grader-manifest` (pr_ci) | yes | — | — | Covers only the five declared grader files; threshold constants embedded in YAML configs are not yet hashed (Phase A hardening).; Does not verify the quarantine replay-corpus delta — that is a T2 operator-tap step run manually when a grader hash legitimately changes. |
| `metabolism.self_mod_fence` | Loop-authored PRs (branch prefix metabolism/* or claude/loop-*, OR a 'Loop-Authored:' commit trailer) may NEVER touch the IMMUTABLE set: .claude/hooks/**, .github/workflows/**, config/grader_manifest.yml, config/capability_manifest.yml, config/metabolism_budget.yml, engine/neuralweb/capability_broker.py, check_self_mod_fence.py, check_grader_manifest.py, settings.json, and the masterplan tier table. Fail-closed: unclassifiable input → blocked (R-AUT-5). | `scripts/check_self_mod_fence.py` | `ci.yml/self-mod-fence` (pr_ci) | yes | — | — | Attribution is by branch prefix and commit trailers, not by GitHub actor identity — the loop and operator share the same OAuth identity. A loop stage that uses a non-reserved branch name and omits the trailer would bypass this fence; the branch-protection rule for metabolism/* (F4, Phase A) adds a second layer.; Does not scan PR body or issue comments for trailer-equivalent text. |
| `nw.dag_conformance` | The per-workflow build-module sequences declared in config/dag.yml must exactly match the live .github/workflows/ files. Any undeclared drift (lane mismatch not covered by a divergences entry) fails CI. | `scripts/check_dag_conformance.py` | `ci.yml/dag-conformance` (pr_ci) | yes | config/dag.yml | — | Textual (AST-level) lane comparison; does not verify execution ordering across parallel bands.; Two suspect drift entries exist from W5a adoption and are tracked but not yet resolved. |
| `nw.entity_thesis_registry` | ETM integrity gate: data/neuralweb/entity_thesis_mechanism_registry.json must keep schema/authority discipline (all rows display_only, is_context_only true), namespace grammar per registry_type, no invented links (every edge carries source.path + source.detail and resolvable endpoints), suggestion-only duplicate flags, recomputable counts, and referential integrity against species/candidates/trial-ledger sources (ETM-C1..C7). | `scripts/check_entity_thesis_registry.py` | `ci.yml/neural-web` (pr_ci) | yes | — | — | ETM-C7 referential integrity only checks source files present on disk; absent sources are skipped, not failed. |
| `nw.evidence_clock_display_only` | The Global Evidence Clock artifact (data/neuralweb/evidence_clock.json) is display-only attention routing: authority must be display_only, every row must carry promote/mutate_source_state in forbidden_actions, states must stay inside the 9-state vocabulary, clock_ids must be unique, and summary counts must match rows. The builder reads every clock source and may never mutate one (EC-R1..R8). | `scripts/check_evidence_clock.py` | `ci.yml/evidence-clock` (pr_ci) | yes | — | — | Validates the committed artifact structurally; the builder's read-only runtime invariance is enforced by unit tests and review, not this gate. |
| `nw.factor_boundaries` | No Article-2 module may write to synapse artifact paths or reference forbidden fields (RUL-NW9/NW11 display-only law). Factor outputs are display-only and must never reach scored/alert pipelines. | `scripts/check_factor_boundaries.py` | `ci.yml/synapse-read-gate` (pr_ci) | yes | inline | — | Literal-path scan; does not catch dynamically constructed paths.; Inline _ALLOWED_ACTIONS_ALLOWLIST_PREFIXES exempts allowed_actions readers; editing that list silently widens the exemption.; Two exact inflation-state producers have AST-proven emit-only spans; dynamically constructed key writes are intentionally rejected. |
| `nw.ruling_graph_conflicts` | Ruling Graph guard (RUL-CL-6/7/8): a new fdr_family token in changed files must be declared in the ruling graph's known_fdr_families (H1, hard); a privacy-boundary token must never reach site/ or scripts that write to site/ (H2 BRIDGE law, hard). Warn tier: re-proposal of a killed/deferred idea without citing its ruling_id (W1) and expired come_back_on clocks (W2). | `scripts/check_ruling_conflicts.py` | `ci.yml/ruling-graph` (pr_ci) | yes | config/ruling_graph.yml | — | Deterministic substring matching only; W1 re-proposal detection can false-positive on benign mentions of a killed idea's match_term. |
| `nw.synapse_registry` | The synapse.yml registry must be internally consistent: all paths exist, enums are valid, no duplicate artifact paths. | `scripts/check_synapse_registry.py` | `ci.yml/synapse-read-gate` (pr_ci) | yes | — | — | Validates registry internal consistency; does not verify that artifact content matches the declared schema. |
| `ops.ci_trigger_closure` | A guarded suite's TRIGGER PATHS must reach every module the suite measures. The fourth darkness class, and the reverse direction of every census beside it: audit_unrun_tests.py asks "can ANY edit start this job" — an any() over the closure, satisfied by the test file itself — so a suite stays "triggerable" while the ONE module it exists to guard is matched by no `on.pull_request.paths` entry. An edit to exactly that module then ships without ever running its own guard. The check is static: discover the PR-gated workflows (a workflow with no `paths:` covers everything it runs unconditionally; one WITH `paths:` governs both its own `run:` steps and any job manifest it hands a runner, which is how the packed legacy jobs inherit ci.yml's single filter), resolve each named suite's direct first-party reads (imports, import_module literals, repo-relative path literals, ROOT/"a"/"b.py" joins), and fail when a resolved module is reachable by no gate that runs it. | `scripts/check_ci_trigger_closure.py` | `ci.yml/workflow-yaml` (pr_ci) | yes | inline (`# ci-trigger-closure: data` comment at the fixture site) | — | A path literal is evidence of a read, not proof of one. A suite whose SUBJECT MATTER is path filtering (merge-on-green, ci-trigger, paths-filter) carries file NAMES as fixture data, so the heuristic and the domain collide by construction. #4733 healed one such finding by adding two `research/**` artifacts to ci.yml's paths — encoding a dependency that does not exist and arming the full 4-pack run on every edit to two decision-packet files. The author-declared `# ci-trigger-closure: data` marker is the sanctioned form; it covers the line it sits on when that line carries a path literal, and otherwise the statement it opens or the one directly below. It reaches only literals and joins — an import is a read no comment can excuse — and every suppression is counted in the summary and named by `--report`. Inference was prototyped and rejected: "a literal that never reaches an open/read_text/Path call is data" drops 787 of 3,824 subjects across 132 suites, including PRESS_MUST_RESTART's 15 real ones (reached through @pytest.mark.parametrize).; The gate is DIRECT reads only (depth 1). `--depth N` widens the report to transitive reads and is deliberately NOT gated: past depth 1 the path-literal heuristic starts resolving placeholder strings out of docstrings, and a guard that cries wolf gets ignored — which is the exact failure mode this file exists to end.; Reachability is decided under GitHub's `paths` glob semantics (exact, fnmatch, `prefix/**` subtree). A path expression GitHub accepts but this resolver models differently would be judged on the model, not the runner.; Suites that NO job names are UNRUN, not trigger gaps — scripts/audit_unrun_tests.py owns that class.; Scope was tests/ only until 2026-08-06 (#4693) — a suite wired from research/ was invisible to this gate even once wired. Suites now come from audit_unrun_tests.discover_suites() and run-step tokens resolve against that inventory, so a test_-shaped CLI instrument named by a run: step is not gated as a suite. |
| `ops.government_revenue_amount_semantics` | Amount classes are never conflated. Obligation, outlay, ceiling, bookings, backlog, funded backlog, and GAAP revenue are distinct quantities. The classes live once in engine/government_revenue/amount_semantics.py and are derived from the collector's canonical number-column declarations. The source guard fails when addition, summation, fallback, or publication combines incompatible classes or drops the class label from a figure. | `scripts/check_government_revenue_amount_semantics.py` | `legacy-jobs.yml/unrun-government-revenue` (pr_ci) | yes | — | — | Static source analysis cannot see a conflation assembled at runtime from names it cannot resolve, such as getattr or a config-built column list.; Coverage derives from the collector's canonical number-column declarations, so a ledger field introduced outside those declarations carries no class and is not judged. |
| `ops.nightly_liveness` | The authoritative US nightly (daily.yml / Build B) must produce a run for every completed NYSE session, that run must conclude success, and the Prophet store must actually advance to that session. All three are separate failures and each is observable on its own. | `scripts/check_nightly_liveness.py` | `nightly-liveness.yml/liveness` (scheduled), `legacy-jobs.yml/dag-conformance` (pr_ci) | yes | — | — | Detection floor is the schedule: 08:00Z and 14:00Z. A bake that dies at 23:00Z is reported the next morning, not within the hour. That is deliberate — the nightly legitimately runs for hours, so a tighter slot could only report INDETERMINATE and would train the operator to ignore the channel.; Check C reads the COMMITTED site/prophet/index.json, so it proves the store advanced in git, not that the VPS served it. The 3-min pull and the VPS-side freshness_sentinel own that last hop.; Blindness (API error, missing artifact, unparseable stamp) is INDETERMINATE by design and exits 0. A guard that is permanently blind therefore reads green; test_default_repo_matches_the_git_remote pins the one input most likely to cause that silently.; Watches Build B only. Build A (closing-bell.yml) ran green through the entire 2026-08-11/12 outage while the board it re-rendered still read price_through=2026-08-10, so its health proves nothing about the ledger. |
| `ops.no_skip_only_suites` | No test may skip in every CI job that names it. A suite can be named by a job, be triggerable, and still execute nowhere — because the one job that names it does not install what its `pytest.importorskip("X")` / module-probe `skipif` gates need. That is the third darkness class, between UNRUN and UNTRIGGERABLE, and an importorskip added to clear a red lane creates it by construction: main goes green AND weaker in the same commit. It shipped twice — #3703, then #3722→#3738 (fixed by #3760, where six tests including the two load_ohlcv crypto-source regressions executed in ZERO jobs for the day they were on main). The check is static: per test file collect the skip gates, per job collect the test files its `run:` steps name and the modules its single pip install line provides, and fail when no naming job provides a gated module. A first-party gate resolves to that module's own third-party import closure (engine.indicators_m2 → numpy/pandas). | `scripts/check_skip_only_suites.py` | `ci.yml/workflow-yaml` (pr_ci) | yes | — | — | Gates on things pip cannot install are deliberately out of scope: a data artifact (_PANEL.exists()) or a binary (shutil.which("node")) is a different darkness class with a different fix.; Suites that NO job names are reported as UNRUN and never fail this gate — scripts/audit_unrun_tests.py owns that class, and wiring every unrun suite would blow the ci-pack budget.; Scope was `tests/` only until 2026-08-06 (#4693), which made this gate and both censuses blind to research/-resident guard suites — the ones most likely to be dark, because a packet fenced to files-only writes its suite next to the instrument. Discovery is now the whole tree via scripts/audit_unrun_tests.discover_suites().; A test_-shaped FILENAME is not a suite: three files under research/ are CLI measurement instruments that collect zero tests. Classification reads the AST for what pytest would collect rather than trusting the name, so those are excluded — reporting them would make the census noise, which is the failure mode these guards exist to end.; Transitive dependency closure (PACKAGE_REQUIRES) is a hand-maintained table of hard runtime deps. Under-declaring costs a false red; over-declaring costs a missed skip-only suite. It is deliberately restricted to certainties (pandas→numpy, jinja2→markupsafe, …). |
| `ops.runner_routing_policy` | Public-repository pull-request workflows remain exactly GitHub-hosted during Wave B/C, cannot delegate through reusable-workflow call jobs, and cannot consume the bounded PC/M1 migration labels. Only registered main-branch workflow-dispatch canaries may request those labels, downstream of a hosted trust gate. PC render and CI labels remain disjoint and the M1 canary cannot regain a broad production label. Every runs-on label in every workflow must be declared in the label_registry, and an orphaned label may not be used by a scheduled workflow without a dated waiver. | `scripts/check_runner_policy.py` | `legacy-jobs.yml/workflow-yaml` (pr_ci) | no | — | — | Repository lint is a merge guard, not a runtime admission boundary. The live home runners must also reside in the organization runner group that is restricted server-side to exact workflow paths pinned to refs/heads/main.; Production self-hosted labels are now in scope for DECLARATION (R11/R12, added 2026-08-17): every literal runs-on label in every workflow must be a label_registry key, and a workflow with a schedule trigger cannot use an orphaned label without a dated waiver. Routing rules R1-R10 still govern only the Wave B/C migration labels — this law does not gate which production label a job may use, only that the label is declared.; label_registry `status` fields are hand-maintained, refreshed by hand from the GitHub runners API, because listing live runners needs an admin-scoped token CI does not carry. R11/R12 therefore catch an undeclared label or an orphan-scheduled label at PR time, never a host that went offline an hour ago — that gap is why `offline` is watch-only and not R12-blocking. |
| `ops.vps_live_freshness` | Once the VPS is the declared live-plane primary, its public status envelope must prove that the fast, snapshot, and session-gated bars lanes are healthy and within their cadence budgets. Quotes, release detection, market-state leaves, baskets, and flow artifacts must also stay within their session-aware freshness limits. This is an external dead-man on GitHub-hosted compute, so an offline VPS or Mac fleet cannot report itself healthy. | `scripts/check_vps_live_health.py` | `vps-live-heartbeat.yml/health` (scheduled) | no | — | — | The ten-minute GitHub schedule is best-effort and can itself be delayed.; The check proves artifact/lane freshness and last-run success; it does not validate the economic correctness of a vendor print.; It becomes scheduled only when VPS_LIVE_PRIMARY=true; manual dispatch is always available during the pre-cutover soak. |
| `ops.workflow_yaml_parses` | Every file under .github/workflows/ must load as YAML and carry an on: trigger block plus a non-empty jobs: mapping. An unparseable workflow file makes GitHub stamp a FAILED zero-job run on every push to every branch carrying it (2026-07-10→11 incident: metabolism-agenda.yml shipped with column-0 python -c continuation lines that terminated the run: | block scalar — ~325 failed runs in ~26h from a schedule-only workflow; introduced #2135, file repaired #2260). dag-conformance only yaml-loads workflows declared as dag.yml lanes, so non-lane workflows had no parse guard at all. | `scripts/check_workflow_yaml.py` | `ci.yml/workflow-yaml` (pr_ci) | yes | — | — | PyYAML (YAML 1.1) is not GitHub's exact parser; semantic errors GitHub rejects (bad expression syntax, unknown keys) can still pass here. The guard covers the observed lethal class: parse failure and missing on:/jobs: blocks. |
| `theme_graph.edge_contract` | The GMI theme graph's structural contract (masterplan §4.1/§4.4, gate G0.2). All three stores keep their exact column set and order and validate against contracts/theme_graph/*.v1.schema.json; every enum value and every company node id is scanned in full; EVERY edge cites at least one evidence_ref that resolves to an evidence row with a parseable dated published_at (the general form of the semantic-leakage law, with source_class=llm_proposed_ratified refused outright without one); identity_epoch >= 2 requires a ratified row in config/theme_graph_identity_breaks.yml; and every closed edge in the full history still resolves in the latest-belief view, so a closed membership stays findable instead of merely absent. | `scripts/check_theme_graph_contracts.py` | `legacy-jobs.yml/unrun-intl-libraries` (pr_ci), `daily.yml/engine` (scheduled) | yes | — | — | The nightly invocation is ADVISORY (rc 0 on a breach, one line-start warning annotation) because the graph is display-tier with all six authority booleans false, so a contract breach must not take the collect lane down. --strict is what CI runs.; A missing store is INDETERMINATE, not a breach — a sparse checkout carries no data/ and the state before the first backfill is not a failure.; jsonschema runs on an evenly-spaced sample of <=200 rows per store; enums and the company-id grammar are full-scanned, so a bad value outside the sample is still caught, but an exotic per-row schema violation outside it could be missed.; The guard checks that evidence is DATED and resolvable, never that it is true. |
| `ui.admin_js_no_undef` | Every bare identifier in admin/static/*.js must resolve to something in scope. app.js is a flat top-level browser script, so a `let` declared in one function and read from another parses clean, reviews clean, and throws a ReferenceError only when the code path runs. PR #3693 shipped exactly that ("rejBox is not defined") and the admin Outbox panel rendered BLANK in production for ~2 days until #3871 module-scoped it as OBX_REJ. node --check cannot see it — an undefined identifier is valid syntax — so this runs eslint with a single rule, no-undef. | `scripts/check_admin_js.py` | `ci.yml/admin-js-no-undef` (pr_ci) | no | — | — | Only the no-undef rule runs. Typos that resolve to a real global (e.g. `name`, `status`, `length` on window) are legal references and stay invisible.; Assumes admin/static/index.html keeps injecting no extra globals; a future vendor <script> would need its names added to the eslint env.; Needs node + npx on the runner. The pytest lane fails RED rather than skipping when the tooling is missing under CI, so an unrunnable guard can never read green. |
| `ui.crypto_shelf_governor` | The Bitcoin Vector and Crypto Cockpit Tier-1 spines are closed sets. Once a surface carries its first data-shelf marker, vector must expose exactly S1-S6 plus one data-verdict marker and crypto must expose exactly H1-H8. W0 deliberately leaves legacy templates unarmed; W1 and W2 arm their respective surface by shipping the complete marker set. | `scripts/check_crypto_shelves.py` | `ci.yml/crypto-shelves` (pr_ci) | yes | — | — | Source-level marker counting cannot prove that CSS or client code leaves every shelf visible.; Content nested inside the final details shelf is intentionally outside the Tier-1 count. |
| `ui.design_system_forward_ratchet` | design.system_ratchet measures the estate but cannot block it: ~19k inherited findings in templates/ mean a whole-estate gate is a gate somebody disables in week one. This law is the forward-only half. In `--mode enforce-added` the checker parses the PR's own unified diff and blocks ONLY on findings whose line number was ADDED by that diff, so a newly introduced raw colour, font stack, non-token radius, literal-valued custom property, emoji-as-UI, or parallel token root fails closed while the identical decision sitting on an unchanged line one row above stays green. Pre-existing debt reports and cannot grow. The blocking set is the four mechanical rules plus `emoji` and the new `parallel-token-root` rule, which fires on a `:root` custom-property family declared anywhere except templates/theme.css — theme.css is the sole token authority, and a second root is how a page mints a private palette the design system cannot see. | `scripts/check_design_system.py` | `legacy-jobs.yml/design-governance` (pr_ci) | yes | — | — | Changed-line only by construction: moving an existing violation to a new line makes it newly blocking, and editing a line adjacent to old debt does not. That asymmetry is the intended forward-only bargain, not a bug.; Needs a real merge base. On a shallow checkout `git merge-base` fails and the wired step exits 0 with an explicit message rather than guarding nothing silently — the fetch-depth 0 in the job is load-bearing.; `parallel-token-root` is naive about nesting: it fires on a `:root` block declaring custom properties outside theme.css even when the values are pure token derivations. Scoped (non-`:root`) derived properties stay legal.; The `emoji` rule BLOCKS on a narrower set than it REPORTS. Rule 8's census regex covers Misc Symbols and Dingbats (U+2600-27BF), where the estate already carries ~1,597 typographic glyphs across 145 template files — `⚠`×188, `✓`×147, `★`×82, `✕`×59. Blocking on those would red an added table row containing a checkmark, so enforce-added blocks only on pictographic planes and regional indicators. `✓`/`★`/`⚠` are typography; the architecture authorizes blocking new emoji-as-UI, not new text symbols. Consequence to know: a pictographic glyph IS still blocking, so editing an added line carrying the `🌙` theme-toggle icon in templates/theme.js will red — correctly, per the doctrine's ban on emoji-as-icon, but it will surprise whoever hits it first. |
| `ui.font_ui_defined` | Every templates/*.html.j2 whose body-level CSS sets the page font via var(--font-ui) (font-family: or the font: shorthand in a body{} rule) must either link theme.css (a <link rel="stylesheet"> whose href attribute points at theme.css — the href VALUE, never the bare string, because CSS comments mention theme.css and defeat naive greps) or define --font-ui: locally, and should also include _interfonts.html.j2 so the self-hosted Inter actually loads (warning if missing). An undefined custom property makes the font-family declaration invalid-at-computed-value -> unset, so the ENTIRE page renders in the browser default serif (Times) — the #2195 sweep broke 6 standalone page families this way, fixed in #2647 (local :root definition + _interfonts include, commodities.html.j2 precedent). Partials (_*.html.j2) referencing var(--font-ui) are allowed (the token resolves at host-page level) and emit a warning-level note only. | `scripts/check_font_ui_defined.py` | `ci.yml/font-ui-defined` (pr_ci) | no | — | — | Textual flat CSS scan (selector{block} realignment; Jinja tags are pre-stripped so an interpolation inside a style block cannot void the scan) — a body font rule contributed to a host page by an included partial is attributed to the partial (warning) rather than the host, and Jinja-conditional branches are treated as always-active.; Presence-based, not cascade-resolved — a theme.css link with media="print" or disabled satisfies the check even though the sheet never applies on screen, and a --font-ui definition anywhere in the page counts regardless of selector reachability.; Checks templates/ only, not rendered site/ copies — render lanes regenerate pages from these templates, so the template is the sole drift vector.; Does not verify that _interfonts.html.j2's referenced .woff2 files ship in site/fonts/ (ui.interfonts_theme_sync guards the partial's @font-face contents). |
| `ui.government_revenue_projection_integrity` | The public Government Revenue surface must be one governed projection of committed canonical evidence: canonical and public JSON twins are byte-identical, schemas and the content-derived workspace bundle are valid, the compact HTML shell is semantically equal to the canonical display payload, required workbench markers remain present, and first-paint HTML stays inside its raw-byte budget. This prevents a long-running generic renderer from rebasing an older full payload over newer procurement truth. | `scripts/check_government_revenue_projection.py` | `render.yml/render` (render), `engine-render.yml/engine-render` (render) | no | — | — | Proves committed serialization and first-paint integrity, not browser interactivity after hydration or authorization middleware behavior.; Exact issuer attribution and award-event truth are governed separately; this fence prevents projection drift but does not create source evidence. |
| `ui.hub_a11y` | The landing hub (and any globe-deck page) must keep its accessibility contract: no focusable elements inside aria-hidden subtrees, the <head> boot sets the document language, the light-theme pill contrast override is present, viewport-fit=cover + safe-area insets ship, and theme.js syncs documentElement.lang. Locks in the 2026-07-10 hub a11y fixes (PR #2096/#2104 review findings). | `scripts/check_hub_a11y.py` | `ci.yml/hub-a11y` (pr_ci) | no | — | — | — |
| `ui.inline_js_parses` | Every inline <script> block AND quoted on*= handler attribute in committed site HTML must be syntactically valid JavaScript (handlers node-checked as function bodies, mirroring browser handler compilation). Smart-quote contamination (U+2018/U+2019/U+201C/U+201D as JS delimiters — in <script> code position or anywhere in a raw on*= value) is a hard error, including in templates/*.j2 (Jinja-stripped). A script syntax error blanks the page silently (IIFE aborts); a handler error throws on every click. | `scripts/check_inline_js.py` | `ci.yml/inline-js` (pr_ci), `render.yml/render` (render), `engine-render.yml/engine-render` (render) | yes | — | — | Scans committed site/; does not cover dynamically generated fragments injected by JavaScript at runtime.; .j2 templates get the smart-quote scan only — node cannot parse Jinja, so plain-JS syntax errors in a .j2 still surface at the next render.; Handlers in unquoted attribute syntax (onclick=f()) are not extracted (none in the tree; quoted is the house style).; Curly quotes INSIDE JS string literals are legal (bilingual display copy); only code-position/raw-attribute hits are contamination. |
| `ui.interfonts_theme_sync` | The Inter @font-face blocks in templates/_interfonts.html.j2 (the self-hosted-font partial included in the <head> of the 16 standalone templates that do not link theme.css, PR #2119) must be byte-identical, in order, to the @font-face lines at the top of templates/theme.css — and exactly six of them must exist. The partial is a manual copy whose comment says "keep the two in sync"; without this gate an edit to theme.css's @font-face rules (a new weight, a renamed font file) drifts silently past the partial and the standalone pages lose the self-hosted font again — the exact regression #2119 fixed. | `scripts/check_interfonts_sync.py` | `ci.yml/template-site-sync` (pr_ci) | yes | — | — | Line-prefix match only — @font-face rules must stay one-per-line at column 0 in both files; the exact-count assertion (EXPECTED_COUNT=6 in the script) catches a reformat that breaks this shape in both files at once, where list equality would pass vacuously.; Does not verify that the standalone templates actually include the partial, nor that site/fonts/ ships the referenced .woff2 files. |
| `ui.ms_board_coherence` | Every rendered Market State board (site/macro.html + the CN/HK/CA dashboards) must be single-snapshot coherent — verdict word, 0-100 score, meter tick, thesis, override notes and flip line must all be derivable from ONE engine/market_state.py snapshot ("Risk Radar forces Risk-off." note => score <= 41 and verdict Risk-off; "Capped at Mixed" => score <= 59, never Risk-on; verdict word <=> score band; thesis/flip shape <=> verdict). 2026-07-07 incident: render commit 5bf116a3b3 raced the 07:10 engine-render (7a1c30ced4); the push step's `git pull --rebase -X theirs` resolved conflict regions toward the replayed stale render, stitching upstream's score 56/"Mixed" beside the stale "Risk Radar forces Risk-off." note — a hybrid ui.template_site_sync cannot catch (it guards plain-copy assets, not rendered pages). | `scripts/check_ms_board_coherence.py` | `ci.yml/ms-board-coherence` (pr_ci), `render.yml/render` (render), `engine-render.yml/engine-render` (render), `daily.yml/engine` (scheduled), `asia-close.yml/asia` (scheduled) | yes | — | — | Parses the EN (l-en) spans only — EN/ZH share each <p> source line, so a line-granular rebase stitch cannot split the pair.; Factor-row tones are not checked against the verdict (a legitimately mixed board carries mixed tones).; Pages without an ms-verdict section are skipped (not every market ships the board). |
| `ui.nav_gap` | Every page's shared nav menu must maintain a top gap (body top padding). A page that zeroes body padding jams the menu against the viewport top. | `scripts/check_nav_gap.py` | `ci.yml/nav-gap` (pr_ci) | no | — | — | Scans committed site/ HTML; does not cover pages generated only at runtime. |
| `ui.nav_mega` | Every page that renders the shared nav must carry the Research mega-menu. An out-of-loop or hand-static page can freeze the nav without the mega-menu. | `scripts/check_nav_mega.py` | `ci.yml/nav-mega` (pr_ci) | no | — | — | Only catches pages already committed to site/; new pages built outside the render loop are not caught until they land in site/. |
| `ui.runtime_style_injection` | The design-system checker scans templates/ only, so a builder could obey the design system there while authoring the real material system as a multi-kilobyte runtime CSS string inside page/composer JavaScript — invisible to every design gate. That is the enforcement escape hatch the theme-parity program exists to close. This guard counts four injection signatures (createElement('style'), style/css .textContent assignment, .insertRule(, and inline <style markup) per user-facing .js under templates/ and site/, against a frozen per-file baseline. A new injecting file absent from the baseline reds; any count above its pinned allowance reds. A count BELOW its allowance ALSO reds, demanding the same PR shrink the stale budget — that third case is what makes this a ratchet rather than a high-water mark. An advisory notice would leave freed headroom lying around for a later PR to silently re-inflate into, which is exactly the regression the fence exists to prevent. | `scripts/check_runtime_style_injection.py` | `legacy-jobs.yml/design-governance` (pr_ci) | yes | config/runtime_style_injection_allowlist.json | 2026-12-01 | Textual signature count, not semantic analysis: it cannot tell a three-line focus-ring injection from a 40KB material system, and a determined builder can evade the exact patterns. It is a ratchet against casual growth, not a proof of absence.; Specific uncovered shapes, named rather than implied: constructable stylesheets (`new CSSStyleSheet()` / `adoptedStyleSheets` / `replaceSync`) are not matched at all, nor is `el.style.cssText=`, `setAttribute('style', ...)`, or a `.textContent =` assignment whose receiver variable is not literally named `style` or `css`. A future wave that adopts constructable stylesheets must extend PATTERNS in the same PR or this fence silently stops covering the estate's real mechanism.; A self-raised budget passes: a PR may add a new injecting file and regenerate the baseline in the same commit, and the only friction is a reviewer reading the JSON diff. That visibility IS the control — the allowlist diff is deliberately human-legible — but it is review-tier, not machine-tier. `generated_from` is recorded, never validated against the PR head.; Vendored minified libraries (plotly-2.32.0.min.js) are counted and frozen like anything else. A vendor upgrade changes the count and requires a visible baseline update. That is intended — a vendor exemption class would be the same bypass in another costume.; theme.js is the sanctioned shared theme owner and the largest single budget (site/theme.js 12, templates/theme.js 10). Those are legitimate, not debt to drive to zero. |
| `ui.site_asset_refs_resolve` | Every template-decided href/src in committed site HTML must resolve to a file in the tree. A page that links a missing asset or page is a silent 404 — the HTML is well-formed and the render succeeds, so the break shows up only in production (#3988 and #4042 pruned a stylesheet a sibling page still linked; templates/ticker.html.j2 emitted the PLURAL ../baskets/ directory for pages shipping under site/basket/, dead on 605 pages and 909 links since the directory was named). Links to stocks/<TICKER>.html warn instead of failing: which ticker pages exist is decided nightly by data, so hard-gating peer/movers/crypto naming would wedge the render on ordinary universe churn. | `scripts/check_site_asset_refs.py` | `render.yml/render` (render), `engine-render.yml/engine-render` (render), `closing-bell.yml/closingbell` (scheduled), `earlyclose.yml/earlyclose` (scheduled) | yes | — | — | Proves file EXISTENCE, not markup validity. Values carrying ${, {{, <, } or whitespace are template/JS fragments rather than URLs and are skipped, so a genuinely broken href built by JS at runtime is invisible here.; Scans committed site/ only; a link a page builds client-side from a JSON payload is not covered.; /api/* and /ws/* are served by the FastAPI app, not the file tree, so they are exempt by prefix rather than checked. |
| `ui.site_js_parses` | Every standalone site/*.js bundle (loaded via <script src=...>) must parse without syntax errors. Smart-quote delimiters once blanked markets.html for ~2 days (PR #1004 → #1353). | `scripts/check_site_js.py` | `ci.yml/inline-js` (pr_ci) | no | — | — | Skips vendor/CDN bundles that are not committed to site/. |
| `ui.start_runtime_single_instance` | site/start.html must load each page runtime (globe-deck.js, sky.js, hub-welcome.js, live_config.js, live.js, wh_banner.js) exactly once. 2026-08-01 second act: an engine-render whose checkout predated the #4163 emitter fix re-rendered start.html with the old builder and pushed it post-rebase (adad513bdfe) — doubled live pair, banner rail gone, zero conflict markers, so git.no_conflict_markers passed and the page shipped to main between PR CI runs. The start-writing lanes gate the page directly: violating copies are healed wholesale from a known-good ref (HEAD pre-commit, origin/main post-rebase) and the lane only goes red if the healed copy still violates. | `scripts/check_start_runtime.py` | `render.yml/render` (render), `engine-render.yml/engine-render` (render), `daily.yml/engine` (scheduled) | yes | — | — | Counts <script src> basenames only; cannot see a runtime inlined into a <script> body or double-executed by client code.; The PR-side twin (tests/test_start_publish_integrity.py) gates pull requests; this guard exists because render lanes push to main without PR CI. |
| `ui.template_site_sync` | Every plain-copy page asset (a non-.j2 file directly under templates/ that also ships as site/<name>) must byte-match its template at every commit. 2026-07-07 incident: the scope=all engine-render run (110 min, checkout 9208ef4920) pushed commit 2ae709f9f0 whose site/sector_cycles.js was byte-identical to the CHECKOUT-TIME template blob — the push step's `git pull --rebase -X theirs` resolved the conflict against #1793's de-escalation (merged mid-render) in favor of the stale copy, resurrecting "validated" wording and reddening validated-claims on main (fixed in #1799). Render lanes self-heal via --fix post-rebase; PR CI + the publish gate keep one-sided template/site edits from arming the divergence window. | `scripts/check_template_site_sync.py` | `ci.yml/template-site-sync` (pr_ci), `render.yml/render` (render), `engine-render.yml/engine-render` (render), `daily.yml/engine` (scheduled) | yes | — | — | Direct children of templates/ only; .j2 render inputs and the templates/fonts/ binary copytree are out of scope.; Pairs whose site copy does not exist yet are skipped (new asset not yet shipped).; theme.js is compared against its baked form (Supabase config injected by lib.site_assets); in stdlib-only contexts (pages.yml publish) this degrades to a token-split prefix/suffix compare. |
| `ui.title_i18n` | Translated (CJK / t() / zh-interpolated) text must never appear inside HTML title= attributes, and t()/td() dual-span markup must never appear inside a <title> ELEMENT (RCDATA — markup renders as literal browser-tab text, #2705). The dual-span l-en/l-zh mechanism cannot operate in either place. Plain CJK inside a <title> element is allowed — the element rule is markup-in-RCDATA, not language. Law source: CLAUDE.md ("no translated text in title= attributes"). | `scripts/check_title_i18n.py` | `ci.yml/title-i18n` (pr_ci) | yes | — | — | Template <title> element rule is line-scoped (dodges committee.html.j2's JS-built SVG title strings), so a bare <title> wrapped across lines with the t()/td() call on its own line is not caught; {% block title %} bodies ARE region-scanned.; Rendered-site rule span-checks the page <title> (first title element) only; inline-SVG <title> tooltips deeper in the page are not span-checked. |
| `ui.visual_evidence_manifest` | "The light page opens and functions" has been accepted as equivalent to "light mode was deliberately designed", which is how a premium dark surface shipped with a degraded light plane. This guard makes a material visual change owe committed evidence a reviewer can actually look at: a receipt (mastermind.page_evidence_receipt.v1, exactly three keys — schema, changed_paths, manifest) maps the PR's changed presentation paths to an EXISTING mastermind.p0_evidence.v2 manifest, which must contain the eight rest cells (desktop/mobile x en/zh x dark/light), each captured, with the applied theme and locale matching what was requested and the referenced PNG present. CI checks existence and state identity ONLY — a human/Opus reviewer owns hierarchy, material depth, semantic colour and taste. | `scripts/check_ui_visual_evidence.py` | `legacy-jobs.yml/design-governance` (pr_ci) | yes | — | — | Material-change detection is deliberately narrow and mechanical (added lines in a templates CSS file, an added inline <style in a template, an added runtime-style signature in user-facing JS). It will miss a visual regression delivered purely through markup reordering, and that is the correct trade: this gate fails closed, so a loose detector becomes a fleet-wide block on ordinary PRs.; It validates every page in the manifest a receipt references. The convention is therefore a migration-scoped manifest; pointing a receipt at a large multi-page estate manifest would demand eight rest cells for every page in it.; Existence and identity only. A committed screenshot of a genuinely ugly light plane passes CI — refusing that verdict to a machine is the point, and the design review is where it is caught.; Known false positive, fail-closed by choice: adding a line INSIDE a pre-existing multi-line CSS comment is treated as material and will demand an evidence matrix. The `/*` opener is unchanged context outside the hunk, so from the diff alone the added line is indistinguishable from a real declaration, and this gate errs toward demanding evidence rather than skipping silently. Adding a whole comment BLOCK is handled correctly — the state machine sees the opener and close together. If you hit this, add the comment in the same hunk as its `/*`, or attach the receipt. |
| `ui.zh_filing_term` | 申报 must never appear in user-facing Chinese copy. A native reader hears a TAX or CUSTOMS declaration (申报纳税, 海关申报) — a self-report filed under legal compulsion — where the platform means a market DISCLOSURE: an SEC 10-K, a 13F, a congressional STOCK Act report is a 披露. Scans templates/ line by line plus the string literals of engine/ and scripts/ builders, so a wrong label is caught at its source rather than a nightly render later. Swept by hand twice before the gate existed (PR #4830 Filing Forensics, PR #4855 site-wide). | `scripts/check_zh_filing_term.py` | `ci.yml/zh-filing-term` (pr_ci) | yes | config/zh_filing_term_allowlist.json | 2026-09-07 | Python COMMENTS and DOCSTRINGS are skipped by construction — developer documentation is never rendered, and without the skip, writing the law down ("use 披露, never 申报") would break the law wherever it is recorded.; Rendered site/ is NOT scanned. A hit there always originates in templates/ or a builder, both of which are; scanning the render output would red main on a stale artifact no PR author can fix by hand.; data/ is NOT scanned. Exchange announcements collected upstream use 申报 as an order-entry term of art; the platform does not author that text.; The `pending` ratchet is per-FILE, not per-phrase, so while an in-flight PR owns a file the guard only caps its count — it cannot tell a swapped occurrence from an original one. |

## Hard For Protected Paths laws

| Law ID | Summary | Enforced by | CI wiring | Selftest | Allowlist | Ratchet | Known limits |
|---|---|---|---|---|---|---|---|
| `authority.cycle_pattern` | No Article-2 money-path module (alert_triage / board_ordering / top_setups) may read the cycle-pattern lake (data/cycle_pattern/). The CPI substrate is display-only (context tier); lake outputs must never reach scored/board/alert pipelines. | `scripts/check_cycle_pattern_authority.py` | `ci.yml/cycle-pattern-authority` (pr_ci), `cycle-calibration.yml/calibrate` (scheduled) | yes | inline | — | Literal-path scan only; misses dynamic path construction (e.g. f"data/cycle_pattern/{name}") that assembles the forbidden path at runtime.; {'_ALLOWED_READER_PREFIXES list': 'money-path modules are never exemptable.'} |
| `authority.research_factory` | No Article-2 money-path module may read data/research_factory/ or import engine.research_factory. The factory is display-only (A0-A2 ceiling); factory outputs must never reach scored/board/alert pipelines. | `scripts/check_research_factory_authority.py` | `ci.yml/research-factory-authority` (pr_ci) | yes | data/research_factory/authority_allowlist.json | 2026-09-15 | Literal-path scan only; misses dynamic path construction. |
| `nw.synapse_reads` | Every file that reads a registered synapse artifact must either appear in the baseline or be declared in config/synapse.yml. Article-2 money-path violations are hard-fail; other undeclared reads are warn-tier. | `scripts/check_synapse_reads.py` | `ci.yml/synapse-read-gate` (pr_ci) | yes | data/neuralweb/read_gate_baseline.json | — | Literal-path scan only; misses string-assembled artifact paths. |

## Warn laws

| Law ID | Summary | Enforced by | CI wiring | Selftest | Allowlist | Ratchet | Known limits |
|---|---|---|---|---|---|---|---|
| `cycles.stretch_oracle_disclosure` | The two stretch/extension oracles (O1 ladder.alignment.overextended, the oscillator-dominant 4-leg brake; O2 entry_signal.status == "extended", a relabel of urgency=="caution") legitimately disagree on ~38% of names, so the contract is DISCLOSURE, not agreement: every flagged name must carry the leg that actually fired (invariants I1-I4 in the checker; contract doc docs/site_semantics/stretch_oracles.md), and the divergence counter is re-emitted nightly against the store the render just wrote, so a stale divergence number can never again be the newest one anybody has. | `scripts/check_stretch_oracle_contract.py` | `daily.yml/engine` (hook), `engine-render.yml/engine-render` (hook) | no | — | — | site/stockdata is nightly-render output and is NOT committed, so no pr_ci lane can run the checker against a real store (CLI exits 2 "nothing to check" in a plain checkout); PR-tier coverage is the synthetic-store pytest suite only.; The in-process nightly invocation is display-tier and never fatal (the guard must never break the render); violations surface as line-start ::warning annotations, never exit codes — lane=hook mirrors the surface_freshness precedent for builder-embedded gates. |
| `data.ohlc_basis_coherence` | The breadth OHLC caches must share ONE yfinance adjustment basis: data/<ns>/_closes_cache, _high_cache and _low_cache are written from the same download, and where all three are present the bar must satisfy low <= close <= high. yfinance auto_adjust back-adjusts ONLY the window it downloads, so a split or large dividend re-bases the fresh 1mo window while the cached history keeps the old basis. Both repair paths (BreadthAdapter._merge_refreshed and scripts/heal_breadth_split_seams.py) run seam_suspects over the CLOSES matrix ALONE and rewrite the extras only as a side effect of a closes-flagged ticker — so a basis split confined to high/low is invisible to every scanner in the tree (2026-08-06 incident: REZI close/high 1.29-1.44 on 773 of 773 bars, 989 bad bars across 37 tickers, zero alarms). Close-only consumers are unaffected, which is what makes it silent; ATR's TR = max(H-L, |H-C_prev|, |L-C_prev|) is dominated by the basis gap and blows up for build_pick_lab, calibrate_bottom_radar and residual_momentum_phase0. | `scripts/check_ohlc_basis_coherence.py` | `daily.yml/collect` (scheduled), `legacy-jobs.yml/ohlc-basis-coherence` (pr_ci) | yes | — | — | Two tiers by design. Only a SPLIT is fatal — worst excess >= 10% of the bar's own range, or >= 20% of a ticker's overlapping bars with at least 5 of them. Isolated bad bars (yfinance ex-dividend rounding; 17 such bars across the three US panels at 2026-08-06, worst 2.5%) annotate as ::warning and never gate. A basis drift that is BOTH under 10% and confined to fewer than a fifth of a ticker's bars is reported as noise, not caught — the alternative is a guard that reds every night on a permanent data-quality floor.; Coverage is deliberately NOT asserted. The extras stores are forward-accruing (collectors/breadth.py "the store grows forward from first collection"), so high/low legitimately carry a fraction of the closes history — a median 55 bars against 777 at 2026-08-06. A ticker whose high/low are entirely absent is therefore invisible here; that is a coverage question, owned by the extras store's own accrual, not a basis question.; The verdict over live data belongs to the daily lane, not the PR pack. The nightly collection pushes straight to main, so no PR gate can see the commit that introduces a split; the pack lane tests the guard (synthetic frames) plus a reader-level pin over the committed caches, and asserts no verdict on them (they advance nightly — asserting one asserts about today).; continue-on-error in daily.yml by design (a data finding must never discard hours of collected bytes). The teeth are the ::error annotation and data/quality/ohlc_basis_coherence.json, committed by "commit market data" in the same job.; Panel discovery is by filesystem shape (any data/<ns>/ holding all three caches), so a namespace that never ships an extras store is silently out of scope rather than reported missing. Today that is every non-US breadth universe plus russell_breadth. |
| `design.system_ratchet` | The design system can only hold if drift is measured before it is banned. templates/ carries ~19k hand-rolled decisions the token layer already owns — colour literals, font stacks, raw radii, shadow palettes declared as custom properties in page-scoped selectors — and each one is a surface that will diverge the next time theme.css moves. A gate that reds on all of them on day one is a gate somebody disables in week one, so this guard ships REPORT-ONLY (R0): it prints per-rule counts plus capped exemplars on every PR and always exits 0. The blocking arm is written and tested from the same commit rather than deferred — `--mode enforce` exits non-zero on the four mechanical rules (colour literal, font-family literal, non-token border-radius, literal-valued custom property outside theme.css) — but it only ever looks at templates a page-registry row marks `design_system.compliant`, or at templates the registry has never seen. That scoping IS the ratchet: a migrated surface cannot regress, a brand-new surface is born compliant, and the backlog is retired one packet at a time instead of in one heroic red-build weekend. Rules 5-8 (new `*-card` classes, banned Tier-1 vocabulary, inline `<style>` byte size, emoji codepoints) are heuristics or measurements and stay warn-tier for as long as this script exists. | `scripts/check_design_system.py` | `legacy-jobs.yml/validated-claims` (pr_ci) | yes | — | 2026-10-01 | Static text scan, not a rendered-DOM check: it cannot see a colour a builder interpolates at render time, and it deliberately blanks Jinja expressions and fragment refs (href="#abc", url(#fade)) so a template decision is not reported as a stylesheet literal.; Rule 5 (`*-card` classes) is a REGEX HEURISTIC — it cannot tell a new component from a rename, so it counts both. Rule 6 ships a deliberately small seed vocabulary; the full Tier-1 doctrine lint needs a per-surface allowlist (methodology and calibration pages legitimately EXPLAIN the terms it bans) and is a later wave.; Rule 7 records inline `<style>` byte size but enforces no growth budget yet; R0 exists partly to establish that baseline.; `--mode enforce` with no readable `--registry` treats EVERY template as new and therefore blocking. That is fail-closed and it warns when it happens, but it is not a mode to wire by accident. |
| `epistemics.engine_authority_evidence` | An engine holding authority over a human must point at the evidence that earned it. Measured 2026-08-12 (Finding C-1): four of the five tier=scored synapse artifacts — vector-calibration, hazard-model, vol-regime-gate, vol-regime-basket-overlay-gate — carry NO qual_ladder_ref, so things with rank/size/gate authority do not name their prereg. Finding C-2: synapse `tier` does not express authority over a human at all — site-us-standouts (the Prophet board that orders what a paying user sees) carried the same tier=display as a decorative chip. This law reports every ARTIFACT above display authority whose qual_ladder_ref is MISSING (AUTHORITY_WITHOUT_EVIDENCE), every qual_ladder_ref that resolves to neither a config/qual_ladder.yml key nor an existing repo FILE (AUTHORITY_EVIDENCE_UNRESOLVABLE — a DIRECTORY is not a prereg), every ref the resolver was given no inputs to probe (AUTHORITY_EVIDENCE_UNCHECKED — "could not look", which is reported and is NOT counted as evidence), every gate-tripping engine with no curated output_class, every graded_by_design='yes' standing on a path-substring heuristic (GRADED_BY_DESIGN_IS_HEURISTIC), a graded_by_design value contradicting a resolved ledger, a species whose ledger_binding matches no engine (SPECIES_UNBOUND), a registered qledger desk with zero rows in the claim corpus, and the scored_path_surfaces completeness flag (an artifact read by an Article-2 enforcer module while declaring no surfaces — the measured prophet-index case). | `scripts/check_intelligence_registry.py` | `legacy-jobs.yml/intelligence-registry` (pr_ci) | yes | — | — | Advisory by default for FINDINGS (exit 0 with ::warning annotations); only --strict reds on them, and --strict is deliberately NOT passed in CI yet. The findings are PRE-EXISTING CONDITIONS of the corpus, not regressions any PR author caused. That leniency does NOT extend to PR-PLANE blindness: a run that could not read config or code exits non-zero on every run (ruling 2026-08-14). DATA-PLANE blindness — the nightly-advanced claim store alone — is reported in full and reds only under --strict, because a PR lane must not go red fleet-wide for a store no PR author touched. See the FAIL CLOSED known_limit on the sibling law governance.intelligence_registry_integrity. MEASURED 2026-08-12 on the derived view (378 engines over 642 synapse artifacts, 7 cells excluded): 181 findings — 21 AUTHORITY_WITHOUT_EVIDENCE, 113 OUTPUT_CLASS_MISSING, 45 SCORED_PATH_SURFACES_INCOMPLETE, 2 SPECIES_UNBOUND, 0 unresolvable and 0 unchecked refs — plus 41 GRADED_BY_DESIGN_IS_HEURISTIC. Authority splits 355 display / 12 engine_input / 6 user_ranking / 5 gate_size, against a synapse `tier` that saw only the 5 scored — which is finding C-2. RE-MEASURED 2026-08-14 after the rule-4 same-program restriction (same 378 engines): 212 findings — 21 AUTHORITY_WITHOUT_EVIDENCE, 109 OUTPUT_CLASS_MISSING, 45 SCORED_PATH_SURFACES_INCOMPLETE, 2 SPECIES_UNBOUND, 0 unresolvable and 0 unchecked — plus 35 GRADED_BY_DESIGN_IS_HEURISTIC; authority is unchanged. These are DATED MEASUREMENTS, deliberately not pinned by any test: every one of them moves when a sibling PR touches config/synapse.yml, and pinning them is what made rounds 1 and 2 scheduled fleet-wide reds.; `graded_by_design: "yes"` IS A WEAK HEURISTIC IN TWO OF ITS FOUR RULES, AND SAYS SO. It means "the ledger waterfall resolved a store-shaped path", and rule 1 accepts any artifact path matching /ledger/i ANYWHERE IN THE PATH while rule 4 hops to a SAME-PROGRAM consumer whose MODULE NAME matches /grade|ledger/i. A name is not proof that graded rows are written, so every `yes` reached that way is a GUESS and some of those guesses are wrong on live rows. The derived claim is neither dropped nor trusted: every engine carries graded_by_design_evidence — `strong` (rule 2, the producer statically imports engine.qledger and a desk literal resolves by AST; or rule 3, an artifact DECLARES tier shadow/scored/confirmer, which synapse's own meta.tier_vocabulary defines as claim-registered and graded), `weak_path_heuristic` (rules 1 and 4), or `none`. Every weak `yes` is ENUMERATED as GRADED_BY_DESIGN_IS_HEURISTIC on every run — 35 of the 100 `yes` engines measured 2026-08-14 — rather than described in prose or kept in a hand-maintained list of known-wrong rows, which would rot. T7 MUST NOT treat a weak `yes` as gradeability. Mirrored in the engine/intelligence_registry.py module docstring.; THE DISCLOSURE SAID "FILENAME" AND WAS MEASURABLY WRONG UNTIL 2026-08-14, and the enum value said so too (`weak_filename_heuristic`). `_LEDGER_PATH_RE` is unanchored, so a DIRECTORY component satisfies rule 1: 5 of the 35 live rule-1 matches carry `ledger` only in a directory — data/qledger/claims.jsonl, data/qledger/falsifier_evaluations.jsonl, data/board_ledger/ca_board.parquet and the two data/us_board_ledger/retro_grades*.parquet — and ALL FIVE are real grading stores whose semantics live in the directory name. Tightening the match to the basename was therefore measured and REJECTED (it would drop five true positives — the shrink direction, where a detector going blind reads exactly like a detector getting stricter); the fix was honesty, so the value is now `weak_path_heuristic` and every disclosure string says PATH. Do not re-litigate the narrowing without re-running that measurement, which is recorded in _cell_ledger_paths' docstring.; LEDGER WATERFALL RULE 4 IS SAME-PROGRAM ONLY SINCE 2026-08-14; the cross-program form is deleted, not deprecated. It used to adopt ANY grader-shaped consumer's ledger "EVEN CROSS-PROGRAM". Measured that day on the live corpus: 7 engines resolved by rule 4 and SIX of the seven hops crossed a program boundary and were wrong or unearned — engine/run.py::engine-fix, the nightly orchestrator, was "graded by" hk-canada's data/board_ledger/ca_board.parquet, and scripts/build_stock_library.py::us-stocks-prebreakout resolved through scripts/grade_us_board.py, a producer owning TWO cells with DIFFERENT ledgers, so the per-producer hop index made an ARBITRARY pick. The index is now keyed by (producer, owner_program) and the hop may only land inside the resolving engine's own program. Live effect measured the same day: rule-4 count 7 -> 1 (the surviving hop is engine/experiments_registry.py::qualitative-intelligence -> engine/qledger.py::qualitative-intelligence, a program grading its own output), the other six fall to rule 5 and publish `ledger='none'` / `graded_by_design='no — not yet'`, `yes` 106 -> 100, weak evidence 41 -> 35, and the content-finding total 222 -> 212. The engine count is unchanged at 378 — this deletes unearned SEMANTICS, never a row.; graded_by_design='yes' also requires a STORE-shaped ledger: a Python module, a charter config, and an unexpanded template/glob path cannot hold a graded row, and an engine/*_ledger.py producer with no store-shaped artifact falls THROUGH the waterfall rather than being its own ledger. Directory paths (data/metabolism/agenda/) DO count as stores — refusing them would be the shrink-direction failure this tightening exists to avoid, since a detector that goes blind reads exactly like a detector getting stricter.; Derived authority UNDER-COUNTS on day one because its input is under-populated: only 11 of 642 synapse artifacts carry a non-empty scored_path_surfaces, and prophet-index is a confirmed false negative (tier=display, no surfaces, yet consumed by scripts/build_site.py). The correct claim is "authority is derived and auditable", NEVER "all authority-bearing engines are identified".; The scored_path_surfaces completeness detector is low-precision BY CONSTRUCTION: scripts/build_site.py is a consumer on 37 artifacts and scripts/build_stock_library.py on 14, so consumer-membership in an Article-2 module is far too wide to carry authority. It PROPOSES (45 engines flagged) and never promotes — deriving user_ranking from it was rejected on that measurement.; output_class is the only field with a real human cost (113 engines require one as measured 2026-08-12). It is deliberately left null rather than guessed: output_class selects the metric contract, and a wrong metric contract is worse than a null one. There is no auto-proposal mode — curating it is T7's work, through the four-key overlay, one rationale per row.; ANNOTATION BUDGET: C-1 findings are annotated per engine (each names its concrete heal); every other code is aggregated into one annotation with a count; the fail-closed COULD NOT LOOK line is a third channel, budgeted at exactly one. One annotation per finding would bury the actionable rows. The budget governs only what GitHub RENDERS: since 2026-08-14 EVERY finding, C-1 included, is also printed as a plain `  [CODE] engine: detail` line, because an annotation-only deliverable is invisible to a terminal run, to a grep of the log body, and to anyone reading the raw job log rather than the Actions summary.; Resolvability is NOT adequacy. AUTHORITY_EVIDENCE_UNRESOLVABLE proves only that a qual_ladder_ref points at something real — a key in config/qual_ladder.yml or a FILE that exists inside this repo. It cannot prove that the document pre-registers anything; that judgment belongs to T7's backlog drain. What it does close is the set of ways the C-1 backlog was drainable to zero without a single real prereg: an arbitrary non-empty string, a DIRECTORY (`git show HEAD:research` exits 0 on a tree, so the earlier path_exists probe accepted any folder), and an ABSOLUTE PATH (`root / "/etc/passwd"` is `/etc/passwd` under pathlib's absolute-operand rule, so the worktree half of the probe answered True for any file on the machine). MISSING, UNRESOLVABLE and UNCHECKED are three DISJOINT codes because they need three different heals; none of the three is ever silent, and none counts as evidence. |
| `governance.intelligence_registry_integrity` | The Mastermind engine registry is a DERIVED ON-DEMAND VIEW, never hand-authored and NEVER COMMITTED. It is a pure function of config/synapse.yml, config/qual_ladder.yml, an AST scan of producer source, data/species/registry.json and data/qledger/, built in memory by scripts/build_intelligence_registry.py and read by consumers either as an import or off `--json` stdout. Only four curated keys may live in config/intelligence_registry_overlay.yml. This law fires ONLY on INVARIANTS of the derivation and of the hand-edited overlay: a synapse artifact mapping to zero or to more than one engine (the partition must be TOTAL and DISJOINT), an authority value that names no derivation rule or no artifact that carries it (attribution), an overlay key outside the four-key allowlist, an overlay key that is DERIVED, an overlay row for a cell the partition does not contain (orphan), a curated not_an_engine exclusion of an AUTHORITY-BEARING or evaluated cell, an unknown enum value, a missing required field, a null ledger, or an exclusion with an empty reason or one that does not record what it deletes. The key allowlist IS the executable form of DNR:KILL-PARALLEL-KNOWLEDGE-BASE — without it the overlay silently becomes the hand-authored engine list that row forbids. | `scripts/check_intelligence_registry.py` | `legacy-jobs.yml/intelligence-registry` (pr_ci) | yes | config/intelligence_registry_overlay.yml | — | THERE IS NO DRIFT GUARD, NO COMMITTED ARTIFACT AND NO `--check` EQUALITY MODE, and none may be re-added. Two earlier rounds committed a generated data/intelligence_registry.json plus a generated Markdown mirror and pinned them by equality against a "stable" input; both were scheduled fleet-wide reds and the pin merely RELOCATED between rounds. Round 1 pinned against data/qledger/claims.jsonl (append-only); round 2 moved the pin to config/synapse.yml, a MORE volatile input. Both rounds cited churn figures taken off a SHALLOW clone (13 and 26 commits in 14 days) and both UNDERSTATED it. RE-MEASURED ON FULL HISTORY 2026-08-14 (unshallowed, 11,971 commits reachable): data/qledger/claims.jsonl 70 commits and config/synapse.yml 69 commits in the trailing 14 days — 5.4x and 2.7x the shallow figures, so the conclusion is strengthened, not softened. The third candidate, data/species/registry.json, looked stable at "one commit in its whole history"; on full history it has 17, every one an adjudication PR (last #4358, 2026-08-03). It is genuinely low-churn — but a pin needs stability of CONTENT, and an adjudication rewrites exactly the rows a registry derives from. There is no stable input on this repo to pin against, so nothing generated is committed and nothing is pinned.; SEVERITY IS `warn`, NOT `hard`, EVEN THOUGH THESE ARE PR-CAUSED INVARIANTS. The two previous rounds each argued a hard pin was legitimate because "every input moves only via a PR", and both were refuted 3/3 by measurement rather than by argument. The invariants here really are green by construction, but the same sentence was true of the drift pin, so the severity stays advisory until T7 has run the gate against the live corpus for a sustained period. Structural violations still exit non-zero on the guard's own exit code, so a real violation is visible in the job log; `warn` governs how loudly the registry treats it, not whether the script reports it.; FAIL CLOSED, NOT SILENT — AND KEYED ON THE PLANE THAT WENT BLIND (ruling 2026-08-14, amended the same day). The summary line ALWAYS carries an `inputs=` clause; when any input could not be read it reads `inputs=INCOMPLETE (...)`, NAMES what was missed AND NAMES ITS PLANE, and the COULD NOT LOOK annotation and the --json `unreadable_inputs`/`unreadable_by_plane` carry the same facts. What differs by plane is only the EXIT CODE. PR-PLANE (config/synapse.yml, the overlay, config/qual_ladder.yml, data/species/registry.json, the Article-2 module table, producer SOURCE — all config and code, all moved only by a pull request) exits non-zero on EVERY run, with or without --strict: a PR author can cause it and a PR author can fix it. DATA-PLANE is exactly ONE input, data/qledger/claims.jsonl, the only one an automated lane advances (70 commits in the 14 days to 2026-08-14); it is fully reported and exits non-zero only under --strict. THE FIRST FORM OF THIS RULING GATED ON ANY INCOMPLETE READ, which handed the nightly the power to red every PR in flight: one truncated line of 46,696 in the claim store reddened the whole intelligence-registry job, for a file no PR author touches — the scheduled-fleet-red shape this program has been refuted for three rounds running, arriving through the fix for a different one. DEFERRED, WITH AN OWNER: no lane passes --strict yet, so corruption of the claim store currently ALERTS everywhere and GATES nowhere; wiring a nightly-side --strict run is T7's, in the same wave that drains the C-1 backlog and flips this law to `hard`. "Unreadable" includes PARTIAL: a store that opens but whose lines do not all parse is named with its count (`data/qledger/claims.jsonl (2 unparseable line(s) of 4)`) while the rows that DID parse stay in the view — the null law requires the incompleteness be REPRESENTED, not that the readable half be discarded. A document that reads but does not PARSE is the same state as one that could not be read: synapse -> NOT CHECKED + exit 1, overlay and qual_ladder -> treated as absent and NAMED, never a bare traceback. Sparse agent worktrees carry no data/ on disk while ~39,900 data paths are tracked in HEAD, so every read — including producer SOURCE and the qual_ladder_ref probe — goes through the worktree-then-`git show` ladder, and a producer readable from neither is announced rather than silently skipped.; `not_an_engine` is the most destructive of the four overlay keys and was once the least gated: a 3-character reason removed an authority-bearing engine with every law green (measured 2026-08-12: 378 -> 376 engines, gate_size 5 -> 4). It now requires ratified_by, a date, a >=40-character reason and a DNR:<KEY>-shaped citation if it cites one; exclusions are derived BEFORE being excluded so each records its would_be_authority; a curated exclusion of an authority-bearing or scored/confirmer/shadow cell is a structural violation; and excluded rows are still audited for content findings, so an exclusion cannot deflate the backlog. The curated-exclusion count is NOT pinned at zero by a test — a count over config/synapse.yml is exactly the class of pin this round deleted.; The engine unit is (producer, owner_program). Measured 2026-08-12, 32 of 378 engines mix artifact tiers, so the engine-level authority roll-up (a MAX) OVERSTATES authority for the low-tier siblings in those cells. A consumer acting per-artifact must read artifacts[].artifact_authority, not the roll-up.; The qledger desk literal is extracted by AST from register()/register_batch()/ make_claim() call sites. A dynamic or indirect desk value does not resolve (1 producer measured 2026-08-12: engine/china_special_situations.py); those engines fall through to ledger waterfall rules 3-5. The builder emits a ::notice naming them, so the gap is counted rather than invisible.; Species-to-engine binding is by ledger path, so several species sharing one ledger collapse to one engine. Measured 2026-08-12: 17 species bind to us_board_ledger, so the conservative least-advanced rule renders that engine phase0 even though 6 of the bound species are validated. The full bound set is recorded in validation_state_evidence.; `graded_by_design: "yes"` IS PARTLY A WEAK HEURISTIC — see the fuller disclosure in epistemics.engine_authority_evidence. Ledger waterfall rules 1 and 4 resolve the ledger from a PATH / MODULE-NAME SUBSTRING, so a `yes` they produce is a guess. Every engine carries graded_by_design_evidence in {strong, weak_path_heuristic, none} and every weak `yes` is enumerated as GRADED_BY_DESIGN_IS_HEURISTIC on every run. |
| `ops.builder_failstreak_escalation` | A brun() builder failing many CONSECUTIVE nightly runs must escalate to a visible ops push instead of hiding behind the band's never-abort design: after each builder band, the per-slug rc scratch files advance a fail-streak ledger and streak >=2 fires a distinct ::error:: per frozen page, a step-summary section and one W6b ops alert. Each write lane advances exactly ONE ledger (nightly -> data/ci/builder_failstreaks.json in daily.yml; asia -> data/ci/builder_failstreaks_asia.json in asia-close.yml, the CN/HK nightly); all other lanes are report-only. Born from the 2026-07-16/17 build_cycle two-night silent freeze (cycle.html served the 07-13 render while the nightly stayed green). | `scripts/check_builder_failstreaks.py` | `daily.yml/engine` (scheduled), `asia-close.yml/asia` (scheduled) | no | — | — | Observability only — never blocks a deploy; a builder red on night 1 stays ::error-only until the streak threshold (2) is met.; Absent-slug decay means a mid-band cancellation that wrote only SOME rc files restarts the missing builders' streaks at 1 (accepted; keeping absent slugs produces 30-day zombie alerts on renames). Same trade-off when one asia band dies but the other still wrote rc files.; Alert dedup rides the W6b spine's (source, type_) key; the asia lane emits type_=fail_streak_asia so the two nightlies never suppress each other inside the dedup window. |
| `ops.builder_failstreaks` | Nightly observability sentinel (added by #2704): scans the engine job's parallel-band artifact dir for per-builder failures and alerts Telegram/Discord when the same builder fails on consecutive nights. Pure telemetry — always exits 0, never blocks the deploy, writes no data/ artifacts and mutates nothing. | `scripts/check_builder_failstreaks.py` | `daily.yml/engine` (scheduled) | no | — | — | Streak state derives from band artifact logs present on the runner; a cancelled engine job (no artifacts) resets streak visibility. |
| `ops.hazard_model_freshness` | The hazard model artifact must not be stale beyond a configured threshold. Permanent warn (ops telemetry); --strict flag exists for opt-in hard mode. | `scripts/check_hazard_model_freshness.py` | `cycle-calibration.yml/calibrate` (scheduled) | no | — | — | Warn-only by design; --strict flag must be opted into explicitly. |
| `ops.ledger_heartbeat` | CSP-W6: On trading days where the nightly site republished, each registered forward ledger (risk_radar, market_state, mag7_regime, leadership_crack, deterioration_cascade) must have advanced its newest asof vs the previous run's snapshot. A stall while the site republished means stale ledger content backed fresh pages silently. Detection only (CSP-R1 forbids fail-dark): stalls append type_=ledger_stall via the W6b push_ops_alert spine and emit a ::warning:: annotation; the pipeline job always exits 0 and never gates any downstream step. Snapshot persisted in data/ci/ledger_heartbeat_state.json (sole writer). Trading-day aware (lib.nyse_calendar); idempotent per calendar date. Born from the 2026-07-16 collect-cancel-while-always() incident where the site republished on 07-15 stores undetected. | `scripts/check_ledger_advance.py` | `daily.yml/ledger_heartbeat` (scheduled) | no | — | — | Stall detection requires --render-happened flag from the workflow; if the flag is not passed (e.g. manual invocation), no stall is escalated even on a genuinely frozen ledger.; Missing ledger files are silently skipped; a ledger that has never been built produces no stall even after republish.; The tail-scan reads the last 20 rows; a ledger whose newest row is buried deeper (malformed file) may produce a stale asof reading. |
| `ops.membership_snapshot_cadence` | The basket-membership point-in-time planes (同花顺 concept baskets, US thematic baskets) must not be able to go cold without anyone being told. Both snapshot writers are CONTENT-deduped — a dated side-car is written only when membership differs from the newest one stored — which is right (membership moves on the vendor's schedule, not the trading calendar) and means a writer that is deduping and a writer that has been UNWIRED leave the identical trace on disk: nothing. That is not hypothetical. The THS PIT store held exactly two snapshots, 2026-06-30 and 2026-07-08, while `scripts/build_baskets_china_ths.py --snapshot` ran green ~35 nights in a row: the step was healthy, its INPUT was frozen, because the scraper (collectors/china_ths_concepts.py) and the seeder (scripts/seed_china_ths_baskets.py) appeared in NO workflow at all and the 2026-07-08 file was a hand-run bundled with a feature commit. The tripwire therefore grades three independent axes: cadence (the writer's own `_cadence.json` liveness stamp, rewritten on every run INCLUDING the dedup skip), source (a THS scrape receipt with complete:true inside the weekly budget — the axis that sees a live writer on a frozen input), and coherence (the PIT parquet's newest snapshot_date must not lag the newest dated side-car, catching a backfill that stopped running). | `scripts/check_membership_snapshot_freshness.py` | `asia-close.yml/asia` (scheduled), `daily.yml/engine` (scheduled) | yes | — | — | The source axis is audited for the THS suite only. The US suite's membership.json is hand-curated, so "the source has not moved this month" is its normal state, not a fault — only the writer's cadence and the side-car→parquet coherence are graded there.; Budgets are calendar-day, not session-anchored: 4 days for the writer's cadence stamp and 10 days for a complete THS scrape (weekly cadence plus one missed week of slack). A fresh freeze is therefore silent for its first few days by design, and a run of holidays widens that window.; A MISSING store, receipt directory or parquet is INDETERMINATE (one ::notice), never a breach — sparse agent checkouts carry no data/ and a suite's first run has no stamp by construction. So this guard cannot distinguish "never wired" from "not checked out"; it only catches a plane that once ran and stopped.; The curated CN suite (baskets_china) is not audited: it shares the THS suite's writer and lane, so a cadence breach there would be reported twice for one cause. |
| `ops.nw_health_escalation` | Persistent Neural Web degradation must escalate to a visible ops push: >=3 consecutive degraded nightly runs (streak keyed on run date, NOT the data-vintage as_of, which freezes when upstream data is stale) fires a maintenance-language alert through the W6b push spine (nw_health lane). Born from the 5-night silent cortex outage 2026-07-04..09. | `scripts/check_nw_health_escalation.py` | `daily.yml/engine` (scheduled) | no | — | — | Detects persistent degradation only (>=3 nights); single-night failures remain ::warning-only by design (fail-open law RUL-LIVE3).; Reads the engine-phase health core; same-night cortex status arrives via cortex_source=previous_run, so cortex-only outages fire on night 4 at the latest rather than night 3. |
| `ops.price_store_freshness` | The regime engine must not silently compute canonical state from a price store missing the latest completed NYSE session. The gate compares the last SPY daily close against lib/nyse_calendar's expected session — an exchange-calendar reference with zero data dependencies, so it still fires when the WHOLE nightly collection push dies and every committed store agrees on the stale date (2026-07-07 incident: run 28845154707's collect push died 5x pre-#1823; the engine persisted a faithful-but-stale RISK_OFF the live feed carried all session). | `scripts/check_price_store_freshness.py` | `daily.yml/engine` (scheduled), `engine-render.yml/engine-render` (render) | yes | — | — | Unscheduled one-off closures (mourning days, disasters) must be appended to lib/nyse_calendar.ONE_OFF_CLOSURES when announced; a missing entry costs a false red step + honest stale stamp — and the intraday fast-path's refresh_regime_if_stale re-pulls yahoo (keyless, ~16s) and logs store-behind-market on every ~30-min tick until the date is listed — never a silent wrong fresh.; continue-on-error by design — the engine job's if:always() philosophy (a scraper failure must still produce a dashboard from cached data); the teeth are the --heal yahoo re-pull in the daily lane, the freshness.stale stamp engine.market_state.persist writes into the canonical snapshot, and the no-regress refusal to clobber a fresher one.; engine-render wiring is check-only (that lane is network-free); the healing lanes are daily.yml (--heal) and the intraday fast-path's refresh_regime_if_stale (calendar-aware market reference). |
| `ops.surface_freshness` | First-class display surfaces (allocation, baskets, oracle_state, sector_pulse) must not silently serve stale as_of dates. The sentinel compares each artifact's as_of stamp against lib/nyse_calendar's expected session and prints a ::warning:: annotation per stale surface (2026-07-06→09 incident: data/allocation froze for 3 sessions inside an exit-0-always sub-build while the allocation page kept rendering the old leader; FTR masterplan FT-R8). | `scripts/check_surface_freshness.py` | `daily.yml/engine` (hook), `engine-render.yml/engine-render` (hook) | yes | — | — | Invoked from inside scripts/build_baskets.py main() (the build_allocation hook pattern — no dedicated workflow step; PAT lacks workflow scope), so wiring is lane:hook via the workflows that run build_baskets.; Warn-only by design (exit 0 always) — a stale surface must not block the rest of the site build; the teeth are the "::warning::" annotation plus the site/allocationdata/freshness.json stale stamp that W3 renders as a visible STALE chip. |
| `ops.tushare_plane_freshness` | The token-gated Tushare stores (flow_hist, moneyflow) must not be able to go cold without anyone being told. collectors/tushare_client.py returns None and never raises for every failure mode it has (no token, endpoint error, access denied, credits short, empty response); its callers omit the leg rather than write a fabricated zero; and asia-close's collect step is explicitly "graceful degradation — never fails on one source". The union of those three individually correct decisions is that the entire Tushare plane can die with every signal staying green: the workflow succeeds, the page rebuilds on schedule, and it prints a three-week-old as-of date in exactly the same confident type as a live one (2026-07-24→08-06 incident: data/tushare/flow_hist.parquet and moneyflow.parquet both froze at 2026-07-24 and were still rendered on flow_velocity.html on 2026-08-06 — thirteen days — while china_lhb, filled by the SAME adapter list in the SAME run, stayed current). The tripwire compares each store's newest trade date against the exchange calendar's expected last session — deliberately NOT against the store's own newest row or a sibling store, because a frozen feed is perfectly self-consistent and reads "fresh" during a total outage, while the calendar keeps advancing whether or not the collector ever runs again. | `scripts/check_tushare_freshness.py` | `asia-close.yml/asia` (scheduled) | yes | — | — | There is no mainland A-share calendar in lib/, so HKEX is used as the closest proxy; it shares most holidays but diverges on mainland-only closures — Golden Week (Oct 1-7) most notably, where HKEX trades and Shanghai does not — and can therefore warn benignly for a few days each October. Priced in on purpose, since this emits a warning and never a failure: a benign October warning is a much cheaper error than silently serving stale flow data for a fortnight.; Audits only the two token-gated stores it names (tushare/flow_hist.parquet, tushare/moneyflow.parquet); other stores the same adapter list fills (e.g. china_lhb) are not covered by this check.; The threshold is 3 sessions behind — sized to absorb a long weekend plus one public holiday and the lane's own once-a-day gate — so a fresh freeze stays silent for its first three sessions by design. |

## Manual laws

| Law ID | Summary | Enforced by | CI wiring | Selftest | Allowlist | Ratchet | Known limits |
|---|---|---|---|---|---|---|---|
| `epistemics.qledger_metric_validity` | Three readings of the Universal Scoreboard produce impressive numbers that do not mean what they appear to mean, and all three are silent. (V1) grades.excess is RAW subject-minus-control return, not direction-signed — hit carries direction — so a correct bearish call contributes NEGATIVE excess and a pooled signed excess_mean over a mixed-direction family measures universe drift, not skill. (V2) a direction==0 claim asserts salience, not direction, so hit is undefined and a hit rate over such a family is a ratio over an empty verdict set; 71% of the live corpus is salience. (V3) grading emits every shorter in-scope horizon as it matures, so a family declaring horizon_d=63 accrues 5d/21d rows long before its own ruler resolves — reading those as its record is DNR:KILL-OFFHORIZON-VERDICTS. | `scripts/check_qledger_metric_validity.py` | — | yes | — | — | Audits the STORE, not the emitters. A consumer that recomputes a pooled signed excess in its own code is invisible here; T3 of the V1 plan retires the known emitter (scripts/grade_qledger.py per-family excess_mean).; The default run is advisory (exit 0); only --strict exits non-zero. CI runs the guard's --selftest and its pytest suite (legacy job qledger-cluster-honest-ci), which proves the auditor works — it does NOT run the audit against the real corpus as a gate. ci_wiring is therefore [] on purpose: the guard is unit-proven in CI but not yet enforcing.; V3 compares the family's declared ruler against horizons present in the grade corpus; it cannot distinguish "not yet matured" from "grading broken". Both are correctly reported as zero verdicts at the ruler.; An absent store exits 0 with a ::notice. Sparse agent worktrees have no data/ on disk while the paths are tracked in HEAD. |
| `governance.adjudication_packet` | Post-Fable succession bench: adjudication packets must be schema-complete per tier and pass hard blockers HB-1..13 — effective-tier law kills omission/understatement laundering, tier-2 needs a refute-stance panel (>=2) plus operator sign-off, authority/privacy evidence floors (HB-11/12) are machine-enforced, and invariant classes (A7 origination, trial-budget laundering, null-history deletion) are never approvable (RUL-SUCC-1..12). | `scripts/check_adjudication_packet.py` | — | yes | — | — | Not yet wired into pr_ci — RUL-SUCC-12 defers CI wiring to succession bench W1 (dag-conformance drift risk on same-day workflow edits). Until then run manually or via tests/test_adjudication_packet.py.; Validates the queue file only when invoked; opus-driven Research Factory gates invoke it as a subprocess via scripts/research_factory_decide.py. |
| `ops.extraction_drift` | Re-score the extraction anchor set against the current LLM model. Gate: field_agree_rate >= 0.85. Requires Anthropic API key; run at model upgrades. | `scripts/check_extraction_drift.py` | — | no | — | — | Requires ANTHROPIC_API_KEY; cannot run in CI without secrets exposure.; --dry-run validates structure only; does not test model agreement. |
| `ops.live_worker` | Pre-flight probe for Cloudflare live-quotes worker deploys. Verifies that the worker is reachable and returning expected schema. | `scripts/check_live_worker.py` | — | no | — | — | Requires live Cloudflare endpoint; not runnable in CI. |
| `site.stretch_oracle_disclosure` | The per-ticker drawer renders TWO independent answers to "has this name already run" — ladder.alignment.overextended (a 4-leg, oscillator-dominant entry-timing brake) and entry_signal.status == "extended" (a relabel of ladder.entry.urgency == "caution"). They disagree on roughly 38% of names and that divergence is LEGITIMATE (a measurement versus a bucket label), so this guard does not force them equal. What it enforces is that the divergence stays DISCLOSED: every flagged name must carry the leg that actually fired, so no consumer has to guess a cause. HARD invariants: overextended=True implies a non-empty overextended_legs; overextended=False implies an empty one; overextended_basis equals overextension_basis(overextended_legs); and an oscillator-only basis may not coexist with a qualifying distance reading (producer and disclosure computed from different inputs). | `scripts/check_stretch_oracle_contract.py` | — | no | — | — | Needs a BUILT stock store to read; it cannot run against a bare checkout, which is why it carries no ci_wiring entry of its own.; The O1 x O2 confusion counters are annotation-only and never fatal; only the four disclosure invariants exit 1. |

## Discipline laws

| Law ID | Summary | Enforced by | CI wiring | Selftest | Allowlist | Ratchet | Known limits |
|---|---|---|---|---|---|---|---|
| `data.append_only_assertions` | A historical assertion over an append-only store must not become false merely because a VALID NEW ROW was appended. The rule is MONOTONICITY, not equality: `len(rows) == 28` and `ledger_ids <= graded_ids` are fragile; `>= floor`, `x in store`, `set(keys) == FIELDS`, a constant-bounded prefix slice, a per-row schema contract, and two reads of the same store in one run are safe. Covers BOTH shapes the defect takes — an assert over store content, and a CI gate whose failing exit code is a function of it. | `scripts/check_append_only_assertions.py` | — | yes | — | — | PRODUCER SIBLING (false positive). A store compared to a receipt written by the SAME producer run is safe because both move in lockstep, but is reported. Observed at tests/test_chronicle.py:2294, where the manifest records rows=5051 and events.jsonl is 5051 lines. Closing it needs a `producer:` sibling lookup in config/synapse.yml.; WITHIN-RUN VIA AN INTERMEDIARY (false positive). When one side reaches the store through a call the scanner cannot see into, only the other side carries taint. Observed at tests/test_personality_relief_hazard.py:400, where `state` comes back from prh.update() over the same ledger being counted.; FROZEN FIRST ROW (false positive). `registration, *events = rows` makes `registration` row 0, which an append cannot change; reported anyway at tests/test_personality_relief_hazard.py:357. The BYTE form of the same idea (`raw[:50_790]`) IS recognised and is not reported.; UNREGISTERED STORE (false negative). A live append-only store missing from config/synapse.yml is invisible. data/basket_turn/ledger.jsonl is tracked in git and pinned by `assert len(live) == 8` at tests/test_basket_membership_stamps.py:259, and this guard reports zero findings in that file because synapse.yml carries data/us_basket_turn/ and data/china_basket_turn/ but not that path.; ROW-GRAIN MEMBERSHIP (false negative, deliberate). The per-row carve-out cannot distinguish a schema contract (`row["kind"] == "event"`, legal) from an enumeration of existing ids (`row["id"] in FROZEN_IDS`, fragile) — they are lexically identical. The carve-out wins. All four motivating defects are whole-store, and without it the guard reports about 5x the findings it can justify.; Scope is tests/ and scripts/, JSONL stores only, and the exit-code family is scoped to scripts/check_*.py plus registered check_scripts. A path arriving via a fixture, another module's constant, or a function parameter is not resolved. |
| `epistemics.display_only_until_gauntleted` | All signals/scores are display-only until formally gauntleted. Partially mechanized by authority.* and nw.factor_boundaries guards (see those law_ids). | — | — | no | — | — | Partially mechanized by authority.* and nw.factor_boundaries; the rest is enforced by review. |
| `epistemics.nulls_are_printed` | Null results must be printed and surfaced — never hidden. A null finding that is not displayed is an epistemics violation. | — | — | no | — | — | Enforced by code review and report standards; no static scan. |
| `epistemics.preregistered_gates` | All statistical experiments must use pre-registered gates. Partially mechanized by epistemics.trial_registration and epistemics.no_literal_ntrials. | — | — | no | — | — | Partially mechanized; pre-registration of the hypothesis itself is by review. |
| `epistemics.research_docs_not_scanned` | research/*.md files are NOT scanned for promotion language or the word "validated". "Validated" is legitimate scientific vocabulary in research verdicts. The enforcement boundary is user-facing surfaces only (templates/, site/). FROZEN RULING: Fable 2026-07-06. | — | — | no | — | — | Intentional carve-out; research verdicts use "validated" as scientific vocabulary. Enforced at the scanner level in check_validated_claims.py. |
| `git.fresh_branch_squash_flow` | Branch off fresh origin/main; finish via commit→push→PR→same-day squash-merge. Stash stack is repo-global — never bare git stash/pop. Law source: CLAUDE.md. | — | — | no | — | — | Enforced by team convention and code review; no static scan. |
| `ledgers.nightly_sole_advancer` | The nightly pipeline is the sole advancer of forward ledgers. Intraday lanes must discard any data/ writes. Law source: CLAUDE.md. | — | — | no | — | — | Enforced by nightly pipeline architecture and code review; no static scan. |
| `nw.article1.no_llm_origination` | LLMs may only de-escalate calibrated keys — never originate signals, scores, or escalations. Article 1 of the Neural Web constitution. | — | — | no | — | — | Enforced by architecture and review; no static scan exists for this constraint. |

## Known Spurious laws

| Law ID | Summary | Enforced by | CI wiring | Selftest | Allowlist | Ratchet | Known limits |
|---|---|---|---|---|---|---|---|
| `ci.workers_builds_spurious` | The "Workers Builds: macro" red X on PRs is a known external false-red from Cloudflare Workers. It must NOT block merges. Law source: CLAUDE.md. | — | — | no | — | — | — |

## Known-spurious CI statuses

These checks are recorded in the registry to document known external false-reds. They must never block merges.

- **ci.workers_builds_spurious**: External Cloudflare Workers build status; not under our control. Never block a merge on this red X.

## Registry ownership

Any agent adding a new `scripts/check_*.py` must register it in `config/house_law_checks.yml` before the PR can merge. The meta-guard (`scripts/check_house_law_registry.py`) runs in CI and blocks merges of unregistered guard scripts.

Severity DOWNGRADES (e.g. hard → warn) or entry REMOVALS require an explicit operator/Fable ruling recorded in the PR description. Removing an entry without a ruling is a process violation.

_Generated from `config/house_law_checks.yml` (updated 2026-08-16) by `python3 scripts/check_house_law_registry.py --emit-docs`._
