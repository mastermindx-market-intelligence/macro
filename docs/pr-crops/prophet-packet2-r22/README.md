# Prophet Packet 2 — R22 runtime and copy qualification

Operation: `prophet-experience-first-vertical-20260924-sol-001`.
Existing implementation carrier: macro#7237. Parent acceptance: macro#6817.
This is source-qualification evidence, not another programme, release approval,
production authentication receipt or trading-permission decision.

## Recovered source and bounded change

Protected procedure: Mastermind `4c6b206d3fb7fbc6d077faf61ae361bedf259925`,
Skillpack `mastermind.sol_skillpack.v1` / `1.0.1` / bootstrap `1`.
The R21 semantic source `e59a7baeaa1ab9542f65e73eda5f43ca13c14f34` and existing
integrated qualification base `549215c3f4bf84d4af999302dc64f900971a89b8` were
preserved. The qualification workspace was not recreated; R20/R21 were not replayed.
The withdrawn designer header reservation is not a release dependency.

F1 limits enhancement to the existing opted-in US standouts/candidate-pool hosts.
F2 validates native link, same-row identity, source generation and inert Table
body before allocating the dialog; native default is cancelled only after a
successful enhancement. The existing exact-link restriction is not broadened.
The same original nodes return to their original live owner; replaced rows are
not resurrected and no detail fetch, persistence or entitlement owner is added.

The bounded design-copy return preserves supplied localized headlines, explicitly
labels original-language fallback/reasons, gives known stages neutral reader labels,
and retains raw stage/lane fields inside the existing closed disclosure. Entry stop,
base invalidation and chase boundary retain separate meanings and exact values.
Targets, holding horizon, reward/risk, win probability and plan linkage remain
unavailable where not supplied. No new strategy binding or plan/position action is added.

## Evidence and reproduction

`qualification.json` binds actual source hashes, test counts, native browser results,
retained R21 evidence and the remaining gates. The browser receipts below are from
local controlled responses with test-only authentication; none proves production
account access. Source-row screenshots use the frozen repository snapshot and are
not current recommendations. The desktop and mobile renders were inspected.

The checked-in runtime probe reproduces both defects on the immutable R21 parent
and checks the repair in both template and published-asset controller copies:

```sh
python3 docs/pr-crops/prophet-packet2-r22/reproduce_runtime.py \
  --repo . --out /tmp/prophet-packet2-r22-runtime
python3 -m pytest -q tests/test_prophet_card_shared.py
```

The probe uses the existing Python Playwright installation and aborts all network
requests. It does not install dependencies, read credentials or modify application
source. Native P0B proof uses the unchanged repository renderer and verifier;
historical baselines and screenshots are retained, not replaced by fabricated hashes.

## Acceptance boundary

The source candidate still requires independent exact-head review, required hosted
CI/security and fresh protected-base compatibility before a gated merge/release.
Authenticated published-data-to-reader proof remains separate from these local tests.
Consult the current #7237 continuation return for the exact candidate/publication
identity and remaining owner actions; a proof artifact is not a merge instruction.
