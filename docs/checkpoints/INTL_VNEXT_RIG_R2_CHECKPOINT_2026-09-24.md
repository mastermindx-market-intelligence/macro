# International Markets vNext — RIG R2 Continuation Checkpoint

**Operation:** `intl-vnext-implementation-20260924-sol-001`
**Mission complete:** false
**Disposition:** `CHECKPOINTED_CONTINUATION` — context-pressure phase boundary after independent design review
**Chairman authority:** current live instruction approved follow-up work and end-to-end implementation
**Protected Skillpack pin:** `1a7d400294b0d37c460b963b8865b40a23173b58`
**Repository:** `mastermindx-market-intelligence/macro`
**Worktree:** `/Volumes/Mastermind/worktrees/intl-vnext-implementation-20260924-sol-001`
**Branch:** `sol/intl-vnext-implementation-20260924`
**Current branch head before this checkpoint commit:** `432ae4addecf683da613a47222b6c0eae2f266c0`

## Outcome

Replace the current `intl.html` dashboard museum with one coherent international operating flow:

`Global Call → Global Pulse → Rotation & Turns → Transmission & Fragility → Country Inspector → Desk Posture / Deep Desks`

The final route must preserve accepted international intelligence, use only existing engine authority, keep stocks mode intact, support dark/light and EN/ZH at desktop/mobile, pass RIG, ship through PR/CI, and receive real-browser production proof.

## Canonical source and collision state

- Implementation base: macro `main` at `48dc431c2d0ece76e6e1b5510ff1f57f7dad1d8b`.
- Open adjacent PR #7872 touches international risk-engine contracts but not `templates/intl.html.j2`, `scripts/build_intl.py`, or `site/intl.html`; preserve its ownership and do not redefine its contracts.
- `templates/intl.html.j2` also owns `intl_stocks.html` under `mode == "stocks"`; production work must prove stocks-mode non-regression.
- No source-writer collision, active worker, or `EFFECT_UNKNOWN` remains at this boundary.
## Material effects and DO_NOT_REDO

1. Original approved Paper composition was frozen at commit `f3873dcbda51f42e3e7a5c6bec51acf5e6a446df`; it is superseded semantically but remains useful visual lineage.
2. Valid production-before evidence was captured by scrolling the real route so content-visibility sections painted: `baseline-r2-*` across desktop/mobile × dark/light × EN/ZH. Commit `6488b8b6bc2421083af8a700192eabd1bdf0c03d`.
3. First blocked RIG cycle and repair contract were persisted at commit `8209b708c69e8d8f9341526b38fc036fb35d7d1e`.
4. Corrected standalone reference `mockups/refs/institutionalize/intl/reference-r2.html` plus 8 default-state and 4 forced-state screenshots was frozen at artifact commit `62d710eedb9e5cc2a9d9f3e17fc02cc302de39f9`.
5. A RED 390px browser proof found page width `581 > 390`; root cause was grid-item automatic minimum width around internally scrolling tables. One containment repair added `min-width: 0` to grid children. GREEN proof: all 8 state cells render with zero page overflow and zero console errors; mobile widths are exactly 390px.
6. Interaction proof passed at 1440 and 390: theme, language, forced states, ARIA tabs, keyboard ArrowRight, and zero external dependencies.
7. RIG manifest/proposal were rebound to the corrected freeze at commit `432ae4addecf683da613a47222b6c0eae2f266c0`.

**Do not recreate** the worktree, branch, Paper design, baseline matrix, reference contract, R2 HTML, screenshot matrix, overflow investigation, frozen commits, or either completed R2 critic pass.

## R2 independent review result

Both independent Opus critics returned `BLOCK` against artifact SHA `62d710eedb9e5cc2a9d9f3e17fc02cc302de39f9`.

Raw receipts are preserved at:

- `research/reference_integrity/intl-vnext-20260924/reviews/r2_pass1_product_regression.raw.md`
- `research/reference_integrity/intl-vnext-20260924/reviews/r2_pass1_visual_taste.raw.md`

Because the bytes require material change, do **not** run second-pass rationale on R2. Build R3, recut a new immutable SHA, then perform fresh pass-one quarantine reviews against that SHA.
## R3 repair obligations

### Restore product capability and reachability

- `PRC-R2-001`: restore a real Dollar Drivers desk or an honest reachable relocation; the safety-bid chip is insufficient.
- `PRC-R2-002` / `VTC-R2-010`: replace self-links with real routes/fragments and prove central banks, credit/bonds, curves, correlation, regime map, and rankings remain reachable.
- `PRC-R2-003`: preserve the canonical site shell and Chairman-directed cross-market overview in the production contract; reference harness controls are not product controls.
- `PRC-R2-004`: redesign empty/error as organ-local fail-open states; never hide independently healthy intelligence.
- `PRC-R2-005`: cover or explicitly disclose the full ten-market turn-state universe, including US/CN/HK.
- `PRC-R2-006`: restore comparable macro rows for all seven economies rather than three one-at-a-time inspectors.
- `PRC-R2-007` / `VTC-R2-005`: restore horizon selection and change-shape evidence; do not replace every time series/spatial plot with static tables.
- `PRC-R2-010`: restore complete directed-pressure coverage or an explicit count plus reachable full view.
- `PRC-R2-011`: provide a separate non-regression reference/evidence path for `mode == "stocks"`; do not redesign it incidentally.

### Correct semantics and evidence

- `PRC-R2-008` / `VTC-R2-004`: remove the equal-to-base dip-odds column from at-rest display.
- `PRC-R2-009` / `VTC-R2-003`: either expose the complete rotation score inputs or present rank as an engine result without a misleading partial derivation.
- `PRC-R2-012`: correct fragility universe accounting; Australia cannot disappear from the honest-null receipt.
- `PRC-R2-014`: reconcile Australia rounding or disclose display rounding.
- `PRC-R2-015`: distinguish equity-FX contribution from currency return wherever two FX numbers differ.
- `PRC-R2-016`: use one accepted state vocabulary on cards and tables.
- `PRC-R2-017`: define the regime hazard token `h` or remove it.
- `VTC-R2-006`: remove unsourced sender/pressure bar magnitudes; no decorative quantitative bars.
- `VTC-R2-011`: print one authoritative drag list in the hero.

### Correct visual-system behavior

- `VTC-R2-001`: use canonical `--ink-up/down/warn` mixed contrast on light surfaces; raw direction tokens fail small-text contrast.
- `VTC-R2-002`: state/severity colors must never invert in ZH; separate direction ink from status ink.
- `VTC-R2-007`: remove Latin tracking from every CJK label and keep microcopy at or above the 10px ramp floor.
- `VTC-R2-008`: make the 390 harness preview trigger the actual responsive composition or remove that preview control.
- `PRC-R2-013` / `VTC-R2-009`: add adjudication evidence for hidden inspector panels and light forced states; avoid whole-page stale opacity.
- `PRC-R2-018`: repair RRG pill contrast in both themes.
- Preserve R2 strengths: engine-owned 66/100 verdict, bounded authority, causal disclaimers, methodology receipts, true ZH copy, 8-cell matrix, six-act rhythm, and honest nulls.
## Capability delta at this boundary

**Before:** the approved Paper composition had not been tested against the real `intl.html` capability surface, authority model, responsive states, or independent regression/taste review.
**After:** a current production baseline, immutable R2 candidate, full default-state evidence matrix, browser interaction proof, exact RIG block receipts, and falsifiable R3 repair contract are durably recoverable on one branch. This is a **records-and-design-validation delta only**; production `intl.html` remains unchanged and the replacement capability is not built.

## Unresolveds and falsifiers

- R2 is not eligible for implementation because both required critics returned `BLOCK` on its exact SHA.
- The production redesign remains `NOT_BUILT`; no PR exists and no deployment/browser acceptance is claimed.
- R3 resolves the design gate only when every blocker is repaired on new bytes, a new immutable SHA is frozen, the expanded evidence matrix is captured, and both independent pass-one critics return no surviving blocker.
- Production implementation becomes eligible only after RIG approval plus a migration packet and test-first implementation plan tied to that approved SHA.
- Completion still requires exact-head tests/CI, independent code review, merge/release evidence, and real production-path browser proof for desktop/mobile × dark/light × EN/ZH, including `intl_stocks.html` non-regression.

## Exact next action

On a fresh Sol Pro continuation, re-pin the protected Skillpack, load this checkpoint and branch head, then repair `mockups/refs/institutionalize/intl/reference-r2.html` into an **R3 reference** that satisfies every recorded R2 blocker while preserving the six-act thesis and all accepted engine authority. Freeze R3 at a new immutable commit only after fresh browser verification.

Then, in order:

1. Generate R3 evidence for desktop/mobile × dark/light × EN/ZH, all organ-local degraded states, every hidden country-inspector panel, and the separate stocks-mode surface.
2. Run two fresh rationale-quarantined independent pass-one reviews against the R3 SHA; do not reuse or amend R2 receipts.
3. If no artifact-changing repair remains, perform the permitted second-pass rationale review, adjudicate verdict/approval, and run `scripts/check_reference_integrity.py` against the exact R3 set.
4. Write the migration packet and implementation plan from the approved reference, including a capability disposition for every existing section and real destination links.
5. Implement `intl.html` test-first without changing accepted engine semantics or breaking `intl_stocks.html`; build `site/intl.html`, run the full relevant test set, open a PR, consume CI and independent review, then obtain real production browser proof before acceptance.

## Continuation boundary

`FINALIZATION_CLASSIFICATION: CHECKPOINTED_CONTINUATION`
`MISSION_COMPLETE: false`
`FINAL_CAPABILITY_STATE: NOT_BUILT`
`PRODUCTION_PROOF_STATUS: not started`
`EFFECT_UNKNOWN: none`
`ACTIVE_WORKERS_OR_WATCHERS: none`
`INTENDED_RESUME_SURFACE: fresh Sol Pro conversation in the MastermindX project, same macro branch/worktree`
`PRIMARY_RETURN_POINT: this checkpoint at its committed revision plus the exact branch head`
`INDEPENDENT_PARALLEL_ACTIONS: none; R3 design, evidence, and RIG adjudication are one coupled gate before production source work`

## What must not be redone

Do not recreate or replace the worktree, branch, Paper design, baseline screenshots, R2 reference, R2 browser proofs, R2 critics, or their findings. Do not start production implementation from the blocked R2 bytes. Do not weaken, hide, or cosmetically dismiss a blocker to make the design pass. Continue from the latest committed effect and create only the new R3 delta.
